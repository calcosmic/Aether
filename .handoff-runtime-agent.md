# Handoff: Runtime Agent

## Role

You own the Go runtime plumbing for the "Global Agents/Commands, Local-Only Repo
State" refactor.

For dummies: make the `aether` binary put shared Aether machinery in the global
hub/platform homes, and make repo setup/update leave only project-local state in
the target repo.

## Coordination Rules

- Work in `/Users/callumcowie/repos/Aether`.
- Read `.continue-here.md` first.
- The worktree is dirty with unrelated pre-existing changes. Do not reset,
  checkout, clean, or revert broad paths.
- Do not commit.
- Do not restore deleted mirror directories.
- Stay in your ownership lane so a second agent can work in parallel.
- Preserve rich command wrapper behavior. Do not flatten wrapper markdown.

## Your File Ownership

Primary runtime files:

- `cmd/install_cmd.go`
- `cmd/setup_cmd.go`
- `cmd/update_cmd.go`
- `cmd/platform_sync.go`
- `cmd/skills.go`
- `cmd/medic_wrapper.go`
- `cmd/integrity_cmd.go`
- `embedded_assets.go`

Runtime-focused tests:

- `cmd/install_cmd_test.go`
- `cmd/setup_cmd_test.go`
- `cmd/update_cmd_test.go`
- `cmd/update_roundtrip_test.go`
- `cmd/e2e_install_setup_update_test.go`
- `cmd/codex_e2e_test.go`
- `cmd/codex_instruction_hygiene_test.go`
- `cmd/medic_wrapper_test.go`

Avoid editing these unless absolutely required to unblock compilation:

- `.claude/commands/ant/*.md`
- `.opencode/commands/ant/*.md`
- `.aether/commands/*.yaml`
- `.aether/docs/**`
- `CLAUDE.md`, `AGENTS.md`, `README.md`, `.codex/CODEX.md`, `.opencode/OPENCODE.md`
- `cmd/source_check.go` and command wrapper hygiene/parity tests

Those are owned by the surface/docs agent.

## Current Runtime Reality Check

As of handoff, this command still reports stale assumptions in non-test Go:

```bash
rg -n "skills-codex|agents-claude|agents-codex|commands/claude|commands/opencode" cmd embedded_assets.go --glob '!cmd/*_test.go'
```

Known hits:

- `cmd/install_cmd.go`
  - install help still says `.aether/skills-codex/ -> ~/.codex/skills/aether/`.
  - `setupInstallHub()` still needs canonical-source hub publishing reviewed.
  - `hubExcludeDirs` has stale source-mirror comments/behavior.
- `cmd/update_cmd.go`
  - dry-run text still says update syncs agents/commands/skills/templates/docs into repos.
  - `runUpdateSync()` still needs to call the cleanup/scaffold helpers from `cmd/platform_sync.go`.
  - stale hub checks still count `system/skills-codex`.
- `cmd/setup_cmd.go`
  - setup still loops over `repoSyncPairs()` but does not clearly run the new local-only scaffold/cleanup behavior.
- `cmd/skills.go`
  - still scans repo-local `.aether/skills-codex`; this must go.
- `cmd/medic_wrapper.go`
  - still checks deleted source mirrors `.aether/agents-claude` and `.aether/agents-codex`.
- `cmd/integrity_cmd.go`
  - hub checks still mention `commands/claude`, `commands/opencode`, and `skills-codex`; hub paths may be valid if retained as hub layout, but source-mirror assumptions must not leak into repo-local behavior.

Additional urgent runtime regression:

- `aether queen-compose` is not currently registered.
- `go run ./cmd/aether queen-compose --help` returns `unknown command "queen-compose"`.
- Current `cmd/queen.go` does not contain a `queen-compose` command.
- The missing `.aether/commands/claude/queen-compose.md` and `.aether/commands/opencode/queen-compose.md` mirror files are expected under this refactor. Do not restore those mirror directories.
- The Go command and tests must be restored/reimplemented.

Additional same-day salvage inventory:

- Read `.handoff-salvage-inventory.md` before editing runtime code.
- Init synthesis is still working and should not be disturbed.
- Core Oracle/RALF exists, but two git stashes contain a likely missing
  `--scope` feature:
  - `stash@{0}` for `cmd/compatibility_cmds.go`
  - `stash@{1}` for `cmd/oracle_loop.go`
- Codebase graph files survive untracked:
  - `cmd/codegraph.go`
  - `pkg/codegraph/codegraph.go`
  - `pkg/codegraph/codegraph_test.go`
  but colonize integration likely is not wired.
- Reference library/wrappers survive untracked, but `reference-match` is
  currently unknown and `cmd/references.go` is a stub.

## Required Behavior

Global/shared assets live in the hub and platform homes:

- `~/.aether/system/` contains shared Aether companion sources: agents, commands,
  shipped skills, templates, docs, utils, workers.md, exchange, references.
- Platform global homes receive installed platform assets:
  - `~/.claude/agents/ant/`
  - `~/.claude/commands/`
  - `~/.config/opencode/agents/`
  - `~/.config/opencode/commands/ant/`
  - `~/.codex/agents/`
  - `~/.codex/skills/aether/`

Target repos keep only local/generated state:

- `.aether/data/`
- `.aether/dreams/`
- `.aether/oracle/`
- `.aether/locks/`
- `.aether/QUEEN.md`
- `.aether/skills/` for custom repo skills only
- `.aether/.gitignore`

Target repos should not receive copied shared assets:

- agents
- commands
- shipped skills
- templates
- docs
- utils
- workers.md
- exchange
- references

Deleted source mirrors are intentional:

- `.aether/agents-claude/`
- `.aether/agents-codex/`
- `.aether/commands/claude/`
- `.aether/commands/opencode/`
- `.aether/skills-codex/`

## Implementation Plan

1. Inspect first, then patch.
   - Run `git status --short`.
   - Run the `rg` command above.
   - Read the actual functions before editing; do not trust prior notes blindly.

2. Finish install/publish hub publishing.
   - `installSyncPairs()` already points Codex skills at `.aether/skills`; verify it still installs correctly to `~/.codex/skills/aether/`.
   - Update install help text to say `.aether/skills/ -> ~/.codex/skills/aether/`.
   - Update `setupInstallHub()` so it publishes canonical sources directly:
     - `.claude/agents/ant` -> hub Claude-agent location
     - `.claude/commands/ant` -> hub Claude-command-wrapper location
     - `.opencode/commands/ant` -> hub OpenCode-command-wrapper location
     - `.opencode/agents` -> hub OpenCode-agent location
     - `.codex/agents` -> hub Codex-agent location
     - `.aether/skills` -> hub shipped-skill location
   - Keep backward compatibility in hub layout if existing runtime checks need it, but do not reintroduce repo-local or source-repo mirrors.
   - Ensure `.aether/` hub sync excludes deleted source mirror dirs and local-only dirs.

3. Finish setup/update local-only repo behavior.
   - `aether setup` and `aether lay-eggs` should create local scaffold and preserve state.
   - `aether update` should:
     - create local scaffold if needed,
     - prune stale generated repo-local platform assets safely,
     - prune shipped repo-local skills only when they match shipped content or when force semantics allow it,
     - preserve custom repo skills,
     - preserve `.aether/data`, `.aether/dreams`, `.aether/oracle`, `.aether/locks`, and `.aether/QUEEN.md`,
     - not copy shared assets into target repos.
   - Wire the helpers already added in `cmd/platform_sync.go`:
     - `ensureRepoLocalScaffold`
     - `pruneLegacyRepoPlatformAssets`
     - `pruneShippedRepoSkills`
     - managed `.aether` cleanup filters.

4. Fix skills scan roots.
   - Remove repo-local `.aether/skills-codex`.
   - Keep repo `.aether/skills` only as custom/local skill source when appropriate.
   - Keep hub/global shipped skills from the final hub location.
   - If hub `skills-codex` remains as a compatibility alias, keep it hub-only and document why in code/tests.

5. Fix medic/integrity runtime checks.
   - Remove source mirror parity checks from `cmd/medic_wrapper.go`.
   - Keep checks for canonical source dirs and hub/global publish integrity.
   - Ensure `cmd/integrity_cmd.go` is checking hub/global completeness, not deleted repo source mirrors.

6. Restore/reimplement `queen-compose`.
   - Add/register `aether queen-compose` in `cmd/queen.go` or a clearly named adjacent Go file.
   - It should create or improve repo-local `.aether/QUEEN.md`, not hub `~/.aether/QUEEN.md`.
   - Use the existing untracked wrapper intent as behavior guidance:
     - `.aether/commands/queen-compose.yaml`
     - `.claude/commands/ant/queen-compose.md`
     - `.opencode/commands/ant/queen-compose.md`
   - Preserve existing `.aether/QUEEN.md` content when updating.
   - Add/restore tests for command registration, file creation/update, and dispatch/platform parity if they were lost in the other terminal.

7. Repair runtime tests without deleting meaningful coverage.
   - Ensure tests prove install publishes canonical source assets to hub/global homes.
   - Ensure setup/update do not copy shared assets into target repos.
   - Ensure stale generated repo-local platform assets are pruned.
   - Ensure custom repo skills and local state survive.
   - Ensure source mirrors are not required.

## Verification

Run at minimum:

```bash
gofmt -w cmd/install_cmd.go cmd/setup_cmd.go cmd/update_cmd.go cmd/platform_sync.go cmd/skills.go cmd/medic_wrapper.go cmd/integrity_cmd.go cmd/queen.go embedded_assets.go
go test ./cmd -run TestDoesNotExist
go test ./cmd -run 'TestInstall|TestSetup|TestUpdate|TestMedic|TestCodex|TestQueen'
```

If focused tests pass, report whether broader tests were run:

```bash
go test ./...
```

Only suggest `go test ./... -race` after focused tests are green.

## Exact Prompt

```text
You are Runtime Agent A in /Users/callumcowie/repos/Aether. Read .continue-here.md and .handoff-runtime-agent.md first.
Also read .handoff-salvage-inventory.md before deleting, restoring, or cleaning anything.

Own only the Go runtime plumbing and runtime-focused tests listed in .handoff-runtime-agent.md. Do not edit wrapper markdown/docs unless compilation is blocked. Do not restore deleted mirror directories. Do not reset or clean the dirty worktree. Do not commit.

Goal: finish the runtime side of "Global Agents/Commands, Local-Only Repo State". Shared Aether assets must be installed globally/hub-side; target repos must keep only local state. Complete install/setup/update/skills/medic/integrity wiring, restore the missing aether queen-compose runtime command, preserve repo-local data/custom skills, and update focused Go tests without deleting meaningful coverage.

Extra salvage goals: preserve init synthesis, inspect and deliberately restore/defer the stashed Oracle scope feature, wire or restore reference commands/injection, and wire codebase graph generation into colonize if that was intended. Do not discard untracked codegraph/reference/to-prd/shelf/source-check/recipes files without classifying them.

Start by running:
git status --short
rg -n "skills-codex|agents-claude|agents-codex|commands/claude|commands/opencode" cmd embedded_assets.go --glob '!cmd/*_test.go'

Then implement the runtime plan in .handoff-runtime-agent.md and run the listed focused tests. In your final report, list changed files, tests run, whether queen-compose is registered and creates/updates repo-local .aether/QUEEN.md, remaining blockers, and any assumptions about hub skill layout compatibility.
```
