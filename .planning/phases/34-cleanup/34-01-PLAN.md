---
phase: 34-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/data/backups/cleanup-{timestamp}/pending-decisions.json
  - .aether/data/backups/cleanup-{timestamp}/COLONY_STATE.json
  - .aether/data/backups/cleanup-{timestamp}/pheromones.json
  - .aether/data/backups/cleanup-{timestamp}/constraints.json
  - .aether/data/backups/cleanup-{timestamp}/session.json
  - .aether/data/backups/cleanup-{timestamp}/flags.json
autonomous: false
requirements:
  - R056
  - R057

must_haves:
  truths:
    - ".aether/data/ is backed up to .aether/data/backups/cleanup-{timestamp}/ before any cleanup"
    - "Both candidate commits (98cda871, 4bbb9273) are evaluated for value, merge-readiness, and safety"
    - "preserve/ branches exist for commits not integrated into main"
  artifacts:
    - path: ".aether/data/backups/cleanup-*"
      provides: "Timestamped backup of all colony data files"
      contains: "pending-decisions.json"
    - path: "preserve/*"
      provides: "Git branches pointing to candidate commit SHAs"
      contains: "preserve/"
  key_links:
    - from: "preserve/claude-dispatch-ux"
      to: "98cda87164c2741df9f777127c201313acdca817"
      via: "git branch pointing to SHA"
      pattern: "98cda871"
    - from: "preserve/intent-workflows"
      to: "4bbb9273379d31721f7cbdf2466a7189626f3109"
      via: "git branch pointing to SHA"
      pattern: "4bbb9273"
---

<objective>
Backup colony data and evaluate two candidate commits for preservation or integration before any cleanup begins.

Purpose: Per D-01, the 2 candidate commits (claude-dispatch-ux, intent-workflows) must be reviewed for value, merge-readiness, and safety before any worktree or branch deletion. Per D-07, .aether/data/ must be backed up before modifications.

Output: A timestamped backup of colony data files, and either preserve/ branches or integrated code for the 2 candidate commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-cleanup/34-CONTEXT.md
@.planning/phases/34-cleanup/34-RESEARCH.md

<interfaces>
<!-- Colony data files to back up -->
Files in .aether/data/ that must be copied:
- pending-decisions.json (27 flags, 18 unresolved)
- COLONY_STATE.json (colony state with phase tracking)
- pheromones.json (active signals)
- constraints.json (focus areas)
- session.json (session state)
- flags.json (legacy flag storage, if exists)

<!-- Backup pattern from cmd/init_cmd.go:100-108 -->
```go
backupDir := filepath.Join(dataDir, "backups", fmt.Sprintf("cleanup-%s", time.Now().Format("20060102-150405")))
os.MkdirAll(backupDir, 0755)
copyFile(srcPath, filepath.Join(backupDir, filename))
```

<!-- Candidate commits -->
Commit 1: 98cda87164c2741df9f777127c201313acdca817 (claude-dispatch-ux-20260421-1)
- 10 files, 377 insertions, 40 deletions
- Files: cmd/codex_build.go, cmd/codex_build_progress.go, cmd/codex_build_test.go, cmd/codex_build_worktree.go, cmd/codex_continue.go, cmd/codex_continue_test.go, cmd/codex_visuals.go, cmd/status.go, pkg/codex/worker.go, pkg/codex/worker_test.go
- ALL 10 files modified on main since branch point. 9 files "changed in both".
- Richer dispatch progress output (worker-started/running events, wave execution plans) NOT on main.
- Research recommends: preserve/ branch (port in future Codex UX phase).

Commit 2: 4bbb9273379d31721f7cbdf2466a7189626f3109 (intent-workflows)
- 33 files, 2878 insertions, 112 deletions
- discuss, assumptions, pheromone sync features -- ALREADY on main in different forms.
- 16 files "changed in both".
- Research recommends: preserve/ branch (most valuable code already on main).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backup .aether/data/ colony files</name>
  <files>.aether/data/backups/cleanup-{timestamp}/</files>
  <read_first>
    .aether/data/pending-decisions.json
    .aether/data/COLONY_STATE.json
    .aether/data/pheromones.json
    .aether/data/constraints.json
    .aether/data/session.json
    .aether/data/flags.json
  </read_first>
  <action>Create a timestamped backup directory and copy all critical colony data files into it. Per D-07, this must happen before any modifications.

1. Create directory: `.aether/data/backups/cleanup-{YYYYMMDD-HHMMSS}/`
2. Copy these files if they exist:
   - `.aether/data/pending-decisions.json` -> backup dir
   - `.aether/data/COLONY_STATE.json` -> backup dir
   - `.aether/data/pheromones.json` -> backup dir
   - `.aether/data/constraints.json` -> backup dir
   - `.aether/data/session.json` -> backup dir
   - `.aether/data/flags.json` -> backup dir (may not exist)
3. Use the pattern from `cmd/init_cmd.go:100-108`: `copyFile` each file to the backup directory.
4. Verify each backed-up file exists and is non-empty (same byte count as source).

The backup directory name should use the format `cleanup-YYYYMMDD-HHMMSS` matching the Phase 26 medic pattern.
</action>
  <verify>
    <automated>BACKUP_DIR=$(ls -td .aether/data/backups/cleanup-* | head -1) && test -f "$BACKUP_DIR/pending-decisions.json" && test -f "$BACKUP_DIR/COLONY_STATE.json" && diff <(wc -c < .aether/data/pending-decisions.json) <(wc -c < "$BACKUP_DIR/pending-decisions.json") && echo "Backup verified"</automated>
  </verify>
  <acceptance_criteria>
    - Backup directory exists under .aether/data/backups/ with name matching cleanup-YYYYMMDD-HHMMSS
    - pending-decisions.json copied with identical byte count to source
    - COLONY_STATE.json copied with identical byte count to source
    - pheromones.json copied with identical byte count to source (if source exists)
    - constraints.json copied (if source exists)
    - session.json copied (if source exists)
  </acceptance_criteria>
  <done>All colony data files backed up to .aether/data/backups/cleanup-{timestamp}/ with verified byte-for-byte copies of pending-decisions.json, COLONY_STATE.json, and pheromones.json.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Evaluate and decide on candidate commits</name>
  <what-built>Pre-evaluation of both candidate commits against current main to determine integration vs preservation.</what-built>
  <read_first>
    cmd/codex_build_progress.go
    cmd/codex_build.go
    cmd/codex_visuals.go
  </read_first>
  <action>Per D-01, evaluate both candidate commits for value, merge-readiness, and safety before any cleanup. Per D-02, integrate if passes all three checks. Per D-03, create preserve/ branch if not.

Before presenting to user, run this pre-evaluation:

1. For commit 1 (98cda871 -- claude-dispatch-ux):
   - Run `git log --oneline 98cda871 -1` to confirm commit exists
   - Run `git diff main...98cda871 --stat` to see changed files
   - Run `git merge-tree $(git merge-base main 98cda871) main 98cda871 2>/dev/null | grep -c "changed in both"` to count conflicts
   - Compare `cmd/codex_build_progress.go` on main (203 lines) vs in commit (164 lines) to assess if progress features are truly missing

2. For commit 2 (4bbb9273 -- intent-workflows):
   - Run `git log --oneline 4bbb9273 -1` to confirm commit exists
   - Run `git diff main...4bbb9273 --stat` to see changed files
   - Verify that cmd/discuss.go, cmd/assumptions.go already exist on main (confirming overlap)

3. Present findings to user with clear recommendation for each:
   - Research recommends BOTH go to preserve/ branches due to conflict levels
   - User decides: integrate (selective port) or preserve for each

After user decision, execute:
- If preserve: `git branch preserve/claude-dispatch-ux 98cda871` and/or `git branch preserve/intent-workflows 4bbb9273`
- If integrate: Selective port of specific valuable hunks (user directs which parts)
</action>
  <how-to-verify>
    Two commits need your decision:

    **Commit 1: claude-dispatch-ux** (98cda871)
    - What it adds: Richer dispatch progress output for Codex runtime (worker-started/running events, wave execution plans)
    - Conflict level: ALL 10 files conflict with main
    - Research recommendation: Create preserve/ branch, port in future phase

    **Commit 2: intent-workflows** (4bbb9273)
    - What it adds: discuss command, assumptions command, pheromone sync
    - Conflict level: 16 files conflict with main
    - Overlap: Most features already exist on main in different forms
    - Research recommendation: Create preserve/ branch, most code already landed

    For each commit, choose:
    - "preserve" -- create a preserve/ branch pointing to the SHA (safe, no integration)
    - "integrate" -- selectively port valuable hunks to main (requires your direction on what to port)
  </how-to-verify>
  <resume-signal>
    For each commit, type "preserve" or "integrate" with optional notes.
    Example: "commit 1: preserve, commit 2: preserve"
  </resume-signal>
  <acceptance_criteria>
    - Both commits have been evaluated against main with diff stats shown to user
    - User has made explicit decision for each commit
    - preserve/ branches created (git branch --list 'preserve/*' shows results)
    - No candidate commit code is lost without either integration or preservation
  </acceptance_criteria>
  <done>Both candidate commits evaluated, user decisions recorded, and either preserve/ branches created or code integrated into main. No valuable code lost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> working tree | Commit evaluation reads git objects (trusted) and may create branches (mutation) |
| .aether/data/ -> backup | File copy crosses from live state to backup (read-only copy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-01 | Tampering | pending-decisions.json | mitigate | Backup created before any modification (D-07), backup verified byte-identical |
| T-34-01-02 | Repudiation | preserve/ branch creation | accept | preserve/ branches are git objects with full history; no risk of silent modification |
</threat_model>

<verification>
1. Backup directory exists with all critical files: `ls .aether/data/backups/cleanup-*`
2. Both commits evaluated with user decision recorded
3. preserve/ branches created (if that was the decision): `git branch --list 'preserve/*'`
4. Tests still green: `go test ./... -count=1`
</verification>

<success_criteria>
- .aether/data/ fully backed up to timestamped directory with verified copies
- Both candidate commits evaluated with explicit user decision for each
- preserve/ branches exist for any commit not integrated
- go test ./... -count=1 passes clean
- No code lost without either integration or preservation
</success_criteria>

<output>
After completion, create `.planning/phases/34-cleanup/34-01-SUMMARY.md`
</output>
