---
phase: 34-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified: []
autonomous: false
requirements:
  - R056
  - R057

must_haves:
  truths:
    - "git worktree list shows only the main working tree (1 entry)"
    - "git branch --list shows only main plus any preserve/* branches"
    - "No feature/test-audit-* branches remain"
    - "Worktree directories (cmd/.aether/worktrees/, Aether-worktrees/) are empty or gone"
    - "Disk space recovered (no 59 GB in worktree directories)"
  artifacts:
    - path: "git worktree state"
      provides: "Clean worktree list with only main"
      min_lines: 1
    - path: "git branch state"
      provides: "Clean branch list with only main + preserve/*"
      min_lines: 2
  key_links:
    - from: "git worktree prune"
      to: "git worktree list"
      via: "prunable entries removed first, then remaining removed individually"
      pattern: "git worktree (prune|remove)"
    - from: "git worktree removal"
      to: "git branch -D"
      via: "branches deleted only AFTER all worktrees removed"
      pattern: "git branch -D"
---

<objective>
Bulk-remove all stale git worktrees and orphaned branches after candidate commits are preserved.

Purpose: Per D-04, everything except the 2 candidate commits (now preserved/integrated via Plan 01) is disposable. Per D-05 and D-06, interactive confirmation is required before any destructive action with no --force bypass.

Output: A clean git state with only the main worktree and main + preserve/* branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-cleanup/34-CONTEXT.md
@.planning/phases/34-cleanup/34-RESEARCH.md
@.planning/phases/34-cleanup/34-01-SUMMARY.md

<interfaces>
<!-- Git worktree/branch cleanup pipeline -->
Order of operations (from RESEARCH.md Pitfall 3):
1. Prune stale (prunable) worktrees first -- directories already gone
2. Remove remaining non-prunable worktrees (bottom-up for nested)
3. Only THEN delete branches (cannot delete branch with active worktree)

Key pitfall (Pitfall 1): The claude-dispatch-ux worktree at
/Users/callumcowie/repos/Aether-worktrees/claude-dispatch-ux-20260421-1
contains 4 nested feature/test-audit-* worktrees inside it.
Must remove nested worktrees BEFORE parent.

Key pitfall (Pitfall 2): ~265 prunable (dir gone) vs ~257 non-prunable.
Run `git worktree prune` first, then remove remaining.

Current counts (live as of planning):
- 524 worktree entries (including main)
- 518 feature/test-audit-* branches
- 523 total branches
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Present worktree and branch inventory for user confirmation, then execute cleanup</name>
  <files>(no source files modified -- git state only)</files>
  <read_first>
    .aether/data/COLONY_STATE.json
  </read_first>
  <action>Per D-05, present a complete inventory of what will be deleted and get explicit user confirmation. Per D-06, there is no --force bypass -- user must confirm.

Execute cleanup in strict order:

**Step 1: Present inventory to user**
```
=== WORKTREE CLEANUP INVENTORY ===
Total worktrees: {count from git worktree list}
To remove: {count - 1} (everything except main)

Categories:
- Prunable (directory gone): {count from git worktree list --porcelain | grep -c prunable}
- Non-prunable (directory exists): {count}
- Nested inside other worktrees: {count}

Disk space to recover: {du -sh from worktree directories}

=== BRANCH CLEANUP INVENTORY ===
Total branches: {count from git branch --list}
To delete: {count - preserve_count - 1} (everything except main + preserve/*)
- feature/test-audit-* branches: {count}
- Other stale branches: {list}

Confirm deletion? (yes/no)
```

**Step 2: After user confirms "yes", execute in this exact order:**

1. `git worktree prune` -- clean up prunable entries (directory already gone)
2. List remaining non-main worktrees: `git worktree list --porcelain`
3. For the claude-dispatch-ux worktree (if still present), first remove any nested worktrees inside it:
   - Check for worktrees at `/Users/callumcowie/repos/Aether-worktrees/claude-dispatch-ux-20260421-1/cmd/.aether/worktrees/`
   - Remove nested worktrees first: `git worktree remove {nested_path} --force`
4. Remove all remaining non-main worktrees: for each worktree path in the list (excluding main repo root), run `git worktree remove {path} --force`
5. `git worktree prune` again (final cleanup)
6. Verify: `git worktree list` should show only 1 entry (main)

**Step 3: Delete branches (only after ALL worktrees removed)**
1. Delete all feature/test-audit-* branches: `git branch -D $(git branch --list 'feature/test-audit-*' | sed 's/^[* ]*//')`
2. Delete stale colony branches if present: `phase-1/builder-1`, `phase-2/builder-1`, `feature/auth` (verify no unique commits first with `git log main..{branch}`)
3. Delete the `claude-dispatch-ux-20260421-1` branch (value preserved in preserve/ branch from Plan 01)
4. Verify: `git branch --list` should show only main + preserve/* branches

**Step 4: Clean up empty directories**
- `find . -type d -name "worktrees" -path "*/.aether/*" -exec find {} -type d -empty -delete \;`
- Verify: `du -sh cmd/.aether/worktrees/` should show ~0 or directory not found

**Important safety rules:**
- NEVER delete the main branch or the main worktree
- NEVER delete preserve/* branches (created in Plan 01)
- If any `git worktree remove` fails, log the error and continue to the next (do not abort)
- If any `git branch -D` fails due to "not fully merged", check if it has unique commits before forcing
</action>
  <verify>
    <automated>test $(git worktree list | wc -l) -eq 1 && echo "Worktrees: clean (1 entry)" || echo "FAIL: $(git worktree list | wc -l) worktrees remain"</automated>
  </verify>
  <acceptance_criteria>
    - `git worktree list | wc -l` returns 1
    - `git branch --list 'feature/test-audit-*' | wc -l` returns 0
    - `git branch --list | grep -v 'main\|preserve/'` returns only unexpected branches (should be empty or minimal)
    - `du -sh cmd/.aether/worktrees/ 2>/dev/null` returns 0 or directory not found
    - `go test ./... -count=1` passes clean (no regression)
  </acceptance_criteria>
  <done>All stale worktrees removed (only main remains), all feature/test-audit-* and stale branches deleted, disk space recovered, tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Verify clean state and run regression tests</name>
  <files>(no files modified -- verification only)</files>
  <read_first>
    .aether/data/COLONY_STATE.json
  </read_first>
  <action>After cleanup is complete, run comprehensive verification:

1. Verify worktree state:
   - `git worktree list` -- should show exactly 1 entry (main repo root)
   - `git worktree prune --dry-run` -- should report nothing to prune

2. Verify branch state:
   - `git branch --list` -- should show main + any preserve/* branches only
   - Count branches and confirm it matches expected (main + preserve/claude-dispatch-ux + preserve/intent-workflows = ~3)

3. Verify disk space:
   - `du -sh Aether-worktrees/ 2>/dev/null || echo "Aether-worktrees/ gone"` -- should be gone or ~0
   - `du -sh cmd/.aether/worktrees/ 2>/dev/null || echo "cmd/.aether/worktrees/ gone"` -- should be gone or ~0

4. Run full test suite:
   - `go test ./... -race -count=1` -- must pass clean

5. Print summary:
   - Worktrees before: ~524, after: {actual}
   - Branches before: ~523, after: {actual}
   - Disk space recovered: {approximate}
   - Tests: GREEN
</action>
  <verify>
    <automated>go test ./... -race -count=1 2>&1 | tail -1 | grep -q "FAIL" && echo "TESTS FAILED" || echo "ALL TESTS GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `git worktree list | wc -l` equals 1
    - `git branch --list | wc -l` equals main + preserve branches only (~3)
    - `git worktree prune --dry-run` reports nothing
    - `go test ./... -race -count=1` passes with 0 failures
    - No worktree directories consuming disk space
  </acceptance_criteria>
  <done>Verification complete: git state clean (1 worktree, ~3 branches), tests green, disk space recovered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User confirmation -> destructive operations | All deletions gated behind explicit human approval |
| git worktree state -> filesystem | Worktree removal touches both git refs and filesystem directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02-01 | Denial of Service | Worktree removal | mitigate | Remove bottom-up (nested first), continue on individual failure, do not abort batch |
| T-34-02-02 | Tampering | Branch deletion | mitigate | Never delete main or preserve/* branches; verify branch has no unique commits before -D |
| T-34-02-03 | Repudiation | Cleanup actions | accept | preserve/ branches retain commit SHAs; git reflog provides recovery window |
</threat_model>

<verification>
1. `git worktree list | wc -l` returns 1
2. `git branch --list | wc -l` returns ~3 (main + preserve/*)
3. `git branch --list 'feature/test-audit-*' | wc -l` returns 0
4. `du -sh cmd/.aether/worktrees/` is ~0 or not found
5. `go test ./... -race -count=1` passes clean
</verification>

<success_criteria>
- Only 1 git worktree remains (main)
- All feature/test-audit-* branches deleted
- All stale colony branches deleted (phase-1/builder-1, phase-2/builder-1, feature/auth, claude-dispatch-ux-20260421-1)
- Disk space recovered (worktree directories empty or gone)
- go test ./... -race -count=1 passes clean
- preserve/* branches intact from Plan 01
</success_criteria>

<output>
After completion, create `.planning/phases/34-cleanup/34-02-SUMMARY.md`
</output>
