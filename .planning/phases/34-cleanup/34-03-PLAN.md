---
phase: 34-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["34-01", "34-02"]
files_modified:
  - .aether/data/pending-decisions.json
autonomous: false
requirements:
  - R058

must_haves:
  truths:
    - "Every unresolved blocker flag has been presented to the user with its age, severity, description, and source phase"
    - "Every flag has an explicit user decision: keep active, archive, or resolve"
    - "No flag was auto-archived by age (per D-10)"
    - "pending-decisions.json reflects all user decisions accurately"
  artifacts:
    - path: ".aether/data/pending-decisions.json"
      provides: "Updated flag state reflecting user decisions"
      contains: "resolved"
  key_links:
    - from: "user decisions"
      to: ".aether/data/pending-decisions.json"
      via: "flag-resolve or direct JSON update"
      pattern: "Resolved.*true"
---

<objective>
Interactive review of all 18 unresolved blocker flags with explicit user decision for each.

Purpose: Per D-08, present each unresolved flag with its age, severity, description, and source phase. Per D-09, user chooses keep/archive/resolve for each. Per D-10, no auto-archive by age -- every flag requires explicit decision.

Output: All 18 unresolved blocker flags reviewed and resolved, archived, or kept per user direction. pending-decisions.json updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-cleanup/34-CONTEXT.md
@.planning/phases/34-cleanup/34-RESEARCH.md
@.planning/phases/34-cleanup/34-01-SUMMARY.md
@.planning/phases/34-cleanup/34-02-SUMMARY.md

<interfaces>
<!-- FlagEntry and FlagsFile types from pkg/colony/flags.go -->
```go
type FlagEntry struct {
    ID           string `json:"id"`
    Type         string `json:"type"`
    Description  string `json:"description"`
    Phase        *int   `json:"phase"`
    Source       string `json:"source"`
    CreatedAt    string `json:"created_at"`
    Resolved     bool   `json:"resolved"`
    ResolvedAt   string `json:"resolved_at,omitempty"`
    Resolution   string `json:"resolution,omitempty"`
    Acknowledged bool   `json:"acknowledged,omitempty"`
}

type FlagsFile struct {
    Version   string      `json:"version"`
    Decisions []FlagEntry `json:"decisions"`
}
```

<!-- Existing flag subcommands from cmd/flag_cmds.go -->
- `flag-resolve --id <id> --message <msg>` -- marks a flag resolved with timestamp and resolution message
- `flag-check-blockers` -- counts active blockers/issues/notes
- `flag-acknowledge` -- acknowledges a flag by ID

<!-- Current unresolved flags (18 total, all type "blocker") -->
flag_1776685401_260a8900 -- cmd/codex_command_contract_test.go requires .aether/docs/cod
flag_1776686654_4450405b -- Fresh AETHER_OUTPUT_MODE=json go test fails
flag_1776686730_d577fccd -- Verification needed explicit retry due to AETHER env
flag_1776700239_1836c0b1 -- codex_continue.go:380-384 treats whole tests step as optional
flag_1776701834_709ea02b -- codex_continue.go:402-405 skips failure lines matching pattern
flag_1776706801_a34736ee -- codex_worker_activity_test.go continue replay issue
flag_1776707047_28c7da9c -- Live smoke repo in /tmp reached run state
flag_1776852661_fb1b77ea -- Fresh go test runs fail
flag_1776854795_5bf83773 -- codex_continue.go marks phase completed prematurely
flag_1776854814_a75b4580 -- artifactEvidenceTrusted becomes true incorrectly
flag_1776858311_ab5bbc8e -- Watcher verification found tests parsing JSON from CLI
flag_1776860474_e6de2f8b -- Verification failed 2026-04-22
flag_1776860486_8f5804ef -- go build ./cmd/aether error 2026-04-22
flag_1776862899_eb079d0e -- Fresh verification failed in go test ./cmd
flag_1776862906_e10cc42d -- go test ./... and -race fail across assumptions
flag_1776862913_e57052b2 -- go test -race concurrent writes to emitVisualProgress
flag_1776866720_9849f108 -- go test ./cmd -race concurrent writes to shared visual buffer
flag_1776869764_d6775013 -- FakeInvoker watcher and review allowed in codex_continue.go
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Present all 18 unresolved blocker flags for user review</name>
  <files>.aether/data/pending-decisions.json</files>
  <read_first>
    .aether/data/pending-decisions.json
  </read_first>
  <action>Per D-08, present each unresolved blocker flag with its full context. Per D-09, user decides keep/archive/resolve for each. Per D-10, no auto-archive.

**Step 1: Categorize flags for presentation**

Read `.aether/data/pending-decisions.json` and group the 18 unresolved flags by theme. Based on the flag descriptions, they likely group into:

- **Codex continue truth issues** (flags about codex_continue.go behavior, premature completion, evidence trust) -- Many of these may be resolved by Phase 31-33 fixes
- **Test reliability issues** (concurrent writes, race conditions, build errors) -- Check if these still reproduce after Phase 31-33
- **Environment/AETHER issues** (env var interference, explicit retry needed) -- May be stale after Phase 31 fixes
- **Single-instance issues** (smoke repo, contract test) -- Likely stale

**Step 2: Pre-filter check**

For each flag, run a quick assessment:
- If the flag mentions a specific file/line, check if that code still exists and has the described problem
- If the flag mentions a test failure, run the specific test to see if it still fails
- If the flag mentions an environment issue, check if the relevant code path still exists

Present the pre-filter results alongside each flag so the user has context.

**Step 3: Present to user in grouped format**

For each flag, display:
```
[{N}/18] {flag_id}
  Type: blocker
  Age: {days since created}
  Phase: {phase number}
  Description: {full description}
  Quick check: {still relevant / likely stale / fixed by Phase XX}
  Decision: [keep / archive / resolve]
```

Wait for user response with decisions for each flag.

**Step 4: Apply decisions**

For each flag based on user decision:
- **keep**: Leave as-is (Resolved: false, no changes)
- **archive**: Set Resolved: true, Resolution: "archived-cleanup-34", ResolvedAt: current timestamp
- **resolve**: Set Resolved: true, Resolution: "resolved-cleanup-34", ResolvedAt: current timestamp

Write updated `.aether/data/pending-decisions.json` preserving all other flags unchanged.
</action>
  <how-to-verify>
    18 unresolved blocker flags will be presented in groups.

    For each flag, you will see:
    - Its ID, age, and source phase
    - Its full description
    - A quick assessment of whether it is still relevant (checked against current code)

    For each flag, respond with one of:
    - "keep" -- leave it active, still needs attention
    - "archive" -- no longer relevant but retain for history
    - "resolve" -- confirmed fixed

    You can batch responses: "1-5: archive, 6-8: resolve, 9-18: keep"
    Or respond individually.
  </how-to-verify>
  <resume-signal>
    Provide your decisions for each flag. You can batch them:
    "1-7: archive, 8-12: resolve, 13-18: keep"
    Or respond to each individually.
  </resume-signal>
  <acceptance_criteria>
    - All 18 unresolved flags presented with ID, age, phase, description, and relevance assessment
    - User has provided explicit decision for every flag
    - pending-decisions.json updated: flags marked resolved=true for archive/resolve decisions
    - Flags marked "keep" remain with resolved=false
    - Total unresolved count matches expected: initial 18 minus archived minus resolved
  </acceptance_criteria>
  <done>All 18 unresolved blocker flags reviewed with explicit user decisions. pending-decisions.json updated to reflect decisions. No flag auto-archived.</done>
</task>

<task type="auto">
  <name>Task 2: Verify flag state and run final regression</name>
  <files>.aether/data/pending-decisions.json</files>
  <read_first>
    .aether/data/pending-decisions.json
  </read_first>
  <action>After all flag decisions are applied, verify the final state:

1. Count unresolved flags in pending-decisions.json:
   `python3 -c "import json; d=json.load(open('.aether/data/pending-decisions.json')); print(f'Total: {len(d[\"decisions\"])}, Unresolved: {sum(1 for f in d[\"decisions\"] if not f[\"resolved\"])}')"`

2. Verify the count matches expected (initial 18 minus user's archive/resolve decisions)

3. Run `go run ./cmd/aether flag-check-blockers` to confirm blocker count via the CLI

4. Run full test suite: `go test ./... -race -count=1`

5. Print phase summary:
   - Flags reviewed: 18
   - Kept active: {count}
   - Archived: {count}
   - Resolved: {count}
   - Remaining unresolved: {count}
   - Tests: GREEN/RED
</action>
  <verify>
    <automated>go test ./... -race -count=1 2>&1 | tail -1 | grep -q "FAIL" && echo "TESTS FAILED" || echo "ALL TESTS GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `go run ./cmd/aether flag-check-blockers` reports blocker count matching user decisions
    - `go test ./... -race -count=1` passes clean
    - pending-decisions.json has correct resolved/resolution fields for archived/resolved flags
    - No flags were auto-archived (all decisions explicit from user)
  </acceptance_criteria>
  <done>Flag review verified. Blocker count matches user decisions. Tests green. Phase 34 cleanup complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User decisions -> pending-decisions.json | Human judgment translated to JSON state mutations |
| .aether/data/ backup -> current state | Backup from Plan 01 protects against data loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03-01 | Tampering | pending-decisions.json mutations | mitigate | Backup exists from Plan 01; only Resolved/ResolvedAt/Resolution fields modified |
| T-34-03-02 | Repudiation | Flag decisions | accept | User makes explicit per-flag decisions; no automated resolution applied |
| T-34-03-03 | Elevation of Privilege | Archive decision on active blocker | accept | User explicitly chose archive; flag history preserved in JSON even when resolved=true |
</threat_model>

<verification>
1. `python3 -c "import json; d=json.load(open('.aether/data/pending-decisions.json')); print(sum(1 for f in d['decisions'] if not f['resolved']))"` returns expected count
2. `go run ./cmd/aether flag-check-blockers` returns matching blocker count
3. `go test ./... -race -count=1` passes clean
4. All 18 flags have explicit user decision recorded
</verification>

<success_criteria>
- All 18 unresolved blocker flags reviewed with explicit user decision for each
- pending-decisions.json updated: archived/resolved flags have resolved=true, resolution message, and timestamp
- Remaining unresolved count matches user's "keep" decisions
- go test ./... -race -count=1 passes clean
- No flag auto-archived by age (D-10 honored)
</success_criteria>

<output>
After completion, create `.planning/phases/34-cleanup/34-03-SUMMARY.md`
</output>
