---
phase: 102-worker-economy-visual-ceremony-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 102-01
files_modified:
  - cmd/worker_economy_test.go
  - cmd/testdata/worker_economy_snapshot.json
autonomous: true
requirements:
  - WORK-01
  - WORK-02
  - VIZ-01
  - VIZ-02

must_haves:
  truths:
    - "Every caste found in dispatch code (Caste: string literals) has a matching entry in the documented caste table"
    - "No dispatched caste is missing from the WORKER-ECONOMY.md report"
    - "Visual rendering functions reference real state variables, not hardcoded strings claiming dynamic behavior"
    - "casteEmojiMap, casteColorMap, and casteLabelMap all contain the same set of caste keys (with documented exceptions)"
  artifacts:
    - path: "cmd/worker_economy_test.go"
      provides: "Spawn coverage and visual ceremony verification tests"
      exports: ["TestDispatchedCastesDocumented", "TestNoChatOnlyWorkersUndocumented", "TestVisualOutputTracesToState", "TestCasteRegistryConsistency"]
    - path: "cmd/testdata/worker_economy_snapshot.json"
      provides: "Golden snapshot of documented dispatched castes"
      contains: "caste"
  key_links:
    - from: "cmd/worker_economy_test.go"
      to: ".planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md"
      via: "golden snapshot cross-reference"
      pattern: "WORKER-ECONOMY"
    - from: "cmd/worker_economy_test.go"
      to: "cmd/codex_visuals.go casteEmojiMap"
      via: "registry consistency check"
      pattern: "casteEmojiMap"
---

<objective>
Create automated tests that verify the WORKER-ECONOMY.md report accurately covers all dispatched castes and that visual rendering functions trace to real runtime state.

Purpose: D-09 requires an automated spawn coverage test. These tests freeze the audit findings so that future drift (undocumented spawns, new visual elements without state backing) fails CI. They cross-reference dispatch code against the documented report and verify visual ceremony integrity.

Output: cmd/worker_economy_test.go and cmd/testdata/worker_economy_snapshot.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-worker-economy-visual-ceremony-audit/102-RESEARCH.md
@.planning/phases/102-worker-economy-visual-ceremony-audit/102-01-SUMMARY.md
@.planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md
@.planning/phases/101-platform-parity-verification/101-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write spawn coverage and visual ceremony verification tests</name>
  <files>cmd/worker_economy_test.go, cmd/testdata/worker_economy_snapshot.json</files>
  <behavior>
    - TestDispatchedCastesDocumented: Extract all caste names from Caste: string literals in cmd/*.go dispatch code. For each dispatched caste, verify it appears in WORKER-ECONOMY.md. Fail with the specific undocumented caste name.
    - TestNoChatOnlyWorkersUndocumented: For each caste marked as "chat-only" in WORKER-ECONOMY.md, verify it is flagged as a WORK-02 finding. For each dispatched caste not in WORKER-ECONOMY.md, fail.
    - TestVisualOutputTracesToState: For each render* function in codex_visuals.go that produces user-visible output, verify it reads from at least one runtime state variable (colony state, phase data, dispatch plan). The Aether wordmark (renderAetherWordmark) is excluded per D-05.
    - TestCasteRegistryConsistency: Verify casteEmojiMap and casteLabelMap have identical key sets. Verify casteColorMap has the same keys or the difference is documented in WORKER-ECONOMY.md findings.
  </behavior>
  <read_first>
    - cmd/codex_visuals.go (casteEmojiMap lines 28-55, casteColorMap lines 57-84, casteLabelMap lines 86-113; renderStageMarker, renderBanner, renderProgressSummary, renderSpawnPlanForDispatches, renderCloseoutVisual functions)
    - cmd/codex_build.go (search for Caste: string literals for dispatch sites)
    - cmd/codex_continue.go (search for Caste: string literals)
    - cmd/codex_plan.go (search for Caste: string literals)
    - cmd/codex_colonize.go (search for Caste: string literals)
    - cmd/seal_final_review.go (search for Caste: string literals)
    - cmd/oracle_loop.go (search for Caste: string literals)
    - cmd/swarm_cmd.go (search for Caste: string literals)
    - cmd/parity_test.go (golden file test pattern reference -- the `updateGolden` flag, golden file reading/writing, JSON marshal approach)
    - .planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md (the report to validate against)
  </read_first>
  <action>
Create cmd/worker_economy_test.go with 4 test functions following the golden file pattern from cmd/parity_test.go. The test file goes in package cmd so it can access casteEmojiMap, casteColorMap, casteLabelMap, and dispatch functions directly.

### WorkerEconomySnapshot struct

```go
type WorkerEconomySnapshot struct {
    DocumentedCastes    []string `json:"documented_castes"`
    DispatchedCastes    []string `json:"dispatched_castes"`
    ChatOnlyCastes      []string `json:"chat_only_castes"`
    CasteRegistryKeys   []string `json:"caste_registry_keys"`
    ColorMapKeys        []string `json:"color_map_keys"`
}
```

### Test 1: TestDispatchedCastesDocumented

1. Extract dispatched castes by reading the golden snapshot file testdata/worker_economy_snapshot.json (field: dispatched_castes). The golden file is built by scanning cmd/*.go for Caste: string literals -- use the same grep approach that the research used, but encoded as a static list in the golden file since AST-level extraction in tests is fragile.
2. Read WORKER-ECONOMY.md from the phase directory (construct path using runtime.Caller or a known relative path from the test file).
3. For each caste in dispatched_castes, verify the caste name appears in WORKER-ECONOMY.md.
4. If any dispatched caste is missing from the report, fail with: "dispatched caste %q not documented in WORKER-ECONOMY.md".

### Test 2: TestNoChatOnlyWorkersUndocumented

1. Read chat_only_castes from the golden snapshot.
2. For each chat-only caste, verify WORKER-ECONOMY.md contains both the caste name AND the string "WORK-02" within 200 characters of the caste name (indicating it is flagged as a finding).
3. If any chat-only caste is not flagged, fail with: "chat-only caste %q not flagged as WORK-02 finding".

### Test 3: TestVisualOutputTracesToState

This is a static analysis test, not a runtime test. For each visual rendering function that produces user-visible output (renderStageMarker, renderBanner, renderProgressSummary, renderSpawnPlanForDispatches, renderCloseoutVisual, renderSignalVisual, renderContinueWorkerFlowLine, renderSealVisual), verify in WORKER-ECONOMY.md's Visual Ceremony Traceability table that:
- The function name appears
- The "Traces to Runtime?" column says "Yes" OR the "Finding" column contains "acceptable per D-05"

Exclude renderAetherWordmark -- it is pure decoration per D-05.

Implementation: read WORKER-ECONOMY.md, parse the Visual Ceremony Traceability section by splitting on "|" in the table rows, and verify each expected function name appears. Use a hardcoded list of expected function names:

```go
var visualFunctions = []string{
    "casteIdentity",
    "renderStageMarker",
    "renderProgressSummary",
    "renderBanner",
    "renderSpawnPlanForDispatches",
    "renderCloseoutVisual",
    "renderSignalVisual",
    "renderContinueWorkerFlowLine",
    "renderSealVisual",
}
```

### Test 4: TestCasteRegistryConsistency

1. Get keys from casteEmojiMap and casteLabelMap (both are package-level vars accessible in the test).
2. Verify they have the same count and the same key set. If not, fail listing the differences.
3. Get keys from casteColorMap. If the key set differs from casteEmojiMap, verify the difference is documented in WORKER-ECONOMY.md findings (search for "casteColorMap" within 200 chars of the missing key).

### Golden File: testdata/worker_economy_snapshot.json

Create the initial golden file with data extracted from the source code. The dispatched_castes list comes from the Caste: string literal grep across cmd/*.go. Use the list from the research file:

Dispatched castes: archaeologist, oracle, architect, ambassador, builder, watcher, probe, measurer, chaos, gatekeeper, auditor, scout, route_setter, surveyor-nest, surveyor-disciplines, surveyor-pathogens, surveyor-provisions, tracker.

Documented castes: all 27 from casteEmojiMap.

Chat-only castes: empty list initially (WORKER-ECONOMY.md findings will determine this; the test verifies the report's claim).

CasteRegistryKeys: all 27 from casteEmojiMap.

ColorMapKeys: all 26 from casteColorMap.

Use the same -update-golden pattern as parity_test.go (declare `var updateGolden = flag.Bool("update-golden", false, ...)` in the test file if not already available, or reuse the existing one from the package).
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && go test ./cmd/ -run "TestDispatchedCastesDocumented|TestNoChatOnlyWorkersUndocumented|TestVisualOutputTracesToState|TestCasteRegistryConsistency" -count=1 -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - cmd/worker_economy_test.go exists and compiles (go vet ./cmd/worker_economy_test.go passes)
    - cmd/testdata/worker_economy_snapshot.json exists and contains "documented_castes", "dispatched_castes", "chat_only_castes", "caste_registry_keys", "color_map_keys" fields
    - TestDispatchedCastesDocumented passes (all dispatched castes found in WORKER-ECONOMY.md)
    - TestCasteRegistryConsistency passes (casteEmojiMap and casteLabelMap have same keys)
    - All 4 test functions exist in the file (grep -c "func Test" cmd/worker_economy_test.go returns 4)
    - Golden file contains at least 18 dispatched caste entries
  </acceptance_criteria>
  <done>
    cmd/worker_economy_test.go contains 4 passing test functions. cmd/testdata/worker_economy_snapshot.json golden file exists with documented and dispatched caste lists. Every dispatched caste is verified against the WORKER-ECONOMY.md report. Caste registry maps are verified consistent. Visual ceremony traceability is verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code reading report file | Test reads WORKER-ECONOMY.md from phase directory -- trusted local file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-02 | All | N/A | accept | Test file has no attack surface -- reads local files only |
</threat_model>

<verification>
1. `go test ./cmd/ -run TestDispatchedCastesDocumented -count=1 -v` passes
2. `go test ./cmd/ -run TestNoChatOnlyWorkersUndocumented -count=1 -v` passes
3. `go test ./cmd/ -run TestVisualOutputTracesToState -count=1 -v` passes
4. `go test ./cmd/ -run TestCasteRegistryConsistency -count=1 -v` passes
5. Golden file exists at cmd/testdata/worker_economy_snapshot.json
</verification>

<success_criteria>
- All 4 test functions pass
- Golden snapshot file contains documented and dispatched caste lists
- Every dispatched caste in dispatch code appears in WORKER-ECONOMY.md
- casteEmojiMap and casteLabelMap have identical key sets
- Any difference between casteEmojiMap and casteColorMap is documented in findings
</success_criteria>

<output>
After completion, create `.planning/phases/102-worker-economy-visual-ceremony-audit/102-02-SUMMARY.md`
</output>
