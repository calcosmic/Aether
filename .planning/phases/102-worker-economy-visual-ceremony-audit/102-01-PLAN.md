---
phase: 102-worker-economy-visual-ceremony-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md
autonomous: true
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - VIZ-01
  - VIZ-02

must_haves:
  truths:
    - "Every one of the 27 defined worker castes appears in a table with purpose, durable output, and downstream consumer"
    - "Every dispatched caste (found via Caste: literals in cmd/*.go) has all three fields populated"
    - "Castes that only return chat without persisting are flagged as WORK-02 violations"
    - "Build, continue, seal, colonize, and plan each have a per-command wave shape table"
    - "Every visual element (caste identity, stage markers, progress bars, closeout banners, wordmark) is traced to its state source or flagged as decoration"
    - "No visual element that implies a state transition lacks a backing runtime change"
  artifacts:
    - path: ".planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md"
      provides: "Combined worker economy and visual ceremony audit report"
      contains: "## Severity Summary"
  key_links:
    - from: "WORKER-ECONOMY.md caste tables"
      to: "cmd/codex_visuals.go casteEmojiMap"
      via: "caste name string matching"
      pattern: "casteEmojiMap\\["
    - from: "WORKER-ECONOMY.md wave shape tables"
      to: "cmd/codex_build.go plannedBuildDispatches"
      via: "dispatch site documentation"
      pattern: "plannedBuildDispatches"
---

<objective>
Produce the combined WORKER-ECONOMY.md audit report covering worker economy findings, visual ceremony findings, and per-command wave shape tables.

Purpose: This is the primary deliverable of Phase 102. It documents every worker caste's purpose, output, and consumer; flags castes that violate WORK-02 (chat-only); provides per-command wave shape tables for build/continue/seal/colonize/plan; and traces every visual element to its runtime state source. Phase 105 will use this report for remediation.

Output: WORKER-ECONOMY.md in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/102-worker-economy-visual-ceremony-audit/102-RESEARCH.md
@.planning/phases/102-worker-economy-visual-ceremony-audit/102-CONTEXT.md
@.planning/phases/101-platform-parity-verification/KNOWN-GAPS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build combined worker economy and visual ceremony audit report</name>
  <files>.planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md</files>
  <read_first>
    - cmd/codex_visuals.go (lines 28-113 for casteEmojiMap, casteColorMap, casteLabelMap; search for renderStageMarker, renderBanner, renderProgressSummary, renderAetherWordmark, renderSpawnPlanForDispatches, renderCloseoutVisual, renderSignalVisual, renderContinueWorkerFlowLine, renderSealVisual)
    - cmd/codex_build.go (search for plannedBuildDispatches, Caste: string literals)
    - cmd/codex_continue.go (search for codexContinueReviewSpecs, Caste: string literals)
    - cmd/codex_plan.go (search for plannedPlanningWorkers, Caste: string literals)
    - cmd/codex_colonize.go (search for surveyorSpecs, Caste: string literals)
    - cmd/seal_final_review.go (search for sealFinalReviewRequiredCastes, Caste: string literals)
    - cmd/oracle_loop.go (search for Caste: string literals)
    - cmd/swarm_cmd.go (search for Caste: string literals)
    - .claude/agents/ant/aether-builder.md (purpose statement example)
    - .claude/agents/ant/aether-watcher.md (purpose statement example)
    - .aether/workers.md (worker definitions)
    - .planning/phases/101-platform-parity-verification/KNOWN-GAPS.md (report format pattern)
  </read_first>
  <action>
Produce WORKER-ECONOMY.md following the KNOWN-GAPS.md severity pattern (Critical/Warning/Info). The report has these sections:

## 1. Severity Summary Table

Table with columns: Severity | Count. Same three tiers as KNOWN-GAPS.md.

## 2. Worker Caste Inventory

For all 27 castes in casteEmojiMap, produce a table with columns:
- Caste (name)
- Status: "dispatched" or "defined-only"
- Dispatch Location (file:line or "never dispatched")
- Purpose (one-line from agent definition frontmatter or .aether/workers.md)
- Durable Output (file artifact, state mutation, review ledger entry, or "chat-only")
- Downstream Consumer (command/function that reads the output, or "none identified")

Group into two subsections: "Actively Dispatched" and "Defined But Never Dispatched".

For castes flagged as "chat-only" in the Durable Output column, add a finding in the Warning section below.

## 3. Wave Shape Tables

Five separate per-command tables (per D-06 and D-07), one for each of: build, continue, seal, colonize, plan. Each table has columns:
- Stage
- Wave (wave number or "parallel")
- Caste
- Condition (when this spawn happens -- e.g., "always", "depth == full", "reviewDepth >= standard")
- Output (what the worker produces)
- Downstream Need (why this spawn exists -- what consumes its output)

Extract wave shape data by reading the dispatch planning functions in each command file:
- Build: `plannedBuildDispatches()` in codex_build.go
- Continue: `codexContinueReviewSpecs()` in codex_continue.go
- Plan: `plannedPlanningWorkers()` in codex_plan.go
- Colonize: `surveyorSpecs` in codex_colonize.go
- Seal: `sealFinalReviewRequiredCastes` in seal_final_review.go

## 4. Visual Ceremony Traceability

Table with columns:
- Visual Element (e.g., "Caste identity", "Stage markers", "Progress bar", "Build banner", "Aether wordmark", "Spawn plan", "Closeout banner", "Signal visual", "Continue worker flow", "Seal summary")
- Rendering Function (exact function name from codex_visuals.go)
- State Source (what runtime data it reads)
- Traces to Runtime? (yes/no)
- Finding (blank if yes; "decorative-only -- acceptable per D-05" or "claims state transition without backing change" if no)

For each visual element, read the rendering function and verify it reads from real state (not hardcoded strings that pretend to be dynamic). The Aether wordmark is pure decoration -- mark it as "decorative-only -- acceptable per D-05" (per D-04 and D-05).

## 5. Findings

Severity-classified findings with the format:
```
### [C/W/I]-NN: Finding Title

Description of the finding.

Severity: [Critical/Warning/Info] because [reason].
```

Expected findings to document (verify each against the source code):
- Castes defined but never dispatched (chronicler, sage, keeper, weaver, includer, guardian, dreamer, medic, fixer) -- likely Info severity since they have agent definitions but no dispatch sites
- Porter dispatch path -- clarify it runs through seal workflow, not standard caste dispatch
- Any caste where the agent definition promises persistent output but dispatch code does not enforce it
- Any visual element that claims a state transition without backing runtime change (if found)
- casteColorMap has 26 entries while casteEmojiMap has 27 (porter was added later) -- Info

Do NOT include fix suggestions. Phase 105 handles remediation.

## 6. Verified Counts

Summary table:
| Category | Count | Notes |
|----------|-------|-------|
| Total defined castes | 27 | From casteEmojiMap |
| Actively dispatched | N | Found via Caste: string literals in cmd/*.go |
| Defined but never dispatched | N | Difference |
| Visual elements traced | N | From codex_visuals.go render* functions |
| Visual elements decorative | N | Pure decoration (acceptable per D-05) |

Header metadata:
- Phase: 102 (Worker Economy & Visual Ceremony Audit)
- Generated: [current date]
- Status: For Phase 105 remediation
  </action>
  <verify>
    <automated>test -f /Users/callumcowie/repos/Aether/.planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md && grep -c '## Severity Summary' /Users/callumcowie/repos/Aether/.planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md && grep -c '## Worker Caste Inventory' /Users/callumcowie/repos/Aether/.planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md && grep -c '## Wave Shape Tables' /Users/callumcowie/repos/Aether/.planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md && grep -c '## Visual Ceremony Traceability' /Users/callumcowie/repos/Aether/.planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md && grep -c '## Findings' /Users/callumcowie/repos/Aether/.planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md && grep -c '## Verified Counts' /Users/callumcowie/repos/Aether/.planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md</automated>
  </verify>
  <acceptance_criteria>
    - WORKER-ECONOMY.md exists at .planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md
    - Contains "## Severity Summary" section
    - Contains "## Worker Caste Inventory" section with all 27 caste names appearing (verifiable: grep for each of queen, builder, watcher, scout, colonizer, surveyor, architect, chaos, archaeologist, oracle, route_setter, ambassador, auditor, chronicler, gatekeeper, porter, guardian, includer, keeper, measurer, probe, tracker, weaver, dreamer, medic, fixer)
    - Contains 5 per-command wave shape tables (grep for "### Build Wave Shape" OR "Build", "Continue", "Seal", "Colonize", "Plan" as wave shape section headers)
    - Contains "## Visual Ceremony Traceability" section
    - Contains "## Findings" section with at least one severity-classified entry matching pattern "[CWI]-[0-9]"
    - Contains "## Verified Counts" section with table showing total defined castes = 27
    - No "fix" or "remediation" suggestions in findings (grep for "fix" in findings section returns 0)
    - Aether wordmark marked as acceptable decoration per D-05
  </acceptance_criteria>
  <done>
    WORKER-ECONOMY.md exists with all 6 required sections. Every one of the 27 defined castes appears in the inventory table. All 5 wave shape tables are present. Every visual element is traced to its state source. Findings are severity-classified with no fix suggestions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None applicable | Read-only audit phase with no user input, no network calls, no state mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | All | N/A | accept | Read-only audit phase -- no attack surface beyond file reads |
</threat_model>

<verification>
1. WORKER-ECONOMY.md exists and contains all 6 required sections
2. grep verifies all 27 caste names appear in the document
3. grep verifies 5 wave shape section headers exist
4. grep verifies findings use [CWI]-NN severity pattern
5. grep verifies no "fix" suggestions appear in findings
</verification>

<success_criteria>
- WORKER-ECONOMY.md contains a severity summary table
- All 27 defined worker castes appear in the caste inventory table with purpose, output, and consumer columns
- All 5 per-command wave shape tables are present (build, continue, seal, colonize, plan)
- All visual elements traced to state source or marked as acceptable decoration
- Findings use Critical/Warning/Info severity tiers with no fix suggestions
- Verified counts table shows 27 total defined castes
</success_criteria>

<output>
After completion, create `.planning/phases/102-worker-economy-visual-ceremony-audit/102-01-SUMMARY.md`
</output>
