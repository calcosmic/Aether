---
phase: 202-swarm-oracle-and-live-colony
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md
  - cmd/testdata/classic-contract/v1/schema.json
  - cmd/testdata/classic-contract/v1/mechanisms.json
  - cmd/classic_contract_test.go
autonomous: true
requirements: [SYNTH-04]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every Phase 202 Swarm, Oracle and live-display mechanism has an evidence-backed keep-current, restore-modern, replace-better or retire-with-proof disposition recorded before any runtime work begins (SYNTH-04)."
    - "Each of the eight CAP rows routed to Phase 202 is independently confirmed or revised against cited Classic and current-Go evidence rather than inherited from the capability ledger's hypothesis."
    - "The Classic Feb-April presentation of watch, Swarm and Oracle is reconstructed from cited historical anchors, so D-04's visual character is an evidenced description rather than a remembered impression."
    - "Every locked decision D-01 through D-12 traces to at least one SYN-202 row and at least one named implementation plan."
    - "A SYN-202 row for which no Classic evidence can be recovered records an explicit no-historical-evidence citation and a current-only disposition, and is never omitted from the matrix."
    - "SYN-202 identifier matching in the versioned corpus is exact byte equality against the schema pattern; a case-variant or look-alike identifier is rejected by name rather than silently accepted."
    - "The versioned Classic contract corpus accepts SYN-202 mechanism identifiers and live-colony groups, and rejects an unknown SYN identifier by name."
  artifacts:
    - path: ".planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md"
      provides: "Signed Phase 202 Swarm/Oracle/live-display mechanism study and CAP routing"
      contains: "SYN-202-12"
    - path: "cmd/testdata/classic-contract/v1/mechanisms.json"
      provides: "SYN-202 mechanism registry entries"
      contains: "SYN-202-01"
    - path: "cmd/testdata/classic-contract/v1/schema.json"
      provides: "Widened synthesis-decision pattern and live-colony group enum"
      contains: "V-202-EVENTS"
  key_links:
    - from: "cmd/testdata/classic-contract/v1/mechanisms.json"
      to: ".planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md"
      via: "SYN-202 and CAP identifiers"
      pattern: "SYN-202-"
  prohibitions:
    - "Never justify an implementation task by a Classic feature name or a remembered screenshot alone."
    - "Never replace a current Go safety behaviour without a documented comparative reason."
    - "Never present an unresolved owner choice as an engineering conclusion."
    - "Never record a disposition for a routed capability row without citing both old and current evidence."
---

<objective>
Produce the Phase 202 mechanism study that SYNTH-04 requires and that SYNTH-07 makes a hard planning gate, and widen the versioned Classic contract corpus to accept Phase 202 mechanism identifiers.

Purpose: Phase 202's three subsystems each already have a partially-built modern mechanism — a live event bus with the wrong fields, a Swarm pipeline with no comparison step, an Oracle loop that tracks everything and shows nothing. Without a cited comparison of what the Classic mechanism actually did against what the current Go path actually does, this phase's most likely failure is building a fourth parallel version of something that already exists — the recurring defect `.planning/codebase/CONCERNS.md` names by file. The owner has additionally asked for the February-April visual character (D-04); that character must be reconstructed from evidence, not from memory.
Output: A signed `202-CLASSIC-SYNTHESIS.md` carrying SYN-202-01..12, and a corpus schema plus mechanism registry that accept those identifiers.

Tracer note: this plan is the SYNTH-07 planning gate and produces no runtime behaviour, so it carries no `type="tracer"` task. The phase's tracer — the thin end-to-end slice through every layer this phase modifies — is Task 1 of plan `202-02`, the first implementation plan, which depends on this one.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/202-swarm-oracle-and-live-colony/202-CONTEXT.md
@.planning/phases/202-swarm-oracle-and-live-colony/202-RESEARCH.md
@.planning/phases/202-swarm-oracle-and-live-colony/202-PATTERNS.md
@.planning/research/v1.28-classic-synthesis-template.md
@.planning/research/v1.28-classic-capability-ledger.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the cited Phase 202 Swarm, Oracle and live-display mechanism synthesis</name>
  <files>.planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md (create; confirm absent)
    - .planning/research/v1.28-classic-synthesis-template.md
    - .planning/research/v1.28-classic-capability-ledger.md
    - .planning/phases/202-swarm-oracle-and-live-colony/202-CONTEXT.md
    - .planning/phases/202-swarm-oracle-and-live-colony/202-RESEARCH.md
    - .planning/phases/202-swarm-oracle-and-live-colony/202-PATTERNS.md
    - .planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md
    - .planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md
    - .aether/dreams/2026-09-01-comprehensive-aether-colony-review.md
    - .planning/codebase/CONCERNS.md
    - .planning/codebase/ARCHITECTURE.md
    - cmd/swarm_cmd.go
    - cmd/swarm_strikes.go
    - cmd/oracle_loop.go
    - cmd/oracle_progress.go
    - cmd/oracle_research_doc.go
    - cmd/compatibility_cmds.go
    - cmd/work_repair.go
    - cmd/ceremony_emitter.go
    - cmd/event_types.go
    - cmd/event_stream.go
    - cmd/event_bridge.go
    - cmd/codex_visuals.go
    - cmd/spend_cost_line.go
    - pkg/events/ceremony.go
    - pkg/events/bus.go
  </read_first>
  <action>Instantiate `.planning/research/v1.28-classic-synthesis-template.md` for area `swarm-oracle-live`, phase `202`. Reconstruct the Classic mechanism from the historical anchors `3a5b81c2`, `v5.0.0` and `v5.4.0`, plus the removal commit `0063be8b` and the restoration commit `2c9e2a98`, as `OLD-*` rows, each citing an exact `git show <anchor>:<path>` reference. Cover the four-pane watch cockpit, the four-lens Swarm control loop with its real-time renderer, and the Oracle survey-investigate-synthesize-verify wizard. Because D-04 makes presentation a requirement, each OLD row must additionally record what the screen actually showed — glyphs, wave framing, ceremony lines, Queen voice — not only what the loop computed.

Audit the current Go mechanism as `NOW-*` rows citing `path:line` plus the named test that locks each, covering at minimum watchCmd and buildIdleWatchResult, runSwarmDestroy, executeSwarmWave, buildSwarmPlansForWave, swarmTaskForCaste, renderSwarmFindingSummary, persistSwarmResultOutcome, evaluateSwarmStrikeHistory, runOracleLoop, oracleDepthLevels, noveltyTracker, emitOracleProgress, saveOracleResearchDocument, oracleResearchPartialLabel, emitLifecycleCeremony, events.Bus, the retired cmd event stream trio, saveRepairCheckpoint and restoreRepairCheckpoint, renderSpendCostLine, and the caste identity functions in cmd/codex_visuals.go.

Record exactly twelve comparative synthesis rows with these identities and subjects: SYN-202-01 one versioned typed live-event model with replay and resume semantics; SYN-202-02 live cockpit information grammar — dashboard in place plus event ticker, current wave in depth, wider colony compressed to one header line, drill-down for the rest; SYN-202-03 replay-backed idle and the honest no-history floor; SYN-202-04 Classic February-April visual character carried over truthful events; SYN-202-05 four genuinely distinct Swarm lenses including the absent external-research lens; SYN-202-06 hypothesis comparison, contradiction surfacing and repair ranking as a first-class artifact; SYN-202-07 one checkpoint, verify and rollback transaction shared with every repair lane; SYN-202-08 three-strike architectural escalation preserved through the refactor; SYN-202-09 one durable replay-safe Swarm episode with retention and cleanup; SYN-202-10 Oracle depth vocabulary shared with planning while Oracle's own economics stay unchanged; SYN-202-11 Oracle round visibility — clarified question, confidence movement, contradictions, diminishing returns; SYN-202-12 durable Oracle synthesis, discoverability, and one honest partial-work vocabulary. Give each a disposition drawn from keep-current, restore-modern, replace-better, retire-with-proof.

Route and independently adjudicate all eight Phase 202 capability rows: CAP-021, CAP-044, CAP-045, CAP-046, CAP-047, CAP-048, CAP-063 and CAP-072. The ledger's proposed disposition is a hypothesis; confirm or revise each with its own cited old and current evidence. For CAP-045 specifically, rule in writing which half belongs to this phase — proposing scoped FOCUS or REDIRECT learning from successful Swarm evidence — and record that measuring that learning's later effect is Phase 204's boundary, not an omission.

Decide in writing, as explicit synthesis rows rather than silent assumptions, the five questions RESEARCH.md leaves open: (a) the exact typed event schema and its versioning strategy, including whether the version rides on the payload or the topic name, and which fields are mandatory per event kind (Open Question 1); (b) whether the Swarm hypothesis type reuses oracleWorkerResponse or is a Swarm-owned sibling with the same field shape (Open Question 2); (c) what the fourth Swarm lens actually is, given no external-research caste exists today (assumption A1); (d) whether pkg/events.Bus is extended or bridged as the one live model (assumption A2); (e) whether the in-place dashboard can be delivered with the existing interval-and-reprint pattern or genuinely needs a terminal-UI dependency — and record that adding one is an owner-facing decision, not a planner default (assumption A3).

Record one further ruling the implementation plans need and evidence can decide: money never rides on a live event. Name renderSpendCostLine and loadSpendLedgersForPhase as the single cost authority the cockpit reads, and record the Phase 196 precedent that makes an unread currency field on a shared struct a standing hazard.

Include a traceability section mapping each of D-01 through D-12 to its SYN-202 rows and to the named implementation plan that delivers it, in the range 202-02 to 202-15. Include a rejected-alternatives table naming at minimum: reviving the retired cmd event stream trio, building a second event bus, a second checkpoint primitive for Swarm, and rewriting Oracle's confidence targets to match planning's. Mark Phase 203, 204 and 205 capabilities as explicit boundaries, not omissions. Where no Classic evidence can be recovered for a row, state that in the citation cell in those words and give the row a current-only disposition rather than dropping it. Do not modify the Dreams report or any historical evidence file.</action>
  <acceptance_criteria>
    - The document contains each identifier `SYN-202-01` through `SYN-202-12` exactly once as a decision heading or row identifier.
    - Each of CAP-021, CAP-044, CAP-045, CAP-046, CAP-047, CAP-048, CAP-063 and CAP-072 appears in a routed coverage table with exactly one disposition and at least one cited source path.
    - Each of D-01 through D-12 appears in the traceability section next to at least one SYN-202 identifier and one plan number in the range 202-02 to 202-15.
    - The Classic reconstruction table contains at least one `git show` citation per OLD row, and the current audit table contains at least one `path:line` citation per NOW row.
    - The five open items from RESEARCH.md — event schema and versioning, hypothesis type shape, the fourth lens, bus unification versus bridge, and the terminal-UI dependency question — each appear as a decided row or an open-decisions entry with a named authority.
    - The rejected-alternatives table names the retired event stream trio, a second event bus, a second checkpoint primitive, and rewriting Oracle's confidence targets.
    - Front matter sets `phase: "202"` and `area: "swarm-oracle-live"`.
  </acceptance_criteria>
  <verify>
    <automated>SYN=.planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md; test -f "$SYN" &amp;&amp; for i in 01 02 03 04 05 06 07 08 09 10 11 12; do test "$(grep -c "SYN-202-$i" "$SYN")" -ge 1 || exit 1; done &amp;&amp; for c in 021 044 045 046 047 048 063 072; do grep -q "CAP-$c" "$SYN" || exit 1; done &amp;&amp; for d in 01 02 03 04 05 06 07 08 09 10 11 12; do grep -q "D-$d" "$SYN" || exit 1; done &amp;&amp; grep -q "git show" "$SYN" &amp;&amp; grep -q 'area: "swarm-oracle-live"' "$SYN"</automated>
  </verify>
  <done>One evidence-backed brief covers every Phase 202 mechanism, locked decision and routed capability, so no later plan can be justified by a feature name or a remembered screen alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register SYN-202 mechanisms in the versioned Classic corpus</name>
  <files>cmd/testdata/classic-contract/v1/schema.json, cmd/testdata/classic-contract/v1/mechanisms.json, cmd/classic_contract_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/testdata/classic-contract/v1/schema.json
    - cmd/testdata/classic-contract/v1/mechanisms.json
    - cmd/classic_contract_test.go
    - .planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md
  </read_first>
  <behavior>
    - A mechanism entry whose id is outside the registered SYN-199, SYN-200, SYN-201 and SYN-202 sets fails registry validation and the error names the offending identifier.
    - A case declaring `synthesis_decision` of `SYN-202-06` and group `V-202-LENS` validates against the schema.
    - A case declaring a SYN-202 decision that no mechanism entry registers fails coverage validation by name.
    - An identifier differing from a registered one only by letter case or by a visually similar character is rejected, and the rejection names the identifier it was compared against.
    - Every SYN-202 mechanism entry names its CAP identifiers, public commands, source citations and owning groups.
  </behavior>
  <action>Widen `schema.json`'s `synthesis_decision` pattern, at both places it appears, from its current value ending `201-(?:0[1-9]|1[0-4]))$` to additionally accept `202-(?:0[1-9]|1[0-2])` as a further alternative in the same alternation. Add these group enum members alongside the existing ones without removing any: `V-202-EVENTS`, `V-202-COCKPIT`, `V-202-LENS`, `V-202-REPAIR`, `V-202-EPISODE`, `V-202-ORACLE`.

Append twelve mechanism entries `SYN-202-01` through `SYN-202-12` to `mechanisms.json`, each carrying the required registry fields `id`, `title`, `disposition`, `cap_ids`, `public_commands`, `source_citations` and `groups`, with values taken verbatim from the synthesis Task 1 produced. Add `.planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md` to the `synthesis_sources` array; leave the legacy scalar `synthesis_source` key untouched. Public commands for these entries are the owner-facing spellings `/ant-watch`, `/ant-swarm`, `/ant-oracle`, `/ant-status` and `/ant-history`, matching the vocabulary Phase 199 locked.

Extend `cmd/classic_contract_test.go` with a `TestClassicContractPhase202MechanismRegistry` that loads the registry through the existing strict loader, asserts the twelve SYN-202 identifiers are present exactly once each, asserts every entry names at least one CAP identifier and at least one source citation, and proves by negative fixture that an unregistered SYN identifier, a case referencing an unregistered decision, and an identifier differing only by letter case each fail with the offending identifier in the message. Do not add executable behaviour cases here — plan 202-15 owns the Phase 202 case set once the behaviours exist.</action>
  <acceptance_criteria>
    - `python3 -c "import json;json.load(open('cmd/testdata/classic-contract/v1/schema.json'))"` succeeds and the pattern string accepts a Phase 202 identifier.
    - The registry contains exactly twelve entries whose id begins `SYN-202-`, and the pre-existing ten SYN-199, twelve SYN-200 and fourteen SYN-201 entries are all still present, for forty-eight total.
    - `synthesis_sources` contains the Phase 202 synthesis path.
    - The three negative fixtures fail with the offending identifier named in the error text.
    - `TestClassicContractSchema`, `TestClassicMechanismCoverage` and `TestClassicContractPhase201MechanismRegistry` still pass unchanged.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestClassicContractSchema|TestClassicMechanismCoverage|TestClassicContractPhase201MechanismRegistry|TestClassicContractPhase202MechanismRegistry)$' -count=1 &amp;&amp; python3 -c "import json,sys; m=json.load(open('cmd/testdata/classic-contract/v1/mechanisms.json')); ids=[x['id'] for x in m['mechanisms']]; sys.exit(0 if len([i for i in ids if i.startswith('SYN-202-')])==12 and len([i for i in ids if i.startswith('SYN-201-')])==14 and len([i for i in ids if i.startswith('SYN-200-')])==12 and len([i for i in ids if i.startswith('SYN-199-')])==10 else 1)"</automated>
  </verify>
  <done>The executable Classic corpus can carry Phase 202 mechanism identities, so later plans register cases against a contract that already exists rather than inventing one late.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created by this plan (new to the codebase — not drift):

- `.planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md` (new file)
- Mechanism identifiers `SYN-202-01` … `SYN-202-12` (new registry entries)
- Corpus group names `V-202-EVENTS`, `V-202-COCKPIT`, `V-202-LENS`, `V-202-REPAIR`, `V-202-EPISODE`, `V-202-ORACLE` (new schema enum members)
- `TestClassicContractPhase202MechanismRegistry` (new Go test)

<verification>
- The synthesis covers SYNTH-04's full scope: Swarm lenses, hypothesis comparison, repair loop, Oracle research iterations, synthesis, typed or inferred state, and live presentation.
- All eight routed CAP rows carry an independently adjudicated disposition.
- The corpus schema and registry accept SYN-202 identifiers and reject unknown ones by name.
- Existing Phase 199, 200 and 201 corpus tests remain green.
</verification>

<success_criteria>
- No later Phase 202 plan can be executed without a cited disposition to point at.
- The five RESEARCH.md open items are decided in writing, not left to an executor's improvisation.
</success_criteria>

<output>
Create `.planning/phases/202-swarm-oracle-and-live-colony/202-01-SUMMARY.md` when done.
</output>
