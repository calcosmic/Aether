---
phase: 202-swarm-oracle-and-live-colony
plan: "04"
type: execute
wave: 3
depends_on: ["202-02"]
files_modified:
  - cmd/event_types.go
  - cmd/event_stream.go
  - cmd/event_bridge.go
  - cmd/event_reader.go
  - cmd/event_writer.go
  - cmd/events_test.go
  - cmd/live_model_singleton_test.go
  - cmd/testdata/orphan_allowlist.json
autonomous: true
requirements: [LIVE-01]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Exactly one live-event model exists in the repository; the retired NDJSON stream and its bridge are gone rather than merely unused (LIVE-01)."
    - "The removal is proved caller-free before it happens, by a search over the whole repository rather than by assumption."
    - "A future file reintroducing a second event transport is refused by name with its file position."
    - "Deleting the retired files leaves every other package building and every existing event test green."
    - "The orphan allowlist is not used to keep the retired stream alive; any entry it held for those files is removed rather than retained."
  artifacts:
    - path: "cmd/live_model_singleton_test.go"
      provides: "The guard that refuses a second event transport"
      contains: "TestOneLiveEventModelOnly"
  key_links:
    - from: "cmd/live_model_singleton_test.go"
      to: "pkg/events/colony_live.go"
      via: "the guard names the surviving model as the only permitted transport"
      pattern: "ColonyLiveTopics"
  prohibitions:
    - "Never build a second event bus or revive the retired NDJSON event stream."
    - "Never delete a file whose caller-freedom has not been demonstrated by a repository-wide search first."
    - "Never use the orphan allowlist as a way to keep dead code registered."
---

<objective>
Delete the retired NDJSON event stream, its bridge, its reader and its writer, and install a guard that refuses a second event transport from returning.

Purpose: This repository's documented signature failure is a well-shaped capability that nothing calls. `cmd/event_types.go`, `cmd/event_stream.go`, `cmd/event_bridge.go`, `cmd/event_reader.go` and `cmd/event_writer.go` are a complete, tested, zero-caller event system — a standing invitation for a future contributor to wire the wrong one. LIVE-01 asks for one versioned model reaching all renderers; leaving a second one in the tree with its own tests passing is how "one model" quietly becomes two again.
Output: Five deleted files, their tests removed with them, and one AST guard that names any reintroduction.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md
@.planning/codebase/CONCERNS.md
@pkg/events/colony_live.go
@cmd/live_events.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the retired stream caller-free, then delete it</name>
  <files>cmd/event_types.go, cmd/event_stream.go, cmd/event_bridge.go, cmd/event_reader.go, cmd/event_writer.go, cmd/events_test.go, cmd/testdata/orphan_allowlist.json</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/event_types.go
    - cmd/event_stream.go
    - cmd/event_bridge.go
    - cmd/event_reader.go
    - cmd/event_writer.go
    - cmd/events_test.go
    - cmd/eventbus.go
    - cmd/serve.go
    - cmd/testdata/orphan_allowlist.json
    - cmd/cli_flag_audit_test.go
    - .planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md
  </read_first>
  <reversibility rating="costly">Deleting five source files plus their test file is recoverable from git history, but the decision to consolidate on one transport is what every later emission site in this phase depends on. The synthesis in plan 202-01 records the comparative reasoning; this task executes it.</reversibility>
  <action>Before deleting anything, run a repository-wide search across every tracked Go file, every TypeScript file under `.aether/ts-host`, and every wrapper markdown and YAML source, for references to the exported and unexported symbols declared in the five files — the event-type enum and its constants, the line struct, the stream handle type, the read and tail helpers, the write and memory-buffer helpers, and the bridge synchroniser. Record the search result in the plan summary. If any non-test, non-self reference exists anywhere, stop and report it instead of deleting; the synthesis's retirement disposition assumed zero callers and a live caller would invalidate it.

Given a clean result, delete `cmd/event_types.go`, `cmd/event_stream.go`, `cmd/event_bridge.go`, `cmd/event_reader.go` and `cmd/event_writer.go`, and delete `cmd/events_test.go`, which exists only to exercise them. Leave `cmd/eventbus.go` untouched — it is the CLI surface over the surviving bus and has real callers. Leave `cmd/serve.go` untouched — its streaming endpoints already read the surviving bus and become a renderer of the live model for free.

Remove any entry in `cmd/testdata/orphan_allowlist.json` that referred to the deleted files or their symbols, and leave every other entry byte-identical. Do not lower or raise any baseline count in `cmd/testdata/orphan_allowlist_baseline.json`; if the allowlist audit requires a count adjustment, make it in the allowlist file the audit reads, not in the frozen baseline.

Confirm afterwards that the whole module builds, that `go vet` is clean, and that the surviving event tests in `cmd/eventbus_test.go`, `cmd/eventbus_subscribe_test.go`, `cmd/eventbus_timeout_test.go` and `pkg/events` pass unchanged.</action>
  <acceptance_criteria>
    - The repository-wide search result is recorded in the summary and shows no non-test caller outside the deleted set.
    - The five source files and `cmd/events_test.go` no longer exist.
    - `cmd/eventbus.go` and `cmd/serve.go` are unmodified.
    - `go build ./...` succeeds and `go vet ./...` is clean.
    - `cmd/testdata/orphan_allowlist_baseline.json` is byte-identical to its pre-task content.
    - The existing event bus tests in `cmd` and `pkg/events` pass unchanged.
  </acceptance_criteria>
  <verify>
    <automated>test ! -f cmd/event_types.go &amp;&amp; test ! -f cmd/event_stream.go &amp;&amp; test ! -f cmd/event_bridge.go &amp;&amp; test ! -f cmd/event_reader.go &amp;&amp; test ! -f cmd/event_writer.go &amp;&amp; test ! -f cmd/events_test.go &amp;&amp; go build ./... &amp;&amp; go vet ./... &amp;&amp; go test ./pkg/events -count=1 &amp;&amp; go test ./cmd -run '^TestEventBus' -count=1</automated>
  </verify>
  <done>There is one event transport in the repository, and the one that never ran is gone rather than dormant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refuse a second event transport by name</name>
  <files>cmd/live_model_singleton_test.go</files>
  <read_first>
    - cmd/live_model_singleton_test.go (create; confirm absent)
    - cmd/live_events.go
    - cmd/live_projection.go
    - pkg/events/colony_live.go
    - pkg/events/bus.go
    - cmd/eventbus.go
  </read_first>
  <behavior>
    - A newly added file in package `cmd` that declares its own append-only event line type and writes it to a file is reported by the guard, naming the file and the declaration position.
    - The guard passes against the real package after Task 1.
    - The guard identifies a candidate transport structurally — a type whose serialized form carries a timestamp and an event-kind field, written to a path the package owns — rather than by matching today's file names.
    - The surviving bus, its CLI surface and the streaming server are not reported.
  </behavior>
  <action>Add `TestOneLiveEventModelOnly` to `cmd/live_model_singleton_test.go`. Parse the `cmd` package with `go/parser` and `go/ast` following this package's existing AST-guard precedent. Assert that the only event serialization path reaching durable storage in package `cmd` is the one that goes through the surviving bus — that is, that no other top-level type in the package both declares a serialized timestamp-plus-event-kind shape and is passed to a file write in the same package. Report any candidate by file name and declaration position.

Allow the surviving bus, its CLI subcommand surface and the streaming server explicitly by the symbols they use rather than by their file names, so renaming a file does not silently widen the guard. Include a negative fixture that adds a candidate transport to the parsed set and asserts the guard reports it by name, so the guard is demonstrably able to fail.</action>
  <acceptance_criteria>
    - The guard fails, naming the file and declaration position, when the negative fixture transport is present.
    - The guard passes against the real package after Task 1.
    - The guard's permitted set is expressed as symbols, not file paths.
    - The guard derives candidate transports from the syntax tree rather than from a maintained name list.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestOneLiveEventModelOnly$' -count=1</automated>
  </verify>
  <done>A second event transport cannot reappear quietly; the next one is named by a failing test at the moment it is written.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created by this plan (new to the codebase — not drift):

- `cmd/live_model_singleton_test.go` (new file)
- `TestOneLiveEventModelOnly` (new test)

Files deleted by this plan: `cmd/event_types.go`, `cmd/event_stream.go`, `cmd/event_bridge.go`, `cmd/event_reader.go`, `cmd/event_writer.go`, `cmd/events_test.go`.

<verification>
- One event transport remains, proved by deletion rather than by convention.
- Caller-freedom was demonstrated by search before removal, not assumed.
- The reintroduction guard is proved able to fail by its own negative fixture.
</verification>

<success_criteria>
- LIVE-01's "one versioned model" is structurally true, not a description of intent.
- `go build ./...`, `go vet ./...` and the surviving event tests are clean.
</success_criteria>

<output>
Create `.planning/phases/202-swarm-oracle-and-live-colony/202-04-SUMMARY.md` when done.
</output>
