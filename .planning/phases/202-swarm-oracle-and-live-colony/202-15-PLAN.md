---
phase: 202-swarm-oracle-and-live-colony
plan: "15"
type: execute
wave: 9
depends_on: ["202-06", "202-07", "202-09", "202-11", "202-12", "202-14"]
files_modified:
  - .aether/commands/watch.yaml
  - .aether/commands/swarm.yaml
  - .aether/commands/oracle.yaml
  - .claude/commands/ant/watch.md
  - .claude/commands/ant/swarm.md
  - .claude/commands/ant/oracle.md
  - .opencode/commands/ant/watch.md
  - .opencode/commands/ant/swarm.md
  - .opencode/commands/ant/oracle.md
  - cmd/testdata/classic-contract/v1/cases.json
  - cmd/classic_contract_202_test.go
  - CLAUDE.md
  - cmd/claudemd_live_colony_test.go
autonomous: true
requirements: [LIVE-02, LIVE-03, LIVE-06, CEC-05]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The watch, Swarm and Oracle wrappers invoke the Go runtime and render what it returns; they compute no liveness, no verification and no lifecycle truth (LIVE-02, CEC-05)."
    - "The Claude and OpenCode wrappers for each of the three commands are semantically identical, and the existing ceremony contracts they already carry are unchanged."
    - "Phase 202's behaviours are registered as executable cases in the versioned Classic corpus, each bound to a named Go test that genuinely exists."
    - "Every Phase 202 claim written into the shipped guide names the test that proves it, and a cited test that stops existing fails the build."
    - "A case referencing a Phase 202 decision that no mechanism entry registers fails coverage validation by name."
    - "The whole module builds, vets clean, and passes under race instrumentation after the phase's work is complete."
  artifacts:
    - path: "cmd/testdata/classic-contract/v1/cases.json"
      provides: "Executable Phase 202 behaviour cases"
      contains: "V-202-COCKPIT"
    - path: "cmd/classic_contract_202_test.go"
      provides: "The Phase 202 case-set validation and coverage proof"
      contains: "TestClassicContractPhase202Cases"
    - path: "cmd/claudemd_live_colony_test.go"
      provides: "The guard that every shipped Phase 202 claim names a live test"
      contains: "TestEveryLiveColonyClaimInCLAUDEMDNamesALiveTest"
  key_links:
    - from: "cmd/testdata/classic-contract/v1/cases.json"
      to: ".planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md"
      via: "every case cites the synthesis decision it proves"
      pattern: "SYN-202-"
    - from: "CLAUDE.md"
      to: "cmd/claudemd_live_colony_test.go"
      via: "every documented claim names the test that locks it"
      pattern: "TestSwarmInvestigationWaveReachesTheLiveWatchScreen"
  prohibitions:
    - "Never let a wrapper compute liveness, verification or lifecycle truth."
    - "Never let the Claude and OpenCode wrappers for one command diverge in meaning."
    - "Never remove a string an existing wrapper ceremony contract requires."
    - "Never write a documentation claim about runtime behaviour that no test can fail on."
    - "Never register a corpus case whose named Go test does not exist."
---

<objective>
Close the phase: make the three owner-facing wrappers speak the restored behaviour identically on both platforms, register Phase 202's behaviours as executable cases in the versioned corpus, and make every shipped claim name the test that proves it.

Purpose: The runtime work is only half of an owner-visible restoration — the wrappers are what the owner actually types, and a wrapper that describes behaviour the runtime no longer has is this repository's documented failure pattern. The corpus cases are what turn "Phase 202 shipped" into something a future change can break loudly. And the shipped guide is where a claim about runtime behaviour either names its test or should not be written.
Output: Nine updated wrapper files, a Phase 202 case set in the corpus, updated guide sections, and two guards that fail when any of the three drift.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/202-swarm-oracle-and-live-colony/202-CONTEXT.md
@.planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md
@.aether/docs/wrapper-runtime-ux-contract.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update the watch, Swarm and Oracle wrapper triplets on both platforms</name>
  <files>.aether/commands/watch.yaml, .aether/commands/swarm.yaml, .aether/commands/oracle.yaml, .claude/commands/ant/watch.md, .claude/commands/ant/swarm.md, .claude/commands/ant/oracle.md, .opencode/commands/ant/watch.md, .opencode/commands/ant/swarm.md, .opencode/commands/ant/oracle.md</files>
  <read_first>
    - AGENTS.md
    - .aether/commands/watch.yaml
    - .aether/commands/swarm.yaml
    - .aether/commands/oracle.yaml
    - .claude/commands/ant/watch.md
    - .claude/commands/ant/swarm.md
    - .claude/commands/ant/oracle.md
    - .opencode/commands/ant/watch.md
    - .opencode/commands/ant/swarm.md
    - .opencode/commands/ant/oracle.md
    - cmd/command_parity_test.go
    - cmd/swarm_wrapper_ceremony_test.go
    - cmd/command_source_hygiene_test.go
    - .aether/docs/wrapper-runtime-ux-contract.md
  </read_first>
  <behavior>
    - Each wrapper's runtime line invokes the Go command once and renders its output; no wrapper inspects a file, computes liveness or decides a mode.
    - The watch wrapper describes three possible screens — live, the last episode replayed, and the honest idle card — and states that the runtime chooses which.
    - The Swarm wrapper describes the four lenses, the comparison card, the checkpointed repair and the three-strike case, and keeps every string its existing ceremony contract requires, unchanged and in the same order.
    - The Oracle wrapper describes the one clarification at the start, the four shared depth names, the watchable rounds and the recommendation-first answer.
    - The Claude and OpenCode copies of each command carry the same meaning, and the parity test passes.
    - Each managed wrapper keeps its runtime-spec header so the sync mechanism still recognizes it as managed.
  </behavior>
  <action>Update the three YAML sources first, since the wrappers derive from them: in `.aether/commands/watch.yaml`, replace the compatibility-surface description with the three-screen behaviour, naming that the runtime resolves which screen applies and that the wrapper never decides; add the refresh flags to the runtime line's documented arguments; keep the existing artifact and guardrail entries. In `.aether/commands/swarm.yaml`, describe the four lenses, the end-of-investigation comparison card, the checkpointed and rolled-back repair, and the third-strike architectural case, leaving every existing ceremony contract entry intact. In `.aether/commands/oracle.yaml`, describe the single clarification step, the four shared depth names with each preset's own target and cap, the watchable rounds, and the recommendation-first answer.

Then update the six markdown wrappers so the Claude and OpenCode copies of each command carry the same meaning. Keep the managed-file header comment at the top of every one. For `swarm.md`, keep every string `TestSwarmWrapperCeremonyContract` requires, in its required order, and add the new descriptions around them rather than between two strings the order test pairs.

Write every owner-facing sentence in ordinary words, translating any colony term inline the first time it appears in each file.

Confirm `TestClaudeOpenCodeCommandParity`, `TestSwarmWrapperCeremonyContract` and the command source hygiene tests all pass.</action>
  <acceptance_criteria>
    - All nine files carry their managed header or YAML shape unchanged in kind.
    - `TestClaudeOpenCodeCommandParity` passes.
    - `TestSwarmWrapperCeremonyContract` passes with its required strings present and in order.
    - The command source hygiene tests pass.
    - Each of the three markdown pairs is semantically identical between the Claude and OpenCode copies, asserted by the parity test rather than by reading.
    - No wrapper instructs the assistant to read a colony file, compute a mode or decide a verification outcome.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestClaudeOpenCodeCommandParity|TestSwarmWrapperCeremonyContract|TestCommandSourceHygiene|TestOracleYamlWrapperRoleMatchesRuntimeOwnership)$' -count=1</automated>
  </verify>
  <done>The three commands the owner types describe what the runtime now actually does, identically on both platforms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register Phase 202's behaviours as executable corpus cases</name>
  <files>cmd/testdata/classic-contract/v1/cases.json, cmd/classic_contract_202_test.go</files>
  <read_first>
    - cmd/testdata/classic-contract/v1/cases.json
    - cmd/testdata/classic-contract/v1/mechanisms.json
    - cmd/testdata/classic-contract/v1/schema.json
    - cmd/classic_contract_test.go
    - .planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md
  </read_first>
  <behavior>
    - Every Phase 202 case validates against the corpus schema and names a synthesis decision the mechanism registry declares.
    - Each of the six Phase 202 groups carries at least two cases, and at least one of those is a negative case proving a rejected shortcut did not return.
    - Every case names a Go test symbol that exists in the package, and a case naming a symbol that does not exist fails by name.
    - Every one of the twelve Phase 202 synthesis decisions is covered by at least one case.
    - Adding a Phase 202 decision with no case fails coverage validation naming the decision.
    - The existing 126 cases are unchanged.
  </behavior>
  <action>Append Phase 202 cases to `cmd/testdata/classic-contract/v1/cases.json`, following the exact field shape the existing Phase 201 cases use — identifier, group, kind, historical anchors, synthesis decision, capability identifiers, source citations, platform, command, and the expected block with its outcome, semantic fields, requirement identifiers, digest expectation, proof reference, structured assertions and Go test symbol. Distribute them across the six groups so each carries at least two cases: the live event model and lane coverage; the cockpit, ticker and replay-backed idle; the four lenses and the comparison card; the checkpointed repair, rollback and three-strike case; the durable episode, retention and learning proposal; and the Oracle presets, rounds and recommendation-first synthesis.

Give every case a Go test symbol from the tests this phase's plans created, and include at least one negative case per group proving a rejected shortcut did not return — a second event transport, an invented liveness row, four cosmetically identical lenses, an uncheckpointed repair, a prefix-based deletion, and a synthesis that leads with its sources.

Add `cmd/classic_contract_202_test.go` with `TestClassicContractPhase202Cases`, loading the corpus through the existing strict loader, asserting every Phase 202 case validates, that each named Go test symbol exists in the package's parsed syntax tree, that each of the twelve Phase 202 decisions has at least one case, and that each of the six groups has at least two cases including one negative. Prove the guard can fail with a negative fixture naming a symbol that does not exist and a fixture referencing an unregistered decision.

Leave the existing cases byte-identical.</action>
  <acceptance_criteria>
    - Every Phase 202 case validates against the schema through the existing strict loader.
    - Each of the six Phase 202 groups has at least two cases and at least one negative case.
    - Each of `SYN-202-01` through `SYN-202-12` is named by at least one case.
    - Every case's Go test symbol is found in the parsed `cmd` package.
    - A fixture case naming a nonexistent symbol fails with that symbol named.
    - A fixture case referencing an unregistered decision fails with that decision named.
    - The 126 pre-existing cases are unchanged, asserted by count and by digest of their entries.
    - `TestClassicContractSchema` and `TestClassicMechanismCoverage` pass.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestClassicContractPhase202Cases|TestClassicContractSchema|TestClassicMechanismCoverage|TestClassicContractPhase201MechanismRegistry|TestClassicContractPhase202MechanismRegistry)$' -count=1 &amp;&amp; python3 -c "import json,sys; d=json.load(open('cmd/testdata/classic-contract/v1/cases.json')); g=[c['group'] for c in d['cases'] if str(c['group']).startswith('V-202-')]; import collections; n=collections.Counter(g); sys.exit(0 if len(n)==6 and min(n.values())>=2 and len(d['cases'])-len(g)==126 else 1)"</automated>
  </verify>
  <done>Phase 202's behaviours are executable contract, so a future change that removes one fails a named test rather than passing quietly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make every shipped claim name its test, then sweep the phase</name>
  <files>CLAUDE.md, cmd/claudemd_live_colony_test.go</files>
  <read_first>
    - CLAUDE.md
    - cmd/phase_end_hive_test.go
    - cmd/claudemd_coherent_jobs_test.go
    - cmd/claudemd_verification_depth_test.go
    - .planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md
  </read_first>
  <behavior>
    - The guide gains a section describing the live colony view, the four-lens Swarm and the iterative Oracle, in ordinary words with each colony term translated inline.
    - Every sentence in that section that claims runtime behaviour names the Go test that locks it.
    - A named test that stops existing fails the guard by name.
    - The guard finds candidate claims structurally rather than from a maintained list of sentences.
    - The guide's existing sections are unchanged.
    - The whole module builds, vets clean and passes under race instrumentation.
  </behavior>
  <action>Add a section to `CLAUDE.md` describing what this phase delivered: the live colony view with its three screens and its ticker, the four-lens Swarm with its comparison card and its checkpointed repair, the iterative Oracle with its one clarification and its recommendation-first answer, and the shared standing vocabulary for unproven work. Translate every colony-invented term inline the first time it appears, and close the section with a plain-language paragraph in the same style as the existing ones. Name, beside each behavioural claim, the Go test that proves it.

Add `cmd/claudemd_live_colony_test.go` with `TestEveryLiveColonyClaimInCLAUDEMDNamesALiveTest`, following the precedent of the existing learning-claim guard: parse the new section, find every sentence that asserts runtime behaviour, require each to name at least one test symbol, and assert every named symbol exists in the parsed `cmd` package. Report a missing symbol by name and by the sentence that cited it. Include a negative fixture citing a symbol that does not exist so the guard is demonstrably able to fail.

Change no other section of the guide.

Finish with a full sweep: build the whole module, vet it, and run the complete test suite both normally and under race instrumentation, recording the pass counts in the summary.</action>
  <acceptance_criteria>
    - The new guide section exists and every behavioural claim in it names at least one Go test symbol.
    - Every named symbol is found in the parsed `cmd` package.
    - The negative fixture fails with the missing symbol and the citing sentence named.
    - Every other section of the guide is byte-identical to its pre-task content.
    - `go build ./...` succeeds, `go vet ./...` is clean.
    - `go test ./...` and `go test ./... -race` both pass, with the counts recorded in the summary.
    - `TestEveryLearningClaimInCLAUDEMDNamesALiveTest` and the other guide guards still pass.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestEveryLiveColonyClaimInCLAUDEMDNamesALiveTest|TestEveryLearningClaimInCLAUDEMDNamesALiveTest|TestCLAUDEMDVerificationDepthClaims|TestCLAUDEMDNoOldModes|TestCLAUDEMDDepthTableEvaluates|TestCLAUDEMDStatesCoherentJobContract|TestCLAUDEMDCitesLiveTestsForCoherentJobClaims)$' -count=1 &amp;&amp; go build ./... &amp;&amp; go vet ./... &amp;&amp; go test ./... -count=1</automated>
  </verify>
  <done>Everything this phase claims in the shipped guide can be falsified by a named test, and the whole module is green.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created by this plan (new to the codebase — not drift):

- `cmd/classic_contract_202_test.go`, `cmd/claudemd_live_colony_test.go` (new files)
- Phase 202 case entries in `cmd/testdata/classic-contract/v1/cases.json` (new corpus cases)
- `TestClassicContractPhase202Cases`, `TestEveryLiveColonyClaimInCLAUDEMDNamesALiveTest` (new tests)
- A new live-colony section in `CLAUDE.md`

<verification>
- Wrapper parity and the existing ceremony contracts both pass on the updated triplets.
- Every Phase 202 synthesis decision is covered by at least one executable case with a real Go test symbol.
- Every shipped claim names a test that exists, proved by a guard with its own negative fixture.
- The module builds, vets and passes normally and under race instrumentation.
</verification>

<success_criteria>
- The commands the owner types, the contract the corpus enforces and the guide the project ships all describe the same runtime.
- `go test ./...`, `go test ./... -race` and `go vet ./...` are clean.
</success_criteria>

<output>
Create `.planning/phases/202-swarm-oracle-and-live-colony/202-15-SUMMARY.md` when done.
</output>
