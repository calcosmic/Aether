---
phase: 25-medic-ant-core
plan: 01
type: execute
wave: 1
files_modified:
  - cmd/medic_cmd.go
  - cmd/medic_cmd_test.go
  - cmd/codex_visuals.go
  - .aether/agents-claude/aether-medic.md
  - .claude/agents/ant/aether-medic.md
  - .opencode/agents/aether-medic.md
  - .codex/agents/aether-medic.toml
autonomous: true
requirements:
  - R039
  - R044

must_haves:
  truths:
    - "`aether medic` command exists and runs without errors"
    - "Medic caste identity exists with 🩹 emoji, color, and deterministic name"
    - "Output is human-readable visual report by default, --json for programmatic"
    - "Read-only by default; --fix required for any mutation"
    - "Exit codes: 0=healthy, 1=warnings, 2=critical issues"
  artifacts:
    - path: "cmd/medic_cmd.go"
      provides: "Medic CLI command implementation"
      contains: "medicCmd, runMedic"
    - path: "cmd/medic_cmd_test.go"
      provides: "Medic command tests"
      contains: "TestMedicCommand"
  key_links:
    - from: "medic command"
      to: "health scanner"
      via: "medic scanner interface"
---

<objective>
Build the Medic CLI command (`aether medic`) with visual output, caste identity, and proper flag handling. Create the Medic worker definition across all platforms (Claude, OpenCode, Codex).

Purpose: Give users a manual entry point to diagnose colony health, and establish the Medic as a first-class colony worker.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add Medic caste to visual system</name>
  <files>cmd/codex_visuals.go</files>
  <read_first>
    - cmd/codex_visuals.go (casteEmojiMap, casteColorMap, casteLabelMap)
  </read_first>
  <action>
    1. Add `"medic"` entry to `casteEmojiMap` with emoji `🩹`
    2. Add `"medic"` entry to `casteColorMap` with a distinct color (e.g., cyan/bright blue)
    3. Add `"medic"` entry to `casteLabelMap` with label `"Medic"`
    4. Add test in `cmd/codex_visuals_test.go` verifying Medic caste identity renders correctly
  </action>
  <acceptance_criteria>
    - `casteIdentity("medic")` returns string containing `🩹` and `"Medic"`
    - Test passes: `TestCasteIdentityMedic` (or existing caste identity test covers medic)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create medic command file with CLI flags</name>
  <files>cmd/medic_cmd.go</files>
  <read_first>
    - cmd/status.go (command pattern, visual output)
    - cmd/patrol.go (system check pattern)
    - cmd/root.go (command registration)
  </read_first>
  <action>
    1. Create `cmd/medic_cmd.go`:
       - Define `medicCmd` cobra command with:
         - Use: `"medic"`
         - Short: `"Diagnose colony health"`
         - Flags:
           - `--fix` — enable repair mode (default false)
           - `--force` — allow destructive repairs (default false)
           - `--json` — output structured JSON (default false)
           - `--deep` — scan all files including wrappers (default false)
       - `RunE` function `runMedic(cmd, args)`:
         - Load colony state
         - If no colony: render "No colony initialized" visual
         - Build `MedicOptions` struct from flags
         - Call `performHealthScan(options)` (stub for now — scanner implemented in Plan 02)
         - Render results via `renderMedicReport(results, options)`
         - Return exit code based on max severity found
    2. Register command in `cmd/root.go`
    3. Add `MedicOptions` struct:
       ```go
       type MedicOptions struct {
           Fix   bool
           Force bool
           JSON  bool
           Deep  bool
       }
       ```
    4. Add `HealthIssue` struct for results:
       ```go
       type HealthIssue struct {
           Severity string // "critical", "warning", "info"
           Category string // "state", "pheromone", "session", "wrapper", "trace", etc.
           Message  string
           File     string
           Fixable  bool
       }
       ```
  </action>
  <acceptance_criteria>
    - `aether medic --help` shows all flags
    - `aether medic` runs without panic on repo with no colony
    - `aether medic` runs without panic on repo with active colony
    - Command registered in rootCmd
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement visual report renderer</name>
  <files>cmd/medic_cmd.go</files>
  <read_first>
    - cmd/codex_visuals.go (renderBanner, renderStageMarker, visualDivider)
    - cmd/status.go (renderDashboard pattern)
  </read_first>
  <action>
    1. Implement `renderMedicReport(results []HealthIssue, opts MedicOptions) string`:
       - Banner: `renderBanner(commandEmoji("medic"), "Colony Health")`
       - Summary section: counts of critical/warning/info issues
       - Critical Issues section (if any): list with red severity marker
       - Warnings section (if any): list with yellow severity marker
       - Info section (if any): list with blue severity marker
       - If `--fix` was used: Repair Log section showing what was fixed
       - Next Steps section: guidance based on severity
         - Critical: "Run `aether medic --fix` to attempt repairs"
         - Warning: "Review warnings above. Some issues can be auto-fixed."
         - Healthy: "Colony is healthy. No action needed."
    2. Implement `renderMedicJSON(results []HealthIssue) string`:
       - Marshal results to JSON with metadata (timestamp, colony goal, phase)
    3. Add severity color helper:
       ```go
       func severityColor(sev string) string {
           switch sev {
           case "critical": return "\033[31m" // red
           case "warning": return "\033[33m" // yellow
           case "info": return "\033[34m" // blue
           default: return ""
           }
       }
       ```
  </action>
  <acceptance_criteria>
    - Visual report contains banner, summary counts, issue lists by severity
    - JSON output is valid parseable JSON
    - Report includes next-steps guidance
    - All existing tests still pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Create Medic worker definitions</name>
  <files>.aether/agents-claude/aether-medic.md, .claude/agents/ant/aether-medic.md, .opencode/agents/aether-medic.md, .codex/agents/aether-medic.toml</files>
  <read_first>
    - .aether/agents-claude/aether-builder.md (frontmatter pattern)
    - .claude/agents/ant/aether-builder.md (agent definition pattern)
    - .codex/agents/aether-builder.toml (TOML format)
  </read_first>
  <action>
    1. Create `.aether/agents-claude/aether-medic.md`:
       - Frontmatter: name, emoji, color, label matching codex_visuals.go
       - Description: "Use this agent when diagnosing and repairing colony health issues"
       - Resilience sections (LOW risk — read-only unless --fix)
    2. Mirror to `.claude/agents/ant/aether-medic.md`
    3. Create `.opencode/agents/aether-medic.md` (structural parity)
    4. Create `.codex/agents/aether-medic.toml`:
       - TOML format matching existing agents
       - Name: "aether-medic"
       - Description: Medic worker for colony health
    5. Update agent count in `CLAUDE.md` Quick Reference (25 agents)
  </action>
  <acceptance_criteria>
    - All 4 agent files exist with consistent frontmatter
    - Medic emoji is 🩹 across all files
    - Agent count in CLAUDE.md updated to 25
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Add tests for medic command</name>
  <files>cmd/medic_cmd_test.go</files>
  <read_first>
    - cmd/medic_cmd.go (implementation)
    - cmd/status_test.go (test patterns)
  </read_first>
  <action>
    1. Create `cmd/medic_cmd_test.go`:
       - `TestMedicCommandNoColony` — verify graceful handling when no colony exists
       - `TestMedicCommandWithColony` — verify runs without error on active colony
       - `TestMedicCommandJSONOutput` — verify `--json` produces valid JSON
       - `TestMedicCommandFixFlag` — verify `--fix` is parsed correctly
       - `TestMedicReportRendering` — verify visual output contains expected sections
    2. All tests should use `setupBuildFlowTest` or similar test infrastructure
    3. Mock health scanner for testing (injectable interface)
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run "TestMedic" -v` passes all subtests
    - Tests cover no-colony, with-colony, JSON output, and flag parsing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Medic CLI → colony data | Read-only by default; `--fix` required for mutation |
| Medic report → user | Visual output may expose internal paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | Report exposes file paths | accept | File paths are local only, no remote exposure |
| T-25-02 | Denial of Service | `--fix` could corrupt data | mitigate | Backups before repair, `--force` required for destructive ops |
</threat_model>

<verification>
- `go test ./cmd -run "TestMedic" -v` — all pass
- `go test ./cmd/...` — all pass
- `go vet ./...` — clean
- `go build ./cmd/aether` — compiles
- `aether medic --help` — shows correct flags
- `aether medic` — runs on repo with no colony (visual output)
- `aether medic` — runs on repo with colony (shows scan results)
- `aether medic --json` — produces valid JSON
</verification>

<success_criteria>
- `aether medic` command exists and is registered
- Medic caste identity (🩹) exists in visual system
- Visual report renders banner, summary, issues by severity, next steps
- JSON output mode produces valid structured data
- `--fix` and `--force` flags are parsed but do not mutate (scanner in Plan 02)
- Tests cover all command paths
- Agent definitions exist for all 3 platforms
</success_criteria>
