---
phase: 194-the-queen-decides-the-team
plan: 05
type: execute
wave: 5
depends_on: ["194-04"]
files_modified:
  - cmd/one_task_bug_fix_test.go
  - cmd/boundary_double_dispatch_test.go
  - cmd/claudemd_verification_depth_test.go
  - CLAUDE.md
  - .claude/commands/ant/build.md
  - .claude/commands/ant-build.md
  - .opencode/commands/ant/build.md
  - .claude/commands/ant/continue.md
  - .claude/commands/ant-continue.md
  - .opencode/commands/ant/continue.md
  - .aether/docs/wrapper-runtime-ux-contract.md
  - .planning/WINDOWS.md
  - .planning/todos/pending/2026-08-21-weight-classes-pipeline-fits-the-task.md
autonomous: true
requirements: [TEAM-01, TEAM-04, TEAM-05]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A one-task bug-fix phase is sent one worker plus the program's own checks, counting build and continue together, on the proposal path AND on the no-proposal fallback path (TEAM-04, roadmap criterion 4)."
    - "For a production-mode phase with a security signal and no proposal, no caste appears in both the build dispatch list and the continue dispatch list — the defect recorded as broken-window #1 is closed by a test that walks a real manifest and a real continue plan (D-05)."
    - "The documentation of what each depth means matches what the runtime does, for every cell of the table, asserted by evaluating the runtime rather than by finding words in the file (CLAUDE.md Definition of Done)."
    - "No shipped instruction file still states that every build receives a verification worker, or that any caste is required by inferred project type."
    - "All three copies of each wrapper command file remain byte-identical after the edit."
    - "The folded todo about small jobs getting a small pipeline is marked resolved, naming the test that proves it."
    - statement: "The measured worker count for the sample one-task bug fix is recorded in the SUMMARY alongside the 2026-08-22 baseline of eight, so the improvement is a number and not a claim."
      verification: backstop
  artifacts:
    - cmd/one_task_bug_fix_test.go
    - cmd/boundary_double_dispatch_test.go
  key_links:
    - "build dispatch list + continue dispatch list -> one counted total (the thing the owner feels)"
    - "CLAUDE.md depth table -> queenMaxWorkersForBudget and isAlwaysRequired (documented contract must evaluate, not just appear)"
    - "WINDOWS.md #1 -> gsd-tools windows fixed 1 (the ship gate reopens)"
  prohibitions:
    - statement: "A documentation claim about runtime behaviour must be testable or removed. No sentence may be added to CLAUDE.md or a wrapper file in this plan that no test can falsify."
      status: unverified
      verification: flagged
    - statement: "A broken-window entry must never be marked fixed on the strength of a summary. It is marked fixed only after a test that walks the real dispatch lists passes."
      status: unverified
      verification: flagged
---

<objective>
Prove the number the owner actually cares about, close the defect that blocks
shipping, and correct every document that still describes the old rule. A
one-task bug fix was measured at eight workers on 2026-08-22 — five at build,
three at the checking step — against three or four in the older version of
Aether. This plan asserts the new number end to end on both paths, walks a real
manifest and a real continue plan to prove no worker is summoned twice, and
rewrites the two CLAUDE.md sections and the six wrapper files that still teach
the deleted rules.

Purpose: roadmap criteria 4 and 5, closing `.planning/WINDOWS.md` #1.
Output: two proof tests, corrected documentation, a closed broken window, and a
resolved todo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/194-the-queen-decides-the-team/194-CONTEXT.md
@.planning/phases/194-the-queen-decides-the-team/194-01-SUMMARY.md
@.planning/phases/194-the-queen-decides-the-team/194-02-SUMMARY.md
@.planning/phases/194-the-queen-decides-the-team/194-03-SUMMARY.md
@.planning/phases/194-the-queen-decides-the-team/194-04-SUMMARY.md
@.planning/WINDOWS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
New symbols created by this plan:

- `cmd/one_task_bug_fix_test.go` (new file) with `TestOneTaskBugFixIsOneWorkerPlusChecks`
- `cmd/boundary_double_dispatch_test.go` (new file) with `TestNoCasteIsDispatchedAtBothBoundaries`
- `countWorkersAcrossBothBoundaries(...)` (test helper, in `cmd/one_task_bug_fix_test.go`)
- `TestCLAUDEMDDepthTableEvaluates` (replaces the position-based assertions inside `TestCLAUDEMDVerificationDepthClaims`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The one-task bug fix, counted end to end, and the double-dispatch window closed</name>
  <files>cmd/one_task_bug_fix_test.go, cmd/boundary_double_dispatch_test.go, .planning/WINDOWS.md</files>
  <read_first>
    - cmd/phase_verified_once_test.go lines 190-300 (`TestPhaseVerifiedOnce` — it already walks a real build manifest and a real continue plan for the same phase; this is the harness to copy, not to reinvent)
    - cmd/queen_judgement_test.go lines 200-260 (`TestQueenChoiceReachesTheDispatchList` — the assert-the-spawn-list discipline)
    - cmd/codex_build.go `plannedBuildDispatchesWithJudgement`
    - cmd/codex_continue.go `plannedContinueReviewDispatches`
    - .planning/WINDOWS.md (entry #1 — its description is the exact defect this task closes)
    - .planning/research/v1.27-milestone-brief.md (the measured eight-worker baseline and its breakdown)
  </read_first>
  <behavior>
    - A phase with one task, prototype or maintenance mode, no security wording, standard depth: build produces exactly one worker dispatch, continue produces zero reviewer dispatches. Total across both boundaries: one.
    - The same phase with a proposal naming only the implementation worker and a reason for it: still one, total.
    - A production-mode phase whose wording names a security signal, with no proposal: the build dispatch list and the continue dispatch list share no caste.
    - A phase where the owner explicitly asked for heavy: more than one worker is expected and the test must not assert one — it asserts the panel instead, so the proof cannot pass by everything having become empty.
  </behavior>
  <action>
Write `TestOneTaskBugFixIsOneWorkerPlusChecks` in a new
`cmd/one_task_bug_fix_test.go`. Build the fixture as the measured field case,
not a convenient one: a single-task phase describing an ordinary defect fix,
with no vocabulary from the five-signal table and no performance, security or
research words. Write `countWorkersAcrossBothBoundaries` as a helper that runs
`plannedBuildDispatchesWithJudgement` and `plannedContinueReviewDispatches` for
the same phase and state and returns the total dispatch count and the union of
castes, because the number the owner feels is the sum, not either half.

Run the assertion twice: once on the proposal path (a proposal naming the
implementation worker, with a per-worker reason) and once on the fallback path
(nil proposal, nil reasons) — D-11 says both must land on the same team, and
the whole point of the retune is that the automatic path is no longer more
expensive than the judged one.

Add a guard row so the test cannot pass by the system having become inert: the
same phase with an explicit heavy request must produce the full review panel at
the checking step. A test that only ever asserts a smaller number will keep
passing when the pipeline stops working entirely, which is the v1.26 lesson
this repo already paid for.

Record the measured count in the test's own failure message against the
2026-08-22 baseline of eight, so a regression reads as "this went from one back
to N" rather than as an unexplained assertion failure.

Write `TestNoCasteIsDispatchedAtBothBoundaries` in a new
`cmd/boundary_double_dispatch_test.go`, copying the harness shape from
`TestPhaseVerifiedOnce`. Use a production-mode phase whose wording names a
security signal and pass NO proposal — that is the exact condition recorded in
the broken-window entry, where build and continue each derived the same
required caste independently. Assert the intersection of the two caste sets is
empty. Assert it for a second fixture too: a phase whose wording names a
migration, so both reviewer classes are exercised.

Only after both tests pass, mark the window closed:
`node ~/.claude/gsd-core/bin/gsd-tools.cjs windows fixed 1`. Do not mark it
fixed first and write the test after; the entry exists because a previous
milestone marked something done on the strength of a summary.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestOneTaskBugFixIsOneWorkerPlusChecks|TestNoCasteIsDispatchedAtBothBoundaries' -count=1` passes
    - `countWorkersAcrossBothBoundaries` returns a total of 1 for the bug-fix fixture on the proposal path and 1 on the fallback path
    - The same helper returns a total greater than 1 for the same fixture with an explicit heavy request
    - For the production-plus-security fixture with no proposal, the intersection of the build caste set and the continue caste set is empty
    - `.planning/WINDOWS.md` frontmatter shows `open_count: 0` and entry 1 has status `fixed` with a `resolved_at` timestamp
    - `go test ./cmd -count=1` passes
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestOneTaskBugFixIsOneWorkerPlusChecks|TestNoCasteIsDispatchedAtBothBoundaries' -count=1 &amp;&amp; go test ./cmd -count=1</automated>
  </verify>
  <done>The sample bug fix costs one worker plus the program's own checks on both paths, no worker is summoned at both boundaries, and the broken window is closed by a passing test rather than by a claim.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the two CLAUDE.md sections that describe the old floor, and make the table evaluate</name>
  <files>CLAUDE.md, cmd/claudemd_verification_depth_test.go</files>
  <read_first>
    - CLAUDE.md, the "Queen-Owned Orchestration" section in full (the depth priority list, the smart defaults list, the three-column depth table, the required-caste table with its four rows, and the "for dummies" paragraph)
    - CLAUDE.md, the "Team Check-In and Owner Decisions" section in full
    - CLAUDE.md, the "Definition of Done" section (the rule this task is governed by) and the block at the top about writing for the owner
    - cmd/claudemd_verification_depth_test.go (whole file — note that `TestCLAUDEMDVerificationDepthClaims` mixes word-presence checks with runtime evaluation, and that its final assertion depends on the position rule plan 194-03 deleted)
    - .planning/phases/194-the-queen-decides-the-team/194-01-SUMMARY.md through 194-04-SUMMARY.md (the behaviour that actually shipped — write the documentation from these, not from this plan's intentions)
  </read_first>
  <action>
Rewrite, do not append. Both sections describe a floor that no longer exists,
and this repo's own history shows that leaving a stale paragraph beside a new
one produces two contradictory truths and no way to tell which is current.

In "Queen-Owned Orchestration": the priority list keeps the explicit-flag rows
and the keyword row; delete the row that says a smart default is chosen from
phase position. The smart-defaults list loses its final-phase row entirely —
the last phase of a plan is now treated like any other phase, which is the
owner's choice of 2026-08-23 and should be stated as such. The three-column
depth table keeps its build row unchanged (the worker-cap numbers are still
true and still asserted) and gets a rewritten continue row and seal row per the
shipped behaviour from plan 194-03. The four-row required-caste table is
replaced by the new rule: one row for the worker that writes the code, and one
row for the reviewer forced by a named signal, listing the five signals and
which reviewer each one summons. Delete the rows for the coverage caste, the
quality caste and the verification caste as required castes — they are now
judgement or nothing.

Add one short paragraph stating that the program's own checks are unchanged by
any of this and run on every phase at every depth, because a reader seeing the
worker count fall will otherwise reasonably assume the checking fell with it.
Rewrite the "for dummies" paragraph at the end of that section from scratch in
the same voice, describing the new behaviour: who turns up by default, what
makes a reviewer turn up anyway, and that saying "light" never switches off the
program's own checks.

In "Team Check-In and Owner Decisions": correct the sentence that describes
required workers as fixed because the runtime re-adds them, to say what is now
true — the worker that writes the code and any reviewer forced by a named
signal are shown as required, the signal is shown beside the reviewer, and the
owner can decline the forced reviewer with a reason that is recorded. Name the
tests that lock it, in the same style the section already uses for its other
claims.

Then fix the test. `TestCLAUDEMDVerificationDepthClaims` asserts the presence of
the final-phase rule as a string and then evaluates it against the runtime; both
halves must go. Split the test: keep the word-presence checks only where no
runtime evaluation is possible, and add `TestCLAUDEMDDepthTableEvaluates`, which
reads the depth table's cells and evaluates each one against
`queenMaxWorkersForBudget` and `isAlwaysRequired` — the same discipline
`TestBuildWorkerCapHonoursVerificationDepth` already applies to the build row,
extended to the continue row. A documentation test that only looks for words is
what let the table stay false for months.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestCLAUDEMDVerificationDepthClaims|TestCLAUDEMDNoOldModes|TestCLAUDEMDDepthTableEvaluates|TestBuildWorkerCapHonoursVerificationDepth' -count=1` passes
    - `grep -c 'Final phase → heavy' CLAUDE.md` returns 0
    - `grep -c 'Watcher | always | always — unchanged, and must stay so' CLAUDE.md` returns 0
    - `grep -c 'always, on every build' CLAUDE.md` returns 0
    - CLAUDE.md contains the words `credentials`, `payments`, and `migration` inside the "Queen-Owned Orchestration" section
    - `TestCLAUDEMDDepthTableEvaluates` fails if the continue row of the table is edited to a different caste set than `isAlwaysRequired` returns — demonstrate this by temporarily editing the table, running the test, and restoring; record the observed failure in the SUMMARY
    - `go test ./cmd -count=1` passes
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestCLAUDEMDVerificationDepthClaims|TestCLAUDEMDNoOldModes|TestCLAUDEMDDepthTableEvaluates|TestBuildWorkerCapHonoursVerificationDepth' -count=1 &amp;&amp; go test ./cmd -count=1</automated>
  </verify>
  <done>The two CLAUDE.md sections describe what the runtime does, and the depth table is checked by evaluating the runtime rather than by finding words in the file.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the six wrapper files, the UX contract, and close the folded todo</name>
  <files>.claude/commands/ant/build.md, .claude/commands/ant-build.md, .opencode/commands/ant/build.md, .claude/commands/ant/continue.md, .claude/commands/ant-continue.md, .opencode/commands/ant/continue.md, .aether/docs/wrapper-runtime-ux-contract.md, .planning/todos/pending/2026-08-21-weight-classes-pipeline-fits-the-task.md</files>
  <read_first>
    - .claude/commands/ant/build.md lines 165-215 (the "What the runtime will do to your choice" block and its worked-examples table) and lines 240-275 (the Team Check-In stage)
    - .claude/commands/ant/continue.md lines 118-135 (the paragraph describing which reviewers the runtime restores)
    - .aether/docs/wrapper-runtime-ux-contract.md (the rules a wrapper edit must not break)
    - .planning/todos/pending/2026-08-21-weight-classes-pipeline-fits-the-task.md (whole file)
    - .planning/phases/194-the-queen-decides-the-team/194-04-SUMMARY.md (the waive choice as it actually shipped, including any wording the owner changed at the checkpoint)
    - Any test asserting the three copies of a wrapper command file are identical — grep the test files for the file names before editing, so you know which test will catch a partial edit
  </read_first>
  <action>
Edit all three copies of `build.md` identically and all three copies of
`continue.md` identically. They are byte-identical triplets held that way by a
parity test; a single-copy edit is caught, but a near-identical edit with
different trailing whitespace is the failure mode to avoid — make the change
once and copy the file, rather than typing it three times.

In `build.md`, the block headed "What the runtime will do to your choice"
contains a sentence asserting that the verification caste accompanies every
build and that credential, auth and release-gate work always draws a security
review. Both halves are now false: the first is the rule the 2026-08-22 ruling
retired, and the second overstates the second half of the new rule, which
forces one reviewer per named signal at the checking step rather than at the
build. Replace the whole sentence with the new rule stated plainly: the worker
that writes the code is added back if omitted; a reviewer is added only when the
phase touches one of five named things, that reviewer runs at the checking step
rather than during the build, and the card says which of the five it was.

Add the per-worker reason requirement to the "Applying the decision" block: the
example invocation must show the new repeatable reason flag alongside the caste
list, and the surrounding text must say that a worker named without its own
reason is refused by name and the rest of the team still goes. Keep the existing
worked-examples table — its verdicts are still correct — but correct the row
whose verdict names the verification caste as automatically accompanying the
implementation worker.

In the Team Check-In stage of `build.md`, add the waive choice as a fourth
option in the question the wrapper asks, worded from `194-04-SUMMARY.md`. It
must state the consequence before naming anything: declining means the security
or quality reviewer will not run on this phase, the reason is written down, and
it applies to that one thing on that one phase. The wrapper relays the runtime's
command; it does not construct the question text itself.

In `continue.md`, correct the paragraph describing which reviewers the runtime
restores. An empty optional team is now genuinely empty, not the verification
caste alone, and the sentence about a phase that requires a security or quality
review must be narrowed to the five named signals.

Update `.aether/docs/wrapper-runtime-ux-contract.md` if any rule there restates
the old floor.

Finally, mark the folded todo resolved. Its ask — that a small job should
automatically get a small pipeline — is answered without a separate lane by the
shrunken floor, the retuned fallback, and the depth dials. Record the resolution
in the todo file naming the test that proves it, and move the file to the
resolved location the project's todo convention uses (check how a previously
resolved todo is stored before moving this one).
  </action>
  <acceptance_criteria>
    - `grep -rc 'A build always gets a Watcher' .claude/commands/ .opencode/commands/` returns 0 for every file
    - `md5 .claude/commands/ant/build.md .claude/commands/ant-build.md .opencode/commands/ant/build.md` shows three identical checksums
    - `md5 .claude/commands/ant/continue.md .claude/commands/ant-continue.md .opencode/commands/ant/continue.md` shows three identical checksums
    - Each copy of `build.md` contains the string `--caste-why`
    - Each copy of `build.md` contains the word `waive` or `decline` inside the Team Check-In stage
    - Each copy of `continue.md` no longer states that the verification caste is restored when omitted
    - `go test ./cmd -count=1` passes, including whichever test enforces wrapper-copy parity
    - The folded todo file records a resolution naming `TestOneTaskBugFixIsOneWorkerPlusChecks`
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -count=1 &amp;&amp; test "$(md5 -q .claude/commands/ant/build.md)" = "$(md5 -q .opencode/commands/ant/build.md)" &amp;&amp; test "$(md5 -q .claude/commands/ant/build.md)" = "$(md5 -q .claude/commands/ant-build.md)"</automated>
  </verify>
  <done>No shipped instruction file teaches the retired rule; all three copies of each wrapper file are identical; the todo that asked for small jobs to get a small pipeline is closed with the test that proves it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> operator behaviour | Wrapper files are instructions an agent follows literally; a stale sentence causes wrong behaviour, not just a wrong belief. |
| WINDOWS ledger -> ship gate | Marking entry 1 fixed reopens `/gsd-ship`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-194-15 | Repudiation | closing broken-window #1 | high | mitigate | The entry is marked fixed only after `TestNoCasteIsDispatchedAtBothBoundaries` passes against real dispatch lists, in the same task, with the ordering stated explicitly in the action. |
| T-194-16 | Tampering | a partial wrapper-triplet edit | medium | mitigate | An existing parity test enforces byte-identity across the three copies; the acceptance criteria check the checksums directly as well, so a whitespace-level divergence is caught before commit. |
| T-194-17 | Information disclosure | new owner-facing wrapper text | low | accept | The added text is instructional prose about this project's own behaviour; it exposes no secret and no path outside the repository. |
| T-194-18 | Elevation of privilege | wrapper text describing the waive choice | high | mitigate | The wrapper relays the runtime-generated command and never constructs the question text, so a wrapper edit cannot widen a waiver's scope; scope lives in the question text the runtime builds (plan 194-04). |
| T-194-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan. Gate not triggered. |
</threat_model>

<verification>
- `go build ./cmd/aether` succeeds; `go vet ./...` clean; `go test ./cmd -count=1` passes.
- `.planning/WINDOWS.md` shows `open_count: 0`.
- All three copies of each wrapper command file have identical checksums.
- The SUMMARY records the measured worker total for the sample bug fix against the 2026-08-22 baseline of eight.
</verification>

<success_criteria>
- A one-task bug fix costs one worker plus the program's own checks, on both paths, counted across both boundaries.
- No worker is summoned at both the build and the checking step without an explicit ask.
- Every shipped document describing who gets sent describes what the runtime actually does, and a test fails if they drift apart.
- The broken window is closed and the folded todo is resolved.
</success_criteria>

<output>
Create `.planning/phases/194-the-queen-decides-the-team/194-05-SUMMARY.md` when done
</output>
