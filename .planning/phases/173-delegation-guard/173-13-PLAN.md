---
phase: 173-delegation-guard
plan: 13
type: execute
wave: 3
gap_closure: true
depends_on: ["173-11", "173-12"]
files_modified:
  - cmd/spawn_failclosed_test.go
  - cmd/internal_cmds.go
  - .planning/phases/173-delegation-guard/173-RESIDUE.md
autonomous: true
requirements: [SPAWN-03, SPAWN-04]

closes_gap:
  source: .planning/phases/173-delegation-guard/173-VERIFICATION.md
  truth: "Every delegation guard fails closed when its inputs are unreadable (ROADMAP Phase 173 success criterion 4)"
  missing_item: "spawnTreeBudgetReason (and, for defense in depth, spawnAncestorCycleReason) must turn that propagated error into a deny — proven across every guard, not only the one the exploit used."

must_haves:
  truths:
    - "The cross-guard fail-closed table covers a corrupted-content ledger, so a future regression to lenient parsing fails a test that names the guard it broke"
    - "The table also covers an obstructed run-state file, the input whose silent swallow was the second route to a free budget"
    - "Every delegation guard — the recorder, the advisory checker, and the swarm checker — is proven to deny against a corrupted-but-regular-file ledger, each through its own deny path"
    - "No comment anywhere in the repository still claims Parse() can never return an error or cannot tell an absent ledger from a corrupt one, and a single repo-wide command proves it for all five known statements at once"
    - "The phase's residue record states what verification actually found, what was closed, and what remains open — including the second reset route and the forgery bound nobody had written down before"
  artifacts:
    - path: "cmd/spawn_failclosed_test.go"
      provides: "The cross-guard fault table extended with the corrupted-ledger axis for all three file-reading guards, plus an obstructed run-state axis on the recorder"
      contains: "ledger-corrupt"
    - path: ".planning/phases/173-delegation-guard/173-RESIDUE.md"
      provides: "A corrected, dated residue record naming what the gap closure proved and what it did not"
      contains: "TestCorruptingTheLedgerDoesNotResetTheWholeRunBudget"
  key_links:
    - from: "cmd/spawn_failclosed_test.go delegationGuardFaultTable"
      to: "TestEveryDelegationGuardFailsClosedOnItsOwnUnreadableInputs"
      via: "a new exercisable axis per file-reading guard"
      pattern: "ledger-corrupt"
    - from: "pkg/agent/spawn_tree.go loadRunStateLocked"
      to: "cmd/spawn_failclosed_test.go spawn-log run-state-obstructed axis"
      via: "a propagated read error that reaches spawnTreeBudgetReason's deny branch"
      pattern: "run-state-obstructed"
    - from: ".planning/phases/173-delegation-guard/173-RESIDUE.md residue 7"
      to: "the guard/axis pairs the tests actually prove"
      via: "table rows added for the corrupted-content and obstructed-run-state axes"
      pattern: "ledger-corrupt"
---

<objective>
Make the phase's own cross-guard proof and its own written record tell the truth after the fix:
every guard that reads the ledger or the run record is proven to refuse an unreadable one, and no
comment or residue line still asserts the thing that made the exploit possible.

Purpose: this phase already caught itself once — 173-RESIDUE.md residue 7 named the mechanism
(`Parse()` cannot distinguish absent from corrupted) before any verifier tested it, but framed it
as a proof-scope limitation rather than a working exploit. Plans 11 and 12 close the exploit, and
close a second route to the same outcome that needed no tampering at all. What remains is the part
CLAUDE.md's Definition of Done is explicit about: a documentation claim about runtime behaviour
must be testable or removed, and five separate statements across two files currently assert, as
fact, that no byte content can make `Parse()` return an error. That is now false. Leaving them
would hand the next phase a false guarantee in the exact documents written to prevent false
guarantees.

Output: four new fault axes in the cross-guard table, five corrected statements, and a residue
record that matches reality — including the limits this gap closure discovered and did not close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-delegation-guard/173-CONTEXT.md
@.planning/phases/173-delegation-guard/173-VERIFICATION.md
@.planning/phases/173-delegation-guard/173-RESIDUE.md
@.planning/phases/173-delegation-guard/173-11-SUMMARY.md
@.planning/phases/173-delegation-guard/173-12-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
The cross-guard table's own types (`cmd/spawn_failclosed_test.go:219-246`):

- `type delegationGuardFaultAxis struct { Name string; NotExercisable string; Verify func(t *testing.T) }`
- `type delegationGuardTableEntry struct { Command string; Axes []delegationGuardFaultAxis }`
- `var delegationGuardFaultTable []delegationGuardTableEntry` — entries for `spawn-can-spawn`,
  `spawn-log`, `spawn-can-spawn-swarm`, `hook-pre-tool-use`.

`TestDelegationGuardTableCoversEveryGuardCommand` holds that table to the live cobra registry and
fails if a registered `spawn-can-spawn*` / `spawn-log` / `hook-pre-tool-use` command has no entry,
if a guard's axis list has zero exercisable entries, or if any guard starts reading
`COLONY_STATE.json` (an AST scan of `cmd/spawn.go`, `cmd/spawn_budget.go`, `cmd/spawn_ancestor.go`
for that string literal — nothing in this gap closure adds it).

FIVE statements in the codebase are true before this gap closure and false after it. All five must
be corrected, and the acceptance criteria gate all five with one repo-wide command. Each quoted
fragment below sits on a single source line, which is what makes it greppable:

1. `cmd/spawn_failclosed_test.go:27-39` — the file-top bounded-residue note:
   *"Parse() itself can therefore never return a non-nil error, and no byte content exists that
   makes it do so."* — greppable fragment: `never return a non-nil error`
2. `cmd/spawn_failclosed_test.go:~104-105` — the doc comment above
   `TestSpawnCanSpawnSwarmFailsClosedOnUnreadableSpawnTree`: *"since agent.SpawnTree.Parse() cannot
   itself be made to return an error."* — fragment: `cannot itself be made to return`
3. `cmd/spawn_failclosed_test.go:~222-224` — `delegationGuardFaultAxis.NotExercisable`'s own doc
   comment, which uses the claim as its worked example: *"e.g. agent.SpawnTree.Parse() can never
   itself return an error -- see the bounded-residue note atop this file"* — fragment:
   `can never itself return`
4. `cmd/spawn_failclosed_test.go:~486-492` — the `spawn-tree-unreadable` axis comment: *"Parse()
   itself cannot be made to error … so this axis is exercisable via the directory route, not via
   Parse() rejecting content."* — fragment: `cannot be made to error`
5. `cmd/internal_cmds.go:573-577` — *"agent.SpawnTree.Parse() swallows a missing file as 'empty'
   (nil error) exactly like an unreadable one -- it cannot tell the two apart."* — fragments:
   `swallows a missing file` and `cannot tell the two apart`

Statements 2 and 3 match none of the phrasings a narrower grep would catch, which is why the
criterion below uses all six fragments and runs repo-wide rather than over two named files.

A sixth instance was already corrected upstream: `cmd/spawn_ancestor_test.go`'s fixture comment
("a non-numeric depth field is skipped entirely by parseSpawnTreeBytes") is plan 11's task 2. The
repo-wide grep includes its fragment too, so if that correction regresses, this plan's gate catches
it rather than nobody catching it.

Already correct and needing no change: `cmd/internal_cmds.go:598-608` already treats a non-nil
`parseErr` from `Parse()` as a deny with reason `tree-unreadable`, so `spawn-can-spawn-swarm`
starts refusing a corrupted-content ledger the moment plan 11 lands — the code was ready for an
error the parser never produced. Only its comment is wrong.

New in this iteration: plan 11 also makes `loadRunStateLocked` propagate a read failure on
`spawn-runs.json`, and plan 12 makes the budget deny on it. That input has an existing axis for
unparseable CONTENT (`run-state-unreadable`, invalid JSON, on `spawn-can-spawn`) but none for an
unreadable FILE, and none on the recorder at all — which is the guard that actually consumes
budget. That is the fourth axis this plan adds.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Prove the unreadable inputs across every guard, and delete the claim that it was impossible</name>
  <files>cmd/spawn_failclosed_test.go, cmd/internal_cmds.go</files>
  <read_first>
    - cmd/spawn_failclosed_test.go (whole file — the file-top note, both type declarations, all four table entries, and both tests at the end)
    - cmd/internal_cmds.go lines 540-640 (`spawn-can-spawn-swarm`, the os.Stat guard, the parseErr deny branch)
    - pkg/agent/spawn_tree.go (as edited by plan 11 — `ErrSpawnTreeCorrupt`, `parseFile`, `Parse`, `loadRunStateLocked`)
    - cmd/spawn_budget.go (as edited by plan 12 — the integrity call ahead of `CurrentRun`, and the no-run counting branch)
    - cmd/spawn_budget_test.go (the three tests plan 12 added — do not duplicate their assertions here; this task proves breadth across guards, not depth on one)
    - .planning/phases/173-delegation-guard/173-VERIFICATION.md § "Gaps Summary"
  </read_first>
  <action>
Add one new fault axis named `ledger-corrupt` to three of the four entries in
`delegationGuardFaultTable`, each following the file's existing axis idiom: `saveGlobals`,
`resetRootCmd`, an isolated `newTestStore`, the fault written directly with `os.WriteFile` against
`filepath.Join(store.BasePath(), "spawn-tree.txt")`, execution through `rootCmd.SetArgs` plus
`rootCmd.Execute`, then assertions on `renderedCommandExitCode` and the parsed error envelope.

The fault injected by all three axes is the same one byte-for-byte, and it is the verifier's own:
a single line of non-pipe-format text written over `spawn-tree.txt` as a regular file. This is
deliberately NOT the directory-at-path fault the existing `spawn-tree-unreadable` axis uses — that
axis proves the `os.Stat` route, this one proves content rejection, and the whole point of Gap 1
is that the two were not the same thing.

- `spawn-can-spawn` gains `ledger-corrupt`: run `spawn-can-spawn --enforce` with no `--name` and
  assert a non-zero exit whose message names the budget as unverifiable. The deny arrives through
  `spawnTreeBudgetReason`, the second of `spawnCanSpawnDecision`'s three checks.
- `spawn-log` gains `ledger-corrupt`: run a Queen-parented `spawn-log` and assert a non-zero exit
  whose message names the budget as unverifiable, plus that `spawn-tree.txt`'s bytes are unchanged
  (D-09). Note in the axis comment why `Queen` specifically: a coordinator sentinel skips
  `deriveSpawnDepth`'s tree lookup entirely, so the budget check is the only guard left standing —
  which is exactly why the exploit worked.
- `spawn-can-spawn-swarm` gains `ledger-corrupt`: write a valid `COLONY_STATE.json` first (reuse
  `writeValidColonyState`), then corrupt the ledger, and assert a non-zero exit whose message
  contains `cannot verify spawn budget`. This guard needs no production change — its `parseErr`
  branch has been waiting for an error the parser never produced.

Then add a FOURTH axis, `run-state-obstructed`, to the `spawn-log` entry. This covers the second
route to a free budget, on the guard that actually consumes budget. Create a DIRECTORY at
`filepath.Join(store.BasePath(), "spawn-runs.json")` with `os.MkdirAll`, leave `spawn-tree.txt`
absent or valid — do not corrupt it, because the point of this axis is that the ledger is fine and
the run record is not — then run a Queen-parented `spawn-log` and assert a non-zero exit whose
message names the budget as unverifiable. Note in the axis comment that this is distinct from the
existing `run-state-unreadable` axis on `spawn-can-spawn`: that one injects unparseable CONTENT
(invalid JSON), this one makes the FILE unreadable, and before plan 11 the second was swallowed as
"no runs have ever been recorded" while the first was not.

Do NOT add an axis for the deleted-`spawn-runs.json` route. Deleting the file is indistinguishable
from a colony that has never run a lifecycle command, so plan 12 counts the whole ledger there
rather than refusing, and a deny assertion would be wrong. That route is proven instead by plan
12's `TestErasingTheRunRecordDoesNotResetTheWholeRunBudget`, which spawns a full ledger first;
reference that test by name in the axis comment so the coverage is traceable from here.

Leave `hook-pre-tool-use` untouched: it reads no file at all, and asserting a file fault against a
guard that does not read one is the exact mistake this table's own comment refuses to make.

Then correct the five false statements listed in this plan's `<interfaces>` section. Do not delete
the historical record — each was accurate when written, and this repo's own audits show what
happens when a correction erases the reasoning behind it. Rewrite each as a dated correction:
state what was true before 173-11, what changed, and what is true now. Concretely:

- The file-top note must stop asserting that no byte content can make `Parse()` return an error and
  must instead state that a present-but-unparseable ledger and a read failure other than not-exist
  both propagate as errors, while an absent, zero-byte, or whitespace-only ledger still parses to
  empty with a nil error. Add that the same distinction now applies to `spawn-runs.json`.
- The `TestSpawnCanSpawnSwarmFailsClosedOnUnreadableSpawnTree` doc comment must stop saying the
  directory route is used *because* `Parse()` cannot be made to error, and must instead say the
  directory route is one of two distinct faults, naming the axis that now covers the other.
- `delegationGuardFaultAxis.NotExercisable`'s doc comment must use a worked example that does not
  assert anything about current runtime behaviour — the old example is now false, and an example
  that can go stale is how this drift happened in the first place.
- The `spawn-tree-unreadable` axis comment must stop saying content rejection is impossible and
  must instead say the directory route and the content route are two different faults, both now
  exercised, listing the axis that covers each.
- `cmd/internal_cmds.go`'s comment must stop saying `Parse()` cannot tell absent from unreadable;
  the `os.Stat` guard stays because it is a cheap, explicit check that names its own reason string,
  not because `Parse()` is untrustworthy.
  </action>
  <verify>
    <automated>go build ./cmd/aether && go vet ./cmd && go test ./cmd -run 'TestEveryDelegationGuardFailsClosedOnItsOwnUnreadableInputs|TestDelegationGuardTableCoversEveryGuardCommand|TestSpawnCanSpawnSwarmFailsClosedOnUnreadableSpawnTree' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - The verbose output of the verify command shows three subtests whose names contain `ledger-corrupt`, one each under `spawn-can-spawn`, `spawn-log` and `spawn-can-spawn-swarm`, plus one subtest named `run-state-obstructed` under `spawn-log`.
    - `grep -v '^\s*//' cmd/spawn_failclosed_test.go | grep -c 'ledger-corrupt'` returns at least 3, and the same filtered grep for `run-state-obstructed` returns at least 1.
    - All five stale statements are gone, proven repo-wide by one command returning nothing: `grep -rn --include="*.go" 'never return a non-nil error\|cannot itself be made to return\|can never itself return\|cannot be made to error\|cannot tell the two apart\|swallows a missing file\|skipped entirely by parseSpawnTreeBytes' .`
    - Each of the five corrected comments carries a date and names plan 173-11 (or 173-12, where the budget behaviour is what changed) as the change that invalidated the earlier statement.
    - `delegationGuardFaultAxis.NotExercisable`'s doc comment contains no claim about what any function can or cannot currently do.
    - The `run-state-obstructed` axis does not corrupt `spawn-tree.txt`, and its comment names `TestErasingTheRunRecordDoesNotResetTheWholeRunBudget` as the proof for the deleted-file route.
    - `hook-pre-tool-use`'s axis list is unchanged — `git diff cmd/spawn_failclosed_test.go` shows no edit inside that table entry.
    - No production file other than `cmd/internal_cmds.go` is touched, and that file's diff is comment-only: `git diff cmd/internal_cmds.go` shows changed lines all beginning with `//` after their indentation.
    - Recorded in the summary: reverting plan 11's corrupt branch to a nil error makes at least one `ledger-corrupt` subtest fail by name, and reverting plan 11's `loadRunStateLocked` change makes `run-state-obstructed` fail by name; restore afterwards.
  </acceptance_criteria>
  <done>The table proves what the exploit disproved, on both routes, and no comment in the tree still says it could not happen.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the residue record to match what is now true</name>
  <files>.planning/phases/173-delegation-guard/173-RESIDUE.md</files>
  <read_first>
    - .planning/phases/173-delegation-guard/173-RESIDUE.md (whole file — especially residue 7's guard/axis table and its closing paragraph about criterion 4)
    - .planning/phases/173-delegation-guard/173-VERIFICATION.md (whole file — the reproduction, the requirements-coverage table, and the human-verification item)
    - .planning/phases/173-delegation-guard/173-11-SUMMARY.md and 173-12-SUMMARY.md (what was actually built and what each summary names as not closed)
    - .planning/phases/172-wiring-proof/172-STOP-RULE.md (the standard for narrowing a claim after evidence, not before)
    - cmd/codex_build.go lines 2699-2708 (`recordCodexBuildDispatches`) and cmd/spawn.go lines 100-120 (the two call sites of `spawnCanSpawnDecision`)
  </read_first>
  <action>
Update `.planning/phases/173-delegation-guard/173-RESIDUE.md` so it records the outcome rather
than the prediction. Four edits, all additive or corrective — do not delete the existing residues,
which are the record of what the phase honestly disclosed before it was tested.

First, add four rows to residue 7's `(guard, axis)` table for the new axes, naming the deny path
each reaches: `ledger-corrupt` via `spawnTreeBudgetReason` for `spawn-can-spawn` and `spawn-log`,
`ledger-corrupt` via the `parseErr` branch for `spawn-can-spawn-swarm`, and `run-state-obstructed`
via `spawnTreeBudgetReason` for `spawn-log`. Also correct residue 7's EXISTING
`spawn-can-spawn` / `requester-not-recorded` row: its "Fault injected" cell currently reads
"corrupted tree line", but plan 173-12 re-points that axis to a well-formed ledger that simply
lacks the agent — rewrite the cell to "name absent from a readable tree", matching plan 12's
code-comment correction, so the table describes the fault the test actually injects.

Second, rewrite residue 7's closing paragraph. It currently says criterion 4 is "deliberately left
at its original absolute wording … to be judged against evidence at verification time." That
judgement has now happened. Replace it with a dated record of the actual sequence, and record BOTH
routes, not just the one the verifier used:

- verification on 2026-08-13 reproduced a working budget-reset by overwriting the spawn ledger with
  one line of garbage;
- planning the fix found a second, easier route that needed no tampering with the ledger at all —
  deleting the file that records which run is current made a valid ledger's helpers uncountable,
  and the budget's "this colony has never begun a run" shortcut then reported zero used;
- plans 173-11, 173-12 and 173-13 closed both at the parse boundary, at the budget check, and in
  the cross-guard proof;
- the named commands that now fail if either regresses are
  `go test ./cmd -run 'TestCorruptingTheLedgerDoesNotResetTheWholeRunBudget|TestErasingTheRunRecordDoesNotResetTheWholeRunBudget|TestAFreshColonyWithNoLedgerIsStillAllowedToSpawn'`
  together with
  `go test ./pkg/agent -run 'TestSpawnTreeParseTreatsAnAbsentLedgerAsEmptyButACorruptOneAsAnError|TestSpawnTreeRefusesToRewriteACorruptLedger|TestSpawnRunStateTellsAnAbsentRunFileApartFromAnUnreadableOne'`.

State plainly that the criterion was closed by building, not by narrowing — this is the direction
`172-STOP-RULE.md` requires and the opposite of softening a criterion to make it passable.

Third, add four new numbered residues, in the same bounded-claim style as the existing nine. Each
must state the bounded claim, the wrong inference to head off, and what would close it:

- **A ledger that is correctly shaped but false is still believed.** The parser checks the shape of
  every line, not its truth, so anyone who copies the format can still write a ledger that omits
  helpers. Counting rules cannot see that, and neither can the corruption check. What would close
  it: integrity data the file does not currently carry — an append-only log, or a signature —
  which is a storage-format change, not a guard change.
- **The whole-run budget gates the two CLI commands, not the coordinator's own dispatch.**
  `spawnCanSpawnDecision` has exactly two call sites, both in `cmd/spawn.go` (`spawn-log` and
  `spawn-can-spawn`). `recordCodexBuildDispatches` in `cmd/codex_build.go`, and the equivalent
  recorders in the build-finalize, plan, plan-finalize, colonize, colonize-finalize and continue
  paths, call `agent.SpawnTree.RecordSpawn` directly and consult no budget at all. Those spawns
  are COUNTED once recorded, so they consume the budget for anything that checks later — but the
  ceiling itself is not enforced against the Queen's own wave dispatch. What would close it:
  routing every recorder through the decision chokepoint, which is a behaviour change to the
  build path and deliberately not attempted in a gap-closure plan.
- **A corrupted ledger makes the operator's live view quieter, not louder.**
  `spawn-tree-active` renders its budget header only when `spawnTreeBudgetState` succeeds
  (`cmd/spawn.go` `renderSpawnTreeActiveVisual`), and `Active()` swallows the parse error by
  design, so a tampered ledger now shows no header and no helpers rather than saying the ledger is
  unreadable. The enforcement path fails closed; the *display* path fails silent. What would close
  it: a plain-English line in that view naming the unreadable ledger — wrapper-and-view work, not
  guard work.
- **Nothing here addresses concurrent processes near the ceiling.** WR-05's check-then-act race in
  `cmd/spawn.go` is untouched and still open; two processes can still pass the budget check
  simultaneously and both record. What would close it: holding a write lock across the check and
  the record, which is a different change to a different seam.

Fourth, update the "What this phase proves" list so criterion 3's and criterion 4's entries name
the new commands alongside the existing ones, and so criterion 4 is no longer marked
"**Partially, with a named bound**" in a way that predates the evidence — state its status as of
the gap closure, with the commands that prove it, and with the forgery bound named as the reason it
is not an unqualified yes.

Write every line for a reader who has never opened a file in this repository. The owner is
non-technical: "spawn ledger" is the file that records which helpers were started, "the run record"
is the file that says which batch of work is currently running, "the whole-run budget" is the limit
of 20 helpers per run, and a "guard" is a check that refuses. Use those words before any
identifier, and keep file paths and test names at the end of sentences where they can be skipped.
  </action>
  <verify>
    <automated>grep -q 'ledger-corrupt' .planning/phases/173-delegation-guard/173-RESIDUE.md && grep -q 'run-state-obstructed' .planning/phases/173-delegation-guard/173-RESIDUE.md && grep -q 'TestCorruptingTheLedgerDoesNotResetTheWholeRunBudget' .planning/phases/173-delegation-guard/173-RESIDUE.md && grep -q 'TestErasingTheRunRecordDoesNotResetTheWholeRunBudget' .planning/phases/173-delegation-guard/173-RESIDUE.md && grep -q 'TestSpawnRunStateTellsAnAbsentRunFileApartFromAnUnreadableOne' .planning/phases/173-delegation-guard/173-RESIDUE.md && grep -q 'recordCodexBuildDispatches' .planning/phases/173-delegation-guard/173-RESIDUE.md && ! grep -q 'to be judged against evidence at verification time' .planning/phases/173-delegation-guard/173-RESIDUE.md && go test ./... -count=1 -timeout 900s</automated>
  </verify>
  <acceptance_criteria>
    - Residue 7's guard/axis table contains four new rows — three reading `ledger-corrupt` and one reading `run-state-obstructed` — each naming its deny path.
    - The closing paragraph of residue 7 is dated, names 173-VERIFICATION.md's reproduction AND the second route found during planning, and names the three plans that closed them.
    - The phrase `to be judged against evidence at verification time` no longer appears — that judgement has been made.
    - Four new numbered residues exist, each with a bounded claim, a named wrong inference, and a "what would close it" line.
    - The residue naming the forgery bound states that shape checking cannot detect a correctly shaped but false ledger.
    - The residue naming the un-gated dispatch path names `recordCodexBuildDispatches` and states that those spawns are counted but not gated.
    - Every new paragraph states the plain-English meaning before any identifier, file path, or test name.
    - `go test ./... -count=1 -timeout 900s` exits 0.
  </acceptance_criteria>
  <done>The phase's written record says what is true, including the second reset route and three limits nobody had written down before this gap closure found them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the phase's written record → the next phase's planner | A false claim here is inherited as a guarantee. This is the boundary CLAUDE.md's Definition of Done exists to police: 18 of 25 milestones were framed around restoring something a previous record said was already done |
| the cross-guard fault table → future guard changes | A guard added or loosened later is caught only if the table still enumerates the faults that matter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-61 | Repudiation | five statements across `cmd/spawn_failclosed_test.go` and `cmd/internal_cmds.go` | mitigate | Rewrite all five as dated corrections stating what changed and when, gated by one repo-wide grep so a narrower check cannot leave two of them standing as it previously would have |
| T-173-62 | Tampering | a future regression to lenient parsing | mitigate | Three new `ledger-corrupt` axes fail by guard name if any guard stops refusing a tampered ledger |
| T-173-63 | Repudiation | `173-RESIDUE.md` | mitigate | Record the exploit, the second route found during planning, the closure of both, and the limits that were NOT closed, rather than implying full coverage |
| T-173-64 | Elevation of privilege | `recordCodexBuildDispatches` and the six sibling direct recorders | accept | Named as residue, not fixed here: routing the Queen's own wave dispatch through the decision chokepoint is a behaviour change to the build path, outside a gap-closure plan's remit. The spawns are still counted once recorded, so the ceiling still applies to everything that checks afterwards |
| T-173-65 | Denial of service | `spawn-tree-active` on a tampered ledger | accept | Named as residue: enforcement fails closed, the operator's view fails silent. Closing it is view work, and the guard is unaffected |
| T-173-72 | Tampering | `.aether/data/spawn-runs.json` obstructed on the recorder | mitigate | The `run-state-obstructed` axis proves `spawn-log` itself refuses when the run record exists and cannot be read, so the second route to a free budget is enumerated in the table rather than living only in one plan's regression test |
</threat_model>

<verification>
- `go build ./cmd/aether && go vet ./cmd` exits 0.
- `go test ./cmd -run 'TestEveryDelegationGuardFailsClosedOnItsOwnUnreadableInputs|TestDelegationGuardTableCoversEveryGuardCommand' -count=1 -v` passes and shows three `ledger-corrupt` subtests plus one `run-state-obstructed` subtest.
- `go test ./... -count=1 -timeout 900s` exits 0.
- One repo-wide grep proves no file still states that `Parse()` cannot return an error or cannot
  tell an absent ledger from a corrupt one — all five known statements at once.
- 173-RESIDUE.md names four new bounded residues, both reset routes, and the commands that prove
  each one closed.
</verification>

<success_criteria>
- Three guards are each proven, by a named subtest, to refuse a corrupted-but-regular-file ledger,
  and the recorder is additionally proven to refuse an obstructed run record.
- Every comment that asserted this was impossible is corrected in place, with a date and a reason,
  and a single command proves none survives anywhere in the repository.
- The residue record states what verification found, what planning additionally found, what was
  closed and how it is proven, and names the limits — forgery, the un-gated dispatch path, the
  silent operator view, and the concurrency race — that this gap closure deliberately did not close.
</success_criteria>

<output>
After completion, create `.planning/phases/173-delegation-guard/173-13-SUMMARY.md`, including:
- all five corrected statements quoted before and after;
- the output of the repo-wide stale-claim grep, showing it returns nothing;
- the verbose test output showing the three `ledger-corrupt` subtests and the `run-state-obstructed`
  subtest running;
- the red-proof transcripts for at least one `ledger-corrupt` subtest and for `run-state-obstructed`;
- a short plain-English paragraph the owner can read that says what changed, what it means for
  him, and what is still not guaranteed.
</output>
