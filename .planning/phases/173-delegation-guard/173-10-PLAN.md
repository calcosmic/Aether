---
phase: 173-delegation-guard
plan: 10
type: execute
wave: 5
depends_on: ["173-03", "173-05", "173-06", "173-07", "173-08", "173-09"]
files_modified:
  - cmd/ci_wiring_gate_test.go
  - .github/workflows/ci.yml
  - cmd/spawn_failclosed_test.go
  - cmd/testdata/orphan_allowlist.json
  - .planning/phases/173-delegation-guard/173-RESIDUE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SPAWN-04, SPAWN-06]

must_haves:
  truths:
    - "Every guard test file this phase created is tracked by the CI wiring ratchet, and every test function in those files is named in the CI run filter — so a future edit that deletes one fails CI by name"
    - "With colony state and the spawn tree made unreadable, every delegation guard in this phase denies — proven in one test that names them all, not guard by guard"
    - "No contract stub from plan 04 survives; the budget and ancestor checks have real bodies"
    - "The subcommands this phase wired to real callers are removed from the tolerated-orphan list, so the wiring proof stays honest"
    - "Every residue this phase knows about is written down in one place, in bounded language, and the phase claims nothing about them"
  artifacts:
    - path: "cmd/ci_wiring_gate_test.go"
      provides: "wiringGateGuardFiles extended with this phase's five new guard test files"
      contains: "spawn_budget_test.go"
    - path: ".planning/phases/173-delegation-guard/173-RESIDUE.md"
      provides: "The phase's named, bounded residue — what is proven and what is not"
      contains: "## What this phase does NOT prove"
    - path: "cmd/spawn_failclosed_test.go"
      provides: "The cross-guard fail-closed proof covering every delegation guard at once"
      contains: "TestAllDelegationGuardsFailClosedOnUnreadableState"
  key_links:
    - from: "cmd/ci_wiring_gate_test.go wiringGateGuardFiles"
      to: ".github/workflows/ci.yml -run filter"
      via: "TestWiringGateStepRunsEveryWiringTest, which fails in both directions"
      pattern: "spawn_budget_test\\.go|spawn_ancestor_test\\.go|spawn_reap_test\\.go"
    - from: "cmd/testdata/orphan_allowlist.json"
      to: "the newly wired spawn subcommands"
      via: "shrink-only removal of names that now have real callers"
      pattern: "spawn-"
---

<objective>
Close the phase honestly: put every new guard under the ratchet that makes deleting it fail by
name, prove the fail-closed property across all guards at once rather than one at a time, remove
the stubs and the stale orphan entries, and write down exactly what this phase does not prove.

Purpose: Phase 172 ran four build-verify rounds because two of its criteria were absolutes over an
adversarial surface with no defined edge. `172-STOP-RULE.md` is explicit — *a narrower true claim
beats a broader unproven one.* This plan is where that discipline is applied: every residue this
phase already knows about gets named, so no later phase inherits a false guarantee. And this repo
has 18 of 25 milestones framed around restoring something previously marked done, which is what
happens when a guard ships without a caller and a test that fails when it is removed.

Output: five new guard files under the ratchet, one cross-guard fault-injection test, a shrunk
orphan allowlist, no surviving contract stubs, a residue document, and one ROADMAP correction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-delegation-guard/173-CONTEXT.md
@.planning/phases/173-delegation-guard/173-RESEARCH.md
@.planning/phases/173-delegation-guard/173-PATTERNS.md
@.planning/phases/173-delegation-guard/173-VALIDATION.md
@.planning/phases/173-delegation-guard/173-HOOK-FINDINGS.md
@.planning/phases/172-wiring-proof/172-STOP-RULE.md
@CLAUDE.md
</context>

<interfaces>
`cmd/ci_wiring_gate_test.go:87-93`:
`var wiringGateGuardFiles = []string{"subcommand_reachability_ratchet_test.go", "command_call_audit_test.go", "cli_flag_audit_test.go", "spawn_enforce_test.go", "ci_wiring_gate_test.go"}`
Every top-level `func Test…` in a file in this list must appear as an alternative in the
single-quoted `-run` argument of the CI step named `Verify subcommand wiring and CLI flag contracts`
(`.github/workflows/ci.yml`, around line 100). `TestWiringGateStepRunsEveryWiringTest` fails in
BOTH directions — a guard test missing from the filter, and a filter alternative matching no guard
test. `runFlagArgRe` extracts the filter with the pattern `-run\s+'([^']*)'`.

Files created by this phase that are NOT yet in that list:
`spawn_failclosed_test.go` (plan 03), `spawn_budget_test.go` (plan 05),
`spawn_ancestor_test.go` (plan 06), `spawn_tree_view_test.go` (plan 08),
`spawn_reap_test.go` (plan 09). `hook_cmds_test.go` (plans 01, 07) is deliberately NOT added —
it predates this phase, it is not a wiring guard, and adding it would drag every pre-existing hook
test into the filter for no gain.

`cmd/testdata/orphan_allowlist.json` currently tolerates, among others:
`aether spawn-can-spawn`, `aether spawn-can-spawn-swarm`, `aether spawn-efficiency`,
`aether spawn-get-depth`, `aether spawn-tree-active`, `aether spawn-tree-depth`,
`aether spawn-tree-load`. `TestOrphanAllowlistOnlyShrinks` fails on any ADDITION; removals are the
point of the ratchet.

Contract stub markers plan 04 planted: `CONTRACT-STUB-PLAN-05` in `cmd/spawn_budget.go`,
`CONTRACT-STUB-PLAN-06` in `cmd/spawn_ancestor.go`. Plans 05 and 06 were each required to delete
their own; this plan proves neither survived.

ROADMAP correction needed: Phase 173 success criterion 2 currently reads "`aether spawn-tree-depth`
reports the true depth (3 for a 3-deep tree)". D-05 and D-07, locked in `173-CONTEXT.md`, fix the
number at 2 (coordinator 0, workers 1, helpers 2). The parenthetical predates the ruling.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Put this phase's guard files under the ratchet and prove no stub survived</name>
  <files>cmd/ci_wiring_gate_test.go, .github/workflows/ci.yml</files>
  <read_first>
    - cmd/ci_wiring_gate_test.go (whole file — `wiringGateGuardFiles`, `runFlagArgRe`, `TestWiringGateStepRunsEveryWiringTest`, `blanketReleaseGateProblem`, and every other test in the file)
    - .github/workflows/ci.yml lines 88-110 (the named wiring step and the blanket release-gate step beside it)
    - cmd/spawn_failclosed_test.go, cmd/spawn_budget_test.go, cmd/spawn_ancestor_test.go, cmd/spawn_tree_view_test.go, cmd/spawn_reap_test.go (all five — to enumerate every top-level test function name exactly)
    - cmd/spawn_budget.go and cmd/spawn_ancestor.go (to confirm both stub markers are gone)
    - .planning/phases/172-wiring-proof/172-STOP-RULE.md § "What is already proven and must not regress"
  </read_first>
  <action>
Add these five filenames to `wiringGateGuardFiles` in `cmd/ci_wiring_gate_test.go`, keeping the
slice's existing entries and its existing comment intact: `spawn_failclosed_test.go`,
`spawn_budget_test.go`, `spawn_ancestor_test.go`, `spawn_tree_view_test.go`,
`spawn_reap_test.go`. Do NOT add `hook_cmds_test.go`.

Then enumerate every top-level `func Test…` in those five files and append each name as an
alternative to the single-quoted `-run` argument of the CI step named
`Verify subcommand wiring and CLI flag contracts` in `.github/workflows/ci.yml`, using `|`
separators. Change nothing else on that line: `TestWiringGateStepRunsEveryWiringTest` also fails
on a stale alternative matching no guard test, and `blanketReleaseGateProblem` fails if the
blanket `go test ./...` step beside it is narrowed, given an `if:`, given a `continue-on-error`,
or given an exit-status-swallowing fallback. Do not touch that step at all.

Add one new test to `cmd/ci_wiring_gate_test.go` named
`TestNoDelegationGuardContractStubSurvives`: read `cmd/spawn_budget.go` and
`cmd/spawn_ancestor.go` from the repo root and fail, naming the file, if either contains
`CONTRACT-STUB-PLAN-05` or `CONTRACT-STUB-PLAN-06`. This is the invariant that stops a
half-finished phase shipping an always-allow guard behind a real-looking call site — the exact
shape of defect this repo's own audits keep finding. Because this test lives in
`ci_wiring_gate_test.go`, which is itself in `wiringGateGuardFiles`, its own name must also go
into the `-run` filter.

Run `go test ./cmd -run 'TestWiringGateStepRunsEveryWiringTest|TestWiringGuardsHaveNoRuntimeEscapeHatch|TestAllowlistPolicyNamesEveryGuardedFile|TestBlanketGateCheckRejectsADecoyStep' -count=1 -v`
before committing. `TestAllowlistPolicyNamesEveryGuardedFile` may require the new guard files to be
enumerated in a policy list as well; if it fails, satisfy it where it points rather than exempting
the new files.

Red-proof required by D-22: delete one test function from `cmd/spawn_budget_test.go`, confirm
`TestWiringGateStepRunsEveryWiringTest` goes red naming the stale filter alternative, restore.
Then re-insert `CONTRACT-STUB-PLAN-05` as a comment in `cmd/spawn_budget.go`, confirm
`TestNoDelegationGuardContractStubSurvives` goes red, restore. Quote both failure outputs in the
plan summary.
  </action>
  <verify>
    <automated>go test ./cmd -run 'TestWiringGateStepRunsEveryWiringTest|TestWiringGuardsHaveNoRuntimeEscapeHatch|TestAllowlistPolicyNamesEveryGuardedFile|TestBlanketGateCheckRejectsADecoyStep|TestNoDelegationGuardContractStubSurvives' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `wiringGateGuardFiles` contains all five new filenames and still contains its original five entries.
    - `hook_cmds_test.go` is NOT in `wiringGateGuardFiles`.
    - Every top-level `func Test…` in the five new files appears exactly once as an alternative in the CI `-run` filter, and `TestNoDelegationGuardContractStubSurvives` appears there too.
    - `grep -c 'CONTRACT-STUB-PLAN-05' cmd/spawn_budget.go` and `grep -c 'CONTRACT-STUB-PLAN-06' cmd/spawn_ancestor.go` both return 0.
    - The blanket `go test ./...` release-gate step in `.github/workflows/ci.yml` is byte-identical to its state before this task.
    - Both red-proofs recorded with quoted failure output in the summary.
  </acceptance_criteria>
  <done>Deleting any guard this phase built now fails CI by name, and an unfinished guard cannot ship behind a real-looking call site.</done>
</task>

<task type="auto">
  <name>Task 2: Prove every delegation guard fails closed, in one test</name>
  <files>cmd/spawn_failclosed_test.go</files>
  <read_first>
    - cmd/spawn_failclosed_test.go (plan 03's three tests — extend this file, do not create another)
    - cmd/spawn.go (the widened decision, `spawnCanSpawnCmd`, `spawnLogCmd`)
    - cmd/spawn_budget.go, cmd/spawn_ancestor.go (their error branches)
    - cmd/internal_cmds.go (`spawnCanSpawnSwarmCmd` as fixed by plan 03)
    - cmd/hook_cmds.go (`hookSpawnDenyReason` as written by plan 07)
    - .planning/ROADMAP.md § "Phase 173" success criterion 4
    - .planning/phases/173-delegation-guard/173-VALIDATION.md § "Per-Task Verification Map" row SPAWN-04
  </read_first>
  <action>
Add one test to `cmd/spawn_failclosed_test.go` named
`TestAllDelegationGuardsFailClosedOnUnreadableState`. ROADMAP criterion 4 says *every* delegation
guard denies on unreadable state; a set of per-guard tests proves each one in isolation but cannot
catch a guard added later that nobody wrote a test for. This test asserts the property across an
enumerated set, so extending the set is a deliberate act.

Structure it as a table over the guards this phase built, each entry naming the guard and carrying
a closure that invokes it:
- `spawn-can-spawn --enforce`
- `spawn-log`
- `spawn-can-spawn-swarm`
- `hook-pre-tool-use` with a subagent-dispatch payload

For each guard, run it twice in a fresh store: once with `COLONY_STATE.json` corrupted, once with
`spawn-tree.txt` corrupted. Assert in every case that the guard denies — for the CLI guards, a
non-zero exit and a payload whose `can_spawn` is false or whose envelope is an error; for the hook,
a `decision` of `block`. Assert the deny reason is non-empty in every case: a silent denial is
almost as bad as a silent allow, because the operator cannot tell a guard from a crash.

Add a paired control in the same function: with both files valid and consumption below every
limit, every guard in the table allows. Without it, the whole table passes against four guards
that deny unconditionally.

Add a source-level assertion to the same test, or as a sibling named
`TestDelegationGuardTableCoversEveryGuardCommand`: enumerate the registered cobra commands whose
names begin with `spawn-can-spawn`, plus `spawn-log` and `hook-pre-tool-use`, and fail if any of
them is absent from the table above. This is what turns "we tested four guards" into "we tested
every guard that exists" — a future fifth guard command joins the table or fails the build.

Red-proof required by D-22: make `spawnTreeBudgetReason` return the empty string on error, confirm
the table test goes red naming `spawn-log`; restore. Then make `hookSpawnDenyReason` return empty
on its unresolved branch, confirm it goes red naming the hook; restore. Quote both failure outputs
in the plan summary.

Add both new test names to the CI `-run` filter — `spawn_failclosed_test.go` became a tracked
guard file in Task 1, so `TestWiringGateStepRunsEveryWiringTest` now requires it.
  </action>
  <verify>
    <automated>go test ./cmd -run 'TestAllDelegationGuardsFailClosedOnUnreadableState|TestDelegationGuardTableCoversEveryGuardCommand|TestWiringGateStepRunsEveryWiringTest' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - Both new tests pass, and `TestWiringGateStepRunsEveryWiringTest` still passes with their names in the filter.
    - The table covers `spawn-can-spawn`, `spawn-log`, `spawn-can-spawn-swarm` and `hook-pre-tool-use`, and each is exercised against both a corrupt colony state and a corrupt spawn tree.
    - Every deny assertion also asserts a non-empty reason.
    - The all-valid control exists in the same function and asserts every guard allows.
    - The coverage assertion enumerates registered commands rather than a hardcoded list, so a future guard command cannot silently escape the table.
    - Both red-proofs recorded with quoted failure output in the summary.
  </acceptance_criteria>
  <done>One command proves the fail-closed property across every delegation guard at once, and refuses to let a new guard skip it.</done>
</task>

<task type="auto">
  <name>Task 3: Shrink the tolerated-orphan list to match what is now genuinely called</name>
  <files>cmd/testdata/orphan_allowlist.json</files>
  <read_first>
    - cmd/testdata/orphan_allowlist.json (the `spawn-` entries around lines 1150-1190)
    - cmd/subcommand_reachability_ratchet_test.go (`TestNoRegisteredSubcommandIsUnreferenced`, `TestOrphanAllowlistOnlyShrinks`, `TestOrphanAllowlistIsPathKeyed`, `TestPathMigrationDidNotWidenTolerance`, and the allowlist regeneration mechanism)
    - cmd/spawn.go, cmd/spawn_reap.go, cmd/hook_cmds.go, cmd/internal_cmds.go (to establish which spawn subcommands now have real callers)
    - .planning/phases/172-wiring-proof/172-STOP-RULE.md § "What is already proven and must not regress" (the allowlist shrink evidence)
  </read_first>
  <action>
Determine, by running the ratchet rather than by reading, which of the tolerated spawn orphans now
have real callers as a result of this phase. Run
`go test ./cmd -run 'TestNoRegisteredSubcommandIsUnreferenced' -count=1 -v` with each candidate
entry removed from `cmd/testdata/orphan_allowlist.json`, one at a time, and keep the removals that
leave the ratchet green.

Candidates, with the reason each might now be reachable:
- `aether spawn-can-spawn` — `.aether/workers.md`'s spawn protocol invokes it, and plan 04 gave it
  a real decision.
- `aether spawn-tree-depth` — plan 02's tests and the corrected depth convention.
- `aether spawn-tree-active` — plan 08's rendering and the operator flow.
- `aether spawn-get-depth` — the orphaned hand-rolled lookup. If no plan gave it a caller, LEAVE
  it in the allowlist and say so; do not manufacture a caller to make a number look better.
- `aether spawn-can-spawn-swarm` — plan 03 fixed it but did not give it a caller. Expect this one
  to stay.

Remove only entries the ratchet confirms are now referenced. Do not remove an entry the ratchet
still needs — that turns a green build red for no gain. Do not ADD any entry:
`TestOrphanAllowlistOnlyShrinks` fails on an addition, and an addition would be exactly the
"built but nothing calls it" pattern this milestone exists to end.

Record in the plan summary: which entries were removed, which were kept, and for each kept one the
reason it is still an orphan. A kept orphan with a stated reason is honest; a kept orphan with no
reason is the thing this repo keeps rediscovering.
  </action>
  <verify>
    <automated>go test ./cmd -run 'TestNoRegisteredSubcommandIsUnreferenced|TestOrphanAllowlistOnlyShrinks|TestOrphanAllowlistIsPathKeyed|TestPathMigrationDidNotWidenTolerance|TestPreMigrationSnapshotIsFrozen' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `git diff cmd/testdata/orphan_allowlist.json` shows removals only — zero added entries.
    - All five named ratchet tests pass.
    - The plan summary lists every entry removed and every spawn-related entry kept, with a one-line reason for each kept one.
    - No caller was added anywhere solely to permit an allowlist removal.
  </acceptance_criteria>
  <done>The tolerated-orphan list shrank by exactly as much as this phase genuinely earned, and the remainder is explained.</done>
</task>

<task type="auto">
  <name>Task 4: Write down what this phase does not prove, and correct the one ROADMAP number the ruling changed</name>
  <files>.planning/phases/173-delegation-guard/173-RESIDUE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/172-wiring-proof/172-STOP-RULE.md (the whole file — the reasoning this task applies)
    - .planning/phases/173-delegation-guard/173-VALIDATION.md § "Known Residue"
    - .planning/phases/173-delegation-guard/173-HOOK-FINDINGS.md (the verdict and its `## Named residue` section)
    - .planning/phases/173-delegation-guard/173-02-SUMMARY.md through 173-09-SUMMARY.md (every `must_haves` RESIDUE line and every summary's recorded decisions)
    - .planning/ROADMAP.md § "Phase 173: Delegation Guard" (goal, six success criteria, the SPAWN-06 decision note)
    - .planning/phases/173-delegation-guard/173-CONTEXT.md § D-05, D-07, D-21
    - CLAUDE.md § "Definition of Done"
  </read_first>
  <action>
Write `.planning/phases/173-delegation-guard/173-RESIDUE.md` with two clearly separated halves.

`## What this phase proves` — one line per success criterion and per requirement, each naming the
command that proves it. Every line must be a bounded claim with a runnable proof beside it. If a
criterion cannot be stated as a bounded claim with a named command, it goes in the other half
instead, not here.

`## What this phase does NOT prove` — every residue, each in the same shape: what the bounded claim
actually is, what a reader might wrongly infer from it, and what would be needed to close it.
Collect at minimum these six, gathered from the plan summaries:

1. `spawn-can-spawn` without `--name` is advisory. It reports on a depth the caller states about
   itself. Only `spawn-log` is authoritative, because a caller can lie to the checker but cannot
   avoid the recorder.
2. `--parent` is caller-supplied. Deriving depth from the parent closes "lie about your own
   depth"; it does not close "name a real but shallower ancestor". The bounded claim is that a
   caller must name a recorded ancestor and cannot invent one.
3. There is no bridge from the platform's own subagent identifier to Aether's spawn-tree agent
   names. The hook resolves requester depth by a heuristic over supplied fields. Quote the dated
   verdict from `173-HOOK-FINDINGS.md` and state exactly which dispatch levels the hook was
   observed to cover.
4. Run membership is a wall-clock activity window, not a transactional link to a run identifier.
   Two runs starting within the same second, or clock skew between processes, can misattribute a
   spawn. The claim is "spawns whose activity timestamp falls in the current run's window".
5. Ancestor-cycle matching is textual after normalisation, not semantic. Reworded but identical
   work is not detected. The claim is "the same caste on a task that normalises to the same
   string".
6. There is no liveness signal. The reaper does not detect abandonment; it detects that a
   configured elapsed time passed with no completion, and the threshold is generous because no
   better signal exists.

Add a closing `## Stop rule` section applying `172-STOP-RULE.md`'s reasoning to this phase in
advance: if verification finds a NEW class of bypass not listed above, the correct response is to
narrow the criterion and record the residue here, not to open another build round. A narrower true
claim beats a broader unproven one.

Then make one correction in `.planning/ROADMAP.md`. Phase 173's success criterion 2 currently
reads that `spawn-tree-depth` reports "the true depth (3 for a 3-deep tree)". D-05 and D-07 fix the
number at 2 — coordinator 0, its workers 1, their helpers 2 — and the parenthetical predates the
ruling. Change the parenthetical to state 2 for a three-level tree and add a short clause naming
D-05 as the convention that fixes it. Change nothing else in the ROADMAP: the goal, the other five
criteria and the SPAWN-06 decision note stay as written.

Do not mark any requirement satisfied in `.planning/REQUIREMENTS.md` from this plan. Verification
owns that transition, and this repo's own audits record milestones shipping 19 of 19 with wiring
that was never added.
  </action>
  <verify>
    <automated>grep -q '## What this phase does NOT prove' .planning/phases/173-delegation-guard/173-RESIDUE.md && grep -q '## Stop rule' .planning/phases/173-delegation-guard/173-RESIDUE.md && grep -c 'spawn-tree-depth' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `173-RESIDUE.md` exists with both halves and a `## Stop rule` section.
    - The "does NOT prove" half contains at least the six residues listed above, each stating the bounded claim, the wrong inference, and what would close it.
    - Residue 3 quotes the dated verdict from `173-HOOK-FINDINGS.md` rather than restating it from memory.
    - Every line in the "proves" half names a runnable command.
    - `.planning/ROADMAP.md` Phase 173 criterion 2 states 2, not 3, and cites D-05.
    - `git diff .planning/ROADMAP.md` touches only that one criterion line.
    - `.planning/REQUIREMENTS.md` is unmodified by this plan.
  </acceptance_criteria>
  <done>Every later phase that depends on this guard inherits a documented limit rather than a false guarantee.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.github/workflows/ci.yml` → whether a guard actually runs | An adversarial surface Phase 172 spent four rounds on; a guard absent from the filter is a guard that does not exist |
| `cmd/testdata/orphan_allowlist.json` → what "wired" means | The allowlist is the written record of tolerated exceptions; widening it silently is how orphans persist |
| The phase's own success claims → later phases building on them | Phase 174's subtree roll-up walks the parent linkage this phase records; a false guarantee here propagates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-173-52 | Tampering | A guard test silently dropped from the CI filter, so deleting the guard passes | mitigate | The five new files join `wiringGateGuardFiles`, and `TestWiringGateStepRunsEveryWiringTest` fails in both directions; red-proofed by deleting a test function and observing the failure |
| T-173-53 | Elevation of Privilege | A contract stub shipping as a permanent always-allow guard behind a real-looking call site | mitigate | `TestNoDelegationGuardContractStubSurvives` fails on either marker string; red-proofed by re-inserting one |
| T-173-54 | Elevation of Privilege | A future fifth delegation guard added without a fail-closed test | mitigate | `TestDelegationGuardTableCoversEveryGuardCommand` enumerates registered commands rather than a hardcoded list, so a new guard command must join the table or fail the build |
| T-173-55 | Repudiation | The allowlist growing to absorb this phase's new commands, recreating the orphan condition | mitigate | `TestOrphanAllowlistOnlyShrinks` fails on additions; the acceptance criteria require a removals-only diff, and plan 09 was forbidden from using an allowlist entry instead of a caller |
| T-173-56 | Spoofing | The phase claiming coverage it does not have, so Phase 174 builds on a false guarantee | mitigate | `173-RESIDUE.md` names six residues in bounded language with a stop rule, following `172-STOP-RULE.md`; requirement satisfaction is deliberately left to verification |
| T-173-57 | Tampering | The blanket `go test ./...` release gate narrowed while adding filter alternatives beside it | mitigate | `blanketReleaseGateProblem` already fails on narrowing, `if:`, `continue-on-error` or exit-swallowing; the acceptance criteria require that step byte-identical |
</threat_model>

<verification>
- `go test ./... -count=1 -timeout 900s` passes.
- `go vet ./...` clean.
- `go test ./cmd -run 'TestWiringGateStepRunsEveryWiringTest|TestNoRegisteredSubcommandIsUnreferenced|TestOrphanAllowlistOnlyShrinks|TestOrphanAllowlistIsPathKeyed|TestPathMigrationDidNotWidenTolerance|TestPreMigrationSnapshotIsFrozen|TestBlanketGateCheckRejectsADecoyStep|TestReleaseGateWorkflowShapeIsWhitelisted|TestNoDelegationGuardContractStubSurvives|TestAllDelegationGuardsFailClosedOnUnreadableState|TestDelegationGuardTableCoversEveryGuardCommand' -count=1 -v` passes.
- `git diff cmd/testdata/orphan_allowlist.json` shows removals only.
- `173-RESIDUE.md` exists with both halves and a stop rule; ROADMAP criterion 2 states 2.
</verification>

<success_criteria>
Every guard this phase built fails CI by name if removed, one command proves the fail-closed
property across all delegation guards at once, no contract stub survives, the tolerated-orphan list
shrank by exactly what was earned, and every residue is written down in bounded language so no
later phase inherits a false guarantee.
</success_criteria>

<output>
After completion, create `.planning/phases/173-delegation-guard/173-10-SUMMARY.md`.
</output>
