---
phase: 195-coherent-jobs
plan: 05
type: execute
wave: 3
depends_on: ["195-03"]
files_modified:
  - cmd/codex_workflow_cmds.go
  - cmd/ceremony_team_checkin.go
  - cmd/ceremony_team_checkin_test.go
  - cmd/forced_reviewer_waiver_test.go
  - cmd/orchestrator_boundary_questions_test.go
autonomous: true
requirements: [JOBS-01, JOBS-03]

must_haves:
  truths:
    - "Exactly one implementation job with no pending owner decision proceeds without a blocking check-in, regardless of how many coherent tasks it covers."
    - "The fast path still renders a compact summary naming the worker, covered tasks, relationship/benefit reason, and why approval is unnecessary."
    - "A forced-reviewer waiver or any other unanswered owner decision keeps the blocking check-in."
    - "--checkin forces the pause; --checkin with --no-checkin is rejected before any attempt or manifest mutation."
  artifacts:
    - path: "cmd/ceremony_team_checkin.go"
      provides: "Runtime-owned check-in policy, pending-decision predicate, and compact summary"
    - path: "cmd/ceremony_team_checkin_test.go"
      provides: "Decision matrix replacing TestOneWorkerTeamStillPauses"
  key_links:
    - from: "cmd/codex_workflow_cmds.go"
      to: "cmd/ceremony_team_checkin.go"
      via: "post-plan decideBuildCheckin policy and pre-plan flag conflict"
      pattern: "checkin"
    - from: "cmd/ceremony_team_checkin.go"
      to: "cmd/forced_reviewer_waiver.go"
      via: "live pending waiver detection"
      pattern: "forced-reviewer-waiver"
---

<objective>
Replace the redundant one-worker approval pause with a runtime-owned,
decision-aware fast path and an explicit owner override.

Purpose: This implements folded owner feedback D-11 through D-14 without
hiding grouping decisions or bypassing Phase 194's reviewer-waiver boundary.
Output: pure policy, `--checkin`, compact summary fields/rendering, and a full
check-in decision matrix.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-coherent-jobs/195-CONTEXT.md
@.planning/phases/195-coherent-jobs/195-RESEARCH.md
@.planning/phases/195-coherent-jobs/195-PATTERNS.md
@.planning/phases/195-coherent-jobs/195-03-SUMMARY.md
@.planning/phases/194-the-queen-decides-the-team/194-07-SUMMARY.md
@.planning/todos/pending/2026-08-23-one-worker-build-skips-the-checkin-pause.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the decision-aware check-in policy and early flag conflict</name>
  <files>cmd/codex_workflow_cmds.go, cmd/ceremony_team_checkin.go, cmd/ceremony_team_checkin_test.go, cmd/forced_reviewer_waiver_test.go, cmd/orchestrator_boundary_questions_test.go</files>
  <read_first>
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-11, D-13, D-14 and the exact owner intent
    - .planning/phases/195-coherent-jobs/195-RESEARCH.md — Pattern 7 and Pitfalls 7/8
    - .planning/phases/195-coherent-jobs/195-PATTERNS.md — flag/input, pending-decision guard, and test-to-replace sections
    - cmd/codex_workflow_cmds.go — build command handler, no-checkin flag, and current one-line checkin_requested assignment
    - cmd/ceremony_team_checkin.go — current card result map plus waiver-question persistence
    - cmd/forced_reviewer_waiver.go — live unresolved waiver records and phase/attempt scope
    - cmd/forced_reviewer_waiver_test.go — preserve Phase 194 owner-only/scope/capability tests while adding the check-in counterexample
    - cmd/orchestrator_boundary_questions.go — unanswered boundary questions that also require owner input
    - cmd/orchestrator_boundary_questions_test.go — add a generic unanswered-question check-in counterexample here
    - cmd/ceremony_team_checkin_test.go — TestOneWorkerTeamStillPauses; replace/invert it rather than leaving contradictory tests
  </read_first>
  <behavior>
    - Autopilot or explicit --no-checkin remains non-interactive.
    - Explicit --checkin forces a pause on an otherwise eligible one-worker build.
    - An unwaived forced reviewer or any unanswered orchestrator boundary decision forces a pause.
    - Exactly one implementation dispatch and no other build worker skips the pause; two workers pause.
    - --checkin plus --no-checkin returns a named conflict before plan-only, with no checkpoint/manifest/attempt/state mutation.
  </behavior>
  <action>
Write the table-driven policy and command side-effect tests first; replace
`TestOneWorkerTeamStillPauses` with the new decision, retaining its old behavior
as explicit-checkin/pending-decision rows. Add a symmetric `--checkin` Cobra
flag and reject its conflict with `--no-checkin` before calling
`runCodexBuildPlanOnlyWithOptions`. Implement pure `decideBuildCheckin` and a
single `buildHasPendingOwnerDecision` predicate over live, phase/attempt-scoped
forced-reviewer waiver rows and unanswered orchestrator boundary questions.
After final coherent jobs are known, apply D-11/D-13/D-14 precedence:
autopilot/no-checkin
non-interactive; explicit checkin; pending owner decision; exactly one
implementation dispatch; otherwise pause. Return machine-readable reason codes
and never infer pending state from rendered text.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestBuildCheckinDecisionMatrix|TestOneWorkerBuildSkipsCheckin|TestOneWorkerWithForcedReviewerWaiverStillPauses|TestOneWorkerWithBoundaryQuestionStillPauses|TestCheckinFlagConflictHasNoSideEffects' -count=1` passes
    - The decision matrix covers autopilot, --no-checkin, --checkin, both flags, one grouped worker, one ungrouped worker, two workers, forced-reviewer waiver, and generic boundary question
    - The conflict error names both flags and the test proves attempt/latest pointer, manifest, checkpoint, and colony state are unchanged
    - No test named `TestOneWorkerTeamStillPauses` remains with the old unconditional assertion
    - Pending-decision detection reads runtime state/records rather than searching summary prose
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(BuildCheckinDecisionMatrix|OneWorker.*(SkipsCheckin|StillPauses)|CheckinFlagConflict)' -count=1</automated>
  </verify>
  <done>The runtime skips only redundant one-worker approval and preserves every real owner decision boundary, with explicit owner override and fail-closed flag handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the required compact, non-blocking one-worker summary</name>
  <files>cmd/ceremony_team_checkin.go, cmd/ceremony_team_checkin_test.go, cmd/codex_workflow_cmds.go</files>
  <read_first>
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-12 exact required facts and D-04 reason quality
    - .planning/phases/195-coherent-jobs/195-RESEARCH.md — compact-summary requirement and "What Might Have Been Missed" check-in wording warning
    - .planning/phases/195-coherent-jobs/195-PATTERNS.md — separate compact renderer; do not render the full card and discard its question
    - .planning/phases/195-coherent-jobs/195-03-SUMMARY.md — actual dispatch JobName/JobReason/JobSource and grouped identity fields
    - cmd/ceremony_team_checkin.go — existing deterministic worker/task/reason rendering helpers
    - cmd/ceremony_team_checkin_test.go and cmd/codex_workflow_cmds.go — compact/full rendering assertions and returned result fields modified by this task
  </read_first>
  <behavior>
    - Fast-path result/visual names the worker, every covered task, grouping relationship, grouping benefit, and `no owner decision is pending, so dispatch continues`.
    - A single-task job uses an honest single-task reason rather than an empty grouping slot.
    - The compact renderer contains no question, approval choices, or blocking resume signal; the full card is unchanged for Requested=true.
  </behavior>
  <action>
Write compact/full-card contrast tests first. Add a distinct
`renderBuildFastPathSummary` that consumes final manifest dispatch data and the
check-in decision. Return `checkin_requested`, `checkin_reason`, and a structured
`checkin_summary` alongside the visual text. Reuse deterministic worker identity
and covered-task rendering; use accepted JobReason (relationship + benefit),
not a wrapper reconstruction. Per D-12, the summary must carry every covered
task and the no-owner-decision explanation. For a single ungrouped task, state that one worker
owns that task and no grouping was needed. Do not call the full card renderer on
the fast path and do not change the existing waiver options when a pause is
required.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestOneWorkerFastPathSummaryCarriesEveryFact|TestFastPathSummaryIsNonBlocking|TestPendingDecisionStillRendersFullCheckinCard' -count=1` passes
    - The grouped fixture's result map contains worker name, all covered task IDs, relationship, benefit, and the no-owner-decision reason
    - Compact visual output contains no approval prompt or `Approve/Adjust/Waive` choice
    - Requested=true still renders the existing full check-in and Phase 194 waiver choice
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(OneWorkerFastPathSummary|FastPathSummaryIsNonBlocking|PendingDecisionStillRendersFullCheckinCard)' -count=1</automated>
  </verify>
  <done>A one-worker build goes straight through visibly: the owner sees who, which tasks, why grouped, and why no approval is required.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(BuildCheckin|OneWorker|FastPathSummary|ForcedReviewerWaiver|BoundaryQuestion)' -count=1`
- Confirm the flag-conflict fixture leaves the entire temp data directory byte-equivalent.
</verification>

<success_criteria>
- D-11 through D-14 are implemented by runtime policy.
- Decision-free one-worker builds do not block and are not invisible.
- Pending decisions and explicit --checkin still pause.
</success_criteria>

<output>
Create `.planning/phases/195-coherent-jobs/195-05-SUMMARY.md` when done.
</output>
