---
phase: 195-coherent-jobs
plan: 04
type: execute
wave: 3
depends_on: ["195-02", "195-03"]
files_modified:
  - cmd/coherent_job_receipts.go
  - cmd/coherent_job_receipts_test.go
  - cmd/internal_worker_adapter.go
  - cmd/build_worker_run.go
  - cmd/codex_build.go
  - cmd/merged_dispatch_task_credit_test.go
autonomous: true
requirements: [JOBS-02]

must_haves:
  truths:
    - "A failed/interrupted grouped native worker credits only task IDs with manifest-validated receipts; touched files or mentioned tasks never imply completion."
    - "Structural receipt admission yields candidate task claims and sync paths without credit; only post-root evidence finalization can yield CompletedTaskIDs."
    - "Every credited task has its own requirements, normalized claimed paths, passing verification evidence, and root-computed artifact evidence."
    - "Whole-job success without receipts remains backward-compatible and still credits all covered task IDs."
  artifacts:
    - path: "cmd/coherent_job_receipts.go"
      provides: "Shared structural admission and root-evidence finalization contract"
    - path: "cmd/merged_dispatch_task_credit_test.go"
      provides: "Full and four-of-six native credit regressions through real reconciliation"
  key_links:
    - from: "cmd/codex_build.go"
      to: "cmd/coherent_job_receipts.go"
      via: "native structural admission followed by root-evidence finalization before completedBuildTaskIDs"
      pattern: "(admit|finalize)CoherentJobTaskReceipt"
    - from: "cmd/coherent_job_receipts.go"
      to: "cmd/codex_build.go"
      via: "task-specific codexBuildClaims.TaskClaims and root artifact attachment"
      pattern: "TaskClaims"
---

<objective>
Build the shared receipt trust boundary and use it for honest native partial
credit.

Purpose: Grouping increases the blast radius of coarse completion. D-08/D-09
require exact task evidence before any failed group can receive credit, while
preserving the existing full-success contract.
Output: one two-stage receipt boundary, native/internal transport,
task-specific claims, and fail-then-pass four-of-six reconciliation tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-coherent-jobs/195-CONTEXT.md
@.planning/phases/195-coherent-jobs/195-RESEARCH.md
@.planning/phases/195-coherent-jobs/195-PATTERNS.md
@.planning/phases/195-coherent-jobs/195-02-SUMMARY.md
@.planning/phases/195-coherent-jobs/195-03-SUMMARY.md
@pkg/codex/task_receipt.go
@cmd/codex_build.go
@cmd/internal_worker_adapter.go
</context>

<interfaces>
Add runtime-only `CompletedTaskIDs []string` and accepted task claims to the
internal dispatch/result representation. `CoveredTaskIDs` continues to mean
assignment. Define one shared two-stage contract: (1)
`admitCoherentJobTaskReceipts` validates manifest membership, task-specific
claims, normalized paths, status, summary, and verification, and returns a
`coherentJobReceiptAdmission` containing candidate claims plus `SyncPaths` but
no completion credit; (2) `finalizeCoherentJobTaskReceiptEvidence` consumes
only an admission after its files are present in root, attaches root-computed
artifact evidence, and returns accepted task claims plus `CompletedTaskIDs`.
The native lane uses both stages here, the external lane reuses both in plan
195-06, and plan 195-08 owns the same module while wiring admission → worktree
sync → finalization.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define two-stage receipt admission/finalization and preserve native transport</name>
  <files>cmd/coherent_job_receipts.go, cmd/coherent_job_receipts_test.go, cmd/internal_worker_adapter.go, cmd/build_worker_run.go</files>
  <read_first>
    - cmd/coherent_job_receipts.go and cmd/coherent_job_receipts_test.go — new files; create one shared admission/finalization contract and its adversarial table, not lane-specific copies
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-08/D-09 exact proof boundary
    - .planning/phases/195-coherent-jobs/195-RESEARCH.md — Pattern 4, Pitfalls 3/4, and adversarial JOBS-02 matrix
    - .planning/phases/195-coherent-jobs/195-PATTERNS.md — handoff normalization, packet violation, and task-claim analogs
    - pkg/codex/task_receipt.go and pkg/codex/handoff.go — shared transport and verification semantics from plan 195-02
    - cmd/internal_worker_adapter.go — internalWorkerResult and mapInternalWorkerResult
    - cmd/build_worker_run.go — terminal worker record and internal-to-external completion conversion
    - cmd/codex_build_finalize.go — contractViolation aggregation style; reuse its named-rule vocabulary rather than returning the first error
  </read_first>
  <behavior>
    - Structural admission accepts a candidate only when its unique task ID belongs to the dispatch, status is successful, summary is non-empty, normalized paths are root-contained subsets of aggregate claims, and handoff verification passes with concrete evidence.
    - Duplicate, unknown, out-of-scope, failed-status, empty-summary, unevidenced, and path-laundered receipts are refused by named rule without admitting that task.
    - Admission returns candidate task claims and a normalized union of sync paths but cannot return or populate CompletedTaskIDs.
    - Root-evidence finalization projects admitted claims under their own task IDs, attaches artifact hashes from root files, and returns CompletedTaskIDs only for candidates whose evidence succeeds.
    - Native/in-process result adapters retain task receipts even when the invocation returns a failed/interrupted terminal outcome.
  </behavior>
  <action>
Write the full adversarial table first. Implement
`admitCoherentJobTaskReceipts(root, phase, dispatch, aggregateClaims, receipts)`
as stage 1. Use `root` only for lexical containment/normalization through
existing repository-relative path helpers; do not require candidate files to
exist there. Require successful status, non-empty summary, passing concrete
verification, membership in `dispatchCoveredTaskIDs`, and claims bound to the
manifest task's criteria/evidence per D-09. Return candidate task claims,
normalized `SyncPaths`, and all named violations. This stage must not stat/read
root artifacts, attach hashes, mutate state, or expose `CompletedTaskIDs`.

Implement `finalizeCoherentJobTaskReceiptEvidence(root, phase, dispatch,
admission)` as stage 2. Accept only stage-1 candidates, call
`attachBuildArtifactEvidence` against the root checkout, collect named evidence
failures per task, and return task-specific accepted claims plus the only
runtime-owned `CompletedTaskIDs` set. Extend internalWorkerResult and
build_worker_run mapping so raw receipts survive native, hosted, cached, and
error-terminal paths. Keep both functions lane-neutral: plans 195-06 and 195-08
must reuse them rather than duplicating validation.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestCoherentJobReceiptAdmission|TestCoherentJobReceiptFinalization|TestNativeWorkerTransportKeepsTaskReceiptsOnFailure' -count=1` passes
    - The adversarial table names and asserts duplicate, unknown, out-of-scope, failed, empty-summary, unevidenced, absolute/parent path, aggregate-claim subset, and task-requirement mismatch refusals
    - Structural admission returns candidate claims plus normalized SyncPaths and has no CompletedTaskIDs field/output
    - Finalization has one accepted TaskClaims entry per credited task ID, root-computed artifact evidence, and an exactly matching CompletedTaskIDs set
    - Removing or making a root artifact unavailable after admission causes finalization to omit that task from credit
    - Removing a receipt's verification commands/evidence makes that task uncredited
    - A failed native WorkerResult still reaches validation with its receipts intact
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(CoherentJobReceipt(Admission|Finalization)|NativeWorkerTransportKeepsTaskReceipts)' -count=1</automated>
  </verify>
  <done>One shared two-stage contract admits receipt candidates without credit and finalizes only root-backed task claims/completed IDs, without losing valid native failure evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Credit exactly four of six tasks on the native path</name>
  <files>cmd/codex_build.go, cmd/merged_dispatch_task_credit_test.go, cmd/coherent_job_receipts_test.go</files>
  <read_first>
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-08/D-09 and "never infer completion because a related file changed"
    - cmd/codex_build.go — terminal result projection, completedBuildTaskIDs, reconcileCompletedBuildTasks, codexBuildClaims.TaskClaims, buildDispatchClaimOutputs, persistDispatchWorkerHandoff
    - cmd/merged_dispatch_task_credit_test.go — existing end-to-end whole-success covered chain; extend rather than replace it
    - cmd/coherent_job_receipts.go — accepted result contract created in Task 1
    - cmd/coherent_job_receipts_test.go — extend the same shared-validator fixtures with persisted-claim assertions
  </read_first>
  <behavior>
    - Whole success with no receipts still credits all six covered IDs.
    - Failed six-task job with four valid receipts credits exactly those four; the other two remain pending.
    - Failed six-task job with no valid receipt credits zero, even if all files changed or the summary names every task.
    - Task-specific claims/evidence exist for every credited ID and not for uncredited IDs.
  </behavior>
  <action>
Add the four-of-six and zero-receipt fail-then-pass cases to the real merged
dispatch credit test. Thread raw TaskReceipts through the native terminal
result, call structural admission, then immediately call root-evidence
finalization because in-repo files already live in root. Copy only the
finalizer's output into dispatch `CompletedTaskIDs` and task-specific claims. Keep the
existing `completed`/`completed_no_change` branch of `completedBuildTaskIDs`
crediting all covered tasks for compatibility. Add a distinct partial-terminal
branch that, per D-08, reads only finalizer-produced CompletedTaskIDs and never
infers completion from changed files or mentioned tasks. Ensure
`reconcileCompletedBuildTasks` and persisted `last-build-claims.json` operate on
that exact set. Do not implement retry or alter the append-only attempt yet;
plan 195-07 owns recovery persistence.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestMergedDispatchCreditsEveryCoveredTask|TestFailedGroupedDispatchCreditsExactlyReceiptedTasks|TestFailedGroupedDispatchWithoutReceiptsCreditsNone' -count=1` passes
    - The four-of-six fixture asserts four completed and two pending task statuses by ID
    - The same fixture asserts task claims/evidence for exactly four IDs
    - A touched-file/named-task counterexample with no receipts credits zero
    - A structurally admitted receipt whose root-evidence finalization fails remains pending and has no persisted task claim
    - Existing whole-success/no-change covered-task tests remain green
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(MergedDispatchCreditsEveryCoveredTask|FailedGroupedDispatch)' -count=1</automated>
  </verify>
  <done>Native partial completion is exact and evidence-backed, while the proven whole-success covered-task chain remains compatible.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(CoherentJobReceipt(Admission|Finalization)|NativeWorkerTransport|MergedDispatchCredits|FailedGroupedDispatch)' -count=1`
- Inspect persisted claims from the four-of-six fixture: four task IDs, no aggregate-primary-task laundering.
</verification>

<success_criteria>
- D-08 and D-09 hold on native results.
- Whole success remains backward-compatible.
- Partial credit is driven only by manifest-validated task receipts.
</success_criteria>

<output>
Create `.planning/phases/195-coherent-jobs/195-04-SUMMARY.md` when done.
</output>
