---
phase: 195-coherent-jobs
plan: 09
type: execute
wave: 7
depends_on: ["195-05", "195-08"]
files_modified:
  - .aether/commands/build.yaml
  - cmd/command_guide.go
  - cmd/command_guide_test.go
  - cmd/parity_test.go
  - .claude/commands/ant/build.md
  - .claude/commands/ant-build.md
  - .opencode/commands/ant/build.md
  - .aether/skills/colony/aether-colony-build-cycle/SKILL.md
  - cmd/lifecycle_wrapper_contract_test.go
  - cmd/cli_flag_audit_test.go
  - CLAUDE.md
  - .planning/todos/pending/2026-08-23-one-worker-build-skips-the-checkin-pause.md
  - .planning/todos/completed/2026-08-23-one-worker-build-skips-the-checkin-pause.md
autonomous: true
requirements: [JOBS-01, JOBS-02, JOBS-03, JOBS-04]

must_haves:
  truths:
    - "Wrappers may propose jobs and submit task receipts, but every accepted group, completion credit, retry, worktree reconciliation, and check-in decision remains Go-owned."
    - "The YAML command source, Codex command guide, Codex build-cycle skill, Claude/OpenCode wrappers, CLI flags, and completion schema describe one contract."
    - "All three build-wrapper copies are byte-identical and render runtime decisions rather than constructing manifest jobs themselves."
    - "The folded one-worker todo is closed with executable tests and all phase-wide focused/full gates pass."
  artifacts:
    - path: ".aether/commands/build.yaml"
      provides: "Canonical lifecycle contract for job proposals, receipts, recovery, and check-in fields"
    - path: ".claude/commands/ant/build.md"
      provides: "Primary wrapper behavior for proposing/rendering/finalizing coherent jobs"
    - path: "cmd/command_guide.go"
      provides: "Codex orchestration guidance aligned with the Go runtime"
  key_links:
    - from: ".aether/commands/build.yaml"
      to: "cmd/command_guide.go"
      via: "parity tests over canonical build contract"
      pattern: "job_proposal"
    - from: ".claude/commands/ant/build.md"
      to: "aether build/build-finalize"
      via: "runtime manifest decisions and task_receipts completion packet"
      pattern: "task_receipts"
    - from: ".aether/schemas/completion-packet.schema.json"
      to: ".aether/commands/build.yaml"
      via: "documented packet fields match generated contract"
      pattern: "task_receipts"
---

<objective>
Align every shipped orchestration surface with the runtime behavior and run the
phase's executable proof gates.

Purpose: Claude Code and OpenCode are the primary user paths, while Codex uses
the command guide/skill. A runtime-only implementation would leave wrappers
unable to propose jobs, file partial receipts, render local repairs, or honor the
new check-in reason.
Output: synchronized canonical YAML, guide, skill, wrapper triplet, docs/tests,
closed folded todo, and green full verification.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-coherent-jobs/195-CONTEXT.md
@.planning/phases/195-coherent-jobs/195-RESEARCH.md
@.planning/phases/195-coherent-jobs/195-PATTERNS.md
@.planning/phases/195-coherent-jobs/195-05-SUMMARY.md
@.planning/phases/195-coherent-jobs/195-06-SUMMARY.md
@.planning/phases/195-coherent-jobs/195-07-SUMMARY.md
@.planning/phases/195-coherent-jobs/195-08-SUMMARY.md
@.aether/schemas/completion-packet.schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the canonical YAML, Codex guide, and Codex build-cycle skill</name>
  <files>.aether/commands/build.yaml, cmd/command_guide.go, cmd/command_guide_test.go, cmd/parity_test.go, .aether/skills/colony/aether-colony-build-cycle/SKILL.md</files>
  <read_first>
    - AGENTS.md — Codex orchestration layer and the rule that intelligent build changes keep YAML, wrappers, skill, and command guide aligned
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — Go-authority boundary and D-04 through D-14 user-facing facts
    - .planning/phases/195-coherent-jobs/195-RESEARCH.md — Project Constraints, Prescriptive Ordering wave 5, deprecated claims, and full gate list
    - .planning/phases/195-coherent-jobs/195-PATTERNS.md — wrapper/skill/guide parity assignment and authority pattern
    - .aether/commands/build.yaml — current manifest/finalizer/check-in contract and drift-guard declaration
    - cmd/command_guide.go and cmd/command_guide_test.go — build PreSteps/contract assertions
    - cmd/parity_test.go — YAML/wrapper/guide parity family
    - .aether/skills/colony/aether-colony-build-cycle/SKILL.md — Codex plan/build/finalize orchestration; read completely before editing
    - .aether/schemas/completion-packet.schema.json — generated task_receipts shape from plan 195-02; document it, never edit it here
  </read_first>
  <action>
Update the build YAML contract to name repeatable `--job-proposal`, runtime
`job_decisions` (accepted/refused/repaired), dispatch job name/reason/source,
`task_receipts`, runtime-owned `completed_task_ids`, parent-linked recovery job,
`--checkin`, `checkin_requested`, `checkin_reason`, and compact summary. State
plainly that wrappers propose/render/spawn/submit while Go validates grouping,
dependency order, completion credit, retry, worktree reconciliation, and check-in
policy. Mirror the same sequence in the Codex command guide and build-cycle
skill: inspect tasks, optionally re-fetch using structured proposals, discard
unsafe proposal assumptions in favor of runtime repair decisions, submit
task-level receipts for partial proof, and obey runtime check-in fields. Extend
existing parity tests before prose changes so removing any contract item causes
a named failure. Do not teach Codex to edit a manifest or state file.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestCommandGuideBuildCoherentJobsContract|TestBuildCommandYAMLCoherentJobsParity|TestPlatformParity' -count=1` passes
    - YAML, guide, and skill each state that Go owns accepted groups, credit, retry, worktree reconciliation, and check-in policy
    - YAML/guide/skill name --job-proposal, job_decisions, task_receipts, completed_task_ids, --checkin, and checkin_reason
    - No surface instructs an agent to write CoveredTaskIDs/CompletedTaskIDs directly into a manifest or colony state
    - `go run ./cmd/aether contract-schema --check` remains green and this task does not modify the generated schema
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(CommandGuideBuildCoherentJobsContract|BuildCommandYAMLCoherentJobsParity|PlatformParity)' -count=1 &amp;&amp; go run ./cmd/aether contract-schema --check</automated>
  </verify>
  <done>The canonical command source and Codex orchestration surfaces describe and enforce one runtime-authoritative coherent-job contract.</done>
</task>

<task type="auto">
  <name>Task 2: Update the wrapper triplet and owner docs, close the todo, and run phase gates</name>
  <files>.claude/commands/ant/build.md, .claude/commands/ant-build.md, .opencode/commands/ant/build.md, cmd/lifecycle_wrapper_contract_test.go, cmd/cli_flag_audit_test.go, cmd/parity_test.go, CLAUDE.md, .planning/todos/pending/2026-08-23-one-worker-build-skips-the-checkin-pause.md, .planning/todos/completed/2026-08-23-one-worker-build-skips-the-checkin-pause.md</files>
  <read_first>
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — all D-01 through D-14; wrappers must not implement deferred cost/closing-card/spec-builder ideas
    - .planning/phases/195-coherent-jobs/195-05-SUMMARY.md — actual check-in flags/reasons/summary and pending-decision behavior
    - .planning/phases/195-coherent-jobs/195-06-SUMMARY.md and 195-07-SUMMARY.md — actual task receipt and recovery response fields
    - .planning/phases/195-coherent-jobs/195-08-SUMMARY.md — actual worktree grouping/recovery behavior
    - .claude/commands/ant/build.md — current manifest re-fetch, team check-in, spawn, completion-packet, and finalize stages
    - cmd/lifecycle_wrapper_contract_test.go, cmd/cli_flag_audit_test.go, cmd/parity_test.go — byte parity, referenced flags, and contract tests
    - CLAUDE.md — Definition of Done, Queen-Owned Orchestration, Team Check-In and Owner Decisions, and plain-English mandate
    - .planning/todos/pending/2026-08-23-one-worker-build-skips-the-checkin-pause.md — preserve the owner's verbatim intent and existing history when resolving/moving it
    - .planning/todos/completed/2026-08-23-one-worker-build-skips-the-checkin-pause.md — new destination; create it by moving the preserved pending file after appending resolution evidence
  </read_first>
  <action>
Write/extend wrapper-contract tests first so stale one-worker/check-in and
wrapper-authored-group behavior fail. Edit `.claude/commands/ant/build.md` as
the canonical body, then make the flat Claude and OpenCode copies byte-identical
rather than typing three variants. The wrapper may propose structured jobs and
re-fetch through the existing Phase 194 proposal loop, but it must render the
runtime's accepted/refused/repaired decisions and never mutate dispatches. If
`checkin_requested` is false because of automatic one-worker policy, render the
runtime compact summary and continue; do not claim only `--no-checkin` can make
it false. If true, keep the full Phase 194 waiver flow. Completion instructions
must submit per-task receipts for proven partial work, preserve covered_task_ids
as assignment scope, and use the runtime recovery job/attempt on failure.
Rewrite the relevant CLAUDE.md paragraphs from current summaries (do not append
a contradictory second rule). Preserve the dirty folded todo's existing text,
set `resolves_phase: 195`, append the tests proving closure, and move it to the
completed path. Do not add Phase 196 cost display or Phase 197/198 closing-card
work.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestLifecycleWrapper.*CoherentJob|TestCLIFlagAudit|TestPlatformParity|TestCommandGuideBuildCoherentJobsContract' -count=1` passes
    - `cmp -s .claude/commands/ant/build.md .claude/commands/ant-build.md` and `cmp -s .claude/commands/ant/build.md .opencode/commands/ant/build.md` both succeed
    - No wrapper says `checkin_requested` false only means --no-checkin, and each consumes runtime checkin_reason/compact summary
    - Each wrapper names task_receipts, renders refused/repaired job decisions, and never authors manifest job membership
    - CLAUDE.md states one decision-free worker proceeds, pending decisions/--checkin pause, and both in-repo/worktree grouping occur before ownership
    - The completed todo preserves owner intent and names `TestOneWorkerBuildSkipsCheckin`, `TestOneWorkerWithForcedReviewerWaiverStillPauses`, and `TestCheckinFlagConflictHasNoSideEffects`
    - `go test ./... -count=1`, `go test ./... -race -count=1`, `go vet ./...`, `go build ./cmd/aether`, and `go run ./cmd/aether contract-schema --check` all pass
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(LifecycleWrapper.*CoherentJob|CLIFlagAudit|PlatformParity|CommandGuideBuildCoherentJobsContract)' -count=1 &amp;&amp; cmp -s .claude/commands/ant/build.md .claude/commands/ant-build.md &amp;&amp; cmp -s .claude/commands/ant/build.md .opencode/commands/ant/build.md &amp;&amp; go run ./cmd/aether contract-schema --check</automated>
  </verify>
  <done>Every shipped build surface follows the same runtime-owned coherent-job/check-in/receipt contract, the wrapper triplet is identical, the folded todo is closed, and all full gates are green.</done>
</task>

</tasks>

<verification>
- Focused: `go test ./cmd ./pkg/colony ./pkg/codex -run 'Test(Coherent|Job|CalVault|Receipt|Checkin|DetectCycles|Worktree|LifecycleWrapper|CommandGuide|PlatformParity)' -count=1`
- Full: `go test ./... -count=1`
- Race: `go test ./... -race -count=1`
- Static/build: `go vet ./...` and `go build ./cmd/aether`
- Contract: `go run ./cmd/aether contract-schema --check`
- Wrapper parity: all three build wrapper files compare byte-identical.
</verification>

<success_criteria>
- Canonical wrappers/docs/schema/YAML/guide/skill surfaces are aligned.
- All JOBS-01 through JOBS-04 executable proofs pass in both execution modes.
- The owner-facing fast path is visible, decision-safe, and documented plainly.
- No deferred Phase 196–198 or spec-builder work entered this phase.
</success_criteria>

<output>
Create `.planning/phases/195-coherent-jobs/195-09-SUMMARY.md` when done.
</output>
