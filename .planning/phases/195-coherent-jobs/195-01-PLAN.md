---
phase: 195-coherent-jobs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/coherent_jobs.go
  - cmd/coherent_jobs_test.go
autonomous: true
requirements: [JOBS-01, JOBS-03]

must_haves:
  truths:
    - "A real missing dependency or dependency cycle blocks the phase before any job, manifest, attempt, or worker exists."
    - "The Queen can propose a named, reasoned job; an unsafe proposal is refused by name while unrelated safe proposals remain intact."
    - "Without a proposal, same-caste tasks connected by dependencies or meaningful shared files form coherent jobs before execution waves are assigned."
    - "Automatic job size is governed by the existing worker-brief allowance, never by a magic task-count cap."
  artifacts:
    - path: "cmd/coherent_jobs.go"
      provides: "Pure, Go-owned coherent-job planner and proposal decisions"
    - path: "cmd/coherent_jobs_test.go"
      provides: "Fail-then-pass graph, proposal, CalVault, path-policy, and soft-limit regressions"
  key_links:
    - from: "cmd/coherent_jobs.go"
      to: "pkg/colony/cycle.go"
      via: "whole-plan DetectCycles preflight"
      pattern: "DetectCycles"
    - from: "cmd/coherent_jobs.go"
      to: "cmd/codex_build_worktree.go"
      via: "declaredPathsForTask exact path evidence"
      pattern: "declaredPathsForTask"
---

<objective>
Define the authoritative task-to-job contract and pure planner before any build
pipeline wiring changes.

Purpose: JOBS-01 and JOBS-03 cannot be satisfied by broadening the current
post-wave coalescer. This plan makes grouping a deterministic, testable runtime
decision whose output later plans can consume before waves and worktrees exist.
Output: `cmd/coherent_jobs.go` and exhaustive pure regressions, including the
real six-batch CalVault shape.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-coherent-jobs/195-CONTEXT.md
@.planning/phases/195-coherent-jobs/195-RESEARCH.md
@.planning/phases/195-coherent-jobs/195-PATTERNS.md
@cmd/codex_build.go
@cmd/codex_build_worktree.go
@pkg/colony/cycle.go
@pkg/agent/task_graph.go
</context>

<interfaces>
The plan must create and keep these package-private contracts stable for later
plans: `coherentJobProposal`, `coherentJobTask`, `coherentJob`,
`coherentJobDecision`, `coherentJobPlan`, and `planCoherentJobs`. Proposal JSON
fields are `name`, `task_ids`, `owner_caste`, `relationship`, `benefit`, and
`owner_reason`. Job source values are `queen`, `automatic`, and `single`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define proposal contracts, global dependency preflight, and local repair</name>
  <files>cmd/coherent_jobs.go, cmd/coherent_jobs_test.go</files>
  <read_first>
    - cmd/coherent_jobs.go and cmd/coherent_jobs_test.go — new files; create them only after reading the analogs below
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-02 and D-04 through D-07 are locked and must be named in the test table
    - .planning/phases/195-coherent-jobs/195-RESEARCH.md — Patterns 1 and 3, plus Pitfalls 2 and 6
    - .planning/phases/195-coherent-jobs/195-PATTERNS.md — `cmd/coherent_jobs.go` assignment and the Queen proposal analog
    - pkg/colony/cycle.go and pkg/colony/cycle_test.go — reuse typed `MissingDepError` and `CycleError`; do not write another DFS
    - cmd/queen_judgement.go — reuse `resolveCasteName` semantics and the refuse-one/keep-the-rest decision style
    - pkg/agent/task_graph.go — understand the existing wave API, but do not reuse its fail-open no-ready-task fallback
  </read_first>
  <behavior>
    - A safe proposal with ordered task IDs, a normalized owner caste, relationship, and benefit is accepted and produces one job.
    - A proposal whose task order places a task before an in-group dependency is refused with proposal name, offending task ID, and dependency ID; its members alone return to automatic grouping.
    - Two safe proposals remain accepted when a third is refused; no global one-worker-per-task fallback occurs.
    - A cross-caste proposal requires `owner_reason`; automatic grouping never crosses caste boundaries.
    - A missing dependency or named cycle returns a hard error before any job plan is returned, with the repair action `repair depends_on in the plan`.
  </behavior>
  <action>
Write the named tests first and run them to record RED in the summary. Then add
the package-private proposal/job structs and `planCoherentJobs`. The function
accepts the full `colony.Phase`, selected coherent-job task seeds, and proposals;
it calls `colony.DetectCycles` before proposal processing. Validate unique
proposal names and task membership, selected-task scope, owner caste,
relationship, benefit, cross-caste `owner_reason`, and the proposal's supplied
dependency order. Per D-02, automatic ownership never crosses a caste boundary
and an explicit cross-caste proposal needs one suitable named owner plus
`owner_reason`. Per D-04, construct `JobReason` from the two structured facts
(`relationship + ", so " + benefit`) rather than accepting generic prose. Per
D-05/D-06, record an explicit refused decision and runtime-generated replacement
jobs for only that proposal; never silently reorder the Queen's list. Per D-07,
missing dependencies and cycles return a named hard error before a partial plan.
Keep all code pure: no store, manifest, attempt, brief, or worktree writes.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestCoherentJobProposalAccepted|TestCoherentJobProposalOrderRefusedByName|TestCoherentJobRepairKeepsSafeProposals|TestCoherentJobCrossCasteNeedsOwnerReason|TestCoherentJobGraphErrorsHaveNoPlan' -count=1` passes
    - The order-refusal assertion contains the proposal name, offending task ID, and unmet dependency ID
    - The cycle assertion contains every task in the cycle and `repair depends_on in the plan`
    - The safe-proposal fixture still returns both safe jobs after the third proposal is refused
    - No function in cmd/coherent_jobs.go writes through `store`, creates an attempt, or constructs a worktree
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestCoherentJob(Proposal|Repair|CrossCaste|Graph)' -count=1</automated>
  </verify>
  <done>The Go runtime has a pure, structurally reasoned Queen-job contract that fails closed globally and repairs invalid proposals locally.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Form automatic coherent components from dependencies and meaningful paths</name>
  <files>cmd/coherent_jobs.go, cmd/coherent_jobs_test.go</files>
  <read_first>
    - cmd/coherent_jobs.go and cmd/coherent_jobs_test.go — extend Task 1's new contracts and tests; do not fork a second planner
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-01 through D-03 and the CalVault acceptance fixture
    - .planning/phases/195-coherent-jobs/195-RESEARCH.md — Pattern 2, the named incidental-path list, soft-limit rule, selected-task warning, and JOBS-03 test map
    - .planning/phases/195-coherent-jobs/195-PATTERNS.md — meaningful-path input, soft-limit constant, and `dispatch_coalesce_test.go` analog
    - cmd/codex_build_worktree.go — `declaredPathsForTask` is the only path-evidence source
    - cmd/build_print_brief.go — `briefTaskContentAllowanceChars`; reference this constant instead of duplicating 6000
    - cmd/dispatch_coalesce_test.go — copy the literal six dependent file-copy task shape and its independent/cross-caste controls
  </read_first>
  <behavior>
    - The real six-batch CalVault file-copy fixture becomes one automatic Builder job.
    - Same-caste non-consecutive tasks connected by a dependency chain form one component and receive deterministic topological order.
    - An exact meaningful shared implementation path joins tasks; overlap only through README/changelog/release-note/dependency-manifest/lockfile paths does not.
    - Different castes remain separate automatically, with dependency edges retained in the job DAG.
    - A projected component over `briefTaskContentAllowanceChars` splits only at dependency-safe boundaries; a single oversized task is never split and there is no task-count cap.
  </behavior>
  <action>
Write the CalVault and table regressions first, run them against the absent
planner, and record RED. Implement exact normalized meaningful-path edges via
`declaredPathsForTask`, a table-tested `isIncidentalGroupingPath`, same-caste
dependency edges, deterministic connected components, topological ordering with
original plan order as the independent-task tie breaker, and job-DAG waves.
Per D-01, incidental bookkeeping overlap is never an edge; per D-02, automatic
edges never cross resolved owner castes.
Use a named `projectCoherentJobBriefChars` calculation over the goal,
constraints, hints, criteria, and evidence text the brief renders; compare it
to `briefTaskContentAllowanceChars` and split automatic components only where
dependencies remain safe. Per D-03, never use `len(tasks)` as a limit and never
split an explicitly proposed oversized job whose relationship/benefit explains
end-to-end ownership. Tasks without exact path evidence group only through a
dependency or explicit proposal.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestCalVaultSixBatchesPlanAsOneCoherentJob|TestCoherentJobsGroupMeaningfulPathsAndDependencies|TestCoherentJobsIgnoreIncidentalPaths|TestCoherentJobsRespectCasteBoundaries|TestCoherentJobsUseBriefAllowanceNotTaskCount' -count=1` passes
    - The CalVault fixture contains six literal file-copy batches and asserts one job with all six ordered IDs, not merely fewer than six
    - Incidental-path tests cover README variants, changelog/history/release notes, Go/npm/Python/Cargo/Ruby manifests, and their lockfiles
    - A non-consecutive dependency fixture proves grouping is independent of adjacency
    - A source assertion or behavioral counterexample proves task count alone never triggers a split
    - `go test ./cmd -run 'TestCoherentJob' -count=1` passes as one suite
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(CalVaultSixBatchesPlanAsOneCoherentJob|CoherentJobs)' -count=1</automated>
  </verify>
  <done>Default grouping produces deterministic, dependency-safe jobs from meaningful relationships, including one CalVault job, without crossing castes or using a magic task cap.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(CoherentJob|CalVaultSixBatchesPlanAsOneCoherentJob)' -count=1`
- No stateful runtime helper is called by `planCoherentJobs`.
</verification>

<success_criteria>
- The pure planner covers D-01 through D-07 exactly.
- The real CalVault shape is one automatic job and independent work still fans out.
- A bad proposal is visibly repaired without corrupting safe proposal choices.
</success_criteria>

<output>
Create `.planning/phases/195-coherent-jobs/195-01-SUMMARY.md` when done.
</output>
