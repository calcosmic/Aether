---
phase: 195-coherent-jobs
plan: 07
type: execute
wave: 5
depends_on: ["195-06"]
files_modified:
  - cmd/coherent_job_retry.go
  - cmd/coherent_job_retry_test.go
  - cmd/build_attempt.go
  - cmd/build_attempt_test.go
  - cmd/build_attempt_external_test.go
  - cmd/codex_build.go
  - cmd/codex_build_finalize.go
autonomous: true
requirements: [JOBS-02]

must_haves:
  truths:
    - "A partial grouped outcome creates a new dependency-safe job containing only unfinished tasks; credited tasks are treated as satisfied and never redone."
    - "The recovery job is recorded as a new append-only attempt linked to the original job/attempt."
    - "The first attempt's dispatches, receipts, claims, completion digest, and history remain unchanged after retry creation."
    - "Direct and external lanes use the same unfinished-task/revalidation logic."
  artifacts:
    - path: "cmd/coherent_job_retry.go"
      provides: "Shared unfinished-only recovery planner"
    - path: "cmd/build_attempt.go"
      provides: "Optional parent attempt/job provenance on new attempt records"
  key_links:
    - from: "cmd/coherent_job_retry.go"
      to: "cmd/coherent_jobs.go"
      via: "re-plan unfinished task seeds with credited dependencies satisfied"
      pattern: "planCoherentJobs"
    - from: "cmd/codex_build.go"
      to: "cmd/build_attempt.go"
      via: "new child attempt after partial terminal persistence"
      pattern: "ParentAttemptID"
---

<objective>
Turn honest partial credit into honest, append-only recovery.

Purpose: D-10 requires a new job/attempt for unfinished work, not mutation of
the first receipt or a worker redoing proven tasks. Both completion lanes must
reach the same recovery shape.
Output: unfinished-only planner, parent-linked attempt records, and direct plus
external append-only regression fixtures.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-coherent-jobs/195-CONTEXT.md
@.planning/phases/195-coherent-jobs/195-RESEARCH.md
@.planning/phases/195-coherent-jobs/195-PATTERNS.md
@.planning/phases/195-coherent-jobs/195-04-SUMMARY.md
@.planning/phases/195-coherent-jobs/195-06-SUMMARY.md
@cmd/coherent_jobs.go
@cmd/build_attempt.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add parent-linked attempt provenance and unfinished-only job planning</name>
  <files>cmd/coherent_job_retry.go, cmd/coherent_job_retry_test.go, cmd/build_attempt.go, cmd/build_attempt_test.go</files>
  <read_first>
    - cmd/coherent_job_retry.go and cmd/coherent_job_retry_test.go — new files; reuse planCoherentJobs and existing attempt helpers rather than creating a second graph/store
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-10 exact append-only and no-redo contract
    - .planning/phases/195-coherent-jobs/195-RESEARCH.md — Pattern 5 and the partial-terminal transition warning
    - .planning/phases/195-coherent-jobs/195-PATTERNS.md — attempt record/SaveJSON/atomic append analog
    - cmd/build_attempt.go — buildAttemptRecord, beginBuildAttempt, transitionBuildAttempt, existing checkFixAttemptRecord ParentAttemptID precedent, latest pointer binding
    - cmd/coherent_jobs.go — canonical planner; retry must reuse it rather than hand-building waves
    - cmd/build_attempt_test.go — attempt immutability and transition fixture style
  </read_first>
  <behavior>
    - A parent six-task job with four credited IDs yields one child job containing only the remaining two IDs.
    - Dependencies on the four credited IDs are treated as satisfied; an unfinished-to-unfinished dependency remains ordered.
    - A new attempt records optional parent_attempt_id and parent_job_name while existing attempt JSON remains readable.
    - Creating/updating the child leaves the parent's serialized record byte/JSON-equivalent.
  </behavior>
  <action>
Write the unfinished-selection, dependency-revalidation, compatibility, and
parent-immutability tests first. Add optional `ParentAttemptID` and
`ParentJobName` fields to `buildAttemptRecord`, plus a child-attempt constructor
that creates a new record through the existing SaveJSON/latest-pointer path and
never edits the parent. Per D-10, implement `planCoherentJobRetry` to select only
uncredited IDs from the original job, treat credited task dependencies as
satisfied, preserve unfinished dependencies, and call the canonical coherent
planner for ordering/waves. Return no retry for full success and a named hard
error if the unfinished subgraph is genuinely cyclic/missing after credited
dependencies are accounted for.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestCoherentJobRetryContainsOnlyUnfinishedTasks|TestCoherentJobRetryTreatsCreditedDependenciesAsSatisfied|TestBuildAttemptChildLinksParentWithoutMutation|TestLegacyBuildAttemptReadsWithoutParentFields' -count=1` passes
    - The four-of-six fixture's child has exactly the two unfinished IDs in dependency-safe order
    - Parent record bytes or deep JSON equality are unchanged before/after child creation
    - Child history and status transitions append only to the child
    - A missing/cycle error names the unfinished task path and creates no child attempt
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(CoherentJobRetry|BuildAttemptChild|LegacyBuildAttempt)' -count=1</automated>
  </verify>
  <done>The runtime can derive and persist a new unfinished-only job without rewriting the attempt that supplied partial proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use the same append-only recovery transition in direct and external lanes</name>
  <files>cmd/codex_build.go, cmd/codex_build_finalize.go, cmd/build_attempt_test.go, cmd/build_attempt_external_test.go, cmd/coherent_job_retry_test.go</files>
  <read_first>
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-08 through D-10 together; retry follows accepted credit, never precedes it
    - .planning/phases/195-coherent-jobs/195-04-SUMMARY.md — native partial credit and result retention
    - .planning/phases/195-coherent-jobs/195-06-SUMMARY.md — external partial state and accepted diagnostics
    - cmd/codex_build.go — direct terminal result persistence and rollbackCodexBuildFailure; partial proof must not be erased by wholesale rollback
    - cmd/codex_build_finalize.go — terminal evidence/claims order and incomplete-result recovery response
    - cmd/build_attempt_external_test.go — bound external attempt and immutable completion digest fixtures
    - cmd/build_attempt_test.go and cmd/coherent_job_retry_test.go — direct parent/child and unfinished-only integration fixtures modified by this task
  </read_first>
  <behavior>
    - Native four-of-six failure persists four credits, closes the parent partial terminal, creates a child attempt/job for two, and returns its exact recovery command/data.
    - External four-of-six finalization produces the same parent/child/task-state relationship.
    - Repeating the same completion cannot create duplicate children or overwrite the parent.
    - A new worker's child job never includes any credited task.
  </behavior>
  <action>
Add direct/external fail-then-pass integration fixtures. Introduce an explicit
partial-terminal path after terminal results, accepted claims, and task credit
are durable. On the direct lane, bypass wholesale
`rollbackCodexBuildFailure(originalState, ...)` only for validated partial
proof; preserve completed task state and the parent terminal record, then create
the child through `planCoherentJobRetry`. Per D-10, neither lane may overwrite
the parent or assign credited tasks to the child. On external finalize, run the same
helper after accepted partial state is persisted. Return structured
`recovery_job`, `parent_attempt_id`, `retry_attempt_id`, unfinished IDs, and an
exact redispatch command. Make child creation idempotent against the same parent
completion digest and keep the phase in progress until all tasks are proven.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestGroupedJobPartialRetryIsAppendOnlyDirect|TestGroupedJobPartialRetryIsAppendOnlyExternal|TestGroupedJobPartialRetryIsIdempotent|TestGroupedJobRetryNeverReassignsCreditedTasks' -count=1` passes
    - Both lane fixtures finish with four completed/two pending tasks, one immutable parent attempt, and one child attempt containing two IDs
    - Parent dispatches, receipts, claims, history, and completion digest are unchanged after child creation and replay
    - The structured recovery response names both attempt IDs, unfinished task IDs, and an executable redispatch command
    - A worker dispatch derived from the child contains no credited ID
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestGroupedJob(PartialRetry|RetryNever)' -count=1</automated>
  </verify>
  <done>Native and external partial outcomes create the same idempotent, unfinished-only, parent-linked recovery job without losing or replaying proof.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(CoherentJobRetry|BuildAttemptChild|GroupedJobPartialRetry|GroupedJobRetryNever)' -count=1`
- Inspect both parent/child attempt JSON pairs and exact task state in direct and external fixtures.
</verification>

<success_criteria>
- D-10 holds end to end on both lanes.
- Valid partial evidence survives failure and retry creation.
- No proven task is re-assigned and no earlier attempt is overwritten.
</success_criteria>

<output>
Create `.planning/phases/195-coherent-jobs/195-07-SUMMARY.md` when done.
</output>
