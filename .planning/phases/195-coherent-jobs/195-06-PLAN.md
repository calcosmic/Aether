---
phase: 195-coherent-jobs
plan: 06
type: execute
wave: 4
depends_on: ["195-04"]
files_modified:
  - cmd/coherent_job_receipts.go
  - cmd/codex_build_finalize.go
  - cmd/wrapper_bundled_completion_test.go
  - cmd/codex_build_finalize_test.go
autonomous: true
requirements: [JOBS-02]

must_haves:
  truths:
    - "External/wrapper completion uses the same structural-admission and root-evidence-finalization stages as native completion."
    - "A four-of-six failed grouped packet credits exactly four tasks and persists task-specific claims; absent or refused receipts credit none for those tasks."
    - "Legacy whole-success packets without receipts continue to credit all manifest-covered task IDs."
    - "Correctable receipt violations are named per receipt and cannot launder aggregate files into another task."
  artifacts:
    - path: "cmd/codex_build_finalize.go"
      provides: "External task-receipt admission, root-evidence finalization, task-claim projection, and partial credit"
    - path: "cmd/wrapper_bundled_completion_test.go"
      provides: "Manifest-validated wrapper partial-receipt and legacy covered-task regressions"
  key_links:
    - from: "cmd/codex_build_finalize.go"
      to: "cmd/coherent_job_receipts.go"
      via: "external structural admission plus post-root evidence finalization"
      pattern: "(admit|finalize)CoherentJobTaskReceipt"
    - from: "cmd/codex_build_finalize.go"
      to: "cmd/codex_build.go"
      via: "runtime-owned CompletedTaskIDs and codexBuildClaims.TaskClaims"
      pattern: "CompletedTaskIDs"
---

<objective>
Close native/external lane drift by applying the same task-receipt trust
boundary to `build-finalize`.

Purpose: The chat wrappers are the owner's primary path. JOBS-02 and D-08/D-09
are incomplete if only the direct Go worker path can record honest partial
proof.
Output: external receipt admission, task-specific claims, partial-credit state,
and adversarial wrapper packet tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-coherent-jobs/195-CONTEXT.md
@.planning/phases/195-coherent-jobs/195-RESEARCH.md
@.planning/phases/195-coherent-jobs/195-PATTERNS.md
@.planning/phases/195-coherent-jobs/195-02-SUMMARY.md
@.planning/phases/195-coherent-jobs/195-04-SUMMARY.md
@cmd/coherent_job_receipts.go
@cmd/codex_build_finalize.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admit external receipt candidates through the shared stage-one contract</name>
  <files>cmd/coherent_job_receipts.go, cmd/codex_build_finalize.go, cmd/wrapper_bundled_completion_test.go</files>
  <read_first>
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-08/D-09 and existing wrapper-bundled trust boundary references
    - .planning/phases/195-coherent-jobs/195-RESEARCH.md — Pattern 4 and external adversarial fixture list
    - .planning/phases/195-coherent-jobs/195-04-SUMMARY.md — actual admission/finalization interfaces and native accepted-credit semantics
    - cmd/coherent_job_receipts.go — shared stage-one admission contract modified by this task; extend it rather than creating an external-only validator
    - cmd/codex_build_finalize.go — mergeExternalBuildResults, selectExternalBuildResultForDispatch, validateExternalWorkerResultClaimPaths, buildExternalBuildClaims, attachBuildArtifactEvidence, result diagnostics
    - cmd/wrapper_bundled_completion_test.go — FIELD-02 unknown/duplicate/fabricated/unevidenced covered_task_ids locks; retain them all
    - .planning/todos/completed/2026-08-21-completion-packet-cannot-express-bundled-work.md — field failure that distinguishes assignment coverage from receipt proof
  </read_first>
  <behavior>
    - External result receipts are structurally admitted by the same function/rules as native receipts.
    - Four valid receipts plus two absent receipts yield four candidate claims and normalized sync paths but no CompletedTaskIDs before root finalization.
    - Duplicate/unknown/out-of-scope/failed/unevidenced/path-laundered receipt entries are refused by named rule and do not erase other valid receipts.
    - Worker-provided hashes are ignored; stage one performs no artifact reads and exposes no credit.
  </behavior>
  <action>
Write external parity and adversarial cases first. In
`mergeExternalBuildResults`, map each result's aggregate claims and TaskReceipts
into `admitCoherentJobTaskReceipts` using the manifest dispatch and phase tasks.
Store the immutable admission result (candidate task claims and normalized sync
paths) on the merged runtime intermediate; do not set `CompletedTaskIDs` or
persist accepted task claims in this stage. Accumulate a named diagnostic for each refused receipt
while retaining unrelated valid receipts; absent receipts on a failed job mean
no credit, not an invented packet error. Preserve every existing
covered_task_ids unknown/duplicate/fabricated/unevidenced guard. Keep the
admission shape identical to the native lane so in-repo finalization here and
worktree sync/finalization in plan 195-08 consume the same contract.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestExternalTaskReceiptsUseSharedAdmission|TestWrapperGroupedFailureCreditsFourOfSix|TestWrapperTaskReceiptRefusalsDoNotEraseValidReceipts|TestWrapperBundled' -count=1` passes
    - The four-of-six merged intermediate contains four admitted candidate claims and their normalized sync paths, with no CompletedTaskIDs yet
    - Every native adversarial rule from plan 195-04 has an external equivalent with the same rule identifier
    - Existing covered_task_ids unknown/duplicate/fabricated/unevidenced tests remain green
    - No aggregate primary-task claim is copied to a covered task without that task's receipt
    - A source/behavior assertion proves mergeExternalBuildResults cannot call attachBuildArtifactEvidence or expose completion credit
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(ExternalTaskReceipts|WrapperGroupedFailure|WrapperTaskReceipt|WrapperBundled)' -count=1</automated>
  </verify>
  <done>The primary wrapper lane produces the same no-credit structural admission as the native lane, without weakening FIELD-02 guards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize external root evidence and reconcile exact task state</name>
  <files>cmd/codex_build_finalize.go, cmd/codex_build_finalize_test.go, cmd/wrapper_bundled_completion_test.go</files>
  <read_first>
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-08 through D-10 and the requirement that partial state stay honest before retry
    - cmd/codex_build_finalize.go — runCodexBuildFinalize ordering: terminal evidence, claims, manifest, worktree merge, state transition, reconcileCompletedBuildTasks, attempt transition
    - cmd/codex_build.go — completedBuildTaskIDs and reconcileCompletedBuildTasks semantics from plan 195-04
    - cmd/codex_build_finalize_test.go — real temp-colony state/finalizer fixtures
    - cmd/wrapper_bundled_completion_test.go — whole-success covered-task end-to-end state assertions
  </read_first>
  <behavior>
    - Whole successful grouped completion without receipts still reaches BUILT and marks every covered task completed.
    - In-repo partial external completion finalizes admitted candidates against root, then marks exactly the resulting CompletedTaskIDs complete and keeps the phase in progress rather than falsely BUILT.
    - An admitted candidate whose root artifact/evidence is absent is removed during finalization and remains pending.
    - No valid receipts means no grouped task state changes.
    - Result collection diagnostics and last-build-claims agree with final task state.
  </behavior>
  <action>
Add full/partial/no-proof finalizer state tests first. Route the external merged
admission through `finalizeCoherentJobTaskReceiptEvidence` only after accepted
files are available in root; use its exact task claims/CompletedTaskIDs and
never reconstruct credit from receipts or aggregate claims. For an in-repo
completion, admission and finalization can run consecutively. If the completion
is worktree-backed, retain the admission for plan 195-08's sync-first adapter
and do not expose completion IDs in this pre-sync path. Preserve the
legacy full-success transition to BUILT. For a partial terminal job, durably
write terminal results, accepted task claims, and exact task status before
returning a recoverable incomplete outcome; do not set the phase/colony BUILT
until all selected tasks are proven. Ensure result diagnostics list credited
and unfinished IDs separately. Plan 195-07 will create and link the new retry
attempt; this task must leave enough durable evidence to do so without replaying
the packet.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestExternalGroupedFullSuccessCreditsAll|TestExternalGroupedPartialPersistsExactTaskState|TestExternalGroupedFailureWithoutReceiptsCreditsNone|TestExternalPartialClaimsMatchTaskState' -count=1` passes
    - Four-of-six state is four completed/two pending and colony state is not BUILT
    - Full success without receipts remains BUILT with all six complete
    - Removing a root artifact between admission and finalization leaves its task pending and excludes its claim/CompletedTaskID
    - A worktree-backed result has no pre-sync CompletedTaskIDs; plan 195-08 consumes its admission object
    - Replaying an identical partial packet cannot duplicate task claims or broaden credit
    - Result diagnostics name both credited and unfinished task IDs
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestExternalGrouped(FullSuccess|Partial|Failure)|TestExternalPartialClaims' -count=1</automated>
  </verify>
  <done>External completion projects exact proof into exact task state, preserving legacy full success and leaving honest durable partial state for retry.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(ExternalTaskReceipt|ExternalGrouped|ExternalPartial|WrapperBundled)' -count=1`
- `go run ./cmd/aether contract-schema --check`
</verification>

<success_criteria>
- Native and external receipt admission/credit semantics match.
- Four-of-six is durable and exact on the wrapper path.
- Legacy whole-success covered_task_ids behavior remains green.
</success_criteria>

<output>
Create `.planning/phases/195-coherent-jobs/195-06-SUMMARY.md` when done.
</output>
