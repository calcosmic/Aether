---
context: phase
phase: 195-coherent-jobs
task: 2
total_tasks: 2
status: paused
last_updated: 2026-08-27T03:58:41.223Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. The constraint below was discovered through an
> interrupted executor with partial production work.

- [ ] **RESUME PLAN 195-03 IN PLACE:** Production commits and an uncommitted
  Task 2 working set exist, but `195-03-SUMMARY.md` does not. Before dispatching
  any executor, inspect the commits and working-tree diff, then continue Task 2
  from the current checkout or explicitly close/supersede the partial work.

**Do not proceed until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Redispatching a partial plan from scratch | Plan 195-03 already has three commits plus an uncommitted Task 2 implementation. A clean redispatch would duplicate Task 1 and omit or overwrite the working set. | blocking | Run the safe-resume checks (`git log --grep=195-03`, summary existence, and `git diff`) and continue Task 2 in the current checkout before any new executor dispatch. |
| Assuming an interrupted agent stopped its child processes | Interrupting the Codex executor left `rtk go test ./cmd`, `go test`, and `cmd.test` running for almost four minutes. | advisory | After any agent interruption, inspect the exact process tree and terminate only the scoped leftover test processes before resuming. |

<current_state>
Phase 195 is paused on branch `oracle-reinstate` at committed HEAD `c722d43a`.
Plans 195-01 and 195-02 are complete with committed summaries. Plan 195-03 is
mid-Task 2: Task 1 RED/GREEN is committed, Task 2 RED is committed, Task 2 GREEN
is partially implemented in the working tree, and no `195-03-SUMMARY.md` exists.

The stalled executor was interrupted at the owner's request. Its leftover Go
test process tree was terminated and no deliberate background service remains.
</current_state>

<completed_work>

- Plan 195-01: coherent-job proposal planner — complete and summarized.
- Plan 195-02: native/external task-receipt wire contract — complete and summarized.
- Plan 195-03 Task 1 RED: `1cfa72ee` — failing coherent-job wiring tests.
- Plan 195-03 Task 1 GREEN: `e4b351d2` — coherent jobs wired into build planning.
- Plan 195-03 Task 2 RED: `c722d43a` — failing grouped-job compatibility tests.
- Wave 1 post-merge build passed. The full suite reached the workflow's
  five-minute timeout without reporting a failure.
</completed_work>

<remaining_work>

1. Inspect the interrupted Task 2 working set before editing or staging:
   - `.aether/schemas/completion-packet.schema.json`
   - `cmd/build_print_brief_test.go`
   - `cmd/codex_build.go`
   - `cmd/codex_build_worktree_test.go`
   - `cmd/codex_continue.go`
   - `cmd/codex_visuals.go`
   - `cmd/dispatch_coalesce_test.go`
   - `cmd/testdata/golden_build.txt`
2. Finish Plan 195-03 Task 2 and run its targeted acceptance tests.
3. Commit Task 2 GREEN, run Plan 195-03 verification, create and commit
   `195-03-SUMMARY.md`, and update GSD tracking.
4. Run the Wave 2 post-merge gate, then execute Plans 195-04 through 195-10.
5. Run code review, regression/schema/drift gates, phase verification, and phase
   completion only after all ten summaries exist.
</remaining_work>

<decisions_made>

- Use GSD pause/resume, not an Aether lifecycle command, because `.planning/`
  owns this execution state.
- Stop the repeatedly stalled Codex executor and hand the partial plan to Claude.
- Keep `workflow.use_worktrees=false` until Plan 195-03's uncommitted working set
  is committed or deliberately superseded. Claude may restore worktrees later.
- Preserve pre-existing user changes and untracked artifacts; stage only the
  exact Plan 195-03 or handoff files intended for each commit.
</decisions_made>

<blockers>

- **Partial-plan safe-resume gate:** production commits exist and the summary is
  missing. This blocks a clean executor redispatch.
- **Long-running test harness:** broad `go test ./cmd` runs exceeded GSD's
  ten-minute no-commit stall window. Prefer targeted Task 2 tests first and use
  an explicit timeout for broader gates.
- No unresolved product decision or external credential blocker is known.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable exact pause state.
2. This `.continue-here.md` — blocking safe-resume rule and working-set map.
3. `.planning/phases/195-coherent-jobs/195-03-PLAN.md` — Task 2 contract and acceptance tests.
4. `.planning/phases/195-coherent-jobs/195-CONTEXT.md` — locked owner decisions.
5. `.planning/phases/195-coherent-jobs/195-RESEARCH.md` and `195-PATTERNS.md` — architecture and existing patterns.
6. `195-01-SUMMARY.md` and `195-02-SUMMARY.md` — contracts already delivered.
7. `git log --oneline --grep=195-03` and `git diff` — actual partial execution state.
8. `AGENTS.md` and `/Users/callumcowie/.codex/RTK.md` — repository operating rules.
9. `.planning/METHODOLOGY.md` if present — project analytical lenses.

## Critical Anti-Patterns (do NOT repeat these)

- **Do not rerun Plan 195-03 from a clean HEAD.** Continue or deliberately close
  the partial plan after inspecting its commits and working tree.
- **Do not assume every dirty file belongs to Task 2.** `.planning/config.json`,
  the pending one-worker-pause todo, `.gsd/`, Phase 194 review artifacts, and the
  parity handoff predated this interrupted task and remain user-owned.
- **Do not use `git reset --hard`, checkout-based rollback, broad cleanups, or
  broad staging.** The checkout intentionally contains unrelated user work.

## Infrastructure State

- Branch: `oracle-reinstate`
- HEAD: `c722d43a` (`test(195-03): add failing grouped job compatibility tests`)
- GSD config: `workflow.use_worktrees=false` (set for Codex safety).
- Running test processes: the specific leftover `go test ./cmd` tree was
  terminated; verify again before resuming.
- No worktrees were created during this Codex execution.
- Plans 195-01 and 195-02 are committed; Plan 195-03 Task 2 edits are uncommitted.

<context>
The intended direction is sound: Plans 195-01/02 established the pure planner
and additive receipt vocabulary, and Plan 195-03 Task 1 made the planner
reachable before waves/worktrees. The immediate job is recovery, not redesign.
Treat the interrupted diff as a candidate Task 2 GREEN implementation: inspect
it, run the exact acceptance tests, repair deliberately, and commit only after
the grouped brief, CalVault, task-credit, and deterministic-identity controls
all pass.
</context>

<next_action>
Start with: run `$gsd-resume-work`, acknowledge the blocking safe-resume
constraint, confirm no leftover `go test` process, then inspect
`git log --oneline --grep=195-03` and `git diff`. Continue Plan 195-03 Task 2 in
place; do not spawn a clean executor while the uncommitted working set exists.
</next_action>
