---
phase: 195-coherent-jobs
plan: 08
type: execute
wave: 6
depends_on: ["195-07"]
files_modified:
  - pkg/codex/dispatch.go
  - cmd/coherent_job_receipts.go
  - cmd/coherent_job_receipts_test.go
  - cmd/codex_build_worktree.go
  - cmd/codex_build_worktree_test.go
autonomous: true
requirements: [JOBS-02, JOBS-03, JOBS-04]

must_haves:
  truths:
    - "The six-batch CalVault fixture produces one worktree, branch, worker session, and merge-back for one grouped job."
    - "Worktree ownership sees the unioned grouped paths, while unrelated jobs that overlap in one wave are still refused."
    - "Structural admission yields candidate worktree sync paths without credit; only after those paths reach root can finalization yield CompletedTaskIDs."
    - "Uncredited edits remain preserved in the orphan/recovery path and never become task credit."
  artifacts:
    - path: "cmd/coherent_job_receipts.go"
      provides: "Shared admission-to-root-finalization contract wired for worktree sequencing"
    - path: "cmd/codex_build_worktree.go"
      provides: "Grouped ownership and receipt-scoped root synchronization"
    - path: "cmd/codex_build_worktree_test.go"
      provides: "Real CalVault full and partial worktree integration regressions"
  key_links:
    - from: "cmd/codex_build_worktree.go"
      to: "cmd/coherent_jobs.go"
      via: "consumes already-grouped dispatch ownership rather than grouping after allocation"
      pattern: "CoveredTaskIDs"
    - from: "cmd/codex_build_worktree.go"
      to: "cmd/coherent_job_receipts.go"
      via: "admission SyncPaths are synchronized before root-evidence finalization can return CompletedTaskIDs"
      pattern: "admitCoherentJobTaskReceipts.*finalizeCoherentJobTaskReceiptEvidence"
---

<objective>
Make coherent jobs and partial receipt truth hold in isolated worktree mode.

Purpose: JOBS-04 is not a flag flip. Grouping must precede ownership so one job
has one worktree, and worktree-only files must never be credited before accepted
changes reach root.
Output: grouped WorkerDispatch metadata, unioned ownership, one-worktree
CalVault proof, and receipt-scoped partial sync/recovery.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-coherent-jobs/195-CONTEXT.md
@.planning/phases/195-coherent-jobs/195-RESEARCH.md
@.planning/phases/195-coherent-jobs/195-PATTERNS.md
@.planning/phases/195-coherent-jobs/195-03-SUMMARY.md
@.planning/phases/195-coherent-jobs/195-04-SUMMARY.md
@.planning/phases/195-coherent-jobs/195-06-SUMMARY.md
@.planning/phases/195-coherent-jobs/195-07-SUMMARY.md
@cmd/coherent_job_receipts.go
@cmd/codex_build_worktree.go
@pkg/codex/dispatch.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run the real CalVault coherent job in one worktree</name>
  <files>pkg/codex/dispatch.go, cmd/codex_build_worktree.go, cmd/codex_build_worktree_test.go</files>
  <read_first>
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — JOBS-04 integration point and CalVault fixture
    - .planning/phases/195-coherent-jobs/195-RESEARCH.md — Pattern 6 full-success paragraph and Pitfall 1
    - .planning/phases/195-coherent-jobs/195-PATTERNS.md — worktree ownership guard and exclusion-removal contract
    - .planning/phases/195-coherent-jobs/195-03-SUMMARY.md — final grouped dispatch metadata and proof grouping already occurs before ownership
    - cmd/codex_build_worktree.go — declaredPathsForTask, validateDeclaredWorktreeOwnership, dispatch conversion/execution, sync/merge/cleanup
    - pkg/codex/dispatch.go — WorkerDispatch and summary contracts
    - cmd/codex_build_worktree_test.go — temp git repository/invoker and overlap/dependency fixtures
  </read_first>
  <behavior>
    - `TestCalVaultSixBatchesBecomeOneWorktreeJob` fails at multiple worktrees/conflict before the adapter change and passes at one after it.
    - One grouped dispatch carries ordered covered IDs, job name/reason, and the union of all six tasks' declared paths into one WorkerDispatch.
    - Exactly one branch/session/worktree is allocated and merged once.
    - Two unrelated jobs in the same wave that overlap a path still fail atomically through validateDeclaredWorktreeOwnership.
  </behavior>
  <action>
Add the literal CalVault worktree integration test first and capture RED. Extend
`codex.WorkerDispatch` additively with covered task IDs and grouped job metadata
needed for worktree identity/diagnostics. Consume the already-grouped dispatch
from plan 195-03; never regroup inside the worktree adapter. Ensure declared
ownership is the unique sorted union before `validateDeclaredWorktreeOwnership`,
so intentional within-job overlap has one owner. Allocate, invoke, sync, merge,
and clean up once for the grouped job. Keep the existing distinct-job overlap
guard and primary TaskID compatibility; enhance errors to name job plus covered
IDs where useful.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestCalVaultSixBatchesBecomeOneWorktreeJob|TestGroupedWorktreeOwnsUnionedPaths|TestDistinctWorktreeJobsStillRejectOverlap' -count=1` passes
    - The CalVault fixture is the same six-batch source-list shape as the in-repo regression and asserts one invocation/worktree/branch/merge
    - WorkerDispatch carries all six ordered IDs and unioned paths
    - The existing unrelated overlap conflict test remains green and still preserves rejected worktrees safely
    - No worktree function calls a second grouping/coalescing algorithm
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(CalVaultSixBatchesBecomeOneWorktreeJob|GroupedWorktreeOwnsUnionedPaths|DistinctWorktreeJobsStillRejectOverlap)' -count=1</automated>
  </verify>
  <done>Worktree mode uses the same pre-wave job plan: CalVault is one worker/worktree and unrelated conflicting jobs remain protected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire admission, partial sync, and root finalization without circular credit</name>
  <files>cmd/coherent_job_receipts.go, cmd/coherent_job_receipts_test.go, cmd/codex_build_worktree.go, cmd/codex_build_worktree_test.go</files>
  <read_first>
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-08 through D-10 in worktree mode
    - .planning/phases/195-coherent-jobs/195-RESEARCH.md — Open Question 1 resolution, Pattern 4's two-stage contract, Pattern 6 partial paragraph, and Pitfall 5
    - .planning/phases/195-coherent-jobs/195-PATTERNS.md — admission-sync-finalization and safe path-I/O patterns
    - .planning/phases/195-coherent-jobs/195-04-SUMMARY.md — actual admission/finalization contracts and native parity semantics
    - .planning/phases/195-coherent-jobs/195-06-SUMMARY.md — external admission transport and deferred worktree finalization seam
    - .planning/phases/195-coherent-jobs/195-07-SUMMARY.md — child retry and immutable parent contract
    - cmd/coherent_job_receipts.go and cmd/coherent_job_receipts_test.go — extend the shared stages for explicit sync success/failure input; do not create a worktree-only validator
    - cmd/codex_build_worktree.go — outcome.touched, accepted/rejected branches, syncWorktreeChangesToRoot, evidence timing, preserveWorktreeWork/orphan tracking
    - cmd/codex_build_worktree_test.go — extend the real temp-git worktree fixtures with partial receipt sync and orphan assertions
  </read_first>
  <behavior>
    - Four structurally admitted candidates expose normalized sync paths but zero CompletedTaskIDs before synchronization.
    - The adapter syncs only admission SyncPaths to root, then shared finalization computes root evidence and credits exactly four tasks.
    - Touched paths outside the admission are not synced or credited and cause the dirty worktree to be preserved with a named diagnostic.
    - If candidate-path sync or root evidence fails, finalization cannot return that task's CompletedTaskID.
    - The child retry contains the two unfinished IDs; root and orphan state make their recovery honest.
  </behavior>
  <action>
Write the partial worktree test before production edits. Derive a normalized
stage-1 admission with `admitCoherentJobTaskReceipts(root, ...)`; `root` is used
only for lexical containment, so candidate files need not exist there yet. It
validates manifest, path, status, summary, and verification structure and
returns candidate claims plus `SyncPaths`, never credit. Partition touched paths against that admission:
sync only candidate paths to root through existing safe path logic, leave all
other edits in the worker checkout, and preserve that checkout as recoverable.
After sync, pass only successfully synchronized candidates to
`finalizeCoherentJobTaskReceiptEvidence`; per D-08/D-09, this shared stage
attaches artifact evidence from root and is the only function allowed to expose
`CompletedTaskIDs` to reconciliation. A sync/evidence failure excludes that task
from finalization output and records the concrete path/error. Use this exact
admission → sync → finalization sequence for native worktree and external
worktree result paths; do not validate against the worker checkout, duplicate
validation, or credit provisional candidates. Do not delete the orphan while
uncredited edits remain. Per D-10, reuse plan 195-07's child retry; do not invent
a worktree-only retry store or mark completion from branch existence.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestGroupedWorktreePartialReceiptsSyncBeforeCredit|TestGroupedWorktreeUncreditedEditsRemainOrphaned|TestGroupedWorktreeSyncFailureCreditsNone|TestGroupedWorktreeRetryContainsOnlyUnfinishedTasks' -count=1` passes
    - A pre-sync assertion proves four candidates and zero CompletedTaskIDs; after sync/finalization, four task paths exist in root before their statuses change
    - Unaccepted touched paths do not exist in root and remain in a tracked orphaned worktree
    - A forced sync failure leaves the task pending and evidence absent
    - Native and external worktree fixtures call the same admission/finalization functions and produce the same credit set
    - Parent/child attempts and exact four-of-six task state match the in-repo/external fixtures
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestGroupedWorktree(PartialReceipts|UncreditedEdits|SyncFailure|Retry)' -count=1</automated>
  </verify>
  <done>Worktree partial credit is root-backed and exact; unproven edits remain recoverable instead of becoming false completion.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(CalVaultSixBatchesBecomeOneWorktreeJob|GroupedWorktree|DistinctWorktreeJobs)' -count=1`
- Compare in-repo and worktree CalVault manifests: same one job, same six covered IDs, one execution owner each.
</verification>

<success_criteria>
- JOBS-04 holds for full and partial grouped jobs.
- Grouping occurs before worktree ownership and allocation.
- Root-backed evidence always precedes task credit.
</success_criteria>

<output>
Create `.planning/phases/195-coherent-jobs/195-08-SUMMARY.md` when done.
</output>
