---
phase: 103-data-flow-artifact-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/103-data-flow-artifact-wiring/DATA-FLOW.md
autonomous: true
requirements: [LIFE-03, DATA-01, DATA-02]

must_haves:
  truths:
    - "Every artifact in .aether/data/ has at least one identified writer function and one identified reader function or consumer"
    - "Artifacts with no production consumer are classified as dead-end, async-write-only, or ghost file"
    - "QUEEN.md, Hive Brain, graph, and survey artifacts have verified colony-prime wiring status"
    - "Colony-prime section names match actual code registration in colony_prime_context.go"
  artifacts:
    - path: ".planning/phases/103-data-flow-artifact-wiring/DATA-FLOW.md"
      provides: "Complete data flow audit report with artifact inventory, wiring status, and findings"
      contains: "## 1. Severity Summary"
    - path: ".planning/phases/103-data-flow-artifact-wiring/DATA-FLOW.md"
      provides: "Dead-end artifact detection"
      contains: "Dead End"
  key_links:
    - from: "DATA-FLOW.md artifact inventory"
      to: "cmd/colony_prime_context.go section names"
      via: "cross-reference"
      pattern: "colony.prime.*section|Section Name"
    - from: "DATA-FLOW.md graph/survey entries"
      to: "cmd/colony_prime_context.go"
      via: "absence verification"
      pattern: "NOT wired.*colony-prime"
---

<objective>
Build the complete data flow audit report (DATA-FLOW.md) by extracting writer/reader functions from Go source code and cross-referencing against colony-prime section registration.

Purpose: Identify every dead-end artifact and verify which data files actually reach dispatched workers through colony-prime injection. This is the core audit deliverable for Phase 103.

Output: DATA-FLOW.md with complete artifact inventory, colony-prime wiring map, context capsule wiring map, and severity-classified findings. No runtime code is modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-data-flow-artifact-wiring/103-CONTEXT.md
@.planning/phases/103-data-flow-artifact-wiring/103-RESEARCH.md
@.planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md

<interfaces>
<!-- Key interfaces the executor needs from the codebase. No exploration required. -->

From cmd/colony_prime_context.go -- Colony-prime section names (the authoritative consumer list):
  Sections registered with name field:
  - "state" (priority 5), "review_depth" (priority 6), "pheromones" (priority 9),
  - "instincts" (priority 6), "decisions" (priority 3), "learnings" (priority 2),
  - "worker_handoffs" (priority 4), "hive_wisdom" (priority 4), "learned_memory" (priority 5),
  - "global_queen_md" (priority 5), "user_preferences" (priority 7), "prior_reviews" (priority 8),
  - "local_queen_wisdom" (priority 5), "clarified_intent" (priority 8), "blockers" (priority 10),
  - "medic_health" (priority 9)
  Total: 16 named sections.

From cmd/context.go -- Context capsule section names (fallback consumer):
  - "state", "signals", "decisions", "risks", "recent_narrative"
  Total: 5 named sections.

From pkg/storage/storage.go -- Storage API (all artifact I/O goes through here):
  Writers: SaveJSON, AtomicWrite, AppendJSONL, UpdateFile, UpdateJSONAtomically
  Readers: LoadJSON, ReadJSONL, ReadFile, LoadRawJSON

From cmd/worker_economy_test.go -- Phase 102 test pattern:
  Golden snapshot file at cmd/testdata/worker_economy_snapshot.json
  4 test functions: TestDispatchedCastesDocumented, TestNoChatOnlyWorkersUndocumented,
    TestVisualOutputTracesToState, TestCasteRegistryConsistency
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete data flow audit report (DATA-FLOW.md)</name>
  <files>.planning/phases/103-data-flow-artifact-wiring/DATA-FLOW.md</files>
  <read_first>
    - cmd/colony_prime_context.go (extract all 16 section name fields and their source file paths)
    - cmd/context.go (extract 5 context capsule section names and source paths, lines 480-575)
    - pkg/storage/storage.go (confirm 6 storage method signatures)
    - .planning/phases/103-data-flow-artifact-wiring/103-RESEARCH.md (complete artifact inventory tables as starting point)
    - .planning/phases/102-worker-economy-visual-ceremony-audit/WORKER-ECONOMY.md (report format reference)
  </read_first>
  <action>
Build the DATA-FLOW.md report with the following sections, following the WORKER-ECONOMY.md report structure from Phase 102.

Per D-01: Audit EVERYTHING -- all files in .aether/data/ plus all hub-level artifacts. Use the research doc's Complete Artifact Inventory (Section 6) as the starting point, but VERIFY each entry against actual source code rather than copying the research tables directly.

Per D-02: For each artifact, document at command + prompt section level:
(1) Writer function: the Go file and function name that writes the artifact (grep for SaveJSON/AtomicWrite/AppendJSONL/UpdateFile/UpdateJSONAtomically with the artifact filename)
(2) Reader function: the Go file and function name that reads the artifact (grep for LoadJSON/ReadJSONL/ReadFile/LoadRawJSON with the artifact filename)
(3) Colony-prime section: which of the 16 section names from colony_prime_context.go injects this data into worker prompts (or "NOT wired" if absent)
(4) Context capsule section: which of the 5 capsule section names injects this data (or "NOT wired" if absent)
(5) User-facing CLI: which CLI commands display this data to the user
(6) Classification: "colony-prime-injected" | "capsule-injected" | "cli-consumed" | "async-pipeline" | "specialized-consumer" | "dead-end" | "ghost-file"

Per D-03: For graph artifacts (codebase-graph.json, instinct-graph.json) and survey artifacts (survey/*.json), explicitly verify wiring. Run grep to confirm these artifact filenames do NOT appear in colony_prime_context.go. Document the specialized consumers they DO have (codegraph_context.go for codebase-graph, loadCodexSurveyContext in codex_plan.go for survey).

Per D-06: Findings must NOT include fix suggestions. Use format: "Finding text. Phase 105 handles remediation."

Section structure for DATA-FLOW.md:

## 1. Severity Summary
Table: Critical count | Warning count | Info count

## 2. Colony-Prime Section Map
Table: Section name | Title | Source File | Priority | Protected status
Extract all 16 section names from colony_prime_context.go.

## 3. Context Capsule Section Map
Table: Section name | Source File
Extract all 5 capsule section names from context.go.

## 4. Core Artifact Inventory (.aether/data/)
Table: Artifact | Writer Functions | Reader Functions | Colony-Prime Section | Capsule Section | User-Facing CLI | Classification | Dead End?

Artifacts to include (per D-01 exhaustive scope):
- COLONY_STATE.json
- pheromones.json
- instincts.json
- pending-decisions.json
- flags.json
- session.json
- handoffs/worker-handoffs.json
- entries.json (learn store)
- midden.json (or midden/midden.json)
- event-bus.jsonl
- behavior-observations.jsonl
- profile.json
- rolling-summary.log
- constraints.json
- assumptions.json
- medic-last-scan.json
- colony.db (SQLite)
- Any other .aether/data/ files discovered during grep extraction

## 5. Survey Artifacts (.aether/data/survey/)
Table: Artifact | Writer | Reader | Colony-Prime? | Classification
- blueprint.json, chambers.json, disciplines.json, provisions.json, pathogens.json
Include the D-03 wiring verification result.

## 6. Graph Artifacts
Table: Artifact | Writer | Reader | Colony-Prime? | Classification
- codebase-graph.json, instinct-graph.json
Include the D-03 wiring verification result.

## 7. Review Artifacts (.aether/data/reviews/)
Table: Artifact | Writer | Reader | Colony-Prime? | Classification
- reviews/{domain}/ledger.json, reviews/_summary_cache.json

## 8. Hub-Level Artifacts (~/.aether/)
Table: Artifact | Writer | Reader | Colony-Prime? | Classification
- QUEEN.md (global + local), hive/wisdom.json, registry/registry.json, eternal/memory.json

## 9. Findings
Severity-classified findings following KNOWN-GAPS.md pattern (Critical/Warning/Info).
Each finding format: [SEVERITY-ID]: Title + description + affected artifacts. No fix suggestions.

Expected findings based on research:
- constraints.json ghost file (medic-flagged, Go code ignores content)
- graph artifacts NOT wired to colony-prime (specialized consumer only)
- survey artifacts NOT wired to colony-prime (specialized consumer only)
- event-bus.jsonl limited consumers (primarily tests and medic scanner)
- Any additional dead-end artifacts discovered during extraction

## 10. Verified Counts
Table: Category | Count | Notes
- Total artifacts inventoried
- Colony-prime injected
- Capsule injected
- CLI consumed
- Async pipeline consumed
- Specialized consumer
- Dead-end / ghost files
  </action>
  <verify>
    <automated>grep -c '## ' .planning/phases/103-data-flow-artifact-wiring/DATA-FLOW.md | grep -v '^0$' && grep -c 'NOT wired' .planning/phases/103-data-flow-artifact-wiring/DATA-FLOW.md</automated>
  </verify>
  <done>
    DATA-FLOW.md exists with all 10 sections. Every artifact in .aether/data/ appears in the inventory. Graph and survey artifacts have explicit wiring verification. At least 3 "NOT wired" entries exist (graph x2, survey x5). Findings use severity classification with no fix suggestions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read-only audit | This plan reads Go source but writes only documentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | I | DATA-FLOW.md findings | accept | Read-only audit -- no code changes to attack surface |
</threat_model>

<verification>
1. DATA-FLOW.md has all 10 sections (grep for "## " headers)
2. Every artifact from RESEARCH.md Complete Artifact Inventory appears in DATA-FLOW.md
3. Graph and survey artifacts verified as NOT wired to colony-prime (grep for "NOT wired" in graph/survey sections)
4. Findings section has severity-classified entries with no fix suggestions
5. Colony-prime section count matches 16 (extracted from colony_prime_context.go)
6. Context capsule section count matches 5 (extracted from context.go)
</verification>

<success_criteria>
- DATA-FLOW.md contains complete artifact inventory with writer/reader tracing for every file in .aether/data/
- Colony-prime section map has 16 entries matching source code
- Graph/survey wiring gaps verified and documented as findings
- constraints.json documented as ghost file
- No fix suggestions in any finding
- Report follows severity classification pattern from Phase 101/102
</success_criteria>

<output>
After completion, create `.planning/phases/103-data-flow-artifact-wiring/103-01-SUMMARY.md`
</output>
