---
phase: 103-data-flow-artifact-wiring
plan: 02
type: execute
wave: 2
depends_on: ["103-01"]
files_modified:
  - cmd/data_flow_audit_test.go
  - cmd/testdata/data_flow_snapshot.json
autonomous: true
requirements: [LIFE-03, DATA-01, DATA-02]

must_haves:
  truths:
    - "Golden snapshot captures every artifact with its writer and reader functions"
    - "Tests verify DATA-FLOW.md claims match the golden snapshot"
    - "Dead-end artifact test flags artifacts with no production consumers"
    - "Colony-prime wiring test verifies section count and artifact coverage"
  artifacts:
    - path: "cmd/data_flow_audit_test.go"
      provides: "4 test functions freezing data flow audit findings"
      exports: ["TestDataFlowSnapshot", "TestDataFlowDeadEnds", "TestDataFlowColonyPrimeWiring", "TestDataFlowReportAccuracy"]
    - path: "cmd/testdata/data_flow_snapshot.json"
      provides: "Golden snapshot of artifact inventory"
      contains: "artifacts"
  key_links:
    - from: "cmd/data_flow_audit_test.go"
      to: ".planning/phases/103-data-flow-artifact-wiring/DATA-FLOW.md"
      via: "report cross-reference"
      pattern: "DATA-FLOW\\.md"
    - from: "cmd/data_flow_audit_test.go"
      to: "cmd/testdata/data_flow_snapshot.json"
      via: "golden file loading"
      pattern: "data_flow_snapshot\\.json"
---

<objective>
Create automated tests that freeze the Phase 103 data flow audit findings, following the Phase 102 golden snapshot + report verification pattern.

Purpose: Ensure DATA-FLOW.md findings remain accurate as the codebase evolves. Tests catch drift when new artifacts are added without writers/readers, or when colony-prime wiring changes.

Output: data_flow_audit_test.go with 4 test functions and data_flow_snapshot.json golden file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-data-flow-artifact-wiring/103-CONTEXT.md
@.planning/phases/103-data-flow-artifact-wiring/DATA-FLOW.md
@cmd/worker_economy_test.go
@cmd/testdata/worker_economy_snapshot.json

<interfaces>
<!-- Phase 102 test pattern to replicate. Executor reads this file for structure. -->

From cmd/worker_economy_test.go:
  Pattern: Load golden JSON file from testdata/, parse into struct, cross-reference against report markdown.
  Test structure: table-driven subtests, report section extraction by header boundaries.
  Golden file: JSON with structured data (caste names, dispatched lists, findings).

From cmd/testdata/worker_economy_snapshot.json:
  Pattern: JSON object with typed arrays for golden snapshot data.
  Fields: documented_castes (array), dispatched_castes (array), chat_only_castes (array), visual_functions (array), registry_map_keys (array).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write data flow audit tests with golden snapshot</name>
  <files>cmd/data_flow_audit_test.go, cmd/testdata/data_flow_snapshot.json</files>
  <read_first>
    - cmd/worker_economy_test.go (replicate this exact test pattern: golden file loading, report cross-referencing, section extraction)
    - cmd/testdata/worker_economy_snapshot.json (replicate this golden file structure)
    - .planning/phases/103-data-flow-artifact-wiring/DATA-FLOW.md (the report being verified -- extract artifact names, findings, section counts)
    - cmd/colony_prime_context.go (verify colony-prime section count matches test expectations)
  </read_first>
  <action>
Per D-05: Create automated tests following the Phase 102 golden snapshot + report verification pattern.

Create cmd/testdata/data_flow_snapshot.json golden file with this structure:
```json
{
  "artifacts": [
    {
      "name": "COLONY_STATE.json",
      "classification": "colony-prime-injected",
      "colony_prime_sections": ["state", "review_depth", "decisions", "learnings"],
      "capsule_sections": ["state"],
      "dead_end": false
    }
  ],
  "colony_prime_section_count": 16,
  "capsule_section_count": 5,
  "dead_end_artifacts": ["constraints.json"],
  "ghost_files": ["constraints.json"],
  "not_wired_to_colony_prime": ["survey/*.json", "codebase-graph.json", "instinct-graph.json"],
  "findings_count": {"critical": 0, "warning": 0, "info": 5}
}
```

Populate the golden file by extracting data from DATA-FLOW.md. Each artifact entry must have: name, classification, colony_prime_sections (array of section names or empty), capsule_sections (array or empty), dead_end (boolean).

Create cmd/data_flow_audit_test.go with 4 test functions:

1. TestDataFlowSnapshot:
   - Load golden file from testdata/data_flow_snapshot.json
   - Verify every artifact in the snapshot appears in DATA-FLOW.md
   - Verify the report's "Verified Counts" section matches the snapshot totals
   - Verify colony_prime_section_count matches grep count of `name:` fields in colony_prime_context.go (exclude the duplicate "prior_reviews" entry around line 323)
   - Acceptable range: 15-17 sections (accounts for potential additions/removals)

2. TestDataFlowDeadEnds:
   - Load golden file
   - For each artifact marked dead_end=true, verify it appears in DATA-FLOW.md Findings section
   - For each artifact in the ghost_files list, verify it appears in DATA-FLOW.md Findings section
   - Verify no artifact classified as "colony-prime-injected" is also in the dead_end list

3. TestDataFlowColonyPrimeWiring:
   - Load golden file
   - For each artifact in not_wired_to_colony_prime list, verify DATA-FLOW.md explicitly states "NOT wired" for that artifact
   - Verify graph artifacts (codebase-graph.json, instinct-graph.json) appear in not_wired_to_colony_prime
   - Verify survey artifacts (survey/*.json pattern) appear in not_wired_to_colony_prime
   - This fulfills D-03: verify actual wiring for graph/survey

4. TestDataFlowReportAccuracy:
   - Load golden file
   - Verify findings_count in snapshot matches the "Severity Summary" table in DATA-FLOW.md
   - Verify the report has no fix suggestions (grep for "fix" | "suggest" | "remediat" in findings section should return 0 results, or only the phrase "Phase 105 handles remediation")
   - Verify colony-prime section map in report has entries matching snapshot's colony_prime_section_count

Follow the exact file structure pattern from cmd/worker_economy_test.go:
- Package: main
- Imports: encoding/json, os, strings, testing
- Helper function to load golden file: loadDataFlowSnapshot(t *testing.T)
- Helper function to read DATA-FLOW.md: readDataFlowReport(t *testing.T) string
- Report path: .planning/phases/103-data-flow-artifact-wiring/DATA-FLOW.md (relative to repo root -- use os.Getwd() to find repo root by walking up to find go.mod)
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && go test ./cmd/ -run TestDataFlow -count=1 -timeout 60s</automated>
  </verify>
  <done>
    cmd/data_flow_audit_test.go exists with 4 test functions. cmd/testdata/data_flow_snapshot.json exists with golden data. All 4 tests pass. Tests cross-reference DATA-FLOW.md against the golden snapshot. Dead-end, ghost, and unwired artifacts are verified by automated tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read-only tests | Tests read source code and report files, write nothing to production paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-02 | I | Test code | accept | Read-only test verification, no security impact |
</threat_model>

<verification>
1. `go test ./cmd/ -run TestDataFlow -count=1` passes all 4 tests
2. cmd/data_flow_audit_test.go has exactly 4 test functions
3. cmd/testdata/data_flow_snapshot.json has artifact entries covering all inventory sections from DATA-FLOW.md
4. Golden file includes dead_end_artifacts list with at least constraints.json
5. Golden file includes not_wired_to_colony_prime list with graph and survey artifacts
</verification>

<success_criteria>
- 4 test functions in data_flow_audit_test.go, all passing
- Golden snapshot captures artifact inventory, classifications, wiring status, and findings count
- Tests verify DATA-FLOW.md accuracy against the golden snapshot
- Graph/survey wiring gaps verified by automated test (D-03)
- Dead-end artifact detection verified by automated test (LIFE-03)
- Report accuracy (findings count, section counts) verified by automated test (DATA-01, DATA-02)
</success_criteria>

<output>
After completion, create `.planning/phases/103-data-flow-artifact-wiring/103-02-SUMMARY.md`
</output>
