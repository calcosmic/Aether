---
phase: 124-host-entry-point
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/host_cmd.go
  - .aether/ts-host/src/host.ts
  - .aether/ts-host/src/go-bridge.ts
autonomous: true
requirements:
  - HEP-01
  - HEP-02
  - HEP-03
  - HEP-04
  - HEP-05
  - HEP-07

must_haves:
  truths:
    - "User can run `aether host lifecycle` and the TS host executes"
    - "User can run `aether host plan`, `build`, `continue`, `oracle` and each delegates to the TS host"
    - "If Node.js is missing, the user gets a clear install message"
    - "If the TS host dist files are missing, the user gets a clear update message"
  artifacts:
    - path: "cmd/host_cmd.go"
      provides: "Go CLI `host` command with lifecycle/plan/build/continue/oracle subcommands"
      exports: ["hostCmd", "hostLifecycleCmd", "hostPlanCmd", "hostBuildCmd", "hostContinueCmd", "hostOracleCmd"]
    - path: ".aether/ts-host/src/host.ts"
      provides: "TS host entry point with oracle command support"
      contains: "case \"oracle\""
    - path: ".aether/ts-host/dist/host.js"
      provides: "Compiled JS output"
      contains: "oracle"
  key_links:
    - from: "cmd/host_cmd.go"
      to: ".aether/ts-host/dist/host.js"
      via: "os/exec Command node <path> <workflow>"
      pattern: "node.*dist/host\.js"
    - from: ".aether/ts-host/src/host.ts"
      to: "cmd/oracle_loop.go"
      via: "go-bridge.ts callGoJSON"
      pattern: "oracle-iterate.*--plan-only"
---

<objective>
Add a stable `aether host <workflow>` entry point that runs the built TypeScript host from installed Aether assets. Implement lifecycle, plan, build, continue, and oracle subcommands in Go, plus minimal oracle support in the TS host. Add fallback messaging when Node.js or host assets are missing.

Purpose: Make the TS host the primary orchestration layer for real user workflows.
Output: `cmd/host_cmd.go`, updated `host.ts` with oracle command, rebuilt `dist/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-124-host-entry-point/124-RESEARCH.md

<interfaces>
From cmd/root.go:
```go
var rootCmd = &cobra.Command{...}
var stdout io.Writer = os.Stdout
var stderr io.Writer = os.Stderr
```

From cmd/compatibility_cmds.go (existing oracle command pattern):
```go
var oracleCmd = &cobra.Command{
  Use:   "oracle [topic|status|stop]",
  Short: "Run the autonomous Oracle RALF research loop",
  Args:  cobra.ArbitraryArgs,
  RunE: func(cmd *cobra.Command, args []string) error { ... },
}
```

From .aether/ts-host/src/host.ts (current commands):
```typescript
case "plan": { ... callGoJSON<PlanCompletion>(bridge, ["plan", "--plan-only", "--depth", "fast"]) ... }
case "build": { ... callGoJSON<BuildManifest>(bridge, ["build", phase, "--plan-only"]) ... }
case "continue": { ... callGoJSON<ContinueCompletion>(bridge, ["continue", "--plan-only"]) ... }
case "lifecycle": { ... runLifecycle(lifecycleOpts) ... }
```

From .aether/ts-host/src/go-bridge.ts:
```typescript
export function callGoJSON<T>(opts: GoBridgeOptions, args: string[]): T;
export function discoverGoBinary(): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `aether host` CLI command with subcommands and fallback checks</name>
  <files>cmd/host_cmd.go</files>
  <read_first>
    - cmd/root.go (Cobra root setup, stdout/stderr writers, skipStoreInit)
    - cmd/compatibility_cmds.go (existing subcommand registration pattern)
    - cmd/publish_cmd.go (outputWorkflow and visual rendering patterns)
    - .aether/ts-host/package.json (engines.node field for version check)
  </read_first>
  <action>
    Create cmd/host_cmd.go with:
    - A `host` parent Cobra command (Use: "host", Short: "Run the TypeScript orchestration host")
    - Five subcommands: `host lifecycle [phase]`, `host plan`, `host build <phase>`, `host continue`, `host oracle [topic]`
    - Each subcommand:
      1. Checks if `node` is in PATH using exec.LookPath("node"). If missing, print to stderr: "Node.js >=20 required. Install from https://nodejs.org/" and return error exit code 1.
      2. Checks if `.aether/ts-host/dist/host.js` exists relative to the current working directory. If missing, print to stderr: "TS host not found. Run `aether update --force` or `aether install`." and return error exit code 1.
      3. Resolves the absolute path to `.aether/ts-host/dist/host.js` using filepath.Abs or filepath.Join with os.Getwd().
      4. Builds argv: `["node", hostJsPath, workflow, ...args]` where workflow is the subcommand name and args are any positional arguments from cobra.
      5. Runs with os/exec Command, streaming stdout and stderr directly to the package-level `stdout` and `stderr` writers (do NOT use os.Stdout/os.Stdout directly; use the package vars so tests can override).
      6. Returns the child process exit code (if non-zero, return renderedErrorExit with that code).
    - Register all commands in `init()` with `rootCmd.AddCommand(hostCmd)`.
    - Use existing patterns from compatibility_cmds.go for flag handling.
    - Do NOT duplicate the full Oracle loop logic; the `host oracle` subcommand simply passes through to the TS host (which will delegate to Go in the next task).
    - Add `host` to `skipStoreInit` in root.go so it does not require a store (the TS host manages its own state).
  </action>
  <verify>
    <automated>go build ./cmd/aether && ./aether host --help 2>&1 | grep -q "Run the TypeScript orchestration host"</automated>
    <automated>./aether host plan --help 2>&1 | grep -q "plan"</automated>
    <automated>./aether host build --help 2>&1 | grep -q "build"</automated>
    <automated>./aether host continue --help 2>&1 | grep -q "continue"</automated>
    <automated>./aether host oracle --help 2>&1 | grep -q "oracle"</automated>
    <automated>./aether host lifecycle --help 2>&1 | grep -q "lifecycle"</automated>
  </verify>
  <done>
    - `cmd/host_cmd.go` exists and compiles
    - `aether host --help` lists all five subcommands
    - Missing `node` produces the exact fallback message and exit code 1
    - Missing `dist/host.js` produces the exact fallback message and exit code 1
    - `host` is added to `skipStoreInit` in root.go
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `oracle` command to TS host and rebuild dist</name>
  <files>.aether/ts-host/src/host.ts, .aether/ts-host/dist/host.js</files>
  <read_first>
    - .aether/ts-host/src/host.ts (current switch statement and parseArgs)
    - .aether/ts-host/src/go-bridge.ts (callGoJSON signature and discoverGoBinary)
    - .aether/ts-host/package.json (build script)
  </read_first>
  <action>
    In `.aether/ts-host/src/host.ts`:
    - Add `"oracle"` to the `printUsage()` command list.
    - Add a new `case "oracle":` in the `switch (command)` block.
    - The oracle case:
      1. Takes an optional topic from `positional[0]`.
      2. Builds Go args: `["oracle", "--plan-only"]` plus the topic if provided.
      3. Calls `callGoJSON<unknown>(bridge, args)`.
      4. Writes JSON result to stdout with `JSON.stringify(result, null, 2) + "\n"`.
    - Run `npm run build` in `.aether/ts-host/` to regenerate `dist/host.js`.
    - Verify the compiled `dist/host.js` contains the string `"oracle"`.
  </action>
  <verify>
    <automated>cd .aether/ts-host && npm run build</automated>
    <automated>grep -q '"oracle"' .aether/ts-host/dist/host.js</automated>
    <automated>node .aether/ts-host/dist/host.js oracle --help 2>&1 | grep -q "Usage:" || true</automated>
  </verify>
  <done>
    - `host.ts` has a working `oracle` case
    - `dist/host.js` is rebuilt and contains the oracle command
    - `node dist/host.js oracle` runs without crashing
  </done>
</task>

<task type="auto">
  <name>Task 3: End-to-end verification of host commands</name>
  <files>cmd/host_cmd.go, .aether/ts-host/dist/host.js</files>
  <read_first>
    - cmd/host_cmd.go (the command just created)
    - .aether/ts-host/dist/host.js (compiled output)
  </read_first>
  <action>
    Run the Go binary and verify each `aether host` subcommand:
    1. Build the binary: `go build -o /tmp/aether-test ./cmd/aether`
    2. From the repo root, run `/tmp/aether-test host plan` and assert it returns exit code 0 and produces valid JSON output (starts with `{`).
    3. Run `/tmp/aether-test host build 1` and assert it returns exit code 0 and produces valid JSON.
    4. Run `/tmp/aether-test host continue` and assert it returns exit code 0 and produces valid JSON.
    5. Run `/tmp/aether-test host oracle` and assert it returns exit code 0 and produces valid JSON.
    6. Run `/tmp/aether-test host lifecycle --simulate --no-dashboard 1` and assert it completes without crashing (exit code may vary based on colony state; just verify it runs and streams output).
    7. Temporarily rename `.aether/ts-host/dist/host.js` to `.aether/ts-host/dist/host.js.bak`, run `/tmp/aether-test host plan`, assert stderr contains "TS host not found", then restore the file.
    8. Verify `go test ./cmd/...` still passes (no regressions from new command).
  </action>
  <verify>
    <automated>go test ./cmd/... -count=1</automated>
    <automated>go build -o /tmp/aether-test ./cmd/aether && /tmp/aether-test host plan | head -1 | grep -q "^\s*{"</automated>
    <automated>/tmp/aether-test host build 1 | head -1 | grep -q "^\s*{"</automated>
    <automated>/tmp/aether-test host continue | head -1 | grep -q "^\s*{"</automated>
    <automated>/tmp/aether-test host oracle | head -1 | grep -q "^\s*{"</automated>
  </verify>
  <done>
    - All five subcommands run through the binary and produce JSON output
    - Missing dist file produces correct fallback message
    - `go test ./cmd/...` passes with no regressions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go CLI -> Node child process | Untrusted input (args) crosses from Go to TS host via argv |
| TS host -> Go binary | TS host calls Go CLI with user-provided args |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Tampering | cmd/host_cmd.go | mitigate | Validate `host.js` path is within repo `.aether/ts-host/` before execution; reject absolute paths or `..` traversal |
| T-124-02 | Denial of Service | cmd/host_cmd.go | mitigate | Set a reasonable timeout on the Node child process (e.g., 5 minutes for plan, 30 minutes for lifecycle) to prevent runaway TS hosts |
| T-124-03 | Information Disclosure | Fallback messages | accept | Low risk — messages only reveal that Node or TS host is missing, which is expected diagnostic info |
| T-124-04 | Elevation of Privilege | Node child process | mitigate | Do NOT run Node with elevated privileges; inherit Go process permissions only |
</threat_model>

<verification>
- `go build ./cmd/aether` succeeds
- `go test ./cmd/...` passes
- `aether host <subcommand>` runs for all five subcommands
- Fallback messages appear correctly when Node or dist is missing
</verification>

<success_criteria>
- HEP-01: `aether host lifecycle` runs the TS host end-to-end
- HEP-02: `aether host plan` delegates to TS host
- HEP-03: `aether host build` delegates to TS host
- HEP-04: `aether host continue` delegates to TS host
- HEP-05: `aether host oracle` delegates to TS host
- HEP-07: Fallback messaging works for missing Node and missing dist
</success_criteria>

<output>
After completion, create `.planning/phases/124-host-entry-point/124-01-SUMMARY.md`
</output>
