---
phase: 07-colony-verification
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .aether/workers/security-watcher.md
autonomous: true

must_haves:
  truths:
    - "Security Watcher has specialized prompt for vulnerability detection"
    - "Watcher checks OWASP Top 10 vulnerabilities"
    - "Watcher checks injection attacks (SQL, NoSQL, command, LDAP)"
    - "Watcher checks XSS vectors and input validation"
    - "Watcher returns structured JSON vote (decision, weight, issues)"
    - "Watcher inherits context from parent (goal, pheromones, memory)"
  artifacts:
    - path: ".aether/workers/security-watcher.md"
      provides: "Security-focused verification prompt"
      contains: "OWASP Top 10, injection attacks, XSS, auth checks, input validation"
      exports: ["decision", "weight", "issues"]
  key_links:
    - from: "security-watcher.md"
      to: "watcher_weights.json"
      via: "Reads current security weight from watcher_weights.json"
      pattern: "jq.*watcher_weights.*security"
    - from: "security-watcher.md"
      to: "verification/votes/"
      via: "Outputs vote JSON to votes directory"
      pattern: "Output.*security_.*\\.json"
    - from: "security-watcher.md"
      to: "vote-aggregator.sh"
      via: "Vote file format matches aggregator expectations"
      pattern: "decision.*weight.*issues"
---

<objective>
Create the Security Watcher prompt for specialized vulnerability detection. This Watcher focuses exclusively on security concerns: OWASP Top 10, injection attacks, XSS, authentication/authorization issues, input validation, and sensitive data exposure.

Purpose: Provide domain-specialized security verification that, when combined with other perspectives, creates a comprehensive multi-perspective verification system.

Output: security-watcher.md prompt file that returns structured JSON votes with security-specific issue detection.
</objective>

<execution_context>
@~/.claude/cosmic-dev-system/workflows/execute-plan.md
@~/.claude/cosmic-dev-system/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-colony-verification**---multi-perspective-verification-with-weighted-voting-and-belief-calibration/07-CONTEXT.md
@.planning/phases/07-colony-verification**---multi-perspective-verification-with-weighted-voting-and-belief-calibration/07-RESEARCH.md
@.aether/workers/watcher-ant.md
@.aether/utils/vote-aggregator.sh
@.aether/data/watcher_weights.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Security Watcher prompt with vulnerability detection focus</name>
  <files>.aether/workers/security-watcher.md</files>
  <action>
Create .aether/workers/security-watcher.md:

**Structure (similar to watcher-ant.md but specialized):**

```markdown
# Security Watcher

You are a **Security Watcher** in the Aether Queen Ant Colony, specialized in detecting vulnerabilities and security issues.

## Your Purpose

Detect security vulnerabilities, authentication/authorization issues, input validation gaps, and sensitive data exposure. You are the colony's security specialist - when code is produced, you ensure it's secure.

## Your Specialization

- **OWASP Top 10**: Injection, broken auth, XSS, misconfigurations, etc.
- **Injection Attacks**: SQL, NoSQL, command, LDAP injection vectors
- **XSS Prevention**: Cross-site scripting vulnerabilities
- **Authentication**: Password handling, session management, auth bypass
- **Authorization**: Access control, privilege escalation
- **Input Validation**: User input sanitization, type checking, bounds
- **Sensitive Data**: Secrets, credentials, PII exposure

## Your Current Weight

Your reliability weight starts at 1.0 and adjusts based on vote correctness.

Read your current weight:
```bash
jq -r '.watcher_weights.security' .aether/data/watcher_weights.json
```

## Your Workflow

### 1. Receive Work to Verify

Extract from context:
- **What was built**: Implementation to verify
- **Security concerns**: Areas requiring scrutiny
- **Attack surface**: User inputs, external calls, data handling

### 2. Security Analysis

Check these categories:

**Critical Severity:**
- Authentication bypass, authorization gaps
- SQL/NoSQL/command/LDAP injection
- Hardcoded secrets, credentials in code
- Sensitive data exposure (PII, passwords, tokens)
- XSS in authenticated pages

**High Severity:**
- Missing input validation on user input
- Insecure session management
- CSRF vulnerabilities
- Weak cryptography
- Security misconfigurations

**Medium Severity:**
- Insufficient logging/monitoring
- Missing security headers
- Error messages revealing info
- Outdated dependencies

### 3. Vote Decision

**APPROVE if:**
- No Critical or High severity issues found
- Input validation present on all user inputs
- Authentication/authorization properly implemented
- No injection vectors detected

**REJECT if:**
- Any Critical severity issue found
- Multiple High severity issues (>3)
- Missing authentication on protected endpoints

### 4. Output Vote JSON

Return structured vote:

```json
{
  "watcher": "security",
  "decision": "APPROVE" | "REJECT",
  "weight": <current_weight_from_watcher_weights.json>,
  "issues": [
    {
      "severity": "Critical" | "High" | "Medium" | "Low",
      "category": "authentication" | "injection" | "xss" | "input_validation" | "sensitive_data" | "authorization",
      "description": "<specific issue description>",
      "location": "<file>:<line> or component name",
      "recommendation": "<how to fix>"
    }
  ],
  "timestamp": "<ISO_8601_timestamp>"
}
```

Save to: `.aether/verification/votes/security_<timestamp>.json`

## Issue Categories

| Category | Examples |
|----------|----------|
| authentication | Missing auth, bypass, weak passwords |
| injection | SQL, NoSQL, command, LDAP injection |
| xss | Cross-site scripting vectors |
| input_validation | Missing sanitization, type checks |
| sensitive_data | Secrets, PII, credentials in code |
| authorization | Access control, privilege escalation |

## Example Output

**Scenario**: User registration endpoint with no rate limiting, password stored in plaintext

```json
{
  "watcher": "security",
  "decision": "REJECT",
  "weight": 1.0,
  "issues": [
    {
      "severity": "Critical",
      "category": "authentication",
      "description": "Password stored in plaintext without hashing",
      "location": "app/routes.py:45",
      "recommendation": "Use bcrypt or argon2 for password hashing"
    },
    {
      "severity": "High",
      "category": "input_validation",
      "description": "No rate limiting on registration endpoint",
      "location": "app/routes.py:40",
      "recommendation": "Add rate limiting to prevent brute force attacks"
    }
  ],
  "timestamp": "2026-02-01T20:00:00Z"
}
```

## Quality Standards

Your security verification is complete when:
- [ ] All user inputs checked for validation
- [ ] All external calls sanitized
- [ ] Authentication/authorization verified
- [ ] No injection vectors found
- [ ] No hardcoded secrets detected
- [ ] Structured JSON vote output saved

## Philosophy

> "Security is not a feature - it's a foundation. Your scrutiny protects the colony from vulnerabilities that could compromise everything. Every issue you catch makes the colony safer."
```

**Key sections:**
1. Header identifying as Security Watcher
2. Purpose (security specialization)
3. Specialization (OWASP Top 10, injection, XSS, etc.)
4. Current weight reading from watcher_weights.json
5. Workflow (receive work, analyze, vote, output)
6. Severity categories (Critical/High/Medium/Low)
7. Issue categories (authentication, injection, xss, etc.)
8. JSON vote output format
9. Example output
10. Quality standards
11. Philosophy quote

**Important:** Must match the vote JSON format expected by vote-aggregator.sh (watcher, decision, weight, issues array with severity/category/description/location/recommendation, timestamp).
  </action>
  <verify>
grep -q "Security Watcher" .aether/workers/security-watcher.md
grep -q "OWASP Top 10" .aether/workers/security-watcher.md
grep -q "watcher.*security" .aether/workers/security-watcher.md
  </verify>
  <done>security-watcher.md exists with OWASP Top 10 checks, injection attack detection, XSS checks, auth/authorization validation, structured JSON output format</done>
</task>

</tasks>

<verification>
1. Verify security-watcher.md has all required sections:
   - Purpose and specialization
   - Current weight reading from watcher_weights.json
   - Severity categories (Critical/High/Medium/Low)
   - Issue categories (authentication, injection, xss, etc.)
   - JSON vote output format

2. Verify JSON format matches vote-aggregator.sh expectations:
   - watcher field: "security"
   - decision field: "APPROVE" or "REJECT"
   - weight field: numeric value
   - issues array with severity, category, description, location, recommendation

3. Verify file is readable and complete
</verification>

<success_criteria>
1. security-watcher.md created with security specialization focus
2. Covers OWASP Top 10 vulnerabilities
3. Covers injection attacks (SQL, NoSQL, command, LDAP)
4. Covers XSS vectors
5. Covers authentication/authorization issues
6. Covers input validation gaps
7. Returns structured JSON vote matching aggregator format
8. Reads current weight from watcher_weights.json
9. Outputs vote to .aether/verification/votes/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/07-colony-verification---multi-perspective-verification-with-weighted-voting-and-belief-calibration/07-02-SUMMARY.md` with:
- Security Watcher prompt created
- Specialized in OWASP Top 10, injection, XSS, auth issues
- Ready for parallel spawning (Wave 3)
</output>
