---
phase: 164-research-feeds-planning
plan: 09
type: execute
wave: 4
depends_on: [04, 05, 07]
files_modified:
  - cmd/codex_plan.go
  - cmd/plan_depth_proposal_manifest_test.go
  - cmd/plan_wrapper_cards_test.go
  - .claude/commands/ant/plan.md
  - .opencode/commands/ant/plan.md
  - .aether/commands/plan.yaml
autonomous: true
requirements: [RESEARCH-01, RESEARCH-09, RESEARCH-10]

must_haves:
  truths:
    - "Running /ant-plan presents exactly two decision moments, both tap-to-approve"
    - "The depth card shows granularity, task decomposition depth and verification depth together with a reason each"
    - "The research batch card lists every phase with the Queen's recommendation and reason"
    - "Autopilot accepts both cards automatically, records the acceptance and prints it in the run log"
    - "Claude and OpenCode wrappers stay in lockstep"
  artifacts:
    - path: ".claude/commands/ant/plan.md"
      provides: "The two-decision-moment plan ceremony"
      contains: "Decision Moment 2"
    - path: "cmd/plan_wrapper_cards_test.go"
      provides: "Cross-platform parity invariant for the two cards"
      contains: "TestPlanWrapperCardsParity"
  key_links:
    - from: "cmd/codex_plan.go"
      to: "computeDepthProposal"
      via: "the plan-only result carries depth_proposal and depth_proposal_card in both branches"
      pattern: "computeDepthProposal\\("
    - from: ".claude/commands/ant/plan.md"
      to: "aether plan-research-approve"
      via: "the research batch card's approve and flip actions"
      pattern: "plan-research-approve"
---

<objective>
Put both decision moments in front of the user: one depth proposal card and one
research batch card, both tap-to-approve, both rendered from runtime-computed
strings, on Claude and OpenCode alike.

Purpose: RESEARCH-09 and RESEARCH-10 require the three depth controls to be
reachable and explained at plan time with a plain-English reason each;
RESEARCH-01 requires the research decision to be visible and overridable. D-14
caps the flow at exactly two decision moments.
Output: the depth proposal on the manifest, both cards in both wrappers and the
YAML source, and a parity test that fails when the two wrappers drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-research-feeds-planning/164-CONTEXT.md
@.planning/phases/164-research-feeds-planning/164-RESEARCH.md
@.planning/phases/164-research-feeds-planning/164-PATTERNS.md
@.planning/phases/164-research-feeds-planning/164-04-SUMMARY.md
@.planning/phases/164-research-feeds-planning/164-05-SUMMARY.md
</context>

<interfaces>
<!-- Delivered by Plan 04 (cmd/plan_depth_proposal.go). -->
  func computeDepthProposal(state colony.ColonyState, granularity colony.PlanGranularity,
      planningDepth, verificationDepth string,
      planningSmartDefault, verificationSmartDefault bool) depthProposal
  func renderDepthProposalCard(p depthProposal) string

<!-- Delivered by Plan 05 (cmd/codex_plan.go, cmd/phase_research_decision_cmd.go). -->
  Result/manifest keys already present: research_proposal, research_proposal_card,
  research_awaiting_approval, research_warning
  CLI verb: aether plan-research-approve [--approve-all] [--flip <ids>] [--auto]
    -> outputs { approved, skipped, overrides, log_line }

<!-- Delivered by Plan 07. -->
  Finalize result keys: research_failed_phases, research_warning

<!-- Existing in cmd/codex_plan.go:812-1000 (runCodexPlanPlanOnly). -->
  granularity, planDepth, planningDepth, verificationDepth are all resolved
  before both return branches; planningSmartDefault / verificationSmartDefault
  are computed at lines 824-825. Two branches must both gain the new keys:
  the existing-plan early return (line ~837) and the fresh-plan result (line ~963).

<!-- Wrapper facts. -->
  .claude/commands/ant/plan.md and .opencode/commands/ant/plan.md are 141 lines
  each and currently mirror one another. The sections being replaced are
  "## Depth Ceremony" (lines 11-22) and "## Planning Depth" (lines 24-30).
  .aether/commands/plan.yaml is the source of truth; its wrapper_additions keys
  depth_ceremony and planning_depth_ceremony back those two sections.
  Per .aether/docs/wrapper-runtime-ux-contract.md the wrapper prints
  runtime-emitted strings; it does not compose its own recommendations.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The manifest carries the depth proposal</name>
  <files>cmd/codex_plan.go, cmd/plan_depth_proposal_manifest_test.go</files>
  <read_first>
    - cmd/codex_plan.go lines 812-1000 (`runCodexPlanPlanOnly` — both result branches and the `codexPlanManifest` literal)
    - cmd/plan_depth_proposal.go (Plan 04's `computeDepthProposal` and `renderDepthProposalCard`)
    - cmd/codex_plan.go lines 200-260 (the `codexPlanManifest` struct and its json tags)
    - .planning/phases/164-research-feeds-planning/164-CONTEXT.md decisions D-13, D-14
  </read_first>
  <behavior>
    - The fresh-plan result map contains depth_proposal with three knobs and a non-empty depth_proposal_card
    - The existing-plan early-return result map contains the same two keys
    - The proposal's recommended values equal the granularity, planning depth and verification depth the same result already reports
    - The card's accept line carries those same three values
    - When the user supplied --planning-depth explicitly, that knob's reason says it was selected explicitly rather than reporting a smart default
    - The plan_manifest carries the proposal too, so the wrapper can read it from either place
  </behavior>
  <action>
    In `runCodexPlanPlanOnly` (cmd/codex_plan.go), build the proposal once, after
    `granularity`, `planningDepth`, `verificationDepth`, `planningSmartDefault`
    and `verificationSmartDefault` are all resolved (they are all in scope by
    line 826, before the existing-plan branch):

      proposal := computeDepthProposal(state, granularity, planningDepth,
          verificationDepth, planningSmartDefault, verificationSmartDefault)
      proposalCard := renderDepthProposalCard(proposal)

    Add `depth_proposal` and `depth_proposal_card` to BOTH result maps — the
    existing-plan early return around line 837 and the fresh-plan result around
    line 963 — and add matching fields to `codexPlanManifest` with the same json
    tags so the wrapper can read them from `result.plan_manifest` as well as from
    the top-level result.

    Do not change how granularity or either depth is resolved. This task only
    surfaces the reasoning that already drives those values; the resolution logic
    (`resolvePlanningDepthSmart`, `resolveVerificationDepthSmart`) is untouched.

    Add `cmd/plan_depth_proposal_manifest_test.go` with
    `TestPlanManifestCarriesDepthProposal` covering the six behaviours, asserting
    both branches.
  </action>
  <verify>
    <automated>go test ./cmd/... -run 'TestPlanManifestCarriesDepthProposal|TestDepthProposal' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/codex_plan.go` contains `computeDepthProposal(` and `renderDepthProposalCard(`
    - `grep -c 'depth_proposal' cmd/codex_plan.go` returns 4 or more (two keys in two branches, plus manifest tags)
    - The existing-plan result map and the fresh-plan result map both contain `depth_proposal` and `depth_proposal_card`
    - The test asserts the proposal's recommended granularity equals the result's `granularity` value
    - `go test ./cmd/... -count=1` exits 0
    - `go build ./cmd/aether` exits 0
  </acceptance_criteria>
  <done>Both plan-only response shapes carry the three-knob proposal and its rendered card.</done>
</task>

<task type="auto">
  <name>Task 2: Two decision moments in both wrappers and the YAML source</name>
  <files>.claude/commands/ant/plan.md, .opencode/commands/ant/plan.md, .aether/commands/plan.yaml</files>
  <read_first>
    - .claude/commands/ant/plan.md (whole file, 141 lines — especially lines 11-31 for the sections being replaced, lines 55-61 for the clarification gate, lines 72-93 for the worker-spawning choreography that must stay accurate, and lines 121-141 for the drift guard and guardrails)
    - .opencode/commands/ant/plan.md (whole file — confirm it currently mirrors the Claude wrapper before editing)
    - .aether/commands/plan.yaml (whole file — `wrapper_additions.depth_ceremony`, `wrapper_additions.planning_depth_ceremony`, `guardrails`)
    - .aether/docs/wrapper-runtime-ux-contract.md (wrappers present, runtime owns truth)
    - .planning/phases/164-research-feeds-planning/164-CONTEXT.md decisions D-01, D-13, D-14, D-15, D-16 and the <specifics> section
  </read_first>
  <action>
    Replace the two static ceremonies with two runtime-driven cards in
    `.claude/commands/ant/plan.md`, then mirror the identical changes into
    `.opencode/commands/ant/plan.md` and update `.aether/commands/plan.yaml`,
    which is the source of truth both wrappers are generated against.

    Delete the `## Depth Ceremony` and `## Planning Depth` sections. In their
    place add `## Decision Moment 1 — Depth Proposal`, which instructs the
    orchestrator to:
      1. Request a first manifest with `aether host plan` and NO depth flags, so
         Go computes its smart defaults and their reasons.
      2. Print `result.depth_proposal_card` verbatim. Do not restate, summarise
         or re-reason the recommendations — the reasons are computed by the
         runtime and the wrapper prints them (wrapper-runtime UX contract).
      3. Accept the recommendations on a single confirmation, or, when the user
         names a knob and an option number, request a fresh manifest with the
         corresponding `--depth` / `--planning-depth` / `--verification-depth`
         flag set. Never ask the user to type a value.

    After the manifest section and before worker spawning, add
    `## Decision Moment 2 — Research Batch`, which instructs the orchestrator to:
      1. Print `result.research_proposal_card` verbatim when it is non-empty.
      2. Approve the batch with `aether plan-research-approve --approve-all`, or
         flip specific phases with `aether plan-research-approve --flip <ids>`.
      3. Request a fresh manifest afterwards so the gated `phase_research`
         dispatches appear, and surface `result.research_warning` whenever
         `result.research_awaiting_approval` is true — a plan that skipped
         research because nobody answered must say so.
      4. Note the fast-depth behaviour: on a fast run the Queen recommends skip
         for every phase, the batch still appears, and a flipped-on phase
         researches at the fast preset's 80% / 4-iteration budget.

    Add an autopilot paragraph to both cards: under `/ant-run`, accept the depth
    recommendations without prompting and answer the research batch with
    `aether plan-research-approve --auto`, then print the returned `log_line` in
    the run log. Autopilot never pauses for either card.

    Keep the existing worker-spawning choreography (lines 72-93) accurate — it
    already describes wave-1 research Scouts running before the wave-2
    Route-Setter, which remains true. Add one sentence noting that research
    Scouts now iterate under a confidence loop and that their per-iteration
    confidence lines come from the runtime.

    Add three guardrails to both wrappers and to `plan.yaml`'s `guardrails` list:
      - the plan flow has exactly two decision moments; do not add a third
      - do not route either card through `aether discuss`; the discuss redirect
        remains only for `orchestrator_boundary_guidance`
      - do not compose depth recommendations, reasons or research
        recommendations in the wrapper; print the runtime-emitted card

    In `plan.yaml`, replace the `depth_ceremony` and `planning_depth_ceremony`
    entries under `wrapper_additions` with `depth_proposal_card` and
    `research_batch_card` entries carrying the same instructions, and update the
    `runtime.command` line to include `--verification-depth`. Update the
    `iterative_planning_ceremony` entry to mention that `--target` and
    `--max-iterations` now also bound per-phase research iteration.

    The Cross-Platform Drift Guard section at the bottom of the Claude wrapper
    already names `.aether/commands/plan.yaml`, `cmd/command_guide.go` and the
    Codex skill — verify `aether command-guide plan --platform codex` still
    describes a matching flow after the edit, and update
    `cmd/command_guide.go` only if it contradicts the new two-card flow.
  </action>
  <verify>
    <automated>go run ./cmd/aether command-guide plan --platform codex</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/commands/ant/plan.md` contains `## Decision Moment 1 — Depth Proposal` and `## Decision Moment 2 — Research Batch`
    - `.claude/commands/ant/plan.md` contains `depth_proposal_card`, `research_proposal_card`, `research_awaiting_approval` and `plan-research-approve`
    - `grep -c '^## Depth Ceremony$' .claude/commands/ant/plan.md` returns 0
    - `.opencode/commands/ant/plan.md` contains the identical two section headings and the identical four runtime keys
    - `.aether/commands/plan.yaml` contains `depth_proposal_card` and `research_batch_card` under `wrapper_additions` and no longer contains `planning_depth_ceremony`
    - `.aether/commands/plan.yaml` `runtime.command` contains `--verification-depth`
    - Both wrappers contain the `--auto` autopilot instruction
    - `go run ./cmd/aether command-guide plan --platform codex` exits 0
  </acceptance_criteria>
  <done>Both wrappers present exactly two runtime-rendered, tap-to-approve cards, and the YAML source matches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Parity invariant for the two cards</name>
  <files>cmd/plan_wrapper_cards_test.go</files>
  <read_first>
    - .claude/commands/ant/plan.md and .opencode/commands/ant/plan.md (post-edit state from Task 2)
    - .aether/ts-host/test/cross-platform-parity.test.ts (the existing parity-checking approach, for reference on what is already covered so this test does not duplicate it)
    - CLAUDE.md section "Definition of Done" — prefer an invariant assertion over a named-section-exists assertion
  </read_first>
  <behavior>
    - The ordered list of level-2 headings in the Claude wrapper is identical to the ordered list in the OpenCode wrapper
    - Both wrappers reference all four runtime result keys the cards depend on
    - Neither wrapper still contains the retired "Depth Ceremony" or "Planning Depth" headings
    - Both wrappers contain exactly one instruction to run plan-research-approve with --approve-all and one with --auto
    - plan.yaml's wrapper_additions contains a card entry for each of the two decision moments
    - The test fails if either wrapper gains a heading the other lacks
  </behavior>
  <action>
    Create `cmd/plan_wrapper_cards_test.go` with `TestPlanWrapperCardsParity`.

    Read `.claude/commands/ant/plan.md`, `.opencode/commands/ant/plan.md` and
    `.aether/commands/plan.yaml` from the repository root (locate the root by
    walking up from the test's working directory until `go.mod` is found, the
    same way other repo-file tests in this package do — read one and copy its
    approach rather than hard-coding a relative path).

    Assert the invariant, not just the presence of named sections: extract every
    line beginning with `## ` from each wrapper, in order, and require the two
    slices to be deeply equal. A test that only checks for "Decision Moment 2"
    cannot catch a future rename or a one-sided addition; an ordered-heading-set
    equality assertion catches both, which is what this repository's Definition
    of Done asks for.

    Then assert the four runtime keys (`depth_proposal_card`,
    `research_proposal_card`, `research_awaiting_approval`, `research_warning`)
    appear in both wrappers, that neither contains `## Depth Ceremony` or
    `## Planning Depth`, and that `plan.yaml` contains both card entries under
    `wrapper_additions`.

    When counting occurrences of a token in the wrapper files, filter out
    HTML-comment lines first (the wrappers open with an
    `<!-- Aether-managed ... -->` banner) so a comment mentioning a token cannot
    satisfy an assertion about instructions.
  </action>
  <verify>
    <automated>go test ./cmd/... -run 'TestPlanWrapperCardsParity' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/plan_wrapper_cards_test.go` contains `TestPlanWrapperCardsParity`
    - The test asserts deep equality of the ordered `## ` heading slices from the two wrappers
    - The test asserts all four runtime keys appear in both wrappers
    - The test asserts neither wrapper contains `## Depth Ceremony` or `## Planning Depth`
    - `go test ./cmd/... -run TestPlanWrapperCardsParity -count=1` exits 0
    - `go test ./... -count=1` exits 0
  </acceptance_criteria>
  <done>A one-sided wrapper edit fails the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user selection -> CLI flags | The tapped knob option becomes a `--depth` / `--planning-depth` / `--verification-depth` argument |
| user selection -> decision resolution | The tapped batch action becomes a `plan-research-approve` invocation with phase IDs |
| runtime string -> terminal | Cards are runtime-computed strings printed by the wrapper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-25 | Tampering | depth values reaching CLI flags | mitigate | The card only ever offers values from the fixed option sets; `resolvePlanningDepthSmart` and `resolveVerificationDepthSmart` already reject unknown values with an error, which is the V5 structured-selection control from 164-RESEARCH.md's security domain |
| T-164-26 | Spoofing | a wrapper inventing a recommendation or reason the runtime did not compute | mitigate | Both cards are runtime-rendered strings printed verbatim, a new guardrail forbids the wrapper composing its own recommendations, and `TestPlanWrapperCardsParity` pins that both wrappers reference the runtime keys rather than static prose |
| T-164-27 | Repudiation | autopilot silently choosing depth and research scope | mitigate | D-16 requires `--auto` to record every acceptance through `pending-decisions.json` (Plan 05) and to print the returned `log_line`; the wrapper instruction to print that line is asserted by the parity test |
</threat_model>

<verification>
- `go test ./... -count=1` passes
- `go build ./cmd/aether` succeeds
- `go run ./cmd/aether command-guide plan --platform codex` exits 0
- `npm --prefix .aether/ts-host run test:all` passes
</verification>

<success_criteria>
- Both plan-only result branches carry `depth_proposal` and `depth_proposal_card`
- Both wrappers present exactly two decision moments, with identical ordered heading sets
- Both cards are printed from runtime keys, not composed by the wrapper
- Autopilot answers both cards via `--auto` and prints the log line
- `.aether/commands/plan.yaml` matches the wrappers
</success_criteria>

<output>
After completion, create `.planning/phases/164-research-feeds-planning/164-09-SUMMARY.md`
</output>
