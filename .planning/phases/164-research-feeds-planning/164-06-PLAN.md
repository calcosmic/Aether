---
phase: 164-research-feeds-planning
plan: 06
type: execute
wave: 2
depends_on: [03]
files_modified:
  - .aether/ts-host/src/host.ts
  - .aether/ts-host/test/research-confidence-loop.test.ts
  - .aether/ts-host/dist
autonomous: true
requirements: [RESEARCH-07, RESEARCH-08]

must_haves:
  truths:
    - "A live plan run prints a confidence line per research iteration while research is still running"
    - "Research iterates until the depth's target is met or its iteration budget runs out"
    - "A phase that reaches target stops iterating while a phase still below target keeps going"
    - "The existing ConfidenceLoop class drives the iteration — no fourth loop is written"
    - "A stalled or near-target phase prompts for early accept instead of nagging every iteration"
  artifacts:
    - path: ".aether/ts-host/src/host.ts"
      provides: "runResearchConfidenceLoop — the research construction site for ConfidenceLoop"
      contains: "new ConfidenceLoop("
    - path: ".aether/ts-host/test/research-confidence-loop.test.ts"
      provides: "Behavioural and construction-site tests for the research loop"
  key_links:
    - from: ".aether/ts-host/src/host.ts"
      to: ".aether/ts-host/src/confidence-loop.ts"
      via: "runResearchConfidenceLoop constructs one ConfidenceLoop per approved research phase"
      pattern: "new ConfidenceLoop\\("
    - from: ".aether/ts-host/src/host.ts"
      to: ".aether/ts-host/src/research-confidence.ts"
      via: "researchLoopOptions binds depth to target/iterations; ResearchConfidenceEvaluator scores each artifact"
      pattern: "researchLoopOptions|ResearchConfidenceEvaluator"
---

<objective>
Give the plan path what only the build path has today: a real confidence loop.
Research Scouts iterate until their depth-bound target is met, with a visible
ceremony line every iteration and an early-accept prompt when progress stalls.

Purpose: RESEARCH-07 and RESEARCH-08 are unmet today for one specific reason —
`runDispatchedPlanCommand` constructs zero `ConfidenceLoop` instances. The class
exists, is tested and is correct; the gap is the construction site.
Output: `runResearchConfidenceLoop` in `host.ts`, called from the plan pipeline
before the Route-Setter runs, plus a rebuilt `dist/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-research-feeds-planning/164-CONTEXT.md
@.planning/phases/164-research-feeds-planning/164-RESEARCH.md
@.planning/phases/164-research-feeds-planning/164-PATTERNS.md
@.planning/phases/164-research-feeds-planning/164-03-SUMMARY.md
</context>

<interfaces>
<!-- Delivered by Plan 03 (.aether/ts-host/src/research-confidence.ts). -->

  export function researchLoopPreset(depth: string): ResearchLoopPreset
  export function researchLoopOptions(depth: string, totalBudget: number): ConfidenceLoopOptions
  export class ResearchConfidenceEvaluator {
    evaluate(input: { markdown: string; repoRoot: string; selfAssessedGaps?: number })
      : EvaluatedResearchConfidence   // { score, sectionsFilled, citationsVerified, filesVerified, gaps, ... }
  }

<!-- Existing host.ts surface. -->

  runDispatchedPlanCommand(bridge, parsed)            // host.ts:979-1052, today single-shot
  function renderIterationCeremony(result: ConfidenceResult): void   // host.ts:641, writes via emitCeremonyOutput
  function renderIterationComplete(stopReason: string): void         // host.ts:655
  let _dispatchWorkersRef = dispatchWorkers;          // host.ts:82, swappable for tests (host.ts:91/96)
  function toWorkerDispatches(dispatches): WorkerDispatch[]          // host.ts:429
  ParsedHostArgs carries: depth, targetConfidence, maxIterations, accept, simulate
  export { runDispatchedBuildCommand, runDispatchedPlanCommand, ... }  // host.ts:128

  Loop-shape reference to mirror: runDispatchedBuildCommand, host.ts:842-920.

<!-- The artifact each research Scout writes (cmd/phase_research.go:124). -->

  .aether/data/phase-research/phase-<ID>-research.md
  Research dispatches are identified by stage === "phase_research"; their
  task_id is "plan-research-phase-<ID>" and outputs[0] is the filename above.

<!-- Do NOT modify confidence-loop.ts (RESEARCH-07). Do NOT add a third copy of
     renderIterationCeremony — it already exists twice (host.ts:641 free
     function, confidence-loop.ts:158 instance method). -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runResearchConfidenceLoop — one ConfidenceLoop per approved phase</name>
  <files>.aether/ts-host/src/host.ts</files>
  <read_first>
    - .aether/ts-host/src/host.ts lines 640-680 (`renderIterationCeremony`, `renderIterationComplete`, `WaveResult`)
    - .aether/ts-host/src/host.ts lines 783-973 (`runDispatchedBuildCommand` — the loop shape to mirror exactly)
    - .aether/ts-host/src/host.ts lines 979-1052 (`runDispatchedPlanCommand` — the function this plan restructures)
    - .aether/ts-host/src/host.ts lines 420-450 (`toWorkerDispatches`) and lines 78-100 (`_dispatchWorkersRef` and its mock hooks)
    - .aether/ts-host/src/confidence-loop.ts lines 200-250 (`checkStopConditions` priority order and `isDiminishingReturns`)
    - .aether/ts-host/src/research-confidence.ts (Plan 03's module)
    - .planning/phases/164-research-feeds-planning/164-CONTEXT.md decisions D-09, D-10, D-12
    - .planning/phases/164-research-feeds-planning/164-RESEARCH.md "Open Questions" 2 (per-phase instantiation is the recommended reading)
  </read_first>
  <behavior>
    - Given two research dispatches and a scorer that returns 95 for phase 1 and 60 for phase 2 at balanced depth, phase 1 stops after one iteration and phase 2 keeps iterating
    - The number of dispatch rounds for a never-improving phase equals the depth's maxIterations (balanced = 6), never more
    - A ceremony line is emitted for every phase on every iteration and contains the word "confidence", the phase number and the Scout's name
    - Passing accept=true stops every phase's loop after its first iteration with stop reason "accepted"
    - When a phase's stop reason is "diminishing_returns" and its confidence is below target, an early-accept prompt line is emitted exactly once for that phase
    - When a phase's confidence is within 5 of target with iterations remaining, the early-accept prompt line is emitted exactly once for that phase
    - A plan run with zero research dispatches performs zero dispatch rounds and emits no ceremony lines
  </behavior>
  <action>
    Add an exported async function to `.aether/ts-host/src/host.ts`:

      export async function runResearchConfidenceLoop(
        bridge: GoBridgeOptions,
        parsed: ParsedHostArgs,
        researchDispatches: PlanDispatchLike[],
      ): Promise<ResearchLoopSummary>

    with `ResearchLoopSummary` reporting, per phase: phaseId, iterations,
    finalConfidence, stopReason, and a top-level `escalations: number[]` field
    (left empty by this plan; Plan 08 populates it).

    Implementation, mirroring `runDispatchedBuildCommand`'s shape:

    1. Return an empty summary immediately when `researchDispatches` is empty.
    2. Build `loopOpts` from `researchLoopOptions(parsed.depth ?? "balanced",
       spawnBudget)` (D-12). Honour explicit overrides exactly as the build path
       does: when `parsed.targetConfidence` is set, override `confidenceTarget`;
       when `parsed.maxIterations` is set, override `maxIterations`. The depth
       preset is the default, the flags are the override.
    3. Create a `Map<number, ConfidenceLoop>` keyed by phase ID with one
       `new ConfidenceLoop(loopOpts)` per research dispatch, plus a single shared
       `new ResearchConfidenceEvaluator()`. Per-phase instantiation is required:
       D-10's prompt copy and D-04's "escalate that one phase" both operate on a
       single phase's progress, not a batch average.
    4. Loop: dispatch the still-active research dispatches via
       `_dispatchWorkersRef` with `toWorkerDispatches`, workflow "plan". After
       each round, for each active phase read
       `.aether/data/phase-research/phase-<ID>-research.md` under `bridge.cwd`
       (treat a missing file as empty markdown), score it with
       `evaluator.evaluate({ markdown, repoRoot: bridge.cwd, selfAssessedGaps })`
       — derive `selfAssessedGaps` from the worker result's claimed blockers/gaps
       count when present, otherwise 0 — then call that phase's
       `loop.evaluate(score, 1)`.
    5. Render the ceremony for every phase every iteration (D-09) by calling the
       existing free function `renderIterationCeremony`. Extend that function
       with an optional second parameter `prefix?: string` appended before
       "Iteration"; pass `${dispatch.name} phase ${id}` as the prefix. Extending
       the existing duplication is required — do not write a third copy of the
       ceremony renderer, and do not change its output format for the build path
       (calling it without a prefix must produce a byte-identical line).
    6. Remove a phase from the active set when `shouldContinue` is false, and
       call `renderIterationComplete(stopReason)` for it once.
    7. Early accept (D-10): after each `loop.evaluate`, emit a single prompt line
       for a phase when either its stopReason is "diminishing_returns" while
       `currentConfidence < confidenceTarget`, or `currentConfidence >=
       confidenceTarget - 5` with iterations still remaining. The line states the
       current confidence, the per-iteration delta and the two choices, for
       example: "phase 3 research at 87%, gaining 2%/iteration — accept now or
       keep digging? (re-run with --accept to accept)". Emit it at most once per
       phase — track a per-phase flag. The loop is non-interactive: when
       `parsed.accept` is true, stop every phase after its current iteration with
       stopReason "accepted". No per-iteration nagging.

    Then restructure `runDispatchedPlanCommand`: after the ceremony render and
    before dispatching the remaining workers, partition `dispatches` into those
    with `stage === "phase_research"` and the rest. When the research partition
    is non-empty, await `runResearchConfidenceLoop(bridge, parsed, researchPart)`
    first, then dispatch only `rest` through the existing
    `_dispatchWorkersRef` path. This preserves today's ordering contract — all
    wave-1 research completes before the wave-2 Route-Setter — while giving
    research its own iteration budget. Merge the research worker results into
    `mappedResults` so the completion packet still reports every dispatched
    worker, and include the loop summary in the stdout JSON under an
    `research_iterations` key.

    Export `runResearchConfidenceLoop` from the existing test-only export list at
    host.ts:128.
  </action>
  <verify>
    <automated>npm --prefix .aether/ts-host run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'new ConfidenceLoop(' .aether/ts-host/src/host.ts` returns 2 or more (build path plus research path)
    - `.aether/ts-host/src/host.ts` contains `export async function runResearchConfidenceLoop`
    - `.aether/ts-host/src/host.ts` contains `researchLoopOptions(` and `new ResearchConfidenceEvaluator()`
    - `grep -c 'function renderIterationCeremony' .aether/ts-host/src/host.ts` returns exactly 1 (no third copy added)
    - `git diff --stat .aether/ts-host/src/confidence-loop.ts` is empty
    - `runDispatchedPlanCommand` partitions on `stage === "phase_research"` and awaits the research loop before dispatching the rest
    - `npm --prefix .aether/ts-host run typecheck` exits 0
  </acceptance_criteria>
  <done>The plan pipeline constructs a ConfidenceLoop per approved research phase, iterates each to its depth-bound target, and prints a ceremony line every iteration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Loop tests and dist rebuild</name>
  <files>.aether/ts-host/test/research-confidence-loop.test.ts, .aether/ts-host/dist</files>
  <read_first>
    - .aether/ts-host/test/confidence-loop.test.ts (the node:test style and assertion conventions)
    - .aether/ts-host/test/host-flags.test.ts or another test that mocks `_dispatchWorkersRef` via the exported mock hooks — read whichever exists to copy the mocking setup
    - .aether/ts-host/src/host.ts (the function written in Task 1)
    - CLAUDE.md section "Definition of Done" (prefer tests asserting an invariant over tests asserting a section exists)
  </read_first>
  <behavior>
    - A fake dispatcher records dispatch rounds; a never-improving phase at balanced depth produces exactly 6 rounds and no more
    - A phase scored at or above the depth target stops after one round while a sibling below target continues
    - Ceremony output captured from stderr contains one line per phase per iteration and includes the phase number and the word "confidence"
    - accept=true produces exactly one round per phase with stopReason "accepted"
    - The early-accept prompt appears at most once per phase across the whole run
    - An invariant test asserts host.ts constructs at least two ConfidenceLoop instances across the file, so removing the research construction site fails the suite
    - Zero research dispatches produce zero dispatch rounds
  </behavior>
  <action>
    Create `.aether/ts-host/test/research-confidence-loop.test.ts` using
    `node:test` and `node:assert/strict`.

    Drive `runResearchConfidenceLoop` directly with a fake dispatcher installed
    through host.ts's existing dispatch-mock hook, and control the confidence
    score by writing real `phase-N-research.md` fixture files into a temp repo
    root between rounds (empty file -> low score, fully-evidenced file -> high
    score). Scoring through the real `ResearchConfidenceEvaluator` rather than a
    stub keeps the test honest about the Plan 03 contract.

    Capture ceremony output by intercepting the stderr write used by
    `emitCeremonyOutput`, following whatever pattern the existing ceremony tests
    in this directory already use.

    Include the construction-site invariant test: read
    `.aether/ts-host/src/host.ts` from disk, strip comment lines
    (`grep -v` equivalent: filter lines whose trimmed form starts with `//` or
    `*`), and assert the count of `new ConfidenceLoop(` occurrences is at least
    2. This is the test that fails if a future change deletes the research
    construction site — the exact regression 164-RESEARCH.md identifies as the
    unmet part of RESEARCH-07.

    After the tests pass, rebuild the embedded bundle with
    `npm --prefix .aether/ts-host run build` and commit the regenerated files
    under `.aether/ts-host/dist/` — `dist` is `//go:embed`-ed and tracked in git,
    so a source change without a rebuild ships stale behaviour.
  </action>
  <verify>
    <automated>npm --prefix .aether/ts-host exec -- tsx --test .aether/ts-host/test/research-confidence-loop.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.aether/ts-host/test/research-confidence-loop.test.ts` exists and its suite exits 0
    - The suite includes an assertion that `new ConfidenceLoop(` appears at least twice in `src/host.ts` after comment lines are filtered out
    - The suite asserts exactly 6 dispatch rounds for a never-improving balanced-depth phase
    - `npm --prefix .aether/ts-host run build` exits 0
    - `git status --porcelain .aether/ts-host/dist` shows regenerated output staged or modified
    - `npm --prefix .aether/ts-host run test:all` exits 0
    - `go build ./cmd/aether` exits 0 (the embed still resolves)
  </acceptance_criteria>
  <done>The research loop's iteration count, stop behaviour, ceremony output and construction site are all pinned by tests, and the embedded dist matches the source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker-written markdown -> loop control flow | `phase-N-research.md` content determines whether more workers are spawned |
| CLI flags -> iteration budget | `--target` and `--max-iterations` override the depth preset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-16 | Denial of Service | unbounded research iteration | mitigate | Every phase's loop is constructed with the depth preset's `maxIterations` (4/6/8/12) and the shared `totalBudget` from the manifest spawn budget; `ConfidenceLoop.checkStopConditions` enforces `max_iterations_met` and `budget_exhausted` before any other condition |
| T-164-17 | Tampering | `--target 99 --max-iterations 12` supplied to force a costly run | mitigate | Both flags are already parsed and range-documented host-side (`--target <n> 70-99`, `--max-iterations <n> 2-12` per host.ts usage text); clamp the applied values to those documented ranges before writing them into `ConfidenceLoopOptions` rather than trusting the raw parse |
| T-164-18 | Information Disclosure | ceremony lines echoing research content to the terminal | accept | Ceremony lines carry only the phase number, worker name, confidence percentage, delta and budget — never research body text; this matches the existing build-path ceremony contract |
</threat_model>

<verification>
- `npm --prefix .aether/ts-host run typecheck` passes
- `npm --prefix .aether/ts-host run test:all` passes
- `npm --prefix .aether/ts-host run build` succeeds and `dist/` is regenerated
- `go build ./cmd/aether` succeeds
- `git diff --stat .aether/ts-host/src/confidence-loop.ts` is empty
</verification>

<success_criteria>
- `host.ts` constructs `new ConfidenceLoop(` in the research path as well as the build path
- A never-improving balanced-depth phase iterates exactly 6 times
- A phase reaching target stops while a sibling below target continues
- Ceremony lines appear per phase per iteration and carry confidence and delta
- `--accept` short-circuits every phase after one iteration
- `confidence-loop.ts` is unmodified
</success_criteria>

<output>
After completion, create `.planning/phases/164-research-feeds-planning/164-06-SUMMARY.md`
</output>
