---
phase: 164-research-feeds-planning
plan: 05
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - cmd/phase_research_decision_cmd.go
  - cmd/phase_research.go
  - cmd/codex_plan.go
  - cmd/phase_research_dispatch_test.go
autonomous: true
requirements: [RESEARCH-01, RESEARCH-02, RESEARCH-06]

must_haves:
  truths:
    - "A plan manifest carries the Queen's research proposal and its rendered batch card"
    - "Research Scouts are dispatched only for phases the user approved"
    - "A user flip in either direction is recorded in pending-decisions.json"
    - "No new JSON store file is created for research decisions"
    - "A plan run whose research batch was never answered says so loudly instead of silently skipping research"
  artifacts:
    - path: "cmd/phase_research_decision_cmd.go"
      provides: "The plan-research-approve CLI verb"
      contains: "plan-research-approve"
    - path: "cmd/phase_research.go"
      provides: "Approval-gated research dispatch"
      contains: "approved map[int]bool"
  key_links:
    - from: "cmd/codex_plan.go"
      to: "computePhaseResearchProposal"
      via: "runCodexPlanPlanOnly builds the proposal, persists unresolved decisions and gates dispatch on the resolved ones"
      pattern: "computePhaseResearchProposal\\("
    - from: "cmd/phase_research_decision_cmd.go"
      to: "pending-decisions.json"
      via: "store.LoadJSON / store.SaveJSON on the existing PendingDecisionFile"
      pattern: "pendingDecisionsFile"
---

<objective>
Wire the Queen's research decision to the dispatch: the manifest proposes,
`aether plan-research-approve` records the answer in the existing decision
store, and only approved phases get a research Scout.

Purpose: RESEARCH-01 (decision + reason + two-way override), RESEARCH-02
(research runs automatically when warranted) and RESEARCH-06 (no new planning
store) all land here. Today `plannedPhaseResearchDispatches` runs
unconditionally with no decision recorded anywhere.
Output: one runnable command that answers the batch, a manifest that carries the
proposal, and dispatch gating with a loud warning when nobody answered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-research-feeds-planning/164-CONTEXT.md
@.planning/phases/164-research-feeds-planning/164-RESEARCH.md
@.planning/phases/164-research-feeds-planning/164-PATTERNS.md
@.planning/phases/164-research-feeds-planning/164-02-SUMMARY.md
</context>

<interfaces>
<!-- Delivered by Plan 02 (cmd/phase_research_decision.go). Use directly. -->

  const phaseResearchDecisionType = "research-decision"
  type phaseResearchRecommendation struct { PhaseID int; PhaseName, Recommend, Reason string; Hints phaseResearchHint }
  type phaseResearchProposal struct { Depth string; Replan bool; Phases []phaseResearchRecommendation }

  func computePhaseResearchProposal(planDepth string, replan bool,
      survey codexSurveyContext, candidates []phaseResearchCandidate,
      phases []colony.Phase) phaseResearchProposal
  func renderPhaseResearchProposalBlock(p phaseResearchProposal) string
  func newPhaseResearchDecision(rec phaseResearchRecommendation) PendingDecision
  func phaseResearchDecisionResolution(rec phaseResearchRecommendation, flipped bool, auto bool) string
  func resolvePhaseResearchDecisions(file PendingDecisionFile) map[int]string

<!-- Delivered by Plan 01 (cmd/phase_research.go). Signature this plan extends again. -->

  func plannedPhaseResearchDispatches(root, planDepth, goal string,
      candidates []phaseResearchCandidate, survey codexSurveyContext,
      reresearch bool) []codexPlanningDispatch

<!-- Existing store surface (cmd/pending_decision.go) — reuse, do not extend. -->

  const pendingDecisionsFile = "pending-decisions.json"
  type PendingDecisionFile struct { Decisions []PendingDecision }
  store.LoadJSON(pendingDecisionsFile, &file) / store.SaveJSON(pendingDecisionsFile, file)
  func loadCurrentPendingDecisionScope() ...
  func stampPendingDecisionScope(d *PendingDecision, scope ...)
  func pendingDecisionMatchesScope(d PendingDecision, scope ...) bool

<!-- Test harness helpers already available in package cmd tests. -->

  saveGlobals(t), resetRootCmd(t), newTestStore(t) -> (store, tmpDir)
  Pattern reference: cmd/autopilot_pause_test.go lines 11-30
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The plan-research-approve command</name>
  <files>cmd/phase_research_decision_cmd.go, cmd/phase_research_dispatch_test.go</files>
  <read_first>
    - cmd/pending_decision.go (whole file, 217 lines — the three existing verbs, their flag registration, scope stamping and `outputOK`/`outputError` conventions)
    - cmd/phase_research_decision.go (delivered by Plan 02 — the record builders and `resolvePhaseResearchDecisions`)
    - cmd/autopilot_pause_test.go lines 1-40 (the `saveGlobals` / `resetRootCmd` / `newTestStore` harness and `rootCmd.SetArgs` invocation style)
    - .planning/phases/164-research-feeds-planning/164-CONTEXT.md decisions D-01, D-03, D-06, D-16
  </read_first>
  <behavior>
    - `plan-research-approve --approve-all` resolves every unresolved research-decision with the Queen's recommendation and reports the approved and skipped phase IDs
    - `plan-research-approve --flip 3` resolves phase 3's decision with the opposite direction and a resolution containing "user overrode"
    - `--flip` accepts a comma-separated list and flips each named phase while approving the rest
    - `--auto` approves everything, prefixes each resolution with "auto-accepted (autopilot)" and returns a single log line naming the researched phase IDs
    - Running the command with no unresolved research-decisions returns a non-error result reporting zero phases, not a crash
    - After any invocation, `pending-decisions.json` is the only file the command wrote — no new JSON file appears in the store directory
  </behavior>
  <action>
    Create `cmd/phase_research_decision_cmd.go` registering a new cobra command
    `plan-research-approve` on `rootCmd`, following the exact structure of the
    three verbs in `cmd/pending_decision.go`: nil-store guard with
    `outputErrorMessage`, `store.LoadJSON(pendingDecisionsFile, &file)`,
    mutate, `store.SaveJSON`, then `outputOK` with a result map.

    Flags:
      --approve-all   bool    accept the Queen's recommendation for every phase
      --flip          string  comma-separated phase IDs to flip
      --auto          bool    autopilot: approve all and mark the records auto-accepted

    Behaviour: load `PendingDecisionFile`, select unresolved decisions whose
    `Type == phaseResearchDecisionType` and which match
    `pendingDecisionMatchesScope(d, loadCurrentPendingDecisionScope())`. For each
    one, reconstruct its recommendation from the stored `Description`
    (`newPhaseResearchDecision` wrote it as
    "<recommend> phase <id> (<name>): <reason>") and determine whether that phase
    ID appears in the parsed `--flip` set. Set `Resolved = true`, `ResolvedAt` to
    RFC3339 UTC, `Resolution` to
    `phaseResearchDecisionResolution(rec, flipped, auto)`, and re-stamp the scope
    with `stampPendingDecisionScope`.

    When neither `--approve-all`, `--flip` nor `--auto` is supplied, treat it as
    `--approve-all` — the card's default action is acceptance and the user should
    never be forced to type a flag to agree with the Queen.

    Emit `outputOK` with keys `approved` ([]int, phases that will be researched),
    `skipped` ([]int), `overrides` ([]int, the flipped IDs) and `log_line` (a
    single string of the form "auto-accepted: research phases 2,4" under `--auto`
    or "research phases 2,4; skipping 1,3" otherwise). D-16 requires autopilot to
    print this line in the run log.

    Do not add a new JSON file, a new struct for storage, or a new store path.
    RESEARCH-06 is explicit and the "no new store" property is asserted below.

    Add `TestPlanResearchApproveRecordsDecisions` to
    `cmd/phase_research_dispatch_test.go` covering the six behaviours, using the
    `saveGlobals` / `resetRootCmd` / `newTestStore` harness. Include the
    no-new-store assertion: snapshot the store directory's file list before the
    command runs and assert the after-list adds nothing beyond
    `pending-decisions.json`.
  </action>
  <verify>
    <automated>go test ./cmd/... -run 'TestPlanResearchApproveRecordsDecisions' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/phase_research_decision_cmd.go` registers a cobra command whose `Use` is `plan-research-approve`
    - The command declares the flags `--approve-all`, `--flip` and `--auto`
    - `go run ./cmd/aether plan-research-approve --help` exits 0 and lists all three flags
    - After `--flip 3`, the phase-3 decision in `pending-decisions.json` has `resolved: true` and a `resolution` containing `user overrode`
    - After `--auto`, every resolution string starts with `auto-accepted (autopilot)` and the result `log_line` contains `auto-accepted:`
    - The test asserts the store directory gains no file other than `pending-decisions.json`
    - `go test ./cmd/... -run TestPlanResearchApproveRecordsDecisions -count=1` exits 0
  </acceptance_criteria>
  <done>A user (or autopilot) can answer the whole research batch with one command, and both directions of every decision are durable in the existing store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The manifest proposes and records; unanswered batches warn loudly</name>
  <files>cmd/codex_plan.go, cmd/phase_research_dispatch_test.go</files>
  <read_first>
    - cmd/codex_plan.go lines 812-1000 (`runCodexPlanPlanOnly` — both the existing-plan early-return branch and the main manifest-building branch)
    - cmd/phase_research_decision.go (Plan 02's proposal computation and record builders)
    - cmd/pending_decision.go lines 32-84 (the store load/append/save shape to mirror)
    - .planning/phases/164-research-feeds-planning/164-CONTEXT.md decisions D-01, D-06, D-08
    - .planning/phases/160-fail-loudly/160-CONTEXT.md (gate-vs-enrichment classification: research is enrichment, so this must warn, never block)
  </read_first>
  <behavior>
    - The plan-only result map carries `research_proposal` with one entry per candidate phase and `research_proposal_card` as a non-empty rendered string whenever candidates exist
    - Building the manifest writes one unresolved research-decision per candidate into pending-decisions.json
    - Re-running the manifest supersedes the previous run's unresolved research-decisions rather than accumulating duplicates for the same phase
    - When no research-decision is resolved yet, the result carries `research_awaiting_approval: true` and a non-empty `research_warning`
    - When decisions are resolved, `research_awaiting_approval` is false and the warning is empty
    - A `fast` depth run still emits the proposal and the card so the user can flip a phase on
  </behavior>
  <action>
    In `runCodexPlanPlanOnly` (cmd/codex_plan.go), immediately after
    `candidates := phaseResearchCandidates(state, iterationSeed)` is available
    and `survey` is loaded, build the proposal:

      proposal := computePhaseResearchProposal(planDepth, opts.Refresh, survey,
          candidates, state.Plan.Phases)

    Persist it as unresolved decisions: load `PendingDecisionFile`, drop every
    existing decision with `Type == phaseResearchDecisionType` that is still
    unresolved and in scope (a fresh manifest re-proposes — D-06 says overrides
    are not sticky and the batch is re-shown each replan), append
    `newPhaseResearchDecision(rec)` for each recommendation with
    `stampPendingDecisionScope` applied, and save. Resolved decisions from the
    current run must be left intact so the wrapper can fetch a second manifest
    after answering the card without losing the answer.

    Read back the resolved directions with
    `resolvePhaseResearchDecisions(file)` and compute
    `approved := map[int]bool{}` where the direction is "research".

    Add to both the manifest struct (as new `codexPlanManifest` fields with
    json tags `research_proposal`, `research_proposal_card`,
    `research_awaiting_approval`, `research_warning`) and the returned result
    map, using the same key names. Set `research_awaiting_approval` to true when
    the proposal has at least one phase and `resolvePhaseResearchDecisions`
    returned no entry for any of them. Set `research_warning` in that case to a
    sentence of the form "research batch not answered — no phase research will
    run; answer with `aether plan-research-approve --approve-all`". Research is
    enrichment, not a gate (Phase 160 classification): never return an error and
    never block manifest generation on an unanswered batch.

    Also add `research_proposal` / `research_proposal_card` to the existing-plan
    early-return branch (the `len(state.Plan.Phases) > 0 && !opts.Refresh` path
    around line 827) so the wrapper can render the card there too.

    Add `TestPlanManifestCarriesResearchProposal` and
    `TestPlanManifestWarnsWhenResearchBatchUnanswered` to
    `cmd/phase_research_dispatch_test.go` covering the six behaviours.
  </action>
  <verify>
    <automated>go test ./cmd/... -run 'TestPlanManifestCarriesResearchProposal|TestPlanManifestWarnsWhenResearchBatchUnanswered' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/codex_plan.go` contains `computePhaseResearchProposal(` and `resolvePhaseResearchDecisions(`
    - `cmd/codex_plan.go` declares manifest fields with json tags `research_proposal`, `research_proposal_card`, `research_awaiting_approval` and `research_warning`
    - The plan-only result map contains all four keys in both the existing-plan and fresh-plan branches
    - A manifest built twice for the same candidates yields exactly one unresolved research-decision per phase, not two
    - `runCodexPlanPlanOnly` returns a nil error when the batch is unanswered
    - `go test ./cmd/... -run 'TestPlanManifest' -count=1` exits 0
  </acceptance_criteria>
  <done>The manifest proposes research per phase, records the proposal as unresolved decisions, and states loudly when nobody has answered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dispatch only the approved phases</name>
  <files>cmd/phase_research.go, cmd/codex_plan.go, cmd/phase_research_dispatch_test.go</files>
  <read_first>
    - cmd/phase_research.go lines 57-100 (`plannedPhaseResearchDispatches` as extended by Plan 01)
    - cmd/codex_plan.go lines 895-915 (the dispatch call site and the Route-Setter brief append block)
    - cmd/phase_research_dispatch_test.go (all existing call sites needing the new argument)
    - .planning/phases/164-research-feeds-planning/164-CONTEXT.md decisions D-01, D-15
  </read_first>
  <behavior>
    - With approved = {2: true} and candidates 1..3, exactly one dispatch is emitted and its TaskID is plan-research-phase-2
    - With an empty approved map, zero dispatches are emitted
    - With a nil approved map, zero dispatches are emitted (nobody answered yet — the manifest's warning covers this)
    - Approval does not override the reresearch rule: an approved phase with worker-authored research on disk and reresearch=false is still skipped
    - Approval does not override the fast-depth rule in either direction: a fast run with an approved phase DOES dispatch that phase, because D-15 requires a flipped-on phase to actually run
    - The dispatch shape (stage, caste, wave, outputs, deterministic name) is unchanged for approved phases
  </behavior>
  <action>
    Add an `approved map[int]bool` parameter to `plannedPhaseResearchDispatches`
    as the final argument. Inside the candidate loop, `continue` when
    `!approved[candidate.ID]`.

    Change the early `if planDepth == "fast"` guard: it must no longer return nil
    unconditionally. D-15 requires that a phase the user flipped ON during a fast
    run actually runs its research (at the fast preset's 80%/4 budget). Replace
    the depth check with the approval check alone — the "fast leans skip"
    behaviour now lives entirely in `computePhaseResearchProposal`'s
    recommendations (Plan 02), which is where the user can see and override it.
    Keep the `len(candidates) == 0` early return.

    Update the doc comment to state the three independent gates in order:
    approval (this plan), re-research (Plan 01), candidate availability.

    At the call site in `runCodexPlanPlanOnly`, pass the `approved` map computed
    in Task 2.

    Update `TestPlanFastPresetSkipsPhaseResearch` to reflect the new contract:
    a fast run with no approvals dispatches nothing, and a fast run with one
    approved phase dispatches exactly that phase. Rename it to
    `TestFastPresetSkipsUnlessUserFlipsResearchOn` so the name states the rule.

    Add `TestResearchDispatchGatedOnApproval` covering the six behaviours, and
    update every remaining call site in `cmd/phase_research_dispatch_test.go` to
    pass an approvals map.
  </action>
  <verify>
    <automated>go test ./cmd/... -run 'TestResearchDispatchGatedOnApproval|TestFastPresetSkipsUnlessUserFlipsResearchOn|TestPhaseResearch|TestPlanEmitsPhaseResearchDispatchesFromDraft|TestReplanReResearchesPhases' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/phase_research.go` contains `approved map[int]bool` in the `plannedPhaseResearchDispatches` signature
    - `grep -v '^\s*//' cmd/phase_research.go | grep -c 'planDepth == "fast"'` returns 0
    - `cmd/codex_plan.go` passes the approvals map into `plannedPhaseResearchDispatches`
    - `TestFastPresetSkipsUnlessUserFlipsResearchOn` exists and asserts both the no-approval and flipped-on cases
    - `go test ./cmd/... -count=1` exits 0
    - `go build ./cmd/aether` exits 0
  </acceptance_criteria>
  <done>Research Scouts spawn only for phases the user approved, in every depth, and nothing spawns before the batch is answered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flag input -> decision resolution | `--flip` carries user-supplied phase IDs that select which durable records are mutated |
| stored decision -> dispatch gate | A resolution string in pending-decisions.json decides whether a worker spawns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-13 | Tampering | `--flip` phase ID parsing | mitigate | Parse each comma-separated token with `strconv.Atoi` and match it only against the phase IDs present in the current unresolved research-decision set; unknown or unparsable IDs are reported in the result and never create a record, matching the V5 structured-selection control in 164-RESEARCH.md's security domain |
| T-164-14 | Elevation of Privilege | a stale decision from a different goal/session gating this run's dispatch | mitigate | Every read and write goes through `pendingDecisionMatchesScope` / `stampPendingDecisionScope`, the existing scope guard `pendingDecisionResolveCmd` already enforces; out-of-scope decisions are ignored rather than honoured |
| T-164-15 | Denial of Service | unanswered batch silently disabling all research | mitigate | `research_awaiting_approval` plus a non-empty `research_warning` are emitted on the manifest result, and Plan 09's wrapper must surface them; the failure is loud, matching the project's Definition of Done rather than a silent skip |
</threat_model>

<verification>
- `go test ./cmd/... -count=1` passes
- `go build ./cmd/aether` succeeds
- `go run ./cmd/aether plan-research-approve --help` exits 0
- `go vet ./cmd/...` passes
</verification>

<success_criteria>
- `aether plan-research-approve` exists, runs, and answers the whole batch in one invocation
- A flip in either direction is durable in `pending-decisions.json` with a resolution naming the override
- No JSON file other than `pending-decisions.json` is created by the flow
- Research dispatches are emitted only for approved phase IDs
- An unanswered batch produces a warning on the manifest result and never an error
</success_criteria>

<output>
After completion, create `.planning/phases/164-research-feeds-planning/164-05-SUMMARY.md`
</output>
