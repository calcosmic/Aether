---
phase: 164-research-feeds-planning
plan: 07
type: execute
wave: 3
depends_on: [05]
files_modified:
  - cmd/codex_plan.go
  - cmd/codex_plan_finalize.go
  - cmd/phase_research_dispatch_test.go
autonomous: true
requirements: [RESEARCH-03]

must_haves:
  truths:
    - "The Route-Setter's brief carries the actual research findings, not just a path to read"
    - "A brief carrying research for many phases stays within a bounded size"
    - "A phase approved for research whose worker produced nothing is named in the plan artifact and the finalize output"
    - "A failed research worker never blocks planning"
  artifacts:
    - path: "cmd/codex_plan.go"
      provides: "Research content injected into the Route-Setter brief and failure marking in the fallback template"
      contains: "routeSetterResearchBudgetChars"
    - path: "cmd/codex_plan_finalize.go"
      provides: "research_failed_phases and research_warning on the finalize result"
      contains: "research_failed_phases"
  key_links:
    - from: "cmd/codex_plan.go"
      to: "resolvePhaseResearchSection"
      via: "Route-Setter brief append reuses the existing truncation helper instead of new logic"
      pattern: "resolvePhaseResearchSection\\(root,"
---

<objective>
Close the half of RESEARCH-03 that is not already true: the planner receives the
research content itself, and a research worker that produced nothing is named
out loud rather than papered over with a template.

Purpose: `resolvePhaseResearchSection` already injects research into *build*
briefs correctly. The Route-Setter — the actual planner — gets one pointer
sentence telling it to go read files. RESEARCH-03 says findings are "injected
into the planner's context, not pasted by hand". D-08 requires a failed research
worker to be loud, and to never block planning.
Output: research content in the Route-Setter brief under a shared budget, and
`research_failed_phases` / `research_warning` on the finalize result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-research-feeds-planning/164-CONTEXT.md
@.planning/phases/164-research-feeds-planning/164-RESEARCH.md
@.planning/phases/164-research-feeds-planning/164-PATTERNS.md
@.planning/phases/164-research-feeds-planning/164-05-SUMMARY.md
</context>

<interfaces>
<!-- Existing helpers to reuse. Do not write new truncation logic. -->

cmd/phase_research.go:141-170
  const phaseResearchBriefBudgetChars = 3500
  func resolvePhaseResearchSection(root string, phaseID int) string
  // Reads .aether/data/phase-research/phase-<ID>-research.md, returns "" when
  // absent, prefixes "## Phase Research", truncates at a paragraph boundary
  // beyond budget and appends "_(truncated — full research: ...)_".

cmd/phase_research.go:15
  const phaseResearchTemplateMarker = "no dedicated research worker ran for this phase"
  func hasWorkerAuthoredResearch(path string) bool

cmd/codex_plan.go:3102
  func writePhaseResearchArtifacts(root, dir string, survey codexSurveyContext,
      report codexScoutReport, phases []colony.Phase,
      snapshots map[string]codexArtifactSnapshot,
      dispatches []codexPlanningDispatch) ([]string, int, error)
  // Preserves worker-authored files via shouldPreserveWorkerArtifact; otherwise
  // writes the six-section fallback template carrying phaseResearchTemplateMarker.
  // Called from cmd/codex_plan.go:582 and cmd/codex_plan_finalize.go:384.

cmd/codex_plan.go:900-910
  The Route-Setter brief append block currently adds only the pointer sentence
  beginning "Parallel research Scouts are writing per-phase findings to".

cmd/codex_plan.go:641
  Finalize already iterates dispatches filtering on
  `dispatch.Stage != phaseResearchStage || !strings.EqualFold(dispatch.Caste, "scout")`
  — the same filter identifies which phases were approved for research.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route-Setter brief carries research content, not a pointer</name>
  <files>cmd/codex_plan.go, cmd/phase_research_dispatch_test.go</files>
  <read_first>
    - cmd/codex_plan.go lines 890-915 (the Route-Setter brief append block — the exact site Pitfall 4 identifies)
    - cmd/phase_research.go lines 138-170 (`resolvePhaseResearchSection` and its budget/truncation behaviour)
    - cmd/phase_research_dispatch_test.go lines 110-154 (`TestBuildBriefCarriesPhaseResearch`, `TestPhaseResearchSectionIsBounded` — the assertion style to mirror)
    - .planning/phases/164-research-feeds-planning/164-RESEARCH.md "Pitfall 4"
  </read_first>
  <behavior>
    - With phase-2-research.md on disk containing a Recommended Approach naming cmd/retry.go, the route_setter dispatch brief contains that text
    - The route_setter brief still contains the existing pointer sentence so the worker can read the full file when it needs more than the excerpt
    - With research on disk for three phases, all three excerpts appear, each headed by its phase number
    - With oversized research for many phases, the total appended research stays within the shared budget and the brief names which phases were reduced to a pointer only
    - With no research on disk, the brief is byte-identical to today's pointer-only output
    - Only the route_setter dispatch's brief is modified; scout and phase_research briefs are untouched
  </behavior>
  <action>
    In `runCodexPlanPlanOnly` (cmd/codex_plan.go), extend the
    `if len(researchDispatches) > 0` block so the Route-Setter brief carries
    content in addition to the existing pointer sentence.

    Declare `const routeSetterResearchBudgetChars = 12000` next to the existing
    `phaseResearchBriefBudgetChars` usage. For each research candidate in
    ascending phase order, call `resolvePhaseResearchSection(root, candidate.ID)`
    — reuse it rather than writing new truncation logic; it already truncates at
    a paragraph boundary and appends a pointer to the full file. Append each
    non-empty section under a per-phase heading naming the phase number and name,
    accumulating until the running total would exceed
    `routeSetterResearchBudgetChars`. Phases beyond the budget are not appended;
    instead add one closing line naming those phase numbers and stating their
    research must be read from `.aether/data/phase-research/`.

    Keep the existing pointer sentence exactly as it is — it remains correct and
    is what covers the first iteration, when the research Scouts have not written
    anything yet. Content injection is additive: on iteration 1 the sections are
    empty and today's behaviour is preserved byte-for-byte; on later iterations
    and on replans the freshly written findings are in the brief.

    Modify only dispatches whose `Caste == "route_setter"`, matching the existing
    loop.

    Add `TestRouteSetterBriefIncludesResearchContent` and
    `TestRouteSetterBriefResearchIsBounded` to
    `cmd/phase_research_dispatch_test.go` covering the six behaviours.
  </action>
  <verify>
    <automated>go test ./cmd/... -run 'TestRouteSetterBriefIncludesResearchContent|TestRouteSetterBriefResearchIsBounded' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/codex_plan.go` contains `routeSetterResearchBudgetChars`
    - `cmd/codex_plan.go` calls `resolvePhaseResearchSection(root,` inside the Route-Setter brief append block
    - The route_setter brief for a run with research on disk contains both the pointer sentence and the research body text
    - The route_setter brief for a run with no research on disk is unchanged from today's output
    - `TestRouteSetterBriefResearchIsBounded` asserts the appended research length stays at or below `routeSetterResearchBudgetChars` plus one closing line
    - `go test ./cmd/... -run TestRouteSetterBrief -count=1` exits 0
  </acceptance_criteria>
  <done>The planner reads the research in its brief; the pointer remains as a fallback for the full file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A failed research worker is named, and planning proceeds</name>
  <files>cmd/codex_plan.go, cmd/codex_plan_finalize.go, cmd/phase_research_dispatch_test.go</files>
  <read_first>
    - cmd/codex_plan.go lines 3102-3156 (`writePhaseResearchArtifacts` — the fallback template path and `phaseResearchTemplateMarker`)
    - cmd/codex_plan_finalize.go lines 340-400 (the finalize call into `writePhaseResearchArtifacts` and the surrounding result assembly) and lines 455-470 (`phase_research_dir` / `phase_research_files` result keys)
    - cmd/codex_plan_finalize.go line 641 area (the existing `dispatch.Stage != phaseResearchStage` filter that identifies approved research dispatches)
    - cmd/phase_research.go lines 12-15 and 91-100 (`phaseResearchTemplateMarker`, `hasWorkerAuthoredResearch`)
    - .planning/phases/164-research-feeds-planning/164-CONTEXT.md decision D-08
    - .planning/phases/160-fail-loudly/160-CONTEXT.md (research is enrichment, not a gate)
  </read_first>
  <behavior>
    - A phase with a phase_research dispatch that produced no worker-authored file is reported in the finalize result's research_failed_phases
    - The finalize result's research_warning names those phases with the phrase "planned WITHOUT its research"
    - The fallback template written for such a phase carries a research-status line naming the failure, in addition to the existing template marker
    - A phase with worker-authored research is not reported as failed and its file is preserved unchanged
    - A phase that was never dispatched for research (the user skipped it) is not reported as failed
    - Finalize returns no error and still writes the plan when one or more research workers failed
  </behavior>
  <action>
    Make research failure loud in two places.

    In `writePhaseResearchArtifacts` (cmd/codex_plan.go:3102): when a phase falls
    through to the fallback-template branch AND that phase has a `phase_research`
    dispatch in the supplied `dispatches` slice (meaning it was approved and a
    worker was actually sent), write an additional status line immediately after
    the existing "**Research scope:**" line, of the form
    "**Research status:** phase N planned WITHOUT its research — worker failed".
    Collect those phase IDs and return them: widen the return signature to
    `([]string, int, []int, error)` where the new slice is the failed phase IDs,
    and update both call sites (cmd/codex_plan.go:582 and
    cmd/codex_plan_finalize.go:384) plus
    `cmd/phase_research_preserve_test.go:95`. Identify approved research
    dispatches with the same filter finalize already uses at
    cmd/codex_plan_finalize.go:641 — stage equals `phaseResearchStage` and caste
    equals "scout" (case-insensitive). Keep `phaseResearchTemplateMarker` in the
    template unchanged so `hasWorkerAuthoredResearch` still classifies these as
    replaceable.

    In `runCodexPlanFinalize` (cmd/codex_plan_finalize.go around line 384-470):
    add `research_failed_phases` ([]int) and `research_warning` (string) to the
    result map. The warning is empty when no phase failed; otherwise it names the
    failed phase numbers with the phrase "planned WITHOUT its research — worker
    failed". Do not return an error, do not set any blocking flag, and do not
    change `requires_next_iteration` on account of a research failure. Research
    is enrichment per the Phase 160 classification: it warns, planning proceeds.

    Add `TestResearchWorkerFailureWarnsLoudlyWithoutBlocking` to
    `cmd/phase_research_dispatch_test.go` covering the six behaviours, including
    the assertion that finalize returns a nil error and still produces phases.
  </action>
  <verify>
    <automated>go test ./cmd/... -run 'TestResearchWorkerFailureWarnsLoudlyWithoutBlocking|TestPhaseResearchPreserve' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/codex_plan.go` contains the literal string `planned WITHOUT its research — worker failed`
    - `writePhaseResearchArtifacts` returns a failed-phase-ID slice and all three call sites compile against the new signature
    - `cmd/codex_plan_finalize.go` result map contains the keys `research_failed_phases` and `research_warning`
    - The test asserts finalize returns a nil error when a research worker failed and that the plan phases are still written
    - The test asserts a phase never dispatched for research is absent from `research_failed_phases`
    - `go test ./cmd/... -count=1` exits 0
    - `go build ./cmd/aether` exits 0
  </acceptance_criteria>
  <done>A research worker that produced nothing is named in both the artifact and the finalize output, and the plan still ships.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker-written research markdown -> planner prompt | `phase-N-research.md` content is now interpolated into the Route-Setter's brief |
| dispatch list -> failure classification | Which phases count as "failed research" is derived from the dispatch manifest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-19 | Tampering | research markdown reaching the Route-Setter prompt | mitigate | Content is injected through `resolvePhaseResearchSection`, which wraps it under an explicit `## Phase Research` heading and truncates it, and it is appended after the Route-Setter's own mission text so it reads as reference material rather than instruction; the same injection already occurs on the build-brief side today and this plan adds no new author of that file |
| T-164-20 | Denial of Service | oversized research drowning the planner brief | mitigate | `routeSetterResearchBudgetChars = 12000` caps the total appended research and `resolvePhaseResearchSection`'s existing 3500-char per-phase budget caps each excerpt; over-budget phases degrade to a pointer line, asserted by `TestRouteSetterBriefResearchIsBounded` |
| T-164-21 | Repudiation | a plan silently shipping without the research it claimed | mitigate | `research_failed_phases` and `research_warning` on the finalize result plus the "**Research status:**" line inside the artifact make the omission durable in two independent places, which is the D-08 contract |
</threat_model>

<verification>
- `go test ./cmd/... -count=1` passes
- `go build ./cmd/aether` succeeds
- `go vet ./cmd/...` passes
- `grep -c 'resolvePhaseResearchSection' cmd/codex_plan.go` returns at least 1
</verification>

<success_criteria>
- The Route-Setter brief contains research body text when research exists on disk, and the pointer sentence in every case
- Appended research is bounded and over-budget phases are named
- `research_failed_phases` and `research_warning` appear on the finalize result
- Finalize returns nil error with failed research workers and still writes the plan
</success_criteria>

<output>
After completion, create `.planning/phases/164-research-feeds-planning/164-07-SUMMARY.md`
</output>
