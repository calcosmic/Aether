---
phase: 164-research-feeds-planning
plan: 10
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .aether/ts-host/src/types.ts
  - .aether/ts-host/src/host.ts
  - .aether/ts-host/src/worker-dispatch.ts
  - .aether/ts-host/test/dispatch-field-fidelity.test.ts
  - .aether/ts-host/dist/host.js
  - .aether/ts-host/dist/host.d.ts
  - .aether/ts-host/dist/worker-dispatch.js
  - .aether/ts-host/dist/worker-dispatch.d.ts
  - .aether/ts-host/dist/types.js
  - .aether/ts-host/dist/types.d.ts
  - cmd/phase_research_permission_boundary_test.go
  - .aether/commands/plan.yaml
  - .claude/commands/ant/plan.md
  - .claude/commands/ant-plan.md
  - .opencode/commands/ant/plan.md
  - cmd/command_guide.go
autonomous: true
requirements: [RESEARCH-01, RESEARCH-02, RESEARCH-03, RESEARCH-04, RESEARCH-05, RESEARCH-07]

must_haves:
  truths:
    - "A Go-emitted plan dispatch carrying permission_profile arrives at the worker request with that exact profile — the TS host never substitutes its own guess"
    - "A phase_research dispatch's six-section research mission (Go field `brief`) arrives at the worker as task_brief, so a real Scout receives the mission instead of a one-line task string"
    - "The TS-side fallback permission profile for caste scout is workspace_write, matching pkg/codex/permission_profile.go's canonical profile, so a real Scout dispatch passes ResolvePermissionProfile's exact-equality check"
    - "A test fails when any future Go dispatch field is silently dropped by toWorkerDispatches, not just permission_profile and brief"
    - "The compiled .aether/ts-host/dist/host.js — the artifact the runtime actually executes — carries both fixes, provable by running it"
    - "No wrapper or command-guide instruction still claims Scout runs under a host-enforced repository_read_only profile"
  artifacts:
    - path: ".aether/ts-host/src/host.ts"
      provides: "toWorkerDispatches maps permission_profile and brief"
      contains: "workerDispatch.permission_profile"
    - path: ".aether/ts-host/src/worker-dispatch.ts"
      provides: "TS fallback permission profile aligned with the Go canonical caste map"
    - path: ".aether/ts-host/src/types.ts"
      provides: "PlanningDispatch and ContinueExternalDispatch declare permission_profile"
      contains: "permission_profile?: PermissionProfile"
    - path: ".aether/ts-host/test/dispatch-field-fidelity.test.ts"
      provides: "Non-mocked field-fidelity invariant over the real toWorkerDispatches"
      min_lines: 60
    - path: "cmd/phase_research_permission_boundary_test.go"
      provides: "Go proof that a real plan-time research dispatch resolves through internalWorkerConfig/ResolvePermissionProfile for caste scout"
      min_lines: 50
    - path: ".aether/ts-host/dist/host.js"
      provides: "Compiled host carrying both mappings"
  key_links:
    - from: ".aether/ts-host/src/host.ts (toWorkerDispatches)"
      to: ".aether/ts-host/src/worker-dispatch.ts (dispatchRealWorker)"
      via: "BuildDispatch.permission_profile and BuildDispatch.task_brief"
      pattern: "workerDispatch\\.permission_profile"
    - from: ".aether/ts-host/src/worker-dispatch.ts (GoWorkerDispatchRequest)"
      to: "cmd/internal_worker_adapter.go (internalWorkerConfig)"
      via: "codex.ResolvePermissionProfile exact-equality enforcement"
      pattern: "ResolvePermissionProfile"
---

<objective>
Close CR-01 and CR-02 from `164-VERIFICATION.md`: `toWorkerDispatches` in the TS
host silently drops two fields the Go plan manifest emits — `permission_profile`
and `brief` — so every real (non-simulated) research Scout dispatch fails Go's
exact-equality permission check before doing any work, and any Scout that did
run would receive a one-line task string instead of the six-section research
mission `renderPhaseResearchBrief` builds.

Purpose: four of Phase 164's five success criteria are blocked by this one
conversion function. Fixing field fidelity here unblocks RESEARCH-01, -02, -03,
-04, -05 and -07 at once. The phase's own test suites (548 TS tests, full Go
suite) all pass today because every test that touches this boundary mocks it —
this plan replaces that with tests that run the real conversion and the real
permission resolution.

Output: a faithful `toWorkerDispatches`, a Go-aligned TS permission fallback, a
field-fidelity invariant test that fails on *any* future dropped field, a Go
adapter-boundary test proving a real plan-time research dispatch resolves for
caste scout, a rebuilt `dist/`, and wrapper docs that no longer assert a
sandbox guarantee the runtime stopped making.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/164-research-feeds-planning/164-VERIFICATION.md
@.planning/phases/164-research-feeds-planning/164-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

Go emits `permission_profile` on EVERY planning dispatch (cmd/codex_plan.go):

  type codexPlanningDispatch struct {
      Stage             string                  `json:"stage,omitempty"`
      ...
      Brief             string                  `json:"brief,omitempty"`
      ...
      PermissionProfile codex.PermissionProfile `json:"permission_profile"`
  }

  func attachPlanningDispatchSkillAssignments(dispatches []codexPlanningDispatch) {
      for i := range dispatches {
          dispatches[i].PermissionProfile = codex.PermissionProfileForCaste(dispatches[i].Caste)
          ...
      }
  }

`plannedPhaseResearchDispatches` (cmd/phase_research.go) calls
`attachPlanningDispatchSkillAssignments`, so every phase_research dispatch
already carries the canonical profile and a `Brief` from
`renderPhaseResearchBrief`.

Canonical caste profile (pkg/codex/permission_profile.go) — scout is NOT in the
read-only set:

  var repositoryReadOnlyCastes = map[string]struct{}{
      "includer": {},
  }

Exact-equality enforcement (pkg/codex/permission_profile.go), which errors
rather than downgrading:

  func ResolvePermissionProfile(caste string, requested PermissionProfile) (PermissionProfile, error)
  // returns error "permission profile mismatch for caste %q: requested %q but canonical profile is %q"
  // comparison ignores BehavioralRestrictions; it compares SchemaVersion, Name,
  // Filesystem, Shell, Network, Approval, WriteScopes

Go adapter entry point a Go test can drive directly
(cmd/internal_worker_adapter.go):

  func internalWorkerConfig(root string, invoker codex.WorkerInvoker, request internalWorkerDispatchRequest) (codex.WorkerConfig, error)

  type internalWorkerDispatchRequest struct {
      SchemaVersion     int                     `json:"schema_version"`
      Caste             string                  `json:"caste"`
      WorkerName        string                  `json:"worker_name"`
      TaskID            string                  `json:"task_id,omitempty"`
      Task              string                  `json:"task,omitempty"`
      TaskBrief         string                  `json:"task_brief,omitempty"`
      TimeoutMS         int64                   `json:"timeout_ms,omitempty"`
      PermissionProfile codex.PermissionProfile `json:"permission_profile"`
      ...
  }
  // Note: the request decoder uses DisallowUnknownFields.

Existing Go test precedent for driving that entry point
(cmd/internal_worker_adapter_test.go): `internalWorkerConfig(root, &codex.FakeInvoker{}, request)`.

The TS worker request shape (`.aether/ts-host/src/worker-dispatch.ts`), already
correct — it only ever receives an under-populated BuildDispatch:

  const request: GoWorkerDispatchRequest = {
      ...
      task_brief: dispatch.task_brief ?? dispatch.task,
      permission_profile: dispatch.permission_profile ?? permissionProfileForCaste(dispatch.caste),
  };

The six section headings `renderPhaseResearchBrief` writes into `Brief`
(cmd/phase_research.go): `## Hive Wisdom (Pre-existing Knowledge)`,
`## Key Patterns`, `## External Context`, `## Gotchas`,
`## Recommended Approach`, `## Files to Study` — plus the brief's own
`## Output` heading that introduces them.

TS type declarations (`.aether/ts-host/src/types.ts`):
- `BuildDispatch` ALREADY declares `permission_profile?: PermissionProfile` and `task_brief?: string`.
- `PlanningDispatch` declares `brief?: string` but NOT `permission_profile`.
- `ContinueExternalDispatch` declares `brief?: string` but NOT `permission_profile`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make toWorkerDispatches field-faithful and align the TS permission fallback with Go</name>
  <files>.aether/ts-host/src/types.ts, .aether/ts-host/src/host.ts, .aether/ts-host/src/worker-dispatch.ts, .aether/ts-host/test/dispatch-field-fidelity.test.ts</files>

  <read_first>
    - `.aether/ts-host/src/host.ts` lines 412-480 (the `HostInjectedDispatchFields` type, the `PlanDispatchLike`/`ContinueDispatchLike` unions, and the whole `toWorkerDispatches` body) and line 136 (the existing internal-export line)
    - `.aether/ts-host/src/types.ts` lines 95-141 (`BuildDispatch`, `PermissionProfile`), 355-379 (`PlanningDispatch`), 512-540 (`ContinueExternalDispatch`)
    - `.aether/ts-host/src/worker-dispatch.ts` lines 267-300 (`dispatchRealWorker` request construction) and 386-400 (`permissionProfileForCaste`)
    - `pkg/codex/permission_profile.go` lines 53-83 (`repositoryReadOnlyCastes`, `PermissionProfileForCaste`) and 113-140 (`ResolvePermissionProfile`, `permissionEnforcementEqual`)
    - `.aether/ts-host/test/research-confidence-loop.test.ts` lines 1-40 (test file conventions: node:test, `describe`/`it`, imports from `../src/*.js`)
    - `.aether/ts-host/test/repo-root.ts` (the `REPO_ROOT` constant every filesystem-touching test uses)
  </read_first>

  <behavior>
    Write these tests in `.aether/ts-host/test/dispatch-field-fidelity.test.ts`
    BEFORE editing the source. All four must fail against current `src/`.

    - Test 1 (invariant, the primary guard): construct a Go-shaped plan dispatch
      object populated with every field name `PlanningDispatch` declares plus
      `permission_profile`. Call the real exported `toWorkerDispatches` on it.
      Assert that every own enumerable key of the source object is present on
      the returned object, EXCEPT keys listed in a module-level
      `INTENTIONALLY_UNMAPPED` set and keys listed in a `RENAMED` map. Each
      entry in both collections carries an inline comment stating why. The set
      starts as: `agent_name`, `outputs`, `files_created`, `files_modified`,
      `scout_report`, `phase_plan` (plan-side) and `timeout_seconds`, `report`,
      `findings`, `issues`, `recommendations`, `weak_spots`,
      `edge_cases_discovered`, `reusable_lessons`, `handoff` (continue-side).
      `RENAMED` starts as `{ brief: "task_brief" }`. A future Go field added to
      the dispatch and not mapped fails this test until someone consciously
      classifies it.
    - Test 2: a Go-shaped scout dispatch carrying
      `permission_profile: {schema_version: 1, name: "workspace_write",
      filesystem: "workspace_write", shell: "within_filesystem_boundary",
      network: "provider_default", approval: "never", behavioral_restrictions:
      ["write phase research artifacts under .aether/data/phase-research only"]}`
      survives `toWorkerDispatches` with a deep-equal `permission_profile` on
      the output.
    - Test 3: a `phase_research` dispatch whose `brief` is a fixture containing
      all seven headings (`## Output`, `## Hive Wisdom (Pre-existing Knowledge)`,
      `## Key Patterns`, `## External Context`, `## Gotchas`,
      `## Recommended Approach`, `## Files to Study`) produces an output whose
      `task_brief` contains every one of those headings. Also assert that when a
      dispatch carries BOTH `task_brief` and `brief`, the host-injected
      `task_brief` wins (build/continue paths already inject `task_brief` and
      must not regress).
    - Test 4 (cross-language invariant): read
      `pkg/codex/permission_profile.go` from `REPO_ROOT`, extract the caste
      keys inside the `repositoryReadOnlyCastes` map literal, and assert the
      exported TS fallback returns `repository_read_only` for exactly those
      castes and `workspace_write` for `scout`, `route_setter`, `builder`,
      `oracle`. This test fails the next time Go's read-only set changes
      without the TS mirror following — the exact regression class that
      produced CR-01.
  </behavior>

  <action>
    Three source edits plus two test-visibility exports.

    1. `.aether/ts-host/src/types.ts` — add `permission_profile?: PermissionProfile;`
       to `PlanningDispatch` and to `ContinueExternalDispatch`, with a doc
       comment reading "Canonical Go-owned permission request for this caste;
       the host copies it through verbatim and never substitutes its own."
       `PermissionProfile` is already declared in this file (line ~134); do not
       redeclare it.

    2. `.aether/ts-host/src/host.ts` — inside `toWorkerDispatches`:
       - Add `if (dispatch.permission_profile !== undefined) workerDispatch.permission_profile = dispatch.permission_profile;`
       - Add the brief mapping with `task_brief` taking precedence: keep the
         existing `task_brief` assignment, then add a fallback branch so that
         when `dispatch.task_brief` is undefined and `dispatch.brief` is a
         non-empty string, `workerDispatch.task_brief` is set to
         `dispatch.brief`. Precedence order is `task_brief` (host-injected,
         build/continue) then `brief` (Go-emitted, plan-time). `brief` is
         declared on both `PlanningDispatch` and `ContinueExternalDispatch`, so
         no union widening is needed for it.
       - Add a comment above the function stating that this function is the
         Go→worker fidelity boundary, that a silently dropped field here fails
         only at Go's `ResolvePermissionProfile` exact-equality check (CR-01) or
         not at all (CR-02), and that `test/dispatch-field-fidelity.test.ts`
         guards it.
       - Export `toWorkerDispatches` so the fidelity test drives the real
         function rather than reaching it through a dispatch mock. Follow the
         existing internal-export convention on line 136 (`export { runDispatchedBuildCommand, ... }`)
         — either add it to that list or mark the declaration `export function`.

    3. `.aether/ts-host/src/worker-dispatch.ts` — in `permissionProfileForCaste`,
       change the read-only predicate from
       `normalized === "scout" || normalized === "includer"` to
       `normalized === "includer"` only, mirroring
       `repositoryReadOnlyCastes` in `pkg/codex/permission_profile.go`, which
       contains exactly one entry: `"includer"`. Update the comment above the
       function to name that Go map as the source of truth and to state that
       Go's `ResolvePermissionProfile` rejects any mismatch outright rather than
       downgrading. Export `permissionProfileForCaste` so Test 4 can assert
       against it directly.

    Do not change `dispatchRealWorker`'s request construction — it is already
    correct and becomes correct in effect once the BuildDispatch it receives
    carries the fields.
  </action>

  <verify>
    <!-- Fast targeted gate: typecheck plus ONLY the new test file. The full
         548-test suite is the phase gate in Task 3, not this task's inner loop. -->
    <automated>(cd .aether/ts-host && npm run typecheck && node --import tsx --import ./test/ensure-aether-binary.ts --test test/dispatch-field-fidelity.test.ts)</automated>
  </verify>

  <acceptance_criteria>
    - `(cd .aether/ts-host && npm run typecheck && node --import tsx --import ./test/ensure-aether-binary.ts --test test/dispatch-field-fidelity.test.ts)` exits 0 with 4 passing tests and 0 failing.
    - `cd .aether/ts-host && npm test` reports 0 failing; the total pass count is at least the prior 548 plus the new assertions.
    - `grep -n "workerDispatch.permission_profile" .aether/ts-host/src/host.ts` returns at least one line.
    - `grep -n "dispatch.brief" .aether/ts-host/src/host.ts` returns a line inside `toWorkerDispatches`.
    - `grep -vn '^\s*[/*]' .aether/ts-host/src/worker-dispatch.ts | grep -c 'normalized === "scout"'` returns 0 (no non-comment line still forces scout read-only).
    - `grep -n "permission_profile?: PermissionProfile" .aether/ts-host/src/types.ts` returns 3 lines (BuildDispatch, PlanningDispatch, ContinueExternalDispatch).
    - Reverting only the `worker-dispatch.ts` predicate makes Test 4 fail; reverting only the two `host.ts` mappings makes Tests 1-3 fail. Confirm by temporary local revert during development, then restore.
  </acceptance_criteria>

  <done>Real `toWorkerDispatches` preserves `permission_profile` and promotes `brief` to `task_brief`; the TS fallback classifies scout as `workspace_write`; four non-mocked tests in `dispatch-field-fidelity.test.ts` pass and each fails when its own fix is reverted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove a real plan-time research dispatch resolves at the Go permission boundary</name>
  <files>cmd/phase_research_permission_boundary_test.go</files>

  <read_first>
    - `cmd/phase_research.go` lines 76-107 (`plannedPhaseResearchDispatches`) and 120-156 (`renderPhaseResearchBrief` and its six output headings)
    - `cmd/internal_worker_adapter.go` lines 28-46 (`internalWorkerDispatchRequest`) and 359-423 (`internalWorkerConfig`)
    - `cmd/internal_worker_adapter_test.go` lines 40-80 (existing `internalWorkerConfig(root, &codex.FakeInvoker{}, request)` usage, including the "requires permission_profile" and "mismatch" assertions)
    - `pkg/codex/permission_profile.go` lines 53-140
    - `cmd/phase_research_escalate_test.go` lines 36-95 (test helper conventions in this package: `escalationTestSurvey()`, direct dispatch-builder calls)
  </read_first>

  <behavior>
    New file `cmd/phase_research_permission_boundary_test.go`. Every case starts
    from a dispatch built by the real `plannedPhaseResearchDispatches` — never a
    hand-written profile literal, because a hand-written literal is what let
    CR-01 hide.

    - `TestPlanResearchDispatchResolvesAtWorkerBoundary`: build a real
      phase_research dispatch, marshal it to JSON, unmarshal the
      `permission_profile` into a `codex.PermissionProfile`, assemble an
      `internalWorkerDispatchRequest` with `SchemaVersion: 1`, `Caste: "scout"`,
      `WorkerName` and `TaskID` from the dispatch, `TaskBrief` from the
      dispatch's `brief`, and that profile. Call
      `internalWorkerConfig(root, &codex.FakeInvoker{}, request)` and assert it
      returns no error and a config whose `PermissionProfile.Name` equals
      `codex.PermissionWorkspaceWrite`.
    - `TestScoutReadOnlyProfileIsRejectedAtWorkerBoundary`: same request but
      with the profile the stale TS fallback produced
      (`name`/`filesystem` = `repository_read_only`, shell
      `within_filesystem_boundary`, network `provider_default`, approval
      `never`, schema_version 1). Assert `internalWorkerConfig` returns an error
      containing "mismatch". This is the exact failure every real Scout dispatch
      hit; the test documents it as intended enforcement, not a bug to soften.
    - `TestPlanResearchDispatchCarriesSixSectionMission`: assert the real
      dispatch's `Brief` contains `## Output`, `## Hive Wisdom (Pre-existing Knowledge)`,
      `## Key Patterns`, `## External Context`, `## Gotchas`,
      `## Recommended Approach`, `## Files to Study`, and that the
      `TaskBrief` carried into `internalWorkerConfig` from that `Brief` reaches
      `WorkerConfig.TaskBrief` intact (assert on the returned config, not on the
      input).
  </behavior>

  <action>
    Create `cmd/phase_research_permission_boundary_test.go` in package `cmd`
    implementing the three tests above.

    Build the source dispatch with:
    `plannedPhaseResearchDispatches(root, "deep", goal, []phaseResearchCandidate{{ID: 3, Name: "Wire exporter", Description: "Ship the exporter"}}, codexSurveyContext{}, false, map[int]bool{3: true})`
    and assert the returned slice has exactly one element before proceeding.

    Round-trip through JSON (`encoding/json` Marshal on the dispatch, then
    Unmarshal into a `map[string]json.RawMessage` or a purpose-built struct)
    rather than reading `dispatch.PermissionProfile` directly — the JSON hop is
    the boundary the TS host actually crosses, and asserting on the Go struct
    field would skip the serialization contract entirely.

    Use `&codex.FakeInvoker{}` as the invoker so `internalWorkerConfig` skips
    `ValidateAgent`; use `t.TempDir()` for `root`. Do not add a
    `t.Skip`, build tag, or environment guard — this test must run in the
    default `go test ./cmd/...` set.

    Add a file-level comment recording that the phase's original suites passed
    while every real Scout dispatch failed, because the only CLI-level
    permission test constructed its request by hand.
  </action>

  <verify>
    <automated>go test ./cmd/ -run 'TestPlanResearchDispatchResolvesAtWorkerBoundary|TestScoutReadOnlyProfileIsRejectedAtWorkerBoundary|TestPlanResearchDispatchCarriesSixSectionMission' -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `go test ./cmd/ -run 'TestPlanResearchDispatch|TestScoutReadOnlyProfile' -v` shows three PASS lines and no SKIP.
    - `go vet ./cmd/` exits 0.
    - Temporarily adding `"scout": {}` back into `repositoryReadOnlyCastes` in `pkg/codex/permission_profile.go` makes `TestPlanResearchDispatchResolvesAtWorkerBoundary` fail. Confirm during development, then restore the file unchanged (`git diff --exit-code pkg/codex/permission_profile.go` returns 0 at task end).
    - `grep -c 'json.Marshal' cmd/phase_research_permission_boundary_test.go` returns at least 1 (the JSON hop is present, not bypassed).
  </acceptance_criteria>

  <done>`go test ./cmd/...` includes a test that starts from a real `plannedPhaseResearchDispatches` output, crosses the JSON boundary, and resolves through `internalWorkerConfig`/`ResolvePermissionProfile` for caste scout — passing on the canonical profile and failing loudly on the stale read-only one.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild dist, prove the compiled runtime carries the fix, and retire the stale read-only doc claim</name>
  <files>.aether/ts-host/dist/host.js, .aether/ts-host/dist/worker-dispatch.js, .aether/ts-host/dist/types.js, .aether/commands/plan.yaml, .claude/commands/ant/plan.md, .claude/commands/ant-plan.md, .opencode/commands/ant/plan.md, cmd/command_guide.go</files>

  <read_first>
    - `.aether/ts-host/package.json` (the `build` script: `tsc -p tsconfig.build.json`)
    - `.gitignore` lines 105-115 (confirming `.aether/ts-host/dist/` is tracked, not ignored — the compiled output must be committed)
    - `.aether/commands/plan.yaml` line 50 (guardrail bullet containing the stale claim)
    - `.claude/commands/ant/plan.md` line 71, `.claude/commands/ant-plan.md` line 71, `.opencode/commands/ant/plan.md` line 71 (the same sentence in all three wrappers; the three files are byte-parallel at 141 lines each)
    - `cmd/command_guide.go` lines 205-215 (the `catalog["plan"]` PreSteps entry carrying the same claim) and line 315 (the generic permission-inspection step, which is NOT scout-specific and stays as-is)
    - `CLAUDE.md` section "Definition of Done" (a documentation claim about runtime behaviour must be testable or removed)
  </read_first>

  <action>
    Two pieces of work: publish the compiled artifact, and delete a claim the
    runtime no longer makes.

    1. Rebuild the TS host: `npm --prefix .aether/ts-host run build`. The
       runtime executes `.aether/ts-host/dist/host.js`, not `src/` — the
       verification probe that caught CR-01 and CR-02 ran against `dist/`.
       Commit the regenerated `dist/` files; they are deliberately un-ignored
       in `.gitignore`.

    2. Prove the compiled output carries the fix by running the same shape of
       probe the verifier ran: a `node --input-type=module` one-liner that
       imports `toWorkerDispatches` from `.aether/ts-host/dist/host.js`, calls
       it on a Go-shaped scout dispatch carrying
       `permission_profile: {schema_version: 1, name: "workspace_write", filesystem: "workspace_write", shell: "within_filesystem_boundary", network: "provider_default", approval: "never"}`
       and a `brief` containing `## Files to Study`, and exits non-zero unless
       the returned object has a `permission_profile` with `name === "workspace_write"`
       and a `task_brief` containing `## Files to Study`. Record the exact
       command and its output in the SUMMARY.

    3. Retire the stale claim in all five files. The sentence asserts a sandbox
       guarantee that stopped being true when scout left
       `repositoryReadOnlyCastes`:
       - `.aether/commands/plan.yaml` guardrail: replace the "Scout
         repository_read_only must use a host-enforced no-write boundary"
         clause with a claim the runtime does make — that every planning
         dispatch's `permission_profile` must be passed through verbatim from
         the manifest, never substituted or broadened by the wrapper, and that
         Scout's canonical profile is `workspace_write` scoped behaviorally to
         `.aether/data/phase-research`.
       - The three wrapper markdown files: replace the same sentence in the
         wave-1 dispatch paragraph with the equivalent statement. These three
         files are byte-parallel; make the identical edit in all three so any
         wrapper-parity check stays green.
       - `cmd/command_guide.go` `catalog["plan"]` PreSteps: same replacement.
         Leave the generic line 315 step (which covers repository_read_only,
         scoped_write and test_write generically, without naming Scout) exactly
         as it is.
       Do not describe behavioral restrictions as a sandbox: the Go code marks
       them explicitly as "required behavior but are not a narrower host
       sandbox" (`RenderPermissionProfileSection`).

    4. Run the full verification set and record results.
  </action>

  <verify>
    <!-- The dist probe runs HERE, not in a SUMMARY note. A stale or broken
         dist rebuild must fail this command mechanically (CLAUDE.md Definition
         of Done). Run from the repo root; `-e` resolves the relative specifier
         against the cwd. dist/host.js has a main-module guard, so importing it
         does not execute the CLI (verified during planning). This probe was
         dry-run against the current dist and exits 1 with
         "toWorkerDispatches is not a function" — confirming both that the
         command fails mechanically when the requirement is unmet and that
         Task 1's `export` is load-bearing for it. Log files are used instead of `| tail` so a
         failing suite's exit code is not masked by the pipe. -->
    <automated>npm --prefix .aether/ts-host run build && node --input-type=module -e 'const { toWorkerDispatches } = await import("./.aether/ts-host/dist/host.js"); const brief = "## Output\n## Hive Wisdom (Pre-existing Knowledge)\n## Key Patterns\n## External Context\n## Gotchas\n## Recommended Approach\n## Files to Study\n- cmd/phase_research.go\n"; const profile = { schema_version: 1, name: "workspace_write", filesystem: "workspace_write", shell: "within_filesystem_boundary", network: "provider_default", approval: "never" }; const [out] = toWorkerDispatches([{ stage: "phase_research", wave: 1, caste: "scout", agent_name: "aether-scout", name: "Antenna-42", task: "Research domain knowledge for phase 3", task_id: "plan-research-phase-3", outputs: ["phase-3-research.md"], status: "planned", brief, permission_profile: profile }]); if (out?.permission_profile?.name !== "workspace_write") { console.error("FAIL dist permission_profile:", JSON.stringify(out?.permission_profile)); process.exit(1); } if (!String(out?.task_brief ?? "").includes("## Files to Study")) { console.error("FAIL dist task_brief:", JSON.stringify(out?.task_brief)); process.exit(1); } console.log("dist probe OK:", out.permission_profile.name);' && go build ./cmd/aether && (go test ./cmd/... > /tmp/164-10-gotest.log 2>&1 || (tail -30 /tmp/164-10-gotest.log; exit 1)) && (cd .aether/ts-host && npm test > /tmp/164-10-npmtest.log 2>&1 || (tail -30 /tmp/164-10-npmtest.log; exit 1))</automated>
  </verify>

  <acceptance_criteria>
    - `npm --prefix .aether/ts-host run build` exits 0.
    - The node probe against `.aether/ts-host/dist/host.js` — the one embedded in this task's `<verify><automated>` block, run as part of that chain — exits 0 and prints `dist probe OK: workspace_write`. Deleting either mapping from `src/` and rebuilding must make this exact command exit non-zero; confirm during development, then restore. Paste the command and its output into the SUMMARY.
    - `git status --porcelain .aether/ts-host/dist` shows modified compiled files (the rebuild actually changed the tracked artifact).
    - `grep -rn "Scout.*repository_read_only\|repository_read_only.*Scout" .aether/commands/plan.yaml .claude/commands/ant/plan.md .claude/commands/ant-plan.md .opencode/commands/ant/plan.md cmd/command_guide.go | grep -v "^cmd/command_guide.go:315"` returns no lines.
    - The three wrapper files remain byte-identical to each other: `diff .claude/commands/ant/plan.md .opencode/commands/ant/plan.md` and `diff .claude/commands/ant/plan.md .claude/commands/ant-plan.md` both exit 0.
    - `go build ./cmd/aether` exits 0; `go test ./cmd/...` reports no failures; `cd .aether/ts-host && npm test` reports 0 failing.
  </acceptance_criteria>

  <done>The compiled `dist/host.js` the runtime actually executes carries both mappings, proven by running it; no wrapper or command-guide instruction still claims a host-enforced read-only sandbox for Scout; both suites and the Go build are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS host → Go worker adapter | The TS host composes a `GoWorkerDispatchRequest` (including a permission profile) and hands it to `aether internal-worker-adapter`. A profile that arrives broader than the caste's canonical profile would grant a worker more filesystem access than the colony intends. |
| Go manifest → TS host | The plan manifest is the only authority on a dispatch's permission profile. Any field the host invents locally is an unverified claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01 | Elevation of Privilege | `toWorkerDispatches` copying `permission_profile` through from the manifest | mitigate | Copy-through is pass-through only, never construction. The enforcement point stays `codex.ResolvePermissionProfile` in `cmd/internal_worker_adapter.go`, which does exact-equality against `PermissionProfileForCaste(caste)` and returns an error on any mismatch — a tampered or broadened manifest profile is rejected, not honored. Task 2's `TestScoutReadOnlyProfileIsRejectedAtWorkerBoundary` proves the rejection path still fires. |
| T-164-02 | Elevation of Privilege | `permissionProfileForCaste` TS fallback widening scout from `repository_read_only` to `workspace_write` | mitigate | The widening is not a policy change made here — it mirrors a canonical Go change already made in this phase (scout removed from `repositoryReadOnlyCastes`, gaining `behavioral_restrictions` scoping writes to `.aether/data/phase-research`). Task 1 Test 4 derives the read-only caste set from `pkg/codex/permission_profile.go` at test time, so the TS mirror cannot drift again in either direction without failing. |
| T-164-03 | Tampering | `brief` promoted into `task_brief` and injected into a worker prompt | accept | The brief is produced by `renderPhaseResearchBrief` in the Go runtime from colony-owned state (goal, phase name/description, survey context); it is not user-supplied at dispatch time and crosses no new trust boundary. Existing prompt-injection sanitization applies to pheromone content, which is a separate field. |
| T-164-04 | Information Disclosure | Wrapper docs describing a sandbox guarantee the runtime does not enforce | mitigate | Task 3 removes the claim from all five surfaces. Per CLAUDE.md's Definition of Done, a documentation claim about runtime behaviour must be testable or removed; this one is now false and is removed. |
</threat_model>

<verification>
- `go build ./cmd/aether` exits 0
- `go vet ./cmd/` exits 0
- `go test ./cmd/...` passes, including the three new boundary tests
- `cd .aether/ts-host && npm run typecheck` exits 0
- `cd .aether/ts-host && npm test` passes with 0 failing
- `npm --prefix .aether/ts-host run build` exits 0 and the node probe against `dist/host.js` confirms both fields survive conversion
- No file under `.planning/phases/164-research-feeds-planning/` numbered 164-01 through 164-09 is modified
</verification>

<success_criteria>
- A Go-shaped plan dispatch carrying `permission_profile` and `brief` emerges from the real `toWorkerDispatches` with both preserved — proven against `src/` by unit test and against `dist/` by direct execution
- The TS permission fallback for scout is `workspace_write`, derived-and-asserted against the Go canonical map rather than hand-mirrored
- `go test ./cmd/...` contains a test that fails if scout's canonical profile and the dispatch's emitted profile ever diverge again
- A newly added Go dispatch field that `toWorkerDispatches` forgets to map fails the fidelity invariant test
- No surface still documents a host-enforced `repository_read_only` sandbox for Scout
</success_criteria>

<output>
After completion, create `.planning/phases/164-research-feeds-planning/164-10-SUMMARY.md`
</output>
