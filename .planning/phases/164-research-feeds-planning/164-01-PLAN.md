---
phase: 164-research-feeds-planning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/phase_research.go
  - cmd/codex_plan.go
  - cmd/phase_research_dispatch_test.go
autonomous: true
requirements: [RESEARCH-04, RESEARCH-05]

must_haves:
  truths:
    - "A research Scout's brief names the territory survey docs it should read before scanning the repo"
    - "Re-running plan with --refresh re-researches every approved phase from scratch instead of reusing the file on disk"
    - "Within one plan run's iterations, a phase is still researched only once"
    - "Re-research overwrites phase-N-research.md in place — no timestamped archive siblings appear"
  artifacts:
    - path: "cmd/phase_research.go"
      provides: "Survey-aware research brief and replan-aware dispatch gating"
      contains: "func renderPhaseResearchBrief(goal string, candidate phaseResearchCandidate, survey codexSurveyContext)"
    - path: "cmd/phase_research_dispatch_test.go"
      provides: "Regression tests pinning survey injection and replan re-research"
      contains: "TestReplanReResearchesPhases"
  key_links:
    - from: "cmd/codex_plan.go"
      to: "plannedPhaseResearchDispatches"
      via: "survey and reresearch arguments threaded at the existing call site"
      pattern: "plannedPhaseResearchDispatches\\(root, planDepth, \\*state.Goal,.*survey"
---

<objective>
Close the two mechanical gaps in the existing research dispatch: the research
Scout never receives territory survey context, and a replan silently reuses
stale findings.

Purpose: RESEARCH-05 (survey reaches the research worker) and RESEARCH-04
(replans re-research) are the two most concrete, best-isolated gaps in this
phase. Landing them first unblocks every later plan that touches
`plannedPhaseResearchDispatches`.
Output: `plannedPhaseResearchDispatches` and `renderPhaseResearchBrief` gain
survey and re-research awareness, wired at the single existing call site, with
tests that fail if either behaviour regresses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-research-feeds-planning/164-CONTEXT.md
@.planning/phases/164-research-feeds-planning/164-RESEARCH.md
@.planning/phases/164-research-feeds-planning/164-PATTERNS.md
</context>

<interfaces>
<!-- Contracts the executor needs. Do not go looking for these in the codebase. -->

Existing, unchanged (cmd/codex_plan.go:50):

  type codexSurveyContext struct {
      SurveyDir, SurveyDocs, Languages, Frameworks, Directories,
      EntryPoints, Dependencies, TestFiles, Issues, SecurityPatterns,
      SourceAnchors  // SurveyDir is a string; every other field is []string
  }

Existing, unchanged (cmd/phase_research.go:20):

  type phaseResearchCandidate struct { ID int; Name string; Description string }

Signatures this plan changes:

  plannedPhaseResearchDispatches(root, planDepth, goal string,
      candidates []phaseResearchCandidate)
    -> plannedPhaseResearchDispatches(root, planDepth, goal string,
         candidates []phaseResearchCandidate, survey codexSurveyContext,
         reresearch bool)

  renderPhaseResearchBrief(goal string, candidate phaseResearchCandidate)
    -> renderPhaseResearchBrief(goal string, candidate phaseResearchCandidate,
         survey codexSurveyContext)

Sole production call site: cmd/codex_plan.go:900, inside runCodexPlanPlanOnly.
`survey` is already in scope there (loaded at line 874); `opts.Refresh` and the
computed `iteration` are also already in scope (lines 879-884).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Territory survey reaches the research Scout's brief</name>
  <files>cmd/phase_research.go, cmd/codex_plan.go, cmd/phase_research_dispatch_test.go</files>
  <read_first>
    - cmd/phase_research.go (the whole file — 170 lines; `renderPhaseResearchBrief` is lines 105-136)
    - cmd/codex_plan.go lines 1919-1960 (`renderPlanningWorkerBrief` — the survey-injection pattern to mirror verbatim)
    - cmd/codex_plan.go lines 874-914 (the call site where `survey` is already in scope but not threaded)
    - cmd/phase_research_dispatch_test.go (existing tests that call `plannedPhaseResearchDispatches` and will need the new arguments)
    - .planning/phases/164-research-feeds-planning/164-PATTERNS.md section "cmd/codex_plan.go (controller, request-response)"
  </read_first>
  <behavior>
    - Given a survey with SurveyDocs ["nest.md", "provisions.md"], the rendered brief contains ".aether/data/survey/nest.md" and ".aether/data/survey/provisions.md"
    - Given a survey with Languages ["Go", "TypeScript"] and Frameworks ["cobra"], the rendered brief names Go, TypeScript and cobra as already-mapped territory
    - Given a zero-value codexSurveyContext, the rendered brief contains the explicit fallback sentence rather than an empty section
    - The six-section output contract already in the brief is unchanged — "## Recommended Approach" and "## Files to Study" still appear
  </behavior>
  <action>
    Add a `survey codexSurveyContext` parameter to `renderPhaseResearchBrief` and
    to `plannedPhaseResearchDispatches`, and thread `survey` through at the sole
    production call site (cmd/codex_plan.go:900) where it is already in scope.

    In `renderPhaseResearchBrief`, insert a new `## Territory Survey` section
    between the existing `## Mission` and `## Research Areas` sections. Build it
    with the same shape `renderPlanningWorkerBrief` uses: derive `surveyDir` as
    `filepath.ToSlash(filepath.Join(".aether", "data", "survey"))`, join each
    `survey.SurveyDocs` entry onto that directory with `filepath.Join` +
    `filepath.ToSlash`, and emit:
      - a "Primary survey source:" line carrying surveyDir
      - a "Survey docs to read first:" line carrying the joined doc paths,
        comma-separated, emitted only when SurveyDocs is non-empty
      - an "Already-mapped territory:" line naming survey.Languages,
        survey.Frameworks and survey.Dependencies (comma-separated, each list
        emitted only when non-empty) with an instruction that findings about
        already-mapped territory add no value — spend the budget on what the
        survey does not cover
    When SurveyDocs, Languages, Frameworks and Dependencies are all empty, emit
    the single line: "No territory survey available — scan the repository
    directly." Never emit an empty section.

    Keep every existing brief section byte-identical apart from the insertion;
    the six-section output contract is a downstream consumer contract.

    Update the four existing call sites in cmd/phase_research_dispatch_test.go to
    pass `codexSurveyContext{}` (the new parameter's behaviour is asserted by the
    new test below, not by the pre-existing ones).

    Add `TestRenderPhaseResearchBriefIncludesSurvey` to
    cmd/phase_research_dispatch_test.go covering the four behaviours above.
  </action>
  <verify>
    <automated>go test ./cmd/... -run 'TestRenderPhaseResearchBriefIncludesSurvey|TestPlanEmitsPhaseResearchDispatchesFromDraft|TestPlanFastPresetSkipsPhaseResearch' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/phase_research.go` contains `func renderPhaseResearchBrief(goal string, candidate phaseResearchCandidate, survey codexSurveyContext) string`
    - `cmd/phase_research.go` contains the literal section heading `## Territory Survey`
    - `cmd/phase_research.go` contains the literal fallback string `No territory survey available — scan the repository directly.`
    - `cmd/codex_plan.go` line ~900 passes `survey` into `plannedPhaseResearchDispatches`
    - `go test ./cmd/... -run TestRenderPhaseResearchBriefIncludesSurvey -count=1` exits 0
    - `go build ./cmd/aether` exits 0
  </acceptance_criteria>
  <done>A research Scout's brief names the survey docs and already-mapped languages/frameworks/dependencies, or explicitly says no survey exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replans re-research from scratch, overwriting in place</name>
  <files>cmd/phase_research.go, cmd/codex_plan.go, cmd/phase_research_dispatch_test.go</files>
  <read_first>
    - cmd/phase_research.go lines 57-100 (`plannedPhaseResearchDispatches` and `hasWorkerAuthoredResearch` — the exact `continue` D-05 inverts)
    - cmd/phase_research_dispatch_test.go lines 63-88 (`TestPhaseResearchDispatchedOncePerPhase` — pins today's skip-if-exists behaviour and must keep passing for non-replan runs)
    - cmd/codex_plan.go lines 874-914 (`opts.Refresh` and `iteration` are computed at 879-884, in scope at the call site)
    - .planning/phases/164-research-feeds-planning/164-CONTEXT.md decisions D-05, D-06, D-07
  </read_first>
  <behavior>
    - reresearch=true with worker-authored phase-1-research.md on disk: phase 1 IS dispatched (stale findings are not reused)
    - reresearch=false with the same file on disk: phase 1 is NOT dispatched (single-run iterations still research once per phase)
    - reresearch=true: every dispatch's Outputs is exactly ["phase-N-research.md"] — no timestamped archive sibling filename is emitted
    - planDepth "fast" still returns zero dispatches regardless of the reresearch flag
  </behavior>
  <action>
    Add a `reresearch bool` parameter to `plannedPhaseResearchDispatches` as the
    final argument. Inside the candidate loop, gate the existing
    `hasWorkerAuthoredResearch(existingPath)` skip on `!reresearch` — when
    `reresearch` is true, the `continue` never fires and every candidate is
    dispatched.

    Do not delete, rename or archive the existing `phase-N-research.md` file.
    D-07 requires overwrite-in-place: the worker's `Outputs` entry stays exactly
    `fmt.Sprintf("phase-%d-research.md", candidate.ID)` and no sibling path is
    introduced anywhere in this function.

    At the call site in `runCodexPlanPlanOnly` (cmd/codex_plan.go:900), compute
    the argument as `opts.Refresh && iteration == 1`. The `iteration` variable is
    already computed at line 881. This is the D-05 boundary: a replan's first
    manifest re-researches from scratch, while iterations 2..N of that same
    replan run reuse what the first iteration's Scouts wrote.

    Update the doc comment on `plannedPhaseResearchDispatches` to state both
    rules explicitly so the next reader does not re-derive them.

    Add `TestReplanReResearchesPhases` to cmd/phase_research_dispatch_test.go
    covering the four behaviours above, and leave
    `TestPhaseResearchDispatchedOncePerPhase` asserting the reresearch=false path
    (update its call to pass `false`).
  </action>
  <verify>
    <automated>go test ./cmd/... -run 'TestReplanReResearchesPhases|TestPhaseResearchDispatchedOncePerPhase|TestPlanFastPresetSkipsPhaseResearch' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/phase_research.go` contains `reresearch bool` in the `plannedPhaseResearchDispatches` signature
    - `cmd/phase_research.go` gates the staleness skip: the `hasWorkerAuthoredResearch` call is guarded by `!reresearch`
    - `cmd/codex_plan.go` passes `opts.Refresh && iteration == 1` as the reresearch argument
    - `grep -n 'phase-%d-research' cmd/phase_research.go` shows only the plain (non-timestamped) filename pattern
    - `go test ./cmd/... -run 'TestReplanReResearchesPhases|TestPhaseResearchDispatchedOncePerPhase' -count=1` exits 0
    - `go test ./cmd/... -count=1` exits 0
  </acceptance_criteria>
  <done>A `--refresh` plan run re-dispatches research for every candidate phase; a same-run iteration does not; the artifact filename is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| colony state -> worker brief | Phase names and descriptions are user-authored text that this plan now interpolates into a Scout's prompt alongside survey paths |
| filesystem -> dispatch decision | `phase-N-research.md` contents decide whether a worker is spawned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01 | Tampering | `renderPhaseResearchBrief` survey section | mitigate | Survey values interpolated into the brief are runtime-derived paths and dependency names from `loadCodexSurveyContext`, never raw user free-text; the existing goal/description interpolation is unchanged by this plan and carries no new injection surface |
| T-164-02 | Information Disclosure | survey doc paths in the brief | accept | Paths are repository-relative under `.aether/data/survey` and already appear in `renderPlanningWorkerBrief`; the Scout's `repository_read_only` permission profile is unchanged |
| T-164-03 | Denial of Service | replan re-dispatch of every phase | mitigate | Re-research is bounded to `iteration == 1` of a refresh run, and Plan 05's approved batch is the user-visible cost control before any worker spawns |
</threat_model>

<verification>
- `go test ./cmd/... -count=1` passes
- `go build ./cmd/aether` succeeds
- `grep -c 'codexSurveyContext' cmd/phase_research.go` returns at least 2
</verification>

<success_criteria>
- A research Scout brief rendered with a populated survey names the survey directory, the survey docs, and the already-mapped languages/frameworks/dependencies
- A research Scout brief rendered with an empty survey states that no survey is available
- `plannedPhaseResearchDispatches(..., reresearch=true)` dispatches phases that already have worker-authored research on disk
- `plannedPhaseResearchDispatches(..., reresearch=false)` still skips them
- No timestamped or archived research filename appears anywhere in `cmd/phase_research.go`
</success_criteria>

<output>
After completion, create `.planning/phases/164-research-feeds-planning/164-01-SUMMARY.md`
</output>
