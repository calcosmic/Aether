---
phase: 201-queen-led-work-cycle
plan: "10"
type: execute
wave: 7
depends_on: ["201-09"]
files_modified:
  - cmd/memory_feed.go
  - cmd/work_repair.go
  - cmd/codex_build.go
  - cmd/failure_evidence_test.go
autonomous: true
requirements: [WORK-06, WORK-05]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Worker failures and build failures write canonical failure evidence through the one existing memory-feed boundary, and that evidence is what bounded recovery consumes (CAP-003, WORK-06)."
    - "A worker's reported blocker becomes durable attempt-bound result truth and appears consistently wherever advancement, status and closure decisions read it (CAP-004, WORK-05)."
    - "The escalated-blocker count shown to the owner equals the exact number of unresolved blockers bound to the relevant attempts, with no separate counting path (CAP-051)."
    - "Bounded recovery consumes unresolved flags and recurring failure classes through the same one repair model, not a second recovery path (CAP-024)."
    - "A signal born from this phase's own failures reaches the repair wave's worker briefs in the same run, so the second attempt cannot repeat the first attempt's mistake (D-12)."
    - "Worker-authored text is sanitised before it is stored and before it is replayed into any later brief."
    - "Writing failure evidence never fails a build or a check."
  artifacts:
    - path: "cmd/failure_evidence_test.go"
      provides: "Failure-evidence, blocker-truth, escalated-count and same-run signal-delivery proofs"
      contains: "TestFailureBornSignalReachesTheRepairBrief"
  key_links:
    - from: "cmd/work_repair.go"
      to: "cmd/memory_feed.go"
      via: "the repair round consumes the failure evidence the failing attempt wrote"
      pattern: "recordDispatchWorkerOutcome"
    - from: "cmd/codex_build.go"
      to: "cmd/work_repair.go"
      via: "repair-wave briefs carry this phase's own failure-born signals"
      pattern: "composeBuildManifestBrief"
  prohibitions:
    - "Never add a second path by which a worker outcome becomes failure evidence."
    - "Never store or replay worker-authored text without sanitising it."
    - "Never let writing failure evidence fail a build or a check."
    - "Never count escalated blockers through a second counting path."
---

<objective>
Make failure leave usable evidence, make blockers durable, and make this phase's own failures steer its own repair.

Purpose: The repair round plan 201-09 built gets one attempt. If the repair worker's brief does not carry what just went wrong, that one attempt is a coin flip. D-12 closes the loop inside the run: a signal born from this attempt's failure reaches the next worker in the same run, not in the next phase.
Output: Canonical failure evidence, attempt-bound blocker truth with one counting path, and same-run signal delivery into repair briefs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/201-queen-led-work-cycle/201-CONTEXT.md
@.planning/phases/201-queen-led-work-cycle/201-RESEARCH.md
@.planning/phases/201-queen-led-work-cycle/201-PATTERNS.md
@.planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md
@.planning/research/v1.28-classic-capability-ledger.md
@cmd/memory_feed.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind failure evidence and blocker truth to the exact attempt</name>
  <files>cmd/memory_feed.go, cmd/failure_evidence_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/memory_feed.go
    - cmd/failure_evidence_test.go (create; confirm absent)
    - pkg/colony/midden.go
    - cmd/build_attempt.go
    - cmd/status.go
    - cmd/lifecycle_status_render.go
  </read_first>
  <behavior>
    - A failed worker on either build lane writes one failure record through the single memory-feed boundary, carrying the attempt identifier.
    - A worker-reported blocker becomes a durable record bound to that attempt and is visible to advancement, status and closure reads.
    - The escalated-blocker count equals the exact number of unresolved attempt-bound blockers, computed once and read everywhere.
    - Worker-authored text is sanitised before storage and again is not re-sanitised into a different value on replay.
    - A storage failure while writing evidence is reported but does not fail the build or the check.
  </behavior>
  <action>Extend `recordDispatchWorkerOutcome` in `cmd/memory_feed.go` — the single boundary both build lanes already feed — so the failure record and the observation it writes carry the attempt identifier and the job name from plan 201-08's identity work. Do not add a second feeding path; the existing syntax-tree guard that refuses a third path must stay green.

Make a worker-reported blocker a durable record bound to the same attempt, stored through the same boundary, and make the advancement, status and closure reads resolve blockers from that one store. Compute the escalated-blocker count once, in one function, and have every surface read it — the count the owner sees and the count a gate consults must be the same number by construction, not by agreement.

Keep the existing sanitisation of worker-authored text before every store, since these entries are replayed verbatim into later briefs. Keep the existing rule that feeding memory never fails a build or a check: a storage error is reported and swallowed.

Add `cmd/failure_evidence_test.go` covering a failed worker on each build lane, a worker-reported blocker's visibility at all three reading surfaces, an escalated count compared against the fixture's own unresolved blocker count, a sanitisation case with hostile worker text, and an induced storage failure asserting the build still completes.</action>
  <acceptance_criteria>
    - A failed worker on each lane produces exactly one failure record carrying the attempt identifier and job name.
    - A worker-reported blocker resolves identically at the advancement, status and closure reads.
    - The rendered escalated-blocker count equals the fixture's exact unresolved blocker count, and exactly one function computes it.
    - Hostile worker text is stored sanitised and replays as the sanitised value.
    - An induced storage failure leaves the build result unchanged and reports the error without failing.
    - The existing single-boundary syntax-tree guard still passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestFailureEvidenceCarriesTheAttemptIdentity|TestBlockerTruthIsOneStore|TestEscalatedCountHasOneCountingPath|TestEvidenceStorageFailureNeverFailsTheRun)$' -count=1 &amp;&amp; go test ./cmd -run '^TestEveryBuildLaneFeedsMemoryThroughOneBoundary$' -count=1</automated>
  </verify>
  <done>A failure leaves evidence that names the exact attempt it came from, and a blocker means the same thing everywhere it is read.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Let bounded recovery consume flags and recurring failure classes</name>
  <files>cmd/work_repair.go, cmd/failure_evidence_test.go</files>
  <read_first>
    - cmd/work_repair.go
    - cmd/memory_feed.go
    - pkg/colony/midden.go
    - cmd/autopilot_policy.go
    - .planning/research/v1.28-classic-capability-ledger.md
  </read_first>
  <behavior>
    - The repair round's eligibility evaluation reads unresolved flags and recurring failure classes as inputs.
    - A failure class already seen three times unacknowledged is treated as recurring and changes the repair evaluation's recorded reason.
    - A phase with unresolved flags cannot be evaluated as having nothing to repair.
    - There is exactly one recovery model; no second recovery path is added for flags.
  </behavior>
  <action>Extend the repair eligibility evaluation in `cmd/work_repair.go` so it reads, as inputs, the unresolved flags and the recurring failure classes bound to the phase's attempts, alongside the failing check it already consumes. Feed them into the existing `autopilotRepairEvaluation` shape — eligible, pause, reason — so the recorded reason names which of the three inputs drove the decision.

Preserve the existing three-unacknowledged-failures rule that turns a recurring failure class into a single steering signal; consume that signal here rather than reimplementing the counting. Do not add a separate flag-driven recovery path: CAP-024 requires one bounded recovery model, and a second path is exactly the duplication this phase exists to remove.

Extend `cmd/failure_evidence_test.go` with cases for a phase carrying unresolved flags only, a phase carrying a recurring failure class only, a phase carrying both, and a phase carrying neither — asserting the recorded evaluation reason names the driving input in each, and asserting only one recovery entry point exists in the package syntax tree.</action>
  <acceptance_criteria>
    - The repair evaluation's recorded reason names which input drove it, for each of the four fixture cases.
    - A phase with unresolved flags is never evaluated as having nothing to repair.
    - A failure class with three unacknowledged occurrences is treated as recurring; two occurrences is not.
    - Exactly one recovery entry point is declared in the package, asserted from the parsed syntax tree.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestRepairEvaluationNamesItsDrivingInput|TestOneRecoveryModelConsumesFlagsAndFailures)$' -count=1</automated>
  </verify>
  <done>Unresolved flags and repeated failures feed the one repair model instead of sitting unread in a log.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deliver this phase's failure-born signals into the repair brief</name>
  <files>cmd/codex_build.go, cmd/work_repair.go, cmd/failure_evidence_test.go</files>
  <read_first>
    - cmd/codex_build.go
    - cmd/work_repair.go
    - cmd/colony_prime_context.go
    - cmd/build_print_brief.go
    - cmd/memory_feed.go
  </read_first>
  <behavior>
    - A signal created from this attempt's own failure appears in the repair wave's worker brief in the same run.
    - The repair brief names the specific failure the signal came from, not a generic instruction.
    - A repair worker whose brief carries the signal does not repeat the exact action the signal names.
    - Signal content in the brief is the sanitised stored value.
    - The brief stays within its declared content budget; the signal does not push other required content out silently.
  </behavior>
  <action>Wire the failure-born signals produced during a failing attempt into the brief assembled for that same run's repair wave. The repair round in `cmd/work_repair.go` must read the signals written for the current attempt and pass them to `composeBuildManifestBrief` in `cmd/codex_build.go` before the repair wave dispatches, so the delivery is inside the run rather than at the next phase boundary.

Carry the signal into the brief through the existing steering-signal section rather than a new section, and respect the existing brief content budget. Where the budget forces a trim, follow the established trim order in which steering signals are the last thing trimmed and blockers are never trimmed; if the signal cannot fit, say so in the brief rather than dropping it silently.

Add the closing-loop test: build a fixture whose first attempt fails in a specific, nameable way; run the repair round; and assert the repair worker's brief contains the sanitised signal text naming that specific failure. Assert the delivery happens within the one run by checking the brief is composed after the failure record is written and before the repair dispatch.</action>
  <acceptance_criteria>
    - The repair worker's brief contains the sanitised signal text naming the specific failure from the same run.
    - The signal is delivered after the failure record is written and before the repair dispatch, asserted on the ordering of the recorded events.
    - The brief remains within its declared content budget, and a forced trim reports the omission rather than dropping it silently.
    - The signal appears in the existing steering-signal section, with no new brief section added.
    - `go vet ./cmd` is clean.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestFailureBornSignalReachesTheRepairBrief|TestRepairBriefRespectsItsContentBudget)$' -count=1 &amp;&amp; go vet ./cmd</automated>
  </verify>
  <done>The one repair attempt starts knowing exactly what just went wrong, so it is not a second guess at the same problem.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created by this plan (new to the codebase — not drift):

- `cmd/failure_evidence_test.go` (new file)
- Attempt-identifier and job-name fields on the failure record and observation written by `recordDispatchWorkerOutcome`; the durable attempt-bound blocker record; the single escalated-blocker counting function
- Flag and recurring-failure inputs on the repair eligibility evaluation; same-run signal delivery into the repair brief
- `TestFailureEvidenceCarriesTheAttemptIdentity`, `TestBlockerTruthIsOneStore`, `TestEscalatedCountHasOneCountingPath`, `TestEvidenceStorageFailureNeverFailsTheRun`, `TestRepairEvaluationNamesItsDrivingInput`, `TestOneRecoveryModelConsumesFlagsAndFailures`, `TestFailureBornSignalReachesTheRepairBrief`, `TestRepairBriefRespectsItsContentBudget` (new tests)

<verification>
- CAP-003, CAP-004, CAP-024 and CAP-051 are delivered against cited evidence.
- One memory-feed boundary, one blocker store, one escalated count, one recovery model.
- The failure-to-repair loop closes inside a single run.
</verification>

<success_criteria>
- The single repair attempt is informed rather than blind.
- `go test ./cmd` and `go vet ./cmd` are clean.
</success_criteria>

<output>
Create `.planning/phases/201-queen-led-work-cycle/201-10-SUMMARY.md` when done.
</output>
