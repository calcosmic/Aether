---
phase: 201-queen-led-work-cycle
plan: "15"
type: execute
wave: 12
depends_on: ["201-01", "201-05", "201-08", "201-11", "201-12", "201-14"]
files_modified:
  - cmd/testdata/classic-contract/v1/cases.json
  - cmd/classic_contract_test.go
  - cmd/classic_coverage_201_test.go
autonomous: true
requirements: [WORK-03, WORK-04, WORK-05, CEC-06]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every SYN-201 decision has at least one executable case that drives a real public command and asserts both what the owner sees and the causal state transition underneath it."
    - "No Phase 201 case passes on rendered text alone; each carries a state assertion or a forbidden-artifact assertion."
    - "Each of the eight CAP rows routed to Phase 201 resolves to a named delivered proof or is explicitly recorded as unresolved."
    - "The visible build, check and autopilot behaviours are tied to the authoritative Go transitions, not to snapshots of output."
    - "Negative cases prove the rejected shortcuts did not return: a second automatic repair attempt, a doubled review pass, a derived timing segment, and a non-success outcome rendered as success."
    - "The coverage ledger asserts exact cardinality — fourteen decisions and eight capability rows — so a silently dropped row fails by name."
  artifacts:
    - path: "cmd/testdata/classic-contract/v1/cases.json"
      provides: "Phase 201 executable work-cycle behaviour cases"
      contains: "V-201-BOUNDARY"
    - path: "cmd/classic_coverage_201_test.go"
      provides: "Exact-cardinality coverage ledger for the fourteen decisions and eight capability rows"
      contains: "TestClassicCoverage201ExactSets"
  key_links:
    - from: "cmd/testdata/classic-contract/v1/cases.json"
      to: "cmd/testdata/classic-contract/v1/mechanisms.json"
      via: "every case names a registered SYN-201 decision"
      pattern: "SYN-201-"
  prohibitions:
    - "Never accept a case that asserts only rendered output."
    - "Never build a fixture in a shape the runtime cannot produce."
    - "Never mark a capability row resolved without a named delivered proof."
---

<objective>
Prove the restored work cycle by executing it, and record exactly what was proved.

Purpose: This repository's own audits show the alternative failing — eighteen of twenty-five milestones framed around restoring something previously marked done, because "covered by existing tests" was accepted as proof. A phase this large closes with cases that run the real public commands and check the state underneath, plus a ledger with exact cardinality so a dropped row is a failure rather than an omission nobody notices.
Output: Executable Phase 201 behaviour cases with causal assertions, negative cases for the rejected shortcuts, and an exact-cardinality coverage ledger.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/201-queen-led-work-cycle/201-CONTEXT.md
@.planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md
@.planning/research/v1.28-classic-capability-ledger.md
@cmd/classic_contract_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the Phase 201 behaviour cases with causal assertions</name>
  <files>cmd/testdata/classic-contract/v1/cases.json</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/testdata/classic-contract/v1/cases.json
    - cmd/testdata/classic-contract/v1/schema.json
    - cmd/testdata/classic-contract/v1/mechanisms.json
    - cmd/classic_contract_test.go
    - .planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md
    - cmd/blackbox_harness_test.go
  </read_first>
  <behavior>
    - Every added case names a registered SYN-201 decision and one of the six Phase 201 groups.
    - Every added case carries at least one state assertion or forbidden-artifact assertion in addition to its semantic assertions.
    - A case that asserts only rendered text fails schema validation.
    - Cases run against isolated fixture roots and never mutate the source checkout or a shared home directory.
  </behavior>
  <action>Append Phase 201 behaviour cases to `cmd/testdata/classic-contract/v1/cases.json`, one or more per registered SYN-201 decision, distributed across the six Phase 201 groups: boundary cases under `V-201-BOUNDARY`, outcome and result-truth cases under `V-201-OUTCOME`, job and attempt identity cases under `V-201-IDENTITY`, repair cases under `V-201-REPAIR`, controller cases under `V-201-AUTOPILOT`, and timing cases under `V-201-TELEMETRY`.

Each case drives a real public command in the owner-facing vocabulary Phase 199 locked and asserts two things: what the owner sees, and the causal state transition underneath. Examples of the causal half: after a check-step boundary build, the attempt record carries the recorded boundary and no reviewer dispatch at build-end; after a failed repair, the working-tree digest equals the checkpoint digest and exactly one repair receipt exists; after a partial build, the credited task set is exactly the finalised tasks and the uncredited files are listed with locations; after an instrumented build, the timing record exists at the attempt-bound path with the measured segments naming their sources.

Include the negative cases that prove the rejected shortcuts did not return: a second automatic repair attempt is refused; the same caste does not appear in both the build and the check dispatch lists; a timing segment is never present without a source; and a non-success run's card carries no token from the success verdict's label.

Derive every fixture value the way the runtime derives it. A fixture in a shape the runtime cannot produce is a false certificate, and it is how this repository has previously shipped a broken feature with a green suite.</action>
  <acceptance_criteria>
    - Every added case validates against the schema, naming a registered SYN-201 decision and one of the six Phase 201 groups.
    - Every added case carries at least one state assertion or forbidden-artifact assertion.
    - A deliberately text-only case fails schema validation.
    - Each of the four named negative cases is present.
    - No case mutates the source checkout or a shared home directory, proved by a before-and-after digest.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestClassicContractSchema|TestClassicContractPhase201Cases)$' -count=1 &amp;&amp; python3 -c "import json,sys; c=json.load(open('cmd/testdata/classic-contract/v1/cases.json'))['cases']; p=[x for x in c if x['synthesis_decision'].startswith('SYN-201-')]; sys.exit(0 if p and all(('state_assertions' in x['expected']) or ('forbidden_artifacts' in x['expected']) for x in p) else 1)"</automated>
  </verify>
  <done>Every mechanism this phase restored is proved by running the real command and looking at what actually changed on disk.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute the Phase 201 cases as a registered group</name>
  <files>cmd/classic_contract_test.go</files>
  <read_first>
    - cmd/classic_contract_test.go
    - cmd/testdata/classic-contract/v1/cases.json
    - cmd/blackbox_harness_test.go
    - cmd/classic_coverage_199_test.go
  </read_first>
  <behavior>
    - Every registered SYN-201 decision has at least one executing case, and a decision with none fails by name.
    - Every Phase 201 case executes against an isolated fixture root and its assertions are evaluated.
    - A case whose command exits unexpectedly fails with the command, the exit code and the captured output named.
    - A group with zero cases fails rather than passing vacuously.
  </behavior>
  <action>Add `TestClassicContractPhase201CausalExecution` to `cmd/classic_contract_test.go`, following the structure of the existing Phase 200 causal-execution test. Load the corpus through the strict loader, select every case naming a SYN-201 decision, execute each against an isolated fixture root using the existing black-box harness, and evaluate both its semantic and its causal assertions.

Add a coverage assertion that every registered SYN-201 decision is exercised by at least one executing case and that every Phase 201 group contains at least one case — a group or decision with zero cases must fail by name rather than pass because the selector matched nothing. Report failures with the command, exit code and captured output so a break reads as what it is.

Do not weaken any existing execution test to make the new group fit. If a shared helper needs extending, extend it additively and keep the Phase 199 and Phase 200 executions passing unchanged.</action>
  <acceptance_criteria>
    - Every registered SYN-201 decision is exercised by at least one executing case.
    - Every Phase 201 group contains at least one case; an empty group fails by name.
    - A deliberately broken case fails with the command, exit code and captured output named.
    - Phase 199 and Phase 200 execution tests pass unmodified.
    - No test run mutates the source checkout.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestClassicContractPhase201CausalExecution|TestClassicContractPhase200CausalExecution|TestClassicContractCorpusCausalReceipts)$' -count=1</automated>
  </verify>
  <done>The Phase 201 cases actually run, against real commands, and a group nobody wrote cases for cannot pass by matching nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record exact coverage for the decisions and capability rows</name>
  <files>cmd/classic_coverage_201_test.go</files>
  <read_first>
    - cmd/classic_coverage_201_test.go (create; confirm absent)
    - cmd/classic_coverage_199_test.go
    - cmd/testdata/classic-contract/v1/mechanisms.json
    - .planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md
    - .planning/research/v1.28-classic-capability-ledger.md
  </read_first>
  <behavior>
    - The ledger contains exactly fourteen decision rows and exactly eight capability rows; any other cardinality fails naming the difference.
    - Every row resolves to a named delivered proof — a test name or an executing case identifier — or is explicitly recorded as unresolved with a reason.
    - A row whose named proof does not exist fails, naming the missing proof.
    - A row marked resolved with an empty proof reference fails.
    - Comment lines and string decoys in the source files are not accepted as proof.
  </behavior>
  <action>Create `cmd/classic_coverage_201_test.go` modelled on `cmd/classic_coverage_199_test.go`. Build a coverage ledger for Phase 201 with one row per SYN-201 decision and one row per routed capability row, each naming the requirement it serves, its disposition, and its delivered proof — a Go test name or an executing corpus case identifier.

Assert exact cardinality: fourteen decision rows and eight capability rows, failing with the difference named if either count changes. Assert every named proof resolves: a test name must be a declared top-level test in the package, and a case identifier must be present in the corpus. Resolve proofs from parsed declarations rather than from text search, so a commented-out test or a matching string literal cannot be accepted as proof — the same decoy-rejection discipline the Phase 199 ledger already applies.

Permit a row to be explicitly unresolved with a stated reason; do not permit a row to be marked resolved with an empty or unresolvable proof reference.</action>
  <acceptance_criteria>
    - The ledger has exactly fourteen decision rows and exactly eight capability rows.
    - Every resolved row's named proof resolves to a declared test or a corpus case identifier.
    - A row naming a non-existent proof fails with the proof named.
    - A commented-out test and a matching string literal are both rejected as proof.
    - An unresolved row is permitted only with a stated reason.
    - `go vet ./cmd` is clean.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestClassicCoverage201ExactSets|TestClassicCoverage201ProofResolution|TestClassicCoverage201RejectsDecoys)$' -count=1 &amp;&amp; go vet ./cmd</automated>
  </verify>
  <done>What this phase proved, and what it did not, is written down with exact counts and named evidence rather than a checked box.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created by this plan (new to the codebase — not drift):

- `cmd/classic_coverage_201_test.go` (new file)
- Phase 201 behaviour cases in `cmd/testdata/classic-contract/v1/cases.json` under the six `V-201-*` groups
- `TestClassicContractPhase201Cases`, `TestClassicContractPhase201CausalExecution`, `TestClassicCoverage201ExactSets`, `TestClassicCoverage201ProofResolution`, `TestClassicCoverage201RejectsDecoys` (new tests)

<verification>
- Every SYN-201 decision has at least one executing case with a causal assertion.
- The four rejected shortcuts each have a negative case proving they did not return.
- The coverage ledger enforces exact cardinality and resolves every named proof from parsed declarations.
- Phase 199 and Phase 200 corpus behaviour is unchanged.
</verification>

<success_criteria>
- The phase closes with executable proof rather than a summary claim.
- `go test ./...` and `go test ./... -race` are clean.
</success_criteria>

<output>
Create `.planning/phases/201-queen-led-work-cycle/201-15-SUMMARY.md` when done.
</output>
