---
phase: 201-queen-led-work-cycle
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md
  - cmd/testdata/classic-contract/v1/schema.json
  - cmd/testdata/classic-contract/v1/mechanisms.json
  - cmd/classic_contract_test.go
autonomous: true
requirements: [SYNTH-03]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every Phase 201 work-cycle mechanism has an evidence-backed keep-current, restore-modern, replace-better, or retire-with-proof disposition recorded before any runtime work begins (SYNTH-03)."
    - "Each of the eight CAP rows routed to Phase 201 is independently confirmed or revised against cited Classic and current-Go evidence rather than inherited from the capability ledger's hypothesis."
    - "The synthesis records a disposition for every SYN-201 row and every routed CAP row; an item evidence cannot decide is listed as an open decision with a named authority, never omitted."
    - "Every locked decision D-01 through D-16 traces to at least one SYN-201 row and at least one named implementation plan."
    - "The versioned Classic contract corpus accepts SYN-201 mechanism identifiers and work-cycle groups, and rejects an unknown SYN identifier by name."
  artifacts:
    - path: ".planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md"
      provides: "Signed Phase 201 work-cycle mechanism study and CAP routing"
      contains: "SYN-201-14"
    - path: "cmd/testdata/classic-contract/v1/mechanisms.json"
      provides: "SYN-201 mechanism registry entries"
      contains: "SYN-201-01"
    - path: "cmd/testdata/classic-contract/v1/schema.json"
      provides: "Widened synthesis-decision pattern and work-cycle group enum"
      contains: "V-201-BOUNDARY"
  key_links:
    - from: "cmd/testdata/classic-contract/v1/mechanisms.json"
      to: ".planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md"
      via: "SYN-201 and CAP identifiers"
      pattern: "SYN-201-"
  prohibitions:
    - "Never justify an implementation task by a Classic feature name alone."
    - "Never replace a current Go safety behavior without a documented comparative reason."
    - "Never present an unresolved owner choice as an engineering conclusion."
---

<objective>
Produce the Phase 201 mechanism study that SYNTH-03 requires and that SYNTH-07 makes a hard planning gate, and widen the versioned Classic contract corpus to accept Phase 201 mechanism identifiers.

Purpose: Every later Phase 201 plan changes code that already exists and is already test-locked. Without a cited comparison of the Classic mechanism against the current Go mechanism, this phase's most likely failure is re-inventing a slightly different version of something already built — the exact "parallel implementations kept in sync by hand" pattern `.planning/codebase/CONCERNS.md` names as this repository's recurring defect.
Output: A signed `201-CLASSIC-SYNTHESIS.md` carrying SYN-201-01..14, and a corpus schema plus mechanism registry that accept those identifiers.

Tracer note: this plan is the SYNTH-07 planning gate and produces no runtime behavior, so it carries no `type="tracer"` task. The phase's tracer — the thin end-to-end slice through every layer this phase modifies — is Task 1 of plan `201-02`, which is the first implementation plan and depends on this one.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/201-queen-led-work-cycle/201-CONTEXT.md
@.planning/phases/201-queen-led-work-cycle/201-RESEARCH.md
@.planning/phases/201-queen-led-work-cycle/201-PATTERNS.md
@.planning/research/v1.28-classic-synthesis-template.md
@.planning/research/v1.28-classic-capability-ledger.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the cited Phase 201 work-cycle mechanism synthesis</name>
  <files>.planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md (create; confirm absent)
    - .planning/research/v1.28-classic-synthesis-template.md
    - .planning/research/v1.28-classic-capability-ledger.md
    - .planning/phases/201-queen-led-work-cycle/201-CONTEXT.md
    - .planning/phases/201-queen-led-work-cycle/201-RESEARCH.md
    - .planning/phases/201-queen-led-work-cycle/201-PATTERNS.md
    - .planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
    - .planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md
    - .aether/dreams/2026-09-01-comprehensive-aether-colony-review.md
    - .planning/codebase/CONCERNS.md
    - .planning/codebase/ARCHITECTURE.md
    - cmd/queen_judgement.go
    - cmd/coherent_jobs.go
    - cmd/deterministic_floor.go
    - cmd/build_attempt.go
    - cmd/autopilot_policy.go
    - cmd/verification_scope.go
    - cmd/lifecycle_closeout.go
  </read_first>
  <action>Instantiate `.planning/research/v1.28-classic-synthesis-template.md` for area `work-cycle`, phase `201`. Reconstruct the Classic mechanism from the historical anchors `3a5b81c2`, `v5.0.0` and `v5.4` for build, continue, run, checks, recovery, context relay and ceremony as `OLD-*` rows, each citing an exact `git show <anchor>:<path>` reference. Audit the current Go mechanism as `NOW-*` rows citing `path:line` plus the named test that locks it, covering at minimum queenApplyJudgement, planCoherentJobs, runDeterministicFloor, runCodexContinue, runCodexContinuePlanOnly, runCodexContinueFinalize, queenBuildPostWaveDispatches, buildAttemptRecord, checkFixAttemptRecord, autopilotRepairLedger, buildLifecycleCloseout, renderSpendCostLine, deriveVerificationScope and scopedGoTestCommand.

Record exactly fourteen comparative synthesis rows with these identities and subjects: SYN-201-01 Queen work judgment and proportionate team; SYN-201-02 coherent jobs, waves and workspace leases; SYN-201-03 one verification boundary and its recorded choice; SYN-201-04 one accept/verify/advance authority across three lanes; SYN-201-05 honest unverified build closeout; SYN-201-06 complete outcome vocabulary with equal ceremony; SYN-201-07 attempt-bound cost and elapsed time; SYN-201-08 one outcome-matched next action; SYN-201-09 credited and orphaned file precision; SYN-201-10 bounded checkpointed repair and rollback; SYN-201-11 failure evidence and same-phase repair steering; SYN-201-12 goal-level autopilot on one result model; SYN-201-13 per-job telemetry with honest unmeasured segments; SYN-201-14 approved turnaround levers. Give each a disposition drawn from keep-current, restore-modern, replace-better, retire-with-proof.

Route and independently adjudicate all eight Phase 201 capability rows: CAP-003, CAP-004, CAP-022, CAP-024, CAP-029, CAP-051, CAP-066 and CAP-071. The ledger's proposed disposition is a hypothesis; confirm or revise each with its own cited old and current evidence.

Resolve, as explicit synthesis rows rather than silent assumptions, the three questions RESEARCH.md leaves open: (a) whether the verification-boundary choice reuses queenCasteJudgement's struct or gets a sibling type — record the selected shape; (b) how the shared accept/verify/advance function reconciles the direct lane's in-process error surface with the finalize lane's untrusted-JSON completion-contract validation — record which responsibilities stay lane-local; (c) whether caste model routing needs new spend-ledger fields or only a policy layer above the existing ledger. Also resolve RESEARCH.md assumption A2 by ruling whether bounded-repair checkpointing reuses the Phase 199 pause/resume handoff-ID idempotency pattern (colony.PauseHandoff.HandoffID) or requires a distinct primitive.

Include a traceability section mapping each of D-01 through D-16 to its SYN-201 rows and to the named implementation plan that delivers it (201-02 through 201-14). Include a rejected-alternatives table that names parallel-wave expansion as rejected per D-15 and automatic self-tuning of routing as deferred per D-16. Mark Phase 202, 203, 204 and 205 capabilities as explicit boundaries, not omissions. Do not modify the Dreams report or any historical evidence file.</action>
  <acceptance_criteria>
    - The document contains each identifier `SYN-201-01` through `SYN-201-14` exactly once as a decision heading or row identifier.
    - Each of CAP-003, CAP-004, CAP-022, CAP-024, CAP-029, CAP-051, CAP-066 and CAP-071 appears in a routed coverage table with exactly one disposition and at least one cited source path.
    - Each of D-01 through D-16 appears in the traceability section next to at least one SYN-201 identifier and one plan number in the range 201-02 to 201-14.
    - The Classic reconstruction table contains at least one `git show` citation per OLD row, and the current audit table contains at least one `path:line` citation per NOW row.
    - The open-decisions table is present and either lists an item with a named authority or states that no evidence-unresolved owner choice remains.
    - Front matter sets `phase: "201"` and `area: "work-cycle"`.
  </acceptance_criteria>
  <verify>
    <automated>SYN=.planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md; test -f "$SYN" &amp;&amp; for i in 01 02 03 04 05 06 07 08 09 10 11 12 13 14; do test "$(grep -c "SYN-201-$i" "$SYN")" -ge 1 || exit 1; done &amp;&amp; for c in 003 004 022 024 029 051 066 071; do grep -q "CAP-$c" "$SYN" || exit 1; done &amp;&amp; for d in 01 02 03 04 05 06 07 08 09 10 11 12 13 14 15 16; do grep -q "D-$d" "$SYN" || exit 1; done &amp;&amp; grep -q "git show" "$SYN"</automated>
  </verify>
  <done>One evidence-backed brief covers every Phase 201 mechanism, locked decision and routed capability, so no later plan can be justified by a feature name alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register SYN-201 mechanisms in the versioned Classic corpus</name>
  <files>cmd/testdata/classic-contract/v1/schema.json, cmd/testdata/classic-contract/v1/mechanisms.json, cmd/classic_contract_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/testdata/classic-contract/v1/schema.json
    - cmd/testdata/classic-contract/v1/mechanisms.json
    - cmd/classic_contract_test.go
    - .planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md
  </read_first>
  <behavior>
    - A mechanism entry whose id is outside the registered SYN-199, SYN-200 and SYN-201 sets fails registry validation and the error names the offending identifier.
    - A case declaring `synthesis_decision` of `SYN-201-07` and group `V-201-OUTCOME` validates against the schema.
    - A case declaring a SYN-201 decision that no mechanism entry registers fails coverage validation by name.
    - Every SYN-201 mechanism entry names its CAP identifiers, public commands, source citations and owning groups.
  </behavior>
  <action>Widen `schema.json`'s `synthesis_decision` pattern from its current `^SYN-(?:199-(?:0[1-9]|10)|200-(?:0[1-9]|1[0-2]))$` to additionally accept `201-(?:0[1-9]|1[0-4])`. Add these group enum members alongside the existing ones without removing any: `V-201-BOUNDARY`, `V-201-OUTCOME`, `V-201-IDENTITY`, `V-201-REPAIR`, `V-201-AUTOPILOT`, `V-201-TELEMETRY`.

Append fourteen mechanism entries `SYN-201-01` through `SYN-201-14` to `mechanisms.json`, each carrying the required registry fields `id`, `title`, `disposition`, `cap_ids`, `public_commands`, `source_citations` and `groups`, with values taken verbatim from the synthesis Task 1 produced. Add `.planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md` to the `synthesis_sources` array; leave the legacy scalar `synthesis_source` key untouched. Public commands for these entries are the owner-facing spellings `/ant-build`, `/ant-continue`, `/ant-run`, `/ant-status` and `/ant-quick`, matching the vocabulary Phase 199 locked.

Extend `cmd/classic_contract_test.go` with a `TestClassicContractPhase201MechanismRegistry` that loads the registry through the existing strict loader, asserts the fourteen SYN-201 identifiers are present exactly once each, asserts every entry names at least one CAP identifier and at least one source citation, and proves by negative fixture that an unregistered SYN identifier and a case referencing an unregistered decision each fail with the offending identifier in the message. Do not add executable behavior cases here — plan 201-14 owns the Phase 201 case set once the behaviors exist.</action>
  <acceptance_criteria>
    - `python3 -c "import json;json.load(open('cmd/testdata/classic-contract/v1/schema.json'))"` succeeds and the pattern string contains `201-`.
    - The registry contains exactly fourteen entries whose id begins `SYN-201-`, and the pre-existing twenty-two SYN-199/SYN-200 entries are all still present.
    - `synthesis_sources` contains the Phase 201 synthesis path.
    - The negative fixtures fail with the offending identifier named in the error text.
    - `TestClassicContractSchema` and `TestClassicMechanismCoverage` still pass unchanged.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestClassicContractSchema|TestClassicMechanismCoverage|TestClassicContractPhase201MechanismRegistry)$' -count=1 &amp;&amp; python3 -c "import json,sys; m=json.load(open('cmd/testdata/classic-contract/v1/mechanisms.json')); ids=[x['id'] for x in m['mechanisms']]; sys.exit(0 if len([i for i in ids if i.startswith('SYN-201-')])==14 and len([i for i in ids if i.startswith('SYN-199-')])==10 and len([i for i in ids if i.startswith('SYN-200-')])==12 else 1)"</automated>
  </verify>
  <done>The executable Classic corpus can carry Phase 201 mechanism identities, so later plans register cases against a contract that already exists rather than inventing one late.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created by this plan (new to the codebase — not drift):

- `.planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md` (new file)
- Mechanism identifiers `SYN-201-01` … `SYN-201-14` (new registry entries)
- Corpus group names `V-201-BOUNDARY`, `V-201-OUTCOME`, `V-201-IDENTITY`, `V-201-REPAIR`, `V-201-AUTOPILOT`, `V-201-TELEMETRY` (new schema enum members)
- `TestClassicContractPhase201MechanismRegistry` (new Go test)

<verification>
- The synthesis covers SYNTH-03's full scope: Queen judgment, caste selection, job and wave coordination, build, continue, run, checks, recovery, context relay and ceremony.
- All eight routed CAP rows carry an independently adjudicated disposition.
- The corpus schema and registry accept SYN-201 identifiers and reject unknown ones by name.
- Existing Phase 199 and Phase 200 corpus tests remain green.
</verification>

<success_criteria>
- No later Phase 201 plan can be executed without a cited disposition to point at.
- The three RESEARCH.md open questions and assumption A2 are decided in writing, not left to an executor's improvisation.
</success_criteria>

<output>
Create `.planning/phases/201-queen-led-work-cycle/201-01-SUMMARY.md` when done.
</output>
