---
phase: 201-queen-led-work-cycle
plan: "02"
type: execute
wave: 2
depends_on: ["201-01"]
files_modified:
  - cmd/codex_verify_advance.go
  - cmd/codex_verify_advance_test.go
  - cmd/codex_continue.go
  - cmd/codex_continue_plan.go
  - cmd/codex_continue_finalize.go
autonomous: true
requirements: [WORK-04, CEC-06]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One Go function body decides continue acceptance, verification consumption and phase advancement, and all three continue lanes reach that decision only by calling it (D-04, WORK-04)."
    - "The direct lane, the plan-only/snapshot lane and the external finalize lane produce the same advancement decision for the same normalized inputs, so lane parity is structural rather than hand-maintained."
    - "Supplying a reconciliation record is accepted or refused identically on every lane; the finalize lane no longer refuses what the direct lane accepts."
    - "Untrusted external completion JSON is parsed and contract-validated only by the finalize lane; the shared decision body never sees unvalidated input."
    - "Evidence written by an interrupted run stays bound to the attempt that produced it, and a concurrently running lane never adopts or overwrites another attempt's evidence (CEC-06)."
    - "runDeterministicFloor remains the only source of a pass on every lane, including the plan-only and finalize lanes."
  artifacts:
    - path: "cmd/codex_verify_advance.go"
      provides: "The single accept/verify/advance decision body for every continue lane"
      contains: "runContinueAcceptVerifyAdvance"
    - path: "cmd/codex_verify_advance_test.go"
      provides: "Three-lane parity proof and single-body AST guard"
      contains: "TestAllThreeContinueLanesShareOneAcceptVerifyAdvanceBody"
  key_links:
    - from: "cmd/codex_continue_finalize.go"
      to: "cmd/codex_verify_advance.go"
      via: "external lane calls the shared decision body after its own contract validation"
      pattern: "runContinueAcceptVerifyAdvance"
    - from: "cmd/codex_continue_plan.go"
      to: "cmd/codex_verify_advance.go"
      via: "snapshot lane calls the shared decision body"
      pattern: "runContinueAcceptVerifyAdvance"
  prohibitions:
    - "Never add a fourth result-envelope type that each lane translates into."
    - "Never let a dispatched reviewer verdict supply a pass; it may only add a block."
    - "Never weaken the deterministic floor to make lane collapse easier."
---

<objective>
Collapse the three continue lanes onto one authoritative accept/verify/advance decision body, proven end-to-end on one real path before any other Phase 201 work builds on it.

Purpose: `.planning/codebase/CONCERNS.md` names three separate continue implementations that must stay behaviourally identical with no test asserting end-to-end parity, and names one already-live consequence — the finalize lane refuses a reconciliation the direct lane accepts. Every later plan in this phase adds a rule to the continue path; adding those rules three times is how the divergence grows. This is the phase's architectural spine, so it is proved first, on the executor's freshest context.
Output: `cmd/codex_verify_advance.go` holding the one decision body, three thin lane callers, and a guard that refuses a fourth.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/201-queen-led-work-cycle/201-CONTEXT.md
@.planning/phases/201-queen-led-work-cycle/201-RESEARCH.md
@.planning/phases/201-queen-led-work-cycle/201-PATTERNS.md
@.planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md
@.planning/codebase/CONCERNS.md
@cmd/deterministic_floor.go
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "one continue decision, three lanes" — the direct path only</name>
  <files>cmd/codex_verify_advance.go, cmd/codex_verify_advance_test.go, cmd/codex_continue.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/codex_verify_advance.go (create; confirm absent)
    - cmd/deterministic_floor.go
    - cmd/codex_continue.go
    - cmd/codex_continue_plan.go
    - cmd/codex_continue_finalize.go
    - cmd/build_attempt.go
    - .planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md
    - cmd/deterministic_floor_test.go
  </read_first>
  <reversibility rating="costly">Collapsing three lanes onto one body is hard to unwind: undoing it means re-splitting the assessment, gate-evaluation and advancement logic back across three files and reinstating the manual-parity burden the collapse removes. This matches the owner's own rating of D-04.</reversibility>
  <behavior>
    - Given a normalized manifest, verification report and assessment for a passing phase, the shared body returns advance with the phase identified and the deterministic floor named as the pass source.
    - Given the same inputs with a failing deterministic floor, the shared body returns block with the failing check named, and a passing reviewer verdict does not change that.
    - Given a passing floor plus a blocking reviewer finding, the shared body returns block.
    - Given a supplied reconciliation record, the shared body applies one acceptance rule regardless of which lane supplied it.
  </behavior>
  <action>Create `cmd/codex_verify_advance.go` containing `runContinueAcceptVerifyAdvance`, a pure decision function taking already-normalized inputs — the existing shared types `codexContinueManifest`, `codexContinueVerificationReport`, `codexContinueAssessment`, the deterministic floor result, the reviewer report and the current `colony.ColonyState` — and returning one typed decision value carrying the advancement verdict, the phase advanced to when advancing, the named pass source, the ordered blocking reasons, and the accepted reconciliation disposition. Model the doc comment on `runDeterministicFloor` in `cmd/deterministic_floor.go`: state in the comment which callers are the thin wrappers around this body and that lane parity is therefore structural.

Do not create a new envelope type for results. Operate on the types already shared across `codex_continue.go`, `codex_continue_plan.go` and `codex_continue_finalize.go`. Move the decision logic out of `runCodexContinue` — assessment consumption, gate evaluation via `runCodexContinueGates`/`runCodexContinueGatesWithAutopilotBaseline`, reviewer-verdict folding, and the advancement determination — into this body. Keep in `codex_continue.go` only the lane-local work: reading state, dispatching reviewers in-process, and writing the result envelope.

Preserve every existing invariant exactly: `runDeterministicFloor` stays the sole source of a pass; a reviewer verdict can only add a block; scoped verification commands keep their existing derivation. Wire the direct lane through the new body in this task and prove the whole path end to end. Leave the other two lanes untouched here — Task 2 owns them.</action>
  <acceptance_criteria>
    - `cmd/codex_verify_advance.go` declares exactly one top-level `runContinueAcceptVerifyAdvance` function.
    - `runCodexContinue` in `cmd/codex_continue.go` reaches its advancement verdict only through `runContinueAcceptVerifyAdvance`; no advancement verdict is computed inline in that function afterwards.
    - `TestDeterministicFloorIsTheOnlySourceOfAPass` and `TestBothContinueLanesApplyTheSameFloor` still pass unmodified.
    - A phase fixture whose deterministic floor fails yields a block verdict naming the failing check even when the reviewer report carries a pass.
    - The end-to-end direct-lane test drives the real `aether continue` code path against an isolated fixture root and asserts both the rendered verdict and the resulting durable phase status.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestDeterministicFloorIsTheOnlySourceOfAPass|TestBothContinueLanesApplyTheSameFloor|TestDirectContinueLaneReachesAdvancementThroughTheSharedBody)$' -count=1</automated>
  </verify>
  <done>The single happy path — a passing phase advancing through the shared decision body on the direct lane — works end to end against a real fixture and is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route the snapshot and external finalize lanes through the same body</name>
  <files>cmd/codex_continue_plan.go, cmd/codex_continue_finalize.go, cmd/codex_verify_advance_test.go</files>
  <read_first>
    - cmd/codex_continue_plan.go
    - cmd/codex_continue_finalize.go
    - cmd/codex_verify_advance.go
    - cmd/codex_continue.go
    - .planning/codebase/CONCERNS.md
  </read_first>
  <behavior>
    - The same normalized inputs produce the same decision value on the direct lane, the snapshot lane and the finalize lane.
    - A completion packet that supplies a reconciliation record is accepted on the finalize lane exactly as the direct lane accepts it.
    - A malformed or contract-violating external completion packet is refused by the finalize lane before the shared body is called, and the refusal names the contract violation.
    - Two lanes operating concurrently on different attempts never read or write each other's attempt-bound evidence.
  </behavior>
  <action>Make `runCodexContinuePlanOnly` and `runCodexContinueVerificationSnapshot` in `cmd/codex_continue_plan.go`, and `runCodexContinueFinalize` in `cmd/codex_continue_finalize.go`, reach their advancement verdict only by calling `runContinueAcceptVerifyAdvance`. Each lane keeps its own input responsibilities and nothing more: the finalize lane keeps `loadExternalContinueCompletion`, `validateExternalContinueState` and its completion-contract validation, running them before the shared call so untrusted JSON never reaches the shared body; the snapshot lane keeps its own snapshot assembly.

Fix the named divergence while doing so: the finalize lane's contract validation currently rejects a supplied reconciliation record that the direct lane accepts. Single-source that acceptance rule inside `runContinueAcceptVerifyAdvance` and delete the lane-local rejection, so reconciliation acceptance has exactly one definition.

Add a three-lane parity test that builds one normalized input set, drives it through all three lane entry points against isolated fixture roots, and asserts the returned decisions are equal field by field — including the pass source, the ordered blocking reasons and the reconciliation disposition. Add a concurrency case asserting that two lanes running against two different attempt identities each write only their own attempt-bound evidence and that an interrupted run leaves its evidence bound to its own attempt.</action>
  <acceptance_criteria>
    - `runCodexContinuePlanOnly`, `runCodexContinueVerificationSnapshot` and `runCodexContinueFinalize` each contain at least one call to `runContinueAcceptVerifyAdvance`.
    - A finalize fixture supplying a reconciliation record advances, and the identical input advances on the direct lane, with equal decision values.
    - A finalize fixture whose completion packet violates the completion contract is refused before the shared body runs, with the violation named.
    - The concurrency case shows each attempt's evidence file referencing only its own attempt identifier.
    - The full `./cmd` package builds and `go vet ./cmd` is clean.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestThreeContinueLanesProduceOneDecision|TestFinalizeLaneAcceptsReconciliationLikeTheDirectLane|TestConcurrentLanesKeepEvidenceAttemptBound)$' -count=1 &amp;&amp; go vet ./cmd</automated>
  </verify>
  <done>All three continue lanes reach acceptance, verification consumption and advancement through one body, and the reconciliation divergence CONCERNS.md named is closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard the single decision body against a fourth implementation</name>
  <files>cmd/codex_verify_advance_test.go</files>
  <read_first>
    - cmd/codex_verify_advance.go
    - cmd/codex_verify_advance_test.go
    - cmd/boundary_double_dispatch_test.go
    - cmd/deterministic_floor_test.go
  </read_first>
  <behavior>
    - A newly added function in package `cmd` that computes an advancement verdict without calling the shared body fails the guard, and the failure names the offending function.
    - Adding a fourth continue entry point that does not call the shared body fails the guard by name.
    - The guard counts declarations from the parsed Go syntax tree, not from a maintained list of today's function names.
  </behavior>
  <action>Add `TestAllThreeContinueLanesShareOneAcceptVerifyAdvanceBody` to `cmd/codex_verify_advance_test.go`. Parse the `cmd` package with `go/parser` and `go/ast`, following the AST-guard precedent already used in this package for single-boundary invariants. Assert that exactly one top-level function named `runContinueAcceptVerifyAdvance` is declared; that each of the three known lane entry points calls it; and that no other top-level function in the package both consumes a `codexContinueAssessment` and returns or assigns an advancement verdict without a call to it. Report any violation by function name and file position. Derive the lane entry points from the call graph reaching the shared body rather than from a hardcoded name list, so a fourth lane added later is caught rather than silently excluded.</action>
  <acceptance_criteria>
    - The guard fails, naming the offending function, when a temporary fixture function computing an advancement verdict without the shared call is present.
    - The guard passes against the real package after Task 2.
    - The guard's violation message includes file and line position.
    - The guard does not read any hand-maintained list of function names as its authority.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestAllThreeContinueLanesShareOneAcceptVerifyAdvanceBody$' -count=1</automated>
  </verify>
  <done>A future fourth continue path cannot quietly grow its own advancement logic; the parity that was hand-maintained is now enforced by the compiler's own syntax tree.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created by this plan (new to the codebase — not drift):

- `cmd/codex_verify_advance.go`, `cmd/codex_verify_advance_test.go` (new files)
- `runContinueAcceptVerifyAdvance` (new function) and its returned decision type
- `TestDirectContinueLaneReachesAdvancementThroughTheSharedBody`, `TestThreeContinueLanesProduceOneDecision`, `TestFinalizeLaneAcceptsReconciliationLikeTheDirectLane`, `TestConcurrentLanesKeepEvidenceAttemptBound`, `TestAllThreeContinueLanesShareOneAcceptVerifyAdvanceBody` (new tests)

<verification>
- Three lanes, one decision body, proven by an equality test across real entry points rather than by review.
- The deterministic floor keeps its existing role untouched on every lane.
- The reconciliation divergence named in CONCERNS.md is closed with one acceptance rule.
- An AST guard refuses a fourth advancement implementation by name.
</verification>

<success_criteria>
- Every later Phase 201 plan adds its rule in one place instead of three.
- `go test ./cmd` and `go vet ./cmd` are clean.
</success_criteria>

<output>
Create `.planning/phases/201-queen-led-work-cycle/201-02-SUMMARY.md` when done.
</output>
