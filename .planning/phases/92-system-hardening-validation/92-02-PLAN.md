---
phase: 92-system-hardening-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/colony_prime_context.go
  - cmd/colony_prime_audit_test.go
  - cmd/context_freshness_test.go
  - pkg/codex/process_tracker_test.go
  - pkg/codex/process_group_unix_test.go
  - cmd/codex_worker_cleanup_test.go
autonomous: true
requirements:
  - SAFE-05
  - SAFE-06
  - PLAT-04
  - PLAT-05
  - PLAT-06

must_haves:
  truths:
    - "Worker prompts include all AAC-005 context sections: colony-prime, prompt_section, survey context, phase research, matched skills, midden/graveyard cautions"
    - "Colony-prime context is assembled fresh per worker spawn, not cached from session start"
    - "Workers spawn in process groups with Setpgid on Unix"
    - "Worker PIDs are tracked and killed on exit (SIGTERM then SIGKILL)"
    - "Stale workers from previous sessions are detected and cleaned before dispatch"
  artifacts:
    - path: "cmd/colony_prime_audit_test.go"
      provides: "Audit test verifying all AAC-005 sections reach workers"
      contains: "TestColonyPrimeAAC005Audit"
    - path: "cmd/context_freshness_test.go"
      provides: "Freshness verification test"
      contains: "TestContextFreshPerDispatch"
    - path: "pkg/codex/process_tracker_test.go"
      provides: "Verification tests for PID tracking and cleanup"
      contains: "TestProcessTracker"
    - path: "pkg/codex/process_group_unix_test.go"
      provides: "Verification tests for process group management"
      contains: "TestProcessGroup"
    - path: "cmd/codex_worker_cleanup_test.go"
      provides: "Verification tests for stale worker cleanup"
      contains: "TestStaleWorkerCleanup"
  key_links:
    - from: "cmd/codex_build.go"
      to: "cmd/colony_prime_context.go"
      via: "resolveCodexWorkerContext() -> buildColonyPrimeOutput()"
      pattern: "resolveCodexWorkerContext"
    - from: "cmd/codex_build.go"
      to: "cmd/codex_build.go"
      via: "resolveSkillSectionForWorkflow -> skill injection"
      pattern: "resolveSkillSectionForWorkflow"
    - from: "cmd/codex_build.go"
      to: "cmd/context.go"
      via: "buildContextCapsuleOutput for midden/survey context"
      pattern: "buildContextCapsuleOutput"
---

<objective>
Audit colony-prime context completeness against AAC-005 and verify existing worker lifecycle infrastructure works correctly (PLAT-04/05/06).

Purpose: SAFE-05 requires all v5.4 context sections to reach workers. SAFE-06 requires fresh assembly per spawn. PLAT-04/05/06 are already implemented but need verification tests. The audit determines whether sections are missing from colony-prime but delivered through other paths (context capsule, skill injection, task brief).

Output: Audit test proving all AAC-005 sections reach workers, freshness test, and verification tests for existing process management code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-system-hardening-validation/92-CONTEXT.md
@.planning/phases/92-system-hardening-validation/92-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From cmd/colony_prime_context.go:
```go
func buildColonyPrimeOutput(compact bool) colonyPrimeOutput
func resolveCodexWorkerContext() string
type colonyPrimeOutput struct {
    Context       string `json:"context"`
    PromptSection string `json:"prompt_section"`
    Sections      int    `json:"sections"`
    Budget        int    `json:"budget"`
    Used          int    `json:"used"`
    // ... 15 sections: state, review_depth, pheromones, instincts, decisions, learnings,
    //     hive_wisdom, learned_memory, global_queen_md, user_preferences, prior_reviews,
    //     local_queen_wisdom, clarified_intent, blockers, medic_health
}
```

From cmd/codex_build.go:
```go
// Full worker prompt assembly at lines 926-942:
// 1. capsule := resolveCodexWorkerContext()       -- colony-prime + context capsule fallback
// 2. pheromoneSection := resolvePheromoneSection() -- pheromone signals
// 3. TaskBrief: renderCodexBuildWorkerBrief(...)   -- task assignment
// 4. SkillSection: resolveSkillSectionForWorkflow(...) -- matched skills
// WorkerDispatch combines: ContextCapsule + SkillSection + PheromoneSection + TaskBrief
```

From cmd/context.go:
```go
// buildContextCapsuleOutput() includes midden data at line ~817
// Midden entries loaded from midden/midden.json, sorted by timestamp, limited to 10
```

From pkg/codex/worker.go:
```go
type WorkerConfig struct {
    ContextCapsule   string  // colony-prime context
    SkillSection     string  // matched skills
    PheromoneSection string  // pheromone signals
    TaskBrief        string  // task brief with assignment
}
```

AAC-005 required sections:
1. colony-prime     -> resolveCodexWorkerContext() -> buildColonyPrimeOutput()
2. prompt_section   -> same as colony-prime (result.PromptSection)
3. survey context   -> buildContextCapsuleOutput() fallback or renderCodexBuildWorkerBrief()
4. phase research   -> renderCodexBuildWorkerBrief() playbooks section OR phase.Description
5. matched skills   -> resolveSkillSectionForWorkflow() -> WorkerConfig.SkillSection
6. midden/graveyard -> buildContextCapsuleOutput() midden section (context.go line ~817)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AAC-005 context completeness audit test + SAFE-06 freshness test</name>
  <files>cmd/colony_prime_audit_test.go, cmd/context_freshness_test.go</files>
  <read_first>
    - cmd/colony_prime_context.go (full file -- buildColonyPrimeOutput with all 15 sections, resolveCodexWorkerContext)
    - cmd/context.go (lines 800-850 -- midden section in context capsule)
    - cmd/codex_build.go (lines 918-950 -- executeCodexBuildDispatches: how capsule, skills, pheromones are assembled into WorkerDispatch)
    - cmd/codex_build.go (lines 1412-1511 -- renderCodexBuildWorkerBrief: task brief with playbooks)
    - cmd/skills.go (resolveSkillSectionForWorkflow function)
  </read_first>
  <behavior>
    - Test 1 (AAC-005 Audit): Create a fully populated test store with all data sources (COLONY_STATE.json, pheromones.json, instincts.json, midden/midden.json, survey/, phase research). Call resolveCodexWorkerContext(). Verify the output contains colony-prime content. Verify that WorkerDispatch assembly includes: ContextCapsule (from resolveCodexWorkerContext), SkillSection (from resolveSkillSectionForWorkflow), PheromoneSection (from resolvePheromoneSection), TaskBrief (from renderCodexBuildWorkerBrief which includes playbooks/phase research). Document which AAC-005 sections come from which assembly path.
    - Test 2 (Freshness): Call resolveCodexWorkerContext() twice with different colony state data between calls. Verify the second call returns updated content reflecting the changed state, proving no session-level caching of the assembly result.
    - Test 3 (Freshness - session cache is data-only): Verify that SessionCache (24h TTL) caches individual data file reads but NOT the final assembled output. This means buildColonyPrimeOutput() re-assembles from (possibly cached) data reads each time.
    - Test 4 (Section presence): Verify buildColonyPrimeOutput produces sections for each populated data source: state, pheromones, instincts, decisions, learnings, hive_wisdom, learned_memory, global_queen_md, user_preferences, prior_reviews, local_queen_wisdom, clarified_intent, blockers, medic_health.
    - Test 5 (Missing section graceful): Verify buildColonyPrimeOutput returns valid output even when most data sources are empty (no pheromones, no instincts, no decisions, etc.). Only colony state is required.
  </behavior>
  <action>
Per D-04 and D-05, create two test files:

**1. `cmd/colony_prime_audit_test.go`** -- AAC-005 completeness audit:

```go
func TestColonyPrimeAAC005Audit(t *testing.T)
```
- Create a temp directory with full test data: COLONY_STATE.json (with goal, phases, decisions, learnings), pheromones.json (with active signals), instincts.json, midden/midden.json (with entries), survey/ files.
- Initialize the global `store` with the temp directory (use existing `saveGlobals` / `initRecoverTestStore` patterns).
- Call `buildColonyPrimeOutput(false)`.
- Verify the output contains sections for each of the 15 colony-prime sections.
- Then simulate the full WorkerDispatch assembly path:
  a. `capsule := resolveCodexWorkerContext()` -- colony-prime (AAC-005: colony-prime, prompt_section)
  b. `pheromoneSection := resolvePheromoneSection()` -- pheromone signals
  c. `skillSection := resolveSkillSectionForWorkflow("build", "builder", "implement auth")` -- matched skills (AAC-005: matched skills)
  d. Verify capsule contains midden data if midden entries exist (AAC-005: midden/graveyard cautions)
  e. Verify that `renderCodexBuildWorkerBrief()` includes playbooks/phase info (AAC-005: survey context, phase research)
- Document in the test the mapping:
  | AAC-005 Section | Delivery Path |
  |-----------------|---------------|
  | colony-prime | resolveCodexWorkerContext() -> buildColonyPrimeOutput() |
  | prompt_section | Same as colony-prime (result.PromptSection) |
  | survey context | renderCodexBuildWorkerBrief() + buildContextCapsuleOutput() fallback |
  | phase research | renderCodexBuildWorkerBrief() playbooks section |
  | matched skills | resolveSkillSectionForWorkflow() -> WorkerConfig.SkillSection |
  | midden/graveyard | buildContextCapsuleOutput() midden section |

If any AAC-005 section is NOT reaching the worker through ANY path, the test must FAIL with a clear message identifying the missing section. Per RESEARCH, the expectation is that all sections DO reach the worker through the combined assembly, but the audit must prove it.

```go
func TestColonyPrimeSectionsPresent(t *testing.T)
```
- Verify all 15 sections appear when data sources are populated.

```go
func TestColonyPrimeGracefulWithMissingData(t *testing.T)
```
- Verify output is valid with minimal data (only COLONY_STATE.json).

**2. `cmd/context_freshness_test.go`** -- SAFE-06 freshness:

```go
func TestContextFreshPerDispatch(t *testing.T)
```
- Step 1: Initialize store with a colony state having Goal = "First goal"
- Step 2: Call resolveCodexWorkerContext(), capture result1
- Step 3: Modify COLONY_STATE.json to change Goal to "Second goal"
- Step 4: Clear session cache (or use a new cache instance) to simulate fresh data reads
- Step 5: Call resolveCodexWorkerContext() again, capture result2
- Step 6: Verify result2 contains "Second goal" and NOT "First goal"
- This proves the assembly is not session-cached.

```go
func TestSessionCacheCachesDataNotAssembly(t *testing.T)
```
- Verify that `cache.NewSessionCache` caches individual file reads but `buildColonyPrimeOutput` re-assembles from those reads each call.
- Call buildColonyPrimeOutput twice. Modify underlying data between calls. Clear session cache between calls. Verify second call gets fresh data.
  </action>
  <verify>
    <automated>go test ./cmd/... -run "TestColonyPrimeAAC005|TestColonyPrimeSections|TestColonyPrimeGraceful|TestContextFresh" -v -count=1</automated>
  </verify>
  <done>
    - AAC-005 audit test proves all 6 required sections reach workers through the combined assembly path
    - Test documents which delivery path carries each section
    - Freshness test proves context is re-assembled per dispatch (not session-cached)
    - Graceful degradation test proves output works with minimal data
    - If any section is truly missing, the test fails with actionable message
  </done>
</task>

<task type="auto">
  <name>Task 2: Verification tests for existing process group, PID tracking, and stale worker cleanup (PLAT-04/05/06)</name>
  <files>pkg/codex/process_tracker_test.go, pkg/codex/process_group_unix_test.go, cmd/codex_worker_cleanup_test.go</files>
  <read_first>
    - pkg/codex/process_tracker.go (full file: TrackProcess, UntrackProcess, KillAll, DetectStaleWorkers, CleanupStaleWorkers, readTrackedProcesses, writeTrackedProcesses)
    - pkg/codex/process_group_unix.go (full file: workerSysProcAttr, terminateWorkerProcess, killWorkerProcess, workerProcessExists)
    - cmd/codex_worker_cleanup.go (full file: cleanupStaleWorkersBeforeDispatch)
  </read_first>
  <acceptance_criteria>
    - `grep -c "TestProcessTracker" pkg/codex/process_tracker_test.go` >= 1
    - `grep -c "TestProcessGroup" pkg/codex/process_group_unix_test.go` >= 1
    - `grep -c "TestStaleWorker" cmd/codex_worker_cleanup_test.go` >= 1
    - All new tests pass: `go test ./pkg/codex/... ./cmd/... -run "TestProcessTracker|TestProcessGroup|TestStaleWorker" -v -count=1`
  </acceptance_criteria>
  <action>
Per RESEARCH finding: PLAT-04 and PLAT-05 are already fully implemented in `pkg/codex/process_group_unix.go` and `pkg/codex/process_tracker.go`. PLAT-06 stale worker cleanup exists in `cmd/codex_worker_cleanup.go`. These need verification tests, not new code.

**1. `pkg/codex/process_tracker_test.go`** (PLAT-05):
Write tests verifying:
- `TestProcessTrackerTrackUntrack`: Track a process, verify it appears in the tracker, untrack it, verify it is removed
- `TestProcessTrackerKillAll`: Track multiple processes, call KillAll with empty root (kills all), verify cleanup. Use mock functions for terminateWorkerFunc/killWorkerFunc since actual process killing requires real PIDs. Set `workerProcessExistsFunc` to always return false (process already gone) to avoid actual kill attempts.
- `TestProcessTrackerPersistRead`: Track a process with a root, verify the JSON file is written to `.aether/data/worker-processes.json`, read it back with `readTrackedProcesses`, verify data matches
- `TestProcessTrackerDetectStaleWorkers`: Write a `worker-processes.json` with processes NOT in the current tracker (simulating a previous session). Call `DetectStaleWorkers`. Verify stale processes are detected. Use mock `workerProcessExistsFunc` returning true and `workerProcessCommandFunc` returning "codex" to pass `isKnownWorkerProcess` check.
- `TestProcessTrackerCleanupStaleWorkers`: Similar to Detect but calls `CleanupStaleWorkers` and verifies the kill logic runs (with mocked kill functions).

**2. `pkg/codex/process_group_unix_test.go`** (PLAT-04):
Write tests verifying:
- `TestProcessGroupSetpgid`: Call `workerSysProcAttr()` and verify the returned `syscall.SysProcAttr` has `Setpgid: true`. This confirms process groups are configured.
- `TestProcessGroupTerminateKill`: These functions call `syscall.Kill(-pid, ...)`. Since we cannot safely test with real PIDs in unit tests, verify the functions exist and have correct signatures. For the actual kill behavior, rely on the E2E integration test in Plan 03.

**3. `cmd/codex_worker_cleanup_test.go`** (PLAT-06):
Write tests verifying:
- `TestStaleWorkerCleanupBeforeDispatch`: Call `cleanupStaleWorkersBeforeDispatch(root)` with a temp directory. Verify it calls through to `codex.CleanupStaleWorkers(root)` and handles the result correctly. When there are no stale workers, verify no visual output. When there are stale workers, verify visual progress is emitted.
- `TestStaleWorkerCleanupEmptyRoot`: Call with empty string, verify it returns without error.
- `TestStaleWorkerCleanupIntegration`: Seed stale worker processes in a temp dir's `worker-processes.json`, call cleanup, verify stale processes are cleaned up.

Note: Use existing test helpers (`saveGlobals`, `resetRootCmd`, etc.) from the codebase. Mock process existence functions where needed to avoid depending on real processes.
  </action>
  <verify>
    <automated>go test ./pkg/codex/... ./cmd/... -run "TestProcessTracker|TestProcessGroup|TestStaleWorker" -v -count=1</automated>
  </verify>
  <done>
    - Process tracker tests verify TrackProcess, UntrackProcess, KillAll, file persistence, stale detection, cleanup
    - Process group test verifies Setpgid configuration
    - Stale worker cleanup tests verify before-dispatch cleanup works correctly
    - All tests pass without requiring real worker processes
    - Existing tests continue to pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go runtime -> data files | Trusted data files read during context assembly |
| Go runtime -> worker-processes.json | File-based PID persistence (trusted but could be stale) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-04 | Tampering | worker-processes.json | accept | File is local, trusted; stale entries are handled by isKnownWorkerProcess check |
| T-92-05 | Information Disclosure | colony-prime context | accept | Context contains colony state, not secrets; privacy scan already exists for learning writes |
</threat_model>

<verification>
go test ./cmd/... -run "TestColonyPrimeAAC005|TestColonyPrimeSections|TestColonyPrimeGraceful|TestContextFresh" -v -count=1
go test ./pkg/codex/... ./cmd/... -run "TestProcessTracker|TestProcessGroup|TestStaleWorker" -v -count=1
go test ./... -race -count=1
</verification>

<success_criteria>
- AAC-005 audit test documents and proves all 6 required sections reach workers
- Freshness test proves context is re-assembled per dispatch
- Process tracker tests cover Track/Untrack/KillAll/persistence/stale-detection
- Process group test verifies Setpgid configuration
- Stale worker cleanup tests verify before-dispatch cleanup
- All tests pass with race detection
- No new production code changes required for PLAT-04/05/06 (verification only)
</success_criteria>

<output>
After completion, create `.planning/phases/92-system-hardening-validation/92-02-SUMMARY.md`
</output>
