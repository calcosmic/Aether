---
phase: 163.2-ts-host-preflight-configurability
verified: 2026-08-01T00:00:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
---

# Phase 163.2: ts-host preflight configurability — Verification Report

**Phase Goal:** The paid provider preflight probe stops running on every dispatch and stops being un-tunable: a success is cached per platform for a configurable trust window (default 1h), both hosts honour one `AETHER_PREFLIGHT_TIMEOUT` knob with the same 45s default, the probe runs in a neutral temp directory instead of the repo, a deliberate skip announces itself, and a provider/auth failure clears the window immediately.

**Verified:** 2026-08-01
**Status:** passed
**Re-verification:** No — initial verification (post-code-review-fix)

## Goal Achievement

### Observable Truths (mapped to D-01..D-07 decision IDs from 163.2-CONTEXT.md)

| # | Truth (Decision ID) | Status | Evidence |
|---|---|---|---|
| 1 | D-01: Time-based per-platform preflight success cache in a runtime state file | ✓ VERIFIED | `cmd/preflight_cache.go` implements `preflightCacheEntry`/`preflightCacheFile` map keyed by platform, persisted at `.aether/data/preflight-cache.json` via `store.SaveJSON`/`UpdateJSONAtomically`. `gatedProviderPreflight` (cmd/preflight_cache.go:330) is called from both dispatch chokepoints (`cmd/dispatch_runtime.go:preflightWorkerProvider`, `cmd/internal_worker_adapter.go` `--preflight` branch), verified with `TestDispatchChokepointsShareOneCache` (PASS) proving a single shared trust window across both hosts, and `TestGatedPreflightSkipsProbeOnWarmCache` (PASS) proving a second dispatch inside the window costs zero probes. |
| 2 | D-02: TTL default 1h, tunable via `AETHER_PREFLIGHT_CACHE_TTL`, invalid values fall back | ✓ VERIFIED | `resolvedPreflightCacheTTL()` (cmd/preflight_cache.go:60) mirrors `resolvedPreflightTimeout`'s safe-fallback idiom; `preflightCacheDefaultTTL = 1 * time.Hour`. `TestResolvedPreflightCacheTTLEnvOverride` (PASS) asserts "90s"->90s, "banana"->default, "-5s"->default, ""->default, and pins the default at 1h. |
| 3 | D-03: Auto-invalidation when a dispatch fails with a provider/auth-classified error | ✓ VERIFIED | `providerAuthFailure` (cmd/preflight_cache.go:391) + `invalidatePreflightCacheOnProviderFailure` (cmd/preflight_cache.go:406), wired into `dispatchBatchByWaveWithVisuals` (cmd/dispatch_runtime.go:93,102) and `cmd/internal_worker_adapter.go:208-209`. `TestAuthFailedDispatchClearsTrustWindow` and `TestOrdinaryFailureKeepsTrustWindow` (both PASS) prove the distinction; `TestProviderAuthFailureClassification` (16 subtests, all PASS) covers all 5 AvailabilityCategory values, 6 auth message strings, and 4 ordinary-failure false-cases. `TestAuthFailureRegexMatchesAcrossHosts` (PASS, added as WR-03 fix) pins Go/TS regex byte-identity. |
| 4 | D-04: The probe stays a real model round-trip; no cheap substitute, no model auto-selection | ✓ VERIFIED | `grep "Return exactly OK"` present unchanged in `pkg/codex/platform_dispatch.go` (Claude, OpenCode), `pkg/codex/worker.go` (Codex), and `.aether/ts-host/src/platform-dispatcher.ts` (all 3 platforms). No model-selection logic found in preflight code paths. `TestPreflightProbeRemainsARealModelRoundTrip` (PASS) guards the payload. |
| 5 | D-05: One shared `AETHER_PREFLIGHT_TIMEOUT` across Go and the TS host; the hardcoded 20s is gone | ✓ VERIFIED | Go: `hostedPreflightTimeout = 45 * time.Second` (pkg/codex/platform_dispatch.go:729), applies to Claude/OpenCode/Codex via the unified `runHostedProviderPreflight`. TS: `PREFLIGHT_DEFAULT_TIMEOUT_MS = 45_000` in the new production module `.aether/ts-host/src/preflight-config.ts` (re-exported from the deprecated `platform-dispatcher.ts`). No `AETHER_PREFLIGHT_TIMEOUT_MS` variable exists anywhere (`grep` returns 0 hits). `TestHostsAgreeOnPreflightDefaultBudget` and `TestNoHardcodedPreflightBudgetRemains` (both PASS) enforce this. Critical review finding CR-01 (TS adapter's 120s Node-side kill budget did not scale with the knob) is fixed: `resolvePreflightAdapterBudgetMs()` in `.aether/ts-host/src/worker-dispatch.ts:490` computes `max(120s, resolvePreflightTimeoutMs() * PREFLIGHT_GO_ATTEMPTS + slack)`, confirmed by 4 passing TS unit tests ("preflight adapter budget" describe block) and committed dist. WR-01 (Go/TS duration-syntax parity) is fixed: `parseGoDurationMs` in `preflight-config.ts` now accepts compound `ms|s|m|h` tokens matching Go's `time.ParseDuration`. |
| 6 | D-06: `AETHER_SKIP_PREFLIGHT` skip switch with a loud, never-silent notice | ✓ VERIFIED | Go: `preflightSkipRequested()` + `emitPreflightSkipNotice` (cmd/preflight_cache.go); skip branch runs before the cache read in `gatedProviderPreflight`, verified by `TestGatedPreflightSkipSwitchBypassesProbeAndCache` (PASS, strengthened to warm the cache first so ordering is actually exercised). TS: `host.ts` checks `AETHER_SKIP_PREFLIGHT` at the top of `preflightHostWorkerDispatch` before either branch. `TestSkipNoticeWordingMatchesAcrossHosts` (PASS) proves byte-identical wording between `cmd/preflight_cache.go` and `.aether/ts-host/src/host.ts`. |
| 7 | D-07: The probe runs in a neutral temp directory, not the repo working tree | ✓ VERIFIED | Go: `makePreflightTempDir` seam (pkg/codex/platform_dispatch.go:742) creates `os.MkdirTemp("", "aether-preflight-")`, wired into `attemptHostedProviderPreflight`; `cmd.Dir = root` no longer appears anywhere in the Go preflight path (the two remaining `cmd.Dir = root` hits are in `execution_binding.go`, unrelated worker-task execution, confirmed pre-existing via `git log`/`git show`). Degradation on temp-dir-creation failure is proven by `TestHostedPreflightSurvivesTempDirFailure` (PASS). TS: `_makePreflightTempDirRef` seam (`.aether/ts-host/src/platform-dispatcher.ts`) mirrors the same pattern with `mkdtempSync(join(tmpdir(), "aether-preflight-"))` and a cwd-fallback on throw. `TestHostedPreflightRunsOutsideRepoWorkingTree`, `TestCodexPreflightRunsOutsideRepoWorkingTree` (Go, PASS) and the TS cwd-isolation + fallback tests (PASS) confirm both hosts. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `cmd/preflight_cache.go` | Cost-policy layer: TTL cache, skip switch, gate, invalidation | ✓ VERIFIED | 418 lines. Contains all 8 plan-01 helpers + `gatedProviderPreflight` (plan 04) + `providerAuthFailure`/`invalidatePreflightCacheOnProviderFailure` (plan 05) + WR-02/WR-05 fixes (`preflightCacheUnmarshalError`, schema-downgrade refusal). |
| `cmd/preflight_cache_test.go` | Invariant tests for TTL, freshness, keying, skip | ✓ VERIFIED | 335 lines, all tests PASS including post-fix `TestPreflightCacheSelfHealsAfterCorruption` and `TestPreflightCacheWritePathsRefuseFutureSchema`. |
| `cmd/dispatch_runtime.go` | Cache-and-skip aware `preflightWorkerProvider` | ✓ VERIFIED | Calls `gatedProviderPreflight` once; calls `invalidatePreflightCacheOnProviderFailure` on both the error-return and normal-return paths. |
| `cmd/internal_worker_adapter.go` | Same gate on TS-hosted path + `preflight` JSON field | ✓ VERIFIED | `Preflight *preflightOutcome` json field present, additive/omitempty, schema version unchanged at 1; `providerAuthFailure`/`clearPreflightCache` wired at line 208-209. |
| `cmd/preflight_gate_test.go` | Probe-count invariants | ✓ VERIFIED | 331 lines, 8 tests, all PASS including `TestDispatchChokepointsShareOneCache`. |
| `cmd/preflight_invalidation_test.go` | Invalidation tests | ✓ VERIFIED | 179 lines, 5 tests, all PASS. |
| `cmd/preflight_docs_test.go` | Doc-coverage + cross-host parity | ✓ VERIFIED | 293 lines, 4 tests (+ WR-03's `TestAuthFailureRegexMatchesAcrossHosts`), all PASS. |
| `pkg/codex/platform_dispatch.go` | Shared probe runner, temp-dir, env timeout | ✓ VERIFIED | `runHostedProviderPreflight`/`attemptHostedProviderPreflight` take `stdin` not `root`; `makePreflightTempDir` seam present. |
| `pkg/codex/worker.go` | Codex `RealInvoker.Preflight` routed through shared runner | ✓ VERIFIED | Line 286: delegates to `runHostedProviderPreflight`; no more inline `20*time.Second`/`exec.CommandContext`. |
| `pkg/codex/preflight_retry_test.go` | cwd-isolation, temp-dir-failure, shared-timeout, payload tests | ✓ VERIFIED | 360 lines, all 13 tests PASS (3 pre-existing + 5 new + others). |
| `.aether/ts-host/src/platform-dispatcher.ts` | Env-configurable timeout, temp-dir probe cwd | ✓ VERIFIED | No `20_000` literal remains; re-exports `resolvePreflightTimeoutMs`/`PREFLIGHT_DEFAULT_TIMEOUT_MS` from `preflight-config.ts`. |
| `.aether/ts-host/src/preflight-config.ts` | Production timeout resolver (post-CR-01 extraction) | ✓ VERIFIED | New file, 79 lines, holds `resolvePreflightTimeoutMs`/`parseGoDurationMs`/`PREFLIGHT_DEFAULT_TIMEOUT_MS`, importable by production code without the retired-launcher guard violation. |
| `.aether/ts-host/src/host.ts` | Skip switch + notice surfacing | ✓ VERIFIED | `AETHER_SKIP_PREFLIGHT` check + exact skip notice text present. |
| `.aether/ts-host/src/worker-dispatch.ts` | `preflight` outcome field + scaled adapter budget | ✓ VERIFIED | `GoWorkerAdapterResponse.preflight` field present; `resolvePreflightAdapterBudgetMs()` (CR-01 fix) present and used at the `internal-worker-adapter --preflight` call site. |
| `.aether/ts-host/dist/*` | Rebuilt bundle reflecting all source changes | ✓ VERIFIED | `npm run build` produces zero diff against the committed dist (`git status --porcelain .aether/ts-host/dist` = 0 lines after rebuild); dist contains `resolvePreflightAdapterBudgetMs`, `AETHER_PREFLIGHT_TIMEOUT`, `preflight-config.js`; no `20_000`/`20000` literal for the preflight probe budget. |
| `.aether/docs/preflight-configuration.md` | Single documented knob surface | ✓ VERIFIED | 87 lines (> 40 min_lines), documents all 3 env vars, cache file, notice wording verbatim, "for dummies" section present. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `cmd/dispatch_runtime.go preflightWorkerProvider` | `gatedProviderPreflight` | single shared gate | ✓ WIRED | `grep -c "gatedProviderPreflight("` = 1 in this file; direct-Go `preflighter.Preflight(ctx, root)` call removed. |
| `cmd/internal_worker_adapter.go` preflight branch | `gatedProviderPreflight` | same shared gate | ✓ WIRED | Same gate call present; `TestDispatchChokepointsShareOneCache` proves shared window empirically. |
| `cmd/internal_worker_adapter.go` | `.aether/ts-host/src/worker-dispatch.ts` | `response.preflight {source, notice}` JSON contract | ✓ WIRED | Go: additive `json:"preflight,omitempty"` field. TS: `GoWorkerAdapterResponse.preflight?: {source, notice}` reads it and writes `notice` to stderr. `TestAdapterPreflightResponseCarriesOutcome` (Go, PASS) confirms JSON shape both with and without an outcome. |
| `cmd/dispatch_runtime.go dispatchBatchByWaveWithVisuals` | `clearPreflightCache` | provider/auth-classified dispatch failure | ✓ WIRED | `invalidatePreflightCacheOnProviderFailure` called on both return paths; test-proven. |
| `pkg/codex/worker.go RealInvoker.Preflight` | `runHostedProviderPreflight` | shared probe runner | ✓ WIRED | Direct delegation at worker.go:286, confirmed by `TestCodexPreflightHonorsSharedTimeoutAndRetry`. |
| `attemptHostedProviderPreflight` | `makePreflightTempDir` | neutral probe working directory | ✓ WIRED | Seam called, cleaned up with `defer os.RemoveAll`. |
| `.aether/ts-host/src/worker-dispatch.ts preflightGoWorkerProvider` | `resolvePreflightAdapterBudgetMs` (CR-01 fix) | Node-side kill-budget scaling | ✓ WIRED | Call site at worker-dispatch.ts:506-507; 4 TS unit tests cover floor, unparseable-fallback, scaling, and the invariant that the wrapper budget always exceeds `resolved Go budget × attempts`. |

### Data-Flow Trace (Level 4)

Not applicable in the strict UI-rendering sense — this phase is a runtime cost-policy layer, not a rendering surface. The equivalent trace is: cache-file → gate decision → dispatch chokepoint → CLI/JSON notice. This was traced above under Key Link Verification and confirmed with a real dispatch simulation in `TestDispatchChokepointsShareOneCache`, which prints the actual notice text ("preflight: cached OK for claude, 59m left in trust window") during test execution — not a hardcoded static string, but computed from a live TTL calculation against `time.Now()`.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Go build succeeds | `go build ./...` | exit 0 | ✓ PASS |
| Go vet clean | `go vet ./cmd/ ./pkg/codex/` | exit 0 | ✓ PASS |
| Full `cmd` package suite | `go test ./cmd/... -count=1` | `ok github.com/calcosmic/Aether/cmd 262.518s` | ✓ PASS |
| `pkg/codex` preflight suite | `go test ./pkg/codex/ -run Preflight -count=1 -v` | 13/13 tests PASS | ✓ PASS |
| TS typecheck | `npm run typecheck --prefix .aether/ts-host` | exit 0 | ✓ PASS |
| TS build | `npm run build --prefix .aether/ts-host` | exit 0, dist matches committed bundle | ✓ PASS |
| TS full test suite | `npm test --prefix .aether/ts-host` | 518/518 pass, 100 suites | ✓ PASS |
| All targeted preflight tests (cmd) | `go test ./cmd/ -run '...' -v` (30 named tests across all 5 plans + all review fixes) | all PASS | ✓ PASS |

### Probe Execution

Not applicable — no `scripts/*/tests/probe-*.sh` files exist in this repo, and neither the PLAN.md files nor the phase success criteria reference a probe-script convention. This phase's "verification" is entirely encoded as Go/TS unit and integration tests, all of which were re-run directly above rather than trusted from SUMMARY narration.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| D-01 | 01, 04 | Time-based per-platform success cache | ✓ SATISFIED | See Truth #1 |
| D-02 | 01 | TTL default 1h, tunable, safe fallback | ✓ SATISFIED | See Truth #2 |
| D-03 | 05 | Auto-invalidation on provider/auth failure | ✓ SATISFIED | See Truth #3 |
| D-04 | 02, 03 | Probe stays real model round-trip | ✓ SATISFIED | See Truth #4 |
| D-05 | 02, 03, 05 | One shared `AETHER_PREFLIGHT_TIMEOUT`, 20s hardcode gone | ✓ SATISFIED | See Truth #5 (including CR-01/WR-01 post-review fixes) |
| D-06 | 01, 03, 04 | `AETHER_SKIP_PREFLIGHT` loud skip switch | ✓ SATISFIED | See Truth #6 |
| D-07 | 02, 03 | Probe runs in neutral temp directory | ✓ SATISFIED | See Truth #7 |

No orphaned requirements — all 7 decision IDs declared in `163.2-CONTEXT.md` are claimed by at least one plan's `requirements:` frontmatter and are independently verified above. No REQUIREMENTS.md IDs map to this inserted phase (confirmed in ROADMAP.md phase header).

### Anti-Patterns Found

None. Scanned all 17 phase-touched files (Go and TS source, tests, docs) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers — zero hits. No empty stub implementations (`return null`, `return {}`, `=> {}`) found in the preflight code paths.

### Code Review Resolution Verification

163.2-REVIEW.md reported 1 Critical + 5 Warnings, all marked `issues_resolved`. Independently verified each fix is actually present in the current codebase (not just claimed in REVIEW.md):

| Finding | Claim | Verified In Codebase |
|---|---|---|
| CR-01 | TS adapter timeout now scales with `AETHER_PREFLIGHT_TIMEOUT` | `resolvePreflightAdapterBudgetMs()` present at worker-dispatch.ts:490, used at call site; 4 passing TS tests confirm floor/scaling behavior |
| WR-01 | TS parses full Go duration syntax (`ms\|s\|m\|h`, compounds) | `parseGoDurationMs` in preflight-config.ts implements compound-token summing exactly as claimed |
| WR-02 | Corrupt cache self-heals instead of permanent lockout | `preflightCacheUnmarshalError` + `TestPreflightCacheSelfHealsAfterCorruption` (PASS) |
| WR-03 | Drift-guard test for twin auth-failure regexes | `TestAuthFailureRegexMatchesAcrossHosts` (PASS) present in `cmd/preflight_docs_test.go` |
| WR-04 | Gate/cache tests pin env knobs to empty | Verified test setup in `cmd/preflight_cache_test.go`/`preflight_gate_test.go`/`preflight_invalidation_test.go`; full suite re-run clean |
| WR-05 | Write paths refuse to downgrade a future-schema cache file | `TestPreflightCacheWritePathsRefuseFutureSchema` (PASS) |

All 6 fix commits (`904994f9`, `ad82ab3f`, `e8331ae2`, `e723f7ca`, `a67677fb`, `75160bd9`) confirmed as ancestors of the current `HEAD` (`c4cef3dd`) via `git merge-base --is-ancestor`.

### Human Verification Required

None. This phase's success criteria are entirely runtime/behavioral cost-policy claims (cache hits, timeout values, temp-dir isolation, notice wording) that are directly testable and were directly tested — no UI, visual, or subjective-quality judgment is involved.

### Gaps Summary

No gaps. All 7 decision IDs (D-01 through D-07) have working code, are exercised by passing automated tests re-run independently during this verification (not trusted from SUMMARY claims), and the post-review-fix state (6 fix commits) was independently confirmed present and correct rather than assumed from REVIEW.md's own "RESOLVED" annotations.

---

_Verified: 2026-08-01_
_Verifier: Claude (gsd-verifier)_
