# Phase 163.2: ts-host preflight configurability - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-01
**Phase:** 163.2-ts-host-preflight-configurability
**Areas discussed:** Re-check frequency, Probe cost design, User control knobs, Where the probe runs

---

## Todo cross-reference

| Option | Description | Selected |
|--------|-------------|----------|
| Preflight timeout todo | "ts-host preflight timeout hardcoded, not configurable, runs in repo cwd" — this IS the phase (score 0.9) | ✓ |
| Finalize reconcile-gate todo | 163.1 follow-up in cmd/continue; weak match (0.6), folding widens scope | |

**User's choice:** Fold only the preflight timeout todo. Reconcile-gate todo reviewed, left pending.

---

## Re-check frequency (caching)

| Option | Description | Selected |
|--------|-------------|----------|
| Time-based cache (Recommended) | Remember success in a small file; skip the paid check while fresh — kills ~$0.59/dispatch | ✓ |
| No cache, config only | Keep per-dispatch checking; only fix the hardcoded timeout | |
| You decide | Claude picks mechanism and defaults | |

**TTL sub-question:** 1 hour (Recommended) / 15 minutes / 24 hours / **You decide ✓** — Claude exercised discretion: 1-hour default, tunable via env var.

**Invalidation sub-question:** **Auto-clear on failure (Recommended) ✓** / Time expiry only / You decide — provider/auth-classified worker failure wipes the cache; per-platform cache keys.

---

## Probe cost design

| Option | Description | Selected |
|--------|-------------|----------|
| Keep AI round-trip (Recommended) | Only check that proves auth AND model config; cost solved by caching | ✓ |
| Two-tier check | Free non-AI check per dispatch + full AI check on stale cache; more moving parts | |
| Replace with cheap check | Binary-responds check only; model failures return to mid-build discovery | |

**User's choice:** Keep AI round-trip.

---

## User control knobs

| Option | Description | Selected |
|--------|-------------|----------|
| One shared var (Recommended) | TS host honors AETHER_PREFLIGHT_TIMEOUT with Go-duration syntax; inherits 45s default | ✓ |
| Separate TS var | AETHER_PREFLIGHT_TIMEOUT_MS just for TS; two knobs for one concept | |
| You decide | Claude picks the config surface | |

**Skip-switch sub-question:** **Yes, with loud notice (Recommended) ✓** / No skip switch / You decide — skip env var prints one clear line when active, per fail-loudly.

---

## Where the probe runs

| Option | Description | Selected |
|--------|-------------|----------|
| Neutral directory (Recommended) | Clean temp dir; repo config only adds delay and false failures; closes the 163.1 deferred item | ✓ |
| Keep repo directory | Probe experiences what workers will; workers already fail loudly on repo-config problems | |
| You decide | Claude picks, possibly per-provider | |

**User's choice:** Neutral directory.

---

## Claude's Discretion

- TTL default value (1 hour chosen) and TTL env-var name
- Cache file location, name, and schema
- Provider/auth failure classification mechanism for auto-invalidation
- Whether TS reads the env var directly or via the Go adapter boundary
- Skip-switch name and notice wording; cached-skip one-liner wording

## Deferred Ideas

- Finalize reconcile-gate todo (reviewed, not folded) — belongs with future cmd/continue work.
