---
phase: 198.1-feed-the-memory
plan: 04
type: execute
wave: 3
depends_on: ["198.1-02", "198.1-03"]
files_modified:
  - cmd/phase_end_signals.go
  - cmd/phase_end_signals_test.go
  - cmd/consolidation_lifecycle.go
  - cmd/pheromone_write.go
  - cmd/internal_cmds.go
  - cmd/handoff_decisions_cmd.go
autonomous: true
requirements: [FEED-04]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Finishing a phase leaves a note the next phase's workers can read, naming that phase and what it produced."
    - "Answering a question a worker asked leaves a note carrying the owner's own answer."
    - "Three unacknowledged failures of the same kind produce one automatic don't-do-this note naming the failure."
    - "A high-value note that expires is preserved in long-term memory instead of vanishing."
  artifacts:
    - cmd/phase_end_signals.go
    - cmd/phase_end_signals_test.go
  key_links:
    - "runPhaseEndConsolidation -> emitPhaseCompletionFeedback -> writePheromoneSignal -> pheromones.json"
    - "recordDecisionAnswer -> emitDecisionFeedback -> writePheromoneSignal"
    - "midden.json category counts -> emitMiddenThresholdRedirect -> REDIRECT signal"
    - "expireSignalsByType -> appendEternalMemoryEntry -> ~/.aether/eternal/memory.json"
---

<objective>
CLAUDE.md tells the owner that decisions become notes the colony reads, that a run of failures automatically produces a "don't do this" note, and that valuable notes are preserved when they expire. None of those three is true at HEAD: the only pheromone reference on the check path is expiry housekeeping, no code emits an automatic note of any kind, and the long-term memory store has no writer at all. This plan makes all three true.

Purpose: FEED-04 — the notes the colony steers by are written by the colony again.
Output: `cmd/phase_end_signals.go` plus two extracted shared writers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198.1-feed-the-memory/198.1-CONTEXT.md
@.planning/phases/198.1-feed-the-memory/198.1-02-SUMMARY.md
@.planning/phases/198.1-feed-the-memory/198.1-03-SUMMARY.md
@CLAUDE.md
</context>

<user_decisions_honoured>
- D-01 (feed the existing pipeline): the signals go into the existing `pheromones.json` through the existing signal shape, sanitiser, content-hash dedup and expiry rules — the CLI's own body is EXTRACTED into a function, never duplicated.
- D-02 (automatic on the path the owner uses): phase-completion notes are emitted from `runPhaseEndConsolidation`, which both check lanes already call; decision notes from `recordDecisionAnswer`, the single writer both the ordinary answer path and the reviewer-decline path pass through.
- D-03 (content locks): every test asserts the note's own text, read back out of `pheromones.json`, never that a signal of some type exists.
- D-05 (inspection never mutates): emission happens only on the durable-advance path; a named test proves `pheromone-display` and `consolidation-phase-end --dry-run` still write nothing.
- D-06 (CLAUDE.md claims become true or are deleted): the claims this plan makes true are re-stated with their test names by Plan 05, which owns CLAUDE.md.
- D-09 (no new ceremony, no new workers).
</user_decisions_honoured>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A finished phase and an answered question each leave a note</name>
  <files>cmd/phase_end_signals.go, cmd/phase_end_signals_test.go, cmd/pheromone_write.go, cmd/consolidation_lifecycle.go, cmd/handoff_decisions_cmd.go</files>
  <read_first>
    - cmd/pheromone_write.go lines 20-180 (the whole `pheromoneWriteCmd` RunE) — id generation, content hashing BEFORE sanitisation, `colony.SanitizeSignalContent`, the `{"text": "..."}` content envelope, default priorities per type, `SourcePhase` population, and the type-based expiry defaults. All of this must move into the extracted function unchanged.
    - cmd/pheromone_write.go lines 286-305 — `expireSignalsByType` and `deactivateSignal`, for how an existing signal is retired.
    - Grep for the existing duplicate-content reinforcement path (content hash match → strength maxed, `reinforcement_count` incremented) before writing the extraction, so the extracted function keeps it.
    - cmd/handoff_decisions_cmd.go lines 102-165 — `recordDecisionAnswer` and its two callers (the ordinary clarification branch and the reviewer-decline branch of `decisionAnswerCmd`).
    - cmd/consolidation_lifecycle.go lines 96-175 — `runPhaseEndConsolidation` after Plan 03's edit; the summary now carries `QueenPromoted` and `InstinctApplicationsRecorded`.
    - cmd/colony_prime_context.go lines 560-600 — how active signals are rendered into a worker's brief, so the note's wording is written for that destination.
  </read_first>
  <behavior>
    - Completing a phase writes one FEEDBACK signal whose text names the phase number, the phase name, and what the phase produced for memory (lessons captured, failures recorded, instincts promoted).
    - Answering a worker's open question writes one FEEDBACK signal whose text carries the question and the owner's own answer.
    - Both signals are readable back out of `pheromones.json` with their text intact after sanitisation.
    - Emitting the same note twice reinforces the existing signal rather than creating a second one, exactly as the CLI already does.
    - A signal-write failure never breaks a phase advance or a decision answer.
  </behavior>
  <action>
In `cmd/pheromone_write.go`, extract the body of `pheromoneWriteCmd.RunE` into `writePheromoneSignal(sigType, content, priority, source, reason, ttl string, strength float64, tags []string) (colony.PheromoneSignal, bool, error)`, returning the stored signal, whether it reinforced an existing one, and any error. The RunE becomes a thin flag-reader that calls it and renders the result. Move nothing else and change no defaulting rule — the extraction must be behaviour-preserving, and the existing pheromone tests are the proof.

Create `cmd/phase_end_signals.go`.

Declare `emitPhaseCompletionFeedback(phase colony.Phase, summary phaseEndConsolidationSummary, lessonsCaptured, failuresRecorded int)`. It composes one sentence naming the phase number and name and the three counts, keeps it under the 500-character signal ceiling, and writes it as a FEEDBACK signal with source "aether continue". Deliberately do NOT write "phase N completed" and nothing else — a note that only narrates progress is the exact content this repo's admissibility rules exist to keep out; say that in a comment. Failures warn to stderr and return.

Declare `emitDecisionFeedback(question, answer string, phase int)`. It composes one sentence carrying the owner's answer and the question it answers, and writes it as a FEEDBACK signal with source "aether decision-answer".

Call `emitPhaseCompletionFeedback` from the end of `runPhaseEndConsolidation`, after the summary is assembled and before it is returned, so the counts in the note are the ones the summary reports.

Call `emitDecisionFeedback` from the end of `recordDecisionAnswer`, after the decision is durably stored, so a failed store never produces a note for an answer that was not recorded.

Add `TestFinishedPhaseLeavesANoteNamingWhatItProduced` to `cmd/phase_end_signals_test.go`. Run `runPhaseEndConsolidation` on a fixture colony whose phase is named `Feed the Memory`, then read `pheromones.json` back and assert an active FEEDBACK signal exists whose decoded text contains `Feed the Memory` and contains the counts the returned summary reports — reading those numbers off the summary rather than typing them.

Add `TestAnsweredQuestionLeavesANoteCarryingTheAnswer`. Call `recordDecisionAnswer` with question `should the drill-down show deferred lessons separately?` and answer `yes, in their own block under the pending list`, then assert an active FEEDBACK signal's decoded text contains that exact answer.

Add `TestRepeatedNoteReinforcesRatherThanDuplicates` asserting two identical emissions leave one signal with a raised reinforcement count.

Add `TestSignalWriteFailureNeverBlocksThePhase` asserting `runPhaseEndConsolidation` still returns `Ran: true` when the pheromone file is unwritable.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestFinishedPhaseLeavesANoteNamingWhatItProduced|TestAnsweredQuestionLeavesANoteCarryingTheAnswer|TestRepeatedNoteReinforcesRatherThanDuplicates|TestSignalWriteFailureNeverBlocksThePhase' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/ -run TestFinishedPhaseLeavesANoteNamingWhatItProduced -count=1` passes and asserts the signal text contains `Feed the Memory` and the counts read off the returned summary.
    - `go test ./cmd/ -run TestAnsweredQuestionLeavesANoteCarryingTheAnswer -count=1` passes and asserts the exact string `yes, in their own block under the pending list` appears in the stored signal text.
    - `go test ./cmd/ -run TestRepeatedNoteReinforcesRatherThanDuplicates -count=1` passes and asserts one signal, not two.
    - `go test ./cmd/ -run 'TestPheromone' -count=1` passes — the extraction changed no pheromone behaviour.
    - `go test ./cmd/ -run 'TestDecisionAnswer|TestResolvedOpenDecisionReachesNextWorkerPrompt|TestOnlyTheOwnerCanWaiveAForcedReviewer' -count=1` passes.
  </acceptance_criteria>
  <done>Finishing a phase and answering a question each leave a readable note, once, without ever blocking the action that produced them.</done>
  <reversibility rating="reversible">One behaviour-preserving extraction plus two additive emission calls.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A run of the same failure produces an automatic don't-do-this note</name>
  <files>cmd/phase_end_signals.go, cmd/phase_end_signals_test.go, cmd/consolidation_lifecycle.go</files>
  <read_first>
    - cmd/midden_shared.go — `loadMiddenFile` and the `colony.MiddenEntry` fields, especially `Category` and `Acknowledged *bool`.
    - cmd/memory_feed.go and cmd/memory_feed_continue.go (Plans 01 and 02) — the category constants now in use: `worker_failed`, `check_failed`, `quick_failed`, `swarm_worker_failed`, plus the pre-existing `autopilot_retry_exhausted` and the spawn-budget ceiling category.
    - cmd/colony_prime_context.go lines 560-600 — how a REDIRECT reaches a worker's brief, and lines 930-995 — how the failure log reaches the same brief, so the note adds steering rather than repeating the log.
    - cmd/pheromone_write.go — REDIRECT's default priority (high) and 30-day expiry, and the content-hash reinforcement path from Task 1.
  </read_first>
  <behavior>
    - When a single failure category holds three or more unacknowledged entries, one REDIRECT signal is written naming that category and the most recent failure's own message.
    - A fourth failure in the same category reinforces that signal instead of writing a second one.
    - Two unacknowledged entries in a category produce no signal.
    - Acknowledging the entries and then adding one more does not re-trigger below the threshold.
  </behavior>
  <action>
Add `const middenAutoRedirectThreshold = 3` and `emitMiddenThresholdRedirect() int` to `cmd/phase_end_signals.go`. The function loads the failure log with `loadMiddenFile`, groups unacknowledged entries by `Category`, and for each group at or above the threshold writes one REDIRECT signal whose text leads with the newest entry's own message and then states how many unacknowledged failures of that kind have accumulated. Source is "aether continue". It returns the number of categories that crossed the threshold.

Because the signal writer already reinforces on a content-hash match, a stable message for a given (category, newest message) pair is what makes repeat emission idempotent — do not add a second dedup mechanism.

Call `emitMiddenThresholdRedirect` from `runPhaseEndConsolidation`, immediately after `emitPhaseCompletionFeedback`, and add the returned count to `phaseEndConsolidationSummary` as `AutoRedirectsEmitted int`.

Add `TestThreeFailuresOfOneKindProduceOneRedirect` to `cmd/phase_end_signals_test.go`. Seed three unacknowledged entries in category `check_failed`, the newest with message `go test ./cmd/ -run TestExpireSignals failed: nil pointer dereference in expireSignalsByType`, run the emitter, and assert exactly one active REDIRECT whose decoded text contains that exact message.

Add `TestTwoFailuresProduceNoRedirect`.

Add `TestFourthFailureReinforcesTheSameRedirect` asserting the signal count stays 1 while the reinforcement count rises.

Add `TestAcknowledgedFailuresDoNotCountTowardTheThreshold` seeding three entries with `Acknowledged` true and asserting zero signals.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestThreeFailuresOfOneKindProduceOneRedirect|TestTwoFailuresProduceNoRedirect|TestFourthFailureReinforcesTheSameRedirect|TestAcknowledgedFailuresDoNotCountTowardTheThreshold' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/ -run TestThreeFailuresOfOneKindProduceOneRedirect -count=1` passes and asserts the exact message `go test ./cmd/ -run TestExpireSignals failed: nil pointer dereference in expireSignalsByType` appears in the stored REDIRECT text.
    - `go test ./cmd/ -run TestTwoFailuresProduceNoRedirect -count=1` and `-run TestAcknowledgedFailuresDoNotCountTowardTheThreshold -count=1` each assert zero signals.
    - `go test ./cmd/ -run TestFourthFailureReinforcesTheSameRedirect -count=1` passes and asserts one signal with a higher reinforcement count.
    - `go build ./cmd/aether && go vet ./cmd/` exit 0.
  </acceptance_criteria>
  <done>A repeated failure steers the next workers away from it automatically, once, carrying the failure's own words.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: A valuable note that expires is kept in long-term memory</name>
  <files>cmd/internal_cmds.go, cmd/pheromone_write.go, cmd/phase_end_signals.go, cmd/phase_end_signals_test.go</files>
  <read_first>
    - cmd/internal_cmds.go lines 135-200 — `eternalStoreCmd`: its entry and file types are declared INSIDE the RunE closure, which is why nothing else can write to long-term memory. The extraction must lift them to package scope unchanged in JSON shape.
    - cmd/internal_cmds.go — `resolveHubPath()`, the hub location resolver.
    - cmd/pheromone_write.go lines 221-305 — `pheromoneExpireCmd` and `expireSignalsByType`, the two expiry paths.
    - cmd/codex_workflow_cmds.go around line 700 — how the seal path purges FOCUS signals via `expireSignalsByType`, so the promotion fires there too.
    - cmd/codex_continue.go around line 4297 — the check path's signal housekeeping, the other live expiry caller.
    - pkg/colony (grep `PheromoneSignal`) — `Strength`, `Priority`, `Type` and the reinforcement count field, which together define "high value".
  </read_first>
  <behavior>
    - Expiring a signal whose type is REDIRECT, or whose reinforcement count is above zero, appends one entry to the hub's long-term memory file carrying that signal's own text.
    - Expiring a low-value FEEDBACK signal that was never reinforced appends nothing.
    - The same signal expiring twice appends one entry, not two.
    - The appended entry has the same JSON shape `eternal-store` already writes, so the existing command and the new writer are interchangeable.
    - Expiry still succeeds when the hub is unwritable.
  </behavior>
  <action>
In `cmd/internal_cmds.go`, lift the `eternalEntry` and `eternalData` types out of `eternalStoreCmd`'s RunE to package scope and extract `appendEternalMemoryEntry(content, category string, confidence float64, reason string) (bool, error)`. It resolves the hub with `resolveHubPath()`, creates the eternal directory, reads the existing file, skips the append and returns `(false, nil)` when an entry with identical content and category already exists, and otherwise appends and writes. The RunE becomes a thin flag-reader calling it. Keep every existing JSON field name and default (confidence 0.9, category "general") exactly as they are.

In `cmd/phase_end_signals.go`, declare `signalIsWorthKeeping(sig colony.PheromoneSignal) bool` — true when the type is REDIRECT, or the reinforcement count is greater than zero. Declare `promoteExpiredSignalToEternal(sig colony.PheromoneSignal) bool` which decodes the signal's `{"text": ...}` content envelope and calls `appendEternalMemoryEntry` with category "promoted_on_expire" and the reason `promoted_on_expire`, returning whether it appended.

In `cmd/pheromone_write.go`, call `promoteExpiredSignalToEternal` for each signal `expireSignalsByType` deactivates, and for the single signal `pheromoneExpireCmd` deactivates by id. Count the promotions and include the count in `expireSignalsByType`'s return by changing it to return `(expired int, promoted int)`; update its call sites. A hub write failure warns to stderr and never fails the expiry.

Add `TestExpiringAValuableNoteKeepsItInLongTermMemory` to `cmd/phase_end_signals_test.go`. Point the hub at a temp directory, seed one active REDIRECT whose text is `never edit cmd/pheromone_write.go expiry without running go test ./cmd/ -run TestPheromoneExpire`, expire it, and assert the hub's `eternal/memory.json` contains an entry whose `content` is that exact sentence.

Add `TestExpiringAThrowawayNoteKeepsNothing` using an unreinforced FEEDBACK signal and asserting the file holds zero entries.

Add `TestExpiringTheSameNoteTwiceKeepsOneCopy`.

Add `TestEternalWriterShapeMatchesTheEternalStoreCommand` — run the real `eternal-store` command and the new writer against the same hub and assert both entries carry the same JSON key set.

Add `TestExpiryStillSucceedsWhenTheHubIsUnwritable`.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestExpiringAValuableNoteKeepsItInLongTermMemory|TestExpiringAThrowawayNoteKeepsNothing|TestExpiringTheSameNoteTwiceKeepsOneCopy|TestEternalWriterShapeMatchesTheEternalStoreCommand|TestExpiryStillSucceedsWhenTheHubIsUnwritable' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/ -run TestExpiringAValuableNoteKeepsItInLongTermMemory -count=1` passes and asserts the exact sentence `never edit cmd/pheromone_write.go expiry without running go test ./cmd/ -run TestPheromoneExpire` is the stored entry's `content`.
    - `go test ./cmd/ -run TestExpiringAThrowawayNoteKeepsNothing -count=1` asserts zero entries.
    - `go test ./cmd/ -run TestEternalWriterShapeMatchesTheEternalStoreCommand -count=1` compares key sets derived from both writers' real output.
    - `go test ./cmd/ -run 'TestPheromoneExpire|TestSignalHousekeep|TestSeal' -count=1` passes — both live expiry callers still work.
    - `grep -n 'func appendEternalMemoryEntry' cmd/internal_cmds.go` returns exactly one hit.
  </acceptance_criteria>
  <done>A note worth keeping survives its own expiry in long-term memory, a throwaway one does not, and the writer matches the command that was already there.</done>
  <reversibility rating="costly">Writes to the shared hub directory, which every colony on the machine reads. Reverting stops future writes but leaves existing entries; the value filter and the duplicate refusal are what keep that acceptable.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner answer → pheromone → every future worker prompt | `--answer` text is owner-supplied and steers workers. |
| failure log message → REDIRECT → every future worker prompt | Failure messages originate from workers and from failing commands. |
| colony → shared hub (`~/.aether/eternal/`) | A per-project write that every other project on the machine will read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198.1-15 | Tampering | worker/owner text becoming a steering signal | high | mitigate | Every emission goes through the extracted `writePheromoneSignal`, which retains `colony.SanitizeSignalContent` (XML tag rejection, angle-bracket escaping, shell-injection and instruction-override blocking) and the 500-character cap. No emission bypasses it. |
| T-198.1-16 | Denial of Service | a failure loop flooding the signal store | medium | mitigate | Content-hash reinforcement means a repeated note raises one signal's strength rather than adding rows; `TestFourthFailureReinforcesTheSameRedirect` proves it. |
| T-198.1-17 | Elevation of Privilege | a worker's text crossing into the shared hub and steering other projects | high | mitigate | Only signals passing `signalIsWorthKeeping` (a REDIRECT, or a note reinforced at least once) are promoted, and the content was already sanitised at signal-write time. Duplicate content is refused. |
| T-198.1-18 | Repudiation | a phase advance silently blocked by a bookkeeping write | medium | mitigate | Every emission warns to stderr and returns; `TestSignalWriteFailureNeverBlocksThePhase` and `TestExpiryStillSucceedsWhenTheHubIsUnwritable` prove it. |
| T-198.1-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages. |
</threat_model>

<artifacts_this_phase_produces>
**New file:** `cmd/phase_end_signals.go`
- `func emitPhaseCompletionFeedback(phase colony.Phase, summary phaseEndConsolidationSummary, lessonsCaptured, failuresRecorded int)`
- `func emitDecisionFeedback(question, answer string, phase int)`
- `func emitMiddenThresholdRedirect() int`
- `func signalIsWorthKeeping(sig colony.PheromoneSignal) bool`
- `func promoteExpiredSignalToEternal(sig colony.PheromoneSignal) bool`
- `const middenAutoRedirectThreshold = 3`

**New file:** `cmd/phase_end_signals_test.go`
- `TestFinishedPhaseLeavesANoteNamingWhatItProduced`
- `TestAnsweredQuestionLeavesANoteCarryingTheAnswer`
- `TestRepeatedNoteReinforcesRatherThanDuplicates`
- `TestSignalWriteFailureNeverBlocksThePhase`
- `TestThreeFailuresOfOneKindProduceOneRedirect`
- `TestTwoFailuresProduceNoRedirect`
- `TestFourthFailureReinforcesTheSameRedirect`
- `TestAcknowledgedFailuresDoNotCountTowardTheThreshold`
- `TestExpiringAValuableNoteKeepsItInLongTermMemory`
- `TestExpiringAThrowawayNoteKeepsNothing`
- `TestExpiringTheSameNoteTwiceKeepsOneCopy`
- `TestEternalWriterShapeMatchesTheEternalStoreCommand`
- `TestExpiryStillSucceedsWhenTheHubIsUnwritable`

**Modified:** `cmd/pheromone_write.go` — new `func writePheromoneSignal(...) (colony.PheromoneSignal, bool, error)`; `expireSignalsByType` signature becomes `(expired int, promoted int)`
**Modified:** `cmd/internal_cmds.go` — `eternalEntry` and `eternalData` lifted to package scope; new `func appendEternalMemoryEntry(content, category string, confidence float64, reason string) (bool, error)`
**Modified:** `cmd/consolidation_lifecycle.go` — `runPhaseEndConsolidation` emits both notes; `phaseEndConsolidationSummary` gains `AutoRedirectsEmitted int`
**Modified:** `cmd/handoff_decisions_cmd.go` — `recordDecisionAnswer` emits a note after a durable store

**New hub artifact:** entries in `~/.aether/eternal/memory.json` with category `promoted_on_expire`
**No new CLI flags, no new subcommands.**
</artifacts_this_phase_produces>

<verification>
- `go build ./cmd/aether` exits 0.
- `go vet ./cmd/` exits 0.
- `go test ./cmd/ -count=1` passes.
- `go test ./cmd/ -run 'TestPheromone|TestSignalHousekeep|TestDecisionAnswer' -count=1` passes.
</verification>

<success_criteria>
- A finished phase and an answered question each leave one readable note carrying real content.
- Three unacknowledged failures of one kind leave one automatic don't-do-this note carrying the failure's own message; two leave none.
- A valuable expiring note is preserved in long-term memory with the same shape the existing command writes; a throwaway one is not.
- No emission can block a phase advance or a decision answer.
</success_criteria>

<output>
Create `.planning/phases/198.1-feed-the-memory/198.1-04-SUMMARY.md` when done.
</output>
