---
phase: 155-go-boundary-refactor
plan: 03
type: execute
wave: 2
depends_on: ["155-01", "155-02"]
files_modified:
  - cmd/colony_prime_context.go
  - colony/prompts/colony-prime.md
  - cmd/colony_prime_context_test.go
autonomous: true
requirements:
  - EXTRACT-07
must_haves:
  truths:
    - "Go loads prompt section templates (headers, intro text, formatting patterns) from colony/prompts/colony-prime.md instead of hardcoding them"
    - "The context assembly algorithm (trim order, scoring, budget management) stays in Go"
    - "If the prompts file is missing or malformed, Go falls back to hardcoded section templates"
    - "All existing tests pass; new tests verify the file-loading path"
  artifacts:
    - path: "colony/prompts/colony-prime.md"
      provides: "Editable prompt section templates"
      contains: "section_templates for state, pheromones, instincts, decisions, learnings, worker_handoffs, hive_wisdom, learned_memory, global_queen_md, user_preferences, prior_reviews, local_queen_wisdom, clarified_intent, blockers, medic_health"
    - path: "cmd/colony_prime_context.go"
      provides: "Runtime context assembly with file-backed prompt templates"
      min_lines_changed: 80
    - path: "cmd/colony_prime_context_test.go"
      provides: "Tests for prompt template file loading"
      exports: ["TestColonyPrimeTemplateLoad", "TestColonyPrimeTemplateFallback"]
  key_links:
    - from: "cmd/colony_prime_context.go"
      to: "colony/prompts/colony-prime.md"
      via: "loadColonyPrimeTemplates()"
      pattern: "loadColonyPrimeTemplates|colony/prompts/colony-prime"
---

<objective>
Extract prompt section text from `cmd/colony_prime_context.go` into `colony/prompts/colony-prime.md`, making Go load templates at runtime while keeping the context assembly algorithm in Go.

Purpose: Prompt text is pure behaviour — it tells workers what to see and how to format it. The algorithm that assembles, trims, and scores context is spine logic that stays in Go. This is the highest-impact MIXED target.
Output: `colony/prompts/colony-prime.md` with all section templates; updated `cmd/colony_prime_context.go` that loads templates from file; tests proving both paths work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-boundary-parity/152-BEHAVIOUR_EXTRACTION_AUDIT.md
@cmd/colony_prime_context.go

# Interfaces the executor needs
From cmd/colony_prime_context.go (current hardcoded section text):
- "## Colony State\n\n" — header + format for goal, state, phase, tasks, parallel mode
- "## Review Depth\n\n" — header + depth text (light/standard/heavy)
- "## Pheromone Signals\n\n" — header + lifecycle context + signal list format
- "## Active Instincts\n\n" — header + instinct list format
- "## Key Decisions\n\n" — header + decision list format
- "## Phase Learnings\n\n" — header + learning list format
- "## Previous Worker Handoffs\n\n" — header + handoff record format (rendered by renderWorkerHandoffSection in codex_dispatch_contract.go)
- "## HIVE WISDOM (Cross-Colony Patterns)\n\n" — header + wisdom line format
- "## LEARNED MEMORY (Verified Outcomes)\n\n" — header + memory entry format
- "## GLOBAL QUEEN WISDOM (Cross-Colony)\n\n" — header + wisdom line format
- "## USER PREFERENCES\n\n" — header + preference line format
- "## Prior Reviews\n\n" — header + review finding format (rendered by buildPriorReviewsSection)
- "## Local Queen Wisdom (Repo-Specific)\n\n" — header + wisdom line format
- "## CLARIFIED INTENT\n\n" — header + clarification line format
- "## Active Blockers\n\n" — header + blocker line format
- "## Colony Health Issues\n\n" — header + health issue format

Key functions that build sections:
- `buildColonyPrimeOutput(compact bool) colonyPrimeOutput` — assembles all sections
- `buildPriorReviewsSection(s *storage.Store, compact bool) (colonyPrimeSection, int)` — builds prior reviews
- `renderWorkerHandoffSection(workflow string, phaseID int, workerName string) string` — builds handoffs (in codex_dispatch_contract.go)

The algorithm that stays in Go:
- Section ordering and priority assignment
- Budget management (8000/4000 chars)
- Freshness, confirmation, and relevance scoring
- Trust class assessment via `colony.AssessPromptSource`
- Context candidate ranking via `colony.RankContextCandidates`
- Trimming decisions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create colony/prompts/colony-prime.md with all section templates</name>
  <files>colony/prompts/colony-prime.md</files>
  <action>
Create `colony/prompts/colony-prime.md` containing all currently hardcoded prompt section templates from `cmd/colony_prime_context.go`.

Use YAML frontmatter for structured templates so Go can parse them. Each template is a format string with optional placeholders.

Structure:
```yaml
---
colony_prime_version: "1.0"
section_templates:
  state:
    header: "## Colony State\n\n"
    goal_format: "Goal: %s\n"
    state_format: "State: %s\n"
    phase_format: "Phase: %d\n"
    phase_name_format: "Phase Name: %s\n"
    task_format: "  - [%s] %s\n"
    tasks_header: "Tasks:\n"
    parallel_mode_format: "Parallel Mode: %s\n"
  review_depth:
    header: "## Review Depth\n\n"
    light_text: "Light review -- core verification only"
    standard_text: "Standard review -- watcher and probe verification"
    heavy_text: "Heavy review -- full quality gauntlet"
    default_text: "Standard review -- watcher and probe verification"
  pheromones:
    header: "## Pheromone Signals\n\n"
    lifecycle_context: "..."  # colonyLifecycleSignalContext() output is injected separately
    signal_format: "- [%s] %s\n"
  instincts:
    header: "## Active Instincts\n\n"
    instinct_format: "- [%s] %s (confidence: %.2f)\n"
  decisions:
    header: "## Key Decisions\n\n"
    decision_format: "- Phase %d: %s — %s\n"
  learnings:
    header: "## Phase Learnings\n\n"
    phase_header_format: "### Phase %d: %s\n"
    learning_format: "  - %s [%s]\n"
  worker_handoffs:
    header: "## Previous Worker Handoffs\n\n"
    worker_header_format: "### %s\n"
    status_format: "- Status: %s; verification: %s\n"
    summary_format: "- Summary: %s\n"
    list_format: "- %s: %s\n"
  hive_wisdom:
    header: "## HIVE WISDOM (Cross-Colony Patterns)\n\n"
    entry_format: "- %s\n"
  learned_memory:
    header: "## LEARNED MEMORY (Verified Outcomes)\n\n"
    entry_format: "- [Phase %d] %s (confidence: %.0f%%, classification: %s)\n"
  global_queen_md:
    header: "## GLOBAL QUEEN WISDOM (Cross-Colony)\n\n"
    entry_format: "- %s\n"
  user_preferences:
    header: "## USER PREFERENCES\n\n"
    entry_format: "- %s\n"
  prior_reviews:
    header: "## Prior Reviews\n\n"
    domain_format: "- %s (%d open): %s\n"
    domain_count_only_format: "- %s (%d open)\n"
  local_queen_wisdom:
    header: "## LOCAL QUEEN WISDOM (Repo-Specific)\n\n"
    entry_format: "- %s\n"
  clarified_intent:
    header: "## CLARIFIED INTENT\n\n"
  blockers:
    header: "## Active Blockers\n\n"
    blocker_format: "- %s\n"
  medic_health:
    header: "## Colony Health Issues\n\n"
    scan_timestamp_format: "Last scan: %s\n\n"
    issue_format: "- [%s] %s"
    issue_file_format: " (%s)"
---
```

After the frontmatter, add a brief Markdown description of what this file controls and how to edit it safely.

Do NOT change any template text — copy exactly from the current Go source. The goal is extraction, not redesign.
  </action>
  <verify>
    <automated>grep -c "section_templates:" colony/prompts/colony-prime.md && grep -c "state:" colony/prompts/colony-prime.md && grep -c "blockers:" colony/prompts/colony-prime.md</automated>
  </verify>
  <done>File exists with all 16 section templates; template text matches current Go source exactly; valid YAML frontmatter.</done>
</task>

<task type="auto">
  <name>Task 2: Add prompt template loader and refactor colony_prime_context.go</name>
  <files>cmd/colony_prime_context.go, cmd/prompt_template_loader.go</files>
  <action>
Create `cmd/prompt_template_loader.go` with the template-loading logic, then refactor `cmd/colony_prime_context.go` to use it.

In `cmd/prompt_template_loader.go`:
1. Define `type colonyPrimeTemplates struct` with fields matching the YAML structure (use `yaml` tags).
2. Define `type sectionTemplate struct` with fields for header, and any format strings the section uses.
3. Define `var loadedColonyPrimeTemplates *colonyPrimeTemplates` and `var colonyPrimeTemplatesOnce sync.Once`.
4. Implement `func loadColonyPrimeTemplates() *colonyPrimeTemplates` that:
   - Looks for `colony/prompts/colony-prime.md` relative to working directory.
   - If found, reads file, extracts YAML frontmatter, unmarshals into `colonyPrimeTemplates`.
   - If file missing, unreadable, or unparseable, returns `nil` (fallback path).
   - Caches result via `sync.Once`.
5. Implement helper:
   - `func getSectionTemplate(name string) *sectionTemplate` — returns the template for a named section, or nil if not loaded.

In `cmd/colony_prime_context.go`:
Refactor each section builder to use templates from file first, fallback to hardcoded strings.

For each section in `buildColonyPrimeOutput`:
1. Look up the section template via `getSectionTemplate("state")`, etc.
2. Use `template.Header` if available, else use hardcoded header string.
3. Use template format strings if available, else use hardcoded format strings.
4. Keep all algorithmic logic (scoring, trimming, ranking, budget) unchanged.

Example refactor for the "state" section:
```go
// Before:
stateSection.WriteString("## Colony State\n\n")

// After:
if t := getSectionTemplate("state"); t != nil && t.Header != "" {
    stateSection.WriteString(t.Header)
} else {
    stateSection.WriteString("## Colony State\n\n")
}
```

Apply this pattern to all 16 sections:
- state
- review_depth
- pheromones
- instincts
- decisions
- learnings
- worker_handoffs (note: the rendering logic lives in `renderWorkerHandoffSection` in `cmd/codex_dispatch_contract.go`; add template usage there)
- hive_wisdom
- learned_memory
- global_queen_md
- user_preferences
- prior_reviews (note: rendering logic lives in `buildPriorReviewsSection`)
- local_queen_wisdom
- clarified_intent
- blockers
- medic_health

For sections where the format strings are used inline (e.g., `fmt.Sprintf("Goal: %s\n", ...)`), replace with template-driven format if template is loaded.

Keep all original hardcoded strings as package-level constants or fallback inline. Do NOT delete them.
  </action>
  <verify>
    <automated>go build ./cmd/aether && go test ./cmd/... -run ColonyPrime -v</automated>
  </verify>
  <done>Binary builds; all existing tests pass; new tests (Task 3) verify file-loading and fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add tests for prompt template file loading and fallback</name>
  <files>cmd/colony_prime_context_test.go</files>
  <action>
Create `cmd/colony_prime_context_test.go` with tests that exercise both the file-loading and fallback paths for prompt templates.

Tests:
- Test 1 (Template load): Create a temp colony-prime.md with custom section_templates.state.header = "## CUSTOM STATE\n\n". Build a minimal colonyPrimeOutput and assert the state section contains "CUSTOM STATE".
- Test 2 (Fallback): Run with no colony-prime.md. Build output and assert state section contains original "## Colony State" header.
- Test 3 (Partial fallback): Create a colony-prime.md with only state and pheromones templates. Build output and assert instincts section still uses original "## Active Instincts" header.
- Test 4 (Review depth text): Create a colony-prime.md with custom review_depth.heavy_text. Build output for a heavy-review phase and assert custom text appears.
- Test 5 (Blocker format): Create a colony-prime.md with custom blockers.blocker_format. Inject a test blocker and assert the custom format is used.
- Test 6 (Pheromone signal format): Create a colony-prime.md with custom pheromones.signal_format. Inject a test signal and assert custom format is used.

For testability, add a package-level `colonyPrimeTemplatesPathOverride` string in `cmd/prompt_template_loader.go`, and a test-only `resetColonyPrimeTemplatesCache()` function.

Each test must:
1. Set up a temp colony-prime.md (or ensure none exists).
2. Reset the sync.Once cache.
3. Build colony prime output with test data.
4. Assert the expected template text appears in the output.
5. Clean up.

Run all tests with `go test ./cmd/... -run ColonyPrime -v`.
  </action>
  <verify>
    <automated>go test ./cmd/... -run ColonyPrime -v && go test ./cmd/... -count=1</automated>
  </verify>
  <done>All 6 test cases pass; `go test ./cmd/...` passes with no regressions; tests cover template load, fallback, partial fallback, and specific section types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system -> Go runtime | Untrusted file content crosses here; colony-prime.md could be malformed or attacker-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-07 | Tampering | colony/prompts/colony-prime.md | mitigate | File is loaded read-only; tampering only affects prompt formatting, not runtime logic or scoring |
| T-155-08 | Denial of Service | YAML parser on malformed file | mitigate | Fallback to hardcoded defaults on any parse error; no panic |
| T-155-09 | Elevation of Privilege | Malicious template injects instructions | accept | Prompt content is injected into worker prompts, but workers already run with full tool access; template text is no more privileged than hardcoded text was |
</threat_model>

<verification>
- `go test ./cmd/...` passes with no regressions
- `go build ./cmd/aether` succeeds
- `aether version` reports correctly
- Spot-check: temporarily modify `colony/prompts/colony-prime.md` state header, run `go test ./cmd/... -run ColonyPrime`, confirm test picks up change
</verification>

<success_criteria>
- `colony/prompts/colony-prime.md` exists with all 16 section templates extracted from Go
- `cmd/colony_prime_context.go` loads templates from file with fallback to hardcoded defaults
- `cmd/codex_dispatch_contract.go` `renderWorkerHandoffSection` uses templates for handoff section
- `cmd/colony_prime_context_test.go` has >= 6 tests covering file load, fallback, and partial fallback
- `go test ./cmd/...` passes
- No user-facing behaviour changes (same prompt text output)
</success_criteria>

<output>
After completion, create `.planning/phases/155-go-boundary-refactor/155-03-SUMMARY.md`
</output>
