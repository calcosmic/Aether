---
phase: 155-go-boundary-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/codex_dispatch_contract.go
  - cmd/review_depth.go
  - colony/policies/dispatch-contract.yaml
  - colony/policies/review-depth.yaml
  - cmd/dispatch_contract_test.go
  - cmd/review_depth_test.go
autonomous: true
requirements:
  - EXTRACT-07
must_haves:
  truths:
    - "Go loads dispatch contract text (execution model descriptions, deadline policies, fallback behaviours) from colony/policies/dispatch-contract.yaml instead of hardcoding them"
    - "Go loads review depth keyword tables and smart-default reason strings from colony/policies/review-depth.yaml instead of hardcoding them"
    - "If policy files are missing or malformed, Go falls back to hardcoded defaults"
    - "All existing tests pass; new tests verify the YAML-loading path"
  artifacts:
    - path: "colony/policies/dispatch-contract.yaml"
      provides: "Editable dispatch contract policy"
      contains: "execution_models, deadline_policies, fallback_behaviours, fallback_visibility, result_collection_policies"
    - path: "colony/policies/review-depth.yaml"
      provides: "Editable review depth policy"
      contains: "heavy_keywords, security_risk_keywords, blast_radius_keywords, smart_default_reasons"
    - path: "cmd/codex_dispatch_contract.go"
      provides: "Runtime dispatch contract with file-backed policy"
      min_lines_changed: 30
    - path: "cmd/review_depth.go"
      provides: "Runtime review depth with file-backed policy"
      min_lines_changed: 30
    - path: "cmd/dispatch_contract_test.go"
      provides: "Tests for dispatch contract file loading"
      exports: ["TestDispatchContractLoad", "TestDispatchContractFallback"]
    - path: "cmd/review_depth_test.go"
      provides: "Tests for review depth file loading"
      exports: ["TestReviewDepthLoad", "TestReviewDepthFallback"]
  key_links:
    - from: "cmd/codex_dispatch_contract.go"
      to: "colony/policies/dispatch-contract.yaml"
      via: "loadDispatchContractPolicy()"
      pattern: "loadDispatchContractPolicy|colony/policies/dispatch-contract"
    - from: "cmd/review_depth.go"
      to: "colony/policies/review-depth.yaml"
      via: "loadReviewDepthPolicy()"
      pattern: "loadReviewDepthPolicy|colony/policies/review-depth"
---

<objective>
Extract structured policy config from `cmd/codex_dispatch_contract.go` and `cmd/review_depth.go` into YAML files under `colony/policies/`, making Go load them at runtime with fallback to hardcoded defaults.

Purpose: Policy rules (spawn budgets, timeout defaults, keyword tables, depth reasons) are behaviour definitions that should be editable without recompiling. This plan proves the YAML-loading pattern for structured policy.
Output: Two new YAML policy files; updated Go source that loads from them; tests proving both paths work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-boundary-parity/152-BEHAVIOUR_EXTRACTION_AUDIT.md
@cmd/codex_dispatch_contract.go
@cmd/review_depth.go
@colony/policies/dispatch-contract.yaml

# Interfaces the executor needs
From cmd/codex_dispatch_contract.go (current hardcoded values):
- `surveyDispatchContractWithTimeout` — hardcoded strings: ExecutionModel, DeadlinePolicy, DependencyBehavior, FallbackBehavior, FallbackVisibility, ResultCollectionPolicy
- `planningDispatchContractWithTimeout` — hardcoded strings: same fields
- Timeout defaults: `planningScoutTimeout`, `planningRouteSetterTimeout`, `surveyorDispatchTimeout`, `continueReviewTimeout`, `continueVerificationTimeout`

From cmd/review_depth.go (current hardcoded values):
- `heavyKeywords` — []string (security/auth/release keywords)
- `securityRiskKeywords` — []string
- `blastRadiusKeywords` — []string
- `renderSmartDepthReason` — returns hardcoded reason strings: "auto: security risk", "auto: final phase", "auto: high blast radius", "auto: early phase", "auto: late phase", "auto: standard"

Existing colony/policies/dispatch-contract.yaml (from Phase 154):
```yaml
dispatch_contract:
  max_workers_per_phase: 10
  spawn_depth_limits:
    0: 4
    1: 4
    2: 2
    3: 0
  timeout_defaults:
    build: 600
    continue: 300
    plan: 300
    verify: 180
```
This file already exists but only contains numeric config. We will extend it with the string policy fields.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend colony/policies/dispatch-contract.yaml with string policy fields</name>
  <files>colony/policies/dispatch-contract.yaml</files>
  <action>
Extend the existing `colony/policies/dispatch-contract.yaml` with all hardcoded string policy fields from `cmd/codex_dispatch_contract.go`.

Add under the existing `dispatch_contract` key:
```yaml
  execution_models:
    survey: "1 wave, parallel read-only worker execution"
    planning: "2 staged workers, scout then route-setter"
    planning_extended: "%d staged planning workers, scout plus route-setter with supporting castes"
  deadline_policies:
    survey: "Each surveyor gets its own timeout. One surveyor timing out does not reduce sibling surveyor budgets."
    planning: "Each planning worker gets its own timeout. The route-setter only runs after a completed scout stage; otherwise it becomes dependency_blocked."
  dependency_behaviors:
    survey: "Surveyors are independent read-only workers; real dispatch requires an authenticated platform dispatcher."
    planning: "Real worker dispatch requires an authenticated platform dispatcher. Route-setter execution depends on the scout completing first."
    planning_extended: "Real worker dispatch requires an authenticated platform dispatcher. Supporting planning castes may contribute evidence, and route-setter finalization is selected by caste identity rather than fixed array position."
  fallback_behaviors:
    survey: "If any surveyor fails, blocks, or times out after dispatch starts, emit dispatch_mode=fallback and synthesize survey artifacts locally while preserving any real worker artifacts that landed first."
    planning: "If the scout or route-setter fails, blocks, or times out after dispatch starts, emit dispatch_mode=fallback and synthesize planning artifacts locally while preserving any real worker artifacts that landed first."
  fallback_visibility:
    survey: ["dispatch_mode", "survey_warning", "provider_diagnostics", "artifact_source"]
    planning: ["dispatch_mode", "planning_warning", "provider_diagnostics", "artifact_source", "plan_source", "planning_loop"]
  result_collection_policies:
    survey: "Wrapper result artifacts must stay outside .aether/data; finalizers reject malformed completion JSON and .aether/data completion files."
    planning: "A structurally valid completed result wins over a timeout placeholder for the same worker; duplicate terminal results remain invalid."
```

Keep the existing numeric fields intact. The file must remain valid YAML.
  </action>
  <verify>
    <automated>grep -c "execution_models:" colony/policies/dispatch-contract.yaml && grep -c "deadline_policies:" colony/policies/dispatch-contract.yaml && grep -c "fallback_behaviors:" colony/policies/dispatch-contract.yaml</automated>
  </verify>
  <done>File contains all string policy fields extracted from Go; existing numeric config preserved; valid YAML.</done>
</task>

<task type="auto">
  <name>Task 2: Create colony/policies/review-depth.yaml</name>
  <files>colony/policies/review-depth.yaml</files>
  <action>
Create `colony/policies/review-depth.yaml` containing all hardcoded keyword tables and smart-default reason strings from `cmd/review_depth.go`.

Structure:
```yaml
---
review_depth_version: "1.0"
heavy_keywords:
  - security
  - auth
  - crypto
  - secrets
  - permissions
  - compliance
  - audit
  - release
  - deploy
  - production
  - ship
  - launch
security_risk_keywords:
  - security
  - auth
  - crypto
  - secrets
  - permissions
  - compliance
  - audit
  - token
  - session
  - password
blast_radius_keywords:
  - core runtime
  - state mutation
  - colony state
  - state machine
  - phase transition
  - dispatch
  - build command
  - continue command
  - verification depth
  - planning depth
smart_default_reasons:
  high_risk: "auto: security risk"
  final_phase: "auto: final phase"
  medium_risk: "auto: high blast radius"
  early_phase: "auto: early phase"
  late_phase: "auto: late phase"
  standard: "auto: standard"
---
```

Copy values exactly from current Go source. Do not change keywords or reason strings.
  </action>
  <verify>
    <automated>grep -c "heavy_keywords:" colony/policies/review-depth.yaml && grep -c "smart_default_reasons:" colony/policies/review-depth.yaml && grep -c "auto: security risk" colony/policies/review-depth.yaml</automated>
  </verify>
  <done>File exists with all keyword tables and reason strings matching current Go source exactly.</done>
</task>

<task type="auto">
  <name>Task 3: Add policy file loaders and refactor Go source</name>
  <files>cmd/codex_dispatch_contract.go, cmd/review_depth.go, cmd/policy_loader.go</files>
  <action>
Create `cmd/policy_loader.go` with shared YAML policy loading utilities, then refactor the two target files.

In `cmd/policy_loader.go`:
1. Define generic helpers:
   - `func loadYAMLPolicy(path string, out interface{}) error` — reads file, extracts YAML frontmatter if present (between `---` lines), unmarshals into `out`.
   - `func policyPath(name string) string` — returns `colony/policies/<name>.yaml`.
2. Define `var policyCache = map[string]interface{}{}` with `var policyCacheMu sync.RWMutex` for in-memory caching.
3. Define `func cachedPolicyLoad(path string, loader func() (interface{}, error)) (interface{}, error)` that caches loaded policies.

In `cmd/codex_dispatch_contract.go`:
1. Define `type dispatchContractPolicy struct` matching the YAML structure.
2. Define `var loadedDispatchPolicy *dispatchContractPolicy` loaded via `sync.Once`.
3. Implement `func loadDispatchContractPolicy() *dispatchContractPolicy` using `loadYAMLPolicy`.
4. Refactor `surveyDispatchContractWithTimeout` and `planningDispatchContractWithTimeout` to load string fields from policy first, fallback to current hardcoded strings.
5. Keep original hardcoded strings as package-level `const` or `var` fallback values. Do NOT delete them.

In `cmd/review_depth.go`:
1. Define `type reviewDepthPolicy struct` matching the YAML structure.
2. Define `var loadedReviewDepthPolicy *reviewDepthPolicy` loaded via `sync.Once`.
3. Implement `func loadReviewDepthPolicy() *reviewDepthPolicy` using `loadYAMLPolicy`.
4. Refactor `heavyKeywords`, `securityRiskKeywords`, `blastRadiusKeywords` to be initialized from policy if available, else use current hardcoded slices.
5. Refactor `renderSmartDepthReason` to load reason strings from policy if available, else use current hardcoded strings.
6. Keep original hardcoded slices/strings as fallback. Do NOT delete them.

Pattern for slice initialization (must happen at runtime, not init()):
```go
func getHeavyKeywords() []string {
    if p := loadReviewDepthPolicy(); p != nil && len(p.HeavyKeywords) > 0 {
        return p.HeavyKeywords
    }
    return heavyKeywordsFallback
}
```

Note: `heavyKeywords` is currently a package-level `var`. Change it to `heavyKeywordsFallback` and update all readers to use `getHeavyKeywords()`.
  </action>
  <verify>
    <automated>go build ./cmd/aether && go test ./cmd/... -run DispatchContract -v && go test ./cmd/... -run ReviewDepth -v</automated>
  </verify>
  <done>Binary builds; all existing tests pass; new tests (Task 4) verify file-loading and fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add tests for policy file loading and fallback</name>
  <files>cmd/dispatch_contract_test.go, cmd/review_depth_test.go</files>
  <action>
Create two test files that exercise both file-loading and fallback paths.

`cmd/dispatch_contract_test.go`:
- Test 1: Create a temp dispatch-contract.yaml with a custom execution_models.survey value. Call a helper that reads it (e.g., via `loadDispatchContractPolicy` with path override) and assert the custom value is returned.
- Test 2: With no policy file, assert fallback strings match original hardcoded values.
- Test 3: Create a policy file with partial data (only survey fields). Assert planning fields still fall back to hardcoded values.
- Test 4: Assert timeout defaults from existing numeric config are accessible.

`cmd/review_depth_test.go`:
- Test 1: Create a temp review-depth.yaml with custom heavy_keywords. Call `getHeavyKeywords()` (or equivalent) and assert custom list is returned.
- Test 2: With no policy file, assert `getHeavyKeywords()` returns original fallback.
- Test 3: Create a policy file with custom smart_default_reasons. Call `renderSmartDepthReason` with a high-risk phase and assert custom reason string.
- Test 4: Assert `phaseHasHeavyKeywords` uses the loaded keywords (test with a custom keyword not in fallback).

For testability, add package-level `dispatchContractPathOverride` and `reviewDepthPathOverride` strings in the respective Go files, guarded by build tags or test-only accessors. Reset `sync.Once` between tests using `sync.Once` replacement or by exposing a test-only reset function.

Run all tests with `go test ./cmd/... -run "DispatchContract|ReviewDepth" -v`.
  </action>
  <verify>
    <automated>go test ./cmd/... -run "DispatchContract|ReviewDepth" -v && go test ./cmd/... -count=1</automated>
  </verify>
  <done>All test cases pass; `go test ./cmd/...` passes with no regressions; tests cover file load, fallback, and partial fallback for both policies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system -> Go runtime | Untrusted YAML content crosses here; policy files could be malformed or attacker-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-04 | Tampering | colony/policies/dispatch-contract.yaml | mitigate | File is loaded read-only; tampering only affects ceremony text, not runtime safety logic (timeouts still have hardcoded fallbacks) |
| T-155-05 | Tampering | colony/policies/review-depth.yaml | mitigate | Tampering could change review depth selection; fallback to hardcoded safe defaults on parse error |
| T-155-06 | Denial of Service | YAML parser on malformed file | mitigate | Fallback to hardcoded defaults on any parse error; no panic |
</threat_model>

<verification>
- `go test ./cmd/...` passes with no regressions
- `go build ./cmd/aether` succeeds
- `aether version` reports correctly
- Spot-check: temporarily modify `colony/policies/review-depth.yaml` heavy_keywords, run `go test ./cmd/... -run ReviewDepth`, confirm test picks up change
</verification>

<success_criteria>
- `colony/policies/dispatch-contract.yaml` extended with all string policy fields from Go
- `colony/policies/review-depth.yaml` created with all keyword tables and reason strings
- `cmd/codex_dispatch_contract.go` and `cmd/review_depth.go` load from YAML with fallback to hardcoded defaults
- `cmd/dispatch_contract_test.go` and `cmd/review_depth_test.go` have >= 4 tests each covering file load and fallback
- `go test ./cmd/...` passes
- No user-facing behaviour changes (same ceremony text, same depth logic)
</success_criteria>

<output>
After completion, create `.planning/phases/155-go-boundary-refactor/155-02-SUMMARY.md`
</output>
