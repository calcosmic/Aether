---
phase: 14-planning-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/ant/plan.md
  - .opencode/commands/ant/plan.md
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "/ant:plan spawns a research scout before the planning loop to investigate domain knowledge"
    - "Research scout checks hive wisdom first before exploring"
    - "Research scout output is written as RESEARCH.md to .aether/data/phase-research/"
    - "Re-running /ant:plan deletes and regenerates research from scratch"
    - "Research findings are passed to the Route-Setter in the planning loop"
    - "OpenCode plan.md mirrors the same research step"
  artifacts:
    - path: ".claude/commands/ant/plan.md"
      provides: "Step 3.6: Phase Domain Research between territory survey and planning loop"
      contains: "Step 3.6"
    - path: ".opencode/commands/ant/plan.md"
      provides: "Mirrored Step 3.6 with normalize-args compatibility"
      contains: "Step 3.6"
  key_links:
    - from: ".claude/commands/ant/plan.md"
      to: "aether-scout agent"
      via: "Task tool spawn with structured research prompt"
      pattern: "subagent_type.*aether-scout"
    - from: ".claude/commands/ant/plan.md"
      to: ".aether/data/phase-research/"
      via: "Queen writes RESEARCH.md after scout returns JSON"
      pattern: "phase-research"
    - from: ".claude/commands/ant/plan.md"
      to: "Route-Setter planning prompt"
      via: "research_findings injected into RESEARCH FINDINGS section"
      pattern: "RESEARCH FINDINGS"
---

<objective>
Add per-phase research step to /ant:plan that spawns a scout to investigate domain knowledge before task decomposition.

Purpose: Every phase gets domain research (hive wisdom, codebase patterns, external context, gotchas) written to RESEARCH.md before the planning loop begins, so the Route-Setter has richer context for creating plans.
Output: Modified plan.md (Claude + OpenCode) with Step 3.6: Phase Domain Research
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-planning-depth/14-RESEARCH.md
@.planning/phases/14-planning-depth/14-CONTEXT.md
@.claude/commands/ant/plan.md
@.opencode/commands/ant/plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 3.6 Phase Domain Research to Claude plan.md</name>
  <files>.claude/commands/ant/plan.md, .opencode/commands/ant/plan.md</files>
  <action>
Add a new "Step 3.6: Phase Domain Research" section to `.claude/commands/ant/plan.md` between the existing Step 3.5 (Load Territory Survey) and Step 4 (Research and Planning Loop).

The new step must implement this sequence:

1. **Retrieve hive wisdom** for research priming:
   ```
   hive_context=$(bash .aether/aether-utils.sh hive-read --limit 5 --format text 2>/dev/null)
   ```
   Parse the JSON result to extract `.result.text` as `hive_text`. If command fails or returns empty, set `hive_text = ""`.

2. **Clean any previous research** for this phase:
   ```
   research_dir=".aether/data/phase-research"
   mkdir -p "$research_dir"
   rm -f "$research_dir/phase-{phase_number}-research.md"
   ```
   Per user decision: re-running /ant:plan always re-researches from scratch.

3. **Spawn Research Scout** via Task tool with `subagent_type="aether-scout"`, with this structured prompt:
   - Mission: "Investigate the domain knowledge needed for Phase {phase_number}: {phase_name}"
   - Include the colony goal and phase description
   - Include the hive_text as "PRE-EXISTING COLONY WISDOM" section
   - Include the context_capsule_prompt (already loaded in Step 2.5)
   - Research areas: (1) Key patterns in existing codebase, (2) External library/API docs if relevant, (3) Common gotchas and pitfalls, (4) Recommended implementation approach
   - Scope constraints: Maximum 5 key patterns, maximum 3 gotchas, maximum 1 recommended approach paragraph, total output under 3000 words
   - Instruct scout to check hive wisdom first and not re-discover known patterns
   - Output format: JSON with fields `hive_wisdom_used`, `key_patterns` (array of {pattern, source, relevance}), `external_context` (array of {topic, finding, source}), `gotchas` (array of {issue, prevention, source}), `recommended_approach` (string), `files_to_study` (array of paths)
   - FALLBACK: If "Agent type not found", use general-purpose and inject role: "You are a Scout Ant performing Phase Domain Research."

4. **Wait for scout to complete** (blocking -- use TaskOutput with block: true if needed, or direct Task return).

5. **Parse scout JSON output** and write RESEARCH.md to disk. The Queen (plan.md orchestrator) writes the file -- scout is read-only. Write to: `.aether/data/phase-research/phase-{phase_number}-research.md`

   RESEARCH.md format (6 fixed sections per user decision):
   ```markdown
   # Phase {N} Research: {Phase Name}

   **Generated:** {ISO-8601 timestamp}
   **Phase:** {N} - {Phase Name}
   **Research scope:** {brief summary of what was investigated}

   ## Hive Wisdom (Pre-existing Knowledge)
   {Format hive_wisdom_used entries, or "No relevant hive wisdom found" if empty}

   ## Key Patterns
   {Format each key_patterns entry as: **{pattern}:** {relevance} (Source: {source})}

   ## External Context
   {Format each external_context entry as: **{topic}:** {finding} (Source: {source})}
   {If empty: "No external research needed for this phase"}

   ## Gotchas
   {Format each gotchas entry as: **{issue}:** {prevention} (Source: {source})}

   ## Recommended Approach
   {recommended_approach text}

   ## Files to Study
   {Format as bullet list of file paths}
   ```

6. **Store research findings** in a variable `research_findings_summary` for injection into the Route-Setter prompt in Step 4. This is a compact summary (not the full RESEARCH.md) -- format as:
   ```
   Key Patterns: {bullet list of pattern names}
   Gotchas: {bullet list of gotcha titles}
   Recommended: {recommended_approach, first sentence only}
   Files: {comma-separated file paths}
   ```

7. **Display completion:**
   ```
   Research complete: phase-{phase_number}-research.md ({word_count} words)
   ```

8. **Update the Route-Setter prompt in Step 4** to include the research findings. In the existing `--- RESEARCH FINDINGS ---` section of the Route-Setter prompt, add the `research_findings_summary` variable after the scout findings and before "Remaining Gaps". This gives the planner domain knowledge alongside codebase knowledge.

After modifying the Claude plan.md, apply the **same changes** to `.opencode/commands/ant/plan.md` with the following OpenCode-specific adjustments:
- OpenCode plan.md already has a "Step -1: Normalize Arguments" -- the new Step 3.6 goes in the same position (between 3.5 and 4)
- Agent spawning uses the OpenCode equivalent mechanism (see existing patterns in the OpenCode file)
- All other logic is identical

Run `npm run lint:sync` after both files are modified to verify command parity (per Pitfall 6 from research).
  </action>
  <verify>
1. Grep `.claude/commands/ant/plan.md` for "Step 3.6" -- must exist
2. Grep `.claude/commands/ant/plan.md` for "phase-research" -- must exist
3. Grep `.claude/commands/ant/plan.md` for "hive-read" -- must exist in Step 3.6
4. Grep `.opencode/commands/ant/plan.md` for "Step 3.6" -- must exist
5. Verify Step 3.6 appears between Step 3.5 and Step 4 in both files
6. Run `npm run lint:sync` -- must pass
  </verify>
  <done>
Both plan.md files contain Step 3.6: Phase Domain Research that (1) reads hive wisdom, (2) cleans previous research, (3) spawns a scope-constrained research scout, (4) writes structured RESEARCH.md to .aether/data/phase-research/, (5) passes findings to the Route-Setter, and (6) always runs on every /ant:plan invocation. OpenCode mirror is in sync.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests for research step in plan.md</name>
  <files>tests/bash/test-plan-research.sh</files>
  <action>
Create a new bash test file `tests/bash/test-plan-research.sh` that validates the research infrastructure. These are structural/integration tests (not live agent tests -- we cannot spawn real agents in tests).

Tests to include:

1. **Research directory creation:** Verify that `mkdir -p .aether/data/phase-research` creates the directory structure correctly. Set up a temp directory, run the mkdir, assert directory exists.

2. **RESEARCH.md structure validation:** Create a sample RESEARCH.md file with all 6 required sections (Hive Wisdom, Key Patterns, External Context, Gotchas, Recommended Approach, Files to Study). Validate that each section header exists using grep.

3. **Research file cleanup on re-plan:** Create a dummy research file at `.aether/data/phase-research/phase-1-research.md`, then run `rm -f` on it, verify it no longer exists. This tests the "always re-research from scratch" behavior.

4. **Hive-read graceful failure:** Run `bash .aether/aether-utils.sh hive-read --limit 5 --format text 2>/dev/null` in a test environment (may not have hive initialized). Verify it either returns valid JSON or empty output -- never crashes the calling script.

5. **Research file naming convention:** Verify the naming pattern `phase-{N}-research.md` produces correct filenames for phases 1, 10, and 99.

Follow the existing test patterns in `tests/bash/` (look at test-oracle-*.sh for structure). Use the standard pass/fail assertion helpers. Each test should be independent (create its own temp directory, clean up after).
  </action>
  <verify>
1. File exists: `tests/bash/test-plan-research.sh`
2. Run: `bash tests/bash/test-plan-research.sh` -- all tests pass
3. Test count: at least 4 test cases
  </verify>
  <done>
Test file validates: research directory creation, RESEARCH.md section structure, file cleanup on re-plan, hive-read graceful degradation, and file naming convention. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "Step 3.6" .claude/commands/ant/plan.md` shows the new step
2. `grep -n "Step 3.6" .opencode/commands/ant/plan.md` shows the mirrored step
3. `npm run lint:sync` passes (command parity)
4. `bash tests/bash/test-plan-research.sh` passes all tests
5. The RESEARCH.md structure has exactly 6 sections: Hive Wisdom, Key Patterns, External Context, Gotchas, Recommended Approach, Files to Study
</verification>

<success_criteria>
- /ant:plan contains a research step (Step 3.6) that runs before the planning loop
- Research scout is scope-constrained (max 3000 words, 5 patterns, 3 gotchas)
- Scout checks hive wisdom first
- RESEARCH.md is written to .aether/data/phase-research/ with standard structure
- Re-planning always deletes and regenerates research
- OpenCode mirror is in sync
- Tests validate the research infrastructure
</success_criteria>

<output>
After completion, create `.planning/phases/14-planning-depth/14-01-SUMMARY.md`
</output>
