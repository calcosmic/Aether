---
phase: 172-wiring-proof
plan: 04
type: execute
wave: 2
depends_on: ["172-02"]
files_modified:
  - cmd/cli_flag_audit_test.go
  - cmd/testdata/flag_audit_skiplist_baseline.json
  - .aether/docs/orphan-allowlist-policy.md
autonomous: true
requirements: [WIRE-01, WIRE-03]
user_setup: []

must_haves:
  truths:
    - "Adding a third entry to the flag audit's `skipSubcommands` map fails a test, naming the added entry"
    - "The skip list and the orphan allowlist are guarded by the same comparator, so exemption pressure cannot relocate to whichever list is unguarded"
    - "A written policy names every file the guards read, and a test derives that list from the code rather than from the prose"
  artifacts:
    - path: "cmd/testdata/flag_audit_skiplist_baseline.json"
      provides: "The committed baseline for the flag audit's skip list"
      contains: "verify-castes"
    - path: "cmd/cli_flag_audit_test.go"
      provides: "TestCLIFlagAudit plus the shrink-only guard over its skipSubcommands map"
      exports: ["TestFlagAuditSkipListOnlyShrinks", "TestAllowlistPolicyNamesEveryGuardedFile"]
    - path: ".aether/docs/orphan-allowlist-policy.md"
      provides: "The written shrink-only policy for both guarded lists, in plain English"
      contains: "orphan_allowlist_baseline.json"
  key_links:
    - from: "cmd/cli_flag_audit_test.go skipSubcommands"
      to: "cmd/testdata/flag_audit_skiplist_baseline.json"
      via: "the same set-membership comparator the orphan allowlist uses"
      pattern: "flag_audit_skiplist_baseline\\.json"
    - from: ".aether/docs/orphan-allowlist-policy.md"
      to: "the four guarded data files"
      via: "TestAllowlistPolicyNamesEveryGuardedFile deriving paths from the Go constants"
      pattern: "orphan-allowlist-policy\\.md"
---

<objective>
Close the second, currently unguarded exemption list.

`cmd/cli_flag_audit_test.go` holds a `skipSubcommands` map with two entries and no guard at
all — a third can be added silently by anyone who finds it easier than fixing the call. Once
the orphan allowlist becomes shrink-only, this map is the path of least resistance. D-12
exists for exactly that reason: apply the same discipline, or exemption pressure simply
relocates.

Purpose: WIRE-01 (D-12) and WIRE-03. Also produces the written policy that makes both lists
legible to a non-technical operator, following this repo's existing "policy doc + checked-in
data + named CI gate" template.
Output: A shrink-only guard over `skipSubcommands`, its committed baseline, and a policy
document whose accuracy is asserted by a test rather than assumed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-wiring-proof/172-CONTEXT.md
@.planning/phases/172-wiring-proof/172-RESEARCH.md
@.planning/phases/172-wiring-proof/172-PATTERNS.md
@.planning/phases/172-wiring-proof/172-02-SUMMARY.md
@CLAUDE.md

<interfaces>
From cmd/subcommand_reachability_ratchet_test.go (created by plan 172-02, same package —
reuse its comparator; do not write a second one):
  the set-membership diff helper that fails naming every name present in a live list and
  absent from its committed baseline, and the JSON entry type with fields
  `name`, `reason`, `owner_phase`.
  Also: TestWiringGuardsHaveNoRuntimeEscapeHatch already reads this plan's target file and
  asserts it contains no os.Getenv / t.Skip / build constraint. Do not add one.

From cmd/cli_flag_audit_test.go (current, lines 21-40):
  func TestCLIFlagAudit(t *testing.T)
  markdownDirs := []string{"../.claude/commands/ant/", "../.opencode/commands/ant/", "../.aether/docs/command-playbooks/"}
  skipSubcommands := map[string]bool{
      "verify-castes":     true,  // markdown-only command, no Go subcommand
      "pending-decisions": true,  // playbook shorthand; actual Go subcommands are pending-decision-{add,list,resolve}
  }

Precedent for the policy doc (read it for tone and structure, do not copy its content):
  .aether/docs/command-catalog-policy.md, paired with cmd/testdata/command_catalog.json and
  the "Verify command catalog classification" CI step.

Note on scope: cmd/cli_flag_audit_test.go is the OLDER, regex-only audit. It cannot see
positional-argument violations, which is why cmd/command_call_audit_test.go was written.
Leave its scanning logic alone. This plan touches only its exemption list.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give the flag audit's skip list the same shrink-only guard as the orphan allowlist</name>

  <read_first>
    - cmd/cli_flag_audit_test.go — the full file (188 lines). The `skipSubcommands` map is at lines 37-40; understand how it is consulted at line ~93 before changing where it lives.
    - cmd/subcommand_reachability_ratchet_test.go — as created by 172-02. Read the comparator helper's exact signature and failure-message shape; you are calling it, not reimplementing it.
    - cmd/testdata/orphan_allowlist_baseline.json — the JSON shape to mirror.
    - .planning/phases/172-wiring-proof/172-CONTEXT.md § D-10, D-11, D-12.
  </read_first>

  <files>cmd/cli_flag_audit_test.go, cmd/testdata/flag_audit_skiplist_baseline.json</files>

  <action>
Promote `skipSubcommands` from a function-local map literal to a package-level declaration
in `cmd/cli_flag_audit_test.go` so a companion test can read it, keeping its two existing
entries and their explanatory comments verbatim. Give each entry the same structured shape
the orphan allowlist uses — name, reason, owning phase — so the two lists are queryable the
same way. `verify-castes` and `pending-decisions` are both markdown-only shorthands with no
Go subcommand; tag them `"reason": "markdown-only-shorthand"` and `"owner_phase": "RECLAIM"`,
matching the parked RECLAIM item in `.planning/REQUIREMENTS.md` § Future Requirements. Keep
`TestCLIFlagAudit`'s existing lookup working against the new declaration; do not change what
it skips or how it scans.

Create `cmd/testdata/flag_audit_skiplist_baseline.json` containing exactly those two entries,
sorted by name, in the same JSON array-of-objects format as
`cmd/testdata/orphan_allowlist_baseline.json`, with a trailing newline.

Add `TestFlagAuditSkipListOnlyShrinks` in the same file, calling the comparator 172-02
created. It must fail, naming each offending entry, for any name present in the live
`skipSubcommands` declaration and absent from the baseline JSON. Same rules as the orphan
allowlist and for the same reasons: set membership, not counts (a one-out-one-in swap must
fail); no comparison against git history (CI shallow-clones); no override field, no sign-off
path, no environment variable. The failure message must tell a non-technical operator that a
command was added to the tolerated list without being added to the baseline, and that the fix
is to make the call correct or delete it — not to edit the baseline.

This plan may not edit `cmd/subcommand_reachability_ratchet_test.go`. If 172-02's comparator
signature does not accept a second list, replicate the same set-membership logic and the same
message shape here and record in the SUMMARY that the two implementations must be kept in
step, so a later phase can unify them. Prefer calling the existing helper whenever its
signature allows it.
  </action>

  <acceptance_criteria>
    - `go test ./cmd -run 'TestCLIFlagAudit|TestFlagAuditSkipListOnlyShrinks' -count=1 -v` passes with both tests shown as RUN and PASS.
    - `cmd/testdata/flag_audit_skiplist_baseline.json` parses as a JSON array of exactly two objects with keys `name`, `reason`, `owner_phase`, containing `verify-castes` and `pending-decisions`.
    - Behavioural proof, recorded in the SUMMARY: add a third entry named `skiplist-proof-entry` to the live `skipSubcommands` declaration, run `go test ./cmd -run TestFlagAuditSkipListOnlyShrinks -count=1`, confirm it FAILS with output containing `skiplist-proof-entry`, then revert and confirm it passes. Paste both outputs.
    - One-out-one-in proof, recorded in the SUMMARY: remove `verify-castes` and add `skiplist-swap-proof`, confirm FAIL, revert. This is the case a count comparison would pass.
    - `TestCLIFlagAudit` still skips exactly `verify-castes` and `pending-decisions` — its own pass/fail behaviour is unchanged by this refactor.
    - `go test ./cmd -run TestWiringGuardsHaveNoRuntimeEscapeHatch -count=1` still passes: this file remains free of `os.Getenv`, `t.Skip` and build constraints.
  </acceptance_criteria>

  <verify>
    <automated>go test ./cmd -run 'TestCLIFlagAudit|TestFlagAuditSkipListOnlyShrinks|TestWiringGuardsHaveNoRuntimeEscapeHatch' -count=1 -v && python3 -c "import json;d=json.load(open('cmd/testdata/flag_audit_skiplist_baseline.json'));assert len(d)==2;assert {e['name'] for e in d}=={'verify-castes','pending-decisions'};assert all(set(e)=={'name','reason','owner_phase'} for e in d);print('ok')"</automated>
  </verify>

  <done>Both exemption lists in this repo are shrink-only against a committed baseline, so there is no unguarded list left for exemption pressure to move to.</done>
</task>

<task type="auto">
  <name>Task 2: Write the shrink-only policy and assert it names every guarded file</name>

  <read_first>
    - .aether/docs/command-catalog-policy.md — the precedent for tone, structure and length. This is the repo's working example of a policy doc paired with checked-in data and a CI gate.
    - cmd/subcommand_reachability_ratchet_test.go and cmd/cli_flag_audit_test.go — for the exact path constants of all four guarded files, which the policy must name and the test must derive from the code.
    - CLAUDE.md § "The owner is non-technical. This repo's vocabulary is invented." and § "Definition of Done" — the doc is for a reader who does not know this repo's words, and a documentation claim about runtime behaviour must be testable or removed.
    - .planning/phases/172-wiring-proof/172-02-SUMMARY.md — the real seeded orphan count, which the policy must state accurately.
  </read_first>

  <files>.aether/docs/orphan-allowlist-policy.md, cmd/cli_flag_audit_test.go</files>

  <action>
Write `.aether/docs/orphan-allowlist-policy.md`. Keep it to roughly one screen. It must
cover, in plain English before any file path or Go identifier appears:

  - What the rule is: a command that nothing calls does not ship. If you add a command, add
    something that runs it, or delete the command.
  - What the two lists are: a written record of the commands that already break that rule
    today, tolerated because they predate the rule. Each entry says why it is tolerated and
    which phase owns fixing it.
  - The one thing that is not allowed: a list may only get shorter. Adding a name fails CI.
    There is no override flag, no reviewer sign-off, no environment variable. Changing a
    baseline is possible, but only as a visible edit to a checked-in file that shows up in a
    code review diff.
  - Why it is built this way, briefly: counting entries would let a one-out-one-in swap
    through; comparing against git history breaks under CI's shallow clones.
  - The real numbers as seeded, taken from 172-02-SUMMARY.md: how many entries the orphan
    list has today and how many carry `owner_phase: 178`, since Phase 178 measures those
    reaching zero.
  - A closing "Detail" section listing the four guarded files — the two live lists and their
    two baselines — and the names of the tests that enforce them. Paths and identifiers go
    here, not in the explanation above.

Then add `TestAllowlistPolicyNamesEveryGuardedFile` to `cmd/cli_flag_audit_test.go`. It must
derive the guarded-file paths from the Go path constants the guards use, read
`.aether/docs/orphan-allowlist-policy.md`, and fail naming any guarded file whose path does
not appear in the document. This is an invariant, not a section-presence check: rename or add
a guarded data file and the doc goes stale loudly. CLAUDE.md names the alternative — three
files described phase-end consolidation running for months while it never ran.
  </action>

  <acceptance_criteria>
    - `.aether/docs/orphan-allowlist-policy.md` exists and its first paragraph contains no file path, no Go identifier, and no repo-invented term left untranslated.
    - `go test ./cmd -run TestAllowlistPolicyNamesEveryGuardedFile -count=1 -v` passes.
    - Behavioural proof, recorded in the SUMMARY: delete the line naming `flag_audit_skiplist_baseline.json` from the policy doc, run the test, confirm it FAILS naming that file, restore, confirm it passes. Paste both outputs.
    - The orphan-list entry count stated in the policy doc equals the number of entries in `cmd/testdata/orphan_allowlist.json`, verified by the automated command below rather than by eye.
    - The policy doc states the number of entries carrying `owner_phase: 178` and that number matches the file.
    - `go test ./cmd/... -count=1 -timeout 900s` passes.
  </acceptance_criteria>

  <verify>
    <automated>go test ./cmd -run 'TestAllowlistPolicyNamesEveryGuardedFile|TestFlagAuditSkipListOnlyShrinks|TestOrphanAllowlistOnlyShrinks' -count=1 -v && python3 -c "import json; d=json.load(open('cmd/testdata/orphan_allowlist.json')); n=len(d); k=len([e for e in d if e['owner_phase']=='178']); p=open('.aether/docs/orphan-allowlist-policy.md').read(); assert str(n) in p, 'policy doc does not state the real entry count %d' % n; assert str(k) in p, 'policy doc does not state the phase-178 count %d' % k; print('policy states', n, 'entries,', k, 'owned by phase 178')"</automated>
  </verify>

  <done>The written policy exists, is legible to someone who does not read code, and cannot silently drift out of step with the files it describes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor / PR → the CI gate | The only boundary. This plan adds test-time comparisons over checked-in files and one document. No network, auth, persistence or untrusted-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-14 | Tampering | `skipSubcommands` in `cmd/cli_flag_audit_test.go` | mitigate | With the orphan allowlist guarded and this map not, the cheapest way to silence a broken documented call becomes adding a third entry here. Mitigated by `TestFlagAuditSkipListOnlyShrinks` diffing the live declaration against `cmd/testdata/flag_audit_skiplist_baseline.json` by set membership, proven by the add-an-entry and one-out-one-in transcripts. |
| T-172-15 | Repudiation | the policy document | mitigate | A policy that describes a guard the code no longer implements is worse than no policy: it makes the gap invisible. Mitigated by `TestAllowlistPolicyNamesEveryGuardedFile` deriving the file list from the Go constants and by the automated count check tying the stated numbers to the real JSON. |
| T-172-16 | Elevation of privilege | — | accept | Nothing here grants any capability or touches runtime behaviour. The change is confined to test code, one test fixture and one document. |
</threat_model>

<verification>
1. `go vet ./cmd` — clean.
2. `go test ./cmd -run 'TestCLIFlagAudit|TestFlagAuditSkipList|TestAllowlistPolicy|TestOrphanAllowlist|TestWiringGuards' -count=1 -v` — all pass, none skipped.
3. `go test ./cmd/... -count=1 -timeout 900s` — no regression.
4. The three red-then-green transcripts required by the acceptance criteria are present in the SUMMARY.
</verification>

<success_criteria>
- D-12 is satisfied: the flag audit's skip list carries the same shrink-only treatment as the orphan allowlist, so exemption pressure has nowhere unguarded to move to.
- D-11's "no escape hatch" holds across both lists, asserted rather than assumed.
- The written policy states the real seeded numbers and cannot drift out of step with the files it describes.
</success_criteria>

<output>
After completion, create `.planning/phases/172-wiring-proof/172-04-SUMMARY.md`.
Record: whether the 172-02 comparator was reused or replicated, and the three red-then-green
transcripts.
</output>
