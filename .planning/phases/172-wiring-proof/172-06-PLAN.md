---
phase: 172-wiring-proof
plan: 06
type: execute
wave: 5
depends_on: ["172-00", "172-03", "172-05"]
gap_closure: true
files_modified:
  - cmd/command_call_audit_test.go
  - .aether/docs/command-playbooks/continue-full.md
  - .aether/docs/command-playbooks/continue-advance.md
  - .aether/docs/command-playbooks/continue-finalize.md
  - .aether/docs/command-playbooks/build-full.md
  - .aether/docs/command-playbooks/build-wave.md
  - .aether/docs/command-playbooks/build-verify.md
  - .aether/docs/command-playbooks/build-complete.md
  - .github/workflows/ci.yml
  - .planning/phases/172-wiring-proof/deferred-items.md
autonomous: true
requirements: [WIRE-03]
user_setup: []

must_haves:
  truths:
    - "ROADMAP success criterion 3, verbatim: 'A test enumerates every `aether …` invocation in `.aether/*.md` and fails naming the file, the line, and the offending flag when an instruction names a flag the binary does not register. Seeded to fail today against `--enforce`, and it passes only once criterion 2 does'"
    - "A closing code-fence marker glued to the end of a content line does not desync the extractor's in-fence/out-of-fence state, so no invocation after it becomes invisible to the audit"
    - "`.aether/docs/command-playbooks/continue-full.md:1243` no longer passes a bare positional to `midden-recent-failures`, and the audit can see the line either way"
    - "A single command fails when any file in the audited corpus gains a code-fence marker glued to trailing content, so the defect class cannot return unnoticed"
    - "Every `aether` invocation the fence repair makes newly visible either satisfies its command's real cobra contract or is a reported violation — none is silently absorbed"
  artifacts:
    - path: "cmd/command_call_audit_test.go"
      provides: "Fence-tolerant extraction, the glued-marker pinning test, and the corpus-wide glued-marker sweep test"
      exports: ["TestExtractorDoesNotDesyncOnGluedFenceMarker", "TestAuditedCorpusHasNoGluedFenceMarkers", "auditedFilePaths"]
    - path: ".aether/docs/command-playbooks/continue-full.md"
      provides: "Two corrected invocations and three de-glued fence markers"
      contains: "midden-recent-failures --limit 50"
    - path: ".github/workflows/ci.yml"
      provides: "The named wiring step's -run filter extended to the two new guard tests"
      contains: "TestAuditedCorpusHasNoGluedFenceMarkers"
  key_links:
    - from: "cmd/command_call_audit_test.go extractDocumentedCalls"
      to: ".aether/docs/command-playbooks/*.md lines that follow a glued fence marker"
      via: "a fence toggle that fires on a marker anywhere in the line, after processing the content before it"
      pattern: "TestExtractorDoesNotDesyncOnGluedFenceMarker"
    - from: "cmd/command_call_audit_test.go TestAuditedCorpusHasNoGluedFenceMarkers"
      to: "auditedCorpora + auditedFiles"
      via: "the shared auditedFilePaths enumeration, so the sweep cannot drift from the audited set"
      pattern: "auditedFilePaths"
---

<objective>
Close verification gap 1: ROADMAP success criterion 3 is FALSE as stated today. The audit
reports "954 documented invocations, 0 violations" while a live positional-argument violation
of exactly the class WIRE-03 exists to catch sits inside its declared corpus, invisible.

The cause is not the violation. It is that `extractDocumentedCalls` only recognises a code-fence
marker when the marker is the first non-whitespace content on its line. Fourteen lines across
seven playbook files glue a closing marker to the end of content, so fence parity desyncs and
whole regions of those files are classified as the wrong side of a fence. `continue-full.md:1243`
(`aether midden-recent-failures 50` — a bare positional handed to a `cobra.NoArgs` command)
lives in one of those regions.

Measured before planning, not assumed: with all fourteen markers repaired, the audit's
invocation count rises from **954 to 988** and produces exactly **four** findings — two contract
violations (`continue-full.md:1243` and `:1531`) and two commands with no D-01 severity
classification (`backup-prune-global`, `temp-clean`). Nothing else in the corpus is hiding.

This plan is ordered so the tree is green at the end of every task: fix the content the audit
cannot yet see FIRST, then give the audit its sight back, then repair the markdown and pin the
defect class shut.

Purpose: WIRE-03 (currently BLOCKED). Closes VERIFICATION.md gap 1 in full.
Output: A fence-tolerant extractor, two corrected invocations, two deliberate severity
classifications, fourteen repaired fence markers, and two new guard tests — one pinning the
extractor against desync, one failing when the defect shape reappears anywhere in the corpus.
</objective>

## Objective — locked decisions honoured

- **D-06** — The retired playbooks (`.aether/docs/command-playbooks/*.md`) stay **in scope for
  the flag audit**: a stale doc naming a non-existent flag is still misleading to a human reader.
  All fourteen glued markers and both corrected invocations are in that directory, so D-06 is
  what makes this work in scope rather than deferrable.
- **D-14** — Fix the command, not the manual, when the runtime is wrong. Here the reverse holds
  and is stated deliberately: `midden-recent-failures` and `generate-commit-message` have
  correct, deliberate contracts (`cobra.NoArgs` plus flags) and correct sibling call sites
  already in the repo, so the *instruction* is the thing that is wrong. This is the same ruling
  172-00 made at its four sibling call sites — not a reversal of D-14.

## Scope exclusions (recorded, not silently dropped)

Findings in `172-REVIEW.md` deliberately NOT addressed by this plan or its siblings:

- **CR-06** (`.aether/workers.md:329,376` — `swarm-display-update` remediation makes the audit
  green with a call that can only fail at runtime) — excluded: not a VERIFICATION.md gap, and
  choosing between fixing `cmd/swarm.go` and rewriting the instruction is a decision, not a
  repair. Needs its own plan.
- **CR-07** (`continue-gates.md` remediation reads the wrong JSON level and can only return
  `{}`) — excluded, same reason: a runtime-behaviour defect in a shipped instruction, outside
  both confirmed gaps.
- **IN-01** (`--` terminator not honoured by the cobra validator), **IN-02**
  (`tokenizeShellLike` swallows the rest of a line on an unmatched `<` or `{`), **IN-03** (a
  test writes into the checked-in source tree), **IN-04** (minor hygiene) — excluded: latent,
  no live instance, and none is load-bearing for either gap.
- **Duplicated shrink-only comparator** across `subcommand_reachability_ratchet_test.go` and
  `cli_flag_audit_test.go` — excluded: already recorded by 172-04 as a deliberate follow-up.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-wiring-proof/172-CONTEXT.md
@.planning/phases/172-wiring-proof/172-VERIFICATION.md
@.planning/phases/172-wiring-proof/172-00-SUMMARY.md
@.planning/phases/172-wiring-proof/172-03-SUMMARY.md
@.planning/phases/172-wiring-proof/deferred-items.md
@CLAUDE.md

<interfaces>
The extractor and its corpus (cmd/command_call_audit_test.go):
  var auditedCorpora = []string{ .claude/commands/ant, .opencode/commands/ant,
      .aether/commands, .aether/docs/command-playbooks, colony/playbooks }   // walked recursively
  var auditedFiles   = []string{ .aether/CONTEXT.md, .aether/CROWNED-ANTHILL.md,
      .aether/QUEEN.md, .aether/workers.md }                                  // exact files
  func extractDocumentedCalls(t *testing.T, path string) []documentedCall     // line 160; fence toggle at line 170
  func parseFencedInvocation(path string, line int, text string) (documentedCall, bool)  // line 253
  func collectDocumentedCalls(t *testing.T, root string) []documentedCall     // line 308; walks corpora then files
  func validateCallAgainstCobra(call documentedCall) string                   // line 443; "" means contract satisfied
  var knownEnrichmentSubcommands map[string]bool                              // line 840; the D-01 enrichment tier
  func TestCommandCallsMatchCobraContracts(t *testing.T)                      // line 537; logs the audited count at line 586
  func TestDocumentedSubcommandsAreSeverityClassified(t *testing.T)           // line 1005; forces the gate/enrichment decision
  func TestCommandCallExtractorSeesRealInvocationsAndSkipsProse(t *testing.T) // line 708; the extractor's own pinning test

From cmd/command_source_hygiene_test.go:
  func repoRootForCommandSourceTest() (string, error)     // line 157; repo-relative path resolution

From cmd/command_call_severity.go:
  var gateClassifiedCommands map[string]string            // line 37; halting-condition commands, each with a rationale

The contracts the two broken instructions violate:
  cmd/midden_cmds.go:16-19   Use: "midden-recent-failures", Args: cobra.NoArgs
  cmd/midden_cmds.go:492     middenRecentFailuresCmd.Flags().Int("limit", 10, "Max entries to return")
  cmd/generate_cmds.go:81-83 Use: "generate-commit-message", Args: cobra.NoArgs
  cmd/generate_cmds.go:44    validCommitTypes includes "contextual"
  cmd/generate_cmds.go:93-95 the command reads --type, --subject, --scope, --body — no positionals

The correct sibling call site to copy verbatim (already in the repo, already audited green):
  .aether/docs/command-playbooks/continue-finalize.md:227
    aether generate-commit-message --type contextual --scope "{phase_id}-{plan_number}" --subject "{phase_name}" --body "{ai_description}"

The fourteen glued closing fence markers, measured (file:line, current working tree):
  continue-advance.md:504
  continue-full.md:1194, 1759, 1763
  build-full.md:851, 913, 1398
  continue-finalize.md:452, 456
  build-complete.md:133
  build-wave.md:672, 734
  build-verify.md:388, 425
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the two invocations and classify the two commands the audit is about to see</name>

  <read_first>
    - `.aether/docs/command-playbooks/continue-full.md` lines 1188-1200 (the glued marker at 1194 — read it, do not fix it yet), 1240-1246 (the `midden-recent-failures` call at 1243), and 1518-1545 (the `generate-commit-message` call at 1531 with its surrounding "Step 2.4.2" instructions).
    - `.aether/docs/command-playbooks/continue-finalize.md` line 227 — the correct flag-form `generate-commit-message` invocation to copy. This is the authoritative target state; do not invent a different flag ordering.
    - `cmd/midden_cmds.go` lines 14-24 and 486-495 — `Args: cobra.NoArgs` and the `--limit` int flag registration. This is why the bare positional is a violation.
    - `cmd/generate_cmds.go` lines 43-45 and 78-125 — `validCommitTypes` (which contains `contextual`), `Args: cobra.NoArgs`, and the four flags the RunE actually reads.
    - `cmd/command_call_audit_test.go` lines 834-1000 — `knownEnrichmentSubcommands` and its doc comment, which explicitly forbids adding entries reflexively. Read the doc comment before adding anything.
    - `cmd/command_call_severity.go` lines 29-49 — `gateClassifiedCommands` and its three existing entries with rationales, so you can judge whether either new command belongs there instead.
    - `.aether/docs/command-playbooks/continue-full.md` lines 1755-1768 — the `aether backup-prune-global` and `aether temp-clean` lines (1759, 1763), which are themselves glued to closing fence markers and are the source of the two unclassified commands.
  </read_first>

  <files>.aether/docs/command-playbooks/continue-full.md, cmd/command_call_audit_test.go</files>

  <action>
Three edits. All three fix content that the audit **cannot currently see**, so the whole suite
must still be green when this task ends and the audited invocation count must still read 954.
That is deliberate: the content is repaired before the extractor is given its sight back, so no
intermediate commit is red.

**1. `.aether/docs/command-playbooks/continue-full.md:1243`.** The line currently reads
`midden_result=$(aether midden-recent-failures 50 2>/dev/null || echo '{"count":0,"failures":[]}')`.
Change the bare positional `50` to the flag form `--limit 50`, so the invocation reads
`aether midden-recent-failures --limit 50`. Leave the `2>/dev/null`, the `|| echo` fallback and
the surrounding `midden_result=$(…)` assignment exactly as they are. The target state is
byte-identical in shape to `.aether/docs/command-playbooks/continue-advance.md:553`, which
172-00 already fixed the same way — read that line and match it.

**2. `.aether/docs/command-playbooks/continue-full.md:1531`.** The line currently reads
`aether generate-commit-message "contextual" {phase_id} "{phase_name}" "{ai_description}" {plan_number}`
— five positional arguments handed to a `cobra.NoArgs` command. This is the old shell-utils
signature; the Go command has never accepted it. Replace the whole invocation with the flag
form used at `.aether/docs/command-playbooks/continue-finalize.md:227`, copied verbatim
including its placeholder spellings: `--type contextual`, `--scope "{phase_id}-{plan_number}"`,
`--subject "{phase_name}"`, `--body "{ai_description}"`. The prose immediately below the fence
tells the reader to parse `message`, `body`, `files_changed`, `subsystem` and `scope` out of the
returned JSON; the real command only ever returns `message`, so add one sentence after the
fence stating that the command returns `message` only and that the other fields are not produced
by this command. Do not delete the surrounding instructions, and do not delete `continue-full.md`
— Phase 170 criterion 5 owns removing this file, not this plan.

**3. Classify `backup-prune-global` and `temp-clean` in `cmd/command_call_audit_test.go`.**
Both are invoked at `continue-full.md:1759` and `:1763` and at `continue-finalize.md:452` and
`:456`, glued to closing fence markers, and are therefore invisible to
`TestDocumentedSubcommandsAreSeverityClassified` today. Task 3's fence repair makes both visible
and that test will name them. Make the D-01 judgement now, on purpose, as the list's own doc
comment demands: both are housekeeping/cleanup commands whose failure degrades tidiness and
nothing else — no verification result, no security scan, no gate outcome depends on either — so
both belong in `knownEnrichmentSubcommands`, not in `gateClassifiedCommands`. Add both keys to
`knownEnrichmentSubcommands` in the map's existing alphabetical position, and add one short
comment above the two entries recording that the judgement was made here, deliberately, when the
fence repair in this plan's Task 3 made them visible — not inherited from the enrichment default.
Do NOT add them to `gateClassifiedCommands`; state in the SUMMARY why not, in one sentence each.
  </action>

  <acceptance_criteria>
    - `grep -c 'midden-recent-failures --limit 50' .aether/docs/command-playbooks/continue-full.md` returns 1, and `grep -c 'midden-recent-failures 50' .aether/docs/command-playbooks/continue-full.md` returns 0.
    - `grep -c 'generate-commit-message "contextual"' .aether/docs/command-playbooks/continue-full.md` returns 0.
    - `.aether/docs/command-playbooks/continue-full.md` contains a line matching `generate-commit-message --type contextual --scope` and that line's flag set is `--type`, `--scope`, `--subject`, `--body` — the same four flags, in the same order, as `.aether/docs/command-playbooks/continue-finalize.md:227`.
    - `grep -c '"backup-prune-global"' cmd/command_call_audit_test.go` returns 1 and `grep -c '"temp-clean"' cmd/command_call_audit_test.go` returns 1; neither name appears in `cmd/command_call_severity.go`.
    - `go test ./cmd -run 'TestCommandCallsMatchCobraContracts|TestDocumentedSubcommandsAreSeverityClassified|TestDocumentedCommandNamesResolve|TestGateClassifiedCallsHaveGateWiring|TestCLIFlagAudit' -count=1 -v` passes, and the verbose log for `TestCommandCallsMatchCobraContracts` still reads `audited 954 documented invocations across 6 corpora` — unchanged, because the extractor still cannot see any of these lines. A different number at this point means an unintended edit landed; stop and reconcile before Task 2.
    - `go vet ./cmd` is clean.
  </acceptance_criteria>

  <verify>
    <automated>go vet ./cmd && go test ./cmd -run 'TestCommandCallsMatchCobraContracts|TestDocumentedSubcommandsAreSeverityClassified|TestDocumentedCommandNamesResolve|TestGateClassifiedCallsHaveGateWiring|TestCLIFlagAudit' -count=1 -v 2>&1 | tee /tmp/172-06-t1.log | grep -q 'audited 954 documented invocations' && grep -q 'midden-recent-failures --limit 50' .aether/docs/command-playbooks/continue-full.md && ! grep -q 'generate-commit-message "contextual"' .aether/docs/command-playbooks/continue-full.md && grep -q '"temp-clean"' cmd/command_call_audit_test.go && echo TASK1-OK</automated>
  </verify>

  <done>The two hidden violations are corrected against the commands' real contracts, and the two commands the fence repair will expose have a deliberate, recorded D-01 classification — all while the suite stays green and the audit's blind spot is still measurable at 954.</done>
</task>

<task type="auto">
  <name>Task 2: Make the fence toggle see a marker glued to trailing content, and pin it</name>

  <read_first>
    - `cmd/command_call_audit_test.go` lines 159-250 — `extractDocumentedCalls`, in full. The defect is line 170: `strings.HasPrefix(strings.TrimSpace(line), "```")`. Note the exact order of operations inside the loop: fence toggle, then `negatedCallRe`, then the in-fence branch, then the backtick branch.
    - `cmd/command_call_audit_test.go` lines 253-306 — `parseFencedInvocation`, the in-fence branch's parser, which you are calling with a substring rather than a whole line.
    - `cmd/command_call_audit_test.go` lines 699-798 — `TestCommandCallExtractorSeesRealInvocationsAndSkipsProse`, the established fixture idiom: build a markdown file in `t.TempDir()`, run the real extractor over it, assert on the returned calls, and end with an exact `len(calls)` count so the fixture cannot rot into a vacuous pass. Mirror this shape; note that this existing test's own `len(calls) != 6` assertion must stay correct — do not add lines to *its* fixture.
    - `.aether/docs/command-playbooks/continue-full.md` lines 1190-1196 and 1755-1768 — the two shapes of glued marker you must handle: one where the content before the marker carries no `aether` token (`  --ttl "30d"` + marker at 1194) and one where the content before the marker IS an invocation (`aether backup-prune-global` + marker at 1759).
    - `.github/workflows/ci.yml` line 99-100 — the named step `Verify subcommand wiring and CLI flag contracts` and its `-run` alternation, which must gain this task's new test name.
    - `cmd/ci_wiring_gate_test.go` lines 26-113 — `TestWiringGateStepRunsEveryWiringTest`, which AST-enumerates every `func Test…` in the guard files and fails if the CI step's `-run` regex does not match one. This is why the ci.yml edit is mandatory, not cosmetic.
  </read_first>

  <files>cmd/command_call_audit_test.go, .github/workflows/ci.yml</files>

  <action>
**1. Rework the fence toggle in `extractDocumentedCalls` (currently line 170).**

Target behaviour, stated exactly, because "make it tolerant" has more than one meaning and only
one of them is correct:

  - Locate the first occurrence of a triple-backtick marker in the line.
  - **No marker:** behave exactly as today.
  - **Marker is the first non-whitespace content on the line** (today's only recognised case):
    toggle `inFence` and skip the line entirely. Unchanged.
  - **Marker appears after other content on the line** (the glued case): first process the
    substring *before* the marker as an ordinary line under the **current** `inFence` state —
    that means running the same `negatedCallRe` skip and then either `parseFencedInvocation` (if
    currently in a fence) or the backtick-span loop (if not) — and only then toggle `inFence`.
    Discard the remainder of the line after the marker; an opening fence's info string
    (`bash`, `yaml`) never carries an invocation.

Processing the content before the marker is not optional. Two real corpus lines
(`continue-full.md:1759`, `:1763`) are `aether backup-prune-global` and `aether temp-clean`
glued directly to their closing markers. A shortcut that toggles and `continue`s would drop both,
which is the same class of silent loss this gap is about — and Task 3's count-equivalence
criterion will catch it.

To avoid two divergent copies of the per-line body, extract the existing loop body into one
local closure or one package-level helper that takes the line text, the 1-based line number and
the current `inFence` value, appends any calls it finds, and is called from both the ordinary
path and the glued-content path. Do not duplicate the backtick loop.

Handle the multi-marker line by toggling exactly once, at the first marker, and record that
decision in a comment: the corpus-wide sweep test added in Task 3 forbids the glued shape
outright, so a line carrying two markers cannot accumulate in the corpus unnoticed. State the
bound rather than pretending the tolerance is a full CommonMark parser.

**2. Add `TestExtractorDoesNotDesyncOnGluedFenceMarker` to `cmd/command_call_audit_test.go`.**

A new dedicated test — not extra cases inside the existing extractor test, whose exact
`len(calls)` count must stay at 6. Build one markdown file in `t.TempDir()` reproducing the exact
corpus shapes and assert all four properties below; each maps to a way the fix could be wrong:

  - A fenced block whose closing marker is glued to a **non-invocation** content line
    (`  --ttl "30d"` immediately followed by the marker), followed later in the file by a second
    fenced block containing `aether midden-recent-failures --limit 50`. Assert the
    `midden-recent-failures` call IS extracted and that its `InFence` field is true. Before this
    task's change, it is not extracted at all — that is the defect.
  - A fenced block whose closing marker is glued to an **invocation** content line
    (`aether backup-prune-global` immediately followed by the marker). Assert that call IS
    extracted with `InFence` true and zero args. This is the assertion a toggle-and-skip
    shortcut fails.
  - A prose line **outside** any fence that merely mentions a marker mid-sentence together with
    a backticked call — assert the backticked call is still extracted, so the tolerance did not
    make ordinary prose lines disappear.
  - An exact `len(calls)` assertion for the fixture, so a future change that silently stops
    extracting cannot pass this test.

Every failure message must name what was expected and quote the line, in the style of the
existing extractor test.

**3. Extend the named CI step's `-run` alternation in `.github/workflows/ci.yml`.**
Add `TestExtractorDoesNotDesyncOnGluedFenceMarker` to the alternation on the `run:` line of the
step named exactly `Verify subcommand wiring and CLI flag contracts`. Do not touch the blanket
`go test ./... -count=1 -timeout 900s` step. `TestWiringGateStepRunsEveryWiringTest` fails if you
skip this.
  </action>

  <acceptance_criteria>
    - **Red proof (mandatory, transcript in the SUMMARY):** with `TestExtractorDoesNotDesyncOnGluedFenceMarker` written but the toggle at line 170 still in its original `strings.HasPrefix(strings.TrimSpace(line), "```")` form, `go test ./cmd -run TestExtractorDoesNotDesyncOnGluedFenceMarker -count=1 -v` FAILS, and the failure output contains the substring `midden-recent-failures`. Paste that output.
    - **Second red proof (mandatory, transcript in the SUMMARY):** with the toggle fixed but the glued-content path implemented as toggle-and-skip (content before the marker discarded), the same test FAILS naming `backup-prune-global`. Paste that output. This proves the content-before-marker requirement is actually tested and not merely described.
    - **Green:** `go test ./cmd -run TestExtractorDoesNotDesyncOnGluedFenceMarker -count=1 -v` passes with both proofs' mutations reverted.
    - `go test ./cmd -run TestCommandCallsMatchCobraContracts -count=1 -v` passes, and its log line now reads `audited 988 documented invocations across 6 corpora`. The number 988 was measured before planning by repairing the fences and running the pre-existing extractor; a different number means the tolerant toggle is not semantically equivalent to putting the marker on its own line — reconcile before continuing rather than adjusting the expectation.
    - `go test ./cmd -run TestCommandCallExtractorSeesRealInvocationsAndSkipsProse -count=1 -v` still passes with its `len(calls) != 6` assertion unchanged.
    - `go test ./cmd -run 'TestDocumentedSubcommandsAreSeverityClassified|TestDocumentedCommandNamesResolve|TestGateClassifiedCallsHaveGateWiring|TestAuditDetectsPositionalDrift|TestAetherCorpusCatchesAnUnregisteredFlag' -count=1 -v` passes — the two commands Task 1 classified and the two calls Task 1 fixed are why.
    - `.github/workflows/ci.yml` contains `TestExtractorDoesNotDesyncOnGluedFenceMarker` inside the `-run` argument of the step named `Verify subcommand wiring and CLI flag contracts`, and still contains the unmodified `go test ./... -count=1 -timeout 900s` step.
    - `go test ./cmd -run TestWiringGateStepRunsEveryWiringTest -count=1 -v` passes.
    - The per-line body exists in exactly one place: `grep -c 'backtickCallRe.FindAllStringSubmatch' cmd/command_call_audit_test.go` returns 1.
  </acceptance_criteria>

  <verify>
    <automated>go vet ./cmd && go test ./cmd -run 'TestExtractorDoesNotDesyncOnGluedFenceMarker|TestCommandCallExtractorSeesRealInvocationsAndSkipsProse|TestCommandCallsMatchCobraContracts|TestDocumentedSubcommandsAreSeverityClassified|TestDocumentedCommandNamesResolve|TestGateClassifiedCallsHaveGateWiring|TestAuditDetectsPositionalDrift|TestAetherCorpusCatchesAnUnregisteredFlag|TestWiringGateStepRunsEveryWiringTest' -count=1 -v 2>&1 | tee /tmp/172-06-t2.log | grep -q 'audited 988 documented invocations' && test "$(grep -c 'backtickCallRe.FindAllStringSubmatch' cmd/command_call_audit_test.go)" = "1" && grep -q 'TestExtractorDoesNotDesyncOnGluedFenceMarker' .github/workflows/ci.yml && grep -q 'go test ./\.\.\. -count=1 -timeout 900s' .github/workflows/ci.yml && echo TASK2-OK</automated>
  </verify>

  <done>The extractor sees 988 invocations instead of 954, the previously invisible region of `continue-full.md` is audited, and a dedicated test fails if a glued closing marker ever desyncs fence parity again.</done>
</task>

<task type="auto">
  <name>Task 3: Repair all fourteen glued markers, and make the sweep a test rather than a one-off pass</name>

  <read_first>
    - Each of the seven files at the exact lines listed in this plan's `<interfaces>` block: `continue-advance.md:504`; `continue-full.md:1194, 1759, 1763`; `build-full.md:851, 913, 1398`; `continue-finalize.md:452, 456`; `build-complete.md:133`; `build-wave.md:672, 734`; `build-verify.md:388, 425`. Re-derive the list from the working tree before editing — line numbers shift as you edit each file.
    - `cmd/command_call_audit_test.go` lines 308-341 — `collectDocumentedCalls`, whose corpus walk (recursive `filepath.Walk` over `auditedCorpora`, then the exact `auditedFiles` paths, with the `.md`/`.yaml`/`.yml` extension filter and the tolerant `os.Stat` skip) is the enumeration the sweep must share rather than re-implement.
    - `.planning/phases/172-wiring-proof/deferred-items.md` — the 172-00 entry that flagged `continue-advance.md:503` and deferred it. This plan closes it.
    - `cmd/subcommand_reachability_ratchet_test.go` lines 1171-1224 — this phase's anti-vacuity idiom: `t.Fatalf` when an enumeration finds nothing, and a column-anchored regex written so the check cannot self-trigger on its own source. Mirror the anti-vacuity discipline.
    - `.github/workflows/ci.yml` line 99-100 — the named step's `-run` alternation, which must gain this task's new test name too.
  </read_first>

  <files>.aether/docs/command-playbooks/continue-full.md, .aether/docs/command-playbooks/continue-advance.md, .aether/docs/command-playbooks/continue-finalize.md, .aether/docs/command-playbooks/build-full.md, .aether/docs/command-playbooks/build-wave.md, .aether/docs/command-playbooks/build-verify.md, .aether/docs/command-playbooks/build-complete.md, cmd/command_call_audit_test.go, .github/workflows/ci.yml, .planning/phases/172-wiring-proof/deferred-items.md</files>

  <action>
**1. Repair all fourteen glued closing fence markers.** For each, split the line in two: the
content, with trailing whitespace trimmed, on its own line; the triple-backtick marker alone on
the next line. Change nothing else — no rewording, no reindentation, no removal of the content.
All fourteen are closing markers glued to the last line of a fenced block.

Do not attempt this with a blanket regex across the whole tree. Edit the fourteen known
locations, then re-run the detection to confirm zero remain, then confirm with `git diff --stat`
that exactly seven files changed and the diff adds exactly fourteen lines and modifies fourteen.

**2. Add the corpus-wide sweep as a test: `TestAuditedCorpusHasNoGluedFenceMarkers`.**

CLAUDE.md's Definition of Done governs here: the sweep must be a command someone can run that
fails when a new glued marker appears, not a one-off manual pass recorded in a SUMMARY. Requirements:

  - Factor the file enumeration out of `collectDocumentedCalls` into a helper — name it
    `auditedFilePaths(t *testing.T, root string) []string` — that returns every file the audit
    reads: the recursive walk over `auditedCorpora` filtered to `.md`/`.yaml`/`.yml`, plus each
    existing path in `auditedFiles`. Call it from **both** `collectDocumentedCalls` and the new
    sweep test, so the sweep can never drift from the audited set. This is the whole point of the
    "sweep the audited corpus" requirement: a hardcoded second file list would rot.
  - The sweep fails, listing every offender as `path:line` plus the offending line's text, for
    any line that contains a triple-backtick marker where that marker is not the line's first
    non-whitespace content.
  - Anti-vacuity: `t.Fatalf` if `auditedFilePaths` returns fewer than 200 paths. The measured
    count today is 215 (211 across the five corpora plus 4 exact files); an enumeration that
    silently returns nothing would pass forever, which is the failure mode this phase exists to
    make impossible.
  - Write the marker literal so the test's own source cannot self-trigger, following the
    `buildConstraintRe` precedent at `cmd/subcommand_reachability_ratchet_test.go:72` (string
    concatenation rather than a bare literal). The failure message should explain, in one plain
    sentence, that a marker glued to content desyncs the extractor's fence tracking and hides
    every invocation after it — an operator reading a CI failure should not have to reconstruct
    why this matters.

**3. Add `TestAuditedCorpusHasNoGluedFenceMarkers`** to the `-run` alternation of the
`Verify subcommand wiring and CLI flag contracts` step in `.github/workflows/ci.yml`.

**4. Close the deferred item.** In `.planning/phases/172-wiring-proof/deferred-items.md`, mark
the 172-00 `continue-advance.md` fence entry as closed by plan 172-06, naming the file and the
test that now prevents its return. Keep the original text — the record of what was deferred and
for how long is the useful part; append the closure rather than deleting the entry.
  </action>

  <acceptance_criteria>
    - Zero glued markers remain in the audited corpus: the sweep test passes, and independently `go test ./cmd -run TestAuditedCorpusHasNoGluedFenceMarkers -count=1 -v` reports the number of files it scanned as at least 200.
    - **Red proof (mandatory, transcript in the SUMMARY):** re-glue exactly one marker — put `continue-full.md`'s repaired `--ttl "30d"` line and its closing marker back on one line — then run `go test ./cmd -run TestAuditedCorpusHasNoGluedFenceMarkers -count=1 -v`. It must FAIL and the failure output must contain `continue-full.md:` with the line number and the text `--ttl`. Restore, confirm green. Paste both outputs and confirm `git status --porcelain .aether/docs/command-playbooks/continue-full.md` is empty relative to this task's intended state.
    - **Equivalence proof (mandatory, stated in the SUMMARY):** after the markdown repair, `TestCommandCallsMatchCobraContracts` still logs `audited 988 documented invocations across 6 corpora` — the same number Task 2 produced with the markdown unrepaired. The tolerant extractor and the repaired markdown must agree exactly; any difference means the tolerance is not equivalent to a correctly placed marker and must be reconciled, not papered over.
    - `git diff --stat -- .aether/docs/command-playbooks/` shows exactly 7 files changed and 14 insertions attributable to this task's split (Task 1's two invocation fixes are already committed separately).
    - `grep -rn '```' .aether/docs/command-playbooks/ | grep -v ':[0-9]*:[[:space:]]*```' | wc -l` returns 0.
    - `.planning/phases/172-wiring-proof/deferred-items.md` names plan `172-06` and `TestAuditedCorpusHasNoGluedFenceMarkers` in the closure note for the `continue-advance.md` entry.
    - `.github/workflows/ci.yml` contains `TestAuditedCorpusHasNoGluedFenceMarkers` in the named step's `-run` argument, and `go test ./cmd -run TestWiringGateStepRunsEveryWiringTest -count=1 -v` passes.
    - `go test ./... -count=1 -timeout 900s` passes on the full tree.
  </acceptance_criteria>

  <verify>
    <automated>go vet ./cmd && test "$(grep -rn '```' .aether/docs/command-playbooks/ | grep -v ':[0-9]*:[[:space:]]*```' | wc -l | tr -d ' ')" = "0" && go test ./cmd -run 'TestAuditedCorpusHasNoGluedFenceMarkers|TestCommandCallsMatchCobraContracts|TestExtractorDoesNotDesyncOnGluedFenceMarker|TestWiringGateStepRunsEveryWiringTest|TestDocumentedSubcommandsAreSeverityClassified|TestCLIFlagAudit' -count=1 -v 2>&1 | tee /tmp/172-06-t3.log | grep -q 'audited 988 documented invocations' && grep -q 'TestAuditedCorpusHasNoGluedFenceMarkers' .github/workflows/ci.yml && grep -q '172-06' .planning/phases/172-wiring-proof/deferred-items.md && go test ./... -count=1 -timeout 900s && echo TASK3-OK</automated>
  </verify>

  <done>The audited corpus is free of the defect shape, a runnable command fails the moment it returns, and the extractor's tolerance is proven equivalent to correct markdown by an unchanged invocation count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped instruction markdown → the audit | The audit's only input is text it parses. Anything the parser mis-frames is unaudited, and reports as clean. This is the boundary both this gap and this plan live on. |
| contributor / PR → the CI gate | New guard tests only bite if the named CI step runs them; a new test not added to the `-run` alternation runs under the blanket step but loses its legible failure surface. |
| the guard's own source → itself | The sweep test searches for the same marker text its own source must contain, so it can trivially report itself as an offender or be written to never fire. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-26 | Tampering (of the guard's view of the world) | `extractDocumentedCalls` fence toggle, `cmd/command_call_audit_test.go:170` | mitigate | A closing marker glued to content desyncs in-fence parity and hides every invocation after it — the live realisation of this threat is `continue-full.md:1243`, which reported clean for the whole phase. Mitigated by the toggle firing on a marker anywhere in the line (Task 2), pinned by `TestExtractorDoesNotDesyncOnGluedFenceMarker` with a recorded red proof naming `midden-recent-failures`, and by the corpus-wide `TestAuditedCorpusHasNoGluedFenceMarkers` (Task 3) failing on the shape itself. |
| T-172-27 | Denial of service (of the guard, by over-correction) | the glued-content path in `extractDocumentedCalls` | mitigate | The cheap version of the tolerance — toggle on the marker and skip the whole line — silently discards `aether backup-prune-global` and `aether temp-clean` at `continue-full.md:1759` and `:1763`, trading one blind spot for another. Mitigated by the second mandatory red proof (the test must fail naming `backup-prune-global` under the shortcut implementation) and by the Task 3 equivalence criterion requiring the invocation count to be identical with and without the markdown repair. |
| T-172-28 | Spoofing (of assurance) | `TestAuditedCorpusHasNoGluedFenceMarkers` | mitigate | A sweep whose file enumeration returns nothing — a moved corpus, a wrong root, a silently-swallowed walk error — passes forever while checking nothing, the exact "satisfiable without the property holding" shape CLAUDE.md names. Mitigated by sharing one `auditedFilePaths` enumeration with `collectDocumentedCalls` (so the sweep cannot diverge from the audited set), a `t.Fatalf` below 200 paths against a measured 215, and the re-glue red proof. |
| T-172-29 | Repudiation | `knownEnrichmentSubcommands` gaining `backup-prune-global` and `temp-clean` | mitigate | Two commands become visible only because this plan repairs the fences; adding them to the enrichment tier to make a test green is precisely the reflexive addition the list's own doc comment forbids, and leaves no record of who decided or why. Mitigated by making the judgement in Task 1 before the test can force it, by an in-code comment recording that it was made deliberately at this point, and by an acceptance criterion requiring a one-sentence rationale per command in the SUMMARY for why neither is a gate. |
| T-172-30 | Information disclosure | none | accept | This plan reads and rewrites checked-in documentation and test code only. No secrets, no runtime data paths, no network or filesystem surface beyond `t.TempDir()` fixtures. Accepted with no mitigation. |
</threat_model>

<verification>
1. `go vet ./cmd` — clean.
2. `go test ./cmd -run TestExtractorDoesNotDesyncOnGluedFenceMarker -count=1 -v` — passes, with both recorded red proofs (unfixed toggle; toggle-and-skip shortcut) present in the SUMMARY.
3. `go test ./cmd -run TestAuditedCorpusHasNoGluedFenceMarkers -count=1 -v` — passes, scans ≥ 200 files, with the re-glue red proof recorded.
4. `go test ./cmd -run TestCommandCallsMatchCobraContracts -count=1 -v` — passes and logs `audited 988 documented invocations across 6 corpora`, both before and after the markdown repair.
5. `grep -rn '```' .aether/docs/command-playbooks/ | grep -v ':[0-9]*:[[:space:]]*```'` — no output.
6. The named CI step's command, copied verbatim out of `.github/workflows/ci.yml` and run locally — passes, and its verbose output shows both new tests as RUN and PASS.
7. `go test ./... -count=1 -timeout 900s` — the full release-gate command, green.
8. `git status --porcelain | wc -l` — recorded before the first commit and after the last; the pre-existing count must be unchanged (it was 115 during 172-05).
</verification>

<success_criteria>
- ROADMAP success criterion 3 is TRUE as stated: the audit enumerates every `aether …`
  invocation in its declared corpus, including the region `continue-full.md:1194` used to hide,
  and there is no live violation of the class it exists to catch anywhere inside that corpus.
- WIRE-03 is no longer blocked: a real positional-argument violation inside the audited corpus
  is now reported, and both previously hidden ones are fixed against their commands' real contracts.
- The defect class is closed by a command, not by a claim: re-introducing a glued fence marker
  anywhere in the audited corpus fails a named test whose failure names the file and the line.
- The phase's own deferred finding about `continue-advance.md` is closed rather than carried.
</success_criteria>

<output>
After completion, create `.planning/phases/172-wiring-proof/172-06-SUMMARY.md`.
It MUST contain: the two Task 2 red proofs (unfixed toggle failing on `midden-recent-failures`;
toggle-and-skip shortcut failing on `backup-prune-global`) with their outputs; the Task 3
re-glue red proof and its green restoration; the audited invocation count at the end of each
task (954, 988, 988) with the equivalence statement; and one sentence per newly classified
command explaining why it is enrichment rather than a gate.
</output>
