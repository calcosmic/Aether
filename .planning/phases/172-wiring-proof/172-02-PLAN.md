---
phase: 172-wiring-proof
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/subcommand_reachability_ratchet_test.go
  - cmd/testdata/orphan_allowlist.json
  - cmd/testdata/orphan_allowlist_baseline.json
autonomous: true
requirements: [WIRE-01]
user_setup: []

must_haves:
  truths:
    - "Registering a cobra subcommand with no caller outside its own definition file makes `go test ./cmd -run TestNoRegisteredSubcommandIsUnreferenced` fail, and the failure output names that command"
    - "The orphan allowlist is seeded from the scanner's real output, not from a pre-assumed count"
    - "Adding a name to the live allowlist that is absent from the committed baseline fails the test, naming that command"
    - "Removing the only caller of a currently-called command makes the ratchet name that command as an orphan"
    - "No environment variable, build tag, or skip flag can turn any of these guards off at runtime"
  artifacts:
    - path: "cmd/subcommand_reachability_ratchet_test.go"
      provides: "The orphan ratchet, its shrink-only guard, and its own self-tests"
      exports: ["TestNoRegisteredSubcommandIsUnreferenced", "TestOrphanAllowlistOnlyShrinks", "TestRatchetDetectsASyntheticOrphan", "TestDeletingACallerMakesTheRatchetNameIt", "TestWiringGuardsHaveNoRuntimeEscapeHatch"]
      min_lines: 200
    - path: "cmd/testdata/orphan_allowlist.json"
      provides: "The live, may-only-shrink list of tolerated orphaned subcommands, each with a reason tag and owning phase"
      contains: "owner_phase"
    - path: "cmd/testdata/orphan_allowlist_baseline.json"
      provides: "The committed baseline the live list is diffed against"
      contains: "owner_phase"
  key_links:
    - from: "cmd/subcommand_reachability_ratchet_test.go"
      to: "rootCmd"
      via: "recursive walk of rootCmd.Commands() for the registration truth"
      pattern: "rootCmd\\.Commands\\(\\)"
    - from: "cmd/subcommand_reachability_ratchet_test.go"
      to: "cmd/*.go"
      via: "go/ast var-block walk finding `var xCmd = &cobra.Command{Use: ...}` and its enclosing file"
      pattern: "parser\\.ParseFile"
    - from: "cmd/subcommand_reachability_ratchet_test.go"
      to: "cmd/testdata/orphan_allowlist_baseline.json"
      via: "set-membership diff: any live name absent from the baseline is a failure"
      pattern: "orphan_allowlist_baseline\\.json"
---

<objective>
Build the ratchet: a test that fails when a registered cobra subcommand has no caller
outside its own definition file, seeded honestly with today's orphans in an allowlist that
may only shrink.

This is the standard the rest of milestone v1.26 is measured against, and it lands before
any of the capabilities it constrains, so it is shaped by the standard rather than by
whatever shipped.

Purpose: WIRE-01. 18 of 25 milestones in this repo have been framed around restoring
something previously marked done. A command that exists, passes its tests, and is called by
nothing is the shape that failure takes. This makes that shape fail CI.
Output: `TestNoRegisteredSubcommandIsUnreferenced`, its shrink-only companion, two committed
JSON files, and three self-tests proving the ratchet actually detects what it claims to.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/172-wiring-proof/172-CONTEXT.md
@.planning/phases/172-wiring-proof/172-RESEARCH.md
@.planning/phases/172-wiring-proof/172-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Everything below already exists in package `cmd` and is directly callable from a new
     test file in the same package. Do not re-derive any of it. -->

From cmd/visual_writer_discipline_test.go (the AST var-block walker — a FuncDecl-only walk
misses every cobra command, because they are declared as `var xCmd = &cobra.Command{...}`):
  func declNameAndBody(decl ast.Decl) (string, ast.Node)

From cmd/command_call_audit_test.go (the fence-aware, placeholder-aware, prose-negation-aware
markdown/YAML invocation parser — do not hand-roll a second one):
  type documentedCall struct { File string; Line int; Raw string; Command string; Args []string; InFence bool }
  func extractDocumentedCalls(t *testing.T, path string) []documentedCall
  func tokenizeShellLike(s string) []string
  func isShellOperator(tok string) bool
  func isPlaceholder(tok string) bool
  func validateCallAgainstCobra(call documentedCall) string
  var auditedCorpora []string   // the FLAG-AUDIT corpus. NOT the caller corpus. See below.

From cmd/command_source_hygiene_test.go:
  func repoRootForCommandSourceTest() (string, error)

From cmd/regression_test.go (the checked-in-golden read/compare shape and its `-update-…`
flag registration idiom — copy the shape, not the exact-match semantics):
  func loadRegressionSnapshot(t *testing.T) *RegressionSnapshot

Cobra:
  rootCmd.Commands() []*cobra.Command   // recursive walk gives the full registered tree
  cmd.Name(), cmd.Use, cmd.Parent(), cmd.Hidden
</interfaces>

<caller_corpora>
<!-- D-01/D-02/D-03/D-05/D-06 translated into an exact file list. This is the load-bearing
     definition of the whole plan; getting it wrong produces a ratchet that reports zero
     orphans on a codebase with hundreds. -->

COUNTS AS A CALLER (exactly these three kinds, "nothing else" — D-01):
  (a) Go code outside the command's own definition file that EXECUTES the binary —
      a string-literal `exec.Command("aether", …)` / `exec.Command(os.Args[0], …)`
      self-invocation naming the subcommand. Expect zero hits today; implement it
      narrowly and honestly anyway (D-01a, RESEARCH Pitfall 5).
  (b) A platform wrapper the runtime actually loads:
        .claude/commands/ant/**       (*.md)
        .opencode/commands/ant/**     (*.md)
        .aether/commands/**           (*.yaml, *.yml — including the `runtime.command` field)
  (c) A shipped hook or script checked into the repo:
        .aether/utils/hooks/**        (*.js)
        scripts/**                    (*.sh)

DOES NOT COUNT (D-02, D-05, D-06):
  CLAUDE.md, AGENTS.md, README.md, docs/**, .aether/docs/** (INCLUDING
  .aether/docs/command-playbooks/**), .aether/skills/**, .aether/references/**,
  .aether/templates/**, .aether/rules/**, .aether/workers.md, colony/**,
  .claude/agents/**, .opencode/agents/**, .codex/**, .planning/**,
  cmd/testdata/command_catalog.json, cmd/testdata/parity_snapshot.json,
  and every *_test.go file.

Note the deliberate asymmetry with `auditedCorpora` in cmd/command_call_audit_test.go:
`.aether/docs/command-playbooks/` and `colony/playbooks/` ARE in the flag-audit corpus
(D-06 — a stale doc naming a non-existent flag still misleads a human) but are NEVER
caller evidence (D-06 — this is exactly how `suggest-analyze` stayed marked complete for a
milestone with its only call site in an unloaded playbook behind `2>/dev/null`).
</caller_corpora>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the reachability scanner, seed the allowlist from its real output</name>

  <read_first>
    - cmd/visual_writer_discipline_test.go — the full file. `declNameAndBody` (lines 61-85) is the var-block AST walker to reuse verbatim; lines 96-160 are the enumeration driver shape (ReadDir, parser.ParseFile, iterate file.Decls, ast.Inspect the body).
    - cmd/command_call_audit_test.go lines 1-200 and 243-305 — `documentedCall`, `extractDocumentedCalls`, `collectDocumentedCalls`, `tokenizeShellLike`, `isShellOperator`, `validateCallAgainstCobra`. You are reusing the extraction half and the token-comparison discipline, not the validation half.
    - cmd/regression_test.go lines 1-120 — the `testdata/*.json` golden read plus its `-update-…` flag registration idiom.
    - cmd/command_source_hygiene_test.go lines 150-190 — `repoRootForCommandSourceTest` and how tests in `cmd/` resolve repo-relative paths.
    - cmd/skills.go and cmd/skill_lifecycle.go — the eight `skill-*` `Use:` declarations. `cmd/skill_lifecycle.go:125` declares `skill-list-lifecycle`, of which `skill-list` is a literal substring. This is the token-boundary trap.
    - .claude/commands/ant/skill-create.md lines 215-245 — real caller evidence for `skill-parse-frontmatter` and `skill-cache-rebuild` inside fenced bash blocks. If your scan reports these two as orphans, your extraction is wrong.
    - .aether/commands/skill-create.yaml — the `runtime.command` field shape, which is how a menu entry names its command (D-03).
    - .planning/phases/172-wiring-proof/172-PATTERNS.md § `cmd/subcommand_reachability_ratchet_test.go` — all three analogs, in full.
    - The `<caller_corpora>` block in this plan's context — the exact include/exclude lists.
  </read_first>

  <files>cmd/subcommand_reachability_ratchet_test.go, cmd/testdata/orphan_allowlist.json, cmd/testdata/orphan_allowlist_baseline.json</files>

  <action>
Create `cmd/subcommand_reachability_ratchet_test.go` in package `cmd`.

**1. Enumerate every registered subcommand and its own definition file.**

Two halves, cross-checked:
  - Registration truth: walk `rootCmd.Commands()` recursively to get the full registered
    tree. Record each command's invocation path as a `[]string` (`host build` is
    `{"host","build"}`). Skip cobra's own generated `help` and `completion` commands —
    they are not repo-owned and have no definition file to point at. Do NOT skip `Hidden`
    commands; a hidden orphan is still an orphan.
  - Own definition file: parse every non-`_test.go` `.go` file in `cmd/` with
    `parser.ParseFile`, walk `file.Decls` through `declNameAndBody`, and `ast.Inspect` for
    `&cobra.Command{...}` composite literals. Extract the `Use:` basic-literal string and
    take its first whitespace-delimited token as the command name — several carry argument
    hints (`skill-match [role] [task]` at cmd/skills.go:264). Map name → declaring file.
    Anonymous inline registrations (`hostCmd.AddCommand(&cobra.Command{...})` at
    cmd/host_cmd.go:31-63) have no `var` declaration; attribute those to the file the
    composite literal appears in, which the same `ast.Inspect` gives you.
  If a command in the cobra tree has no AST-resolved definition file, `t.Errorf` naming it
  rather than skipping it — an unattributable command is a hole in the ratchet, not a pass.

**2. Collect caller evidence from exactly the three permitted kinds.**

Write this as a parameterised function, not an inline loop — Task 2 needs to call it with
a file suppressed. Suggested signature:
  `func collectCallerEvidence(t *testing.T, root string, skipFiles map[string]bool) map[string]bool`
returning the set of command names that have at least one caller.

  - (b) wrappers and menu specs: walk `.claude/commands/ant`, `.opencode/commands/ant`,
    `.aether/commands` and run `extractDocumentedCalls` over every `.md`/`.yaml`/`.yml`.
    Each returned `documentedCall.Command` (plus, for multi-word paths, the following
    non-flag tokens) is caller evidence. Per D-03 a menu entry counts: a `.aether/commands/*.yaml`
    `runtime.command` naming the command is sufficient, and `extractDocumentedCalls` already
    picks it up because the field is a backticked-free plain string containing `aether <name>`
    — verify that it does, and if it does not, read the YAML `runtime.command` value directly
    and tokenize it with `tokenizeShellLike`.
  - (c) hooks and scripts: read `.aether/utils/hooks/*.js` and `scripts/*.sh` and tokenize
    each line with `tokenizeShellLike`, treating a token equal to `aether` (or ending in
    `/aether`, or a shell variable holding the binary path such as `"$BIN"`) followed by a
    registered command name as caller evidence.
  - (a) Go self-invocation: scan non-`_test.go` `.go` files under `cmd/` and `pkg/` for
    `exec.Command` calls whose first argument is the literal `"aether"` or `os.Args[0]`,
    and treat the following string-literal argument as caller evidence — but only when the
    file is not the command's own definition file. Expect zero hits today.

**TOKEN BOUNDARIES ARE LOAD-BEARING.** Never use `strings.Contains` or a substring grep to
decide whether a corpus mentions a command. `skill-list` is a literal substring of
`skill-list-lifecycle`. Tokenize first with `tokenizeShellLike`, then compare exact strings.
A single substring match silently clears a real orphan, which is the one failure mode that
makes this entire test worthless.

**3. Write `TestNoRegisteredSubcommandIsUnreferenced`.**

Compute the orphan set = registered commands with no caller evidence. Subtract the names in
`testdata/orphan_allowlist.json`. Fail if anything remains, listing **each command by name**
on its own line — not "N orphans found". CONTEXT.md is explicit that the failure surface is
for a non-technical operator, so each line should read as
`<command> is registered but nothing calls it (searched: wrappers, menu specs, hooks, scripts)`.

Add two anti-vacuity guards, both `t.Fatal` on failure:
  - the enumeration found at least 100 registered commands (the binary has ~369; a
    silently-empty enumeration would pass forever);
  - the caller-evidence set is non-empty and contains a known-called command resolved at
    runtime rather than hardcoded — e.g. assert that at least one command whose name appears
    in a `.aether/commands/*.yaml` `runtime.command` field is in the evidence set.

**4. Seed both JSON files from the scanner's real output.**

Register a package-level `flag.Bool("update-orphan-allowlist", …)` following
`cmd/regression_test.go`'s `-update-…` idiom. When set, the test writes the computed orphan
set to `testdata/orphan_allowlist.json` as a JSON array of objects with exactly the fields
`name`, `reason`, `owner_phase`, sorted by `name`, with a trailing newline. Assign the tags
programmatically:
  - a name in the set `{skill-index, skill-detect, skill-match, skill-inject, skill-list,
    skill-diff, skill-parse-frontmatter, skill-cache-rebuild}` that the scan actually reports
    as an orphan → `"reason": "skill-lifecycle", "owner_phase": "178"`. Phase 178's success
    criterion measures these reaching zero, so they must be queryable by `owner_phase`.
  - everything else → `"reason": "unreviewed-pre-existing", "owner_phase": "RECLAIM"`,
    matching the parked RECLAIM item in `.planning/REQUIREMENTS.md` § Future Requirements.

The update flag may write ONLY `testdata/orphan_allowlist.json`. It must never write
`testdata/orphan_allowlist_baseline.json` — that would be the runtime bypass D-11 forbids.
Create the baseline once, explicitly, with
`cp cmd/testdata/orphan_allowlist.json cmd/testdata/orphan_allowlist_baseline.json`,
and record that command in the SUMMARY.

**Do not hardcode a count.** D-07 requires the baseline to be whatever the scan honestly
finds. Planning-time estimation puts it near 270 entries, not the 8 named in ROADMAP.md,
because a caller is something that *executes* the command and most of this binary's ~369
subcommands are executed by nothing under that rule. If your number is materially different,
trust your scanner and record the number and the reason for the difference in the SUMMARY —
do not bend the scan to fit a documented figure. A result of exactly 8 skill orphans is a
red flag that `.claude/commands/ant/skill-create.md`'s fenced invocations are not being
credited (RESEARCH Pitfall 1).

**5. Prove the ratchet does not consult the regenerated catalog.**

Add `TestRatchetDoesNotConsultTheRegeneratedCatalog`: read this test file's own source and
assert no non-comment line references `command_catalog` or `parity_snapshot`. D-09 rules the
catalog out as storage because `scripts/classify_commands.py:191` rewrites it in place, and
D-04 rejects exempting commands by its auto-assigned `classification` tier. This turns both
rejections into an assertion instead of a hope.
  </action>

  <acceptance_criteria>
    - `go test ./cmd -run TestNoRegisteredSubcommandIsUnreferenced -count=1 -v` passes and its log line reports the number of registered commands enumerated (≥ 100) and the number of orphans found.
    - `cmd/testdata/orphan_allowlist.json` parses as a JSON array where every element has exactly the keys `name`, `reason`, `owner_phase`, is sorted by `name`, and contains no duplicate `name`.
    - `cmd/testdata/orphan_allowlist_baseline.json` is byte-identical to `cmd/testdata/orphan_allowlist.json` at seeding time: `diff cmd/testdata/orphan_allowlist.json cmd/testdata/orphan_allowlist_baseline.json` produces no output.
    - `skill-parse-frontmatter` and `skill-cache-rebuild` do NOT appear in the allowlist, because `.claude/commands/ant/skill-create.md:215-245` invokes them. If they do appear, the extraction is wrong — fix it rather than accepting the entry.
    - `skill-list` appears in the allowlist and `skill-list-lifecycle` is handled independently of it — proving token-boundary matching. Assert this in the test itself, not only by eye.
    - Every entry whose `name` begins with `skill-` carries `"owner_phase": "178"`.
    - `go test ./cmd -run TestRatchetDoesNotConsultTheRegeneratedCatalog -count=1` passes.
    - `grep -c 'os.Getenv\|t.Skip\|//go:build' cmd/subcommand_reachability_ratchet_test.go` returns 0.
    - `go vet ./cmd` is clean.
  </acceptance_criteria>

  <verify>
    <automated>go test ./cmd -run 'TestNoRegisteredSubcommandIsUnreferenced|TestRatchetDoesNotConsultTheRegeneratedCatalog' -count=1 -v && diff cmd/testdata/orphan_allowlist.json cmd/testdata/orphan_allowlist_baseline.json && python3 -c "import json;d=json.load(open('cmd/testdata/orphan_allowlist.json'));assert all(set(e)=={'name','reason','owner_phase'} for e in d);assert [e['name'] for e in d]==sorted(e['name'] for e in d);assert len({e['name'] for e in d})==len(d);print(len(d),'entries')"</automated>
  </verify>

  <done>The scanner enumerates the real command tree, credits only the three permitted caller kinds, and the allowlist plus its baseline are seeded from its actual output with queryable reason tags.</done>
</task>

<task type="auto">
  <name>Task 2: Make the allowlist shrink-only and prove the ratchet actually fires</name>

  <read_first>
    - cmd/subcommand_reachability_ratchet_test.go — as written by Task 1, including the signature of the parameterised caller-evidence function.
    - cmd/testdata/orphan_allowlist.json and cmd/testdata/orphan_allowlist_baseline.json — as seeded by Task 1.
    - cmd/command_call_audit_test.go lines 411-449 — `TestAuditDetectsPositionalDrift`, the function-local `rootCmd.AddCommand(x)` + `defer rootCmd.RemoveCommand(x)` fixture pattern. Copy this shape exactly; a package-scope fixture would become a permanent real orphan (RESEARCH Pitfall 6).
    - cmd/regression_test.go lines 45-57 — the golden-file read shape.
    - .planning/phases/172-wiring-proof/172-CONTEXT.md § D-10, D-11 — what shrink-only means and what was explicitly rejected.
  </read_first>

  <files>cmd/subcommand_reachability_ratchet_test.go</files>

  <action>
Add four tests to `cmd/subcommand_reachability_ratchet_test.go`.

**`TestOrphanAllowlistOnlyShrinks` (D-10, D-11).**
Read both JSON files. Build the name set from each. Fail, naming each offending command,
for every name present in `testdata/orphan_allowlist.json` and absent from
`testdata/orphan_allowlist_baseline.json`. This is a set-membership diff and nothing else:
  - do NOT compare counts — a one-out-one-in swap must fail, and counting passes it silently;
  - do NOT consult git history — CI shallow-clones and history is rewritable, so the test
    would go red or green for reasons unrelated to the list;
  - provide no reason-field override, no reviewer sign-off path, no environment variable.
    D-11 is "no escape hatch". Changing the baseline is possible, but only as a visible,
    on-the-record edit to a checked-in file that shows up in a diff.
The failure message must read as an instruction to a non-technical operator: name the
command, say that it was added to the tolerated list without being added to the baseline,
and say that the fix is to give it a caller or delete it — not to edit the baseline.

**`TestRatchetDetectsASyntheticOrphan` (success criterion 1's fixture).**
Inside the test function body, register a throwaway command with a name that appears nowhere
in the repo — `rootCmd.AddCommand(&cobra.Command{Use: "ratchet-selftest-orphan", …})`
immediately followed by `defer rootCmd.RemoveCommand(...)`. Recompute the orphan set and
assert `ratchet-selftest-orphan` is in it and that the ratchet's own failure-message
formatter, given that set, produces a string containing the command name. Never register
this command at package scope: it would be a real permanent orphan needing a baseline entry,
and "test fixture" is not debt.

**`TestDeletingACallerMakesTheRatchetNameIt` (success criterion 4's behavioural half,
automated).**
Do not hardcode a victim command. At runtime, find a command whose caller evidence comes
from exactly one file across all permitted corpora. Assert that command is NOT in the orphan
set with all files present. Then call the parameterised caller-evidence function again with
that one file suppressed, recompute, and assert the command IS now in the orphan set and
that the failure message names it. `t.Fatal` if no single-caller command exists at all —
that means the search found nothing to test and the assertion would be vacuous.
This is the "delete a caller, watch it go red" proof made repeatable and hermetic: it runs
on every CI run instead of once on a scratch branch, and it never leaves the branch broken.

**`TestWiringGuardsHaveNoRuntimeEscapeHatch` (D-11, and the threat register's T-172-06).**
Read the source of each guard file — this file plus `cmd/command_call_audit_test.go` and
`cmd/cli_flag_audit_test.go` — and assert that no non-comment line contains `os.Getenv`,
`t.Skip`, `t.SkipNow`, or a `//go:build` constraint. A guard that can be switched off by an
environment variable at runtime is not a guard. Strip `//`-prefixed lines and the contents
of `/* */` blocks before matching, so a comment explaining this rule does not invalidate it.
Also assert that the string `orphan_allowlist_baseline.json` appears in no `os.WriteFile`
or `os.Create` call in this file, so the update flag can never regenerate the baseline.
  </action>

  <acceptance_criteria>
    - `go test ./cmd -run 'TestOrphanAllowlistOnlyShrinks|TestRatchetDetectsASyntheticOrphan|TestDeletingACallerMakesTheRatchetNameIt|TestWiringGuardsHaveNoRuntimeEscapeHatch' -count=1 -v` passes with all four tests shown as RUN and PASS (none skipped).
    - Behavioural proof of the shrink guard, performed once by hand and recorded in the SUMMARY: append `{"name":"ratchet-proof-entry","reason":"proof","owner_phase":"172"}` to `cmd/testdata/orphan_allowlist.json`, run `go test ./cmd -run TestOrphanAllowlistOnlyShrinks -count=1`, confirm it FAILS and the output contains the string `ratchet-proof-entry`, then revert the file and confirm it passes. Paste both outputs into the SUMMARY.
    - Behavioural proof of the ratchet itself, recorded in the SUMMARY: the verbose log of `TestDeletingACallerMakesTheRatchetNameIt` names both the command it chose and the caller file it suppressed.
    - `TestOrphanAllowlistOnlyShrinks` fails on a one-out-one-in swap: remove any one entry from the live list and add `{"name":"ratchet-swap-proof",...}`, confirm FAIL, revert. Record in the SUMMARY. (This is the check a count comparison would pass.)
    - The word `count` does not appear in `TestOrphanAllowlistOnlyShrinks` as the basis of its comparison — the assertion is set membership.
    - `go test ./cmd/... -count=1 -timeout 900s` passes.
  </acceptance_criteria>

  <verify>
    <automated>go test ./cmd -run 'TestNoRegisteredSubcommandIsUnreferenced|TestOrphanAllowlist|TestRatchet|TestDeletingACaller|TestWiringGuards' -count=1 -v</automated>
  </verify>

  <done>The allowlist can only shrink, proven by a red-then-green transcript; the ratchet is proven to name a synthetic orphan and to name a real command the moment its only caller disappears; no guard in this phase can be switched off at runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor / PR → the CI gate | The only boundary that matters here. The threat model for this plan is integrity of the gate itself, not application security: it adds test-time static analysis over files already checked into this repo by its own maintainers. No network, auth, session, crypto, or untrusted-input surface exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-172-05 | Tampering | `cmd/testdata/orphan_allowlist.json` | mitigate | A silently widened allowlist defeats the entire ratchet. Mitigated by `TestOrphanAllowlistOnlyShrinks` diffing live against a committed baseline by set membership (D-10), with no override field and no sign-off path (D-11), and by forbidding the `-update-orphan-allowlist` flag from ever writing the baseline file — asserted in `TestWiringGuardsHaveNoRuntimeEscapeHatch`. |
| T-172-06 | Tampering / Repudiation | the guard tests themselves | mitigate | An `os.Getenv("SKIP_RATCHET")`-style bypass would reintroduce silent orphan accumulation with no record. Mitigated by `TestWiringGuardsHaveNoRuntimeEscapeHatch` asserting no `os.Getenv`, `t.Skip`, or build constraint exists in any guard file. No existing audit test in this repo has such a hatch; this keeps it that way. |
| T-172-07 | Spoofing (of caller evidence) | `collectCallerEvidence` | mitigate | Substring rather than token matching lets `skill-list-lifecycle` clear the unrelated `skill-list`, and a doc mention clear a real orphan. Both are the documented way this class of gate has failed before. Mitigated by reusing `tokenizeShellLike` with exact token comparison, by the fixed include/exclude corpora in `<caller_corpora>`, and by the acceptance criterion asserting `skill-list` and `skill-list-lifecycle` are handled independently. |
| T-172-08 | Denial of service (of the gate, by vacuity) | `TestNoRegisteredSubcommandIsUnreferenced` | mitigate | A scan that silently enumerates nothing passes forever and proves nothing — the exact failure the existing `command_call_audit_test.go` guards against with its "extracted zero documented calls" fatal. Mitigated by the ≥100-commands and non-empty-evidence `t.Fatal` guards, and by `TestRatchetDetectsASyntheticOrphan`. |
| T-172-09 | Information disclosure | — | accept | The allowlist and baseline contain only subcommand names already visible in `aether --help`. Nothing sensitive is written. No mitigation needed. |
</threat_model>

<verification>
1. `go vet ./cmd` — clean.
2. `go test ./cmd -run 'TestNoRegisteredSubcommandIsUnreferenced|TestOrphanAllowlist|TestRatchet|TestDeletingACaller|TestWiringGuards' -count=1 -v` — all pass, none skipped.
3. `diff cmd/testdata/orphan_allowlist.json cmd/testdata/orphan_allowlist_baseline.json` — empty.
4. `go test ./cmd/... -count=1 -timeout 900s` — no regression.
5. The three red-then-green transcripts required by the acceptance criteria are present in the SUMMARY.
</verification>

<success_criteria>
- Phase success criterion 1 is met in full: the ratchet exists, a fixture proves it fails naming a caller-less command, and the allowlist may only shrink against a committed baseline.
- D-01 through D-05 and D-07 through D-11 are each implemented and, where they are a rejection, asserted rather than merely respected.
- The seeded orphan count is the scanner's honest output, recorded in the SUMMARY alongside the reason it differs from the 8 named in ROADMAP.md.
</success_criteria>

<output>
After completion, create `.planning/phases/172-wiring-proof/172-02-SUMMARY.md`.
It MUST record: the seeded orphan count, how many carry `owner_phase: 178`, the exact
`cp` command used to create the baseline, and the three red-then-green transcripts.
Phase 178's success criterion reads this number, so state it plainly.
</output>
