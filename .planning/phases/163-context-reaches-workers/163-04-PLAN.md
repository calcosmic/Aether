---
phase: 163-context-reaches-workers
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/helpers.go
  - cmd/survey_staleness.go
  - cmd/survey_staleness_test.go
  - cmd/codex_build_test.go
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CONTEXT-01, CONTEXT-04]
must_haves:
  truths:
    - "CONTEXT-01: Territory survey findings are present in a build worker's actual prompt, and a named test fails if they stop arriving"
    - "CONTEXT-04: Phase research findings are present in the same prompt, pinned by the same test"
    - "D-10: A worker and the inspector are told how old the codebase map is — 'mapped N commits ago' — and warned loudly past a threshold, so nobody grounds on fiction unknowingly"
    - "D-10: No auto-refresh — the map refreshes only when the user runs colonize"
    - "D-02: The survey stays a pointer list and codegraph stays the compact graph form; no new full-text dump and no new compression format is introduced"
    - "D-07: CONTEXT-01 and CONTEXT-04 are reworded to name outcomes instead of the deleted playbook loader and survey-load, with the mechanism change and its reason recorded"
    - "D-08: CONTEXT-09's staged benchmark is recorded as descoped in REQUIREMENTS.md, with inspector-plus-tests named as the replacement evidence"
  artifacts:
    - path: "cmd/survey_staleness.go"
      provides: "surveyStalenessNotice — commits-since-survey age line and loud stale warning"
      contains: "surveyStaleCommitThreshold"
    - path: "cmd/helpers.go"
      provides: "resolveSurveySection emitting the staleness notice with the pointer list"
    - path: "cmd/codex_build_test.go"
      provides: "TestBuildWorkerBriefIncludesSurveyAndResearch — the pin CONTEXT-01/04 lacked"
    - path: ".planning/REQUIREMENTS.md"
      provides: "reworded CONTEXT-01/04, corrected CONTEXT-08 budget list, descoped CONTEXT-09, reason recorded"
  key_links:
    - from: "cmd/helpers.go resolveSurveySection"
      to: "surveyStalenessNotice"
      via: "prepended notice line inside the Territory Survey section"
      pattern: "surveyStalenessNotice\\(\\)"
    - from: "cmd/survey_staleness.go"
      to: "state.TerritorySurveyed"
      via: "git rev-list --count --since=<timestamp> HEAD"
      pattern: "rev-list"
---

<objective>
Two of this phase's four "disconnected" items were quietly reconnected on
2026-07-26 (commits `281dd34a`, `a2c8288e`): survey findings and phase research
already reach build workers through `renderCodexBuildWorkerBrief`. Neither has a
test that fails when they stop, and neither requirement text says what it now
means — CONTEXT-01 still names a deleted playbook loader, CONTEXT-04 still names
the deleted `survey-load`.

Purpose: grounding is the payload (D-01), so the grounding that already works
should be pinned rather than re-argued, honest about its age (D-10), and
described by requirement text that a future reader can act on (D-07).

Output: a commits-since-colonize staleness notice, the presence test CONTEXT-01
and CONTEXT-04 never had, and a REQUIREMENTS.md that says what it means.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-context-reaches-workers/163-RESEARCH.md
@.planning/phases/163-context-reaches-workers/163-PATTERNS.md
@.planning/phases/163-context-reaches-workers/163-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. -->

cmd/helpers.go:222-259
  func resolveSurveySection() string
  Returns "### Territory Survey" plus a repo-relative pointer list of
  .aether/data/survey/*.md|json. Returns "" when store is nil or the dir is empty.
  This pointer-list form IS the compact delivery D-02 asks for — do not replace it.

cmd/phase_research.go:146-170
  func resolvePhaseResearchSection() string   (budget 3500 chars)

cmd/codex_build.go:2321-2456
  func renderCodexBuildWorkerBrief(root string, phase colony.Phase,
      dispatch codexBuildDispatch, startedAt time.Time) string
  Already calls resolveSurveySection and resolvePhaseResearchSection.
  This plan does NOT modify cmd/codex_build.go — plan 01 owns it this wave.

pkg/colony/colony.go:323
  TerritorySurveyed *string `json:"territory_surveyed,omitempty"`
  Set at cmd/codex_colonize.go:1041 to an RFC3339 timestamp. This is the only
  colonize-time baseline that exists; there is no stored baseline commit SHA.

cmd/session_cmds.go:488-496   getGitHEAD() — the existing git-invocation idiom
cmd/init_research.go:201      an existing `git rev-list --count HEAD` call site

NO existing function computes "commits since a baseline" (PATTERNS.md § No
Analog Found). A small new helper is legitimate here; do not force-fit
sessionVerifyFresh's binary fresh/stale model onto a count.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Say how old the map is, loudly when it is old</name>
  <files>cmd/survey_staleness.go, cmd/survey_staleness_test.go, cmd/helpers.go</files>
  <read_first>
    - cmd/helpers.go:222-259 (resolveSurveySection — the section this notice joins)
    - cmd/session_cmds.go:485-500 (getGitHEAD — the git invocation idiom to follow: exec.Command with separate argv, never a shell string)
    - cmd/init_research.go:195-210 (the existing rev-list call site)
    - cmd/session_flow_cmds.go:36-73 (sessionVerifyFresh — the nearest staleness shape, deliberately NOT the model here)
    - pkg/colony/colony.go:318-335 (TerritorySurveyed)
    - cmd/codex_colonize.go:1035-1045 (where TerritorySurveyed is set)
  </read_first>
  <behavior>
    - Test 1: state with no TerritorySurveyed produces a notice saying the territory has never been surveyed and naming the command that fixes it
    - Test 2: state surveyed at a timestamp with N commits since produces a notice containing the count
    - Test 3: N at or above the threshold produces the loud stale wording; N below produces the quiet age wording
    - Test 4: a TerritorySurveyed value that is not valid RFC3339 produces an empty notice and no git invocation
    - Test 5: outside a git repo (or git failing) produces the quiet age wording without the count, never an error or a panic
  </behavior>
  <action>
    Create cmd/survey_staleness.go with `surveyStalenessNotice() string`, loading
    active colony state internally and returning "" on any failure — the same
    defensive posture `resolveSurveySection` already uses. A staleness helper
    that can break a build is worse than one that stays quiet.

    Compute the count with `git rev-list --count --since=<timestamp> HEAD`,
    passing the timestamp as its own argv element via exec.Command — never
    interpolated into a shell string. Validate the stored value parses as
    RFC3339 BEFORE calling git and return "" if it does not; the value comes from
    a state file a worker could have influenced, and this is the only place in
    this phase where state text reaches a subprocess argument (T-163-10).

    Add a named `surveyStaleCommitThreshold` constant set to 25 with a comment
    stating it is a judgement call, not a measurement, so a later tuner knows it
    may move. Below the threshold the notice is a quiet age line; at or above it
    the notice is loud and names `/ant-colonize` as the manual refresh. Never
    refresh automatically — D-10 rejects it and the user's no-silent-automation
    stance is what descoped Phase 161.

    Wire the notice into `resolveSurveySection` so it renders inside the existing
    Territory Survey section, immediately under the heading and above the pointer
    list. Keeping it inside that section means the brief inspector, the hosted
    path and the wrapper path all get it with no further wiring.

    Do not add a new state field and do not backfill one. The existing
    `TerritorySurveyed` timestamp is sufficient and works for colonies created
    before this change — the milestone already has one lesson (TYPED-01/03) about
    requiring a field before backfilling it.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run TestSurveyStaleness -count=1` passes with all five behaviors as subtests
    - `grep -n 'exec.Command' cmd/survey_staleness.go` shows the timestamp passed as a separate argument; no `sh -c`, no fmt.Sprintf into a command string
    - `grep -n 'surveyStaleCommitThreshold' cmd/survey_staleness.go` shows a named constant, not an inline 25
    - Behavior 4 asserts no git invocation occurs for a malformed timestamp
    - `go build ./cmd/aether && go vet ./cmd/...` exit 0
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestSurveyStaleness|TestResolveSurveySection' -count=1</automated>
  </verify>
  <done>Every worker brief carrying a survey section also carries how old that survey is, loudly past 25 commits, and never refreshes itself.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin survey and research presence with the test they never had</name>
  <files>cmd/codex_build_test.go</files>
  <read_first>
    - cmd/codex_build_test.go:3111-3260 (TestBuildWorkerBriefOmitsHeartbeat, OmitsPlaybooks, IsMostlyTask, IncludesCodegraphContext — the fixture setup and assertion voice to follow)
    - cmd/helpers.go:222-259 and cmd/phase_research.go:146-170 (the two resolvers under test)
    - .planning/phases/163-context-reaches-workers/163-RESEARCH.md § Pitfall 5 (do not re-prove what is already covered; spend the budget where coverage is zero)
  </read_first>
  <behavior>
    - Test 1: a colony with survey artifacts on disk produces a brief containing the Territory Survey pointer list with real repo-relative paths
    - Test 2: a colony with a phase research artifact produces a brief containing the Phase Research section
    - Test 3: a colony with a survey produces a brief containing the staleness notice from task 1
    - Test 4: with neither artifact present, neither section appears and no empty heading is emitted
  </behavior>
  <action>
    Before writing anything, re-run the coverage check research Pitfall 5
    demands: `grep -n 'func Test' cmd/codex_build_test.go` and search for any
    existing assertion on `resolveSurveySection` or
    `resolvePhaseResearchSection`. Planning verified the gap is real (the file
    has OmitsHeartbeat, OmitsPlaybooks, IsMostlyTask and IncludesCodegraphContext
    and no survey or research presence test) — confirm it still holds and record
    the grep result in the SUMMARY. If coverage has appeared since, extend it
    rather than duplicating it.

    Add `TestBuildWorkerBriefIncludesSurveyAndResearch` to cmd/codex_build_test.go
    covering the four behaviors, building the fixture from real files on disk in
    a temp colony rather than stubbing the resolvers — the requirement is that
    content reaches the prompt, and a stubbed resolver proves only that a stub
    was called.

    Assert on the delivered pointer paths (the `.aether/data/survey/` prefix) and
    on research content, not merely on the heading strings. A test that checks
    for a heading passes when the heading survives and the content vanishes,
    which is the exact failure mode CLAUDE.md's Definition of Done calls out.

    Deliberate-regression proof, recorded in the SUMMARY: remove the
    `resolveSurveySection` call from the brief renderer, observe the failure,
    quote it, revert. Note that cmd/codex_build.go is owned by plan 01 this
    wave — perform the proof on a scratch copy or revert immediately and confirm
    `git diff cmd/codex_build.go` is empty before finishing.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run TestBuildWorkerBriefIncludesSurveyAndResearch -count=1` passes
    - `git diff --stat cmd/codex_build.go` is empty at task end (plan 01 owns that file this wave)
    - The test asserts on delivered paths and content, not only on section headings
    - The SUMMARY records the Pitfall 5 grep result and quotes the observed regression failure
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestBuildWorkerBrief' -count=1</automated>
  </verify>
  <done>A command exists that fails when survey findings or phase research stop reaching a worker's prompt, and its failure has been observed.</done>
</task>

<task type="auto">
  <name>Task 3: Reword CONTEXT-01/04, correct CONTEXT-08, descope CONTEXT-09 — with reasons recorded</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md:11-27 (the "Corrections carried forward" table — the established place this project records a refuted premise)
    - .planning/REQUIREMENTS.md:65-77 (the CONTEXT block being edited)
    - .planning/REQUIREMENTS.md:212-250 (the traceability table — CONTEXT rows stay as they are)
    - .planning/phases/163-context-reaches-workers/163-CONTEXT.md § D-07, D-08
    - .planning/phases/163-context-reaches-workers/163-RESEARCH.md § Reconciliation Corrections (the evidence for each rewording)
  </read_first>
  <action>
    Rewrite four requirement lines in .planning/REQUIREMENTS.md, preserving the
    IDs, the checkbox form and the surrounding formatting. Each rewritten line
    keeps a short parenthetical recording that it was reworded on 2026-07-29 per
    Phase 163 D-07/D-08 and why — an unexplained edit to a requirement is
    indistinguishable from scope reduction.

    CONTEXT-01 — before:
    "`build-context.md` is added to `codexBuildPlaybooks()` (`cmd/codex_build.go:894`) — it is the one build playbook Go omits"
    after: territory survey findings are demonstrably present in a build
    worker's actual prompt on both dispatch paths, delivered by the runtime brief
    renderer. Note that the original named `build-context.md` and
    `codexBuildPlaybooks()`, both deleted in Phase 160, and that the playbook
    corpus stays dead (`TestSurveyLoadAbsentAndUncalled`); the outcome is now
    delivered by `resolveSurveySection()` inside `renderCodexBuildWorkerBrief`.

    CONTEXT-04 — before:
    "`survey-load` output and phase research reach worker prompts"
    after: phase research findings are demonstrably present in a build worker's
    actual prompt via `resolvePhaseResearchSection()`, and a worker is told when
    the survey map is stale rather than grounding on fiction. Note `survey-load`
    was deleted in Phase 160 and is pinned absent.

    CONTEXT-08 — before, the budget list reads
    "colony-prime 8K, skills 8K, playbook injection 7K".
    after: correct it to the budgets that actually exist — colony-prime
    8000/4000, skills ~8000, phase research 3500, codegraph 2200 — and record
    that playbook injection was removed entirely (the code comment near
    `renderCodexBuildWorkerBrief` measured it at 5,733 of 7,485 characters before
    removal). The requirement's substance is unchanged; only the stale figure goes.

    CONTEXT-09 — before:
    "A before/after comparison on an inexpensive model is recorded for the same task with and without the restored context"
    after: mark the staged benchmark descoped per D-08 (the user's decision:
    testing happens through real repos in real use), and state the replacement
    evidence — the brief inspector plus the automated presence and budget tests
    this phase ships. Real-world validation is explicitly post-phase, not
    abandoned.

    Add one row to the "Corrections carried forward" table recording that the
    CONTEXT block's premise ("four things are genuinely disconnected") was half
    stale: survey and phase research were reconnected by commits `281dd34a` and
    `a2c8288e` on 2026-07-26, before Phase 160 executed. This is the table's
    exact purpose and keeps the correction visible to every later phase, not just
    this one.

    Do not touch the traceability table's CONTEXT rows and do not renumber or
    remove any requirement. Rewording is permitted by D-07; deletion is not.
  </action>
  <acceptance_criteria>
    - `grep -c 'CONTEXT-0' .planning/REQUIREMENTS.md` returns the same count as before the edit (no requirement added or removed)
    - `grep -n 'codexBuildPlaybooks\|survey-load' .planning/REQUIREMENTS.md` returns matches only inside the recorded-reason parentheticals, never as the requirement's own success condition
    - `grep -n 'playbook injection 7K' .planning/REQUIREMENTS.md` returns nothing
    - Each of the four reworded lines contains the date 2026-07-29 and the decision ID that authorised it
    - The "Corrections carried forward" table has exactly one new row citing commits 281dd34a and a2c8288e
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'CONTEXT-0' .planning/REQUIREMENTS.md && ! grep -q 'playbook injection 7K' .planning/REQUIREMENTS.md && grep -q '281dd34a' .planning/REQUIREMENTS.md && echo REWORDING_OK</automated>
  </verify>
  <done>CONTEXT-01, -04, -08 and -09 describe outcomes that are achievable against the current tree, each carrying the reason it changed and the decision that authorised it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Colony state file → subprocess argument | `TerritorySurveyed` is read from `COLONY_STATE.json` and passed to `git` |
| Survey artifacts on disk → worker prompt | Filenames from `.aether/data/survey/` become pointer text in the brief |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-10 | Tampering / Elevation of Privilege | `surveyStalenessNotice` passing a state-derived timestamp to `git` | mitigate | Value must parse as RFC3339 before any git call; passed as a discrete `exec.Command` argument, never interpolated into a shell string; no `sh -c` anywhere in the helper. Asserted by acceptance criteria and by the malformed-timestamp subtest. |
| T-163-11 | Information Disclosure | survey pointer list naming files a worker then reads | accept | Pre-existing behaviour of `resolveSurveySection`; the pointer list is repo-relative and confined to `.aether/data/survey/`, which the colony itself generated. |
| T-163-12 | Repudiation | requirement text reworded in a way that quietly reduces scope | mitigate | Every reworded line carries the date and authorising decision ID; a "Corrections carried forward" row records the premise change; acceptance criteria assert the requirement count is unchanged. |
</threat_model>

<verification>
- `go test ./cmd -run 'TestSurveyStaleness|TestBuildWorkerBrief' -count=1` passes
- `go test ./cmd/... -count=1` passes
- `go build ./cmd/aether && go vet ./...` exit 0
- `git diff --stat cmd/codex_build.go` empty (plan 01 owns it this wave)
- REQUIREMENTS.md rewording verification command exits 0
</verification>

<success_criteria>
1. A worker brief carrying a survey section states how old that map is, and says so loudly past 25 commits
2. No automatic refresh path exists — colonize remains user-invoked
3. A named test fails when survey findings or phase research stop reaching the brief
4. CONTEXT-01/04/08/09 read as achievable outcomes against the current tree, each with its rewording reason and authorising decision recorded
5. No requirement was deleted or renumbered
</success_criteria>

<output>
After completion, create `.planning/phases/163-context-reaches-workers/163-04-SUMMARY.md`
</output>
