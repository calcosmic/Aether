---
phase: 05-learning-pipeline-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/learning-pipeline-e2e.test.js
autonomous: true
requirements:
  - LRNG-01
  - LRNG-03

must_haves:
  truths:
    - "A realistic observation (based on actual Aether development patterns) flows through memory-capture to create an observation record"
    - "Calling memory-capture twice with identical realistic content triggers auto-promotion at pattern threshold=2"
    - "After auto-promotion, QUEEN.md contains the promoted content in the correct section AND COLONY_STATE.json has an instinct with matching action, domain, and source"
    - "The full pipeline works across multiple wisdom types (pattern, failure, philosophy) with their distinct thresholds"
  artifacts:
    - path: "tests/integration/learning-pipeline-e2e.test.js"
      provides: "End-to-end pipeline validation tests with non-synthetic data"
      min_lines: 150
  key_links:
    - from: "tests/integration/learning-pipeline-e2e.test.js"
      to: ".aether/aether-utils.sh"
      via: "runAetherUtil helper calling memory-capture subcommand"
      pattern: "runAetherUtil.*memory-capture"
    - from: "memory-capture"
      to: "COLONY_STATE.json + QUEEN.md"
      via: "learning-promote-auto -> queen-promote + instinct-create"
      pattern: "auto_promoted.*true"
---

<objective>
Write end-to-end integration tests that validate the complete learning pipeline (memory-capture -> learning-observe -> learning-promote-auto -> queen-promote + instinct-create) using realistic, non-synthetic data derived from actual Aether development patterns.

Purpose: Prove LRNG-01 (pipeline works with real data) and LRNG-03 (integration test covers full pipeline path) are satisfied.
Output: `tests/integration/learning-pipeline-e2e.test.js` with 6-8 passing tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-learning-pipeline-validation/05-RESEARCH.md

@tests/integration/wisdom-promotion.test.js
@tests/integration/learning-pipeline.test.js
@tests/integration/instinct-pipeline.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create end-to-end pipeline tests with realistic content</name>
  <files>tests/integration/learning-pipeline-e2e.test.js</files>
  <action>
Create a new test file `tests/integration/learning-pipeline-e2e.test.js` that validates the full memory-capture -> instinct-create pipeline using non-synthetic data.

Use the established test patterns from wisdom-promotion.test.js:
- Copy the helper functions: createTempDir, cleanupTempDir, runAetherUtil, parseLastJson, setupTestColony (the version from wisdom-promotion.test.js that creates COLONY_STATE.json with memory.instincts array AND pheromones.json -- both are required).
- All tests use test.serial and try/finally for temp dir cleanup.

**CRITICAL: Use parseLastJson() for ALL memory-capture and learning-promote-auto calls.** These subcommands emit multiple JSON lines (instinct-create outputs its own JSON before the final result). Using JSON.parse() directly will fail.

**CRITICAL: Use realistic content, NOT synthetic test strings.** The requirement (LRNG-01) demands "real (non-test) data." Use patterns discovered during actual Aether development:

Realistic content examples:
- Pattern: `'Use explicit jq if/elif chains instead of the // operator when checking fields that can legitimately be false'` (from Phase 3 jq boolean fix)
- Failure: `'Validate JSON structure before atomic_write to prevent corrupted state files'` (recurring build issue)
- Redirect: `'Never commit test artifacts to colony state files that ship to users'` (from Phase 1 data purge)
- Philosophy: `'Clean state data before integrating new features to avoid false positives from stale artifacts'` (multi-phase principle)

Write these tests (6-8 total):

1. **"memory-capture records realistic observation on first call without promoting"** -- Call memory-capture once with the pattern content above, event_type='learning', wisdom_type='pattern', source='worker:builder'. Verify: ok=true, auto_promoted=false, observation_count=1. Verify learning-observations.json has 1 observation with the content.

2. **"memory-capture auto-promotes realistic pattern after second observation (threshold=2)"** -- Call memory-capture twice with IDENTICAL pattern content. First call: verify auto_promoted=false, observation_count=1. Second call: verify auto_promoted=true. Then verify BOTH artifacts:
   - QUEEN.md contains the content in the Patterns section
   - COLONY_STATE.json memory.instincts has an instinct with action matching the content, domain='pattern', source='promoted_from_learning', confidence >= 0.7

3. **"memory-capture auto-promotes realistic failure content after second observation (threshold=2)"** -- Same pattern as test 2 but with failure content and wisdom_type='failure'. Failure maps to Patterns section in QUEEN.md. Verify: QUEEN.md has content in Patterns section, instinct domain is 'failure', instinct created.

4. **"memory-capture auto-promotes philosophy only after third observation (threshold=3)"** -- Call memory-capture THREE times with the philosophy content (wisdom_type='philosophy'). First two calls: auto_promoted=false. Third call: auto_promoted=true. Verify both QUEEN.md (Philosophies section) and COLONY_STATE.json instinct created.

5. **"full pipeline: memory-capture -> instinct in colony-prime prompt_section"** -- This is the LRNG-03 test. Call memory-capture twice with pattern content to trigger promotion. Then call colony-prime. Verify:
   - colony-prime returns ok=true
   - prompt_section includes 'INSTINCTS' header
   - prompt_section includes the content text
   - prompt_section includes domain grouping (e.g., 'Pattern:')
   - QUEEN.md has the content
   - COLONY_STATE.json has the instinct

6. **"idempotent: third memory-capture with same content does not create duplicate instinct"** -- Call memory-capture three times with the same pattern content. Verify: second call promotes (auto_promoted=true), third call does NOT create a second instinct. COLONY_STATE.json has exactly 1 instinct with matching action. The instinct confidence should be boosted (>= 0.8) from the third call.

7. **"confidence formula: promoted instinct has recurrence-calibrated confidence"** -- Call memory-capture twice with pattern content. Read the instinct from COLONY_STATE.json. Verify confidence is approximately 0.75 (formula: min(0.7 + (2-1)*0.05, 0.9) = 0.75). Use approximate comparison: Math.abs(confidence - 0.75) < 0.01.

Do NOT include agent definition tests here -- those belong in plan 05-02.
  </action>
  <verify>
Run `npm test -- tests/integration/learning-pipeline-e2e.test.js` and verify all tests pass.
Run `npm test` to verify no existing tests are broken.
  </verify>
  <done>
All 6-7 tests pass. Each test uses realistic (non-synthetic) content derived from actual Aether development patterns. Tests verify both QUEEN.md and COLONY_STATE.json artifacts after promotion. The full pipeline test (LRNG-03) covers memory-capture through to colony-prime prompt_section output.
  </done>
</task>

</tasks>

<verification>
- `npm test -- tests/integration/learning-pipeline-e2e.test.js` passes all tests
- `npm test` passes (no regressions in existing 490+ tests)
- Test content uses realistic strings, not synthetic "test observation" strings
- Tests verify BOTH QUEEN.md AND COLONY_STATE.json instincts after promotion
- At least one test verifies the full chain through colony-prime prompt_section
</verification>

<success_criteria>
- All new tests pass with non-synthetic, realistic observation content
- LRNG-01 satisfied: pipeline validated end-to-end with real data
- LRNG-03 satisfied: integration test covers memory-capture -> learning-observe -> threshold met -> learning-promote-auto -> instinct-create -> colony-prime
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/05-learning-pipeline-validation/05-01-SUMMARY.md`
</output>
