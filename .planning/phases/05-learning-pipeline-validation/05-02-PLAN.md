---
phase: 05-learning-pipeline-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/learning-pipeline-e2e.test.js
autonomous: true
requirements:
  - LRNG-02

must_haves:
  truths:
    - "Promoted instincts appear in colony-prime prompt_section with correct formatting (domain grouping, confidence display, trigger/action text)"
    - "Agent definitions for builder, watcher, and scout contain pheromone_protocol sections that instruct workers to act on instincts"
    - "Colony-prime assembles BOTH QUEEN wisdom AND instincts into a single prompt_section"
  artifacts:
    - path: "tests/integration/learning-pipeline-e2e.test.js"
      provides: "Instinct influence and colony-prime verification tests (appended to plan 01 file)"
      min_lines: 80
  key_links:
    - from: "colony-prime"
      to: "pheromone-prime"
      via: "Internal subcommand call for instinct formatting"
      pattern: "INSTINCTS.*Learned Behaviors"
    - from: ".claude/agents/ant/aether-builder.md"
      to: "prompt_section"
      via: "pheromone_protocol section referencing instincts"
      pattern: "pheromone_protocol"
---

<objective>
Write integration tests that verify promoted instincts reach worker prompts: instincts appear correctly formatted in colony-prime prompt_section, and agent definitions contain pheromone_protocol sections that establish the influence mechanism.

Purpose: Prove LRNG-02 (promoted instincts appear in colony-prime output and influence worker prompts) is satisfied.
Output: Additional tests appended to `tests/integration/learning-pipeline-e2e.test.js` (created in plan 05-01), covering 4-5 tests for instinct visibility and agent protocol verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-learning-pipeline-validation/05-RESEARCH.md
@.planning/phases/05-learning-pipeline-validation/05-01-SUMMARY.md

@tests/integration/instinct-pipeline.test.js
@.claude/agents/ant/aether-builder.md
@.claude/agents/ant/aether-watcher.md
@.claude/agents/ant/aether-scout.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add instinct influence and colony-prime verification tests</name>
  <files>tests/integration/learning-pipeline-e2e.test.js</files>
  <action>
Append tests to the existing `tests/integration/learning-pipeline-e2e.test.js` file created by plan 05-01. These tests verify LRNG-02: that promoted instincts appear in colony-prime output and that agent definitions establish the influence mechanism.

The helpers (createTempDir, cleanupTempDir, runAetherUtil, parseLastJson, setupTestColony) are already defined in the file from plan 05-01. Do NOT duplicate them.

Add a comment section separator:
```javascript
// =============================================================================
// LRNG-02: Instinct influence on worker prompts
// =============================================================================
```

Write these tests (4-5 total):

1. **"promoted instinct appears in colony-prime prompt_section with domain grouping"** -- Use memory-capture twice with realistic pattern content to trigger promotion. Then call colony-prime (not --compact). Verify:
   - prompt_section includes 'INSTINCTS (Learned Behaviors)' header
   - prompt_section includes 'Pattern:' domain grouping (capitalize first letter of domain)
   - prompt_section includes the instinct action text
   - prompt_section includes confidence display (e.g., '[0.75]' or similar)

2. **"colony-prime includes BOTH QUEEN wisdom AND instincts in prompt_section"** -- Use memory-capture twice with pattern content to trigger promotion (this writes to both QUEEN.md and instincts). Call colony-prime. Verify prompt_section includes BOTH:
   - 'QUEEN WISDOM' header with the promoted content in patterns
   - 'INSTINCTS (Learned Behaviors)' header with the instinct

3. **"colony-prime --compact caps instincts at 3"** -- Pre-seed COLONY_STATE.json with 5 instincts (use setupTestColony opts.instincts with 5 instinct objects at varying confidence: 0.9, 0.85, 0.8, 0.75, 0.7). Call colony-prime --compact. Verify: prompt_section includes 'INSTINCTS' header, and count the instinct entries -- should be at most 3 (the 3 highest confidence). The 0.7 and 0.75 confidence instincts should NOT appear.

4. **"agent definitions contain pheromone_protocol for instinct influence"** -- Read the three agent files directly from the filesystem (not through aether-utils):
   - `.claude/agents/ant/aether-builder.md`
   - `.claude/agents/ant/aether-watcher.md`
   - `.claude/agents/ant/aether-scout.md`
   For each, verify:
   - File contains `<pheromone_protocol>` tag
   - File references instincts or learned behaviors (check for 'INSTINCTS' or 'instinct' or 'Learned Behaviors' case-insensitively)

5. **"instinct auto-generated trigger format matches expected pattern"** -- Use memory-capture twice with pattern content to trigger promotion. Read the instinct from COLONY_STATE.json. Verify the trigger field matches the auto-generated format: `'When working on pattern patterns'` (the exact format from learning-promote-auto lines 5393-5399).
  </action>
  <verify>
Run `npm test -- tests/integration/learning-pipeline-e2e.test.js` and verify all tests pass (both plan 01 and plan 02 tests).
Run `npm test` to verify no regressions.
  </verify>
  <done>
All tests pass (plan 01 + plan 02 combined in single file). Colony-prime prompt_section correctly displays promoted instincts with domain grouping, confidence, and trigger/action text. Agent definitions verified to contain pheromone_protocol sections. LRNG-02 is satisfied: promoted instincts appear in colony-prime output and the influence mechanism is in place via agent pheromone_protocol.
  </done>
</task>

</tasks>

<verification>
- `npm test -- tests/integration/learning-pipeline-e2e.test.js` passes all tests (plan 01 + plan 02)
- `npm test` passes (no regressions)
- Colony-prime prompt_section verified to contain BOTH QUEEN wisdom AND instinct sections
- Agent definition files verified to contain pheromone_protocol sections
- Instinct format (trigger, action, domain, confidence) verified in prompt_section output
</verification>

<success_criteria>
- LRNG-02 satisfied: promoted instincts appear in colony-prime output (prompt_section includes INSTINCTS header, domain grouping, confidence, action text)
- Agent definitions verified: builder, watcher, and scout contain pheromone_protocol sections referencing instinct/learned behavior handling
- All tests pass alongside plan 01 tests without conflicts
</success_criteria>

<output>
After completion, create `.planning/phases/05-learning-pipeline-validation/05-02-SUMMARY.md`
</output>
