---
phase: 63-lifecycle-ceremony-status-entomb-resume
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/entomb_cmd.go
  - cmd/registry.go
  - cmd/entomb_cmd_test.go
autonomous: true
requirements:
  - CERE-07

must_haves:
  truths:
    - "Running aether entomb extracts near-miss instincts (confidence 0.5-0.8) and logs them to the chamber manifest"
    - "Entomb outputs a suggestion line for hive promotion of near-miss instincts"
    - "Entomb cleans midden entries older than 30 days and expired pheromones (strength 0)"
    - "Registry entry is updated with final stats when colony is marked inactive at entomb"
  artifacts:
    - path: "cmd/entomb_cmd.go"
      provides: "Near-miss extraction, temp sweep, registry stats update"
      contains: "extractNearMissInstincts"
    - path: "cmd/registry.go"
      provides: "Final stats fields on registryEntry"
      contains: "FinalStats"
  key_links:
    - from: "cmd/entomb_cmd.go"
      to: "cmd/registry.go"
      via: "updateRegistryFinalStats call"
      pattern: "updateRegistryFinalStats"
    - from: "cmd/entomb_cmd.go"
      to: "state.Memory.Instincts"
      via: "confidence filtering in extractNearMissInstincts"
      pattern: "Instinct.*Confidence"
---

<objective>
Enrich the entomb ceremony with near-miss wisdom extraction, full temp sweep, and registry final stats.

Purpose: Near-miss instincts (0.5-0.8 confidence) are preserved in the chamber archive instead of lost. Old temp files are cleaned up properly. The registry gets a complete final record of the colony's life.
Output: Modified entomb_cmd.go with near-miss extraction, temp sweep, and registry update. Modified registry.go with final stats fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-lifecycle-ceremony-status-entomb-resume/63-CONTEXT.md
@.planning/phases/63-lifecycle-ceremony-status-entomb-resume/63-RESEARCH.md
@.planning/phases/63-lifecycle-ceremony-status-entomb-resume/63-PATTERNS.md
@.planning/phases/63-lifecycle-ceremony-status-entomb-resume/63-VALIDATION.md

<interfaces>
<!-- Instinct struct for near-miss extraction -->
From pkg/colony/colony.go (lines 412-427):
```go
type Instinct struct {
    ID           string   `json:"id"`
    Trigger      string   `json:"trigger"`
    Action       string   `json:"action"`
    Confidence   float64  `json:"confidence"`
    Status       string   `json:"status"`
    Domain       string   `json:"domain"`
    Source       string   `json:"source"`
    Evidence     []string `json:"evidence"`
    Tested       bool     `json:"tested"`
    CreatedAt    string   `json:"created_at"`
    LastApplied  *string  `json:"last_applied"`
    Applications int      `json:"applications"`
    Successes    int      `json:"successes"`
    Failures     int      `json:"failures"`
}
```

<!-- MiddenEntry struct for 30-day cleanup -->
From pkg/colony/midden.go (lines 6-17):
```go
type MiddenEntry struct {
    ID                string   `json:"id"`
    Timestamp         string   `json:"timestamp"`
    Category          string   `json:"category"`
    Source            string   `json:"source"`
    Message           string   `json:"message"`
    Reviewed          bool     `json:"reviewed"`
    Acknowledged      *bool    `json:"acknowledged,omitempty"`
    AcknowledgedAt    *string  `json:"acknowledged_at,omitempty"`
    AcknowledgeReason *string  `json:"acknowledge_reason,omitempty"`
    Tags              []string `json:"tags,omitempty"`
}
```

<!-- registryEntry struct to extend -->
From cmd/registry.go (lines 16-22):
```go
type registryEntry struct {
    RepoPath     string   `json:"repo_path"`
    Domains      []string `json:"domains"`
    Active       bool     `json:"active"`
    RegisteredAt string   `json:"registered_at"`
    LastGoal     string   `json:"last_goal,omitempty"`
}
```

<!-- Entomb command flow (simplified from entomb_cmd.go lines 19-126) -->
```go
var entombCmd = &cobra.Command{
    RunE: func(cmd *cobra.Command, args []string) error {
        // 1. Guard checks + load state
        // 2. Create chamber
        // 3. writeEntombManifest(chamberDir, chamberName, state, now)
        // 4. copyEntombArtifacts(aetherRoot, store.BasePath(), chamberDir)
        // 5. verifyEntombedChamber(chamberDir)
        // 6. resetColonyStateForEntomb(state) + save
        // 7. clearActiveColonyRuntimeFiles(aetherRoot, store.BasePath())
        // 8. writeEntombRecoveryDocs(...)
        // 9. emitLifecycleCeremony(...)
        // 10. outputWorkflow(result, renderEntombVisual(result))
    },
}
```

<!-- writeEntombManifest pattern (lines 226-247) -->
```go
func writeEntombManifest(chamberDir, chamberName string, state colony.ColonyState, now time.Time) error {
    manifest := map[string]interface{}{
        "name": chamberName, "goal": goal, "scope": string(state.EffectiveScope()),
        "milestone": state.Milestone, "phases_completed": completedPhaseCount(state),
        "total_phases": len(state.Plan.Phases), "entombed_at": now.Format(time.RFC3339),
        "colony_version": state.ColonyVersion, "state": state.State,
    }
    data, _ := json.MarshalIndent(manifest, "", "  ")
    return os.WriteFile(filepath.Join(chamberDir, "manifest.json"), append(data, '\n'), 0644)
}
```

<!-- clearActiveColonyRuntimeFiles pattern (lines 426-464) -->
```go
func clearActiveColonyRuntimeFiles(aetherRoot, dataDir string) error {
    toRemove := []string{
        filepath.Join(dataDir, "session.json"), filepath.Join(dataDir, "pheromones.json"),
        filepath.Join(dataDir, "activity.log"), filepath.Join(dataDir, "flags.json"),
        filepath.Join(dataDir, "constraints.json"), filepath.Join(dataDir, "spawn-tree.txt"),
        filepath.Join(dataDir, "spawn-runs.json"), filepath.Join(dataDir, "timing.log"),
        filepath.Join(dataDir, "view-state.json"), filepath.Join(dataDir, ".version-check-cache"),
        filepath.Join(aetherRoot, ".aether", "CROWNED-ANTHILL.md"),
        filepath.Join(aetherRoot, ".aether", "CONTEXT.md"),
    }
    // ... remove each + XML + colonies + reviews + worktrees dirs
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Near-miss instinct extraction, temp sweep, and registry final stats for entomb</name>
  <files>cmd/entomb_cmd.go, cmd/registry.go, cmd/entomb_cmd_test.go</files>
  <read_first>
    - cmd/entomb_cmd.go (full file -- extending manifest, adding temp sweep, registry update)
    - cmd/registry.go (full file -- extending registryEntry with FinalStats)
    - cmd/entomb_cmd_test.go (existing test patterns)
    - pkg/colony/colony.go (Instinct struct -- lines 412-427)
    - pkg/colony/midden.go (MiddenEntry struct -- lines 6-17)
    - pkg/colony/pheromones.go (PheromoneSignal struct -- for expired pheromone cleanup)
  </read_first>
  <behavior>
    - Test 1: TestEntombNearMissExtraction -- create colony state with instincts at confidence 0.4, 0.6, 0.9. Run entomb. Verify only the 0.6 instinct appears in chamber manifest under "near_miss_instincts". Verify the 0.9 instinct is NOT in near_miss (it is high-confidence).
    - Test 2: TestEntombTempSweepMidden -- create midden.json with one entry 40 days old and one entry 5 days old. Run entomb. Verify midden.json in chamber contains only the 5-day entry.
    - Test 3: TestEntombTempSweepExpiredPheromones -- create pheromones.json with signals where Strength=0 and Active=false. Run entomb. Verify those signals are removed from the pheromones file but still archived in chamber.
    - Test 4: TestEntombRegistryFinalStats -- register a colony, then run entomb. Verify registry entry has FinalStats with phase_count, plan_count, learning_count, instinct_count, seal_date, colony_duration fields and Active=false.
    - Test 5: TestEntombRegistryNoEntry -- run entomb for a colony not in registry. Verify entomb still succeeds (best-effort, no error).
    - Test 6: TestNearMissSuggestionOutput -- verify entomb output contains "N instincts eligible for hive promotion" when near-miss instincts exist.
  </behavior>
  <action>
    **Part A: Extend registryEntry with FinalStats (per D-06)**

    In `cmd/registry.go`, add a new struct and extend `registryEntry`:

    ```go
    type registryFinalStats struct {
        PhaseCount    int    `json:"phase_count,omitempty"`
        PlanCount     int    `json:"plan_count,omitempty"`
        LearningCount int    `json:"learning_count,omitempty"`
        InstinctCount int    `json:"instinct_count,omitempty"`
        SealDate      string `json:"seal_date,omitempty"`
        Duration      string `json:"duration,omitempty"`
    }

    type registryEntry struct {
        RepoPath     string            `json:"repo_path"`
        Domains      []string          `json:"domains"`
        Active       bool              `json:"active"`
        RegisteredAt string            `json:"registered_at"`
        LastGoal     string            `json:"last_goal,omitempty"`
        FinalStats   *registryFinalStats `json:"final_stats,omitempty"`
    }
    ```

    Add a helper function `updateRegistryFinalStats`:

    ```go
    func updateRegistryFinalStats(repoPath string, stats registryFinalStats) {
        hub := resolveHubPath()
        registryPath := filepath.Join(hub, "registry", "registry.json")

        var rd registryData
        if raw, err := os.ReadFile(registryPath); err != nil {
            return // no registry -- best effort
        }
        if err := json.Unmarshal(raw, &rd); err != nil {
            return
        }
        for i, c := range rd.Colonies {
            if c.RepoPath == repoPath {
                rd.Colonies[i].Active = false
                rd.Colonies[i].FinalStats = &stats
                _ = writeRegistry(registryPath, rd)
                return
            }
        }
        // No matching entry -- best effort, don't error
    }
    ```

    **Part B: Near-miss instinct extraction (per D-04)**

    In `cmd/entomb_cmd.go`, add a new helper function:

    ```go
    func extractNearMissInstincts(state colony.ColonyState) []colony.Instinct {
        var nearMiss []colony.Instinct
        for _, inst := range state.Memory.Instincts {
            if inst.Confidence >= 0.5 && inst.Confidence < 0.8 {
                nearMiss = append(nearMiss, inst)
            }
        }
        return nearMiss
    }
    ```

    **Part C: Full temp sweep (per D-05)**

    In `cmd/entomb_cmd.go`, add a new function `entombTempSweep` that extends cleanup beyond what `clearActiveColonyRuntimeFiles` already handles:

    ```go
    func entombTempSweep(dataDir string, s *storage.Store) (int, error) {
        cleaned := 0

        // 1. Prune expired pheromones (Active=false and Strength=0 or nil)
        var pf colony.PheromoneFile
        if err := s.LoadJSON("pheromones.json", &pf); err == nil && len(pf.Signals) > 0 {
            var kept []colony.PheromoneSignal
            for _, sig := range pf.Signals {
                if !sig.Active && (sig.Strength == nil || *sig.Strength == 0) {
                    cleaned++
                    continue
                }
                kept = append(kept, sig)
            }
            if cleaned > 0 {
                pf.Signals = kept
                _ = s.SaveJSON("pheromones.json", pf)
            }
        }

        // 2. Prune midden entries older than 30 days
        var mf struct {
            Entries []colony.MiddenEntry `json:"entries"`
        }
        if err := s.LoadJSON("midden.json", &mf); err == nil && len(mf.Entries) > 0 {
            cutoff := time.Now().Add(-30 * 24 * time.Hour)
            var kept []colony.MiddenEntry
            for _, entry := range mf.Entries {
                ts, err := time.Parse(time.RFC3339, entry.Timestamp)
                if err != nil || ts.After(cutoff) {
                    kept = append(kept, entry) // keep if parse error or recent
                    continue
                }
                cleaned++
            }
            if len(kept) < len(mf.Entries) {
                mf.Entries = kept
                _ = s.SaveJSON("midden.json", mf)
            }
        }

        // 3. Clean old session snapshots (backups directory)
        backupDir := filepath.Join(dataDir, "backups")
        if entries, err := os.ReadDir(backupDir); err == nil {
            cutoff := time.Now().Add(-7 * 24 * time.Hour)
            for _, entry := range entries {
                if entry.IsDir() {
                    continue
                }
                info, err := entry.Info()
                if err != nil {
                    continue
                }
                if info.ModTime().Before(cutoff) {
                    _ = os.Remove(filepath.Join(backupDir, entry.Name()))
                    cleaned++
                }
            }
        }

        return cleaned, nil
    }
    ```

    **Part D: Wire into entomb command flow**

    Modify the `entombCmd.RunE` function in `cmd/entomb_cmd.go`. The changes go between existing steps:

    1. After `copyEntombArtifacts()` (line 74) and before `verifyEntombedChamber()` (line 82):
       - Extract near-miss instincts
       - Extend the manifest with near-miss data
       - Run temp sweep

    2. After `clearActiveColonyRuntimeFiles()` (line 96) and before `writeEntombRecoveryDocs()` (line 98):
       - Update registry with final stats

    3. Extend the `result` map and visual output with near-miss count and suggestion.

    Specifically, after `copyEntombArtifacts` call succeeds and before `verifyEntombedChamber`:

    ```go
    // Extract near-miss instincts and extend manifest
    nearMiss := extractNearMissInstincts(state)
    if len(nearMiss) > 0 {
        nearMissData, _ := json.MarshalIndent(nearMiss, "", "  ")
        _ = os.WriteFile(filepath.Join(chamberDir, "near-miss-instincts.json"), append(nearMissData, '\n'), 0644)
        // Also update manifest with count
        manifestPath := filepath.Join(chamberDir, "manifest.json")
        if raw, err := os.ReadFile(manifestPath); err == nil {
            var manifest map[string]interface{}
            if json.Unmarshal(raw, &manifest) == nil {
                manifest["near_miss_instincts"] = len(nearMiss)
                if data, err := json.MarshalIndent(manifest, "", "  "); err == nil {
                    _ = os.WriteFile(manifestPath, append(data, '\n'), 0644)
                }
            }
        }
    }

    // Run temp sweep (before reset, after artifacts are copied)
    tempSwept, _ := entombTempSweep(store.BasePath(), store)
    ```

    After `clearActiveColonyRuntimeFiles` succeeds and before `writeEntombRecoveryDocs`:

    ```go
    // Update registry with final stats (best-effort)
    repoPath, _ := os.Getwd()
    updateRegistryFinalStats(repoPath, registryFinalStats{
        PhaseCount:    len(state.Plan.Phases),
        PlanCount:     countTotalPlans(state),
        LearningCount: len(state.Memory.PhaseLearnings),
        InstinctCount: len(state.Memory.Instincts),
        SealDate:      now.Format(time.RFC3339),
        Duration:      computeColonyDuration(state),
    })
    ```

    Add helper functions:

    ```go
    func countTotalPlans(state colony.ColonyState) int {
        count := 0
        for _, phase := range state.Plan.Phases {
            count += phase.PlanCount
        }
        return count
    }

    func computeColonyDuration(state colony.ColonyState) string {
        if state.InitializedAt == nil {
            return ""
        }
        initTime, err := time.Parse(time.RFC3339, *state.InitializedAt)
        if err != nil {
            return ""
        }
        duration := time.Since(initTime)
        days := int(duration.Hours() / 24)
        if days > 0 {
            return fmt.Sprintf("%dd", days)
        }
        return fmt.Sprintf("%.0fh", duration.Hours())
    }
    ```

    Extend the `result` map (around line 113-122) to include near-miss and temp sweep data:

    ```go
    result["near_miss_instincts"] = len(nearMiss)
    result["temp_swept"] = tempSwept
    if len(nearMiss) > 0 {
        result["suggestion"] = fmt.Sprintf("%d instincts eligible for hive promotion -- run aether hive-promote", len(nearMiss))
    }
    ```

    Note: Check if `Phase` struct has a `PlanCount` field. If not, count plans differently (e.g., from completed plan files or a different approach).

    **Part E: Write tests**

    Add tests to `cmd/entomb_cmd_test.go` following existing patterns (saveGlobals, resetRootCmd, setupBuildFlowTest, createTestColonyState, redirect stdout).

    Key tests:
    1. Near-miss extraction with mixed confidence instincts
    2. Midden 30-day cleanup
    3. Expired pheromone pruning
    4. Registry final stats update
    5. Best-effort registry (no entry)
    6. Near-miss suggestion in output
  </action>
  <verify>
    <automated>go test ./cmd/... -run "TestEntomb" -count=1 -timeout 60s</automated>
  </verify>
  <done>
    - extractNearMissInstincts correctly filters instincts with 0.5 <= confidence < 0.8
    - Near-miss instincts are written to near-miss-instincts.json in chamber and count added to manifest.json
    - entombTempSweep cleans expired pheromones (Active=false, Strength=0), midden entries >30 days, old backups
    - Registry entry gets FinalStats with phase_count, plan_count, learning_count, instinct_count, seal_date, duration when entomb runs
    - Entomb output includes suggestion line when near-miss instincts exist
    - Registry update is best-effort (no error if colony not registered)
    - Temp sweep runs AFTER copyEntombArtifacts (artifacts preserved in chamber before cleanup)
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Entomb reads colony state | Local trusted data |
| Registry file writes | Local filesystem, only written during entomb ceremony |
| Temp sweep file deletion | Only targets files within colony data directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-02-01 | T | Temp sweep file deletion | mitigate | Sweep only targets files within dataDir path; uses filepath.Join for path safety; runs AFTER artifacts are copied to chamber |
| T-63-02-02 | T | Registry update | accept | Registry is local-only; stats derived from colony state (trusted local data) |
| T-63-02-03 | I | Near-miss manifest data | accept | Manifest is written to local chamber directory; content comes from colony instincts (already validated) |
</threat_model>

<verification>
1. `go test ./cmd/... -run "TestEntomb" -count=1 -timeout 60s` passes
2. `go test ./... -race -count=1` passes
3. `grep -c "extractNearMissInstincts" cmd/entomb_cmd.go` returns >= 2
4. `grep -c "entombTempSweep" cmd/entomb_cmd.go` returns >= 2
5. `grep -c "updateRegistryFinalStats" cmd/registry.go` returns >= 1
6. `grep -c "FinalStats" cmd/registry.go` returns >= 3
</verification>

<success_criteria>
- Near-miss instincts (0.5-0.8 confidence) extracted and preserved in chamber
- Chamber manifest includes near_miss_instincts count
- Temp sweep removes expired pheromones and old midden entries
- Registry entry updated with final stats and marked inactive
- Entomb output includes hive promotion suggestion when near-miss instincts exist
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/63-lifecycle-ceremony-status-entomb-resume/63-02-SUMMARY.md`
</output>
