---
phase: 100-command-inventory-lifecycle-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/contracts/init.md
  - cmd/contracts/discuss.md
  - cmd/contracts/colonize.md
  - cmd/contracts/plan.md
  - cmd/contracts/build.md
  - cmd/contracts/continue.md
  - cmd/contracts/seal.md
  - cmd/contracts/entomb.md
  - cmd/contracts/publish.md
  - cmd/contracts/update.md
  - cmd/contracts/recover.md
  - cmd/contracts/status.md
  - cmd/contracts/resume.md
  - cmd/contracts/watch.md
  - cmd/contracts/patrol.md
  - cmd/contracts/profile.md
  - cmd/contract_validate_test.go
autonomous: true
requirements:
  - LIFE-01

must_haves:
  truths:
    - "Each of the 16 lifecycle commands has a contract document with all 4 required sections"
    - "Contract documents accurately describe the command's inputs, outputs, state mutations, and preconditions"
    - "An automated test verifies all 16 contract files exist and have the correct structure"
  artifacts:
    - path: "cmd/contracts/init.md"
      provides: "Lifecycle contract for init command"
      contains: "## Inputs"
    - path: "cmd/contracts/build.md"
      provides: "Lifecycle contract for build command"
      contains: "## State Mutations"
    - path: "cmd/contracts/continue.md"
      provides: "Lifecycle contract for continue command"
      contains: "## Outputs"
    - path: "cmd/contracts/seal.md"
      provides: "Lifecycle contract for seal command"
      contains: "## Preconditions"
    - path: "cmd/contract_validate_test.go"
      provides: "Contract structure validation test"
      exports: ["TestLifecycleContracts", "TestContractStructure"]
  key_links:
    - from: "cmd/contract_validate_test.go"
      to: "cmd/contracts/*.md"
      via: "os.ReadFile on each contract file, parse for required sections"
      pattern: "contracts/"
---

<objective>
Create hand-curated lifecycle contract documents for all 16 user-facing lifecycle commands and a validation test that ensures structural completeness.

Purpose: Per D-04 and D-05, these contracts document what each lifecycle command requires, produces, and mutates. They are the human-readable foundation for Phases 101-104 (parity checks, worker audits, data flow tracing). They are hand-curated by reading Go source to ensure accuracy, not auto-generated. Per D-06, exactly 16 commands get full contracts.

Output: 16 Markdown contract files in `cmd/contracts/` and `cmd/contract_validate_test.go` for automated structure verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-command-inventory-lifecycle-contracts/100-CONTEXT.md
@.planning/phases/100-command-inventory-lifecycle-contracts/100-RESEARCH.md

<interfaces>
<!-- Source files for each lifecycle command -- executor MUST read these to write accurate contracts -->

From RESEARCH.md Lifecycle Command Source Map:
```
init       -> cmd/init_cmd.go, cmd/init_research.go, cmd/init_ceremony.go
discuss    -> cmd/discuss.go, cmd/discuss_analyze.go, cmd/pending_decision.go
colonize   -> cmd/codex_colonize.go, cmd/codex_colonize_finalize.go
plan       -> cmd/codex_plan.go, cmd/codex_plan_finalize.go
build      -> cmd/codex_build.go, cmd/codex_build_finalize.go, cmd/codex_build_worktree.go, cmd/build_flow_cmds.go
continue   -> cmd/codex_continue.go, cmd/codex_continue_finalize.go, cmd/codex_continue_plan.go
seal       -> cmd/seal_final_review.go, cmd/shelf_seal.go
entomb     -> cmd/entomb_cmd.go, cmd/shelf_entomb.go, cmd/chamber.go
publish    -> cmd/publish_cmd.go
update     -> cmd/update_cmd.go
recover    -> cmd/recover.go, cmd/recover_scanner.go, cmd/recover_repair.go, cmd/recovery_classify.go
status     -> cmd/status.go
resume     -> cmd/session_flow_cmds.go, cmd/session_cmds.go
watch      -> cmd/compatibility_cmds.go
patrol     -> cmd/patrol_check.go
profile    -> cmd/profile.go
```

Contract 4-section structure per D-04:
```markdown
# <command-name> -- Lifecycle Contract

## Inputs
<!-- Flags, args, required vs optional -->

## Outputs
<!-- stdout format, files created/modified, exit codes -->

## State Mutations
<!-- Which .aether/data files change, what colony state transitions occur -->

## Preconditions
<!-- What must exist before the command can run successfully -->
```

Common data artifacts referenced in .aether/data/:
- COLONY_STATE.json -- colony state with phases, instincts, pheromones
- pheromones.json -- active signals
- session.json -- session data
- midden/midden.json -- failure tracking
- handoffs/worker-handoffs.json -- worker relay notes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 16 lifecycle contract documents</name>
  <files>cmd/contracts/init.md, cmd/contracts/discuss.md, cmd/contracts/colonize.md, cmd/contracts/plan.md, cmd/contracts/build.md, cmd/contracts/continue.md, cmd/contracts/seal.md, cmd/contracts/entomb.md, cmd/contracts/publish.md, cmd/contracts/update.md, cmd/contracts/recover.md, cmd/contracts/status.md, cmd/contracts/resume.md, cmd/contracts/watch.md, cmd/contracts/patrol.md, cmd/contracts/profile.md</files>
  <read_first>
    cmd/init_cmd.go (init command implementation)
    cmd/init_research.go (init research sub-implementation)
    cmd/init_ceremony.go (init ceremony logic)
    cmd/discuss.go (discuss command implementation)
    cmd/discuss_analyze.go (discuss analysis logic)
    cmd/pending_decision.go (decision storage)
    cmd/codex_colonize.go (colonize implementation)
    cmd/codex_colonize_finalize.go (colonize finalization)
    cmd/codex_plan.go (plan implementation)
    cmd/codex_plan_finalize.go (plan finalization)
    cmd/codex_build.go (build entry point)
    cmd/codex_build_finalize.go (build finalization)
    cmd/codex_build_worktree.go (build worktree logic)
    cmd/build_flow_cmds.go (build flow helpers)
    cmd/codex_continue.go (continue main logic)
    cmd/codex_continue_finalize.go (continue finalize)
    cmd/codex_continue_plan.go (continue plan-only)
    cmd/seal_final_review.go (seal implementation)
    cmd/shelf_seal.go (seal shelf logic)
    cmd/entomb_cmd.go (entomb implementation)
    cmd/shelf_entomb.go (entomb shelf logic)
    cmd/chamber.go (chamber management)
    cmd/publish_cmd.go (publish implementation)
    cmd/update_cmd.go (update implementation)
    cmd/recover.go (recover implementation)
    cmd/recover_scanner.go (recover scanning)
    cmd/recover_repair.go (recover repair)
    cmd/recovery_classify.go (recovery classification)
    cmd/status.go (status implementation)
    cmd/session_flow_cmds.go (resume flow)
    cmd/session_cmds.go (session commands)
    cmd/compatibility_cmds.go (watch command)
    cmd/patrol_check.go (patrol implementation)
    cmd/profile.go (profile implementation)
    cmd/helpers.go (outputOK, outputError, mustGetString patterns)
  </read_first>
  <action>
Per D-04, D-05, D-06: Create cmd/contracts/ directory with 16 Markdown files, one per lifecycle command.

First create the directory: cmd/contracts/

For each of the 16 commands, read the listed source files and write a contract document following this exact structure:

```markdown
# {command-name} -- Lifecycle Contract

**Last verified:** 2026-05-07
**Source files:** {comma-separated list of primary source files}

## Inputs

### Flags
| Flag | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| --{flag} | {string/bool/int} | {yes/no} | {value} | {what it does} |

### Arguments
{positional args if any, or "None"}

### Environment
{env vars read, or "None"}

## Outputs

### Stdout
{format: JSON envelope via outputOK, visual via outputWorkflow, or plain text}

### Files Created/Modified
| File | Operation | When |
|------|-----------|------|
| {path} | {create/modify/read} | {condition} |

### Exit Codes
| Code | Meaning |
|------|---------|
| 0 | Success |
| 1 | {error condition} |

## State Mutations

### Colony State Transitions
{If command changes colony phase/status, describe before -> after}

### Data Artifacts Modified
| Artifact | Write Type | Content Changed |
|----------|------------|-----------------|
| .aether/data/{file} | {create/update/delete} | {what changes} |

## Preconditions

{What must be true before running this command successfully}
```

The 16 contract files and their specific guidance:

1. **init.md** -- cmd/init_cmd.go, cmd/init_research.go, cmd/init_ceremony.go
   - Inputs: positional arg for goal (required), --skip-colonize flag, --research flag
   - Outputs: creates COLONY_STATE.json, session.json, may trigger colonize
   - State: creates new colony state with phase tracking
   - Preconditions: .aether/ directory must exist (from ant-lay-eggs or auto-created)

2. **discuss.md** -- cmd/discuss.go, cmd/discuss_analyze.go, cmd/pending_decision.go
   - Inputs: --text flag for clarification text, --interactive flag
   - Outputs: updates pending-decisions.json, may emit pheromones
   - State: stores pending decisions, emits REDIRECT pheromones for hard constraints
   - Preconditions: colony must be initialized (COLONY_STATE.json exists)

3. **colonize.md** -- cmd/codex_colonize.go, cmd/codex_colonize_finalize.go
   - Inputs: --root flag for repo root, --compact flag
   - Outputs: survey data in .aether/data/survey/, colony state updates
   - State: updates colony state with survey results
   - Preconditions: colony must be initialized

4. **plan.md** -- cmd/codex_plan.go, cmd/codex_plan_finalize.go
   - Inputs: --force flag to overwrite, --depth flag
   - Outputs: plan files in .planning/phases/, ROADMAP.md updates
   - State: updates colony state with plan metadata
   - Preconditions: colony initialized, may need colonize complete

5. **build.md** -- cmd/codex_build.go, cmd/codex_build_finalize.go, cmd/codex_build_worktree.go, cmd/build_flow_cmds.go
   - Inputs: positional arg for phase number, --verbose flag, --max-workers flag
   - Outputs: dispatches workers, creates build artifacts
   - State: updates colony state with build results, worker spawn records
   - Preconditions: plan must exist, colony initialized

6. **continue.md** -- cmd/codex_continue.go, cmd/codex_continue_finalize.go, cmd/codex_continue_plan.go
   - Inputs: --plan-only flag, --finalize flag, --verification-depth flag
   - Outputs: verification results, review reports, phase advancement
   - State: advances colony phase, records instincts/learnings, updates review ledgers
   - Preconditions: build must have completed for current phase

7. **seal.md** -- cmd/seal_final_review.go, cmd/shelf_seal.go
   - Inputs: --skip-review flag, --archive flag
   - Outputs: archive in .aether/chambers/, hive promotion of high-confidence instincts
   - State: transitions colony to sealed state, promotes instincts to hive
   - Preconditions: all phases complete, colony at final phase

8. **entomb.md** -- cmd/entomb_cmd.go, cmd/shelf_entomb.go, cmd/chamber.go
   - Inputs: --name flag for archive name
   - Outputs: archive entry in chambers directory
   - State: archives colony data, clears active colony state
   - Preconditions: colony must be sealed

9. **publish.md** -- cmd/publish_cmd.go
   - Inputs: --version flag, --channel flag (stable/dev), --binary-dest flag, --skip-binary flag
   - Outputs: syncs companion files to hub (~/.aether/ or ~/.aether-dev/), optionally builds binary
   - State: updates hub file manifest, version tracking
   - Preconditions: must run from Aether repo root, git working tree clean

10. **update.md** -- cmd/update_cmd.go
    - Inputs: --force flag, --download-binary flag
    - Outputs: syncs hub files to repo .aether/ and .claude/ .opencode/ wrapper dirs
    - State: updates local companion files from hub
    - Preconditions: hub must have published content

11. **recover.md** -- cmd/recover.go, cmd/recover_scanner.go, cmd/recover_repair.go, cmd/recovery_classify.go
    - Inputs: --scan flag for scan-only mode, --repair flag
    - Outputs: recovery report, repaired state files
    - State: may repair COLONY_STATE.json, clear stuck flags, reset phase status
    - Preconditions: colony initialized but in stuck/broken state

12. **status.md** -- cmd/status.go
    - Inputs: --json flag, --verbose flag
    - Outputs: colony dashboard (visual or JSON)
    - State: read-only, no mutations
    - Preconditions: colony initialized

13. **resume.md** -- cmd/session_flow_cmds.go, cmd/session_cmds.go
    - Inputs: none (reads session.json automatically)
    - Outputs: restores session context, displays status
    - State: restores colony state into active session
    - Preconditions: session.json exists from prior session

14. **watch.md** -- cmd/compatibility_cmds.go
    - Inputs: --interval flag, --watch flag for continuous mode
    - Outputs: live colony dashboard with periodic refresh
    - State: read-only, no mutations
    - Preconditions: colony initialized

15. **patrol.md** -- cmd/patrol_check.go
    - Inputs: --json flag
    - Outputs: health check report (data integrity, file consistency)
    - State: read-only diagnostics
    - Preconditions: colony initialized

16. **profile.md** -- cmd/profile.go
    - Inputs: --refresh flag to re-analyze, --list flag
    - Outputs: behavioral profile display, QUEEN.md preference updates
    - State: may update QUEEN.md with [profiled] directives
    - Preconditions: colony initialized with behavioral observations

For each contract, READ the listed source files carefully to extract:
- Actual flag names from cobra.Command definitions (grep for `Flags().String`, `Flags().Bool`, etc.)
- Actual output patterns (outputOK vs outputWorkflow vs fmt.Println)
- Actual .aether/data/ files touched (grep for store.SaveJSON, store.LoadJSON, os.WriteFile, etc.)
- Actual preconditions (grep for error returns when files/state missing)

Write the facts as found in source, not assumptions. Use "None" where a section genuinely has no content (e.g., status has no state mutations). Include "Last verified" date as 2026-05-07.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && ls cmd/contracts/init.md cmd/contracts/discuss.md cmd/contracts/colonize.md cmd/contracts/plan.md cmd/contracts/build.md cmd/contracts/continue.md cmd/contracts/seal.md cmd/contracts/entomb.md cmd/contracts/publish.md cmd/contracts/update.md cmd/contracts/recover.md cmd/contracts/status.md cmd/contracts/resume.md cmd/contracts/watch.md cmd/contracts/patrol.md cmd/contracts/profile.md && echo "All 16 contracts exist"</automated>
  </verify>
  <done>
    - cmd/contracts/ directory exists with exactly 16 .md files
    - Each file follows the 4-section structure (Inputs, Outputs, State Mutations, Preconditions)
    - Each file has "Last verified" date header
    - Each file lists its source files
    - Contracts reflect actual Go source code (flag names, output patterns, data artifacts)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create contract structure validation test</name>
  <files>cmd/contract_validate_test.go</files>
  <read_first>
    cmd/audit_catalog_test.go (if plan 01 already executed, for test pattern reference)
    cmd/testing_main_test.go (test globals pattern)
    cmd/contracts/ (all 16 contract files created in Task 1)
  </read_first>
  <behavior>
    - Test 1: TestLifecycleContracts -- verifies all 16 contract files exist in cmd/contracts/ with exact expected names
    - Test 2: TestContractStructure -- verifies each contract file contains all 4 required section headers: "## Inputs", "## Outputs", "## State Mutations", "## Preconditions"
  </behavior>
  <action>
Create cmd/contract_validate_test.go with two test functions.

The 16 expected contract names (per D-06):
```
init, discuss, colonize, plan, build, continue, seal, entomb, publish, update, recover, status, resume, watch, patrol, profile
```

1. TestLifecycleContracts:
   - Define expectedContracts slice with the 16 names above
   - For each name, read cmd/contracts/{name}.md using os.ReadFile
   - If any file is missing, fail with t.Errorf listing which are missing
   - Also verify no extra .md files exist in cmd/contracts/ (list directory, check count is exactly 16)

2. TestContractStructure:
   - Required sections: []string{"## Inputs", "## Outputs", "## State Mutations", "## Preconditions"}
   - For each of the 16 contract files:
     - Read file content
     - For each required section, use strings.Contains(content, section)
     - If any section is missing, fail with t.Errorf naming the file and missing section
   - Also verify each file has a "# " title line and "Last verified:" date line

These tests ensure the contract files maintain their required structure going forward. If any contract is accidentally deleted or a section is removed, CI catches it.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && go test ./cmd/ -run "TestLifecycleContracts|TestContractStructure" -v</automated>
  </verify>
  <done>
    - cmd/contract_validate_test.go exists with TestLifecycleContracts and TestContractStructure
    - Both tests pass
    - Tests verify existence of all 16 contract files
    - Tests verify 4-section structure in each file
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code -> Contract | Hand-curated contracts document what source code does; contracts may drift if code changes |
| Contract files on disk | Committed to git; test verifies structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-03 | Tampering | Contract files | mitigate | Committed to git; automated test verifies structure; "Last verified" date tracks freshness |
| T-100-04 | Repudiation | Contract accuracy | accept | Contracts are best-effort documentation; Phase 101+ adds automated parity checks |
</threat_model>

<verification>
1. `go test ./cmd/ -run "TestLifecycleContracts|TestContractStructure" -v` passes
2. All 16 contract files exist in cmd/contracts/
3. Each contract has all 4 required sections
4. `go vet ./cmd/contract_validate_test.go` passes clean
5. Existing test suite unaffected: `go test ./cmd/ -run TestCommandCount -v` passes
</verification>

<success_criteria>
- 16 contract documents exist in cmd/contracts/ with correct naming
- Each contract has all 4 sections: Inputs, Outputs, State Mutations, Preconditions
- Contracts accurately reflect source code (flag names, output patterns, data files touched)
- Validation tests pass and guard against structural regressions
</success_criteria>

<output>
After completion, create `.planning/phases/100-command-inventory-lifecycle-contracts/100-02-SUMMARY.md`
</output>
