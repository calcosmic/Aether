---
phase: 131-test-coverage-host-commands
plan: 01
type: execute
wave: 1
depends_on:
  - 130-01
files_modified:
  - .aether/ts-host/test/host-flags.test.ts
  - .aether/ts-host/test/host-integration.test.ts
  - .aether/docs/host-command-reference.md
autonomous: true
requirements:
  - TCV-01
  - TCV-02
  - TCV-03
  - TCV-04
  - TCV-05

must_haves:
  truths:
    - "`aether host plan --depth balanced --planning-depth standard` is exercised in tests"
    - "`aether host continue --verification-depth heavy` is exercised in tests"
    - "`aether host watch --no-dashboard` is exercised in tests"
    - "`aether host swarm <target> --no-dashboard` is exercised in tests"
    - "Every documented `aether host` subcommand has at least one test invocation"
  artifacts:
    - path: ".aether/ts-host/test/host-flags.test.ts"
      provides: "Unit tests for parseArgs flag parsing and passthrough"
    - path: ".aether/ts-host/test/host-integration.test.ts"
      provides: "Integration tests for each host subcommand with realistic flags"
  key_links:
    - from: ".aether/ts-host/test/host-flags.test.ts"
      to: ".aether/ts-host/src/host.ts"
      via: "parseArgs function"
    - from: ".aether/ts-host/test/host-integration.test.ts"
      to: ".aether/ts-host/src/host.ts"
      via: "main() switch cases"
---

<objective>
Write tests that exercise every documented `aether host` subcommand with realistic flag combinations, ensuring the flag parsing and passthrough logic is correct.

Purpose: Guarantee that the host surface fixes from Phase 130 don't regress — every flag parses correctly and reaches the Go CLI.
Output: Two test files (unit + integration) covering all 7 subcommands and their flags.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/130-host-surface-completeness/130-01-SUMMARY.md

## Key interfaces from existing code

From `.aether/ts-host/src/host.ts` (after Phase 130 fixes):
- `parseArgs(argv)` returns: command, cwd, simulate, noDashboard, skipMiddenCheck, depth, planningDepth, verificationDepth, light, heavy, workerTimeout, help, positional
- `main()` switch handles: plan, build, continue, oracle, lifecycle, watch, swarm
- plan: passes --depth, --planning-depth, --verification-depth, --synthetic, --worker-timeout to Go
- build: passes --synthetic, --light, --worker-timeout to Go
- continue: passes --verification-depth, --light, --heavy, --synthetic, --worker-timeout to Go

From `.aether/docs/host-command-reference.md`:
- Documents all 7 subcommands with their flags

From existing test patterns (`.aether/ts-host/test/go-bridge.test.ts`, `.aether/ts-host/test/oracle-lifecycle.test.ts`):
- Uses `node:test` and `node:assert/strict`
- Uses `__setCallGoJSON` / `__restoreCallGoJSON` mock injection pattern
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write unit tests for parseArgs flag parsing</name>
  <files>.aether/ts-host/test/host-flags.test.ts</files>
  <read_first>
    - .aether/ts-host/src/host.ts (parseArgs function)
    - .aether/ts-host/test/oracle-lifecycle.test.ts (mock injection pattern)
  </read_first>
  <action>
Create `.aether/ts-host/test/host-flags.test.ts` with unit tests for `parseArgs`:

1. **Plan flags:**
   - Input: `["node", "host.js", "plan", "--depth", "balanced", "--planning-depth", "standard"]`
   - Assert: command="plan", depth="balanced", planningDepth="standard"

2. **Continue flags:**
   - Input: `["node", "host.js", "continue", "--verification-depth", "heavy", "--light"]`
   - Assert: command="continue", verificationDepth="heavy", light=true

3. **Watch flags:**
   - Input: `["node", "host.js", "watch", "--no-dashboard"]`
   - Assert: command="watch", noDashboard=true

4. **Swarm flags:**
   - Input: `["node", "host.js", "swarm", "test bug", "--no-dashboard"]`
   - Assert: command="swarm", positional=["test bug"], noDashboard=true

5. **Build positional + flags:**
   - Input: `["node", "host.js", "build", "1", "--light"]`
   - Assert: command="build", positional=["1"], light=true

6. **Help flag:**
   - Input: `["node", "host.js", "--help"]`
   - Assert: help=true

7. **Unknown command:**
   - Input: `["node", "host.js", "unknown"]`
   - Assert: command="unknown" (main() handles the error)

Use `node:test` and `node:assert/strict`.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npm test</automated>
  </verify>
  <done>
    - host-flags.test.ts exists and passes
    - All flag combinations covered
    - npm test passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Write integration tests for host subcommands</name>
  <files>.aether/ts-host/test/host-integration.test.ts</files>
  <read_first>
    - .aether/ts-host/src/host.ts (main() switch cases)
    - .aether/ts-host/test/go-bridge.test.ts (callGoJSON mock pattern)
    - .aether/docs/host-command-reference.md (documented commands)
  </read_first>
  <action>
Create `.aether/ts-host/test/host-integration.test.ts` with integration tests for each subcommand:

Use the `__setCallGoJSON` / `__restoreCallGoJSON` mock injection pattern from `oracle-lifecycle.test.ts`.

For each subcommand, mock `callGoJSON` to return a valid result and verify the args passed:

1. **plan:**
   - Mock callGoJSON to return a PlanCompletion
   - Call main with `plan --depth balanced --planning-depth standard`
   - Verify callGoJSON called with args containing `["plan", "--plan-only", "--depth", "balanced", "--planning-depth", "standard"]`

2. **build:**
   - Mock callGoJSON to return a BuildManifest
   - Call main with `build 1 --light`
   - Verify callGoJSON called with args containing `["build", "1", "--plan-only", "--light"]`

3. **continue:**
   - Mock callGoJSON to return a ContinueCompletion
   - Call main with `continue --verification-depth heavy`
   - Verify callGoJSON called with args containing `["continue", "--plan-only", "--verification-depth", "heavy"]`

4. **watch:**
   - Mock `runWatchDisplay` to return WatchDisplayResult
   - Call main with `watch --no-dashboard`
   - Verify runWatchDisplay called with dashboard=false

5. **swarm:**
   - Mock `runSwarmDisplay` to return SwarmDisplayResult
   - Call main with `swarm "test bug" --no-dashboard`
   - Verify runSwarmDisplay called with target="test bug", dashboard=false

6. **oracle:**
   - Mock `runOracleLifecycle` to return a result
   - Call main with `oracle "topic" --simulate`
   - Verify runOracleLifecycle called with simulateWorkers=true

7. **lifecycle:**
   - Mock `runLifecycle` to return a result
   - Call main with `lifecycle 1 --skip-midden-check`
   - Verify runLifecycle called with phase=1, skipMiddenCheck=true

Note: For integration tests, you may need to export `main` or `parseArgs` from host.ts, or test via a different entry point. Alternatively, add `__setCallGoJSON` and similar mock helpers to the relevant modules.

Use `node:test` and `node:assert/strict`.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npm test</automated>
  </verify>
  <done>
    - host-integration.test.ts exists and passes
    - All 7 subcommands covered
    - npm test passes with no regressions
  </done>
</task>

<task type="auto">
  <name>Task 3: Update host-command-reference.md with test coverage note</name>
  <files>.aether/docs/host-command-reference.md</files>
  <read_first>
    - .aether/docs/host-command-reference.md
    - .aether/ts-host/test/host-flags.test.ts
    - .aether/ts-host/test/host-integration.test.ts
  </read_first>
  <action>
Add a "Test Coverage" section to the end of `.aether/docs/host-command-reference.md`:

```markdown
## Test Coverage

All subcommands and flags are exercised in the test suite:

- **Flag parsing**: `.aether/ts-host/test/host-flags.test.ts` verifies `parseArgs` handles all documented flags correctly.
- **Integration**: `.aether/ts-host/test/host-integration.test.ts` verifies each subcommand calls the correct Go CLI with correct arguments.
```
  </action>
  <verify>
    <manual>Review doc for accuracy</manual>
  </verify>
  <done>
    - Test coverage section added to reference doc
    - Links to test files included
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests -> TS host | Tests verify parseArgs logic; they don't spawn real processes |
| Tests -> Go CLI | Integration tests mock callGoJSON; no real Go subprocess spawned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01 | Information Disclosure | Test logs show command args | accept | Test args are synthetic, not sensitive |
| T-131-02 | Denial of Service | Infinite test recursion | mitigate | Tests don't spawn subprocesses that call tests |
</threat_model>

<verification>
1. `cd .aether/ts-host && npm run typecheck` passes
2. `cd .aether/ts-host && npm test` passes (all new tests + no regressions)
3. `go test ./cmd/...` passes (no Go regressions)
4. Every documented subcommand has at least one test invocation
</verification>

<success_criteria>
- `host-flags.test.ts` covers all flag combinations (plan, build, continue, watch, swarm)
- `host-integration.test.ts` covers all 7 subcommands
- All tests pass with no regressions
- Reference doc includes test coverage section
- All typecheck and test commands pass
</success_criteria>

<output>
After completion, create `.planning/phases/131-test-coverage-host-commands/131-01-SUMMARY.md`
</output>
