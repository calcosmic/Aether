---
phase: 160-fail-loudly
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - AGENTS.md
  - .aether/docs/structural-learning-stack.md
  - cmd/doc_consolidation_claims_test.go
  - cmd/live_wrapper_stderr_test.go
autonomous: true
requirements: [LOUD-06, LOUD-07]
user_setup: []

must_haves:
  truths:
    - "No documentation file states in the present tense that /ant-continue runs phase-end consolidation, because no caller exists"
    - "No documentation file states in the present tense that /ant-seal runs the full consolidation pass, because no caller exists"
    - "A test fails if any of those claims is reintroduced into CLAUDE.md, AGENTS.md, or structural-learning-stack.md"
    - "A test fails if any NEW 2>/dev/null suppression appears anywhere in the two live wrapper directories"
  artifacts:
    - path: "cmd/doc_consolidation_claims_test.go"
      provides: "Static assertion that the three named docs carry no present-tense consolidation-runs claim"
      contains: "TestDocsDoNotClaimConsolidationRunsToday"
    - path: "cmd/live_wrapper_stderr_test.go"
      provides: "Count invariant over stderr suppression in the live wrapper dirs"
      contains: "TestLiveWrapperStderrSuppressionCount"
    - path: ".aether/docs/structural-learning-stack.md"
      provides: "Consolidation section describing what actually runs today"
  key_links:
    - from: "cmd/doc_consolidation_claims_test.go"
      to: "CLAUDE.md, AGENTS.md, .aether/docs/structural-learning-stack.md"
      via: "os.ReadFile plus strings.Contains absence assertions on exact stale phrases"
      pattern: "structural-learning-stack.md"
    - from: "cmd/live_wrapper_stderr_test.go"
      to: ".claude/commands/ant/, .opencode/commands/ant/"
      via: "strings.Count of the suppression token summed across both directories, asserted equal to the allowed total"
      pattern: "commands/ant"
---

<objective>
Make the documentation stop asserting runtime behaviour that has never happened,
and lock the two live wrapper directories against new silent-failure suppression.

RESEARCH.md corrects CONTEXT.md here: only `CLAUDE.md` shows the described
half-corrected state. `AGENTS.md:899` and `.aether/docs/structural-learning-stack.md`
(lines 14, 205-211, 227) are entirely uncorrected and still assert in the present
tense that consolidation runs. Verified during planning: `consolidation-phase-end`
AND `consolidation-seal` both have zero callers, so BOTH claims are false, not just
the phase-end one named in LOUD-07's text.

LOUD-06 is scoped, per RESEARCH.md Pitfall 2, to the two LIVE wrapper directories
where suppression could actually hide a failure from a user. The sites there are
benign (three `grep ... || true` fallbacks per mirror, one `git diff` inside prose).
The deliverable is not deleting them — it is a count invariant so any NEW
suppression trips a test, per CLAUDE.md's Definition of Done corollary preferring
invariants over named-section checks.

Purpose: LOUD-06, LOUD-07, and ROADMAP success criterion 4.
Output: three corrected docs, two invariant/static tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-fail-loudly/160-RESEARCH.md
@.planning/phases/160-fail-loudly/160-PATTERNS.md
@CLAUDE.md

<interfaces>
Exact stale claims located during planning. These are the strings the corrections
must remove and the tests must assert absent:

- CLAUDE.md line 838 — the bullet reading: Lifecycle integration: phase-end at /ant-continue, full at /ant-seal
  (contradicted by the honest CLAUDE.md line 37, which states both consolidation
  subcommands still have no caller)
- AGENTS.md line 899 — the same bullet in AGENTS.md phrasing, using `aether continue` and `aether seal`
- .aether/docs/structural-learning-stack.md line 14 — the sentence: The stack runs automatically at phase-end and seal — workers never call it directly.
- .aether/docs/structural-learning-stack.md line 207 — the sentence opening: Runs at the end of every phase (`/ant-continue`). Executes three ants only
- .aether/docs/structural-learning-stack.md line 211 — the sentence opening: Runs once during `/ant-seal`. Executes five steps
- .aether/docs/structural-learning-stack.md lines 224-231 — the Integration Points table rows keyed `/ant-continue` to `consolidation-phase-end` and `/ant-seal` to `consolidation-seal`

Verified caller state (repo-wide grep excluding tests, .planning/, control-ts/):
`consolidation-phase-end` and `consolidation-seal` are registered at
cmd/graph_consolidation_cmds.go lines 375 and 379 and are invoked by nothing.

The live-wrapper suppression sites, confirmed identical across both platform mirrors:
- .claude/commands/ant/dream.md line 56 and .opencode/commands/ant/dream.md line 56 — a `git diff --stat HEAD~10..HEAD` call inside prose
- .claude/commands/ant/archaeology.md lines 185, 188, 191 and .opencode/commands/ant/archaeology.md lines 185, 188, 191 — grep fallbacks
Total occurrences across both directories: 8 (4 per platform mirror).
</interfaces>

**Scope boundaries:**
- Do NOT wire consolidation into `/ant-continue` or `/ant-seal`. That is Phase 162
  (LEARN-01/02). This plan only stops the docs claiming it already happened.
- Do NOT remove the live suppression sites. They are grep fallbacks with `|| true`
  and a prose `git diff`; removing them changes behaviour for no benefit. Pin the count.
- Do NOT touch `.aether/docs/command-playbooks/`. Its remaining suppression
  occurrences are out of scope (RESEARCH.md Pitfall 2) — nothing executes them.
- Do NOT edit any file under `.claude/commands/ant/` or `.opencode/commands/ant/`.
  Those directories are read-only inputs to this plan's invariant test.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the false consolidation-runs claims in all three documents</name>
  <read_first>
    - `CLAUDE.md` lines 30-50 (the Definition of Done section, including line 37's honest statement and line 44's note that three files described phase-end consolidation running while it never ran) and lines 823-845 (the Structural Learning Stack section containing line 838)
    - `AGENTS.md` lines 885-905 — the parallel Key additions list containing line 899, and the header block carrying the Last Updated date
    - `.aether/docs/structural-learning-stack.md` lines 1-40 (intro plus line 14) and lines 198-235 (the Consolidation Pipeline section and the Integration Points table)
    - `cmd/graph_consolidation_cmds.go` lines 200-215, 265-285 and 370-382 — proof that both commands exist as registered CLI subcommands with no caller, which is what the corrected text must describe
  </read_first>
  <files>CLAUDE.md, AGENTS.md, .aether/docs/structural-learning-stack.md</files>
  <action>
Rewrite each stale claim so it states what is true today: the consolidation
subcommands exist and work when run manually, and no lifecycle command invokes
them. Correct BOTH the phase-end and the seal claim — planning verified neither
has a caller, so correcting only phase-end would leave the same defect in place.

CLAUDE.md line 838: replace the `Lifecycle integration` bullet with a bullet
stating that `consolidation-phase-end` and `consolidation-seal` are available as
CLI subcommands and that no lifecycle command invokes them yet, naming Phase 162
as where that wiring lands. Keep the bullet in the same list position and the same
single-line format. Do not touch CLAUDE.md lines 37 or 43-44 — those are already
honest and are the historical record this correction answers to.

AGENTS.md line 899: apply the same correction in AGENTS.md's own phrasing (it uses
`aether continue` and `aether seal` rather than slash-command names). AGENTS.md's
header shows a Last Updated date of 2026-05-20; update that date as part of this edit.

.aether/docs/structural-learning-stack.md:
- Line 14: replace the sentence claiming the stack runs automatically at phase-end
  and seal with one stating the stack is invoked through the
  `consolidation-phase-end` and `consolidation-seal` subcommands, which no
  lifecycle command calls yet, and that workers never call it directly.
- Line 207: replace the `Runs at the end of every phase` assertion with text
  describing it as the lightweight mode intended for phase end, currently invoked
  manually. Keep the factual description of what it executes (nurse then herald
  then janitor, the `consolidation.phase_end` event, non-blocking steps).
- Line 211: apply the same change to the `Runs once during /ant-seal` assertion —
  keep the five-step list, correct only the "runs during" claim.
- The Integration Points table around lines 224-231: change the Trigger column
  values for the two consolidation rows from `/ant-continue` and `/ant-seal` to the
  manual subcommand invocation, or add an explicit Status column marking those two
  rows as not yet wired with a Phase 162 reference. Leave the other rows
  (`/ant-build` to `learning-observe`, `colony-prime` to `instinct-read-trusted`)
  unchanged — those are separate claims not in scope here.
- Add a short note near the top of the Consolidation Pipeline section stating the
  wiring is Phase 162's scope, so a reader knows the gap is tracked rather than
  merely undocumented.

Do not soften the correction into "may run" or "can run at phase end". Ambiguous
phrasing is what allowed this claim to survive four milestones. State plainly that
nothing calls them today.
  </action>
  <verify>
    <automated>go test ./cmd -run TestDocsDoNotClaimConsolidationRunsToday -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF 'Lifecycle integration: phase-end at /ant-continue' CLAUDE.md` returns 0
    - `grep -cF 'Lifecycle integration: phase-end at' AGENTS.md` returns 0
    - `grep -cF 'The stack runs automatically at phase-end and seal' .aether/docs/structural-learning-stack.md` returns 0
    - `grep -cF 'Runs at the end of every phase' .aether/docs/structural-learning-stack.md` returns 0
    - `grep -cF 'Runs once during' .aether/docs/structural-learning-stack.md` returns 0
    - Each of the three files contains a replacement sentence naming Phase 162 as the wiring owner (`grep -cF 'Phase 162' CLAUDE.md AGENTS.md .aether/docs/structural-learning-stack.md` returns a non-zero count for each)
    - CLAUDE.md line 37's sentence about `consolidation-phase-end` and `consolidation-seal` having no caller is still present and unmodified
    - `git diff --stat -- .claude/commands/ant/ .opencode/commands/ant/` is empty
  </acceptance_criteria>
  <done>Reading any of the three documents tells a person that consolidation is available as a manual subcommand and is not yet invoked by any lifecycle command — matching what the code does.</done>
</task>

<task type="auto">
  <name>Task 2: Add the static-doc test that fails if any consolidation-runs claim returns</name>
  <read_first>
    - `cmd/claudemd_verification_depth_test.go` — the exact structural analog; `TestCLAUDEMDNoOldModes` (lines 77-91) is the "must NOT contain X" half to imitate, including its failure-message style naming the file and the exact stale phrase
    - `CLAUDE.md`, `AGENTS.md`, `.aether/docs/structural-learning-stack.md` as corrected by Task 1
    - `cmd/command_source_hygiene_test.go` lines 155-195 — `repoRootForCommandSourceTest()`, in case relative `../` paths prove insufficient for reaching `.aether/docs/`
  </read_first>
  <files>cmd/doc_consolidation_claims_test.go</files>
  <action>
Create `cmd/doc_consolidation_claims_test.go` in package `cmd` with a test named
`TestDocsDoNotClaimConsolidationRunsToday`.

Drive the test from a table of `{file, forbiddenPhrase, why}` entries covering all
six located claims (CLAUDE.md's Lifecycle integration bullet, AGENTS.md's
equivalent, and structural-learning-stack.md's four: the line-14 sentence, the
phase-end "runs at the end of every phase" assertion, the seal "runs once during"
assertion, and the Integration Points table trigger cells). Read each file once
with `os.ReadFile` and assert with `strings.Contains` that no forbidden phrase is
present.

Choose forbidden phrases that are specific enough not to trip on legitimate text.
In particular the phrase list must NOT match CLAUDE.md line 37 or line 44, which
legitimately mention `consolidation-phase-end` and describe the historical false
claim. Verify this by running the test against the corrected files and confirming
it passes; if a phrase is too broad, narrow it rather than adding an exclusion.

Add a second, stronger assertion that does not depend on the exact wording: for
each of the three files, assert that the file does not contain a line matching a
compiled regexp of the shape "(runs|executes|invoked) ... at (the end of )?(every )?phase" combined
with a consolidation identifier on the same line. This is the invariant half —
it catches a rephrased reintroduction that an exact-string list would miss. Tune
the regexp against the corrected files so it does not fire on the replacement text.

Failure messages must name the file, the offending line number, and the offending
text. Add a doc comment recording why this test exists: CLAUDE.md's Definition of
Done corollary that a documentation claim about runtime behaviour must be testable
or removed, and the fact that this specific claim survived four milestones.

Add a third assertion that acts as the trip-wire for the opposite direction: if
`consolidation-phase-end` or `consolidation-seal` ever gains a real caller in
non-test Go code under `cmd/` or `pkg/`, this test should fail with a message
telling the developer to update the documentation (Phase 162 will hit this
deliberately). Implement it by scanning non-test `.go` files under `../cmd` and
`../pkg` for the literal subcommand strings appearing outside
`cmd/graph_consolidation_cmds.go` and outside comments, and failing if found.
Exclude comment-only lines from the count so header prose cannot self-invalidate
the check.
  </action>
  <verify>
    <automated>go test ./cmd -run TestDocsDoNotClaimConsolidationRunsToday -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd -run TestDocsDoNotClaimConsolidationRunsToday -count=1` exits 0
    - Re-inserting the exact string `Lifecycle integration: phase-end at /ant-continue, full at /ant-seal` into CLAUDE.md makes the same command exit non-zero with a message naming `CLAUDE.md` and the line number (executor performs this check, reverts, and quotes the observed failure message in the summary)
    - The regexp-based assertion also fires on a paraphrase: temporarily inserting `consolidation-phase-end executes at the end of every phase` into `.aether/docs/structural-learning-stack.md` fails the test (executor performs, reverts, and reports)
    - The test passes against the corrected files with no exclusion list carved out for CLAUDE.md lines 37 or 44
    - `go vet ./cmd` exits 0
  </acceptance_criteria>
  <done>The claim cannot silently return: a `go test` command fails on both the exact phrase and a paraphrase of it, and warns when a real caller appears so the docs get updated together.</done>
</task>

<task type="auto">
  <name>Task 3: Pin live-wrapper stderr suppression with a count invariant</name>
  <read_first>
    - `cmd/codex_build_test.go` lines 3182-3221 — `TestBuildWorkerBriefIsMostlyTask`, the proportion/invariant testing style: assert a measured value, and put the measured value in the failure message
    - `cmd/cli_flag_audit_test.go` lines 71-83 — the `os.ReadDir` plus `.md` filter plus `os.ReadFile` directory-walking style to reuse
    - `.claude/commands/ant/archaeology.md` lines 180-195 and `.claude/commands/ant/dream.md` line 56 — the four Claude-side suppression sites, to confirm each is a grep fallback or prose git call and not a load-bearing `aether` invocation
    - `.opencode/commands/ant/archaeology.md` and `.opencode/commands/ant/dream.md` — the mirrored sites
    - `cmd/command_source_hygiene_test.go` lines 155-195 — `repoRootForCommandSourceTest()` for resolving the two wrapper directories
  </read_first>
  <files>cmd/live_wrapper_stderr_test.go</files>
  <action>
Create `cmd/live_wrapper_stderr_test.go` in package `cmd` with a test named
`TestLiveWrapperStderrSuppressionCount`.

The test walks every `.md` file under `.claude/commands/ant/` and
`.opencode/commands/ant/` (resolved via `repoRootForCommandSourceTest()`), counts
total occurrences of the stderr-suppression token across all files in both
directories, and asserts the total equals the allowed constant. Read the live
files first and set the constant to the count you actually measure; planning
measured 8 (4 per platform mirror). Declare it as a named constant with a comment
explaining that every counted site is a `grep` fallback or a prose `git diff`, not
an `aether` invocation, and that the number is a ceiling on known-benign
suppression rather than a target.

Build the suppression token from parts at runtime (for example by concatenating
the redirect operator with the device path) so the test file's own source does not
add an occurrence if anyone later widens the scan to include `cmd/`.

Add a second, sharper assertion that is independent of the count: for every
occurrence found, fail if the same line also contains the token `aether`. This is
the assertion that actually encodes LOUD-06's requirement — suppression on a
non-`aether` shell fallback is benign, suppression on an `aether` invocation is a
swallowed colony failure. A future contributor adding `aether foo 2>/dev/null` to a
live wrapper fails this assertion regardless of what the total count is.

The failure message for the count assertion must state both the expected constant
and the measured total, and list each file with its per-file count, following the
measured-value style of `TestBuildWorkerBriefIsMostlyTask`. A bare "count mismatch"
message does not satisfy the acceptance criteria.

Scope the walk to exactly those two directories. Do not scan
`.aether/docs/command-playbooks/`, `colony/`, or any agent directory.

Do not modify any wrapper file in this task.
  </action>
  <verify>
    <automated>go test ./cmd -run TestLiveWrapperStderrSuppressionCount -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd -run TestLiveWrapperStderrSuppressionCount -count=1` exits 0
    - The test declares a named constant for the allowed total with a comment explaining what each counted site is
    - Appending a line containing an `aether` call with stderr suppression to `.claude/commands/ant/status.md` makes the same command exit non-zero, and the failure message names `status.md`, the line number, and states that suppression on an `aether` invocation is forbidden (executor performs this check, reverts, and quotes the observed message in the summary)
    - Appending a benign extra `grep` fallback with suppression also fails, with a message stating both the expected constant and the measured total (executor performs, reverts, reports)
    - `git diff --name-only` for this task lists only `cmd/live_wrapper_stderr_test.go`
    - `go vet ./cmd` exits 0
  </acceptance_criteria>
  <done>Any new stderr suppression in a live wrapper — and specifically any suppression on an `aether` call — fails a test, so a swallowed colony failure cannot be introduced unnoticed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrapper markdown → user's shell | Live wrapper files are literal prompt text an LLM executes as shell commands; suppressed stderr on an `aether` call hides colony failures from the operator |
| documentation → operator trust | A false runtime claim causes an operator to believe a safety-relevant subsystem (learning consolidation) is active when it is not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-01 | Information Disclosure | `check-antipattern` gate with no live caller | transfer | Owned by Plan 04 of this phase |
| T-160-02 | Information Disclosure | Worker debug artifacts | transfer | Owned by Plan 05 of this phase |
| T-160-07 | Spoofing | Documentation asserting a subsystem runs when it has no caller, causing an operator to trust an inactive control | mitigate | Task 1 corrects all six claims; Task 2's test fails on the exact phrase, on a paraphrase, and warns when a real caller appears without a doc update |
| T-160-08 | Denial of Service (of diagnostic signal) | A new `aether ... 2>/dev/null` in a live wrapper silently swallowing a colony failure | mitigate | Task 3 asserts no counted suppression site shares a line with the token `aether`, and pins the total count so any new site is surfaced |
| T-160-09 | Repudiation | Suppression count drifting upward one benign site at a time until a load-bearing one hides in the crowd | accept | The count invariant makes each addition a deliberate, reviewed constant change; a per-site allowlist was rejected because it would not catch a new site in a new file |
</threat_model>

<verification>
```bash
go test ./cmd -run TestDocsDoNotClaimConsolidationRunsToday -count=1
go test ./cmd -run TestLiveWrapperStderrSuppressionCount -count=1
go test ./cmd -run TestCLAUDEMDVerificationDepthClaims -count=1
go vet ./cmd
git diff --stat -- .claude/commands/ant/ .opencode/commands/ant/   # expect empty
git diff --stat -- .aether/docs/command-playbooks/                 # expect empty
```
</verification>

<success_criteria>
- All six located consolidation-runs claims are corrected across the three documents
- `TestDocsDoNotClaimConsolidationRunsToday` passes, and fails on both the exact phrase and a paraphrase
- `TestLiveWrapperStderrSuppressionCount` passes, and fails on a new suppression site and on any suppression sharing a line with `aether`
- No wrapper file and no command-playbook file was modified
- Existing `cmd` tests still pass (`go test ./cmd -count=1`)
</success_criteria>

<output>
After completion, create `.planning/phases/160-fail-loudly/160-03-SUMMARY.md`.
Record the measured suppression constant, the exact replacement wording used in
each of the three documents, and the observed failure messages from the deliberate
regression checks in the acceptance criteria — those messages are the evidence that
the commands actually fail when the requirement is unmet.
</output>
