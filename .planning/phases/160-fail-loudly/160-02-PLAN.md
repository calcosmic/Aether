---
phase: 160-fail-loudly
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - colony/playbooks/build.md
  - .aether/docs/command-playbooks/build-prep.md
  - .aether/docs/command-playbooks/build-context.md
  - .aether/docs/command-playbooks/build-wave.md
  - .aether/docs/command-playbooks/build-full.md
  - .aether/docs/command-playbooks/build-complete.md
  - .aether/docs/command-playbooks/continue-verify.md
  - cmd/survey_load_absence_test.go
autonomous: true
requirements: [LOUD-01, LOUD-03]
user_setup: []

must_haves:
  truths:
    - "No file in the repository instructs anyone to run `aether survey-load`, a command that does not exist"
    - "Every documented call to print-next-up, verify-claims, state-checkpoint, generate-progress-bar and skill-detect matches that command's actual cobra contract"
    - "A test fails if `survey-load` reappears as an instruction or as a registered command"
  artifacts:
    - path: "cmd/survey_load_absence_test.go"
      provides: "Invariant test that survey-load is neither registered nor referenced"
      contains: "TestSurveyLoadAbsentAndUncalled"
    - path: "colony/playbooks/build.md"
      provides: "Build playbook with the dead survey-load instruction removed"
  key_links:
    - from: "cmd/survey_load_absence_test.go"
      to: "rootCmd.Commands()"
      via: "assert no registered subcommand named survey-load"
      pattern: "survey-load"
    - from: ".aether/docs/command-playbooks/*.md"
      to: "cobra command contracts in cmd/"
      via: "call text rewritten to the flag form each command actually accepts"
      pattern: "aether (state-checkpoint|generate-progress-bar|skill-detect|verify-claims|print-next-up)"
---

<objective>
Correct the six confirmed-broken documented CLI calls so the documentation
describes commands that actually exist with the arguments they actually take.

Six call sites pass positional arguments to `cobra.NoArgs` commands, or name a
command (`survey-load`) that was deleted from the Go runtime entirely. All six
live in the DEAD documentation corpus (`.aether/docs/command-playbooks/` and
`colony/playbooks/`) — RESEARCH.md confirms three independent ways that nothing
loads these files since commit `b2b41486` deleted `playbook-loader.ts`. So this is
a documentation-correctness fix, not a runtime-behaviour fix, and the plan says so
out loud rather than letting a corrected doc be mistaken for a working feature.

Purpose: LOUD-01, LOUD-03. Also unblocks Plan 07's audit, which fails while these
call sites remain wrong.
Output: six corrected call sites, one invariant test for `survey-load`'s absence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-fail-loudly/160-RESEARCH.md
@.planning/phases/160-fail-loudly/160-PATTERNS.md
@CLAUDE.md

<interfaces>
Verified cobra contracts for the five commands being called incorrectly. The
call sites must conform to these; the Go commands must NOT change.

- `cmd/build_flow_cmds.go:219` — `print-next-up`, `Args: cobra.NoArgs`, takes no flags for state/phase/total (it reads colony state itself)
- `cmd/verify_claims.go:10` — `verify-claims`, `Args: cobra.NoArgs`, reads `COLONY_STATE.json` directly; it has no claims-file / watcher-json / exit-code parameters and never has
- `cmd/state_extra.go:14` — `state-checkpoint`, `Args: cobra.NoArgs`, checkpoint name comes from the required `--name` flag (`cmd/state_extra.go:259`)
- `cmd/generate_cmds.go:128` — `generate-progress-bar`, `Args: cobra.NoArgs`, flags `--current` (int), `--total` (int), `--width` (int, default 30) at `cmd/generate_cmds.go:247-249`
- `cmd/skills.go:233` — `skill-detect`, `Args: cobra.NoArgs`, resolves the hub path itself; it takes no directory argument
- `survey-load` — NOT REGISTERED anywhere in `cmd/`. There is no flag form and no positional form that works.
</interfaces>

**Scope boundaries — read before editing:**

1. **`build.md` ownership.** ROADMAP.md (line 49) makes Phase 165 the sole
   structural owner of `build.md`. This plan may make ONLY narrow, isolated
   call-argument fixes. Verification already performed during planning:
   `.claude/commands/ant/build.md` contains no broken calls, so this plan does
   not touch it at all. `colony/playbooks/build.md` is a different, dead third-copy
   file and is in scope for the one-line `survey-load` removal only. Do not
   restructure either file.

2. **Do not "fix" the Go commands.** RESEARCH.md Pitfall 3: the
   `verify-claims` call site implies an external-claims-comparison feature that
   was never built. The correct fix is the call site, not the command. Any diff to
   `cmd/verify_claims.go`, `cmd/skills.go`, `cmd/state_extra.go`,
   `cmd/generate_cmds.go`, or `cmd/build_flow_cmds.go` in this plan is a defect.

3. **Stderr suppression scope.** Three of the six lines being edited carry
   `2>/dev/null`. Remove the suppression on those specific lines only, since the
   line is being rewritten anyway and leaving it would re-hide the call we just
   fixed. The remaining ~176 `2>/dev/null` occurrences in
   `.aether/docs/command-playbooks/` are explicitly OUT OF SCOPE (RESEARCH.md
   Pitfall 2 — they are in files nothing executes; LOUD-06's behaviour fix is
   scoped to the live wrapper directories and is Plan 03's work).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the six broken call sites to match their real cobra contracts</name>
  <read_first>
    - `colony/playbooks/build.md` around line 45 — the `survey-load` instruction
    - `.aether/docs/command-playbooks/build-prep.md` around line 328 — `generate-progress-bar` call
    - `.aether/docs/command-playbooks/build-context.md` around line 220 — `skill-detect` call
    - `.aether/docs/command-playbooks/build-wave.md` around line 9 — `state-checkpoint` call
    - `.aether/docs/command-playbooks/build-full.md` around line 1697 and `.aether/docs/command-playbooks/build-complete.md` around line 403 — `print-next-up` calls
    - `.aether/docs/command-playbooks/continue-verify.md` around line 468 — `verify-claims` call
    - `cmd/build_flow_cmds.go` (lines 215-240), `cmd/verify_claims.go` (lines 1-30), `cmd/state_extra.go` (lines 10-40 and 255-265), `cmd/generate_cmds.go` (lines 125-150 and 243-252), `cmd/skills.go` (lines 230-250) — the authoritative contracts each call must conform to
    - `.planning/phases/160-fail-loudly/160-RESEARCH.md` § Common Pitfalls (Pitfalls 2 and 3)
  </read_first>
  <files>colony/playbooks/build.md, .aether/docs/command-playbooks/build-prep.md, .aether/docs/command-playbooks/build-context.md, .aether/docs/command-playbooks/build-wave.md, .aether/docs/command-playbooks/build-full.md, .aether/docs/command-playbooks/build-complete.md, .aether/docs/command-playbooks/continue-verify.md</files>
  <action>
Rewrite each of the six call sites to the exact form its command accepts. Change
only the invocation and the surrounding sentence where the old wording described
arguments that no longer appear. Do not restructure sections, do not renumber
steps, do not reflow unrelated prose.

1. `colony/playbooks/build.md` (~line 45): the instruction
   ``Run `aether survey-load "{phase_name}"` and load relevant survey documents
   based on phase type.`` names a command that does not exist in the Go runtime.
   Delete the `aether survey-load` invocation. Keep the surrounding intent
   (loading relevant survey documents by phase type) as prose that no longer
   instructs anyone to run a nonexistent command, and add a short parenthetical
   noting that no `survey-load` subcommand exists. Do not invent a substitute
   command.

2. `.aether/docs/command-playbooks/build-prep.md` (~line 328): change
   `aether generate-progress-bar "$current_phase" "$total_phases" 20` to the flag
   form `aether generate-progress-bar --current "$current_phase" --total "$total_phases" --width 20`.
   Remove the `2>/dev/null` from this line. Keep the `|| echo ""` fallback if the
   surrounding shell depends on a non-empty assignment.

3. `.aether/docs/command-playbooks/build-context.md` (~line 220): change
   `aether skill-detect "$(pwd)"` to `aether skill-detect` (the command resolves
   the hub path itself). Remove the `2>/dev/null` from this line.

4. `.aether/docs/command-playbooks/build-wave.md` (~line 9): change
   `aether state-checkpoint "pre-build-wave"` to
   `aether state-checkpoint --name "pre-build-wave"`. Remove the `2>/dev/null`;
   keep the existing `|| echo "Warning: ..." >&2` fallback, which is exactly the
   loud-warning-and-continue behaviour D-01 prescribes for an enrichment call.

5. `.aether/docs/command-playbooks/build-full.md` (~line 1697) and
   `.aether/docs/command-playbooks/build-complete.md` (~line 403): change
   `aether print-next-up "$state" "$current_phase" "$total_phases"` to
   `aether print-next-up` in both files. If the surrounding prose explains the
   three arguments, replace that explanation with a note that the command reads
   colony state itself.

6. `.aether/docs/command-playbooks/continue-verify.md` (~line 468): change
   `aether verify-claims ".aether/data/last-build-claims.json" "<watcher_json_or_path>" "<test_exit_code>"`
   to `aether verify-claims`. Rewrite the accompanying sentence to describe what
   the command actually does — reads `COLONY_STATE.json` and runs fixed internal
   consistency checks — instead of implying it compares an external claims file
   against watcher output and a test exit code. That comparison feature does not
   exist; describing it is the exact doc-drift class this phase exists to remove.

Additionally, add a short banner near the top of each edited file under
`.aether/docs/command-playbooks/` — one sentence — stating that the file is
reference documentation and is not loaded or executed by the runtime as of Phase
160, matching the statement already in CLAUDE.md's "Command Playbooks (Reference
Material)" section. Skip files that already carry such a banner.
  </action>
  <verify>
    <automated>! grep -rn 'aether survey-load' --include='*.md' --include='*.go' --include='*.yaml' . 2>/dev/null | grep -v '^\./\.planning' | grep -v '^\./control-ts' && ! grep -rn 'aether skill-detect "' .aether/docs/command-playbooks/ && ! grep -rn 'aether state-checkpoint "' .aether/docs/command-playbooks/ && ! grep -rn 'aether verify-claims "' .aether/docs/command-playbooks/ && ! grep -rnE 'aether print-next-up +"' .aether/docs/command-playbooks/ && echo CALLSITES_OK</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `CALLSITES_OK`
    - `grep -rn 'survey-load' colony/ .aether/ .claude/ .opencode/` returns no `aether survey-load` invocation
    - `grep -n 'generate-progress-bar' .aether/docs/command-playbooks/build-prep.md` shows `--current` and `--total` and `--width` on the same line
    - `grep -n 'state-checkpoint' .aether/docs/command-playbooks/build-wave.md` shows `--name "pre-build-wave"`
    - None of the six edited lines contains `2>/dev/null` (`grep -n '2>/dev/null' .aether/docs/command-playbooks/build-prep.md .aether/docs/command-playbooks/build-context.md .aether/docs/command-playbooks/build-wave.md` returns no line that also contains `aether generate-progress-bar`, `aether skill-detect`, or `aether state-checkpoint`)
    - `git diff --name-only` lists no file under `cmd/` for this task
    - `git diff --stat -- .claude/commands/ant/build.md` is empty (build.md ownership boundary honoured)
    - `git diff --stat -- .aether/docs/command-playbooks/` touches at most 6 files
  </acceptance_criteria>
  <done>Every documented invocation of the six commands would succeed if pasted into a terminal, and no file instructs anyone to run `aether survey-load`.</done>
</task>

<task type="auto">
  <name>Task 2: Pin survey-load's absence with an invariant test</name>
  <read_first>
    - `cmd/claudemd_verification_depth_test.go` — the Go static-assertion test idiom (read a repo file, assert a string is absent, name the file and phrase in the failure message)
    - `cmd/cli_flag_audit_test.go` (lines 40-90) — how a test builds the registered-subcommand set from `rootCmd.Commands()` and how it walks markdown directories with `os.ReadDir` / `os.ReadFile`
    - `cmd/command_source_hygiene_test.go` (lines 155-195) — `repoRootForCommandSourceTest()`, the defensive repo-root finder to reuse rather than re-deriving `../` paths
    - `colony/playbooks/build.md` — the file just corrected in Task 1
  </read_first>
  <files>cmd/survey_load_absence_test.go</files>
  <action>
Create `cmd/survey_load_absence_test.go` in package `cmd` with a test named
`TestSurveyLoadAbsentAndUncalled`.

The test asserts two independent facts and reports them separately:

1. **Not registered.** No command returned by `rootCmd.Commands()` has
   `Name() == "survey-load"`, and none lists `survey-load` among its `Aliases`.
   Failure message must state that `survey-load` is registered in the Go runtime,
   which contradicts LOUD-01's resolution.

2. **Not referenced.** Walk `.claude/commands/ant/`, `.opencode/commands/ant/`,
   `.aether/docs/command-playbooks/`, `.aether/commands/`, and `colony/playbooks/`
   (resolved from `repoRootForCommandSourceTest()`), read every `.md` and `.yaml`
   file, and assert none contains the string `aether survey-load`. Report every
   offending file and line number, not just the first. Do not scan `.planning/`
   or `control-ts/`, and exclude the test file itself from the walk so its own
   literal does not trip the assertion.

The failure message for the second assertion must name the file, the line number,
and the offending text, following the reporting style of `cmd/cli_flag_audit_test.go`.

Add a doc comment explaining that `survey-load` was resolved "done differently" —
the command was deleted rather than fixed — so this test is the command that
fails if it ever comes back as an instruction without a runtime implementation.

This test must not require a colony store, temp directory, or `rootCmd.Execute()`
call; it is a pure static/registry assertion.
  </action>
  <verify>
    <automated>go test ./cmd -run TestSurveyLoadAbsentAndUncalled -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd -run TestSurveyLoadAbsentAndUncalled -count=1` exits 0
    - Adding the line `Run \`aether survey-load "x"\`` to `colony/playbooks/build.md` makes the same command exit non-zero with a message naming `colony/playbooks/build.md` and its line number (executor performs this check, reverts, and reports the observed failure message in the summary)
    - The test file contains no `rootCmd.Execute()` call and no `newTestStore` usage
    - `go vet ./cmd` exits 0
  </acceptance_criteria>
  <done>A single `go test` command fails the moment `survey-load` reappears either as a registered command or as an instruction in any live or reference file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator | Corrected call text is what a human or an LLM orchestrator will copy and run; a wrong form here produces a failed command, not a security breach |
| markdown corpus → test process | `TestSurveyLoadAbsentAndUncalled` reads repository-controlled files only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-01 | Information Disclosure | `check-antipattern` gate with no live caller | transfer | Owned by Plan 04; this plan deliberately does NOT treat corrected `check-antipattern` doc text as satisfying it |
| T-160-02 | Information Disclosure | Worker debug artifacts | transfer | Owned by Plan 05 |
| T-160-05 | Spoofing | Corrected documentation being mistaken for restored runtime behaviour — a reader assumes `build-context.md` now runs because its calls are valid | mitigate | Every edited `.aether/docs/command-playbooks/` file gains a one-sentence banner stating it is reference documentation not loaded by the runtime; the plan text states the same boundary |
| T-160-06 | Denial of Service | A future contributor re-adding `aether survey-load` and the failure being swallowed again | mitigate | `TestSurveyLoadAbsentAndUncalled` fails on both re-registration and re-reference |
</threat_model>

<verification>
```bash
go test ./cmd -run TestSurveyLoadAbsentAndUncalled -count=1
go test ./cmd -run TestCLIFlagAudit -count=1
go vet ./cmd
grep -rn 'aether survey-load' colony/ .aether/ .claude/ .opencode/   # expect no results
git diff --stat -- .claude/commands/ant/build.md                      # expect empty
```
</verification>

<success_criteria>
- All six broken call sites conform to their commands' real cobra contracts
- No file in the repository instructs anyone to run `aether survey-load`
- `TestSurveyLoadAbsentAndUncalled` passes and fails when the reference is reintroduced
- Existing `TestCLIFlagAudit` still passes
- Zero files under `cmd/` other than the new test were modified
- `.claude/commands/ant/build.md` untouched (Phase 165 ownership boundary)
</success_criteria>

<output>
After completion, create `.planning/phases/160-fail-loudly/160-02-SUMMARY.md`.
Explicitly state that these fixes change documentation only and change nothing a
user observes running `/ant-build` or `/ant-continue` today, so the next plan's
audit is not mistaken for proof that the playbooks execute.
</output>
