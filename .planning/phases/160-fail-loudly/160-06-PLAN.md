---
phase: 160-fail-loudly
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified:
  - control-ts/
  - .aether/docs/retired-tests-ledger.md
  - cmd/retired_packages_test.go
autonomous: true
requirements: [RETIRE-01, RETIRE-02, RETIRE-03, RETIRE-04]
user_setup: []

must_haves:
  truths:
    - "control-ts/ no longer exists in the repository"
    - ".aether/ts-host/ and .aether/ts/ are byte-for-byte unchanged by this plan"
    - "go build, aether publish and aether integrity all still succeed after the deletion"
    - "Every test file removed with control-ts/ has a ledger entry stating whether its coverage survived"
    - "A test fails if control-ts/ reappears or if either kept TypeScript package loses its embed directive"
  artifacts:
    - path: "cmd/retired_packages_test.go"
      provides: "Invariant test binding control-ts absence, ts-host/ts embed survival, and ledger completeness"
      contains: "TestRetiredPackagesStayRetired"
    - path: ".aether/docs/retired-tests-ledger.md"
      provides: "Ledger extended with one entry per removed control-ts test file"
      contains: "control-ts/tests"
  key_links:
    - from: "cmd/retired_packages_test.go"
      to: "embedded_assets.go"
      via: "assert the go:embed directives for .aether/ts-host and .aether/ts are still present"
      pattern: "go:embed"
    - from: "cmd/retired_packages_test.go"
      to: ".aether/docs/retired-tests-ledger.md"
      via: "assert every path listed in the ledger is absent from the working tree and every disposition is well-formed"
      pattern: "Disposition"
---

<objective>
Delete `control-ts/` — the one component confirmed genuinely dead, self-described
in its own `package.json` as a "Retired experimental Aether control-plane
prototype" — and prove by test that the two TypeScript packages that must survive
did survive.

ROADMAP and CONTEXT.md both require this as a standalone commit. RESEARCH.md
verified zero Go references, zero CI references, zero embed references and zero
goreleaser references. RETIRE-04's replacement schema test landed in Plan 01, which
this plan depends on, so no coverage is lost at the moment of deletion.

The kept packages are load-bearing in a way that fails loudly if ignored:
`/embedded_assets.go` line 13 embeds `.aether/ts-host/dist` plus its
`package.json`/`package-lock.json`, and line 12 embeds `.aether/ts/` at source
level. Deleting either breaks `go build` outright and hard-fails `aether publish`
and `aether integrity`. Note the path correction: the embed directive is at the
REPO ROOT `/embedded_assets.go`, not `cmd/embedded_assets.go`, which does not
exist — CONTEXT.md's canonical_refs has this wrong.

Purpose: RETIRE-01, RETIRE-02, RETIRE-03, RETIRE-04, and ROADMAP success criteria
6 and 7.
Output: `control-ts/` gone, ledger complete, one invariant test guarding all three.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-fail-loudly/160-RESEARCH.md
@.planning/phases/160-fail-loudly/160-PATTERNS.md
@.planning/phases/160-fail-loudly/160-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Verified during planning.

Files to delete: everything under `control-ts/`. 130 git-tracked files.
The 16 non-`node_modules` test files that need ledger entries:
- control-ts/tests/adapters/adapters.test.ts
- control-ts/tests/agents/loadAgents.test.ts
- control-ts/tests/control/integration.test.ts
- control-ts/tests/events/bridge.test.ts
- control-ts/tests/events/events.test.ts
- control-ts/tests/memory/store.test.ts
- control-ts/tests/oracle/oracle.test.ts
- control-ts/tests/orchestrator/executePlan.test.ts
- control-ts/tests/orchestrator/runPhase.test.ts
- control-ts/tests/phases/loadPhases.test.ts
- control-ts/tests/prompts/assemblePrompt.test.ts
- control-ts/tests/schemas/agent.schema.test.ts
- control-ts/tests/schemas/event.schema.test.ts
- control-ts/tests/schemas/phase.schema.test.ts
- control-ts/tests/schemas/policy.schema.test.ts  (already ledgered in Plan 01 as recovered-by:cmd/policy_schema_test.go)
- control-ts/tests/skills/loadSkills.test.ts

Files that MUST NOT change:
- `/embedded_assets.go` line 12 — the `.aether/ts/` embed
- `/embedded_assets.go` line 13 — `all:.aether/ts-host/dist`, `package.json`, `package-lock.json`
- everything under `.aether/ts-host/` and `.aether/ts/`

Verification commands available: `go build ./cmd/aether`, `go vet ./...`,
`go test ./... -count=1`, `aether integrity`, `aether version --check`,
`goreleaser check`, `git diff --stat -- .aether/ts-host .aether/ts`.
</interfaces>

**Scope boundaries:**
- This must be a STANDALONE commit containing only the deletion, the ledger
  entries, and the new invariant test. Do not fold any other change into it.
- Do not delete, edit, rename or reformat anything under `.aether/ts-host/` or
  `.aether/ts/`, including their `dist/` output. Do not run
  `npm --prefix .aether/ts-host run build` in this plan — nothing here changes
  `.aether/ts-host/src/*.ts`, so a rebuild would produce unrelated `dist/` churn
  that this plan's own `git diff` check is designed to catch.
- Do not remove `control-ts` references from historical files (CHANGELOG.md,
  `.planning/` documents, `.aether/QUEEN.md`). Those are records of what happened,
  not live references.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record every control-ts test in the ledger before deleting anything</name>
  <read_first>
    - `.aether/docs/retired-tests-ledger.md` as created by Plan 01 — the entry format and the two existing entries; match them exactly
    - `control-ts/package.json` — the `description` field ("Retired experimental Aether control-plane prototype (non-production, fail-closed)") and the `scripts` block, which names which test groups existed
    - Each of the 16 test files under `control-ts/tests/` — read enough of each to write an accurate one-or-two-sentence "What it covered" field; a generic "tested the control plane" entry does not satisfy RETIRE-04
    - `.planning/REQUIREMENTS.md` line 166 (RETIRE-04) — the requirement being satisfied
  </read_first>
  <files>.aether/docs/retired-tests-ledger.md</files>
  <action>
Append one ledger entry per removed `control-ts` test file, using the exact
four-field format Plan 01 established: `**Original path:**`,
`**What it covered:**`, `**Disposition:**`, `**Removed in:**`.

`control-ts/tests/schemas/policy.schema.test.ts` already has an entry from Plan 01
with disposition `recovered-by:cmd/policy_schema_test.go` — update its
`**Removed in:**` field to name this plan's commit rather than duplicating the
entry.

For each of the other 15, read the test file, state concretely what behaviour it
asserted, and assign a disposition:
- `recovered-by:<path>` only where a specific surviving Go test genuinely covers
  the same behaviour. Name the surviving test file. Do not assert recovery you have
  not verified by reading the surviving test.
- `dead-with-no-replacement` where the feature under test no longer exists in the
  system at all (the TS control plane's orchestrator, phase loader, prompt
  assembler, event bridge and adapters are the expected cases here). State in
  "What it covered" that the subject itself was removed, so a future reader knows
  the gap is intentional rather than an oversight.

If any of the 15 turns out to cover behaviour that DOES still exist in Go with no
equivalent test, do not paper over it: give it disposition
`dead-with-no-replacement`, add an explicit note naming the surviving Go code that
is now untested, and record the finding prominently in the plan summary. That is a
real coverage gap the next milestone needs to know about, and burying it would
repeat exactly the failure CLAUDE.md's Definition of Done section documents.

Add a short header line above the appended entries noting they were all removed
together as part of RETIRE-01, so the batch is legible as one event.

Do NOT create entries for anything under `control-ts/node_modules/`.
  </action>
  <verify>
    <automated>test "$(grep -c '^### ' .aether/docs/retired-tests-ledger.md)" -eq 17 && test "$(grep -c '\*\*Disposition:\*\*' .aether/docs/retired-tests-ledger.md)" -eq 17 && ! grep -q 'node_modules' .aether/docs/retired-tests-ledger.md && echo LEDGER_COMPLETE</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `LEDGER_COMPLETE`
    - The ledger contains exactly 17 entries: the 16 control-ts test files plus the retroactive `playbook-loader.test.ts` entry from Plan 01
    - Every `**Disposition:**` value begins with `dead-with-no-replacement` or `recovered-by:`
    - Every `recovered-by:` value names a file path that exists in the working tree (`for p in $(grep -o 'recovered-by:[^ ]*' .aether/docs/retired-tests-ledger.md | cut -d: -f2); do test -f "$p" || echo "MISSING $p"; done` prints nothing)
    - No `**What it covered:**` field is shorter than 40 characters
    - `control-ts/` still exists at the end of this task — the ledger lands first, by design
  </acceptance_criteria>
  <done>Before a single file is deleted, a reader can see for all 16 removed tests what they covered and whether that coverage survived.</done>
</task>

<task type="auto">
  <name>Task 2: Delete control-ts/ and prove the build, publish and integrity chain still holds</name>
  <read_first>
    - `/embedded_assets.go` in full — the embed directives at lines 12 and 13, so the executor can see exactly what must survive
    - `control-ts/package.json` — confirm the retired-prototype description before deleting
    - `.github/workflows/` — scan for any reference to `control-ts` before deleting; RESEARCH.md verified there are none, but confirm rather than assume
    - `.goreleaser.yaml` (or `.goreleaser.yml`) — same confirmation
    - `.aether/docs/retired-tests-ledger.md` as completed in Task 1
  </read_first>
  <files>control-ts/</files>
  <action>
First, re-verify the deletion is safe. Run repo-wide searches for the string
`control-ts` across Go source, CI workflow files, the goreleaser config, the embed
file, `.aether/commands/`, `.claude/`, `.opencode/`, and `.codex/`. The only
matches permitted are historical records: `CHANGELOG.md`, `.planning/` documents,
`.aether/QUEEN.md`, and the new ledger. If any live reference turns up, STOP and
report it in the summary instead of deleting — RESEARCH.md's verification would
then be stale and the deletion is no longer standalone-safe.

Then delete the entire `control-ts/` directory with `git rm -r control-ts`.

Then run the full verification chain and record each result in the summary:
- `go build ./cmd/aether`
- `go vet ./...`
- `go test ./... -count=1`
- `git diff --stat -- .aether/ts-host .aether/ts` — must be empty
- `goreleaser check`
- `aether integrity`
- `aether version --check`

`aether integrity` and `aether version --check` exercise the publish chain and are
the commands ROADMAP success criterion 6 names. If either fails, do not proceed to
commit: report the failure, since a failing integrity check after this deletion
would mean RESEARCH.md's "zero publish references" finding was wrong.

Commit this as a standalone commit containing only the `control-ts/` deletion, the
ledger changes from Task 1, and Task 3's test file. No other change may ride along.
  </action>
  <verify>
    <automated>test ! -d control-ts && go build ./cmd/aether && go vet ./... && test -z "$(git diff --stat -- .aether/ts-host .aether/ts)" && echo DELETION_SAFE</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d control-ts` succeeds
    - `git ls-files control-ts | wc -l` returns 0
    - `go build ./cmd/aether` exits 0
    - `go vet ./...` exits 0
    - `go test ./... -count=1` exits 0
    - `git diff --stat -- .aether/ts-host .aether/ts` produces no output
    - `goreleaser check` exits 0
    - `aether integrity` exits 0
    - `aether version --check` exits 0
    - `grep -rn 'control-ts' --include='*.go' --include='*.yml' --include='*.yaml' . | grep -v '^\./\.planning'` returns no results
    - The commit for this task touches only `control-ts/**`, `.aether/docs/retired-tests-ledger.md`, and `cmd/retired_packages_test.go`
  </acceptance_criteria>
  <done>The retired prototype is gone, and every build, vet, test, publish-chain and integrity command still passes with the two kept TypeScript packages untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Add the invariant test that keeps retired packages retired and kept packages kept</name>
  <read_first>
    - `/embedded_assets.go` — the exact embed directive text the test will assert is still present
    - `cmd/command_source_hygiene_test.go` lines 155-195 — `repoRootForCommandSourceTest()`, the repo-root resolver to reuse for reaching `/embedded_assets.go` and `.aether/docs/`
    - `.aether/docs/retired-tests-ledger.md` as completed in Task 1 — the format the test parses
    - `cmd/claudemd_verification_depth_test.go` — the file-read-and-assert idiom and failure-message style
  </read_first>
  <files>cmd/retired_packages_test.go</files>
  <action>
Create `cmd/retired_packages_test.go` in package `cmd` with a test named
`TestRetiredPackagesStayRetired` covering three independent assertions, each
reported separately so a failure names which one broke.

1. **control-ts stays deleted.** Assert no directory named `control-ts` exists at
   the repo root, and that no Go, YAML, or workflow file outside `.planning/` and
   `CHANGELOG.md` contains the string `control-ts`. Exclude comment-only lines from
   the source scan so a historical note in a comment cannot self-invalidate the
   check.

2. **The kept packages keep their embeds.** Read `/embedded_assets.go` from the repo
   root (not `cmd/embedded_assets.go`, which does not exist — this path error is
   recorded in RESEARCH.md Pitfall 4) and assert both embed directives are present:
   one covering `.aether/ts/` and one covering `.aether/ts-host/dist` along with its
   `package.json` and `package-lock.json`. Also assert both directories exist on
   disk and are non-empty. The failure message must state that deleting either
   breaks `go build`, `aether publish` and `aether integrity`, so a future
   contributor reading the failure understands the stakes rather than deleting the
   assertion.

3. **The ledger stays honest.** Parse `.aether/docs/retired-tests-ledger.md` and
   assert, for every entry: the `**Disposition:**` value starts with either
   `dead-with-no-replacement` or `recovered-by:`; every `recovered-by:` path exists
   in the working tree; and every `**Original path:**` does NOT exist in the working
   tree. That last assertion is the one that matters — it fails if someone ledgers a
   file they did not actually remove, or restores a file without updating the
   ledger.

Add a doc comment explaining that this test exists because RETIRE-02 and RETIRE-03
are "do not delete" guarantees with no other enforcement, and that RETIRE-04 is
otherwise a documentation claim with no executable check — both of which CLAUDE.md's
Definition of Done rejects.

The test must not shell out, must not require a colony store, and must not depend
on network access.
  </action>
  <verify>
    <automated>go test ./cmd -run TestRetiredPackagesStayRetired -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd -run TestRetiredPackagesStayRetired -count=1` exits 0
    - Creating an empty `control-ts/` directory makes the same command exit non-zero naming assertion 1 (executor performs, removes the directory, and quotes the observed message in the summary)
    - Temporarily commenting out the `.aether/ts-host` embed directive in `/embedded_assets.go` makes the command exit non-zero naming assertion 2 with the `go build` / `aether publish` / `aether integrity` consequence text (executor performs, reverts, reports)
    - Adding a ledger entry whose `**Original path:**` points at a file that still exists makes the command exit non-zero naming assertion 3 (executor performs, reverts, reports)
    - The test contains no `exec.Command` call and no `newTestStore` usage
    - `go vet ./cmd` exits 0
  </acceptance_criteria>
  <done>Three "do not regress" guarantees that previously existed only as prose now fail a `go test` command when violated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository tree → build and publish pipeline | Deleting an embedded directory breaks `go build` at compile time and `aether publish` at distribution time; this plan crosses that boundary deliberately for one directory and must not for two others |
| ledger claim → actual working tree | A ledger entry is a claim about what was removed; the invariant test is what makes the claim checkable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-18 | Denial of Service | Deleting `.aether/ts-host/` or `.aether/ts/` by association, breaking `go build`, `aether publish` and `aether integrity` | mitigate | `git diff --stat -- .aether/ts-host .aether/ts` must be empty in Task 2's acceptance criteria, and `TestRetiredPackagesStayRetired` assertion 2 fails if either embed directive or directory disappears |
| T-160-19 | Repudiation | Losing the only schema validation of `colony/policies/*.yaml` silently, while `MODEL-01` in Phase 161 is about to make `model-routing.yaml` load-bearing for dispatch | mitigate | Plan 01 (this plan's dependency) landed `cmd/policy_schema_test.go` first; the ledger records the recovery and assertion 3 verifies the named replacement exists |
| T-160-20 | Tampering | A removed test being ledgered as `recovered-by:` a test that does not actually cover the same behaviour | accept | The executor is instructed to read each surviving test before claiming recovery and to prefer `dead-with-no-replacement` when unsure; the invariant test can verify the replacement file exists but cannot verify semantic equivalence. Recorded rather than silently assumed |
| T-160-01 | Information Disclosure | `check-antipattern` gate with no live caller | transfer | Owned by Plan 04 of this phase |
| T-160-02 | Information Disclosure | Worker debug artifacts | transfer | Owned by Plan 05 of this phase |
</threat_model>

<verification>
```bash
test ! -d control-ts
go build ./cmd/aether
go vet ./...
go test ./... -count=1
go test ./cmd -run TestRetiredPackagesStayRetired -count=1
git diff --stat -- .aether/ts-host .aether/ts    # expect empty
goreleaser check
aether integrity
aether version --check
```
</verification>

<success_criteria>
- `control-ts/` no longer exists and is not referenced by any Go, CI, embed or goreleaser file
- `.aether/ts-host/` and `.aether/ts/` are byte-for-byte unchanged
- `go build`, `go vet`, `go test ./...`, `goreleaser check`, `aether integrity` and `aether version --check` all pass after the deletion
- The ledger holds 17 entries with well-formed dispositions, and every `recovered-by:` target exists
- `TestRetiredPackagesStayRetired` passes and fails on each of the three deliberate regressions
- The deletion landed as a standalone commit
</success_criteria>

<output>
After completion, create `.planning/phases/160-fail-loudly/160-06-SUMMARY.md`.
Record: the pre-deletion reference scan results, the exit status of every command
in the verification chain, the disposition breakdown across the 17 ledger entries,
and — prominently — any test whose subject still exists in Go with no equivalent
coverage. That last item is a real gap the next milestone must see.
</output>
