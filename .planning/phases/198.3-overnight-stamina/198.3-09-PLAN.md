---
phase: 198.3-overnight-stamina
plan: 09
type: execute
wave: 6
depends_on: ["198.3-08"]
files_modified:
  - cmd/recovery_classify.go
  - cmd/recovery_classify_test.go
  - cmd/medic_auto_spawn.go
  - cmd/medic_auto_spawn_test.go
  - cmd/command_call_audit_test.go
  - cmd/testdata/command_catalog.json
  - cmd/testdata/parity_snapshot.json
  - cmd/testdata/regression_snapshot.json
  - cmd/testdata/orphan_allowlist.json
autonomous: true
requirements: []
audit_scope: [orphan-recovery-medic-commands, command-surface-ratchets]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The failure-classify, recovery-log-read/write, and medic-auto-spawn-check public adapters are removed only after plan 05 proves run/build/continue call their internal helpers directly."
    - "Classifier, recovery-log, recovery-budget, and Medic eligibility functions remain live for run/build/continue/Queen; no capability or data file is deleted."
    - "The command catalog, platform parity snapshot, command-count regression snapshot, and current orphan allowlist receive the second staged shrink for this plan's four removals, leaving the final inventory eleven below the pre-phase surface."
    - "Generated diffs remove exactly the audited names/count and preserve unrelated command metadata."
    - "The historical orphan baseline is byte-identical; live allowlists only shrink."
  artifacts:
    - internal-only recovery classifier/log helpers
    - internal-only Medic eligibility helper
    - regenerated command catalog/parity/regression/current orphan fixtures
  key_links:
    - "plan 05 report stop -> internal classifier/log/Medic functions -> retained recovery behavior"
    - "plan 08 seven-name fixture shrink + plan 09 four removals -> final root runtime catalog -> second four-name fixture shrink"
  prohibitions:
    - statement: "Do not delete internal recovery state types/functions or their live callers."
      status: required
      verification: focused report/build/continue/Queen tests and production caller search
    - statement: "Do not modify orphan_allowlist_baseline.json or hand-edit generated JSON."
      status: required
      verification: owning generator flags, exact diff inspection, and baseline git-diff assertion
---

<objective>
Remove the remaining orphan recovery/Medic command adapters and apply the second staged command-surface update, completing the eleven-command shrink.

Purpose: useful recovery behavior now appears in the supported overnight report. These public helpers are dead alternate entrypoints; their internal functions are still valuable.

Output: internal-only recovery capability plus final honest catalog/parity/regression/orphan fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-RESEARCH.md
@.planning/phases/198.3-overnight-stamina/198.3-PATTERNS.md
@.planning/phases/198.3-overnight-stamina/198.3-05-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete recovery classifier/log Cobra adapters while preserving direct helper callers</name>
  <files>cmd/recovery_classify.go, cmd/recovery_classify_test.go</files>
  <read_first>
    - cmd/recovery_classify.go in full, separating types/read-write/classification helpers from `failure-classify`, `recovery-log-read`, and `recovery-log-write` declarations/flags/formatters
    - cmd/recovery_classify_test.go command-specific tests versus direct function tests
    - cmd/autopilot_recovery_198_3_test.go from plan 05
    - cmd/codex_build_finalize.go, Queen recovery files, and other production callers of recovery classification/log helpers
    - plan 08 summary confirming the seven autopilot removals awaiting one fixture refresh
  </read_first>
  <behavior>
    - Runtime registers none of `failure-classify`, `recovery-log-read`, or `recovery-log-write`.
    - Genuine run/build/continue failures still classify and persist/read the same phase recovery-log schema through direct calls.
    - Recovery budget and Queen recovery behavior remain unchanged.
    - Tests call functions directly; no internal code invokes a Cobra command as an API.
  </behavior>
  <action>
    Run a production caller search and focused plan 05/build/continue/Queen recovery tests first. Separate any formatting/imports used only by Cobra adapters from reusable types/functions. Remove the three command declarations, flags, and registration without changing file formats, classifier categories, idempotency, or budget behavior.

    Refactor `cmd/recovery_classify_test.go`: retain direct classification and recovery-log round-trip cases, remove command-envelope tests, and add a runtime-catalog/caller assertion that the three names are absent while direct production callers remain. Do not regenerate command fixtures yet; Task 2 owns one complete refresh.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestRun.*Recovery|TestFailureClassif|TestRecoveryLog|TestQueen.*Recovery|TestRecoveryBudget' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Production search finds none of the three Cobra declarations/registrations and finds direct helper callers.
    - Classification, recovery-log round trip/idempotency, budget, run report, and Queen recovery tests pass.
    - Recovery-log JSON shape is unchanged.
    - No generated fixture or baseline is modified in Task 1.
  </acceptance_criteria>
  <reversibility rating="costly">Dead public adapters are removed after caller proof; internal data/functions remain stable.</reversibility>
  <done>Recovery classification and logs are internal capabilities reached through supported workflows, not orphan commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete the Medic adapter and regenerate all command-surface ratchets once</name>
  <files>cmd/medic_auto_spawn.go, cmd/medic_auto_spawn_test.go, cmd/command_call_audit_test.go, cmd/testdata/command_catalog.json, cmd/testdata/parity_snapshot.json, cmd/testdata/regression_snapshot.json, cmd/testdata/orphan_allowlist.json</files>
  <read_first>
    - cmd/medic_auto_spawn.go eligibility/budget helper versus `medic-auto-spawn-check` declaration
    - cmd/medic_auto_spawn_test.go direct eligibility versus command-envelope tests
    - cmd/audit_catalog_test.go `-update-golden`
    - cmd/parity_test.go `TestPlatformParityGolden` and its `-update-golden`
    - cmd/regression_test.go `TestRegressionSnapshotUpdate` and its `-update-golden`
    - cmd/subcommand_reachability_ratchet_test.go `-update-orphan-allowlist` and the explicit live-only writer
    - cmd/command_call_audit_test.go reviewed command exceptions; remove the Medic entry rather than preserving a name that no longer exists
    - cmd/testdata/orphan_allowlist_baseline.json shrink-only policy (read only)
  </read_first>
  <behavior>
    - Runtime registers no `medic-auto-spawn-check`, while genuine-stop reporting still calculates eligibility and only recommends `aether medic --deep`.
    - No Medic worker is automatically spawned by run.
    - Catalog/parity/regression/live orphan fixtures retain plan 08's seven-name shrink and add this plan's four-name shrink.
    - No unrelated command, flag, classification, history field, or snapshot count changes.
  </behavior>
  <action>
    Remove only the Medic Cobra declaration/flags/registration and command-only rendering; retain eligibility/budget functions. Refactor tests to call eligibility directly, keep plan 05's zero-dispatch proof, and remove the deleted Medic command from `command_call_audit_test.go`'s reviewed exception set.

    Run focused lifecycle/recovery tests, then regenerate each owned fixture through its test mechanism: `TestAuditCatalogGolden -update-golden`, `TestPlatformParityGolden -update-golden`, `TestRegressionSnapshotUpdate -update-golden`, and the reachability test with `-update-orphan-allowlist`. Inspect this plan's diff line by line. It must remove exactly the four recovery/Medic names and reduce the command-count field by four; compared with the pre-phase base, the cumulative inventory is eleven lower. Retain every unrelated enrichment field. Never edit JSON by hand.

    Run the generated tests again without update flags plus `TestOrphanAllowlistOnlyShrinks`. Assert `git diff -- cmd/testdata/orphan_allowlist_baseline.json` is empty.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestRun.*Recovery|TestMedicAuto|TestAuditCatalogGolden|TestPlatformParityGolden|TestRegressionSnapshot|TestNoOrphanRuntimeCommands|TestOrphanAllowlistOnlyShrinks' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Production search finds no `medic-auto-spawn-check` declaration/registration and finds live direct eligibility callers.
    - Runtime/catalog/parity/current orphan list contain none of all eleven removed names; this plan's regression count drops by four and the cumulative pre-phase delta is eleven.
    - Generated diff contains no unrelated command/metadata change.
    - Baseline file has no diff and shrink-only ratchet passes.
    - Run recovery/Medic tests prove advice-only, zero auto-dispatch behavior.
  </acceptance_criteria>
  <reversibility rating="costly">The last dead adapter and generated inventory entries are removed; internal eligibility remains stable.</reversibility>
  <done>The recovery engine remains live behind supported workflows, and every command-surface record honestly reflects the complete cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|---|---|---|
| caller audit -> adapter deletion | Hidden production caller could lose recovery | Repository call graph plus run/build/continue/Queen tests before/after |
| command/helper separation | Cobra removal could accidentally change JSON/budget behavior | Direct round-trip/category/budget tests; no schema edits |
| generator flags -> golden files | Broad refresh could hide unrelated drift/history loss | One owning generator per file, line-by-line diff, rerun without flags, immutable baseline |
</threat_model>

<verification>
- `go test ./cmd/ -run 'Run.*Recovery|FailureClassif|RecoveryLog|RecoveryBudget|MedicAuto|Queen.*Recovery' -count=1`
- `go test ./cmd/ -run 'TestAuditCatalogGolden|TestPlatformParityGolden|TestRegressionSnapshot|TestNoOrphanRuntimeCommands|TestOrphanAllowlistOnlyShrinks' -count=1`
- `go build ./cmd/aether`
- `go vet ./...`
</verification>

<success_criteria>
The four orphan recovery/Medic commands are gone while internal recovery remains fully live, and all command-surface fixtures reflect exactly the complete eleven-command Phase 198.3 shrink without touching the historical orphan baseline.
</success_criteria>

## Artifacts this phase produces (Plan 09)

| Symbol/path | Kind |
|---|---|
| removal of three recovery classifier/log adapters | command cleanup |
| removal of Medic eligibility adapter | command cleanup |
| retained internal classifier/log/budget/eligibility | preserved capability |
| regenerated catalog/parity/regression/current orphan fixtures | updated ratchets |

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-09-SUMMARY.md` when done.
</output>
