---
phase: 198.3-overnight-stamina
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/state_extra.go
  - cmd/state_extra_test.go
  - cmd/command_guide.go
  - cmd/command_guide_test.go
  - cmd/visual_wrapper_contract_test.go
  - .aether/commands/insert-phase.yaml
  - .claude/commands/ant/insert-phase.md
  - .opencode/commands/ant/insert-phase.md
autonomous: true
requirements: []
audit_scope: [guided-insert-phase]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The audited insert-phase escape hatch is usable from one issue sentence without forcing the owner to invent internal phase fields."
    - "Existing explicit --after/--name/--description automation remains compatible."
    - "Zero input in non-interactive mode never mutates state and returns the exact command needed; zero input on a TTY gathers issue, outcome, and constraints."
    - "Go owns input resolution; Claude/OpenCode wrappers are byte-aligned passthrough guidance, not separate implementations."
  artifacts:
    - cmd/state_extra.go
    - cmd/state_extra_test.go
    - .aether/commands/insert-phase.yaml
    - wrapper and command-guide parity tests
  key_links:
    - "positional issue/TTY answers/explicit flags -> one resolved insert request -> existing phase insertion mutation"
    - ".aether command source -> Claude/OpenCode managed wrappers -> direct Go command"
  prohibitions:
    - statement: "Missing non-TTY input must not partially write COLONY_STATE.json."
      status: required
      verification: before/after byte assertion
    - statement: "Do not implement the interview only in wrapper prose."
      status: required
      verification: direct Go command tests cover shorthand and input_required behavior
---

<objective>
Restore the guided `insert-phase` recovery flow found by the phase audit while keeping the current explicit CLI contract compatible.

Purpose: after repeated failure, “insert a stabilisation phase” should be a usable next action, not a command that demands three internal flags the owner may not understand.

Output: one Go-owned request resolver, TTY interview/non-TTY guidance, and aligned command-guide/wrapper surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-RESEARCH.md
@.planning/phases/198.3-overnight-stamina/198.3-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve explicit, shorthand, interactive, and missing insert input before mutation</name>
  <files>cmd/state_extra.go, cmd/state_extra_test.go</files>
  <read_first>
    - cmd/state_extra.go around `phase-insert`/`insert-phase`, including validation, numbering, CSV/plan updates, and output behavior
    - cmd/state_extra_test.go from the phase-insert tests onward, especially quoted fields, missing fields, sealed colony, and invalid `--after`
    - current shared TTY/prompt helpers used by another Go lifecycle command; reuse them rather than adding direct terminal reads
    - repository tag `v5.3.3` `.aether/commands/insert-phase.yaml` for the historical issue/outcome/constraints interview and `Stabilize` naming intent
    - `.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md` audit-subject decision
  </read_first>
  <behavior>
    - Existing `phase-insert --after 2 --name "Fix auth" --description "..."` produces the same inserted phase as before.
    - `aether insert-phase "login retries lose state"` defaults `after` to the current phase, description to the issue, and a deterministic bounded name beginning `Stabilize`.
    - Explicit flags override derived values; optional constraints are retained in the phase description/task context through the existing supported field, not a new state schema.
    - Zero input on a TTY asks issue, desired outcome, and constraints once each, then shows/resolves the request through the same code path.
    - Zero input without a TTY returns `input_required` with an exact quoted command example and leaves state bytes unchanged.
  </behavior>
  <action>
    Add table-driven failing tests to `cmd/state_extra_test.go` for explicit compatibility, positional shorthand, override precedence, six-useful-word name bound, current-phase default, TTY answers, cancelled/empty TTY answer, and non-TTY missing input. For the last case, read `COLONY_STATE.json` before and after and assert byte equality. Use an injectable existing prompt/TTY seam; never depend on the test process's actual terminal.

    Extract a pure `resolvePhaseInsertRequest` (name may vary) that merges inputs with precedence `explicit flags > positional issue defaults > TTY answers > input_required`. Keep the existing insertion mutation below this resolver. Accept at most one positional issue. Resolve omitted `after` from current active phase only after valid state is loaded, but ensure the non-TTY no-input path returns before any write. Derive `Stabilize <phrase>` deterministically from at most six useful words and validate the merged request with existing checks.

    Return a structured `input_required` result in JSON mode and the plain-English exact command in visual mode. Do not silently choose an issue or write a placeholder phase.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestPhaseInsert|TestGuidedInsert|TestInsertPhase' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All pre-existing explicit phase-insert tests still pass unchanged or with assertions extended only for new compatible fields.
    - Positional shorthand inserts after the current phase with a deterministic `Stabilize` name of no more than seven total words (`Stabilize` plus six useful words).
    - Non-TTY zero input returns `input_required`, includes an executable example, and leaves the state file byte-identical.
    - Interactive test records exactly three prompt attempts and reaches the same resolved request helper as shorthand.
    - `go test ./cmd/ -run 'PhaseInsert|GuidedInsert|InsertPhase' -count=1` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">The current explicit mutation path remains; new resolution occurs before it and can be removed without data migration.</reversibility>
  <done>The recovery command is usable by describing the problem, while scripts using the old flags keep working.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align command guide and managed wrappers with the Go-owned guided flow</name>
  <files>cmd/command_guide.go, cmd/command_guide_test.go, cmd/visual_wrapper_contract_test.go, .aether/commands/insert-phase.yaml, .claude/commands/ant/insert-phase.md, .opencode/commands/ant/insert-phase.md</files>
  <read_first>
    - .aether/commands/insert-phase.yaml (canonical managed command source)
    - .claude/commands/ant/insert-phase.md and .opencode/commands/ant/insert-phase.md (current passthrough copies)
    - cmd/command_guide.go around insert-phase support and platform output
    - cmd/visual_wrapper_contract_test.go insert-phase expectations
    - repository publish/sync helper used to generate managed wrappers from `.aether/commands`
  </read_first>
  <behavior>
    - Every platform tells the owner they may pass one issue sentence or use explicit flags.
    - Wrappers execute the same visual Go command and do not independently derive phase name, description, or position.
    - Claude/OpenCode managed copies and canonical YAML express the same behavior and pass repository parity tests.
    - Command guide includes a non-interactive exact form suitable for the swarm three-strike recommendation in plan 07.
  </behavior>
  <action>
    Update the canonical YAML help/usage so the simplest example is `aether insert-phase "problem to stabilise"`, followed by the explicit automation form. Keep the wrapper action as direct `AETHER_OUTPUT_MODE=visual aether insert-phase $ARGUMENTS`; add no wrapper-side interview or derivation.

    Regenerate/sync the Claude and OpenCode managed files through the repository's normal source transformation if available; otherwise make the minimum byte-aligned edits and prove their managed bodies match. Update `aether command-guide insert-phase --platform codex` output to explain shorthand and exact non-interactive flags in plain English. Extend parity/guide tests so changing only one copy fails.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'Test.*InsertPhase.*Guide|Test.*InsertPhase.*Wrapper|TestVisualWrapperContract|TestParity' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Canonical source and both managed wrappers invoke the Go runtime directly with `$ARGUMENTS` and contain no independent naming algorithm.
    - Command-guide output includes shorthand and explicit non-interactive examples.
    - Wrapper/parity test fails when one managed copy is changed alone and passes after restoration.
    - `go test ./cmd/ -run 'InsertPhase|VisualWrapperContract|Parity' -count=1` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation/managed wrapper changes mirror a backward-compatible Go command extension.</reversibility>
  <done>Codex, Claude, and OpenCode all reach one guided Go implementation and show an exact usable recovery command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|---|---|---|
| owner issue text -> roadmap/state | Injection-like control text or malformed CSV could corrupt planning artifacts | Reuse current escaping/validation; treat text as data; keep bounded lengths and existing state writer |
| non-TTY caller -> mutation | Automation with missing arguments could insert an unintended phase | Return `input_required` before writes; before/after byte test |
| wrapper -> runtime | Platform wrapper could derive different phase fields | Wrappers only pass arguments; Go resolver is authoritative; parity tests |
</threat_model>

<verification>
- `go test ./cmd/ -run 'PhaseInsert|GuidedInsert|InsertPhase|VisualWrapperContract|Parity' -count=1`
- `go build ./cmd/aether`
- `go vet ./...`
</verification>

<success_criteria>
An owner can insert a stabilisation phase with one problem sentence or a short interactive interview, automation gets exact missing-input guidance without mutation, and every platform delegates the behavior to the same Go resolver.
</success_criteria>

## Artifacts this phase produces (Plan 06)

| Symbol/path | Kind |
|---|---|
| phase-insert request resolver | new pure input layer |
| positional issue and optional constraints support | compatible CLI extension |
| TTY interview / non-TTY `input_required` result | new UX behavior |
| canonical and managed insert-phase guidance | updated platform contract |
| guided input and no-mutation tests | regression proof |

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-06-SUMMARY.md` when done.
</output>
