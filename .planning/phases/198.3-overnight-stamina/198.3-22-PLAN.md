---
phase: 198.3-overnight-stamina
plan: 22
type: execute
wave: 3
depends_on:
  - 198.3-17
  - 198.3-18
  - 198.3-19
  - 198.3-20
  - 198.3-21
files_modified:
  - cmd/blocker_snapshot.go
  - cmd/blocker_snapshot_test.go
  - cmd/status.go
  - cmd/flag_cmds.go
  - cmd/codex_continue.go
  - cmd/gate.go
  - cmd/compatibility_cmds.go
  - cmd/autopilot_report.go
  - cmd/autopilot_report_test.go
  - cmd/autopilot_recovery_198_3_test.go
  - cmd/run_overnight_198_3_test.go
autonomous: true
requirements:
  - STAM-01
  - STAM-02
  - STAM-04
  - STAM-05
  - STAM-06
gap_closure: true
must_haves:
  truths:
    - "A genuine absence of both current and legacy blocker stores is an available empty snapshot."
    - "Malformed or unreadable current/legacy blocker storage is unavailable, never zero."
    - "aether run fails closed as colony_not_runnable before further dispatch whenever blocker truth is unavailable at baseline or a post-stage read."
    - "The actual continue advancement gate reads blocker truth unconditionally and cannot credit a phase while that truth is unavailable."
    - "aether status labels blocker truth unavailable and never prints a false zero count."
    - "A persisted morning report represents unavailable before/after blocker truth explicitly and neither its JSON nor its run/status visual rendering claims a false 0 -> 0 movement."
    - "After all six repairs, the six-phase unattended fixture still completes with honest persisted report data and all repository gates pass."
  artifacts:
    - path: "cmd/blocker_snapshot.go"
      provides: "Error-aware canonical blocker snapshot reader"
      contains: "readBlockerSnapshot"
    - path: "cmd/status.go"
      provides: "Explicit unavailable blocker status in JSON and visual output"
      contains: "blocker_snapshot_available"
    - path: "cmd/gate.go"
      provides: "Fail-closed blocker truth input for the phase-advancement gate"
      contains: "checkUnresolvedBlockerFlags"
    - path: "cmd/compatibility_cmds.go"
      provides: "Fail-closed baseline/post-stage blocker read handling"
      contains: "colony_not_runnable"
    - path: "cmd/autopilot_report.go"
      provides: "Availability-aware before/after blocker evidence in persisted reports and shared run/status rendering"
      contains: "autopilotBlockerSnapshotReport"
    - path: "cmd/autopilot_report_test.go"
      provides: "Persisted JSON plus run/status visual regressions forbidding false zero blocker movement"
    - path: "cmd/run_overnight_198_3_test.go"
      provides: "Unavailable-state adversarial tests and final six-phase contract gate"
  key_links:
    - from: "cmd/blocker_snapshot.go"
      to: "pending-decisions.json / flags.json"
      via: "missing-aware prioritized storage read with parse/read errors preserved"
      pattern: "pending-decisions.json|flags.json"
    - from: "cmd/compatibility_cmds.go"
      to: "cmd/blocker_snapshot.go"
      via: "every pre/post authorization snapshot propagates errors to colony_not_runnable"
      pattern: "readBlockerSnapshot"
    - from: "cmd/status.go"
      to: "cmd/blocker_snapshot.go"
      via: "status renders available counts or an explicit unavailable state from the same reader"
      pattern: "readBlockerSnapshot"
    - from: "cmd/codex_continue.go"
      to: "cmd/gate.go"
      via: "one unconditional canonical snapshot result, including its error, reaches the no-unresolved-blockers gate before phase advancement"
      pattern: "checkUnresolvedBlockerFlags"
    - from: "cmd/compatibility_cmds.go"
      to: "cmd/autopilot_report.go"
      via: "terminal finalization carries snapshot availability/error into the single persisted report instead of substituting a zero snapshot"
      pattern: "buildAutopilotInvocationReport"
    - from: "cmd/autopilot_report.go"
      to: "cmd/status.go"
      via: "the same stored availability-aware report renderer is reused by the closing run output and later status output"
      pattern: "renderAutopilotInvocationReport"
---

<objective>
Make blocker truth error-aware, fail the run closed when it is unavailable, and re-prove the full overnight contract.

Purpose: Eliminate the last fail-open authorization input, then execute the complete six-gap regression and repository gate set so the phase goal is proven as a whole.
Output: Missing-versus-unavailable storage semantics, run/status integration, and final end-to-end evidence.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-RESEARCH.md
@.planning/phases/198.3-overnight-stamina/198.3-PATTERNS.md
@.planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
@.planning/phases/198.3-overnight-stamina/198.3-17-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-18-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-19-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-20-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-21-SUMMARY.md

<interfaces>
`readBlockerSnapshot` currently has callers in `cmd/flag_cmds.go`, `cmd/compatibility_cmds.go`, `cmd/autopilot_recovery_198_3_test.go`, `cmd/run_overnight_198_3_test.go`, `cmd/codex_continue.go`, `cmd/autopilot_report.go`, `cmd/status.go`, and `cmd/blocker_snapshot_test.go`. Go compiles every test file even under `-run`, so the error-return migration must update this complete inventory atomically. `checkUnresolvedBlockerFlags` currently reads the stores separately and returns `Passed=true` when both reads fail; `runCodexContinueGatesWithAutopilotBaseline` reads the canonical snapshot only after that check already reports a blocker. The report currently persists non-optional `blockerSnapshot` values and both report renderers print integer movement unconditionally. Replace all three fail-open seams as one contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Distinguish empty blocker storage from unavailable blocker truth in reader and status</name>
  <files>cmd/blocker_snapshot.go, cmd/blocker_snapshot_test.go, cmd/status.go, cmd/flag_cmds.go, cmd/autopilot_report.go, cmd/autopilot_report_test.go, cmd/autopilot_recovery_198_3_test.go, cmd/run_overnight_198_3_test.go</files>
  <read_first>
    cmd/blocker_snapshot.go
    cmd/blocker_snapshot_test.go
    cmd/status.go
    cmd/flag_cmds.go
    cmd/autopilot_report.go
    cmd/autopilot_report_test.go
    cmd/autopilot_recovery_198_3_test.go
    cmd/run_overnight_198_3_test.go
    cmd/pending_decision.go
    pkg/storage/storage.go
    pkg/storage/lock.go
    pkg/storage/storage_malformed_test.go
    cmd/status_test.go
    .planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
    .planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
  </read_first>
  <behavior>
    - Missing `pending-decisions.json` falls back to legacy `flags.json`; both missing returns an available empty snapshot.
    - Malformed/non-regular/unreadable authoritative current storage returns an error and is not masked by legacy data.
    - When current storage is genuinely absent, malformed/non-regular/unreadable legacy storage returns an error.
    - Status renders counts only on success; on error it renders an explicit unavailable warning and JSON availability=false/null counts.
    - `flag-check-blockers` returns a non-zero error result on unavailable truth and never emits `has_blockers=false` or numeric zero counts.
    - Test/report fixture callers fail their test on an unexpected read error rather than silently keeping a zero snapshot.
  </behavior>
  <action>Change `readBlockerSnapshot` to return `(blockerSnapshot, error)` and perform a missing-aware prioritized read: `pending-decisions.json` is authoritative when present; fall back to `flags.json` only on `os.ErrNotExist`; return an initialized empty snapshot only when both are genuinely absent. A nil store is unavailable. Reject directories and other non-regular files explicitly, and preserve JSON decode, permission, and other I/O failures with data-relative path context. Migrate every current caller in this task or Task 2—none may use `_` for the error or synthesize an empty snapshot. In `status.go`, set `blocker_snapshot_available=true` and retain the four existing count/ID fields only on success; on error set it false, expose a bounded/sanitized `blocker_snapshot_error`, set the blocker/escalation count fields to JSON null (and IDs to null/omitted), and render exactly `Blocker truth: unavailable` instead of the `Flags: 0 blockers` / `Existing blocker work: 0 active` lines. In `flag_cmds.go`, make `flag-check-blockers` return a non-zero error envelope with availability=false and no `has_blockers=false` or numeric blocker facts; do not continue into the fail-open `loadFlagsFile` issue/note projection after snapshot failure. Update `blocker_snapshot_test.go` and the non-production snapshot callers in `autopilot_recovery_198_3_test.go` and `run_overnight_198_3_test.go` to assert/fail on errors. Prepare the report-side availability value used in Task 2 as `autopilotBlockerSnapshotReport { Available bool, Snapshot *blockerSnapshot, Error string }`, where an unavailable JSON value omits/nulls the snapshot and therefore contains no count fields. This implements D-03/D-06: unknown authorization input is a stop condition, not an empty baseline.</action>
  <acceptance_criteria>
    - The canonical reader has an error return; a repository-wide caller audit shows every call in `flag_cmds.go`, `compatibility_cmds.go`, `autopilot_recovery_198_3_test.go`, `run_overnight_198_3_test.go`, `codex_continue.go`, `autopilot_report.go`, `status.go`, and `blocker_snapshot_test.go` handles it explicitly.
    - `TestBlockerSnapshotDistinguishesAbsentFromUnavailable` covers both missing, current valid, legacy valid fallback, malformed current with valid legacy present, malformed legacy, and current/legacy unreadable or non-regular paths.
    - `TestStatusReportsUnavailableBlockerTruth` asserts JSON `blocker_snapshot_available=false`, non-numeric/null blocker counts, a path-safe error, and visual text containing `Blocker truth: unavailable` with no `0 active` claim.
    - `TestFlagCheckBlockersFailsWhenTruthUnavailable` asserts non-zero/error output, availability=false, and absence of `has_blockers=false`, `blockers:0`, and `escalated_blockers:0`.
    - Existing valid snapshot sorting/comparison and status parity tests remain green.
    - `rg -n 'readBlockerSnapshot\(' cmd --glob '*.go'` matches only explicitly handled two-value reads (plus the definition), and `go test ./cmd -run 'Test(BlockerSnapshot|Status.*Blocker|FlagCheckBlockers)' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(BlockerSnapshot|Status.*Blocker|FlagCheckBlockers)' -count=1</automated>
  </verify>
  <done>Only true absence means zero blockers; malformed or unreadable blocker truth is explicit and visible in status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail the live run closed on unavailable blocker truth and execute the final stamina gate</name>
  <files>cmd/compatibility_cmds.go, cmd/codex_continue.go, cmd/gate.go, cmd/autopilot_report.go, cmd/autopilot_report_test.go, cmd/autopilot_recovery_198_3_test.go, cmd/run_overnight_198_3_test.go, cmd/blocker_snapshot.go, cmd/blocker_snapshot_test.go</files>
  <read_first>
    cmd/compatibility_cmds.go
    cmd/codex_continue.go
    cmd/gate.go
    cmd/run_overnight_198_3_test.go
    cmd/blocker_snapshot.go
    cmd/blocker_snapshot_test.go
    cmd/autopilot_report.go
    cmd/autopilot_report_test.go
    cmd/autopilot_recovery_198_3_test.go
    cmd/run_autopilot_198_3_test.go
    cmd/autopilot_checkpoints_test.go
    cmd/reviewer_artifact_198_3_test.go
    cmd/autopilot_lessons_test.go
    cmd/swarm_cmd_test.go
    cmd/swarm_strikes_test.go
    .planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
    .planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
  </read_first>
  <behavior>
    - Unavailable blocker truth at READY/BUILT baseline stops with `colony_not_runnable` before build/continue dispatch.
    - The continue advancement gate reads the canonical snapshot unconditionally; unavailable truth fails `no_unresolved_blockers`, and neither current phase nor phase status changes.
    - If blocker storage becomes unavailable after build or during/before continue advancement, the authorization check stops before phase credit or another dispatch and persists the honest terminal report.
    - Persisted before/after blocker report values carry `available=false`, a sanitized diagnostic, and a null/omitted snapshot; run visual, persisted JSON, and later status visual never print `0 -> 0` for unknown data.
    - Genuine empty storage permits the existing six-phase fixture to finish.
    - The final fixture still reports invocation wall-clock, measured/unreported spend, queued owner work, blocker movement, exact next command, and no automatic seal.
  </behavior>
  <action>Make the canonical error-aware snapshot the actual advancement input, not an outer after-the-fact check. In `gate.go`, add a snapshot-taking form of `checkUnresolvedBlockerFlags` that returns `Passed=false` with an unavailable diagnostic when given an error; retain the zero-argument helper only as a delegating compatibility wrapper for its existing tests. In `runCodexContinueGatesWithAutopilotBaseline`, call `readBlockerSnapshot(store)` unconditionally before evaluating `no_unresolved_blockers`, pass both snapshot and error into that gate, and apply D-06's existing-baseline exception only when the read succeeded and comparison proves no count increase or new escalation. A failed read can never be overwritten to passed. Add a direct/public continue regression that corrupts blocker storage before this boundary, invokes the real continuation path, and proves `advanced=false`, the unavailable gate diagnostic is durable, `CurrentPhase` is unchanged, and the current phase status is not `completed`.

Handle every error-aware read in `runCompatibilityAutopilot`: invocation-start, READY/BUILT baseline, post-build, and post-continue failures become a typed `colony_not_runnable` decision with phase/stage evidence, funnel through the one terminal finalizer, and dispatch no subsequent worker. Do not pass an unavailable `AutopilotBlockerBaseline`. In `autopilot_report.go`, replace non-optional before/after snapshots with the explicit `autopilotBlockerSnapshotReport` contract from Task 1; `beginAutopilotInvocation`, `buildAutopilotInvocationReport`, and `finishAutopilotInvocation` carry each read's error into the report rather than manufacturing zero values. Persist the terminal snapshot error, keep the primary `colony_not_runnable` cause, and render numeric movement only when both snapshots are available; otherwise render `Blocker movement: unavailable` plus a sanitized diagnostic with no integer transition. The ordinary report renderer and the report-durability-failure renderer must share that formatter, so both immediate run output and later `aether status` reuse identical honest text. Add persisted-JSON and visual/status assertions in `autopilot_report_test.go`, plus public run tests for corrupt/non-regular current and legacy stores at initial baseline and hooks that make storage fail between baseline and post-build/post-continue reads. Assert dispatch counters, trigger code, non-zero/error result, persisted stop reason/next command, availability flags/null snapshots, and absence of false zero blocker facts. Then run the existing six-phase/mutation/report suite plus every new Plan 17-22 regression and full Go gates.</action>
  <acceptance_criteria>
    - `TestRunFailsClosedWhenBlockerTruthUnavailable` covers READY and BUILT baselines for malformed/unreadable current and legacy stores and proves zero build/continue dispatch.
    - `TestContinueAdvancementFailsClosedWhenBlockerTruthUnavailable` corrupts storage before the actual continue gate and proves the public/direct result is blocked with current phase and phase status byte-for-byte unchanged.
    - `TestRunFailsClosedWhenBlockerTruthBecomesUnavailable` covers post-build failure (no continue/phase credit) and post-continue failure after an otherwise valid advancement (no next-phase dispatch); the separate direct advancement regression is the proof that corruption before credit cannot advance. Both endings persist `colony_not_runnable` with the exact resume/status action.
    - `TestAutopilotReportUnavailableBlockerTruthIsNeverZero` asserts stored JSON has explicit before/after availability and null/omitted snapshots/counts, and both run and status visual output say unavailable without `0 -> 0`, `0 active`, or `0 escalated` claims.
    - `checkUnresolvedBlockerFlags` no longer contains a both-stores-failed `Passed=true` branch, and the autopilot baseline override is reachable only after a successful canonical snapshot read.
    - `TestOvernightRunCompletesSixPhases` and `TestOvernightRunQueueableMutationStopsEarly` remain green after all six gap repairs.
    - All new focused tests from Plans 17-22 pass together.
    - `go test ./...`, `go vet ./...`, and `go test ./... -race` pass.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(ContinueAdvancementFailsClosedWhenBlockerTruthUnavailable|RunFailsClosedWhenBlockerTruth|AutopilotReportUnavailableBlockerTruthIsNeverZero|OvernightRun|VisualCheckpointWorkGeneration|RuntimeCheckpointWorkGeneration|LegacyAnsweredDecisionText|OwnerConfirmationAnsweredRejectsCrossScope|SuggestionOnlyCritical|AutopilotReplanCadence|RunReplanCadence|SwarmFinalizeRejectsUnbound|SwarmThirdStrikeRepairs|SwarmPartialEscalation)' -count=1</automated>
  </verify>
  <done>The live run never authorizes work from unavailable blocker data, and the complete repaired overnight contract passes focused, full, vet, and race gates.</done>
</task>

</tasks>

<source_coverage_audit>

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Fresh owner approval and active-scope legacy answers | Plans 17-18 | COVERED |
| GOAL | Malformed Critical evidence stops before advancement | Plan 19 | COVERED |
| GOAL | Replan cadence survives bounded/interrupted runs | Plan 20 | COVERED |
| GOAL | Empty versus unavailable blocker truth | Plan 22 | COVERED |
| GOAL | Exact terminal external swarm evidence | Plan 21 Task 1 | COVERED |
| GOAL | Recoverable third-strike escalation transition | Plan 21 Task 2 | COVERED |
| REQ | STAM-01 | Plans 17-19, 21-22 | COVERED |
| REQ | STAM-02 | Plans 17, 19, 22 | COVERED |
| REQ | STAM-03 | Plan 20 | COVERED |
| REQ | STAM-04 | Plan 22 final report regression | COVERED |
| REQ | STAM-05 | Plans 21-22 | COVERED |
| REQ | STAM-06 | Plan 22 six-phase and mutation regressions | COVERED |
| RESEARCH | Go runtime owns stop/queue/state; wrappers do not mutate state | Plans 17-22 modify Go runtime/tests only | COVERED |
| RESEARCH | Reuse pending decisions, plan revisions, blocker flags, swarm result history, and morning report | Plans 17-22 extend those existing stores/contracts; no parallel pipeline | COVERED |
| RESEARCH | Automation-first adversarial Go verification | Every task is TDD-scoped with named automated regressions | COVERED |
| CONTEXT | D-01/D-02 owner work queues but remains current and enforceable | Plans 17-18 | COVERED |
| CONTEXT | D-03/D-05 malformed Critical and corrupt state stop; valid High remains report-only | Plans 19 and 22 | COVERED |
| CONTEXT | D-06 blocker baseline/status truth | Plan 22 | COVERED |
| CONTEXT | D-09 through D-12 revision-aware queue/pause, interval plus confirmed lessons, one accumulated note | Plan 20 | COVERED |
| CONTEXT | D-17/D-20 outcome-first persisted report/status | Plan 22 regression gate | COVERED |
| CONTEXT | D-04, D-07/D-08, D-13 through D-16, D-18/D-19 | Completed Plans 01-16; Plan 22 full regression preserves them | COVERED |

Excluded per CONTEXT Deferred Ideas: automatic provider switching, automatic plan rewriting, the broad turnaround programme, and the real owner-observed Phase 199 run. No deferred item is implemented by Plans 17-22.
</source_coverage_audit>

<verification>
- `go test ./cmd -run 'Test(RunFailsClosedWhenBlockerTruth|BlockerSnapshot|Status.*Blocker|OvernightRun)' -count=1`
- `go test ./cmd -run 'Test(ContinueAdvancementFailsClosedWhenBlockerTruthUnavailable|AutopilotReportUnavailableBlockerTruthIsNeverZero|FlagCheckBlockersFailsWhenTruthUnavailable)' -count=1`
- `go test ./...`
- `go vet ./...`
- `go test ./... -race`
</verification>

<success_criteria>
- Missing blocker files mean available/empty; corrupt or unreadable files mean unavailable.
- Run fails closed before unsafe dispatch and status never displays false zero blocker truth.
- Continue's own advancement gate fails closed before phase credit when blocker truth is unavailable.
- Persisted reports and both immediate/later renderers represent unavailable before/after snapshots without numeric zero claims.
- All six verification blockers have named adversarial tests.
- STAM-01 through STAM-06 remain collectively covered.
- The six-phase unattended fixture, report honesty, full tests, vet, and race gates pass.
</success_criteria>

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-22-SUMMARY.md` when done.
</output>
