---
phase: 198.3-overnight-stamina
plan: 01
subsystem: runtime
tags: [autopilot, policy, blockers, status, tdd]

# Dependency graph
requires: []
provides:
  - Canonical typed catalogue for every overnight stop, queueable checkpoint, and normal stop
  - Deterministic live blocker snapshot with ordinary and escalation evidence
  - Shared diagnostic and status projections backed by the same blocker reader
affects: [198.3-02, 198.3-04, 198.3-05, 198.3-07, 198.3-10]

# Tech tracking
tech-stack:
  added: []
  patterns: [typed policy catalogue, pure durable-store projection, renderer-at-edge, TDD red-green]

key-files:
  created: [cmd/autopilot_policy.go, cmd/blocker_snapshot.go]
  modified: [cmd/compatibility_cmds.go, cmd/run_visuals.go, cmd/flag_cmds.go, cmd/status.go]

key-decisions:
  - "Stable trigger codes and dispositions are control data; prose is rendered only at the CLI edge."
  - "Blocker truth includes only exact unresolved Type=blocker rows, with exact Source=escalation rows tracked separately."
  - "A same-count ordinary blocker ID replacement is diagnostic evidence, not a blocker-count increase."

patterns-established:
  - "Canonical policy projection: dry-run renders the exact typed catalogue future enforcement consumes."
  - "Shared live-state projection: diagnostic and status adapters add the same snapshot fields without rescanning unrelated stores."

requirements-completed: [STAM-02, STAM-05]

# Metrics
duration: 50min
completed: 2026-08-31
---

# Phase 198.3 Plan 01: Overnight Trigger and Blocker Truth Summary

**Typed overnight dispositions now drive dry-run documentation, while one deterministic flag-store snapshot supplies ordinary and escalated blocker truth to diagnostics and status.**

## Performance

- **Duration:** 50 min
- **Started:** 2026-08-31T15:00:23Z
- **Completed:** 2026-08-31T15:51:21Z
- **Tasks:** 2
- **Files modified:** 10

## Accomplishments

- Defined and validated 14 stable overnight trigger codes with exact headless and interactive dispositions, detection descriptions, and next-action templates.
- Made `aether run --dry-run` carry and render that canonical catalogue while preserving the existing phase/build/continue preview.
- Added a deterministic blocker snapshot with sorted, deduplicated evidence IDs and separate escalation counts/IDs.
- Aligned `flag-check-blockers` and status JSON/visual output on the same live snapshot while preserving legacy diagnostic fields.
- Added comparison semantics that distinguish count growth and escalation without treating same-count ordinary ID replacement as growth.

## Task Commits

Each task followed its required TDD red/green sequence:

1. **Task 1 RED: Pin the canonical overnight trigger contract** - `177d3c57` (test)
2. **Task 1 GREEN: Define and render the canonical trigger catalogue** - `ff18cd33` (feat)
3. **Task 2 RED: Pin blocker snapshot and consumer agreement** - `bd38e588` (test)
4. **Task 2 GREEN: Share live blocker snapshot truth** - `08b230e9` (feat)
5. **Rule 1 fix: Shrink obsolete next-action ratchet entries** - `4a997588` (fix)

## Files Created/Modified

- `cmd/autopilot_policy.go` - Typed trigger codes, dispositions, catalogue validation, and lookup.
- `cmd/autopilot_policy_test.go` - Exact catalogue/order, validation, disposition, and dry-run projection contracts.
- `cmd/blocker_snapshot.go` - Live blocker reader, escalation evidence, comparison, and shared JSON projection.
- `cmd/blocker_snapshot_test.go` - Storage-boundary, ordering, comparison, diagnostic, and status regression coverage.
- `cmd/compatibility_cmds.go` - Dry-run result construction from the canonical trigger catalogue.
- `cmd/run_visuals.go` - Plain-English rendering of structured catalogue rows.
- `cmd/flag_cmds.go` - Diagnostic adapter over the shared blocker snapshot with legacy fields intact.
- `cmd/status.go` - Shared blocker fields in JSON plus explicit existing/escalated blocker visual output.
- `cmd/testdata/next_action_hardcode.json` - Regenerated live scanner output after dry-run advice consolidation.
- `cmd/testdata/next_action_hardcode_baseline.json` - Frozen ratchet shrunk by the same three obsolete sites.

## Decisions Made

- Kept the trigger catalogue descriptive in this plan; stage-specific evidence evaluation remains assigned to plans 02 and 04.
- Retained exact, case-sensitive `Type == "blocker"` and `Source == "escalation"` matching so neighboring or malformed record categories cannot silently widen unattended stop policy.
- Counted unresolved blocker rows while sorting/deduplicating only their nonblank evidence IDs, preserving safety counts even when persisted evidence is malformed.
- Preserved the diagnostic command's existing rule that unknown non-blocker rows count as issues; those rows never enter the blocker snapshot.
- Kept `checkAutopilotPauseConditions` untouched as required; plan 04 owns its live-loop replacement.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Shrunk stale next-action hardcode ratchet entries after dry-run consolidation**

- **Found during:** Overall verification after Task 2
- **Issue:** Consolidating repeated dry-run result maps removed three scanner-visible hardcoded `next` sites, so the shrink-only live/frozen ratchet correctly failed on obsolete `aether continue`, `aether plan`, and `aether seal` entries.
- **Fix:** Regenerated the live scanner artifact and removed exactly those three obsolete entries from the frozen baseline; no new allowlist entry was added.
- **Files modified:** `cmd/testdata/next_action_hardcode.json`, `cmd/testdata/next_action_hardcode_baseline.json`
- **Verification:** Next-action ratchet tests passed, followed by `go test ./...` and `go test -race ./...` with 8,209 passing tests.
- **Committed in:** `4a997588`

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bug)
**Impact on plan:** The additional generated-file shrink records an improvement directly caused by the planned refactor; it adds no runtime scope.

## TDD Gate Compliance

- Task 1 RED `177d3c57` precedes GREEN `ff18cd33`.
- Task 2 RED `bd38e588` precedes GREEN `08b230e9`.
- Both RED runs failed on the intended undefined policy/snapshot symbols before implementation.

## Verification

- Focused trigger/blocker/status contracts: 30 passed.
- Run compatibility and status regression set: 49 passed.
- Broader blocker/status acceptance set: 118 passed.
- `go build ./cmd/aether`: passed.
- `go vet ./...`: passed.
- `go test ./...`: 8,209 passed across 20 packages.
- `go test -race ./...`: 8,209 passed across 20 packages.

## Issues Encountered

- The full-suite next-action ratchet detected the intended baseline shrink; resolved through the Rule 1 deviation above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plans 02 and 04 can evaluate current build/continue evidence using the stable trigger vocabulary without parsing rendered prose.
- Plan 04 can take pre/post snapshots and consume `count_increased` / `escalation_added` directly.
- Plan 07 can write normal `Source=escalation` flags and rely on this snapshot without swarm-specific status code.
- No known stubs, deferred issues, or execution blockers remain.

## Self-Check: PASSED

- All 11 expected implementation, generated, test, and summary files exist.
- All five task/deviation commits are present in repository history.
- Worktree contains only this uncommitted summary before the metadata commit.

---
*Phase: 198.3-overnight-stamina*
*Completed: 2026-08-31*
