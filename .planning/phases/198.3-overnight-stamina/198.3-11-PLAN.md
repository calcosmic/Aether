---
phase: 198.3-overnight-stamina
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/autopilot_checkpoints.go
  - cmd/pending_decision.go
  - cmd/handoff_decisions_cmd.go
  - cmd/autopilot_checkpoints_test.go
  - cmd/pending_decision_test.go
  - cmd/handoff_decisions_cmd_test.go
autonomous: true
requirements:
  - STAM-01
  - STAM-02
gap_closure: true
must_haves:
  truths:
    - "A visual or runtime owner checkpoint can be answered only with a capability issued for that exact pending row."
    - "A checkpoint capability works once; missing, wrong, cross-checkpoint, and replayed capabilities leave the checkpoint unresolved."
    - "Generic pending-decision and clarification resolvers cannot bypass the protected checkpoint path."
    - "Checkpoint identity is stable across capability rotations, and only capability hashes are durable; raw owner capabilities appear only in an immediate owner-facing recovery command."
  artifacts:
    - path: "cmd/autopilot_checkpoints.go"
      provides: "Capability issuance, binding, rendering, and atomic single-use checkpoint resolution"
    - path: "cmd/pending_decision.go"
      provides: "Wire-compatible checkpoint capability hashes and generic-resolver exclusion"
    - path: "cmd/handoff_decisions_cmd.go"
      provides: "Capability-aware decision-answer routing without checkpoint fallback"
    - path: "cmd/autopilot_checkpoints_test.go"
      provides: "Positive and adversarial owner-checkpoint lifecycle proof"
  key_links:
    - from: "cmd/autopilot_checkpoints.go"
      to: "cmd/handoff_decisions_cmd.go"
      via: "the recovery command's checkpoint capability is consumed by the dedicated resolver"
      pattern: "checkpoint-capability"
    - from: "cmd/pending_decision.go"
      to: "cmd/autopilot_checkpoints.go"
      via: "persisted SHA-256 capability binding, never raw capability storage"
      pattern: "CapabilitySHA256"
    - from: "cmd/autopilot_checkpoints.go"
      to: "cmd/codex_workflow_cmds.go"
      via: "stable checkpoint ID/CheckpointKey remains independent of each rotated capability so seal can deduplicate consumers by identity"
      pattern: "CheckpointKey"
---

<objective>
Close the owner-checkpoint authorization gap by making visual and runtime checkpoint answers single-use, capability-bound owner actions.

Purpose: D-02 permits unattended work to queue subjective owner work, but that queue is only trustworthy if generic CLI paths cannot silently manufacture the owner's answer.
Output: Capability-bound checkpoint issuance and resolution with negative-path CLI tests and no raw-token persistence.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
@.planning/phases/198.3-overnight-stamina/198.3-REVIEW.md
@.planning/phases/198.3-overnight-stamina/198.3-04-SUMMARY.md
@cmd/forced_reviewer_waiver.go

<interfaces>
- `upsertAutopilotCheckpoint(PendingDecision, phaseID, subject)` is the stable checkpoint creation seam.
- `checkpointReference(PendingDecision)` supplies the in-memory owner recovery command.
- `recordDecisionAnswer(question, answer, phase, source)` remains the ordinary clarification path and must no longer resolve checkpoint rows.
- `resolveForcedReviewerWaiverPendingDecision` and `newForcedReviewerWaiverCapability` are the source-of-truth single-use, SHA-256-at-rest capability analogs.
- `pending-decision-resolve` is a generic resolver and must reject both checkpoint types before any state change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind every owner checkpoint to a single-use capability</name>
  <files>cmd/autopilot_checkpoints.go, cmd/pending_decision.go, cmd/autopilot_checkpoints_test.go</files>
  <read_first>
    - cmd/autopilot_checkpoints.go
    - cmd/pending_decision.go
    - cmd/autopilot_checkpoints_test.go
    - cmd/forced_reviewer_waiver.go
    - cmd/forced_reviewer_waiver_test.go
    - cmd/autopilot_report.go
  </read_first>
  <behavior>
    - A newly materialized visual or runtime checkpoint returns an exact `decision-answer` command containing a freshly generated checkpoint capability.
    - The pending row stores only one or more SHA-256 hashes bound to its stable ID, type, phase, checkpoint key, goal, and session; JSON never contains the raw capability.
    - Correct capability plus the exact question and phase atomically resolves one row; a replay, missing token, wrong token, or a token from a sibling checkpoint resolves none.
    - Re-rendering an unresolved checkpoint may append a new hash without invalidating a previously displayed command, matching the forced-reviewer capability precedent.
    - Issuing or rotating a capability never changes the checkpoint's stable ID/`CheckpointKey`; capability material is not part of checkpoint identity.
  </behavior>
  <action>
Add wire-compatible checkpoint capability hash fields to `PendingDecision` plus a transient, `json:"-"` raw capability carrier used only while composing the immediate `autopilotCheckpointReference`. Reuse the cryptographic generator and constant-time hash comparison pattern from `forced_reviewer_waiver.go`; never persist, log, or include the raw token in the stable identity material. Derive ID/`CheckpointKey` solely from checkpoint type, phase, normalized subject, session, and goal before capability issuance. Change `upsertAutopilotCheckpoint` so every unresolved result has a usable transient capability and atomically appends only its hash to the exact scoped row. Add a dedicated atomic resolver that matches checkpoint type, stable ID/question, phase, current goal/session scope, and capability hash before setting `Resolved`, `Resolution`, and `ResolvedAt`; successful resolution consumes the row so the same capability cannot be replayed. Update `checkpointDecisionAnswerCommand` to emit `--checkpoint-capability`. Preserve stable checkpoint IDs and evidence merging so Plan 198.3-12 can migrate seal deduplication by identity rather than command text. This implements D-02's owner-only boundary and D-01's continue-until-genuine-stop rule without weakening the established forced-reviewer protection.
  </action>
  <verify>
    <automated>rtk go test ./cmd -run 'Test(VisualCheckpointMaterializesFromPersistedClaims|RuntimeVerificationDecisionIsIdempotent|CheckpointCapability)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: raw checkpoint capability fields are tagged `json:"-"`; only SHA-256 values are serialized.
    - Identity assertion: issuing a second capability leaves ID/`CheckpointKey` unchanged while appending a distinct hash.
    - Behavior assertion: correct capability resolves exactly one matching row and reuse returns an error with the row still resolved only once.
    - Negative-path assertion: missing, wrong, cross-row, stale-scope, and replayed capabilities do not alter any unresolved row.
    - Test assertion: the focused checkpoint capability suite passes with `-count=1`.
  </acceptance_criteria>
  <done>Checkpoint issuance stores hashes only and the dedicated resolver accepts exactly one correctly bound capability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exclude protected checkpoints from every generic answer path</name>
  <files>cmd/pending_decision.go, cmd/handoff_decisions_cmd.go, cmd/pending_decision_test.go, cmd/handoff_decisions_cmd_test.go, cmd/autopilot_checkpoints_test.go</files>
  <read_first>
    - cmd/pending_decision.go
    - cmd/handoff_decisions_cmd.go
    - cmd/pending_decision_test.go
    - cmd/handoff_decisions_cmd_test.go
    - cmd/autopilot_checkpoints_test.go
    - cmd/forced_reviewer_waiver.go
  </read_first>
  <behavior>
    - `pending-decision-resolve` rejects visual and runtime checkpoint IDs before writing.
    - `recordDecisionAnswer` records ordinary clarification answers but never searches for or resolves checkpoint rows.
    - `decision-answer` detects an exact pending checkpoint first and requires `--checkpoint-capability`; it cannot fall through and append a generic resolved clarification on authorization failure.
    - Generic listing may expose checkpoint identity and evidence but redacts every capability hash.
  </behavior>
  <action>
Add `--checkpoint-capability` to `decision-answer` and route an exact, current-scope checkpoint match to the dedicated resolver before ordinary clarification handling. If a checkpoint match exists, missing or invalid authorization must render a non-zero error and must not fall through to `recordDecisionAnswer`. Remove checkpoint resolution from `recordDecisionAnswer`, retaining its ordinary worker-handoff and seal-confirmation behavior. Extend the existing protected-row guard in `pending-decision-resolve` from forced-reviewer source rows to both `isAutopilotCheckpointType` values. Redact checkpoint capability hashes in `pending-decision-list`, as already done for forced-reviewer hashes. Update old checkpoint tests that called the internal generic answer helper to exercise the public capability-aware seam instead. Do not add identity inference from `--source`; authorization is the capability, not caller-supplied prose.
  </action>
  <verify>
    <automated>rtk go test ./cmd -run 'Test(PendingDecisionResolveRejects.*Checkpoint|DecisionAnswer.*Checkpoint|CheckpointAnswerResolvesOriginalRow|HandoffDecisions)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: generic resolve and capability-free `decision-answer` return non-zero error envelopes for a pending checkpoint.
    - State assertion: failed generic and capability checks leave the original row byte-for-byte unresolved and append no clarification row.
    - Redaction assertion: list JSON contains neither stored capability hashes nor raw capabilities.
    - Compatibility assertion: ordinary worker-handoff answers and forced-reviewer waiver answers still pass their existing suites.
  </acceptance_criteria>
  <done>All public generic paths reject protected checkpoints while ordinary and forced-reviewer answers remain compatible.</done>
</task>

</tasks>

<verification>
Run `rtk go test ./cmd -run 'Test(Checkpoint|PendingDecision|DecisionAnswer|HandoffDecisions|ForcedReviewerWaiver)' -count=1`, then `rtk go test ./cmd -count=1`. Inspect the persisted pending-decision fixture to confirm it contains only capability hashes; Plan 198.3-12 performs the cross-consumer recursive audit of reports and lifecycle events after their migrations land.
</verification>

<success_criteria>
- Owner checkpoint commands contain a per-row single-use capability.
- No generic resolver can resolve or shadow a protected checkpoint.
- Authorization failures are non-zero and non-mutating.
- Persisted state contains hashes only, while stable IDs, evidence, scope, and ordinary clarification behavior remain intact.
</success_criteria>

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-11-SUMMARY.md` when done.
</output>
