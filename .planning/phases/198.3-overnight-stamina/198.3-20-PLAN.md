---
phase: 198.3-overnight-stamina
plan: 20
type: execute
wave: 2
depends_on:
  - 198.3-17
files_modified:
  - cmd/autopilot_lessons.go
  - cmd/autopilot_lessons_test.go
  - cmd/compatibility_cmds.go
  - cmd/run_autopilot_198_3_test.go
autonomous: true
requirements:
  - STAM-03
gap_closure: true
must_haves:
  truths:
    - "Completed-phase cadence survives bounded and interrupted aether run invocations."
    - "A due boundary missed between durable phase advancement and replan persistence is reconstructed before another build dispatches."
    - "Cadence resets only when a new plan revision is accepted; process restart, note resolution, and --max-phases do not reset it."
    - "Replan still requires both a cadence boundary and at least one unique confirmed post-revision lesson."
  artifacts:
    - path: "cmd/autopilot_lessons.go"
      provides: "Revision-scoped durable cadence reconstruction"
      contains: "autopilotReplanCadence"
    - path: "cmd/compatibility_cmds.go"
      provides: "Pre-dispatch and post-advance use of the same cadence evaluator"
      contains: "evaluateAutopilotReplan"
    - path: "cmd/run_autopilot_198_3_test.go"
      provides: "Two-invocation and interrupted-boundary public regressions"
  key_links:
    - from: "cmd/autopilot_lessons.go"
      to: "pkg/colony/colony.go"
      via: "current completed phase IDs are compared with the active immutable PlanRevision snapshot"
      pattern: "PlanRevision|PhaseCompleted"
    - from: "cmd/autopilot_lessons.go"
      to: "cmd/pending_decision.go"
      via: "the active revision's latest replan checkpoint records the last handled cadence boundary"
      pattern: "LatestCheckpointPhase|PlanRevisionID"
    - from: "cmd/compatibility_cmds.go"
      to: "cmd/autopilot_lessons.go"
      via: "live and dry-run routes consume one pure revision-scoped cadence evaluation"
      pattern: "evaluateAutopilotReplan"
---

<objective>
Make replan cadence durable and reconstructible across run restarts.

Purpose: Ensure bounded or interrupted overnight invocations cannot indefinitely skip D-10's completed-phase checkpoint, while continuing to require confirmed lessons and never rewriting the plan automatically.
Output: Revision-derived cadence state, catch-up handling before dispatch, and public two-invocation tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
@.planning/phases/198.3-overnight-stamina/198.3-04-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-14-SUMMARY.md

<interfaces>
`PlanRevision.Phases` is the immutable accepted-plan snapshot. `PendingDecision.PlanRevisionID`, `FirstCheckpointPhase`, and `LatestCheckpointPhase` already persist which revision and phase boundary a replan note handled. Use those durable facts; do not add a process-local counter store.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconstruct revision-scoped cadence from plan and decision history</name>
  <files>cmd/autopilot_lessons.go, cmd/autopilot_lessons_test.go</files>
  <read_first>
    cmd/autopilot_lessons.go
    cmd/autopilot_lessons_test.go
    cmd/pending_decision.go
    cmd/plan_revision.go
    pkg/colony/colony.go
    cmd/plan_revision_test.go
    .planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
    .planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
  </read_first>
  <behavior>
    - One newly completed phase under revision R yields durable count 1 in every process; two yield count 2 and the interval-2 boundary.
    - A current-revision replan note at the second phase marks boundary 2 handled even if the note is later resolved.
    - A newly accepted revision snapshots all preserved completed phases as the new baseline and resets the durable count to zero.
    - If phase 2 is durable but no replan note was persisted, boundary 2 remains due after restart.
  </behavior>
  <action>Add a pure `autopilotReplanCadence` projection that compares current `Plan.Phases` by stable phase ID/status against the active `PlanRevision.Phases` snapshot to count completions since acceptance. Derive the last handled boundary from every same-revision replan decision, including resolved rows, by mapping its `LatestCheckpointPhase` back to the revision-relative completion ordinal. Compute the earliest unhandled interval boundary and its phase ID; do not use invocation-local `phasesCompleted` for eligibility. For legacy plans without revision snapshots, use one deterministic compatibility projection and document it, but modern revision acceptance is the only reset boundary. Keep `lessonAwareReplanDue` dependent on both the due cadence projection and non-empty confirmed lessons per D-10/D-11.</action>
  <acceptance_criteria>
    - `autopilotReplanCadence` exposes completed-since-revision, last-handled boundary, next/due boundary, and the exact checkpoint phase ID without wall-clock or process-local input.
    - Same-revision resolved and unresolved replan rows preserve the handled boundary; rows for another revision do not.
    - `TestAutopilotReplanCadenceReconstructsAcrossRestart` covers counts 0/1/2, a missed persistence boundary, and a handled boundary.
    - `TestAutopilotReplanCadenceResetsOnlyOnAcceptedRevision` proves process restart and note resolution do not reset, while a new active revision snapshot does.
    - `TestLessonAwareReplanMatrix` still proves zero lessons never triggers.
    - `go test ./cmd -run 'Test(AutopilotReplanCadence|LessonAwareReplanMatrix|ReplanDecisionUpsert)' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(AutopilotReplanCadence|LessonAwareReplanMatrix|ReplanDecisionUpsert)' -count=1</automated>
  </verify>
  <done>Replan interval progress is reproducible from accepted-plan and decision history, and only a new accepted revision resets it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce reconstructed cadence before dispatch and across bounded invocations</name>
  <files>cmd/compatibility_cmds.go, cmd/run_autopilot_198_3_test.go, cmd/autopilot_lessons.go</files>
  <read_first>
    cmd/compatibility_cmds.go
    cmd/run_autopilot_198_3_test.go
    cmd/autopilot_lessons.go
    cmd/autopilot_lessons_test.go
    cmd/autopilot_report.go
    cmd/pending_decision.go
    .planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
    .planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
  </read_first>
  <behavior>
    - Two separate `aether run --max-phases 1` invocations under interval 2 queue exactly one revision-scoped replan note after the second durable completion.
    - Restart after phase 2 persisted but before note persistence catches boundary 2 before phase 3 build dispatch.
    - Headless queues and continues; interactive pauses; `--continue` bypasses without falsely marking the boundary handled.
    - Dry-run projects the same boundaries without mutating state.
  </behavior>
  <action>Replace the live call's `phasesCompleted` cadence input with the durable projection. Evaluate an unhandled due boundary at the READY/pre-build edge as well as immediately after a successful advance, using one helper so an interruption between state persistence and note upsert cannot lose the event. Headless calls `upsertAutopilotReplanDecision` with the exact due boundary phase and continues; interactive returns `replan_due` before dispatch; `--continue` bypasses the event without persisting it as handled. Make preview advance a cloned plan and use the same pure cadence rules, with synthetic handled boundaries only inside the preview. Keep the invocation-local counter solely for this invocation's morning-report count per D-18, not replan eligibility.</action>
  <acceptance_criteria>
    - `evaluateAutopilotReplan` no longer decides cadence from invocation-local `phasesCompleted`.
    - `TestRunReplanCadencePersistsAcrossBoundedInvocations` executes two real max-one-phase calls and finds one same-revision note with checkpoint phase 2 after the second.
    - `TestRunReplanCadenceCatchesInterruptedBoundaryBeforeDispatch` seeds durable phase-2 completion without a note, restarts, and proves no phase-3 build occurs before the replan disposition is handled.
    - Existing `TestRunHeadlessQueuesVisualRuntimeAndReplanThenCompletes` and dry-run parity tests remain green.
    - `go test ./cmd -run 'TestRun(ReplanCadence|HeadlessQueuesVisualRuntimeAndReplanThenCompletes)|Test.*DryRun.*Replan' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestRun(ReplanCadence|HeadlessQueuesVisualRuntimeAndReplanThenCompletes)|Test.*DryRun.*Replan' -count=1</automated>
  </verify>
  <done>Bounded and interrupted invocations cannot erase a due replan checkpoint, and headless/interactive/dry-run dispositions remain aligned.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(AutopilotReplan|LessonAwareReplan|ReplanDecision|Run.*Replan|.*DryRun.*Replan)' -count=1`
- `go vet ./cmd/...`
</verification>

<success_criteria>
- Cadence survives restart and catches an unhandled durable boundary before further dispatch.
- Only accepted plan revision resets cadence.
- Replan still requires both interval and confirmed lessons.
- Headless queues, interactive pauses, and dry-run accurately projects the same policy.
</success_criteria>

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-20-SUMMARY.md` when done.
</output>
