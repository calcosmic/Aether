---
phase: 198.3-overnight-stamina
plan: 21
subsystem: orchestration
tags: [swarm, external-finalize, evidence-validation, strike-repair, tdd]

# Dependency graph
requires:
  - phase: 198.3-07
    provides: External swarm manifest/finalization protocol and terminal worker evidence
  - phase: 198.3-16
    provides: Stable same-target escalation identity and target-bound corrective recovery epoch
provides:
  - Exact pre-mutation binding of external results to manifest-owned worker identity
  - Shared terminal-status validation for external swarm evidence
  - Idempotent repair of a missing third-strike escalation flag from durable history
  - Public proof that repaired flags authorize the supported corrective-phase recovery flow
affects: [swarm-finalize, worker-dispatch, strike-escalation, insert-phase, overnight-stamina]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Pure validate-and-project boundary before durable finalizer mutation
    - Durable result history as the source of truth for idempotent escalation repair
    - Fail-closed re-read after cross-artifact repair

key-files:
  created: []
  modified:
    - cmd/swarm_cmd.go
    - cmd/swarm_cmd_test.go
    - cmd/swarm_strikes.go
    - cmd/swarm_strikes_test.go

key-decisions:
  - "Treat manifest name, caste, role, and task as immutable identity; submitted identity fields may only confirm, never replace, them."
  - "Use the dispatch contract's six terminal statuses as the single external-finalization allowlist and reject every other state before mutation."
  - "Repair a missing escalation from the three durable results, then re-read the exact stable flag before returning guidance or allowing any dispatch lane."

patterns-established:
  - "External evidence boundary: validate manifest shape, exact cardinality, immutable identity, and terminal status before initializing any runtime artifact."
  - "Partial-write recovery: derive the missing secondary artifact from durable primary history, upsert by stable identity, and fail closed unless a confirming read succeeds."

requirements-completed: [STAM-01, STAM-05]

# Metrics
duration: 15min
completed: 2026-09-01
---

# Phase 198.3 Plan 21: Exact Swarm Evidence and Escalation Repair Summary

**External swarm results are now bound exactly to their manifest before any write, while a durable third strike can safely recreate its one missing escalation flag and continue through the supported corrective recovery flow.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-09-01T17:59:32Z
- **Completed:** 2026-09-01T18:14:57Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Moved external result validation ahead of spawn-run, swarm, midden, finding, result, flag, and strike mutation.
- Enforced exact worker cardinality and manifest-owned identity, including duplicate, surplus, missing, forged, role-mismatch, and non-terminal rejection with exhaustive pre/post data-tree checks.
- Reused the published six-value terminal allowlist and made unexpected status summarization an explicit error instead of implicit success.
- Added an idempotent three-strike repair seam that fails closed while flag storage is unavailable, creates no fourth result, dispatches no worker, and preserves the target-bound recovery epoch.

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: Add failing exact external evidence tests** - `a13e7351` (test)
2. **Task 1 GREEN: Validate external evidence before mutation** - `cfa66486` (feat)
3. **Task 2 RED: Add failing partial escalation repair tests** - `0aca92e2` (test)
4. **Task 2 GREEN: Repair partial swarm escalations** - `0a336ecd` (fix)

_TDD tasks intentionally use separate RED and GREEN commits._

## Files Created/Modified

- `cmd/swarm_cmd.go` - Validates and projects exact manifest-bound external results before finalizer mutation, and repairs three-strike escalation before concern return.
- `cmd/swarm_cmd_test.go` - Covers the full external rejection matrix, all terminal statuses, and the repaired public corrective-phase flow.
- `cmd/swarm_strikes.go` - Ensures the exact stable escalation exists after a third strike and confirms it with a durable re-read.
- `cmd/swarm_strikes_test.go` - Injects the result-written/flag-failed split, then proves fail-closed retry, idempotency, unchanged result history, and zero dispatch.

## Decisions Made

- Kept all external outcome data subordinate to the manifest: immutable identity comes from the dispatch plan, while only validated outcome fields come from the completion payload.
- Centralized terminal status values so the published host contract and runtime validator cannot drift.
- Used the append-only result history as repair truth rather than adding a mutable transaction marker or counter.
- Required a successful post-upsert read of the exact target-bound flag, so a repair attempt cannot return actionable guidance on an unconfirmed write.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Verification

- `go test ./cmd -run 'TestSwarmFinalize(RejectsUnboundOrNonTerminalEvidenceWithoutMutation|RecordsExternalTaskResults)' -count=1` - 22 passed during Task 1.
- `go test ./cmd -run 'TestSwarm(ThirdStrikeRepairsEscalationAfterPartialPersistenceFailure|PartialEscalationRepairPublicFlow|ThreeStrikeRecoveryExactTargetRetryReEscalates|ThreeStrikeReplayKeepsOneEscalation)' -count=1` - 4 passed during Task 2.
- `go test ./cmd -run 'TestSwarm(Finalize|Strike|ThreeStrike|PartialEscalation|Recovery)' -count=1` - 57 passed at the plan gate.
- `go vet ./cmd/...` - passed with no findings.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Both remaining Plan 21 gaps from the phase verifier are closed with adversarial and fault-injection coverage.
- No blockers remain for the orchestrator's shared tracking update and phase verification refresh.

## Self-Check: PASSED

- All four implementation/test files and this summary exist in the assigned worktree.
- All four Task 1/Task 2 RED and GREEN commits are present in git history.
- Shared `STATE.md`, `ROADMAP.md`, and `REQUIREMENTS.md` tracking artifacts remain untouched for the orchestrator.

---
*Phase: 198.3-overnight-stamina*
*Completed: 2026-09-01*
