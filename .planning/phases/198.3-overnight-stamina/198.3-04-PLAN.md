---
phase: 198.3-overnight-stamina
plan: 04
type: execute
wave: 3
depends_on: ["198.3-01", "198.3-02", "198.3-03"]
files_modified:
  - cmd/autopilot_lessons.go
  - cmd/autopilot_lessons_test.go
  - cmd/pending_decision.go
  - cmd/compatibility_cmds.go
  - cmd/run_visuals.go
  - cmd/run_autopilot_198_3_test.go
autonomous: true
requirements: [STAM-01, STAM-02, STAM-03, STAM-05]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "D-08: Phase 198.3 explicitly supersedes the old headless queue-then-stop rule and restores the optional-Auditor score floor; unaffected historical mappings remain only where the current typed catalogue retains them."
    - "D-10/D-11: replan becomes due only at the configured interval and with at least one unique, evidence-confirmed lesson since the active plan revision."
    - "D-09/D-12: headless replan upserts one pending note and continues; interactive pauses; the note records lesson count and first/latest checkpoints without rewriting the plan."
    - "D-01/D-02/D-03: the live run loop enforces plan 01's typed dispositions: owner checkpoints queue headlessly, genuine blockers stop, and cancel/timeout/max/complete are named normal stops."
    - "D-13/D-15: the whole build is never retried by run; provider readiness owns its timeout-only retry and unfinished work resumes the same phase."
  artifacts:
    - cmd/autopilot_lessons.go
    - integrated typed-policy `runCompatibilityAutopilot`
    - replan pending-decision metadata/upsert
  key_links:
    - "captured learning evidence + plan revision -> lesson selector -> replan trigger -> pending decision"
    - "plan 02 current-result signals + plan 01 blocker snapshots/specs -> live stage evaluation -> mode disposition"
    - "plan 03 readiness error/state -> single terminal classification; no whole-build retry"
  prohibitions:
    - statement: "Headless queue-and-continue must not call the terminal pause helper afterwards."
      status: required
      verification: multi-phase focused test advances after each queueable condition
    - statement: "Do not read stale global gate/midden/signal history to decide current review or verification failure."
      status: required
      verification: stale-store fixture does not stop; current-result fixture does
    - statement: "Autopilot must never invoke seal."
      status: required
      verification: invoker call log contains no seal command
---

<objective>
Turn the typed evidence from plans 01–03 into the real overnight control loop and make replanning evidence-aware.

Purpose: this is the behavioral heart of the phase. It replaces “queue then stop” and broad stale pause scans with the locked stop/queue/normal-stop matrix, while keeping each phase resumable.

Output: a headless loop that continues through owner/replan work, stops for genuine danger, and has no broad whole-build retry. Plan 08 removes the now-unused legacy command adapters after this replacement is proven.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-RESEARCH.md
@.planning/phases/198.3-overnight-stamina/198.3-PATTERNS.md
@.planning/phases/198.3-overnight-stamina/198.3-01-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-02-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Select confirmed lessons since the active plan revision and upsert one replan decision</name>
  <files>cmd/autopilot_lessons.go, cmd/autopilot_lessons_test.go, cmd/pending_decision.go, cmd/compatibility_cmds.go, cmd/run_autopilot_198_3_test.go</files>
  <read_first>
    - cmd/codex_continue_finalize.go `captureContinueLearning` and the exact learning/evidence types it persists
    - pkg learning/memory entry types, status vocabulary, and normalized content hashing helpers
    - colony plan revision type and `Plan.GeneratedAt` fallback
    - cmd/pending_decision.go plus plan 02 metadata/upsert changes
    - cmd/compatibility_cmds.go current modulus-only replan branch around line 647
    - cmd/run_autopilot_test.go current replan tests and fixture clock/state setup
  </read_first>
  <behavior>
    - Eligible lessons have phase > 0, non-empty content, non-blocked/non-disproven status, evidence after active plan revision, non-empty completed worker evidence, and all recorded gates passed with `GatesTotal > 0`.
    - Normalized duplicate content counts once; raw observations, pre-revision entries, incomplete workers, and missing/failing gates do not count.
    - Replan is due only when interval checkpoint is reached and eligible count is positive.
    - Headless mode upserts one unresolved replan decision per active plan revision and continues; repeated checkpoints update lesson count and first/latest phase metadata. Interactive mode pauses with the same evidence. A new revision supersedes/resolves the stale note.
  </behavior>
  <action>
    Add failing table tests for each eligibility dimension and normalized duplicate content. Seed learning through the real entry/evidence writer shape used by `captureContinueLearning`, not a planner-only mock. Add replan matrix tests for interval/no lesson, lesson/not interval, both, headless replay/upsert, interactive pause, and new plan revision.

    Implement `confirmedAutopilotLessonsSincePlan` as a pure selector in `cmd/autopilot_lessons.go`. Determine the active revision boundary from the latest successful plan revision timestamp/ID and fall back to `Plan.GeneratedAt` only for old state. Sort deterministically and deduplicate by the repository's normalized content hash. Return evidence-rich lesson summaries rather than a bare count.

    Replace the phase-count-only due predicate with `interval reached AND len(confirmedLessons)>0`. Add/upsert one typed `replan` pending decision for the active revision using plan 02's optional metadata: revision identity, unique lesson count, first checkpoint phase, latest checkpoint phase, and compact lesson references. Never call plan or mutate the current plan. Preserve `--continue` as the explicit bypass.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestConfirmedAutopilotLessons|TestLessonAwareReplan|TestReplanDecisionUpsert|TestRun.*Replan' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Each excluded evidence case has its own named subtest; only fully proven post-revision entries are returned.
    - Duplicate normalized content contributes one lesson.
    - Interval-only and lesson-only cases do not queue/pause; both conditions do.
    - Three headless due checkpoints for one revision leave one unresolved decision whose first/latest metadata spans all checkpoints.
    - No test or production path promotes habits or counts raw observations to make the trigger fire.
  </acceptance_criteria>
  <reversibility rating="reversible">The selector reads existing learning evidence and adds optional pending-decision metadata; it never rewrites the plan.</reversibility>
  <done>Replan work is raised only when there is something proven to replan around, and the owner gets one accumulating note rather than duplicates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the canonical mode policy in the live run loop</name>
  <files>cmd/compatibility_cmds.go, cmd/run_visuals.go, cmd/run_autopilot_198_3_test.go</files>
  <read_first>
    - cmd/compatibility_cmds.go `runCompatibilityAutopilot` in full, including all early returns and the current unconditional second build attempt
    - cmd/run_visuals.go pause/queue renderers from plan 01
    - cmd/autopilot_policy.go and cmd/blocker_snapshot.go from plan 01
    - plan 02 continue/build `autopilot_signals` and pending-decision IDs
    - plan 03 provider readiness error classification and phase-scoped cache behavior
    - cmd/autopilot.go in full (state fields, broad scanner, and seven public adapters that plan 08 will remove once this loop no longer calls them)
    - cmd/run_autopilot_test.go and cmd/autopilot_pause_test.go as existing behavior coverage; add phase-specific cases in the new focused test file
  </read_first>
  <behavior>
    - Before a phase, ordinary existing blockers are shown/counted and work proceeds; any escalation is a genuine stop. After build/continue, blocker count increase or escalation stops.
    - Headless visual/runtime/replan triggers persist/upsert their decisions and continue. Interactive mode pauses. Queueing never falls through to terminal pause.
    - Current deterministic failure, Auditor score below 60, Critical finding, non-runnable state, and exhausted/immediate provider unavailability stop with exact same-phase recovery.
    - Cancel, worker timeout, max phases, and completion are named normal stops. Timeout/cancel preserve completed state and do not switch/skip.
    - Run calls build once per phase attempt; provider preflight owns its own timeout retry. Run never calls seal.
    - The live loop has no call to `checkAutopilotPauseConditions`; plan 08 may therefore remove the broad scanner and its seven orphan adapters without changing live behavior.
  </behavior>
  <action>
    Create `cmd/run_autopilot_198_3_test.go` and add focused run-loop tests before production edits. Use `seedRunFixture` and a recording fake invoker for: ordinary blocker baseline; baseline escalation; count increase; stale failed gate/midden/signal ignored; current deterministic failure; Auditor 59/60/no Auditor; Critical/High; headless visual/runtime/replan continuation across a next phase; interactive pauses; provider timeout/auth; cancel; max; complete; and no seal. Assert build invocation count is one on generic build error so restoring the outer retry fails.

    Refactor `runCompatibilityAutopilot` into stage evaluation plus disposition handling using plan 01 specs, snapshots, plan 02 current-result signals/checkpoint helpers, and Task 1 lessons. Immediately after successful build, pass its real claims path/result to plan 02's visual-checkpoint materializer before evaluating the post-build stage. Take a blocker baseline before dispatch and compare after build/continue. For `queue_and_continue`, require the pending-decision upsert to succeed, append a non-terminal step, render a queue line, and continue. For stop/normal-stop choose typed reason/next action and preserve exact current phase. Keep final report details minimal for now; plan 05 centralizes terminal reporting.

    Delete the unconditional “retry whole build after any error” branch. Do not add provider switching or phase skipping. Ensure every error mapping distinguishes provider-unavailable, worker-timeout/cancel, and non-runnable state with the catalogue codes rather than stringifying all as `blocked`.

    Leave the now-unused legacy scanner/adapters intact for this integration commit; plan 08 owns their deletion and the generated command-surface update. Add a source/call-path assertion that `runCompatibilityAutopilot` no longer calls the broad scanner so cleanup is behavior-neutral.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestRun.*(Headless|Interactive|Blocker|Auditor|Critical|High|Provider|Timeout|Cancel|Max|Complete|NoSeal|NoWholeBuildRetry)|TestAuditCatalogGolden|TestPlatformParityGolden|TestOrphanAllowlist' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - A headless fixture reaches phase 2 after visual, runtime, and replan decisions; switching any one spec to stop makes a named test fail.
    - Ordinary blocker baseline proceeds; count increase and escalation stop; stale historical gate/midden/signal fixture proceeds.
    - Generic build failure records one build invocation, never two.
    - Every terminal result carries one canonical trigger code and exact same-phase/next command; no invoker log contains `seal` or provider switch/phase skip.
    - A call-path/source assertion proves the live run loop no longer invokes `checkAutopilotPauseConditions`.
    - Targeted run and policy tests exit 0.
  </acceptance_criteria>
  <reversibility rating="costly">This replaces live stop behavior; the phase-specific tests make rollback explicit while public adapter deletion remains isolated in plan 08.</reversibility>
  <done>The real run loop now behaves like an overnight runner: it records owner work and keeps going, but stops deterministically when continuing is unsafe or impossible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|---|---|---|
| current result/stores -> unattended disposition | Stale or spoofed evidence could stop/continue incorrectly | Current reviewer/floor signals are typed; global read limited to before/after blocker snapshots; stable catalogue codes |
| learning store -> replan request | Unverified observations could drive plan churn | Require post-revision completed workers and all gates passed; dedup content |
| queue persistence -> continued execution | Failed queue write could lose owner work while continuing | Queue/upsert must succeed before `continue`; write error is non-runnable stop |
| timeout/cancel -> retry | Broad retry could duplicate side effects | Remove whole-build retry; provider layer alone owns timeout readiness retry; call-count tests |
</threat_model>

<verification>
- `go test ./cmd/ -run 'ConfirmedAutopilotLessons|LessonAwareReplan|Run.*(Headless|Interactive|Blocker|Auditor|Critical|High|Provider|Timeout|Cancel|Max|Complete|NoSeal|NoWholeBuildRetry)' -count=1`
- `go build ./cmd/aether`
- `go vet ./...`
</verification>

<success_criteria>
The live runner uses the canonical catalogue and current evidence, queues owner/replan work headlessly, pauses it interactively, stops genuine blockers, preserves exact-phase recovery, and never retries whole work or seals. The legacy scanner is proven unreachable from this loop, ready for plan 08 cleanup.
</success_criteria>

## Artifacts this phase produces (Plan 04)

| Symbol/path | Kind |
|---|---|
| confirmed-lesson selector | new pure evidence reader |
| one-per-revision replan decision/upsert | new durable behavior |
| typed live run stage evaluation | changed core behavior |
| removal of whole-build retry and broad-scanner call | deleted unsafe live behavior |

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-04-SUMMARY.md` when done.
</output>
