---
phase: 198.3-overnight-stamina
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/autopilot_policy.go
  - cmd/autopilot_policy_test.go
  - cmd/blocker_snapshot.go
  - cmd/blocker_snapshot_test.go
  - cmd/compatibility_cmds.go
  - cmd/run_visuals.go
  - cmd/flag_cmds.go
  - cmd/status.go
autonomous: true
requirements: [STAM-02, STAM-05]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-03/D-07: one typed catalogue names every genuine stop, queueable checkpoint, and normal stop; dry-run renders that catalogue rather than a hand-written copy."
    - "D-06: existing ordinary blockers are counted and shown without becoming an automatic stop; a count increase or escalation is distinguishable from the baseline."
    - "STAM-05: flag-check-blockers, run policy inputs, and status all read one blocker snapshot implementation."
  artifacts:
    - cmd/autopilot_policy.go
    - cmd/blocker_snapshot.go
    - cmd/autopilot_policy_test.go
    - cmd/blocker_snapshot_test.go
  key_links:
    - "autopilotTriggerSpecs -> run --dry-run output and later run enforcement"
    - "readBlockerSnapshot -> flag-check-blockers + status + later run baseline/post comparison"
  prohibitions:
    - statement: "Rendered prose must never be parsed to decide whether the run stops."
      status: required
      verification: tests compare stable trigger codes and dispositions
    - statement: "The legacy broad pause scanner must not become the new blocker snapshot."
      status: required
      verification: focused tests exclude signals, stale gates, midden entries, ordinary issues, and pending owner checkpoints
---

<objective>
Create the two shared truths every later overnight behavior depends on: a typed stop/queue/normal-stop catalogue and a live blocker snapshot.

Purpose: today dry-run, status, `flag-check-blockers`, and the run loop can disagree because each assembles its own strings or counts. This plan makes the rule names and flag counts reusable before the run loop starts enforcing them in plan 04.

Output: stable trigger codes/dispositions rendered by dry-run, plus one blocker reader used by both the diagnostic command and status.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-RESEARCH.md
@.planning/phases/198.3-overnight-stamina/198.3-PATTERNS.md
@.planning/decisions/autopilot-pause-conditions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define one canonical overnight trigger catalogue and render dry-run from it</name>
  <files>cmd/autopilot_policy.go, cmd/autopilot_policy_test.go, cmd/compatibility_cmds.go, cmd/run_visuals.go</files>
  <read_first>
    - cmd/compatibility_cmds.go lines 210-330 and 388-730 (run options, dry-run result, live loop, and its current independent reason strings)
    - cmd/run_visuals.go lines 1-180 (run ceremony, reason humanisation, and the current always-terminal pause helper)
    - cmd/autopilot.go lines 1-105 (legacy broad pause scan; use it as evidence of what must not be copied)
    - cmd/preflight_cache.go (closest repository analogue for a typed policy/helper below Cobra adapters, with rendering kept outside the policy)
    - `.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md` D-01 through D-09 (the binding disposition matrix)
    - repository tag `v5.3.3` `.aether/commands/run.yaml` (historical names only; current CONTEXT wins on disposition and score floor)
  </read_first>
  <behavior>
    - The catalogue contains stable codes for deterministic verification failure, Auditor score below 60, Critical finding, blocker-count increase, blocker escalation, non-runnable state, provider unavailable, runtime verification, visual checkpoint, replan due, cancelled, worker timeout, max phases, and colony complete.
    - Each entry carries a plain-English label, detection description, exact next-action template, headless disposition, and interactive disposition.
    - Runtime verification, visual checkpoint, and replan are `queue_and_continue` headlessly and `pause` interactively. Genuine blockers are `stop`; cancel/timeout/max/complete are `normal_stop`.
    - `aether run --dry-run` lists every catalogue code/name and both dispositions from the catalogue itself.
  </behavior>
  <action>
    Write `cmd/autopilot_policy_test.go` first. Add a table that pins the complete set of trigger codes, and a disposition table for both modes. Add a structural test that obtains the dry-run contract and asserts its trigger codes equal `autopilotTriggerSpecs()` exactly; it must fail if a hand-written dry-run row is added or a catalogue row is omitted.

    Implement `cmd/autopilot_policy.go` with typed disposition constants, `autopilotTriggerSpec`, `autopilotTriggerEvaluation`, stable code constants, and one deterministic `autopilotTriggerSpecs()` slice. Keep the catalogue descriptive: stage-specific evidence evaluation lands in plans 02/04. Add lookup/validation helpers only where tests need them; reject duplicate/blank codes and unknown dispositions.

    Refactor the dry-run branch/result in `runCompatibilityAutopilot` to project its pause/queue contract from the catalogue. Update `run_visuals.go` to render the structured rows in plain English. Preserve existing dry-run phase/build/continue preview and do not change live enforcement in this task. Delete any independent dry-run trigger list made redundant by the catalogue.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestAutopilotTriggerCatalogue|TestRunDryRunUsesCanonicalTriggerCatalogue|TestAutopilotDispositionMatrix' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The catalogue has no duplicate or blank code and every entry has both mode dispositions, detection text, and a next-action template.
    - A test compares the exact set/order of dry-run trigger codes to `autopilotTriggerSpecs()`; adding a trigger to either side alone fails it.
    - Score-floor text says below 60, and no catalogue entry implies an Auditor must be dispatched.
    - `go test ./cmd/ -run 'TestRun.*DryRun|TestAutopilot.*Catalogue|TestAutopilot.*Disposition' -count=1` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">This adds a typed policy description and changes dry-run projection only; live enforcement is integrated later.</reversibility>
  <done>Dry-run and future enforcement have one complete, test-pinned vocabulary for every stop and queue condition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make one live blocker snapshot serve the diagnostic command and status</name>
  <files>cmd/blocker_snapshot.go, cmd/blocker_snapshot_test.go, cmd/flag_cmds.go, cmd/status.go</files>
  <read_first>
    - cmd/flag_cmds.go lines 1-270 (flag store conventions and `flag-check-blockers` handler)
    - cmd/status.go lines 1320-1405 (`countFlags` and current status projection)
    - cmd/flags_test.go around `TestFlagCheckBlockers` (runtime-shaped flag seeding)
    - cmd/status_test.go and cmd/status_ux_test.go (JSON versus visual status contracts)
    - pkg/colony flag types and the writers that set `Source: "escalation"`
  </read_first>
  <behavior>
    - The snapshot counts unresolved blocker flags, returns their stable sorted IDs, and separately counts/returns unresolved blockers whose source is `escalation`.
    - Resolved blockers, issues, notes, pending owner decisions, signals, gate history, and midden history are not blocker-snapshot entries.
    - `flag-check-blockers` and status return the same ordinary/escalated counts for the same seeded store.
    - Visual status names the escalated count in plain English; JSON exposes the same snapshot fields.
  </behavior>
  <action>
    Add failing table tests in `cmd/blocker_snapshot_test.go` for empty/missing flags, resolved rows, ordinary blockers, escalation source, issues/notes, and deterministic ID ordering. Use the existing flag writer or `writeTestFlags`; do not handwrite an invented storage schema. Add a baseline/post comparison helper whose output distinguishes `count_increased` and `escalation_added` without treating a same-count ID replacement as a count increase.

    Implement a pure `readBlockerSnapshot(*storage.Store)` and comparison type in `cmd/blocker_snapshot.go`. Refactor `flag-check-blockers` and `countFlags`/status to call it. Preserve the diagnostic command's existing compatibility fields, adding escalated count/IDs without breaking JSON consumers. Add status JSON and visual tests proving both consumers agree byte-for-field on counts and that existing ordinary blockers are described as existing work rather than an automatic run stop.

    Do not modify `checkAutopilotPauseConditions` here; plan 04 removes it from the live loop. Do not read pending decisions or stale gate/midden data in the new helper.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestBlockerSnapshot|TestFlagCheckBlockers|TestStatus.*Blocker|TestStatus.*Escalat' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - One production function reads unresolved blocker/escalation truth; both `flag-check-blockers` and status call it.
    - A fixture with 2 blockers (1 escalation), 1 resolved blocker, 1 issue, and 1 pending owner decision reports blocker=2 and escalated=1 in diagnostic JSON and status JSON/visual.
    - A same-count ordinary blocker ID replacement does not set `count_increased`; adding one blocker does.
    - `go test ./cmd/ -run 'FlagCheckBlockers|BlockerSnapshot|Status' -count=1` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">This consolidates duplicate read logic without changing the flag storage format.</reversibility>
  <done>Run, status, and the diagnostic command can now consume the same deterministic blocker baseline, including escalation truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|---|---|---|
| persisted flags -> unattended stop policy | Stale/non-blocker records could halt the night | Snapshot accepts only unresolved `Type == blocker`; tests exclude every neighboring store/type |
| policy catalogue -> user understanding | Dry-run could claim behavior different from enforcement | Stable typed slice is the only source; structural equality test pins dry-run projection |
| arbitrary flag IDs/descriptions -> terminal output | Control characters or unstable order could corrupt display/snapshots | Reuse stored sanitized fields, sort IDs, and render through existing visual helpers |
</threat_model>

<verification>
- `go test ./cmd/ -run 'AutopilotTriggerCatalogue|RunDryRun|BlockerSnapshot|FlagCheckBlockers|Status.*Blocker|Status.*Escalat' -count=1`
- `go test ./cmd/ -run 'TestRunCompatibility|TestStatus' -count=1`
- `go build ./cmd/aether`
- `go vet ./...`
</verification>

<success_criteria>
There is one complete trigger catalogue behind dry-run and one live blocker snapshot behind `flag-check-blockers` and status. The types expose enough evidence for plans 02/04 to enforce the locked overnight policy without parsing strings or rescanning stale stores.
</success_criteria>

## Artifacts this phase produces (Plan 01)

| Symbol/path | Kind |
|---|---|
| `autopilotTriggerSpec`, `autopilotTriggerEvaluation`, disposition/code constants | new policy types |
| `autopilotTriggerSpecs()` | canonical catalogue |
| `blockerSnapshot` and comparison helper | shared live-state projection |
| dry-run catalogue projection | changed behavior |
| escalated blocker fields in diagnostic/status results | changed behavior |
| focused catalogue/blocker/status tests | regression proof |

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-01-SUMMARY.md` when done.
</output>
