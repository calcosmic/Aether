---
phase: 198.3-overnight-stamina
plan: 08
type: execute
wave: 5
depends_on: ["198.3-05"]
files_modified:
  - cmd/autopilot.go
  - cmd/autopilot_pause_test.go
  - cmd/autopilot_test.go
  - cmd/compatibility_cmds_test.go
  - cmd/midden_unification_test.go
  - cmd/testdata/command_catalog.json
  - cmd/testdata/parity_snapshot.json
  - cmd/testdata/regression_snapshot.json
  - cmd/testdata/orphan_allowlist.json
autonomous: true
requirements: []
audit_scope: [orphan-autopilot-commands]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The seven legacy autopilot management subcommands and broad pause scanner are removed only after plan 04 proves the live run loop no longer calls them."
    - "Useful autopilot state/report types remain; this plan removes duplicate CLI surface, not lifecycle data."
    - "Supported autopilot lifecycle is `aether run` plus `aether status`."
    - "Command fixtures shrink by the seven removals in this plan; plan 09 performs the second honest shrink after its four recovery/Medic removals."
  artifacts:
    - reduced cmd/autopilot.go
    - state-compatibility coverage in cmd/autopilot_pause_test.go
    - staged command catalog/parity/regression/current orphan fixtures
  key_links:
    - "plan 04 live run policy -> no broad scanner/legacy autopilot adapter dependency -> safe deletion"
    - "retained autopilot state/report types -> aether run + aether status"
    - "root runtime catalog after seven removals -> four generated command-surface fixtures"
  prohibitions:
    - statement: "Do not delete autopilot state/report types used by run or status."
      status: required
      verification: old/new state fixture plus live run/status tests
    - statement: "Do not modify orphan_allowlist_baseline.json or hand-edit generated JSON."
      status: required
      verification: owning generator flags, exact diff, baseline git-diff assertion
---

<objective>
Close the audited duplicate autopilot command surface after the replacement behavior is already integrated and tested.

Purpose: Aether should have one understandable autopilot lifecycle, not seven orphan commands that expose a second, incomplete way to manipulate the same state.

Output: behavior-neutral autopilot adapter/scanner deletion, retained compatible state, and a clean staged command-inventory shrink. Plan 09 applies the remaining recovery/Medic shrink.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-RESEARCH.md
@.planning/phases/198.3-overnight-stamina/198.3-PATTERNS.md
@.planning/phases/198.3-overnight-stamina/198.3-04-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete the unreachable broad pause scanner and seven legacy autopilot adapters</name>
  <files>cmd/autopilot.go, cmd/autopilot_pause_test.go, cmd/autopilot_test.go, cmd/compatibility_cmds_test.go, cmd/midden_unification_test.go</files>
  <read_first>
    - cmd/autopilot.go in full after plans 04/05 (separate state/report types used by run/status from Cobra-only declarations)
    - cmd/run_autopilot_198_3_test.go plan 04 proof that the live loop uses typed current evidence and never calls `checkAutopilotPauseConditions`
    - cmd/autopilot_pause_test.go command-only tests versus any reusable state tests
    - cmd/autopilot_test.go and cmd/compatibility_cmds_test.go legacy command invocations versus supported run/state assertions
    - cmd/midden_unification_test.go old broad-scanner assertion; preserve midden writer truth while removing the obsolete autopilot coupling
    - repository-wide callers for `autopilotInitCmd`, `autopilotUpdateCmd`, `autopilotStatusCmd`, `autopilotStopCmd`, `autopilotSetHeadlessCmd`, `autopilotHeadlessCheckCmd`, `autopilotResumeCmd`, and `checkAutopilotPauseConditions`
    - cmd/root registration conventions so imports/flags are removed cleanly
  </read_first>
  <behavior>
    - Runtime registers none of the seven legacy autopilot command names.
    - `aether run` and status still load/write the same compatible autopilot state/report.
    - No broad scanner reads pending decisions, old signals/gates, midden, or uncommitted markers for live run policy.
    - Command-only tests disappear or become direct state compatibility tests without resurrecting the adapters.
  </behavior>
  <action>
    Before deleting, run/capture repository caller search and plan 04 focused tests. Add a source/call-audit assertion if one does not already exist: every legacy command/scanner symbol must have no production caller outside its own declaration/registration.

    Remove the seven Cobra command values, their flags/registration `init`, `checkAutopilotPauseConditions`, and imports used only by those declarations. Retain `autopilotState`, report fields, `autopilotStatePath`, and any normalization/load helpers proven live by run/status. Delete or refactor legacy command cases across `autopilot_pause_test.go`, `autopilot_test.go`, and `compatibility_cmds_test.go` so state backward compatibility and typed run behavior remain covered by plan 04/05 tests. Refactor `midden_unification_test.go` to keep its midden write/read proof without asserting that stale midden controls autopilot.

    Do not regenerate catalog fixtures until Task 2 has proved state compatibility, so the generated diff follows the behavior proof.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestRun.*(Headless|Interactive|Blocker|Report)|TestStatus.*LastReport|TestAutopilot.*State' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Production search finds no declaration/registration for the seven command names and no `checkAutopilotPauseConditions` symbol.
    - `autopilotState`/`LastReport` old/new fixture tests and live run/status tests pass.
    - Stale gate/midden/signal fixtures still do not stop run.
    - No replacement hidden command or second scanner is introduced.
  </acceptance_criteria>
  <reversibility rating="costly">Public orphan adapters are deleted after caller proof; data state remains compatible and can be inspected through supported status.</reversibility>
  <done>The live autopilot has one control surface and no stale scanner capable of re-entering the run path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove state compatibility and regenerate the seven-command inventory shrink</name>
  <files>cmd/autopilot.go, cmd/autopilot_pause_test.go, cmd/autopilot_test.go, cmd/compatibility_cmds_test.go, cmd/midden_unification_test.go, cmd/testdata/command_catalog.json, cmd/testdata/parity_snapshot.json, cmd/testdata/regression_snapshot.json, cmd/testdata/orphan_allowlist.json</files>
  <read_first>
    - cmd/autopilot_report_test.go from plan 05 (old/new JSON state and `last_report` coverage)
    - cmd/run_autopilot_198_3_test.go from plan 04 and status report tests from plan 05
    - cmd/autopilot_pause_test.go, cmd/autopilot_test.go, cmd/compatibility_cmds_test.go, and cmd/midden_unification_test.go remaining tests after Task 1
    - cmd/audit_catalog_test.go, cmd/parity_test.go, cmd/regression_test.go, and cmd/subcommand_reachability_ratchet_test.go owning update flags
    - cmd/testdata/orphan_allowlist_baseline.json shrink-only rule (read only)
  </read_first>
  <behavior>
    - Old autopilot state without a report and new state with a report both load through run/status after the Cobra declarations are gone.
    - Headless/interative policy, exact-phase resume, and status rendering remain unchanged.
    - Runtime catalog and generated fixtures contain none of the seven names; unrelated entries/metadata stay unchanged.
  </behavior>
  <action>
    Refactor the remaining useful cases in `autopilot_pause_test.go` into direct state compatibility and live run/status tests; remove tests whose only purpose was executing deleted commands. Add a runtime-catalog assertion against `buildAuditCatalog(rootCmd)` that the seven names are absent.

    Run the old/new state, live typed-policy, report, and status suites. Regenerate `command_catalog.json`, `parity_snapshot.json`, and `regression_snapshot.json` with their owning `-update-golden` tests, and regenerate only the current `orphan_allowlist.json` with `-update-orphan-allowlist`. Inspect the diff: exactly the seven names disappear and the command-count field drops by seven; unrelated metadata is unchanged. Rerun without update flags and assert the orphan baseline has no diff.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestRun.*(Headless|Interactive|Blocker|Report)|TestStatus.*LastReport|TestAutopilot.*State|TestLegacyAutopilotCommandsAbsent|TestAuditCatalogGolden|TestPlatformParityGolden|TestRegressionSnapshot|TestNoOrphanRuntimeCommands|TestOrphanAllowlistOnlyShrinks' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Old/new autopilot state fixtures and live run/status report tests pass.
    - In-memory and generated runtime catalog/parity/current orphan data contain none of the seven names; regression count drops by seven.
    - Generated diff contains no unrelated change and baseline has no diff.
    - Plan 09 is the named owner of the second four-command fixture shrink.
  </acceptance_criteria>
  <reversibility rating="costly">Public dead adapters are removed; state compatibility and supported run/status behavior remain directly tested.</reversibility>
  <done>The autopilot duplicate CLI is gone and supported run/status state remains compatible, ready for one final command-surface regeneration in plan 09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|---|---|---|
| caller audit -> deletion | Hidden script/internal caller could lose functionality | Repository caller scan, orphan ratchet, supported replacement mapping, focused lifecycle tests |
| command/state separation | Deleting Cobra code could accidentally delete state/report fields | Direct old/new state plus live run/status tests |
| generator flags -> golden files | Staged refresh could hide unrelated drift | Owning flags, exact seven-name/count diff, rerun clean, immutable baseline |
</threat_model>

<verification>
- `go test ./cmd/ -run 'Run.*(Headless|Interactive|Blocker|Report)|Status.*LastReport|Autopilot.*State|LegacyAutopilotCommandsAbsent' -count=1`
- `go test ./cmd/ -run 'TestAuditCatalogGolden|TestPlatformParityGolden|TestRegressionSnapshot|TestNoOrphanRuntimeCommands|TestOrphanAllowlistOnlyShrinks' -count=1`
- `go build ./cmd/aether`
- `go vet ./...`
</verification>

<success_criteria>
The seven audited autopilot commands and stale broad scanner are gone while supported run/status state remains compatible; command fixtures cleanly reflect this seven-command stage and are ready for plan 09's remaining four-command shrink.
</success_criteria>

## Artifacts this phase produces (Plan 08)

| Symbol/path | Kind |
|---|---|
| removal of seven legacy autopilot adapters/scanner | command cleanup |
| retained autopilot state/report types | preserved capability |
| direct state/runtime-catalog coverage | regression proof |
| staged catalog/parity/regression/current orphan refresh | updated ratchets |

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-08-SUMMARY.md` when done.
</output>
