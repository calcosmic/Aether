---
phase: 198.3-overnight-stamina
plan: 21
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/swarm_cmd.go
  - cmd/swarm_cmd_test.go
  - cmd/swarm_strikes.go
  - cmd/swarm_strikes_test.go
autonomous: true
requirements:
  - STAM-01
  - STAM-05
gap_closure: true
must_haves:
  truths:
    - "External swarm finalization accepts exactly one terminal result for every manifest dispatch and no other result."
    - "Result payloads cannot replace manifest-owned worker identity, role, caste, or task."
    - "Rejected duplicate, surplus, forged, missing, or non-terminal evidence causes zero durable swarm, spawn, midden, flag, or strike mutation."
    - "If the third result persists but escalation persistence fails, the next same-target command repairs the one stable escalation before returning corrective guidance."
    - "Escalation repair is idempotent and never records a fourth strike or dispatches another worker."
  artifacts:
    - path: "cmd/swarm_cmd.go"
      provides: "Pure exact-manifest external result validation before finalizer mutation"
      contains: "mergeExternalSwarmResults"
    - path: "cmd/swarm_strikes.go"
      provides: "Recoverable idempotent third-strike escalation transition"
      contains: "upsertSwarmEscalationFlag"
    - path: "cmd/swarm_cmd_test.go"
      provides: "Adversarial finalizer and public repair-flow regressions"
    - path: "cmd/swarm_strikes_test.go"
      provides: "Partial-persistence fault injection and idempotency assertions"
  key_links:
    - from: "cmd/swarm_cmd.go"
      to: "swarmManifest.Dispatches"
      via: "exact result cardinality, identity, and terminal-status validation"
      pattern: "mergeExternalSwarmResults"
    - from: "cmd/swarm_cmd.go"
      to: "cmd/swarm_strikes.go"
      via: "only validated terminal outcomes reach persistSwarmResultOutcome"
      pattern: "persistSwarmResultOutcome"
    - from: "cmd/swarm_strikes.go"
      to: "pending-decisions.json"
      via: "same-target strike history deterministically recreates the stable escalation flag"
      pattern: "upsertSwarmEscalationFlag"
---

<objective>
Make external swarm evidence exact and make the third-strike escalation transition recoverable.

Purpose: Prevent incomplete or forged wrapper evidence from becoming success/strike history, and ensure one failed escalation write cannot permanently wedge the documented corrective-phase recovery.
Output: Pre-mutation finalizer validation, idempotent escalation repair, and fault-injection tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
@.planning/phases/198.3-overnight-stamina/198.3-07-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-16-SUMMARY.md

<interfaces>
`swarmManifest.Dispatches` owns worker `Name`, `Caste`, `Role`, `Task`, `TaskID`, stage and wave. Its published terminal allowlist is `completed`, `passed`, `code_written`, `blocked`, `failed`, and `timeout`. `persistSwarmResultOutcome` is the shared durable completion seam; keep both direct and external lanes behind it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate exact terminal external swarm evidence before any mutation</name>
  <files>cmd/swarm_cmd.go, cmd/swarm_cmd_test.go</files>
  <read_first>
    cmd/swarm_cmd.go
    cmd/swarm_cmd_test.go
    cmd/swarm_strikes.go
    cmd/spawn_runs.go
    cmd/midden_shared.go
    cmd/codex_dispatch_contract.go
    .planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
    .planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
  </read_first>
  <behavior>
    - An exact one-to-one set of terminal results matching every manifest worker by immutable name/caste/role/task is accepted.
    - Duplicate names or roles, surplus/unknown workers, missing workers, response-role mismatch, and any forged manifest-owned field are rejected.
    - Blank, planned, spawned, running, starting, and unknown statuses are rejected; only the documented terminal allowlist is admitted.
    - Every rejection leaves `.aether/data` durable swarm/spawn/midden/flag/strike artifacts unchanged.
  </behavior>
  <action>Refactor `mergeExternalSwarmResults` into a pure validate-and-project boundary. First validate the manifest itself (`Workflow`, non-empty swarm ID/target, `WorkerCount == len(Dispatches)`, and unique non-empty dispatch names/roles). Then require `len(results) == len(Dispatches)`, exactly one result by immutable worker name, exact equality for non-empty submitted `Caste`, `Role`, `Task`, and `Response.Role`, and no role-only fallback. Admit only the six terminal values published in `DispatchContract.worker_status_values`; normalize them only after allowlist validation. Construct each execution with the manifest-owned identity fields and result-owned outcome fields. Move this validation ahead of `beginRuntimeSpawnRun`, `initializeSwarmRun`, midden writes, spawn-tree writes, display/finding writes, and `persistSwarmResultOutcome`; keep the merge helper mutation-free. Do not let `summarizeSwarmOutcome` default an unrecognized status to completed—make unreachable invalid status explicit/failing as a defense in depth.</action>
  <acceptance_criteria>
    - `runSwarmFinalize` performs exact validation before its first durable writer or spawn-run initializer.
    - Result `Name`, `Caste`, `Role`, `Task`, or nested `Response.Role` cannot replace the corresponding manifest value.
    - `TestSwarmFinalizeRejectsUnboundOrNonTerminalEvidenceWithoutMutation` has table rows for duplicate name, duplicate role mapping, surplus worker, missing worker, forged caste/role/task/name, nested role mismatch, blank, planned, spawned, running, and unknown status.
    - Each rejection compares the complete pre/post `.aether/data` tree (or an equivalent exhaustive artifact set) and proves no swarm result, spawn run/tree, display/finding, midden, pending-decision, or strike record changed.
    - `TestSwarmFinalizeRecordsExternalTaskResults` still accepts each documented terminal status and preserves manifest identity.
    - `go test ./cmd -run 'TestSwarmFinalize(RejectsUnboundOrNonTerminalEvidenceWithoutMutation|RecordsExternalTaskResults)' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestSwarmFinalize(RejectsUnboundOrNonTerminalEvidenceWithoutMutation|RecordsExternalTaskResults)' -count=1</automated>
  </verify>
  <done>Only exact manifest-bound terminal evidence can mutate swarm state or contribute to success/strike history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair a partially persisted third-strike escalation idempotently</name>
  <files>cmd/swarm_strikes.go, cmd/swarm_strikes_test.go, cmd/swarm_cmd.go, cmd/swarm_cmd_test.go</files>
  <read_first>
    cmd/swarm_strikes.go
    cmd/swarm_strikes_test.go
    cmd/swarm_cmd.go
    cmd/swarm_cmd_test.go
    cmd/blocker_snapshot.go
    cmd/state_extra.go
    cmd/state_extra_test.go
    .planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
    .planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
  </read_first>
  <behavior>
    - When result 3 persists and its flag write fails, strike history remains three and the next same-target command retries the stable flag upsert before returning concern guidance.
    - A repeated repair creates exactly one active escalation and does not append a result, increase strike count, or dispatch a worker.
    - If repair persistence is still unavailable, the command fails closed with zero dispatch; once storage is writable, retry repairs and the emitted corrective-phase flow is usable.
  </behavior>
  <action>Extract an idempotent `ensureSwarmEscalationForHistory` seam that verifies a three-strike same-target history has the exact stable `swarmEscalationFlagID`, calls `upsertSwarmEscalationFlag` when missing, and re-reads/validates the active flag. Use it after `saveSwarmResultRecord` inside `persistSwarmResultOutcome` and, critically, immediately after `evaluateSwarmStrikeHistory` in `runSwarmCompatibility` before the `StrikeCount >= 3` architectural-concern return. On repair failure return an error before plan-only/native dispatch. On success return the existing concern payload and guided `aether insert-phase` command without writing a fourth result. Preserve Plan 16's target-bound recovery epoch: only the repaired exact-target flag can authorize corrective insertion and subsequent retry.</action>
  <acceptance_criteria>
    - `TestSwarmThirdStrikeRepairsEscalationAfterPartialPersistenceFailure` makes the third result write succeed while the pending-decision atomic write fails, proves three durable strikes/no flag, restores storage, retries, and observes exactly one active stable escalation with the same three evidence IDs.
    - Repeating the repair leaves strike count/result-file count and active escalation count unchanged and invokes zero workers.
    - `TestSwarmPartialEscalationRepairPublicFlow` proves the emitted guided corrective-phase insertion authenticates against the repaired flag and an exact-target retry becomes dispatchable after the recovery epoch.
    - With the flag store still failing, the public command returns an error and worker invocation count remains zero.
    - Existing `TestSwarmThreeStrikeRecoveryExactTargetRetryReEscalates` and `TestSwarmThreeStrikeReplayKeepsOneEscalation` remain green.
    - `go test ./cmd -run 'TestSwarm(ThirdStrikeRepairsEscalationAfterPartialPersistenceFailure|PartialEscalationRepairPublicFlow|ThreeStrikeRecoveryExactTargetRetryReEscalates|ThreeStrikeReplayKeepsOneEscalation)' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestSwarm(ThirdStrikeRepairsEscalationAfterPartialPersistenceFailure|PartialEscalationRepairPublicFlow|ThreeStrikeRecoveryExactTargetRetryReEscalates|ThreeStrikeReplayKeepsOneEscalation)' -count=1</automated>
  </verify>
  <done>The result-plus-escalation transition self-repairs safely after a partial write and the supported corrective recovery remains usable.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'TestSwarm(Finalize|Strike|ThreeStrike|PartialEscalation|Recovery)' -count=1`
- `go vet ./cmd/...`
</verification>

<success_criteria>
- External finalization is one-to-one, manifest-owned, terminal-only, and non-mutating on rejection.
- Unknown/non-terminal status can never become durable completion.
- The third-strike escalation is eventually established exactly once after a recoverable flag-write failure.
- No repair path dispatches a fourth worker or forges a recovery epoch.
</success_criteria>

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-21-SUMMARY.md` when done.
</output>
