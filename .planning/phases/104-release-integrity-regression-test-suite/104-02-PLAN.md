---
phase: 104-release-integrity-regression-test-suite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/regression_test.go
  - cmd/testdata/regression_snapshot.json
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "Master regression snapshot freezes all six audit dimension counts from Phases 100-104"
    - "Single test function verifies every snapshot count matches actual codebase state"
    - "Golden file uses -update-golden flag pattern for intentional refresh"
    - "Test fails loudly when any audit dimension drifts (command count, contract count, caste count, section count, artifact count, gate classifications)"
  artifacts:
    - path: "cmd/regression_test.go"
      provides: "Master regression test freezing all six audit dimensions"
      exports: ["TestRegressionSnapshot"]
    - path: "cmd/testdata/regression_snapshot.json"
      provides: "Golden snapshot of all verified counts from Phases 100-104"
      contains: "command_count"
  key_links:
    - from: "cmd/regression_test.go"
      to: "cmd/testdata/command_catalog.json"
      via: "golden file reference"
      pattern: "command_catalog.json"
    - from: "cmd/regression_test.go"
      to: "cmd/testdata/worker_economy_snapshot.json"
      via: "golden file reference"
      pattern: "worker_economy_snapshot.json"
    - from: "cmd/regression_test.go"
      to: "cmd/testdata/data_flow_snapshot.json"
      via: "golden file reference"
      pattern: "data_flow_snapshot.json"
---

<objective>
Create a master regression snapshot test that freezes all six audit dimension counts from Phases 100-104. A single test function verifies every count matches the actual codebase, failing CI if any dimension drifts.

Purpose: Future code changes that invalidate any audit finding (add commands without catalog updates, add castes without registry updates, add artifacts without wiring) will fail CI immediately.

Output: cmd/regression_test.go with TestRegressionSnapshot and cmd/testdata/regression_snapshot.json golden file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-release-integrity-regression-test-suite/104-CONTEXT.md

<interfaces>
From cmd/audit_catalog_test.go:
  - Pattern: buildAuditCatalog(rootCmd) returns []CatalogEntry
  - Golden file: testdata/command_catalog.json with 377 entries
  - Test pattern: golden comparison, completeness check (>= 300), schema validation

From cmd/worker_economy_test.go:
  - Golden file: testdata/worker_economy_snapshot.json
  - Fields: documented_castes, dispatched_castes, chat_only_castes, visual_functions, registry_map_keys
  - Test pattern: load golden, verify against report markdown

From cmd/data_flow_audit_test.go:
  - Golden file: testdata/data_flow_snapshot.json
  - Fields: artifacts (33 entries), colony_prime_section_count (16), capsule_section_count (5)
  - Test pattern: load golden, verify against DATA-FLOW.md report

From cmd/colony_prime_context.go:
  - Section names: 16 sections registered with name field (state, review_depth, pheromones, instincts, decisions, learnings, worker_handoffs, hive_wisdom, learned_memory, global_queen_md, user_preferences, prior_reviews, local_queen_wisdom, clarified_intent, blockers, medic_health)

From cmd/codex_visuals.go:
  - casteColorMap, casteEmojiMap, casteLabelMap define 26 castes
  - casteIdentity(), casteLabel(), casteEmoji() functions

From cmd/gate_classify.go (Phase 93):
  - Gate classification logic with hard_block, soft_block, advisory
  - 11 gates classified

From cmd/contracts/:
  - 16 lifecycle contract .md files

From cmd/release_pipeline_test.go (Phase 104-01):
  - Will produce release_pipeline_snapshot.json with sync pair counts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write master regression snapshot test</name>
  <files>cmd/regression_test.go, cmd/testdata/regression_snapshot.json</files>
  <read_first>
    - cmd/audit_catalog_test.go (golden comparison pattern)
    - cmd/worker_economy_test.go (snapshot struct pattern)
    - cmd/data_flow_audit_test.go (snapshot struct pattern)
    - cmd/testdata/command_catalog.json (check entry count)
    - cmd/testdata/worker_economy_snapshot.json (check structure)
    - cmd/testdata/data_flow_snapshot.json (check structure)
    - cmd/colony_prime_context.go (count section names)
    - cmd/codex_visuals.go (count castes in casteColorMap)
    - cmd/gate_classify.go (count classified gates)
    - cmd/contracts/ (count contract files)
  </read_first>
  <action>
Per TEST-01 and TEST-02: Create a master regression snapshot that freezes all verified counts from Phases 100-104.

Create cmd/testdata/regression_snapshot.json golden file with this exact structure:
```json
{
  "audit_dimensions": {
    "command_contracts": {
      "command_count": 377,
      "lifecycle_contracts": 16,
      "output_modes": ["json", "visual", "json+visual", "text", "unknown"]
    },
    "worker_economy": {
      "documented_castes": 26,
      "dispatched_castes": 0,
      "chat_only_castes": 0,
      "visual_functions": 0
    },
    "data_flow": {
      "artifact_count": 33,
      "colony_prime_sections": 16,
      "capsule_sections": 5,
      "dead_end_artifacts": ["constraints.json"]
    },
    "release_integrity": {
      "sync_pair_count": 0,
      "home_sync_pair_count": 0
    },
    "gate_classifications": {
      "total_gates": 11,
      "hard_block": 0,
      "soft_block": 0,
      "advisory": 0
    }
  },
  "version": "1.0.34"
}
```

Populate the counts by reading the actual codebase:
- command_count: count entries in testdata/command_catalog.json OR call len(buildAuditCatalog(rootCmd))
- lifecycle_contracts: count .md files in cmd/contracts/
- documented_castes: count keys in casteColorMap in codex_visuals.go (use grep to count map entries)
- artifact_count: count artifacts array in testdata/data_flow_snapshot.json
- colony_prime_sections: count name fields in colony_prime_context.go (exclude duplicate "prior_reviews" around line 323; acceptable range 15-17)
- capsule_sections: count section names in context.go (range 4-6)
- sync_pair_count: count entries returned by installSyncPairs() in platform_sync.go (call it in test or count struct literals)
- home_sync_pair_count: count entries returned by platformHomeHubSyncPairs()
- total_gates: count gate classifications in gate_classify.go
- hard_block/soft_block/advisory: count each classification type in gate_classify.go

For worker_economy dispatched_castes, chat_only_castes, visual_functions: read these from testdata/worker_economy_snapshot.json at test time rather than hardcoding. The regression snapshot references the sub-snapshots.

Create cmd/regression_test.go with 1 test function:

1. TestRegressionSnapshot:
   - Load regression_snapshot.json golden file
   - Load sub-snapshots: command_catalog.json, worker_economy_snapshot.json, data_flow_snapshot.json
   - Verify command_count matches len(buildAuditCatalog(rootCmd))
   - Verify lifecycle_contracts matches count of .md files in cmd/contracts/
   - Verify documented_castes matches count of casteColorMap entries (grep "\"[a-z_]*\":" in codex_visuals.go casteColorMap)
   - Verify artifact_count matches len(data_flow_snapshot.artifacts)
   - Verify colony_prime_sections matches colony_prime_context.go section count (acceptable range 15-17)
   - Verify capsule_sections matches context.go section count (acceptable range 4-6)
   - Verify sync_pair_count matches len(installSyncPairs())
   - Verify home_sync_pair_count matches len(platformHomeHubSyncPairs())
   - Verify total_gates matches gate count in gate_classify.go
   - For each dimension: if count differs from golden, fail with clear message including -update-golden hint
   - Support -update-golden flag to refresh the snapshot

Package: main
Imports: testing, os, encoding/json, path/filepath, strings
Helper functions:
  - loadRegressionSnapshot(t) *RegressionSnapshot
  - countContractFiles(t) int
  - countCasteMapEntries(t) int
  - countGateClassifications(t) (total, hard, soft, advisory int)

Follow the exact golden file pattern from cmd/audit_catalog_test.go:
- Global flag: var updateGolden = flag.Bool("update-golden", false, "update golden files")
- On -update-golden: marshal current state, write to testdata/regression_snapshot.json, return early
- On normal run: read golden, compare, fail on mismatch
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && go test ./cmd/ -run TestRegressionSnapshot -count=1 -timeout 60s</automated>
  </verify>
  <done>
    cmd/regression_test.go exists with TestRegressionSnapshot. cmd/testdata/regression_snapshot.json exists with golden data. Test passes. Test fails if any audit dimension count drifts. -update-golden flag supported for refresh.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read-only tests | Tests read source code and golden files, write nothing to production paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-02 | I | Test code | accept | Read-only test verification, no security impact |
</threat_model>

<verification>
1. `go test ./cmd/ -run TestRegressionSnapshot -count=1` passes
2. cmd/regression_test.go has exactly 1 test function
3. Golden snapshot captures all six audit dimension counts
4. Test supports -update-golden flag for refresh
5. Test fails if any count drifts (add a dummy entry to a map, verify test fails)
</verification>

<success_criteria>
- TestRegressionSnapshot passes, verifying all six audit dimensions
- Golden snapshot freezes counts from Phases 100-104
- Test supports -update-golden for intentional refresh
- Test fails CI on drift in any dimension
- All sub-snapshots (command_catalog.json, worker_economy_snapshot.json, data_flow_snapshot.json) referenced and verified
</success_criteria>

<output>
After completion, create `.planning/phases/104-release-integrity-regression-test-suite/104-02-SUMMARY.md`
</output>
