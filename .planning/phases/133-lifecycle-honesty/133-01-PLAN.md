---
phase: 133-lifecycle-honesty
plan: 01
type: execute
wave: 1
depends_on:
  - 132-01
files_modified:
  - .aether/ts-host/src/worker-dispatch.ts
  - .aether/ts-host/src/lifecycle.ts
  - .aether/docs/host-command-reference.md
  - .aether/ts-host/test/lifecycle-honesty.test.ts
autonomous: true
requirements:
  - LHO-01
  - LHO-02
  - LHO-03

must_haves:
  truths:
    - "Worker dispatch defaults to real execution, not simulation"
    - "Missing platform CLI without --simulate produces an error, not silent simulation"
    - "Simulated placeholder file creation is gated behind --simulate"
    - "--simulate is documented in host-command-reference.md"
    - "--simulate behavior is tested"
  artifacts:
    - path: ".aether/ts-host/src/worker-dispatch.ts"
      provides: "Default real dispatch, simulation only with explicit --simulate"
    - path: ".aether/ts-host/src/lifecycle.ts"
      provides: "Error on missing platforms without --simulate"
    - path: ".aether/ts-host/test/lifecycle-honesty.test.ts"
      provides: "Tests verifying honest lifecycle behavior"
  key_links:
    - from: ".aether/ts-host/src/lifecycle.ts"
      to: ".aether/ts-host/src/worker-dispatch.ts"
      via: "simulateWorkers option"
---

<objective>
Remove production-facing synthetic lifecycle shortcuts. Worker dispatch must default to real execution. If no platform CLI is available and the user did not pass --simulate, error out rather than silently simulating. Gate all synthetic paths behind the explicit --simulate flag.

Purpose: Prevent accidental simulation runs in production — users must explicitly opt in with --simulate.
Output: Updated dispatch logic, error on missing platforms, tests, documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

## Current synthetic behavior (to fix)

From `.aether/ts-host/src/worker-dispatch.ts:146`:
```typescript
const simulate = opts.simulateWorkers !== false; // default true
```
This means simulation is the DEFAULT unless explicitly set to false.

From `.aether/ts-host/src/lifecycle.ts:320-332`:
```typescript
let simulateWorkers = opts.simulateWorkers;
if (simulateWorkers === undefined) {
  if (hasPlatforms) {
    simulateWorkers = false;
  } else {
    process.stderr.write("Warning: no platform CLI available...");
    simulateWorkers = true;  // silently falls back to simulation
  }
}
```

From `.aether/ts-host/src/lifecycle.ts:340-355`:
Creates `SIMULATED_BUILD_OUTPUT.txt` placeholder file for simulated workers to claim.

## Required behavior

1. Default: real worker dispatch (simulateWorkers = false)
2. `--simulate` flag: explicitly opt into simulation
3. If no platforms and no `--simulate`: throw error, do not simulate
4. Placeholder file creation only happens when `--simulate` is true
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix worker-dispatch default</name>
  <files>.aether/ts-host/src/worker-dispatch.ts</files>
  <read_first>
    - .aether/ts-host/src/worker-dispatch.ts (dispatch function)
    - .aether/ts-host/src/lifecycle.ts (simulateWorkers usage)
  </read_first>
  <action>
Fix `.aether/ts-host/src/worker-dispatch.ts`:

1. Change the default from simulation to real dispatch:
   ```typescript
   // BEFORE:
   const simulate = opts.simulateWorkers !== false; // default true

   // AFTER:
   const simulate = opts.simulateWorkers === true; // default false
   ```

2. Update the warning logic:
   - If `opts.simulateWorkers === true` → log "Simulating worker..."
   - If `opts.simulateWorkers === false` or undefined → attempt real dispatch
   - If real dispatch fails because no platforms available → throw error (don't warn and simulate)

3. Remove the "no platforms available; simulating worker" warning that silently falls back.

4. Keep the actual simulation behavior intact for when `simulateWorkers === true`.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npm run typecheck</automated>
  </verify>
  <done>
    - worker-dispatch.ts defaults to real dispatch
    - Simulation only happens with explicit simulateWorkers=true
    - npm run typecheck passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix lifecycle.ts platform detection</name>
  <files>.aether/ts-host/src/lifecycle.ts</files>
  <read_first>
    - .aether/ts-host/src/lifecycle.ts (platform detection block)
    - .aether/ts-host/src/worker-dispatch.ts (after Task 1 changes)
  </read_first>
  <action>
Fix `.aether/ts-host/src/lifecycle.ts`:

1. Change the platform detection block (lines ~320-332):
   ```typescript
   // BEFORE: warn and fall back to simulation
   let simulateWorkers = opts.simulateWorkers;
   if (simulateWorkers === undefined) {
     if (hasPlatforms) {
       simulateWorkers = false;
     } else {
       process.stderr.write("Warning...");
       simulateWorkers = true;
     }
   }

   // AFTER: error if no platforms and no --simulate
   let simulateWorkers = opts.simulateWorkers ?? false;
   if (!hasPlatforms && !simulateWorkers) {
     throw new Error(
       "No platform CLI available (claude, opencode, or codex). " +
       "Install a platform CLI or run with --simulate to use simulation mode."
     );
   }
   ```

2. Gate the placeholder file creation behind `simulateWorkers`:
   ```typescript
   // Only create placeholder when simulating
   if (simulateWorkers) {
     // existing placeholder creation code
   }
   ```

3. Remove the warning message about falling back to simulation.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npm run typecheck</automated>
  </verify>
  <done>
    - lifecycle.ts errors on missing platforms without --simulate
    - Placeholder file creation gated behind simulateWorkers
    - npm run typecheck passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Document --simulate in reference</name>
  <files>.aether/docs/host-command-reference.md</files>
  <read_first>
    - .aether/docs/host-command-reference.md (lifecycle section)
    - .aether/docs/wrapper-host-contract.md
  </read_first>
  <action>
Update `.aether/docs/host-command-reference.md`:

1. In the `lifecycle` section, add:
   - `--simulate` flag description: "Run in simulation mode (no real worker spawning). Required when no platform CLI is available."
   - Error note: "Without --simulate, lifecycle requires a platform CLI (claude, opencode, or codex) to be installed."

2. In the `build` section, add:
   - `--simulate` flag description: "Skip real worker dispatch and use simulation."

3. In the `plan` section, add:
   - `--synthetic` flag description: "Skip real worker dispatch."
   - Note the difference: `--synthetic` (Go CLI flag) vs `--simulate` (TS host flag)

Clarify that `--simulate` is the TS host flag that maps to `simulateWorkers`, while `--synthetic` is the Go CLI flag.
  </action>
  <verify>
    <manual>Review doc for accuracy</manual>
  </verify>
  <done>
    - --simulate documented for lifecycle and build
    - --synthetic documented for plan
    - Difference between flags explained
  </done>
</task>

<task type="auto">
  <name>Task 4: Write lifecycle honesty tests</name>
  <files>.aether/ts-host/test/lifecycle-honesty.test.ts</files>
  <read_first>
    - .aether/ts-host/src/lifecycle.ts
    - .aether/ts-host/src/worker-dispatch.ts
    - .aether/ts-host/test/oracle-lifecycle.test.ts (mock pattern)
  </read_first>
  <action>
Create `.aether/ts-host/test/lifecycle-honesty.test.ts`:

1. Test: lifecycle without --simulate and no platforms → throws error
   - Mock detectAvailablePlatforms to return []
   - Call lifecycle with simulateWorkers=false (or undefined)
   - Assert error message contains "No platform CLI available"

2. Test: lifecycle with --simulate and no platforms → succeeds
   - Mock detectAvailablePlatforms to return []
   - Call lifecycle with simulateWorkers=true
   - Assert no error

3. Test: lifecycle without --simulate and platforms available → real dispatch
   - Mock detectAvailablePlatforms to return ['claude']
   - Call lifecycle with simulateWorkers=false
   - Assert no error, workers dispatched

4. Test: worker-dispatch defaults to real execution
   - Mock detectAvailablePlatforms to return ['claude']
   - Call dispatch with simulateWorkers undefined
   - Assert attempts real worker spawn

5. Test: worker-dispatch simulates only with explicit true
   - Call dispatch with simulateWorkers=true
   - Assert simulation path taken

6. Test: worker-dispatch does not simulate with explicit false
   - Mock detectAvailablePlatforms to return []
   - Call dispatch with simulateWorkers=false
   - Assert throws error

Use `node:test` and `node:assert/strict`.
Use `__setCallGoJSON` / `__restoreCallGoJSON` and `__setDispatchSingleWorker` patterns if needed.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npm test</automated>
  </verify>
  <done>
    - lifecycle-honesty.test.ts exists and passes
    - All 5-6 test scenarios covered
    - npm test passes with no regressions
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify no silent simulation paths remain</name>
  <files>.aether/ts-host/src/</files>
  <read_first>
    - .aether/ts-host/src/worker-dispatch.ts (after changes)
    - .aether/ts-host/src/lifecycle.ts (after changes)
  </read_first>
  <action>
Search the codebase for any remaining silent simulation fallbacks:

1. Grep for "simulating" and "simulate" in .aether/ts-host/src/
2. Verify no path defaults to simulation without explicit user intent
3. Verify all simulation paths require simulateWorkers === true
4. Check that no production code creates SIMULATED_BUILD_OUTPUT.txt without --simulate

If any violations found, fix them.
  </action>
  <verify>
    <automated>grep -rn "simulateWorkers" /Users/callumcowie/repos/Aether/.aether/ts-host/src/ | grep -v test | grep -v "\.d\.ts"</automated>
  </verify>
  <done>
    - No silent simulation paths remain
    - All simulation gated behind explicit flag
    - grep confirms clean codebase
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User intent -> simulation | User must explicitly opt in with --simulate |
| Lifecycle -> worker dispatch | Lifecycle passes simulateWorkers; dispatch respects it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Elevation of Privilege | Silent simulation skips real workers | mitigate | Default to real dispatch; simulation requires --simulate |
| T-133-02 | Denial of Service | Error on missing platforms blocks users | accept | Error message tells user to install platform or use --simulate |
| T-133-03 | Information Disclosure | Error reveals platform list | accept | Platform names are not sensitive |
</threat_model>

<verification>
1. `cd .aether/ts-host && npm run typecheck` passes
2. `cd .aether/ts-host && npm test` passes (all tests + no regressions)
3. No silent simulation paths remain in src/
4. `--simulate` documented in host-command-reference.md
5. Error message is helpful when platforms are missing without --simulate
</verification>

<success_criteria>
- Worker dispatch defaults to real execution
- Missing platforms without --simulate produces clear error
- Placeholder file creation gated behind --simulate
- --simulate documented and tested
- All typecheck and test commands pass
- No silent simulation fallbacks remain
</success_criteria>

<output>
After completion, create `.planning/phases/133-lifecycle-honesty/133-01-SUMMARY.md`
</output>
