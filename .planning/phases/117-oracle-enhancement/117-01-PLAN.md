---
phase: 117-oracle-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/oracle_loop.go
  - .aether/utils/oracle/oracle.md
  - cmd/ceremony_emitter.go
  - .aether/ts-host/src/types.ts
  - .aether/ts-host/src/narrator.ts
  - .aether/ts-host/test/narrator.test.ts
  - .aether/ts-host/test/oracle-events.test.ts
autonomous: true
requirements:
  - ORA-01
  - ORA-02
must_haves:
  truths:
    - "Oracle prompts include phase-aware directives (survey, verify, investigate, synthesize)"
    - "Diminishing returns detection stops loop when novelty < 15% for 3 consecutive iterations"
    - "Ceremony events emitted for Oracle phase transitions and iterations"
    - "TS host narrator handles new ceremony.oracle.* topics"
    - "No TS host state mutation; Go owns all Oracle logic"
  artifacts:
    - path: "cmd/oracle_loop.go"
      provides: "Enhanced Oracle loop with phase-aware prompts and novelty detection"
      exports: []
    - path: ".aether/utils/oracle/oracle.md"
      provides: "Updated Oracle instructions with phase directive examples"
      exports: []
    - path: "cmd/ceremony_emitter.go"
      provides: "New emitOraclePhaseTransition and emitOracleIteration functions"
      exports: []
    - path: ".aether/ts-host/src/types.ts"
      provides: "New ceremony topics for Oracle events"
      exports: []
    - path: ".aether/ts-host/src/narrator.ts"
      provides: "Narrator handles ceremony.oracle.* topics"
      exports: ["createNarrator"]
    - path: ".aether/ts-host/test/oracle-events.test.ts"
      provides: "Oracle ceremony event tests"
      min_tests: 4
  key_links:
    - from: "cmd/oracle_loop.go"
      to: "cmd/ceremony_emitter.go"
      via: "emitOraclePhaseTransition called on phase change"
    - from: "cmd/ceremony_emitter.go"
      to: "pkg/events/ceremony.go"
      via: "event bus publish"
    - from: ".aether/ts-host/src/narrator.ts"
      to: "cmd/ceremony_emitter.go"
      via: "event bridge reads emitted Oracle events"
---

<objective>
Enhance the Go Oracle loop with phase-aware prompts and diminishing-returns detection, emit ceremony events for TS host visibility.

Purpose: Satisfies ORA-01 (phase-aware prompts) and ORA-02 (diminishing returns detection).
Output: Updated cmd/oracle_loop.go, cmd/ceremony_emitter.go, oracle.md, TS host narrator updates, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-oracle-enhancement/117-CONTEXT.md
@.planning/phases/117-oracle-enhancement/117-RESEARCH.md
@cmd/oracle_loop.go
@cmd/ceremony_emitter.go
@.aether/utils/oracle/oracle.md
@.aether/ts-host/src/types.ts
@.aether/ts-host/src/narrator.ts
</context>

<interfaces>
From cmd/oracle_loop.go (key functions):
```go
func runOracleLoop(state *oracleState, plan *oraclePlan) error
func nextOraclePhase(state *oracleState, plan *oraclePlan) string
func buildOracleWorkerConfig(state *oracleState, plan *oraclePlan, phase string) workerConfig
func oracleProgressedSince(state *oracleState, plan *oraclePlan, since IterationKey) bool
func writeOracleSynthesisReport(state *oracleState, plan *oraclePlan) error
func applyOracleWorkerResponse(state *oracleState, plan *oraclePlan, resp oracleResponse) error
```

From cmd/ceremony_emitter.go:
```go
func emitLoopBreakEvent(opts ceremonyOptions, reason string) error
func emitVisualProgress(visual string)
```

From .aether/ts-host/src/types.ts:
```typescript
export const CEREMONY_TOPICS = [
  "ceremony.build.spawn",
  "ceremony.build.wave.start",
  // ... existing topics
] as const;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add phase-aware prompt directives to buildOracleWorkerConfig</name>
  <files>
    cmd/oracle_loop.go
  </files>
  <read_first>
    cmd/oracle_loop.go
    .aether/utils/oracle/oracle.md
  </read_first>
  <action>
    Update `cmd/oracle_loop.go`:

    1. Modify `nextOraclePhase(state, plan)` to return an explicit "synthesize" phase:
       - After "verify" phase completes, return "synthesize" instead of stopping
       - "synthesize" is the final phase before loop completion
    2. Modify `buildOracleWorkerConfig(state, plan, phase)` to inject phase-aware directives:
       - "survey" phase directive: "Your task is to survey the landscape. Identify key concepts, existing solutions, and open questions. Do not form conclusions yet."
       - "verify" phase directive: "Your task is to verify previous findings. Test assumptions, look for contradictions, and assess confidence levels."
       - "investigate" phase directive: "Your task is to investigate specific questions. Deep-dive into the most promising areas identified in the survey."
       - "synthesize" phase directive: "Your task is to synthesize all findings into a coherent report. Connect dots, resolve contradictions, and formulate recommendations."
    3. The directive is appended to the worker brief before the response contract.
    4. Add `oraclePhaseDirective(phase string) string` helper function.

    Keep all existing behavior for other phases.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && go test ./cmd/... -run "Oracle"</automated>
  </verify>
  <done>
    - nextOraclePhase returns "synthesize" as final phase
    - buildOracleWorkerConfig injects phase-specific directive
    - oraclePhaseDirective helper exists
    - Go tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement diminishing-returns detection via novelty delta</name>
  <files>
    cmd/oracle_loop.go
  </files>
  <read_first>
    cmd/oracle_loop.go
  </read_first>
  <action>
    Update `cmd/oracle_loop.go`:

    1. Add `noveltyTracker` struct to `oracleState`:
       ```go
       type noveltyTracker struct {
         LastKeywords    map[string]bool
         ConsecutiveLow  int
         Threshold       float64 // default 0.15
       }
       ```
    2. Add `extractKeywords(text string) map[string]bool` function:
       - Split text into words
       - Filter out stop words (the, and, a, etc.)
       - Return set of significant words (length > 3)
    3. Add `computeNoveltyDelta(current, previous map[string]bool) float64`:
       - Calculate Jaccard distance: 1 - |intersection| / |union|
       - Return delta as float64 (0.0 = identical, 1.0 = completely different)
    4. Modify `oracleProgressedSince(state, plan, since)` to check novelty:
       - After the existing coarse checks (answered count, findings count), compute novelty delta between current iteration's response and previous iteration
       - If delta < 0.15 (15% novelty), increment `ConsecutiveLow`
       - If `ConsecutiveLow >= 3`, return `false` (no progress — loop should stop)
       - Otherwise, reset `ConsecutiveLow` to 0 and return existing progress check
    5. Modify `applyOracleWorkerResponse` to update `noveltyTracker.LastKeywords` after each response

    Stop words list (English): the, and, a, an, in, on, at, to, for, of, with, by, is, are, was, were, be, been, being, have, has, had, do, does, did, will, would, could, should, may, might, can, this, that, these, those
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && go test ./cmd/... -run "Oracle"</automated>
  </verify>
  <done>
    - noveltyTracker struct added to oracleState
    - extractKeywords and computeNoveltyDelta functions exist
    - oracleProgressedSince checks novelty delta
    - Loop stops after 3 consecutive low-novelty iterations
    - Go tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Oracle ceremony event emitters</name>
  <files>
    cmd/ceremony_emitter.go
    cmd/oracle_loop.go
  </files>
  <read_first>
    cmd/ceremony_emitter.go
    cmd/oracle_loop.go
    pkg/events/ceremony.go
  </read_first>
  <action>
    Update `cmd/ceremony_emitter.go`:

    1. Add `emitOraclePhaseTransition(opts ceremonyOptions, fromPhase, toPhase string, iteration int) error`:
       - Publish event with topic `ceremony.oracle.phase_transition`
       - Payload: `{ phase_from: fromPhase, phase_to: toPhase, iteration: iteration }`
    2. Add `emitOracleIteration(opts ceremonyOptions, iteration int, question string, phase string) error`:
       - Publish event with topic `ceremony.oracle.iteration`
       - Payload: `{ iteration: iteration, question: question, phase: phase }`
    3. Follow the same pattern as `emitLoopBreakEvent`

    Update `cmd/oracle_loop.go`:
    1. In `runOracleLoop`, call `emitOraclePhaseTransition` when `nextOraclePhase` returns a different phase
    2. In `runOracleIterationAttempt`, call `emitOracleIteration` at the start of each iteration

    Add ceremony topics to `pkg/events/ceremony.go`:
    - "ceremony.oracle.phase_transition"
    - "ceremony.oracle.iteration"
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && go test ./cmd/... -run "Ceremony|Oracle" && go test ./pkg/events/...</automated>
  </verify>
  <done>
    - emitOraclePhaseTransition and emitOracleIteration exist
    - Called from oracle_loop.go at appropriate points
    - New ceremony topics added to pkg/events/ceremony.go
    - Go tests pass
  </done>
</task>

<task type="auto">
  <name>Task 4: Update TS host narrator for Oracle ceremony topics</name>
  <files>
    .aether/ts-host/src/types.ts
    .aether/ts-host/src/narrator.ts
  </files>
  <read_first>
    .aether/ts-host/src/types.ts
    .aether/ts-host/src/narrator.ts
  </read_first>
  <action>
    Update `.aether/ts-host/src/types.ts`:

    1. Add new ceremony topics to CEREMONY_TOPICS:
       ```typescript
       "ceremony.oracle.phase_transition",
       "ceremony.oracle.iteration",
       ```

    Update `.aether/ts-host/src/narrator.ts`:

    1. Add handlers for new topics:
       - `ceremony.oracle.phase_transition` → render stage separator with "Oracle: {from} → {to}"
       - `ceremony.oracle.iteration` → render spawn frame with caste "oracle", name "Oracle-{iteration}", task "Researching: {question}"
    2. Import `getCasteEmoji`, `getCasteColor`, `getCasteLabel` for oracle caste
    3. The oracle caste should already exist in ceremony.yaml (check if present; if not, the default config has it)

    No changes needed to event-bridge.ts (it already handles arbitrary ceremony topics).
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npx tsx --test test/narrator.test.ts</automated>
  </verify>
  <done>
    - CEREMONY_TOPICS includes ceremony.oracle.phase_transition and ceremony.oracle.iteration
    - Narrator handles both topics with appropriate rendering
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 5: Write tests for Oracle ceremony events</name>
  <files>
    .aether/ts-host/test/oracle-events.test.ts
  </files>
  <read_first>
    .aether/ts-host/src/narrator.ts
    .aether/ts-host/test/narrator.test.ts
  </read_first>
  <action>
    Create `.aether/ts-host/test/oracle-events.test.ts` using `node:test` + `node:assert/strict`.

    Tests:
    1. `narrator renders phase transition for ceremony.oracle.phase_transition` — mock stdout.write, call onEvent with topic "ceremony.oracle.phase_transition" and payload { phase_from: "survey", phase_to: "verify", iteration: 3 }, assert output contains "survey" and "verify"
    2. `narrator renders iteration frame for ceremony.oracle.iteration` — mock stdout.write, call onEvent with topic "ceremony.oracle.iteration" and payload { iteration: 5, question: "What is the best approach?", phase: "investigate" }, assert output contains "Oracle" and "Researching"
    3. `novelty delta computes correctly for identical texts` — test extractKeywords and computeNoveltyDelta with identical strings, assert delta === 0.0
    4. `novelty delta computes correctly for different texts` — test with completely different strings, assert delta > 0.5

    For tests 3 and 4, you can test the Go functions via a small Go test or mock the logic in TS. Since the Go logic is the source of truth, focus TS tests on the narrator rendering (tests 1 and 2). Tests 3 and 4 are optional — include them only if you can easily port the keyword extraction logic to TS for testing.

    Use REPO_ROOT = "/Users/callumcowie/repos/Aether".
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npx tsx --test test/oracle-events.test.ts</automated>
  </verify>
  <done>
    - oracle-events.test.ts exists with >= 2 tests
    - Tests cover narrator rendering for Oracle phase transitions and iterations
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cmd/oracle_loop.go → ceremony_emitter.go | Go emits events; TS host reads them |
| TS host narrator → stdout | Presentation only; no state mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Denial of Service | oracle_loop.go | mitigate | Diminishing returns detection prevents infinite loops |
| T-117-02 | Information Disclosure | ceremony_emitter.go | accept | Oracle events contain research topics (non-sensitive) |
</threat_model>

<verification>
- `go test ./cmd/... -run "Oracle"` passes
- `go test ./pkg/events/...` passes
- `npm test -- test/narrator.test.ts` passes
- `npm test -- test/oracle-events.test.ts` passes
- `npx tsc --noEmit -p tsconfig.build.json` compiles cleanly
</verification>

<success_criteria>
- Oracle loop has phase-aware prompts (survey, verify, investigate, synthesize)
- Diminishing returns detection stops loop after 3 consecutive low-novelty iterations
- Ceremony events emitted for Oracle phase transitions and iterations
- TS host narrator renders Oracle events
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/117-oracle-enhancement/117-01-SUMMARY.md`
</output>
