---
phase: 117-oracle-enhancement
plan: 02
type: execute
wave: 2
depends_on:
  - "117-01"
files_modified:
  - cmd/oracle_loop.go
  - .aether/references/templates/oracle-tech-evaluation.md
  - .aether/references/templates/architecture-review-template.md
  - .aether/references/templates/bug-investigation-template.md
  - .aether/ts-host/src/dashboard.ts
  - .aether/ts-host/src/dashboard/worker-widget.ts
  - .aether/ts-host/test/dashboard.test.ts
autonomous: true
requirements:
  - ORA-03
must_haves:
  truths:
    - "writeOracleSynthesisReport branches on state.Template to select synthesis structure"
    - "Tech evaluation template includes sections: Overview, Alternatives, Criteria, Scoring, Recommendation"
    - "Architecture review template includes sections: Context, Constraints, Options, Trade-offs, Decision"
    - "Bug investigation template includes sections: Symptoms, Root Cause, Fix, Verification, Prevention"
    - "Dashboard displays Oracle phase and iteration count"
  artifacts:
    - path: "cmd/oracle_loop.go"
      provides: "Template-specific synthesis report generation"
      exports: []
    - path: ".aether/references/templates/oracle-tech-evaluation.md"
      provides: "Tech evaluation synthesis template"
      exports: []
    - path: ".aether/references/templates/architecture-review-template.md"
      provides: "Architecture review synthesis template"
      exports: []
    - path: ".aether/references/templates/bug-investigation-template.md"
      provides: "Bug investigation synthesis template"
      exports: []
    - path: ".aether/ts-host/src/dashboard.ts"
      provides: "Dashboard displays Oracle progress"
      exports: ["createDashboard"]
    - path: ".aether/ts-host/test/dashboard.test.ts"
      provides: "Updated dashboard tests with Oracle"
      min_tests: 3
  key_links:
    - from: "cmd/oracle_loop.go"
      to: ".aether/references/templates/"
      via: "writeOracleSynthesisReport reads template file based on state.Template"
    - from: ".aether/ts-host/src/dashboard.ts"
      to: "cmd/ceremony_emitter.go"
      via: "dashboard.onEvent handles ceremony.oracle.iteration"
---

<objective>
Implement template-specific synthesis reports and dashboard Oracle visibility.

Purpose: Satisfies ORA-03 (template-specific synthesis).
Output: Updated cmd/oracle_loop.go, template files, dashboard updates, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-oracle-enhancement/117-CONTEXT.md
@.planning/phases/117-oracle-enhancement/117-RESEARCH.md
@.planning/phases/117-oracle-enhancement/117-01-PLAN.md
@cmd/oracle_loop.go
@.aether/references/templates/oracle-tech-evaluation.md
@.aether/references/templates/architecture-review-template.md
@.aether/references/templates/bug-investigation-template.md
@.aether/ts-host/src/dashboard.ts
@.aether/ts-host/src/dashboard/worker-widget.ts
</context>

<interfaces>
From cmd/oracle_loop.go:
```go
func writeOracleSynthesisReport(state *oracleState, plan *oraclePlan) error
```

From .aether/ts-host/src/dashboard.ts:
```typescript
export interface Dashboard { onEvent(event: CeremonyEvent): void; start(): void; stop(): void; }
export function createDashboard(opts: DashboardOptions): Dashboard;
```

From .aether/ts-host/src/types.ts:
```typescript
export interface CeremonyPayload {
  phase?: number; phase_name?: string; wave?: number; spawn_id?: string;
  caste?: string; name?: string; task_id?: string; task?: string;
  status?: string; message?: string; completed?: number; total?: number;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update writeOracleSynthesisReport to branch on template</name>
  <files>
    cmd/oracle_loop.go
  </files>
  <read_first>
    cmd/oracle_loop.go
    .aether/references/templates/oracle-tech-evaluation.md
    .aether/references/templates/architecture-review-template.md
    .aether/references/templates/bug-investigation-template.md
  </read_first>
  <action>
    Update `cmd/oracle_loop.go`:

    1. Modify `writeOracleSynthesisReport(state, plan)`:
       - Read `state.Template` (e.g., "tech-evaluation", "architecture-review", "bug-investigation")
       - Branch on template:
         ```go
         switch state.Template {
         case "tech-evaluation":
           return writeTechEvaluationReport(state, plan)
         case "architecture-review":
           return writeArchitectureReviewReport(state, plan)
         case "bug-investigation":
           return writeBugInvestigationReport(state, plan)
         default:
           return writeGenericReport(state, plan)
         }
         ```
    2. Add `writeTechEvaluationReport(state, plan)`:
       - Write sections: Overview, Alternatives Considered, Evaluation Criteria, Scoring Matrix, Recommendation
       - Populate from plan.Findings and plan.Answers
    3. Add `writeArchitectureReviewReport(state, plan)`:
       - Write sections: Context, Constraints, Options Considered, Trade-off Analysis, Decision
       - Populate from plan.Findings
    4. Add `writeBugInvestigationReport(state, plan)`:
       - Write sections: Symptoms, Root Cause Analysis, Fix Applied, Verification Steps, Prevention
       - Populate from plan.Findings and plan.Answers
    5. Keep the existing generic report as `writeGenericReport`

    The reports are written as markdown files to the Oracle output directory.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && go test ./cmd/... -run "Oracle"</automated>
  </verify>
  <done>
    - writeOracleSynthesisReport branches on state.Template
    - Three template-specific report writers exist
    - Generic report preserved as fallback
    - Go tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update template files with section definitions</name>
  <files>
    .aether/references/templates/oracle-tech-evaluation.md
    .aether/references/templates/architecture-review-template.md
    .aether/references/templates/bug-investigation-template.md
  </files>
  <read_first>
    .aether/references/templates/oracle-tech-evaluation.md
    .aether/references/templates/architecture-review-template.md
    .aether/references/templates/bug-investigation-template.md
  </read_first>
  <action>
    Update or create the three template files:

    1. `oracle-tech-evaluation.md`:
       ```markdown
       # Tech Evaluation: {{title}}

       ## Overview
       <!-- 2-3 sentence summary of the technology area -->

       ## Alternatives Considered
       <!-- List of options evaluated -->

       ## Evaluation Criteria
       <!-- Criteria used for scoring -->

       ## Scoring Matrix
       <!-- Table: alternative x criteria = score -->

       ## Recommendation
       <!-- Clear recommendation with rationale -->
       ```
    2. `architecture-review-template.md`:
       ```markdown
       # Architecture Review: {{title}}

       ## Context
       <!-- Current state and problem statement -->

       ## Constraints
       <!-- Technical, business, and team constraints -->

       ## Options Considered
       <!-- List of architectural options -->

       ## Trade-off Analysis
       <!-- Pros/cons of each option -->

       ## Decision
       <!-- Selected option with rationale -->
       ```
    3. `bug-investigation-template.md`:
       ```markdown
       # Bug Investigation: {{title}}

       ## Symptoms
       <!-- What was observed -->

       ## Root Cause Analysis
       <!-- Why it happened -->

       ## Fix Applied
       <!-- What was changed -->

       ## Verification Steps
       <!-- How we confirmed the fix -->

       ## Prevention
       <!-- How to prevent recurrence -->
       ```

    These templates serve as reference documentation for the report structure. The Go code writes reports following these structures.
  </action>
  <verify>
    <automated>ls -1 /Users/callumcowie/repos/Aether/.aether/references/templates/*.md | wc -l | grep -q "3"</automated>
  </verify>
  <done>
    - Three template files exist with correct section headers
    - Templates use {{variable}} placeholder syntax
    - Templates are valid markdown
  </done>
</task>

<task type="auto">
  <name>Task 3: Update dashboard to display Oracle progress</name>
  <files>
    .aether/ts-host/src/dashboard.ts
    .aether/ts-host/src/dashboard/worker-widget.ts
  </files>
  <read_first>
    .aether/ts-host/src/dashboard.ts
    .aether/ts-host/src/dashboard/worker-widget.ts
  </read_first>
  <action>
    Update `.aether/ts-host/src/dashboard.ts`:

    1. Add Oracle state tracking to Dashboard:
       ```typescript
       interface OracleState {
         phase: string;
         iteration: number;
         active: boolean;
       }
       ```
    2. In `onEvent`, handle Oracle events:
       - `ceremony.oracle.phase_transition` → update `oracleState.phase`
       - `ceremony.oracle.iteration` → update `oracleState.iteration`, set `oracleState.active = true`
       - `ceremony.loop.break` with source "oracle" → set `oracleState.active = false`
    3. In `render`, include Oracle section in the dashboard frame:
       - If `oracleState.active`, show: "🔮 Oracle Phase: {phase} | Iteration: {iteration}"
       - If not active, omit the Oracle section

    Update `.aether/ts-host/src/dashboard/worker-widget.ts`:
    1. No changes needed (Oracle is not a worker widget; it's a dashboard header section)

    The Oracle section appears in the dashboard header alongside wave info.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npx tsx --test test/dashboard.test.ts</automated>
  </verify>
  <done>
    - Dashboard tracks Oracle phase and iteration
    - Oracle section renders in dashboard frame when active
    - Dashboard tests pass
  </done>
</task>

<task type="auto">
  <name>Task 4: Write tests for dashboard Oracle visibility</name>
  <files>
    .aether/ts-host/test/dashboard.test.ts
  </files>
  <read_first>
    .aether/ts-host/src/dashboard.ts
    .aether/ts-host/test/dashboard.test.ts
  </read_first>
  <action>
    Update `.aether/ts-host/test/dashboard.test.ts`:

    Add 3 tests:
    1. `dashboard shows Oracle phase on ceremony.oracle.phase_transition` — pass phase_transition event, assert dashboard frame includes "Oracle Phase: verify"
    2. `dashboard shows Oracle iteration on ceremony.oracle.iteration` — pass iteration event, assert frame includes "Iteration: 5"
    3. `dashboard hides Oracle section on loop break` — pass loop.break event, assert frame does not include "Oracle" (or is hidden)

    Mock log-update to capture frame output for assertions.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npx tsx --test test/dashboard.test.ts</automated>
  </verify>
  <done>
    - dashboard.test.ts has 3 new Oracle visibility tests
    - Tests cover phase display, iteration display, and hide on break
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cmd/oracle_loop.go → filesystem | Synthesis reports written to Oracle output directory (not .aether/data/) |
| dashboard.ts → stdout | Oracle progress display is presentation-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-03 | Tampering | oracle_loop.go | mitigate | Report output directory is outside .aether/data/ |
| T-117-04 | Information Disclosure | synthesis reports | accept | Reports contain research findings (non-sensitive by design) |
</threat_model>

<verification>
- `go test ./cmd/... -run "Oracle"` passes
- `npm test -- test/dashboard.test.ts` passes
- `npx tsc --noEmit -p tsconfig.build.json` compiles cleanly
- Template files exist and are valid markdown
</verification>

<success_criteria>
- writeOracleSynthesisReport branches on template type
- Three template-specific report structures implemented
- Dashboard displays Oracle phase and iteration
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/117-oracle-enhancement/117-02-SUMMARY.md`
</output>
