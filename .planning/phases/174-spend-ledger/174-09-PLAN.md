---
phase: 174-spend-ledger
plan: 09
type: execute
wave: 5
depends_on: [07, 08]
files_modified:
  - CLAUDE.md
  - AGENTS.md
  - .aether/docs/context-continuity.md
  - .aether/docs/ci-context-assembly-design.md
  - .aether/docs/PARITY_CLASSIC_VS_GO.md
  - .aether/QUEEN.md
  - .aether/skills/colony/worker-priming/SKILL.md
  - .aether/skills/colony/pheromone-protocol/SKILL.md
  - cmd/spend_naming_test.go
autonomous: true
requirements: [SPEND-07]

must_haves:
  truths:
    - "No figure in the spend report or the end-of-build line can be traced to a character budget"
    - "The documentation that describes colony-prime's 8,000-character context budget no longer calls it a token budget"
    - "A future change that wires a character budget into a spend figure fails a named test rather than shipping a green dashboard"
  artifacts:
    - path: "cmd/spend_naming_test.go"
      provides: "Source invariant over the spend files plus a doc-lint over the renamed documents"
      contains: "TestSpendFigureNeverDerivesFromCharBudget"
    - path: "CLAUDE.md"
      provides: "The colony-prime budget section renamed to name characters"
      contains: "## Context Assembly Budget"
  key_links:
    - from: "cmd/spend_naming_test.go"
      to: "cmd/colony_prime_context.go"
      via: "the forbidden identifiers colonyPrimeBudgetChars and colonyPrimeCompactBudgetChars"
      pattern: "colonyPrimeBudgetChars"
    - from: "cmd/spend_naming_test.go"
      to: "CLAUDE.md"
      via: "the doc-lint file table"
      pattern: "CLAUDE.md"
---

<objective>
Close the naming hole that let a 186x undercount stay invisible: make it impossible for a spend
figure to come from a character budget, and stop nine documents calling an 8,000-character context
budget a "Token Budget".

Purpose: SPEND-07. A dashboard fed by `colonyPrimeBudgetChars` can go green while real cost is
unchanged. The code is already correctly named — `colonyPrimeBudgetChars` counts characters and
`proofContext.BudgetMetric` already reports `"chars"`. The mislabelling is in prose, and prose is
what a future planner reads before wiring the wrong constant into a report.

Output: the renamed sections, and two named tests that fail if either half of the rule is broken.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/174-spend-ledger/174-CONTEXT.md
@.planning/phases/174-spend-ledger/174-RESEARCH.md
@.planning/phases/174-spend-ledger/174-PATTERNS.md

<interfaces>
The code is already honest — this is the state to protect, not to change:

    cmd/colony_prime_context.go:21    colonyPrimeBudgetChars        = 8000
    cmd/colony_prime_context.go:108   BudgetMetric:      "chars",
    cmd/colony_prime_context.go:348   budget := colonyPrimeBudgetChars
    cmd/build_print_brief.go:288      // Sums colonyPrimeCompactBudgetChars, not colonyPrimeBudgetChars (WR-03)
    cmd/proof_cmd.go:148              BudgetMetric:  "chars",
    cmd/proof_cmd.go:163              BudgetMetric:  "words",
    cmd/context.go:71                 BudgetMetric:      "words",

The spend surface this plan fences off, produced by plans 174-01 through 174-08:

    pkg/codex/usage.go
    cmd/spend_ledger.go
    cmd/spend_resolve.go
    cmd/spend_cmd.go
    cmd/spend_session_capture.go
    cmd/wrapper_usage_claude.go
    cmd/wrapper_usage_opencode.go
    plus `writeCeremonySpendLine` in cmd/ceremony_cmd.go

Every occurrence of the phrase "token budget" in the repo today, from 174-RESEARCH.md's audit,
with the ruling for each (RESEARCH's Pitfall 5 requires individual review, not a global replace —
some of these describe a real token budget and must be left alone):

| File | Ruling |
|---|---|
| `CLAUDE.md:271-272`, `:589`, `:619` (`## Token Budget`) | RENAME — describes `colonyPrimeBudgetChars` |
| `AGENTS.md:171`, `:591`, `:622` (`## Token Budget`) | RENAME — the same section, mirrored |
| `.aether/docs/context-continuity.md:12` | RENAME — "Hard token budgets on injected context" is the same char budget |
| `.aether/docs/ci-context-assembly-design.md:32`, `:98`, `:183`, `:373`, `:798` | RENAME — every hit is qualified by a char count |
| `.aether/docs/PARITY_CLASSIC_VS_GO.md:52` | RENAME — "token budget trimming" names colony-prime's trim order |
| `.aether/skills/colony/worker-priming/SKILL.md:51` | RENAME — describes the same trim order |
| `.aether/skills/colony/pheromone-protocol/SKILL.md:87` | RENAME — "trimmed for token budget" is the same trimming |
| `.aether/QUEEN.md:72`, `:230` | CORRECT IN PLACE — a learned wisdom entry whose own text says "8000 chars"; colony-prime injects this string into worker prompts, so a misnaming here reaches workers |
| `CHANGELOG.md:429` | LEAVE — a dated historical entry; rewriting shipped history is dishonest |
| `.aether/skills/colony/ai-design-contract/SKILL.md:54` | LEAVE — "max input tokens, max output tokens" is a genuine token budget |
| `.aether/codex/aether-scout.toml:30`, `.codex/agents/aether-scout.toml:30` (`### Token Budget Awareness`) | VERIFY THEN RULE — read the section body; leave if it concerns a worker's real context window, rename if it describes the char budget |
| `.aether/ts-host/src/hive-injector.ts` and its `dist/` build output, `.aether/ts-host/test/hive-injector.test.ts` | LEAVE — the hive injector enforces a genuine token budget on injected wisdom |
| `pkg/exchange/testdata/colony-archive.xml`, `pkg/colony/testdata/learning-observations.golden.json`, `pkg/codex/prompt_test.go:405` | LEAVE — test fixtures and golden files; editing them changes what the tests assert |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the character budget wherever it is called a token budget</name>
  <files>CLAUDE.md, AGENTS.md, .aether/docs/context-continuity.md, .aether/docs/ci-context-assembly-design.md, .aether/docs/PARITY_CLASSIC_VS_GO.md, .aether/QUEEN.md, .aether/skills/colony/worker-priming/SKILL.md, .aether/skills/colony/pheromone-protocol/SKILL.md</files>
  <read_first>
    - CLAUDE.md (the `## Token Budget` section near line 619, and the colony-prime paragraph near line 271)
    - AGENTS.md (the mirrored `## Token Budget` section near line 622 and the paragraph near line 171)
    - cmd/colony_prime_context.go (lines 1-40 and 340-360 — proof that the constant counts characters)
    - .aether/docs/ci-context-assembly-design.md (lines 25-40, 90-105, 175-190, 365-380, 790-805)
    - .aether/docs/context-continuity.md (lines 1-25)
    - .aether/docs/PARITY_CLASSIC_VS_GO.md (line 52)
    - .aether/skills/colony/worker-priming/SKILL.md (lines 45-60)
    - .aether/skills/colony/pheromone-protocol/SKILL.md (lines 80-95)
    - .aether/QUEEN.md (lines 65-80 and 225-235)
    - .aether/codex/aether-scout.toml and .codex/agents/aether-scout.toml (the `### Token Budget Awareness` sections — read the body before ruling)
    - .planning/phases/174-spend-ledger/174-RESEARCH.md § "Pitfall 5"
  </read_first>
  <action>
    Apply the ruling table in `<interfaces>` above file by file. Do NOT run a global find-and-replace
    — several hits describe a real token budget and renaming them would create the opposite
    confusion.

    In `CLAUDE.md` and `AGENTS.md`, rename the `## Token Budget` heading to
    `## Context Assembly Budget (characters)` in both files, and update the section body's first
    sentence to say plainly that colony-prime assembles worker context within a **character**
    budget. Add one short paragraph beneath the table, in the same plain-English register the rest
    of the file uses, stating: this budget counts characters of assembled context, it is not a
    measure of what a worker costs, a real worker spends roughly twenty times what this budget
    caps, and the number that reflects real spend is the token ledger surfaced at the end of every
    build and by `aether spend`. Cross-reference `aether spend` by name so a reader who wants cost
    is sent to the right place.

    Also fix the two prose references that feed the heading: `CLAUDE.md:271-272`'s "all within a
    token budget (see Token Budget below)" and `AGENTS.md:171`'s equivalent become "all within a
    character budget (see Context Assembly Budget below)". Fix `CLAUDE.md:589` and `AGENTS.md:591`
    ("independent of the colony-prime token budget") to say character budget.

    While in `CLAUDE.md`, correct the Quick Reference table's slash-command counts to the real
    post-Phase-174 numbers. Count them rather than guessing: `ls .claude/commands/ant/*.md | wc -l`
    and `ls .opencode/commands/ant/*.md | wc -l`. The current entry reads 60 + 60 and is already
    stale before this phase; state the counted figures. Mirror the same correction in `AGENTS.md`
    if it carries an equivalent table.

    In `.aether/docs/context-continuity.md`, change design principle 2 from "Hard token budgets on
    injected context" to name character budgets, and add a half-sentence noting that token spend is
    measured separately by the spend ledger.

    In `.aether/docs/ci-context-assembly-design.md`, rename each of the five hits to say character
    budget (including the `## 6. Token Budget Management` heading and both table rows), keeping the
    existing char figures exactly as they are.

    In `.aether/docs/PARITY_CLASSIC_VS_GO.md:52`, change "token budget trimming" to "character
    budget trimming".

    In `.aether/skills/colony/worker-priming/SKILL.md` and
    `.aether/skills/colony/pheromone-protocol/SKILL.md`, change the two references to say character
    budget. These files are injected into worker prompts, so the wording a worker reads must match
    what the runtime actually does.

    In `.aether/QUEEN.md`, correct the wisdom entry text at line 72 and its mirror in the log at
    line 230 so they read "character budget" rather than "token budget" — the entry's own body
    already says "8000 chars", and colony-prime injects this string into worker context. Change the
    wording only; do not alter the timestamps, ids, or surrounding structure.

    For `.aether/codex/aether-scout.toml` and `.codex/agents/aether-scout.toml`: read the
    `### Token Budget Awareness` section body. If it concerns the scout's real context window or
    output length, leave both files untouched and record that ruling in the plan summary. If it
    describes colony-prime's assembled-context budget, rename it in both files identically —
    `.codex/agents/` is a canonical platform source and the two must not diverge.

    Leave `CHANGELOG.md`, `.aether/skills/colony/ai-design-contract/SKILL.md`, the
    `.aether/ts-host/` hive-injector files, and every `testdata`/golden file untouched, for the
    reasons in the ruling table.
  </action>
  <verify>
    <automated>go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Token Budget$' CLAUDE.md AGENTS.md` returns 0 for both files.
    - `grep -c '^## Context Assembly Budget (characters)$' CLAUDE.md AGENTS.md` returns 1 for both files.
    - `grep -in 'token budget' CLAUDE.md AGENTS.md .aether/docs/context-continuity.md .aether/docs/ci-context-assembly-design.md .aether/docs/PARITY_CLASSIC_VS_GO.md .aether/QUEEN.md .aether/skills/colony/worker-priming/SKILL.md .aether/skills/colony/pheromone-protocol/SKILL.md` returns no matches.
    - `grep -c 'aether spend' CLAUDE.md` returns at least 1 — the reader who wants cost is redirected.
    - `git diff --stat CHANGELOG.md .aether/skills/colony/ai-design-contract/SKILL.md` is empty — history and the genuine token budget were left alone.
    - `git diff --name-only | grep -c 'testdata\|golden'` returns 0.
    - The counts in CLAUDE.md's Quick Reference equal the output of `ls .claude/commands/ant/*.md | wc -l` and `ls .opencode/commands/ant/*.md | wc -l`.
    - `go test ./... -count=1` exits 0 — no doc-contract or skill-contract test was broken by the rewording.
  </acceptance_criteria>
  <done>Every document that describes colony-prime's 8,000-character budget now names characters, redirects a cost question to the spend ledger, and the documents describing a genuine token budget are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two named guards — no char budget in the spend surface, no token-budget naming in the renamed docs</name>
  <files>cmd/spend_naming_test.go</files>
  <read_first>
    - cmd/colony_prime_context.go (lines 15-30 — the two budget constants and their names)
    - cmd/spend_ledger.go, cmd/spend_resolve.go, cmd/spend_cmd.go (the files the invariant fences)
    - cmd/command_source_hygiene_test.go (lines 1-75 — `repoRootForCommandSourceTest`, the repo-root resolution helper to reuse rather than a `..`-relative path)
    - cmd/subcommand_reachability_ratchet_test.go (lines 1-60 — the house style for a guard test that names every offender rather than failing on the first)
    - .planning/phases/174-spend-ledger/174-RESEARCH.md § "Pitfall 5"
  </read_first>
  <behavior>
    - The spend source-file list the invariant checks is non-empty and every named file exists — a guard that silently checks nothing is a guard that passed vacuously, which is the failure mode this repo has shipped before.
    - No file in the spend surface references `colonyPrimeBudgetChars` or `colonyPrimeCompactBudgetChars`.
    - Adding a reference to `colonyPrimeBudgetChars` in `cmd/spend_cmd.go` makes the test fail, naming that file.
    - None of the eight renamed documents contains the phrase "token budget" in any case.
    - Adding the phrase back to `CLAUDE.md` makes the doc-lint fail, naming the file and the line.
    - The lint's file table is explicit, so a document that legitimately discusses a token budget is never scanned.
  </behavior>
  <action>
    Create `cmd/spend_naming_test.go` with two tests.

    `TestSpendFigureNeverDerivesFromCharBudget`:
    - Declare `spendSurfaceFiles` as an explicit slice of repo-relative paths: `pkg/codex/usage.go`,
      `cmd/spend_ledger.go`, `cmd/spend_resolve.go`, `cmd/spend_cmd.go`,
      `cmd/spend_session_capture.go`, `cmd/wrapper_usage_claude.go`,
      `cmd/wrapper_usage_opencode.go`.
    - Resolve the repo root with `repoRootForCommandSourceTest()` — never a `..`-relative path,
      for the reason `cmd/cli_flag_audit_test.go` documents (a relative corpus silently
      mis-scopes depending on the test binary's working directory).
    - `t.Fatalf` if the slice is empty or if any listed file does not exist. Comment that this
      emptiness check is what keeps the guard from passing vacuously.
    - Read each file and collect every file that contains `colonyPrimeBudgetChars` or
      `colonyPrimeCompactBudgetChars`. Fail once at the end naming every offender, not on the
      first.
    - Additionally assert that `writeCeremonySpendLine`'s body in `cmd/ceremony_cmd.go` contains
      neither identifier: extract the function body by scanning from its `func` line to the first
      line that is exactly `}`, the same lightweight extraction the acceptance criteria in plan
      174-08 use, and check that slice. Comment why the whole of `ceremony_cmd.go` is not fenced:
      the file legitimately renders unrelated context figures, and fencing it wholesale would be a
      rule nobody could keep.
    - Add a doc comment stating the reason in plain terms: a spend report fed by a character budget
      can show a healthy number while real cost is unchanged, which is how the 186x undercount
      stayed invisible.

    `TestDocsDoNotNameTheCharacterBudgetATokenBudget`:
    - Declare `charBudgetDocs` as an explicit slice of the eight repo-relative paths renamed in
      task 1.
    - `t.Fatalf` if the slice is empty or any file is missing.
    - For each, scan line by line for a case-insensitive match of `token budget` and collect
      `path:line` for every hit. Fail once naming all of them.
    - Comment that the table is deliberately explicit rather than a repo-wide scan, because
      `CHANGELOG.md` records history, `ai-design-contract/SKILL.md` and the hive injector describe
      genuine token budgets, and test fixtures must not be edited to satisfy a lint — the ruling
      for each of those lives in `.planning/phases/174-spend-ledger/174-09-PLAN.md`.
  </action>
  <verify>
    <automated>go test ./cmd/... -run 'TestSpendFigureNeverDerivesFromCharBudget|TestDocsDoNotNameTheCharacterBudgetATokenBudget' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/... -run 'TestSpendFigureNeverDerivesFromCharBudget|TestDocsDoNotNameTheCharacterBudgetATokenBudget' -count=1` exits 0 with both tests reported as PASS.
    - `grep -c 'repoRootForCommandSourceTest' cmd/spend_naming_test.go` returns at least 1, and `grep -c '"\.\."' cmd/spend_naming_test.go` returns 0.
    - `grep -c 'len(spendSurfaceFiles) == 0\|len(charBudgetDocs) == 0' cmd/spend_naming_test.go` returns 2 — both guards refuse to pass vacuously.
    - Temporarily adding `_ = colonyPrimeBudgetChars` to `cmd/spend_cmd.go` makes `TestSpendFigureNeverDerivesFromCharBudget` fail naming that file; the line is removed and the test passes. Record this negative check in the plan summary.
    - Temporarily adding the line `## Token Budget` to `CLAUDE.md` makes `TestDocsDoNotNameTheCharacterBudgetATokenBudget` fail naming the file and line; the line is removed and the test passes. Record this negative check too.
    - `go test ./... -count=1` exits 0 and `go test ./... -race -count=1` exits 0.
  </acceptance_criteria>
  <done>Two named tests fail if a spend figure ever reaches for a character budget, or if the renamed documents ever call one a token budget, and both have been shown to fail when the rule is broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → a future planner's design choice | Prose that misnames a constant is how the wrong constant gets wired into a report |
| `.aether/QUEEN.md` wisdom text → worker prompts | Colony-prime injects this string, so a misnaming here reaches every worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-32 | Spoofing | a character budget presented as spend | mitigate | `TestSpendFigureNeverDerivesFromCharBudget` fences the seven spend source files plus `writeCeremonySpendLine` against both budget identifiers, and refuses to run against an empty file list |
| T-174-33 | Repudiation | documentation drifting back | mitigate | `TestDocsDoNotNameTheCharacterBudgetATokenBudget` lints the eight renamed documents on every test run and names every offending line |
| T-174-34 | Tampering | over-broad rename breaking a genuine token budget | mitigate | The lint table is explicit and the ruling for every excluded file is recorded in this plan and repeated in the test's own comment; asserted by a `git diff --stat` check showing `CHANGELOG.md`, the design-contract skill and all fixtures untouched |
| T-174-35 | Tampering | edited golden/fixture files to satisfy a lint | mitigate | An acceptance criterion asserts no `testdata` or golden file appears in the diff |
</threat_model>

<verification>
- `go test ./... -count=1` passes, including every doc-contract, skill-contract and wrapper-contract test.
- `go test ./... -race` passes.
- `go vet ./...` passes.
- `grep -rniI "token budget" --exclude-dir=.git --exclude-dir=.planning .` returns only the deliberately-excluded files listed in this plan's ruling table.
</verification>

<success_criteria>
- No spend source file and no part of the closeout spend line references a character budget constant.
- The eight documents describing colony-prime's character budget no longer call it a token budget, and point a cost question at the spend ledger instead.
- The genuinely token-denominated documents and all historical and fixture files are untouched.
- Both guards have been demonstrated to fail when their rule is broken.
</success_criteria>

<output>
After completion, create `.planning/phases/174-spend-ledger/174-09-SUMMARY.md`.

This is the phase's last plan, so its SUMMARY carries two items forward that would otherwise
evaporate with the phase directory:

1. **The `pkg/trace/cost.go` residue, restated in full.** That file holds a hardcoded per-model
   USD rate table for retired model names, called from `pkg/agent/pool.go:192`, whose
   `CalculateCost` multiplies only input and output tokens and ignores cache tokens entirely — the
   same undercount shape Phase 174 exists to prevent, and a direct contradiction of D-02's "no
   model-price table is ever built". It predates this phase and no plan touched it. No live caller
   was found under `cmd/` for any of the three dispatch platforms, so it is probably dead, but that
   was not proven exhaustively. It needs an owner ruling — delete it as dead code, or fix its
   arithmetic — and the SUMMARY must say so in plain English rather than by file reference alone.
   The same note is recorded in `.planning/STATE.md` § "Blockers/Concerns" and in
   174-RESEARCH.md's Open Question 1 resolution.

2. **The ruling made on the two `aether-scout.toml` files** in task 1 — whether their
   `### Token Budget Awareness` sections were left alone (a genuine token budget) or renamed (the
   character budget), with the reason.
</output>
