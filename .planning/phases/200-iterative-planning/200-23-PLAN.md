---
phase: 200-iterative-planning
plan: 23
type: execute
wave: 13
depends_on: [200-17, 200-18, 200-19, 200-20, 200-21, 200-22, 200-24, 200-25]
files_modified:
  - cmd/testdata/classic-contract/v1/schema.json
  - cmd/testdata/classic-contract/v1/mechanisms.json
  - cmd/testdata/classic-contract/v1/cases.json
  - cmd/classic_contract_test.go
  - cmd/planning_public_paths_200_test.go
  - cmd/planning_real_repo_200_test.go
  - .planning/phases/200-iterative-planning/200-GATE-RECEIPT.md
autonomous: true
requirements: [SYNTH-02, CEC-03, PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Executable proofs cover the entire intent-to-spec-to-multi-pass-plan-to-exact-acceptance journey."
    - "Every confidence, decision, stop, acceptance, revision, replay, and platform invariant has a positive and refusal case."
    - "The finished repository passes targeted, full, and race suites with an auditable gate receipt."
  artifacts:
    - path: "cmd/testdata/classic-contract/v1/mechanisms.json"
      provides: "SYN-200-01 through SYN-200-12 source and capability mappings"
    - path: "cmd/testdata/classic-contract/v1/cases.json"
      provides: "Executable Phase-200 semantic proof cases"
    - path: "cmd/planning_real_repo_200_test.go"
      provides: "Real temporary-repository multi-pass lifecycle proof"
    - path: ".planning/phases/200-iterative-planning/200-GATE-RECEIPT.md"
      provides: "Final deterministic validation evidence"
  key_links:
    - from: "cmd/testdata/classic-contract/v1/mechanisms.json"
      to: "cmd/testdata/classic-contract/v1/cases.json"
      via: "resolvable positive and refusal case IDs created in one task"
      pattern: "SYN-200|V-200"
    - from: "cmd/testdata/classic-contract/v1/cases.json"
      to: "cmd/classic_contract_test.go"
      via: "case execution and causal assertions"
      pattern: "SYN-200|V-200"
    - from: "cmd/planning_real_repo_200_test.go"
      to: "cmd/codex_workflow_cmds.go"
      via: "public command execution in temporary repository"
      pattern: "plan|spec|discuss"
---

<objective>
Turn the full Phase-200 contract into executable semantic proof, exercise it through public paths in a real temporary repository, and record final full/race gates.

Purpose: Prove behavior and causality rather than merely the presence of UI strings, files, or mechanism labels.
Output: Extended classic cases, public-platform tests, real-repository end-to-end tests, and a final gate receipt.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md
@.planning/phases/200-iterative-planning/200-UI-SPEC.md
@cmd/testdata/classic-contract/v1/schema.json
@cmd/testdata/classic-contract/v1/mechanisms.json
@cmd/testdata/classic-contract/v1/cases.json
@cmd/classic_contract_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Phase-200 mechanism schema and executable causal cases atomically</name>
  <read_first>
    - cmd/testdata/classic-contract/v1/cases.json
    - cmd/testdata/classic-contract/v1/mechanisms.json
    - cmd/testdata/classic-contract/v1/schema.json
    - cmd/classic_contract_test.go
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Validation Architecture)
    - .planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md (complete decision table)
  </read_first>
  <files>cmd/testdata/classic-contract/v1/schema.json, cmd/testdata/classic-contract/v1/mechanisms.json, cmd/testdata/classic-contract/v1/cases.json, cmd/classic_contract_test.go</files>
  <behavior>
    - Schema accepts existing SYN-199 records and exactly formed SYN-200-01 through SYN-200-12 records without weakening required evidence links.
    - Every SYN-200 mechanism links source anchors, valid classic capability IDs, a modern invariant, and resolvable executable positive and negative case IDs created by this task.
    - Named proof groups V-200-E2E, V-200-CONFIDENCE, V-200-DECISION, V-200-STOP, V-200-ACCEPT, V-200-REVISION, V-200-REPLAY, and V-200-PLATFORM execute deterministically.
    - Cases assert state/artifact/receipt before and after facts plus exact refusal side effects, not string presence alone.
    - Every SYN-200 mechanism references resolvable success and negative cases, and every Phase-200 case is referenced.
    - Causal cases require evidence_that_would_change on every continue/stop/gap/candidate and a persisted typed Queen recommendation before candidate acceptance.
  </behavior>
  <action>Extend the schema's decision-ID, phase-group, mechanism, and case rules additively so all existing Phase-199 corpus data remains valid while current Phase-200 records require source anchors, capability references, modern invariants, and positive plus negative executable case IDs. Add exactly SYN-200-01 through SYN-200-12 to mechanisms.json from 200-CLASSIC-SYNTHESIS.md, linking the documented capability set including CAP-005, CAP-010, CAP-011, CAP-012, CAP-056, CAP-061, and CAP-069 where specified. In the same task, extend cases.json additively with deterministic fixtures and expected causal assertions for: whole-goal and feature-scoped draft versus approved specifications carrying every typed stable-ID body category; all four presets; two or more Scout to Route passes; Route-Setter-proposed/Go-validated five-score movement with fresh evidence and restatement or supplied-overall refusal; first-pass post-Scout and later post-card material decisions; equivalent direct-resume and contract-affecting successor-DRAFT/approval/reconciliation branches; all four stop reasons plus material override; `evidence_that_would_change` on every continue, stop, residual gap, and candidate; persisted typed Queen accept-or-revise recommendation with rationale/evidence/producer; pending candidate non-eligibility; exact acceptance and stale/refused acceptance; scoped revision preservation and affected closure; legacy migration; both crash windows; exact/divergent replay; projection tamper repair; width/no-color/JSON semantics with internal stop enums mapped to the exact human labels `target sufficiency`, `diminishing returns`, `stall detected`, and `iteration cap`; full `Specification` wording; and platform managed-wrapper paths. Enhance the Go corpus runner to require unique decision IDs, valid capability references, resolvable case IDs, no orphan Phase-200 case, and execution through the relevant pure kernel or command fixture with before/after state hashes, artifact sets, dispatch counts, receipt chains, and structured result fields. Do not allow expected-output text alone to satisfy a case. Mechanisms, cases, cross-reference validation, and their tests must land atomically in this task; no assertion may depend on a later plan.</action>
  <acceptance_criteria>
    - Exactly SYN-200-01 through SYN-200-12 appear once each with source anchors, valid CAP references, modern invariants, and executable positive plus negative cases.
    - All eight named V-200 groups contain at least one success and one refusal/regression case.
    - Each SYN-200-01 through SYN-200-12 mechanism resolves to executable cases and no Phase-200 case is orphaned.
    - Refusal cases assert zero prohibited writes, dispatches, activation, or approval.
    - Existing Phase-199 cases remain byte-valid and passing.
    - Removing a mechanism, source, CAP link, case link, or referenced case makes the contract test fail in this task.
    - Corpus cases fail when a readable SPEC category, causal evidence field, recommendation authority, public stop label, or post-card later-decision boundary is missing.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestClassicContract.*Schema|TestClassicContract.*Mechanism|TestClassicContract.*Phase200|TestClassicContract.*Causal' -count=1</automated>
  </verify>
  <done>The schema, twelve synthesis mechanisms, and all referenced causal cases form one closed, executable corpus with no unresolved or orphan links.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove public platform paths and a real multi-pass repository journey</name>
  <read_first>
    - cmd/planning_public_paths_200_test.go (create if absent)
    - cmd/planning_real_repo_200_test.go (create if absent)
    - cmd/classic_contract_test.go
    - cmd/codex_workflow_cmds.go
    - .aether/commands/plan.yaml
    - .aether/commands/spec.yaml
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Platform and Responsive Contracts)
  </read_first>
  <files>cmd/planning_public_paths_200_test.go, cmd/planning_real_repo_200_test.go</files>
  <behavior>
    - A temporary real repository executes init/discuss draft, exact spec approval, preset selection, at least two Scout/Route passes, candidate review, exact acceptance, and build eligibility.
    - Codex direct, Claude-generated, and OpenCode-generated paths resolve to the same runtime semantics and authority outcomes.
    - Failures at every authority boundary leave state and artifacts unchanged and provide exact recovery.
    - Human views use exact approved stop labels and `Specification`; machine results retain internal enums plus semantic public-label fields.
  </behavior>
  <action>Create deterministic integration tests using the real CLI command constructors, repository filesystem, lifecycle transactions, and staged worker fixture artifacts; do not require live model providers or network. Drive at least two passes where the first card identifies a weakest gap and the second cites fresh evidence that changes Route-Setter-proposed applicable scores and semantic plan content after Go validation. Prove that a later Scout material candidate does not pause before Route-Setter, that the full card appends before the owner boundary, and that a contract-affecting answer creates a successor DRAFT requiring exact approval/reconciliation while an equivalent non-contract answer resumes directly. Assert ordered receipts/cards, evidence_that_would_change for every decision/stop/gap/candidate, reasoned stop, typed Queen recommendation/rationale/producer, non-active candidate, exact acceptance, accepted PlanRevision bindings, build/run gate parity, full typed Specification body, and scoped spec-revision invalidation/preservation. Run the same structured result fixtures through Codex visual/JSON renderers and validate manually synchronized Claude/OpenCode wrapper contracts and command spellings with `go run ./cmd/aether source-check`. Cover widths 47, 48, 63, 64, 95, and 96 and redirected no-color output, asserting exact human stop labels and full `Specification` wording while machine enums remain internal.</action>
  <acceptance_criteria>
    - Real-repository test has at least two complete Scout and Route receipt pairs and two ordered cards.
    - First pass weakest gap becomes the next Scout focus; second-pass score movement cites genuinely fresh applicable evidence.
    - Candidate cannot build before acceptance and can build/run only after exact receipt-bound acceptance.
    - Direct Codex, Claude, and OpenCode paths agree semantically; managed wrapper projections pass source-check.
    - Candidate recommendation and every evidence_that_would_change value are identical across human and JSON projections and originate in the persisted result.
    - Scoped revision test preserves an independent completed branch and blocks affected build/seal scope.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningPublicPaths200|TestPlanningRealRepo200' -count=1</automated>
  </verify>
  <done>The restored workflow is proven through real repository state and every supported public presentation path.</done>
</task>

<task type="auto">
  <name>Task 3: Run final targeted, full, and race gates and record the receipt</name>
  <read_first>
    - .planning/phases/200-iterative-planning/200-GATE-RECEIPT.md (create if absent)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Validation Architecture and Gate Commands)
    - cmd/planning_real_repo_200_test.go
    - cmd/testdata/classic-contract/v1/cases.json
    - .aether/commands/plan.yaml
    - .aether/commands/spec.yaml
    - cmd/front_door_init_wrapper_199_test.go
    - cmd/init_proposals_test.go
    - cmd/front_door_199_test.go
    - cmd/plan_depth_proposal_manifest_test.go
    - cmd/phase_research_dispatch_test.go
    - cmd/plan_wrapper_cards_test.go
    - cmd/plan_wrapper_ceremony_test.go
    - cmd/platform_doc_hygiene_test.go
    - cmd/lifecycle_wrapper_contract_test.go
  </read_first>
  <files>.planning/phases/200-iterative-planning/200-GATE-RECEIPT.md</files>
  <action>Run the focused Phase-200 test selection, classic contract suite, `go run ./cmd/aether source-check`, the exact migrated regression tests named below, go test -timeout 180s ./..., and go test -race -timeout 300s ./.... The migrated regressions must prove that post-init `/ant-plan`, the three-knob depth card, research approval commands/state, old Decision Moment headings, and legacy wrapper result keys no longer remain required, while their still-valid safety assertions still pass. Record exact commands, UTC timestamp, repository revision, dirty-worktree note without modifying unrelated files, Go version, pass/fail, test package counts, corpus/schema/mechanism/case digests, source-check result, and any bounded retry in 200-GATE-RECEIPT.md. The receipt may claim implementation gate success only when all commands pass; it must not claim Phase-205 owner product acceptance, publish, deploy, or release completion.</action>
  <acceptance_criteria>
    - Focused Phase-200, classic corpus, source-check, migrated named regressions, full, and race commands all exit zero.
    - Receipt contains exact commands and environment/revision/digest evidence sufficient to reproduce the run.
    - Receipt explicitly distinguishes automated implementation verification from future product acceptance.
    - No unrelated dirty file is staged, reverted, or rewritten.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(FrontDoorInitWrapperParity|InitSuggestedNextMatchesTopProposal|FrontDoorHelp|FrontDoorInit|PlanManifestCarriesDepthProposal|PlanEmitsPhaseResearchDispatchesFromDraft|PhaseResearchDispatchedOncePerPhase|PlanWrapperCardsParity|PlanWrapperCeremonyContract|PlanWrapperStageSkeleton|LifecycleCommandDocsPreferRuntimeCLI|PlanningContractDocuments200|LifecycleFlatMirrorsMatchCanonical|LifecycleWrappersAvoidRetiredDepthVocabulary|LifecycleWrappersCarryStructuredBlocks)' -count=1 &amp;&amp; go run ./cmd/aether source-check &amp;&amp; go test -timeout 180s ./... &amp;&amp; go test -race -timeout 300s ./...</automated>
  </verify>
  <done>Phase 200 has a reproducible green implementation receipt across semantic, public-path, full, and race gates.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestClassicContract.*Phase200|TestPlanningPublicPaths200|TestPlanningRealRepo200' -count=1, the exact migrated regression selection, `go run ./cmd/aether source-check`, go test -timeout 180s ./..., and go test -race -timeout 300s ./....
</verification>

<success_criteria>
- The classic synthesis is backed by executable causal positive and negative cases.
- A real repository demonstrates multiple evidence-driven Scout/Route passes, exact candidate acceptance, living-plan revision, and public-platform parity.
- Targeted, full, and race gates pass and are recorded without claiming out-of-scope final product acceptance.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-23-SUMMARY.md when done.
</output>
