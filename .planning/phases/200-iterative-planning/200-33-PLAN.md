---
phase: 200-iterative-planning
plan: 33
type: execute
wave: 19
depends_on: [200-31]
files_modified:
  - cmd/build_attempt.go
  - cmd/build_start_transaction.go
  - cmd/build_start_transaction_200_test.go
autonomous: true
gap_closure: true
requirements: [PLAN-06]
must_haves:
  truths:
    - "One build-start request canonically binds accepted plan/spec/candidate/timeline authority, selected tasks, execution owner/mode, attempt identity, and every conditional start effect."
    - "Checkpoint, attempt journal, latest pointer, manifest binding, state transition, stale-artifact cleanup, reviewer-window effects, and retry provenance commit all-or-none before dispatch."
    - "The transaction holds the repository session from authority reload through target derivation and commit, and faults or stale baselines dispatch no worker."
    - "[spec-less edge: PLAN-06/idempotency] Exact replay of one committed build-start request returns the same receipt and authority without duplicating an attempt, latest pointer, checkpoint, manifest, state event, parent/check-fix provenance, or supersession history."
  artifacts:
    - path: "cmd/build_start_transaction.go"
      provides: "Canonical all-target build-start request, receipt, and transaction"
      exports: ["commitBuildStart"]
    - path: "cmd/build_attempt.go"
      provides: "Pure build-attempt record derivation consumed by the transaction"
      contains: "buildAttemptRecord"
    - path: "cmd/build_start_transaction_200_test.go"
      provides: "Target matrix, replay, fault, and stale-authority proof"
      contains: "TestBuildStartTransaction200"
  key_links:
    - from: "cmd/build_start_transaction.go"
      to: "cmd/planning_repository_session.go"
      via: "authority reload and all start effects occur inside one session"
      pattern: "withPlanningMutationSession"
    - from: "cmd/build_start_transaction.go"
      to: "cmd/plan_authority.go"
      via: "accepted authority is recomputed before building targets"
      pattern: "PlanAuthority"
---

<objective>
Define one atomic build-start transaction before migrating any production caller.

Purpose: A build must not become partially runnable or dispatch workers against mixed old/new authority.
Output: Pure request/target derivation, idempotent receipt, and all-target session commit.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-28-SUMMARY.md
@.planning/phases/200-iterative-planning/200-31-SUMMARY.md
@cmd/build_attempt.go
@cmd/codex_build.go
@cmd/codex_build_finalize.go
@cmd/check_fix_attempt.go
@cmd/coherent_job_retry.go
@cmd/forced_reviewer_waiver.go
</context>

<interfaces>
Define buildStartRequest, buildStartEffects, buildStartReceipt, and commitBuildStart(root, request, options). Request identity includes canonical accepted authority, phase/tasks, execution owner/mode, generated time, dispatches, and optional checkpoint/manifest/claims/parent/check-fix/reviewer effects. Existing callers remain until Plan 34 migrates them.
</interfaces>

<scope_guard>
Modify only the three declared files. Do not migrate production callers in this plan. Preserve user dirt and use scoped task commits.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the complete build-start target and fault matrix</name>
  <files>cmd/build_start_transaction_200_test.go</files>
  <behavior>
    - "Each request variant declares exactly the effects it needs and no implicit write occurs."
    - "Every injected apply/rename/receipt fault restores all start targets and emits no dispatch authorization."
    - "A stale state/spec/candidate/revision/task baseline refuses before writes."
    - "Exact replay returns the same receipt and produces no duplicate history."
  </behavior>
  <action>Create table-driven tests for direct, plan-only, Queen-led, externally finalized unbound, automatic check-fix, and coherent child-retry target sets. Include conditional stale cleanup and forced-reviewer window/pending-decision removal, latest-pointer true/false, parent link, manifest/completion binding, and state promotion. Inject faults at every lifecycle transaction point and compare all target bytes plus artifact inventory. Add the tagged exact-replay proof and verify no worker callback runs before durable success.</action>
  <verify>
    <automated>go test ./cmd -run '^TestBuildStartTransaction200' -count=1</automated>
  </verify>
  <done>The fail-first suite defines one finite all-target contract for every production start variant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build canonical requests and pure attempt records</name>
  <files>cmd/build_attempt.go, cmd/build_start_transaction.go, cmd/build_start_transaction_200_test.go</files>
  <action>Extract deterministic attempt ID, run ID, workspace fingerprint, attempt record, pointer, parent/check-fix provenance, and transition target derivation from beginBuildAttempt into pure functions that accept all time/random inputs explicitly. Canonically hash the complete buildStartRequest and validate phase/task/dispatch/manifest/authority consistency. Preserve makeLatest=false for coherent recovery records. Do not write files, clear windows, or consult global store from pure derivation.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestBuildStartTransaction200|TestBuildAttempt.*)' -count=1</automated>
  </verify>
  <done>Every build-start byte can be derived and validated before mutation from one canonical request.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Commit all start effects under one repository session</name>
  <files>cmd/build_start_transaction.go, cmd/build_attempt.go, cmd/build_start_transaction_200_test.go</files>
  <action>Implement commitBuildStart by acquiring the Plan 28 session before reloading state and Plan 31 accepted authority, validating selected tasks and all optional effect prerequisites, then deriving request/targets. Declare exact expected baselines for checkpoint, attempt, pointer, manifest, state, reviewer window/pending decisions, stale deletions, and parent/check-fix artifacts. Commit through one session-aware lifecycle transaction and write a canonical receipt as the last declared target. Exact replay independently validates and returns that receipt; conflicting replay refuses. Return dispatch authority only after durable commit. No call to legacy SaveJSON helpers may occur inside this canonical path.</action>
  <verify>
    <automated>go test ./cmd -run '^TestBuildStartTransaction200' -count=1</automated>
  </verify>
  <done>The canonical transaction makes every conditional build-start effect atomic, idempotent, and authority-bound.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| accepted plan to build request | Start must bind exact current execution authority. |
| conditional effects to transaction | Omitted or partial targets can leave mixed start state. |
| durable receipt to worker dispatch | No worker may run before commit is complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-33-01 | Elevation of privilege | build authority | mitigate | Canonical authority-bound request under repository session. |
| T-200-33-02 | Tampering | conditional effects | mitigate | Closed target matrix and fault rollback. |
| T-200-33-03 | Repudiation | replay | mitigate | Canonical request/receipt and duplicate-free exact replay. |
| T-200-33-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run every target/fault/replay variant and existing build-attempt tests. Confirm the canonical transaction has no caller yet and no global-store side effects during derivation.
</verification>

<success_criteria>
- One request covers every build-start variant and conditional effect.
- All effects commit atomically before dispatch.
- Replay is idempotent and stale authority is mutation-free.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-33-SUMMARY.md when done.
</output>
