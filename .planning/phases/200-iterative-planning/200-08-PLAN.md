---
phase: 200-iterative-planning
plan: 08
type: execute
wave: 3
depends_on: [200-01, 200-02, 200-07]
files_modified:
  - cmd/specification.go
  - cmd/specification_test.go
  - cmd/spec_projection.go
  - cmd/spec_projection_test.go
autonomous: true
requirements: [PLAN-04, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Settled intent produces one canonical draft specification lineage."
    - "Only exact explicit approval of a revision makes that specification eligible to ground planning."
    - "Scoped revisions preserve unaffected items and invalidate only affected downstream scope."
  artifacts:
    - path: "cmd/specification.go"
      provides: "Draft, revision, approval, and reconciliation engine"
    - path: "cmd/spec_projection.go"
      provides: "Readable .aether/SPEC.md projection and repair"
  key_links:
    - from: "cmd/specification.go"
      to: "pkg/colony/specification.go"
      via: "immutable revision transitions"
      pattern: "SpecRevision|SpecApprovalReceipt"
    - from: "cmd/spec_projection.go"
      to: ".aether/SPEC.md"
      via: "derived human-readable projection"
      pattern: "SPEC.md"
---

<objective>
Implement the Go-owned specification lineage beneath discuss and the public spec command.

Purpose: Make product intent durable, reviewable, explicitly approved, and safely revisable before iterative planning can rely on it.
Output: Transactional draft/revise/approve engine plus deterministic readable projection.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-PATTERNS.md
@pkg/colony/specification.go
@cmd/lifecycle_tx.go
@cmd/state_load.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create and revise immutable specification lineages</name>
  <read_first>
    - cmd/specification.go (create if absent)
    - pkg/colony/specification.go
    - cmd/lifecycle_tx.go
    - cmd/state_load.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-09, D-11, D-12)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Specification Lifecycle)
  </read_first>
  <files>cmd/specification.go, cmd/specification_test.go</files>
  <behavior>
    - Settled charter and decision evidence creates one draft revision idempotently.
    - Whole-goal and feature-scoped drafts populate typed stable-ID sections for outcome, included behavior, exclusions, binding decisions, requirements, owner-checkable acceptance, negative expectations, recovery expectations, and affected public paths.
    - Add, modify, or remove creates an immutable successor with an explicit classified delta and unchanged stable IDs outside scope.
    - A material revision marks exactly the requirement, task, and proof closure affected; it does not mutate the active plan.
  </behavior>
  <action>Implement pure draft construction followed by a lifecycle-transaction write. Transform settled charter, material decisions, constraints, acceptance evidence, negative/recovery expectations, and known public surfaces into the distinct typed body sections defined in pkg/colony/specification.go: outcome, included behavior, exclusions, binding decisions, requirements, owner-checkable acceptance, negative expectations, recovery expectations, and affected public paths. Derive every section item's stable ID from semantic lineage rather than ordinal position, include evidence refs, and return the existing revision on exact replay. Whole-goal drafts must cover the complete settled contract; feature-scoped drafts must identify their exact requirement/acceptance scope while preserving all unaffected sections and IDs. Implement successor creation for explicit add, modify, and remove operations: require predecessor ID/hash, calculate the classified delta across each typed section, preserve unaffected item IDs/content, and mark the predecessor superseded only as part of the atomic successor commit. Accept the typed successor_spec_required disposition from planning decisions as revision evidence. Compute an affected-spec-item set for later plan reconciliation but do not activate or rewrite a plan here. Reject stale predecessor, duplicate ID, ambiguous removal, missing required body category, or divergent replay without mutation.</action>
  <acceptance_criteria>
    - Exact draft or revision retry returns the same revision and receipt.
    - A scoped change leaves every unaffected item ID and hash unchanged.
    - Add, modify, and remove appear distinctly in the stored delta.
    - Whole-goal and feature-scoped fixtures contain every required body category in canonical order, and scoped revision preserves unaffected IDs byte-for-byte.
    - Stale predecessor and divergent replay fixtures leave state byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestSpecification.*Draft|TestSpecification.*Revise|TestSpecification.*Replay' -count=1</automated>
  </verify>
  <done>The repository has a canonical, immutable, scope-aware specification lineage with safe transactional writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Approve exact revisions and maintain the readable projection</name>
  <read_first>
    - cmd/specification.go
    - cmd/spec_projection.go (create if absent)
    - pkg/colony/specification.go
    - cmd/lifecycle_tx.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-10, D-12)
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Specification Review)
  </read_first>
  <files>cmd/specification.go, cmd/specification_test.go, cmd/spec_projection.go, cmd/spec_projection_test.go</files>
  <behavior>
    - Approval requires an exact revision ID, content hash, and explicit approval token.
    - Approval writes state and regenerates .aether/SPEC.md atomically; exact retry is idempotent and stale approval fails.
    - Editing or deleting .aether/SPEC.md cannot change authority; projection repair restores it from state.
  </behavior>
  <action>Implement approval validation that accepts only the documented exact token bound to the current draft revision ID and hash. Store an immutable approval receipt and never infer approval from file presence, editor exit, generic yes, or planning invocation. Render .aether/SPEC.md deterministically from state in the UI-SPEC order: outcome, included behavior, explicit exclusions, binding decisions, stable requirements, owner-checkable acceptance, negative expectations, recovery expectations, and affected public paths, followed by revision/status, scope, evidence summaries, classified delta, affected downstream scope, and exact next command. Do not render a generic `items` section or omit an empty but contractually required category without an explicit `none`. Declare state and projection in one lifecycle transaction. Add projection drift detection and repair from canonical state; never parse the projection back into authority. An approved material successor remains unreconciled for plan purposes until later plans accept a new candidate.</action>
  <acceptance_criteria>
    - Wrong token, wrong hash, superseded revision, and stale revision approval all fail without mutation.
    - Exact approval retry returns the original receipt.
    - State and projection cannot diverge under injected write failure.
    - Manual projection alteration is detected and repaired from state without changing revision or approval records.
    - Projection tests assert every typed body category and stable ID for whole-goal and feature-scoped revisions in the approved UI-SPEC order.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestSpecification.*Approve|TestSpecProjection' -count=1</automated>
  </verify>
  <done>Only explicit exact approval grants specification authority, while humans receive a repairable readable projection.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestSpecification|TestSpecProjection' -count=1 and inspect exact replay, stale base, partial-write, projection tamper, and affected-scope fixtures.
</verification>

<success_criteria>
- A single canonical specification lineage moves draft to approved only by an exact receipt.
- Scoped revisions preserve unaffected semantic identity and expose affected downstream scope.
- .aether/SPEC.md is readable and repairable but never authoritative.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-08-SUMMARY.md when done.
</output>
