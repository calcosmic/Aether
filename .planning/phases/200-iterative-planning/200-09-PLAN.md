---
phase: 200-iterative-planning
plan: 09
type: execute
wave: 3
depends_on: [200-01, 200-03, 200-04, 200-05, 200-06, 200-07]
files_modified:
  - cmd/planning_stage.go
  - cmd/planning_stage_test.go
  - cmd/planning_stage_receipt.go
  - cmd/planning_stage_receipt_test.go
autonomous: true
requirements: [CEC-03, PLAN-01, PLAN-03, PLAN-04]
must_haves:
  truths:
    - "Scout and Route-Setter run as separately authorized visible stages."
    - "A crash resumes from the last committed receipt without rerunning or skipping completed work."
    - "No manifest pre-authorizes later stages or directly activates a plan."
  artifacts:
    - path: "cmd/planning_stage.go"
      provides: "Legal iterative-planning transition state machine"
    - path: "cmd/planning_stage_receipt.go"
      provides: "Content-addressed, chained stage receipts"
  key_links:
    - from: "cmd/planning_stage.go"
      to: "cmd/planning_stage_receipt.go"
      via: "receipt-gated transitions"
      pattern: "StageReceipt"
    - from: "cmd/planning_stage.go"
      to: "cmd/planning_timeline.go"
      via: "completed Route stage appends one iteration card"
      pattern: "IterationCard"
---

<objective>
Create the staged transition and receipt engine for the visible Scout to Route-Setter loop.

Purpose: Replace whole-chain prompt ownership with one-stage-at-a-time Go authorization, crash safety, exact replay, and explicit human boundaries.
Output: Pure state machine, persisted receipt envelope, and exhaustive transition tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-PATTERNS.md
@pkg/colony/planning.go
@cmd/codex_plan.go
@cmd/codex_plan_finalize.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define legal planning stages and one-stage authorization</name>
  <read_first>
    - cmd/planning_stage.go (create if absent)
    - cmd/codex_plan.go
    - cmd/codex_plan_finalize.go
    - pkg/colony/planning.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-05, D-06, D-13 through D-16)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Staged State Machine)
  </read_first>
  <files>cmd/planning_stage.go, cmd/planning_stage_test.go</files>
  <behavior>
    - Legal states include preset_required, scout_ready, scout_running, owner_decision, spec_approval_required, reconciliation_required, route_ready, route_running, continue_ready, candidate_ready, accepted, and failed.
    - A dispatch manifest authorizes exactly one worker caste, iteration, input frontier, and output contract.
    - Route cannot run until the exact Scout receipt exists; a next Scout cannot run until the prior Route card exists.
  </behavior>
  <action>Implement a pure transition reducer over typed planning stage state. Require approved specification revision, preset, run ID, pass ordinal, base plan revision, prior card hash, and expected worker caste in each authorization. Emit one-stage manifests only: Scout receives evidence frontier plus weakest gap; Route-Setter receives the exact Scout receipt and current candidate snapshot. Human owner-decision, spec-approval-required, reconciliation-required, and candidate-acceptance states emit no worker manifest. A contract-affecting decision resolution must transition owner_decision to spec_approval_required, then only an exact approved successor plus affected-scope reconciliation may rebind or restart the planning frontier; it may not jump directly to Route-Setter or candidate progression. Reject illegal jumps, mismatched caste, future-stage fields, reused authorization IDs, missing predecessor receipts, and any manifest field that purports to activate a plan.</action>
  <acceptance_criteria>
    - A table enumerates every allowed transition and rejects every direct skipped-stage transition.
    - Each manifest names exactly one caste and one expected result type.
    - Route authorization requires the current Scout receipt hash.
    - Candidate-ready and accepted states cannot dispatch a worker.
    - Contract-affecting answer fixtures cannot dispatch a worker until exact successor SPEC approval and scoped reconciliation complete.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningStage.*Transition|TestPlanningStage.*Manifest' -count=1</automated>
  </verify>
  <done>The runtime can authorize only the next legitimate Scout or Route-Setter action and no prompt owns later transitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist chained receipts and resume exactly after interruption</name>
  <read_first>
    - cmd/planning_stage_receipt.go (create if absent)
    - cmd/planning_stage.go
    - cmd/planning_timeline.go
    - cmd/lifecycle_tx.go
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Receipts, Replay, and Recovery)
    - .planning/phases/200-iterative-planning/200-PATTERNS.md (finalizer and transaction analogues)
  </read_first>
  <files>cmd/planning_stage_receipt.go, cmd/planning_stage_receipt_test.go, cmd/planning_stage.go, cmd/planning_stage_test.go</files>
  <behavior>
    - A stage receipt binds manifest hash, input frontier, output hash, caste, pass, prior receipt, and resulting state.
    - Exact finalizer retry returns the same receipt; changed output for the same manifest is a conflict.
    - Resume derives the sole next legal action from committed receipts and cards.
  </behavior>
  <action>Define canonical StageReceipt hashing and atomic persistence under the planning run. Chain each receipt to the previous committed stage receipt and validate that its output hash matches the staged artifact. Commit receipt plus state transition through lifecycle transactions. On exact replay return the stored receipt without duplicate artifacts; on divergent replay fail closed and leave the frontier unchanged. Implement resume reconstruction by verifying receipt and card chains, identifying an in-flight manifest without receipt, and returning retry, finalize, next stage, owner decision, or candidate review. Do not add Phase-202 general live-event transport; Phase-200 receipts are bounded lifecycle records only.</action>
  <acceptance_criteria>
    - Crash-before-receipt resumes finalization of the same manifest; crash-after-receipt advances once.
    - Exact replay is byte-stable and divergent replay reports a conflict.
    - Tampered output, broken prior hash, or mismatched caste blocks resume without rewriting state.
    - No receipt type imports or introduces a general event-bus dependency.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningStageReceipt|TestPlanningStage.*Resume' -count=1</automated>
  </verify>
  <done>Every stage boundary is content-addressed, transactionally committed, and exactly resumable after interruption.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestPlanningStage|TestPlanningStageReceipt' -count=1, including illegal transitions, both crash windows, exact and divergent replay, and chain tamper.
</verification>

<success_criteria>
- Scout and Route-Setter are distinct receipt-gated stages.
- Manifests authorize one next action and cannot pre-authorize acceptance or activation.
- Crash recovery and replay derive a single truthful next state from durable artifacts.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-09-SUMMARY.md when done.
</output>
