---
phase: 200-iterative-planning
plan: 11
type: execute
wave: 4
depends_on: [200-07, 200-08]
files_modified:
  - cmd/discuss.go
  - cmd/discuss_test.go
  - cmd/discuss_spec_200_test.go
autonomous: true
requirements: [PLAN-04, PLAN-05]
must_haves:
  truths:
    - "Discuss asks only unresolved material questions, grounded in available evidence."
    - "Settled discussion creates and renders a draft specification, then directs the owner to spec review."
  artifacts:
    - path: "cmd/discuss.go"
      provides: "Evidence-first intent clarification and draft-spec handoff"
  key_links:
    - from: "cmd/discuss.go"
      to: "cmd/planning_decision.go"
      via: "material decision classification and answer reuse"
      pattern: "PlanningDecision"
    - from: "cmd/discuss.go"
      to: "cmd/specification.go"
      via: "settled intent draft creation"
      pattern: "Spec|Specification"
---

<objective>
Restore discuss as an evidence-first intent-refinement stage that hands settled intent to a draft specification.

Purpose: Prevent generic interviews and ensure planning starts only after an owner-reviewed specification rather than directly from chat answers.
Output: Updated discuss state transitions, draft creation handoff, and backward-compatible resume tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-UI-SPEC.md
@cmd/discuss.go
@cmd/planning_decision.go
@cmd/specification.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace generic question menus with evidence-first material batches</name>
  <read_first>
    - cmd/discuss.go
    - cmd/discuss_test.go
    - cmd/planning_decision.go
    - cmd/planning_evidence.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-05 through D-08)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Discuss Integration)
  </read_first>
  <files>cmd/discuss.go, cmd/discuss_test.go, cmd/discuss_spec_200_test.go</files>
  <behavior>
    - Existing charter, survey, context, and prior equivalent answers are evaluated before a question is emitted.
    - All currently known material behavior, authority, risk, scope, and acceptance questions appear in one batch.
    - Evidence-answerable or generic preference questions are suppressed, while scoped pending decisions still resume exactly.
  </behavior>
  <action>Replace category-count-driven generic question generation with the shared planning decision policy. Build an admissible evidence frontier from existing charter, survey, context, and prior answers; classify only unresolved material questions; render each with why-now, evidence, recommendation, consequences, affected scope, and exact answer syntax. Preserve current pending-decision state and exact answer command compatibility by migrating it into the new batch representation. Exact equivalent answers may be reused; changed meaning or impact must be revalidated. Keep redirect and explicit owner constraints authoritative.</action>
  <acceptance_criteria>
    - A fully evidence-resolved fixture emits no generic fallback questions.
    - Five material domains can appear in one deterministic batch with all decision-card fields.
    - Existing pending discussion fixtures resume and exact replay does not duplicate answers.
    - Scope or acceptance-impact drift forces a new question even when normalized prose matches.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestDiscuss.*Evidence|TestDiscuss.*Material|TestDiscuss.*Resume' -count=1</automated>
  </verify>
  <done>Discuss interrupts the owner only for material unresolved intent and does so with enough evidence to decide.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create a draft specification when discussion is settled</name>
  <read_first>
    - cmd/discuss.go
    - cmd/discuss_spec_200_test.go
    - cmd/specification.go
    - cmd/spec_projection.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-09, D-10)
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Discuss Closeout)
  </read_first>
  <files>cmd/discuss.go, cmd/discuss_test.go, cmd/discuss_spec_200_test.go</files>
  <behavior>
    - Settled intent creates or reuses the exact canonical draft, including every required typed readable body category, and renders .aether/SPEC.md.
    - Discuss closeout reports draft status and directs the owner to aether spec review or approval, never directly to plan.
    - An existing approved specification is not silently replaced by rerunning discuss.
  </behavior>
  <action>At the settled-intent transition, call the specification engine with the charter, evidence, accepted answers, constraints, explicitly excluded scope, owner-checkable acceptance, negative/recovery expectations, and known affected public paths. Populate the typed stable-ID outcome, included-behavior, exclusion, binding-decision, requirement, acceptance, negative, recovery, and public-path sections rather than forwarding a generic item list. Commit the draft plus projection atomically, reuse the same revision on exact retry, and refuse to overwrite or fork an existing approved lineage without an explicit scoped revision request through aether spec. Return a structured closeout containing draft revision/hash, per-section counts and body content, unresolved count zero, projection path, and exact next spec command. Remove any settled-to-plan guidance. Do not approve the draft automatically.</action>
  <acceptance_criteria>
    - Settled discuss creates one draft revision and .aether/SPEC.md with matching hash.
    - Exact rerun returns the same draft and does not append a revision.
    - Closeout says draft and offers aether spec, not aether plan.
    - Existing approved specification remains unchanged and yields explicit revision guidance.
    - Whole-goal and feature-scoped settled fixtures project all required body categories with stable IDs and preserve unaffected scoped content.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestDiscuss.*DraftSpec|TestDiscuss.*Settled|TestDiscuss.*ApprovedSpec' -count=1</automated>
  </verify>
  <done>Discuss now terminates in a visible draft specification that requires separate owner approval.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestDiscuss|TestSpecification.*Draft' -count=1; verify no-evidence generic prompts are gone and settled output never skips spec review.
</verification>

<success_criteria>
- Discuss questions are evidence-first, material, batched, and scoped.
- Settled intent transactionally creates the canonical draft specification.
- The next authority is explicit specification review, never plan generation or acceptance.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-11-SUMMARY.md when done.
</output>
