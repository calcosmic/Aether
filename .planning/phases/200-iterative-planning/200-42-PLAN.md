---
phase: 200-iterative-planning
plan: 42
type: execute
wave: 26
depends_on: [200-41]
files_modified:
  - cmd/curation_cmds_test.go
  - cmd/flags_test.go
  - cmd/history_test.go
  - cmd/memory_test.go
  - cmd/phase_test.go
autonomous: true
gap_closure: true
requirements: [CEC-03, PLAN-06]
must_haves:
  truths:
    - "Curation, flag, history, memory, and phase tests restore the original AETHER_ROOT instead of re-saving their temporary value."
    - "These test families pass in either order and leave no deleted repository selected for later Phase 200 public-path tests."
  artifacts:
    - path: "cmd/history_test.go"
      provides: "Order-independent history fixtures"
    - path: "cmd/phase_test.go"
      provides: "Order-independent phase fixtures"
  key_links:
    - from: "cmd/history_test.go"
      to: "cmd/testing_main_test.go"
      via: "shared exact environment cleanup"
      pattern: "Setenv|bind.*Repository"
---

<objective>
Remove the first bounded family of stale-root test contamination.

Purpose: The final gate showed later tests resolving a deleted temp repository. These five files contain the same proven self-restoring cleanup defect.
Output: Order-independent curation, flag, history, memory, and phase tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-41-SUMMARY.md
@cmd/testing_main_test.go
</context>

<scope_guard>
Mechanical test-isolation migration only. Do not change command behavior, expected modern output, D-01..D-16 authority, or production repository resolution.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace self-restoring root cleanup in curation, flags, and history</name>
  <files>cmd/curation_cmds_test.go, cmd/flags_test.go, cmd/history_test.go</files>
  <behavior>
    - Each test sees its own repository while running.
    - Cleanup restores the value captured before mutation, including true absence.
    - Reversed and repeated runs have identical outcomes.
  </behavior>
  <action>Replace every `defer os.Setenv("AETHER_ROOT", os.Getenv("AETHER_ROOT"))` pattern with the Plan 41 helper or `t.Setenv` as appropriate. Keep fixture data repository-local and remove redundant cleanup that can run after `t.TempDir` deletion. Preserve all assertions.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(Curation|Flag|History)' -count=2</automated>
  </verify>
  <done>No self-restoring cleanup remains in these three files and their tests pass twice uncached.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace self-restoring root cleanup in memory and phase</name>
  <files>cmd/memory_test.go, cmd/phase_test.go</files>
  <behavior>
    - Memory and phase tests do not leak store or repository authority.
    - Running phase tests after memory tests and vice versa produces no containment error.
  </behavior>
  <action>Apply the same exact cleanup contract. When a case executes Cobra, reset root flags and bind the data path with its repository rather than setting only AETHER_ROOT. Keep legacy compatibility expectations that do not grant modern acceptance authority.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(Memory|Phase)' -count=2</automated>
  </verify>
  <done>All five owned files are order-independent and preserve their existing behavioral assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| one test to the next | Process environment and package globals outlive a test body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-42-01 | Tampering | AETHER_ROOT cleanup | mitigate | Capture before mutation and restore through testing cleanup. |
| T-200-42-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
`! rg -n 'defer os\.Setenv\("AETHER_ROOT", os\.Getenv\("AETHER_ROOT"\)\)' cmd/{curation_cmds,flags,history,memory,phase}_test.go`
</verification>

<success_criteria>
- The owned stale-root patterns are gone.
- Owned test families pass twice without command or authority changes.
</success_criteria>

<output>Create `.planning/phases/200-iterative-planning/200-42-SUMMARY.md` when done.</output>
