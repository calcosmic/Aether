---
phase: 200-iterative-planning
plan: 45
type: execute
wave: 27
depends_on: [200-42, 200-43, 200-44]
files_modified:
  - pkg/storage/storage.go
  - pkg/storage/lock_unix.go
  - pkg/storage/lock_windows.go
  - pkg/storage/storage_malformed_test.go
  - pkg/agent/spawn_tree_test.go
autonomous: true
gap_closure: true
requirements: [CEC-03, PLAN-06]
must_haves:
  truths:
    - "A missing optional file in a repository-backed store remains detectable with errors.Is(err, fs.ErrNotExist)."
    - "Fresh spawn-tree, spawn-run, learning, instinct-delivery, and midden ledgers initialize normally instead of failing on their first read."
    - "Unreadable, linked, non-regular, and corrupt files still fail closed and remain distinguishable from absence."
  artifacts:
    - path: "pkg/storage/storage.go"
      provides: "Portable repository-store error identity"
    - path: "pkg/agent/spawn_tree_test.go"
      provides: "Repository-backed absent-versus-corrupt spawn ledger proof"
  key_links:
    - from: "pkg/agent/spawn_tree.go"
      to: "pkg/storage/storage.go"
      via: "errors.Is(fs.ErrNotExist) across wrapped repository reads"
      pattern: "ErrNotExist"
---

<objective>
Restore the three-way optional-file contract under the physical repository store.

Purpose: Plan 27 correctly tightened containment but repository-backed ENOENT lost portable error identity, so fresh colonies could not create several append-only ledgers.
Output: Cross-platform error normalization and tests proving absent is allowed while corruption/tampering is refused.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-27-SUMMARY.md
@.planning/phases/200-iterative-planning/200-44-SUMMARY.md
@pkg/agent/spawn_tree.go
@cmd/instinct_application.go
@cmd/learning_cmds.go
</context>

<scope_guard>
Do not convert arbitrary repository I/O failures into absence. Symlink, directory, permission, malformed JSON, and corrupt spawn-tree cases remain errors. This repair preserves the modern contained store and only restores the documented fresh-file case.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve portable ENOENT identity through repository reads</name>
  <files>pkg/storage/storage.go, pkg/storage/lock_unix.go, pkg/storage/lock_windows.go, pkg/storage/storage_malformed_test.go</files>
  <behavior>
    - Missing file and missing parent components satisfy `errors.Is(err, os.ErrNotExist)` after every storage wrapper.
    - A path replaced by a symlink, directory, or permission-denied node does not satisfy ErrNotExist.
    - Unix and Windows adapters expose the same Go error contract.
  </behavior>
  <action>Start with repository-backed tests, then normalize platform open errors at the narrow boundary so `%w` wrapping retains `os.ErrNotExist`. Do not use error-string matching. Audit `readFileUnlocked`, repository parent opening, and `DataFileExists` for the same contract, retaining full path/context in the rendered error.</action>
  <verify>
    <automated>go test ./pkg/storage -run '^TestRepository(StoreMissingErrorIdentity200|StoreRefusesNonAbsentFailures200)$' -count=1 &amp;&amp; GOOS=windows GOARCH=amd64 go test -exec=/usr/bin/true ./pkg/storage -run '^$' -count=1</automated>
  </verify>
  <done>Repository-store absence is portable and every non-absence safety error remains distinct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove fresh optional ledgers and corrupt-ledger refusal</name>
  <files>pkg/agent/spawn_tree_test.go</files>
  <behavior>
    - RecordSpawn and BeginRun create the first repository-backed ledger when neither file exists.
    - Parse returns an empty ledger for absence only.
    - Corrupt, linked, or unreadable ledgers return errors and are not overwritten.
  </behavior>
  <action>Add repository-backed cases alongside the legacy-store cases; use a real temporary repository authority. Prove the first append, replay, and corruption fingerprint. Then run the existing command-level consumers that failed alone (colonize, completion packet, learning, and instinct delivery) without changing their expected behavior.</action>
  <verify>
    <automated>go test ./pkg/agent -run '^TestSpawnTreeRepository(StoreFresh|StoreCorrupt|RunLedgerFresh)200$' -count=1 &amp;&amp; go test ./cmd -run '^(TestApplicationHistoryShapeMatchesInstinctApply|TestLearningPropose|TestColonizeFallsBackWhenRealSurveyorsFail|TestColonizeFinalizeRecordsExternalSurveyors|TestColonizeIncludesDispatchContract|TestColonizePreservesWorkerWrittenSurveyArtifacts|TestColonizeWritesSurveyArtifactsAndUpdatesState|TestCompletionPacketSubmittedBytes)$' -count=1</automated>
  </verify>
  <done>All named fresh-ledger consumers pass; corruption and containment refusal remain fail-closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| OS open error to optional-ledger caller | Misclassification can either brick a fresh colony or erase evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-45-01 | Tampering | optional-file classification | mitigate | Only real ENOENT is treated as absence; corrupt/unreadable nodes remain errors. |
| T-200-45-02 | Repudiation | append-only ledgers | mitigate | Fingerprint corrupt inputs and prove zero overwrite. |
| T-200-45-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run `go test ./pkg/storage ./pkg/agent -count=1`, then the focused command consumers above.
</verification>

<success_criteria>
- Fresh repository-backed ledgers create successfully.
- `errors.Is` works across platforms and wrapper layers.
- No tampered ledger is mislabeled missing.
</success_criteria>

<output>Create `.planning/phases/200-iterative-planning/200-45-SUMMARY.md` when done.</output>
