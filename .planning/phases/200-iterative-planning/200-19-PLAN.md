---
phase: 200-iterative-planning
plan: 19
type: execute
wave: 9
depends_on: [200-10, 200-11, 200-15, 200-16, 200-17, 200-18]
files_modified:
  - cmd/planning_visuals.go
  - cmd/planning_visuals_test.go
  - cmd/codex_visuals.go
  - cmd/codex_visuals_test.go
autonomous: true
requirements: [CEC-03, PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Users can see Scout and Route-Setter alternate, with compact evidence-backed iteration cards and explicit stop reasons."
    - "Specification, decision, candidate, acceptance, impact, and recovery states have legible terminal output at every supported width."
    - "JSON exposes the same semantic model as terminal output without ANSI, cursor rewrites, or hidden authority."
  artifacts:
    - path: "cmd/planning_visuals.go"
      provides: "Terminal and JSON projections for specification and iterative planning"
    - path: "cmd/codex_visuals.go"
      provides: "Registered caste identity, stage markers, and spec icon"
  key_links:
    - from: "cmd/codex_visuals.go"
      to: "cmd/planning_visuals.go"
      via: "planning/spec result delegation"
      pattern: "renderPlanning|renderSpec"
    - from: "cmd/planning_visuals.go"
      to: "cmd/lifecycle_facts.go"
      via: "shared renderer-neutral facts and result models"
      pattern: "LifecycleFacts|PlanningIterationCard"
---

<objective>
Implement the approved terminal interaction contract and JSON sibling projection for the restored planning loop.

Purpose: Make the evidence, confidence, authority, and recovery model visibly understandable without changing state or relying on color.
Output: Width-aware renderers, identity/stage integration, JSON parity, and golden/semantic tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-UI-SPEC.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@cmd/codex_visuals.go
@cmd/lifecycle_facts.go
@cmd/next_action.go
@cmd/planning_decision.go
@cmd/plan_candidate.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render every specification and planning state from shared models</name>
  <read_first>
    - cmd/planning_visuals.go (create if absent)
    - cmd/codex_visuals.go
    - cmd/lifecycle_projection.go
    - cmd/next_action.go
    - cmd/planning_decision.go
    - cmd/plan_candidate.go
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (complete contract)
  </read_first>
  <files>cmd/planning_visuals.go, cmd/planning_visuals_test.go</files>
  <behavior>
    - One banner and identity strip precede preset, Scout, Route-Setter, iteration card, decision, stop, candidate, acceptance, revision-impact, and refusal content.
    - Specification views render the typed stable-ID body in the approved order: outcome, included behavior, exclusions, binding decisions, requirements, owner-checkable acceptance, negative/recovery expectations, and affected public paths.
    - Compact cards show five before-to-after scores, fresh evidence, weakest gap, evidence that would change the next decision, semantic delta, authority impact, and continue/stop; full detail appears only on detail routes.
    - Candidate review renders the persisted typed Queen accept-or-revise recommendation, evidence-grounded rationale, and producing authority before the acceptance prompt; candidate success offers build and run equally, while draft specification and pending-candidate states never show an execution action.
  </behavior>
  <action>Create pure text and JSON renderers for the result models delivered by Plans 10 through 18. Use the exact UI-SPEC vocabulary and information hierarchy: concise headline, evidence/rationale, status, then Next Up. Render the complete specification body from its typed stable-ID sections in the approved order; never substitute a generic item list. Render all five dimensions in locked order and use words/symbols in addition to color for added, changed, removed, warning, stop, and accepted states. Decision cards include exact decision, why now, evidence, recommendation, consequences, affected scope, and resume. Keep machine enums internal: map `target_met` to `target sufficiency`, `diminishing_returns` to `diminishing returns`, `stalled_gap` to `stall detected`, and `pass_cap` to `iteration cap` in every human view; JSON retains the closed enum and carries an explicit public label sibling. Every continue/stop card, detail view, residual gap, and candidate review renders `Evidence that would change this decision:` from the typed `evidence_that_would_change` field. Candidate review renders the persisted typed `Queen recommends: accept|revise because ...` advice, evidence IDs, and producer before the exact acceptance prompt; the renderer must not invent it. JSON must expose equivalent typed fields and never embed ANSI-formatted text as its only meaning.</action>
  <acceptance_criteria>
    - Semantic tests find all five dimensions, evidence IDs, weakest gap, delta, disposition, and next action in both terminal and JSON projections.
    - Decision, stop, candidate, accepted, impact, and refusal fixtures contain every UI-SPEC field.
    - Whole-goal and feature-scoped specification fixtures render all required typed body sections and stable IDs in order.
    - Human fixtures use only target sufficiency, diminishing returns, stall detected, and iteration cap; internal enum tokens remain machine-only.
    - Continue/stop/detail/candidate fixtures contain evidence_that_would_change, and candidate human/JSON fixtures agree on Queen recommendation, rationale, evidence IDs, and producer.
    - Candidate-ready output contains no build/run; accepted output presents both with equal priority.
    - Detail output is reachable only through the explicit iteration/candidate routes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningVisuals.*Semantic|TestPlanningVisuals.*JSON|TestPlanningVisuals.*Authority' -count=1</automated>
  </verify>
  <done>Every Phase-200 state has a concise terminal view and semantically equivalent structured JSON view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce width, no-color, identity, and no-rewrite contracts</name>
  <read_first>
    - cmd/codex_visuals.go
    - cmd/codex_visuals_test.go
    - cmd/planning_visuals.go
    - cmd/planning_visuals_test.go
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Responsive Terminal Rules and Icons)
  </read_first>
  <files>cmd/planning_visuals.go, cmd/planning_visuals_test.go, cmd/codex_visuals.go, cmd/codex_visuals_test.go</files>
  <behavior>
    - Width below 48 stacks labels and values; 48-63 uses compact rows; 64-95 uses the standard table; 96 or more uses compact wide rows.
    - Color and no-color output communicate identical statuses and actions, and redirected output has no ANSI or cursor-control sequences.
    - Scout and Route-Setter retain caste identities; specification uses the locked scroll icon and the full label Specification.
  </behavior>
  <action>Integrate planning renderers with the existing visual-mode switch, width detector, caste palette, and stage-marker conventions. Add specification identity as the scroll icon followed by the exact plain label `Specification`; never abbreviate it to `Spec` in human output (the compact identity key `SPEC` remains allowed where the UI-SPEC explicitly requires it). Render Scout and Route-Setter as alternating complete stage blocks, never an in-place spinner or rewritten card. Implement the four exact width bands with bounded line lengths and stable column fallbacks. Honor NO_COLOR, AETHER_FORCE_COLOR, CLICOLOR_FORCE, TTY, and JSON mode through existing helpers. In no-color and redirected output retain textual +/-/removed/warning/status cues, full stop-label mappings, evidence-that-would-change, and recommendation authority. Reject any emitted carriage return, cursor-up, clear-line, or progress rewrite sequence.</action>
  <acceptance_criteria>
    - Golden or semantic fixtures cover widths 47, 48, 63, 64, 95, and 96 for every major card class.
    - No nonempty rendered line exceeds its requested width except an explicitly tested unbreakable identifier with a documented safe wrap.
    - NO_COLOR and redirected fixtures contain no ANSI or cursor control and retain all status words.
    - Output alternates Scout complete then Route-Setter complete and includes the specification scroll identity.
    - Width, NO_COLOR, redirected, and JSON-parity tests assert every internal-enum-to-public-label mapping and the full `Specification` label.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningVisuals.*Width|TestPlanningVisuals.*NoColor|TestCodexVisuals.*Planning|TestCodexVisuals.*Spec' -count=1</automated>
  </verify>
  <done>The visible loop meets the approved responsive terminal contract in color, no-color, TTY, redirected, and JSON modes.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestPlanningVisuals|TestCodexVisuals.*Planning|TestCodexVisuals.*Spec' -count=1 and scan rendered fixtures for ANSI/cursor leakage and line-width overflow.
</verification>

<success_criteria>
- Scout to Route-Setter progress and every compact card field are visible in the locked order.
- Terminal and JSON carry the same facts and authority states.
- All responsive widths and no-color/redirected modes remain legible without in-place rewriting.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-19-SUMMARY.md when done.
</output>
