---
phase: 200-iterative-planning
plan: 41
type: execute
wave: 25
depends_on: [200-39]
files_modified:
  - cmd/testing_main_test.go
  - cmd/context_test.go
  - cmd/build_flow_cmds_test.go
  - cmd/patrol_check_test.go
  - cmd/legacy_recovery_199_test.go
autonomous: true
gap_closure: true
requirements: [CEC-03, PLAN-06]
must_haves:
  truths:
    - "Every command test binds AETHER_ROOT, COLONY_DATA_DIR, and the package store to one real temporary repository and restores prior absent-versus-present environment state exactly."
    - "Hook, patrol, build-flow, and legacy-recovery fixtures pass alone and in adversarial sequence without weakening repository containment."
    - "A deleted temporary repository from one test can never become the next test's authority."
  artifacts:
    - path: "cmd/testing_main_test.go"
      provides: "Shared repository-scoped command-test binding and exact cleanup contract"
    - path: "cmd/context_test.go"
      provides: "Context and hook fixtures using one contained repository root"
    - path: "cmd/patrol_check_test.go"
      provides: "Patrol fixtures aligned with physical repository authority"
  key_links:
    - from: "cmd/context_test.go"
      to: "cmd/root.go"
      via: "AETHER_ROOT and COLONY_DATA_DIR identify the same temporary repository"
      pattern: "AETHER_ROOT|COLONY_DATA_DIR"
    - from: "cmd/build_flow_cmds_test.go"
      to: "cmd/testing_main_test.go"
      via: "shared test repository binder"
      pattern: "bind.*Test.*Repository"
---

<objective>
Make the command-test harness express the same repository authority that production now enforces.

Purpose: The failed final gate mixed genuine regressions with stale or escaped temporary roots. A trustworthy Phase 200 receipt first needs deterministic, contained tests.
Output: One reusable repository binding helper and migrated hook, patrol, build-flow, and legacy-recovery fixtures.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-27-SUMMARY.md
@.planning/phases/200-iterative-planning/200-39-SUMMARY.md
@cmd/root.go
@pkg/storage/storage.go
</context>

<scope_guard>
This is test-authority repair, not permission to relax `storage.OpenRepositoryRoot`, accept an external `COLONY_DATA_DIR`, or restore pre-Plan-27 path behavior. Preserve D-10/D-16 fail-closed owner authority and the modern physical containment boundary.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define one exact repository-test binding</name>
  <files>cmd/testing_main_test.go, cmd/context_test.go</files>
  <behavior>
    - An initially absent environment variable is absent after cleanup, not present with an empty value.
    - A temporary repository binds both root variables and the package store to a contained `.aether/data` path.
    - Cleanup clears store/tracer/Cobra state before the temporary directory disappears.
    - Sequential command executions never reuse the preceding deleted store or root.
  </behavior>
  <action>Write failing harness tests first, then add a shared test helper that uses `t.Setenv`/`t.Cleanup` with exact `LookupEnv` semantics, creates a real repository-local data root, installs the matching store, and restores every mutable bootstrap global. Migrate `newTestStoreCmd` to it. Do not add a production fallback for stale globals: tests must satisfy the production contract.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(RepositoryTestBinding200|HookPreToolUseBlocksProtectedPath|HookPreToolUseAllowsSanctionedScratchDirs)$' -count=1</automated>
  </verify>
  <done>The shared helper proves exact setup/teardown and representative hook commands pass under physical containment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate patrol and legacy recovery to contained fixtures</name>
  <files>cmd/patrol_check_test.go, cmd/legacy_recovery_199_test.go</files>
  <behavior>
    - Patrol sees its intended temporary state rather than the checkout or a stale prior store.
    - Legacy recovery keeps non-default data-path coverage, but that path remains physically inside its test repository.
    - Read-only legacy diagnostics retain byte-for-byte no-mutation assertions.
  </behavior>
  <action>Replace ad hoc environment manipulation with the shared binding. Move the legacy recovery data fixture from an external temp root to a non-default contained path so it still proves configured-store routing without violating the Plan 27 safety contract. Keep every legacy diagnostic assertion and no-write fingerprint.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(PatrolCheck(AllHealthy|InvalidJSON|MissingFile|EmptyFile|StalePheromones|ZeroStrength|NoStaleSignals|InterruptedBuild|NoInterrupt)|LegacyRecoveryCommands199MaintenanceDiagnosis)$' -count=1</automated>
  </verify>
  <done>All patrol cases and the legacy maintenance diagnosis pass alone with no containment refusal or hidden mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the shared build-flow fixture restore both authorities</name>
  <files>cmd/build_flow_cmds_test.go, cmd/testing_main_test.go</files>
  <behavior>
    - `setupBuildFlowTest` cannot inherit a configured data root from the caller or a prior test.
    - Running a temp-store test before hook, patrol, and build-flow cases produces the same results as running each alone.
  </behavior>
  <action>Migrate `setupBuildFlowTest` to the common repository binding without silently adding accepted specification/plan authority; later plans migrate those fixtures explicitly per D-10 and D-16. Add a deterministic sequence regression that runs the formerly contaminating setup before representative command paths and proves the store/root pair is fresh each time.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(RepositoryTestBinding200|RepositoryTestBindingSequence200|PatrolCheckAllHealthy|HookPreToolUseBlocksProtectedPath)$' -count=1</automated>
  </verify>
  <done>Build-flow setup is isolated, and the sequence proof distinguishes remaining real build failures from cross-test root contamination.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test environment to repository bootstrap | Mutable process globals can select the wrong repository. |
| legacy fixture to modern containment | Compatibility evidence must not grant external-write authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-41-01 | Tampering | test root selection | mitigate | Bind root, data path, and store together and restore exact prior state. |
| T-200-41-02 | Elevation of privilege | external data fixture | mitigate | Keep every writable fixture physically within its repository. |
| T-200-41-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run the three focused commands above, then run `go test ./cmd -run '^(TestHook|TestPatrolCheck|TestLegacyRecoveryCommands199MaintenanceDiagnosis|TestRepositoryTestBinding)' -count=1` twice to prove repeatability.
</verification>

<success_criteria>
- Root/store tests pass alone and in sequence.
- No production containment branch is weakened.
- No cleanup leaves an empty replacement for an originally absent environment variable.
</success_criteria>

<output>
Create `.planning/phases/200-iterative-planning/200-41-SUMMARY.md` when done.
</output>
