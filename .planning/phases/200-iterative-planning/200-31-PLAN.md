---
phase: 200-iterative-planning
plan: 31
type: execute
wave: 18
depends_on: [200-28, 200-29, 200-30]
files_modified:
  - cmd/plan_revision.go
  - cmd/plan_impact.go
  - cmd/planning_state.go
  - cmd/planning_state_test.go
  - cmd/plan_authority.go
  - cmd/plan_candidate_acceptance_200_test.go
autonomous: true
gap_closure: true
requirements: [PLAN-04, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Acceptance independently derives the exact proposal and semantic/authority delta from the session-loaded base plan, current approved specification, and candidate proposal."
    - "Impact coverage and later build/run eligibility use independently derived closure, never persisted candidate claims as their proof."
    - "The acceptance repository session begins before candidate/spec/base/timeline reads and atomically commits accepted candidate, receipt, active revision, stage, and state."
    - "Any candidate/spec/delta/impact mismatch or concurrent authority change refuses with no partial mutation."
    - "[spec-less edge: PLAN-04/idempotency] Replaying the exact accepted candidate ID and token returns the byte-identical receipt even after other clock-independent reads, without a second plan revision."
    - "[spec-less edge: PLAN-04/concurrency-effect-ordering] Two acceptance processes serialize before authoritative reload; each either observes the accepted replay or re-derives against the newly current proposal, and neither overwrites the other."
  artifacts:
    - path: "cmd/plan_revision.go"
      provides: "Session-bound independently derived candidate acceptance"
      contains: "acceptPlanCandidate"
    - path: "cmd/plan_impact.go"
      provides: "Derived specification/plan impact closure"
      contains: "validatePlanCandidateImpactCoverage"
    - path: "cmd/plan_authority.go"
      provides: "Accepted lineage validation for build and run"
      contains: "validateAcceptedPlanAuthority"
    - path: "cmd/planning_state_test.go"
      provides: "Existing modern planning-state fixture rebuilt from canonical production identities"
      contains: "TestPlanningState"
    - path: "cmd/plan_candidate_acceptance_200_test.go"
      provides: "Forgery, replay, rollback, and process-race proof"
      contains: "TestPlanCandidateAcceptanceIntegrity200"
  key_links:
    - from: "cmd/plan_revision.go"
      to: "cmd/plan_impact.go"
      via: "acceptance derives closure from base/current-spec/proposal and compares canonical candidate delta"
      pattern: "validatePlanCandidateImpactCoverage"
    - from: "cmd/plan_authority.go"
      to: "cmd/planning_state.go"
      via: "build/run reload and validate candidate, spec, receipt, revision, and derived impact"
      pattern: "validatePlanningRecordHashes"
  prohibitions:
    - statement: "Candidate acceptance must not treat persisted SemanticDelta or AuthorityImpacts as proof of what changed or which immutable authority is affected."
      status: unresolved
      reason: "The prohibition closes only when acceptance and build/run independently derive the same exact closure and reject forged persisted claims without mutation."
---

<objective>
Make candidate acceptance and living-plan reconciliation independent of candidate-authored impact claims.

Purpose: Exact hashing is insufficient if the later authority decision merely trusts the hashed assertion instead of deriving reality.
Output: Session-bound acceptance, independent semantic/impact derivation, and process-level replay/race proof.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-28-SUMMARY.md
@.planning/phases/200-iterative-planning/200-29-SUMMARY.md
@.planning/phases/200-iterative-planning/200-30-SUMMARY.md
@cmd/plan_revision.go
@cmd/plan_impact.go
@cmd/planning_state.go
@cmd/planning_state_test.go
@cmd/plan_authority.go
</context>

<interfaces>
Expose one pure derivation from session-loaded base plan revision, canonical current approved specification, and canonical proposal to the expected PlanningSemanticDelta and planImpactClosure. Acceptance compares full canonical values and build/run repeat the closure check against the accepted lineage.
</interfaces>

<scope_guard>
Modify only the six declared files. Candidate temporal standing is added in Plan 37; do not introduce new public commands here. Preserve user dirt and use scoped task commits.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define forged-coverage, replay, rollback, and acceptance races</name>
  <files>cmd/plan_candidate_acceptance_200_test.go</files>
  <behavior>
    - "A candidate with self-consistent re-addressed but false semantic delta or authority impacts is rejected after independent derivation."
    - "A candidate derived against an old base or specification loses a race to the newer authority and cannot activate."
    - "Exact accepted replay returns identical candidate/revision/receipt without writes."
    - "Faults at every multi-file apply point and every mismatch leave state, candidate, stage, receipt, and plan artifacts unchanged."
  </behavior>
  <action>Create temporary real-repository fixtures and separate helper processes with deterministic barriers. Include forged added/changed/removed phase, task, dependency, acceptance-check, recovery, and authority-impact claims; missing affected scope; overbroad invalidation; changed base/spec between start and lock; simultaneous exact acceptance; and distinct competing candidates. Assert the two tagged replay/concurrency truths and D-12 affected-only preservation. Compare complete before/after path inventories on refusal.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestPlanCandidateAcceptanceIntegrity200|TestPlanCandidateAcceptanceConcurrentProcesses200)$' -count=1</automated>
  </verify>
  <done>The fail-first tests prove stored impact claims cannot grant authority and concurrent acceptance cannot lose updates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-derive exact delta and impact at acceptance</name>
  <files>cmd/plan_revision.go, cmd/plan_impact.go, cmd/planning_state.go, cmd/planning_state_test.go, cmd/plan_candidate_acceptance_200_test.go</files>
  <action>Enter Plan 28's repository session before loading the candidate artifact, state, active base revision, approved spec, timeline, or existing receipt. Canonically validate Plans 29 and 30, derive the expected semantic delta and affected/preserved proof closure from the exact base/current-spec/proposal tuple, and compare complete canonical structures with the candidate. Before strict load validation lands, rebuild planningStateTestFixture and its specification, candidate, delta, recommendation, acceptance, timeline, and active-revision identities with the production canonical builders/preimages introduced in Plans 29-30; retain corruption cases by cloning that valid fixture and mutating one field, never by accepting planningStateTestDigest placeholders as authority. Build the accepted revision and receipt only from derived values. Declare and commit candidate, receipt, stage, active state/revision, and any projection targets as one session-aware lifecycle transaction. Exact accepted replay must be read-only and byte-identical; conflicting or stale derivation must refuse without writes.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestPlanningState.*|TestCandidateAcceptanceBase.*|TestPlanCandidateAcceptanceIntegrity200|TestPlanCandidateAcceptanceConcurrentProcesses200|TestPlanCandidateAccept.*)' -count=1</automated>
  </verify>
  <done>Acceptance binds independently derived plan/spec meaning and commits every authority target atomically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Revalidate accepted impact before build and run</name>
  <files>cmd/plan_authority.go, cmd/plan_impact.go, cmd/planning_state.go, cmd/plan_candidate_acceptance_200_test.go</files>
  <action>When loading accepted plan authority, recompute the specification, candidate payload, acceptance receipt, active revision, timeline, and exact derived impact closure. Require the accepted receipt and active revision to bind the same canonical values and prove all affected requirements/tasks/checks/recovery links are reconciled. Build and run must share this evaluator and refuse forged or incomplete authority before any start effect. Retain explicit legacy-plan compatibility only for genuinely legacy-unbound state; never downgrade malformed modern authority to legacy.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestPlanningState.*|TestCandidateAcceptanceBase.*|TestPlanCandidateAcceptanceIntegrity200|Test.*PlanAuthority.*|Test.*Build.*Authority.*|Test.*Run.*Authority.*|TestSpecProjection.*|TestLifecycleProjection.*)' -count=1 &amp;&amp; go test ./... -count=1</automated>
  </verify>
  <done>Acceptance, build, and run all derive the same exact impact truth independently of persisted candidate claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| candidate claims to acceptance | A correctly hashed claim can still be false. |
| unlocked repository to acceptance | Base/spec/candidate can change concurrently. |
| accepted lineage to execution | Build/run must not rely on acceptance-time trust alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-31-01 | Tampering | impact coverage | mitigate | Independent base/spec/proposal derivation. |
| T-200-31-02 | Elevation of privilege | acceptance | mitigate | Session acquired before authoritative reload and atomic commit. |
| T-200-31-03 | Repudiation | accepted replay | mitigate | Byte-identical receipt/revision replay with no second write. |
| T-200-31-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run acceptance races repeatedly, the existing planning-state/authority/specification-projection/lifecycle-projection suites, and the full repository test reachability gate. Inspect that no authority decision reads candidate.SemanticDelta as proof without independent comparison and no canonical validator relies on placeholder identities.
</verification>

<success_criteria>
- Acceptance derives semantic and impact truth independently.
- Multi-file activation is session-atomic and exact replay is read-only.
- Build/run reject any forged or incomplete accepted lineage.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-31-SUMMARY.md when done.
</output>
