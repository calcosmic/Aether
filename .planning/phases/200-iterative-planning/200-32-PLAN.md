---
phase: 200-iterative-planning
plan: 32
type: execute
wave: 20
depends_on: [200-31]
files_modified:
  - cmd/codex_plan.go
  - cmd/codex_plan_finalize.go
  - cmd/specification.go
  - cmd/plan_revision.go
  - cmd/state_extra.go
  - cmd/planning_writer_session_200_test.go
autonomous: true
gap_closure: true
requirements: [PLAN-01, PLAN-04, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Every planning/specification/revision writer or deleter acquires the shared repository session before authoritative reads and commits against the exact bytes used for derivation."
    - "runCodexPlanWithOptions covers ResearchDocs, both VerificationDepth writes, the existing-plan return, run-header/stage writes, intermediate iteration persistence, finalization, and refresh cleanup without an unlocked partial branch."
    - "Specification create/revise/approve, candidate finalization, timeline/stage linkage, and public insert-phase each commit all owned targets or none."
    - "An AST inventory names every authoritative planning writer/deleter and fails when a direct SaveJSON, UpdateJSONAtomically, os.Remove, or pre-session authoritative read reappears."
    - "[spec-less edge: PLAN-01/boundary-values] All five evidence scores accept exact 0 and 100 endpoints and reject -1 and 101 before stage, card, index, or candidate mutation."
    - "[spec-less edge: PLAN-01/precision-overflow] Confidence input accepts only whole JSON integers in range; fractions, exponent overflow, and implementation-sized integer overflow refuse without rounding or wrapping."
    - "[spec-less edge: PLAN-04/boundary-values] Revision insert positions 0 and len(plan) are valid, while negative and greater-than-len positions refuse before candidate or receipt mutation."
    - "[spec-less edge: PLAN-04/precision-overflow] Revision positions and full SHA-256 identities reject fractional, overflowing, truncated, or prefix-only representations."
  artifacts:
    - path: "cmd/codex_plan.go"
      provides: "Session-bound public planning state and cleanup paths"
      contains: "runCodexPlanWithOptions"
    - path: "cmd/codex_plan_finalize.go"
      provides: "Session-bound stage, iteration, candidate, and final plan publication"
      contains: "persistIntermediatePlanningIteration"
    - path: "cmd/specification.go"
      provides: "Session-bound specification lifecycle writers"
      contains: "commitSpecificationTargets"
    - path: "cmd/state_extra.go"
      provides: "Session-bound insert-phase writer"
      contains: "insert-phase"
    - path: "cmd/planning_writer_session_200_test.go"
      provides: "Branch-complete process races and AST writer ratchet"
      contains: "TestPlanningWriterCoverage200"
  key_links:
    - from: "cmd/codex_plan.go"
      to: "cmd/planning_repository_session.go"
      via: "one session encloses load, branch selection, derivation, cleanup, and write"
      pattern: "withPlanningMutationSession"
    - from: "cmd/specification.go"
      to: "cmd/lifecycle_transaction.go"
      via: "spec targets commit with session-captured baselines"
      pattern: "commitSpecificationTargets"
---

<objective>
Move every authoritative planning writer and deletion branch behind the shared read-through-commit session.

Purpose: Fixing one transaction is insufficient while adjacent early returns, metadata writes, or cleanup paths can still overwrite or delete concurrent work.
Output: Complete writer migration plus deterministic process races and an AST regression ratchet.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-28-SUMMARY.md
@.planning/phases/200-iterative-planning/200-31-SUMMARY.md
@cmd/codex_plan.go
@cmd/codex_plan_finalize.go
@cmd/specification.go
@cmd/plan_revision.go
@cmd/state_extra.go
</context>

<scope_guard>
Modify only the six declared files. Do not broaden the session migration to unrelated lifecycle commands. Preserve user dirt and stage only task-owned paths.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inventory every planning writer branch and reproduce stale updates</name>
  <files>cmd/planning_writer_session_200_test.go</files>
  <behavior>
    - "AST inspection resolves actual call expressions and requires a session-dominated path for every listed writer/deleter branch."
    - "ResearchDocs merge, both VerificationDepth writes, existing-plan return, refresh cleanup, staged and legacy finalizers, and intermediate iteration state are individually exercised."
    - "Specification draft/revise/approve and insert-phase/timeline/revision races preserve the winner or refuse the stale writer."
    - "Invalid score/position/hash boundaries refuse before any state or artifact mutation."
  </behavior>
  <action>Create an exact AST inventory over the five production files, resolving calls rather than comments or strings. Name each runCodexPlanWithOptions branch: all ResearchDocs writes, VerificationDepth on existing-plan return and new/refresh paths, persistPlanningScoutStage run header/stage, clearFallbackPlanningArtifacts deletions, immediate finalizer, staged Route finalizer, and persistIntermediatePlanningIteration. Also name create/revise/approve, candidate acceptance hooks, timeline/stage publication, and insert-phase. Add separate-process barrier races for each writer family and before/after artifact inventories. Add the four tagged score/insert/hash edge tests with concrete function names so the final audit can map each truth to executable proof.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestPlanningWriterCoverage200|TestPlanningWriterConcurrentProcesses200|TestPlanningNumericBoundaries200)$' -count=1</automated>
  </verify>
  <done>The fail-first suite names every required branch and demonstrates stale overwrite or partial cleanup where still present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enclose every plan-command write and refresh deletion</name>
  <files>cmd/codex_plan.go, cmd/codex_plan_finalize.go, cmd/planning_writer_session_200_test.go</files>
  <action>Restructure runCodexPlanWithOptions and its staged/finalizer helpers so the shared session is acquired before state, research, existing-plan, stage, iteration, candidate, or refresh decisions are read. Merge every opts.ResearchDocs value and persist it in the same expected-baseline transaction as the selected branch. Cover both VerificationDepth assignments, including the existing-plan early return. Make clearFallbackPlanningArtifacts return errors, declare each exact delete target, and execute cleanup only inside the session transaction. Route run-header, stage, intermediate iteration, candidate, final plan, session summary, and state changes through declared targets. Preserve D-01 through D-04 cards and D-13 through D-15 preset/stop behavior. No successful return may leave a subset of its planned writes or deletes.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestPlanningWriterCoverage200|TestPlanningWriterConcurrentProcesses200|TestPlanningNumericBoundaries200|TestPlan.*Refresh.*|TestPlanOnlyUnchanged)$' -count=1</automated>
  </verify>
  <done>Every public plan branch uses one authoritative session and atomic declared target set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enclose specification, revision, timeline, and insert writers</name>
  <files>cmd/specification.go, cmd/plan_revision.go, cmd/state_extra.go, cmd/codex_plan_finalize.go, cmd/planning_writer_session_200_test.go</files>
  <action>Move specification draft/revise/approve authoritative loads and projection derivation inside the repository session and pass its expected baselines into commitSpecificationTargets. Replace canonicalSpecificationRoot's standalone path check and direct creation with the verified physical root/session authority from Plans 27-28. Apply the same rule to public insert-phase and every revision/timeline/stage/candidate finalization entry not already migrated in Plans 28 and 31. Insert-phase must validate numeric position and canonical full identities before target declaration, preserve immutable predecessor/reason/evidence history per D-11/D-12, and commit state plus projections atomically. Remove bypasses found by the AST test; allow read-only functions only when they provably make no later mutation from that snapshot.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestPlanningWriterCoverage200|TestPlanningWriterConcurrentProcesses200|TestPlanningNumericBoundaries200|Test.*Specification.*Concurrent.*|Test.*InsertPhase.*)' -count=1</automated>
  </verify>
  <done>Every named specification, revision, timeline, stage, candidate, and insert writer has a session-held authoritative read and stale-writer refusal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| plan branch read to state write | Early-return metadata can overwrite concurrent state. |
| refresh decision to artifact deletion | Cleanup can remove another process's new run. |
| spec/revision input to multi-file projection | State and owner-readable files must agree atomically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-32-01 | Tampering | planning state | mitigate | Session before branch reads and exact target baselines. |
| T-200-32-02 | Denial of service | refresh cleanup | mitigate | Declared atomic deletes and stale conflict refusal. |
| T-200-32-03 | Repudiation | writer coverage | mitigate | AST call inventory plus process-race tests. |
| T-200-32-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run the writer AST suite, every process race, and existing plan/spec/insert tests. Inspect that no named branch bypass remains.
</verification>

<success_criteria>
- All named writer/deleter branches are session-dominated.
- Stale concurrent work is preserved or rejected, never silently overwritten.
- Boundary/overflow inputs fail before mutation.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-32-SUMMARY.md when done.
</output>
