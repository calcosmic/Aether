---
phase: 200-iterative-planning
plan: 12
type: execute
wave: 4
depends_on: [200-01, 200-08]
files_modified:
  - cmd/lifecycle_facts.go
  - cmd/lifecycle_facts_test.go
  - cmd/lifecycle_projection.go
  - cmd/lifecycle_projection_test.go
  - cmd/next_action.go
  - cmd/next_action_input.go
  - cmd/next_action_test.go
autonomous: true
requirements: [CEC-03, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Every lifecycle surface derives the same honest next step from one state snapshot."
    - "The visible journey is init to discuss to spec to plan to candidate acceptance to build or run."
  artifacts:
    - path: "cmd/lifecycle_facts.go"
      provides: "Single-read specification, candidate, and acceptance facts"
    - path: "cmd/next_action.go"
      provides: "Pure next-step decision policy"
  key_links:
    - from: "cmd/next_action.go"
      to: "cmd/lifecycle_facts.go"
      via: "pure facts input"
      pattern: "LifecycleFacts"
    - from: "cmd/lifecycle_projection.go"
      to: "cmd/next_action.go"
      via: "shared next-action result"
      pattern: "NextAction"
---

<objective>
Extend the shared lifecycle-facts and next-action model for specification, candidate, and accepted-plan authority.

Purpose: Prevent command help, status, and renderers from disagreeing about what the user should do next.
Output: One-read facts, pure next-step policy, and synchronized projections.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-UI-SPEC.md
@pkg/colony/colony.go
@cmd/lifecycle_facts.go
@cmd/lifecycle_projection.go
@cmd/next_action.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add specification and planning authority to lifecycle facts</name>
  <read_first>
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_facts_test.go
    - cmd/lifecycle_projection.go
    - cmd/lifecycle_projection_test.go
    - pkg/colony/specification.go
    - pkg/colony/planning.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-09 through D-16)
  </read_first>
  <files>cmd/lifecycle_facts.go, cmd/lifecycle_facts_test.go, cmd/lifecycle_projection.go, cmd/lifecycle_projection_test.go</files>
  <behavior>
    - One state read yields charter, discussion, spec revision/status, planning stage, pending candidate, active accepted revision, legacy classification, and affected-scope facts.
    - Facts distinguish approved spec from accepted plan and candidate-ready from build-ready.
    - Projection never performs another state read or mutation.
  </behavior>
  <action>Extend the existing lifecycle facts snapshot with explicit current spec revision/hash/status, unresolved discussion count, planning run/stage/preset/pass, pending candidate ID/status/stop reason, plan acceptance binding status, legacy-unbound status, and affected unresolved semantic IDs. Derive each field from the already loaded ColonyState and validated artifacts passed into the collector. Extend terminal and JSON projection structs without renaming existing stable fields; add new sibling fields. Keep collection free of repair or state mutation and add a read-count test proving one state snapshot feeds all projections.</action>
  <acceptance_criteria>
    - Draft, approved, candidate-ready, accepted, affected-revision, and legacy fixtures yield distinct facts.
    - ApprovedSpec and AcceptedPlan are separate booleans or closed statuses with independent bindings.
    - Projection tests prove terminal and JSON consume identical facts.
    - Fact collection performs one state load and no write.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestLifecycleFacts.*Spec|TestLifecycleFacts.*Plan|TestLifecycleProjection' -count=1</automated>
  </verify>
  <done>All lifecycle consumers can reason from one authoritative snapshot without conflating spec, candidate, or plan states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route every state to one honest next action</name>
  <read_first>
    - cmd/next_action.go
    - cmd/next_action_input.go
    - cmd/next_action_test.go
    - cmd/lifecycle_facts.go
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Next Up and Error Recovery)
  </read_first>
  <files>cmd/next_action.go, cmd/next_action_input.go, cmd/next_action_test.go</files>
  <behavior>
    - Accepted charter with unsettled intent routes to discuss; draft spec routes to spec; approved spec without a plan routes to plan.
    - An active planning stage routes to its exact resume command; a stopped candidate routes to candidate review and exact acceptance.
    - A newly accepted plan offers build and run with equal visual priority.
  </behavior>
  <action>Extend the pure next-action state table with precedence: blocked recovery, unsettled material intent, draft/revision spec review, approved-spec reconciliation, in-flight planning resume, owner decision, candidate review, candidate exact acceptance, affected active plan revision, and build/run readiness. Emit canonical aether command spellings and structured alternative actions. For accepted plan readiness return both aether build plus phase and aether run as equal primary choices. Never route a draft spec to plan, a pending candidate to build, or spec approval to build. Retain legacy-unbound build routing for migrated plans.</action>
  <acceptance_criteria>
    - A table-driven test covers every precedence branch and ambiguous combined-state fixture.
    - Candidate-ready output contains review and exact accept commands but no build/run.
    - Accepted output contains build and run as equal-primary actions.
    - Legacy-unbound active plans retain their existing build route.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestNextAction.*Spec|TestNextAction.*Candidate|TestNextAction.*Accepted|TestNextAction.*Legacy' -count=1</automated>
  </verify>
  <done>Every lifecycle state has one truthful recovery or progress route and no authority boundary is skipped.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestLifecycleFacts|TestLifecycleProjection|TestNextAction' -count=1.
</verification>

<success_criteria>
- One immutable facts snapshot drives terminal and JSON lifecycle guidance.
- Every specification, planning, candidate, accepted, revision, and legacy state maps to the correct next authority.
- All downstream lifecycle surfaces can consume the same authority-aware action result without another state read.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-12-SUMMARY.md when done.
</output>
