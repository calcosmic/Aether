---
phase: 200-iterative-planning
plan: 05
type: execute
wave: 2
depends_on: [200-01]
files_modified:
  - cmd/planning_delta.go
  - cmd/planning_delta_test.go
autonomous: true
requirements: [CEC-03, PLAN-03, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Each planning pass explains semantic changes to phases, tasks, dependencies, proof checks, and recovery paths."
    - "Authority changes are visible but cannot masquerade as plan-content improvements."
  artifacts:
    - path: "cmd/planning_delta.go"
      provides: "Stable semantic plan comparison and proposal completeness validation"
  key_links:
    - from: "cmd/planning_delta.go"
      to: "pkg/colony/planning.go"
      via: "PlanningSemanticDelta output"
      pattern: "PlanningSemanticDelta"
---

<objective>
Define and compute the semantic before-to-after plan delta used in every Route-Setter card.

Purpose: Replace opaque text-diff claims with stable-ID changes that cover executable plan content and keep authority impacts separate.
Output: Semantic plan snapshot, comparison, and completeness-validation functions.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md
@pkg/colony/colony.go
@pkg/colony/planning.go
@cmd/plan_revision.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compare executable plan semantics by stable identity</name>
  <read_first>
    - cmd/planning_delta.go (create if absent)
    - cmd/plan_revision.go
    - pkg/colony/colony.go
    - pkg/colony/planning.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-03, D-11, D-12)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Semantic Delta)
  </read_first>
  <files>cmd/planning_delta.go, cmd/planning_delta_test.go</files>
  <behavior>
    - Stable IDs classify phases and tasks as added, modified, removed, or unchanged despite ordering or prose-format changes.
    - Dependency edges, acceptance checks, negative tests, recovery instructions, requirement links, and public paths are compared as semantic sets.
    - Specification approval, candidate status, and plan acceptance appear only in authority impacts.
  </behavior>
  <action>Build a canonical semantic snapshot from Plan, PlanRevision, Phase, and Task using stable IDs and normalized set ordering. Compare before and after snapshots into explicit added, modified, removed, and preserved entries for phases, tasks, dependency edges, acceptance checks, negative tests, recovery instructions, requirement links, and public command paths. Ignore timestamps, whitespace, ordering-only movement, renderer text, and receipt transport fields. Compute authority impacts in a separate collection so an approval or acceptance transition cannot raise semantic confidence. Reject duplicate or missing stable IDs for current-schema proposals.</action>
  <acceptance_criteria>
    - Reordering phases or changing whitespace produces an empty semantic delta.
    - Changing one task recovery instruction produces exactly one modified task/property entry.
    - Removing a dependency or negative test is visible as a removal.
    - Approving an unchanged specification produces an authority impact and no semantic change.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningDelta.*Compare|TestPlanningDelta.*Authority' -count=1</automated>
  </verify>
  <done>Every pass can show an exact semantic delta that is stable across formatting and independent of authority transitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require complete executable plan proposals</name>
  <read_first>
    - cmd/planning_delta.go
    - cmd/planning_delta_test.go
    - pkg/colony/colony.go
    - .planning/REQUIREMENTS.md (PLAN-05)
    - .planning/phases/200-iterative-planning/200-PATTERNS.md (finalizer guardrails)
  </read_first>
  <files>cmd/planning_delta.go, cmd/planning_delta_test.go</files>
  <behavior>
    - Every current-schema phase and task has stable identity, requirement coverage, dependencies, positive checks, negative checks, recovery guidance, and truthful public paths where applicable.
    - An incomplete worker proposal is rejected before it can become an iteration card or candidate.
    - Removed scope requires an explicit removal classification rather than silent omission.
  </behavior>
  <action>Implement validatePlanProposalContract as a pure boundary validator. Require phase and task stable IDs; nonempty task objectives, exact files or declared no-file reason, dependency references that resolve without cycles, requirement IDs, automated acceptance checks, at least one relevant negative or refusal check, recovery behavior, and public command paths for user-facing workflows. Require an explicit removal entry for predecessor semantic IDs absent from the proposal. Return field-addressable errors and never patch missing content with defaults. Legacy active plans are excluded from this proposal validator; new candidates are not.</action>
  <acceptance_criteria>
    - Fixtures independently missing requirements, negative checks, recovery, dependency targets, or removal declarations fail with the exact field path.
    - A complete multi-phase proposal validates and produces a stable snapshot hash.
    - A dependency cycle is rejected before candidate persistence.
    - Legacy plan loading is unaffected because validation applies only at the current proposal boundary.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningDelta.*Proposal|TestPlanningDelta.*Removal|TestPlanningDelta.*Cycle' -count=1</automated>
  </verify>
  <done>Only complete, executable, semantically traceable Route-Setter proposals can enter the iteration timeline.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run TestPlanningDelta -count=1 and inspect fixtures for every PLAN-05 field plus formatting-only and authority-only changes.
</verification>

<success_criteria>
- Semantic deltas cover all executable planning content named by PLAN-05.
- Formatting and authority transitions do not count as semantic plan improvement.
- Silent removals and structurally incomplete proposals fail before persistence.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-05-SUMMARY.md when done.
</output>
