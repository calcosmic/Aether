---
phase: 200-iterative-planning
plan: 29
type: execute
wave: 17
depends_on: [200-28]
files_modified:
  - pkg/colony/specification.go
  - pkg/colony/specification_test.go
  - cmd/specification.go
  - cmd/planning_state.go
  - cmd/plan_authority.go
  - cmd/specification_integrity_200_test.go
autonomous: true
gap_closure: true
requirements: [PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Every typed specification item, section projection, revision body, revision ID, and approval receipt is recomputed from canonical content whenever specification authority is validated."
    - "A copied stored hash cannot preserve approval, planning eligibility, candidate authority, build eligibility, or run eligibility after any typed body field changes."
    - "Draft and approved revisions retain D-09 through D-12 lifecycle semantics while the approval token remains a distinct owner action from D-16 plan acceptance."
    - "Every tamper refusal is read-only: state, specification projection, stage, candidate, active plan, build journal, and repository artifact set remain unchanged."
  artifacts:
    - path: "pkg/colony/specification.go"
      provides: "Typed revision and receipt structural invariants"
      contains: "SpecRevision"
    - path: "pkg/colony/specification_test.go"
      provides: "Existing typed specification fixtures rebuilt with production canonical identities"
      contains: "TestSpec"
    - path: "cmd/specification.go"
      provides: "Canonical item, revision, projection, and approval recomputation"
      contains: "specificationRevisionContentHash"
    - path: "cmd/planning_state.go"
      provides: "Load-boundary specification integrity validation"
      contains: "validateSpecificationState"
    - path: "cmd/plan_authority.go"
      provides: "Planning/build/run authority consumes recomputed specification truth"
      contains: "approvedPlanningSpecification"
    - path: "cmd/specification_integrity_200_test.go"
      provides: "Per-section and per-boundary tamper/refusal matrix"
      contains: "TestSpecificationIntegrity200"
  key_links:
    - from: "cmd/planning_state.go"
      to: "cmd/specification.go"
      via: "one canonical recomputation function validates loaded lineage"
      pattern: "validateSpecification"
    - from: "cmd/plan_authority.go"
      to: "cmd/planning_state.go"
      via: "planning and execution authority reject invalid loaded specification state"
      pattern: "validateSpecificationState"
  prohibitions:
    - statement: "Planning UI, receipts, build, and run must not label a plan approved, accepted, or buildable solely because copied persisted digest fields agree; body-derived canonical validation plus the exact owner receipt is required."
      status: unresolved
      reason: "The prohibition closes only when every typed section and authority boundary recomputes the stored identities and tampering causes zero mutation."
---

<objective>
Make approved specification authority derive from its typed body rather than trusted stored hash strings.

Purpose: A valid-looking copied digest must never bless edited owner intent.
Output: Canonical specification recomputation at load, approval, planning, acceptance, build, and run boundaries.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-VERIFICATION.md
@pkg/colony/specification.go
@pkg/colony/specification_test.go
@cmd/specification.go
@cmd/planning_state.go
@cmd/plan_authority.go
</context>

<interfaces>
Use one canonical specification validator that recomputes each typed item identity, the complete revision content hash and derived revision ID, the owner-readable projection content, and the approval token/receipt identity. Structural Validate methods remain useful but cannot substitute for canonical recomputation.
</interfaces>

<scope_guard>
Modify only the six declared files. Plan 32 will move specification writers into the shared session; this plan owns body authority and refusal behavior. Preserve user dirt and use scoped task commits.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the complete specification tamper matrix</name>
  <files>cmd/specification_integrity_200_test.go</files>
  <behavior>
    - "Changing one field in each of outcomes, included behaviors, exclusions, binding decisions, requirements, acceptance checks, negative expectations, recovery expectations, and affected public paths fails even when all stored hashes are copied."
    - "Changing scope, delta, predecessor, timestamps, approval actor/time/token, revision ID/hash, or projected SPEC.md content fails at its applicable authority boundary."
    - "Draft/revise/approve happy paths still produce canonical identities and separate owner approvals."
    - "Planning, candidate review/acceptance, build, and run refuse tampered approval with identical before/after snapshots."
  </behavior>
  <action>Create table-driven fixtures from a valid approved specification and mutate each typed field independently while retaining the old stored identifiers. Exercise direct canonical validation and the real planning, plan-authority, build-authority, and run-authority entry points. Add projection mismatch and receipt-forgery cases. Snapshot all relevant repository paths and state around every refusal. Include a clean draft, scoped successor revision, explicit approval, and legacy spec-less compatibility control per D-09, D-10, D-11, and D-12.</action>
  <verify>
    <automated>go test ./cmd -run '^TestSpecificationIntegrity200' -count=1</automated>
  </verify>
  <done>The fail-first test names every typed section and every downstream authority boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recompute typed revision and approval identities canonically</name>
  <files>pkg/colony/specification.go, pkg/colony/specification_test.go, cmd/specification.go, cmd/specification_integrity_200_test.go</files>
  <action>Centralize canonical normalization and hashing for all nine typed body sections, scope, predecessor, delta, and immutable revision metadata. Recompute every item hash/derived ID, the revision ContentHash and ID, the SPEC.md projection hash/binding, the approval token hash, approval receipt ID, and all cross-references; compare full values in constant shape rather than accepting syntax or suffixes. Ensure builders use the same canonical material as validators. In pkg/colony/specification_test.go, replace placeholder strings such as spec-rev-*-content-hash, hash-*, and approval-token-hash with fixtures assembled by those production canonical builders/preimages; negative tests must construct a fully valid object first and then mutate only the field under test. Keep status and approval transition fields outside immutable body material only where the current lifecycle requires, and validate them through the separate receipt relationship. Do not add a second test-only hash algorithm or accept prefix matches.</action>
  <verify>
    <automated>go test ./pkg/colony -run '^TestSpec' -count=1 &amp;&amp; go test ./cmd -run '^(TestSpecificationIntegrity200|Test.*Spec.*)' -count=1</automated>
  </verify>
  <done>Canonical construction and validation agree for every typed section, revision, projection, and approval receipt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Require recomputation at every authority boundary</name>
  <files>cmd/planning_state.go, cmd/plan_authority.go, cmd/specification.go, cmd/specification_integrity_200_test.go</files>
  <action>Call the canonical validator when loading specification state, resolving the current approved revision, approving or revising, starting Scout/Route planning, validating accepted plan lineage, and deciding build/run eligibility. Reject a mismatch before producing a manifest, acceptance result, build attempt, or state write. Retain explicit legacy-unbound classification rather than fabricating a modern approval. Make all errors identify the invalid specification/revision field without echoing owner content. Verify D-16 remains a separate candidate-acceptance authority.</action>
  <verify>
    <automated>go test ./pkg/colony -run '^TestSpec' -count=1 &amp;&amp; go test ./cmd -run '^(TestSpecificationIntegrity200|TestSpecification.*|TestSpecProjection.*|TestSpecCommand.*|Test.*PlanAuthority.*|Test.*Specification.*Authority.*)' -count=1</automated>
  </verify>
  <done>No planning or execution boundary trusts stored specification hashes without recomputing the typed body and receipt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| persisted specification to validator | Stored IDs and hashes are attacker-controlled input. |
| approved revision to planning | Planning must receive exact owner-approved intent. |
| accepted plan to build/run | Execution must revalidate its specification lineage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-29-01 | Tampering | SpecRevision body | mitigate | Full canonical typed-body recomputation. |
| T-200-29-02 | Spoofing | SpecApprovalReceipt | mitigate | Token, receipt ID, actor/time, and revision binding recomputation. |
| T-200-29-03 | Elevation of privilege | plan/build authority | mitigate | Fail closed before manifest or mutation. |
| T-200-29-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run the complete specification matrix and existing pkg/colony specification plus command specification, authority, and projection suites. Confirm copied hashes never turn altered content into authority and no strict validator is fed a placeholder identity.
</verification>

<success_criteria>
- Every typed specification and receipt identity is recomputed.
- All planning/build/run boundaries consume canonical validation.
- Tampering refuses with zero mutation while valid and legacy-compatible paths still work.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-29-SUMMARY.md when done.
</output>
