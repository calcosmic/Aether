---
phase: 200-iterative-planning
plan: 26
type: execute
wave: 14
depends_on: [200-23]
files_modified:
  - cmd/testdata/current-vocabulary-199.json
  - cmd/phase199_gate_receipt_test.go
  - .planning/phases/199-front-door-and-classic-contract/199-GATE-RECEIPT.json
autonomous: true
gap_closure: true
requirements: [SYNTH-02, CEC-03, PLAN-06]
must_haves:
  truths:
    - "The Phase 199 vocabulary inventory records 199-UAT.md legacy_pause exactly once and legacy_resume exactly once without changing the historical UAT."
    - "The checked-in Phase 199 receipt remains valid after ordinary later GSD STATE bookkeeping, mutation of only .gsd/dispatch-isolation-sentinel.json, and the exact normal auto-chain reset."
    - "Historical gate evidence remains bound to valid RFC3339 chronology, bounded execution duration, exact command order, zero exits, output digests, and a real recorded Git revision/tree."
    - "Tampering with historical evidence, 199-PATTERNS.md, any protected config key, or any live .gsd path other than the exact dispatch-isolation sentinel still fails deterministically."
  artifacts:
    - path: "cmd/testdata/current-vocabulary-199.json"
      provides: "Exact inventory rows for the two Phase 199 UAT occurrences"
      contains: "199-UAT.md"
    - path: "cmd/phase199_gate_receipt_test.go"
      provides: "Durable historical-evidence and live-user-protection validator"
      contains: "TestPhase199GateReceipt"
    - path: ".planning/phases/199-front-door-and-classic-contract/199-GATE-RECEIPT.json"
      provides: "Reclassified Phase 199 historical and live protection evidence"
      contains: "phase199-gate-receipt"
  key_links:
    - from: "cmd/phase199_gate_receipt_test.go"
      to: ".planning/phases/199-front-door-and-classic-contract/199-GATE-RECEIPT.json"
      via: "strict JSON decoding and independent Git-object validation"
      pattern: "validatePhase199GateReceipt"
    - from: "cmd/testdata/current-vocabulary-199.json"
      to: ".planning/phases/199-front-door-and-classic-contract/199-UAT.md"
      via: "two exact path/token/count inventory rows"
      pattern: "199-UAT.md"
---

<objective>
Repair the inherited Phase 199 bookkeeping that prevents Phase 200 from proving a genuinely green repository.

Purpose: Preserve historical evidence as history while protecting current user-owned files from mutation.
Output: Two exact vocabulary rows plus a durable Phase 199 receipt contract and regression proof.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/199-front-door-and-classic-contract/199-GATE-RECEIPT.json
@.planning/phases/199-front-door-and-classic-contract/199-UAT.md
@.planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md
@cmd/current_vocabulary_199_test.go
@cmd/testdata/current-vocabulary-199.json
@cmd/phase199_gate_receipt_test.go
</context>

<scope_guard>
Modify only the three frontmatter-owned files. Do not edit 199-UAT.md, 199-PATTERNS.md, STATE.md, config.json, or .gsd. Preserve the starting worktree and stage only task-owned paths in scoped commits.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Account for the two historical vocabulary occurrences exactly</name>
  <files>cmd/testdata/current-vocabulary-199.json</files>
  <behavior>
    - "The historical evidence bucket contains 199-UAT.md legacy_pause with expected count 1."
    - "The same bucket contains 199-UAT.md legacy_resume with expected count 1."
    - "No duplicate row, wildcard, or source-file edit is accepted."
  </behavior>
  <action>Add exactly two rows to historical_evidence_versioned_fixture_test_input: .planning/phases/199-front-door-and-classic-contract/199-UAT.md with legacy_pause count 1 and that exact path with legacy_resume count 1. Keep the JSON canonical and leave every existing row untouched. This is inventory repair only; the UAT and untracked PATTERNS evidence remain immutable per the user reset and D-02's historical-record requirement.</action>
  <verify>
    <automated>go test ./cmd -run '^TestCurrentVocabulary199($|/)' -count=1</automated>
  </verify>
  <done>The parent test and its exact-occurrence subtest pass with two and only two new inventory rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Separate historical lifecycle evidence from live user protection</name>
  <files>cmd/phase199_gate_receipt_test.go, .planning/phases/199-front-door-and-classic-contract/199-GATE-RECEIPT.json</files>
  <behavior>
    - "A historical receipt can be old, but each timestamp parses as RFC3339, gates are ordered, finish is not before start, future skew is bounded, and each execution duration is bounded."
    - "The exact commands are go test ./... followed by go test ./... -race; both have exit 0, SHA-256 output, and the recorded revision resolves to the recorded tree."
    - "Later ordinary STATE changes, mutation of exactly .gsd/dispatch-isolation-sentinel.json, and config _auto_chain_active returning to false do not invalidate historical proof."
    - "Every other pre-existing .gsd entry is recursively live-protected: byte mutation, addition, removal, file/directory type change, or symlink replacement fails, including beneath .gsd/scratch and .gsd/worktrees."
    - "A changed historical Git object, command, order, exit, output hash, chronology, duration, 199-PATTERNS.md byte, protected config value, scratch entry, or worktree entry fails."
  </behavior>
  <action>Replace the rolling now-minus-started under 24 hours rule with durable chronology validation: parse CreatedAt and all gate times as RFC3339Nano, allow at most one minute future skew, require CreatedAt no later than the first start, require each finish at or after its start and each later gate to start after the prior finish, and cap each individual execution at two hours. Require the exact command order go test ./... then go test ./... -race, zero exits, full output SHA-256, and matching full revision/tree IDs. Independently require the recorded revision to be a real commit, resolve its tree, and validate every declared tracked historical blob from that Git object. Reclassify .planning/STATE.md as historical lifecycle evidence that is never compared with live lifecycle bytes. For .gsd, permit historical/lifecycle mutation only at the exact path .gsd/dispatch-isolation-sentinel.json; do not exempt its parent, a prefix, or any other GSD-looking name. Replace the monolithic .gsd exception with a closed receipt baseline that recursively inventories every other pre-existing live .gsd entry by relative path, lstat type, symlink target where applicable, and byte digest for regular files. Each validation recollects the complete non-sentinel tree and compares path membership plus those attributes, rejecting byte mutation, addition, removal, file/directory type change, or symlink replacement. Replace validatePhase199ReceiptEvidenceOnlyChanges: later implementation or planning-document changes are not evidence tampering merely because they differ from the old tested revision. Keep .planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md and every .planning/config.json key user-protected, except permit only the exact normal _auto_chain_active reset to false. Preserve strict unknown-field rejection, exact path sets, and all negative tamper cases. Add temporary-repository regressions proving ordinary later STATE bookkeeping plus exact sentinel/config effects pass while unchanged pre-existing .gsd/scratch and .gsd/worktrees entries remain protected; separately mutate, add, remove, change type, and replace with symlinks under both scratch and worktrees and require deterministic failure. Update the checked-in receipt only to the new canonical schema/data required by those tests; do not regenerate its historical gate claims.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestPhase199GateReceiptSchema|TestPhase199GateReceipt|TestPhase199GateReceiptSurvivesLaterLifecycleBookkeeping)$' -count=1</automated>
  </verify>
  <done>The Phase 199 receipt survives only the exact allowed lifecycle changes; historical tampering and every non-sentinel live .gsd or other user-owned change remain rejected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| receipt JSON to Git history | Recorded claims must resolve to immutable repository objects. |
| historical lifecycle to live files | Later STATE bookkeeping and the one exact dispatch-isolation sentinel may change; all other live .gsd paths remain user-protected. |
| user-owned files to test harness | PATTERNS and protected config values must not be normalized away. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-26-01 | Tampering | historical gate record | mitigate | Exact command/order/digest checks and Git revision-to-tree/blob resolution. |
| T-200-26-02 | Repudiation | gate chronology | mitigate | RFC3339, ordering, future-skew, and bounded-duration validation. |
| T-200-26-03 | Tampering | live user files | mitigate | Recursive lstat/type/target/digest protection with only the exact dispatch-isolation sentinel excluded. |
| T-200-26-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run the two focused Phase 199 test families. Confirm git status shows no change to UAT, PATTERNS, STATE, config, or .gsd.
</verification>

<success_criteria>
- Both exact vocabulary rows pass.
- The checked-in historical receipt validates without a wall-clock freshness deadline.
- Later STATE bookkeeping, the exact dispatch-isolation sentinel, and the exact auto-chain reset pass; historical and every other live .gsd/user-owned tamper fail.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-26-SUMMARY.md when done.
</output>
