---
phase: 200-iterative-planning
plan: 50
type: execute
wave: 30
depends_on: [200-45, 200-46, 200-48, 200-49]
files_modified:
  - cmd/codex_continue_test.go
  - cmd/compatibility_cmds_test.go
  - cmd/phase_recovery_test.go
  - cmd/forced_reviewer_waiver_test.go
  - cmd/narrator_launcher_test.go
autonomous: true
gap_closure: true
requirements: [CEC-03, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Continue, run compatibility, force redispatch, reviewer-waiver, and narrator tests enter through canonical accepted-plan/build-start state."
    - "Tests intended to fail closed still obstruct a target that has not already been legitimately created by build start."
    - "External finalize and compatibility output remain valid JSON with truthful nonzero errors."
  artifacts:
    - path: "cmd/codex_continue_test.go"
      provides: "Canonical continue/finalize fixtures"
    - path: "cmd/compatibility_cmds_test.go"
      provides: "Run compatibility fixtures using modern authority"
    - path: "cmd/forced_reviewer_waiver_test.go"
      provides: "Current fail-closed reviewer waiver fixtures"
  key_links:
    - from: "cmd/codex_continue_test.go"
      to: "cmd/build_start_test_helpers_200_test.go"
      via: "durable canonical attempt before finalization"
      pattern: "commitTestBuildStart|createApproved"
---

<objective>
Migrate downstream lifecycle fixtures that depend on the canonical build boundary.

Purpose: After the shared production fixes, these tests must exercise their actual continue/run/recovery subjects rather than fail earlier on stale setup.
Output: Five bounded fixture files aligned to accepted authority and current error semantics.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-45-SUMMARY.md
@.planning/phases/200-iterative-planning/200-48-SUMMARY.md
@.planning/phases/200-iterative-planning/200-49-SUMMARY.md
@cmd/build_start_test_helpers_200_test.go
</context>

<scope_guard>
Fixture migration only unless an owned test exposes a genuine regression in its named behavior. Do not implement Phase 201 lifecycle redesign, change run semantics, or waive D-16 acceptance.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate continue and external-finalize evidence</name>
  <files>cmd/codex_continue_test.go</files>
  <behavior>
    - External review finalization records spawn/result evidence and advances once.
    - Abandoned recovery and worker-outcome report cases start from a canonical attempt and preserve their recovery assertions.
    - Fresh optional ledgers are created; corrupt evidence remains a refusal.
  </behavior>
  <action>Replace hand-written attempt/manifests with the Plan 49 helper and seed accepted plan/spec authority explicitly. Keep external completion packets, terminal states, reports, and advance assertions unchanged. Use the Plan 45 absence contract rather than pre-creating empty ledgers.</action>
  <verify>
    <automated>go test ./cmd -run '^TestContinue(FinalizeRecordsExternalReviewAndAdvances|EndToEndAfterAbandonedRecovery|FinalizeWritesWorkerOutcomeReports)$' -count=1</automated>
  </verify>
  <done>All three continue paths reach their named assertions through canonical evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate run compatibility and force redispatch</name>
  <files>cmd/compatibility_cmds_test.go, cmd/phase_recovery_test.go</files>
  <behavior>
    - Run executes one accepted phase and forwards worker timeout without manufacturing authority.
    - `build --force` redispatches a genuinely active accepted phase and emits parseable output.
    - Unaccepted or mismatched states remain refused.
  </behavior>
  <action>Seed exact approved/accepted state and one canonical attempt for success cases. Retain negative cases without that state and update them only to expect the current nonzero Cobra error plus error envelope. Do not treat rendered JSON as a zero exit when the command failed.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(RunCompatibilityExecutesSinglePhase|RunCompatibilityPassesWorkerTimeoutToBuildAndContinue|BuildForceRedispatchesActiveExecutingPhase)$' -count=1</automated>
  </verify>
  <done>Compatibility and redispatch tests prove current authority and output semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Repair reviewer-waiver fault injection and narrator build setup</name>
  <files>cmd/forced_reviewer_waiver_test.go, cmd/narrator_launcher_test.go</files>
  <behavior>
    - Waiver persistence failure is injected after canonical start at the actual waiver target, not by colliding with the already-created dispatch marker.
    - A one-worker forced reviewer still pauses under the existing policy.
    - Narrator JSON/visual tests reach narration only after accepted build authority and never pollute JSON.
  </behavior>
  <action>Migrate authority setup and relocate fault injection to the operation each test claims to cover. Preserve all no-mutation fingerprints, pause behavior, and output-mode assertions. Do not delete a legitimate start artifact just to recreate the old setup.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(SpawnLogFailsClosedWhenWaiverWindowCannotPersist|OneWorkerWithForcedReviewerWaiverStillPauses|BuildSyntheticNarratorDoesNotPolluteJSONOutput)$' -count=1</automated>
  </verify>
  <done>The reviewer and narrator cases test their named behavior rather than colliding with canonical build start.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test completion packet to lifecycle finalizer | Stale fixtures can accidentally bypass or obscure receipt checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-50-01 | Spoofing | downstream build fixture | mitigate | Exact accepted phase and canonical start receipt. |
| T-200-50-02 | Repudiation | rendered error output | mitigate | Assert both envelope and nonzero error semantics. |
| T-200-50-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run all named focused tests in one command twice; no case may rely on execution order.
</verification>

<success_criteria>
- Downstream lifecycle fixtures pass alone and together.
- Fail-closed cases still fail at their intended boundary.
- No Phase 201 behavior is introduced.
</success_criteria>

<output>Create `.planning/phases/200-iterative-planning/200-50-SUMMARY.md` when done.</output>
