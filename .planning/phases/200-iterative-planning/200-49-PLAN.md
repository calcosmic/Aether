---
phase: 200-iterative-planning
plan: 49
type: execute
wave: 29
depends_on: [200-47, 200-48]
files_modified:
  - cmd/build_start_test_helpers_200_test.go
  - cmd/codex_build_test.go
  - cmd/ceremony_team_checkin_test.go
  - cmd/orchestrator_boundary_guidance_test.go
  - cmd/build_attempt_test.go
autonomous: true
gap_closure: true
requirements: [CEC-03, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Every build test that intends to cross execution authority explicitly seeds the matching approved specification, accepted plan phase, execution owner, mode, and canonical start receipt."
    - "Fixture phase IDs match the phase actually requested; tests no longer succeed through implicit phase-zero or mismatched-plan state."
    - "Live-process recovery tests use a genuinely live injected identity, while dead-process cases remain dead."
    - "The tests retain current D-16 product behavior rather than weakening authority to preserve legacy setup."
  artifacts:
    - path: "cmd/build_start_test_helpers_200_test.go"
      provides: "Explicit canonical build fixture options and assertions"
    - path: "cmd/codex_build_test.go"
      provides: "Core build cases migrated to accepted authority"
  key_links:
    - from: "cmd/codex_build_test.go"
      to: "cmd/build_start_test_helpers_200_test.go"
      via: "canonical test start and accepted-plan fixture"
      pattern: "commitTestBuildStart|createApproved"
---

<objective>
Migrate the core build fixture family to the final Phase 200 execution authority contract.

Purpose: Standalone failures showed many old tests reach their intended UI/team/forwarding assertion only after now-required accepted-plan and build-start boundaries, but their setup still stops earlier.
Output: Explicit canonical helpers plus migrated core, check-in, boundary, and recovery tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-35-SUMMARY.md
@.planning/phases/200-iterative-planning/200-36-SUMMARY.md
@.planning/phases/200-iterative-planning/200-48-SUMMARY.md
@cmd/plan_authority.go
</context>

<scope_guard>
Per D-10 and D-16, fixtures must earn specification approval and exact plan acceptance before build. Never add a production legacy bypass, silently auto-approve in a generic helper, or change current expected behavior merely to satisfy an old assertion.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make canonical build fixture authority explicit and assertable</name>
  <files>cmd/build_start_test_helpers_200_test.go</files>
  <behavior>
    - Callers must name the phase/execution owner/mode when the default would be ambiguous.
    - Accepted-plan fixtures contain the exact requested phase and canonical specification binding.
    - Live process uses an injected liveness seam or current test process; the fixed dead PID remains available only by explicit request.
    - Helper returns/validates the durable start receipt instead of directly writing attempt state.
  </behavior>
  <action>Extend the existing transaction-only helper with explicit options and guard assertions; do not replace canonical `commitBuildStart`. Make unsafe ambiguous defaults fail the test with a useful message. Provide narrowly named builders for accepted execution and live/dead recovery rather than one magic setup that grants authority to unrelated tests.</action>
  <verify>
    <automated>go test ./cmd -run '^TestCanonicalBuildStartFixture(Authority|LiveProcess|RefusesMismatch)200$' -count=1</automated>
  </verify>
  <done>The helper cannot fabricate a mismatched accepted phase or accidental liveness state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate core build command and verification-depth cases</name>
  <files>cmd/codex_build_test.go</files>
  <behavior>
    - Build writes/dispatch paths reach their intended assertions through approved spec, accepted plan, and one start receipt.
    - Plan-only team selection, verification-depth forwarding, task-scoped redispatch, prior-task repair, proposal round-trip, and external result collection preserve their current outputs.
    - No case writes attempts/latest pointers/manifests directly.
  </behavior>
  <action>Migrate every standalone-failing case in this file to the explicit helper. Preserve the test's subject: authority setup belongs before the act phase and must not alter expected team, depth, selected tasks, artifact paths, or next action. For cases intentionally testing refusal before acceptance, keep them unapproved and assert the modern refusal.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestBuildWritesDispatchArtifactsAndUpdatesState|TestDispatchEntryCarriesBriefPath|TestBuildPlanOnlyCLIForwardsVerificationDepth|TestBuildPlanOnlyHeavyReviewAllowsPolicyMeasurerAndChaos|TestBuildPlanOnlyKeepsRoutineUIQueenSelectionLean|TestBuildCLIForwardsVerificationDepth|TestBuildFinalizeRecordsExternalTaskResultsForContinue|TestBuildSupportsTaskScopedRedispatch|TestBuildRepairsCompletedPriorPhaseTasksFromTrustedManifest|TestBuildJobProposalRoundTrip)$' -count=1</automated>
  </verify>
  <done>All core build cases reach and prove their named behavior through canonical authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate check-in, boundary-guidance, and process-liveness fixtures</name>
  <files>cmd/ceremony_team_checkin_test.go, cmd/orchestrator_boundary_guidance_test.go, cmd/build_attempt_test.go</files>
  <behavior>
    - Pending decision check-in uses the same phase ID in accepted plan and requested build.
    - Finalizer boundary guidance carries required execution owner/mode/binding.
    - A live attempt recommends watch and a dead/interrupted attempt recommends force redispatch.
  </behavior>
  <action>Correct the phase-5/phase-1 mismatch in the check-in fixture, start the boundary-guidance manifest canonically, and inject honest process liveness. Keep owner-decision content and Next Up expectations unchanged under D-05..D-08 and D-16.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(PendingDecisionStillRendersFullCheckinCard|BuildFinalizeAddsOrchestratorBoundaryGuidance|ResumeDashboardDoesNotRedispatchLiveBuildProcess)$' -count=1</automated>
  </verify>
  <done>All three representative failures pass for the intended reason, not through weaker product gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test fixture to execution authority | A convenient helper can accidentally bypass the product's owner boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-49-01 | Elevation of privilege | accepted build fixture | mitigate | Explicit approved spec/plan/phase/owner/mode and canonical receipt. |
| T-200-49-02 | Spoofing | process liveness | mitigate | Inject a real live identity and explicit dead cases. |
| T-200-49-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run the focused cases in each task both individually and as one regex sequence.
</verification>

<success_criteria>
- Core build tests pass alone and together.
- No retired partial start helper or direct journal writer returns.
- D-10/D-16 authority remains stricter than legacy fixtures.
</success_criteria>

<output>Create `.planning/phases/200-iterative-planning/200-49-SUMMARY.md` when done.</output>
