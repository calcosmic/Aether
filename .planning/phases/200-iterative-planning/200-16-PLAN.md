---
phase: 200-iterative-planning
plan: 16
type: execute
wave: 7
depends_on: [200-08, 200-15]
files_modified:
  - cmd/plan_candidate.go
  - cmd/plan_candidate_test.go
  - cmd/plan_revision.go
  - cmd/plan_revision_test.go
  - cmd/codex_workflow_cmds.go
  - cmd/codex_workflow_cmds_test.go
autonomous: true
requirements: [PLAN-03, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "A stopped plan remains a reviewable candidate until the owner explicitly accepts that exact candidate."
    - "Acceptance atomically binds candidate, approved spec, base plan revision, complete timeline, and proposal hash."
    - "Stale or divergent acceptance cannot mutate the active plan."
  artifacts:
    - path: "cmd/plan_candidate.go"
      provides: "Candidate inspection and exact acceptance validation"
    - path: "cmd/plan_revision.go"
      provides: "Atomic accepted-candidate PlanRevision activation"
  key_links:
    - from: "cmd/plan_candidate.go"
      to: "cmd/plan_revision.go"
      via: "validated exact acceptance request"
      pattern: "PlanAcceptanceReceipt"
    - from: "cmd/plan_revision.go"
      to: "cmd/planning_timeline.go"
      via: "accepted timeline binding"
      pattern: "Timeline"
---

<objective>
Implement the separate review and exact acceptance authority that turns a stopped candidate into an active immutable plan revision.

Purpose: Ensure neither target attainment, convergence, specification approval, nor the deprecated accept flag can silently activate work.
Output: Candidate CLI routes, exact acceptance receipts, transactional revision activation, and stale/replay tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-UI-SPEC.md
@cmd/plan_revision.go
@cmd/codex_workflow_cmds.go
@cmd/planning_timeline.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose candidate review and exact acceptance inputs</name>
  <read_first>
    - cmd/plan_candidate.go (create if absent)
    - cmd/codex_workflow_cmds.go
    - cmd/codex_workflow_cmds_test.go
    - pkg/colony/planning.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-15, D-16)
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Candidate Review)
  </read_first>
  <files>cmd/plan_candidate.go, cmd/plan_candidate_test.go, cmd/codex_workflow_cmds.go, cmd/codex_workflow_cmds_test.go</files>
  <behavior>
    - aether plan --candidate displays the full proposed plan, five scores, stop reason, remaining gaps with evidence that would change each one, typed Queen recommendation/rationale/producer, and complete iteration timeline binding.
    - Acceptance requires exact candidate ID, spec revision/hash, base plan revision, timeline digest, proposal hash, and documented explicit token.
    - --show-iteration N --details resolves one immutable card without changing state.
  </behavior>
  <action>Add renderer-neutral candidate and iteration-detail query operations to the public plan command. Add an exact candidate-accept operation whose request names every binding and the explicit acceptance token; reject missing, ambiguous, or mixed review/accept flags before state access. Keep the old bare --accept flag as a non-mutating migration error only. Review output must disclose target versus actual confidence, exact stop reason and diagnostics, the candidate's top-level `evidence_that_would_change`, every nonmaterial remaining gap plus its own causal field, semantic additions/modifications/removals, authority impacts, spec/base bindings, timeline digest, and the persisted Queen recommendation (`accept` or `revise`), evidence-grounded rationale, evidence IDs, and authorized producer identity before the exact acceptance command. The query must return these typed fields from the candidate and may not derive or invent a recommendation in the renderer. Detail queries validate pass ordinal against the bound timeline and return each card's evidence_that_would_change.</action>
  <acceptance_criteria>
    - Candidate review and iteration detail operations perform no writes.
    - Exact acceptance request contains all six bindings plus explicit token.
    - Bare --accept and partial acceptance inputs fail with recovery guidance and zero mutation.
    - Out-of-range iteration detail fails without falling back to another card.
    - Candidate and iteration detail results expose nonempty evidence_that_would_change; candidate result exposes the persisted recommendation, rationale, evidence IDs, and producer unchanged.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanCandidate.*Review|TestPlanCandidate.*Inputs|TestPlanCandidate.*Details|TestPlanCommand.*Candidate' -count=1</automated>
  </verify>
  <done>The owner can inspect every relevant detail and can express acceptance only for one exact candidate frontier.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Activate an immutable PlanRevision only after exact acceptance</name>
  <read_first>
    - cmd/plan_candidate.go
    - cmd/plan_revision.go
    - cmd/plan_revision_test.go
    - cmd/planning_timeline.go
    - cmd/lifecycle_tx.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-10, D-12, D-16)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Candidate Acceptance Transaction)
  </read_first>
  <files>cmd/plan_candidate.go, cmd/plan_candidate_test.go, cmd/plan_revision.go, cmd/plan_revision_test.go</files>
  <behavior>
    - Exact current bindings produce one immutable accepted PlanRevision and PlanAcceptanceReceipt atomically.
    - The revision preserves completed compatible work and stores spec, candidate, base, proposal, and timeline identities.
    - Exact retry returns the original receipt; any stale or divergent binding fails with byte-identical state.
  </behavior>
  <action>Validate candidate status pending_review, current approved spec revision/hash, current active base revision, complete verified timeline digest, proposal hash, and exact token. Construct a new immutable PlanRevision from the candidate proposal, copying stable semantic/proof IDs and binding all acceptance inputs plus the receipt hash. Through one lifecycle transaction append the revision, store the acceptance receipt, mark the candidate accepted, and set ActiveRevisionID and active phases. Validate the transaction before commit. Do not mutate a predecessor revision or treat spec approval as acceptance. On exact replay return the original receipt; on stale base/spec/timeline, changed proposal, already rejected candidate, or token mismatch return a field-specific refusal with no writes.</action>
  <acceptance_criteria>
    - Successful acceptance changes active revision exactly once and binds every required identity.
    - Predecessor revision remains byte-identical and complete accepted timeline is still readable.
    - Exact retry returns the same revision and receipt without an extra revision.
    - Each stale or divergent binding fixture leaves state and artifacts byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanCandidate.*Accept|TestPlanRevision.*Candidate|TestPlanRevision.*Replay|TestPlanRevision.*Stale' -count=1</automated>
  </verify>
  <done>Only an explicit exact owner action can atomically promote a stopped candidate into the active immutable plan lineage.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestPlanCandidate|TestPlanRevision.*Candidate|TestPlanCommand.*Candidate' -count=1; compare state and artifact hashes before all refusal cases.
</verification>

<success_criteria>
- Candidate review exposes the full plan and its complete evidence timeline.
- Exact candidate acceptance is distinct from specification approval and all automatic stop policies.
- Activation is atomic, immutable, idempotent on exact replay, and fail-closed on drift.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-16-SUMMARY.md when done.
</output>
