---
phase: 200-iterative-planning
plan: 43
type: execute
wave: 26
depends_on: [200-41]
files_modified:
  - cmd/pheromone_write_test.go
  - cmd/pheromones_test.go
  - cmd/shelf_entomb_test.go
  - cmd/shelf_init_test.go
  - cmd/shelf_seal_test.go
autonomous: true
gap_closure: true
requirements: [CEC-03, PLAN-06]
must_haves:
  truths:
    - "Pheromone and shelf lifecycle tests restore repository authority exactly and cannot poison later planning/build tests."
    - "Signal and archive compatibility assertions remain unchanged while all writable roots stay contained."
  artifacts:
    - path: "cmd/pheromone_write_test.go"
      provides: "Isolated pheromone write fixtures"
    - path: "cmd/shelf_seal_test.go"
      provides: "Isolated sealed-shelf fixtures"
  key_links:
    - from: "cmd/pheromone_write_test.go"
      to: "cmd/root.go"
      via: "repository-local root/data binding"
      pattern: "AETHER_ROOT|COLONY_DATA_DIR"
---

<objective>
Remove the pheromone and shelf family of stale-root contamination.

Purpose: These fixtures use the same broken cleanup expression that retained deleted temp roots during the 119-failure gate.
Output: Five isolated test files with unchanged product assertions.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-41-SUMMARY.md
@cmd/testing_main_test.go
</context>

<scope_guard>
Do not alter pheromone policy, shelf semantics, learning governance, or Phase 200 planning decisions. This plan changes test ownership and cleanup only.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Isolate pheromone reader and writer fixtures</name>
  <files>cmd/pheromone_write_test.go, cmd/pheromones_test.go</files>
  <behavior>
    - Every case restores root presence/value exactly.
    - A later repository-backed command never observes a deleted pheromone fixture.
  </behavior>
  <action>Replace all self-restoring defers with the Plan 41 binding or exact testing cleanup. Preserve signal sanitization, deduplication, decay, and output assertions; do not make production paths tolerate a stale root.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(Pheromone|.*Pheromone)' -count=2</automated>
  </verify>
  <done>Pheromone tests pass twice and leak neither environment nor store state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isolate entomb, init, and seal shelf fixtures</name>
  <files>cmd/shelf_entomb_test.go, cmd/shelf_init_test.go, cmd/shelf_seal_test.go</files>
  <behavior>
    - Shelf commands use the repository that owns their fixture data.
    - Archive/restore tests retain their existing no-loss and fail-closed assertions.
  </behavior>
  <action>Migrate the three shelf files to exact repository binding. Keep destructive-target guards and archive fingerprints intact; this cleanup must not broaden what entomb or seal may touch.</action>
  <verify>
    <automated>go test ./cmd -run '^TestShelf(Entomb|Init|Seal)' -count=2</automated>
  </verify>
  <done>The owned shelf families pass repeatedly with their containment guarantees intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| lifecycle fixture to later test | Archive and signal tests mutate process-wide selectors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-43-01 | Tampering | test root cleanup | mitigate | Testing-owned exact restoration and repository-local data. |
| T-200-43-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
`! rg -n 'defer os\.Setenv\("AETHER_ROOT", os\.Getenv\("AETHER_ROOT"\)\)' cmd/{pheromone_write,pheromones,shelf_entomb,shelf_init,shelf_seal}_test.go`
</verification>

<success_criteria>
- All owned broken cleanup sites are removed.
- Signal and shelf assertions are not weakened.
</success_criteria>

<output>Create `.planning/phases/200-iterative-planning/200-43-SUMMARY.md` when done.</output>
