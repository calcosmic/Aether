---
phase: 200-iterative-planning
plan: 17
type: execute
wave: 8
depends_on: [200-08, 200-16]
files_modified:
  - cmd/plan_impact.go
  - cmd/plan_impact_test.go
  - cmd/plan_revision.go
  - cmd/plan_revision_test.go
  - cmd/state_extra.go
  - cmd/state_extra_test.go
  - cmd/seal_outcome.go
  - cmd/spec_plan_seal_gate_200_test.go
autonomous: true
requirements: [CEC-03, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "A material specification revision invalidates only the affected requirement-to-task-to-proof closure."
    - "Unaffected and completed compatible work stays preserved across accepted plan revisions."
    - "Build completion cannot seal as verified while affected scope lacks an approved spec and reconciled accepted plan."
  artifacts:
    - path: "cmd/plan_impact.go"
      provides: "Affected-scope closure and preservation policy"
    - path: "cmd/state_extra.go"
      provides: "Insert-phase routed through immutable candidate revision"
    - path: "cmd/seal_outcome.go"
      provides: "Affected-scope specification and plan seal gate"
  key_links:
    - from: "cmd/plan_impact.go"
      to: "cmd/plan_revision.go"
      via: "affected and preserved semantic IDs"
      pattern: "Affected|Preserved"
    - from: "cmd/seal_outcome.go"
      to: "cmd/plan_impact.go"
      via: "unresolved affected-scope refusal"
      pattern: "Impact|Affected"
---

<objective>
Make the specification and accepted plan genuinely living: scoped intent changes invalidate only affected work, reconcile through a new candidate, and gate verified seal.

Purpose: Preserve completed compatible work without allowing changed intent to hide behind an old accepted plan.
Output: Impact closure, immutable insert/revision behavior, and affected-scope seal enforcement.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md
@cmd/plan_revision.go
@cmd/state_extra.go
@cmd/seal_outcome.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compute affected closure and preserve compatible plan history</name>
  <read_first>
    - cmd/plan_impact.go (create if absent)
    - cmd/plan_revision.go
    - cmd/plan_revision_test.go
    - pkg/colony/specification.go
    - pkg/colony/planning.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-11, D-12)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Living-Plan Reconciliation)
  </read_first>
  <files>cmd/plan_impact.go, cmd/plan_impact_test.go, cmd/plan_revision.go, cmd/plan_revision_test.go</files>
  <behavior>
    - Changed or removed spec item IDs traverse requirement links to affected phases, tasks, dependencies, positive checks, negative checks, and recovery proofs.
    - Unaffected semantic IDs and completed compatible task status are preserved.
    - An approved material spec successor marks affected scope unreconciled until a new exact candidate is accepted.
  </behavior>
  <action>Build a deterministic graph from specification item IDs through plan requirement links, task/dependency edges, and proof-link categories. Compute transitive affected closure for add/modify/remove deltas, plus an explicit preserved set. Classify completed tasks as preserved only when neither their semantic content nor upstream dependency/proof closure is affected. Record impact on the new spec revision and lifecycle facts; do not alter the predecessor PlanRevision or reset unrelated task status. Require subsequent planning to use the current accepted revision as base and include all affected IDs. Clear the unreconciled marker only when exact candidate acceptance binds the new approved spec and covers that closure.</action>
  <acceptance_criteria>
    - One changed requirement invalidates its linked task, dependent task, and proof checks but not an independent completed branch.
    - Removal and addition fixtures produce deterministic affected and preserved sets.
    - Accepting a candidate that omits any affected ID is rejected.
    - Predecessor PlanRevision and unaffected completed status remain byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanImpact|TestPlanRevision.*Preserve|TestPlanRevision.*Affected' -count=1</automated>
  </verify>
  <done>Specification changes invalidate exactly the work whose meaning or proof changed and preserve everything else honestly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route insert-phase through the immutable revision path</name>
  <read_first>
    - cmd/state_extra.go
    - cmd/state_extra_test.go
    - cmd/plan_revision.go
    - cmd/plan_impact.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-11, D-12, D-16)
    - .planning/phases/200-iterative-planning/200-PATTERNS.md (mutation guardrails)
  </read_first>
  <files>cmd/state_extra.go, cmd/state_extra_test.go, cmd/plan_revision.go, cmd/plan_revision_test.go</files>
  <behavior>
    - Insert-phase never mutates or renumbers the active accepted PlanRevision in place.
    - A semantic insertion creates a proposal and pending candidate against the current approved spec and base revision.
    - Active attempts and stale bases refuse without partial state.
  </behavior>
  <action>Replace direct phase slice mutation and renumbering with stable-ID insertion into a new proposal derived from the active revision. Require an approved spec item or explicit spec-revision prerequisite for material new scope, compute affected and preserved sets, and persist a pending candidate through the same candidate boundary as iterative planning. Preserve predecessor ordinals as display metadata only; derive dependency ordering separately. Refuse insertion during an active attempt, against a stale base, or without specification coverage. Return the exact candidate review/accept path and leave ActiveRevisionID unchanged.</action>
  <acceptance_criteria>
    - Insert-phase produces a pending candidate and does not change active phases or predecessor revision.
    - Existing stable IDs and completed unaffected work are preserved.
    - Missing spec coverage, active attempt, and stale base each fail without writes.
    - Candidate acceptance later activates through the same exact binding checks as normal planning.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestInsertPhase.*Candidate|TestInsertPhase.*Immutable|TestInsertPhase.*Refuse' -count=1</automated>
  </verify>
  <done>Manual phase insertion obeys the same immutable specification and candidate-acceptance authority as generated planning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Block verified seal on unreconciled affected scope</name>
  <read_first>
    - cmd/seal_outcome.go
    - cmd/spec_plan_seal_gate_200_test.go (create if absent)
    - cmd/plan_impact.go
    - cmd/lifecycle_facts.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-12)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Seal Gate)
  </read_first>
  <files>cmd/seal_outcome.go, cmd/spec_plan_seal_gate_200_test.go</files>
  <behavior>
    - Verified seal refuses a draft or unapproved current spec, an unreconciled material spec revision, or an accepted plan missing affected-scope closure.
    - Historical legacy-unbound plans and unaffected completed scope keep their documented compatibility.
    - Existing forced-incomplete closure remains explicit and can never claim verified completion.
  </behavior>
  <action>Add the specification and plan authority checks to the existing seal outcome evaluation. For current-schema lineages require current spec approved, active plan acceptance bound to that exact spec, no pending affected IDs, and completed proof status for the reconciled plan. Return affected IDs and exact spec/plan recovery commands on refusal. Do not turn this into Phase-205 final owner product acceptance; it is only a structural lifecycle truth gate. Preserve the existing explicit forced-incomplete path and ensure it records non-verified status. Apply the legacy compatibility policy from migration rather than fabricating modern bindings.</action>
  <acceptance_criteria>
    - Draft, approved-but-unreconciled, wrong-spec accepted plan, and missing affected-proof fixtures cannot seal verified.
    - Fully reconciled accepted plan can reach the existing verified seal checks.
    - Legacy fixture follows documented legacy behavior without synthetic approval.
    - Forced-incomplete result remains visibly non-verified.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestSpecPlanSealGate200|TestSealOutcome.*Legacy|TestSealOutcome.*Forced' -count=1</automated>
  </verify>
  <done>Verified closure cannot outlive materially changed intent, while unaffected and legacy behavior remain honest.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestPlanImpact|TestInsertPhase|TestSpecPlanSealGate200|TestSealOutcome' -count=1 and compare immutable predecessor fixtures.
</verification>

<success_criteria>
- Impact closure follows requirement, task, dependency, and proof semantics rather than resetting the whole plan.
- Material plan changes always produce a new candidate and explicit acceptance.
- Verified seal requires the current approved specification and reconciled accepted plan, without adding Phase-205 product approval.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-17-SUMMARY.md when done.
</output>
