---
phase: 200-iterative-planning
plan: 54
type: execute
wave: 33
depends_on: [200-52, 200-53]
files_modified:
  - cmd/lifecycle_projection.go
  - cmd/lifecycle_card_startup_test.go
  - cmd/lifecycle_card_workloop_test.go
  - cmd/golden_workflow_test.go
  - cmd/testdata/golden_build.txt
  - cmd/testdata/golden_continue.txt
  - cmd/testdata/next_action_hardcode_baseline.json
  - cmd/testdata/colony_state_write_allowlist.json
autonomous: true
gap_closure: true
requirements: [SYNTH-02, CEC-03, PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Startup and work-loop cards/envelopes resolve one shared Next Up truth, including partial redispatch."
    - "Golden build/continue/state fixtures are refreshed only from behavior that passes semantic assertions under canonical authority."
    - "AST/allowlist baselines record only verified source moves; no new hand-typed command advice or direct colony-state writer is normalized away."
  artifacts:
    - path: "cmd/lifecycle_projection.go"
      provides: "Shared lifecycle/Next Up projection"
    - path: "cmd/testdata/golden_build.txt"
      provides: "Reviewed build visual baseline"
    - path: "cmd/testdata/next_action_hardcode_baseline.json"
      provides: "Exact current legacy command-advice debt, not an expanding waiver"
  key_links:
    - from: "cmd/lifecycle_projection.go"
      to: "cmd/lifecycle_card_workloop_test.go"
      via: "same card and JSON envelope facts"
      pattern: "Next|next"
    - from: "cmd/testdata/next_action_hardcode_baseline.json"
      to: "cmd/next_action_hardcode_ratchet_test.go"
      via: "one-for-one source identity after function rename"
      pattern: "runCodexPlanAgentDelegate"
---

<objective>
Reconcile lifecycle projections and refresh reviewed golden/static baselines last.

Purpose: Baselines currently mix genuine stale identities with upstream failures. They may change only after semantic behavior is correct.
Output: One shared lifecycle truth, reviewed goldens, and shrinking-only ratchet inventories.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-38-SUMMARY.md
@.planning/phases/200-iterative-planning/200-52-SUMMARY.md
@.planning/phases/200-iterative-planning/200-53-SUMMARY.md
@cmd/next_action.go
@cmd/colony_state_atomicity_ratchet_test.go
</context>

<scope_guard>
Do not bulk-accept output. Semantic assertions must pass before golden refresh. Baselines are exact inventories, not waivers: update moved identities, remove retired sites, and reject genuinely new hardcoded advice/direct writers.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore shared startup/work-loop lifecycle projection</name>
  <files>cmd/lifecycle_projection.go, cmd/lifecycle_card_startup_test.go, cmd/lifecycle_card_workloop_test.go</files>
  <behavior>
    - Each startup/work-loop terminal card and JSON envelope ends with the same resolver-issued action.
    - Partial build uses the exact Plan 48 redispatch command and never falls back to generic continue.
    - No command advice is hand-typed in a renderer.
  </behavior>
  <action>Repair the shared projection input/wiring after canonical fixtures and partial recovery. Feed exact runtime-issued action objects through card and envelope paths. Preserve platform spelling and accepted-plan gating under D-16.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(StartupLifecycle|WorkLoop|RedispatchOverrideReachesBothTheCardAndTheEnvelope|MigratedWorkLoopClosingsSayItOnce)' -count=1</automated>
  </verify>
  <done>Cards and envelopes agree on one authoritative lifecycle action for every covered state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild golden workflow fixtures from canonical successful runs</name>
  <files>cmd/golden_workflow_test.go, cmd/testdata/golden_build.txt, cmd/testdata/golden_continue.txt</files>
  <behavior>
    - Golden build/continue setup contains approved spec, accepted plan, and canonical start evidence.
    - State mutation reaches BUILT/current phase only through successful finalization.
    - Refreshed text matches reviewed current semantics, including recovery and Next Up.
  </behavior>
  <action>Migrate the golden harness first and run semantic state assertions. Inspect each textual diff; then update only the two owned golden files. Do not use a blanket update command before the semantic tests pass, and do not change expected modern behavior to reproduce an old snapshot.</action>
  <verify>
    <automated>go test ./cmd -run '^TestGolden(BuildVisualOutput|ContinueVisualOutput|StateMutations)$' -count=1</automated>
  </verify>
  <done>All three golden tests pass from canonical lifecycle state with reviewed diffs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile exact AST and writer inventories</name>
  <files>cmd/testdata/next_action_hardcode_baseline.json, cmd/testdata/colony_state_write_allowlist.json</files>
  <behavior>
    - The known delegate function rename updates one identity without increasing command-advice debt.
    - Retired direct writers are removed from the allowlist; any genuinely new writer remains a failure.
    - Both baseline tests prove their inventory only shrinks or moves one-for-one.
  </behavior>
  <action>Compare current AST sites to each baseline after all production fixes. Update the stale `runCodexPlanAgentDelegate` identity to its in-session name only if it is the same existing site, and reconcile colony-state writers one-for-one. Document every delta in the summary; never add a new site simply to turn the test green.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(NextActionNeverHardcoded|ColonyStateWriteAllowlistOnlyShrinks)$' -count=1</automated>
  </verify>
  <done>Both ratchets pass with no expanded debt and an audited delta list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| runtime lifecycle fact to presentation | Drift can advertise an unauthorized command. |
| source inventory to baseline | Blind refresh can normalize a new bypass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-54-01 | Spoofing | lifecycle command advice | mitigate | Resolver-issued action shared by terminal and JSON. |
| T-200-54-02 | Tampering | static baselines | mitigate | One-for-one audited deltas; no bulk acceptance. |
| T-200-54-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run all task commands, then `go test ./cmd -run '^Test(Golden|StartupLifecycle|WorkLoop|NextActionNeverHardcoded|ColonyStateWriteAllowlistOnlyShrinks)' -count=1`.
</verification>

<success_criteria>
- Lifecycle terminal/JSON truth agrees.
- Goldens reflect successful modern behavior.
- Static baselines do not expand debt.
</success_criteria>

<output>Create `.planning/phases/200-iterative-planning/200-54-SUMMARY.md` when done.</output>
