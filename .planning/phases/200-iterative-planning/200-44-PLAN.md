---
phase: 200-iterative-planning
plan: 44
type: execute
wave: 26
depends_on: [200-41]
files_modified:
  - cmd/shelf_test.go
  - cmd/worktree_merge_gate_test.go
  - cmd/worktree_operator_destruction_test.go
  - cmd/worktree_test.go
  - cmd/testing_main_test.go
autonomous: true
gap_closure: true
requirements: [CEC-03, PLAN-06]
must_haves:
  truths:
    - "Shelf and worktree tests cannot leak deleted repository roots, branches, stores, or Cobra state."
    - "A test-only ratchet rejects the exact self-restoring AETHER_ROOT anti-pattern across cmd tests."
    - "Worktree ownership and destruction guards remain fail-closed."
  artifacts:
    - path: "cmd/worktree_operator_destruction_test.go"
      provides: "Contained worktree destruction fixtures"
    - path: "cmd/testing_main_test.go"
      provides: "Regression ratchet for the proven cleanup anti-pattern"
  key_links:
    - from: "cmd/testing_main_test.go"
      to: "cmd/*_test.go"
      via: "source scan excluding comments"
      pattern: "AETHER_ROOT"
---

<objective>
Finish the stale-root cleanup and make the defect non-regressing.

Purpose: Worktree tests carry extra branch/filesystem state, so exact cleanup is required before full-suite concurrency or runtime work is trustworthy.
Output: Four migrated test files and one focused anti-pattern ratchet.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-41-SUMMARY.md
@cmd/testing_main_test.go
</context>

<scope_guard>
Keep registered-worktree cleanup exact and recoverable. Never delete an unregistered worktree/branch and never weaken operator-destruction refusal to make a test pass.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Isolate shelf and worktree fixtures</name>
  <files>cmd/shelf_test.go, cmd/worktree_merge_gate_test.go, cmd/worktree_operator_destruction_test.go, cmd/worktree_test.go</files>
  <behavior>
    - Root cleanup restores the original state rather than the temporary value.
    - Test-created worktrees remain registered and are the only worktrees cleanup may remove.
    - Repeated execution leaves no branch, directory, store, or environment leak.
  </behavior>
  <action>Replace every proven self-restoring defer with exact testing cleanup and use the shared repository binder for Cobra paths. Preserve `registerTestOwnedWorktree` ownership checks and all destructive-operation refusals. Run gofmt only on touched Go files.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(Shelf|Worktree)' -count=2</automated>
  </verify>
  <done>Shelf/worktree tests pass twice and their before/after repository fingerprints remain clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ratchet out self-restoring environment cleanup</name>
  <files>cmd/testing_main_test.go</files>
  <behavior>
    - Active Go test code containing the exact broken defer is rejected.
    - Comments and explanatory strings do not make the ratchet fail itself.
    - The repository contains zero active instances after Plans 42-44.
  </behavior>
  <action>Add a narrow source ratchet for `defer os.Setenv("AETHER_ROOT", os.Getenv("AETHER_ROOT"))`. Parse or filter executable source so its own prose cannot self-match; do not create a broad style linter. Assert all 120 known active sites are gone after the three bounded migrations.</action>
  <verify>
    <automated>go test ./cmd -run '^TestNoSelfRestoringAetherRootCleanup200$' -count=1</automated>
  </verify>
  <done>The ratchet passes and a deliberately injected in-memory sample of the anti-pattern is detected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test-owned worktree to user worktree | Cleanup must not infer ownership from path shape alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-44-01 | Tampering | worktree cleanup | mitigate | Retain positive test ownership registry and exact target validation. |
| T-200-44-02 | Repudiation | cleanup ratchet | mitigate | Source-level proof names every remaining active site. |
| T-200-44-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run `go test ./cmd -run '^Test(NoSelfRestoringAetherRootCleanup200|Shelf|Worktree)' -count=2` and confirm zero active bad sites with an AST-aware check.
</verification>

<success_criteria>
- All 14 files / 120 active cleanup defects are closed across Plans 42-44.
- Worktree safety stays fail-closed.
- The anti-pattern cannot silently return.
</success_criteria>

<output>Create `.planning/phases/200-iterative-planning/200-44-SUMMARY.md` when done.</output>
