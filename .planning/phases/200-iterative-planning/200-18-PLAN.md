---
phase: 200-iterative-planning
plan: 18
type: execute
wave: 8
depends_on: [200-16]
files_modified:
  - cmd/plan_authority.go
  - cmd/plan_authority_test.go
  - cmd/codex_build.go
  - cmd/plan_acceptance_gate_200_test.go
  - cmd/autopilot_policy.go
  - cmd/autopilot_policy_test.go
autonomous: true
requirements: [PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Build and run consume the same accepted-plan authority policy."
    - "Draft specs and pending candidates are never build-eligible."
    - "Legacy active plans retain explicitly classified compatibility."
  artifacts:
    - path: "cmd/plan_authority.go"
      provides: "Shared accepted-plan authority validator"
    - path: "cmd/codex_build.go"
      provides: "Build preflight using exact authority"
    - path: "cmd/autopilot_policy.go"
      provides: "Run/autopilot preflight using the same authority"
  key_links:
    - from: "cmd/codex_build.go"
      to: "cmd/plan_authority.go"
      via: "pre-dispatch gate"
      pattern: "validateAcceptedPlanAuthority"
    - from: "cmd/autopilot_policy.go"
      to: "cmd/plan_authority.go"
      via: "pre-loop gate"
      pattern: "validateAcceptedPlanAuthority"
---

<objective>
Enforce one shared accepted-plan authority policy at both build and run/autopilot entry points.

Purpose: Make candidate acceptance meaningful by ensuring no execution surface can bypass it, while preserving honest legacy compatibility.
Output: Shared validator plus build and autopilot preflight integration and parity tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@cmd/codex_build.go
@cmd/autopilot_policy.go
@cmd/plan_revision.go
@cmd/planning_migration.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared current and legacy plan authority</name>
  <read_first>
    - cmd/plan_authority.go (create if absent)
    - cmd/plan_revision.go
    - cmd/planning_migration.go
    - pkg/colony/planning.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-10, D-12, D-16)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Build Eligibility)
  </read_first>
  <files>cmd/plan_authority.go, cmd/plan_authority_test.go</files>
  <behavior>
    - Current-schema plan is eligible only when active revision has an exact valid acceptance receipt bound to current approved spec and complete timeline.
    - Draft spec, pending/rejected candidate, stale spec, stale base, broken timeline, or unresolved affected scope is ineligible.
    - Explicit legacy_unbound active plans follow the compatibility rule and expose that classification.
  </behavior>
  <action>Implement validateAcceptedPlanAuthority as a pure validator over lifecycle facts and verified bindings. Return a structured decision containing eligible, classification current_accepted or legacy_unbound, active revision, spec/candidate/timeline bindings, refusal code, affected IDs, and exact recovery command. Current plans must validate every acceptance receipt field and current specification relation. Legacy plans are eligible only under the explicit migration marker and existing active-plan conditions; never synthesize receipts. Pending candidates and spec approval alone are categorically insufficient.</action>
  <acceptance_criteria>
    - Current accepted, draft spec, pending candidate, stale spec, broken timeline, affected scope, and legacy fixtures have exact expected decisions.
    - No boolean shortcut can make a current candidate eligible.
    - Refusal results name the failed authority and exact review, spec, or plan command.
    - Validator performs no state mutation.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanAuthority' -count=1</automated>
  </verify>
  <done>One pure policy now defines exactly which plan lineage may execute and why.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply identical authority at build and autopilot preflight</name>
  <read_first>
    - cmd/codex_build.go
    - cmd/plan_acceptance_gate_200_test.go (create if absent)
    - cmd/autopilot_policy.go
    - cmd/autopilot_policy_test.go
    - cmd/plan_authority.go
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Candidate and Recovery States)
  </read_first>
  <files>cmd/codex_build.go, cmd/plan_acceptance_gate_200_test.go, cmd/autopilot_policy.go, cmd/autopilot_policy_test.go</files>
  <behavior>
    - Build checks plan authority before worker manifest generation or task mutation.
    - Run/autopilot checks the same policy before entering its loop.
    - Both surfaces return the same refusal code and equivalent recovery actions for the same state.
  </behavior>
  <action>Call validateAcceptedPlanAuthority at the earliest shared preflight point in build and in the run/autopilot policy. On refusal, emit the structured authority decision through existing error/result plumbing and perform zero dispatches, phase mutations, or attempts. On success, carry accepted revision, spec, candidate, and timeline IDs into the build context for attribution; carry the legacy classification explicitly when applicable. Add cross-surface table tests asserting identical eligibility and recovery for candidate-ready, draft spec, stale revision, affected scope, current accepted, and legacy states.</action>
  <acceptance_criteria>
    - Candidate-ready and draft-spec build/run fixtures perform zero dispatch and zero phase mutation.
    - Current exact accepted and legacy-compatible fixtures reach existing preflight continuation.
    - Build and run return identical refusal codes and recovery commands for every shared fixture.
    - Existing build safety and autopilot preflight tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanAcceptanceGate200|TestAutopilotPolicy.*Authority|TestCodexBuild.*Authority' -count=1</automated>
  </verify>
  <done>Neither manual build nor autopilot can bypass candidate acceptance, and both explain recovery consistently.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestPlanAuthority|TestPlanAcceptanceGate200|TestAutopilotPolicy.*Authority|TestCodexBuild.*Authority' -count=1 and assert zero dispatch on every refusal.
</verification>

<success_criteria>
- Build and run share one exact accepted-plan authority validator.
- Current pending or stale planning states cannot execute.
- Legacy behavior remains compatible only under an explicit honest classification.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-18-SUMMARY.md when done.
</output>
