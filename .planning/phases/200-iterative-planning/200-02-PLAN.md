---
phase: 200-iterative-planning
plan: 02
type: execute
wave: 2
depends_on: [200-01]
files_modified:
  - cmd/planning_state.go
  - cmd/planning_state_test.go
  - cmd/planning_migration.go
  - cmd/planning_migration_test.go
  - cmd/state_load.go
  - cmd/state_load_test.go
autonomous: true
requirements: [PLAN-05, PLAN-06]
must_haves:
  truths:
    - "An existing colony remains buildable without fabricated modern approvals."
    - "Interrupted or upgraded planning state resumes from a classified, validated representation."
  artifacts:
    - path: "cmd/planning_state.go"
      provides: "Planning and specification state validation"
    - path: "cmd/planning_migration.go"
      provides: "Explicit legacy and current-schema migration"
  key_links:
    - from: "cmd/state_load.go"
      to: "cmd/planning_migration.go"
      via: "load-time normalization"
      pattern: "migratePlanning|normalizePlanning"
---

<objective>
Make the new specification and planning records safe to load, migrate, replay, and validate across old and interrupted colonies.

Purpose: Preserve honest backward compatibility: legacy plans work as legacy plans, while new authority is never invented from missing history.
Output: Schema validation, migration classifiers, and representative old/current/future fixture tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@pkg/colony/colony.go
@pkg/colony/specification.go
@pkg/colony/planning.go
@cmd/state_load.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate current specification and planning state</name>
  <read_first>
    - cmd/state_load.go
    - cmd/planning_state.go (create if absent)
    - pkg/colony/specification.go
    - pkg/colony/planning.go
    - cmd/lifecycle_tx.go
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Storage and Replay Invariants)
  </read_first>
  <files>cmd/planning_state.go, cmd/planning_state_test.go</files>
  <behavior>
    - Current records reject dangling predecessor IDs, duplicate stable IDs, invalid hashes, impossible status combinations, and candidate bindings to absent revisions.
    - Optional absent planning fields remain valid for old colonies.
    - Unknown future major schema versions fail closed with an actionable error.
  </behavior>
  <action>Implement pure validation and normalization helpers for the specification lineage, iteration cards, timelines, candidates, and acceptance receipts. Validate content-addressed IDs, strictly increasing iteration order, exactly five unique dimensions per completed card, immutable predecessor chains, and exact binding reachability. Treat absent aggregates as legacy-compatible, but reject partially populated current records. Reject unknown future major versions rather than dropping fields. Keep validation read-only and deterministic so lifecycle transactions can call it before commit.</action>
  <acceptance_criteria>
    - Current valid fixtures round-trip byte-stably after normalization.
    - Fixtures with a dangling spec predecessor, duplicate dimension, wrong digest, or unknown future major version produce distinct errors.
    - An empty legacy aggregate validates without gaining an approval receipt or candidate.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningState' -count=1</automated>
  </verify>
  <done>Current planning state has deterministic structural validation and old state remains representable without synthetic authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify and migrate legacy planning artifacts on load</name>
  <read_first>
    - cmd/state_load.go
    - cmd/state_load_test.go
    - cmd/planning_migration.go (create if absent)
    - cmd/planning_state.go
    - cmd/codex_plan.go (existing iteration artifact shapes)
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-11, D-12, D-16)
  </read_first>
  <files>cmd/planning_migration.go, cmd/planning_migration_test.go, cmd/state_load.go, cmd/state_load_test.go</files>
  <behavior>
    - Pre-Phase-200 active plans load as legacy_unbound and retain their existing build eligibility.
    - Old planning-loop artifacts are indexed as legacy evidence or timeline material but never treated as approved specs or accepted candidates.
    - Running migration twice produces the same state.
  </behavior>
  <action>Invoke an idempotent planning migration from the existing state-load normalization path. Detect legacy plans by missing current schema or bindings, mark them with an explicit legacy_unbound classification, and preserve their phases, task status, active revision, and build behavior. Discover existing Phase-199 planning-loop artifacts only under validated repository-relative .aether/data paths, record their hashes as legacy-unbound references, and never infer a specification approval, candidate acceptance, semantic IDs, or evidence freshness. Persist migration only through the existing safe state write mechanism and surface malformed or path-escaping artifacts according to existing loader error conventions.</action>
  <acceptance_criteria>
    - A pre-Phase-200 fixture has identical active phase and task content after load and an explicit legacy_unbound marker.
    - No migrated fixture contains a fabricated SpecApprovalReceipt or PlanAcceptanceReceipt.
    - Two normalization passes serialize identically.
    - A path-escaping legacy artifact reference is rejected and no external file is read.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningMigration|TestStateLoad.*Planning' -count=1</automated>
  </verify>
  <done>Upgrade and replay preserve truthful legacy authority while preparing current state for the new workflow.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestPlanningState|TestPlanningMigration|TestStateLoad.*Planning' -count=1 and compare pre/post legacy plan phase and task payloads.
</verification>

<success_criteria>
- Legacy active plans remain usable under an explicit legacy classification.
- Current records reject corrupt lineage, digest, order, and binding combinations.
- Migration is idempotent and never manufactures user approval.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-02-SUMMARY.md when done.
</output>
