---
phase: 200-iterative-planning
plan: 55
type: execute
wave: 34
depends_on: [200-42, 200-43, 200-44, 200-45, 200-46, 200-47, 200-48, 200-49, 200-50, 200-51, 200-52, 200-53, 200-54]
files_modified:
  - cmd/testing_main_test.go
  - cmd/full_suite_runtime_200_test.go
  - cmd/isolated_process_test.go
autonomous: true
gap_closure: true
requirements: [SYNTH-02, CEC-03, PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "The exact commands `go test ./... -count=1` and `go test ./... -race -count=1` each finish below the execution harness's fixed 11-minute ceiling."
    - "Every discovered cmd top-level test runs exactly once; no test, subtest, failure, panic, race, or output is skipped or hidden."
    - "Focused `-run` commands retain their normal direct behavior and explicit child timeouts remain fail-closed."
    - "Concurrent test processes have isolated hub/repository state, while tests that truly share checkout/git resources stay in a proven serial lane."
  artifacts:
    - path: "cmd/testing_main_test.go"
      provides: "Bounded complete cmd-suite execution harness"
    - path: "cmd/full_suite_runtime_200_test.go"
      provides: "Exact-once, failure propagation, isolation, and runtime contract tests"
  key_links:
    - from: "cmd/testing_main_test.go"
      to: "cmd/isolated_process_test.go"
      via: "current race-instrumented test binary child execution with recursion sentinel"
      pattern: "os.Executable|test.run|AETHER.*SHARD"
---

<objective>
Make the complete normal and race repository gates finish inside the fixed 11-minute execution harness without weakening coverage.

Purpose: Raising Go's internal timeout to 30 minutes did not help because the external runner killed the cmd package at 11 minutes. Plan 40's exact commands cannot change.
Output: A deterministic, complete, bounded cmd test harness and measured normal/race proof.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-26-SUMMARY.md
@.planning/phases/200-iterative-planning/200-44-SUMMARY.md
@.planning/phases/200-iterative-planning/200-54-SUMMARY.md
@cmd/testing_main_test.go
@cmd/isolated_process_test.go
</context>

<scope_guard>
Plan 40's commands remain byte-for-byte unchanged. Do not raise `test.timeout` as the solution, skip slow tests, add build tags, lower race coverage, cache results, weaken assertions, or classify failures as expected. The current race-instrumented binary must execute every selected test.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define exact-once full-suite partition and failure propagation</name>
  <files>cmd/testing_main_test.go, cmd/full_suite_runtime_200_test.go, cmd/isolated_process_test.go</files>
  <behavior>
    - Only an unfiltered default cmd-package run enters the controller; focused `-test.run` children call `m.Run` directly.
    - Top-level test discovery is deterministic and partitions the complete set exactly once.
    - The current executable is reused, preserving race instrumentation and other build-time instrumentation.
    - Any child nonzero exit, panic, race report, timeout, or launch failure makes the parent nonzero and includes actionable output.
  </behavior>
  <action>Write a small pure planner plus subprocess contract tests first. In `TestMain`, detect the exact unfiltered suite and use a recursion sentinel to enumerate/partition top-level tests into bounded child invocations of `os.Executable`; do not invoke `go test` recursively. Preserve relevant test flags and explicit timeout semantics. Capture each child result, emit deterministic readable output, and return failure if any child fails. Prove with a fake child/helper-process seam that duplicates, omissions, and swallowed failures are impossible.</action>
  <verify>
    <automated>go test ./cmd -run '^TestFullSuiteRuntime200(PartitionExactOnce|PreservesFocusedRun|PropagatesFailure|UsesCurrentBinary)$' -count=1</automated>
  </verify>
  <done>The controller is completely unit-proven without needing a full-suite run and focused tests do not recurse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isolate safe parallel lanes and serialize shared checkout resources</name>
  <files>cmd/testing_main_test.go, cmd/full_suite_runtime_200_test.go</files>
  <behavior>
    - Each child receives its own AETHER_HUB_DIR and no stale parent store/root.
    - Tests using the real checkout, git worktree registry, fixed external paths, or other proven shared resources run in a deterministic serial lane.
    - Remaining shards run concurrently with a bounded worker count and pass repeated contamination probes.
  </behavior>
  <action>Classify only evidence-backed shared-resource top-level tests into a named serial lane; partition all other tests by stable cost-aware buckets using measured durations or a checked-in deterministic grouping. Initialize suite-global environment inside each child, not once in the controller. Add repeated adversarial order probes around root/store, worktree, hook, patrol, planning, and build tests. Avoid `t.Parallel` changes across thousands of cases.</action>
  <verify>
    <automated>go test ./cmd -run '^TestFullSuiteRuntime200(IsolatesChildEnvironment|SerializesSharedResources|AdversarialOrder)$' -count=1</automated>
  </verify>
  <done>Parallel children share no mutable test authority and the serial inventory is explicit, narrow, and covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove both exact repository commands within the external ceiling</name>
  <files>cmd/full_suite_runtime_200_test.go, cmd/testing_main_test.go</files>
  <behavior>
    - Normal and race commands each complete below 11 minutes on uncached runs.
    - Controller output reports discovered/executed counts and per-lane durations so omissions and regressions are auditable.
    - A second uncached normal run remains green and bounded.
  </behavior>
  <action>After focused controller tests pass, run the exact Plan 40 commands with wall-clock measurement. Require a practical margin below the 11-minute harness rather than a 10m59s near-pass; tune only bounded concurrency/grouping, never test selection. Record test counts and slowest lanes in the summary. If either exact command is nonzero, fix the owning earlier repair rather than adding exclusions.</action>
  <verify>
    <automated>go test ./cmd -run '^TestFullSuiteRuntime200ReportsCompleteAccounting$' -count=1 &amp;&amp; go test ./... -count=1 &amp;&amp; go test ./... -race -count=1</automated>
  </verify>
  <done>`go test ./... -count=1` and `go test ./... -race -count=1` both exit zero below 11 minutes with complete accounting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| controller to child test processes | Partitioning can silently omit tests or hide failures. |
| parallel children to shared repository | Shared git/filesystem state can make results nondeterministic or destructive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-55-01 | Repudiation | test accounting | mitigate | Discover/execute exact-once accounting and deterministic output. |
| T-200-55-02 | Denial of service | 11-minute harness | mitigate | Bounded current-binary shards with measured margin. |
| T-200-55-03 | Tampering | shared checkout | mitigate | Isolated child environments and explicit serial resource lane. |
| T-200-55-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<source_coverage>
## Multi-Source Coverage Audit

| Source | Items | Coverage |
|---|---|---|
| GOAL | Visible Scout → Route-Setter improvement over authoritative modern finalization | Existing Plans 01-39 retain the implementation; Plans 41-55 restore trustworthy proof without changing the goal. |
| REQ | SYNTH-02, CEC-03, PLAN-01..06 | Present across repair-plan frontmatter; final exact gates exercise all. |
| RESEARCH | Mechanism synthesis, Go authority, thin wrappers, semantic tests, containment | Repairs preserve these constraints; no new dependency or wrapper authority. |
| CONTEXT | D-01..D-16 | Presentation plans preserve D-01..04; decisions D-05..08; specification D-09..12; stop policy D-13..15; execution gate D-16. No deferred Phase 201-205 feature is implemented. |
</source_coverage>

<verification>
1. `go test ./cmd -run '^TestFullSuiteRuntime200' -count=1`
2. `go test ./... -count=1` under 11 minutes
3. `go test ./... -race -count=1` under 11 minutes
4. Repeat command 2 uncached and compare discovered/executed accounting.
</verification>

<success_criteria>
- Both exact full gates are zero and below the external ceiling.
- Every cmd top-level test executes exactly once per gate.
- Focused tests and race instrumentation remain real.
- Plan 40 can now run unchanged.
</success_criteria>

<output>Create `.planning/phases/200-iterative-planning/200-55-SUMMARY.md` when done, then execute existing Plan 200-40.</output>
