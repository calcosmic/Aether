---
phase: 200-iterative-planning
plan: 25
type: execute
wave: 11
depends_on: [200-10, 200-19, 200-20]
files_modified:
  - .aether/commands/spec.yaml
  - .claude/commands/ant-spec.md
  - .claude/commands/ant/spec.md
  - .opencode/commands/ant/spec.md
  - .aether/commands/classic-command-parity.json
  - cmd/classic_command_parity_test.go
autonomous: true
requirements: [SYNTH-02, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "A real /ant-spec surface exists on Claude and OpenCode and delegates every mutation to the Go specification command."
    - "Specification approval remains visibly separate from planning stop and exact plan-candidate acceptance."
    - "The public command inventory records discuss and spec only when their runtime and managed platform paths actually exist."
  artifacts:
    - path: ".aether/commands/spec.yaml"
      provides: "Canonical specification wrapper source"
    - path: ".aether/commands/classic-command-parity.json"
      provides: "Updated public workflow inventory"
    - path: "cmd/classic_command_parity_test.go"
      provides: "Runtime and managed-surface inventory enforcement"
  key_links:
    - from: ".aether/commands/spec.yaml"
      to: "cmd/spec_cmd.go"
      via: "runtime-backed inspect, revise, approve, and repair routes"
      pattern: "approve|revision|repair"
    - from: ".aether/commands/classic-command-parity.json"
      to: "cmd/classic_command_parity_test.go"
      via: "closed inventory validation against real runtime and wrapper paths"
      pattern: "discuss|spec"
---

<objective>
Create the real specification wrappers for Claude and OpenCode and extend the public command inventory in the same bounded surface plan.

Purpose: Make specification review and exact approval publicly reachable while keeping inventory claims tied to real Go and managed-wrapper paths.
Output: Canonical spec YAML, synchronized Claude/OpenCode projections, updated inventory, and parity enforcement.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-UI-SPEC.md
@.planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md
@cmd/spec_cmd.go
@cmd/specification.go
@.aether/commands/classic-command-parity.json
@cmd/classic_command_parity_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and synchronize the real specification wrapper</name>
  <read_first>
    - .aether/commands/spec.yaml (create if absent)
    - .claude/commands/ant-spec.md (create if absent)
    - .claude/commands/ant/spec.md (create if absent)
    - .opencode/commands/ant/spec.md (create if absent)
    - cmd/spec_cmd.go
    - cmd/specification.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-09 through D-12)
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Spec Review and Platform Spellings)
  </read_first>
  <files>.aether/commands/spec.yaml, .claude/commands/ant-spec.md, .claude/commands/ant/spec.md, .opencode/commands/ant/spec.md</files>
  <action>Per D-09 through D-12, create canonical spec YAML with inspect, add, modify, remove, approve, and projection-repair routes backed by aether spec. The wrapper displays exact revision and hash binding, draft or approved status, every typed stable-ID specification body category in the approved order, classified delta, preserved and affected scope, and exact approval syntax. It must not treat editor completion, generic confirmation, or file existence as approval and must never accept a plan. Manually synchronize Claude flat/nested and OpenCode managed projections from this YAML-backed source with platform-native /ant-spec spelling, retain direct Codex guidance as aether spec, and verify drift with `go run ./cmd/aether source-check`.</action>
  <acceptance_criteria>
    - All three managed spec entries exist, match the canonical YAML-backed contract, and pass source-check.
    - Each surface names draft, exact approval, immutable successor revision, and affected-scope reconciliation.
    - No spec surface claims candidate acceptance, plan activation, build eligibility, or wrapper-owned state writes.
    - Direct Codex documentation uses aether spec, not an unsupported alias.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test.*Spec.*Wrapper|TestSpecCommand' -count=1 &amp;&amp; go run ./cmd/aether source-check</automated>
  </verify>
  <done>/ant-spec is a real runtime-backed workflow on both primary platforms while Go retains all approval authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the public command parity inventory</name>
  <read_first>
    - .aether/commands/classic-command-parity.json
    - cmd/classic_command_parity_test.go
    - .aether/commands/discuss.yaml
    - .aether/commands/spec.yaml
    - cmd/root.go
    - .planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md (SYN-200-11)
  </read_first>
  <files>.aether/commands/classic-command-parity.json, cmd/classic_command_parity_test.go</files>
  <behavior>
    - Discuss and spec inventory entries resolve to actual Cobra registration, canonical YAML, and every managed primary-platform path.
    - Existing command entries and classifications remain unchanged.
    - Codex exposes direct aether spellings without claiming native $ant aliases.
  </behavior>
  <action>Make D-09's real public specification surface inventory-visible by adding discuss and spec to the normal public workflow inventory with their Go runtime, Claude, and OpenCode paths and canonical YAML source. Preserve all existing commands and classifications. Extend parity tests to require each inventory entry's actual Cobra registration and managed projection files, to forbid inventory-only claims, and to verify that direct Codex spellings are aether discuss/spec while primary-platform spellings are /ant-discuss/spec. Keep unsupported native Codex dollar-command claims absent.</action>
  <acceptance_criteria>
    - Inventory includes both discuss and spec with existing commands unchanged.
    - Every listed runtime and wrapper path exists and matches its platform.
    - Tests fail if a command is listed without Cobra registration or a managed projection.
    - Codex alias assertions reject $ant-discuss, $ant-spec, and $ant-plan claims.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestClassicCommandParity|Test.*Spec.*Registered' -count=1</automated>
  </verify>
  <done>Public inventory truthfully records the restored discuss/spec/plan journey on every supported platform.</done>
</task>

</tasks>

<verification>
Run `go run ./cmd/aether source-check`, then go test ./cmd -run 'Test.*Spec.*Wrapper|TestClassicCommandParity|Test.*Spec.*Registered' -count=1.
</verification>

<success_criteria>
- Canonical spec YAML has synchronized Claude and OpenCode projections verified by source-check.
- Specification wrappers expose exact review and approval without plan-acceptance or state authority.
- Public inventory and actual runtime/managed paths agree for discuss, spec, and all pre-existing commands.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-25-SUMMARY.md when done.
</output>
