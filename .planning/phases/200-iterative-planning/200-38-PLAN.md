---
phase: 200-iterative-planning
plan: 38
type: execute
wave: 21
depends_on: [200-37]
files_modified:
  - cmd/lifecycle_facts.go
  - cmd/planning_visuals.go
  - cmd/next_action.go
  - cmd/codex_workflow_cmds.go
  - cmd/planning_visuals_test.go
  - cmd/planning_expiry_presentation_200_test.go
autonomous: true
gap_closure: true
requirements: [PLAN-03, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Terminal and JSON expose the same current, expired, stale, or accepted standing, expiry timestamp, reason/evidence, state effect, and exact next command."
    - "Current review retains the complete D-16 acceptance command and NOT ACTIVE warning; stale/expired views recursively omit acceptance fields and show State: unchanged plus aether plan --refresh."
    - "Lifecycle facts capture standing once, and Next Up/current-step selection consumes those facts rather than deriving a second clock or contradictory command."
    - "The real Cobra acceptance path retains Plan 37's typed refusal details in stable JSON and the dedicated visual error while still returning nonzero."
    - "Widths below 48, 48-63, and 64 or more cells, color, NO_COLOR, and piped JSON keep exact IDs, hashes, timestamps, labels, and commands untruncated."
  artifacts:
    - path: "cmd/lifecycle_facts.go"
      provides: "One captured candidate-standing fact model"
      contains: "PendingCandidateStanding"
    - path: "cmd/planning_visuals.go"
      provides: "Shared terminal and JSON standing projection"
      contains: "planningCandidateProjection"
    - path: "cmd/next_action.go"
      provides: "Authority-aware Next Up/current-step resolution"
      contains: "next"
    - path: "cmd/codex_workflow_cmds.go"
      provides: "Nonzero Cobra refusal path that preserves typed candidate details"
      contains: "runPlanCandidateCommand"
    - path: "cmd/planning_expiry_presentation_200_test.go"
      provides: "Format, width, command, and resolver parity matrix"
      contains: "TestPlanningExpiryPresentation200"
  key_links:
    - from: "cmd/lifecycle_facts.go"
      to: "cmd/plan_candidate.go"
      via: "facts capture Plan 37 assessment once"
      pattern: "assessPlanCandidateStanding"
    - from: "cmd/next_action.go"
      to: "cmd/lifecycle_facts.go"
      via: "Next Up selects only commands authorized by captured standing"
      pattern: "PendingCandidateStanding"
    - from: "cmd/codex_workflow_cmds.go"
      to: "cmd/planning_visuals.go"
      via: "candidate refusal details choose the dedicated error renderer before the command returns nonzero"
      pattern: "runPlanCandidateCommand"
  prohibitions:
    - statement: "Terminal, JSON, lifecycle, managed wrapper, or Next Up presentation must not show an old accept, build, or run command as actionable for expired or stale authority, or hide whether repository state changed."
      status: unresolved
      reason: "The prohibition closes only when every format and width consumes one fact model and exposes the exact live recovery command."
  flagged_assumptions:
    - requirement_id: PLAN-03
      category: unclassified/manual-review
      status: unresolved
      flagged: true
      statement: "Automated color, NO_COLOR, JSON, and width checks prove all authority fields remain present, but a human must judge whether the live terminal hierarchy makes the current specification authority immediately understandable."
    - requirement_id: PLAN-05
      category: unclassified/manual-review
      status: unresolved
      flagged: true
      statement: "Automated state-specific recovery and nontruncation checks prove commands remain exact, but a human must judge whether the live candidate decision and stale-before-expiry explanation are clear enough to act on confidently."
---

<objective>
Render candidate expiry and recovery honestly across terminal, JSON, lifecycle facts, and Next Up.

Purpose: A sound authority rule still fails users if another surface offers a stale acceptance or build action.
Output: One fact model, accessible responsive rendering, and one current-step resolver.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-UI-SPEC.md
@.planning/phases/200-iterative-planning/200-37-SUMMARY.md
@cmd/lifecycle_facts.go
@cmd/planning_visuals.go
@cmd/next_action.go
@cmd/codex_workflow_cmds.go
@cmd/planning_visuals_test.go
</context>

<interfaces>
Extend lifecycle planning facts with standing, expires_at, reason, evidence, state_effect, acceptance_available, acceptance_command, and recovery_command. Terminal, JSON, Next Up, and Plan 37's typed planCandidateRefusalDetails consume these exact values; only Plan 37 assesses standing.
</interfaces>

<scope_guard>
Modify only the six declared files. Existing Claude/OpenCode wrappers remain thin runtime delegates and are validated later; do not invent wrapper-side state. Preserve user dirt and use scoped task commits.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock semantic parity across state, width, color, JSON, and commands</name>
  <files>cmd/codex_workflow_cmds.go, cmd/planning_visuals.go, cmd/planning_expiry_presentation_200_test.go, cmd/planning_visuals_test.go</files>
  <behavior>
    - "Current, each early-stale reason, expired, and accepted fixtures share an exact required/forbidden field matrix."
    - "Every printed command resolves against the live Cobra command and flag tree."
    - "Widths 32, 47, 48, 63, 64, and 80 retain complete semantic values; ANSI-stripped color equals NO_COLOR."
    - "Candidate review and Next Up are read-only and choose the same action."
    - "A first acceptance refusal at or after the deadline reports Candidate status: expired, Active plan: unchanged, and Next: aether plan --refresh in terminal and JSON; later expired review reports the same persisted standing without another mutation."
    - "Executing the exact at-deadline acceptance command through rootCmd returns a nonzero rendered-command error, preserves the active plan, and emits the same typed refusal fields in JSON and visual modes."
  </behavior>
  <action>First create table-driven projections using Plan 37's standing fixtures. Compare normalized terminal fields with JSON rather than border glyphs, but byte-check stable IDs/hashes/timestamps and exact commands. Under TestPlanningExpiryPresentation200, add fresh at_deadline_cobra_json_refusal and at_deadline_cobra_visual_refusal real rootCmd subprocess/in-process fixtures that inject Plan 37's command clock to equal ExpiresAt, supply the complete exact acceptance flags, assert Execute returns the rendered nonzero code, reload the now-expired candidate, prove the active plan bytes are unchanged, and decode stable JSON details for candidate_id, candidate_status=expired, standing=expired, expires_at, why_unavailable, evidence, state_effect=candidate_marked_expired, active_plan_effect=unchanged, and recovery_command=aether plan --refresh. Then change codex_workflow_cmds.go so the candidateErr branch retains the non-nil typed result from runPlanCandidateCommand instead of calling outputError with nil: JSON passes the typed refusal as details, while visual mode calls a dedicated planning refusal renderer showing Candidate status: expired, State: candidate marked expired, Active plan: unchanged, and Next: aether plan --refresh; both paths mark the command failed and return nonzero. Require current review to show NOT ACTIVE and full acceptance, every stale/expired result to omit token/accept/build/run fields, and later expired review to report State: unchanged without mutation. Cover all UI-SPEC width/color modes.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestPlanningExpiryPresentation200|TestGoldenPlanVisualOutput)$' -count=1</automated>
  </verify>
  <done>The fail-first presentation matrix specifies identical semantics for every state and surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate one fact model and drive terminal, JSON, and Next Up</name>
  <files>cmd/lifecycle_facts.go, cmd/planning_visuals.go, cmd/next_action.go, cmd/planning_visuals_test.go, cmd/planning_expiry_presentation_200_test.go</files>
  <action>Capture Plan 37 standing once at LifecycleFacts.CapturedAt and populate the additive fields without another clock read or write. Render one shared semantic projection in terminal and JSON using the locked labels Standing, Expires, Why unavailable, State, and Next. Stack below width 48, wrap labels at 48-63, and use the wider table at 64+, pairing color with words and never truncating identifiers or commands. Make Next Up/current-step consume the facts: current review/acceptance only when authorized, aether plan --refresh for stale/expired, accepted plan's existing build path after exact acceptance. Preserve D-01 compact iteration cards and D-16 acceptance separation; no renderer or resolver mutates state.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestPlanningExpiryPresentation200|TestGoldenPlanVisualOutput|Test.*NextAction.*|Test.*LifecycleFacts.*Planning.*)$' -count=1</automated>
  </verify>
  <done>All owner-facing surfaces show one unambiguous standing and only its exact executable next action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| standing assessment to lifecycle facts | Projection cannot recompute a different clock/state. |
| semantic facts to terminal/JSON | Formatting cannot hide authority or recovery. |
| facts to Next Up | Resolver cannot expose a command forbidden by review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-38-01 | Spoofing | stale candidate display | mitigate | Explicit standing/reason/state fields. |
| T-200-38-02 | Elevation of privilege | Next Up | mitigate | Same captured facts and recursive forbidden-field tests. |
| T-200-38-03 | Information disclosure | width/color output | mitigate | Semantic parity and untruncated stable values. |
| T-200-38-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run the full state/format matrix, actual Cobra at-deadline refusal, and Next Up tests. Inspect that typed refusal facts survive JSON/visual error rendering, the command returns nonzero, and facts/renderers/resolver perform no independent standing calculation.
</verification>

<success_criteria>
- Terminal and JSON expose identical authority/recovery semantics.
- Actual Cobra refusal preserves typed details, leaves the active plan unchanged, and returns nonzero.
- Next Up matches candidate review and uses only live commands.
- Human UAT assumptions remain explicit for final verification.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-38-SUMMARY.md when done.
</output>
