---
phase: 200-iterative-planning
plan: 53
type: execute
wave: 32
depends_on: [200-50, 200-51, 200-52]
files_modified:
  - cmd/codex_visuals.go
  - cmd/codex_visuals_test.go
  - cmd/codex_output_mode_test.go
  - cmd/build_print_brief_test.go
  - cmd/ceremony_closeout_spend_test.go
autonomous: true
gap_closure: true
requirements: [SYNTH-02, CEC-03, PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05]
must_haves:
  truths:
    - "Build and colonize visuals reach their intended artifact/spawn/dispatch assertions through current fixture authority."
    - "Explicit visual mode renders to a buffered host writer, while JSON mode stays valid and contains no visual/narrator noise."
    - "Each lane renders at most one cost line and full brief output retains its coverage contract."
  artifacts:
    - path: "cmd/codex_visuals.go"
      provides: "Consistent explicit output-mode and cost-line rendering"
    - path: "cmd/codex_visuals_test.go"
      provides: "Current build/colonize visual parity fixtures"
  key_links:
    - from: "cmd/codex_visuals.go"
      to: "cmd/codex_output_mode_test.go"
      via: "explicit AETHER_OUTPUT_MODE takes precedence over writer TTY shape"
      pattern: "AETHER_OUTPUT_MODE|shouldRenderVisualOutput"
---

<objective>
Restore the remaining build/colonize presentation contract after authority and recovery repair.

Purpose: The standalone gate exposed real output-mode/cost regressions mixed with visual fixtures that no longer reached rendering.
Output: Current fixtures and one consistent runtime-owned visual/JSON projection.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-38-SUMMARY.md
@.planning/phases/200-iterative-planning/200-51-SUMMARY.md
@cmd/build_start_test_helpers_200_test.go
</context>

<scope_guard>
Preserve D-01..D-04 compact causal presentation and D-16 exact runtime authority. Do not make tests pass by deleting owner-visible evidence, changing JSON semantics, or refreshing expected text before inspecting the behavioral diff.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate visual fixtures and preserve semantic parity</name>
  <files>cmd/codex_visuals_test.go, cmd/build_print_brief_test.go</files>
  <behavior>
    - Build visual cases show current spawn plan and artifact contract after accepted-plan start.
    - Colonize cases show dispatch preview/tree/contract after fresh-ledger creation.
    - Full brief contains each expected coverage item and no obsolete authority field.
  </behavior>
  <action>Migrate setup to Plans 45/49 and update assertions only where the final runtime-owned semantic contract changed intentionally. Keep checks on typed facts rather than ANSI spacing. Do not regenerate snapshots in this task.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestBuildVisualOutputShowsArtifactContract|TestBuildVisualOutputShowsSpawnPlan|TestColonizeVisualOutputShowsDispatchContractDetails|TestColonizeVisualOutputShowsDispatchPreview|TestColonizeVisualOutputShowsSpawnTreeContract|TestCodexVisualParity|TestPrintBriefFullFlagAndCoverage)$' -count=1</automated>
  </verify>
  <done>Visual fixtures reach rendering and prove the same artifact/dispatch facts as JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Honor explicit output mode and render spend once</name>
  <files>cmd/codex_visuals.go, cmd/codex_output_mode_test.go, cmd/ceremony_closeout_spend_test.go</files>
  <behavior>
    - `AETHER_OUTPUT_MODE=visual` renders visual output even when the host supplies a buffer.
    - JSON mode is one valid envelope with no banner, progress, or narrator prefix.
    - Every build lane contains zero or one authoritative spend line, never two.
  </behavior>
  <action>Make the explicit mode override authoritative before TTY inference and centralize spend rendering at one shared boundary. Keep unknown/empty mode behavior unchanged. Test both buffered and terminal-shaped writers and both build lanes.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(BuildBufferedOutputBreaksJSONUnderVisualEnv|NoLaneRendersTwoCostLines)$' -count=1</automated>
  </verify>
  <done>Explicit visual buffers work, JSON remains clean, and spend appears no more than once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| runtime facts to host writer | Mode inference or duplicate emitters can hide/corrupt authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-53-01 | Spoofing | JSON/visual mode | mitigate | Explicit mode precedence and envelope parse tests. |
| T-200-53-02 | Repudiation | spend line | mitigate | One authoritative emitter and per-lane cardinality tests. |
| T-200-53-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run all owned tests as one group, then the Phase 200 planning visual tests to guard cross-surface parity.
</verification>

<success_criteria>
- Visual and JSON modes are unambiguous.
- Build/colonize visual facts remain complete.
- Cost and brief output are neither duplicated nor clipped.
</success_criteria>

<output>Create `.planning/phases/200-iterative-planning/200-53-SUMMARY.md` when done.</output>
