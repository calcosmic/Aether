---
phase: 200-iterative-planning
plan: 51
type: execute
wave: 30
depends_on: [200-48, 200-49]
files_modified:
  - cmd/codex_build.go
  - cmd/codex_visuals.go
  - cmd/build_finalize_partial_screen_test.go
  - cmd/codex_build_native_partial_test.go
  - cmd/build_blocker_advisory_test.go
autonomous: true
gap_closure: true
requirements: [SYNTH-02, CEC-03, PLAN-05]
must_haves:
  truths:
    - "A partial build never renders the ordinary finished-build screen and always names unfinished task IDs plus its exact recovery command."
    - "Plan-only and executing build lanes emit the same one-time blocker heads-up and boundary-question signal."
    - "Non-interactive output retains the heads-up without duplicating it or hiding machine truth."
  artifacts:
    - path: "cmd/codex_visuals.go"
      provides: "Honest partial and blocker presentation"
    - path: "cmd/codex_build.go"
      provides: "Shared typed partial/blocker facts supplied to the renderer"
  key_links:
    - from: "cmd/codex_build.go"
      to: "cmd/codex_visuals.go"
      via: "typed unfinished IDs and recovery command"
      pattern: "unfinished|recovery"
    - from: "cmd/build_blocker_advisory_test.go"
      to: "cmd/codex_build.go"
      via: "same advisory facts across both lanes"
      pattern: "HeadsUp|heads_up|boundary"
---

<objective>
Restore the owner-visible partial-build and blocker-advisory contract.

Purpose: Standalone tests showed real outputs that either hid unfinished work or lost the shared heads-up before the Phase 200 final gate.
Output: One typed projection used consistently by native/external and plan/execution lanes.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-38-SUMMARY.md
@.planning/phases/200-iterative-planning/200-48-SUMMARY.md
@cmd/lifecycle_projection.go
</context>

<scope_guard>
Restore existing behavior only. Phase 201 owns new build-cycle UX and team/recovery redesign. Keep Phase 200's runtime-owned authority and do not manufacture commands in the renderer.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project partial completion as unfinished work with one recovery route</name>
  <files>cmd/codex_build.go, cmd/codex_visuals.go, cmd/build_finalize_partial_screen_test.go, cmd/codex_build_native_partial_test.go</files>
  <behavior>
    - Partial output contains each unfinished task ID and the exact command returned by Plan 48.
    - Partial output omits ordinary success/final completion markers.
    - External and native lanes render equivalent semantic facts in terminal and JSON.
  </behavior>
  <action>Feed the durable Plan 48 recovery outcome into the shared build result/projection, then render from typed fields. Never reconstruct `--task` advice from prose in the visual layer. Keep JSON machine fields and terminal wording semantically equal and preserve exact acceptance/attempt evidence.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(WrapperPartialFinalizeDoesNotShowTheFinishedBuildScreen|PartialBuildDoesNotShowTheOrdinaryBuildDoneScreen)$' -count=1</automated>
  </verify>
  <done>Both partial lanes name unfinished work and one recovery command without claiming completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse one blocker advisory across build lanes</name>
  <files>cmd/codex_build.go, cmd/codex_visuals.go, cmd/build_blocker_advisory_test.go</files>
  <behavior>
    - Plan-only and execution lanes each emit exactly one heads-up from the same typed advisory.
    - Boundary-question signaling is present in both lanes.
    - Non-interactive execution keeps the advisory and emits valid JSON when JSON mode is selected.
  </behavior>
  <action>Route blocker/boundary facts through one shared result field before lane branching and render once at the appropriate boundary. Preserve D-05..D-08 material-decision semantics: an advisory is not owner approval and cannot alter the accepted plan.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(BothBuildLanesEmitTheHeadsUp|BothBuildLanesAgreeOnBoundaryQuestionSignal|NonInteractiveRunsStillPrintTheHeadsUp)$' -count=1</automated>
  </verify>
  <done>Both lanes expose one consistent advisory without output pollution or authority mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| durable recovery facts to terminal | Renderer omissions can falsely imply completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-51-01 | Repudiation | partial completion screen | mitigate | Render typed unfinished IDs and exact runtime command; suppress success screen. |
| T-200-51-02 | Spoofing | blocker advisory | mitigate | One runtime-owned fact source across both lanes. |
| T-200-51-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run all five named tests individually and as one regex; compare JSON semantic fields between lanes.
</verification>

<success_criteria>
- Partial work is never presented as finished.
- Heads-up and boundary facts agree across lanes.
- No future Phase 201 behavior is added.
</success_criteria>

<output>Create `.planning/phases/200-iterative-planning/200-51-SUMMARY.md` when done.</output>
