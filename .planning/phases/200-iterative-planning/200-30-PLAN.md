---
phase: 200-iterative-planning
plan: 30
type: execute
wave: 17
depends_on: [200-28]
files_modified:
  - pkg/colony/planning.go
  - pkg/colony/planning_test.go
  - cmd/planning_delta.go
  - cmd/codex_plan_finalize.go
  - cmd/plan_candidate.go
  - cmd/plan_candidate_binding_200_test.go
autonomous: true
gap_closure: true
requirements: [PLAN-02, PLAN-03, PLAN-06]
must_haves:
  truths:
    - "Candidate identity binds the canonical review payload: exact proposal, base/spec/timeline authority, stop decision, five assessments, semantic delta, residual gaps, evidence-that-would-change, Queen recommendation, and created/expiry boundary."
    - "Every nested semantic change, authority impact, assessment, gap, recommendation, proposal, and timeline identity is recomputed rather than accepted from stored hash syntax."
    - "The primary user-facing unit remains planning iteration/pass, and an unchanged semantic result is exactly no-change."
    - "Changing any review-visible or authority-relevant field changes the candidate content hash, candidate ID, and acceptance token or causes validation refusal."
    - "[spec-less edge: PLAN-02/adjacency] Moving an otherwise identical change between adjacent plan sections changes the semantic-change hash, enclosing delta hash, review payload hash, candidate ID, and acceptance token."
    - "[spec-less edge: PLAN-02/empty-degenerate] Empty evidence IDs, duplicate authority impacts, missing before/after identity, and an empty material delta are rejected rather than addressed."
    - "[spec-less edge: PLAN-02/ordering-stability] Canonical ordering is deterministic for set-like evidence while sequence-bearing proposal sections remain order-sensitive across repeated finalization."
  artifacts:
    - path: "cmd/planning_delta.go"
      provides: "Canonical addressed semantic-change and authority-impact validation"
      contains: "PlanningSemanticDelta"
    - path: "pkg/colony/planning_test.go"
      provides: "Existing planning contracts rebuilt with production canonical identities"
      contains: "TestPlanning"
    - path: "cmd/codex_plan_finalize.go"
      provides: "Candidate construction from one canonical review payload"
      contains: "buildPlanningRouteCandidate"
    - path: "cmd/plan_candidate.go"
      provides: "Load/review recomputation and exact acceptance token"
      contains: "planCandidateAcceptanceToken"
    - path: "cmd/plan_candidate_binding_200_test.go"
      provides: "Nested mutation, ordering, and review-payload binding matrix"
      contains: "TestPlanCandidateSemanticIntegrity200"
  key_links:
    - from: "cmd/codex_plan_finalize.go"
      to: "cmd/planning_delta.go"
      via: "candidate builder canonicalizes and addresses the semantic delta"
      pattern: "buildPlanningRouteCandidate"
    - from: "cmd/plan_candidate.go"
      to: "pkg/colony/planning.go"
      via: "loaded candidate review payload is recomputed before display or token creation"
      pattern: "validatePlanningRecordHashes"
---

<objective>
Bind the complete canonical plan-review meaning into candidate identity.

Purpose: An owner must accept the exact semantic change and authority impact that later reconciliation uses.
Output: Canonical nested delta hashes, complete candidate payload addressing, and mutation-matrix tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-VERIFICATION.md
@pkg/colony/planning.go
@pkg/colony/planning_test.go
@cmd/planning_delta.go
@cmd/planning_state.go
@cmd/codex_plan_finalize.go
@cmd/plan_candidate.go
</context>

<interfaces>
Define one canonical candidate review-payload digest. It includes all immutable review and authority fields and excludes only the mutable status/acceptance transition envelope. To avoid a fixed-point hash, canonicalize the proposal revision with its proposal-, phase-, and task-level CandidateID/CandidateContentHash fields cleared, and clear QueenPlanRecommendation.CandidateID; these are derived back-references, not authority inputs. Hash that stripped semantic proposal and complete review payload first, derive the final candidate hash/ID, then populate every back-reference and require its exact match during validation.
</interfaces>

<scope_guard>
Modify only the six declared files. Acceptance-time independent re-derivation belongs to Plan 31. Preserve existing user dirt and use scoped task commits.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify complete candidate and nested-delta addressing</name>
  <files>cmd/plan_candidate_binding_200_test.go</files>
  <behavior>
    - "A one-field mutation across every review payload component either changes all enclosing identities or fails validation with old identities."
    - "Nested changes and authority impacts recompute their bodies, IDs, hashes, evidence sets, before/after values, and ordering semantics."
    - "Set-like evidence order normalizes deterministically; phase/task/dependency/proof/recovery sequence changes remain meaningful."
    - "Status/acceptance transition does not rewrite immutable candidate identity, but an invalid derived back-reference fails."
  </behavior>
  <action>Create a valid stopped two-pass candidate fixture, then table-drive mutations for proposal phases and hashes, base revision, approved spec, timeline/card binding, stop reason, each assessment/evidence, each semantic change, every authority impact, residual gap, evidence-that-would-change, recommendation, CreatedAt, and ExpiresAt. Assert construction determinism, load/review refusal with copied hashes, exact acceptance-token changes, and repository byte equality on refusal. Include the three tagged adjacency/empty/order cases and explicit planning iteration/pass plus no-change vocabulary assertions per D-01, D-03, D-04, D-14, and D-15.</action>
  <verify>
    <automated>go test ./cmd -run '^TestPlanCandidateSemanticIntegrity200' -count=1</automated>
  </verify>
  <done>The fail-first suite covers every immutable candidate and nested semantic field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Canonically recompute semantic changes and authority impacts</name>
  <files>pkg/colony/planning.go, pkg/colony/planning_test.go, cmd/planning_delta.go, cmd/plan_candidate_binding_200_test.go</files>
  <action>Implement canonical normalization and full-body hashing for PlanningSemanticChange, PlanningAuthorityImpact, PlanningSemanticDelta, PlanningDimensionAssessment, PlanningGap, and QueenPlanRecommendation. Preserve order where it carries plan meaning and sort/deduplicate only fields whose contract is a set. Reject empty material deltas, duplicate IDs/impacts, invalid evidence, missing before/after identities, truncated hashes, and derived-ID mismatches. Rebuild validPlanningSemanticDelta and the other pkg/colony/planning_test.go fixtures from the production canonical builders/preimages instead of *-hash placeholders; each rejection test must start canonical and alter only its named field. Keep whole-number confidence validation and D-03's distinct authority-impact channel. Builders, validators, and existing tests must share one canonical preimage definition, with no test-only digest substitute.</action>
  <verify>
    <automated>go test ./pkg/colony -run '^TestPlanning' -count=1 &amp;&amp; go test ./cmd -run '^(TestPlanCandidateSemanticIntegrity200|TestPlanningSemanticDelta.*|TestPlanning.*Assessment.*)' -count=1</automated>
  </verify>
  <done>Every nested authority input has one recomputable canonical identity and strict shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Address and load the complete immutable review payload</name>
  <files>cmd/codex_plan_finalize.go, cmd/plan_candidate.go, pkg/colony/planning.go, pkg/colony/planning_test.go, cmd/plan_candidate_binding_200_test.go</files>
  <action>Change buildPlanningRouteCandidate to derive ProposalHash from the canonical semantic proposal with only proposal-, phase-, and task-level CandidateID/CandidateContentHash back-references cleared, then derive candidate ContentHash/ID from the complete immutable review payload defined above, including CreatedAt and ExpiresAt with QueenPlanRecommendation.CandidateID cleared. Populate every derived back-reference only after addressing and require each exact value during validation; no iterative or self-referential hashing is allowed. Finish migrating the existing pkg/colony candidate, acceptance, timeline, and proof-binding fixtures through the same production builders/preimages before enabling strict top-level validation. On artifact load and review, independently reconstruct that stripped proposal/payload, recompute proposal, timeline binding, nested records, candidate digest/ID, validate populated back-references, and recompute the acceptance token before returning any acceptance fields. Preserve D-16: stopped candidates remain non-active and only the full exact command grants acceptance. Do not bind mutable status/acceptance into immutable candidate identity or trust a stored enclosing hash to validate nested content.</action>
  <verify>
    <automated>go test ./pkg/colony -run '^TestPlanning' -count=1 &amp;&amp; go test ./cmd -run '^(TestPlanCandidateSemanticIntegrity200|TestPlanCandidate.*|TestPlanningRoute.*Candidate.*)' -count=1</automated>
  </verify>
  <done>Candidate construction and every review/load boundary bind the same complete canonical semantic payload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Route result to candidate | Worker-provided semantic content is untrusted until canonicalized. |
| persisted candidate to review | Stored hashes and nested objects can be edited together or separately. |
| review payload to acceptance token | Token must identify exactly what the owner reviewed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-30-01 | Tampering | semantic delta | mitigate | Recompute every nested body/hash/ID. |
| T-200-30-02 | Spoofing | candidate identity | mitigate | Complete immutable review-payload digest. |
| T-200-30-03 | Repudiation | owner token | mitigate | Token derived only after canonical candidate validation. |
| T-200-30-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run existing pkg/colony planning contracts plus candidate/delta suites twice and confirm every valid fixture uses production addressing and every single-field mutation is either re-addressed or rejected with old identity.
</verification>

<success_criteria>
- Candidate identity covers every review and authority input.
- Nested hashes are recomputed at construction and load.
- Exact iteration/pass and no-change vocabulary remains stable.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-30-SUMMARY.md when done.
</output>
