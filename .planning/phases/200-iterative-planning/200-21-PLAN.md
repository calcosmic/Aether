---
phase: 200-iterative-planning
plan: 21
type: execute
wave: 11
depends_on: [200-10, 200-13, 200-16, 200-19, 200-20, 200-24]
files_modified:
  - .aether/commands/plan.yaml
  - .claude/commands/ant-plan.md
  - .claude/commands/ant/plan.md
  - .opencode/commands/ant/plan.md
  - cmd/plan_wrapper_cards_test.go
  - cmd/plan_wrapper_ceremony_test.go
  - cmd/platform_doc_hygiene_test.go
autonomous: true
requirements: [SYNTH-02, CEC-03, PLAN-01, PLAN-03, PLAN-04, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Claude and OpenCode expose the four-preset Scout to Route-Setter loop and exact candidate acceptance without owning state."
    - "Canonical planning sources and every managed planning projection stay synchronized without restoring legacy depth or research-approval behavior."
  artifacts:
    - path: ".aether/commands/plan.yaml"
      provides: "Canonical iterative planning wrapper source"
    - path: "cmd/plan_wrapper_cards_test.go"
      provides: "Cross-platform staged planning-card parity"
  key_links:
    - from: ".aether/commands/plan.yaml"
      to: "cmd/codex_plan.go"
      via: "runtime orchestration delegation"
      pattern: "preset|candidate"
    - from: ".aether/commands/plan.yaml"
      to: ".claude/commands/ant/plan.md"
      via: "YAML-backed manual synchronization verified by source-check"
      pattern: "Scout|Route-Setter|candidate"
---

<objective>
Project the iterative planning workflow onto Claude and OpenCode from its canonical source.

Purpose: Deliver platform parity for the restored loop without weakening Go-owned receipts, stage transitions, or separate approval/acceptance.
Output: Canonical plan YAML, manually synchronized managed wrappers, and focused parity tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-UI-SPEC.md
@.planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md
@.aether/commands/plan.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace and synchronize the planning wrapper with staged preset and candidate semantics</name>
  <read_first>
    - .aether/commands/plan.yaml
    - .claude/commands/ant-plan.md
    - .claude/commands/ant/plan.md
    - .opencode/commands/ant/plan.md
    - cmd/codex_plan.go
    - cmd/plan_candidate.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-01 through D-08 and D-13 through D-16)
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Planning Surface)
  </read_first>
  <files>.aether/commands/plan.yaml, .claude/commands/ant-plan.md, .claude/commands/ant/plan.md, .opencode/commands/ant/plan.md</files>
  <action>Rewrite canonical plan YAML around the runtime states: approved-spec preflight, explicit Fast/Balanced/Deep/Exhaustive choice, one Scout stage, optional complete first-pass material-decision batch, one Route-Setter stage, compact card, weakest-gap continuation, post-card later material pause, reasoned stop, full candidate review, and exact candidate acceptance. Remove the old three-knob interview, phase-research approval checkpoint, and any --accept shortcut. Require wrappers to dispatch or resume only the stage authorized by the runtime result and to render structured receipts/cards without editing planning files or state. Manually synchronize both Claude projections and the OpenCode projection from the YAML-backed contract—no wrapper generator is checked in—and verify with `go run ./cmd/aether source-check`.</action>
  <acceptance_criteria>
    - Canonical and generated plan surfaces contain all four exact presets and no default.
    - Scout and Route-Setter are separate runtime-authorized steps with visible compact cards.
    - Phase research approval and bare --accept semantics are absent.
    - Candidate review and exact acceptance remain separate from stopping and spec approval.
    - Managed sources pass the repository source-check before test-contract migration begins.
  </acceptance_criteria>
  <verify>
    <automated>go run ./cmd/aether source-check &amp;&amp; go test ./cmd -run 'TestPlanCommand.*Preset|TestPlanCandidate' -count=1</automated>
  </verify>
  <done>Primary-platform plan wrappers expose the full restored iteration loop while delegating every authority transition to Go.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate planning wrapper and documentation parity tests</name>
  <read_first>
    - cmd/plan_wrapper_cards_test.go
    - cmd/plan_wrapper_ceremony_test.go
    - cmd/platform_doc_hygiene_test.go
    - .aether/commands/plan.yaml
    - .claude/commands/ant-plan.md
    - .claude/commands/ant/plan.md
    - .opencode/commands/ant/plan.md
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-01 through D-08 and D-13 through D-16)
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Planning Surface)
  </read_first>
  <files>cmd/plan_wrapper_cards_test.go, cmd/plan_wrapper_ceremony_test.go, cmd/platform_doc_hygiene_test.go</files>
  <action>Migrate cmd/plan_wrapper_cards_test.go, cmd/plan_wrapper_ceremony_test.go, and the plan/init/discuss portions of cmd/platform_doc_hygiene_test.go to the synchronized staged planning contract. Preserve thin-host, stage-order, structured-result, command-spelling, no-direct-write, and source-parity safeguards. Replace old Decision Moment headings, three-knob depth cards, research approval commands/state, legacy result keys, and post-init or settled-discuss plan shortcuts with assertions for the four-preset choice, separately authorized Scout and Route-Setter stages, autonomous research, discuss → Specification handoff, compact card/timeline fields, non-active candidate review, and exact candidate acceptance. Ensure no test keeps a retired token merely as a positive expectation; explicit negative assertions may name it only when they prove absence.</action>
  <acceptance_criteria>
    - Wrapper tests prove exact four-preset values, stage order, compact-card causality, candidate inactivity, and separate exact acceptance.
    - Still-valid host-safety, managed-source parity, structured-result, and forbidden-state-write assertions remain enforced.
    - No positive assertion requires Decision Moment, depth_proposal_card, research_proposal_card, research_awaiting_approval, plan-research-approve, or a settled-discuss plan shortcut.
    - Platform hygiene preserves /ant-* for Claude/OpenCode and direct aether spellings for Codex.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanWrapperCardsParity|TestPlanWrapperCeremonyContract|TestPlanWrapperStageSkeleton|TestLifecycleCommandDocsPreferRuntimeCLI' -count=1 &amp;&amp; go run ./cmd/aether source-check</automated>
  </verify>
  <done>Planning wrapper regressions enforce the restored staged experience and reject every retired prompt-owned contract.</done>
</task>

</tasks>

<verification>
Run `go run ./cmd/aether source-check`, then go test ./cmd -run 'Test.*Plan.*Wrapper|TestLifecycleCommandDocsPreferRuntimeCLI|TestPlanCandidate' -count=1.
</verification>

<success_criteria>
- Canonical plan YAML has current manually synchronized Claude and OpenCode projections verified by source-check.
- Wrappers expose the locked UX while Go remains the only state and acceptance authority.
- Planning wrappers preserve staged runtime authorization, autonomous research, compact cards, and exact candidate acceptance.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-21-SUMMARY.md when done.
</output>
