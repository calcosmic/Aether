---
phase: 200-iterative-planning
plan: 28
type: execute
wave: 16
depends_on: [200-27]
files_modified:
  - cmd/lifecycle_transaction.go
  - cmd/planning_repository_session.go
  - cmd/planning_timeline.go
  - cmd/planning_stage_receipt.go
  - cmd/planning_mutation_session_200_test.go
autonomous: true
gap_closure: true
requirements: [CEC-03, PLAN-02, PLAN-06]
must_haves:
  truths:
    - "A repository mutation session holds one cross-process root lock from authoritative reads through derivation, expected-baseline comparison, multi-file commit, and rollback."
    - "A transaction supplied with a session cannot silently recapture a newer baseline or deadlock by reacquiring the same root lock."
    - "Timeline append and stage/index receipt publication load their authority only after session acquisition and either preserve every committed update or reject the stale request."
    - "Physical roots resolved by Plan 27 are reused by timeline, stage, and transaction code without a second weaker path policy."
    - "[spec-less edge: CEC-03/ordering-stability] Concurrent timeline writers serialize from authoritative load through commit, preserve deterministic append order, and lose neither card."
  artifacts:
    - path: "cmd/planning_repository_session.go"
      provides: "Shared read-derive-commit repository mutation session"
      exports: ["withPlanningMutationSession"]
    - path: "cmd/lifecycle_transaction.go"
      provides: "Session-aware expected-baseline multi-file commit"
      contains: "lifecycleTransactionConfig"
    - path: "cmd/planning_timeline.go"
      provides: "Session-bound timeline append"
      contains: "appendPlanningTimeline"
    - path: "cmd/planning_mutation_session_200_test.go"
      provides: "Deterministic separate-process lost-update and rollback proof"
      contains: "TestPlanningMutationSession200"
  key_links:
    - from: "cmd/planning_repository_session.go"
      to: "cmd/lifecycle_transaction.go"
      via: "session authority and captured expected digests supplied to commit"
      pattern: "planningMutationSession"
    - from: "cmd/planning_timeline.go"
      to: "cmd/planning_repository_session.go"
      via: "timeline load and append occur inside the root-lock callback"
      pattern: "withPlanningMutationSession"
---

<objective>
Create one shared mutation session and prove it on the append-only planning timeline.

Purpose: File-level atomic writes do not prevent a writer from deriving stale state before it acquires a lock.
Output: Repository session substrate, session-aware lifecycle transactions, and process-level concurrency proof.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-27-SUMMARY.md
@cmd/lifecycle_transaction.go
@cmd/planning_timeline.go
@cmd/planning_stage_receipt.go
@pkg/storage/storage.go
</context>

<interfaces>
Define planningMutationSession as the sole owner of one verified repository-root lock, authoritative reads, exact SHA-256 or absent baselines, and commit/rollback lifetime. Expose withPlanningMutationSession(root, operation, callback), session read/load helpers, and a session-aware lifecycleTransactionConfig field. A callback cannot escape with the lock held.
</interfaces>

<scope_guard>
Modify only the five declared files. Do not migrate other writers yet. Preserve user dirt and stage only the current task's paths.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify session locking, stale baselines, and process races</name>
  <files>cmd/planning_mutation_session_200_test.go</files>
  <behavior>
    - "Two processes released at the same barrier cannot both derive from the same old authoritative state and overwrite one another."
    - "Expected present, expected absent, write, and delete targets are compared beneath the held root lock."
    - "A fault at each commit step rolls back every target while retaining unrelated concurrent data."
    - "Nested transaction use under one session does not reacquire or release the root lock early."
  </behavior>
  <action>Create helper-process tests with explicit ready/release/result pipes, not timing sleeps. Exercise same-file and multi-file writers, expected-absent creation, stale deletion, fault injection, and process termination. Add an append race using two valid distinct timeline cards and assert the final chain contains both in deterministic serialized order with correct hashes. Snapshot outside-tree paths to retain Plan 27 containment coverage.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestPlanningMutationSession200|TestPlanningTimelineConcurrentProcesses200)$' -count=1</automated>
  </verify>
  <done>The fail-first tests reproduce stale overwrite and establish exact serialization and rollback behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared repository mutation session</name>
  <files>cmd/planning_repository_session.go, cmd/lifecycle_transaction.go, cmd/planning_mutation_session_200_test.go</files>
  <action>Implement the session over Plan 27's verified root and lock APIs. Acquire the root lock before every authoritative read; capture each read target as a full digest or explicit absence; keep the lock through derivation, lifecycle transaction apply, rollback, and receipt publication. Make lifecycle transactions consume supplied session baselines instead of reading a new baseline, reject undeclared targets, and retain existing fault hooks. Return deterministic stale-writer errors without mutation. Do not introduce a global snapshot, process-local mutex substitute, or second repository path resolver.</action>
  <verify>
    <automated>go test ./cmd -run '^TestPlanningMutationSession200' -count=1</automated>
  </verify>
  <done>The session provides one reusable, cross-process read-through-commit authority boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Move timeline and stage receipt persistence inside the session</name>
  <files>cmd/planning_timeline.go, cmd/planning_stage_receipt.go, cmd/lifecycle_transaction.go, cmd/planning_mutation_session_200_test.go</files>
  <action>Acquire the session before loading timeline index, cards, stage state, or receipts. Derive append/replay decisions from session-loaded bytes, declare every index/card/stage/receipt target and expected baseline, then commit atomically. Exact replay returns the existing receipt without rewriting; conflicting replay or stale expected state refuses. Remove any independent MkdirAll or weaker canonical-root code from these paths and consume Plan 27's verified physical roots. Preserve D-01 compact cards and D-02 ordered immutable history.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestPlanningMutationSession200|TestPlanningTimelineConcurrentProcesses200|TestPlanningTimeline.*|TestPlanningStage.*Receipt.*)$' -count=1</automated>
  </verify>
  <done>Timeline and stage receipt writers cannot lose updates or escape the verified repository root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| unlocked read to locked commit | Another process may commit between derivation and write. |
| session to lifecycle transaction | A transaction must not replace the session baseline. |
| timeline card to ordered index | Concurrent append must preserve both content and order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-28-01 | Tampering | mutation baseline | mitigate | Root lock acquired before read plus exact expected digests. |
| T-200-28-02 | Denial of service | nested locks | mitigate | Explicit session ownership and no recursive acquisition. |
| T-200-28-03 | Repudiation | timeline ordering | mitigate | Hashed append chain and deterministic process-race proof. |
| T-200-28-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run the new process tests repeatedly and the existing lifecycle transaction/timeline suites. Confirm no same-session path performs an unlocked authoritative read.
</verification>

<success_criteria>
- One root lock spans read, derive, compare, commit, and rollback.
- Timeline and stage publication preserve concurrent updates.
- All session errors are mutation-free and repository-contained.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-28-SUMMARY.md when done.
</output>
