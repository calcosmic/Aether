---
phase: 200-iterative-planning
plan: 46
type: execute
wave: 27
depends_on: [200-42, 200-43, 200-44]
files_modified:
  - pkg/memory/pipeline.go
  - pkg/memory/pipeline_test.go
  - cmd/graph_consolidation_cmds.go
  - cmd/consolidation_lifecycle.go
  - cmd/consolidation_promotion_target_test.go
autonomous: true
gap_closure: true
requirements: [CEC-03, PLAN-06]
must_haves:
  truths:
    - "Existing phase-end consolidation can still promote an eligible instinct into repository-local `.aether/QUEEN.md` after repository-store containment is enabled."
    - "The data store never receives an absolute sibling path, and failed local-Queen writes are reported as failed promotions."
    - "This compatibility repair does not add or change Phase 204 learning policy."
  artifacts:
    - path: "pkg/memory/pipeline.go"
      provides: "Injected Queen-promotion boundary separated from the data store"
    - path: "cmd/consolidation_lifecycle.go"
      provides: "Contained repository-local Queen promotion wiring"
  key_links:
    - from: "cmd/consolidation_lifecycle.go"
      to: "pkg/memory/pipeline.go"
      via: "injected promotion callback"
      pattern: "Queen.*Promot|Promot.*Queen"
    - from: "cmd/consolidation_promotion_target_test.go"
      to: "cmd/colony_prime_context.go"
      via: "same local Queen file is written then read into a worker brief"
      pattern: "QUEEN.md"
---

<objective>
Reconnect the existing local-Queen promotion path without escaping the repository data store.

Purpose: The full gate exposed a containment regression: an absolute `.aether/QUEEN.md` path was sent through a store authorized only for `.aether/data`.
Output: A narrow injected writer boundary and end-to-end reachability proof.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-27-SUMMARY.md
@cmd/queen.go
@cmd/colony_prime_context.go
@pkg/memory/queen.go
</context>

<scope_guard>
Phase 204 owns learning governance. Do not change eligibility, confidence, promotion policy, Queen content, or Hive behavior. Repair only the existing writer boundary, with repository containment and truthful failure accounting.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Separate data-store consolidation from local-Queen writing</name>
  <files>pkg/memory/pipeline.go, pkg/memory/pipeline_test.go</files>
  <behavior>
    - With no injected writer, existing relative-store package behavior remains compatible.
    - With an injected writer, each eligible instinct is passed exactly once and only successful writes enter `QueenPromoted`.
    - Writer failure remains visible and cannot be reported as promotion success.
  </behavior>
  <action>Add the smallest callback/interface to `PipelineConfig` for Queen instinct promotion. Keep observation/instinct files on the original store and preserve package-level default behavior for current callers/tests. The callback receives typed instinct data, not an unchecked absolute path. Do not let memory policy choose repository authority.</action>
  <verify>
    <automated>go test ./pkg/memory -run '^TestPipeline(InjectedQueenPromotion|QueenPromotionFailureAccounting)200$' -count=1</automated>
  </verify>
  <done>The package exposes a typed writer boundary and accurately distinguishes success from failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire consolidation to the repository-local Queen writer</name>
  <files>cmd/graph_consolidation_cmds.go, cmd/consolidation_lifecycle.go, cmd/consolidation_promotion_target_test.go</files>
  <behavior>
    - Real consolidation writes `<repo>/.aether/QUEEN.md`, never `.aether/data/QUEEN.md`.
    - Colony-prime reads the exact promoted entry from that file.
    - A linked/replaced `.aether` boundary or write failure is refused and not counted as promoted.
  </behavior>
  <action>Provide the injected callback from cmd using the existing local-Queen helpers, upgraded to the repository-authorized mutation mechanism already established by Plans 27-28. Do not call `Store.AtomicWrite` with an absolute sibling path and do not fall back to raw `os.WriteFile` after a containment failure. Keep dry-run behavior mutation-free.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(ConsolidationPromotesIntoLocalQueen|PromotedInstinctReachesWorkerPrompt|ConsolidationPhaseEndDryRunDoesNotMutate)$' -count=1</automated>
  </verify>
  <done>The existing promotion reaches worker context through the correct contained file and failures remain honest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| memory package to repository writer | Policy data crosses into a filesystem authority outside `.aether/data`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-46-01 | Elevation of privilege | absolute Queen path | mitigate | Inject a typed, repository-authorized writer; never widen Store absolute-path access. |
| T-200-46-02 | Repudiation | promotion result | mitigate | Append `QueenPromoted` only after confirmed write. |
| T-200-46-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run `go test ./pkg/memory ./cmd -run 'Test(Consolidation|PromotedInstinct)' -count=1` and verify no `.aether/data/QUEEN.md` is created.
</verification>

<success_criteria>
- Repository containment stays strict.
- Existing local Queen promotion and readback work.
- No learning-policy behavior moves into Phase 200.
</success_criteria>

<output>Create `.planning/phases/200-iterative-planning/200-46-SUMMARY.md` when done.</output>
