---
phase: 200-iterative-planning
plan: 06
type: execute
wave: 2
depends_on: [200-01]
files_modified:
  - cmd/planning_timeline.go
  - cmd/planning_timeline_test.go
autonomous: true
requirements: [CEC-03, PLAN-01, PLAN-03, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Every accepted plan retains an ordered, append-only chain of the passes that produced it."
    - "Retrying a completed pass is idempotent; conflicting replay is rejected without mutation."
  artifacts:
    - path: "cmd/planning_timeline.go"
      provides: "Content-addressed iteration-card storage, ordering, digesting, and replay"
  key_links:
    - from: "cmd/planning_timeline.go"
      to: ".aether/data/planning"
      via: "validated atomic artifact writes"
      pattern: "iterations|timeline"
---

<objective>
Persist planning iteration cards as an append-only, replay-safe evidence timeline.

Purpose: Make the visible Scout and Route-Setter loop durable enough to resume after interruption and audit after plan acceptance.
Output: Timeline storage API, digest/binding logic, and crash/replay tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-PATTERNS.md
@pkg/colony/planning.go
@cmd/lifecycle_tx.go
@cmd/codex_plan.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store content-addressed iteration cards atomically</name>
  <read_first>
    - cmd/planning_timeline.go (create if absent)
    - cmd/lifecycle_tx.go
    - cmd/codex_plan.go (existing PlanningLoop and IterationState paths)
    - pkg/colony/planning.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-01, D-02)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Artifact Layout and Persistence)
  </read_first>
  <files>cmd/planning_timeline.go, cmd/planning_timeline_test.go</files>
  <behavior>
    - A validated card writes once under .aether/data/planning/{run}/iterations with a content-derived ID and monotonically increasing pass ordinal.
    - Card plus timeline index commit atomically or neither changes.
    - A card contains before-to-after scores, fresh evidence IDs, weakest gap, semantic delta, authority impacts, and continue or stop disposition.
  </behavior>
  <action>Implement repository-scoped planning artifact paths and serialization for immutable PlanningIterationCard records plus a small ordered timeline index. Derive card IDs and hashes from canonical payloads, require the exact prior card hash for append, and use the existing lifecycle transaction primitives to declare both card and index writes, validate staged content, then commit. Reject path traversal, missing five-dimensional assessments, bad prior hashes, skipped ordinals, and post-write payload mutation. Store detail locators, not terminal formatting, so visual and JSON renderers consume the same model later.</action>
  <acceptance_criteria>
    - Successful append creates one immutable card and an index whose last hash matches it.
    - Injected failure between staging and commit leaves neither artifact changed.
    - Skipped ordinal, wrong prior hash, and path escape fixtures fail without partial files.
    - Card round trip preserves every evidence, delta, gap, and disposition field.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningTimeline.*Append|TestPlanningTimeline.*Atomic|TestPlanningTimeline.*Path' -count=1</automated>
  </verify>
  <done>A completed pass can be durably appended without partial or mutable history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replay timelines and bind immutable accepted history</name>
  <read_first>
    - cmd/planning_timeline.go
    - cmd/planning_timeline_test.go
    - pkg/colony/planning.go
    - cmd/planning_state.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-02, D-16)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Crash and Replay Cases)
  </read_first>
  <files>cmd/planning_timeline.go, cmd/planning_timeline_test.go</files>
  <behavior>
    - Replaying the same append receipt is an exact no-op success.
    - Reusing a receipt ID with different content is a conflict.
    - A timeline bound to an accepted plan remains readable and cannot be pruned or reordered.
  </behavior>
  <action>Add timeline load, chain verification, deterministic digest, and exact replay handling. Return the existing receipt on identical retry, reject payload divergence for the same card or receipt ID, and verify the complete predecessor chain before exposing a timeline binding. Add a protected binding check used by cleanup code: candidate-only timelines may follow the documented retention policy, but any timeline digest referenced by an accepted PlanRevision is immutable and non-prunable. Treat legacy-unbound iteration artifacts as readable evidence only, never as a current accepted chain.</action>
  <acceptance_criteria>
    - Exact append retry returns the original card ID, digest, and write receipt without file changes.
    - Conflicting retry leaves the original chain intact and returns a typed conflict.
    - Tampering with any historical card makes full-chain validation fail.
    - Accepted timeline binding blocks reorder and prune operations; legacy-unbound material remains separately classified.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningTimeline.*Replay|TestPlanningTimeline.*Digest|TestPlanningTimeline.*Bound' -count=1</automated>
  </verify>
  <done>The complete research and revision chain is replay-safe and permanently attached once a plan is accepted.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run TestPlanningTimeline -count=1 with atomic-failure, exact-replay, divergent-replay, tamper, and accepted-binding fixtures.
</verification>

<success_criteria>
- Iteration history is ordered, content-addressed, append-only, and transactionally written.
- Exact retries are idempotent while divergent replays fail closed.
- Accepted plan revisions preserve their full evidence timeline permanently.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-06-SUMMARY.md when done.
</output>
