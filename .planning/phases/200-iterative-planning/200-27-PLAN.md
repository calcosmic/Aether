---
phase: 200-iterative-planning
plan: 27
type: execute
wave: 15
depends_on: [200-26]
files_modified:
  - cmd/root.go
  - pkg/storage/storage.go
  - pkg/storage/lock.go
  - pkg/storage/lock_unix.go
  - pkg/storage/lock_windows.go
  - cmd/root_repository_bootstrap_200_test.go
autonomous: true
gap_closure: true
requirements: [CEC-03, PLAN-06]
must_haves:
  truths:
    - "Root PersistentPreRunE proves the physical repository, .aether, data, and locks containment before storage.NewStore can read, create, or lock anything."
    - "Intermediate symlinks, reparse points, adjacent-prefix roots, and component swaps are refused with zero repository or outside-tree mutation."
    - "Lock files are opened relative to the verified physical root with no-follow semantics and cannot collide merely because two data paths share a basename."
    - "Read-only first status still performs no creation, while an allowed first mutation creates only the verified in-repository data and locks paths."
    - "[spec-less edge: CEC-03/adjacency] A repository root adjacent to, but not an ancestor of, the physical data root is rejected before the first read or create."
    - "[spec-less edge: CEC-03/empty-degenerate] Missing .aether/data may be created only through a verified real .aether component; empty, dot, dot-dot, or symlinked components are refused."
  artifacts:
    - path: "pkg/storage/storage.go"
      provides: "Verified repository-scoped store bootstrap"
      contains: "NewStore"
    - path: "pkg/storage/lock.go"
      provides: "Root-bound collision-resistant file-lock naming"
      contains: "NewFileLocker"
    - path: "cmd/root.go"
      provides: "Containment-first Cobra initialization order"
      contains: "PersistentPreRunE"
    - path: "cmd/root_repository_bootstrap_200_test.go"
      provides: "Actual Cobra subprocess hostile-root and swap proof"
      contains: "TestRepositoryBootstrapContainment200"
  key_links:
    - from: "cmd/root.go"
      to: "pkg/storage/storage.go"
      via: "validated repository/data-root descriptor passed before NewStore initialization"
      pattern: "NewStore"
    - from: "pkg/storage/storage.go"
      to: "pkg/storage/lock.go"
      via: "store and lock roots share one verified containment authority"
      pattern: "NewFileLocker"
  prohibitions:
    - statement: "Repository-scoped planning, specification, and build mutations must not read, create, write, rename, lock, or remove outside the physically contained repository data roots, including through an intermediate symlink or component swap."
      status: unresolved
      reason: "This remains unresolved until actual Cobra subprocess attacks prove refusal leaves both repository and outside trees byte-identical."
---

<objective>
Make repository containment a bootstrap invariant that runs before generic store or lock initialization.

Purpose: A command must not touch an attacker-selected outside path before later lifecycle validation gets a chance to refuse it.
Output: Containment-first root initialization, root-anchored locks, and hostile filesystem subprocess tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-VERIFICATION.md
@cmd/root.go
@pkg/storage/paths.go
@pkg/storage/storage.go
@pkg/storage/lock.go
@pkg/storage/lock_unix.go
@pkg/storage/lock_windows.go
</context>

<scope_guard>
Modify only the six declared files. Do not touch planning state, config, .gsd, Phase 199 evidence, or global tool installations. Preserve starting dirt and use scoped task commits.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define bootstrap refusal through actual Cobra subprocesses</name>
  <files>cmd/root_repository_bootstrap_200_test.go</files>
  <behavior>
    - "A custom data root outside, adjacent to, or lexically inside but physically outside the real repository is rejected before any stat-dependent creation, first-run marker, data file, or lock file."
    - "Existing .aether or data symlinks and directory-to-symlink swaps at controlled barriers fail closed."
    - "Two logical data paths with the same basename acquire distinct root-bound locks."
    - "First read-only status creates nothing; an allowed mutation creates only real in-repository components."
  </behavior>
  <action>Create helper-process tests that execute the real Cobra root with isolated environment and a temporary repository. Snapshot repository and outside trees before and after every refused invocation. Cover intermediate .aether and data links, adjacent-prefix directories, missing parents, empty/dot/dot-dot input, hostile locks path, same-basename lock targets, and a deterministic component swap released after validation but before open. Require stable refusal diagnostics and zero mutation on both sides. Keep platform-specific expectations explicit rather than skipping the attack on supported Unix or Windows hosts.</action>
  <verify>
    <automated>go test ./cmd -run '^TestRepositoryBootstrapContainment200' -count=1</automated>
  </verify>
  <done>The fail-first suite observes no bootstrap API that can satisfy containment, no-follow locking, and zero-mutation refusal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Open the store and locks only through verified roots</name>
  <files>cmd/root.go, pkg/storage/storage.go, pkg/storage/lock.go, pkg/storage/lock_unix.go, pkg/storage/lock_windows.go</files>
  <action>Add a repository-aware storage bootstrap that canonicalizes the existing repository physically, validates every existing .aether/data/locks component without following links or reparse points, rejects paths outside the physical root using filepath.Rel component semantics, and creates missing children only through the verified root. Keep the validated root authority alive through creation/open so a component swap cannot redirect it; use the existing x/sys platform support for root-relative no-follow opens where required. Make lock identity a digest of the complete normalized relative data path, not filepath.Base. Change root PersistentPreRunE to resolve and validate this authority before status probing, storage.NewStore, tracer creation, or first-run output. Retain read-only status no-create behavior and normal in-repository first-run behavior. Do not add a fallback to lexical prefix checks or follow a replaced component.</action>
  <verify>
    <automated>go test ./pkg/storage ./cmd -run '^(TestRepositoryBootstrapContainment200|Test.*Store.*|Test.*FileLock.*)' -count=1</automated>
  </verify>
  <done>Every store and lock used by the Cobra root is physically repository-contained before first I/O, including under hostile component swaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI environment to repository root | User-controlled paths may point outside or through links. |
| verified directory to store creation | A component can be swapped between validation and open. |
| logical data path to lock file | Basename collisions can serialize or redirect unrelated data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-27-01 | Tampering | root bootstrap | mitigate | Physical component walk and root-relative no-follow creation/open. |
| T-200-27-02 | Elevation of privilege | locks directory | mitigate | Verified root authority shared by store and locker. |
| T-200-27-03 | Denial of service | lock basename collision | mitigate | Full relative-path digest and collision tests. |
| T-200-27-SC | Tampering | package supply chain | accept | No package installation occurs; existing x/sys is reused. |
</threat_model>

<verification>
Run storage and Cobra hostile-path tests twice, then inspect git diff to confirm no unrelated path is owned.
</verification>

<success_criteria>
- Containment precedes all store reads, creates, and locks.
- All hostile-path and swap cases refuse without mutation.
- Allowed in-repository and read-only flows retain their contracts.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-27-SUMMARY.md when done.
</output>
