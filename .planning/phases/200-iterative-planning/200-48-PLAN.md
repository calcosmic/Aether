---
phase: 200-iterative-planning
plan: 48
type: execute
wave: 28
depends_on: [200-45, 200-47]
files_modified:
  - cmd/coherent_job_retry.go
  - cmd/codex_build_finalize.go
  - cmd/build_attempt.go
  - cmd/build_attempt_external_test.go
  - cmd/build_finalize_partial_replay_test.go
autonomous: true
gap_closure: true
requirements: [CEC-03, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Validated partial credit creates or reuses exactly one child recovery attempt covering only unfinished tasks."
    - "The first partial finalize and an identical replay both return the same recovery command and unfinished task IDs."
    - "A recovery-start failure cannot be downgraded to a warning that leaves the owner with no recovery job."
    - "Different-packet replay, exhausted retry, and forged completed-task claims fail closed without duplicate attempts or state mutation."
  artifacts:
    - path: "cmd/coherent_job_retry.go"
      provides: "Deterministic partial-credit recovery plan and canonical child start"
    - path: "cmd/codex_build_finalize.go"
      provides: "Honest partial finalize/replay result projection"
  key_links:
    - from: "cmd/codex_build_finalize.go"
      to: "cmd/coherent_job_retry.go"
      via: "partial terminal result must obtain a durable recovery outcome before success is rendered"
      pattern: "reconcilePartialBuildRetry|recovery_job"
    - from: "cmd/coherent_job_retry.go"
      to: "cmd/build_start_transaction.go"
      via: "append-only retry attempt committed through canonical build start"
      pattern: "commitBuildStart"
---

<objective>
Restore the existing partial-finalize recovery contract end to end.

Purpose: Several standalone failures proved a real regression: partial work was credited while retry creation failed, yet finalize returned apparent success without a usable recovery command.
Output: Durable exactly-once recovery attempts and identical replay projection.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-47-SUMMARY.md
@cmd/build_start_transaction.go
@cmd/context.go
</context>

<scope_guard>
This is restoration of the already-tested partial/replay contract required for a green Phase 200 repository gate. Do not redesign Queen-led execution, team sizing, turnaround, or future recovery UX; Phase 201 owns those. Do not weaken D-16 accepted-plan authority for retries.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make partial credit and recovery-attempt creation one honest outcome</name>
  <files>cmd/coherent_job_retry.go, cmd/codex_build_finalize.go, cmd/build_attempt_external_test.go</files>
  <behavior>
    - Four completed tasks in a six-task coherent job create one child attempt for exactly tasks five and six.
    - The child names its parent and never becomes the latest dispatched attempt until actually dispatched under the existing contract.
    - If child creation fails, finalize returns a nonzero error or rolls back the partial transition; it never reports success without recovery.
  </behavior>
  <action>Start from `TestGroupedJobPartialRetryIsAppendOnlyExternal`. Reorder/compose finalize so validated partial credit and the canonical retry outcome cannot diverge. Use the current transaction/session boundaries; do not reintroduce direct SaveJSON writers. Preserve completed evidence, retry budget, and append-only journal linkage.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(GroupedJobPartialRetryIsAppendOnlyExternal|PartialRetryFailureIsNeverSuccessful200)$' -count=1</automated>
  </verify>
  <done>A successful partial result always includes a durable recovery job, and injected retry-start failure is an honest non-success with a consistent tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconstruct identical partial replay from durable evidence</name>
  <files>cmd/build_attempt.go, cmd/codex_build_finalize.go, cmd/build_finalize_partial_replay_test.go</files>
  <behavior>
    - Identical completion packet replay returns `recovery_job=true`, the same child attempt/path, unfinished IDs, and exact redispatch command.
    - Replay writes zero bytes and creates no duplicate spawn/result/attempt entry.
    - A changed completion digest is refused with exact recovery guidance.
  </behavior>
  <action>Persist every field needed to reconstruct recovery on the parent/child attempt record, then make `idempotentExternalPartialFinalizeResult` read that verified evidence. Compare full before/after fingerprints. Keep replay independent of wall-clock time and do not rebuild security-sensitive commands from incomplete prose.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(PartialFinalizeCanBeReplayedWithTheSamePacket|PartialFinalizeReplayWritesNothing|PartialFinalizeRejectsDifferentPacket200)$' -count=1</automated>
  </verify>
  <done>First finalize and replay expose the same recovery contract, with zero replay mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| worker completion to task credit | Self-asserted completed IDs can suppress required work. |
| partial parent to retry child | Lost linkage can duplicate or orphan work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-48-01 | Tampering | partial completed-task claims | mitigate | Validate against the accepted job/task graph before credit. |
| T-200-48-02 | Repudiation | retry linkage/replay | mitigate | Persist parent, digest, unfinished IDs, and command; prove exact-once replay. |
| T-200-48-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run `go test ./cmd -run '^Test(GroupedJobPartialRetry|PartialFinalize|PartialBuild)' -count=1`.
</verification>

<success_criteria>
- Every successful partial finalize gives the owner one executable recovery route.
- Replay is identical and mutation-free.
- No acceptance or containment guard is weakened.
</success_criteria>

<output>Create `.planning/phases/200-iterative-planning/200-48-SUMMARY.md` when done.</output>
