---
phase: 200-iterative-planning
plan: 04
type: execute
wave: 2
depends_on: [200-01]
files_modified:
  - cmd/planning_confidence.go
  - cmd/planning_confidence_test.go
autonomous: true
requirements: [CEC-03, PLAN-02, PLAN-03]
must_haves:
  truths:
    - "Each pass shows five evidence-backed whole-number confidence scores and the weakest material gap."
    - "The loop stops only for an explicit, reproducible policy reason."
  artifacts:
    - path: "cmd/planning_confidence.go"
      provides: "Scoring, gap ranking, target evaluation, diminishing-return, stall, and cap policy"
  key_links:
    - from: "cmd/planning_confidence.go"
      to: "pkg/colony/planning.go"
      via: "DimensionAssessment and stop-reason values"
      pattern: "PlanningDimensionAssessment|Stop"
---

<objective>
Implement the deterministic confidence and stop-policy kernel for iterative planning.

Purpose: Turn pass evidence into explainable five-dimensional movement, material-first gap selection, and honest continuation or stopping.
Output: Pure policy functions with semantic regression tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md
@pkg/colony/planning.go
@cmd/codex_plan_finalize.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate five proposed dimensions and rank gaps</name>
  <read_first>
    - cmd/planning_confidence.go (create if absent)
    - cmd/codex_plan_finalize.go
    - pkg/colony/planning.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-01, D-03, D-04)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Confidence Semantics)
  </read_first>
  <files>cmd/planning_confidence.go, cmd/planning_confidence_test.go</files>
  <behavior>
    - Route-Setter proposes exactly one 0-100 integer before/after assessment for Knowledge, Requirements, Risks, Dependencies, and Effort, with evidence, remaining gap, and rationale for each.
    - Go validates every proposed dimension value and is the sole calculator of the weighted overall.
    - Any changed score cites at least one fresh applicable evidence ID and names the remaining gap.
    - Scores may rise, remain unchanged, or fall when evidence warrants it; restatement alone cannot move them.
  </behavior>
  <action>Implement a pure evaluator for Route-Setter-proposed PlanningDimensionAssessment values. Require exactly five unique assessments, verify each proposed before value equals the persisted prior frontier, bound before/after values to integers 0 through 100, require a rationale and remaining gap, and reject any delta without fresh evidence that the evidence catalogue marks applicable to that exact dimension. The evaluator authorizes validated per-dimension values but never invents values from an unspecified evidence-to-score rubric. Ignore and reject any Route-Setter-supplied overall, then recompute overall confidence in Go with the locked weights Knowledge 25%, Requirements 25%, Risks 20%, Dependencies 15%, Effort 15%, using deterministic whole-number rounding. Require every remaining PlanningGap to name nonempty `evidence_that_would_change`; rank gaps material-first, then by dimension deficit, explicit severity, and stable ID so the same input selects the same weakest gap. Keep authority impacts separate from semantic plan deltas per D-03. Include negative tests for repeated prose, unrelated evidence, missing dimensions, mismatched before values, supplied-overall claims, score inflation, absent evidence-that-would-change, and evidence that legitimately lowers a score.</action>
  <acceptance_criteria>
    - A card missing or duplicating any dimension is rejected.
    - Overall confidence exactly follows the 25/25/20/15/15 formula for boundary fixtures.
    - A changed score without fresh applicable evidence is rejected.
    - A supplied overall is rejected and the accepted overall exactly matches Go's recomputation from the five validated proposals.
    - A gap without evidence_that_would_change is rejected.
    - Material gaps outrank nonmaterial gaps regardless of a higher nonmaterial numeric deficit.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningConfidence.*Score|TestPlanningConfidence.*Gap' -count=1</automated>
  </verify>
  <done>Five-dimensional confidence and weakest-gap selection are deterministic, evidence-gated, and auditable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decide target, diminishing-return, stall, and cap stops</name>
  <read_first>
    - cmd/planning_confidence.go
    - cmd/planning_confidence_test.go
    - cmd/codex_plan_finalize.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-13 through D-16)
    - .planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md (SYN-200-03, SYN-200-05, SYN-200-06)
  </read_first>
  <files>cmd/planning_confidence.go, cmd/planning_confidence_test.go</files>
  <behavior>
    - Target met stops; explicit pass cap stops; reproducible diminishing returns or a repeated unresolved weakest gap can stop below target only when no material gap remains.
    - A material unresolved gap never silently auto-closes below target.
    - Stop evaluation reports the exact reason, residual gaps, and evidence that would change the next decision, not a generic done boolean.
  </behavior>
  <action>Define a closed machine stop decision result for target_met, diminishing_returns, stalled_gap, and pass_cap, plus continue and owner_decision states. Evaluate target before below-target convergence policies and cap at the configured preset. Implement the resolved named `grounded_two_pass_lt2` diminishing policy: after at least three completed passes, require two consecutive grounded Route-Setter passes that each cite fresh evidence, each have absolute Go-derived overall movement below two points, and make no material semantic change. Encode stall as the same weakest gap remaining unresolved across two consecutive Route-Setter passes with no evidence-backed improvement. Permit either below-target stop only when every remaining gap is classified nonmaterial; otherwise produce an owner-decision state. Require the selected gap and every stop variant to carry specific `evidence_that_would_change` derived from validated gap/proposal data, and persist all diagnostic inputs so replay reaches the same outcome.</action>
  <acceptance_criteria>
    - Boundary tests distinguish 1-point from 2-point movement and one repeat from two repeats.
    - Any material remaining gap converts a below-target stop into owner_decision rather than candidate eligibility.
    - Pass cap evaluates using the preset cap exactly, including Fast 4 and Exhaustive 12 fixtures.
    - Identical assessment history always returns the same reason and next action.
    - Continue and every stop variant contain nonempty evidence_that_would_change tied to the selected or residual gaps.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningConfidence.*Stop|TestPlanningConfidence.*Diminishing|TestPlanningConfidence.*Stall' -count=1</automated>
  </verify>
  <done>The loop has an explicit, materiality-safe, replayable policy for every automatic continuation and stop.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run TestPlanningConfidence -count=1; verify target, each convergence reason, material override, score decrease, and deterministic tie cases.
</verification>

<success_criteria>
- All five dimensions are present once, integer-bounded, and evidence-gated.
- The weakest gap is deterministic and material-first.
- Every continue or stop outcome is reproducible from persisted inputs and never hides a material owner decision.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-04-SUMMARY.md when done.
</output>
