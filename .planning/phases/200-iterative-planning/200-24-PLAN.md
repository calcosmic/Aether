---
phase: 200-iterative-planning
plan: 24
type: execute
wave: 10
depends_on: [200-11, 200-12, 200-19]
files_modified:
  - cmd/init_cmd.go
  - cmd/front_door_199_test.go
  - cmd/init_proposals_test.go
  - cmd/front_door_200_test.go
  - .aether/commands/init.yaml
  - .claude/commands/ant-init.md
  - .claude/commands/ant/init.md
  - .opencode/commands/ant/init.md
  - cmd/front_door_init_wrapper_199_test.go
autonomous: true
requirements: [SYNTH-02, CEC-03, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "A new colony closes init by directing the owner to discuss, never straight to planning."
    - "Claude and OpenCode init wrappers present the same init to discuss to draft-specification journey as the shared Go next-action result."
    - "No init surface creates, approves, or accepts a specification or plan on the owner's behalf."
  artifacts:
    - path: "cmd/init_cmd.go"
      provides: "Init closeout backed by the shared authority-aware next-action projection"
    - path: ".aether/commands/init.yaml"
      provides: "Canonical init-to-discuss platform journey"
    - path: "cmd/front_door_init_wrapper_199_test.go"
      provides: "Flat, nested, and OpenCode init-wrapper parity and authority checks"
  key_links:
    - from: "cmd/init_cmd.go"
      to: "cmd/next_action.go"
      via: "shared next-action projection after charter initialization"
      pattern: "NextAction|discuss"
    - from: ".aether/commands/init.yaml"
      to: ".claude/commands/ant/init.md"
      via: "YAML-backed manual synchronization verified by source-check"
      pattern: "discuss|spec"
---

<objective>
Migrate init closeout and every primary-platform init wrapper to the restored discuss-before-spec-before-plan journey.

Purpose: Make the first public lifecycle handoff consume the same Go-owned next-action truth as later surfaces, without mixing this migration into the lifecycle-facts or discuss-wrapper plans.
Output: Updated init runtime closeout, synchronized canonical and managed init wrappers, and focused front-door regressions.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-UI-SPEC.md
@cmd/init_cmd.go
@cmd/next_action.go
@cmd/lifecycle_projection.go
@.aether/commands/init.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route init closeout through the restored lifecycle truth</name>
  <read_first>
    - cmd/init_cmd.go
    - cmd/front_door_199_test.go
    - cmd/init_proposals_test.go
    - cmd/front_door_200_test.go (create if absent)
    - cmd/next_action.go
    - cmd/lifecycle_projection.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-09, D-10, D-16)
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (End-to-End Journey)
  </read_first>
  <files>cmd/init_cmd.go, cmd/front_door_199_test.go, cmd/init_proposals_test.go, cmd/front_door_200_test.go</files>
  <behavior>
    - A successful new-colony init projects discuss as the next lifecycle authority from the shared next-action result.
    - The front-door state table proceeds init to discuss to draft specification to exact specification approval to planning candidate to exact plan acceptance.
    - Direct Codex output uses aether commands and never advertises unsupported slash or dollar aliases.
  </behavior>
  <action>Per D-09, D-10, and D-16, replace any init closeout that jumps straight to plan with the shared next-action projection created in Plan 12. After charter initialization, show discuss as the next lifecycle authority and explain in one line that discuss settles intent before specification review. Migrate the superseded Phase-199 assertions in cmd/front_door_199_test.go and cmd/init_proposals_test.go: preserve still-valid init atomicity, charter, territory, responsive-help, command-grouping, and proposal-ranking safety checks, but replace `aether plan` or `/ant-plan` post-init expectations and `TestInitWrapperClosesAtPlan` with the init → discuss → draft specification contract and explicit no-direct-plan assertions. Add a state-table front-door test covering init to discuss, discuss to draft spec, exact spec approval to plan, iterative stop to candidate review, exact candidate acceptance to build/run, and scoped spec revision back to reconciliation. Assert direct Codex output uses aether commands and does not advertise unsupported slash or dollar aliases.</action>
  <acceptance_criteria>
    - Init closeout directs a new colony to aether discuss.
    - The full front-door state table contains every journey transition in order.
    - No direct Codex string contains /ant- or $ant- command syntax.
    - Existing init state, atomicity, and charter persistence tests still pass.
    - TestInitSuggestedNextMatchesTopProposal and the renamed wrapper-close regression assert discuss/spec handoff rather than post-init planning.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(InitSuggestedNextMatchesTopProposal|InitWrapper.*Discuss|FrontDoorHelp|FrontDoorInit|FrontDoor200)' -count=1</automated>
  </verify>
  <done>The Go init closeout begins the restored discuss and specification journey from the shared lifecycle truth.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize the init wrapper journey on Claude and OpenCode</name>
  <read_first>
    - .aether/commands/init.yaml
    - .claude/commands/ant-init.md
    - .claude/commands/ant/init.md
    - .opencode/commands/ant/init.md
    - cmd/front_door_init_wrapper_199_test.go
    - cmd/init_cmd.go
    - cmd/next_action.go
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Platform Spellings and Journey)
  </read_first>
  <files>.aether/commands/init.yaml, .claude/commands/ant-init.md, .claude/commands/ant/init.md, .opencode/commands/ant/init.md, cmd/front_door_init_wrapper_199_test.go</files>
  <action>Per D-09 and D-10, edit canonical init YAML to describe the runtime-backed journey init to discuss to spec to plan, then manually synchronize that YAML-backed contract into both Claude entrypoints and the OpenCode entrypoint; this repository has no checked-in wrapper generator. Preserve managed-file headers and platform-specific invocation syntax, and use `go run ./cmd/aether source-check` as the authoritative synchronization/drift check. The wrapper may perform its platform-native scoping interaction but must delegate persistence to the Go runtime, render the runtime result, and offer /ant-discuss as the next command. It must not call plan directly, create SPEC.md itself, edit colony state, or claim approval. Migrate cmd/front_door_init_wrapper_199_test.go from post-init `/ant-plan` expectations to `/ant-discuss` followed by draft `/ant-spec`, while preserving flat/nested/OpenCode parity, runtime-authority, and forbidden-write assertions.</action>
  <acceptance_criteria>
    - Canonical init YAML ends in discuss guidance rather than plan guidance.
    - Both Claude projections and the OpenCode projection are manually synchronized with canonical YAML and `go run ./cmd/aether source-check` exits zero.
    - Generated text contains no wrapper-owned state edit or specification approval instruction.
    - Direct Codex runtime remains documented with aether command spelling, not /ant syntax.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestFrontDoorInitWrapperParity|Test.*Init.*Wrapper|TestFrontDoor200' -count=1 &amp;&amp; go run ./cmd/aether source-check</automated>
  </verify>
  <done>All init surfaces lead honestly into intent discussion before specification or planning.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestFrontDoorInitWrapperParity|TestInitSuggestedNextMatchesTopProposal|Test.*Init.*Wrapper|TestFrontDoor200' -count=1 and `go run ./cmd/aether source-check`.
</verification>

<success_criteria>
- Runtime init closeout and all managed init wrappers agree on discuss as the next authority.
- The full front-door journey no longer skips specification review or conflates specification approval with plan acceptance.
- Canonical and managed init sources pass source-check without wrapper-owned state writes.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-24-SUMMARY.md when done.
</output>
