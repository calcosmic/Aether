---
phase: 200-iterative-planning
plan: 35
type: execute
wave: 21
depends_on: [200-34]
files_modified:
  - cmd/build_start_test_helpers_200_test.go
  - cmd/build_attempt_test.go
  - cmd/preflight_phase_198_3_test.go
  - cmd/autopilot_checkpoints_test.go
  - cmd/floor_fix_attempt_test.go
  - cmd/codex_build_test.go
autonomous: true
gap_closure: true
requirements: [PLAN-06]
must_haves:
  truths:
    - "Core build-attempt, preflight, autopilot, floor-fix, and codex-build fixtures create attempts and latest pointers only through the canonical transaction."
    - "The test helper accepts explicit authority, clock, IDs, and conditional effects and cannot bypass repository containment or the mutation session."
    - "Migrated fixtures retain their original behavioral assertions rather than weakening them to implementation-only checks."
  artifacts:
    - path: "cmd/build_start_test_helpers_200_test.go"
      provides: "Transaction-only build-start fixture API"
      contains: "commitTestBuildStart"
    - path: "cmd/build_attempt_test.go"
      provides: "Core attempt tests on canonical start receipts"
      contains: "commitTestBuildStart"
    - path: "cmd/codex_build_test.go"
      provides: "Arbitrary-status attempt fixture built by canonical start plus a production transition"
      contains: "commitTestBuildStart"
  key_links:
    - from: "cmd/build_start_test_helpers_200_test.go"
      to: "cmd/build_start_transaction.go"
      via: "fixture constructs a real canonical request and invokes commitBuildStart"
      pattern: "commitBuildStart"
---

<objective>
Migrate the first bounded fixture set to the canonical build-start transaction.

Purpose: Production callers are safe, but tests must stop depending on partial helpers before those helpers can be removed.
Output: One transaction-only fixture plus five core test-file migrations, including the direct codex-build attempt/pointer seeder.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-33-SUMMARY.md
@.planning/phases/200-iterative-planning/200-34-SUMMARY.md
@cmd/build_attempt_test.go
@cmd/preflight_phase_198_3_test.go
@cmd/autopilot_checkpoints_test.go
@cmd/floor_fix_attempt_test.go
@cmd/codex_build_test.go
</context>

<scope_guard>
Modify only the six declared test files. Do not remove production compatibility helpers until Plan 36 has migrated every remaining fixture. Preserve user dirt and use scoped task commits.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce a transaction-only test start fixture</name>
  <files>cmd/build_start_test_helpers_200_test.go, cmd/build_attempt_test.go</files>
  <behavior>
    - "The helper creates a physically contained temporary repository/store and valid accepted authority before invoking commitBuildStart."
    - "All nondeterministic inputs are explicit and returned receipt/path data is available to existing assertions."
    - "The helper has no direct SaveJSON shortcut for attempt, pointer, state, manifest, or provenance targets."
  </behavior>
  <action>Create commitTestBuildStart around the real Plan 33 request and transaction. Accept options for makeLatest, selected tasks, manifest/checkpoint/claims, parent/check-fix data, execution owner/mode, and reviewer effects. Seed only prerequisite accepted authority through existing canonical fixture builders; do not write any start target directly. Migrate build_attempt_test.go and preserve every assertion about record fields, pointers, transitions, recovery, and replay.</action>
  <verify>
    <automated>go test ./cmd -run '^TestBuildAttempt' -count=1</automated>
  </verify>
  <done>Core build-attempt tests use the real transaction while retaining their full behavior coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate preflight, autopilot, floor-fix, and codex-build seed fixtures</name>
  <files>cmd/preflight_phase_198_3_test.go, cmd/autopilot_checkpoints_test.go, cmd/floor_fix_attempt_test.go, cmd/codex_build_test.go, cmd/build_start_test_helpers_200_test.go</files>
  <action>Replace direct beginBuildAttempt fixture calls with commitTestBuildStart. Map each old argument to explicit request authority/effects and keep plan-only, latest-pointer, selected-task, checkpoint, and failure/recovery assertions intact. Replace codex_build_test.go's seedBuildAttemptRecord direct attempt JSON plus latest-pointer writes with commitTestBuildStart(makeLatest=true) followed by transitionBuildAttempt to the requested arbitrary status; the helper may seed accepted prerequisites but must never write a start target directly. Preserve worker-name and active-attempt behavior. Do not alter expected product behavior merely to fit the new helper. Add a local AST assertion across all six owned files rejecting both legacy helper calls and a reconstructed direct attempt-plus-pointer seed.</action>
  <verify>
    <automated>go test ./cmd -run 'Test(PreflightPhase1983|AutopilotCheckpoint|FloorFixAttempt|EnsureUniqueBuildDispatchNames|BuildAttempt)' -count=1</automated>
  </verify>
  <done>The first bounded fixture set no longer depends on a partial build-start API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test fixture to production transaction | A shortcut could hide a production invariant regression. |
| old test arguments to canonical request | Lost options could silently weaken behavior coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-35-01 | Tampering | test setup | mitigate | Fixture calls only the canonical transaction. |
| T-200-35-02 | Repudiation | migrated assertions | mitigate | Preserve original assertions and add legacy-call AST check. |
| T-200-35-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run the five named fixture families, including the codex-build arbitrary-status seeder, and compare their assertion coverage with the pre-migration tests.
</verification>

<success_criteria>
- Six test files stay within one coherent fixture ownership boundary.
- No migrated test calls a legacy start helper.
- Original behavioral checks remain green.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-35-SUMMARY.md when done.
</output>
