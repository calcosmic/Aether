---
phase: 200-iterative-planning
plan: 13
type: execute
wave: 4
depends_on: [200-08, 200-09]
files_modified:
  - cmd/codex_plan.go
  - cmd/codex_plan_test.go
  - cmd/codex_workflow_cmds.go
  - cmd/codex_workflow_cmds_test.go
  - cmd/phase_research_decision.go
  - cmd/phase_research_decision_test.go
  - cmd/plan_depth_proposal_manifest_test.go
  - cmd/phase_research_dispatch_test.go
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-04, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Unflagged planning requires the owner to choose Fast, Balanced, Deep, or Exhaustive; valid explicit flags bypass the prompt."
    - "Planning begins only from an exact approved specification and dispatches one autonomous Scout stage."
    - "Phase research no longer asks for a routine approval checkpoint."
  artifacts:
    - path: "cmd/codex_plan.go"
      provides: "Preset resolution, evidence priming, and Scout-stage manifest construction"
    - path: "cmd/codex_workflow_cmds.go"
      provides: "Public plan flags and candidate detail routes"
  key_links:
    - from: "cmd/codex_plan.go"
      to: "cmd/planning_stage.go"
      via: "single-stage authorization"
      pattern: "PlanningStage|Manifest"
    - from: "cmd/codex_plan.go"
      to: "cmd/specification.go"
      via: "approved revision binding"
      pattern: "SpecRevision"
---

<objective>
Refit the public plan command to choose a locked preset, prime evidence, and dispatch only the first visible Scout stage.

Purpose: Establish the owner-visible research loop at the command boundary while removing routine phase-research approval and the old ambiguous accept shortcut.
Output: Preset resolver, approved-spec gate, staged manifest, evidence priming, flags, and command tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-UI-SPEC.md
@cmd/codex_plan.go
@cmd/codex_workflow_cmds.go
@cmd/phase_research_decision.go
@cmd/planning_stage.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve the four exact presets without an implicit default</name>
  <read_first>
    - cmd/codex_plan.go
    - cmd/codex_plan_test.go
    - cmd/codex_workflow_cmds.go
    - cmd/plan_depth_proposal_manifest_test.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-13, D-14, D-15)
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Preset Selection)
  </read_first>
  <files>cmd/codex_plan.go, cmd/codex_plan_test.go, cmd/codex_workflow_cmds.go, cmd/codex_workflow_cmds_test.go, cmd/plan_depth_proposal_manifest_test.go</files>
  <behavior>
    - Fast means target 80 and cap 4; Balanced 90 and 6; Deep 95 and 8; Exhaustive 99 and 12.
    - With no valid preset or complete equivalent flags, the command returns preset_required before any Scout dispatch.
    - A named preset or complete valid explicit target/cap pair bypasses selection and produces the same normalized policy.
  </behavior>
  <action>Replace the current depth knobs with a closed PlanningPreset resolver for the four exact values. Accept a named preset flag and retain documented equivalent explicit confidence-target plus pass-cap flags only when both are valid; reject partial, conflicting, out-of-range, and unknown inputs. An unflagged call must produce a renderer-neutral preset selection result with all four choices and no selected default. Persist the chosen normalized preset on the run before dispatch. Remove the old meaning of --accept as permission to accept below target: if retained for compatibility, return an error that points to the exact candidate acceptance command and never changes state. Migrate cmd/plan_depth_proposal_manifest_test.go from its three-knob/recommended/Accept-all contract to exact four-preset options, no recommendation/default, zero unflagged dispatch, explicit flag bypass, and the single-stage manifest shape; preserve its early-return and result/manifest consistency protections under the new fields.</action>
  <acceptance_criteria>
    - Table tests assert exact 80/4, 90/6, 95/8, and 99/12 values.
    - Unflagged invocation performs zero worker dispatches and zero planning-run writes beyond an optional non-authoritative prompt result.
    - Valid preset and valid explicit pair bypass selection; invalid or conflicting flags fail.
    - --accept cannot stop, accept, activate, or otherwise mutate a plan.
    - The migrated TestPlanManifestCarriesDepthProposal suite asserts the four-preset contract and contains no three-knob or Accept-all expectation.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestCodexPlan.*Preset|TestPlanCommand.*Flags|TestPlanCommand.*LegacyAccept|TestPlanManifestCarriesDepthProposal' -count=1</automated>
  </verify>
  <done>Planning depth is an explicit owner choice or valid explicit configuration, never an invisible default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate on approved spec, prime evidence, and emit one Scout manifest</name>
  <read_first>
    - cmd/codex_plan.go
    - cmd/codex_plan_test.go
    - cmd/planning_stage.go
    - cmd/planning_evidence.go
    - cmd/specification.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-01, D-10, D-14)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Evidence Priming and Dispatch)
  </read_first>
  <files>cmd/codex_plan.go, cmd/codex_plan_test.go</files>
  <behavior>
    - Draft, missing, superseded, or hash-mismatched specifications block before dispatch with the exact spec recovery command.
    - A valid start snapshots approved spec, survey, charter, decisions, context, research, Hive, and relevant outcome evidence.
    - The produced manifest authorizes one Scout pass and carries exact spec, base revision, preset, evidence frontier, and receipt contract.
  </behavior>
  <action>At plan start, validate the exact approved specification revision and projection consistency, normalize the selected preset, and collect the evidence catalogue. Persist a planning-run header that binds goal/session, spec revision/hash, base plan revision, preset, and evidence frontier. Ask the stage engine for only scout_ready to scout_running authorization and construct the existing worker dispatch envelope around that single-stage manifest. Remove whole-chain Scout-plus-Route authorization and any prompt-owned state mutation. Include the prior weakest gap on passes after the first and make missing or stale evidence visible in the manifest as gaps rather than silent confidence.</action>
  <acceptance_criteria>
    - Missing or draft spec fixtures have zero worker dispatch and point to aether spec.
    - Scout manifest contains exact run, pass, caste, spec hash, base revision, preset, evidence IDs, input hash, and expected output contract.
    - Manifest contains no Route-Setter output slot, candidate acceptance, or plan activation authority.
    - Existing dispatch safety and path-containment tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestCodexPlan.*ApprovedSpec|TestCodexPlan.*Evidence|TestCodexPlan.*ScoutManifest' -count=1</automated>
  </verify>
  <done>A valid plan invocation visibly starts one evidence-grounded Scout stage against an exact approved specification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make phase research autonomous and attributable</name>
  <read_first>
    - cmd/phase_research_decision.go
    - cmd/phase_research_decision_test.go
    - cmd/phase_research_dispatch_test.go
    - cmd/codex_plan.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-14)
    - .planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md (SYN-200-05 and SYN-200-10)
  </read_first>
  <files>cmd/phase_research_decision.go, cmd/phase_research_decision_test.go, cmd/phase_research_dispatch_test.go, cmd/codex_plan.go, cmd/codex_plan_test.go</files>
  <behavior>
    - Research need is selected automatically from preset, weakest gap, and evidence freshness.
    - Research output is attributed to Scout evidence and cannot self-approve a spec or plan.
    - No normal phase-research approval checkpoint remains.
  </behavior>
  <action>Replace the routine approval decision with a deterministic research policy: the Scout may gather in-scope repository and authoritative external evidence when the selected preset and weakest gap warrant it, and must record source kind, origin, revision, freshness, applicability, and hash. Keep owner interruption only for material product decisions identified by the shared policy. Remove the normal phase-research approval state/renderer path while preserving safe rejection for out-of-scope, secret-bearing, or unavailable sources. Migrate cmd/phase_research_dispatch_test.go: retain bounded research injection, real Scout attribution, dispatch-once/research-failure visibility, source preservation, and re-research safety tests; replace plan-research-approve, flip, approval-gated dispatch, proposal-card, and research_awaiting_approval assertions with automatic preset/gap policy, no-owner-prompt, typed evidence, and fail-loudly behavior. Do not introduce new packages or Phase-204 learning-governance behavior.</action>
  <acceptance_criteria>
    - Research-needed and research-not-needed fixtures resolve without an owner prompt.
    - Every research result becomes a typed evidence ref with Scout attribution.
    - Material product ambiguity still yields owner_decision after a completed Scout pass.
    - No active code path emits the old phase-research approval checkpoint.
    - The migrated TestPlanResearchApproveRecordsDecisions, TestPlanManifestCarriesResearchProposal, TestPlanManifestWarnsWhenResearchBatchUnanswered, and TestResearchDispatchGatedOnApproval coverage is replaced by exact automatic-research tests while still-valid bounded/failure tests remain.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPhaseResearch.*Automatic|TestCodexPlan.*Research|TestCodexPlan.*MaterialDecision|Test(PlanEmitsPhaseResearchDispatchesFromDraft|PhaseResearchDispatchedOncePerPhase|RouteSetterBriefResearchIsBounded|ResearchWorkerFailureWarnsLoudlyWithoutBlocking)' -count=1</automated>
  </verify>
  <done>Routine research runs autonomously inside Scout passes while owner authority remains limited to material product choices.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestCodexPlan|TestPlanCommand|TestPhaseResearch' -count=1 and assert no dispatch occurs before valid preset plus exact approved spec.
</verification>

<success_criteria>
- Preset selection exactly implements Fast, Balanced, Deep, and Exhaustive with no default.
- The first authorized worker action is one evidence-grounded Scout pass.
- Routine phase research is autonomous, attributable, and cannot cross approval or acceptance boundaries.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-13-SUMMARY.md when done.
</output>
