---
phase: 200-iterative-planning
plan: 15
type: execute
wave: 6
depends_on: [200-03, 200-04, 200-05, 200-06, 200-09, 200-13, 200-14]
files_modified:
  - cmd/codex_plan_finalize.go
  - cmd/codex_plan_finalize_test.go
  - cmd/planning_route_stage_200_test.go
autonomous: true
requirements: [CEC-03, PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05]
must_haves:
  truths:
    - "Route-Setter turns the exact Scout evidence into a complete semantic proposal and one compact iteration card."
    - "Route-Setter proposes five evidence-backed dimension assessments; Go validates them, recomputes weighted overall, and derives delta, weakest gap, and continue or stop."
    - "Every stop yields a non-active candidate with evidence that would change the decision and a typed Queen recommendation; no stop directly activates a plan."
  artifacts:
    - path: "cmd/codex_plan_finalize.go"
      provides: "Route-Setter boundary, card derivation, continuation, stop, and candidate persistence"
  key_links:
    - from: "cmd/codex_plan_finalize.go"
      to: "cmd/planning_confidence.go"
      via: "validation of Route-Setter assessments, Go-owned overall recomputation, and stop policy"
      pattern: "PlanningConfidence"
    - from: "cmd/codex_plan_finalize.go"
      to: "cmd/planning_timeline.go"
      via: "append one validated iteration card"
      pattern: "PlanningIterationCard"
---

<objective>
Complete the visible iterative loop by finalizing one Route-Setter pass into a compact evidence-backed card, then continuing or persisting a non-active candidate.

Purpose: Restore the classic research-confidence feedback loop over the current safe finalizer without restoring prompt-owned activation.
Output: Route boundary validation, semantic derivation, timeline append, continuation, decision pause, and stopped candidate creation.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md
@cmd/codex_plan_finalize.go
@cmd/planning_confidence.go
@cmd/planning_delta.go
@cmd/planning_timeline.go
@cmd/planning_stage_receipt.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate one Route-Setter proposal against the exact Scout receipt</name>
  <read_first>
    - cmd/codex_plan_finalize.go
    - cmd/codex_plan_finalize_test.go
    - cmd/planning_route_stage_200_test.go (create if absent)
    - cmd/planning_stage_receipt.go
    - cmd/planning_delta.go
    - pkg/colony/planning.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-01 through D-04)
  </read_first>
  <files>cmd/codex_plan_finalize.go, cmd/codex_plan_finalize_test.go, cmd/planning_route_stage_200_test.go</files>
  <behavior>
    - Route result must match the current one-stage manifest, Route-Setter caste, Scout receipt, run/pass, spec, base revision, and prior candidate hash.
    - The proposal contains complete phase/task/dependency/check/recovery/public-path semantics, five proposed before/after integer dimension assessments, remaining gaps, evidence that would change each gap, and evidence citations.
    - Worker-supplied overall, authority, activation, stop, candidate status, or acceptance fields are rejected; proposed dimension values are retained only for Go validation.
  </behavior>
  <action>Add a typed Route-Setter result boundary to the split finalizer. Validate exact manifest and Scout receipt linkage, then run validatePlanProposalContract and canonical semantic snapshot validation. Require each proposed change and each claimed gap resolution to cite evidence from the current frontier or explicit decision answers. Require Route-Setter to propose exactly five PlanningDimensionAssessment values—before, after, fresh evidence IDs, resolved gap IDs, remaining gap, rationale, and worker receipt—and require every proposed remaining gap to name specific `evidence_that_would_change`. Preserve these as proposals for the Go confidence validator; reject a supplied weighted overall and reject worker-computed stop reasons, candidate status, authority, acceptance, or active-revision mutations. Carry any pass-two-or-later material decision candidates from Scout through the proposal so the full pass can be finalized before the pause. Normalize the proposal before hashing; reject missing stable IDs, silent removals, incomplete checks, stale base, mismatched caste, missing causal fields, and shell-authored state patches with zero writes.</action>
  <acceptance_criteria>
    - Complete linked Route output validates and produces a canonical proposal hash.
    - Wrong Scout receipt, stale base/spec, or missing PLAN-05 field fails without card or receipt.
    - Five Route-Setter-proposed dimension values are accepted only as validation inputs; any supplied weighted overall, stop, acceptance, or activation field is rejected.
    - A remaining gap without evidence_that_would_change is rejected before card creation.
    - Formatting-only proposal differences normalize to the same semantic hash.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningRouteStage.*Validate|TestPlanningRouteStage.*Reject|TestPlanningRouteStage.*Normalize' -count=1</automated>
  </verify>
  <done>Only a complete evidence-linked Route-Setter proposal crosses the worker-to-runtime boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive and append the compact iteration card</name>
  <read_first>
    - cmd/codex_plan_finalize.go
    - cmd/planning_route_stage_200_test.go
    - cmd/planning_confidence.go
    - cmd/planning_delta.go
    - cmd/planning_timeline.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-01 through D-04)
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Iteration Card)
  </read_first>
  <files>cmd/codex_plan_finalize.go, cmd/codex_plan_finalize_test.go, cmd/planning_route_stage_200_test.go</files>
  <behavior>
    - Go validates Route-Setter's five before-to-after whole-number assessments against the persisted prior frontier and fresh applicable evidence, then recomputes weighted overall.
    - Go derives semantic delta, separate authority impacts, weakest remaining gap, evidence that would change the next decision, and the stop-policy diagnostic.
    - One card and one Route receipt append atomically to the ordered timeline; details remain addressable on demand.
  </behavior>
  <action>Use the evidence, confidence, and semantic-delta kernels to calculate the card from the prior accepted or candidate snapshot and validated proposal. Validate exactly five Route-Setter-proposed assessments: each before value matches persisted history, each changed after value cites fresh applicable evidence, and each has concise rationale and remaining gap. Reject any proposed overall and recompute the accepted weighted overall in Go. Include added/modified/removed semantic entries and separate authority impacts. Select one weakest material-first gap, carry its specific `evidence_that_would_change`, and compute the continue/stop decision from persisted preset and history. Require the card's decision and every stop diagnostic to expose `evidence_that_would_change`, even for non-material residual gaps. Append the card, Route artifact, Route StageReceipt, timeline index, and resulting stage transition in one lifecycle transaction. Return the renderer-neutral compact card plus detail locator. Exact replay returns the same IDs and no duplicate append.</action>
  <acceptance_criteria>
    - Card contains all five validated before/after scores, Go-derived overall, changed-dimension evidence, weakest gap, evidence_that_would_change, semantic delta, authority impacts, and disposition.
    - Restated or unrelated evidence cannot change a dimension.
    - Injected transaction failure leaves card, receipt, timeline, and stage unchanged.
    - Exact retry yields one card at that ordinal and the original receipt.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningRouteStage.*Card|TestPlanningRouteStage.*Confidence|TestPlanningRouteStage.*Atomic|TestPlanningRouteStage.*Replay' -count=1</automated>
  </verify>
  <done>Each Scout to Route-Setter pass leaves one durable, compact, evidence-backed before-to-after card.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Continue on the weakest gap or stop at a non-active candidate</name>
  <read_first>
    - cmd/codex_plan_finalize.go
    - cmd/planning_route_stage_200_test.go
    - cmd/planning_confidence.go
    - cmd/planning_decision.go
    - cmd/specification.go
    - cmd/planning_stage.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-06, D-15, D-16)
  </read_first>
  <files>cmd/codex_plan_finalize.go, cmd/codex_plan_finalize_test.go, cmd/planning_route_stage_200_test.go</files>
  <behavior>
    - Continue authorizes the next Scout against the exact weakest gap and current proposal hash.
    - A newly discovered material decision pauses after the full card and before acceptance or next worker dispatch; a contract-affecting answer must pass through successor SPEC approval and scoped reconciliation.
    - Target, diminishing-return, stall, or cap stop persists a candidate only with residual evidence-that-would-change and an authorized typed Queen accept-or-revise recommendation; material below-target gaps require owner decision.
  </behavior>
  <action>For continue, transition to the next scout_ready state and create one Scout manifest carrying the prior card hash, current proposal hash, exact weakest gap, and its `evidence_that_would_change`. If the completed Route pass discovered a new material decision, persist its evidence-first batch only after the card and dispatch nothing until exact resume. Consume the answer disposition: an equivalent non-contract answer may resume the authorized next stage directly; an answer changing promised behavior, scope, risk, or acceptance calls the specification engine to create an immutable successor DRAFT, marks the current candidate/frontier stale for affected scope, and requires exact successor approval plus scoped plan reconciliation before any worker or candidate progression. For permitted stop reasons, persist a content-addressed PlanCandidate bound to spec revision, base plan revision, complete timeline digest, proposal hash, five validated scores, remaining nonmaterial gaps, each gap's `evidence_that_would_change`, a nonempty top-level `evidence_that_would_change` aggregate, and stop reason. Before persistence, invoke the authorized Go-owned Queen recommendation producer: emit `accept` only for target sufficiency and `revise` for diminishing returns, stall, or iteration cap; persist the producer ID, evidence IDs, and evidence-grounded rationale, and never let this advice grant acceptance. Mark the candidate pending review and leave Plan.ActiveRevisionID and current Plan.Phases untouched. If a material gap remains below target, transition to owner_decision instead of creating an eligible candidate.</action>
  <acceptance_criteria>
    - Continue dispatches one Scout with the exact weakest gap and prior card/proposal bindings.
    - Later material decision appears after the card and before any next dispatch.
    - Equivalent non-contract answers resume directly; contract-affecting answers create a successor DRAFT and cannot progress until exact approval plus scoped reconciliation.
    - Every permitted stop creates a pending candidate with nonempty evidence_that_would_change and a typed Queen recommendation/rationale/producer while active plan bytes remain unchanged.
    - Material below-target fixture creates no eligible candidate and requires owner decision.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningRouteStage.*Continue|TestPlanningRouteStage.*Stop|TestPlanningRouteStage.*Candidate|TestPlanningRouteStage.*Material' -count=1</automated>
  </verify>
  <done>The iterative loop either researches its weakest gap again or stops honestly at a reviewable non-active candidate.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestPlanningRouteStage|TestCodexPlanFinalize.*Route' -count=1 and assert multi-pass histories show Scout receipt, Route receipt, one card per pass, and no direct activation.
</verification>

<success_criteria>
- Every Route pass is linked to one exact Scout result and yields a complete semantic proposal.
- Runtime-derived cards expose five confidence movements, evidence, delta, weakest gap, and reasoned disposition.
- Continue, material pause, and all stop reasons preserve authority boundaries; stopped output is only a candidate.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-15-SUMMARY.md when done.
</output>
