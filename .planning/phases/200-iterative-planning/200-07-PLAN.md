---
phase: 200-iterative-planning
plan: 07
type: execute
wave: 2
depends_on: [200-01]
files_modified:
  - cmd/planning_decision.go
  - cmd/planning_decision_test.go
autonomous: true
requirements: [CEC-03, PLAN-04]
must_haves:
  truths:
    - "The owner is interrupted only for material choices that evidence cannot answer."
    - "Decision prompts are batched, evidence-first, scoped, and safely reusable only when meaning is equivalent."
  artifacts:
    - path: "cmd/planning_decision.go"
      provides: "Materiality, batching, decision-card, answer-equivalence, and resume-token policy"
  key_links:
    - from: "cmd/planning_decision.go"
      to: "pkg/colony/planning.go"
      via: "decision evidence and authority scope"
      pattern: "PlanningEvidenceRef|Decision"
    - from: "cmd/planning_decision.go"
      to: "cmd/specification.go"
      via: "typed contract-impact resolution consumed by the specification engine in Plans 14 and 15"
      pattern: "successor_spec_required|direct_resume"
---

<objective>
Implement the decision-boundary policy shared by discuss, specification revision, and iterative planning.

Purpose: Keep routine Scout and Route-Setter work autonomous while pausing precisely for material owner authority.
Output: Deterministic decision discovery, batching, cards, scoped answer reuse, and resume validation.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md
@pkg/colony/planning.go
@cmd/discuss.go
@cmd/codex_plan.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify and batch only material owner decisions</name>
  <read_first>
    - cmd/planning_decision.go (create if absent)
    - cmd/discuss.go
    - cmd/codex_plan.go
    - pkg/colony/planning.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-05, D-06, D-14)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Owner Decision Boundary)
  </read_first>
  <files>cmd/planning_decision.go, cmd/planning_decision_test.go</files>
  <behavior>
    - Material choices concern behavior, authority, risk tolerance, scope, or acceptance meaning and cannot be resolved from admissible evidence.
    - All material choices known after the first Scout pass are emitted as one ordered batch.
    - A later newly discovered material choice pauses only after the complete Scout to Route-Setter pass and persisted iteration card that explain its impact.
  </behavior>
  <action>Implement typed decision candidates and pure classification for the five material domains: behavior, authority, risk tolerance, scope, and acceptance meaning. Reject generic preference prompts and any question answerable by current admissible evidence. Sort and batch all decisions known at the first post-Scout boundary, including evidence citations and affected semantic IDs. Restrict that post-Scout pause to pass one. For pass two and later, retain candidates through Route-Setter and require the completed Scout receipt, Route-Setter receipt, and persisted iteration-card hash before yielding a batch, so a discovery cannot truncate the full pass required by D-06. Routine research depth, worker mechanics, scoring, and plan formatting remain autonomous under D-14.</action>
  <acceptance_criteria>
    - Evidence-answerable and generic preference fixtures produce no owner decision.
    - Multiple first-pass material decisions produce one stable ordered batch.
    - A later material decision without a completed-pass receipt is rejected.
    - A later-pass Scout receipt alone cannot produce owner_decision; both stage receipts and the iteration card are required.
    - Routine Scout or Route-Setter choices never generate a decision card.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningDecision.*Classify|TestPlanningDecision.*Batch|TestPlanningDecision.*Boundary' -count=1</automated>
  </verify>
  <done>Owner interruptions are limited to material authority and occur at complete-pass boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build evidence-first cards and scoped answer reuse</name>
  <read_first>
    - cmd/planning_decision.go
    - cmd/planning_decision_test.go
    - pkg/colony/planning.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-07, D-08)
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Decision Cards and Resume)
  </read_first>
  <files>cmd/planning_decision.go, cmd/planning_decision_test.go</files>
  <behavior>
    - Every card shows the exact decision, why now, relevant evidence, recommendation, consequences, affected scope, and exact resume instruction.
    - An answer is reused only when goal, session, spec revision, plan revision, behavior, scope, risk, and acceptance meaning are equivalent.
    - Stale or altered resume tokens cannot authorize continuation.
    - Resolution classifies an answer as direct_resume only when the promised behavior, scope, risk, and acceptance contract remain equivalent; otherwise it requires a successor specification draft and reconciliation before any stage or candidate progresses.
  </behavior>
  <action>Define the decision-card projection model, canonical equivalence key, and a closed resolution disposition of direct_resume or successor_spec_required. The key must include goal ID, session ID, approved specification revision, base plan revision, normalized decision text, behavior impact, scope impact, risk impact, acceptance impact, and affected semantic IDs. Reuse an answer only on an exact key match; otherwise surface the prior answer as evidence and ask again. After an exact answer, compare its consequences with the approved specification: only an equivalent non-contract answer may return direct_resume; any answer that changes promised behavior, scope, risk treatment, acceptance meaning, or affected specification item returns successor_spec_required with the exact affected stable IDs and normalized revision evidence. Generate a content-addressed resume token binding the batch, frontier receipt or completed-card hash, answers, and disposition. Validate exact tokens idempotently, reject stale or divergent tokens, and return a concise recovery command without mutating planning state. Plans 14 and 15 consume successor_spec_required through the specification engine; this policy helper must never silently rebind a planning run itself.</action>
  <acceptance_criteria>
    - Card fixtures contain every D-07 field and a nonempty evidence citation.
    - Identical semantic decisions reuse the answer; changing any equivalence-key dimension forces revalidation.
    - Exact resume retry succeeds idempotently.
    - Stale, altered, or wrong-session resume tokens fail without advancing a stage.
    - Tests distinguish equivalent non-contract answers that resume directly from contract-affecting answers that require a successor DRAFT with affected IDs.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningDecision.*Card|TestPlanningDecision.*Reuse|TestPlanningDecision.*Resume' -count=1</automated>
  </verify>
  <done>Material choices are actionable, auditable, and resumable without leaking authority across changed meaning or scope.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run TestPlanningDecision -count=1; cover evidence-answerable suppression, first-pass batching, later-pass boundary, equivalence drift, and resume conflict.
</verification>

<success_criteria>
- No routine research or presentation choice interrupts the owner.
- Every material pause presents the exact evidence, recommendation, consequences, and resume path.
- Answer reuse and resume are cryptographically scoped to unchanged meaning and planning frontier.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-07-SUMMARY.md when done.
</output>
