---
phase: 200-iterative-planning
plan: 37
type: execute
wave: 20
depends_on: [200-31, 200-32]
files_modified:
  - pkg/colony/planning.go
  - cmd/planning_stage.go
  - cmd/plan_candidate.go
  - cmd/plan_revision.go
  - cmd/plan_authority.go
  - cmd/plan_candidate_expiry_200_test.go
autonomous: true
gap_closure: true
requirements: [PLAN-04, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "ExpiresAt is strictly after CreatedAt, and a pending candidate is accept-eligible only while now is strictly before ExpiresAt and all bound authority remains current."
    - "Review, acceptance, and build authority derive the same current, expired, stale-before-expiry, or accepted standing from one injected clock and canonical bindings."
    - "Just before expiry can accept; the exact boundary and just after atomically persist expired while leaving the active plan unchanged; accepted replay remains exact only when AcceptedAt was before expiry."
    - "A stale or expired candidate remains read-only reviewable with its full timeline and safe exact regeneration command aether plan --refresh, but never exposes an acceptance token/command."
    - "An accepted plan does not expire after valid timely acceptance, while a forged receipt at or after the deadline is never buildable."
    - "An acceptance refusal returns typed candidate standing, expiry, reason/evidence, state effect, active-plan effect, and recovery facts together with its error so the command layer cannot discard them."
  artifacts:
    - path: "pkg/colony/planning.go"
      provides: "Candidate temporal ordering invariant"
      contains: "ExpiresAt"
    - path: "cmd/plan_candidate.go"
      provides: "Canonical standing assessment and read-only review policy"
      contains: "assessPlanCandidateStanding"
    - path: "cmd/plan_revision.go"
      provides: "Injected-time acceptance boundary and safe replay"
      contains: "acceptPlanCandidate"
    - path: "cmd/plan_authority.go"
      provides: "Accepted-before-expiry build/run validation"
      contains: "validateAcceptedPlanAuthority"
    - path: "cmd/plan_candidate_expiry_200_test.go"
      provides: "Boundary, clock, stale recovery, replay, and authority proof"
      contains: "TestPlanCandidateExpiry200"
  key_links:
    - from: "cmd/plan_revision.go"
      to: "cmd/plan_candidate.go"
      via: "acceptance consumes one injected canonical standing assessment"
      pattern: "assessPlanCandidateStanding"
    - from: "cmd/plan_authority.go"
      to: "pkg/colony/planning.go"
      via: "accepted receipt time is checked against immutable candidate expiry"
      pattern: "ExpiresAt"
  prohibitions:
    - statement: "An expired or stale candidate, or mere clock passage, must not revive, extend, reuse, or authorize its old acceptance token or command after its bound specification, base plan, timeline, proposal, or deadline changes."
      status: unresolved
      reason: "The prohibition closes only when review, acceptance, and execution share the same standing, expiry is persisted atomically at the acceptance boundary, and stale/expired review exposes no authority."
---

<objective>
Enforce candidate expiry and stale-before-expiry recovery at every authority boundary.

Purpose: ExpiresAt is an acceptance deadline, not decorative metadata and not a lifetime limit on a valid timely accepted plan.
Output: Canonical standing assessment, exact boundary enforcement, safe existing-command recovery, and clock-controlled tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-31-SUMMARY.md
@.planning/phases/200-iterative-planning/200-32-SUMMARY.md
@pkg/colony/planning.go
@cmd/planning_stage.go
@cmd/plan_candidate.go
@cmd/plan_revision.go
@cmd/plan_authority.go
</context>

<interfaces>
Define a pure assessPlanCandidateStanding(candidate, currentAuthority, now) result with standing current, expired, stale, or accepted; stable reason/evidence fields; acceptance availability; state effect unchanged; and recovery command using current vocabulary. Define a typed planCandidateRefusalDetails projection with stable JSON names for candidate ID/status, standing, expires_at, why_unavailable, evidence, state_effect, active_plan_effect, and recovery_command. A first at-deadline acceptance refusal reports state_effect=candidate_marked_expired after its commit; read-only stale/expired review reports state_effect=unchanged; active_plan_effect is unchanged in both. The pure assessment function does not persist a clock-derived status.
</interfaces>

<scope_guard>
Modify only the six declared files. Use existing plan --candidate and plan --refresh commands; do not add a new command or wrapper lifecycle. Preserve user dirt and use scoped task commits.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define temporal boundaries and every early-stale reason</name>
  <files>cmd/plan_candidate_expiry_200_test.go</files>
  <behavior>
    - "ExpiresAt before/equal CreatedAt is invalid."
    - "Acceptance succeeds one tick before expiry and at/after the boundary atomically changes only candidate status to expired."
    - "Spec revision, base revision, proposal, timeline, candidate body, and stage divergence each produce stale before expiry."
    - "Expired/stale review is read-only and returns only aether plan --refresh as recovery."
    - "Accepted-before-expiry remains valid later; forged late acceptance and non-monotonic clock inputs fail."
    - "At-deadline acceptance returns non-empty typed refusal details with the non-nil error after the atomic expiry transition."
  </behavior>
  <action>Per D-16, create an injected-clock table at CreatedAt, just before, exact expiry, and just after. Add each early-stale binding independently, exact accepted replay before/after wall-clock expiry, forged AcceptedAt at/after deadline, invalid CreatedAt/ExpiresAt ordering, and multiple-clock-read canaries. Exercise real review, acceptance, plan-authority, build-authority, and stage paths. Snapshot all repository bytes around read-only review, stale refusal, and build refusal; at exact/after acceptance require one session-atomic pending_review-to-expired candidate/stage transition with the active plan and every other path unchanged.</action>
  <verify>
    <automated>go test ./cmd -run '^TestPlanCandidateExpiry200' -count=1</automated>
  </verify>
  <done>The fail-first matrix defines one deadline and one safe recovery for every stale/expired authority path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce one canonical standing in review and acceptance</name>
  <files>pkg/colony/planning.go, cmd/planning_stage.go, cmd/plan_candidate.go, cmd/plan_revision.go, cmd/plan_candidate_expiry_200_test.go</files>
  <action>Require ExpiresAt.After(CreatedAt). Implement assessPlanCandidateStanding from canonical Plan 29/30/31 bindings and one supplied UTC time. Let plan --candidate load pending_review and persisted expired stopped candidates for read-only inspection, derive time expiry without writing, and omit acceptance request/token/command unless standing is current. Return exact State: unchanged and aether plan --refresh recovery for expired or early-stale review; that refresh already commits cleanup/regeneration under Plan 32's session. Route CLI acceptance through one package clock seam used by runPlanCandidateCommand: production returns time.Now().UTC(), tests restore it after injecting an exact boundary, and its one value is passed as opts.AcceptedAt so acceptPlanCandidate performs no fallback clock read. Assess inside the repository session with that AcceptedAt. On exact-boundary/after expiry, atomically change the canonical candidate and matching stage/index authority to expired while leaving the active plan unchanged, then build planCandidateRefusalDetails from the committed standing with state_effect=candidate_marked_expired and active_plan_effect=unchanged and return it through runPlanCandidateCommand as a non-nil typed candidate/refusal result together with the non-nil refusal error; never collapse this case to nil facts. Exact replay of an already accepted receipt is checked first. Early-stale or structurally invalid candidates also return safe typed refusal facts with state_effect=unchanged when candidate identity is known and refuse with zero mutation. Keep D-16 explicit owner acceptance and existing human-facing command vocabulary.</action>
  <verify>
    <automated>go test ./cmd ./pkg/colony -run '^(TestPlanCandidateExpiry200|TestPlanCandidate.*)' -count=1</automated>
  </verify>
  <done>Review and acceptance agree on one boundary, expiry is durably recorded at acceptance, and no stale/expired token can activate a plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate timely acceptance again at build and run authority</name>
  <files>cmd/plan_authority.go, cmd/plan_candidate.go, cmd/plan_candidate_expiry_200_test.go</files>
  <action>Per D-12 and D-16, in accepted replay and plan authority, require the receipt AcceptedAt to be strictly before immutable ExpiresAt and all canonical candidate/spec/base/timeline bindings to match. Build and run consume that same validated accepted authority; a pending candidate whose deadline passed is non-buildable even before its status is normalized by an acceptance attempt. Do not later expire a valid timely accepted plan merely because the current clock passed its former acceptance deadline, and do not downgrade a forged late modern receipt to legacy compatibility.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestPlanCandidateExpiry200|Test.*PlanAuthority.*|Test.*Build.*Authority.*|Test.*Run.*Authority.*)' -count=1</automated>
  </verify>
  <done>Build and run accept only a timely exact receipt while preserving valid already-accepted plans after the deadline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| clock to acceptance | Multiple or implicit clock reads can disagree at the deadline. |
| stale candidate to owner action | Old exact tokens may appear actionable. |
| accepted candidate to execution | Deadline must validate acceptance time without expiring the active plan later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-37-01 | Elevation of privilege | expired acceptance | mitigate | Strict injected boundary inside repository session. |
| T-200-37-02 | Spoofing | stale authority | mitigate | Canonical binding comparison and state-specific reason. |
| T-200-37-03 | Repudiation | accepted timing | mitigate | Receipt AcceptedAt must precede immutable ExpiresAt. |
| T-200-37-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run the entire clock/stale matrix and existing candidate/authority suites with no real-time sleeps.
</verification>

<success_criteria>
- Exact-boundary semantics are enforced.
- Early-stale and expired candidates stay reviewable but non-accepting.
- Timely accepted authority remains valid and forged late authority fails.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-37-SUMMARY.md when done.
</output>
