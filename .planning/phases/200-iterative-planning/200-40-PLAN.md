---
phase: 200-iterative-planning
plan: 40
type: execute
wave: 35
depends_on: [200-55]
files_modified:
  - .planning/phases/200-iterative-planning/200-GATE-RECEIPT.md
autonomous: true
gap_closure: true
requirements: [SYNTH-02, CEC-03, PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Phase 199 focused, Phase 200 targeted, Classic, public journey, source-check, full, and race commands run in the declared order and all exit zero."
    - "The existing 200-GATE-RECEIPT.md records exact commands, start/finish, zero exits, output SHA-256, tested revision/tree, and scoped worktree state without a waiver or expected failure."
    - "The receipt distinguishes automated verification from the two explicit human UAT checks and makes no claim about later standard GSD phase completion."
    - "STATE.md, REQUIREMENTS.md, config.json, .gsd, Phase 199 UAT/PATTERNS, and unrelated user dirt remain untouched by this plan."
  artifacts:
    - path: ".planning/phases/200-iterative-planning/200-GATE-RECEIPT.md"
      provides: "Final all-green implementation gate evidence required by executed Plan 23"
      contains: "Final Gate Commands"
  key_links:
    - from: ".planning/phases/200-iterative-planning/200-GATE-RECEIPT.md"
      to: "cmd/planning_adversarial_200_test.go"
      via: "exact targeted Phase 200 command and output digest"
      pattern: "TestPlanningAdversarial200"
    - from: ".planning/phases/200-iterative-planning/200-GATE-RECEIPT.md"
      to: "cmd/source_check.go"
      via: "exact go run ./cmd/aether source-check result before full/race"
      pattern: "go run ./cmd/aether source-check"
---

<objective>
Run the final focused, source, full, and race gates and update the existing Phase 200 gate receipt.

Purpose: Executed Plan 23 already established the receipt as implementation evidence; it now must report a genuinely green repository after the repairs.
Output: One refreshed all-green 200-GATE-RECEIPT.md, followed by normal GSD verification and phase completion.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-GATE-RECEIPT.md
@.planning/phases/200-iterative-planning/200-39-SUMMARY.md
@.planning/phases/200-iterative-planning/200-55-SUMMARY.md
@cmd/planning_adversarial_200_test.go
@cmd/planning_real_repo_200_test.go
@cmd/planning_public_paths_200_test.go
@cmd/classic_contract_test.go
@cmd/phase199_gate_receipt_test.go
</context>

<scope_guard>
Modify only 200-GATE-RECEIPT.md. Record initial git status, run read-only gates, and preserve every other tracked/untracked/staged path exactly. Use the normal scoped plan commit and summary flow; standard GSD verifier decides completion.
</scope_guard>

<tasks>

<task type="auto">
  <name>Task 1: Run the exact final gate sequence without waivers</name>
  <files>.planning/phases/200-iterative-planning/200-GATE-RECEIPT.md</files>
  <action>Capture the current Aether HEAD/tree and git status, then run these exact commands from the repository root in this order, retaining complete stdout/stderr, UTC start/finish, exit code, and SHA-256 for each: (1) go test ./cmd -run '^(TestCurrentVocabulary199($|/)|TestPhase199GateReceiptSchema$|TestPhase199GateReceipt$)' -count=1; (2) go test ./cmd -run '^Test(RepositoryBootstrapContainment200|PlanningMutationSession200|PlanningTimelineConcurrentProcesses200|SpecificationIntegrity200|PlanCandidateSemanticIntegrity200|PlanCandidateAcceptanceIntegrity200|PlanCandidateAcceptanceConcurrentProcesses200|PlanningWriterCoverage200|PlanningWriterConcurrentProcesses200|PlanningNumericBoundaries200|BuildStartTransaction200|BuildStartCallers200|BuildStartConcurrentProcesses200|BuildStartLegacyHelpersRetired200|PlanCandidateExpiry200|PlanningExpiryPresentation200|PlanningAdversarial200|PlanningGapEdgeAccounting200)$' -count=1; (3) go test ./cmd -run '^TestClassicContract' -count=1; (4) go test ./cmd -run '^(TestPlanningPublicPaths200|TestPlanningRealRepo200)$' -count=1; (5) go run ./cmd/aether source-check; (6) go test ./... -count=1; (7) go test ./... -race -count=1. Stop on the first nonzero result and fix the owning earlier-plan implementation/test rather than editing the receipt to classify or waive it. Do not update the receipt until all seven pass on the same implementation state.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestCurrentVocabulary199($|/)|TestPhase199GateReceiptSchema$|TestPhase199GateReceipt$|TestPlanningAdversarial200$|TestPlanningGapEdgeAccounting200$|TestPlanningPublicPaths200$|TestPlanningRealRepo200$)' -count=1 &amp;&amp; go run ./cmd/aether source-check</automated>
  </verify>
  <done>All seven commands have zero exits on one implementation state, with raw output and digests retained for the receipt.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the inherited red baseline with exact all-green evidence</name>
  <files>.planning/phases/200-iterative-planning/200-GATE-RECEIPT.md</files>
  <action>Update the existing receipt in place. Preserve its useful artifact-digest and retry-history context, but mark all prior expected-baseline failures as superseded. Record the exact seven-command order and unmodified command strings, tested revision/tree, scoped git status, UTC timestamps, durations, zero exits, output digests, and concise pass counts. State explicitly that no waiver/classification was used and that the receipt is implementation gate evidence required by Plan 23, not product acceptance or phase-completion proof. Recheck the ending git status and document that only this receipt changed during the plan.</action>
  <verify>
    <automated>awk 'BEGIN{n=0} /^\| (Phase 199 focused|Phase 200 targeted|Classic contract|Public planning journey|Source parity|Repository-wide|Repository-wide race) \|/{n++} END{exit n==7 ? 0 : 1}' .planning/phases/200-iterative-planning/200-GATE-RECEIPT.md &amp;&amp; ! rg -n 'EXPECTED BASELINE FAIL|\|[[:space:]]*WAIVED[[:space:]]*\|' .planning/phases/200-iterative-planning/200-GATE-RECEIPT.md</automated>
  </verify>
  <done>The existing receipt contains seven exact all-green rows and no failure waiver, ready for the standard GSD verifier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| command process to receipt | Summaries can misstate exits, output, or tested tree. |
| implementation gate to product acceptance | Automated success cannot replace human experience checks. |
| gate run to user worktree | Test execution must not sweep unrelated dirt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-40-01 | Repudiation | gate receipt | mitigate | Exact command/order/time/exit/output/tree records. |
| T-200-40-02 | Tampering | failed gate classification | mitigate | Stop on nonzero; forbid waiver language. |
| T-200-40-03 | Tampering | user worktree | mitigate | Before/after status and one-file ownership. |
| T-200-40-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
<automated>
1. go test ./cmd -run '^(TestCurrentVocabulary199($|/)|TestPhase199GateReceiptSchema$|TestPhase199GateReceipt$)' -count=1
2. go test ./cmd -run '^Test(RepositoryBootstrapContainment200|PlanningMutationSession200|PlanningTimelineConcurrentProcesses200|SpecificationIntegrity200|PlanCandidateSemanticIntegrity200|PlanCandidateAcceptanceIntegrity200|PlanCandidateAcceptanceConcurrentProcesses200|PlanningWriterCoverage200|PlanningWriterConcurrentProcesses200|PlanningNumericBoundaries200|BuildStartTransaction200|BuildStartCallers200|BuildStartConcurrentProcesses200|BuildStartLegacyHelpersRetired200|PlanCandidateExpiry200|PlanningExpiryPresentation200|PlanningAdversarial200|PlanningGapEdgeAccounting200)$' -count=1
3. go test ./cmd -run '^TestClassicContract' -count=1
4. go test ./cmd -run '^(TestPlanningPublicPaths200|TestPlanningRealRepo200)$' -count=1
5. go run ./cmd/aether source-check
6. go test ./... -count=1
7. go test ./... -race -count=1
</automated>
<human-check>
1. In disposable real repositories, use both Claude and OpenCode managed surfaces for init, discuss, specification review/approval, a two-pass Balanced plan, candidate review, and exact acceptance. Confirm Scout and Route-Setter visibly alternate, every card explains evidence/scores/weakest gap/delta/stop cause, material decisions alone interrupt, and build/run appears only after acceptance.
2. Review specification, decision, iteration, candidate, acceptance, impact, and recovery screens at narrow, normal, and wide terminal widths. Confirm authority is not clipped or ambiguous and expiry plus stale-before-expiry recovery are immediately understandable without JSON.
</human-check>
</verification>

<success_criteria>
- All seven automated gates are truly green on one tested tree.
- The existing receipt records exact auditable evidence and no waiver.
- Both original human UAT checks remain explicit for the standard verifier.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-40-SUMMARY.md when done. Then run normal GSD phase verification/completion; do not create a successor phase or custom closeout mechanism.
</output>
