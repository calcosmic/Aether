---
phase: 200-iterative-planning
plan: 10
type: execute
wave: 4
depends_on: [200-08]
files_modified:
  - cmd/spec_cmd.go
  - cmd/spec_cmd_test.go
  - cmd/root.go
  - cmd/root_test.go
autonomous: true
requirements: [PLAN-05]
must_haves:
  truths:
    - "The owner can inspect, revise, and exactly approve the canonical specification through a real public command."
    - "Normal lifecycle help includes discuss and spec without pretending Codex has slash commands."
  artifacts:
    - path: "cmd/spec_cmd.go"
      provides: "Public aether spec Cobra command and structured results"
  key_links:
    - from: "cmd/spec_cmd.go"
      to: "cmd/specification.go"
      via: "draft/revise/approve engine calls"
      pattern: "Specification|SpecRevision"
    - from: "cmd/root.go"
      to: "cmd/spec_cmd.go"
      via: "registered normal command"
      pattern: "newSpec|specCmd"
---

<objective>
Expose the specification lifecycle as the real public aether spec command and normal lifecycle step.

Purpose: Give users a supported surface for exact specification review and approval before planning.
Output: Cobra command, terminal/JSON-neutral result model, help registration, and command-contract tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-UI-SPEC.md
@cmd/specification.go
@cmd/spec_projection.go
@cmd/root.go
@cmd/codex_workflow_cmds.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add inspect, revise, approve, and repair operations</name>
  <read_first>
    - cmd/spec_cmd.go (create if absent)
    - cmd/specification.go
    - cmd/spec_projection.go
    - cmd/codex_workflow_cmds.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-09 through D-12)
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Spec Surface)
  </read_first>
  <files>cmd/spec_cmd.go, cmd/spec_cmd_test.go</files>
  <behavior>
    - aether spec with no mutation flag reports current revision, status, every typed readable body section, delta, affected scope, and exact next action.
    - Explicit add, modify, remove, revise, approve, and repair operations call the Go specification engine.
    - Approval requires exact revision, hash, and approval token and returns a structured receipt in both output modes.
  </behavior>
  <action>Create a public Cobra command whose options map one-to-one to inspect, add, modify, remove, approve, and projection-repair operations. Require the typed section kind plus stable item ID for modify/remove, source text or file input for add/modify, predecessor revision for mutations, and exact revision plus hash plus explicit approval token for approval. Reject mutually exclusive operations before state access. Return a renderer-neutral result containing before/after revision, status, scope, typed stable-ID arrays for outcome, included behavior, exclusions, binding decisions, requirements, owner-checkable acceptance, negative expectations, recovery expectations, and affected public paths, plus classified delta, affected downstream IDs, receipt, and next action. Never collapse the body into generic `items`, accept a generic yes, or infer approval from editor completion. Keep direct Codex syntax honest as aether spec; wrapper aliases are delivered later.</action>
  <acceptance_criteria>
    - Command tests cover inspect, add, modify, remove, approve, repair, invalid flag combinations, stale revision, and exact replay.
    - Structured results distinguish draft creation, revision creation, approval, and projection repair.
    - Inspect results round-trip every readable body category and stable ID for whole-goal and feature-scoped specifications.
    - No command path activates or accepts a plan.
    - aether spec --help documents the exact approval inputs.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestSpecCommand' -count=1</automated>
  </verify>
  <done>The complete specification lifecycle is accessible through one truthful public runtime command.</done>
</task>

<task type="auto">
  <name>Task 2: Register spec and the restored lifecycle journey in root help</name>
  <read_first>
    - cmd/root.go
    - cmd/root_test.go
    - cmd/spec_cmd.go
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Journey and Vocabulary)
    - .aether/commands/classic-command-parity.json
  </read_first>
  <files>cmd/root.go, cmd/root_test.go</files>
  <action>Register spec as a normal public command and update the root lifecycle journey to show init, discuss, spec, plan, build or run in that order. Preserve status, update, and other existing commands. Describe discuss as intent clarification, spec as draft/review/approval, and plan as evidence-driven candidate generation; do not imply that spec approval accepts a plan. In Codex-facing help show aether spellings, not unsupported slash-command or dollar-command syntax. Add root-help assertions for command presence, ordering, and separation of authorities.</action>
  <acceptance_criteria>
    - aether --help lists discuss, spec, and plan as normal commands.
    - Lifecycle journey order is init then discuss then spec then plan then build/run.
    - Help contains no claim that aether spec accepts or activates a plan.
    - Existing public command registrations remain present.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestRoot.*Help|TestSpecCommand.*Registered' -count=1</automated>
  </verify>
  <done>The public CLI advertises the restored intent-to-spec-to-plan journey accurately.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestSpecCommand|TestRoot.*Help' -count=1 and inspect aether spec --help plus root help in a temporary repository.
</verification>

<success_criteria>
- aether spec is registered, testable, and covers inspect/revise/approve/repair.
- Exact approval inputs are required at the CLI boundary.
- Root help presents discuss, spec, and plan as separate lifecycle authorities.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-10-SUMMARY.md when done.
</output>
