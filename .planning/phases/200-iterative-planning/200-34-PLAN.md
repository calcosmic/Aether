---
phase: 200-iterative-planning
plan: 34
type: execute
wave: 21
depends_on: [200-32, 200-33]
files_modified:
  - cmd/codex_build.go
  - cmd/forced_reviewer_waiver.go
  - cmd/codex_build_finalize.go
  - cmd/check_fix_attempt.go
  - cmd/coherent_job_retry.go
  - cmd/build_start_callers_200_test.go
autonomous: true
gap_closure: true
requirements: [PLAN-06]
must_haves:
  truths:
    - "Direct, plan-only, Queen-led, external-finalize, automatic check-fix, and coherent child-retry starts all call the same canonical build-start transaction."
    - "Each caller supplies its exact conditional effects and performs worker dispatch, completion processing, or retry work only after the transaction returns durable authority."
    - "No production caller directly creates an attempt/pointer, clears stale artifacts or reviewer windows, checkpoints state, promotes execution state, or attaches parent/check-fix provenance outside the transaction."
    - "[spec-less edge: PLAN-06/concurrency-effect-ordering] When an accepted living-plan revision and a public build start race, repository sessions serialize them so the start either binds the current complete spec/candidate/timeline/revision/task authority or refuses before runnable promotion; mixed old/new authority never dispatches."
  artifacts:
    - path: "cmd/codex_build.go"
      provides: "Canonical direct, plan-only, and Queen-led start callers"
      contains: "commitBuildStart"
    - path: "cmd/codex_build_finalize.go"
      provides: "Canonical bound/unbound external-finalize start handling"
      contains: "commitBuildStart"
    - path: "cmd/check_fix_attempt.go"
      provides: "Atomic automatic check-fix start"
      contains: "commitBuildStart"
    - path: "cmd/coherent_job_retry.go"
      provides: "Atomic child retry and parent provenance"
      contains: "commitBuildStart"
    - path: "cmd/build_start_callers_200_test.go"
      provides: "Production caller AST and process-race proof"
      contains: "TestBuildStartCallers200"
  key_links:
    - from: "cmd/codex_build.go"
      to: "cmd/build_start_transaction.go"
      via: "each direct/plan-only/Queen path constructs and commits one request"
      pattern: "commitBuildStart"
    - from: "cmd/coherent_job_retry.go"
      to: "cmd/build_start_transaction.go"
      via: "child attempt, parent link, and state change share one transaction"
      pattern: "commitBuildStart"
---

<objective>
Migrate every production build-start caller to the atomic authority-bound transaction.

Purpose: The new substrate closes no gap until all real callers stop assembling partial start state themselves.
Output: Six caller families migrated, dispatch ordered after commit, and AST/process-race enforcement.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-31-SUMMARY.md
@.planning/phases/200-iterative-planning/200-33-SUMMARY.md
@cmd/codex_build.go
@cmd/codex_build_finalize.go
@cmd/check_fix_attempt.go
@cmd/coherent_job_retry.go
@cmd/forced_reviewer_waiver.go
</context>

<scope_guard>
Modify only the six declared files. Legacy test-facing helpers remain until Plans 35–36 migrate fixtures and retire them. Preserve user dirt and use scoped task commits.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate direct, plan-only, Queen-led, and reviewer effects</name>
  <files>cmd/codex_build.go, cmd/forced_reviewer_waiver.go, cmd/build_start_callers_200_test.go</files>
  <behavior>
    - "Plan-only and Queen-led share one prepared start and do not duplicate attempts."
    - "Direct starts atomically include checkpoint, attempt/pointer, manifest binding, state promotion, stale cleanup, and reviewer effects."
    - "An active orchestrator-boundary guidance path creates no start."
    - "A losing plan-revision/build race dispatches no worker."
  </behavior>
  <action>Replace both production beginBuildAttempt call sites in codex_build.go with complete buildStartRequest construction and commitBuildStart. Fold clearPhaseDispatchWindow and its matching pending-decision/window updates into optional transaction effects; remove pre-transaction calls. Include cleanupStaleBuildAttemptArtifacts targets, checkpoint, manifest, state, pointer, and selected-task authority where applicable. Preserve plan-only versus real dispatch semantics and ensure Queen-led reuse does not begin twice. Add helper-process races against accepted-plan revision and assert the tagged concurrency-effect-ordering truth.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestBuildStartCallers200|TestBuildStartConcurrentProcesses200|Test.*PlanOnly.*Build.*|Test.*Queen.*Build.*|TestForcedReviewer.*)' -count=1</automated>
  </verify>
  <done>All public direct/plan-only/Queen starts and reviewer effects commit atomically before dispatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate external finalize, check-fix, and coherent retry</name>
  <files>cmd/codex_build_finalize.go, cmd/check_fix_attempt.go, cmd/coherent_job_retry.go, cmd/build_start_callers_200_test.go</files>
  <action>For external finalize, preserve an already-bound attempt and use commitBuildStart only for the unbound path, with checkpoint/completion/manifest inputs included atomically. For automatic check-fix, commit attempt/pointer/check-fix provenance before dispatch and decline to run when commit fails. For coherent retry, commit the makeLatest=false child attempt, parent ID/job link, selected unfinished tasks, and any lifecycle state change together; never call beginChildBuildAttempt then attach metadata afterward. Preserve exact existing recovery and plan-only semantics.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestBuildStartCallers200|TestBuildStartConcurrentProcesses200|Test.*External.*Finalize.*|Test.*CheckFixAttempt.*|Test.*CoherentJobRetry.*)' -count=1</automated>
  </verify>
  <done>Every auxiliary production start path uses the same atomic request/receipt contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce complete production caller coverage and dispatch ordering</name>
  <files>cmd/build_start_callers_200_test.go, cmd/codex_build.go, cmd/codex_build_finalize.go, cmd/check_fix_attempt.go, cmd/coherent_job_retry.go</files>
  <action>Add an AST ratchet that requires commitBuildStart at the exact six caller families, rejects production references to beginBuildAttempt, beginBuildAttemptRecord, beginChildBuildAttempt, and attachBuildAttemptParentLink, and detects direct start-related SaveJSON/UpdateJSONAtomically/remove calls. Use injected dispatch callbacks or fake invokers to prove no worker starts before durable receipt and no worker starts after any stale/fault refusal. Cover bound external replay and plan-only Queen reuse as explicit non-duplication cases.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestBuildStartCallers200|TestBuildStartConcurrentProcesses200)$' -count=1</automated>
  </verify>
  <done>The production call graph contains no partial build-start writer and all dispatch follows durable authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller-local state to canonical transaction | Optional effects can be omitted or written early. |
| transaction result to dispatch | Workers must not run on failed or stale authority. |
| retry parent to child record | Split provenance writes can create an untraceable attempt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-34-01 | Elevation of privilege | public build callers | mitigate | Canonical transaction at every production start site. |
| T-200-34-02 | Tampering | retry/check-fix provenance | mitigate | Parent/provenance/state in one target set. |
| T-200-34-03 | Repudiation | caller coverage | mitigate | AST ratchet and post-commit dispatch proof. |
| T-200-34-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run caller tests, process races, and existing direct/plan-only/external/check-fix/retry suites. Confirm production has no old helper references.
</verification>

<success_criteria>
- Every production build-start family uses commitBuildStart.
- Conditional effects are in the transaction and dispatch is after receipt.
- Accepted-plan/build races cannot produce mixed authority.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-34-SUMMARY.md when done.
</output>
