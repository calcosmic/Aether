---
phase: 200-iterative-planning
plan: 14
type: execute
wave: 5
depends_on: [200-09, 200-11, 200-13]
files_modified:
  - cmd/codex_plan_finalize.go
  - cmd/codex_plan_finalize_test.go
  - cmd/planning_scout_stage_200_test.go
autonomous: true
requirements: [CEC-03, PLAN-01, PLAN-04]
must_haves:
  truths:
    - "A completed Scout pass is visible and durably finalized before Route-Setter can start."
    - "Known material owner decisions are batched after the full first Scout pass."
    - "A later-pass Scout never pauses planning by itself; its material candidates reach Route-Setter so the complete pass card can explain their impact."
    - "Invalid or replay-conflicting Scout output changes no score, card, or plan."
  artifacts:
    - path: "cmd/codex_plan_finalize.go"
      provides: "Scout-only output validation, persistence, and next-boundary authorization"
  key_links:
    - from: "cmd/codex_plan_finalize.go"
      to: "cmd/planning_stage_receipt.go"
      via: "Scout result receipt"
      pattern: "StageReceipt|Scout"
    - from: "cmd/codex_plan_finalize.go"
      to: "cmd/planning_decision.go"
      via: "first-pass post-Scout batch and typed direct-resume versus successor-SPEC resolution"
      pattern: "Decision"
    - from: "cmd/codex_plan_finalize.go"
      to: "cmd/specification.go"
      via: "contract-affecting first-pass answers create a successor draft before Route-Setter"
      pattern: "successor_spec_required|SpecRevision"
---

<objective>
Refactor the plan finalizer so a Scout result commits as its own visible stage and, on the first pass only, hands control to either one material decision batch or Route-Setter; later passes always complete through Route-Setter before any material pause.

Purpose: Re-establish the first half of the classic iterative loop without allowing worker output to score, accept, or activate plans.
Output: Scout result boundary validation, durable receipt, decision batching, and exact replay behavior.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-PATTERNS.md
@cmd/codex_plan_finalize.go
@cmd/planning_stage.go
@cmd/planning_stage_receipt.go
@cmd/planning_decision.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate and commit one Scout-stage result</name>
  <read_first>
    - cmd/codex_plan_finalize.go
    - cmd/codex_plan_finalize_test.go
    - cmd/planning_scout_stage_200_test.go (create if absent)
    - cmd/planning_stage.go
    - cmd/planning_stage_receipt.go
    - cmd/planning_evidence.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-01, D-14)
  </read_first>
  <files>cmd/codex_plan_finalize.go, cmd/codex_plan_finalize_test.go, cmd/planning_scout_stage_200_test.go</files>
  <behavior>
    - Finalizer accepts a Scout artifact only for the exact current Scout manifest, pass, spec, base revision, and evidence frontier.
    - It validates findings, new evidence refs, unresolved gaps, and material decision candidates, then commits one Scout receipt atomically.
    - Scout output cannot include dimension scores, semantic plan changes, a candidate, acceptance, or activation.
  </behavior>
  <action>Split the existing whole-worker-chain finalizer at the Scout boundary. Decode a typed Scout result; require manifest ID/hash, run/pass, scout caste, approved spec binding, base plan revision, and prior frontier to match current state. Validate new evidence through the evidence catalogue and require each finding/gap/decision candidate to cite evidence or explicitly state unknown. Reject Route-Setter proposal fields, confidence scores, stop claims, candidate fields, shell-owned state patches, and unrecognized content. Stage normalized Scout artifact plus StageReceipt and transition together through the lifecycle transaction. On exact retry return the same receipt; divergent retry fails without writing.</action>
  <acceptance_criteria>
    - Valid Scout output produces one artifact and one chained receipt, with no iteration card yet.
    - Wrong caste, spec hash, pass, frontier, or manifest hash fails without mutation.
    - Any Scout-supplied score, semantic delta, stop, candidate, or activation field is rejected.
    - Exact replay returns the stored receipt; changed replay preserves the original frontier.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningScoutStage.*Finalize|TestPlanningScoutStage.*Reject|TestPlanningScoutStage.*Replay' -count=1</automated>
  </verify>
  <done>A Scout pass becomes a durable, auditable research boundary and cannot exercise Route-Setter or owner authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pause for the complete material batch or authorize Route-Setter</name>
  <read_first>
    - cmd/codex_plan_finalize.go
    - cmd/planning_scout_stage_200_test.go
    - cmd/planning_decision.go
    - cmd/specification.go
    - cmd/planning_stage.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-05 through D-08)
    - .planning/phases/200-iterative-planning/200-UI-SPEC.md (Scout Complete and Decision Card)
  </read_first>
  <files>cmd/codex_plan_finalize.go, cmd/codex_plan_finalize_test.go, cmd/planning_scout_stage_200_test.go</files>
  <behavior>
    - First-pass material decisions are emitted once as a complete evidence-first batch after Scout receipt commit.
    - Later Scout material discoveries are carried into Route-Setter input and do not create a post-Scout pause; Plan 15 may pause only after the complete pass card.
    - An equivalent non-contract first-pass answer may authorize Route-Setter, while an answer changing promised behavior, scope, risk, or acceptance creates a successor DRAFT and requires exact approval plus scoped reconciliation first.
    - With no first-pass material decision, the runtime creates one Route-Setter authorization linked to the Scout receipt.
  </behavior>
  <action>After committing the Scout result, branch on the persisted pass ordinal. On pass one, pass all decision candidates and the completed Scout receipt to the shared decision policy; if a material batch remains, persist owner_decision with the content-addressed batch and resume token, return its renderer-neutral card model, and dispatch nothing. On an exact answer, consume the policy's resolution disposition: direct_resume converts an equivalent non-contract answer to decision evidence and authorizes Route-Setter exactly once; successor_spec_required calls the specification engine to create an immutable successor DRAFT from the answer and affected stable IDs, marks the planning frontier awaiting SPEC approval/reconciliation, invalidates any affected pending planning authorization, and dispatches nothing until that exact successor is approved and the affected scope is rebound. On pass two or later, never enter owner_decision after Scout: carry all material candidates and their evidence in the exact Route-Setter authorization so Plan 15 can compute and append the complete iteration card before pausing under D-06. If no first-pass decision remains, directly request the single Route-Setter manifest from the stage engine. Ensure the response exposes Scout complete, evidence added, gaps found, and the correct next boundary; do not create the Route-Setter result or iteration card early.</action>
  <acceptance_criteria>
    - Multiple material candidates yield one batch and zero Route-Setter dispatches.
    - Exact completed answers yield one Route-Setter authorization; stale or partial answers do not.
    - Equivalent non-contract answers resume Route-Setter directly; contract-affecting answers create a successor DRAFT and cannot dispatch until exact SPEC approval plus scoped reconciliation.
    - No-material fixture yields one Route-Setter authorization linked to the committed Scout receipt.
    - A pass-two-or-later material discovery produces one Route-Setter authorization, no post-Scout owner_decision, and is surfaced only after Plan 15 appends the full pass card.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningScoutStage.*Decision|TestPlanningScoutStage.*Route|TestPlanningScoutStage.*LateMaterial|TestPlanningScoutStage.*ContractAnswer' -count=1</automated>
  </verify>
  <done>Every Scout pass visibly completes before the runtime chooses the precise human or Route-Setter boundary.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run 'TestPlanningScoutStage|TestCodexPlanFinalize.*Scout' -count=1, covering valid, malformed, exact replay, material batch, answer resume, and direct Route authorization.
</verification>

<success_criteria>
- Scout has its own validated artifact and receipt before Route-Setter begins.
- Owner decisions are complete, evidence-first, and emitted only at a full-pass boundary.
- Scout results cannot score, stop, accept, or activate a plan.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-14-SUMMARY.md when done.
</output>
