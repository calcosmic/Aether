---
phase: 200-iterative-planning
plan: 52
type: execute
wave: 31
depends_on: [200-44, 200-45, 200-49, 200-50]
files_modified:
  - cmd/codex_plan.go
  - cmd/codex_plan_finalize_test.go
  - cmd/codex_plan_test.go
  - cmd/composed_questions_test.go
autonomous: true
gap_closure: true
requirements: [SYNTH-02, CEC-03, PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Legacy planning tests construct canonical nested assessment, gap, timeline, candidate, and acceptance hashes instead of hand-copying stale digests."
    - "Expiry-sensitive fixtures use one injected observed time and remain valid independent of today's date."
    - "Force replan removes declared stale fallback artifacts atomically while preserving newer worker-authored artifacts."
    - "Rendered command failures retain their nonzero Cobra result and exact modern recovery guidance."
  artifacts:
    - path: "cmd/codex_plan_finalize_test.go"
      provides: "Canonical planning history/clock fixtures"
    - path: "cmd/codex_plan.go"
      provides: "Session-backed fallback cleanup used by force replan"
  key_links:
    - from: "cmd/codex_plan_finalize_test.go"
      to: "cmd/planning_state.go"
      via: "canonical nested digest helpers"
      pattern: "Canonical|canonical"
    - from: "cmd/codex_plan.go"
      to: "cmd/planning_mutation_session.go"
      via: "declared cleanup targets in one repository session"
      pattern: "planningMutationSession|InSession"
---

<objective>
Migrate remaining legacy planning fixtures and restore atomic force-replan cleanup.

Purpose: The full gate found old hand-built hashes/clocks plus one real cleanup regression after the canonical planning-session migration.
Output: Time-stable canonical fixtures and a complete session-declared fallback cleanup.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-28-SUMMARY.md
@.planning/phases/200-iterative-planning/200-32-SUMMARY.md
@.planning/phases/200-iterative-planning/200-37-SUMMARY.md
@cmd/planning_mutation_session_200_test.go
</context>

<scope_guard>
Honor D-01..D-16 and the manual repair pattern in commit `5bac43d0`: recompute canonical nested bodies rather than weakening validators or freezing expected old hashes. Do not revive fallback planning as authority.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild nested planning fixtures through canonical constructors</name>
  <files>cmd/codex_plan_finalize_test.go, cmd/codex_plan_test.go</files>
  <behavior>
    - Stop-policy history validates every nested assessment/gap/content hash before policy evaluation.
    - Candidate/acceptance fixtures derive current timestamps from one injected UTC clock and remain before expiry where intended.
    - Tamper cases mutate only the field named by the test after canonical construction.
  </behavior>
  <action>Replace handwritten nested planning values with existing production-equivalent test constructors, following `planningAdversarialCanonicalTimelineCard200` from `5bac43d0`. Keep RED evidence for the stale fixture, then make the fixture canonical; do not relax `validatePlanning*`. Preserve the exact stop-policy, acceptance, and refusal assertions.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(PlanFinalizeStallsOnlyAfterTwoLowImprovements|PlanAcceptWithExistingPlanFailsLoudly)$' -count=1</automated>
  </verify>
  <done>Both tests reach their intended policy assertions with fully canonical nested evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete force-replan cleanup inside the mutation session</name>
  <files>cmd/codex_plan.go, cmd/codex_plan_test.go</files>
  <behavior>
    - Stale fallback marker, fallback plan, backup, and stale phase research are removed in the same declared commit.
    - Newer worker-authored Scout/Route/phase artifacts are preserved byte-for-byte.
    - Injected failure restores all present/absent baselines.
  </behavior>
  <action>Add every cleanup target, including `.fallback-marker`, to the Plan 28 repository session before the first authoritative read. Preserve the timestamp/source classification for worker-authored artifacts. Prove success and rollback; do not add direct `os.Remove` after commit.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(E2EForceReplanRecovery|ClearFallbackPlanningArtifactsPreservesNewerWorkerFiles)$' -count=1</automated>
  </verify>
  <done>Force replan removes only declared stale fallback state and rolls back atomically on fault.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Align discussion/acceptance command fixtures with modern error semantics</name>
  <files>cmd/codex_plan_test.go, cmd/composed_questions_test.go</files>
  <behavior>
    - `plan --accept` without an exact candidate returns a nonzero command error and an error envelope naming `--accept-candidate`.
    - Discuss question round-trip binds the repository selected by its fixture and returns parseable JSON.
  </behavior>
  <action>Update tests to assert the current rendered-error bridge rather than expecting a nil Cobra error, and use the Plan 41 repository binding for discussion. Preserve exact recovery wording and zero-mutation checks.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(PlanAcceptWithExistingPlanFailsLoudly|DiscussAddQuestionRoundTrip)$' -count=1</automated>
  </verify>
  <done>Both command fixtures prove the current nonzero/error-envelope and repository-bound behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| fixture digest to validator | Stale copied hashes can test the wrong branch or invite validator weakening. |
| fallback cleanup to worker artifacts | Broad deletion can destroy newer evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-52-01 | Tampering | planning fixture identity | mitigate | Canonical construction then one-field mutation for negative cases. |
| T-200-52-02 | Tampering | force-replan cleanup | mitigate | Exact declared targets and transaction rollback fingerprints. |
| T-200-52-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run `go test ./cmd -run '^Test(Plan|DiscussAddQuestionRoundTrip|E2EForceReplanRecovery)' -count=1`.
</verification>

<success_criteria>
- Canonical validation remains strict.
- Time does not rot success fixtures.
- Force replan is atomic and preserves newer evidence.
</success_criteria>

<output>Create `.planning/phases/200-iterative-planning/200-52-SUMMARY.md` when done.</output>
