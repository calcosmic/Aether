---
phase: 200-iterative-planning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/colony/colony.go
  - pkg/colony/specification.go
  - pkg/colony/specification_test.go
  - pkg/colony/planning.go
  - pkg/colony/planning_test.go
autonomous: true
requirements: [PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Specification approval, planning stop, and candidate acceptance are represented as separate authorities."
    - "Every new accepted plan revision can name its exact specification, candidate, base revision, and evidence timeline."
    - "The canonical specification carries every owner-readable contract section as typed stable-ID content."
  artifacts:
    - path: "pkg/colony/specification.go"
      provides: "Immutable specification revision and approval contracts"
    - path: "pkg/colony/planning.go"
      provides: "Typed evidence, assessment, delta, timeline, candidate, and acceptance contracts"
  key_links:
    - from: "pkg/colony/colony.go"
      to: "pkg/colony/specification.go"
      via: "ColonyState specification field"
      pattern: "Specification"
    - from: "pkg/colony/colony.go"
      to: "pkg/colony/planning.go"
      via: "Plan candidate and revision bindings"
      pattern: "Candidate|Timeline|SpecRevision"
---

<objective>
Define the durable domain contracts for the specification lineage and iterative planning authority model.

Purpose: Give every later command and finalizer one Go-owned vocabulary for evidence, confidence, semantic change, stopping, candidates, and exact acceptance without putting state authority in prompts or shell wrappers.
Output: Backward-compatible colony-state types and validation-focused unit tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md
@pkg/colony/colony.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define immutable specification contracts</name>
  <read_first>
    - pkg/colony/colony.go
    - pkg/colony/specification.go (create if absent)
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-09 through D-12)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Specification Domain Model)
  </read_first>
  <files>pkg/colony/colony.go, pkg/colony/specification.go, pkg/colony/specification_test.go</files>
  <behavior>
    - A specification starts as draft, becomes approved only through an approval receipt for the exact revision, and an immutable successor supersedes rather than mutates its predecessor.
    - A scoped successor carries explicit added, modified, and removed stable item IDs while preserving unaffected IDs.
    - Every revision has typed stable-ID sections for outcome, included behavior, exclusions, binding decisions, requirements, owner-checkable acceptance, negative expectations, recovery expectations, and affected public paths.
    - Specification approval is not plan-candidate acceptance.
  </behavior>
  <action>Create versioned Specification, SpecRevision, SpecScope, SpecRevisionDelta, and SpecApprovalReceipt types plus closed draft, approved, and superseded status constants. Model the complete readable body with distinct typed stable-ID entries and explicit revision fields for outcomes, included behaviors, exclusions, binding decisions, requirements, owner-checkable acceptance checks, negative expectations, recovery expectations, and affected public paths; do not collapse these into a generic `items` bag. Add the specification aggregate to ColonyState as an optional backward-compatible field and store its canonical immutable revision snapshots there so approval plus impact can commit atomically. Each revision must carry immutable ID, predecessor ID, created timestamp, content hash, scope, status, and optional exact approval receipt. Model add/modify/remove classifications explicitly for every body category; do not infer them from prose. Keep the human-readable .aether/SPEC.md outside these authority types because it is a deterministic projection. Enforce D-09 through D-12 by making draft and approved states distinct and by making approval identify the exact revision and hash.</action>
  <acceptance_criteria>
    - go test ./pkg/colony -run TestSpec -count=1 passes.
    - pkg/colony/specification.go contains closed status constants for draft, approved, and superseded and structs named SpecRevisionDelta and SpecApprovalReceipt.
    - A whole-goal and feature-scoped revision round trip every required typed body category with stable IDs and no generic-item loss.
    - ColonyState can unmarshal a legacy fixture with no specification field.
    - No specification field is named or documented as plan acceptance.
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/colony -run 'TestSpec' -count=1</automated>
  </verify>
  <done>The state schema can preserve an immutable, explicitly approved specification lineage without changing legacy state decoding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define evidence, iteration, candidate, and revision bindings</name>
  <read_first>
    - pkg/colony/colony.go
    - pkg/colony/planning.go (create if absent)
    - pkg/colony/specification.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-01 through D-08 and D-13 through D-16)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Planning Domain Model and Responsibility Map)
  </read_first>
  <files>pkg/colony/colony.go, pkg/colony/planning.go, pkg/colony/planning_test.go</files>
  <behavior>
    - An iteration records Route-Setter-proposed before/after values for all five dimensions, fresh evidence references, the weakest remaining gap, semantic delta, evidence that would change the next decision, and a continue-or-stop result for Go to validate.
    - A stopped candidate is non-active until a separate exact acceptance receipt binds candidate, specification, base revision, and complete timeline; it also carries a typed Queen recommendation with accept-or-revise disposition, evidence IDs, rationale, and producing-authority identity.
    - Semantic plan content has stable IDs and proof links that survive compatible revisions.
  </behavior>
  <action>Create closed types and constants for evidence kinds specification, survey, charter, decision, context, research, hive, and outcome; the five dimensions Knowledge, Requirements, Risks, Dependencies, and Effort; gap materiality; semantic change kinds; authority impacts; stop reasons; candidate status; recommendation disposition; recommendation producer; and acceptance policy. Define PlanningEvidenceRef, PlanningDimensionAssessment, PlanningGap, PlanningSemanticDelta, PlanningIterationCard, PlanningStopDecision, PlanningTimelineBinding, QueenPlanRecommendation, PlanCandidate, and PlanAcceptanceReceipt with stable IDs and content hashes. PlanningDimensionAssessment is the Route-Setter proposal contract: whole-number before/after values, applicable fresh evidence IDs, resolved gap IDs, remaining gap, rationale, and producing worker receipt; Go later validates these values and recomputes weighted overall rather than accepting a worker-supplied overall. Require `evidence_that_would_change` on every gap, iteration decision, stop decision, and PlanCandidate (both a top-level aggregate and each residual gap), and include it in every renderer-neutral iteration/stop/candidate result representation. QueenPlanRecommendation must carry `accept` or `revise`, evidence-grounded rationale, evidence IDs, and the authorized producer identifier; it is advice and grants no acceptance. Extend Plan with optional candidate storage, pending candidate ID, and explicit acceptance policy. Extend PlanRevision, Phase, and Task with optional stable semantic IDs, requirement/acceptance/negative/recovery/public-path proof links, exact spec/candidate/timeline bindings, and affected/preserved IDs. Retain ordinal identifiers for build/display compatibility but use immutable semantic IDs for trace, diff, and impact. Use omitempty only where legacy JSON compatibility requires absence; current-schema causal fields are required. Do not add worker-cycle, live-event, pheromone-transport, learning-governance, or final product-acceptance fields.</action>
  <acceptance_criteria>
    - go test ./pkg/colony -run TestPlanning -count=1 passes.
    - Exactly five planning-dimension constants exist and score fields use whole-number integers.
    - Current iteration, stop, gap, and candidate fixtures reject an empty evidence_that_would_change field.
    - QueenPlanRecommendation identifies its authorized producer and cannot mutate candidate or acceptance status.
    - PlanAcceptanceReceipt has distinct candidate, specification revision, base plan revision, and timeline digest bindings.
    - A new non-accepted PlanCandidate cannot be confused with Plan.ActiveRevisionID by its schema.
    - Existing serialized Plan, Phase, and Task fixtures still unmarshal.
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/colony -run 'TestPlanning|TestSpec' -count=1</automated>
  </verify>
  <done>All downstream Phase 200 components can compile against one typed, backward-compatible separation of evidence, candidate state, and active-plan authority.</done>
</task>

</tasks>

<verification>
Run go test ./pkg/colony -count=1; inspect JSON round trips for both legacy and new fixtures; confirm approval and acceptance receipts have no shared boolean shortcut.
</verification>

<success_criteria>
- Legacy colony state still decodes without fabricated specification or candidate authority.
- All five confidence dimensions and every semantic/proof category have explicit Go types.
- Specification approval, candidate stopping, and plan activation require different typed records.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-01-SUMMARY.md when done.
</output>
