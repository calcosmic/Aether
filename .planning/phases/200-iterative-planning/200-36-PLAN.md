---
phase: 200-iterative-planning
plan: 36
type: execute
wave: 23
depends_on: [200-35]
files_modified:
  - cmd/build_attempt_external_test.go
  - cmd/coherent_job_retry_test.go
  - cmd/coherent_job_retry_plan_only_test.go
  - cmd/coherent_job_retry_command_test.go
  - cmd/pause_resume_199_test.go
  - cmd/build_attempt.go
autonomous: true
gap_closure: true
requirements: [PLAN-06]
must_haves:
  truths:
    - "External-finalize, coherent retry, and pause/resume fixtures use the transaction-only helper without collapsing bound, unbound, latest-pointer, active-work, or plan-only semantics."
    - "Legacy partial start/provenance helpers are removed only after production and every fixture caller is migrated."
    - "A repository-wide AST/compile ratchet fails on any reintroduced call or equivalent direct partial start writer."
  artifacts:
    - path: "cmd/build_attempt_external_test.go"
      provides: "External bound/unbound fixtures on canonical transaction plus repository-wide helper-retirement AST ratchet"
      contains: "TestBuildStartLegacyHelpersRetired200"
    - path: "cmd/coherent_job_retry_plan_only_test.go"
      provides: "Plan-only child retry fixture preserving non-latest semantics"
      contains: "commitTestBuildStart"
    - path: "cmd/pause_resume_199_test.go"
      provides: "Active-work pause fixture built by canonical start plus dispatch transition"
      contains: "commitTestBuildStart"
    - path: "cmd/build_attempt.go"
      provides: "Attempt transition logic without obsolete partial start helpers"
  key_links:
    - from: "cmd/coherent_job_retry_test.go"
      to: "cmd/build_start_test_helpers_200_test.go"
      via: "canonical child start with explicit parent provenance"
      pattern: "commitTestBuildStart"
    - from: "cmd/build_attempt_external_test.go"
      to: "cmd/build_attempt.go"
      via: "AST rejects retired declarations and call sites"
      pattern: "LegacyHelpersRetired"
---

<objective>
Finish fixture migration and retire every partial build-start helper.

Purpose: Once callers are canonical, leaving an attractive bypass invites future lost-update regressions.
Output: Remaining fixture migrations, helper deletion, and repository-wide compile/AST enforcement.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-35-SUMMARY.md
@cmd/build_start_test_helpers_200_test.go
@cmd/build_attempt_external_test.go
@cmd/coherent_job_retry_test.go
@cmd/coherent_job_retry_plan_only_test.go
@cmd/coherent_job_retry_command_test.go
@cmd/pause_resume_199_test.go
@cmd/build_attempt.go
@cmd/build_start_callers_200_test.go
</context>

<scope_guard>
Modify only the six declared files. Retire helpers only after Task 1 proves the last fixture references are gone. Preserve user dirt and use scoped task commits.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate every remaining external, retry, and pause fixture</name>
  <files>cmd/build_attempt_external_test.go, cmd/coherent_job_retry_test.go, cmd/coherent_job_retry_plan_only_test.go, cmd/coherent_job_retry_command_test.go, cmd/pause_resume_199_test.go</files>
  <behavior>
    - "External tests preserve already-bound replay versus unbound start behavior."
    - "Coherent retries preserve parent linkage, unfinished-only task selection, and makeLatest=false."
    - "Plan-only retry never appears as an active dispatched attempt."
    - "The active-work pause fixture creates its attempt/latest pointer through commitTestBuildStart and reaches dispatching only through transitionBuildAttempt."
  </behavior>
  <action>Use Plan 35's read-only commitTestBuildStart helper to replace the remaining beginBuildAttempt and beginChildBuildAttempt calls, including coherent_job_retry_command_test.go, passing explicit external completion/binding and child-parent options. In pause_resume_199_test.go active_work_waits, replace the direct attempt record and latest-pointer SaveJSON pair with commitTestBuildStart(makeLatest=true), then call transitionBuildAttempt to reach buildAttemptDispatching before invoking pause; preserve the real process binding and unchanged-state/zero-receipt assertions. Retain all original receipt, path, transition, selected-task, and pointer assertions. Add focused cases for bound replay and plan-only child recovery so migration cannot accidentally create a new attempt or promote the child to latest.</action>
  <verify>
    <automated>go test ./cmd -run 'Test(BuildAttemptExternal|CoherentJobRetry|CoherentJobRetryPlanOnly|CoherentJobRetryCommand|PauseResume199)' -count=1</automated>
  </verify>
  <done>Every known fixture calls the canonical transaction and preserves its prior lane semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove partial helpers and enforce their retirement</name>
  <files>cmd/build_attempt.go, cmd/build_attempt_external_test.go</files>
  <action>Only after Task 1 and Plan 35 have migrated codex_build_test.go, pause_resume_199_test.go, and every named fixture, remove beginBuildAttempt, beginBuildAttemptRecord, beginChildBuildAttempt, attachBuildAttemptParentLink, and any now-dead write-oriented helpers from build_attempt.go; retain pure record derivation and post-start transition APIs used by the canonical transaction. Define TestBuildStartLegacyHelpersRetired200 in the already-owned build_attempt_external_test.go and scan all non-vendored Go source and tests to reject retired declarations/calls and direct reconstruction of their attempt-plus-pointer write sequence. Keep Plan 34's production-caller ratchet green and require go test compilation of every package so hidden fixture references cannot survive.</action>
  <verify>
    <automated>go test ./cmd -run '^(TestBuildStartLegacyHelpersRetired200|TestBuildStartCallers200)$' -count=1 &amp;&amp; go test ./... -run '^$' -count=1</automated>
  </verify>
  <done>No production or test bypass remains; every package compiles against the canonical build-start API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| legacy helper to future caller | A surviving bypass can recreate partial starts. |
| external/retry fixture to helper | Lane-specific semantics can be lost during migration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-36-01 | Elevation of privilege | legacy start helpers | mitigate | Remove declarations after complete caller migration. |
| T-200-36-02 | Tampering | retry lane semantics | mitigate | Focused bound/unbound/latest/plan-only assertions. |
| T-200-36-03 | Repudiation | hidden caller | mitigate | Repository-wide AST plus all-package compile gate. |
| T-200-36-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run all remaining fixture suites, the existing production-caller ratchet, the repository-wide AST retirement test, and compile all packages. Confirm no removed helper name or direct attempt-plus-pointer reconstruction occurs in executable Go syntax.
</verification>

<success_criteria>
- All fixtures use commitBuildStart.
- Retired helper declarations and calls are absent.
- Full repository compile stays green.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-36-SUMMARY.md when done.
</output>
