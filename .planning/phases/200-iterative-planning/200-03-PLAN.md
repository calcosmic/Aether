---
phase: 200-iterative-planning
plan: 03
type: execute
wave: 2
depends_on: [200-01]
files_modified:
  - cmd/planning_evidence.go
  - cmd/planning_evidence_test.go
autonomous: true
requirements: [CEC-03, PLAN-02, PLAN-06]
must_haves:
  truths:
    - "Every confidence movement cites fresh, applicable, content-addressed evidence."
    - "Stale or scope-mismatched context cannot silently inflate planning confidence."
  artifacts:
    - path: "cmd/planning_evidence.go"
      provides: "Evidence collection, normalization, admissibility, freshness, and applicability"
  key_links:
    - from: "cmd/planning_evidence.go"
      to: "pkg/colony/planning.go"
      via: "PlanningEvidenceRef construction"
      pattern: "PlanningEvidenceRef"
---

<objective>
Build the evidence catalogue used by both Scout research and Route-Setter scoring.

Purpose: Make confidence auditable and resistant to stale, repeated, irrelevant, or tampered context.
Output: Pure evidence indexing and validation helpers with adversarial tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-RESEARCH.md
@.planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md
@pkg/colony/planning.go
@cmd/colony_prime.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collect and normalize admissible planning evidence</name>
  <read_first>
    - cmd/planning_evidence.go (create if absent)
    - pkg/colony/planning.go
    - cmd/colony_prime.go
    - cmd/context_capsule.go
    - .planning/phases/200-iterative-planning/200-CONTEXT.md (D-01, D-04, D-08)
    - .planning/phases/200-iterative-planning/200-RESEARCH.md (Evidence Catalogue and Freshness)
  </read_first>
  <files>cmd/planning_evidence.go, cmd/planning_evidence_test.go</files>
  <behavior>
    - Specification, survey, charter, decision, context, research, Hive, and outcome sources become stable evidence refs with origin, scope, revision, observed time, and content hash.
    - Identical content deduplicates without becoming fresh again.
    - Changed content at the same path produces a new ref and invalidates hash claims against the old ref.
  </behavior>
  <action>Implement deterministic evidence collection and normalization for every evidence kind defined in pkg/colony/planning.go. Canonicalize repository-relative origins, compute hashes from normalized content plus source identity, preserve source revision and timestamp metadata, deduplicate exact refs, and sort by stable ID. Return typed refusals for paths outside approved roots, unsupported source kinds, missing content, or hash mismatch. Record redacted summaries and locator metadata rather than secrets or unrestricted raw file bodies. Do not assign dimension scores here.</action>
  <acceptance_criteria>
    - Table tests cover all eight evidence kinds and stable ordering.
    - Exact duplicate evidence retains one ID and its original observation time.
    - Modified evidence receives a different hash and ID.
    - Repository path traversal is rejected before reading content.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningEvidence.*Collect|TestPlanningEvidence.*Normalize' -count=1</automated>
  </verify>
  <done>The workflow can build a content-addressed, safely scoped evidence catalogue without scoring side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce freshness and dimension applicability</name>
  <read_first>
    - cmd/planning_evidence.go
    - cmd/planning_evidence_test.go
    - pkg/colony/planning.go
    - .planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md (SYN-200-03 and SYN-200-10)
    - .planning/phases/200-iterative-planning/200-PATTERNS.md (evidence guardrails)
  </read_first>
  <files>cmd/planning_evidence.go, cmd/planning_evidence_test.go</files>
  <behavior>
    - A ref is fresh only relative to its goal, session, approved specification revision, plan revision, and prior-card frontier.
    - Applicability is explicit per confidence dimension; unrelated evidence cannot justify another dimension.
    - Revoked, superseded, dormant, or scope-mismatched Hive or context material is inadmissible.
  </behavior>
  <action>Add pure isFreshPlanningEvidence and evidenceAppliesToDimension policies using explicit scope and revision inputs. Fresh means not already cited at the relevant frontier and not superseded by changed source content; it does not mean merely reread. Encode an explicit applicability matrix with tests, permitting a ref to support multiple dimensions only when its typed claims name each one. Revalidate reused owner answers whenever behavior, impact, risk, acceptance, goal/session/spec, or base-plan scope changes per D-08. Preserve evidence IDs when equivalence truly holds. Treat Hive material as untrusted cross-colony input and honor its revoked, quarantined, and dormant status.</action>
  <acceptance_criteria>
    - Re-citing an unchanged ref after the frontier is not fresh.
    - The same answer is reusable only under an identical equivalence key.
    - A risk-only ref cannot move Requirements or Effort unless those applicability claims are present.
    - Superseded and scope-mismatched evidence is rejected with a reason surfaced to the caller.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestPlanningEvidence.*Fresh|TestPlanningEvidence.*Applies|TestPlanningEvidence.*Scope' -count=1</automated>
  </verify>
  <done>Every downstream score change can be gated on evidence that is both genuinely new and relevant to that exact dimension.</done>
</task>

</tasks>

<verification>
Run go test ./cmd -run TestPlanningEvidence -count=1; confirm tests include restatement, changed file, scope drift, revoked Hive, and cross-dimension misuse.
</verification>

<success_criteria>
- Evidence IDs are stable, deduplicated, content-addressed, and path-safe.
- Freshness is relative to the exact planning frontier rather than wall-clock rereading.
- Confidence consumers receive explicit applicability and refusal reasons.
</success_criteria>

<output>
Create .planning/phases/200-iterative-planning/200-03-SUMMARY.md when done.
</output>
