---
phase: 200-iterative-planning
plan: 47
type: execute
wave: 27
depends_on: [200-42, 200-43, 200-44]
files_modified:
  - cmd/build_attempt.go
  - cmd/build_start_transaction.go
  - cmd/codex_build_finalize.go
  - cmd/build_attempt_external_test.go
  - cmd/codex_build_finalize_test.go
autonomous: true
gap_closure: true
requirements: [CEC-03, PLAN-05, PLAN-06]
must_haves:
  truths:
    - "Internal build-start and reconciliation paths use one repository-relative representation and public output applies `.aether/data/` exactly once."
    - "Journal reconciliation never tries to read `.aether/data/.aether/data`."
    - "A genuinely legacy unbound manifest is accepted only through the documented compatibility adapter, receives a derived canonical binding, and warns; modern malformed bindings still fail closed."
  artifacts:
    - path: "cmd/build_attempt.go"
      provides: "Canonical build-attempt path conversion and reconciliation"
    - path: "cmd/build_start_transaction.go"
      provides: "Receipt-last manifest binding derivation with legacy adapter"
  key_links:
    - from: "cmd/codex_build_finalize.go"
      to: "cmd/build_attempt.go"
      via: "relative claims/attempt paths for reconciliation"
      pattern: "ClaimsPath|AttemptPath"
    - from: "cmd/build_start_transaction.go"
      to: "cmd/plan_authority.go"
      via: "accepted plan authority before manifest binding"
      pattern: "validate.*Authority|authority"
---

<objective>
Repair canonical build-attempt paths and the retained legacy-unbound start adapter.

Purpose: The failed gate found both a real doubled-data-root reconciliation bug and a compatibility path rejected before it could acquire canonical start authority.
Output: One path contract, canonical derived manifest binding, and refusal/replay tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/200-iterative-planning/200-CONTEXT.md
@.planning/phases/200-iterative-planning/200-33-SUMMARY.md
@.planning/phases/200-iterative-planning/200-34-SUMMARY.md
@.planning/phases/200-iterative-planning/200-36-SUMMARY.md
@cmd/plan_authority.go
</context>

<scope_guard>
Preserve the receipt-last atomic transaction and D-16 accepted-plan boundary. Legacy-unbound means an explicitly supported old artifact that is rebound by trusted state; it is not a bypass for a supplied wrong attempt, owner, phase, mode, or receipt.
</scope_guard>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize build-start paths at one boundary</name>
  <files>cmd/build_attempt.go, cmd/build_start_transaction.go, cmd/build_attempt_external_test.go</files>
  <behavior>
    - Relative `last-build-claims.json` persists as one public `.aether/data/last-build-claims.json` value.
    - An already displayed path is normalized once or rejected at input; it is never prefixed twice.
    - Reconciliation resolves every persisted path beneath the authorized data root and refuses absolute/escaping paths.
    - Same-attempt replay yields byte-identical path fields.
  </behavior>
  <action>Write table tests for raw relative, public display, duplicate-prefix, absolute, and traversal forms. Establish one internal relative-path invariant in `deriveBuildAttempt`/transaction preparation and one presentation conversion at result serialization. Replace ad hoc `TrimPrefix`/`displayDataPath` chains with the checked helper. Keep path traversal and cross-root input fail-closed.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(BuildAttemptPathContract200|BuildFinalizeReconcilesJournalAfterBuiltStateCommit)$' -count=1</automated>
  </verify>
  <done>Reconciliation passes and no durable/public field can contain `.aether/data/.aether/data`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebind legacy-unbound manifests inside canonical start</name>
  <files>cmd/build_start_transaction.go, cmd/codex_build_finalize.go, cmd/codex_build_finalize_test.go</files>
  <behavior>
    - A manifest with no historical attempt binding follows the explicit legacy adapter, derives owner/mode/attempt/receipt from current trusted inputs, and emits the established warning.
    - A manifest with a partial, contradictory, or forged binding is refused before mutation.
    - A modern manifest cannot claim legacy status to bypass D-16 authority.
  </behavior>
  <action>Move legacy classification ahead of strict modern binding validation, then pass the classified input through `commitBuildStart` so the transaction derives every execution field and writes its receipt last. Retain the loud compatibility warning. Add before/after fingerprints for every refusal and exact replay.</action>
  <verify>
    <automated>go test ./cmd -run '^Test(BuildFinalizeWarnsOnLegacyUnboundManifest|BuildStartLegacyBinding200)$' -count=1</automated>
  </verify>
  <done>The supported legacy case works through canonical authority; malformed modern and ambiguous partial bindings mutate nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| persisted display path to filesystem path | A duplicate prefix or traversal can address the wrong file. |
| legacy artifact to modern execution | Old missing fields must be derived, never trusted or invented by the artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-200-47-01 | Tampering | build attempt path | mitigate | Checked relative canonicalization plus one-way display conversion. |
| T-200-47-02 | Elevation of privilege | legacy manifest adapter | mitigate | Derive all authority in canonical transaction; reject partial/contradictory claims. |
| T-200-47-SC | Tampering | package supply chain | accept | No package installation occurs. |
</threat_model>

<verification>
Run `go test ./cmd -run '^Test(BuildAttempt|BuildStart|BuildFinalizeWarnsOnLegacyUnboundManifest)' -count=1`.
</verification>

<success_criteria>
- No doubled data-root path is produced or consumed.
- Legacy compatibility goes through, not around, canonical build start.
- All refusal paths are mutation-free.
</success_criteria>

<output>Create `.planning/phases/200-iterative-planning/200-47-SUMMARY.md` when done.</output>
