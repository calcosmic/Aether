---
phase: 68-gate-recovery-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/59-gate-failure-recovery/59-VERIFICATION.md
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03

must_haves:
  truths:
    - "Phase 59 VERIFICATION.md exists with evidence for all three GATE requirements"
    - "GATE-01 verified: recovery templates exist and render for each gate type"
    - "GATE-02 verified: Watcher Veto shows three-choice prompt without auto-stash"
    - "GATE-03 verified: incremental gate checking works via shouldSkipGate and CLI subcommands"
  artifacts:
    - path: ".planning/phases/59-gate-failure-recovery/59-VERIFICATION.md"
      provides: "Verification evidence for GATE-01, GATE-02, GATE-03"
      contains: "GATE-01"
  key_links:
    - from: "59-VERIFICATION.md"
      to: "cmd/gate.go"
      via: "References to gateRecoveryTemplates, shouldSkipGate, gateResultsWrite"
      pattern: "gateRecoveryTemplates|shouldSkipGate"
    - from: "59-VERIFICATION.md"
      to: ".aether/docs/command-playbooks/continue-gates.md"
      via: "References to Watcher Veto three-choice prompt"
      pattern: "AskUserQuestion|watcher_veto"
---

<objective>
Create Phase 59 VERIFICATION.md with evidence that all three GATE requirements were implemented.

Purpose: Phase 59 was implemented (Plan 01 has 6 commits, Plan 02 has 2 commits) but never received a VERIFICATION.md. This plan gathers test evidence, code evidence, and playbook evidence to formally verify GATE-01, GATE-02, and GATE-03.

Output: `59-VERIFICATION.md` with per-requirement evidence sections containing test output, grep proofs, and code references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-gate-recovery-verification/68-RESEARCH.md
@.planning/phases/68-gate-recovery-verification/68-VALIDATION.md

<interfaces>
<!-- No code interfaces needed -- this plan creates a verification document by gathering evidence from tests and grep. -->

Evidence sources to reference:
- cmd/gate.go: gateRecoveryTemplates map (12 entries), gateRecoveryTemplate(), shouldSkipGate(), gateResultsWrite(), gateResultsRead(), formatSkipSummary()
- cmd/gate_test.go: 23 tests covering recovery templates, skip logic, CLI subcommands, persistence
- cmd/gate_incremental_test.go: 5 tests covering continue gate skip, persistence, cleanup
- .aether/docs/command-playbooks/continue-verify.md: Skip summary rendering, recovery template rendering, gate-results-write calls
- .aether/docs/command-playbooks/continue-gates.md: Skip checks in all 9 gate steps, Watcher Veto three-choice prompt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run gate tests and gather evidence for VERIFICATION.md</name>
  <files>.planning/phases/59-gate-failure-recovery/59-VERIFICATION.md</files>
  <read_first>
    cmd/gate.go (lines 473-522 for gateRecoveryTemplates, lines 526-531 for gateRecoveryTemplate, lines 536-546 for shouldSkipGate, lines 549-565 for gateResultsWrite/Read),
    .aether/docs/command-playbooks/continue-gates.md (lines 838-966 for Watcher Veto three-choice prompt),
    .aether/docs/command-playbooks/continue-verify.md (lines 98-114 for skip summary, lines 365-395 for recovery template rendering)
  </read_first>
  <action>
    1. Run all gate-related tests and capture output:
       ```
       go test ./cmd/... -run "TestGateRecovery|TestShouldSkip|TestGateResults|TestFormatSkip|TestContinueGates_|TestGateResultsReadCmd|TestGateResultsWriteCmd|TestShouldSkipGateCmd|TestGateRecoveryTemplateCmd|TestIncrementalGateChecking" -count=1 -v
       ```
    2. Grep evidence for each requirement:

       GATE-01 (Recovery instructions):
       - `grep -c "gateRecoveryTemplates" cmd/gate.go` -- should be >= 2 (declaration + usage)
       - `grep "gate-recovery-template" cmd/gate.go | head -5` -- CLI subcommand registration
       - `grep "gate-recovery-template" .aether/docs/command-playbooks/continue-verify.md` -- playbook usage

       GATE-02 (Watcher Veto confirmation):
       - `grep -c "AskUserQuestion" .aether/docs/command-playbooks/continue-gates.md` -- should be >= 1
       - `grep "watcher_veto\|Stash changes\|Keep working\|Force advance" .aether/docs/command-playbooks/continue-gates.md`
       - Verify no `git stash push` without user choice in the playbook

       GATE-03 (Incremental gate checking):
       - `grep -c "shouldSkipGate" cmd/gate.go` -- should be >= 2 (function + calls)
       - `grep "should-skip-gate\|gate-results-write\|gate-results-read" cmd/gate.go | head -10` -- CLI subcommands
       - `grep "should-skip-gate\|gate-results-write\|gate-results-read" .aether/docs/command-playbooks/continue-gates.md | head -10` -- playbook usage

    3. Create the VERIFICATION.md file at `.planning/phases/59-gate-failure-recovery/59-VERIFICATION.md` with this structure:

       ```markdown
       # Phase 59: Gate Failure Recovery -- Verification

       **Verified:** [today's date]
       **Verifier:** Phase 68 automated verification

       ## GATE-01: Recovery Instructions
       **Requirement:** Verification gate failures show clear, actionable recovery instructions instead of just "FAILED" banner
       **Status:** VERIFIED

       ### Evidence
       - `gateRecoveryTemplates` map in cmd/gate.go contains 12 entries (verification_loop, spawn_gate, anti_pattern, complexity, gatekeeper, auditor, tdd_evidence, runtime, flags, watcher_veto, medic, tests_pass)
       - `gate-recovery-template` CLI subcommand outputs per-gate recovery instructions
       - continue-verify.md playbook renders recovery templates for each failed gate
       - Tests: TestGateRecoveryTemplates_HasAllGateNames, TestGateRecoveryTemplate_KnownGate, TestGateRecoveryTemplate_UnknownGate, TestGateRecoveryTemplateCmd_KnownGate, TestGateRecoveryTemplateCmd_UnknownGate -- ALL PASS

       [Insert test output here]

       ## GATE-02: Watcher Veto Confirmation
       **Requirement:** Watcher Veto does not auto-stash work without explicit user confirmation
       **Status:** VERIFIED

       ### Evidence
       - continue-gates.md Step 1.13 contains three-choice AskUserQuestion: "Stash changes", "Keep working", "Force advance"
       - Veto reason shown before choices (lines 876-887)
       - "Stash changes" runs git stash + creates blocker (lines 902-923)
       - "Keep working" does nothing, phase stays blocked (lines 925-934)
       - "Force advance" creates FEEDBACK pheromone (lines 936-953)
       - No auto-stash without user choice (grep confirms absence of unconditional git stash push)

       [Insert grep output here]

       ## GATE-03: Incremental Gate Checking
       **Requirement:** Re-running /ant-continue only re-checks previously failed gates, not all gates from scratch
       **Status:** VERIFIED

       ### Evidence
       - `shouldSkipGate()` in cmd/gate.go skips passed gates except tests_pass
       - `gateResultsWrite()` merges entries by name (CR-01 fixed in Phase 68)
       - `gate-results-write`, `gate-results-read`, `should-skip-gate` CLI subcommands exist
       - continue-gates.md has skip checks in all 9 gate steps
       - continue-verify.md has skip summary rendering (lines 98-114)
       - Gate results persist to COLONY_STATE.json and clear on phase advance
       - Tests: TestShouldSkipGate_PassedGateSkipped, TestShouldSkipGate_TestsNeverSkipped, TestShouldSkipGate_FailedGateNotSkipped, TestShouldSkipGate_NoPriorResults, TestContinueGates_SkipPassedGates, TestContinueGates_TestsAlwaysRun, TestContinueGates_ResultsPersisted, TestContinueGates_ClearedOnAdvance, TestContinueGates_ResultsPreservedOnFailure, TestIncrementalGateChecking_SkipsPriorPassed, TestGateResultsWrite_MergesEntries, TestGateResultsWrite_UpsertsExistingEntry, TestGateResultsWrite_MergesMultipleEntriesAtOnce -- ALL PASS

       [Insert test output here]

       ## Summary
       | Requirement | Status | Evidence Sources |
       |-------------|--------|-----------------|
       | GATE-01 | VERIFIED | 12 recovery templates, CLI subcommand, playbook rendering, 5 tests pass |
       | GATE-02 | VERIFIED | Three-choice prompt in playbook, no auto-stash, grep-confirmed |
       | GATE-03 | VERIFIED | shouldSkipGate, merge write, CLI subcommands, 13 tests pass |
       ```

    Insert the actual test output and grep results into the [Insert ... here] placeholders.
  </action>
  <verify>
    <automated>test -f .planning/phases/59-gate-failure-recovery/59-VERIFICATION.md && grep -c "GATE-01\|GATE-02\|GATE-03" .planning/phases/59-gate-failure-recovery/59-VERIFICATION.md</automated>
  </verify>
  <done>
    - `59-VERIFICATION.md` exists in the Phase 59 directory
    - All three GATE requirements have VERIFIED status with evidence
    - Test output is embedded showing all tests pass
    - Grep evidence is embedded showing code and playbook references
  </done>
  <acceptance_criteria>
    - `test -f .planning/phases/59-gate-failure-recovery/59-VERIFICATION.md` succeeds
    - `grep -c "GATE-01" .planning/phases/59-gate-failure-recovery/59-VERIFICATION.md` returns >= 1
    - `grep -c "GATE-02" .planning/phases/59-gate-failure-recovery/59-VERIFICATION.md` returns >= 1
    - `grep -c "GATE-03" .planning/phases/59-gate-failure-recovery/59-VERIFICATION.md` returns >= 1
    - `grep -c "VERIFIED" .planning/phases/59-gate-failure-recovery/59-VERIFICATION.md` returns >= 3
    - `grep -c "PASS" .planning/phases/59-gate-failure-recovery/59-VERIFICATION.md` returns >= 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only task, no security surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-03 | - | - | accept | Documentation-only task, no security surface |
</threat_model>

<verification>
1. VERIFICATION.md file exists in Phase 59 directory
2. All three GATE requirements listed with VERIFIED status
3. Test output embedded (showing passing tests)
4. Grep evidence embedded (showing code/playbook references)
</verification>

<success_criteria>
- Phase 59 VERIFICATION.md exists with GATE-01, GATE-02, GATE-03 all marked VERIFIED
- Each requirement has concrete evidence (test output, grep results, code references)
- All gate-related tests pass at the time of verification
</success_criteria>

<output>
After completion, create `.planning/phases/68-gate-recovery-verification/68-02-SUMMARY.md`
</output>
