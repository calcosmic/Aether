---
phase: 06-state-architecture-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - .claude/commands/ant/oracle.md
  - .opencode/commands/ant/oracle.md
  - tests/unit/oracle-state.test.js
  - tests/bash/test-oracle-state.sh
autonomous: true
requirements:
  - LOOP-01
  - INTL-01
  - INTL-04

must_haves:
  truths:
    - "The oracle wizard creates state.json, plan.json, gaps.md, synthesis.md, and research-plan.md when starting a new session"
    - "A research topic decomposes into 3-8 tracked sub-questions with status (open/partial/answered) visible in plan.json"
    - "research-plan.md is generated as an executive summary showing topic, status, questions, confidence, and next steps"
    - "State files pass jq validation after creation and after simulated iteration updates"
    - "Oracle status command reads from state.json and research-plan.md instead of progress.md"
  artifacts:
    - path: ".claude/commands/ant/oracle.md"
      provides: "Updated wizard that writes state.json + plan.json + initial gaps.md + synthesis.md + research-plan.md; updated status display reading new files"
      contains: "state.json"
    - path: ".opencode/commands/ant/oracle.md"
      provides: "Mirror of Claude Code oracle command with identical state file handling"
      contains: "state.json"
    - path: "tests/unit/oracle-state.test.js"
      provides: "Ava tests for validate-oracle-state subcommand covering valid state, valid plan, missing fields, invalid types, question count bounds, status enum, confidence range"
      exports: []
    - path: "tests/bash/test-oracle-state.sh"
      provides: "Bash tests for oracle state file creation, archive behavior, research-plan.md generation"
      exports: []
  key_links:
    - from: ".claude/commands/ant/oracle.md"
      to: ".aether/oracle/state.json"
      via: "wizard writes state.json at session start"
      pattern: "state\\.json"
    - from: ".claude/commands/ant/oracle.md"
      to: ".aether/oracle/plan.json"
      via: "wizard writes plan.json with decomposed questions"
      pattern: "plan\\.json"
    - from: ".claude/commands/ant/oracle.md"
      to: ".aether/oracle/research-plan.md"
      via: "wizard generates research-plan.md from plan.json after creation"
      pattern: "research-plan\\.md"
    - from: "tests/unit/oracle-state.test.js"
      to: ".aether/aether-utils.sh"
      via: "execSync calls validate-oracle-state subcommand"
      pattern: "validate-oracle-state"
---

<objective>
Update the oracle wizard commands (Claude Code + OpenCode) to create structured state files on session start, and write comprehensive tests for the validate-oracle-state subcommand and state file lifecycle.

Purpose: The wizard is the entry point where state files get created. Without updating it, the new state infrastructure from Plan 01 is never populated. Tests ensure the schemas are enforced and state files remain valid across the lifecycle.

Output: Two updated command files (Claude Code + OpenCode parity) and two test files (ava unit + bash integration).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-state-architecture-foundation/06-CONTEXT.md
@.planning/phases/06-state-architecture-foundation/06-RESEARCH.md
@.planning/phases/06-state-architecture-foundation/06-01-SUMMARY.md
@.claude/commands/ant/oracle.md
@.opencode/commands/ant/oracle.md
@tests/unit/validate-state.test.js
@tests/bash/test-session-freshness.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update oracle wizard commands to write structured state files</name>
  <files>.claude/commands/ant/oracle.md, .opencode/commands/ant/oracle.md</files>
  <action>
Update both oracle command files to create the new state files instead of research.json and progress.md. The Claude Code version is at `.claude/commands/ant/oracle.md`; the OpenCode version is at `.opencode/commands/ant/oracle.md`. Both must be updated to maintain command parity.

**Changes to Step 0c (Show Status):**
Replace the status display that reads progress.md + research.json. Instead:
- Check if `.aether/oracle/research-plan.md` exists. If not, show "No Research In Progress."
- If it exists, read `.aether/oracle/research-plan.md` (this is the executive summary — display it directly).
- Also read `.aether/oracle/state.json` for topic and iteration count.
- Display format:
```
Oracle Status
Topic:       {topic from state.json}
Iteration:   {iteration} of {max_iterations}
Status:      {status}

{contents of research-plan.md}

  /ant:oracle stop     Halt the loop
  /ant:oracle          Start new research
```

**Changes to Step 1.5 (Stale Session Check):**
- The session-verify-fresh call already works because Plan 01 updated the file list. No change needed to the bash call itself — it will now check for state.json, plan.json, etc.

**Changes to Step 2 (Configure Research):**
Replace the research.json and progress.md creation with new state file creation.

**Archive previous research:**
Replace the existing archive logic. Check if `.aether/oracle/state.json` exists. If it does:
```bash
ARCHIVE_TS=$(date +%Y-%m-%d-%H%M%S)
mkdir -p .aether/oracle/archive/$ARCHIVE_TS
for f in state.json plan.json gaps.md synthesis.md research-plan.md; do
  [ -f ".aether/oracle/$f" ] && cp ".aether/oracle/$f" ".aether/oracle/archive/$ARCHIVE_TS/"
done
```

**Write state.json** (replaces research.json):
Use the Write tool to create `.aether/oracle/state.json`:
```json
{
  "version": "1.0",
  "topic": "<the research topic>",
  "scope": "<codebase|web|both>",
  "phase": "survey",
  "iteration": 0,
  "max_iterations": <number from depth choice>,
  "target_confidence": <number from confidence choice>,
  "overall_confidence": 0,
  "started_at": "<ISO-8601 UTC timestamp>",
  "last_updated": "<ISO-8601 UTC timestamp>",
  "status": "active"
}
```

**Write plan.json** (new — replaces questions in research.json):
Break the topic into 3-8 sub-questions (adapt to topic complexity — broader topics get more questions, focused topics fewer). Use the Write tool to create `.aether/oracle/plan.json`:
```json
{
  "version": "1.0",
  "questions": [
    {
      "id": "q1",
      "text": "<specific research question>",
      "status": "open",
      "confidence": 0,
      "key_findings": [],
      "iterations_touched": []
    }
  ],
  "created_at": "<ISO-8601 UTC timestamp>",
  "last_updated": "<ISO-8601 UTC timestamp>"
}
```

**Write gaps.md** (new — initial empty structure):
```markdown
# Knowledge Gaps

## Open Questions
(No research conducted yet)

## Contradictions
(None identified)

## Last Updated
Iteration 0 -- <ISO-8601 UTC timestamp>
```

**Write synthesis.md** (new — initial empty structure):
```markdown
# Research Synthesis

## Topic
<the research topic>

## Findings by Question
(No findings yet -- research has not started)

## Last Updated
Iteration 0 -- <ISO-8601 UTC timestamp>
```

**Generate research-plan.md:**
After writing plan.json, generate research-plan.md as the executive summary. Per user decision: big picture only, a few scannable lines. Structure:
```markdown
# Research Plan

**Topic:** <topic>
**Status:** active | **Iteration:** 0 of <max>
**Overall Confidence:** 0%

## Questions
| # | Question | Status | Confidence |
|---|----------|--------|------------|
| q1 | <question text> | open | 0% |
| q2 | ... | open | 0% |

## Next Steps
Next investigation: <text of q1, the first question>

---
*Generated from plan.json -- do not edit directly*
```

**Changes to Step 2.5 (Verify Oracle Files Are Fresh):**
Update the fresh count check from `fresh_count -lt 2` to `fresh_count -lt 5` (now expecting 5 files: state.json, plan.json, gaps.md, synthesis.md, research-plan.md).

**Changes to Step 3 (Launch):**
Update the research configuration summary to show the sub-questions from plan.json. Change "Results will accumulate in .aether/oracle/progress.md" to "Research progress visible at .aether/oracle/research-plan.md". Update the tmux fallback message similarly.

**OpenCode parity:**
Apply identical changes to `.opencode/commands/ant/oracle.md`. The OpenCode version uses `$normalized_args` instead of `$ARGUMENTS` (Step -1 normalizes args). The rest of the logic should be identical. Copy all Step 0c, Step 2, Step 2.5, and Step 3 changes verbatim.

Do NOT change: Step 0 (argument parsing), Step 0b (stop), Step 0.5 (visual mode), Step 1 (research wizard questions), Step 3 launch mechanics (tmux detection). These are unchanged.
  </action>
  <verify>
Run: `grep "state.json" .claude/commands/ant/oracle.md` — should find references to new state file.

Run: `grep "research.json" .claude/commands/ant/oracle.md` — should find NO references to old file (except possibly in archive/migration comments).

Run: `grep "progress.md" .claude/commands/ant/oracle.md` — should find NO references to old file.

Run: `grep "state.json" .opencode/commands/ant/oracle.md` — OpenCode version also updated.

Run: `grep "research-plan.md" .claude/commands/ant/oracle.md` — should find references to the generated summary.
  </verify>
  <done>Both oracle wizard commands create state.json, plan.json, gaps.md, synthesis.md, and research-plan.md on session start. Status display reads from research-plan.md. No references to progress.md or research.json remain. Claude Code and OpenCode commands are in parity.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for oracle state validation and lifecycle</name>
  <files>tests/unit/oracle-state.test.js, tests/bash/test-oracle-state.sh</files>
  <action>
Create two test files covering the oracle state validation subcommand and state file lifecycle.

**tests/unit/oracle-state.test.js** (ava unit tests):

Follow the exact pattern from `tests/unit/validate-state.test.js` — use `require('ava')`, `execSync`, temp directories with `fs.mkdtempSync`, and `t.teardown` for cleanup. Use the `ORACLE_DIR` env var override to point validate-oracle-state at temp directories.

Test cases for `validate-oracle-state state`:
1. **Valid state.json passes validation** — create a complete valid state.json in temp dir, run `ORACLE_DIR="$tmpDir" bash aether-utils.sh validate-oracle-state state`, assert `result.pass === true`
2. **Missing required field fails** — create state.json missing `topic`, assert `result.pass === false` and checks include a fail message mentioning "topic"
3. **Wrong type fails** — create state.json with `iteration: "not-a-number"`, assert `result.pass === false`
4. **Missing file returns error** — run against empty temp dir, assert error (json_err pattern)

Test cases for `validate-oracle-state plan`:
5. **Valid plan.json passes** — create complete valid plan.json with 3 questions, assert `result.pass === true`
6. **Question missing required fields fails** — create plan.json with a question missing `status`, assert `result.pass === false`
7. **Invalid status value fails** — create plan.json with `status: "skipped"`, assert `result.pass === false` (skipped is not a valid status per user decision)
8. **Confidence out of range fails** — create plan.json with `confidence: 150`, assert `result.pass === false`
9. **Too many questions fails** — create plan.json with 9 questions (max is 8), assert `result.pass === false`
10. **Zero questions fails** — create plan.json with empty questions array, assert `result.pass === false`

Test cases for `validate-oracle-state all`:
11. **Both valid passes** — create valid state.json + plan.json, run `validate-oracle-state all`, assert overall pass
12. **One invalid fails overall** — create valid state.json + invalid plan.json, assert overall fail

Use this minimal valid state.json for tests:
```json
{
  "version": "1.0",
  "topic": "Test topic",
  "scope": "codebase",
  "phase": "survey",
  "iteration": 0,
  "max_iterations": 15,
  "target_confidence": 95,
  "overall_confidence": 0,
  "started_at": "2026-03-13T00:00:00Z",
  "last_updated": "2026-03-13T00:00:00Z",
  "status": "active"
}
```

Use this minimal valid plan.json:
```json
{
  "version": "1.0",
  "questions": [
    {"id": "q1", "text": "Test question?", "status": "open", "confidence": 0, "key_findings": [], "iterations_touched": []}
  ],
  "created_at": "2026-03-13T00:00:00Z",
  "last_updated": "2026-03-13T00:00:00Z"
}
```

**tests/bash/test-oracle-state.sh** (bash integration tests):

Follow the pattern from `tests/bash/test-session-freshness.sh` — use the same `run_test`, `setup_tmpdir`, `cleanup_tmpdir` helpers and PASS/FAIL counting.

Test cases:
1. **session-verify-fresh finds new oracle files** — create state.json + plan.json + gaps.md + synthesis.md + research-plan.md in temp dir, run session-verify-fresh, assert ok=true
2. **session-verify-fresh reports missing files** — create only state.json, assert missing list includes plan.json etc.
3. **session-clear removes oracle files** — create all 5 state files + .stop in temp dir, run session-clear, assert files are gone
4. **session-clear preserves archive directory** — create state files + archive/ subdirectory, run session-clear, assert archive/ still exists
5. **generate_research_plan produces valid markdown** — create valid state.json + plan.json in temp dir, source oracle.sh's generate_research_plan function (or replicate it inline), verify output contains expected table headers and question text

Make the bash test file executable (chmod +x).

Print a summary line at the end: `echo "Oracle State Tests: $TESTS_PASSED passed, $TESTS_FAILED failed out of $TESTS_RUN"` and exit with code 1 if any failed.
  </action>
  <verify>
Run: `npx ava tests/unit/oracle-state.test.js --timeout=30s` — all tests should pass.

Run: `bash tests/bash/test-oracle-state.sh` — all tests should pass.

Run: `npx ava tests/unit/oracle-state.test.js --timeout=30s 2>&1 | tail -5` — confirm test count matches expected (12 tests).
  </verify>
  <done>12 ava unit tests pass covering validate-oracle-state state/plan/all with valid data, missing fields, wrong types, invalid enums, out-of-range values, and boundary conditions. 5 bash integration tests pass covering session-verify-fresh, session-clear, and research-plan.md generation with the new oracle file set. State files pass jq validation after creation and after simulated iteration updates (satisfying Phase 6 success criterion 4).</done>
</task>

</tasks>

<verification>
1. `npx ava tests/unit/oracle-state.test.js --timeout=30s` — all 12 tests pass
2. `bash tests/bash/test-oracle-state.sh` — all 5 tests pass
3. `grep "state.json" .claude/commands/ant/oracle.md` — wizard references new files
4. `grep "research.json\|progress.md" .claude/commands/ant/oracle.md` — no old file references
5. `grep "state.json" .opencode/commands/ant/oracle.md` — OpenCode parity maintained
6. `npm run lint:sync` — sync check passes (both oracle commands updated)
</verification>

<success_criteria>
- Oracle wizard (both Claude Code and OpenCode) creates state.json, plan.json, gaps.md, synthesis.md, and research-plan.md on session start
- research-plan.md is an executive summary: topic, status, iteration, confidence, questions table, next steps — per user decision
- Topic decomposes into 3-8 sub-questions with open/partial/answered status in plan.json
- Oracle status command displays research-plan.md content instead of progress.md tail
- 12 ava unit tests validate the validate-oracle-state subcommand thoroughly
- 5 bash integration tests validate session management with new file set
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-state-architecture-foundation/06-02-SUMMARY.md`
</output>
