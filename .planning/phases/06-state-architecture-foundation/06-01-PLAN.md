---
phase: 06-state-architecture-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/aether-utils.sh
  - .aether/oracle/oracle.sh
  - .aether/oracle/oracle.md
autonomous: true
requirements:
  - LOOP-01
  - INTL-01

must_haves:
  truths:
    - "Oracle state is validated via a dedicated validate-oracle-state subcommand in aether-utils.sh"
    - "session-verify-fresh and session-clear reference the new state files (state.json, plan.json, gaps.md, synthesis.md, research-plan.md) instead of progress.md and research.json"
    - "oracle.sh archives new state files on topic change and adds basic jq validation after each iteration"
    - "oracle.md instructs each iteration to read/write structured state files instead of appending to progress.md"
  artifacts:
    - path: ".aether/aether-utils.sh"
      provides: "validate-oracle-state subcommand with state and plan sub-targets, updated session-verify-fresh and session-clear oracle file lists"
      contains: "validate-oracle-state"
    - path: ".aether/oracle/oracle.sh"
      provides: "Updated archive list covering new state files, basic jq validation after iteration, reads state.json instead of research.json"
      contains: "state.json"
    - path: ".aether/oracle/oracle.md"
      provides: "Rewritten prompt that reads state.json + plan.json + gaps.md + synthesis.md and writes updates to them"
      contains: "state.json"
  key_links:
    - from: ".aether/oracle/oracle.sh"
      to: ".aether/oracle/state.json"
      via: "jq read for topic/config at startup"
      pattern: "jq.*state\\.json"
    - from: ".aether/oracle/oracle.md"
      to: ".aether/oracle/plan.json"
      via: "iteration reads plan.json for sub-questions"
      pattern: "plan\\.json"
    - from: ".aether/aether-utils.sh"
      to: ".aether/oracle/state.json"
      via: "validate-oracle-state subcommand validates JSON schema"
      pattern: "validate-oracle-state"
---

<objective>
Create the structured state file infrastructure for the oracle: a validate-oracle-state subcommand in aether-utils.sh, updated session management file lists, updated oracle.sh orchestrator, and rewritten oracle.md iteration prompt.

Purpose: Replace the oracle's flat progress.md append model with structured JSON state files that enable gap-targeted, deepening research across iterations. This is the data layer foundation that Phase 7 (prompts) and Phase 8 (convergence) build on.

Output: Three modified files providing oracle state validation, updated session management, updated loop orchestrator, and a rewritten iteration prompt.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-state-architecture-foundation/06-CONTEXT.md
@.planning/phases/06-state-architecture-foundation/06-RESEARCH.md
@.aether/aether-utils.sh
@.aether/oracle/oracle.sh
@.aether/oracle/oracle.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validate-oracle-state subcommand and update session file lists in aether-utils.sh</name>
  <files>.aether/aether-utils.sh</files>
  <action>
Add a new `validate-oracle-state` subcommand to aether-utils.sh, placed immediately after the existing `validate-state` case block (after line ~1201). Follow the exact same pattern as `validate-state colony` — using the `chk` and `opt` jq helper functions for field type assertions.

The subcommand should accept sub-targets:

**`validate-oracle-state state`**: Validates `.aether/oracle/state.json` (using `ORACLE_DIR` env var override, defaulting to `.aether/oracle`). Required fields:
- `version` (string)
- `topic` (string)
- `scope` (string) — must be one of: codebase, web, both
- `phase` (string) — must be one of: survey, investigate, synthesize, verify
- `iteration` (number)
- `max_iterations` (number)
- `target_confidence` (number)
- `overall_confidence` (number)
- `started_at` (string)
- `last_updated` (string)
- `status` (string) — must be one of: active, complete, stopped

**`validate-oracle-state plan`**: Validates `.aether/oracle/plan.json`. Required fields:
- `version` (string)
- `questions` (array) — must have 1-8 elements, each with `id` (string), `text` (string), `status` (string, one of open/partial/answered), `confidence` (number, 0-100), `key_findings` (array), `iterations_touched` (array)
- `created_at` (string)
- `last_updated` (string)

**`validate-oracle-state all`**: Runs both state and plan validation, returns combined result with `pass: true` only if both pass. Follow the same `jq -s` combination pattern as `validate-state all`.

Use `json_ok` for output wrapping (consistent with validate-state). Use `json_err "$E_FILE_NOT_FOUND"` if the file doesn't exist.

**Then update session-verify-fresh** (line ~6488-6490):
Change the oracle case from:
```
required_docs="progress.md research.json"
```
to:
```
required_docs="state.json plan.json gaps.md synthesis.md research-plan.md"
```

**Then update session-clear** (line ~6602-6606):
Change the oracle case from:
```
files="progress.md research.json .stop"
subdir_files="discoveries/*"
```
to:
```
files="state.json plan.json gaps.md synthesis.md research-plan.md .stop .last-topic"
subdir_files="discoveries/*"
```
Keep the discoveries subdirectory clearing logic as-is.
  </action>
  <verify>
Run: `bash .aether/aether-utils.sh validate-oracle-state 2>&1` — should return a usage error message (no sub-target provided), confirming the subcommand exists.

Run: `grep -n "validate-oracle-state" .aether/aether-utils.sh` — should show the new case block.

Run: `grep -A1 "oracle)" .aether/aether-utils.sh | grep "state.json"` — should confirm session-verify-fresh references new files.
  </verify>
  <done>validate-oracle-state subcommand exists with state, plan, and all sub-targets. session-verify-fresh and session-clear both reference the new oracle state files. Old file references (progress.md, research.json) are removed from session management.</done>
</task>

<task type="auto">
  <name>Task 2: Update oracle.sh orchestrator for new state files</name>
  <files>.aether/oracle/oracle.sh</files>
  <action>
Rewrite oracle.sh to work with the new state file structure. Keep the same overall loop pattern (RALF iteration spawning, stop signal, completion detection) but update all file references.

**File reference changes:**
- Replace `RESEARCH_FILE="$SCRIPT_DIR/research.json"` with `STATE_FILE="$SCRIPT_DIR/state.json"` and `PLAN_FILE="$SCRIPT_DIR/plan.json"`
- Replace `PROGRESS_FILE="$SCRIPT_DIR/progress.md"` with `GAPS_FILE="$SCRIPT_DIR/gaps.md"`, `SYNTHESIS_FILE="$SCRIPT_DIR/synthesis.md"`, `RESEARCH_PLAN_FILE="$SCRIPT_DIR/research-plan.md"`
- Keep STOP_FILE, ARCHIVE_DIR, DISCOVERIES_DIR as-is

**Config reading (lines 26-34):**
- Read from state.json instead of research.json: `jq -r '.topic // empty' "$STATE_FILE"`, `.target_confidence`, `.max_iterations`

**Archive logic (lines 50-68):**
- Replace the `.last-topic` file approach. Instead, check if `state.json` exists and compare its `.topic` field with the new topic. The wizard (oracle.md command) will pass the topic; for now, read from state.json directly.
- Archive ALL new state files: state.json, plan.json, gaps.md, synthesis.md, research-plan.md
- Use timestamped folder: `ARCHIVE_FOLDER="$ARCHIVE_DIR/$(date +%Y-%m-%d-%H%M%S)"`
- After archiving, do NOT create empty files — the wizard handles initial file creation

**Progress file initialization (lines 76-79):**
- Remove the old progress.md initialization. The wizard creates all files before launching oracle.sh.
- Add a check: if state.json doesn't exist, error and exit.

**After each iteration (after line 110, inside the loop):**
- Add basic jq validation: `if ! jq -e . "$STATE_FILE" >/dev/null 2>&1; then echo "WARNING: state.json is invalid JSON after iteration $i"; fi`
- Same for plan.json: `if ! jq -e . "$PLAN_FILE" >/dev/null 2>&1; then echo "WARNING: plan.json is invalid JSON after iteration $i"; fi`
- These are safety checks only — Phase 8 adds recovery logic.

**After each iteration, regenerate research-plan.md:**
Add a `generate_research_plan` function that reads state.json and plan.json to produce research-plan.md as an executive summary. Per user decision: big picture only, a few scannable lines. Structure:
```markdown
# Research Plan

**Topic:** {topic}
**Status:** {status} | **Iteration:** {iteration} of {max_iterations}
**Overall Confidence:** {overall_confidence}%

## Questions
| # | Question | Status | Confidence |
|---|----------|--------|------------|
| q1 | What is X? | open | 0% |

## Next Steps
Next investigation: {lowest-confidence non-answered question text, or "All questions answered"}

---
*Generated from plan.json -- do not edit directly*
```
Call this function after each iteration completes (before the sleep).

**Final message (line 132):**
- Change "Check $PROGRESS_FILE" to "Check $RESEARCH_PLAN_FILE for current research status."

Keep the overall structure: set -e, unset CLAUDECODE, AI CLI detection, main loop with stop signal, completion signal detection. Do not change the loop mechanics or AI invocation pattern.
  </action>
  <verify>
Run: `bash -n .aether/oracle/oracle.sh` — should parse without syntax errors.

Run: `grep "state.json" .aether/oracle/oracle.sh` — should find references to the new state file.

Run: `grep "progress.md\|research.json" .aether/oracle/oracle.sh` — should find NO references to old files (except possibly in comments).

Run: `grep "generate_research_plan" .aether/oracle/oracle.sh` — should find the function definition and at least one call.
  </verify>
  <done>oracle.sh reads config from state.json, archives all new state files on topic change, validates JSON after each iteration, and regenerates research-plan.md after each iteration. No references to progress.md or research.json remain.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite oracle.md iteration prompt for structured state files</name>
  <files>.aether/oracle/oracle.md</files>
  <action>
Rewrite oracle.md (the prompt fed to each AI iteration) to read and write the new structured state files instead of appending to progress.md. Keep the same overall role (Oracle Ant deep research agent) but completely replace the instructions.

**New structure:**

```
You are an **Oracle Ant** - a deep research agent in the Aether Colony.

## Your Mission

Research a topic thoroughly. Each iteration targets knowledge gaps and deepens understanding.

## Instructions

### Step 1: Read State Files
Read these files to understand the current research state:
- `.aether/oracle/state.json` — Session metadata (topic, scope, iteration count, phase, confidence)
- `.aether/oracle/plan.json` — Sub-questions with status and confidence
- `.aether/oracle/gaps.md` — Current knowledge gaps and contradictions
- `.aether/oracle/synthesis.md` — Accumulated findings organized by question

### Step 2: Identify Target
From plan.json, find the sub-question with the LOWEST confidence that is NOT "answered". This is your target for this iteration. If all questions are "answered", proceed to Step 6.

### Step 3: Research
Research the target question deeply using available tools (Glob, Grep, Read, WebFetch). Focus on:
- Filling the specific knowledge gap for this question
- Finding evidence that increases or decreases confidence
- Identifying contradictions with existing findings

### Step 4: Update State Files
After researching, update these files:

**plan.json:** Update the target question:
- Set `status` to "partial" if you found useful information but gaps remain, or "answered" if the question is thoroughly addressed
- Update `confidence` (0-100) based on evidence quality
- Add brief key findings to `key_findings` array
- Add current iteration number to `iterations_touched` array
- If a question turns out to be IRRELEVANT to the topic, REMOVE it from the questions array entirely. Do not leave irrelevant questions.
- Do NOT add new questions. Work through the original plan.
- Write the COMPLETE updated plan.json (not a partial update)

**gaps.md:** Rewrite with current state:
- List remaining open questions with their confidence levels under "## Open Questions"
- Note any contradictions discovered under "## Contradictions"
- Update the "## Last Updated" line with current iteration number and timestamp

**synthesis.md:** Update the findings section for the question you worked on:
- Keep the "## Findings by Question" structure
- Add new findings under the relevant question heading
- Include question status and confidence in the heading
- Do not remove findings from other questions

**state.json:** Update:
- `last_updated` to current ISO-8601 UTC timestamp
- `overall_confidence` to the average of all remaining questions' confidence values (exclude removed questions)
- Do NOT change `iteration` (oracle.sh handles this)
- Do NOT change `phase` (Phase 7 will manage phase transitions)

### Step 5: Rate Confidence
State your assessment: "Confidence: X% — {brief reason}"

### Step 6: Check Completion
If overall_confidence >= target_confidence (from state.json) OR all remaining questions are "answered":
Output `<oracle>COMPLETE</oracle>`
Otherwise, end normally for another iteration.

## Important Rules
- Target ONE question per iteration (the lowest-confidence non-answered question)
- Write COMPLETE JSON files, not partial updates (prevents corruption)
- Do NOT add new sub-questions — work through the original plan
- Remove irrelevant questions entirely — do not mark them as "skipped"
- Do NOT modify any code files or colony state
- Only write to `.aether/oracle/` directory
```

This prompt replaces the old flat-append model entirely. Per user decision: questions are mostly fixed, irrelevant ones are removed (not skipped), and the plan does not expand mid-research.
  </action>
  <verify>
Read `.aether/oracle/oracle.md` and confirm:
1. References state.json, plan.json, gaps.md, synthesis.md (not progress.md or research.json)
2. Contains instruction to target lowest-confidence question
3. Contains instruction NOT to add new questions
4. Contains instruction to REMOVE irrelevant questions
5. Contains instruction to write COMPLETE JSON files
  </verify>
  <done>oracle.md instructs each iteration to read structured state files, target the lowest-confidence gap, update all four state files with findings, and never expand the question set. The flat progress.md append model is completely replaced.</done>
</task>

</tasks>

<verification>
1. `bash -n .aether/oracle/oracle.sh` — no syntax errors in updated orchestrator
2. `grep "validate-oracle-state" .aether/aether-utils.sh` — subcommand exists
3. `grep "state.json" .aether/oracle/oracle.sh` — orchestrator uses new files
4. `grep -c "progress.md" .aether/oracle/oracle.sh` — should be 0 (no old references)
5. `grep "state.json" .aether/oracle/oracle.md` — prompt references new files
6. `grep -c "progress.md" .aether/oracle/oracle.md` — should be 0 (no old references)
7. `grep "state.json plan.json" .aether/aether-utils.sh` — session management updated
</verification>

<success_criteria>
- validate-oracle-state subcommand validates state.json and plan.json with jq type checks following the validate-state colony pattern
- session-verify-fresh and session-clear reference new oracle state files (state.json, plan.json, gaps.md, synthesis.md, research-plan.md)
- oracle.sh reads state.json for config, archives all new files, validates JSON after iterations, and regenerates research-plan.md
- oracle.md instructs iterations to read/write structured state files with gap-targeted research
- No references to progress.md or research.json remain in active code paths
</success_criteria>

<output>
After completion, create `.planning/phases/06-state-architecture-foundation/06-01-SUMMARY.md`
</output>
