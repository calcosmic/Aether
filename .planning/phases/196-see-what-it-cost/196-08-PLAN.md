---
phase: 196-see-what-it-cost
plan: 08
type: execute
wave: 4
depends_on: ["196-01", "196-05", "196-06", "196-07"]
files_modified:
  - cmd/spend_pipeline_e2e_test.go
  - cmd/branch_disposition_test.go
  - .planning/phases/196-see-what-it-cost/196-BRANCH-DISPOSITION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [COST-05, COST-01, COST-02, COST-03]

must_haves:
  truths:
    - "COST-05: all three preserved branches are gone from the repository, each with a written reason recorded first."
    - "The whole path is reachable end to end: a finished run leaves rows, the detail view shows them, and the closeout prints one line over them."
    - "No symbol added by this phase is code with no caller."
    - "D-01 as amended still holds at the end of the phase: the closeout total matches the measured rows on disk, and no length-derived figure has crept back in."
    - "The disposition record names every branch, its commit, its verdict and its reason, so the deletions remain auditable."
    - "The roadmap's criterion 1 and COST-01 no longer describe the superseded estimated-figure shape, so a verifier reading them cannot mark correct code as failing."
  artifacts:
    - path: ".planning/phases/196-see-what-it-cost/196-BRANCH-DISPOSITION.md"
      provides: "The written reason and recorded commit for each of the three branches"
    - path: "cmd/spend_pipeline_e2e_test.go"
      provides: "The end-to-end reachability proof from finished run to rendered line"
  key_links:
    - from: "cmd/spend_pipeline_e2e_test.go"
      to: "cmd/spend_writer.go"
      via: "drives a real finalize and reads the resulting rows back"
      pattern: "writeSpendRowsForRun"
    - from: ".planning/phases/196-see-what-it-cost/196-BRANCH-DISPOSITION.md"
      to: "git"
      via: "records each branch tip commit before the branch is deleted"
      pattern: "worktree-agent-"
---

<objective>
Prove the whole cost path runs end to end, then delete the three preserved
branches with their written reasons recorded.

Purpose: this repository's documented failure mode is capability that exists and
is never called — eighteen of twenty-five milestones have been framed around
restoring something previously marked done. Every part of this phase passes its
own tests; this plan is the one that asks whether they are joined up. Criterion 5
is then satisfied by actually running the deletions, not by citing the reasons
for them.
Output: an end-to-end proof, an anti-orphan check, a disposition record, and
three fewer branches.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-see-what-it-cost/196-CONTEXT.md
@.planning/phases/196-see-what-it-cost/196-SALVAGE-ASSESSMENT.md
@.planning/phases/196-see-what-it-cost/196-05-SUMMARY.md
@.planning/phases/196-see-what-it-cost/196-07-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
The three branches and their assessed verdicts:

- `worktree-agent-a47f78913caf6fde0` — the ledger. Salvaged at file level in plan
  196-01 with four fixes. Delete.
- `worktree-agent-aa57076cd698da76f` — the OpenCode reader. Salvaged at file
  level in plan 196-05, inside the plan that wired it, with its crash removed.
  Delete.
- `worktree-agent-a59fd3ee68644ea21` — a test file for an implementation that has
  never existed in any ref. Nothing merged; its six requirements were copied into
  plan 196-03 and satisfied there by named tests. Delete.

Record each branch's tip commit in the disposition file before deleting, so the
work remains recoverable by commit even after the branch name is gone.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the path is joined up and nothing is orphaned</name>
  <files>cmd/spend_pipeline_e2e_test.go</files>
  <read_first>
    - CLAUDE.md — Definition of Done, and the four recorded cases of work marked complete whose wiring was never added
    - .planning/phases/196-see-what-it-cost/196-05-SUMMARY.md — the writer and resolver call sites as actually landed
    - .planning/phases/196-see-what-it-cost/196-06-SUMMARY.md — the detail view's command name and output shape
    - .planning/phases/196-see-what-it-cost/196-07-SUMMARY.md — the cost line renderer and which surfaces call it
    - cmd/subcommand_reachability_ratchet_test.go — the caller-evidence rules and the shrink-only allowlist
    - cmd/spend_pipeline_e2e_test.go — does not exist yet; model it on the existing end-to-end tests in this package
  </read_first>
  <behavior>
    - Driving a build finalize in a temporary store leaves per-worker rows on disk without any test reaching into the writer directly.
    - The detail view, run afterwards in the same store, shows those same workers and figures.
    - The closeout for that run renders exactly one cost line whose total matches the rows on disk.
    - Doing the same for a continue leaves separate rows and renders its own single line.
    - No function, type or command added by this phase is unreachable from a production path, and the shrink-only orphan allowlist has gained no entry.
    - The proof fails if any single link is removed — the writer call, the resolver call, the renderer call, or the command registration.
  </behavior>
  <action>
Write one end-to-end test that drives the real entry points in a temporary store
and asserts at each hop: rows on disk after the finalize, the same workers and
figures in the detail view, and one cost line in the closeout whose total agrees
with the rows. Drive the commands as a user would, through the command tree, not
by calling internal helpers — a test that calls the writer directly proves the
writer works and proves nothing about whether a build calls it.

Then remove each link in turn in a scratch copy and confirm the test fails for
each. Record which link produced which failure in the summary. A single test that
passes when a link is cut is worse than no test, and this subsystem has already
shipped one.

Confirm the orphan ratchet reports every command added by this phase as reached,
with no new allowlist entry, and that the allowlist has only shrunk or stayed the
same.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestSpendPipelineIsReachableEndToEnd' -count=1` passes
    - Cutting the writer call, the resolver call, the renderer call or the command registration each fails it, verified in a scratch copy and recorded in the summary
    - `go test ./cmd -run 'TestOrphanAllowlistOnlyShrinks|TestNoOrphanRuntimeCommands' -count=1` passes with no new entry
    - The total the closeout prints equals the total computed from the rows read back off disk
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(SpendPipelineIsReachableEndToEnd|OrphanAllowlistOnlyShrinks|NoOrphanRuntimeCommands)' -count=1</automated>
  </verify>
  <done>The cost path is proven joined up from a finished run to the line the owner reads, and nothing this phase added sits uncalled.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the superseded wording, record the disposition, delete the three branches</name>
  <files>.planning/phases/196-see-what-it-cost/196-BRANCH-DISPOSITION.md, cmd/branch_disposition_test.go, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/196-see-what-it-cost/196-SALVAGE-ASSESSMENT.md — the verdict table, the written reason for the deleted branch, and the revive-cost reasoning behind each verdict
    - .planning/phases/196-see-what-it-cost/196-CONTEXT.md — D-03, and the requirement that none be left dangling
    - .planning/phases/196-see-what-it-cost/196-01-SUMMARY.md and 196-05-SUMMARY.md — confirmation that the salvaged files actually landed
    - .planning/ROADMAP.md — the Phase 196 success criterion 1, still worded for the superseded measured-versus-estimated shape
    - .planning/REQUIREMENTS.md — COST-01, worded the same superseded way
  </read_first>
  <behavior>
    - The roadmap's criterion 1 and COST-01 describe the shipped behaviour: no figure for an unreported worker, and a total counting only measured workers.
    - The disposition file names all three branches, each with its tip commit, its verdict, the plan that salvaged it if any, and a written reason in plain English.
    - A test fails if the file omits any of the three branches or leaves any reason empty.
    - After the deletions, listing branches by those three names returns nothing.
    - The salvaged files are present and their tests pass, verified before any branch is deleted.
  </behavior>
  <action>
First correct the two places that still describe the superseded cost-line shape:
the roadmap's criterion 1 for this phase, and the requirement line it came from.
Both promise a measured-versus-estimated label, which the amended D-01 abolished.
Left standing, a verifier reading them literally would mark the shipped, correct
behaviour as a failure. Change the wording only — the criterion's named test and
its no-currency clause stay exactly as they are — and cite the amendment so the
change reads as a correction rather than a quiet loosening.

Then write the disposition file. For each branch record its name, its tip commit
recorded before deletion, whether its content was salvaged or discarded, which
plan salvaged it, and one paragraph in ordinary words saying why. The reason for
the discarded branch is already written in the salvage assessment and should be
carried across rather than reworded.

Add the test that reads the file and fails if any of the three branch names is
missing, if any commit is missing, or if any reason is empty. That is the command
that fails when this requirement is unmet, which is what makes it done rather
than claimed.

Only then delete the three branches. Before deleting, confirm the salvaged files
are present in the tree and their tests pass, so nothing is discarded that is not
already safely landed. Recording the commit first means the work stays reachable
by commit even after the name is gone.
  </action>
  <acceptance_criteria>
    - The roadmap criterion 1 and COST-01 no longer promise an estimated figure, and both still name their existing tests
    - `go test ./cmd -run 'TestBranchDispositionRecordsAllThreeBranches' -count=1` passes, and fails if any branch, commit or reason is removed from the file
    - `test -z "$(git branch --list 'worktree-agent-a47f78913caf6fde0' 'worktree-agent-aa57076cd698da76f' 'worktree-agent-a59fd3ee68644ea21')"` succeeds
    - The salvaged files are present and `go test ./cmd -run 'Test(SpendLedger|OpenCode)' -count=1` passes before the deletions are run
    - No branch is deleted whose tip commit is not recorded in the disposition file
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestBranchDispositionRecordsAllThreeBranches' -count=1 && test -z "$(git branch --list 'worktree-agent-a47f78913caf6fde0' 'worktree-agent-aa57076cd698da76f' 'worktree-agent-a59fd3ee68644ea21')"</automated>
  </verify>
  <done>The written criteria describe what shipped, and all three branches are gone, each with its reason and its commit written down first.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(SpendPipelineIsReachableEndToEnd|BranchDisposition|Orphan|NoTokenCountIsDerivedFromLength)' -count=1`
- `go test ./... -count=1` once, as the phase-closing full run
- `git branch --list 'worktree-agent-a47f*' 'worktree-agent-aa57*' 'worktree-agent-a59f*'` prints nothing
- `go build ./cmd/aether && go vet ./...`
</verification>

<success_criteria>
- The cost path is proven reachable end to end, link by link.
- Nothing this phase added is uncalled.
- No written criterion still describes the superseded cost-line shape.
- The three branches are deleted, each with a written reason and a recorded commit.
</success_criteria>

<output>
Create `.planning/phases/196-see-what-it-cost/196-08-SUMMARY.md` when done.
</output>
