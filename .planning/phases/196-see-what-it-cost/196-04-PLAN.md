---
phase: 196-see-what-it-cost
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/codex_visuals.go
  - cmd/caste_model_reason.go
  - cmd/caste_model_test.go
  - cmd/ceremony_team_checkin.go
  - cmd/ceremony_team_checkin_test.go
  - .claude/agents/ant/aether-chronicler.md
  - .claude/agents/ant/aether-keeper.md
  - .claude/agents/ant/aether-includer.md
autonomous: true
requirements: [COST-04]

must_haves:
  truths:
    - "D-02: the documentation writer, the knowledge-keeper and the accessibility checker run on the cheaper model, pinned in their own agent files."
    - "D-02: no worker role resolves to whatever model happened to run last."
    - "D-02: every role kept on the expensive model carries a written reason, and that reason is shown on the pre-build team card."
    - "The reason is written for someone who has never opened a file: no invented repo vocabulary."
    - "This does not reopen automatic model routing, which the owner declined on 2026-07-28."
  artifacts:
    - path: "cmd/caste_model_reason.go"
      provides: "The written reason each expensive-model role carries, and the resolver the card uses"
    - path: "cmd/ceremony_team_checkin.go"
      provides: "Model and reason on every worker line of the pre-build team card"
  key_links:
    - from: "cmd/ceremony_team_checkin.go"
      to: "cmd/caste_model_reason.go"
      via: "the card renders the recorded reason beside the resolved model"
      pattern: "casteModelReason"
    - from: "cmd/codex_visuals.go"
      to: ".claude/agents/ant"
      via: "the display table and the agent frontmatter must agree in both directions"
      pattern: "casteModelSlot"
---

<objective>
Pin the routine roles to the cheaper model and make every expensive choice carry
a reason the owner can read on the team card.

Purpose: criterion 4 is untouched by all three salvaged branches. Three roles
currently run on whatever model happened to run last, which means their cost is
unpredictable and unattributable, and nothing anywhere records why an expensive
role is expensive.
Output: three pinned agent files, a reason for every expensive role, and a team
card that names both.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-see-what-it-cost/196-CONTEXT.md
@CLAUDE.md
@cmd/codex_visuals.go
@cmd/ceremony_team_checkin.go
@cmd/caste_model_test.go
</context>

<interfaces>
Only `.claude/agents/ant/aether-<role>.md` declares a model line; the OpenCode
and Codex agent definitions carry no model field, so they are not touched by
this plan.

`casteModelSlot` in `cmd/codex_visuals.go` stays the display table and stays
locked to the agent files in both directions by the existing parity test. It is
DISPLAY ONLY: nothing in the runtime reads it to choose a model. Routing stays
with the platform's own agent frontmatter. Do not add a code path that selects a
model — automatic model routing was declined by the owner on 2026-07-28 and
stays out of scope.

New in `cmd/caste_model_reason.go`: a table from role to its written reason, and
a resolver returning the reason for a role. The card is its only consumer.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the routine roles and record a reason for every expensive one</name>
  <files>.claude/agents/ant/aether-chronicler.md, .claude/agents/ant/aether-keeper.md, .claude/agents/ant/aether-includer.md, cmd/codex_visuals.go, cmd/caste_model_reason.go, cmd/caste_model_test.go</files>
  <read_first>
    - .planning/phases/196-see-what-it-cost/196-CONTEXT.md — D-02 in full, including what was rejected and what stays out of scope
    - CLAUDE.md — the section on writing for the owner, and the table of invented repo vocabulary that must never appear untranslated in something he reads
    - cmd/codex_visuals.go — casteModelSlot, resolveCasteModel and casteIdentityWithModel, plus the comment explaining why the table is display-only
    - cmd/caste_model_test.go — TestCasteModelSlotMatchesAgentFrontmatter and how it derives the mapping from the agent files in both directions
    - .claude/agents/ant/aether-chronicler.md, aether-keeper.md, aether-includer.md — the model line each declares today
  </read_first>
  <behavior>
    - The documentation writer, the knowledge-keeper and the accessibility checker declare the cheaper model in their own agent files and resolve to it.
    - No role in the display table resolves to the sentinel meaning "whatever ran last".
    - Every role on the expensive model has a non-empty written reason; a role on the expensive model with no reason fails by name.
    - The reason table has no entry for a role that is not on the expensive model, so it cannot go stale in the other direction.
    - No reason contains a word from the repo's own invented vocabulary, tested against a list held in the test file.
    - The existing two-way parity between the display table and the agent files still holds.
  </behavior>
  <action>
Write the tests first and record the failing run.

Change the model line in the three routine roles' agent files to the cheaper
model, and update the display table to match, keeping the existing parity test
green in both directions. Choose the cheaper model by the name the table already
uses for it; do not invent a third name for the same slot.

Add the reason table in a new file. Every role currently on the expensive model
needs an entry saying, in one plain sentence, why that role is worth the more
expensive model — the kind of judgement it makes, and what a cheaper model would
get wrong. Write each sentence for someone who has never opened a file in this
repository: no invented vocabulary, no role name used as though its meaning were
obvious, no file paths.

Add the two named tests. The first asserts the three routine roles are pinned
and that no role anywhere resolves to the sentinel meaning "whatever ran last".
The second asserts every expensive role has a reason, that no reason is empty or
a placeholder, that the table has no entry for a role that is not expensive, and
that no reason contains a word from the invented-vocabulary list the test holds.

Do not add any code that selects or overrides a model at dispatch time.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestRoutineBuilderIsSonnetNeverInherit|TestOpusRequiresRecordedReason|TestCasteModelSlotMatchesAgentFrontmatter' -count=1` passes
    - Removing any one reason fails the second test naming the role
    - Reverting any one of the three agent files to its previous model line fails the first test naming the role
    - No new function chooses a model; the display table's own comment still says so and the parity test still enforces it
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(RoutineBuilderIsSonnetNeverInherit|OpusRequiresRecordedReason|CasteModelSlotMatchesAgentFrontmatter)' -count=1</automated>
  </verify>
  <done>Three routine roles are pinned to the cheaper model, and every expensive role carries a written, plain-English reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Put the model and its reason on the pre-build team card</name>
  <files>cmd/ceremony_team_checkin.go, cmd/ceremony_team_checkin_test.go</files>
  <read_first>
    - .planning/phases/196-see-what-it-cost/196-CONTEXT.md — D-02: the reason is shown on the pre-build team card
    - cmd/ceremony_team_checkin.go — renderCeremonyTeamCheckin, the per-worker line built from casteIdentityWithModel, the required/optional marking, and the returned result map
    - cmd/ceremony_team_checkin_test.go — the existing card assertions this task extends, including the ones from Phase 194 and 195 that must keep passing
    - cmd/caste_model_reason.go — the resolver added in Task 1
  </read_first>
  <behavior>
    - Every worker line on the card names the model that worker runs on.
    - A worker on the expensive model shows its recorded reason on the card; a worker on the cheaper model shows the model without a reason, because there is nothing to justify.
    - The card's returned result map carries the model and the reason as fields, not only inside rendered text, so nothing downstream has to parse prose to know them.
    - The compact one-worker fast-path summary carries the same two facts, since that path never shows the full card.
    - The existing required/optional marking, forced-reviewer signal naming and waiver choices are unchanged.
  </behavior>
  <action>
Write the card assertions first and record the failing run.

Extend the per-worker card line to show the resolved model and, when the role is
on the expensive model, its recorded reason. Put the model and the reason into
the returned result map as their own fields as well as into the rendered line —
a wrapper that needs to narrate this must read fields, never scrape text.

Do the same for the compact fast-path summary a one-worker build shows instead
of the card, so the model and reason are visible on both surfaces rather than
only the one that pauses.

Keep the rest of the card exactly as Phase 194 and 195 left it: the required and
optional marking, the named risk signal beside a forced reviewer, and the waiver
choices. Add a field, do not restructure the card.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestTeamCardNamesModelAndReasonForEveryWorker|TestCheapModelWorkerNeedsNoReasonOnTheCard|TestFastPathSummaryCarriesModelAndReason' -count=1` passes
    - The result map contains a model field and a reason field per worker
    - `go test ./cmd -run 'Test(TeamCheckin|OneWorker|FastPathSummary|RequiredMeansBuilderOrNamedSignal)' -count=1` still passes unchanged
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(TeamCardNamesModelAndReason|CheapModelWorkerNeedsNoReason|FastPathSummaryCarriesModelAndReason|TeamCheckin|RequiredMeansBuilderOrNamedSignal)' -count=1</automated>
  </verify>
  <done>Before anything is spawned, the owner can see which model each worker uses and why an expensive one was kept.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(CasteModel|RoutineBuilder|OpusRequires|TeamCard|TeamCheckin|FastPathSummary)' -count=1`
- `go build ./cmd/aether && go vet ./cmd`
- Render one team card by hand and read it as someone who has never opened a file here: every model and reason must make sense without a glossary.
</verification>

<success_criteria>
- No role runs on whatever model happened to run last.
- Every expensive role's reason is recorded and visible before spawning.
- Nothing in this plan chooses a model at runtime.
</success_criteria>

<output>
Create `.planning/phases/196-see-what-it-cost/196-04-SUMMARY.md` when done.
</output>
