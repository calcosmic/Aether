---
phase: 196-see-what-it-cost
plan: 06
type: execute
wave: 2
depends_on: ["196-01", "196-02"]
files_modified:
  - cmd/spend_cmd.go
  - cmd/spend_cmd_test.go
  - cmd/testdata/command_catalog.json
  - .claude/commands/ant/build.md
  - .claude/commands/ant-build.md
  - .opencode/commands/ant/build.md
  - .claude/commands/ant/continue.md
  - .claude/commands/ant-continue.md
  - .opencode/commands/ant/continue.md
autonomous: true
requirements: [COST-03]

must_haves:
  truths:
    - "Running the spend command shows per-worker token usage for the current run."
    - "Running it changes no file on disk — the inspection path does not mutate state, which CLAUDE.md makes a named rule with its own history here."
    - "Every figure it shows comes from a recorded ledger row; it computes no figure of its own."
    - "D-01 as amended: a row whose tool reported nothing shows no number at all, and the total counts only measured workers."
    - "D-06: the command is surfaced by a plain sentence in the build and continue wrapper output, which is also what satisfies the repo's orphan ratchet."
  artifacts:
    - path: "cmd/spend_cmd.go"
      provides: "The read-only per-worker spend view"
    - path: "cmd/spend_cmd_test.go"
      provides: "The no-mutation proof and the ledger-only-figures proof"
  key_links:
    - from: "cmd/spend_cmd.go"
      to: "cmd/spend_ledger.go"
      via: "reads rows and totals through the ledger's own load and total entry points"
      pattern: "loadSpendLedgersForPhase"
    - from: ".claude/commands/ant/build.md"
      to: "cmd/spend_cmd.go"
      via: "the build and continue wrappers name the command as the next step for detail"
      pattern: "aether spend"
---

<objective>
Add the read-only per-worker spend view, and prove it changes nothing.

Purpose: criterion 3 is at zero today — no such command exists anywhere. Two
inspection paths in this repository have already been caught writing to disk
while their own help text promised they would not, so the no-mutation proof is
part of the feature, not an extra.
Output: the command, its no-mutation proof, and a wrapper that names it so it
does not join the list of things that work and are never called.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-see-what-it-cost/196-CONTEXT.md
@.planning/phases/196-see-what-it-cost/196-01-SUMMARY.md
@CLAUDE.md
@cmd/spend_ledger.go
@cmd/spawn_reap_test.go
</context>

<interfaces>
New cobra command `spend`, registered on the root command in its own file's
init, following the pattern used by the existing ledger and listing commands.

It reads through the ledger's own load and total entry points from plan 196-01.
It must not construct a row, recompute a total, or derive a figure of its own.

Reachability is enforced by the orphan ratchet in
`cmd/subcommand_reachability_ratchet_test.go`: a registered subcommand must be
named by a Go call site, a wrapper the runtime loads, or a shipped hook or
script. The allowlist may only shrink, so adding an entry for this command is
not an option — the wrapper mention is the wiring. Per D-06 the owner declined a
first-class slash command for this view, so the wrapper sentence is the whole of
its surfacing; do not add a command definition file.

The six wrapper files are three byte-identical pairs plus their OpenCode mirrors
and must change identically in this one plan.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The read-only spend view</name>
  <files>cmd/spend_cmd.go, cmd/spend_cmd_test.go</files>
  <read_first>
    - .planning/phases/196-see-what-it-cost/196-CONTEXT.md — D-01 as amended: the per-worker shape, and the ruling that an unreported worker shows no number; and D-06 for how this command is surfaced
    - CLAUDE.md — Definition of Done, and the corollary that an inspection path must not mutate state, with the two cases where it did
    - .planning/phases/196-see-what-it-cost/196-01-SUMMARY.md — the ledger's real load and totals signatures and the two-file phase-and-workflow keying
    - cmd/spawn_reap_test.go — TestSpawnOrphansListingMutatesNothing, and the store hash snapshot helpers it uses; reuse them rather than writing a second snapshot mechanism
    - cmd/review_ledger.go — the house pattern for a small read-only cobra command, its output modes and its registration
    - cmd/spend_ledger.go — loadSpendLedgersForPhase, computeSpendTotals, and the refusal to average over estimates
  </read_first>
  <behavior>
    - With rows recorded for the current phase, the command lists every worker with its role and either its measured token figure or a not-reported mark, plus the total of the measured workers.
    - Running it twice produces byte-identical output and leaves every file in the data directory unchanged, compared by content hash.
    - With no rows recorded it says so in plain words and shows no number, rather than inventing one.
    - Every figure shown equals a seeded ledger value; the command performs no arithmetic of its own beyond what the ledger's own totals entry point returns.
    - A row whose tool reported nothing shows no number of any kind, and is not included in the total; the total counts only measured workers and says so.
    - Both the build-keyed and continue-keyed rows for a phase are shown, and neither erases the other.
  </behavior>
  <action>
Write the tests first and record the failing run. Start with the no-mutation
test: snapshot the content hashes of every file under the store before running,
run the command twice, and assert the snapshot is unchanged both times and the
two outputs are byte-identical. Reuse the existing snapshot helpers rather than
writing a second mechanism — this repository has shipped a mutating inspection
path twice, and the second time the flag's own documentation said it would not.

Implement the command as a pure read: load the phase's rows through the ledger's
load entry point, take totals from the ledger's totals entry point, and render.
Do not construct a row, do not recompute a total, do not touch the store.

Render for someone who has never opened a file here: worker name, what that
worker does in ordinary words, and either the measured token figure or, per D-01
as amended, a dash and the words for not reported. Never show a number for a row
whose tool reported nothing, and never show a zero in its place. Sum only the
measured rows into the total and say that is what the total counts.

Prove the figures come only from recorded rows by seeding a ledger with known
values and asserting the rendered figures equal those literals, and by seeding an
empty ledger and asserting the command shows no figure at all. Seed one row whose
tool reported nothing and assert that its figure column is exactly the dash
sentinel. Assert on the parsed cell, not on the whole line: deterministic worker
names carry digits of their own, so a whole-line digit check fails on correct
output and would be weakened by the next person who hits it.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestSpendDoesNotMutate|TestSpendShowsEveryWorkerRow|TestSpendEmptyLedgerShowsNoFigure|TestSpendShowsNoNumberForUnreportedRows|TestSpendShowsBuildAndContinueRows' -count=1` passes
    - The no-mutation test compares content hashes of the whole store before and after, and asserts two runs are byte-identical
    - Seeded literals appear unchanged in the output and no figure appears that was not seeded
    - `go test ./cmd -run 'TestOrphanAllowlistOnlyShrinks|TestNoTokenCountIsDerivedFromLength' -count=1` passes with no new allowlist entry
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(SpendDoesNotMutate|SpendShowsEveryWorkerRow|SpendEmptyLedgerShowsNoFigure|SpendShowsNoNumberForUnreportedRows|SpendShowsBuildAndContinueRows|OrphanAllowlistOnlyShrinks)' -count=1</automated>
  </verify>
  <done>The owner can ask what a run cost per worker and get an answer that changes nothing and invents nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Make it reachable and refresh the command catalog</name>
  <files>.claude/commands/ant/build.md, .claude/commands/ant-build.md, .opencode/commands/ant/build.md, .claude/commands/ant/continue.md, .claude/commands/ant-continue.md, .opencode/commands/ant/continue.md, cmd/testdata/command_catalog.json</files>
  <read_first>
    - cmd/subcommand_reachability_ratchet_test.go — the three permitted kinds of caller evidence and which wrapper trees count
    - cmd/lifecycle_wrapper_contract_test.go — the byte-identical mirror rule for the six wrapper files
    - cmd/audit_catalog_test.go — the catalog golden and its refresh flag
    - .claude/commands/ant/build.md — the closing next-steps section where the mention belongs
    - .claude/commands/ant/continue.md — the same section on the continue side
  </read_first>
  <behavior>
    - Both the build and the continue wrapper name the new command as the optional next step for per-worker detail, in one plain-English sentence.
    - All six wrapper files remain byte-identical in their pairs.
    - The orphan ratchet finds the command reached, with no allowlist entry added.
    - The command catalog golden includes the new command and its flags.
    - No wrapper gains any state-mutating instruction; the mention is a suggestion to the reader, not a step the wrapper performs.
  </behavior>
  <action>
Per D-06, add one sentence to the closing section of the build wrapper and the
continue wrapper naming the command and saying, in ordinary words, what it shows
— the tokens each worker used on this run. Apply the identical edit to all six files;
they are hand-maintained byte-identical copies with no generator, and the
contract test fails if they drift.

Do not make the wrapper run it. The cost line the owner sees at the end of every
run is rendered by the runtime in plan 196-07; this command is the detail view he
can ask for, and a wrapper that ran it automatically would print the same numbers
twice.

Refresh the command catalog golden so the new command and its flags are recorded.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestAuditCatalogGolden' -count=1` passes after refreshing the golden with its update flag
    - `go test ./cmd -run 'Test(LifecycleWrapper|CommandGuide|Parity)' -count=1` passes, proving the six files still match in their pairs
    - The reachability ratchet reports the command reached and the allowlist is unchanged
    - The added sentence names no file path and no invented vocabulary
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(AuditCatalogGolden|LifecycleWrapper|OrphanAllowlist|NoOrphanRuntimeCommands)' -count=1</automated>
  </verify>
  <done>The command is named by the wrappers the runtime loads, so it cannot become another feature that works and is never called.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(Spend|AuditCatalogGolden|LifecycleWrapper|Orphan)' -count=1`
- `go build ./cmd/aether && go vet ./cmd`
- Run the command by hand against a seeded store and confirm nothing under the data directory changes.
</verification>

<success_criteria>
- The per-worker view exists, reads only, and shows only recorded figures.
- Its no-mutation property is proven by a test, not by its help text.
- A wrapper the runtime loads names it.
</success_criteria>

<output>
Create `.planning/phases/196-see-what-it-cost/196-06-SUMMARY.md` when done.
</output>
