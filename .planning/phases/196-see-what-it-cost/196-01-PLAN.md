---
phase: 196-see-what-it-cost
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/spend_ledger.go
  - cmd/spend_ledger_test.go
autonomous: true
requirements: [COST-03, COST-05]

must_haves:
  truths:
    - "D-03: the salvaged ledger is in the tree, compiling, with its nine original tests still green."
    - "D-01/FIX 1-1: no field on the ledger or its totals relays a currency amount, so no later renderer can put one on the headline."
    - "FIX 1-3: a row records the job name a Phase 195 grouped worker owned, so cost attribution survives one worker covering several tasks."
    - "FIX 1-2: the aggregation invariant is proven against hand-computed literals, not against the same helper the implementation calls."
    - "FIX 1-4: a row's status comes from the dispatch status vocabulary already in the tree, not a second free-form one."
    - "D-01 as amended: no row will ever carry a token figure invented from a length — plan 196-02 removes the only producer, so the ledger's non-measured subtotal is empty by construction."
  artifacts:
    - path: "cmd/spend_ledger.go"
      provides: "Per-worker, per-workflow durable spend rows with measured/estimated subtotals kept apart"
    - path: "cmd/spend_ledger_test.go"
      provides: "The nine salvaged tests plus the four fix regressions"
  key_links:
    - from: "cmd/spend_ledger.go"
      to: "pkg/codex/usage.go"
      via: "spendRow carries codex.WorkerUsage verbatim and totals through BilledTotalTokens"
      pattern: "BilledTotalTokens"
    - from: "cmd/spend_ledger.go"
      to: "pkg/storage"
      via: "schema-versioned, fail-closed save/load under the spend/ store subdirectory"
      pattern: "spend/phase-"
---

<objective>
Bring the salvaged spend ledger into the tree with the four fixes the salvage
assessment made conditions of merge, so every later plan in this phase has one
durable place to write per-worker token usage.

Purpose: this is the storage layer criterion 3 needs and the thing criterion 1
reads from. It is merged first because nothing depends on it and everything
else writes into it.
Output: `cmd/spend_ledger.go` and its tests, with the currency relay removed,
job attribution added, an independent aggregation invariant, and one status
vocabulary rather than two.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-see-what-it-cost/196-CONTEXT.md
@.planning/phases/196-see-what-it-cost/196-SALVAGE-ASSESSMENT.md
@pkg/codex/usage.go
@cmd/spend_session_capture.go
</context>

<interfaces>
The salvaged file is brought in at FILE level, not by merging the branch:
`git checkout worktree-agent-a47f78913caf6fde0 -- cmd/spend_ledger.go cmd/spend_ledger_test.go`.
The branch itself is deleted in plan 196-08, with its written reason.

After this plan, the names later plans depend on are: `spendRow` (fields
AgentName, Caste, ParentName, JobName, Status, Usage), `spendLedger`,
`saveSpendLedger`, `loadSpendLedger`, `loadSpendLedgersForPhase`,
`computeSpendTotals`, `spendPerWorkerAverageTokens`, `spendRollupByParent`.
Store layout stays `spend/phase-<N>-build.json` and
`spend/phase-<N>-continue.json` — two files, so a continue can never erase a
build's rows. Do not change that keying; its reasoning is written out in the
salvaged file and is correct.

`codex.WorkerUsage` stays the authoritative row type. No consumer may re-derive
a token total by adding fields itself; `BilledTotalTokens()` is the only
entry point (Pitfall 1, the 186x undercount).

The salvaged file's source tag vocabulary is unchanged by this plan, so its nine
tests pass as written. Note for context only: after plan 196-02 the tag that
reads as "estimated" means "not measured", because the character-derived figure
that used to fill such a row is deleted at source per D-01 as amended. Nothing in
this plan needs to change for that; do not rename the tag.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land the salvaged ledger and prove its aggregation independently</name>
  <files>cmd/spend_ledger.go, cmd/spend_ledger_test.go</files>
  <read_first>
    - .planning/phases/196-see-what-it-cost/196-SALVAGE-ASSESSMENT.md — Branch 1 section, FIX 1-1 and FIX 1-2 in full, and the "Are the tests real?" weakness
    - .planning/phases/196-see-what-it-cost/196-CONTEXT.md — D-01 (no currency headline, no price table) and the standing constraint on tests that re-implement the code's own arithmetic
    - pkg/codex/usage.go — WorkerUsage fields, Measured/Estimated/Empty, billedTotal, TotalInputTokens, BilledTotalTokens and the comment recording the 186x undercount
    - cmd/spend_session_capture.go — the sibling spend/ store path and its schema-version guard, so the two files agree on conventions
    - cmd/internal_worker_usage_test.go — the house style for usage assertions: every expected number a literal, never a call back into the helper under test
  </read_first>
  <behavior>
    - The nine salvaged tests pass unchanged against today's tree.
    - Anthropic's documented disjoint-column example (50 input, 100,000 cache read, 2,000 cache creation, 500 output) totals 102,550 when recomputed from the four raw columns by hand, with no call to the helper the implementation uses.
    - Measured and estimated subtotals stay in separate fields and are never summed into a figure presented as a measurement.
    - Nothing on the ledger or its totals carries a currency amount: no struct field name and no serialized key matches a currency-name pattern.
    - A derived per-worker average over a row set containing estimates is still refused by name unless explicitly permitted.
  </behavior>
  <action>
Bring `cmd/spend_ledger.go` and `cmd/spend_ledger_test.go` into the working tree
from the salvaged branch at file level, then run the nine existing tests and
record that they pass before changing anything — that green run is the evidence
the assessment's compile claim held.

Then apply FIX 1-1: remove the two totals fields that relay the provider's own
reported monetary cost, and every assignment to them. Nothing reads them. They
are removed rather than kept because criterion 1 forbids a currency figure on
the headline and an unread field is a standing invitation to put one there.
Removing them must not touch `codex.WorkerUsage`, which keeps its own
provider-reported cost field on the shared type.

Then apply FIX 1-2: add one aggregation invariant test that states its expected
total as a hand-computed literal derived from the four raw columns, and that
never calls `BilledTotalTokens` to build its expectation. This is the specific
shape that let the 186x undercount ship green in this same subsystem: an
aggregation test whose expectation is produced by the function it is checking
cannot fail when that function is wrong.

Add a structural assertion that walks the ledger and totals types by reflection
and their marshalled JSON keys, and fails naming any field or key matching a
case-insensitive currency-name pattern. Assert on the type and the serialized
form, not on a text search of the source file, so a comment can never satisfy
or break it.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestSpendLedgerPersistsAcrossProcesses|TestSpendLedgerContinueDoesNotEraseBuild|TestSpendLedgerSameWorkflowRerunReplaces|TestSpendLedgerLoadEdgeCases|TestLedgerMeasuredEstimatedSubtotalsAreSeparate|TestLedgerGrandTotalEqualsSumOfRows|TestLedgerRefusesDerivedMetricOverEstimates|TestLedgerParentRollupCountsEachWorkerOnce|TestLedgerPhaseTotalAddsBuildAndContinue' -count=1` passes
    - `TestLedgerGrandTotalFromRawColumnsWithoutHelper` exists, asserts 102,550 as a literal, and its source contains no call to the billed-total helper
    - `TestSpendLedgerCarriesNoCurrencyField` fails if a currency-named field or JSON key is reintroduced on the ledger or its totals
    - `go vet ./cmd` is clean
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(SpendLedger|LedgerMeasured|LedgerGrandTotal|LedgerRefusesDerived|LedgerParentRollup|LedgerPhaseTotal)' -count=1</automated>
  </verify>
  <done>The ledger is in the tree, green, with no currency relay, and its aggregation is locked by an expectation the implementation did not produce.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give a row its job attribution and one status vocabulary</name>
  <files>cmd/spend_ledger.go, cmd/spend_ledger_test.go</files>
  <read_first>
    - .planning/phases/196-see-what-it-cost/196-SALVAGE-ASSESSMENT.md — FIX 1-3 and FIX 1-4, including why adding the field later would force a schema bump
    - cmd/codex_build.go — codexBuildDispatch JobName, JobReason, JobSource and CoveredTaskIDs as Phase 195 left them
    - cmd/closeout_cmd.go — normalizeCloseoutWorkerStatus and the dispatch status vocabulary already in the tree
    - cmd/spend_ledger.go — spendRow, the schema version constant, and save/load validation as landed in Task 1
  </read_first>
  <behavior>
    - A row records the job name the worker owned; a worker that owned a single ungrouped task records no job name rather than a placeholder.
    - Job name survives a save and load round trip and is absent from the serialized form when empty.
    - The schema version is set once, in this plan, before any row is ever written anywhere — so no migration is needed later.
    - A row whose status is outside the dispatch status vocabulary is refused by name on save rather than stored.
  </behavior>
  <action>
Add the job-name field to `spendRow` with an omit-when-empty JSON tag, per FIX
1-3. Phase 195 made one worker able to own a chain of tasks, so a ledger keyed
only by worker cannot say which grouped job the tokens belong to. Add it now,
while the schema has never been written to disk in production: adding it after
the first real row exists means a schema version bump and a migration for a
one-line field.

Per FIX 1-4, stop treating the row status as free-form text. Validate it on save
against the dispatch status vocabulary already normalized elsewhere in the
package, and reject an unknown value by name. Document in the field's own
comment that this status is the dispatch status, not the task-receipt status,
so the two vocabularies for one idea this repo keeps producing do not appear
here.

Write the round-trip and refusal tests first and record the failing run. Assert
the absent-when-empty behaviour on the marshalled bytes, not on a struct value,
so an omit tag that silently stops working is caught.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestSpendRowCarriesJobName|TestSpendRowWithoutJobNameOmitsIt|TestSpendLedgerRefusesUnknownRowStatus' -count=1` passes
    - A grouped fixture round-trips job name through save and load and an ungrouped one serializes no job-name key at all
    - The refusal names the offending status and the worker, and writes no file
    - The schema version constant is unchanged from the salvaged value, because the field was added before any production write
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(SpendRowCarriesJobName|SpendRowWithoutJobName|SpendLedgerRefusesUnknownRowStatus|SpendLedgerPersistsAcrossProcesses)' -count=1</automated>
  </verify>
  <done>A ledger row can say which grouped job a worker's tokens belong to, and its status word comes from the vocabulary the rest of the runtime already uses.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(SpendLedger|SpendRow|Ledger)' -count=1`
- `go build ./cmd/aether && go vet ./cmd`
- Confirm by reading the diff that no currency-named field survives on the ledger or its totals.
</verification>

<success_criteria>
- The salvaged ledger is in the tree and green, with the four assessment fixes applied.
- No currency amount can reach a renderer from this layer.
- A grouped Phase 195 job is attributable in the ledger.
</success_criteria>

<output>
Create `.planning/phases/196-see-what-it-cost/196-01-SUMMARY.md` when done.
</output>
