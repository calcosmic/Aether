---
phase: 196-see-what-it-cost
plan: 07
type: execute
wave: 3
depends_on: ["196-02", "196-05"]
files_modified:
  - cmd/codex_continue_finalize.go
  - cmd/spend_writer.go
  - cmd/spend_writer_test.go
  - cmd/spend_cost_line.go
  - cmd/spend_cost_line_test.go
  - cmd/ceremony_cmd.go
  - cmd/ceremony_closeout_spend_test.go
  - cmd/testdata/golden_build.txt
  - cmd/testdata/golden_continue.txt
autonomous: true
requirements: [COST-01, COST-02]

must_haves:
  truths:
    - "D-01: every build and every continue ends with exactly one cost line — run total, per-worker breakdown, and an explicit mark on anything estimated."
    - "D-01: no currency figure is the headline, and no price table exists anywhere."
    - "A continue writes its own rows, keyed separately from the build's, so neither erases the other."
    - "A run with nothing recorded says so plainly and shows no number, rather than showing a zero that reads as a measurement."
    - "The line renders over one authoritative token source, because plan 196-02 landed first."
  artifacts:
    - path: "cmd/spend_cost_line.go"
      provides: "The one closeout cost line, in the shape the owner chose"
    - path: "cmd/ceremony_closeout_spend_test.go"
      provides: "The exactly-one-line and no-currency proofs on both workflows"
  key_links:
    - from: "cmd/ceremony_cmd.go"
      to: "cmd/spend_cost_line.go"
      via: "the closeout renders the cost line last, for build and for continue"
      pattern: "renderSpendCostLine"
    - from: "cmd/codex_continue_finalize.go"
      to: "cmd/spend_writer.go"
      via: "the continue finalize path writes the continue-keyed rows"
      pattern: "writeSpendRowsForRun"
---

<objective>
Write the continue lane's rows, then end every build and every continue with one
honest cost line in the shape the owner chose.

Purpose: this is criterion 1, the thing the phase is named for. It lands after
the two accounting paths were reconciled and after rows are actually being
written, because a cost line rendered over two disagreeing sources, or over an
empty ledger, is a confidently wrong number — and the owner's stated reason for
the measured-versus-estimated mark is that a number he cannot trust is worse
than no number.
Output: continue-lane rows, one shared renderer, and the named regressions.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-see-what-it-cost/196-CONTEXT.md
@.planning/phases/196-see-what-it-cost/196-02-SUMMARY.md
@.planning/phases/196-see-what-it-cost/196-05-SUMMARY.md
@CLAUDE.md
@cmd/spend_ledger.go
@cmd/ceremony_cmd.go
</context>

<interfaces>
The line's shape is the owner's own choice and is not open to redesign: a total
line naming the token count and the number of workers, then one indented line per
worker with its name, its figure and its measured-or-estimated mark, then a short
footnote explaining what the estimated mark means.

Render it from ONE function in `cmd/spend_cost_line.go`. Both workflows call the
same function; nothing else in the tree may render a second one. The line goes
last in the closeout, after the worker summary and before or after the next-step
block as the existing structure dictates — but exactly once per run.

Do not add a price table, a rate, or a currency figure anywhere. Plan 196-01
already removed the only currency relay from the ledger.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the continue lane's rows</name>
  <files>cmd/codex_continue_finalize.go, cmd/spend_writer.go, cmd/spend_writer_test.go</files>
  <read_first>
    - .planning/phases/196-see-what-it-cost/196-05-SUMMARY.md — the writer's real signature and the resolver's behaviour on each platform
    - .planning/phases/196-see-what-it-cost/196-SALVAGE-ASSESSMENT.md — Branch 1's two-file keying and the reasoning written out in the salvaged file
    - cmd/codex_continue_finalize.go — where continue dispatches reach terminal status and where the result is assembled
    - cmd/spend_ledger.go — the continue-keyed ledger path and the replace-on-rerun behaviour
    - cmd/spend_writer.go — the build-lane call site added in plan 196-05, which this task mirrors
  </read_first>
  <behavior>
    - A finished continue writes one row per reviewer or watcher it ran, into the continue-keyed file for that phase.
    - The build's rows for the same phase are untouched by a continue, and a continue's rows are untouched by a later build finalize.
    - A continue that ran no workers writes no rows and does not create an empty file that would later read as a zero-cost run.
    - A write failure is reported and does not fail the continue.
    - The phase total is the build rows plus the continue rows, and the existing ledger test proving that still passes.
  </behavior>
  <action>
Write the tests first and record the failing run.

Call the writer from the continue finalize path with the continue workflow word,
after dispatches reach terminal status and before the result is assembled. Reuse
the writer exactly as plan 196-05 left it; do not fork a second one for this
lane, because two writers is how two vocabularies for one idea start.

Assert the rows on disk in both directions: run a build finalize then a continue
finalize in the same temporary store and check that each file holds only its own
rows. This is the specific mistake the salvaged file's own comments record being
made and corrected once already.

A continue with no workers must leave no file behind. An empty ledger file is
indistinguishable from a run that cost nothing.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestContinueFinalizeWritesItsOwnRows|TestContinueDoesNotEraseBuildRowsEndToEnd|TestContinueWithNoWorkersWritesNoFile|TestLedgerPhaseTotalAddsBuildAndContinue' -count=1` passes
    - The end-to-end assertion runs both finalize paths against one store and reads both files back
    - Only one writer exists in the tree and both lanes call it
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(ContinueFinalizeWritesItsOwnRows|ContinueDoesNotEraseBuildRows|ContinueWithNoWorkersWritesNoFile|LedgerPhaseTotalAddsBuildAndContinue|BuildFinalizeWritesOneRow)' -count=1</automated>
  </verify>
  <done>Both lanes file their own rows, and neither can overwrite the other's.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The one cost line</name>
  <files>cmd/spend_cost_line.go, cmd/spend_cost_line_test.go, cmd/ceremony_cmd.go, cmd/ceremony_closeout_spend_test.go, cmd/testdata/golden_build.txt, cmd/testdata/golden_continue.txt</files>
  <read_first>
    - .planning/phases/196-see-what-it-cost/196-CONTEXT.md — D-01: the exact line shape the owner chose, what he rejected, and why
    - CLAUDE.md — the section on writing for the owner; every word of this line is read by someone who has never opened a file here
    - cmd/ceremony_cmd.go — renderCeremonyCloseout and renderCeremonyCloseoutVisual, and the order the closeout sections are written in
    - cmd/visual_wrapper_contract_test.go — TestLifecycleWrappersHaveVisualCloseoutAfterJSONFinalizer, which proves both wrappers reach this surface
    - cmd/golden_workflow_test.go — which surfaces the golden files capture, so a golden is refreshed only if its own surface changed
    - cmd/spend_ledger.go — computeSpendTotals and the separate measured and estimated subtotals
  </read_first>
  <behavior>
    - The build closeout contains exactly one cost line block; so does the continue closeout.
    - The block states the run total and the number of workers, then one line per worker with its name, its figure and its mark, then the footnote explaining the estimated mark.
    - An estimated figure is never added into a subtotal presented as measured.
    - The rendered block contains no currency symbol and no monetary amount, on any input, including an input whose rows carry a provider-reported cost on the shared usage type.
    - A phase with no recorded rows produces a single plain sentence saying nothing was recorded, and no number.
    - No lane renders the block twice: driving the interactive lane and the autopilot lane each yields exactly one.
    - The block's wording contains no invented repo vocabulary, tested against a list held in the test file.
  </behavior>
  <action>
Write the tests first and record the failing run. `TestBuildEndsWithOneCostLine`
is named by the roadmap and must count occurrences, not merely assert presence —
a test that only checks the block appears cannot catch a second one appearing.

Implement one renderer taking the phase's rows and totals and producing the
block in the owner's chosen shape. Call it from the closeout for both workflows.
Then find every other terminal surface a run can end on — the interactive wrapper
lane and the autopilot lane — and make sure exactly one of them emits the block
per run, with a test that drives each lane and counts. If a lane does not reach
the closeout today, route it through the same renderer rather than writing a
second one.

Render the figures with a short human-readable magnitude rather than raw digits,
matching the shape the owner wrote. Mark anything not measured, and put the
explanation of that mark in the footnote in ordinary words: the tool did not
report usage.

Assert the absence of a monetary amount on the rendered output, not on the source
file, so a comment can never satisfy or break the check. Feed the renderer rows
whose shared usage type carries a provider-reported cost and assert it still does
not reach the output — the type keeps that field, and the guarantee is that the
renderer never shows it.

Refresh a golden file only if the surface that golden captures actually changed;
if the block lands only on the closeout surface, the build and continue goldens
are untouched and must not be regenerated for churn.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestBuildEndsWithOneCostLine|TestContinueEndsWithOneCostLine|TestCostLineShowsPerWorkerAndTotal|TestCostLineMarksEstimatedSeparately|TestCostLineHasNoMonetaryFigure|TestNoLaneRendersTwoCostLines|TestCostLineWithNoRowsShowsNoNumber' -count=1` passes
    - The exactly-one tests count occurrences and fail on two
    - The no-monetary test is fed a row carrying a provider-reported cost and still passes
    - `go test ./cmd -run 'Test(GoldenBuildVisualOutput|GoldenContinueVisualOutput|LifecycleWrappersHaveVisualCloseout)' -count=1` passes
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(BuildEndsWithOneCostLine|ContinueEndsWithOneCostLine|CostLine|NoLaneRendersTwoCostLines|GoldenBuildVisualOutput|GoldenContinueVisualOutput|LifecycleWrappersHaveVisualCloseout)' -count=1</automated>
  </verify>
  <done>Every build and every continue ends with one line the owner can read and trust, with the guessed figures visibly marked as guesses.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(CostLine|EndsWithOneCostLine|ContinueFinalizeWrites|Spend|Golden|LifecycleWrapper)' -count=1`
- `go build ./cmd/aether && go vet ./cmd`
- Render one build closeout and one continue closeout by hand and read them as someone who has never opened a file here.
</verification>

<success_criteria>
- Exactly one cost line ends every build and every continue, on every lane.
- Estimated figures are marked and kept out of measured subtotals.
- No monetary amount appears anywhere in the rendered output.
</success_criteria>

<output>
Create `.planning/phases/196-see-what-it-cost/196-07-SUMMARY.md` when done.
</output>
