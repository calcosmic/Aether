---
phase: 67-seal-hive-brain-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/ant/seal.md
  - .opencode/commands/ant/seal.md
  - .planning/phases/62-lifecycle-ceremony-seal-and-init/62-VERIFICATION.md
autonomous: true
requirements:
  - CERE-04

must_haves:
  truths:
    - "OpenCode seal.md wrapper body matches Claude seal.md wrapper body (parity test passes for seal.md)"
    - "Both wrappers reflect hive promotion confirmation instead of SUGGESTION relay"
    - "Porter readiness section in wrapper only runs after seal succeeds (gated by success)"
    - "Phase 62 VERIFICATION.md updated with CERE-02 and parity gaps marked closed"
  artifacts:
    - path: ".opencode/commands/ant/seal.md"
      provides: "OpenCode seal wrapper matching Claude seal wrapper"
      contains: "Shelf Candidate Detection"
    - path: ".claude/commands/ant/seal.md"
      provides: "Updated Claude seal wrapper with hive promotion confirmation"
      contains: "Hive Brain"
    - path: ".planning/phases/62-lifecycle-ceremony-seal-and-init/62-VERIFICATION.md"
      provides: "Phase 62 verification gaps updated to closed"
      contains: "closed"
  key_links:
    - from: ".opencode/commands/ant/seal.md"
      to: ".claude/commands/ant/seal.md"
      via: "parity test body comparison"
      pattern: "same body content"
---

<objective>
Fix wrapper parity between Claude and OpenCode seal.md files, update both wrappers to reflect the new hive promotion confirmation output, and close the Phase 62 verification gaps.

Purpose: The Phase 62 verification report identified two gaps: (1) OpenCode seal.md is missing the "Shelf Candidate Detection" section added in Phase 65, causing TestClaudeOpenCodeCommandParity to fail for seal.md, and (2) CERE-02 was marked BLOCKED. Plan 01 closes CERE-02 in the runtime; this plan closes the parity gap and the VERIFICATION.md documentation.

Output: Matching seal.md wrappers, updated VERIFICATION.md.

Requirements traceability:
- CERE-04 (CROWNED-ANTHILL enrichment): This plan updates VERIFICATION.md to confirm the enrichment is complete. The runtime enrichment was delivered in Phase 62-01 (learnings, instincts, signals, flags) and Plan 01 adds HivePromoted. This plan documents closure of the verification gap.
- CERE-03 (FOCUS expiry, REDIRECT preservation): Already delivered in Phase 62-01 (expireSignalsByType). Not reimplemented here. Removed from requirements frontmatter.
- CERE-05 (init-research deep analysis): Already delivered in Phase 62-02 (init-research subcommand) and 62-03 (init wrappers with charter). Not reimplemented here. Removed from requirements frontmatter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-seal-hive-brain-wiring/67-RESEARCH.md

@.claude/commands/ant/seal.md
@.opencode/commands/ant/seal.md
@.planning/phases/62-lifecycle-ceremony-seal-and-init/62-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync OpenCode seal.md with Claude seal.md and update both for hive promotion</name>
  <files>.claude/commands/ant/seal.md, .opencode/commands/ant/seal.md</files>
  <read_first>
    - .claude/commands/ant/seal.md (full file, current Claude wrapper)
    - .opencode/commands/ant/seal.md (full file, current OpenCode wrapper -- has parity drift)
  </read_first>
  <action>
**Step A: Update Claude seal.md (.claude/commands/ant/seal.md)**

The current "Post-Seal Report" section says:
```
- If the runtime printed a SUGGESTION about hive-eligible instincts, relay that to the user: "Consider promoting eligible instincts globally with `aether queen-promote-instinct <id>`"
```

This must change because Plan 01 replaces the SUGGESTION with an automatic promotion confirmation. Update the "Post-Seal Report" section bullet to:
```
- "Hive Brain promotions: {count promoted} instinct(s) promoted to Hive Brain"
- If any hive promotions failed, relay the warning: "N hive promotion(s) failed (see log)"
```

Also verify the "Shelf Candidate Detection" section is present (it already is in Claude seal.md).

Also verify the "Post-Seal: Porter Delivery" section is properly gated on seal success (it is -- the section runs "After seal succeeds" per the header text).

**Step B: Sync OpenCode seal.md (.opencode/commands/ant/seal.md)**

The OpenCode seal.md is MISSING:
1. The "## Shelf Candidate Detection" section (13 lines with shelf-detect, tick-to-approve, shelf-add commands)
2. The shelf candidates report line in Post-Seal Report section (1 line about "Shelf candidates: {N}")

Copy the ENTIRE body content from the updated Claude seal.md into the OpenCode seal.md, preserving:
- The `<!-- Generated from .aether/commands/seal.yaml - DO NOT EDIT DIRECTLY -->` header line
- The YAML frontmatter block between `---` markers
- The body content must be byte-identical to Claude seal.md body (after frontmatter closing `---`)

The parity test (`TestClaudeOpenCodeCommandParity`) compares the normalized body content of both files. The files must match exactly (after normalization: CRLF->LF, and the runtime-CLI-sentence normalization already in the test).

**Critical:** Both files must have the same `generated-from` header (first line). If they differ in the header, update OpenCode to match Claude's header. The test checks `claudeSnapshot.source == opencodeSnapshot.source` and `claudeSnapshot.body == opencodeSnapshot.body`.
  </action>
  <verify>
    <automated>go test ./cmd/... -run "TestClaudeOpenCodeCommandParity" -count=1 2>&1 | grep -E "(PASS|FAIL|seal)" || go test ./cmd/... -run "TestClaudeOpenCodeCommandParity" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `.opencode/commands/ant/seal.md` contains `## Shelf Candidate Detection`
    - `.opencode/commands/ant/seal.md` contains `shelf-detect`
    - `.opencode/commands/ant/seal.md` contains `shelf-add`
    - Both seal.md files contain "Hive Brain promotions" (replacing the SUGGESTION relay)
    - Both seal.md files contain "Post-Seal: Porter Delivery"
    - `go test ./cmd/... -run "TestClaudeOpenCodeCommandParity" -count=1` does NOT report seal.md in drift list
  </acceptance_criteria>
  <done>OpenCode seal.md matches Claude seal.md. Both reflect hive promotion confirmation. Parity test passes for seal.md.</done>
</task>

<task type="auto">
  <name>Task 2: Update Phase 62 VERIFICATION.md to close gaps</name>
  <files>.planning/phases/62-lifecycle-ceremony-seal-and-init/62-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/62-lifecycle-ceremony-seal-and-init/62-VERIFICATION.md (full file, the current verification report)
  </read_first>
  <action>
Update the Phase 62 VERIFICATION.md to reflect that both identified gaps are now closed.

**YAML frontmatter changes:**
1. Change `status: gaps_found` to `status: gaps_resolved`
2. Change `score: 4/5 must-haves verified` to `score: 5/5 must-haves verified`

**Gap 1 (CERE-02) -- Update:**
In the `gaps` array, change the first gap entry:
- `status: "partial"` -> `status: "closed"`
- Update `reason` to: "CERE-02 closed in Phase 67. Seal ceremony now calls promoteToHive for instincts >= 0.8 confidence (non-blocking). See cmd/codex_workflow_cmds.go promoteToHive call in instinct loop."
- Remove the `missing` array items (replace with empty or remove the key)

**Gap 2 (Parity) -- Update:**
Change the second gap entry:
- `status: "failed"` -> `status: "closed"`
- Update `reason` to: "OpenCode seal.md synced with Claude seal.md in Phase 67 Plan 02. Shelf Candidate Detection section added. Hive promotion confirmation text added to both wrappers."
- Remove the `missing` array items (replace with empty or remove the key)

**Observable Truths table (body text):**
- Row 2 ("Seal automatically promotes instincts with confidence >= 0.8 to Hive Brain"): Change Status from "FAILED" to "VERIFIED", update Evidence to include: "promoteToHive() called from sealCmd instinct loop for each entry with confidence >= 0.8. Non-blocking: failures logged, seal continues. Tests: TestSealHivePromote, TestSealHivePromoteNonBlocking."

**Key Link Verification table:**
- Row for `.opencode/commands/ant/seal.md`: Change Status from "NOT_WIRED" to "WIRED", update Details to: "OpenCode seal.md synced with Claude seal.md. Contains blocker relay, shelf detection, hive promotion confirmation, Porter delivery."

**Anti-Patterns table:**
- Remove the "Auto-Promotion section still present" row (it is now fixed)
  </action>
  <verify>
    <automated>FILE="/Users/callumcowie/repos/Aether/.planning/phases/62-lifecycle-ceremony-seal-and-init/62-VERIFICATION.md" && grep -q "status: gaps_resolved" "$FILE" && grep -q "score: 5/5" "$FILE" && grep -c "status:.*closed" "$FILE" | grep -qv "^0$" && grep -q "VERIFIED" "$FILE" && grep -q "WIRED" "$FILE" && echo "PASS: all verification checks" || echo "FAIL: missing verification markers"</automated>
  </verify>
  <acceptance_criteria>
    - 62-VERIFICATION.md frontmatter contains `status: gaps_resolved`
    - 62-VERIFICATION.md frontmatter contains `score: 5/5`
    - 62-VERIFICATION.md contains `status: "closed"` for the CERE-02 gap entry
    - 62-VERIFICATION.md contains `status: "closed"` for the parity gap entry
    - 62-VERIFICATION.md Observable Truths row 2 contains "VERIFIED" (not "FAILED")
    - 62-VERIFICATION.md Key Link for OpenCode seal.md contains "WIRED" (not "NOT_WIRED")
  </acceptance_criteria>
  <done>Phase 62 VERIFICATION.md updated to reflect both gaps closed. CERE-02 and parity drift marked as resolved with evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wrapper -> runtime CLI | Wrappers relay runtime output, do not mutate state directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-03 | Information Disclosure | Wrapper output | accept | Wrappers relay runtime output which is local-only, no sensitive data exposed |
</threat_model>

<verification>
```bash
go test ./cmd/... -run "TestClaudeOpenCodeCommandParity" -count=1
FILE=".planning/phases/62-lifecycle-ceremony-seal-and-init/62-VERIFICATION.md"
grep "status: gaps_resolved" "$FILE" && grep "score: 5/5" "$FILE" && grep "VERIFIED" "$FILE" && grep "WIRED" "$FILE"
```
</verification>

<success_criteria>
1. `go test ./cmd/... -run "TestClaudeOpenCodeCommandParity" -count=1` does not report seal.md drift
2. Both seal.md files contain "Hive Brain promotions" and "Shelf Candidate Detection"
3. Phase 62 VERIFICATION.md updated with both gaps closed
4. VERIFICATION.md score updated to 5/5
5. VERIFICATION.md contains "VERIFIED" in Observable Truths and "WIRED" in Key Links
</success_criteria>

<output>
After completion, create `.planning/phases/67-seal-hive-brain-wiring/67-02-SUMMARY.md`
</output>
