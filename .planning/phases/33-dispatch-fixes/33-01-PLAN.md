---
phase: 33-dispatch-fixes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - cmd/colony_prime_context.go
  - cmd/codex_build.go
  - cmd/compatibility_cmds.go
  - cmd/codex_continue.go
  - pkg/agent/spawn_tree.go
autonomous: true
requirements: [R052, R053, R054, R055, R056]
---

<objective>
Fix 5 P1 dispatch robustness issues from the Oracle audit so workers dispatch more safely and failures are visible instead of silent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-dispatch-fixes/33-CONTEXT.md
@cmd/colony_prime_context.go
@cmd/codex_build.go
@cmd/compatibility_cmds.go
@cmd/codex_continue.go
@pkg/agent/spawn_tree.go
</context>

<feature>
  <name>P1 Dispatch Robustness Fixes</name>
  <files>cmd/colony_prime_context.go, cmd/codex_build.go, cmd/compatibility_cmds.go, cmd/codex_continue.go, pkg/agent/spawn_tree.go</files>
  <behavior>
    - Empty context capsules log a warning and block dispatch if below 512 chars
    - Caste routing uses explicit builder keyword priority over scout keywords
    - Autopilot retries once on transient build errors before pausing
    - Continue skips review wave when Codex CLI is unavailable, proceeds with claims verification
    - SpawnTree returns error when store is nil instead of silently succeeding
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for P1 fixes</name>
  <files>cmd/colony_prime_context_test.go, cmd/codex_build_test.go, cmd/compatibility_cmds_test.go, cmd/codex_continue_test.go, pkg/agent/spawn_tree_test.go</files>
  <read_first>
    cmd/colony_prime_context.go (lines 500-520 for resolveCodexWorkerContext)
    cmd/codex_build.go (lines 2040-2060 for suggestedBuildCaste)
    cmd/compatibility_cmds.go (lines 200-220 for autopilot loop)
    cmd/codex_continue.go (lines 490-500 for Codex CLI availability check)
    pkg/agent/spawn_tree.go (lines 200-210, 250-260 for RecordSpawn/UpdateStatus)
  </read_first>
  <action>
    Write 5 test functions that fail because the P1 fixes don't exist yet:

    1. TestContextCapsuleBlocksDispatchWhenEmpty: Set up colony state with empty context capsule (< 512 chars). Call resolveCodexWorkerContext. Assert it returns error or warning.

    2. TestCasteRoutingPrioritizesBuilderKeywords: Call suggestedBuildCaste with task descriptions containing both builder and scout keywords (e.g., "implement spec document"). Assert it returns "builder", not "scout".

    3. TestAutopilotRetriesOnTransientError: Set up autopilot with a build that fails once then succeeds. Assert the retry attempt is logged and the loop continues.

    4. TestContinueSkipsReviewWhenCodexUnavailable: Mock Codex CLI as unavailable. Run continue. Assert it warns about missing Codex but proceeds with claims verification and does not deadlock.

    5. TestSpawnTreeReturnsErrorWhenStoreNil: Call RecordSpawn with nil store. Assert it returns error instead of nil.

    Run tests — they MUST fail.
  </action>
  <acceptance_criteria>
    - 5 test functions exist across the 5 test files
    - Tests compile but fail because the fixes don't exist yet
    - `go test ./cmd/... ./pkg/agent/... -run "TestContextCapsule|TestCasteRouting|TestAutopilotRetry|TestContinueSkipReview|TestSpawnTreeNil" -count=1 -timeout 60s` fails with expected errors
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/... ./pkg/agent/... -run "TestContextCapsule|TestCasteRouting|TestAutopilotRetry|TestContinueSkipReview|TestSpawnTreeNil" -count=1 -timeout 60s 2>&1 | grep -E "(FAIL|undefined|cannot)"</automated>
  </verify>
  <done>5 failing tests committed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement P1 fixes</name>
  <files>cmd/colony_prime_context.go, cmd/codex_build.go, cmd/compatibility_cmds.go, cmd/codex_continue.go, pkg/agent/spawn_tree.go</files>
  <read_first>
    cmd/colony_prime_context.go (lines 500-520)
    cmd/codex_build.go (lines 2040-2060)
    cmd/compatibility_cmds.go (lines 200-220)
    cmd/codex_continue.go (lines 490-500)
    pkg/agent/spawn_tree.go (lines 200-210, 250-260)
  </read_first>
  <action>
    Implement each fix:

    1. Empty Context Capsules (cmd/colony_prime_context.go):
       - In resolveCodexWorkerContext(), after resolving context, check len(context) < 512
       - If below threshold: log warning "Context capsule below minimum threshold (%d chars) — dispatch blocked to prevent zero-context worker execution"
       - Return error: fmt.Errorf("context capsule too short (%d chars, min 512)", len(context))

    2. Fragile Caste Routing (cmd/codex_build.go):
       - In suggestedBuildCaste(), restructure keyword matching:
         - Phase 1: Check builder keywords first ("implement", "build", "create", "fix", "add", "write", "code", "refactor", "test")
         - Phase 2: Only if no builder match, check scout keywords ("research", "investigate", "explore", "document", "spec")
         - Phase 3: Only if no scout match, check other castes
       - Add comment showing matched keyword for dispatch preview

    3. Autopilot Retry (cmd/compatibility_cmds.go):
       - In the autopilot loop (around line 210), after a build error:
         - Log "Build failed, attempting single retry..."
         - Sleep 2 seconds
         - Retry the build once
         - If retry also fails: pause autopilot as before
         - If retry succeeds: continue loop

    4. Continue Codex Fallback (cmd/codex_continue.go):
       - In the review wave section (around line 494), check if Codex CLI is available
       - If not available: log warning "Codex CLI unavailable — skipping review wave, proceeding with claims verification"
       - Skip the review wave but continue to claims verification and advancement
       - Do NOT return error or block advancement

    5. SpawnTree Nil Store (pkg/agent/spawn_tree.go):
       - In RecordSpawn() and UpdateStatus(), check if store is nil
       - If nil: return fmt.Errorf("spawn tree: store is nil — cannot record spawn/update")
       - Remove the silent success path

    Run all 5 tests — they MUST pass.
    Run full test suite — no regression.
  </action>
  <acceptance_criteria>
    - `go test ./cmd/... ./pkg/agent/... -run "TestContextCapsule|TestCasteRouting|TestAutopilotRetry|TestContinueSkipReview|TestSpawnTreeNil" -count=1 -timeout 60s` passes
    - `go test ./cmd/... -count=1 -timeout 120s` passes (no regression)
    - `go test ./pkg/agent/... -count=1 -timeout 60s` passes (no regression)
    - Each fix has at least one test proving it works
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/... ./pkg/agent/... -count=1 -timeout 180s</automated>
  </verify>
  <done>All 5 P1 fixes implemented and tested</done>
</task>

</tasks>

<verification>
- All 5 P1 tests pass
- Full cmd test suite passes
- Full pkg/agent test suite passes
- No regression in existing tests
</verification>

<success_criteria>
- Empty context capsules blocked below 512 chars
- Builder keywords prioritized over scout keywords in caste routing
- Autopilot retries once on transient build errors
- Continue proceeds without Codex CLI (skips review, verifies claims)
- SpawnTree returns error on nil store
</success_criteria>

<output>
After completion, create `.planning/phases/33-dispatch-fixes/33-01-SUMMARY.md`
</output>
