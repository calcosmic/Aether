---
phase: 37-codebase-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/codex_build.go
  - cmd/midden_internal.go
  - .planning/phases/31-p0-runtime-truth-fixes/31-PLAN-02-SUMMARY.md
  - .planning/phases/31-p0-runtime-truth-fixes/31-PLAN-04-SUMMARY.md
  - .planning/phases/33-dispatch-fixes/33-02-SUMMARY.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "No orphaned exports remain in the v1.5 integration audit"
    - "Phase 31 Plan 02 SUMMARY lists requirements it actually completed"
    - "Phase 31 Plan 04 SUMMARY lists requirements it actually completed"
    - "Phase 33 Plan 02 SUMMARY no longer misattributes R057/R058 to Phase 33"
  artifacts:
    - path: "cmd/codex_build.go"
      provides: "newCodexWorkerInvokerOrError removed, no references remain"
      min_lines: 1
    - path: "cmd/midden_internal.go"
      provides: "writeMiddenEntry removed or wired into dispatch failure paths"
      min_lines: 1
    - path: ".planning/phases/31-p0-runtime-truth-fixes/31-PLAN-02-SUMMARY.md"
      provides: "requirements-completed frontmatter field present with correct IDs"
    - path: ".planning/phases/31-p0-runtime-truth-fixes/31-PLAN-04-SUMMARY.md"
      provides: "requirements-completed frontmatter field present with correct IDs"
    - path: ".planning/phases/33-dispatch-fixes/33-02-SUMMARY.md"
      provides: "requirements-completed frontmatter no longer lists R057/R058"
  key_links:
    - from: "cmd/codex_build.go"
      to: "codex.NewWorkerInvokerOrError"
      via: "grep -c"
      pattern: "newCodexWorkerInvokerOrError"
    - from: "cmd/midden_internal.go"
      to: "dispatch failure callers"
      via: "grep -c"
      pattern: "writeMiddenEntry"
---

<objective>
Clean up orphaned code and fix SUMMARY metadata from v1.5 phases.

Purpose: Close the four tech-debt items identified in the v1.5 milestone audit so the integration score moves from 18/20 to 20/20 and SUMMARY metadata is accurate for future traceability.
Output: Dead code removed or wired, SUMMARY frontmatter corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.5-MILESTONE-AUDIT.md
@cmd/codex_build.go
@cmd/midden_internal.go
@.planning/phases/31-p0-runtime-truth-fixes/31-PLAN-02-SUMMARY.md
@.planning/phases/31-p0-runtime-truth-fixes/31-PLAN-04-SUMMARY.md
@.planning/phases/33-dispatch-fixes/33-02-SUMMARY.md

## Orphaned Export 1: newCodexWorkerInvokerOrError

In `cmd/codex_build.go` line 87:
```go
var newCodexWorkerInvokerOrError = codex.NewWorkerInvokerOrError
```

This variable is assigned but NEVER referenced anywhere in the file (or the codebase). All production paths use `newCodexWorkerInvoker` (line 86) plus a manual `IsAvailable()` check (see `executeCodexBuildDispatches` around line 637).

Decision from audit: remove the orphaned variable.

## Orphaned Export 2: writeMiddenEntry

In `cmd/midden_internal.go` line 13:
```go
func writeMiddenEntry(category, source, message string) (string, error) { ... }
```

Defined to record dispatch failures to colony midden but never called. Dispatch failure history is lost across sessions.

Decision from audit: this function was intended to be wired into dispatch error paths. The simplest correct fix is to remove the dead code (since the midden system is not actively used for dispatch failures and no callers exist). If a future phase wants midden logging, it should be designed with actual call sites.

## Phase 31 Plan 02 SUMMARY

Current frontmatter is missing `requirements-completed`. The plan completed R047 and R048 (continue bypass closure and reconcile hardening).

## Phase 31 Plan 04 SUMMARY

Current frontmatter is missing `requirements-completed`. The plan completed R049 and R050 (git-verified claims and no environmental dismissal).

## Phase 33 Plan 02 SUMMARY

Current frontmatter lists `requirements-completed: [R057, R058]`. These were actually completed by Phase 34 (Cleanup), not Phase 33. Must be corrected to an empty list or the actual requirements completed by Plan 02 (if any). From the SUMMARY content, Plan 02 completed platform-aware context-clear guidance, archaeologist dispatch, dead pool removal, and visual test fixes. None of these map to R057/R058. The correct `requirements-completed` value is `[]`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove orphaned newCodexWorkerInvokerOrError from codex_build.go</name>
  <files>cmd/codex_build.go</files>
  <read_first>
    - cmd/codex_build.go (read lines 80-95 to confirm the variable declaration and verify no references exist)
  </read_first>
  <action>
    Delete line 87 from cmd/codex_build.go:
    ```go
    var newCodexWorkerInvokerOrError = codex.NewWorkerInvokerOrError
    ```
    Do NOT delete line 86 (`var newCodexWorkerInvoker = codex.NewWorkerInvoker`).
    Do NOT modify any other code.
  </action>
  <verify>
    <automated>
      grep -n "newCodexWorkerInvokerOrError" cmd/codex_build.go | wc -l | xargs -I {} test {} -eq 0
    </automated>
  </verify>
  <done>
    - `grep -c "newCodexWorkerInvokerOrError" cmd/codex_build.go` returns 0
    - `grep -c "newCodexWorkerInvoker" cmd/codex_build.go` returns at least 1 (the remaining valid variable)
    - `go build ./cmd/aether` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned writeMiddenEntry from midden_internal.go</name>
  <files>cmd/midden_internal.go</files>
  <read_first>
    - cmd/midden_internal.go (read entire file to confirm no callers exist in the codebase)
  </read_first>
  <action>
    Delete the entire file cmd/midden_internal.go. The function `writeMiddenEntry` has zero callers across the codebase and the midden system is not actively used for dispatch failures. If future work needs midden logging, it should be designed with actual call sites.
  </action>
  <verify>
    <automated>
      test ! -f cmd/midden_internal.go && grep -r "writeMiddenEntry" cmd/ | wc -l | xargs -I {} test {} -eq 0
    </automated>
  </verify>
  <done>
    - `cmd/midden_internal.go` no longer exists
    - `grep -r "writeMiddenEntry" cmd/` returns zero matches
    - `go build ./cmd/aether` succeeds
    - `go test ./cmd/...` passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix SUMMARY metadata for Phases 31 and 33</name>
  <files>
    - .planning/phases/31-p0-runtime-truth-fixes/31-PLAN-02-SUMMARY.md
    - .planning/phases/31-p0-runtime-truth-fixes/31-PLAN-04-SUMMARY.md
    - .planning/phases/33-dispatch-fixes/33-02-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/31-p0-runtime-truth-fixes/31-PLAN-02-SUMMARY.md
    - .planning/phases/31-p0-runtime-truth-fixes/31-PLAN-04-SUMMARY.md
    - .planning/phases/33-dispatch-fixes/33-02-SUMMARY.md
  </read_first>
  <action>
    For each SUMMARY file, add or correct the `requirements-completed` field in the YAML frontmatter:

    1. In `31-PLAN-02-SUMMARY.md`: add `requirements-completed: [R047, R048]` to the frontmatter. Place it after the `metrics:` block, before the `# Phase 31 Plan 02...` heading.

    2. In `31-PLAN-04-SUMMARY.md`: add `requirements-completed: [R049, R050]` to the frontmatter. Place it after the `metrics:` block, before the `# Phase 31 Plan 04...` heading.

    3. In `33-02-SUMMARY.md`: change `requirements-completed: [R057, R058]` to `requirements-completed: []`. The plan completed ceremony restoration work (platform guidance, archaeologist dispatch, dead code removal) but none of the tracked requirements R045-R058.
  </action>
  <verify>
    <automated>
      grep -A1 "requirements-completed" .planning/phases/31-p0-runtime-truth-fixes/31-PLAN-02-SUMMARY.md | grep -q "R047" && grep -A1 "requirements-completed" .planning/phases/31-p0-runtime-truth-fixes/31-PLAN-04-SUMMARY.md | grep -q "R049" && grep -A1 "requirements-completed" .planning/phases/33-dispatch-fixes/33-02-SUMMARY.md | grep -q "\[\]"
    </automated>
  </verify>
  <done>
    - `31-PLAN-02-SUMMARY.md` frontmatter contains `requirements-completed: [R047, R048]`
    - `31-PLAN-04-SUMMARY.md` frontmatter contains `requirements-completed: [R049, R050]`
    - `33-02-SUMMARY.md` frontmatter contains `requirements-completed: []` (not R057/R058)
    - No other frontmatter fields are modified or corrupted
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan frontmatter -> executor | SUMMARY files are documentation-only; no runtime behavior depends on their content |
| dead code removal -> build | Removing unused variables and functions cannot break active code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Denial of Service | codex_build.go build break | mitigate | `go build ./cmd/aether` and `go test ./cmd/...` must pass before task is marked done |
| T-37-02 | Information Disclosure | midden_internal.go deletion | accept | File contains no secrets; deletion removes unused code |
| T-37-03 | Repudiation | SUMMARY metadata incorrect | mitigate | Automated grep verification ensures exact values present |
</threat_model>

<verification>
- `go build ./cmd/aether` succeeds
- `go test ./cmd/...` passes
- `grep -r "newCodexWorkerInvokerOrError" cmd/` returns 0 matches
- `grep -r "writeMiddenEntry" cmd/` returns 0 matches
- `test ! -f cmd/midden_internal.go`
- All three SUMMARY files have correct `requirements-completed` frontmatter
</verification>

<success_criteria>
- Integration audit orphaned exports count drops from 2 to 0
- Phase 31 Plans 02 and 04 SUMMARYs include requirements-completed fields
- Phase 33 Plan 02 SUMMARY no longer misattributes R057/R058
- All Go code compiles and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/37-codebase-hygiene/37-01-SUMMARY.md`
</output>
