---
phase: 204-learning-governor
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/204-learning-governor/204-CLASSIC-SYNTHESIS.md
  - .planning/phases/204-learning-governor/204-CLASSIC-HISTORICAL-EVIDENCE.md
  - cmd/testdata/classic-contract/v1/schema.json
  - cmd/testdata/classic-contract/v1/mechanisms.json
  - cmd/classic_contract_test.go
autonomous: true
requirements: [SYNTH-06]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A cited mechanism study reconstructs how Classic observations, instincts, Queen memory, Hive wisdom, midden failures, signal reinforcement and learning presentation influenced later work, and exists before any Phase 204 runtime work begins (SYNTH-06)."
    - "Every one of the thirteen stores in RESEARCH.md's Current Learning Store Census is re-verified against live code at this phase's own HEAD and carries exactly one evidence-backed disposition drawn from keep-current, restore-modern, replace-better or retire-with-proof."
    - "Each of the seven capability rows routed to Phase 204 is independently confirmed or revised against cited historical and current evidence rather than inherited from the ledger's frozen hypothesis, with CAP-001 and CAP-057 explicitly re-adjudicated because RESEARCH.md finds both materially stale."
    - "A mechanism for which no Classic evidence can be recovered records an explicit no-historical-evidence-recoverable citation and a current-only disposition, and is never omitted from the matrix."
    - "Two SYN-204 rows whose subject is exactly equal are merged into one row carrying both citations; each SYN-204 identifier appears exactly once in the synthesis and exactly once in the registry."
    - "SYN-204 rows are stored and rendered in ascending identifier order, and two rows of equal subject weight are separated by identifier, so the order is total and stable."
    - "Re-running the corpus registration against an already-registered SYN-204 set adds no duplicate mechanism entry and changes no existing entry."
    - "No production, non-test Go file writes cmd/testdata/classic-contract/v1/mechanisms.json, so no concurrent runtime write can interleave a partial registry."
    - "The versioned Classic contract corpus accepts SYN-204 mechanism identifiers and Phase 204 group names, and rejects an unknown or case-variant SYN identifier by naming the offending identifier."
    - "Every LEARN requirement in this phase has at least one SYN-204 row and at least one named implementation plan in the range 204-02 to 204-11."
  artifacts:
    - path: ".planning/phases/204-learning-governor/204-CLASSIC-HISTORICAL-EVIDENCE.md"
      provides: "Direct git-show citations for Classic-era learning mechanics recovered from before the shell-to-Go migration, or an explicit not-recoverable record per mechanism"
      contains: "git show"
    - path: ".planning/phases/204-learning-governor/204-CLASSIC-SYNTHESIS.md"
      provides: "Signed Phase 204 learning-governor mechanism study, store census re-verification, CAP routing and open-question rulings"
      contains: "SYN-204-12"
    - path: "cmd/testdata/classic-contract/v1/mechanisms.json"
      provides: "SYN-204 mechanism registry entries"
      contains: "SYN-204-01"
    - path: "cmd/testdata/classic-contract/v1/schema.json"
      provides: "Widened synthesis-decision pattern and Phase 204 group enum"
      contains: "V-204-TRUTH"
  key_links:
    - from: "cmd/testdata/classic-contract/v1/mechanisms.json"
      to: ".planning/phases/204-learning-governor/204-CLASSIC-SYNTHESIS.md"
      via: "SYN-204 and CAP identifiers"
      pattern: "SYN-204-"
    - from: ".planning/phases/204-learning-governor/204-CLASSIC-SYNTHESIS.md"
      to: ".planning/phases/204-learning-governor/204-CLASSIC-HISTORICAL-EVIDENCE.md"
      via: "OLD-row citations"
      pattern: "OLD-"
  prohibitions:
    - statement: "Never justify an implementation task by a Classic feature name, a screenshot, or a remembered behaviour alone."
      status: flagged-unverified
    - statement: "Never record a disposition for a routed capability row without citing both historical and current evidence."
      status: flagged-unverified
    - statement: "Never present an unresolved owner choice as an engineering conclusion."
      status: flagged-unverified
    - statement: "Never cite a STATE.md or CLAUDE.md claim about runtime behaviour as current fact without re-reading the code it describes."
      status: flagged-unverified
    - statement: "Never build new work on top of a layer this study proves has neither a live writer nor a live reader."
      status: flagged-unverified
---

<objective>
Produce the mechanism study SYNTH-06 requires and SYNTH-07 makes a hard planning gate, recover the Classic-era learning mechanics that RESEARCH.md explicitly left unreconstructed, and widen the versioned Classic contract corpus to accept Phase 204 mechanism identifiers.

Purpose: this phase decides what counts as learned, credited, verified, promoted and rolled back. RESEARCH.md's own confidence note caps the historical half of this study at MEDIUM with one direct citation obtained, and flags the rest as required first-task work rather than skipped work. Without that recovery, SYNTH-07's gate ("cites exact historical and current evidence") is under-evidenced on the historical side even though the current-Go side is exhaustive. The second and larger risk this plan exists to remove is the one this repository names as its own signature failure: nine of the thirteen stores in the census already have either a writer with no reader or a reader with no writer, and two prior audits' dispositions are already stale. A plan set written from the ledger's frozen hypothesis rather than from re-verified code would build the tenth.

Output: a signed `204-CLASSIC-SYNTHESIS.md` carrying SYN-204-01 through SYN-204-12, a separate historical-evidence file carrying the recovered citations, and a corpus schema and mechanism registry that accept those identifiers.

Tracer note: this plan is the SYNTH-07 planning gate and produces no runtime behaviour, so it carries no `type="tracer"` task. The phase's tracer — the thin end-to-end slice through every layer this phase modifies — is Task 1 of plan `204-02`, which depends on this one.
</objective>

## Planner Assumptions (no CONTEXT.md — owner may veto)

There was no discussion round for this phase, so nothing here is a locked owner decision. These are the planner's answers to questions the research left open. Each says what was chosen, in plain English, and what changes if the owner disagrees.

**Assumption A (Research Open Question 1) — we go back and read the old code, we do not take the design document's word for it.**
The research found one old design document describing how the system used to learn, but not the working code behind it. This plan spends a task digging the old code out of the project's own history and quoting it directly. Where nothing survives, it says so in writing rather than guessing.
*If the owner disagrees* (thinks one document is enough): Task 1 can be dropped and Task 2 proceeds from the single citation already obtained, at the cost of the study's historical half staying at medium confidence — which the next phase's acceptance gate is entitled to reject.
*Technical:* SYNTH-06 requires reconstruction of observations, instincts, Queen memory, Hive, midden and signal reinforcement. RESEARCH.md Assumptions Log A1 states only `3a5b81c2:.aether/learning.md` was read. Commit `6732b0ba` ("Deprecate dead shell scripts") is the migration boundary; `instinct-store.sh` and `learning.sh` existed before it.

**Assumption B — the study may fix design, not merely describe it.**
SYNTH-06's own words are "choose the smallest architecture that can prove beneficial application and rollback." Choosing is a design act. This study therefore records rulings that later plans are bound by. A reviewer who disagrees would be disagreeing about the scope of the word "study", not about correctness.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/204-learning-governor/204-RESEARCH.md
@.planning/phases/204-learning-governor/204-PATTERNS.md
@.planning/research/v1.28-classic-synthesis-template.md
@.planning/research/v1.28-classic-capability-ledger.md
@.planning/WINDOWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recover the Classic-era learning mechanics from this repository's own history</name>
  <files>.planning/phases/204-learning-governor/204-CLASSIC-HISTORICAL-EVIDENCE.md</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .planning/phases/204-learning-governor/204-RESEARCH.md
    - .planning/research/v1.28-classic-capability-ledger.md
    - .planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md
    - .planning/phases/204-learning-governor/204-CLASSIC-HISTORICAL-EVIDENCE.md (create; confirm absent)
  </read_first>
  <action>Create a standalone evidence file whose only job is to carry direct, quotable historical citations for the seven Classic learning mechanics SYNTH-06 names, so Task 2's synthesis can cite them rather than restate them.

Locate the migration boundary first. Commit `6732b0ba` is the "Deprecate dead shell scripts" commit named in RESEARCH.md Open Question 1; run `git log --oneline --all -- '*.sh'` scoped to the shell-era learning files and `git log --diff-filter=D --name-only` to find the commits that deleted them. The specific historical file names to hunt are `instinct-store.sh`, `learning.sh`, `hive.sh`, `midden.sh`, `pheromone.sh`, `event-bus.sh`, `memory.sh` and `queen.sh`, under `.aether/lib/`, `.aether/scripts/` or `.aether/utils/` — search all three prefixes, and search by basename with `git log --all --diff-filter=A --name-only` when a path guess misses.

For each of these seven mechanisms, produce one section carrying at minimum one verbatim excerpt obtained through `git show <commit>:<path>`, with the command that produced it written beside the excerpt so a reader can reproduce it:

1. Observation capture — what a worker reported, in what shape, and what the runtime did with it immediately.
2. Instinct formation and confidence adjustment — the promotion threshold, the step size, and specifically whether confidence ever moved DOWN, and on what evidence.
3. Queen memory promotion — what made a lesson eligible to reach the shared instruction file, and whether genuine use was required.
4. Hive cross-colony sharing — what crossed a project boundary, under what confidence gate, and whether the receiving project could tell where it came from.
5. Midden failure reinforcement — how a repeated failure changed later behaviour, and after how many recurrences.
6. Signal strength reinforcement and decay — the arithmetic, the floor and ceiling, and what expiry did.
7. Learning presentation — the exact heading and wording a worker was shown, and whether the wording distinguished a proven lesson from an untested one.

For each mechanism also record three judgements the synthesis will consume: whether the behaviour was causally real, presentation only, or honour-system (the worker asserted it and nothing checked); which limitation or unsafe shortcut must not return; and whether the mechanic differed meaningfully before and after the shell-to-Go migration.

Where a mechanism genuinely cannot be recovered, write the literal words `no historical evidence recoverable` in that mechanism's own section, name every search that was run, and state the consequence for the synthesis. Do not substitute a plausible reconstruction for a missing citation, and do not treat the markdown design spec `3a5b81c2:.aether/learning.md` as a substitute for the executable code where the code survives — where both exist, cite both and say where they disagree.

Do not modify any historical file, any planning artefact other than this new one, or any Go source.</action>
  <acceptance_criteria>
    - The file exists and contains at least one `git show` invocation string per mechanism section.
    - The file contains a section heading naming each of the seven mechanics: observation capture, instinct confidence, Queen memory promotion, Hive sharing, midden reinforcement, signal strength, learning presentation.
    - Every mechanism section contains either at least one fenced verbatim excerpt or the exact phrase `no historical evidence recoverable`.
    - The file names commit `6732b0ba` and states which mechanics were recovered from before it and which only from after it.
    - Every mechanism section states one of: causally real, presentation only, or honour-system.
    - `git status --porcelain` shows no modification to any file outside `.planning/phases/204-learning-governor/`.
  </acceptance_criteria>
  <verify>
    <automated>EV=.planning/phases/204-learning-governor/204-CLASSIC-HISTORICAL-EVIDENCE.md; test -f "$EV" &amp;&amp; grep -q "git show" "$EV" &amp;&amp; for s in "observation capture" "instinct confidence" "Queen memory promotion" "Hive sharing" "midden reinforcement" "signal strength" "learning presentation"; do grep -qi "$s" "$EV" || exit 1; done &amp;&amp; grep -q "6732b0ba" "$EV" &amp;&amp; grep -qiE "causally real|presentation only|honour-system" "$EV"</automated>
  </verify>
  <done>The historical half of the SYNTH-06 study rests on quotable code rather than on one design document and a memory, and every gap in it is written down instead of filled in.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the cited Phase 204 learning-governor mechanism synthesis</name>
  <files>.planning/phases/204-learning-governor/204-CLASSIC-SYNTHESIS.md</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .planning/phases/204-learning-governor/204-CLASSIC-SYNTHESIS.md (create; confirm absent)
    - .planning/phases/204-learning-governor/204-CLASSIC-HISTORICAL-EVIDENCE.md
    - .planning/research/v1.28-classic-synthesis-template.md
    - .planning/research/v1.28-classic-capability-ledger.md
    - .planning/phases/204-learning-governor/204-RESEARCH.md
    - .planning/phases/204-learning-governor/204-PATTERNS.md
    - .planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md
    - .planning/phases/203-biological-runtime/203-VERIFICATION.md
    - .planning/WINDOWS.md
    - .planning/audits/2026-08-30-whole-system-audit.md
    - cmd/recruitment_credit.go
    - cmd/pheromone_outcome.go
    - cmd/instinct_application.go
    - cmd/colony_prime_context.go
    - cmd/autopilot_lessons.go
    - cmd/memory_feed.go
    - cmd/consolidation_lifecycle.go
    - cmd/learning_cmds.go
    - cmd/midden_shared.go
    - cmd/hive.go
    - cmd/work_repair.go
    - cmd/suggest_approve.go
    - cmd/agency_contract.go
    - cmd/trophallaxis.go
    - pkg/learn/learn.go
    - pkg/learn/colony_store.go
    - pkg/learn/skills.go
    - pkg/learn/difficulty.go
    - pkg/colony/instincts.go
    - pkg/colony/midden.go
    - pkg/colony/pheromones.go
    - pkg/colony/lifecycle.go
    - pkg/memory/promote.go
    - pkg/memory/instinct_stats.go
    - pkg/events/bus.go
    - pkg/events/event.go
    - pkg/events/colony_live.go
    - pkg/agent/curation/librarian.go
  </read_first>
  <action>Instantiate `.planning/research/v1.28-classic-synthesis-template.md` for area `learning`, phase `204`. Front matter sets `phase: "204"` and `area: "learning"`, lists the historical refs actually used, and sets `current_revision` to this working tree's own HEAD commit.

Section 2 (Classic mechanism reconstruction): one `OLD-` row per mechanism recovered in Task 1, each citing that file's own `git show` reference. A mechanism Task 1 could not recover still gets a row, carrying the not-recoverable citation and a current-only disposition.

Section 3 (Current Go mechanism audit): re-verify every one of the thirteen stores in RESEARCH.md's Current Learning Store Census against the code at this working tree's HEAD, as `NOW-` rows citing `path:line` plus the named test that locks each claim where one exists. RESEARCH.md declares a fourteen-day validity window and names two findings for immediate re-verification before implementation; do that re-verification here and record the result either way. Cover at minimum these symbols: `recordRecruitmentCredit`, `recruitmentCreditRecordID`, `recruitmentCreditForDecision`, `recruitmentContributionMemoryItem`, `recruitmentContributionSpecialist`, `tuneNoteStrengthFromOutcomes`, `pheromoneOutcomeReadCreditRecords`, `noteHarmfulQuarantineThreshold`, `captureContinueLearning`, `learn.StatusHypothesis`, `learn.StatusValidated`, `learn.StatusDisproven`, `learn.EntryFilter`, `confirmedAutopilotLessonsSincePlan`, `recordInstinctDeliveries`, `recordInstinctApplicationsForPhase`, `SummarizeInstinctApplications`, `recordDispatchWorkerOutcome`, `runPhaseEndConsolidation`, `runSealConsolidation`, `appendMiddenEntryOnce`, `promoteToHiveWithReference`, `appendEternalMemoryEntry`, `AutoCreateSkillIfDifficult`, `LoadAutoSkillMode`, `SkillService`, `events.Bus.Query`, `events.DefaultTTL`, `emitColonyLive`, `saveRepairCheckpoint`, `restoreRepairCheckpoint`, `colony.PendingSuggestion`, `colony.SanitizeSignalContent`, `colony.LifecycleDecision`, `recordTrophallaxisDecision`, and `pkg/graph`'s import set.

Record these six rulings in writing as decided rows rather than silent assumptions. Each must carry its own citation and name the plan that owns the change.

(a) **The credit ledger is the highest-value orphan in the system.** `recordRecruitmentCredit` has no non-test caller; its reader `tuneNoteStrengthFromOutcomes` runs on every check on both lanes against a permanently empty store. This is the outcome-weighted tuning the owner explicitly asked for in the immediately preceding phase. Rule that this phase wires a real production writer into that exact function rather than building a second outcome store, and name plan 204-02 as the owner.

(b) **Unverified guesses are shown to every worker under a heading that calls them verified.** `colony_prime_context.go`'s learning-entry filter sets no status, and the heading it writes claims verified outcomes, while `captureContinueLearning` writes hypothesis status on every check. Record the second, separate site the research did not name: `confirmedAutopilotLessonsSincePlan` excludes disproven entries and applies real gate evidence, but still admits hypothesis-status entries and calls the result a confirmed lesson. Rule that one shared status-aware vocabulary governs both sites, and name plan 204-02 as the owner.

(c) **Two frozen capability dispositions are stale and must be re-adjudicated, not inherited.** CAP-001 (observation to instinct to Queen promotion) and CAP-057 (signal expiry to long-term memory) were both written against a state the code has since left: `recordDispatchWorkerOutcome` now genuinely writes the observation log on both build lanes, and signal expiry now genuinely writes long-term memory. Re-adjudicate both against code read in this task and state the revised disposition explicitly.

(d) **The runtime can already create a skill with no owner involvement.** `captureContinueLearning` calls `AutoCreateSkillIfDifficult` on every check; in `auto` mode that creates an active skill with no approval, and the mode is read from a bare file in the data directory with a default that this task must read and state. LEARN-07 names skills as retaining required authority. Rule how that is closed — the narrowest closure being that an automatically derived skill enters the existing owner approval queue rather than becoming active — and name plan 204-09 as the owner.

(e) **The relationship graph is doubly orphaned and is not a foundation.** `pkg/graph` is imported by exactly one file whose own commands have no caller, and the curation ant most plausibly responsible does read-only inventory counting. Rule its disposition as retire-with-proof unless this study identifies a concrete new consumer, naming the specific edge type and the specific query call site that would read it. Do not let any later plan cite its existence as justification.

(f) **The one event bus forgets after thirty days.** `events.Bus.Query` and `Replay` both exclude events past `ExpiresAt`, and `DefaultTTL` is thirty days. Read `TestOneLiveEventModelOnly`'s actual assertion body before ruling, and record what it does and does not forbid: it is an abstract-syntax-tree scan for a second event-shaped type reaching a file write inside package `cmd`, not a count of bus constructions. State the ruling — that liveness stays on the one bus and durability lives beside it in a never-expiring record — and name plan 204-04 as the owner.

Section 4 (Comparative synthesis matrix): record exactly twelve rows with these identities and subjects, each with a disposition drawn from keep-current, restore-modern, replace-better and retire-with-proof, and each citing both an OLD row and a NOW row or an explicit not-recoverable note. SYN-204-01 one shared status vocabulary, so a guess is never rendered under a verified heading; SYN-204-02 versioned, provenance-carrying memory schemas with every writerless legacy field explicitly migrated or retired; SYN-204-03 one evidence-gated credit ledger serving every contribution kind, never a second outcome store; SYN-204-04 an immutable episode and outcome ledger with idempotent derived views, liveness on the one event bus and durability beside it; SYN-204-05 application evidence earned rather than inferred, with a real negative branch; SYN-204-06 worker self-report treated as untrusted input the runtime independently verifies; SYN-204-07 confirmed incidents becoming sanitized, deduplicated, versioned failing fixtures with provenance and retirement rules; SYN-204-08 named budgeted gates tiering the existing corpus, with holdouts that cannot be found by searching test names; SYN-204-09 a candidate that structurally cannot select or edit its evaluator; SYN-204-10 only reversible low-risk project knowledge or routing entering a bounded canary, with the retained-authority list refused by name; SYN-204-11 atomic rollback and quarantine reusing the proven checkpoint-verify-restore and threshold-count patterns; SYN-204-12 honest improvement reporting plus a source boundary that cannot self-approve, merge, publish or deploy.

Merge rule and order rule, both stated in the document: two candidate rows whose subject is exactly equal become one row carrying both citations rather than two rows, and rows are written in ascending identifier order with the identifier as the tie-break for equal-weight subjects.

Route and independently adjudicate the seven capability rows the ledger sends to Phase 204: CAP-001, CAP-025, CAP-043, CAP-055, CAP-057, CAP-067 and CAP-070. The ledger's proposed disposition is a hypothesis; confirm or revise each with its own cited evidence.

Section 6 (Research-to-plan linkage): map every requirement — SYNTH-06, LEARN-01, LEARN-02, LEARN-03, LEARN-04, LEARN-05, LEARN-06, LEARN-07 and LEARN-08 — to at least one SYN-204 row and at least one named plan in the range 204-02 to 204-11.

Include a rejected-alternatives table naming at minimum: a second outcome ledger beside the credit ledger; a second event bus or a second bus instance beside the existing one; a second approval queue beside the existing tick-to-approve surface; a second quarantine mechanism beside the existing threshold-count one; a second test runner beside the existing suite; reviving the relationship graph as decoration; and reviving worker self-reported per-instinct success without independent verification.

Include a section headed "Unresolved edge probes (flagged planner assumptions)" listing the six requirement probes this phase could not auto-resolve — LEARN-01, LEARN-03, LEARN-04, LEARN-05, LEARN-06 and LEARN-07 — each with the one-line assumption the plans proceed on and the plan number affected if that assumption is wrong. State plainly that these are surfaced assumptions, not resolved criteria.

Include a section headed "Owner-facing summary" written in plain English: no word this project invented — colony, caste, pheromone, midden, instinct, hive, trophallaxis, ratchet, canary, holdout — may appear in it without an inline translation in the same sentence. It must answer, in ordinary words, the one question this phase raises for the owner: how much the system may change about itself without asking, and what it may never change without being asked.

Do not modify the historical evidence file, the capability ledger, the template, or any Go source.</action>
  <acceptance_criteria>
    - The document contains each identifier `SYN-204-01` through `SYN-204-12` exactly once as a decision heading or row identifier.
    - Each of CAP-001, CAP-025, CAP-043, CAP-055, CAP-057, CAP-067 and CAP-070 appears in a routed coverage table with exactly one disposition and at least one cited source path.
    - Each of SYNTH-06, LEARN-01, LEARN-02, LEARN-03, LEARN-04, LEARN-05, LEARN-06, LEARN-07 and LEARN-08 appears in the linkage section next to at least one SYN-204 identifier and one plan number in the range 204-02 to 204-11.
    - The current-audit table contains at least one `path:line` citation per NOW row and covers all thirteen census stores.
    - The six rulings (a) through (f) each appear as a decided row or a named open-decisions entry, and the text contains the literal strings `recordRecruitmentCredit`, `confirmedAutopilotLessonsSincePlan`, `AutoCreateSkillIfDifficult`, `TestOneLiveEventModelOnly` and `pkg/graph`.
    - A section headed with the words `Unresolved edge probes` lists all six of LEARN-01, LEARN-03, LEARN-04, LEARN-05, LEARN-06 and LEARN-07.
    - A section headed with the words `Owner-facing summary` exists and states what the system may and may not change about itself.
    - Front matter sets `phase: "204"` and `area: "learning"`.
  </acceptance_criteria>
  <verify>
    <automated>SYN=.planning/phases/204-learning-governor/204-CLASSIC-SYNTHESIS.md; test -f "$SYN" &amp;&amp; for i in 01 02 03 04 05 06 07 08 09 10 11 12; do test "$(grep -c "SYN-204-$i" "$SYN")" -ge 1 || exit 1; done &amp;&amp; for c in 001 025 043 055 057 067 070; do grep -q "CAP-$c" "$SYN" || exit 1; done &amp;&amp; for r in SYNTH-06 LEARN-01 LEARN-02 LEARN-03 LEARN-04 LEARN-05 LEARN-06 LEARN-07 LEARN-08; do grep -q "$r" "$SYN" || exit 1; done &amp;&amp; for s in "recordRecruitmentCredit" "confirmedAutopilotLessonsSincePlan" "AutoCreateSkillIfDifficult" "TestOneLiveEventModelOnly" "pkg/graph" "Unresolved edge probes" "Owner-facing summary"; do grep -q "$s" "$SYN" || exit 1; done &amp;&amp; grep -q 'area: "learning"' "$SYN"</automated>
  </verify>
  <done>One evidence-backed brief covers every Phase 204 mechanism, store, routed capability and open question, so no later plan can be justified by a feature name, a stale audit row, or a remembered behaviour.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register SYN-204 mechanisms in the versioned Classic corpus</name>
  <files>cmd/testdata/classic-contract/v1/schema.json, cmd/testdata/classic-contract/v1/mechanisms.json, cmd/classic_contract_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/testdata/classic-contract/v1/schema.json
    - cmd/testdata/classic-contract/v1/mechanisms.json
    - cmd/classic_contract_test.go
    - .planning/phases/204-learning-governor/204-CLASSIC-SYNTHESIS.md
  </read_first>
  <behavior>
    - A mechanism entry whose id is outside the registered SYN-199, SYN-200, SYN-201, SYN-202, SYN-203 and SYN-204 sets fails registry validation and the error names the offending identifier.
    - A case declaring `synthesis_decision` of `SYN-204-05` and group `V-204-EVIDENCE` validates against the schema.
    - A case declaring a SYN-204 decision that no mechanism entry registers fails coverage validation by name.
    - An identifier differing from a registered one only by letter case is rejected, and the rejection names the identifier it was compared against.
    - Every SYN-204 mechanism entry names its capability identifiers, public commands, source citations and owning groups.
    - Loading the registry twice and comparing the parsed result yields identical entries in identical order, and the identifiers are in ascending order within the SYN-204 block.
    - No non-test Go file in the repository writes the registry file.
  </behavior>
  <action>Widen `schema.json`'s synthesis-decision pattern at both places it appears — `$defs/case/properties/synthesis_decision/pattern` and `$defs/mechanism/properties/id/pattern` — from its current value, whose final alternation branch is `203-(?:0[1-9]|1[0-2])`, so the alternation additionally accepts `204-(?:0[1-9]|1[0-2])`. Both patterns currently hold the same string; both must be widened identically.

Add these members to `$defs/case/properties/group/enum` alongside the existing ones without removing any: `V-204-TRUTH`, `V-204-LEDGER`, `V-204-EVIDENCE`, `V-204-FIXTURES`, `V-204-GATES`, `V-204-PROMOTION`.

Append twelve mechanism entries `SYN-204-01` through `SYN-204-12` to `mechanisms.json`, in ascending identifier order, each carrying the required registry fields `id`, `title`, `disposition`, `cap_ids`, `public_commands`, `source_citations` and `groups`, with values taken verbatim from the synthesis Task 2 produced. The capability identifiers distributed across these entries must between them cover CAP-001, CAP-025, CAP-043, CAP-055, CAP-057, CAP-067 and CAP-070. Add `.planning/phases/204-learning-governor/204-CLASSIC-SYNTHESIS.md` to the `synthesis_sources` array; leave the legacy scalar `synthesis_source` key untouched. Public commands for these entries are the owner-facing spellings `/ant-continue`, `/ant-build`, `/ant-status`, `/ant-seal`, `/ant-patrol`, `/ant-skill-create` and `/ant-pheromones`, matching the vocabulary Phase 199 locked.

Extend `cmd/classic_contract_test.go` with a `TestClassicContractPhase204MechanismRegistry` that loads the registry through the existing strict loader, asserts the twelve SYN-204 identifiers are present exactly once each and in ascending order, asserts every entry names at least one capability identifier and at least one source citation, asserts the union of capability identifiers across SYN-204 entries contains all seven routed rows, and proves by negative fixture that an unregistered SYN identifier, a case referencing an unregistered decision, and an identifier differing only by letter case each fail with the offending identifier in the message.

Add a second test `TestClassicContractRegistryHasNoRuntimeWriter` that parses every non-test `.go` file under `cmd/` and `pkg/` and fails, naming the offending file and function, if any of them contains a write call whose path argument mentions the registry file's own name — proving the registry is an authored artefact no concurrent runtime write can corrupt. Include a synthetic fixture in that test proving the detector is not vacuous.

Do not add executable behaviour cases to the corpus here — the later implementation plans register their own cases once the behaviours exist.</action>
  <acceptance_criteria>
    - `python3 -c "import json;json.load(open('cmd/testdata/classic-contract/v1/schema.json'))"` succeeds and the widened pattern string accepts `SYN-204-01`.
    - The registry contains exactly twelve entries whose id begins `SYN-204-`, and the pre-existing ten SYN-199, twelve SYN-200, fourteen SYN-201, twelve SYN-202 and twelve SYN-203 entries are all still present, for seventy-two total.
    - The twelve SYN-204 entries appear in ascending identifier order within the registry array.
    - The union of `cap_ids` across the SYN-204 entries contains CAP-001, CAP-025, CAP-043, CAP-055, CAP-057, CAP-067 and CAP-070.
    - `synthesis_sources` contains `.planning/phases/204-learning-governor/204-CLASSIC-SYNTHESIS.md`.
    - The three negative fixtures fail with the offending identifier named in the error text.
    - `TestClassicContractRegistryHasNoRuntimeWriter` passes against the real tree and reports its synthetic fixture by name.
    - `TestClassicContractSchema`, `TestClassicMechanismCoverage`, `TestClassicContractPhase201MechanismRegistry`, `TestClassicContractPhase202MechanismRegistry` and `TestClassicContractPhase203MechanismRegistry` still pass unchanged.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestClassicContractSchema|TestClassicMechanismCoverage|TestClassicContractPhase201MechanismRegistry|TestClassicContractPhase202MechanismRegistry|TestClassicContractPhase203MechanismRegistry|TestClassicContractPhase204MechanismRegistry|TestClassicContractRegistryHasNoRuntimeWriter)$' -count=1 -timeout 90m &amp;&amp; python3 -c "import json,sys; m=json.load(open('cmd/testdata/classic-contract/v1/mechanisms.json')); ids=[x['id'] for x in m['mechanisms']]; four=[i for i in ids if i.startswith('SYN-204-')]; caps=set(c for x in m['mechanisms'] if x['id'].startswith('SYN-204-') for c in x['cap_ids']); ok=len(four)==12 and four==sorted(four) and len([i for i in ids if i.startswith('SYN-203-')])==12 and len([i for i in ids if i.startswith('SYN-202-')])==12 and len([i for i in ids if i.startswith('SYN-201-')])==14 and len([i for i in ids if i.startswith('SYN-200-')])==12 and len([i for i in ids if i.startswith('SYN-199-')])==10 and len(ids)==72 and {'CAP-001','CAP-025','CAP-043','CAP-055','CAP-057','CAP-067','CAP-070'}.issubset(caps) and any('204-learning-governor' in s for s in m['synthesis_sources']); sys.exit(0 if ok else 1)"</automated>
  </verify>
  <done>The executable Classic corpus can carry Phase 204 mechanism identities, so later plans register cases against a contract that already exists rather than inventing one late.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created by this plan (new to the codebase — not drift):

- `.planning/phases/204-learning-governor/204-CLASSIC-HISTORICAL-EVIDENCE.md` (new file)
- `.planning/phases/204-learning-governor/204-CLASSIC-SYNTHESIS.md` (new file)
- Mechanism identifiers `SYN-204-01` … `SYN-204-12` (new registry entries)
- Corpus group names `V-204-TRUTH`, `V-204-LEDGER`, `V-204-EVIDENCE`, `V-204-FIXTURES`, `V-204-GATES`, `V-204-PROMOTION` (new schema enum members)
- `TestClassicContractPhase204MechanismRegistry` (new Go test)
- `TestClassicContractRegistryHasNoRuntimeWriter` (new Go test)

<verification>
- The synthesis covers SYNTH-06's full scope: observations, instincts, Queen memory, Hive wisdom, midden failures, signal reinforcement and learning presentation, reconciled against every current store and reader.
- All seven routed capability rows carry an independently adjudicated disposition, and the two the research flags as stale are explicitly re-adjudicated against code read at this HEAD.
- The corpus schema and registry accept SYN-204 identifiers and reject unknown or case-variant ones by name.
- Existing Phase 199, 200, 201, 202 and 203 corpus tests remain green.
</verification>

<success_criteria>
- No later Phase 204 plan can be executed without a cited disposition to point at.
- The six rulings — the orphaned credit ledger, the verified-looking guesses, the two stale capability rows, the unapproved skill creation, the doubly-orphaned graph, and the thirty-day forgetting — are decided in writing, not left to an executor's improvisation.
</success_criteria>

<output>
Create `.planning/phases/204-learning-governor/204-01-SUMMARY.md` when done.
</output>
