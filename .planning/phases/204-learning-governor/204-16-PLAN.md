---
phase: 204-learning-governor
plan: 16
type: execute
wave: 3
depends_on: ["204-12", "204-13", "204-14", "204-15"]
files_modified:
  - cmd/source_proposal.go
  - cmd/source_proposal_test.go
  - cmd/improvement_pass.go
  - cmd/improvement_pass_test.go
  - cmd/codex_visuals.go
  - cmd/learning_validator.go
  - cmd/learning_validator_test.go
  - cmd/consolidation_lifecycle.go
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
  - CLAUDE.md
  - .claude/rules/aether-colony.md
autonomous: true
requirements: [LEARN-08, LEARN-02, LEARN-05, LEARN-06, LEARN-07]
gap_closure: true

estimate:
  tokens: 126000
  raw_tokens: 126000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC5d: the running program can actually create a source-improvement proposal — an automatic trigger calls proposeSourceImprovement when the improvement report shows the same preventable intervention category repeatedly, and the trigger is reached from the same phase-end boundary both check lanes already reach (D-01, D-05)."
    - "A source proposal still structurally cannot self-approve, self-merge, publish or deploy, and the new automatic entry point is inside the call-graph walk that proves it (WINDOWS 45)."
    - "A learned entry recorded as a hypothesis is promoted to validated automatically once the program's own records show that guidance genuinely helped — never merely because a note was delivered or a phase passed (WINDOWS 44)."
    - "REQUIREMENTS.md tells the truth at the end of this closure: a requirement is ticked only where its own wording is now true of the running program, and any requirement still only partly true carries its remaining limit in writing rather than a tick (D-11)."
    - "WINDOWS.md entries 38 through 45 are each closed, or narrowed with the remaining limit named, and the markdown table and the JSON ledger agree on every one of them (D-11)."
    - "The SYNTH-06 probes (adjacency, empty, ordering, idempotency, concurrency) are surfaced as flagged assumptions against an already-satisfied requirement this closure does not modify, rather than silently dropped."
  prohibitions:
    - "A source proposal must never merge, push, publish, deploy, tag, release or approve itself, and no new call path may reach an operation that could."
    - "The automatic proposal trigger must never run more than once for the same evidence, must never leave the working tree or the checked-out branch changed, and must never block or fail a check."
    - "A hypothesis must never be promoted to validated on a worker's own claim, on a delivered note, or on a passing phase — only on a recorded helpful application the program independently corroborated."
    - "A requirement must never be ticked in REQUIREMENTS.md on the evidence that code exists; the named wiring test must pass and must fail when its production call site is removed."
    - "A WINDOWS.md entry must never be marked fixed while its own defect is still reachable; narrowing with the remaining limit named is the honest alternative."
  assumptions:
    - "Edge probe SYNTH-06/adjacency is surfaced unresolved: SYNTH-06 (the mechanism study) is a completed documentation artifact this closure does not modify, so no defensible acceptance criterion for gap-closure work can be written against it."
    - "Edge probe SYNTH-06/empty is surfaced unresolved, same reason."
    - "Edge probe SYNTH-06/ordering is surfaced unresolved, same reason."
    - "Edge probe SYNTH-06/idempotency is surfaced unresolved, same reason."
    - "Edge probe SYNTH-06/concurrency is surfaced unresolved, same reason."
  artifacts:
    - "cmd/learning_validator.go — the automatic hypothesis-to-validated promoter"
    - ".planning/REQUIREMENTS.md — truthful satisfaction state at the end of the closure"
    - ".planning/WINDOWS.md — entries 38-45 closed or narrowed, table and JSON ledger in agreement"
  key_links:
    - "cmd/improvement_pass.go (runAutomaticImprovementPass) -> cmd/source_proposal.go (proposeSourceImprovement)"
    - "cmd/consolidation_lifecycle.go (runPhaseEndConsolidation) -> cmd/learning_validator.go (promoteHelpfulHypotheses)"
    - "cmd/application_evidence.go (guidanceApplicationStateHelpful records) -> cmd/learning_validator.go -> pkg/learn (StatusValidated)"
---

<objective>
Close the last verification gap, SC5d, close the validator gap WINDOWS entry 44 records, and make this project's own ledgers tell the truth about what is and is not done.

`proposeSourceImprovement` and its cannot-merge/publish/deploy refusal are real and tested, but a repo-wide search finds no non-test caller — the structural boundary is real while the capability it bounds does not run. Separately, `learn.StatusValidated` is written only by the hand-run `learning-validate` command, so the verified-memory section a worker reads stays honestly empty in any unattended run even though the filtering machinery is correct and wired at both render sites.

Purpose: give the proposal capability a real automatic trigger inside the bounds D-01 and D-04 set; give the validator the credit-ledger feed WINDOWS 44 names; and finish the closure by updating REQUIREMENTS.md and WINDOWS.md to what the tests actually prove, per D-11.

Output: an automatic, repeat-evidence-gated source proposal; an automatic hypothesis promoter gated on a recorded helpful application; and a truthful requirements and defect register.

**Depends on:** 204-12 (the automatic pass this trigger hangs off), 204-13 (the closed intervention vocabulary the trigger reads), 204-14 (the fixture floor whose real number WINDOWS 42 is narrowed to), 204-15 (the episode fields the improvement report needs to produce a real repeat signal). This plan runs last because D-11's truth-telling may only be written after the tests it cites have been confirmed green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/204-learning-governor/204-CONTEXT.md
@.planning/phases/204-learning-governor/204-VERIFICATION.md
@.planning/phases/204-learning-governor/204-10-SUMMARY.md
@.planning/phases/204-learning-governor/204-11-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A repeated preventable intervention automatically becomes a source-improvement proposal</name>
  <files>cmd/source_proposal.go, cmd/source_proposal_test.go, cmd/improvement_pass.go, cmd/improvement_pass_test.go</files>
  <read_first>
    - cmd/source_proposal.go (all of it — `sourceProposalPath`, `sourceProposalBranchPrefix`, `sourceProposalVerificationState` and its vocabulary, `sourceChangeSetFile`, `sourceChangeSet`, `sourceProposal`, `sourceProposalID`, `sourceProposalBranchName`, `writeSourceProposal`, `sourceProposalWorkingTreeIsDirty`, `sourceProposalBranchExists`, `sourceProposalApplyChangeSet`, `proposeSourceImprovement`, `recordIndependentVerification`, `renderSourceProposalAnnouncement`, `renderSourceProposalReadiness`, `sourceProposalForbiddenOperation` and `sourceProposalForbiddenOperations`)
    - cmd/source_proposal_test.go lines 420-460 (`sourceProposalReachabilityEntryPoints` and its comment explaining that the two library functions currently ARE the entry points because nothing calls them) and lines 760-800 (`sourceProposalCallGraphViolations` and how the entry-point list feeds it)
    - cmd/improvement_report.go lines 246-300 (`improvementReportInterventionEntry`, `collectPreventableInterventions`, `improvementReport`) as left by plan 204-13
    - cmd/improvement_pass.go (as written by plan 204-12) — `improvementPassSummary`, `runAutomaticImprovementPass`, and the non-blocking discipline it follows
    - cmd/rollback.go lines 95-120 (`canaryRegressionQuarantineThreshold` and its comment on why a named constant, never an inline number, and why a separate constant per domain)
    - cmd/episode_ledger.go (`readEpisodeLedger`) and cmd/eval_gates.go (`loadEvalGateSentinels`)
  </read_first>
  <behavior>
    - Test 1: a ledger in which the same declared intervention kind appears on three distinct episodes triggers exactly one source proposal, on a branch carrying exactly one new file, with the working tree and checked-out branch unchanged afterwards.
    - Test 2: the same evidence seen a second time creates no second proposal and no second branch (the existing deterministic proposal identity already gives this; assert it through the automatic path).
    - Test 3: two occurrences of the same kind trigger nothing.
    - Test 4: three occurrences of three DIFFERENT kinds trigger nothing — the threshold is per category, not a total.
    - Test 5: a dirty working tree is refused by name, creates nothing, and does not fail the check.
    - Test 6: the automatic trigger is inside `sourceProposalReachabilityEntryPoints`, and `TestSourceProposalCannotMergePublishOrDeploy` still finds no path from it to any forbidden operation.
  </behavior>
  <action>
Give `proposeSourceImprovement` its first real caller, gated on evidence the program measured rather than on a schedule.

**(a) The trigger.** In `cmd/improvement_pass.go`, extend `runAutomaticImprovementPass` with a final step: build the improvement report over the episodes already on disk (`readEpisodeLedger` plus `loadEvalGateSentinels`, unbounded window), count the preventable-intervention entries per declared `episodeInterventionKind`, and when any single category's count reaches a new named constant `sourceProposalRepeatedInterventionThreshold` (set it to 3, matching `canaryRegressionQuarantineThreshold`'s "the same kind of thing keeps going wrong" reading, declared separately in `cmd/source_proposal.go` because the two domains count different identities — follow the reasoning `canaryRegressionQuarantineThreshold`'s own comment sets out), call `proposeSourceImprovement`.

  - `candidateID` is derived deterministically from the repeated category, so the same repeated problem always proposes under the same identity and the existing replay rule collapses a second run to no second branch.
  - `evidence` is the episode identifiers that carried that category — real identifiers the ledger holds, never a synthesised list.
  - `changes` is a `sourceChangeSet` writing EXACTLY ONE new markdown file at `.aether/proposals/<proposal-id>.md`, plus a commit message naming the repeated problem in plain English. Before choosing that directory, confirm it is not one of the trees `aether publish` / `aether install` sync (read `cmd/install_cmd.go`'s sync list: it names `.aether/skills` and `.aether/docs` and the command wrapper trees, not `.aether/proposals`); if that has changed, pick a sibling directory that is not synced and name the choice in the SUMMARY. The file's content is the plain-English case: what keeps happening, on which episodes, and what change is being proposed. No code is generated, and nothing outside that one file is touched — a proposal is a case for a person to read, not an applied change.
  - The whole step is non-blocking and inherits the pass's existing contract: a refusal (dirty tree, branch collision, no store) is recorded in the summary and the check continues. `renderImprovementPassBeat` gains one more plain-English line for "a proposal was written", per D-03.

**(b) Keep the structural boundary covering the new entry point.** Add the trigger function's name to `sourceProposalReachabilityEntryPoints` in `cmd/source_proposal_test.go` in the SAME change, and rewrite that variable's stale comment, which currently states that no command invokes either function. `TestSourceProposalCannotMergePublishOrDeploy` must then walk from the new entry point too and still find no path to a forbidden operation — this is the whole reason WINDOWS entry 45 says to extend the list in the same change as the caller.

**(c) Do not widen the boundary.** Do not add any field, flag, parameter or helper to `cmd/source_proposal.go` that could push, merge, tag, release, publish or deploy. `sourceProposalForbiddenOperations` and its four families stay exactly as they are, and `TestForbiddenOperationListIsReadFromTheSource` must stay green.
  </action>
  <verify>
    <automated>go test ./cmd -run '^(TestRepeatedInterventionProposesExactlyOneSourceChange|TestTwoOccurrencesProposeNothing|TestThresholdIsPerCategoryNotATotal|TestAutomaticProposalReplayCreatesNoSecondBranch|TestDirtyTreeAutomaticProposalIsRefusedAndNeverBlocks|TestSourceProposalCannotMergePublishOrDeploy|TestForbiddenOperationListIsReadFromTheSource)$' -count=1 -timeout 15m</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd -run '^(TestRepeatedInterventionProposesExactlyOneSourceChange|TestTwoOccurrencesProposeNothing|TestThresholdIsPerCategoryNotATotal|TestAutomaticProposalReplayCreatesNoSecondBranch|TestDirtyTreeAutomaticProposalIsRefusedAndNeverBlocks|TestSourceProposalCannotMergePublishOrDeploy|TestForbiddenOperationListIsReadFromTheSource)$' -count=1 -timeout 15m` exits 0.
    - Source assertion: `grep -rln 'proposeSourceImprovement(' cmd/ --include='*.go' | grep -v _test.go` lists `cmd/improvement_pass.go` in addition to `cmd/source_proposal.go` — the first non-test caller in the tree.
    - Source assertion: the new trigger function name appears in `sourceProposalReachabilityEntryPoints`; confirm by running `go test ./cmd -run '^TestSourceProposalCannotMergePublishOrDeploy$' -count=1 -v` and checking it reports walking more than two entry points.
    - Named constant, not a literal: `grep -c 'sourceProposalRepeatedInterventionThreshold' cmd/source_proposal.go` returns at least 1.
    - FAILS-WHEN-UNWIRED (perform, record, revert): comment out the `proposeSourceImprovement(` call in `cmd/improvement_pass.go` and confirm `TestRepeatedInterventionProposesExactlyOneSourceChange` exits NON-ZERO.
    - Working tree safety: `TestRepeatedInterventionProposesExactlyOneSourceChange` asserts the checked-out branch name and `git status --porcelain` output are identical before and after the trigger runs.
    - Non-blocking: `go test ./cmd -run '^TestImprovementPassFailureNeverBlocksTheCheck$' -count=1` still exits 0.
    - `go build ./cmd/aether` exits 0, `go vet ./cmd/...` exits 0, `gofmt -l` reports nothing for the files touched.
  </acceptance_criteria>
  <done>When the same kind of thing keeps going wrong, the program writes up the case on an isolated branch for a person to read — and still has no route by which it could approve, merge, publish or deploy that case itself.</done>
  <reversibility rating="costly">The trigger creates real git branches in the owner's repository automatically. Reversible (a branch is deleted with one command) but visible and not free, so the threshold and the one-file change set are chosen conservatively here and the trigger is proven to leave the working tree and checked-out branch exactly as it found them.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A hypothesis becomes validated only once the program's own records show it helped</name>
  <files>cmd/learning_validator.go, cmd/learning_validator_test.go, cmd/consolidation_lifecycle.go</files>
  <read_first>
    - cmd/learning_status_vocabulary.go (all of it — `learningVerifiedEntries`, `learningUnverifiedEntries`, `learningEntryStatusEquals`, and the rule that a disproven entry belongs to neither set)
    - pkg/learn/learn.go lines 20-75 (`StatusHypothesis`, `StatusValidated`, `Entry` and every field) and pkg/learn/colony_store.go (`ColonyStore`, `Add`, `Get`, `Replace`, and the rule that an entry with no status defaults to a hypothesis)
    - cmd/learning_cmds.go lines 525-565 (the hand-run `learning-validate` command — the only writer of `StatusValidated` today; the automatic promoter must reuse the same `learnStore.Replace` write, not a second one)
    - cmd/application_evidence.go lines 336-610 (`guidanceApplicationState` with its nine members, `guidanceApplicationStateHelpful`, `guidanceApplicationRecord`, `guidanceApplicationRecordID`, `guidanceReachedStates`, `findGuidanceApplicationRecord`, `recordGuidanceApplicationState`) and lines 606-900 (`guidanceClaim`, `corroborateGuidanceClaim`, `recordGuidanceStatesForWorkerOutcome`) — note the rule that a worker's own claim is never taken on its own word
    - cmd/recruitment_credit.go lines 70-130 (`recruitmentContributionKind`, especially `recruitmentContributionMemoryItem`, and `recruitmentCreditRecord`)
    - cmd/consolidation_lifecycle.go lines 200-340 (`runPhaseEndConsolidation` and where the credit, hive, tuning and improvement-pass steps chain)
    - .planning/WINDOWS.md entry 44
  </read_first>
  <behavior>
    - Test 1: a learned entry in hypothesis status, with a guidance application record for its own identifier in the helpful state, is promoted to validated by the automatic pass, and then appears in `learningVerifiedEntries`.
    - Test 2: a hypothesis with a guidance record only in the rendered, consulted or acted-on state is NOT promoted.
    - Test 3: a hypothesis with a guidance record in the neutral or harmful state is NOT promoted.
    - Test 4: a hypothesis with a worker CLAIM record but no corroborated helpful application is NOT promoted.
    - Test 5: a hypothesis with no guidance record at all is NOT promoted, and the verified-memory section a worker reads stays honestly empty.
    - Test 6: running the promoter twice promotes once and writes nothing the second time.
    - Test 7: a disproven entry is never promoted by this path.
    - Test 8: the promoter is reached from both check lanes, by the same call-graph technique, because it sits on the `runPhaseEndConsolidation` chain.
  </behavior>
  <action>
Wire the validator WINDOWS entry 44 names, gated on the evidence the previous plans of this phase built.

Create `cmd/learning_validator.go` with `promoteHelpfulHypotheses(phaseID int) learningValidationSummary`. It reads every learned entry through `learn.NewColonyStore(store)`, selects those `learningUnverifiedEntries` reports as hypotheses, and for each one looks up the guidance application records for that entry's own identifier. An entry is promoted to `learn.StatusValidated` — through the same `learnStore.Replace` call the hand-run `learning-validate` command already uses, never a second writer — if and only if a guidance application record exists for it in the `guidanceApplicationStateHelpful` state. Every other state, including `acted_on`, is insufficient: acting on guidance is not the same as being helped by it, which is exactly the distinction LEARN-03 and the nine-state vocabulary were built to hold, and a worker's own unverified claim record is explicitly not evidence.

Call it from `runPhaseEndConsolidation` in `cmd/consolidation_lifecycle.go`, on the same chain as the credit, hive, tuning and improvement-pass steps, and record its counts on `phaseEndConsolidationSummary` so the one call site reaches both check lanes exactly as those steps do. It is non-blocking: a read or write failure warns to stderr and returns a summary reporting it, never an error a caller could propagate.

Do not change `learningVerifiedEntries`, `learningUnverifiedEntries` or `learningEntryStatusEquals`; the filtering machinery is already correct and wired at both render sites. Do not remove or weaken the hand-run `learning-validate` command — it stays as the manual path, exactly as `consolidation-phase-end` stays as the manual inspection path beside its automatic caller.
  </action>
  <verify>
    <automated>go test ./cmd -run '^(TestHelpfulHypothesisIsPromotedAutomatically|TestActedOnIsNotEnoughToValidate|TestNeutralOrHarmfulIsNeverValidated|TestUncorroboratedClaimIsNeverValidated|TestHypothesisWithNoRecordStaysAHypothesis|TestPromotionIsIdempotent|TestDisprovenEntryIsNeverPromoted|TestHypothesisPromoterIsReachedFromBothCheckLanes|TestHypothesisIsNeverRenderedAsVerified|TestOneLearningStatusVocabulary)$' -count=1 -timeout 15m</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd -run '^(TestHelpfulHypothesisIsPromotedAutomatically|TestActedOnIsNotEnoughToValidate|TestNeutralOrHarmfulIsNeverValidated|TestUncorroboratedClaimIsNeverValidated|TestHypothesisWithNoRecordStaysAHypothesis|TestPromotionIsIdempotent|TestDisprovenEntryIsNeverPromoted|TestHypothesisPromoterIsReachedFromBothCheckLanes|TestHypothesisIsNeverRenderedAsVerified|TestOneLearningStatusVocabulary)$' -count=1 -timeout 15m` exits 0.
    - Source assertion: `grep -c 'promoteHelpfulHypotheses(' cmd/consolidation_lifecycle.go` returns at least 1.
    - Both lanes: `TestHypothesisPromoterIsReachedFromBothCheckLanes` uses the same `buildCmdFuncGraph`/`reachableFrom` technique with lanes `runCodexContinue` and `runCodexContinueFinalize`, and includes the synthetic-unreachable subtest proving the scanner can fail.
    - One writer only: `grep -rc 'StatusValidated' cmd/ --include='*.go' | grep -v _test.go` shows `StatusValidated` written in exactly two places — the hand-run command and the new promoter — and in no third.
    - FAILS-WHEN-UNWIRED (perform, record, revert): comment out the `promoteHelpfulHypotheses(` call in `cmd/consolidation_lifecycle.go` and confirm `TestHelpfulHypothesisIsPromotedAutomatically` exits NON-ZERO.
    - Corroboration rule held: `go test ./cmd -run '^(TestCorroboratedClaimBecomesConsulted|TestCorroborationNeverReadsTheWorkersOwnText|TestPromotionRequiresAHelpfulApplication|TestGuidanceStateVocabularyIsClosed)$' -count=1` exits 0.
    - `go build ./cmd/aether` exits 0, `go vet ./cmd/...` exits 0.
  </acceptance_criteria>
  <done>A lesson the program recorded as a guess becomes a verified lesson only when the program's own independently corroborated records show it genuinely helped — automatically, at the end of every check, on both lanes.</done>
  <reversibility rating="reversible">One new function and one call site; the manual path is untouched, and a wrongly promoted entry is returnable to hypothesis status through the existing store.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Make REQUIREMENTS.md, WINDOWS.md and the shipped documents tell the truth</name>
  <files>.planning/REQUIREMENTS.md, .planning/WINDOWS.md, CLAUDE.md, .claude/rules/aether-colony.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 20-40 and 90-140 (the SYNTH-06 line, the LEARN-01 through LEARN-08 lines with their current checkbox state, and the phase coverage table row for Phase 204)
    - .planning/WINDOWS.md — the markdown table rows with ids 38 through 45, AND the JSON ledger block further down the same file that carries the same ids with `status`, `reason`, `recorded_at` and `resolved_at` fields. Both representations must be updated together.
    - .planning/phases/204-learning-governor/204-VERIFICATION.md (the `requirements_discrepancy` block and the Requirements Coverage table — the exact claims this task is correcting)
    - .planning/phases/204-learning-governor/204-CONTEXT.md, the Deferred Ideas section (code-valued shadow candidates, the three knowingly-empty memory fields, and guarding all 40 fixtures are all explicitly NOT part of this closure — they are narrowings, not closures)
    - CLAUDE.md, the "Learning Governor (v1.28, Phase 204)" section in full, and the Quick Reference table's slash-command counts
    - .claude/rules/aether-colony.md, the "Learning Governor (v1.28, Phase 204)" section and the command tables
    - cmd/claudemd_learning_governor_test.go or whichever file declares `TestCLAUDEMDLearningGovernorClaimsCiteLiveTests` and `TestCLAUDEMDLearningGovernorSectionIsPlainEnglish` (find it with a grep for the test name), so every new claim added to CLAUDE.md cites a test that genuinely exists
    - The SUMMARY files for plans 204-12 through 204-15, which carry the proven numbers this task writes down
  </read_first>
  <action>
Finish the closure by writing down only what the tests now prove.

**(a) REQUIREMENTS.md.** For each of SYNTH-06 and LEARN-01 through LEARN-08, decide the state from evidence, not from intent: tick the box only where the requirement's own wording is now true of the RUNNING program and a named wiring test proves it. Where a requirement is only partly true, leave it unticked and append one plain sentence naming the exact remaining limit. Specifically:
  - LEARN-02 is ticked only if plan 204-13's lane subtests and plan 204-15's field and census tests all pass.
  - LEARN-06 is ticked only if plan 204-12's grader tests pass; append the remaining limit that comparison still covers settings and rules and never code-valued candidates, which LEARN-06 itself scoped out.
  - LEARN-07 is ticked only if plan 204-12's promotion and canary wiring tests pass.
  - LEARN-08 is ticked only if plan 204-15's live-report test and this plan's Task 1 tests pass.
  - LEARN-05 is ticked only if its own wording (the named gates, sentinels, holdouts and budgets) is true; if the remaining unguarded fixture count is the reason to hold it back, say so with the exact number plan 204-14 proved rather than a vague note.
  - Update the Phase 204 row in the phase coverage table to the resulting state.

**(b) WINDOWS.md entries 38 through 45.** Update BOTH the markdown table row and the matching JSON ledger entry for each id, keeping `status`, `reason` and `resolved_at` consistent between the two representations. Expected dispositions, each to be confirmed against the actual test results before writing:
  - 38 (nine writerless episode fields) — fixed by plan 204-15; name the census registration as the guard that stops it reopening.
  - 39 (unregistered shadow commands) — fixed by plan 204-12; name the registered commands and the wrapper.
  - 40 (placeholder grader) — NARROWED, not fixed: the grader is real, and the remaining limit is that comparison still never covers code-valued candidates. Name that limit in the reason.
  - 41 (skipped swarm and recovery subtests) — fixed by plan 204-13; name the replacement failing branch.
  - 42 (fixture guard floor) — NARROWED: record the exact new floor plan 204-14 proved and state that the remainder is deliberately deferred.
  - 43 (three knowingly-empty memory fields) — unchanged and still open; it is explicitly deferred by 204-CONTEXT.md and closes only when a future phase names a consumer.
  - 44 (no automatic validator for a validated learning) — fixed by this plan's Task 2.
  - 45 (no source-proposal caller, no closed intervention vocabulary) — fixed by this plan's Task 1 and plan 204-13's Task 3; name both halves.
  Do not mark anything fixed whose defect is still reachable. A narrowing that names its remaining limit is the honest outcome and is what D-11 asks for.

**(c) CLAUDE.md and the distributed rules mirror.** Correct the "Learning Governor" section's prose where the verification found it overstating what an unattended run can do — specifically the claims about a proposed change being tried beside current behaviour and about running automatically on a watched reversible trial. Those claims become TRUE with plans 204-12 and 204-16, so the fix is to cite the tests that now prove them rather than to soften the wording: add the new test names to the section so `TestCLAUDEMDLearningGovernorClaimsCiteLiveTests` covers them, and keep every sentence plain-English so `TestCLAUDEMDLearningGovernorSectionIsPlainEnglish` stays green. Add a short plain-English paragraph covering the new hand-run command and what it shows. Update the Quick Reference slash-command counts to the new totals. Mirror the same corrections into `.claude/rules/aether-colony.md`'s Learning Governor section and its command tables, keeping the two documents consistent with each other.

**(d) Do not invent.** Every number written in this task — the fixture floor, the command counts, the requirement states — is read from the artifacts plans 204-12 through 204-15 actually produced, and is cross-checked against a command before it is written down.
  </action>
  <verify>
    <automated>go test ./cmd -run '^(TestCLAUDEMDLearningGovernorClaimsCiteLiveTests|TestCLAUDEMDLearningGovernorSectionIsPlainEnglish|TestCurrentVocabulary199|TestPlatformParityGolden)$' -count=1 -timeout 15m</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd -run '^(TestCLAUDEMDLearningGovernorClaimsCiteLiveTests|TestCLAUDEMDLearningGovernorSectionIsPlainEnglish|TestCurrentVocabulary199|TestPlatformParityGolden)$' -count=1 -timeout 15m` exits 0.
    - WINDOWS.md internal consistency: a parse check confirms every id from 38 to 45 present in the markdown table is also present in the JSON ledger block with the same `status` value. Record the check command and its output in the SUMMARY.
    - Every requirement ticked in REQUIREMENTS.md names, in the SUMMARY, the specific test whose passing justifies the tick — and for each one, the corresponding FAILS-WHEN-UNWIRED mutation recorded by plans 204-12 through 204-16 is cited by plan and test name.
    - Every requirement left unticked carries one plain sentence naming its remaining limit, with a real number where a number applies.
    - Entries 40 and 42 are NARROWED with their remaining limit named, not marked fixed. Entry 43 is unchanged and still open.
    - The fixture floor figure written into WINDOWS entry 42 equals the `seedBankUnguardedFloor` constant value in `cmd/eval_gates.go`; confirm with a command comparing the two.
    - The slash-command counts written into CLAUDE.md equal the real directory counts: `ls .claude/commands/ant/ | wc -l` and `ls .opencode/commands/ant/ | wc -l`.
    - No claim added to CLAUDE.md cites a test that does not exist — that is exactly what `TestCLAUDEMDLearningGovernorClaimsCiteLiveTests` enforces and it must pass.
  </acceptance_criteria>
  <done>This project's own requirements register, defect register and shipped documents say what is true of the running program, with every remaining limit named rather than hidden — the exact discipline its Definition of Done demands and that this phase's verification found missing.</done>
  <reversibility rating="reversible">Documentation and register edits, all revertable in one commit.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automatic trigger → the owner's git repository | The program creates a branch and a commit in a real working tree |
| guidance record → validated learning | A record decides whether a guess is shown to workers as proven |
| register documents → future decisions | A ticked box and a closed defect row are what the next phase trusts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-16-01 | Elevation of Privilege | the automatic source-proposal trigger | critical | mitigate | The new entry point is added to `sourceProposalReachabilityEntryPoints` in the same change, so `TestSourceProposalCannotMergePublishOrDeploy` walks from it and proves no path reaches a merge, push, publish or deploy |
| T-204-16-02 | Tampering | automatic branch creation in the owner's repo | high | mitigate | Refused by name on a dirty tree; exactly one new file; checked-out branch and `git status --porcelain` asserted identical before and after |
| T-204-16-03 | Spoofing | a worker claiming its guidance helped | high | mitigate | Promotion requires a corroborated `helpful` application record, never a claim record; `TestUncorroboratedClaimIsNeverValidated` and the existing `TestCorroborationNeverReadsTheWorkersOwnText` both assert it |
| T-204-16-04 | Repudiation | a requirement ticked without proof | critical | mitigate | Each tick must name its passing test and cite the recorded FAILS-WHEN-UNWIRED mutation; a partly-true requirement is annotated, not ticked |
| T-204-16-05 | Denial of Service | proposal or validation failure aborting a check | medium | mitigate | Both new steps follow the existing non-blocking phase-end contract; `TestImprovementPassFailureNeverBlocksTheCheck` stays green |
| T-204-16-06 | Information Disclosure | proposal file content on a branch | low | accept | The file carries episode identifiers and a plain-English case, both already local to the colony's own repository |

No package-manager installs occur in this plan; no npm/pip/cargo dependency is added, so no package-legitimacy checkpoint applies.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

**New files**
- `cmd/learning_validator.go`
- `cmd/learning_validator_test.go`
- (at run time, per proposal) `.aether/proposals/<proposal-id>.md` on an isolated branch

**New Go symbols**
- `sourceProposalRepeatedInterventionThreshold` (named constant, value 3)
- the automatic proposal trigger function called from `runAutomaticImprovementPass`
- `promoteHelpfulHypotheses(phaseID int) learningValidationSummary`
- `learningValidationSummary` (struct)
- `phaseEndConsolidationSummary` gains the validation counts

**Changed Go symbols**
- `runAutomaticImprovementPass` — gains the repeated-intervention proposal step
- `runPhaseEndConsolidation` — gains one call to `promoteHelpfulHypotheses`
- `renderImprovementPassBeat` — gains one plain-English line for a written proposal
- `sourceProposalReachabilityEntryPoints` — gains the new entry point, comment rewritten

**New test names**
- `TestRepeatedInterventionProposesExactlyOneSourceChange`
- `TestTwoOccurrencesProposeNothing`
- `TestThresholdIsPerCategoryNotATotal`
- `TestAutomaticProposalReplayCreatesNoSecondBranch`
- `TestDirtyTreeAutomaticProposalIsRefusedAndNeverBlocks`
- `TestHelpfulHypothesisIsPromotedAutomatically`
- `TestActedOnIsNotEnoughToValidate`
- `TestNeutralOrHarmfulIsNeverValidated`
- `TestUncorroboratedClaimIsNeverValidated`
- `TestHypothesisWithNoRecordStaysAHypothesis`
- `TestPromotionIsIdempotent`
- `TestDisprovenEntryIsNeverPromoted`
- `TestHypothesisPromoterIsReachedFromBothCheckLanes`

**Changed documents**
- `.planning/REQUIREMENTS.md`, `.planning/WINDOWS.md` (entries 38-45, table and JSON ledger), `CLAUDE.md`, `.claude/rules/aether-colony.md`
</artifacts_produced>

<flagged_assumptions>
## Flagged assumptions (edge probes surfaced, not resolved)

All five applicable SYNTH-06 probes arrived without a resolvable predicate for this closure's work and are surfaced rather than auto-resolved:

| Requirement | Probe | Disposition |
|-------------|-------|-------------|
| SYNTH-06 | adjacency — "When two things are exactly equal or just touch, do they merge, collide, or separate?" | Surfaced unresolved. SYNTH-06 is a completed documentation artifact (the mechanism study) that this closure does not modify; no gap-closure acceptance criterion can be defensibly written against it. |
| SYNTH-06 | empty — "What is the result for empty, single-element, or null input?" | Surfaced unresolved, same reason. |
| SYNTH-06 | ordering — "When elements compare equal, is output order specified and stable?" | Surfaced unresolved, same reason. |
| SYNTH-06 | idempotency — "What happens if this runs twice on the same input?" | Surfaced unresolved, same reason. |
| SYNTH-06 | concurrency — "If interrupted or run in parallel, what is guaranteed?" | Surfaced unresolved, same reason. |
</flagged_assumptions>

<verification>
- `go test ./cmd -run '^(TestRepeatedInterventionProposesExactlyOneSourceChange|TestTwoOccurrencesProposeNothing|TestThresholdIsPerCategoryNotATotal|TestAutomaticProposalReplayCreatesNoSecondBranch|TestDirtyTreeAutomaticProposalIsRefusedAndNeverBlocks|TestHelpfulHypothesisIsPromotedAutomatically|TestActedOnIsNotEnoughToValidate|TestNeutralOrHarmfulIsNeverValidated|TestUncorroboratedClaimIsNeverValidated|TestHypothesisWithNoRecordStaysAHypothesis|TestPromotionIsIdempotent|TestDisprovenEntryIsNeverPromoted|TestHypothesisPromoterIsReachedFromBothCheckLanes)$' -count=1 -timeout 20m` exits 0.
- Boundary guards still green: `go test ./cmd -run '^(TestSourceProposalCannotMergePublishOrDeploy|TestForbiddenOperationListIsReadFromTheSource|TestProposalCannotVerifyItself|TestUnverifiedProposalIsReportedNotReady|TestHypothesisIsNeverRenderedAsVerified|TestOneLearningStatusVocabulary|TestCorroborationNeverReadsTheWorkersOwnText|TestPromotionRequiresAHelpfulApplication)$' -count=1 -timeout 20m` exits 0.
- Document guards green: `go test ./cmd -run '^(TestCLAUDEMDLearningGovernorClaimsCiteLiveTests|TestCLAUDEMDLearningGovernorSectionIsPlainEnglish|TestCurrentVocabulary199|TestPlatformParityGolden)$' -count=1 -timeout 15m` exits 0.
- Both FAILS-WHEN-UNWIRED mutations (the proposal trigger and the hypothesis promoter) were performed, produced a non-zero exit, and were reverted; the observed failure text is quoted in the SUMMARY.
- Before writing Task 3, re-run every named wiring test from plans 204-12 through 204-15 and record the result. A requirement is ticked only against a test observed passing in this session.
- Do NOT claim the full suite is green: this repository carries a documented pre-existing red baseline. Any full-suite run carries `-timeout 90m` and the `discovered=N executed=N` headline is confirmed equal before any FAIL list is trusted.
</verification>

<success_criteria>
- SC5d is closed: `proposeSourceImprovement` has a real, evidence-gated production caller, and the cannot-merge/publish/deploy boundary now provably covers that caller too.
- WINDOWS entry 44 is closed: a hypothesis is promoted to validated automatically, and only on a corroborated helpful application.
- D-11 is implemented: REQUIREMENTS.md and WINDOWS.md entries 38-45 say what the tests prove; entries 40, 42 are narrowed with their remaining limits named and entry 43 stays open as deferred.
- D-01, D-04 and D-05 are each cited by at least one acceptance criterion above.
- No boundary was widened: the four forbidden operation families, the retained-authority refusals, and the corroboration rule are all still green.
</success_criteria>

<output>
Create `.planning/phases/204-learning-governor/204-16-SUMMARY.md` when done. It must list, requirement by requirement, the test that justifies its state; quote the WINDOWS.md consistency check command and its output; name the directory chosen for the proposal file and the evidence it is not a synced tree; and quote the observed non-zero failure output from both FAILS-WHEN-UNWIRED mutations.
</output>
