---
phase: 204-learning-governor
plan: 12
type: execute
wave: 1
depends_on: ["204-08", "204-09", "204-10"]
files_modified:
  - cmd/shadow_cmds.go
  - cmd/improvement_pass.go
  - cmd/improvement_cmds.go
  - cmd/consolidation_lifecycle.go
  - cmd/codex_visuals.go
  - cmd/command_guide.go
  - cmd/improvement_pass_test.go
  - cmd/shadow_grader_test.go
  - cmd/parity_test.go
  - cmd/testdata/parity_snapshot.json
  - cmd/testdata/command_catalog.json
  - cmd/testdata/orphan_allowlist.json
  - .aether/commands/improve.yaml
  - .claude/commands/ant/improve.md
  - .opencode/commands/ant/improve.md
autonomous: true
requirements: [LEARN-06, LEARN-07]
gap_closure: true

estimate:
  tokens: 132000
  raw_tokens: 132000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC4b: a beneficial candidate and a harmful/overfit candidate receive DIFFERENT verdicts through the production comparison path, because shadowEvaluator's run function is a real per-fixture classifier rather than an unconditional pass (D-08)."
    - "SC5b: a declared, beneficial, promotable candidate is driven automatically from comparison, through admitCandidateToCanary, into startCanary, and out through completeCanary or rollbackCanary, by the running program at the end of every check, on both check lanes (D-01, D-05)."
    - "The automatic pass is reached from both check lanes — runCodexContinue and runCodexContinueFinalize — through the single call site inside runPhaseEndConsolidation, proven by a call-graph reachability test in the TestPhaseApplicationCreditIsReachedFromBothCheckLanes shape (D-01)."
    - "The closing card a person reads after a check names, in plain English, when a proposal was tried, promoted onto a trial, undone, or when nothing happened — one line per event, nothing at all when the pass did nothing (D-03)."
    - "A comparison, admission refusal, canary start, completion or rollback that fails is logged and surfaced, and the check still reports success — never fatal, the same non-blocking contract hive promotion already follows (D-04)."
    - "A check on a colony with no declared candidate performs no comparison, opens no canary, writes no canary or shadow file, and adds no measurable work (D-04)."
    - "aether shadow-declare --help and aether shadow-compare --help both exit 0 against the real built binary; neither reports an unknown command (D-02)."
    - "aether improve with no flags renders the two-figure improvement report and mutates nothing on disk (D-02, and this project's inspection-never-mutates rule)."
    - "The /ant-improve wrapper exists on the Claude and OpenCode surfaces and the two copies are byte-identical (D-02)."
  prohibitions:
    - "The automatic pass must never admit or promote a candidate whose declared scope is outside the two canary-promotable scopes (project knowledge, routing); the nine retained-authority categories keep refusing structurally and the pass adds no waiver, actor, or bypass parameter of any kind."
    - "A failed, refused, or empty comparison, admission, canary, or rollback must never block, fail, or abort a check."
    - "The grader must never be selectable, alterable, or reachable by the candidate it grades; nothing added here may let a candidate name or choose its evaluator, its acceptance criteria, or the holdout set."
    - "This gap must never be reported closed on the evidence that a function exists — only a named test that fails when the production call site is removed closes it."
    - "aether improve in its default inspection form must never write to the store, the working tree, or any colony file."
  assumptions:
    - "Edge probe LEARN-06/unclassified is surfaced unresolved and NOT auto-resolved: the probe text is 'unclassified — review manually', which names no predicate this plan can defensibly convert into an acceptance criterion. Recorded here so it is visible, not dropped."
    - "Edge probe LEARN-07/unclassified is surfaced unresolved and NOT auto-resolved, for the same reason. Recorded here so it is visible, not dropped."
  artifacts:
    - "cmd/improvement_pass.go — runAutomaticImprovementPass and improvementPassSummary"
    - "cmd/improvement_cmds.go — the registered improve command and the rootCmd registration of shadowDeclareCmd and shadowCompareCmd"
    - ".aether/commands/improve.yaml, .claude/commands/ant/improve.md, .opencode/commands/ant/improve.md"
  key_links:
    - "cmd/consolidation_lifecycle.go (runPhaseEndConsolidation) -> cmd/improvement_pass.go (runAutomaticImprovementPass) — one call site, both check lanes reach it"
    - "cmd/improvement_pass.go -> cmd/shadow_cmds.go (runShadowCompare) -> cmd/promotion_gate.go (admitCandidateToCanary) -> cmd/rollback.go (startCanary/completeCanary/rollbackCanary)"
    - "cmd/consolidation_lifecycle.go (attachConsolidationSummary) -> cmd/codex_visuals.go (renderImprovementPassBeat) — the closing-card line on both lanes"
---

<objective>
Close verification gaps SC4b and SC5b by giving the already-built shadow comparison, promotion gate and canary lifecycle a real grader and a real production caller.

Today `shadowEvaluator()`'s run function returns a passing result for every subject and every task, so every real comparison ties and nothing is ever promotable; and `admitCandidateToCanary`, `startCanary`, `completeCanary` and `rollbackCanary` have zero non-test callers anywhere in the tree. Both mechanisms are correct and individually tested. Neither can happen in a real run.

Purpose: make the phase goal's second half ("independent shadow comparison, bounded promotion, and rollback") true of the running program, per owner decision D-01 (automatically at the end of every check, on both check lanes) and D-02 (plus one hand-run command to look).

Output: a real per-fixture classifier behind the proven `FrozenEvaluator.Run` seam; `runAutomaticImprovementPass`, called once from `runPhaseEndConsolidation` so both check lanes provably reach it; one plain-English closing-card line; and a registered `aether improve` surface with its `/ant-improve` wrapper triplet.

**Depends on executed plans:** 204-08 (`pkg/shadow`, `cmd/shadow_cmds.go`), 204-09 (`cmd/promotion_gate.go`, `cmd/rollback.go`), 204-10 (`cmd/improvement_report.go`). All are merged; this plan adds callers, it does not redesign them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/204-learning-governor/204-CONTEXT.md
@.planning/phases/204-learning-governor/204-VERIFICATION.md
@.planning/phases/204-learning-governor/204-08-SUMMARY.md
@.planning/phases/204-learning-governor/204-09-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A declared candidate travels comparison → gate → canary → closing card, automatically, on one check lane</name>
  <files>cmd/shadow_cmds.go, cmd/improvement_pass.go, cmd/consolidation_lifecycle.go, cmd/codex_visuals.go, cmd/improvement_pass_test.go, cmd/shadow_grader_test.go</files>
  <read_first>
    - cmd/shadow_cmds.go (whole file — `shadowEvaluator`, `shadowBaseline`, `shadowVisibleAndHoldoutTasks`, `runShadowCompare`, `shadowComparisonOutcome`, `loadShadowCandidate`, `shadowCandidateToDomain`, `shadowCandidateStorePath`)
    - pkg/shadow/evaluator.go and pkg/shadow/comparison.go (`FrozenEvaluator`, `NewFrozenEvaluator`, `Task`, `Result`, `NewResult`, `Compare`, `runScore`, `determineVerdict`, `Verdict` constants, `baselineAsCandidate`)
    - pkg/shadow/candidate.go (`Candidate`, its `ID()`/`Scope()`/`ExpectedBenefit()`/`Harms()`/`Expiry()` accessors, and `baselineAsCandidate`, whose id is the literal "baseline")
    - cmd/promotion_gate.go (`canaryScope`, `canaryScopeProjectKnowledge`, `canaryScopeRouting`, `canaryDeclaredScope`, `canaryAdmission`, `canaryBoundExceeded`, `canaryGateResolvesEvaluatorDigest`, `admitCandidateToCanary`)
    - cmd/rollback.go (`canaryRun`, `canaryRunStatus` constants, `canaryRunStorePath`, `loadCanaryRun`, `startCanary`, `completeCanary`, `rollbackCanary`, `canaryEpisodeID`)
    - cmd/fixture_conversion.go lines 220-300 (`regressionFixtureGuard`, `regressionFixture`, `regressionFixtureBank`, `loadFixtureBank`)
    - cmd/consolidation_lifecycle.go lines 130-340 (`phaseEndConsolidationSummary`, `LearningBeatLine`, `ZeroState`, `runPhaseEndConsolidation`, `attachConsolidationSummary`)
    - cmd/phase_end_hive.go (`promotePhaseEndInstinctsToHive`, `attachHivePromotionSummary`) — the non-blocking, summary-returning shape this pass mirrors
    - cmd/codex_visuals.go lines 270-300 and 2360-2500 (`voiceLine`, `renderStageMarker`, `casteIdentity`, `renderContinueVisual`, `renderLearningBeat`)
    - cmd/application_evidence.go lines 60-240 (`recordPhaseApplicationCredit` — the precedent for "exactly one call site inside runPhaseEndConsolidation, reached from both lanes")
  </read_first>
  <behavior>
    - Test 1 (grader): a candidate declaring a scope that names a subject an UNGUARDED fixture's own invariant protects outscores the baseline on the visible set and does not lose on the holdout set, producing `shadow.VerdictBeneficial`.
    - Test 2 (grader): a candidate whose declared harms name a subject a GUARDED fixture's invariant protects scores below the baseline on the visible set, producing `shadow.VerdictNotBeneficial`.
    - Test 3 (grader): a candidate that wins on the visible set and loses on the holdout set produces `shadow.VerdictOverfit` and is refused at the gate.
    - Test 4 (grader identity): `shadowEvaluator().Digest()` is byte-identical across two calls in the same process, and differs from the digest the previous always-pass definition produced.
    - Test 5 (tracer): a colony with one declared, unexpired candidate whose scope is exactly `project knowledge` runs `runPhaseEndConsolidation` once and ends with a canary run recorded at `canary/runs.json` in status running, its checkpoint saved, and an `improvementPassSummary` naming the candidate, the verdict and the action taken.
    - Test 6 (tracer): the same fixture's `result["improvement_pass"]` renders one plain-English closing-card line naming what happened, with no candidate identifier, no verdict token, and no raw key=value pair in the rendered text.
    - Test 7 (zero state): a colony with no declared candidate runs `runPhaseEndConsolidation` and creates no `canary/runs.json` and no `shadow/candidates.json`; the summary reports that nothing was proposed; the closing card shows no improvement line at all.
    - Test 8 (non-blocking): with the candidate store made unreadable, `runPhaseEndConsolidation` still returns `Ran: true` and the pass records its own failure reason instead of propagating an error.
  </behavior>
  <action>
Replace the placeholder grader and add the automatic pass, then wire one production call site.

**(a) Real per-fixture classifier behind `FrozenEvaluator.Run` (D-08).** In `cmd/shadow_cmds.go`, change `shadowEvaluator()`'s run function from the current unconditional passing result to a real classifier built over the committed fixture bank (`loadFixtureBank`). Contract the classifier must satisfy — all three properties, because a grader that only ever agrees with itself is the same stub under a new name:

  1. The task identifier is a fixture ID. Resolve the fixture from the bank by `regressionFixture.ID`. A task naming no fixture in the bank grades as not passing for every subject alike, so an unresolvable task can never advantage either side.
  2. The BASELINE subject (`shadow.Candidate` whose `ID()` is the literal "baseline") passes a fixture exactly when that fixture carries a non-nil `Guard` — the current behaviour is genuinely protected against that confirmed incident today, and genuinely unprotected where the guard is absent. This is real, committed data (7 of 47 guarded at this HEAD), never a typed-in constant.
  3. A NON-baseline subject passes a fixture when either (i) the fixture is guarded, or (ii) the subject's own declared scope and expected-benefit text names the subject matter the fixture's `Invariant` protects — that is, the candidate claims to address that incident. It FAILS a fixture, even a guarded one, when the subject's declared `Harms()` text names the subject matter that fixture's `Invariant` protects. Derive the "names the subject matter" match from the fixture's own `Invariant`/`Title` words at run time; do not hand-type a per-fixture table.

  Bump `shadowEvaluatorDefinitionPrefix` from its current `shadow-evaluator-v1` value to `shadow-evaluator-v2` in the same change, so the grader's identity changes with the grader. `canaryGateResolvesEvaluatorDigest()` already resolves `shadowEvaluator().Digest()`, so a comparison recorded under the old grader can never be admitted under the new one — that is the correct outcome, not a bug to route around. Do NOT fold the bank's content digest into the definition; the grader's identity tracks its own code version only, and folding the bank in is explicitly out of scope for this closure.

  `pkg/shadow` must not gain an import of, or a reference to, the fixture bank: the classifier is constructed in the command layer and handed in through the existing `NewFrozenEvaluator(def, run)` seam, exactly as today. `TestHoldoutResolutionHappensOnlyInTheCommandLayer` and `TestPackageImportsNothingFromCmd` must both still pass.

**(b) `cmd/improvement_pass.go` — the automatic pass (D-01, D-04).** Create `improvementPassSummary` and `runAutomaticImprovementPass(phaseID int) improvementPassSummary`. It never returns an error type a caller could propagate. Behaviour, in order:

  - Read the declared-candidate store at `shadowCandidateStorePath`. A missing or empty store returns a zero summary immediately, having done no further work: no comparison, no gate call, no canary file, no write of any kind. This is the "costs nothing measurable" path (Claude's Discretion, 204-CONTEXT.md).
  - For every declared candidate with no existing canary run (`loadCanaryRun` reports not found): call `runShadowCompare(candidateID)`, then `admitCandidateToCanary(candidate, outcome.Comparison)`. An admission refusal is recorded in the summary with the gate's own refusal sentence and the pass moves to the next candidate. An admitted candidate goes to `startCanary(admission, scopePaths)` where `scopePaths` is derived from the admitted scope only — `canaryScopeProjectKnowledge` and `canaryScopeRouting` and nothing else.
  - For every existing canary run in status running: if `canaryBoundExceeded(admission-equivalent bound, now)` is reached and this phase's own gates passed, call `completeCanary(run)`; if this phase's gates did not pass, call `rollbackCanary(run, reason)` with a reason naming the failing gate. Read the phase's gate outcome from what is already available at this boundary rather than re-running anything.
  - Every step is wrapped so a failure is captured into the summary (a reason string) and never returned as an error. Warn to stderr in the same shape `recordEpisodeLedgerClose` and `runPhaseEndConsolidation` already use.

  Add a named constant for any threshold this pass introduces; no inline numeric literal (this repository's established "named-constant threshold" pattern).

**(c) One production call site (D-01).** In `cmd/consolidation_lifecycle.go`, add an `ImprovementPass improvementPassSummary` field to `phaseEndConsolidationSummary`, and call `runAutomaticImprovementPass(phaseID)` at the end of `runPhaseEndConsolidation`, after `emitPhaseCompletionFeedback` and `emitMiddenThresholdRedirect`, assigning the result to that field before the summary is returned. This is deliberately ONE call site rather than a call added to each of `runCodexContinue` and `runCodexContinueFinalize`: `runPhaseEndConsolidation` is already reached from both check lanes and is the exact boundary D-01 names, and `attachConsolidationSummary` is already called on both lanes, so both the pass and its closing-card data reach both lanes structurally rather than by two edits that could drift.

  Extend `attachConsolidationSummary` to also set `result["improvement_pass"]` from `s.ImprovementPass`, using the same snake_case key discipline the existing `result["consolidation"]` map uses. Do not change any existing key.

**(d) The closing-card line (D-03).** In `cmd/codex_visuals.go`, add `renderImprovementPassBeat(raw interface{}) string` and call it from `renderContinueVisual` immediately after the existing `renderLearningBeat(result["consolidation"])` call. It renders one `voiceLine` per event that happened — a proposal was tried, a proposal was put on a trial, a trial was kept, a trial was undone — in ordinary English, and renders NOTHING AT ALL when the pass did nothing. It must read `result["improvement_pass"]` dual-typed (the in-process struct and the JSON-round-tripped map), following `renderContinueGateDetail`'s own dual-type precedent. Every word this repository invented must be glossed in the same sentence, and no candidate identifier, verdict token, scope constant, status constant or `key=value` pair may appear in the rendered text — `TestVoicedScreensCarryNoRawStateToken` and `TestVoicedScreensSpeakPlainEnglish` both cover this renderer once it is registered in the voice corpus.
  </action>
  <verify>
    <automated>go test ./cmd -run '^(TestShadowGraderDistinguishesBeneficialFromHarmful|TestOverfitCandidateIsRefusedAtTheGate|TestEvaluatorDigestIsStableAndChanged|TestAutomaticImprovementPassTracerEndToEnd|TestImprovementPassClosingLineIsPlainEnglish|TestCheckWithNoDeclaredCandidateCostsNothing|TestImprovementPassFailureNeverBlocksTheCheck)$' -count=1 -timeout 10m</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd -run '^TestAutomaticImprovementPassTracerEndToEnd$' -count=1 -timeout 10m` exits 0.
    - `go test ./cmd -run '^(TestShadowGraderDistinguishesBeneficialFromHarmful|TestOverfitCandidateIsRefusedAtTheGate|TestEvaluatorDigestIsStableAndChanged|TestImprovementPassClosingLineIsPlainEnglish|TestCheckWithNoDeclaredCandidateCostsNothing|TestImprovementPassFailureNeverBlocksTheCheck)$' -count=1 -timeout 10m` exits 0.
    - FAILS-WHEN-UNWIRED (perform this mutation, record the observed output in the SUMMARY, then revert): comment out the `runAutomaticImprovementPass(` call inside `runPhaseEndConsolidation` and confirm `go test ./cmd -run '^TestAutomaticImprovementPassTracerEndToEnd$' -count=1` exits NON-ZERO.
    - FAILS-WHEN-UNGRADED (perform this mutation, record the observed output, then revert): restore `shadowEvaluator`'s run function to an unconditional passing result and confirm `go test ./cmd -run '^TestShadowGraderDistinguishesBeneficialFromHarmful$' -count=1` exits NON-ZERO.
    - Source assertion: `grep -c 'runAutomaticImprovementPass(' cmd/consolidation_lifecycle.go` returns at least 1.
    - Source assertion: `grep -c 'func runAutomaticImprovementPass(' cmd/improvement_pass.go` returns exactly 1.
    - Source assertion: `grep -c 'renderImprovementPassBeat(' cmd/codex_visuals.go` returns at least 2 (the declaration and the call from `renderContinueVisual`).
    - Source assertion: `grep -c 'shadow-evaluator-v2' cmd/shadow_cmds.go` returns at least 1.
    - Isolation preserved: `go test ./pkg/shadow/... -count=1` exits 0, and `go test ./cmd -run '^(TestHoldoutResolutionHappensOnlyInTheCommandLayer|TestCandidateCannotAlterItsEvaluatorDigest)$' -count=1` exits 0.
    - Retained authority preserved: `go test ./cmd -run '^(TestOnlyTwoScopesAreCanaryPromotable|TestRetainedAuthorityCannotBecomeCanaryPromotable|TestEachRetainedScopeRefusalNamesItsAuthority|TestNeitherCoordinatorNorAutopilotCanWaiveARetainedRefusal)$' -count=1` exits 0.
    - `go build ./cmd/aether` exits 0, `go vet ./cmd/... ./pkg/...` exits 0, `gofmt -l` reports nothing for every file this task touched.
  </acceptance_criteria>
  <done>A declared, beneficial, promotable candidate is compared against a real grader, admitted by the gate, started as a canary, and reported on the closing card in one plain-English line — all driven by the running program at the end of a check, and proven to stop working when the production call is removed.</done>
  <reversibility rating="reversible">Adding a call site and replacing a placeholder grader; both are revertable in one commit with no durable data migration. The new grader definition version means comparisons recorded under the old version stop being admissible, which is the intended safety behaviour, not a loss.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove both check lanes reach the pass, and that it can never widen what may change</name>
  <files>cmd/improvement_pass_test.go</files>
  <read_first>
    - cmd/application_evidence_test.go lines 360-431 (`TestPhaseApplicationCreditIsReachedFromBothCheckLanes`, `applicationCreditUnreachedLanes`, and the `buildCmdFuncGraph`/`reachableFrom` helpers it uses) — copy this shape exactly, including the synthetic-unreachable-fixture subtest that proves the scanner can fail
    - cmd/codex_continue.go around line 1201 (the native check lane's `runPhaseEndConsolidation` call)
    - cmd/codex_continue_finalize.go around line 643 (the delegate check lane's `runPhaseEndConsolidation` call)
    - cmd/promotion_gate.go lines 31-180 (`canaryPromotableScopes`, `canaryRetainedAuthorityScopes`, `canaryRetainedAuthority`, `canaryRetainedAuthorityFor`)
    - cmd/improvement_pass.go (as written by Task 1)
  </read_first>
  <behavior>
    - Test 1: `runAutomaticImprovementPass` is transitively reachable from BOTH `runCodexContinue` and `runCodexContinueFinalize` in the real `cmd` call graph, and a synthetic graph in which neither lane reaches it is reported by name on both lanes.
    - Test 2: for every scope in `canaryRetainedAuthorityScopes`, a declared candidate carrying that scope is refused by the automatic pass, the refusal names the authority that retains it, and no canary run record is created.
    - Test 3: the automatic pass's own function signature carries no actor, caller-identity, coordinator, autopilot or waiver parameter, and its body contains no branch on any such value — a structural assertion in the shape of `TestNeitherCoordinatorNorAutopilotCanWaiveARetainedRefusal`.
    - Test 4: a candidate declaring a scope that is neither promotable nor retained is refused and creates nothing.
  </behavior>
  <action>
Add `TestAutomaticImprovementPassIsReachedFromBothCheckLanes` to `cmd/improvement_pass_test.go`, built on the existing `buildCmdFuncGraph` / `reachableFrom` helpers, with `target` set to `runAutomaticImprovementPass` and `lanes` set to `runCodexContinue` and `runCodexContinueFinalize`. Include the same anti-vacuity guards the precedent test carries: fail if zero top-level functions were indexed, fail if the target or either lane is not an indexed function (renamed or moved), and include the synthetic-graph subtest proving the scanner reports both lanes by name when neither reaches the target.

Add `TestAutomaticPassNeverPromotesOutsideTheTwoScopes`, table-driven over `canaryRetainedAuthorityScopeNames()` rather than a hand-typed list, so a tenth retained category added later is covered automatically. For each retained scope: declare a candidate with that scope, run the pass, assert no entry appears in `canary/runs.json`, and assert the summary's recorded refusal text contains the authority string `canaryRetainedAuthorityFor` returns for that scope.

Add `TestAutomaticPassCarriesNoBypassParameter` as a structural (AST or source-text) assertion over `cmd/improvement_pass.go`, in the shape the promotion gate's own structural test already uses: `runAutomaticImprovementPass` takes exactly one parameter, an int phase identifier, and the file contains no parameter or field named for an actor, a caller identity, a coordinator, autopilot, a waiver, or a bypass.

Add `TestUnrecognizedScopeIsRefusedAndWritesNothing` for the third branch (`canaryDeclaredScope` classifying to raw text that is neither promotable nor retained).
  </action>
  <verify>
    <automated>go test ./cmd -run '^(TestAutomaticImprovementPassIsReachedFromBothCheckLanes|TestAutomaticPassNeverPromotesOutsideTheTwoScopes|TestAutomaticPassCarriesNoBypassParameter|TestUnrecognizedScopeIsRefusedAndWritesNothing)$' -count=1 -timeout 10m</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd -run '^(TestAutomaticImprovementPassIsReachedFromBothCheckLanes|TestAutomaticPassNeverPromotesOutsideTheTwoScopes|TestAutomaticPassCarriesNoBypassParameter|TestUnrecognizedScopeIsRefusedAndWritesNothing)$' -count=1 -timeout 10m` exits 0.
    - FAILS-WHEN-UNWIRED (perform, record, revert): comment out the `runAutomaticImprovementPass(` call inside `runPhaseEndConsolidation` and confirm `TestAutomaticImprovementPassIsReachedFromBothCheckLanes` exits NON-ZERO and names BOTH `runCodexContinue` and `runCodexContinueFinalize` in its failure text.
    - Non-vacuity: the synthetic-graph subtest inside `TestAutomaticImprovementPassIsReachedFromBothCheckLanes` passes, proving the scanner is capable of reporting an unreachable target.
    - Both lanes are named literally in the test source: `grep -c 'runCodexContinueFinalize' cmd/improvement_pass_test.go` returns at least 1 and `grep -c '"runCodexContinue"' cmd/improvement_pass_test.go` returns at least 1.
    - `TestAutomaticPassNeverPromotesOutsideTheTwoScopes` derives its cases from `canaryRetainedAuthorityScopeNames()` at run time, not from a literal slice: `grep -c 'canaryRetainedAuthorityScopeNames()' cmd/improvement_pass_test.go` returns at least 1.
  </acceptance_criteria>
  <done>Both check lanes are proven to reach the automatic pass by the same call-graph technique this project already trusts, and the pass is proven unable to promote anything outside the two permitted scopes.</done>
  <reversibility rating="reversible">Test-only additions.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The hand-run improvement surface — a registered command and its wrapper triplet</name>
  <files>cmd/improvement_cmds.go, cmd/command_guide.go, cmd/parity_test.go, cmd/testdata/parity_snapshot.json, cmd/testdata/command_catalog.json, cmd/testdata/orphan_allowlist.json, .aether/commands/improve.yaml, .claude/commands/ant/improve.md, .opencode/commands/ant/improve.md</files>
  <read_first>
    - cmd/shadow_cmds.go lines 393-466 (`shadowDeclareCmd`, `shadowCompareCmd`, their flag declarations, and the `init()` comment explaining why they were left unregistered)
    - cmd/improvement_report.go (`reportWindow`, `buildImprovementReport`, `renderImprovementReport`, `improvementReport`)
    - cmd/eval_gates.go (`loadEvalGateSentinels`, `evalGateSentinel`) and cmd/episode_ledger.go (`readEpisodeLedger`)
    - cmd/alias_cmds.go (the minimal `init()` + `rootCmd.AddCommand` registration shape)
    - cmd/command_guide.go lines 120-180 (`commandGuideCatalog`, `commandGuideDefinition`, `commandGuideLiteralCommands`)
    - cmd/parity_test.go lines 1-140 and 255-300 (`parityGolden` struct, `yamlToRuntimeName`, `extractYAMLNames`, `extractWrapperNames`, `extractCommandGuideNames`, `TestPlatformParityGolden`)
    - cmd/subcommand_reachability_ratchet_test.go lines 40-100 and 1411-1530 (`callerWrapperCorpora`, `collectCallerEvidence`, `TestNoRegisteredSubcommandIsUnreferenced`, `loadOrphanAllowlist`)
    - .aether/commands/pheromones.yaml and .claude/commands/ant/pheromones.md and .opencode/commands/ant/pheromones.md — the exact three-file shape and the byte-identical wrapper rule
  </read_first>
  <action>
Give the improvement machinery the one hand-run surface D-02 requires, and register the two commands WINDOWS entry 39 left inert.

**(a) `cmd/improvement_cmds.go` (new).** Declare `improveCmd` with `Use: "improve"`, a short description in plain English ("Look at how the colony's own suggestions are doing, or try one by hand"), and `Args: cobra.NoArgs`. Its default form (no flags) is an INSPECTION and must mutate nothing: read the episode ledger with `readEpisodeLedger`, the sentinels with `loadEvalGateSentinels`, build an unbounded `reportWindow`, and emit `renderImprovementReport(buildImprovementReport(...))` through the project's existing output path. Add a `--declare` form routing to `declareShadowCandidate` with the same six flags `shadowDeclareCmd` already declares, and a `--compare <candidate-id>` form routing to `runShadowCompare`. In the file's `init()`, call `rootCmd.AddCommand` for `improveCmd`, for `shadowDeclareCmd`, and for `shadowCompareCmd` — the three registrations that close WINDOWS entry 39. Do not edit `cmd/shadow_cmds.go`'s own `init()`; leave its flag declarations exactly where they are, and update the stale comment block above that `init()` only if `gofmt`/`go vet` require it.

**(b) Command guide.** Add an `improve` entry to `cmd/command_guide.go` so `commandGuideCatalog()` covers it, matching the category and intent shape of the entries already there.

**(c) Wrapper triplet (D-02).** Create `.aether/commands/improve.yaml` following `.aether/commands/pheromones.yaml`'s exact key shape (`name: ant-improve`, `description`, `source_of_truth`, `runtime.command`, `wrapper_additions.routing`, `guardrails`). The routing block must name all three runtime commands literally — `aether improve`, `aether shadow-declare`, `aether shadow-compare` — because `collectCallerEvidence` reads `.aether/commands` and that is what credits each registered command with a real caller. Then create `.claude/commands/ant/improve.md` and `.opencode/commands/ant/improve.md` as BYTE-IDENTICAL copies of each other, following the `pheromones.md` shape: the `<!-- Aether-managed: runtime spec at .aether/commands/improve.yaml. Synced by aether update. -->` first line, then frontmatter with `name: ant-improve` and the same description, then the routing body. Every repo-invented word in the wrapper text is glossed inline in plain English.

**(d) Golden fixtures.** Re-run the parity and reachability gates and update the committed fixtures with the scanner's own output rather than hand-typed values: add `improve` to the `yaml_catalog`, `claude_wrapper_catalog`, `opencode_wrapper_catalog`, `command_guide_catalog`, `runtime_catalog_names` and `runtime_resolved_names` entries of `cmd/testdata/parity_snapshot.json` as the test's own failure output dictates, and add the three new runtime command names to `cmd/testdata/command_catalog.json`. `cmd/testdata/orphan_allowlist.json` MAY ONLY SHRINK: the three newly registered commands must NOT be added to it — they have caller evidence from the YAML routing block. If the reachability gate still reports one of them as an orphan, the routing text is wrong; fix the routing text.
  </action>
  <verify>
    <automated>go test ./cmd -run '^(TestPlatformParityGolden|TestNoRegisteredSubcommandIsUnreferenced|TestCodexCoverageByDesign|TestCLIFlagAudit)$' -count=1 -timeout 15m</automated>
  </verify>
  <acceptance_criteria>
    - Behaviour against the real binary: `go run ./cmd/aether shadow-declare --help` exits 0; `go run ./cmd/aether shadow-compare --help` exits 0; `go run ./cmd/aether improve --help` exits 0. (Before this task all three exited non-zero; the verification report reproduced that live for the first.)
    - Inspection never mutates: capture a checksum of the colony data directory before and after `go run ./cmd/aether improve` in a scratch colony; the two checksums are identical.
    - `go test ./cmd -run '^(TestPlatformParityGolden|TestNoRegisteredSubcommandIsUnreferenced|TestCodexCoverageByDesign|TestCLIFlagAudit)$' -count=1 -timeout 15m` exits 0.
    - Allowlist ratchet: `python3 -c "import json;print(len(json.load(open('cmd/testdata/orphan_allowlist.json'))))"` reports a count less than or equal to the count recorded before this task, AND a structural check over the parsed JSON confirms none of the three newly registered command names appears among its entry names (parse the JSON and compare entry-by-entry; do not text-grep the file).
    - Wrapper parity: `diff .claude/commands/ant/improve.md .opencode/commands/ant/improve.md` produces no output.
    - FAILS-WHEN-UNREGISTERED (perform, record, revert): comment out the `rootCmd.AddCommand` line for `shadowDeclareCmd` in `cmd/improvement_cmds.go` and confirm `go run ./cmd/aether shadow-declare --help` exits NON-ZERO.
    - `go build ./cmd/aether` exits 0 and `go vet ./cmd/...` exits 0.
  </acceptance_criteria>
  <done>A person can type one menu command to look at how the colony's own suggestions are doing or to try one by hand, the two commands WINDOWS entry 39 recorded as inert are registered and reachable in the shipped binary, and the orphan ratchet did not widen.</done>
  <reversibility rating="costly">Registering runtime commands and shipping a new `/ant-improve` menu wrapper is a published surface: `aether publish` distributes the wrapper triplet to every repo on the machine, so withdrawing it later means another publish and breaking anyone who has started using it. Reversible, but not free. The command name and its three runtime forms are therefore chosen once, here, and not renamed in a later plan of this closure.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declared candidate → grader | A candidate is untrusted declared text; it must never influence which grader runs |
| declared candidate → promotion gate | An untrusted scope string decides whether anything is promoted |
| automatic pass → working tree | `startCanary` saves a restore point and `rollbackCanary` restores it — real file mutation on a person's repo |
| CLI/wrapper text → runtime | Wrapper markdown is a prompt an assistant executes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-12-01 | Tampering | `shadowEvaluator` classifier | high | mitigate | The classifier is constructed in the command layer and handed through `NewFrozenEvaluator`; `pkg/shadow` gains no reference to the bank; `TestCandidateCannotAlterItsEvaluatorDigest` and `TestHoldoutResolutionHappensOnlyInTheCommandLayer` remain green (Task 1 acceptance criteria) |
| T-204-12-02 | Elevation of Privilege | `runAutomaticImprovementPass` | critical | mitigate | The pass carries no actor/waiver/bypass parameter and is proven structurally unable to branch on one (`TestAutomaticPassCarriesNoBypassParameter`); every retained-authority scope is refused (`TestAutomaticPassNeverPromotesOutsideTheTwoScopes`) |
| T-204-12-03 | Denial of Service | automatic pass on every check | medium | mitigate | A colony with no declared candidate does no work at all (`TestCheckWithNoDeclaredCandidateCostsNothing`); every failure is non-fatal (`TestImprovementPassFailureNeverBlocksTheCheck`) |
| T-204-12-04 | Tampering | `startCanary` file mutation | high | mitigate | `startCanary` refuses to proceed without a saved restore point (existing, tested); scope paths are derived from the admitted scope only |
| T-204-12-05 | Information Disclosure | closing-card line | low | mitigate | No candidate identifier, verdict token or raw state token is rendered (`TestVoicedScreensCarryNoRawStateToken`) |
| T-204-12-06 | Repudiation | which grader produced a verdict | medium | mitigate | The grader definition is versioned (`shadow-evaluator-v2`) and the gate refuses a comparison graded by a different evaluator digest (existing, tested) |

No package-manager installs occur in this plan; no npm/pip/cargo dependency is added, so no package-legitimacy checkpoint applies.
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

**New files**
- `cmd/improvement_pass.go`
- `cmd/improvement_cmds.go`
- `cmd/improvement_pass_test.go`
- `cmd/shadow_grader_test.go`
- `.aether/commands/improve.yaml`
- `.claude/commands/ant/improve.md`
- `.opencode/commands/ant/improve.md`

**New Go symbols**
- `improvementPassSummary` (struct)
- `runAutomaticImprovementPass(phaseID int) improvementPassSummary`
- `renderImprovementPassBeat(raw interface{}) string`
- `improveCmd` (cobra command, `Use: "improve"`)
- `phaseEndConsolidationSummary.ImprovementPass` (new field)
- a named constant for any threshold the pass introduces

**Changed Go symbols**
- `shadowEvaluator()` — run function replaced by a real per-fixture classifier
- `shadowEvaluatorDefinitionPrefix` — `shadow-evaluator-v1` → `shadow-evaluator-v2`
- `attachConsolidationSummary` — additionally sets `result["improvement_pass"]`
- `runPhaseEndConsolidation` — gains one call to `runAutomaticImprovementPass`
- `renderContinueVisual` — gains one call to `renderImprovementPassBeat`
- `commandGuideCatalog()` — gains an `improve` entry

**New CLI surface**
- `aether improve` (default: inspection report; `--declare` flags; `--compare <candidate-id>`)
- `aether shadow-declare` (registered; previously constructed but unreachable)
- `aether shadow-compare` (registered; previously constructed but unreachable)
- `/ant-improve` on Claude and OpenCode

**New test names**
- `TestShadowGraderDistinguishesBeneficialFromHarmful`
- `TestOverfitCandidateIsRefusedAtTheGate`
- `TestEvaluatorDigestIsStableAndChanged`
- `TestAutomaticImprovementPassTracerEndToEnd`
- `TestImprovementPassClosingLineIsPlainEnglish`
- `TestCheckWithNoDeclaredCandidateCostsNothing`
- `TestImprovementPassFailureNeverBlocksTheCheck`
- `TestAutomaticImprovementPassIsReachedFromBothCheckLanes`
- `TestAutomaticPassNeverPromotesOutsideTheTwoScopes`
- `TestAutomaticPassCarriesNoBypassParameter`
- `TestUnrecognizedScopeIsRefusedAndWritesNothing`

**Changed data files**
- `cmd/testdata/parity_snapshot.json`, `cmd/testdata/command_catalog.json`, `cmd/testdata/orphan_allowlist.json` (may only shrink)
</artifacts_produced>

<flagged_assumptions>
## Flagged assumptions (edge probes surfaced, not resolved)

Two applicable edge probes for this plan's requirements arrived `unclassified` and are surfaced rather than auto-resolved, per the spec-less fallback's no-silent-drop rule:

| Requirement | Probe | Disposition |
|-------------|-------|-------------|
| LEARN-06 | "unclassified — review manually" | Surfaced unresolved. The probe names no predicate; converting it into an acceptance criterion would mean inventing one. |
| LEARN-07 | "unclassified — review manually" | Surfaced unresolved, same reason. |
</flagged_assumptions>

<verification>
- `go test ./cmd -run '^(TestShadowGraderDistinguishesBeneficialFromHarmful|TestOverfitCandidateIsRefusedAtTheGate|TestEvaluatorDigestIsStableAndChanged|TestAutomaticImprovementPassTracerEndToEnd|TestImprovementPassClosingLineIsPlainEnglish|TestCheckWithNoDeclaredCandidateCostsNothing|TestImprovementPassFailureNeverBlocksTheCheck|TestAutomaticImprovementPassIsReachedFromBothCheckLanes|TestAutomaticPassNeverPromotesOutsideTheTwoScopes|TestAutomaticPassCarriesNoBypassParameter|TestUnrecognizedScopeIsRefusedAndWritesNothing)$' -count=1 -timeout 15m` exits 0.
- `go test ./pkg/shadow/... -count=1` exits 0 (isolation suite unbroken).
- `go test ./cmd -run '^(TestPlatformParityGolden|TestNoRegisteredSubcommandIsUnreferenced|TestVoicedScreensSpeakPlainEnglish|TestVoicedScreensCarryNoRawStateToken|TestOnlyTwoScopesAreCanaryPromotable|TestRetainedAuthorityCannotBecomeCanaryPromotable)$' -count=1 -timeout 15m` exits 0.
- `go run ./cmd/aether shadow-declare --help`, `go run ./cmd/aether shadow-compare --help` and `go run ./cmd/aether improve --help` each exit 0.
- Both FAILS-WHEN-UNWIRED mutations in Tasks 1 and 2 were performed, produced a non-zero exit, and were reverted; the observed failure text is quoted in the SUMMARY.
- Do NOT claim the full suite is green: this repository carries a documented pre-existing red baseline. If a full-suite run is done at all it must carry `-timeout 90m` and the `discovered=N executed=N` headline must be confirmed equal before any FAIL list is trusted.
</verification>

<success_criteria>
- SC4b is closed: a beneficial candidate and a harmful/overfit candidate reach different verdicts through the production comparison path, and the difference disappears when the classifier is reverted.
- SC5b is closed: `admitCandidateToCanary`, `startCanary`, `completeCanary` and `rollbackCanary` each have a real production caller reachable from both check lanes, proven by a call-graph test that fails when the call site is removed.
- D-01, D-02, D-03, D-04, D-05 and D-08 are each implemented and each cited by at least one acceptance criterion above.
- The orphan allowlist did not widen; the retained-authority boundary and the `pkg/shadow` isolation suite are both still green.
</success_criteria>

<output>
Create `.planning/phases/204-learning-governor/204-12-SUMMARY.md` when done. It must quote the observed non-zero failure output from both FAILS-WHEN-UNWIRED mutations, name the exact classifier rule implemented, and record the before/after orphan-allowlist counts.
</output>
