---
phase: 112-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/ts-host/package.json
  - .aether/ts-host/src/types.ts
  - .aether/config/ceremony.yaml
autonomous: true
requirements:
  - TS-05
  - CER-02
must_haves:
  truths:
    - "Node engine is >=20 and all new dependencies install without errors"
    - "TypeScript types for CeremonyPayload and CeremonyEvent exist in types.ts"
    - "Shared YAML ceremony config exists at .aether/config/ceremony.yaml with all 27 castes"
  artifacts:
    - path: ".aether/ts-host/package.json"
      provides: "Node >=20 engine and runtime dependencies"
      contains: "engines.node: \">=20\""
    - path: ".aether/ts-host/src/types.ts"
      provides: "CeremonyPayload and CeremonyEvent TypeScript interfaces"
      contains: "export interface CeremonyPayload"
    - path: ".aether/config/ceremony.yaml"
      provides: "Shared caste emoji, color, label maps"
      contains: "castes:"
  key_links:
    - from: ".aether/ts-host/src/types.ts"
      to: "pkg/events/ceremony.go"
      via: "TypeScript interfaces mirror Go CeremonyPayload struct fields"
    - from: ".aether/config/ceremony.yaml"
      to: "cmd/codex_visuals.go"
      via: "YAML caste maps match Go hardcoded defaults"
---

<objective>
Prepare the infrastructure for the event bridge and ceremony system.

Purpose: Phase 112 needs Node >=20 for chokidar/log-update compatibility, TypeScript types that mirror Go ceremony events, and a shared YAML config that both Go and TS can consume to prevent platform drift.
Output: Updated package.json with new deps, extended types.ts with ceremony interfaces, and ceremony.yaml with all 27 castes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-foundation/112-CONTEXT.md
@.planning/phases/112-foundation/112-RESEARCH.md
@.planning/phases/112-foundation/112-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

@pkg/events/ceremony.go
@pkg/events/event.go
@cmd/codex_visuals.go
@.aether/ts-host/src/types.ts
@.aether/ts-host/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump Node engine and add dependencies</name>
  <files>.aether/ts-host/package.json</files>
  <read_first>
    - `.aether/ts-host/package.json` — current deps, engine, and scripts
    - `.planning/phases/112-foundation/112-RESEARCH.md` — recommended versions and dependency list
  </read_first>
  <action>
    Modify `.aether/ts-host/package.json`:
    1. Change `engines.node` from `">=18"` to `">=20"`.
    2. Add runtime dependencies (dependencies block) with exact versions: chalk@5.6.2, boxen@8.0.1, figlet@1.11.0, ora@9.4.0, cli-progress@3.12.0, log-update@8.0.0, chokidar@5.0.0, js-yaml@4.1.0, strip-ansi@7.2.0.
    3. Add dev dependencies: @types/figlet@1.7.0, @types/cli-progress@3.11.6.
    4. Update `@types/node` from `18.19.130` to `^20.17.0`.
    5. Run `cd .aether/ts-host && npm install` to regenerate package-lock.json.
    6. If `npm install` fails for `log-update@8.0.0` on Node 20, downgrade `log-update` to `5.0.1` and retry.
  </action>
  <verify>
    <automated>cd .aether/ts-host && npm install && npm run typecheck</automated>
  </verify>
  <done>
    - `engines.node` is `">=20"`
    - `npm install` exits 0 with no unresolvable peer dependency errors
    - `npm ls chalk` shows version 5.6.2
    - `npm ls js-yaml` shows version 4.1.0
    - `npm run typecheck` passes with zero errors
  </done>
  <acceptance_criteria>
    - `package.json` contains `"engines": { "node": ">=20" }`
    - `package.json` `dependencies` includes `chalk`, `boxen`, `figlet`, `ora`, `cli-progress`, `log-update`, `chokidar`, `js-yaml`, `strip-ansi`
    - `package.json` `devDependencies` includes `@types/figlet`, `@types/cli-progress`, `@types/node` at `^20.17.0`
    - `npm install` in `.aether/ts-host/` exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add ceremony event types to types.ts</name>
  <files>.aether/ts-host/src/types.ts</files>
  <read_first>
    - `.aether/ts-host/src/types.ts` — existing type definitions
    - `pkg/events/ceremony.go` — Go CeremonyPayload struct and ceremony topic constants
    - `pkg/events/event.go` — Go Event struct
  </read_first>
  <action>
    Extend `.aether/ts-host/src/types.ts` with two new interface blocks appended after the existing content:
    1. `CeremonyPayload` interface with all optional fields matching the Go struct from `pkg/events/ceremony.go`: phase (number), phase_name (string), wave (number), spawn_id (string), caste (string), name (string), task_id (string), task (string), status (string), message (string), skill (string), pheromone_type (string), strength (number), completed (number), total (number), tool_count (number), token_count (number), files_created (string[]), files_modified (string[]), tests_written (string[]), blockers (string[]), success_criteria (string[]), loop_type (string), detection_signal (string), action_taken (string).
    2. `CeremonyEvent` interface extending the Go Event shape: id (string), topic (string), payload (CeremonyPayload), source (string), timestamp (string), ttl_days (number), expires_at (string).
    3. Export a `CEREMONY_TOPICS` array constant containing all 24 ceremony topic strings from `pkg/events/ceremony.go` (e.g., "ceremony.build.prewave", "ceremony.build.wave.start", etc.).
    4. Add a `CeremonyTopic` union type of all literal topic strings.
    Do NOT modify existing types. Keep exactOptionalPropertyTypes compatibility (omit undefined fields, do not use `?` on required fields).
  </action>
  <verify>
    <automated>cd .aether/ts-host && npm run typecheck</automated>
  </verify>
  <done>
    - `CeremonyPayload` interface exists with all fields from Go struct
    - `CeremonyEvent` interface exists with correct payload typing
    - `CEREMONY_TOPICS` constant exported with all 24 topics
    - TypeScript compilation passes
  </done>
  <acceptance_criteria>
    - `grep -c "export interface CeremonyPayload" .aether/ts-host/src/types.ts` returns 1
    - `grep -c "export interface CeremonyEvent" .aether/ts-host/src/types.ts` returns 1
    - `grep -c "ceremony.build.prewave" .aether/ts-host/src/types.ts` returns at least 1
    - `npm run typecheck` in `.aether/ts-host/` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create shared YAML ceremony config</name>
  <files>.aether/config/ceremony.yaml</files>
  <read_first>
    - `cmd/codex_visuals.go` — casteColorMap, casteEmojiMap, casteLabelMap (exact values)
    - `.planning/phases/112-foundation/112-CONTEXT.md` — D-03, D-04 config requirements
    - `.planning/phases/112-foundation/112-RESEARCH.md` — proposed YAML structure
  </read_first>
  <action>
    Create `.aether/config/ceremony.yaml` with the following top-level keys:
    1. `castes:` — map of all 27 castes from `cmd/codex_visuals.go`. For each caste key, provide `emoji`, `color`, and `label`. Use the exact emoji and label values from Go. For `color`, convert Go ANSI codes to hex: 33 -> "#FFD700" (yellow), 36 -> "#00FFFF" (cyan), 32 -> "#00FF00" (green), 31 -> "#FF0000" (red), 35 -> "#FF00FF" (magenta), 34 -> "#0000FF" (blue), 37 -> "#FFFFFF" (white), 95 -> "#FF00FF" (bright magenta), 93 -> "#FFFF00" (bright yellow), 92 -> "#00FF00" (bright green), 91 -> "#FF0000" (bright red), 94 -> "#00FFFF" (bright cyan), 96 -> "#00FFFF" (bright cyan variant), 90 -> "#808080" (gray). Match each caste to its Go color code exactly.
    2. `stage_separator:` — with `prefix: "── "` and `suffix: " ──"`.
    3. `naming:` — with `deterministic_prefixes:` containing at least 3 name pools per caste (e.g., builder: ["Mason", "Carpenter", "Smith"]). Use thematically appropriate names for each caste.
    4. `banners:` — with `build_start` (figlet_font: "Standard", text: "BUILD") and `seal_complete` (figlet_font: "Standard", text: "CROWNED ANTHILL").
    5. `excavation_phrases:` — list of at least 5 strings (e.g., "Excavating chamber...", "Laying foundation...", "Expanding tunnel...", "Carrying substrate...", "Sealing passage...").
    Ensure the file is valid YAML. No trailing spaces. Use 2-space indentation.
  </action>
  <verify>
    <automated>cd .aether/ts-host && npx tsx -e "import yaml from 'js-yaml'; import fs from 'node:fs'; const doc = yaml.load(fs.readFileSync('../config/ceremony.yaml', 'utf-8')); console.log(Object.keys(doc.castes).length);"</automated>
  </verify>
  <done>
    - `.aether/config/ceremony.yaml` exists and is valid YAML
    - Contains all 27 castes with emoji, color, and label
    - Contains stage_separator, naming, banners, and excavation_phrases sections
    - js-yaml can parse it without errors
  </done>
  <acceptance_criteria>
    - `.aether/config/ceremony.yaml` exists
    - `js-yaml` parses the file successfully (test via `npx tsx` one-liner)
    - The parsed object has `castes` with exactly 27 keys
    - Every caste entry has `emoji`, `color`, and `label` strings
    - `stage_separator.prefix` equals "── "
    - `banners.build_start.text` equals "BUILD"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS host -> npm registry | Untrusted external packages introduced into build |
| YAML config -> TS host | Config file parsed at runtime; malformed input could crash host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | package.json deps | mitigate | Pin exact versions, verify with npm install + typecheck |
| T-112-02 | Denial of Service | ceremony.yaml parser | mitigate | Use js-yaml safeLoad, validate minimum keys after parse |
| T-112-03 | Information Disclosure | npm install network | accept | Standard npm registry TLS, no secrets in package.json |
</threat_model>

<verification>
- `npm run typecheck` passes after all changes
- `npm test` (existing tests) still passes — no regression
- `js-yaml` can parse `.aether/config/ceremony.yaml`
- All 27 castes present in YAML with emoji/color/label
</verification>

<success_criteria>
- Node engine bumped to >=20, all new dependencies install cleanly
- TypeScript ceremony types mirror Go CeremonyPayload exactly
- Shared YAML config exists with all 27 castes and required sections
- No regression in existing TS host tests or typecheck
</success_criteria>

<output>
After completion, create `.planning/phases/112-foundation/112-01-SUMMARY.md`
</output>
