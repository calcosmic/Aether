---
phase: 204.2-codex-native-worker-lifecycle
plan: "07"
type: execute
wave: 5
depends_on: ["204.2-04", "204.2-05", "204.2-06"]
files_modified:
  - cmd/codex_native_worker_live_test.go
  - cmd/codex_native_worker_test.go
  - cmd/codex_native_worker.go
  - pkg/codex/platform_contract.go
  - pkg/codex/platform_contract_test.go
  - cmd/wrapper_usage_resolve_test.go
  - cmd/command_guide.go
  - .aether/skills/colony/aether-colony-build-cycle/SKILL.md
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-capabilities.json
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-claude-comparison.json
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-qualification.json
autonomous: true
requirements: [ANT-04, ANT-05, ANT-06, ANT-07]
must_haves:
  truths:
    - "The ordinary installed native build uses one real Builder plus actual checks, while explicitly requested independent review uses a real named Watcher with returned findings."
    - "Actual host captures prove which permission, cancellation, workspace and nesting controls exist; unsupported requested controls are refused."
    - "One completed helper and one unfinished job survive a stopped parent and fresh-session public resume with no repeated writes or credit."
    - "A native question receives a scope-bound fixture-authorized answer and an actual message delivery to the intended worker."
    - "Absent per-child provider usage/cost remains unreported, with no guessed zero or attribution from parent aggregate usage."
    - "The final candidate exposes native launch/answer/recovery guidance only on Codex and preserves Claude/OpenCode wrapper launch and completion/finalizer routes."
  artifacts:
    - path: cmd/codex_native_worker_live_test.go
      provides: "Actual fresh-host native lifecycle runner and evidence validator"
      contains: "TestCodexNativeWorkerFreshHost"
    - path: pkg/codex/platform_contract.go
      provides: "Evidence-qualified native capabilities distinct from subprocess capabilities"
      contains: "Native"
    - path: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-qualification.json
      provides: "Final source-pinned native behavior evidence"
  key_links:
    - from: cmd/codex_native_worker_live_test.go
      to: .aether/skills/colony/aether-colony-build-cycle/SKILL.md
      via: "Production-installed ant-build and actual native host tool events"
      pattern: "ant-build"
    - from: cmd/codex_native_worker.go
      to: pkg/codex/platform_contract.go
      via: "Native requested-capability admission distinct from subprocess selection"
      pattern: "Native"
---

<objective>
Prove the actual supported native lifecycle and describe its limits accurately. In plain English, demonstrate real helpers doing checked work and surviving a stopped chat, and report what the host cannot do.

Purpose: Establish all five success criteria with actual host evidence, per D-01/D-04/D-05/D-08/D-09/D-10.
Output: Final native scenario receipts, semantic Claude comparison, honest capability claims and absent-cost behavior.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/research/ant-codex-parity-2026-09-16/GSD-INTEGRATION.md
</context>

<interfaces>
Existing cmd/codex_skill_live_test.go has a narrow discovery-only validator; do not widen its receipt meaning. NEW codexNativeLiveReceipt/validateCodexNativeLiveEvidence describe native lifecycle evidence and its negative controls. Actual spawn_agent tool event is the native launch fact; the outer codex exec/resume process only hosts the parent session and is not a Go worker subprocess.
</interfaces>

## Artifacts this phase produces

NEW: native live test file, dedicated native receipt schema/validator, capability and semantic comparison receipts, final qualification receipt; evidence-backed native capability fields.
EXISTING: subprocess platform contract, provider invoker, nine-skill distribution and wrapper usage resolver retain their distinct claims and source-qualified measurements. Extend codexNativeBuildCommandGuide and TestCodexNativeBuildGuidePlatformIsolation from Plan 01; retain the three-platform TestCodexNativeDecisionGuideContract from Plan 04.

## Bounded execution scope

The eleven listed files comprise eight authored code/test/guidance files and three generated JSON receipts. Task 1 extracts and extends the existing runner in two authored files and generates two receipts; Task 2 changes four authored files; Task 3 aligns two guidance files, extends the already-owned deterministic test file and generates the final receipt. The deterministic test file overlaps Tasks 1/3 within this sequential plan, so the unique authored count stays eight. Estimated context consumption is 20% for runner/scenario extension, 15% for capability/usage admission and 10% for guidance/final qualification, approximately 45% overall. Live runtime is not additional implementation scope: use one table-driven scenario matrix, shared fixture/host-launch/capture/receipt helpers and existing bridge operations, with case-specific stimuli/assertions only. Retain every listed scenario and the Claude comparison; add no separate runner, event framework or subsystem per scenario. Keep raw event streams on the durable evidence root and consume bounded summaries during runs. Task 3 must rerun the complete matrix after the final production changes; the earlier receipts remain independently pinned. If measured implementation would exceed this context bound, report the concrete excess for a plan split without dropping scenarios or the final rerun.

<tasks>
<task type="auto">
  <name>Task 1: Exercise the real native lifecycle and host control boundaries</name>
  <files>cmd/codex_native_worker_live_test.go, cmd/codex_native_worker_test.go, .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-capabilities.json, .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-claude-comparison.json</files>
  <read_first>204.2-01-SUMMARY.md and raw host envelope; cmd/codex_skill_live_test.go isolation and evidence validator; Plans 02–06 summaries; host-capabilities.md and parity-evidence.md acceptance matrix; current installed Codex help/configuration and actual supported session-resume operation.</read_first>
  <action>Move the early host runner and receipt validation out of the deterministic test file into the dedicated native live file, preserving test names. Keep TestCodexNativeBuildGuidePlatformIsolation in the deterministic file. Use one table-driven scenario matrix with shared fixture, launch, capture and receipt helpers, following the bounded execution scope above. Use subprocess argv arrays to launch a fresh actual installed Codex parent session with isolated project/Aether/platform roots and production-installed skills/agents; record the observed event ABI and preserve raw parent/child event streams. This is not the Aether Go worker adapter. Extend bounded scenarios: ordinary one-Builder edit/check; explicitly requested independent Watcher with real useful findings; two accepted jobs stopped after one helper terminal is durable but before the other begins; fresh-process public aether resume followed by installed ant-build for only unfinished work; live material question with predeclared fixture response; real cancellation while child is active; crash/stop after native spawn before durable bind, then reconciliation or honest unavailable outcome. Capture actual permission enforcement by a harmless forbidden-write sentinel, exact workspace/cwd and a benign outside-workspace sentinel, and a bounded native child-to-child delegation probe with actual allowed/denied tool result. The latter measures host nesting only and is not Aether recruitment/worktree proof. No instruction such as 'do not write' counts as sandbox enforcement. Include real stale-result/child-mismatch command refusals with unchanged inventories and a missing-skill or unavailable-host control. Run an equivalent minimal actual Claude build/check from the same prepared fixture baseline and compare helper topology, accepted intent, edits, actual check outcomes and credit semantics; describe unavailable comparison honestly. Validate from successful native tool events plus actual edits/checks/receipts, not exit 0 or assistant narration. Record predeclared fixture answers as harness provenance, not owner testimony.</action>
  <acceptance_criteria>The runner records ordinary versus independent-review proof separately; each native result binds host child to Go assignment; partial resume, cancellation and spawn/bind ambiguity have raw evidence. Capability absence and denied controls are explicit. Prepared plans do not count as live planning, nested host probes do not count as governed recruitment, and semantic comparison does not claim full parity.</acceptance_criteria>
  <verify><automated>AETHER_CODEX_NATIVE_LIVE=1 AETHER_CODEX_NATIVE_SCENARIOS=qualification go test ./cmd -run '^TestCodexNativeWorkerFreshHost$' -count=1 -json -timeout 60m; go test ./cmd -run '^TestCodexNativeWorkerReceiptValidation$' -count=1 -timeout 60s</automated></verify>
  <done>All required real-host scenarios have durable pass/refusal/incomplete findings, and no unobserved capability is advertised. Missing mandatory Builder/Watcher/resume proof prevents their success criteria from being marked met.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce and report only the capabilities the host actually provides</name>
  <files>pkg/codex/platform_contract.go, pkg/codex/platform_contract_test.go, cmd/codex_native_worker.go, cmd/wrapper_usage_resolve_test.go</files>
  <read_first>Task 1 capability receipt; pkg/codex/platform_contract.go; cmd/wrapper_usage_resolve.go wrapperUsageWasReported; cmd/internal_worker_adapter.go permission handling; pkg/codex/worker.go subprocess launch.</read_first>
  <behavior>Requested unsupported per-child sandbox/workspace/nesting capability -> explicit native refusal before spawn; supported observed mode -> admission. Existing subprocess capability descriptions remain accurate. Native usage absent -> reported false and no numeric cost; a reported provider zero is distinct from missing data.</behavior>
  <action>Add separately named native capability fields/limitations with qualified client/configuration evidence, keeping existing Go-subprocess fields unchanged. Bind native admission to the actual requested profile and observed supported host envelope; do not promote caller prose or inherited broad permissions into enforced per-child read-only behavior. Unsupported stricter controls refuse the native route and may name the existing explicit subprocess route, never silently switch lanes or launch twice. Keep native worktree allocation/recruitment outside this phase. Add absence/misattribution tests to the existing usage resolver: worker prose, parent totals, missing source tags and mismatched session identity cannot manufacture child usage. Do not add a native usage collector in this phase unless Task 1 exposes a genuine provider-owned per-child source; an exposed source is retained as evidence and may be passed through only with exact worker/session binding and existing reported-source semantics. Empty Usage remains omitted/unreported rather than a zero-cost estimate. No source field claims more than its supporting live capability case.</action>
  <acceptance_criteria>Native claims are distinct from accurate subprocess claims, unsupported requested controls refuse before launch, and missing measurements remain missing in serialized output. Capability claims have a corresponding actual host receipt reference.</acceptance_criteria>
  <verify><automated>go test ./pkg/codex -run '^TestCodexNativePlatformContract' -count=1 -timeout 60s; go test ./cmd -run '^(TestCodexNativeWorkerCapabilities|TestCodexNativeUsageUnreported|TestCodexNativeUsageRejectsParentAttribution)$' -count=1 -timeout 60s</automated></verify>
  <done>Users see truthful native support and costs, and the bridge cannot weaken a requested restriction merely because the host lacks it.</done>
</task>

<task type="auto">
  <name>Task 3: Qualify the final source and installed guidance after capability changes</name>
  <files>cmd/command_guide.go, .aether/skills/colony/aether-colony-build-cycle/SKILL.md, cmd/codex_native_worker_test.go, .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-qualification.json</files>
  <read_first>Task 1 captures and Task 2 final contract; 204.2-SYNTHESIS.md evidence contract; Phase 204.1 generated skill/support tests.</read_first>
  <action>Align the native Build Flow and codexNativeBuildCommandGuide with the implemented reserve/bind/release/record/observe/context/question sequence and public partial-resume behavior. Preserve its build+codex selection and the shared Claude/OpenCode catalog per D-04; native observe/reconnect/cancel guidance must not enter either primary platform's wrapper route. Ensure the installed instructions preserve runtime-selected castes/waves, immediate per-helper records, scoped answer relay, native cancellation acknowledgement and honest unsupported controls. Extend the existing table-driven TestCodexNativeBuildGuidePlatformIsolation in cmd/codex_native_worker_test.go so the final Codex guide requires the complete native operation sequence and public resume routing, while Claude/OpenCode exclude native bridge/launcher/message/reconciliation instructions and retain their matching wrapper, build-completion-stage and exact existing build-finalize command. Keep the Plan 04 question-guide assertions passing and repeat the platform-order mutation control from Plan 01. Build/install a final source-pinned candidate into new disposable roots after all production changes are committed; run the complete qualification selection again against that candidate, including the early ordinary and interruption scenarios, and write native-qualification.json. Keep every earlier receipt with its original candidate identity. The final receipt must contain the production-input inventory/digest, actual binary and agent/skill hashes, raw event digests, scenario assertions, negative controls and explicit unreported measurements. Validate every referenced artifact from disk. If later plans repair any production/skill/harness inputs, invalidate affected derived claims and rerun impacted scenarios against the new exact candidate before standard verification can pass.</action>
  <acceptance_criteria>The final installed native skill, Go binary, tested source and host receipts identify the same candidate. Every claimed successful native scenario has actual successful evidence with no hidden skip, surrogate worker or parent edit. Final three-platform guide tests prove native launch/answer/recovery instructions remain Codex-only and preserve Claude/OpenCode launch, completion-stage and finalizer routes.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexNativeBuildGuidePlatformIsolation|TestCodexNativeDecisionGuideContract|TestCodexAntSkillGuidesPreserveOtherPlatforms)$' -count=1 -timeout 60s; AETHER_CODEX_NATIVE_LIVE=1 AETHER_CODEX_NATIVE_SCENARIOS=all go test ./cmd -run '^TestCodexNativeWorkerFreshHost$' -count=1 -json -timeout 60m; go test ./cmd -run '^TestCodexNativeWorkerReceiptValidation$' -count=1 -timeout 60s</automated></verify>
  <done>The final candidate's native lifecycle is qualified to the exact observed scope, with earlier experiment receipts preserved and unavailable capabilities explicit.</done>
</task>
</tasks>

<verification>
Native live opt-in skips in routine suites are not evidence. With AETHER_CODEX_NATIVE_LIVE=1, missing host/auth/tool/capture writes an incomplete receipt and fails the requested scenario. Bound live runs, retain actual exits, use asynchronous polling and progress updates. ANT-07/unclassified stays a flagged assumption for explicit final claim-versus-evidence review.
</verification>

<success_criteria>
Installed real Builder and explicitly requested Watcher proof, ordinary proportionality, full context/question delivery, partial fresh-session resume, actual host control/activity and absent-usage behavior are all evidenced on the final candidate or honestly incomplete. Final guide regression tests preserve Claude/OpenCode wrapper/finalizer behavior while qualifying Codex-only native instructions.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-07-SUMMARY.md with exact supported host/version and unresolved evidence limits.
</output>
