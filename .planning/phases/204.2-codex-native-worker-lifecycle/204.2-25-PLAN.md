---
phase: 204.2-codex-native-worker-lifecycle
plan: "25"
type: execute
wave: 19
depends_on: ["204.2-24"]
files_modified:
  - cmd/codex_native_gap_host_capture_test.go
  - cmd/codex_native_gap_context_test.go
  - cmd/codex_native_worker_live_test.go
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-3/host-context.json
autonomous: true
gap_closure: true
requirements: ["ANT-04", "ANT-06", "ANT-07"]
reversibility_gate: true
must_haves:
  truths:
    - 'D-04/D-09: One existing launcher and Go finalizer remain authoritative; capture is observational only.'
    - 'D-05/D-10: A supported same-invocation source is required before adapters are implemented; malformed, synthetic and substituted evidence refuses.'
    - 'D-01/D-07: Actual installed ant-build review/question cases are rerun and later included in the complete standard qualification.'
  artifacts:
    - path: cmd/codex_native_gap_host_capture_test.go
      provides: "Narrow acquisition adapter or its rejecting behavioral controls"
    - path: cmd/codex_native_gap_context_test.go
      provides: "Narrow acquisition adapter or its rejecting behavioral controls"
    - path: cmd/codex_native_worker_live_test.go
      provides: "Narrow acquisition adapter or its rejecting behavioral controls"
    - path: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-3/host-context.json
      provides: "Source-bound acquisition evidence; unavailable remains blocked"
  key_links:
    - from: "cmd/codex_native_gap_host_capture_test.go"
      to: "cmd/codex_native_worker_live_test.go"
      via: "Same-invocation raw source, immutable inventory and typed replay validation"
---

<objective>
Connect a demonstrated host evidence source to the existing validator, then complete the previously blocked actual review/context cases. In plain English: the checker reads facts from the same real worker session and rejects lookalike or reconstructed proof.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-VERIFICATION.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-21-PREFLIGHT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP3-PLANNING.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-COLLECTOR-DESIGN.md
</context>

## Shared execution contract

**Owner design decision (2026-09-18):** Design a fixture-scoped passive evidence collector while preserving current proof requirements. Read `204.2-COLLECTOR-DESIGN.md` for storage/source contracts, staged implementation ownership and refusal checks. This is design authority, not demonstrated acquisition: the existing blocked receipt remains binding; no source adapter or dependent live run is released. Collector-core implementation needs the narrow Plan 24 scope amendment specified there before execution; do not infer script ownership from this reference.

Read 204.2-GAP3-PLANNING.md and 204.2-GAP2-PLANNING.md. D-01–D-10 retain the existing SYNTHESIS meanings. Preserve public $ant-* names, one actual native launcher, Go-owned assignment/journal/finalizer authority, proportionate team selection, scoped answers and honestly absent usage. No owner acceptance is delegated by these plans.

Run sequentially on the current branch with workflow.use_worktrees=false. Preserve all prior plans/reports/raw evidence, shared installation/hub/configuration/credentials, CalVault, Phase 205, and unrelated .gsd/ files. New receipts use evidence/gap-closure-3; raw inputs use a new absolute external AETHER_GAP3_EVIDENCE_ROOT. Pin actual source/capture/corpus/binary/host identities. Child-only environment dictionaries own disposable homes/caches; never repurpose parent HOME or CODEX_HOME. Only existing fixture-authorized authentication may be used; no login, package installation, publication or global tracing. Record missing capabilities and stop dependent execution. Source changes invalidate affected proof and the final matrix; no synthetic export, protocol schema, parent reconstruction or elapsed timer can replace actual required evidence. No broad suites until Plan 23.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Wire strict supported settings/tool acquisition</name>
  <files>cmd/codex_native_gap_host_capture_test.go; cmd/codex_native_worker_live_test.go</files>
  <read_first>204.2-ACQUISITION.md; evidence/gap-closure-3/acquisition.json; cmd/codex_native_gap_host_capture_test.go; cmd/codex_native_gap_evidence_test.go; cmd/codex_native_worker_live_test.go</read_first>
  <action>Require Plan 24 supported status and independently verify raw source hashes/client identity before editing. If unavailable or stale, stop and write a blocker; a SUMMARY filename alone is not admission. Add negative controls to TestCodexNativeGapHostCapture before changing nativeGapCollectHostCapture and nativeGapValidateCapture. Decode only the method/version and exact raw-field mappings proved in ACQUISITION.md; do not broaden the envelope to accept arbitrary normalized input. Validate complete effective settings/model/effort and all model-facing tool parameter schemas from the same actual invocation and original inventory. Keep external schema references disabled. Reject wrong session/client/model/argv/effort, missing/extra records, truncated exports, unsupported method/version, duplicate tool identity and newly inserted replay artifacts. A test-only adapter is never accepted by the live path. If supported source extraction needs files outside this narrow ownership or a new control plane, stop for a scoped replan.</action>
  <acceptance_criteria>A supported actual raw export passes semantic AND provenance checks; deterministic or hand-written envelopes still cannot qualify. Removing source corroboration makes a negative control fail. Missing source continues to return incomplete/unavailable.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexNativeGapHostCapture$' -count=1 -timeout 60s
go test ./cmd -race -run '^TestCodexNativeGapHostCapture$' -count=1 -timeout 60s</automated></verify>
  <done>A supported actual raw export passes semantic AND provenance checks; deterministic or hand-written envelopes still cannot qualify. Removing source corroboration makes a negative control fail. Missing source continues to return incomplete/unavailable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Validate actual full context and bound answer delivery</name>
  <files>cmd/codex_native_gap_context_test.go; cmd/codex_native_worker_live_test.go</files>
  <read_first>204.2-ACQUISITION.md; cmd/codex_native_gap_context_test.go; cmd/codex_native_worker_live_test.go; cmd/codex_native_gap_attribution_test.go; 204.2-20-SUMMARY.md</read_first>
  <action>Use only the demonstrated plaintext/delivery source to extend nativeGapHostBoundary and nativeGapCaptureContext. Preserve the full capsule, matched skill, steering and handoff bytes; bind raw delivered plaintext and actual ACK to parent/child/task/turn/message identity and ordering. Answers remain current-question/current-assignment scoped. Preserve exact 2 MiB bounds, Unicode and whitespace, no-leak challenge generation, wrong-child/stale-answer refusal and unchanged rejection journals. First add controls rejecting locally reconstructed prompts, ciphertext/names/digests without plaintext, right bytes sent to the wrong child, pre-question answers and replayed ACKs. Do not change Go decision or completion authority. Unknown export shapes remain incomplete.</action>
  <acceptance_criteria>Full context/answer bytes and child ACK are independently corroborated from the actual supported source. Existing leakage/bounds/stale controls remain effective; reconstructed expected bytes cannot stand in for delivered bytes.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexNativeGapContext$' -count=1 -timeout 60s
go test ./cmd -race -run '^TestCodexNativeGapContext$' -count=1 -timeout 60s</automated></verify>
  <done>Full context/answer bytes and child ACK are independently corroborated from the actual supported source. Existing leakage/bounds/stale controls remain effective; reconstructed expected bytes cannot stand in for delivered bytes.</done>
</task>
<task type="auto">
  <name>Task 3: Capture actual requested review and question outcomes</name>
  <files>.planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-3/host-context.json</files>
  <read_first>evidence/gap-closure-3/host-context.json if present; 204.2-20-PLAN.md; 204.2-20-SUMMARY.md; cmd/codex_native_worker_live_test.go; cmd/codex_native_gap_attribution_test.go; evidence/gap-closure-3/acquisition.json</read_first>
  <action>Freeze this source/capture identity and pass isolated resource admission. Run the existing ordinary, review and question scenarios using the new capture path. Ordinary retains Builder-plus-checks; review must use an actually requested independent Watcher with returned findings, not a parent review. Question must have actual exact context/answer delivery plus child implementation, checks, durable terminal and credit. Retain raw originals and capture/validator identities separately. Write host-context.json only from actual receipts; link source inventory, installed skill/support/agent bytes and candidate/client identities. If a scenario fails, keep the receipt incomplete and stop dependent qualification; diagnose a narrow reproducible defect under this plan, then freeze and rerun affected cases. Preserve Plan 20 and its unavailable receipts unchanged.</action>
  <acceptance_criteria>All three actual scenario receipts pass attribution and new provenance/context gates on one candidate. Missing Watcher terminal, plaintext or ACK blocks Plan 26/21. Focused tests alone cannot satisfy this task.</acceptance_criteria>
  <verify><automated>AETHER_CODEX_NATIVE_LIVE=1 AETHER_CODEX_NATIVE_SCENARIOS=ordinary,review,question AETHER_CODEX_NATIVE_EVIDENCE_DIR="$AETHER_GAP3_EVIDENCE_ROOT/host-context" go test ./cmd -run "^TestCodexNativeWorkerFreshHost$" -count=1 -json -timeout 120m</automated></verify>
  <done>All three actual scenario receipts pass attribution and new provenance/context gates on one candidate. Missing Watcher terminal, plaintext or ACK blocks Plan 26/21. Focused tests alone cannot satisfy this task.</done>
</task>
</tasks>

<verification>
Use isolated child environment/offline caches and prior resource-admission protocol for every Go invocation. Retain exact normal/race results and actual scenario outcomes. This renews incomplete Plan 20 acceptance; final all-case capture still belongs to Plan 22.
</verification>

<success_criteria>
All task criteria hold with current source-bound evidence. An unavailable prerequisite is a blocked outcome, never plan completion. Do not create a success SUMMARY or advance dependent plans for an unavailable result. Keep qualification gaps and ANT requirements pending until Plan 23 verifies them.
</success_criteria>

<output>
After successful execution create 204.2-25-SUMMARY.md; on blocked acquisition write a dated BLOCKED report and update session continuity instead.
</output>
