---
phase: 204.2-codex-native-worker-lifecycle
plan: "09"
type: execute
wave: 6
depends_on: ["204.2-07"]
files_modified:
  - cmd/codex_native_worker.go
  - pkg/codex/platform_contract.go
  - pkg/codex/platform_contract_test.go
  - cmd/wrapper_usage_resolve_test.go
  - cmd/command_guide.go
  - cmd/codex_skill_surface.go
  - cmd/codex_skill_surface_test.go
  - cmd/command_guide_test.go
  - cmd/testdata/command_catalog.json
  - .aether/skills/colony/aether-colony-build-cycle/SKILL.md
  - cmd/codex_native_worker_test.go
  - cmd/codex_native_worker_live_test.go
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-qualification.json
autonomous: true
requirements: [ANT-04, ANT-05, ANT-06, ANT-07]
must_haves:
  truths:
    - "Native admission refuses unsupported requested controls without silently changing lanes or weakening restrictions."
    - "Raw exposed usage, collected worker usage and absent cost remain distinct with exact provider/thread provenance."
    - "The final installed candidate reruns every original native qualification scenario with actual helper and host events."
    - "Final guidance preserves runtime-selected teams and keeps native launch, answer and recovery instructions Codex-only."
  artifacts:
    - path: pkg/codex/platform_contract.go
      provides: "Evidence-qualified native capabilities distinct from subprocess capabilities"
      contains: "Native"
    - path: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-qualification.json
      provides: "Final production-input, binary, installed-agent/skill and raw event identities"
  key_links:
    - from: cmd/codex_native_worker.go
      to: pkg/codex/platform_contract.go
      via: "Requested native capability admission"
      pattern: "Native"
    - from: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-qualification.json
      to: cmd/codex_native_worker_live_test.go
      via: "Complete shared matrix executed against the final committed candidate"
      pattern: "native"
---

<objective>
Implement truthful native capability admission and qualify the final installed candidate using Plan 07’s reviewed real-host harness. In plain English, advertise only demonstrated controls, then prove the final version still performs the complete helper lifecycle.

Purpose: Complete original Plan 07 Tasks 2–3 without dropping any scenario, final rerun or success criterion.
Output: Evidence-backed native capability contract, aligned platform guidance and final native-qualification.json for Plan 08.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/research/ant-codex-parity-2026-09-16/GSD-INTEGRATION.md
</context>

## Execution amendment and scope

See 204.2-SCOPE-AMENDMENT.md. This is the unchanged substantive remainder of original Plan 07, split after its measured implementation-context excess. Read revised 204.2-07-SUMMARY.md, reviewed shared harness, all capability/comparison receipts and root review findings before editing. Fix any remaining concrete proof gap; an inherited incomplete mandatory scenario cannot become passed by changing a label.

Ownership is the thirteen listed files plus this plan’s SUMMARY. The root explicitly adds the existing live harness for the confirmed Claude fixture setup correction: use the production-installed flat `ant-build.md` wrapper and `/ant-build` entrypoint, enable discovery from the isolated fixture's user configuration, and check installed wrapper/Builder identity. Original c370 invoked `/ant:build` with a nonexistent nested wrapper path and disabled setting sources; its zero-turn unknown-command result is a harness defect, not proof of missing Claude capability. The root also adds the generated Codex shim renderer and its two existing expectation-test files: `cmd/codex_skill_surface.go` currently renders the shared catalog before native adaptation, so installed ant-build embeds contradictory subprocess instructions. Apply the existing platform adapter before rendering, assert native installed guidance, and preserve the other eight skills and primary wrappers. Root additionally permits exact regeneration/review of embedded `cmd/testdata/command_catalog.json` after a targeted check showed drift beyond the historical baseline; classify every delta against registered source before accepting the refresh. Reuse the reviewed shared matrix; coordinate any other necessary harness repair explicitly. Such a repair invalidates affected evidence and requires the affected rerun. No new runner or per-scenario subsystem is introduced. The complete final matrix retains all eleven entries and may use the measured 120-minute overall timeout with individually bounded host runs. Preserve all original captures and validator identities.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce and report only the capabilities the host actually provides</name>
  <files>pkg/codex/platform_contract.go, pkg/codex/platform_contract_test.go, cmd/codex_native_worker.go, cmd/wrapper_usage_resolve_test.go</files>
  <read_first>Plan 07 capability receipt; pkg/codex/platform_contract.go; cmd/wrapper_usage_resolve.go wrapperUsageWasReported; cmd/internal_worker_adapter.go permission handling; pkg/codex/worker.go subprocess launch.</read_first>
  <behavior>Requested unsupported per-child sandbox/workspace/nesting capability -> explicit native refusal before spawn; supported observed mode -> admission. Existing subprocess capability descriptions remain accurate. Native usage absent -> reported false and no numeric cost; a reported provider zero is distinct from missing data.</behavior>
  <action>Add separately named native capability fields/limitations with qualified client/configuration evidence, keeping existing Go-subprocess fields unchanged. Bind native admission to the actual requested profile and observed supported host envelope; do not promote caller prose or inherited broad permissions into enforced per-child read-only behavior. Unsupported stricter controls refuse the native route and may name the existing explicit subprocess route, never silently switch lanes or launch twice. Keep native worktree allocation/recruitment outside this phase. Add absence/misattribution tests to the existing usage resolver: worker prose, parent totals, missing source tags and mismatched session identity cannot manufacture child usage. Do not add a native usage collector in this phase unless Plan 07 exposes a genuine provider-owned per-child source; an exposed source is retained as evidence and may be passed through only with exact worker/session binding and existing reported-source semantics. Empty Usage remains omitted/unreported rather than a zero-cost estimate. No source field claims more than its supporting live capability case.</action>
  <acceptance_criteria>Native claims are distinct from accurate subprocess claims, unsupported requested controls refuse before launch, and missing measurements remain missing in serialized output. Capability claims have a corresponding actual host receipt reference.</acceptance_criteria>
  <verify><automated>go test ./pkg/codex -run '^TestCodexNativePlatformContract' -count=1 -timeout 60s; go test ./cmd -run '^(TestCodexNativeWorkerCapabilities|TestCodexNativeUsageUnreported|TestCodexNativeUsageRejectsParentAttribution)$' -count=1 -timeout 60s</automated></verify>
  <done>Users see truthful native support and costs, and the bridge cannot weaken a requested restriction merely because the host lacks it.</done>
</task>

<task type="auto">
  <name>Task 2: Qualify the final source and installed guidance after capability changes</name>
  <files>cmd/command_guide.go, cmd/codex_skill_surface.go, cmd/codex_skill_surface_test.go, cmd/command_guide_test.go, cmd/testdata/command_catalog.json, .aether/skills/colony/aether-colony-build-cycle/SKILL.md, cmd/codex_native_worker_test.go, cmd/codex_native_worker_live_test.go, .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-qualification.json</files>
  <read_first>Plan 07 captures and this plan’s Task 1 final contract; 204.2-SYNTHESIS.md evidence contract; Phase 204.1 generated skill/support tests.</read_first>
  <action>Align the native Build Flow and codexNativeBuildCommandGuide with the implemented reserve/bind/release/record/observe/context/question sequence and public partial-resume behavior. Preserve its build+codex selection and the shared Claude/OpenCode catalog per D-04; native observe/reconnect/cancel guidance must not enter either primary platform's wrapper route. Ensure the installed instructions preserve runtime-selected castes/waves, immediate per-helper records, scoped answer relay, native cancellation acknowledgement and honest unsupported controls. Extend the existing table-driven TestCodexNativeBuildGuidePlatformIsolation in cmd/codex_native_worker_test.go so the final Codex guide requires the complete native operation sequence and public resume routing, while Claude/OpenCode exclude native bridge/launcher/message/reconciliation instructions and retain their matching wrapper, build-completion-stage and exact existing build-finalize command. Keep the Plan 04 question-guide assertions passing and repeat the platform-order mutation control from Plan 01. Build/install a final source-pinned candidate into new disposable roots after all production changes are committed; run the complete qualification selection again against that candidate, including the early ordinary and interruption scenarios, and write native-qualification.json. Keep every earlier receipt with its original candidate identity. The final receipt must contain the production-input inventory/digest, actual binary and agent/skill hashes, raw event digests, scenario assertions, negative controls and explicit unreported measurements. Validate every referenced artifact from disk. If later plans repair any production/skill/harness inputs, invalidate affected derived claims and rerun impacted scenarios against the new exact candidate before standard verification can pass.</action>
  <acceptance_criteria>The final installed native skill, Go binary, tested source and host receipts identify the same candidate. Every claimed successful native scenario has actual successful evidence with no hidden skip, surrogate worker or parent edit. Final three-platform guide tests prove native launch/answer/recovery instructions remain Codex-only and preserve Claude/OpenCode launch, completion-stage and finalizer routes.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexNativeBuildGuidePlatformIsolation|TestCodexNativeDecisionGuideContract|TestCodexAntSkillGuidesPreserveOtherPlatforms)$' -count=1 -timeout 60s; AETHER_CODEX_NATIVE_LIVE=1 AETHER_CODEX_NATIVE_SCENARIOS=all go test ./cmd -run '^TestCodexNativeWorkerFreshHost$' -count=1 -json -timeout 120m; go test ./cmd -run '^TestCodexNativeWorkerReceiptValidation$' -count=1 -timeout 60s</automated></verify>
  <done>The final candidate's native lifecycle is qualified to the exact observed scope, with earlier experiment receipts preserved and unavailable capabilities explicit.</done>
</task>
</tasks>

<verification>
Routine live opt-in skips are not evidence. Explicit qualification requires durable real-host pass/refusal/incomplete dispositions with raw source events, useful helper work and exact candidate identity. Missing mandatory Builder, Watcher, scoped answer or fresh-session resume proof prevents the associated success criterion from passing. No model/profile weakening, parent substitute or hidden scenario omission. Plan 08 owns the one full normal/race regression gate.
</verification>

<success_criteria>
Requested native controls are admitted only to observed support, costs remain honest, complete final native lifecycle proof matches the exact production and installed inputs, and Claude/OpenCode wrapper/finalizer behavior remains intact. The final receipt is ready for fail-capable Plan 08 verification or explicitly records the unresolved gap.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-09-SUMMARY.md with both task outcomes, exact final source/installed identities and unresolved evidence limits. Shared tracking belongs to the root orchestrator.
</output>
