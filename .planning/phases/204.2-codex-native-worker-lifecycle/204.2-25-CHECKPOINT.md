# Plan 25 implementation checkpoint

Task 1 is implemented and tested in `88f79590`. Plan 25 remains incomplete; no success SUMMARY or ANT completion is claimed.

In plain English: Aether now refuses a requested cancellation guarantee unless support is proved. Missing optional host internals stay honestly unavailable, while worker/context/recovery checks remain required.

The guard runs before reserve, bind, observe, record, stage and question mutation. An empty, unknown, limited, test-only or unavailable capability cannot authorize the guarantee. Ordinary requests remain admitted. New proof receipts bind the owner-approved amendment; standalone replay and derived/aggregate receipts reject mismatched contracts. Claude model/session metadata now derives only from its actual root startup event.

## Focused verification

| Run | Top-level tests | Exit | Seconds |
|---|---:|---:|---:|
| normal-cmd | 18 | 0 | 77.39 |
| normal-pkg-codex | 3 | 0 | 4.63 |
| race-cmd | 18 | 0 | 103.19 |
| race-pkg-codex | 3 | 0 | 6.77 |

All 21 selected tests passed normally and with race detection, including unchanged durable state after refusal, ordinary behavior, recovery, exact context controls, metadata substitution and proof-contract replay controls. Nested tests were required to pass without skips. [Exact evidence](evidence/proof-amendment/implementation.json) binds raw outputs, commands, source inventories and the isolated runner.

The focused resource admission reused only obsolete task-owned compiler/module caches by recorded same-device transfer. It reserved a standalone snapshot, cache growth, active scratch and untouched disk headroom using the existing measurement/admission helpers. It did not lower or replace Plan 23’s full-suite estimate. The initial setup failure happened before Go; its records remain immutable. Shared homes, installed runtime and CalVault state were not test targets.

## Remaining work

Task 25.2 still needs actual source-bound full context/answer delivery and ACK. The two rollout-only app-server reads were inconclusive: they omitted the owned historical thread-history database and WAL. A protected WAL-aware snapshot found 63 history items but no child user message or full spawn/send prompt. The new store therefore does not demonstrate the required plaintext; [source feasibility](evidence/proof-amendment/history-source-feasibility.json) retains exact SQL/correlations and unchanged originals. This is a bounded historical finding, not proof that no future/current host could expose the messages. Task 25.3 remains dependent on a demonstrated source and the actual ordinary/review/question runs.

Reviewed Task 25.1 admits the independent Plan 26 deterministic safe-refusal work. Its actual active-child/runtime guard proof remains required. Current verification stays gaps_found, and Phase 205 owner acceptance is untouched.

## Current-client source check

A fresh Codex CLI 0.155.0 cancellation fixture was inspected after all owned processes and credential copies were gone. Actual spawn/send ciphertext matches the intended child's inbound ciphertext. The 15,574-byte saved runtime prompt is absent from attributable plaintext in the parent events, both rollouts and a protected WAL-aware history snapshot. The history store has no child initial `userMessage`; this cancellation case has no scoped-answer ACK. [Current-source evidence](evidence/proof-amendment/history-source-0155.json) pins the unchanged originals. This proves a bounded export limitation, not that every delivery design is impossible. Task 25.2 remains incomplete under the current native-message delivery protocol.

## Prospective delivery route

A later retained actual child contains two complete successful plaintext tool outputs, independently joined to its command/call/result, turn, parent and task. This establishes the source capability for a new child-read and separate-ACK protocol; it does not qualify full context or answers. [Child-fetch design](204.2-CHILD-FETCH-DESIGN.md) records that mapping and the prospective Task 2 extension. Implementation is in progress; no additional discovery model run is needed. The immutable amendment, historical native-message failures and mandatory full-context/ACK requirements remain unchanged.

## Child-fetch implementation checkpoint

The prospective `child-fetch/v1` implementation is committed as `1f905181`. All 93 selected top-level tests passed normally and with race detection, without skips or missing results. This completes the implementation check, not actual Task 2 delivery qualification or Plan 25.

In plain English: a new worker must read its complete saved instructions and separately confirm what it received. Aether requires the actual child records before accepting successful work. Answers use a fresh request tied to that worker's current question. Older saved jobs retain their original protocol.

| Run | Top-level tests | Exit | Seconds |
|---|---:|---:|---:|
| normal-cmd | 91 | 0 | 231.20 |
| normal-pkg-codex | 2 | 0 | 13.90 |
| race-cmd | 91 | 0 | 296.38 |
| race-pkg-codex | 2 | 0 | 19.18 |

[Implementation evidence](evidence/proof-amendment/child-fetch-implementation.json) pins the four raw runs, exact source/corpus, prior failed attempts and reviewed compiler-cache retirement. The first attempt retains its three failures, timeout and 28 missing tests; the second retains its one failed historical partial-result fixture. Production refusal stayed enforced; historical packet fixtures now explicitly select their old protocol before workers or packets exist.

The isolated runner preserved all source inputs and historical evidence. It retired only 20,737 exactly inventoried regenerable cache files (4,455,800,832 allocated bytes), preserved directories and excluded artifacts, and retained the measured 10,470,596,608-byte scoped admission floor. Plan 23's 14,191,223,120-byte full-suite estimate is unchanged.

Next: actual ordinary worker plus strict independent replay, then the requested independent review and scoped-question scenarios on the same candidate. No full-suite, actual delivery, installation, Plan 25 SUMMARY or ANT completion is claimed here.

## First actual child-fetch ordinary attempt — incomplete

The actual ordinary fixture on snapshot `e5c56fd5dc471b37ad955af6549417489501e12a` ran for 420.96 seconds and produced an incomplete original receipt. [Attempt evidence](evidence/proof-amendment/child-fetch-ordinary-attempt.json) retains its raw run, original flags, source/candidate identity and unchanged failed outcome.

The first reported rejection is a verifier mismatch: the child called `collaboration.wait_agent` while waiting for its parent's release, before fetching context. The amendment requires ACK before useful work; this passive waiting operation needs a precise classification. Several observed literal output wrappers and inspection commands also remain unclassified. Actual edit, terminal and runtime-credit records do not override these proof failures.

Independent replay did not launch: free space was 10,280,493,056 bytes against the unchanged 10,470,596,608-byte scoped admission floor. Owned-process cleanup and fixture credential cleanup completed. Next work is a narrow source-grounded verifier correction and reviewed owned-cache housekeeping, followed by focused checks and fresh affected live capture. Review/question and all dependent qualification remain blocked; no success SUMMARY is created.

The preceding V3 setup failure also remains unchanged. Its wrong historical mapping was repaired only in a new V4 runner. New reference lists deduplicate exact path/hash pairs per independent verification pass, preserving original evidence while avoiding repeated hashing of identical files.

## Prospective verifier correction and focused retries

Five reviewed source/test files now address the observed passive wait, exact literal output and command forms, strict context-result decoding, and a self-contained synthetic Claude profile fixture. In plain English: the verifier must recognize the commands the worker actually used while still refusing work before the full-context acknowledgement, hidden operations, and unrelated worker evidence.

The first actual ordinary receipt remains incomplete. Its edit, terminal and runtime-credit records do not override the original proof failures. The pinned Plan18 capture is used only for a read-only parser regression; its original outcome also remains incomplete. [Follow-up evidence](evidence/proof-amendment/child-fetch-verifier-followup.json) retains exact source, runner, capture and cleanup references.

Three failed focused attempts remain unchanged:

| Attempt | Actual result | Prospective correction |
|---|---|---|
| Initial committed snapshot | No Go launched; admission short by 937,984 bytes | Reviewed owned scratch retirement; reuse the committed snapshot |
| Continuation | Compilation failed before tests: missing `strconv` import | Add only the missing import in a new independent snapshot |
| Import retry | 22 test families passed, one skipped for a missing capture path, one failed because a historical Git object was absent; no race run | Bind the correct immutable Plan18 capture and use snapshot HEAD for the synthetic profile control |

Three reviewed cleanup receipts removed only their exact inventoried task-owned files: 3,853 obsolete compiler-cache files (1,024,008,192 allocated bytes), 1,583 obsolete Plan26 scratch files (482,758,656 bytes), and 52 old Plan08 test binaries (1,415,651,328 bytes). Directories, protected evidence and original outcomes remain preserved. The measured 10,471,960,576-byte scoped floor and Plan23's 14,191,223,120-byte full-suite estimate remain unchanged.

All **24 selected command test families passed normally and with race detection**, with 400 individual passing test results per run and no skips. Normal execution took 42.68 seconds; race execution took 123.93 seconds. The exact independently committed snapshot `47a21932ef54d1c963ee73e639bfba2e2d66bc71` is retained for later live use. The five-file correction is committed as `850ea7f0`. This is focused implementation evidence; actual fresh delivery qualification remains required.

Next: fresh ordinary worker plus strict independent replay, then independent review and scoped-question scenarios. Plan25 remains incomplete; no success SUMMARY, requirement completion, full-suite pass or installation is claimed.

## Second actual ordinary attempt — context passed, child checks incomplete

The fresh run on snapshot `47a21932ef54d1c963ee73e639bfba2e2d66bc71` and its strict independent replay both remain incomplete. Their first validation stage accepted full context delivery and acknowledgement. The child edit and terminal are corroborated, and the parent-substitution flag is false. The later child-check stage failed; runtime credit does not override this failure. [Second attempt evidence](evidence/proof-amendment/child-fetch-ordinary-second-attempt.json) preserves both receipts, raw runs and exact unclassified operations.

The remaining classification includes read-only `&&` inspection sequences, a literal patch bound through a returned variable, bare `git diff --check`, and a later `go test ./... -cover` after the required uncached test command. These are under source-grounded diagnosis; no corrected or passing outcome is inferred. Review/question and dependent qualification remain blocked.

Before the run, a fourth exact cleanup retired 3,048 regenerable race-cache files (568,868,864 allocated bytes) after the passing focused owner closed. All directories and protected records remained unchanged; normal/module caches stayed intact. The original storage floors were preserved.

## Attribution correction ready; tests blocked by disk reserve

The second actual child passed its required `go test ./... -json -count=1` after the owned edit. The verifier lost that proof at a later successful check whose wrapper printed only `r.output`. The two-file prospective correction preserves prior proof only with a unique same-child/turn command event, exact output, successful exit and unchanged source epoch. A failed later test clears proof. Observed bounded fixture inspection chains, a literal patch result alias, bare diff and coverage commands are also recognized with rejection controls. Both historical ordinary receipts and their outcomes remain unchanged.

The source diff was independently reviewed; formatting and whitespace checks passed. **No Go tests or new actual worker run have executed for this correction.** The files remain uncommitted pending the admitted focused normal/race run. [Pending repair and admission record](evidence/proof-amendment/child-fetch-attribution-pending.json) pins the source files, review, frozen runner, initial config and successful read-only preflight. All 10,192 unique reference checks passed. The initial config predates this checkpoint; resume must use a separately refreshed `config-resume.PENDING.json`, with exact current main/planning/source inputs and fresh resource admission before activation.

A fifth exact cleanup removed only two obsolete command-test archives (381,550,592 allocated bytes), retaining 4,489 other normal-cache files, directories and all protected artifacts. Other recorded obsolete compiler/scratch roots are empty or already transferred; remaining test fixtures and warm package cache are retained. Available disk space is about 10 GB, below the unchanged 10,471,960,576-byte focused floor and 14,191,223,120-byte final-suite estimate. The owner was asked to free at least 6 GB or supply another writable disk. No broad cleanup, storage-estimate reduction, shared installation, credential change or CalVault action is authorized by this checkpoint.

Resume: admit the exact prepared focused run; after normal/race success commit only the two tested source files, then use the separately prepared fresh ordinary V5 packet and strict replay. Requested review/question and dependent recovery/final qualification remain required. Plan25 has no success SUMMARY.

## Attribution correction verified; third actual ordinary attempt incomplete — 2026-09-19

The disk admission blocker is resolved. The two-file correction is committed as `c17c1122`; all 24 selected test families passed normally and with race detection, with **563 individual passes per run and no skips**. The exact snapshot is `abdf3073b7308670684e6e188f534db9b87bab4e`. Normal execution took 44.22 seconds; race execution took 118.63 seconds. A preceding focused failure remains retained: restored historical parser/coordinator bytes and a narrow full-result attribution repair were applied only prospectively. Cache readmission records the new filesystem device identity and eight changed `.DS_Store` metadata files; dependency source contents stayed unchanged.

The third actual ordinary run and strict independent replay both remain incomplete. The child searched the tool catalogue before its context acknowledgement. The context observer rejected that operation; the parent then made unclassified session searches. The original and derived receipts retain `parent_substitution: true`, missing validated child checks and no runtime credit. The child edit does not qualify successful delivery. Owned-process and temporary-credential cleanup completed. [Exact evidence](evidence/proof-amendment/child-fetch-attribution-third-attempt.json) preserves the passing focused run, preceding failure, actual original/replay and resource identities.

In plain English: the local checks pass, but the real worker trial exposed ambiguous setup instructions. The next narrow repair clarifies how the child reads and acknowledges instructions and how the parent preserves a refusal; acknowledgement validation stays strict. The equivalent observed `go test -cover ./...` form also needs bounded additional-check controls. Freeze and test the repaired candidate before another actual ordinary trial. Review/question, recovery, final matrix and final suites remain pending. Plan25 has no success SUMMARY; ANT requirements and Phase205 owner acceptance remain unchanged.

## Canonical bootstrap repair verified; fourth actual ordinary attempt incomplete — 2026-09-19

The three-file canonical bootstrap/coverage repair is committed as `dada224d`. All 24 selected families passed normally and with race detection, **613 individual passes per run with no skips**, on exact snapshot `0f6eb5dba6a076aca00779ee18f808d827dddc2e` (45.02/70.27 seconds). The fresh live runner also closes an inherited cache-type admission gap, with ten offline controls for unexpected links/special files and replacement during hashing.

The fourth actual ordinary run (478.07 seconds) and strict replay (6.08 seconds) remain incomplete. Raw records prove a child `send_message` and a further passive wait between its completed context read and ACK. The message body is encrypted; no harmless-content assumption can satisfy the strict ordering rule. The two earlier waits occurred before the read and are not the failure. Two actual read-only `Promise.all` batches with destructured results and a `JSON.stringify` object also remain unclassified. The parent did not substitute for the child, and actual edit/check commands, terminal and runtime finalizer credit exist; those records do not override the independent validation failure. [Fourth-attempt evidence](evidence/proof-amendment/child-fetch-bootstrap-fourth-attempt.json) preserves both original and derived outcomes and all passing focused results. Cleanup completed.

In plain English: the worker completed the sample repair, but paused to message its parent before confirming it had received the instructions. The next correction will make immediate ACK explicit and teach the parser only the observed safe inspection form. Keep the ordering validator unchanged, freeze and test the new candidate, then rerun the affected actual case. Review/question and dependent recovery/final qualification remain pending. Plan25 and ANT requirements are incomplete; Phase205 owner acceptance is untouched.

## Immediate-ACK repair verified; fifth actual ordinary attempt incomplete — 2026-09-19

The immediate-ACK/named-inspection-batch repair is committed as `3a69a55f`. All 24 selected families passed normally and with race detection: **678 individual passes per run, no failures or skips**, on exact snapshot `cf71f3f738fe1f4e5d46829e963c97f2c1cbbb36` (44.49/70.19 seconds).

The fifth ordinary run (309.48 seconds) and strict replay (4.30 seconds) remain incomplete. The child attempted ACK directly after its read but omitted the required absolute `--request` and added an unsupported `--release-token`. Its retry removed the unknown flag but still omitted the request. Both runtime refusals were correct; the child stopped without edits, tests or credit. Parent literal command wrappers also omitted JavaScript's optional final semicolon, and one printed the bound result through `JSON.stringify`; those forms remain unclassified and the original `parent_substitution: true` flag is preserved. [Fifth-attempt evidence](evidence/proof-amendment/child-fetch-ack-command-fifth-attempt.json) retains both receipts, raw errors, focused results and completed cleanup.

In plain English: the worker followed the timing rule but guessed an incomplete command. The next narrow fix spells out the whole ACK command, retaining the exact request file used by the read. Separate bounded parser controls will recognize the observed optional-semicolon and stringified-result forms. Keep strict binding/order/output validation and all historical outcomes unchanged. Fresh focused and actual ordinary proof are required before review/question and recovery/final qualification. Plan25, ANT requirements and Phase205 owner acceptance remain incomplete.

## Complete ACK command verified; sixth actual ordinary attempt incomplete — 2026-09-19

The complete ACK command and bounded optional-final-semicolon repair is committed as `7ab70e31`. All 24 selected families passed normally and with race detection: **690 individual passes per run, no failures or skips**, on exact snapshot `cff17860b65e834f03c538eeb58bb876fb42bded` (44.85/70.05 seconds).

The sixth actual ordinary run (341.14 seconds) and strict replay (6.73 seconds) remain incomplete. The observer refused an unclassified runtime child operation. Recorded child edits and checks exist; the parent stopped correctly before terminal acceptance or finalizer credit, with `parent_substitution: false`. Unclassified commands are `rg --files` and a semicolon-separated literal fixture inspection. Independent raw mapping shows read24/event26/output27, then separate ACK31/event33/output34 with `ack_validated`, and first later tool40. The immediate refusal is the Python context decoder requiring a final semicolon in the full-result wrapper; the read-only commands are secondary Go classification gaps. No ACK-order violation is established, and strict delivery qualification never completed. [Sixth-attempt evidence](evidence/proof-amendment/child-fetch-readonly-sixth-attempt.json) retains raw original/derived outcomes, focused checks and clean owned-process/auth cleanup.

In plain English: the worker performed the sample repair, but the evidence parser first rejected valid command-wrapper syntax and also lacks two read-only inspection forms. Completion remains unproved. Resolve only demonstrated parsing gaps, then freeze, test and recapture. Review/question remain gated. Current-source cancellation refusal must be recaptured before Plan21 recovery; retain the older Plan26 result historically. Final eleven-case qualification, four regression runs and ANT requirements remain pending; Phase205 owner acceptance is untouched.

## Readonly decoder focused failure retained — 2026-09-19

The first four-file readonly repair snapshot failed its normal run: 752 individual passes and seven failures including parent groups. Three valid paired Python cases lack the test harness datetime import. The new outside-root listing case is rejected by the child classifier but accepted by the pre-existing generic parent read-only batch classifier. Source review confirms the test conflated those contracts; preserve the child rejection and existing parent inspection behavior. Race and live execution were correctly withheld. Source remains uncommitted. [Exact failed evidence](evidence/proof-amendment/child-fetch-readonly-focused-failure.json) preserves the immutable snapshot, raw diagnostics and owned cleanup. Fix the harness and the conflated test expectation with full-child rejection coverage, then create a newly admitted snapshot and rerun normal/race. All successful worker qualification and downstream requirements remain pending.

## Readonly repair verified; seventh actual ordinary attempt incomplete — 2026-09-19

The four-file exact-context/readonly repair is committed as `e8e8d62b`. The corrected independent snapshot `8258180d6a683040a8c0e9cd87541c301a4066fc` passed all 24 families normally and with race detection: **760 individual passes in each mode, no failures or skips**, taking 47.21/74.60 seconds. The previous 752-pass/seven-failure focused run remains unchanged.

The seventh actual ordinary and strict replay remain incomplete. The runtime durably observed initial context and ACK; actual child edits, checks, terminal and finalizer credit exist, with no parent substitution. Independent attribution rejects the literal filtered listing `rg --files -g '!*.sum'`; its two textual representations belong to one actual invocation. Runtime credit cannot override that gap. [Seventh-attempt evidence](evidence/proof-amendment/child-fetch-filtered-listing-seventh-attempt.json) retains exact run durations, raw original/replay and cleanup.

In plain English: the real worker now reads and acknowledges its instructions correctly and completes the sample job, but the evidence checker still rejects one file-listing filter. Repair only the demonstrated listing class, freeze/test and recapture. Plan25 all-three delivery, current refusal repeat, recovery, final matrix and regression obligations remain pending. ANT requirements and Phase205 owner acceptance are unchanged.

## Listing repair verified; eighth ordinary trial interrupted by quota — 2026-09-19

The two-file filtered-listing correction is committed as `9f9194e6`. Snapshot `761735ffa9244ad3bfed0787c46b01288559c5c9` passed all 24 selected families normally and with race detection: **780 individual passes in each mode, no failures or skips** (46.16/72.71 seconds). The bounded classifier recognizes the observed `!*.sum` exclusion; strict child identity, ordering and command evidence remain mandatory.

The eighth ordinary run (258.15 seconds) stopped when the live Codex client reported a usage limit. Its original receipt and strict replay (4.62 seconds) remain incomplete, with no child edit, validated check, terminal or runtime credit. Owned processes and fixture credentials were cleaned up. [Eighth-attempt evidence](evidence/proof-amendment/child-fetch-usage-limit-eighth-attempt.json) preserves both receipts and the actual quota error. The displayed reset was September 25 at 09:47, with no timezone specified by the client.

The owner subsequently reported resetting the quota and explicitly requested safe resumption. In plain English: the local checks pass; the interrupted real-worker test must start fresh. Admit a new unique ordinary capture on the unchanged tested source with ownership transferred from the closed eighth run. Preserve every failed original and replay. Existing Qv9 and Plan26v2 drafts pin the failed eighth root and must not activate unchanged; create new prospective bindings after the new ordinary trial qualifies. Plan25 has no success SUMMARY. Current cancellation recapture, recovery, all eleven final cases, four regression runs, ANT requirements and Phase205 owner acceptance remain pending.
