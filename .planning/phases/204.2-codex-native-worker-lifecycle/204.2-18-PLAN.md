---
phase: 204.2-codex-native-worker-lifecycle
plan: "18"
type: execute
wave: 15
depends_on: ["204.2-17"]
files_modified:
  - cmd/codex_native_gap_evidence_test.go
  - cmd/codex_native_gap_host_capture_test.go
  - cmd/codex_native_worker_live_test.go
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-2/host-provenance.json
autonomous: true
gap_closure: true
requirements: ["ANT-06","ANT-07"]
reversibility_gate: true
must_haves:
  truths:
    - "Only versioned semantically valid configuration and full tool definitions captured from the actual invocation can satisfy host provenance."
    - "Empty, unrelated, names-only, missing-schema and explicitly unavailable exports fail provenance even with valid hashes."
    - "Unsupported export capability remains an explicit ANT-07 gap; no synthetic definition or later-written receipt is accepted as actual host output."
  artifacts:
    - path: cmd/codex_native_gap_evidence_test.go
      provides: "Executable fail-closed qualification contract and controls"
    - path: cmd/codex_native_gap_host_capture_test.go
      provides: "Executable fail-closed qualification contract and controls"
    - path: cmd/codex_native_worker_live_test.go
      provides: "Executable fail-closed qualification contract and controls"
    - path: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-2/host-provenance.json
      provides: "Source-bound new evidence; outcome must be independently checked"
  key_links:
    - from: cmd/codex_native_gap_evidence_test.go
      to: cmd/codex_native_worker_live_test.go
      via: "Actual capture and retained replay feed the mandatory evidence gate"
---

<objective>
Validate and acquire real configuration and complete tool-schema provenance.

Purpose: A hash proves a file stayed unchanged; the gate must also prove it is the actual host configuration and complete tool definitions.
Output: The artifacts listed below; no implementation or passing qualification is claimed by this plan.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SCOPE-AMENDMENT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-VERIFICATION.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP-EXECUTION-REVIEW.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP2-PLANNING.md
</context>

## Execution boundaries and reversibility

D-01–D-10 refer to the existing SYNTHESIS labels for unnumbered CONTEXT decisions, not new owner decisions. Follow CONTEXT and SCOPE-AMENDMENT unchanged. Existing Go ExecutionBinding, reservation/bind/terminal journal and authoritative finalizer remain the only work/credit authority. Keep the native/subprocess distinction and public $ant-* names. Read the current source and prior summaries; old line numbers are navigational hints.

Before any execution, verify the root's protected-file inventory. Preserve all Plans 01–16, summaries, original reports and all evidence/gap-closure receipts byte-for-byte. New raw runs use a unique absolute external directory recorded as AETHER_GAP2_EVIDENCE_ROOT; new checked-in receipts use evidence/gap-closure-2. Plan23 alone archives the current canonical verification before its authorized refresh; no old archive is replaced. Keep the active branch, unrelated changes, shared installation/hub/configuration/homes, CalVault and Phase205 unchanged. No publication, login, fresh credentials or owner walkthrough. Existing authorized auth may be used only by the established disposable fixture; unavailable auth stays incomplete.

Reversibility gates are enabled. Changes are scoped reversible commits; no shared store migration, reset, blanket cleanup, process kill or installation. Before expensive commands measure storage and pass the appropriate isolated preflight. Never repurpose the parent HOME or CODEX_HOME; child environment maps own disposable paths. Retain raw evidence, binaries, manifests and hashes before retiring only demonstrably owned disposable caches. If capacity or host observation cannot be obtained within these bounds, record the measured blocker and stop dependent operations; do not claim completion.

Pin source revision/status, complete admitted production/embedded/test inputs, capture harness, regression contract, Go executable/version/environment, actual candidate binary/build argv, host executable/version/model/effort/configuration/full tool definitions and installed skill/support/agent bytes. The shared installed narrow CalVault hotfix has historical SHA-256 72707dc5d5fa34304c4a4b9455416a440eebad4d22aaede0a373ccf5dc7d8b90; measure it read-only and never relabel it as this candidate. Preserve the old 165db28c/f9e32ca0/cf51e9ac/f8315369/4729f23 identities. Reporting-only commits can cite independently equal inputs but never change a captured binary's identity.

Any production or capture change requires fresh affected live proof and the final full matrix. Any corpus or contract change invalidates focused/full regression credit. Parser-only replay can diagnose immutable original logs with separate capture/validator identities; it cannot replace Plan22's required new final-candidate capture. A fix during Plans22/23 returns through freeze, full matrix and all four current-candidate runs. No EXPECT_INCOMPLETE or weakened selector is acceptance.

Security enforcement, new research and Nyquist scaffolding are disabled by the phase configuration; meaningful focused normal/race controls remain required. No package install, external service integration, ORM migration or UI is introduced. Local patterns and the existing SYNTHESIS are reused. Never claim the previous real-home NPM incident restored: 31 additions, 64 changes and 17 removals retain unestablished prior state.

## Artifacts this phase produces

- cmd/codex_native_gap_evidence_test.go
- cmd/codex_native_gap_host_capture_test.go
- cmd/codex_native_worker_live_test.go
- .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-2/host-provenance.json

<interfaces>
nativeGapHostProvenance contains Version, Model, Args, Executable, ToolSchema and Configuration. nativeGapHostIdentity(p, r) currently verifies identity/hash/original-inventory membership only. codexNativeLiveReceipt holds actual client/model/argv, ConfigPath/ConfigSHA256 and Artifacts; preserve original capture inventory. Add source-owned capture types/helpers in cmd/codex_native_gap_host_capture_test.go and call them from nativeGapHostIdentity and the actual fixture.
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define semantic capture envelopes and reject proof substitution</name>
  <files>cmd/codex_native_gap_evidence_test.go, cmd/codex_native_gap_host_capture_test.go</files>
  <read_first>cmd/codex_native_gap_evidence_test.go; cmd/codex_native_worker_live_test.go; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP-EXECUTION-REVIEW.md</read_first>
  <behavior>Begin TestCodexNativeGapHostCapture with same-hash-content substitution controls, then implement typed semantic validation.</behavior>
  <action>Per D-03/D-04/D-05, replace CR-02's arbitrary positive fixture with minimally valid distinct configuration and tool-schema captures. Define versioned strict JSON envelopes carrying kind, schema version, invocation/session identity, actual executable/version/hash, exact argv, model and effort when reported, capture method, export provenance and original raw artifact references. Configuration must decode measured effective settings and agree with receipt argv/model/effort; absent effort stays unreported rather than defaulted. Tool captures require a nonempty complete definition list with names and valid parameter-schema objects, capture completeness/provenance and supported raw export linkage; the JSON envelope is not itself a host source. Cross-check referenced raw bytes through original Artifacts and the actual same invocation, and reject unknown versions/kinds or incomplete exports. Keep historic schemas readable as history without granting current qualification.</action>
  <acceptance_criteria>Controls reject empty stderr, a prompt, arbitrary bytes, names-only lists, empty/missing parameter schemas, explicitly unavailable export, wrong invocation/model/effort/argv and post-hoc inventory insertion. A valid measured-shape fixture passes; fake fixtures are labeled deterministic only.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexNativeGapHostCapture|TestCodexNativeGapEvidencePaths|TestCodexNativeGapEvidenceReceiptVersions)$' -count=1 -timeout 60s
go test ./cmd -race -run '^(TestCodexNativeGapHostCapture|TestCodexNativeGapEvidencePaths|TestCodexNativeGapEvidenceReceiptVersions)$' -count=1 -timeout 60s</automated></verify>
  <done>Controls reject empty stderr, a prompt, arbitrary bytes, names-only lists, empty/missing parameter schemas, explicitly unavailable export, wrong invocation/model/effort/argv and post-hoc inventory insertion. A valid measured-shape fixture passes; fake fixtures are labeled deterministic only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire supported capture acquisition into an actual host trial</name>
  <files>cmd/codex_native_gap_host_capture_test.go, cmd/codex_native_worker_live_test.go, .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-2/host-provenance.json</files>
  <read_first>cmd/codex_native_worker_live_test.go; cmd/codex_native_gap_host_capture_test.go; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-15-SUMMARY.md; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-VERIFICATION.md</read_first>
  <behavior>A collector's unavailable or failed export produces rejected provenance, while actual supported exports retain raw invocation-bound definitions.</behavior>
  <action>Per D-05/D-08/D-10, inspect the actual installed clients' read-only help/configuration/export surface and existing retained host artifacts to identify a supported complete-schema and effective-config source for Codex and Claude. Record the exact examined command, version, output and limitations. Implement acquisition only through a real supported export or independently recorded actual tool boundary belonging to the invocation; do not scrape model prose, decrypt unavailable fields, change shared settings, substitute observed names or invent parameter schemas. Capture raw source before closing the original artifact inventory, then construct the semantic envelope with source references. Run an ordinary native trial after resource preflight to exercise collector→receipt→semantic gate; preserve a supported Claude export if available, with final actual comparison in Plan22. If a legal capture path does not exist, write an unavailable record with measured reason and stop the dependent provenance acceptance branch; other independent repairs may continue but full qualification cannot pass. Do not retry unchanged unsupported acquisition.</action>
  <acceptance_criteria>Actual collector output is linked to one real invocation and validates against the new envelope, or the unavailable-source gap is explicit and rejects. The collector cannot use a prewritten synthetic envelope or claim all tools from the names seen during a run. Configuration capture exposes no secrets in checked-in receipts.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexNativeGapHostCapture|TestCodexNativeGapEvidencePaths)$' -count=1 -timeout 60s
AETHER_CODEX_NATIVE_LIVE=1 AETHER_CODEX_NATIVE_SCENARIOS=ordinary AETHER_CODEX_NATIVE_EVIDENCE_DIR="$AETHER_GAP2_EVIDENCE_ROOT/host-provenance" go test ./cmd -run '^TestCodexNativeWorkerFreshHost$' -count=1 -json -timeout 30m</automated></verify>
  <done>Actual collector output is linked to one real invocation and validates against the new envelope, or the unavailable-source gap is explicit and rejects. The collector cannot use a prewritten synthetic envelope or claim all tools from the names seen during a run. Configuration capture exposes no secrets in checked-in receipts.</done>
</task>
</tasks>

<verification>
Retain raw exact argv, exit, stdout/stderr and source identities for every executed check. Run behavioral source controls normally and with -race; live runs retain their explicit longer bounds and zero omitted requested cases. Resource or host barriers leave dependent checks unexecuted and gaps explicit. Plan22 supplies the complete frozen live matrix; Plan23 supplies all four final regression runs and the explicit standard verdict. Earlier narrow passes never substitute for those final checks.
</verification>

<success_criteria>
Only versioned semantically valid configuration and full tool definitions captured from the actual invocation can satisfy host provenance.
Empty, unrelated, names-only, missing-schema and explicitly unavailable exports fail provenance even with valid hashes.
Unsupported export capability remains an explicit ANT-07 gap; no synthetic definition or later-written receipt is accepted as actual host output.
Task artifacts and SUMMARY completion do not establish ANT completion. Missing mandatory proof remains a gap.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-18-SUMMARY.md with exact source/evidence identities, task checks and honest remaining gaps. Do not mark pending ANT requirements complete or alter shared trackers; the root owns disposition.
</output>

