---
phase: 204.2-codex-native-worker-lifecycle
plan: "26"
subsystem: testing
tags: [native-workers, cancellation, safe-refusal, immutable-replay]
requires:
  - phase: 204.2-25
    provides: Independently reviewed Task 1 cancellation admission and prospective proof identity
provides:
  - Actual bound-child safe-refusal capture and independent idempotent replay
  - Complete retained authority snapshots and deterministic rejection reporting
affects: [204.2-21, 204.2-22, 204.2-23]
key-files:
  created:
    - evidence/proof-amendment/cancellation-acquisition.json
  modified:
    - cmd/codex_native_gap_cancellation_refusal_test.go
    - cmd/codex_native_cancellation_replay_test.go
    - cmd/codex_native_worker_live_test.go
requirements-completed: []
requirements-addressed: [ANT-05, ANT-07]
completed: 2026-09-19
status: complete
execution: actual-safe-refusal-and-independent-replay
qualification: final-matrix-pending
---

# Plan 26: Actual safe cancellation refusal

Both tasks pass on source `2d7137df17c746be00f7d6a31df57685d607c9e2`. The actual cancellation case and strict independent replay qualify the safe-refusal branch of the unchanged owner-approved amendment.

In plain English: when Aether cannot prove a helper stopped, it refuses unsafe completion or restart. It does not pretend the helper was cancelled or award completion credit.

The real bound child received an actual interruption request. Seven candidate operations exercised cancellation-guarantee admission, staging, pause, resume, duplicate reservation, forced redispatch and adversarial completion. The saved assignment, context and handoff remained unchanged; duplicate reservation did not authorize another launch. Termination remained unknown: `runtime_cancelled`, `no_post_ack_writes` and completion credit all remain false.

Nineteen selected tests passed normally and with race detection. The final recorder correction in `2d7137df` retains binary, nested receipt, checksum, extensionless and long-path authority files under bounded names while preserving exact bytes. Rejection reporting is deterministic. Earlier parser correction `eda9b8d2` accepts the observed literal command/output wrapper without dropping independent command/turn/child correlation. Runtime refusal implementation remains `8d3a5a34`.

The fresh actual live test passed. Its first runner stopped before replay because measured disk space failed admission; that original unsuccessful runner summary remains unchanged. Four additional obsolete owned compiler archives were retired with exact inventories and preserved evidence, then a separate admitted replay passed in 8.81 seconds. Replay derives twice from the original 3,178 indexed artifacts and requires byte-identical results. All original artifact hashes were checked again unchanged. Owned-process and temporary-auth cleanup passed. No shared installation, CalVault state or migration changed.

[Acquisition evidence](evidence/proof-amendment/cancellation-acquisition.json) pins original/derived receipts, candidate/client/source identities, focused checks, both resource retirements and separate runner outcomes. The scoped 10,469,163,008-byte admission and full Plan 23 estimate remain unchanged. Historical incomplete captures were neither repaired retrospectively nor regraded.

Plan 25 full context/answer/ACK work remains incomplete. Plan 21 must rerun cancellation with its final recovery candidate; Plans 22/23 still require all eleven final cases and all four regression runs. This completes Plan 26's actual safe-refusal scope, not Phase 204.2 or any ANT requirement.
