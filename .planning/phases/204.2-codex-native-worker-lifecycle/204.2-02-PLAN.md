---
phase: 204.2-codex-native-worker-lifecycle
plan: "02"
type: execute
wave: 2
depends_on: ["204.2-01"]
files_modified:
  - cmd/codex_native_worker.go
  - cmd/build_worker_run.go
  - cmd/codex_native_worker_test.go
  - cmd/build_execution_binding_test.go
autonomous: true
requirements: [ANT-04, ANT-05]
must_haves:
  truths:
    - "Equal native reservations or terminal results replay the original receipt without writes; conflicting equal-key payloads refuse without changing state or credit."
    - "Empty, stale, wrong-role, wrong-workspace, wrong-child and superseded-attempt requests cannot authorize a helper or save a terminal result."
    - "Runtime execution waves govern admission, and terminal arrival order cannot alter dispatch membership or completed task identity."
    - "A spawn whose child binding is unknown remains unresolved across time and fresh reads; it never grants automatic relaunch permission."
  artifacts:
    - path: cmd/build_worker_run.go
      provides: "Atomic native lifecycle and terminal replay on the existing attempt record"
      contains: "Native"
    - path: cmd/codex_native_worker_test.go
      provides: "Concurrent admission, terminal and ambiguous-launch refusal cases"
      contains: "TestCodexNativeWorkerAdmission"
  key_links:
    - from: cmd/codex_native_worker.go
      to: cmd/build_worker_run.go
      via: "Validation repeated within UpdateJSONAtomically"
      pattern: "UpdateJSONAtomically"
    - from: cmd/build_worker_run.go
      to: cmd/build_attempt.go
      via: "Full saved ExecutionBinding checked at each native mutation"
      pattern: "validateBuildExecutionBinding"
---

<objective>
Make retries and overlapping sessions safe on the working native path. In plain English, the same job cannot accidentally start twice or be credited to a different helper.

Purpose: Close the current journal's PID-only liveness, partial membership and replay gaps for native work, per D-04/D-05/D-09/D-10.
Output: Atomic native admission and terminal behavior, preserving the existing subprocess lane.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/research/ant-codex-parity-2026-09-16/GSD-INTEGRATION.md
</context>

<interfaces>
NEW fields finalized here on codexNativeWorkerBinding: launch_state, host_session_id, child_id, dispatch_sha256, prompt_sha256, permission_profile, workspace_root, source_event_id/source_event_sha256, and context_delivery_ids. Existing buildAttemptWorkerRun.Result and ResultSHA256 remain the terminal payload and canonical digest. Existing process fields apply only when Native is absent.
NEW observe request: exact binding plus actual host observation, source event identity and digest; statuses are running, cancel_requested, cancelled, unavailable, launch_unresolved. Only a confirmed terminal observation/result may become terminal.
After an accepted first state transition, call the existing emitCodexDispatchWorkerStarted/Running/Finished functions with that transition's receipt; do not emit on replay. This gives Plan 06 the existing progress hook without another bridge-controller edit in its parallel wave.
</interfaces>

## Artifacts this phase produces

NEW: native admission/replay/status helpers and observe operation, versioned host observation fields, named race/identity tests.
EXISTING: beginBuildAttemptWorkerRun, recordBuildAttemptWorkerTerminal, buildWorkerRunStillActive and cancellation helper receive only necessary native branches; subprocess behavior remains separately tested.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Refuse duplicate, stale and mismatched native launch claims atomically</name>
  <files>cmd/codex_native_worker.go, cmd/build_worker_run.go, cmd/codex_native_worker_test.go, cmd/build_execution_binding_test.go</files>
  <read_first>204.2-01-SUMMARY.md and actual early host envelope; cmd/build_execution_binding_test.go; cmd/build_worker_run.go; pkg/codex/execution_binding.go; cmd/internal_worker_adapter.go validateInternalWorkerExecutionBinding.</read_first>
  <behavior>Two simultaneous reservations -> exactly one launch permission; same replay -> original receipt and no write; conflicting host child -> refusal; changed manifest/attempt/workspace/owner/name/caste/permission/prompt -> refusal. Empty or unknown dispatch -> refusal. Wrong execution wave -> refusal. Subprocess invocation sees no native reservation as a reusable ordinary provider run.</behavior>
  <action>Complete reserve/bind validation under the existing attempt's atomic update: exact saved assignment and full ExecutionBinding, immutable prompt/dispatch hashes, canonical workspace, role, permission, task coverage, execution wave and one host session/child per reservation. The caller cannot authorize its submitted prompt or environment. Return typed disposition and original immutable receipt for identical replay without changing timestamps, journal bytes or result identity. Check every relevant predicate again under the write lock to close prevalidation races. Native reservations, including unresolved/terminal ones, must block a second native or subprocess launch under the same assignment/attempt; an explicit supported reroute uses the existing new-attempt route after safe reconciliation. Preserve the ordinary adapter's accepted requests and provider execution, and prove the native command never invokes its provider seam. Reject empty dispatches/bindings and partial or unknown wire fields through strict decoding, not generic JSON maps. Add synchronized concurrent tests, with a deterministic pre-write barrier rather than sleeps, plus retained direct-lane regression cases.</action>
  <acceptance_criteria>Concurrent launch count is exactly one; all identity substitutions leave attempt/colony bytes unchanged; identical reserve/bind replays preserve original bytes; current normal Go adapter tests still run their one provider call.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexNativeWorkerAdmission|TestCodexNativeWorkerLaneSeparation|TestPlanOnlyManifestCarriesJournalBoundExecutionIdentity|TestInternalBuildAdapterRejectsStaleRunIdentity|TestTerminalWorkerJournalRejectsChangedWorkspaceIdentity)$' -count=1 -timeout 60s</automated></verify>
  <done>A native helper can start only once for the current exact accepted assignment, including when another session races or the ordinary adapter is invoked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retain every terminal outcome and unresolved launch honestly</name>
  <files>cmd/codex_native_worker.go, cmd/build_worker_run.go, cmd/codex_native_worker_test.go</files>
  <read_first>cmd/build_worker_run.go recordBuildAttemptWorkerTerminal/latestTerminalBuildWorkerRun/cancelBuildAttemptWorkerRuns; cmd/build_attempt_external_test.go append-only partial retry tests; early interruption receipt.</read_first>
  <behavior>Identical completed/failed/blocked/timeout/cancelled terminal response -> original result digest, timestamps and no write. Conflicting terminal or out-of-order running observation -> refusal. No host ID after a spawn attempt, no host connection, or time passing beyond 30 seconds -> unresolved, never automatic cancellation/relaunch. Cancellation request alone -> pending, not terminated.</behavior>
  <action>Normalize a copy of each result before hashing, validate result name/caste/task/child and complete execution identity inside the terminal write, and return the original receipt on byte-equivalent canonical replay before mutating timestamps. Preserve task receipts, changed files, handoff and failure/error details for every terminal status; define confirmed native cancelled as terminal and map it to the existing completion vocabulary at the later aggregate boundary. Unknown remains nonterminal. Implement observe and read-only inspect projections for actual host status and event provenance. Exclude native records from the 30-second/PID death inference and bulk cancellation that currently fabricate ended-process states. A reserved-but-unbound launch stays launch_unresolved until the actual host evidence establishes the same child or an explicit no-launch outcome; a failed tool call without such evidence is ambiguous. A confirmed no-launch may close its reservation without credit, but any new work uses the existing fresh attempt/retry path. Keep terminal failure retries append-only; never overwrite the old worker record or infer completed tasks from edited paths. Reopen stores in tests, permute arrival order, and exercise concurrent terminal writes versus pause/supersession.</action>
  <acceptance_criteria>Every terminal result survives fresh reads; equal replay is byte-stable; conflicts cannot alter credit or results; unknown and cancel_requested never become success/cancelled solely from elapsed time. Native retry requires a new authorized attempt.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexNativeWorker(Terminal|AmbiguousLaunch|Cancellation|ArrivalOrder)$' -count=1 -timeout 60s; go test ./cmd -race -run '^TestCodexNativeWorker(Admission|Terminal|AmbiguousLaunch)$' -count=1 -timeout 60s</automated></verify>
  <done>Finished native work and honest failure evidence remain durable under races and retries, with no extra launch or credit.</done>
</task>
</tasks>

<verification>
Use actual registered command/store boundaries and runtime-produced identities. Preserve rejection before/after inventories. A deterministic host boundary double proves concurrency only; real host behavior remains Plans 01/07 evidence.
</verification>

<success_criteria>
Native records remain replay-safe and retain terminal/nonterminal distinctions. Shared subprocess behavior is preserved and missing host state cannot authorize duplicate work.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-02-SUMMARY.md.
</output>
