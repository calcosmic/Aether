---
phase: 204.2-codex-native-worker-lifecycle
plan: "04"
type: execute
wave: 4
depends_on: ["204.2-03"]
files_modified:
  - cmd/codex_native_decisions.go
  - cmd/codex_native_decisions_test.go
  - cmd/pending_decision.go
  - cmd/handoff_decisions_cmd.go
  - cmd/discuss.go
  - cmd/codex_native_worker.go
  - cmd/codex_native_context.go
  - cmd/command_guide.go
  - .aether/skills/colony/aether-colony-build-cycle/SKILL.md
autonomous: true
requirements: [ANT-06]
must_haves:
  truths:
    - "A native worker's material question reaches the existing owner decision path with its exact goal/session/attempt/child scope."
    - "A wrong-worker, superseded-attempt, changed-goal or already-answered native question cannot accept a new stale answer or relabel it as current."
    - "Only the intended worker receives a native answer, while owner-only checkpoint and reviewer-waiver authorization remains protected."
    - "Native question/answer guidance appears only on Codex; Claude/OpenCode keep their existing wrapper and finalizer instructions."
  artifacts:
    - path: cmd/codex_native_decisions.go
      provides: "Bound native question admission and answer handling"
      contains: "codexNativeDecisionBinding"
    - path: cmd/pending_decision.go
      provides: "Optional native binding in the existing pending-decision store"
      contains: "NativeBinding"
    - path: cmd/codex_native_decisions_test.go
      provides: "Question/answer freshness and scoped delivery refusal tests"
      contains: "TestCodexNativeDecisionScope"
  key_links:
    - from: cmd/handoff_decisions_cmd.go
      to: cmd/codex_native_decisions.go
      via: "decision-answer bound request route, preserving existing protected routes"
      pattern: "Native"
    - from: cmd/codex_native_context.go
      to: cmd/codex_native_decisions.go
      via: "Worker-specific current answer renderer"
      pattern: "renderCodexNative"
---

<objective>
Let a native helper ask a real material question and use the owner's answer safely. In plain English, an answer belongs to the current job and the helper that asked; it cannot become permission for a later or unrelated job.

Purpose: Complete ANT-06's return question path, per D-04/D-05, using existing decision authority.
Output: One bound decision path and tested exact worker delivery.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/research/ant-codex-parity-2026-09-16/GSD-INTEGRATION.md
</context>

<interfaces>
Existing PendingDecision in cmd/pending_decision.go persists under pendingDecisionsFile; recordDecisionAnswer currently stamps current scope and appends a new clarification. NEW NativeBinding *codexNativeDecisionBinding retains goal_hash, session_id, attempt_id, launch_id, worker_name, task_id, child_id and question_id. Existing protected checkpoint/waiver capabilities stay authoritative.
NEW decision-answer --native-request &lt;path&gt; supplies exact binding/question ID and answer; this route resolves the existing row atomically and never uses a stale caller question to create a fresh row.
</interfaces>

## Artifacts this phase produces

NEW: cmd/codex_native_decisions.go/test.go, NativeBinding, native question operation, bound native-request answer route and scoped renderer.
EXISTING: pendingDecisionsFile, decision-answer, handoff-decisions, clarified intent and the protected checkpoint/waiver routes remain one shared decision system. Extend codexNativeBuildCommandGuide from Plan 01; retain TestCodexNativeBuildGuidePlatformIsolation and extend TestCodexNativeDecisionGuideContract over all three platforms.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Save and answer a native question in its exact current scope</name>
  <files>cmd/codex_native_decisions.go, cmd/codex_native_decisions_test.go, cmd/pending_decision.go, cmd/handoff_decisions_cmd.go, cmd/discuss.go</files>
  <read_first>cmd/pending_decision.go; cmd/handoff_decisions_cmd.go recordDecisionAnswer and protected dispatch branches; cmd/discuss.go loadCurrentPendingDecisionScope/pendingDecisionMatchesScope/clarified-intent rendering; cmd/handoff_decisions_cmd_test.go checkpoint/waiver and legacy scope tests.</read_first>
  <behavior>Bound current question -> one durable pending row; identical replay -> same row; same text from different native worker -> distinct bound row. Exact current answer -> one resolution; duplicate identical answer -> original receipt; conflicting/expired/wrong-worker answer -> refusal without feedback/intervention mutation.</behavior>
  <action>Add optional NativeBinding to PendingDecision and implement question admission against the current native assignment before it is stored in the existing pending-decision file. Require both native goal hash and session equality, plus attempt/launch/child/task identity; the general scope helper's session-first compatibility behavior must not allow a changed native goal. Use immutable question IDs, not question-text deduplication, for native rows. Add the --native-request route to existing decision-answer with the strict bounded file loader. Resolve only that already-created native question under the same store lock after a fresh scope check; do not call the old append-only recordDecisionAnswer to mint a replacement row. Preserve checkpoint/waiver routing and refusal rules; native clarification may not impersonate those protected question types. Emit existing feedback/intervention facts only after a first successful answer commit, once. Exclude native rows from generic text-based answered-decision suppression and broad clarified-intent rendering so another worker cannot inherit the answer. Retain legacy unbound compatibility only for non-native rows.</action>
  <acceptance_criteria>Question and answer share an exact durable native scope; stale/duplicate-conflicting requests mutate neither decision nor signal/intervention stores; owner-protected routes retain their negative tests; native answers do not leak into other worker capsules.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexNativeDecisionScope|TestCodexNativeDecisionReplay|TestCodexNativeDecisionProtectedPaths|TestLegacyAnsweredDecisionTextIsScopedToActiveSessionAndGoal|TestDecisionAnswerRejectsCheckpointAuthorizationFailures|TestRecordDecisionAnswerDoesNotResolveCheckpoint)$' -count=1 -timeout 60s</automated></verify>
  <done>A native question is answerable through the existing owner path and cannot be renewed, broadened or reused by stale text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Connect worker questions and current answers to native messages</name>
  <files>cmd/codex_native_worker.go, cmd/codex_native_context.go, cmd/codex_native_decisions_test.go, cmd/command_guide.go, .aether/skills/colony/aether-colony-build-cycle/SKILL.md</files>
  <read_first>Task 1 native decision API; Plan 03 runtime context delivery; native Build Flow and command guide; cmd/codex_dispatch_contract.go handoff records.</read_first>
  <action>Register the native question operation and wire native handoff open_decisions into it before the parent asks a material question. Return the runtime question and exact decision-answer command so the host does not invent approval, substitute an answer, or re-ask an already valid response. Feed the scoped renderer into Plan 03's context route; the host sends its exact delta to the intended child and records the actual delivery acknowledgement. Update only native Build Flow and codexNativeBuildCommandGuide with question, answered, stale, pending and resumed cases, preserving Plan 01's explicit Codex-only branch and unchanged shared Claude/OpenCode catalog per D-04. Extend TestCodexNativeDecisionGuideContract in cmd/codex_native_decisions_test.go to call buildCommandGuide("build", platform) for codex, claude and opencode: Codex requires codex-native-worker question/context and decision-answer --native-request instructions; neither other platform may receive those native operations or the native message-delivery protocol, and each retains its matching wrapper step and exact existing build-finalize RunCommand. Rerun Plan 01's complete platform-isolation test. If a worker is already terminal, preserve its question/handoff and expose the current runtime next decision; never reopen it implicitly or give its answer to an unrelated child. Test the registered question -> decision-answer -> context -> delivery sequence and stale interleavings, checking actual payload bytes. Fixture responses must be labelled harness/predeclared authorization, not owner testimony.</action>
  <acceptance_criteria>The intended child receives the current answer exactly once through a successful native message, wrong children receive none, and required unanswered choices remain pending. Both installed Codex support and the Codex runtime guide name the implemented bound route; three-platform assertions prove native answer instructions do not change Claude/OpenCode wrapper/finalizer guidance.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexNativeDecision(Delivered|StaleInterleaving|GuideContract)|TestCodexNativeBuildGuidePlatformIsolation|TestCodexAntSkillGuidesPreserveOtherPlatforms)$' -count=1 -timeout 60s</automated></verify>
  <done>ANT-06's complete question/answer round trip has a real production caller and exact delivery proof, ready for the live Plan 07 scenario.</done>
</task>
</tasks>

<verification>
Retain existing owner checkpoint/waiver tests. Demonstrate that disconnecting native-row filtering or the scoped delivery call fails the associated test, using a disposable source mutation or genuine RED. Do not treat matched strings in guidance as host delivery proof.
</verification>

<success_criteria>
Material worker questions and answers are current, durable, worker-specific and cannot become stale authority. Native answer guidance remains Codex-only with Claude/OpenCode wrapper/finalizer regressions passing. The real-host round trip is explicitly scheduled in Plan 07.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-04-SUMMARY.md.
</output>
