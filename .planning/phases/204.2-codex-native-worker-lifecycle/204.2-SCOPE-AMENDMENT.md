# Phase 204.2 execution scope amendment — 2026-09-17

The phase remains the same goal and ANT-04–07 requirements. Execution now has nine plans in seven waves: 01; 02; 03+05+06; 04; 07; 09; 08.

The active executor measured roughly 800 added lines in the shared live-file extension before completing the remaining host scenarios. Original Plan 07 combined that work with capability admission, usage handling, platform guidance and a mandatory full final rerun. This exceeded its estimated 45% implementation-context bound, triggering the original plan’s explicit split instruction. The root orchestrator approved this reversible structural adjustment within the already-authorized phase.

- Revised 07 retains original Task 1 in full: one shared matrix, every native scenario and actual Claude comparison, with honest pass/refusal/incomplete evidence.
- New 09 receives original Tasks 2–3, now numbered 1–2: capability/usage contract and guidance, followed by the complete matrix against the exact final committed and installed candidate.
- Existing 08 moves to wave 7 and depends on 09. Its full normal/race accounting, negative controls and explicit phase verification remain required.

After the original eleven-case run, the root adds `cmd/codex_native_worker_live_test.go` to Plan 09's sequential ownership for a confirmed fixture defect. The installed Claude command is the flat `/ant-build` entrypoint, while the original harness requested `/ant:build`, expected the obsolete nested wrapper path, and disabled discovery from its isolated user configuration. The original zero-turn unknown-command receipt remains intact. Plan 09 corrects this setup and checks installed identity before its already-required final comparison. Shared Claude wrappers and user installations remain unchanged. No task or scenario is added or removed.

No scenario, requirement, success criterion, actual-helper proof or final rerun is removed. The eleven current scenario entries include the separate inherited read-only control. No source approval or owner testimony is invented. Shared installation, CalVault and Phase 205 remain outside this engineering execution.

The observed ordinary and initial review runs took 584.73s and 814.44s including harness setup. The aggregate live selection may use a 120-minute overall test timeout when those measured durations exceed the original 60-minute command. Individual host runs remain bounded; exact actual argv and exits must be retained. This is an execution allowance, not a failure waiver or model/profile change.

Original Plan 07 is preserved verbatim at `/Users/callumcowie/.aether-backups/phase204-2-execution-20260917/plan07-original-plan.md` (SHA256 `f95a8a11c512a873ee0d527ef4a2c1085f9c0be2ca07511143cb3827a794b00e`) and in git source `9c705add40375983af2ef36e3b6f018e2dd40c6f`. Original wave 5 worktree manifest, expected base, captures and validator receipts remain unchanged. Root commits this amendment on the orchestrator branch; the active 07 executor reads its updated scope without rebasing or merging its worktree.

Plan 09 preflight found a second concrete installed-guidance defect: the Codex public shim renderer read the shared catalog directly, embedding legacy Task/build-wave, subprocess result staging and worktree instructions in the installed ant-build skill even though its runtime guide/private support had been adapted. Root extends sequential ownership to `cmd/codex_skill_surface.go`, `cmd/codex_skill_surface_test.go` and `cmd/command_guide_test.go` for the minimal adapter wiring and corresponding installed-surface assertions. This completes the existing final-installed-guidance task, adds no task or scenario, and leaves shared Claude/OpenCode wrappers and user installations unchanged. The original wave 6 manifest is preserved before extending only its allowed files; base, branch and worktree identity remain exact. The executor reads this root amendment without merging/rebasing its worktree.

Root also authorized bounded corrections within the already owned shared harness before final capture: explicit `-buildvcs=false` with independent committed-source provenance to avoid outer-root Go VCS stamping in nested worktrees, exact literal read-batch/patch classification only with correlated actual host events and causal negative controls, and the already recorded Claude fixture setup correction. Original captures retain their original identities and incomplete dispositions.
