---
phase: 204.2-codex-native-worker-lifecycle
plan: "11"
subsystem: testing
tags: [codex, context, causal-evidence, native-workers]
requires:
  - phase: 204.2-10
    provides: Correlated native task paths, UUIDs and preserved host captures
provides:
  - Independent post-question fixture challenge and pre-question snapshot
  - Exact host boundary checks with fail-closed plaintext acknowledgement
  - Source-pinned question captures, immutable replays and explicit ANT-06 gaps
affects: [204.2-12, 204.2-15, 204.2-16]
actuals:
  tokens: 24323
  tasks: 2
  commits: 5
tech-stack:
  added: []
  patterns: [Delayed fixture authority, exact-byte host provenance, separately pinned replay]
key-files:
  created:
    - cmd/codex_native_gap_context_test.go
    - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/context.json
  modified:
    - cmd/codex_native_worker_live_test.go
key-decisions:
  - Generate fresh answer material only after an attributed real child question; fixture authorization is never owner testimony.
  - Encrypted exported messages cannot acknowledge exact plaintext delivery.
  - A successful host send and the child's required check remain separate evidence predicates.
  - Preserve original captures and identify later parser-only replay by its own validator commit.
patterns-established:
  - Chronology separates the earlier release message from the post-question answer send without decrypting or guessing payloads.
requirements-completed: []
requirements-addressed: [ANT-06]
coverage:
  - id: context-causality-controls
    description: Exact context, delayed authority and rejection controls
    requirement: ANT-06
    verification:
      - kind: integration
        ref: cmd/codex_native_gap_context_test.go#TestCodexNativeGapContext
        status: pass
    human_judgment: false
  - id: actual-context-delivery
    description: Complete initial context and exclusive scope-bound answer delivery on the actual host
    requirement: ANT-06
    verification:
      - kind: e2e
        ref: evidence/gap-closure/context.json
        status: fail
    human_judgment: false
duration: approximately 45min; end-to-end start was not instrumented
completed: 2026-09-18
status: complete
qualification: incomplete
---

# Phase 204.2 Plan 11 Summary

**Both tasks were executed and their evidence committed. The harness now prevents predeclared-answer contamination, but actual plaintext delivery and subsequent child work remain unproved. ANT-06 is not qualified.**

In plain English: the worker really asked a question before the fixture chose its answer. Aether prepared the right context and the host accepted a message for that worker. The exported message is encrypted, so the checker correctly refuses to claim that its exact contents were delivered or acted upon.

## Task commits

Task 1:

- `a2471b071f3535fc3a58ee862f405e8733c482d2` — independent delayed controller, fresh-child request, context markers, boundary validation and rejecting controls.
- `31804c4f63c70c7a4d6047c7e5480a0d398a8af9` — observed live-fixture corrections: competitive skill matching, actual empty successful send-result shape, and the existing corroborated child-check predicate.
- `5510ecfd52b2452281b8acb580d682fa1cf38740` — parser-only chronological separation of release and answer sends; original captures retained.

Task 2:

- `fe3866a7` — source-pinned context report with both captures, final replay, negative controls and qualification gaps.

This SUMMARY is committed separately with normal hooks. Shared trackers remain the root orchestrator's responsibility.

## What changed

The Go test harness controller now waits for the registered question response and checks its exact child-authored event against the actual child capture. It snapshots pre-question fixture files, public isolated host inputs and history before drawing fresh random challenge bytes. Only afterward does it fill the existing Go-issued bound answer template. The selected answer is absent from the readable coordinator, launch prompt, tests and earlier history; no fallback answer or second decision store was introduced.

The coordinator performs the existing question, `decision-answer --native-request`, context and observed-send acknowledgement operations. It also runs real stale-attempt and wrong-child answer requests and saves unchanged-journal refusals. A read, failed/missing send, altered payload or encrypted payload cannot self-acknowledge an inferred digest. Raw launch/send arguments, successful result records, actual activity and child identities remain linked. The fixture requests `fork_turns=none`; actual host metadata separately records `history_mode=paginated` and native multi-agent version `v2`. Neither field is silently substituted for the other.

Recognizable capsule, matched skill, active steering and prior-handoff facts now appear in the actual Go-issued prompt. The skill fixture declares the actual build workflow, Clamp task vocabulary and Go module detection input so it survives installed-skill competition. Its prior handoff is transparently fixture-prepared, not invented earlier worker activity.

The harness-owned panic behavior checker remains distinct from the child's required uncached Go check. Merely printing or reading the challenge is not credited as a child check. Production Go interfaces, the decision store, ExecutionBinding and immutable terminal authority were not changed.

## Verification

The final source passed **65 cases across six top-level tests normally, and the same 65 with `-race`, with zero skips**. Both runs used `-count=1 -json -timeout 60s`. The selector was:

```
^(TestCodexNativeGapContext|TestCodexNativeContextExactBytes|TestCodexNativeContextBounds|TestCodexNativeDecisionDelivered|TestCodexNativeDecisionStaleInterleaving|TestCodexNativeQualificationCoordinator)$
```

This includes the plan's exact five-test selection plus coordinator syntax coverage. Controls reject prompt/history/fixture leakage, escaped JSON leakage, changed whitespace/Unicode, wrong child, failed or missing send results, inspection-only acknowledgements, stale answers, duplicate results, inherited context, unavailable plaintext and missing provenance. Required empty briefs refuse; exact 2 MiB requests pass while one byte above refuses. Scope refusals leave journals unchanged.

Earlier expanded verification passed **133 normal + 133 race cases across 13 top-level tests**, covering retained context delivery/binding/read-only, decision scope, attribution and coordinator boundaries. Subsequent changes received the narrower final checks above. No new broad repository suite ran; these checks provide no full-regression credit.

The actual historical final165 question receipt was rejected by the new delayed-authority predicate using a read-only external Go overlay. Its SHA-256 remained `2f2ef903450f6f39de3a2b322875919d66ed87e47a51b092b10003885a15d618`. This is rejection of known contaminated input, not requalification of that capture.

Both new raw captures were replayed twice with identical derived results under final validator `5510ecfd`. All **2,372 artifact entries** (1,186 per capture) verified against their original receipt inventories. Replay exit 0 establishes integrity of incomplete evidence, not live success.

## Actual live outcomes

Both live invocations used the required question-only test, `-count=1 -json -timeout 30m`, and the unchanged **900-second per-host bound**. Both outer Go tests exited **1**, both actual host processes exited **0**, and neither scenario skipped. Both source trees stayed clean and frozen during capture.

| Capture | Source | Host duration | Outcome |
|---|---|---|---|
| `native-question-4020214274` | `a2471b07` | 245.115 seconds | Incomplete. Genuine child question and delayed answer; invalid answer routes refused. Fixture skill was not selected, empty host send output needed parser support, and one parent `head -35` read remained unclassified. Original evidence preserved. |
| `native-question-2984286597` | `31804c4f` | 253.368 seconds | Incomplete. All four context markers issued; actual child question, delayed answer and completed child-bound send linked. Encrypted launch/send plaintext prevents ACK; no child edit, required check, terminal result, aggregate or credit proved. No unclassified parent commands. |

The follow-up child was `01a0b184-5005-7853-a090-016df7482fc9`, task path `/root/forge_63`. Its question preceded challenge selection. The independent pre-question scan retained **388 files** and found neither the chosen answer nor its unpredictable challenge bytes. The Go-issued envelope contains that answer for the same child. Both stale-attempt and wrong-child answer calls returned exit 1 and preserved all **131 captured journal files** byte-for-byte.

Final parser replay distinguishes the earlier release send from the later answer send by the independently recorded selection time. The actual answer send call/result/child linkage is corroborated, but its exported body remains encrypted. No later parser commit relabels a captured source, binary or original receipt.

## Evidence and identities

Durable root:
`/Users/callumcowie/.aether-evidence/phase-204.2-gap-20260917T210924Z/plan-11`

- `live-invocation.json`, `live-result.json`, `live.jsonl`, `live.stderr`, `context/native-question-4020214274/` — original capture.
- `followup/live-invocation.json`, `followup/live-result.json`, `followup/live.jsonl`, `followup/context/native-question-2984286597/` — corrected clean-source capture.
- Each capture retains `receipt.json`, `source-files.json`, candidate/build provenance, installed inputs, parent/child sessions, coordination requests/results, controller challenge and immutable pre-question snapshot.
- `parser-final/focused-verification.json` — final normal/race commands, source hashes, environment overrides and raw-log hashes.
- `final-focused/`, `committed-focused/`, `task1-final/` — earlier passing focused generations. Failed fixture checks and their raw output remain separately retained.
- `historical-rejection/` — actual final165 rejection and unchanged original hash.
- `replay-current/` — pre-chronology replay; `replay-final/` — final separately pinned validator, raw logs and derived reports for both unchanged captures.
- `final-evidence-manifest.json` — checked-in report hash and artifact/protected-file audit.

Both separately built candidates have SHA-256 `1644af5f44b35b7500f95b3ec99d428317303672abb8966ed2273a8e09b0b470`; their capture sources remain distinct. Production inventory digest is `sha256:08cf994909e034ec397b454fcd848f0bdc7277fd2d5d13d3ef1191040eacdf0c`. Builds used `-buildvcs=false` and version `1.0.79`. Host: Codex CLI `0.154.0`, `gpt-6-astra`, medium effort. Go: `go1.26.5 darwin/arm64`.

The follow-up inventory pins 835 test files, aggregate SHA-256 `b2c9ab02a19a821ecd45abf02061ca490828bcb5af8762c9ab1453a4e595cbff`. The later parser change is separately pinned by its verification manifest and replay source. It does not inherit prior full-suite credit. Exact candidate/client/build/installed hashes and invocation arguments are in `evidence/gap-closure/context.json` and linked raw receipts. The host did not expose a complete tool-schema export; observed call/result ABI and host metadata are retained without inventing a full schema identity.

## Deviations and issues

- **Observed fixture defect:** a role-only marker skill passed an empty-hub test but lost under installed competition. Added the real workflow/task/module matching inputs and a competing-skill regression fixture, then performed a new live capture. Intermediate missing-directory, missing-capsule and task-mismatch fixture failures remain in raw logs; none earned passing credit.
- **Observed host result shape:** the real completed send exports an empty string, not necessarily `{}`. Added that exact successful result shape while retaining unique call/result/activity/child correlation and all failure controls.
- **Parser-only correction:** encrypted release and answer messages initially looked ambiguous. Final replay uses the independent post-question answer-selection time to exclude the earlier release; duplicate later sends still refuse. Both original captures remain unchanged.
- The first local check encountered a denied shared Go build-cache path. All subsequent checks used the task-owned `/tmp/aether-plan11-gocache`; no shared cache permission or installation was changed.
- Missing plaintext is an observed external evidence limitation, not permission to invent an ACK or infer child behavior. No login or new authentication was attempted; both disposable authentication copies were removed.

## Outstanding obligations

ANT-06 remains pending: independently corroboratable launch and answer plaintext, successful exact-payload ACK, actual answer-dependent child behavior, the child's required check and durable completion are not proved. The prepared fixture is not live planning or owner testimony. Plan 15 owns the frozen complete live matrix; Plan 16 owns final normal/race regression accounting and the explicit goal-backward verdict. Phase 205 remains unchanged and incomplete at its existing owner boundary.

## Boundaries and self-check

Expected base `e340d73d4aefd14dc444a9ae0942108bfed15294` was verified. All **92 protected historical files** match the root start manifest. Only the three declared plan files and this SUMMARY changed. No shared STATE.md, ROADMAP.md or REQUIREMENTS.md edits, original-checkout/branch changes, shared installs/hub/home/config changes, publication, owner walkthrough or Phase 205 edits occurred. Unrelated `.gsd/` was not touched.

## Self-Check: PASSED

All declared artifacts and task commits exist; final focused normal/race checks passed; both immutable captures and their replay inventories verified; protected history and shared trackers are unchanged. This structural execution close-out does **not** mark ANT-06 or the phase qualified.
