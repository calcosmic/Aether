---
phase: 204.2-codex-native-worker-lifecycle
plan: "12"
subsystem: testing
tags: [codex, native-workers, recovery, live-evidence]
requires:
  - phase: 204.2-11
    provides: Correlated native identities and the current host evidence harness
provides:
  - Controller-observed interruption checkpoints and fresh public-resume evidence
  - Immutable completed-worker and finalizer replay checks with rejecting controls
  - Four source-pinned captures with honest recovery qualification dispositions
affects: [204.2-14, 204.2-15, 204.2-16]
actuals:
  tasks: 2
  commits: 5
tech-stack:
  added: []
  patterns: [Observed interruption cut points, immutable capture replay, separate recovery and qualification gates]
key-files:
  created:
    - cmd/codex_native_gap_recovery_test.go
    - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/recovery.json
  modified:
    - cmd/codex_native_worker_live_test.go
key-decisions:
  - Only observed spawn/terminal events plus matching durable state permit the controller to interrupt its owned parent.
  - Recovery of a corroborated terminal may be exercised while independent attribution gaps remain qualification failures.
  - Parser-only comparison recognition is revalidated against preserved raw captures under a separately pinned validator.
requirements-completed: []
requirements-addressed: [ANT-04, ANT-05]
coverage:
  - id: recovery-controls
    description: Deterministic recovery, stale identity, ambiguous launch and finalizer replay controls
    verification:
      - kind: integration
        ref: cmd/codex_native_gap_recovery_test.go#TestCodexNativeGapRecovery
        status: pass
    human_judgment: false
  - id: early-recovery
    description: Actual early interruption followed by fresh public resume and unchanged exactly-once accounting
    verification:
      - kind: e2e
        ref: evidence/gap-closure/recovery.json#native-early-resume-1508094691
        status: pass
    human_judgment: false
  - id: complete-recovery-qualification
    description: Complete partial recovery and same-child ambiguous-launch reconnection
    verification:
      - kind: e2e
        ref: evidence/gap-closure/recovery.json
        status: fail
    human_judgment: true
    rationale: Additional compliant actual-host evidence is required; no owner walkthrough or approval substitutes for the missing proof.
duration: approximately 45min; 39min from first recorded verification to reporting
completed: 2026-09-18
status: complete
qualification: incomplete
---

# Phase 204.2 Plan 12: Native recovery evidence Summary

**Both tasks are executed and reported. Early recovery passes separately pinned raw replay; partial recovery retains useful continuity evidence but remains qualification-incomplete, and spawn-before-bind safely remains unresolved.**

In plain English: a new parent can keep a finished worker's saved work without doing it twice. The real partial run also finished the remaining job, but its unclassified operations prevent full qualification. An uncertain launch stayed blocked instead of creating a duplicate worker. ANT-04 and ANT-05 remain pending phase-wide proof.

## Performance

- Initial reading began after this isolated working copy was prepared; that exact start was not instrumented.
- First recorded verification: 2026-09-17T22:53:55.788482Z; reporting: approximately 23:33Z (2026-09-18 Europe/Zurich).
- Tasks executed: **2**. Declared deliverables plus SUMMARY: **4 files**.
- Three-case live matrix: 23:01:30.909984–23:18:36.278919Z, exit **1**.
- Separately identified early follow-up: 23:22:11.365656–23:27:19.595995Z, original live exit **1**.
- No broad repository suite, shared installation, publication, authentication setup or owner walkthrough was run.

## Task Commits

Task 1, each correction committed with normal hooks:

1. `cecdcb52fe89ef7fa3aff64cf89cd3a878a14696` — observed checkpoints, controller interruption, exact fresh public resume, preserved worker/source and finalizer bytes, and rejecting recovery controls.
2. `3dbafa426d46308601152835d645021cd9b3e8e3` — separate early recovery eligibility from independent attribution qualification; new live capture required and performed.
3. `480d5df8b55027801ab428d21b527d023b371f85` — parser-only recognition of exact `diff -s` comparisons of the coordinator's two retained finalizer state/attempt files, with rejection tests and separately pinned replay.

Task 2:

4. `3ea3e714711c0052cc72a02409b973b82f370b6b` — source-pinned recovery report, raw evidence references, original outcomes and separate final-validator dispositions.

This SUMMARY is the fifth scoped commit. Shared trackers belong to the root orchestrator and were not edited.

## Accomplishments

- The controller requires durable journal state and actual thread-attributed coordination/spawn/child events before stopping its owned parent. It records parent PID/session, child and spawn-call identity, checkpoint snapshots, hashes, successful termination and completed wait. A natural exit cannot masquerade as controller interruption.
- Early resume requires public `aether resume` in a distinct host and no replacement spawn. Partial resume requires the exact completed worker entry/source to survive while precisely one never-started accepted assignment runs.
- Spawn-before-bind observes a real child before interruption, preserves the ambiguous reservation and requires unchanged journal/no result/no replacement in the fresh host. It makes no reconnection claim.
- Repeated actual finalization preserves both colony-state bytes and whole attempt-journal bytes. Deterministic arrival-order controls exercise both permutations through finalization and stable replay.
- Unreadable child evidence, unknown launch, stale parent/run/attempt, non-first or wrong public resume, altered checkpoint bytes, unsafe comparisons, wrong working directory and modified coordinator all remain rejecting controls.

## Actual Outcomes

| Capture | Original live disposition | Final separately pinned interpretation |
|---|---|---|
| `native-early-resume-1606833398` | Incomplete. Real terminal checkpoint and controller stop occurred; attribution gate prevented the fresh leg. | Incomplete, retained unchanged. |
| `native-partial-resume-799225095` | Incomplete qualification. Fresh public resume reported one finished and one unfinished assignment; exactly one new helper ran, completed worker/source stayed unchanged, both tasks received credit and finalization replay preserved bytes. | Still incomplete: arbitrary parent Python and unclassified child operations remain rejected. Useful continuity observations are retained, not upgraded to full qualification. |
| `native-spawn-gap-756264381` | Observed safety result. Actual spawn preceded bind; fresh public resume retained one unresolved launch, no replacement or credit, and byte-identical journal. | Observed no-redispatch safety; same-child reconnection/binding remains **unproved** and the corresponding qualification stays incomplete. |
| `native-early-resume-1508094691` | Incomplete under capture validator: only the fresh parent's two actual `diff -s` finalizer comparisons were unclassified. Real child checks, controlled interruption, distinct public-resume-first host, no replacement, preserved worker/source, actual credit and stable replay were recorded. | **Passed bounded early recovery** under validator `480d5df8`, replayed twice from unchanged inventoried raw bytes. Original live exit 1 and original receipt remain unchanged. This is not complete phase/ANT qualification. |

A successful replay test means integrity and repeatability, not automatic live success. The final four replay invocations all exit 0, while their derived outcomes remain two incomplete original captures, one observed unresolved safety case and one passed early follow-up.

## Exact Source and Evidence

External root: `/Users/callumcowie/.aether-evidence/phase-204.2-gap-20260917T210924Z/plan-12`.

- Expected base verified: `9e9f0379a54096126babb4cbcb8ef3bf2b416fe0`; retained branch `worktree-agent-p2042-12-1789685309`.
- First matrix capture source: `cecdcb52`; early follow-up capture source: `3dbafa42`; final parser validator: `480d5df8`. No source identity was relabeled.
- Production input digest for all four captures: `sha256:08cf994909e034ec397b454fcd848f0bdc7277fd2d5d13d3ef1191040eacdf0c`.
- Candidate binary for all four captures: `sha256:a9d30a3950bca67e7f9f566be61f95c003e424b2d3eefc28fef4c3462eb51ead`, version 1.0.79, built from this worktree with `-buildvcs=false` and the exact version ldflag. This is not the installed narrow CalVault hotfix.
- Go **1.26.5**, darwin/arm64; Codex CLI **0.154.0**, **gpt-6-astra/xhigh**. Exact executable/argv/model, installed skill/support/agent bytes and observed call/result/event shapes are retained. A complete independent tool-schema export was unavailable; no broader capability guarantee follows.
- `candidate-start.json`, `followup-early/candidate-start.json`, and `validator-source.json` pin clean source identities, Go environment, inventories and all 836 test files. Final validator production bytes match the first capture; test/harness bytes differ and have separate identities.
- `recovery/` retains all original matrix captures; `followup-early/recovery/` retains the new early capture. `replay/` preserves the prior interpretations; `replay-final/` preserves final-validator outputs, run manifests and raw test logs. Neither directory overwrites originals.
- `live-run.json` and `followup-early/live-run.json` preserve exact live argv, environment, exit and log hashes. Both use the plan's FreshHost test and 60-minute outer bound, with unchanged per-host 15-minute original / 10-minute fresh-parent bounds.
- `final-integrity.json` verifies **4,781 inventoried artifact entries**, zero mismatches. All **92 protected files** match the root execution-start manifest, including historical Plans01–09/reports/evidence and Phase205.
- `post-run-boundaries.json` records no matching surviving fixture processes and no retained `auth.json`. All four temporary auth copies were removed. Shared executable/config/version pins stayed unchanged across both live invocations.
- Checked-in report: `evidence/gap-closure/recovery.json`, SHA-256 `4e12a148c06d3352ec185838f09c7e65badc7942f9105b750021b28f45fda4a3`.

## Verification

The exact plan Task 1 selector passed initially. After the final parser correction, the following focused selector passed **8 top-level / 55 total cases**, zero failures/skips, both normally and with `-race`:

```sh
go test ./cmd -run '^(TestCodexNativeGapRecovery|TestCodexNativeRecoveryPartialReady|TestCodexNativeRecoveryUnresolved|TestCodexNativeWorkerArrivalOrder|TestCodexNativeRecoveryResumeTransaction|TestCodexNativeQualificationCoordinator|TestCodexNativeGapParentCoordination|TestCodexNativeReadOnlyBatchEventLinkage)$' -count=1 -json -timeout 60s
# Same invocation with -race also passed.
```

Raw final checks: `task1-parser-normal.{json,jsonl}` and `task1-parser-race.{json,jsonl}`. Earlier focused harness coverage passed 142 cases normally and with race detection; later edits were rechecked through their affected controls. All intermediate invocations, source pins and exits remain retained.

The required three-scenario live command ran fully with `AETHER_CODEX_NATIVE_LIVE=1`, selection `early-resume,partial-resume,spawn-gap`, evidence `<root>/recovery`, and `go test ./cmd -run '^TestCodexNativeWorkerFreshHost$' -count=1 -json -timeout 60m`. Exit 1 is preserved. The collection correction received a separate early-only live run; the parser-only correction received separate raw replay, not invented new host evidence.

## Deviations and Issues Encountered

1. **Observed collection gate defect:** the first early parent reached a real saved-terminal checkpoint, but a child clock call and parent code-mode operation prevented complete attribution. The gate incorrectly suppressed the recovery leg for that already accepted terminal. The minimal correction permits collecting recovery while final qualification still rejects missing attribution. It received a fresh run identity and did not modify the original prompt/run.
2. **Observed parser gap:** final early evidence was otherwise complete but two ordinary `diff -s` comparisons were unknown. The correction only accepts the two exact retained finalizer filenames in the hash-verified coordinator directory. Extra flags, other paths, mixed snapshots, wrong cwd, shell additions and changed coordinator bytes are rejected. Existing unique actual-event corroboration remains mandatory. Original receipt and earlier replay remain intact.
3. **Partial-host behavior remains outside the proof contract:** arbitrary Python and child shell/code batches remain unclassified. They were not broadly whitelisted or called successful qualification.

## Outstanding Qualification and Next Readiness

- ANT-04 and ANT-05 remain pending. The standard `requirements-completed` field is intentionally empty because execution/report completion does not satisfy missing mandatory host proof.
- Partial recovery needs a fully attributable capture; useful completed work and stable accounting are not enough to erase the rejected operations.
- Ambiguous-spawn reconnection to the same actual child remains unproved. Safe blocking is not successful reconnection.
- The **ANT-05 unclassified edge probe remains unresolved**, even if the three concrete scenarios later all pass.
- Encrypted launch plaintext remains unproved; this plan does not certify complete context delivery or ANT-06.
- Plan14 can consume the updated harness. Plan15 owns the final frozen complete matrix; Plan16 owns the final complete normal/race regression and goal-backward verdict. Changed test corpus invalidates any claim to reuse older full-run credit.
- Shared trackers, original checkout, branches, installed CLI/hub/homes/config, historical reports and Phase205 were left untouched. No new auth/login, publication or owner walkthrough occurred.

## User Setup Required

None. No owner action or new authentication was requested.

## Self-Check: PASSED

All declared deliverables and this SUMMARY exist. The four task/correction commits listed above exist and contain only owned files. Final focused normal/race tests passed; raw capture and replay identities, 4,781 artifact entries, 92 protected files, no retained temporary auth, and no matching owned fixture processes were checked. The source diff is confined to the three plan-declared files plus SUMMARY; shared trackers and original evidence are unchanged. SUMMARY is committed before final narration.

**This self-check establishes completed execution and honest reporting, not completed qualification. Partial attribution, ambiguous-child reconnection, the unclassified ANT-05 probe and final phase-wide gates remain outstanding.**
