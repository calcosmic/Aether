---
phase: 204.2-codex-native-worker-lifecycle
plan: "19"
type: execute
wave: 16
depends_on: ["204.2-18"]
files_modified:
  - cmd/codex_native_gap_controls_test.go
  - cmd/codex_native_gap_recovery_test.go
  - cmd/codex_native_worker_live_test.go
autonomous: true
gap_closure: true
requirements: ["ANT-05","ANT-07"]
reversibility_gate: true
must_haves:
  truths:
    - "Any nonblank malformed, truncated, unknown or unattributable activity prevents cancellation absence-of-write proof."
    - "Cancellation requires a real child-correlated terminal acknowledgement and a validated end of the observation interval after it."
    - "Retained recovery replay decodes the exact required snapshots and independently proves terminal-before-stage or spawn-before-bind for the saved run/attempt/child."
  artifacts:
    - path: cmd/codex_native_gap_controls_test.go
      provides: "Executable fail-closed qualification contract and controls"
    - path: cmd/codex_native_gap_recovery_test.go
      provides: "Executable fail-closed qualification contract and controls"
    - path: cmd/codex_native_worker_live_test.go
      provides: "Executable fail-closed qualification contract and controls"
  key_links:
    - from: cmd/codex_native_gap_controls_test.go
      to: cmd/codex_native_worker_live_test.go
      via: "Actual capture and retained replay feed the mandatory evidence gate"
---

<objective>
Make cancellation intervals and retained interruption boundaries independently replayable.

Purpose: Incomplete logs and hash-valid but wrong snapshots must never turn into proof that a worker stopped or recovery is safe.
Output: The artifacts listed below; no implementation or passing qualification is claimed by this plan.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SCOPE-AMENDMENT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-VERIFICATION.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP-EXECUTION-REVIEW.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP2-PLANNING.md
</context>

## Execution boundaries and reversibility

D-01–D-10 refer to the existing SYNTHESIS labels for unnumbered CONTEXT decisions, not new owner decisions. Follow CONTEXT and SCOPE-AMENDMENT unchanged. Existing Go ExecutionBinding, reservation/bind/terminal journal and authoritative finalizer remain the only work/credit authority. Keep the native/subprocess distinction and public $ant-* names. Read the current source and prior summaries; old line numbers are navigational hints.

Before any execution, verify the root's protected-file inventory. Preserve all Plans 01–16, summaries, original reports and all evidence/gap-closure receipts byte-for-byte. New raw runs use a unique absolute external directory recorded as AETHER_GAP2_EVIDENCE_ROOT; new checked-in receipts use evidence/gap-closure-2. Plan23 alone archives the current canonical verification before its authorized refresh; no old archive is replaced. Keep the active branch, unrelated changes, shared installation/hub/configuration/homes, CalVault and Phase205 unchanged. No publication, login, fresh credentials or owner walkthrough. Existing authorized auth may be used only by the established disposable fixture; unavailable auth stays incomplete.

Reversibility gates are enabled. Changes are scoped reversible commits; no shared store migration, reset, blanket cleanup, process kill or installation. Before expensive commands measure storage and pass the appropriate isolated preflight. Never repurpose the parent HOME or CODEX_HOME; child environment maps own disposable paths. Retain raw evidence, binaries, manifests and hashes before retiring only demonstrably owned disposable caches. If capacity or host observation cannot be obtained within these bounds, record the measured blocker and stop dependent operations; do not claim completion.

Pin source revision/status, complete admitted production/embedded/test inputs, capture harness, regression contract, Go executable/version/environment, actual candidate binary/build argv, host executable/version/model/effort/configuration/full tool definitions and installed skill/support/agent bytes. The shared installed narrow CalVault hotfix has historical SHA-256 72707dc5d5fa34304c4a4b9455416a440eebad4d22aaede0a373ccf5dc7d8b90; measure it read-only and never relabel it as this candidate. Preserve the old 165db28c/f9e32ca0/cf51e9ac/f8315369/4729f23 identities. Reporting-only commits can cite independently equal inputs but never change a captured binary's identity.

Any production or capture change requires fresh affected live proof and the final full matrix. Any corpus or contract change invalidates focused/full regression credit. Parser-only replay can diagnose immutable original logs with separate capture/validator identities; it cannot replace Plan22's required new final-candidate capture. A fix during Plans22/23 returns through freeze, full matrix and all four current-candidate runs. No EXPECT_INCOMPLETE or weakened selector is acceptance.

Security enforcement, new research and Nyquist scaffolding are disabled by the phase configuration; meaningful focused normal/race controls remain required. No package install, external service integration, ORM migration or UI is introduced. Local patterns and the existing SYNTHESIS are reused. Never claim the previous real-home NPM incident restored: 31 additions, 64 changes and 17 removals retain unestablished prior state.

## Artifacts this phase produces

- cmd/codex_native_gap_controls_test.go
- cmd/codex_native_gap_recovery_test.go
- cmd/codex_native_worker_live_test.go

<interfaces>
nativeGapCancellationFacts(parentRaw, childRaw, parent, child, observations) derives nativeGapCancellationEvidence; nativeGapCollectCancellation feeds nativeValidateInterruptedHostScenario. nativeGapCheckpointRetained feeds nativeGapParentExitAccepted. nativeGapRunInterruptedParent snapshots checkpoint/{parent-events.jsonl,attempt.json,child-events.jsonl,clamp.go,state.json,inventory.json}. nativeGapCheckpointReady uses buildAttemptRecord, PlanManifest/WorkerRuns and saved native terminal state. Reuse those model types and colony state decoding, not a second state model.
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Reject incomplete cancellation streams and bind a real interval end</name>
  <files>cmd/codex_native_gap_controls_test.go, cmd/codex_native_worker_live_test.go</files>
  <read_first>cmd/codex_native_gap_controls_test.go; cmd/codex_native_worker_live_test.go; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-14-SUMMARY.md; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP-EXECUTION-REVIEW.md</read_first>
  <behavior>Malformed record after valid post-ACK evidence must fail before repair; post-ACK activity and unknown coverage can never count as no-write.</behavior>
  <action>Per D-04/D-05/D-10, reproduce CR-03 by appending a truncated FileChange to the existing passing fixture. Parse every nonblank relevant parent/child record fail-closed; malformed JSON, unknown activity shapes, missing timestamps or unattributable calls make the interval unproved, not ignored. Replace observedAfter as the completeness surrogate with an explicit recorded observation-end event/export boundary whose actual collector provenance, child identity, timestamp ordering and raw hash are validated after the cancelled acknowledgement. Do not treat parent process exit, previous_status=running, interrupted or a timer alone as terminal cancelled. Wire added boundary facts through capture and replay without changing old receipt bytes or synthesizing an event. Add bounded valid/no-write and after-ack write fixtures plus truncated final record, malformed middle record, unknown activity, wrong child, absent/earlier end and missing terminal controls.</action>
  <acceptance_criteria>Every corrupted/incomplete stream leaves Qualified and NoPostAckWrites false with an actionable gap. Positive qualification requires complete attributed bytes through the validated end and actual cancelled terminal/runtime linkage. Request-only interruption stays nonterminal.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexNativeGapControls$' -count=1 -timeout 60s
go test ./cmd -race -run '^TestCodexNativeGapControls$' -count=1 -timeout 60s</automated></verify>
  <done>Every corrupted/incomplete stream leaves Qualified and NoPostAckWrites false with an actionable gap. Positive qualification requires complete attributed bytes through the validated end and actual cancelled terminal/runtime linkage. Request-only interruption stays nonterminal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-derive saved recovery cut points from required snapshots</name>
  <files>cmd/codex_native_gap_recovery_test.go, cmd/codex_native_worker_live_test.go</files>
  <read_first>cmd/codex_native_gap_recovery_test.go; cmd/build_attempt.go; cmd/build_worker_run.go; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-12-SUMMARY.md; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP-EXECUTION-REVIEW.md</read_first>
  <behavior>Valid minimal early/partial/spawn-gap snapshots qualify; changing saved identity, completion/credit state or missing required member invalidates the retained cut point even when every hash matches.</behavior>
  <action>Per D-04/D-05/D-09, fix WR-01 by requiring exactly the semantically required checkpoint members rather than len(Inventory)>=4. Decode saved attempt and colony state; correlate run/attempt/manifest/launch/worker/parent/child identity and captured source with the raw spawn/terminal events. Re-derive early/partial terminal-before-stage: saved completed worker result and source event match, no aggregate CompletionPath and no credit/finalization at cut; partial has exactly the intended unfinished accepted assignment. Re-derive spawn-gap: the genuine child exists but saved reservation has no child binding/result, no aggregate/credit and no replacement. Validate checkpoint timestamps/order and successful owned-parent kill/wait independently; live readiness booleans cannot waive replay semantics. Replace literal proof attempt fixture with valid typed snapshots, including state and source inventory, then mutate wrong state/run/attempt/launch/source, already-finalized/credited state, absent snapshot and wrong scenario. Keep no-reconnect claims honest.</action>
  <acceptance_criteria>Hash-valid arbitrary attempt bytes, absent state and semantically wrong snapshots all reject parent-exit acceptance. Both valid cut-point fixtures independently replay; unchanged original captures may now be incomplete but are never rewritten.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexNativeGapRecovery$' -count=1 -timeout 60s
go test ./cmd -race -run '^TestCodexNativeGapRecovery$' -count=1 -timeout 60s</automated></verify>
  <done>Hash-valid arbitrary attempt bytes, absent state and semantically wrong snapshots all reject parent-exit acceptance. Both valid cut-point fixtures independently replay; unchanged original captures may now be incomplete but are never rewritten.</done>
</task>
</tasks>

<verification>
Retain raw exact argv, exit, stdout/stderr and source identities for every executed check. Run behavioral source controls normally and with -race; live runs retain their explicit longer bounds and zero omitted requested cases. Resource or host barriers leave dependent checks unexecuted and gaps explicit. Plan22 supplies the complete frozen live matrix; Plan23 supplies all four final regression runs and the explicit standard verdict. Earlier narrow passes never substitute for those final checks.
</verification>

<success_criteria>
Any nonblank malformed, truncated, unknown or unattributable activity prevents cancellation absence-of-write proof.
Cancellation requires a real child-correlated terminal acknowledgement and a validated end of the observation interval after it.
Retained recovery replay decodes the exact required snapshots and independently proves terminal-before-stage or spawn-before-bind for the saved run/attempt/child.
Task artifacts and SUMMARY completion do not establish ANT completion. Missing mandatory proof remains a gap.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-19-SUMMARY.md with exact source/evidence identities, task checks and honest remaining gaps. Do not mark pending ANT requirements complete or alter shared trackers; the root owns disposition.
</output>

