# Phase 204.2 — PLAN validation

## VERIFICATION PASSED

**Review type:** Pre-execution PLAN validation, not phase implementation VERIFICATION.
**Phase:** 204.2 — Codex Native Worker Lifecycle
**Plans checked:** 8; 17 tasks; 6 waves. Revision 1 rechecked Plans 01, 04, 07 and SYNTHESIS against the initial findings.
**Source reviewed:** `1540f255009e51f6cf774de265180c766715a58e`
**Open issues:** 0 BLOCKER, 0 WARNING
**Revision Gate:** Initial review found 1 BLOCKER and 1 WARNING; both resolved in revision 1.
**Date:** 2026-09-16

The revised plans cover the promised native helper lifecycle and close both initial findings. In plain English: Codex's new helper instructions now have an explicit boundary that protects Claude/OpenCode, and the final qualification work has a bounded workload without losing any required scenarios. This approves the plans for execution; it does not establish that native helpers or recovery already work.

## Initial finding resolution history

### RESOLVED — Initial BLOCKER: platform-specific instructions could escape into shared guides

**Affected plans:** 204.2-01 Task 1; subsequent guide edits in 204.2-04 Task 2 and 204.2-07 Task 3.

The initial plans changed the shared build guide without specifying platform selection or checking the other two returned guides. The current source confirms that `buildCommandGuide` reads one `commandGuideCatalog`, then calls `adaptCommandGuideDefinitionForPlatform` (`cmd/command_guide.go:94–104`). The existing adapter changes the first skill-loading instruction and public spelling, while deliberately leaving executable commands unchanged (`122–141`).

Plan 01 now specifies `codexNativeBuildCommandGuide(def commandGuideDefinition) commandGuideDefinition`, selected only for `command == "build" && platform == "codex"` before the adapter's current early return. It explicitly copies `PreSteps`, `PostSteps` and `DriftGuards` before modification and leaves the shared Claude/OpenCode catalog route intact. This is a concrete, executable placement in the current call path.

`TestCodexNativeBuildGuidePlatformIsolation` is planned in the owned native test file from Task 01.1. It inspects the actual returned guides for all three platforms, including Intent, PreSteps, RunCommand, PostSteps, DriftGuards and RawBypass. Codex must require the native handshake and sole native launcher. Claude/OpenCode must exclude native bridge/launcher operations and retain their matching wrapper, visible Task/subagent dispatch, spawn-log/spawn-complete, completion-stage and exact existing finalizer command. Alternating call order checks shared-state stability. Plan 01 also requires genuine RED or a disposable source mutation to show the test fails when native selection disappears or leaks across platforms. The existing `TestCodexAntSkillGuidesPreserveOtherPlatforms` supplies an independent baseline for the wrapper and exact RunCommand assertions.

Task 04.2 extends only that Codex helper and adds three-platform question/answer assertions. Task 07.3 retains those tests and extends isolation checks through native messaging, reconciliation and public partial-resume guidance on the final candidate. The unchanged shared routes satisfy AGENTS.md's maintained primary-platform boundary without an unrelated wrapper rewrite. SYNTHESIS records the same design. The initial BLOCKER is closed at plan level.

### RESOLVED — Initial WARNING: final qualification plan exceeded the file-count sizing target

**Affected plan:** 204.2-07 `files_modified` and task grouping.

Plan 07 still lists 11 files, but now explicitly distinguishes eight authored code/test/guidance files from three generated JSON receipts. Its three sequential tasks reuse the early runner and fixture/launch/capture/receipt helpers through one table-driven scenario matrix. The overlapping deterministic test file is counted once. The stated context estimate is 20% + 15% + 10%, approximately 45%; raw event streams stay on the evidence root and only bounded summaries enter working context.

All scenarios remain: ordinary Builder/checks, explicit independent Watcher, one completed/one unfinished job, fresh-session public resume, scoped answer delivery, cancellation, spawn/bind ambiguity, permission/workspace/nesting probes, stale/mismatched result refusals, unavailable-host or missing-skill control, and the semantic Claude comparison. Task 07.3 still reruns the complete matrix after final production changes, preserving earlier source-pinned receipts. Concrete scope excess triggers a plan split rather than dropped scenarios. This meets the initial review's accepted bounded-sizing resolution; the WARNING is closed.

## Coverage retained from the initial complete review

| Requirement | Implementing plans | Concrete coverage |
|---|---|---|
| ANT-04 | 01, 02, 05, 07, 08 | Runtime reservation; sole native launcher; attempt/assignment/role/workspace/child binding; stale/conflicting replay rejection; journal-derived aggregate and existing Go finalizer |
| ANT-05 | 01, 02, 05, 06, 07, 08 | Terminal-before-aggregate record; unknown launch/cancel states; partial fresh-session public recovery; finished work and exactly-once credit |
| ANT-06 | 03, 04, 07, 08 | Actual capsule/brief/skill/steering/handoff bytes; current answer deduplication; bound question/answer route; delivered-message evidence and stale-answer refusal |
| ANT-07 | 06, 07, 08 | Actual child activity; real permission/workspace/nesting/cancellation observations; refused unavailable controls and absent usage; candidate-specific evidence gate |

The phase goal is to connect real native Codex helpers to Go-authorized assignments, complete context, durable results and interruption-safe recovery. The five roadmap success criteria each have implementation and actual-host evidence tasks. Ordinary Builder-plus-checks and explicitly requested independent Watcher proof are separate scenarios. PROJECT.md adds no unmapped requirement owned by this phase. Revision 1 does not remove or narrow this coverage.

## Other review conclusions retained and checked for revision impact

- **Task completeness:** All 17 tasks carry files, read-first inputs, action, acceptance criteria, automated verification and done. Plan 01's tracer is intentional, executable vertical-slice work, not a placeholder.
- **Dependencies:** Valid graph: 01 → 02 → {03, 05, 06}; 03 → 04; {04, 05, 06} → 07 → 08. No same-wave file ownership overlap or identified undeclared mutable-resource dependency.
- **Early experiment:** Existing `stageBuildAttemptCompletionFromWorkerRuns` already projects plan-only worker journals through the bound finalizer. Plan 01 explicitly owns baseline reservation/binding/terminal checks and does not require the later Plan 05 hardening to run its one-worker proof. Plan 03 extracts and strengthens its prompt assembly.
- **Native versus subprocess:** The Go bridge never launches; native host spawn is the one launcher. Existing subprocess behavior remains separately identified. The canonical Watcher currently uses `workspace_write` with behavioral restrictions, so independent Watcher proof does not inherently require unsupported per-child read-only enforcement.
- **Recovery and contracts:** Runtime-owned assignments and original prompt identity remain immutable. Normalized terminal copies feed one saved result digest; native source events extend provenance rather than replace execution binding. Unknown spawn/cancellation is nonterminal, and elapsed time never licenses a duplicate launch. Plan 06 supplies recovery facts; Plan 07 supplies installed-build/public-resume wiring and real proof.
- **Context compliance:** SYNTHESIS D-01 through D-10 faithfully label the existing unnumbered CONTEXT constraints; no new owner decisions are implied. Recruitment/worktree/Swarm overhaul, all-command parity, candidate handoff and personal owner acceptance remain in their assigned later phases.
- **Verification derivation:** Actual child edits/checks, returned Watcher findings, delivered context and durable finalizer credit are required; parent prose, exit zero, subprocess substitutes and skipped live tests do not qualify. Relevant changed candidate inputs invalidate affected proof. Plan 08 explicitly produces standard `204.2-VERIFICATION.md`; this plan review cannot replace it.
- **Regression accounting:** Plan 05 repairs the directly relevant completion-schema failure. Plan 08 compares normal/race discovered and executed tests with the named 17 historical failures, without enlarging a waiver list or waiving directly relevant failures.
- **Unclassified assumptions/prohibitions:** ANT-05/ANT-07 assumptions remain explicit. The two transparency prohibitions require attributable evidence review; they do not manufacture an owner-only engineering checkpoint. Missing mandatory real-helper/review/resume evidence cannot become a passed requirement.
- **Pattern compliance:** The plans use the mapped controller, store, context, finalizer, recovery and live-evidence analogs. Optional candidate filenames in PATTERNS are not mandatory rewrites.
- **Research resolution:** Imported research has no unresolved `Open Questions` section. Actual-host uncertainties are explicitly assigned to the early tracer and later measured scenarios, with incomplete outcomes preserved.
- **Dimension 7c:** SKIPPED (no Architectural Responsibility Map section).
- **Dimension 8:** SKIPPED (`workflow.nyquist_validation=false`).
- **Project instructions:** AGENTS.md, applicable CLAUDE Definition of Done/shared-lane constraints and the sole project skill index were considered in the initial review. This recheck reread AGENTS.md, RTK preferences and `.agents/skills/to-prd/SKILL.md`; the PRD skill is unrelated and was not invoked. The revised platform boundary resolves the only initial project-instruction finding.

## Plan sizing

| Plan | Tasks | Files | Wave | Finding |
|---|---:|---:|---:|---|
| 01 | 2 | 7 | 1 | Valid; platform boundary and regression planned |
| 02 | 2 | 4 | 2 | Within bounds |
| 03 | 2 | 4 | 3 | Within bounds |
| 04 | 2 | 9 | 4 | Valid; question guidance preserves platform boundary |
| 05 | 2 | 7 | 3 | Within bounds |
| 06 | 2 | 5 | 3 | Within bounds |
| 07 | 3 | 11 | 5 | Valid; 8 authored files + 3 receipts, bounded runner reuse |
| 08 | 2 | 3 | 6 | Within bounds |

## Revision 1 checks

- Read the prior report, CONTEXT, revised Plans 01/04/07 and SYNTHESIS completely; inspect only the relevant current command-guide adapter, shared build entry and existing primary-platform assertions.
- Independently repeated `frontmatter.validate --schema plan` and `verify.plan-structure` through the installed GSD tool for each of the three changed plans: all six checks returned valid, with no missing/invalid frontmatter and no structure errors or warnings.
- Changed plans retain their requirements, waves, dependencies and file ownership. The graph remains 01 → 02 → {03, 05, 06}; 03 → 04; {04, 05, 06} → 07 → 08. The revisions introduce no new same-wave ownership conflict or shared data transformation.
- Guide isolation is required from the early tracer onward, not deferred to final qualification. Tests are specified against returned behavior and have a failure control. Their implementation and successful execution remain future work.
- Preserved the early live experiment, final-candidate rerun, candidate invalidation after later repairs, Go authority, explicit standard verification and owner-evidence boundaries.

## Residual execution assumptions

Actual host reconnection, accessible terminal events and per-child capability enforcement remain unknown until the early experiment and final measured scenarios run. Unsupported controls must refuse, and unavailable mandatory Builder/Watcher/resume evidence must leave the corresponding criterion incomplete. The context budget is an execution estimate, not a measured guarantee; the explicit split trigger preserves scope if that estimate proves wrong.

The ANT-05/ANT-07 unclassified assumptions and the two descriptor-less transparency prohibition records remain flagged for attributable review during standard verification. This PLAN validation does not mark those records passed, present parent/subprocess work as native-helper proof, or replace the owner's Phase 205 acceptance.

## Structured open issues

```yaml
issues: []
```

Revision Gate passed after one revision. The plans may proceed to `$gsd-execute-phase 204.2`. No application execution, runtime suites, live experiments, source/state edits, commits, installation or publication occurred in this review. Only this PLAN validation report was written; no phase VERIFICATION.md was created.
