---
phase: 204.2-codex-native-worker-lifecycle
plan: "10"
subsystem: testing
tags: [codex, native-workers, attribution, evidence]
requires:
  - phase: 204.2-08
    provides: Native qualification fixture and preserved historical receipts
provides:
  - Causal native task-path to UUID resolution and rejection controls
  - Source-pinned ordinary, early-resume and requested-review live tracer outcomes
  - Separate immutable-capture replays and explicit qualification gaps
affects: [204.2-15, 204.2-16]
actuals:
  tokens: 48423
  tasks: 2
  commits: 6
tech-stack:
  added: []
  patterns: [Causal host evidence correlation, separately pinned parser replay]
key-files:
  created:
    - cmd/codex_native_gap_attribution_test.go
    - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/tracer.json
  modified:
    - cmd/codex_native_worker_live_test.go
key-decisions:
  - Preserve native task path and actual UUID separately; only unique correlated host events establish identity.
  - Failed rejection events prove refusal only, never successful worker work.
  - Preserve all original captures; parser corrections produce separately source-pinned derived receipts.
  - Missing mandatory proof remains incomplete; no requirement or phase completion is asserted.
patterns-established:
  - Exact host-tagged installed skill bytes can prove delivery; arbitrary user prose cannot.
requirements-completed: []
requirements-addressed: [ANT-04, ANT-05, ANT-07]
coverage:
  - id: native-attribution-controls
    description: Causal child mapping and command/evidence rejection controls
    verification:
      - kind: unit
        ref: cmd/codex_native_gap_attribution_test.go
        status: pass
    human_judgment: false
  - id: provisional-live-tracers
    description: Ordinary, early-resume and explicitly requested independent review qualification
    verification:
      - kind: e2e
        ref: evidence/gap-closure/tracer.json
        status: fail
    human_judgment: false
duration: approximately 56min
completed: 2026-09-18
status: complete
qualification: incomplete
---

# Phase 204.2 Plan 10 Summary

**Worker identities now correlate to actual native host evidence. Both plan tasks were executed and their results preserved, but the live qualification remains incomplete.**

In plain English: the checker can now tell which real worker belongs to a named task. The live runs show actual worker edits, review findings and recovery activity, while retaining the evidence gaps that prevent claiming the whole workflow is proved.

## Task commits

Task 1, scoped harness and causal controls:

- `8b499bf3d1e3e9814aa7be2651674c64eafcf102` — correlate native task paths with host UUIDs; preserve runtime launch ID and ExecutionBinding.
- `cc3313a5bdf013185493e9127730cb4c8ece3869` — derive identities before coordination classification; preserve a previously proved uncached check only across an exact corroborated later successful check.
- `a919e593d5914db79d67a48c5ded06df1eef1ffe` — recognize exact uniquely tagged installed skill delivery.
- `0ebefc92` — recognize uniquely recorded failed refusal operations; duplicate, wrong-thread/cwd, wrong-operation and zero-exit controls reject.

Task 2:

- `b975b175` — commit provisional live tracer report, source identities, separate replay results and outstanding gaps.

This SUMMARY is committed separately with normal hooks. No shared trackers were edited.

## Verification

Final scoped normal and race runs each passed **154 cases across 16 top-level tests**, with no failures or skips. Exact argv, environment overrides, source hashes and stdout hashes are in `final3-focused-verification.json` under the external root below. Each uses `-count=1 -timeout 60s`; the race run adds `-race`. The selector covers `TestCodexNativeGap.*`, WorkerAdmission, EvidenceDerivation, FixtureCoordinatorBoundary, ReadOnlyBatchEventLinkage, CodeModeSequenceBoundary, AuxiliaryWatcherLifecycle, ThirdReviewCodeModeOutcome and LiteralPatchBoundary. No broad suite ran.

The original historical task-path mismatch was reproduced from immutable Plan09 ordinary evidence. Causal negative controls preceded the repairs. Historical receipt/session hashes and the original evidence remain unchanged. The final refusal control also has its failing pre-repair output preserved as `plan10-refusal-red.jsonl`.

All four final raw-capture replays exited zero: inventory hashes verified and two independent derivations were byte-identical. This proves replay integrity, not live qualification. The final validator is `0ebefc92`; capture sources remain their original commits.

## Actual live outcomes

The first invocation used committed `8b499bf3`, a clean frozen source tree, scenarios `ordinary,early-resume,review`, and the plan's exact 60-minute outer test bound. It exited 1 after 1362.378 seconds. Per-host limits stayed 900 seconds; the fresh-parent limit stayed 600 seconds. Candidate builds explicitly used `-buildvcs=false`.

| Capture | Observed evidence | Qualification |
|---|---|---|
| Ordinary `native-ordinary-3739933189` | Correlated Builder, edit, uncached check on corrected replay, terminal, runtime credit and stable finalizer replay | Incomplete: actual host exit 101, empty stderr and unfinished command event; cause unproved |
| Early-resume `native-early-resume-3867516012` | Correlated Builder and terminal before aggregate | Incomplete: old check parser prevented the fresh-parent leg; original failed trial preserved |
| Review `native-review-3270563361` | Distinct Builder Brick-46 and Watcher Keen-6, actual review findings, terminals and runtime credit | Incomplete: Watcher review-ledger writes remain unclassified, so required worker check/terminal attribution is not fully proved |
| Follow-up `native-early-resume-2268955952` | Different fresh parent; preserved worker result; no replacement spawn; runtime credit and byte-stable repeated finalization | Incomplete: extra parent `rg` inspection commands remain unclassified |

The targeted early-resume follow-up captured committed `a919e593`, again clean and frozen, with unchanged 900/600-second host bounds and a 30-minute outer bound. It exited 1 after 310.024 seconds. Both actual parents exited zero. Original parent `01a0b15d-96de-7aa3-b455-dfef7b737265` and fresh parent `01a0b161-6a6e-7093-a4b0-dadd7c890955` shared the preserved Builder result from child `01a0b160-241f-7301-9f31-e2b1f1e0558b`.

Independent Watcher evidence includes five uncached Clamp cases (below/above/interior/inclusive endpoints), successful build/vet, and concrete missing coverage for negative/equal bounds, integer extremes and reversed bounds. Findings report correct ordered-bound comparisons, no arithmetic overflow path and constant time/space; no blocking defect was identified. Useful findings do not erase the unclassified-write gap.

## Evidence and exact identities

External root:
`/Users/callumcowie/.aether-evidence/phase-204.2-gap-20260917T210924Z/plan-10`

- `live-invocation.json`, `live-result.json`, `live.jsonl`, `live.stderr`: first three-case run.
- `early-resume-followup/`: independently pinned follow-up invocation, result and raw fixture.
- `tracer/native-*/receipt.json` and `early-resume-followup/tracer/native-*/receipt.json`: original receipts, raw parent/child logs, journals, source patches, binaries and installed artifact inventories.
- `replay/`, `replay-final/`, `replay-closed/`: separately retained parser generations; `replay-closed-manifest.json` pins the final four replay operations.
- `task1-verification.json`, `parser-verification.json`, `final-focused-verification.json`, `final3-focused-verification.json`: staged focused checks and exact source hashes.
- `final-evidence-manifest.json`: checked-in report hash and original/derived receipt hashes.

The checked-in `evidence/gap-closure/tracer.json` embeds exact source/corpus/harness identities, build argv, binary and client hashes, client model/arguments, per-case outcomes, worker identities, raw artifact references and verification evidence. It deliberately separates capture source from validator source. Production inventory digest across captures is `sha256:ce0e931e9aa2b03f231120689e4ec313ac5de981e233edc291d2bd9de3f178fd`; production code did not change. Host was Codex CLI 0.154.0 (`sha256:4f85982624b3898c8991cb80c0981b2aa71070e3537046c9a95950318a95afcc`), model gpt-6-astra; Go was go1.26.5 darwin/arm64. Full installed-byte inventories remain in hashed external receipts.

## Deviations and unresolved obligations

Narrow parser-only corrections were necessary after real captures exposed host-selected skill delivery, failed refusal status and prior uncached-check handling. Each has causal controls; originals were replayed into new files with separately pinned validators. The early-resume follow-up was necessary to execute the previously blocked fresh-parent leg. No timeout was increased and no arbitrary command whitelist was added.

Exact launch prompt plaintext remains host-encrypted in exports; matching is not weakened or inferred from prose. Ordinary host exit 101, review write classification and follow-up parent inspection classification remain outstanding. Plan15 owns the frozen live matrix; Plan16 owns final complete normal/race regression and goal-backward verdict. Changed test corpus invalidates earlier full-suite credit. No ANT requirement or phase is declared qualified by this report.

## Boundaries

Expected base verified: `65dafe3e41618b6c26f8f98b0c29672d33406938`. All 92 root-manifest protected historical/shared files still match their original hashes. Only the three declared plan files and this SUMMARY changed. No original checkout/branch changes, shared installs/hubs/homes/config edits, new auth/login, Phase205 edits, publication or owner walkthrough occurred. Disposable auth copies were removed by the fixture; raw evidence was retained. Existing unrelated `.gsd/` was left alone.

## Self-Check: PASSED

Orchestrator spot-check confirmed all four declared artifacts and task/summary commits exist, the executor returned successfully, and its working tree was clean. Focused normal/race checks and raw replay integrity are recorded above. This structural self-check does not qualify the incomplete live scenarios.
