# Plan 204.2-24 blocked — 2026-09-18

**Status: blocked, incomplete; ready_for_adapter=false.** Task 1 discovery is committed as `71eba9c1`. Task 2 has a truthful blocked acquisition receipt, not successful live acquisition. No `204.2-24-SUMMARY.md` exists, so execution will not skip this unmet prerequisite.

In plain English: we found more precise places to look for worker messages and completion states, but neither installed host has yet supplied all the required evidence. Running the same worker tests again cannot fill missing fields in the host's export.

## What was established

The [acquisition matrix](204.2-ACQUISITION.md) records three candidate mechanisms for each host and twelve separate evidence obligations. Codex 0.155.0 generated its installed app-server schema; nullable collaborative prompt fields and interrupted states are real protocol definitions, not actual worker evidence. A standalone prompt renderer emitted a benign marker after correcting a missing disposable directory; it launched no model or worker and establishes no child delivery. Hash-verified historical Codex 0.154.0 rollout bytes retain encrypted spawn/send bodies and partial settings. Claude 2.1.272 supports promising forwarded-child-text and prompt-snapshot options, but neither is a demonstrated full request export.

The [machine receipt](evidence/gap-closure-3/acquisition.json) retains discovery-start source identity, actual executable identities, 348 raw/discovery artifact hashes, twelve dispositions, resource measurements, and the blocked conjunction. All 909 protected file hashes and 2,347 recorded source input hashes matched at receipt creation. Installed stable Aether 1.0.81 still has SHA-256 `b37f56f6f5c9800483df8e6ea702b5002c9bee7eaff0300bc02830b8b96479f3`.

Raw source, generated schemas, downloaded official references, exact discovery argv/environment/exits and original outputs remain under `/private/tmp/aether-gap3-plan24-htx7w6cb`. They are discovery or explicitly historical artifacts, not a new qualification corpus. No model-worker probe, compilation, resource admission, broad suite or runtime/capture code change was performed. No live timeout or elapsed interval is used as proof.

## Concrete blockers and smallest alternatives

1. **Missing full request provenance:** no examined source demonstrates complete effective settings and every model-facing tool definition for the same actual invocation on both hosts. On-disk config, tool names, API schemas and standalone prompt rendering do not meet it.
2. **Missing cancellation coverage:** no examined source attests the complete attributed no-write interval through an explicit observation end after the actual child's cancelled acknowledgement. Interrupted turn or completed response alone does not meet it.
3. **Undemonstrated partial mappings:** actual byte-exact context, delayed scoped answer plus child ACK, and the required Claude comparison remain unestablished. Optional app-server prompt/terminal fields and forwarded Claude child text warrant a later bounded probe only within an admitted, source-grounded path.

The smallest unchanged-requirement path is a **vendor-supported same-invocation export** with full settings/tools and an explicit complete write-observation boundary. Supply its documented mechanism/version, then resume Plan 24 to demonstrate it on a disposable invocation.

The alternative is an **explicit scoped architecture/proof-contract decision**: choose how to obtain complete observation while preserving the existing native launcher and Go's authorization/result authority, or formally revise the requested proof. A replacement app-server control plane, interception, client patching or weaker proof is not authorized by this execution. These are decisions for subsequent planning, not changes made here. Repeating just prompt/terminal probes would clarify partial capability but cannot by itself release Plan 25.

## Continuation and preservation

Resume **Plan 24**, not Plan 25, after that dependency changes. Execution order remains 24 → 25 → 26 → 21 → 22 → 23. No requirement is completed; ANT-04/05/06/07 and canonical `gaps_found` remain unchanged. No SUMMARY, completion counter, phase advance, publication, login, shared-home change, CalVault action or Phase 205 owner acceptance is claimed.

Verification: JSON parsing, independent raw hash recomputation, obligation/source membership, exact 12-row readiness conjunction, source/protected hash comparisons and `git diff --check`. These checks validate this blocked record, not worker behavior. Their final independent result is retained as external `acquisition-validation.json`.
