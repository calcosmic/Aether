# Plan 24 collector implementation and source feasibility

Measured 2026-09-18. Task 1a: **complete, 19 tests pass**. Task 1b:
**complete with blocked live admission**. Task 2 and Plan 24 remain incomplete.

The recorder now works offline. It can retain supplied files and detect changed
evidence. It cannot establish facts a host does not export. No live model probe,
accepted source adapter, qualification SUMMARY or dependent plan was executed.

## Delivered offline work

- `scripts/collect-native-host-evidence.py`: explicit fixture-root file import,
  original bytes before parsing, exclusive durable writes, private permissions,
  per-run identity, strict JSON/JSONL framing, byte-span indexes and pinned replay.
- `scripts/test_collect_native_host_evidence.py`: 19 tests including CLI capture
  and replay, exact Unicode/whitespace retention, mutation/truncation, inserted or
  omitted files, symlinks, hardlinks, FIFO rejection, concurrent source mutation,
  external references, ambiguous identities, fabricated claims, quotas,
  interruption, disk failure and refusal to reuse a partial capture directory.
- Quotas selected before coding: 16 MiB per file, 64 MiB per run, 128 files,
  10,000 records, 64 JSON levels, 1 MiB manifest. No decompression or schema fetch.
  These are storage limits, not a change to the downstream 2 MiB context limit.
- Verification command: `python3 -m unittest discover -s scripts -p 'test_collect_native_host_evidence.py'`.
  Exit 0; 19 tests, 0.222 seconds. No Go/runtime change warrants a broad suite.

The caller must retain the returned manifest hash independently of the capture
directory. This detects later inventory changes, not authentic host authorship.
All twelve qualification dispositions remain unavailable. The offline core
does not interpret host identities or claim semantic joins; it validates its own
run identity, raw hashes, framing and pointers. Every replay returns
`ready_for_adapter=false`, including source files containing forged positive claims.
CLI exit 0 means successful storage/replay only. Capture errors retain partial
files, and replay cannot silently complete them. There is no live receiver or
accepted vendor decoder. Absolute vendor body references are conservatively
refused by this core; a later admitted adapter must validate any mapping without
altering originals. This is not yet a drop-in Claude export adapter.

## Pinned environment and retained references

Source HEAD: `5f0ee934e7963d5d27e15536ec5147388d8ebcc0`, plus the explicitly
uncommitted collector and planning changes. Both host identities match prior
discovery; do not repeat the earlier six-candidate experiment.

| Executable | Installed version | SHA-256 |
|---|---|---|
| `/Users/callumcowie/.codex-5x/packages/standalone/releases/0.155.0-aarch64-apple-darwin/bin/codex` | 0.155.0 | b0b14f9c1901c1ec44671094b2dc18b39e4bd8d36a6dc2302cc9d961a7e2a197 |
| `/Users/callumcowie/.local/share/claude/versions/2.1.272` | 2.1.272 | 195e24e8e1f9bf46f1eaee72d434a33e18f9f5796f29a6348a00d16c5f8aee75 |
| `/Users/callumcowie/.local/bin/aether` | 1.0.81 | b37f56f6f5c9800483df8e6ea702b5002c9bee7eaff0300bc02830b8b96479f3 |

New documentation-only snapshot directory:
`/Users/callumcowie/.aether-backups/phase204-2-collector/feasibility-a578f816554846209a6584a2812d7379`.
Its `receipt.json` records original downloaded document hashes, current script
hashes and the test result transcribed from execution output. These are not live
worker evidence. Python's certificate store prevented the initial download;
system curl retained the documents with TLS verification enabled.

Sources rechecked: [Codex telemetry](https://learn.chatgpt.com/docs/config-file/config-advanced),
[Codex app-server](https://learn.chatgpt.com/docs/app-server),
[Claude monitoring](https://code.claude.com/docs/en/monitoring-usage).
Existing installed schema and earlier mechanism evidence remain in
`204.2-ACQUISITION.md` and its original external corpus. Current documentation
does not establish that an installed host produces a field until measured.

## Twelve dispositions

Rows concern complete original evidence for each obligation. No row is
demonstrated or qualified. Candidate fields below describe where to look, not
implemented source mappings. Every row still needs protected source origin and
actual parent/child/attempt correlation; a matching hash supplies neither.

| Host / obligation | Disposition | Candidate source / fields and remaining gap |
|---|---|---|
| Codex settings | candidate, incomplete | Telemetry conversation start includes model, reasoning and permission metadata; complete effective invocation settings remain unestablished. |
| Codex tools | unavailable complete source | Tool-result events report activity, not the entire model-facing inventory and definitions. |
| Codex context | candidate, undemonstrated | Prior installed schema: collab prompt and sender/receiver IDs; nullable plaintext lacks actual byte-exact child delivery and ACK. |
| Codex answer/ACK | candidate, undemonstrated | Same collab fields and child messages; no demonstrated current-question/send/child ACK join or ordering. |
| Codex cancelled terminal | candidate, undemonstrated | App-server turn/completed and interrupted status; actual native child plus Go terminal correlation unproved. |
| Codex no-write interval | unavailable | Telemetry batching/shutdown flush and turn completion do not establish complete attributed post-ACK write coverage with an authentic end boundary. |
| Claude settings | candidate, incomplete | Raw request model/system/messages fields cannot alone prove all effective host settings. |
| Claude tools | candidate, version/binding gap | File-mode raw request export may retain full tools. Documented request_body_id and response-index joins require 2.1.274; installed 2.1.272 remains unqualified. |
| Claude context | candidate, undemonstrated | Request body plus forwarded child messages; exact complete payload, intended child and delivery ACK not demonstrated. |
| Claude answer/ACK | candidate, undemonstrated | Prior forwarded parent_tool_use_id candidate; no current-question/answer/ACK ordering proof. |
| Claude cancelled terminal | unavailable demonstrated mapping | Prior hooks/forwarding discovery supplies no actual same-child cancelled terminal plus Go correlation. |
| Claude no-write interval | unavailable | Examined monitoring/hooks contracts do not attest complete post-cancel write coverage through a host-authored observation end. |

Loss/completeness is unestablished for both hosts. The absence of a suitable
contract in these examined sources is an inference about their documented scope,
not a claim that no future vendor export can satisfy it. Request bytes, quiet
logs, collector seals, shutdown flushes and filesystem snapshots remain
insufficient for the missing coverage obligation.

## Admission and next boundary

`live_admitted=false`; `ready_for_adapter=false`. At least both cancellation
coverage sources and Codex's complete invocation tools/settings source are still
missing. Therefore no compilation admission or 15-minute worker probe was run.
The existing acquisition receipt remains byte-for-byte unchanged.

Resume Task 2 only after a version-applicable supported mechanism supplies those
missing contracts and all twelve rows have a documented acquisition path. A
Claude update could change request linkage availability but does not by itself
solve cancellation coverage; no update was performed or assumed. A change to the
proof contract would be a separate scope decision, not collector completion.

CalVault's five runtime fixes are committed in this HEAD but not installed. The
unchanged installed hash above confirms its report. Those fixes, migration
approvals, and Phase 205 acceptance remain separate from collector qualification.
