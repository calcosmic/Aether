# Plan 26 implementation checkpoint

Plan 26 is now complete on `2d7137df`: actual safe-refusal capture and independent replay passed. See [SUMMARY](204.2-26-SUMMARY.md) and [acquisition evidence](evidence/proof-amendment/cancellation-acquisition.json). The checkpoints below retain the earlier progression; phase qualification remains pending.

In plain English: the evidence checker distinguishes “Aether safely refused an unsafe action” from “the worker definitely stopped.” A refusal cannot become cancellation, completion credit or a no-write guarantee.

Seven actual candidate operations are captured: requested cancellation guarantee, stage, pause, resume, duplicate reserve, forced redispatch and completion. Immutable replay binds the exact candidate, request, saved assignment/plan, child identity, original host streams, ordered process outputs and unchanged durable files, including context and handoff. Cached flags cannot supply acceptance. Historical receipts retain their original strict contract. Owned-process cleanup is separate from cancellation proof.

## Focused verification

| Run | Top-level tests | Exit | Seconds |
|---|---:|---:|---:|
| normal-cmd | 14 | 0 | 62.11 |
| normal-pkg-codex | 2 | 0 | 4.00 |
| race-cmd | 14 | 0 | 88.07 |
| race-pkg-codex | 2 | 0 | 6.53 |

All 16 selected tests passed normally and with race detection. Nested tests must pass without skips. The new core includes 22 adversarial controls and known observational host-record shapes; replay controls reject missing original inventory, altered bytes, forged cached flags and retrospective contract changes.

[Exact evidence](evidence/proof-amendment/cancellation-implementation.json) preserves the failed first run and successful retry separately. The first run exposed legacy fixture state; the fixture now saves normal loaded state before creating its actual bound manifest/reservation. The strict validator was unchanged. The same resource guards, explicit source inputs and isolated child environments were retained; no full suite or live host was run by these focused checks.

## Actual-host parser correction

A fresh actual run launched and bound one native Builder, observed a real stop request and retained `cancel_requested` without completion credit. Its original receipt and independent replay remain incomplete: the evidence parser did not recognize the actual sole-command `text(r.output)` presentation wrapper, so the seven runtime guard probes were not admitted.

Commit `eda9b8d2` recognizes only that exact literal-command/output projection and the observed quoted read-only guidance search. Independent command/call/result identity remains mandatory. Nineteen selected tests now pass normally and with race detection, including missing/wrong source events, duplicated calls, rewritten output, extra commands and foreign workspace rejection. [Parser evidence](evidence/proof-amendment/cancellation-parser.json) retains both earlier failed live attempts separately and unchanged. A new actual capture and independent replay are still required for Task 2.

## Actual guards observed; replay capture defect

The next actual run on `eda9b8d2` exercised all seven guards successfully and retained unknown termination, no terminal cancellation and no completion credit. Its live test passed. Independent replay failed because the outer artifact inventory omitted 826 authority snapshot references with binary, checksum, nested receipt or extensionless filenames. Iterating missing references without a stable order also changed the reported failure across identical replays.

Original evidence remains unchanged at `/Users/callumcowie/.aether-backups/phase204-2-collector/plan26-live-parser-20260918T211452Z-v0fitubs/summary.json` (SHA-256 `0f5461c789d2055d097fd579e4d85b67298bcfb6362f1f9304110cf7f7d45e3d`). Both owned-process cleanup and credential cleanup succeeded. No Task 2 success is claimed. A narrow capture fix preserves every authority file under bounded retained names and orders failure reporting; focused verification and a fresh actual capture are required. Adding files retrospectively to the old inventory is not allowed.

## Final capture and replay

The fixed recorder passed 19 focused tests normally and with race detection. A fresh actual cancellation case then passed all seven guards, with 3,178 complete indexed artifacts. Initial replay admission stopped on measured disk space; a separately admitted replay subsequently passed and produced an observed safe-refusal receipt. Original captures and the storage-stopped runner summary remain unchanged. Termination, no-post-ACK-writes and completion credit remain false. No further live run was needed to replay this original capture.

## Remaining phase evidence

Task 25 still needs actual full context/answer/ACK behavior through its prospectively versioned child-fetch route. The basic actual child-output source is demonstrated. Plan 21 recovery and all eleven final scenarios/four regression runs remain required; later source changes require affected recapture.
