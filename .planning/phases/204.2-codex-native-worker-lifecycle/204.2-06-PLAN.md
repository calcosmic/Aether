---
phase: 204.2-codex-native-worker-lifecycle
plan: "06"
type: execute
wave: 3
depends_on: ["204.2-02"]
files_modified:
  - cmd/codex_native_recovery.go
  - cmd/codex_native_recovery_test.go
  - cmd/context.go
  - cmd/session_flow_cmds.go
  - cmd/codex_build_progress.go
autonomous: true
requirements: [ANT-05, ANT-07]
must_haves:
  truths:
    - "Public resume identifies saved finished native workers and unfinished assignments even when no aggregate exists and colony state is still READY."
    - "A reserved but unbound or unreachable child is reported as unresolved; resume cannot force-redispatch it from missing PID data."
    - "Pause/cancellation reports actual host acknowledgement and terminal evidence, while inline activity follows real helper transitions."
  artifacts:
    - path: cmd/codex_native_recovery.go
      provides: "Read-only native recovery facts from the existing journal"
      contains: "buildCodexNativeRecovery"
    - path: cmd/context.go
      provides: "Existing resume dashboard consumes partial native recovery facts"
      contains: "buildCodexNativeRecovery"
    - path: cmd/codex_native_recovery_test.go
      provides: "Fresh-command partial resume and observed cancellation tests"
      contains: "TestCodexNativeRecovery"
  key_links:
    - from: cmd/context.go
      to: cmd/codex_native_recovery.go
      via: "resume_override_command/resume_override_why from actual native facts"
      pattern: "buildCodexNativeRecovery"
    - from: cmd/session_flow_cmds.go
      to: cmd/codex_native_recovery.go
      via: "Pause/resume liveness does not infer native death from missing process"
      pattern: "Native"
---

<objective>
Make pause and resume explain what actually survived an interrupted native build. In plain English, the new chat sees finished work, unfinished jobs and any helper it cannot locate, with one safe next action.

Purpose: Extend existing recovery projection for native facts, per D-04/D-05/D-09.
Output: Partial native resume, honest cancellation and actual inline helper activity.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/research/ant-codex-parity-2026-09-16/GSD-INTEGRATION.md
</context>

<interfaces>
Existing buildResumeDashboardResult accepts resume_override_command/resume_override_why; durable aggregate recovery already returns buildFinalizeRecoveryCommand. NEW buildCodexNativeRecovery is read-only and returns finished/unfinished/unresolved identities, actual liveness facts and one exact safe command. It does not grant dispatch or credit. Plan 02's observe command already stores native host observations; shared subprocess liveness remains unchanged.
For all-terminal/no-aggregate recovery, return the explicit codex-native-worker stage action introduced by Plan 01. Never stage as a side effect of inspect or context. The stage handler uses only retained results and the existing stageBuildAttemptCompletionFromWorkerRuns function.
</interfaces>

## Artifacts this phase produces

NEW: cmd/codex_native_recovery.go/test.go and native recovery projection.
EXISTING: public pause/resume, dashboard, process activity gates and emitCodexDispatchWorkerStarted/Running/Finished are wired to observed native events.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Recover one finished helper while preserving unfinished assignments</name>
  <files>cmd/codex_native_recovery.go, cmd/codex_native_recovery_test.go, cmd/context.go, cmd/session_flow_cmds.go</files>
  <read_first>cmd/context.go buildResumeDashboardResult; cmd/session_flow_cmds.go pause/resume and activity checks; cmd/build_attempt_external_test.go durable reread recovery; Plan 02 journal and inspect states.</read_first>
  <behavior>READY plan-only attempt with one terminal and one undispatched job -> completed/unfinished distinction and no redispatch of terminal work. All terminals but no aggregate -> stage exact saved results; durable aggregate -> finalize exact path. Unbound launch -> unresolved reconciliation. Read-only inspection -> byte-identical store.</behavior>
  <action>Add buildCodexNativeRecovery over the saved attempt and current workspace binding. Prioritize native terminal/partial/unresolved facts before subprocess PID recovery, including plan-only attempts whose colony is READY. Return finished helper IDs/result digests, unfinished dispatch IDs, unresolved launch IDs and a safe runtime action. If an aggregate exists, retain the exact existing finalize command. With all terminal records and no aggregate, route to native inspect/record recovery that stages only saved records; with one unfinished worker, the host can reserve only that never-started current assignment. Reserved/bound unknown work requires reconciliation with the same real host child or its authentic saved terminal event; absence of reconnect support stays incomplete. Integrate these facts into the existing public resume and pause-safe-boundary checks. Do not create a new recovery authority, mint a new attempt from inspection, or automatically force-build an uncertain native attempt. Exercise fresh store/command invocations and assert saved terminal result bytes and credit unchanged.</action>
  <acceptance_criteria>Resume distinguishes completed, never-started and unresolved native work before aggregate completion, exposes one registered next action, and never relaunches or credits finished work. Missing host evidence remains visibly incomplete.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexNativeRecovery(PartialReady|AllTerminal|Aggregate|Unresolved|ReadOnly)$' -count=1 -timeout 60s</automated></verify>
  <done>A fresh session can safely resume partial native work from the existing durable journal without depending on a subprocess PID.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reflect observed native activity and confirmed cancellation inline</name>
  <files>cmd/codex_native_recovery.go, cmd/codex_native_recovery_test.go, cmd/session_flow_cmds.go, cmd/codex_build_progress.go</files>
  <read_first>cmd/codex_build_progress.go emitCodexDispatchWorkerStarted/Running/Finished and updateCodexBuildDispatchRuntimeStatus; Plan 02 observe states; Phase 203 context inline-only activity constraint.</read_first>
  <action>Project actual native bind/running/terminal observation changes through the existing progress functions and session activity gates. Show a reserved launch as awaiting binding, not already working. A pause/cancel request asks the host to invoke its actual interruption/cancellation tool against the bound child; until the matching host terminal acknowledgement is recorded, show cancellation pending or capability unavailable, never completed/cancelled. Preserve any already accepted result and changed files. Repeated status renders/observe replay cannot emit repeated started/finished events or mutate attempt truth; reconnect uses the same identities. Keep liveness inline in the user's working session, using current visual writers and meaningful names rather than requiring a second terminal. Test observer replay, terminal-followed-by-running rejection, host unavailable, cancellation pending/acknowledged and a pause arriving during terminal record; keep subprocess pause/resume regressions intact.</action>
  <acceptance_criteria>Every visible native start/finish corresponds to a bound real observation; missing PID or elapsed time never produces a fabricated cancellation. Cancellation pending and unknown are legible inline, and event replay does not duplicate activity.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexNativeRecovery(Activity|CancelPending|CancelConfirmed|PauseRace|ProcessLane)$' -count=1 -timeout 60s; go test ./cmd -race -run '^TestCodexNativeRecovery(PauseRace|Activity)$' -count=1 -timeout 60s</automated></verify>
  <done>Pause/resume and activity share truthful native lifecycle facts, ready for actual host interruption/cancellation proof in Plan 07.</done>
</task>
</tasks>

<verification>
Retain a flagged unresolved ANT-05/unclassified probe assumption: these concrete tests cover the specified interruption states, but the generic taxonomy did not classify all recovery edges. Plan 08 must explicitly assess that assumption against real Plan 07 captures; no automatic green from a skipped host test.
</verification>

<success_criteria>
Public recovery preserves finished work, names unfinished/unknown work and refuses duplicate dispatch; inline activity and cancellation are grounded in actual host observations.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-06-SUMMARY.md.
</output>
