---
phase: 204.2-codex-native-worker-lifecycle
plan: "18"
subsystem: testing
tags: [go, provenance, host-capture, fail-closed]
requires:
  - phase: 204.2-17
    provides: Resumed isolated normal/race admission and ordinary native worker proof
provides:
  - Strict versioned configuration and tool-definition semantic contracts
  - Rejection of unrelated hashed bytes and unsupported source substitution
  - Original-inventory unavailable acquisition records for actual invocations
affects: [204.2-22, 204.2-23, ANT-07]
tech-stack:
  added: []
  patterns: [offline JSON Schema compilation, immutable capture inventory, separate behavior and provenance verdicts]
key-files:
  created:
    - cmd/codex_native_gap_host_capture_test.go
    - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-2/host-provenance.json
  modified:
    - cmd/codex_native_gap_evidence_test.go
    - cmd/codex_native_worker_live_test.go
key-decisions:
  - A normalized envelope cannot authorize its own acquisition method.
  - No actual-source adapter is admitted without an established supported invocation-bound export.
  - Ordinary worker success and complete host provenance remain separate verdicts.
requirements-completed: []
requirements-addressed: [ANT-06, ANT-07]
status: complete
qualification: incomplete
duration: 22min
completed: 2026-09-18
---

# Phase 204.2 Plan 18: Semantic host provenance Summary

**Configuration/tool-definition substitution now fails semantic checks; unavailable actual exports remain rejected provenance rather than becoming manufactured proof.**

In plain English: a matching file fingerprint no longer lets an empty log or a prompt count as the host's settings and complete tool definitions. The real worker flow can succeed while this separate evidence requirement remains incomplete.

## Task Commits

1. Task 1 RED: `76ca1ac7` — reproduced acceptance of empty, prompt, arbitrary, names-only and explicitly unavailable bytes.
2. Task 1 GREEN: `136e785e` — versioned semantic envelopes, exact session/executable/version/model/argv/effort matching, and original-inventory checks; unsupported actual acquisition remains refused.
3. Task 2 RED: `7009b85f` — reproduced absent explicit unavailable collector output.
4. Task 2 GREEN: `c1e63b8e` — collector writes invocation-linked unavailable records before the original inventory closes; parameter schemas compile offline using the existing dependency.
5. Task 2 actual evidence: `8aa2a3f7` — measured help, final source-bound ordinary capture, unavailable provenance and independent negative gate.

## Verification

- Task 1 focused normal and race: each **54 passing cases**, zero failures/skips.
- Final collector focused normal and race: each **55 passing cases**, zero failures/skips. Exact argv, file hashes, source revision, child environment and free storage are retained separately for every run.
- Both RED runs failed on the intended behavior: same-hash proof substitution and missing unavailable receipt respectively.
- Deterministic semantic positives exercise configuration and distinct tool-definition shapes. They are explicitly test-only; none claims to be a real client export or qualifies actual host provenance.
- Unknown versions/kinds/fields, trailing JSON, wrong invocation/model/argv/effort, defaulted unreported effort, absent/empty schemas, names-only tools, incomplete/unavailable captures and post-hoc replay inventory insertion reject.
- Fresh ordinary trial: actual host exit **0**, one native child and stable finalization replay, but validator/test exit **1**, outcome **incomplete** after 291 seconds. Parent `spawn-log`, `spawn-complete`, ceremony and a `question` operation using the bind request were unclassified, preventing complete child-only attribution. This does not supersede Plan17's earlier passing ordinary trial or qualify this candidate. No unchanged retry was attempted.
- Independent overlay against the immutable actual receipt: **exit 0**, specifically confirming both capture/raw artifacts belong to the same original session/inventory and the full provenance gate returns **`host export unavailable or incomplete`**. This is a passing rejection control, not passing qualification.

## Actual Source Acquisition Boundary

Eight read-only help/version commands measured Codex **0.154.0** and Claude Code **2.1.272**. The exact executable hashes, argv, exits and stdout/stderr references are in `plan18/help/receipt.json` under the external root below. Codex's `app-server generate-json-schema --help` explicitly describes the app-server protocol. That is not the complete model-facing tool list for a particular invocation. Claude's settings and tool-name selection flags likewise do not establish such an export.

Retained actual event inspection finds partial model/effort/permission observations in `turn_context`; it does not establish all effective settings and full tool definitions. No supported complete same-invocation acquisition path was established in the examined surfaces. This is a measured limitation, not a universal claim that no such facility exists.

The collector therefore writes `complete:false` with an explicit unavailable reason, same invocation identity, and raw event reference. The semantic/source gate rejects these records. It cannot accept a prewritten envelope, substitute observed names, or treat protocol schemas as actual tools. The absent supported adapter is an intentional fail-closed boundary required by this plan's unavailable-source branch, not a placeholder that grants success. Dependent provenance acceptance remains blocked; independent repairs may continue.

## Source and Evidence

- Frozen live candidate/harness source: `c1e63b8e961120a5dfa23fa36c1802b53bc899b7`.
- Candidate binary: `sha256:aa7441c4c91faa9569b3f761ef58173b0bfa38a626235a9c8ecb5c835fb0722a`; production digest `sha256:5c71573ed6ab717e9aca8a1ce2db19271297b5bebc60d2c740402e9947642f99`; harness `sha256:bfa2f117180d1bbf767e5ac076aad4215d2ca8b6e626d657fd1dc95d8c2b28cd`.
- Actual session `01a0b379-429d-7e43-9774-661e2eb21d74`, Codex `gpt-6-astra`, reported effort `medium`; temporary auth removed. Original receipt retains **1192 artifact entries**.
- External root: `/private/tmp/aether-gap2-resume-x7pso2qh/plan18`.
- Focused runs retain their original pre-commit revision and exact dirty-source file hashes; they are not relabeled as later commits.
- Clean trial uses a new standalone clone with no source changes and the previously admitted isolated normal-lane compiler/module environment. Before cloning/building, **25,833,738,240 bytes** were free against the measured **14,191,286,771-byte** requirement.
- Shared installed Aether, homes/configuration, original reports/receipts, Phase 205 and the normal/race admission clones were not modified. Existing auth is used only through the established temporary fixture copy.

## Deviations from Plan

**[Rule 3 — Blocking source admission]** The first fresh trial from the main checkout stopped before host launch because unrelated existing `.gsd/` files made source status nonempty. Its rejected receipt is preserved under `plan18/live`. A new clean standalone clone fixed admission; no user files were removed and no existing evidence clone was rewritten.

## Remaining Gaps

Full provenance remains unavailable, so no ANT requirement is marked complete. No full final matrix, Claude live comparison, owner walkthrough or publication occurred. The root owns shared STATE/ROADMAP/REQUIREMENTS disposition and subsequent independent plans.

The new actual ordinary attribution failure is retained for the root's later independent repairs/final matrix; no source owned by subsequent plans was changed to conceal or bypass it. Both Plan18 tasks are complete through the plan's explicit unavailable-source branch. Required publication qualification remains incomplete.

## TDD Gate Compliance

Both tasks have an observed failing RED commit followed by GREEN implementation and passing focused checks.

## Self-Check: PASSED

Verified all five task commits, declared source/receipt/SUMMARY files, and all 29 recursively referenced evidence hashes in the new checked-in provenance receipt. No tracked file deletions were introduced. Shared planning trackers remain exclusively owned by the root.
