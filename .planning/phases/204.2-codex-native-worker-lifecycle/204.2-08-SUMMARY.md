---
phase: 204.2-codex-native-worker-lifecycle
plan: "08"
subsystem: verification
tags: [native-workers, evidence, regression-accounting, real-host-gaps]
requires:
  - phase: 204.2-09
    provides: Frozen final-candidate matrix and reviewed raw qualification receipts
provides:
  - Fail-capable actual native evidence and completed-regression gate
  - Complete source-bound focused and full normal/race accounting
  - Standard verification preserving required native proof gaps
affects: [204.2-gap-closure, 204.3, 204.4, 204.5, 205-owner-acceptance]
tech-stack:
  added: []
  patterns:
    - Reuse one reviewed raw scenario proof engine
    - Independent complete discovery and exact invocation contract
    - Separate production, live-harness and test-corpus bindings
    - Explicit controller diagnostic-echo boundary without deduplicating real execution
key-files:
  created:
    - cmd/codex_native_evidence_test.go
    - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-regression.json
    - .planning/phases/204.2-codex-native-worker-lifecycle/204.2-VERIFICATION.md
  modified:
    - .github/workflows/ci.yml
    - cmd/codex_native_worker_test.go
    - cmd/command_guide_test.go
    - cmd/codex_native_context_test.go
    - cmd/recover_test.go
    - cmd/build_start_callers_200_test.go
    - cmd/lifecycle_card_startup_test.go
    - cmd/codex_native_recovery.go
    - cmd/codex_native_recovery_test.go
    - cmd/codex_build_finalize.go
    - cmd/codex_build_finalize_test.go
    - cmd/subcommand_reachability_ratchet_test.go
    - cmd/testdata/next_action_hardcode.json
    - cmd/testdata/next_action_hardcode_baseline.json
    - cmd/worker_read_cache_discipline_test.go
    - cmd/release_candidate_blackbox_test.go
    - .aether/skills/colony/aether-colony-build-cycle/SKILL.md
    - .claude/commands/ant-plan.md
    - .claude/commands/ant/plan.md
    - .opencode/commands/ant/plan.md
key-decisions:
  - Ten retained incomplete real scenarios remain mandatory evidence failures, never historical exceptions
  - Later test/report commits may reuse live identity only with unchanged production and separately pinned harness; repaired production leaves old live proof explicitly stale
  - Test assertion bytes are separately bound so report-only commits cannot disguise stale test execution
  - Preserve legacy raw receipt schemas explicitly while dispatching the exact aggregate schema
  - Same-checkout full runs require serialization; independent final clones remove shared Git mutation but exposed an ancestor NPM package-root isolation defect
requirements-completed: []
requirements-pending: [ANT-04, ANT-05, ANT-06, ANT-07]
status: execution_complete_with_verification_gaps
duration: 3h 21m
completed: 2026-09-17
coverage:
  - id: D1
    description: Evidence parser and source/identity controls reject causal mutations after valid baselines
    verification:
      - kind: integration
        ref: cmd/codex_native_evidence_test.go#TestCodexNativeEvidenceRejects
        status: pass
      - kind: integration
        ref: cmd/codex_native_evidence_test.go#TestCodexNativeEvidenceParser
        status: pass
      - kind: integration
        ref: cmd/codex_native_evidence_test.go#TestCodexNativeEvidenceDiscovery
        status: pass
    human_judgment: false
  - id: D2
    description: Current real host qualification and complete final regression disposition
    human_judgment: true
    rationale: Required actual gate fails because ten final real-host scenarios remain incomplete; no phase acceptance inferred.
  - id: D3
    description: Standard SC/ANT verification and preserved owner boundary
    human_judgment: true
    rationale: Explicit gaps_found report; root engineering scope review has stated limits and is not personal owner acceptance.
---

# Phase 204.2 Plan 08: Final Evidence and Regression Accounting Summary

**The actual-evidence gate rejects incomplete native captures, and source-bound test accounting preserves the phase's qualification gaps instead of masking them as historical failures.**

In plain English, the tests now check the real saved evidence rather than trusting a success label. The final sessions still lack required proof, so this plan finishes its accounting and report while the phase remains unqualified.

## Tasks executed

| Task | Result | Commits |
|---|---|---|
| 1 — Exact candidate checks and full regression accounting | Execution/accounting complete; required actual evidence remains failed | fee38e01, 5b6b4ab2, 125873d7, bb769827, 9b059c3c, 9f083baa, efce08d3, 2ba2ca88, 4573c47a, 200b14c4, f9e32ca0, d3732084, cf51e9ac |
| 2 — Standard verification and owner boundary | gaps_found; all four ANT requirements pending | a95957ee |

The plan's passing native-proof acceptance condition is **not met**. Completion here means the authorized gate execution, accounting and truthful report are delivered, not that missing host proof was supplied.

Strict gate refusal and independent regression accounting are separate conclusions. At f9 the strict validator stops at a failed required focused run; at cf51 it stops earlier at the changed test corpus. Neither failed path is claimed to validate all later full runs. Complete discovery, raw logs and diagnostic reviews establish the original full regression accounting independently.

## Changes and source boundary

- Added the default actual-qualification gate and strict completed-regression mode, artifact/candidate/source validation and causal controls.
- Reused `nativeReplayQualificationReceipt` for scenario proof. No second per-scenario engine, provider launcher or live harness change was introduced. The full normal exposed a real provider/native recovery classification bug and missing guidance; the orchestrator authorized and coordinated bounded repairs before a new full pair.
- Independent package/Test/Example discovery and source-owned focused selection prevent a restricted run from posing as a full suite. Every terminal needs a unique prior run; outer package consistency, unexpected fatal output and race warnings are checked.
- The explicit package-level controller failure marker separates retained diagnostic echo from authoritative first execution. Missing tests and earlier duplicates still fail.
- Superseded normal candidates were `5b6b4ab2266c1efbca2e531bbc6c35f173800433`, with test corpus `sha256:8e37daa0383bf737a2662354b68656e3c0cdc36adb80e7874a87f151e14685c6`, and `200b14c49ef539936d5e009ad5de1ef9eba9dc89`, with production `sha256:c548b071d4e83c1483189f21e03728a96cc63934cdbf33349e224de2762211f2` and test corpus `sha256:fbf5ae798c526ed3d91ef22a97aa4a62af7375901ff8c3d6754c58279bf022db`. Captured final165 production is stale for these repaired candidates and final f9.
- Final CI-only correction: `f9e32ca000c88b9b723393e4dbf2f2b5801593a8`, production `sha256:641942bc26bc7cc9022078c8247f914521e3c6d37bbe06bc76c395bf309b0a8f`; test corpus and harness unchanged. The final pair uses reviewed independent standalone clones, separate temp/npm state and exact source/discovery pins. Original5b6/200b normal logs remain unchanged; no full race ran on those superseded candidates.
- Post-full fixture repair `cf51e9ac86d11139d806591245d8afa5cbdd593b` changes only `cmd/release_candidate_blackbox_test.go`: current test corpus is `sha256:d2bcfc93a43d0cd9f6879d81e7773d3a8290f78e5a90b682e8ef82fc790e5c98`. Production and live harness remain f9-identical. Its bounded followups do not become a full-suite run, and the f9 corpus is not relabeled.
- Live source remains `165db28c767864a619a6d883c7a2d0db9593741a`, production digest `sha256:f86229cf5dcdd2176c321c080a5e923b99c0e6fcc23cfd905385a5c634011210`, harness `sha256:bafaed8881fa4853d20e030348bc26a56f6f65762ca92b7ace03d96572ca367c`. Later test/report commits do not relabel the captured binaries.

## Verification

Final f9 full normal and race each completed **21 packages, 6,797/6,797 top-level cases, 12,676 total cases: 12,632 pass / 27 fail / 17 skip**, exit 1. Normal took 50m00s; race took 52m44s. All discovered cases have authoritative terminals, with no parser/order/duplicate errors, no race report and no outer 90-minute timeout. Eighteen other tested packages passed; two packages have no tests.

The same15 historical top-level failures (18 failed cases) have pinned substantive comparisons in both runs. The required native-evidence failure remains new, and three additional new groups (eight failed cases) are retained separately: interrupted-build sentinel timeout, absent historical Git objects in the clones, and packed NPM consumer script missing after ancestor home-project discovery. None is an inherited exception. Schema/catalog and the corrected CI selector passed both runs. The [standard verification](204.2-VERIFICATION.md) lists every residual and diagnosis.

The strict Task1 command ran after accounting, exit 1 in 14.01 seconds: incomplete/stale actual evidence is rejected, then required focused evidence failure stops strict regression validation. It does not confer downstream full-run qualification. Strict Task2 at cf51 exited 1 in 3.02 seconds and additionally rejected the changed test corpus; no original full results were relabeled as testing that later repair.

Bounded followups retain the failed full outcomes: importing the exact three archived objects into the disposable clone makes the branch test pass normal/race at unchanged f9; the cf51 NPM prefix fix passes 6/6 normal and 6/6 race with ancestor package/lock/sentinel invariance and causal old-command RED. One interrupted-build targeted normal passes with a longer temporary path, but its original full-load readiness cause remains unproven. The harness still times out before child cleanup/output capture; that diagnostic/cleanup defect and both original interrupted failures remain unresolved. Evidence is under [new-failure-diagnosis](/Users/callumcowie/.aether-backups/phase204-2-execution-20260917/plan08/new-failure-diagnosis/interrupted/REVIEW.md) and the separately linked receipts in VERIFICATION. No additional full suite ran.

The root-authorized [home-NPM partial recovery](/Users/callumcowie/.aether-backups/phase204-2-execution-20260917/plan08/npm-isolation-incident/recovery-receipt.json) removed only attributable Aether entries from three JSON files and quarantined the package/bin symlink, preserving modes and all 12,618 unrelated hash checks without running npm. Residual 31 additions / 64 changes / 17 removals lack established prestate and were not blindly reversed. Full home restoration is not claimed.

The finalized [incident report](/Users/callumcowie/.aether-backups/phase204-2-execution-20260917/plan08/npm-isolation-incident/INCIDENT.md), SHA-256 `d9310d3da4ac509a34012b67937bfdafb855394dc1d294308a8a4d3172580f77`, records the source cause, actual scope, guarded partial recovery and remaining uncertainty. Root's adjacent recovery review independently verifies that limited result.

- First full normal completed in 26m06s, exit1, with 30 top-level failures (40 including failing subcases), 17 explicit skips and two swallowed terminal actions. Schema/catalog passed. Race was withheld pending correction; no obsolete-candidate race was run. Original raw output and both original/corrected-accountant derivations are retained.
- Exact package ownership was a genuine parser RED; corrected normal70/race75 cases passed. Confirmation stderr capture restores real JSON terminal actions without prose-based pass inference. Native pointer corruption now proves the existing journal remains unchanged, and the separate legacy scanner again reports only its original two findings; that scanner is not labeled all-green.

- Initial integrated causal/parser/schema selection caught the historical unversioned raw-receipt compatibility case; preserved raw log, corrected explicit dispatch, then **38/38** controls passed.
- The first affected normal run completed **348/348 top-level, 860 total cases** with **856 pass / 3 fail / 1 explicit live skip**. Besides the intended actual-evidence failure, two old guide assertions demanded Task/subagent prose from the Codex native build guide. The final test-only correction preserved YAML and both primary platform contracts. Targeted normal/race each passed all three affected tests.
- The superseded first-candidate affected normal and race each completed **348/348 top-level, 860 total cases**, **858 pass / 1 required evidence failure / 1 explicit FreshHost skip**.
- Initial actual gate finished in **9.56 seconds** with the intended incomplete-scenario failures and no unexpected schema/source/artifact error.
- First-candidate independent list-only discovery matched the separately captured **21-package / 6,790-top-level** map in **7.27 seconds**. This external-overlay helper probe enumerated tests; it was not a full execution or live-host run.
- Corrected source 200b focused normal and race each completed **372/372 top-level, 1,071 total cases**, **1,069 pass / 1 required current-source evidence failure / 1 explicit FreshHost skip**. Both have complete structural accounting, no unexpected failures and no race or stderr diagnostics. Corrected independent discovery enumerated **21 packages / 6,797 top-level cases** in 15.12 seconds.
- Final f9 focused normal and race independently completed **372/372 top-level, 1,071 cases, 1,069 pass / 1 required evidence failure / 1 FreshHost skip** in the standalone clones. CI selector guards independently passed **22/22 normal and22/22 race**. Both clones matched complete discovery and source/corpus/harness identities; their list-only probes took45.66 and43.66 seconds.
- Every negative control begins with successful deterministic replay/boundary validation. Synthetic positives establish validator behavior, not actual native proof.
- Full raw logs, exact argv/exits, discovery, source pins, case accounting and historical diagnostics remain under the external `plan08/` evidence directory.

## Required remaining gaps

The final matrix retains ten incomplete scenarios and one observed missing-skill refusal. The report scores **0/5 fully qualified final-candidate success criteria**. Actual Watcher checks/findings, scoped envelope/send identity and partial-resume preservation are described with their source limits; they do not promote their incomplete scenario outcomes.

Encrypted message exports do not independently prove full delivered plaintext. Inherited question history prevents exclusive answer-delta causation. Cancellation lacks a terminal acknowledgement, workspace probes lack required sentinel attempts, and the Claude comparison is authentication-incomplete. Missing usage stays unreported.

At final source f9 (unchanged Go bytes from200b), `pkg/codex/platform_contract.go:57–61,76–85` explicitly marks per-child permission selection, separate native worktree allocation and Aether-governed nesting unavailable. Admission refuses those requested controls and accepts only the supported inherited workspace-write profile in the accepted shared workspace. A real nested host response does not establish governed Aether recruitment. These known unsupported controls are distinct from incomplete workspace/sentinel probes. Raw child-attributed `token_usage_record` events do exist, but Aether does not collect them: saved usage and cost remain unreported, never zero.

Both unclassified probes remain explicit. The global no-substitution prohibition remains flagged-unverified; bounded claim-honesty and owner-boundary engineering judgments are attributed separately from personal acceptance. The original Phase205 inventory hash remains `3b2b678284a26332addeadd2bd700bd4f51c4e2b0e6907a987116c85e5c2a8dd`.

## Deviations from plan

1. **[Rule 2 — Necessary compatibility] Receipt environment collision.** Root authorized `cmd/codex_native_worker_test.go` because its existing raw test reads the same explicit receipt-path variable as the new aggregate gate. The exact aggregate schema delegates to the phase gate; versioned and historical unversioned raw receipts keep their original full replay checks; unknown nonempty schemas refuse. Positive raw and missing/corrupt inventory controls remain. Scope authorization: `plan08-receipt-schema-scope-extension.json`. Commit fee38e01.
2. **[Rule 1 — Stale directly affected tests] Platform-specific guide assertions.** The first complete focused run exposed two expectations for legacy activity wording in Codex build guidance. Root authorized only those two contracts in `cmd/command_guide_test.go`. Codex now asserts native spawn/actual-observe instructions while YAML and Claude/OpenCode retain their existing panel expectations. Scope authorization: `plan08-guide-assertion-scope-extension.json`. Commit 5b6b4ab2.
3. **[Execution disposition] Mandatory final host proof is missing.** Root explicitly directed full accounting and `gaps_found`, preserving the failed real matrix. No additional live/authentication attempts were performed; current-candidate qualification remains a gap. Later production repairs are recorded separately below; no success criterion was weakened.
4. **[Rules 1/2 — Full-gate discoveries] Exact accounting and bounded fixture corrections.** The first full normal exposed package-prefix misattribution in the new validator and two interactive confirmation prompts that swallowed Go JSON terminal actions. Exact event-package ownership now has causal positive/negative controls; the two existing tests capture and assert their real prompts. Native stale-pointer controls use a pointer-only corruption helper that proves the existing journal is unchanged, removing precisely three new source-scan findings. The original two unrelated scanner failures remain. Root expanded ownership explicitly; commits bb769827 and efce08d3.
5. **[Rules 1/2 — Integrated production and contract regressions] Root-coordinated repairs.** Ordinary provider journals were incorrectly classified as native recovery; native/mixed/damaged controls now preserve the intended boundary. The finalizer derives its legacy next advice from the canonical resolver. Missing Codex support safeguards and primary wrapper mirroring were restored, stale delegation/repair assertions updated, and command reachability scanning now resolves finite native registrations and installed support without widening an orphan waiver. Root and the qualifier owned these bounded changes and their focused proofs. Commits 125873d7, 9b059c3c, 9f083baa, 2ba2ca88, 4573c47a and 200b14c4. The production digest changed, so original165 live captures remain stale for the corrected candidate; no live evidence was relabeled.

6. **[Rule1 — Newly exposed CI selector omission]** The complete200b normal found `TestWiringGateStepRunsEveryWiringTest` because CI omitted the three new native reachability guards. Root added exactly those three names to `.github/workflows/ci.yml`, commit f9e32ca0, then normal/race each passed22 targeted cases. This YAML input changes the production digest while all Go/test bytes remain identical. A read-only isolation review supports the root-authorized final concurrent full pair in separate standalone repositories; no shared-checkout race was started.

7. **[Rule1 — Execution-isolation defect] Packed NPM consumer escaped to an ancestor project.** Independent Git directories did not establish an NPM project boundary for an empty consumer below the home tree. Both final full runs exposed missing consumer scripts; retained NPM logs confirm home-project writes. Root amended ownership and committed explicit `--prefix consumer` in the test helper (cf51e9ac). Disposable ancestor RED/GREEN and the existing package contract pass normal/race with preserved sentinels. Production bytes remain f9-identical, while the corpus changes to d2bcfc93; no full pair was rerun or relabeled. Three omitted historical Git objects were separately restored only to the disposable clone for bounded checks. The interrupted-build timeout remains a documented unresolved defect, not an automatic contention waiver.

## Review and preservation

The corrected external draft and five parser/discovery/source-boundary repairs were independently reviewed before integration. Root approved the exact integrated source hashes and the guide correction before broad execution. The root orchestrator owns the final GSD code-review hook, merge and shared trackers.

No tracked deletions, shared Aether runtime installation, publish, release, CalVault action, Phase205 owner action or requirements completion occurred. The packed NPM fixture did cause unintended home-project package writes, separately retained and investigated; it is not concealed by the runtime-install statement. The existing owner policy is preserved: failed checks block VERIFIED advancement while valid receipt-backed BUILD credit remains.

## Next dependency

Close the current native evidence gaps before a passed phase verdict. Later workflow coverage (204.3), shared recruitment/worktree/Swarm (204.4), and candidate handoff (204.5) retain their scope; the original Phase205 personal walkthrough remains the owner's action.

## Known stubs and deferred issues

No newly added TODO, FIXME, placeholder, unconnected data source or implementation stub was found in the changed non-planning lines. Missing actual native qualification is a measured evidence gap, not a placeholder implementation. The interrupted-build readiness/cleanup defect, fifteen substantive historical regression failures, incomplete real-host scenarios and partial home-NPM restoration remain explicitly unresolved. There is no completed full-suite execution on the post-full cf51 test corpus; only its bounded fixture checks pass.

Shared STATE, ROADMAP and requirement completion remain with the root orchestrator. No ANT requirement is marked complete by this summary.

## Self-Check: PASSED

- All four expected gate/report files exist; all fourteen listed implementation, correction and task commits resolve.
- All 107 artifact references in the committed regression receipt were hash-checked. Both full raw logs retain exact source, invocation, discovery and failure accounting.
- The current test corpus differs only in the recorded NPM fixture file; all 1,709 production inventory entries and the live harness remain f9-identical. No post-fix full run is claimed.
- Final incident SHA-256 matches the reviewed report. Original qualification, live harness and Phase205 inventory/checkpoint hashes remain unchanged.
- Source stub scan and `git diff --check` pass. This self-check verifies artifact/report integrity; it does not turn failed required evidence or unresolved regressions into passing qualification.
