---
phase: 204.2-codex-native-worker-lifecycle
plan: "12"
type: execute
wave: 10
depends_on: ["204.2-11"]
files_modified:
  - cmd/codex_native_worker_live_test.go
  - cmd/codex_native_gap_recovery_test.go
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/recovery.json
autonomous: true
gap_closure: true
requirements: [ANT-04, ANT-05]
must_haves:
  truths:
    - "A fresh parent retains completed child bytes and credit, identifies unfinished work and launches only currently authorized unfinished assignments."
    - "A spawn-before-bind interruption stays unresolved until the same actual child is corroborated; it never authorizes duplicate dispatch."
    - "Equal-wave completion arrival order cannot change assigned membership, result identities or credited tasks."
  artifacts:
    - path: cmd/codex_native_worker_live_test.go
      provides: "Executable implementation or proof contract"
    - path: cmd/codex_native_gap_recovery_test.go
      provides: "Executable implementation or proof contract"
    - path: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/recovery.json
      provides: "Source-pinned gap-closure evidence"
  key_links:
    - from: cmd/codex_native_worker_live_test.go
      to: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/recovery.json
      via: "Source-bound checks and preserved qualification evidence"
---

<objective>
Qualify fresh-parent early, partial and ambiguous-spawn recovery.

Purpose: Close the observed qualification gap while preserving historical evidence. In plain English, prove the worker really did the promised work on the current program and clearly retain anything still unproved.
Output: cmd/codex_native_worker_live_test.go, cmd/codex_native_gap_recovery_test.go, .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/recovery.json.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-VERIFICATION.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-REVIEW.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP-PLAN-AUDIT.md
@.planning/research/ant-codex-parity-2026-09-16/GSD-INTEGRATION.md
</context>

## Execution boundaries

D-01..D-10 refer to the existing SYNTHESIS mapping of unnumbered CONTEXT decisions, not new owner decisions. Preserve all Plans 01–09, summaries, original evidence and reviews byte-for-byte. Write new receipts under evidence/gap-closure/ and a new durable external run root. Install only inside disposable child environments; never replace shared aether, hub, Codex/Claude homes, CalVault or Phase 205 evidence. Keep current branch and unrelated edits. Do not initiate login, fresh authentication, publication or owner walkthrough. Record existing-auth failure honestly. No new packages or database schema are involved; security enforcement is disabled in project config.

Pin current source revision/status, production inventory, test corpus, live harness, candidate binary/build arguments, Go version/environment, host executable/version/model/effort/tool schema and installed bytes. The installed narrow CalVault hotfix is not this candidate. Old 165db28c, f9e32ca0 and cf51e9ac receipts keep their original identities. A reporting-only commit may reference equal bytes but cannot relabel binaries. Production changes require final live requalification; harness changes require new capture where collection/causality changed, and separately pinned raw replay for parser-only corrections; changed test corpus invalidates previous full-run credit.

Reversibility: source/test edits are reversible through their scoped commits; disposable fixtures can be removed only after external raw evidence is preserved. Journal/result contract changes are costly: preserve readers, immutable terminal records and ExecutionBinding; do not migrate any shared store. A host limitation is a measured incomplete/refused outcome, never permission to remove a required scenario. Live checks may exceed 60 seconds; their explicit bounds preserve evidence and stop owned processes. Ordinary bounded test verification must be run normally and with -race when runtime or harness behavior changes.

## Artifacts this phase produces

- cmd/codex_native_worker_live_test.go
- cmd/codex_native_gap_recovery_test.go
- .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/recovery.json

New named test symbols and script modes are specified in the task actions. Existing runtime interfaces remain authoritative.

<interfaces>
nativeValidateInterruptedHostScenario, nativeCollectResumeEvidence, nativePublicResumeProof and nativeReplayFreshParent already model the three paths. Reuse public resume and worker-run terminals; provider PID recovery cannot classify native or mixed journals as subprocess work.
</interfaces>

<tasks>
<task type="auto">
  <name>Task 1: Make interruption cut points deterministic and preserve fresh-session evidence</name>
  <files>cmd/codex_native_worker_live_test.go, cmd/codex_native_gap_recovery_test.go</files>
  <read_first>cmd/codex_native_worker_live_test.go; cmd/codex_native_gap_recovery_test.go; cmd/codex_native_recovery.go; cmd/codex_native_recovery_test.go; cmd/session_flow_cmds.go; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-06-SUMMARY.md</read_first>
  <behavior>A missing bind does not mean a missing child; a completed terminal without aggregate survives unchanged; repeated finalization is idempotent.</behavior>
  <action>Per D-04/D-09, add explicit fixture checkpoints for terminal-before-aggregate, one-finished/one-unfinished and actual-spawn-before-bind. The controller observes durable files and raw host events, then stops its owned parent and starts a distinct fresh host process whose first recovery operation is public resume. Do not rewrite the accepted prompt to repair a run in place; a changed prompt requires a new run identity. Corroborate every allowed coordination command by its actual event. Retain terminal/source/finalizer hashes across the boundary. Add new TestCodexNativeGapRecovery controls showing that unreadable child events, unknown launch and stale parent/session/attempt do not produce a result, auto-redispatch or credit.</action>
  <acceptance_criteria>Interrupted checkpoints require observed actual events before termination; fresh parent identity differs; completed worker journal/source are byte-identical; only authorized unfinished assignments can start. Equal-wave arrival permutation and stale binding controls remain passing.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexNativeGapRecovery|TestCodexNativeRecoveryPartialReady|TestCodexNativeRecoveryUnresolved|TestCodexNativeWorkerArrivalOrder|TestCodexNativeRecoveryResumeTransaction)$' -count=1 -timeout 60s</automated></verify>
  <done>Interrupted checkpoints require observed actual events before termination; fresh parent identity differs; completed worker journal/source are byte-identical; only authorized unfinished assignments can start. Equal-wave arrival permutation and stale binding controls remain passing.</done>
</task>

<task type="auto">
  <name>Task 2: Run three actual recovery scenarios through public resume</name>
  <files>.planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/recovery.json</files>
  <read_first>.planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/recovery.json; cmd/codex_native_worker_live_test.go; cmd/codex_native_finalize.go; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-09-SUMMARY.md</read_first>
  <action>Run early-resume, partial-resume and spawn-gap on the current candidate in isolated fixtures. Preserve each original parent, genuinely fresh parent, actual child events, public resume invocation/result and before/after durable inventories. Early-resume must launch no replacement; partial-resume must preserve the finished worker and run exactly the remaining accepted assignment rather than claiming zero total new children. Spawn-gap must either reconnect/bind the existing child with observed identity or honestly remain blocked; unobservable reconnection is an ANT-05 gap, not successful recovery. Replay the actual finalizer and require unchanged completed credit and journal bytes. Surface ANT-05's unclassified edge probe as unresolved even when these concrete cases pass.</action>
  <acceptance_criteria>All three scenarios have explicit current-source disposition, no duplicate child/result/credit and actual fresh-session boundaries. Required successful early/partial recovery is proved; unresolved host reconnection remains reported and blocks the corresponding qualification.</acceptance_criteria>
  <verify><automated>AETHER_CODEX_NATIVE_LIVE=1 AETHER_CODEX_NATIVE_SCENARIOS=early-resume,partial-resume,spawn-gap AETHER_CODEX_NATIVE_EVIDENCE_DIR="$AETHER_GAP_EVIDENCE_ROOT/recovery" go test ./cmd -run '^TestCodexNativeWorkerFreshHost$' -count=1 -json -timeout 60m</automated></verify>
  <done>All three scenarios have explicit current-source disposition, no duplicate child/result/credit and actual fresh-session boundaries. Required successful early/partial recovery is proved; unresolved host reconnection remains reported and blocks the corresponding qualification.</done>
</task>
</tasks>

<verification>
Every task's automated verification retains raw exit/output and exact candidate identity. Required live evidence must pass on its own merits; test fixtures and receipt replay cannot replace it. Plan16 owns the final complete regression pair and new explicit goal-backward verdict. Shared planning trackers remain the orchestrator's responsibility.
</verification>

<success_criteria>
A fresh parent retains completed child bytes and credit, identifies unfinished work and launches only currently authorized unfinished assignments.
A spawn-before-bind interruption stays unresolved until the same actual child is corroborated; it never authorizes duplicate dispatch.
Equal-wave completion arrival order cannot change assigned membership, result identities or credited tasks.
Missing mandatory proof remains an explicit gap; writing this plan's execution report never marks the phase complete.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-12-SUMMARY.md with task outcomes, exact source and evidence identities, failures and outstanding qualification obligations.
</output>

