---
phase: 204.2-codex-native-worker-lifecycle
plan: "13"
type: execute
wave: 8
depends_on: ["204.2-08"]
files_modified:
  - cmd/blackbox_harness_test.go
  - cmd/release_candidate_blackbox_test.go
  - scripts/qualify-native-gap-regression.py
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/regression-preflight.json
autonomous: true
gap_closure: true
requirements: [ANT-04, ANT-05, ANT-07]
must_haves:
  truths:
    - "A readiness timeout retains child diagnostics and stops/waits all owned fixture processes before TempDir cleanup."
    - "Packed NPM validation cannot discover or mutate an ancestor real-home project; consumer prefix and ancestor sentinels are enforced."
    - "Standalone regression clones contain the recorded disposed commit objects without changing source refs or historical failure identities."
  artifacts:
    - path: cmd/blackbox_harness_test.go
      provides: "Executable implementation or proof contract"
    - path: cmd/release_candidate_blackbox_test.go
      provides: "Executable implementation or proof contract"
    - path: scripts/qualify-native-gap-regression.py
      provides: "Executable implementation or proof contract"
    - path: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/regression-preflight.json
      provides: "Source-pinned gap-closure evidence"
  key_links:
    - from: cmd/blackbox_harness_test.go
      to: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/regression-preflight.json
      via: "Source-bound checks and preserved qualification evidence"
---

<objective>
Repair regression readiness and enforce disposable package-test isolation.

Purpose: Close the observed qualification gap while preserving historical evidence. In plain English, prove the worker really did the promised work on the current program and clearly retain anything still unproved.
Output: cmd/blackbox_harness_test.go, cmd/release_candidate_blackbox_test.go, scripts/qualify-native-gap-regression.py, .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/regression-preflight.json.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-VERIFICATION.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-REVIEW.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP-PLAN-AUDIT.md
@.planning/research/ant-codex-parity-2026-09-16/GSD-INTEGRATION.md
</context>

## Execution boundaries

D-01..D-10 refer to the existing SYNTHESIS mapping of unnumbered CONTEXT decisions, not new owner decisions. Preserve all Plans 01–09, summaries, original evidence and reviews byte-for-byte. Write new receipts under evidence/gap-closure/ and a new durable external run root. Install only inside disposable child environments; never replace shared aether, hub, Codex/Claude homes, CalVault or Phase 205 evidence. Keep current branch and unrelated edits. Do not initiate login, fresh authentication, publication or owner walkthrough. Record existing-auth failure honestly. No new packages or database schema are involved; security enforcement is disabled in project config.

Pin current source revision/status, production inventory, test corpus, live harness, candidate binary/build arguments, Go version/environment, host executable/version/model/effort/tool schema and installed bytes. The installed narrow CalVault hotfix is not this candidate. Old 165db28c, f9e32ca0 and cf51e9ac receipts keep their original identities. A reporting-only commit may reference equal bytes but cannot relabel binaries. Production changes require final live requalification; harness changes require new capture where collection/causality changed, and separately pinned raw replay for parser-only corrections; changed test corpus invalidates previous full-run credit.

Reversibility: source/test edits are reversible through their scoped commits; disposable fixtures can be removed only after external raw evidence is preserved. Journal/result contract changes are costly: preserve readers, immutable terminal records and ExecutionBinding; do not migrate any shared store. A host limitation is a measured incomplete/refused outcome, never permission to remove a required scenario. Live checks may exceed 60 seconds; their explicit bounds preserve evidence and stop owned processes. Ordinary bounded test verification must be run normally and with -race when runtime or harness behavior changes.

## Artifacts this phase produces

- cmd/blackbox_harness_test.go
- cmd/release_candidate_blackbox_test.go
- scripts/qualify-native-gap-regression.py
- .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/regression-preflight.json

New named test symbols and script modes are specified in the task actions. Existing runtime interfaces remain authoritative.

<interfaces>
TestCLIInterruptedBuildResumesThroughForceRedispatch starts command, waits ten seconds, then Kill/Wait: its current fatal wait bypasses cleanup. npm prefix is already corrected in release_candidate_blackbox_test.go. disposedBranches is authoritative; do not edit its recorded commit IDs. The new script must retain nativeEvidenceRunContract's literal full argv and nativeEvidenceExpectedDiscovery's focused selection.
</interfaces>

<tasks>
<task type="auto">
  <name>Task 1: Diagnose interrupted-build readiness and make cleanup unconditional</name>
  <files>cmd/blackbox_harness_test.go</files>
  <read_first>cmd/blackbox_harness_test.go; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-08-SUMMARY.md; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-VERIFICATION.md</read_first>
  <behavior>Missing sentinel, early child exit and success all release owned processes exactly once, retain useful diagnostics and leave unrelated processes untouched.</behavior>
  <action>Per D-03/D-05/D-08, reproduce TestCLIInterruptedBuildResumesThroughForceRedispatch's readiness failure with retained stdout/stderr, stage timestamps, child exit and sentinel path rather than assuming contention/path length. Register owned-process cleanup immediately after Start, before any fatal assertion; terminate and Wait the parent exactly once and stop only adapter descendants identified by this fixture's log. Read output only after process synchronization to avoid races. Return readiness failure with captured diagnostics and exited-child state instead of hiding the cause in waitForBlackBoxFile. Add a bounded failure-path test TestCLIInterruptedBuildReadinessCleanup that deliberately never writes the sentinel and proves no writes/processes survive cleanup. Adjust readiness timing only if measured startup semantics justify it, retaining explicit bounds and original failures. Preserve the actual interrupted-build→public resume→explicit force-redispatch assertions.</action>
  <acceptance_criteria>Normal and race tests prove the interruption lifecycle and forced readiness-failure cleanup; original ten-second failure remains historical evidence. A longer timeout alone cannot close the diagnosis.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCLIInterruptedBuildResumesThroughForceRedispatch|TestCLIInterruptedBuildReadinessCleanup)$' -count=1 -timeout 120s</automated></verify>
  <done>Normal and race tests prove the interruption lifecycle and forced readiness-failure cleanup; original ten-second failure remains historical evidence. A longer timeout alone cannot close the diagnosis.</done>
</task>

<task type="auto">
  <name>Task 2: Codify NPM isolation and exact full-run preparation</name>
  <files>cmd/release_candidate_blackbox_test.go, scripts/qualify-native-gap-regression.py, .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/regression-preflight.json</files>
  <read_first>cmd/release_candidate_blackbox_test.go; scripts/qualify-native-gap-regression.py; .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/regression-preflight.json; cmd/branch_disposition_test.go; cmd/codex_native_evidence_test.go; .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-regression.json; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-08-SUMMARY.md</read_first>
  <action>Keep cf51's explicit npm install --prefix consumer and add TestPackedNPMAncestorIsolation using a disposable ancestor manifest/lock/node_modules sentinel plus empty child consumer. Demonstrate the old unprefixed command mutates only a wholly disposable negative-control ancestor, while the corrected path preserves every ancestor hash. Never run that negative control beneath the actual home. Add a stdlib-only script with --prepare, --run and --check modes: prepare independent standalone normal/race clones with separate Git/temp/npm roots, capture source/production/test/harness/toolchain identity, import only the exact historical disposed commit objects when missing, and verify them using git cat-file without creating source refs. Use task-specific variables and isolated subprocess environment maps, never repurpose the parent HOME/CODEX_HOME. --prepare runs bounded fixture preflight and outputs immutable manifest; --run later runs the literal discovered focused and full suites; --check verifies raw event/discovery/diagnostic accounting through the existing evidence contract. A failed preflight prevents broad runs. Retain the prior incident's partial-recovery disclosure: 31 additions, 64 changes, 17 removals remain unestablished; this task does not restore the real home.</action>
  <acceptance_criteria>Explicit consumer prefix is enforced and ancestor sentinels remain unchanged; normal/race preflight retains all required disposed hashes and fixture checks. Preparation does not skip tests, create an allowlist or mutate shared homes. Script fails on candidate/corpus mismatch and missing objects.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestPackedNPMAncestorIsolation|TestPackedNPMReleaseCandidateContract|TestDisposedBranchesAreGoneFromTheRepository)$' -count=1 -timeout 180s; python3 scripts/qualify-native-gap-regression.py --prepare --evidence-root "$AETHER_GAP_EVIDENCE_ROOT/regression-preflight"</automated></verify>
  <done>Explicit consumer prefix is enforced and ancestor sentinels remain unchanged; normal/race preflight retains all required disposed hashes and fixture checks. Preparation does not skip tests, create an allowlist or mutate shared homes. Script fails on candidate/corpus mismatch and missing objects.</done>
</task>
</tasks>

<verification>
Every task's automated verification retains raw exit/output and exact candidate identity. Required live evidence must pass on its own merits; test fixtures and receipt replay cannot replace it. Plan16 owns the final complete regression pair and new explicit goal-backward verdict. Shared planning trackers remain the orchestrator's responsibility.
</verification>

<success_criteria>
A readiness timeout retains child diagnostics and stops/waits all owned fixture processes before TempDir cleanup.
Packed NPM validation cannot discover or mutate an ancestor real-home project; consumer prefix and ancestor sentinels are enforced.
Standalone regression clones contain the recorded disposed commit objects without changing source refs or historical failure identities.
Missing mandatory proof remains an explicit gap; writing this plan's execution report never marks the phase complete.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-13-SUMMARY.md with task outcomes, exact source and evidence identities, failures and outstanding qualification obligations.
</output>

