---
phase: 204.2-codex-native-worker-lifecycle
plan: "13"
subsystem: testing
tags: [go, race, subprocess-cleanup, npm-isolation, regression-evidence]
requires:
  - phase: 204.2-08
    provides: Historical failed regression captures and source-owned evidence contracts
provides:
  - Synchronized interrupted-build cleanup with retained readiness diagnostics
  - Disposable NPM ancestor and ambient-configuration isolation controls
  - Standalone source-pinned normal/race regression preparation and fail-closed admission
  - Honest final preflight receipt preserving superseded failures
affects: [204.2-15, 204.2-16]
actuals:
  tokens: 15262
  tasks: 2
  commits: 3
tech-stack:
  added: []
  patterns: [exactly-once process wait, immutable preparation manifests, source-owned Go evidence replay]
key-files:
  created:
    - scripts/qualify-native-gap-regression.py
    - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/regression-preflight.json
    - .planning/phases/204.2-codex-native-worker-lifecycle/204.2-13-SUMMARY.md
  modified:
    - cmd/blackbox_harness_test.go
    - cmd/release_candidate_blackbox_test.go
key-decisions:
  - Retain the original ten-second readiness bound; the historical timeout cause was not reproduced.
  - Failed preflight prohibits broad execution; raw failed receipts are never rewritten.
  - Use child environment maps and disposable module/cache roots without changing shared installations.
  - Reuse the existing Go invocation, discovery and regression validators through an external probe overlay.
patterns-established:
  - Cleanup registered immediately after Start; buffers read only after the single Wait completes.
  - Explicit npm consumer prefix plus isolated config/cache and disposable ancestor controls.
requirements-completed: []
requirements-addressed: [ANT-04, ANT-05, ANT-07]
coverage:
  - id: D1
    description: Interrupted build resumes through explicit force redispatch and failure paths stop owned writers.
    verification:
      - kind: integration
        ref: cmd/blackbox_harness_test.go#TestCLIInterruptedBuildReadinessCleanup
        status: pass
      - kind: integration
        ref: cmd/blackbox_harness_test.go#TestCLIInterruptedBuildResumesThroughForceRedispatch
        status: pass
    human_judgment: false
  - id: D2
    description: NPM installs remain inside a disposable consumer and preserve ancestor bytes.
    verification:
      - kind: integration
        ref: cmd/release_candidate_blackbox_test.go#TestPackedNPMAncestorIsolation
        status: pass
      - kind: integration
        ref: cmd/release_candidate_blackbox_test.go#TestPackedNPMReleaseCandidateContract
        status: pass
    human_judgment: false
  - id: D3
    description: Standalone normal/race preparation verifies historical objects and rejects changed or incomplete evidence.
    verification:
      - kind: integration
        ref: scripts/qualify-native-gap-regression.py --prepare
        status: pass
      - kind: other
        ref: /Users/callumcowie/.aether-evidence/phase-204.2-gap-20260917T210924Z/plan-13/script-guard-checks.json
        status: pass
    human_judgment: false
duration: 32min
completed: 2026-09-17
status: complete
qualification: incomplete
---

# Phase 204.2 Plan 13: Regression isolation and readiness Summary

**Timeouts now stop the fixture and retain its diagnostics; NPM and regression checks have a passing disposable normal/race preflight. Full regression and native-host qualification remain outstanding.**

In plain English: a failed test cannot quietly keep writing after its temporary folder is removed, and package tests cannot wander into the user's parent project. A saved preparation pass is permission to run the exact later checks, not proof those checks already passed.

## Performance

- Started: 2026-09-17T21:13:04.122329+00:00 (first durable local receipt; initial context reads preceded it).
- Completed: 2026-09-17T21:44:47.490256+00:00.
- Tasks: 2; owned deliverables: 4 plus this SUMMARY.
- `actuals.tokens` estimates changed characters divided by four; it is not model usage.

## Task commits

1. **Task 1 — readiness diagnosis and unconditional cleanup:** `bfa78aed458f6d351b9f017c373c39db21b0a3d5`.
2. **Task 2 — NPM isolation and exact regression preparation:** `74c3e21d5734d04d3ccc211bfaed9d80ed8866e0`.

Both used normal Git hooks on this isolated branch. This SUMMARY is committed separately before final narration. Shared STATE, ROADMAP and REQUIREMENTS remain the orchestrator's responsibility.

## What changed

`cmd/blackbox_harness_test.go` registers cleanup immediately after `Start`, gives `Wait` one owner, stops only adapter PIDs recorded in that fixture's log, waits for their non-running state on the measured Unix host, and reads output after synchronization. Diagnostics include startup/readiness/cleanup times, sentinel path, PID, pre-cleanup exit state, wait result, stdout/stderr and the adapter log. Missing sentinel, early exit, success and a logged descendant are bounded controls; cleanup is idempotent and an unrelated writer must survive. The original public resume and explicit force-redispatch assertions remain.

`cmd/release_candidate_blackbox_test.go` retains explicit `npm install --prefix consumer`. The new test demonstrates the old unprefixed ancestor mutation only in a disposable ancestor, while the corrected installation preserves its manifest, lockfile and complete node_modules inventory. Ambient prefix/global settings are explicitly challenged and stripped from the npm child environment. Pack/install use bounded commands and isolated cache/config/home values. Actual candidate/package/archive hashes and 1,119 installed-file entries per final mode are retained.

`scripts/qualify-native-gap-regression.py` uses only Python's standard library plus existing Git/Go/tools. `--prepare` creates standalone normal/race clones, separate Git/temp/npm/Go-cache roots, disposable module cache, exact source patch and production/test/harness/toolchain identities. It imports only the three required historical tips and their reachable objects when missing, creates no source refs, verifies each with `git cat-file`, builds independently pinned reference binaries and runs bounded fixtures before list-only discovery. Unused downloaded toolchains are omitted from the copied cache; the existing pinned Go executable is read only.

`--run` requires unchanged passing preparation and preserves the literal source-owned focused/full argv without narrowing or skipping tests. `--check` calls `nativeEvidenceRegressionRunCheck` and the existing independent discovery/selection functions through an external Go overlay, with raw event/diagnostic accounting retained by that contract. No historical failure allowlist was added. Full execution remains Plan16's task; this plan exercised admission and negative checking, not actual broad suites.

## Verification and precise outcomes

The final combined selector was:

```text
^(TestCLIInterruptedBuildResumesThroughForceRedispatch|TestCLIInterruptedBuildReadinessCleanup|TestPackedNPMAncestorIsolation|TestPackedNPMReleaseCandidateContract|TestDisposedBranchesAreGoneFromTheRepository)$
```

Executed in each standalone clone as `go test ./cmd [-race] -run <selector> -count=1 -timeout 180s -json`, with a 420-second outer bound. **Normal: 16 pass, 0 fail, 0 skip. Race: 16 pass, 0 fail, 0 skip.** These include five top-level tests and eleven subcases. Both discovery probes passed: **21 packages, 6,803 top-level cases, 372 focused cases**. Discovery is list-only and earns no full-suite execution credit. All four planned argv lists passed `nativeEvidenceRunContract`.

Task1's separate required selector passed normal and race at the original 120-second test bound, six cases per mode. Task2's initial three-test selector passed ten cases at the 180-second bound; the final combined pair supersedes its earlier test bytes. Eight script admission controls passed, including candidate/corpus/raw-evidence changes, missing historical commit objects, immutable failed-preflight rejection and actual CLI refusal before a broad launch. The `--check` integration rejected four explicitly synthetic narrowed-run receipts through the Go validator, as required.

The final actual interrupted build became ready after about **2.063 seconds normal / 1.904 seconds race**. Its readiness limit remains **10 seconds**. The historical full-suite timeout did not recur, so its cause remains unresolved; no contention or path-length explanation is claimed. Forced missing-sentinel cleanup is proved independently, rather than hiding that historical gap with a longer timeout.

## Source and durable evidence

- Expected base verified: `65dafe3e41618b6c26f8f98b0c29672d33406938`.
- Final preparation captured `bfa78aed458f6d351b9f017c373c39db21b0a3d5` plus its saved staged candidate patch. Task2's commit has exactly the same production/test/harness bytes; the captured binaries are not relabeled with the later commit.
- Production inventory: **1,711 entries**, `sha256:08cf994909e034ec397b454fcd848f0bdc7277fd2d5d13d3ef1191040eacdf0c`.
- Test corpus: **833 files**, `sha256:d35a29eb9a84d76932467f4b5a22f0cc9d4ab14720728db2bc03d3476752c360`.
- Live harness: `sha256:bafaed8881fa4853d20e030348bc26a56f6f65762ca92b7ace03d96572ca367c`; no live-host run performed by this plan.
- Go: **go1.26.5 darwin/arm64**. Read-only identities also capture Git2.55.0, Node26.7.0, npm11.19.0, Codex CLI0.154.0 and the installed Aether1.0.79 bytes. The shared narrow CalVault hotfix is not this candidate. Host model/effort/tool-schema observations are explicitly unavailable because no native host was launched.
- [Checked-in preflight receipt](evidence/gap-closure/regression-preflight.json).
- [Final immutable manifest](/Users/callumcowie/.aether-evidence/phase-204.2-gap-20260917T210924Z/plan-13/regression-preflight-final/manifest.json), SHA-256 `84e932dcf0151643f54e5b72a46674dd2d5a8fdf9ef019cd29aef081fc56d805`.
- Raw commands, exits, stdout/stderr, source patch, source inventories, reference candidate binaries, discovery and object-import receipts: `/Users/callumcowie/.aether-evidence/phase-204.2-gap-20260917T210924Z/plan-13/regression-preflight-final/normal/` and `/race/`.
- Installed bytes: `/Users/callumcowie/.aether-evidence/phase-204.2-gap-20260917T210924Z/plan-13/installed-inventory-normal.json` and `installed-inventory-race.json`.
- Script controls: `/Users/callumcowie/.aether-evidence/phase-204.2-gap-20260917T210924Z/plan-13/script-guard-checks.json` and `check-mode-negative-outcome.json`.

The three unchanged historical tips are `be1e160b09109c2af56f5df6c8a1adee5b002a5a`, `ecd98b5cc81d0c51b82c04139df14d90e38e4619` and `1cbf3615100203be7d59d49ffc2cf224367cedaa`. Final source refs were identical before/after preparation. Both clone gitdirs are independent with no object alternates.

## Failed attempts and deviations

- Three setup attempts exited before testing: the system Go1.26.1 was too old, then automatic cached-toolchain verification refused with the sum database disabled. Selecting the already-installed Go1.26.5 executable directly resolved this; no download or shared installation changed. Original logs remain under `task1-normal*` and the initial `task1-race*` names.
- First standalone preparation passed normal but its race command exited1 after22.43 seconds with empty output and no test events. A separately retained bounded retry exposed **`compile: writing output: ... no space left on device`**. Neither is a test pass. The failed manifest still rejects `--run`.
- Only owned disposable caches and the two failed standalone checkouts were removed after preserving raw evidence, source pins, patches, manifests and candidate binaries. Cache copies inherited read-only directory modes, so their directory modes were changed only inside those owned copies to permit deletion. Cleanup/retirement receipts remain in the evidence root. No unrelated storage was cleaned.
- The separately named final preparation uses the final source bytes and passed both modes. Earlier receipts were not overwritten. Final preparation and later full execution must still account for the measured host disk limit.
- The environment-control and installed-byte additions are within Task2's declared file ownership. No runtime file or source behavior outside these tests/script changed.

## Remaining obligations

The original ten-second full-suite timeout's cause is not established. Plan16 must run and account for the exact final integrated candidate's focused/full normal/race suites after a fresh source freeze; changes in later plans invalidate this preparation's candidate/corpus pins. No actual broad `--run` was executed here.

Current-candidate native/Claude proof, ANT-04 through ANT-07 qualification and Phase205 personal acceptance remain separate pending work. This SUMMARY records completed Plan13 execution, not phase completion.

The prior real-home NPM incident remains partially recovered: **31 additions, 64 changes and 17 removals remain unestablished**. This task neither restores that home nor declares it restored. Historical Plans01–09, reports/evidence and Phase205 were preserved; all92 protected files matched the root start manifest. No shared installation/hub/home/config update, new authentication, owner walkthrough, publication, original-checkout edit or branch switch occurred.

## Self-Check: PASSED

Both task commits resolve; all four declared deliverables exist, and the source diff is confined to them. Final production/test/harness inventories match the captured staged source bytes. Both preflight lanes and discovery probes passed; eight admission controls and four narrowed-receipt refusals passed. All37 artifact references in the checked-in report were hash-verified. `git diff --check` passed and all92 root-protected files matched. This checks report integrity and bounded engineering outcomes only; outstanding qualification is not converted to success.
