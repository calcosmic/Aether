---
phase: 204.2-codex-native-worker-lifecycle
plan: "17"
type: execute
wave: 14
depends_on: ["204.2-16"]
files_modified:
  - scripts/qualify-native-gap-regression.py
  - scripts/test_qualify_native_gap_regression.py
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-2/admission-preflight.json
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-2/ordinary-tracer.json
autonomous: true
gap_closure: true
requirements: ["ANT-04","ANT-05","ANT-07"]
reversibility_gate: true
must_haves:
  truths:
    - "Untracked production, test and embedded build inputs cannot escape candidate admission before preparation or in any prepared lane."
    - "Failed or insufficient resource/isolation preflight prevents expensive execution without deleting historical evidence."
    - "A fresh disposable installed ant-build ordinary tracer records one real bound Builder's edits, uncached check, durable terminal and exactly-once credit; missing mandatory proof remains incomplete."
  artifacts:
    - path: scripts/qualify-native-gap-regression.py
      provides: "Executable fail-closed qualification contract and controls"
    - path: scripts/test_qualify_native_gap_regression.py
      provides: "Executable fail-closed qualification contract and controls"
    - path: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-2/admission-preflight.json
      provides: "Source-bound new evidence; outcome must be independently checked"
    - path: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-2/ordinary-tracer.json
      provides: "Source-bound new evidence; outcome must be independently checked"
  key_links:
    - from: scripts/qualify-native-gap-regression.py
      to: cmd/codex_native_evidence_test.go
      via: "Complete candidate admission before source-owned regression execution"
---

<objective>
Admit the complete candidate and prove a resource-safe ordinary tracer.

Purpose: The recorded program must be the program actually tested, and tests must have enough isolated storage to start.
Output: The artifacts listed below; no implementation or passing qualification is claimed by this plan.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SCOPE-AMENDMENT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-VERIFICATION.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP-EXECUTION-REVIEW.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP2-PLANNING.md
</context>

## Execution boundaries and reversibility

D-01–D-10 refer to the existing SYNTHESIS labels for unnumbered CONTEXT decisions, not new owner decisions. Follow CONTEXT and SCOPE-AMENDMENT unchanged. Existing Go ExecutionBinding, reservation/bind/terminal journal and authoritative finalizer remain the only work/credit authority. Keep the native/subprocess distinction and public $ant-* names. Read the current source and prior summaries; old line numbers are navigational hints.

Before any execution, verify the root's protected-file inventory. Preserve all Plans 01–16, summaries, original reports and all evidence/gap-closure receipts byte-for-byte. New raw runs use a unique absolute external directory recorded as AETHER_GAP2_EVIDENCE_ROOT; new checked-in receipts use evidence/gap-closure-2. Plan23 alone archives the current canonical verification before its authorized refresh; no old archive is replaced. Keep the active branch, unrelated changes, shared installation/hub/configuration/homes, CalVault and Phase205 unchanged. No publication, login, fresh credentials or owner walkthrough. Existing authorized auth may be used only by the established disposable fixture; unavailable auth stays incomplete.

Reversibility gates are enabled. Changes are scoped reversible commits; no shared store migration, reset, blanket cleanup, process kill or installation. Before expensive commands measure storage and pass the appropriate isolated preflight. Never repurpose the parent HOME or CODEX_HOME; child environment maps own disposable paths. Retain raw evidence, binaries, manifests and hashes before retiring only demonstrably owned disposable caches. If capacity or host observation cannot be obtained within these bounds, record the measured blocker and stop dependent operations; do not claim completion.

Pin source revision/status, complete admitted production/embedded/test inputs, capture harness, regression contract, Go executable/version/environment, actual candidate binary/build argv, host executable/version/model/effort/configuration/full tool definitions and installed skill/support/agent bytes. The shared installed narrow CalVault hotfix has historical SHA-256 72707dc5d5fa34304c4a4b9455416a440eebad4d22aaede0a373ccf5dc7d8b90; measure it read-only and never relabel it as this candidate. Preserve the old 165db28c/f9e32ca0/cf51e9ac/f8315369/4729f23 identities. Reporting-only commits can cite independently equal inputs but never change a captured binary's identity.

Any production or capture change requires fresh affected live proof and the final full matrix. Any corpus or contract change invalidates focused/full regression credit. Parser-only replay can diagnose immutable original logs with separate capture/validator identities; it cannot replace Plan22's required new final-candidate capture. A fix during Plans22/23 returns through freeze, full matrix and all four current-candidate runs. No EXPECT_INCOMPLETE or weakened selector is acceptance.

Security enforcement, new research and Nyquist scaffolding are disabled by the phase configuration; meaningful focused normal/race controls remain required. No package install, external service integration, ORM migration or UI is introduced. Local patterns and the existing SYNTHESIS are reused. Never claim the previous real-home NPM incident restored: 31 additions, 64 changes and 17 removals retain unestablished prior state.

## Artifacts this phase produces

- scripts/qualify-native-gap-regression.py
- scripts/test_qualify_native_gap_regression.py
- .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-2/admission-preflight.json
- .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-2/ordinary-tracer.json

<interfaces>
inventory(source), prepare(args, source, root), validate(source, root), run(args, source, root) and check(args, source, root) in scripts/qualify-native-gap-regression.py are the existing admission path. argv_for reads nativeEvidenceFocusedSelection from the Go contract. PREFLIGHT selects interrupted-build readiness/cleanup, packed-NPM ancestor/contract and disposed-object tests. Existing record() retains raw output and stops only its process group.
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Reject unadmitted build inputs at every candidate boundary</name>
  <files>scripts/qualify-native-gap-regression.py, scripts/test_qualify_native_gap_regression.py</files>
  <read_first>scripts/qualify-native-gap-regression.py; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP-EXECUTION-REVIEW.md; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-13-SUMMARY.md</read_first>
  <behavior>Start with failing controls for omitted untracked production/test inputs; mutation of a prepared lane must be rejected even when its tracked inventory is unchanged.</behavior>
  <action>Per D-03/D-04/D-08, reproduce CR-01 with disposable Git repositories and mocked suite-launch sentinels, then choose the conservative admission policy: reject nonignored untracked production, test and relevant embedded inputs before cloning/building and at every validate call. Preserve tracked dirty patches and hashes as already supported. Explicitly admit only proven non-build metadata (such as the existing .gsd metadata), not entire arbitrary untracked directories; refuse an unknown path unless its exclusion is justified by the actual build inputs. Account for tracked deletions, symlinks, go:embed inputs and ignored inputs actually consumed by the build; a consumed file must be pinned/copied or rejected. Reuse one policy in source and both lanes and revalidate immediately before each of the four suite launches and afterward. Add stdlib unittest coverage without introducing a dependency.</action>
  <acceptance_criteria>Untracked .go, _test.go and embedded-asset controls before prepare, and production/test/embed additions in either lane after prepare, fail before the launch sentinel. Allowed unrelated metadata changes leave identity stable; tracked mutation and missing consumed files still refuse. No submitted file is silently dropped.</acceptance_criteria>
  <verify><automated>python3 -m unittest discover -s scripts -p 'test_qualify_native_gap_regression.py' -v</automated></verify>
  <done>Untracked .go, _test.go and embedded-asset controls before prepare, and production/test/embed additions in either lane after prepare, fail before the launch sentinel. Allowed unrelated metadata changes leave identity stable; tracked mutation and missing consumed files still refuse. No submitted file is silently dropped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make storage admission explicit and retain a fresh normal/race preflight</name>
  <files>scripts/qualify-native-gap-regression.py, scripts/test_qualify_native_gap_regression.py, .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-2/admission-preflight.json</files>
  <read_first>scripts/qualify-native-gap-regression.py; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-16-SUMMARY.md; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-13-SUMMARY.md</read_first>
  <behavior>Low-space and mid-copy/link ENOSPC cannot leave passed=true or erase the first failure; successful capacity permits existing isolated fixture preflight.</behavior>
  <action>Per D-05/D-08, add a recorded storage estimate before module copying, clone creation and normal/race linking. Base required bytes on measured owned cache/copy/build sizes and the prior race-link exhaustion, retain estimate inputs and free bytes, and refuse uncertain/insufficient capacity honestly; do not invent a universal threshold. Unit-test low capacity and mid-operation ENOSPC with retained failed manifest/no broad-launch behavior. Use a fresh external preflight root, separate normal/race caches and the existing disposable environment; never reuse the failed Plan16 manifest or let a later inventory error discard its original diagnostic. Run the existing full normal/race fixture preflight and discovery after admission. If reclaiming is necessary, only remove identified current-task disposable caches after preserving evidence, retaining a cleanup receipt; otherwise stop with the capacity gap. The final regression will prepare anew after the final source freeze.</action>
  <acceptance_criteria>The resource refusal preserves original error and failing manifest; low-space tests launch no broad suite. A successful actual preflight proves both lanes, isolation, required historical objects and discovery with exact source/toolchain pins. If unavailable, receipt is incomplete and the tracer/dependent expensive commands do not start.</acceptance_criteria>
  <verify><automated>python3 -m unittest discover -s scripts -p 'test_qualify_native_gap_regression.py' -v
python3 scripts/qualify-native-gap-regression.py --prepare --evidence-root "$AETHER_GAP2_EVIDENCE_ROOT/admission-preflight"</automated></verify>
  <done>The resource refusal preserves original error and failing manifest; low-space tests launch no broad suite. A successful actual preflight proves both lanes, isolation, required historical objects and discovery with exact source/toolchain pins. If unavailable, receipt is incomplete and the tracer/dependent expensive commands do not start.</done>
</task>

<task type="auto">
  <name>Task 3: Capture a thin installed ordinary build/check/credit tracer</name>
  <files>.planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure-2/ordinary-tracer.json</files>
  <read_first>.planning/phases/204.2-codex-native-worker-lifecycle/204.2-10-SUMMARY.md; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-15-SUMMARY.md; cmd/codex_native_worker_live_test.go</read_first>
  <action>Per D-01/D-05/D-09/D-10, after the successful current preflight freeze the current admitted bytes and run ordinary through the existing disposable installed ant-build fixture. Preserve actual named child task-path/UUID, runtime reservation/binding, child-authored patch, uncached required check, terminal, aggregate credit and byte-stable replay; audit every parent command for coordination-only attribution. Record measured missing config/schema proof as incomplete overall provenance even if ordinary behavior passes. This is the early vertical tracer before expanding repairs; do not add a Watcher or have the parent implement/check the assignment. Retain failed attempts without indefinite unchanged retries.</action>
  <acceptance_criteria>One real Builder demonstrates the complete runtime path or a source-pinned failing receipt records the exact missing link. Exit zero, copied historical proof, parent edits/checks or output without journal credit do not qualify.</acceptance_criteria>
  <verify><automated>AETHER_CODEX_NATIVE_LIVE=1 AETHER_CODEX_NATIVE_SCENARIOS=ordinary AETHER_CODEX_NATIVE_EVIDENCE_DIR="$AETHER_GAP2_EVIDENCE_ROOT/ordinary-tracer" go test ./cmd -run '^TestCodexNativeWorkerFreshHost$' -count=1 -json -timeout 30m</automated></verify>
  <done>One real Builder demonstrates the complete runtime path or a source-pinned failing receipt records the exact missing link. Exit zero, copied historical proof, parent edits/checks or output without journal credit do not qualify.</done>
</task>
</tasks>

<verification>
Retain raw exact argv, exit, stdout/stderr and source identities for every executed check. Run behavioral source controls normally and with -race; live runs retain their explicit longer bounds and zero omitted requested cases. Resource or host barriers leave dependent checks unexecuted and gaps explicit. Plan22 supplies the complete frozen live matrix; Plan23 supplies all four final regression runs and the explicit standard verdict. Earlier narrow passes never substitute for those final checks.
</verification>

<success_criteria>
Untracked production, test and embedded build inputs cannot escape candidate admission before preparation or in any prepared lane.
Failed or insufficient resource/isolation preflight prevents expensive execution without deleting historical evidence.
A fresh disposable installed ant-build ordinary tracer records one real bound Builder's edits, uncached check, durable terminal and exactly-once credit; missing mandatory proof remains incomplete.
Task artifacts and SUMMARY completion do not establish ANT completion. Missing mandatory proof remains a gap.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-17-SUMMARY.md with exact source/evidence identities, task checks and honest remaining gaps. Do not mark pending ANT requirements complete or alter shared trackers; the root owns disposition.
</output>

