---
phase: 204.2-codex-native-worker-lifecycle
plan: "03"
type: execute
wave: 3
depends_on: ["204.2-02"]
files_modified:
  - cmd/codex_native_context.go
  - cmd/codex_native_context_test.go
  - cmd/codex_native_worker.go
  - cmd/codex_build.go
autonomous: true
requirements: [ANT-06]
must_haves:
  truths:
    - "Actual native launch bytes contain the runtime capsule, exact brief, matched skills, effective steering, prior handoff and applicable owner answer exactly once."
    - "Missing required brief content is refused; a request above the 2 MiB limit is refused without truncating prompt or accepting partial JSON."
    - "Unicode and exact brief bytes survive composition and prompt hashes use the original bytes, without rounding or normalization."
    - "A current context delta goes only to its bound child; a stale or unacknowledged delivery cannot be reported as received."
  artifacts:
    - path: cmd/codex_native_context.go
      provides: "Runtime-owned exact native prompt and decision delta assembly"
      contains: "composeCodexNativePrompt"
    - path: cmd/codex_build.go
      provides: "Answer IDs already represented in the immutable manifest capsule"
      contains: "ContextDecisionIDs"
    - path: cmd/codex_native_context_test.go
      provides: "Actual request composition, size and delivery tests"
      contains: "TestCodexNativeContextDelivered"
  key_links:
    - from: cmd/codex_native_worker.go
      to: cmd/codex_native_context.go
      via: "Reserve and context routes consume the one runtime prompt assembler"
      pattern: "composeCodexNativePrompt"
    - from: cmd/codex_native_context.go
      to: cmd/codex_build.go
      via: "Manifest capsule/brief/skill and included answer identity"
      pattern: "ContextDecisionIDs"
---

<objective>
Deliver the complete job context to the worker that actually receives the assignment. In plain English, the helper sees the owner's instructions and prior work rather than only a short task description.

Purpose: Preserve the existing context/skill authority and close the missing native assembly boundary, per D-04/D-05.
Output: Exact runtime-produced native prompt bytes, digest and scoped delivery facts.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/research/ant-codex-parity-2026-09-16/GSD-INTEGRATION.md
</context>

<interfaces>
Existing codexBuildManifest.ContextCapsule and codexBuildDispatch.BriefPath/Brief/SkillSection are the source; attachBuildDispatchContext intentionally avoids duplicated steering in briefs. NEW ContextDecisionIDs []string on codexBuildManifest records which ordinary answer IDs the capsule already contains. NEW composeCodexNativePrompt returns exact bytes, digest and included answer IDs. Native binding's context_delivery_ids comes from Plan 02.
</interfaces>

## Artifacts this phase produces

NEW: cmd/codex_native_context.go/test.go; composeCodexNativePrompt; ContextDecisionIDs; context operation returning a bound delivery envelope.
EXISTING: capsule assembly and live skill matching remain in codex_build.go and skills.go; no separate budget, signal resolver, decision store or handoff ledger is created.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Send the complete runtime context in the real native launch request</name>
  <files>cmd/codex_native_context.go, cmd/codex_native_context_test.go, cmd/codex_native_worker.go, cmd/codex_build.go</files>
  <read_first>cmd/codex_build_manifest_context_test.go; cmd/codex_build.go buildCodexBuildManifest/attachBuildDispatchContext/composeBuildManifestBrief; cmd/skills.go matcher; cmd/discuss.go clarifiedIntentRenderResult; .claude/commands/ant/build.md prompt assembly; 204.2-02-SUMMARY.md.</read_first>
  <behavior>Unique marker for capsule, brief, skill, effective FOCUS/REDIRECT, prior handoff and answer -> present once in submitted launch bytes. Expired/revoked/quarantined signal -> absent. Present brief_path -> exact file bytes; absent path -> brief fallback; unreadable/mismatched path -> refusal. Capsule answer -> no duplicated answer delta.</behavior>
  <action>Extract the tracer's prompt assembly into composeCodexNativePrompt and make native reserve consume that exact returned payload, not reconstruct it in markdown. Preserve ordering: capsule, exact runtime-issued brief bytes, skill section, newer applicable runtime-rendered answer delta. Capture ordinary answer IDs when the manifest capsule is built so later answer rendering removes only already represented answers, never parses prose heuristically. Verify saved brief identity and refuse unreadable or changed required files instead of silently taking a different fallback. Keep the existing context budget and effective pheromone/handoff resolvers; do not invent a second steering resolver. Add populated fixture markers using the existing manifest-context fixture pattern and assert on the actual request passed to the native launch boundary, not merely the manifest JSON. Test empty optional context, required brief absence, multiline/Unicode/quote bytes and valid exact 2 MiB request versus one extra byte. The limit applies to the strict request envelope; do not truncate a runtime-generated prompt to make it fit.</action>
  <acceptance_criteria>The downstream native request contains each intended section once, excludes ineffective steering and prior duplicate answers, and its digest equals the exact delivered bytes. Missing required data or oversized/partial JSON refuses before reservation.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexNativeContext(Delivered|BriefIdentity|AnswerDedup|Bounds|ExactBytes)$' -count=1 -timeout 60s</automated></verify>
  <done>Native workers receive the full authorized context through one real assembly path, including all previously missing sections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relay current context updates to the intended live child</name>
  <files>cmd/codex_native_context.go, cmd/codex_native_context_test.go, cmd/codex_native_worker.go</files>
  <read_first>Task 1 prompt contract; Plan 02 native observation and delivery fields; cmd/handoff_decisions_cmd.go renderClarifiedIntentSection; cmd/build_execution_binding_test.go stale identity cases.</read_first>
  <behavior>New answer after the saved capsule -> one runtime-bound delta; same delivery replay -> same envelope; wrong child or superseded attempt -> refusal; no completed native send event -> not acknowledged. Launch prompt identity remains immutable after a later delta.</behavior>
  <action>Add the read-only context route for a saved assignment and child. It emits only the currently applicable delta plus a digest, included answer IDs and exact target binding; it cannot mutate the original reserved prompt digest. Have the host send the exact returned bytes using its native follow-up/message operation and acknowledge delivery through observe only after a successful observed send, with its real event identity. Expose delivered versus awaiting-delivery facts without treating a rendered message as worker consultation or useful influence. Revalidate attempt/workspace/child at context generation and acknowledgement. Test stale goal/session/attempt, wrong child, a duplicate acknowledgement, and a changed payload under the same delivery ID. Keep the scope hook ready for the exact native-question renderer added in Plan 04; ordinary clarification behavior already works here.</action>
  <acceptance_criteria>Only the bound child receives a current delta, successful send evidence is required for delivered status, and retry neither duplicates the answer nor rewrites the original prompt. Read-only context calls preserve state byte-for-byte.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexNativeContext(Delta|DeliveryBinding|ReadOnly)$' -count=1 -timeout 60s</automated></verify>
  <done>An ongoing native worker can receive a current runtime-scoped clarification without duplicated or cross-worker context.</done>
</task>
</tasks>

<verification>
Run the actual native request tests and retained manifest capsule tests by discovered names. A disconnected composeCodexNativePrompt call must fail the delivered-byte test; record the RED or isolated negative control. Shared finalizer tests run in Plan 05; this plan owns no finalizer/journal edits.
</verification>

<success_criteria>
ANT-06 prompt clauses are tested on delivered requests; context identity remains immutable and new answer deliveries are scoped and acknowledged.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-03-SUMMARY.md.
</output>

