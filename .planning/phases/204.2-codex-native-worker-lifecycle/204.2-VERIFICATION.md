---
phase: 204.2-codex-native-worker-lifecycle
verified: 2026-09-18
status: gaps_found
score: 0/5 fully qualified success criteria; ordinary live scenario passes, mandatory matrix and regression remain incomplete
requirements_passed: []
requirements_pending: [ANT-04, ANT-05, ANT-06, ANT-07]
tested_live_source: f8315369d7f7990c7842da9c80ba5d34df0b604f
regression_preparation_source: 4729f23aa56f997858417c44a4dd93bf9fd82c56
full_regression_source: none on current corpus
qualification: incomplete
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "0/5 fully qualified success criteria"
  gaps_closed: []
  gaps_remaining:
    - "Complete actual native matrix and host provenance"
    - "Current-candidate focused/full normal and race accounting"
  regressions:
    - "New qualification predicates contain CR-01, CR-02 and CR-03 proof-contract defects"
gaps:
  - truth: "All required actual native lifecycle scenarios qualify on the current candidate"
    status: failed
    reason: "Six of eleven cases remain incomplete: review, question, early-resume, partial-resume, cancellation and Claude comparison. Ordinary passes narrowly; four cases are observations."
    artifacts:
      - path: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/native-qualification.json
        issue: "qualification_complete is false"
    missing:
      - "Corroborated review terminal/checks, exact uncontaminated context/answer delivery and ACK, attributable fresh-parent recovery, confirmed cancellation interval, and actual comparison proof"
      - "Complete measured host configuration and tool-schema provenance"
  - truth: "Current-candidate focused checks and complete normal/race suites have source-bound execution accounting"
    status: failed
    reason: "Race preflight failed during linking with no space left on device; all four required runs are absent."
    artifacts:
      - path: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/native-regression.json
        issue: "runs is empty; focused_normal, focused_race, normal and race are missing"
    missing:
      - "Passing fresh isolated preflight followed by the exact focused/full pair and frozen seventeen-name comparison"
  - truth: "Frozen regression identity includes all actual build inputs"
    status: failed
    reason: "CR-01: tracked-only inventory omits untracked production/test inputs before preparation and in prepared lanes."
    artifacts:
      - path: scripts/qualify-native-gap-regression.py
        issue: "inventory uses git ls-files; recorded untracked status is not an admission refusal"
    missing:
      - "Pin/copy or reject untracked build inputs at preparation and validation, with negative controls"
  - truth: "Required host configuration and full tool-schema proof cannot be replaced by unrelated hashed artifacts"
    status: failed
    reason: "CR-02: nativeGapHostIdentity verifies hashes and inventory membership, not configuration/schema content or provenance."
    artifacts:
      - path: cmd/codex_native_gap_evidence_test.go
        issue: "Arbitrary synthetic bytes pass the positive host-provenance control"
    missing:
      - "Versioned semantic capture validation and rejection of empty, unrelated, names-only or unavailable schema captures"
  - truth: "Cancellation absence-of-write proof refuses malformed or incomplete child records"
    status: failed
    reason: "CR-03: JSON decode failures are silently skipped; a truncated activity record need not invalidate NoPostAckWrites."
    artifacts:
      - path: cmd/codex_native_gap_controls_test.go
        issue: "Malformed child-log records do not mark the interval unproved"
    missing:
      - "Fail-closed parsing and validated interval end, with truncated and malformed activity controls"
  - truth: "Retained recovery checkpoints independently establish the actual interruption boundary on replay"
    status: partial
    reason: "WR-01: spawn identity is re-derived, but saved attempt/state semantics and the scenario cut point are not."
    artifacts:
      - path: cmd/codex_native_gap_recovery_test.go
        issue: "Hash-valid arbitrary attempt bytes and no state snapshot satisfy the positive control"
    missing:
      - "Decode required snapshots and re-derive terminal-before-stage or spawn-before-bind semantics"
---

# Phase 204.2: Codex Native Worker Lifecycle Verification

**Status: gaps_found.** Plan 15's frozen eleven-case matrix retains one passed, four observed and six incomplete outcomes. Plan 16's current-candidate normal isolation preflight passed; race compilation failed with `mapping output file failed: no space left on device`. Failed preflight prohibited the final focused/full normal/race runs. ANT-04 through ANT-07 remain pending.

In plain English: a real ordinary Builder now demonstrably edits, checks and returns credited work. Other promised worker behaviors still lack complete proof, and this machine ran out of room before the final test pair could start. Completing this report does not complete qualification.

## Historical preservation

Before replacing this canonical report, the complete original was copied verbatim to [original-204.2-VERIFICATION.md](evidence/gap-closure/original-204.2-VERIFICATION.md). Both original and archive matched the execution-start manifest's SHA-256 **`6dc0c38f55ca69f8367d15ab503cb522fa031bb39b427130697c09f6b34f5170`**. An existing archive with different bytes would have been refused. That archive preserves all original observations and failed runs, including the old 165db28c, f9e32ca0 and cf51e9ac identities; none becomes current proof.

The root [execution-start manifest](/Users/callumcowie/.aether-evidence/phase-204.2-gap-20260917T210924Z/execution-start.json) covered 92 protected files. All matched before this authorized canonical replacement. Final integrity compares the original report through its archive and all other protected files at their unchanged paths. Historical Plans 01–09, reports, evidence, reviews and Phase 205 are unchanged. Shared STATE, ROADMAP and REQUIREMENTS remain the root orchestrator's responsibility.

## Candidate and evidence identity

| Input | Exact identity and scope |
|---|---|
| Plan 16 preparation | `4729f23aa56f997858417c44a4dd93bf9fd82c56`, initially clean isolated branch; no branch switch |
| Plan 15 actual capture and frozen validator | `f8315369d7f7990c7842da9c80ba5d34df0b604f`; equal current production, corpus and live-harness bytes independently checked |
| Production | 1,711 files; `sha256:08cf994909e034ec397b454fcd848f0bdc7277fd2d5d13d3ef1191040eacdf0c` |
| Test corpus | 838 files; `sha256:cf204ea86a8f9c8fc50dd460a6d2fd27e1dc00d2a7931bd0b0b0a9ee238b0695` |
| Live harness | `sha256:780038c2155ba1000ab8ebfe742e2043a0052c793d69263d2159bb20160b91dd` |
| Regression/evidence contract | `sha256:0b68a3ac17b875e35d497bb6a0cf26da71ec54a825b201456c0352655e2df476` |
| Live candidate binary | `sha256:feacd1ae45f3399dc1ce54e1a3466209d9fb6885c86747f9cbf06a2d9056a1de`, version 1.0.79, `go build -buildvcs=false -ldflags '-X github.com/calcosmic/Aether/cmd.Version=1.0.79'` |
| Plan 16 normal reference binary | `sha256:f425e2a8713658d50815917aeb5988a60c07be0ebad0d4724c322565b670f4f3`; independent clone/build, no live qualification |
| Plan 16 race-lane reference binary | `sha256:2db725efe521eaec21471e2a43de4213370f2d86f8d3e07ecfe1efccc9bf4222`; ordinary candidate build passed, race test executable link failed |
| Go | go1.26.5 darwin/arm64; selected executable `sha256:3925fc3221ac440ebf7c35361ff663bed0c7bdb2e0a157b75fe993607ffe0a19`; full Go environment and exact build argv in preparation manifest |
| Actual hosts | codex-cli 0.154.0, gpt-6-astra/medium parent; Claude Code 2.1.272. Actual argv/configuration/installed bytes are pinned per Plan 15 case. Complete exported tool definitions remain unavailable; observed tool names are not a full schema. |
| Shared installed Aether | Separate narrow CalVault hotfix1.0.79, `sha256:72707dc5d5fa34304c4a4b9455416a440eebad4d22aaede0a373ccf5dc7d8b90`; read-only identity, never replaced or labeled this candidate |

The live candidate keeps its original build identity. Separate clone paths can produce different binary bytes without changing source; equal source is not a relabeling of binaries. Reporting-only commits change no production, test or capture bytes. Production/capture changes require new live qualification; corpus changes invalidate full-run credit.

Current evidence: [qualification](evidence/gap-closure/native-qualification.json), SHA-256 `3af18c5a49d7f98b5a8531b11ec30a95a9bebfc84a000784d6128abe60203917`; [regression](evidence/gap-closure/native-regression.json); [Plan 15 summary](204.2-15-SUMMARY.md); [Plan 16 external root](/Users/callumcowie/.aether-evidence/phase-204.2-gap-20260917T210924Z/plan-16). The external `live-readback.json` records the executor's bounded engineering review and verifies **12,785 unique artifact path/hash pairs**, zero mismatches. Integrity proves preservation, not successful behavior.

## Goal-backward success criteria

| Criterion | Current evidence | Disposition |
|---|---|---|
| SC1 — installed named Builder, accepted work, edits/checks/credit once | Ordinary actual native child Brick-46 edited Clamp, ran an uncached check, returned a corroborated terminal; aggregate runtime credit and finalizer replay observed; one native spawn, no parent substitution | **Live scenario passed; full criterion qualification incomplete** because mandatory current regression and complete host provenance are missing. No universal native success claim. |
| SC2 — independent Watcher and proportionate ordinary team | Distinct Keen-6 Watcher wrote testing/quality findings; ordinary scenario used one Builder. Review validator retains incomplete actual terminal/check evidence for Brick-46 and unclassified child commands | **Gap with substantive independent review observations.** No unconditional Watcher floor or new defect discovery claimed. |
| SC3 — complete context and exact scoped answers | Exact runtime-issued bytes/digests and deterministic rejection controls exist; current question child was launched with `fork_turns=none`; actual causality receipt lacks plaintext launch/send proof, completed-send ACK and corroborated uncached child check | **Gap.** Neither encrypted strings, echoed hashes, exit 0 nor prior contaminated-answer behavior proves exact uncontaminated delivery and influence. |
| SC4 — finished work survives fresh-parent resume without duplicate credit | Early/partial actual fresh public resume observed; partial retains finished worker and launches one helper for unfinished work; spawn-gap preserves ambiguous launch without replacement/credit | **Gap.** Early/partial parent operations remain unclassified; expected spawn-gap preservation does not prove completed-work success. |
| SC5 — actual permissions/cancellation/workspace/nesting/activity/usage | Workspace-write and read-only controls now contain target-correlated attempted writes/denials and nested response; missing-skill refusal observed; cancellation remains request-only | **Gap.** Missing cancellation ACK/runtime terminal/no-post-ACK-write interval and unavailable full tool schema prevent qualification. Usage/cost remain unreported, never zero. |

Score is **0/5 fully qualified criteria**, with the narrower ordinary live pass explicitly preserved. Requirement completion is not inferred from that score or from the number of reports delivered.

## Requirement dispositions and implementation links

| Requirement | Actual production chain and checks | Disposition |
|---|---|---|
| ANT-04 — governed native dispatch | Installed ant-build → Codex guide → accepted Go manifest → non-launching native bridge → exact child bind/record → shared stage/finalizer. `codex_native_worker.go`, `build_worker_run.go`, `codex_native_finalize.go`; admission/causal controls and ordinary live pass | **Pending:** ordinary proof is materially improved, but required review/current regression and complete provenance remain missing. |
| ANT-05 — durable result and recovery | Append-only per-child journal → public pause/resume → current attempt reconciliation → shared finalizer. `codex_native_recovery.go`, `session_flow_cmds.go`; deterministic interruption/replay controls, early/partial/spawn-gap captures | **Pending:** partial observations do not close unclassified fresh-parent operations or the ANT-05 assumption. |
| ANT-06 — complete helper context | Canonical context resolver → exact launch/context envelope → protected scoped question/answer → successful-send-only ACK. `codex_native_context.go`, `codex_native_decisions.go`, `pending_decision.go`; exact-context negative controls | **Pending:** question case has no qualifying child edit/check/credit, ACK or independently corroborated plaintext. |
| ANT-07 — honest capabilities and visibility | Actual native observation projection and capability admission, explicit unsupported refusal, no invented PID/usage. `pkg/codex/platform_contract.go`, `codex_visuals.go`, native recovery | **Pending:** measured sentinel behavior is useful, but cancellation and version/configuration-dependent assumptions remain unresolved. |

Only the existing shared finalizer grants credit; inspect/context/stage do not. Valid receipt-backed BUILD credit may survive failed checks, while VERIFIED advancement remains blocked. Historical worker-count/fix-attempt/lifecycle failures cannot discharge ANT scope.

## Actual child and boundary readback

Ordinary Builder **Brick-46 / `01a0b1e1-a7b5-78a2-97f2-6978c663feb7`** has raw child calls for the failing baseline check (line 48), literal Clamp patch (53), uncached post-edit check (58), and ordinary check (68). The top-level receipt corroborates terminal, shared runtime credit and stable finalization replay. Nested worker projections are not independent credit authorities. This is the actual prepared accepted fixture's success, not live-planning/full-workflow proof.

Review Builder **`01a0b1eb-795a-7ff3-9e91-1cc99541f222`** differs from Watcher **Keen-6 / `01a0b1f2-ecf3-7fc0-a9b4-7106df931966`**. The Watcher's raw log contains an uncached `go test ./... -json -count=1` at 54 and testing/quality ledger calls at 75/80. Findings describe five boundary/interior cases and the ordered-bound assumption `low <= high`; reversed bounds remain unspecified. These are useful findings, not a newly discovered defect. The frozen validator flags `git diff --check` and review-ledger commands as unclassified, so its incomplete outcome is preserved; this reporting task does not repair the parser or turn raw command presence into a passing validator result.

Question child **Forge-63 / `01a0b202-3dd9-7112-a52a-869f1b731e6b`** has no qualifying edit/check/terminal. Its `context-causality.json` explicitly lists unavailable spawn/send plaintext, missing completed-send ACK and missing corroborated uncached check. The no-history launch removes one known contamination mechanism but does not alone establish exact payload delivery or causal answer influence. The answer remains a fixture response, not owner testimony.

Early and partial captures retain actual checkpoint-kill/wait records and fresh public resume. Partial's first worker/source remain unchanged, one unfinished helper is launched, and finalization replay is stable; parent code-mode operations remain unclassified. Spawn-gap is **observed expected preservation**: a fresh parent leaves the ambiguous reservation unresolved, without replacement spawn or credit. It is not a finished-helper success scenario.

Workspace-write proves inside write allowed/outside target denied; read-only proves inside/outside attempted writes denied. Both observe actual nested responses. These establish only those measured host profiles; per-child permission selection, separate runtime-owned native worktrees and Aether-governed nesting remain unsupported/refused. Missing-skill is an expected refusal with no launch/source edit and unchanged durable inventory.

Cancellation child **`01a0b206-fdd0-76d3-8e75-505f4a77d07f`** has a correlated request but no actual cancelled acknowledgement, corroborated runtime cancelled terminal or proved no-post-ACK-write interval. An interrupt request is not confirmed cancellation. Claude comparison exits1 under existing authentication, with zero helpers, no child edit/check or credit. No login/authentication change was attempted; its unavailable model observation is not invented.

## Final regression accounting and host limitation

Plan 13's passing normal/race preflight was read before any broad launch. Plan 16 prepared fresh **standalone** normal/race clones with independent Git directories and no alternates, separate disposable homes/configuration/temp/npm/Go caches, and disposable module cache. All three required disposed commit objects were imported and verified in both clones, without source-ref changes. Both candidate builds passed. Normal preflight passed **5 top-level /16 total cases, 0 fail/skip**, including readiness/cleanup, force redispatch, disposed objects and packed-NPM ancestor isolation.

| Plan 16 operation | Exact outcome |
|---|---|
| Normal preflight | exit 0, 47.886s; bounded selector, not broad-suite credit |
| Normal independent discovery probe | exit 0; 21 packages, 6,817 top-level Test/Example cases, 386 focused cases; list-only |
| Race preflight | exit 1, 70.310s; linker reports `mapping output file failed: no space left on device`; **0 executed tests** |
| Preparation | exit 1, 228.898s; immutable `passed:false` manifest |
| Required script `--run` | exit 1: `failed preflight prohibits broad runs`; no focused/full launch |
| Required script `--check` | exit 1: same failed-preflight admission refusal; not successful parser validation |
| Focused normal/race, full normal/race | **Not executed.** No raw full-suite exit, current failure set or complete execution accounting exists. |

The literal full contracts remain `go test ./... -count=1 -timeout 90m -json` and `go test ./... -race -count=1 -timeout 90m -json`, serial in independent clones. They were not narrowed, skipped, replaced by fixtures or credited from earlier corpora. Race discovery was not reached. No duplicate/missing/out-of-order/diagnostic-echo full-run validation can be claimed without raw full runs; the existing parser remains unchanged.

Raw commands, environments, argv, stdout/stderr, exits, candidate hashes and discovery are under `plan-16/final-regression/`; outer invocations are `prepare.json`, `run.json`, `check.json`. Free storage at the first observation was approximately 6.05 GB. The failed compiler's exact output is `final-regression/race/fixture.stdout`, not fabricated test failures. Only the owned failed race compiler cache was retired after immutable raw evidence was saved (`cache-retirement.json`). No unrelated storage cleanup or shared-home restoration was performed. A fresh complete preflight and final pair remain required on a host with enough space.

### Frozen historical comparison

The new regression receipt pins the original baseline verification and **exact seventeen** baseline diagnostic artifacts. Each baseline artifact hash was checked. Current normal/race disposition for every name is **not executed / comparison unavailable**; `runs` is empty and no inherited exception is granted. Fifteen historical failures in the archived f9 runs remain history, not this candidate's residuals.

| Frozen historical test ID | Current disposition |
|---|---|
| TestAuditCatalogGolden | Required pass; not executed in final pair |
| TestBuildStartLegacyHelpersRetired200 | Comparison unavailable |
| TestCodexBuildPlanOnlySpawnBudgetSeparatesCasteBudgetFromWorkerCount | Comparison unavailable; affected behavior cannot qualify ANT |
| TestCompletionPacketSchemaMatchesStructs | Required pass; not executed in final pair |
| TestCurrentVocabulary199 | Comparison unavailable |
| TestFailedCheckSendsExactlyOneBuilderFixAttempt | Comparison unavailable |
| TestFixAttemptIsCountedSeparately | Comparison unavailable |
| TestFixAttemptNeverOverwritesTheFirstResult | Comparison unavailable |
| TestGoSourceHintsMatchCobraContracts | Comparison unavailable |
| TestGoldenBuildVisualOutput | Comparison unavailable |
| TestGoldenContinueVisualOutput | Comparison unavailable |
| TestHumanFacingOutputGoesThroughWriteVisualOutput | Comparison unavailable |
| TestNoSecondAutomaticFixAttempt | Comparison unavailable |
| TestPhase199GateReceipt | Comparison unavailable |
| TestPlanningAdversarial200 | Comparison unavailable |
| TestPlanningPublicPaths200 | Comparison unavailable |
| TestResolveTestCommand_GoProject | Comparison unavailable |

The repaired wiring gate is also a required pass, never an added historical exception. Native evidence, interrupted readiness/cleanup, disposed objects and packed NPM failures remain outside the baseline. Prior f9 normal/race each exited1 with 19 top-level/27 total failed cases on 6,797 discovered tests; cf51 changed the corpus afterward. The original archive retains their substantive diagnostics. Those runs do not qualify today's6,817-test corpus.

### Required gate and canonical routing

Both required invocations used `AETHER_CODEX_NATIVE_REQUIRE_REGRESSION=1 go test ./cmd -run '^TestCodexNativePhaseEvidence$' -count=1 -timeout 60s` in a disposable child environment. Task1 exited1 after52.402s; Task2 exited1 after21.553s. Their raw stdout/stderr and exact invocations are `strict-task1.*` and `strict-task2.*` under the Plan16 external root. Both reject incomplete actual qualification and `required regression run absent: focused_normal`. No missing-run or historical waiver was introduced. These are honest failing gate outcomes, not passing qualification checks.

`git diff --check` passed. Installed GSD's literal `query verification.status 204.2` exited0 but returned **missing** because this implementation resolves its argument as a directory. The source function `cmdVerificationStatus` uses `path.resolve(cwd, phaseDirArg)`. The corresponding `query verification.status .planning/phases/204.2-codex-native-worker-lifecycle`, with **no report selector**, exited0 and returned **gaps_found** from canonical `204.2-VERIFICATION.md`. Both raw calls are retained in `verification-numeric.*` and `verification-canonical.*`. Canonical selection works; numeric-directory resolution is an explicit tool limitation, not a passing numeric query. No shared GSD installation was changed.

## Assumptions, prohibitions and owner boundary

| Item | Explicit disposition |
|---|---|
| ANT-04 adjacency/empty/ordering | Deterministic identity/replay/order controls are retained; no additional current full-run credit. |
| ANT-06 boundary/precision | Exact-byte/Unicode/size controls are retained; actual plaintext/causality remains incomplete. |
| ANT-05 unclassified assumption | **Flagged unresolved:** concrete recovery fixtures do not classify every reconnect/interruption edge. |
| ANT-07 unclassified assumption | **Flagged unresolved:** capabilities depend on actual host/version/configuration; cancellation is incomplete. |
| No parent-substituted/subprocess work presented as native success | **Flagged-unverified global prohibition.** Ordinary has attributed native success, but other unclassified parent/child operations remain. No invented automated descriptor or universal no-substitution pass. |
| Engineering proof must not become Phase 205 owner acceptance | **Flagged-unverified global prohibition; bounded Plan 16 preservation judgment separately recorded.** This executor changed no Phase 205 file and claims no owner verdict. Hash preservation is engineering evidence, not personal acceptance or a fabricated wired check. |

The Phase 205 original inventory still has SHA-256 **`3b2b678284a26332addeadd2bd700bd4f51c4e2b0e6907a987116c85e5c2a8dd`**. Its checkpoint remains owner-only; the ten personal journeys and verdict were not performed. No shared install/hub/home/configuration change, new login, publication, owner walkthrough, original-checkout edit, milestone seal or CalVault action occurred.

The prior real-home NPM incident remains **partially recovered: 31 additions, 64 changes, 17 removals have unestablished prior state**. Disposable ancestor isolation does not restore or certify that home. Preserve this limitation and the separate installed CalVault hotfix identity in any later handoff.

## Remaining qualification obligations

Obtain a passing current-candidate isolation preflight and exact focused/full normal/race execution/accounting with substantive seventeen-name comparisons. Complete the six incomplete native/comparison cases and full host provenance, retaining measured refusals and all original outcomes. Any source/capture/corpus correction must trigger the appropriate requalification; do not silently reinterpret frozen receipts. Phase 204.3/204.4/204.5 ownership does not waive these 204.2 gaps. Root owns shared tracker disposition; no requirement passes merely because Plan 16 has a SUMMARY.

## Independent final verification — 2026-09-18

**Goal:** Connect real native Codex helpers to Go-authorized assignments, complete context, durable results, and interruption-safe recovery. The independent verdict remains **gaps_found**, **0/5 fully qualified roadmap truths**. No override is recorded or inferred. All sixteen plan frontmatters and their must-haves were considered alongside the roadmap contract; SUMMARY narratives were not accepted as behavioral proof.

In plain English: the ordinary worker demonstration is real progress, but several checks can still mistake incomplete evidence for proof. The current report already refuses full success. These newly confirmed defects must also be repaired before future receipts can qualify the phase.

### Truth classification and requirement coverage

| Truth | Independent classification | Reason |
|---|---|---|
| SC1 / ANT-04 — installed governed Builder and exactly-once credit | **FAILED — BLOCKER for full qualification** | Narrow ordinary live pass is retained; required current regression and complete candidate/host proof remain absent. |
| SC2 / ANT-04 — actual independent review and proportionate ordinary team | **FAILED — BLOCKER** | Distinct Watcher findings exist, but the frozen review receipt remains incomplete; raw findings do not discharge terminal/check attribution. |
| SC3 / ANT-06 — full context, scoped answers and refusal boundaries | **FAILED — BLOCKER** | Question receipt lacks completed-send ACK, exact host-boundary plaintext and qualifying child edit/check/credit. |
| SC4 / ANT-05 — interruption-safe fresh-session recovery | **FAILED — BLOCKER** | Early/partial cases remain incomplete; WR-01 additionally leaves cut-point replay semantics unproved. |
| SC5 / ANT-07 — actual host controls and truthful visibility | **FAILED — BLOCKER** | Cancellation remains request-only; schema provenance is absent and CR-02/03 weaken future qualification. |
| Plan 13/16 — complete frozen regression candidate | **FAILED — BLOCKER** | Missing runs and independently confirmed CR-01. |
| Plan 12 — independently verifiable retained cut point | **UNCERTAIN — WARNING** | Live controller checks a boundary, but replay validates only hashes/spawn identity. Repair semantic validation; a human reviewing raw snapshots could decide their narrow evidentiary value, without waiving current failures. |
| Plans 08/16 — preserved history and honest disposition | **VERIFIED, bounded reporting obligation** | All 92 protected hashes match, using the original canonical archive for its one replaced path. All four ANT IDs remain pending; no owner verdict was created. |

ANT-04 through ANT-07 are claimed by the plans and mapped in REQUIREMENTS.md; none is orphaned and none is satisfied by this verification. Later Phase 204.3 workflow expansion, 204.4 recruitment/workspace/Swarm work and 204.5 broader installed parity proof do not defer these explicit 204.2 native lifecycle obligations. No gap was removed as deferred.

### Artifact substance and key links

| Artifact / link inspected | Independent result |
|---|---|
| `codex_native_worker.go` → `composeCodexNativePrompt` | Substantive runtime launch composition is called at reservation; actual exact delivery remains a separate failed truth. |
| `build_attempt.go:1014,1076` and `codex_build_finalize.go:471` → `validateCodexNativeCompletion` | Native journal validation is wired into staging and finalization. No disconnected replacement credit mechanism is implied. |
| `context.go:439` → `buildCodexNativeRecovery` | Public context/recovery uses actual saved native facts. Fresh-host qualification remains incomplete. |
| `codex_native_evidence_test.go:131–135` → outcome and frozen-provenance validation | Default gate consumes the new qualification and does reject incomplete outcomes; CR-02 concerns a separate provenance predicate. |
| `scripts/qualify-native-gap-regression.py:102–110,182–189,245,289–302` | Preparation and later validation reuse tracked-only inventory. Untracked status is recorded but not rejected, confirming CR-01. |
| `codex_native_gap_evidence_test.go:109–128,249–258` → `nativeEvidenceBytes` | Only identity/hash/membership checks protect configuration/schema; the positive fixture deliberately accepts arbitrary bytes. CR-02 confirmed. |
| `codex_native_gap_controls_test.go:123–160` → `nativeGapCollectCancellation` → live validator at `codex_native_worker_live_test.go:1110–1111` | Malformed child records are dropped before NoPostAckWrites, and Qualified is consumed by the live gate. CR-03 confirmed. |
| `codex_native_gap_recovery_test.go:48–96,429–436` → `nativeGapParentExitAccepted` → live receipt validation | Spawn proof is substantive and wired, but checkpoint attempt/state semantics are not validated. WR-01 confirmed. |

These are source-traced defects, not newly executed exploit tests. The advisory [gap execution review](204.2-GAP-EXECUTION-REVIEW.md) supplies detailed counterexamples and corrective controls; its three BLOCKER findings and one WARNING were independently checked against the functions and callers above. They are proof-contract defects, not evidence that the current incomplete report falsely claims success. No `TBD`, `FIXME` or `XXX` debt markers were found in the ten gap-modified source files. Dynamic UI data-flow verification is not applicable to this CLI/evidence phase; receipt-to-validator-to-disposition wiring was checked instead.

### Independent bounded checks and limits

| Check performed in this verifier process | Result |
|---|---|
| Read and hash all eleven original and eleven derived receipt files against the committed qualification | **PASS:** 22/22 references match; outcomes are one passed, four observed and six incomplete. |
| Hash regression preparation, preflight manifest, run refusal, check refusal and live readback references | **PASS:** 5/5 match. Manifest `passed` is false; raw race fixture stdout contains `no space left on device`. |
| Compare execution-start protected inventory | **PASS:** 92/92 match; canonical predecessor checked through its byte-identical archive. |
| Current focused/full normal/race execution | **NOT RUN:** failed preparation prohibits them; prior logs and summaries cannot substitute. |
| New Go behavioral spot-checks / live hosts | **SKIPPED:** bounded verification was restricted to read-only inspection; compilation risks repeating the recorded storage failure. No additional runtime pass is claimed. |
| Declared/conventional `scripts/*/tests/probe-*.sh` discovery | No matching declared or conventional probe was found. Existing regression driver is not a substitute probe and was not rerun. |

Existing strict gate failures are retained evidence, not verifier-run passing tests. Integrity checks prove unchanged bytes, not correctness. No new owner walkthrough or acceptance is requested or claimed here. The warning about checkpoint meaning requires repair or a narrowly documented human decision; it cannot override the five failed roadmap criteria or the three confirmed proof-contract defects. Source fixes require corresponding new capture/corpus qualification before advancement.

_Independent verifier: gsd-verifier. Report only; no source changes or commit._
