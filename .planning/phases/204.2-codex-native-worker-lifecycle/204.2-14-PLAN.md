---
phase: 204.2-codex-native-worker-lifecycle
plan: "14"
type: execute
wave: 11
depends_on: ["204.2-12"]
files_modified:
  - cmd/codex_native_worker_live_test.go
  - cmd/codex_native_gap_controls_test.go
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/controls.json
autonomous: true
gap_closure: true
requirements: [ANT-07]
must_haves:
  truths:
    - "Workspace-write and inherited read-only claims require actual child write attempts against inside and outside disposable sentinels."
    - "Cancellation remains cancel_requested until a correlated actual host acknowledgement and terminal evidence justify cancelled; no success or credit is inferred from interruption alone."
    - "Unsupported per-child permission, separate native workspace and governed nesting requests refuse without weakened controls, substitute launch or invented usage."
  artifacts:
    - path: cmd/codex_native_worker_live_test.go
      provides: "Executable implementation or proof contract"
    - path: cmd/codex_native_gap_controls_test.go
      provides: "Executable implementation or proof contract"
    - path: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/controls.json
      provides: "Source-pinned gap-closure evidence"
  key_links:
    - from: cmd/codex_native_worker_live_test.go
      to: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/controls.json
      via: "Source-bound checks and preserved qualification evidence"
---

<objective>
Measure actual workspace, permission, cancellation and refusal behavior.

Purpose: Close the observed qualification gap while preserving historical evidence. In plain English, prove the worker really did the promised work on the current program and clearly retain anything still unproved.
Output: cmd/codex_native_worker_live_test.go, cmd/codex_native_gap_controls_test.go, .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/controls.json.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-VERIFICATION.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-REVIEW.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP-PLAN-AUDIT.md
@.planning/research/ant-codex-parity-2026-09-16/GSD-INTEGRATION.md
</context>

## Execution boundaries

D-01..D-10 refer to the existing SYNTHESIS mapping of unnumbered CONTEXT decisions, not new owner decisions. Preserve all Plans 01–09, summaries, original evidence and reviews byte-for-byte. Write new receipts under evidence/gap-closure/ and a new durable external run root. Install only inside disposable child environments; never replace shared aether, hub, Codex/Claude homes, CalVault or Phase 205 evidence. Keep current branch and unrelated edits. Do not initiate login, fresh authentication, publication or owner walkthrough. Record existing-auth failure honestly. No new packages or database schema are involved; security enforcement is disabled in project config.

Pin current source revision/status, production inventory, test corpus, live harness, candidate binary/build arguments, Go version/environment, host executable/version/model/effort/tool schema and installed bytes. The installed narrow CalVault hotfix is not this candidate. Old 165db28c, f9e32ca0 and cf51e9ac receipts keep their original identities. A reporting-only commit may reference equal bytes but cannot relabel binaries. Production changes require final live requalification; harness changes require new capture where collection/causality changed, and separately pinned raw replay for parser-only corrections; changed test corpus invalidates previous full-run credit.

Reversibility: source/test edits are reversible through their scoped commits; disposable fixtures can be removed only after external raw evidence is preserved. Journal/result contract changes are costly: preserve readers, immutable terminal records and ExecutionBinding; do not migrate any shared store. A host limitation is a measured incomplete/refused outcome, never permission to remove a required scenario. Live checks may exceed 60 seconds; their explicit bounds preserve evidence and stop owned processes. Ordinary bounded test verification must be run normally and with -race when runtime or harness behavior changes.

## Artifacts this phase produces

- cmd/codex_native_worker_live_test.go
- cmd/codex_native_gap_controls_test.go
- .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/controls.json

New named test symbols and script modes are specified in the task actions. Existing runtime interfaces remain authoritative.

<interfaces>
nativeCollectHostControlEvidence and nativeValidateControlSentinels collect actual CommandExecution and nested child response facts. Existing codex.NativePlatformContract refuses unsupported restrictions. Native observations distinguish cancel_requested from confirmed terminal cancelled; keep that distinction.
</interfaces>

<tasks>
<task type="auto">
  <name>Task 1: Require observable sentinel attempts and cancellation outcomes</name>
  <files>cmd/codex_native_worker_live_test.go, cmd/codex_native_gap_controls_test.go</files>
  <read_first>cmd/codex_native_worker_live_test.go; cmd/codex_native_gap_controls_test.go; pkg/codex/platform_contract.go; cmd/codex_native_worker.go; cmd/codex_native_recovery_test.go; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-09-SUMMARY.md</read_first>
  <behavior>Actual outside denial requires exact attempted target and host response; cancellation credit remains zero for request-only/unknown outcomes.</behavior>
  <action>Per D-05/D-10, repair controls/controls-read-only fixture execution so the actual bound child attempts both source-pinned sentinel commands once and raw command attempts/results are retained even when denied. Before/after absence alone never counts as enforcement. Use only evidence-owned disposable paths; no escalation or mutation of host policy. Keep inherited read-only host probing separate from the runtime's supported inherited workspace-write admission. Cancellation must correlate request, actual interrupt call/result, child identity and terminal observation; distinguish host interrupted from runtime cancelled and verify no post-ack writes. If cancellation is unavailable, preserve explicit unsupported/incomplete behavior rather than synthesize a cancelled terminal. Add TestCodexNativeGapControls with missing-attempt, unrelated denial, wrong-child acknowledgement, request-only and late-write controls. Existing Go refusal rules for stricter per-child permissions, native worktree allocation and governed nesting remain unchanged.</action>
  <acceptance_criteria>Controls reject unchanged sentinels without attempts, fabricated host denials, cancellation request as acknowledgement and unrelated child events. Supported host facts and runtime refusals are separate assertions; absent usage stays unreported.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexNativeGapControls|TestCodexNativeWorkerCancellation|TestCodexNativeRecoveryCancelPending|TestCodexNativeRecoveryCancelConfirmed)$' -count=1 -timeout 60s</automated></verify>
  <done>Controls reject unchanged sentinels without attempts, fabricated host denials, cancellation request as acknowledgement and unrelated child events. Supported host facts and runtime refusals are separate assertions; absent usage stays unreported.</done>
</task>

<task type="auto">
  <name>Task 2: Capture controls, cancellation and missing-skill refusal on the real host</name>
  <files>.planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/controls.json</files>
  <read_first>.planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/controls.json; cmd/codex_native_worker_live_test.go; pkg/codex/platform_contract.go; cmd/wrapper_usage_resolve_test.go</read_first>
  <action>Run controls, controls-read-only, cancellation and missing-skill with current candidate/host identities. Retain actual child inside/outside attempts, sentinel hashes, inherited permission configuration, bounded native child-to-child response, inline activity and source-qualified usage availability. A nesting response proves host nesting only, not Aether-governed recruitment. Execute existing unsupported-control requests and prove prelaunch refusal/no journal mutation. Missing-skill is expected observed refusal with zero helper launch, no parent implementation and unchanged inventory. Cancellation must retain both request and observed terminal facts; refusal/incomplete remains honest if acknowledgement cannot be established. Record ANT-07's unclassified probe unresolved and limit capability claims to the measured version/settings.</action>
  <acceptance_criteria>All four case dispositions and unsupported-control refusals are backed by actual events; missing skill qualifies as expected refusal, not workflow success. Permission or cancellation absence prevents stronger claims; raw available provider tokens are never represented as Aether-collected cost.</acceptance_criteria>
  <verify><automated>AETHER_CODEX_NATIVE_LIVE=1 AETHER_CODEX_NATIVE_SCENARIOS=controls,controls-read-only,cancellation,missing-skill AETHER_CODEX_NATIVE_EVIDENCE_DIR="$AETHER_GAP_EVIDENCE_ROOT/controls" go test ./cmd -run '^TestCodexNativeWorkerFreshHost$' -count=1 -json -timeout 60m</automated></verify>
  <done>All four case dispositions and unsupported-control refusals are backed by actual events; missing skill qualifies as expected refusal, not workflow success. Permission or cancellation absence prevents stronger claims; raw available provider tokens are never represented as Aether-collected cost.</done>
</task>
</tasks>

<verification>
Every task's automated verification retains raw exit/output and exact candidate identity. Required live evidence must pass on its own merits; test fixtures and receipt replay cannot replace it. Plan16 owns the final complete regression pair and new explicit goal-backward verdict. Shared planning trackers remain the orchestrator's responsibility.
</verification>

<success_criteria>
Workspace-write and inherited read-only claims require actual child write attempts against inside and outside disposable sentinels.
Cancellation remains cancel_requested until a correlated actual host acknowledgement and terminal evidence justify cancelled; no success or credit is inferred from interruption alone.
Unsupported per-child permission, separate native workspace and governed nesting requests refuse without weakened controls, substitute launch or invented usage.
Missing mandatory proof remains an explicit gap; writing this plan's execution report never marks the phase complete.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-14-SUMMARY.md with task outcomes, exact source and evidence identities, failures and outstanding qualification obligations.
</output>

