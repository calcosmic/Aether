---
phase: 204.2-codex-native-worker-lifecycle
plan: "19"
subsystem: testing
tags: [codex, cancellation, recovery, evidence]
requires:
  - phase: 204.2-18
    provides: Semantic capture contract and measured unavailable host export
provides:
  - Fail-closed cancellation stream controls with structural proof separated from actual acquisition
  - Typed retained recovery snapshots with independently replayed cut-point semantics
affects: [204.2-20, 204.2-21, 204.2-22, 204.2-23]
tech-stack:
  added: []
  patterns: [typed snapshot replay, explicit unavailable provenance, source-pinned focused controls]
key-files:
  created: []
  modified:
    - cmd/codex_native_gap_controls_test.go
    - cmd/codex_native_gap_recovery_test.go
    - cmd/codex_native_worker_live_test.go
key-decisions:
  - Self-declared observation-end records can establish structural consistency only; unavailable actual exporter provenance cannot qualify cancellation.
  - Successful natural parent exit cannot replace the retained interruption checkpoint.
  - Replay uses existing runtime attempt, manifest, state normalization and worker result types.
requirements-completed: []
requirements-addressed: [ANT-05, ANT-07]
duration: approximately 15min
completed: 2026-09-18
status: complete
qualification: incomplete
---

# Phase 204.2 Plan 19: Fail-closed interruption evidence Summary

**Corrupt cancellation records and hash-valid but semantically wrong recovery snapshots now reject; actual cancellation remains unqualified because supported observation-end acquisition is unavailable.**

In plain English: matching file hashes no longer make bad evidence count as proof. The saved records must tell a consistent story about the same worker, unfinished build and actual interruption. These source repairs are complete; they do not show that the real host supplied all required proof or make the candidate ready to publish.

## Task commits

1. `0930ede6` — RED: reproduced acceptance of truncated final and malformed middle cancellation records.
2. `bf56d2a6` — Task 1 repair: strict attributed stream decoding, explicit interval boundary structure, and refusal to trust self-declared acquisition provenance.
3. `bc66f80d` — Task 1 follow-up: late tool outputs count as activity; unattributed calls reject.
4. `f6050bab` — RED: reproduced acceptance of arbitrary attempt bytes with freshly matching hashes.
5. `41926a57` — Task 2 repair: typed snapshot replay, source/terminal/spawn linkage and ordered controller stop evidence.

Each source commit is scoped to the declared files. Concurrent root changes to routing guidance and a separate provider-dispatch repair were preserved and are not part of these commits. Shared STATE, ROADMAP and requirements updates belong to the root.

## Task outcomes

Cancellation now rejects nonblank malformed JSON, unknown event/activity shapes, missing or regressing timestamps, unattributed turns, wrong child identity, missing terminal/runtime linkage, absent/early ends, writes and tool outputs after acknowledgement. A boundary records its child, parent, timestamp, raw record hash and exact preceding stream hash. The deterministic positive proves only `IntervalStructurallyComplete`; it explicitly requires `Qualified` and `NoPostAckWrites` to remain false. The collector currently has no supported complete-interval export attestation, and no such event was generated or claimed. A magic collector string, parent exit, interrupted result, timer or `previous_status=running` cannot turn into actual cancellation proof.

Recovery requires all six exact members: `parent-events.jsonl`, `child-events.jsonl`, `attempt.json`, `state.json`, `clamp.go`, `inventory.json`. It decodes the existing runtime types and correlates run, attempt, manifest, workspace, launch, worker, parent, child, accepted dispatch and result/source-event identities. Snapshot inventory binds the actual source, state and attempt bytes. State normalization matches the runtime, including valid legacy `current_phase=0` behavior. Aggregate completion, finalization, credit, wrong saved state, replacement worker and unknown scenarios reject.

Completed-worker scenarios require a saved completed result corroborated by the child's terminal event and replayed source change, followed by the parent's actual record command before the cut. Partial recovery has exactly two accepted assignments and one saved worker. Spawn-gap requires one genuine spawn, a saved reservation without child binding/result, unchanged source, no aggregate/credit and no replacement. Parent kill success, failed-process wait result and observed/kill/wait ordering are mandatory. A natural zero exit without a checkpoint cannot waive replay. The capture timestamps the cut after retaining its snapshots; original reports and captures were not rewritten.

## Verification and exact evidence

External evidence root: `/private/tmp/aether-gap2-resume-x7pso2qh/plan19`.

| Run | Result | Meaning |
| --- | --- | --- |
| `task1-red` | exit 1; 46 pass / 4 fail | Both corruption regressions reproduced; parent subtests also fail |
| `task2-red` | exit 1; 55 pass / 3 fail | Arbitrary hash-valid attempt acceptance reproduced |
| `review-normal` | exit 0; 125 pass | 43 control cases and 82 final recovery cases |
| `review-race` | exit 0; 125 pass | Same controls with race detection |
| `final-controls-normal` | exit 0; 45 pass | Final cancellation code including late output/unattributed-call controls |
| `final-controls-race` | exit 0; 45 pass | Same final cancellation code with race detection |

All passing final checks have zero failures and zero skips. Three typed positive cut points and 22 rejecting mutations per cut point run inside the recovery set. Intermediate unsuccessful development checks and superseded passes remain retained separately; they are not relabeled as final evidence.

Exact command shape: pinned Go 1.26.5 executable, `test ./cmd`, optional `-race`, `-run '^(TestCodexNativeGapControls|TestCodexNativeGapRecovery)$'`, `-count=1 -timeout 60s -json`. Final cancellation runs use `-run '^TestCodexNativeGapControls$'`. Per-run `receipt.json` retains actual argv, child environment, revision, file hashes, full admitted production/test inventory and available space; `source.patch`, stdout JSONL and stderr retain the dirty-source delta and outputs. The resource guard compares current free bytes against the prior measured isolated preflight requirement before each launch. The review-normal check recorded 22,163,460,096 free bytes. Isolated normal/race caches and offline module cache were reused; parent HOME/CODEX_HOME, shared installation and manifests were not modified.

`result-manifest.json` SHA-256: `f205569889632b8144042355e6dbe3d8cbcc0abcd5c7ad035f6e02cc3f0461f8`. It pins the selected raw outputs, per-run receipts and runner. Final source commit: `bc66f80dbd07e0fa613ca56c35a4699447182ff2`.

Final scoped source SHA-256:

- controls: `8bc4db1edc24a81fd37bd44d02a844cb125198084a2c603a93dec20f17132ae9`
- recovery: `a6ed59f07b979194c88ec83622624e976fa80a133949db7ad25a06b0b3b9ad88`
- live harness: `dd1e36f1205efb10dbd0d90da6333de3e313767cbdc2e146205231155c1cfa83`

These are focused deterministic checks of the scoped changes, not a frozen whole-candidate matrix. The checkout also contained independently owned edits recorded in the dirty-source receipts. No full suite, new real-host run, publication, installation, login, replacement child, owner walkthrough or ANT completion is claimed.

## Deviations and remaining gaps

- **Rule 2 — provenance separation:** review identified that an internally consistent boundary with a self-declared collector could still self-authorize. Actual acquisition is now a separate explicit unavailable gap. Structural fixture success never grants absence-of-write proof. Completing the actual positive qualification path requires a supported host export, not another synthetic event.
- **Rule 1 — retained exit semantics:** the prior natural-exit compatibility fallback could omit the entire interruption checkpoint. Recovery scenarios now require the checkpoint; historical captures remain unchanged but may fail the stronger current replay.
- Runtime normalization was reused after the first typed fixtures exposed legitimate omitted state defaults; no alternative state model or weakened hash comparison was introduced.
- The standard verification remains `gaps_found`; ANT-05 and ANT-07 remain pending. Plans 21/22 require new actual interruption/cancellation captures against the final candidate, and Plan 23 requires the full matrix and standard verdict. Current source changes invalidate reuse of older full regression credit.
- No new endpoint, auth path, dependency or trust-boundary schema was introduced. No placeholder implementation or TODO was added. The deliberately unavailable actual observation-end provenance is a documented qualification gap, not a stub claimed as working.

## Self-Check: PASSED

The three modified files and this summary exist. All five scoped source commits resolve. Final scoped file hashes match the selected test receipts; all selected output/receipt hashes were assembled into the external manifest. Whitespace checks pass, and there are no unexpected deletions in the scoped commits. Root retains responsibility for the protected-file inventory, shared state and final review disposition. Source/control completion does not establish host qualification or release readiness.
