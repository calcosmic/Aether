---
phase: 204.2-codex-native-worker-lifecycle
plan: "14"
subsystem: testing
tags: [codex, native-workers, permissions, cancellation, evidence]
requires:
  - phase: 204.2-12
    provides: Current native host attribution and recovery harness
  - phase: 204.2-13
    provides: Completed isolated regression preparation; no broad-suite credit reused
provides:
  - Exact sentinel attempt, denial-target and duplicate-event controls
  - Child-correlated interruption evidence with conservative terminal cancellation gating
  - Four immutable live captures, eight separately pinned replays and three actual candidate refusals
affects: [204.2-15, 204.2-16]
actuals:
  tasks: 2
  commits: 4
tech-stack:
  added: []
  patterns: [Actual host event attribution, parser-only immutable replay, separate execution and qualification]
key-files:
  created:
    - cmd/codex_native_gap_controls_test.go
    - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/controls.json
  modified:
    - cmd/codex_native_worker_live_test.go
key-decisions:
  - A real exact command attempt without target-specific output is retained but does not prove filesystem enforcement.
  - Same-child and same-turn open-call correlation plus identical results avoids counting one host command twice.
  - Host interrupted and runtime cancel_requested remain distinct from confirmed terminal cancelled.
patterns-established:
  - Raw capture identity remains immutable when a separately pinned parser reinterprets its events.
requirements-completed: []
requirements-addressed: [ANT-07]
coverage:
  - id: deterministic-controls
    description: Sentinel attempts, correlated denials, duplicate representations, cancellation and refusal controls
    verification:
      - kind: integration
        ref: cmd/codex_native_gap_controls_test.go#TestCodexNativeGapControls
        status: pass
      - kind: integration
        ref: cmd/codex_native_gap_controls_test.go#TestCodexNativeGapControlCommandCorrelation
        status: pass
    human_judgment: false
  - id: measured-host-controls
    description: Inherited workspace-write/read-only and expected missing-skill refusal on the pinned host
    verification:
      - kind: e2e
        ref: evidence/gap-closure/controls.json#cases
        status: pass
    human_judgment: false
  - id: terminal-cancellation
    description: Actual cancelled terminal and no-post-ack-write proof
    verification:
      - kind: e2e
        ref: evidence/gap-closure/controls.json#cancellation
        status: fail
    human_judgment: true
    rationale: Additional actual host evidence is required; owner approval or a walkthrough cannot substitute for it.
duration: approximately 27min from first external evidence setup; initial reading was not timed
completed: 2026-09-18
status: complete
qualification: incomplete
---

# Phase 204.2 Plan 14: Controls and cancellation Summary

**Both tasks are executed and reported. Actual permission probes and expected refusals are observed; cancellation remains request-only and does not qualify a cancelled terminal.**

In plain English: the real helper successfully wrote inside its allowed workspace and was blocked outside it. With inherited read-only permissions, both writes were blocked. Stopping a helper still does not prove that Aether can safely call its work cancelled. ANT-07 and phase qualification remain incomplete.

## Performance and scope

- First recorded external evidence setup: 2026-09-17T23:38:35Z; summary prepared 2026-09-18 at approximately 00:05Z. Initial reading began earlier and was not instrumented.
- Four-case invocation: **23:49:37.374410–23:57:01.957683Z**, exit **1**, approximately **7m25s** including setup/builds. All four scenarios completed within their 15-minute host bounds; no timeout occurred.
- Tasks executed: **2**. Declared deliverables plus SUMMARY: **4 files**.
- Expected base **93d7878495a08ba6bfab809e367bf7aac9eb10ba** verified. Retained branch `worktree-agent-p2042-14-1789688194`; no branch switch or original-checkout edits.
- No production runtime, shared tracker, historical plan/report/evidence, Phase205, shared install/hub/home/config, authentication setup or publication changes. No owner walkthrough. Existing authentication was copied only into disposable fixtures and removed afterward.

## Task Commits

1. **Task 1:** `2758505783f475c305b3d2956848f5cd96bb1858` — exact attempted sentinel commands, target-correlated denial validation, cancellation evidence, bounded owned-host process handling and actual candidate refusal capture.
2. **Task 1 parser correction:** `0b31f525bce351a3ba24df28cb20285aecbffdde` — correlate the two actual host representations of one awaited command; retain rejecting controls for real repeats, different turns and different outputs.
3. **Task 2:** `904250450adcdb6ae1d2c3b4f6aa17b54dc8adfd` — source-pinned controls report with original/derived outcomes and explicit gaps.

This SUMMARY is the fourth scoped commit. Every commit used normal hooks; shared tracker updates belong to the root orchestrator.

## Accomplishments

- The controls prompt asks the actual child to execute each source-pinned probe separately and exactly once, retaining real denied results. Sentinel validation now requires actual attempts, one unambiguous outcome, exact denial targets, unchanged probe bytes and matching before/after contents. Missing, repeated, fabricated, unrelated and late-write evidence is rejected.
- Cancellation requires a matching actual interrupt call/result, bound child, runtime request, correlated cancelled host event and matching runtime terminal, plus a child observation interval without later execution activity. Missing intervals and unknown activity remain unproved. No host-policy escalation or synthesized cancellation was introduced.
- Actual candidate CLI requests for inherited read-only, separate workspace and governed nesting all exited **1**, returned `launch_allowed:false`, and preserved the entire captured runtime inventory. They are runtime refusal evidence, separate from host-only inherited permission/nesting probes.
- Actual controls children each obtained a bounded nested response. This establishes host nesting only; Aether-governed recruitment and native worktree allocation remain unsupported.

## Actual outcomes

| Case | Original live disposition | Final separately pinned interpretation |
|---|---|---|
| `controls` / `native-controls-379287335` | Incomplete: the initial parser counted the successful command's normalized event and wrapper output twice. | **Observed workspace-write enforcement.** Exactly one inside attempt succeeded; exactly one outside attempt returned a target-specific permission error. Before/after hashes agree; actual nested response observed. |
| `controls-read-only` / `native-controls-read-only-1665029889` | Observed. | **Observed inherited read-only enforcement.** Inside and outside attempts both returned target-specific permission errors; both sentinels remain absent. Actual nested response observed. This does not authorize Aether per-child read-only admission. |
| `cancellation` / `native-cancellation-4193846394` | Incomplete. | **Still incomplete.** Actual child `01a0b1c9-cb09-7350-86d7-91fb7d341132` was bound, released and interrupted. Host event kind is `interrupted`; Aether retains `cancel_requested`, nonterminal, with no completion or credit. Cancelled terminal and no-post-ack-write evidence are absent. |
| `missing-skill` / `native-missing-skill-3232600157` | Observed refusal. | **Expected refusal observed.** Missing installed entrypoint, zero helper launch, no parent implementation and unchanged captured inventory. This is not workflow success. |

All eight normal/race replay invocations exited **0** and each internally derived its receipt twice with identical results. Only cancellation explicitly enabled `AETHER_CODEX_NATIVE_REPLAY_EXPECT_INCOMPLETE=1`; its derived outcome remains incomplete. Replay success means integrity/repeatability, not automatic workflow success.

Raw child-attributed provider usage records were available: cancellation **3**, workspace-write **5**, read-only **5**. These are counts of retained events, not token totals or costs. No native Aether usage collector was added: saved usage and cost remain **unreported**, never zero.

## Exact source and evidence

External root: `/Users/callumcowie/.aether-evidence/phase-204.2-gap-20260917T210924Z/plan-14`.

- All four captures used clean source **2758505783f475c305b3d2956848f5cd96bb1858**, production digest `sha256:08cf994909e034ec397b454fcd848f0bdc7277fd2d5d13d3ef1191040eacdf0c`.
- Candidate binary: `sha256:07290062eb72816f7536907970dc5910bd3ba0b52d8b6169c503bf85bf6ce97b`, reports **1.0.79**. Exact per-case build argv uses `go build -buildvcs=false` and the version ldflag. This is an independently built candidate, not the installed narrow CalVault hotfix or a relabeled earlier binary.
- Capture harness: `sha256:d4a56022acf781293738dd1b5e4ddbbefa99ade84a5dd4157277084e53112d69`.
- Final parser validator: **0b31f525bce351a3ba24df28cb20285aecbffdde**. Harness `sha256:780038c2155ba1000ab8ebfe742e2043a0052c793d69263d2159bb20160b91dd`; controls file `sha256:82ef3ea7031341f835034b79dbe6d475164c4261d56f6dc0cb44ae7af0ab40b1`.
- Go **1.26.5**, darwin/arm64; Codex CLI **0.154.0**, parent **gpt-6-astra/xhigh**. Model/effort are invocation preferences, not independently attested per-child controls. Installed skill/support/agent bytes, client executable hash, exact argv and actual tool events are indexed in the receipts. A complete independent tool-schema export remains unavailable.
- `candidate-start.json` pins the capture source/environment, **837 test files**, source inventory, helper bytes and shared read-only pins. `validator-source.json` separately pins the final parser. **1,364 recorded non-test production inputs** match the capture; test/harness bytes changed and have separate identities. No current full-suite credit is inferred.
- `live-run.json`, `live.jsonl`, and `controls/native-*/receipt.json` preserve the original outcomes. Each case retains raw host/child events, installed inputs, sentinel inventories or cancellation/missing-skill evidence.
- `replay/` and `replay-runs.json` retain eight separately named derived receipts, raw logs and exact validator invocations. Originals were not overwritten.
- `final-integrity.json` verifies **13,772 original/replay artifact entries**, zero mismatches, all **92 root-protected files**, final validator bytes and recorded shared executable/config/version pins. Entry counts include repeated references, not 13,772 unique files.
- `post-run-boundaries.json` records no surviving matching fixture processes and no temporary auth files. All four capture receipts confirm authentication cleanup.
- Checked-in report: [controls.json](evidence/gap-closure/controls.json), SHA-256 `08a899794de17317a667f36e4a2821f473c05568fb711237b472fc6320e50b9e`.

## Verification

The exact plan Task1 selection passed normally. The final expanded selector passed **7 top-level / 74 total cases**, zero failures/skips, normally and with `-race`:

```sh
go test ./cmd -run '^(TestCodexNativeGapControls|TestCodexNativeGapControlCommandCorrelation|TestCodexNativeWorkerCancellation|TestCodexNativeRecoveryCancelPending|TestCodexNativeRecoveryCancelConfirmed|TestCodexNativeQualificationCoordinator|TestCodexNativeReviewedProofBoundaries)$' -count=1 -json -timeout 60s
# Identical selection with -race also passed.
```

Authoritative final focused receipts: `task1-parser-normal.{json,jsonl}` and `task1-parser-race.{json,jsonl}`. Capture-harness checks were **6 top-level / 69 cases** in both modes (`task1-frozen-*`). Earlier exploratory outputs are retained but do not replace final-byte checks.

All four required real-host cases ran:

```sh
AETHER_CODEX_NATIVE_LIVE=1 AETHER_CODEX_NATIVE_SCENARIOS=controls,controls-read-only,cancellation,missing-skill AETHER_CODEX_NATIVE_EVIDENCE_DIR="$AETHER_GAP_EVIDENCE_ROOT/controls" go test ./cmd -run '^TestCodexNativeWorkerFreshHost$' -count=1 -json -timeout 60m
```

The original exit **1** is preserved. Three actual unsupported-control CLI checks passed their refusal predicates. Eight final-validator replays passed integrity/repeatability, with cancellation still incomplete. `git diff --check` passed. No broad suite was run; Plan16 owns final source/corpus-qualified normal/race accounting.

## Deviations from Plan

**One evidence-backed parser correction, within declared ownership.** The actual host emitted both a `CommandExecution` and an identical awaited wrapper result for the same inside write. The initial exactly-once counter treated these as two attempts. The correction joins only a unique open exact command in the same child turn with equal exit/output; independent repeats and unrelated events still fail. New normal/race controls passed. Collection, fixture execution and production bytes did not change, so separately pinned immutable raw replay was used as the plan permits for parser-only corrections; the original failed receipt remains intact.

## Issues and outstanding qualification

- **Terminal cancellation remains incomplete.** A real host interruption is observed; an actual cancelled terminal and the required no-post-ack-write interval are not established. No completion, result or credit was manufactured.
- **ANT-07 unclassified probe remains explicitly unresolved.** Findings apply only to the measured version/settings. Missing full tool schema, per-child controls, governed nesting and native worktree allocation cannot be promoted into stronger capability claims.
- Exact full native qualification and the new final regression pair remain Plans15/16 obligations. The changed test corpus invalidates earlier full-run credit. No phase completion or ANT requirement completion is asserted.
- Historical Plans01–09/reports/evidence, Phase205 and its owner-only acceptance boundary remain untouched. The earlier partially recovered home-NPM incident is not repaired or reclassified by this plan.

## User Setup Required

None. No new authentication, owner action or publication was requested or performed. Missing proof is recorded for subsequent qualification, not converted into an approval request.

## Self-Check: PASSED

Both task deliverables and this SUMMARY exist; all three task commits resolve. The delta from the expected base is confined to the three declared files plus SUMMARY. Final normal/race controls pass, all four live cases have honest dispositions, all eight replay results retain their original source linkage, and all three refusal inventories remain unchanged. Artifact, protected-file, production-byte and process/auth checks passed. This is execution/report integrity, **not completed native qualification**; cancellation and ANT-07 remain incomplete.
