---
phase: 204.2-codex-native-worker-lifecycle
plan: "05"
type: execute
wave: 3
depends_on: ["204.2-02"]
files_modified:
  - cmd/codex_native_finalize.go
  - cmd/codex_native_finalize_test.go
  - cmd/codex_build_finalize.go
  - cmd/build_attempt.go
  - cmd/build_worker_run.go
  - .aether/schemas/completion-packet.schema.json
  - cmd/contract_schema_test.go
autonomous: true
requirements: [ANT-04, ANT-05]
must_haves:
  truths:
    - "Native completion credit comes only from the existing finalizer using the current bound manifest and previously accepted terminal journal results."
    - "Missing/changed/duplicate worker results or unbound native completion packets cannot stage or earn task credit."
    - "Terminal arrival order does not change manifest ordering, admitted receipts or credited task IDs; repeated finalization is idempotent."
    - "The shipped completion schema matches current Go structs, and meaningful failed checks cannot be replaced by a successful worker claim."
  artifacts:
    - path: cmd/codex_native_finalize.go
      provides: "Journal-to-completion validation reused by staging and finalizer"
      contains: "validateCodexNativeCompletion"
    - path: cmd/codex_native_finalize_test.go
      provides: "Bound native finalization, refusal and exactly-once tests"
      contains: "TestCodexNativeFinalize"
    - path: .aether/schemas/completion-packet.schema.json
      provides: "Current generated completion contract"
  key_links:
    - from: cmd/build_attempt.go
      to: cmd/codex_native_finalize.go
      via: "Native journal validation before aggregate persistence"
      pattern: "validateCodexNativeCompletion"
    - from: cmd/codex_build_finalize.go
      to: cmd/codex_native_finalize.go
      via: "The same native validation before existing credit commit"
      pattern: "validateCodexNativeCompletion"
---

<objective>
Make the saved helper evidence the only source of native completion credit. In plain English, an overall success message cannot replace or exaggerate what the actual helpers returned.

Purpose: Keep one completion authority while closing native packet replacement/replay gaps, per D-04/D-05/D-09.
Output: Shared native aggregate admission and a passing current completion-schema contract.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/research/ant-codex-parity-2026-09-16/GSD-INTEGRATION.md
</context>

<interfaces>
Existing runCodexBuildFinalize -> validateCompletionPacketSemantics -> mergeExternalBuildResults -> coherent/worktree receipt resolution -> commitBuildFinalizeState remains the one credit chain. NEW validateCodexNativeCompletion checks a current buildAttemptRecord against its projected codexExternalBuildCompletion. It adds no native credit writer.
</interfaces>

## Artifacts this phase produces

NEW: cmd/codex_native_finalize.go/test.go and validateCodexNativeCompletion.
EXISTING, modified: staging/finalizer call sites and worker-run completion projection; generated completion-packet.schema.json synchronized with actual current structs. No second completion schema or finalizer.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Stage and credit only the exact durable native results</name>
  <files>cmd/codex_native_finalize.go, cmd/codex_native_finalize_test.go, cmd/codex_build_finalize.go, cmd/build_attempt.go, cmd/build_worker_run.go</files>
  <read_first>cmd/codex_build_finalize.go runCodexBuildFinalize/commitBuildFinalizeState; cmd/build_attempt.go stageBuildAttemptCompletion; cmd/build_worker_run.go buildCompletionFromWorkerRuns; cmd/codex_build_finalize_test.go atomic refusal matrix; cmd/build_attempt_external_test.go idempotent and partial retry tests.</read_first>
  <behavior>One terminal plus one unfinished helper -> durable first result, no complete aggregate. All required terminals -> aggregate in manifest order. Caller-mutated result/digest/child or dropped binding -> refusal with no state mutation. Identical stage/finalize replay -> same packet/digest/credit. Partial failed job keeps valid task receipts without claiming remaining work.</behavior>
  <action>Implement validateCodexNativeCompletion over the current saved attempt, never a caller's claim that an attempt is native. For every native dispatch require its bound child, immutable assignment/prompt identity and canonical accepted result digest; compare the submitted worker result to the projection of that journal record. Reject missing/duplicate/extra/nonterminal workers, changed identity or replaced result evidence. Call the same helper before stageBuildAttemptCompletion writes and before runCodexBuildFinalize commits, including an under-lock currency check at the final commit boundary. Preserve generic legacy compatibility for genuinely non-native attempts; omitting a native binding cannot select that compatibility route. Extend buildCompletionFromWorkerRuns to preserve per-task receipts, handoffs, actual independent findings and source-qualified measurement fields, with an explicit confirmed-cancelled to existing interrupted/failed vocabulary mapping that cannot turn cancellation into success. Keep unknown/cancel_requested incomplete. Retain current semantic validation, deterministic check requirements and shared receipt resolution; no native-only credit assignment, worktree integration or recovery retry policy is added. Test reversed arrival orders, concurrent supersession/pause, accepted partial receipts and byte-stable replay through real staging and finalization.</action>
  <acceptance_criteria>No submitted packet can diverge from accepted native terminal evidence; the existing atomic finalizer alone credits work. Partial finished tasks remain correctly attributable, unfinished tasks remain unfinished, and equal replay changes neither credit nor journal identity.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexNativeFinalize(BoundJournal|RejectsMutation|Partial|Replay|ArrivalOrder|ConcurrentSupersession)$' -count=1 -timeout 60s</automated></verify>
  <done>The native bridge feeds the established credit chain with durable exact results, including interrupted and partial outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the current completion schema and shared finalizer contracts</name>
  <files>.aether/schemas/completion-packet.schema.json, cmd/contract_schema_test.go, cmd/codex_native_finalize_test.go</files>
  <read_first>cmd/contract_schema.go generateCompletionPacketSchemaBytes/contract-schema; cmd/contract_schema_test.go TestCompletionPacketSchemaMatchesStructs; 204.1-FINAL-GATE.md named baseline failures; current native result projection.</read_first>
  <action>Run the existing schema drift test first and preserve its actual diagnostic. Regenerate the committed schema using the current source command go run ./cmd/aether contract-schema --write, then add native projection/schema cases for actual worker/task receipts and rejection of host-only provenance fields where they do not belong on the completion wire. Keep Native provenance on the existing journal unless a typed completion contract truly requires a field; if a struct does change, regenerate the same schema in this task. Exercise the existing bound-finalizer replay and external grouped-job partial-retry tests along with a failed real fixture check that prevents credit/advance despite worker success prose. Preserve direct/subprocess and Claude/OpenCode generic packet compatibility. The historical schema failure must be an explicitly repaired baseline identity; other known failures are not waived or silently refreshed. Record the actual test command and exit for the tiny fixture so a malformed extracted command or zero executed checks cannot pass the new proof.</action>
  <acceptance_criteria>TestCompletionPacketSchemaMatchesStructs passes against generated current bytes; native packet fields round-trip or refuse as designed; bound native/direct/external finalization contracts stay valid; a failing deterministic check prevents the promised successful outcome.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCompletionPacketSchema|TestCodexNativeFinalizeChecks|TestBuildFinalizeIsIdempotentForBoundCompletion|TestBuildFinalizeRecoversBoundTerminalAttemptWithoutRedispatch|TestGroupedJobPartialRetryIsAppendOnlyExternal|TestGroupedJobPartialRetryIsIdempotent)' -count=1 -timeout 60s</automated></verify>
  <done>The schema failure relevant to this phase is fixed with passing behavior evidence, and native completion uses the same mandatory verification authority as existing lanes.</done>
</task>
</tasks>

<verification>
Use the real stage/finalizer/store chain. Break the native-journal admission call in a disposable source control and require the mutation test to fail; restore and run the focused normal/race selection. No full suite here.
</verification>

<success_criteria>
Durable native results and the current schema govern exactly-once finalization; absent, stale or altered evidence never earns credit.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-05-SUMMARY.md.
</output>

