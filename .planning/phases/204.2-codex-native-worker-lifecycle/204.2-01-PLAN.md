---
phase: 204.2-codex-native-worker-lifecycle
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/codex_native_worker.go
  - cmd/build_worker_run.go
  - cmd/command_guide.go
  - .aether/skills/colony/aether-colony-build-cycle/SKILL.md
  - cmd/codex_native_worker_test.go
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-tracer.json
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-tracer-resume.json
autonomous: true
requirements: [ANT-04, ANT-05]
must_haves:
  truths:
    - "An installed ant-build starts one actual named Builder for the runtime-issued assignment, whose own edit passes a real check and earns finalizer credit once."
    - "An empty or absent assignment/result is refused, while one valid Builder can complete without a mandatory Watcher."
    - "A fresh host session can read a saved terminal result before finalization and finalize it without rerunning the completed helper."
    - "Only the Codex build guide selects the native bridge; Claude/OpenCode retain their own wrapper launch and existing completion/finalizer routes."
  artifacts:
    - path: cmd/codex_native_worker.go
      provides: "Non-launching native reserve/bind/record/stage/inspect command"
      contains: "codex-native-worker"
    - path: cmd/build_worker_run.go
      provides: "Optional native provenance on the existing attempt worker record"
      contains: "Native"
    - path: cmd/codex_native_worker_test.go
      provides: "Registered-command tracer and opt-in actual host harness"
      contains: "TestCodexNativeWorkerTracer"
    - path: cmd/command_guide.go
      provides: "Codex-only native build guidance without changing the shared Claude/OpenCode build route"
      contains: "codexNativeBuildCommandGuide"
    - path: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-tracer-resume.json
      provides: "Source-pinned early interruption evidence, including incomplete outcomes"
  key_links:
    - from: .aether/skills/colony/aether-colony-build-cycle/SKILL.md
      to: cmd/codex_native_worker.go
      via: "Installed ant-build -> runtime reserve -> native spawn_agent -> bind -> terminal record"
      pattern: "codex-native-worker"
    - from: cmd/codex_native_worker.go
      to: cmd/build_worker_run.go
      via: "Durable per-worker result followed by existing aggregate staging"
      pattern: "stageBuildAttemptCompletionFromWorkerRuns"
    - from: cmd/command_guide.go
      to: cmd/codex_native_worker.go
      via: "adaptCommandGuideDefinitionForPlatform selects codexNativeBuildCommandGuide only for build on codex"
      pattern: "codexNativeBuildCommandGuide"
---

<objective>
Prove the smallest real installed-skill native build/check/return path before expanding the bridge. In plain English, make one actual helper do one useful edit, save its result, and demonstrate that a new chat can finish accounting for it.

Purpose: Resolve the missing host integration early, per D-01/D-04/D-05/D-08/D-09.
Output: A retained native bridge, honest real-host tracer receipts, and a clear observed host envelope.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/research/ant-codex-parity-2026-09-16/GSD-INTEGRATION.md
</context>

<interfaces>
Existing: codex.ExecutionBinding binds run_id, attempt_id, manifest_sha256, workspace_fingerprint and execution_owner. runCodexBuildPlanOnlyWithOptions authorizes an attempt; beginBuildAttemptWorkerRun/recordBuildAttemptWorkerTerminal/stageBuildAttemptCompletionFromWorkerRuns persist worker results; runCodexBuildFinalize is the sole credit boundary.
New: codexNativeWorkerRequest and codexNativeWorkerBinding follow the versioned fields and state sequence in 204.2-SYNTHESIS.md. ProviderRunID remains the launch_id. Native is optional on buildAttemptWorkerRun; old records retain their existing lane.
The stage operation is an explicit mutation wrapping stageBuildAttemptCompletionFromWorkerRuns over retained records; inspect remains strictly read-only. Start with a full-identity accepted one-worker path, including atomic reserve/bind validation, stable replay and terminal persistence, so the early live proof needs no later hardening plan to execute.
NEW codexNativeBuildCommandGuide(def commandGuideDefinition) commandGuideDefinition returns a copied native build definition. adaptCommandGuideDefinitionForPlatform calls it only when command == "build" and platform == "codex", before its current Codex early return. The shared catalog continues to define the existing wrapper route for Claude/OpenCode.
</interfaces>

## Artifacts this phase produces

NEW: cmd/codex_native_worker.go; codexNativeWorkerRequest, codexNativeWorkerBinding, runCodexNativeWorker; hidden codex-native-worker reserve/bind/record/stage/inspect routes; optional buildAttemptWorkerRun.Native; cmd/codex_native_worker_test.go and two early evidence receipts.
NEW: codexNativeBuildCommandGuide in cmd/command_guide.go and TestCodexNativeBuildGuidePlatformIsolation in cmd/codex_native_worker_test.go.
EXISTING, reused: plan-only authorization, runtime manifest/briefs, worker-run journal, existing completion staging/finalizer, Phase 204.1 installer and isolated-host evidence discipline.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Complete one installed ant-build through one real native Builder</name>
  <reversibility rating="costly">Additive versioned native provenance shares existing journal readers; no shared installation or published contract is changed.</reversibility>
  <files>cmd/codex_native_worker.go, cmd/build_worker_run.go, cmd/command_guide.go, .aether/skills/colony/aether-colony-build-cycle/SKILL.md, cmd/codex_native_worker_test.go, .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-tracer.json</files>
  <read_first>AGENTS.md; CLAUDE.md Definition of Done; cmd/internal_worker_adapter.go validation/journal choreography; cmd/build_worker_run.go; cmd/codex_skill_live_test.go evidence and isolated fresh-host runner; cmd/codex_skill_surface.go; 204.2-SYNTHESIS.md.</read_first>
  <behavior>Accepted plan -> one durable reservation -> one real host child -> bound identity -> child edit/check -> terminal journal -> aggregate -> one finalizer credit. Missing binding, different child identity, or provider invocation from the bridge must refuse/fail. A replay cannot launch another helper. buildCommandGuide("build", "codex") selects the native handshake and sole spawn_agent launcher; the claude/opencode results exclude Codex-only instructions and preserve their wrapper and finalizer contracts.</behavior>
  <action>Create the registered non-launching native command and optional journal provenance selected in the synthesis. Reserve from the saved runtime manifest under the existing attempt lock; return immutable assignment, actual prompt bytes, names/role/permissions/workspace and digests. Construct the prompt from the current capsule, exact brief_path bytes (fall back only when the path is absent), matched skills and current nonduplicated clarified intent. Spawn only through the host native spawn_agent operation: the child waits for a runtime-issued bound release; bind the returned child ID before sending that release. Record the first validated terminal response immediately, then use existing staging/finalizer functions. Keep unknown host outcomes unresolved and reject unsupported requested permissions/workspace before spawning; never call the Go worker adapter as a second launcher. Update the Codex Build Flow and the Codex-only build guide to this handshake, removing the retired reference-only build-wave authority only from that Codex route. Add codexNativeBuildCommandGuide and select it in adaptCommandGuideDefinitionForPlatform only for command == "build" and platform == "codex", before the existing early return. Copy PreSteps, PostSteps and DriftGuards before changing them; leave the shared catalog's Claude/OpenCode launch, completion-stage and finalizer instructions intact. Per D-04 and AGENTS.md primary-platform preservation, add table-driven TestCodexNativeBuildGuidePlatformIsolation in the existing native test file over actual buildCommandGuide("build", platform) results for codex, claude and opencode. Inspect all instruction-bearing fields: Intent, PreSteps, RunCommand, PostSteps, DriftGuards and RawBypass. Codex must require reserve/bind/record/stage and spawn_agent as its sole launcher, with no second executable provider/adapter dispatch. Claude/OpenCode must exclude codex-native-worker and spawn_agent, have empty SkillReference and a first step naming their generated slash-command wrapper, retain visible live Task/subagent dispatch and spawn-log/spawn-complete, retain build-completion-stage, and preserve the exact existing build-finalize RunCommand. Repeat the calls in alternating platform order and assert identical results to catch shared-slice mutation. Extend the new test file with a registered-command end-to-end case and an opt-in real-host harness using the Phase 204.1 installer and capture pattern. In a disposable dependency-free project, fixture-prepare a one-task accepted plan through the actual runtime acceptance path, then start a fresh actual Codex session with only the installed $ant-build 1 invocation and predeclared fixture authorization. Require the actual Builder to edit the tiny library and run its fast test; parent code changes cannot satisfy the scenario. Capture the complete source/installation/host/child/assignment/result/check provenance specified by the synthesis. This is bridge proof, not live-planning proof.</action>
  <acceptance_criteria>The real capture identifies installed skill/support reads, one named Builder and actual child edit/check events; the bridge has no subprocess launch; the durable terminal and credited task match the accepted attempt. The three-platform guide regression passes: only Codex advertises the native bridge, and both primary platforms preserve their wrapper launch/completion routes. Native-enabled failure or unavailable evidence writes an incomplete receipt and fails, rather than skipping or simulating a pass.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexNativeWorkerTracer|TestCodexNativeBuildGuidePlatformIsolation|TestCodexAntSkillGuidesPreserveOtherPlatforms)$' -count=1 -timeout 60s; AETHER_CODEX_NATIVE_LIVE=1 AETHER_CODEX_NATIVE_SCENARIOS=ordinary go test ./cmd -run '^TestCodexNativeWorkerFreshHost$' -count=1 -json -timeout 30m</automated></verify>
  <done>The one-worker path is runnable from the installed skill, with actual host evidence and no missing capability hidden. If host evidence is unavailable, retain the implementation and incomplete receipt, report the exact blocked proof, and do not mark this live criterion met.</done>
</task>

<task type="auto">
  <name>Task 2: Reopen the stopped tracer before aggregate finalization</name>
  <files>cmd/codex_native_worker_test.go, .planning/phases/204.2-codex-native-worker-lifecycle/evidence/native-tracer-resume.json</files>
  <read_first>Task 1 native receipt and raw events; cmd/build_attempt_external_test.go durable completion/replay tests; cmd/context.go buildResumeDashboardResult; 204.1-VERIFICATION.md current evidence limits.</read_first>
  <action>Extend the same tiny real-host scenario to stop the parent after the Builder's accepted per-worker terminal receipt but before aggregate staging/finalization. Start a new actual host process/session over the same disposable project, read the exact retained attempt through the native inspect route, and finish with the existing runtime staging/finalizer. Record original and resumed host session IDs, journal bytes/digest, original edit diff, completed helper ID, all actual command/tool events and final credit identity. Replay finalization and prove no repeated child writes/credit. The early scenario has one completed helper; the later Plan 07 scenario adds one still-unfinished job and public pause/resume. Include an intentionally empty result request as a real command refusal. Record observed native tool/event names and availability needed by later plans; missing reconnect capability is a fact, not license to respawn a completed child. Preserve the early source pin when later code changes: Plan 07 must rerun the complete scenario on its final candidate.</action>
  <acceptance_criteria>Two distinct parent sessions share the original runtime attempt and terminal digest; the Builder's edit survives; finalization replay leaves credit and worker count unchanged. The receipt labels the prepared plan and the one-worker scope honestly, and no Phase 205 evidence is touched.</acceptance_criteria>
  <verify><automated>AETHER_CODEX_NATIVE_LIVE=1 AETHER_CODEX_NATIVE_SCENARIOS=early-resume go test ./cmd -run '^TestCodexNativeWorkerFreshHost$' -count=1 -json -timeout 30m; go test ./cmd -run '^TestCodexNativeWorkerReceiptValidation$' -count=1 -timeout 60s</automated></verify>
  <done>The smallest useful interruption experiment is durable and reviewable before the remaining bridge expansion begins; an unavailable host produces an explicit incomplete finding.</done>
</task>
</tasks>

<verification>
Run the focused registered-command tracer, three-platform guide regression and replay its real receipts. Prove the guide test fails when the Codex helper is removed or its platform guard is bypassed, using genuine RED or a disposable source mutation. Live runs may exceed 60 seconds; launch asynchronously, retain raw exit/logs and report progress. No broad suites, shared install updates, owner project access or publication. No skipped live test is qualifying evidence.
</verification>

<success_criteria>
A real named Builder executes the requested edit/check from the installed skill; its result is durable before finalization; fresh-session replay credits it once. Native guide selection is Codex-only and Claude/OpenCode wrapper/finalizer behavior remains covered by regression tests. Early failure is an honest diagnostic, never native-parity acceptance.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-01-SUMMARY.md with exact source/host/receipt identities and any incomplete proof.
</output>
