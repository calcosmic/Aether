---
phase: 204.2-codex-native-worker-lifecycle
plan: "10"
type: execute
wave: 8
depends_on: ["204.2-08"]
files_modified:
  - cmd/codex_native_worker_live_test.go
  - cmd/codex_native_gap_attribution_test.go
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/tracer.json
autonomous: true
gap_closure: true
requirements: [ANT-04, ANT-05, ANT-07]
must_haves:
  truths:
    - "An ordinary accepted one-Builder job has attributable child edits, uncached checks, immutable terminal and exactly-once credit; the parent only coordinates."
    - "An explicitly reviewed job has a distinct named Watcher and actual independent findings without changing ordinary team policy."
    - "Equal reservation or terminal repeats return the same receipt; conflicting equal-key or empty binding/result requests refuse without mutation."
  artifacts:
    - path: cmd/codex_native_worker_live_test.go
      provides: "Executable implementation or proof contract"
    - path: cmd/codex_native_gap_attribution_test.go
      provides: "Executable implementation or proof contract"
    - path: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/tracer.json
      provides: "Source-pinned gap-closure evidence"
  key_links:
    - from: cmd/codex_native_worker_live_test.go
      to: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/tracer.json
      via: "Source-bound checks and preserved qualification evidence"
---

<objective>
Recover a trustworthy ordinary Builder tracer and independent Watcher proof.

Purpose: Close the observed qualification gap while preserving historical evidence. In plain English, prove the worker really did the promised work on the current program and clearly retain anything still unproved.
Output: cmd/codex_native_worker_live_test.go, cmd/codex_native_gap_attribution_test.go, .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/tracer.json.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-VERIFICATION.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-REVIEW.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP-PLAN-AUDIT.md
@.planning/research/ant-codex-parity-2026-09-16/GSD-INTEGRATION.md
</context>

## Execution boundaries

D-01..D-10 refer to the existing SYNTHESIS mapping of unnumbered CONTEXT decisions, not new owner decisions. Preserve all Plans 01–09, summaries, original evidence and reviews byte-for-byte. Write new receipts under evidence/gap-closure/ and a new durable external run root. Install only inside disposable child environments; never replace shared aether, hub, Codex/Claude homes, CalVault or Phase 205 evidence. Keep current branch and unrelated edits. Do not initiate login, fresh authentication, publication or owner walkthrough. Record existing-auth failure honestly. No new packages or database schema are involved; security enforcement is disabled in project config.

Pin current source revision/status, production inventory, test corpus, live harness, candidate binary/build arguments, Go version/environment, host executable/version/model/effort/tool schema and installed bytes. The installed narrow CalVault hotfix is not this candidate. Old 165db28c, f9e32ca0 and cf51e9ac receipts keep their original identities. A reporting-only commit may reference equal bytes but cannot relabel binaries. Production changes require final live requalification; harness changes require new capture where collection/causality changed, and separately pinned raw replay for parser-only corrections; changed test corpus invalidates previous full-run credit.

Reversibility: source/test edits are reversible through their scoped commits; disposable fixtures can be removed only after external raw evidence is preserved. Journal/result contract changes are costly: preserve readers, immutable terminal records and ExecutionBinding; do not migrate any shared store. A host limitation is a measured incomplete/refused outcome, never permission to remove a required scenario. Live checks may exceed 60 seconds; their explicit bounds preserve evidence and stop owned processes. Ordinary bounded test verification must be run normally and with -race when runtime or harness behavior changes.

## Artifacts this phase produces

- cmd/codex_native_worker_live_test.go
- cmd/codex_native_gap_attribution_test.go
- .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/tracer.json

New named test symbols and script modes are specified in the task actions. Existing runtime interfaces remain authoritative.

<interfaces>
Existing runCodexNativeLiveScenario(t, codexNativeLiveScenario), nativeCollectLiveEvidence, nativeCollectQualificationWorkers, nativeInspectParentEvents and validateCodexNativeLiveReceipt share codexNativeLiveReceipt. The runtime bind request requires the actual child_id, not a display task path. nativeCollectLiveEvidence is an observer, never a launcher. Add TestCodexNativeGapAttribution in the named new test file.
</interfaces>

<tasks>
<task type="auto">
  <name>Task 1: Bind native task paths to actual child events without guessing</name>
  <files>cmd/codex_native_worker_live_test.go, cmd/codex_native_gap_attribution_test.go</files>
  <read_first>cmd/codex_native_worker_live_test.go; cmd/codex_native_gap_attribution_test.go; cmd/codex_native_worker.go; cmd/build_worker_run.go; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-01-SUMMARY.md; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-09-SUMMARY.md</read_first>
  <behavior>New TestCodexNativeGapAttribution begins with failing causal controls; known captured shape passes after the narrow repair.</behavior>
  <action>Per D-03/D-04/D-09, reproduce final165 ordinary task-path/UUID mismatch from immutable raw events, then repair only the fixture coordinator and evidence collection. Resolve the task path returned by actual spawn_agent to its child UUID using correlated host call/result and child session metadata; retain both identities and refuse absent/ambiguous mappings. Preserve the runtime Go-issued launch_id and ExecutionBinding unchanged. Classify coordination commands only when exact argv/cwd and unique same-thread host events prove the operation; unknown operations remain incomplete. Keep complete child patch reconstruction, uncached test output and original source/test/module hashes. Do not infer a child from prose, treat the task path as a UUID, weaken exact-prompt comparison or broaden literal script acceptance.</action>
  <acceptance_criteria>Controls reject ambiguous child mapping, wrong thread/turn, duplicate reused events, empty binding/result, parent-only edits and unclassified writes; valid captured host shapes correlate to one bound child.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexNativeGapAttribution|TestCodexNativeWorkerAdmission|TestCodexNativeEvidenceDerivation|TestCodexNativeFixtureCoordinatorBoundary)$' -count=1 -timeout 60s</automated></verify>
  <done>Controls reject ambiguous child mapping, wrong thread/turn, duplicate reused events, empty binding/result, parent-only edits and unclassified writes; valid captured host shapes correlate to one bound child.</done>
</task>

<task type="auto">
  <name>Task 2: Run early ordinary, early-resume and requested-review tracers</name>
  <files>.planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/tracer.json</files>
  <read_first>.planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/tracer.json; cmd/codex_native_worker_live_test.go; cmd/codex_native_auxiliary_test.go; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-07-SUMMARY.md</read_first>
  <action>Per D-01/D-05/D-08/D-09/D-10, build current committed source with explicit -buildvcs=false and independent source inventory, then use the existing disposable installed ant-build fixture. Run ordinary, early-resume and review as distinct actual native scenarios. Ordinary is one accepted Builder plus required checks; review explicitly requests one independent Watcher whose child-specific uncached check and concrete findings are preserved. Early-resume stops after the terminal journal exists but before aggregate/finalizer; a different fresh parent preserves the child result and finishes exactly once. Parent must not edit/check for the worker. Preserve failed trials and identify this tracer as provisional until Plan15's frozen matrix. Report actual host bounds; increasing a timeout requires measured stage timings, not blanket retries.</action>
  <acceptance_criteria>All three cases retain raw parent/child attribution, exact candidate identity and explicit pass/incomplete outcomes; credit proof requires actual child work, useful independent review and byte-stable replay. Missing required evidence remains a gap.</acceptance_criteria>
  <verify><automated>AETHER_CODEX_NATIVE_LIVE=1 AETHER_CODEX_NATIVE_SCENARIOS=ordinary,early-resume,review AETHER_CODEX_NATIVE_EVIDENCE_DIR="$AETHER_GAP_EVIDENCE_ROOT/tracer" go test ./cmd -run '^TestCodexNativeWorkerFreshHost$' -count=1 -json -timeout 60m</automated></verify>
  <done>All three cases retain raw parent/child attribution, exact candidate identity and explicit pass/incomplete outcomes; credit proof requires actual child work, useful independent review and byte-stable replay. Missing required evidence remains a gap.</done>
</task>
</tasks>

<verification>
Every task's automated verification retains raw exit/output and exact candidate identity. Required live evidence must pass on its own merits; test fixtures and receipt replay cannot replace it. Plan16 owns the final complete regression pair and new explicit goal-backward verdict. Shared planning trackers remain the orchestrator's responsibility.
</verification>

<success_criteria>
An ordinary accepted one-Builder job has attributable child edits, uncached checks, immutable terminal and exactly-once credit; the parent only coordinates.
An explicitly reviewed job has a distinct named Watcher and actual independent findings without changing ordinary team policy.
Equal reservation or terminal repeats return the same receipt; conflicting equal-key or empty binding/result requests refuse without mutation.
Missing mandatory proof remains an explicit gap; writing this plan's execution report never marks the phase complete.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-10-SUMMARY.md with task outcomes, exact source and evidence identities, failures and outstanding qualification obligations.
</output>

