# Plan 24 prospective source and task admission

Recorded 2026-09-18T19:55:08.542371+00:00. Authority: [204.2-PROOF-AMENDMENT.md](204.2-PROOF-AMENDMENT.md), SHA-256 `9db637757528466e0e19bcf5789ccd6c125944e4e6f910d624c9211bbc46ccbd`. Root review passed for contract admission only; no worker or phase qualification is claimed.

In plain English: we can implement the checks that refuse unsupported cancellation guarantees now. We still cannot prove the full message and answer reached the intended worker, so those actual worker tests remain blocked. Missing optional host internals are no longer a universal gate.

## Evidence identity and preserved work

The machine record [admission.json](evidence/proof-amendment/admission.json) retains exact source, client, collector and predecessor hashes, per-predicate mappings, raw feasibility references and command outputs. Current HEAD is `47705e66f7859253527b33ad3981fe30c5734c41`, with uncommitted concurrent implementation. The source is **evolving, not frozen**: this record grants no build/resource admission and identifies no qualified candidate binary. It snapshots 848 Go test files and 19 explicitly reviewed runtime/harness/collector files. The reviewed source list is not a complete compilation/embedded-input inventory. Recompute exact final inputs before any candidate qualification.

| Current executable | Read-only observed version | SHA-256 |
|---|---|---|
| aether | `{"ok":true,"result":"1.0.81"}` | `45663b7cc4ca018b5024ad78675f4255f894143b820484a8c2846c8270a7f816` |
| claude | `2.1.272 (Claude Code)` | `195e24e8e1f9bf46f1eaee72d434a33e18f9f5796f29a6348a00d16c5f8aee75` |
| codex | `codex-cli 0.155.0` | `b0b14f9c1901c1ec44671094b2dc18b39e4bd8d36a6dc2302cc9d961a7e2a197` |

Installed Aether identity is recorded only to preserve the separate CalVault runtime boundary. It is not the evolving source candidate. No shared installation, host configuration, credentials or CalVault state was changed.

The offline collector's review-followup records **25 passing tests**, exit 0, on the current script hashes. This documentation task verified those hashes and retained the recorded test outcome; it did not rerun the tests. The earlier **19-test** receipt and feasibility report remain historical, unchanged. Successful offline storage neither authenticates a vendor source nor qualifies a worker.

## Newly measured source feasibility

Both read-only experiments used installed Codex **0.155.0** to read **copied historical 0.154.0** fixture rollouts. They started zero model turns and zero workers; no credentials were copied. Original input and retained artifact hashes were independently checked for this record.

| Experiment | Result | Meaning |
|---|---|---|
| `thread/read`, `includeTurns=true`, parent and child | Requests succeeded; both `turns=[]`, history paginated; no prompt or user message | No actual delivered plaintext or ACK exposed by this read |
| `thread/turns/list`, `itemsView=full`, `limit=1`, ascending, parent and child | Requests succeeded; both `data=[]`, `nextCursor=null`, `backwardsCursor=null` | No further page or actual prompt/ACK exposed for these copied fixtures |

Receipts:
- `/Users/callumcowie/.aether-backups/phase204-2-collector/thread-read-zfpz29rw/receipt.json` — `sha256:a8fed43175a4a9de57b5955ee60f248354e299f7f2ee806560a743981734bf18`; 97 retained artifact hashes verified.
- `/Users/callumcowie/.aether-backups/phase204-2-collector/thread-turns-list-hiybl65p/receipt.json` — `sha256:7f0a781117874beba4fd854bfb4da8b60abb6c4b74cb7cbcbe39389d331fb2c2`; 100 retained artifact hashes verified.

These results leave the mandatory source unresolved. They do **not** establish that every current/future Codex invocation lacks plaintext, and they are not fresh live worker qualification. Do not repeat the unchanged copied-fixture queries. A further bounded source probe requires a genuinely new source/field or materially changed host with a specific mapping to test; it does not authorize decryption, interception, indexing/resume as a hidden model run, or a replacement launcher.

## Source and predicate mapping

| Predicate | Class | Required source and identity | Current disposition |
|---|---|---|---|
| Actual worker assignment/result/credit | Mandatory behavior | Native successful spawn/result events, actual edits/checks, Go reserve/bind/terminal/finalizer; exact parent/child/attempt/task/workspace | New candidate live proof still required; parent work cannot substitute |
| Explicit independent review | Mandatory behavior | Real requested distinct Watcher, attributable findings, checks and result bound to accepted task | New candidate live proof still required |
| Complete delivered context | Mandatory behavior | Exact full capsule/skills/steering/handoff bytes from actual delivery and child ACK; Unicode/whitespace and size bounds retained | **Source unresolved**; historical encrypted `response_item.payload.arguments.message` does not establish plaintext |
| Current scoped answer plus ACK | Mandatory behavior | Question/assignment identity; completed send of exact answer; intended child's actual ACK ordered afterward | **Source unresolved**; no expected-prompt reconstruction or fabricated ACK |
| Core recovery and no duplicate credit | Mandatory behavior | Saved terminal before aggregation, replayable cut, distinct fresh parent public resume, stable finished work/journal and correct unfinished assignment | New candidate early/partial recovery required; unsupported is not a substitute |
| Claude comparison | Mandatory behavior | One real installed `/ant-build` build/check from same prepared baseline with actual helper edits/checks/durable credit | New candidate comparison required; no six-row Codex acquisition prerequisite |
| Complete internal tools/settings | Optional metadata | Actual source-bound export only if reported; client/invocation and reported model/effort remain pinned | **Unavailable**, not captured; positive-export validation stays strict |
| Cancellation | Optional capability | Demonstrated actual support, or actual runtime refusal of unsupported guarantee/unsafe active-child completion and redispatch | **Refusal path selected; live refusal not yet demonstrated by this record** |
| Permission/workspace/nesting/activity | Optional capability | Actual sentinel attempts/native events for support; actual attributable refusal for unavailable operations | Final candidate observations pending |
| Usage/cost | Optional metadata | Actual provider-reported source bound to worker/session | Unreported fields stay absent, never zero or guessed |

Documented Codex candidate fields remain `collabAgentToolCall.prompt`, `senderThreadId`, `receiverThreadIds`, child `userMessage` content and turn/child identity in the installed app-server schema. Optional schema fields are discovery facts; neither experiment produced the actual messages needed to prove full delivery. Existing `nativeGapHostBoundary` and `nativeGapCaptureContext` continue to require source-bound plaintext/ACK before an accepted decoder. Full context and scoped-answer controls cannot be satisfied by unavailable metadata handling.

## Actual refusal operations and proof boundary

Task 25.1 adds `RequireCancellation` to the existing platform/native request admission. A request with `require_cancellation:true` must fail before reserve/launch mutation when the host cannot supply the guarantee. The same flag cannot escalate bind, observe, record, stage or question authority. Ordinary requests without the extra guarantee retain supported behavior.

Relevant executable checks are `TestNativeWorkerCancellationRequirement`, `TestCodexNativeCancellationRequirementReserveRefusal` and `TestCodexNativeCancellationRequirementEscalationRefusal`; these invoke the actual registered runtime route and inspect unchanged durable inventory. This record names their contract, not a test result. Plan 26 separately needs a real bound active child: public pause/resume must not write an unsafe handoff, staging/finalization must not grant completion, and reserve/redispatch must not replace ambiguously active work. Retain exact runtime commands/results, child/attempt identity, before/after source/journal/credit and actual spawn accounting. `TestCodexNativeRecoveryCancelPending` is a deterministic guard reference, not live evidence.

Unknown cancellation remains nonterminal; `NoPostAckWrites` stays false/unproved. Timeouts, quiet logs, process cleanup and collector EOF cannot establish a cancelled terminal or no-write guarantee. Malformed or substituted source cannot support a positive claim. A locally authored `refused:true` record is insufficient for safe-refusal qualification.

## Per-task admission

| Task | Implementation | Live work | Boundary |
|---|---|---|---|
| 25.1 metadata/RequireCancellation | **Admitted** | No live qualification authorized by this task | Exact amendment/source contract, scoped ownership and focused controls |
| 25.2 actual delivery decoder | **Blocked** | **Blocked** | Actual complete plaintext/current-answer/child ACK source missing |
| 25.3 ordinary/review/question proof | **Blocked** | **Blocked** | Requires 25.2 plus frozen candidate/resource admission |
| 26.1 deterministic support/refusal | Eligible after root-reviewed 25.1 | Not live proof | Independent of unresolved 25.2; review condition must be checked before release |
| 26.2 actual cancellation/refusal | Not yet admitted | **Blocked pending its own prerequisites** | Real owned bound child/control path, implemented guards, resource admission and actual runtime responses |
| 21 → 22 → 23 | Dependent | Not admitted | All mandatory worker/context/recovery proof and actual support/refusal evidence remain necessary |

All eleven scenario names and four final regression runs remain in the machine record. Mandatory ordinary/review/question/core-recovery cases cannot be marked unsupported. Setting optional tools/settings unavailable never releases 25.2 or 25.3. Implementing deterministic refusal alone cannot release 26.2 or later qualification.

## Verification and remaining work

The admission record validates referenced hashes, amendment identity, collector-script identity, both experiment input/artifact inventories and prospective predicate separation. Historical `acquisition.json` stays `blocked`, `ready_for_adapter=false`; no old capture was relabelled. Root review confirms these task-level release boundaries. Plan 24 may close as admission-only preparation; no requirement credit, candidate freeze, broad suite or live success follows.
