# Second gap-closure execution disposition

Status: **gaps_found; dependent qualification blocked**. This is a new execution record, not a replacement for the protected canonical verification.

The reported Codex-to-Claude routing bug is fixed: detected Codex, Claude Code and OpenCode hosts pin their matching worker dispatcher. An unavailable matching provider stops dispatch; only an explicit override selects another provider. Unknown-shell selection remains compatible, and empty provider environment variables do not establish a host. Routing commits are recorded in `evidence/gap-closure-2/same-host-routing.json`. Focused normal/race tests and command-guide checks pass; the full provider package passed before the final empty-marker correction, which has its own focused normal/race coverage. No installed binary or hub was changed.

## Completed source work and limits

- Plan17: consumed-input/storage guard repaired, isolated normal/race preflight passed, and an ordinary native Builder tracer passed on its recorded earlier candidate. Subsequent source changes mean that tracer does not qualify the current candidate.
- Plan18: semantic configuration/tool-schema controls pass normal/race. Supported invocation-bound complete host/tool export was not established in the inspected installed interfaces. Its live trial remains incomplete; successful child work does not replace missing provenance.
- Plan19: malformed cancellation records and inconsistent retained recovery snapshots reject. Final cancellation controls pass 45 cases per normal/race lane; recovery controls pass 82 per lane. Structural consistency cannot grant actual cancellation or absence-of-write proof. Independent review found no remaining concrete blocker in these repairs.
- Plan20: the observed single-quoted literal command parser defect is repaired without JavaScript evaluation. Duplicate keys, expressions, unrelated operations and incorrect event correlation remain rejected. Focused attribution/context controls pass 164 cases per normal/race lane. Immutable-log replay is diagnostic only. Actual review/context acceptance remains blocked on the unsupported acquisition path.

## Why dependent work stopped

The shared execution contract in `204.2-GAP2-PLANNING.md` requires: “If capacity or host observation cannot be obtained within these bounds, record the measured blocker and stop dependent operations; do not claim completion.” It also says a missing supported export path must stop dependent acceptance. The installed-interface limitation is recorded in Plan18's `host-provenance.json`; actual completed cancellation observation provenance is also unavailable.

Plans21–23 remain pending. There is no fresh final-candidate eleven-case matrix, no four-run final regression accounting, no refreshed canonical verification and no ANT requirement closure. Repeating known blocked live runs would not supply the absent host interface. Resume dependent qualification when a supported capture path exists; preserve exact source/capture identities and rerun affected live proof plus final regression accounting.

## Preservation and delivery

Raw artifacts remain under `/private/tmp/aether-gap2-resume-x7pso2qh`. Original Plans01–16, prior evidence and Phase205 remain unchanged. The original halted Plan17 summary is retained byte-for-byte in `evidence/gap-closure-2/original-17-halted-summary.md`. Shared installation, CalVault, owner acceptance and the prior npm incident have not been rewritten or claimed resolved. No dev/stable/public publication occurred.
