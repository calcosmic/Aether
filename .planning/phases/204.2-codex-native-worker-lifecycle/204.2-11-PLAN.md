---
phase: 204.2-codex-native-worker-lifecycle
plan: "11"
type: execute
wave: 9
depends_on: ["204.2-10"]
files_modified:
  - cmd/codex_native_worker_live_test.go
  - cmd/codex_native_gap_context_test.go
  - .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/context.json
autonomous: true
gap_closure: true
requirements: [ANT-06]
must_haves:
  truths:
    - "Full required capsule, matched skills, steering and handoffs reach the intended child unchanged; absent required brief refuses."
    - "The exact answer is unavailable to the child before its governed question; only the intended bound delivery changes its behavior."
    - "Empty required context refuses; the 2 MiB request limit and one byte above are tested; Unicode and digests preserve exact bytes without rounding."
  artifacts:
    - path: cmd/codex_native_worker_live_test.go
      provides: "Executable implementation or proof contract"
    - path: cmd/codex_native_gap_context_test.go
      provides: "Executable implementation or proof contract"
    - path: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/context.json
      provides: "Source-pinned gap-closure evidence"
  key_links:
    - from: cmd/codex_native_worker_live_test.go
      to: .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/context.json
      via: "Source-bound checks and preserved qualification evidence"
---

<objective>
Prove exact initial context and uncontaminated scope-bound answer delivery.

Purpose: Close the observed qualification gap while preserving historical evidence. In plain English, prove the worker really did the promised work on the current program and clearly retain anything still unproved.
Output: cmd/codex_native_worker_live_test.go, cmd/codex_native_gap_context_test.go, .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/context.json.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-CONTEXT.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-RESEARCH.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-PATTERNS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-SYNTHESIS.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-VERIFICATION.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-REVIEW.md
@.planning/phases/204.2-codex-native-worker-lifecycle/204.2-GAP-PLAN-AUDIT.md
@.planning/research/ant-codex-parity-2026-09-16/GSD-INTEGRATION.md
</context>

## Execution boundaries

D-01..D-10 refer to the existing SYNTHESIS mapping of unnumbered CONTEXT decisions, not new owner decisions. Preserve all Plans 01–09, summaries, original evidence and reviews byte-for-byte. Write new receipts under evidence/gap-closure/ and a new durable external run root. Install only inside disposable child environments; never replace shared aether, hub, Codex/Claude homes, CalVault or Phase 205 evidence. Keep current branch and unrelated edits. Do not initiate login, fresh authentication, publication or owner walkthrough. Record existing-auth failure honestly. No new packages or database schema are involved; security enforcement is disabled in project config.

Pin current source revision/status, production inventory, test corpus, live harness, candidate binary/build arguments, Go version/environment, host executable/version/model/effort/tool schema and installed bytes. The installed narrow CalVault hotfix is not this candidate. Old 165db28c, f9e32ca0 and cf51e9ac receipts keep their original identities. A reporting-only commit may reference equal bytes but cannot relabel binaries. Production changes require final live requalification; harness changes require new capture where collection/causality changed, and separately pinned raw replay for parser-only corrections; changed test corpus invalidates previous full-run credit.

Reversibility: source/test edits are reversible through their scoped commits; disposable fixtures can be removed only after external raw evidence is preserved. Journal/result contract changes are costly: preserve readers, immutable terminal records and ExecutionBinding; do not migrate any shared store. A host limitation is a measured incomplete/refused outcome, never permission to remove a required scenario. Live checks may exceed 60 seconds; their explicit bounds preserve evidence and stop owned processes. Ordinary bounded test verification must be run normally and with -race when runtime or harness behavior changes.

## Artifacts this phase produces

- cmd/codex_native_worker_live_test.go
- cmd/codex_native_gap_context_test.go
- .planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/context.json

New named test symbols and script modes are specified in the task actions. Existing runtime interfaces remain authoritative.

<interfaces>
nativeQualificationPrompt and codexNativeQualificationCoordinator currently disclose the fixed fixture response through readable coordinator text; nativeVerifyQuestionBehavior is harness evidence only. Preserve native context delivery payload/payload_sha256 and exact completed-send acknowledgement contract. New TestCodexNativeGapContext covers evidence causality; do not invent a second decision store.
</interfaces>

<tasks>
<task type="auto">
  <name>Task 1: Remove answer contamination and capture causal delivery evidence</name>
  <files>cmd/codex_native_worker_live_test.go, cmd/codex_native_gap_context_test.go</files>
  <read_first>cmd/codex_native_worker_live_test.go; cmd/codex_native_gap_context_test.go; cmd/codex_native_context.go; cmd/codex_native_decisions.go; cmd/codex_native_context_test.go; cmd/codex_native_decisions_test.go; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-03-SUMMARY.md; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-04-SUMMARY.md</read_first>
  <behavior>Known contaminated final165 input must not qualify; delayed fixture authorization and exact successful message-to-child linkage are necessary.</behavior>
  <action>Per D-04/D-05, replace the question fixture's inherited predeclared answer with an isolated per-run challenge selected by the fixture controller only after a genuine child question. The selection is fixture authority, never owner testimony. Keep controller answer material outside child-readable fixture paths and out of launch prompt, inherited conversation, coordinator script read by the parent, tests and prior context. Use a fresh child without answer-bearing history and record the actual host context mode. Capture actual outgoing launch and message argument bytes at the host tool boundary, correlated with successful call/result and receiving child identity; compare them to Go-issued bytes, not parent prose or encrypted export metadata. If the host does not expose independently corroboratable plaintext, retain the exact-payload gap. Refactor the existing coordinator so it performs only registered question, decision-answer --native-request, context and completed-send context-ack operations; no answer fallback or self-acknowledgement. Preserve old fixture/capture identities.</action>
  <acceptance_criteria>New TestCodexNativeGapContext rejects answer leakage through prompt/history/fixture files, altered whitespace/Unicode, mismatched child, send failure, read-only inspection ACK, stale answer and missing payload provenance; existing scope isolation remains passing.</acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexNativeGapContext|TestCodexNativeContextExactBytes|TestCodexNativeContextBounds|TestCodexNativeDecisionDelivered|TestCodexNativeDecisionStaleInterleaving)$' -count=1 -timeout 60s</automated></verify>
  <done>New TestCodexNativeGapContext rejects answer leakage through prompt/history/fixture files, altered whitespace/Unicode, mismatched child, send failure, read-only inspection ACK, stale answer and missing payload provenance; existing scope isolation remains passing.</done>
</task>

<task type="auto">
  <name>Task 2: Capture actual complete context and answer-controlled child behavior</name>
  <files>.planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/context.json</files>
  <read_first>.planning/phases/204.2-codex-native-worker-lifecycle/evidence/gap-closure/context.json; cmd/codex_native_worker_live_test.go; cmd/colony_prime_context.go; .planning/phases/204.2-codex-native-worker-lifecycle/204.2-09-SUMMARY.md</read_first>
  <action>Run the current-source question scenario using the repaired harness. Include recognizable capsule, skill, steering and prior-handoff facts in the accepted runtime fixture and bind them to exact issued/delivered context bytes. Preserve the genuine question, fixture-authorized answer, immutable envelope, host send result, subsequent ACK and child-authored answer-dependent behavior in chronological order. Exercise stale/wrong-child answer refusals with unchanged journals. Independently scan the pre-question child-accessible material for the chosen answer and prove it was absent. Keep the separate behavior checker explicitly harness-owned; it cannot substitute for the child's own required check. Record encryption/unavailable host payload as incomplete, never reclassify behavior alone as byte-equality proof.</action>
  <acceptance_criteria>Receipt links all required context sources, exact delivery and question/answer scope to the same accepted child; stale routes refuse and no pre-question leak exists. Any unobservable mandatory link prevents ANT-06 passing.</acceptance_criteria>
  <verify><automated>AETHER_CODEX_NATIVE_LIVE=1 AETHER_CODEX_NATIVE_SCENARIOS=question AETHER_CODEX_NATIVE_EVIDENCE_DIR="$AETHER_GAP_EVIDENCE_ROOT/context" go test ./cmd -run '^TestCodexNativeWorkerFreshHost$' -count=1 -json -timeout 30m</automated></verify>
  <done>Receipt links all required context sources, exact delivery and question/answer scope to the same accepted child; stale routes refuse and no pre-question leak exists. Any unobservable mandatory link prevents ANT-06 passing.</done>
</task>
</tasks>

<verification>
Every task's automated verification retains raw exit/output and exact candidate identity. Required live evidence must pass on its own merits; test fixtures and receipt replay cannot replace it. Plan16 owns the final complete regression pair and new explicit goal-backward verdict. Shared planning trackers remain the orchestrator's responsibility.
</verification>

<success_criteria>
Full required capsule, matched skills, steering and handoffs reach the intended child unchanged; absent required brief refuses.
The exact answer is unavailable to the child before its governed question; only the intended bound delivery changes its behavior.
Empty required context refuses; the 2 MiB request limit and one byte above are tested; Unicode and digests preserve exact bytes without rounding.
Missing mandatory proof remains an explicit gap; writing this plan's execution report never marks the phase complete.
</success_criteria>

<output>
Create .planning/phases/204.2-codex-native-worker-lifecycle/204.2-11-SUMMARY.md with task outcomes, exact source and evidence identities, failures and outstanding qualification obligations.
</output>

