---
phase: 203-biological-runtime
plan: "15"
type: execute
wave: 8
depends_on: ["203-09", "203-13", "203-14"]
files_modified:
  - .aether/workers.md
  - CLAUDE.md
  - .aether/rules/aether-colony.md
  - .claude/rules/aether-colony.md
  - cmd/recruitment_latency_test.go
  - cmd/claudemd_biological_runtime_test.go
  - cmd/workers_doc_test.go
autonomous: true
requirements: [BIO-02, BIO-03, BIO-06]
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A build that never recruits performs no probe, no recruitment file read and no recruitment function call, measured rather than asserted in prose."
    - "Nothing in this phase adds a required step to an ordinary build or check; the number of owner-facing pauses on a no-recruitment run is unchanged."
    - "The worker instructions no longer describe a spawning mechanism no caste has ever been granted, and no longer state spawn limits that contradict the enforced ones."
    - "The worker instructions describe the real path — the public command, what a refusal means, and that a refusal is not an error."
    - "Every new sentence written into the shipped documentation about how this runtime behaves names the test that locks it, and a guard fails if a cited test stops existing."
    - "The colony rules are edited at their canonical source, `.aether/rules/aether-colony.md`, and the generated copy at `.claude/rules/aether-colony.md` — the only other copy of that file in the repository — stays byte-identical to it, which is exactly what the existing generation-parity check enforces."
    - "No repo-invented word appears in the owner-facing documentation added by this phase without an inline plain-English translation."
  artifacts:
    - path: "cmd/recruitment_latency_test.go"
      provides: "The measured proof that an ordinary build pays nothing for recruitment"
      contains: "TestNoRecruitmentPathCostsNothing"
    - path: "cmd/claudemd_biological_runtime_test.go"
      provides: "The guard that every documented claim names a live test"
      contains: "TestCLAUDEMDBiologicalRuntimeClaimsCiteLiveTests"
    - path: "cmd/workers_doc_test.go"
      provides: "The guard that the shipped worker instructions describe only mechanisms the runtime grants"
      contains: "TestWorkersDocMatchesRuntime"
  key_links:
    - from: "CLAUDE.md"
      to: "cmd/claudemd_biological_runtime_test.go"
      via: "every documented claim names a test the guard can find"
      pattern: "TestCLAUDEMD"
    - from: ".aether/rules/aether-colony.md"
      to: ".claude/rules/aether-colony.md"
      via: "the canonical rule file is edited and the generated copy is regenerated from it, never hand-edited"
      pattern: "ClaudeRuleGenerationParity"
  prohibitions:
    - statement: "Never add a required step, prompt or pause to an ordinary build or check in this phase."
      status: flagged-unverified
    - statement: "Never leave a documented mechanism in the shipped instructions that no caste can actually perform."
      status: flagged-unverified
    - statement: "Never write a claim about runtime behaviour into a shipped document without a test that can fail."
      status: flagged-unverified
    - statement: "Never use a repo-invented word in owner-facing text without translating it inline."
      status: flagged-unverified
---

<objective>
Close the phase against its two standing constraints: the owner's stated priority that nothing here may cost turnaround time, and this project's own rule that a documented claim about runtime behaviour must be testable or removed.

Purpose: the instructions shipped to every worker currently describe a spawning protocol in complete, convincing detail — a step-by-step sequence, exact commands, an exact tool invocation — for a mechanism no caste has ever been granted, alongside a spawn-limit table that contradicts the limit the runtime actually enforces. That is the repository's signature failure written down. This phase's real path now exists, so the false description has to go and the true one has to be locked to a test. And the promise that an ordinary build pays nothing for any of this has to be a measurement, because a promise about latency that nobody measures is a promise that quietly stops being true.
Output: a measured latency floor, truthful worker instructions, and documentation whose every claim names a live test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/203-biological-runtime/203-CONTEXT.md
@.planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md
@.planning/todos/pending/2026-08-27-worker-turnaround-is-too-slow.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Measure that an ordinary build pays nothing</name>
  <files>cmd/recruitment_latency_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/recruitment.go
    - cmd/recruitment_probe.go
    - cmd/recruitment_admission.go
    - cmd/recruitment_subtree.go
    - cmd/pheromone_outcome.go
    - cmd/codex_build.go
    - cmd/codex_continue.go
    - cmd/ceremony_team_checkin.go
    - .planning/todos/pending/2026-08-27-worker-turnaround-is-too-slow.md
    - .planning/phases/201-queen-led-work-cycle/201-TURNAROUND-BASELINE.md
  </read_first>
  <behavior>
    - A build with no spawn claims and no recruitment invokes the probe zero times.
    - The same build reads zero files under the recruitment store prefix and zero files under the credit store prefix.
    - The same build reaches no recruitment or credit function, derived from the call graph rather than from a file-access counter alone.
    - The number of owner-facing pauses on a no-recruitment build is identical to the count before this phase, taken from the recorded baseline rather than a typed number.
    - A check with no credit records reaches the tuning pass but performs zero writes.
    - Each of these is a counter assertion, not an elapsed-time assertion, so the test is not flaky on a loaded machine.
  </behavior>
  <action>Create `cmd/recruitment_latency_test.go` with `TestNoRecruitmentPathCostsNothing`, driving a real ordinary build through the same entry point the existing build tests use, with counters wrapped around the probe launcher, the store reader and the admission gate. Assert zero on each. Assert the same for the credit store on that build.

Add `TestNoNewMandatoryStep`, comparing the owner-facing pause count on a no-recruitment build against the count recorded in the existing turnaround baseline artifact, so the assertion's expected value is derived from the repository's own recorded measurement rather than typed as a plausible literal.

Add `TestTuningPassIsFreeWithoutCredit`, asserting the end-of-check tuning pass performs zero store writes when no credit records exist.

Use counters, not wall-clock timing, for all three: a timing assertion on shared hardware is a flaky test, and a flaky test is a test the project learns to ignore.</action>
  <acceptance_criteria>
    - Adding a probe call to the ordinary build path makes `TestNoRecruitmentPathCostsNothing` fail and the failure names the counter that moved.
    - Adding a required prompt to the build path makes `TestNoNewMandatoryStep` fail.
    - Making the tuning pass write on an empty credit store makes `TestTuningPassIsFreeWithoutCredit` fail.
    - No test in this file asserts on elapsed wall-clock time.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestNoRecruitmentPathCostsNothing|TestNoNewMandatoryStep|TestTuningPassIsFreeWithoutCredit)$' -count=1</automated>
  </verify>
  <done>The promise that this feature is free when unused is a counter that fails, not a sentence in a plan.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the false spawning instructions with the real ones</name>
  <files>.aether/workers.md, cmd/workers_doc_test.go</files>
  <read_first>
    - AGENTS.md
    - .aether/workers.md
    - cmd/recruitment.go
    - cmd/recruitment_admission.go
    - cmd/spawn.go
    - cmd/spawn_budget.go
    - .claude/agents/ant/aether-builder.md
    - .claude/agents/ant/aether-queen.md
    - .planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md
  </read_first>
  <action>Rewrite the "Spawning Sub-Workers" and "Step-by-Step Spawn Protocol" sections of `.aether/workers.md`. Remove the instruction to use the Task tool with a general-purpose subagent type: no worker caste has ever been granted that tool, so the instruction has never been executable, and the synthesis records that finding. Remove the "Spawn limits: Depth 1 to 4, Depth 2 to 2, Depth 3 to 0" line, which contradicts the enforced cap of two that the same document states three paragraphs earlier.

Replace both with the real protocol: a worker that needs help runs the public recruitment command with its parent name, the caste it wants, the objective, the reason and any evidence; the program decides; an admitted request produces a real helper and an inline line the owner sees; a refused request is not an error and the worker finishes the work alone. Keep the existing depth and budget explanation, which is correct, and keep the existing guidance on when help is genuinely warranted.

State the per-run depth flag honestly: it exists, it raises the cap for one run only, and every use is written permanently into the run's own record.

Apply the project's plain-English rule to every sentence added: no repo-invented word — colony, caste, pheromone, midden, trophallaxis, ratchet — appears without an inline translation on first use in the rewritten sections.

Create `cmd/workers_doc_test.go` with `TestWorkersDocMatchesRuntime`. This test does not exist yet, so the rewrite has nothing locking it until this file is added: it reads `.aether/workers.md` from the repository root and fails by name if the document teaches a tool invocation no caste is granted, or states a depth limit that disagrees with the limit the runtime enforces, deriving that limit from the runtime's own constant rather than from a typed literal. Assert both directions — a document that has lost the real recruitment command also fails — so the guard cannot pass on an empty or truncated file.</action>
  <acceptance_criteria>
    - `grep -n 'subagent_type' .aether/workers.md` returns nothing in the rewritten sections.
    - `grep -n 'Depth 1→4\|Depth 1 to 4' .aether/workers.md` returns nothing.
    - The rewritten section names the public recruitment command and states in plain words that a refusal is not an error.
    - The rewritten section states that the per-run depth flag is recorded permanently.
    - Every repo-invented word in the rewritten sections is followed by an inline translation on first use.
    - `TestWorkersDocMatchesRuntime` exists in `cmd/` and fails when the enforced depth limit is changed in the runtime without the document following it.
  </acceptance_criteria>
  <verify>
    <automated>! grep -q 'subagent_type' .aether/workers.md &amp;&amp; ! grep -qE 'Depth 1.?(→|to) ?4' .aether/workers.md &amp;&amp; grep -q 'aether recruit' .aether/workers.md &amp;&amp; go test ./cmd -run '^TestWorkersDocMatchesRuntime$' -count=1</automated>
  </verify>
  <done>The instructions every worker reads describe something they can actually do, with limits that match the ones the program enforces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock every documented claim to a live test</name>
  <files>CLAUDE.md, .aether/rules/aether-colony.md, .claude/rules/aether-colony.md, cmd/claudemd_biological_runtime_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - CLAUDE.md
    - .aether/rules/aether-colony.md
    - .claude/rules/aether-colony.md
    - cmd/claudemd_coherent_jobs_test.go
    - cmd/claudemd_live_colony_test.go
    - cmd/claudemd_verification_depth_test.go
    - cmd/current_vocabulary_docs_199_test.go
    - .planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md
  </read_first>
  <behavior>
    - Every sentence added to the shipped documentation about this phase's runtime behaviour names at least one test.
    - A cited test name that no longer exists fails the guard, naming the citation and the file it appears in.
    - The canonical colony-rules file is the one edited, and the generated copy is regenerated from it and remains byte-identical to it. These two files are the only copies of that document in the repository; there is no OpenCode or Codex copy of it to keep in step.
    - The added owner-facing section passes the plain-English rule: no untranslated repo-invented word.
    - A claim added without a citation fails the guard.
  </behavior>
  <action>Add a section to `CLAUDE.md` describing what this phase actually delivers, in the register the file's own opening instruction demands: a worker that gets stuck can ask for a helper; the program, not the assistant, decides whether it gets one, using limits on how deep the chain goes, how many helpers the whole run may use, whether the helper is allowed to touch what it wants to touch, and whether someone is already doing that exact job; a refusal never stops the work; the owner sees a line when a helper joins and a line when one is refused, in the window they are already using; at the end they see the family tree with what each branch cost; and notes get stronger or weaker based on what actually happened afterwards, with everything the owner set left alone. Every claim names its test.

Mirror the matching summary into the colony-rules document, editing `.aether/rules/aether-colony.md` — the canonical source — and never the generated copy. Then bring `.claude/rules/aether-colony.md` back into step by regenerating it from the canonical file through the same production path the parity check exercises (`runUpdateSync` from a staged hub), or by copying the canonical file over it verbatim, which is what that path produces. Hand-editing only the generated copy drifts it from canonical and fails the existing `TestCurrentVocabularyDocs199/ClaudeRuleGenerationParity` check; hand-editing both by hand risks a byte-level difference for the same reason. Do not edit either file to soften a claim; if a claim cannot be tested, remove it instead.

Verify before writing: these two files are the only copies of the colony rules in the repository. Do not add a third, and do not write an acceptance claim about copies that do not exist.

Create `cmd/claudemd_biological_runtime_test.go` with `TestCLAUDEMDBiologicalRuntimeClaimsCiteLiveTests`, following `TestCLAUDEMDCitesLiveTestsForCoherentJobClaims`'s existing approach: extract every test name cited in the new section and fail by name for any that no longer exists in the package. Add `TestCLAUDEMDBiologicalRuntimeSectionIsPlainEnglish`, failing by name for any repo-invented word in the new section that is not followed by an inline translation, using the vocabulary table the project's own guidance already lists.</action>
  <acceptance_criteria>
    - Every claim in the new `CLAUDE.md` section names at least one test, asserted by the guard.
    - Renaming any cited test makes `TestCLAUDEMDBiologicalRuntimeClaimsCiteLiveTests` fail and the failure names the citation.
    - `.claude/rules/aether-colony.md` is byte-identical to `.aether/rules/aether-colony.md` after the edit, asserted by the existing generation-parity check rather than by a new one.
    - An untranslated repo-invented word in the new section makes `TestCLAUDEMDBiologicalRuntimeSectionIsPlainEnglish` fail and names the word.
    - The full Go test suite passes, and `go vet ./...` is clean.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestCLAUDEMDBiologicalRuntime|TestCurrentVocabularyDocs199)' -count=1 &amp;&amp; diff .aether/rules/aether-colony.md .claude/rules/aether-colony.md &amp;&amp; go vet ./... &amp;&amp; go build ./...</automated>
  </verify>
  <done>Every sentence this phase adds to the shipped documentation is one a failing test would catch, which is the only kind of sentence this project has agreed to write.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created by this plan (new to the codebase — not drift):

- `cmd/recruitment_latency_test.go`, `cmd/workers_doc_test.go`, `cmd/claudemd_biological_runtime_test.go` (new files)
- `TestNoRecruitmentPathCostsNothing`, `TestNoNewMandatoryStep`, `TestTuningPassIsFreeWithoutCredit`, `TestWorkersDocMatchesRuntime`, `TestCLAUDEMDBiologicalRuntimeClaimsCiteLiveTests`, `TestCLAUDEMDBiologicalRuntimeSectionIsPlainEnglish` (new Go tests)
- A new "Biological Runtime" section in `CLAUDE.md`, and its mirror written into the canonical `.aether/rules/aether-colony.md` and regenerated into `.claude/rules/aether-colony.md` (new documentation)

<verification>
- A build that never recruits is measurably unchanged, by counters rather than timers.
- The shipped worker instructions describe only mechanisms that exist.
- Every documented claim names a test that can fail.
</verification>

<success_criteria>
- The owner's turnaround priority is protected by a test rather than by good intentions.
- The repository stops shipping a detailed protocol for something nothing could do.
</success_criteria>

<output>
Create `.planning/phases/203-biological-runtime/203-15-SUMMARY.md` when done.
</output>
