---
phase: 203-biological-runtime
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md
  - cmd/testdata/classic-contract/v1/schema.json
  - cmd/testdata/classic-contract/v1/mechanisms.json
  - cmd/classic_contract_test.go
autonomous: true
requirements: [SYNTH-05]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every Phase 203 recruitment, delegation, result-exchange, trophallaxis and pheromone mechanism carries an evidence-backed keep-current, restore-modern, replace-better or retire-with-proof disposition recorded before any runtime work begins (SYNTH-05)."
    - "The study separates what was causally real, what was presentation, and what was platform-dependent, and names for each Classic mechanism whether a live Go successor exists today."
    - "The study records, as a decided row rather than a silent assumption, that the TypeScript SpawnOrchestrator is reachable in production through worker-dispatch and wave-orchestrator, correcting RESEARCH.md's claim that it is uncalled."
    - "The study records the D-11 tension explicitly: the owner's locked per-run raise flag against spawnTreeBudgetMax's own comment that a limit you can raise under pressure is a limit that gets raised."
    - "Each of the six capability rows routed to Phase 203 is independently confirmed or revised against cited Classic and current-Go evidence rather than inherited from the ledger's hypothesis."
    - "Every locked decision D-01 through D-12 traces to at least one SYN-203 row and at least one named implementation plan in the range 203-02 to 203-15."
    - "A SYN-203 row for which no Classic evidence can be recovered records an explicit no-historical-evidence citation and a current-only disposition, and is never omitted from the matrix."
    - "The versioned Classic contract corpus accepts SYN-203 mechanism identifiers and Phase 203 groups, and rejects an unknown or case-variant SYN identifier by name."
  artifacts:
    - path: ".planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md"
      provides: "Signed Phase 203 biological-runtime mechanism study, CAP routing and open-question rulings"
      contains: "SYN-203-12"
    - path: "cmd/testdata/classic-contract/v1/mechanisms.json"
      provides: "SYN-203 mechanism registry entries"
      contains: "SYN-203-01"
    - path: "cmd/testdata/classic-contract/v1/schema.json"
      provides: "Widened synthesis-decision pattern and Phase 203 group enum"
      contains: "V-203-ADMISSION"
  key_links:
    - from: "cmd/testdata/classic-contract/v1/mechanisms.json"
      to: ".planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md"
      via: "SYN-203 and CAP identifiers"
      pattern: "SYN-203-"
  prohibitions:
    - statement: "Never justify an implementation task by a Classic feature name or a remembered behaviour alone."
      status: flagged-unverified
    - statement: "Never describe recruitment as being restored or reconnected; no caste has ever held the tool grant the existing narration assumes."
      status: flagged-unverified
    - statement: "Never present an unresolved owner choice as an engineering conclusion."
      status: flagged-unverified
    - statement: "Never record a disposition for a routed capability row without citing both historical and current evidence."
      status: flagged-unverified
---

<objective>
Produce the mechanism study SYNTH-05 requires and SYNTH-07 makes a hard planning gate, and widen the versioned Classic contract corpus to accept Phase 203 mechanism identifiers.

Purpose: this phase's named capability — a worker asking for help mid-task and getting it — has no live path on the interactive lane and a *second, competing* live path on the host lane that never consults the Go safety kernel. Without a cited comparison of what the Classic mechanism actually did, what the current Go path actually does, and where the two lanes already disagree, this phase's most likely failure is building a third parallel recruitment engine beside the two that exist. Two open questions must additionally be ruled on in writing before any design is fixed: whether the owner's per-run depth-raise flag (D-11) may override the sibling budget constant's deliberate non-configurability, and whether native platform nesting is attempted at all in this phase.
Output: a signed `203-CLASSIC-SYNTHESIS.md` carrying SYN-203-01..12, plus a corpus schema and mechanism registry that accept those identifiers.

Tracer note: this plan is the SYNTH-07 planning gate and produces no runtime behaviour, so it carries no `type="tracer"` task. The phase's tracer — the thin end-to-end slice through every layer this phase modifies — is Task 1 of plan `203-02`, which depends on this one.

Flagged planner assumption (unresolved edge probe, SYNTH-05, category `unclassified`): the boundary between "mechanism study" and "design document" is not specified anywhere. This plan assumes the study may record rulings that fix design (adapter default, depth-flag disposition, name reclamation) because SYNTH-05's own wording says "before design is fixed", and that a later reviewer disagreeing with that reading would be disagreeing about scope, not correctness. Surfaced, not resolved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/203-biological-runtime/203-CONTEXT.md
@.planning/phases/203-biological-runtime/203-RESEARCH.md
@.planning/phases/203-biological-runtime/203-PATTERNS.md
@.planning/research/v1.28-classic-synthesis-template.md
@.planning/research/v1.28-classic-capability-ledger.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the cited Phase 203 biological-runtime mechanism synthesis</name>
  <files>.planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md (create; confirm absent)
    - .planning/research/v1.28-classic-synthesis-template.md
    - .planning/research/v1.28-classic-capability-ledger.md
    - .planning/phases/203-biological-runtime/203-CONTEXT.md
    - .planning/phases/203-biological-runtime/203-RESEARCH.md
    - .planning/phases/203-biological-runtime/203-PATTERNS.md
    - .planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md
    - .planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md
    - .aether/dreams/2026-09-01-comprehensive-aether-colony-review.md
    - .planning/codebase/ARCHITECTURE.md
    - .planning/codebase/CONCERNS.md
    - .aether/workers.md
    - cmd/spawn.go
    - cmd/spawn_budget.go
    - cmd/spawn_ancestor.go
    - cmd/immune.go
    - cmd/agency_contract.go
    - cmd/pheromone_write.go
    - cmd/pheromone_loader.go
    - cmd/context.go
    - cmd/suggest_approve.go
    - cmd/codex_dispatch_contract.go
    - pkg/agent/spawn_tree.go
    - pkg/colony/lifecycle.go
    - pkg/colony/pheromones.go
    - pkg/codex/permission_profile.go
    - pkg/events/colony_live.go
    - .aether/ts-host/src/spawn-orchestrator.ts
    - .aether/ts-host/src/wave-orchestrator.ts
    - .aether/ts-host/src/worker-dispatch.ts
    - .aether/ts-host/src/platform-dispatcher.ts
    - .aether/ts-host/src/host.ts
  </read_first>
  <action>Instantiate `.planning/research/v1.28-classic-synthesis-template.md` for area `biology`, phase `203`. Front matter sets `phase: "203"` and `area: "biology"`.

Reconstruct the Classic mechanism from the historical anchors `3a5b81c2`, `v5.0.0` and `v5.4` as `OLD-*` rows, each citing an exact `git show <anchor>:<path>` reference, covering: worker help-seeking and sub-worker spawning, the depth/budget rules as they were then stated, how a child's result returned to its parent, how knowledge was handed back, and how pheromone signals propagated and affected later work. For each OLD row, state in the required "what was causally real" cell whether the behaviour was real, presentation, or honour-system.

Audit the current Go and TypeScript mechanism as `NOW-*` rows, each citing `path:line` plus the named test that locks it where one exists. Cover at minimum: `spawnCanSpawnDecision`, `spawnTreeBudgetState`/`spawnTreeBudgetReason`, `spawnAncestorCycleReason`, `spawnMaxDelegationDepth`, `spawnTreeBudgetMax`, `spawnLogCmd`/`spawnCompleteCmd`/`spawnCanSpawnCmd`, `agent.SpawnTree`, `PermissionProfileForCaste`, `validateSpendContainedPath`, `PauseHandoff` with `LifecycleTransactionReference` and `LifecycleReceiptReference`, `workerHandoffRecord` with `persistDispatchWorkerHandoff`, `writePheromoneSignal`, `computeEffectiveStrength`, `extractSignalTexts`, `extractSignalTextsFrom`, `filterSignalsForPrompt`, `resolvePheromoneSection`, `colony.PendingSuggestion` with `suggestApproveCmd`, `AgencyReceiptEvidence` with `SwarmPhase202Limitation`, `emitColonyLive` with `events.ColonyLivePayload`, `createSpawnOrchestrator` with `processClaims`, `dispatchWaves`, `spawnWorker`, `runProbe`, `runPreflight` and `resolvePreflightTimeoutMs`.

Record exactly twelve comparative synthesis rows with these identities and subjects: SYN-203-01 typed recruitment intent emitted through a public work path; SYN-203-02 one Go admission authority — the existing three-check chokepoint extended, never forked; SYN-203-03 atomic manifest amendment before dispatch and unavailable-truth-denies-launch; SYN-203-04 honest platform dispatch with a promoted adapter identity, root-mediated default and probed native nesting; SYN-203-05 probe contract — configurable, bounded, process-tree-safe and isolated from the repository; SYN-203-06 exactly-once child result and explicit recovery reusing the handoff-ID plus transaction plus receipt pattern; SYN-203-07 trophallaxis as scoped knowledge handback with acknowledgement and one named follow-on; SYN-203-08 whole-subtree truth on one ledger and one live event model with no second accounting; SYN-203-09 inline-only liveness and the end-of-run family tree with refusals; SYN-203-10 one canonical pheromone resolver carrying effective scope, expiry, strength, provenance and quarantine; SYN-203-11 one tick-to-approve surface for suggested notes and quarantined imports plus the full influence action history; SYN-203-12 credit earned only from a recorded changed decision plus a verified later outcome, including neutral and harmful ones. Give each a disposition drawn from keep-current, restore-modern, replace-better, retire-with-proof.

Route and independently adjudicate the six capability rows the ledger sends to Phase 203: CAP-002, CAP-009, CAP-014, CAP-030, CAP-031 and CAP-058. The ledger's proposed disposition is a hypothesis; confirm or revise each with its own cited historical and current evidence.

Record these six rulings in writing as explicit decided rows rather than silent assumptions:

(a) **The second admission engine is live, not dead.** RESEARCH.md states `processClaims` is never called. It is: `host.ts` constructs the orchestrator, passes it into `dispatchBuildWave`, which passes it through `DispatchOptions.spawnOrchestrator` into `worker-dispatch.ts`'s `dispatchWaves`, which calls `orchestrator.processClaims` in `wave-orchestrator.ts` and dispatches accepted children as a spawn wave. Two independent counters therefore exist today — `DEFAULT_TOTAL_BUDGET` per orchestrator instance in TypeScript against `spawnTreeBudgetMax` whole-run in Go — and the TypeScript one never consults the Go gate. Rule that the Go gate becomes the sole admission authority and the TypeScript policy arithmetic is deleted in favour of a bridge call, and name plan 203-09 as the owner of that change.

(b) **The two lanes are asymmetric and the asymmetry must be honest.** The host/autopilot lane dispatches children from `result.spawns`; the Go native and interactive wrapper lanes parse `spawns` as a bare `stringList` in `pkg/codex/worker.go` and discard it. Rule which lanes gain governed recruitment in this phase and state, for any lane that does not, exactly how it fails closed and what the owner is told.

(c) **D-11's raise flag against the sibling constant's non-configurability.** `spawnMaxDelegationDepth = 2` already equals the owner's wanted default; `spawnTreeBudgetMax`'s own comment records that the owner previously rejected a config key because "a limit you can raise under pressure is a limit that gets raised". D-11 is a locked owner decision and is honoured, but record the counter-argument, record that the depth cap and the tree budget remain separate quantities with only the depth cap gaining a per-run flag, and record that every use of the flag is written into the immutable spawn ledger so a raise is always visible after the fact.

(d) **Native nesting is not a launch dependency.** Rule whether native platform nesting is attempted at all in this phase, given that only `aether-queen.md`, `aether-route-setter.md`, `aether-architect.md` and `aether-keeper.md` name `Task` in their tool grants and the upstream capability is disputed. State the ruling as a disposition on SYN-203-04 with the probe as the only thing that may turn it on.

(e) **The word "trophallaxis" is currently squatted by two orphans.** `trophallaxis-diagnose` and `trophallaxis-retry` in `cmd/immune.go` are error-diagnosis and retry-bookkeeping helpers, not knowledge transfer, and both appear in `cmd/testdata/orphan_allowlist.json` with no caller anywhere in the repository. Rule their disposition as retire-with-proof, freeing the name for BIO-05's real meaning, and name plan 203-10 as the owner of that change.

(f) **The read side of the pheromone bus has at least three disagreeing predicates.** `extractSignalTexts` filters on `Active` plus effective strength but never checks expiry; `filterSignalsForPrompt` adds `signalExpiredByTime`; `codex_plan.go`'s REDIRECT scan uses a strictly-greater-than-zero threshold instead of the 0.1 floor. `cmd/build_print_brief.go` already carries a comment noting two of these differ. Rule that one resolver owns the effective scope, expiry and strength decision and that every reader calls it, and name plan 203-05 as the owner.

Include a traceability section mapping each locked owner decision to its SYN-203 rows and to the named implementation plan that delivers it, in the range 203-02 to 203-15. Cover every one of D-01 (automatic recruitment inside program limits, no approval prompt), D-02 (chains permitted while limits hold), D-03 (a refusal never stalls the work), D-04 (one inline line per recruit, never a second terminal), D-05 (end-of-run family tree with costs), D-06 (refusals shown inline and in the summary), D-07 (suggested notes take effect only after owner approval), D-08 (an inline line when a note changes a decision, plus a summary list), D-09 (outcome-weighted tuning on, with immutable history), D-10 (imported notes start quarantined, owner-released), D-11 (default chain depth two, raisable per run by explicit flag) and D-12 (recruits draw from the same team budget). Include a rejected-alternatives table naming at minimum: a recruitment-specific limiter beside `spawnCanSpawnDecision`, a second spawn ledger beside `agent.SpawnTree`, a second idempotency mechanism beside the handoff-ID pattern, a second quarantine inbox beside `colony.PendingSuggestion`, a sixth pheromone write site, and finally calling `processClaims` correctly instead of deleting its arithmetic.

Include a section headed "Unresolved edge probes (flagged planner assumptions)" listing all seven unclassified requirement probes this phase could not auto-resolve — SYNTH-05, BIO-01, BIO-02, BIO-04, BIO-05, BIO-07 and BIO-08 — each with the one-line assumption the plans proceed on and the plan number that would be affected if the assumption is wrong. State plainly that these are surfaced assumptions, not resolved criteria.

Include a section headed "Owner-facing summary" written in the plain English the project mandates: no repo-invented word (colony, caste, pheromone, midden, trophallaxis, ratchet) may appear in it without an inline translation. Do not modify the Dreams report or any historical evidence file.</action>
  <acceptance_criteria>
    - The document contains each identifier `SYN-203-01` through `SYN-203-12` exactly once as a decision heading or row identifier.
    - Each of CAP-002, CAP-009, CAP-014, CAP-030, CAP-031 and CAP-058 appears in a routed coverage table with exactly one disposition and at least one cited source path.
    - Each of D-01 through D-12 appears in the traceability section next to at least one SYN-203 identifier and one plan number in the range 203-02 to 203-15.
    - The Classic reconstruction table contains at least one `git show` citation per OLD row; the current audit table contains at least one `path:line` citation per NOW row.
    - The six rulings (a) through (f) each appear as a decided row or a named open-decisions entry, and the text contains the literal strings `processClaims`, `spawnTreeBudgetMax`, `trophallaxis-diagnose` and `filterSignalsForPrompt`.
    - The rejected-alternatives table names a recruitment-specific limiter, a second spawn ledger, a second idempotency mechanism, a second quarantine inbox and a sixth pheromone write site.
    - A section headed with the words `Unresolved edge probes` lists all seven of SYNTH-05, BIO-01, BIO-02, BIO-04, BIO-05, BIO-07 and BIO-08.
    - Front matter sets `phase: "203"` and `area: "biology"`.
  </acceptance_criteria>
  <verify>
    <automated>SYN=.planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md; test -f "$SYN" &amp;&amp; for i in 01 02 03 04 05 06 07 08 09 10 11 12; do grep -q "SYN-203-$i" "$SYN" || exit 1; done &amp;&amp; for c in 002 009 014 030 031 058; do grep -q "CAP-$c" "$SYN" || exit 1; done &amp;&amp; for d in 01 02 03 04 05 06 07 08 09 10 11 12; do grep -q "D-$d" "$SYN" || exit 1; done &amp;&amp; for s in "git show" "processClaims" "spawnTreeBudgetMax" "trophallaxis-diagnose" "filterSignalsForPrompt" "Unresolved edge probes"; do grep -q "$s" "$SYN" || exit 1; done &amp;&amp; for r in SYNTH-05 BIO-01 BIO-02 BIO-04 BIO-05 BIO-07 BIO-08; do grep -q "$r" "$SYN" || exit 1; done &amp;&amp; grep -q 'area: "biology"' "$SYN"</automated>
  </verify>
  <done>One evidence-backed brief covers every Phase 203 mechanism, locked decision, routed capability and open question, so no later plan can be justified by a feature name or a remembered behaviour alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register SYN-203 mechanisms in the versioned Classic corpus</name>
  <files>cmd/testdata/classic-contract/v1/schema.json, cmd/testdata/classic-contract/v1/mechanisms.json, cmd/classic_contract_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/testdata/classic-contract/v1/schema.json
    - cmd/testdata/classic-contract/v1/mechanisms.json
    - cmd/classic_contract_test.go
    - .planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md
  </read_first>
  <behavior>
    - A mechanism entry whose id is outside the registered SYN-199, SYN-200, SYN-201, SYN-202 and SYN-203 sets fails registry validation and the error names the offending identifier.
    - A case declaring `synthesis_decision` of `SYN-203-06` and group `V-203-DISPATCH` validates against the schema.
    - A case declaring a SYN-203 decision that no mechanism entry registers fails coverage validation by name.
    - An identifier differing from a registered one only by letter case is rejected, and the rejection names the identifier it was compared against.
    - Every SYN-203 mechanism entry names its CAP identifiers, public commands, source citations and owning groups.
  </behavior>
  <action>Widen `schema.json`'s synthesis-decision pattern at both places it appears — `$defs/case/properties/synthesis_decision/pattern` and `$defs/mechanism/properties/id/pattern` — from its current value ending `202-(?:0[1-9]|1[0-2]))$` so the alternation additionally accepts `203-(?:0[1-9]|1[0-2])`. Add these members to `$defs/case/properties/group/enum` alongside the existing ones without removing any: `V-203-INTENT`, `V-203-ADMISSION`, `V-203-DISPATCH`, `V-203-RESULT`, `V-203-SUBTREE`, `V-203-SIGNALS`.

Append twelve mechanism entries `SYN-203-01` through `SYN-203-12` to `mechanisms.json`, each carrying the required registry fields `id`, `title`, `disposition`, `cap_ids`, `public_commands`, `source_citations` and `groups`, with values taken verbatim from the synthesis Task 1 produced. Add `.planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md` to the `synthesis_sources` array; leave the legacy scalar `synthesis_source` key untouched. Public commands for these entries are the owner-facing spellings `/ant-build`, `/ant-continue`, `/ant-watch`, `/ant-status`, `/ant-pheromones`, `/ant-focus` and `/ant-redirect`, matching the vocabulary Phase 199 locked.

Extend `cmd/classic_contract_test.go` with a `TestClassicContractPhase203MechanismRegistry` that loads the registry through the existing strict loader, asserts the twelve SYN-203 identifiers are present exactly once each, asserts every entry names at least one CAP identifier and at least one source citation, and proves by negative fixture that an unregistered SYN identifier, a case referencing an unregistered decision, and an identifier differing only by letter case each fail with the offending identifier in the message. Do not add executable behaviour cases here — the later implementation plans register their own cases once the behaviours exist.</action>
  <acceptance_criteria>
    - `python3 -c "import json;json.load(open('cmd/testdata/classic-contract/v1/schema.json'))"` succeeds and the widened pattern string accepts `SYN-203-01`.
    - The registry contains exactly twelve entries whose id begins `SYN-203-`, and the pre-existing ten SYN-199, twelve SYN-200, fourteen SYN-201 and twelve SYN-202 entries are all still present, for sixty total.
    - `synthesis_sources` contains `.planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md`.
    - The three negative fixtures fail with the offending identifier named in the error text.
    - `TestClassicContractSchema`, `TestClassicMechanismCoverage`, `TestClassicContractPhase201MechanismRegistry` and `TestClassicContractPhase202MechanismRegistry` still pass unchanged.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestClassicContractSchema|TestClassicMechanismCoverage|TestClassicContractPhase201MechanismRegistry|TestClassicContractPhase202MechanismRegistry|TestClassicContractPhase203MechanismRegistry)$' -count=1 &amp;&amp; python3 -c "import json,sys; m=json.load(open('cmd/testdata/classic-contract/v1/mechanisms.json')); ids=[x['id'] for x in m['mechanisms']]; ok=len([i for i in ids if i.startswith('SYN-203-')])==12 and len([i for i in ids if i.startswith('SYN-202-')])==12 and len([i for i in ids if i.startswith('SYN-201-')])==14 and len([i for i in ids if i.startswith('SYN-200-')])==12 and len([i for i in ids if i.startswith('SYN-199-')])==10 and any('203-biological-runtime' in s for s in m['synthesis_sources']); sys.exit(0 if ok else 1)"</automated>
  </verify>
  <done>The executable Classic corpus can carry Phase 203 mechanism identities, so later plans register cases against a contract that already exists rather than inventing one late.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols and files created by this plan (new to the codebase — not drift):

- `.planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md` (new file)
- Mechanism identifiers `SYN-203-01` … `SYN-203-12` (new registry entries)
- Corpus group names `V-203-INTENT`, `V-203-ADMISSION`, `V-203-DISPATCH`, `V-203-RESULT`, `V-203-SUBTREE`, `V-203-SIGNALS` (new schema enum members)
- `TestClassicContractPhase203MechanismRegistry` (new Go test)

<verification>
- The synthesis covers SYNTH-05's full scope: worker emergence, help-seeking, delegation, result return, trophallaxis, pheromone propagation and shared-environment effects, reconciled against modern Go authorization, ledgers, workspaces, adapters and safety limits.
- All six routed CAP rows carry an independently adjudicated disposition.
- The corpus schema and registry accept SYN-203 identifiers and reject unknown ones by name.
- Existing Phase 199, 200, 201 and 202 corpus tests remain green.
</verification>

<success_criteria>
- No later Phase 203 plan can be executed without a cited disposition to point at.
- The six open items — live second admission engine, lane asymmetry, the depth-raise flag tension, native nesting, the squatted trophallaxis name, and the disagreeing pheromone read predicates — are decided in writing, not left to an executor's improvisation.
</success_criteria>

<output>
Create `.planning/phases/203-biological-runtime/203-01-SUMMARY.md` when done.
</output>
