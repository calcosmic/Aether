---
phase: 205-owner-acceptance-and-restoration-seal
plan: 08
type: execute
wave: 2
depends_on: ["205-01"]
files_modified:
  - .planning/phases/204-learning-governor/204-CLASSIC-COVERAGE.json
  - .planning/phases/204-learning-governor/204-CLASSIC-COVERAGE.md
  - cmd/classic_coverage_readjudication_test.go
autonomous: true
requirements: [PROOF-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The seven capability rows the frozen ledger routes to Phase 204 are signed, and the ratchet passes on them."
    - "Exactly the rows whose disposition was re-adjudicated by the Phase 204 study carry a re-adjudication citation, and each citation names the study section that made the ruling."
    - "The set of rows allowed to differ from the frozen ledger's disposition is a named, counted set that a test holds at or below its current size, so a later phase cannot quietly widen it."
    - "A row carrying a re-adjudication citation that does not resolve to a real section in that phase's own study fails the check, naming the row."
    - "A row whose disposition differs from the frozen ledger with no citation at all fails the check, naming the row, the signed value and the ledger value."
  artifacts:
    - .planning/phases/204-learning-governor/204-CLASSIC-COVERAGE.json
    - .planning/phases/204-learning-governor/204-CLASSIC-COVERAGE.md
    - cmd/classic_coverage_readjudication_test.go
  key_links:
    - "The re-adjudication allowance is bound to the Phase 204 study's own ruling section, not to a free-text note."
  prohibitions:
    - statement: "A disposition frozen against a state the code has since left must not be quietly rewritten; the change is recorded as a re-adjudication citing the study that made it, or it is not made."
      status: unverified
      flagged: true
---

<objective>
Sign Phase 204's seven capability rows, and record the two dispositions its own study found stale in a way a command can check.

Purpose: PROOF-02 needs every row signed; the Phase 204 study additionally rules that two frozen dispositions were set against a state the code has since left and must be re-adjudicated. A re-adjudication that lives only in prose is exactly the kind of claim this milestone exists to stop shipping.

Output: Phase 204's signed coverage file, and a bounded, cited re-adjudication record that a named test holds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-RESEARCH.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-01-SUMMARY.md
@.planning/research/v1.28-classic-capability-ledger.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Phase 204's seven rows, signed, with the two re-adjudications cited end to end</name>
  <files>.planning/phases/204-learning-governor/204-CLASSIC-COVERAGE.json, .planning/phases/204-learning-governor/204-CLASSIC-COVERAGE.md, cmd/classic_coverage_readjudication_test.go</files>
  <read_first>
    - cmd/classic_coverage_ratchet_test.go (as built by plan 205-01 — the row shape, the validators, the disposition-versus-ledger rule and its re-adjudication marker)
    - .planning/phases/204-learning-governor/204-CLASSIC-SYNTHESIS.md — section 3's "Ruling (c)" in full, and the comparative synthesis matrix in section 4 that carries the capability routing
    - .planning/phases/204-learning-governor/204-CLASSIC-HISTORICAL-EVIDENCE.md — the historical mechanism evidence the rows cite
    - .planning/research/v1.28-classic-capability-ledger.md — the routed dispositions for CAP-001, CAP-025, CAP-043, CAP-055, CAP-057, CAP-067, CAP-070
    - .planning/phases/204-learning-governor/204-VERIFICATION.md — which Phase 204 claims are verified and which two were accepted as deferrals
    - .planning/phases/200-iterative-planning/200-CLASSIC-COVERAGE.json if plan 205-07 has already landed, otherwise `.planning/phases/203-biological-runtime/203-CLASSIC-COVERAGE.json`, for the shape to follow
  </read_first>
  <action>
Create `.planning/phases/204-learning-governor/204-CLASSIC-COVERAGE.json` with document version `204/classic-coverage/v1` and one `type: GOAL` row per capability id the ledger routes to Phase 204 — CAP-001, CAP-025, CAP-043, CAP-055, CAP-057, CAP-067 and CAP-070.

Read "Ruling (c)" carefully before signing CAP-001 and CAP-057. The ruling states both frozen dispositions are correct in direction but were frozen against a state that has since changed, and it refines what the real remaining gap is in each case. Decide, and say in the SUMMARY which you concluded: either the signed disposition equals the ledger's and the ruling is recorded purely as refined evidence in `historical_evidence`; or the signed disposition genuinely differs, in which case the row must carry the re-adjudication marker plus a citation naming the study's ruling section. Do not guess — read the ruling and the code it cites.

For every row, take `modern_home`, `plan_task` and `artifact` from the Phase 204 study and its plan files, confirm the artifact exists on disk, and name a proof that was individually executed and observed passing in this session. Two Phase 204 claims were accepted as deferrals by the owner and one gap was re-opened on 2026-09-15 — if a row's evidence touches a deferred or re-opened claim, sign it with the proof that genuinely holds today and record the limit plainly in `historical_evidence`, so the limitations card in plan 205-17 can pick it up.

Write `.planning/phases/204-learning-governor/204-CLASSIC-COVERAGE.md` as the rendered companion in the same shape as the other phases.

Add `cmd/classic_coverage_readjudication_test.go` with `TestClassicCoveragePhase204Signed`, calling the shared validator for phase `204`.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestClassicCoveragePhase204Signed' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/204-learning-governor/204-CLASSIC-COVERAGE.json` parses as JSON and contains exactly seven GOAL rows with the ids CAP-001, CAP-025, CAP-043, CAP-055, CAP-057, CAP-067, CAP-070
    - `.planning/phases/204-learning-governor/204-CLASSIC-COVERAGE.md` exists with a `## GOAL` heading and one table row per id
    - `go test ./cmd/ -run 'TestClassicCoveragePhase204Signed' -count=1 -timeout 90m` exits 0
    - The SUMMARY states, for CAP-001 and for CAP-057, whether the signed disposition equals the frozen ledger's and why, citing the study's ruling
    - The SUMMARY lists, per row, the command used to execute its named proof and the observed result
  </acceptance_criteria>
  <done>Phase 204's capability dispositions pass the ratchet and the two stale ones are settled on the record rather than in prose.</done>
</task>

<task type="auto">
  <name>Task 2: The re-adjudication allowance is named, counted, and cannot quietly widen</name>
  <files>cmd/classic_coverage_readjudication_test.go</files>
  <read_first>
    - cmd/classic_coverage_ratchet_test.go — `validateClassicCoverageDispositionMatchesLedger` and the marker it recognises
    - .planning/phases/204-learning-governor/204-CLASSIC-SYNTHESIS.md — the exact heading text of the ruling section a citation must resolve to
    - The five signed coverage files that exist at this point (Phases 200, 201, 202, 203, 204), so the count is taken over all of them
  </read_first>
  <action>
Add to `cmd/classic_coverage_readjudication_test.go`:

`TestClassicCoverageReadjudicationsAreCitedAndBounded` — walk every signed coverage file that exists, collect every row carrying the re-adjudication marker, and assert three things: each such row's citation resolves to a real section heading inside that phase's own study file; the collected set of re-adjudicated row identifiers is exactly the set recorded in a named constant in this test; and the size of that named set is at or below its declared ceiling. Declare the ceiling as a constant with a comment saying it may only shrink, in the same shape this repository already uses for its other only-shrinking allowances.

`TestClassicCoverageReadjudicationCitationMustResolve` — take a real signed row, clone it in memory with a citation naming a section that does not exist in that study, and assert the check fails naming the row.

`TestClassicCoverageUncitedDispositionChangeFails` — clone a real row, change its disposition away from the frozen ledger's value with no marker, and assert the check fails naming the row, the signed value and the ledger value.

Operate entirely on in-memory clones and temporary directories; never modify a real coverage file.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestClassicCoverageReadjudication|TestClassicCoverageUncitedDispositionChangeFails|TestClassicCoveragePhase204Signed' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/ -run 'TestClassicCoverageReadjudicationsAreCitedAndBounded' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestClassicCoverageReadjudicationCitationMustResolve' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestClassicCoverageUncitedDispositionChangeFails' -count=1 -timeout 90m` exits 0
    - `cmd/classic_coverage_readjudication_test.go` declares the re-adjudicated set and its ceiling as named constants, with the only-shrinking rule stated on the ceiling
    - `git status --porcelain .planning/phases/` after the run shows no modification to any coverage file
  </acceptance_criteria>
  <done>Every disposition that departs from the frozen ledger is named, cited to a real ruling, and counted by a check that a later phase cannot widen without being seen.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing):

- New files `.planning/phases/204-learning-governor/204-CLASSIC-COVERAGE.json` and `.md`
- New file `cmd/classic_coverage_readjudication_test.go` with `TestClassicCoveragePhase204Signed`, `TestClassicCoverageReadjudicationsAreCitedAndBounded`, `TestClassicCoverageReadjudicationCitationMustResolve`, `TestClassicCoverageUncitedDispositionChangeFails`
- A named re-adjudicated-row set constant and its only-shrinking ceiling constant

## Flagged planner assumptions

- The Phase 204 study's ruling states both stale dispositions are correct in direction, which may mean no disposition actually changes and the re-adjudicated set is empty. Task 1 requires the executor to read the ruling and decide rather than assume; an empty set is a legitimate outcome and the bounded-allowance test still applies with a ceiling of zero.

<verification>
- Phase 204's seven rows are signed and the ratchet passes
- Every departure from the frozen ledger is cited to a resolvable ruling section, and the set of departures is named and bounded
- Both failure modes — an unresolvable citation and an uncited change — have demonstrated failing tests
</verification>

<success_criteria>
- Phase 204's capability dispositions are facts a command checks
- A re-adjudication is a recorded, cited, counted exception rather than a quiet edit
</success_criteria>

<output>
Create `.planning/phases/205-owner-acceptance-and-restoration-seal/205-08-SUMMARY.md` when done
</output>
