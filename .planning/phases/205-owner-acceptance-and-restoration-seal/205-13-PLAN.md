---
phase: 205-owner-acceptance-and-restoration-seal
plan: 13
type: execute
wave: 5
depends_on: ["205-12"]
files_modified:
  - VERSION
  - CHANGELOG.md
  - .planning/phases/205-owner-acceptance-and-restoration-seal/205-RELEASE-RECORD.md
autonomous: true
requirements: [PROOF-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The released version number is higher than the one already installed; the release is never published under a number that has been published before."
    - "A restorable copy of the machine's shared installed copy exists before anything is published, and its location is recorded."
    - "The publish runs from a working copy pinned to the exact release commit, never from the shared branch other sessions commit to."
    - "After publishing, the installed program and the shared installed copy report the same version, and the full release chain passes its own integrity check."
    - "The owner's project reads the newly released version — confirmed by reading the version the project's installed copy reports, not by reading this repository."
    - "The release record states what changed since the previously installed version in plain words, naming the two stages of work the installed copy predated."
  artifacts:
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-RELEASE-RECORD.md
  key_links:
    - "The readiness record from the previous plan is the gate: a release does not happen without a complete test run recorded there."
    - "The hub backup is the undo path for the whole release."
  prohibitions:
    - statement: "The release must not be published in a way that can leave the machine's shared installed copy unusable without a restorable backup taken first."
      status: unverified
      flagged: true
    - statement: "A version number that has already been published must never be republished under the same number."
      status: unverified
      flagged: true
---

<objective>
Publish the release the owner's session will run against, from a pinned working copy, with a restorable backup of the shared installed copy taken first.

Purpose: the copy the owner's projects use predates two whole stages of this milestone's work, so without this release the walk-through would test stale code. The owner has decided this one publish is the release and the acceptance comes after.

Output: a bumped, published, integrity-checked release, and a plain-English record of what went out and how to undo it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-RESEARCH.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-12-SUMMARY.md
@.aether/docs/publish-update-runbook.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: A new version number, agreed across every place that states one</name>
  <precondition>The readiness record from plan 205-12 exists and states the build is fit to release, with the number of tests discovered equal to the number executed.</precondition>
  <reversibility rating="costly">The release lands in every project on this machine before the owner has accepted it; a journey that fails means a broken version is live until a follow-on release. Undo is a restore of the backed-up shared copy plus a follow-on publish, not a single revert.</reversibility>
  <files>VERSION, CHANGELOG.md</files>
  <read_first>
    - .aether/docs/publish-update-runbook.md in full — the authoritative publish sequence
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md, decision D-14 — this one publish is the release, it must bump the version, synchronise it everywhere, back up the shared installed copy and pass the integrity check, and it runs from a working copy pinned to the release commit
    - Makefile — the version variable, the build flags and the version-sync target
    - scripts/version-sync.sh — what it synchronises and where
    - VERSION and the installed shared copy's recorded version
    - CHANGELOG.md — the existing entry shape
  </read_first>
  <action>
Read the currently installed version from the machine's shared copy and from the installed program. Record both. Choose the next version number strictly above the installed one; a number that has been published before is never reused, because a republish under the same number has previously made a fix look shipped when it was not.

Bump the version, then run the version synchronisation target so every place in the repository that states a version states the same one. Confirm afterwards by searching for the old version string across the repository and finding no remaining occurrence that should have moved.

Add a changelog entry for the new version written for a reader who has never opened this repository: what changed since the installed version, in plain words, naming the two stages of work the installed copy predated and the seal and archive fixes made in this phase. Do not put the limitations card into the changelog — the owner has ruled it stays in the planning records.

Verify version agreement before publishing anything: the program built from this commit and the repository's own stated version must match.
  </action>
  <verify>
    <automated>make version-sync && go build ./cmd/aether && ./aether version 2>/dev/null | head -3; grep -c "$(cat VERSION 2>/dev/null || echo NOVERSION)" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - The new version number is strictly greater than the version the shared installed copy reported before this task
    - `make version-sync` exits 0
    - `go build ./cmd/aether` exits 0 and the built program reports the new version
    - `CHANGELOG.md` contains an entry for the new version, written without repository jargon
    - `CHANGELOG.md` does not contain the limitations card
    - The SUMMARY records the previously installed version, the new version, and the result of the search for stale version strings
  </acceptance_criteria>
  <done>One version number, higher than what is installed, agreed everywhere it is stated.</done>
</task>

<task type="auto">
  <name>Task 2: Back up the shared copy, publish from a pinned working copy, check the chain</name>
  <files>No file in this repository — the machine's shared installed directory (backed up, then republished from a pinned working copy)</files>
  <read_first>
    - .aether/docs/publish-update-runbook.md — the publish, integrity and version-check steps in order
    - The current contents and location of the machine's shared installed copy
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-RESEARCH.md — the warning that this branch is a shared checkout and the record of a previous publish that destroyed the shared copy
  </read_first>
  <action>
Take a complete, restorable copy of the machine's shared installed directory before publishing anything, to a dated location outside it. Verify the backup by comparing its file count and total size against the original, and record both numbers and the backup path. This backup is the undo path for the entire release; a publish without it does not happen.

Confirm no other session is committing to this branch right now. Create a throwaway working copy pinned to the exact release commit and publish from there, never from the shared checkout. Record the commit hash.

Run the publish, then the integrity check over the whole release chain, then the version agreement check. All three must succeed. If the integrity check fails, restore the backup before doing anything else and record what happened.

After a successful publish, confirm the shared copy and the installed program both report the new version, and that the companion file counts reported by the publish are non-zero for each platform — a publish reporting nothing copied and nothing unchanged means the publish did not complete and the shared copy is now incomplete.
  </action>
  <verify>
    <automated>aether version --check && aether integrity</automated>
  </verify>
  <acceptance_criteria>
    - A backup of the shared installed directory exists at a recorded path, with its file count and total size recorded and matching the original
    - The publish ran from a throwaway working copy pinned to a recorded commit hash, not from the shared checkout
    - `aether version --check` exits 0
    - `aether integrity` exits 0
    - The publish output reports a non-zero copied-or-unchanged count for each platform's command set
    - The SUMMARY records the backup path, the pinned commit, and the publish output's per-platform counts
  </acceptance_criteria>
  <done>The release is live on this machine, its chain is verified, and the previous state is one restore away.</done>
</task>

<task type="auto">
  <name>Task 3: The owner's project is actually running the new release</name>
  <files>.planning/phases/205-owner-acceptance-and-restoration-seal/205-RELEASE-RECORD.md</files>
  <read_first>
    - `/Users/callumcowie/Documents/Max 9/M4L-AnalogWave-System/.aether/` — what the project reads from the machine's shared copy
    - .aether/docs/publish-update-runbook.md — the downstream update step and the note that an update without the force option can leave stale managed files behind
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-12-SUMMARY.md — the session branch and the clean-tree state that must survive this task
  </read_first>
  <action>
Quote the target project's path in every command; it contains a space.

Update the owner's project from the machine's shared copy so it picks up the new release, using the force option, because an update without it can leave stale managed files behind. Confirm afterwards by reading the version the project's own installed copy reports — not by reading this repository, which would prove nothing about what the owner's project will actually run.

Check that the update did not dirty the working tree the previous plan cleaned, and did not touch the project's colony records. If the update legitimately changes managed command files, that is expected and those changes are committed on the session branch with a plain message; anything else is investigated before the session.

Write `.planning/phases/205-owner-acceptance-and-restoration-seal/205-RELEASE-RECORD.md` stating, in plain words: the version that was installed before, the version now, what changed between them, where the backup of the previous shared copy is, the exact steps to put the previous version back, and the confirmation that the owner's project reports the new version.
  </action>
  <verify>
    <automated>cd "/Users/callumcowie/Documents/Max 9/M4L-AnalogWave-System" && aether version | head -3 && test -s /Users/callumcowie/repos/Aether/.planning/phases/205-owner-acceptance-and-restoration-seal/205-RELEASE-RECORD.md</automated>
  </verify>
  <acceptance_criteria>
    - Running the program inside the owner's project reports the new version number
    - `git -C "/Users/callumcowie/Documents/Max 9/M4L-AnalogWave-System" status --porcelain` shows either no output or only managed command files, which are committed on the session branch
    - `/Users/callumcowie/Documents/Max 9/M4L-AnalogWave-System/.aether/data/COLONY_STATE.json` is unmodified — its digest matches the one recorded in plan 205-12
    - `.planning/phases/205-owner-acceptance-and-restoration-seal/205-RELEASE-RECORD.md` exists and states the previous version, the new version, the backup location, and the steps to restore
    - The restore steps in the release record were read back and are complete enough to follow without opening any other file
  </acceptance_criteria>
  <done>The owner's project will run the code this milestone actually built, and putting it back is written down.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing):

- New file `.planning/phases/205-owner-acceptance-and-restoration-seal/205-RELEASE-RECORD.md`
- A new version entry in `CHANGELOG.md` and a bumped `VERSION`
- A dated backup copy of the machine's shared installed directory (path recorded in the release record)

## Flagged planner assumptions

- **Costly, not reversible in one step.** The owner has decided this publish is the release and that acceptance follows it. That means a failed journey leaves a live release with a known problem until a follow-on release. The backup taken in Task 2 and the restore steps written in Task 3 are what make that acceptable; both are acceptance criteria, not optional hygiene.
- Whether another session is committing to this branch at publish time cannot be known in advance; Task 2 checks at the moment of publishing and the pinned working copy makes the answer not matter.

<verification>
- The published version is strictly higher than the previously installed one
- A verified restorable backup of the shared installed copy exists before the publish
- The integrity and version-agreement checks both pass after the publish
- The owner's own project reports the new version
</verification>

<success_criteria>
- The owner's session runs against the code this milestone built, not a copy predating two stages of it
- The previous state is restorable and the steps to restore it are written down in plain words
</success_criteria>

<output>
Create `.planning/phases/205-owner-acceptance-and-restoration-seal/205-13-SUMMARY.md` when done
</output>
