---
phase: 205-owner-acceptance-and-restoration-seal
plan: 12
type: execute
wave: 4
depends_on: ["205-02", "205-03", "205-04", "205-05", "205-11"]
files_modified:
  - .planning/phases/205-owner-acceptance-and-restoration-seal/205-BRIEF.md
  - .planning/phases/205-owner-acceptance-and-restoration-seal/205-PRE-SESSION-READINESS.md
autonomous: true
requirements: [PROOF-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The owner's project starts the session on a clean working tree, and every one of the in-flight files that were there beforehand is retrievable by a name written into the brief."
    - "The brief is one page, lists the ten pieces of work in the order they are to be done, states the new project's goal in the owner's own words, and names no command anywhere."
    - "The brief tells the owner that getting stuck on a piece of work means that piece is recorded as a failure and he moves on, with no help asked for and none given."
    - "The brief says which branch the session runs on, and that the work set aside beforehand is safe and retrievable."
    - "A readiness record states the free disk space, the number of open entries in the defect register re-read on the day, and the result of one serialised full test run compared against the known pre-existing failures."
    - "The full test run reports the same number of tests discovered as executed; a run where those two numbers differ is recorded as inconclusive and re-run, never read as a pass."
  artifacts:
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-BRIEF.md
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-PRE-SESSION-READINESS.md
  key_links:
    - "The snapshot branch or tag name recorded in the brief is the only link back to the owner's set-aside work."
    - "The readiness record is the gate the publish plan reads before releasing."
  prohibitions:
    - statement: "The brief must not tell the owner what to type; if the program's own next-step card and help cannot carry him from a plain description of the work, that is a finding about the program."
      status: unverified
      flagged: true
    - statement: "The owner's set-aside work must not be discarded, squashed away, or made unreachable by name in the course of clearing the working tree."
      status: unverified
      flagged: true
---

<objective>
Get everything ready for the owner's day: put his in-flight work safely aside, hand him a one-page description of the ten pieces of work, and prove the build is fit to release.

Purpose: the walk-through is run by the owner alone with no help available, so everything that could go wrong for a preventable reason must be dealt with first. His project currently carries several hundred in-flight files from his own experiment; the session needs a clean start without losing any of it.

Output: a clean working tree with the prior work retrievable by name, a one-page brief naming no commands, and a readiness record the publish plan gates on.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-RESEARCH.md
@.planning/WINDOWS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: The owner's in-flight work set aside, retrievable by name, clean tree</name>
  <precondition>The directory `/Users/callumcowie/Documents/Max 9/M4L-AnalogWave-System` exists, is a git repository, and its current branch still carries the owner's uncommitted experiment.</precondition>
  <reversibility rating="reversible">The whole mechanism is a snapshot commit or branch; the set-aside work stays retrievable by the name written into the brief, and restoring it is one checkout.</reversibility>
  <files>No file in this repository — the git working tree, snapshot branch and session branch of /Users/callumcowie/Documents/Max 9/M4L-AnalogWave-System (quote the path; it contains a space)</files>
  <read_first>
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md, decisions D-05, D-06 and D-07 — the target project, the tidy constraint, and the rule that the finished colony there is not pre-archived
    - `/Users/callumcowie/Documents/Max 9/M4L-AnalogWave-System/CLAUDE.md` and `AGENTS.md` — what the project is, for the brief's wording
    - The target project's current branch, its uncommitted file counts, and its `.gitignore`
  </read_first>
  <action>
Every command touching the target project must quote the path, because it contains a space. Treat that as a real-world stress the program has to cope with, not a reason to move the project.

Inspect the target project's working tree and record, before changing anything, the current branch name and the exact counts of modified, untracked and deleted files. Put those counts in the SUMMARY so the set-aside is auditable.

Preserve the work: create a snapshot that captures all three categories — modified, untracked and deleted — under one retrievable name, and push nothing. Choose a snapshot commit on a clearly named branch rather than a stash, because a branch name is something the brief can say out loud and the owner can act on months later. Verify the snapshot by listing the files it contains and comparing the count to the pre-change counts.

Then return the working tree to a clean state on the branch the session will run on. Record which branch that is.

Do not touch the project's colony records. The finished, unsealed colony there is the owner's own first piece of work in the session and must be left exactly as it is.

Verify the result: the working tree reports no changes, and the snapshot name resolves to a commit containing the recorded file count.
  </action>
  <verify>
    <automated>cd "/Users/callumcowie/Documents/Max 9/M4L-AnalogWave-System" && test -z "$(git status --porcelain)" && git rev-parse --verify "$(cat /tmp/aether-205-snapshot-name)" >/dev/null && ls .aether/data/COLONY_STATE.json</automated>
  </verify>
  <acceptance_criteria>
    - `git -C "/Users/callumcowie/Documents/Max 9/M4L-AnalogWave-System" status --porcelain` produces no output
    - The snapshot name resolves to a real commit, and the file count in that commit matches the pre-change modified-plus-untracked-plus-deleted total recorded in the SUMMARY
    - `/Users/callumcowie/Documents/Max 9/M4L-AnalogWave-System/.aether/data/COLONY_STATE.json` is unmodified — its content digest matches the digest taken before this task ran
    - No branch was pushed to any remote
    - The SUMMARY records the pre-change counts, the snapshot name, and the branch the session runs on
  </acceptance_criteria>
  <done>The owner starts on a clean tree, and every file he had in flight is one named checkout away.</done>
</task>

<task type="auto">
  <name>Task 2: The one-page brief — the work, in order, and not a single command</name>
  <files>.planning/phases/205-owner-acceptance-and-restoration-seal/205-BRIEF.md</files>
  <read_first>
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md — the ten-journey table, and decisions D-02, D-04, D-07, D-08, D-11, D-17, D-18, D-19
    - `/Users/callumcowie/Documents/Max 9/M4L-AnalogWave-System/CLAUDE.md`, `.mds/`, `m4l_builder/`, and `docs/VAL_RULES_BACKLOG.md` — the real work the brief's tasks are drawn from; the tasks come from here and are never invented
    - The snapshot name and session branch recorded by Task 1
    - CLAUDE.md — the communication style block and the table of invented words needing inline translation
  </read_first>
  <action>
Write `.planning/phases/205-owner-acceptance-and-restoration-seal/205-BRIEF.md`. One page. Written for someone who has never opened this repository.

Open with three or four lines: this is a day of real work in his own device-builder project; there are ten pieces of work, in order; he does them himself and nobody is watching; if he gets stuck on one, that piece is a failure, he writes down where he got lost, and he moves on to the next; the program's own next-step card and help are what carry him from one to the next.

Then ten numbered items, in the fixed order from the context: finish and file away the project that is already done there and start the new one; a small clean change; a change with an edit of his own already sitting in the way; planning the work out across several stages; chasing down whatever the checks turn up as broken; a real research question drawn from the goal; stopping mid-run, closing the chat, coming back and picking up; a helper asking for backup and him seeing it happen; something only he can do, tried inside the music software; and — only if a lesson the program taught itself turns out to have been a bad one — undoing it.

For each item, describe the work, not the mechanism. State the new project's goal in the owner's own words, quoted from the context, and say he types it himself on the day.

Name no command anywhere in the document. No slash commands, no program names, no flags, no file paths except the project he is working in. That is the whole point of this artifact: if the program cannot carry him from a plain description to the right action, that is a finding about the program.

Say three more things plainly: three of the ten are also being timed, and taking more than ten minutes from asking to being told it is built and checked counts as a failure on those three even if the work was right; the rest are timed but never failed for length; and the work he had in flight before today is safe under a name, which you give, on the branch you name.

Close with how he records his verdict: one line per piece of work in his own words, then one overall answer to whether it feels like the old system again and he trusts it to operate.
  </action>
  <verify>
    <automated>test -s .planning/phases/205-owner-acceptance-and-restoration-seal/205-BRIEF.md && ! grep -nE "(^|[^a-zA-Z])/ant-|aether [a-z-]+|--[a-z-]{3,}" .planning/phases/205-owner-acceptance-and-restoration-seal/205-BRIEF.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/205-owner-acceptance-and-restoration-seal/205-BRIEF.md` exists and is non-empty
    - The document contains ten numbered items in the order given in the context
    - The document contains the new project's goal quoted in the owner's own words
    - The document contains the snapshot name and the session branch name from Task 1
    - The document names no menu command, no program invocation and no command-line flag
    - Every invented word is explained inline on its first appearance
    - The document states the stuck-means-failure rule, the ten-minute rule for the three timed items, and how the verdict is recorded
  </acceptance_criteria>
  <done>The owner has one page telling him what to do all day and nothing telling him how to ask for it.</done>
</task>

<task type="auto">
  <name>Task 3: The readiness gate — one serialised full run, disk, and the live defect register</name>
  <files>.planning/phases/205-owner-acceptance-and-restoration-seal/205-PRE-SESSION-READINESS.md</files>
  <read_first>
    - CLAUDE.md, the "Verification Commands" section — the ninety-minute timeout rule and the discovered-equals-executed check, including the measurement showing an unqualified run reporting a result after running a third of the tests
    - .planning/WINDOWS.md — re-read on the day; do not trust a cached count
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-RESEARCH.md — the disk-space precaution and the shared-checkout warning
    - The summaries of plans 205-02 through 205-05 and 205-11 — the changes this run is gating
  </read_first>
  <action>
Before running anything, confirm no other session is running the full suite on this checkout. This branch is shared and two concurrent full runs corrupt each other's gates. If another run is in flight, wait rather than starting a second.

Run the full repository test suite once, with the ninety-minute timeout, in a throwaway working copy pinned to the current commit rather than in the shared checkout. Read the run's own headline and confirm the number of tests discovered equals the number executed. If those two numbers differ, the run is inconclusive, not a pass — record it and run again.

Classify every failure against the known pre-existing failures recorded in the project's memory for 2026-09-14. A failure on that list is not caused by this phase; a failure not on that list is, and must be fixed before the release.

Check free disk space and record it. A previous real session filled the owner's disk with test output mid-run; if free space is low, clear the build cache before the session and record that you did.

Re-read the defect register file on the day and record its current open count and the identifiers of every open entry. The count in the context was taken on 2026-09-15 and the file is actively updated; the limitations card in plan 205-17 needs the live list, not a snapshot.

Write `.planning/phases/205-owner-acceptance-and-restoration-seal/205-PRE-SESSION-READINESS.md` recording, in plain words: the commit the run was pinned to, the discovered and executed counts, every failure and its classification, the free disk space, the live open-entry count and identifiers from the defect register, and one clear statement of whether the build is fit to release.
  </action>
  <verify>
    <automated>test -s .planning/phases/205-owner-acceptance-and-restoration-seal/205-PRE-SESSION-READINESS.md && go build ./cmd/aether && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/205-owner-acceptance-and-restoration-seal/205-PRE-SESSION-READINESS.md` exists and records the pinned commit, the discovered count, the executed count, free disk space, and the live open-entry count with identifiers
    - The recorded discovered count equals the recorded executed count
    - Every failure in the run is classified as pre-existing or newly introduced, with the pre-existing list cited
    - No failure is classified as newly introduced and left unfixed
    - `go build ./cmd/aether` exits 0 and `go vet ./...` exits 0
    - The run was performed in a throwaway working copy, not the shared checkout — the SUMMARY names the path used
  </acceptance_criteria>
  <done>The build going out to the owner's machine has one complete, uninterrupted test run behind it, and the live defect list is captured for the card.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing):

- New files `.planning/phases/205-owner-acceptance-and-restoration-seal/205-BRIEF.md` and `205-PRE-SESSION-READINESS.md`
- A snapshot branch in the target project preserving the owner's in-flight work (name recorded in the brief and the SUMMARY)

## Flagged planner assumptions

- The context records several hundred in-flight files on the owner's own experiment branch as of 2026-09-15. Task 1 re-measures before acting rather than trusting that number, and records what it actually found.
- The defect register's open count is read live on the day rather than taken from the context, because the file is actively updated.

<verification>
- The target project's working tree is clean and its colony records are byte-identical to before
- The brief names no command and contains all ten items, the quoted goal, the snapshot name and the session branch
- One complete full test run is recorded with discovered equal to executed and every failure classified
</verification>

<success_criteria>
- Nothing of the owner's is lost, and everything he had is one named checkout away
- He has a one-page description of the day that tells him what, never how
- The build is proven fit to release by a run that provably ran everything it found
</success_criteria>

<output>
Create `.planning/phases/205-owner-acceptance-and-restoration-seal/205-12-SUMMARY.md` when done
</output>
