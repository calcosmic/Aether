---
phase: 205-owner-acceptance-and-restoration-seal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/seal_final_review.go
  - cmd/queen.go
  - cmd/seal_final_review_contract_test.go
autonomous: true
requirements: [PROOF-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A seal final-review worker brief states the handoff fields the finalizer requires, using the same single-source constants the build and check briefs use, so a reviewer following the brief exactly is not rejected on its first finalize attempt."
    - "The handoff sentence appears exactly once in a seal brief — adding it does not produce a duplicate for any caste."
    - "A reviewer-reported lesson containing a shell command, a secrets-file path, an instruction-override phrase, or an XML structural tag is dropped before it can reach the owner-facing habits file, and the seal still succeeds."
    - "A lesson that is dropped for safety is dropped silently, exactly as an empty lesson already is; the seal never fails because of one bad lesson."
    - "A benign, ordinary lesson still reaches the habits file unchanged apart from whitespace collapsing."
  artifacts:
    - cmd/seal_final_review.go
    - cmd/queen.go
    - cmd/seal_final_review_contract_test.go
  key_links:
    - "The seal brief reuses `codex.HandoffFieldsSummary` and `codex.HandoffOpenDecisionsGuidance` — the same constants `cmd/codex_continue_plan.go` and `cmd/codex_build.go` already use."
    - "Lesson safety composes `pkg/colony`'s existing content detector rather than a new hand-written pattern list."
  prohibitions:
    - statement: "A worker-reported lesson must never be promoted into an owner-facing habits file without passing the same untrusted-input filter every other worker-authored store already applies."
      status: unverified
      flagged: true
---

<objective>
Close the two seal defects the 2026-09-14 field report recorded on the exact path the owner's first journey walks: reviewers are not told the contract the finalizer enforces, and whatever a reviewer says gets written into the owner's habits file unfiltered.

Purpose: Journey 1 of the walk-through is seal-then-archive. A known, recorded defect met live is a staged failure, not evidence. Both defects are confirmed still open against current source.

Output: seal reviewer briefs that carry the handoff contract, and a lesson-promotion path that treats worker text as untrusted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-RESEARCH.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-PATTERNS.md
@.planning/field-reports/2026-09-14-cosmic-seal-entomb-lifecycle.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One seal reviewer brief carries the contract the finalizer enforces</name>
  <files>cmd/seal_final_review.go, cmd/seal_final_review_contract_test.go</files>
  <read_first>
    - cmd/seal_final_review.go — `renderSealFinalReviewBrief` in full, plus every call site of it, plus `sealReviewRequiredCastes` and `queenSealReviewSpecs`
    - cmd/codex_continue_plan.go around line 332 — `continueExternalBriefWithHandoffSchema`, including its comment explaining why the append lives at the call site rather than inside the shared renderer
    - cmd/codex_build.go around line 4704 — the identical append on the build lane
    - pkg/codex/handoff.go — `HandoffFieldsSummary`, `HandoffOpenDecisionsGuidance`, `IsEmptyWorkerHandoff`
  </read_first>
  <behavior>
    - A brief rendered for one seal review caste contains the text of `codex.HandoffFieldsSummary`
    - The same brief contains the text of `codex.HandoffOpenDecisionsGuidance`
    - The handoff sentence occurs exactly once in that brief
    - A brief rendered for every caste `queenSealReviewSpecs` can return carries the sentence — no caste is left out
  </behavior>
  <action>
Re-verify first: read `renderSealFinalReviewBrief` and confirm it still carries no handoff-schema text. If a fix has landed since 2026-09-15, record that and treat this task as verification-only rather than inventing a second append.

Add a sibling of the continue lane's helper — name it `sealExternalBriefWithHandoffSchema(rendered string) string` — in `cmd/seal_final_review.go`, appending the same sentence the continue and build lanes append, built from `codex.HandoffFieldsSummary` and `codex.HandoffOpenDecisionsGuidance`. Do not restate the field list inline; the two constants are the single source.

Call it at the same structural position the continue lane uses: at the dispatch call site that consumes the rendered brief, not inside `renderSealFinalReviewBrief` itself, so a native-lane worker that already receives the contract through the response-contract channel does not see it twice. Read the continue lane's own comment on this before choosing the site, and follow the same reasoning.

Write the test first, in a new file `cmd/seal_final_review_contract_test.go`: `TestSealFinalReviewBriefCarriesHandoffSchema` renders a brief for each caste `queenSealReviewSpecs` returns for a representative colony state and asserts the handoff sentence is present exactly once per brief. Show it failing before the fix, then passing after.

Note the review-caste exception rule from CLAUDE.md: the security reviewer and the quality reviewer hold no shell, so they are never instructed to run a command. The handoff sentence is a result-shape instruction, not a command instruction, so it applies to every caste — state this explicitly in the test's own comment so a future reader does not mistake it for the shell-less exception.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestSealFinalReviewBriefCarriesHandoffSchema' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/seal_final_review.go` contains `sealExternalBriefWithHandoffSchema`
    - `cmd/seal_final_review.go` references `codex.HandoffFieldsSummary` and `codex.HandoffOpenDecisionsGuidance`
    - `go test ./cmd/ -run 'TestSealFinalReviewBriefCarriesHandoffSchema' -count=1 -timeout 90m` exits 0
    - The SUMMARY records the test output from the run made BEFORE the fix, showing it failing
    - The rendered brief for each caste contains the handoff sentence exactly once (asserted by count, not by presence)
  </acceptance_criteria>
  <done>A seal reviewer following its brief exactly returns a handoff the finalizer accepts on the first attempt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Worker-reported lessons are untrusted input on the way into the owner's habits file</name>
  <files>cmd/queen.go, cmd/seal_final_review.go, cmd/seal_final_review_contract_test.go, pkg/colony/sanitize.go</files>
  <read_first>
    - cmd/seal_final_review.go — `sealFinalReviewReusableLessons` and `writeSealReusableLessonsToQueen` in full
    - cmd/queen.go — `sanitizeQueenInline`, `appendEntriesToQueenSection`, `writeLocalQueenText`, `isEntryInText`
    - pkg/colony/sanitize.go — `SanitizeSignalContent`, `DetectPromptIntegrityFindings`, and the finding kinds it returns
    - .planning/field-reports/2026-09-14-cosmic-seal-entomb-lifecycle.md — the sixth finding, including the exact shape of the unsafe lesson that was promoted
  </read_first>
  <behavior>
    - A lesson whose text contains a shell command that copies a secrets file is dropped and never appears in the habits text
    - A lesson containing an instruction-override phrase is dropped
    - A lesson containing an XML structural tag is dropped
    - A lesson over the length ceiling the shared detector enforces is dropped
    - An ordinary lesson ("prefer running the focused test before the full suite") is kept, with whitespace collapsed
    - Dropping every lesson in a batch still returns a successful seal result, not an error
  </behavior>
  <action>
Introduce a filter between the collected lessons and the habits file. Keep `sanitizeQueenInline`'s existing whitespace behaviour intact for its other callers; add a new function in `cmd/queen.go` — name it `sanitizeQueenPromotedLesson(value string) (string, bool)` — that first applies the existing whitespace collapse, then runs the lesson through `pkg/colony`'s existing content integrity detector, and returns `("", false)` when the detector reports any finding or when the shared sanitizer refuses the content. Do not write a new regular-expression list; the detector already carries the shell-injection, prompt-injection, XML-tag and length rules.

If the detector's current finding set does not cover a secrets-file path pattern, add that one pattern inside `pkg/colony`'s existing detector alongside its siblings — so every caller of that detector gains the protection — rather than adding a private list inside `cmd/queen.go`.

Change `writeSealReusableLessonsToQueen` to call the new function and to skip a refused lesson with the same silent `continue` shape the empty-string case already uses. The seal must still return success when every lesson is refused.

Write the tests first in `cmd/seal_final_review_contract_test.go`: `TestSealPromotedLessonsAreFiltered` drives `writeSealReusableLessonsToQueen` against a temporary habits file with a mixed batch and asserts the unsafe lines are absent and the ordinary one is present; `TestSealSucceedsWhenEveryLessonIsRefused` asserts a fully-refused batch returns a zero count and an empty error string. Show both failing before the change.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestSealPromotedLessons|TestSealSucceedsWhenEveryLessonIsRefused' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/queen.go` contains `sanitizeQueenPromotedLesson`
    - `cmd/seal_final_review.go`'s `writeSealReusableLessonsToQueen` calls `sanitizeQueenPromotedLesson`
    - `go test ./cmd/ -run 'TestSealPromotedLessonsAreFiltered' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestSealSucceedsWhenEveryLessonIsRefused' -count=1 -timeout 90m` exits 0
    - The SUMMARY records both tests failing before the change
    - Existing callers of `sanitizeQueenInline` are unchanged: `go test ./cmd/ -run 'TestQueen' -count=1 -timeout 90m` exits 0
  </acceptance_criteria>
  <done>An unsafe reviewer-reported lesson can no longer become a line in the owner's habits file, and one bad lesson never blocks a seal.</done>
</task>

<task type="auto">
  <name>Task 3: Regression scope check across the seal and habits packages</name>
  <files>cmd/seal_final_review.go, cmd/queen.go (read and verified only — no edit unless a regression is found)</files>
  <read_first>
    - cmd/seal_final_review.go and cmd/queen.go as changed by Tasks 1 and 2
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-RESEARCH.md, section "Project Constraints (from CLAUDE.md)" — the timeout and discovered-equals-executed rule
  </read_first>
  <action>
Run the scoped package suites that the two changed files belong to and compare any failure against the known-red baseline recorded in the project's memory for 2026-09-14 (seventeen pre-existing failures). A test on that list is not a regression introduced here; a test not on that list is. Record in the SUMMARY which suites were run, the discovered and executed counts, and the classification of every failure seen.

Do not run the whole repository suite in this plan — plan 205-12 owns the single serialised full-gate run, because this checkout is shared with other sessions and concurrent full runs corrupt each other's gates.

If a failure outside the baseline appears, fix it inside this plan rather than deferring it; both changed files are on the owner's first journey.
  </action>
  <verify>
    <automated>go test ./cmd/ ./pkg/colony/ -run 'Seal|Queen|Sanitize|Handoff' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/ ./pkg/colony/ -run 'Seal|Queen|Sanitize|Handoff' -count=1 -timeout 90m` exits 0
    - `go build ./cmd/aether` exits 0
    - `go vet ./cmd/ ./pkg/colony/` exits 0
    - The SUMMARY names every failure observed and classifies each as baseline or regression, citing the baseline list
  </acceptance_criteria>
  <done>Both fixes are green in their own packages and nothing outside the known-red baseline is newly failing.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing):

- `sealExternalBriefWithHandoffSchema` in `cmd/seal_final_review.go`
- `sanitizeQueenPromotedLesson` in `cmd/queen.go`
- Possibly one additional finding kind inside `pkg/colony`'s existing content detector for secrets-file paths
- New file `cmd/seal_final_review_contract_test.go` with `TestSealFinalReviewBriefCarriesHandoffSchema`, `TestSealPromotedLessonsAreFiltered`, `TestSealSucceedsWhenEveryLessonIsRefused`

## Flagged planner assumptions

- The field report's defects 1 and 6 were re-verified against `3c20a884` on 2026-09-15 and confirmed open. Task 1 re-verifies before changing anything; if either has since been fixed by another session on this shared branch, the task becomes verification-only.

<verification>
- The two named fixes each have a test that was observed failing before the change and passing after
- `go build ./cmd/aether` and `go vet ./cmd/ ./pkg/colony/` both exit 0
- No test outside the 2026-09-14 known-red baseline fails in the scoped runs
</verification>

<success_criteria>
- A seal reviewer is told the result shape the finalizer enforces, from the same single source the other lanes use
- An unsafe worker-reported lesson cannot reach the owner's habits file, and refusing it never fails the seal
</success_criteria>

<output>
Create `.planning/phases/205-owner-acceptance-and-restoration-seal/205-02-SUMMARY.md` when done
</output>
