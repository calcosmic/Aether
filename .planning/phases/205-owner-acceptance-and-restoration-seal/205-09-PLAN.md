---
phase: 205-owner-acceptance-and-restoration-seal
plan: 09
type: execute
wave: 2
depends_on: ["205-01", "205-06"]
files_modified:
  - cmd/classic_parity_record_test.go
  - .planning/phases/205-owner-acceptance-and-restoration-seal/205-PARITY.md
autonomous: true
requirements: [PROOF-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every restored slice carries a parity row naming its evidence on all four dimensions — useful outcome, actual behaviour, understandable experience, and fail-closed safety — and a command fails when a dimension has no evidence and no recorded reason."
    - "A slice with no evidence on one of the four dimensions is recorded as not evidenced with its reason, never omitted from the parity table."
    - "Two slices claiming the same piece of parity evidence are recorded as one row citing both slices, never as two rows crediting the same proof twice."
    - "Parity rows are listed in ascending phase order and then ascending slice identifier, with the identifier breaking ties, and the rendering is byte-identical across repeated runs."
    - "The parity record contains no percentage, no endpoint count, and no aggregate score — a count of passing checks is never offered as completion evidence."
    - "The parity audit is read-only: running it twice, or while another session holds this checkout, changes no file on disk."
    - "Every parity evidence entry names a public entry point a person could run, not an internal function."
  artifacts:
    - cmd/classic_parity_record_test.go
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-PARITY.md
  key_links:
    - "The slice list is derived from the signed capability coverage files, so a slice cannot be silently absent from the parity record."
    - "The four dimension names come from the shared synthesis template's verification contract, not a second list."
  prohibitions:
    - statement: "Parity must not be claimed from a count, a percentage, or a passing test alone; each dimension names the public path that demonstrates it, or is recorded as not evidenced with the reason."
      status: unverified
      flagged: true
---

<objective>
Produce the four-dimension parity record PROOF-03 requires, covering every restored slice, and hold it with a command that fails on a blank dimension.

Purpose: PROOF-03 says each phase must prove useful outcome parity, actual behaviour parity and understandable experience parity while preserving the modern kernel's fail-closed safety, and that endpoint counts and percentage claims are not completion evidence. The four dimensions already exist in the shared synthesis template's verification contract; nothing yet gathers them across phases or fails when one is blank.

Output: a parity record over every signed slice, and a named check that refuses a blank dimension and refuses aggregate scoring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/research/v1.28-classic-synthesis-template.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-RESEARCH.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-01-SUMMARY.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-06-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: One slice, four dimensions, held by a command that fails on a blank</name>
  <files>cmd/classic_parity_record_test.go, .planning/phases/205-owner-acceptance-and-restoration-seal/205-PARITY.md</files>
  <read_first>
    - .planning/research/v1.28-classic-synthesis-template.md, section 7 "Verification contract" — the four dimensions, their "what must be true" wording, and the evidence/public-path and failure-case columns
    - .planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md, its own section 7 — a filled-in example of the contract
    - .planning/phases/203-biological-runtime/203-CLASSIC-COVERAGE.json — the signed slice identifiers to key the parity rows on
    - cmd/classic_coverage_ratchet_test.go — the loader and the phase-directory resolution to reuse
    - cmd/classic_synthesis_audit_test.go — the template parsing to reuse rather than duplicate
  </read_first>
  <action>
Create `cmd/classic_parity_record_test.go`.

Derive the four dimension names by parsing the shared template's verification contract table, reusing the template parsing helper plan 205-06 already built. Do not type the four names into this file.

Create these identifiers:
- `classicParityRow` carrying the phase label, the slice identifier, and for each dimension an evidence string, a public-path string, and a not-evidenced reason string.
- `classicParitySliceIDs(root string) ([]classicParitySlice, error)` — the union of signed slice identifiers across every existing coverage file, in ascending phase order then ascending identifier, with the identifier breaking ties.
- `loadClassicParityRecord(root string) ([]classicParityRow, error)` — parses the parity record document into rows.
- `validateClassicParityRecord(rows []classicParityRow, slices []classicParitySlice) error` — every slice has exactly one row; every row's four dimensions each carry either an evidence entry with a public path, or a non-empty not-evidenced reason; a dimension with neither fails naming the slice and the dimension; a slice with no row fails naming the slice; a row for an unknown slice fails naming it.

Write `TestClassicParityRecordCoversPhase203` first: assert every Phase 203 signed slice has a complete parity row. Run it before the record exists so the failure is real, then write the Phase 203 section of `.planning/phases/205-owner-acceptance-and-restoration-seal/205-PARITY.md` and make it pass.

Add `TestClassicParityBlankDimensionFails`: clone a real row in memory with one dimension emptied and no reason, assert it fails naming the slice and the dimension.

Add `TestClassicParityEvidenceNamesAPublicPath`: a dimension whose evidence names an internal function rather than a command, a menu command, or a screen a person sees, fails. Define "public path" by what the project's own wrapper and command inventory exposes, read at test time, rather than by a hand-written list of allowed names.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestClassicParityRecordCoversPhase203|TestClassicParityBlankDimensionFails|TestClassicParityEvidenceNamesAPublicPath' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/classic_parity_record_test.go` derives the four dimension names by parsing the shared template
    - `.planning/phases/205-owner-acceptance-and-restoration-seal/205-PARITY.md` exists and carries a complete parity row for every Phase 203 signed slice
    - `go test ./cmd/ -run 'TestClassicParityRecordCoversPhase203' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestClassicParityBlankDimensionFails' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestClassicParityEvidenceNamesAPublicPath' -count=1 -timeout 90m` exits 0
    - The SUMMARY records the first test failing before the record existed
  </acceptance_criteria>
  <done>One phase's slices carry four-dimension parity evidence, and a blank dimension or an internal-only evidence path fails a command.</done>
</task>

<task type="auto">
  <name>Task 2: Every slice, ordered, deduplicated, and free of aggregate scoring</name>
  <files>.planning/phases/205-owner-acceptance-and-restoration-seal/205-PARITY.md, cmd/classic_parity_record_test.go</files>
  <read_first>
    - Every signed coverage file that exists at this point, under `.planning/phases/*/[0-9]*-CLASSIC-COVERAGE.json`
    - Each phase's own synthesis section 7, for the evidence and failure-case cells already filled in
    - .planning/phases/199-front-door-and-classic-contract/199-UAT.md, 201-UAT.md and 202-UAT.md — where an owner has actually seen a slice, that is the strongest experience-dimension evidence available; where the walk-through was skipped, the experience dimension must say so
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-07-SUMMARY.md and 205-08-SUMMARY.md if they exist — the rows they flagged as having only program-test evidence
  </read_first>
  <action>
Extend `.planning/phases/205-owner-acceptance-and-restoration-seal/205-PARITY.md` to cover every signed slice across all phases, drawing each dimension's evidence from that phase's own synthesis verification contract, its verification record, and its owner walk-through record where one exists.

Where two slices genuinely share one piece of evidence, record one row citing both slice identifiers rather than two rows each crediting the same proof; the validator must treat that as one credit, not two.

Where a dimension has no evidence — most likely the experience dimension for Phase 202, whose owner walk-through was skipped in full — write the not-evidenced reason plainly and route it to the limitations card. Never omit the row.

Add `TestClassicParityRecordCoversEverySignedSlice`, `TestClassicParityRowsAreOrderedAndStable` (render twice, assert byte-identical, ascending phase then ascending identifier), `TestClassicParitySharedEvidenceIsCreditedOnce` (a piece of evidence cited by two slices appears as one row naming both), and `TestClassicParityRecordCarriesNoAggregateScore` (no percent sign, no averaged or totalled score across slices, no endpoint count offered as completion evidence).
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestClassicParity' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/ -run 'TestClassicParityRecordCoversEverySignedSlice' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestClassicParityRowsAreOrderedAndStable' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestClassicParitySharedEvidenceIsCreditedOnce' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestClassicParityRecordCarriesNoAggregateScore' -count=1 -timeout 90m` exits 0
    - `.planning/phases/205-owner-acceptance-and-restoration-seal/205-PARITY.md` has one row per signed slice and contains no percent sign
    - The SUMMARY lists every dimension recorded as not evidenced, with its reason, ready for the limitations card
  </acceptance_criteria>
  <done>Every restored slice carries four-dimension parity evidence or a stated reason it does not, in a stable, count-free record.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the parity audit writes nothing</name>
  <files>cmd/classic_parity_record_test.go</files>
  <read_first>
    - cmd/classic_parity_record_test.go as written by Tasks 1 and 2
    - CLAUDE.md — the corollary that an inspection command must not mutate state, and the two prior cases where a stated read-only mode wrote anyway
  </read_first>
  <action>
Add `TestClassicParityAuditDoesNotMutate`: snapshot the modification time and content digest of every file under the phase directories the audit reads, run the full parity audit twice, and assert nothing changed. Include the parity record itself and every coverage file in the snapshot.

This repository has shipped two inspection commands that wrote to disk despite documenting otherwise. Write the check so it would have caught those: compare content digests, not just modification times, and include files the audit only reads as well as files it might plausibly write.

Record in the SUMMARY the number of files snapshotted and the fact that two consecutive full runs left all of them unchanged.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestClassicParityAuditDoesNotMutate' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/ -run 'TestClassicParityAuditDoesNotMutate' -count=1 -timeout 90m` exits 0
    - The check compares content digests, not modification times alone
    - `git status --porcelain .planning/` after the run shows no modification attributable to the audit
    - The SUMMARY records the snapshotted file count and the two-run result
  </acceptance_criteria>
  <done>The parity audit is provably an inspection, not a writer.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing):

- Go identifiers in `cmd/classic_parity_record_test.go`: `classicParityRow`, `classicParitySlice`, `classicParitySliceIDs`, `loadClassicParityRecord`, `validateClassicParityRecord`
- Tests: `TestClassicParityRecordCoversPhase203`, `TestClassicParityBlankDimensionFails`, `TestClassicParityEvidenceNamesAPublicPath`, `TestClassicParityRecordCoversEverySignedSlice`, `TestClassicParityRowsAreOrderedAndStable`, `TestClassicParitySharedEvidenceIsCreditedOnce`, `TestClassicParityRecordCarriesNoAggregateScore`, `TestClassicParityAuditDoesNotMutate`
- New file `.planning/phases/205-owner-acceptance-and-restoration-seal/205-PARITY.md`

## Flagged planner assumptions

- Phase 202's owner walk-through was skipped in full, so its experience-dimension evidence is expected to be recorded as not evidenced with a reason rather than proven. That is the honest outcome, not a failure of this plan; it becomes a line on the limitations card.

<verification>
- Every signed slice has exactly one parity row and every row's four dimensions carry evidence or a stated reason
- A blank dimension, an internal-only evidence path, an out-of-order rendering, a double-credited proof, and an aggregate score each have a demonstrated failing test
- Two consecutive full audit runs leave every read file byte-identical
</verification>

<success_criteria>
- PROOF-03's four dimensions are evidenced per slice by a command that fails on a blank, with no percentage or count offered as completion evidence
</success_criteria>

<output>
Create `.planning/phases/205-owner-acceptance-and-restoration-seal/205-09-SUMMARY.md` when done
</output>
