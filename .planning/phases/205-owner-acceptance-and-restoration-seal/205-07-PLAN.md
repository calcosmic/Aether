---
phase: 205-owner-acceptance-and-restoration-seal
plan: 07
type: execute
wave: 2
depends_on: ["205-01"]
files_modified:
  - .planning/phases/200-iterative-planning/200-CLASSIC-COVERAGE.json
  - .planning/phases/200-iterative-planning/200-CLASSIC-COVERAGE.md
  - .planning/phases/201-queen-led-work-cycle/201-CLASSIC-COVERAGE.json
  - .planning/phases/201-queen-led-work-cycle/201-CLASSIC-COVERAGE.md
  - .planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-COVERAGE.json
  - .planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-COVERAGE.md
  - cmd/classic_coverage_phase_signing_test.go
autonomous: true
requirements: [PROOF-02]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The seven capability rows the frozen ledger routes to Phase 200 are signed, and the ratchet passes on them."
    - "The eight capability rows routed to Phase 201 are signed, and the ratchet passes on them."
    - "The eight capability rows routed to Phase 202 are signed, and the ratchet passes on them."
    - "Every signed row names at least one proof that resolves to a real top-level test function in the command package, and every named proof was individually executed and observed passing before it was written into the row."
    - "Every signed row's artifact path resolves to a real, non-directory file on disk."
    - "Three rows the ledger marks as already delivered in an earlier phase — CAP-010, CAP-046 and CAP-051 — are signed with a real proof rather than carried as a label."
  artifacts:
    - .planning/phases/200-iterative-planning/200-CLASSIC-COVERAGE.json
    - .planning/phases/201-queen-led-work-cycle/201-CLASSIC-COVERAGE.json
    - .planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-COVERAGE.json
    - cmd/classic_coverage_phase_signing_test.go
  key_links:
    - "Each phase's expected row set comes from the frozen ledger through the shared derivation, never a hand-typed list."
    - "Each phase's disposition and evidence come from that phase's own CLASSIC-SYNTHESIS.md routing table — the narrative form is the justification, the signed row is the fact."
  prohibitions:
    - statement: "A disposition must not be signed because the ledger already proposed it; the phase's own study and a proof that runs are what make it a fact."
      status: unverified
      flagged: true
---

<objective>
Convert the prose capability dispositions in the Phase 200, 201 and 202 studies into machine-signed rows the ratchet checks.

Purpose: PROOF-02 requires every capability row to carry one evidence-backed disposition that a command validates. Twenty-three of the thirty-eight unsigned rows belong to these three phases, and today they exist only as narrative table entries.

Output: three signed coverage files with rendered companions, all passing the ratchet built in plan 205-01.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-RESEARCH.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-01-SUMMARY.md
@.planning/research/v1.28-classic-capability-ledger.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Phase 200's seven rows, signed and passing the ratchet</name>
  <files>.planning/phases/200-iterative-planning/200-CLASSIC-COVERAGE.json, .planning/phases/200-iterative-planning/200-CLASSIC-COVERAGE.md, cmd/classic_coverage_phase_signing_test.go</files>
  <read_first>
    - cmd/classic_coverage_ratchet_test.go (as built by plan 205-01 — the row shape, the validators, the ledger-derived expected set, the re-adjudication marker)
    - .planning/phases/203-biological-runtime/203-CLASSIC-COVERAGE.json and .md (the worked example to follow)
    - .planning/phases/200-iterative-planning/200-CLASSIC-SYNTHESIS.md, section 6 "Research-to-plan linkage" — where Phase 200's capability routing lives as a table column
    - .planning/research/v1.28-classic-capability-ledger.md, section "v1.28 requirement and phase mapping" — the routed disposition for each Phase 200 row
    - The Phase 200 plan and summary files under `.planning/phases/200-iterative-planning/` needed to fill `plan_task` and `artifact` accurately
  </read_first>
  <action>
Create `.planning/phases/200-iterative-planning/200-CLASSIC-COVERAGE.json` with document version `200/classic-coverage/v1` and one `type: GOAL` row per capability id the ledger routes to Phase 200 — CAP-005, CAP-010, CAP-011, CAP-012, CAP-056, CAP-061 and CAP-069.

For each row: take `disposition` from the ledger unless Phase 200's own study re-adjudicated it, in which case use the study's disposition and put the token for a recorded re-adjudication plus the citation into `historical_evidence`. Take `modern_home` and `artifact` from the study's linkage table and confirm the artifact path exists on disk. Take `plan_task` from the Phase 200 plan that delivered it. Set `status` to the pass value.

For `proofs`: find the named test or contract case that would fail if the capability stopped working, run it on its own, observe it passing, and only then write its name into the row. Never write a proof name that has not been executed in this session. Record each command and its result in the SUMMARY.

CAP-010 is one of three rows the ledger labels as already delivered in an earlier phase. A label is not a signature: find the real test that proves it and sign the row with that, or, if no such test exists, write one before signing.

Write `.planning/phases/200-iterative-planning/200-CLASSIC-COVERAGE.md` as the rendered companion in the same shape as Phase 203's: a short plain-English paragraph saying what the table is, a `## GOAL` heading, and a table row per capability id in ascending order pairing the id with its disposition, its modern home and its named proof.

Add `cmd/classic_coverage_phase_signing_test.go` with `TestClassicCoveragePhase200Signed`, calling the shared validator for phase `200`.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestClassicCoveragePhase200Signed' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/200-iterative-planning/200-CLASSIC-COVERAGE.json` parses as JSON and contains exactly seven GOAL rows with the ids CAP-005, CAP-010, CAP-011, CAP-012, CAP-056, CAP-061, CAP-069
    - `.planning/phases/200-iterative-planning/200-CLASSIC-COVERAGE.md` exists with a `## GOAL` heading and one table row per id
    - `go test ./cmd/ -run 'TestClassicCoveragePhase200Signed' -count=1 -timeout 90m` exits 0
    - The SUMMARY lists, per row, the exact command used to execute its named proof and the observed result
    - CAP-010's row names a proof that was executed in this session, not the earlier-phase label
  </acceptance_criteria>
  <done>Phase 200's capability dispositions are facts a command checks, with every proof executed before it was cited.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 201's eight rows, signed and passing the ratchet</name>
  <files>.planning/phases/201-queen-led-work-cycle/201-CLASSIC-COVERAGE.json, .planning/phases/201-queen-led-work-cycle/201-CLASSIC-COVERAGE.md, cmd/classic_coverage_phase_signing_test.go</files>
  <read_first>
    - .planning/phases/201-queen-led-work-cycle/201-CLASSIC-SYNTHESIS.md, section "CAP row routing table (Phase 201's eight rows)" around line 229
    - .planning/research/v1.28-classic-capability-ledger.md — the routed dispositions for CAP-003, CAP-004, CAP-022, CAP-024, CAP-029, CAP-051, CAP-066, CAP-071
    - The Phase 201 plan and summary files needed for `plan_task` and `artifact`
    - `.planning/phases/200-iterative-planning/200-CLASSIC-COVERAGE.json` as written in Task 1, for the shape to follow
  </read_first>
  <action>
Create `.planning/phases/201-queen-led-work-cycle/201-CLASSIC-COVERAGE.json` and its rendered companion by the same method as Task 1, covering CAP-003, CAP-004, CAP-022, CAP-024, CAP-029, CAP-051, CAP-066 and CAP-071.

CAP-051 is the second of the three rows the ledger labels as already delivered in an earlier phase. Sign it with a proof executed in this session, or write the missing test first.

Add `TestClassicCoveragePhase201Signed` to `cmd/classic_coverage_phase_signing_test.go`.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestClassicCoveragePhase201Signed' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/201-queen-led-work-cycle/201-CLASSIC-COVERAGE.json` contains exactly eight GOAL rows with the ids CAP-003, CAP-004, CAP-022, CAP-024, CAP-029, CAP-051, CAP-066, CAP-071
    - `.planning/phases/201-queen-led-work-cycle/201-CLASSIC-COVERAGE.md` exists with a `## GOAL` heading and one table row per id
    - `go test ./cmd/ -run 'TestClassicCoveragePhase201Signed' -count=1 -timeout 90m` exits 0
    - The SUMMARY lists, per row, the command used to execute its named proof and the observed result
  </acceptance_criteria>
  <done>Phase 201's eight capability dispositions pass the ratchet.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 202's eight rows, signed and passing the ratchet</name>
  <files>.planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-COVERAGE.json, .planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-COVERAGE.md, cmd/classic_coverage_phase_signing_test.go</files>
  <read_first>
    - .planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md, section "CAP row routing table (Phase 202's eight rows)" around line 229
    - .planning/research/v1.28-classic-capability-ledger.md — the routed dispositions for CAP-021, CAP-044, CAP-045, CAP-046, CAP-047, CAP-048, CAP-063, CAP-072
    - .planning/phases/202-swarm-oracle-and-live-colony/202-UAT.md — all three of its owner tests were skipped because the owner rejected the flow they needed; note whether any Phase 202 row's only evidence was that skipped walk-through
    - The Phase 202 plan and summary files needed for `plan_task` and `artifact`
  </read_first>
  <action>
Create `.planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-COVERAGE.json` and its rendered companion by the same method, covering CAP-021, CAP-044, CAP-045, CAP-046, CAP-047, CAP-048, CAP-063 and CAP-072.

CAP-046 is the third row labelled as delivered in an earlier phase. Sign it with a proof executed in this session.

Phase 202's owner walk-through was skipped in full. If any row here would have leaned on that skipped walk-through for its evidence, it must instead name a test that runs, and the SUMMARY must say so explicitly — this is exactly the label-only restoration the requirement forbids. Record any row where the only available evidence is a program test and no owner has ever seen the behaviour; that becomes an entry for the parity record in plan 205-09 and, if it survives, a line on the limitations card.

Add `TestClassicCoveragePhase202Signed` to `cmd/classic_coverage_phase_signing_test.go`.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestClassicCoveragePhase20[012]Signed' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-COVERAGE.json` contains exactly eight GOAL rows with the ids CAP-021, CAP-044, CAP-045, CAP-046, CAP-047, CAP-048, CAP-063, CAP-072
    - `.planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-COVERAGE.md` exists with a `## GOAL` heading and one table row per id
    - `go test ./cmd/ -run 'TestClassicCoveragePhase20[012]Signed' -count=1 -timeout 90m` exits 0
    - The SUMMARY names every Phase 202 row whose only evidence is a program test with no owner ever having seen the behaviour
    - The SUMMARY lists, per row, the command used to execute its named proof and the observed result
  </acceptance_criteria>
  <done>All twenty-three capability rows across Phases 200, 201 and 202 are signed and pass the ratchet.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing):

- New files: `.planning/phases/200-iterative-planning/200-CLASSIC-COVERAGE.json` and `.md`, `.planning/phases/201-queen-led-work-cycle/201-CLASSIC-COVERAGE.json` and `.md`, `.planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-COVERAGE.json` and `.md`
- New file `cmd/classic_coverage_phase_signing_test.go` with `TestClassicCoveragePhase200Signed`, `TestClassicCoveragePhase201Signed`, `TestClassicCoveragePhase202Signed`
- Any new test written to supply a genuine proof for CAP-010, CAP-046 or CAP-051 (name it in the SUMMARY)

## Flagged planner assumptions

- The three rows the ledger labels as already delivered in an earlier phase were confirmed as labels, not signatures. This plan treats each as unsigned until a proof has been executed in this session.

<verification>
- Twenty-three rows are signed across three coverage files and the ratchet passes on each phase
- Every named proof was executed individually and observed passing before being cited, with the commands recorded
- No row's disposition differs from the frozen ledger without a recorded re-adjudication citation
</verification>

<success_criteria>
- Phases 200, 201 and 202 each have a signed capability coverage file that the ratchet validates
- No row is signed on a label, a ledger hypothesis, or an unexecuted proof name
</success_criteria>

<output>
Create `.planning/phases/205-owner-acceptance-and-restoration-seal/205-07-SUMMARY.md` when done
</output>
