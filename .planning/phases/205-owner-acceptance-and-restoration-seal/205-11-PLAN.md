---
phase: 205-owner-acceptance-and-restoration-seal
plan: 11
type: execute
wave: 3
depends_on: ["205-01", "205-04", "205-07", "205-08", "205-09", "205-10"]
files_modified:
  - .planning/phases/205-owner-acceptance-and-restoration-seal/205-CLASSIC-COVERAGE.json
  - .planning/phases/205-owner-acceptance-and-restoration-seal/205-CLASSIC-COVERAGE.md
  - cmd/classic_coverage_master_test.go
autonomous: true
requirements: [PROOF-02, PROOF-03, PROOF-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The two capability rows the frozen ledger routes to Phase 205 — documentation accuracy and platform readiness — are signed with proofs built in this phase, and the ratchet passes on them."
    - "The master ratchet passes at exactly seventy-two distinct capability ids across all six signed coverage files, and fails at seventy-one or seventy-three."
    - "A capability id signed in two phases' coverage files fails the master ratchet, naming the id and both phases."
    - "A capability id the frozen ledger routes to a phase whose coverage file does not carry it fails the master ratchet, naming the id and the phase."
    - "The master ratchet reports counts as exact whole numbers and never reports coverage as a percentage or a proportion."
    - "Failure output from the master ratchet lists offending ids in ascending order and is byte-identical across repeated runs."
  artifacts:
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-CLASSIC-COVERAGE.json
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-CLASSIC-COVERAGE.md
    - cmd/classic_coverage_master_test.go
  key_links:
    - "The master ratchet reuses each phase's own loader and validator rather than re-implementing them."
    - "The expected universe of seventy-two ids comes from the frozen ledger, not a literal in the test."
  prohibitions:
    - statement: "The reconciliation must not be declared complete by a count alone; the master check proves every id is signed, unique, correctly routed, and backed by a proof that resolves."
      status: unverified
      flagged: true
---

<objective>
Sign Phase 205's own two capability rows and close the reconciliation with one master check over all seventy-two.

Purpose: PROOF-02's literal wording is that every capability row maps exactly once to a requirement and phase, with a signed disposition, and that missing, duplicate or unsupported dispositions fail the ratchet. Thirty-six rows were signed by the preceding plans and two belong to this phase itself — documentation accuracy, whose proof is the corrected wrapper check, and platform readiness, whose proof is the captured-run card check.

Output: Phase 205's signed coverage file and the master cross-phase ratchet that makes the full reconciliation a fact a command checks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-RESEARCH.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-01-SUMMARY.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-04-SUMMARY.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-10-SUMMARY.md
@.planning/research/v1.28-classic-capability-ledger.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Phase 205's two rows, signed with proofs this phase built</name>
  <files>.planning/phases/205-owner-acceptance-and-restoration-seal/205-CLASSIC-COVERAGE.json, .planning/phases/205-owner-acceptance-and-restoration-seal/205-CLASSIC-COVERAGE.md, cmd/classic_coverage_master_test.go</files>
  <read_first>
    - cmd/classic_coverage_ratchet_test.go — the shared validators and the ledger-derived expected set
    - .planning/research/v1.28-classic-capability-ledger.md — the routed dispositions and rationales for CAP-023 and CAP-054
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-04-SUMMARY.md and `cmd/seal_wrapper_accuracy_test.go` — the documentation-accuracy proofs available to cite
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-10-SUMMARY.md and `cmd/platform_honesty_card_test.go` — the platform-readiness proofs available to cite
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-PARITY.md — where these two rows' parity evidence sits
    - .planning/phases/203-biological-runtime/203-CLASSIC-COVERAGE.json — the shape to follow
  </read_first>
  <action>
Create `.planning/phases/205-owner-acceptance-and-restoration-seal/205-CLASSIC-COVERAGE.json` with document version `205/classic-coverage/v1` and exactly two `type: GOAL` rows, CAP-023 and CAP-054.

CAP-023 covers documentation accuracy checked against real public behaviour. Its modern home and artifact are the corrected wrapper source and the check that holds the wrapper's claim to the runtime's own derivation; its proofs are the named tests plan 205-04 created. Execute each named proof individually before citing it.

CAP-054 covers helper and provider readiness expressed as a versioned capability matrix and actionable preflight truth. Its modern home is the existing versioned platform support contract; its artifact is that file; its proofs are the named tests plan 205-10 created binding each card claim to a captured run. Execute each named proof individually before citing it.

Take each disposition from the frozen ledger unless this phase's own work re-adjudicated it, in which case carry the re-adjudication marker and a citation, and expect the bounded-allowance check from plan 205-08 to account for it.

Write `.planning/phases/205-owner-acceptance-and-restoration-seal/205-CLASSIC-COVERAGE.md` as the rendered companion in the same shape as the other phases, with its short plain-English opening paragraph.

Add `cmd/classic_coverage_master_test.go` with `TestClassicCoveragePhase205Signed`, calling the shared validator for phase `205`.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestClassicCoveragePhase205Signed' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/205-owner-acceptance-and-restoration-seal/205-CLASSIC-COVERAGE.json` parses as JSON and contains exactly two GOAL rows with the ids CAP-023 and CAP-054
    - `.planning/phases/205-owner-acceptance-and-restoration-seal/205-CLASSIC-COVERAGE.md` exists with a `## GOAL` heading and one table row per id
    - `go test ./cmd/ -run 'TestClassicCoveragePhase205Signed' -count=1 -timeout 90m` exits 0
    - CAP-023's proofs name tests declared in `cmd/seal_wrapper_accuracy_test.go`
    - CAP-054's proofs name tests declared in `cmd/platform_honesty_card_test.go`
    - The SUMMARY lists, per row, the command used to execute its named proof and the observed result
  </acceptance_criteria>
  <done>The two capability rows this phase owns are signed with proofs this phase actually built and ran.</done>
</task>

<task type="auto">
  <name>Task 2: The master check over all seventy-two rows</name>
  <files>cmd/classic_coverage_master_test.go</files>
  <read_first>
    - cmd/classic_coverage_ratchet_test.go — `loadClassicCoverageLedgerRouting`, `classicCoveragePathForPhase`, and the per-phase validators to reuse
    - cmd/classic_coverage_199_test.go — Phase 199's own document, whose thirty-four capability rows must be included in the union alongside its other row types
    - The five coverage files created by plans 205-01, 205-07, 205-08 and Task 1
    - .planning/research/v1.28-classic-capability-ledger.md — the frozen universe of seventy-two ids
  </read_first>
  <action>
Add to `cmd/classic_coverage_master_test.go`:

`TestClassicCoverageMasterUnionIsCompleteAndUnique` — load every phase coverage file the ledger routes rows to, including Phase 199's, take the union of their capability rows, and assert: the union equals exactly the frozen ledger's universe of ids; no id appears in more than one file; every id the ledger routes to a phase is present in that phase's own file. Derive the expected universe from the ledger, never from a literal list in the test. Failures name each offending id, in ascending order, with the phase or phases involved.

`TestClassicCoverageMasterFailsOnAMissingRow` — clone the loaded set in memory, remove one row, assert the master check fails naming that id and its routed phase.

`TestClassicCoverageMasterFailsOnADuplicateRow` — clone the set, add the same id into a second phase's document, assert the check fails naming the id and both phases.

`TestClassicCoverageMasterFailsOnAMisroutedRow` — clone the set, move a row into a phase the ledger did not route it to, assert the check fails naming the id, the file it was found in and the phase the ledger routed it to.

`TestClassicCoverageMasterReportsCountsNotPercentages` — assert the master check's own summary output contains a whole-number count and no percent sign and no proportion.

`TestClassicCoverageMasterFailureOutputIsOrderedAndStable` — remove three rows at once and assert the failure text lists their ids in ascending order and is identical across two consecutive runs.

Every one of these must operate on in-memory clones and temporary roots; the real coverage files are never modified by a test.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestClassicCoverage' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/ -run 'TestClassicCoverageMasterUnionIsCompleteAndUnique' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestClassicCoverageMasterFailsOnAMissingRow' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestClassicCoverageMasterFailsOnADuplicateRow' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestClassicCoverageMasterFailsOnAMisroutedRow' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestClassicCoverageMasterReportsCountsNotPercentages' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestClassicCoverageMasterFailureOutputIsOrderedAndStable' -count=1 -timeout 90m` exits 0
    - The master test contains no literal list of capability ids; the universe is derived from the frozen ledger
    - `git status --porcelain .planning/phases/` after the run shows no modification to any coverage file
  </acceptance_criteria>
  <done>One command proves all seventy-two capability rows are signed exactly once, routed as the frozen ledger says, and backed by proofs that resolve.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing):

- New files `.planning/phases/205-owner-acceptance-and-restoration-seal/205-CLASSIC-COVERAGE.json` and `.md`
- New file `cmd/classic_coverage_master_test.go` with `TestClassicCoveragePhase205Signed`, `TestClassicCoverageMasterUnionIsCompleteAndUnique`, `TestClassicCoverageMasterFailsOnAMissingRow`, `TestClassicCoverageMasterFailsOnADuplicateRow`, `TestClassicCoverageMasterFailsOnAMisroutedRow`, `TestClassicCoverageMasterReportsCountsNotPercentages`, `TestClassicCoverageMasterFailureOutputIsOrderedAndStable`

## Flagged planner assumptions

- Phase 202.1 owns no capability rows. The master check derives which phases carry rows from the frozen ledger, so a phase with none is correctly absent rather than a missing-file failure; Task 2's derivation must handle that case without special-casing the phase by name.

<verification>
- All seventy-two capability rows are signed across six files and the master check passes
- The master check has demonstrated failures for a missing row, a duplicate row, a misrouted row, a percentage report, and unstable ordering
- Phase 205's own two rows cite proofs built and executed inside this phase
</verification>

<success_criteria>
- PROOF-02 is satisfied by a command that fails on a missing, duplicate, misrouted or unsupported capability disposition
- The reconciliation is complete and its completeness is a fact a command checks, not a count in a document
</success_criteria>

<output>
Create `.planning/phases/205-owner-acceptance-and-restoration-seal/205-11-SUMMARY.md` when done
</output>
