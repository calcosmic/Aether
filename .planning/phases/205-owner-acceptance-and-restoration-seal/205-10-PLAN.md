---
phase: 205-owner-acceptance-and-restoration-seal
plan: 10
type: execute
wave: 2
depends_on: ["205-02", "205-03", "205-04", "205-05"]
files_modified:
  - .planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-OPENCODE.md
  - .planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-CODEX.md
  - .planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-CLAUDE.md
  - .planning/phases/205-owner-acceptance-and-restoration-seal/evidence/platform-runs/
  - pkg/codex/platform_contract.go
  - cmd/platform_honesty_card_test.go
autonomous: true
requirements: [PROOF-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the three platforms has one card stating, in plain words, what works, what stops early with a truthful message, and what is not supported — and every statement on a card traces to a captured transcript of a real run made in this phase."
    - "A real public entry point was run once on each platform and its full output was captured to a file under this phase's evidence directory."
    - "A card claim that the captured transcript does not support fails a named check, naming the claim and the card."
    - "Where a real run contradicts the versioned platform support contract, the contract is corrected to match the run, and the correction is recorded."
    - "No card claims a platform ability is governed by this program without a recorded run showing the program's own ledger binding it."
    - "The cards contain no engineering promise for the two secondary platforms — they describe what happened, not what could be made to happen."
  artifacts:
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-CLAUDE.md
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-OPENCODE.md
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-CODEX.md
    - cmd/platform_honesty_card_test.go
  key_links:
    - "The cards are rendered from the existing versioned platform support contract, not from a second hand-written list of abilities."
    - "Every card claim is bound to a captured transcript file in this phase's evidence directory."
  prohibitions:
    - statement: "A platform must not be described as working on the strength of a code reading, a shared command name, or a previous release; only a captured run made in this phase may support a card claim."
      status: unverified
      flagged: true
---

<objective>
Run the real public entry point on each supported platform once, capture what actually happened, and write one honest card per platform.

Purpose: PROOF-04 requires real public entry points to prove the declared outcomes or fail early with a truthful limitation, and forbids calling a platform's native ability governed by this program without ledger binding. The owner has decided Claude Code is the only platform that has to work; the other two get the honest minimum and no engineering effort. This is also capability row CAP-054 — provider and helper readiness expressed as a versioned capability matrix and actionable preflight truth — and that matrix already exists in the codebase.

Output: three captured transcripts, three plain-English cards, and a check that refuses a card claim the transcript does not support.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: One platform, one real run, one card bound to the transcript</name>
  <files>.planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-OPENCODE.md, .planning/phases/205-owner-acceptance-and-restoration-seal/evidence/platform-runs/opencode-run.txt, cmd/platform_honesty_card_test.go, pkg/codex/platform_contract.go</files>
  <read_first>
    - pkg/codex/platform_contract.go in full — `CapabilityLevel`, `CapabilitySupport`, `PlatformContract`, `PlatformContractFor`, and the per-platform entries for Claude, OpenCode and Codex
    - .opencode/commands/ant/status.md and .opencode/OPENCODE.md — OpenCode's real public entry points
    - .codex/CODEX.md and .codex/agents/ — what Codex actually exposes, and the note that Codex uses the native command line rather than menu commands
    - .planning/REQUIREMENTS.md, the PROOF-04 entry — the exact wording about truthful limitations and ledger binding
    - CLAUDE.md — the platform policy section and the communication style block
  </read_first>
  <action>
Start with OpenCode, the platform with the most to be honest about.

Create `.planning/phases/205-owner-acceptance-and-restoration-seal/evidence/platform-runs/` and run OpenCode's real public entry point against this repository once, non-interactively, capturing the full output — including any error — to `opencode-run.txt` in that directory. Record the platform version, the exact command line used, the working directory, and the exit status at the top of the capture. If the entry point cannot be driven non-interactively, say so in the capture and drive the closest genuine public path that can be, recording precisely which path was used and why. An entry point that refuses to run is itself a result worth capturing; do not substitute a simulated run.

Create `cmd/platform_honesty_card_test.go` with:
- `platformHonestyCardPath(platform string) string` and `platformRunTranscriptPath(platform string) string`.
- `loadPlatformHonestyCard(path string) (platformHonestyCard, error)` — parses a card into its three lists: what works, what stops early with a truthful message, what is not supported. Each entry carries the claim text and a transcript line reference.
- `validatePlatformHonestyCard(card platformHonestyCard, transcript string) error` — every claim's transcript reference must resolve to a real line in the captured transcript, and the referenced line must be present verbatim. A claim with no reference, or a reference that does not resolve, fails naming the claim.

Write `TestOpenCodeCardClaimsResolveToTheCapturedRun` first and watch it fail with no card present, then write `.planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-OPENCODE.md` and make it pass.

The card itself is for the owner. Open with one or two sentences saying what this platform is and what was tried. Then three plain lists: what worked, what stopped early and what it said when it did, and what is simply not supported. Every invented word explained inline. No line may promise future work — the owner has explicitly excluded engineering effort on this platform for this milestone.

Finally, compare the captured run against the versioned platform support contract's OpenCode entry. Where the run contradicts a promise level, correct the contract entry in `pkg/codex/platform_contract.go` to match the run and record the correction in the SUMMARY. Where the run confirms it, say so. Never edit the run to match the contract.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestOpenCodeCardClaimsResolveToTheCapturedRun' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/205-owner-acceptance-and-restoration-seal/evidence/platform-runs/opencode-run.txt` exists, is non-empty, and records the platform version, the exact command line, the working directory and the exit status
    - `.planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-OPENCODE.md` exists with all three lists present
    - `go test ./cmd/ -run 'TestOpenCodeCardClaimsResolveToTheCapturedRun' -count=1 -timeout 90m` exits 0
    - Every claim on the card carries a transcript reference that resolves to a verbatim line in the capture
    - The SUMMARY states whether the run confirmed or contradicted the versioned contract's OpenCode entry, and records any correction made to the contract
  </acceptance_criteria>
  <done>One platform's card says only what a captured real run supports, and the versioned contract agrees with that run.</done>
</task>

<task type="auto">
  <name>Task 2: The other two platforms, run and carded the same way</name>
  <files>.planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-CODEX.md, .planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-CLAUDE.md, .planning/phases/205-owner-acceptance-and-restoration-seal/evidence/platform-runs/codex-run.txt, .planning/phases/205-owner-acceptance-and-restoration-seal/evidence/platform-runs/claude-run.txt, cmd/platform_honesty_card_test.go, pkg/codex/platform_contract.go</files>
  <read_first>
    - `.planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-OPENCODE.md` and the OpenCode capture, as the worked example
    - .codex/CODEX.md in full — Codex's native command line workflow and the twenty-seven helper definitions
    - .claude/commands/ant/status.md — Claude Code's real public entry point
    - pkg/codex/platform_contract.go — the Codex and Claude entries
  </read_first>
  <action>
Run Codex's real public entry point against this repository once, capturing the full output to `codex-run.txt` in the same evidence directory, with the same header fields. Codex has no menu commands in this milestone and drives the runtime directly; capture that path, not an invented one.

Run Claude Code's real public entry point against this repository once, capturing to `claude-run.txt`. Claude Code is the platform that has to work, so this capture is the primary evidence for PROOF-04's declared outcome, not a formality — capture a real lifecycle command, not a version check.

Write `.planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-CODEX.md` and `205-PLATFORM-CARD-CLAUDE.md` in the same three-list shape, every claim bound to its transcript.

Add `TestCodexCardClaimsResolveToTheCapturedRun`, `TestClaudeCardClaimsResolveToTheCapturedRun`, and `TestEveryContractedPlatformHasACardAndARun` — the last iterating the platforms the versioned contract knows about and failing if any lacks either a card or a capture.

Compare each capture against its contract entry and correct the contract where the run contradicts it, recording every correction.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'CardClaimsResolveToTheCapturedRun|TestEveryContractedPlatformHasACardAndARun' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/platform-runs/codex-run.txt` and `evidence/platform-runs/claude-run.txt` both exist, are non-empty, and carry the four header fields
    - `205-PLATFORM-CARD-CODEX.md` and `205-PLATFORM-CARD-CLAUDE.md` both exist with all three lists
    - `go test ./cmd/ -run 'CardClaimsResolveToTheCapturedRun' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestEveryContractedPlatformHasACardAndARun' -count=1 -timeout 90m` exits 0
    - The Claude Code capture is of a real lifecycle command, not a version or help invocation
    - The SUMMARY records, per platform, whether the contract was confirmed or corrected
  </acceptance_criteria>
  <done>All three platforms have a captured real run and a card whose every claim the capture supports.</done>
</task>

<task type="auto">
  <name>Task 3: No ungoverned ability is called governed, and no card promises future work</name>
  <files>cmd/platform_honesty_card_test.go, .planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-CLAUDE.md, .planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-OPENCODE.md, .planning/phases/205-owner-acceptance-and-restoration-seal/205-PLATFORM-CARD-CODEX.md</files>
  <read_first>
    - The three cards and the three captures
    - .planning/REQUIREMENTS.md, PROOF-04 — the clause forbidding a native platform ability being called governed by this program without ledger binding
    - cmd/episode_ledger.go — what a ledger binding actually looks like on disk
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md, decision D-13 — no engineering effort on the two secondary platforms
  </read_first>
  <action>
Add `TestPlatformCardsDoNotClaimUngovernedAbilityAsGoverned`: for every claim on every card that describes an ability as being run, tracked, or accounted for by this program, assert the capture shows a matching record in this program's own run ledger. A claim that leans on a platform's own native ability with no such record must be phrased as the platform's ability, not this program's, and the check must fail if it is not.

Add `TestPlatformCardsPromiseNoFutureWork`: assert no card contains a forward-looking promise for the two secondary platforms. Define the rule by intent in the test's own comment and implement it as a check over the card's claim entries, each of which must be a statement about what happened.

Re-read all three cards as someone who has never opened this repository. Rewrite any sentence that needs a file, a code, or an invented word to make sense. Record the before and after of every rewrite in the SUMMARY.

Run the existing platform-contract tests to confirm any correction made to the versioned contract in Tasks 1 and 2 did not break an existing promise check, and classify any failure against the known-red baseline recorded in the project's memory for 2026-09-14.
  </action>
  <verify>
    <automated>go test ./cmd/ ./pkg/codex/ -run 'PlatformCard|PlatformContract|Platform' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/ -run 'TestPlatformCardsDoNotClaimUngovernedAbilityAsGoverned' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestPlatformCardsPromiseNoFutureWork' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ ./pkg/codex/ -run 'PlatformCard|PlatformContract|Platform' -count=1 -timeout 90m` exits 0
    - `go build ./cmd/aether` exits 0
    - Every sentence on every card is understandable without opening a file
  </acceptance_criteria>
  <done>The three cards describe only what happened, credit the program only for what its own records show, and read plainly.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing):

- Go identifiers in `cmd/platform_honesty_card_test.go`: `platformHonestyCard`, `platformHonestyCardPath`, `platformRunTranscriptPath`, `loadPlatformHonestyCard`, `validatePlatformHonestyCard`
- Tests: `TestOpenCodeCardClaimsResolveToTheCapturedRun`, `TestCodexCardClaimsResolveToTheCapturedRun`, `TestClaudeCardClaimsResolveToTheCapturedRun`, `TestEveryContractedPlatformHasACardAndARun`, `TestPlatformCardsDoNotClaimUngovernedAbilityAsGoverned`, `TestPlatformCardsPromiseNoFutureWork`
- New files: `205-PLATFORM-CARD-CLAUDE.md`, `205-PLATFORM-CARD-OPENCODE.md`, `205-PLATFORM-CARD-CODEX.md`, and the directory `evidence/platform-runs/` containing `claude-run.txt`, `opencode-run.txt`, `codex-run.txt`
- Any corrected capability level or limitation text inside the existing `PlatformContractFor` entries

These artifacts are the proofs plan 205-11 will cite when it signs capability row CAP-054.

## Flagged planner assumptions

- **Unresolved probe row, PROOF-04 (unclassified).** The edge probe could not classify what boundary, adjacency, ordering or precision question PROOF-04 turns on, and it is deliberately left unresolved rather than filled with a fallback criterion. The open question it stands for is: what counts as a platform outcome being "demonstrated" versus "failing early with a truthful limitation" when an entry point cannot be driven non-interactively at all. Task 1 handles that case explicitly — capture the refusal as the result — but this is a planner judgement, not a derived criterion, and the verifier should treat PROOF-04's completeness as needing a human read rather than a silent pass.

<verification>
- Three captured transcripts exist, each with version, command line, working directory and exit status
- Every card claim resolves to a verbatim line in its capture
- Every contradiction between a real run and the versioned support contract was resolved in favour of the run
</verification>

<success_criteria>
- Each platform's declared outcome is demonstrated by a real public entry point or replaced by a truthful limitation drawn from a captured refusal
- No card claims an ability the program's own records do not show it governing
- No engineering effort was spent making the two secondary platforms work
</success_criteria>

<output>
Create `.planning/phases/205-owner-acceptance-and-restoration-seal/205-10-SUMMARY.md` when done
</output>
