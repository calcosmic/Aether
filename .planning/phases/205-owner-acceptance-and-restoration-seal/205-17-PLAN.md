---
phase: 205-owner-acceptance-and-restoration-seal
plan: 17
type: execute
wave: 9
depends_on: ["205-16"]
files_modified:
  - .planning/phases/205-owner-acceptance-and-restoration-seal/205-LIMITATIONS-CARD.md
  - cmd/limitations_card_test.go
autonomous: true
requirements: [PROOF-06]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The card lists every open entry in the defect register, one line each, in plain words — not a filtered subset and not a count."
    - "A named check fails when the set of entries on the card differs from the set of entries the defect register currently reports as open."
    - "The card carries a named line for the openly recorded gap that nothing proves who is calling."
    - "The card does not carry the hard-coded probe timeout note, and the reason it does not — the check that retired it — is cited on the card itself."
    - "The card carries one line for every piece of work recorded as not exercised, using the exact agreed wording rather than a tick."
    - "The card carries the elapsed time and the cost of every one of the ten pieces of work, as a measurement."
    - "The card carries every item the gap triage routed to the limitations list."
    - "The card lives in the planning records and does not appear in the release notes or the changelog."
    - "Every line on the card is understandable by someone who has never opened this repository, with every invented word explained where it appears."
  artifacts:
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-LIMITATIONS-CARD.md
    - cmd/limitations_card_test.go
  key_links:
    - "The defect register is read live at the time the card is written, never from a cached count."
    - "The not-exercised lines come verbatim from the verdict file, and the timings from the read-back record."
  prohibitions:
    - statement: "The limitations list must never be shrunk, filtered, summarised into a count, or softened to make acceptance more likely."
      status: unverified
      flagged: true
    - statement: "A residual limitation or a piece of deferred proof must never be converted into a green metric or a tick."
      status: unverified
      flagged: true
---

<objective>
Write the one page the owner reads before he decides: everything still wrong, everything still unproven, and what the day cost.

Purpose: the acceptance is only meaningful if what it accepts is fully visible. The owner has ruled the card lists every open defect-register entry in plain words, plus the folded notes, plus every piece of work not exercised — not a filtered subset, not a count.

Output: the limitations card, and a check that fails if the card and the live defect register disagree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md
@.planning/WINDOWS.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-UAT.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-EVIDENCE-READBACK.md
@.planning/phases/205-owner-acceptance-and-restoration-seal/205-GAP-TRIAGE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: One open defect, translated into plain words, held to the live register by a check</name>
  <files>cmd/limitations_card_test.go, .planning/phases/205-owner-acceptance-and-restoration-seal/205-LIMITATIONS-CARD.md</files>
  <read_first>
    - .planning/WINDOWS.md in full — the header counts and the table; note that the description column contains embedded pipe characters, so it must be parsed with a real table parser rather than shell text tools
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md, decisions D-15, D-16, D-20, D-21 and D-22
    - CLAUDE.md — the communication style block and the table of invented words needing inline translation
    - cmd/classic_synthesis_audit_test.go — the planning-document parsing style already established in this phase, to follow rather than duplicate
  </read_first>
  <action>
Create `cmd/limitations_card_test.go`.

Create `loadOpenDefectRegisterEntries(root string) ([]defectRegisterEntry, error)` — parses the defect register's table, returning the identifier, the phase, the file and the description of every entry whose status is open. Parse the table field by field in Go; the description column contains pipe characters and naive splitting will corrupt it.

Create `loadLimitationsCardEntries(path string) ([]limitationsCardEntry, error)` — parses the card's defect section into entries, each carrying the identifier it refers to and its plain-words line.

Create `validateLimitationsCardCoversEveryOpenEntry(card, register) error` — the set of identifiers on the card must equal the set the register reports open. A register entry missing from the card fails naming the identifier; a card entry for an identifier the register does not report open fails naming it too.

Write `TestLimitationsCardCoversEveryOpenDefect` first and watch it fail with no card present. Then create `.planning/phases/205-owner-acceptance-and-restoration-seal/205-LIMITATIONS-CARD.md` with its opening paragraph and the first open entry translated into one plain sentence, and make the check pass for that entry only by scoping the first run to it — then remove the scoping so the real check covers the whole set, which Task 2 completes.

The translation is the work. Each entry's register description is written for an engineer; the card's line is written for someone who has never opened this repository. Say what is wrong and what it means for him, not which function contains it. Keep the identifier on the line so the two can be matched, but never let the identifier stand in place of the sentence.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestLimitationsCardCoversEveryOpenDefect' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `cmd/limitations_card_test.go` exists and parses the defect register's table field by field in Go rather than with shell text tools
    - `.planning/phases/205-owner-acceptance-and-restoration-seal/205-LIMITATIONS-CARD.md` exists with an opening paragraph and at least the first translated entry
    - The summary records `TestLimitationsCardCoversEveryOpenDefect` failing before the card existed
    - The first translated line says what is wrong in ordinary words and does not consist of the identifier alone
    - A register entry deliberately omitted from a temporary copy of the card causes the check to fail naming its identifier
  </acceptance_criteria>
  <done>One open defect is on the card in plain words and a check ties the card's contents to the live register.</done>
</task>

<task type="auto">
  <name>Task 2: The whole card — every open defect, the folded notes, the unexercised work, the measurements</name>
  <files>.planning/phases/205-owner-acceptance-and-restoration-seal/205-LIMITATIONS-CARD.md, cmd/limitations_card_test.go</files>
  <read_first>
    - The full live defect register
    - .planning/todos/2026-09-13-nothing-proves-who-is-calling.md — the openly recorded gap that must appear as a named line
    - .planning/todos/2026-08-01-ts-host-preflight-hardcoded-timeout.md and `.planning/phases/203-biological-runtime/203-CLASSIC-SYNTHESIS.md` — the evidence that this note was retired, and the name of the check that retired it
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-UAT.md — the not-exercised wording, verbatim
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-EVIDENCE-READBACK.md — the elapsed time and cost per piece of work
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-GAP-TRIAGE.md — everything routed to the limitations list
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-SYNTH-07-AUDIT.md and 205-PARITY.md — the shortfalls those two records routed to the card
  </read_first>
  <action>
Complete the card in five sections, each opening with one sentence saying what the section is:

**What is still wrong.** Every open entry in the live defect register, one plain sentence each with its identifier, in register order. Not a subset. Not a count. Include the two things Phase 204 left deliberately unfinished with the owner's agreement — the remaining unguarded regression fixtures and the missing cost and token capture on the checking run — as their own lines, because an agreed deferral is still a limitation.

**What nothing yet proves.** A named line for the openly recorded gap that the depth check trusts a caller's own claim about who it is, with no way yet to prove it. Say plainly what that means: the program takes a caller's word for its identity in one place. Then any item from the synthesis audit or the parity record recorded as not evidenced, each with its reason.

**What was not tried.** One line per piece of work recorded as not exercised, using the exact wording from the verdict file — including, if it applies, that the undo path was not exercised by the owner and is proven only by the program's own tests. Never a tick and never a pass.

**What it cost and how long it took.** All ten pieces of work with their elapsed time and cost, taken from the read-back record. This is a measurement, not a verdict — the three that were also judged on time say so beside their figure.

**What this phase decided not to fix.** Everything the gap triage routed here, in plain words, with the reason.

Do not include the hard-coded probe timeout note. It was retired by the biological-runtime work, and the card must say so in one line naming the check that retired it — so a future reader does not re-open a closed item.

Extend the check with `TestLimitationsCardCarriesTheFoldedNotes` (the named caller-identity line is present; the retired probe-timeout note is absent and its retiring check is cited), `TestLimitationsCardCarriesEveryUnexercisedItem` (every not-exercised line from the verdict file appears verbatim), and `TestLimitationsCardCarriesEveryJourneyMeasurement` (ten pieces of work, each with an elapsed time and a cost).
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestLimitationsCard' -count=1 -timeout 90m</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/ -run 'TestLimitationsCardCoversEveryOpenDefect' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestLimitationsCardCarriesTheFoldedNotes' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestLimitationsCardCarriesEveryUnexercisedItem' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestLimitationsCardCarriesEveryJourneyMeasurement' -count=1 -timeout 90m` exits 0
    - The card has all five sections, each with its opening sentence
    - The card's defect section contains one line per open register entry, with no entry omitted and no count standing in for the list
    - The card names the check that retired the probe-timeout note and does not list that note as open
    - Every not-exercised line matches the verdict file verbatim
  </acceptance_criteria>
  <done>Everything still wrong, still unproven, or never tried is on one page in the owner's language, alongside what the day cost.</done>
</task>

<task type="auto">
  <name>Task 3: Plain English, correct placement, nothing turned into a green tick</name>
  <files>.planning/phases/205-owner-acceptance-and-restoration-seal/205-LIMITATIONS-CARD.md, cmd/limitations_card_test.go</files>
  <read_first>
    - The completed card
    - CLAUDE.md — the communication style block and the invented-word table
    - CHANGELOG.md and any release-notes file — to confirm the card is absent from both
    - .planning/phases/205-owner-acceptance-and-restoration-seal/205-CONTEXT.md, decision D-16
  </read_first>
  <action>
Read every line of the card as someone who has never opened this repository. Any line needing a file path, an internal code or an unexplained invented word to make sense is rewritten. Record the before and after of each rewrite.

Check that no residual limitation and no piece of deferred proof has been turned into a tick, a pass, a percentage or a score. The card reports lists and measurements; it reports no aggregate.

Check placement: the card is in this phase's planning directory and appears in neither the changelog nor any release-notes file. The owner has ruled it stays in the planning records.

Add `TestLimitationsCardStaysInThePlanningRecords` asserting the card's own text does not appear in the changelog, and `TestLimitationsCardCarriesNoGreenMetric` asserting the card contains no percentage and no aggregate score.

Record in the summary the final count of open defect entries listed, the number of unexercised items, and a plain statement of the single biggest thing still wrong — the one a reader should take away first.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestLimitationsCard' -count=1 -timeout 90m && test -s .planning/phases/205-owner-acceptance-and-restoration-seal/205-LIMITATIONS-CARD.md</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/ -run 'TestLimitationsCardStaysInThePlanningRecords' -count=1 -timeout 90m` exits 0
    - `go test ./cmd/ -run 'TestLimitationsCardCarriesNoGreenMetric' -count=1 -timeout 90m` exits 0
    - The card contains no percent sign and no aggregate score
    - Every invented word on the card is explained where it appears
    - The summary states the count of open defect entries listed, the number of unexercised items, and the single biggest outstanding problem in one sentence
  </acceptance_criteria>
  <done>The card is honest, complete, readable without opening a file, and in the one place the owner said it belongs.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing):

- New file `.planning/phases/205-owner-acceptance-and-restoration-seal/205-LIMITATIONS-CARD.md`
- New file `cmd/limitations_card_test.go` with `loadOpenDefectRegisterEntries`, `loadLimitationsCardEntries`, `validateLimitationsCardCoversEveryOpenEntry`, `defectRegisterEntry`, `limitationsCardEntry`, and the tests `TestLimitationsCardCoversEveryOpenDefect`, `TestLimitationsCardCarriesTheFoldedNotes`, `TestLimitationsCardCarriesEveryUnexercisedItem`, `TestLimitationsCardCarriesEveryJourneyMeasurement`, `TestLimitationsCardStaysInThePlanningRecords`, `TestLimitationsCardCarriesNoGreenMetric`

## Flagged planner assumptions

- The defect register reported thirty-four open entries on 2026-09-15, but the file is actively updated and the gap triage may have added more. The card is built from a live read at the time it is written, and the check ties it to the register's current state rather than to any recorded number.
- The card is a planning document the owner reads, not a screen the program draws, so it is not registered into the program's own screen-voice checks. If a later decision renders it through the program instead, those checks become mandatory.

<verification>
- The card's defect section exactly matches the live register's open set, proven by a check that fails on either a missing or an extra entry
- The folded notes are handled as decided: one named line, one omission with its retiring check cited, and one measurement
- Nothing on the card is a tick, a percentage or a score
</verification>

<success_criteria>
- The owner can read one page and know everything that is still wrong, still unproven, never tried, and what the day cost
- Nothing was filtered out to make the answer easier
</success_criteria>

<output>
Create `.planning/phases/205-owner-acceptance-and-restoration-seal/205-17-SUMMARY.md` when done
</output>
