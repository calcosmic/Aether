---
phase: 11-visual-identity
plan: 04
type: execute
wave: 4
depends_on:
  - "11-03"
files_modified:
  - .claude/commands/ant/phase.md
  - .claude/commands/ant/oracle.md
  - .claude/commands/ant/watch.md
  - .claude/commands/ant/swarm.md
  - .claude/commands/ant/colonize.md
  - .claude/commands/ant/chaos.md
  - .claude/commands/ant/archaeology.md
  - .claude/commands/ant/dream.md
  - .claude/commands/ant/flags.md
  - .claude/commands/ant/flag.md
  - .claude/commands/ant/focus.md
  - .claude/commands/ant/redirect.md
  - .claude/commands/ant/feedback.md
  - .claude/commands/ant/help.md
  - .claude/commands/ant/history.md
  - .claude/commands/ant/migrate-state.md
  - .claude/commands/ant/verify-castes.md
  - .claude/commands/ant/maturity.md
  - .claude/commands/ant/organize.md
  - .claude/commands/ant/interpret.md
  - .claude/commands/ant/resume.md
  - .claude/commands/ant/plan.md
  - .claude/commands/ant/update.md
  - .claude/commands/ant/council.md
  - .claude/commands/ant/pause-colony.md
  - .claude/commands/ant/resume-colony.md
  - .claude/commands/ant/lay-eggs.md
  - .claude/commands/ant/tunnels.md
autonomous: true
requirements:
  - VIS-04
  - VIS-02

must_haves:
  truths:
    - "Every command uses the same banner format regardless of complexity"
    - "Every command completion shows a Next Up block"
    - "Users can identify Aether output at a glance"
  artifacts:
    - path: ".claude/commands/ant/*.md"
      provides: "All 34 commands with consistent visual identity"
      contains: "Next Up"
  key_links:
    - from: "all commands"
      to: "standard format"
      via: "shared pattern"
      pattern: "━━━━"
---

<objective>
Apply the standardized visual language to the remaining 28 commands, ensuring every command has consistent banners and Next Up blocks.

Purpose: Complete the visual identity system so the entire Aether experience feels cohesive.
Output: 28 updated command files with standardized banners and Next Up blocks.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-visual-identity/11-CONTEXT.md
@.planning/phases/11-visual-identity/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update medium-complexity commands (phase, oracle, watch, swarm, colonize)</name>
  <files>.claude/commands/ant/phase.md, .claude/commands/ant/oracle.md, .claude/commands/ant/watch.md, .claude/commands/ant/swarm.md, .claude/commands/ant/colonize.md</files>
  <action>
Update these 5 commands with standardized visual elements.

For each file:

1. **Standardize any existing banners:**
   - Replace `═══` or `===` or `***` with `━━━━` (U+2501 heavy horizontal)
   - Standard banner format:
     ```
     ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        C O M M A N D   N A M E
     ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
     ```

2. **Add Next Up block at command completion:**
   ```
   ──────────────────────────────────────────────────
   🐜 Next Up
   ──────────────────────────────────────────────────
      /ant:status             📊 Check colony state
      /ant:build              🔨 Continue building
   ```

3. **Command-specific Next Up recommendations:**
   - **phase.md**: `/ant:build {N}`, `/ant:status`
   - **oracle.md**: `/ant:status`, `/ant:focus`
   - **watch.md**: `/ant:status`, `/ant:continue`
   - **swarm.md**: `/ant:flags`, `/ant:status`
   - **colonize.md**: `/ant:status`, `/ant:plan`

4. **Ensure caste emoji format** for any worker mentions: `{emoji} {name}`
  </action>
  <verify>
    # Verify all 5 files have Next Up blocks
    for f in phase oracle watch swarm colonize; do
      grep -q "Next Up" ".claude/commands/ant/${f}.md" || echo "FAIL: ${f}.md missing Next Up"
    done && \
    echo "PASS: All medium-complexity commands have Next Up blocks"
  </verify>
  <done>All 5 files have standardized banners and Next Up blocks</done>
</task>

<task type="auto">
  <name>Task 2: Update chaos, archaeology, dream, flags, flag commands</name>
  <files>.claude/commands/ant/chaos.md, .claude/commands/ant/archaeology.md, .claude/commands/ant/dream.md, .claude/commands/ant/flags.md, .claude/commands/ant/flag.md</files>
  <action>
Update these 5 commands with standardized visual elements.

For each file, apply the same pattern as Task 1:

1. **Standardize banners** to `━━━━` format
2. **Add Next Up block** at command completion
3. **Command-specific Next Up recommendations:**
   - **chaos.md**: `/ant:status`, `/ant:flags`
   - **archaeology.md**: `/ant:status`, `/ant:build`
   - **dream.md**: `/ant:status`, `/ant:oracle`
   - **flags.md**: `/ant:flag --add`, `/ant:swarm`
   - **flag.md**: `/ant:flags`, `/ant:status`

4. **Ensure caste emoji format** for worker mentions
  </action>
  <verify>
    # Verify all 5 files have Next Up blocks
    for f in chaos archaeology dream flags flag; do
      grep -q "Next Up" ".claude/commands/ant/${f}.md" || echo "FAIL: ${f}.md missing Next Up"
    done && \
    echo "PASS: All special worker commands have Next Up blocks"
  </verify>
  <done>All 5 files have standardized banners and Next Up blocks</done>
</task>

<task type="auto">
  <name>Task 3: Update pheromone commands (focus, redirect, feedback)</name>
  <files>.claude/commands/ant/focus.md, .claude/commands/ant/redirect.md, .claude/commands/ant/feedback.md</files>
  <action>
Update the pheromone signal commands with standardized visual elements.

These commands are simple but should still have consistent formatting:

1. **Standardize any output banners** to `━━━━` format
2. **Add Next Up block** with pheromone-specific recommendations:
   - **focus.md**: `/ant:status`, `/ant:build`
   - **redirect.md**: `/ant:status`, `/ant:focus`
   - **feedback.md**: `/ant:status`, `/ant:build`

3. **Pheromone commands are informational** - their Next Up should guide users back to active work or status checks.
  </action>
  <verify>
    # Verify all 3 pheromone files have Next Up blocks
    for f in focus redirect feedback; do
      grep -q "Next Up" ".claude/commands/ant/${f}.md" || echo "FAIL: ${f}.md missing Next Up"
    done && \
    echo "PASS: All pheromone commands have Next Up blocks"
  </verify>
  <done>All 3 pheromone files have standardized banners and Next Up blocks</done>
</task>

<task type="auto">
  <name>Task 4: Update informational commands (help, history, migrate-state, verify-castes, maturity, organize, interpret)</name>
  <files>.claude/commands/ant/help.md, .claude/commands/ant/history.md, .claude/commands/ant/migrate-state.md, .claude/commands/ant/verify-castes.md, .claude/commands/ant/maturity.md, .claude/commands/ant/organize.md, .claude/commands/ant/interpret.md</files>
  <action>
Update informational/utility commands with standardized visual elements.

These commands are primarily read-only or utility functions:

1. **Standardize any output banners** to `━━━━` format
2. **Add Next Up block** guiding users to active work:
   - **help.md**: `/ant:status`, `/ant:init`
   - **history.md**: `/ant:status`, `/ant:archaeology`
   - **migrate-state.md**: `/ant:status`, `/ant:init`
   - **verify-castes.md**: `/ant:status`, `/ant:build`
   - **maturity.md**: `/ant:status`, `/ant:seal`
   - **organize.md**: `/ant:status`, `/ant:plan`
   - **interpret.md**: `/ant:status`, `/ant:oracle`

3. **Informational commands** should always point users to `/ant:status` as a safe next step.
  </action>
  <verify>
    # Verify all 7 informational files have Next Up blocks
    for f in help history migrate-state verify-castes maturity organize interpret; do
      grep -q "Next Up" ".claude/commands/ant/${f}.md" || echo "FAIL: ${f}.md missing Next Up"
    done && \
    echo "PASS: All informational commands have Next Up blocks"
  </verify>
  <done>All 7 informational files have standardized banners and Next Up blocks</done>
</task>

<task type="auto">
  <name>Task 5: Update remaining commands (resume, plan, update, council, pause-colony, resume-colony, lay-eggs, tunnels)</name>
  <files>.claude/commands/ant/resume.md, .claude/commands/ant/plan.md, .claude/commands/ant/update.md, .claude/commands/ant/council.md, .claude/commands/ant/pause-colony.md, .claude/commands/ant/resume-colony.md, .claude/commands/ant/lay-eggs.md, .claude/commands/ant/tunnels.md</files>
  <action>
Update the final 8 commands with standardized visual elements.

1. **Standardize any output banners** to `━━━━` format
2. **Add Next Up block** with context-appropriate recommendations:
   - **resume.md**: `/ant:status`, `/ant:continue`
   - **plan.md**: `/ant:build 1`, `/ant:status`
   - **update.md**: `/ant:status`, `/ant:version`
   - **council.md**: `/ant:status`, `/ant:oracle`
   - **pause-colony.md**: `/ant:status`, `/ant:resume-colony`
   - **resume-colony.md**: `/ant:status`, `/ant:continue`
   - **lay-eggs.md**: `/ant:init`, `/ant:plan`
   - **tunnels.md**: `/ant:entomb`, `/ant:status`

3. **Ensure consistent formatting** across all commands.
  </action>
  <verify>
    # Verify all 8 remaining files have Next Up blocks
    for f in resume plan update council pause-colony resume-colony lay-eggs tunnels; do
      grep -q "Next Up" ".claude/commands/ant/${f}.md" || echo "FAIL: ${f}.md missing Next Up"
    done && \
    echo "PASS: All remaining commands have Next Up blocks"
  </verify>
  <done>All 8 remaining files have standardized banners and Next Up blocks</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Count Next Up blocks: `grep -l "Next Up" .claude/commands/ant/*.md | wc -l` should be 34
2. Verify no old banner formats remain: `grep -l "═══" .claude/commands/ant/*.md` should be empty
3. Run `npm run lint:sync` to verify no sync issues
4. Spot-check 3-4 files for consistent format
</verification>

<success_criteria>
- All 34 commands have Next Up blocks
- All commands use standardized `━━━━` banner format
- No old banner formats (`═══`, `***`, mixed styles) remain
- `npm run lint:sync` passes
</success_criteria>

<output>
After completion, create `.planning/phases/11-visual-identity/11-04-SUMMARY.md`
</output>
