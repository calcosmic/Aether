---
phase: 154-colony-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - control-ts/src/schemas/agent.schema.ts
  - control-ts/tests/fixtures/agents/queen.yaml
  - control-ts/tests/fixtures/agents/builder.yaml
  - colony/agents/queen.yaml
  - colony/agents/builder.yaml
  - colony/agents/watcher.yaml
  - colony/agents/scout.yaml
  - colony/agents/oracle.yaml
  - colony/agents/gatekeeper.yaml
  - colony/agents/auditor.yaml
  - colony/agents/probe.yaml
  - colony/agents/architect.yaml
  - colony/agents/route-setter.yaml
  - colony/agents/surveyor-nest.yaml
  - colony/agents/surveyor-disciplines.yaml
  - colony/agents/surveyor-pathogens.yaml
  - colony/agents/surveyor-provisions.yaml
  - colony/agents/keeper.yaml
  - colony/agents/tracker.yaml
  - colony/agents/weaver.yaml
  - colony/agents/fixer.yaml
  - colony/agents/medic.yaml
  - colony/agents/porter.yaml
  - colony/agents/ambassador.yaml
  - colony/agents/chronicler.yaml
  - colony/agents/measurer.yaml
  - colony/agents/includer.yaml
  - colony/agents/sage.yaml
  - colony/agents/chaos.yaml
  - colony/agents/archaeologist.yaml
  - colony/prompts/queen.md
  - colony/prompts/builder.md
  - colony/prompts/watcher.md
  - colony/prompts/scout.md
  - colony/prompts/oracle.md
  - colony/prompts/gatekeeper.md
  - colony/prompts/auditor.md
  - colony/prompts/probe.md
  - colony/prompts/architect.md
  - colony/prompts/route-setter.md
  - colony/prompts/surveyor-nest.md
  - colony/prompts/surveyor-disciplines.md
  - colony/prompts/surveyor-pathogens.md
  - colony/prompts/surveyor-provisions.md
  - colony/prompts/keeper.md
  - colony/prompts/tracker.md
  - colony/prompts/weaver.md
  - colony/prompts/fixer.md
  - colony/prompts/medic.md
  - colony/prompts/porter.md
  - colony/prompts/ambassador.md
  - colony/prompts/chronicler.md
  - colony/prompts/measurer.md
  - colony/prompts/includer.md
  - colony/prompts/sage.md
  - colony/prompts/chaos.md
  - colony/prompts/archaeologist.md
autonomous: true
requirements:
  - EXTRACT-01
  - EXTRACT-02

must_haves:
  truths:
    - "User can run `cat colony/agents/builder.yaml` and see a complete agent definition"
    - "User can run `cat colony/prompts/builder.md` and see the full Builder prompt text"
    - "All 27 agent YAML files exist under `colony/agents/`"
    - "All 27 prompt Markdown files exist under `colony/prompts/`"
    - "AgentSchema validates all 27 roles without error"
    - "Schema tests pass for both existing and new agent fixtures"
  artifacts:
    - path: "control-ts/src/schemas/agent.schema.ts"
      provides: "Agent validation with 27-role enum"
      contains: "architect", "route-setter", "surveyor-nest"
    - path: "colony/agents/queen.yaml"
      provides: "Queen agent definition"
      contains: "id: queen"
    - path: "colony/prompts/queen.md"
      provides: "Queen prompt text"
      contains: "<role>"
    - path: "colony/agents/builder.yaml"
      provides: "Builder agent definition"
      contains: "id: builder"
    - path: "colony/prompts/builder.md"
      provides: "Builder prompt text"
      contains: "TDD Workflow"
  key_links:
    - from: "colony/agents/builder.yaml"
      to: "colony/prompts/builder.md"
      via: "prompt_file field"
      pattern: "prompt_file: colony/prompts/builder.md"
    - from: "AgentSchema"
      to: "colony/agents/*.yaml"
      via: "role enum validation"
      pattern: "z.enum\\([.*builder.*watcher.*]\\)"
---

<objective>
Create all 27 agent YAML definitions under `colony/agents/` and their corresponding prompt Markdown files under `colony/prompts/`, expanding the AgentSchema role enum from 8 to 27 castes.

Purpose: This is the core content-creation task for EXTRACT-01 and EXTRACT-02 — moving all agent identities and prompt text from platform-specific files and Go hardcoding into editable colony assets.
Output: 27 `colony/agents/*.yaml` files, 27 `colony/prompts/*.md` files, updated AgentSchema, updated fixtures, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/154-colony-assets/154-CONTEXT.md
@.planning/phases/154-colony-assets/154-RESEARCH.md

@control-ts/src/schemas/agent.schema.ts
@control-ts/tests/fixtures/agents/queen.yaml
@control-ts/tests/fixtures/agents/builder.yaml
@control-ts/tests/helpers.ts
@control-ts/src/utils/projectRoot.ts

<!-- Source material for extraction -->
@.claude/agents/ant/aether-builder.md
@.claude/agents/ant/aether-queen.md
@.claude/agents/ant/aether-scout.md
@.claude/agents/ant/aether-watcher.md
@.claude/agents/ant/aether-oracle.md
@.claude/agents/ant/aether-gatekeeper.md
@.claude/agents/ant/aether-auditor.md
@.claude/agents/ant/aether-probe.md

<!-- Key interfaces -->
<interfaces>
From control-ts/src/schemas/agent.schema.ts:
```typescript
export const AgentSchema = z
  .object({
    id: z.string().min(1),
    role: z.enum(["builder", "watcher", "scout", "queen", "oracle", "gatekeeper", "auditor", "probe"]),
    prompt_file: z.string().min(1),
    allowed_tools: z.array(z.string()).default([]),
  })
  .superRefine((data, ctx) => { /* path traversal + file existence check */ });
```

From control-ts/src/utils/projectRoot.ts:
```typescript
export const projectRoot = resolve(dirname(fileURLToPath(import.meta.url)), "../..");
```

Fixture pattern (queen.yaml):
```yaml
id: "queen"
role: "queen"
prompt_file: "colony/prompts/queen.md"
allowed_tools:
  - "spawn"
  - "delegate"
  - "signal"
```

Agent frontmatter pattern (from .claude/agents/ant/*.md):
```yaml
---
name: aether-builder
description: "Use this agent when..."
tools: Read, Write, Edit, Bash, Grep, Glob
color: yellow
model: sonnet
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand AgentSchema to 27 roles and create all agent YAMLs</name>
  <files>
    control-ts/src/schemas/agent.schema.ts
    colony/agents/queen.yaml
    colony/agents/builder.yaml
    colony/agents/watcher.yaml
    colony/agents/scout.yaml
    colony/agents/oracle.yaml
    colony/agents/gatekeeper.yaml
    colony/agents/auditor.yaml
    colony/agents/probe.yaml
    colony/agents/architect.yaml
    colony/agents/route-setter.yaml
    colony/agents/surveyor-nest.yaml
    colony/agents/surveyor-disciplines.yaml
    colony/agents/surveyor-pathogens.yaml
    colony/agents/surveyor-provisions.yaml
    colony/agents/keeper.yaml
    colony/agents/tracker.yaml
    colony/agents/weaver.yaml
    colony/agents/fixer.yaml
    colony/agents/medic.yaml
    colony/agents/porter.yaml
    colony/agents/ambassador.yaml
    colony/agents/chronicler.yaml
    colony/agents/measurer.yaml
    colony/agents/includer.yaml
    colony/agents/sage.yaml
    colony/agents/chaos.yaml
    colony/agents/archaeologist.yaml
  </files>
  <read_first>
    control-ts/src/schemas/agent.schema.ts
    control-ts/tests/fixtures/agents/queen.yaml
    control-ts/tests/fixtures/agents/builder.yaml
    .claude/agents/ant/aether-builder.md
    .claude/agents/ant/aether-queen.md
  </read_first>
  <action>
    1. Update `control-ts/src/schemas/agent.schema.ts` — expand the `role` z.enum from 8 castes to all 27 castes. The exact 27 roles are: builder, watcher, scout, queen, oracle, gatekeeper, auditor, probe, architect, route-setter, surveyor-nest, surveyor-disciplines, surveyor-pathogens, surveyor-provisions, keeper, tracker, weaver, fixer, medic, porter, ambassador, chronicler, measurer, includer, sage, chaos, archaeologist. Keep the existing superRefine file-existence and path-traversal checks unchanged.

    2. Create the `colony/agents/` directory if it does not exist.

    3. For each of the 27 castes, create a `{role}.yaml` file under `colony/agents/` with the following fields:
       - `id`: same as role name
       - `role`: same as role name
       - `prompt_file`: `"colony/prompts/{role}.md"`
       - `allowed_tools`: array of tool names derived from the corresponding `.claude/agents/ant/aether-{role}.md` frontmatter `tools` field. If an agent file does not exist or lacks a tools field, use a sensible default based on caste (builder/watcher/probe/get read/write/edit/bash/grep/glob; scout/oracle/archaeologist get read/grep/glob/websearch/webfetch; gatekeeper/auditor/measurer/includer get read/grep/glob/write; queen gets spawn/delegate/signal; all others get read/grep/glob).
       - `tier`: optional string, one of "core", "orchestration", "surveyor", "specialist", "niche" — derived from the caste groupings in CLAUDE.md "The 27 Agents" table.
       - `model`: optional string, derived from the agent file frontmatter `model` field if present, otherwise default to "sonnet".
       - `color`: optional string, derived from the agent file frontmatter `color` field if present.
       - `description`: optional string, derived from the agent file frontmatter `description` field if present.

    4. Also create or update the corresponding fixture files under `control-ts/tests/fixtures/agents/` for all 27 agents, using the same YAML content but with `prompt_file` values that resolve correctly from the test helper's `projectRoot` (which resolves to `control-ts/`). The fixture prompt_file paths should be `../../colony/prompts/{role}.md` so that the schema's `resolve(projectRoot, data.prompt_file)` finds the actual file at repo root.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/control-ts && npx vitest run tests/schemas/agent.schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `control-ts/src/schemas/agent.schema.ts` contains a z.enum with exactly 27 string literals including "architect", "route-setter", "surveyor-nest", "chaos", "archaeologist"
    - `ls colony/agents/*.yaml | wc -l` outputs 27
    - `colony/agents/builder.yaml` contains `id: builder`, `role: builder`, `prompt_file: colony/prompts/builder.md`
    - `colony/agents/queen.yaml` contains `allowed_tools` array with "spawn"
    - `control-ts/tests/fixtures/agents/builder.yaml` contains `prompt_file: "../../colony/prompts/builder.md"`
    - `npx vitest run tests/schemas/agent.schema.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>AgentSchema validates 27 roles, all 27 agent YAML files exist with correct fields, schema tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extract prompt Markdown for all 27 agents</name>
  <files>
    colony/prompts/queen.md
    colony/prompts/builder.md
    colony/prompts/watcher.md
    colony/prompts/scout.md
    colony/prompts/oracle.md
    colony/prompts/gatekeeper.md
    colony/prompts/auditor.md
    colony/prompts/probe.md
    colony/prompts/architect.md
    colony/prompts/route-setter.md
    colony/prompts/surveyor-nest.md
    colony/prompts/surveyor-disciplines.md
    colony/prompts/surveyor-pathogens.md
    colony/prompts/surveyor-provisions.md
    colony/prompts/keeper.md
    colony/prompts/tracker.md
    colony/prompts/weaver.md
    colony/prompts/fixer.md
    colony/prompts/medic.md
    colony/prompts/porter.md
    colony/prompts/ambassador.md
    colony/prompts/chronicler.md
    colony/prompts/measurer.md
    colony/prompts/includer.md
    colony/prompts/sage.md
    colony/prompts/chaos.md
    colony/prompts/archaeologist.md
  </files>
  <read_first>
    .claude/agents/ant/aether-builder.md
    .claude/agents/ant/aether-queen.md
    .claude/agents/ant/aether-scout.md
    .claude/agents/ant/aether-watcher.md
    .claude/agents/ant/aether-oracle.md
    .claude/agents/ant/aether-gatekeeper.md
    .claude/agents/ant/aether-auditor.md
    .claude/agents/ant/aether-probe.md
  </read_first>
  <action>
    1. Create the `colony/prompts/` directory if it does not exist.

    2. For each of the 27 castes, read the corresponding `.claude/agents/ant/aether-{role}.md` file (or `.claude/agents/ant/aether-{role}.md` with hyphens for multi-word roles like route-setter, surveyor-nest, etc.). Extract the Markdown body — everything AFTER the second `---` frontmatter delimiter. Strip any leading blank lines.

    3. Write the extracted body to `colony/prompts/{role}.md` as a standalone Markdown file. Do NOT include YAML frontmatter in the prompt file. The file should start directly with the `<role>` tag or first paragraph from the source.

    4. If a source agent file does not exist (should not happen for the 27 defined castes), create a minimal prompt file containing at least a `<role>` section with a brief role description based on the caste name and the CLAUDE.md agent table.

    5. After creating all prompt files, verify that the schema's file-existence check passes by running the agent schema tests. The schema resolves `colony/prompts/{role}.md` relative to `control-ts/src/utils/projectRoot.ts` which points to the repo root (two levels above `src/schemas/`), so files at repo-root `colony/prompts/` will be found.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/control-ts && npx vitest run tests/schemas/agent.schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ls colony/prompts/*.md | wc -l` outputs 27
    - `colony/prompts/builder.md` contains "TDD Workflow" (extracted from source)
    - `colony/prompts/queen.md` contains "Queen Ant" (extracted from source)
    - `colony/prompts/builder.md` does NOT contain YAML frontmatter (no `---` at start)
    - `npx vitest run tests/schemas/agent.schema.test.ts` exits 0 with all tests passing (including file-existence checks)
  </acceptance_criteria>
  <done>All 27 prompt Markdown files exist under colony/prompts/ with content extracted from source agent definitions, schema tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> schema validation | YAML files from disk are parsed and validated by Zod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-01 | Tampering | prompt_file path | mitigate | Existing superRefine rejects `..` segments; plan preserves this behavior |
| T-154-02 | Information Disclosure | Schema error messages | accept | Zod errors may include file paths in test/development context; acceptable for local-only usage |
| T-154-03 | Denial of Service | Malformed YAML | mitigate | `yaml` package parse wrapped in try/catch by caller (existing pattern) |
</threat_model>

<verification>
- Run `cd control-ts && npx vitest run tests/schemas/agent.schema.test.ts` — must pass
- Run `ls colony/agents/*.yaml | wc -l` — must equal 27
- Run `ls colony/prompts/*.md | wc -l` — must equal 27
- Spot-check `colony/agents/builder.yaml` and `colony/prompts/builder.md` for correct content
</verification>

<success_criteria>
- 27 agent YAML files exist under `colony/agents/`
- 27 prompt Markdown files exist under `colony/prompts/`
- AgentSchema role enum contains all 27 castes
- Schema validation tests pass for all fixtures
- No prompt text lives only in compiled Go or platform-specific files (extracted to colony/)
</success_criteria>

<output>
After completion, create `.planning/phases/154-colony-assets/154-01-SUMMARY.md`
</output>
