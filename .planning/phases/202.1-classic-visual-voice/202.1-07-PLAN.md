---
phase: 202.1-classic-visual-voice
plan: "07"
type: execute
wave: 4
depends_on: ["202.1-01", "202.1-02", "202.1-03", "202.1-04", "202.1-05", "202.1-06"]
files_modified:
  - cmd/classic_voice_coverage_test.go
  - cmd/classic_voice_corpus_test.go
  - cmd/lifecycle_status_render.go
  - cmd/discuss.go
  - cmd/spec_cmd.go
  - cmd/codex_visuals.go
  - cmd/planning_visuals.go
  - cmd/claudemd_classic_voice_test.go
  - CLAUDE.md
  - .planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md
autonomous: true
requirements: [CEC-09]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A named test fails if any one of the eight ordinary lifecycle screens is missing from the measured corpus, so a future screen cannot be added unmeasured and an existing one cannot be quietly dropped (criterion 5)."
    - "A named test measures every registered screen against the figure derived from the reference commit and fails naming the screen, the measured figure and the required one — it is a proportion check over rendered bytes, so renaming a renderer, a section or a heading does not change its verdict (criterion 5)."
    - "No registered screen's rendered output carries an internal state token or a key-equals-value bookkeeping pair where a sentence belongs, and the check is proven able to fail on planted input (criterion 4)."
    - "The synthesis protocol's artifact obligation is discharged in writing: a signed synthesis document carries the outcome under investigation, the research-to-plan linkage, the verification contract and the open decisions, cites RESEARCH.md for the reconstruction, audit, matrix and selected architecture it already contains, and records why the executable Classic corpus is not widened by this phase rather than leaving that silent."
    - "CLAUDE.md states what the restored voice guarantees and names the live test behind each claim, and a guard fails if a named test stops existing — matching how this repository already locks its other shipped claims."
    - "Every guardrail named in RESEARCH.md's do-not-regress table still passes, unmodified."
    - statement: "The corpus-wide density gate is not satisfiable by one decorative symbol in a long screen, because it measures a proportion of content lines rather than the presence of a symbol."
      verification: backstop
  artifacts:
    - path: "cmd/classic_voice_coverage_test.go"
      provides: "The eight-screen coverage ratchet and the corpus-wide density and token gates"
      contains: "TestEveryOrdinaryScreenIsMeasuredForVoice"
    - path: ".planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md"
      provides: "The signed synthesis artifact the milestone protocol requires for this phase"
      contains: "SYN-VOICE-05"
    - path: "CLAUDE.md"
      provides: "The shipped statement of what the restored voice guarantees, each claim naming its test"
      contains: "TestEveryVoicedScreenMeetsTheReferenceDensity"
  key_links:
    - from: "cmd/classic_voice_coverage_test.go"
      to: "cmd/classic_voice_corpus_test.go"
      via: "the ratchet walks the shared registry rather than naming renderers"
      pattern: "renderedVoiceScreens"
    - from: "CLAUDE.md"
      to: "cmd/classic_voice_coverage_test.go"
      via: "every voice claim in the shipped document names a live test"
      pattern: "TestEveryOrdinaryScreenIsMeasuredForVoice"
    - from: ".planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md"
      to: ".planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md"
      via: "the synthesis cites the research document for sections it already carries instead of duplicating them"
      pattern: "202.1-RESEARCH.md"
  prohibitions:
    - statement: "Never satisfy the coverage ratchet by removing a screen from the required list; the list is the requirement, not a convenience."
      status: flagged-unverified
    - statement: "Never add a per-screen exemption to the density gate or the token gate."
      status: flagged-unverified
    - statement: "Never write a CLAUDE.md sentence about runtime behaviour that no test can fail on."
      status: flagged-unverified
    - statement: "Never widen the executable Classic corpus schema from this phase; the next widening of that pattern belongs to the already-written Phase 203 plan that quotes its current value."
      status: flagged-unverified
---

<objective>
Close the phase with the checks that make the voice a guarantee rather than a description, and write down the decision record the milestone protocol requires.

Purpose: this phase exists because the voice was described in documentation and therefore drained away unnoticed. Six plans have restored it screen by screen, each with its own gate. What is still missing is the thing that makes it stick: one check that knows which screens are supposed to be measured and fails when one is absent, one check that measures all of them against the derived figure, and one check that no screen shows an internal token where a sentence belongs. RESEARCH.md's Pitfall 4 names the failure this closes by name — the project's own recurring pattern of building machinery and leaving part of it uncalled — and points out that a per-screen gate alone cannot catch a screen nobody registered.
Output: the coverage ratchet, the corpus-wide density and token gates, a signed synthesis document, and a CLAUDE.md section whose every claim names a live test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md
@.planning/research/v1.28-classic-synthesis-template.md
@.planning/phases/202.1-classic-visual-voice/202.1-01-SUMMARY.md
@.planning/phases/202.1-classic-visual-voice/202.1-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The coverage ratchet and the corpus-wide density gate</name>
  <files>cmd/classic_voice_coverage_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/classic_voice_corpus_test.go
    - cmd/classic_voice_metric_test.go
    - cmd/classic_voice_nextaction_test.go
    - cmd/classic_voice_status_test.go
    - cmd/classic_voice_discuss_spec_test.go
    - cmd/classic_voice_work_test.go
    - cmd/classic_voice_plan_test.go
    - .planning/ROADMAP.md (the five success criteria for this phase)
    - .planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md (Pitfall 4 and the density metric proposal)
  </read_first>
  <behavior>
    - The ratchet fails when any of the eight required screen families has no registration in the corpus, naming the missing family.
    - The ratchet fails when the corpus is empty, rather than passing on nothing.
    - The density gate measures every registered screen and reports, per failing screen, its name, its measured figure and the required figure.
    - The density gate reads the required figure from the reference derivation, never from a literal.
    - A planted registration whose rendering is flat fails the density gate.
    - Renaming a renderer function or a section heading does not change either verdict.
  </behavior>
  <action>Create `cmd/classic_voice_coverage_test.go`. Add `TestEveryOrdinaryScreenIsMeasuredForVoice`, which names the eight required screen families the roadmap's second criterion lists — plan, discuss, spec, status, build, continue, seal and what-next — and asserts each has at least one registration in the corpus, matched by a family prefix on the registered name so a plan may register several variants under one family. Fail naming the missing family and the registrations that were present. Fail when the corpus is empty. Do not name renderer functions in this test: it walks `renderedVoiceScreens` and the family list, which is what makes it survive a rename.

Add `TestEveryVoicedScreenMeetsTheReferenceDensity`, which renders every registration and asserts `voiceDensity` at or above `classicReferenceDensity(t)`, as a subtest per screen, reporting the screen name, the measured figure, the required figure and the rendered screen on failure. Add `TestCorpusDensityGateCanFail`, registering a deliberately flat synthetic screen through a local copy of the registry walk — not into the shared registry — and asserting the same comparison fails for it, so the gate is proven able to fail against the same code path the real screens take.

Record in the file's doc comment why this check counts a proportion of rendered content lines rather than looking for a symbol: a presence check is satisfied by one decoration in a fifty-line screen, which is exactly the regression this phase exists to catch, and a heading-name check is satisfied by renaming the heading.</action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestEveryOrdinaryScreenIsMeasuredForVoice|TestEveryVoicedScreenMeetsTheReferenceDensity|TestCorpusDensityGateCanFail' -count=1` passes.
    - The density test reports at least eighteen subtests, one per registration across the six earlier plans.
    - `TestEveryOrdinaryScreenIsMeasuredForVoice` names all eight families and fails when any is removed from the corpus, proven by a subtest that filters one family out of a copied registry and asserts the check reports it.
    - `cmd/classic_voice_coverage_test.go` contains no renderer function name and no decimal density literal.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestEveryOrdinaryScreenIsMeasuredForVoice|TestEveryVoicedScreenMeetsTheReferenceDensity|TestCorpusDensityGateCanFail' -count=1 -v 2>&amp;1 | grep -c '^ *--- PASS' | awk '$1 >= 18 {exit 0} {exit 1}'</automated>
  </verify>
  <done>The voice now has one check that knows which screens are supposed to have it, so a screen added later without it is caught rather than discovered by an owner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: No screen shows an internal token where a sentence belongs</name>
  <files>cmd/classic_voice_corpus_test.go, cmd/classic_voice_coverage_test.go, cmd/lifecycle_status_render.go, cmd/discuss.go, cmd/spec_cmd.go, cmd/codex_visuals.go, cmd/planning_visuals.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/classic_voice_corpus_test.go
    - cmd/classic_voice_coverage_test.go
    - cmd/next_action_card_test.go (untranslatedRepoWords and codeSpanRe — the shape to follow, including how it exempts backticked commands)
    - cmd/session_flow_cmds.go (the sentence constructors added in plan 05)
    - .planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md (the leak table and criterion 4)
    - .planning/REQUIREMENTS.md (CEC-09's fourth clause)
  </read_first>
  <behavior>
    - A rendered line containing a lowercase token joined by one or more underscores, outside a backticked span, is reported.
    - A rendered line containing a key-equals-value pair outside a backticked span is reported.
    - A backticked command, a file path and a flag are not reported, because an owner types those.
    - A planted rendering carrying a raw boundary token is reported, proving the check can fail.
    - Every registered screen passes the check after the fixes this task makes.
    - The check reports the screen, the line and the offending token, not a bare count.
  </behavior>
  <action>Add `rawStateTokenLeaks(text string) []string` to `cmd/classic_voice_corpus_test.go`, following `untranslatedRepoWords`' shape exactly: strip backticked spans and slash-command spans with the existing span pattern first, because those are literals an owner types, then scan the remainder line by line for a lowercase identifier joined by underscores and for a key-equals-value pair, returning one entry per hit naming the line and the token. Add `TestVoicedScreensCarryNoRawStateToken` to `cmd/classic_voice_coverage_test.go`, running it over every registration as a subtest, and `TestRawStateTokenCheckCanFail`, planting a rendering that carries one of the pause boundary values and asserting it is reported.

Then fix what it catches, at the writer of each offending value rather than by filtering at the reader, which is the disposition RESEARCH.md's synthesis already selected for this class. Expect at least the specification card's state-effect line, whose value may be an internal token, and any status or planning line that prints a typed value directly. For each, translate the value into a sentence or a plain-English label at the point the value is turned into text, in the same shape plan 05's boundary constructors use, and keep the identifier visible where an owner needs it to tell two things apart — with a plain-English label beside it, never alone. If a caught value is a stable public vocabulary word an owner already sees elsewhere in the program, say so in the summary and translate it anyway rather than exempting it; there is no exemption list in this check.

If a fix would change what a screen asserts rather than how it says it, stop and record it in the summary as out of scope for this phase instead of making the change — this phase changes how screens speak, never what they claim.</action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestVoicedScreensCarryNoRawStateToken|TestRawStateTokenCheckCanFail' -count=1` passes with one subtest per registration.
    - The check exempts backticked spans, proven by a subtest asserting a rendering containing only a backticked command with an underscore is not reported.
    - Every value translated by this task is translated at the point the value becomes text, not filtered in the check, confirmed by the check containing no value-specific branch.
    - `go test ./cmd -run 'TestEveryVoicedScreenMeetsTheReferenceDensity|TestVoicedScreensSpeakPlainEnglish' -count=1` still passes after the fixes.
    - `go test ./cmd -count=1` passes in full.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestVoicedScreensCarryNoRawStateToken|TestRawStateTokenCheckCanFail|TestEveryVoicedScreenMeetsTheReferenceDensity|TestVoicedScreensSpeakPlainEnglish' -count=1 &amp;&amp; go test ./cmd -count=1</automated>
  </verify>
  <done>The fourth thing the roadmap asked for is now true of every screen, not only of the one place the leak was first noticed.</done>
</task>

<task type="auto">
  <name>Task 3: The synthesis record, and a shipped claim that can fail</name>
  <files>.planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md, CLAUDE.md, cmd/claudemd_classic_voice_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .planning/research/v1.28-classic-synthesis-template.md
    - .planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md
    - .planning/ROADMAP.md (the Mandatory Classic-to-Go synthesis protocol and the Phase 202.1 section)
    - .planning/phases/202-swarm-oracle-and-live-colony/202-CLASSIC-SYNTHESIS.md
    - cmd/claudemd_live_colony_test.go (TestEveryLiveColonyClaimInCLAUDEMDNamesALiveTest — the guard shape to copy)
    - cmd/testdata/classic-contract/v1/schema.json (the synthesis-decision pattern, for the discharge record)
    - .planning/phases/203-biological-runtime/203-01-PLAN.md (the already-written plan that widens that same pattern next)
    - CLAUDE.md (the UX Architecture, Caste Identity System and Stage Markers sections)
  </read_first>
  <behavior>
    - The synthesis document carries the template's front matter and its sections one, six, seven and eight in full.
    - Its reconstruction, audit, matrix and selected-architecture sections cite the research document rather than restating it, and name the identifiers that document already assigned.
    - It records the corpus-registration discharge as a decided row with its reason, not as an omission.
    - Every claim added to CLAUDE.md names a test that exists.
    - The guard fails when a named test is removed.
    - The owner-facing summary in the synthesis uses no repo-invented word without explaining it in the same sentence.
  </behavior>
  <action>Write `.planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md` from the template, with front matter setting the phase to `202.1` and the area to `voice`. Section one states the outcome under investigation: the rendered voice of the ordinary lifecycle screens. Sections two through five cite `202.1-RESEARCH.md` by its section names for the Classic reconstruction, the current-Go audit, the comparative matrix and the selected architecture — that document was written as the cited mechanism study this phase's first criterion demands, and duplicating it here would create a second copy able to drift. Carry the five decision identifiers it already assigned, and record against each its delivered evidence: which plan realized it and which named test proves it.

Section six is the research-to-plan linkage, mapping each of the five roadmap criteria to the plans and the named tests that satisfy it. Section seven is the verification contract: the four dimensions the protocol requires — outcome, behaviour, experience, safety — each naming the command that proves it. Section eight records the open decisions and their rulings: the reference-standard choice the owner made at this phase's checkpoint, the glyph vocabulary as ratified, the two research assumptions and how each was resolved, and the corpus discharge.

Record the corpus discharge plainly, as a decided row: this phase does not widen the executable Classic contract corpus. The reason is concrete and belongs in writing — the corpus keys its synthesis identifiers on whole phase numbers, and the already-written first plan of Phase 203 widens that exact pattern by quoting its current value, so a competing widening here would invalidate an approved downstream plan for no added proof, given that this phase's assertions are rendered-output proportion checks that the corpus's own rule says must not stand alone as text assertions. Name Phase 203's plan as the owner of the next widening.

Add a section to CLAUDE.md stating what the restored voice guarantees, in the plain English the project mandates and following the shape the live-colony and learning sections already use: what a screen now does, what fails if it stops, and a short for-dummies paragraph. Each claim names its live test — the coverage ratchet, the corpus density gate, the token gate, the plain-English scan, the one-glyph-table guard, and the sentence-type guard. Create `cmd/claudemd_classic_voice_test.go` with `TestEveryVoiceClaimInCLAUDEMDNamesALiveTest`, copying the existing live-colony guard's shape: extract every test name cited in the new section and fail when one does not exist in the package. Do not invent a claim the tests do not support, and do not soften a claim to make it safe — remove it instead.</action>
  <acceptance_criteria>
    - `.planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md` exists, sets the phase to `202.1` and the area to `voice`, contains each of `SYN-VOICE-01` through `SYN-VOICE-05`, and contains the string `202.1-RESEARCH.md`.
    - The synthesis contains a section whose heading names the open decisions, and within it a row recording the corpus discharge that names `203-01`.
    - The synthesis maps each of the five roadmap criteria to at least one plan number and at least one test name.
    - CLAUDE.md contains a section on the restored voice naming at least six live tests.
    - `go test ./cmd -run 'TestEveryVoiceClaimInCLAUDEMDNamesALiveTest' -count=1` passes, and the guard is proven able to fail by a subtest that feeds it a fabricated test name.
    - `go test ./cmd -count=1` passes in full, and `go vet ./...` is clean.
  </acceptance_criteria>
  <verify>
    <automated>SYN=.planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md; test -f "$SYN" &amp;&amp; for i in 01 02 03 04 05; do grep -q "SYN-VOICE-$i" "$SYN" || exit 1; done &amp;&amp; grep -q '202.1-RESEARCH.md' "$SYN" &amp;&amp; grep -q '203-01' "$SYN" &amp;&amp; grep -q 'area: "voice"' "$SYN" &amp;&amp; go test ./cmd -run 'TestEveryVoiceClaimInCLAUDEMDNamesALiveTest' -count=1 &amp;&amp; go vet ./... &amp;&amp; go test ./cmd -count=1</automated>
  </verify>
  <done>The decision record the milestone protocol asks for exists, and the shipped description of the restored look is tied to checks that fail if the look goes away.</done>
</task>

</tasks>

## Artifacts this phase produces

New to the codebase in this plan (not drift):

- `cmd/classic_voice_coverage_test.go` (new file) — `TestEveryOrdinaryScreenIsMeasuredForVoice`, `TestEveryVoicedScreenMeetsTheReferenceDensity`, `TestCorpusDensityGateCanFail`, `TestVoicedScreensCarryNoRawStateToken`, `TestRawStateTokenCheckCanFail`
- `rawStateTokenLeaks(text string) []string` — the token check, added to `cmd/classic_voice_corpus_test.go`
- `cmd/claudemd_classic_voice_test.go` (new file) — `TestEveryVoiceClaimInCLAUDEMDNamesALiveTest`
- `.planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md` (new file) — the signed synthesis artifact
- A new CLAUDE.md section stating the restored-voice guarantees, each claim naming its test

<verification>
- All eight screen families are registered and measured; removing one fails the ratchet.
- Every registration measures at or above the figure derived from the reference commit.
- No registration shows an internal token or a bookkeeping pair where a sentence belongs.
- Both new gates are proven able to fail on planted input.
- Every guardrail named in RESEARCH.md's do-not-regress table passes unmodified.
- The synthesis artifact exists and carries the template's required sections, the delivered evidence, and the corpus discharge.
- The shipped claim in CLAUDE.md cannot outlive the tests behind it.
</verification>

<success_criteria>
- The rendered voice is asserted rather than described, and the assertion names screens rather than functions, so it survives the next rename.
- The phase's own decision record exists, including the one thing it deliberately did not do and why.
</success_criteria>

<output>
Create `.planning/phases/202.1-classic-visual-voice/202.1-07-SUMMARY.md` when done.
</output>
