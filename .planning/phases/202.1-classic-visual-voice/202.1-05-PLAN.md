---
phase: 202.1-classic-visual-voice
plan: "05"
type: execute
wave: 2
depends_on: ["202.1-01"]
files_modified:
  - cmd/session_flow_cmds.go
  - cmd/classic_voice_event_test.go
autonomous: true
requirements: [CEC-09]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The known leak is closed: after pausing, the what-next card's changed section shows a readable sentence, and the handoff identifier and the internal boundary token no longer appear on it (criterion 4)."
    - "The sibling leak is closed with it: after resuming, the card shows a sentence rather than the handoff identifier followed by a recovery-provenance token."
    - "The fix is at the writer, not the reader: nothing special-cases a prefix while reading, and the reader's documented contract that the last field is already a human sentence is now true rather than assumed (SYN-VOICE-03)."
    - "All seven pause boundary values and all four recovery provenance values have a sentence, enumerated from the functions that produce them rather than from a hand-typed list, so a new value added later cannot silently reach a screen as a token."
    - "The class is closed at compile time: the event writer's message parameter takes a dedicated type that only the two sentence constructors produce, so passing a typed enum value or a raw state string no longer compiles."
    - "A source guard refuses any conversion into that type outside the two constructors, and the guard is proven able to fail on a planted conversion."
    - "The stored record keeps its four-field shape, so every existing reader that splits it — the phase-completion counter, the health scanner, the planning-evidence scan — behaves exactly as before."
    - statement: "No owner-visible information is lost by removing the identifier from the message field, because the handoff reference remains recorded on colony state where recovery already reads it."
      verification: backstop
  artifacts:
    - path: "cmd/session_flow_cmds.go"
      provides: "Sentence-producing constructors and a writer that can only accept a sentence"
      contains: "lifecycleEventSentence"
    - path: "cmd/classic_voice_event_test.go"
      provides: "The named tests criterion 4 requires, plus the class guard and its planted-failure proof"
      contains: "TestPausedCardShowsASentenceNotAStateToken"
  key_links:
    - from: "cmd/session_flow_cmds.go"
      to: "cmd/next_action.go"
      via: "the writer now satisfies the reader's documented four-field contract that the last field is a human sentence"
      pattern: "nextActionEventSentence"
    - from: "cmd/session_flow_cmds.go"
      to: "pkg/colony/lifecycle.go"
      via: "every declared recovery provenance value has a sentence, enumerated from the declaration"
      pattern: "RecoveryProvenance"
  prohibitions:
    - statement: "Never special-case a prefix inside the reader; the reader's contract is correct and the writer is what broke it."
      status: flagged-unverified
    - statement: "Never widen the stored record beyond four delimiter-separated fields; three existing readers index the first two positions."
      status: flagged-unverified
    - statement: "Never add a sentence for a boundary or provenance value by hand-typing the value; enumerate from the function or type that declares it."
      status: flagged-unverified
---

<objective>
Close the one confirmed identifier leak, and the sibling it has, at the place that creates it — and make the same mistake impossible to repeat.

Purpose: RESEARCH.md traced this end to end. `pauseResumeLifecycleEvent` builds a four-field record whose last field the reader is documented to treat as a finished human sentence, and then puts `handoff=<identifier> <internal token>` in that exact field. The reader is doing precisely what its own comment says it should. Two call sites feed it raw tokens: the pause path feeds one of seven boundary values, the resume path feeds one of four recovery-provenance values. RESEARCH.md's Pitfall 3 names the tempting wrong fix — strip the prefix while reading, which closes one instance and leaves the pattern intact — and its synthesis row selects fixing the writer instead, because that closes the whole class and makes the reader's contract true.
Output: two sentence constructors, a writer that cannot accept anything else, the named tests criterion 4 asks for, and a guard that refuses a future bypass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md
@.planning/phases/202.1-classic-visual-voice/202.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A sentence is the only thing the event writer will accept</name>
  <files>cmd/session_flow_cmds.go, cmd/classic_voice_event_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/session_flow_cmds.go (pauseResumeLifecycleEvent near line 1462, pauseSafeBoundary near line 1017, the pause call site near line 379, the resume call site near line 636)
    - cmd/next_action.go (nextActionEventSentence and its doc comment near lines 979-994, changedFromInput near 964, recoveryFromInput near 1017 as the model for turning state into a sentence)
    - pkg/colony/lifecycle.go (the RecoveryProvenance declarations near lines 101-138)
    - cmd/signal_housekeeping.go (phaseCompletionsSince — an existing reader that splits the record)
    - cmd/medic_scanner.go (the event-format validation near line 318 — a second reader)
    - cmd/codex_plan.go (planningOutcomeEvidence near line 608 — a third reader)
    - .planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md (the NOW-EVENT-LEAK audit row, SYN-VOICE-03, Pitfall 3, and the leak table)
  </read_first>
  <behavior>
    - Pausing at each of the seven boundary values records a message that reads as an ordinary sentence and contains no underscore-joined token and no identifier.
    - Resuming with each of the four provenance values records a message that reads as an ordinary sentence and distinguishes a confirmed recovery from a reconstructed, conflicting or unknown one.
    - The recorded event still has exactly four delimiter-separated fields, with a parseable timestamp first and the event code second.
    - The existing phase-completion counter, health scanner and planning-evidence scan behave identically on the new records.
    - Passing a plain string variable or a typed provenance value directly to the writer fails to compile.
    - An unrecognised boundary value still produces a sentence rather than echoing the value.
  </behavior>
  <action>In `cmd/session_flow_cmds.go`, declare `lifecycleEventSentence`, a named string type, with a doc comment stating plainly that the event record's last field is what an owner reads, that this type exists so a raw enumeration value cannot be assigned into it from a variable, and naming `nextActionEventSentence` as the reader whose contract it upholds.

Add `pauseBoundarySentence(boundary string) lifecycleEventSentence`, switching over the exact seven values `pauseSafeBoundary` returns and producing a plain-English sentence for each, describing what the pause stopped at in ordinary words — a helper had just finished; work was interrupted part way; the phase was built and waiting to be checked; the phase was finished; nothing was running; the pause happened between commands. The default branch produces a general sentence and never echoes the value it was given. Add `resumeProvenanceSentence(provenance colony.RecoveryProvenance) lifecycleEventSentence`, switching over the four declared values and producing a sentence each, keeping the distinction the type exists to carry: a verified recovery point, a rebuilt one, one where the evidence disagreed, and one where the evidence could not be read. The default branch behaves the same way as the other constructor's.

Change `pauseResumeLifecycleEvent` to take `(now time.Time, command string, sentence lifecycleEventSentence)` and to build the same four-field record with the sentence as its last field. The handoff identifier leaves the record entirely — it is already durably recorded on colony state as the pause-handoff reference, which is where recovery reads it, so nothing loses it. Update both call sites to pass a constructor's result. Keep the first two fields byte-compatible with today's so the three existing readers are unaffected.

Create `cmd/classic_voice_event_test.go`. Add `TestEveryPauseBoundaryAndProvenanceHasASentence`: enumerate the boundary values by driving `pauseSafeBoundary` over colony states that produce each of its seven returns rather than by typing the seven strings, enumerate the provenance values from the declared constants, and for each assert the produced sentence contains no underscore-joined lowercase token, contains no equals sign, is not equal to the value it came from, and ends as a sentence. Add `TestPausedCardShowsASentenceNotAStateToken` and `TestResumedCardShowsASentenceNotAStateToken`: build a colony state, run the real pause and the real resume paths so the event is written by production code rather than assembled in the test, render the what-next card from that state, and assert the changed section shows the sentence and contains neither the handoff identifier nor any underscore-joined token. Add `TestStoredEventShapeIsUnchangedForExistingReaders`: write one new pause event and one new resume event and assert `phaseCompletionsSince`, the health scanner's event validation and `planningOutcomeEvidence` all behave the same as they do on a pre-existing four-field record.</action>
  <acceptance_criteria>
    - `go build ./cmd/aether` succeeds and `lifecycleEventSentence`, `pauseBoundarySentence` and `resumeProvenanceSentence` are all declared in `cmd/session_flow_cmds.go`.
    - `go test ./cmd -run 'TestEveryPauseBoundaryAndProvenanceHasASentence|TestPausedCardShowsASentenceNotAStateToken|TestResumedCardShowsASentenceNotAStateToken|TestStoredEventShapeIsUnchangedForExistingReaders' -count=1` passes.
    - `grep -c 'handoff=' cmd/session_flow_cmds.go` returns 0.
    - The seven-boundary enumeration in the test is produced by calling `pauseSafeBoundary`, not by a literal slice of strings, confirmed by reading the test.
    - `cmd/next_action.go` is unmodified: `git diff --quiet cmd/next_action.go` succeeds.
    - `go test ./cmd -run 'TestResolveNextActionIsPure|TestNextActionCardSpeaksPlainEnglish|TestNextActionCardRefusesUnsafeClear' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go build ./cmd/aether &amp;&amp; go test ./cmd -run 'TestEveryPauseBoundaryAndProvenanceHasASentence|TestPausedCardShowsASentenceNotAStateToken|TestResumedCardShowsASentenceNotAStateToken|TestStoredEventShapeIsUnchangedForExistingReaders|TestResolveNextActionIsPure|TestNextActionCardSpeaksPlainEnglish' -count=1 &amp;&amp; test "$(grep -c 'handoff=' cmd/session_flow_cmds.go)" = "0" &amp;&amp; git diff --quiet cmd/next_action.go</automated>
  </verify>
  <done>After pausing or resuming, the card tells the owner what happened in a sentence, and the project's internal bookkeeping stays internal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The class is closed — no future writer can put a token where a sentence belongs</name>
  <files>cmd/classic_voice_event_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/session_flow_cmds.go (the type and the two constructors added in Task 1)
    - cmd/next_action_hardcode_ratchet_test.go (TestNextActionHardcodeDetectsAPlantedViolation — the planted-fixture pattern to follow)
    - cmd/classic_voice_metric_test.go
    - .planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md (Pitfall 3's warning signs)
  </read_first>
  <behavior>
    - A conversion into the sentence type from anywhere other than the two constructors is found and reported by name.
    - The guard reports the file, the enclosing function and the expression it objected to.
    - A planted synthetic file containing such a conversion is caught, proving the guard can fail.
    - The guard passes on the real package as it stands after Task 1.
    - The guard finds the two legitimate constructors and fails if either disappears, so it cannot pass by finding nothing.
  </behavior>
  <action>Add `TestLifecycleEventSentenceTypeCannotBeBypassed` to `cmd/classic_voice_event_test.go`. Walk the non-test Go files of the cmd package with the AST and collect every conversion expression whose target type names the sentence type, and every composite or assignment producing that type. Exactly the two constructor function bodies may contain one; anything else fails, naming the file, the enclosing function and the offending expression. Guard the guard two ways, both mandatory: assert the scanner found the two legitimate constructors, so a scanner that silently matches nothing fails rather than passes; and add `TestLifecycleEventSentenceGuardCanFail`, writing a synthetic package file into a temporary directory that converts a plain string into the sentence type inside a fresh function, then asserting the scanner locates it — the same shape `TestNextActionHardcodeDetectsAPlantedViolation` already uses in this package.

Record in the test's doc comment why this guard exists rather than a check on the rendered output alone: a rendered check can only catch the instance someone happened to render, while this catches the next writer before it ships. RESEARCH.md's Pitfall 3 names the exact failure it prevents — a check that passes because the one known leak was patched while the pattern that produced it remains available.</action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestLifecycleEventSentenceTypeCannotBeBypassed|TestLifecycleEventSentenceGuardCanFail' -count=1` passes.
    - The planted-violation test genuinely locates the synthetic conversion, asserted by matching the synthetic function's name in the scanner's output.
    - The guard asserts a non-zero count of legitimate constructors, so a scanner returning nothing fails.
    - `go test ./cmd -count=1` passes in full.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestLifecycleEventSentenceTypeCannotBeBypassed|TestLifecycleEventSentenceGuardCanFail' -count=1 &amp;&amp; go test ./cmd -count=1</automated>
  </verify>
  <done>The next person who wants to record what happened has one way to do it, and that way produces something the owner can read.</done>
</task>

</tasks>

## Artifacts this phase produces

New to the codebase in this plan (not drift):

- `lifecycleEventSentence` — the named type the event writer's message parameter takes (`cmd/session_flow_cmds.go`)
- `pauseBoundarySentence(boundary string) lifecycleEventSentence` — sentences for the seven pause boundaries
- `resumeProvenanceSentence(provenance colony.RecoveryProvenance) lifecycleEventSentence` — sentences for the four recovery provenances
- `pauseResumeLifecycleEvent` — changed signature: the identifier leaves the record, a sentence is required
- `cmd/classic_voice_event_test.go` (new file) — `TestEveryPauseBoundaryAndProvenanceHasASentence`, `TestPausedCardShowsASentenceNotAStateToken`, `TestResumedCardShowsASentenceNotAStateToken`, `TestStoredEventShapeIsUnchangedForExistingReaders`, `TestLifecycleEventSentenceTypeCannotBeBypassed`, `TestLifecycleEventSentenceGuardCanFail`

<verification>
- The leak named in the roadmap's fourth criterion is closed and has a named test.
- Its sibling on the resume path is closed by the same change.
- All eleven enumerated values are covered, enumerated from their own producers.
- The reader was not modified.
- The stored record's shape is unchanged for all three existing readers.
- A planted bypass is caught.
</verification>

<success_criteria>
- The owner never sees a stored identifier or an internal state word where a sentence belongs on the pause or resume path.
- Closing the known instance also closed the pattern that produced it.
</success_criteria>

<output>
Create `.planning/phases/202.1-classic-visual-voice/202.1-05-SUMMARY.md` when done.
</output>
