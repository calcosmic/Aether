---
phase: 202.1-classic-visual-voice
plan: "03"
subsystem: ui
tags: [terminal-ux, glyph-rendering, discuss, spec, go]

requires:
  - phase: 202.1-01
    provides: "voiceGlyphMap/voiceGlyph/voiceLine, classicReferenceDensity, voiceScreenRegistry/registerVoiceScreen, signalTypeGlyph"
provides:
  - "renderDiscussVisual routed entirely through voiceLine; the one hardcoded warning glyph absorbed into the funnel"
  - "renderSpecCommandVisual routed entirely through voiceLine"
  - "renderSpecCommandVisualSection takes an explicit line-type argument instead of deriving a symbol from its title (survives a section rename)"
  - "discuss-questions, discuss-resolved, and spec screens registered into the shared voice screen corpus"
affects: ["202.1-04", "202.1-05", "202.1-06", "202.1-07"]

actuals:
  tokens: 7500
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "A shared rendering helper (renderSpecCommandVisualSection) takes its line-type as an explicit caller-supplied argument rather than deriving it from a mutable title string, closing the renamer-fragility gap plan 01's own glyph funnel would otherwise reopen"

key-files:
  created:
    - cmd/classic_voice_discuss_spec_test.go
  modified:
    - cmd/discuss.go
    - cmd/spec_cmd.go

key-decisions:
  - "Fixed a pre-existing, previously-uncaught plain-English violation in renderDiscussVisual's 'Queen recommends:' line (repo-invented word 'Queen' with no nearby explanation) by appending an inline gloss after the value, rather than rewording the label itself -- this keeps the pre-existing CLI test (discuss_test.go) that asserts the literal substring 'Queen recommends:' passing unchanged, while satisfying CLAUDE.md's mandate to translate jargon inline the first time the widened corpus scan actually reached this screen."
  - "renderSpecCommandVisualSection's line-type is passed at the call site (spec_cmd.go's own renderSpecCommandVisual, and discuss.go's renderDiscussSpecificationCloseout, which shares the same helper) rather than derived from the title string, per the plan's explicit renamer-fragility requirement (criterion 5)."
  - "TestSpecCardLabelsEveryIdentifier is scoped to the revision/receipt/approval identifiers named in the plan's backstop truth ('let the owner tell one revision from another') -- SpecificationID, BeforeRevisionID, AfterRevisionID, FeatureID, Approval.ID, Receipt.ReceiptID -- not the per-item content IDs (OUT-01, BEH-01, ...), which sit beside their own description text and are covered by the corpus's density/field-preservation checks instead."

requirements-completed: []

coverage:
  - id: D1
    description: "The discussion card's unresolved-questions and resolved-clarification branches are glyph-led on every content line, with every per-question field (why-now, evidence, recommendation, choices, affected scope, prior answer, revalidation, exact-answer syntax, hard-constraint flag) unchanged"
    requirement: "CEC-09"
    verification:
      - kind: unit
        ref: "cmd/classic_voice_discuss_spec_test.go#TestDiscussScreenMeetsTheReferenceDensity"
        status: pass
      - kind: unit
        ref: "cmd/classic_voice_discuss_spec_test.go#TestDiscussCardKeepsEveryQuestionField"
        status: pass
    human_judgment: false
  - id: D2
    description: "Neither card defines a local symbol literal or map; every symbol resolves through voiceLine/voiceGlyph/signalTypeGlyph, and both new screens pass the widened plain-English scan"
    requirement: "CEC-09"
    verification:
      - kind: unit
        ref: "cmd/classic_voice_discuss_spec_test.go#TestVoicedScreensSpeakPlainEnglish"
        status: pass
    human_judgment: false
  - id: D3
    description: "The specification card's top fields, every section's items and details, and the revision-impact lines are glyph-led, with renderSpecCommandVisualSection's symbol chosen by an explicit caller argument rather than derived from its title"
    requirement: "CEC-09"
    verification:
      - kind: unit
        ref: "cmd/classic_voice_discuss_spec_test.go#TestSpecScreenMeetsTheReferenceDensity"
        status: pass
    human_judgment: false
  - id: D4
    description: "Every revision/receipt/approval identifier the specification card prints still carries a plain-English label beside it on the same line, so the owner can still tell one revision, one approval, or one receipt from another"
    requirement: "CEC-09"
    verification:
      - kind: unit
        ref: "cmd/classic_voice_discuss_spec_test.go#TestSpecCardLabelsEveryIdentifier"
        status: pass
    human_judgment: false

duration: 70min
completed: 2026-09-12
status: complete
---

# Phase 202.1 Plan 03: Discussion and Specification Cards Summary

**Both the discussion card and the specification card now render every content line through the shared `voiceLine` glyph funnel, with zero field values, labels, or command syntax changed — verified by two new corpus registrations, four new tests, and the pre-existing `discuss`/`spec` test suites, all green.**

## Performance

- **Duration:** ~70 min
- **Completed:** 2026-09-12
- **Tasks:** 2
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments

- **Task 1:** Routed every content line of `renderDiscussVisual` (both the unresolved-questions branch and the resolved-clarification branch) through `voiceLine`, using the plan's suggested semantic mapping (`decision`, `goal`, `question`, `warning`, `status`, `evidence`, `alternative`, `artifact`, `history`, `checkpoint`, `next`, `avoid`, `done`), plus `signalTypeGlyph` for the REDIRECT-emitted line. Absorbed the one existing hardcoded `⚠` literal in the function into the funnel. Registered `discuss-questions` and `discuss-resolved` into the shared voice screen corpus (`cmd/classic_voice_discuss_spec_test.go`), with `TestDiscussScreenMeetsTheReferenceDensity` (both branches measure at or above the derived reference figure) and `TestDiscussCardKeepsEveryQuestionField` (every per-question field, including the exact answer syntax, is preserved verbatim).
- **Task 2:** Routed every content line of `renderSpecCommandVisual` through `voiceLine`. Changed `renderSpecCommandVisualSection`'s signature to take an explicit `lineType` argument (rather than deriving a symbol from its title string), updating both its call sites — `spec_cmd.go`'s own `renderSpecCommandVisual` and `discuss.go`'s `renderDiscussSpecificationCloseout`, which shares the same helper for the discuss-card's embedded specification closeout. Registered `spec` into the corpus with a fixture covering every section (including one empty section and one item with a detail line), a full classified delta, an approval, and a transaction receipt. Added `TestSpecScreenMeetsTheReferenceDensity` and `TestSpecCardLabelsEveryIdentifier`.

## Task Commits

Each task was committed atomically:

1. **Task 1: The discussion card's question fields carry their own symbols** — `0f1b71f2` (feat)
2. **Task 2: The specification card's fields, sections and revision impact carry their own symbols** — `4ba70f93` (feat)

**Plan metadata:** _pending — this commit_

## Files Created/Modified

- `cmd/discuss.go` — `renderDiscussVisual` routed through `voiceLine`/`signalTypeGlyph`; `renderDiscussSpecificationCloseout`'s `renderSpecCommandVisualSection` call sites updated to the new 3-argument signature
- `cmd/spec_cmd.go` — `renderSpecCommandVisual` and `renderSpecCommandVisualDelta` routed through `voiceLine`; `renderSpecCommandVisualSection` given an explicit `lineType` parameter
- `cmd/classic_voice_discuss_spec_test.go` — discuss/spec fixtures, corpus registrations (`discuss-questions`, `discuss-resolved`, `spec`), and the four new tests

## Decisions Made

- Fixed a pre-existing plain-English gap on the `Queen recommends:` line (see key-decisions above) by appending an inline gloss rather than rewording the label, preserving the literal substring an existing CLI test (`TestDiscussVisualPendingQuestionsAvoidsWorkerTheatre` in `discuss_test.go`) asserts.
- `renderSpecCommandVisualSection`'s line-type is caller-supplied, not title-derived, per the plan's explicit criterion 5 (renamer fragility).
- `TestSpecCardLabelsEveryIdentifier` is scoped to the revision/approval/receipt-level identifiers named in the plan's backstop truth, not per-item content IDs (see key-decisions above for the full rationale).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `renderDiscussVisual`'s pre-existing "Queen recommends:" line violated the widened plain-English scan**
- **Found during:** Task 1, while writing `TestVoicedScreensSpeakPlainEnglish`'s `discuss-questions` subtest
- **Issue:** `question.QueenRecommendation` was rendered as `"Queen recommends: " + value` with no nearby explanation of the repo-invented word "Queen" (CLAUDE.md mandates translating this inline, every time). This line existed before this plan; the discussion card was simply never checked against the corpus's plain-English scan until this plan registered it.
- **Fix:** Appended `" (Queen is this project's coordinator)"` after the recommendation value, keeping the literal prefix `"Queen recommends: "` byte-identical so the pre-existing `discuss_test.go` CLI assertion on that exact substring still passes.
- **Files modified:** `cmd/discuss.go`
- **Verification:** `TestVoicedScreensSpeakPlainEnglish` passes; `TestDiscussVisualPendingQuestionsAvoidsWorkerTheatre` (pre-existing, `discuss_test.go`) still passes unmodified.
- **Committed in:** `0f1b71f2` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bug, a pre-existing jargon-translation gap the plan's own new corpus test was the first thing ever to check for on this screen)
**Impact on plan:** Strictly strengthens Task 1's own mandate (pass the widened plain-English scan) without touching any field value, and without breaking the one pre-existing test asserting the exact wording it touched.

## Issues Encountered

- **Two pre-existing, out-of-scope test failures discovered during full-package verification**, neither touching any file this plan modified (confirmed via `git status --short` and `git diff --diff-filter=D`):
  - `TestHumanFacingOutputGoesThroughWriteVisualOutput` — `cmd/watch_live.go`'s `runColonyLiveRefreshLoop` writes directly to stdout/stderr at three call sites, bypassing `writeVisualOutput`. Newly recorded in `.planning/WINDOWS.md` (entry #13) and this phase's `deferred-items.md`.
  - `TestNoWorkerWithoutStatedReason` — already tracked as `.planning/WINDOWS.md` entry #11 (Phase 201, `cmd/queen_judgement_test.go`); reconfirmed still open, unrelated to this plan.
  - The four `<known_red_baseline>` tests named in the executor's own brief (`TestCurrentVocabulary199`, `TestGoldenBuildVisualOutput`, `TestGoldenContinueVisualOutput`, `TestPhase199GateReceipt`) also failed as expected and were not touched.
- **Full-suite `go test ./cmd -count=1` again hit the project's documented pre-existing environment ceiling** (parallel lanes exceeding a 9-minute deadline, several tests reported as "missing executed" rather than genuinely failing). Every test named in either task's `<verify>`/`<acceptance_criteria>` block, and every test directly exercising the three files this plan changed (`TestDiscuss*` — 42 tests, ~58s; `TestSpec*` — 27 tests, ~216s), was separately re-run to completion and passes.
- **Split the two tasks' combined git working-tree state into two independently-buildable, independently-testable commits**: since Task 2's mandated signature change to `renderSpecCommandVisualSection` required updating a call site inside `discuss.go` (Task 1's own file) to keep the package compiling, both tasks' final states were verified in isolation before each commit — Task 1's commit was built and its full acceptance criteria re-run against a temporarily Task-2-reverted tree, then Task 2's changes were restored and re-verified before its own commit.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- The discussion and specification cards — the two screens with no February ancestor — now read as part of the same restored voice as the screens that do have one, with every field/value/command they reported before this plan unchanged.
- `renderSpecCommandVisualSection`'s new `lineType` parameter is a public (package-internal) signature the remaining plans (04–07) do not call directly, so no further call-site updates are needed for this phase's remaining waves.
- `.planning/phases/202.1-classic-visual-voice/deferred-items.md` now also carries the two pre-existing failures found during this plan's full-suite verification, for whichever plan or phase owns `cmd/watch_live.go` and `cmd/queen_judgement.go` next.
- Requirement `CEC-09` is declared by multiple plans in this phase; left unmarked per the shared-ID gate until every declaring plan has a SUMMARY.

## Self-Check: PASSED

- `cmd/classic_voice_discuss_spec_test.go` verified present on disk (`test -f`).
- Both task commit hashes (`0f1b71f2`, `4ba70f93`) verified present in `git log --oneline`.
- Every `<acceptance_criteria>` item for Tasks 1 and 2 re-verified via its exact stated command: `go test ./cmd -run 'TestDiscussScreenMeetsTheReferenceDensity|TestDiscussCardKeepsEveryQuestionField'` (pass), the no-symbol-literal grep over `renderDiscussVisual`'s body (clean), `grep -c 'voiceLine' cmd/discuss.go` = 22 (≥ 12), `grep -c 'voiceLine' cmd/spec_cmd.go` = 21 (≥ 15), `go test ./cmd -run 'TestSpecScreenMeetsTheReferenceDensity|TestSpecCardLabelsEveryIdentifier'` (pass), `renderSpecCommandVisualSection`'s signature confirmed to take `lineType` as an explicit parameter (read directly), `go test ./cmd -run 'TestVoicedScreensSpeakPlainEnglish'` (pass, all four registered screens).
- Plan-level `<verification>` re-checked: both cards measure at or above the reference figure (pass); every discussion field and specification identifier previously shown is still shown (pass); neither file holds a symbol literal or map of its own (pass); the widened plain-English scan covers both cards and passes (pass).
- `go test ./cmd -run 'TestDiscuss'` (42 tests) and `go test ./cmd -run 'TestSpec'` (27 tests) both pass in full, isolated from the environment's documented full-suite ceiling.

---
*Phase: 202.1-classic-visual-voice*
*Completed: 2026-09-12*
