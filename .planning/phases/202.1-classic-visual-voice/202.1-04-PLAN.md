---
phase: 202.1-classic-visual-voice
plan: "04"
type: execute
wave: 2
depends_on: ["202.1-01"]
files_modified:
  - cmd/codex_visuals.go
  - cmd/codex_build_native_partial_test.go
  - cmd/classic_voice_work_test.go
autonomous: true
requirements: [CEC-09]
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The build screen's phase header, task list, stage bodies and dispatch summary are glyph-led, each task line carries its own done-or-pending symbol, and the screen measures at or above the reference figure (criterion 2)."
    - "The continue screen's verification, worker, housekeeping, next-phase and colony-state bodies are glyph-led and the screen measures at or above the reference figure, with the learning beat and worker-flow lines that already called the shared identity funnel left working exactly as they are."
    - "The seal screen's summary body is glyph-led beneath the ASCII-art crowning banner, which is preserved byte-for-byte as the piece of February ceremony that survived intact."
    - "No caller's command-advice string literal was edited, so the hand-typed-advice ratchet's baseline is unchanged in both directions."
    - "The three screens still report the same phase, the same tasks, the same worker set, the same gate outcomes and the same next command as before; only how each line introduces itself changed (SYN-VOICE-05)."
    - "Every line on the three screens that named repo vocabulary now explains it in the same line, so all three pass the widened plain-English scan."
    - statement: "The build screen's steering-signal block, which already carried the correct per-type symbols, is unchanged by this plan and still renders identically."
      verification: backstop
  artifacts:
    - path: "cmd/codex_visuals.go"
      provides: "Build, continue and seal section prose rendered in the restored voice"
      contains: "voiceLine"
    - path: "cmd/classic_voice_work_test.go"
      provides: "Corpus registration and density gates for the three work-cycle screens"
      contains: "TestBuildScreenMeetsTheReferenceDensity"
  key_links:
    - from: "cmd/codex_visuals.go"
      to: "cmd/testdata/classic-voice/manifest.json"
      via: "the build screen is measured against the February build card extracted from the reference commit"
      pattern: "classicReferenceDensity"
    - from: "cmd/codex_visuals.go"
      to: "cmd/codex_visuals.go"
      via: "task outcome lines resolve their symbol through the shared line-type funnel, not a literal"
      pattern: "voiceGlyph"
  prohibitions:
    - statement: "Never edit a command-advice string literal recorded in testdata/next_action_hardcode_baseline.json; that baseline may only shrink and an edit fails it twice over."
      status: flagged-unverified
    - statement: "Never alter the crowning ASCII art, its rule lines or its letter-spaced title; that ceremony already survived from February and is not this phase's to restyle."
      status: flagged-unverified
    - statement: "Never touch the steering-signal or learning-beat blocks, which already render correctly and are the model being copied."
      status: flagged-unverified
    - statement: "Never change a verification outcome, a gate result, a worker list or the next command; presentation only."
      status: flagged-unverified
---

<objective>
Bring the three screens of the work cycle — starting a phase, finishing a phase, finishing the project — up to the reference voice, copying the two blocks inside these very functions that already do it correctly.

Purpose: RESEARCH.md's audit found the model to copy sitting inside the files being edited. `renderSteeringSignals` and `renderSuggestedSteering` already carry per-type symbols on every line; `renderContinueWorkerFlowLine` and `renderLearningBeat` already call the shared identity funnel per worker. Everything around them — the phase header, the task checkboxes, the stage bodies, the verification and housekeeping summaries, the seal summary under the crowning art — is plain prose. That is the gap, and it is the largest single block of flat content in the program because these are the screens an owner sees on every phase.
Output: build, continue and seal rendering glyph-led content lines at or above the derived figure, with the already-correct blocks and the surviving February ceremony untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md
@.planning/phases/202.1-classic-visual-voice/202.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The build screen — phase header, task list and stage bodies</name>
  <files>cmd/codex_visuals.go, cmd/codex_build_native_partial_test.go, cmd/classic_voice_work_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/codex_visuals.go (renderBuildVisualWithDispatches from line 1940, renderSteeringSignals near 1915, renderSuggestedSteering near 1873, renderProgressSummary, renderQueenTeamChoice, renderSpawnPlanForDispatches, renderArtifactsSection, renderBuildPartialCreditVisual)
    - cmd/codex_visuals.go (voiceGlyphMap, voiceGlyph, voiceLine, signalTypeGlyph as added in plan 01)
    - cmd/classic_voice_metric_test.go
    - cmd/classic_voice_corpus_test.go
    - cmd/codex_build_native_partial_test.go
    - cmd/testdata/classic-voice/build-compact.txt and build-full.txt
    - cmd/testdata/next_action_hardcode_baseline.json
    - .planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md (the NOW-BUILD audit row and the OLD-BUILD-COMPACT and OLD-BUILD-FULL reconstruction rows)
  </read_first>
  <behavior>
    - The phase header, objective, review-depth and progress lines are glyph-led.
    - Each task line carries a symbol reflecting whether that task is done or still pending, in place of the bare bracket marker.
    - A phase with no captured tasks still renders its explicit no-tasks line, glyph-led.
    - The verification, housekeeping, next-phase and colony-complete stage bodies are glyph-led.
    - The steering-signal block renders byte-identically to before this task.
    - The build screen measures at or above the reference figure, both with dispatches present and with none.
    - The partial-credit build screen is glyph-led and still names every unfinished task and the recovery command.
  </behavior>
  <action>Route every content line `renderBuildVisualWithDispatches` emits through `voiceLine`, leaving `renderSteeringSignals`, `renderSuggestedSteering` and the artifact section alone. Suggested mapping: the progress-summary line uses `phase`; the phase-name line uses `phase`; the review-depth line uses `status`; the objective line uses `goal`; each task line uses `done` when that task is already marked complete and `task` otherwise, replacing the bare bracket marker rather than sitting beside it; the no-tasks line uses `task`; the verification stage body uses `evidence`; the housekeeping stage body uses `focus`; the colony-complete body uses `milestone`; and the next-phase body uses `next`.

Apply the same treatment to `renderBuildPartialCreditVisual`: its finished-task lines use `done`, its remaining-task lines use `task`, its explanation lines use `warning`, and its recovery-command line uses `next`. Do not change which tasks it lists or the recovery command it names.

Two hard constraints. First, `cmd/testdata/next_action_hardcode_baseline.json` records 118 command-advice literals by their exact text and may only shrink — every glyph added to a next-step line must be added inside the rendering funnel plan 01 already changed, never by editing a caller's literal in this file. Second, the verification and housekeeping stage bodies name the continue command inside backticks; keep those strings byte-identical and put the symbol in front of the line.

The words `colony` and `signal housekeeping` appear on these bodies and the widened plain-English scan will catch the first. Reword in place so the ordinary word leads and the repo word follows in the same line; do not delete the repo word and do not add an exception.

Create `cmd/classic_voice_work_test.go` with an `init` registering `build` and `build-partial` into the corpus, rendering over fixtures that carry a named phase with a description, at least three tasks of which one is already complete, at least two dispatches, one active steering signal, and a final-phase variant. Add `TestBuildScreenMeetsTheReferenceDensity` with a subtest per registration. Add `TestBuildSteeringBlockIsUnchanged`, capturing `renderSteeringSignals` output and asserting it matches a fixture string built from the same signal set, so a future edit to the surrounding function cannot quietly restyle the block that was already right.

Repair `cmd/codex_build_native_partial_test.go` where it asserts the literal beginning with the word Verification followed by the word happens: widen it to the new voiced line rather than shortening it to a fragment a flat line would also satisfy.</action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestBuildScreenMeetsTheReferenceDensity|TestBuildSteeringBlockIsUnchanged' -count=1` passes with two density subtests.
    - `git diff --stat cmd/testdata/next_action_hardcode_baseline.json` shows no change and `go test ./cmd -run 'TestNextActionNeverHardcoded|TestNextActionHardcodeBaselineMatchesLive' -count=1` passes.
    - The rendered build fixture contains no line whose first non-space characters are a square-bracket checkbox marker, asserted in the new test.
    - The rendered build fixture names the same phase, the same task goals and the same dispatch count as before the change, asserted as an unchanged-facts subtest.
    - `go test ./cmd -run 'TestVoicedScreensSpeakPlainEnglish' -count=1` passes with the new subtests present.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestBuildScreenMeetsTheReferenceDensity|TestBuildSteeringBlockIsUnchanged|TestNextActionNeverHardcoded|TestNextActionHardcodeBaselineMatchesLive|TestVoicedScreensSpeakPlainEnglish' -count=1 &amp;&amp; git diff --quiet cmd/testdata/next_action_hardcode_baseline.json</automated>
  </verify>
  <done>The screen that opens every phase reads like the colony starting work, and the block inside it that was already right is provably untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The continue screen — verification, workers, housekeeping and the end-of-phase footer</name>
  <files>cmd/codex_visuals.go, cmd/classic_voice_work_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/codex_visuals.go (renderContinueVisual from line 2178, renderLearningBeat from 2262, renderContinueWorkerFlowLine near 2632, renderContinueVerificationSummaryMap, renderContinueVerificationDetail, renderContinueGateSummaryMap, renderContinueGateDetail, renderCriterionEvidenceLines, renderSpecialistFindingBlocks, renderPhaseEndFooter, renderProjectComplete)
    - cmd/codex_visuals.go (voiceGlyph, voiceLine, signalTypeGlyph)
    - cmd/classic_voice_metric_test.go
    - .planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md (the NOW-CONTINUE audit row)
  </read_first>
  <behavior>
    - The verification summary, criterion evidence, worker list, gate summary, housekeeping and footer lines are all glyph-led.
    - A passing gate and a failing gate carry different symbols.
    - The worker-flow lines and the learning beat, which already call the shared identity funnel, render byte-identically to before.
    - A run with no workers to close still renders its explicit none line, glyph-led.
    - The final-phase branch and the next-phase branch both measure at or above the reference figure.
    - The screen reports the same verification outcome, the same gate results and the same signal counts as before.
  </behavior>
  <action>Route every content line `renderContinueVisual` emits, and the helper functions it calls that write plain lines, through `voiceLine`. Leave `renderLearningBeat` and `renderContinueWorkerFlowValue`'s per-worker lines exactly as they are — both already resolve identity through the shared funnel and are the model being copied. Suggested mapping: the review-depth line uses `status`; the verification-pass line uses `done`, or `warning` when the result is a partial operational success; the phase-verified line uses `done`; each verification summary and detail row uses `done` or `failed` by its own outcome; the operational-evidence header and its items use `evidence`; criterion evidence lines use `evidence`; the workers header uses `colony` and the no-workers-required line uses `done`; each gate summary and detail row uses `done` or `failed` by its outcome; the workers-closed count uses `colony`; the housekeeping signal counts use `focus`; the expired-signal breakdown uses `history`; the colony-complete body uses `milestone`; the next-phase line uses `next`; and the colony-state footer's own lines use the type that fits each — flags use `flag`, signals use their own type glyph, progress uses `phase`.

The final-phase body and the footer both name repo vocabulary the widened plain-English scan will catch. Reword in place, ordinary word first, repo word beside it in the same line.

Register `continue-midphase` and `continue-final` into the corpus in `cmd/classic_voice_work_test.go`, rendering over fixtures that carry a verification map with one passing and one failing entry, a gate map with both outcomes, at least two closed workers, a housekeeping result with expirations, a consolidation result, and one active suggested steering note. Add `TestContinueScreenMeetsTheReferenceDensity` with a subtest per registration. Add `TestContinueAlreadyCorrectBlocksAreUnchanged`, capturing `renderLearningBeat` and the worker-flow rendering over fixed input and asserting both match the strings they produced before this task, so the two blocks that already carried the voice cannot be accidentally restyled by an edit to their surroundings.</action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestContinueScreenMeetsTheReferenceDensity|TestContinueAlreadyCorrectBlocksAreUnchanged' -count=1` passes with two density subtests.
    - A passing gate line and a failing gate line in the rendered fixture begin with different symbols, asserted explicitly.
    - The rendered continue fixture names the same phase number, the same gate names and the same signal counts as before the change, asserted as an unchanged-facts subtest.
    - `go test ./cmd -run 'TestVoicedScreensSpeakPlainEnglish|TestBuildScreenMeetsTheReferenceDensity' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestContinueScreenMeetsTheReferenceDensity|TestContinueAlreadyCorrectBlocksAreUnchanged|TestVoicedScreensSpeakPlainEnglish|TestBuildScreenMeetsTheReferenceDensity' -count=1</automated>
  </verify>
  <done>The screen that closes every phase reads like the colony reporting back, and a passing check no longer looks identical to a failing one at a glance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The seal screen — the summary beneath the crowning art</name>
  <files>cmd/codex_visuals.go, cmd/classic_voice_work_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/codex_visuals.go (renderSealVisual from line 3375, crownedAnthillArt, spacedTitle, renderLifecycleClosing)
    - cmd/codex_visuals.go (voiceGlyph, voiceLine)
    - cmd/classic_voice_metric_test.go
    - cmd/testdata/classic-voice/entomb.txt
    - .planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md (the NOW-SEAL audit row and the OLD-ENTOMB reconstruction row)
  </read_first>
  <behavior>
    - The crowning ASCII art, its two rule lines and its letter-spaced title render byte-identically to before.
    - Every line of the summary body beneath the art is glyph-led.
    - The three closing sentences beneath the summary are glyph-led.
    - A project with no recorded goal still renders the rest of the summary without an empty goal line.
    - The seal screen measures at or above the reference figure.
    - The screen still names the same completed-phase count and the same summary path.
  </behavior>
  <action>Route the summary body of `renderSealVisual` through `voiceLine`, leaving the art, the rule lines and the letter-spaced title exactly as they are — RESEARCH.md identifies that ceremony as a genuine piece of February that survived intact, and it is the one thing on this screen not to touch. Suggested mapping: the sealed-at line uses `milestone`; the goal line uses `goal`; the completed-phases line uses `phase`; the summary-path line uses `artifact`; and the three closing sentences use `milestone`, `learning` and `archive` respectively.

The sealed-at line and the closing sentences carry repo vocabulary the widened plain-English scan will catch — the project's word for finishing, and its word for the project itself. Reword each so an ordinary word carries the meaning and the repo word sits beside it in the same line. RESEARCH.md's February reconstruction notes that the archive glyph marked finality distinctly from the milestone glyph used for progress; keep that distinction in the mapping above rather than using one symbol for the whole block.

Register `seal` into the corpus in `cmd/classic_voice_work_test.go` with its own `init`, rendering `renderSealVisual` over a fixture carrying a goal, a plan with several phases, a summary path and a version. Add `TestSealScreenMeetsTheReferenceDensity`. Add `TestSealCeremonyArtIsUnchanged`, asserting the rendered screen contains the art constant verbatim, the two rule lines at their original length, and the letter-spaced title with the version, so a future edit to the summary cannot erode the ceremony above it.</action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestSealScreenMeetsTheReferenceDensity|TestSealCeremonyArtIsUnchanged' -count=1` passes.
    - The rendered seal fixture contains the crowning art constant verbatim, asserted by comparing against the constant itself rather than a re-typed copy.
    - The rendered seal fixture names the same completed-phase count and the same summary path as before the change, asserted as an unchanged-facts subtest.
    - `go test ./cmd -run 'TestVoicedScreensSpeakPlainEnglish' -count=1` passes with the seal subtest present.
    - `go test ./cmd -count=1` passes in full.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestSealScreenMeetsTheReferenceDensity|TestSealCeremonyArtIsUnchanged|TestVoicedScreensSpeakPlainEnglish' -count=1 &amp;&amp; go test ./cmd -count=1</automated>
  </verify>
  <done>The finishing screen's facts read the way its drawing already did, and the drawing itself is now protected by a check rather than by nobody happening to touch it.</done>
</task>

</tasks>

## Artifacts this phase produces

New to the codebase in this plan (not drift):

- `cmd/classic_voice_work_test.go` (new file) — `TestBuildScreenMeetsTheReferenceDensity`, `TestBuildSteeringBlockIsUnchanged`, `TestContinueScreenMeetsTheReferenceDensity`, `TestContinueAlreadyCorrectBlocksAreUnchanged`, `TestSealScreenMeetsTheReferenceDensity`, `TestSealCeremonyArtIsUnchanged`
- Corpus registrations `build`, `build-partial`, `continue-midphase`, `continue-final`, `seal`

<verification>
- All five registrations measure at or above the figure derived from the reference commit.
- The two blocks that already carried the voice, and the crowning ceremony, are each locked by their own unchanged assertion.
- The hand-typed-advice baseline is unchanged in both directions.
- The three screens report identical facts to before, asserted per screen.
- The widened plain-English scan covers all five registrations and passes.
</verification>

<success_criteria>
- The three screens an owner sees on every single phase are the ones that changed most, and each has a check that fails if it flattens again.
- Nothing that was already right was restyled, and nothing that was claimed changed.
</success_criteria>

<output>
Create `.planning/phases/202.1-classic-visual-voice/202.1-04-SUMMARY.md` when done.
</output>
