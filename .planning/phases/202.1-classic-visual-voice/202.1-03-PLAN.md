---
phase: 202.1-classic-visual-voice
plan: "03"
type: execute
wave: 2
depends_on: ["202.1-01"]
files_modified:
  - cmd/discuss.go
  - cmd/spec_cmd.go
  - cmd/classic_voice_discuss_spec_test.go
autonomous: true
requirements: [CEC-09]
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The discussion card's question fields are glyph-led on the lines themselves and the card measures at or above the reference figure, while every field the card gained in Phase 200 — why now, evidence, the recommendation, each choice's consequence, affected scope, prior answer, revalidation, the exact answer syntax — is still present and still says the same thing (criterion 2, CEC-03)."
    - "The specification card's field lines, section items and revision-impact lines are glyph-led and the card measures at or above the reference figure."
    - "Neither card acquired a symbol vocabulary of its own: every symbol resolves through the shared funnel added in plan 01."
    - "Both cards are registered in the shared screen corpus and pass the widened plain-English scan."
    - "Neither card changed a value it reports, a decision it records, or the command it closes with."
    - statement: "The specification card's identifier fields still let the owner tell one revision from another after the restyle, because the identifier keeps a plain-English label beside it."
      verification: backstop
  artifacts:
    - path: "cmd/discuss.go"
      provides: "The discussion card rendered in the restored voice"
      contains: "voiceLine"
    - path: "cmd/spec_cmd.go"
      provides: "The specification card rendered in the restored voice"
      contains: "voiceLine"
    - path: "cmd/classic_voice_discuss_spec_test.go"
      provides: "Corpus registration and density gates for both cards"
      contains: "TestDiscussScreenMeetsTheReferenceDensity"
  key_links:
    - from: "cmd/spec_cmd.go"
      to: "cmd/codex_visuals.go"
      via: "every section and item symbol resolves through the shared line-type funnel"
      pattern: "voiceGlyph"
    - from: "cmd/discuss.go"
      to: "cmd/codex_visuals.go"
      via: "every question-field symbol resolves through the shared line-type funnel"
      pattern: "voiceLine"
  prohibitions:
    - statement: "Never reduce or reorder the discussion card's per-question fields; the richer content is Phase 200's delivered value and only its presentation is in scope."
      status: flagged-unverified
    - statement: "Never copy a February template's wording onto these cards; they have no February ancestor and adopt the house style, not a historical script."
      status: flagged-unverified
    - statement: "Never define a local symbol map in either file."
      status: flagged-unverified
---

<objective>
Bring the two newest ordinary screens — the discussion card and the specification card — up to the house voice without touching a word of the content that makes them worth reading.

Purpose: RESEARCH.md is explicit that these two are the opposite of a regression. The discussion card's per-question content is materially richer than anything February produced, and the specification command did not exist at all before v1.28. What both lack is entirely presentational: one symbol appears in the whole discussion renderer, and the specification renderer emits roughly seventeen consecutive plain `Label: value` lines and section bodies. RESEARCH.md's Pitfall 5 names the trap to avoid here — stalling on a search for a February ancestor that does not exist, or forcing a one-to-one port of the council card's wording. These two adopt the house style, not a historical script.
Output: two cards whose lines carry their own symbols and meet the derived figure, with every field they already showed still present and unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md
@.planning/phases/202.1-classic-visual-voice/202.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The discussion card's question fields carry their own symbols</name>
  <files>cmd/discuss.go, cmd/classic_voice_discuss_spec_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/discuss.go (renderDiscussVisual from line 1511 to the end of the function, plus renderDiscussSpecificationCloseout and renderDiscussEvidenceRefs)
    - cmd/codex_visuals.go (voiceGlyphMap, voiceGlyph, voiceLine, signalTypeGlyph)
    - cmd/classic_voice_metric_test.go
    - cmd/classic_voice_corpus_test.go
    - .planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md (the NOW-DISCUSS audit row, Pitfall 5, the OLD-COUNCIL reconstruction row)
  </read_first>
  <behavior>
    - Every content line of both the unresolved-questions rendering and the resolved-clarification rendering is glyph-led.
    - Each of the per-question fields still appears, in the same order, carrying the same text as before.
    - A question with no prior answer still renders its explicit none line.
    - A question marked as a hard constraint still renders that line.
    - The rendered card measures at or above the reference figure in both the questions branch and the resolved branch.
    - The exact answer syntax the owner must type is unchanged, character for character.
  </behavior>
  <action>Route every content line `renderDiscussVisual` emits through `voiceLine`. Suggested mapping, adjusting only where `voiceGlyphMap` offers a better semantic fit: the locked-in line and the decision and answer lines in the resolved branch use `decision`; the emitted-steering-note line uses the signal type's own glyph through `signalTypeGlyph`; the goal line uses `goal`; the question-count line and the already-resolved-count line use `question`; the stale-state notice uses `warning`; the owner-decisions-required header keeps its existing warning symbol, resolved through the funnel rather than as a literal; each `Decision <id>` line uses `question`; why-now uses `status`; evidence uses `evidence`; the recommendation uses `decision`; each choice consequence uses `alternative`; affected scope uses `artifact`; prior answer uses `history`; revalidation uses `checkpoint`; planning-resumes uses `next`; the exact-answer line uses `next`; the hard-constraint line uses `avoid`; and the no-unresolved-questions line uses `done`.

Do not change field order, field labels' meaning, or any value. The point of this card is the content Phase 200 put on it; the only permitted change is what each line opens with. The one existing literal symbol in the function must be replaced by a funnel call so the file holds no symbol literal of its own.

Create `cmd/classic_voice_discuss_spec_test.go` with an `init` registering `discuss-questions` and `discuss-resolved` into the corpus, each rendering `renderDiscussVisual` over a fixture result map: the questions fixture carries at least two questions, one with a prior answer and one without, one marked a hard constraint, each with a full field set and at least two choices; the resolved fixture carries a locked clarification with a steering note emitted. Add `TestDiscussScreenMeetsTheReferenceDensity` with a subtest per registration, asserting `voiceDensity` at or above `classicReferenceDensity(t)`. Add `TestDiscussCardKeepsEveryQuestionField`, asserting each field's text from the fixture appears in the rendering and that the exact-answer string is present unmodified.</action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestDiscussScreenMeetsTheReferenceDensity|TestDiscussCardKeepsEveryQuestionField' -count=1` passes with two density subtests.
    - `cmd/discuss.go` contains no symbol literal inside `renderDiscussVisual`: a search of that function's body for a non-ASCII rune in a string literal returns nothing.
    - `grep -c 'voiceLine' cmd/discuss.go` is at least 12.
    - The fixture's exact-answer string appears verbatim in the rendering, asserted as its own subtest.
    - `go test ./cmd -run 'TestVoicedScreensSpeakPlainEnglish' -count=1` passes with the two new subtests present.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestDiscussScreenMeetsTheReferenceDensity|TestDiscussCardKeepsEveryQuestionField|TestVoicedScreensSpeakPlainEnglish' -count=1</automated>
  </verify>
  <done>The decision cards the owner has to answer look like they belong to this program, while every fact that makes them answerable is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The specification card's fields, sections and revision impact carry their own symbols</name>
  <files>cmd/spec_cmd.go, cmd/classic_voice_discuss_spec_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/spec_cmd.go (renderSpecCommandVisual from line 691, renderSpecCommandVisualSection, renderSpecCommandVisualDelta, specCommandIDSummary)
    - cmd/codex_visuals.go (voiceGlyphMap, voiceGlyph, voiceLine)
    - cmd/classic_voice_metric_test.go
    - .planning/phases/202.1-classic-visual-voice/202.1-RESEARCH.md (the NOW-SPEC audit row, Assumption A1, Open Question 2)
  </read_first>
  <behavior>
    - Every top field line, every section item line and every revision-impact line is glyph-led.
    - A section with no items renders its title and nothing else, exactly as before.
    - An item carrying a detail line renders the detail glyph-led too.
    - A revision-impact line with no changes in a category still renders its none summary.
    - The rendered card measures at or above the reference figure.
    - Every identifier the card previously printed is still printed, and each one is still preceded by a plain-English label saying what it is.
  </behavior>
  <action>Route every content line in `renderSpecCommandVisual` through `voiceLine`. Suggested mapping: the operation line uses `decision`; the specification-and-revision line uses `requirement`; the before and after revision lines use `history`; status uses `status`; scope and feature use `artifact`; the already-recorded line uses `done`; the projection lines use `status` when synchronized or repaired and `warning` when drifted; the approval-receipt and transaction-receipt lines use `checkpoint`; the approval-does-not-activate line uses `warning`; the state-effect line uses `status`; and each affected-identifier summary line uses `artifact`.

In `renderSpecCommandVisualSection`, prefix each item line and each detail line with a symbol chosen by the section, passed in as a new line-type argument at the call site rather than derived inside the helper from the title string — deriving it from a title makes the symbol break when the title is reworded, which is precisely the renamer fragility criterion 5 exists to prevent. Section-to-type mapping: what-this-goal-delivers uses `goal`; included uses `done`; explicitly-excluded uses `avoid`; binding decisions uses `decision`; requirements uses `requirement`; owner-checkable acceptance uses `evidence`; negative expectations uses `avoid`; recovery expectations uses `checkpoint`; affected public paths uses `files`. In `renderSpecCommandVisualDelta`, prefix each impact line with `history`.

RESEARCH.md's Open Question 2 asked which symbol represents v1.28-only ideas with no February precedent. The answer is already fixed by plan 01's table: pick by semantic analogy to the nearest February category and use the key that table already holds. Do not add a new key to that table from this plan; if a genuinely missing concept appears, use the closest existing key and record the substitution in one line in this plan's summary so the final gate can consider it.

Register `spec` into the corpus in `cmd/classic_voice_discuss_spec_test.go` with its own `init`, rendering `renderSpecCommandVisual` over a `specCommandResult` fixture that populates every section with at least one item, at least one item with a detail, at least one empty section, a full classified delta, an approval and a receipt. Add `TestSpecScreenMeetsTheReferenceDensity` and `TestSpecCardLabelsEveryIdentifier`, the latter asserting that every identifier string the fixture supplies appears in the rendering preceded on the same line by a plain-English label rather than standing alone.</action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestSpecScreenMeetsTheReferenceDensity|TestSpecCardLabelsEveryIdentifier' -count=1` passes.
    - `renderSpecCommandVisualSection` takes a line-type argument and no longer derives a symbol from its title argument, confirmed by reading the signature.
    - `grep -c 'voiceLine' cmd/spec_cmd.go` is at least 15.
    - `go test ./cmd -run 'TestVoicedScreensSpeakPlainEnglish|TestDiscussScreenMeetsTheReferenceDensity' -count=1` passes.
    - `go test ./cmd -count=1` passes in full.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestSpecScreenMeetsTheReferenceDensity|TestSpecCardLabelsEveryIdentifier|TestVoicedScreensSpeakPlainEnglish' -count=1 &amp;&amp; go test ./cmd -count=1</automated>
  </verify>
  <done>The newest screen in the program reads like the oldest one did, without pretending it had a February ancestor it never had.</done>
</task>

</tasks>

## Artifacts this phase produces

New to the codebase in this plan (not drift):

- `cmd/classic_voice_discuss_spec_test.go` (new file) — `TestDiscussScreenMeetsTheReferenceDensity`, `TestDiscussCardKeepsEveryQuestionField`, `TestSpecScreenMeetsTheReferenceDensity`, `TestSpecCardLabelsEveryIdentifier`
- Corpus registrations `discuss-questions`, `discuss-resolved`, `spec`
- A line-type argument on `renderSpecCommandVisualSection` (changed signature, so the symbol survives a section rename)

<verification>
- Both cards measure at or above the figure derived from the reference commit.
- Every discussion field and every specification identifier the cards previously showed is still shown.
- Neither file holds a symbol literal or a symbol map of its own.
- The widened plain-English scan covers both cards and passes.
</verification>

<success_criteria>
- The two screens with no February ancestor read as part of the same program as the ones that have one.
- The content that makes them valuable is provably untouched.
</success_criteria>

<output>
Create `.planning/phases/202.1-classic-visual-voice/202.1-03-SUMMARY.md` when done.
</output>
