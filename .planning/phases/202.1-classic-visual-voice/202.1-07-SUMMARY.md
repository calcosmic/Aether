---
phase: 202.1-classic-visual-voice
plan: "07"
subsystem: ui
tags: [terminal-ux, coverage-ratchet, ast-free-guard, synthesis, go]

requires:
  - phase: 202.1-01
    provides: "voiceScreenRegistry/registerVoiceScreen, classicReferenceDensity/voiceDensity, the voiceGlyph/voiceLine funnel"
  - phase: 202.1-05
    provides: "lifecycleEventSentence, the writer-side fix for the pause/resume identifier leak"
provides:
  - "TestEveryOrdinaryScreenIsMeasuredForVoice: the coverage ratchet naming the eight required screen families, failing on an empty corpus or a missing family without ever mutating the shared registry"
  - "TestEveryVoicedScreenMeetsTheReferenceDensity: the corpus-wide density gate, one subtest per registered screen (21 currently)"
  - "TestCorpusDensityGateCanFail: proves the density comparison fails against a planted flat screen via a local voiceScreenCase copy"
  - "rawStateTokenLeaks (classic_voice_corpus_test.go): a corpus-wide token-leak scanner, following untranslatedRepoWords' shape"
  - "TestVoicedScreensCarryNoRawStateToken / TestRawStateTokenCheckCanFail: the corpus-wide token gate and its can-fail proof"
  - ".planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md: the phase's signed Classic-to-Go synthesis record"
  - "CLAUDE.md's \"Classic Visual Voice (v1.28)\" section, each claim naming a live test"
  - "cmd/claudemd_classic_voice_test.go: TestEveryVoiceClaimInCLAUDEMDNamesALiveTest"
affects: []

actuals:
  tokens: 11400
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "A coverage ratchet walks the shared voiceScreenRegistry by family-prefix match rather than naming renderer functions, so it survives a rename of any renderer, section, or heading"
    - "An enum-to-plain-English translation is applied only at the point a value is formatted into a rendered string, never inside a projection struct's construction, when that struct also feeds a machine-readable JSON envelope (planning_projection) -- keeps a presentation-only fix from silently becoming a data-contract change"
    - "A can-fail proof for a corpus-wide gate uses a local (non-shared) copy of the registry's own case type, never registering a synthetic screen into the shared corpus every other test in the package walks"

key-files:
  created:
    - cmd/classic_voice_coverage_test.go
    - cmd/claudemd_classic_voice_test.go
    - .planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md
  modified:
    - cmd/classic_voice_corpus_test.go
    - cmd/lifecycle_status_render.go
    - cmd/planning_visuals.go
    - CLAUDE.md

key-decisions:
  - "rawStateTokenLeaks exempts a full CLI invocation (a slash command or an \"aether <verb>\" line, through the rest of that line) an owner is told to type verbatim -- not just the bare command token codeSpanRe already exempted -- after the discuss card's \"Answer exactly: /ant-discuss --answer D1=local\" line was flagged as a false positive (the D1=local argument value, not an internal token)."
  - "Every enum-shaped value this task translates (phase status, gap materiality, candidate status, authority-impact kind) is translated at the render call site or inside a plain []string display list already built purely for presentation -- never inside a projection struct field that also serializes into the planning_projection JSON envelope consumed by TestPlanningVisualsJSONStopLabelsAndEvidence and its siblings. Confirmed by re-running that test unmodified after the fix."
  - "The corpus is not widened to accept a 202.1-scoped synthesis identifier: the schema's synthesis_decision pattern keys on whole phase numbers only (^SYN-(?:199-...|200-...|201-...|202-(?:0[1-9]|1[0-2]))$), has no slot for a decimal phase, and Phase 203's already-approved plan 203-01 owns the next widening (to add 203-(?:0[1-9]|1[0-2])) by quoting this exact pattern. Recorded as a decided row in the synthesis document rather than a silent omission."
  - "requirements-completed left empty, same as plan 05: the installed gsd-tools in this worktree does not expose the shared-ID requirements.ready-ids gate. CEC-09 is declared by all seven plans in this phase; this is the seventh and last, so the orchestrator should mark it complete centrally once this final wave merges."

requirements-completed: []

coverage:
  - id: D1
    description: "The coverage ratchet fails naming any of the eight required screen families (plan, discuss, spec, status, build, continue, seal, what-next) absent from the voice corpus, or when the corpus is empty -- proven on a filtered copy of registered names, never the shared registry itself"
    requirement: "CEC-09"
    verification:
      - kind: unit
        ref: "cmd/classic_voice_coverage_test.go#TestEveryOrdinaryScreenIsMeasuredForVoice"
        status: pass
    human_judgment: false
  - id: D2
    description: "Every screen currently registered in the shared voice corpus (21 registrations across plans 01-06) measures at or above the figure derived from the reference commit, and the comparison is proven able to fail against a planted flat screen via a local, non-shared registry copy"
    requirement: "CEC-09"
    verification:
      - kind: unit
        ref: "cmd/classic_voice_coverage_test.go#TestEveryVoicedScreenMeetsTheReferenceDensity"
        status: pass
      - kind: unit
        ref: "cmd/classic_voice_coverage_test.go#TestCorpusDensityGateCanFail"
        status: pass
    human_judgment: false
  - id: D3
    description: "No screen registered in the shared voice corpus shows an internal state token (an underscore-joined identifier) or a key=value bookkeeping pair on a rendered line; the two real leaks the check found (a phase-status token in the status card, and gap/candidate/authority-impact enum values on three planning cards) are fixed at their writers, never filtered at the reader"
    requirement: "CEC-09"
    verification:
      - kind: unit
        ref: "cmd/classic_voice_coverage_test.go#TestVoicedScreensCarryNoRawStateToken"
        status: pass
      - kind: unit
        ref: "cmd/classic_voice_coverage_test.go#TestRawStateTokenCheckCanFail"
        status: pass
    human_judgment: false
  - id: D4
    description: "The signed 202.1-CLASSIC-SYNTHESIS.md discharges the milestone's mandatory Classic-to-Go synthesis protocol for this phase: cites 202.1-RESEARCH.md for the reconstruction/audit/matrix/architecture, carries SYN-VOICE-01..05 with delivered evidence, maps all five roadmap criteria to plans and named tests, states a four-dimension verification contract, and records the corpus-widening discharge as a decided row naming 203-01"
    requirement: "CEC-09"
    verification:
      - kind: other
        ref: "grep checks for SYN-VOICE-01..05, 202.1-RESEARCH.md, 203-01, area: \"voice\", and the ## 8 open-decisions heading, all against 202.1-CLASSIC-SYNTHESIS.md (see plan's <verify> block)"
        status: pass
    human_judgment: false
  - id: D5
    description: "CLAUDE.md's new \"Classic Visual Voice (v1.28)\" section states what the restored voice guarantees, each claim naming a live test, and a guard fails if a cited test stops existing -- proven able to fail on a fabricated test name"
    requirement: "CEC-09"
    verification:
      - kind: unit
        ref: "cmd/claudemd_classic_voice_test.go#TestEveryVoiceClaimInCLAUDEMDNamesALiveTest"
        status: pass
    human_judgment: false

duration: 85min
completed: 2026-09-12
status: complete
---

# Phase 202.1 Plan 07: Coverage Ratchet, Corpus-Wide Density and Token Gates, Synthesis Record and Shipped Claim Summary

**Closes the phase with three new corpus-wide gates (a coverage ratchet that notices a missing screen family, a density gate over all 21 registered screens, and a token gate closing the class of internal-state leaks), two real production leaks fixed at their writers rather than filtered at a reader, the phase's signed Classic-to-Go synthesis record, and a CLAUDE.md section whose every claim names a live test.**

## Performance

- **Duration:** ~85 min
- **Completed:** 2026-09-12
- **Tasks:** 3
- **Files modified:** 7 (3 created, 4 modified)

## Accomplishments

- **Task 1:** Created `cmd/classic_voice_coverage_test.go` with `TestEveryOrdinaryScreenIsMeasuredForVoice` (the eight-family coverage ratchet, proven on a filtered copy of registered names and on an empty corpus, never on the shared registry itself), `TestEveryVoicedScreenMeetsTheReferenceDensity` (a per-screen density subtest over all 21 current registrations), and `TestCorpusDensityGateCanFail` (a planted flat screen through a local `voiceScreenCase` copy, proving the gate can fail against the same code path the real screens take). The file names no renderer function and contains no decimal density literal.
- **Task 2:** Added `rawStateTokenLeaks` to `cmd/classic_voice_corpus_test.go`, following `untranslatedRepoWords`' shape: strip a backticked span or a full CLI invocation (a slash command or an `aether <verb>` line an owner types verbatim, through the rest of its line), then scan each remaining line for an underscore-joined identifier or a `key=value` bookkeeping pair. Added `TestVoicedScreensCarryNoRawStateToken` (a subtest per corpus registration) and `TestRawStateTokenCheckCanFail` to `classic_voice_coverage_test.go`. Running the real check against the full corpus surfaced two genuine, previously-uncaught leaks: `lifecycle_status_render.go`'s phase-status line rendering `[in_progress]` verbatim, and three sites in `planning_visuals.go` rendering a gap's materiality (`non_material`), a plan candidate's status (`pending_review`), and an authority-impact kind (`owner_decision`) directly. Both were fixed at the writer -- translated to plain words at the exact point each value becomes rendered text -- while leaving every projection struct field that also feeds the machine-readable `planning_projection` JSON envelope untouched, confirmed by re-running `TestPlanningVisualsJSONStopLabelsAndEvidence` and `TestPlanningVisualsCandidateAuthority` unmodified.
- **Task 3:** Wrote `.planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md` from the mandatory template: cites `202.1-RESEARCH.md` for the Classic reconstruction, current-Go audit, comparative matrix, and selected architecture rather than duplicating them; carries `SYN-VOICE-01` through `SYN-VOICE-05` each with the plan and test that delivered it; maps all five roadmap success criteria to plan numbers and named tests; states a four-dimension verification contract naming the exact `go test` command that proves each; and records the corpus-widening discharge as a decided row naming `203-01` as the next widening's owner, with the reasoning that the corpus's `synthesis_decision` pattern keys on whole phase numbers only and has no slot for a decimal sub-phase like `202.1`. Added a "Classic Visual Voice (v1.28)" section to CLAUDE.md stating what the restored voice guarantees, with each claim naming one of six live tests, and `cmd/claudemd_classic_voice_test.go`'s `TestEveryVoiceClaimInCLAUDEMDNamesALiveTest`, copying the existing live-colony guard's exact shape and proven able to fail on a fabricated test name.

## Task Commits

Each task was committed atomically:

1. **Task 1: The coverage ratchet and the corpus-wide density gate** — `0a81263b` (feat)
2. **Task 2: No screen shows an internal token where a sentence belongs** — `025fe67f` (feat)
3. **Task 3: The synthesis record, and a shipped claim that can fail** — `018d6851` (docs)

**Plan metadata:** _pending — this commit_

## Files Created/Modified

- `cmd/classic_voice_coverage_test.go` (new) — `TestEveryOrdinaryScreenIsMeasuredForVoice`, `TestEveryVoicedScreenMeetsTheReferenceDensity`, `TestCorpusDensityGateCanFail`, `TestVoicedScreensCarryNoRawStateToken`, `TestRawStateTokenCheckCanFail`
- `cmd/classic_voice_corpus_test.go` — `rawStateTokenLeaks` and its supporting patterns (reuses `codeSpanRe`/`underscoreTokenPattern` from sibling test files in the same package)
- `cmd/lifecycle_status_render.go` — the phase-status line now translates its internal token to plain words at render time
- `cmd/planning_visuals.go` — new `planningEnumLabel` helper (refactors `planningDimensionLabel` to use it); gap-materiality, candidate-status, and authority-impact-kind lines all translated at render time; the "Contract impact" line reworded from a `behavior=X authority=Y` bookkeeping shape to a plain sentence
- `.planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md` (new) — the phase's signed Classic-to-Go synthesis record
- `CLAUDE.md` — new "Classic Visual Voice (v1.28)" section
- `cmd/claudemd_classic_voice_test.go` (new) — `TestEveryVoiceClaimInCLAUDEMDNamesALiveTest`

## Decisions Made

See `key-decisions` in the frontmatter above: the CLI-invocation exemption widening in `rawStateTokenLeaks`; translating enum values only at render time, never inside a JSON-serialized projection struct; the corpus-widening discharge naming `203-01`; and leaving `requirements-completed` empty pending the orchestrator's centralized mark after this final wave merges.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `rawStateTokenLeaks` false-positived on the discussion card's exact-answer-syntax lines**
- **Found during:** Task 2, first run of `TestVoicedScreensCarryNoRawStateToken` against the full corpus
- **Issue:** The discuss-questions fixture's `ExactAnswerSyntax` lines (`/ant-discuss --answer D1=local`, `/ant-discuss --answer D2=fixed`) were flagged as leaking a `D1=local`/`D2=fixed` bookkeeping pair. These are CLI invocation examples an owner is explicitly told to type verbatim, not internal tokens.
- **Fix:** Widened the check's exemption from "strip a bare slash-command token" to "strip a full CLI invocation (slash command or `aether <verb>`) through the rest of its line," applied before the existing backtick/slash-command stripping.
- **Files modified:** `cmd/classic_voice_corpus_test.go`
- **Verification:** `TestVoicedScreensCarryNoRawStateToken/discuss-questions` passes; `TestRawStateTokenCheckCanFail`'s new subtest proves a backticked occurrence is still exempted; the check still catches the planted `handoff=<id> between_commands_boundary` fixture with no command prefix.
- **Committed in:** `025fe67f` (Task 2 commit)

**2. [Rule 1 - Bug] Four genuine internal-state-token leaks found by the corpus-wide token gate**
- **Found during:** Task 2, running the check against the full corpus
- **Issue:** `lifecycle_status_render.go`'s phase-status line rendered `phase.Current.Status` (e.g. `in_progress`) verbatim in `[...]`. `planning_visuals.go` rendered a gap's `Materiality` (`non_material`), a plan candidate's `CandidateStatus` (`pending_review`), and a semantic delta's `AuthorityImpact` kind (`owner_decision`) directly at three separate call sites (`renderPlanningIterationVisual`'s weakest-gap line, `renderPlanningCandidateVisual`'s residual-gaps loop, `renderPlanningStopVisual`'s residual-gaps loop, and `projectPlanningSemanticDelta`'s authority-impact list).
- **Fix:** Translated each value to plain words (`strings.ReplaceAll(value, "_", " ")`, via a new shared `planningEnumLabel` helper in `planning_visuals.go`) at the exact point it becomes rendered text -- never inside the projection struct fields that also feed the `planning_projection` JSON envelope, so no machine-readable claim changed, only the human-readable rendering.
- **Files modified:** `cmd/lifecycle_status_render.go`, `cmd/planning_visuals.go`
- **Verification:** `TestVoicedScreensCarryNoRawStateToken` passes for `status-full`, `plan-iteration`, `plan-candidate-review`, and `plan-stop`; `TestPlanningVisualsJSONStopLabelsAndEvidence`, `TestPlanningVisualsCandidateAuthority`, and every other pre-existing `TestPlanningVisuals*`/`TestLifecycleStatus199*` test re-run unmodified and still passes.
- **Committed in:** `025fe67f` (Task 2 commit)

**3. [Rule 1 - Bug] The specification card's "Contract impact" line carried a bookkeeping `key=value` shape**
- **Found during:** Task 2, code inspection while auditing every `fmt.Sprintf` site the check's patterns could plausibly reach (this specific line is not exercised by any registered corpus fixture's `Impact.material()` branch, so the test itself did not catch it -- found by direct read, then fixed as the same class of bug the task explicitly targets)
- **Issue:** `renderPlanningDecisionVisual`'s per-choice "Contract impact" line rendered `behavior=%s authority=%s scope=%s risk=%s acceptance=%s` -- a raw bookkeeping shape, not a sentence, regardless of what the five values themselves were (free text, not enums).
- **Fix:** Reworded to `Contract impact — behavior: %s; authority: %s; scope: %s; risk: %s; acceptance: %s`, removing every `=` while keeping all five values.
- **Files modified:** `cmd/planning_visuals.go`
- **Verification:** `go build`/`go vet` clean; `TestPlanningVisualsDecisionSemanticAuthority` (the pre-existing test exercising this render path) still passes.
- **Committed in:** `025fe67f` (Task 2 commit)

---

**Total deviations:** 3 auto-fixed (3 Rule 1 bugs — one false positive in this plan's own new check, three genuine production leaks the check's real run surfaced, plus one latent instance of the same bookkeeping-shape defect found by direct inspection)
**Impact on plan:** All three are squarely inside this task's own mandate (no owner-facing line shows an internal token or bookkeeping pair, fixed at the writer). No scope creep — every fix is a pure string-formatting change; no projection struct's serialized field values changed, confirmed by the JSON-contract tests passing unmodified.

## Issues Encountered

- **Local disk space exhaustion mid-task:** the `go build`/`go test` cache (`~/Library/Caches/go-build`) had grown to 45G, filling the shared disk to 98% capacity and causing a `no space left on device` build failure partway through Task 2. Ran `go clean -cache` (a standard, non-destructive Go tooling operation that only removes reusable compiled-object cache, never source or git state) to reclaim ~46G, then retried the build successfully. This is an environment/tooling issue, not a code defect, and the fix touches no tracked file.
- **Full-suite `go test ./cmd -count=1` run (background, per this plan's own `<verify>` for Task 2) reported `FAIL` at the package level with zero individual `--- FAIL:` lines** — every reported problem was a parallel-lane accounting message (`lane parallel-048 exceeded 9m0s: context deadline exceeded`, `missing executed tests: ...`), the same documented pre-existing environment ceiling every prior plan in this phase recorded (`cmd` suite floors ~20 min complete under this machine's kernel spawn cap). Confirmed via `grep -c -- "--- FAIL:"` returning `0` against the full saved output.
- **All five `<known_red_baseline>` tests named in this executor's own brief were individually re-run and confirmed still failing for their documented pre-existing reasons, unrelated to this plan's changes:** `TestCurrentVocabulary199` (its `tracked-occurrences-are-exhaustively-classified` subtest, a pre-existing vocabulary-inventory gap), `TestGoldenBuildVisualOutput`/`TestGoldenContinueVisualOutput` (golden fixtures not refreshed, per this executor's explicit brief not to touch them, diffing against glyph text prior plans in this phase already introduced), `TestGoSourceHintsMatchCobraContracts` (an unrelated `aether interpret` hint, pre-existing), and `TestPhase199GateReceipt` (a stale protected-ownership fingerprint, pre-existing). None of these five touch any file this plan modified.
- Every test named in this plan's own `<verify>`/`<acceptance_criteria>` blocks, plus a broad targeted sweep across `TestVoice*`, `TestPlan*`, `TestLifecycle*`, `TestClaudeMD*`, `TestEveryLiveColony*`, and `TestEveryLearning*` prefixes, plus `go build ./cmd/aether` and `go vet ./...`, were run to completion and pass — see Self-Check below for the exact commands.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Phase 202.1 (Classic Visual Voice) is complete: all 7 plans have SUMMARYs. The voice is now asserted by three corpus-wide gates (coverage, density, token) rather than described, and the phase's signed synthesis record and CLAUDE.md claim both exist with named, live tests behind them.
- The corpus-widening discharge (this phase deliberately does not widen `cmd/testdata/classic-contract/v1/schema.json`) is recorded in `202.1-CLASSIC-SYNTHESIS.md` §8, naming Phase 203's already-approved `203-01-PLAN.md` as the next widening's owner. No action needed from this phase; the next phase's own first plan already accounts for it.
- Requirement `CEC-09` is declared by all seven plans in this phase (01 through 07); this is the last of the seven to produce a SUMMARY. The orchestrator should mark it complete centrally once this final wave merges, per the shared-ID gate's design (`requirements.ready-ids` was not available in this worktree's installed `gsd-tools`, same limitation plan 05 recorded).
- Two genuine production defects (the phase-status leak, the three planning-card enum leaks) that predate this phase and were never caught by any prior check are now fixed and locked by the corpus-wide token gate, which will catch a recurrence of the same class in any future screen that joins the corpus.

## Self-Check: PASSED

- All 3 created files verified present on disk (`test -f`): `cmd/classic_voice_coverage_test.go`, `cmd/claudemd_classic_voice_test.go`, `.planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md`.
- All 4 modified files verified changed (`git diff --stat` against the plan's base commit `0bcbdb79`): `cmd/classic_voice_corpus_test.go`, `cmd/lifecycle_status_render.go`, `cmd/planning_visuals.go`, `CLAUDE.md`.
- All 3 task commit hashes (`0a81263b`, `025fe67f`, `018d6851`) verified present in `git log --oneline`.
- Every `<acceptance_criteria>` item for Tasks 1-3 re-verified via its exact stated command: the named `go test -run` invocations for all three tasks (all pass); `grep -c` for renderer-function-name and decimal-literal absence in `classic_voice_coverage_test.go` (both zero); the `go test ./cmd -run '...' -v | grep -c '^ *--- PASS'` count for Task 1 (26, ≥ 18 required); the five `grep`/`test -f` checks plus `go test`/`go vet` for Task 3's synthesis-document verify block (all pass).
- Plan-level `<verification>` re-checked: all eight screen families registered and measured, removing one fails the ratchet (pass); every registration meets or exceeds the reference density (pass); no registration shows an internal token or bookkeeping pair (pass, after the two writer fixes); both new gates proven able to fail on planted input (pass); every guardrail named in RESEARCH.md's do-not-regress table re-run and passing (`TestLifecycleStatus199NoInventedEvidence`, `TestNextActionCardSpeaksPlainEnglish`, `TestPlainEnglishCheckCanFail`, `TestNextActionNeverHardcoded`, `TestNextActionHardcodeBaselineMatchesLive`, `TestNextActionHardcodeDetectsAPlantedViolation`, `TestResolveNextActionIsPure`, `TestNextActionCardRefusesUnsafeClear`, `TestEveryLifecycleCommandEndsWithNextAction`, `TestEveryLiveEventGoesThroughOneBoundary`, `TestOneLiveEventModelOnly`, `TestClosingCardsAreUnchangedByTheGreetingBlock`, `TestCasteIdentityUsesHouseStyle` — all pass); the synthesis artifact exists with the template's required sections, delivered evidence, and corpus discharge (pass); the shipped CLAUDE.md claim is test-locked (pass).
- `go build ./cmd/aether` and `go vet ./...` both clean.

---
*Phase: 202.1-classic-visual-voice*
*Completed: 2026-09-12*
