---
phase: 202.1-classic-visual-voice
verified: 2026-09-12T20:38:23Z
status: passed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 202.1: Classic Visual Voice Verification Report

**Phase Goal:** Restore the Classic per-line visual voice to every ordinary lifecycle screen, and make it a guarantee rather than a description.
**Verified:** 2026-09-12T20:38:23Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | One glyph vocabulary (`voiceGlyphMap`/`voiceGlyph`/`voiceLine`) exists for the whole runtime, with no second hand-rolled glyph table anywhere in `cmd/` | ✓ VERIFIED | `voiceGlyphMap` at `cmd/codex_visuals.go:218-249` (29 semantic kinds, `goal`→👑 distinct from `phase`→📍); `TestVoiceGlyphsHaveOneTable` and `TestVoiceGlyphTableGuardCanFail` pass — the latter proves the AST guard genuinely fires on a planted duplicate, not just on presence |
| 2 | The reference density figure is computed at test time from real Classic-era source (commits `3a5b81c2`/`794fadfb`), never typed in, and drift-checked against the live git objects | ✓ VERIFIED | `cmd/testdata/classic-voice/manifest.json` names both commits; both objects independently confirmed present (`git cat-file -e`) and correctly identified (`git log -1` on each); `TestClassicReferenceFixtureMatchesTheReferenceCommit` re-extracts from the git object itself (not a stored copy) and compares digests — genuinely fails on drift, not tautological; `TestClassicReferenceDensityIsDerivedFromTheReferenceCommit` passes |
| 3 | The measurement can fail in both directions (a planted flat screen scores below the figure, a planted decorated screen at/above it) | ✓ VERIFIED | `TestVoiceDensityCanFail` and `TestCorpusDensityGateCanFail` (plants a literal unglyphed 4-line screen through the real `voiceDensity` function) both pass |
| 4 | Every ordinary lifecycle screen (plan, discuss, spec, status×2, build×2, continue×2, seal, what-next — 21 registrations, 8 families) is glyph-led and meets the reference density | ✓ VERIFIED | `go test ./cmd -run TestEveryVoicedScreenMeetsTheReferenceDensity -v` — 21/21 subtests pass; `TestEveryOrdinaryScreenIsMeasuredForVoice` (the coverage ratchet) passes and its own two negative subtests (family removed, corpus emptied) prove it can fail |
| 5 | Next-step menus name each option's effect beside its command, with a leading glyph, and the command text itself is unchanged | ✓ VERIFIED | `TestNextStepMenuNamesItsEffectBesideItsCommand` and `TestWhatNextCardMeetsTheReferenceDensity` (both claude/codex spellings) pass |
| 6 | No owner-facing line exposes a raw internal identifier, hash, or state token in place of a sentence, including the two confirmed pause/resume identifier leaks | ✓ VERIFIED | `TestVoicedScreensCarryNoRawStateToken` (21/21 registrations) and `TestRawStateTokenCheckCanFail` pass; `pauseResumeLifecycleEvent`'s signature now takes only a `lifecycleEventSentence`, produced by two constructors covering all 7 pause boundaries + 4 recovery-provenance values (`cmd/session_flow_cmds.go:1037-1077`), confirmed at both real call sites (lines 379, 636); `TestLifecycleEventSentenceTypeCannotBeBypassed`/`...GuardCanFail` pass |
| 7 | Voicing changed no command, recommendation, claim, gate result, or alternative — presentation only | ✓ VERIFIED | `TestBuildScreenFactsUnchanged`, `TestContinueScreenFactsUnchanged`, `TestSealScreenFactsUnchanged`, `TestStatusUnchangedFacts`, `TestPlanningCardsKeepEveryFigureAndIdentifier`, `TestSpecCardLabelsEveryIdentifier`, `TestDiscussCardKeepsEveryQuestionField`, `TestPlanVisualStillDelegatesToThePrimaryCard`, `TestNextActionHardcodeBaselineMatchesLive`, `TestNextActionNeverHardcoded` all pass |
| 8 | The two defects the phase's own code review found (glyph override-precedence bypass; unvoiced/double-voiced Next Up primary line, plus a glyph leak into machine-readable JSON) are genuinely fixed at the writer, not merely described as fixed | ✓ VERIFIED | `planningStripLeadingVoiceGlyph` now resolves through `voiceGlyph(kind)` (`cmd/planning_visuals.go:1171-1181`), honouring the operator override; `renderNextUp`'s primary line now uses `voiceLine("next", ...)` (`cmd/codex_visuals.go:666-668`); `nextActionPrimarySuggestion`/`nextActionAlternativeSuggestions` are explicitly unglyphed and feed `result["next"]` at `cmd/closeout_cmd.go:57` and `cmd/codex_plan.go:341` verbatim (confirmed by direct read); `TestNextUpLinesCarryExactlyOneGlyph`, `TestMachineReadableNextSuggestionCarriesNoGlyph`, `TestNextUpFunnelGuardsCanFail` all pass |
| 9 | The synthesis protocol's mandatory artifact is discharged, and CLAUDE.md's shipped claim about the voice guarantee names live tests, with a guard that can fail on a fabricated name | ✓ VERIFIED | `.planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md` (176 lines) cites `202.1-RESEARCH.md`, carries `SYN-VOICE-01..05` with plan/test evidence, and records the corpus-widening discharge naming Phase 203's `203-01` as owner; CLAUDE.md's "Classic Visual Voice (v1.28)" section (line 1390+) exists on disk with each claim naming a test; `TestEveryVoiceClaimInCLAUDEMDNamesALiveTest` passes and its own subtest proves it can fail on a fabricated test name |

**Score:** 9/9 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `cmd/codex_visuals.go` (`voiceGlyphMap`, `voiceGlyph`, `voiceLine`, `signalTypeGlyph`) | One semantic glyph vocabulary + funnel | ✓ VERIFIED | Present, wired into next-up, build, continue, seal renderers; confirmed by direct read and passing tests |
| `cmd/testdata/classic-voice/manifest.json` + 6 extracted blocks | Provenance for the reference density | ✓ VERIFIED | 6 blocks, both cited commits (`3a5b81c2`, `794fadfb`) verified present in git; digest-drift guard re-extracts from the object itself |
| `cmd/classic_voice_metric_test.go` | Density measurement + reference figure | ✓ VERIFIED | `voiceDensity`/`classicReferenceDensity` present and tested |
| `cmd/classic_voice_corpus_test.go` | Screen registry + widened plain-English scan | ✓ VERIFIED | `voiceScreenRegistry`/`registerVoiceScreen`, `rawStateTokenLeaks` present |
| `cmd/lifecycle_status_render.go` | Voiced full/compact status cards, `casteIdentity()` wiring | ✓ VERIFIED | `lifecycleStatusActorLine` calls `casteIdentity()`; history reads via `nextActionEventSentence` |
| `cmd/discuss.go`, `cmd/spec_cmd.go` | Voiced discussion/spec cards | ✓ VERIFIED | Both route through `voiceLine`; `renderSpecCommandVisualSection` takes explicit `lineType` |
| `cmd/session_flow_cmds.go` | `lifecycleEventSentence` type + constructors | ✓ VERIFIED | Type present, only two constructors produce it, AST guard confirms |
| `cmd/planning_visuals.go` | Voiced planning cards + fixed overflow-exemption helper | ✓ VERIFIED | `planningStripLeadingVoiceGlyph` resolves through `voiceGlyph` (post-repair) |
| `cmd/classic_voice_coverage_test.go` | Coverage ratchet + corpus-wide density/token gates | ✓ VERIFIED | `TestEveryOrdinaryScreenIsMeasuredForVoice`, `TestEveryVoicedScreenMeetsTheReferenceDensity`, `TestVoicedScreensCarryNoRawStateToken` all present and passing |
| `.planning/phases/202.1-classic-visual-voice/202.1-CLASSIC-SYNTHESIS.md` | Signed synthesis record | ✓ VERIFIED | Present, substantive (176 lines), cites RESEARCH.md, carries SYN-VOICE-01..05 |
| `CLAUDE.md` "Classic Visual Voice (v1.28)" section | Shipped claim naming live tests | ✓ VERIFIED | Present at line 1390; `TestEveryVoiceClaimInCLAUDEMDNamesALiveTest` passes and can fail |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `cmd/classic_voice_metric_test.go` | `cmd/codex_visuals.go` | glyph set read from production tables | ✓ WIRED | Confirmed by direct read; no re-typed glyph literal in tests |
| `cmd/classic_voice_nextaction_test.go` | `cmd/testdata/classic-voice/manifest.json` | threshold derived from extracted blocks | ✓ WIRED | `classicReferenceDensity` reads the manifest at test time |
| `cmd/lifecycle_status_render.go` | `cmd/codex_visuals.go` | `casteIdentity()` call | ✓ WIRED | Confirmed: `lifecycleStatusActorLine` opens with `casteIdentity(caste)` |
| `cmd/lifecycle_status_render.go` | `cmd/next_action.go` | history reads via `nextActionEventSentence` | ✓ WIRED | Confirmed by test `TestStatusHistoryShowsASentenceNotTheStoredRecord` |
| `cmd/session_flow_cmds.go` | `cmd/next_action.go` | writer now satisfies reader's sentence contract | ✓ WIRED | `git diff --quiet cmd/next_action.go` against base — reader untouched, writer fixed |
| `cmd/planning_visuals.go` | `cmd/codex_visuals.go` | shared `voiceGlyph`/`voiceLine` funnel, no local symbol table | ✓ WIRED | Confirmed; `planningStripLeadingVoiceGlyph` now itself calls `voiceGlyph` |
| `CLAUDE.md` | `cmd/classic_voice_coverage_test.go` | every voice claim names a live test | ✓ WIRED | `TestEveryVoiceClaimInCLAUDEMDNamesALiveTest` passes |

### Behavioral Spot-Checks / Test Execution

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Build succeeds | `go build ./cmd/aether` | clean | ✓ PASS |
| Vet clean | `go vet ./...` | clean | ✓ PASS |
| All 21 corpus registrations meet density | `go test ./cmd -run TestEveryVoicedScreenMeetsTheReferenceDensity -v` | 21/21 pass | ✓ PASS |
| Coverage ratchet + its can-fail subtests | `go test ./cmd -run TestEveryOrdinaryScreenIsMeasuredForVoice -v` | 3/3 pass (incl. 2 negative subtests) | ✓ PASS |
| No raw state token on any registered screen | `go test ./cmd -run TestVoicedScreensCarryNoRawStateToken -v` | 21/21 pass | ✓ PASS |
| Density gate can genuinely fail | `go test ./cmd -run TestCorpusDensityGateCanFail -v` | pass (planted flat screen scores below reference) | ✓ PASS |
| Digest-drift guard re-extracts from real git objects | `go test ./cmd -run TestClassicReferenceFixtureMatchesTheReferenceCommit -v` | 6/6 subtests pass; both cited commits independently confirmed present via `git cat-file -e` | ✓ PASS |
| Glyph-table AST guard fires on planted duplicate | `go test ./cmd -run TestVoiceGlyphTableGuardCanFail -v` | pass | ✓ PASS |
| Lifecycle-event-sentence class guard fires on planted bypass | `go test ./cmd -run TestLifecycleEventSentenceGuardCanFail -v` | pass | ✓ PASS |
| CLAUDE.md claim guard fires on fabricated test name | `go test ./cmd -run TestEveryVoiceClaimInCLAUDEMDNamesALiveTest -v` | pass, subtest proves failure mode | ✓ PASS |
| Next Up double-glyph / JSON-leak repair holds | `go test ./cmd -run 'TestNextUpLinesCarryExactlyOneGlyph|TestMachineReadableNextSuggestionCarriesNoGlyph|TestNextUpFunnelGuardsCanFail' -v` | 3/3 pass | ✓ PASS |
| All screen-family unit suites (build/continue/seal/status/discuss/spec/plan) | targeted `-run` invocations, see below | all pass | ✓ PASS |
| Known-red baseline still fails identically at pre-phase commit | `TestGoldenBuildVisualOutput`, `TestPhase199GateReceipt` re-run against a worktree at `2757534e` | both fail identically (pre-existing) | ✓ CONFIRMED PRE-EXISTING |
| Guardrail regressions (hardcode ratchet, pure resolver, unsafe-clear guard) | `go test ./cmd -run 'TestNextActionHardcodeBaselineMatchesLive|TestNextActionNeverHardcoded|TestNextActionHardcodeDetectsAPlantedViolation|TestResolveNextActionIsPure|TestNextActionCardRefusesUnsafeClear|TestNextActionCardSpeaksPlainEnglish|TestLifecycleStatus199NoInventedEvidence|TestLifecycleStatus199FullOrder' -v` | 8/8 pass | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CEC-09 | Plans 01–07 (all declare it) | Every ordinary lifecycle screen renders in the Classic voice at reference density; next-step menus name effect beside command; no raw internal identifier/hash/state token; asserted, not described | ✓ SATISFIED (functionally) — checkbox not yet flipped in REQUIREMENTS.md | All four clauses independently verified above via passing, fail-capable tests. `REQUIREMENTS.md` line 41 still shows `- [ ]` for CEC-09 — an unflipped bookkeeping checkbox, not a functional gap (see Anti-Patterns/Gaps below) |

No orphaned requirements: `grep -E "Phase 202.1" .planning/REQUIREMENTS.md` maps only CEC-09 to this phase, and all seven plans declare it.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any file this phase touched | — | none |

Two pre-existing code-review findings (WR-03: `lifecycleStatusGlyphTokens` handles only the variation-selector case, not the wider ZWJ/combining-mark class `planningRuneWidth` covers; IN-01/IN-02: uniform `feedback` glyph for all signal types, `checkpoint` glyph reused for "Success Criteria"/"Repairs") were explicitly deferred/accepted by the orchestrator as documented, non-blocking, cosmetic limitations with no live bug against today's glyph set. Recorded here for completeness, not treated as gaps.

### Human Verification Required

None. Every must-have in this phase resolves to a programmatically verifiable, fail-capable test, and all were independently re-run (not merely trusted from SUMMARY.md) and pass.

### Gaps Summary

No functional gaps found. One administrative/bookkeeping item: `.planning/REQUIREMENTS.md` line 41 still shows CEC-09 as `- [ ]` (unchecked) even though all seven declaring plans have SUMMARYs and the requirement's four clauses are independently verified as satisfied above. Plans 05 and 07 both explicitly noted this was left for the orchestrator to flip centrally once the final wave merged (the installed `gsd-tools` in the plan-execution worktrees did not expose the `requirements.ready-ids` gate). This is a paperwork item, not a functional deficiency — every test backing CEC-09's guarantee exists and passes. Recommend flipping the REQUIREMENTS.md checkbox as a trivial follow-up; not a blocker to proceeding.

---

_Verified: 2026-09-12T20:38:23Z_
_Verifier: Claude (gsd-verifier)_
