---
phase: 130-host-surface-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/host_cmd.go
  - cmd/status.go
  - .aether/ts-host/src/host.ts
  - .aether/ts-host/src/types.ts
  - .aether/ts-host/src/go-bridge.ts
  - .aether/docs/host-command-reference.md
autonomous: true
requirements:
  - HSC-01
  - HSC-02
  - HSC-03
  - HSC-04
  - HSC-05
  - HSC-06
  - HSC-07

must_haves:
  truths:
    - "`aether host plan` accepts --depth and --planning-depth and executes end-to-end"
    - "`aether host build` dispatches to TS host with correct args"
    - "`aether host continue` accepts --verification-depth and executes end-to-end"
    - "`aether host oracle` dispatches Oracle lifecycle through TS host"
    - "`aether host watch` accepts --no-dashboard and renders status"
    - "`aether host swarm` accepts <target> and --no-dashboard"
    - "`aether host lifecycle` orchestrates full plan->build->continue"
    - "All flags match between Go CLI definition, TS host arg parsing, and wrapper markdown"
  artifacts:
    - path: "cmd/host_cmd.go"
      provides: "Go CLI host command with all subcommands and flags"
    - path: ".aether/ts-host/src/host.ts"
      provides: "TS host entry point handling all 7 subcommands"
    - path: ".aether/docs/host-command-reference.md"
      provides: "Human-readable reference of every host subcommand and flag"
  key_links:
    - from: "cmd/host_cmd.go"
      to: ".aether/ts-host/src/host.ts"
      via: "Go spawns TS host process with subcommand + flags"
    - from: ".aether/ts-host/src/host.ts"
      to: "cmd/*.go"
      via: "callGoJSON invokes Go CLI commands"
---

<objective>
Ensure `aether host` exposes every workflow the docs claim: plan, build, continue, oracle, watch, swarm, lifecycle. Fix any missing subcommands, broken flags, or doc/CLI mismatches.

Purpose: Close the gap between documented capabilities and actual CLI behavior so users can trust the command reference.
Output: All 7 host subcommands working end-to-end with correct flags, plus a reference doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

## Key interfaces from existing code

From `cmd/host_cmd.go`:
- The `host` command currently dispatches to the TS host binary
- Subcommands are passed as positional args to the TS host
- Flags are passed through via environment variables and positional args

From `.aether/ts-host/src/host.ts`:
- Commands: plan, build, continue, oracle, lifecycle, watch, swarm
- Parses `--no-dashboard` flag
- Uses `callGoJSON` to invoke Go CLI
- Writes JSON result to stdout

From `.aether/ts-host/src/types.ts`:
- `GoBridgeOptions` with `goBinaryPath`, `cwd`, `outputMode`
- Command-specific options extend `GoBridgeOptions`

From `cmd/plan.go`, `cmd/build.go`, `cmd/continue.go`:
- Flags like `--depth`, `--planning-depth`, `--verification-depth`
- These are Go CLI flags, not TS host flags

## Known issues

1. The TS host may not be passing through all flags correctly to Go CLI
2. Some `aether host` subcommands may not exist in the Go CLI definition
3. `--no-dashboard` exists but other flags may be missing
4. Documentation may claim commands/flags that don't exist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit current host command surface</name>
  <files>cmd/host_cmd.go, .aether/ts-host/src/host.ts</files>
  <read_first>
    - cmd/host_cmd.go (host command definition)
    - .aether/ts-host/src/host.ts (command switch)
    - .aether/ts-host/src/types.ts (option types)
  </read_first>
  <action>
Audit the current host command surface:

1. List all subcommands defined in `cmd/host_cmd.go`
2. List all subcommands handled in `.aether/ts-host/src/host.ts`
3. List all flags accepted by each Go CLI command (plan, build, continue, oracle, watch, swarm, lifecycle)
4. List all flags parsed by the TS host
5. Identify mismatches:
   - Missing subcommands in host.ts
   - Missing flags in host.ts arg parsing
   - Flags documented but not implemented
   - Flags implemented but not documented

Document findings in a table:
| Subcommand | Go Flags | TS Host Flags | Status |
|------------|----------|---------------|--------|
  </action>
  <verify>
    <manual>Review audit table for completeness</manual>
  </verify>
  <done>
    - Audit table exists showing all subcommands and flags
    - Mismatches identified and documented
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix missing subcommands and flags in TS host</name>
  <files>.aether/ts-host/src/host.ts, .aether/ts-host/src/types.ts</files>
  <read_first>
    - .aether/ts-host/src/host.ts (current command switch)
    - .aether/ts-host/src/types.ts (option interfaces)
    - cmd/plan.go (plan flags)
    - cmd/build.go (build flags)
    - cmd/continue.go (continue flags)
  </read_first>
  <action>
Fix the TS host to handle all 7 subcommands correctly:

1. Ensure all subcommands exist in the host.ts switch:
   - plan, build, continue, oracle, lifecycle, watch, swarm

2. Add missing flag parsing:
   - `--depth` (string) for plan/build/continue
   - `--planning-depth` (string) for plan
   - `--verification-depth` (string) for continue
   - `--no-dashboard` (boolean) for watch/swarm
   - Any other flags found in the audit

3. Update `parseArgs()` to extract these flags from process.argv

4. Pass flags through to Go CLI via `callGoJSON` or environment variables

5. Update `printUsage()` to document all subcommands and flags

Rules:
- Keep changes minimal — only add what's missing
- Don't refactor working code
- Maintain exactOptionalPropertyTypes compatibility
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npm run typecheck</automated>
  </verify>
  <done>
    - All 7 subcommands handled in host.ts
    - All flags parsed and passed through
    - Usage text documents all commands and flags
    - npm run typecheck passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Go CLI host command if needed</name>
  <files>cmd/host_cmd.go</files>
  <read_first>
    - cmd/host_cmd.go (host command)
    - .aether/ts-host/src/host.ts (after Task 2 changes)
  </read_first>
  <action>
Update `cmd/host_cmd.go` if needed:

1. Ensure the host command passes through all subcommands correctly
2. Ensure flags are passed to the TS host process correctly
3. Check if any new flags need to be added to the Go CLI flag set
4. Verify error handling for unknown subcommands
5. Test that `aether host <subcommand>` works for all 7 subcommands

If no changes are needed, document that the Go side is already correct.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && go test ./cmd/...</automated>
  </verify>
  <done>
    - Go host command passes all subcommands correctly
    - go test ./cmd/... passes
  </done>
</task>

<task type="auto">
  <name>Task 4: Create host command reference doc</name>
  <files>.aether/docs/host-command-reference.md</files>
  <read_first>
    - .aether/ts-host/src/host.ts (final version)
    - cmd/host_cmd.go
    - .aether/docs/publish-update-runbook.md (doc style reference)
  </read_first>
  <action>
Create `.aether/docs/host-command-reference.md` documenting:

1. **Overview**: What `aether host` does and when to use it
2. **Subcommands**: One section per subcommand with:
   - Purpose
   - Usage syntax
   - All flags with types and defaults
   - Examples
3. **Flag reference table**: All flags across all subcommands
4. **Error messages**: Common errors and what they mean
5. **Architecture note**: How host delegates to TS host which delegates to Go

Subcommands to document:
- `aether host plan [flags]`
- `aether host build [flags]`
- `aether host continue [flags]`
- `aether host oracle [flags]`
- `aether host watch [flags]`
- `aether host swarm <target> [flags]`
- `aether host lifecycle [flags]`
  </action>
  <verify>
    <manual>Review doc for accuracy against actual code</manual>
  </verify>
  <done>
    - host-command-reference.md exists and is accurate
    - All 7 subcommands documented with flags
    - Examples provided for common use cases
  </done>
</task>

<task type="auto">
  <name>Task 5: Manual smoke test all host subcommands</name>
  <files></files>
  <read_first>
    - .aether/docs/host-command-reference.md
  </read_first>
  <action>
Run manual smoke tests for each host subcommand:

1. `aether host plan --help` (or equivalent)
2. `aether host plan --depth balanced --planning-depth standard`
3. `aether host build`
4. `aether host continue --verification-depth heavy`
5. `aether host watch --no-dashboard`
6. `aether host swarm "test bug" --no-dashboard`
7. `aether host oracle`
8. `aether host lifecycle`

For each test, verify:
- Command executes without panic
- Help text or output is reasonable
- Flags are accepted (not rejected as unknown)

Note any failures and fix them before completing this task.
  </action>
  <verify>
    <manual>All 8 smoke tests executed and passed</manual>
  </verify>
  <done>
    - All 7 subcommands execute without panic
    - All documented flags are accepted
    - No CLI rejects documented commands
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go CLI -> TS host | Go spawns TS process; TS must not crash on bad input |
| TS host -> Go CLI | TS calls Go via subprocess; flags must match |
| Docs -> Reality | Doc claims must match CLI behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-01 | Spoofing | Unknown subcommand | mitigate | host.ts and host_cmd.go both validate subcommands |
| T-130-02 | Denial of Service | Invalid flag crashes TS host | mitigate | parseArgs catches unknown flags gracefully |
| T-130-03 | Information Disclosure | --help reveals internal paths | accept | Paths are not sensitive |
</threat_model>

<verification>
1. `cd .aether/ts-host && npm run typecheck` passes
2. `cd .aether/ts-host && npm test` passes
3. `go test ./cmd/...` passes
4. All 7 `aether host` subcommands execute without panic
5. All documented flags accepted by CLI
6. `.aether/docs/host-command-reference.md` is accurate
</verification>

<success_criteria>
- All 7 `aether host` subcommands exist and execute
- `--depth`, `--planning-depth`, `--verification-depth`, `--no-dashboard` flags work
- No mismatch between docs, Go CLI, and TS host
- Reference doc is accurate and complete
- All typecheck and test commands pass
</success_criteria>

<output>
After completion, create `.planning/phases/130-host-surface-completeness/130-01-SUMMARY.md`
</output>
