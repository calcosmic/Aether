---
phase: 101-platform-parity-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/101-platform-parity-verification/KNOWN-GAPS.md
autonomous: true
requirements: [PLAT-01, PLAT-02, PLAT-03]

must_haves:
  truths:
    - "Every known parity gap is classified by severity (Critical/Warning/Info)"
    - "Gap counts per tier are documented"
    - "No fix suggestions are included per D-02"
  artifacts:
    - path: ".planning/phases/101-platform-parity-verification/KNOWN-GAPS.md"
      provides: "Human-readable parity gap report for Phase 105"
      contains: "## Critical"
  key_links:
    - from: ".planning/phases/101-platform-parity-verification/KNOWN-GAPS.md"
      to: ".planning/phases/105-*/105-*-PLAN.md"
      via: "Phase 105 consumes this report to resolve gaps"
      pattern: "Phase 105"
---

<objective>
Create a severity-classified KNOWN-GAPS.md report documenting all known parity gaps across the 5 surfaces for Phase 105 to act on.

Purpose: Per D-02, the report includes counts per tier and gap descriptions but no fix suggestions. Phase 105 uses this to plan remediation work. The parity test in Plan 01 freezes current state; this document explains what the frozen state means.

Output: `.planning/phases/101-platform-parity-verification/KNOWN-GAPS.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-platform-parity-verification/101-CONTEXT.md
@.planning/phases/101-platform-parity-verification/101-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create KNOWN-GAPS.md with severity-classified parity gap report</name>
  <files>.planning/phases/101-platform-parity-verification/KNOWN-GAPS.md</files>
  <read_first>
    - .planning/phases/101-platform-parity-verification/101-CONTEXT.md (D-01 through D-08 severity definitions and scope)
    - .planning/phases/101-platform-parity-verification/101-RESEARCH.md (verified surface counts, alias map, known drift details)
    - cmd/command_guide.go (commandGuideLiteralCommands list -- 52 literal + 9 intelligent = 61 entries vs 60 YAML)
    - cmd/audit_catalog.go (buildAuditCatalog produces 377 Cobra commands)
    - .aether/commands/ directory (60 YAML files)
    - .claude/commands/ant/ directory (60 wrapper files)
    - .opencode/commands/ant/ directory (60 wrapper files)
    - .codex/agents/ directory (27 TOML agent files)
  </read_first>
  <action>
Create `.planning/phases/101-platform-parity-verification/KNOWN-GAPS.md` with the following structure and content. The file must be a human-readable markdown report classifying known parity gaps by the three-tier system defined in D-01.

**Header section:**
```markdown
# Platform Parity Known Gaps

**Phase:** 101 (Platform Parity Verification)
**Generated:** 2026-05-07
**Status:** For Phase 105 remediation

## Summary

| Severity | Count |
|----------|-------|
| Critical | 0     |
| Warning  | 1     |
| Info     | 2     |
```

**Critical section (## Critical):**
- Currently empty. The alias resolution map in the parity test correctly maps all 11 YAML-to-runtime name differences. No phantom commands exist.
- State: "No critical gaps found. All wrapper and YAML command names resolve to valid runtime commands after alias resolution."

**Warning section (## Warning):**
- **W-01: Lifecycle commands missing from Codex TOML agents** (per D-05)
  - The 16 lifecycle commands (init, discuss, colonize, plan, build, continue, seal, entomb, publish, update, recover, status, resume, watch, patrol, profile) have YAML definitions and Claude/OpenCode wrappers but no dedicated Codex TOML agent entries.
  - Codex accesses these commands through `commandGuideCatalog()` (the command-guide surface), not through TOML agents. The command-guide covers all 60 YAML commands.
  - Severity: Warning because lifecycle commands are the most-used surface and their absence from TOML agents means Codex users must rely on the command-guide path instead of agent-native discovery.

**Info section (## Info):**
- **I-01: 33 YAML commands have no Codex TOML agent** (per D-04)
  - Of the 60 YAML commands, 27 have corresponding Codex TOML agent files in `.codex/agents/`. The remaining 33 commands have no TOML agent. This is by design -- TOML agents represent worker castes (builder, watcher, scout, etc.), not command wrappers.
  - All 60 commands ARE covered by the `commandGuideCatalog()` function which provides Codex orchestration guidance.

- **I-02: Command-guide has 61 entries vs 60 YAML files**
  - The command-guide catalog includes `resume-colony` as both a literal command entry AND the fact that `resume` (the YAML wrapper name) maps to `resume-colony` (the Cobra primary). This is correct behavior -- `resume-colony` is a distinct runtime command that appears in the guide alongside the alias `resume`.
  - The parity test accounts for this by checking YAML names against the guide catalog, not the reverse.

**Scope section:**
- Note that this report covers command name parity only (per D-07). Flag parity and description parity are not audited in this phase.
- Note that the Codex TOML coverage gap (I-01) is structural -- TOML agents are worker definitions, not command wrappers. This is not a drift issue.

**Do NOT include:**
- Fix suggestions (per D-02)
- Flag-level comparisons
- Description text comparisons
- Remediation timelines
</action>
  <verify>
    <automated>test -f .planning/phases/101-platform-parity-verification/KNOWN-GAPS.md && grep -c "## Critical" .planning/phases/101-platform-parity-verification/KNOWN-GAPS.md && grep -c "## Warning" .planning/phases/101-platform-parity-verification/KNOWN-GAPS.md && grep -c "## Info" .planning/phases/101-platform-parity-verification/KNOWN-GAPS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/101-platform-parity-verification/KNOWN-GAPS.md exists
    - File contains "## Critical" section header
    - File contains "## Warning" section header
    - File contains "## Info" section header
    - File contains "W-01" entry about lifecycle commands missing from Codex TOML
    - File contains "I-01" entry about 33 commands without Codex TOML agents
    - File contains "I-02" entry about command-guide 61 vs YAML 60 entry count
    - File contains summary table with severity counts
    - File does NOT contain the string "fix" in a suggestion context (per D-02)
    - File references Phase 105 as the remediation target
  </acceptance_criteria>
  <done>
    KNOWN-GAPS.md exists in the phase directory with three severity sections (Critical, Warning, Info), a summary table, and exactly 3 documented gaps (0 Critical, 1 Warning, 2 Info). No fix suggestions included. File is ready for Phase 105 consumption.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Document -> Phase 105 | Read-only report consumed by later phase; no execution risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-03 | Tampering | KNOWN-GAPS.md | accept | Committed to git; tampering visible in diff |

This plan creates a documentation file only. No code execution, no state mutation, no security surface.
</threat_model>

<verification>
1. `test -f .planning/phases/101-platform-parity-verification/KNOWN-GAPS.md` passes
2. File contains all 3 severity sections
3. Summary table has correct gap counts
4. No fix suggestions present
</verification>

<success_criteria>
- KNOWN-GAPS.md exists with severity-classified gaps
- 0 Critical, 1 Warning (lifecycle commands in Codex), 2 Info (TOML coverage, guide count)
- No fix suggestions (per D-02)
- File references Phase 105 as remediation target
</success_criteria>

<output>
After completion, create `.planning/phases/101-platform-parity-verification/101-02-SUMMARY.md`
</output>
