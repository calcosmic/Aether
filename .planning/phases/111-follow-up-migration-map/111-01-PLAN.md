---
phase: 111-follow-up-migration-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/docs/migration-map.md
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, MAP-04]

must_haves:
  truths:
    - "Oracle/RALF migration has a milestone-ready plan with phases, requirements, and success criteria (MAP-01)"
    - "Swarm visibility migration has a milestone-ready plan with phases, requirements, and success criteria (MAP-02)"
    - "Parity migration has a milestone-ready plan with phases, requirements, and success criteria (MAP-03)"
    - "The map includes phase numbers, estimated scope per phase, and explicit dependency ordering across all three milestones (MAP-04)"
    - "Every migration step references the Go/TS boundary contract and uses the plan-only/manifest/finalizer pattern from Phase 109"
  artifacts:
    - path: ".aether/docs/migration-map.md"
      provides: "Complete migration map with three milestone sections, phase tables, requirement tables, dependency graph, and boundary contract compliance notes"
      contains: "## Milestone A: Oracle/RALF Confidence Iteration"
      contains: "## Milestone B: Swarm Visibility"
      contains: "## Milestone C: Build/Continue Parity"
  key_links:
    - from: ".aether/docs/migration-map.md"
      to: ".aether/references/contracts/runtime-boundary-contract.md"
      via: "boundary contract compliance section per milestone"
      pattern: "runtime-boundary-contract"
    - from: ".aether/docs/migration-map.md"
      to: ".aether/ts-host/src/lifecycle.ts"
      via: "references runLifecycle pattern as template for all migrations"
      pattern: "runLifecycle|callGoJSON"
---

<objective>
Produce the complete follow-up migration map document for three deferred capabilities: Oracle/RALF confidence iteration, swarm visibility, and broader build/continue parity.

Purpose: This map fulfills the deferral recorded in STATE.md and provides milestone-ready plans that can be directly planned via `/gsd-plan-phase` without additional research or discuss cycles. Each milestone includes phase numbers, requirements with IDs, success criteria, and dependency ordering per D-01 through D-07.

Output: `.aether/docs/migration-map.md` -- a single combined document with three milestone sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-follow-up-migration-map/111-CONTEXT.md
@.planning/phases/111-follow-up-migration-map/111-RESEARCH.md
@.aether/references/contracts/runtime-boundary-contract.md
@.aether/ts-host/src/lifecycle.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Milestone A -- Oracle/RALF Confidence Iteration migration plan</name>
  <files>.aether/docs/migration-map.md</files>
  <read_first>
    - .planning/phases/111-follow-up-migration-map/111-CONTEXT.md (user decisions D-01 through D-07)
    - .planning/phases/111-follow-up-migration-map/111-RESEARCH.md (Oracle loop architecture, scope estimates, pitfalls)
    - .aether/ts-host/src/lifecycle.ts (existing TS host lifecycle pattern to reference)
    - .aether/references/contracts/runtime-boundary-contract.md (boundary contract compliance)
  </read_first>
  <action>
Create `.aether/docs/migration-map.md` with the document header and Milestone A section.

**Document header:**
- Title: "Follow-up Migration Map"
- Purpose: Migration plans for three deferred capabilities from v1.16
- Ordering: Milestone A (Oracle) -> Milestone B (Swarm) -> Milestone C (Parity), per D-02 and D-03
- Reference the boundary contract at `.aether/references/contracts/runtime-boundary-contract.md`
- Reference the TS host lifecycle pattern established in Phase 109 at `.aether/ts-host/src/lifecycle.ts`

**Milestone A section structure (per D-01, D-05):**

1. **Scope** -- What gets migrated, what stays in Go:
   - TS host drives the outer iteration loop (when to start, stop, which iteration)
   - Go owns oracle state, question selection, confidence calculation, workspace management, all `.aether/data/oracle/` writes
   - Migration does NOT move loop logic to TS (per D-05). TS host orchestrates timing, Go handles reasoning.

2. **Phases table** with these 3 phases:
   - Phase 1: Go Oracle Iteration Commands -- Add `oracle-iterate --plan-only` (returns current state + next question + iteration spec as JSON manifest) and `oracle-iterate-finalize --completion-file` (commits iteration results, updates oracle state). Scope: medium. Dependencies: none.
   - Phase 2: TS Host Oracle Lifecycle -- Add `runOracleLifecycle()` to lifecycle.ts that drives the RALF loop: calls `oracle-iterate --plan-only`, dispatches worker per manifest, collects results, calls `oracle-iterate-finalize`. Handles max-iterations, confidence targets, and stop conditions from manifest. Scope: medium-high. Dependencies: Phase 1.
   - Phase 3: Oracle Integration Tests -- End-to-end test that runs Oracle through TS host with a known topic, verifies confidence iteration progresses, state writes go through Go finalizers, no direct `.aether/data/oracle/` writes from TS. Scope: medium. Dependencies: Phase 2.

3. **Requirements table** with requirement IDs following the pattern ORA-01 through ORA-08:
   - ORA-01: Go command `oracle-iterate --plan-only` returns JSON manifest with iteration state, next question, confidence target, max iterations
   - ORA-02: Go command `oracle-iterate-finalize --completion-file` commits iteration results and updates oracle state atomically
   - ORA-03: TS host `runOracleLifecycle()` drives the iteration loop using Go manifests and finalizers
   - ORA-04: TS host dispatches Oracle workers per manifest (same dispatch pattern as build workers)
   - ORA-05: TS host respects stop conditions from manifest (confidence target met, max iterations reached, manual stop)
   - ORA-06: No TS host direct writes to `.aether/data/oracle/` -- all state through Go finalizers
   - ORA-07: Oracle RALF behavior preserved exactly (confidence targets, question selection, phase transitions)
   - ORA-08: End-to-end integration test proves Oracle lifecycle runs through TS host

   Each requirement maps to a phase in the phases table and has 1-3 success criteria.

4. **Boundary Contract Compliance** -- Explicit note that oracle workspace files (state, plan, research plan) in `.aether/data/oracle/` are Go-owned. TS host MUST NOT write these directly. All oracle state mutation goes through `oracle-iterate-finalize`. Reference boundary contract anti-pattern #1.

5. **Risk Assessment:**
   - Risk: Oracle loop has internal state dependencies between iterations (e.g., question selection depends on prior iteration findings). Mitigation: `oracle-iterate --plan-only` returns the full context needed for each iteration; TS host does not need to maintain state between iterations.
   - Risk: Background controller mode (detached Oracle with PID tracking). Mitigation: Map background mode to a future enhancement; initial migration covers foreground interactive mode only.
  </action>
  <acceptance_criteria>
    - File `.aether/docs/migration-map.md` exists
    - File contains section `## Milestone A: Oracle/RALF Confidence Iteration`
    - Milestone A has a `### Phases` table with exactly 3 phases
    - Milestone A has a `### Requirements` table with exactly 8 requirements (ORA-01 through ORA-08)
    - Milestone A has `### Boundary Contract Compliance` section referencing anti-pattern #1 (no TS direct state writes)
    - Milestone A has `### Risk Assessment` section with at least 2 risks and mitigations
    - All phase dependency chains are explicit (Phase 2 depends on Phase 1, Phase 3 depends on Phase 2)
    - No task-level breakdowns (per D-01: milestone-ready granularity, not task-level)
  </acceptance_criteria>
  <verify>
    <automated>grep -c '## Milestone A' .aether/docs/migration-map.md && grep -c 'ORA-0[1-8]' .aether/docs/migration-map.md && grep -c 'oracle-iterate' .aether/docs/migration-map.md</automated>
  </verify>
  <done>Milestone A (Oracle/RALF) section written with 3 phases, 8 requirements, boundary compliance notes, and risk assessment. File created at .aether/docs/migration-map.md.</done>
</task>

<task type="auto">
  <name>Task 2: Write Milestones B, C, dependency graph, and map summary</name>
  <files>.aether/docs/migration-map.md</files>
  <read_first>
    - .aether/docs/migration-map.md (file created by Task 1 -- append to it)
    - .planning/phases/111-follow-up-migration-map/111-CONTEXT.md (D-02, D-03, D-06, D-07)
    - .planning/phases/111-follow-up-migration-map/111-RESEARCH.md (swarm display inventory, remaining flows, scope estimates)
    - .aether/ts-host/src/lifecycle.ts (pattern reference)
  </read_first>
  <action>
Append Milestone B, Milestone C, and the overall dependency graph to the existing `.aether/docs/migration-map.md` file.

**Milestone B: Swarm Visibility (per D-06):**

1. **Scope** -- What gets migrated, what stays in Go:
   - TS host invokes Go `swarm-display-render`, `swarm-display-inline`, `swarm-display-text` commands with `AETHER_OUTPUT_MODE=json` and presents structured output
   - Go owns all colony state data, rendering logic, and ANSI formatting
   - TS host owns presentation layer: calls Go commands, formats JSON output for the user's platform (Claude Code, OpenCode, Codex)

2. **Phases table** with 2 phases:
   - Phase 1: TS Host Swarm Display -- Add `runSwarmDisplay()` to TS host that calls existing Go `swarm-display-render --format json` via `callGoJSON()`, formats the result, and outputs to stderr. Add `runLiveWatch()` that polls at configurable interval. Scope: low-medium. Dependencies: Milestone A complete (D-03).
   - Phase 2: Swarm Integration Tests -- Test that swarm display output renders correctly through TS host, all three formats (tree, JSON, flat) work, live watch polls and updates. Scope: low. Dependencies: Phase 1.

3. **Requirements table** with IDs SWA-01 through SWA-05:
   - SWA-01: TS host calls Go `swarm-display-render --format json` via `callGoJSON()` and presents structured output
   - SWA-02: TS host supports all three swarm display formats (tree, JSON, flat) by passing format flag to Go
   - SWA-03: TS host `runLiveWatch()` polls `swarm-display-render` at configurable interval and refreshes output
   - SWA-04: No TS-side rendering logic -- all tree/text/ANSI rendering done by Go, TS host only presents Go output
   - SWA-05: Integration tests prove swarm display works through TS host in all formats

4. **Boundary Contract Compliance** -- Note that swarm display is read-only (Go reads COLONY_STATE.json, outputs formatted display). No state mutation involved. Reference boundary contract rule #2 (use `AETHER_OUTPUT_MODE=json` for programmatic consumption, not visual output parsing).

5. **Risk Assessment:**
   - Risk: Go `swarm-display-*` commands may not support `AETHER_OUTPUT_MODE=json`. Mitigation: Verify with `AETHER_OUTPUT_MODE=json aether swarm-display-render --format json` before planning; if unsupported, Phase 1 adds JSON output mode to Go commands.
   - Risk: Live watch polling may cause terminal flickering. Mitigation: TS host uses ANSI clear sequences only when output changes; configurable interval defaults to 2 seconds.

**Milestone C: Build/Continue Parity (per D-07):**

1. **Scope** -- What gets migrated, what stays in Go:
   - TS host adds orchestration for colonize, seal, oracle (if not covered in Milestone A), and watch/swarm flows
   - Plan/build/continue already work through TS host (Phase 109)
   - Go owns all state mutation; TS host follows the same plan-only/manifest/finalizer pattern
   - Each flow adapts the pattern to its specific characteristics (colonize = 4 surveyor dispatches, seal = ceremony with blocker check, oracle = iterative loop, watch = polling)

2. **Phases table** with 3 phases:
   - Phase 1: Colonize TS Host Integration -- Add `runColonizeLifecycle()` to TS host. Colonize already has `colonize --plan-only` (returns `codexColonizeManifest` with 4 surveyor dispatches) and `colonize-finalize` command. TS host calls plan-only, dispatches surveyor workers, calls finalizer. Scope: medium. Dependencies: Milestones A and B complete (D-03).
   - Phase 2: Seal TS Host Integration -- Add `runSealLifecycle()` to TS host. Seal already has `seal --plan-only` and `seal-finalize` commands. TS host calls plan-only, checks for blockers, dispatches review workers if needed, calls finalizer. Scope: medium. Dependencies: Phase 1.
   - Phase 3: End-to-End Parity Verification -- Integration test that runs all 6 flows (plan, build, continue, colonize, seal, oracle) through TS host. Verify no direct `.aether/data/` writes, all state through Go finalizers. Scope: medium. Dependencies: Phase 2.

3. **Requirements table** with IDs PAR-01 through PAR-07:
   - PAR-01: TS host `runColonizeLifecycle()` calls `colonize --plan-only`, dispatches 4 surveyor workers, calls `colonize-finalize`
   - PAR-02: TS host `runSealLifecycle()` calls `seal --plan-only`, checks blockers, dispatches review workers, calls `seal-finalize`
   - PAR-03: Seal blocker handling: TS host calls `seal --plan-only`, if blockers exist, reports blocked status to user (does not auto-force)
   - PAR-04: All 6 colony flows (plan, build, continue, colonize, seal, oracle) use TS host orchestration
   - PAR-05: No direct `.aether/data/` writes from TS host for any flow -- all state mutation through Go finalizers
   - PAR-06: Existing behavior preserved: colonize surveyors produce same results, seal ceremony produces same state changes
   - PAR-07: End-to-end parity test proves all 6 flows run through TS host with no regressions

4. **Boundary Contract Compliance** -- Note that colonize dispatches are specified by Go manifest (not invented by TS host). Seal state mutation is safety-critical and goes through `seal-finalize`. Reference boundary contract rules #1, #3.

5. **Risk Assessment:**
   - Risk: Seal blocker/recovery-menu case requires TS host to handle interactive decisions. Mitigation: TS host reports blocker status from `seal --plan-only` manifest and stops. User handles blockers manually, then re-runs seal.
   - Risk: Colonize surveyor dispatches may need different handling than build workers (surveyors explore, builders implement). Mitigation: `dispatchWorkers()` already handles arbitrary dispatch manifests; surveyors are just different worker types dispatched from the manifest.

**Overall Dependency Graph section:**

Add a `## Dependency Graph` section with:
- ASCII diagram showing Milestone A -> Milestone B -> Milestone C sequence (per D-02, D-03)
- Table summarizing: Milestone | Phases | Requirements | Estimated Risk | Depends On
- Explicit note: "Parity depends on Oracle and swarm patterns being proven. Do not parallelize." (per D-03)

**Map Summary section:**

Add a `## Summary` section with:
- Total phases across all milestones: 8 (3 Oracle + 2 Swarm + 3 Parity)
- Total requirements: 20 (8 ORA + 5 SWA + 7 PAR)
- Ordering: A -> B -> C, strictly sequential (D-03)
- Key constraint: Migration only, no new features (D-04)
- All migrations follow the plan-only/manifest/finalizer pattern from Phase 109
  </action>
  <acceptance_criteria>
    - File contains section `## Milestone B: Swarm Visibility` with phases table (2 phases), requirements table (5 requirements SWA-01 through SWA-05), boundary compliance section, and risk assessment
    - File contains section `## Milestone C: Build/Continue Parity` with phases table (3 phases), requirements table (7 requirements PAR-01 through PAR-07), boundary compliance section, and risk assessment
    - File contains `## Dependency Graph` section with ASCII diagram showing A -> B -> C
    - File contains `## Summary` section with totals: 8 phases, 20 requirements, sequential ordering
    - Dependency ordering is explicit: Milestone B depends on Milestone A, Milestone C depends on Milestones A and B (per D-03)
    - Colonize requirements reference `colonize --plan-only` and `colonize-finalize` (existing Go commands)
    - Seal requirements reference `seal --plan-only` and `seal-finalize` (existing Go commands)
    - No task-level breakdowns in any milestone (per D-01)
    - All milestones reference the boundary contract and the Phase 109 TS host pattern
  </acceptance_criteria>
  <verify>
    <automated>grep -c '## Milestone B' .aether/docs/migration-map.md && grep -c '## Milestone C' .aether/docs/migration-map.md && grep -c 'SWA-0[1-5]' .aether/docs/migration-map.md && grep -c 'PAR-0[1-7]' .aether/docs/migration-map.md && grep -c 'Dependency Graph' .aether/docs/migration-map.md</automated>
  </verify>
  <done>Milestones B (Swarm Visibility) and C (Build/Continue Parity) written with phases, requirements, boundary compliance, and risk assessments. Overall dependency graph and summary added. Complete migration map at .aether/docs/migration-map.md.</done>
</task>

</tasks>

<verification>
1. File `.aether/docs/migration-map.md` exists and contains all three milestone sections
2. Milestone A has 3 phases and 8 requirements (ORA-01 through ORA-08)
3. Milestone B has 2 phases and 5 requirements (SWA-01 through SWA-05)
4. Milestone C has 3 phases and 7 requirements (PAR-01 through PAR-07)
5. Dependency graph shows A -> B -> C sequential ordering
6. Summary section has correct totals
7. Every milestone references boundary contract and Phase 109 pattern
8. No task-level breakdowns (milestone-ready granularity per D-01)
</verification>

<success_criteria>
- `.aether/docs/migration-map.md` contains complete migration plans for Oracle/RALF (MAP-01), Swarm Visibility (MAP-02), and Parity (MAP-03)
- The map includes phase numbers, estimated scope, and dependency ordering across all three milestones (MAP-04)
- Each milestone has phases, requirements with IDs, success criteria, boundary contract compliance notes, and risk assessment
- The document is detailed enough for `/gsd-plan-phase` to proceed without additional research (D-01)
- Ordering is Oracle first, Swarm second, Parity last (D-02)
- Dependency chain is explicit with no parallelization (D-03)
</success_criteria>

<output>
After completion, create `.planning/phases/111-follow-up-migration-map/111-01-SUMMARY.md`
</output>
