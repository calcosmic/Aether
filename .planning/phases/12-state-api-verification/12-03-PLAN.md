---
phase: 12-state-api-verification
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - .aether/aether-utils.sh
autonomous: true
requirements: [QUAL-04]

must_haves:
  truths:
    - "8-10 HIGH/MEDIUM priority subcommands use _state_read_field or _state_mutate instead of raw jq on COLONY_STATE.json"
    - "Remaining unmigrated subcommands have MIGRATE comments marking them for Phase 13"
    - "All migrated subcommands produce identical output to before migration"
    - "Full test suite passes after migration (no behavioral regressions)"
  artifacts:
    - path: ".aether/aether-utils.sh"
      provides: "Migrated subcommands using state-api facade functions"
      contains: "_state_read_field\\|_state_mutate"
  key_links:
    - from: ".aether/aether-utils.sh (migrated subcommands)"
      to: ".aether/utils/state-api.sh"
      via: "function calls replacing raw jq access"
      pattern: "_state_read_field\\|_state_mutate\\|_state_read"
---

<objective>
Migrate 8-10 HIGH and MEDIUM priority subcommands from raw COLONY_STATE.json access to the state-api.sh facade functions. Mark remaining LOW priority subcommands with MIGRATE comments for Phase 13.

Purpose: Prove the facade pattern works in practice across a representative set of callers. This validates the API design before Phase 13 migrates the remaining subcommands during monolith modularization.

Output: 8-10 subcommands migrated to use _state_read_field/_state_mutate, remaining subcommands marked with MIGRATE comments, full test suite green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-state-api-verification/12-RESEARCH.md
@.planning/phases/12-state-api-verification/12-01-SUMMARY.md

# Key source file
@.aether/aether-utils.sh
@.aether/utils/state-api.sh (created in Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate HIGH priority subcommands to state-api facade</name>
  <files>.aether/aether-utils.sh</files>
  <action>
Migrate the following HIGH priority subcommands from raw COLONY_STATE.json access to state-api facade functions. For each migration, the external behavior (stdout JSON output) must remain identical.

**HIGH priority migrations (5 subcommands):**

1. **validate-state** (lines ~1226-1244) -- Currently reads COLONY_STATE.json directly with `cat` and `jq`. Replace with `_state_read` for the initial read. Keep the validation logic but use the facade's read function. Call `_state_migrate` (moved to state-api.sh in Plan 01) for migration.

2. **error-add** (line ~1426) -- Currently reads state, appends to errors.records, writes back. Replace with `_state_mutate` using a jq expression like `.errors.records += [{"message": $msg, ...}]`. Preserve the existing lock acquire/release pattern through the facade (it handles locking internally).

3. **phase-insert** (line ~3577) -- Complex read+write. Replace the initial state read with `_state_read`. For the multi-step mutation (insert phase, renumber, write signals), use `_state_mutate` for the state write. Keep the pheromone-write call as-is (that's a separate system).

4. **instinct-create** (line ~7778) -- Read + write with lock. Replace with `_state_mutate` using a jq expression that appends to `.memory.instincts`. Remove the manual lock acquire/release since `_state_mutate` handles it.

5. **instinct-apply** (line ~7905) -- Read + write with lock. Replace with `_state_mutate` using a jq expression that updates the matching instinct's confidence/applied_at. Remove manual lock acquire/release.

**Migration pattern for each subcommand:**

For read-only access:
```bash
# BEFORE:
local state_file="$DATA_DIR/COLONY_STATE.json"
local value=$(jq -r '.current_phase' "$state_file")

# AFTER:
local value=$(_state_read_field '.current_phase')
```

For read-modify-write:
```bash
# BEFORE:
acquire_lock "$state_file" || ...
local current=$(cat "$state_file")
local updated=$(echo "$current" | jq '.field = "new_value"')
atomic_write "$state_file" "$updated" || ...
release_lock

# AFTER:
_state_mutate '.field = "new_value"'
```

Important constraints:
- Preserve all JSON output schemas exactly (no changes to what callers see)
- Preserve all error handling behavior (same error codes, same messages)
- Do NOT change the subcommand names or argument signatures
- Some subcommands do complex multi-step operations (read state, compute something, write back). For these, use `_state_read` for the initial read and `_state_mutate` for the final write -- don't try to cram everything into a single jq expression if it would be unreadable
- Run `npm test` after each migration to catch regressions early
  </action>
  <verify>
Run `npm test` -- full suite green (0 failures).
Run `bash .aether/aether-utils.sh validate-state` -- returns valid state JSON.
Run `bash .aether/aether-utils.sh error-add "test error" "test_context"` -- appends error to state.
Verify migrated subcommands no longer have raw `$DATA_DIR/COLONY_STATE.json` or `cat "$state_file"` patterns (grep should find fewer direct access points).
  </verify>
  <done>5 HIGH priority subcommands (validate-state, error-add, phase-insert, instinct-create, instinct-apply) migrated to use state-api facade. All produce identical output. No regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate MEDIUM priority subcommands and add MIGRATE comments to remaining</name>
  <files>.aether/aether-utils.sh</files>
  <action>
**MEDIUM priority migrations (4-5 subcommands):**

1. **spawn-complete** (line ~1656) -- Write (locked). Currently appends event to state. Replace with `_state_mutate` using a jq expression that appends to `.events`. Remove manual lock logic.

2. **grave-add** (line ~2744) -- Read + Write (locked). Replace initial read with `_state_read`, final write with `_state_mutate` to append to `.graveyards`.

3. **grave-check** (lines ~2812, 2826) -- Read only. Replace raw jq reads with `_state_read_field` calls for the specific fields needed.

4. **instinct-read** (line ~7707) -- Read only, field access. Replace with `_state_read_field '.memory.instincts'`.

5. **milestone-detect** (lines ~3509, 3562) -- Read only. Replace raw reads with `_state_read_field` for `.plan.phases` and `.current_phase`.

**For each migration, follow the same pattern as Task 1.** Preserve external behavior exactly.

**Add MIGRATE comments to all remaining LOW priority subcommands:**

For each unmigrated subcommand that directly accesses COLONY_STATE.json, add a comment above the first direct access line:
```bash
# MIGRATE: direct COLONY_STATE.json access -- use _state_read_field instead
```

The LOW priority subcommands to mark (from the research table):
- error-pattern-check, error-summary (simple reads)
- learning-promote-auto, memory-capture, learning-approve-proposals (field extracts)
- pheromone-write, pheromone-prime, colony-prime, pheromone-expire (complex pipelines)
- wisdom-export-xml, colony-archive-xml, context-capsule (complex jq)
- session-update, resume-dashboard, autopilot-check-replan (field extracts)

These will be migrated in Phase 13 during domain extraction.

Run `npm test` after completing all migrations to confirm full suite green.

Count the remaining direct COLONY_STATE.json access points after migration:
```bash
grep -c 'COLONY_STATE\.json' .aether/aether-utils.sh
```
This should be lower than the pre-migration count of 47 (expect ~30 remaining = 17 migrated access points across the 9-10 subcommands).
  </action>
  <verify>
Run `npm test` -- full suite green (0 failures).
Run `grep -c 'MIGRATE:' .aether/aether-utils.sh` -- returns 15+ (one per unmigrated subcommand area).
Run `grep -c 'COLONY_STATE\.json' .aether/aether-utils.sh` -- count is lower than 47.
Verify grave-add, spawn-complete, instinct-read still produce correct output by running:
- `bash .aether/aether-utils.sh grave-check "nonexistent.txt"` -- returns result without error
- `bash .aether/aether-utils.sh instinct-read` -- returns instincts array
  </verify>
  <done>9-10 total subcommands migrated to state-api facade (5 HIGH + 4-5 MEDIUM). All LOW priority subcommands marked with MIGRATE comments. Direct COLONY_STATE.json access count reduced from 47 to ~30. Full test suite green. Migration pattern proven across readers, writers, and read-modify-write operations.</done>
</task>

</tasks>

<verification>
1. Run `npm test` -- full suite passes (530+ tests, 0 failures)
2. Run `grep -c 'MIGRATE:' .aether/aether-utils.sh` -- 15+ MIGRATE comments on unmigrated subcommands
3. Run `grep -c '_state_read_field\|_state_mutate\|_state_read' .aether/aether-utils.sh` -- 10+ calls to facade functions
4. Verify migrated subcommands: validate-state, error-add, phase-insert, instinct-create, instinct-apply, spawn-complete, grave-add, grave-check, instinct-read, milestone-detect all use facade functions
5. Run `bash .aether/aether-utils.sh validate-state` -- still works correctly
</verification>

<success_criteria>
- 9-10 subcommands migrated from raw COLONY_STATE.json access to state-api facade
- All migrated subcommands produce identical output (no regressions)
- LOW priority subcommands marked with MIGRATE comments for Phase 13
- Direct COLONY_STATE.json access count reduced significantly (from 47 toward ~30)
- Full test suite green
- Migration proves the facade pattern works for readers, writers, and read-modify-write patterns
</success_criteria>

<output>
After completion, create `.planning/phases/12-state-api-verification/12-03-SUMMARY.md`
</output>
