---
phase: 187-crash-safe-worktrees-ecosystem-neutrality
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - cmd/codex_build_worktree.go
  - cmd/merge_phase_worktrees_test.go
  - cmd/worktree_destruction_ratchet_test.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "mergePhaseWorktrees runs the project's own test command, not a hard-coded Go one"
    - "mergePhaseWorktrees refuses to merge when no test command can be determined, and preserves the branch"
    - "mergePhaseWorktrees is covered by tests that exercise the test gate, the clash gate and a real merge"
    - "A future change reintroducing a hard-coded go test on a merge path fails a test"
    - "A future change reintroducing an implicit caller of removeGitWorktree fails a test"
  artifacts:
    - path: "cmd/codex_build_worktree.go"
      provides: "build-path merge gate driven by resolveTestCommand()"
      contains: "resolveTestCommand"
    - path: "cmd/merge_phase_worktrees_test.go"
      provides: "Real-git-fixture tests for mergePhaseWorktrees (ROADMAP criterion 3)"
      contains: "mergePhaseWorktrees"
    - path: "cmd/worktree_destruction_ratchet_test.go"
      provides: "Source ratchets against hard-coded go test on merge paths and implicit destruction callers"
      contains: "removeGitWorktree"
  key_links:
    - from: "cmd/codex_build_worktree.go"
      to: "resolveTestCommand"
      via: "replacing the hard-coded literal at line 1124"
      pattern: "resolveTestCommand\\(\\)"
    - from: "cmd/worktree_destruction_ratchet_test.go"
      to: "cmd/*.go source"
      via: "walk and grep for banned literals with an explicit allowlist"
      pattern: "filepath.Walk|os.ReadDir"
---

<objective>
Finish criterion 2's second half, deliver criterion 3, and lock both fixes plus plan 03's
against regression.

Three jobs:

1. `mergePhaseWorktrees` (`cmd/codex_build_worktree.go:1098`) hard-codes
   `exec.CommandContext(testCtx, "go", "test", "./...")` at **line 1124** (not 1118 —
   PATTERNS.md corrected CONTEXT.md here). This is the merge path actually reached during a
   build, so fixing only the CLI command in plan 02 would leave the bug live where it matters.
2. That function is tested only with an empty worktree list (`TestMergePhaseWorktreesEmpty`).
   The test gate, clash gate, checkout fallback and merge are entirely uncovered. ROADMAP
   criterion 3 asks for real-path tests.
3. This codebase has no ratchet that greps a banned literal out of `.go` source. PATTERNS.md
   is explicit that the two nearest analogs must be composed, not copied. Without ratchets,
   both defects can silently return — and CLAUDE.md records that 18 of 25 milestones were
   spent restoring things previously marked done.

Purpose: D-03 (refuse rather than merge unverified, never destroy what you declined to merge)
and ROADMAP criteria 2 and 3.

Output: the build-path gate rewired; real-path tests for `mergePhaseWorktrees`; two source
ratchets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-crash-safe-worktrees-ecosystem-neutrality/187-CONTEXT.md
@.planning/phases/187-crash-safe-worktrees-ecosystem-neutrality/187-PATTERNS.md
@CLAUDE.md
@.planning/phases/187-crash-safe-worktrees-ecosystem-neutrality/187-01-SUMMARY.md
@.planning/phases/187-crash-safe-worktrees-ecosystem-neutrality/187-02-SUMMARY.md
@.planning/phases/187-crash-safe-worktrees-ecosystem-neutrality/187-03-SUMMARY.md

<interfaces>
<!-- The function being changed. Signature already returns err, so a refusal path needs no
     signature change (PATTERNS.md confirms this). -->
  func mergePhaseWorktrees(phaseNum int) (merged []string, failed []string, err error)  // line 1098

<!-- From cmd/gate.go:242-290 -->
  func resolveTestCommand() string
  // Chain: CLAUDE.md → .aether/data/codebase.md → go.mod → package.json → Cargo.toml → pom.xml → ""
  // Roots at filepath.Dir(filepath.Dir(store.BasePath())), NOT process cwd.
  // cmd/gate.go:277 legitimately contains the literal "go test ./..." as its Go fallback —
  // the ratchet must allowlist that, not ban the string outright.

<!-- From plan 01 -->
  func worktreeDestructionSafety(root string, entry colony.WorktreeEntry) worktreeSafety
  func preserveWorktreeWork(root string, entry colony.WorktreeEntry, safety worktreeSafety) (bool, string, error)
  func reportWorktreePreservation(safety worktreeSafety, detail string)

<!-- From plan 03 -->
  func gcOrphanedWorktrees() (cleaned int, preserved int, err error)   // no longer destroys
  // cmd/worktree_reap.go: the only sanctioned caller of removeGitWorktree

<!-- Ratchet analogs (PATTERNS.md: compose these two, no single exact analog exists) -->
  cmd/spawn_enforce_test.go:690-769  — filepath.Walk + regex over a corpus, with a
    "found no occurrences at all → t.Fatalf, this would pass vacuously forever" self-check
    at lines 750-752
  cmd/retired_packages_test.go:29-71 — deleted-thing-stays-deleted, with an
    allowlist-of-expected-occurrences loop at line 65
  cmd/subcommand_reachability_ratchet_test.go:1382 — the self-exclusion idiom for a ratchet
    skipping its own source file

<!-- Test helpers -->
  func runGit(t *testing.T, dir string, args ...string)                     // cmd/init_cmd_test.go:993
  func makeTestStateWithWorktrees(worktrees []colony.WorktreeEntry) string  // cmd/worktree_test.go:499
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive the build-path merge gate from resolveTestCommand</name>

  <files>cmd/codex_build_worktree.go</files>

  <read_first>
    - cmd/codex_build_worktree.go lines 1096-1167 (the whole mergePhaseWorktrees function:
      the hard-coded test command at line 1124, the existing `failed = append(...)` branches,
      the clash gate at 1133-1142, the main/master checkout fallback at 1146-1154, and the
      merge at 1156)
    - cmd/gate.go lines 242-290 (resolveTestCommand and its rooting comment at 245-249)
    - cmd/gate.go lines 174-184 (checkTestsPass — the pass-by-default shape D-03 rejects)
    - cmd/worktree.go (plan 02's rewired equivalent — match its refusal wording so the two
      sites say the same thing to the user)
    - .planning/phases/187-crash-safe-worktrees-ecosystem-neutrality/187-CONTEXT.md § D-03
      and § Deferred (consolidation of the two merge implementations belongs to Phase 190)
  </read_first>

  <action>
In `cmd/codex_build_worktree.go`, inside `mergePhaseWorktrees`, replace the hard-coded test
invocation at line 1124.

Resolve the command once before the per-entry loop — `resolveTestCommand()` reads repository
manifest files and the answer does not vary per worktree, so calling it once per entry would
be wasted work. Store `testCommand := strings.TrimSpace(resolveTestCommand())`.

Add the D-03 refusal. If `testCommand == ""`, do not enter the merge loop at all. For each
candidate entry in the phase, append to `failed` a string of the form
`<branch> (cannot determine how to test this project)` and emit a plain-English report — reuse
`reportWorktreePreservation` from plan 01 where the shape fits, or a sibling helper with the
same wording rules, so the message names the branch and says the work was kept. Then return
normally with the populated `failed` slice. Do not return a hard `err` for this case: `failed`
is the existing channel this function uses to report per-branch outcomes, and the caller
already surfaces it.

Critically, the refusal must not destroy anything. `mergePhaseWorktrees` itself does not call
`removeGitWorktree` today, but its callers may act on the `merged` list. Ensure a refused
branch appears in `failed` and never in `merged`, so no downstream cleanup can mistake it for
a completed merge.

For the non-empty case, execute the resolved command: split with `strings.Fields` and build
`exec.CommandContext(testCtx, fields[0], fields[1:]...)`. Keep `testCmd.Dir = wtAbsPath` — the
tests must run inside the worktree. Keep the existing tests-failed branch exactly as it is
(`failed = append(failed, fmt.Sprintf("%s (tests failed)", entry.Branch))`); a resolved command
that runs and fails is an ordinary test failure, which D-03's boundary note says keeps existing
behaviour. Keep the `testCancel()` placement correct — the current code calls it explicitly
after `CombinedOutput()` inside the loop rather than deferring, which is right for a loop; do
not convert it to a `defer` inside the loop body.

Do NOT consolidate this function with `cmd/worktree.go`'s `worktreeMergeBackCmd`. CONTEXT.md's
`<deferred>` section assigns that to Phase 190. Do match the user-facing refusal wording
between the two so the product does not describe the same event two different ways.
  </action>

  <verify>
    <automated>go build ./cmd/ && go vet ./cmd/ && go test ./cmd/ -run TestMergePhaseWorktrees -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `cmd/codex_build_worktree.go` contains `resolveTestCommand()` at least once.
    - `cmd/codex_build_worktree.go` contains no occurrence of the argv sequence
      `"go", "test", "./..."`.
    - `resolveTestCommand()` is called once outside the per-entry loop, not inside it.
    - The empty-command path appends to `failed` (never to `merged`) and emits a report.
    - The existing tests-failed branch still appends the exact suffix `(tests failed)`.
    - The existing `TestMergePhaseWorktreesEmpty` still passes.
    - `go build ./cmd/` and `go vet ./cmd/` exit 0.
  </acceptance_criteria>

  <done>The merge path a build actually reaches runs the project's own test command and refuses safely when it cannot tell.</done>
</task>

<task type="auto">
  <name>Task 2: Real-path tests for mergePhaseWorktrees</name>

  <files>cmd/merge_phase_worktrees_test.go</files>

  <read_first>
    - cmd/codex_build_worktree.go lines 1096-1167 (the function under test, as modified in
      Task 1 — every branch listed there needs a test)
    - cmd/worktree_test.go lines 1392-1461 (the real-git fixture shape to follow)
    - cmd/worktree_test.go lines 1196-1289 (the failure-path assertion shape)
    - the existing TestMergePhaseWorktreesEmpty (locate it by grepping cmd/*_test.go) — the
      only current coverage, and the reason criterion 3 exists
    - cmd/init_cmd_test.go line 993 (runGit)
  </read_first>

  <action>
Create `cmd/merge_phase_worktrees_test.go` in package `cmd`. This is ROADMAP criterion 3.
Every fixture is a real `git init` repo with a real `git worktree add` — the shape from
`TestWorktreeMergeBackSuccess`. The existing empty-list test stays; these are additions.

Cover every branch the function has:

`TestMergePhaseWorktreesMergesCleanBranch` — a Go fixture with a passing test on the worktree
branch. Assert the branch appears in `merged`, `failed` is empty, and the commit is genuinely
reachable from `main` afterwards (`git -C <root> log main --oneline` contains the worktree
commit, or `git merge-base --is-ancestor` succeeds). Assert against git, not just the returned
slice — a function that returns "merged" without merging is exactly the failure class CLAUDE.md
warns about.

`TestMergePhaseWorktreesBlocksOnFailingTests` — a Go fixture whose worktree test fails. Assert
the branch is in `failed` with the `(tests failed)` suffix, is NOT in `merged`, and that
`main` does not contain the worktree commit.

`TestMergePhaseWorktreesRefusesWhenTestCommandUnknown` — a fixture with no `go.mod`,
no `package.json`, no `Cargo.toml`, no `pom.xml`, and no test command in `CLAUDE.md` or
`.aether/data/codebase.md`. Assert the branch is in `failed` with a message containing
`cannot determine how to test this project`, is NOT in `merged`, and that the branch and the
worktree directory both still exist afterwards. This is D-03's "never destroys the work it
declined to merge" for the build path.

`TestMergePhaseWorktreesMergesNonGoProject` — a fixture with no `go.mod` but with a resolvable
test command. Prefer putting the command in `CLAUDE.md` so the test controls it exactly and
does not depend on npm being installed; use a command that reliably exits 0. Assert the merge
succeeds. This is ROADMAP criterion 2's positive case on the build path.

`TestMergePhaseWorktreesBlocksOnClash` — construct the clash condition
`checkClashesForWorktree` detects (read that function to determine what produces a clash, then
build the fixture that triggers it). Assert the branch lands in `failed` with the `(clash:` 
prefix and is not merged. If, after reading `checkClashesForWorktree`, the clash condition
proves impossible to construct in a temp-dir fixture, do not fake it — instead document
precisely why in the plan summary and add a narrower unit test for whatever part is reachable.
Do not write a test that asserts nothing.

`TestMergePhaseWorktreesFallsBackToMasterBranch` — a fixture whose default branch is `master`
rather than `main`, exercising the fallback at lines 1146-1154. Assert the merge still succeeds.
This branch has never been covered and is the kind of untested fallback that quietly rots.

`TestMergePhaseWorktreesSkipsOtherPhases` — two worktrees on different phases. Call with one
phase number. Assert only that phase's branch is touched and the other's is untouched
(still unmerged, still present).

`TestMergePhaseWorktreesSkipsAlreadyMerged` — an entry whose Status is `colony.WorktreeMerged`.
Assert it appears in neither `merged` nor `failed`, matching the existing skip at line 1113.
  </action>

  <verify>
    <automated>go test ./cmd/ -run 'TestMergePhaseWorktrees' -count=1 -v</automated>
  </verify>

  <acceptance_criteria>
    - All eight named tests exist and pass:
      `go test ./cmd/ -run TestMergePhaseWorktrees -count=1` exits 0 (including the
      pre-existing `TestMergePhaseWorktreesEmpty`).
    - Each fixture calls `runGit(t, ..., "worktree", "add", ...)` — at least 7 occurrences in
      the file.
    - `TestMergePhaseWorktreesMergesCleanBranch` asserts reachability from `main` using a real
      git command, not only the returned `merged` slice.
    - `TestMergePhaseWorktreesRefusesWhenTestCommandUnknown` asserts the worktree directory and
      the branch both still exist after the call.
    - Coverage improves measurably on the target function: record
      `go test ./cmd/ -run TestMergePhaseWorktrees -coverprofile=/tmp/c.out -count=1` followed by
      `go tool cover -func=/tmp/c.out | grep mergePhaseWorktrees` in the plan summary, showing a
      figure well above the empty-list-only baseline.
  </acceptance_criteria>

  <done>The merge gate, clash gate, checkout fallback, refusal path and real merge are all exercised by real git.</done>
</task>

<task type="auto">
  <name>Task 3: Source ratchets against the two defects returning</name>

  <files>cmd/worktree_destruction_ratchet_test.go</files>

  <read_first>
    - cmd/spawn_enforce_test.go lines 690-769 (the walk+regex ratchet shape, and especially the
      vacuous-pass self-check at 745-755)
    - cmd/retired_packages_test.go lines 29-71 (the deleted-thing-stays-deleted shape and the
      allowlist-of-expected-occurrences loop at line 65)
    - cmd/subcommand_reachability_ratchet_test.go around line 1382 (the self-exclusion idiom
      for a ratchet that must not match its own source)
    - cmd/gate.go line 277 (the ONE legitimate occurrence of the literal `go test ./...`,
      which must be allowlisted rather than banned)
    - cmd/worktree_reap.go (the one sanctioned caller of removeGitWorktree, from plan 03)
    - .planning/phases/187-crash-safe-worktrees-ecosystem-neutrality/187-PATTERNS.md
      § "No Analog Found" — this pattern is being composed, not copied
  </read_first>

  <action>
Create `cmd/worktree_destruction_ratchet_test.go` in package `cmd`. Two ratchets. Both walk
`cmd/*.go`, both skip `_test.go` files and their own source file, and both include the
vacuous-pass self-check.

**Ratchet A — `TestNoHardCodedGoTestOnMergePaths`.**

Walk every non-test `.go` file under `cmd/`. Find occurrences of a hard-coded Go test
invocation: the argv sequence `"go", "test"` inside an `exec.Command`/`exec.CommandContext`
call, and separately the literal string `go test ./...`.

Maintain an explicit allowlist of files where the literal is legitimate. Today that is exactly
one entry: `cmd/gate.go`, which contains `return "go test ./..."` at line 277 as
`resolveTestCommand`'s own Go-language fallback. Structure the allowlist as a map from filename
to a short reason string, and print the reason in the failure message so a future engineer
adding an entry has to justify it in writing — the `retired_packages_test.go:65` allowlist
discipline.

Fail listing `file:line` for every non-allowlisted occurrence, with a message explaining that
the merge gate must use `resolveTestCommand()` because a hard-coded Go command makes the gate
unpassable in any non-Go project, and pointing at the two sites this phase fixed
(`cmd/worktree.go` and `cmd/codex_build_worktree.go`).

Self-check: if the walk finds zero `.go` files, or the allowlisted occurrence in `cmd/gate.go`
is NOT found, `t.Fatalf`. If `resolveTestCommand`'s fallback ever moves or is renamed, this
ratchet would otherwise silently stop checking anything — the exact vacuous-pass failure mode
`cmd/spawn_enforce_test.go:750-752` guards against.

**Ratchet B — `TestRemoveGitWorktreeHasNoImplicitCaller`.**

Walk every non-test `.go` file under `cmd/`. Find every call site of `removeGitWorktree(`,
excluding its own declaration line in `cmd/codex_build_worktree.go`.

Maintain an allowlist of sanctioned callers with reasons. After plans 01-03 the sanctioned set
is:
  - `cmd/worktree_reap.go` — the explicit, operator-invoked destruction command
  - `cmd/codex_build_worktree.go`'s `finalizeBuildWorktree` (line 804) — the normal
    end-of-worker cleanup on a successfully synced worktree. Before allowlisting it, read that
    function and confirm it is only reached for worktrees the wave reconciler accepted; if it
    can be reached for a preserved/orphaned worktree, that is an unplanned finding — report it
    in the plan summary rather than allowlisting it silently.

Fail for any other caller, with a message stating that destruction must not be reachable from
`resume`, `continue`, `init`, `build`, `run` or any autopilot path, and that resuming a crashed
run must never be the thing that destroys the work being resumed.

Self-check: if zero call sites are found at all, `t.Fatalf` — a ratchet that finds nothing
would pass forever even after someone renames the function.

Additionally assert, as a third subtest `TestGCAndReapAgreeOnDangerousState`, that the
disagreement CONTEXT.md identifies as "the defect" is closed: read
`cmd/recover_repair.go`'s `isDestructiveCategory` and confirm `dirty_worktree` is still
classified destructive, and read `cmd/codex_build_worktree.go` and confirm `gcOrphanedWorktrees`
calls `worktreeDestructionSafety`. If one classifies a dirty worktree as dangerous and the other
no longer consults the guard, the two halves of the program have drifted apart again. Include a
comment explaining that this subtest exists because two parts of one program previously
disagreed about how dangerous the same state was, and that the disagreement — not just the
deletion — was the defect.
  </action>

  <verify>
    <automated>go test ./cmd/ -run 'TestNoHardCodedGoTestOnMergePaths|TestRemoveGitWorktreeHasNoImplicitCaller|TestGCAndReapAgreeOnDangerousState' -count=1 -v</automated>
  </verify>

  <acceptance_criteria>
    - All three tests exist and pass:
      `go test ./cmd/ -run 'TestNoHardCodedGoTestOnMergePaths|TestRemoveGitWorktreeHasNoImplicitCaller|TestGCAndReapAgreeOnDangerousState' -count=1` exits 0.
    - Ratchet A's allowlist is a map from filename to a reason string, and the failure message
      prints the reason.
    - Ratchet A `t.Fatalf`s if the allowlisted `cmd/gate.go` occurrence is not found.
    - Ratchet B `t.Fatalf`s if zero `removeGitWorktree` call sites are found.
    - Both ratchets exclude `_test.go` files and their own source file.
    - Fail-then-pass demonstration for each ratchet, performed and recorded:
      * Ratchet A: temporarily reintroduce `exec.CommandContext(ctx, "go", "test", "./...")`
        into `cmd/codex_build_worktree.go`; confirm `TestNoHardCodedGoTestOnMergePaths` fails;
        revert.
      * Ratchet B: temporarily add a `removeGitWorktree` call into `cmd/codex_continue.go`;
        confirm `TestRemoveGitWorktreeHasNoImplicitCaller` fails; revert.
      Record both observed failures in the plan summary. A ratchet nobody has watched fail is a
      ratchet nobody knows works.
  </acceptance_criteria>

  <done>Both defects fail a test if they return, and each ratchet has been observed failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unverified worktree branch → user's main branch | `mergePhaseWorktrees` is the build path's only gate between unreviewed worker output and `git merge` into main. |
| Project config → executed test command | The resolved test command string comes from repository files and is executed by Aether. |
| Future contributor → the fixes in this phase | Nothing structural prevents the hard-coded command or the implicit destruction from being reintroduced; that is what the ratchets defend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-15 | Tampering | Unverified code merged into main because the build-path gate could not run | mitigate | D-03: empty resolver result appends to `failed` and never to `merged`; asserted by `TestMergePhaseWorktreesRefusesWhenTestCommandUnknown` |
| T-187-16 | Denial of Service (data loss) | A refused branch being treated downstream as merged and cleaned up | mitigate | Refused branches appear only in `failed`; the test asserts the branch and directory still exist afterwards |
| T-187-17 | Repudiation | `mergePhaseWorktrees` reporting a branch as merged when no merge occurred | mitigate | `TestMergePhaseWorktreesMergesCleanBranch` asserts reachability from `main` with a real git command, not the returned slice |
| T-187-18 | Elevation of Privilege | A future implicit caller reacquiring destruction authority | mitigate | `TestRemoveGitWorktreeHasNoImplicitCaller` with an explicit reasoned allowlist and a vacuous-pass self-check |
| T-187-19 | Elevation of Privilege | Ratchets silently stopping work after a rename, giving false assurance | mitigate | Both ratchets `t.Fatalf` when they find nothing to check, following `cmd/spawn_enforce_test.go:750-752` |
| T-187-20 | Elevation of Privilege | Test command string from CLAUDE.md executed by Aether | accept | Pre-existing and unchanged; the string comes from the user's own repository, and `strings.Fields` into `exec.CommandContext` adds no shell |
</threat_model>

<verification>
```bash
go build ./cmd/
go vet ./cmd/
go test ./cmd/ -run TestMergePhaseWorktrees -count=1
go test ./cmd/ -run 'TestNoHardCodedGoTestOnMergePaths|TestRemoveGitWorktreeHasNoImplicitCaller|TestGCAndReapAgreeOnDangerousState' -count=1
go test ./... -count=1
go test ./... -race
```
All must exit 0. Then perform the three fail-then-pass demonstrations in Task 3's acceptance
criteria and record them.

Whole-phase gate — run once at the end of this plan, since it is the last plan in the phase:
```bash
go build ./cmd/aether
go vet ./...
go test ./... -race
```
</verification>

<success_criteria>
- `cmd/codex_build_worktree.go` contains no `"go", "test", "./..."` argv literal and calls
  `resolveTestCommand()` once outside the merge loop.
- `mergePhaseWorktrees` has real-path tests covering merge success, test failure, unresolvable
  test command, non-Go project, clash, master fallback, phase filtering and already-merged skip.
- Measured coverage of `mergePhaseWorktrees` is recorded in the summary and is far above the
  empty-list-only baseline.
- Two source ratchets exist, both with reasoned allowlists and vacuous-pass self-checks, and
  each has been observed failing when its defect is reintroduced.
- `go test ./... -race` passes.
</success_criteria>

<output>
After completion, create
`.planning/phases/187-crash-safe-worktrees-ecosystem-neutrality/187-04-SUMMARY.md`.

Also record in the summary, for the phase-level verification step:
- the fail-then-pass observations from plans 01, 03 and 04
- the measured `mergePhaseWorktrees` coverage figure
- the deferred items this phase deliberately did not touch (WorktreeMerged's dual meaning, full
  merge-gate consolidation → Phase 190, `detectOrphanedWorktrees`'s current-phase blind spot,
  the 120s BuildTimeout now reachable by slower non-Go suites)

Note: `.planning/` is gitignored but tracked — new files there need `git add -f` or they
silently vanish from the commit.
</output>
