---
phase: 120-platform-dispatch-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/ts-host/src/platform-dispatcher.ts
  - .aether/ts-host/src/worker-dispatch.ts
  - .aether/ts-host/test/platform-dispatcher.test.ts
  - .aether/ts-host/test/worker-dispatch.test.ts
---

# Phase 120 Plan 01: Platform Dispatch Correctness

## Goal
Fix Codex prompt passing, add platform argument tests, and make simulation fallback explicit.

## Tasks

### Task 1: Export and fix `buildArgs` for all platforms
**File:** `src/platform-dispatcher.ts`

1. Export `buildArgs` function (currently private, add `@internal` JSDoc)
2. Fix Codex case: pass `config.prompt` as final positional argument
   - Current: `codex exec --json --ephemeral --skip-git-repo-check --output-schema /tmp/schema.json`
   - Fixed: append `config.prompt` to args array
3. Verify typecheck passes

**Evidence of done:**
- `export function buildArgs` exists in module exports
- Codex args array includes the prompt as last element
- `npm run typecheck` passes

### Task 2: Add platform argument construction tests
**File:** `test/platform-dispatcher.test.ts`

1. Add test: `buildArgs for Claude includes -p, --output-format, --json-schema, --agent, --permission-mode, and prompt`
2. Add test: `buildArgs for OpenCode includes run, --agent, --format, and prompt`
3. Add test: `buildArgs for Codex includes exec, --json, --ephemeral, --output-schema, and prompt`
4. Add test: `buildArgs for Codex writes schema to tmpdir (not .aether/data/)`

**Evidence of done:**
- 4 new tests pass
- Tests verify exact args array contents, not just shape
- `npm test` passes

### Task 3: Make simulation fallback explicit
**File:** `src/worker-dispatch.ts`

1. In `dispatchSingleWorker`, before simulation block:
   - If `opts.simulateWorkers === undefined` and no platforms available, write warning to stderr:
     `⚠️  No platforms available; simulating worker {name}`
   - If `opts.simulateWorkers === true`, write info to stderr:
     `ℹ️  Simulating worker {name} (simulateWorkers=true)`
2. Keep existing behavior unchanged (default still simulates)

**Evidence of done:**
- Warning appears in test output when simulateWorkers is undefined and no platforms
- `npm test` passes
- `npm run typecheck` passes

### Task 4: Verify spawn-log only records manifest workers
**File:** `src/worker-dispatch.ts`

1. Confirm `dispatchSingleWorker` is only called from `dispatchWaves` with manifest dispatches
2. Confirm spawn-log/spawn-complete receive name/caste/task from dispatch (not hardcoded)
3. No code changes needed if already correct — add comment confirming

**Evidence of done:**
- Comment added confirming manifest-only recording
- No regression in existing tests

### Task 5: Full verification
1. `npm run typecheck` — zero errors
2. `npm test` — all tests pass
3. `npm run build` — no build errors

## Verification Steps

- [ ] Task 1: buildArgs exported, Codex prompt fixed, typecheck clean
- [ ] Task 2: Platform arg tests pass
- [ ] Task 3: Simulation warning tests pass
- [ ] Task 4: Spawn-log comment confirms manifest-only
- [ ] Task 5: Full suite passes

## Rollback
- Revert `platform-dispatcher.ts`, `worker-dispatch.ts`, test files
- No state files modified
