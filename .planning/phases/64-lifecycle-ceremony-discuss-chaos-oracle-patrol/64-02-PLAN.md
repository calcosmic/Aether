---
phase: 64-lifecycle-ceremony-discuss-chaos-oracle-patrol
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/ant/chaos.md
  - .opencode/commands/ant/chaos.md
  - .aether/commands/chaos.yaml
  - .claude/commands/ant/oracle.md
  - .opencode/commands/ant/oracle.md
  - .aether/commands/oracle.yaml
autonomous: true
requirements: [CERE-10, CERE-11]

must_haves:
  truths:
    - "Chaos wrapper runs aether flag-add for HIGH severity findings (already exists as Step 6.5)"
    - "Chaos wrapper checks midden for recurring failure categories and suggests REDIRECT pheromone"
    - "Oracle wrapper suggests persisting high-value findings as pheromone signals or hive wisdom after loop completes"
    - "Oracle wrapper only suggests persistence when oracle status is complete (not blocked/stopped)"
  artifacts:
    - path: ".claude/commands/ant/chaos.md"
      provides: "Chaos wrapper with midden recurrence check instructions"
      contains: "midden-recent-failures"
    - path: ".opencode/commands/ant/chaos.md"
      provides: "OpenCode chaos wrapper with midden recurrence check"
      contains: "midden-recent-failures"
    - path: ".claude/commands/ant/oracle.md"
      provides: "Oracle wrapper with post-completion persistence suggestions"
      contains: "pheromone-write"
    - path: ".opencode/commands/ant/oracle.md"
      provides: "OpenCode oracle wrapper with persistence suggestions"
      contains: "pheromone-write"
  key_links:
    - from: ".claude/commands/ant/chaos.md"
      to: "aether midden-recent-failures"
      via: "CLI call for recurrence detection"
      pattern: "midden-recent-failures"
    - from: ".claude/commands/ant/oracle.md"
      to: "aether pheromone-write"
      via: "CLI call for persistence suggestion"
      pattern: "pheromone-write"
---

<objective>
Enhance chaos and oracle wrappers with ceremony-level intelligence.

Purpose: CERE-10 requires chaos to auto-flag HIGH findings and suggest REDIRECT for recurring midden patterns. CERE-11 requires oracle to suggest persisting high-value research as pheromone signals or hive wisdom. Both are wrapper-only changes per D-04, D-05, D-06, D-07.

Output: Updated chaos wrappers (2 platforms) with midden recurrence check, updated oracle wrappers (2 platforms) with persistence suggestions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-lifecycle-ceremony-discuss-chaos-oracle-patrol/64-CONTEXT.md
@.planning/phases/64-lifecycle-ceremony-discuss-chaos-oracle-patrol/64-RESEARCH.md

<interfaces>
<!-- Existing subcommands the wrapper will call -->

From cmd/flag_cmds.go:
```bash
aether flag-add --severity "critical" --type "blocker" --title "{title}" --description "{desc}" --source "chaos-standalone" --phase {N}
```

From cmd/midden_cmds.go:
```bash
aether midden-recent-failures --limit 20
# Returns: {"entries": [...MiddenEntry...], "total": N}
# Each MiddenEntry has: id, category, description, timestamp, acknowledged
```

From cmd/pheromone_write.go (pheromone_cmds.go):
```bash
aether pheromone-write --type "FOCUS" --content "{\"text\":\"...\"}" --priority "normal" --source "oracle" --reason "high-value research finding"
```

From cmd/hive_cmds.go:
```bash
aether hive-store --text "..." --domain "..." --source-repo "..."
```

From cmd/oracle_loop.go:
```bash
# Oracle state file: .aether/oracle/state.json
# Status values: "running", "complete", "max_iterations_reached", "blocked", "stopped"
# Synthesis file: .aether/oracle/synthesis.md
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add midden recurrence check to chaos wrapper (Claude + OpenCode)</name>
  <files>
    .claude/commands/ant/chaos.md,
    .opencode/commands/ant/chaos.md,
    .aether/commands/chaos.yaml
  </files>
  <read_first>
    .claude/commands/ant/chaos.md
    .opencode/commands/ant/chaos.md
    .aether/commands/chaos.yaml
    cmd/midden_cmds.go
    cmd/flag_cmds.go
  </read_first>
  <acceptance_criteria>
    - .claude/commands/ant/chaos.md contains "### Step 6.6: Midden Recurrence Check"
    - .claude/commands/ant/chaos.md contains "aether midden-recent-failures"
    - .claude/commands/ant/chaos.md contains "REDIRECT" (pheromone suggestion)
    - .opencode/commands/ant/chaos.md contains "### Step 6.6: Midden Recurrence Check"
    - .opencode/commands/ant/chaos.md contains "aether midden-recent-failures"
    - .opencode/commands/ant/chaos.md contains "REDIRECT"
    - .aether/commands/chaos.yaml contains "midden" in follow_up section
    - Step 6.5 (existing flag-add step) is preserved unchanged
    - go test ./cmd/... -count=1 passes (no Go code changes)
  </acceptance_criteria>
  <action>
Per D-04 and D-05: Add midden recurrence detection to chaos wrapper. The existing Step 6.5 already handles flag-add for CRITICAL/HIGH findings, so do NOT duplicate that. The new step detects recurring failure patterns.

Insert a new section "### Step 6.6: Midden Recurrence Check" AFTER Step 6.5 (before Step 7: Log Activity) in both .claude/commands/ant/chaos.md and .opencode/commands/ant/chaos.md.

Content for Step 6.6:

```
### Step 6.6: Midden Recurrence Check

After flagging individual findings, check for recurring failure patterns across the colony's history.

Run using the Bash tool with description "Checking midden for recurring failures...":
```bash
aether midden-recent-failures --limit 20
```

Analyze the returned entries:
1. Group entries by the `category` field
2. If any single category appears 3 or more times, this indicates a recurring failure pattern
3. For each recurring category, suggest a REDIRECT pheromone to the user:
   ```
   ⚠️ Recurring failure pattern detected: "{category}" ({count} occurrences)
   Suggesting REDIRECT to prevent recurrence.

   Run: aether pheromone-write --type "REDIRECT" --content '{"text":"Avoid {category} pattern: {summary of recurring descriptions}"}' --priority "high" --source "chaos-recurrence" --reason "Recurring midden category detected {count} times"
   ```

4. Do NOT automatically create the REDIRECT -- present the suggestion to the user and let them approve

**Skip this step if there are fewer than 3 entries total or no category appears 3+ times.**
```

Also update .aether/commands/chaos.yaml: add a follow_up section (create if missing) with:
```yaml
follow_up:
  midden_recurrence: "After flagging, run `aether midden-recent-failures --limit 20` and check for categories appearing 3+ times. Suggest REDIRECT pheromone for recurring patterns."
```

Keep ALL existing content in chaos.md intact. The new step is purely additive between existing Step 6.5 and Step 7.
  </action>
  <verify>
    <automated>grep -c "Step 6.6" .claude/commands/ant/chaos.md .opencode/commands/ant/chaos.md | grep -v ":0$"</automated>
  </verify>
  <done>
    - Both chaos wrapper files have Step 6.6 midden recurrence check
    - Step 6.5 (existing flag-add) is preserved unchanged
    - Midden recurrence suggests REDIRECT but does NOT auto-create it
    - chaos.yaml has follow_up section with midden_recurrence entry
  </done>
</task>

<task type="auto">
  <name>Task 2: Add post-completion persistence suggestions to oracle wrapper (Claude + OpenCode)</name>
  <files>
    .claude/commands/ant/oracle.md,
    .opencode/commands/ant/oracle.md,
    .aether/commands/oracle.yaml
  </files>
  <read_first>
    .claude/commands/ant/oracle.md
    .opencode/commands/ant/oracle.md
    .aether/commands/oracle.yaml
    cmd/oracle_loop.go
    cmd/pheromone_cmds.go
    cmd/hive_cmds.go
  </read_first>
  <acceptance_criteria>
    - .claude/commands/ant/oracle.md contains "pheromone-write"
    - .claude/commands/ant/oracle.md contains "hive-store"
    - .claude/commands/ant/oracle.md contains "Post-Completion Persistence Suggestions"
    - .claude/commands/ant/oracle.md contains "status" with context about checking oracle state before suggesting
    - .opencode/commands/ant/oracle.md contains "pheromone-write"
    - .opencode/commands/ant/oracle.md contains "hive-store"
    - .opencode/commands/ant/oracle.md contains "Post-Completion Persistence Suggestions"
    - .aether/commands/oracle.yaml contains "persistence" in follow_up section
    - Existing oracle wrapper content is preserved (runtime command, guardrails, status/stop subcommands)
    - go test ./cmd/... -count=1 passes
  </acceptance_criteria>
  <action>
Per D-06 and D-07: Add post-completion persistence suggestion instructions to oracle wrappers. The wrapper agent decides what is "high-value" based on confidence, applicability, and actionability (D-07).

Append the following section to the END of both .claude/commands/ant/oracle.md and .opencode/commands/ant/oracle.md, AFTER the existing content:

```
## Post-Completion Persistence Suggestions

After the oracle loop completes and you have reported the research results, check whether the findings are worth preserving for the colony:

**Only suggest persistence when:**
- The oracle completed successfully (status is "complete" or "max_iterations_reached")
- Do NOT suggest persistence if the oracle was blocked or stopped early

**How to identify high-value findings:**
- Findings with high confidence that apply to the colony's current goal
- Actionable recommendations that would benefit future workers
- Domain-specific insights that are not obvious from the codebase alone
- Architecture or design decisions documented in the research

**Suggestion format:**
Present findings worth preserving as a tick-to-approve list:

```
🔮 Research findings worth preserving:

1. [x] {finding title}
   → Suggest: aether pheromone-write --type "FOCUS" --content '{"text":"{finding summary}"}' --priority "normal" --source "oracle" --reason "High-value research finding"

2. [ ] {finding title}
   → Suggest: aether hive-store --text "{generalized finding}" --domain "{detected domain}" --source-repo "{repo name}"
```

For each approved finding:
- If it is colony-specific guidance: use `aether pheromone-write --type "FOCUS" --content '{"text":"{finding}"}' --priority "normal" --source "oracle" --reason "High-value research finding"`
- If it is generalizable cross-colony wisdom: use `aether hive-store --text "{generalized finding}" --domain "{domain}" --source-repo "{repo name}"`
- Let the user approve each suggestion before persisting

**Do NOT suggest persistence for:** low-confidence findings, obvious observations, or findings already captured as pheromones.
```

Also update .aether/commands/oracle.yaml: add a follow_up section (create if missing):
```yaml
follow_up:
  persistence: "After oracle completes, suggest persisting high-value findings as pheromone signals (aether pheromone-write) or hive wisdom (aether hive-store). User approves each suggestion."
```

Keep ALL existing content in oracle.md intact. This section is purely additive.
  </action>
  <verify>
    <automated>grep -c "pheromone-write" .claude/commands/ant/oracle.md .opencode/commands/ant/oracle.md | grep -v ":0$"</automated>
  </verify>
  <done>
    - Both oracle wrapper files have "Post-Completion Persistence Suggestions" section
    - Persistence is only suggested when oracle completes (not blocked/stopped)
    - User approves each suggestion (not auto-created)
    - Both pheromone-write and hive-store are suggested as options
    - oracle.yaml has follow_up section with persistence entry
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrapper agent -> CLI | Wrapper agent calls existing Go subcommands (flag-add, midden-recent-failures, pheromone-write, hive-store) |
| wrapper agent -> user | User approves persistence suggestions before they are created |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-02-01 | I | chaos midden output | accept | Midden entries are colony-scoped failure records, no sensitive data |
| T-64-02-02 | T | oracle persistence content | accept | User approves each suggestion; content comes from oracle research output |
</threat_model>

<verification>
1. Both chaos wrappers contain "Step 6.6" and "midden-recent-failures"
2. Both oracle wrappers contain "pheromone-write" and "hive-store"
3. Both YAML source files have new follow_up entries
4. `go test ./... -race -count=1` passes (no Go code changes, only markdown)
</verification>

<success_criteria>
- Chaos wrapper Step 6.5 (existing flag-add) preserved unchanged
- Chaos wrapper has Step 6.6 for midden recurrence check with REDIRECT suggestion
- Oracle wrapper has post-completion persistence suggestions for pheromone-write and hive-store
- All existing wrapper behavior preserved
</success_criteria>

<output>
After completion, create `.planning/phases/64-lifecycle-ceremony-discuss-chaos-oracle-patrol/64-02-SUMMARY.md`
</output>
