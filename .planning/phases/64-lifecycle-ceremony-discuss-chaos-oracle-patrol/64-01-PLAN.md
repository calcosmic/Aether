---
phase: 64-lifecycle-ceremony-discuss-chaos-oracle-patrol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/discuss_analyze.go
  - cmd/discuss_analyze_test.go
  - .claude/commands/ant/discuss.md
  - .opencode/commands/ant/discuss.md
  - .claude/commands/ant/council.md
  - .opencode/commands/ant/council.md
  - .aether/commands/discuss.yaml
  - .aether/commands/council.yaml
autonomous: true
requirements: [CERE-09]

must_haves:
  truths:
    - "Running aether discuss-analyze outputs structured JSON with scan data and suggested questions"
    - "Discuss wrapper calls discuss-analyze before presenting questions to the user"
    - "Council wrapper uses the same discuss-analyze output for multi-position framing"
    - "Suggested questions cover architecture, dependencies, testing, deployment, and performance"
  artifacts:
    - path: "cmd/discuss_analyze.go"
      provides: "discuss-analyze subcommand with codebase inventory scan"
      exports: ["discussAnalyzeCmd"]
    - path: "cmd/discuss_analyze_test.go"
      provides: "Tests for discuss-analyze scan and question generation"
    - path: ".claude/commands/ant/discuss.md"
      provides: "Discuss wrapper with discuss-analyze call"
    - path: ".opencode/commands/ant/discuss.md"
      provides: "OpenCode discuss wrapper with discuss-analyze call"
  key_links:
    - from: ".claude/commands/ant/discuss.md"
      to: "aether discuss-analyze"
      via: "CLI call before question presentation"
      pattern: "discuss-analyze"
    - from: ".claude/commands/ant/council.md"
      to: "aether discuss-analyze"
      via: "CLI call before deliberation"
      pattern: "discuss-analyze"
---

<objective>
Create the discuss-analyze Go subcommand and wire it into the discuss and council wrappers.

Purpose: CERE-09 requires discuss/council to analyze the codebase first before asking comprehensive questions. The new discuss-analyze subcommand performs an inventory-level codebase scan (file tree, tech stack, test frameworks, architecture patterns) and outputs structured suggested questions as JSON. Wrappers call this before presenting questions.

Output: New Go subcommand, test file, updated wrappers and YAML sources for 2 platforms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-lifecycle-ceremony-discuss-chaos-oracle-patrol/64-CONTEXT.md
@.planning/phases/64-lifecycle-ceremony-discuss-chaos-oracle-patrol/64-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From cmd/discuss.go:
```go
type discussQuestion struct {
    ID             string   `json:"id,omitempty"`
    Category       string   `json:"category"`
    Question       string   `json:"question"`
    Options        []string `json:"options"`
    Reasoning      string   `json:"reasoning"`
    HardConstraint bool     `json:"hard_constraint,omitempty"`
    Status         string   `json:"status,omitempty"`
    Source         string   `json:"source,omitempty"`
}
```

From cmd/init_research.go:
```go
var projectDetectors = []struct {
    file       string
    typ        string
    frameworks []string
}{...}

var extendedSkipDirs = map[string]bool{
    ".git": true, "node_modules": true, ".next": true, "dist": true,
    "build": true, "vendor": true, ".venv": true, "venv": true,
    "coverage": true, ".aether": true, ".claude": true,
    ".opencode": true, ".codex": true, "__pycache__": true,
}

func detectGovernance(target string) governanceInfo
```

From cmd/helpers.go:
```go
func outputOK(result interface{})
func outputError(code int, message string, details interface{})
func outputWorkflow(result interface{}, visual string)
```

From cmd/init_research.go (governance detectors):
```go
var governanceDetectors = []struct {
    file     string
    category string
    label    string
}{...}
```

From cmd/discuss.go:
```go
func generateDiscussCandidates(goal string, survey codexSurveyContext) []discussQuestion
func discussSource(category string, hard bool) string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create discuss-analyze Go subcommand with inventory scan and question generation</name>
  <files>cmd/discuss_analyze.go, cmd/discuss_analyze_test.go</files>
  <read_first>
    cmd/discuss.go
    cmd/init_research.go
    cmd/init_research_test.go
    cmd/helpers.go
    cmd/discuss_test.go
  </read_first>
  <behavior>
    - Test: discuss-analyze with --target pointing to a temp dir containing go.mod and a subdirectory returns detected_type "go" and at least one suggested question
    - Test: discuss-analyze questions use distinct categories (architecture, dependencies, testing_infrastructure, deployment, performance) that do NOT overlap with existing discuss categories (surface, integration, scope, verification)
    - Test: discuss-analyze with empty target dir returns questions with generic options (not empty)
    - Test: discuss-analyze output JSON contains both "scan" and "questions" keys
  </behavior>
  <action>
Per D-01: Create a new `discuss-analyze` cobra subcommand in cmd/discuss_analyze.go.

Follow the init-research pattern from cmd/init_research.go (lines 423-578):
1. Register `discussAnalyzeCmd` with Use: "discuss-analyze", Short: "Analyze codebase and generate suggested discussion questions", Args: cobra.NoArgs
2. Add --target flag (string, default ".") for directory to scan
3. Add --goal flag (string) for colony goal context

Implementation:
- Read target directory entries (os.ReadDir), identify top-level dirs and files (same as init-research lines 448-467)
- Run projectDetectors to detect languages/frameworks (reuse the same loop pattern from init-research lines 471-487)
- Run detectGovernance(target) to find test frameworks, linters, CI configs (same call as init-research line 541)
- Run filepath.WalkDir with extendedSkipDirs for file count and total dirs (same pattern as init-research lines 495-530)
- DO NOT read source files or README (D-01 says "inventory only")
- Detect architecture patterns: check for multiple go.mod (monorepo), docker-compose (services indicator), k8s manifests (deployment complexity), Makefile/build scripts
- Generate suggested questions using distinct categories that COMPLEMENT (not duplicate) existing discuss categories:
  - "architecture": "What architectural pattern should the plan assume?" with options from detected patterns
  - "dependencies": "How should new dependencies be managed?" with options based on detected package managers
  - "testing_infrastructure": "What testing approach should the colony adopt?" with options based on detected test frameworks
  - "deployment": "What deployment target should the plan optimize for?" with options based on detected CI/Docker
  - "performance": "What performance considerations should the plan account for?" with generic + detected-specific options
- Each question must be a discussQuestion struct (same type from discuss.go) with Category, Question, Options, Reasoning, HardConstraint, Source fields
- Use discussSource("architecture", false) pattern for Source field but with "analyze:" prefix instead of "discuss:" to distinguish
- Output via outputOK() with structure: {"scan": {...inventory data...}, "questions": [...discussQuestion slice...]}
- Register in init() with rootCmd.AddCommand(discussAnalyzeCmd)

Write tests in cmd/discuss_analyze_test.go following the pattern in cmd/discuss_test.go and cmd/init_research_test.go:
- TestDiscussAnalyzeBasicScan: creates temp dir with go.mod, runs command, verifies output contains "scan" and "questions" keys
- TestDiscussAnalyzeDistinctCategories: verifies question categories don't overlap with surface/integration/scope/verification
- TestDiscussAnalyzeEmptyDir: verifies graceful handling with generic questions
  </action>
  <verify>
    <automated>go test ./cmd/... -run TestDiscussAnalyze -count=1 -timeout 30s</automated>
  </verify>
  <done>
    - `go test ./cmd/... -run TestDiscussAnalyze -count=1` passes with 3+ tests
    - `aether discuss-analyze --target .` outputs JSON with "scan" and "questions" keys
    - Question categories are: architecture, dependencies, testing_infrastructure, deployment, performance (NOT surface, integration, scope, verification)
    - No source file reading occurs (inventory-only scan per D-01)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update discuss and council wrappers and YAML sources to call discuss-analyze</name>
  <files>
    .claude/commands/ant/discuss.md,
    .opencode/commands/ant/discuss.md,
    .claude/commands/ant/council.md,
    .opencode/commands/ant/council.md,
    .aether/commands/discuss.yaml,
    .aether/commands/council.yaml
  </files>
  <read_first>
    .claude/commands/ant/discuss.md
    .opencode/commands/ant/discuss.md
    .claude/commands/ant/council.md
    .opencode/commands/ant/council.md
    .aether/commands/discuss.yaml
    .aether/commands/council.yaml
  </read_first>
  <acceptance_criteria>
    - .claude/commands/ant/discuss.md contains "aether discuss-analyze"
    - .opencode/commands/ant/discuss.md contains "aether discuss-analyze"
    - .claude/commands/ant/council.md contains "aether discuss-analyze"
    - .opencode/commands/ant/council.md contains "aether discuss-analyze"
    - .aether/commands/discuss.yaml contains "discuss-analyze" in follow_up section
    - .aether/commands/council.yaml contains "discuss-analyze" in follow_up section
    - All 4 wrapper .md files still start with the YAML generation comment "<!-- Generated from"
    - go test ./cmd/... -count=1 still passes
  </acceptance_criteria>
  <action>
Per D-02 and D-03: Update wrappers to call discuss-analyze before their existing behavior.

**Discuss wrappers (Claude + OpenCode):** Add a new instruction between the existing "Execute" line and the guardrails. The instruction should read:

"For codebase-aware questioning, run `aether discuss-analyze --target .` first. Use the structured output (scan data + suggested questions) to formulate comprehensive multiple-choice questions covering features, priorities, scope, trade-offs, and architecture. Present the discuss-analyze questions as a 'Codebase Analysis' section before the standard discuss questions. The wrapper still owns question presentation and freeform handling."

Insert this instruction after the "Execute `AETHER_OUTPUT_MODE=visual aether discuss $ARGUMENTS`" line and before the guardrails section.

**Council wrappers (Claude + OpenCode):** Add a new instruction after the existing subcommand list. The instruction should read:

"For codebase-aware deliberation context, run `aether discuss-analyze --target .` first. Use the scan data to inform multi-position framing. The discuss-analyze output provides the same codebase inventory for council as for discuss -- no separate council-specific analysis is needed (per the discuss-analyze sharing contract)."

**YAML source files:**
- discuss.yaml: Add to the follow_up section: `analyze: "aether discuss-analyze --target ."` and `analyze_purpose: "Run before discussion to get codebase-aware suggested questions covering architecture, dependencies, testing, deployment, and performance."`
- council.yaml: Add to the follow_up section (create follow_up key if missing): `analyze: "aether discuss-analyze --target ."` and `analyze_purpose: "Run before council deliberation for codebase inventory context."`

Keep all existing content intact -- these are additive instructions.
  </action>
  <verify>
    <automated>grep -l "discuss-analyze" .claude/commands/ant/discuss.md .opencode/commands/ant/discuss.md .claude/commands/ant/council.md .opencode/commands/ant/council.md .aether/commands/discuss.yaml .aether/commands/council.yaml | wc -l | grep -q "6"</automated>
  </verify>
  <done>
    - All 4 wrapper .md files reference "aether discuss-analyze"
    - Both YAML source files have follow_up entries for discuss-analyze
    - Existing discuss wrapper behavior preserved (runtime call, guardrails, resolve flow)
    - Existing council wrapper behavior preserved (subcommand list, guardrails)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> discuss-analyze | File paths from --target flag are trusted (CLI user controls invocation) |
| discuss-analyze -> wrapper | JSON output is consumed by wrapper agent for question presentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01-01 | S | discuss-analyze output | accept | Output is codebase inventory only, no user secrets scanned |
| T-64-01-02 | T | wrapper markdown injection | accept | Wrapper files are git-controlled, not user-generated |
</threat_model>

<verification>
1. `go test ./cmd/... -run TestDiscussAnalyze -count=1` passes (3+ tests)
2. `aether discuss-analyze --target .` outputs valid JSON with scan and questions keys
3. All 6 files (4 wrappers + 2 YAML) contain "discuss-analyze"
4. `go test ./... -race -count=1` passes (no regressions)
</verification>

<success_criteria>
- discuss-analyze subcommand registered and callable via `aether discuss-analyze --target .`
- Output contains structured scan data and suggested questions with distinct categories
- Discuss wrapper calls discuss-analyze before presenting questions (per D-02)
- Council wrapper calls discuss-analyze before deliberation (per D-03)
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/64-lifecycle-ceremony-discuss-chaos-oracle-patrol/64-01-SUMMARY.md`
</output>
