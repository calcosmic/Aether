---
phase: 121-go-runtime-test-restoration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/context_test.go
  - cmd/data_flow_audit_test.go
  - cmd/worker_economy_test.go
---

# Phase 121 Plan 01: Go Runtime Test Restoration

## Goal
Fix all `go test ./cmd` failures: resume dashboard signal injection and tests depending on archived phase artifacts.

## Tasks

### Task 1: Fix resume dashboard signal injection
**File:** `cmd/context_test.go`

1. Read `TestResumeDashboardIncludesSignalsBlockersSurveyAndRecoverySource` to understand expected behavior
2. Determine if the test expectation is wrong or the `buildResumeContext` code is broken
3. Apply fix:
   - If code is broken: fix signal injection in the resume dashboard builder
   - If test is outdated: update assertion to match current behavior
4. Run `go test ./cmd -run TestResumeDashboardIncludesSignalsBlockersSurveyAndRecoverySource` to verify

**Evidence of done:**
- Resume dashboard test passes in isolation

### Task 2: Fix data flow audit tests for missing DATA-FLOW.md
**File:** `cmd/data_flow_audit_test.go`

1. In `TestDataFlowSnapshot`, `TestDataFlowDeadEnds`, `TestDataFlowColonyPrimeWiring`, `TestDataFlowReportAccuracy`:
   - Add `os.Stat` check for the DATA-FLOW.md path
   - If file missing, call `t.Skip("DATA-FLOW.md archived")` instead of failing
2. Verify each test skips gracefully by running individually

**Evidence of done:**
- All 4 data flow tests skip when file is missing
- `go test ./cmd -run TestDataFlow` shows 4 skipped, 0 failed

### Task 3: Fix worker economy tests for missing WORKER-ECONOMY.md
**File:** `cmd/worker_economy_test.go`

1. In `TestDispatchedCastesDocumented` and `TestVisualOutputTracesToState`:
   - Add `os.Stat` check for the WORKER-ECONOMY.md path
   - If file missing, call `t.Skip("WORKER-ECONOMY.md archived")` instead of failing
2. Verify each test skips gracefully by running individually

**Evidence of done:**
- Both worker economy tests skip when file is missing
- `go test ./cmd -run TestDispatchedCastes` shows skipped, 0 failed

### Task 4: Full verification
1. `go test ./cmd` — zero failures
2. `go vet ./cmd` — zero issues

## Verification Steps

- [ ] Task 1: Resume dashboard test passes
- [ ] Task 2: Data flow tests skip gracefully
- [ ] Task 3: Worker economy tests skip gracefully
- [ ] Task 4: `go test ./cmd` passes completely

## Rollback
- Revert the 3 test files
- No state files or runtime code modified
