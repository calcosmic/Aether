---
phase: 115-swarm-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - "115-01"
files_modified:
  - .aether/ts-host/src/narrator.ts
  - .aether/ts-host/src/lifecycle.ts
  - .aether/ts-host/src/index.ts
  - .aether/ts-host/test/lifecycle.test.ts
  - .aether/ts-host/test/narrator.test.ts
autonomous: true
requirements:
  - SW-03
  - SW-05
  - SW-06
must_haves:
  truths:
    - "Dashboard is wired into lifecycle start/stop around worker dispatch"
    - "Narrator suppresses stdout writes when dashboard is active"
    - "Dashboard auto-refreshes from event bridge in real-time"
    - "--no-dashboard CLI flag disables dashboard and restores narrator output"
    - "Dashboard only activates when process.stdout.isTTY is true"
  artifacts:
    - path: ".aether/ts-host/src/narrator.ts"
      provides: "Narrator with dashboard-aware output suppression"
      exports: ["createNarrator"]
      contains: "suppressStdoutWhenDashboardActive"
    - path: ".aether/ts-host/src/lifecycle.ts"
      provides: "Lifecycle with dashboard start/stop"
      exports: ["runLifecycle"]
    - path: ".aether/ts-host/src/index.ts"
      provides: "CLI entry point with --no-dashboard flag"
      exports: []
    - path: ".aether/ts-host/test/lifecycle.test.ts"
      provides: "Lifecycle integration tests with dashboard"
      min_tests: 4
    - path: ".aether/ts-host/test/narrator.test.ts"
      provides: "Updated narrator tests for suppression"
      min_tests: 2
  key_links:
    - from: "lifecycle.ts"
      to: "dashboard.ts"
      via: "createDashboard and start/stop calls"
    - from: "narrator.ts"
      to: "dashboard.ts"
      via: "suppression flag shared via process.env or direct reference"
    - from: "index.ts"
      to: "lifecycle.ts"
      via: "dashboard: !args.noDashboard passed in options"
---

<objective>
Wire the dashboard into the lifecycle: start/stop around worker dispatch, suppress narrator stdout when active, auto-refresh from event bridge, add --no-dashboard CLI flag.

Purpose: Satisfies SW-03 (real-time tool counters), SW-05 (elapsed time and token consumption), SW-06 (auto-refresh from event stream).
Output: Updated narrator.ts, lifecycle.ts, index.ts, integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/115-swarm-dashboard/115-CONTEXT.md
@.planning/phases/115-swarm-dashboard/115-RESEARCH.md
@.planning/phases/115-swarm-dashboard/115-01-PLAN.md
@.aether/ts-host/src/lifecycle.ts
@.aether/ts-host/src/narrator.ts
@.aether/ts-host/src/index.ts
@.aether/ts-host/src/dashboard.ts
@.aether/ts-host/src/event-bridge.ts
</context>

<interfaces>
From .aether/ts-host/src/lifecycle.ts:
```typescript
export interface LifecycleOptions extends DispatchOptions {
  phase?: number;
}
export async function runLifecycle(opts: LifecycleOptions): Promise<LifecycleResult>;
```

From .aether/ts-host/src/narrator.ts:
```typescript
export interface NarratorOptions { cwd: string; outputMode?: string | undefined; }
export interface Narrator { onEvent(event: CeremonyEvent): void; stop(): void; }
export function createNarrator(opts: NarratorOptions): Narrator;
```

From .aether/ts-host/src/dashboard.ts (Wave 1 output):
```typescript
export interface DashboardOptions { cwd: string; outputMode?: string; }
export interface Dashboard { onEvent(event: CeremonyEvent): void; start(): void; stop(): void; }
export function createDashboard(opts: DashboardOptions): Dashboard;
```

From .aether/ts-host/src/index.ts:
```typescript
function parseArgs(argv: string[]): { command: string; cwd: string; simulate: boolean; positional: string[]; };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update narrator.ts to suppress stdout when dashboard is active</name>
  <files>
    .aether/ts-host/src/narrator.ts
  </files>
  <read_first>
    .aether/ts-host/src/narrator.ts
    .aether/ts-host/src/dashboard.ts
  </read_first>
  <action>
    Update `.aether/ts-host/src/narrator.ts`:

    1. Add `suppressOutput?: boolean` to `NarratorOptions`
    2. In `createNarrator`, store `suppressOutput` flag
    3. In `onEvent`, before writing to stdout:
       - If `suppressOutput` is true, return early (do not write)
       - Otherwise, proceed with normal stdout write
    4. Keep all other behavior unchanged (handler map, renderer selection, stop)

    The lifecycle will set `suppressOutput: true` when dashboard is active.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npx tsx --test test/narrator.test.ts</automated>
  </verify>
  <done>
    - narrator.ts accepts suppressOutput option
    - When suppressOutput is true, onEvent does not write to stdout
    - When suppressOutput is false or undefined, normal behavior
    - Existing tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update lifecycle.ts to start/stop dashboard around worker dispatch</name>
  <files>
    .aether/ts-host/src/lifecycle.ts
  </files>
  <read_first>
    .aether/ts-host/src/lifecycle.ts
    .aether/ts-host/src/dashboard.ts
    .aether/ts-host/src/narrator.ts
    .aether/ts-host/src/event-bridge.ts
  </read_first>
  <action>
    Update `.aether/ts-host/src/lifecycle.ts`:

    1. Add `dashboard?: boolean` to `LifecycleOptions` (default: true when TTY)
    2. Import `createDashboard` from `./dashboard.js`
    3. In `runLifecycle`, before the build step:
       - Determine if dashboard should be active:
         ```typescript
         const useDashboard = opts.dashboard !== false && process.stdout.isTTY;
         ```
       - If `useDashboard`:
         - Create dashboard: `const dashboard = createDashboard({ cwd: opts.cwd });`
         - Start dashboard: `dashboard.start();`
    4. Modify the event bridge wiring (if present) or add it:
       - The event bridge should send events to BOTH narrator and dashboard:
         ```typescript
         onEvent: (evt) => {
           narrator.onEvent(evt);
           dashboard?.onEvent(evt);
         }
         ```
       - If dashboard is active, narrator gets `suppressOutput: true`
    5. After the build step completes:
       - If dashboard was started: `dashboard.stop();`
    6. During worker dispatch:
       - Each `dispatchSingleWorker` result should emit a ceremony event (or update dashboard directly)
       - The dashboard already listens to event bridge, so events from Go `spawn-log`/`spawn-complete` will update it

    Keep plan and continue steps unchanged.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npx tsx --test test/lifecycle.test.ts</automated>
  </verify>
  <done>
    - lifecycle.ts accepts dashboard option
    - Dashboard created and started before build dispatch when TTY
    - Dashboard stopped after build dispatch
    - Narrator suppresses output when dashboard is active
    - Plan and continue steps unchanged
  </done>
</task>

<task type="auto">
  <name>Task 3: Update index.ts to accept --no-dashboard flag</name>
  <files>
    .aether/ts-host/src/index.ts
  </files>
  <read_first>
    .aether/ts-host/src/index.ts
    .aether/ts-host/src/lifecycle.ts
  </read_first>
  <action>
    Update `.aether/ts-host/src/index.ts`:

    1. In `parseArgs`, add `--no-dashboard` flag parsing:
       - If `--no-dashboard` appears in args, set `noDashboard = true`
    2. Update return type: `{ command: string; cwd: string; simulate: boolean; noDashboard: boolean; positional: string[]; }`
    3. In the `lifecycle` branch:
       - Pass `dashboard: !args.noDashboard` to `LifecycleOptions`
    4. Update `printUsage()` to document `--no-dashboard` flag:
       ```
       Options:
         --cwd <path>    Working directory
         --simulate      Run in simulation mode (no real worker spawning)
         --no-dashboard  Disable live dashboard, use plain text output
       ```

    Example: `node dist/host.js lifecycle 1 --no-dashboard`
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npx tsx src/index.ts lifecycle --no-dashboard --cwd /Users/callumcowie/repos/Aether</automated>
  </verify>
  <done>
    - --no-dashboard flag parsed and passed to lifecycle
    - Usage text documents the flag
    - Dashboard disabled when flag is present
  </done>
</task>

<task type="auto">
  <name>Task 4: Write integration tests for lifecycle with dashboard</name>
  <files>
    .aether/ts-host/test/lifecycle.test.ts
    .aether/ts-host/test/narrator.test.ts
  </files>
  <read_first>
    .aether/ts-host/src/lifecycle.ts
    .aether/ts-host/src/narrator.ts
    .aether/ts-host/test/lifecycle.test.ts
    .aether/ts-host/test/narrator.test.ts
  </read_first>
  <action>
    Update existing test files.

    narrator.test.ts — add 2 tests:
    1. `narrator suppresses stdout when suppressOutput is true` — create narrator with suppressOutput: true, mock stdout.write, call onEvent with known topic, assert write not called
    2. `narrator writes stdout when suppressOutput is false` — create narrator with suppressOutput: false, mock stdout.write, call onEvent, assert write called

    lifecycle.test.ts — add 4 tests (or update existing):
    1. `lifecycle with dashboard option creates dashboard` — mock createDashboard, call runLifecycle with dashboard: true, assert createDashboard called
    2. `lifecycle with no-dashboard skips dashboard` — mock createDashboard, call runLifecycle with dashboard: false, assert createDashboard not called
    3. `lifecycle passes events to both narrator and dashboard` — mock both, emit event, assert both onEvent called
    4. `lifecycle stops dashboard after build` — mock dashboard, run lifecycle, assert dashboard.stop called

    For mocking, you may need to use `node:test` mocking or simple wrapper functions.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npx tsx --test test/lifecycle.test.ts test/narrator.test.ts</automated>
  </verify>
  <done>
    - narrator.test.ts has 2 new suppression tests
    - lifecycle.test.ts has 4 new dashboard integration tests
    - All tests pass
    - Existing tests still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lifecycle.ts → dashboard.ts | Dashboard is presentation-only; no state writes |
| narrator.ts → process.stdout | Suppression is safe (just skips write) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-03 | Denial of Service | lifecycle.ts | mitigate | Dashboard.stop always called in finally block |
| T-115-04 | Information Disclosure | lifecycle.ts | accept | Dashboard shows ceremony data only (non-sensitive) |
</threat_model>

<verification>
- `npm test -- test/lifecycle.test.ts` passes
- `npm test -- test/narrator.test.ts` passes
- `npx tsc --noEmit -p tsconfig.build.json` compiles cleanly
- `node dist/host.js lifecycle --no-dashboard --cwd <repo>` runs without error
</verification>

<success_criteria>
- Dashboard is wired into lifecycle start/stop around worker dispatch
- Narrator suppresses stdout when dashboard is active
- --no-dashboard flag disables dashboard and restores narrator output
- Dashboard only activates in TTY environments
- All integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/115-swarm-dashboard/115-02-SUMMARY.md`
</output>
