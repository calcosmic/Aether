---
phase: 199-front-door-and-classic-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/testing_main_test.go
  - cmd/worktree_test.go
  - cmd/testing_cleanup_ownership_test.go
autonomous: true
requirements: [PROOF-01]
must_haves:
  truths:
    - "Running Phase 199's Go proof suite cannot delete a user-owned branch or worktree merely because its name starts with phase- or feature/."
    - "Test cleanup removes only paths and branches registered by the test that created them."
  artifacts:
    - path: "cmd/testing_main_test.go"
      provides: "Exact-ownership cleanup registry used by TestMain"
      contains: "cleanupTestWorktrees"
    - path: "cmd/testing_cleanup_ownership_test.go"
      provides: "Destructive-cleanup decoy regression proof"
      contains: "TestCleanupTestWorktreesPreservesUnregisteredDecoys"
  key_links:
    - from: "cmd/worktree_test.go"
      to: "cmd/testing_main_test.go"
      via: "test-owned branch/worktree registration before creation"
      pattern: "registerTestOwned"
---

<objective>
Make the test gate safe before Phase 199 adds broad command and filesystem coverage.

Purpose: The current package cleanup discovers branches by broad name prefixes. Phase 199's proof work must never turn a test run into a destructive operation against the owner's real work.
Output: Exact ownership registration, bounded cleanup, and a decoy regression test.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md
@.planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md
@cmd/AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace prefix discovery with explicit test ownership</name>
  <files>cmd/testing_main_test.go, cmd/worktree_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/testing_main_test.go
    - cmd/worktree_test.go
    - cmd/blackbox_harness_test.go
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md (test-safety and destructive-cleanup sections)
    - .planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md (cmd/testing_main_test.go assignment)
  </read_first>
  <behavior>
    - A registered test worktree and its registered branch are removed during cleanup.
    - An unregistered phase-* branch, feature/* branch, or worktree under an Aether-looking directory is never selected.
    - Missing registered artifacts are idempotent no-ops rather than reasons to broaden discovery.
  </behavior>
  <action>Refactor `cleanupTestWorktrees` to consume an in-process, mutex-protected registry of exact canonical worktree paths and exact branch names. Add `registerTestOwnedWorktree(t *testing.T, repoRoot, path, branch string)` and register the real repository-scoped artifact created by `TestWorktreeAllocateAuditLog` before creation. Resolve and validate each registered path beneath its registered repository, reject empty/root paths and symlink escapes, remove only those entries, and delete only their exact registered branch names. Delete the `git worktree list` selection by `/.aether/worktrees/` or `-worktrees/` and the `phase-`/one-segment `feature/` branch sweep. Do not call `git worktree prune` globally; it is not ownership proof. Preserve existing user changes and do not inspect or remove `.gsd/`.</action>
  <acceptance_criteria>
    - `cmd/testing_main_test.go` contains no cleanup predicate matching every `phase-` branch or every one-segment `feature/` branch.
    - `cmd/worktree_test.go` registers the exact branch/path it creates in the source checkout.
    - Cleanup operations are parameterized by a validated repository root and exact registered artifacts.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestWorktreeAllocateAuditLog$' -count=1</automated>
  </verify>
  <done>The one existing test that leaves source-repository artifacts is registered explicitly, and TestMain no longer discovers deletion targets by naming convention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove unregistered owner artifacts survive cleanup</name>
  <files>cmd/testing_cleanup_ownership_test.go, cmd/testing_main_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/testing_cleanup_ownership_test.go (create; confirm absent)
    - cmd/testing_main_test.go
    - cmd/worktree_test.go
    - cmd/blackbox_harness_test.go
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md (destructive test cleanup risk)
  </read_first>
  <behavior>
    - Exact registered artifacts are deleted.
    - Decoy branches named `phase-199/owner-work` and `feature/owner-work` survive.
    - A decoy worktree in an Aether-shaped path survives when it is not registered.
  </behavior>
  <action>Create an isolated temporary Git repository test that registers one owned branch/worktree, creates the registered artifact plus the three owner-looking decoys, invokes the same cleanup function TestMain uses, and asserts byte/path/branch survival for every decoy and absence for only the registered pair. Make the helper injectable with a repository root so the test never creates or deletes artifacts in the source checkout. Include an idempotent second cleanup call.</action>
  <acceptance_criteria>
    - `TestCleanupTestWorktreesPreservesUnregisteredDecoys` fails if cleanup reintroduces prefix-based selection.
    - The test asserts exact branch lists and exact worktree paths after cleanup, not only exit status.
    - The test uses `t.TempDir()` and cannot target the source repository.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestCleanupTestWorktrees(PreservesUnregisteredDecoys|IsIdempotent)$' -count=1</automated>
  </verify>
  <done>Focused tests demonstrate that only registered test artifacts are deleted and plausible user work remains untouched.</done>
</task>

</tasks>

<verification>
- Both focused commands pass in less than 60 seconds on a warm Go cache.
- `rg -n 'HasPrefix\(br, "phase-"\)|HasPrefix\(br, "feature/"\)|worktree", "prune"' cmd/testing_main_test.go` returns no destructive discovery path.
</verification>

<success_criteria>
- Subsequent Phase 199 tests can run without placing user branches or worktrees in the cleanup target set.
- Cleanup ownership is positive evidence (registration), never a guessed name.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-01-SUMMARY.md` when done.
</output>
