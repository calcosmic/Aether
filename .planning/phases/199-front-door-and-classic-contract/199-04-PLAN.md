---
phase: 199-front-door-and-classic-contract
plan: "04"
type: execute
wave: 4
depends_on: ["199-03"]
files_modified:
  - cmd/lifecycle_facts.go
  - cmd/lifecycle_facts_test.go
  - cmd/state_load.go
  - cmd/lifecycle_projection.go
  - cmd/lifecycle_projection_test.go
  - cmd/next_action.go
  - cmd/next_action_input.go
  - cmd/next_action_card.go
  - cmd/lifecycle_next_action_199_test.go
autonomous: true
requirements: [CEC-01, CEC-02, LIFE-03]
must_haves:
  truths:
    - "All ordinary lifecycle views derive identity, progress, activity, evidence, blockers, and next choices from one read-only fact load and one pure projection."
    - "Reading orientation facts causes zero repository, hub, registry, session, timestamp, or Git mutation."
    - "An accepted plan presents build and run as equal choices; a sealed colony does not imply entomb is mandatory."
  artifacts:
    - path: "cmd/lifecycle_facts.go"
      provides: "Read-only LifecycleFacts loader with explicit provenance"
      exports: ["loadLifecycleFacts"]
    - path: "cmd/lifecycle_projection.go"
      provides: "Pure LifecycleProjection and shared result-object builder"
      exports: ["projectLifecycle"]
    - path: "cmd/next_action.go"
      provides: "One lifecycle decision policy consumed by Next Up"
  key_links:
    - from: "cmd/lifecycle_projection.go"
      to: "cmd/lifecycle_facts.go"
      via: "pure projection input"
      pattern: "LifecycleFacts"
    - from: "cmd/next_action.go"
      to: "cmd/lifecycle_projection.go"
      via: "projected next action and equal-choice set"
      pattern: "projectLifecycle|LifecycleProjection"
---

<objective>
Establish the single read-only truth and projection path behind every ordinary lifecycle screen.

Purpose: Status, phase, history, run, pause, and closure cannot agree if each independently repairs or infers state while rendering.
Output: A zero-write fact loader, a pure projection, and Next Up migrated to the shared policy.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md
@.planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@pkg/colony/lifecycle.go
@cmd/state_load.go
@cmd/next_action.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Load authoritative lifecycle facts without writes</name>
  <files>cmd/lifecycle_facts.go, cmd/lifecycle_facts_test.go, cmd/state_load.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/lifecycle_facts.go (create; confirm absent)
    - cmd/lifecycle_facts_test.go (create; confirm absent)
    - cmd/state_load.go
    - cmd/lifecycle_helpers.go
    - cmd/status.go
    - cmd/phase.go
    - cmd/history.go
    - cmd/context.go
    - pkg/colony/lifecycle.go
    - pkg/colony/colony.go
    - pkg/colony/session.go
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (status sections and shared result fields)
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md (read-only facts boundary)
  </read_first>
  <behavior>
    - Missing, malformed, and legacy files produce explicit unavailable/unknown provenance and diagnostics.
    - Reading a valid colony includes identity, goal, standing, phase/tasks, castes/lineage, signals, research/Dreams, memory/findings, verification, elapsed/reported cost, history, blockers, and evidence.
    - Before/after digests of repository state, hub registry, Git refs, and modification times are identical.
  </behavior>
  <action>Create an immutable `LifecycleFacts` aggregate and `loadLifecycleFacts(root, store, now)` that uses read-only file operations and existing parsers. Include per-source provenance (`confirmed`, `missing`, `malformed`, or `unavailable`) and diagnostics instead of repairing on read. Extract the non-mutating load path from `state_load.go`; keep legacy normalization in memory only. Do not call `SaveJSON`, `MkdirAll`, `Remove`, session-mirror creation, registry upsert, Git mutation, or timestamp touch. Per D-09, D-11, and SYN-199-03, gather all authoritative domains required by the full snapshot, including real actor/lineage evidence and reported cost only when recorded. Represent no evidence as unknown/empty, never a fabricated worker, cost, or outcome.</action>
  <acceptance_criteria>
    - A source assertion test prevents write-capable storage/Git helpers from being called by `loadLifecycleFacts`.
    - Tests fingerprint `.aether`, a fake hub, session files, registry files, and Git refs before and after every valid/missing/malformed fixture.
    - Every fact domain records its source/provenance.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestLifecycleFacts(ReadOnly|Provenance|MissingAndMalformed)$' -count=1</automated>
  </verify>
  <done>There is one complete fact loader whose read-only behavior is causally proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project one semantic lifecycle result</name>
  <files>cmd/lifecycle_projection.go, cmd/lifecycle_projection_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/lifecycle_projection.go (create; confirm absent)
    - cmd/lifecycle_projection_test.go (create; confirm absent)
    - cmd/lifecycle_facts.go
    - pkg/colony/lifecycle.go
    - cmd/next_action.go
    - cmd/next_action_input.go
    - cmd/next_action_card.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-02, D-09 through D-12, D-15, D-17)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (projection and compact/full semantics)
  </read_first>
  <behavior>
    - The same facts and projection revision always produce the same structured result.
    - JSON, visual, full, compact, and focused views select from the same semantic fields.
    - Missing evidence remains explicitly unknown and cannot become a success label.
  </behavior>
  <action>Define a pure `LifecycleProjection` and `projectLifecycle(facts, view, platform)` function. Populate the UI-SPEC's shared structured fields and an ordered status-section model; use a stable `projection_revision`. Derive platform spelling (`aether X` versus `/ant-X`) only at the final projection boundary. Encode D-02 as a `choose_execution_mode` next action with an equal, non-ranked choice set containing build phase 1/current phase and run after an accepted plan. Encode D-15 so ambiguous recovery points to resume, not recover. Encode D-17 so sealed state remains a valid inspectable state and entomb is an optional alternative, not a forced Next Up. Keep ANSI, terminal width, filesystem access, clocks, and mutation outside the pure function.</action>
  <acceptance_criteria>
    - Table tests cover empty repository, accepted goal/no plan, accepted plan, executing, paused, blocked, verified-complete, forced-incomplete, sealed, and malformed-evidence states.
    - Build and run have identical rank/recommendation markers in the accepted-plan case.
    - Projection tests compare structured fields and section order rather than terminal snapshots.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestLifecycleProjection' -count=1</automated>
  </verify>
  <done>One deterministic projection defines the semantic answer consumed by all later renderers and commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make Next Up a view of the shared projection</name>
  <files>cmd/next_action.go, cmd/next_action_input.go, cmd/next_action_card.go, cmd/lifecycle_next_action_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/next_action.go
    - cmd/next_action_input.go
    - cmd/next_action_card.go
    - cmd/lifecycle_next_action_199_test.go (create; confirm absent)
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_projection.go
    - cmd/codex_visuals.go (existing next-action rendering helpers only)
    - .planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md (Next Up analog and hardcode ratchet)
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-02, D-03, D-15, D-17)
  </read_first>
  <behavior>
    - No goal yields exactly init.
    - A goal with no accepted plan yields plan.
    - An accepted plan yields one equal build/run choice card.
    - Paused or uncertain recovery yields resume; sealed never demands entomb.
  </behavior>
  <action>Per D-02, D-03, D-15, and D-17, remove independent lifecycle inference from `next_action.go` and adapt `next_action_input.go` to supply `LifecycleFacts` to `projectLifecycle`. Have `next_action_card.go` render the projection's `next_action`, equal choice set, alternatives, reason, and evidence without changing their meaning. Preserve existing extension points for non-lifecycle command detail, but make the lifecycle projection the only decider for init/plan/build/run/resume/seal/entomb. Add a ratchet proving no renderer or command-local branch hardcodes a contradictory lifecycle next command.</action>
  <acceptance_criteria>
    - All locked next-action cases are asserted for runtime, Claude, and OpenCode spellings.
    - No accepted-plan assertion labels build or run recommended/preferred.
    - `next_action_card.go` contains no state-machine branching that competes with `projectLifecycle`.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestLifecycleNextAction|TestNextActionCardUsesProjection)$' -count=1</automated>
  </verify>
  <done>Next Up is a renderer of the one shared lifecycle answer, including equal build/run choice and optional entomb semantics.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run '^(TestLifecycleFacts|TestLifecycleProjection|TestLifecycleNextAction|TestNextActionCardUsesProjection)' -count=1` passes.
- Read-only tests assert unchanged bytes and metadata, not merely successful return values.
</verification>

<success_criteria>
- A single fact load and projection can drive every Phase 199 view.
- No orientation read repairs or mutates state.
- D-02, D-03, D-15, and D-17 resolve consistently before command-specific UI work begins.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-04-SUMMARY.md` when done.
</output>
