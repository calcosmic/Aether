---
phase: 199-front-door-and-classic-contract
plan: "29"
type: execute
wave: 17
depends_on: ["199-28"]
files_modified:
  - .planning/phases/199-front-door-and-classic-contract/199-CLASSIC-COVERAGE.md
  - .planning/phases/199-front-door-and-classic-contract/199-GATE-RECEIPT.json
  - cmd/phase199_gate_receipt_test.go
autonomous: true
requirements: [SYNTH-01, CEC-01, CEC-02, CEC-04, CEC-08, LIFE-01, LIFE-02, LIFE-03, LIFE-04, LIFE-05, LIFE-06, PROOF-01]
must_haves:
  truths:
    - "Focused Phase 199 proofs and both exact repository gates pass before completion is recorded."
    - "The gate receipt can be produced only from successful `go test ./...` and `go test ./... -race` runs."
    - "Pre-existing user-owned STATE.md, untracked .gsd/, and untracked 199-PATTERNS.md have identical before/after ownership fingerprints."
  artifacts:
    - path: ".planning/phases/199-front-door-and-classic-contract/199-GATE-RECEIPT.json"
      provides: "Machine-validated exact command, exit, revision, timestamp, and protected-fingerprint evidence"
    - path: "cmd/phase199_gate_receipt_test.go"
      provides: "Gate command/success and user-owned fingerprint receipt validator"
      contains: "TestPhase199GateReceipt"
  key_links:
    - from: "cmd/phase199_gate_receipt_test.go"
      to: ".planning/phases/199-front-door-and-classic-contract/199-GATE-RECEIPT.json"
      via: "exact command and successful result validation"
      pattern: "go test ./..."
---

<objective>
Run and machine-record the exact Phase 199 completion gates while proving user-owned changes were preserved.

Purpose: Completion requires actual full-suite and race-suite success, not a promise in prose or focused tests alone.
Output: Focused/full gate evidence, protected-change fingerprints, and a validated gate receipt.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-COVERAGE.md
@cmd/classic_contract_test.go
@cmd/classic_coverage_199_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define a non-forgeable gate and ownership receipt</name>
  <files>.planning/phases/199-front-door-and-classic-contract/199-GATE-RECEIPT.json, cmd/phase199_gate_receipt_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .planning/phases/199-front-door-and-classic-contract/199-GATE-RECEIPT.json (create; confirm absent)
    - cmd/phase199_gate_receipt_test.go (create; confirm absent)
    - .planning/STATE.md (fingerprint only; do not edit)
    - .gsd/ (fingerprint only; do not edit)
    - .planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md (fingerprint only; do not edit)
  </read_first>
  <behavior>
    - Receipt requires exact commands, exit code zero, repository revision/tree identity, execution timestamps, and output digest.
    - Protected paths record status/type/content-tree fingerprints before and after and must compare equal.
  </behavior>
  <action>Create a gate-receipt schema/validator with exact top-level tests `TestPhase199GateReceiptSchema` and `TestPhase199GateReceipt`. Capture before fingerprints for the user-owned `.planning/STATE.md`, untracked `.gsd/` tree, and untracked `199-PATTERNS.md`: git status classification, file type/mode, and SHA-256 content/tree digest without modifying them. Require receipt entries for exact commands `go test ./...` and `go test ./... -race`, each with exit zero, start/end time, revision/tree identity, and output digest; reject absent, edited, nonzero, differently spelled, or stale entries. Require identical after fingerprints. The receipt starts incomplete and is populated only by Task 2 after commands actually succeed.</action>
  <acceptance_criteria>
    - `cmd/phase199_gate_receipt_test.go` defines exact top-level `TestPhase199GateReceiptSchema` and `TestPhase199GateReceipt`; schema validation rejects missing, stale, nonzero, or differently spelled gate entries.
    - The incomplete receipt records before fingerprints for `.planning/STATE.md`, `.gsd/`, and `199-PATTERNS.md`, including status/type/mode and SHA-256 content or tree digest.
    - The exact schema test passes while protected before/after fingerprints remain equal.
  </acceptance_criteria>
  <verify>
    <automated>test "$(rg -c '^func TestPhase199GateReceiptSchema\(' cmd/phase199_gate_receipt_test.go)" -eq 1 &amp;&amp; test "$(rg -c '^func TestPhase199GateReceipt\(' cmd/phase199_gate_receipt_test.go)" -eq 1 &amp;&amp; go test ./cmd -run '^TestPhase199GateReceiptSchema$' -count=1</automated>
  </verify>
  <done>The receipt validator cannot accept missing/full-gate substitutes or changed protected ownership state.</done>
</task>

<task type="auto">
  <name>Task 2: Run focused and exact repository gates</name>
  <files>.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-COVERAGE.md, .planning/phases/199-front-door-and-classic-contract/199-GATE-RECEIPT.json, cmd/phase199_gate_receipt_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/phase199_gate_receipt_test.go
    - .planning/phases/199-front-door-and-classic-contract/199-CLASSIC-COVERAGE.md
    - .planning/phases/199-front-door-and-classic-contract/199-GATE-RECEIPT.json
    - .planning/STATE.md (fingerprint only; do not edit)
    - .gsd/ (fingerprint only; do not edit)
    - .planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md (fingerprint only; do not edit)
  </read_first>
  <action>Assert that every focused top-level test family selected below exists before running corpus, lifecycle, source/generated parity, command reachability, structured coverage, cleanup-ownership, and gate-schema tests. Resolve Phase 199-caused failures through the executor deviation protocol without weakening assertions. Then execute the two exact repository gates below using non-blocking sessions/polling when needed. Only after each exits zero, record its evidence in the receipt. Recompute protected fingerprints and require exact equality before marking coverage complete. Do not edit, stage, delete, or normalize `.planning/STATE.md`, `.gsd/`, or `199-PATTERNS.md`; pre-existing unrelated failures remain visible and block the phase rather than being hidden.</action>
  <acceptance_criteria>
    - Source assertions find at least five `TestClassicContractCorpus*` functions and all five exact `TestClassicCoverage199*` functions before the focused lane runs.
    - The focused lane executes matching corpus and coverage prefixes plus exact parity, vocabulary, receipt-schema, source-hygiene, alias-pruning, and cleanup tests; no family may pass by matching zero tests.
    - Exact `go test ./...` and `go test ./... -race` entries both record exit zero in the receipt, final `TestPhase199GateReceipt` passes, and all three protected fingerprints are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>test "$(rg -c '^func TestClassicContractCorpus' cmd/classic_contract_test.go)" -ge 5 &amp;&amp; test "$(rg -c '^func TestClassicCoverage199(ExactSets|RequiredFields|ProofResolution|PassingStatus|RejectsInvalidRows)\(' cmd/classic_coverage_199_test.go)" -eq 5 &amp;&amp; test "$(rg -c '^func TestPhase199GateReceipt\(' cmd/phase199_gate_receipt_test.go)" -eq 1 &amp;&amp; go test ./cmd -run '^(TestClassicContractCorpus.*|TestClassicCommandParity|TestClassicCoverage199.*|TestCurrentVocabulary199|TestPhase199GateReceiptSchema|TestCommandSourceHygiene|TestRetiredLifecycleAliasPruning199|TestCleanupTestWorktreesPreservesUnregisteredDecoys)$' -count=1 &amp;&amp; go test ./pkg/colony -run '^TestLifecycle' -count=1 &amp;&amp; go test ./... &amp;&amp; go test ./... -race &amp;&amp; go test ./cmd -run '^TestPhase199GateReceipt$' -count=1</automated>
  </verify>
  <done>Focused proofs and both exact full repository commands pass, the receipt validates, and all protected user-owned fingerprints are unchanged.</done>
</task>

</tasks>

<verification>
- `go test ./...` passes.
- `go test ./... -race` passes.
- The validated receipt proves exact commands, zero exits, fresh evidence, and unchanged protected fingerprints.
</verification>

<success_criteria>
- Phase 199 has objective full-suite, race-suite, semantic-corpus, coverage, and user-change-preservation proof.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-29-SUMMARY.md` when done.
</output>
