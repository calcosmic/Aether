---
phase: 199-front-door-and-classic-contract
plan: "13"
type: execute
wave: 9
depends_on: ["199-04", "199-05", "199-12"]
files_modified:
  - cmd/session_flow_cmds.go
  - cmd/session_compat.go
  - cmd/normalize_args.go
  - cmd/pause_resume_199_test.go
  - .aether/commands/pause.yaml
  - .claude/commands/ant-pause.md
  - .claude/commands/ant/pause.md
  - .opencode/commands/ant/pause.md
  - .aether/commands/resume.yaml
  - .claude/commands/ant-resume.md
  - .claude/commands/ant/resume.md
  - .opencode/commands/ant/resume.md
autonomous: true
requirements: [CEC-04, LIFE-04]
must_haves:
  truths:
    - "Pause commits one validated handoff at a safe boundary with partial work, attempt, decisions, context, signals, evidence, and real lineage exactly once."
    - "Resume is the sole public recovery entry and distinguishes confirmed, reconstructed, conflicting, and unknown evidence before mutation."
    - "Only pause and resume are canonical public commands; old suffixed inputs are bounded hidden parser redirects with a canonical replacement notice."
  artifacts:
    - path: "cmd/pause_resume_199_test.go"
      provides: "Safe-boundary, handoff, provenance, transaction, replay, and conflict proof"
      contains: "TestPauseResume199"
    - path: ".aether/commands/pause.yaml"
      provides: "Canonical public pause contract"
    - path: ".aether/commands/resume.yaml"
      provides: "Canonical sole recovery contract"
  key_links:
    - from: "cmd/session_flow_cmds.go"
      to: "cmd/lifecycle_transaction.go"
      via: "atomic handoff/state/session commit and restore"
      pattern: "LifecycleTransaction"
    - from: "cmd/session_flow_cmds.go"
      to: "pkg/colony/lifecycle.go"
      via: "PauseHandoff and RecoveryProvenance"
      pattern: "PauseHandoff|RecoveryProvenance"
---

<objective>
Replace the pause/resume state flip with a safe-boundary, exactly-once handoff and evidence-based recovery flow.

Purpose: Partial work and worker lineage must survive interruption without duplicate artifacts, while conflicts must stop rather than be guessed away.
Output: Transactional pause/resume, provenance views, hidden old-input redirects, and synchronized canonical wrappers.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@pkg/colony/lifecycle.go
@cmd/lifecycle_facts.go
@cmd/lifecycle_projection.go
@cmd/lifecycle_transaction.go
@cmd/session_flow_cmds.go
@cmd/session_compat.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit and restore one handoff exactly once</name>
  <files>cmd/session_flow_cmds.go, cmd/session_compat.go, cmd/normalize_args.go, cmd/pause_resume_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/session_flow_cmds.go
    - cmd/session_compat.go
    - cmd/normalize_args.go
    - cmd/pause_resume_199_test.go (create; confirm absent)
    - pkg/colony/lifecycle.go
    - pkg/colony/session.go
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_projection.go
    - cmd/lifecycle_transaction.go
    - cmd/worktree_crash_safety_test.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-13 through D-15)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (pause/resume choreography and provenance)
  </read_first>
  <behavior>
    - Active work pauses only at a named safe boundary and produces one transaction-linked handoff.
    - Replaying pause returns `Already paused; the existing validated handoff was retained.` with no duplicate effect.
    - Consistent evidence resumes the safest runnable point; conflicting evidence performs no runnable-state mutation.
    - Old `pause-colony`/`resume-colony` input maps to canonical commands with a deprecation notice but is absent from Cobra aliases/help.
  </behavior>
  <action>Per D-13, D-14, D-15, and SYN-199-06, make `pause` and `resume` the primary Cobra `Use` values and remove public `Aliases`. Add a narrow pre-Cobra argument normalizer for exact first-token `pause-colony` and `resume-colony`, active only through the current 1.28 compatibility window with an explicit 1.29 removal marker; it rewrites to the canonical command, emits the replacement, and never registers help/completion/wrapper names. For pause, derive a safe-boundary state from recorded attempts/receipts; if no safe boundary can be established, return the named wait/block condition without flipping paused state. Build one versioned `PauseHandoff` containing partial work, phase/task/attempt, scoped decisions, context/repository/worktree digests, actual lineage/status, blockers, signal snapshot, last verified evidence, and restart point. Commit handoff, state, session, and receipt in one lifecycle transaction keyed by handoff ID. For resume, evaluate handoff, state, session, activity, repository changes, worktrees, and transaction receipts into Confirmed/Reconstructed/Conflicting/Unknown groups. With no conflict, restore one runnable point transactionally and consume/retain the handoff by recorded status rather than best-effort deletion; with conflict, write nothing and name the exact evidence/decision needed. Remove loader-side session mirror creation from this path. Replay by transaction ID returns the same receipt and never duplicates decisions, workers, artifacts, cleanup, or events.</action>
  <acceptance_criteria>
    - The handoff schema contains every D-14 evidence class and cross-references its state/session transaction.
    - Pause and resume fault matrices cover each transaction stage and exact byte restoration.
    - Repeated pause/resume assertions find one handoff ID, one receipt, and one lifecycle effect.
    - Conflicting fixtures have identical before/after runnable-state fingerprints.
    - Root command metadata contains no public pause-colony/resume-colony alias.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestPauseResume199(SafeBoundary|HandoffContents|ReplayExactlyOnce|Confirmed|Reconstructed|ConflictZeroWrite|HiddenRedirect)$' -count=1</automated>
  </verify>
  <done>Pause preserves one complete recovery point and resume restores only evidence-consistent state through the sole public recovery door.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize the canonical pause surface</name>
  <files>.aether/commands/pause.yaml, .claude/commands/ant-pause.md, .claude/commands/ant/pause.md, .opencode/commands/ant/pause.md</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .aether/commands/pause.yaml
    - .claude/commands/ant-pause.md
    - .claude/commands/ant/pause.md
    - .opencode/commands/ant/pause.md
    - cmd/session_flow_cmds.go
    - cmd/platform_sync.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-13 and D-14)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (exact pause copy)
  </read_first>
  <action>Update canonical pause YAML first with exact description `Stop at a safe boundary and save one resumable handoff.` Remove the `pause-colony` alias declaration and any wrapper-owned handoff/state write. Require the wrapper to render the runtime safe-boundary/transaction receipt and close with `/ant-resume`. Regenerate Claude flat/nested and OpenCode managed files from the YAML source.</action>
  <acceptance_criteria>
    - Canonical YAML has no `pause-colony` alias.
    - Generated descriptions exactly match the approved text.
    - Every wrapper invokes `aether pause` and contains no direct handoff/state mutation.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestPauseWrapperContract199|TestCommandSourceHygiene)$' -count=1</automated>
  </verify>
  <done>The only public pause surface is `/ant-pause`, backed by one Go-owned handoff transaction.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize the canonical resume surface</name>
  <files>.aether/commands/resume.yaml, .claude/commands/ant-resume.md, .claude/commands/ant/resume.md, .opencode/commands/ant/resume.md</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .aether/commands/resume.yaml
    - .claude/commands/ant-resume.md
    - .claude/commands/ant/resume.md
    - .opencode/commands/ant/resume.md
    - cmd/session_flow_cmds.go
    - cmd/session_compat.go
    - cmd/platform_sync.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-13 through D-15)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (exact resume copy)
  </read_first>
  <action>Update canonical resume YAML first with exact description `Validate and restore the safest honest recovery point.` Make it the sole public recovery route; wrappers pass arguments/results to the Go runtime, group runtime-provided provenance, and do not select evidence, edit state, or call recover. Regenerate Claude flat/nested and OpenCode managed files from the YAML definition. Do not expose the hidden old-input normalizer in metadata.</action>
  <acceptance_criteria>
    - Generated descriptions exactly match the approved text.
    - All wrappers invoke `aether resume` and contain no `/ant-recover` or `resume-colony` route.
    - Conflicting recovery is represented as a stop, never a wrapper choice.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestResumeWrapperContract199|TestCommandSourceHygiene)$' -count=1</automated>
  </verify>
  <done>Claude and OpenCode have one recovery front door whose truth and mutation remain runtime-owned.</done>
</task>

</tasks>

<verification>
- Focused safe-boundary, handoff, replay, provenance, conflict, redirect, and wrapper tests pass.
- No public Cobra/YAML/generated metadata declares an old suffixed alias after this plan.
</verification>

<success_criteria>
- Partial work, attempts, decisions, context, lineage, and evidence survive pause exactly once.
- Resume never guesses through conflicting evidence.
- Canonical public vocabulary is pause/resume only.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-13-SUMMARY.md` when done.
</output>
