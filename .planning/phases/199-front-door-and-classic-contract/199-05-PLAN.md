---
phase: 199-front-door-and-classic-contract
plan: "05"
type: execute
wave: 4
depends_on: ["199-03"]
files_modified:
  - cmd/lifecycle_transaction.go
  - cmd/lifecycle_transaction_test.go
  - cmd/lifecycle_transaction_fault_test.go
  - cmd/lifecycle_transaction_cross_root_test.go
autonomous: true
requirements: [CEC-04, CEC-08, LIFE-04, LIFE-05, LIFE-06]
must_haves:
  truths:
    - "A multi-artifact lifecycle operation either commits every declared effect with matching evidence or leaves/reconstructs the pre-operation state."
    - "Interrupted, replayed, or tampered operations resolve from a durable journal without guessing success."
    - "Every mutating lifecycle result names its transaction, verification state, and recovery action."
  artifacts:
    - path: "cmd/lifecycle_transaction.go"
      provides: "Validate-stage-journal-commit-verify-rollback transaction engine"
      exports: ["beginLifecycleTransaction", "resumeLifecycleTransaction"]
    - path: "cmd/lifecycle_transaction_fault_test.go"
      provides: "Failure-injection, tamper, and replay tests"
      contains: "TestLifecycleTransactionFaultMatrix"
  key_links:
    - from: "cmd/lifecycle_transaction.go"
      to: "pkg/colony/lifecycle.go"
      via: "typed transaction record and receipt"
      pattern: "LifecycleTransactionRecord|LifecycleReceipt"
    - from: "cmd/lifecycle_transaction.go"
      to: ".aether/data/transactions"
      via: "owned staging and journal directory"
      pattern: "transactions"
---

<objective>
Create the crash-safe mutation primitive used by pause/resume, closure, territory publication, and maintenance.

Purpose: Sequential `SaveJSON` and best-effort follow-up writes can expose half-completed lifecycle truth. Phase 199 needs one transaction whose evidence survives interruption and replay.
Output: A contained, journaled transaction engine with fault, tamper, rollback, and replay proof.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md
@.planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md
@pkg/colony/lifecycle.go
@pkg/storage/storage.go
@cmd/command_truth.go
@cmd/blackbox_harness_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement contained lifecycle transactions</name>
  <files>cmd/lifecycle_transaction.go, cmd/lifecycle_transaction_test.go, cmd/lifecycle_transaction_cross_root_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/lifecycle_transaction.go (create; confirm absent)
    - cmd/lifecycle_transaction_test.go (create; confirm absent)
    - cmd/lifecycle_transaction_cross_root_test.go (create; confirm absent)
    - pkg/colony/lifecycle.go
    - pkg/storage/storage.go
    - cmd/command_truth.go (migration backup/rollback transaction analog)
    - cmd/codex_colonize_finalize.go (baseline and declared-output validation analog)
    - cmd/blackbox_harness_test.go (backup/replay assertion helpers)
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md (transaction protocol and path containment)
    - .planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md (validate-stage-commit sequence)
  </read_first>
  <behavior>
    - Invalid roots, absolute/out-of-root targets, symlink escapes, duplicate targets, and changed baselines are rejected before mutation.
    - Repository, lifecycle-data, selected hub, resolved platform-home, and exact binary destinations can participate only through an explicit typed allowlist; any other root is rejected.
    - Each allowed root stages its own outputs and pre-images on that root's filesystem while the central coordinator journal records every root subtransaction and digest.
    - Every declared output is staged and digested before one durable intent record becomes commit-authoritative.
    - Successful commit verifies every target digest and emits one typed receipt.
  </behavior>
  <action>Implement an internal multi-root transaction API around `colony.LifecycleTransactionRecord`. Use `.aether/data/transactions/&lt;transaction-id&gt;/` as the durable coordinator journal containing `intent.json`, a root manifest, root subtransaction IDs, target/pre-image digests, commit order, and `receipt.json`. Require callers to choose destinations from a typed allowlist limited to the repository managed root, lifecycle-data root, selected stable/dev hub root, explicitly resolved Claude/OpenCode/Codex platform homes, and one exact binary destination. For each participating root, create staging and pre-images on that root's own filesystem, validate containment and symlink ancestry, close and digest bytes, then use same-filesystem replacement for each target; never depend on a rename crossing filesystems. Persist coordinator intent before any root commits, record each root transition durably, verify every target after commit, and on a later-root failure restore already committed roots from validated local pre-images in reverse order. Expose typed declare-write/removal, validate, commit, rollback, and resume operations. Return one `LifecycleReceipt` naming roots, targets, baselines, verification, coordinator journal, state effect, and recovery command. Never delete journals/pre-images before verified global completion and never infer success from target existence alone.</action>
  <acceptance_criteria>
    - No target mutation occurs before all targets and baselines validate and the durable intent exists.
    - A committed receipt contains target digests equal to bytes on disk.
    - Path containment and symlink rejection have explicit table tests.
    - Cross-filesystem seams are simulated: staging is per root, no cross-root rename is attempted, and failure after one root commits restores every root exactly.
    - The engine uses existing storage/lock primitives and introduces no external dependency.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestLifecycleTransaction(Commit|RejectsInvalidTarget|RejectsChangedBaseline|Receipt|RootAllowlist|PerRootStaging|CrossFilesystemRollback)$' -count=1</automated>
  </verify>
  <done>A successful lifecycle transaction has one verifiable all-target commit and an actionable durable receipt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove rollback, interruption recovery, and exactly-once replay</name>
  <files>cmd/lifecycle_transaction.go, cmd/lifecycle_transaction_fault_test.go, cmd/lifecycle_transaction_cross_root_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/lifecycle_transaction.go
    - cmd/lifecycle_transaction_fault_test.go (create; confirm absent)
    - cmd/lifecycle_transaction_cross_root_test.go
    - cmd/lifecycle_transaction_test.go
    - pkg/colony/lifecycle.go
    - cmd/blackbox_harness_test.go (tamper and restored-byte precedent)
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-14, D-16, D-17)
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md (fault points and recovery rules)
  </read_first>
  <behavior>
    - Failure at each boundary from per-root staging through global verification restores exact pre-image bytes across every committed root or leaves an explicit recoverable coordinator journal.
    - Replaying a committed transaction returns the same receipt without applying effects twice.
    - Tampered staging, intent, backup, or target evidence produces a conflict and no invented rollback/commit.
  </behavior>
  <action>Add named fault hooks after validation, each per-root staged write, coordinator-intent persistence, every root/target commit, and global verification. Implement `resumeLifecycleTransaction` as a deterministic coordinator-state reducer: finish only when intent, root manifests, and digests prove remaining commits safe; otherwise roll back each committed root from matching local pre-images in reverse order; return conflicting/unknown provenance if any evidence is missing or altered. Make replay of a globally committed transaction return its existing receipt. Test multi-root writes plus deletion at every fault point, include a fake EXDEV boundary, compare complete per-root before/after byte maps, and verify no duplicate event/receipt. Preserve all coordinator and root evidence on conflicts.</action>
  <acceptance_criteria>
    - The fault matrix covers every named transaction stage and at least two targets plus one removal.
    - Rollback restores filenames, bytes, and absence/presence exactly.
    - Replay tests assert exactly one receipt/effect by transaction ID.
    - Tamper tests retain evidence and return conflicting provenance.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestLifecycleTransaction(FaultMatrix|MultiRootFaultMatrix|ReplayExactlyOnce|TamperConflict)$' -count=1</automated>
  </verify>
  <done>The transaction primitive fails closed and can safely explain or resume every tested interruption without duplicating effects.</done>
</task>

</tasks>

<verification>
- Focused transaction tests pass and compare actual bytes/digests.
- `rg -n 'os\.Rename|SaveJSON|os\.Remove' cmd/lifecycle_transaction.go` is accounted for by stage/commit/rollback paths covered in the fault matrix.
</verification>

<success_criteria>
- Later mutators need no bespoke multi-file commit protocol.
- Crash, replay, and tamper cases remain honest and recoverable.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-05-SUMMARY.md` when done.
</output>
