---
phase: 199-front-door-and-classic-contract
plan: "16"
type: execute
wave: 11
depends_on: ["199-15"]
files_modified:
  - cmd/entomb_manifest.go
  - cmd/entomb_manifest_199_test.go
  - cmd/entomb_cmd.go
  - cmd/entomb_transaction_199_test.go
  - .aether/commands/entomb.yaml
  - .claude/commands/ant-entomb.md
  - .claude/commands/ant/entomb.md
  - .opencode/commands/ant/entomb.md
autonomous: true
requirements: [CEC-08, LIFE-05]
must_haves:
  truths:
    - "D-17: Seal retains reviewable state with status primary; entomb is a separate optional owner-confirmed archive-and-clear action."
    - "CAP-006 and CAP-007: entomb preserves safe finalization, retained memory, and tombstone behavior in the locked separate post-seal transition."
    - "Entomb refuses any unsealed, inconsistent, or unverifiable closure without clearing active state."
    - "Archive bytes and state/report/XML cross-references verify before chamber/tombstone publication and active-state clear."
    - "The forced-incomplete marker survives in manifest, chamber, tombstone, receipt, and future archive views."
  artifacts:
    - path: "cmd/entomb_manifest.go"
      provides: "Content-digest archive manifest and cross-reference verifier"
      contains: "ArchiveManifest"
    - path: "cmd/entomb_transaction_199_test.go"
      provides: "Ordered stage, interruption, rollback, force-marker, and retained-state proof"
      contains: "TestEntombTransaction199"
    - path: ".aether/commands/entomb.yaml"
      provides: "Canonical optional archive-and-clear wrapper"
  key_links:
    - from: "cmd/entomb_cmd.go"
      to: "cmd/lifecycle_transaction.go"
      via: "stage/verify/publish/clear transaction"
      pattern: "LifecycleTransaction"
    - from: "cmd/entomb_manifest.go"
      to: "pkg/colony/lifecycle.go"
      via: "ArchiveManifest and SealOutcome cross-reference"
      pattern: "ArchiveManifest|SealOutcome"
---

<objective>
Make entomb a separately invoked, verified archive-and-clear transaction.

Purpose: Directory existence is not archive proof, and active state must never be cleared before every archived byte and cross-reference has been verified.
Output: Digest manifest/verifier, ordered transaction, honest failure/success views, and synchronized entomb wrappers.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@pkg/colony/lifecycle.go
@cmd/lifecycle_transaction.go
@cmd/seal_outcome.go
@cmd/entomb_cmd.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a content-addressed archive manifest</name>
  <files>cmd/entomb_manifest.go, cmd/entomb_manifest_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/entomb_manifest.go (create; confirm absent)
    - cmd/entomb_manifest_199_test.go (create; confirm absent)
    - cmd/entomb_cmd.go
    - pkg/colony/lifecycle.go
    - cmd/seal_outcome.go
    - cmd/chamber.go
    - cmd/blackbox_harness_test.go (archive/tamper byte assertions)
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-17)
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md (archive manifest/cross-reference protocol)
  </read_first>
  <behavior>
    - Manifest entries include relative path, kind, size, content digest, and source cross-reference.
    - State, Crowned report, XML/export, findings, learnings, signals, owner checkpoints, rollback, and seal outcome cross-reference one closure identity.
    - Any byte/digest/reference mismatch fails verification.
  </behavior>
  <action>D-17: keep entomb separate, optional, and owner-controlled after reviewable seal state. Per SYN-199-08, implement CAP-006 and CAP-007 with a deterministic `ArchiveManifest` builder/verifier over an explicitly enumerated archive source set, retaining memory and tombstone inputs in this separate transition. Reject absolute paths, symlink escapes, missing required artifacts, duplicate paths, mismatched closure/transaction IDs, and normal/forced seal disagreement. Calculate content digests from bytes, not metadata. Include retained memory/tombstone inputs, report/state/XML cross-references, rollback/checkpoint references, and exact `SealOutcome` disposition/reason. Sort entries deterministically and calculate a canonical manifest digest. Do not clear or publish from the builder.</action>
  <acceptance_criteria>
    - One-byte tamper, missing file, wrong closure ID, and dropped forced marker each fail with a named path/reason.
    - Rebuilding unchanged input produces byte-identical canonical manifest/digest.
    - The manifest records every source that entomb will clear or promise to retain.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestEntombManifest199(Build|Deterministic|Tamper|CrossReferences|ForcedMarker)$' -count=1</automated>
  </verify>
  <done>Entomb has content-level, cross-referenced proof of exactly what it will archive and clear.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stage, verify, publish, then clear transactionally</name>
  <files>cmd/entomb_cmd.go, cmd/entomb_transaction_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/entomb_cmd.go
    - cmd/entomb_transaction_199_test.go (create; confirm absent)
    - cmd/entomb_manifest.go
    - cmd/lifecycle_transaction.go
    - cmd/lifecycle_projection.go
    - cmd/seal_outcome.go
    - cmd/chamber.go
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (entomb ordered stages/copy)
  </read_first>
  <behavior>
    - Unsealed/inconsistent/unverifiable preflight changes nothing.
    - The visible stages occur in exact order: Stage archive, Write digest manifest, Verify bytes and cross-references, Publish chamber and tombstone, Clear active state.
    - Any interruption before verified publication retains active state and supports resume/rollback.
    - Success clears active state only after archive proof and returns init-next-goal.
  </behavior>
  <action>Refactor `entomb` into preflight, exact confirmation `Archive and clear this sealed colony after archive verification? [y/N]`, and one lifecycle transaction. Stage archive bytes, write the digest manifest, verify bytes/cross-references, atomically publish chamber/tombstone, and only then clear active state. Record stage receipts so interruption resumes or rolls back deterministically. On every failure render `Active colony state was retained` plus exact safe inspection/retry path. On success report chamber path, manifest digest, transaction receipt, retained memory/tombstone, normal/forced disposition, and `/ant-init "next goal"`. Prevent entomb from becoming an automatic seal next step; invocation remains separate and owner-controlled.</action>
  <acceptance_criteria>
    - Fault injection at each ordered stage proves active state retained until verified publication.
    - Replay produces one chamber, tombstone, clear effect, and receipt.
    - Forced marker/reason appear in every archive view and cannot be omitted.
    - Directory existence without matching manifest bytes never counts as success.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestEntombTransaction199(RefusalZeroWrite|StageOrder|FaultRetainsActive|ReplayExactlyOnce|ForcedMarker|Success)$' -count=1</automated>
  </verify>
  <done>Archive publication is verified before clear, and all failure/replay paths preserve honest state and evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize the optional entomb wrapper</name>
  <files>.aether/commands/entomb.yaml, .claude/commands/ant-entomb.md, .claude/commands/ant/entomb.md, .opencode/commands/ant/entomb.md, cmd/entomb_transaction_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .aether/commands/entomb.yaml
    - .claude/commands/ant-entomb.md
    - .claude/commands/ant/entomb.md
    - .opencode/commands/ant/entomb.md
    - cmd/entomb_cmd.go
    - cmd/entomb_manifest.go
    - cmd/platform_sync.go
    - cmd/entomb_transaction_199_test.go
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (exact entomb description and transaction order)
  </read_first>
  <action>First implement exact named test `TestEntombWrapperContract199` in `cmd/entomb_transaction_199_test.go`; load canonical/generated files and assert source linkage, exact description, runtime-owned preview/confirmation/stage/verify/publish/clear, and absence of wrapper archive copies, visual parsing, state clearing, or automatic invocation after seal. Then update canonical entomb YAML and regenerate Claude flat/nested and OpenCode files.</action>
  <acceptance_criteria>
    - Generated descriptions match the approved sentence exactly.
    - Wrappers call `aether entomb` and cannot clear state or fabricate verification.
    - Wrapper text describes entomb as separately invoked and optional.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestEntombWrapperContract199$' -count=1 &amp;&amp; go test ./cmd -run '^TestCommandSourceHygiene$' -count=1</automated>
  </verify>
  <done>Claude/OpenCode expose one optional archive command while the Go runtime owns all destructive ordering and proof.</done>
</task>

</tasks>

<verification>
- Manifest, cross-reference, transaction-stage, replay, forced-marker, retained-state, and wrapper tests pass.
- Every failure assertion includes unchanged active state bytes and the exact retained-state sentence.
</verification>

<success_criteria>
- Entomb is safely separable from seal and never clears first.
- Normal/forced closure truth survives every archive artifact and view.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-16-SUMMARY.md` when done.
</output>
