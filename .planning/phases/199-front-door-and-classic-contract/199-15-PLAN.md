---
phase: 199-front-door-and-classic-contract
plan: "15"
type: execute
wave: 10
depends_on: ["199-05", "199-08", "199-12", "199-13"]
files_modified:
  - cmd/seal_outcome.go
  - cmd/seal_outcome_199_test.go
  - cmd/seal_final_review.go
  - cmd/seal_confirmation.go
  - cmd/codex_workflow_cmds.go
  - cmd/seal_render.go
  - cmd/seal_transaction_199_test.go
autonomous: true
requirements: [CEC-08, LIFE-05]
must_haves:
  truths:
    - "D-16: Seal requires verified completion by default and records owner-only forced_incomplete closure without success language."
    - "CAP-026, CAP-036, CAP-039, and CAP-041: seal presents an evidence-backed closure card, retains an explicit owner boundary, fails closed, and records force authority/reason/consequences."
    - "CAP-037: Seal preserves project-local and colony memory with source, scope, digest, and closure provenance."
    - "CAP-038 and CAP-040: seal distinguishes retained learnings/future work from completed scope and emits the useful final summary/next-project handoff."
    - "CAP-042: Signal retention and Hive wisdom promotion obey explicit privacy/policy boundaries and transaction-linked receipts."
    - "Normal seal succeeds only from verified completion and retains an active reviewable Crowned state."
    - "Forced seal requires a direct `--force --reason` owner invocation, records incomplete truth durably, and never uses success language or art."
    - "All seal state, report, registry, session, findings, signals, evidence, rollback, and receipt effects commit transactionally or roll back."
  artifacts:
    - path: "cmd/seal_outcome.go"
      provides: "Typed verified versus forced-incomplete seal preflight/result"
      contains: "SealOutcome"
    - path: "cmd/seal_transaction_199_test.go"
      provides: "Atomic seal, force authority, replay, and renderer truth proof"
      contains: "TestSealTransaction199"
    - path: "cmd/seal_final_review.go"
      provides: "Memory, signal, wisdom, checkpoint, and risk provenance assembled before closure"
  key_links:
    - from: "cmd/codex_workflow_cmds.go"
      to: "cmd/lifecycle_transaction.go"
      via: "single seal commit"
      pattern: "LifecycleTransaction"
    - from: "cmd/seal_render.go"
      to: "cmd/seal_outcome.go"
      via: "distinct verified/forced structured outcomes"
      pattern: "forced_incomplete|verified"
---

<objective>
Make seal an honest, transactional retained closure with visibly distinct verified and forced-incomplete outcomes.

Purpose: Completion truth must never be inferred from a force flag or a partially written report, and sealing must not erase the colony that the owner still needs to inspect.
Output: Typed preflight/outcomes, memory/privacy provenance, atomic seal commit, and exact UI variants; wrapper/skill parity follows in Plan 199-23.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@pkg/colony/lifecycle.go
@cmd/lifecycle_facts.go
@cmd/lifecycle_projection.go
@cmd/lifecycle_transaction.go
@cmd/seal_final_review.go
@cmd/seal_confirmation.go
@cmd/codex_workflow_cmds.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Separate verified and forced-incomplete seal preflights</name>
  <files>cmd/seal_outcome.go, cmd/seal_outcome_199_test.go, cmd/seal_final_review.go, cmd/seal_confirmation.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/seal_outcome.go (create; confirm absent)
    - cmd/seal_outcome_199_test.go (create; confirm absent)
    - cmd/seal_final_review.go
    - cmd/seal_confirmation.go
    - cmd/codex_workflow_cmds.go (existing seal handler and flags)
    - pkg/colony/lifecycle.go
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_projection.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-16 and D-17)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (verified/forced preflight and confirmation copy)
  </read_first>
  <behavior>
    - Verified seal requires completed phases/tasks, passed required gates, evidence, owner checkpoints, and internally consistent lifecycle facts.
    - Incomplete facts refuse normal seal and enumerate unresolved evidence.
    - Forced seal requires both `--force` and nonblank `--reason` on the direct command and uses the exact destructive confirmation.
  </behavior>
  <action>D-16: per SYN-199-07, implement CAP-026, CAP-036, CAP-039, and CAP-041 with a typed `SealPreflight`/`SealOutcome` over `LifecycleFacts` that renders the evidence-backed completion/seal card, preserves the explicit owner boundary, and refuses malformed, conflicting, missing-required, skipped, or unfinished evidence by default. Normal preflight lists completed phases/tasks, gates, evidence, owner checkpoints, residual risk, rollback, and preserved Crowned contents. Forced preflight requires direct owner flags `--force --reason "..."`, enumerates every unfinished/failed/skipped/missing item, records authority, reason, consequences, and `forced_incomplete`, and uses the exact UI-SPEC confirmations. Ensure Autopilot, workers, wrappers, recovery, and finalizers cannot construct or suggest force flags. D-17: keep status as the primary post-seal action and entomb only as an optional owner action. Do not implement Phase 205 owner acceptance.</action>
  <acceptance_criteria>
    - Every normal/forced preflight field is covered by structured tests.
    - Blank reason, wrapper/finalizer force input, and incomplete normal seal are rejected before mutation.
    - Confirmation copy matches the UI-SPEC byte-for-byte after count substitution.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestSealOutcome199(VerifiedPreflight|IncompleteRefusal|ForceAuthority|ConfirmCopy)$' -count=1</automated>
  </verify>
  <done>Seal decides verified versus forced-incomplete truth before any effect and cannot silently upgrade incomplete work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit and render the selected seal outcome atomically</name>
  <files>cmd/codex_workflow_cmds.go, cmd/seal_final_review.go, cmd/seal_render.go, cmd/seal_transaction_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/codex_workflow_cmds.go
    - cmd/seal_render.go (create; confirm absent)
    - cmd/seal_transaction_199_test.go (create; confirm absent)
    - cmd/seal_outcome.go
    - cmd/seal_final_review.go
    - cmd/seal_confirmation.go
    - cmd/lifecycle_transaction.go
    - cmd/lifecycle_projection.go
    - cmd/codex_visuals.go (existing seal art and semantic color helpers)
    - cmd/registry.go
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (forced wording/forbidden success vocabulary)
  </read_first>
  <behavior>
    - Seal commits state, Crowned report, session, registry, outcome, findings/learnings/signals/checkpoints/rollback references, event, and receipt together.
    - Fault/replay is all-or-recoverable and exactly once.
    - Forced rendering uses exact incomplete heading/result and none of the forbidden success discriminators.
    - Sealed active state remains available; status is primary next and entomb optional.
    - Project and colony memories retain source/scope/digest provenance; signal and wisdom decisions retain privacy/policy evidence.
    - `AETHER_HIVE_POLICY=read`, `off`, or invalid never promotes; `promote`/unset may promote only verified, sanitized, non-sensitive eligible material after verified closure.
  </behavior>
  <action>Replace sequential seal writes in `codex_workflow_cmds.go` with one lifecycle transaction covering all durable effects and baselines. Implement CAP-037 through `seal_final_review.go` and the transaction payload: enumerate project-local and colony memory entries with source path/store, repository identity, scope, content digest, originating evidence/decision IDs, sanitization status, and closure transaction ID so provenance survives review and archive. Implement CAP-038 and CAP-040 by rendering retained learnings/future work as explicitly uncompleted material alongside a useful final summary and next-project handoff. Implement CAP-042: classify active/expired/retained signals and eligible wisdom, record privacy/sensitivity filtering and the resolved `AETHER_HIVE_POLICY`, never promote for `read`, `off`, invalid, or forced-incomplete closure, and only after a verified seal commit allow the existing sanitized Hive promotion path under `promote`/unset with a linked promotion receipt. Stage retained signal/wisdom decisions with findings, learnings, checkpoints, risk, rollback, registry/session state, Crowned record, event, and receipt. Leave sealed state active. Render verified and forced branches exactly; forced output must contain no Crowned/success claims. D-17: status is primary Next Up and entomb optional.</action>
  <acceptance_criteria>
    - Fault injection at every target either restores exact pre-seal bytes or leaves a resumable journal.
    - Replay produces one seal event/outcome/receipt/registry transition.
    - Forced output and every durable artifact carry `forced_incomplete` plus owner reason.
    - Active `COLONY_STATE.json` remains present and inspectable after either seal.
    - Memory provenance round-trips by source/scope/digest, and policy/privacy fixtures prove prohibited signal/wisdom promotion never occurs.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestSealTransaction199(Atomic|ReplayExactlyOnce|ForcedTruth|RetainsActiveState|MemoryProvenance|SignalRetention|WisdomPrivacy|HivePolicy|NextActions)$' -count=1</automated>
  </verify>
  <done>Seal produces one honest retained record or no completed effect, with forced incompleteness impossible to mistake for success.</done>
</task>

</tasks>

<verification>
- Focused preflight, authority, transaction, replay, renderer, retained-state, memory-provenance, and privacy-policy tests pass; wrapper parity follows in Plan 199-23.
- Forced output is scanned for every forbidden success phrase in both visual and NO_COLOR modes.
</verification>

<success_criteria>
- Verified closure and owner-forced recordkeeping are unambiguously different.
- A sealed colony remains the active reviewable record until optional entomb.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-15-SUMMARY.md` when done.
</output>
