---
phase: 199-front-door-and-classic-contract
plan: "09"
type: execute
wave: 5
depends_on: ["199-04"]
files_modified:
  - cmd/status.go
  - cmd/lifecycle_status_render.go
  - cmd/lifecycle_status_199_test.go
  - .aether/commands/status.yaml
  - .claude/commands/ant-status.md
  - .claude/commands/ant/status.md
  - .opencode/commands/ant/status.md
autonomous: true
requirements: [CEC-01, CEC-02, LIFE-03]
must_haves:
  truths:
    - "Default status is the complete authoritative snapshot in the approved ten-section order."
    - "Compact status is a strict subset of the same projection and stays at or below twelve non-wrapped lines."
    - "Status never invents an active ant, cost, signal acknowledgement, verification result, or recovery outcome."
  artifacts:
    - path: "cmd/lifecycle_status_render.go"
      provides: "Pure full/compact terminal renderers over LifecycleProjection"
      contains: "renderLifecycleStatus"
    - path: "cmd/lifecycle_status_199_test.go"
      provides: "Section order, compact subset, responsive, and zero-write proofs"
      contains: "TestLifecycleStatus199"
  key_links:
    - from: "cmd/status.go"
      to: "cmd/lifecycle_projection.go"
      via: "one fact load and projection per invocation"
      pattern: "projectLifecycle"
    - from: ".aether/commands/status.yaml"
      to: "cmd/status.go"
      via: "runtime-owned status invocation"
      pattern: "aether status"
---

<objective>
Make status the complete authoritative snapshot and compact status its exact semantic subset.

Purpose: Owners need one reliable place to understand identity, work, evidence, cost, blockers, and Next Up without decoding protocol files.
Output: Shared-projection status renderers, responsive/zero-write tests, and synchronized status wrappers.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@cmd/lifecycle_facts.go
@cmd/lifecycle_projection.go
@cmd/status.go
@cmd/codex_visuals.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render full and compact status from one projection</name>
  <files>cmd/status.go, cmd/lifecycle_status_render.go, cmd/lifecycle_status_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/status.go
    - cmd/lifecycle_status_render.go (create; confirm absent)
    - cmd/lifecycle_status_199_test.go (create; confirm absent)
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_projection.go
    - cmd/next_action_card.go
    - cmd/codex_visuals.go (semantic colors, width, and NO_COLOR helpers)
    - cmd/visuals_config.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-09 through D-11)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (status lines 297-340 and shared result fields)
  </read_first>
  <behavior>
    - Full status renders all ten approved sections in order from the structured result.
    - Compact output selects the approved subset and never exceeds twelve non-wrapped lines.
    - Missing recorded cost is `Unreported`; no active actors is `No ants are active`.
    - Visual, NO_COLOR, and JSON modes carry the same semantic facts and next choices.
  </behavior>
  <action>Refactor the default `status` path to call `loadLifecycleFacts` and `projectLifecycle` exactly once, then render through a new pure status renderer. Order the full view exactly: (1) identity/goal/state revision/milestone, (2) phase/task/dependency/success criteria, (3) active ants/tasks/status/lineage with recent outcomes separate, (4) active pheromones/delivery/ack/measured effect, (5) survey freshness/research/Dreams, (6) memory/findings/gates/verification/attempt evidence, (7) elapsed time/reported cost, (8) recent human-readable history, (9) blockers/debt/unknowns/decisions, (10) Next Up/secondary safe choices. Add `--compact` as a projection selector, not another loader or policy path; include only the UI-SPEC subset and the equal build/run pair when applicable. Per D-09/D-11, use strong Queen voice only for the status opening/standing and never synthesize evidence. Use the existing semantic color map with paired text, 4/8/16/24/32/48/64 spacing rules, row fallback below 64 columns, and no horizontal scroll.</action>
  <acceptance_criteria>
    - Structured tests assert all ten section IDs in exact order.
    - Compact fields are a subset of the full projection and output is at most twelve lines at tested widths.
    - Full/compact/read-error invocations preserve complete workspace/hub fingerprints.
    - JSON tests assert the UI-SPEC shared field names and no `$0` fallback for missing cost.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestLifecycleStatus199(FullOrder|CompactSubset|Responsive|OutputModes|ReadOnly|NoInventedEvidence)$' -count=1</automated>
  </verify>
  <done>Status is the single complete snapshot and compact status is a bounded view of exactly the same facts.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize the authoritative status wrapper</name>
  <files>.aether/commands/status.yaml, .claude/commands/ant-status.md, .claude/commands/ant/status.md, .opencode/commands/ant/status.md</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .aether/commands/status.yaml
    - .claude/commands/ant-status.md
    - .claude/commands/ant/status.md
    - .opencode/commands/ant/status.md
    - cmd/status.go
    - cmd/platform_sync.go
    - cmd/source_check.go
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (status exact copy and authority rules)
  </read_first>
  <action>Update canonical status YAML first with exact description `Show the complete authoritative colony snapshot.`, default-full and optional `--compact` semantics, and guardrails that wrappers display the runtime result rather than read state, infer activity, or calculate cost. Regenerate the managed Claude nested/flat and OpenCode files from that source. Ensure the wrapper passes arguments unchanged and does not replace full status with watch or a compact-only display.</action>
  <acceptance_criteria>
    - All three generated descriptions exactly match the canonical YAML.
    - Each wrapper delegates to `aether status $ARGUMENTS` and contains no state/file interpretation.
    - Source-check finds no status source/generated drift.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestLifecycleStatusWrapper199|TestCommandSourceHygiene)$' -count=1</automated>
  </verify>
  <done>Claude, OpenCode, and raw runtime status all point at the same authoritative projection.</done>
</task>

</tasks>

<verification>
- Focused runtime and wrapper tests pass.
- Full/compact agreement is asserted on structured result paths before terminal text.
</verification>

<success_criteria>
- An owner can obtain every D-09 fact and one safe next choice from default status.
- Compact status is concise without becoming a competing truth source.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-09-SUMMARY.md` when done.
</output>
