---
phase: 199-front-door-and-classic-contract
plan: "17"
type: execute
wave: 12
depends_on: ["199-06", "199-07", "199-13", "199-15", "199-16"]
files_modified:
  - cmd/recover.go
  - cmd/abandon_cmd.go
  - cmd/maintenance_cmd.go
  - cmd/legacy_recovery_199_test.go
  - .aether/commands/recover.yaml
  - .claude/commands/ant-recover.md
  - .claude/commands/ant/recover.md
  - .opencode/commands/ant/recover.md
autonomous: true
requirements: [CEC-04, LIFE-01, LIFE-04, LIFE-05]
must_haves:
  truths:
    - "Resume is the only public recovery door; useful recovery diagnosis remains available through expert maintenance without a second state mutator."
    - "Abandon is not a public lifecycle escape hatch and cannot silently replace the explicit owner-only forced seal record."
    - "Recover is absent from canonical/generated surfaces; the now-hidden zero-write abandon surface is removed in Plan 199-24."
  artifacts:
    - path: "cmd/legacy_recovery_199_test.go"
      provides: "Hidden legacy routing, no-mutation, and public-absence proof"
      contains: "TestLegacyRecoveryCommands199"
    - path: "cmd/maintenance_cmd.go"
      provides: "Read-only recovery diagnosis entry under expert maintenance"
      contains: "recovery"
  key_links:
    - from: "cmd/recover.go"
      to: "cmd/session_flow_cmds.go"
      via: "canonical resume route rather than independent mutation"
      pattern: "resume"
    - from: "cmd/abandon_cmd.go"
      to: "cmd/seal_outcome.go"
      via: "explicit force-seal guidance without invoking force"
      pattern: "seal.*force"
---

<objective>
Retire recover and abandon as public lifecycle commands while preserving useful diagnosis under maintenance.

Purpose: Parallel recovery and abandonment doors undermine the canonical resume path and honest forced-closure record.
Output: Hidden compatibility behavior, expert diagnosis, recover-surface removal, and absence tests; abandon removal follows in Plan 199-24.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@cmd/recover.go
@cmd/abandon_cmd.go
@cmd/session_flow_cmds.go
@cmd/seal_outcome.go
@cmd/maintenance_cmd.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route legacy recovery and abandonment to canonical owner choices</name>
  <files>cmd/recover.go, cmd/abandon_cmd.go, cmd/maintenance_cmd.go, cmd/legacy_recovery_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/recover.go
    - cmd/abandon_cmd.go
    - cmd/maintenance_cmd.go
    - cmd/legacy_recovery_199_test.go (create; confirm absent)
    - cmd/session_flow_cmds.go
    - cmd/seal_outcome.go
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_projection.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-15 and D-16)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (public absence and maintenance boundary)
  </read_first>
  <behavior>
    - Recover is hidden from default help; invoking legacy recover does not restore state and routes to resume.
    - Useful recovery scanning is read-only under maintenance and returns provenance/evidence for resume.
    - Abandon is hidden; invoking it changes nothing and explains the explicit owner-only seal force command without invoking it.
  </behavior>
  <action>Per D-15 and D-16, mark the legacy `recover` and `abandon` Cobra commands hidden and remove them from ordinary suggestions/completion. Split useful recovery scanning into a read-only `maintenance recovery-inspect` result that uses `LifecycleFacts` provenance and always routes actual restoration to `resume`. Make direct legacy recover return a canonical resume migration message with zero state effect; do not retain an independent `--apply` mutator. Make direct abandon return a no-mutation explanation that incomplete recordkeeping requires the owner to invoke `seal --force --reason` explicitly; never construct or call that command autonomously. Preserve forensic raw functions behind internal helpers where maintenance diagnostics need them. No worker, wrapper, recovery loop, or Autopilot path may invoke legacy mutation.</action>
  <acceptance_criteria>
    - Root/default help and completion omit recover/abandon.
    - Legacy invocations have identical before/after state, repository, hub, registry, and Git fingerprints.
    - Maintenance recovery inspection is read-only and names resume as its only recovery action.
    - No code path from abandon invokes forced seal or writes an abandonment state.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestLegacyRecoveryCommands199(Hidden|RecoverRoutesToResume|AbandonZeroWrite|MaintenanceDiagnosis)$' -count=1</automated>
  </verify>
  <done>Legacy inputs cannot compete with resume or honest force-seal authority, while useful diagnosis remains inspectable.</done>
</task>

<task type="auto">
  <name>Task 2: Remove recover from public source/generated surfaces</name>
  <files>.aether/commands/recover.yaml, .claude/commands/ant-recover.md, .claude/commands/ant/recover.md, .opencode/commands/ant/recover.md</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .aether/commands/recover.yaml
    - .claude/commands/ant-recover.md
    - .claude/commands/ant/recover.md
    - .opencode/commands/ant/recover.md
    - .aether/commands/resume.yaml
    - cmd/recover.go
    - cmd/platform_sync.go
    - cmd/source_check.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-15)
  </read_first>
  <action>Delete the canonical recover YAML and all three managed repository wrappers. Rely on managed-header pruning to remove installed copies. Do not replace it with another public recovery command; `/ant-resume` is the only public route.</action>
  <acceptance_criteria>
    - All four public recover files are absent.
    - Source-check reports no orphan or missing generated surface.
    - The canonical resume files remain present and unchanged by deletion.
  </acceptance_criteria>
  <verify>
    <automated>test ! -e .aether/commands/recover.yaml &amp;&amp; test ! -e .claude/commands/ant-recover.md &amp;&amp; test ! -e .claude/commands/ant/recover.md &amp;&amp; test ! -e .opencode/commands/ant/recover.md &amp;&amp; go test ./cmd -run '^TestCommandSourceHygiene$' -count=1</automated>
  </verify>
  <done>Recover no longer exists as a canonical or generated public command.</done>
</task>

</tasks>

<verification>
- Legacy invocation zero-write, maintenance diagnosis, help absence, and source hygiene tests pass.
- Public source/generated inventories contain resume and no recover; Plan 199-24 removes abandon after this runtime retirement.
</verification>

<success_criteria>
- Recovery has one public door and incomplete closure has one explicit owner-controlled record.
- Useful diagnosis remains available without reviving competing mutators.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-17-SUMMARY.md` when done.
</output>
