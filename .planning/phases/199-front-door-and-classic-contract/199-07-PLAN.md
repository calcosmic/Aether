---
phase: 199-front-door-and-classic-contract
plan: "07"
type: execute
wave: 6
depends_on: ["199-04", "199-05", "199-06", "199-08"]
files_modified:
  - cmd/root.go
  - cmd/init_cmd.go
  - cmd/front_door_199_test.go
  - pkg/colony/colony.go
  - .aether/commands/help.yaml
  - .claude/commands/ant-help.md
  - .claude/commands/ant/help.md
  - .opencode/commands/ant/help.md
autonomous: true
requirements: [CEC-01, CEC-02, LIFE-01]
must_haves:
  truths:
    - "A new Claude/OpenCode owner sees one grouped `/ant-init` → `/ant-plan` → `/ant-build 1` or `/ant-run` journey before expert plumbing."
    - "The first init performs required setup and registry work automatically; an active colony is refused without replacement mutation."
    - "Help uses the approved command groups and exact descriptions without exposing protocol-only or retired commands."
    - "D-01: Claude and OpenCode present the guided normal journey while raw Go commands remain plumbing and Codex keeps its existing lifecycle skills."
    - "D-02: Init is the single guided goal front door, performs setup and territory evidence automatically, and refuses an active colony without writes."
    - "CAP-016: Help teaches the one normal journey; CAP-017: init performs required setup and registry work without exposing registry plumbing."
    - "CAP-068: Accepted init persists and displays one versioned charter/episode contract for the accepted goal."
  artifacts:
    - path: "cmd/root.go"
      provides: "Progressive Cobra help grouping and command visibility policy"
      contains: "Normal journey"
    - path: "cmd/init_cmd.go"
      provides: "Five-stage guided init using typed territory freshness and persisted accepted charter"
      contains: "initCmd"
    - path: "pkg/colony/colony.go"
      provides: "Versioned accepted charter/episode state contract"
      contains: "Charter"
    - path: ".aether/commands/help.yaml"
      provides: "Canonical public command hierarchy"
  key_links:
    - from: "cmd/init_cmd.go"
      to: "cmd/lifecycle_projection.go"
      via: "accepted goal identity and next action"
      pattern: "projectLifecycle"
    - from: "cmd/init_cmd.go"
      to: "cmd/survey_staleness.go"
      via: "typed Fresh/Refreshed/Stale/Unavailable territory result supplied by Plan 199-08"
      pattern: "(Fresh|Refreshed|Stale|Unavailable)"
    - from: "cmd/init_cmd.go"
      to: "pkg/colony/colony.go"
      via: "persisted charter/episode contract written in the init transaction"
      pattern: "Charter"
---

<objective>
Ship the visible front door: progressive help and a guided init that owns first-run setup.

Purpose: Owners should understand the normal journey without learning setup, registry, finalizer, or repair plumbing.
Output: Grouped runtime help, automatic five-stage initialization, persisted charter, and synchronized help surfaces; init wrapper/skill parity follows in Plan 199-20.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@cmd/root.go
@cmd/init_cmd.go
@cmd/setup_cmd.go
@cmd/codex_workflow_cmds.go
@cmd/registry.go
@cmd/lifecycle_projection.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make help and init the one normal front door</name>
  <files>cmd/root.go, cmd/init_cmd.go, pkg/colony/colony.go, cmd/front_door_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/root.go
    - cmd/init_cmd.go
    - pkg/colony/colony.go
    - cmd/front_door_199_test.go (create; confirm absent)
    - cmd/setup_cmd.go
    - cmd/codex_workflow_cmds.go (lay-eggs/run/build/plan/seal command definitions)
    - cmd/compatibility_cmds.go (run/watch definitions)
    - cmd/session_flow_cmds.go (pause/resume definitions)
    - cmd/entomb_cmd.go
    - cmd/maintenance_cmd.go
    - cmd/registry.go
    - cmd/lifecycle_projection.go
    - cmd/survey_staleness.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-01, D-02, D-11)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (help order, exact copy, responsive rules)
  </read_first>
  <behavior>
    - Root help shows `Normal journey`, `Steer and inspect`, then `Expert maintenance` in that order.
    - Protocol/finalizer commands, lay-eggs plumbing, recover, abandon, pause-colony, and resume-colony do not appear in ordinary help.
    - Init in an unprepared repository performs setup and creates one accepted goal; init against an active colony refuses without mutation.
    - Empty help begins exactly `No colony is active` then `Start a guided colony for one goal with /ant-init "goal".`; a fixed active fixture asserts the complete compact standing line byte-for-byte from the lifecycle projection.
    - Accepted init renders all five UI-SPEC stages in order and persists the visible charter before the exact `/ant-plan` closeout.
    - Territory renders `Fresh`, `Refreshed`, `Stale—refresh required`, or `Unavailable` from typed evidence, never from prose parsing.
  </behavior>
  <action>D-01: preserve Claude/OpenCode as the guided public vocabulary, raw Go as plumbing, and existing Codex lifecycle skills. D-02: make init the only guided goal entry and refuse an active colony before any mutation. D-11: derive help standing from the shared lifecycle projection. Per SYN-199-01, configure Cobra groups in `root.go` after the command tree is registered. Put exactly init, plan, build, run, status, pause, resume, seal, and entomb in `Normal journey`; focus, feedback, redirect, watch, phase, history, swarm, and existing research/Dreams/memory/findings inspection in `Steer and inspect`; expose only `maintenance` in `Expert maintenance`. Empty-state help must begin with the two exact UI-SPEC lines `No colony is active` and `Start a guided colony for one goal with /ant-init "goal".` Active-colony help must show one compact projection-derived standing line containing colony identity, accepted goal/episode, phase/standing, actual acting castes or explicit `No ants are active`, blockers, and exact Next Up; `TestFrontDoorHelpActiveStanding` compares the full rendered line byte-for-byte for a fixed fixture. Hide protocol/finalizer, setup, mutation, recover, and abandon commands from ordinary help. Apply exact descriptions and responsive/NO_COLOR rules. In `init_cmd.go`, implement the UI-SPEC choreography as five visible ordered stages: Queen opening; setup result `Ready` or `Bootstrapped` (or a truthful setup failure); accepted intent plus a visible charter/episode summary; territory evidence `Fresh`, `Refreshed`, `Stale—refresh required`, or `Unavailable`; and closeout naming colony, goal, created artifacts, and exact next command `/ant-plan`. Reuse the Go setup path without shell recursion, consume Plan 199-08's typed freshness evaluator and automatic refresh result during init, and transactionally persist a versioned `AcceptedCharter`/episode contract with the accepted goal and provenance in the colony state schema. CAP-068 is not a display-only label: reload must reproduce the charter summary from persisted data. `/ant-init` is the approved `do "goal"` equivalent; do not add `do`. After planning, retain equal build/run choice.</action>
  <acceptance_criteria>
    - Golden help tests assert group order, exact descriptions, and absence of every hidden/retired name in visual and plain modes.
    - An empty temporary repository can run init without first invoking lay-eggs.
    - Active-colony refusal has an identical before/after workspace, hub, registry, and Git fingerprint.
    - First init and each territory outcome assert the five stage order, persisted/reloaded charter contents, and exact `/ant-plan` closeout.
    - Empty and active help tests assert the exact standing copy and projection-derived values.
    - Root help at widths 63, 64, and 100 does not horizontally truncate command meaning.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestFrontDoor(HelpGroups|HelpEmptyStanding|HelpActiveStanding|HelpResponsive|InitBootstraps|InitFiveStages|InitTerritoryStates|InitCharterPersists|InitRefusalZeroWrite)$' -count=1</automated>
  </verify>
  <done>The normal runtime journey is obvious, first-run setup is automatic, and advanced plumbing remains inspectable outside the main path.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the public help surface from canonical YAML</name>
  <files>.aether/commands/help.yaml, .claude/commands/ant-help.md, .claude/commands/ant/help.md, .opencode/commands/ant/help.md, cmd/front_door_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .aether/commands/help.yaml
    - .claude/commands/ant-help.md
    - .claude/commands/ant/help.md
    - .opencode/commands/ant/help.md
    - cmd/root.go
    - cmd/platform_sync.go
    - cmd/source_check.go
    - cmd/front_door_199_test.go
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (command hierarchy and exact copy)
  </read_first>
  <action>First add the exact named `TestFrontDoorHelpWrapperParity` semantic test to `cmd/front_door_199_test.go`; it must load canonical/generated files, assert source headers, command order, runtime delegation, exact empty/active standing contract, and forbidden retired/protocol vocabulary. Then update `.aether/commands/help.yaml` as the canonical wrapper definition for the same three groups and runtime-owned help output. List normal commands in approved order, steer/inspect second, and only maintenance for expert work. Remove public pause-colony, resume-colony, recover, and abandon mentions. Regenerate Claude flat/nested and OpenCode files from YAML, keeping bodies semantically identical and headers source-linked. Do not add raw protocol commands or Codex `$ant-*` surfaces.</action>
  <acceptance_criteria>
    - All four files present identical public command membership and ordering.
    - `rg 'pause-colony|resume-colony|/ant-recover|/ant-abandon'` finds no match in these four files.
    - Generated headers point to `.aether/commands/help.yaml`.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestFrontDoorHelpWrapperParity$' -count=1 &amp;&amp; go test ./cmd -run '^TestCommandSourceHygiene$' -count=1</automated>
  </verify>
  <done>Claude and OpenCode help present the same uncluttered normal journey as the Go runtime.</done>
</task>

</tasks>

<verification>
- Focused help/init runtime and wrapper tests pass.
- Help remains meaningful under visual, NO_COLOR, JSON guidance, and narrow terminal fixtures.
</verification>

<success_criteria>
- A beginner sees one understandable journey and can start it without a setup command.
- Claude/OpenCode are the ordinary `/ant-*` vocabulary; raw `aether` remains plumbing and Codex remains on existing lifecycle guidance.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-07-SUMMARY.md` when done.
</output>
