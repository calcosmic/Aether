---
phase: 199-front-door-and-classic-contract
plan: "07"
type: execute
wave: 6
depends_on: ["199-04", "199-05", "199-06"]
files_modified:
  - cmd/root.go
  - cmd/init_cmd.go
  - cmd/front_door_199_test.go
  - .aether/commands/help.yaml
  - .claude/commands/ant-help.md
  - .claude/commands/ant/help.md
  - .opencode/commands/ant/help.md
  - .aether/commands/init.yaml
  - .claude/commands/ant-init.md
  - .claude/commands/ant/init.md
  - .opencode/commands/ant/init.md
autonomous: true
requirements: [CEC-01, CEC-02, LIFE-01]
must_haves:
  truths:
    - "A new Claude/OpenCode owner sees one grouped `/ant-init` → `/ant-plan` → `/ant-build 1` or `/ant-run` journey before expert plumbing."
    - "The first init performs required setup and registry work automatically; an active colony is refused without replacement mutation."
    - "Help uses the approved command groups and exact descriptions without exposing protocol-only or retired commands."
  artifacts:
    - path: "cmd/root.go"
      provides: "Progressive Cobra help grouping and command visibility policy"
      contains: "Normal journey"
    - path: "cmd/init_cmd.go"
      provides: "Guided first-run init with automatic setup"
      contains: "initCmd"
    - path: ".aether/commands/help.yaml"
      provides: "Canonical public command hierarchy"
  key_links:
    - from: "cmd/init_cmd.go"
      to: "cmd/lifecycle_projection.go"
      via: "accepted goal identity and next action"
      pattern: "projectLifecycle"
    - from: ".aether/commands/init.yaml"
      to: "cmd/init_cmd.go"
      via: "runtime invocation and runtime-owned mutation"
      pattern: "aether init"
---

<objective>
Ship the visible front door: progressive help and a guided init that owns first-run setup.

Purpose: Owners should understand the normal journey without learning setup, registry, finalizer, or repair plumbing.
Output: Grouped runtime help, automatic first-run initialization, and synchronized Claude/OpenCode help/init surfaces.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@cmd/root.go
@cmd/init_cmd.go
@cmd/setup_cmd.go
@cmd/codex_workflow_cmds.go
@cmd/registry.go
@cmd/lifecycle_projection.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make help and init the one normal front door</name>
  <files>cmd/root.go, cmd/init_cmd.go, cmd/front_door_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/root.go
    - cmd/init_cmd.go
    - cmd/front_door_199_test.go (create; confirm absent)
    - cmd/setup_cmd.go
    - cmd/codex_workflow_cmds.go (lay-eggs/run/build/plan/seal command definitions)
    - cmd/compatibility_cmds.go (run/watch definitions)
    - cmd/session_flow_cmds.go (pause/resume definitions)
    - cmd/entomb_cmd.go
    - cmd/maintenance_cmd.go
    - cmd/registry.go
    - cmd/lifecycle_projection.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-01, D-02, D-11)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (help order, exact copy, responsive rules)
  </read_first>
  <behavior>
    - Root help shows `Normal journey`, `Steer and inspect`, then `Expert maintenance` in that order.
    - Protocol/finalizer commands, lay-eggs plumbing, recover, abandon, pause-colony, and resume-colony do not appear in ordinary help.
    - Init in an unprepared repository performs setup and creates one accepted goal; init against an active colony refuses without mutation.
  </behavior>
  <action>Per D-01, D-02, D-11, and SYN-199-01, configure Cobra groups in `root.go` at execution/help time over the fully registered command tree so init-order does not matter. Put exactly init, plan, build, run, status, pause, resume, seal, and entomb in `Normal journey`; focus, feedback, redirect, watch, phase, history, swarm, and the existing research/Dreams/memory/findings inspection commands in `Steer and inspect`; expose only the `maintenance` landing in `Expert maintenance`. Hide raw setup, finalizers, migration/cleanup, recover, abandon, and other protocol commands from ordinary help while keeping raw Go plumbing callable where compatibility requires it. Apply the UI-SPEC exact descriptions for init, run, status, pause, resume, seal, entomb, and maintenance, render no horizontal table below 64 columns, and retain text meaning under NO_COLOR. In `init_cmd.go`, extract/reuse the existing Go setup path (no shell recursion) so missing `.aether` companions and registry are prepared before state creation, then create the accepted goal once. `/ant-init` is the approved `do "goal"` equivalent; do not add a new public `do` command. Refuse an active goal without writing backups, registry entries, sessions, or timestamps. After accepted planning, rely on the projection's equal build/run choice rather than recommending one.</action>
  <acceptance_criteria>
    - Golden help tests assert group order, exact descriptions, and absence of every hidden/retired name in visual and plain modes.
    - An empty temporary repository can run init without first invoking lay-eggs.
    - Active-colony refusal has an identical before/after workspace, hub, registry, and Git fingerprint.
    - Root help at widths 63, 64, and 100 does not horizontally truncate command meaning.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestFrontDoor(HelpGroups|HelpResponsive|InitBootstraps|InitRefusalZeroWrite)$' -count=1</automated>
  </verify>
  <done>The normal runtime journey is obvious, first-run setup is automatic, and advanced plumbing remains inspectable outside the main path.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the public help surface from canonical YAML</name>
  <files>.aether/commands/help.yaml, .claude/commands/ant-help.md, .claude/commands/ant/help.md, .opencode/commands/ant/help.md</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .aether/commands/help.yaml
    - .claude/commands/ant-help.md
    - .claude/commands/ant/help.md
    - .opencode/commands/ant/help.md
    - cmd/root.go
    - cmd/platform_sync.go
    - cmd/source_check.go
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (command hierarchy and exact copy)
  </read_first>
  <action>Update `.aether/commands/help.yaml` as the canonical wrapper definition for the same three groups and runtime-owned help output. List the normal commands in approved order, the steer/inspect commands second, and only maintenance for expert work. Remove public mentions of pause-colony, resume-colony, recover, and abandon. Regenerate the managed Claude flat/nested and OpenCode wrapper files from the YAML definition; keep their bodies semantically identical and their headers source-linked. Do not add raw protocol commands or Codex `$ant-*` surfaces.</action>
  <acceptance_criteria>
    - All four files present identical public command membership and ordering.
    - `rg 'pause-colony|resume-colony|/ant-recover|/ant-abandon'` finds no match in these four files.
    - Generated headers point to `.aether/commands/help.yaml`.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestFrontDoorHelpWrapperParity|TestCommandSourceHygiene)$' -count=1</automated>
  </verify>
  <done>Claude and OpenCode help present the same uncluttered normal journey as the Go runtime.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the guided init surface</name>
  <files>.aether/commands/init.yaml, .claude/commands/ant-init.md, .claude/commands/ant/init.md, .opencode/commands/ant/init.md</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .aether/commands/init.yaml
    - .claude/commands/ant-init.md
    - .claude/commands/ant/init.md
    - .opencode/commands/ant/init.md
    - cmd/init_cmd.go
    - cmd/command_guide.go
    - cmd/platform_sync.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-01 and D-02)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (init copy)
  </read_first>
  <action>Make `.aether/commands/init.yaml` own the exact description `Start a guided colony for one goal.` and a wrapper flow that may clarify/synthesize the goal but delegates setup, registry, state creation, and result truth to `aether init`. Remove any instruction requiring lay-eggs first or writing `.aether/data` from the wrapper. Regenerate both Claude locations and OpenCode from the YAML-owned definition. Keep Codex on its existing command-guide/raw lifecycle policy; do not create a Codex `/ant-init` surface.</action>
  <acceptance_criteria>
    - All generated init descriptions exactly match the approved text.
    - No generated init wrapper instructs the host to write colony state or to invoke lay-eggs first.
    - Claude and OpenCode wrapper orchestration is semantically identical.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestFrontDoorInitWrapperParity|TestCommandSourceHygiene)$' -count=1</automated>
  </verify>
  <done>The public init surface is one guided goal entry point backed by Go-owned setup and mutation.</done>
</task>

</tasks>

<verification>
- Focused help/init runtime and wrapper tests pass.
- Help remains meaningful under visual, NO_COLOR, JSON guidance, and narrow terminal fixtures.
</verification>

<success_criteria>
- A beginner sees one understandable journey and can start it without a setup command.
- Claude/OpenCode are the ordinary `/ant-*` vocabulary; raw `aether` remains plumbing and Codex remains on existing lifecycle guidance.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-07-SUMMARY.md` when done.
</output>
