---
phase: 199-front-door-and-classic-contract
plan: "19"
type: execute
wave: 6
depends_on: ["199-06"]
files_modified:
  - cmd/maintenance_wrapper_contract_199_test.go
  - .aether/commands/maintenance.yaml
  - .claude/commands/ant-maintenance.md
  - .claude/commands/ant/maintenance.md
  - .opencode/commands/ant/maintenance.md
autonomous: true
requirements: [CEC-02, LIFE-06]
must_haves:
  truths:
    - "Claude and OpenCode expose the same maintenance landing while Go remains the sole state/evidence authority."
    - "The named semantic wrapper test exists and proves more than generic managed-header hygiene."
  artifacts:
    - path: "cmd/maintenance_wrapper_contract_199_test.go"
      provides: "Exact maintenance wrapper semantics and runtime-authority proof"
      contains: "TestMaintenanceWrapperContract"
    - path: ".aether/commands/maintenance.yaml"
      provides: "Canonical maintenance wrapper source"
      contains: "Inspect or repair Aether internals with preview and rollback."
  key_links:
    - from: ".aether/commands/maintenance.yaml"
      to: "cmd/maintenance_cmd.go"
      via: "one runtime-owned invocation"
      pattern: "aether maintenance"
---

<objective>
Publish the maintenance wrapper only after the expert runtime and live inspection contracts exist.

Purpose: Generated surfaces must be small, mechanically synchronized, and semantically tested rather than borrowing a passing hygiene test.
Output: One named execution test, canonical YAML, and three generated Claude/OpenCode surfaces.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@cmd/maintenance_cmd.go
@cmd/platform_sync.go
@cmd/source_check.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ratchet maintenance wrapper semantics</name>
  <files>cmd/maintenance_wrapper_contract_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/maintenance_cmd.go
    - cmd/maintenance_wrapper_contract_199_test.go (create; confirm absent)
    - .aether/commands/status.yaml (canonical/generated test analog)
    - cmd/command_source_hygiene_test.go
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
  </read_first>
  <behavior>
    - The exact named test fails if canonical/generated descriptions, invocation, operation inventory, or authority boundaries differ.
    - It proves wrappers never mutate state, parse visuals, or invent repair success.
  </behavior>
  <action>Create exact test `TestMaintenanceWrapperContract` before editing wrapper files. Load YAML and all generated surfaces, assert source headers and the exact approved description, prove one `AETHER_OUTPUT_MODE=visual aether maintenance $ARGUMENTS` delegation, and reject wrapper-owned file/state writes, visual parsing, invented repair/evidence, root-level live-skill commands, or Codex-native `$ant-*` additions. Make fixtures fail independently of `TestCommandSourceHygiene`.</action>
  <verify>
    <automated>go test ./cmd -run '^TestMaintenanceWrapperContract$' -count=1</automated>
  </verify>
  <done>The named maintenance semantic test exists and fails on every material wrapper-contract drift.</done>
</task>

<task type="auto">
  <name>Task 2: Publish canonical and generated maintenance surfaces</name>
  <files>.aether/commands/maintenance.yaml, .claude/commands/ant-maintenance.md, .claude/commands/ant/maintenance.md, .opencode/commands/ant/maintenance.md, cmd/maintenance_wrapper_contract_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/maintenance_wrapper_contract_199_test.go
    - cmd/maintenance_cmd.go
    - .aether/commands/maintenance.yaml (create; confirm absent)
    - cmd/platform_sync.go
    - cmd/source_check.go
  </read_first>
  <action>Create canonical maintenance YAML with the exact approved description, runtime invocation, runtime-owned state/evidence, and guardrails from Task 1. Mechanically generate Claude flat/nested and OpenCode surfaces using managed headers. Keep bodies semantically identical and expose live skills only through the maintenance landing.</action>
  <verify>
    <automated>go test ./cmd -run '^TestMaintenanceWrapperContract$' -count=1 &amp;&amp; go test ./cmd -run '^TestCommandSourceHygiene$' -count=1</automated>
  </verify>
  <done>All public maintenance wrappers are source-linked, synchronized, and runtime-authoritative.</done>
</task>

</tasks>

<verification>
- The exact semantic test and the independent source-hygiene test both pass.
</verification>

<success_criteria>
- Expert maintenance is consistently discoverable on Claude/OpenCode without adding a second state authority or deferred Codex-native command skill.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-19-SUMMARY.md` when done.
</output>
