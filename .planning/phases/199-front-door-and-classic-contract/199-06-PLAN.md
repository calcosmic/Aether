---
phase: 199-front-door-and-classic-contract
plan: "06"
type: execute
wave: 5
depends_on: ["199-04", "199-05"]
files_modified:
  - cmd/maintenance_cmd.go
  - cmd/maintenance_cmd_test.go
  - cmd/integrity_cmd.go
  - cmd/source_check.go
  - cmd/maintenance_read_199_test.go
  - cmd/skills.go
  - cmd/maintenance_skills_199_test.go
autonomous: true
requirements: [CEC-02, LIFE-06]
must_haves:
  truths:
    - "Ordinary help can point advanced/internal work to one clearly labeled maintenance front door."
    - "Maintenance inspection reports what it checked and proves it made no state change."
    - "D-01: Advanced mechanics remain discoverable behind one maintenance landing without becoming normal-journey commands."
    - "D-10: Focused maintenance results use the shared closeout contract rather than duplicating status."
    - "D-11: Maintenance consumes the same factual lifecycle projection as other orientation surfaces."
    - "CAP-062: Experts can inspect the current live skill inventory and semantic drift without restoring the deleted cache-rebuild CLI."
  artifacts:
    - path: "cmd/maintenance_cmd.go"
      provides: "Expert maintenance landing and typed operation catalog"
      contains: "maintenanceCmd"
    - path: "cmd/skills.go"
      provides: "Live skill scan and semantic diff maintenance contracts"
      contains: "scan"
  key_links:
    - from: "cmd/maintenance_cmd.go"
      to: "cmd/lifecycle_projection.go"
      via: "shared identity, blockers, next action, and state-effect fields"
      pattern: "projectLifecycle"
    - from: "cmd/maintenance_cmd.go"
      to: "cmd/skills.go"
      via: "typed live-scan and diff catalog operations"
      pattern: "skill.*(scan|diff)"
---

<objective>
Create the expert maintenance landing and make its inspection lane demonstrably read-only.

Purpose: The normal journey needs a clean front door, while advanced integrity and repair capabilities remain discoverable without flooding ordinary help.
Output: A runtime-owned maintenance catalog, read-only integrity/source checks, and live skill scan/diff contracts; wrapper publication follows in Plan 199-19.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@cmd/maintenance.go
@cmd/integrity_cmd.go
@cmd/source_check.go
@cmd/command_truth.go
@cmd/lifecycle_projection.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the expert maintenance landing</name>
  <files>cmd/maintenance_cmd.go, cmd/maintenance_cmd_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/maintenance_cmd.go (create; confirm absent)
    - cmd/maintenance_cmd_test.go (create; confirm absent)
    - cmd/maintenance.go
    - cmd/integrity_cmd.go
    - cmd/source_check.go
    - cmd/update_cmd.go
    - cmd/command_truth.go
    - cmd/registry.go
    - cmd/chamber.go
    - cmd/context.go
    - cmd/lifecycle_projection.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-01, D-10, D-11)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (help grouping and maintenance copy)
  </read_first>
  <behavior>
    - `aether maintenance` is read-only and lists inspection separately from mutation.
    - Every catalog entry names preview availability, mutation class, receipt type, and recovery/rollback command.
    - Internal protocol commands are not promoted into the normal journey.
  </behavior>
  <action>D-01: keep advanced mechanics behind one expert landing. D-10: use the focused closeout contract. D-11: load the shared lifecycle projection rather than infer state. Per SYN-199-09, create a Cobra `maintenance` command with exact short help `Inspect or repair Aether internals with preview and rollback.` Its default action must render an expert catalog for integrity, generated/source parity, update, migration, cleanup, registry, chamber, context, archive inspection, and live skill inspection/diff. Catalog entries carry stable operation IDs, current public/raw command, `read_only` or `mutating`, preview flag, transaction requirement, and recovery action. Use a concise Queen explanation plus structured JSON result; do not execute a repair from the landing. Preserve existing raw commands as plumbing and do not create Codex-native `$ant-*` skills.</action>
  <acceptance_criteria>
    - `aether maintenance` changes no repository/hub bytes or mtimes in a fingerprint test.
    - Visual and JSON modes enumerate the same stable operation IDs.
    - The exact approved short help appears in the Cobra command.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestMaintenanceLanding(ReadOnly|Catalog|OutputModes)$' -count=1</automated>
  </verify>
  <done>Advanced capabilities have one discoverable expert landing that performs no implicit repair.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give inspection commands typed zero-write results</name>
  <files>cmd/integrity_cmd.go, cmd/source_check.go, cmd/maintenance_read_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/integrity_cmd.go
    - cmd/source_check.go
    - cmd/maintenance_read_199_test.go (create; confirm absent)
    - cmd/maintenance_cmd.go
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_projection.go
    - cmd/platform_sync.go
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md (maintenance transaction table)
  </read_first>
  <behavior>
    - Integrity and source checks return stable operation, findings, evidence, verification, state_effect, and next-action fields.
    - Both commands are zero-write for valid, invalid, and missing input states.
    - The maintenance landing can render their result contracts without parsing prose.
  </behavior>
  <action>Extract typed result builders from `integrity_cmd.go` and `source_check.go`, retain their current checks, and add `state_effect: none`, evidence paths/counts, verification status, blockers, and actionable next command. Keep rendering separate from result construction. Add workspace/hub fingerprint tests around pass, drift, and missing-source cases. Do not auto-fix from an inspection command; mutating repairs remain explicit Plan 199-18 operations.</action>
  <acceptance_criteria>
    - JSON has stable fields and no renderer-only parsing is required by maintenance.
    - Drift findings name exact source/generated paths and a recovery command.
    - Before/after fingerprints are identical for every test fixture.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestMaintenanceInspection(ReadOnly|StructuredResult|DriftEvidence)$' -count=1</automated>
  </verify>
  <done>The expert inspection lane is factual, machine-readable, and causally proven read-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restore inspectable live skill scanning and diffing</name>
  <files>cmd/skills.go, cmd/maintenance_cmd.go, cmd/maintenance_skills_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/skills.go (preserved in-process live scan/match/injection functions after Phase 191 CLI deletion)
    - cmd/maintenance_cmd.go
    - cmd/maintenance_skills_199_test.go (create; confirm absent)
    - cmd/skills_test.go
    - .planning/research/v1.28-classic-capability-ledger.md (CAP-062)
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md (NOW-06 and live-scan replacement boundary)
  </read_first>
  <behavior>
    - Live scan enumerates the installed colony/domain skill sources on every invocation and reports source, digest, role/workflow metadata, and parse status.
    - Diff compares two explicit live-scan receipts or a live scan against a supplied manifest and reports added, removed, changed, and invalid entries deterministically.
    - The maintenance surface is read-only and never recreates a cache/index or resurrects `skill-cache-rebuild`, `skill-list`, or `skill-diff` as root CLI commands.
  </behavior>
  <action>Implement exact named `TestMaintenanceSkills199` subtests `LiveScan`, `Diff`, `Invalid`, `ReadOnly`, and `NoLegacyCLI` in `cmd/maintenance_skills_199_test.go` before production edits. Implement CAP-062 concretely by extracting a typed, side-effect-free live inventory receipt from the existing `cmd/skills.go` scanner used by worker-brief assembly, and expose it only as `maintenance skills inspect` plus `maintenance skills diff`. Include stable source identity, path class, content digest, parsed metadata, and errors without writing an index/cache. Diff two receipt/manifest inputs by stable identity and digest, returning deterministic added/removed/changed/invalid sets and `state_effect: none`. Keep matching/injection behavior unchanged and preserve the Phase 191 deletion of standalone skill/cache commands. Run the named tests over temporary hub and repo skill roots, changed/removed/invalid skills, repeat scans, and complete before/after filesystem fingerprints.</action>
  <acceptance_criteria>
    - A newly added or edited skill appears immediately without cache rebuild.
    - Diff results are deterministic and name exact evidence digests.
    - Inspection and diff leave hub/repository bytes and mtimes unchanged.
    - Deleted standalone skill/cache CLI names remain absent from root help and registration.
  </acceptance_criteria>
  <verify>
    <automated>test "$(rg -c '^func TestMaintenanceSkills199\(' cmd/maintenance_skills_199_test.go)" -eq 1 &amp;&amp; for case_name in LiveScan Diff Invalid ReadOnly NoLegacyCLI; do rg -q "\"$case_name\"" cmd/maintenance_skills_199_test.go || exit 1; done &amp;&amp; go test ./cmd -run '^TestMaintenanceSkills199$/(LiveScan|Diff|Invalid|ReadOnly|NoLegacyCLI)$' -count=1</automated>
  </verify>
  <done>CAP-062 has a concrete, execution-proven live scan/diff home without reviving the obsolete cache surface.</done>
</task>

</tasks>

<verification>
- Focused maintenance landing and inspection tests pass.
- Live skill scan/diff tests prove immediate discovery, deterministic drift evidence, and zero writes.
</verification>

<success_criteria>
- Maintenance is available but visually separated from the ordinary lifecycle.
- Inspection is read-only and explicit mutations remain separately gated.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-06-SUMMARY.md` when done.
</output>
