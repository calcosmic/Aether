---
phase: 199-front-door-and-classic-contract
plan: "10"
type: execute
wave: 6
depends_on: ["199-04", "199-09"]
files_modified:
  - cmd/phase.go
  - cmd/lifecycle_phase_199_test.go
  - cmd/history.go
  - cmd/lifecycle_history_199_test.go
  - cmd/orientation_agreement_199_test.go
autonomous: true
requirements: [CEC-01, CEC-02, LIFE-03]
must_haves:
  truths:
    - "D-10: Phase and history provide focused closeouts over the shared orientation contract rather than duplicating full status."
    - "CAP-027: Phase supports current detail, numbered detail, and a deterministic list/all projection over all recorded phases."
    - "CAP-028: Every phase detail includes accepted-plan dependencies and success criteria; CAP-015: history is the canonical event projection."
    - "Phase is a focused read-only projection of objective, dependencies, tasks, actors, evidence, blockers, and the shared Next Up."
    - "History is reverse-chronological human activity with actor, result, and timestamp, not raw event envelopes."
    - "Status, phase, and history agree on identity, phase standing, blockers, and Next Up for the same revision."
  artifacts:
    - path: "cmd/lifecycle_phase_199_test.go"
      provides: "Focused phase semantic and zero-write proof"
      contains: "TestLifecyclePhase199"
    - path: "cmd/lifecycle_history_199_test.go"
      provides: "Human history ordering and evidence proof"
      contains: "TestLifecycleHistory199"
    - path: "cmd/orientation_agreement_199_test.go"
      provides: "Cross-view agreement ratchet"
      contains: "TestOrientationViewsAgree199"
  key_links:
    - from: "cmd/phase.go"
      to: "cmd/lifecycle_projection.go"
      via: "focused phase view selector"
      pattern: "projectLifecycle"
    - from: "cmd/history.go"
      to: "cmd/lifecycle_projection.go"
      via: "history view selector over same facts"
      pattern: "projectLifecycle"
---

<objective>
Turn phase and history into focused, read-only views of the same authoritative projection as status.

Purpose: Major commands should close with useful focused context without reproducing the full dashboard or contradicting it.
Output: Projection-backed phase/history views and a cross-view agreement test.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@cmd/lifecycle_facts.go
@cmd/lifecycle_projection.go
@cmd/phase.go
@cmd/history.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project the focused phase view</name>
  <files>cmd/phase.go, cmd/lifecycle_phase_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/phase.go
    - cmd/lifecycle_phase_199_test.go (create; confirm absent)
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_projection.go
    - cmd/next_action_card.go
    - .aether/commands/phase.yaml
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-10 and D-11)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (phase focused-view contract)
  </read_first>
  <behavior>
    - A valid phase shows identity strip, objective, dependencies, tasks/success criteria, current attempt/ants, verification/evidence, blockers, and shared Next Up.
    - Missing/invalid phase produces a specific read-only error and canonical recovery action.
    - It never expands into the whole status dashboard.
  </behavior>
  <action>D-10: render phase as a focused view with the shared closeout slots, never a duplicate status dashboard. Implement CAP-027 concretely in `cmd/phase.go`: replace phase-local inference with the shared fact load/projection and support current phase detail, existing `--number N` detail, plus explicit `--list` and `--all` selectors. `--list` returns deterministic number/name/state/blocked/verification/attempt summary rows for every accepted phase; `--all` returns each complete focused phase projection in phase-number order. Implement CAP-028 by including accepted-plan dependencies and success criteria in every detail projection, with missing plan evidence reported rather than invented. Reject mutually exclusive selectors and nonexistent numbers without mutation. Build all forms from one `LifecycleFacts` load with common identity, evidence, blockers, projection revision, and resolved Next Up; preserve wrapper invocation and add zero-write, empty, ordering, and malformed-state assertions.</action>
  <acceptance_criteria>
    - Every required focused field is asserted in structured output.
    - The view uses the projection's command/reason unchanged.
    - Before/after workspace/hub fingerprints match for success and error cases.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestLifecyclePhase199(FocusedProjection|List|All|SelectorValidation|MissingPhase|ReadOnly)$' -count=1</automated>
  </verify>
  <done>Phase gives the requested detail plus the shared orientation answer, without duplicating status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project human-readable reverse-chronological history</name>
  <files>cmd/history.go, cmd/lifecycle_history_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/history.go
    - cmd/lifecycle_history_199_test.go (create; confirm absent)
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_projection.go
    - pkg/colony/lifecycle.go
    - .aether/commands/history.yaml
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (history focused-view contract)
  </read_first>
  <behavior>
    - Human history sorts newest first and names event, actor, result, and timestamp.
    - Active work and recent completed outcomes remain distinct.
    - Raw payloads remain available only through JSON/detail, not default prose.
  </behavior>
  <action>Implement CAP-015 by having `history` obtain events/receipts from `LifecycleFacts`, project them to typed human-history rows, sort by authoritative timestamp with deterministic tie-breaking, and render an identity strip plus focused closeout. Translate known event/receipt kinds into plain English while preserving raw payloads only in JSON/detail fields. Do not infer actors from command names or processes and do not repair malformed events during the read.</action>
  <acceptance_criteria>
    - Ordering tests use mixed events/receipts and assert deterministic reverse chronology.
    - Unknown event types render as named unknown evidence rather than disappearing or becoming success.
    - Visual output contains no raw JSON envelope by default.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestLifecycleHistory199(Order|UnknownEvidence|FocusedProjection|ReadOnly)$' -count=1</automated>
  </verify>
  <done>History explains actual activity in human terms while retaining machine detail in structured output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ratchet orientation agreement</name>
  <files>cmd/orientation_agreement_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/orientation_agreement_199_test.go (create; confirm absent)
    - cmd/status.go
    - cmd/phase.go
    - cmd/history.go
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_projection.go
    - cmd/lifecycle_status_199_test.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-09 and D-10)
  </read_first>
  <behavior>
    - For one fixture/revision, status, phase, and history return identical identity, standing, blocker, and Next Up semantics.
    - Rendering order or focused detail cannot change the underlying answer.
  </behavior>
  <action>Create a cross-command table test over empty, planned, executing, paused, blocked, forced-incomplete, and sealed fixtures. Invoke structured builders for status, phase (including list/all), and history, compare shared field values and `projection_revision`, and assert each command reads the fact bundle once. Include a mutation fingerprint around all invocations. This task deliberately covers only these three finalized orientation views; Plan 199-26 owns the cross-lifecycle init/colonize/plan/build/run/pause/resume/seal/entomb closeout ratchet after those implementations are final.</action>
  <acceptance_criteria>
    - Every fixture compares identity, goal, phase, standing, blockers, decisions, next action, and projection revision.
    - Any command-local contradictory next-action branch fails the test.
    - The agreement test observes zero writes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestOrientationViewsAgree199$' -count=1</automated>
  </verify>
  <done>Status, phase, and history are independently focused but semantically identical where their contracts overlap.</done>
</task>

</tasks>

<verification>
- Focused phase/history and agreement tests pass.
- Existing phase/history YAML/generated wrappers remain valid because invocation and runtime authority do not change.
</verification>

<success_criteria>
- Owners can move between summary, phase detail, and history without receiving a different lifecycle answer.
- All three paths are causally read-only.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-10-SUMMARY.md` when done.
</output>
