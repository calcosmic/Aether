---
phase: 199-front-door-and-classic-contract
plan: "32"
type: execute
wave: 13
depends_on: ["199-17", "199-26"]
files_modified:
  - cmd/codex_visuals.go
  - cmd/init_cmd.go
  - cmd/entomb_cmd.go
  - cmd/clash.go
  - cmd/worktree_safety.go
  - cmd/codex_build_worktree.go
  - cmd/worktree_reap.go
  - cmd/runtime_recovery_routes_199_test.go
autonomous: true
requirements: [SYNTH-01, CEC-02, CEC-04, LIFE-03, LIFE-04, LIFE-05]
must_haves:
  truths:
    - "D-15: Every active Go lifecycle suggestion uses raw `aether resume` as the sole recovery door or the explicitly named read-only maintenance inspection route."
    - "Preserved worker work is never advertised through `aether recover`; inspection uses `aether maintenance recovery-inspect` and restoration returns to resume."
    - "The final runtime vocabulary test exercises emitted messages, not only source comments or broad string counts."
  artifacts:
    - path: "cmd/runtime_recovery_routes_199_test.go"
      provides: "Executed lifecycle/worktree recovery-route and forbidden-suggestion proof"
      contains: "TestRuntimeRecoveryRoutes199"
    - path: "cmd/codex_visuals.go"
      provides: "Canonical user-visible Go lifecycle closeout and recovery rendering"
    - path: "cmd/worktree_safety.go"
      provides: "Safe preserved-work guidance through maintenance inspection"
  key_links:
    - from: "cmd/init_cmd.go"
      to: "cmd/session_flow_cmds.go"
      via: "canonical raw resume route for lifecycle restoration"
      pattern: "aether resume"
    - from: "cmd/codex_build_worktree.go"
      to: "cmd/maintenance_cmd.go"
      via: "read-only recovery-inspect route for preserved worker branches"
      pattern: "aether maintenance recovery-inspect"
---

<objective>
Remove retired recovery-door suggestions from every listed active Go runtime path.

Purpose: Correct wrappers and docs are insufficient if runtime failures still tell owners to use a retired mutator.
Output: Canonical lifecycle and worktree recovery messages plus execution-level route tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@cmd/session_flow_cmds.go
@cmd/maintenance_cmd.go
@cmd/lifecycle_closeout.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct lifecycle renderer, init, and entomb suggestions</name>
  <files>cmd/codex_visuals.go, cmd/init_cmd.go, cmd/entomb_cmd.go, cmd/runtime_recovery_routes_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/codex_visuals.go
    - cmd/init_cmd.go
    - cmd/entomb_cmd.go
    - cmd/runtime_recovery_routes_199_test.go (create; confirm absent)
    - cmd/session_flow_cmds.go
    - cmd/maintenance_cmd.go
    - cmd/lifecycle_projection.go
    - cmd/lifecycle_closeout.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-13, D-15, D-17)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (error/recovery presentation)
  </read_first>
  <behavior>
    - Init and ordinary lifecycle recovery messages route restoration to exact raw `aether resume`.
    - Entomb failure or preserved-work messages use status/resume for lifecycle state and `aether maintenance recovery-inspect` only for read-only diagnostics.
    - Visual/NO_COLOR/JSON results expose no `aether recover`, `aether abandon`, `pause-colony`, or `resume-colony` suggestion.
  </behavior>
  <action>Implement D-15 per SYN-199-06 after Plans 199-17 and 199-26 finalize recovery and closeout semantics. Create exact top-level `TestRuntimeRecoveryRoutes199` with named lifecycle and worktree subtests before edits, backed by one reusable package-test helper `collectRuntimeRecoverySuggestions199` that Plan 199-28 can call to ratchet the same executed outputs. In `cmd/codex_visuals.go`, `cmd/init_cmd.go`, and `cmd/entomb_cmd.go`, replace every command-shaped retired recovery suggestion on active result paths with exact raw `aether resume` when the requested action is lifecycle restoration. When the message is only inspecting preserved worker work, use exact read-only `aether maintenance recovery-inspect`, state that mutation is none, and route any later restoration back through resume. Preserve status-primary post-seal review and optional entomb. Remove stale comments that describe retired names as current alternatives, but do not rename internal recovery data types or ordinary English. Exercise the real message builders in visual, NO_COLOR, and JSON modes; do not satisfy the test with source grep alone.</action>
  <acceptance_criteria>
    - All three owned runtime files emit only allowed current recovery routes for every exercised success, refusal, retained-state, and preserved-work fixture.
    - JSON recovery/next fields and human output agree semantically.
    - No lifecycle restoration message routes directly to maintenance or a retired recover/abandon command.
    - `collectRuntimeRecoverySuggestions199` returns the same executed fixture set asserted by `TestRuntimeRecoveryRoutes199` for the final vocabulary ratchet.
    - The exact test definition and lifecycle subtests pass.
  </acceptance_criteria>
  <verify>
    <automated>test "$(rg -c '^func TestRuntimeRecoveryRoutes199\(' cmd/runtime_recovery_routes_199_test.go)" -eq 1 &amp;&amp; go test ./cmd -run '^TestRuntimeRecoveryRoutes199$' -count=1</automated>
  </verify>
  <done>Ordinary runtime lifecycle failures and closeouts point to the sole resume door, with diagnostics clearly separated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct clash and worktree preservation suggestions</name>
  <files>cmd/clash.go, cmd/worktree_safety.go, cmd/codex_build_worktree.go, cmd/worktree_reap.go, cmd/runtime_recovery_routes_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/clash.go
    - cmd/worktree_safety.go
    - cmd/codex_build_worktree.go
    - cmd/worktree_reap.go
    - cmd/runtime_recovery_routes_199_test.go
    - cmd/maintenance_cmd.go
    - cmd/session_flow_cmds.go
    - cmd/worktree_test.go
    - cmd/worktree_crash_safety_test.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-15)
  </read_first>
  <behavior>
    - Clash cancellation, unproven partial work, worktree preservation, and reap notices point to `aether maintenance recovery-inspect` for inspection.
    - An inspection notice never implies that maintenance restores runnable lifecycle state; resume remains the only restoration door.
    - Every kept branch/path remains named and unchanged while only its suggested command changes.
  </behavior>
  <action>Per D-15, replace each user-visible `aether recover` suggestion in the four owned worktree files with exact `aether maintenance recovery-inspect`. Preserve branch names, saved-work detail, non-copy/non-delete truth, and all worktree safety behavior. Add a plain follow-up that runnable lifecycle restoration still occurs through `aether resume` where relevant; do not make maintenance a second state mutator. Extend `TestRuntimeRecoveryRoutes199` to execute clash, dirty/unproven worktree, cancellation, and reap message builders, assert the allowed inspection route plus preserved evidence, and reject all command-shaped retired spellings. Inspect Go AST string literals in these seven explicitly owned runtime files as a backstop, but exclude comments, identifiers such as `Recovery`, and internal filenames from the product-vocabulary decision.</action>
  <acceptance_criteria>
    - Each of the four owned files has an executed fixture proving the precise current route and unchanged preservation evidence.
    - No emitted message or structured suggestion contains a retired lifecycle command.
    - Source-AST checks target string literals only and cannot fail on internal recovery identifiers or generic prose.
    - Existing worktree preservation tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>test "$(rg -c '^func TestRuntimeRecoveryRoutes199\(' cmd/runtime_recovery_routes_199_test.go)" -eq 1 &amp;&amp; rg -q '^func TestWorktree.*(Preserv|Reap|Clash)' cmd/*_test.go &amp;&amp; go test ./cmd -run '^TestRuntimeRecoveryRoutes199$' -count=1 &amp;&amp; go test ./cmd -run '^TestWorktree.*(Preserv|Reap|Clash)' -count=1</automated>
  </verify>
  <done>Every preserved-work message names the explicit read-only inspection route without reviving a competing recovery mutator.</done>
</task>

</tasks>

<verification>
- Executed message-builder tests cover all seven listed Go runtime files and all supported output modes.
- AST string-literal checks reject retired command suggestions while preserving internal recovery implementation names.
</verification>

<success_criteria>
- Active Go runtime output never sends an owner to recover, abandon, pause-colony, or resume-colony.
- Resume remains the sole lifecycle restoration door; maintenance recovery inspection stays read-only and explicit.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-32-SUMMARY.md` when done.
</output>
