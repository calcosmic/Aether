---
phase: 199-front-door-and-classic-contract
plan: "08"
type: execute
wave: 5
depends_on: ["199-04", "199-05"]
files_modified:
  - cmd/survey_staleness.go
  - cmd/survey_staleness_199_test.go
  - cmd/codex_colonize.go
  - cmd/codex_colonize_finalize.go
  - cmd/codex_workflow_cmds.go
  - cmd/codex_plan_finalize.go
  - cmd/territory_lifecycle_199_test.go
  - cmd/territory_wrapper_contract_199_test.go
autonomous: true
requirements: [CEC-01, CEC-02, LIFE-02]
must_haves:
  truths:
    - "The lifecycle classifies territory evidence as missing, fresh, stale, or unavailable from typed evidence."
    - "Init/plan decides when survey work is required and feeds a verified publication into planning without asking the owner to choose colonize."
    - "A failed or partial survey never replaces the last verified territory snapshot."
    - "CAP-049 and CAP-059: One authoritative survey-freshness projection drives status evidence and automatic lifecycle refresh."
  artifacts:
    - path: "cmd/survey_staleness.go"
      provides: "Typed survey freshness classifier with reasons and evidence"
      contains: "SurveyFreshness"
    - path: "cmd/territory_lifecycle_199_test.go"
      provides: "Automatic refresh, transactional publication, and plan-input proof"
      contains: "TestTerritoryLifecycle"
  key_links:
    - from: "cmd/codex_plan_finalize.go"
      to: "cmd/codex_colonize_finalize.go"
      via: "verified territory snapshot ID and digest"
      pattern: "survey|territory"
    - from: "cmd/codex_colonize_finalize.go"
      to: "cmd/lifecycle_transaction.go"
      via: "transactional multi-artifact survey publication"
      pattern: "LifecycleTransaction"
---

<objective>
Make territory understanding automatic, typed, transactionally published, and available to planning.

Purpose: Owners should not need to know whether colonize is an internal prerequisite, and planning must never consume a half-published or stale survey silently.
Output: Freshness classification, automatic refresh routing, atomic survey publication, plan evidence wiring, and wrapper-authority proof.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md
@.planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md
@pkg/colony/lifecycle.go
@cmd/lifecycle_facts.go
@cmd/lifecycle_transaction.go
@cmd/survey_staleness.go
@cmd/codex_colonize_finalize.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace boolean staleness with typed freshness evidence</name>
  <files>cmd/survey_staleness.go, cmd/survey_staleness_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/survey_staleness.go
    - cmd/survey_staleness_199_test.go (create; confirm absent)
    - cmd/lifecycle_facts.go
    - pkg/colony/lifecycle.go
    - cmd/codex_colonize.go
    - cmd/codex_plan_finalize.go
    - .planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md (SYN-199-02, CAP-049, CAP-059)
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md (freshness inputs and unavailable behavior)
  </read_first>
  <behavior>
    - Missing required survey artifacts classify as missing.
    - Matching schema/root/revision/digests classify as fresh.
    - Changed repository inputs or expired revision classify as stale with reasons.
    - I/O or malformed evidence classifies as unavailable, never fresh.
  </behavior>
  <action>Per SYN-199-02, implement CAP-049 and CAP-059 by replacing the prose-only survey-status decision with a typed `SurveyFreshnessResult` using `colony.SurveyFreshness`. Include schema version, repository identity/root, source revision, generated time, required artifact digests, age, reason codes, and evidence paths. Retain `surveyStalenessNotice` as a compatibility renderer over the typed result for existing non-lifecycle callers, while status and all automatic lifecycle callers consume the typed result directly. Do not update survey timestamps during classification. Surface unavailable separately from missing/stale so errors are not misrepresented as ordinary refresh needs.</action>
  <acceptance_criteria>
    - Table tests cover all four freshness values and stable reason codes.
    - Classifier tests prove zero writes and no timestamp refresh.
    - Fresh requires verified digests for every declared survey artifact.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestSurveyFreshness199' -count=1</automated>
  </verify>
  <done>Territory freshness is a typed fact with inspectable reasons, not an opaque boolean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh and publish territory automatically before planning</name>
  <files>cmd/codex_colonize.go, cmd/codex_colonize_finalize.go, cmd/codex_workflow_cmds.go, cmd/codex_plan_finalize.go, cmd/territory_lifecycle_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/codex_colonize.go
    - cmd/codex_colonize_finalize.go
    - cmd/codex_workflow_cmds.go
    - cmd/codex_plan_finalize.go
    - cmd/territory_lifecycle_199_test.go (create; confirm absent)
    - cmd/survey_staleness.go
    - cmd/lifecycle_transaction.go
    - cmd/lifecycle_projection.go
    - .aether/commands/colonize.yaml
    - .aether/commands/plan.yaml
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (automatic normal journey boundary)
    - .planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md (manifest/finalizer authority)
  </read_first>
  <behavior>
    - Fresh territory proceeds directly to plan with its snapshot ID/digest.
    - Missing/stale territory produces the existing real surveyor manifest and resumes planning only after verified finalization.
    - Unavailable freshness pauses with evidence and no state mutation.
    - Partial, tampered, or replayed survey completion cannot publish divergent artifacts or duplicate a plan transition.
  </behavior>
  <action>Extract an internal `ensureTerritoryFreshness` contract shared by init and plan, then call it at plan lifecycle preflight. It must return exactly one typed `SurveyFreshnessResult` outcome suitable for the UI-SPEC labels `Fresh`, `Refreshed`, `Stale—refresh required`, or `Unavailable`, with reasons and evidence. For fresh evidence, attach the immutable territory snapshot ID, revision, and digest set to the planning manifest/finalizer input. For missing/stale evidence, invoke the existing colonize manifest/finalizer path as internal lifecycle work and publish all required survey artifacts plus freshness metadata through `lifecycle_transaction`; do not ask the owner to choose colonize. For unavailable evidence, plan returns a material blocker with source error and recovery command, while init can render the same typed unavailable/recovery result after its charter stage. Preserve manual `aether colonize` as expert plumbing. Validate worker-declared paths/results, real spawn-tree evidence, baseline digest, and transaction ID before publication; plan finalization must refuse absent/mismatched territory evidence. Plan 199-07 consumes this function directly rather than duplicating freshness logic. Do not implement Phase 200's Scout/Route-Setter planning iteration.</action>
  <acceptance_criteria>
    - Fresh fixtures dispatch zero survey workers and planning receives the exact existing snapshot digest.
    - Missing/stale fixtures publish all survey artifacts atomically before plan finalization.
    - Fault injection at every publication target leaves the previous snapshot intact or a recoverable transaction journal.
    - Unavailable/tampered fixtures create no accepted plan or fabricated survey activity.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestTerritoryLifecycle(FreshPassThrough|AutomaticRefresh|UnavailableStops|TransactionalPublish|PlanRequiresSnapshot)$' -count=1</automated>
  </verify>
  <done>Planning always receives verified territory evidence, refreshing it automatically only when the typed facts require it.</done>
</task>

<task type="auto">
  <name>Task 3: Ratchet wrapper authority around automatic territory</name>
  <files>cmd/territory_wrapper_contract_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/territory_wrapper_contract_199_test.go (create; confirm absent)
    - .aether/commands/colonize.yaml
    - .aether/commands/plan.yaml
    - .claude/commands/ant/colonize.md
    - .claude/commands/ant/plan.md
    - .opencode/commands/ant/colonize.md
    - .opencode/commands/ant/plan.md
    - cmd/codex_colonize.go
    - cmd/codex_colonize_finalize.go
    - cmd/codex_plan_finalize.go
    - cmd/platform_sync.go
    - .planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md (runtime/finalizer authority guardrails)
  </read_first>
  <action>First implement exact named test `TestTerritoryWrapperAuthority199` in `cmd/territory_wrapper_contract_199_test.go`, proving the canonical YAML and generated Claude/OpenCode wrappers delegate freshness decisions, manifest truth, declared output validation, publication, and plan mutation to the Go runtime/finalizers. Assert wrappers never write survey/state artifacts, parse ANSI, synthesize worker success, or bypass snapshot validation. The researched wrappers already follow the manifest/finalizer authority pattern, so this task ratchets that existing contract without changing their public invocation or introducing a host-owned freshness decision.</action>
  <acceptance_criteria>
    - Tests cover YAML, both Claude locations, and OpenCode for colonize and plan.
    - Every surface invokes an existing runtime/finalizer command and contains no host-owned state write.
    - Source-check and command parity remain green without a Codex `$ant-*` addition.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestTerritoryWrapperAuthority199$' -count=1 &amp;&amp; go test ./cmd -run '^TestCommandSourceHygiene$' -count=1</automated>
  </verify>
  <done>Automatic territory remains Go-owned while Claude/OpenCode wrappers orchestrate only the host work requested by typed manifests.</done>
</task>

</tasks>

<verification>
- All focused freshness, publication, plan-input, and wrapper-authority tests pass.
- Failed publication tests compare the entire prior survey snapshot byte-for-byte.
</verification>

<success_criteria>
- Owners never need to select an internal survey step in the normal journey.
- Planning cannot consume stale, unavailable, partial, or fabricated territory evidence.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-08-SUMMARY.md` when done.
</output>
