---
phase: 199-front-door-and-classic-contract
plan: "25"
type: execute
wave: 13
depends_on: ["199-05", "199-06", "199-16", "199-18"]
files_modified:
  - cmd/chamber.go
  - cmd/context.go
  - cmd/integrity_cmd.go
  - cmd/maintenance_archive_199_test.go
autonomous: true
requirements: [CEC-04, LIFE-05, LIFE-06]
must_haves:
  truths:
    - "Chamber/context inspection is zero-write and cites content/cross-reference evidence."
    - "Create/repair validates preview, baseline, manifest, and seal disposition before a recoverable transaction."
    - "CAP-052, CAP-064, and CAP-065: content-digest chamber creation/verification supplies the shared expert integrity line with exact evidence."
  artifacts:
    - path: "cmd/maintenance_archive_199_test.go"
      provides: "Archive/context inspection, tamper refusal, rollback, and receipt proof"
      contains: "TestMaintenanceArchive199"
    - path: "cmd/integrity_cmd.go"
      provides: "Shared expert chamber/context integrity line over typed digest evidence"
  key_links:
    - from: "cmd/chamber.go"
      to: "cmd/entomb_manifest.go"
      via: "content digest and closure cross-reference verification"
      pattern: "ArchiveManifest"
    - from: "cmd/context.go"
      to: "cmd/lifecycle_transaction.go"
      via: "previewed repair transaction"
      pattern: "LifecycleTransaction"
    - from: "cmd/integrity_cmd.go"
      to: "cmd/chamber.go"
      via: "typed chamber/context digest result"
      pattern: "ArchiveManifest|integrity"
---

<objective>
Verify chamber and context truth before any archive maintenance mutation.

Purpose: Archive repair must preserve historical bytes and forced-incomplete truth instead of normalizing it away.
Output: Separated inspect/repair builders with transaction and fault proof.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@cmd/chamber.go
@cmd/context.go
@cmd/entomb_manifest.go
@cmd/lifecycle_transaction.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build zero-write archive and context inspection</name>
  <files>cmd/chamber.go, cmd/integrity_cmd.go, cmd/maintenance_archive_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/chamber.go
    - cmd/integrity_cmd.go
    - cmd/maintenance_archive_199_test.go (create; confirm absent)
    - cmd/entomb_manifest.go
    - cmd/lifecycle_facts.go
    - cmd/maintenance_cmd.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md
  </read_first>
  <behavior>
    - Inspection reports manifest/content/cross-reference truth and performs zero writes.
    - Missing/conflicting forced-incomplete markers are reported as tamper evidence without mutation.
  </behavior>
  <action>Implement CAP-052, CAP-064, and CAP-065 by separating a pure chamber/archive inspect result builder from all create/repair functions. Reuse `ArchiveManifest` byte digests and cross-reference verification for archived state/report/XML/tombstone data, report missing/conflicting forced markers as tamper evidence, and feed the same typed evidence into the expert integrity view rather than a directory-exists metaphor. Snapshot isolated fixture roots before and after inspection so the test proves byte-for-byte zero-write behavior and assert the integrity line cites the manifest/content result.</action>
  <verify>
    <automated>go test ./cmd -run '^TestMaintenanceArchive199(InspectReadOnly|ManifestRefusal|ForcedMarker)$' -count=1</automated>
  </verify>
  <done>Archive inspection reports digest, cross-reference, and forced-closure truth without changing any byte.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Put archive create and repair behind a recoverable transaction</name>
  <files>cmd/context.go, cmd/maintenance_archive_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/context.go
    - cmd/chamber.go
    - cmd/maintenance_archive_199_test.go
    - cmd/entomb_manifest.go
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_transaction.go
    - cmd/maintenance_cmd.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md
  </read_first>
  <behavior>
    - Mutating create/repair requires explicit preview/checkpoint and a validated baseline.
    - Missing/conflicting forced-incomplete markers block mutation.
    - Fault recovery restores prior historical bytes and leaves an auditable receipt.</behavior>
  <action>Route context/archive create and repair through the Plan 199-05 multi-root lifecycle transaction only after the pure inspection result validates the preview, baseline, manifest, and seal disposition. Report changed files, state effect, receipt, rollback, and next action. Keep historical bytes immutable except through this explicit repair transaction, retaining local pre-images in the journal and refusing missing/conflicting forced markers before any write.</action>
  <verify>
    <automated>go test ./cmd -run '^TestMaintenanceArchive199(RepairRollback|Receipt)$' -count=1</automated>
  </verify>
  <done>Explicit archive repair either commits with a receipt or restores every prior historical byte from journaled pre-images.</done>
</task>

</tasks>

<verification>
- Focused read-only, tamper, rollback, and receipt tests pass in isolated roots.
</verification>

<success_criteria>
- Maintenance cannot silently rewrite archive content or closure disposition.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-25-SUMMARY.md` when done.
</output>
