---
phase: 199-front-door-and-classic-contract
plan: "30"
type: execute
wave: 14
depends_on: ["199-14", "199-17", "199-21", "199-22", "199-24"]
files_modified:
  - .aether/commands/organize.yaml
  - .claude/commands/ant-organize.md
  - .claude/commands/ant/organize.md
  - .opencode/commands/ant/organize.md
  - .aether/skills/colony/context-management/SKILL.md
  - .aether/templates/handoff-build-success.template.md
  - .aether/templates/opencode-md-template.md
  - cmd/recovery_snapshot.go
  - .aether/CONTEXT.md
autonomous: true
requirements: [SYNTH-01, LIFE-03, LIFE-04, PROOF-01]
must_haves:
  truths:
    - "D-13: Current organize, context-management, handoff, and recovery guidance teaches one pause command and one resume command."
    - "Canonical source files are edited before their managed/generated consumers, so publish and update cannot restore retired lifecycle names."
    - "Tracked recovery snapshot output and its Go renderer agree on raw Codex `aether resume` vocabulary."
  artifacts:
    - path: ".aether/commands/organize.yaml"
      provides: "Canonical organize vocabulary guardrail"
    - path: ".aether/skills/colony/context-management/SKILL.md"
      provides: "Current cross-platform context handoff instructions"
    - path: "cmd/recovery_snapshot.go"
      provides: "Canonical raw-CLI recovery snapshot rendering"
  key_links:
    - from: ".aether/commands/organize.yaml"
      to: ".claude/commands/ant-organize.md"
      via: "managed source header and synchronized closeout wording"
      pattern: "ant-organize"
    - from: "cmd/recovery_snapshot.go"
      to: ".aether/CONTEXT.md"
      via: "rendered recovery next-step vocabulary"
      pattern: "aether resume"
---

<objective>
Complete the D-13 current-product vocabulary migration across the remaining canonical and managed sources.

Purpose: Retired pause-colony/resume-colony/recover/abandon teaching in source templates or renderers would be reinstalled even if one generated copy were fixed.
Output: Synchronized organize surfaces, context/handoff templates, and recovery snapshot rendering using only canonical pause/resume vocabulary.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md
@.planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md
@.aether/commands/pause.yaml
@.aether/commands/resume.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synchronize managed ant-organize vocabulary</name>
  <files>.aether/commands/organize.yaml, .claude/commands/ant-organize.md, .claude/commands/ant/organize.md, .opencode/commands/ant/organize.md</files>
  <read_first>
    - AGENTS.md
    - .aether/commands/organize.yaml
    - .claude/commands/ant-organize.md
    - .claude/commands/ant/organize.md
    - .opencode/commands/ant/organize.md
    - .aether/commands/pause.yaml
    - .aether/commands/resume.yaml
  </read_first>
  <action>Implement D-13 in the managed organize family. Add an explicit canonical pause/resume vocabulary guardrail to `.aether/commands/organize.yaml`, then synchronize the Claude flat/nested and OpenCode organize surfaces so their persistence closeout renders exact `/ant-resume` rather than `/ant-resume-colony`. Preserve their managed source headers, runtime-owned state behavior, and the organize-only advisory report exception; do not expose the hidden compatibility redirect or introduce native Codex `$ant-*` skills.</action>
  <acceptance_criteria>
    - The canonical organize YAML explicitly requires current pause/resume vocabulary and all three managed organize surfaces identify it as their source.
    - Each managed organize surface renders `Resume: /ant-resume` and contains no current pause-colony, resume-colony, recover, or abandon instruction.
    - The scoped retired-name grep and exact `TestCommandSourceHygiene` gate both pass.
  </acceptance_criteria>
  <verify>
    <automated>! rg -n '(pause-colony|resume-colony|/ant-(recover|abandon)|aether (recover|abandon))' .aether/commands/organize.yaml .claude/commands/ant-organize.md .claude/commands/ant/organize.md .opencode/commands/ant/organize.md &amp;&amp; go test ./cmd -run '^TestCommandSourceHygiene$' -count=1</automated>
  </verify>
  <done>Publish/update-safe organize sources and all managed copies teach exact `/ant-resume`.</done>
</task>

<task type="auto">
  <name>Task 2: Correct context-management and handoff templates</name>
  <files>.aether/skills/colony/context-management/SKILL.md, .aether/templates/handoff-build-success.template.md, .aether/templates/opencode-md-template.md</files>
  <read_first>
    - AGENTS.md
    - .aether/skills/colony/context-management/SKILL.md
    - .aether/templates/handoff-build-success.template.md
    - .aether/templates/opencode-md-template.md
    - .aether/commands/pause.yaml
    - .aether/commands/resume.yaml
  </read_first>
  <action>Replace current guidance in the context-management skill and both handoff/OpenCode templates with the one-door contract from D-13: Claude/OpenCode use exact `/ant-pause` and `/ant-resume`; raw CLI guidance uses exact `aether pause` and `aether resume`. Collapse the OpenCode session-management table to these canonical entries while preserving semantic descriptions and handoff requirements. Historical Dreams, archived reports, changelog, fixtures, and quoted evidence remain untouched.</action>
  <acceptance_criteria>
    - The context-management skill names only `/ant-pause`, `/ant-resume`, `aether pause`, and `aether resume` for current handoff/recovery actions.
    - The build-success and OpenCode templates contain no current pause-colony/resume-colony/recover/abandon teaching and retain their handoff sections.
    - The scoped retired-name grep exits zero across all three owned files.
  </acceptance_criteria>
  <verify>
    <automated>! rg -n '(pause-colony|resume-colony|/ant-(recover|abandon)|aether (recover|abandon))' .aether/skills/colony/context-management/SKILL.md .aether/templates/handoff-build-success.template.md .aether/templates/opencode-md-template.md</automated>
  </verify>
  <done>Current skill and template guidance exposes one pause door and one resume door per supported host.</done>
</task>

<task type="auto">
  <name>Task 3: Align recovery snapshot renderer and tracked output</name>
  <files>cmd/recovery_snapshot.go, .aether/CONTEXT.md</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/recovery_snapshot.go
    - .aether/CONTEXT.md
    - cmd/session_flow_cmds_test.go
  </read_first>
  <action>Update `cmd/recovery_snapshot.go` so generated next-step, context-collapse, and full-restore guidance all use exact raw CLI `aether resume`, preserving the richer-view explanation without a second public command name. Regenerate or synchronously update tracked `.aether/CONTEXT.md` so it matches the renderer's vocabulary and retains all existing colony data. Do not alter lifecycle state or the hidden parser compatibility path.</action>
  <acceptance_criteria>
    - Neither `cmd/recovery_snapshot.go` nor `.aether/CONTEXT.md` contains current `aether pause-colony`, `aether resume-colony`, `aether recover`, or `aether abandon` guidance.
    - Every recovery next-step emitted by the renderer uses exact `aether resume`, and the tracked output retains its non-vocabulary content.
    - The scoped grep exits zero and exact existing `TestPauseColonyWritesHandoffAndSession` still passes, proving the edit compiles without changing pause persistence.
  </acceptance_criteria>
  <verify>
    <automated>! rg -n '(pause-colony|resume-colony|aether (recover|abandon))' cmd/recovery_snapshot.go .aether/CONTEXT.md &amp;&amp; go test ./cmd -run '^TestPauseColonyWritesHandoffAndSession$' -count=1</automated>
  </verify>
  <done>The raw recovery renderer and tracked context snapshot agree on the single canonical `aether resume` door.</done>
</task>

</tasks>

<verification>
- All nine owned current-product paths pass scoped retired-vocabulary gates.
- Managed organize source hygiene and the existing pause persistence test pass independently.
</verification>

<success_criteria>
- Publish, update, template expansion, and recovery snapshot generation cannot reinstall retired lifecycle vocabulary into current guidance.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-30-SUMMARY.md` when done.
</output>
