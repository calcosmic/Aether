---
phase: 199-front-door-and-classic-contract
plan: "12"
type: execute
wave: 8
depends_on: ["199-03", "199-04", "199-08", "199-11"]
files_modified:
  - cmd/agency_contract.go
  - cmd/agency_receipt_199_test.go
  - cmd/codex_workflow_cmds.go
  - cmd/swarm_cmd.go
  - cmd/swarm_scope_199_test.go
  - cmd/agency_wrapper_contract_199_test.go
autonomous: true
requirements: [CEC-04, LIFE-03]
must_haves:
  truths:
    - "D-07: Steering receipts distinguish delivery, acknowledgement, measured effect, and resulting work effect without fabricated causality."
    - "D-08: Swarm localizes one intervention and preserves independent safe work; unsupported live mechanics remain explicit."
    - "CAP-013: FOCUS remains an ordinary owner-agency verb backed by a durable, evidence-qualified signal receipt."
    - "Every focus, feedback, and redirect action returns a durable signal receipt with honest delivery, acknowledgement, measured effect, and work effect."
    - "Before Phase 203, no surface claims live signal delivery or causal influence without evidence."
    - "Before Phase 202, Swarm reports its localization contract and limitation without simulating typed job events."
  artifacts:
    - path: "cmd/agency_contract.go"
      provides: "Signal and Swarm structured receipt builders/renderers"
      contains: "SignalDeliveryReceipt"
    - path: "cmd/agency_receipt_199_test.go"
      provides: "FOCUS/FEEDBACK/REDIRECT delivery and effect proof"
      contains: "TestAgencyReceipt199"
    - path: "cmd/swarm_scope_199_test.go"
      provides: "Affected-job localization and honest capability-boundary proof"
      contains: "TestSwarmScope199"
  key_links:
    - from: "cmd/codex_workflow_cmds.go"
      to: "cmd/agency_contract.go"
      via: "typed signal write receipt"
      pattern: "SignalDeliveryReceipt"
    - from: "cmd/swarm_cmd.go"
      to: "cmd/lifecycle_projection.go"
      via: "active job/dependency facts and current capability"
      pattern: "LifecycleProjection"
---

<objective>
Deliver the Phase 199 agency contract with honest signal receipts and scoped Swarm behavior boundaries.

Purpose: Owners need proof that steering was stored and where it can take effect, without Phase 199 pretending future causal delivery or live job machinery already exists.
Output: Typed agency results, runtime wiring, scoped Swarm preflight, and cross-wrapper authority tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@pkg/colony/lifecycle.go
@cmd/lifecycle_facts.go
@cmd/lifecycle_projection.go
@cmd/codex_workflow_cmds.go
@cmd/swarm_cmd.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define honest signal and Swarm receipt projections</name>
  <files>cmd/agency_contract.go, cmd/agency_receipt_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/agency_contract.go (create; confirm absent)
    - cmd/agency_receipt_199_test.go (create; confirm absent)
    - pkg/colony/lifecycle.go
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_projection.go
    - cmd/pheromone_mgmt.go
    - cmd/codex_workflow_cmds.go (focus/feedback/redirect handlers)
    - cmd/swarm_cmd.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-07 and D-08)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (steering receipt fields)
  </read_first>
  <behavior>
    - A stored signal has ID, type, owner wording, scope, delivery, acknowledgement, measured effect, and work effect.
    - No Phase 203 delivery evidence yields `next_safe_boundary` or `unsupported`, `Not yet acknowledged`, and `No measured effect yet`.
    - Swarm preflight identifies the affected job/dependency when evidence exists and exposes unsupported localization machinery otherwise.
  </behavior>
  <action>D-07: distinguish delivery, acknowledgement, measured effect, and work effect using only recorded evidence. D-08: define a localized Swarm intervention that preserves independent safe work and reports unsupported live mechanics honestly. Per SYN-199-04, create pure typed result builders and focused renderers over `colony.SignalDeliveryReceipt` plus a `SwarmInterventionContract`. Use only recorded facts to select delivery `live`; otherwise choose `next_safe_boundary` when an existing lifecycle boundary will consume the durable signal, or `unsupported`. Require named runtime evidence for acknowledgement and a linked changed-decision receipt for measured effect. Work effect is one of continued independent work, named conflicting job paused, or no active work. Define Swarm's affected job/checkpoint/dependency fields, independent-work policy, verified-result requirement, resume point, and current capability field. Keep actual Phase 202 events and Phase 203 causal transport out of this plan.</action>
  <acceptance_criteria>
    - Table tests make a `live` or measured-effect claim impossible without linked evidence IDs.
    - FOCUS/FEEDBACK default to continued independent work; REDIRECT cannot pause an unrelated job.
    - Unsupported future capability is explicit in structured and visual output.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestAgencyReceipt199(DeliveryEvidence|Acknowledgement|MeasuredEffect|WorkEffect)$' -count=1</automated>
  </verify>
  <done>The product can acknowledge durable steering honestly today while reserving live/causal claims for real later evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire steering and Swarm to the typed contract</name>
  <files>cmd/codex_workflow_cmds.go, cmd/swarm_cmd.go, cmd/swarm_scope_199_test.go, cmd/agency_wrapper_contract_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/codex_workflow_cmds.go
    - cmd/swarm_cmd.go
    - cmd/swarm_scope_199_test.go (create; confirm absent)
    - cmd/agency_wrapper_contract_199_test.go (create; confirm absent)
    - cmd/agency_contract.go
    - pkg/colony/lifecycle.go
    - .aether/commands/focus.yaml
    - .aether/commands/feedback.yaml
    - .aether/commands/redirect.yaml
    - .aether/commands/swarm.yaml
    - .claude/commands/ant/focus.md
    - .claude/commands/ant/feedback.md
    - .claude/commands/ant/redirect.md
    - .claude/commands/ant/swarm.md
    - .opencode/commands/ant/focus.md
    - .opencode/commands/ant/feedback.md
    - .opencode/commands/ant/redirect.md
    - .opencode/commands/ant/swarm.md
    - .planning/phases/199-front-door-and-classic-contract/199-PATTERNS.md (runtime mutation/wrapper authority)
  </read_first>
  <behavior>
    - Signal commands persist one sanitized signal and render the same typed receipt in JSON/visual modes.
    - REDIRECT selects only a proven conflicting job; otherwise no active work is stopped.
    - Swarm during run reports the affected path and lets independent work continue only where current evidence supports it; unsupported machinery is named.
  </behavior>
  <action>Implement CAP-013 by routing focus, feedback, and redirect handlers through one signal write plus typed receipt. Retain existing sanitization/dedup/reinforcement behavior. Link a receipt to an active job only from recorded task/dependency facts; never process-scan or infer from prose. FOCUS/FEEDBACK must not change unrelated active-job state. REDIRECT may record a pause request only against a job whose declared scope conflicts, otherwise use `next_safe_boundary`. In `swarm_cmd.go`, add the read-only localization preflight and contract result; integrate a result only through the existing verified finalizer path, and explicitly report that typed live checkpoint/pause/resume machinery awaits Phase 202. Add wrapper source tests proving canonical/generated focus/feedback/redirect/swarm surfaces invoke Go and never claim live delivery or mutate state themselves; no wrapper edits are expected because the runtime result remains authoritative.</action>
  <acceptance_criteria>
    - Signal replay reinforces/returns one durable signal identity without duplicate effects.
    - Independent-job state is byte-identical after FOCUS/FEEDBACK and non-conflicting REDIRECT fixtures.
    - Swarm rejects unverified result integration and states the Phase 202 limitation.
    - YAML, Claude, and OpenCode surfaces contain no host-owned acknowledgement/effect claim.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestSwarmScope199|TestAgencyWrapperAuthority199|TestAgencyReceipt199CommandWiring)$' -count=1</automated>
  </verify>
  <done>Owners receive honest steering/Swarm receipts, unrelated work remains unaffected, and future machinery is not simulated.</done>
</task>

</tasks>

<verification>
- Focused receipt, work-effect, Swarm, and wrapper-authority tests pass.
- Source assertions prohibit unsupported `live` claims without evidence IDs.
</verification>

<success_criteria>
- D-07 and D-08 are executable contracts with useful behavior now and explicit Phase 202/203 seams.
- Runtime Go remains the only mutator and evidence source.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-12-SUMMARY.md` when done.
</output>
