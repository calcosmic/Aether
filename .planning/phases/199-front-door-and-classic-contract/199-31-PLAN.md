---
phase: 199-front-door-and-classic-contract
plan: "31"
type: execute
wave: 15
depends_on: ["199-19", "199-30"]
files_modified:
  - README.md
  - AGENTS.md
  - cmd/.opencode/OPENCODE.md
  - docs/phase3-section-commands.md
  - bench/RUNBOOK.md
  - bench/harness/permitted-inputs.md
  - bench/tasks/03-interrupted-execution.md
  - cmd/current_vocabulary_docs_199_test.go
autonomous: true
requirements: [SYNTH-01, CEC-02, CEC-04, LIFE-01, LIFE-04, PROOF-01]
must_haves:
  truths:
    - "D-13: Every active product guide teaches exactly pause/resume for the ordinary lifecycle, using /ant-* on Claude/OpenCode and raw aether commands on Codex."
    - "D-15: Interrupted-execution benchmark inputs use resume as the sole recovery action and never script a retired recover door."
    - "Generated OpenCode project guidance is refreshed from its canonical template rather than becoming an independently edited source."
  artifacts:
    - path: "README.md"
      provides: "Current public lifecycle diagram and command reference"
      contains: "/ant-resume"
    - path: "bench/tasks/03-interrupted-execution.md"
      provides: "Canonical interruption/resume benchmark procedure"
      contains: "/ant-resume"
    - path: "cmd/current_vocabulary_docs_199_test.go"
      provides: "Exact active-document and benchmark vocabulary parity proof"
      contains: "TestCurrentVocabularyDocs199"
  key_links:
    - from: "cmd/.opencode/OPENCODE.md"
      to: ".aether/templates/opencode-md-template.md"
      via: "production project-document generation from the canonical template"
      pattern: "ant-(pause|resume)"
    - from: "bench/harness/permitted-inputs.md"
      to: "bench/tasks/03-interrupted-execution.md"
      via: "identical one-command Aether interruption recovery sequence"
      pattern: "/ant-resume"
---

<objective>
Finish the D-13/D-15 migration in active product documentation and benchmark inputs.

Purpose: Current guides and benchmark scripts are executable instructions. Leaving retired doors in them would retrain owners and automated runs after the canonical command sources are corrected.
Output: Canonical pause/resume documentation, one-command interruption benchmarks, regenerated OpenCode guidance, and exact parity tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@.aether/commands/pause.yaml
@.aether/commands/resume.yaml
@.aether/templates/opencode-md-template.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate active public and platform guides</name>
  <files>README.md, AGENTS.md, cmd/.opencode/OPENCODE.md, docs/phase3-section-commands.md, cmd/current_vocabulary_docs_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - README.md
    - cmd/.opencode/OPENCODE.md
    - docs/phase3-section-commands.md
    - cmd/current_vocabulary_docs_199_test.go (create; confirm absent)
    - .aether/templates/opencode-md-template.md
    - .aether/commands/pause.yaml
    - .aether/commands/resume.yaml
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-01, D-13, D-15)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (help hierarchy and platform vocabulary)
  </read_first>
  <behavior>
    - README lifecycle diagrams/tables and the standalone command reference contain one `/ant-pause` row and one `/ant-resume` row, with no current suffixed or recover/abandon command.
    - Root AGENTS.md teaches exact raw `aether pause` and `aether resume` on Codex and does not advertise a `resume-colony` alias.
    - The generated `cmd/.opencode/OPENCODE.md` uses `/ant-pause` and `/ant-resume` and matches the canonical OpenCode template through the production generator.
  </behavior>
  <action>Implement D-13 and D-15 per SYN-199-06. Create exact top-level `TestCurrentVocabularyDocs199` first. In `README.md` and `docs/phase3-section-commands.md`, replace the session diagram and command table with exactly `/ant-pause` and `/ant-resume`, collapse the old split between full and quick resume, and describe resume's confirmed/reconstructed/conflicting/unknown recovery behavior. Remove public `/ant-recover` and `/ant-abandon` suggestions while preserving unrelated Oracle's `recover` subcommand and ordinary prose words such as recovery or abandoned. In `AGENTS.md`, document raw Codex `aether pause`/`aether resume` only and remove the parenthetical `resume-colony` alias; do not add Codex-native `$ant-*` skills. Treat `.aether/templates/opencode-md-template.md` from Plan 199-30 as authoritative and regenerate `cmd/.opencode/OPENCODE.md` through the existing project-doc generator instead of hand-maintaining divergent content. Preserve all unrelated documentation.</action>
  <acceptance_criteria>
    - The exact test inventories all four owned current-document paths and distinguishes command-shaped retired tokens from unrelated English or Oracle subcommands.
    - README and the standalone reference agree on the two session commands and their current semantics.
    - The generated OpenCode guide is proven to derive from the canonical template and contains no stale lifecycle row.
    - Root Codex guidance contains raw `aether pause` and `aether resume`, with no suffixed alias or Codex `$ant-*` surface.
  </acceptance_criteria>
  <verify>
    <automated>test "$(rg -c '^func TestCurrentVocabularyDocs199\(' cmd/current_vocabulary_docs_199_test.go)" -eq 1 &amp;&amp; go test ./cmd -run '^TestCurrentVocabularyDocs199$' -count=1</automated>
  </verify>
  <done>Every active public/platform guide teaches one pause and one resume door using the correct host vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make interruption benchmarks exercise the sole resume door</name>
  <files>bench/RUNBOOK.md, bench/harness/permitted-inputs.md, bench/tasks/03-interrupted-execution.md, cmd/current_vocabulary_docs_199_test.go</files>
  <read_first>
    - AGENTS.md
    - bench/RUNBOOK.md
    - bench/harness/permitted-inputs.md
    - bench/tasks/03-interrupted-execution.md
    - cmd/current_vocabulary_docs_199_test.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-15)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (`/ant-resume` conflict and maintenance behavior)
  </read_first>
  <behavior>
    - Both Aether benchmark lanes issue exactly one scripted `/ant-resume` after SIGKILL.
    - Runbook, permitted-input rules, and task specification agree byte-for-byte on the scripted sequence.
    - A conflicting resume may name `/ant-maintenance recovery-inspect` as a logged diagnostic, but it is not silently appended to the successful scripted resume action.
  </behavior>
  <action>Replace every Aether interrupted-execution sequence `/ant-resume`, then `/ant-recover` with exactly `/ant-resume` in all three benchmark authorities. Explain that D-15 makes resume itself validate clean handoffs and reconstruct unclean interruptions. If a conflict blocks restoration, name `/ant-maintenance recovery-inspect` only as the explicit, logged expert diagnostic route followed by a fresh `/ant-resume`; do not make it part of an ordinary successful sequence or count it as an invisible intervention. Keep the GSD lane unchanged. Extend `TestCurrentVocabularyDocs199` to parse the three tables/rules, require identical lane commands, count exactly one scripted resume action per Aether lane, and reject `/ant-recover`, `/ant-resume-colony`, `/ant-pause-colony`, or `/ant-abandon` in current benchmark instructions.</action>
  <acceptance_criteria>
    - All Aether interrupted-execution tables and prose specify exactly `/ant-resume` for the normal scripted action.
    - The permitted-input file cannot authorize a second retired command.
    - The diagnostic exception is explicitly logged and routes back to resume rather than becoming another recovery mutator.
    - The exact benchmark-parity subtests pass and fail if any of the three sources drifts.
  </acceptance_criteria>
  <verify>
    <automated>test "$(rg -c '^func TestCurrentVocabularyDocs199\(' cmd/current_vocabulary_docs_199_test.go)" -eq 1 &amp;&amp; go test ./cmd -run '^TestCurrentVocabularyDocs199$' -count=1</automated>
  </verify>
  <done>The benchmark now proves the single smart resume path instead of rehearsing a retired second recovery command.</done>
</task>

</tasks>

<verification>
- `TestCurrentVocabularyDocs199` proves exact active-document inventory, derived OpenCode freshness, and synchronized benchmark commands.
- Scoped command-token checks preserve unrelated Oracle recovery terminology and generic historical prose.
</verification>

<success_criteria>
- Owners and benchmark operators receive one consistent pause/resume contract from every active guide.
- Generated OpenCode guidance cannot drift from its canonical template.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-31-SUMMARY.md` when done.
</output>
