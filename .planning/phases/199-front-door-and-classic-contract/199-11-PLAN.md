---
phase: 199-front-door-and-classic-contract
plan: "11"
type: execute
wave: 7
depends_on: ["199-05", "199-07", "199-08", "199-09"]
files_modified:
  - cmd/compatibility_cmds.go
  - cmd/autopilot_policy.go
  - cmd/run_visuals.go
  - cmd/autopilot_contract_199_test.go
  - cmd/watch_idle_199_test.go
  - .aether/commands/run.yaml
  - .claude/commands/ant-run.md
  - .claude/commands/ant/run.md
  - .opencode/commands/ant/run.md
autonomous: true
requirements: [CEC-02, CEC-04, LIFE-01, LIFE-03]
must_haves:
  truths:
    - "Run before init or accepted planning performs zero work and zero mutation and names the exact next front door."
    - "Valid run displays the accepted goal, remaining phase range, active signals, permitted revisions, and pause boundaries, then starts without a second confirmation."
    - "Run never changes owner-controlled goal/scope/risk/acceptance without pausing; idle watch shows real compact status and recent activity without fake liveness."
  artifacts:
    - path: "cmd/autopilot_policy.go"
      provides: "Typed run preflight, authority fence, pause taxonomy, and debt/repair receipt policy"
      contains: "autopilot"
    - path: "cmd/autopilot_contract_199_test.go"
      provides: "Zero-write entry, operating-card, authority, and completion proofs"
      contains: "TestAutopilotContract199"
    - path: "cmd/watch_idle_199_test.go"
      provides: "Honest idle-watch fallback proof"
      contains: "TestWatchIdle199"
  key_links:
    - from: "cmd/compatibility_cmds.go"
      to: "cmd/lifecycle_projection.go"
      via: "read-only entry preflight and compact idle status"
      pattern: "projectLifecycle"
    - from: "cmd/run_visuals.go"
      to: "cmd/autopilot_policy.go"
      via: "typed operating contract and pause receipt rendering"
      pattern: "Autopilot"
---

<objective>
Turn optional Autopilot into a truthful, bounded normal entry and make idle watch useful without simulating Phase 202 liveness.

Purpose: Build and run must remain equal choices, but run needs a clear operating contract and a hard authority fence before unattended work starts.
Output: Zero-write preflight, auto-start operating card, bounded repair/debt semantics, idle watch fallback, and synchronized run wrappers.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@cmd/lifecycle_facts.go
@cmd/lifecycle_projection.go
@cmd/compatibility_cmds.go
@cmd/autopilot_policy.go
@cmd/run_visuals.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce the Autopilot entry and authority contract</name>
  <files>cmd/compatibility_cmds.go, cmd/autopilot_policy.go, cmd/run_visuals.go, cmd/autopilot_contract_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/compatibility_cmds.go
    - cmd/autopilot_policy.go
    - cmd/run_visuals.go
    - cmd/autopilot_contract_199_test.go (create; confirm absent)
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_projection.go
    - cmd/lifecycle_transaction.go
    - cmd/autopilot_report_test.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-02 through D-06)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (`/ant-run` invalid/valid contract)
  </read_first>
  <behavior>
    - No goal returns `Autopilot did not start`, missing initialized colony, state unchanged, and init next.
    - Goal without accepted plan returns the same refusal with plan next.
    - Valid invocation renders the operating contract and starts the existing loop with no confirmation prompt.
    - A proposed owner-authority change pauses before applying it; bounded in-scope repair emits a receipt and continues within budget.
  </behavior>
  <action>Per D-02, D-03, D-04, D-05, D-06, and SYN-199-05, split `run` into a read-only `AutopilotPreflight` over `LifecycleFacts` and the existing execution loop. On invalid entry, return the exact semantic error block from the UI-SPEC and bypass welcome markers, migrations, session mirrors/backfill, repairs, temporary files, registry updates, and timestamps. On valid entry, emit a typed operating contract containing accepted goal, first/last remaining accepted phases, typed active pheromones, allowed revision scope (tasks, dependencies, sequence, implementation), forbidden owner-authority changes (goal, promised behavior, scope, risk authority, acceptance), and exact pause classes (safety failure, corrupt state, missing authority, material decision, invalidating dependency). Invocation is consent: start immediately. Preserve the existing bounded repair engine/reporting; bind each repair to receipt ID, phase/attempt, failing check, allowed scope, action, verification, remaining budget, and accumulated non-blocking warnings/debt. Do not implement Phase 201's substantive work-cycle redesign or auto-seal at completion.</action>
  <acceptance_criteria>
    - Invalid preflight fixtures have identical repository, hub, registry, session, Git, and temporary-artifact fingerprints.
    - Valid operating-card structured fields and visual order match D-04/UI-SPEC.
    - No code path prompts for confirmation after a valid invocation.
    - Authority-boundary tests prove proposed goal/scope/risk/acceptance changes are not applied.
    - Completion says phases built/verified and leaves seal as an explicit action.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestAutopilotContract199(InvalidZeroWrite|OperatingCard|StartsImmediately|AuthorityFence|RepairReceipt|CompletionDoesNotSeal)$' -count=1</automated>
  </verify>
  <done>Run is an optional equal execution mode with visible bounds, honest receipts, and no silent owner-authority expansion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make idle watch an honest status-plus-activity fallback</name>
  <files>cmd/compatibility_cmds.go, cmd/watch_idle_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/compatibility_cmds.go
    - cmd/watch_idle_199_test.go (create; confirm absent)
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_projection.go
    - cmd/status.go
    - cmd/history.go
    - .aether/commands/watch.yaml
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-12)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (watch boundary)
  </read_first>
  <behavior>
    - With no typed active event source, watch says `No ants are active right now` and renders latest compact status plus recent actual activity.
    - It does not use process lists, spinners, prose, or stale files as liveness evidence.
    - Status remains the named authoritative snapshot.
  </behavior>
  <action>Per D-12, change the existing watch compatibility path so absence of a Phase 202 typed live source takes the explicit idle fallback. Reuse the compact `LifecycleProjection` and human-history rows; include projection revision and evidence timestamps. Remove/disable any visual animation or process inference that could imply current activity. Retain the interface seam for Phase 202 typed events, but render its current capability as unsupported rather than manufacturing events. Do not build the Phase 202 live cockpit machinery.</action>
  <acceptance_criteria>
    - Idle visual and JSON output contain the exact idle sentence, compact status facts, and only recorded recent activity.
    - A stale activity record is labeled by timestamp and never counted as active.
    - Watch performs zero state/filesystem mutation.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestWatchIdle199(StatusFallback|NoFakeLiveness|ReadOnly)$' -count=1</automated>
  </verify>
  <done>Watch remains useful while honestly deferring real live-event semantics to Phase 202.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize the run operating contract</name>
  <files>.aether/commands/run.yaml, .claude/commands/ant-run.md, .claude/commands/ant/run.md, .opencode/commands/ant/run.md</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .aether/commands/run.yaml
    - .claude/commands/ant-run.md
    - .claude/commands/ant/run.md
    - .opencode/commands/ant/run.md
    - cmd/compatibility_cmds.go
    - cmd/autopilot_policy.go
    - cmd/platform_sync.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-03 through D-06)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (exact run copy)
  </read_first>
  <action>Update canonical run YAML first with exact description `Autopilot the remaining accepted phases within the displayed safety contract.` Make the wrapper call the runtime once, treat invocation as consent, and follow typed pause/result fields; forbid a second confirmation, host-authored plan changes, host state writes, visual scraping, fabricated worker events, or auto-seal. Regenerate Claude flat/nested and OpenCode managed files from the YAML definition. Preserve build and run as equal help choices.</action>
  <acceptance_criteria>
    - All generated descriptions match the approved sentence exactly.
    - No wrapper asks `Continue?` or writes lifecycle state.
    - Wrapper guardrails explicitly stop on runtime owner-authority pause results.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^(TestAutopilotWrapperContract199|TestCommandSourceHygiene)$' -count=1</automated>
  </verify>
  <done>The public Autopilot surface delegates execution and authority decisions to the same Go contract on Claude and OpenCode.</done>
</task>

</tasks>

<verification>
- Invalid entry zero-write, valid operating-card, authority fence, repair receipt, idle-watch, and wrapper tests all pass.
- No test depends on the future Phase 202 event stream or Phase 201 work-cycle redesign.
</verification>

<success_criteria>
- Run starts automatically only from a valid accepted plan and stays within displayed authority.
- Warnings/debt accumulate without hiding them; only locked pause classes stop safe independent work.
- Idle watch is truthful and useful today.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-11-SUMMARY.md` when done.
</output>
