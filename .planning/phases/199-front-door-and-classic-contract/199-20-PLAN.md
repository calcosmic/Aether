---
phase: 199-front-door-and-classic-contract
plan: "20"
type: execute
wave: 7
depends_on: ["199-07", "199-08"]
files_modified:
  - cmd/front_door_init_wrapper_199_test.go
  - .aether/commands/init.yaml
  - .claude/commands/ant-init.md
  - .claude/commands/ant/init.md
  - .opencode/commands/ant/init.md
  - .aether/skills/colony/aether-colony-creation/SKILL.md
  - cmd/command_guide.go
  - cmd/command_guide_test.go
autonomous: true
requirements: [CEC-01, CEC-02, LIFE-01]
must_haves:
  truths:
    - "Claude/OpenCode init wrappers and the existing Codex creation skill describe the same five-stage, runtime-owned init contract."
    - "Codex alignment updates existing wrapper-equivalent guidance only; no deferred Codex-native `$ant-*` skill is created."
  artifacts:
    - path: "cmd/front_door_init_wrapper_199_test.go"
      provides: "Cross-platform init choreography and authority parity proof"
      contains: "TestFrontDoorInitWrapperParity"
    - path: ".aether/skills/colony/aether-colony-creation/SKILL.md"
      provides: "Existing Codex init orchestration aligned to accepted charter and territory evidence"
  key_links:
    - from: ".aether/commands/init.yaml"
      to: "cmd/init_cmd.go"
      via: "runtime-owned setup, charter, freshness, state, and closeout"
      pattern: "aether init"
    - from: ".aether/skills/colony/aether-colony-creation/SKILL.md"
      to: "cmd/command_guide.go"
      via: "matching Codex command-guide contract"
      pattern: "command-guide init"
---

<objective>
Synchronize guided init across canonical wrappers and the existing Codex lifecycle orchestration surface.

Purpose: The five-stage runtime contract must remain truthful on every supported platform without adding deferred native Codex commands.
Output: Named parity test, generated init wrappers, updated creation skill, and aligned command guide/tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@cmd/init_cmd.go
@cmd/command_guide.go
@.aether/skills/colony/aether-colony-creation/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define cross-platform init parity</name>
  <files>cmd/front_door_init_wrapper_199_test.go, cmd/command_guide_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/front_door_init_wrapper_199_test.go (create; confirm absent)
    - cmd/command_guide_test.go
    - cmd/init_cmd.go
    - .aether/commands/init.yaml
    - .aether/skills/colony/aether-colony-creation/SKILL.md
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
  </read_first>
  <behavior>
    - The exact named test requires setup, visible accepted charter, four typed territory results, exact closeout, and active-colony zero-write refusal.
    - All surfaces delegate durable mutation and truth to Go.
  </behavior>
  <action>Create exact named `TestFrontDoorInitWrapperParity` and focused command-guide subtests before surface edits. Assert the exact description, five ordered stages, persisted-charter/runtime authority, Fresh/Refreshed/Stale/Unavailable handling, exact `/ant-plan` closeout, active-colony refusal, and absence of direct `.aether/data` writes or required pre-init lay-eggs. Verify the existing Codex skill/guide uses raw `aether init` orchestration and never creates a Codex `$ant-*` surface.</action>
  <verify>
    <automated>go test ./cmd -run '^TestFrontDoorInitWrapperParity$' -count=1 &amp;&amp; go test ./cmd -run '^TestCommandGuideInit199$' -count=1</automated>
  </verify>
  <done>Cross-platform init expectations are executable and independent of generic source hygiene.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize canonical and generated init wrappers</name>
  <files>.aether/commands/init.yaml, .claude/commands/ant-init.md, .claude/commands/ant/init.md, .opencode/commands/ant/init.md, cmd/front_door_init_wrapper_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/front_door_init_wrapper_199_test.go
    - .aether/commands/init.yaml
    - cmd/init_cmd.go
  </read_first>
  <action>Update canonical init YAML with exact `Start a guided colony for one goal.` copy and the five-stage runtime-owned choreography; wrappers may clarify/synthesize intent but must delegate setup, registry, charter/state creation, territory evidence, and result truth to `aether init`. Regenerate Claude/OpenCode surfaces with managed source headers and no direct state writes or setup detour.</action>
  <verify>
    <automated>go test ./cmd -run '^TestFrontDoorInitWrapperParity$' -count=1 &amp;&amp; go test ./cmd -run '^TestCommandSourceHygiene$' -count=1</automated>
  </verify>
  <done>Claude and OpenCode init wrappers agree with the five-stage runtime contract.</done>
</task>

<task type="auto">
  <name>Task 3: Align the existing Codex creation skill and command guide</name>
  <files>.aether/skills/colony/aether-colony-creation/SKILL.md, cmd/command_guide.go, cmd/command_guide_test.go, cmd/front_door_init_wrapper_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - .aether/skills/colony/aether-colony-creation/SKILL.md
    - cmd/command_guide.go
    - cmd/command_guide_test.go
    - cmd/front_door_init_wrapper_199_test.go
    - cmd/init_cmd.go
  </read_first>
  <action>Update the existing Codex creation skill and `command_guide.go` to require the same accepted-charter and typed territory result, active-colony zero-write refusal, and exact `/ant-plan` closeout as the runtime/wrappers. Preserve raw-command Codex orchestration and the Phase 200–205/native `$ant-*` scope fence.</action>
  <verify>
    <automated>go test ./cmd -run '^TestCommandGuideInit199$' -count=1 &amp;&amp; go test ./cmd -run '^TestFrontDoorInitWrapperParity$' -count=1</automated>
  </verify>
  <done>Existing Codex init orchestration agrees with runtime and public wrapper semantics without a native `$ant-*` addition.</done>
</task>

</tasks>

<verification>
- Named wrapper/guide semantics and independent source hygiene pass.
</verification>

<success_criteria>
- No supported platform teaches a setup-first, invisible-charter, or territory-free init flow.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-20-SUMMARY.md` when done.
</output>
