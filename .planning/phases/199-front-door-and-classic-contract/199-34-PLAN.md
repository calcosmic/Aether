---
phase: 199-front-door-and-classic-contract
plan: "34"
type: execute
wave: 13
depends_on: ["199-13", "199-17", "199-18"]
files_modified:
  - cmd/normalize_args.go
  - cmd/hook_cmds.go
  - cmd/recovery_engine.go
  - cmd/recover_visuals.go
  - cmd/recover_repair.go
  - cmd/worktree.go
  - .aether/docs/PARITY_CLASSIC_VS_GO.md
  - cmd/runtime_recovery_compat_199_test.go
autonomous: true
requirements: [SYNTH-01, CEC-02, CEC-04, LIFE-03, LIFE-04, LIFE-06, PROOF-01]
must_haves:
  truths:
    - "D-13/D-15: Persisted legacy resume-colony values normalize once at the compatibility boundary and every downstream state/routing decision uses canonical resume."
    - "No active runtime renderer, repair path, worktree route, or parity guide emits or recommends aether recover --apply."
    - "Useful diagnosis is read-only maintenance inspection and runnable lifecycle restoration returns only through resume."
  artifacts:
    - path: "cmd/normalize_args.go"
      provides: "Sole exact-token compatibility boundary expiring in 1.29"
      contains: "resume-colony"
    - path: "cmd/runtime_recovery_compat_199_test.go"
      provides: "Persisted-state normalization and retired recovery-route execution proof"
      contains: "TestRuntimeRecoveryCompatibility199"
    - path: ".aether/docs/PARITY_CLASSIC_VS_GO.md"
      provides: "Current parity evidence naming maintenance inspection and resume rather than recover"
  key_links:
    - from: "cmd/hook_cmds.go"
      to: "cmd/normalize_args.go"
      via: "legacy LastCommand normalization before post-resume grace evaluation"
      pattern: "normalize.*Command"
    - from: "cmd/recover_visuals.go"
      to: "cmd/maintenance_cmd.go"
      via: "read-only diagnosis route with resume as the only state restoration"
      pattern: "maintenance recovery-inspect|aether resume"
---

<objective>
Close the remaining active runtime/state recovery vocabulary outside the original route inventory.

Purpose: A hidden parser redirect is not sufficient if persisted state still carries a second command identity or current recovery output recommends the retired mutator.
Output: Canonical downstream resume state, retired apply suggestions, maintenance-bound repairs/inspection, corrected worktree routing, updated parity proof, and execution tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@cmd/AGENTS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@cmd/session_flow_cmds.go
@cmd/maintenance_cmd.go
@cmd/lifecycle_transaction.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize persisted legacy command state at one boundary</name>
  <files>cmd/normalize_args.go, cmd/hook_cmds.go, cmd/recovery_engine.go, cmd/runtime_recovery_compat_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/normalize_args.go
    - cmd/hook_cmds.go
    - cmd/recovery_engine.go
    - cmd/runtime_recovery_compat_199_test.go (create; confirm absent)
    - cmd/hook_cmds_test.go
    - cmd/recovery_engine_test.go
    - cmd/session_flow_cmds.go
    - pkg/colony/session.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-13, D-15)
  </read_first>
  <behavior>
    - A persisted legacy `LastCommand=resume-colony` is accepted only through the exact-token compatibility normalizer and is observed downstream as canonical `resume`.
    - New pause/resume writes, hook grace checks, recovery-loop filtering, result fields, receipts, and trace sources use canonical `pause` or `resume` only.
    - Unknown or partial command spellings are not rewritten, and the legacy redirect expires under the existing machine-checked 1.29 marker.
  </behavior>
  <action>Extend the single compatibility boundary in `cmd/normalize_args.go` from Plan 199-13 so exact legacy CLI and persisted command tokens share one in-memory normalizer; do not add another alias map or public Cobra command. In `cmd/hook_cmds.go`, normalize a loaded legacy `SessionFile.LastCommand` before the recent-resume grace check and keep every newly persisted value canonical. In `cmd/recovery_engine.go`, remove the local `resume-colony` switch/alias logic, consume the normalized command ID, and ensure every candidate/filter/result uses canonical `resume`. Keep old bytes as an input-only compatibility fixture and never write the retired value back. Create `TestRuntimeRecoveryCompatibility199` with named legacy-load, canonical-write, hook-grace, exact-token, expiry, and recovery-candidate cases.</action>
  <acceptance_criteria>
    - Outside tests and historical evidence, the only retained old command input tokens are in `cmd/normalize_args.go` with expiry 1.29.
    - Legacy persisted input still receives the correct post-resume grace behavior, but all returned or newly stored command fields equal `resume`.
    - Partial/unknown spellings remain unchanged and cannot broaden compatibility authority.
  </acceptance_criteria>
  <verify>
    <automated>test "$(rg -c '^func TestRuntimeRecoveryCompatibility199\(' cmd/runtime_recovery_compat_199_test.go)" -eq 1 &amp;&amp; go test ./cmd -run '^TestRuntimeRecoveryCompatibility199$' -count=1</automated>
  </verify>
  <done>Legacy stored input remains readable through one expiring boundary while canonical downstream lifecycle state and routing use resume only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retire apply suggestions and route useful recovery through maintenance and resume</name>
  <files>cmd/recover_visuals.go, cmd/recover_repair.go, cmd/worktree.go, .aether/docs/PARITY_CLASSIC_VS_GO.md, cmd/runtime_recovery_compat_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/recover_visuals.go
    - cmd/recover_repair.go
    - cmd/worktree.go
    - .aether/docs/PARITY_CLASSIC_VS_GO.md
    - cmd/runtime_recovery_compat_199_test.go
    - cmd/recover.go
    - cmd/maintenance_cmd.go
    - cmd/maintenance_mutation_199_test.go
    - cmd/session_flow_cmds.go
    - cmd/runtime_recovery_routes_199_test.go
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (recovery and expert-maintenance presentation)
  </read_first>
  <behavior>
    - Executed diagnosis/repair views never emit `aether recover`, `recover --apply`, `/ant-recover`, or a force variant.
    - Read-only diagnosis routes to exact `aether maintenance recovery-inspect`; any state restoration route is exact `aether resume`.
    - A repair that remains useful executes only as an explicit maintenance mutation with preview, transaction, rollback, and receipt; direct legacy recover input mutates nothing.
    - Worktree preservation output keeps branch/path evidence unchanged while pointing inspection to maintenance and lifecycle return to resume.
    - The current parity guide tests the same inspection/return contract instead of advertising recover as the modern proof path.
  </behavior>
  <action>Per D-15 and Plans 199-17/18, remove every active `aether recover --apply` and force suggestion from `cmd/recover_visuals.go`; render recovery findings as read-only maintenance inspection and route the runnable lifecycle only to `aether resume`. In `cmd/recover_repair.go`, retire the independent recover-apply entry contract. Preserve only repairs that are already assigned to an explicit `maintenance` mutation, and make them use Plan 199-18 preview, lifecycle transaction, rollback, authority checkpoint, and typed receipt rather than a second public recovery door. In `cmd/worktree.go`, replace the remaining recover locator with exact `aether maintenance recovery-inspect` while preserving orphan/preserved-work state and the later resume route. Update `.aether/docs/PARITY_CLASSIC_VS_GO.md` so current E2E evidence proves maintenance inspection plus resume, not recover. Extend the execution test across human, `NO_COLOR`, JSON, safe/destructive issue classes, and worktree preservation; inspect Go string literals as a backstop while ignoring internal type/function names.</action>
  <acceptance_criteria>
    - The six newly audited current files contain no retired command-shaped spelling after the task; `cmd/normalize_args.go` is the sole compatibility exception.
    - Executed outputs and JSON next-action fields expose only the maintenance inspection or resume route appropriate to the requested effect.
    - Direct legacy recover/apply inputs perform zero mutation, while any retained maintenance mutation proves preview/receipt/rollback behavior.
    - The parity guide and execution test name the same current public route and evidence.
  </acceptance_criteria>
  <verify>
    <automated>! rg -n '(pause-colony|resume-colony|aether recover|/ant-recover|recover --apply)' .aether/docs/PARITY_CLASSIC_VS_GO.md cmd/hook_cmds.go cmd/recovery_engine.go cmd/recover_visuals.go cmd/recover_repair.go cmd/worktree.go &amp;&amp; test "$(rg -c '^func TestRuntimeRecoveryCompatibility199\(' cmd/runtime_recovery_compat_199_test.go)" -eq 1 &amp;&amp; go test ./cmd -run '^TestRuntimeRecoveryCompatibility199$' -count=1</automated>
  </verify>
  <done>All six newly discovered current surfaces use the canonical inspection/resume contract and no active apply suggestion remains.</done>
</task>

</tasks>

<verification>
- The exact compatibility test proves legacy persisted input normalization, canonical downstream state, output-mode parity, zero-write legacy invocation, maintenance transaction behavior, and worktree evidence retention.
- A scoped command-token gate finds the old spelling only at the 1.29 compatibility boundary or in the separately enumerated historical/test inventory owned by Plan 199-28.
</verification>

<success_criteria>
- Resume is the only lifecycle return door, and current runtime recovery guidance cannot revive recover/apply through state, rendering, repair, documentation, or worktree paths.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-34-SUMMARY.md` when done.
</output>
