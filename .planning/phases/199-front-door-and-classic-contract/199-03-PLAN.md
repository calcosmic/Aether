---
phase: 199-front-door-and-classic-contract
plan: "03"
type: execute
wave: 3
depends_on: ["199-01", "199-02"]
files_modified:
  - pkg/colony/lifecycle.go
  - pkg/colony/lifecycle_test.go
  - pkg/colony/colony.go
  - pkg/colony/session.go
  - pkg/colony/state_machine.go
autonomous: true
requirements: [CEC-01, CEC-04, CEC-08, LIFE-03, LIFE-04, LIFE-05, LIFE-06]
must_haves:
  truths:
    - "Lifecycle facts, receipts, handoffs, recovery provenance, closure outcome, and archive evidence have one versioned durable vocabulary shared by commands."
    - "Legacy colony and session JSON still load without fabricating facts that were never recorded."
  artifacts:
    - path: "pkg/colony/lifecycle.go"
      provides: "Versioned lifecycle enums and durable contracts"
      exports: ["LifecycleReceipt", "PauseHandoff", "SealOutcome", "ArchiveManifest", "SurveyFreshness", "RecoveryProvenance"]
    - path: "pkg/colony/lifecycle_test.go"
      provides: "Validation, JSON compatibility, and illegal-value tests"
  key_links:
    - from: "pkg/colony/colony.go"
      to: "pkg/colony/lifecycle.go"
      via: "additive lifecycle outcome and receipt fields"
      pattern: "Lifecycle|SealOutcome"
    - from: "pkg/colony/session.go"
      to: "pkg/colony/lifecycle.go"
      via: "typed handoff and recovery provenance"
      pattern: "PauseHandoff|RecoveryProvenance"
---

<objective>
Define the durable lifecycle language every Phase 199 reader, renderer, and mutator will share.

Purpose: One authoritative experience is impossible if pause, status, seal, entomb, and maintenance encode the same truth with incompatible strings or maps.
Output: Backward-compatible, versioned Go contracts and validation tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md
@.planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md
@pkg/colony/session.go
@pkg/colony/state_machine.go
@pkg/colony/colony.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create versioned lifecycle value contracts</name>
  <files>pkg/colony/lifecycle.go, pkg/colony/lifecycle_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - pkg/colony/lifecycle.go (create; confirm absent)
    - pkg/colony/lifecycle_test.go (create; confirm absent)
    - pkg/colony/colony.go
    - pkg/colony/session.go
    - pkg/colony/state_machine.go
    - pkg/storage/storage.go
    - .planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md (SYN-199-02 through SYN-199-09)
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-05 through D-17)
    - .planning/phases/199-front-door-and-classic-contract/199-UI-SPEC.md (shared result-object contract)
  </read_first>
  <behavior>
    - Enumerated wire values reject unknown values and serialize deterministically.
    - Forced-incomplete closure cannot validate without an owner reason and unresolved evidence.
    - A recovery result distinguishes confirmed, reconstructed, conflicting, and unknown provenance.
    - A signal receipt distinguishes live, next-safe-boundary, and unsupported delivery.
  </behavior>
  <action>Create `pkg/colony/lifecycle.go` with a single `LifecycleSchemaVersion` and typed string enums for survey freshness (`missing`, `fresh`, `stale`, `unavailable`), transaction stage, recovery provenance (`confirmed`, `reconstructed`, `conflicting`, `unknown`), signal delivery (`live`, `next_safe_boundary`, `unsupported`), seal disposition (`verified`, `forced_incomplete`), and outcome kind. Define validated structs `LifecycleReceipt`, `LifecycleTransactionRecord`, `PauseHandoff`, `SealOutcome`, `ArchiveManifest`, and `SignalDeliveryReceipt`. Use the UI-SPEC field vocabulary (`schema_version`, `command`, `outcome_kind`, `projection_revision`, `changes`, `evidence`, `verification`, `warnings`, `debt`, `blockers`, `decisions`, `state_effect`, `transaction`, `receipt`, `recovery`, `provenance`) rather than untyped `map[string]interface{}` at durable boundaries. Per D-07, include acknowledgment, delivery mode, affected job/decision IDs, and effect/fallback evidence without claiming a live mechanism. Per D-14/D-16/D-17, require a stable handoff/transaction ID, one closure disposition, forced reason/evidence/rollback, and archive source digests/cross-references. Keep renderer wording and Cobra concerns out of `pkg/colony`.</action>
  <acceptance_criteria>
    - All named contracts and enum constants are exported and have JSON tags.
    - Validation rejects missing schema/transaction IDs, invalid enum strings, forced closure without reason, and archive entries without source digest.
    - No package-level contract imports `cmd`, Cobra, or terminal styling packages.
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/colony -run '^TestLifecycle(Enums|Receipt|PauseHandoff|SealOutcome|ArchiveManifest)' -count=1</automated>
  </verify>
  <done>Commands have one durable and validated type vocabulary for all Phase 199 lifecycle facts and outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add lifecycle fields without breaking legacy state</name>
  <files>pkg/colony/colony.go, pkg/colony/session.go, pkg/colony/state_machine.go, pkg/colony/lifecycle_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - pkg/colony/colony.go
    - pkg/colony/session.go
    - pkg/colony/state_machine.go
    - pkg/colony/lifecycle.go
    - pkg/colony/lifecycle_test.go
    - cmd/state_load.go
    - cmd/lifecycle_helpers.go
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md (compatibility and one-state-machine constraints)
  </read_first>
  <behavior>
    - Pre-Phase-199 JSON decodes with nil/unknown lifecycle evidence rather than invented success.
    - New state/session JSON round-trips handoff, receipt, closure, and archive references exactly.
    - Existing legal colony transitions remain legal; no second state machine is introduced.
  </behavior>
  <action>Add optional, `omitempty` lifecycle receipt, handoff reference, recovery provenance, seal outcome, and archive reference fields to the existing `ColonyState` and `SessionFile` types. Add only transition guards needed to prevent a `forced_incomplete` outcome from masquerading as verified completion; preserve the existing state machine as the sole state-transition authority. Normalize absence as unknown/not-recorded, never `verified`. Preserve existing JSON field names and legacy loader behavior. Add fixture-based tests for old JSON, new JSON, unknown future fields, and invalid new enum values. Do not create a parallel lifecycle-state enum or migration command.</action>
  <acceptance_criteria>
    - Existing state/session fixture JSON decodes and re-encodes without requiring Phase 199 fields.
    - New optional fields are absent from legacy re-encoding unless explicitly set.
    - A forced-incomplete outcome cannot satisfy any helper that means verified completion.
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/colony -run '^(TestLifecycleLegacyJSONCompatibility|TestLifecycleStateRoundTrip|TestForcedClosureIsNotVerifiedCompletion)$' -count=1</automated>
  </verify>
  <done>The new contract is additive, honest for missing evidence, and integrated into existing durable state types.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/colony -run '^TestLifecycle' -count=1` passes.
- `rg -n 'cobra|lipgloss|ansi' pkg/colony/lifecycle.go` returns no UI dependency.
</verification>

<success_criteria>
- Every later plan can depend on exported types instead of inventing command-local map schemas.
- Legacy colonies remain readable and never gain fabricated verification or recovery provenance.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-03-SUMMARY.md` when done.
</output>
