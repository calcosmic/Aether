---
phase: 199-front-door-and-classic-contract
plan: "18"
type: execute
wave: 12
depends_on: ["199-05", "199-06", "199-14", "199-16"]
files_modified:
  - cmd/update_cmd.go
  - cmd/command_truth.go
  - cmd/platform_sync.go
  - cmd/maintenance_mutation_199_test.go
  - cmd/maintenance.go
  - cmd/registry.go
  - cmd/maintenance_state_199_test.go
  - cmd/chamber.go
  - cmd/context.go
  - cmd/maintenance_archive_199_test.go
autonomous: true
requirements: [CEC-04, LIFE-06]
must_haves:
  truths:
    - "Update, migration, generated sync, cleanup, registry, chamber, and context mutations validate and preview before committing."
    - "Every maintenance failure reports none, rolled back, or recovery-required state effect with a receipt/journal and safe next action."
    - "Injected failure restores exact prior bytes or retains a deterministic recovery transaction; command exit alone is never success proof."
  artifacts:
    - path: "cmd/maintenance_mutation_199_test.go"
      provides: "Update/migrate/generated-sync transaction and rollback proof"
      contains: "TestMaintenanceMutation199"
    - path: "cmd/maintenance_state_199_test.go"
      provides: "Cleanup/registry safe-scope and rollback proof"
      contains: "TestMaintenanceState199"
    - path: "cmd/maintenance_archive_199_test.go"
      provides: "Chamber/context verification and transaction proof"
      contains: "TestMaintenanceArchive199"
  key_links:
    - from: "cmd/update_cmd.go"
      to: "cmd/lifecycle_transaction.go"
      via: "validated staged sync and rollback receipt"
      pattern: "LifecycleTransaction"
    - from: "cmd/platform_sync.go"
      to: "cmd/source_check.go"
      via: "canonical/generated verification before publish"
      pattern: "sourceCheck|sync"
---

<objective>
Route all Phase 199 maintenance mutations through the shared preview/transaction/receipt grammar.

Purpose: Advanced repair remains powerful, but no migration, update, cleanup, registry, chamber, context, or generated-surface operation may leave silent partial state.
Output: Transactional maintenance operations with bounded targets, automatic rollback, deterministic recovery, and fault tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-front-door-and-classic-contract/199-CLASSIC-SYNTHESIS.md
@.planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md
@.planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md
@cmd/lifecycle_transaction.go
@cmd/maintenance_cmd.go
@cmd/update_cmd.go
@cmd/command_truth.go
@cmd/maintenance.go
@cmd/registry.go
@cmd/chamber.go
@cmd/context.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transact update, migration, and generated-surface sync</name>
  <files>cmd/update_cmd.go, cmd/command_truth.go, cmd/platform_sync.go, cmd/maintenance_mutation_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/update_cmd.go
    - cmd/command_truth.go
    - cmd/platform_sync.go
    - cmd/maintenance_mutation_199_test.go (create; confirm absent)
    - cmd/source_check.go
    - cmd/lifecycle_transaction.go
    - cmd/maintenance_cmd.go
    - cmd/install_cmd.go
    - cmd/publish_cmd.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-01 and maintenance discretion)
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md (maintenance safety table)
  </read_first>
  <behavior>
    - Preview resolves exact source/destination/version and planned changes without mutation.
    - Commit stages all managed changes, validates source/generated parity and version, then applies atomically or rolls back.
    - Retired managed aliases are removed while custom/unmanaged files remain.
  </behavior>
  <action>Per SYN-199-09, extract update, migrate-state, and platform sync mutation plans into explicit typed target sets. Add preview results containing scope, target, current/desired versions/digests, changes, checkpoint, and recovery. Validate hub/source identity, channel, source manifest, version agreement, destination containment, managed ownership, and canonical/generated command parity before staging. Apply through `lifecycle_transaction`, verify all destination digests and stale managed removals, then emit a typed receipt; on failure automatically roll back or retain a recovery-required journal. Preserve custom files and channel separation. Keep the repository's existing Go runtime as authority and do not add packages.</action>
  <acceptance_criteria>
    - Preview creates no files/directories and changes no bytes/mtimes.
    - Fault injection at each target produces exact rollback or named recovery journal.
    - A successful receipt lists every copied/removed/unchanged managed path and verified digest.
    - Unmanaged/custom decoys survive update and generated pruning.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestMaintenanceMutation199(Update|Migrate|GeneratedSync|Rollback|CustomPreserved)$' -count=1</automated>
  </verify>
  <done>System/update/generated maintenance has a previewable, all-target commit and honest recovery receipt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound cleanup and registry mutations by exact ownership</name>
  <files>cmd/maintenance.go, cmd/registry.go, cmd/maintenance_state_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/maintenance.go
    - cmd/registry.go
    - cmd/maintenance_state_199_test.go (create; confirm absent)
    - cmd/lifecycle_transaction.go
    - cmd/maintenance_cmd.go
    - cmd/testing_main_test.go (positive ownership precedent)
    - pkg/storage/storage.go
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md (cleanup/registry risks)
  </read_first>
  <behavior>
    - Cleanup preview names every exact owned artifact; no prefix/glob target becomes deletion authority.
    - Registry mutation validates repository identity and baseline and commits with its related lifecycle receipt.
    - Failure leaves exact original bytes or a recovery journal.
  </behavior>
  <action>Replace broad cleanup target discovery with explicit schema/owner markers and an exact target manifest. Preview and validate every target below its allowed root, reject symlinks/path escapes/unknown ownership, and delete through the transaction engine. Refactor registry add/update/remove to validate stable repository identity, current baseline digest, active/sealed relationship, and duplicate conflicts before one atomic file replacement. Return typed receipts with `none`, `rolled_back`, `committed`, or `recovery_required` state effect and exact next action. Never use a successful process exit as proof that targets/registry now match.</action>
  <acceptance_criteria>
    - Decoy paths sharing cleanup prefixes survive.
    - Registry conflict/baseline-change tests mutate nothing.
    - Committed receipt digests match resulting registry and deletion set exactly.
    - Failure-injection cases prove byte-identical rollback or retained journal.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestMaintenanceState199(CleanupOwnership|CleanupRollback|RegistryBaseline|RegistryReceipt)$' -count=1</automated>
  </verify>
  <done>Cleanup and registry repair act only on proven owned targets and leave auditable all-or-recoverable results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify chamber and context operations before mutation</name>
  <files>cmd/chamber.go, cmd/context.go, cmd/maintenance_archive_199_test.go</files>
  <read_first>
    - AGENTS.md
    - cmd/AGENTS.md
    - cmd/chamber.go
    - cmd/context.go
    - cmd/maintenance_archive_199_test.go (create; confirm absent)
    - cmd/entomb_manifest.go
    - cmd/lifecycle_facts.go
    - cmd/lifecycle_transaction.go
    - cmd/maintenance_cmd.go
    - .planning/phases/199-front-door-and-classic-contract/199-CONTEXT.md (D-17)
    - .planning/phases/199-front-door-and-classic-contract/199-RESEARCH.md (chamber/context integrity)
  </read_first>
  <behavior>
    - Chamber/context inspection is read-only and reports content/cross-reference verification.
    - Create/repair validates the archive manifest, seal disposition, and context baseline before transaction.
    - Forced marker cannot be removed or normalized to success.
  </behavior>
  <action>Separate chamber/context inspect result builders from mutating create/repair functions. Reuse `ArchiveManifest` content-digest and cross-reference verification for archived state/report/XML/tombstone data. Require an explicit preview/checkpoint and baseline before create/repair, commit through the lifecycle transaction, and report exact changed files, state effect, receipt, rollback, and next action. Treat missing/conflicting forced-incomplete markers as blocking tamper evidence. Keep historical archive bytes immutable except through an explicit repair transaction that preserves prior bytes in its journal.</action>
  <acceptance_criteria>
    - Inspect fixtures are zero-write.
    - Tampered or forced-marker-mismatched archives refuse mutation.
    - Repair fault tests restore exact prior bytes or retain a recovery journal.
    - Receipts name manifest digest and every changed path.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run '^TestMaintenanceArchive199(InspectReadOnly|ManifestRefusal|ForcedMarker|RepairRollback|Receipt)$' -count=1</automated>
  </verify>
  <done>Archive/context maintenance verifies content truth first and never silently rewrites closure history.</done>
</task>

</tasks>

<verification>
- All three focused maintenance fault/rollback suites pass in isolated temp roots/homes.
- Every mutating command result carries operation, preview/baseline, transaction, receipt, state effect, verification, and recovery fields.
</verification>

<success_criteria>
- Advanced maintenance is safe to invoke without guessing whether partial writes occurred.
- Generated surfaces, local state, registry, and archives preserve ownership and closure truth under failure.
</success_criteria>

<output>
Create `.planning/phases/199-front-door-and-classic-contract/199-18-SUMMARY.md` when done.
</output>
