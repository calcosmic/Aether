---
phase: 162-switch-on-learning
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - cmd/memory_injection_test.go
  - .planning/phases/162-switch-on-learning/162-MEMORY-PROOF.md
autonomous: false
requirements: [LEARN-05]

must_haves:
  truths:
    - "A deterministic CI test proves a worker's assembled brief differs when colony memory is populated versus wiped (D-08 Layer 1)"
    - "The test fails if memory injection silently breaks, forever, with no model calls"
    - "One real before/after worker comparison is recorded as an artifact in the phase directory (D-08 Layer 2)"
  artifacts:
    - path: "cmd/memory_injection_test.go"
      provides: "Layer 1 permanent deterministic proof over buildColonyPrimeOutput"
    - path: ".planning/phases/162-switch-on-learning/162-MEMORY-PROOF.md"
      provides: "Layer 2 recorded before/after exhibit from real worker runs"
  key_links:
    - from: "instincts.json / .aether/QUEEN.md / hub wisdom.json"
      to: "buildColonyPrimeOutput(true).PromptSection"
      via: "colony-prime memory sections"
      pattern: "buildColonyPrimeOutput"
---

<objective>
Prove the learning loop reaches a worker, in two layers: one that runs in CI forever
and one recorded exhibit.

Purpose: LEARN-05 requires the difference be "measured, not assumed", and the four
previous milestones that declared this pipeline restored all assumed. D-08 splits
the proof deliberately. Layer 1 is prompt-level and deterministic, so it makes no
model calls and can live in CI as a permanent guard — this is the Definition-of-Done
artefact. Layer 2 is the one-time recorded before/after the requirement's wording
asks for; a repeatable `learning-proof` harness is explicitly NOT built here, that
is Phase 171's territory.

Output: a memory-injection test with populated and wiped fixtures, and a recorded
exhibit in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-switch-on-learning/162-CONTEXT.md
@.planning/phases/162-switch-on-learning/162-RESEARCH.md
@.planning/phases/162-switch-on-learning/162-PATTERNS.md
@.planning/phases/162-switch-on-learning/162-02-SUMMARY.md
@.planning/phases/162-switch-on-learning/162-04-SUMMARY.md

<interfaces>
Contracts this plan asserts against (read the files for current bodies):

cmd/colony_prime_context.go
  func buildColonyPrimeOutput(compact bool) colonyPrimeResult   // .PromptSection is the assembled text
  func resolveCodexWorkerContext() string                       // the single funnel every dispatch path uses
  // memory-bearing sections and their sources:
  //   "## Active Instincts"                    <- instincts.json                (unconditional, no policy gate)
  //   "## LEARNED MEMORY (Verified Outcomes)"  <- entries.json via pkg/learn    (unconditional, min confidence 0.3)
  //   "## HIVE WISDOM (Cross-Colony)"          <- <hub>/hive/wisdom.json        (policy-gated; default-on after plan 01)
  //   "## LOCAL QUEEN WISDOM (Repo-Specific)"  <- .aether/QUEEN.md              (Wisdom + Instincts after plan 02)
  //   "## GLOBAL QUEEN WISDOM (Cross-Colony)"  <- <hub>/QUEEN.md

Existing test pattern to copy:
  cmd/codex_build_test.go:3691  TestBuildWorkerBriefIncludesSurveyAndResearch
  // two t.Run subtests, each with its own t.TempDir() + storage.NewStore(dataDir),
  // one writing a fixture file and one omitting it, asserting strings.Contains /
  // !strings.Contains on the assembled prompt
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Layer 1 — a permanent deterministic populated-versus-wiped brief test</name>
  <files>cmd/memory_injection_test.go</files>
  <read_first>
    - cmd/codex_build_test.go lines 3691-3760 (TestBuildWorkerBriefIncludesSurveyAndResearch — the exact fixture pattern to copy: per-subtest TempDir, store construction, seed-or-omit, Contains assertions)
    - cmd/colony_prime_context.go lines 600-800 and 955-975 (the four memory-bearing sections and the resolveCodexWorkerContext funnel)
    - cmd/colony_prime_context_test.go (surrounding conventions for constructing a colony-prime fixture — note plan 01 removed enableHiveForTest, so hive read is now the default and needs no opt-in)
    - .planning/phases/162-switch-on-learning/162-CONTEXT.md §D-08 (why Layer 1 must be model-free)
  </read_first>
  <behavior>
    New test file `cmd/memory_injection_test.go`, `TestWorkerBriefMemoryInjection` with
    two subtests sharing one set of sentinel strings that could not occur by accident
    (for example `SENTINEL-INSTINCT-ACTION-162`, `SENTINEL-QUEEN-WISDOM-162`,
    `SENTINEL-HIVE-WISDOM-162`):

    - subtest "populated": seed `instincts.json` with an active non-archived instinct whose
      Action is the instinct sentinel; seed `.aether/QUEEN.md` with the queen sentinel under
      `## Wisdom`; seed `<AETHER_HUB_DIR>/hive/wisdom.json` with an entry carrying the hive
      sentinel and a domain matching the colony's registry domains, at confidence above the
      0.3 dormancy floor. Assert `buildColonyPrimeOutput(true).PromptSection` contains all
      three sentinels, and contains the section headers `## Active Instincts`,
      `## LOCAL QUEEN WISDOM` and `## HIVE WISDOM`.
    - subtest "wiped": identical colony fixture with no `instincts.json`, no
      `.aether/QUEEN.md`, and an empty hub `wisdom.json`. Assert the assembled section
      contains none of the three sentinels and none of the three headers.
    - a final assertion comparing the two: the populated section is strictly longer than
      the wiped one. Prefer this proportion-style invariant over a bare header-exists check,
      per CLAUDE.md's guidance that a test asserting a named section exists cannot catch its
      replacement.

    Also add `TestResolveCodexWorkerContextCarriesMemory`: the same populated fixture, but
    asserting through `resolveCodexWorkerContext()` rather than `buildColonyPrimeOutput`
    directly — that is the funnel every dispatch path actually calls, so proving it there
    proves it for real workers.
  </behavior>
  <action>
    Create `cmd/memory_injection_test.go` implementing the behaviour above. Use
    `t.TempDir()` and `t.Setenv("AETHER_HUB_DIR", ...)` per subtest so the two runs cannot
    contaminate each other; do not share a store between them.

    Make no model calls, spawn no workers, and read no network. Layer 1 is deterministic
    by design — that is what lets it live in CI forever.

    Do not set `AETHER_HIVE_POLICY` in the populated subtest. Leaving it unset is itself
    part of the proof: after plan 01 the default is `promote`, so hive wisdom must arrive
    with no configuration. If the test needs the env var set to pass, plan 01's default
    flip did not land correctly — fix that, not the test.

    Keep the file focused on memory injection. Do not extend it into general colony-prime
    budget or ordering coverage, which `cmd/colony_prime_budget_test.go` already owns.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestWorkerBriefMemoryInjection|TestResolveCodexWorkerContextCarriesMemory' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/ -run 'TestWorkerBriefMemoryInjection|TestResolveCodexWorkerContextCarriesMemory' -count=1` passes.
    - Deleting the seeded `instincts.json` from the populated subtest makes it fail.
    - Emptying the seeded hub `wisdom.json` in the populated subtest makes it fail — proving
      the hive default carries wisdom without configuration.
    - The test file contains no `AETHER_HIVE_POLICY` assignment in the populated subtest.
    - The test file makes no network call and spawns no worker: it completes in under two
      seconds on a cold run.
    - The populated-versus-wiped length comparison is asserted, not just per-sentinel presence.
  </acceptance_criteria>
  <done>A model-free CI test proves that populated colony memory changes what a worker is sent, and fails the moment any of the three injection routes breaks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Layer 2 — record the one-time real before/after exhibit</name>
  <files>.planning/phases/162-switch-on-learning/162-MEMORY-PROOF.md</files>
  <read_first>
    - .planning/phases/162-switch-on-learning/162-CONTEXT.md §D-08 (Layer 2's scope: recorded once, not a repeatable harness)
    - .planning/phases/162-switch-on-learning/162-VALIDATION.md §Manual-Only Verifications
    - cmd/memory_injection_test.go (as created in Task 1 — the sentinel strings to reuse so the two layers describe the same thing)
  </read_first>
  <what-built>
    Plans 01-04 wired phase-end and seal consolidation into the runtime, made the promotion
    target reachable, flipped the hive default on, and rendered the learning beats. Task 1
    added the permanent deterministic proof. Everything needed for the recorded exhibit is
    now live.
  </what-built>
  <how-to-verify>
    1. In this repo with colony memory populated (instincts present in
       `.aether/data/instincts.json`, wisdom present in `.aether/QUEEN.md`), run
       `aether build <n> --print-brief` and save the full output.
    2. Move `.aether/data/instincts.json` and `.aether/QUEEN.md` aside (do not delete —
       rename with a `.bak` suffix), then run the same `aether build <n> --print-brief`
       command again and save that output.
    3. Restore both files from their `.bak` copies and confirm with `git status` that the
       working tree is unchanged.
    4. Write `.planning/phases/162-switch-on-learning/162-MEMORY-PROOF.md` containing:
       the exact command run, the date, the two outputs (or their memory-bearing sections
       verbatim if the full briefs are very long), a short diff summary naming which
       sections and which specific instinct or wisdom text appear only in the populated
       run, and one sentence stating what a worker would therefore know in one case and
       not the other.
    5. Confirm the exhibit shows a real difference. If the two outputs are identical, this
       phase has not achieved LEARN-05 and the wiring must be re-examined before the phase
       is marked complete — do not record a null result as a pass.
    6. Optionally, run a real `/ant-continue` at a phase end and confirm the 🧠 learning
       beat renders, and a `/ant-seal` on a scratch colony and confirm the eight named ants
       render with distinct emoji. Note the observed output in the exhibit.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/162-switch-on-learning/162-MEMORY-PROOF.md` exists and is non-empty.
    - It names the exact command used, so a reader can reproduce it.
    - It contains two distinguishable outputs and a stated difference — not a claim that a
      difference occurred.
    - `git status` shows `.aether/data/instincts.json` and `.aether/QUEEN.md` restored to
      their pre-exercise state.
    - The exhibit does not claim a repeatable harness exists; it is explicitly a one-time
      recording, per D-08.
  </acceptance_criteria>
  <resume-signal>Type "approved" once 162-MEMORY-PROOF.md records a real difference, or describe what you saw instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| colony memory files → worker prompt | The whole point of the test: memory text becomes model input |
| Layer 2 exercise → live repo state | The manual before/after temporarily moves real colony memory files aside |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-19 | Tampering | The Layer 2 exercise destroying real colony memory when files are moved aside | mitigate | Task 2 requires rename-to-`.bak` rather than delete, explicit restoration, and a `git status` check as an acceptance criterion. |
| T-162-20 | Information Disclosure | The recorded exhibit committing real instinct and wisdom text into `.planning/` | accept | The same text already lives in `.aether/QUEEN.md` and `.aether/data/` in this repo; the exhibit adds no new class of content. Reviewers should redact if a sentinel happens to contain credentials — none of the seeded sentinels do. |
| T-162-21 | Repudiation | Recording a null result as a pass, repeating the "declared restored" history | mitigate | Task 2's step 5 and its acceptance criteria explicitly forbid it: identical outputs mean the phase has not achieved LEARN-05. |
</threat_model>

<verification>
- `go test ./cmd/ -run 'TestWorkerBriefMemoryInjection|TestResolveCodexWorkerContextCarriesMemory' -count=1` passes.
- `go test ./cmd/ -count=1` is green.
- `.planning/phases/162-switch-on-learning/162-MEMORY-PROOF.md` records a real, stated difference.
- The working tree is clean of `.bak` leftovers after the Layer 2 exercise.
</verification>

<success_criteria>
LEARN-05 is satisfied in both the permanent sense (a CI command that fails when memory
injection breaks) and the recorded sense (one real before/after comparison in the phase
directory), without building the repeatable harness Phase 171 owns.
</success_criteria>

<output>
After completion, create `.planning/phases/162-switch-on-learning/162-05-SUMMARY.md`
</output>
</content>
