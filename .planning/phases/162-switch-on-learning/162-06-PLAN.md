---
phase: 162-switch-on-learning
plan: 06
type: execute
wave: 4
depends_on: [01, 03, 04]
files_modified:
  - .aether/docs/learning-system-authority.md
  - .aether/docs/structural-learning-stack.md
  - CLAUDE.md
  - AGENTS.md
  - cmd/docs_truth_test.go
autonomous: true
requirements: [LEARN-03, LEARN-04]

must_haves:
  truths:
    - "A written decision record in the repo states that pkg/memory is authoritative and pkg/learn is the subordinate capture front-end, and what happened to every duplicated stage (D-09, D-10)"
    - "The decision record states the Hive Brain default decision and its reasoning (D-03, LEARN-04)"
    - "No document claims consolidation is unwired, and none claims phase-end runs three named ants"
    - "Every remaining runtime claim in these documents is checkable by a named command"
  artifacts:
    - path: ".aether/docs/learning-system-authority.md"
      provides: "The ADR-style decision record for LEARN-03 and LEARN-04"
      contains: "pkg/memory"
    - path: "cmd/docs_truth_test.go"
      provides: "Test that fails if the retired claims reappear or the decision record vanishes"
  key_links:
    - from: "cmd/docs_truth_test.go"
      to: ".aether/docs/learning-system-authority.md"
      via: "existence and content assertion"
      pattern: "learning-system-authority.md"
---

<objective>
Write down the two decision-shaped requirements, and make every document that
describes the learning pipeline agree with the code that now exists.

Purpose: LEARN-03 and LEARN-04 are decision-shaped. The roadmap's success criteria
3 and 4 say the decision must be "documented in the repo, not only implemented".
CLAUDE.md names this exact pipeline as its canonical example of the failure being
avoided — "The learning pipeline has been declared restored in v1.10, v1.11, v1.13
and v1.23" — so this plan runs LAST, after the wiring is real, and adds a test so
the corrected claims cannot silently rot back.

Three specific false statements come out. Two say no lifecycle command invokes
consolidation (CLAUDE.md:838, AGENTS.md:899, structural-learning-stack.md:15/214/221).
One is false independently of this phase: structural-learning-stack.md:214-215 says
phase-end "Executes three ants only: nurse → herald → janitor", but
`consolidationPhaseEndCmd` calls `pipeline.RunConsolidation`, which runs
`pkg/memory/consolidate.go`'s monolithic algorithm and never touches
`pkg/agent/curation` at all. Removing the "Phase 162 wires this" caveat without
fixing the ant-list underneath would leave a fresh false claim behind.

Output: one decision record, three corrected documents, and a test that fails if
any of it regresses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-switch-on-learning/162-CONTEXT.md
@.planning/phases/162-switch-on-learning/162-RESEARCH.md
@.planning/phases/162-switch-on-learning/162-01-SUMMARY.md
@.planning/phases/162-switch-on-learning/162-02-SUMMARY.md
@.planning/phases/162-switch-on-learning/162-03-SUMMARY.md
@.planning/phases/162-switch-on-learning/162-04-SUMMARY.md

<interfaces>
Facts this plan must document accurately (verify each against the code before writing):

Authority (D-09):
  pkg/memory      = authoritative pipeline (Observe -> Promote -> Queen -> Consolidate,
                    decay, archive, the eight curation ants)
  pkg/learn/learn.go, curator.go, colony_store.go
                  = subordinate live capture front-end (Entry/Evidence/trust scoring,
                    entries.json + colony.db), invoked by captureContinueLearning on
                    every continue, feeding the "## LEARNED MEMORY" context section
  pkg/learn/wrappers.go
                  = NOT a competing system. It is a thin forwarding shim
                    (learn.NewPipeline -> memory.NewPipeline,
                     learn.ConsolidationResult = memory.ConsolidationResult) that
                    cmd/graph_consolidation_cmds.go imports directly. It is not retired.

Duplicated stages found and their resolution:
  QUEEN.md instinct promotion at seal — implemented in both. Resolved in pkg/memory's
    favour by plan 04: pipeline.RunConsolidation writes first; completeSealRuntime's
    loop skips any instinct the pipeline already promoted and survives only as the
    explicitly-subordinate fallback for instincts with no application history
    (QueenEligible requires Applications >= 3, which young colonies never have).
  Hive promotion at seal — NOT duplicated. pkg/memory has no hive stage; the seal
    loop remains its sole implementation.
  Promotion target reachability — fixed by plan 02: consolidation now writes to
    .aether/QUEEN.md (the file colony-prime reads), not .aether/data/QUEEN.md.

Deliberate non-wiring to record:
  pkg/memory/pipeline.go's PipelineConfig.LearningValidator callback field is invoked
  by handleObserveEvent at trust score >= 0.8 but is never assigned by
  pipelineConfigForStore(), so it is always nil. This phase leaves it unwired
  deliberately — a bridge from the authoritative pipeline back into the subordinate
  capture layer would invert the dependency direction D-09 establishes. Record it as
  a stated choice with a reason, not as an oversight.

Hive default (D-01/D-02/D-03), as implemented by plan 01:
  AETHER_HIVE_POLICY unset -> promote (read + seal-time promotion)
  "read"/"inject"/"on" -> read;  "promote"/"full" -> promote;  "off" -> off
  unrecognized -> off, with a one-line stderr warning naming the value
  The per-colony consent file and the hive-opt-in / hive-opt-out commands are retired.

Stale text to remove (exact locations):
  CLAUDE.md:838                          "no lifecycle command invokes either one yet (Phase 162 wires this)"
  AGENTS.md:899                          "do not invoke either one yet (Phase 162 wires this)"
  .aether/docs/structural-learning-stack.md:15, :214, :221   "(Phase 162 wires this)"
  .aether/docs/structural-learning-stack.md:214-215          "Executes three ants only: nurse -> herald -> janitor"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the learning-system authority decision record</name>
  <files>.aether/docs/learning-system-authority.md</files>
  <read_first>
    - .aether/docs/structural-learning-stack.md (the whole file — the prose structure, heading style and tone to match; there is no prior ADR in this repo to template from)
    - pkg/learn/wrappers.go (confirm for yourself it is a forwarding shim before writing that it is)
    - pkg/memory/pipeline.go lines 1-60 and 125-200 (PipelineConfig, LearningValidator, RunConsolidation)
    - cmd/codex_continue_finalize.go lines 1264-1360 (captureContinueLearning — what the subordinate layer actually does)
    - .planning/phases/162-switch-on-learning/162-04-SUMMARY.md (the reconciliation as actually implemented, which may differ in detail from the plan)
    - .planning/phases/162-switch-on-learning/162-CONTEXT.md §D-09 and §D-10
  </read_first>
  <action>
    Create `.aether/docs/learning-system-authority.md` as an ADR-style record. Flat file,
    no new subdirectory, so it distributes with the rest of `.aether/docs/` without any
    publish-path change.

    Required sections and content:

    Status and date. Decided 2026-08-04, implemented in v1.25 Phase 162.

    Context. Two learning systems existed under confusingly similar names. State what each
    one is, using the facts in <interfaces> — including the distinction RESEARCH.md drew
    that `pkg/learn/wrappers.go` is a forwarding shim rather than a competitor, so nobody
    later "retires" it and breaks the two consolidation subcommands that import it.

    Decision 1 — authority. `pkg/memory` is authoritative. `pkg/learn`'s Entry/Evidence
    capture layer is explicitly subordinate: it is the live capture and trust-scoring
    front-end and it stays, because it is the only working capture path and deleting it
    would remove the colony's only means of observing anything.

    Decision 2 — duplicated stages. Enumerate every stage found in both, and its
    resolution, using the three rows in <interfaces>. For the seal promotion stage, state
    plainly why the subordinate writer survives rather than being deleted: `QueenEligible`
    requires at least three recorded applications, so deleting the seal bar would make
    seal promote nothing in a young colony. Name the invariant that replaced the
    duplication — one instinct produces exactly one QUEEN.md entry — and name the test
    that enforces it.

    Decision 3 — LearningValidator stays unwired, deliberately, with the reason in
    <interfaces>. State it as a decision so a future reader does not read `nil` as a bug.

    Decision 4 — the Hive Brain default. Record the flip to `promote`, the retirement of
    the consent file, the full resolution table, and the reasoning: one honest control
    surface beats two gates where the second silently vetoes the first. Note explicitly
    that hive *trust redesign* remains shelved per REQUIREMENTS.md Non-Goals — this was a
    default change and a documentation correction, nothing more.

    Consequences. What is now true at runtime, and which named tests fail if it stops
    being true. List the test names introduced by plans 01-05 so every claim in this
    document points at a command. Do not make a single claim about runtime behaviour that
    is not covered by one of them — per CLAUDE.md, an uncheckable claim must be removed
    rather than written.

    Keep it to roughly two pages. This is a decision record, not a tutorial.
  </action>
  <verify>
    <automated>test -f .aether/docs/learning-system-authority.md && grep -q 'pkg/memory' .aether/docs/learning-system-authority.md && grep -q 'AETHER_HIVE_POLICY' .aether/docs/learning-system-authority.md</automated>
  </verify>
  <acceptance_criteria>
    - `.aether/docs/learning-system-authority.md` exists and covers all four decisions.
    - It names `pkg/learn/wrappers.go` explicitly as a shim that must not be retired.
    - It records the LearningValidator non-wiring as a decision with a reason.
    - It contains the full `AETHER_HIVE_POLICY` resolution table including the unrecognized-value row.
    - Every runtime claim in the Consequences section names a test from plans 01-05, and
      each named test exists: verify by running each one.
    - It does not describe hive trust redesign as in scope.
  </acceptance_criteria>
  <done>The two decision-shaped requirements are written down in the repo, with every runtime claim pointing at a command that fails when the claim stops holding.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the three documents that describe the learning pipeline</name>
  <files>.aether/docs/structural-learning-stack.md, CLAUDE.md, AGENTS.md</files>
  <read_first>
    - .aether/docs/structural-learning-stack.md lines 1-30 and 195-240 (the wiring-status banner at :15 and the Consolidation Pipeline section at :204-235, including the false three-ant claim)
    - CLAUDE.md line 838 and its surrounding Structural Learning Stack bullet list
    - AGENTS.md line 899 and its surrounding section (note plan 01 already rewrote AGENTS.md's hive gating section — do not re-edit that section, only the consolidation lifecycle claim)
    - cmd/graph_consolidation_cmds.go lines 204-270 (proof that phase-end calls pipeline.RunConsolidation, not the curation ants)
    - pkg/memory/consolidate.go (confirm for yourself there is no reference to pkg/agent/curation before writing that phase-end runs no ants)
    - .planning/phases/162-switch-on-learning/162-03-SUMMARY.md and 162-04-SUMMARY.md (what the wiring actually does, so the corrected text describes reality)
  </read_first>
  <action>
    structural-learning-stack.md:
    - Line ~15: replace the wiring-status banner with an accurate statement that phase-end
      consolidation runs automatically when `/ant-continue` advances a phase and seal
      consolidation runs at `/ant-seal`, both invoked by the Go runtime and both
      non-blocking, naming the tests that enforce it.
    - Lines ~211-217 (phase-end): remove "(Phase 162 wires this)" AND replace the false
      "Executes three ants only: nurse → herald → janitor" with what actually happens —
      `pipeline.RunConsolidation` runs `pkg/memory`'s consolidation algorithm (instinct
      decay, observation decay, archival, promotion-candidate and QueenEligible
      computation) and runs no curation ants; the eight-ant pass is seal-only. Keep the
      accurate detail that it publishes a `consolidation.phase_end` event.
    - Line ~219-221 (seal): remove "(Phase 162 wires this)" and state that
      `completeSealRuntime` invokes it, that the eight ants each announce themselves, and
      that the scribe report is written to `.aether/CURATION-REPORT.md`.
    - Add a pointer to `.aether/docs/learning-system-authority.md` for the authority decision.

    CLAUDE.md line 838: replace the "no lifecycle command invokes either one yet (Phase 162
    wires this)" bullet with the accurate statement — both are invoked by the runtime
    (continue-advance and seal respectively), both remain user-invocable as the manual
    inspection path, and both are non-blocking. Also update CLAUDE.md's §Hive Brain and
    §Wisdom Pipeline text so nothing there still implies the hive default is off or that
    a consent step exists, and reference the decision record.

    AGENTS.md line 899: same correction to the lifecycle-integration bullet. Do not touch
    the hive gating section — plan 01 already rewrote it and re-editing risks undoing it.

    Do not update CLAUDE.md's version or Last Updated header fields; those are release
    bookkeeping, not this phase's scope.

    Every sentence you write about runtime behaviour must be backed by a test from plans
    01-05. If you cannot name the test, delete the sentence instead of softening it.
  </action>
  <verify>
    <automated>! grep -rn 'Phase 162 wires this\|three ants only\|nurse → herald → janitor' CLAUDE.md AGENTS.md .aether/docs/structural-learning-stack.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'Phase 162 wires this' CLAUDE.md AGENTS.md .aether/docs/structural-learning-stack.md` returns nothing.
    - `grep -rn 'three ants only' .aether/docs/structural-learning-stack.md` returns nothing.
    - `grep -rn 'nurse → herald → janitor\|nurse -> herald -> janitor' .aether/docs/structural-learning-stack.md` returns nothing.
    - `grep -rn 'no lifecycle command invokes\|do not invoke either one yet' CLAUDE.md AGENTS.md .aether/docs/structural-learning-stack.md` returns nothing.
    - The AGENTS.md hive gating section written by plan 01 is byte-identical to how plan 01
      left it: verify with `git diff` scoped to that section.
    - Each of the three files references `.aether/docs/learning-system-authority.md`.
  </acceptance_criteria>
  <done>Every document describing consolidation says what the code does, including the ant-list claim that was false before this phase started.</done>
</task>

<task type="auto">
  <name>Task 3: Add a docs-truth test so the corrected claims cannot rot back</name>
  <files>cmd/docs_truth_test.go</files>
  <read_first>
    - CLAUDE.md §"Definition of Done" (the documentation-claim corollary and the three-files-described-phase-end-consolidation-for-months example — this test is the guard that example asks for)
    - cmd/ (any existing test that reads a repo-root file from within the cmd package, to copy its relative-path convention — tests run with cwd set to the package directory)
    - .aether/docs/learning-system-authority.md and the three corrected docs (as produced by Tasks 1 and 2)
  </read_first>
  <action>
    Create `cmd/docs_truth_test.go` with a table-driven test
    `TestLearningDocsDoNotClaimUnwiredConsolidation` that reads `../CLAUDE.md`,
    `../AGENTS.md` and `../.aether/docs/structural-learning-stack.md` and fails if any of
    them contains any of these retired substrings: `Phase 162 wires this`,
    `no lifecycle command invokes`, `do not invoke either one yet`, `three ants only`,
    `nurse → herald → janitor`, `hive-opt-in`, `opt-in, twice over`.

    Add `TestLearningDecisionRecordExists` asserting `../.aether/docs/learning-system-authority.md`
    exists, is at least a few hundred bytes, and mentions both `pkg/memory` and
    `AETHER_HIVE_POLICY` — so deleting the record, or gutting it to a stub, fails CI.

    Strip comment lines before matching where a file's own comment syntax could contain a
    banned phrase, so the test cannot be self-invalidated by prose about the test. For
    markdown that mostly means: match on the raw text but choose banned strings that would
    only appear in a genuine claim, and put this test's own explanatory prose in Go
    comments inside the `.go` file, never in the markdown files under assertion.

    Skip the test with a clear message if the files are not found relative to the package
    directory, so the test does not fail spuriously when the package is vendored or run
    from an unusual working directory — but do not skip when the files exist and merely
    fail to match.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestLearningDocsDoNotClaimUnwiredConsolidation|TestLearningDecisionRecordExists' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/ -run 'TestLearningDocs|TestLearningDecisionRecord' -count=1` passes.
    - Temporarily re-adding the string `Phase 162 wires this` to CLAUDE.md makes
      `TestLearningDocsDoNotClaimUnwiredConsolidation` fail. Restore afterwards.
    - Temporarily renaming `.aether/docs/learning-system-authority.md` makes
      `TestLearningDecisionRecordExists` fail. Restore afterwards.
    - The test does not fail spuriously when run via `go test ./...` from the repo root.
    - None of the banned substrings appear inside the three markdown files as part of this
      test's own documentation — the test's prose lives in the Go file.
  </acceptance_criteria>
  <done>A command exists that fails if the retired documentation claims reappear or the decision record is removed — the documentation-claim corollary now has teeth for this pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → operator's mental model | These files are what a person and every future agent believe about runtime behaviour |
| `.aether/docs/` → hub → downstream repos | Corrected docs are distributed by `aether publish` to every colony on the machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-162-22 | Spoofing | A documentation claim asserting a security control that does not exist — the class of defect that let "retrieval is opt-in, twice over" persist while the gate silently vetoed an explicitly enabled policy | mitigate | Task 3's substring test fails if that specific text returns; Task 1 requires every runtime claim in the record to name a test that exists and is run as an acceptance criterion. |
| T-162-23 | Repudiation | The corrected claims rotting back over future milestones, as they did across v1.10, v1.11, v1.13 and v1.23 | mitigate | `TestLearningDocsDoNotClaimUnwiredConsolidation` and `TestLearningDecisionRecordExists` run on every `go test ./...`, including CI. |
| T-162-24 | Information Disclosure | The decision record documenting that instinct text leaves the repo by default, published to the hub | accept | This is the intent. Operators need to know what crosses the boundary; T-162-01 in plan 01 depends on this being written down. |
</threat_model>

<verification>
- `go test ./cmd/ -count=1` is green.
- `grep -rn 'Phase 162 wires this\|three ants only\|hive-opt-in\|opt-in, twice over' CLAUDE.md AGENTS.md .aether/docs/` returns nothing.
- `.aether/docs/learning-system-authority.md` exists and every runtime claim in it names a test that passes.
- Re-adding any retired claim fails a named test.
</verification>

<success_criteria>
LEARN-03 and LEARN-04's decision halves are satisfied: both decisions are written down in
the repo with reasoning, every document that describes the learning pipeline agrees with
the code, the pre-existing false three-ant claim is corrected rather than inherited, and a
test fails if any of it regresses.
</success_criteria>

<output>
After completion, create `.planning/phases/162-switch-on-learning/162-06-SUMMARY.md`
</output>
</content>
