---
phase: 32-intelligence-enhancements
plan: 03
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - tests/bash/test-intelligence.sh
  - package.json
autonomous: true
requirements:
  - INTEL-01
  - INTEL-02
  - INTEL-03

must_haves:
  truths:
    - "_scan_colony_context returns valid JSON with prior_colonies array from chambers with CROWNED-ANTHILL.md"
    - "_scan_colony_context returns empty prior_colonies when no chambers exist"
    - "_scan_colony_context extracts existing charter content from QUEEN.md"
    - "_scan_pheromone_suggestions returns FOCUS signal when test config + test files exist"
    - "_scan_pheromone_suggestions returns REDIRECT signal when .env files exist"
    - "_scan_pheromone_suggestions caps output at 5 suggestions maximum"
    - "_scan_pheromone_suggestions returns empty array when no patterns match"
    - "_scan_governance returns prescriptive rules from detected test config + test files"
    - "_scan_governance returns empty rules when no governance files exist"
    - "_scan_governance skips TDD rule when test config exists but no test files"
    - "_scan_init_research includes colony_context, pheromone_suggestions, and governance in output"
  artifacts:
    - path: "tests/bash/test-intelligence.sh"
      provides: "Integration tests for all three intelligence sub-scan functions"
      min_lines: 200
    - path: "package.json"
      provides: "npm script for running intelligence tests"
      contains: "test:intelligence"
  key_links:
    - from: "tests/bash/test-intelligence.sh"
      to: ".aether/utils/scan.sh"
      via: "source aether-utils.sh then call _scan_colony_context, _scan_pheromone_suggestions, _scan_governance"
      pattern: "_scan_colony_context|_scan_pheromone_suggestions|_scan_governance"
---

<objective>
Add integration tests for the three intelligence sub-scan functions, verifying correct output shapes, edge cases, and the 5-suggestion cap.

Purpose: Verify that prior colony context extraction, pheromone suggestion generation, and governance inference produce correct, well-structured output and handle edge cases gracefully.

Output: tests/bash/test-intelligence.sh with comprehensive test coverage for all three intelligence functions; npm script wired up.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/bash/test-init-smart-flow.sh
@tests/bash/test-helpers.sh
@.aether/utils/scan.sh
@.planning/phases/32-intelligence-enhancements/32-RESEARCH.md
@.planning/phases/32-intelligence-enhancements/32-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test-intelligence.sh with integration tests for all three scan functions</name>
  <files>tests/bash/test-intelligence.sh</files>
  <action>
Create tests/bash/test-intelligence.sh following the exact pattern from test-init-smart-flow.sh: use test-helpers.sh (run_test, assert_json_has_field, log_pass, log_fail, require_jq), create isolated temp directories per test, source aether-utils.sh to get the functions, clean up temp dirs after.

**Test setup helper (similar to setup_smart_init_env from test-init-smart-flow.sh):**
- Create a function `setup_intelligence_env` that:
  - Creates temp dir with `.aether/data/`, `.aether/utils/`, `.aether/chambers/`
  - Copies aether-utils.sh, utils/, and exchange/ from project root
  - Copies QUEEN.md template as `.aether/QUEEN.md`
  - Returns the temp dir path

**Tests for _scan_colony_context (INTEL-01):**

1. **test_colony_context_with_chambers**: Create 2 fake chambers in temp dir, each with manifest.json (goal, phases_completed, total_phases, milestone) and CROWNED-ANTHILL.md (with `## The Work` section). Run _scan_colony_context. Assert: prior_colonies array has 2 entries, each entry has goal/phases/outcome/summary fields.

2. **test_colony_context_no_chambers**: Run _scan_colony_context on temp dir with no chambers/ directory. Assert: prior_colonies is empty array, existing_charter fields are empty strings.

3. **test_colony_context_manifest_only**: Create chamber with manifest.json but NO CROWNED-ANTHILL.md (simulates older pre-v2 chamber per pitfall 1). Assert: colony entry has goal and outcome from manifest, summary is empty string.

4. **test_colony_context_max_three**: Create 4 fake chambers (with dated names so sort order is clear). Assert: prior_colonies array has exactly 3 entries (most recent 3).

5. **test_colony_context_existing_charter**: Write charter entries to QUEEN.md in the temp dir (lines like `- [charter] **Intent**: Build a REST API (Colony: test)`). Assert: existing_charter.intent returns "Build a REST API" (stripped of Colony suffix).

**Tests for _scan_pheromone_suggestions (INTEL-02):**

6. **test_pheromone_env_files**: Create `.env` file in temp dir root. Run _scan_pheromone_suggestions. Assert: result contains at least one entry with type "REDIRECT" and content matching "secrets" or ".env".

7. **test_pheromone_test_config_with_tests**: Create `jest.config.js` and a `tests/example.test.js` file. Assert: result contains FOCUS entry about testing.

8. **test_pheromone_test_config_no_tests**: Create `jest.config.js` but NO test files. Assert: result contains REDIRECT entry about "no test files" (not FOCUS about testing -- cross-reference validation).

9. **test_pheromone_empty_repo**: Run on temp dir with no config files. Assert: result is empty array `[]`.

10. **test_pheromone_cap_five**: Create enough config files to trigger more than 5 patterns (e.g., .env, jest.config.js + tests, .eslintrc.json, .github/workflows/ci.yml, tsconfig.json with strict:true, Dockerfile, CONTRIBUTING.md). Assert: result array length is exactly 5.

11. **test_pheromone_sorted_by_priority**: Trigger at least 3 suggestions with different priorities. Assert: first entry has highest priority value, entries are in descending priority order.

**Tests for _scan_governance (INTEL-03):**

12. **test_governance_contributing**: Create CONTRIBUTING.md with some content (at least 2 lines). Assert: rules array contains entry with rule matching "CONTRIBUTING.md" and source "CONTRIBUTING.md".

13. **test_governance_test_config_with_tests**: Create jest.config.js AND tests/foo.test.js. Assert: rules contain "TDD required" entry.

14. **test_governance_test_config_no_tests**: Create jest.config.js but NO test files. Assert: rules do NOT contain TDD entry (cross-reference prevents false positive per pitfall 2).

15. **test_governance_cicd**: Create `.github/workflows/` directory with a dummy `.yml` file. Assert: rules contain CI/CD entry.

16. **test_governance_empty_repo**: No governance files. Assert: rules is empty array.

**Integration test:**

17. **test_init_research_includes_intelligence**: Run `_scan_init_research --target "$test_dir"` on a test dir with some chambers and config files. Assert: result JSON has `colony_context`, `governance`, and `pheromone_suggestions` at the top level (use `jq -e '.colony_context'`, `jq -e '.governance'`, `jq -e '.pheromone_suggestions'` -- per v2.5 decision to use jq -e for nested paths).

**Implementation notes:**
- Use `jq -e` for nested path assertions (per accumulated decision: assert_json_has_field only supports top-level keys).
- Use `2>&1` for error path tests if any (per accumulated decision: json_err writes to stderr).
- Each test should clean up its temp dir.
- Follow test-helpers.sh conventions: pass/fail counting, summary at end.
  </action>
  <verify>
Run: `bash tests/bash/test-intelligence.sh` -- all tests pass.
Run: `npm test` -- all tests pass including the new intelligence tests.
  </verify>
  <done>
test-intelligence.sh has 17 integration tests covering all three intelligence functions: colony context extraction (5 tests), pheromone suggestions (6 tests), governance inference (5 tests), and full integration (1 test). All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire npm test script for intelligence tests</name>
  <files>package.json</files>
  <action>
Add a `test:intelligence` npm script to package.json following the existing pattern (look at how other test:* scripts are defined, e.g., test:skills, test:scan, test:init-smart-flow).

Add: `"test:intelligence": "bash tests/bash/test-intelligence.sh"`

Ensure the main `test` script includes the new test file (if the main test script uses a glob or explicit list, add test-intelligence.sh to it).

Verify the test runs via `npm run test:intelligence`.
  </action>
  <verify>
Run: `npm run test:intelligence` -- should execute the intelligence tests and pass.
Run: `npm test` -- should include intelligence tests in the full suite and pass.
  </verify>
  <done>
npm script test:intelligence wired up. Intelligence tests run as part of the full test suite. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `bash tests/bash/test-intelligence.sh` passes all 17 tests
- `npm run test:intelligence` runs successfully
- `npm test` passes with all tests including intelligence tests
- Test coverage includes: happy paths, empty/missing data, max-3 colony cap, 5-suggestion cap, cross-reference validation (test config without tests), charter extraction
</verification>

<success_criteria>
- 17 integration tests exist and pass covering INTEL-01, INTEL-02, INTEL-03
- Edge cases tested: no chambers, no config files, manifest without CROWNED-ANTHILL.md, test config without test files, more than 5 pheromone patterns
- Tests use isolated temp directories (no side effects)
- npm script wired and full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/32-intelligence-enhancements/32-03-SUMMARY.md`
</output>
