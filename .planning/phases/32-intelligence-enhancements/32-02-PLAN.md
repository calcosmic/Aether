---
phase: 32-intelligence-enhancements
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - .claude/commands/ant/init.md
  - .opencode/commands/ant/init.md
autonomous: true
requirements:
  - INTEL-01
  - INTEL-02
  - INTEL-03

must_haves:
  truths:
    - "When prior colonies exist, the approval prompt includes a Prior Context section before Charter"
    - "When no prior colonies exist, the Prior Context section is omitted entirely (no placeholder)"
    - "The approval prompt Pheromones section shows numbered suggestions with type prefix instead of the default 'No suggestions yet' message"
    - "The approval prompt Charter Governance field is pre-populated with inferred governance rules"
    - "After user approval, suggested pheromones are auto-applied via pheromone-write calls"
    - "All sections are editable by the user before approval (no visual distinction between auto and manual)"
  artifacts:
    - path: ".claude/commands/ant/init.md"
      provides: "Intelligence-enriched approval prompt with prior context, pheromone suggestions, and governance"
      contains: "colony_context"
    - path: ".opencode/commands/ant/init.md"
      provides: "Mirror of Claude init.md with OpenCode-specific normalization preserved"
      contains: "colony_context"
  key_links:
    - from: ".claude/commands/ant/init.md (Step 3)"
      to: "scan.sh init-research result"
      via: "jq extraction of colony_context, governance, pheromone_suggestions fields"
      pattern: "colony_context|governance|pheromone_suggestions"
    - from: ".claude/commands/ant/init.md (Step 7)"
      to: "pheromone-write subcommand"
      via: "loop over approved pheromone suggestions calling pheromone-write"
      pattern: "pheromone-write"
    - from: ".claude/commands/ant/init.md"
      to: ".opencode/commands/ant/init.md"
      via: "content-identical Steps 2-8 with OpenCode Step -1 and Step 0 preserved"
      pattern: "Step 3|Step 5|Step 7"
---

<objective>
Update both init.md command files to consume the new intelligence scan data and display an enriched approval prompt with prior colony context, pheromone suggestions, and governance rules.

Purpose: Make the init approval prompt intelligent -- showing what the system learned about the repo so the user starts with a richer, pre-populated charter instead of blank fields.

Output: Updated init.md (Claude + OpenCode mirrors) with enriched Step 3 (extract new scan fields), Step 5 (display enriched prompt), and Step 7 (auto-apply pheromones post-approval).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/commands/ant/init.md
@.opencode/commands/ant/init.md
@.planning/phases/32-intelligence-enhancements/32-RESEARCH.md
@.planning/phases/32-intelligence-enhancements/32-CONTEXT.md
@.planning/phases/32-intelligence-enhancements/32-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update init.md Step 3, 5, and 7 with intelligence enrichment</name>
  <files>.claude/commands/ant/init.md</files>
  <action>
Update three steps in .claude/commands/ant/init.md to consume and display the new intelligence data from scan.sh. All other steps remain unchanged.

**Step 3 updates (Scan Repository) -- add extraction of new fields after existing extractions:**
Add these jq extractions after the existing field extractions:
- `colony_context_colonies`: `.colony_context.prior_colonies // []` -- array of prior colony summaries
- `colony_context_charter`: `.colony_context.existing_charter // {}` -- existing charter content
- `governance_rules`: `.governance.rules // []` -- array of governance rule objects
- `pheromone_suggestions`: `.pheromone_suggestions // []` -- array of suggestion objects

If scan_result is empty or jq fails, set these to empty arrays/objects as fallback values (same graceful degradation pattern as existing fields).

**Step 5 updates (Assemble and Display Approval Prompt) -- restructure prompt sections:**

Section ordering (per Claude's discretion from research): Prior Context (if any) -> Charter -> Context -> Pheromones

**NEW: Prior Context section (before Charter, only when prior colonies exist):**
- If `colony_context_colonies` has entries (length > 0), display:
```markdown
## Prior Context

Previous colonies in this repo:

{For each colony (max 3):}
- **{goal}** -- {outcome} ({phases} phases){if summary: ". {summary}"}
```
- Per locked decision: When no prior colonies exist, omit this section entirely (no placeholder, no header).
- Keep each colony to 1-2 lines. Show goal, outcome (milestone), phase count, and summary from CROWNED-ANTHILL.md if available.

**Updated Charter section:**
- For fresh init with governance_rules available: pre-populate the Governance field with semicolon-separated rules (per research recommendation). Format: `"TDD required; ESLint enforced; Follow CONTRIBUTING.md"`. These are editable by the user.
- For re-init: keep existing behavior (pre-populate from QUEEN.md charter entries). If existing_governance is empty but governance_rules are available, pre-populate from governance_rules.
- Intent and Vision behavior unchanged.
- Goals behavior unchanged.

**Updated Pheromones section:**
- If `pheromone_suggestions` has entries (length > 0), display as numbered list with type prefix:
```markdown
## Pheromones

Suggested signals based on repo analysis:

1. [FOCUS] Testing infrastructure present (47 test files) -- maintain TDD discipline
2. [REDIRECT] Environment files detected -- never commit secrets or .env files
3. [FOCUS] Code quality tools configured -- follow existing lint/format rules

Edit, remove, or add signals as needed. Approved signals will be auto-applied.
```
- Per locked decision: suggestions are fully editable. User can reword, remove, or add their own.
- Per locked decision: all sections look the same (no visual distinction between auto-generated and user-written).
- If no suggestions available, keep the existing default message: "No pheromone suggestions yet -- use /ant:focus and /ant:redirect to guide the colony."

**Step 7 updates (Create Colony -- Post-Approval) -- add pheromone auto-apply:**

After charter-write (in both fresh init and re-init paths), add pheromone auto-apply logic:

- If approved pheromone suggestions exist (user approved them in the prompt and didn't remove them):
- For each approved pheromone suggestion, call:
  ```bash
  bash .aether/aether-utils.sh pheromone-write "{type}" "{content}" --source "system:init" --reason "{reason}" --ttl "30d"
  ```
- Use `2>/dev/null || true` to make each call non-blocking (per existing pattern).
- The pheromone auto-apply is the mechanism that fulfills the locked decision: "On approval, suggested pheromones are auto-applied as active signals (no manual emit needed)."
- Note: Claude (the LLM executing init.md) tracks which pheromones the user kept, edited, or removed during the approval loop. Only emit pheromones that survived approval.

Important implementation notes:
- Per pitfall 4 (shell quoting): Use single quotes around pheromone content when constructing the bash commands, or properly escape any special characters from governance-derived content.
- Per locked decision: no length cap on intelligence content. Show everything found, user removes what they don't want.
- Per locked decision: intelligence is always on. No opt-out flag.
  </action>
  <verify>
Read the updated init.md and verify:
1. Step 3 extracts colony_context, governance, and pheromone_suggestions from scan data
2. Step 5 has Prior Context section conditionally displayed before Charter
3. Step 5 Charter Governance field is pre-populated from governance_rules
4. Step 5 Pheromones section shows numbered suggestions with type prefix
5. Step 7 has pheromone-write loop for approved suggestions
Run: `npm run lint:sync` -- must pass (verifies Claude/OpenCode parity).
  </verify>
  <done>
init.md Step 3 extracts three new intelligence fields. Step 5 displays enriched prompt with conditional Prior Context section, pre-populated Governance, and numbered pheromone suggestions. Step 7 auto-applies approved pheromones via pheromone-write. All sections are editable before approval.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mirror init.md changes to OpenCode</name>
  <files>.opencode/commands/ant/init.md</files>
  <action>
Mirror all Step 3, 5, and 7 changes from .claude/commands/ant/init.md to .opencode/commands/ant/init.md.

**Critical mirroring rules (per research pitfall 3 and established pattern):**
- OpenCode has `Step -1: Normalize Arguments` and `Step 0: Initialize Visual Mode` at the start -- DO NOT modify or remove these.
- OpenCode uses `$normalized_args` instead of `$ARGUMENTS` -- preserve this difference.
- Steps 2-8 should be content-identical between Claude and OpenCode versions.
- Copy Steps 3, 5, and 7 verbatim from the updated Claude init.md, only replacing `$ARGUMENTS` with `$normalized_args` in Step 5 Charter Intent line (if the original references $ARGUMENTS there).

After mirroring, verify the two files differ ONLY in:
1. OpenCode has Step -1 (normalize) and Step 0 (visual mode)
2. OpenCode uses $normalized_args where Claude uses $ARGUMENTS
3. All Step 3, 5, 7 content is otherwise identical.

Run `npm run lint:sync` to verify sync policy passes.
  </action>
  <verify>
Run: `npm run lint:sync` -- must pass (this is the authoritative check for Claude/OpenCode parity).
Run: `diff <(sed -n '/### Step 3/,/### Step 4/p' .claude/commands/ant/init.md) <(sed -n '/### Step 3/,/### Step 4/p' .opencode/commands/ant/init.md)` -- should show no diff or only $ARGUMENTS vs $normalized_args difference.
  </verify>
  <done>
OpenCode init.md mirrors all intelligence enrichment changes. Steps 3, 5, and 7 are content-identical to Claude version. OpenCode-specific Step -1 and Step 0 preserved. lint:sync passes.
  </done>
</task>

</tasks>

<verification>
- Both init.md files display Prior Context section when prior colonies exist
- Both init.md files omit Prior Context section when no prior colonies exist
- Both init.md files show numbered pheromone suggestions with type prefix
- Both init.md files pre-populate Governance field from detected config files
- Both init.md files auto-apply approved pheromones via pheromone-write in Step 7
- `npm run lint:sync` passes
- `npm test` passes with no regressions
</verification>

<success_criteria>
- Approval prompt shows Prior Context -> Charter -> Context -> Pheromones when prior colonies exist
- Approval prompt shows Charter -> Context -> Pheromones when no prior colonies exist (no empty section)
- Governance field pre-populated with prescriptive rules from config detection
- Pheromone suggestions shown as numbered list with [FOCUS]/[REDIRECT] prefix
- Approved pheromones auto-applied via pheromone-write with source "system:init" and TTL "30d"
- OpenCode mirror is content-identical (Steps 2-8) with lint:sync passing
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/32-intelligence-enhancements/32-02-SUMMARY.md`
</output>
