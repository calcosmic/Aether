---
phase: 71-platform-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/codex_worker_cleanup.go
  - cmd/verification_process_group_unix.go
  - cmd/verification_process_group_windows.go
  - cmd/worker_cleanup_signal_common.go
  - cmd/worker_cleanup_signal_unix.go
  - cmd/worker_cleanup_signal_windows.go
  - pkg/codex/process_tracker.go
  - pkg/codex/process_tracker_test.go
  - pkg/codex/process_group_unix.go
  - pkg/codex/process_group_unix_test.go
  - pkg/codex/process_group_windows.go
  - pkg/codex/process_group_windows_test.go
  - cmd/codex_build.go
  - cmd/codex_colonize.go
  - cmd/codex_continue.go
  - cmd/codex_continue_finalize.go
  - cmd/codex_continue_plan.go
  - cmd/codex_continue_test.go
  - cmd/codex_plan.go
  - cmd/codex_visuals.go
  - cmd/codex_visuals_test.go
  - cmd/codex_workflow_cmds.go
  - cmd/compatibility_cmds.go
  - cmd/compatibility_cmds_test.go
  - cmd/continue_wrapper_ceremony_test.go
  - cmd/gate.go
  - cmd/gate_test.go
  - cmd/hook_cmds.go
  - cmd/hook_cmds_test.go
  - cmd/platform_doc_hygiene_test.go
  - cmd/recovery_snapshot.go
  - cmd/root.go
  - cmd/root_test.go
  - cmd/timeouts_test.go
  - pkg/codex/platform_dispatch.go
  - pkg/codex/worker.go
  - pkg/codex/worker_test.go
  - cmd/state_cmds.go
  - cmd/shelf_cmd.go
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - PLAT-02
  - PLAT-03

must_haves:
  truths:
    - "Uncommitted process tracking and worker cleanup code is committed and tested"
    - "All 25 agent types produce valid dispatch manifests via codexBuildManifest"
    - "Shelf subcommands (shelf-list, shelf-promote-batch, shelf-dismiss-batch) work correctly from Go CLI"
    - "All existing tests pass after committing uncommitted work"
  artifacts:
    - path: "pkg/codex/process_tracker.go"
      provides: "Worker process tracking with TrackProcess, UntrackProcess, KillProcess, KillAll, DetectStaleWorkers"
    - path: "pkg/codex/process_group_unix.go"
      provides: "Unix process group management with setpgid, SIGTERM, SIGKILL"
    - path: "pkg/codex/process_group_windows.go"
      provides: "Windows process group management"
    - path: "cmd/codex_worker_cleanup.go"
      provides: "Stale worker cleanup before dispatch"
    - path: "cmd/state_cmds.go"
      provides: "state-mutate --verify-only and --revert flags"
  key_links:
    - from: "cmd/codex_worker_cleanup.go"
      to: "pkg/codex/process_tracker.go"
      via: "calls codex.CleanupStaleWorkers(root)"
      pattern: "CleanupStaleWorkers"
    - from: "cmd/root.go"
      to: "cmd/codex_worker_cleanup.go"
      via: "setupWorkerCleanupHandler in init()"
      pattern: "setupWorkerCleanupHandler"
---

<objective>
Commit the 20+ modified and 10 new uncommitted files as the foundation for platform hardening. Add missing state-mutate flags. Verify dispatch manifest correctness for all 25 agent types. Audit shelf operations for OpenCode parity.

Purpose: The uncommitted work (process tracking, worker cleanup, process groups, dispatch improvements) must be committed before any flag-fix work begins, preventing merge conflicts. Per D-12, the planner must incorporate existing work rather than planning from scratch.

Output: All uncommitted changes committed with passing tests. Dispatch manifest test covering all 25 agent types. Shelf audit confirming PLAT-01/PLAT-02 parity. Missing state-mutate --verify-only and --revert flags added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-platform-hardening/71-CONTEXT.md
@.planning/phases/71-platform-hardening/71-RESEARCH.md
@.planning/phases/71-platform-hardening/71-PATTERNS.md

@cmd/root.go
@cmd/codex_build.go
@cmd/codex_worker_cleanup.go
@cmd/state_cmds.go
@cmd/shelf_cmd.go
@pkg/codex/process_tracker.go
@pkg/codex/platform_dispatch.go
@pkg/codex/worker.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit uncommitted foundation work and verify tests</name>
  <files>
    cmd/codex_worker_cleanup.go,
    cmd/verification_process_group_unix.go,
    cmd/verification_process_group_windows.go,
    cmd/worker_cleanup_signal_common.go,
    cmd/worker_cleanup_signal_unix.go,
    cmd/worker_cleanup_signal_windows.go,
    pkg/codex/process_tracker.go,
    pkg/codex/process_tracker_test.go,
    pkg/codex/process_group_unix.go,
    pkg/codex/process_group_unix_test.go,
    pkg/codex/process_group_windows.go,
    pkg/codex/process_group_windows_test.go,
    cmd/codex_build.go,
    cmd/codex_colonize.go,
    cmd/codex_continue.go,
    cmd/codex_continue_finalize.go,
    cmd/codex_continue_plan.go,
    cmd/codex_continue_test.go,
    cmd/codex_plan.go,
    cmd/codex_visuals.go,
    cmd/codex_visuals_test.go,
    cmd/codex_workflow_cmds.go,
    cmd/compatibility_cmds.go,
    cmd/compatibility_cmds_test.go,
    cmd/continue_wrapper_ceremony_test.go,
    cmd/gate.go,
    cmd/gate_test.go,
    cmd/hook_cmds.go,
    cmd/hook_cmds_test.go,
    cmd/platform_doc_hygiene_test.go,
    cmd/recovery_snapshot.go,
    cmd/root.go,
    cmd/root_test.go,
    cmd/timeouts_test.go,
    pkg/codex/platform_dispatch.go,
    pkg/codex/worker.go,
    pkg/codex/worker_test.go
  </files>
  <read_first>
    cmd/root.go,
    cmd/codex_worker_cleanup.go,
    pkg/codex/process_tracker.go,
    pkg/codex/worker.go,
    pkg/codex/platform_dispatch.go
  </read_first>
  <action>
    1. Stage all modified and untracked files listed in `<files>`. Use `git add` for each specific file path -- do NOT use `git add -A` or `git add .`.

    2. Run `go test ./... -count=1` to confirm all tests pass before committing.

    3. Commit with message: `feat(71): commit process tracking, worker cleanup, and dispatch improvements`

    Per D-12: The 20+ modified cmd/ files and 10 new untracked files implement process group management (Unix/Windows), worker process tracking (TrackProcess/UntrackProcess/KillAll/DetectStaleWorkers), stale worker cleanup before dispatch, and dispatch improvements across build/continue flows. These files are already functional and tested.
  </action>
  <acceptance_criteria>
    - `git log --oneline -1` shows the commit message containing "feat(71)"
    - `go test ./... -count=1` exits with code 0
    - `git status --short` shows no uncommitted cmd/ or pkg/codex/ files
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -count=1 && git diff --name-only HEAD~1 | grep -c 'cmd\|pkg/codex'</automated>
  </verify>
  <done>All uncommitted process tracking and dispatch work committed. All 18 packages pass. Working tree clean for cmd/ and pkg/codex/.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing state-mutate flags and dispatch manifest test</name>
  <files>
    cmd/state_cmds.go,
    cmd/shelf_cmd.go
  </files>
  <read_first>
    cmd/state_cmds.go (lines 840-862 for init() function with current flags),
    cmd/shelf_cmd.go (to verify shelf subcommand flags),
    cmd/codex_build.go (lines 1653-1663 for codexAgentFileForCaste and codexAgentNameForCaste),
    cmd/gate.go (lines 1-50 for existing gate flag patterns)
  </read_first>
  <action>
    **Part A: Add missing state-mutate --verify-only flag**

    In `cmd/state_cmds.go`, in the `init()` function (around line 859, after the `--guard` flag registration), add:

    ```go
    stateMutateCmd.Flags().Bool("verify-only", false, "Only verify the guard precondition without mutating state")
    stateMutateCmd.Flags().String("revert", "", "Revert a guard: task-complete:<id> or phase-advance:<id>")
    ```

    In the stateMutateCmd RunE function, handle the `--verify-only` flag:
    - Read the flag: `verifyOnly, _ := cmd.Flags().GetBool("verify-only")`
    - When `verifyOnly` is true and `guard` is non-empty, only check the guard condition but do NOT execute the mutation. Return early with `outputOK` showing the guard check result.
    - When `revert` is non-empty, remove the matching guard entry from state (reverse of `--guard`). Use the same parsing logic as guard but remove the task-complete or phase-advance entry instead of adding it.

    **Part B: Write dispatch manifest test for all 25 agent types**

    Create a test in `cmd/codex_build_test.go` (if it doesn't exist, add to existing test file) that:
    1. Iterates over all known caste names: `builder`, `watcher`, `scout`, `oracle`, `architect`, `route_setter`, `queen`, `colonizer`, `surveyor-nest`, `surveyor-disciplines`, `surveyor-pathogens`, `surveyor-provisions`, `keeper`, `tracker`, `probe`, `weaver`, `auditor`, `chaos`, `archaeologist`, `gatekeeper`, `includer`, `measurer`, `sage`, `ambassador`, `chronicler`, `medic`
    2. For each caste, calls `codexAgentFileForCaste(caste)` and verifies the result is a non-empty `.toml` filename
    3. For each caste, calls `codexAgentNameForCaste(caste)` and verifies the result is a non-empty agent name without `.toml` extension
    4. Verifies all results are unique (no two castes map to the same agent file)

    **Part C: Verify shelf subcommands for PLAT-01/PLAT-02**

    Read `cmd/shelf_cmd.go` and verify the following subcommands are registered with correct flags:
    - `shelf-list` with `--status` and `--json` flags
    - `shelf-add` with `--text`, `--category`, `--source`, `--tag` flags
    - `shelf-promote-batch` with `--ids` flag
    - `shelf-dismiss-batch` with `--ids` flag

    If any are missing, add them following the existing flag registration pattern in the file. Per D-01 and D-02: these subcommands must work correctly when called from the OpenCode wrapper flow, which delegates to the Go runtime.

    Run `go test ./cmd/ -run 'TestStateMutate|TestDispatch|TestShelf' -count=1 -v` after changes.
  </action>
  <acceptance_criteria>
    - `cmd/state_cmds.go` contains `stateMutateCmd.Flags().Bool("verify-only"` on a line matching the flag registration pattern
    - `cmd/state_cmds.go` contains `stateMutateCmd.Flags().String("revert"` on a line matching the flag registration pattern
    - `go test ./cmd/ -run TestStateMutate -count=1` exits with code 0
    - A test exists that iterates over all 25 agent caste names and validates `codexAgentFileForCaste` and `codexAgentNameForCaste`
    - `cmd/shelf_cmd.go` registers `shelf-list`, `shelf-add`, `shelf-promote-batch`, `shelf-dismiss-batch` subcommands
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/ -run 'TestStateMutate|TestDispatchManifest|TestShelf' -count=1 -timeout 60s</automated>
  </verify>
  <done>state-mutate has --verify-only and --revert flags. Dispatch manifest test validates all 25 agent types. Shelf subcommands verified for PLAT-01/PLAT-02 parity. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process tracker -> OS | ProcessTracker reads/writes JSON files and sends signals to PIDs. PID values must be positive integers. |
| Worker cleanup -> Process tree | Stale worker detection enumerates PIDs and sends SIGTERM/SIGKILL. Must only target known worker processes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | E (Elevation) | process_tracker.go KillProcess | mitigate | `isKnownWorkerProcess()` already validates target is codex/claude/opencode binary before killing. No change needed. |
| T-71-02 | T (Tampering) | state_cmds.go --revert flag | accept | Guard revert requires store access; no external input path. Low risk. |
| T-71-03 | S (Spoofing) | dispatch manifest generation | accept | Manifest is generated from hardcoded caste map; no user-controlled input in mapping. |
</threat_model>

<verification>
1. `go test ./... -count=1` -- all packages pass
2. `git log --oneline -1` -- uncommitted work committed
3. `grep -c 'verify-only' cmd/state_cmds.go` -- returns >= 1
4. `grep -c 'revert' cmd/state_cmds.go` -- returns >= 1
</verification>

<success_criteria>
- All uncommitted cmd/ and pkg/codex/ changes committed with passing tests
- state-mutate --verify-only and --revert flags registered and functional
- Dispatch manifest test validates all 25 agent types produce correct TOML filenames
- Shelf subcommands verified present with correct flags
- Full test suite green: `go test ./... -count=1`
</success_criteria>

<output>
After completion, create `.planning/phases/71-platform-hardening/71-01-SUMMARY.md`
</output>
