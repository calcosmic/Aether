---
phase: 22-config-foundation-core-routing
plan: 03
type: execute
wave: 3
depends_on: [22-01, 22-02]
files_modified:
  - .aether/workers.md
  - .claude/commands/ant/verify-castes.md
autonomous: true
requirements: [ROUTE-05, ROUTE-07, ROUTE-08]

must_haves:
  truths:
    - "workers.md no longer claims per-caste routing is impossible"
    - "workers.md documents the two-tier caste-to-slot assignment"
    - "workers.md documents how to activate GLM-5 through the opus slot (settings.json change)"
    - "workers.md documents dual-mode switching (Claude API vs GLM proxy)"
    - "verify-castes.md no longer says per-caste routing is impossible"
    - "verify-castes.md displays caste-to-slot assignments"
  artifacts:
    - path: ".aether/workers.md"
      provides: "Updated model selection documentation"
      contains: "model: opus"
    - path: ".claude/commands/ant/verify-castes.md"
      provides: "Updated verify-castes command with slot display"
      contains: "Model Slot"
  key_links:
    - from: ".aether/workers.md"
      to: ".aether/model-profiles.yaml"
      via: "References YAML as source of truth for caste-to-slot mapping"
      pattern: "model-profiles.yaml"
    - from: ".claude/commands/ant/verify-castes.md"
      to: ".claude/agents/ant/*.md"
      via: "Verify command reads agent frontmatter to display slot assignments"
      pattern: "model:"
---

<objective>
Rewrite the Model Selection section in workers.md and the verify-castes command to reflect the new per-caste routing via agent frontmatter, document dual-mode operation, and provide settings.json change instructions for activating GLM-5.

Purpose: Remove outdated "per-caste routing is impossible" claims and document the working two-tier routing system so users can understand and modify it.

Output: Updated workers.md and verify-castes.md with accurate routing documentation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-config-foundation-core-routing/22-CONTEXT.md
@.planning/phases/22-config-foundation-core-routing/22-01-SUMMARY.md
@.planning/phases/22-config-foundation-core-routing/22-02-SUMMARY.md
@.aether/workers.md
@.claude/commands/ant/verify-castes.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite workers.md Model Selection section</name>
  <files>.aether/workers.md</files>
  <action>
Replace the entire "Model Selection (Session-Level)" section in `.aether/workers.md` (lines 53-91) with updated documentation reflecting the new two-tier routing system.

**Replace the section** (from `## Model Selection (Session-Level)` through the `---` before `## Honest Execution Model`) with:

```markdown
## Model Selection (Per-Caste via Agent Frontmatter)

Aether uses a two-tier model routing system. Each agent's model slot is set in its frontmatter `model:` field, and Claude Code resolves the slot to an actual model via environment variables in `~/.claude/settings.json`.

### Caste-to-Slot Assignment

| Slot | Castes | Role |
|------|--------|------|
| **Opus** | queen, archaeologist, route-setter, sage, tracker, auditor, gatekeeper, measurer | Reasoning, analysis, coordination |
| **Sonnet** | builder, watcher, scout, chaos, probe, weaver, ambassador, nest, disciplines, pathogens, provisions | Execution, research, implementation |
| **Inherit** | chronicler, includer, keeper | Uses parent's model |

The source of truth for caste-to-slot assignments is `.aether/model-profiles.yaml` (`worker_models` section). Agent frontmatter in `.claude/agents/ant/*.md` mirrors these assignments.

### How Slot Resolution Works

When Claude Code spawns a sub-agent with `model: opus`, it looks up the `ANTHROPIC_DEFAULT_OPUS_MODEL` environment variable to determine the actual model. The slot name is abstract -- the same routing code works regardless of which models are mapped.

### Activating GLM-5 Through the Opus Slot

In `~/.claude/settings.json` (or `~/.claude/settings.json.glm` for local proxy mode), set:

```json
{
  "env": {
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "glm-5",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "glm-5-turbo",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "glm-4.5-air"
  }
}
```

This maps the opus slot to GLM-5 and the sonnet slot to GLM-5-Turbo.

### Dual-Mode Switching

The same caste-to-slot assignments work with both Claude API and GLM proxy:

**GLM Proxy mode** (via `~/.claude/settings.json.glm`):
- opus -> glm-5, sonnet -> glm-5-turbo, haiku -> glm-4.5-air
- Activate: `cp ~/.claude/settings.json.glm ~/.claude/settings.json`

**Claude API mode** (via `~/.claude/settings.json.claude`):
- opus -> claude-opus-4, sonnet -> claude-sonnet-4, haiku -> claude-haiku-4
- Activate: `cp ~/.claude/settings.json.claude ~/.claude/settings.json`
- No Aether code changes needed to switch modes

### Available Models (via GLM Proxy)

| Model | Slot | Best For | Provider |
|-------|------|----------|----------|
| glm-5 | opus | Deep reasoning, bounded analysis | Z_AI |
| glm-5-turbo | sonnet | Agent workflows, coding, orchestration | Z_AI |
| glm-4.5-air | haiku | Lightweight/haiku-tier tasks | Z_AI |

### Historical Note

A model-per-caste routing system was previously attempted using environment variable injection at spawn time (archived in `.aether/archive/model-routing/`). This approach could not function due to Claude Code Task tool limitations. The current approach uses agent frontmatter `model:` fields, which Claude Code handles natively at spawn time -- no Aether code intervention required.
```

**Do NOT modify any other section of workers.md.** Only the Model Selection section (lines 53-91) gets replaced.
  </action>
  <verify>
1. `grep -c "per-caste routing is impossible" .aether/workers.md` -- should output 0
2. `grep -c "model: opus" .aether/workers.md` -- should output at least 1
3. `grep -c "Dual-Mode" .aether/workers.md` -- should output at least 1
4. `grep "ANTHROPIC_DEFAULT_OPUS_MODEL" .aether/workers.md` -- should match (settings instructions present)
  </verify>
  <done>
The Model Selection section no longer claims per-caste routing is impossible. It documents the two-tier caste-to-slot system, slot resolution mechanism, GLM-5 activation steps, dual-mode switching instructions, and retains the historical note with updated context.
</done>
</task>

<task type="auto">
  <name>Task 2: Update verify-castes.md to show slot assignments and remove impossibility claims</name>
  <files>.claude/commands/ant/verify-castes.md</files>
  <action>
Rewrite `.claude/commands/ant/verify-castes.md` to show caste-to-slot assignments and remove all claims that per-caste routing is impossible.

**Replace Step 1** (lines 8-30) with a new caste table that includes the Model Slot column:

```markdown
## Step 1: Show Caste Assignments

Display the colony caste structure with model slot assignments:

```
Aether Colony Caste System
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CASTE ASSIGNMENTS
─────────────────
Slot: OPUS (reasoning/analysis)
  👑 Queen              - Colony coordination and strategic planning
  🏺 Archaeologist      - Git history analysis and pattern excavation
  🗺️ Route Setter      - Task decomposition and planning
  📜 Sage               - Wisdom synthesis and cross-colony knowledge
  🔍 Tracker            - Bug investigation and root cause analysis
  📋 Auditor            - Quality gate and code review
  🔒 Gatekeeper         - Security scanning and antipattern detection
  📏 Measurer           - Performance analysis and metrics

Slot: SONNET (execution/implementation)
  🔨 Builder            - Implementation and coding
  👁️ Watcher            - Verification and testing
  🔎 Scout              - Research and exploration
  🎲 Chaos              - Edge case testing and resilience probing
  🔬 Probe              - Test coverage analysis
  🧵 Weaver             - Refactoring specialist
  🌐 Ambassador         - External integrations
  🏠 Nest               - Directory structure mapping
  📚 Disciplines        - Convention documentation
  🦠 Pathogens          - Tech debt identification
  📦 Provisions         - Dependency mapping

Slot: INHERIT (uses parent's model)
  📝 Chronicler        - Documentation
  ♿ Includer           - Accessibility audits
  🗄️ Keeper            - Knowledge preservation

───────────────────────────────────────────
```

The model slot assignments come from agent frontmatter (`model:` field).
Source of truth: `.aether/model-profiles.yaml` `worker_models` section.
```

**Replace Step 3** (lines 43-58) with:

```markdown
## Step 3: Show Current Model Configuration

Display the active model mapping:

```
MODEL CONFIGURATION
───────────────────
To change models, swap settings files:

  GLM Proxy mode:
    cp ~/.claude/settings.json.glm ~/.claude/settings.json
    (opus -> glm-5, sonnet -> glm-5-turbo, haiku -> glm-4.5-air)

  Claude API mode:
    cp ~/.claude/settings.json.claude ~/.claude/settings.json
    (opus -> claude-opus-4, sonnet -> claude-sonnet-4, haiku -> claude-haiku-4)

Current model mapping can be verified by reading agent frontmatter:
  grep "^model:" .claude/agents/ant/*.md
```
```

**Replace Step 4** (lines 60-73) with:

```markdown
## Step 4: Summary

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
System Status
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Utils: ✓ Operational
Proxy: {status from Step 2}
Castes: 22 defined (8 opus, 11 sonnet, 3 inherit)
Routing: Per-caste via agent frontmatter model: field
```
```

**Replace the Historical Note** (lines 86-95) with:

```markdown
## Historical Note

Per-caste model routing was initially attempted using environment variable
injection at spawn time (archived in `.aether/archive/model-routing/`).
That approach failed due to Claude Code Task tool limitations.

The current approach uses agent frontmatter `model:` fields, which Claude Code
handles natively. No Aether code intervention is required -- Claude Code reads
the frontmatter and resolves the slot name through `ANTHROPIC_DEFAULT_*_MODEL`
environment variables.

To view the archived v1 configuration:
```bash
git show model-routing-v1-archived
```
```
  </action>
  <verify>
1. `grep -c "not possible" .claude/commands/ant/verify-castes.md` -- should output 0
2. `grep -c "model: opus" .claude/commands/ant/verify-castes.md` -- should output at least 1
3. `grep -c "INHERIT" .claude/commands/ant/verify-castes.md` -- should output at least 1
4. `grep -c "Model Slot" .claude/commands/ant/verify-castes.md` -- should output at least 1
  </verify>
  <done>
verify-castes.md displays a three-tier caste table (Opus/Sonnet/Inherit), shows current model configuration with dual-mode switching instructions, and the historical note correctly states that the current approach works via agent frontmatter.
</done>
</task>

</tasks>

<verification>
1. `grep -c "is impossible\|not possible\|cannot function" .aether/workers.md` -- should output 0 (or only in historical note context)
2. `grep -c "is impossible\|not possible\|cannot function" .claude/commands/ant/verify-castes.md` -- should output 0 (historical note should say the OLD approach failed, not that routing is impossible)
3. `grep "ANTHROPIC_DEFAULT_OPUS_MODEL" .aether/workers.md` -- matches (GLM-5 activation documented)
4. `grep "Dual-Mode" .aether/workers.md` -- matches (dual-mode documented)
5. `grep "model: opus\|model: sonnet\|model: inherit" .claude/commands/ant/verify-castes.md` -- all three present in display
</verification>

<success_criteria>
- workers.md Model Selection section describes the working two-tier routing system
- workers.md documents settings.json change for GLM-5 activation (ROUTE-05)
- workers.md documents dual-mode switching (ROUTE-07)
- workers.md no longer claims per-caste routing is impossible (ROUTE-08)
- verify-castes.md shows caste-to-slot assignments with tier grouping
- verify-castes.md no longer says per-caste routing is impossible
- Historical notes in both files correctly distinguish between failed v1 (env var injection) and working v2 (agent frontmatter)
</success_criteria>

<output>
After completion, create `.planning/phases/22-config-foundation-core-routing/22-03-SUMMARY.md`
</output>
