---
phase: 84-verification-depth-extension
plan: 02
type: execute
wave: 2
depends_on: ["84-01"]
files_modified:
  - .aether/commands/continue.yaml
  - .aether/commands/build.yaml
  - .claude/commands/ant/continue.md
  - .claude/commands/ant/build.md
  - .opencode/commands/ant/continue.md
  - .opencode/commands/ant/build.md
autonomous: true
requirements: [DEPTH-02]

must_haves:
  truths:
    - "Continue YAML source defaults to --verification-depth standard (no longer --light)"
    - "Build YAML source references --verification-depth instead of --light"
    - "Both wrappers (Claude and OpenCode) pass the new flag and explain the 3-level system"
  artifacts:
    - path: ".aether/commands/continue.yaml"
      provides: "Updated runtime command with --verification-depth standard"
      contains: "verification-depth"
    - path: ".claude/commands/ant/continue.md"
      provides: "Updated Claude wrapper with verification depth section"
      contains: "verification-depth"
    - path: ".opencode/commands/ant/continue.md"
      provides: "Updated OpenCode wrapper (structural parity with Claude)"
      contains: "verification-depth"
  key_links:
    - from: ".claude/commands/ant/continue.md"
      to: ".aether/commands/continue.yaml"
      via: "Wrapper references runtime command from YAML source"
      pattern: "verification-depth"
    - from: ".opencode/commands/ant/continue.md"
      to: ".claude/commands/ant/continue.md"
      via: "Structural parity -- same content, different platform"
      pattern: "verification-depth"
---

<objective>
Update YAML source definitions and both platform wrapper markdowns to expose the 3-level verification depth system.

Purpose: Users see the new --verification-depth flag in wrapper UX instead of the old --light/--heavy flags. The default changes from light to standard.

Output: Updated continue.yaml, build.yaml, and matching Claude + OpenCode wrapper markdowns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/84-verification-depth-extension/84-RESEARCH.md
@.planning/phases/84-verification-depth-extension/84-01-SUMMARY.md
@.planning/phases/83-planning-depth-system/83-02-SUMMARY.md

<interfaces>
<!-- Key patterns from Phase 83 Plan 02 (wrapper updates) -->

From .aether/commands/plan.yaml (Phase 83 added):
- Added `--planning-depth <light|standard|deep>` to runtime command
- Added ceremony section for planning depth selection
- Updated orchestration and post-plan sections

From .aether/commands/continue.yaml (current):
```yaml
name: ant-continue
runtime:
  command: "AETHER_OUTPUT_MODE=visual aether continue --skip-watchers --light $ARGUMENTS"
```

From .claude/commands/ant/continue.md (current, line 24):
```
AETHER_OUTPUT_MODE=visual aether continue --skip-watchers --light $ARGUMENTS
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update YAML source definitions for verification depth</name>
  <files>.aether/commands/continue.yaml, .aether/commands/build.yaml</files>
  <read_first>
    - .aether/commands/continue.yaml (full file -- current YAML source)
    - .aether/commands/build.yaml (full file -- check if it references --light/--heavy)
    - .aether/commands/plan.yaml (reference for how Phase 83 added --planning-depth)
  </read_first>
  <action>
    1. In .aether/commands/continue.yaml:
       - Change runtime.command from:
         `AETHER_OUTPUT_MODE=visual aether continue --skip-watchers --light $ARGUMENTS`
         to:
         `AETHER_OUTPUT_MODE=visual aether continue --skip-watchers --verification-depth standard $ARGUMENTS`
       - In wrapper_additions.orchestration, update the default path command (step 1) to use `--verification-depth standard` instead of `--light`
       - In wrapper_additions.orchestration, update the heavy external review path (step 1) to use `--verification-depth heavy` instead of `--heavy`:
         `AETHER_OUTPUT_MODE=json aether continue --plan-only --verification-depth heavy $ARGUMENTS`
       - Add a `verification_depth_ceremony` key after the orchestration section with this content:
         ```
         Verification depth controls how thorough the continue review is. Three levels:
         - light: Skip all review agents (fastest, minimal safety net)
         - standard: Probe-only review (default, watcher + probe coverage)
         - heavy: Full quality gauntlet (gatekeeper + auditor + probe)
         The default is "standard" for intermediate phases. Final phases always use "heavy".
         Users can override with --verification-depth <light|standard|heavy>.
         ```
       - Update pre_continue step 3: change "unless the user explicitly requested `--heavy`" to "unless the user explicitly requested `--verification-depth heavy` or `--heavy`"
       - Keep --light and --heavy mentioned as backward-compatible aliases where they appear in guardrails or instructions

    2. In .aether/commands/build.yaml:
       - Read the current file first. If the runtime command references --light or --heavy, update to use --verification-depth standard as the default
       - If the build YAML does not reference --light/--heavy in its runtime command, add a note in wrapper_additions about the new --verification-depth flag availability
       - Do NOT change the build command structure -- only update flag references
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && grep -c "verification-depth" .aether/commands/continue.yaml && grep "verification-depth standard" .aether/commands/continue.yaml</automated>
  </verify>
  <done>
    - continue.yaml runtime command uses --verification-depth standard (not --light)
    - continue.yaml has verification_depth_ceremony section
    - build.yaml updated if it referenced --light/--heavy
    - Heavy path uses --verification-depth heavy
    - Both files valid YAML (no syntax errors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Claude and OpenCode wrapper markdowns for verification depth</name>
  <files>.claude/commands/ant/continue.md, .claude/commands/ant/build.md, .opencode/commands/ant/continue.md, .opencode/commands/ant/build.md</files>
  <read_first>
    - .claude/commands/ant/continue.md (full file -- current Claude wrapper)
    - .claude/commands/ant/build.md (full file -- check for --light/--heavy references)
    - .opencode/commands/ant/continue.md (full file -- current OpenCode wrapper)
    - .opencode/commands/ant/build.md (full file -- check for --light/--heavy references)
    - .aether/commands/continue.yaml (read after Task 1 changes -- updated source)
    - .aether/commands/build.yaml (read after Task 1 changes -- updated source)
    - .planning/phases/83-planning-depth-system/83-02-SUMMARY.md (reference for Phase 83 wrapper pattern)
  </read_first>
  <action>
    1. In .claude/commands/ant/continue.md:
       - Find every occurrence of `--light` in a runtime command invocation and replace with `--verification-depth standard`
       - Find every occurrence of `--heavy` in a runtime command invocation and replace with `--verification-depth heavy`
       - Add a `## Verification Depth` section after any existing depth/granularity section (mirroring Phase 83's `## Planning Depth` section pattern):
         ```
         ## Verification Depth

         Verification depth controls how thorough the continue review is:
         - **light**: Skip all review agents -- fastest, minimal safety net
         - **standard**: Probe-only review (default) -- watcher + probe coverage
         - **heavy**: Full quality gauntlet -- gatekeeper + auditor + probe

         The default is "standard" for intermediate phases. Final phases always get "heavy".
         Override with `--verification-depth <light|standard|heavy>`.
         The old `--light` and `--heavy` flags still work as backward-compatible aliases.
         ```
       - Update any manifest invocation to include `--verification-depth <choice>`
       - Update the orchestration instructions to reference the new flag
       - Keep structural parity with Phase 83's wrapper update pattern

    2. In .claude/commands/ant/build.md:
       - Find every occurrence of `--light` or `--heavy` in a runtime command and update to use `--verification-depth` equivalents
       - If the build wrapper references review depth, update the text to explain the 3-level system
       - If it does not explicitly reference depth in commands, add a brief note about the --verification-depth flag being available

    3. In .opencode/commands/ant/continue.md:
       - Apply the EXACT same changes as the Claude continue.md (structural parity)
       - Diff both files after editing to confirm parity

    4. In .opencode/commands/ant/build.md:
       - Apply the EXACT same changes as the Claude build.md (structural parity)
       - Diff both files after editing to confirm parity

    5. After all edits, verify structural parity between Claude and OpenCode wrappers:
       - `diff <(grep -v "^#" .claude/commands/ant/continue.md) <(grep -v "^#" .opencode/commands/ant/continue.md)` should show no differences in content
       - Same for build.md
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && grep -c "verification-depth" .claude/commands/ant/continue.md && grep -c "verification-depth" .opencode/commands/ant/continue.md && diff <(grep -v "^#" .claude/commands/ant/continue.md | sed 's/\.claude/.opencode/g') <(grep -v "^#" .opencode/commands/ant/continue.md | sed 's/\.opencode/.claude/g') || true</automated>
  </verify>
  <done>
    - Both continue.md wrappers use --verification-depth standard in default runtime command
    - Both continue.md wrappers have ## Verification Depth section
    - Both build.md wrappers reference --verification-depth instead of --light/--heavy
    - Claude and OpenCode wrappers have structural parity (same content, different platform paths)
    - No remaining --light or --heavy in runtime command invocations (only as alias mentions)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wrapper markdown -> Go CLI | Wrapper instructions invoke the Go binary with flags; the binary validates all input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-06 | S (Spoofing) | Wrapper markdown content | accept | Wrappers are checked into version control; no runtime tampering vector |
| T-84-07 | T (Tampering) | YAML source flags | accept | YAML is source-of-truth in repo; Go CLI validates the actual flag values |
| T-84-08 | I (Information Disclosure) | Depth level in wrapper docs | accept | Depth level is not sensitive; it controls local review agent dispatch |
</threat_model>

<verification>
1. `grep "verification-depth standard" .aether/commands/continue.yaml` -- YAML source updated
2. `grep "verification-depth" .claude/commands/ant/continue.md` -- Claude wrapper updated
3. `grep "verification-depth" .opencode/commands/ant/continue.md` -- OpenCode wrapper updated
4. `grep "## Verification Depth" .claude/commands/ant/continue.md` -- Section added
5. Structural parity check between Claude and OpenCode wrappers
6. No `--light` remaining in runtime command invocations (only in alias documentation text)
</verification>

<success_criteria>
1. continue.yaml runtime command uses --verification-depth standard
2. Both continue.md wrappers have ## Verification Depth section explaining 3 levels
3. Build wrappers updated to reference --verification-depth
4. Claude and OpenCode wrappers have structural parity
5. Backward-compatible aliases (--light, --heavy) mentioned in documentation but not used as primary flags
</success_criteria>

<output>
After completion, create `.planning/phases/84-verification-depth-extension/84-02-SUMMARY.md`
</output>
