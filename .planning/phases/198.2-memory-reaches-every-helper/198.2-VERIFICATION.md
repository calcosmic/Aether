---
phase: 198.2-memory-reaches-every-helper
verified: 2026-08-30T17:10:57Z
status: human_needed
score: 7/7 success criteria verified by named passing tests
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "WIRE-06 'adjacency' truth (198.2-04-PLAN.md must_haves, tagged verification: backstop): confirm that memoryPackPartWriters (cmd/capsule_writer_invariant_198_2_test.go) is not just a hand-typed claim of 'this part has a writer' but that each listed writer genuinely produces non-empty content on the command(s) named in `reachedBy`, in a real run — not merely that a write call exists somewhere in the source."
    expected: "Every one of the 18 entries in memoryPackPartWriters names a writer that a human confirms actually fires and produces content during ordinary use of the named command(s) — not a writer that is reachable in source but dead or perpetually empty in practice (the exact failure mode WIRE-06 exists to close, previously true of Decisions/PhaseLearnings)."
    why_human: "TestEveryMemoryPackPartHasALiveWriter only checks that every colonyPrimeSection name has a matching entry in the hand-maintained memoryPackPartWriters map (or a justified, shrink-only exception) — it cannot verify that a claimed writer actually produces non-empty content in a real run, versus merely existing in source and firing on a path that never has real data. The plan's own frontmatter tags this specific truth 'verification: backstop', i.e. no held-out behavioral test proves it; per this project's proof standard, presence-plus-wiring alone does not satisfy a backstop-tagged truth. The 18 entries look individually well-reasoned (each names a concrete writer function and file, and a real command that exercises it) and the code review that ran after execution found no issue here, but no automated test exercises 'a writer that can source-compile but never fires in practice would still be treated as no writer.'"
---

# Phase 198.2: Memory Reaches Every Helper Verification Report

**Phase Goal:** Every piece of memory Aether keeps (Queen-file rules, hive lessons, instincts, failures, focus notes, the owner's answers, the last helper's relay note, the colonize map, Oracle research) reaches every helper that should read it, as actual content, on the command lane the owner actually runs — with one test per command asserting a sentinel sentence from each memory source.
**Verified:** 2026-08-30T17:10:57Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Summary

All 7 ROADMAP success criteria map to named tests that exist and pass against the
code at HEAD (`ffa2712d`), which includes the post-execution code-review fixes
(`b7dfc50d`, `101d558c`, `5d9edc60`). `go build ./...`, `go vet ./...`,
`go test ./pkg/...` and `go test ./cmd/... -count=1` (282s, full suite, not
filtered) all pass with zero failures. This is not a SUMMARY-trusting pass —
every test named below was run directly in this session, and the fixed source
lines for the review's Critical and Warning findings were read and confirmed
present, not just claimed in 198.2-REVIEW-FIX.md.

One item is routed to human verification rather than marked VERIFIED: a single
must-have truth in plan 04 is explicitly tagged `verification: backstop` in its
own frontmatter (no automated test can prove it), and this project's proof
standard does not accept presence-plus-wiring as satisfying a backstop-tagged
truth. See Human Verification Required below. Nothing else in the phase is
gated on it — it does not block the other six success criteria.

## Goal Achievement

### Observable Truths (Success Criteria → Named Tests)

| # | Success Criterion | Status | Evidence |
|---|---|---|---|
| 1 | `/ant-plan` and `/ant-colonize` deliver the full memory capsule to the planner and every surveyor on the real delegate lane, table-tested per source | ✓ VERIFIED | `TestPlanAndColonizeDelegateLanesCarryEveryMemorySource`, `TestPlanAndColonizeCapsulesMatchTheInProcessLane`, `TestDelegateCapsuleRendersSteeringAndRelayExactlyOnce`, `TestDelegateCapsuleIsStableAcrossRuns`, `TestPlanAndColonizeWrappersAreByteIdentical` — all PASS (`cmd/wire_capsule_198_2_test.go`), run directly this session |
| 2 | The research scout is shown the hive wisdom it is asked to summarise; invent-it instruction gone | ✓ VERIFIED | `TestResearchHelperIsShownTheSharedLessons`, `TestResearchHelperSeesTheSameSelectionAsEveryoneElse`, `TestSharedLessonsRespectTheCrossProjectSwitch`, `TestNoSharedLessonsMeansNoSection`, `TestFallbackResearchTemplateInventsNothing` — all PASS (`cmd/scout_hive_198_2_test.go`); `grep` for `"No relevant hive wisdom found"` / invent-instruction text returns zero hits in `cmd/codex_plan.go` and `cmd/phase_research.go` |
| 3 | Condensed colonize-report digest reaches build/plan/research briefs within budget; deletion changes the prompt | ✓ VERIFIED | `TestMapDigestCarriesASurveyorsOwnSentence`, `TestDeletingASurveyReportChangesTheDigest`, `TestSurveyorSentenceReachesAllThreeBriefs`, `TestTheAgeLineAppearsOncePerBrief`, `TestBriefsAreUnchangedWithoutASurvey`, `TestMapDigestIsDeterministic`, `TestLongFilenameTruncationNoticeStaysInsideTheSanitizerBoundary` — all PASS (`cmd/brief_digest_198_2_test.go`) |
| 4 | A finished Oracle run registers its output by itself; no hand-typed save/pointer needed | ✓ VERIFIED | `TestFinishedResearchRegistersItselfForTheNextHelper`, `TestStoppedResearchIsFiledAndLabelledPartial`, `TestStrongResearchFindingBecomesALabelledHabit`, `TestResearchOnTheSameTopicKeepsBothWriteUps`, `TestResearchPromotionDryRunDoesNotMutate` — all PASS (`cmd/oracle_autofile_198_2_test.go`); confirmed `finalizeOracleResearchArtifacts` is called from both `finalizeOracleLoop` and `stopOracleCompatibility` (`cmd/oracle_loop.go`) |
| 5 | Welcome card carries preferences, top instincts, last handoff — content-locked | ✓ VERIFIED | `TestGreetingCarriesPreferencesHabitsAndTheLastNote`, `TestGreetingOmitsWhatTheProjectDoesNotHaveYet`, `TestGreetingStaysSilentWithoutAProject`, `TestStrongestHabitsHaveOneRankingRule`, `TestClosingCardsAreUnchangedByTheGreetingBlock` — all PASS (`cmd/session_greeting_198_2_test.go`); `status.go` and `next_action_input.go` both call the one shared `loadStrongestRuntimeInstincts` |
| 6 | No capsule section empty by construction; Phase Learnings/Key Decisions have a writer or are removed | ✓ VERIFIED | `TestEveryMemoryPackPartHasALiveWriter` (AST-driven discovery, not a hand-typed list), `TestMemoryPackHasNoEmptyHeadings`, `TestMemoryPackWriterExceptionsOnlyShrink`, `TestPhaseLessonsStillReachAHelperUnderLearnedMemory`, `TestOwnerAnswersStillReachAHelperUnderClarifiedIntent` — all PASS (`cmd/capsule_writer_invariant_198_2_test.go`); `Decisions`/`PhaseLearnings` section builders confirmed removed from `cmd/colony_prime_context.go` with an explanatory comment |
| 7 | Previous phase's outcome/verification/review content reaches next phase's build brief | ✓ VERIFIED | `TestOutcomeFileMatchesTheClosingScreen`, `TestEveryCheckLaneWritesOneOutcome`, `TestBothCheckLanesLeaveTheSameOutcomeText`, `TestCarryForwardNamesOnlyWhatFailedOrWasFlagged`, `TestCarryForwardSurfacesNeedsOwnerConfirmationCriteria`, `TestPreviousPhaseFailureReachesTheNextBuildersBrief`, `TestOnlyTheImmediatelyPrecedingPhaseIsCarried`, `TestBriefGrowthIsCappedAtTheTwoNewSlots` — all PASS (`cmd/phase_outcome_198_2_test.go`, `cmd/brief_growth_cap_198_2_test.go`); `writePhaseOutcomeDocument` confirmed called from both `codex_continue.go` (direct lane) and `codex_continue_finalize.go` (delegate lane) |

**Score:** 7/7 success criteria backed by passing named tests (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `cmd/wire_capsule_198_2_test.go` | Delegate-lane per-command table test | ✓ VERIFIED | Exists, 7 tests, all pass |
| `cmd/oracle_autofile_198_2_test.go` | Oracle auto-file/register/promote coverage | ✓ VERIFIED | Exists, 9 tests, all pass |
| `cmd/session_greeting_198_2_test.go` | Greeting card content-lock coverage | ✓ VERIFIED | Exists, 6 tests, all pass |
| `cmd/capsule_writer_invariant_198_2_test.go` | Writer invariant | ✓ VERIFIED | Exists, 6 tests, all pass; discovery is AST-based (not hand-typed) |
| `cmd/phase_outcome_198_2_test.go` | outcome.md persistence | ✓ VERIFIED | Exists, 8 tests, all pass |
| `cmd/brief_digest_198_2_test.go` | Survey digest coverage | ✓ VERIFIED | Exists, 10 tests, all pass |
| `cmd/brief_growth_cap_198_2_test.go` | Carry-forward + growth cap coverage | ✓ VERIFIED | Exists, 11 tests, all pass (includes CR-01 regression test) |
| `cmd/scout_hive_198_2_test.go` | Research-scout hive wiring coverage | ✓ VERIFIED | Exists, 5 tests, all pass |
| `.claude/commands/ant/plan.md`, `.claude/commands/ant-plan.md`, `.opencode/commands/ant/plan.md` | Byte-identical triplet, capsule instruction added | ✓ VERIFIED | `diff` confirms byte-identical; capsule prepend instruction present in all three |
| `.claude/commands/ant/colonize.md`, `.claude/commands/ant-colonize.md`, `.opencode/commands/ant/colonize.md` | Byte-identical triplet, capsule instruction added | ✓ VERIFIED | `diff` confirms byte-identical; capsule prepend instruction present in all three; `TestSpecialistCommandSurfacesUnchanged` hash ledger updated in the same commit |
| `cmd/phase_carry_forward.go` | CR-01 fix present | ✓ VERIFIED | Loop branches on `!c.Passed` vs `c.Passed && c.State != ""`, matching the doc comment; read directly |
| `cmd/colony_research.go` | WR-01 fix present | ✓ VERIFIED | `registerColonyResearchDoc` routes through `store.UpdateFile` (locked), not raw `os.ReadFile`/`os.WriteFile`; read directly |
| `cmd/helpers.go` | WR-02/WR-03 fix present | ✓ VERIFIED | Truncation-notice length reserved from budget before slicing; `used` tracks pre-notice length only; read directly |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `codexPlanManifest.ContextCapsule` | `resolveCodexWorkerContext()` | Single call site in `runCodexPlanPlanOnly` | ✓ WIRED | Field exists at `codex_plan.go:274`, assigned at `:1523` |
| `codexColonizeManifest.ContextCapsule` | `resolveCodexWorkerContext()` | Single call site in `runCodexColonizePlanOnly` | ✓ WIRED | Field exists at `codex_colonize.go:99`, assigned at `:742` |
| `manifest.context_capsule` | spawned worker prompt | Platform wrapper prepend instruction | ✓ WIRED | Confirmed in all 6 wrapper files (plan × 3, colonize × 3) |
| `finalizeOracleLoop` / `stopOracleCompatibility` | `saveOracleResearchDocument` → `registerColonyResearchDoc` → `COLONY_STATE.json research_docs` | `finalizeOracleResearchArtifacts` (shared body) | ✓ WIRED | Both call sites confirmed at `oracle_loop.go:1387` (via `finalizeOracleResearchArtifacts`) and `oracle_loop.go` stop path |
| `resolveHiveWisdomEntriesForDomains` | `renderPhaseResearchBrief` | `resolveResearchSharedLessonsSection` | ✓ WIRED | Confirmed reused, not reimplemented, at `phase_research.go:154` |
| `resolveSurveyDigestSection()` | build / plan / research briefs | Three call sites, one function | ✓ WIRED | Confirmed at `codex_build.go:3354`, `codex_plan.go:2197`, `phase_research.go:141` |
| `resolvePreviousPhaseCarryForward` | build brief | `codex_build.go:3367` | ✓ WIRED | Confirmed call site; also confirmed CR-01 fix in the resolver itself |
| `writePhaseOutcomeDocument` | both check lanes | `codex_continue.go` (5 call sites) + `codex_continue_finalize.go` (2 call sites) | ✓ WIRED | Confirmed via grep and read |
| `loadStrongestRuntimeInstincts` | greeting card + status dashboard | Shared loader, one ranking rule | ✓ WIRED | Confirmed both `next_action_input.go:117` and `status.go:1043` call the same function |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| WIRE-01 | 198.2-01 | Plan/colonize delegate manifests carry capsule; per-command delegate-lane test | ✓ SATISFIED | See row 1 above |
| WIRE-02 | 198.2-08 | Research scout shown hive wisdom, invent-instruction removed | ✓ SATISFIED | See row 2 above |
| WIRE-03 | 198.2-06 | Condensed survey digest reaches build/plan/research briefs | ✓ SATISFIED | See row 3 above |
| WIRE-04 | 198.2-02 | Oracle auto-files/registers/promotes | ✓ SATISFIED | See row 4 above |
| WIRE-05 | 198.2-03 | Session-start card carries preferences/instincts/handoff | ✓ SATISFIED | See row 5 above |
| WIRE-06 | 198.2-04 | No capsule section empty by construction; invariant test | ✓ SATISFIED (with one backstop-tagged truth routed to human review — see Human Verification) | See row 6 above |
| WIRE-07 | 198.2-05, 198.2-07 | Previous phase outcome/verification/review reaches next build brief | ✓ SATISFIED | See row 7 above |

No orphaned requirements: `.planning/REQUIREMENTS.md` lists exactly WIRE-01..WIRE-07 mapped to "Phase 198.2 — Complete", and all seven appear in at least one plan's `requirements:` frontmatter field.

### Anti-Patterns Found

None. `grep -n -E "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` over every file named in the 8 plans' `files_modified` lists (excluding `_test.go`) returned zero hits.

### Behavioral Spot-Checks / Full-Suite Verification

| Check | Command | Result | Status |
|---|---|---|---|
| Build | `go build ./...` | Clean, no output | ✓ PASS |
| Vet | `go vet ./...` | Clean, no output | ✓ PASS |
| pkg/ tests | `go test ./pkg/... -count=1` | All 17 packages `ok` | ✓ PASS |
| cmd/ full suite | `go test ./cmd/... -count=1` | `ok github.com/calcosmic/Aether/cmd 282.181s` — no failures | ✓ PASS |
| All 61 phase-specific `_198_2_test.go` named tests | `go test ./cmd/ -run '<name>' -v -count=1` (run individually, per success-criterion table above) | All PASS | ✓ PASS |
| Wrapper triplet hash ledger | `go test ./cmd/ -run TestSpecialistCommandSurfacesUnchanged` | PASS, including updated colonize.md hashes | ✓ PASS |

### Code Review Fix Verification (not trusted from REVIEW-FIX.md narrative — read directly)

| Finding | Fix location | Verified in source? |
|---|---|---|
| CR-01 (carry-forward silently drops needs-owner-confirmation criteria) | `cmd/phase_carry_forward.go:104-119` | ✓ Yes — loop now branches on `!c.Passed` vs `c.Passed && c.State != ""`; `TestCarryForwardSurfacesNeedsOwnerConfirmationCriteria` passes |
| WR-01 (unlocked read-modify-write of COLONY_STATE.json) | `cmd/colony_research.go:213-256` | ✓ Yes — routed through `store.UpdateFile`, guarded against nil/mismatched store |
| WR-02/WR-03 (truncation-notice budget overflow / accounting skew) | `cmd/helpers.go:337-360` | ✓ Yes — notice length reserved from budget before slicing; `used` tracks pre-notice length; `TestLongFilenameTruncationNoticeStaysInsideTheSanitizerBoundary` passes |
| IN-01 (unsanitized greeting text) | Not fixed — explicitly Info-tier, explicitly out of scope | Confirmed skip is legitimate: Info-tier finding, terminal-output-only risk, explicitly named out of scope in the fix task instructions |

## Human Verification Required

### 1. WIRE-06 "adjacency" truth — writer map entries genuinely produce content in practice, not just in source

**Test:** Review the 18 entries in `memoryPackPartWriters` (`cmd/capsule_writer_invariant_198_2_test.go`) against real project usage — for at least a sample of parts (e.g. `medic_health`, `activity_tail`, `prior_reviews`), confirm the named writer actually populates the section with content during ordinary `/ant-build` / `/ant-continue` / `/ant-plan` / `/ant-colonize` usage, not merely that a Go function with a matching name exists and compiles.

**Expected:** Every entry's claimed writer is a genuine, regularly-firing write path — none of them is a writer that exists in source but is effectively dead (unreachable in practice, or only ever produces empty content), which is exactly the failure mode WIRE-06 was built to close (the prior state of `Decisions`/`PhaseLearnings`).

**Why human:** This specific must-have truth is explicitly tagged `verification: backstop` in `198.2-04-PLAN.md`'s own frontmatter — the plan's author already flagged that no automated test proves it. `TestEveryMemoryPackPartHasALiveWriter` only checks that a name-to-writer mapping exists in a hand-maintained table (via AST discovery of section names, which is solid) — it cannot check that the claimed writer function is genuinely exercised with real data during ordinary operation. Per this project's proof standard, presence-plus-wiring does not satisfy a truth explicitly marked as requiring behavioral/backstop evidence. This does not block the other six success criteria, which each have dedicated passing behavioral tests.

## Gaps Summary

No gaps. All 7 ROADMAP success criteria are backed by passing, directly-run named tests against the code at HEAD (post code-review-fix commits `b7dfc50d`, `101d558c`, `5d9edc60`). The full `cmd/` test suite (282s) and `pkg/` suite pass with zero failures; `go build ./...` and `go vet ./...` are clean. The three code-review fixes (CR-01, WR-01, WR-02/WR-03) were read directly in source and confirmed genuinely applied, not merely claimed in 198.2-REVIEW-FIX.md. The single Info-tier finding (IN-01) was legitimately left unfixed per its own explicit out-of-scope instruction.

The phase is not blocked by any FAILED truth or missing/stub artifact. The one item routed to human verification (the WIRE-06 backstop-tagged truth) is a narrow, explicitly-flagged gap between "table exists and is internally consistent" and "every entry's real-world behavior was independently confirmed" — worth a quick owner glance at the `memoryPackPartWriters` table, but not evidence of a defect.

---

_Verified: 2026-08-30T17:10:57Z_
_Verifier: Claude (gsd-verifier)_
