---
phase: 191-dead-wood
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - colony/ceremony/visuals.md
  - cmd/visuals_config.go
  - cmd/codex_visuals.go
  - cmd/codex_visuals_test.go
  - cmd/visuals_config_ratchet_test.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "colony/ceremony/visuals.md no longer exists, yet caste identity output (emoji, ANSI color, human-readable label, name prefixes, wordmark, divider) is byte-identical to what it produced before deletion, both in a Go-level regression test and in a real dev-checkout CLI capture"
    - "Every field visuals.md defined that diverged from its Go compiled default in cmd/codex_visuals.go has been folded into that default before deletion"
    - "loadVisualsConfig() remains in the codebase as a permanently-fallback function -- not deleted, per 191-CONTEXT.md D-04's explicit discretion"
    - "Reintroducing colony/ceremony/visuals.md causes a named test to fail, proven fail-then-pass"
  artifacts:
    - path: "cmd/codex_visuals.go"
      provides: "The folded, now-authoritative casteColorMap/casteEmojiMap/casteLabelMap and any command-emoji/wordmark/divider constants"
    - path: "cmd/visuals_config_ratchet_test.go"
      provides: "Reappearance ratchet for colony/ceremony/visuals.md"
      exports: ["TestVisualsConfigDoesNotReappear"]
  key_links:
    - from: "cmd/codex_visuals.go (casteIdentity / casteLabel / casteEmoji)"
      to: "cmd/visuals_config.go (fileCasteEmoji / fileCasteColor / fileCasteLabel)"
      via: "direct Go function call, (v, ok) pattern -- ok=false falls back to the compiled map"
      pattern: "fileCasteEmoji\\(|fileCasteColor\\(|fileCasteLabel\\("
---

<objective>
Resolve the third of the four CWD-relative silent-fallback loaders identified in 191-CONTEXT.md
§"Criterion 2": `loadVisualsConfig()` (`cmd/visuals_config.go`), which reads `colony/ceremony/visuals.md`
and feeds `cmd/codex_visuals.go`'s caste-identity rendering (the emoji/color/label/name-prefix system
CLAUDE.md's "Caste Identity System" section documents: `🔨🐜 Builder Mason-67`). Like the other three
loaders, this only ever successfully reads the file in a dev checkout (CWD = repo root) — every
installed/published Aether run already hits the compiled fallback (`colony/` is not published,
191-CONTEXT.md D-13). Diff `visuals.md`'s current values against `codex_visuals.go`'s compiled maps,
fold any divergence in, delete the file, and prove byte-identical output before and after.

Fully self-contained: `cmd/visuals_config.go` and `cmd/codex_visuals.go` share no files with any other
plan in this phase (verified in 191-CONTEXT.md's `<domain>`/`<canonical_refs>`), so this plan runs
independently in Wave 1.

Purpose: ROADMAP criterion 2 (one of four loaders).

Output: 1 file deleted, its Go compiled defaults folded to match, byte-identical output proven, one
reappearance ratchet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/191-dead-wood/191-CONTEXT.md
@.planning/phases/191-dead-wood/191-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing contracts the executor must build against -- extracted from the codebase. -->

From cmd/visuals_config.go (existing, do not reinvent):
```go
var visualsPathOverride string
func resetVisualsCache() // call between renders in a test to force a fresh load
type visualsConfig struct {
	VisualsVersion  string
	CasteEmojiMap   map[string]string
	CasteColorMap   map[string]string
	CasteLabelMap   map[string]string
	CommandEmojiMap map[string]string
	CastePrefixes   map[string][]string
	DefaultPrefixes []string
	AetherWordmark  string
	VisualDivider   string
}
func fileCasteEmoji(caste string) (string, bool)
func fileCasteColor(caste string) (string, bool)
func fileCasteLabel(caste string) (string, bool)
func fileCommandEmoji(command string) (string, bool)
func fileCastePrefixes(caste string) ([]string, bool)
func fileDefaultPrefixes() ([]string, bool)
func fileAetherWordmark() (string, bool)
func fileVisualDivider() (string, bool)
```

From CLAUDE.md's "Caste Identity System" section (already-documented, confirms the compiled-default
location):
```
Color maps in cmd/codex_visuals.go: casteColorMap (ANSI codes), casteEmojiMap (single emoji),
casteLabelMap (human-readable name). Functions: casteIdentity(), casteLabel(), casteEmoji().
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Baseline-capture, diff-and-fold, delete visuals.md</name>

  <files>colony/ceremony/visuals.md, cmd/codex_visuals.go, cmd/codex_visuals_test.go</files>

  <read_first>
    - `colony/ceremony/visuals.md` (full file) -- every field under its YAML frontmatter
      (`caste_emoji_map`, `caste_color_map`, `caste_label_map`, `command_emoji_map`, `caste_prefixes`,
      `default_prefixes`, `aether_wordmark`, `visual_divider`)
    - `cmd/codex_visuals.go` (full file) -- `casteColorMap`, `casteEmojiMap`, `casteLabelMap`, and any
      command-emoji/wordmark/divider constants; `casteIdentity()`, `casteLabel()`, `casteEmoji()`, and
      every other function that calls `fileCasteEmoji`/`fileCasteColor`/etc.
    - `cmd/codex_visuals_test.go` (full file) -- existing test patterns and fixtures to extend
    - 191-PATTERNS.md §"Baseline Capture Before Any Change" -- the two-layer requirement
    - A concrete CLI surface that renders caste identity (e.g. a build's worker-dispatch banner output,
      or `aether build --print-brief` — locate the most direct, already-existing render path via grep
      for `casteIdentity(` call sites in `cmd/`)
  </read_first>

  <behavior>
    - Every caste's emoji, ANSI color, and label — plus command emoji, name prefixes, wordmark, and
      divider — render identically whether `colony/ceremony/visuals.md` is present with its current
      content or absent (using the folded compiled default).
    - A before/after regression test in `cmd/codex_visuals_test.go`, using `visualsPathOverride` +
      `resetVisualsCache()`, proves this for every field in `visualsConfig`, not just a sample caste.
    - A real dev-checkout CLI capture (visuals.md present vs. deleted) is byte-identical.
  </behavior>

  <action>
From this repo's root, build the binary and run the CLI surface `read_first` identifies as rendering
caste identity output; save the result verbatim (e.g. `/tmp/aether-191-baseline-visuals.txt`).

Read `colony/ceremony/visuals.md` field by field against `cmd/codex_visuals.go`'s compiled maps/constants.
Produce a complete field-by-field diff table (every caste's emoji/color/label, every command emoji, every
caste's name prefixes, the default prefixes, the wordmark, the divider) — record it in the SUMMARY.

Fold every divergent value into `cmd/codex_visuals.go`'s compiled maps/constants. Write (or extend) a
before/after regression test in `cmd/codex_visuals_test.go`: render via `visualsPathOverride` pointed at
a temp copy of visuals.md's ORIGINAL content, then via the override pointed at a nonexistent path
(`resetVisualsCache()` between renders), assert every accessor (`fileCasteEmoji`, `fileCasteColor`,
`fileCasteLabel`, `fileCommandEmoji`, `fileCastePrefixes`, `fileDefaultPrefixes`, `fileAetherWordmark`,
`fileVisualDivider` and whatever `casteIdentity()`/`casteLabel()`/`casteEmoji()` ultimately produce)
matches across both renders, for every entry, not a sample. Run it before deleting the file (proves the
fold is complete).

Delete `colony/ceremony/visuals.md` (`git rm`). Re-run the regression test (must still pass). Re-run the
real dev-checkout CLI command and diff byte-for-byte against the baseline; record the diff (or its clean
absence) in the SUMMARY.
  </action>

  <verify>
    <automated>go test ./cmd/ -run 'TestVisualsConfig|TestCasteIdentity|TestFileCaste' -v -count=1</automated>
  </verify>

  <acceptance_criteria>
    - The before/after regression test covers every field in `visualsConfig` (not a sample) and passes
      both before and after deletion.
    - The real dev-checkout CLI capture, re-run after deletion, is byte-for-byte identical to the
      baseline -- diff recorded in the SUMMARY.
    - `colony/ceremony/visuals.md` no longer exists.
    - `TestCasteIdentityUsesHouseStyle` (the existing test CLAUDE.md's Caste Identity System section
      references) still passes unmodified -- confirms this fold did not disturb the locked
      `🔨🐜 Builder Mason-67`-style format.
    - `go build ./... && go vet ./...` exit 0.
    - `go test ./cmd/... -count=1` exits 0.
  </acceptance_criteria>

  <done>colony/ceremony/visuals.md is deleted; cmd/codex_visuals.go's compiled defaults now match what it last said for every field; caste-identity rendering is proven byte-identical before and after.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the reappearance ratchet with fail-then-pass proof</name>

  <files>cmd/visuals_config_ratchet_test.go</files>

  <read_first>
    - 191-PATTERNS.md §"The Ratchet House Style" -- Tier 1 shape and the mandatory fail-then-pass proof
    - `cmd/colony_prime_and_dispatch_contract_ratchet_test.go` (written by plan 191-02, same wave -- if
      it exists when this task runs, match its exact style for consistency; if it does not yet exist,
      follow 191-PATTERNS.md's Tier-1 shape directly without waiting for it)
  </read_first>

  <behavior>
    - `TestVisualsConfigDoesNotReappear` fails, by name, if `colony/ceremony/visuals.md` exists on disk.
    - Passes cleanly against the current (post-Task-1) repository state.
    - Fail-then-pass proven: temporarily recreate the file, confirm failure naming it, remove it,
      confirm the test passes again.
  </behavior>

  <action>
Create `cmd/visuals_config_ratchet_test.go` with `TestVisualsConfigDoesNotReappear`, following
191-PATTERNS.md's Tier-1 existence-check shape exactly: `os.Stat` against repo root (reuse this
repo's existing repo-root-resolution helper — grep an existing `cmd/*_ratchet_test.go` for it, do not
invent a new one), `t.Errorf` with a message naming the file and citing this plan's ruling if it exists.

Run the fail-then-pass proof described in `<behavior>` and record the transcript in the SUMMARY.
  </action>

  <verify>
    <automated>go test ./cmd/ -run 'TestVisualsConfigDoesNotReappear' -v -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `TestVisualsConfigDoesNotReappear` passes against the current repository state.
    - Fail-then-pass demonstrated and recorded in the SUMMARY.
    - `go build ./... && go vet ./...` exit 0.
    - `go test ./cmd/... -count=1` exits 0.
  </acceptance_criteria>

  <done>A permanent, proven ratchet fails loudly and by name if colony/ceremony/visuals.md reappears.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `colony/ceremony/visuals.md` content -> every worker-dispatch banner and status line a user sees | Caste identity output is the single most visible, most frequently-rendered UI surface in the whole colony ceremony -- a fold mistake here is immediately visible to a human, unlike a silent data-layer regression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-191-03-01 | Tampering (silent visual regression) | `cmd/codex_visuals.go`'s compiled maps | mitigate | Field-by-field diff table covers every caste/command/prefix entry, not a sample; before/after regression test asserts every accessor, not just `casteIdentity()`'s composed output |
| T-191-03-02 | Tampering (silent reintroduction) | `colony/ceremony/visuals.md` | mitigate | `TestVisualsConfigDoesNotReappear`, proven fail-then-pass |
| T-191-03-03 | Denial of Service (locked-format regression) | `TestCasteIdentityUsesHouseStyle` (pre-existing, house-locked format test) | mitigate | Explicitly re-run and required to pass unmodified as part of this task's acceptance criteria |

</threat_model>

<verification>
```bash
go build ./...
go vet ./...
go test ./cmd/ -run 'TestVisualsConfig|TestCasteIdentity|TestFileCaste' -v -count=1
go test ./cmd/... -count=1
```
</verification>

<success_criteria>
- `colony/ceremony/visuals.md` is deleted.
- Every field it defined that diverged from `cmd/codex_visuals.go`'s compiled defaults was folded in
  before deletion.
- Caste-identity rendering is proven byte-identical before and after, by Go-level regression test AND
  real dev-checkout CLI capture.
- A proven, fail-then-pass reappearance ratchet exists.
- The pre-existing `TestCasteIdentityUsesHouseStyle` still passes unmodified.
- Full `go test ./cmd/... -count=1` suite passes with zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/191-dead-wood/191-03-SUMMARY.md`, including the field-by-field
diff table and the before/after CLI capture diff.
Note: `.planning/` is gitignored but tracked -- new files there need `git add -f` or they silently
vanish from the commit.
</output>
