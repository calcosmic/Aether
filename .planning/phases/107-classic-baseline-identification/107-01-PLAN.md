---
phase: 107-classic-baseline-identification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/references/classic-baseline.md
autonomous: true
requirements:
  - BASE-01
  - BASE-03

must_haves:
  truths:
    - "All 16 Classic modules are documented with purpose, expected behavior, version presence, and 4-category classification"
    - "The comparison explains what changed between v5.3.0, v5.3.3, and v5.4.0"
    - "v5.4.0 is selected with rationale explaining why it is the Node-to-Go bridge"
    - "Known limitations of the selected version are listed"
  artifacts:
    - path: ".aether/references/classic-baseline.md"
      provides: "Classic baseline document with version comparison, selection rationale, behavioral checklist, and known limitations"
      contains: "## Selection Rationale"
  key_links:
    - from: ".aether/references/classic-baseline.md"
      to: ".aether/references/contracts/runtime-boundary-contract.md"
      via: "cross-reference to Phase 106 16-module classification table"
      pattern: "runtime-boundary-contract"
---

<objective>
Create the Classic Baseline document that compares v5.3.0, v5.3.3, and v5.4.0 with full behavioral evidence, selects v5.4.0 with rationale, and provides a per-module behavioral checklist with 4-category classification.

Purpose: This document is the behavior comparison anchor for the hybrid runtime milestone. Phase 108 (Golden Workflow Tests) and Phase 109 (TypeScript Orchestration Host) will reference the "Restore in TS" modules to know what the TS host must reimplement. Phase 110 (Go Safety) will reference "Keep in Go" modules to know what must remain unchanged.

Output: .aether/references/classic-baseline.md -- a comprehensive baseline document with version comparison, selection rationale, known limitations, and behavioral checklist for all 16 Classic modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-classic-baseline-identification/107-CONTEXT.md
@.planning/phases/107-classic-baseline-identification/107-RESEARCH.md
@.aether/references/contracts/runtime-boundary-contract.md
@.planning/phases/106-boundary-contract/106-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Classic Baseline Document with Version Comparison and Behavioral Checklist</name>
  <files>.aether/references/classic-baseline.md</files>
  <read_first>
    - .aether/references/contracts/runtime-boundary-contract.md (16-module classification table -- source of truth for classifications)
    - .planning/phases/107-classic-baseline-identification/107-RESEARCH.md (full version comparison data, module inventory, selection rationale, pitfalls)
    - .planning/phases/107-classic-baseline-identification/107-CONTEXT.md (user decisions D-01, D-02, D-03)
  </read_first>
  <action>
    Create .aether/references/classic-baseline.md with the following sections, in order:

    **Section 1: Header** -- Title "Classic Baseline Document", phase reference (Phase 107), date, selected tag (v5.4.0).

    **Section 2: Executive Summary** -- One paragraph stating v5.4.0 is selected as the Classic baseline because it is the only version with the Node-to-Go delegation bridge (version-gate.js + binary-downloader.js), has the complete 16-module set, and was the last production Classic release before the full Go transition.

    **Section 3: Version Comparison** -- Table with columns: Module, v5.3.0, v5.3.3, v5.4.0, Classification. Rows for all 16 modules. Use the exact data from 107-RESEARCH.md "Module Inventory" table. Below the table, add a prose subsection "What Changed Across Versions" explaining: (a) v5.3.0 and v5.3.3 are functionally identical (only 15 lines differ in update-transaction.js), (b) v5.4.0 adds binary-downloader.js and version-gate.js plus a 134-line delegation shim in cli.js, (c) this makes v5.4.0 the bridge between Node era and Go era. Per D-01 and D-03.

    **Section 4: Selection Rationale** -- Three numbered reasons: (1) Bridge architecture -- v5.4.0 contains the Node-to-Go delegation bridge that the hybrid runtime milestone restores, (2) Complete module set -- 16 modules vs 14, every Classic behavior covered, (3) Production endpoint -- last Classic release with accumulated fixes. Per D-03.

    **Section 5: Known Limitations** -- Five numbered items: (1) package.json reports "5.3.3" not "5.4.0" (metadata bug, use git tag), (2) delegation shim delegates to Go binary if installed (smoke test must isolate HOME), (3) plan/build/continue are slash commands only, not CLI subcommands, (4) requires Node.js + npm install before use, (5) commander npm dependency required. Each limitation must include a "Workaround" sub-bullet.

    **Section 6: Behavioral Checklist** -- Per D-02. For each of the 16 modules, create a subsection with: Purpose (one sentence), Expected Behavior (bullet list of observable behaviors), Versions Present (which tags), Classification (Restore in TS / Keep in Go / Obsolete / Reject as unsafe), Migration Notes (what replaces it in current Go runtime or what TS host must reimplement). Use the Phase 106 contract's classification as the authority -- do not reclassify. Group by classification: first "Restore in TS" modules, then "Keep in Go" modules, then "Obsolete" modules.

    The 16 modules and their classifications from the Phase 106 contract:
    - Restore in TS: spawn-logger.js, logger.js, errors.js
    - Keep in Go: state-guard.js, caste-colors.js, event-types.js, file-lock.js, banner.js, colors.js, init.js, binary-downloader.js, update-transaction.js, version-gate.js
    - Obsolete: state-sync.js, interactive-setup.js, nestmate-loader.js

    **Section 7: Cross-References** -- Link to runtime-boundary-contract.md, Phase 107 RESEARCH.md, and note that Phase 108 golden tests and Phase 109 TS host will use the "Restore in TS" classification.

    For the behavioral expectations per module, read the actual source code at each git tag to extract real function names and observable behaviors. Use `git show v5.4.0:bin/lib/{module}.js` to read each module. Do NOT fabricate function names -- extract them from the actual source.
  </action>
  <verify>
    <automated>grep -c "Selection Rationale\|Known Limitations\|Behavioral Checklist\|Version Comparison\|spawn-logger\|state-guard\|binary-downloader\|version-gate\|Restore in TS\|Keep in Go\|Obsolete" .aether/references/classic-baseline.md | grep -v '^0$' && echo "PASS: all sections present" || echo "FAIL: missing sections"</automated>
  </verify>
  <acceptance_criteria>
    - Document contains all 7 sections: Executive Summary, Version Comparison, Selection Rationale, Known Limitations, Behavioral Checklist, Cross-References
    - Version comparison table has exactly 16 module rows
    - Behavioral checklist has exactly 16 module subsections
    - Each module subsection contains Purpose, Expected Behavior, Versions Present, Classification, and Migration Notes
    - v5.4.0 is stated as the selected tag with rationale
    - All 5 known limitations are listed with workarounds
    - Classifications match the Phase 106 contract exactly (3 Restore in TS, 11 Keep in Go, 3 Obsolete)
    - Cross-references link to runtime-boundary-contract.md
    - No module function names are fabricated -- all extracted from actual source at git tags
  </acceptance_criteria>
  <done>Classic baseline document exists at .aether/references/classic-baseline.md with full version comparison of all 3 candidates, v5.4.0 selected with rationale, 5 known limitations, and behavioral checklist for all 16 modules with correct classifications matching Phase 106 contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This phase is documentation only. No code execution, no user input, no network access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | E (Elevation) | Baseline document | accept | Documentation artifact only -- no runtime authority |
</threat_model>

<verification>
- Baseline document exists with all required sections
- grep confirms all 16 modules are covered in behavioral checklist
- grep confirms selection rationale and known limitations present
- Classifications match Phase 106 contract (cross-reference check)
</verification>

<success_criteria>
- .aether/references/classic-baseline.md exists and is committed
- Document covers BASE-01 (version comparison with evidence) and BASE-03 (selected tag, rationale, limitations, behavioral checklist)
- All 16 modules have behavioral entries with correct 4-category classifications
- No fabricated content -- all module details extracted from actual git tag source
</success_criteria>

<output>
After completion, create .planning/phases/107-classic-baseline-identification/107-01-SUMMARY.md
</output>
