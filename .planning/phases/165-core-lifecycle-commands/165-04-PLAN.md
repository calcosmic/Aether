---
phase: 165-core-lifecycle-commands
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - cmd/plan_wrapper_ceremony_test.go
  - .claude/commands/ant/plan.md
  - .opencode/commands/ant/plan.md
  - .claude/commands/ant-plan.md
  - .aether/commands/plan.yaml
autonomous: true
requirements: [CMD-01, CMD-02]
must_haves:
  truths:
    - "A user reading plan.md can say what each stage does, what it reads, which workers it spawns, and when the planning loop stops"
    - "The planning loop's termination conditions are stated in prose — confidence target, stall detection, iteration cap, escape hatch — matching what the runtime enforces"
    - "The two-decision-moment contract from Phase 164 survives the rewrite intact"
    - "Adding a new stage heading to plan.md without a Purpose marker fails a test"
  artifacts:
    - path: ".claude/commands/ant/plan.md"
      provides: "Rewritten plan wrapper carrying method + Classic ceremony, both decision moments preserved"
      contains: "## Required Cross-Stage State"
    - path: ".opencode/commands/ant/plan.md"
      provides: "Byte-parity OpenCode mirror"
    - path: "cmd/plan_wrapper_ceremony_test.go"
      provides: "Extended contract: stage-skeleton density, structured blocks, stop-condition prose"
      exports: ["TestPlanWrapperCeremonyContract", "TestPlanWrapperStageSkeleton"]
    - path: ".aether/commands/plan.yaml"
      provides: "Guardrail source updated in lockstep"
  key_links:
    - from: "cmd/plan_wrapper_ceremony_test.go"
      to: ".claude/commands/ant/plan.md"
      via: "assertStageSkeletonDensity"
      pattern: "assertStageSkeletonDensity"
    - from: ".claude/commands/ant/plan.md"
      to: ".aether/docs/wrapper-host-contract.md"
      via: "single contract pointer replacing inline envelope-shape prose"
      pattern: "wrapper-host-contract.md"
---

<objective>
Rewrite `plan.md` (both platforms) onto the uniform D-05 stage skeleton with
Classic Queen-voice beats and restored stop-condition prose, without disturbing
the two-decision-moment contract Phase 164 established.

`plan.md` is already the closest of the four wrappers to the target shape — it
has quasi-stage headings and print-verbatim card discipline — so this is the
lowest-risk of the four rewrites. Its distinctive contribution is the
termination-condition prose (D-06): the v5.4.0 planning loop documented its
confidence threshold, stall detection, iteration cap, and escape hatch, and
today's wrapper documents only `requires_next_iteration`. The wrapper describes
what the runtime enforces; it never re-implements it, and it never invents a flag
that does not exist.

Purpose: a user can tell when planning will stop and why, before starting it.
Output: Rewritten plan wrapper on both platforms, resynced flat mirror, updated
YAML guardrails, extended test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-core-lifecycle-commands/165-CONTEXT.md
@.planning/phases/165-core-lifecycle-commands/165-ARCHAEOLOGY.md
@.planning/phases/165-core-lifecycle-commands/165-PATTERNS.md
@.planning/phases/165-core-lifecycle-commands/165-RESEARCH.md
@.planning/phases/164-research-feeds-planning/164-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Helpers created by Plan 01 in cmd/lifecycle_wrapper_contract_test.go. -->
  func canonicalWrapperPaths(repoRoot, verb string) []string
  func flatMirrorPath(repoRoot, verb string) string
  func countMarkerLines(text string, markers []string) int
  func stageSkeletonMarkers() []string
  func envelopeMechanicsMarkers() []string
  func assertOrderedHeadingParity(t *testing.T, verb, claudeText, opencodeText string)
  func assertStageSkeletonDensity(t *testing.T, path, text string, exemptHeadings []string)

<!-- Pre-existing package-level helpers -->
  func repoRootForCommandSourceTest() (string, error)
  func assertSubstringsInOrder(t *testing.T, path, content string, ordered []string)
  func stripCommentLines(text string) string
  func level2Headings(text string) []string

<!-- plan.md is asserted across THREE paths by TestPlanWrapperCeremonyContract:  -->
<!--   .claude/commands/ant/plan.md, .claude/commands/ant-plan.md (flat mirror), -->
<!--   .opencode/commands/ant/plan.md                                            -->
<!-- Ordered-heading parity is already covered by TestPlanWrapperCardsParity.    -->
<!-- Runtime keys both wrappers must reference: depth_proposal_card,             -->
<!--   research_proposal_card, research_awaiting_approval, research_warning      -->
<!-- Exactly-once counts enforced: plan-research-approve --approve-all,          -->
<!--   plan-research-approve --auto                                              -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the plan wrapper contract test with skeleton density, structured blocks, and stop-condition prose</name>
  <files>cmd/plan_wrapper_ceremony_test.go</files>
  <read_first>
    - `cmd/plan_wrapper_ceremony_test.go` (92 lines — the three-path `wrapperPaths` list including the flat mirror, 26 `required` substrings, 9 `inOrder` markers, 8 `forbidden` strings; all must survive unchanged)
    - `cmd/plan_wrapper_cards_test.go` (138 lines — already owns ordered-heading parity, the exactly-once approve-all/auto counts, the retired-heading fence, and the plan.yaml card-key assertion; this task must not duplicate any of them)
    - `cmd/lifecycle_wrapper_contract_test.go` (created by Plan 01 — the helper signatures this test calls)
    - `.claude/commands/ant/plan.md` (the current file, so the RED assertions are calibrated against real content)
    - `.planning/phases/165-core-lifecycle-commands/165-ARCHAEOLOGY.md` (§B's `## Auto-Termination Safeguards` quotes — the four stop-condition concepts to assert)
  </read_first>
  <behavior>
    - `TestPlanWrapperStageSkeleton` / `stage_skeleton_density`: every non-exempt level-2 heading in plan.md is followed within its section by `**Purpose:**`. RED today.
    - `TestPlanWrapperStageSkeleton` / `structured_blocks_present`: `<success_criteria>`, `<failure_modes>`, `<read_only>`, and `## Required Cross-Stage State` present in all three plan.md paths. RED today.
    - `TestPlanWrapperStageSkeleton` / `termination_conditions_documented`: the wrapper names the four loop stop conditions — a confidence target, stall detection, an iteration cap, and an escape hatch. RED today (only `requires_next_iteration` is documented).
    - `TestPlanWrapperStageSkeleton` / `method_outweighs_envelope_mechanics`: stage-skeleton marker lines are at least 3x envelope-mechanics marker lines. RED today.
    - `TestPlanWrapperCeremonyContract`: all 26 existing required substrings, 9 ordered markers, and 8 forbidden strings still assert, across all three paths.
  </behavior>
  <action>
    Extend `cmd/plan_wrapper_ceremony_test.go`. Preserve every existing
    assertion including the three-path `wrapperPaths` list (the flat
    `.claude/commands/ant-plan.md` mirror is already asserted here — keep it) and
    the two-decision-moment invariant `Do NOT add a third decision moment`.

    Add to the existing `required` slice: `## Required Cross-Stage State`,
    `**Purpose:**`, `**Reads:**`, `**Spawns:**`, `**Stop conditions:**`,
    `<success_criteria>`, `<failure_modes>`, `<read_only>`.

    Add `TestPlanWrapperStageSkeleton` with the four subtests named in the
    behavior block. Use the exempt-heading list
    `{"## Required Cross-Stage State", "## Cross-Platform Drift Guard", "## Guardrails"}`.
    Do not add an ordered-heading-parity subtest here — `TestPlanWrapperCardsParity`
    in `cmd/plan_wrapper_cards_test.go` already owns that invariant for plan.md
    and must not be duplicated; note that in a comment so a future reader does not
    add a second one.

    For `termination_conditions_documented`, assert the presence of four distinct
    stop-condition concepts rather than one long sentence: a target-confidence
    marker, a stall-detection marker, an iteration-cap marker, and an
    escape-hatch marker. Pick the four literal substrings to match whatever
    wording Task 2 writes, and choose substrings that name the concept
    (`target confidence`, `stall`, `max iteration`, `escape hatch`) rather than
    a specific numeric threshold — the wrapper must not carry threshold
    arithmetic, only describe that the runtime enforces one.
  </action>
  <verify>
    <automated>go vet ./cmd/ && go test ./cmd/... -run 'TestPlanWrapperCardsParity' && go test ./cmd/... -run TestPlanWrapperStageSkeleton/stage_skeleton_density 2>&1 | grep -q FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./cmd/` exits 0
    - `go test ./cmd/... -run TestPlanWrapperCardsParity` PASSES (unchanged and not duplicated)
    - `TestPlanWrapperStageSkeleton/stage_skeleton_density` FAILS naming at least one heading without a Purpose marker (intended RED)
    - `TestPlanWrapperStageSkeleton/termination_conditions_documented` FAILS (intended RED)
    - `git diff cmd/plan_wrapper_ceremony_test.go` shows no deletion from the existing required, ordered, or forbidden slices, and the three-path `wrapperPaths` list is unchanged
    - No new `level2Headings` parity assertion was added for plan.md
  </acceptance_criteria>
  <done>The plan wrapper's target structure and its missing termination-condition prose are both expressed as failing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite plan.md on both platforms with the D-05 stage skeleton, colony beats, and restored stop conditions</name>
  <files>.claude/commands/ant/plan.md, .opencode/commands/ant/plan.md</files>
  <read_first>
    - `.claude/commands/ant/plan.md` (141 lines — the file being rewritten; it is also the structural exemplar the other three wrappers were measured against, so its density is the reference bar)
    - `.opencode/commands/ant/plan.md` (currently byte-identical; gets the same rewrite)
    - `cmd/plan_wrapper_ceremony_test.go` (as extended by Task 1 — 26 required substrings, 9 ordered markers, 8 forbidden strings)
    - `cmd/plan_wrapper_cards_test.go` (the ordered-heading parity, exactly-once approve-all/auto counts, retired-heading fence `## Depth Ceremony` / `## Planning Depth`, and the plan.yaml card-key assertion)
    - `cmd/platform_doc_hygiene_test.go` (the plan.md entries near lines 87 and 401 — forbidden strings include `Update watch files for tmux visibility`, `Write COLONY_STATE.json`, `.aether/aether-utils.sh`)
    - `.planning/phases/165-core-lifecycle-commands/165-ARCHAEOLOGY.md` (§A-1 exemplar #7 — the Classic plan.md Queen opener; §B's `## Auto-Termination Safeguards` quotes at v5.4.0 plan.md:679-693, the source for the stop-condition prose; §D-3 forbids watch-file/tmux writes)
    - `.planning/phases/165-core-lifecycle-commands/165-PATTERNS.md` (plan.md section: the print-verbatim card idiom, the auto-termination text to modernize, and the warning not to invent a flag)
    - `.aether/docs/wrapper-host-contract.md` (as extended by Plan 01 — the pointer target)
  </read_first>
  <action>
    Rewrite both plan.md files. They must remain byte-identical to each other at
    the end of this task. Write one, then copy it to the other path.

    Stage skeleton (D-05). Keep the existing level-2 headings and their order —
    `## Decision Moment 1 — Depth Proposal`, `## Planning Manifest`,
    `## Decision Moment 2 — Research Batch`, `## Clarification Gate`,
    `## Runtime Spawn Ceremony`, `## Worker Spawning`, `## Finalize`,
    `## After Planning`, `## Cross-Platform Drift Guard`, `## Guardrails` — and
    insert `## Required Cross-Stage State` immediately after the file's opening
    prose and before `## Decision Moment 1 — Depth Proposal`. Under every heading
    except the three exempt ones, write in order: one colony beat line, then
    `**Purpose:**`, `**Reads:**`, `**Spawns:**` where the stage spawns anything,
    then the stage's imperative instructions and command blocks, then
    `**Stop conditions:**`.

    Colony beats (D-01, D-02). Open the file body with the Classic plan-era Queen
    opener — orchestrate research and planning until the selected confidence
    target is reached within the selected iteration budget — as one beat, then
    engineering substance. Keep the 🐜 and 👑 glyphs for beats. Write no literal
    separator rules and no reproduction of any runtime-rendered visual.

    Preserve the Phase 164 contract exactly. Both decision-moment cards are
    printed verbatim and never re-reasoned; there are exactly two decision
    moments and the `Do NOT add a third decision moment` guardrail stays; the
    four runtime keys `depth_proposal_card`, `research_proposal_card`,
    `research_awaiting_approval`, `research_warning` all remain referenced;
    `plan-research-approve --approve-all` and `plan-research-approve --auto` each
    appear exactly once. Do not reintroduce the retired headings
    `## Depth Ceremony` or `## Planning Depth`.

    Envelope mechanics (D-03, D-04, CMD-02). Under `## Planning Manifest` keep
    the manifest fetch command and the strings the existing test requires
    (`result.plan_manifest`, `result.planning_manifest`) but subordinate them to
    the stage's method as one line per mechanical step, followed by exactly one
    pointer sentence to `.aether/docs/wrapper-host-contract.md`. Remove prose
    that enumerates manifest field semantics beyond that — those field names now
    live in the contract doc. The contract reference must appear exactly once in
    the whole file.

    D-06 method assets. Add `## Required Cross-Stage State` listing
    `planning_run_id`, `iteration`, `target_confidence`, `manifest_file`,
    `selected_gaps`, `stop_reason`, `next_action` — current vocabulary only,
    never `colony_depth`. Add a `<success_criteria>` block (a completed plan
    with `requires_next_iteration` false, both decision moments answered, the
    Scout and Route-Setter dispatches from the manifest all run, closeout
    rendered), a `<failure_modes>` block (unresolved clarifications — route to
    `/ant-discuss`; boundary guidance active — route to `aether discuss` and
    request a fresh manifest; research batch unanswered — surface
    `result.research_warning` rather than shipping a silently unresearched plan;
    `requires_next_iteration` true — never claim the plan is complete), and a
    `<read_only>` block naming the state, session, planning-artifact, and
    pheromone files this wrapper may never write.

    Stop conditions (D-06 — the distinctive addition for this file). Add
    stop-condition prose describing what the planning loop actually enforces
    today: the loop exits when the selected target confidence is reached; stall
    detection ends a loop that stops improving; an iteration cap bounds the run;
    and an escape hatch accepts the current plan. Name the concepts and describe
    that the runtime enforces them. Do NOT write numeric thresholds, do NOT
    restate percentage arithmetic, and do NOT name a CLI flag you have not
    verified exists in the current runtime — the v5.4.0 `--accept` escape-hatch
    flag is historical text and must be checked against today's plan flow before
    being named. If no current flag matches, describe the escape hatch by
    behavior rather than inventing a flag name.

    Guardrails. Keep every existing guardrail bullet verbatim including the
    closing `If docs and runtime disagree, runtime wins.`
  </action>
  <verify>
    <automated>diff .claude/commands/ant/plan.md .opencode/commands/ant/plan.md && go test ./cmd/... -run 'TestPlanWrapperCeremonyContract|TestPlanWrapperCardsParity|TestPlanWrapperStageSkeleton|TestLifecycleCommandDocsPreferRuntimeCLI|TestLifecycleWrappersAvoidRetiredDepthVocabulary' && test "$(grep -c 'wrapper-host-contract.md' .claude/commands/ant/plan.md)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `diff .claude/commands/ant/plan.md .opencode/commands/ant/plan.md` produces no output
    - All tests named in the verify command pass
    - `grep -c 'wrapper-host-contract.md' .claude/commands/ant/plan.md` returns exactly 1
    - `grep -c 'plan-research-approve -- approve-all'` style counts: `plan-research-approve --approve-all` appears exactly once and `plan-research-approve --auto` appears exactly once
    - `grep -c -- '[*][*]Purpose:[*][*]' .claude/commands/ant/plan.md` returns at least 7
    - `grep -c 'colony_depth' .claude/commands/ant/plan.md` returns 0
    - `grep -cE 'Depth Ceremony|Planning Depth' .claude/commands/ant/plan.md` returns 0
    - The file contains `<success_criteria>`, `<failure_modes>`, `<read_only>`, `## Required Cross-Stage State`, and the four stop-condition concept markers asserted by Task 1
    - Every CLI flag named in the stop-condition prose is verifiable against the current runtime; no flag is introduced that `aether plan --help` or the current wrapper does not already support
  </acceptance_criteria>
  <done>Both platforms' plan.md carry the uniform stage skeleton and honest termination-condition prose, with the Phase 164 two-card contract intact.</done>
</task>

<task type="auto">
  <name>Task 3: Resync the plan flat mirror and update plan.yaml guardrails in lockstep</name>
  <files>.claude/commands/ant-plan.md, .aether/commands/plan.yaml</files>
  <read_first>
    - `.claude/commands/ant/plan.md` (the rewritten canonical file to mirror)
    - `.claude/commands/ant-plan.md` (the flat installed mirror, currently in sync and directly asserted by `TestPlanWrapperCeremonyContract`)
    - `.aether/commands/plan.yaml` (64 lines — must carry both `depth_proposal_card:` and `research_batch_card:` keys per `TestPlanWrapperCardsParity`)
    - `cmd/lifecycle_wrapper_contract_test.go` (the flat-mirror byte-equality invariant from Plan 01)
  </read_first>
  <action>
    Replace `.claude/commands/ant-plan.md` with the exact bytes of the rewritten
    `.claude/commands/ant/plan.md`. Straight copy, no edits. This mirror is
    directly asserted by `TestPlanWrapperCeremonyContract`'s three-path list, so
    a stale copy fails immediately.

    Update `.aether/commands/plan.yaml` in lockstep (D-11). Add a
    `wrapper_additions` entry named `stage_skeleton` describing the D-05 order
    and naming the `## Required Cross-Stage State` values `planning_run_id`,
    `iteration`, `target_confidence`, `manifest_file`, `selected_gaps`,
    `stop_reason`, `next_action`. Add a `wrapper_additions` entry named
    `termination_conditions` naming the four loop stop conditions by concept
    (target confidence reached, stall detected, iteration cap, escape hatch) with
    no numeric thresholds. Add one guardrail bullet forbidding hand-rendering any
    visual that `aether ceremony` renders. Do not remove or reword the existing
    `depth_proposal_card:` and `research_batch_card:` keys or any existing
    guardrail bullet.
  </action>
  <verify>
    <automated>diff .claude/commands/ant-plan.md .claude/commands/ant/plan.md && grep -q 'stage_skeleton' .aether/commands/plan.yaml && grep -q 'termination_conditions' .aether/commands/plan.yaml && grep -q 'depth_proposal_card:' .aether/commands/plan.yaml && grep -q 'research_batch_card:' .aether/commands/plan.yaml && go test ./cmd/... && go vet ./... && go build ./cmd/aether</automated>
  </verify>
  <acceptance_criteria>
    - `diff .claude/commands/ant-plan.md .claude/commands/ant/plan.md` produces no output
    - `.aether/commands/plan.yaml` contains `stage_skeleton:` and `termination_conditions:` under `wrapper_additions`
    - `.aether/commands/plan.yaml` still contains `depth_proposal_card:` and `research_batch_card:`
    - `.aether/commands/plan.yaml` gained exactly 1 new guardrail bullet and lost none
    - `go test ./cmd/...` passes in full
    - `go vet ./...` is clean and `go build ./cmd/aether` succeeds
  </acceptance_criteria>
  <done>The installed mirror and the YAML guardrail source both reflect the rewritten wrapper; no surface drifted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan.md prose → LLM orchestrator context | Every sentence is executed as instruction by the model conducting planning |
| decision-moment card → user | The card is runtime-composed; wrapper re-reasoning would present invented recommendations as runtime truth |
| plan.md → Scout worker permission profile | Scout writes under `.aether/data/phase-research`; a broadened profile would grant a research worker unscoped write access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-04-01 | Spoofing | Wrapper composing its own depth or research recommendation instead of printing the runtime card verbatim | mitigate | The print-verbatim idiom and the `Do NOT compose depth recommendations, reasons, or research recommendations in this wrapper` guardrail are preserved verbatim; `TestPlanWrapperCardsParity` asserts all four runtime keys stay referenced |
| T-165-04-02 | Tampering | Restored v5.4.0 prose reintroducing watch-file/tmux writes (D-3) | mitigate | `Update watch files for tmux visibility` is a forbidden string in `platform_doc_hygiene_test.go`; Task 2 read_first names D-3 explicitly and the `<read_only>` block records the no-write boundary in the wrapper itself |
| T-165-04-03 | Tampering | Wrapper state mutation of COLONY_STATE.json returning from the v5.4.0 retry ladder | mitigate | `Write COLONY_STATE.json` forbidden by the hygiene test; the existing `plan-finalize owns plan persistence` guardrail is preserved |
| T-165-04-04 | Elevation of Privilege | Restored spawn prose broadening Scout's `permission_profile` beyond the manifest value | mitigate | The pass-through-verbatim permission_profile instruction is preserved in `## Worker Spawning`; the contract doc's obligation column states the same rule |
| T-165-04-05 | Repudiation | Stop-condition prose naming a flag the runtime does not implement, making the documented escape hatch unverifiable | mitigate | Task 2 forbids naming an unverified flag and requires describing the escape hatch by behavior if no current flag matches; acceptance criteria make flag verification explicit |
| T-165-04-06 | Tampering | Threshold arithmetic re-entering markdown as part of the restored termination prose (D-6) | mitigate | Task 1's assertion targets concept markers, not numbers; Task 2 forbids numeric thresholds outright |
</threat_model>

<verification>
1. `go test ./cmd/...` passes in full.
2. `go vet ./...` clean; `go build ./cmd/aether` succeeds.
3. `.claude`, `.opencode`, and flat-mirror plan.md are all byte-identical.
4. `TestPlanWrapperCardsParity` passes unchanged, proving the Phase 164 two-card contract survived the rewrite.
</verification>

<success_criteria>
- Reading plan.md shows stage purpose, files-to-read guidance, spawn choreography, and stop conditions for every stage (CMD-01)
- The planning loop's four termination conditions are documented in prose without threshold arithmetic or invented flags (CMD-01)
- Envelope-shape detail lives in `wrapper-host-contract.md` with exactly one pointer from plan.md (CMD-02)
- The two-decision-moment contract and all four runtime card keys survive
</success_criteria>

<output>
After completion, create `.planning/phases/165-core-lifecycle-commands/165-04-SUMMARY.md`.
</output>
