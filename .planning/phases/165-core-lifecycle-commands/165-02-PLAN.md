---
phase: 165-core-lifecycle-commands
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - cmd/build_wrapper_ceremony_test.go
  - .claude/commands/ant/build.md
  - .opencode/commands/ant/build.md
  - .claude/commands/ant-build.md
  - .aether/commands/build.yaml
autonomous: true
requirements: [CMD-01, CMD-02, CMD-03, CMD-05]
must_haves:
  truths:
    - "A user reading build.md can say what each stage does, what it reads, and when it stops, without opening Go source"
    - "build.md opens each stage with a colony beat and follows it with engineering substance, not envelope-parsing instructions"
    - "build.md records what Phase 160 fixed before it and reserves the section Phase 168 appends after it, so the merge order is traceable in the file itself"
    - "Adding a new stage heading to build.md without a Purpose marker fails a test"
    - "A one-sided edit to either platform's build.md fails a test"
  artifacts:
    - path: ".claude/commands/ant/build.md"
      provides: "Rewritten build wrapper carrying method + Classic ceremony + D-08 ownership handshake"
      contains: "## Required Cross-Stage State"
    - path: ".opencode/commands/ant/build.md"
      provides: "Byte-parity OpenCode mirror of the rewritten build wrapper"
    - path: "cmd/build_wrapper_ceremony_test.go"
      provides: "Extended contract: stage-skeleton density, heading parity, D-08 ownership handshake"
      exports: ["TestBuildWrapperCeremonyContract", "TestBuildMdOwnershipHandshake", "TestBuildWrapperStageSkeletonAndParity"]
    - path: ".aether/commands/build.yaml"
      provides: "Guardrail source updated in lockstep with the wrapper"
  key_links:
    - from: "cmd/build_wrapper_ceremony_test.go"
      to: ".claude/commands/ant/build.md"
      via: "assertStageSkeletonDensity + assertOrderedHeadingParity from cmd/lifecycle_wrapper_contract_test.go"
      pattern: "assertStageSkeletonDensity"
    - from: ".claude/commands/ant/build.md"
      to: ".aether/docs/wrapper-host-contract.md"
      via: "single contract pointer replacing inline envelope-shape prose"
      pattern: "wrapper-host-contract.md"
---

<objective>
Rewrite `build.md` (both platforms) so its stages read as engineering method with
Classic Queen-voice ceremony, and pin the result with tests that fail when the
ceremony or the method is absent. This plan is the sole structural owner of
`build.md` for milestone v1.25 (CMD-05), and it records that ownership in the
file so the merge order with Phase 160 (before) and Phase 168 (after) is
traceable rather than assumed.

Ceremony has been "restored" seven times since v5.4.0 and evaporated each time.
The difference this time is that the restoration ships with assertions: stage
skeleton density, heading parity, and the ownership handshake are all failable.

Purpose: `/ant-build` becomes readable as a colony ritual and as an engineering
procedure at the same time, which is what the user asked for.
Output: Rewritten build wrapper on both platforms, resynced flat mirror, updated
YAML guardrails, three extended/new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-core-lifecycle-commands/165-CONTEXT.md
@.planning/phases/165-core-lifecycle-commands/165-ARCHAEOLOGY.md
@.planning/phases/165-core-lifecycle-commands/165-PATTERNS.md
@.planning/phases/165-core-lifecycle-commands/165-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Helpers created by Plan 01 in cmd/lifecycle_wrapper_contract_test.go. -->
<!-- Call these; do not reimplement. -->
  func canonicalWrapperPaths(repoRoot, verb string) []string
  func flatMirrorPath(repoRoot, verb string) string
  func countMarkerLines(text string, markers []string) int
  func stageSkeletonMarkers() []string
  func envelopeMechanicsMarkers() []string
  func assertOrderedHeadingParity(t *testing.T, verb, claudeText, opencodeText string)
  func assertStageSkeletonDensity(t *testing.T, path, text string, exemptHeadings []string)

<!-- Pre-existing package-level helpers -->
  func repoRootForCommandSourceTest() (string, error)
  func assertSubstringsInOrder(t *testing.T, path, content string, ordered []string)
  func mustExtractLineContaining(t *testing.T, content, marker string) string
  func stripCommentLines(text string) string
  func level2Headings(text string) []string

<!-- Runtime ceremony CLI — the ONLY four registered `aether ceremony` subcommands. -->
<!-- `skill-assignments` and `queen-frame` are internal Go functions, NOT CLI commands. -->
  aether ceremony spawn-plan      --workflow <build|plan|continue> --manifest-file <path>
  aether ceremony wave-start      --workflow <...> --manifest-file <path> --execution-wave <N>
  aether ceremony worker-complete --workflow <...> --worker-file <path>
  aether ceremony closeout        --workflow <...> --completion-file <path>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the build wrapper contract test with skeleton density, heading parity, and the D-08 ownership handshake</name>
  <files>cmd/build_wrapper_ceremony_test.go</files>
  <read_first>
    - `cmd/build_wrapper_ceremony_test.go` (188 lines — the 23 `required` substrings, 10 `inOrder` markers, 5 `forbidden` strings, plus `TestBriefPathReferencedAcrossAllFourSurfaces` and `TestBuildWrapperVerbatimBriefBulletsStayMirrored`; every one of these must survive unchanged)
    - `cmd/lifecycle_wrapper_contract_test.go` (created by Plan 01 — the helper signatures this test calls)
    - `cmd/plan_wrapper_cards_test.go` (the heading-parity and negative-control patterns being generalized)
    - `.claude/commands/ant/build.md` (the current file, so the RED assertions are calibrated against real content rather than assumed content)
    - `.planning/phases/165-core-lifecycle-commands/165-CONTEXT.md` (D-08's ownership handshake and D-09's preference for proportion over named-section greps)
  </read_first>
  <behavior>
    - `TestBuildMdOwnershipHandshake`: both canonical build.md files contain an HTML comment recording what Phase 160 fixed and the exact reserved marker `<!-- PHASE-168: visual-guidance trailer appends below this line -->`. RED today (neither exists).
    - `TestBuildWrapperStageSkeletonAndParity` / `stage_skeleton_density`: every non-exempt `## ` heading in build.md is followed, within its own section, by `**Purpose:**`. RED today (no Purpose markers exist).
    - `TestBuildWrapperStageSkeletonAndParity` / `ordered_heading_parity`: `.claude` and `.opencode` build.md have identical ordered level-2 heading lists. GREEN today (files are byte-identical); must stay green.
    - `TestBuildWrapperStageSkeletonAndParity` / `structured_blocks_present`: build.md contains `<success_criteria>`, `<failure_modes>`, and `<read_only>` blocks and the `## Required Cross-Stage State` heading. RED today.
    - `TestBuildWrapperCeremonyContract`: all 23 existing required substrings, 10 ordered markers, and 5 forbidden strings still assert. Extended with the colony-beat and stop-condition markers.
    - `TestBuildWrapperCeremonyContract` / forbidden: no wrapper-driven git mutation — `git stash`, `git add -A`, and `git commit` are absent (D-10 fence item 9). GREEN today; the risk is the mining pass, because the exact playbook text this plan tells executors to mine contains these verbatim.
  </behavior>
  <action>
    Extend `cmd/build_wrapper_ceremony_test.go`. Preserve every existing
    assertion — the 23 `required` substrings, the 10 `inOrder` markers, the 5
    `forbidden` strings, `TestBriefPathReferencedAcrossAllFourSurfaces`, and
    `TestBuildWrapperVerbatimBriefBulletsStayMirrored` — unchanged. This phase
    extends, never weakens.

    Add to the existing `required` slice in `TestBuildWrapperCeremonyContract`:
    `## Required Cross-Stage State`, `**Purpose:**`, `**Reads:**`,
    `**Stop conditions:**`, `<success_criteria>`, `<failure_modes>`,
    `<read_only>`, and the two colony-beat anchors `You DIRECTLY spawn multiple
    workers` and `Why this matters`.

    Add to the existing `forbidden` slice in `TestBuildWrapperCeremonyContract`
    the three wrapper-driven git-mutation strings `git stash`, `git add -A`, and
    `git commit`, with a comment above them citing D-10 regression-fence item 9
    (no wrapper-driven git stash or commit; checkpointing, if it ever returns, is
    runtime work). This is the one D-10 item with no existing test coverage
    anywhere in the codebase, and it is the item most exposed by this phase: the
    playbook text Task 2 instructs an executor to mine carries these instructions
    verbatim at `build-prep.md:292-300` (a `git stash push` checkpoint plus a
    `git stash pop` rollback), and `build-prep.md` is named in CONTEXT.md's
    canonical mining list, so an executor is being sent directly at it. All three
    strings have zero occurrences in both build.md files today, so the assertion
    is green on arrival and its whole value is catching a mining pass that copies
    a git command along with the colony beat sitting next to it.

    Add a new test `TestBuildMdOwnershipHandshake`. For each of the two canonical
    build.md paths, assert the raw file (do NOT strip comment lines here — these
    assertions are about comment lines) contains both:
    the substring `PHASE-160:` on an HTML comment line describing the narrow
    call-argument fixes Phase 160 merged first, and the exact literal
    `<!-- PHASE-168: visual-guidance trailer appends below this line -->`.
    Also assert the `PHASE-160:` comment appears before the `PHASE-168:` marker
    using `assertSubstringsInOrder`, and that the `PHASE-168:` marker is the last
    non-empty line of the file, so a future appender has an unambiguous insertion
    point. Add a doc comment on the test naming CMD-05 and the ownership chain
    (160 fixes calls first → 165 restructures → 168 appends a trailer).

    Add a new test `TestBuildWrapperStageSkeletonAndParity` with four subtests:
    `stage_skeleton_density` calling `assertStageSkeletonDensity` on each
    canonical path with the exempt-heading list
    `{"## Ownership Split", "## Required Cross-Stage State", "## Cross-Platform Drift Guard", "## Guardrails"}`;
    `ordered_heading_parity` calling `assertOrderedHeadingParity` with verb
    `build`; `structured_blocks_present` asserting the three XML-ish block
    markers and the state-carry heading in both files; and
    `method_outweighs_envelope_mechanics` asserting
    `countMarkerLines(text, stageSkeletonMarkers()) >= 3 * countMarkerLines(text, envelopeMechanicsMarkers())`
    with a failure message quoting both counts. That last subtest is the
    proportion assertion CLAUDE.md prefers: it survives a rename of any
    individual section.
  </action>
  <verify>
    <automated>go vet ./cmd/ && go test ./cmd/... -run 'TestBuildMdOwnershipHandshake|TestBuildWrapperStageSkeletonAndParity' 2>&1 | grep -qE 'PHASE-168|Purpose|success_criteria' && go test ./cmd/... -run TestBuildWrapperStageSkeletonAndParity/ordered_heading_parity</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./cmd/` exits 0
    - `go test ./cmd/... -run TestBuildMdOwnershipHandshake` FAILS, output naming the missing `PHASE-168` marker (intended RED)
    - `go test ./cmd/... -run TestBuildWrapperStageSkeletonAndParity/stage_skeleton_density` FAILS naming at least one heading without a Purpose marker (intended RED)
    - `go test ./cmd/... -run TestBuildWrapperStageSkeletonAndParity/ordered_heading_parity` PASSES (parity holds today and must not be broken by the test itself)
    - `git diff cmd/build_wrapper_ceremony_test.go` shows no deletion from the existing `required`, `inOrder`, or `forbidden` slices — only additions and new functions
    - `TestBriefPathReferencedAcrossAllFourSurfaces` and `TestBuildWrapperVerbatimBriefBulletsStayMirrored` are byte-unchanged
    - The `forbidden` slice gained exactly the three entries `git stash`, `git add -A`, `git commit`, carrying a comment that cites D-10 item 9
    - `go test ./cmd/... -run TestBuildWrapperCeremonyContract` reports no failure attributable to the three new git entries (they are green on arrival), confirmed by `grep -cE 'git stash|git add -A|git commit' .claude/commands/ant/build.md .opencode/commands/ant/build.md` returning 0 for both paths
  </acceptance_criteria>
  <done>The build wrapper's target structure is now expressed as failing tests; the rewrite in Task 2 has an executable definition of done.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite build.md on both platforms with the D-05 stage skeleton, Classic colony beats, and the D-08 ownership handshake</name>
  <files>.claude/commands/ant/build.md, .opencode/commands/ant/build.md</files>
  <read_first>
    - `.claude/commands/ant/build.md` (151 lines — the file being rewritten; every existing heading, command string, and guardrail must be accounted for)
    - `.opencode/commands/ant/build.md` (currently byte-identical to the above; both get the same rewrite)
    - `cmd/build_wrapper_ceremony_test.go` (as extended by Task 1 — the exact required/ordered/forbidden strings that constrain the rewrite)
    - `cmd/platform_doc_hygiene_test.go` (the build.md entry near line 197 and the `.opencode` build.md entry near line 509 — forbidden strings `Briefly name the castes the colony is likely to send`, `such as Builder, Watcher, Scout, Architect, Oracle, or Chaos`, `Read the file with the Read tool.`, `Read \`.aether/data/COLONY_STATE.json\`.`, `Write COLONY_STATE.json`, `.aether/aether-utils.sh`)
    - `.planning/phases/165-core-lifecycle-commands/165-ARCHAEOLOGY.md` (§A-1 Queen voice exemplars #1 and #4/#5, §B's Required Cross-Stage State block and stop-condition prose, §C delta table ownership column, §D regression fence)
    - `.planning/phases/165-core-lifecycle-commands/165-PATTERNS.md` (build.md section: frontmatter shape, Ownership Split table, ceremony invocation shape, sanitized-availability sentence, guardrail block shape)
    - `.aether/docs/wrapper-host-contract.md` (as extended by Plan 01 — the pointer target)
    - `.aether/docs/wrapper-runtime-ux-contract.md` (the four anti-patterns; §139-150 forbid wrapper state mutation and duplicated gating)
  </read_first>
  <action>
    Rewrite both build.md files. They must remain byte-identical to each other at
    the end of this task. Write one, then copy it to the other path; do not hand-
    edit the second.

    **Ownership handshake (D-08, CMD-05).** Immediately after the existing
    `<!-- Aether-managed: ... -->` line, add an HTML comment recording the
    ownership chain: that Phase 160 (Fail Loudly) merged first and made only
    narrow call-argument fixes, that Phase 165 is the sole structural owner of
    this file for milestone v1.25, and that Phase 168 appends a visual-guidance
    trailer afterward. Use the token `PHASE-160:` in that comment. As the final
    non-empty line of the file, place exactly
    `<!-- PHASE-168: visual-guidance trailer appends below this line -->`.

    **Stage skeleton (D-05).** Keep the existing level-2 heading set and its
    order exactly as the `inOrder` assertion requires — `## Ownership Split`,
    `## Colony Context`, `## Active Signals`, `## Phase Framing`,
    `## Dispatch Manifest`, `## Guided Boundary Gate`, `## Runtime Spawn
    Ceremony`, `## Worker Spawning`, `## Finalize`, `## After the Build`,
    `## Verification Depth`, `## Cross-Platform Drift Guard`, `## Guardrails` —
    and insert `## Required Cross-Stage State` after `## Ownership Split`.
    Under every heading except the four exempt ones, write in this order: one
    colony beat line (a single sentence opening with 🐜 or 👑 in the Classic
    register), then `**Purpose:**`, then `**Reads:**`, then `**Spawns:**` where
    the stage spawns anything, then the stage's imperative instructions and
    command blocks, then `**Stop conditions:**`. Align the heading names with the
    runtime's `── Stage Name ──` markers where they correspond (Context, Tasks,
    Dispatch, Verification, Housekeeping, Next Phase) but do not rename a heading
    that an existing assertion pins.

    **Colony beats to restore (D-01, D-02).** Open the file body with the Classic
    Queen opener including the literal phrase `You DIRECTLY spawn multiple
    workers` and the no-Prime-Worker rule stated as colony law. At the Worker
    Spawning stage, restore the rule-with-reason idiom under a `Why this matters`
    line explaining in colony terms why the Queen spawns directly and why one
    agent doing everything is not a colony — reworded from the archaeology
    exemplar, not copy-pasted wholesale (the surrounding playbook block contains
    forbidden content). Keep 🐜 and 👑 glyphs for beats. Do NOT write any literal
    `━━━` or `────` separator block, any caste emoji legend, or any reproduction
    of a spawn-plan / wave-start / worker-complete / closeout visual: 20 of the
    32 Classic ceremony elements are Go-renderer-owned and the wrapper's only job
    is to invoke the renderer and narrate around it.

    **Envelope mechanics (D-03, D-04, CMD-02).** Under `## Dispatch Manifest`,
    keep the manifest fetch command and the strings the existing test requires
    (`aether host build --dry-run`, `result.manifest.dispatch_manifest`,
    ``temporary manifest file outside `.aether/data/` ``) but subordinate them to
    the stage's method: one line per mechanical step, framed as the colony's
    marching order, followed by exactly one pointer sentence to
    `.aether/docs/wrapper-host-contract.md` for the full field shapes. Remove any
    prose that enumerates or explains manifest field semantics beyond that; those
    now live in the contract doc. The `.aether/docs/wrapper-host-contract.md`
    reference must appear exactly once in the whole file. Keep the terminal
    structured result bullet and the `handoff` object bullet inline — per the
    Plan 01 ruling they are method (what a worker owes back), not envelope
    mechanics. Keep the `brief` / `brief_path` / `context_capsule` /
    `skill_section` / `permission_profile` instructions inline as spawn method.

    **Byte-exact mirrored lines.** The line containing ``Each dispatch carries
    `brief` `` and the line containing `The worker's prompt =` are compared
    character-for-character between platforms by
    `TestBuildWrapperVerbatimBriefBulletsStayMirrored`. If you reword either, the
    reworded line must be identical in both files. `brief_path` and a
    case-insensitive `verbatim` must both survive in this file per
    `TestBriefPathReferencedAcrossAllFourSurfaces`.

    **D-06 method assets.** Add a `## Required Cross-Stage State` section listing
    the values carried across stages using current vocabulary only: `phase_id`,
    `verification_depth`, `manifest_file`, `context_capsule`, `wave_results`,
    `verification_status`, `next_action`. The retired `colony_depth` name and the
    four-value `light, standard, deep, full` enum are forbidden and asserted
    against by `TestLifecycleWrappersAvoidRetiredDepthVocabulary`. Add a
    `<success_criteria>` block (what a finished build looks like: every manifest
    dispatch spawned, every terminal result collected with a non-empty handoff,
    `build-finalize` run on the Go-owned completion path, closeout rendered), a
    `<failure_modes>` block (wave failure mid-build — do not continue to the next
    wave because failed dependencies cascade; provider dispatch unavailable —
    surface the sanitized availability message and stop; boundary guidance active
    — route to `aether discuss` and request a fresh manifest), and a
    `<read_only>` block (the files and commands this wrapper may read but never
    write: colony state, session files, pheromone files).

    **Stop conditions (D-06).** Every non-exempt stage ends with
    `**Stop conditions:**`. Describe what the runtime already enforces — do not
    invent thresholds, do not restate gate arithmetic, do not add a retry ladder.

    **Guardrails.** Keep every existing guardrail bullet verbatim, including the
    closing `If docs and runtime disagree, runtime wins.`
  </action>
  <verify>
    <automated>diff .claude/commands/ant/build.md .opencode/commands/ant/build.md && go test ./cmd/... -run 'TestBuildWrapperCeremonyContract|TestBuildMdOwnershipHandshake|TestBuildWrapperStageSkeletonAndParity|TestBriefPathReferencedAcrossAllFourSurfaces|TestBuildWrapperVerbatimBriefBulletsStayMirrored|TestLifecycleCommandDocsPreferRuntimeCLI|TestLifecycleWrappersAvoidRetiredDepthVocabulary' && test "$(grep -c 'wrapper-host-contract.md' .claude/commands/ant/build.md)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `diff .claude/commands/ant/build.md .opencode/commands/ant/build.md` produces no output
    - All tests named in the verify command pass
    - `grep -c 'wrapper-host-contract.md' .claude/commands/ant/build.md` returns exactly 1
    - `grep -c -- '\*\*Purpose:\*\*' .claude/commands/ant/build.md` returns at least 9
    - The last non-empty line of the file is exactly `<!-- PHASE-168: visual-guidance trailer appends below this line -->`
    - `grep -c 'PHASE-160:' .claude/commands/ant/build.md` returns at least 1, and that line appears before the PHASE-168 marker
    - `grep -cE '━━━|────' .claude/commands/ant/build.md` returns 0 (no hand-rendered runtime visuals)
    - `grep -c 'colony_depth' .claude/commands/ant/build.md` returns 0
    - The file contains `<success_criteria>`, `<failure_modes>`, `<read_only>`, and `## Required Cross-Stage State`
    - `grep -v '^<!--' .claude/commands/ant/build.md | grep -c 'You DIRECTLY spawn multiple workers'` returns at least 1
  </acceptance_criteria>
  <done>Both platforms' build.md carry stage purpose, files-to-read guidance, spawn choreography, and stop conditions wrapped in Classic Queen voice, with the ownership chain recorded in the file.</done>
</task>

<task type="auto">
  <name>Task 3: Resync the build flat mirror and update build.yaml guardrails in lockstep</name>
  <files>.claude/commands/ant-build.md, .aether/commands/build.yaml</files>
  <read_first>
    - `.claude/commands/ant/build.md` (the rewritten canonical file to mirror)
    - `.claude/commands/ant-build.md` (the flat installed mirror, resynced by Plan 01 and now stale again)
    - `.aether/commands/build.yaml` (58 lines — `wrapper_additions.pre_build` / `manifest_fetch` / `post_build` and the `guardrails` list must stay consistent with the wrapper per D-11)
    - `cmd/lifecycle_wrapper_contract_test.go` (the flat-mirror byte-equality invariant)
  </read_first>
  <action>
    Replace `.claude/commands/ant-build.md` with the exact bytes of the rewritten
    `.claude/commands/ant/build.md`. Straight copy, no edits.

    Update `.aether/commands/build.yaml` so its guardrail source stays in lockstep
    with the wrapper (D-11). Specifically: add a `wrapper_additions` entry named
    `stage_skeleton` describing the D-05 order (colony beat → Purpose → Reads →
    Spawns → Stop conditions) and naming the `## Required Cross-Stage State`
    values `phase_id`, `verification_depth`, `manifest_file`, `context_capsule`,
    `wave_results`, `verification_status`, `next_action`; add a
    `wrapper_additions` entry named `ownership_chain` recording the
    160-before / 165-owns / 168-appends order and the reserved
    `PHASE-168` trailer marker; and add two guardrail bullets — one forbidding
    hand-rendering any visual that `aether ceremony` renders (spawn plans, wave
    starts, worker completions, closeouts, caste legends), and one forbidding
    reintroduction of the retired `colony_depth` four-value vocabulary. Do not
    delete or reword any existing guardrail bullet or `wrapper_additions` entry.
    Keep the existing `drift_guard` line unchanged.
  </action>
  <verify>
    <automated>diff .claude/commands/ant-build.md .claude/commands/ant/build.md && go test ./cmd/... && go vet ./... && go build ./cmd/aether && grep -q 'stage_skeleton' .aether/commands/build.yaml && grep -q 'ownership_chain' .aether/commands/build.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `diff .claude/commands/ant-build.md .claude/commands/ant/build.md` produces no output
    - `.aether/commands/build.yaml` contains the keys `stage_skeleton:` and `ownership_chain:` under `wrapper_additions`
    - `.aether/commands/build.yaml` still contains its original `pre_build:`, `manifest_fetch:`, `post_build:`, `drift_guard:` entries and all 16 original guardrail bullets
    - `.aether/commands/build.yaml` gained exactly 2 new guardrail bullets
    - `go test ./cmd/...` passes in full
    - `go vet ./...` is clean and `go build ./cmd/aether` succeeds
  </acceptance_criteria>
  <done>The installed mirror and the YAML guardrail source both reflect the rewritten wrapper; no surface drifted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build.md prose → LLM orchestrator context | Every sentence is executed as instruction by the model conducting a build |
| mined playbook text → build.md | Classic source text sits adjacent to forbidden mechanisms in the same playbook blocks |
| build.md → spawned worker prompt | Wrapper instructions determine what a worker receives and what permissions it claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-02-01 | Tampering | Prompt-injection-shaped text carried in from mined playbook prose (D-8 scar: `build-prep.md:29-36` quoted a verbatim user transcript as a worked example) | mitigate | Task 2 action forbids copying whole playbook blocks and requires sentence-by-sentence mining against the D-10 fence; the Context Confirmation Rule is restored as neutral instruction only, never as quoted user text |
| T-165-02-02 | Elevation of Privilege | Rewrite weakening the `permission_profile` instruction so a `repository_read_only` worker gains write scope | mitigate | Task 2 retains the permission_profile bullet inline as spawn method; `TestLifecycleCommandDocsPreferRuntimeCLI` and the acceptance criteria keep it asserted |
| T-165-02-03 | Tampering | Regression: reintroducing the playbook Read-loader (`Read the file with the Read tool.`) while restoring Classic text | mitigate | Already forbidden by `TestBuildWrapperCeremonyContract` and `platform_doc_hygiene_test.go`; both run in Task 2's verify, and the CRITICAL D-1 fence is named in read_first |
| T-165-02-04 | Tampering | Regression: wrapper state mutation prose (`Write COLONY_STATE.json`) reappearing from v5.4.0 init/plan-era text | mitigate | Forbidden strings asserted by `platform_doc_hygiene_test.go`; the `<read_only>` block Task 2 adds makes the no-write boundary explicit in the wrapper itself |
| T-165-02-05 | Information Disclosure | Restored narration exposing raw provider stdout/stderr, tokens, or auth probe output | mitigate | The sanctioned sanitized-availability sentence ("provider, sanitized cause, and next action; no raw provider stdout, stderr, tokens, or auth probe output") is preserved verbatim and moved under the Dispatch Manifest stop conditions |
| T-165-02-06 | Spoofing | Speculative caste naming in prose letting the model invent workers not in the manifest | mitigate | `grep -cE '━━━\|────'` returning 0 plus the existing forbidden caste-legend strings; the `Do NOT invent worker names, castes, or waves` guardrail is preserved verbatim |
| T-165-02-07 | Repudiation | build.md restructured by a second phase with no record of the order | mitigate | D-08 handshake: `PHASE-160:` record + reserved `PHASE-168:` trailer marker, pinned by `TestBuildMdOwnershipHandshake` (CMD-05) |
| T-165-02-08 | Tampering | D-10 item 9 — wrapper-driven `git stash` / `git add -A && git commit` carried in from `build-prep.md:292-300` during the mining pass (that file is in CONTEXT.md's canonical mining list), letting wrapper prose mutate the user's working tree | mitigate | Task 1 adds `git stash`, `git add -A`, and `git commit` to the build wrapper's `forbidden` slice with a D-10 item 9 citation — the only D-10 item that had no test coverage anywhere before this phase |
</threat_model>

<verification>
1. `go test ./cmd/...` passes in full.
2. `go vet ./...` clean; `go build ./cmd/aether` succeeds.
3. `.claude`, `.opencode`, and flat-mirror build.md are all byte-identical.
4. Manual read-through of `.claude/commands/ant/build.md` top to bottom: for each stage, its purpose and the worker's inputs are statable in one sentence without consulting `cmd/*.go` (CMD-03 manual verification, recorded in 165-VALIDATION.md).
</verification>

<success_criteria>
- Reading build.md shows stage purpose, files-to-read guidance, spawn choreography, and stop conditions, with Classic Queen voice framing each stage (CMD-01, CMD-03)
- Envelope-shape detail lives in `wrapper-host-contract.md` with exactly one pointer from build.md (CMD-02)
- The Phase 160 → 165 → 168 ownership chain is recorded in the file and asserted by a test (CMD-05)
- Adding a stage without a Purpose marker, or editing one platform's build.md alone, fails a test
</success_criteria>

<output>
After completion, create `.planning/phases/165-core-lifecycle-commands/165-02-SUMMARY.md`.
The commit message for Task 2 must state the ownership chain explicitly:
Phase 160 fixed call arguments first, Phase 165 owns the structural rewrite,
Phase 168 appends the visual-guidance trailer.
</output>
