---
phase: 165-core-lifecycle-commands
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - cmd/lifecycle_wrapper_contract_test.go
  - .planning/phases/165-core-lifecycle-commands/165-VALIDATION.md
autonomous: false
requirements: [CMD-01, CMD-02, CMD-03, CMD-04]
must_haves:
  truths:
    - "Across all four lifecycle wrappers, method prose measurably outweighs envelope-parsing prose — asserted as a proportion, not as a list of named sections"
    - "All four wrappers carry the three structured blocks and the named cross-stage state-carry contract"
    - "Editing any of the eight specialist and delight command surfaces fails a test unless the change is deliberate"
    - "A person has read build.md top to bottom and confirmed each stage is describable without opening Go source"
  artifacts:
    - path: "cmd/lifecycle_wrapper_contract_test.go"
      provides: "Phase-level cross-wrapper invariants and the specialist-command fence"
      exports: ["TestLifecycleWrappersDoNotParseEnvelopeAsPrimaryJob", "TestLifecycleWrappersCarryStructuredBlocks", "TestSpecialistCommandSurfacesUnchanged"]
    - path: ".planning/phases/165-core-lifecycle-commands/165-VALIDATION.md"
      provides: "Completed per-task verification map with real Task IDs and test names"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "cmd/lifecycle_wrapper_contract_test.go"
      to: "all eight canonical lifecycle wrapper files"
      via: "proportion assertion over stage-skeleton vs envelope-mechanics marker lines"
      pattern: "TestLifecycleWrappersDoNotParseEnvelopeAsPrimaryJob"
    - from: "cmd/lifecycle_wrapper_contract_test.go"
      to: "chaos, archaeology, dream, oracle, swarm, sage, colonize, council surfaces"
      via: "SHA-256 content ledger"
      pattern: "TestSpecialistCommandSurfacesUnchanged"
---

<objective>
Close the phase with the assertions that can only be written once all four
wrappers exist in their rewritten form: the cross-wrapper CMD-02 proportion
invariant, the CMD-01 structural-block invariant, the CMD-04 specialist-command
fence, and the one manual verification (CMD-03) that is honestly not
mechanizable.

The project's own history is why this is its own wave. Ceremony has been declared
restored seven times since v5.4.0 and evaporated each time, and the milestone
audit's finding was blunt: a claim in a SUMMARY is not a requirement satisfied.
The tests written here are the ones that fail when the work is undone by a future
phase, which is the project's Definition of Done.

Purpose: make Phase 165 restoration number eight only if a test lets it be.
Output: Three phase-level tests, a completed validation ledger, and one human
read-through checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-core-lifecycle-commands/165-CONTEXT.md
@.planning/phases/165-core-lifecycle-commands/165-VALIDATION.md
@.planning/phases/165-core-lifecycle-commands/165-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Helpers created by Plan 01 in cmd/lifecycle_wrapper_contract_test.go; -->
<!-- this plan extends that same file. -->
  var lifecycleWrapperVerbs = []string{"init", "plan", "build", "continue"}
  func canonicalWrapperPaths(repoRoot, verb string) []string
  func flatMirrorPath(repoRoot, verb string) string
  func countMarkerLines(text string, markers []string) int
  func stageSkeletonMarkers() []string
  func envelopeMechanicsMarkers() []string
  func assertOrderedHeadingParity(t *testing.T, verb, claudeText, opencodeText string)
  func assertStageSkeletonDensity(t *testing.T, path, text string, exemptHeadings []string)

<!-- CMD-04 surfaces, verified by directory listing during planning.            -->
<!-- Seven of the eight named commands have wrapper files on both platforms:    -->
<!--   .claude/commands/ant/{chaos,archaeology,dream,oracle,swarm,colonize,council}.md   -->
<!--   .opencode/commands/ant/{chaos,archaeology,dream,oracle,swarm,colonize,council}.md -->
<!-- `sage` has NO slash command on any platform. Its surface is the agent      -->
<!-- definition trio:                                                           -->
<!--   .claude/agents/ant/aether-sage.md                                        -->
<!--   .opencode/agents/aether-sage.md                                          -->
<!--   .codex/agents/aether-sage.toml                                           -->
<!-- `aether command-guide <verb>` exists for archaeology, chaos, colonize,     -->
<!-- council, dream, oracle, swarm — but NOT for sage.                          -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the cross-wrapper CMD-01 and CMD-02 invariants</name>
  <files>cmd/lifecycle_wrapper_contract_test.go</files>
  <read_first>
    - `cmd/lifecycle_wrapper_contract_test.go` (created by Plan 01 — the helper signatures and the three tests already there, none of which may be weakened)
    - `.claude/commands/ant/build.md` (as rewritten by Plan 02 — the marker density this test measures)
    - `.claude/commands/ant/continue.md` (as rewritten by Plan 03)
    - `.claude/commands/ant/plan.md` (as rewritten by Plan 04)
    - `.claude/commands/ant/init.md` (as rewritten by Plan 05 — the only wrapper with zero contract-pointer references, because it has no host-manifest step)
    - `cmd/plan_wrapper_cards_test.go` (the `a_one_sided_heading_addition_would_fail` negative-control pattern to mirror)
    - `CLAUDE.md` (the Definition of Done section, and its stated preference for proportion or invariant assertions over section-exists greps — `TestBuildWorkerBriefIsMostlyTask` is the named exemplar)
  </read_first>
  <behavior>
    - `TestLifecycleWrappersDoNotParseEnvelopeAsPrimaryJob`: for each of the eight canonical wrapper files, stage-skeleton marker lines outnumber envelope-mechanics marker lines by at least 3 to 1. GREEN after Waves 1 and 2; fails if a future edit re-inflates envelope prose or strips method prose.
    - Subtest `contract_pointer_is_singular`: build, plan and continue each reference the wrapper-host contract doc exactly once; init references it zero times.
    - Subtest `a_wrapper_dominated_by_envelope_prose_would_fail`: a synthetic in-memory body with one Purpose line and five envelope-mechanics lines is rejected by the ratio check.
    - `TestLifecycleWrappersCarryStructuredBlocks`: all eight canonical files contain the three structured block markers and the state-carry heading.
    - Subtest `state_carry_uses_current_vocabulary`: each state-carry section names at least four backticked values and none of the retired v5.4.0 names.
  </behavior>
  <action>
    Extend `cmd/lifecycle_wrapper_contract_test.go`. Do not modify or weaken the
    three tests Plan 01 already placed there.

    Add `TestLifecycleWrappersDoNotParseEnvelopeAsPrimaryJob`. For each verb in
    `lifecycleWrapperVerbs` and each canonical path, compute a method count with
    `countMarkerLines(text, stageSkeletonMarkers())` and an envelope count with
    `countMarkerLines(text, envelopeMechanicsMarkers())`, then fail unless the
    method count is at least three times the envelope count. The failure message
    must quote both counts and the path and state the requirement in CMD-02's own
    terms: the wrapper's primary job is method, not envelope parsing. Add a doc
    comment explaining why this is a ratio rather than a forbidden-string list —
    a forbidden-string list is evaded by rewording, whereas a ratio fails
    whenever envelope prose grows back regardless of the words chosen. Cite
    `TestBuildWorkerBriefIsMostlyTask` as the pattern precedent named in
    CLAUDE.md.

    Add the `contract_pointer_is_singular` subtest asserting exactly one
    occurrence of the string `.aether/docs/wrapper-host-contract.md` in each
    canonical build, plan and continue file, and exactly zero occurrences in each
    canonical init file. The failure message must explain that D-03 puts envelope
    mechanics in the contract doc referenced once per wrapper, and that init has
    no host-manifest step at all.

    Add the negative-control subtest `a_wrapper_dominated_by_envelope_prose_would_fail`.
    Build a synthetic string in memory containing one `**Purpose:**` line and
    five lines each matching one of the envelope-mechanics markers, then assert
    the ratio check rejects it. This mirrors the
    `a_one_sided_heading_addition_would_fail` subtest in
    `cmd/plan_wrapper_cards_test.go` and proves the assertion has teeth.

    Add `TestLifecycleWrappersCarryStructuredBlocks` asserting the markers
    `<success_criteria>`, `<failure_modes>`, `<read_only>` and
    `## Required Cross-Stage State` in all eight canonical files. Add the
    `state_carry_uses_current_vocabulary` subtest: extract the section running
    from `## Required Cross-Stage State` to the next level-2 heading, assert it
    names at least four backticked values, and assert it contains none of
    `colony_depth`, `visual_mode`, `verbose_mode`, `suggest_enabled`, or
    `synthesis_status` — the v5.4.0 names the D-06 modernization replaced.
  </action>
  <verify>
    <automated>go vet ./cmd/ && go test ./cmd/... -run 'TestLifecycleWrappersDoNotParseEnvelopeAsPrimaryJob|TestLifecycleWrappersCarryStructuredBlocks' -v 2>&1 | grep -c '^    --- PASS' | grep -qvx 0 && go test ./cmd/... -run 'TestLifecycleWrappersDoNotParseEnvelopeAsPrimaryJob|TestLifecycleWrappersCarryStructuredBlocks'</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./cmd/` exits 0
    - `go test ./cmd/... -run TestLifecycleWrappersDoNotParseEnvelopeAsPrimaryJob` passes for all eight canonical files
    - `go test ./cmd/... -run TestLifecycleWrappersCarryStructuredBlocks` passes for all eight canonical files
    - The subtest `a_wrapper_dominated_by_envelope_prose_would_fail` exists and passes, proving the ratio assertion rejects an envelope-dominated body
    - The subtest `contract_pointer_is_singular` passes, with init asserted at zero occurrences
    - `git diff cmd/lifecycle_wrapper_contract_test.go` shows only additions — the three Plan 01 tests and all helper signatures are unchanged
  </acceptance_criteria>
  <done>CMD-01's structural obligations and CMD-02's primary-job obligation are now measured as invariants that survive rewording and renaming.</done>
</task>

<task type="auto">
  <name>Task 2: Fence the eight specialist and delight command surfaces (CMD-04)</name>
  <files>cmd/lifecycle_wrapper_contract_test.go</files>
  <read_first>
    - `cmd/lifecycle_wrapper_contract_test.go` (the file being extended)
    - `.claude/commands/ant/chaos.md`, `archaeology.md`, `dream.md`, `oracle.md`, `swarm.md`, `colonize.md`, `council.md` (seven of the eight named CMD-04 surfaces on the Claude side)
    - `.opencode/commands/ant/chaos.md`, `archaeology.md`, `dream.md`, `oracle.md`, `swarm.md`, `colonize.md`, `council.md` (the OpenCode mirrors)
    - `.claude/agents/ant/aether-sage.md` (sage's actual surface — there is no `/ant-sage` slash command on any platform)
    - `cmd/platform_doc_hygiene_test.go` (already covers `oracle.md`, `colonize.md`, `swarm.md`, `council.md` with required and forbidden lists — this new test complements it, it does not replace it)
  </read_first>
  <action>
    Add `TestSpecialistCommandSurfacesUnchanged` to
    `cmd/lifecycle_wrapper_contract_test.go`.

    Declare a package-level slice `specialistCommandSurfaces` holding the
    repo-relative paths of all seventeen files: the seven Claude wrappers, the
    seven OpenCode wrappers, and sage's three agent definitions
    (`.claude/agents/ant/aether-sage.md`, `.opencode/agents/aether-sage.md`,
    `.codex/agents/aether-sage.toml`). Add a comment above the slice naming
    CMD-04 and recording the finding that `sage` has no slash command on any
    platform, so its "keeps working unchanged" surface is the agent definition —
    otherwise a future reader will assume the file is simply missing.

    The test does three things:

    First, an existence and count check: assert the slice has exactly 17 entries
    and every path exists. A dropped surface fails here rather than silently
    reducing coverage, the same defensive shape
    `TestBriefPathReferencedAcrossAllFourSurfaces` uses with its `len == 4`
    assertion.

    Second, a content ledger: declare a map from repo-relative path to the
    lowercase hex SHA-256 of that file's bytes, computed at the time this test is
    written, and assert each file still hashes to its recorded value. The failure
    message must say that CMD-04 requires these commands to keep working
    unchanged through milestone v1.25, that the ledger is deliberately brittle,
    and that a legitimate future edit updates the recorded hash in the same
    commit with a stated reason. Compute the hashes by running the test once in a
    mode that prints actual values, or by shelling `shasum -a 256` over the
    seventeen paths; do not guess them.

    Third, a runtime-reachability check for the seven verbs that have a
    command-guide entry (`archaeology`, `chaos`, `colonize`, `council`, `dream`,
    `oracle`, `swarm`): assert `aether command-guide <verb>` resolves. Do this by
    calling the same in-process code path the existing command-guide tests use
    rather than shelling out to a built binary, so the test stays fast and does
    not require a prior `go build`. If no such in-process entry point exists,
    skip this third check and record the reason in a comment rather than
    fabricating a weaker assertion — the ledger plus existence check already
    satisfies CMD-04's failable requirement.
  </action>
  <verify>
    <automated>go vet ./cmd/ && go test ./cmd/... -run TestSpecialistCommandSurfacesUnchanged && git diff --stat HEAD -- .claude/commands/ant/chaos.md .claude/commands/ant/archaeology.md .claude/commands/ant/dream.md .claude/commands/ant/oracle.md .claude/commands/ant/swarm.md .claude/commands/ant/colonize.md .claude/commands/ant/council.md .claude/agents/ant/aether-sage.md | grep -q . && echo UNEXPECTED_CHANGES || echo CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/... -run TestSpecialistCommandSurfacesUnchanged` passes
    - `specialistCommandSurfaces` has exactly 17 entries and the test asserts that count explicitly
    - Every recorded SHA-256 matches the file's actual current hash (verify independently with `shasum -a 256` on at least three of the seventeen paths)
    - `git log --oneline -1 -- .claude/commands/ant/chaos.md` shows no commit from this phase; the same holds for the other six specialist wrappers and the three sage agent files
    - `git diff --stat <phase-base-commit>..HEAD -- .claude/commands/ant/chaos.md .claude/commands/ant/archaeology.md .claude/commands/ant/dream.md .claude/commands/ant/oracle.md .claude/commands/ant/swarm.md .claude/commands/ant/colonize.md .claude/commands/ant/council.md .claude/agents/ant/aether-sage.md` is empty, proving Phase 165 changed none of them
    - The comment above the slice records that sage has no slash command surface
  </acceptance_criteria>
  <done>CMD-04 is enforced by a test that fails on an undeclared edit, rather than by an assertion that nobody touched anything.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human read-through of build.md (CMD-03) and validation ledger sign-off</name>
  <files>.planning/phases/165-core-lifecycle-commands/165-VALIDATION.md</files>
  <read_first>
    - `.claude/commands/ant/build.md` (the rewritten file under review)
    - `.planning/phases/165-core-lifecycle-commands/165-VALIDATION.md` (the ledger to complete: Task ID column, Status column, Wave 0 checkboxes, and the `nyquist_compliant` frontmatter flag)
  </read_first>
  <what-built>
    All four lifecycle wrappers have been rewritten onto the D-05 stage skeleton
    with Classic Queen-voice ceremony, and eleven automated assertions now fail if
    the structure, the ceremony beats, the platform parity, the flat mirrors, the
    ownership handshake, or the specialist commands regress. Everything that can
    be measured mechanically has been. CMD-03 is the one success criterion the
    research explicitly flagged as prose-quality rather than grep-quality: it asks
    whether a person reading build.md can describe what each stage does and what
    context a worker receives, without opening Go source. Only a person can answer
    that.
  </what-built>
  <how-to-verify>
    1. Open `.claude/commands/ant/build.md` and read it top to bottom. Do not open
       any file under `cmd/` while reading.
    2. For each stage heading in turn — Colony Context, Active Signals, Phase
       Framing, Dispatch Manifest, Guided Boundary Gate, Runtime Spawn Ceremony,
       Worker Spawning, Finalize, After the Build — say out loud, in one sentence
       each: what this stage is for, and what a worker spawned at this stage
       receives as input.
    3. Answer three questions from the file alone: when does a build stop early?
       Where does the worker's prompt come from? What does the wrapper do that the
       Go runtime does not?
    4. Judge the voice: does it read like the Queen running a colony, or like a
       protocol specification? The user's stated goal for this phase was "the rich
       ceremony we used to have."
    5. If any stage cannot be described without guessing, name it — that stage's
       Purpose, Reads, or Stop conditions text needs another pass, and the fix
       belongs in this phase, not a later one.
  </how-to-verify>
  <action>
    After the read-through, complete
    `.planning/phases/165-core-lifecycle-commands/165-VALIDATION.md`: replace every
    `TBD (planner fills)` Task ID with the real plan and task reference
    (`165-01 T2`, `165-02 T1`, and so on), fill the Plan and Wave columns, replace
    each proposed test name with the test actually shipped, set every Status cell
    to its observed result, tick all five Wave 0 Requirements checkboxes with the
    file that closed each one, record the CMD-03 manual verification outcome in
    the Manual-Only Verifications table with the date and the reviewer's verdict,
    tick the six Validation Sign-Off boxes, set `nyquist_compliant: true` and
    `wave_0_complete: true` in the frontmatter, and change `status: draft` to
    `status: complete`. Set Approval to the recorded verdict rather than
    `pending`. Note that `.planning/` is gitignored but tracked in this repo, so
    the commit needs `git add -f` for this file or the change will silently vanish
    from the commit.
  </action>
  <verify>
    <automated>grep -q 'nyquist_compliant: true' .planning/phases/165-core-lifecycle-commands/165-VALIDATION.md && grep -q 'wave_0_complete: true' .planning/phases/165-core-lifecycle-commands/165-VALIDATION.md && ! grep -q 'TBD (planner fills)' .planning/phases/165-core-lifecycle-commands/165-VALIDATION.md && ! grep -q '⬜ pending' .planning/phases/165-core-lifecycle-commands/165-VALIDATION.md && go test ./cmd/... && go vet ./... && go build ./cmd/aether</automated>
  </verify>
  <acceptance_criteria>
    - The reviewer stated, for all nine build.md stages, the stage purpose and the worker's inputs without opening any file under `cmd/`
    - No `TBD` or `⬜ pending` entries remain in `165-VALIDATION.md`
    - Frontmatter reads `nyquist_compliant: true`, `wave_0_complete: true`, `status: complete`
    - All five Wave 0 Requirements checkboxes are ticked, each naming the file that closed it
    - The Manual-Only Verifications table records the CMD-03 outcome with a date and verdict
    - `go test ./cmd/...` passes in full, `go vet ./...` is clean, `go build ./cmd/aether` succeeds
    - The commit including `165-VALIDATION.md` used `git add -f` (verify the file appears in `git show --stat HEAD`)
  </acceptance_criteria>
  <resume-signal>Type "approved" to confirm every build.md stage was describable without opening Go source, or name the stages that were not so they get another pass in this phase.</resume-signal>
  <done>CMD-03 has a recorded human verdict, and the validation ledger reflects what actually shipped rather than what was proposed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test assertions → future phases | These tests are the only mechanism preventing restoration number eight; a weakened assertion silently removes the protection |
| specialist command files → this milestone's other phases | Phases 160, 166, 168 and 170 all run against the same tree; an accidental edit to a delight command would breach CMD-04 unnoticed |
| validation ledger → milestone audit | The ledger is the artifact a later audit reads to decide whether Phase 165 actually shipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-06-01 | Repudiation | A ledger marked complete while the assertions behind it were never run — the exact v1.23 `WORKFLOW-01..11` failure mode CLAUDE.md cites | mitigate | Task 3's automated verify re-runs the full `go test ./cmd/...`, `go vet ./...` and `go build ./cmd/aether` chain as a precondition for the ledger being accepted, and greps that no pending or TBD cells remain |
| T-165-06-02 | Tampering | A future phase weakening the ratio assertion instead of fixing the wrapper it flags | mitigate | The negative-control subtest `a_wrapper_dominated_by_envelope_prose_would_fail` fails if the ratio check is loosened enough to accept envelope-dominated prose, so weakening the main assertion breaks the control too |
| T-165-06-03 | Tampering | Silent edits to a specialist or delight command breaching CMD-04 | mitigate | `TestSpecialistCommandSurfacesUnchanged` pins seventeen surfaces by SHA-256 with an explicit count assertion; a dropped path fails the count, an edited file fails the hash |
| T-165-06-04 | Spoofing | The CMD-03 manual verdict being recorded without an actual read-through | mitigate | The checkpoint requires the reviewer to state nine stage descriptions and answer three specific questions before the resume signal, and the ledger records a dated verdict rather than a checkbox |
| T-165-06-05 | Information Disclosure | The SHA-256 ledger leaking nothing but creating a false sense of integrity coverage | accept | The ledger is a change-detection fence, not an integrity control against an adversary; the comment above the slice states this so no one mistakes it for tamper-proofing |
</threat_model>

<verification>
1. `go test ./cmd/...` passes in full, including all five pre-existing wrapper enforcement tests and every test added by Plans 01 through 06.
2. `go vet ./...` clean; `go build ./cmd/aether` succeeds.
3. All four flat mirrors are byte-identical to their canonical sources.
4. `git diff --stat` between the phase base commit and HEAD, restricted to the eight CMD-04 specialist surfaces, is empty.
5. `165-VALIDATION.md` carries no `TBD` and no pending status cells.
</verification>

<success_criteria>
- CMD-01: all eight canonical wrappers carry the stage skeleton, the three structured blocks, and the state-carry contract, all asserted
- CMD-02: method prose outweighs envelope prose at least 3 to 1 in every wrapper, with the contract referenced exactly once (zero times for init)
- CMD-03: a recorded, dated human verdict that every build.md stage is describable without opening Go source
- CMD-04: seventeen specialist and delight surfaces pinned by hash with an explicit count assertion; zero of them changed during this phase
- The validation ledger reflects shipped tests, not proposed ones
</success_criteria>

<output>
After completion, create `.planning/phases/165-core-lifecycle-commands/165-06-SUMMARY.md`.
Commit `165-VALIDATION.md` with `git add -f` — `.planning/` is gitignored but tracked in this repo.
</output>
