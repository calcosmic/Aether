---
phase: 165-core-lifecycle-commands
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - cmd/continue_wrapper_ceremony_test.go
  - .claude/commands/ant/continue.md
  - .opencode/commands/ant/continue.md
  - .claude/commands/ant-continue.md
  - .aether/commands/continue.yaml
autonomous: true
requirements: [CMD-01, CMD-02]
must_haves:
  truths:
    - "A user reading continue.md can say what the fast path does, when the heavy review path is used instead, what each reads, and when each stops"
    - "The verification stage explains in colony terms why an independent Watcher checks the Builder's work"
    - "The runtime-owned context-clear line stays out of the wrapper, still asserted at both markdown and Go-render level"
    - "Adding a new stage heading to continue.md without a Purpose marker fails a test"
  artifacts:
    - path: ".claude/commands/ant/continue.md"
      provides: "Rewritten continue wrapper carrying method + Classic ceremony for both paths"
      contains: "## Required Cross-Stage State"
    - path: ".opencode/commands/ant/continue.md"
      provides: "Byte-parity OpenCode mirror"
    - path: "cmd/continue_wrapper_ceremony_test.go"
      provides: "Extended contract: stage-skeleton density, heading parity, structured blocks, widened context-clear fence"
      exports: ["TestContinueWrapperCeremonyContract", "TestContinueWrapperStageSkeletonAndParity"]
    - path: ".aether/commands/continue.yaml"
      provides: "Guardrail source updated in lockstep"
  key_links:
    - from: "cmd/continue_wrapper_ceremony_test.go"
      to: ".claude/commands/ant/continue.md"
      via: "assertStageSkeletonDensity + assertOrderedHeadingParity"
      pattern: "assertStageSkeletonDensity"
    - from: ".claude/commands/ant/continue.md"
      to: ".aether/docs/wrapper-host-contract.md"
      via: "single contract pointer replacing inline envelope-shape prose"
      pattern: "wrapper-host-contract.md"
---

<objective>
Rewrite `continue.md` (both platforms) so its two paths — the fast runtime-owned
default and the wrapper-conducted heavy external review — each read as
engineering method with Classic Queen-voice ceremony, and pin the result with
failable assertions.

Continue carries the single most explicitly test-guarded regression in the whole
change zone: the context-clear ceremony line was moved to the Go runtime in
commit `ecf68ba4` and the test was flipped from requiring it to forbidding it.
Any restored ceremony prose that paraphrases "it's safe to clear your context
now" or routes to `/ant-resume` re-breaks it. That fence is named explicitly in
this plan's read_first so a mining pass cannot drift into it.

Purpose: `/ant-continue` explains why verification is independent, not just how
to invoke it.
Output: Rewritten continue wrapper on both platforms, resynced flat mirror,
updated YAML guardrails, extended test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/165-core-lifecycle-commands/165-CONTEXT.md
@.planning/phases/165-core-lifecycle-commands/165-ARCHAEOLOGY.md
@.planning/phases/165-core-lifecycle-commands/165-PATTERNS.md
@.planning/phases/165-core-lifecycle-commands/165-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Helpers created by Plan 01 in cmd/lifecycle_wrapper_contract_test.go. -->
  func canonicalWrapperPaths(repoRoot, verb string) []string
  func flatMirrorPath(repoRoot, verb string) string
  func countMarkerLines(text string, markers []string) int
  func stageSkeletonMarkers() []string
  func envelopeMechanicsMarkers() []string
  func assertOrderedHeadingParity(t *testing.T, verb, claudeText, opencodeText string)
  func assertStageSkeletonDensity(t *testing.T, path, text string, exemptHeadings []string)

<!-- Pre-existing package-level helpers -->
  func repoRootForCommandSourceTest() (string, error)
  func assertSubstringsInOrder(t *testing.T, path, content string, ordered []string)
  func stripCommentLines(text string) string
  func level2Headings(text string) []string

<!-- Byte-exact command string required in FOUR files by                       -->
<!-- TestContinueWrapperSourcesUseFastDevContinue:                             -->
<!--   .aether/commands/continue.yaml, .claude/commands/ant-continue.md,       -->
<!--   .claude/commands/ant/continue.md, .opencode/commands/ant/continue.md    -->
<!--   AETHER_OUTPUT_MODE=visual aether continue --verification-depth standard $ARGUMENTS -->

<!-- Runtime ceremony CLI: only four registered subcommands exist -->
<!--   spawn-plan, wave-start, worker-complete, closeout          -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the continue wrapper contract test with skeleton density, heading parity, and structured blocks</name>
  <files>cmd/continue_wrapper_ceremony_test.go</files>
  <read_first>
    - `cmd/continue_wrapper_ceremony_test.go` (164 lines — read both halves: the markdown-content assertions this task extends, and the `renderContinueVisual` / `renderContinueBlockedVisual` Go-render assertions that are out of scope and must not be touched)
    - `cmd/lifecycle_wrapper_contract_test.go` (created by Plan 01 — the helper signatures this test calls)
    - `cmd/plan_wrapper_cards_test.go` (the heading-parity pattern being generalized)
    - `.claude/commands/ant/continue.md` (the current file, so the RED assertions are calibrated against real content)
    - `.claude/commands/ant-continue.md` (the flat mirror the context-clear fence is being widened to cover)
    - `.planning/phases/165-core-lifecycle-commands/165-ARCHAEOLOGY.md` (§D-5 — why the context-clear line is the most explicitly-flipped test in the codebase)
  </read_first>
  <behavior>
    - `TestContinueWrapperStageSkeletonAndParity` / `stage_skeleton_density`: every non-exempt level-2 heading in continue.md is followed within its section by `**Purpose:**`. RED today.
    - `TestContinueWrapperStageSkeletonAndParity` / `ordered_heading_parity`: `.claude` and `.opencode` continue.md have identical ordered level-2 heading lists. GREEN today; must stay green.
    - `TestContinueWrapperStageSkeletonAndParity` / `structured_blocks_present`: `<success_criteria>`, `<failure_modes>`, `<read_only>`, and `## Required Cross-Stage State` present. RED today.
    - `TestContinueWrapperStageSkeletonAndParity` / `method_outweighs_envelope_mechanics`: stage-skeleton marker lines are at least 3x envelope-mechanics marker lines. RED today.
    - `TestContinueWrapperStageSkeletonAndParity` / `context_clear_stays_runtime_owned`: the two forbidden strings are absent from both canonical files AND the flat mirror. GREEN today; widens existing coverage.
    - `TestContinueWrapperCeremonyContract`: all 14 existing required substrings, the ordered marker chain, and both existing forbidden strings still assert; the Go-level render assertions are untouched.
  </behavior>
  <action>
    Extend `cmd/continue_wrapper_ceremony_test.go`. Preserve every existing
    assertion. In particular do NOT modify the half of this file that calls
    `renderContinueVisual` and `renderContinueBlockedVisual` and asserts on their
    output strings — those are Go-render assertions, already correct, and this
    phase changes no runtime behavior. Treat that block as read-only.

    Add to the existing `required` slice: `## Required Cross-Stage State`,
    `**Purpose:**`, `**Reads:**`, `**Stop conditions:**`, `<success_criteria>`,
    `<failure_modes>`, `<read_only>`, and the colony-beat anchor
    `Why this matters`.

    Add `TestContinueWrapperStageSkeletonAndParity` with the five subtests named
    in the behavior block. Use the exempt-heading list
    `{"## Ownership Split", "## Required Cross-Stage State", "## Cross-Platform Drift Guard", "## Guardrails"}`.
    Call `assertOrderedHeadingParity` with verb `continue`, and
    `assertStageSkeletonDensity` once per canonical path. The
    `method_outweighs_envelope_mechanics` subtest compares
    `countMarkerLines(text, stageSkeletonMarkers())` against
    `3 * countMarkerLines(text, envelopeMechanicsMarkers())` and reports both
    counts on failure — a proportion assertion that survives any section rename.

    The `context_clear_stays_runtime_owned` subtest re-asserts the two forbidden
    strings (the context-clear sentence and the `/ant-resume` route) are absent
    from both canonical files AND from `.claude/commands/ant-continue.md`. The
    flat mirror is not covered by that specific forbidden-string check today;
    extending it there closes the same class of gap that let `ant-build.md` and
    `ant-init.md` drift unnoticed.
  </action>
  <verify>
    <automated>go vet ./cmd/ && go test ./cmd/... -run TestContinueWrapperStageSkeletonAndParity/ordered_heading_parity && go test ./cmd/... -run TestContinueWrapperStageSkeletonAndParity/context_clear_stays_runtime_owned && go test ./cmd/... -run TestContinueWrapperStageSkeletonAndParity/stage_skeleton_density 2>&1 | grep -q FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `go vet ./cmd/` exits 0
    - `TestContinueWrapperStageSkeletonAndParity/ordered_heading_parity` PASSES
    - `TestContinueWrapperStageSkeletonAndParity/context_clear_stays_runtime_owned` PASSES
    - `TestContinueWrapperStageSkeletonAndParity/stage_skeleton_density` FAILS naming at least one heading without a Purpose marker (intended RED)
    - `git diff cmd/continue_wrapper_ceremony_test.go` shows no change to the `renderContinueVisual` / `renderContinueBlockedVisual` assertion block, and no deletion from the existing required or forbidden slices
  </acceptance_criteria>
  <done>The continue wrapper's target structure is expressed as failing tests, with the context-clear fence widened to the flat mirror.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite continue.md on both platforms with the D-05 stage skeleton and Classic verification ceremony</name>
  <files>.claude/commands/ant/continue.md, .opencode/commands/ant/continue.md</files>
  <read_first>
    - `.claude/commands/ant/continue.md` (123 lines — the file being rewritten)
    - `.opencode/commands/ant/continue.md` (currently byte-identical; gets the same rewrite)
    - `cmd/continue_wrapper_ceremony_test.go` (as extended by Task 1 — the constraints, especially the forbidden context-clear strings and the byte-exact fast-path command)
    - `cmd/platform_doc_hygiene_test.go` (the continue.md entries near lines 216 and 526 — forbidden strings include `continue-verify.md`, `continue-gates.md`, `Read build packet files:`, `Write COLONY_STATE.json`, `.aether/aether-utils.sh`)
    - `.planning/phases/165-core-lifecycle-commands/165-ARCHAEOLOGY.md` (§A-1 exemplars #4 and #5 — the "colony requires actual parallelism" and "Builders verify their own work = confirmation bias" blocks are this file's source text; §D-5 and §D-6 are the fences)
    - `.planning/phases/165-core-lifecycle-commands/165-PATTERNS.md` (continue.md section: fast-path vs heavy-path split, the CRITICAL forbidden-regression excerpt, the byte-exact fast command)
    - `.aether/docs/wrapper-host-contract.md` (as extended by Plan 01 — the pointer target)
  </read_first>
  <action>
    Rewrite both continue.md files. They must remain byte-identical to each other
    at the end of this task. Write one, then copy it to the other path.

    Stage skeleton (D-05). Keep the existing level-2 headings and their order as
    the ordered-marker assertion requires — `## Ownership Split`,
    `## Default Continue`, `## Heavy External Review`, `## After Continue`,
    `## Cross-Platform Drift Guard`, `## Guardrails` — and insert
    `## Required Cross-Stage State` after `## Ownership Split`. Under
    `## Default Continue`, `## Heavy External Review`, and `## After Continue`
    write, in order: one colony beat line, then `**Purpose:**`, `**Reads:**`,
    `**Spawns:**` where applicable (Default Continue spawns nothing — say so
    explicitly, that absence is itself useful method), then the stage's
    imperative instructions and command blocks, then `**Stop conditions:**`.

    The verification beat (D-01, D-02). `## Heavy External Review` opens with the
    rule-with-reason idiom in colony terms under a `Why this matters` line
    saying, reworded from the Classic source: builders verifying their own work
    is confirmation bias, independent Watchers catch what builders miss, and
    "build passing" is not the same as "app working". This is the signature
    Classic idiom and it belongs at exactly this moment. Keep the 🐜 and 👑
    glyphs for beats. Write no literal heavy or light separator rules and no
    reproduction of any runtime-rendered visual — the spawn plan, wave start,
    worker completion and closeout are all rendered by `aether ceremony`.

    HARD FENCE. Do not write, paraphrase, or gesture at "it's safe to clear your
    context now", and do not route the user to `/ant-resume` anywhere in this
    file. That line is owned exclusively by `renderContinueVisual` and
    `renderContinueBlockedVisual` in Go, and it is the most explicitly-flipped
    test in the codebase. Also do not name any file under
    `.aether/docs/command-playbooks/` — `continue-verify.md` and
    `continue-gates.md` are forbidden strings — and do not restate gate
    thresholds or gate decision trees in markdown (D-6). Describe what the
    runtime enforces; never re-implement it.

    Envelope mechanics (D-03, D-04, CMD-02). Under `## Heavy External Review`
    keep the required strings `aether host continue --dry-run --classic-ceremony`
    and `result.manifest.continue_manifest`, but subordinate them to the stage's
    method as one line per mechanical step, followed by exactly one pointer
    sentence to `.aether/docs/wrapper-host-contract.md` for the field shapes. The
    file references that contract twice today; reduce to exactly one occurrence.
    Under `## Default Continue` keep the fast-path command byte-identical to the
    string asserted across four files — do not add flags, reorder flags, or
    change spacing.

    D-06 method assets. Add `## Required Cross-Stage State` listing `phase_id`,
    `verification_depth`, `verification_status`, `manifest_file` (heavy path
    only), and `next_action` — current vocabulary only, never `colony_depth`.
    Add a `<success_criteria>` block (the runtime verified the phase and emitted
    a next-step line; on the heavy path every manifest reviewer returned a
    terminal result and `continue-finalize` ran on the collected packet), a
    `<failure_modes>` block (continue blocked — translate the blocker into plain
    language and route to the runtime's own recovery command first; boundary
    guidance active — route to `aether discuss` and request a fresh manifest;
    reviewer failure on the heavy path — never fabricate a verification result),
    and a `<read_only>` block naming the state files this wrapper may read but
    never write.

    Stop conditions (D-06). Each of the three stages ends with
    `**Stop conditions:**` describing what the runtime already enforces.

    Guardrails. Keep every existing guardrail bullet verbatim, including the
    closing `If docs and runtime disagree, runtime wins.`
  </action>
  <verify>
    <automated>diff .claude/commands/ant/continue.md .opencode/commands/ant/continue.md && go test ./cmd/... -run 'TestContinueWrapperCeremonyContract|TestContinueWrapperStageSkeletonAndParity|TestContinueWrapperSourcesUseFastDevContinue|TestLifecycleCommandDocsPreferRuntimeCLI|TestLifecycleWrappersAvoidRetiredDepthVocabulary' && test "$(grep -c 'wrapper-host-contract.md' .claude/commands/ant/continue.md)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `diff .claude/commands/ant/continue.md .opencode/commands/ant/continue.md` produces no output
    - All tests named in the verify command pass
    - `grep -c 'wrapper-host-contract.md' .claude/commands/ant/continue.md` returns exactly 1
    - `grep -c "safe to clear your context" .claude/commands/ant/continue.md` returns 0
    - `grep -c 'ant-resume' .claude/commands/ant/continue.md` returns 0
    - `grep -cE 'continue-verify[.]md|continue-gates[.]md' .claude/commands/ant/continue.md` returns 0
    - `grep -c -- '[*][*]Purpose:[*][*]' .claude/commands/ant/continue.md` returns at least 3
    - `grep -c 'colony_depth' .claude/commands/ant/continue.md` returns 0
    - The fast-path command line `AETHER_OUTPUT_MODE=visual aether continue --verification-depth standard` followed by the arguments token is present unchanged
    - The file contains `<success_criteria>`, `<failure_modes>`, `<read_only>`, `## Required Cross-Stage State`, and a `Why this matters` line
  </acceptance_criteria>
  <done>Both platforms' continue.md explain both paths as method with Classic verification ceremony, and the context-clear fence is intact.</done>
</task>

<task type="auto">
  <name>Task 3: Resync the continue flat mirror and update continue.yaml guardrails in lockstep</name>
  <files>.claude/commands/ant-continue.md, .aether/commands/continue.yaml</files>
  <read_first>
    - `.claude/commands/ant/continue.md` (the rewritten canonical file to mirror)
    - `.claude/commands/ant-continue.md` (the flat installed mirror, currently in sync with the pre-rewrite canonical)
    - `.aether/commands/continue.yaml` (80 lines — carries the byte-exact fast-path command asserted by `TestContinueWrapperSourcesUseFastDevContinue`, plus `wrapper_additions` and `guardrails`)
    - `cmd/lifecycle_wrapper_contract_test.go` (the flat-mirror byte-equality invariant from Plan 01)
  </read_first>
  <action>
    Replace `.claude/commands/ant-continue.md` with the exact bytes of the
    rewritten `.claude/commands/ant/continue.md`. Straight copy, no edits.

    Update `.aether/commands/continue.yaml` in lockstep (D-11). Add a
    `wrapper_additions` entry named `stage_skeleton` describing the D-05 order
    (colony beat, Purpose, Reads, Spawns, Stop conditions) and naming the
    `## Required Cross-Stage State` values `phase_id`, `verification_depth`,
    `verification_status`, `manifest_file`, `next_action`. Add two guardrail
    bullets: one forbidding hand-rendering any visual that `aether ceremony`
    renders, and one restating that the context-clear guidance is runtime-owned
    and must never appear in wrapper prose. Do not modify the existing byte-exact
    fast-path command string anywhere in this file, and do not delete or reword
    any existing guardrail bullet or `wrapper_additions` entry.
  </action>
  <verify>
    <automated>diff .claude/commands/ant-continue.md .claude/commands/ant/continue.md && grep -q 'stage_skeleton' .aether/commands/continue.yaml && go test ./cmd/... && go vet ./... && go build ./cmd/aether</automated>
  </verify>
  <acceptance_criteria>
    - `diff .claude/commands/ant-continue.md .claude/commands/ant/continue.md` produces no output
    - `.aether/commands/continue.yaml` contains a `stage_skeleton:` key under `wrapper_additions`
    - `.aether/commands/continue.yaml` still contains the byte-exact fast-path continue command asserted by `TestContinueWrapperSourcesUseFastDevContinue`
    - `.aether/commands/continue.yaml` gained exactly 2 new guardrail bullets and lost none
    - `go test ./cmd/...` passes in full
    - `go vet ./...` is clean and `go build ./cmd/aether` succeeds
  </acceptance_criteria>
  <done>The installed mirror and the YAML guardrail source both reflect the rewritten wrapper; no surface drifted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| continue.md prose → LLM orchestrator context | Every sentence is executed as instruction by the model conducting verification |
| mined playbook text → continue.md | `continue-gates.md` holds the wanted rule-with-reason beat immediately beside forbidden gate arithmetic |
| wrapper narration → user's trust in verification | Fabricated or wrapper-composed verification results would be indistinguishable from runtime truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-165-03-01 | Tampering | Restoring the context-clear ceremony line while mining Classic continue prose (D-5, the most explicitly-flipped test) | mitigate | Named as a HARD FENCE in the Task 2 action and read_first; asserted at markdown level for both canonical files and now also the flat mirror, and independently at Go-render level by the untouched existing assertions |
| T-165-03-02 | Spoofing | Wrapper prose implying it computed a verification result the runtime actually owns | mitigate | The `<failure_modes>` block explicitly forbids fabricating a verification result; the existing guardrail `Do NOT replay verification loops or reimplement runtime gate logic` is preserved verbatim |
| T-165-03-03 | Tampering | Gate threshold arithmetic re-entering markdown from `continue-gates.md` (D-6) | mitigate | Task 2 forbids restating thresholds or decision trees; `platform_doc_hygiene_test.go` forbids the playbook filenames, and the acceptance criteria grep for them |
| T-165-03-04 | Tampering | Playbook Read-loader phrasing reappearing (CRITICAL D-1) | mitigate | Forbidden strings `continue-verify.md` / `continue-gates.md` asserted by the existing hygiene test and by an acceptance grep |
| T-165-03-05 | Information Disclosure | Restored narration exposing raw provider stdout/stderr or auth probe output | mitigate | The sanctioned sanitized-availability idiom is retained; the existing guardrail forbidding raw provider output is preserved verbatim |
| T-165-03-06 | Elevation of Privilege | Heavy-path reviewer prose broadening a reviewer's write scope | mitigate | The `<read_only>` block names the state files the wrapper may never write; the existing `Do NOT read or write colony state files by hand` guardrail is preserved |
</threat_model>

<verification>
1. `go test ./cmd/...` passes in full.
2. `go vet ./...` clean; `go build ./cmd/aether` succeeds.
3. `.claude`, `.opencode`, and flat-mirror continue.md are all byte-identical.
4. `TestContinueWrapperSourcesUseFastDevContinue` passes, proving the fast-path command survived intact in all four files.
</verification>

<success_criteria>
- Reading continue.md shows both paths' purpose, reads, spawn choreography, and stop conditions, with the Classic verification beat restored (CMD-01)
- Envelope-shape detail lives in `wrapper-host-contract.md` with exactly one pointer from continue.md (CMD-02)
- The context-clear regression fence is intact and now also covers the flat mirror
- Adding a stage without a Purpose marker, or editing one platform's continue.md alone, fails a test
</success_criteria>

<output>
After completion, create `.planning/phases/165-core-lifecycle-commands/165-03-SUMMARY.md`.
</output>
