---
phase: 17-local-wisdom-accumulation
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - .aether/docs/command-playbooks/continue-advance.md
  - .aether/docs/command-playbooks/continue-finalize.md
  - .aether/QUEEN.md
autonomous: true
requirements: [QUEEN-01, QUEEN-02]

must_haves:
  truths:
    - "After /ant:continue runs, QUEEN.md Build Learnings section contains new entries from the completed phase"
    - "After /ant:continue runs, instincts at confidence >= 0.8 appear in QUEEN.md Instincts section"
    - "A brief notice is shown when wisdom entries are written to QUEEN.md"
    - "The current repo's QUEEN.md is migrated from v1 (6-section) to v2 (4-section) format"
    - "Build learnings entries are tagged [repo] or [general] with source attribution"
  artifacts:
    - path: ".aether/docs/command-playbooks/continue-advance.md"
      provides: "Instinct promotion sweep after Steps 3/3a/3b"
      contains: "queen-promote-instinct"
    - path: ".aether/docs/command-playbooks/continue-finalize.md"
      provides: "Build learnings write step using queen-write-learnings"
      contains: "queen-write-learnings"
    - path: ".aether/QUEEN.md"
      provides: "Migrated local QUEEN.md in v2 format"
      contains: "## Build Learnings"
  key_links:
    - from: ".aether/docs/command-playbooks/continue-advance.md"
      to: ".aether/aether-utils.sh"
      via: "bash call to queen-promote-instinct subcommand"
      pattern: "aether-utils.sh queen-promote-instinct"
    - from: ".aether/docs/command-playbooks/continue-finalize.md"
      to: ".aether/aether-utils.sh"
      via: "bash call to queen-write-learnings subcommand"
      pattern: "aether-utils.sh queen-write-learnings"
    - from: ".aether/docs/command-playbooks/continue-advance.md"
      to: ".aether/data/COLONY_STATE.json"
      via: "reads instincts from colony state for promotion sweep"
      pattern: "memory.instincts.*confidence"
---

<objective>
Wire the new write subcommands into the build workflow so QUEEN.md accumulates wisdom automatically, and migrate the current repo's QUEEN.md to the new format.

Purpose: Plan 01 created the subcommands; this plan connects them to the actual colony workflow. After this, every /ant:continue will write build learnings and promote high-confidence instincts to QUEEN.md without user intervention. The current repo's QUEEN.md is also migrated from the old 6-section format to the new 4-section format.

Output: Updated playbooks with write hooks, migrated local QUEEN.md, wisdom accumulation working end-to-end.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-local-wisdom-accumulation/17-RESEARCH.md
@.planning/phases/17-local-wisdom-accumulation/17-CONTEXT.md
@.planning/phases/17-local-wisdom-accumulation/17-01-SUMMARY.md
@.aether/docs/command-playbooks/continue-advance.md
@.aether/docs/command-playbooks/continue-finalize.md
@.aether/QUEEN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire build learnings and instinct promotion into continue playbooks</name>
  <files>
    .aether/docs/command-playbooks/continue-advance.md
    .aether/docs/command-playbooks/continue-finalize.md
  </files>
  <action>
    **1. Add instinct promotion sweep to continue-advance.md:**

    After Steps 3, 3a, and 3b (instinct creation from phase patterns, midden errors, and success patterns), add a new **Step 3c: Promote high-confidence instincts to QUEEN.md (QUEEN-02)**:

    ```markdown
    ### Step 3c: Promote High-Confidence Instincts to QUEEN.md (QUEEN-02)

    After instinct creation (Steps 3/3a/3b), sweep all instincts for confidence >= 0.8
    and promote to the QUEEN.md Instincts section. This runs every /ant:continue to catch
    newly boosted instincts.

    Run using the Bash tool with description "Promoting high-confidence instincts to QUEEN.md...":
    ```bash
    instinct_data=$(jq -r '.memory.instincts // []' .aether/data/COLONY_STATE.json 2>/dev/null || echo '[]')
    promoted_instinct_count=0

    for encoded in $(echo "$instinct_data" | jq -r '.[] | select(.confidence >= 0.8) | @base64'); do
        trigger=$(echo "$encoded" | base64 -d | jq -r '.trigger')
        action=$(echo "$encoded" | base64 -d | jq -r '.action')
        confidence=$(echo "$encoded" | base64 -d | jq -r '.confidence')
        domain=$(echo "$encoded" | base64 -d | jq -r '.domain // "workflow"')

        # queen-promote-instinct handles dedup internally (skips if already in QUEEN.md)
        result=$(bash .aether/aether-utils.sh queen-promote-instinct \
            "$trigger" "$action" "$confidence" "$domain" 2>/dev/null || echo '{"ok":false}')

        was_promoted=$(echo "$result" | jq -r '.result.promoted // false' 2>/dev/null || echo "false")
        if [[ "$was_promoted" == "true" ]]; then
            promoted_instinct_count=$((promoted_instinct_count + 1))
        fi
    done

    if [[ $promoted_instinct_count -gt 0 ]]; then
        echo "Promoted $promoted_instinct_count instinct(s) to QUEEN.md"
    fi
    ```

    This step is NON-BLOCKING -- if queen-promote-instinct fails for any entry, log and continue.
    The dedup check inside queen-promote-instinct ensures idempotency (safe to run repeatedly).
    ```

    Place this step AFTER Step 3b and BEFORE Step 4 (Advance state). This ordering ensures all instincts (including newly created ones from this phase) are swept for promotion.

    **2. Add build learnings write step to continue-finalize.md:**

    Add a new **Step 2.1.7: Write Build Learnings to QUEEN.md (QUEEN-01)** immediately after the existing Step 2.1.6 (Batch Wisdom Auto-Promotion):

    ```markdown
    ### Step 2.1.7: Write Build Learnings to QUEEN.md (QUEEN-01)

    After learning extraction and auto-promotion, write the current phase's learnings directly
    to the QUEEN.md Build Learnings section. This captures both successes and failures from every
    build, regardless of recurrence thresholds.

    **This step uses queen-write-learnings, which bypasses observation thresholds.**
    Every build writes learnings -- this is the user's explicit decision.

    Run using the Bash tool with description "Writing build learnings to QUEEN.md...":
    ```bash
    # Get phase info
    current_phase=$(jq -r '.current_phase' .aether/data/COLONY_STATE.json 2>/dev/null || echo "0")
    phase_name=$(jq -r --argjson p "$current_phase" '.plan.phases[] | select(.id == $p) | .name // "unknown"' .aether/data/COLONY_STATE.json 2>/dev/null || echo "unknown")

    # Extract learnings from the just-completed phase
    learnings_json=$(jq -r --argjson p "$((current_phase - 1))" '
      [.memory.phase_learnings[]
       | select(.phase == $p)
       | .learnings[]
       | {
           claim: .claim,
           tag: (if (.claim | test("this (codebase|repo|project)"; "i")) then "repo" else "general" end),
           evidence: (.evidence // "")
         }
      ]' .aether/data/COLONY_STATE.json 2>/dev/null || echo '[]')

    learnings_count=$(echo "$learnings_json" | jq 'length' 2>/dev/null || echo "0")

    if [[ "$learnings_count" -gt 0 ]] && [[ "$learnings_json" != "[]" ]]; then
        prev_phase=$((current_phase - 1))
        result=$(bash .aether/aether-utils.sh queen-write-learnings \
            "$prev_phase" "$phase_name" "$learnings_json" 2>/dev/null || echo '{"ok":false}')

        written=$(echo "$result" | jq -r '.result.written // 0' 2>/dev/null || echo "0")
        if [[ "$written" -gt 0 ]]; then
            echo "Written $written learning(s) to QUEEN.md"
        fi
    fi
    ```

    **Tag heuristic (Claude's discretion per CONTEXT.md):**
    - Default tag is "general" (most learnings are broadly applicable)
    - Tag as "repo" if the claim mentions "this codebase", "this repo", "this project", or contains repo-specific names
    - The jq query uses a regex test for common repo-specific indicators
    - Claude (the builder agent) should use judgment when extracting learnings in Step 2 -- learnings about specific files/modules of this repo are [repo], learnings about general practices are [general]

    **Brief notice (per user decision):**
    The echo statements above ("Written N learning(s) to QUEEN.md", "Promoted N instinct(s) to QUEEN.md") serve as the brief notice. They only appear when entries are actually written. No notice for zero writes.

    This step is NON-BLOCKING -- QUEEN.md write failures never block phase advancement.
    The dedup check inside queen-write-learnings prevents duplicate entries.
    ```

    **3. Update Step 2.1.6 (Batch Wisdom Auto-Promotion) if needed:**

    The existing Step 2.1.6 calls `learning-promote-auto` which routes through `queen-promote`. Since queen-promote was updated in Plan 01 to use new section mappings, Step 2.1.6 should still work. However, verify the step comment references are accurate. If queen-promote now maps to `## Codebase Patterns` instead of the old section names, the comment should note this.

    No code changes needed to 2.1.6 itself -- just update any comments that reference old section names.

    **4. Update the Display Result (Step 3) in continue-finalize.md:**

    In the display template (around the "Wisdom Promoted to QUEEN.md" section), update the display to also show:
    - Build learnings written count
    - Instincts promoted count

    Add after the existing wisdom promoted section:
    ```
    QUEEN.md Updated:
       Build learnings: {written_count} entries
       Instincts promoted: {promoted_instinct_count} entries
    ```

    This gives the user visibility into the automatic accumulation.
  </action>
  <verify>
    - `bash -n .aether/docs/command-playbooks/continue-advance.md` -- no bash syntax errors in code blocks (visual check; .md files don't have bash -n validation)
    - Verify continue-advance.md contains "Step 3c" with "queen-promote-instinct" call
    - Verify continue-finalize.md contains "Step 2.1.7" with "queen-write-learnings" call
    - Verify Step 3c is placed AFTER Steps 3/3a/3b and BEFORE Step 4
    - Verify Step 2.1.7 is placed AFTER Step 2.1.6
    - `grep -c "queen-write-learnings" .aether/docs/command-playbooks/continue-finalize.md` returns 1+
    - `grep -c "queen-promote-instinct" .aether/docs/command-playbooks/continue-advance.md` returns 1+
    - `npm test` -- all tests pass
  </verify>
  <done>
    continue-advance.md has instinct promotion sweep (Step 3c) that runs after all instinct creation steps. continue-finalize.md has build learnings write (Step 2.1.7) that runs after batch auto-promotion. Both steps are non-blocking and show brief notices. The workflow is wired: every /ant:continue will now automatically write learnings and promote instincts to QUEEN.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate current repo QUEEN.md and validate end-to-end</name>
  <files>
    .aether/QUEEN.md
  </files>
  <action>
    **1. Migrate the current repo's QUEEN.md from v1 to v2 format:**

    Read the existing `.aether/QUEEN.md`. Based on research, it has only 1 pattern promoted (the rest is template default).

    Create a new v2-format QUEEN.md that:
    - Uses the new 4-section structure (User Preferences, Codebase Patterns, Build Learnings, Instincts)
    - Migrates any existing content from old sections to new sections:
      - Any content from Philosophies, Patterns, Redirects, Stack Wisdom -> Codebase Patterns (tagged appropriately)
      - Any content from Decrees, old User Preferences -> User Preferences
      - Build Learnings starts empty (new section)
      - Instincts starts empty (new section)
    - Preserves existing Evolution Log entries
    - Updates METADATA to version 2.0.0 with new stats keys
    - Sets `last_evolved` to current timestamp
    - Adds an Evolution Log entry: `| {timestamp} | system | migrated | QUEEN.md migrated from v1 (6-section) to v2 (4-section) format |`

    Use the Write tool to create the new QUEEN.md content. This is a one-time migration for the development repo.

    **2. Validate end-to-end by running queen subcommands against the migrated file:**

    ```bash
    # Verify queen-read works with new format
    bash .aether/aether-utils.sh queen-read
    # Should return JSON with codebase_patterns, build_learnings, instincts, user_prefs keys

    # Verify queen-write-learnings works
    bash .aether/aether-utils.sh queen-write-learnings 0 "migration-test" '[{"claim":"QUEEN.md v2 migration validated","tag":"repo","evidence":"manual validation"}]'
    # Should return ok:true, written:1

    # Verify the entry is in QUEEN.md
    grep "QUEEN.md v2 migration validated" .aether/QUEEN.md
    # Should find the entry under Build Learnings

    # Verify queen-promote-instinct works
    bash .aether/aether-utils.sh queen-promote-instinct "codebase changes" "always run full test suite after module extraction" "0.85" "testing"
    # Should return ok:true, promoted:true

    # Verify the instinct is in QUEEN.md
    grep "always run full test suite" .aether/QUEEN.md
    # Should find the entry under Instincts

    # Verify queen-read still works after writes
    bash .aether/aether-utils.sh queen-read
    # Should show has_build_learnings:true and has_instincts:true
    ```

    **3. Run full test suite to confirm nothing is broken:**

    ```bash
    npm test
    ```

    All 584+ tests must pass.

    **Note on the migration test entries:** The entries written during validation ("QUEEN.md v2 migration validated" and the test instinct) are real entries that document the migration event. They should remain in QUEEN.md as the first real wisdom entries -- they demonstrate the system works. If desired, they can be considered the "seed" content that shows the before/after difference (success criterion 4).
  </action>
  <verify>
    - `.aether/QUEEN.md` contains `## Build Learnings` and `## Instincts` and `## Codebase Patterns` and `## User Preferences`
    - `.aether/QUEEN.md` does NOT contain emoji section headers (`grep -c "📜\|🧭\|⚠️\|🔧\|🏛️\|👤\|📊" .aether/QUEEN.md` returns 0)
    - `bash .aether/aether-utils.sh queen-read | jq -r '.result.wisdom | keys'` includes codebase_patterns, build_learnings, instincts
    - QUEEN.md contains at least one Build Learnings entry and one Instincts entry from the validation step
    - `npm test` passes all tests
  </verify>
  <done>
    The repo's QUEEN.md is in v2 format with 4 sections. queen-read, queen-write-learnings, and queen-promote-instinct all work end-to-end against the migrated file. The system is ready for real colony builds to accumulate wisdom automatically.
  </done>
</task>

</tasks>

<verification>
- continue-advance.md wires instinct promotion sweep (Step 3c) after all instinct creation
- continue-finalize.md wires build learnings write (Step 2.1.7) after batch auto-promotion
- Both hooks are non-blocking and show brief notices
- Current repo QUEEN.md is migrated to v2 (4-section) format
- End-to-end validation: queen-write-learnings and queen-promote-instinct produce visible entries in QUEEN.md
- All tests pass (584+)
</verification>

<success_criteria>
1. Every /ant:continue run will write build learnings to QUEEN.md Build Learnings section (QUEEN-01)
2. Every /ant:continue run will promote instincts at confidence >= 0.8 to QUEEN.md Instincts section (QUEEN-02)
3. Brief notice shown when entries are written
4. Current repo QUEEN.md migrated from v1 to v2 format with real entries demonstrating the system works
5. All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/17-local-wisdom-accumulation/17-02-SUMMARY.md`
</output>
