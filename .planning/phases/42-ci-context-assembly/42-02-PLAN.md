---
phase: 42-ci-context-assembly
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - .aether/docs/command-playbooks/continue-advance.md
  - .aether/docs/command-playbooks/build-verify.md
autonomous: true
requirements:
  - CI-01

must_haves:
  truths:
    - "pr-context is called during /ant:continue to generate review context after verification"
    - "pr-context is called during /ant:run before review cycles in compact mode"
  artifacts:
    - path: ".aether/docs/command-playbooks/continue-advance.md"
      provides: "Review context generation step using pr-context"
      contains: "pr-context"
    - path: ".aether/docs/command-playbooks/build-verify.md"
      provides: "Review context generation step for autopilot review cycles"
      contains: "pr-context --compact"
  key_links:
    - from: "continue-advance.md"
      to: "pr-context subcommand"
      via: "bash invocation during post-verify review"
      pattern: "aether.*pr-context"
    - from: "build-verify.md"
      to: "pr-context subcommand"
      via: "bash invocation before autopilot review"
      pattern: "pr-context --compact"
---

<objective>
Wire pr-context into the /ant:continue and /ant:run workflows so review cycles have structured colony context available.

Purpose: CI agents and autopilot review cycles need colony context generated at the right moment. Per D-14, integrate pr-context into continue-advance.md (post-verify review context) and build-verify.md (before review cycles).

Output: Updated playbooks with pr-context invocation steps.
</objective>

<execution_context>
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-ci-context-assembly/42-CONTEXT.md

<!-- Depends on Plan 01 output -->
@.planning/phases/42-ci-context-assembly/42-01-SUMMARY.md

<interfaces>
<!-- pr-context subcommand from Plan 01 -->
```bash
# Usage: aether pr-context [--compact] [--branch <name>] [--ci-run-id <id>]
# Output: JSON via json_ok() with structured colony context
# Exit: Always 0 (never hard-fails)
# --compact: 3000 char budget instead of 6000
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pr-context invocation to continue-advance.md and build-verify.md playbooks</name>
  <files>.aether/docs/command-playbooks/continue-advance.md, .aether/docs/command-playbooks/build-verify.md</files>
  <read_first>
    .aether/docs/command-playbooks/continue-advance.md (full file — find review/post-verify section)
    .aether/docs/command-playbooks/build-verify.md (full file — find review cycle section)
    .aether/docs/command-playbooks/continue-verify.md (for pattern reference — Step 2.0.5/2.0.6 midden wiring pattern)
  </read_first>
  <acceptance_criteria>
    - .aether/docs/command-playbooks/continue-advance.md contains "pr-context" (case-sensitive)
    - .aether/docs/command-playbooks/build-verify.md contains "pr-context --compact" (case-sensitive)
    - Both additions are non-blocking (output captured but failures do not stop the workflow)
    - Neither addition changes existing step numbering (use fractional step numbers like 2.0.7 to avoid renumbering)
  </acceptance_criteria>
  <action>
    **1. In continue-advance.md:**
    Add a new step after the existing review context steps (after the midden wiring step at 2.0.5/2.0.6 pattern). Use a fractional step number that does not renumber existing steps (e.g., Step 2.0.7 or equivalent).

    The step should:
    - Call `bash "$AETHER_UTILS" pr-context 2>/dev/null` to generate review context
    - Store output in a `REVIEW_CONTEXT` variable
    - This is NON-BLOCKING: if pr-context fails or returns empty, log a warning and continue
    - The review context is available for subsequent review/learning steps but does not gate them
    - Follow the same pattern as the midden wiring (Step 2.0.5 in continue-verify.md): capture output, suppress stderr, non-blocking

    Example step text:
    ```
    Step X.Y.Z: Generate review context (pr-context)
    - Run: REVIEW_CONTEXT=$(bash "$AETHER_UTILS" pr-context 2>/dev/null) || true
    - If REVIEW_CONTEXT is empty or fails, continue without it (non-blocking)
    - This provides structured colony context for review/learning steps
    ```

    **2. In build-verify.md:**
    Add a new step before the review cycle section (the section where /ant:run triggers agent reviews). Use a fractional step number.

    The step should:
    - Call `bash "$AETHER_UTILS" pr-context --compact 2>/dev/null` for compact review context
    - Store output for CI agent consumption during review cycles
    - NON-BLOCKING: failures do not stop verification
    - Note: This is the autopilot integration point where review cycles get colony context

    Example step text:
    ```
    Step X.Y.Z: Generate compact review context for review cycles
    - Run: REVIEW_CONTEXT=$(bash "$AETHER_UTILS" pr-context --compact 2>/dev/null) || true
    - If empty, continue without review context (non-blocking)
    - Provides compact colony context for CI/autopilot agent review cycles
    ```
  </action>
  <verify>
    <automated>grep -c "pr-context" .aether/docs/command-playbooks/continue-advance.md .aether/docs/command-playbooks/build-verify.md | grep -v ":0$"</automated>
  </verify>
  <done>
    - continue-advance.md has a pr-context invocation step (non-blocking, post-verify)
    - build-verify.md has a pr-context --compact invocation step (non-blocking, before review cycles)
    - Neither step breaks existing workflow numbering or gates any subsequent step
    - D-14 integration points covered: /ant:continue and /ant:run
  </done>
</task>

</tasks>

<verification>
1. `grep "pr-context" .aether/docs/command-playbooks/continue-advance.md` returns at least 1 match
2. `grep "pr-context --compact" .aether/docs/command-playbooks/build-verify.md` returns at least 1 match
3. Neither file has broken step numbering (verify existing step references still valid)
</verification>

<success_criteria>
- pr-context wired into /ant:continue post-verify flow (D-14)
- pr-context --compact wired into /ant:run review cycle flow (D-14)
- Both integrations are non-blocking
- CI pipeline integration explicitly NOT included (deferred to Phase 44)
</success_criteria>

<output>
After completion, create `.planning/phases/42-ci-context-assembly/42-02-SUMMARY.md`
</output>
