---
phase: 42-ci-context-assembly
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/utils/pheromone.sh
  - .aether/aether-utils.sh
  - tests/bash/test-pr-context.sh
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03

must_haves:
  truths:
    - "aether pr-context outputs valid JSON with colony_state, pheromones/signals, hive_wisdom, blockers, decisions, midden, and prompt_section sections"
    - "When COLONY_STATE.json or pheromones.json is missing, pr-context returns partial data with null/empty section and a fallbacks_used entry -- never exits non-zero"
    - "Normal mode output stays under 6,000 characters; compact mode under 3,000 characters"
    - "Budget trim order is identical to colony-prime: rolling-summary first, blockers never"
    - "colony-prime output is byte-identical before and after _budget_enforce() extraction"
    - "Midden section includes bounded entries (max 10 recent or last 7 days) and cross-PR analysis data"
  artifacts:
    - path: ".aether/utils/pheromone.sh"
      provides: "_budget_enforce() and _pr_context() functions"
      contains: "_budget_enforce()"
    - path: ".aether/aether-utils.sh"
      provides: "pr-context dispatch entry and help JSON entry"
      contains: "pr-context)"
    - path: "tests/bash/test-pr-context.sh"
      provides: "Test suite covering CI-01, CI-02, CI-03"
      min_lines: 200
  key_links:
    - from: "_pr_context()"
      to: "_budget_enforce()"
      via: "function call with pc_ prefix and max_chars parameter"
      pattern: "_budget_enforce pc_ pc_final_prompt"
    - from: "_pr_context()"
      to: "pheromone-prime subcommand"
      via: "subcommand invocation for signal assembly"
      pattern: "pheromone-prime"
    - from: "_pr_context()"
      to: "hive-read subcommand"
      via: "subcommand invocation for cross-colony wisdom"
      pattern: "hive-read"
    - from: "_pr_context()"
      to: "midden-cross-pr-analysis subcommand"
      via: "subcommand invocation for cross-PR failure patterns"
      pattern: "midden-cross-pr-analysis"
    - from: "aether-utils.sh dispatch"
      to: "_pr_context()"
      via: "case statement entry pr-context)"
      pattern: "pr-context\\) _pr_context"
---

<objective>
Extract shared budget enforcement function from colony-prime, then implement the full pr-context subcommand that produces machine-readable JSON colony context for CI agents.

Purpose: CI agents need structured colony context (signals, wisdom, blockers, midden) without relying on interactive colony-prime. pr-context never hard-fails and includes a TTL-based cache layer for expensive source reads.

Output: Working `aether pr-context` subcommand with test coverage, budget extraction verified against existing colony-prime tests.
</objective>

<execution_context>
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/repos/Aether/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-ci-context-assembly/42-CONTEXT.md
@.planning/phases/42-ci-context-assembly/42-RESEARCH.md
@.aether/docs/ci-context-assembly-design.md
@.planning/phases/41-midden-collection/41-01-SUMMARY.md

<interfaces>
<!-- Key interfaces the executor needs from existing codebase -->

From .aether/utils/pheromone.sh:
```bash
# Budget enforcement block (lines 1376-1492) to extract into _budget_enforce()
# Uses cp_sec_* variables (cp_sec_rolling, cp_sec_learnings, etc.)
# Trim order: rolling, learnings, decisions, hive, capsule, user_prefs, queen_global, queen_local, signals
# NEVER trims blockers
# Preserves REDIRECTs when signals section trimmed

# _extract_wisdom() - callable directly for QUEEN.md parsing
# _filter_wisdom_entries() - callable directly for wisdom entry filtering
# _colony_prime() - lines 737-1553, reference implementation
```

From .aether/aether-utils.sh:
```bash
# Dispatch pattern (line ~3908): colony-prime) _colony_prime "$@" ;;
# Help JSON pattern (line ~1254): {"name": "colony-prime", "description": "Assemble unified worker priming payload"}
# context-capsule subcommand (lines 4172-4368) - reusable via subcommand invocation
```

From .aether/utils/midden.sh:
```bash
# midden-cross-pr-analysis - returns cross-PR failure patterns (Phase 41)
# midden.json stored at .aether/data/midden/midden.json
```

From .aether/utils/file-lock.sh:
```bash
# acquire_lock <path> - acquire file lock for cache writes
# release_lock <path> - release file lock
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract _budget_enforce() and create test scaffold</name>
  <files>.aether/utils/pheromone.sh, tests/bash/test-pr-context.sh</files>
  <read_first>
    .aether/utils/pheromone.sh (lines 1376-1492 — budget enforcement block)
    .aether/utils/pheromone.sh (lines 737-760 — _colony_prime function header)
    tests/bash/test-colony-prime-budget.sh (lines 1-80 — existing budget test pattern)
    tests/bash/test-pheromone-module.sh (lines 27-76 — setup_pheromone_env pattern)
    tests/bash/test-helpers.sh (test helper functions)
  </read_first>
  <behavior>
    - Test: colony-prime output is byte-identical before and after _budget_enforce() extraction (regression guard for D-09)
    - Test: _budget_enforce() trims rolling-summary first when over budget
    - Test: _budget_enforce() never trims blockers even when over budget
    - Test: _budget_enforce() preserves REDIRECTs when signals section is trimmed
    - Test: _budget_enforce() with pc_ prefix works identically to cp_ prefix
    - Test: pr-context test scaffold has setup/teardown with isolated tmpdir
  </behavior>
  <action>
    1. Create `tests/bash/test-pr-context.sh` following the test-helpers.sh pattern:
       - Source test-helpers.sh and require_jq
       - Create `setup_pr_context_env()` helper that creates an isolated tmpdir with:
         - Copied .aether/aether-utils.sh and .aether/utils/ (follow setup_pheromone_env pattern)
         - Minimal COLONY_STATE.json with goal, state, current_phase, total_phases, phase_name
         - Minimal pheromones.json with version, colony_id, empty signals array
         - Empty midden/midden.json with entries array
         - Optional QUEEN.md with emoji-prefixed section headers (matching _extract_wisdom pattern)
       - Create `teardown_pr_context_env()` that rm -rf the tmpdir
       - Add test stubs for: test_output_has_required_sections, test_missing_colony_state, test_missing_pheromones, test_no_queen_md_soft_fail, test_normal_mode_budget, test_compact_mode_budget, test_trim_order, test_blockers_never_trimmed, test_midden_section_bounded, test_cache_status, test_corrupt_json_fallback
       - All stubs initially pass (return 0) except budget extraction tests which work immediately

    2. Extract `_budget_enforce()` from colony-prime budget block (pheromone.sh lines 1376-1492):
       - Place the new function BEFORE `_colony_prime()` (around line 730, after _pheromone_prime)
       - Function signature: `_budget_enforce() { local _be_prefix="${1:-cp_}" ...`
       - The function takes a single argument: the variable prefix ("cp_" or "pc_")
       - Use indirect variable access with eval (NOT nameref) to read/write the section variables:
         `eval "local _be_max_chars=\${${_be_prefix}max_chars}"`
         `eval "${_be_prefix}sec_rolling=\"\""`
         etc.
       - The function accesses these variables via the prefix:
         {prefix}max_chars, {prefix}budget_len, {prefix}final_prompt,
         {prefix}sec_rolling, {prefix}sec_learnings, {prefix}sec_decisions,
         {prefix}sec_hive, {prefix}sec_capsule, {prefix}sec_user_prefs,
         {prefix}sec_queen_global, {prefix}sec_queen_local, {prefix}sec_signals,
         {prefix}sec_blockers, {prefix}budget_trimmed_list
       - Sets {prefix}budget_trimmed_list to comma-separated list of trimmed section names
       - Recalculates {prefix}final_prompt and {prefix}budget_len after each trim
       - NEVER trims {prefix}sec_blockers
       - Preserves REDIRECTs when signals section trimmed (copy the REDIRECT extraction logic verbatim)

    3. Replace the inline budget block in _colony_prime() with a call to _budget_enforce():
       - Replace lines 1388-1492 with: `_budget_enforce "cp_"`
       - Keep the comment block above (lines 1376-1384) for documentation
       - Keep the "Budget trimming notification" block below (lines 1495+) unchanged

    4. Run existing colony-prime budget tests to verify byte-identical output:
       `bash tests/bash/test-colony-prime-budget.sh`
       ALL tests must pass unchanged. If any fail, the extraction introduced a regression and must be fixed.

    5. Add budget extraction tests to test-pr-context.sh that call _budget_enforce via pr-context (once implemented):
       - These will be activated when Task 2 adds the pr-context function
  </action>
  <verify>
    <automated>bash tests/bash/test-colony-prime-budget.sh && echo "BUDGET REGRESSION: PASS"</automated>
  </verify>
  <done>
    - _budget_enforce() function exists in pheromone.sh before _colony_prime()
    - _colony_prime() calls _budget_enforce "cp_" instead of inline budget code
    - ALL existing colony-prime budget tests pass unchanged (D-09 regression guard)
    - tests/bash/test-pr-context.sh exists with setup/teardown helpers and test stubs
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement _pr_context() with all sections, cache, midden, and dispatch registration</name>
  <files>.aether/utils/pheromone.sh, .aether/aether-utils.sh, tests/bash/test-pr-context.sh</files>
  <read_first>
    .aether/utils/pheromone.sh (lines 730-770 — _budget_enforce and _colony_prime header, after Task 1 extraction)
    .aether/utils/pheromone.sh (lines 770-1376 — _colony_prime body for reference pattern)
    .aether/utils/pheromone.sh (lines 1493-1553 — _colony_prime tail: JSON output, budget notification)
    .aether/aether-utils.sh (lines 3907-3909 — dispatch case entries near colony-prime)
    .aether/aether-utils.sh (lines 1249-1261 — help JSON Pheromone System section)
    .aether/docs/ci-context-assembly-design.md (Section 3.2 — output schema, Section 5 — fallback chains, Section 4 — cache layer)
    tests/bash/test-pr-context.sh (current state after Task 1)
    .aether/utils/midden.sh (midden-cross-pr-analysis function signature)
  </read_first>
  <acceptance_criteria>
    - .aether/utils/pheromone.sh contains function _pr_context()
    - .aether/aether-utils.sh contains 'pr-context) _pr_context "$@" ;;' dispatch entry
    - .aether/aether-utils.sh help JSON contains "pr-context" entry in Pheromone System section
    - `AETHER_ROOT=/tmp/empty bash .aether/aether-utils.sh pr-context 2>/dev/null | jq .ok` returns "true"
    - `AETHER_ROOT=/tmp/empty bash .aether/aether-utils.sh pr-context 2>/dev/null | jq .result.colony_state.exists` returns "false"
    - `AETHER_ROOT=/tmp/empty bash .aether/aether-utils.sh pr-context 2>/dev/null | jq .result.fallbacks_used | length` is greater than 0
    - `bash tests/bash/test-pr-context.sh` exits 0 with all test stubs passing
    - `bash tests/bash/test-colony-prime-budget.sh` still exits 0 (regression guard)
  </acceptance_criteria>
  <action>
    Implement `_pr_context()` in pheromone.sh AFTER the _colony_prime() function (after its closing brace, around line 1554). Then register in aether-utils.sh and activate test stubs.

    **1. _pr_context() function body:**

    Parse flags: `--compact`, `--branch <name>` (default: `git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "unknown"`), `--ci-run-id <id>`.

    Set budgets: pc_max_chars=6000 (default), pc_max_chars=3000 (compact). Per D-03/D-07.

    Initialize arrays: `pc_warnings=()` and `pc_fallbacks=()`.

    **2. Assemble each section (follow colony-prime pattern but with soft-fail everywhere, per D-10/D-11):**

    a) **queen (global)**: Read `~/.aether/QUEEN.md` via `_extract_wisdom()`. On missing/empty: `pc_fallbacks+=("queen_global: no file found")`, set `pc_queen_global_data="{}"`.

    b) **queen (local)**: Read `$AETHER_ROOT/.aether/QUEEN.md` via `_extract_wisdom()`. On missing/empty: `pc_fallbacks+=("queen_local: no file found")`, set `pc_queen_local_data="{}"`.

    c) **user_preferences**: Extract from queen sections. On missing: empty array.

    d) **signals**: Call `pheromone-prime` via `$AETHER_UTILS pheromone-prime 2>/dev/null` (or source the function). Parse the JSON output to build structured signal arrays:
       - `redirects`: filter signals where type="REDIRECT"
       - `focus`: filter signals where type="FOCUS"
       - `feedback`: filter signals where type="FEEDBACK"
       - `instincts`: from COLONY_STATE.json memory.instincts array
       On missing/empty pheromones.json: `pc_fallbacks+=("pheromones: no active signals")`.

    e) **hive_wisdom**: Call `hive-read` via subcommand invocation. On empty: fall back to eternal memory (`~/.aether/eternal/memory.json`). On both empty: `pc_fallbacks+=("hive_wisdom: no hive or eternal data")`.

    f) **colony_state**: Read `.aether/data/COLONY_STATE.json`. Extract goal, state, current_phase, total_phases, phase_name. On missing: set `exists:false` with defaults (goal="No goal set", state="UNKNOWN", current_phase=0, total_phases=0, phase_name=""). `pc_fallbacks+=("colony_state: COLONY_STATE.json missing")`.

    g) **blockers**: Read `.aether/data/flags.json`. Extract flags with type BLOCKER or severity CRITICAL. On missing: `{count:0, items:[]}`. `pc_fallbacks+=("blockers: flags.json missing")`.

    h) **decisions**: Read `.aether/CONTEXT.md` if exists, extract decisions section. On missing: `{count:0, items:[]}`.

    i) **rolling_summary**: Read `.aether/data/rolling-summary.log` last 20 lines. On missing: empty string.

    j) **midden (per D-01/D-02/D-03)**: ALWAYS fresh read from `.aether/data/midden/midden.json` (VOLATILE, never cached). Bound: `jq` filter for entries from last 7 days, cap at 10 entries. Truncate individual entry descriptions to 160 chars. Add cross-PR analysis via `midden-cross-pr-analysis --window 14` subcommand invocation (non-blocking: redirect stderr to /dev/null). On missing midden.json: `{count:0, entries:[], cross_pr_analysis:{}}`. `pc_fallbacks+=("midden: midden.json missing")`.

    k) **context_capsule**: Call `context-capsule` subcommand via `$AETHER_UTILS context-capsule 2>/dev/null`. On empty: fallback empty. Note: context-capsule already handles missing COLONY_STATE.json gracefully.

    **3. Cache layer (per D-04/D-05/D-06):**

    Implement `_cache_read()` and `_cache_write()` as helper functions within or before _pr_context:
    - Cache file: `$COLONY_DATA_DIR/pr-context-cache.json` (lives in .aether/data/, already gitignored)
    - `_cache_read(source_name, source_path, ttl_seconds)`:
      - Read cached entry for source_name from cache file
      - Get source file mtime: `date -r "$source_path" +%s 2>/dev/null || echo "0"` (cross-platform, per Pitfall 2)
      - If mtime matches AND entry age < ttl: return cached data
      - Else: return "null" (cache miss)
    - `_cache_write(source_name, source_path, data)`:
      - Acquire lock on cache file via `acquire_lock` (D-05)
      - Update cache entry for source_name with {path, mtime, data, cached_at}
      - Release lock via `release_lock`
    - TTL values (D-06): queen_global/queen_local=3600, hive/eternal=7200
    - Evict stale entries on each pr-context call (remove entries past their TTL)

    **4. Apply cache to cacheable sources:**
    Before reading queen_global, queen_local, hive, eternal: check cache first. On hit: use cached data. On miss: read fresh and write to cache.
    Track cache status in `pc_cache_status` object for each source.

    **5. Build prompt_section (text version for direct injection):**
    Concatenate sections into a text string following the same format as colony-prime:
    ```
    --- COLONY CONTEXT (CI pr-context) ---
    [QUEEN WISDOM sections]
    [SIGNALS section]
    [HIVE WISDOM section]
    [CONTEXT CAPSULE section]
    [PHASE LEARNINGS section]
    [KEY DECISIONS section]
    [BLOCKERS section]
    [ROLLING SUMMARY section]
    --- END COLONY CONTEXT ---
    ```

    **6. Budget enforcement (per D-07/D-08):**
    Call `_budget_enforce "pc_"` to trim sections if over budget.
    This uses pc_sec_* variables and pc_max_chars set earlier.

    **7. Build output JSON:**
    Use `jq -n` with `--arg`/`--argjson` to construct the output matching design doc Section 3.2 schema:
    ```json
    {
      "schema": "pr-context-v1",
      "generated_at": "<ISO timestamp>",
      "branch": "<branch name>",
      "cache_status": { ... per source cached/fresh status ... },
      "queen": { "global": ..., "local": ..., "combined_prefs": [...] },
      "signals": { "count": N, "redirects": [...], "focus": [...], "feedback": [...], "instincts": [...] },
      "hive": { "source": "hive|eternal|empty", "count": N, "entries": [...] },
      "colony_state": { "exists": bool, "goal": "...", ... },
      "blockers": { "count": N, "items": [...] },
      "decisions": { "count": N, "items": [...] },
      "midden": { "count": N, "entries": [...], "cross_pr_analysis": {...} },
      "prompt_section": "<text version>",
      "prompt_section_json": "<JSON-escaped text version>",
      "char_count": N,
      "budget": N,
      "trimmed_sections": [...],
      "warnings": [...],
      "fallbacks_used": [...]
    }
    ```
    Wrap in `json_ok()` envelope.

    **8. Dispatch registration in aether-utils.sh:**
    Add `pr-context) _pr_context "$@" ;;` to the case statement near line 3909 (after colony-prime line).

    Add help JSON entry in the Pheromone System section (around line 1260):
    `{"name": "pr-context", "description": "Generate CI-ready colony context as structured JSON"}`

    **9. Activate test stubs in test-pr-context.sh:**
    Replace passing stubs with actual test implementations:
    - `test_output_has_required_sections`: Run pr-context in isolated env, verify JSON has all required top-level keys (schema, generated_at, branch, cache_status, queen, signals, hive, colony_state, blockers, decisions, midden, prompt_section, char_count, budget, trimmed_sections, warnings, fallbacks_used)
    - `test_missing_colony_state`: Run without COLONY_STATE.json, verify colony_state.exists=false and fallbacks_used contains "colony_state"
    - `test_missing_pheromones`: Run without pheromones.json, verify signals.count=0 and fallbacks_used contains "pheromones"
    - `test_no_queen_md_soft_fail`: Run without any QUEEN.md, verify exit code 0 and fallbacks_used contains "queen_global"
    - `test_normal_mode_budget`: Run with large data, verify char_count <= 6000
    - `test_compact_mode_budget`: Run with --compact and large data, verify char_count <= 3000
    - `test_trim_order`: Run with oversized data, verify trimmed_sections follows colony-prime trim order (rolling first, signals last before blockers)
    - `test_blockers_never_trimmed`: Run with oversized data that has blockers, verify blockers section is present and non-empty in output
    - `test_midden_section_bounded`: Create midden.json with 20 entries, verify output has at most 10 entries
    - `test_cache_status`: Run twice, verify cache_status shows fresh on first call and cached on second for cacheable sources
    - `test_corrupt_json_fallback`: Create corrupt COLONY_STATE.json (not valid JSON), verify pr-context returns exit 0 with fallback

    Use `setup_pheromone_env` pattern from test-pheromone-module.sh for test isolation (tmpdir + AETHER_ROOT override).
  </action>
  <verify>
    <automated>bash tests/bash/test-pr-context.sh && bash tests/bash/test-colony-prime-budget.sh && echo "ALL TESTS PASS"</automated>
  </verify>
  <done>
    - `aether pr-context` outputs valid JSON wrapped in json_ok() envelope
    - All 11 test cases in test-pr-context.sh pass
    - pr-context never exits non-zero even with empty/missing data files (D-10)
    - Output includes midden section with bounded entries (D-01/D-03)
    - Cache layer works with TTL-based invalidation (D-04/D-05/D-06)
    - Budget enforcement uses shared _budget_enforce() (D-07)
    - colony-prime budget tests still pass unchanged (D-09)
    - Dispatch entry and help JSON registered in aether-utils.sh (D-15)
  </done>
</task>

</tasks>

<verification>
1. `bash tests/bash/test-pr-context.sh` -- all pr-context tests pass
2. `bash tests/bash/test-colony-prime-budget.sh` -- colony-prime regression tests pass
3. `AETHER_ROOT=/tmp/empty-test-dir bash .aether/aether-utils.sh pr-context 2>/dev/null | jq .ok` returns "true" -- never hard-fails
4. `grep -c '_budget_enforce' .aether/utils/pheromone.sh` shows at least 2 (definition + colony-prime call)
5. `grep 'pr-context)' .aether/aether-utils.sh` shows dispatch entry
6. `grep 'pr-context' .aether/aether-utils.sh | grep description` shows help entry
</verification>

<success_criteria>
- pr-context subcommand callable via `aether pr-context`
- Valid JSON output with all required sections (CI-01)
- Soft-fail on all missing sources with fallbacks_used logging (CI-02)
- Budget enforcement at 6K/3K with correct trim order (CI-03)
- Budget extraction does not regress colony-prime (D-09)
- 11+ test cases passing in test-pr-context.sh
</success_criteria>

<output>
After completion, create `.planning/phases/42-ci-context-assembly/42-01-SUMMARY.md`
</output>
