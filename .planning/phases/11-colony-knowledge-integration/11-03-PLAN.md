---
phase: 11-colony-knowledge-integration
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - tests/unit/oracle-colony.test.js
  - tests/bash/test-oracle-colony.sh
autonomous: true
requirements: [COLN-01, COLN-02, OUTP-01, OUTP-03]

must_haves:
  truths:
    - "Unit tests verify promote_to_colony extracts only 80%+ confidence answered findings"
    - "Unit tests verify build_synthesis_prompt produces template-specific sections for each template type"
    - "Unit tests verify custom template produces the original generic output"
    - "Unit tests verify validate-oracle-state accepts and rejects template values correctly"
    - "Bash integration tests verify promote function calls colony APIs end-to-end"
    - "Bash integration tests verify template-aware synthesis prompt construction"
    - "All tests pass alongside existing oracle test suites"
  artifacts:
    - path: "tests/unit/oracle-colony.test.js"
      provides: "Ava unit tests for colony promotion and template-aware synthesis"
      min_lines: 80
    - path: "tests/bash/test-oracle-colony.sh"
      provides: "Bash integration tests for promotion and template validation"
      min_lines: 40
  key_links:
    - from: "tests/unit/oracle-colony.test.js"
      to: ".aether/oracle/oracle.sh"
      via: "sed extraction of promote_to_colony and build_synthesis_prompt functions"
      pattern: "sed.*promote_to_colony\\|sed.*build_synthesis_prompt"
    - from: "tests/bash/test-oracle-colony.sh"
      to: ".aether/oracle/oracle.sh"
      via: "sed extraction for isolated function testing"
      pattern: "sed.*oracle.sh"
---

<objective>
Add comprehensive tests for colony knowledge promotion and template-aware synthesis. Ava unit tests cover promote_to_colony threshold filtering, build_synthesis_prompt template branches, confidence grouping, and validate-oracle-state template field. Bash integration tests cover end-to-end promotion with mock colony APIs and template-aware synthesis prompt construction.

Purpose: Ensure all Phase 11 functionality has regression protection, following the established oracle test patterns from Phases 7-10.
Output: Two new test files covering all 4 requirements (COLN-01, COLN-02, OUTP-01, OUTP-03).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-colony-knowledge-integration/11-RESEARCH.md
@.planning/phases/11-colony-knowledge-integration/11-01-SUMMARY.md
@.planning/phases/11-colony-knowledge-integration/11-02-SUMMARY.md
@tests/unit/oracle-steering.test.js
@tests/bash/test-oracle-steering.sh
@tests/bash/test-helpers.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Ava unit tests for colony promotion and template-aware synthesis</name>
  <files>tests/unit/oracle-colony.test.js</files>
  <action>
Create a new Ava test file following the exact patterns from `tests/unit/oracle-steering.test.js` (createTmpDir, writeState, writePlan helpers, sed extraction of oracle.sh functions).

**Test helpers** (reuse from existing patterns):
- `createTmpDir()` -- creates temp directory
- `writeState(dir, overrides)` -- writes state.json with defaults including `template: 'custom'`
- `writePlanWithFindings(dir, questions, sources)` -- writes plan.json with structured findings (v1.1 format: `{text, source_ids, iteration}`)
- `writeColonyState(dir)` -- creates `.aether/data/COLONY_STATE.json` with minimal valid colony state
- `mockUtils(dir)` -- creates a mock aether-utils.sh that logs calls to a file instead of executing real subcommands (instinct-create, learning-promote, memory-capture log their args to `$dir/promotion-log.txt`)

**Extract functions via sed** (established pattern from Phases 7-10):
- Extract `promote_to_colony` from oracle.sh via `sed -n '/^promote_to_colony()/,/^}/p'`
- Extract `build_synthesis_prompt` from oracle.sh via `sed -n '/^build_synthesis_prompt()/,/^}/p'`
- When build_synthesis_prompt depends on SCRIPT_DIR for oracle.md cat, set SCRIPT_DIR explicitly (same as Phase 8 pattern)

**promote_to_colony tests (COLN-01):**
1. Extracts only 80%+ confidence answered findings -- write plan.json with mixed confidence questions (90%, 70%, 50%), verify only the 90% one triggers API calls
2. Rejects promotion when status is "active" -- write state.json with status "active", verify function returns error
3. Allows promotion when status is "complete" -- verify function runs successfully
4. Allows promotion when status is "stopped" -- verify function runs successfully
5. Returns 0 with message when no findings meet threshold -- all questions below 80% or all open
6. Handles v1.0 string findings gracefully -- plan.json with key_findings as string array (not objects)
7. Rejects when COLONY_STATE.json missing -- no colony state file exists, verify error

**build_synthesis_prompt template tests (COLN-02, OUTP-01, OUTP-03):**
8. tech-eval template produces Comparison Matrix section -- write state with template "tech-eval", capture build_synthesis_prompt output, assert contains "Comparison Matrix"
9. architecture-review template produces Component Map section -- assert contains "Component Map"
10. bug-investigation template produces Root Cause Analysis section -- assert contains "Root Cause Analysis"
11. best-practices template produces Gap Analysis section -- assert contains "Gap Analysis"
12. custom template produces Findings by Question section -- assert contains "Findings by Question" (current generic structure)
13. All templates include confidence grouping -- for each template, assert output contains "Confidence Grouping"
14. Unknown template falls through to custom -- write state with template "unknown-value", assert output contains "Findings by Question"

**validate-oracle-state tests:**
15. Accepts valid template values -- for each of the 5 enum values, validate passes
16. Rejects invalid template value -- template "invalid-template" fails validation
17. Accepts state without template field -- backward compatibility

Use the same `execSync` + `eval` pattern from oracle-steering.test.js to run extracted functions. Use `jq -n` for JSON construction in bash test scripts (not heredocs) per Phase 9 decision.
  </action>
  <verify>
Run `npx ava tests/unit/oracle-colony.test.js` -- all tests pass.
Run `npm test` -- all tests pass (including existing suites).
  </verify>
  <done>17 Ava unit tests pass covering promote_to_colony threshold filtering, status guards, colony state check, v1.0 compatibility, and build_synthesis_prompt template branching with confidence grouping. All existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create bash integration tests for promotion and template validation</name>
  <files>tests/bash/test-oracle-colony.sh</files>
  <action>
Create a new bash integration test file following the exact patterns from `tests/bash/test-oracle-steering.sh` (source test-helpers.sh, sed function extraction, run_test/run_test_not helpers, PASS/FAIL counting).

**Function extraction** (same pattern as Phases 7-10):
- Extract `promote_to_colony` from oracle.sh via sed
- When promote_to_colony calls aether-utils.sh subcommands, mock by creating a minimal utils script that echoes success JSON
- Extract `build_synthesis_prompt` from oracle.sh via sed, setting SCRIPT_DIR and STATE_FILE

**Integration test assertions:**

1. **promote_to_colony with qualifying findings** -- create plan.json with 2 answered questions at 85% and 90% confidence, state.json with status "complete", mock colony state and utils -- verify function outputs "Promoting 2 high-confidence findings"
2. **promote_to_colony with no qualifying findings** -- all questions below 80% -- verify output contains "No findings meet promotion threshold"
3. **promote_to_colony blocks on active status** -- state.json with status "active" -- verify error output
4. **promote_to_colony calls instinct-create** -- check mock utils log file contains "instinct-create" call
5. **promote_to_colony calls learning-promote** -- check mock utils log file contains "learning-promote" call
6. **promote_to_colony calls memory-capture** -- check mock utils log file contains "memory-capture" call
7. **build_synthesis_prompt tech-eval** -- state.json with template "tech-eval" -- verify output contains "Comparison Matrix"
8. **build_synthesis_prompt architecture-review** -- verify output contains "Component Map"
9. **build_synthesis_prompt bug-investigation** -- verify output contains "Root Cause Analysis"
10. **build_synthesis_prompt best-practices** -- verify output contains "Gap Analysis"
11. **build_synthesis_prompt custom** -- verify output contains "Findings by Question"
12. **build_synthesis_prompt confidence grouping** -- verify output contains "Confidence Grouping" for all templates
13. **validate-oracle-state template valid** -- run validate-oracle-state with template field set to each valid enum value -- verify success
14. **validate-oracle-state template invalid** -- template set to "invalid" -- verify failure
15. **validate-oracle-state no template** -- state without template field -- verify success (backward compat)

Use the same assertion patterns from test-oracle-steering.sh (run_test checks exit code and output, run_test_not verifies negative assertions).

At the end, print test summary: "X/Y tests passed" and exit with appropriate code.
  </action>
  <verify>
Run `bash tests/bash/test-oracle-colony.sh` -- all tests pass.
Run `npm test` -- all tests pass (including existing suites).
  </verify>
  <done>15 bash integration assertions pass covering promote_to_colony end-to-end flow with mock colony APIs, template-aware synthesis prompt construction for all 5 templates, confidence grouping, and validate-oracle-state backward compatibility. All existing tests still pass.</done>
</task>

</tasks>

<verification>
- `npx ava tests/unit/oracle-colony.test.js` -- all 17 tests pass
- `bash tests/bash/test-oracle-colony.sh` -- all 15 assertions pass
- `npm test` -- full test suite passes (490+ existing tests + new tests)
- Test coverage spans all 4 requirements: COLN-01 (promotion), COLN-02 (templates), OUTP-01 (structured report), OUTP-03 (adaptive output)
</verification>

<success_criteria>
All new tests pass. All existing tests still pass. Both unit and integration test files follow established oracle test patterns (sed extraction, createTmpDir, mock utils, run_test helpers). Tests provide regression protection for promotion threshold (80%), status guards, template branching, confidence grouping, and validate-oracle-state backward compatibility.
</success_criteria>

<output>
After completion, create `.planning/phases/11-colony-knowledge-integration/11-03-SUMMARY.md`
</output>
