---
phase: 204.1-codex-ant-skill-surface
plan: "02"
type: execute
wave: 2
depends_on: ["204.1-01"]
files_modified:
  - cmd/codex_skill_install.go
  - cmd/codex_skill_ownership.go
  - cmd/codex_skill_ownership_test.go
  - cmd/testdata/codex-skills/legacy-v1.0.79.json
  - cmd/platform_sync.go
  - cmd/platform_sync_test.go
autonomous: true
requirements: [ANT-01, ANT-02]
must_haves:
  truths:
    - "An unmodified known legacy installation migrates to nine ant entries and private support while custom, edited and unfamiliar files retain their exact bytes."
    - "Repeating the same skill synchronization leaves installed bytes, modes and ownership identity unchanged."
    - "Concurrent skill commits serialize at the selected Codex home; every skill, support, retirement and ownership-manifest target retains its ownership-time existence/absence, digest and mode through commit."
    - "A custom canonical file created after planning or an intervening permission-only edit causes refusal before any target write or journaled mutation, preserving all user bytes/modes and ownership state."
    - "Failed or interrupted unverified skill transactions restore prior owned bytes and modes, while verified receipt rollback remains refused."
  artifacts:
    - path: cmd/codex_skill_ownership.go
      provides: Content-proven ownership, explicit retirement and shared target-lock policy
    - path: cmd/testdata/codex-skills/legacy-v1.0.79.json
      provides: Frozen legacy digest evidence with original source revision and paths
    - path: cmd/codex_skill_ownership_test.go
      provides: Real migration, collision, concurrent-edit and rollback checks
    - path: cmd/platform_sync.go
      provides: Optional ExpectedMode contract and common commit validation of the original complete Codex-target baseline
  key_links:
    - from: cmd/codex_skill_install.go
      to: cmd/codex_skill_ownership.go
      via: Every exact write/removal is classified before Managed becomes true
    - from: cmd/platform_sync.go
      to: cmd/lifecycle_transaction.go
      via: Shared skill-target lock and original existence/digest/mode validation precede transaction mutation and surround commit/recovery
---

<objective>
Upgrade an existing installation without deleting custom work or treating a name prefix as ownership.
Purpose: The owner can adopt ant names safely and repeat the operation.
Output: A single ownership policy used by every skill distribution caller.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/204.1-codex-ant-skill-surface/204.1-CONTEXT.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-RESEARCH.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-PATTERNS.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-01-SUMMARY.md

Precondition: Plan 01's real discovery tracer passed. This plan expands that production installation path. Managed:true is an assertion to the transaction API, not evidence. Existing prepareMaintenanceMutation checks ExpectedDigest only when nonempty; commitMaintenanceMutation calls prepare again. Its later declaration-mode comparison uses that fresh preview, so neither expected absence nor an ownership-time mode may be discarded.

<interfaces>
Existing readLifecycleFileState returns lifecycleFileState with Exists, Digest, Mode and Bytes; absent paths have Digest=lifecycleTransactionMissingDigest ("missing") and Mode=0. Extend maintenanceMutationTarget with ExpectedMode *os.FileMode, distinct from desired Mode. For every planned Codex skill/support/retirement/.aether-owned.json target, including unchanged targets, ExpectedDigest must be the captured digest or missing sentinel, and ExpectedMode must point to an immutable copy of captured Mode.Perm() (zero for absence). A nonnil zero is a real expectation, not omission. Other maintenance callers retain their existing optional ExpectedDigest behavior and may leave ExpectedMode nil.

Proposed lock contract: lockCodexSkillTargets(maintenanceMutationPlan) returns an unlock function and error. A package-local maintenanceCodexSkillLocker function variable defaults to this real locker and is called at the common commit boundary; tests may wrap it to make a post-classification edit before calling the real locker. It is not a new CLI flag or bypass, and tests restore it with t.Cleanup without t.Parallel.
</interfaces>
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Migrate a source-proven legacy installation through the real install path</name>
  <files>cmd/codex_skill_install.go, cmd/codex_skill_ownership.go, cmd/codex_skill_ownership_test.go, cmd/testdata/codex-skills/legacy-v1.0.79.json, cmd/platform_sync.go</files>
  <read_first>Read the listed existing files, cmd/platform_sync.go, the Plan 01 payload contracts, and the pinned 404731ccffda7bbca64ce801b74b0752a7161315 versions of the legacy generator and guide. Use the imported skill-surface report's fourteen-name inventory; do not calculate legacy ownership from the newly renamed output.</read_first>
  <behavior>A known untouched legacy install loses only its fourteen exact old SKILL.md files and receives nine canonical skills/private support. Edited legacy files and unknown siblings remain. Canonical custom collisions refuse the entire skill transaction even under force. Newer installed inventories are preserved.</behavior>
  <action>Add the optional ExpectedMode field to maintenanceMutationTarget before its consumers, then implement codexSkillOwnership, codexSkillOwnedFile, classifyCodexSkillTarget and legacyCodexSkillDigests. Persist schema codex-skill-ownership/v1 with payload version/digest and owned relative path, digest and mode. Derive the frozen legacy fixture from the pinned original source generator and record that provenance; independently verify its fourteen names and byte digests. For each existing target, require a matching prior owned digest/mode or exact approved legacy digest before authorizing replacement/removal. At that ownership decision, freeze the complete baseline from the same readLifecycleFileState snapshot used to authorize each skill, support, retirement and ownership-manifest target, including absent and unchanged targets. Populate ExpectedDigest and ExpectedMode according to the interface contract; parse ownership evidence from those captured manifest Bytes, not a later read. Carry these original expectations unchanged in planCodexSkillTargets outputs through install/publish/update assembly and commit. Explicitly retire only the nine old public files and five old helper files proven by that fixture; never remove a directory recursively or use source:shipped/name/allowlist as proof. Preserve and report unknown/edited old entries. Canonical destination collisions, malformed existing ownership records and modified owned support files fail before any skill-set write; --force does not override this rule. Refuse desired version older than installed ownership, and preserve unfamiliar newer entries absent an explicit compatible retirement record. Include the ownership record in the same transaction as skill writes/removals.</action>
  <acceptance_criteria>
    - TestCodexAntSkillLegacyMigration uses the registered install path and the frozen legacy body/digest fixture, then checks every retained custom byte and all nine new entries.
    - TestCodexAntSkillOwnership covers an edited legacy file, custom ant-plan with and without frontmatter, symlinks, malformed manifest, modified support, extra sibling files and a newer installed entry.
    - TestCodexAntSkillBaselineCapture checks every planned skill/support/retirement/ownership target, including absence and unchanged cases: ExpectedDigest is never empty, ExpectedMode is nonnil, and both retain the exact classification snapshot rather than desired bytes/mode or a later preview.
    - Unknown old files are reported preserved; they do not produce a falsely complete legacy-cleanup claim.
    - Deleting ownership evidence or substituting source:shipped does not authorize the collision fixture.
  </acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexAntSkill(LegacyMigration|Ownership|NewerInventory|BaselineCapture)$' -count=1 -timeout 90m</automated></verify>
  <done>Safe migration uses exact owned-file targets with explicit preservation results and immutable existence/digest/mode expectations for every planned target.</done>
</task>

<task type="auto" tdd="true">
  <name>Protect repeated, competing and failed skill commits</name>
  <files>cmd/codex_skill_ownership.go, cmd/platform_sync.go, cmd/codex_skill_ownership_test.go, cmd/platform_sync_test.go</files>
  <read_first>Read all four files, pkg/storage/lock.go, cmd/lifecycle_transaction.go and cmd/maintenance_mutation_199_test.go. Reuse the current transaction fault/rename seams and filesystem containment; do not copy broad deletion expectations from old shim tests.</read_first>
  <action>Enforce the interface contract in prepareMaintenanceMutation. All this surface's planned Codex-home targets require both expectations; unrelated callers retain optional digest checks and nil-mode behavior. Add lockCodexSkillTargets and its maintenanceCodexSkillLocker seam at the common maintenance commit boundary. Reuse storage.FileLocker with a stable, contained lock identity under the selected Codex home, so separate projects/processes updating the same skills serialize; preview remains read-only and creates no lock/journal files. Acquire the lock before commit's prepareMaintenanceMutation call, compare every target's existence/absence, digest and mode to its original classification expectations before any target write or journaled mutation, and keep those expectations immutable. A fresh preview is only current observation, never replacement ownership evidence; compare transaction declaration BeforeExists/BeforeDigest/BeforeMode with the original expectations as well, retaining existing later transaction guards. Validate unchanged targets before skipping them. Refuse the whole plan on any mismatch without ownership-state changes, and hold the lock through commit/recovery, releasing it on every exit. Add deterministic common-boundary tests that plan and preview, then create a custom ant-plan/SKILL.md at an expected-absent path or chmod an owned target from 0644 to 0600 without changing bytes, then commit. Also replay a committed payload, attempt competing old/new plans, inject failure after one write and one removal, and edit a staged target before rollback. Assert exact bytes/modes and ownership record consistency; interrupted unverified recovery must finish rollback or name recovery-required without erasing edits. Preserve transaction symlink containment, dev install/publish isolation and explicit home-sync opt-in; update only obsolete platform_sync_test expectations tied to public names.</action>
  <acceptance_criteria>
    - TestCodexAntSkillReplay proves second-run bytes/modes/ownership digest are identical and no false restart-worthy change is counted.
    - TestCodexAntSkillConcurrentCommit uses separate processes or a real cross-process lock fixture; optimistic stale targets refuse after the first commit.
    - TestCodexAntSkillPostPlanCreationRefused creates custom canonical bytes after planning/preview at an expected-absent destination, then calls common commit; it refuses with every target's bytes/modes and ownership state matching the post-edit pre-commit snapshot and no transaction journal or success receipt.
    - TestCodexAntSkillPostPlanModeChangeRefused changes only an owned target's mode after planning/preview; common commit refuses and preserves the user's 0600 mode, all bytes and the original ownership manifest, with no transaction journal or success receipt. Include support, retirement and ownership-manifest mode cases so no target category escapes the guard.
    - TestCodexAntSkillBaselineCompatibility rejects a missing expectation on a skill target, treats nonnil zero mode as an expectation, and preserves existing non-skill callers' nil-mode/optional-digest behavior.
    - TestCodexAntSkillRollback proves old names, new-name absence, ownership bytes and modes after injected failure, and preserves a later edit as recovery-required.
    - TestCodexAntSkillPreview preserves all bytes/mtimes and creates no lock or journal; default dev home bytes stay identical.
  </acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexAntSkill(Replay|ConcurrentCommit|PostPlanCreationRefused|PostPlanModeChangeRefused|BaselineCompatibility|Rollback|Preview|ChannelIsolation)$' -count=1 -timeout 90m</automated></verify>
  <done>Every skill commit validates the complete original ownership baseline under one shared lock before mutation; post-plan creation and chmod edits refuse without loss, and unrelated callers and rollback behavior remain compatible.</done>
</task>
</tasks>

<verification>
Run both focused groups and their relevant platform-sync regressions. Demonstrate an ownership-check bypass makes the custom collision case fail. Separately, dropping the missing-state expectation or replacing the original expected mode with a fresh preview must make its corresponding post-plan refusal test fail; restore production code before recording passing results.
</verification>
<success_criteria>
Known managed legacy files migrate; custom/edited content is preserved; replays are stable. Post-plan custom creation and permission-only edits refuse before target/journal mutation with all user bytes/modes and ownership state unchanged; competing and interrupted changes cannot silently lose edits.
</success_criteria>

<assumption_delta_decision>
Primary noun: owned payload file. Decision: no-change. The detector fired on custom, but custom content already exists; an ant name is a display identity, never a new ownership key. Content/version evidence supplies authorization.
</assumption_delta_decision>

## Artifacts this phase produces

- New files: cmd/codex_skill_ownership.go, cmd/codex_skill_ownership_test.go, cmd/testdata/codex-skills/legacy-v1.0.79.json.
- New symbols: codexSkillOwnership, codexSkillOwnedFile, classifyCodexSkillTarget, legacyCodexSkillDigests, lockCodexSkillTargets and maintenanceCodexSkillLocker (the default-real lock-acquisition seam).
- New maintenanceMutationTarget field: ExpectedMode *os.FileMode. For this surface, existing ExpectedDigest always records the original digest or lifecycleTransactionMissingDigest; ExpectedMode always records the original permission bits, including zero for absence. Nil remains backward-compatible only for unrelated callers.
- New tests: TestCodexAntSkillLegacyMigration, TestCodexAntSkillOwnership, TestCodexAntSkillNewerInventory, TestCodexAntSkillBaselineCapture, TestCodexAntSkillReplay, TestCodexAntSkillConcurrentCommit, TestCodexAntSkillPostPlanCreationRefused, TestCodexAntSkillPostPlanModeChangeRefused, TestCodexAntSkillBaselineCompatibility, TestCodexAntSkillRollback, TestCodexAntSkillPreview and TestCodexAntSkillChannelIsolation.
- Ownership fields: schema_version, payload_version, payload_digest, files with relative_path/sha256/mode. Legacy fixture fields include source_revision, original_path and sha256.

<output>
Create .planning/phases/204.1-codex-ant-skill-surface/204.1-02-SUMMARY.md.
</output>
