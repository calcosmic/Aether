# Phase 204.1 display regression repair

Completed 2026-09-16. This is the bounded Wave 2 follow-up, not execution of another numbered plan.

All 29 newly reported presentation failures pass in normal and race modes. The repair changes 22 owned test files; no production fix was needed. In plain English: the tests now recognize the names shown to Codex users while still requiring the program to execute the original `aether` commands.

## Scope and diagnosis

Base commit: `52e3d073a29f1e059b9af6edf22888bd63b36c9e`. Worktree: `/Users/callumcowie/repos/Aether/.claude/worktrees/agent-p204-1-display-repair-20260916`. Branch: `worktree-agent-p204-1-display-repair-20260916`.

Read repository/nested AGENTS instructions, RTK preferences, phase CONTEXT/RESEARCH, Plan 04 SUMMARY, and execute-plan task/commit protocol. The task's ownership and no-shared-tracking instructions override generic plan advancement. No other plan was started.

The Wave 2 review recorded all 5,596 command tests executed and all 18 other packages passing. Its 46 command failures split into 29 new presentation failures and 17 historical names. The historical set is preserved verbatim in `wave-2-review.json`; none belongs to this focused selection and none was waived.

Production `platformCommandName` limits Codex skill spelling to init, discuss, oracle, colonize, plan, build, continue, swarm and seal. `lifecycleApplyPlatform` changes display fields while retaining runtime commands and IDs. The Wave 2 excerpts show the intended `$ant-*` output. Obsolete assertions expected raw CLI spelling; shared card matchers skipped skill lines and could silently select the following `aether run` choice.

The repair uses explicit literals or `expectedCodexSkillDisplays`, a nine-entry test table independent of the production inventory and formatter. Machine command fields, IDs, command execution, authority, coequal choices, incomplete/recovery facts, ordering, layout and other-platform checks remain tested. Unsupported status/run/spec/maintenance actions remain `aether` text. Matchers now include Codex skills; a negative-control test rejects `$ant-status` and requires skill/runtime spellings to resolve to the same executable for duplicate detection.

## All 29 failures and outcomes

Each name is unchanged. PASS / PASS means the named top-level test completed successfully in both final focused runs, with no skips.

| Original Wave 2 failure | Owned file | Diagnosis and correction | Normal / race |
|---|---|---|---|
| `TestAbandonPreviewNamesWhatWouldBeLost` | `cmd/abandon_cmd_test.go` | Expect `$ant-seal --force --reason`; keep retired-command, zero-write and `aether status` guidance. | PASS / PASS |
| `TestBuildVisualOutputShowsSpawnPlan` | `cmd/codex_visuals_test.go` | Expect `$ant-continue`; keep spawn count, serial execution, stage order/markers and safe-close checks. | PASS / PASS |
| `TestCheckAndAutopilotCloseoutsCarryAVerdict` | `cmd/work_closeout_lanes_test.go` | Require runtime `aether continue` and displayed `$ant-continue` separately; retain verdict and exactly-one cost block. | PASS / PASS |
| `TestContinueVisualOutputShowsColonyCompleteStageMarker` | `cmd/codex_visuals_test.go` | Expect `$ant-seal`; retain completed-phase, completion marker and handoff claims. | PASS / PASS |
| `TestDiscussVisualPendingQuestionsAvoidsWorkerTheatre` | `cmd/discuss_test.go` | Expect `$ant-discuss --resolve pd_visual_scope`; retain exact answer scope, hard constraint and no-worker-theatre checks. | PASS / PASS |
| `TestEveryLifecycleCommandEndsWithNextAction` | `cmd/lifecycle_next_action_coverage_test.go` | Compare screen and executable envelope using independent displays; retain all lifecycle cases, availability and other-platform checks. Correct isolated update fixture (below). | PASS / PASS |
| `TestFrontDoor200InitCodexUsesNativeCommands` | `cmd/front_door_200_test.go` | Expect `Next Up: $ant-discuss`; still forbid premature planning in both spellings, slash wrappers and unsupported spec/status skills. | PASS / PASS |
| `TestLegacyRecoveryCommands199AbandonZeroWrite` | `cmd/legacy_recovery_199_test.go` | Expect `$ant-seal --force --reason`; retain direct-owner authority and unchanged-state fingerprints. | PASS / PASS |
| `TestLifecycleHealth199Verified` | `cmd/lifecycle_health_199_test.go` | Expect `$ant-continue`; retain health, readiness and test-evidence facts. | PASS / PASS |
| `TestLifecycleHistory199FocusedProjection` | `cmd/lifecycle_history_199_test.go` | Expect `$ant-continue` visually; JSON runtime remains explicitly `aether continue`, with shared reason and no leaked detail. | PASS / PASS |
| `TestLifecycleNextAction` | `cmd/lifecycle_next_action_199_test.go` | Expect `$ant-build 1` beside unchanged `aether run`; preserve IDs, ordering, unranked choices and Claude/OpenCode spellings. | PASS / PASS |
| `TestLifecycleStatus199CompactSubset` | `cmd/lifecycle_status_199_test.go` | Expect `$ant-continue`; retain strict subset, shared facts and 12-line limit. | PASS / PASS |
| `TestLifecycleStatus199FullOrder` | `cmd/lifecycle_status_199_test.go` | Expect `$ant-continue`; retain full section order and every recorded fact. | PASS / PASS |
| `TestMigratedLifecycleSurfacesAgree` | `cmd/lifecycle_card_agreement_test.go` | Matcher now sees `$ant-build 1` before `aether run`; compare resolver-issued action via independent display table. | PASS / PASS |
| `TestNextActionCardRendersEveryField` | `cmd/next_action_card_test.go` | Expect `$ant-continue`; keep reflection completeness, all other fields and omitted-empty-section checks. | PASS / PASS |
| `TestNextActionCardTranslatesOnlyOnTheWayOut` | `cmd/next_action_card_test.go` | Require `$ant-continue`, reject raw/slash display on Codex, retain unchanged executable command and alternatives on all platforms. | PASS / PASS |
| `TestNextActionEnvelopeCarriesTheSameFields` | `cmd/next_action_card_test.go` | Require exact `aether continue` in envelope and `$ant-continue` in card; retain alternatives, recommendation and JSON round-trip checks. | PASS / PASS |
| `TestNextStepMenuNamesItsEffectBesideItsCommand` | `cmd/classic_voice_nextaction_test.go` | Expect `$ant-continue`; retain same-line effect and glyph-led layout. | PASS / PASS |
| `TestPartialBuildDoesNotShowTheOrdinaryBuildDoneScreen` | `cmd/codex_build_native_partial_test.go` | Use independent literal runtime/display redispatch commands with both task IDs; retain typed/JSON recovery and forbid finished-build claims in both spellings. | PASS / PASS |
| `TestPlanningExpiryPresentation200` | `cmd/planning_expiry_presentation_200_test.go` | Expect `Next: $ant-plan --refresh` only at visual output boundary; keep runtime projection, refusal, deadline, color/layout and no-acceptance-token assertions. | PASS / PASS |
| `TestRedispatchOverrideReachesBothTheCardAndTheEnvelope` | `cmd/lifecycle_card_workloop_test.go` | Check display separately; preserve typed redispatch, executable envelope, legacy next and recovery_command equality. | PASS / PASS |
| `TestSessionStartCardReflectsState` | `cmd/hook_session_start_test.go` | Expect `$ant-continue`/`$ant-seal`; compare runtime hook/resolver decisions through independent display table and preserve saved-state facts. | PASS / PASS |
| `TestSetupVisualOutput` | `cmd/codex_visuals_test.go` | Expect `$ant-init`; retain setup banner and visual-versus-JSON checks. | PASS / PASS |
| `TestStartupLifecycleEnvelopesMatchTheirCards` | `cmd/lifecycle_card_startup_test.go` | Shared assertion compares independent displayed commands while requiring aether runtime routes; retains resolver-issued projection, alternatives and specification authority. | PASS / PASS |
| `TestStatusNoColonyVisual` | `cmd/status_test.go` | Expect `$ant-init`; keep empty-colony banner and unsupported `aether lay-eggs` spelling. | PASS / PASS |
| `TestStatusShowsRecoveryDoorway` | `cmd/status_test.go` | Expect `$ant-build 2 --task 2.1`; leave fixture redispatch command aether, and preserve reason and report path. | PASS / PASS |
| `TestVisualOutputNeverLeaksRawWrapperCommands` | `cmd/visual_command_naming_test.go` | Require supported skill hints and unchanged unsupported CLI/env-prefixed shell forms; retain all Claude/OpenCode and flag-preservation assertions. | PASS / PASS |
| `TestWorkLoopEnvelopesMatchTheirCards` | `cmd/lifecycle_card_workloop_test.go` | Use shared independent display assertion for build/check/closeout, partial and blocked cases; retain machine identity and shared authority. | PASS / PASS |
| `TestWrapperPartialFinalizeDoesNotShowTheFinishedBuildScreen` | `cmd/build_finalize_partial_screen_test.go` | Use literal aether runtime and `$ant-build` display with exact unfinished task IDs; retain genuine finalization, incomplete screen and JSON recovery checks. | PASS / PASS |

## Bounded fixture deviation

The first repaired census ran all 239 selected tests: 238 passed; `TestEveryLifecycleCommandEndsWithNextAction/updating` failed before presentation because the isolated child inherited an explicitly empty data-root environment and the older alias-install fixture never bound a repository. The raw error was `storage: repository containment refused: data root path must not be empty`.

That alias fixture also omitted the three currently required private support sources; Wave 2's raw output already contained an install-error envelope that the old fixture did not check. This is fixture drift, not evidence of a production formatter or controller bug.

Within owned `lifecycle_next_action_coverage_test.go`, the update case now binds a fresh temporary repository, reuses the existing package builder and real support-source seeder, invokes registered install/setup commands, requires success envelopes, and verifies the installed payload using the existing fixture assertion. It still runs real update in both output modes. No install/publish/update controller or payload-test file was edited. The one-test fixture check passed before repeating the whole focused census. This necessary fixture correction is the only non-presentation deviation (execute-plan Rule 3: blocker).

## Focused verification and evidence

Durable evidence root:

`/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/display-regression-repair/`

Source discovery found 232 tests in all 22 changed files (231 existing plus the matcher guard). Seven additional existing tests cover the required skill surface and other consumers of the shared envelope assertion, for **239 distinct top-level tests**. Actual `go test -list` discovery confirmed exactly that set, including all 29 names above.

| Check | Top-level result | Visible test/subtest terminal events | Raw exit |
|---|---|---|---:|
| Final discovery | 239 names; zero missing/unexpected | n/a | 0 |
| First normal census | 238 pass, 1 fixture failure | 539 pass, 1 fail | 1 |
| Isolated fixture correction | 1 pass | 1 pass | 0 |
| Final normal census | 239/239 pass, zero skip | 540 pass, zero fail/skip | 0 |
| Final race census | 239/239 pass, zero skip | 540 pass, zero fail/skip; no race report | 0 |

The isolated lifecycle wrapper internally runs child subtests whose successful output is suppressed by the existing harness; terminal-event counts above describe emitted Go JSON, not an invented child-subtest count. Its successful parent status proves the child exited successfully. All 29 outcomes are additionally recorded individually in `all29-outcomes.json`.

Commands: `go test ./cmd -count=1 -timeout 90m -list <exact-regex>` and `go test ./cmd -count=1 -timeout 90m [-race] -json -run <exact-regex>`. The exact anchored regex is in `focused-regex.txt`; exact argv, cwd, source HEAD/status, UTC times, raw exit, names/counts and raw SHA-256 are in each receipt. No broad full suite ran.

Evidence files:

- `wave-2-review.json`, `wave-2-new-failure-details.json`, `wave-2-relevant-excerpts.txt`, `wave-2-display-excerpts.txt`: original failure classification and bounded line-numbered output excerpts.
- `owned-files.json`, `source-census.json`, `discovery.raw`, `discovery.receipt.json`, `focused-regex.txt`, `all29-regex.txt`, `all29-outcomes.json`: selection and per-name accounting.
- `normal-before-fixture-repair.raw` / `.receipt.json`, `fixture-check.raw` / `.receipt.json`, `normal.raw` / `.receipt.json`, `race.raw` / `.receipt.json`: raw verification and exact statuses.
- `discovery-sandbox-denied.*`: initial tool failure accessing the existing Go build cache, before any tests executed. Discovery succeeded with authorized cache access. `discovery-initial-name-mismatch.*` records a corrected obsolete name in the extra-helper-consumer selection; final discovery has no omissions.
- `initial-source.json`, `tested-source.patch`, `tested-source.json`, `preservation.json`, `final-identity.json`, `evidence-manifest.json`: source binding, preservation and artifact hashes.
- `fixes-commit.*`, `report-commit.*`, `run_checks.py`, `commit_changes.py`, `write_report.py`: commit/hook receipts and reproducible accounting scripts.

`gofmt` and `git diff --check` pass. Verification completed at 2026-09-16T12:42:28.116639+00:00.

## Atomic commits and hooks

1. `9e761dd1c946c2e416a45bc77c8f2146ebf2995f` — `test(204.1-display): align Codex presentation assertions with ant skills`; all 22 test files form one coherent repair, including the isolated-fixture blocker correction.
2. This report is committed separately as `docs(204.1-display): record regression repair outcomes and evidence`. Its exact hash is retained in `report-commit.receipt.json` and `final-identity.json` after commit, avoiding a self-referential commit hash.

Both commits use normal configured hooks, explicit owned-file staging, worktree/branch guards and post-commit deletion checks; no `--no-verify` or hook override. Hook results are retained verbatim. The fixes hook completed successfully.

## Preservation, limitations and handoff

No production files, sibling-owned controllers/payload tests or `codex_project_docs.go`/test, docs/templates, STATE, ROADMAP or REQUIREMENTS were changed. Protected file hashes match the initial snapshot. All installation fixtures use disposable home/repository paths; shared installed configuration/skills and the Phase 205 owner project were not altered.

No unresolved issue remains in this repair: all 29 names and the complete 239-test focused census pass normally and under the race detector. The separate 17 historical failures remain the parent's baseline, not a waiver. The parent retains the broad merged-suite gate. This source-only repair makes no claim of fresh-client discovery, native-worker parity, full workflow parity or Phase 205 owner acceptance.

## Self-check

The repair commit exists, all 22 owned files are represented in the census, actual discovery matches final normal/race execution, all 29 original failures map to PASS/PASS, no selected test is skipped, and protected hashes are unchanged. This report is committed before returning completion.
