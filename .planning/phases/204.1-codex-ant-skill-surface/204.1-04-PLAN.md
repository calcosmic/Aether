---
phase: 204.1-codex-ant-skill-surface
plan: "04"
type: execute
wave: 2
depends_on: ["204.1-01"]
files_modified:
  - cmd/codex_visuals.go
  - cmd/lifecycle_projection.go
  - cmd/codex_visuals_test.go
  - cmd/lifecycle_projection_test.go
  - cmd/classic_command_parity_test.go
  - cmd/command_guide.go
  - cmd/command_guide_test.go
  - .aether/skills/colony/aether-colony-creation/SKILL.md
  - .aether/skills/colony/aether-colony-research/SKILL.md
  - .aether/skills/colony/aether-colony-build-cycle/SKILL.md
autonomous: true
requirements: [ANT-01, ANT-03]
must_haves:
  truths:
    - "Codex help and next actions display the nine available ant entrypoints with arguments preserved."
    - "Machine-facing runtime commands, IDs and environment-prefixed executable lines retain their original aether identities."
    - "Unavailable ant actions remain honest CLI suggestions, and shared helper instructions resolve without a public aether helper menu."
  artifacts:
    - path: cmd/codex_visuals.go
      provides: Inventory-aware display formatting
    - path: cmd/command_guide.go
      provides: Nine-entry Codex invocation guidance and private support references
  key_links:
    - from: cmd/lifecycle_projection.go
      to: cmd/codex_visuals.go
      via: Shared inventory-aware display naming while RuntimeCommand remains unchanged
    - from: cmd/root.go
      to: cmd/codex_visuals.go
      via: Existing frontDoorCommandForPlatform path renders actual available names
---

<objective>
Show the same ant names wherever Codex tells the owner what to do next.
Purpose: The owner sees usable commands while the program's executable routes remain intact.
Output: Shared display formatting and current public/private instruction wording.
</objective>
<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/204.1-codex-ant-skill-surface/204.1-CONTEXT.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-RESEARCH.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-PATTERNS.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-01-SUMMARY.md

Existing interfaces: LifecycleActionChoice contains ID, RuntimeCommand and DisplayCommand. lifecycleApplyPlatform formats DisplayCommand only. platformCommandName is already consumed by root help and first-run output. Reuse those boundaries and the new nine-entry inventory; no local duplicate availability lists.
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Render a real lifecycle next action with its new skill name</name>
  <files>cmd/codex_visuals.go, cmd/lifecycle_projection.go, cmd/codex_visuals_test.go, cmd/lifecycle_projection_test.go, cmd/classic_command_parity_test.go</files>
  <read_first>Read the five files, cmd/codex_skill_surface.go, cmd/root.go and cmd/ux_firstrun.go. Read the scope guard in cmd/maintenance_wrapper_contract_199_test.go without changing it.</read_first>
  <behavior>A Codex accepted-plan projection displays $ant-build 1 and still contains runtime_command aether build 1. aether run, status, spec, publish, maintenance and finalizers remain CLI text; Claude/OpenCode keep their slash spelling.</behavior>
  <action>Change platformCommandName, translateHintCommandsForPlatform and lifecycleProjectionCommand to consult codexPublicSkillCommands at the display boundary. Translate only the nine supported public commands to dollar-prefixed ant names and preserve arguments exactly. Keep the existing environment-prefix protection and every RuntimeCommand/ID/event/source value unchanged. Exercise help and first-run through their existing shared callers before adding any new formatter. Add cases for quoted arguments, Unicode argument text, environment-prefixed shell lines and unavailable public names. Narrowly revise old Codex name prohibitions for discuss/plan/build and the other supported entries; retain ant-spec and ant-maintenance deferral checks and all authority assertions.</action>
  <acceptance_criteria>
    - TestCodexAntSkillDisplayRoutes runs the actual help/next-action presentation paths and checks all nine display names against an independent expected table.
    - TestCodexAntSkillRuntimeIdentity checks unchanged structured RuntimeCommand/ID values, preserved arguments and unmodified environment-prefixed shell invocations.
    - Claude/OpenCode expectations and unsupported Codex routes remain unchanged.
    - The existing TestLifecycleProjectionAcceptedPlanChoicesAreCoequal still proves coequal choices with only the supported Codex spelling revised.
  </acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexAntSkill(DisplayRoutes|RuntimeIdentity)|TestLifecycleProjectionAcceptedPlanChoicesAreCoequal|TestClassicCommandParity)$' -count=1 -timeout 90m</automated></verify>
  <done>Real user-facing help and next actions teach only installed ant skills while machine routes stay stable.</done>
</task>

<task type="auto">
  <name>Make command guides and shared support describe the installed surface</name>
  <files>cmd/command_guide.go, cmd/command_guide_test.go, .aether/skills/colony/aether-colony-creation/SKILL.md, .aether/skills/colony/aether-colony-research/SKILL.md, .aether/skills/colony/aether-colony-build-cycle/SKILL.md</files>
  <read_first>Read all five files, cmd/codex_skill_surface.go, the corresponding .aether/commands YAML entries and the phase's selected private-support policy. Retain shared runtime safety and instruction ordering from current source.</read_first>
  <action>Teach the nine $ant-* invocations in Codex-specific guide narration and replace instructions to invoke/load separately discoverable aether helper skills with their installed support-file references. Remove only obsolete statements that the nine-name surface is deferred. Preserve command IDs, RunCommand, literal raw/exact/no-orchestration bypass, owner answers, existing checks, state authority and existing workflow semantics. Keep SkillReference as an internal identity if useful, but render a resolvable private file path for Codex consumers. Do not repair Oracle, approval, native dispatch or worktree semantics assigned to later phases. Update generated-command tests to independently assert all nine names, their actual guide/runtime routes, private support resolution and no extra public helper entries. No YAML/Claude/OpenCode behavior change is required for this presentation-only amendment.</action>
  <acceptance_criteria>
    - TestCodexGeneratedCommandShimsCoverIntelligentCommands expects exactly the nine canonical ant names and real runtime routes.
    - TestCodexAntSkillGuideSupport resolves every private support reference in an installed fixture, with no dependency on a separate public aether helper skill.
    - Existing raw bypass and runtime-authority assertions remain enabled.
    - Guide output does not advertise $ant-status, $ant-help, $ant-spec or full 64-command parity.
  </acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexGeneratedCommandShimsCoverIntelligentCommands|TestCodexAntSkillGuideSupport|TestCommandGuideBuildSmoke)$' -count=1 -timeout 90m</automated></verify>
  <done>Generated public skills, guides and private shared bodies describe one nine-command invocation surface.</done>
</task>
</tasks>
<verification>
Run the focused display/guide tests; check actual outputs and stable machine fields. These wording changes need meaningful passing assertions, not mutation testing of prose.
</verification>
<success_criteria>
All nine public display names and support references agree. No later-phase action or stronger workflow capability is advertised.
</success_criteria>

## Flagged assumption

ANT-03's edge probe returned unclassified. This remains an explicit unresolved probe item; the independent verification in Plan 07 must inspect actual installed guidance and record whether any additional behavioral edge was found. The concrete display/guidance requirements above are still mandatory and tested.

## Artifacts this phase produces

- New test symbols in existing files: TestCodexAntSkillDisplayRoutes, TestCodexAntSkillRuntimeIdentity and TestCodexAntSkillGuideSupport.
- No new runtime command, CLI flag, worker ID or state schema. Existing display functions gain nine-entry availability awareness; existing guides gain private support path presentation.

<output>Create .planning/phases/204.1-codex-ant-skill-surface/204.1-04-SUMMARY.md.</output>
