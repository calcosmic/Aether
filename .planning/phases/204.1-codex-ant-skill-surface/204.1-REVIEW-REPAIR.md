---
phase: 204.1-codex-ant-skill-surface
finding: CR-01
repaired: 2026-09-16T16:11:48.829586+00:00
status: repaired_pending_parent_rereview
production_commit: 3e03c4f0362676fec882969ef0a2210cd573cf7e
phase_verification: human_needed
owner_acceptance: not_performed
---

# Phase 204.1 review repair — CR-01

The confirmed unstamped-install blocker is repaired. A current binary built without linker flags can run its actual embedded install from an empty consumer directory with fresh HOME/CODEX_HOME/XDG/AETHER_HUB_DIR. Older stamped executables and genuinely too-new payload requirements still refuse before destination mutation. All final live sessions were rerun after the production repair commit.

In plain English: the installer now checks the version built into the executable itself, so it can recognize a current Go build on a fresh machine without trusting newer files elsewhere.

## Atomic implementation and production trace

Commit **`3e03c4f0362676fec882969ef0a2210cd573cf7e`** — `fix(204.1): bind Codex skill compatibility to executable metadata`. Normal repository pre-commit hook ran; no bypass, no deleted tracked files. `commit-implementation.json/.raw` and `commit-3e03c4f0.json` record the exact exit, branch, file list and hook digest.

- `embedded_assets.go` embeds the existing source `.aether/version.json` and provides `SourceReleaseVersion`. `MaterializeInstallPackage` consequently includes that same metadata in the bundled package.
- `cmd/root.go` adds `executingRuntimeVersion`: an explicit `cmd.Version` stamp wins; otherwise the immutable embedded release version supplies compatibility identity. It does not read desired package bytes, installed hub versions or the working directory. The existing display resolver is unchanged.
- `cmd/codex_skill_surface.go` uses this identity for the runtime minimum and generator identity. Normal install traces through `runInstall` → `resolveInstallPackageDir` → `MaterializeInstallPackage` → `readRepoVersion` → `setupInstallHub` → `buildCodexSkillPayload`/validation → publication → platform home synchronization. No explicit package-dir or skip-build flag is used by the causal fresh-home regression.
- Validation stays before publication/synchronization mutation and is shared by published-envelope loading/update. No dev-version exemption was added. The shared black-box builder remains stamped for its existing release tests; its misleading comment is corrected and a distinct unstamped executable regression is retained in `cmd/codex_skill_bootstrap_test.go`.

Only the five owned implementation/test files above changed in the production commit. The remaining owned changes are the fresh receipt, verification and this repair record. No state/roadmap/requirement or original review/Plan 07 summary changes were made.

## Causal reproduction, green checks and controls

`bootstrap-red` builds without `-ldflags` and executes plain embedded `install` outside the source checkout: actual install exit **2**, runtime `0.0.0-dev` older than `1.0.79`; Go test exit **1**. This is a current local build with the same missing-stamp condition as documented `go install`, not a downloaded published-release qualification.

`bootstrap-controls-red` demonstrates the other unsafe branch: high external versions permit a too-new publication under the former resolver; Go test exit **1**. After the minimal repair, the bootstrap group is **3/3 top-level tests**, **5 pass / 0 fail / 0 skip**, exit **0**. It asserts the complete hub payload, exactly nine public skills, three private support files, matching ownership bytes/modes/identity, source release version and generator identity. Both deliberately stamped `1.0.78` install controls exit **2** and preserve their destinations, including the high-version external source/hub/cwd control. The unstamped high-minimum control exercises publication, home sync and the envelope loader without allowing external `999.0.0` metadata to authorize execution.

The supplemental final unstamped proof retains its executable, actual embedded publication, ownership manifest, raw command outputs and complete before/after inventories. Its too-new **normal update** exits **1**, reports `state_effect: none` / `outcome: no_change`, and leaves destination paths, bytes and modes equal.

| Check | Discovered / top-level executed | Pass / fail / skip events | Raw exit |
| --- | --- | --- | --- |
| `bootstrap-red` | 1/1 | 0 / 1 / 0 | 1 |
| `bootstrap-controls-red` | 1/1 | 0 / 1 / 0 | 1 |
| `bootstrap-green` | 3/3 | 5 / 0 / 0 | 0 |
| `focused-normal` | 50/50 | 377 / 0 / 2 | 0 |
| `focused-race` | 50/50 | 377 / 0 / 2 | 0 |
| `live-final` | 3/3 | 151 / 0 / 0 | 0 |
| `receipt-replay` | 1/1 | 128 / 0 / 0 | 0 |
| `contract-final-receipt` | 2/2 | 5 / 0 / 0 | 0 |

All raw logs and command/exit records are under **`/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/review-repair/`**. `focused-discovery.json` and `live-discovery.json` bind the corresponding exact selected/executed identities. The two focused skips do not replace live proof. The live run completes **18 positives + four real controls**, with all provider exits **0** and expected missing-skill/wrong-payload evidence rejections. Replay revalidates the actual new raw events and installed bytes, not prose summaries. The unchanged existing live harness and environment-based receipt replay were used.

## Final candidate bindings

- Production repair commit / build source: **`3e03c4f0362676fec882969ef0a2210cd573cf7e`**. Both live builds followed this commit with empty recorded worktree status and production patch.
- Production-input digest: **`sha256:e76a724600b37167ab2f68b18837a475e93d0e2c530bd352e8463c9e819d44a4`**, independently checked against **1702** tracked inputs in each live scenario. Test files and planning prose are outside this aggregate.
- Live candidate version **1.0.79**, SHA-256: `sha256:dc9e021e47b451f3048aca20dbde735aab89c816b8fb710851c9ac1b11516c71`.
- Payload identity: `sha256:eb6d8954de3a10d65e749434d9da0a7339b9aa13b0e6b715f46cce7afed92b0e`; source version **1.0.79**. The manifest differs from historical Plan 07 only in generator identity (the preserved raw Git build stamp); all 12 skill/support file digests are unchanged.
- Client **codex-cli 0.154.0**, SHA-256 **`sha256:4f85982624b3898c8991cb80c0981b2aa71070e3537046c9a95950318a95afcc`**; inherited **gpt-6-astra / xhigh**. Actual arguments and isolated discovery roots are retained in every case.
- Final clean and upgrade capture roots: `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/review-repair/fresh-host-clean-2786538153` and `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/review-repair/fresh-host-upgrade-1568907993`.
- [Current fresh-session receipt](evidence/fresh-codex-skills.json): **22 new cases**, **252 indexed artifacts** with verified SHA-256 values; 18 qualified positives and four intentional evidence rejections. All 22 client exits are **0**, all temporary auth copies are removed, and disposable project snapshots are unchanged.
- Current receipt file SHA-256: **`9309bf1af888417650e55d808693dc208a4ee3c2e10ea43d7c4e95a60a1f6178`**. Per-case raw event/stderr hashes and exact tool-event references are indexed in that receipt.
- Retained final unstamped executable: `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/review-repair/bootstrap-final-retry1/aether-unstamped`, SHA-256 **`835e22995b0a181f21a0ac0724a750cb9d58c10d180ecb8b539f04e5614381b6`**. Plain embedded install exits **0**, publishes all 12 payload files, installs nine public skills plus three private supports and the ownership manifest; actual normal update with minimum `999.0.0` exits **1** with identical before/after destination inventories, bytes and modes.
- `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/review-repair/final-source-check-receipt.json` binds read-only source-check to the final live candidate: exit **0**, 128 Claude/OpenCode wrappers plus 12 Codex outputs, no findings. `final-binding-and-preservation.json` independently verifies source, candidate, captures, guide contract and protected-file identities.

Raw Go VCS metadata is preserved separately from this nested worktree's actual source revision in `candidate-build-info.txt` and `final-binding-and-preservation.json`. Final candidate source-check passes. The original Plan 07 source `f5449c61…`, its receipt, captures and attempt failures remain historical and are not relabelled.

## Deviations and evidence-only correction

1. **Authorized Rule 1 review repair:** this bounded necessary correction follows the already-authorized execute-phase 204.1. It does not create a new plan or change the roadmap. GSD's generic state/summary close-out is narrowed by the explicit user instruction: the original Plan 07 summary and shared tracking stay unchanged; this repair record supplies the new close-out evidence.
2. **Additional narrow proof:** besides the required regression and exact final live matrix, a retained unstamped executable runs embedded install and the actual normal-update high-minimum refusal. This strengthens final-source binding without a broad suite or another preliminary live matrix.
3. **Proof-script assertion correction:** `bootstrap-final` expected exit 2 for normal update; the executable correctly returned exit 1 with the intended refusal. The script assertion therefore exited 1. The separately retained `bootstrap-final-retry1` uses the observed update contract, exits 0 overall, and verifies no mutation. Neither attempt overwrites the other; no production code was changed after commit `3e03c4f0`.

No other ownership expansion, code change or approval was inferred. All final production changes precede both live scenario builds.

## Preservation and remaining limits

The original review finding is unchanged and the parent owns rereview. Original Plan 07 summary, STATE, ROADMAP, REQUIREMENTS and the original Phase 205 inventory retain their starting hashes. The latter remains **`3b2b678284a26332addeadd2bd700bd4f51c4e2b0e6907a987116c85e5c2a8dd`**. Prior receipts are preserved under their original durable roots plus an explicitly historical copy here. No shared installs/configuration, owner-project access, helpers or other agents were used; live Codex sessions are the explicitly authorized disposable read-only harness sessions, with temporary authentication removed.

Verification remains **human_needed**, retaining **ANT-03/unclassified and both descriptor-less prohibitions as three flagged-unverified items**. All **17 inherited failures** and their names remain in the verification and receipt. The interrupted parent Wave 5 broad gate is **not a pass**; no broad suite was run here. Parent owns the post-merge broad gate and rereview. No phase completion, flag clearance, owner acceptance or public release is claimed. Phase 204.2 remains the next implementation dependency, release qualification belongs to 204.5, and owner acceptance belongs to 205.
