# Phase 204.1 Wave 3 integration gate

The merged build passed. The complete command-suite census ran **5,609/5,609 tests across 59 lanes**; all 18 other Go packages passed. The raw full-suite exit remains **1**: the same 17 historical failure identities and two newly introduced test-fixture failures. All 29 Wave 2 presentation regressions are resolved.

The two new failures were repaired without changing production code:

- `TestCodexLiteralCommandGuidanceStaysMinimal`: replace obsolete literal wording with the templates' equivalent explicit no-preface and one-sentence requirements. Both constraints remain asserted in both templates.
- `TestDeterministicCommandsNoSpawn/update`: use the existing disposable registered-update fixture with a real published payload and known legacy skills. Require installed payload agreement after successful execution, and retain the before/after spawn-tree check. An incomplete hub is no longer mistaken for a successful update fixture.

All seven tests in the two changed files and the related guidance/update group passed **normally and with race detection**, with **33 passing test/subtest events per run and no failures or skips**. The related positive spawn-detection test also passes. These results establish the repairs; they do not rewrite the full suite's raw failing result.

Evidence: `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/`:

- `wave-3/build-result.json`, `wave-3/tests-result.json`, `wave-3/tests.log`, `wave-3/cmd-output.txt`, `wave-3/review.json`
- `wave-3-repair/selection.json`, `wave-3-repair/normal-result.json`, `wave-3-repair/normal.jsonl`, `wave-3-repair/race-result.json`, `wave-3-repair/race.jsonl`

The full-gate candidate was `24040b9ae7c1f5e284b701c2126d986b3e3f5846`. The focused runs include the two test-only repairs in this commit. Schema drift, codebase drift, and UI safety hooks reported no blockers. The existing 17 failures remain visible by identity in `wave-3/review.json`; they are not a waiver for any required Phase 204.1 check. Full candidate/release qualification remains Phase 204.5.

Five numbered plans are implemented (01–05). Plan 06 supplies the remaining deterministic adversarial proof, and Plan 07 supplies all nine real fresh-session clean/upgrade captures and explicit standard verification. Phase completion and ANT requirement credit remain pending that evidence. No shared installed configuration or owner project was changed.
