---
phase: 204.1-codex-ant-skill-surface
work: bounded-post-merge-regression-repair
status: complete
completed: 2026-09-16
scope: supplemental repair outside numbered Plans 02 and 04
requirements-contributed: [ANT-01, ANT-02]
---

# Phase 204.1 Post-Merge Regression Repair

The 26 assigned additional failures now pass: install/publish fixtures supply real private support, compiled CLI fixtures carry the source version, and Classic audit discovery retains the original restoration corpus.

In plain English: the tests now install a complete package and identify their test executable correctly. The historical audit still checks every original study without demanding invented studies for the new ANT phases. Production validation remains strict.

## Scope and attribution

- Worktree: `/Users/callumcowie/repos/Aether/.claude/worktrees/agent-p204-1-regression-20260916`.
- Starting revision: `758d125b1f90071c45c1bf5e687529e756c05c4a`.
- Read the repository and `cmd/` AGENTS instructions, RTK preferences, execute-plan task/commit protocol, phase CONTEXT/RESEARCH, GSD-INTEGRATION, and 204.1-01-SUMMARY.
- Reviewed `wave-1/review.json`, `new-failure-details.json`, and bounded command-output sections. The parent gate discovered/executed all 5,578 command tests and passed 18 other packages. Its separately listed 17 historical failures were neither edited nor selected here.
- Of the 30 additional failures, four remain assigned to Plans 02/04. This repair handles the other 26, not another numbered plan.

### Classic failures predate Plan 01 implementation

Exported revision `51c39f5860e11e83a6234d2c440f09a00c0ff1ab` with `git archive` into `/tmp/aether-2041-preimplementation-51c39f5`. No branch checkout, owner project, or historical artifact was modified. The five explicitly selected checks all failed there, exit 1, because discovery demanded `204.1-CLASSIC-SYNTHESIS.md`:

1. `TestClassicSynthesisAuditOutputIsOrderedAndStable`
2. `TestClassicSynthesisAuditReportsCountsNotPercentages`
3. `TestClassicSynthesisCapabilityRowsAreNotDoubleClaimed`
4. `TestClassicSynthesisEveryArtifactHasEveryMandatorySection`
5. `TestClassicSynthesisPhase203HasEveryMandatorySection`

The audit source was byte-identical at the pre-implementation and repair-start revisions: SHA-256 `610e156e1e9b0fe74b608eadd632608ec84472f33f120e30767a2d3084e4be2f`. These are insertion/discovery failures already present before the installer change, despite appearing in the gate's additional-failure bucket. `classic-attribution.json` records the revisions, source digests, snapshot path, and five actual error messages.

GSD-INTEGRATION retains the existing 72 Classic capability rows and their original owners. Phase 205's SYNTH-07 audit covers seven studies: 199, 200, 201, 202, 202.1, 203, and 204. Phase 205 supplies `205-SYNTH-07-AUDIT.md`, not an eighth synthesis study.

## Changes

### Complete install/publish fixtures

- Added `cmd/codex_skill_fixture_test.go`. Minimal packages copy the actual three checked-in `.aether/skills/colony/aether-colony-*/SKILL.md` support sources. No placeholder support, validator bypass, or skipped skill synchronization was introduced.
- Updated successful install, Codex lifecycle, publish, release-pipeline, and install/setup/update fixtures. This includes formerly false-green install checks that only inspected unrelated copied files after an install error.
- Assertions inspect all nine public skill names and command-guide routes, compare installed private support byte-for-byte with package source, and check ownership entries against installed digests and modes. Existing behavior-specific assertions remain.
- The version-bump regression now simulates a supported stale runtime/hub at 1.0.79 publishing source 1.0.80. It still verifies a single publish updates both hub version files and the result envelope. No product release version changed.
- Replaced `TestCodexInstallPrunesHomeFullSkillMirrorAndWritesShims` with `TestCodexInstallPreservesUnknownHomeSkillAndWritesAntSkills`. Its edited `colony/build-discipline/SKILL.md` survives byte-for-byte while the canonical skill payload is installed and verified.

### Compiled CLI fixture identity

The three CLI journeys install from the real source checkout, so they already had the support files. Their unversioned test binary resolved to `0.0.0-dev` outside that checkout; strict payload validation correctly refused a runtime below 1.0.79. The shared black-box build now supplies the source version through the same linker variable used by release builds. The adapter binary is unchanged. Install-to-seal, provider-backed revision, and install/update/migration journeys all pass through the real executable.

### Bounded Classic discovery

Only `cmd/classic_synthesis_audit_test.go` changed for this fix. Discovery explicitly scopes the seven original studies and rejects a missing original phase directory, missing artifact, or duplicate original phase directory. Existing mandatory-section, capability-ownership, ordering, and count checks remain intact. The shared phase regex keeps both capture groups for its parity-record consumer.

Added 15 discovery subtests: one proves ANT insertions do not expand the corpus; fourteen remove each original artifact/directory in disposable fixtures and require a phase-specific error. A separate check requires the nonempty Phase 205 audit. Existing Phase 205 signed coverage and shared parity tests also passed. No synthesis documents or capability rows were created or altered.

## Verification and durable evidence

Evidence root:

`/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/regression-repair`

Every run has a `*-discovery.txt`, raw `*.jsonl`, and `*-accounting.json` containing the exact command, discovered names, executed test/subtest names, outcomes, and exit code. `run-accounting.py` records the accounting method. `final-accounting.json` maps all 26 original failure names to passing final tests, including the preservation-test rename.

| Run | Top-level discovered/executed | Subtests executed | Result / exit |
|---|---:|---:|---|
| `classic-preimplementation` | 5 / 5 | 0 | All five intended discovery failures reproduced / 1 |
| `fixtures-before` | 21 / 21 | 7 | All 21 assigned fixture/journey tests failed / 1 |
| `classic-after` | 11 / 11 | 15 | Initial focused audit check passed / 0 |
| `fixtures-after` | 48 / 48 | 36 | 47 passed; version-bump fixture still below minimum / 1 |
| `publish-version-final` | 1 / 1 | 0 | Supported stale-version correction passed / 0 |
| **`classic-final`** | **20 / 20** | **15** | **All passed / 0** |
| **`fixtures-final`** | **48 / 48** | **36** | **All passed / 0** |

Final selections: **68 distinct top-level tests and 51 subtests passed; zero skips, zero missing discovered tests, both commands exited 0.**

The final Classic command was:

```sh
go test ./cmd -count=1 -timeout 90m -run '^TestClassic(Synthesis|Parity|CoveragePhase205Signed)' -json
```

The final fixture command used an anchored alternation of the 48 exact names recorded in `fixtures-final-accounting.json`; it includes the three actual CLI journeys plus Plan 01's install tracer, inventory validation, and clean-install collision checks. This was a bounded affected-test selection, not the broad full suite. `gofmt -l` and `git diff --check` were clean.

## Atomic commits and hooks

Each repair was committed separately after verification, with explicit file staging and normal configured hooks. No `--no-verify`, hook override, or blanket staging was used. Commit output receipts and the configured hook identity are retained in the evidence root.

1. `e2cedb653266f3385d5b7de91a3daf372e3c0d30` — scope Classic audit to the original restoration corpus.
2. `114098da5bdc986bbafcf19004811b64d68749f7` — supply real support in install/publish fixtures and verify the installed payload.
3. `a12f23dd59c4ba876e636f0b21a7f88c7fada510` — stamp CLI journey candidates with the source version.

This report is committed separately as `docs(204.1-regression): record bounded repair evidence and handoff`.

## Remaining integration responsibilities

No assigned test remains pending or skipped. These passing checks do not complete the other plans:

- **Plan 02:** exact content-proven retirement/adoption of the frozen fourteen v1.0.79 legacy files, modified/unknown preservation, ownership and concurrent-mutation behavior. This repair's preservation fixture deliberately supplies unknown edited bytes, not a name-based ownership claim. It must continue to survive Plan 02's migration. `platform_sync.go`, `platform_sync_test.go`, `codex_skill_install.go`, and ownership production code were untouched.
- **Plan 03 and later publication work:** published versioned desired payload consumption through ordinary update. Existing publish/home-install and update journey checks pass here; they do not independently prove the later payload distribution contract.
- **Plan 04:** command guides, display/projection, support-source changes, and its two generated-shim tests. Those sources and `command_guide_test.go` were untouched.
- **Parent:** merge sibling work and run the broad integration gate. The 17 separately recorded historical failures remain outside this repair.

## Deviations and boundary checks

No scope deviation or production-code repair was necessary. The supplemental report replaces numbered-plan metadata by explicit owner instruction. `STATE.md`, `ROADMAP.md`, and `REQUIREMENTS.md` were not edited. No sibling changes were reverted, another numbered plan was not started, and shared installed skills/configuration were not modified.

## Self-check: PASSED

All 26 assigned failures map to passing final tests. Every discovered final test executed. Ten changed code files are test files within the authorized scope; the only added planning document is this report. All three task commits exist, hooks ran normally, and none deleted a tracked file. Cross-plan and full-suite responsibilities remain explicitly separate from this completed repair.
