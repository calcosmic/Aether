---
phase: 204.1-codex-ant-skill-surface
plan: "01"
subsystem: infra
tags: [codex, skills, install, transactions, discovery, go]
requires:
  - phase: 204
    provides: Runtime-owned command guides and shared maintenance transactions
provides:
  - Validated deterministic nine-command ant skill payload with private support
  - Registered clean install through exact Codex targets and shared transaction
  - Real fresh-client ant-plan discovery and missing-skill negative-control receipts
affects: [204.1-02, 204.1-04, 204.1-07]
actuals:
  tokens: 20784
  tasks: 2
  commits: 4
tech-stack:
  added: []
  patterns:
    - Versioned skill payload with exact ASCII source inventory and per-file digests
    - Skills and ownership record committed together through maintenance transactions
    - Real host tool events bind discovery to installed bytes and candidate execution
key-files:
  created:
    - cmd/codex_skill_surface.go
    - cmd/codex_skill_install.go
    - cmd/codex_skill_surface_test.go
    - cmd/codex_skill_live_test.go
    - .planning/phases/204.1-codex-ant-skill-surface/evidence/discovery-tracer.json
  modified:
    - cmd/platform_sync.go
    - cmd/install_cmd.go
key-decisions:
  - Keep the selected .codex/skills/aether root; fresh codex-cli 0.154.0 positively discovered it.
  - Install journal lives under the selected disposable home's .aether/data; the Codex target root remains .codex.
  - Reuse only the supported file auth cache and model preferences in isolated child homes; remove temporary credentials afterward.
patterns-established:
  - A missing installer connection must fail the independently specified nine-name tracer.
  - Assistant claims, echoed commands, stale proof fields, and known routes without a present loaded skill never qualify live evidence.
requirements-completed: [ANT-01, ANT-02]
coverage:
  - id: D1
    description: Registered install creates nine public ant skills and private support with transactional ownership.
    requirement: ANT-02
    verification:
      - kind: integration
        ref: cmd/codex_skill_surface_test.go#TestCodexAntSkillInstallTracer
        status: pass
      - kind: integration
        ref: cmd/codex_skill_surface_test.go#TestCodexAntSkillCleanInstallCollision
        status: pass
    human_judgment: false
  - id: D2
    description: Invalid inventories and payloads fail before skill targets are planned or written.
    requirement: ANT-01
    verification:
      - kind: unit
        ref: cmd/codex_skill_surface_test.go#TestCodexAntSkillInventory
        status: pass
    human_judgment: false
  - id: D3
    description: Fresh Codex loads installed ant-plan and runs its first read-only guide; removing the skill prevents qualification.
    requirement: ANT-01
    verification:
      - kind: e2e
        ref: cmd/codex_skill_live_test.go#TestCodexAntSkillFreshHost
        status: pass
      - kind: unit
        ref: cmd/codex_skill_live_test.go#TestCodexAntSkillLiveEvidence
        status: pass
    human_judgment: false
duration: 21min
completed: 2026-09-16
status: complete
---

# Phase 204.1 Plan 01: Clean Installation and Discovery Tracer Summary

**The registered installer now creates nine ant skills in one skill-set transaction, and fresh Codex 0.154.0 sessions prove ant-plan discovery plus rejection when that skill is missing.**

For dummies: a clean install gives Codex the nine expected ant actions. A real Codex session found the installed planning instructions and asked the correct Aether executable for guidance. A second session, with those instructions removed, could not pass the same check.

## Performance

- **Duration:** 21 minutes from recorded execution start.
- **Started:** 2026-09-16T10:39:04Z.
- **Completed:** 2026-09-16T10:59:17Z.
- **Tasks:** 2/2.
- **Files changed:** 8 including this summary.
- **Status:** Complete; no designed stop or outstanding checkpoint.

## Accomplishments

- Exactly `ant-init`, `ant-discuss`, `ant-oracle`, `ant-colonize`, `ant-plan`, `ant-build`, `ant-continue`, `ant-swarm`, and `ant-seal` are generated publicly. Three full support sources remain byte-identical ordinary `support/*.md` files, referenced relative to the installed skill.
- `codex-skill-payload/v1` records source/minimum runtime versions, available generator identity, commands, file paths, modes, and SHA-256 digests. Source inventory validation refuses missing, empty, duplicate, case-variant, Unicode-lookalike, or invalid guide entries. The payload reader also refuses missing files, malformed metadata, incompatible runtime requirements, and content mismatches.
- The registered install controller reaches `planCodexSkillTargets` through `syncCodexSkillsFromPayload`. Its thirteen targets (nine public files, three support files, one ownership record) use `commitMaintenanceMutation`. The former direct-write/prune implementation was replaced by delegation to this same path. Existing unproven canonical destinations are preserved and reported as collisions.
- Default dev operations retain their existing home-sync guard. Explicit dev home opt-in keeps the existing stable-home scope and reports it. Unrelated install/hub behavior remains outside the skill-set transaction.
- Both real fresh processes used disposable repositories and isolated child `HOME`, `CODEX_HOME`, and configuration. No shared installed skills/configuration were edited. Temporary auth cache copies were removed; their contents were never logged or committed.

## Task Commits

All commits ran the configured repository hooks; none used `--no-verify`.

1. **Task 1 RED:** `610d3d6854bc49e8361dd08233b66a22354762c7` — registered-install and collision tests demonstrate the old behavior.
2. **Task 1 GREEN:** `1afd7b0373f684410067c501e5451b17a984f0ac` — validated payload, exact targets, transactional clean install, and inventory checks.
3. **Task 2:** `65650ffdd04d1e1c268272979818f14309fe8818` — opt-in real-client harness, evidence validator, and observed discovery receipt.

**Plan metadata:** this summary is committed separately as `docs(204.1-01): complete clean install and discovery tracer`.

## Verification and Exact Counts

Durable evidence root:
`/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/plan-01`

The checked-in [discovery receipt](evidence/discovery-tracer.json) contains every executed test/subtest name, outcome, raw capture references and digests. Durable `test-accounting.json` repeats that accounting. `test-discovery.txt` records these five discovered top-level tests:

1. `TestCodexAntSkillInstallTracer`
2. `TestCodexAntSkillInventory`
3. `TestCodexAntSkillCleanInstallCollision`
4. `TestCodexAntSkillLiveEvidence`
5. `TestCodexAntSkillFreshHost`

| Check | Actual result | Durable raw file |
|---|---|---|
| Initial task-1 RED | 2 top-level tests executed, both failed on the intended old behavior | `task1-red.jsonl` |
| Task-1 GREEN | 3 top-level tests + 27 subtests executed; all 30 passed, zero skips | `task1-green.jsonl` |
| Installer connection removed | 1 top-level tracer executed and failed: installed names were empty | `task1-missing-generator.jsonl` and `task1-missing-generator-control.json` |
| Restored exact task-1 command | Passed | `task1-required-command.txt` |
| Live-proof validator | 1 top-level test + 9 subtests passed; the separately selected live test skipped because opt-in was off | `task2-validator.jsonl` |
| Explicit real-client check | 1 top-level test + 2 real subtests passed; zero skips | `task2-live.jsonl` |

Task-1 required command, run after restoring the negative-control mutation:

```sh
go test ./cmd -run '^TestCodexAntSkill(InstallTracer|Inventory|CleanInstallCollision)$' -count=1 -timeout 90m
```

Actual explicit live command (the additional `-json` preserves machine-readable raw test events):

```sh
AETHER_CODEX_SKILL_LIVE=1 AETHER_CODEX_SKILL_LIVE_CASES=plan AETHER_CODEX_SKILL_EVIDENCE_DIR=/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/plan-01 go test ./cmd -run '^TestCodexAntSkillFreshHost$' -count=1 -json -timeout 90m
```

The validator suite rejects model-only claims, command-without-load, wrong candidate, no completed turn, echoed invocation, wrong loaded bytes, failed tools, stale evidence fields, and a missing skill even when the expected route is known. Its synthetic fixtures validate parsing only; live qualification comes exclusively from the two actual client processes.

`gofmt -l` and `git diff --check` were clean. No broad full suite or full race suite was run.

## Real Client and Candidate Identity

- **Source revision:** `1afd7b0373f684410067c501e5451b17a984f0ac`.
- **Build-time source status:** only the new, then-untracked `cmd/codex_skill_live_test.go`; compiled production sources were committed and unchanged. The subsequent task-2 commit contains that harness and the receipt.
- **Candidate version:** `1.0.79`.
- **Candidate SHA-256:** `ff152987388228f57c910e9591b54ca55c3d71e0e9727c75ecfe0821c1b67e55`.
- **Client:** `codex-cli 0.154.0`, existing model preference `gpt-6-astra`.
- **Client SHA-256:** `4f85982624b3898c8991cb80c0981b2aa71070e3537046c9a95950318a95afcc`.
- **Payload identity:** `sha256:10d92b57b09f353b4879b291c880a63e3fe879a8d1a25c9534e7bb2ef9fd80bd`.
- **Installed/loaded ant-plan SHA-256:** `5840279bd8d241fb4397067202f282fd0c27e3ba33acd5b9cf80139f56844db4`.
- **Durable run:** `fresh-host-1811947334/` beneath the evidence root, including the actual `bin/aether` candidate.

The positive receipt links successful host `cat` output to the full installed skill bytes, `command -v aether` to that exact candidate, and the successful `AETHER_OUTPUT_MODE=json aether command-guide plan --platform codex` tool call to the returned guide. The negative session reported the skill absent from its discovery catalog and stopped without tool calls. Its evidence validator rejected the absent installed file. Both processes exited 0; their qualification outcomes are separately `qualified` and `rejected-missing-skill`.

The selected discovery roots are the two case-local `home/.codex/skills/aether` directories. No alternate root was tried. Official [authentication guidance](https://learn.chatgpt.com/docs/auth) supports reusing the file cache; isolated command-line settings follow the [configuration reference](https://learn.chatgpt.com/docs/config-file/config-reference). Installed CLI help captures record the actual supported flags.

## Files Created/Modified

- `cmd/codex_skill_surface.go` — public inventory, deterministic payload, strict source/payload validation.
- `cmd/codex_skill_install.go` — exact target planning, initial ownership record, shared transaction adapter.
- `cmd/platform_sync.go` — public ant rendering, relative private-support references, removal of the independent writer/pruner.
- `cmd/install_cmd.go` — registered install wiring and explicit home scope.
- `cmd/codex_skill_surface_test.go` — independent install tracer, inventory cases, collision preservation.
- `cmd/codex_skill_live_test.go` — isolated real client capture and evidence-validation controls.
- `evidence/discovery-tracer.json` — actual receipt, exact accounting, artifact and executable digests.

## Decisions Made

- Retained `.codex/skills/aether` because the required fresh client positively discovered that root. This is scoped evidence for the tested client, not a universal host-version guarantee.
- Kept `.codex` as the typed transaction root. Installation journals use the selected home's `.aether/data`, never the worktree or an owner colony.
- Preserved the public guides' actual runtime routes and full private support bytes. Broader guidance changes remain owned by later plans.
- Credential reuse was limited to temporary file cache copies and safe model preferences; no shared client configuration was imported or modified.

## Deviations from Plan

None in implementation scope or root selection. The live command adds `-json` and the durable evidence directory variable so raw test events survive worktree cleanup.

## Issues Encountered

The first GREEN attempt exposed two test-harness assumptions: lifecycle digests include the `sha256:` prefix, and install errors use stderr. Both were corrected before the passing checks. An evidence-output assertion also assumed an unpadded Go `ok` prefix; the raw command had passed and its result was checked directly. No product check was waived, no credential/provider gate remained, and no discovery failure was hidden.

## TDD Gate Compliance

Task-scoped RED preceded GREEN. Both RED tests failed for the intended missing behavior. The source mutation control later proved the real installer connection is necessary; it was restored byte-for-byte before the final task-1 check and GREEN commit.

## Boundary Preservation

- `STATE.md`, `ROADMAP.md`, and global requirement status were not changed; the orchestrator owns them. Requirement IDs above trace this plan's contributions, not completion of the whole phase.
- The Phase 205 owner project was not accessed. Its original checked-in before-inventory remains SHA-256 `3b2b678284a26332addeadd2bd700bd4f51c4e2b0e6907a987116c85e5c2a8dd`, matching the authoritative baseline.
- Shared installed skills and shared Codex configuration were not changed. No credentials remain in the retained case homes.
- No other plan was started.

## User Setup Required

None. Existing authentication was reused through supported isolated client storage for this run.

## Next Phase Readiness

The clean-install and selected-root tracer prerequisites are met. Existing-install migration, strengthened concurrent mutation checks, versioned publish/update consumption, broad guidance alignment, all-nine real-host proof, and full release qualification remain with their assigned later plans. This receipt proves discovery and entry routing only; it does not prove planning, dispatch, native-worker parity, or owner acceptance.

## Self-Check: PASSED

Both tasks' acceptance criteria and required checks passed. All created files and three task commits exist; all twelve referenced raw artifacts match their stored digests; retained candidate/client digests match the receipt; temporary auth copies are absent. Formatting is clean, and unrelated planning state and Phase 205 inventory have no diff.
