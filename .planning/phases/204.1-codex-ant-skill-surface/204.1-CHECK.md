---
phase: 204.1-codex-ant-skill-surface
checked: 2026-09-16
status: passed
review_iteration: 2
plans_checked: 7
blockers: 0
warnings: 0
review_scope: pre-execution plans only
---

# Phase 204.1 independent plan recheck

## VERIFICATION PASSED

The revision closes the preservation gap: the plans now require an update to stop if someone creates a custom file or changes a file's permissions after the updater decides what it may change. Both the shared commit path and the ordinary update command have explicit tests planned for those cases.

**Verdict:** Seven plans, sixteen tasks and five dependency waves are ready for execution. This is a planning verdict. Implementation, product tests, installation and fresh-client proof have not been performed by this review.

## Revision resolution

The first review found one **BLOCKER**, concerning the complete file state used to authorize a change. It is resolved in the current plans; no remaining issue or warning was found.

| Required correction | Current executable coverage | Result |
|---|---|---|
| Preserve original existence, content and permissions | Plan 02 interface and T1 capture each skill, support, retirement and ownership-manifest target from the same `readLifecycleFileState` observation; absent and unchanged targets are included. `ExpectedDigest` carries the original digest or the existing missing sentinel; new `ExpectedMode *os.FileMode` carries an immutable copy of the original permission bits. Manifest ownership evidence is parsed from the captured bytes. | Resolved |
| Validate before mutation, under the shared lock | Plan 02 T2 acquires the Codex-target lock before commit re-prepares the plan, compares every original expectation before target writes or journaled mutation, validates unchanged targets before skipping them, and compares declaration existence/digest/mode to the original expectations. A fresh preview cannot replace them. The lock covers commit/recovery and is released on every exit. | Resolved |
| Retain compatibility for other maintenance callers | Plan 02 distinguishes expected mode from desired mode, treats nonnil zero as an actual expectation, and keeps nil-mode/optional-digest behavior for unrelated callers. `TestCodexAntSkillBaselineCompatibility` names these cases. | Resolved |
| Demonstrate both late-edit refusals at the common boundary | Plan 02 T2 names `TestCodexAntSkillPostPlanCreationRefused` and `TestCodexAntSkillPostPlanModeChangeRefused`; the permission cases include support, retirement and the ownership manifest. They require all post-edit bytes/modes and ownership state to survive, with no transaction journal or success receipt. The plan separately requires breaking each guard to make its test fail. | Resolved |
| Carry the original expectations through ordinary update | Plan 03 T2 appends the shared planner's targets into `runMaintenanceUpdate` before prepare/commit. Plan 02 T1 requires those outputs to retain their original expectations through assembly. Plan 06 T1 uses the default-real lock seam after actual classification/preview, checks the original values reached the boundary, delegates to the real locker and commit, and forbids rebuilding the plan or refreshing expectations. | Resolved |
| Name runnable production-path tests and align supporting documents | Plan 06 T1 names `TestCodexAntSkillUpdatePostPlanCreationRefused` and `TestCodexAntSkillUpdatePostPlanModeChangeRefused`, both on normal `rootCmd.Execute` update. Its verify command, acceptance criteria, done condition, test inventory and final proof census include them. RESEARCH and SOURCE-AUDIT describe the same complete-state contract. | Resolved |

The bounded source inspection confirms that the amendment addresses the actual seam. `cmd/platform_sync.go:31` currently has only `ExpectedDigest`; preparation checks it only when nonempty at `:192`; commit re-prepares at `:224` and compares declaration modes to that newer preview at `:267`. `cmd/lifecycle_transaction.go:1611` already supplies the missing sentinel, existence, permission bits and bytes, so the proposed capture contract does not depend on an invented existing symbol. `cmd/update_cmd.go:173` previews the assembled plan and `:192` commits that same plan, providing the exact handoff exercised by the added update tests. `ExpectedMode` and `maintenanceCodexSkillLocker` are correctly identified as proposed additions.

## Coverage summary

**Phase goal:** Make the existing nine Codex workflows available under canonical `$ant-*` skills, with reliable discovery, installation, upgrades and matching guidance.

| Requirement | Covering plans/tasks | Status |
|---|---|---|
| ANT-01 — nine canonical names, actual discovery/invocation, bounded legacy migration and private helpers | 01 T1–2; 02 T1; 04 T2; 07 T1 | Covered |
| ANT-02 — versioned install/publish/update, repeatability, custom preservation, channels and failed-update rollback | 01 T1; 02 T1–2; 03 T1–2; 06 T1–2 | Covered, including the corrected original-state contract |
| ANT-03 — installed instructions, guides, help and next actions agree while machine identities remain stable | 04 T1–2; 05 T1–3; 06 T2; 07 T2–3 | Covered |

All three roadmap requirement IDs appear in plan frontmatter and have substantive tasks. The current plan set preserves the prior review's complete coverage of relevant PROJECT requirements and owner decisions. No later-phase requirement was pulled into this revision or silently dropped.

## Plan summary

| Plan | Tasks | Listed files | Wave | Status |
|---|---:|---:|---:|---|
| 01 | 2 | 7 | 1 | Valid |
| 02 | 2 | 6 | 2 | Valid |
| 03 | 2 | 7 | 3 | Valid |
| 04 | 2 | 10 | 2 | Valid |
| 05 | 3 | 11 | 3 | Valid |
| 06 | 2 | 5 | 4 | Valid |
| 07 | 3 | 5 | 5 | Valid |

Dependencies remain acyclic and wave-correct: 01 precedes 02/04; 03 needs 02/04; 05 needs 04; 06 needs 03/05; 07 needs 06. No same-wave file ownership overlaps. The revision adds no task or file. The prior sizing judgment remains appropriate: Plans 04/05 include bounded synchronized wording/support files, with no task spanning ten files; those file counts do not require a new split.

## Dimension results

| Dimension | Result | Basis |
|---|---|---|
| Requirement coverage | PASS | ANT-01/02/03 have specific actions and acceptance evidence; the prior ANT-02 gap is closed. |
| Task completeness | PASS | All sixteen tasks have files, read_first, action, acceptance_criteria, verify and done; new field, seam and test symbols are explicit. |
| Dependency correctness | PASS | Existing references, increasing waves and exclusive same-wave file ownership independently checked. |
| Key links planned | PASS | Shared payload and ownership planner reach install/publish and registered update; skills/removals/manifest stay within the update transaction. |
| Scope sanity | PASS | Seven bounded plans retain the previously reviewed two-or-three-task structure and file scope. |
| Verification derivation | PASS | User-visible truths map to actual registered callers, preservation assertions, causal controls and separately identified live proof. |
| Context compliance | PASS | Exactly init/discuss/oracle/colonize/plan/build/continue/swarm/seal; private support, stable runtime identities, proportionate existing team behavior and explicit verification remain. |
| Scope reduction | PASS | No new reduction. Full command coverage, native helpers, recruitment/worktrees/Swarm and release qualification keep their agreed later-phase ownership. |
| Architectural tier compliance | SKIPPED | No Architectural Responsibility Map section in RESEARCH. Go remains the state and authorization authority. |
| Nyquist compliance | SKIPPED | `workflow.nyquist_validation` is explicitly false. |
| Cross-plan data contracts | PASS | Original absence/digest/mode now survive classification, plan assembly, locked revalidation and declaration checks; other payload/receipt contracts remain consistent. |
| AGENTS.md compliance | PASS | Source-owned changes, literal command behavior, isolated proof, named meaningful checks and the project timeout/count rules remain respected. |
| Research resolution | PASS | Selected design and Classic synthesis are retained; no unresolved Open Questions section. The fresh-client assumption remains an explicit early execution gate. |
| Pattern compliance | PASS | Existing transaction/generator/display/document/test analogs are retained. RESEARCH explicitly corrects the older pattern map's digest-only guidance without inventing a second distribution design. |

## Retained evidence boundaries

- The five edge probes still have five dispositions: four explicit predicates and the flagged ANT-03 unclassified item. The concurrency predicate now has the missing concrete capture/commit/test contract.
- Both descriptor-less prohibitions remain in Plan 07's supported statement/status form, without fabricated check descriptors or automatic verification credit.
- Plan 01 requires a genuine fresh-client discovery result before expansion. A failed root experiment requires a focused revision; it cannot silently switch roots or widen containment.
- Plan 07 still requires all nine entrypoints after clean installation and normal upgrade, raw evidence tied to the candidate bytes, negative controls and standard `204.1-VERIFICATION.md`. Unavailable host proof prevents a passed implementation verdict.
- Custom/unknown content and newer inventories remain protected. Preview stays read-only; failed/interrupted unverified recovery remains distinct from refused rollback of a verified receipt.
- The Phase 205 original inventory, owner project and personal acceptance remain untouched and outside this review.

## Checks performed

Reused the completed pattern map and first review, reread all current plans and the affected research/coverage contracts, and inspected only the current common preparation/commit, file-state and registered-update seams for the correction. Independently ran `gsd-sdk query init.phase-op 204.1` and `query verify.plan-structure` on all seven plans: every structure is valid, with no errors or warnings. Independently parsed YAML must_haves and checked task fields, requirement union, dependency waves and same-wave file overlap.

No product tests, live sessions, installations, source/plan/root-state edits, commits or owner-evidence changes were performed. This report is the review's sole output; `.gsd/` was preserved.

## Structured issues

```yaml
issues: []
```

The Revision Gate passes on review 2: one prior blocker resolved, zero remaining blockers or warnings. The orchestrator may mark the plans ready for `$gsd-execute-phase 204.1`; this does not mark the phase implementation complete.
