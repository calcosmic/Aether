---
phase: 204.1-codex-ant-skill-surface
plan: "04"
subsystem: cli
tags: [codex, ant-skills, command-guide, lifecycle, go]
requires:
  - phase: 204.1-01
    provides: Nine-command inventory, transactional disposable install, private support payload, and fresh Codex root-discovery receipt
provides:
  - Inventory-aware ant names in actual root help, first-run output, and lifecycle next actions
  - Stable runtime commands, IDs, source facts, and environment-prefixed shell invocations
  - Nine command guides and generated public skills resolving installed private support files
affects: [204.1-05, 204.1-07, 204.5]
actuals:
  tokens: 17998
  tasks: 2
  commits: 5
tech-stack:
  added: []
  patterns:
    - Public naming remains a display boundary over the existing runtime inventory
    - Installed support references resolve relative to the selected public SKILL.md
key-files:
  created:
    - .planning/phases/204.1-codex-ant-skill-surface/204.1-04-SUMMARY.md
  modified:
    - cmd/codex_visuals.go
    - cmd/lifecycle_projection.go
    - cmd/root.go
    - cmd/command_guide.go
    - cmd/codex_visuals_test.go
    - cmd/lifecycle_projection_test.go
    - cmd/classic_command_parity_test.go
    - cmd/command_guide_test.go
    - cmd/hint_translation_test.go
    - cmd/root_test.go
    - cmd/ux_firstrun_test.go
    - .aether/skills/colony/aether-colony-creation/SKILL.md
    - .aether/skills/colony/aether-colony-research/SKILL.md
    - .aether/skills/colony/aether-colony-build-cycle/SKILL.md
key-decisions:
  - Reused Plan 01's nine-command production inventory and proven .codex/skills/aether root without modifying shared installations.
  - Included colonize and continue in the existing help renderer because its historical journey catalogue omitted them.
  - Retained SkillReference as the internal YAML identity while rendering resolvable private support paths.
patterns-established:
  - Independent expected names and exact routes verify both generated skills and registered command-guide output.
  - Missing installed support is a real filesystem negative control; text-only evidence does not claim fresh-client lifecycle parity.
requirements-completed: [ANT-01, ANT-03]
coverage:
  - id: D1
    description: All nine public ant names appear in help and next actions, with first-run and unsupported-route checks.
    requirement: ANT-01
    verification:
      - kind: integration
        ref: cmd/codex_visuals_test.go#TestCodexAntSkillDisplayRoutes
        status: pass
    human_judgment: false
  - id: D2
    description: Display translation preserves executable commands, argument bytes, structured IDs and facts, and other-platform routes.
    requirement: ANT-03
    verification:
      - kind: unit
        ref: cmd/lifecycle_projection_test.go#TestCodexAntSkillRuntimeIdentity
        status: pass
      - kind: unit
        ref: cmd/lifecycle_projection_test.go#TestLifecycleProjectionAcceptedPlanChoicesAreCoequal
        status: pass
    human_judgment: false
  - id: D3
    description: Nine generated public skills and actual command guides resolve the three installed private support documents.
    requirement: ANT-03
    verification:
      - kind: integration
        ref: cmd/command_guide_test.go#TestCodexAntSkillGuideSupport
        status: pass
      - kind: unit
        ref: cmd/command_guide_test.go#TestCodexGeneratedCommandShimsCoverIntelligentCommands
        status: pass
      - kind: unit
        ref: cmd/command_guide_test.go#TestCodexGeneratedShimsIncludeCommandGuideSkills
        status: pass
    human_judgment: false
duration: 16min
completed: 2026-09-16
status: complete
---

# Phase 204.1 Plan 04: Codex Ant Display and Guidance Summary

**Nine public ant skills now appear in help, first-run output, next actions, and command guides while executable Aether routes retain their identities.**

For dummies: Codex now shows the names the owner can select. The program still runs the same commands and checks; its helper instructions are ordinary installed support files rather than extra menu entries.

## Performance

- **Recorded execution/evidence window:** 2026-09-16T11:30:26Z through 2026-09-16T11:46:10Z; approximately 16 minutes. Initial context loading preceded this measured window.
- **Tasks:** 2/2, complete.
- **Files changed:** 14 implementation/test/support files plus this summary.
- **Commits:** 4 task commits plus this summary commit; all normal hooks enabled.
- **Actual tokens:** changed diff characters divided by four, including this summary; not a harness token-usage claim.

## Accomplishments

- Shared display formatting consumes `codexPublicSkillCommands`. Actual root help includes all nine names; lifecycle choices display `$ant-build 1` alongside `aether run` without ranking either. First-run output shows `$ant-init` while setup/status stay executable CLI text.
- Structured runtime commands, IDs, provenance, evidence, and other semantic fields remain unchanged. Tests cover quoted and Unicode arguments, extra/trailing spaces, command-looking text inside quotes, quoted environment values, and unsupported/finalizer routes. Claude/OpenCode retain slash-command presentation.
- Command guides retain exact `Command`, `RunCommand`, `SkillReference`, owner-answer rules, raw bypass, and runtime authority. Public instructions read `../support/*.md` relative to the installed skill. The three full support sources remain ordinary installed `.md` files; there are exactly nine public `SKILL.md` entries.
- The two assigned Wave 1 shim failures are corrected. Their replacement expectations independently specify all nine names, routes and support identities; the previous safety assertions remain exercised.

## Task Commits

1. **Task 1 RED:** `42eada4826a915cddbf7a8411be98228a27c82a9` — failing real display and runtime-identity tests.
2. **Task 1 GREEN:** `fae31cb6ca6b0112f53e2de5177b182aa134a651` — shared display formatting and complete help inventory.
3. **Task 1 caller regression follow-up:** `642aae78f4ed5505ce67f80c89aba89ec0dff44b` — narrowly update pre-existing help/first-run/hint spelling expectations.
4. **Task 2:** `688236b29b04b445d754537e6f16fffac08099ae` — installed-support guidance, exact generated routes, and assigned Wave 1 test corrections.

**Plan metadata:** committed separately as `docs(204.1-04): complete ant display and guidance plan`.

## Verification and Durable Evidence

Evidence root: `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/plan-04/`.

`test-accounting.json` records every discovered test and every executed test/subtest identity. Each `*.receipt.json` records exact argv, working directory, branch, source HEAD/status, timestamps, raw process exit status, result counts, and raw-output SHA-256. `source-identity.json` binds the tested changes to production commit `688236b29b04b445d754537e6f16fffac08099ae`; `evidence-manifest.json` hashes the durable artifacts. Raw actual help/first-run output and registered guide routes are retained in the test logs.

| Check | Exact result | Raw evidence |
|---|---|---|
| Task 1 initial RED | Exit 1; 4 top-level tests, 28 total events: 12 pass / 16 fail / 0 skip | `task1-red.raw` |
| Task 1 required check after implementation | Exit 0; 4 top-level tests, 30 total pass / 0 fail / 0 skip | `task1-required-command.raw` |
| Task 2 before guidance change | Exit 1; 4 top-level tests, 23 total: 12 pass / 11 fail / 0 skip | `task2-before-guidance.raw` |
| Task 2 required check | Exit 0; 3 top-level tests, 22 total pass / 0 fail / 0 skip | `task2-required-command.raw` |
| Final discovery | Exit 0; exactly 65 distinct top-level test names | `final-discovery.raw` |
| Final normal focused check | Exit 0; 65/65 discovered tests executed; 172 total pass / 0 fail / 0 skip | `final-normal.raw` |
| Final race focused check | Exit 0; same 65/65 tests and 172 total pass / 0 fail / 0 skip; no race report | `final-race.raw` |

The plan's two exact `-run` selections were executed with `-count=1 -timeout 90m`; `-json` was added solely to preserve raw machine-readable events. The final focused selection includes every named plan check, all 38 tests in `command_guide_test.go`, directly affected display/caller tests, and unchanged `TestMaintenanceWrapperContract`. The exact final regex is saved in `p204-04-final-regex`; normal and race argv are in their receipts. No broad full suite ran and no historical 17-failure waiver was used.

Negative controls are actual failures: pre-implementation display tests rejected the old output; pre-change support tests rejected the old guide instructions; deleting the installed build-cycle support file in a disposable production-install fixture makes reference resolution fail with a missing-file error. The passing suite requires that refusal. These controls prove presentation and file resolution, not a new native worker or full lifecycle journey.

`git diff --check` and formatting checks passed. Every commit used the configured hooks at `/Users/callumcowie/repos/Aether/.git/hooks`. Task 2's hook exited 0 and printed `Package validation completed with warnings`; the hook treats package validation as advisory and suppresses its detailed stderr. This is recorded in `task2-commit.raw`, not represented as a clean package-validation gate.

## Files Created/Modified

- `cmd/codex_visuals.go`, `cmd/lifecycle_projection.go`, `cmd/root.go`: inventory-aware display boundaries and complete public help.
- `cmd/command_guide.go`: public invocation wording, relative private-support instructions, and host-specific suggestions.
- Seven test files listed in frontmatter: independent names/routes, actual registered callers, argument/identity invariants, support resolution and existing authority checks.
- Three `.aether/skills/colony/aether-colony-*/SKILL.md` sources: installed support presentation and supported closeout spelling.
- This summary; raw evidence is stored outside the worktree at the required durable location.

## Decisions Made

- Reused Plan 01's production installer/payload interfaces and its fresh-client root-selection receipt. The original receipt remains unchanged at `evidence/discovery-tracer.json` (SHA-256 `9def2938442ce4fe57649186573712f14141914e78f7da6e8820048cd284152f`). It records Codex CLI 0.154.0 discovering `.codex/skills/aether`. No new paid/fresh client was required or run by this plan, and that old payload receipt is not claimed to qualify the newly changed guidance bytes.
- Kept executable routes and internal helper identities separate from public display names. No runtime command, worker ID, event, flag or schema was introduced.

## Deviations from Plan

### Auto-fixed bounded issues

1. **Rule 2 — Missing critical help coverage:** `root.go` already consumed the formatter but its historical catalogue omitted `colonize` and `continue`. Added missing public inventory entries through existing registered descriptions. Required real-help assertions prove all nine names; other-platform help remains unchanged. Committed in `fae31cb6`.
2. **Rule 1 — Direct caller expectation drift:** five pre-existing tests in `hint_translation_test.go`, `root_test.go`, and `ux_firstrun_test.go` asserted the superseded raw Codex spelling. Changed only the relevant supported-name expectations, retaining lifecycle ordering, specification/plan authority, unavailable-route prohibitions, platform behavior and aligned columns. Their focused before-run exited 1; after-run and both final runs exit 0. Committed in `642aae78`.

These four additional files are necessary bounded deviations. No unrelated Wave 1 fixture repairs were taken from the separate regression executor.

## Issues Encountered

- The wider guide regression initially found the old init `$ant-*` prohibition; corrected its supported-name expectation while retaining unsupported spec/status and state-authority guards. All final checks pass. Intermediate failures and their exact identities remain in durable receipts.
- ANT-03's previously unclassified edge probe remains the explicit Plan 07 verification item. This plan completes its concrete display/guidance requirements only.

## Preservation and User Setup

No shared installed skills/configuration or Phase 205 owner project was changed. No shared tracking file was edited. Final hashes match the initial hashes for `STATE.md`, `ROADMAP.md`, `REQUIREMENTS.md`, Plan 01's discovery receipt, and the original Phase 205 before-inventory. The before-inventory SHA-256 remains `3b2b678284a26332addeadd2bd700bd4f51c4e2b0e6907a987116c85e5c2a8dd`.

None — no user setup is required for this source-only plan.

## Next Phase Readiness

Ready for the orchestrator to merge Plan 04 and continue its existing sequence. Plan 05 owns broader generated/project documentation; Plan 07 owns fresh-client proof of all nine updated skills and the unresolved edge-probe disposition. Full workflow coverage, native-worker parity, release qualification and owner acceptance remain later work. No other plan was started.

## Self-Check

All four task commits exist; all 65 discovered focused tests executed in normal and race modes; all 172 results in each run pass without skips. Durable evidence and original inventory hashes were verified. This summary is committed before returning PLAN COMPLETE.
