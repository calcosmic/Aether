---
phase: 204.1-codex-ant-skill-surface
verified: 2026-09-16T16:11:48.829586+00:00
status: human_needed
score: "4/4 bounded technical success criteria evidenced; 1 probe and 2 descriptor-less prohibitions remain flagged-unverified"
behavior_unverified: 3
overrides_applied: 0
source_revision: 3e03c4f0362676fec882969ef0a2210cd573cf7e
historical_source_revision: f5449c61868cf0458c071d7253cd2261c3f2e9da
requirements: [ANT-01, ANT-02, ANT-03]
owner_acceptance: not_performed
human_verification:
  - id: ANT-03-unclassified
    status: flagged-unverified
    reason: Installed-guidance review is recorded, but the descriptor-less probe has no authorized stronger disposition.
  - id: prohibition-complete-parity
    status: flagged-unverified
    reason: Preserve the original projectProhibitions item without inventing a check descriptor or approval.
  - id: prohibition-owner-acceptance
    status: flagged-unverified
    reason: Assistant evidence cannot supply the owners Phase 205 acceptance.
---

# Phase 204.1: Codex Ant Skill Surface — Verification

## Review repair CR-01 and current final candidate

**CR-01 is repaired and the final live proof has been rerun against the committed repair.** Compatibility now uses the executing binary's explicit `cmd.Version` stamp or its immutable embedded source release metadata. The same embedded `.aether/version.json` accompanies the normal materialized install package, so publication identifies its own current source. External desired packages, mutable hubs and working checkouts cannot authorize runtime compatibility. The display-oriented resolver remains unchanged.

In plain English: a current Go-installed binary can set itself up on an empty machine. An old executable still cannot pass the minimum-version check merely because newer files sit beside it.

`TestCodexAntSkillUnstampedEmbeddedInstall` was added and run before production changes: unstamped `go build` succeeded, then plain `aether install` outside the checkout exited **2** with `runtime 0.0.0-dev is older than required 1.0.79`. `TestCodexAntSkillUnstampedMinimumUsesExecutable` separately failed before repair because mutable high-version metadata authorized incompatible publication. Both causal RED records remain durable. After repair, the new bootstrap group passes; a deliberately stamped **1.0.78** executable exits **2** on both fresh embedded install and install against high-version source/hub/cwd controls, before destination mutation. A too-new minimum is also refused by publication, home synchronization, envelope loading and the retained executable's normal update.

- Production repair commit / build source: **`3e03c4f0362676fec882969ef0a2210cd573cf7e`**. Both live builds followed this commit with empty recorded worktree status and production patch.
- Production-input digest: **`sha256:e76a724600b37167ab2f68b18837a475e93d0e2c530bd352e8463c9e819d44a4`**, independently checked against **1702** tracked inputs in each live scenario. Test files and planning prose are outside this aggregate.
- Live candidate version **1.0.79**, SHA-256: `sha256:dc9e021e47b451f3048aca20dbde735aab89c816b8fb710851c9ac1b11516c71`.
- Payload identity: `sha256:eb6d8954de3a10d65e749434d9da0a7339b9aa13b0e6b715f46cce7afed92b0e`; source version **1.0.79**. The manifest differs from historical Plan 07 only in generator identity (the preserved raw Git build stamp); all 12 skill/support file digests are unchanged.
- Client **codex-cli 0.154.0**, SHA-256 **`sha256:4f85982624b3898c8991cb80c0981b2aa71070e3537046c9a95950318a95afcc`**; inherited **gpt-6-astra / xhigh**. Actual arguments and isolated discovery roots are retained in every case.
- Final clean and upgrade capture roots: `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/review-repair/fresh-host-clean-2786538153` and `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/review-repair/fresh-host-upgrade-1568907993`.
- [Current fresh-session receipt](evidence/fresh-codex-skills.json): **22 new cases**, **252 indexed artifacts** with verified SHA-256 values; 18 qualified positives and four intentional evidence rejections. All 22 client exits are **0**, all temporary auth copies are removed, and disposable project snapshots are unchanged.
- Current receipt file SHA-256: **`9309bf1af888417650e55d808693dc208a4ee3c2e10ea43d7c4e95a60a1f6178`**. Per-case raw event/stderr hashes and exact tool-event references are indexed in that receipt.
- Retained final unstamped executable: `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/review-repair/bootstrap-final-retry1/aether-unstamped`, SHA-256 **`835e22995b0a181f21a0ac0724a750cb9d58c10d180ecb8b539f04e5614381b6`**. Plain embedded install exits **0**, publishes all 12 payload files, installs nine public skills plus three private supports and the ownership manifest; actual normal update with minimum `999.0.0` exits **1** with identical before/after destination inventories, bytes and modes.
- `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/review-repair/final-source-check-receipt.json` binds read-only source-check to the final live candidate: exit **0**, 128 Claude/OpenCode wrappers plus 12 Codex outputs, no findings. `final-binding-and-preservation.json` independently verifies source, candidate, captures, guide contract and protected-file identities.

Go's raw embedded VCS stamp remains separately recorded in each `candidate-build-info.txt` and the binding audit. This nested worktree's `.git` file can lead Go to use the enclosing checkout's revision; that stamp is not relabelled as the worktree HEAD. The independent production-input inventories and actual build records bind this candidate to `3e03c4f0362676fec882969ef0a2210cd573cf7e`.

### Current capture references

Cells identify **skill read / guide / support read** as `line#item` in `<clean-or-upgrade-run>/<command>/events.jsonl` at the roots above. All entries qualify; full binary-resolution events, raw hashes and four rejecting control records are in the current receipt.

| Entry | Clean events | Upgrade events |
| --- | --- | --- |
| `$ant-init` | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 |
| `$ant-discuss` | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 |
| `$ant-oracle` | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 |
| `$ant-colonize` | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 |
| `$ant-plan` | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 |
| `$ant-build` | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 |
| `$ant-continue` | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 |
| `$ant-swarm` | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 |
| `$ant-seal` | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 |

### Repair checks and counts

Counts include top-level and subtest terminal events; overlapping runs are not additive coverage. Each named run has a durable `.json` command/exit receipt and unfiltered `.raw` output under `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/review-repair/`.

| Check | Discovered / top-level executed | Pass / fail / skip events | Raw exit |
| --- | --- | --- | --- |
| `bootstrap-red` | 1/1 | 0 / 1 / 0 | 1 |
| `bootstrap-controls-red` | 1/1 | 0 / 1 / 0 | 1 |
| `bootstrap-green` | 3/3 | 5 / 0 / 0 | 0 |
| `focused-normal` | 50/50 | 377 / 0 / 2 | 0 |
| `focused-race` | 50/50 | 377 / 0 / 2 | 0 |
| `live-final` | 3/3 | 151 / 0 / 0 | 0 |
| `receipt-replay` | 1/1 | 128 / 0 / 0 | 0 |
| `contract-final-receipt` | 2/2 | 5 / 0 / 0 | 0 |

The two focused skips are only `FreshHost` and `FreshHostUpgrade`'s opt-in checks; the separate explicit live run completes all cases. Focused discovery/execution inventories match exactly. The retained bootstrap proof's corrected retry exits **0** overall; its normal update intentionally exits **1** for incompatible minimum. The first supplemental proof script expected install's exit 2 for update, so its own assertion exited 1 even though update correctly refused with exit 1. Both attempts and exact command exits are preserved; no production change followed the repair commit.

Required final live command (run only after the production commit):

```sh
AETHER_CODEX_SKILL_LIVE=1 AETHER_CODEX_SKILL_LIVE_CASES=all \
AETHER_CODEX_SKILL_EVIDENCE_DIR=/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/review-repair \
go test ./cmd -run '^TestCodexAntSkill(FreshHost|FreshHostUpgrade|ReceiptValidation)$' -count=1 -json -timeout 90m
```

The focused selector is `^(TestCodexAntSkill|TestCodexGeneratedCommandShimsCoverIntelligentCommands|TestSourceCheckValidatesCurrentSourceSurfaces|TestLifecycleProjectionAcceptedPlanChoicesAreCoequal|TestResolveVersionPrefersRepoVersionFile|TestInstallUsesEmbeddedAssetsWithoutPackageDir|TestVersionFlag)`, separately normal/race with `-count=1 -json -timeout 90m`. Recorded-capture replay uses `AETHER_CODEX_SKILL_RECEIPT_PATH` set to the absolute current checked-in receipt path and `^TestCodexAntSkillReceiptValidation$`. No preliminary live matrix was run for this repair.

### Current scope and remaining limits

| Criterion / requirement | Current repair evidence | Disposition |
| --- | --- | --- |
| SC1 / ANT-01: nine fresh entrypoints before/after update | 18 new positive captures, four real negative controls; exact candidate/skill/support/guide identities | Verified for Codex 0.154.0 and the bounded first read-only step. |
| SC2 / ANT-02: distribution and compatibility | Causal unstamped RED/GREEN; old stamped refusal; too-new minimum and external-metadata controls; focused normal/race distribution tests | Bootstrap repaired; original transaction, channel, custom-preservation and rollback limits retained. |
| SC3 / ANT-03: guidance and machine identities | Focused guides/display tests, all 18 real guide responses, installed-guidance audit, receipt-backed contract test and final source-check | Bounded checks pass; ANT-03/unclassified remains flagged-unverified. |
| SC4: proof detects broken wiring/inputs | Preserved Plan 06 causal mutations, two new causal RED tests, four fresh-host rejecting controls and replay | Complementary deterministic and real-host evidence; no full workflow claim. |

The **three flagged-unverified items**, **17 inherited failures**, and **human_needed** status remain. The parent interrupted the Wave 5 broad gate after CR-01 was found; that interrupted run is **not a passing gate**. This executor ran no broad suite. Parent owns the broad post-merge gate and rereview. No phase completion, flag clearance, owner acceptance, public release or shared installation/configuration change is claimed. The original Phase 205 inventory remains **`3b2b678284a26332addeadd2bd700bd4f51c4e2b0e6907a987116c85e5c2a8dd`**.

See [review repair record](204.1-REVIEW-REPAIR.md) for production-call tracing, deviations and exact preservation boundaries.

## Historical Plan 07 verification — preserved, superseded candidate

Everything below records the original Plan 07 evidence at source **`f5449c61868cf0458c071d7253cd2261c3f2e9da`**. Its uses of “final” describe that historical attempt, not the repaired candidate above. Original raw evidence and [Plan 07 summary](204.1-07-SUMMARY.md) remain untouched. The complete former checked-in receipt is preserved at `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/review-repair/historical-fresh-codex-skills.json`; its SHA-256 is `581334a03409e8fe599120ea63bee8c8d88de8b8c5c58e30ca5836842e455d3d`. Earlier attempt failures, five probe records, three flagged items, 17 inherited failures and owner boundaries retain their original dispositions.

The nine installed `$ant-*` entrypoints work in fresh Codex CLI 0.154.0 processes after clean installation and normal legacy update. The final production candidate was rebuilt after the platform-contract change and all 22 captures were rerun. **Status remains `human_needed`**: the unclassified guidance probe and two descriptor-less prohibitions remain individually flagged-unverified. This is the required standard phase goal check; automatic GSD verification is disabled.

In plain English: the expected nine menu actions can open their installed instructions and reach the correct runtime guide after an update. This proves that doorway works. The later workflows, helper behavior, release qualification and the owner's acceptance still need their own evidence.

### Evidence identity and scope

- Build worktree HEAD: `f5449c61868cf0458c071d7253cd2261c3f2e9da`; both scenario builds observed a clean worktree and empty production patch.
- Final production-input digest: `sha256:7d4050be02a7783f2529fe2e83e9465a6867028d01b03c950843fc28aaf1b30d`; **1702** tracked production inputs, including embedded assets and symlink targets, checked independently against both build inventories and final source. Test files and planning prose are excluded from this aggregate; executed test-binary hashes are in the raw test logs.
- Candidate version: **1.0.79**. Candidate SHA-256: `sha256:0e0917fd50ee9337e4b2701248c8170416221df7e544904150041adc1cb6462c`. Both independent scenario builds produced identical candidate bytes.
- Published payload identity: `sha256:16061c2e6e5a7af731c9abc31e1d9e8f2e282ad78c8f97f3e2a33bbb301c9887`, source version **1.0.79**. The platform-contract change leaves skill payload bytes unchanged; it changes the executable candidate.
- Client: **codex-cli 0.154.0**, SHA-256 `sha256:4f85982624b3898c8991cb80c0981b2aa71070e3537046c9a95950318a95afcc`. Existing model selection `gpt-6-astra` / `xhigh`; actual arguments are retained per case.
- Qualified discovery root: isolated `<home>/.codex/skills/aether`. Plan 01's original discovery receipt selected this production interface and remains unchanged; it is not relabelled as final-candidate proof.
- Raw evidence root: `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/plan-07/`. [Historical Plan 07 live receipt](/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/review-repair/historical-fresh-codex-skills.json) indexes per-case source/client/binary/payload identity, loaded path/digest, private support event, exact guide command, returned runtime route, raw events/stderr and their hashes. `final-binding-and-preservation.json` checks those artifacts and all source inputs. `final-source-check-receipt.json` binds the final candidate's read-only source check.
- Go **1.26.5** embeds enclosing-checkout revision `36c0667df1a41d60bb08ba19fcd50cab9f3b8e2f` with `vcs.modified=true`. Its local `cmd/go/internal/vcs/vcs.go:214–219` recognizes a `.git` directory; this nested worktree has a `.git` file. The raw stamp is retained, not rewritten or presented as worktree HEAD. Independent worktree/source/build records establish the build source, and **all 18 actual live guide responses return the new `limited` nine-skill platform contract**. The final binary differs from preliminary SHA-256 `53a30692108ff4da45e930882ca80375c8ed4418f0439f7634e70b64a453dd2e`.

### Goal-backward success criteria

| Context success criterion | Production connection | Runnable evidence and disposition |
| --- | --- | --- |
| SC1: Fresh supported sessions invoke all nine; managed legacy names/helpers migrate without touching custom skills | `codexPublicSkillCommands` → `buildCodexSkillPayload` → registered install/update → selected home; `planCodexSkillTargets` classifies ownership | `TestCodexAntSkillFreshHost`, `TestCodexAntSkillFreshHostUpgrade`: 18/18 qualified positives plus four rejecting controls. `TestCodexAntSkillLegacyMigration`, `TestCodexAntSkillOwnership`, `TestCodexAntSkillUpdatePreservationReceipt`: PASS normally/race. Clean/default owned menus contain exactly nine public skills; three support documents are ordinary `.md` files. Custom collisions remain an explicit limitation. **Evidenced within scope.** |
| SC2: Distribution, replay, collisions, channels and rollback preserve payload and ownership | `runInstall` and `runPublish` call `setupInstallHub`; `publishCodexSkillPayload` supplies versioned bytes; `runUpdate` calls `appendMaintenanceUpdateCodexSkillTargets` before the shared locked transaction | `TestCodexAntSkillPublishedPayload`, `PublishRepeat`, `PublishChannel`, `RegisteredUpdate`, `RegisteredUpdateRepeat`, `UpdateMatrix`, `UpdateVersions`, `UpdateChannel`, `UpdatePostPlanCreationRefused`, `UpdatePostPlanModeChangeRefused`, `UpdateFailureRecovery` (all with `TestCodexAntSkill` prefix): PASS normally/race. Live normal update consumes the published payload. **Evidenced within declared rollback/channel limits.** |
| SC3: Generated instructions, guides, help and Next Up agree; later coverage stays pending | `renderCodexCommandSkillShimBody`, `buildCommandGuide`, `lifecycleProjectionCommand`, managed templates and `syncManagedProjectDoc`; only Codex `NativeCommandSurface` changes in `PlatformContractFor` | `TestCodexAntSkillManagedGuidance`, `CustomGuidancePreserved`, `RestartNotice`, `DisplayRoutes`, `GuideSupport`, `RuntimeIdentity`, `GuidesPreserveOtherPlatforms`, `TestLifecycleProjectionAcceptedPlanChoicesAreCoequal` and `TestCodexAntSkillPlatformContract`: PASS. Actual installed guides/support reviewed below. **Bounded technical checks evidenced; ANT-03 unclassified remains flagged-unverified.** |
| SC4: Focused proof detects removed generation/update wiring; real discovery is evidenced | Install publication and registered update assembly feed the same interfaces used by live scenarios | Plan 06 actual isolated install-generator, update-assembly and original-baseline mutations each exit 1 for the intended failure. Raw hashes and all 16 recorded source/test/asset hashes were independently corroborated; none changed here. Current focused normal/race runs pass. Four fresh-host controls reject absent/altered skills. **Evidenced as complementary deterministic causal and real-host checks**, not a claim that provider sessions themselves were run against disconnected callers. |

### Requirement dispositions

| Requirement | Evidence | Disposition |
| --- | --- | --- |
| ANT-01 — canonical names, real discovery, owned migration and private helpers | SC1, final 18 positive captures, missing/wrong-payload controls, inventory/source-check/ownership tests | **Verified for the qualified client/root and bounded first-step invocation.** Edited/unproven legacy skills can remain discoverable and are explicitly reported. This does not prove every later workflow. |
| ANT-02 — install/publish/update/replay/failed-update rollback, ownership and channels | SC2, final normal/race group, final normal-update captures, actual predecessor removed-wiring controls | **Verified within tested distribution/recovery contracts.** Publish command coverage is deterministic registered-command evidence; the live harness publishes the hub payload through registered `install`, not a fabricated `publish` invocation. Old pre-feature binaries require bootstrap. |
| ANT-03 — consistent generated guidance and stable machine identities | SC3, actual installed guidance, guide tool outputs, final contract tests and source checker | **Bounded checks pass; overall disposition remains human_needed** because the original unclassified review item is still flagged-unverified. No broader parity or owner verdict is inferred. |

Requirement IDs are evidence mappings, not edits to shared requirement checkboxes. Parent orchestration owns final integration, code review, tracking and `phase.complete`.

### Real fresh-host observations

Each positive process starts with isolated HOME/CODEX_HOME/XDG paths, ignored user configuration/rules, read-only sandbox and a disposable repository. It receives the dollar skill name and a request for its first read-only step, without an expected file path or runtime route. Qualification requires actual successful tool events for reading exact installed skill bytes, resolving `aether` to the candidate, executing the guide and reading exact private support bytes; assistant prose is insufficient. The guide's returned route is recorded but never executed. Repository snapshots remain unchanged, and temporary authentication copies are removed without entering the receipt.

Clean run root: `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/plan-07/fresh-host-clean-3006983913`. Upgrade run root: `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/plan-07/fresh-host-upgrade-1385229808`. Event cells below are `line#item` for **skill read / guide / support read** in `<run>/<name>/events.jsonl`; every binary-resolution reference is additionally retained in the receipt. All client raw exits are **0**.

| Entry | Private support | Clean event references | Upgrade event references | Outcomes |
| --- | --- | --- | --- | --- |
| `$ant-init` | aether-colony-creation.md | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 | qualified / qualified |
| `$ant-discuss` | aether-colony-research.md | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 | qualified / qualified |
| `$ant-oracle` | aether-colony-research.md | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 | qualified / qualified |
| `$ant-colonize` | aether-colony-build-cycle.md | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 | qualified / qualified |
| `$ant-plan` | aether-colony-build-cycle.md | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 | qualified / qualified |
| `$ant-build` | aether-colony-build-cycle.md | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 | qualified / qualified |
| `$ant-continue` | aether-colony-build-cycle.md | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 | qualified / qualified |
| `$ant-swarm` | aether-colony-build-cycle.md | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 | qualified / qualified |
| `$ant-seal` | aether-colony-build-cycle.md | 5#item_1 / 9#item_3 / 11#item_4 | 5#item_1 / 9#item_3 / 11#item_4 | qualified / qualified |

Clean scopes use registered `aether install --package-dir <this source> --home-dir <isolated home> --channel stable --skip-build-binary`. Upgrade scopes begin with the 14 source-proven legacy entries and platform-home directories required by the normal updater, then run registered **`aether update` without force** against the disposable publisher's hub. Its skill receipt reports 13 copies (nine skills, three supports, ownership manifest) and 14 removals. No positive installed skill is edited by the harness. Both scenarios assert exactly nine public `SKILL.md` entries and no ownership-proven old public/helper entries.

Each scenario additionally starts fresh processes after removing `ant-plan/SKILL.md` and after altering its bytes. Missing-skill sessions report unavailable without substituting a globally installed entry. Wrong-payload sessions actually read the altered file and execute its guide; their receipts still fail validation against candidate bytes. Thus all four controls are **expected evidence rejections**, with successful client raw exits. These controlled unavailable outcomes remain visible; they are not counted as qualified positives.

### Executed checks and counts

Counts include terminal top-level and subtest events; overlapping runs are not additive coverage. Exact selected/executed identities and raw process statuses are retained in each named JSON/raw pair under the evidence root.

| Check | Discovery / top-level execution | Pass / fail / skip events | Raw exit |
| --- | --- | --- | --- |
| Final focused normal (`focused-final-normal`) | 44/44, 42 ordinary plus 2 explicit live opt-in skips | 369 / 0 / 2 | 0 |
| Final focused race (`focused-final-race`) | same 44/44 | 369 / 0 / 2 | 0 |
| Final required live (`live-final`) | 3/3; 18 positives + 4 controls, plus receipt matrix | 151 / 0 / 0 | 0 |
| Contract behavioral RED (`contract-red`) | 2/2 | 4 / 1 / 0 | 1 |
| Contract normal / race (`contract-green`, `contract-race`) | 2/2 each | 5 / 0 / 0 each | 0 each |
| Final recorded receipt replay (`final-recorded-validation-absolute`) | 1/1 | 128 / 0 / 0 | 0 |
| Final receipt-backed contract (`contract-final-receipt`) | 2/2 | 5 / 0 / 0 | 0 |
| Final candidate source-check | actual executable | 128 Claude/OpenCode wrappers + 12 Codex outputs; 0 findings | 0 |

Required live command:

```sh
AETHER_CODEX_SKILL_LIVE=1 AETHER_CODEX_SKILL_LIVE_CASES=all \
AETHER_CODEX_SKILL_EVIDENCE_DIR=<durable-plan-07-root> \
go test ./cmd -run '^TestCodexAntSkill(FreshHost|FreshHostUpgrade|ReceiptValidation)$' -count=1 -json -timeout 90m
```

The focused group uses `^(TestCodexAntSkill|TestCodexGeneratedCommandShimsCoverIntelligentCommands|TestSourceCheckValidatesCurrentSourceSurfaces|TestLifecycleProjectionAcceptedPlanChoicesAreCoequal)` with `-count=1 -json -timeout 90m`, separately with `-race`. Contract selection is `TestCodexAntSkillPlatformContract|TestPlatformContract`. Final recorded replay sets `AETHER_CODEX_SKILL_RECEIPT_PATH` to the checked-in final receipt and runs `^TestCodexAntSkillReceiptValidation$`. The two opt-in skips never substitute for the explicit live run.

Plan 06's preserved [deterministic receipt](evidence/deterministic-proof.json) records 42 discovered/executed top-level identities, 242 pass / 0 fail / 1 intentional opt-in skip in each normal/race run. Its three actual mutations caused: install tracer 1 failure; update matrix parent+legacy subtest 2 failures; original-baseline replacement 2 failures; each raw exit **1**. `predecessor-corroboration.json/.raw` independently verifies source/log identities, not summaries alone.

Preliminary failed attempts remain durable: `live-preliminary` exits 1 with 109 pass / 2 fail because source hashing mishandled a tracked directory symlink; `live-preliminary-retry1` exits 1 with 121 pass / 12 fail because the upgrade fixture lacked the existing platform-home directories. The fixture corrections are in the harness. `live-preliminary-retry2` exits 0 with 133 pass / 0 fail / 0 skip, but predates the production contract change and is explicitly superseded. No old capture is relabelled as the final candidate. The first saved-capture replay (`final-recorded-validation`) used a repository-relative path while Go ran in `cmd/`, so it exited 1 with 126 pass / 2 fail / 0 skip before reading the receipt. The absolute-path retry above passes all 128 results; no product change or evidence substitution was needed.

### Five original probe items

The preserved original `original-edge-coverage.json` has **5 unresolved / 0 resolved**, with null verification/resolution fields. It is unchanged. SOURCE-AUDIT later projected four explicit predicates and retained one flagged item. This table distinguishes the original record, that planning projection, and the current evidence without rewriting history.

| Original item | Original status / planning projection | Current evidence and disposition |
| --- | --- | --- |
| ANT-01 / empty — empty, single-element or null input | unresolved → explicit predicate | `validateCodexSkillInventory` and `TestCodexAntSkillInventory`: empty/null/incomplete/duplicate inventories refuse before writes. **Projected predicate verified** normally/race; original item remains as emitted. |
| ANT-01 / encoding — equality/normalization rules | unresolved → explicit predicate | Same inventory test rejects case/Unicode lookalikes; `TestCodexAntSkillDisplayRoutes` and `RuntimeIdentity` preserve runtime arguments/identities. **Projected predicate verified**, exact lower-case ASCII identifiers; no claim of arbitrary Unicode normalization. |
| ANT-02 / idempotency — repeated identical input | unresolved → explicit predicate | `TestCodexAntSkillReplay`, `PublishRepeat`, `RegisteredUpdateRepeat`: bytes, modes and ownership identity remain stable. **Projected predicate verified** normally/race. |
| ANT-02 / concurrency — interruption or parallel runs | unresolved → explicit predicate | `TestCodexAntSkillConcurrentCommit`, both update post-plan refusal tests, `UpdateFailureRecovery`, and real original-baseline mutation: original existence/digest/mode survive through the shared lock; edits refuse before writes/journal changes; recovery protects later edits. **Projected predicate verified** normally/race within transaction seams. |
| ANT-03 / unclassified — review manually | unresolved → flagged | Actual installed instructions/support and raw guide outputs reviewed below. **flagged-unverified** retained; no invented automatic check target or approval. |

Original requirements SHA-256: `b4f672351ccdaa11e6069f0bf0a11b89ff02c3d23bf5758074cd979ede545f32`; original probe SHA-256: `c64c3f82fc8f525e496515e3d8d8788282dd08dbf2813e425386eade93567d18`. Provenance copies remain in the durable evidence root.

### Two descriptor-less prohibitions

1. **“Must not advertise complete 64-command or native-helper parity from nine installed skill names or generated file counts.”** Original projected status: unresolved. Current disposition: **flagged-unverified**. This report and the limited platform contract state the boundary, but passing Go tests or this assistant's wording do not constitute an authorized downstream upgrade of that descriptor-less item.
2. **“Must not present assistant-generated proof or installation preparation as the owners Phase 205 acceptance.”** Original projected status: unresolved. Current disposition: **flagged-unverified**. No owner acceptance was requested, performed or inferred, and no check_kind/check_target or owner override is manufactured.

### Installed-guidance review and limits

`installed-guidance-review.json` hashes the actual nine generated instructions and three private support documents in both final installations and records the inspected scope. Public names, relative support resolution, raw bypass, Go-owned state, guide routes and nine-entry scope agree with the generated project templates and final captures. Creation support closes with `$ant-plan`; research and build-cycle support identify their public entrypoints and retain private-helper identity. The three source support documents may retain frontmatter internally but are installed as ordinary support `.md` files, not extra discoverable `SKILL.md` entries.

No new concrete nine-entry naming/routing drift was found in that bounded inspection. A material limit remains explicit: skill/support bodies describe later owner interviews, worker dispatch and finalizers that these read-only captures deliberately never execute. Wording such as wrapper-equivalent purpose is instruction intent, not proven lifecycle parity. The contract's `limited` level and the pending-parity statements in templates/support preserve this distinction; the unclassified probe stays flagged-unverified.

- **Custom preservation:** exact historical ownership bytes permit removal. Edited old public/helper files and unknown custom skills survive; they can remain visible alongside the canonical menu and receipts report them. Custom canonical collisions refuse even under force. Custom AGENTS/CODEX documents remain unchanged; they can therefore retain older guidance. Full-home comparisons and independent ownership tests provide the preservation evidence.
- **Channels:** stable sync is the normal home path. Dev install/publish leaves stable homes untouched unless the existing explicit `--sync-platform-homes` opt-in is used; dev `update --sync-platform-homes` is refused. This is the declared existing channel contract, not universal separation despite explicit opt-in.
- **Versions:** update consumes the producer's complete validated manifest, including a compatible newer inventory. Missing/malformed payloads and incompatible version/schema/runtime cases refuse. A binary predating these delivery interfaces requires bootstrap; old binaries are not claimed to acquire new behavior by copying skill files.
- **Rollback:** injected failed/interrupted transaction paths restore original bytes/modes/ownership or report recovery-required when later edits conflict. A verified durable transaction cannot be rolled back by this API. Recovery tests capture the registered controller plan and use its real transaction fault seams; they do not claim an external process crash or general downgrade command was exercised.
- **Session scope:** qualification is for fresh CLI processes at this selected root/version. Stale already-running client discovery, other Codex clients/versions, custom collisions, all 64 commands, native-worker permissions/routing/cancellation/context and complete lifecycle behavior remain outside this receipt.

### Inherited integration gate — failures retained

The latest parent [Wave 4 gate](204.1-WAVE-4-GATE.md), durable `wave-4/review.json`, and detail-comparison record were read. At source **3f7d345d1c0dcb25b13c9c3d7ee74bd1068afd71**, the parent executed **5615/5615 command tests across 59 lanes**, and all **18 other tested packages passed**. The full suite raw exit is **1**, with the following **17 inherited failing identities**, no new identities at that gate:

- `TestAuditCatalogGolden`
- `TestBuildStartLegacyHelpersRetired200`
- `TestCodexBuildPlanOnlySpawnBudgetSeparatesCasteBudgetFromWorkerCount`
- `TestCompletionPacketSchemaMatchesStructs`
- `TestCurrentVocabulary199`
- `TestFailedCheckSendsExactlyOneBuilderFixAttempt`
- `TestFixAttemptIsCountedSeparately`
- `TestFixAttemptNeverOverwritesTheFirstResult`
- `TestGoSourceHintsMatchCobraContracts`
- `TestGoldenBuildVisualOutput`
- `TestGoldenContinueVisualOutput`
- `TestHumanFacingOutputGoesThroughWriteVisualOutput`
- `TestNoSecondAutomaticFixAttempt`
- `TestPhase199GateReceipt`
- `TestPlanningAdversarial200`
- `TestPlanningPublicPaths200`
- `TestResolveTestCommand_GoProject`

Four complete static blocks (catalog, build/continue goldens, visual-writer discipline) match the original Phase 205 baseline after checkout/time normalization. Fourteen diagnostic sets match Wave 3 after line normalization; the other three match after runtime fixture normalization. Vocabulary and source-hint diagnostics remain unchanged. These are failure comparisons, **not passes or waivers**. The original baseline remains visible in [205-RELEASE-RECORD.md](../205-owner-acceptance-and-restoration-seal/205-RELEASE-RECORD.md): its normal and race runs each exited 1, executing 5573/5573 command tests across 59 groups, with 18 other packages passing. No earlier approval wording in that historical record grants a waiver here. In particular, golden/visual/catalog failures are not used to claim broad workflow/output correctness; the named phase-specific guide/display tests above establish only their bounded claims.

No broad suite was run by this executor. That inherited gate predates the final Plan 07 changes and is not represented as a post-merge final gate. Parent owns broad post-merge checks and final review. Every required Plan 07 phase check was run independently; none was waived because an inherited failure existed.

### Owner boundary and next dependency

The original `205-SESSION-BEFORE-INVENTORY.json` remains SHA-256 **3b2b678284a26332addeadd2bd700bd4f51c4e2b0e6907a987116c85e5c2a8dd**. STATE, ROADMAP, REQUIREMENTS, Plan 01's discovery receipt and Plan 06's deterministic receipt remain byte-identical to the starting inventory. This executor stayed in the isolated worktree, used disposable host scopes, did not change shared installed skills/configuration, and did not access the Phase 205 owner project or manufacture an owner session.

The next implementation dependency is **Phase 204.2**. Full candidate/release qualification belongs to **Phase 204.5**; personal owner acceptance remains **Phase 205**. Completing this plan and its verification document does not authorize `phase.complete`, clear flagged items, or supply acceptance.

## Parent final integration and handoff

Recorded 2026-09-16T16:40:21.765542+00:00, after the repaired production candidate and clean repair recheck. All seven plans and the CR-01 repair are merged at `9c9eaaf02fcdc1db61810681fbb375ff73cea785`. The final build passes; the complete merged-source suite executes **5620/5620 command tests across 59 lanes**, with **18 supporting packages passed** and raw exit **1**. All 17 failure identities remain historical, with diagnostic comparisons showing no new regression. [204.1-FINAL-GATE.md](204.1-FINAL-GATE.md) records the commands, counts, hashes, inherited-failure comparison, and interrupted earlier gate; it supersedes earlier statements that parent integration was pending.

The [repair recheck](204.1-REVIEW-RECHECK.md) resolves CR-01 with no new findings. All required focused, race, repaired-candidate live and receipt-replay checks pass within their stated scope. These results do not turn the broad suite green or clear the three original judgment items. Status remains **human_needed**, and [204.1-UAT.md](204.1-UAT.md) persists all three as pending. No human verdict, phase completion, requirement override, later-phase execution or Phase 205 acceptance is inferred. Next: `gsd verify-work 204.1`.
