# Phase 204.1 Wave 4 integration gate

The merged build passed. The complete command suite ran **5,615/5,615 tests across 59 lanes**. All 18 other tested Go packages passed. There are **no new failing test identities**; the same 17 historical failures remain. The full suite's raw exit is **1**, not an all-green release result.

Candidate source: `3f7d345d1c0dcb25b13c9c3d7ee74bd1068afd71`. Raw build/test logs, exit receipts, package outcomes and all 17 identities are under `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/wave-4/`.

The comparison also checks failure details: 14 of 17 diagnostic sets are identical to Wave 3 after line-number normalization. The other three match after normalizing runtime fixture timestamps, process/attempt IDs, addresses and generated fixture digests. Four complete static failure blocks (catalog, build/continue goldens, and visual-writer discipline) match the historical Phase 205 baseline after checkout-path and timing normalization. Vocabulary and source-hint diagnostics remain identical. These comparisons identify inherited failures; they do not waive unresolved behavior or turn those failures into passes. Comparison records are retained under the same evidence root's `orchestrator/` directory.

The two new Wave 3 failures are absent. Plan 06's required deterministic proof remains 41 ordinary tests passing normally and with race detection, plus one separately recorded intentional live opt-in skip. Schema, codebase-drift and UI hooks report no blockers.

Six numbered plans are implemented. Plan 07 still must prove all nine actual entrypoints in fresh supported Codex processes after both clean installation and normal legacy upgrade, bind the final candidate, and produce standard phase verification. Full release qualification remains Phase 204.5; owner acceptance remains Phase 205.
