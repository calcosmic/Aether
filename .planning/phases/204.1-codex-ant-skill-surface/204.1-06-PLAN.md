---
phase: 204.1-codex-ant-skill-surface
plan: "06"
type: execute
wave: 4
depends_on: ["204.1-03", "204.1-05"]
files_modified:
  - cmd/codex_skill_update_test.go
  - cmd/maintenance_mutation_199_test.go
  - cmd/source_check.go
  - cmd/source_check_test.go
  - .planning/phases/204.1-codex-ant-skill-surface/evidence/deterministic-proof.json
autonomous: true
requirements: [ANT-01, ANT-02, ANT-03]
must_haves:
  truths:
    - "Registered update proves preview, normal/forced migration, replay, collisions, channel isolation and failed/interrupted recovery using real temporary filesystem targets."
    - "Normal registered update refuses a custom canonical file created or an owned target's permissions changed after planning, before target/journal mutation, preserving every user byte/mode and ownership state."
    - "A missing generation/update connection causes a named production-path check to fail."
    - "The registered source checker validates nine Codex names/routes/support references and continues to validate Claude/OpenCode surfaces."
  artifacts:
    - path: cmd/codex_skill_update_test.go
      provides: Registered normal-update post-plan creation and permission-only conflict refusal checks
    - path: cmd/source_check.go
      provides: Bounded generated-Codex source component with structured findings
    - path: .planning/phases/204.1-codex-ant-skill-surface/evidence/deterministic-proof.json
      provides: Candidate-bound test counts, outcomes and meaningful negative controls
  key_links:
    - from: cmd/source_check.go
      to: cmd/codex_skill_surface.go
      via: Generated surface checked against the independent nine-name/route contract
    - from: cmd/codex_skill_update_test.go
      to: cmd/update_cmd.go
      via: rootCmd.Execute exercises the production planner and transaction
---

<objective>
Prove the combined installation/update/guidance behavior cannot pass when its real wiring is absent.
Purpose: Catch safe-looking unit tests that never exercise the update path people actually use.
Output: Adversarial update fixtures, source validation and a candid deterministic proof receipt.
</objective>
<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/204.1-codex-ant-skill-surface/204.1-CONTEXT.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-RESEARCH.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-03-SUMMARY.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-05-SUMMARY.md

sourceCheckIssue already has area, path, message, expected and actual. Existing transaction fixtures provide Rename/Fault seams and temporary repository/hub/platform roots. Plan 02 adds immutable ExpectedDigest (including lifecycleTransactionMissingDigest) and ExpectedMode on all Codex skill/support/retirement/ownership targets, checked under the shared lock. Its maintenanceCodexSkillLocker seam lets tests inject a user edit after normal update has classified targets but before common commit acquires the real lock; wrap and delegate to the real locker, restore with t.Cleanup, and do not use t.Parallel. Reuse these contracts; do not invent a second installer or fake provider proof.
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Exercise interruption and custom-file preservation through registered update</name>
  <files>cmd/codex_skill_update_test.go, cmd/maintenance_mutation_199_test.go</files>
  <read_first>Read both files, cmd/update_cmd.go, cmd/codex_skill_install.go, cmd/codex_skill_ownership.go, cmd/platform_sync.go, cmd/lifecycle_transaction.go and cmd/codex_project_docs.go. Consume the successful Plan 01 tracer and completed Plan 03 production integration.</read_first>
  <action>Extend the actual registered-update fixture into an independently specified matrix: clean home, known legacy migration, normal/forced repeat, custom canonical collision, edited old public/helper entry, unknown newer skill, preserved custom AGENTS/CODEX documents, default dev and forbidden dev home update. Add two normal rootCmd.Execute update cases using Plan 02's lock seam after actual ownership classification and controller preview: create custom ant-plan/SKILL.md where the captured baseline says missing; separately change only an owned target's mode from 0644 to 0600. Confirm original ExpectedDigest/ExpectedMode reached the boundary, then invoke the real locker and commit path without rebuilding the plan or substituting expected values. Each must refuse before any target write or journaled mutation, preserving every target's post-edit pre-commit bytes/modes and the ownership manifest's original contents/mode or absence; inspect the error/output and absence of a success receipt. Inject failure after at least one skill write and one removal using the existing transaction seam, and exercise recovery after an unverified interruption. Assert exact old bytes/modes and ownership restoration, retained custom content, new-name absence after rollback, recovery-required on intervening edit and no post-success Rollback promise. Include removal-only restart and preview zero-mutation/mtime/journal/lock assertions. Verify preservation through the registered result rather than the old direct sync helper. Where tests expose a defect, report the failed contract and make a bounded source fix under the execution deviation protocol before claiming success.</action>
  <acceptance_criteria>
    - TestCodexAntSkillUpdateMatrix reaches rootCmd.Execute for every applicable registered update variant.
    - TestCodexAntSkillUpdatePostPlanCreationRefused invokes normal registered update, injects a custom canonical file after planning, and proves missing-state expectations survive through the real locked commit; all user bytes/modes and ownership state remain unchanged, with no transaction journal or success receipt.
    - TestCodexAntSkillUpdatePostPlanModeChangeRefused invokes normal registered update, injects chmod-only 0644 to 0600 after planning, and proves the original mode is checked rather than a refreshed preview; refusal preserves the 0600 mode, every target's bytes/modes and ownership state, with no transaction journal or success receipt.
    - TestCodexAntSkillUpdateFailureRecovery exercises actual transaction rollback/recovery and compares bytes/modes plus ownership manifest.
    - TestCodexAntSkillUpdatePreservationReceipt identifies preserved custom instructions and old edited skills without claiming they were migrated.
    - A refusal occurs before target/journal mutation for post-plan custom creation and mode-only edits, and before skill mutation for canonical collisions, malformed source payloads, version mismatch and forbidden dev synchronization.
  </acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexAntSkill(UpdateMatrix|UpdatePostPlanCreationRefused|UpdatePostPlanModeChangeRefused|UpdateFailureRecovery|UpdatePreservationReceipt)$' -count=1 -timeout 90m</automated></verify>
  <done>The registered update path proves complete original-baseline enforcement, including post-plan custom creation and chmod-only refusal with no lost bytes, modes or ownership state, alongside every required success and recovery case.</done>
</task>

<task type="auto" tdd="true">
  <name>Add source validation and record checks that fail when wiring is broken</name>
  <files>cmd/source_check.go, cmd/source_check_test.go, .planning/phases/204.1-codex-ant-skill-surface/evidence/deterministic-proof.json</files>
  <read_first>Read cmd/source_check.go, cmd/source_check_test.go, cmd/codex_skill_surface.go, cmd/codex_skill_surface_test.go, cmd/codex_skill_update_test.go and root CLAUDE.md's Definition of Done and Verification Commands.</read_first>
  <action>Add checkCodexSkillSurface to the actual runSourceCheck path, using structured sourceCheckIssue findings for missing/duplicate names, unparseable frontmatter, invalid guide/runtime route and unresolved private support. Validate the independent nine-name contract, not merely the generator's own count; absence of the other 55 is intentional. Retain all Claude/OpenCode checks. Add negative subject fixtures for each source finding and verify registered source-check output. Run the focused affected test census with normal and race modes, retain discovered/executed counts and raw exit statuses, and record source/binary/payload identities. In an isolated source copy, separately remove the install generator call and the update target-assembly call, prove the corresponding production-path tests fail for the expected reason, then rerun the clean source. Record the actual controls, not an attestation. Ordinary help wording does not require mutation controls. Capture any inherited failure by identity and do not waive a failure that the phase relies on. Full release qualification remains Phase 204.5.</action>
  <acceptance_criteria>
    - TestCodexAntSkillSourceCheck invokes the real source checker and rejects missing names, bad frontmatter, route drift and absent private support.
    - Existing TestSourceCheckValidatesCurrentSourceSurfaces passes with all three relevant platform surfaces accounted for.
    - The proof receipt names every selected test, discovered/executed count, normal/race outcome, raw log and negative-control outcome.
    - Both disconnected-caller variants fail the intended test while clean source passes; no mutation remains in the working source.
  </acceptance_criteria>
  <verify><automated>go test ./cmd -run '^(TestCodexAntSkill|TestCodexGeneratedCommandShimsCoverIntelligentCommands|TestSourceCheckValidatesCurrentSourceSurfaces|TestLifecycleProjectionAcceptedPlanChoicesAreCoequal)' -count=1 -timeout 90m; go test ./cmd -race -run '^(TestCodexAntSkill|TestCodexGeneratedCommandShimsCoverIntelligentCommands|TestSourceCheckValidatesCurrentSourceSurfaces|TestLifecycleProjectionAcceptedPlanChoicesAreCoequal)' -count=1 -timeout 90m</automated></verify>
  <done>Source drift and disconnected production wiring cannot pass silently; deterministic proof is recorded separately from live host evidence.</done>
</task>
</tasks>
<verification>
Run each verify command separately and preserve both exit statuses. Include both named registered post-plan refusal tests in the selected census and proof receipt. Live opt-in cases being skipped in this deterministic run is expected and does not satisfy Plan 07. Confirm discovered equals executed for the selected ordinary test set, and report intentional live skips separately.
</verification>
<success_criteria>
All required deterministic cases pass with causal negative controls and honest counts. Normal registered update preserves post-plan custom files and permission edits by refusing before target/journal mutation; source checking protects the declared nine-name surface.
</success_criteria>

## Artifacts this phase produces

- New production function: checkCodexSkillSurface in cmd/source_check.go.
- New tests: TestCodexAntSkillUpdateMatrix, TestCodexAntSkillUpdatePostPlanCreationRefused, TestCodexAntSkillUpdatePostPlanModeChangeRefused, TestCodexAntSkillUpdateFailureRecovery, TestCodexAntSkillUpdatePreservationReceipt and TestCodexAntSkillSourceCheck.
- New proof artifact: evidence/deterministic-proof.json plus referenced raw logs, carrying source/payload identities, selected_test_names, discovered, executed, skipped, exit_code, normal/race outcomes and negative_controls.

<output>Create .planning/phases/204.1-codex-ant-skill-surface/204.1-06-SUMMARY.md.</output>
