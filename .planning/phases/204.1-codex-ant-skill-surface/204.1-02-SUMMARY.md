---
phase: 204.1-codex-ant-skill-surface
plan: "02"
subsystem: infra
tags: [codex, skills, ownership, migration, transactions, locking, go]
requires:
  - phase: 204.1-01
    provides: Validated ant payload, registered install path, and Codex 0.154.0 discovery receipt
provides:
  - Content-proven legacy retirement and strict persisted ownership with preservation reports
  - Immutable existence/digest/mode expectations for all exact Codex skill targets
  - Shared Codex-home commit lock with stale-plan refusal and verified recovery evidence
affects: [204.1-03, 204.1-06, 204.1-07]
actuals:
  tokens: 29926
  tasks: 2
  commits: 5
tech-stack:
  added: []
  patterns:
    - Frozen source-cited legacy bytes authorize exact-file retirement
    - Common maintenance commit locks the physical Codex home before validating original baselines
    - Existing ownership aliases remain readable alongside explicit payload version/digest fields
key-files:
  created:
    - cmd/codex_skill_ownership.go
    - cmd/codex_skill_ownership_test.go
    - cmd/testdata/codex-skills/legacy-v1.0.79.json
  modified:
    - cmd/codex_skill_install.go
    - cmd/platform_sync.go
    - cmd/platform_sync_test.go
    - cmd/install_cmd.go
key-decisions:
  - Retain the Plan 01 production interfaces and qualified .codex/skills/aether discovery root.
  - Preserve unfamiliar owned entries and reject payload downgrades; omission is never retirement authority.
  - Report preserved files through structured installer results instead of mixing warnings into JSON.
patterns-established:
  - ExpectedDigest includes the missing sentinel; ExpectedMode always carries captured permission bits, including nonnil zero for absence.
  - A fresh preview validates original ownership evidence and cannot replace it.
  - Preview never creates locks or transaction evidence; commit holds the home lock through recovery.
requirements-completed: [ANT-01, ANT-02]
coverage:
  - id: D1
    description: Registered install retires only fourteen proven legacy files, creates the canonical public skills, and preserves custom content.
    requirement: ANT-01
    verification:
      - kind: integration
        ref: cmd/codex_skill_ownership_test.go#TestCodexAntSkillLegacyMigration
        status: pass
      - kind: integration
        ref: cmd/codex_skill_surface_test.go#TestCodexAntSkillInstallTracer
        status: pass
    human_judgment: false
  - id: D2
    description: Ownership, newer inventory preservation, and complete original target baselines prevent custom-file loss.
    requirement: ANT-02
    verification:
      - kind: integration
        ref: cmd/codex_skill_ownership_test.go#TestCodexAntSkillOwnership
        status: pass
      - kind: integration
        ref: cmd/codex_skill_ownership_test.go#TestCodexAntSkillNewerInventory
        status: pass
      - kind: unit
        ref: cmd/codex_skill_ownership_test.go#TestCodexAntSkillBaselineCapture
        status: pass
    human_judgment: false
  - id: D3
    description: Separate processes serialize at a shared Codex home and stale creations or chmod edits refuse before journaled mutation.
    requirement: ANT-02
    verification:
      - kind: integration
        ref: cmd/codex_skill_ownership_test.go#TestCodexAntSkillConcurrentCommit
        status: pass
      - kind: integration
        ref: cmd/codex_skill_ownership_test.go#TestCodexAntSkillPostPlanCreationRefused
        status: pass
      - kind: integration
        ref: cmd/codex_skill_ownership_test.go#TestCodexAntSkillPostPlanModeChangeRefused
        status: pass
      - kind: integration
        ref: cmd/codex_skill_ownership_test.go#TestCodexAntSkillBaselineCompatibility
        status: pass
    human_judgment: false
  - id: D4
    description: Repeated sync is unchanged, failed or interrupted unverified transactions recover safely, and previews/dev defaults preserve user files.
    requirement: ANT-02
    verification:
      - kind: integration
        ref: cmd/codex_skill_ownership_test.go#TestCodexAntSkillReplay
        status: pass
      - kind: integration
        ref: cmd/codex_skill_ownership_test.go#TestCodexAntSkillRollback
        status: pass
      - kind: integration
        ref: cmd/codex_skill_ownership_test.go#TestCodexAntSkillPreview
        status: pass
      - kind: integration
        ref: cmd/codex_skill_ownership_test.go#TestCodexAntSkillChannelIsolation
        status: pass
      - kind: integration
        ref: cmd/platform_sync_test.go#TestDevPlatformHomeSyncRequiresExplicitOptIn
        status: pass
    human_judgment: false
duration: 23min
completed: 2026-09-16
status: complete
---

# Phase 204.1 Plan 02: Safe Codex Skill Migration and Commit Ownership Summary

**Legacy skills now migrate through exact content-proven targets, and shared-home commits reject custom creations or permission edits against the original ownership baseline.**

For dummies: an upgrade removes only old files whose original contents are known. Custom work stays intact. If someone changes a file or its permissions after the upgrade was planned, the upgrade stops before writing skill files or a transaction journal.

## Performance

- **Recorded execution duration:** 23 minutes, from the first retained task-test start; preparatory reading preceded that timestamp.
- **First recorded task execution:** 2026-09-16T11:32:28.316871+00:00.
- **Completed:** 2026-09-16T11:55:53.261824+00:00.
- **Tasks:** 2/2.
- **Files changed:** 7 implementation/test/fixture files plus this summary.
- **Actuals:** 29,926 tokens estimated as characters/4 of the realized implementation diff, not model usage.

## Accomplishments

- The frozen fourteen-file fixture comes from revision `404731ccffda7bbca64ce801b74b0752a7161315`, using the original `platform_sync.go` renderer, `command_guide.go` definitions, and `finalizer_completion_contract.go` dependency. Every expected name and body digest was independently checked. Fixture SHA-256: `c6f7dc9d0b701df4e76ddf2c66706050ddb08cb5eb86e30ef2998fdeaed3d32a`.
- Registered installation removes only proven legacy `SKILL.md` files, installs nine public skills and three private support files, and writes ownership in the same transaction. Edited legacy files, symlinks, unknown entries, and extra siblings remain. Preserved paths appear in the preview and structured install result. No recursive directory removal was added.
- The `codex-skill-ownership/v1` reader validates paths, duplicates, digests, modes, and version identity. It retains Plan 01's `source_version`/`payload_identity` fields and adds agreeing `payload_version`/`payload_digest` fields. Older Plan 01 records remain readable. Malformed ownership and modified owned support refuse; missing ownership or a `source: shipped` marker does not authorize a collision. Force/opt-in does not bypass ownership.
- A typical plan now has 27 frozen targets: nine public files, three support files, fourteen exact legacy retirement paths (including expected absence), and the ownership manifest. `ExpectedDigest` is never empty; `ExpectedMode` points to captured permissions, including zero for absence. Manifest parsing uses captured bytes. Unfamiliar owned entries remain recorded; a lower-version payload refuses.
- `maintenanceCodexSkillLocker` defaults to the real `lockCodexSkillTargets` implementation. Separate projects/processes share a lock identity under the physical selected Codex home. Lock paths retain symlink containment checks. Common commit acquires the lock before prepare, validates unchanged targets too, compares declaration existence/digest/mode with original expectations, checks the whole set again before journaling, and holds the lock through failure recovery. Unrelated callers retain optional digest/nil-mode compatibility.
- Replay leaves bytes, modes, and ownership identity unchanged and reports no new write/removal. Failure tests cover new-name absence after legacy rollback, old-name restoration, ownership bytes/modes, faults after one write and one removal, fresh-coordinator rollback of interrupted unverified journals, preservation of later edits as recovery-required, and refusal to roll back a verified receipt.
- The assigned platform tests now preserve unproven full-mirror bytes, use canonical `ant-plan`, and supply all three actual support sources plus the source version in the dev opt-in fixture. Strict production payload validation is unchanged.

## Task Commits

Every commit used normal repository hooks; none used `--no-verify`.

1. **Task 1 RED:** `77a0a4090dbfbeefd769bb843251af23e998159c` — legacy migration, ownership and baseline tests plus frozen fixture.
2. **Task 1 GREEN:** `6a465836817136b8133e21a15b19792db169a26e` — exact ownership classification, retirement, baseline capture and preservation reporting.
3. **Task 2 RED:** `f715811a3a65d6ee65abfb1aa1ac27769f8f9520` — stale commits, recovery, replay and channel tests, with the assigned platform fixture repairs.
4. **Task 2 GREEN:** `3760fcbfb0d93f5c36cf0bd1a275114f92d71fc3` — locked common commit, immutable baseline validation and strengthened evidence checks.

**Plan metadata:** committed separately as `docs(204.1-02): complete safe Codex skill migration plan`.

## Verification and Exact Counts

Durable evidence: `/Users/callumcowie/.aether-backups/phase204-1-execution-evidence-20260916/plan-02/`.

`test-accounting.json` records all commands, every executed test/subtest identity, raw exit statuses, source hashes, raw-output hashes, restoration hashes, and final source revision. `final-discovery.txt` lists exactly **35 discovered top-level tests**. Normal execution and race execution each ran those same **35 top-level tests + 54 subtests = 89 entries**, all passing, zero skips. Normal execution used the three disjoint focused selections below; race used their exact union.

| Final check | Top-level / subtests | Raw exit | Outcome | Raw output |
|---|---:|---:|---|---|
| Required migration/ownership group | 4 / 10 | 0 | 14 passed | `final-task1.txt` |
| Required replay/concurrency/recovery group | 8 / 17 | 0 | 25 passed | `final-task2.txt` |
| Platform, shared maintenance, and Plan 01 regressions | 23 / 27 | 0 | 50 passed | `final-platform-regressions.txt` |
| Focused race union | 35 / 54 | 0 | 89 passed, no race reports | `final-race.txt` |
| Test discovery | 35 discovered | 0 | Exact execution/discovery identity match | `final-discovery.txt` |

Both plan-required commands were executed explicitly, with `-json` added solely to retain raw test events:

```sh
go test ./cmd -run '^TestCodexAntSkill(LegacyMigration|Ownership|NewerInventory|BaselineCapture)$' -count=1 -json -timeout 90m
go test ./cmd -run '^TestCodexAntSkill(Replay|ConcurrentCommit|PostPlanCreationRefused|PostPlanModeChangeRefused|BaselineCompatibility|Rollback|Preview|ChannelIsolation)$' -count=1 -json -timeout 90m
```

The exact platform/race regular expressions are recorded in their adjacent `.json` command records. This includes all `TestMaintenanceMutation199*` checks and the repaired `TestDevPlatformHomeSyncRequiresExplicitOptIn`. The obsolete pruning test was renamed to `TestSyncCodexSkillShimsPreservesUnprovenMirrorAndCustom` to describe its corrected contract.

Execution logs also record the actual test executable SHA-256, generated payload identity, source version/generator identity, and separate child-process identity. Final source bytes match the committed implementation. `gofmt -l` and `git diff --check` were clean.

### Real negative controls

| Deliberate mutation | Executed entries | Raw exit | Required observed failure |
|---|---:|---:|---|
| Bypass ownership classification | 2 | 1 | Custom plain canonical file incorrectly authorized |
| Replace expected absence with current observed existence/digest/mode | 1 | 1 | Post-plan custom creation is overwritten instead of refused |
| Replace captured expected modes with fresh observed modes | 5 | 1 | All four skill/support/retirement/manifest chmod cases incorrectly accepted |
| Remove the common lock call | 1 | 1 | Separate process never enters the required shared-lock boundary |

Raw files are `negative-{ownership,absence,mode,lock}.txt`; each has its exact mutated source and a restoration receipt. Production source was restored byte-for-byte after every control, before final passing checks. The absence control drops the complete missing-state expectation; dropping only a digest would leave the independent zero-mode guard active.

### TDD gate compliance and evidence corrections

- Task 1 behavioral RED ran 4 top-level tests plus 10 subtests: 8 passing entries and 6 failing entries. Failures exposed retained legacy files, unreported preservation, rejection of unfamiliar owned entries, and absent expected-mode fields. Task 1 GREEN and the repeated tracer gate each passed all 14 entries before Task 2 began.
- Task 2's initial two attempts did not compile because the test used a nonexistent receipt field. They executed **zero tests** and are not claimed as behavioral RED. After correcting the test, `task2-red-behavior.txt` ran 21 entries: 10 passed and 11 failed. Its intended failures exposed missing mode guards and missing required expectations; its rollback assertion also needed to respect the existing wrapped recovery-error contract.
- The first timing-only lock observation was too weak. The final fixture requires the child to reach the common lock boundary; removing that actual call demonstrably fails the final test.
- The absence-control result validator initially compared escaped raw JSON text against `<nil>`. Decoding the already-retained events confirmed the intended failure; `negative-absence-validation.json` records that evidence-parser correction. The test itself exited 1 as required, and restoration succeeded.

## Files Created/Modified

- `cmd/codex_skill_ownership.go` — ownership reader/classifier, source-proven legacy map, exact target planner, shared home lock.
- `cmd/testdata/codex-skills/legacy-v1.0.79.json` — original paths, source revision/dependencies, fourteen frozen bodies, digests and modes; embedded as production retirement evidence.
- `cmd/codex_skill_ownership_test.go` — twelve named plan checks with registered installation, independent inventories, separate processes, fault injection and refusal assertions.
- `cmd/codex_skill_install.go` — delegates planning to the ownership module and reports preservation/removal results.
- `cmd/platform_sync.go` — optional expected-mode contract, structured preserved paths, locked commit and original-baseline checks.
- `cmd/platform_sync_test.go` — assigned obsolete mirror expectation and complete dev support fixture.
- `cmd/install_cmd.go` — bounded preservation-result field and installer JSON plumbing.

## Decisions Made

Kept the qualified Plan 01 discovery root and existing transaction authority. Ownership records preserve the earlier fields while adding the plan's explicit payload version/digest names; disagreeing aliases fail closed. Lock identity follows the physical Codex home, independent of each project's journal. Unknown entries are preserved rather than inferred obsolete from today's inventory.

## Deviations from Plan

**1. [Rule 2 — Missing critical reporting] Add structured installer preservation results.**

- **Found during:** Task 1 registered-install migration.
- **Issue:** The existing `syncResult` and installer JSON entry had no channel for preserved file identities. Writing warnings to stderr also corrupts consumers that combine JSON output streams, including the existing registered-install tracer.
- **Fix:** Add `preserved []string` to `syncResult` and expose it on the Codex skill result. No unrelated installer behavior changed.
- **Files:** `cmd/install_cmd.go`, plus the already-owned installation adapter.
- **Verification:** Registered migration asserts the unknown legacy path is reported; clean install/collision and platform tests remain passing.
- **Commit:** `6a465836817136b8133e21a15b19792db169a26e`.

**Total deviations:** one bounded reporting fix. The two platform fixture corrections were explicitly assigned and are within Task 2, not additional scope. No architectural change or designed stop was needed.

## Issues Encountered

Only the test/evidence-harness corrections described above. No required phase check was waived, and the historical 17-failure baseline was not used as an exemption. The broad full suite remains the orchestrator's post-merge responsibility.

## Boundary Preservation

- Stayed on `worktree-agent-p204-1-02-20260916`; no additional executor or plan was started.
- `STATE.md`, `ROADMAP.md`, and `REQUIREMENTS.md` were not edited. Requirement IDs in this summary describe this plan's contribution; they do not mark sibling plans or the whole phase complete.
- No shared installed skills/configuration or Phase 205 owner project was modified or exercised.
- The original Phase 205 before-inventory remains SHA-256 `3b2b678284a26332addeadd2bd700bd4f51c4e2b0e6907a987116c85e5c2a8dd`.
- Reused Plan 01's Codex CLI 0.154.0 discovery receipt at the selected `.codex/skills/aether` root. No new live client run was required by Plan 02, and none is claimed. The copied predecessor receipt and its hash are retained with this evidence.

## User Setup Required

None.

## Next Phase Readiness

The safe migration policy and common locked commit interface are ready for the assigned publish/update plans. Their callers must carry each returned target's original `ExpectedDigest` and `ExpectedMode` unchanged. Broader publish/update wiring, all-nine fresh-client proof, post-merge full gates, and Phase 205 owner acceptance remain outside this plan.

## Self-Check: PASSED

All two tasks' named acceptance checks pass. The 35 discovered top-level identities match normal and race execution exactly; all 89 entries pass without skips. All four negative controls fail meaningfully, with restored production hashes. Raw artifacts and final source hashes verify, all four task commits exist, hooks ran, formatting is clean, and protected tracking/inventory bytes are unchanged.
