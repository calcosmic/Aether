---
phase: 204.1-codex-ant-skill-surface
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/codex_skill_surface.go
  - cmd/codex_skill_install.go
  - cmd/platform_sync.go
  - cmd/install_cmd.go
  - cmd/codex_skill_surface_test.go
  - cmd/codex_skill_live_test.go
  - .planning/phases/204.1-codex-ant-skill-surface/evidence/discovery-tracer.json
autonomous: true
requirements: [ANT-01, ANT-02]
must_haves:
  truths:
    - "A clean registered install produces exactly nine public ant skills with their actual command-guide routes and private readable support."
    - "Empty, null, incomplete, duplicate or invalid nine-entry source inventories fail validation before any skill target is written."
    - "Public skill identifiers compare as exact lower-case ASCII bytes; case variants and Unicode lookalikes are rejected rather than normalized into another command."
    - "A fresh real Codex client loads the installed ant-plan skill from the selected isolated home and executes its real read-only command-guide route."
  artifacts:
    - path: cmd/codex_skill_surface.go
      provides: Nine-entry inventory, deterministic payload and strict validation
    - path: cmd/codex_skill_install.go
      provides: Exact skill-target planner and transactional clean installation
    - path: cmd/codex_skill_live_test.go
      provides: Opt-in real client capture with an isolated discovery root
    - path: .planning/phases/204.1-codex-ant-skill-surface/evidence/discovery-tracer.json
      provides: Actual client/version/path/digest and loaded-skill evidence or an honest failed/unavailable result
  key_links:
    - from: cmd/install_cmd.go
      to: cmd/codex_skill_install.go
      via: Registered install reaches the common skill target planner and maintenance transaction
    - from: cmd/codex_skill_surface.go
      to: cmd/command_guide.go
      via: Command IDs resolve through commandGuideCatalog without replacing runtime routes
---

<objective>
Give a fresh installation nine working ant entrypoints and prove the selected discovery location with a real Codex client.
Purpose: Establish an end-to-end path before expanding migration and guidance.
Output: A deterministic nine-skill payload, transactional clean install and a narrowly scoped real-client receipt.
</objective>

<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-CONTEXT.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-RESEARCH.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-PATTERNS.md

Existing interfaces: codexSkillShim has Dir, Name, Description, Body, WorkflowTriggers and TaskKeywords. commandGuideCatalog supplies commandGuideDefinition, including RunCommand and SkillReference. maintenanceMutationTarget supplies Root, RelativeTarget, Content, Mode, ExpectedDigest and Managed. commitMaintenanceMutation accepts a maintenanceMutationPlan. These are existing interfaces; the functions listed under Artifacts below are proposed.

The owner chose canonical ant names. Go state authority and all actual runtime routes remain unchanged. No new runtime workflow, external package, plugin or public helper command is needed.
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Install the complete nine-entry public surface through the registered install path</name>
  <reversibility rating="costly">A versioned distribution envelope is shared across later consumers; its strict reader and retained transaction images keep replacement recoverable.</reversibility>
  <files>cmd/codex_skill_surface.go, cmd/codex_skill_install.go, cmd/platform_sync.go, cmd/install_cmd.go, cmd/codex_skill_surface_test.go</files>
  <read_first>Read each listed existing file; for new files inspect their absence and the analogs in cmd/platform_sync.go. Read cmd/command_guide.go, cmd/lifecycle_transaction.go, cmd/e2e_install_setup_update_test.go and the three .aether/skills/colony/aether-colony-*/SKILL.md support sources. Read the phase RESEARCH distribution contract.</read_first>
  <behavior>Registered install into a temporary home produces ant-init, ant-discuss, ant-oracle, ant-colonize, ant-plan, ant-build, ant-continue, ant-swarm and ant-seal; no helper SKILL.md or default aether menu is produced. Bad inventories and an unowned destination refuse skill writes. Actual command-guide/runtime routes remain byte-correct.</behavior>
  <action>Create codexPublicSkillCommands, codexSkillPayload, buildCodexSkillPayload and validateCodexSkillPayload in cmd/codex_skill_surface.go. Use exact ASCII command IDs and the nine independent expected names above. Emit schema codex-skill-payload/v1, source_version, min_runtime_version, available generator identity and per-file relative path/mode/SHA-256; validation rejects missing definitions instead of silently continuing. Generate public ant- names/triggers and place the three full helper bodies in support/*.md with no SKILL.md leaf; fold the command-guide and automatic worker-skill-loader explanations into public bodies. Resolve support references relative to the installed skill, keeping aether command-guide and def.RunCommand unchanged. Replace install's syncCodexSkillShims call with planCodexSkillTargets and syncCodexSkillsFromPayload in cmd/codex_skill_install.go: validate the payload and exact CodexHome targets before committing the skills and .aether-owned.json through commitMaintenanceMutation. Clean absent targets are writable; existing unproven targets are reported collisions, never overwritten. Preserve existing non-skill installation behavior and channel selection. Use .codex/skills/aether under the selected home; do not change a transaction root to the whole home. Retain no independent direct-write/prune kernel for public skills. Add a registered-install test and a missing-generator-call negative control, using temporary roots and no provider.</action>
  <acceptance_criteria>
    - TestCodexAntSkillInstallTracer executes the registered install command and checks the independent list of nine names, YAML names, runtime routes, relative support resolution and ownership digests.
    - TestCodexAntSkillInventory rejects nil/empty inventories, one missing member, duplicate names, case variants, Unicode lookalikes and missing guide definitions before target writes.
    - A collision at ant-plan/SKILL.md preserves its bytes and causes a non-success skill result; no new ownership proof is fabricated.
    - Removing the install-to-skill planner connection makes the tracer fail.
  </acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexAntSkill(InstallTracer|Inventory|CleanInstallCollision)$' -count=1 -timeout 90m</automated></verify>
  <done>A real clean installation contains nine canonical entrypoints and private support, with all skill writes governed by the shared transaction.</done>
</task>

<task type="auto">
  <name>Qualify the chosen discovery root before migration work proceeds</name>
  <files>cmd/codex_skill_live_test.go, .planning/phases/204.1-codex-ant-skill-surface/evidence/discovery-tracer.json</files>
  <read_first>Read the listed files if present, cmd/codex_skill_surface_test.go, cmd/codex_e2e_test.go, the host-capabilities research and Phase 205's existing codex-run.txt only as a capture-format analog. Inspect the installed codex --help and exec --help before selecting supported flags.</read_first>
  <action>Add opt-in TestCodexAntSkillFreshHost and capture/receipt helpers. With AETHER_CODEX_SKILL_LIVE=1 and AETHER_CODEX_SKILL_LIVE_CASES=plan, build/use the exact candidate executable, install into a disposable home and open a genuinely new Codex process with an isolated discovery/configuration scope and disposable repository. Reuse existing authentication through supported host configuration without logging or committing credentials, and do not modify the user's configuration or shared installed skills. Invoke only $ant-plan with an instruction to perform its first read-only guidance step and stop before planning/dispatch/mutation. Require host/tool evidence of the installed SKILL.md being selected/loaded and the actual aether command-guide plan --platform codex call; a model's claimed success is insufficient. Capture source revision, executable digest/version, client version, payload identity, root, loaded paths/digests, raw event-log references, exit status and outcome. Repeat the same case in a fresh isolated home with the selected skill missing; the evidence validator must refuse it. Only a real positive observation qualifies the legacy root. Missing authentication/provider access records unavailable and fails the required live check. A discovery failure records the observed limitation and returns for a narrowly scoped root/transaction revision; do not silently switch to .agents, widen containment or continue claiming supported discovery.</action>
  <acceptance_criteria>
    - The positive receipt links actual host events to the installed ant-plan bytes and observed command-guide invocation.
    - The missing-skill control cannot pass even if the model knows the expected command.
    - Live=false may skip during ordinary product tests, but the explicit live command fails on skipped, missing, unavailable or fabricated proof.
    - The owner project and original Phase 205 before-inventory are untouched.
  </acceptance_criteria>
  <verify><automated>AETHER_CODEX_SKILL_LIVE=1 AETHER_CODEX_SKILL_LIVE_CASES=plan go test ./cmd -run '^TestCodexAntSkillFreshHost$' -count=1 -timeout 90m</automated></verify>
  <done>The selected root has a real bounded discovery receipt; downstream expansion consumes this successful tracer.</done>
</task>
</tasks>

<verification>
Run both task commands. Record actual test discovery/execution counts, candidate/client identity and the missing-skill negative control. Do not infer workflow completion from this entry-routing proof.
</verification>

<success_criteria>
Clean install is wired through production code; exactly nine public names exist; the helper bodies are readable private support; the real-client root assumption is qualified before dependent plans execute.
</success_criteria>

## Flagged assumptions

- The observed .codex/skills/aether discovery root works on the supported fresh client. Task 2 must settle this with evidence; a different root requires an explicit follow-up revision.
- Paid/authenticated host availability is an execution prerequisite, not a planning result.

## Artifacts this phase produces

- New files: cmd/codex_skill_surface.go, cmd/codex_skill_install.go, cmd/codex_skill_surface_test.go, cmd/codex_skill_live_test.go and evidence/discovery-tracer.json.
- New symbols: codexPublicSkillCommands, codexSkillPayload, codexSkillPayloadFile, buildCodexSkillPayload, validateCodexSkillPayload, planCodexSkillTargets, syncCodexSkillsFromPayload and live-capture/receipt helpers; TestCodexAntSkillInstallTracer, TestCodexAntSkillInventory, TestCodexAntSkillCleanInstallCollision and TestCodexAntSkillFreshHost.
- New generated artifacts: nine ant-*/SKILL.md files, three support/aether-colony-*.md files and .aether-owned.json; payload fields schema_version, source_version, min_runtime_version, generator_identity, commands, files, relative_path, mode and sha256. Exact ownership structure is completed in Plan 02.

<output>
Create .planning/phases/204.1-codex-ant-skill-surface/204.1-01-SUMMARY.md with commands, raw capture locations and honest outcomes.
</output>
