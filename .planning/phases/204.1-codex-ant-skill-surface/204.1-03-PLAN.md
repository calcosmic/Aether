---
phase: 204.1-codex-ant-skill-surface
plan: "03"
type: execute
wave: 3
depends_on: ["204.1-02", "204.1-04"]
files_modified:
  - cmd/install_cmd.go
  - cmd/publish_cmd.go
  - cmd/codex_skill_payload_test.go
  - cmd/update_cmd.go
  - cmd/codex_skill_install.go
  - cmd/codex_skill_update_test.go
  - cmd/e2e_install_setup_update_test.go
autonomous: true
requirements: [ANT-01, ANT-02, ANT-03]
must_haves:
  truths:
    - "Publish, install and normal registered update consume the same validated versioned skill payload and ownership policy."
    - "Ordinary update includes skills, bounded legacy removals and the ownership record in its existing preview and transaction."
    - "Repeating an update with the same published input leaves skill bytes, modes and ownership identity unchanged."
    - "An updater with an older compiled command inventory preserves a compatible newer published inventory or refuses an incompatible payload before mutation."
  artifacts:
    - path: cmd/codex_skill_payload_test.go
      provides: Published-payload and install-consumption production tests
    - path: cmd/codex_skill_update_test.go
      provides: Registered normal/forced update and version compatibility tests
  key_links:
    - from: cmd/publish_cmd.go
      to: cmd/install_cmd.go
      via: setupInstallHub publishes the validated codex-skills payload before successful home installation
    - from: cmd/update_cmd.go
      to: cmd/codex_skill_install.go
      via: appendMaintenanceUpdateCodexSkillTargets before prepareMaintenanceMutation
---

<objective>
Make publishing and ordinary updates deliver the same ant skills that a clean install produces.
Purpose: An update must keep the new names working instead of skipping skills or restoring older ones.
Output: A versioned hub payload and registered update wiring with truthful receipts.
</objective>
<execution_context>
@/Users/callumcowie/.claude/gsd-core/workflows/execute-plan.md
@/Users/callumcowie/.claude/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/204.1-codex-ant-skill-surface/204.1-CONTEXT.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-RESEARCH.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-PATTERNS.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-02-SUMMARY.md
@.planning/phases/204.1-codex-ant-skill-surface/204.1-04-SUMMARY.md

Existing production signatures: setupInstallHub(hubDir, packageDir, version) returns a result map; syncPlatformHomeAssets(packageDir, homeDir, channel, force) returns result details/errors; runMaintenanceUpdate assembles maintenanceMutationPlan then prepares/commits it. platformHomeHubSyncPairs currently has no skills. Plans 01/02 provide buildCodexSkillPayload, validateCodexSkillPayload, planCodexSkillTargets and ownership handling.
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Publish a validated payload and install those exact bytes</name>
  <files>cmd/install_cmd.go, cmd/publish_cmd.go, cmd/codex_skill_install.go, cmd/codex_skill_payload_test.go</files>
  <read_first>Read all listed existing files, cmd/codex_skill_surface.go, cmd/codex_skill_ownership.go, cmd/publish_cmd_test.go if present, and the actual hub sync cleanup policy in cmd/install_cmd.go. Read Plan 04's completed support/guide changes before generating payloads.</read_first>
  <behavior>The registered publish path places a complete manifest and matching files at hub/system/codex-skills; its install step uses exactly those bytes. A corrupt, incomplete or incompatible payload cannot produce a successful skill sync. Dev remains isolated by default.</behavior>
  <action>Publish the Plan 01 payload as system/codex-skills/manifest.json plus the nine public files and three private support files. Add loadCodexSkillPayload and publishCodexSkillPayload behind the existing setupInstallHub/runPublish path; stage and validate the complete desired payload and commit its exact owned files/manifest together through the existing transaction mechanism. Make generic hub cleanup explicitly preserve this generated subtree rather than deleting it because it has no .aether source mirror. Source version is the caller's authoritative version; include the actual executing generator identity when available and never invent a revision. Validate minimum runtime/schema and all file digests. After publishing, syncPlatformHomeAssets must read the same validated hub payload consumed by update, not regenerate an independent home payload. Preserve install from a supported local package by constructing/publishing that payload through the same helper first. Propagate payload errors to non-success publish/install output, including aggregated hub errors that would otherwise be hidden. Preserve existing binary build/version verification and TS-host behavior. Use temporary homes/binaries in tests; no shared local or public publication.</action>
  <acceptance_criteria>
    - TestCodexAntSkillPublishedPayload executes registered publish and install against isolated homes and compares payload identity plus installed bytes/modes.
    - TestCodexAntSkillPayloadValidation rejects absent/empty/partial payloads, changed content with stale digest, unsupported schema and incompatible min_runtime_version before home mutation.
    - TestCodexAntSkillPublishRepeat leaves the payload and installed ownership identity stable, and hub cleanup does not erase codex-skills.
    - Dev publish/install without explicit home-sync preserves stable skill bytes; any existing explicit opt-in is clearly reported.
  </acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexAntSkill(PublishedPayload|PayloadValidation|PublishRepeat|PublishChannel)$' -count=1 -timeout 90m</automated></verify>
  <done>One versioned published payload reaches both the hub and installed Codex surface through the shared ownership policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Include skills in ordinary update's exact transaction</name>
  <files>cmd/update_cmd.go, cmd/codex_skill_install.go, cmd/codex_skill_update_test.go, cmd/e2e_install_setup_update_test.go</files>
  <read_first>Read every listed existing file, cmd/platform_sync.go, cmd/codex_project_docs.go and cmd/maintenance_mutation_199_test.go. Trace runMaintenanceUpdate, appendMaintenanceUpdatePlatformTargets and maintenancePreviewSyncDetails rather than the unused direct helper.</read_first>
  <action>Add appendMaintenanceUpdateCodexSkillTargets to load the hub payload and append planCodexSkillTargets outputs before the public update controller calls prepareMaintenanceMutation. The main plan must contain every skill write/removal and .aether-owned.json change; there is no post-commit sync. Carry collision/preserved-legacy and preserved-custom-project-document dispositions in structured result details and the plain-English result, using the shared Skills (codex shims) asset label for restart compatibility. Reject malformed/missing declared payloads rather than silently reporting completeness. A compatible later manifest supplies its own desired inventory; never derive retirement from this binary's nine names. Respect minimum runtime version, refuse installed-version downgrade, and retain dev update's existing no-home-sync default and explicit --sync-platform-homes refusal. Add real rootCmd.Execute tests for update and update --force, update --dry-run, second-run idempotency and compatible newer inventory. Keep fixture state consistent with the actual runtime's version/stale-publish gates.</action>
  <acceptance_criteria>
    - TestCodexAntSkillRegisteredUpdate runs normal update without force and migrates the known legacy fixture to the published nine entries.
    - TestCodexAntSkillRegisteredUpdateRepeat proves no skill byte/mode/ownership changes on the next update.
    - TestCodexAntSkillUpdatePreview proves zero mutation/journal/lock creation and includes the same exact skill targets as commit.
    - TestCodexAntSkillUpdateVersions preserves a compatible later payload's extra entry, refuses unsupported/downgrade cases and never prunes unfamiliar entries.
    - Removing appendMaintenanceUpdateCodexSkillTargets makes the registered update test fail while generator unit tests can still pass.
  </acceptance_criteria>
  <verify><automated>go test ./cmd -run '^TestCodexAntSkill(RegisteredUpdate|RegisteredUpdateRepeat|UpdatePreview|UpdateVersions|UpdateChannel)$' -count=1 -timeout 90m</automated></verify>
  <done>Normal update reaches the same payload and ownership policy within the transaction it already owns, with honest preservation and restart inputs.</done>
</task>
</tasks>
<verification>
Run both task groups, inspect actual registered-command outputs and retain the removed-update-caller negative control. Existing pre-feature binaries require a current runtime/bootstrap; do not promise they gain this update behavior without upgrading.
</verification>
<success_criteria>
Clean install, publish, normal update and replay agree on payload identity. Validation failures and collisions cannot become partial successful skill updates.
</success_criteria>

## Artifacts this phase produces

- New files: cmd/codex_skill_payload_test.go and cmd/codex_skill_update_test.go.
- New functions: loadCodexSkillPayload, publishCodexSkillPayload and appendMaintenanceUpdateCodexSkillTargets.
- New generated hub tree: system/codex-skills/manifest.json, ant-*/SKILL.md and support/*.md.
- New tests: TestCodexAntSkillPublishedPayload, PayloadValidation, PublishRepeat, PublishChannel, RegisteredUpdate, RegisteredUpdateRepeat, UpdatePreview, UpdateVersions and UpdateChannel, each with the TestCodexAntSkill prefix.
- New result details: skill payload identity and preserved/collision dispositions within the existing maintenance result, not a second state store.

<output>Create .planning/phases/204.1-codex-ant-skill-surface/204.1-03-SUMMARY.md.</output>
