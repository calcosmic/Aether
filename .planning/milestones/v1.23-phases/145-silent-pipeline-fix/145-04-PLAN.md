---
phase: 145-silent-pipeline-fix
plan: 04
type: execute
wave: 2
depends_on:
  - 145-01-PLAN.md
files_modified:
  - .aether/docs/command-playbooks/build-full.md
  - .aether/docs/command-playbooks/continue-full.md
autonomous: true
requirements:
  - PIPE-02
must_haves:
  truths:
    - No playbook or wrapper instruction writes COLONY_STATE.json directly via shell jq or node
    - All state mutations go through aether state-mutate with targeted jq expressions
    - Playbook prose documents the state-mutate-only policy
  artifacts:
    - path: .aether/docs/command-playbooks/build-full.md
      provides: Build full playbook using only state-mutate for state writes
    - path: .aether/docs/command-playbooks/continue-full.md
      provides: Continue full playbook using only state-mutate for state writes
  key_links:
    - from: playbook instructions
      to: cmd/state_cmds.go
      via: aether state-mutate CLI calls
      pattern: "aether state-mutate"
---

<objective>
Replace all direct COLONY_STATE.json writes in playbooks with state-mutate CLI calls, per PIPE-02 and D-02.

Purpose: Prevent LLM wrappers from reconstructing full JSON and corrupting colony state. All state mutations must use the atomic, targeted jq path in the Go runtime.
Output: build-full.md and continue-full.md updated to use state-mutate exclusively for state writes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-silent-pipeline-fix/145-CONTEXT.md
@.planning/phases/145-silent-pipeline-fix/145-RESEARCH.md

## Target Patterns

Direct COLONY_STATE.json writes found in playbooks:
- build-full.md:242-251 — "Read then update .aether/data/COLONY_STATE.json" followed by "Write COLONY_STATE.json."
- continue-full.md:1149 — "Write COLONY_STATE.json."
- continue-full.md:1012 — "Update COLONY_STATE.json:" (may be via state-mutate already — verify).
- continue-full.md:1579 — "Set last_commit_suggestion_phase to {phase_id} in COLONY_STATE.json"
- continue-full.md:1620 — "Record the suggestion: Set context_clear_suggested to true in COLONY_STATE.json"

## Reference Implementation

From cmd/state_cmds.go:
- `state-mutate` supports: `--field` / `--value` mode, jq expression mode, `--guard`, `--verify-only`, `--revert`.
- `executeFieldMode` handles: goal, state, current_phase, milestone, colony_depth, plan_granularity, colony_name, parallel_mode.
- `executeExpression` handles arbitrary jq-like expressions with atomic write.
- `applyCappedAppend` handles `.events = ((.events[-99:]) + ["..."])` patterns.

## Decision Reference

- D-02: State mutations that fail are hard failures — corrupted state is worse than no state.
- state-mutate already implements atomic writes with validation.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace direct COLONY_STATE.json writes in build-full.md</name>
  <files>.aether/docs/command-playbooks/build-full.md</files>
  <read_first>
    .aether/docs/command-playbooks/build-full.md
    cmd/state_cmds.go (lines 251-324 for field mode, lines 326-352 for expression mode)
  </read_first>
  <action>
    In build-full.md, find the "Step 2: Update State" section (around line 240-258) which currently says:
    - "Read then update .aether/data/COLONY_STATE.json"
    - Lists fields to set: state, current_phase, plan.phases[N].status, build_started_at, events
    - "Write COLONY_STATE.json."

    Replace the entire direct-write block with a single `aether state-mutate` expression call that sets all fields atomically. Use the same jq expression pattern already used in build-prep.md line 266:
    ```
    aether state-mutate \
      --argjson phase "$PHASE_NUMBER" \
      --arg phase_name "$PHASE_NAME" \
      --arg timestamp "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
      '.state = "EXECUTING" | .current_phase = $phase | .plan.phases |= map(if .id == $phase then .status = "in_progress" else . end) | .build_started_at = $timestamp | .events = ((.events[-99:]) + [$timestamp + "|phase_started|build|Phase " + ($phase|tostring) + ": " + $phase_name + " started"])'
    ```

    Also check build-full.md line 251 "Write COLONY_STATE.json." — remove it entirely since state-mutate handles the write.

    Verify no other direct-write instructions exist in build-full.md by searching for "Write COLONY_STATE.json" or "jq ... > .aether/data/COLONY_STATE.json".
  </action>
  <verify>
    <automated>grep -n "Write COLONY_STATE.json" .aether/docs/command-playbooks/build-full.md | wc -l | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    build-full.md Step 2 uses state-mutate exclusively.
    No "Write COLONY_STATE.json" instructions remain in build-full.md.
  </done>
  <acceptance_criteria>
    - `grep -n "Write COLONY_STATE.json" .aether/docs/command-playbooks/build-full.md` returns zero lines.
    - The Step 2 section contains an `aether state-mutate` call with the correct jq expression.
    - The state-mutate call sets state, current_phase, plan.phases status, build_started_at, and events in one atomic operation.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace direct COLONY_STATE.json writes in continue-full.md</name>
  <files>.aether/docs/command-playbooks/continue-full.md</files>
  <read_first>
    .aether/docs/command-playbooks/continue-full.md
    .aether/docs/command-playbooks/continue-advance.md (lines 303-326 for state-mutate reference)
  </read_first>
  <action>
    In continue-full.md, find and replace all direct COLONY_STATE.json write instructions:

    1. Around line 1012 "Update COLONY_STATE.json:" — verify it already uses state-mutate (from continue-advance.md pattern). If it uses direct jq > file, replace with state-mutate.

    2. Around line 1149 "Write COLONY_STATE.json." — replace with state-mutate calls. The fields being set are: state, current_phase, plan.phases status, build_started_at=null, and events append. Use the same capped-append pattern for events.

    3. Around line 1579 "Set last_commit_suggestion_phase to {phase_id} in COLONY_STATE.json" — replace with:
       ```
       aether state-mutate --argjson phase_id "$phase_id" '.last_commit_suggestion_phase = $phase_id'
       ```

    4. Around line 1620 "Record the suggestion: Set context_clear_suggested to true in COLONY_STATE.json" — replace with:
       ```
       aether state-mutate '.context_clear_suggested = true'
       ```

    Also check for any `jq ... > .aether/data/COLONY_STATE.json` patterns and replace them with equivalent state-mutate calls. Use `--field` mode for simple single-field updates and expression mode for complex updates.
  </action>
  <verify>
    <automated>grep -n "Write COLONY_STATE.json" .aether/docs/command-playbooks/continue-full.md | wc -l | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    continue-full.md has zero "Write COLONY_STATE.json" instructions.
    All state mutations use aether state-mutate with targeted jq.
  </done>
  <acceptance_criteria>
    - `grep -n "Write COLONY_STATE.json" .aether/docs/command-playbooks/continue-full.md` returns zero lines.
    - `grep -n "> .aether/data/COLONY_STATE.json" .aether/docs/command-playbooks/continue-full.md` returns zero lines.
    - All former direct-write locations now use `aether state-mutate`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add state-mutate-only policy note to playbook prose</name>
  <files>
    .aether/docs/command-playbooks/build-full.md
    .aether/docs/command-playbooks/continue-full.md
  </files>
  <read_first>
    .aether/docs/command-playbooks/build-prep.md (line 255 for policy reference)
  </read_first>
  <action>
    Add a policy note near the top of build-full.md and continue-full.md (in the "Instructions" or "Context" section):

    ```
    ## State Mutation Policy

    All writes to COLONY_STATE.json MUST go through `aether state-mutate` with targeted jq expressions.
    Never use the Write tool, `jq ... > file`, or any other direct file modification on COLONY_STATE.json.
    The Go runtime handles atomic writes, validation, and backups — direct writes risk corrupting colony state.
    ```

    This documents the policy for future playbook editors and LLM executors.
  </action>
  <verify>
    <automated>grep -n "State Mutation Policy" .aether/docs/command-playbooks/build-full.md .aether/docs/command-playbooks/continue-full.md | wc -l | xargs -I {} test {} -eq 2</automated>
  </verify>
  <done>
    Both build-full.md and continue-full.md contain the State Mutation Policy note.
  </done>
  <acceptance_criteria>
    - `grep -n "State Mutation Policy" .aether/docs/command-playbooks/build-full.md` finds the note.
    - `grep -n "State Mutation Policy" .aether/docs/command-playbooks/continue-full.md` finds the note.
    - Each note explicitly prohibits direct COLONY_STATE.json writes and mandates state-mutate.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playbook instruction -> COLONY_STATE.json | Direct writes bypass atomic guards and validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-09 | Tampering | COLONY_STATE.json | mitigate | Mandate state-mutate for all writes; atomic temp+rename prevents partial writes |
| T-145-10 | Denial of Service | Playbook policy | mitigate | Policy note ensures future playbook edits follow the safe path |
</threat_model>

<verification>
1. `grep -rn "Write COLONY_STATE.json" .aether/docs/command-playbooks/` returns zero lines.
2. `grep -rn "> .aether/data/COLONY_STATE.json" .aether/docs/command-playbooks/` returns zero lines.
3. `grep -rn "State Mutation Policy" .aether/docs/command-playbooks/build-full.md .aether/docs/command-playbooks/continue-full.md` finds both notes.
</verification>

<success_criteria>
- No playbook contains instructions to write COLONY_STATE.json directly.
- All state mutations use aether state-mutate with targeted jq.
- Policy note documents the requirement for future editors.
</success_criteria>

<output>
After completion, create `.planning/phases/145-silent-pipeline-fix/145-04-SUMMARY.md`
</output>
