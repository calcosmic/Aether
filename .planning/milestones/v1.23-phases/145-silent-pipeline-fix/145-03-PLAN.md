---
phase: 145-silent-pipeline-fix
plan: 03
type: execute
wave: 2
depends_on:
  - 145-01-PLAN.md
files_modified:
  - cmd/pending_decision.go
  - cmd/discuss.go
  - cmd/orchestrator_boundary_guidance.go
  - cmd/orchestrator_boundary_clarification.go
  - cmd/discuss_test.go
autonomous: true
requirements:
  - PIPE-03
must_haves:
  truths:
    - Every command that reads pending decisions filters by current session scope
    - Stale decisions from prior colonies never appear in /ant-flag, /ant-plan, or /ant-council output
    - Session scoping is enforced in the Go runtime, not left to playbook instructions
  artifacts:
    - path: cmd/pending_decision.go
      provides: Pending decision CLI commands with session scoping
      exports: ["pendingDecisionAddCmd", "pendingDecisionListCmd", "pendingDecisionResolveCmd"]
    - path: cmd/discuss.go
      provides: Discuss command with session-scoped pending decisions
      exports: ["filterPendingDecisionFileForScope", "pendingDecisionMatchesScope", "loadCurrentPendingDecisionScope"]
    - path: cmd/orchestrator_boundary_guidance.go
      provides: Boundary guidance with scoped pending decisions
    - path: cmd/orchestrator_boundary_clarification.go
      provides: Boundary clarification with scoped pending decisions
    - path: cmd/discuss_test.go
      provides: Tests for session scoping behavior
  key_links:
    - from: cmd/pending_decision.go
      to: cmd/discuss.go
      via: imports filterPendingDecisionFileForScope, pendingDecisionMatchesScope, loadCurrentPendingDecisionScope
      pattern: "filterPendingDecisionFileForScope.*loadCurrentPendingDecisionScope"
    - from: cmd/orchestrator_boundary_guidance.go
      to: cmd/discuss.go
      via: calls filterPendingDecisionFileForScope and loadCurrentPendingDecisionScope
    - from: cmd/orchestrator_boundary_clarification.go
      to: cmd/discuss.go
      via: calls loadCurrentPendingDecisionScope and stampPendingDecisionScope
---

<objective>
Verify and enforce pending-decisions session scoping across all Go runtime readers, per PIPE-03.

Purpose: Ensure stale decisions from prior colonies never leak into current colony operations.
Output: All pending-decision readers use session scoping, with tests proving cross-session isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-silent-pipeline-fix/145-CONTEXT.md
@.planning/phases/145-silent-pipeline-fix/145-RESEARCH.md

## Interface Context

From cmd/pending_decision.go (current):
- `pendingDecisionAddCmd` at line 32: adds decisions but does NOT stamp with session scope on add (line 63 calls stampPendingDecisionScope — OK).
- `pendingDecisionListCmd` at line 86: loads ALL decisions, filters by `filterUnresolved` and `filterType` but does NOT filter by session scope. This is the gap.
- `pendingDecisionResolveCmd` at line 137: checks scope on resolve (line 166) — OK.

From cmd/discuss.go (current):
- `loadCurrentPendingDecisionScope()` at line 546: reads COLONY_STATE.json, extracts SessionID and GoalHash.
- `pendingDecisionMatchesScope()` at line 603: prefers SessionID match, falls back to GoalHash, then InitializedAt.
- `filterPendingDecisionFileForScope()` at line 590: splits file into active vs stale decisions.
- `discuss.go` already uses scope filtering (line 208, 212).

From cmd/orchestrator_boundary_guidance.go (current):
- Line 102: `filterPendingDecisionFileForScope(loadPendingDecisionFile(), loadCurrentPendingDecisionScope())` — already scoped.

From cmd/orchestrator_boundary_clarification.go (current):
- Line 34: `loadCurrentPendingDecisionScope()` — already scoped.
- Line 89: `stampPendingDecisionScope()` — already scoped.

## Gap Analysis

The ONLY reader that does NOT filter by session scope is `pendingDecisionListCmd` in `cmd/pending_decision.go`.
All other readers (discuss, boundary_guidance, boundary_clarification, resolve) already scope correctly.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session scope filtering to pending-decision-list</name>
  <files>cmd/pending_decision.go</files>
  <read_first>
    cmd/pending_decision.go
    cmd/discuss.go (lines 546-603 for scope functions)
  </read_first>
  <action>
    In `pendingDecisionListCmd` (line 86-134), after loading the decision file and before building the `filtered` slice, call `loadCurrentPendingDecisionScope()` and `filterPendingDecisionFileForScope(file, scope)` to split into active and stale decisions. Use only `active.Decisions` for the filtered output. Include the stale count in the output JSON under a new `stale` field so callers can see how many old decisions were hidden.

    The output JSON should include:
    - `total`: len(file.Decisions) (all decisions in file)
    - `unresolved`: count of unresolved in active
    - `stale`: len(stale.Decisions)
    - `decisions`: filtered active decisions (after applying unresolved/type filters)

    Do NOT change the command-line flags. The `--unresolved` and `--type` filters apply AFTER session scoping.
  </action>
  <verify>
    <automated>go test ./cmd/... -run TestPendingDecision -v</automated>
  </verify>
  <done>
    `pendingDecisionListCmd` returns only session-scoped decisions.
    Output includes `stale` count for transparency.
    `--unresolved` and `--type` flags still work on scoped subset.
  </done>
  <acceptance_criteria>
    - `cmd/pending_decision.go` `pendingDecisionListCmd` calls `filterPendingDecisionFileForScope` before other filters.
    - Output JSON contains `stale` field with count of hidden stale decisions.
    - `total` field reflects all decisions in file (active + stale).
    - `go test ./cmd/...` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add cross-session isolation tests</name>
  <files>cmd/discuss_test.go, cmd/pending_decision.go</files>
  <read_first>
    cmd/discuss_test.go
    cmd/pending_decision.go
  </read_first>
  <action>
    Add tests to `cmd/discuss_test.go` (or create `cmd/pending_decision_test.go` if discuss_test.go is too large):

    1. Test `pendingDecisionListCmd` with mixed-scope decisions:
       - Create a `PendingDecisionFile` with 2 decisions: one matching current session, one from a different session.
       - Call list logic (or simulate via test helper).
       - Assert returned decisions contain only the matching session decision.
       - Assert `stale` count is 1.

    2. Test `pendingDecisionResolveCmd` rejects stale decisions:
       - Create a decision from a different session.
       - Attempt to resolve it.
       - Assert error response (decision is stale for current goal/session).

    3. Test `pendingDecisionAddCmd` stamps new decisions with current scope:
       - Add a decision.
       - Load the file and assert the new decision has SessionID and GoalHash set.

    Use `loadCurrentPendingDecisionScope()` to get the current scope for test setup. Mock `store` with a temp directory and `storage.NewStore`.
  </action>
  <verify>
    <automated>go test ./cmd/... -run "TestPendingDecision|TestDecisionScope" -v</automated>
  </verify>
  <done>
    Tests prove list filters by session, resolve rejects stale, and add stamps scope.
    All tests pass.
  </done>
  <acceptance_criteria>
    - Test file exists with at least 3 test cases covering list filtering, resolve rejection, and add stamping.
    - Each test uses distinct session IDs to prove isolation.
    - `go test ./cmd/...` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify playbook readers use scoped CLI</name>
  <files>
    .aether/docs/command-playbooks/plan-prep.md
    .aether/docs/command-playbooks/build-prep.md
    .aether/docs/command-playbooks/continue-gates.md
  </files>
  <read_first>
    .aether/docs/command-playbooks/plan-prep.md
    .aether/docs/command-playbooks/build-prep.md
    .aether/docs/command-playbooks/continue-gates.md
  </read_first>
  <action>
    Verify that playbook instructions which read pending decisions use the scoped CLI path:
    - `aether pending-decision-list` (which now returns scoped results automatically — no playbook change needed).
    - Check if any playbook reads `pending-decisions.json` directly via `jq` or `cat` instead of using the CLI. If found, replace with `aether pending-decision-list` so scoping is enforced.
    - Check plan-prep.md line 49: `aether pending-decisions --count 2>/dev/null || echo "0"` — verify this command exists and is scoped. If `pending-decisions` is a different command from `pending-decision-list`, ensure it also scopes or redirect to the scoped command.

    If `aether pending-decisions --count` does not exist as a separate command, update the playbook to use `aether pending-decision-list --unresolved | jq '.unresolved'` or equivalent.
  </action>
  <verify>
    <automated>grep -rn "pending-decisions" .aether/docs/command-playbooks/ | grep -v "pending-decision-list" | grep -v "pending-decision-add" | grep -v "pending-decision-resolve" | wc -l | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    All playbook pending-decision reads go through the scoped CLI.
    No playbook bypasses scoping by reading the JSON file directly.
  </done>
  <acceptance_criteria>
    - `grep -rn "pending-decisions" .aether/docs/command-playbooks/` shows only calls to `pending-decision-list`, `pending-decision-add`, or `pending-decision-resolve`.
    - No playbook uses `jq` or `cat` on `pending-decisions.json` directly.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI reader -> pending-decisions.json | Unscoped reads could expose stale decisions |
| Playbook instruction -> JSON file | Playbooks bypassing CLI lose runtime scoping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-06 | Information Disclosure | pending-decision-list | mitigate | Filter by session scope so stale decisions are never returned |
| T-145-07 | Elevation of Privilege | pending-decision-resolve | mitigate | Reject resolve attempts on stale decisions with explicit error |
| T-145-08 | Tampering | Playbook direct JSON reads | mitigate | Replace direct jq/cat reads with scoped CLI calls |
</threat_model>

<verification>
1. `go test ./cmd/...` passes.
2. `grep -n "filterPendingDecisionFileForScope" cmd/pending_decision.go` finds the call in list command.
3. `grep -rn "pending-decisions.json" .aether/docs/command-playbooks/` returns zero lines (no direct file reads).
</verification>

<success_criteria>
- `pending-decision-list` only returns decisions matching the current session scope.
- Stale decisions are counted and reported but not returned.
- Playbooks use the scoped CLI, never direct JSON reads.
- Tests prove cross-session isolation.
</success_criteria>

<output>
After completion, create `.planning/phases/145-silent-pipeline-fix/145-03-SUMMARY.md`
</output>
