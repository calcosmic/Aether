---
phase: 145-silent-pipeline-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 145-01-PLAN.md
files_modified:
  - cmd/init_cmd.go
  - pkg/storage/storage.go
  - pkg/colony/colony.go
autonomous: true
requirements:
  - PIPE-04
  - PIPE-05
must_haves:
  truths:
    - Running `/ant-init` clears any stale session.json from a prior colony
    - The event array in colony state never exceeds 100 entries
    - Event cap enforcement happens in the Go storage layer, not in playbook instructions
  artifacts:
    - path: cmd/init_cmd.go
      provides: Init command with session.json cleanup
      exports: ["initCmd"]
    - path: pkg/storage/storage.go
      provides: Atomic write with event cap enforcement
      exports: ["Store.AtomicWrite", "Store.SaveJSON", "Store.UpdateFile"]
    - path: pkg/colony/colony.go
      provides: ColonyState struct with Events field
      exports: ["ColonyState"]
  key_links:
    - from: cmd/init_cmd.go
      to: pkg/storage/storage.go
      via: store.SaveJSON("session.json", session)
      pattern: "store.SaveJSON"
    - from: pkg/storage/storage.go
      to: pkg/colony/colony.go
      via: SaveJSON marshals ColonyState which contains Events
      pattern: "json.MarshalIndent.*ColonyState"
---

<objective>
Add session.json cleanup to `/ant-init` and enforce event array capping at the Go storage layer, per D-01 and D-02.

Purpose: Prevent stale session data from leaking between colonies, and ensure the event array cannot grow unbounded regardless of caller behavior.
Output: Updated init command, storage layer with cap enforcement, and tests verifying both behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-silent-pipeline-fix/145-CONTEXT.md
@.planning/phases/145-silent-pipeline-fix/145-RESEARCH.md

## Interface Context

From cmd/init_cmd.go (current):
- `initCmd` creates `session.json` via `store.SaveJSON("session.json", session)` at line 188.
- It does NOT remove any existing `session.json` before creating the new one.
- `createFreshColony` label at line 84 is where new colony state is built.

From pkg/storage/storage.go (current):
- `SaveJSON(path string, data interface{})` marshals and calls `AtomicWrite`.
- `AtomicWrite` does JSON validation but has no concept of array capping.
- No existing cap enforcement in the storage layer.

From pkg/colony/colony.go (current):
- `ColonyState.Events` is `[]string` with no size limit.
- Playbook instructions currently cap at 100 via jq: `.events = ((.events[-99:]) + [...])`.

## Decision References

- D-01: Data-persistence failures are non-blocking but visible (already handled in Plan 01).
- D-02: State mutations are hard failures — corrupted state is worse than no state.
- Event cap value stays at 100 (Claude's discretion, per CONTEXT.md).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session.json cleanup to init command</name>
  <files>cmd/init_cmd.go</files>
  <read_first>
    cmd/init_cmd.go
  </read_first>
  <action>
    In `initCmd`, before creating the new `session.json` (around line 175-188), add code that removes any existing `session.json` file from the data directory. Use `os.Remove` on `filepath.Join(dataDir, "session.json")`. Do not error if the file does not exist (it is idempotent). Add a comment explaining why: "Clear stale session from any prior colony to prevent old decisions from leaking in."

    The cleanup must happen after the `createFreshColony` label and before the new `session` struct is created, so that the new session is written to a clean file.
  </action>
  <verify>
    <automated>go test ./cmd/... -run TestInit -v</automated>
  </verify>
  <done>
    `initCmd` removes any existing session.json before writing the new one.
    No error occurs if session.json does not exist.
  </done>
  <acceptance_criteria>
    - `cmd/init_cmd.go` contains `os.Remove(filepath.Join(dataDir, "session.json"))` before the new session creation.
    - The removal is wrapped with a comment referencing stale session prevention.
    - `go test ./cmd/...` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Enforce event array cap in storage layer</name>
  <files>pkg/storage/storage.go, pkg/colony/colony.go</files>
  <read_first>
    pkg/storage/storage.go
    pkg/colony/colony.go
  </read_first>
  <action>
    In `pkg/storage/storage.go`, add event array capping to `SaveJSON` and `AtomicWrite`:
    1. When the path is `COLONY_STATE.json` and the data unmarshals to a `colony.ColonyState`, check if `len(state.Events) > 100`.
    2. If exceeded, trim to the last 100 entries: `state.Events = state.Events[len(state.Events)-100:]`.
    3. Re-marshal the trimmed state before writing.
    4. Log a warning to stderr when trimming occurs: `fmt.Fprintf(os.Stderr, "warning: event array capped at 100 (dropped %d old events)\n", dropped)`.

    Do NOT modify `pkg/colony/colony.go` — the struct stays unchanged. The cap is enforced at the storage write boundary.

    Avoid circular imports: `pkg/storage` must not import `pkg/colony`. Instead, use a generic JSON approach — unmarshal to `map[string]interface{}`, check `.events` array length, trim if needed, then re-marshal. This keeps the storage package independent.
  </action>
  <verify>
    <automated>go test ./pkg/storage/... -v</automated>
  </verify>
  <done>
    `SaveJSON` and `AtomicWrite` in `pkg/storage/storage.go` cap the `events` array at 100 entries when writing `COLONY_STATE.json`.
    A warning is logged to stderr when events are dropped.
    No circular dependency on `pkg/colony` exists.
  </done>
  <acceptance_criteria>
    - `pkg/storage/storage.go` contains cap logic that triggers only for `COLONY_STATE.json`.
    - Cap value is exactly 100.
    - Uses generic `map[string]interface{}` unmarshal to avoid importing `pkg/colony`.
    - Warning includes the count of dropped events.
    - `go test ./pkg/storage/...` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add tests for session cleanup and event cap</name>
  <files>cmd/init_test.go, pkg/storage/storage_test.go</files>
  <read_first>
    cmd/init_cmd.go
    pkg/storage/storage.go
    cmd/init_test.go (if exists)
    pkg/storage/storage_test.go (if exists)
  </read_first>
  <action>
    Add or update tests:

    1. In `cmd/init_test.go` (create if missing):
       - Test that running init when `session.json` already exists replaces it with a new session (different SessionID).
       - Test that running init when no `session.json` exists still succeeds.

    2. In `pkg/storage/storage_test.go` (create if missing):
       - Test that `SaveJSON("COLONY_STATE.json", stateWith120Events)` writes a file with exactly 100 events (the last 100).
       - Test that `SaveJSON("other-file.json", dataWithLargeArray)` does NOT cap any arrays (cap only applies to COLONY_STATE.json events).
       - Test that `AtomicWrite` with `COLONY_STATE.json` and 120 events produces 100 events.

    Use `os.MkdirAll` to create a temp directory, `storage.NewStore(tempDir)`, and `store.SaveJSON` / `store.LoadJSON` for assertions.
  </action>
  <verify>
    <automated>go test ./cmd/... ./pkg/storage/... -v</automated>
  </verify>
  <done>
    Tests prove session.json is replaced on init.
    Tests prove event array is capped at 100 for COLONY_STATE.json only.
    All tests pass.
  </done>
  <acceptance_criteria>
    - `cmd/init_test.go` contains a test verifying session.json replacement.
    - `pkg/storage/storage_test.go` contains tests verifying 100-event cap for COLONY_STATE.json and no cap for other files.
    - `go test ./cmd/... ./pkg/storage/...` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Init command -> filesystem | Init writes to session.json; stale data could leak across colonies |
| Storage layer -> COLONY_STATE.json | Unbounded events could cause file bloat or parsing issues |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-03 | Tampering | session.json | mitigate | Remove old file before writing new one prevents append or merge of stale data |
| T-145-04 | Denial of Service | COLONY_STATE.json Events | mitigate | Cap at 100 prevents unbounded growth causing slow loads or OOM |
| T-145-05 | Information Disclosure | Event cap warning | accept | Warning logs to stderr with drop count; no sensitive data in event count |
</threat_model>

<verification>
1. `go test ./cmd/... ./pkg/storage/...` passes.
2. `grep -n "os.Remove.*session.json" cmd/init_cmd.go` finds the cleanup line.
3. `grep -n "events" pkg/storage/storage.go` finds cap logic.
4. Manual: run `aether init "test goal"` twice with different goals and verify `session.json` SessionID changes.
</verification>

<success_criteria>
- `/ant-init` always starts with a fresh session.json.
- COLONY_STATE.json events array never exceeds 100 entries, enforced in Go storage.
- Tests cover both behaviors with executable evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/145-silent-pipeline-fix/145-02-SUMMARY.md`
</output>
