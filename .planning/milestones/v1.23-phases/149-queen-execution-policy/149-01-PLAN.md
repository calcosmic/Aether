---
phase: 149-queen-execution-policy
plan: 01
wave: 1
depends_on: []
autonomous: true
requirements: [QUEEN-01]
files_modified:
  - CLAUDE.md
---

# Plan 149-01: Align CLAUDE.md with Go Runtime VerificationDepth

**Objective:** Replace the incomplete 3-mode execution mode documentation in CLAUDE.md with accurate descriptions that match `cmd/review_depth.go` behavior exactly.

## must_haves

- CLAUDE.md documents the full 5-level priority chain for verification depth resolution
- CLAUDE.md documents what each depth level (light/standard/heavy) means in terms of spawned agents
- CLAUDE.md documents the smart default algorithm (mode + position + risk)
- The documentation is verifiable by a user reading it and predicting runtime behavior

## tasks

### Task 1: Audit current CLAUDE.md execution mode section

<read_first>
- CLAUDE.md (execution mode section, lines ~80-95)
- cmd/review_depth.go (resolveVerificationDepth, resolveSmartVerificationDepth, phaseRiskLevel, phasePositionLevel)
</read_first>

<action>
1. Read the execution mode section in CLAUDE.md
2. Document the exact lines where the 3-mode description lives
3. Read the full resolveVerificationDepth and resolveSmartVerificationDepth functions
4. Create a comparison table:
   | What CLAUDE.md says | What the runtime does | Match? |
5. Note every discrepancy
</action>

<acceptance_criteria>
- A markdown comparison table exists with at least 5 rows
- Every discrepancy is documented with exact CLAUDE.md quote and exact Go behavior
</acceptance_criteria>

### Task 2: Rewrite CLAUDE.md execution mode documentation

<read_first>
- CLAUDE.md (full file, to understand context)
- cmd/review_depth.go (all functions)
- cmd/caste_relevance.go (isAlwaysRequired for continue at each depth)
- cmd/queen_spawn_budget.go (queenMaxWorkersForBudget for continue)
</read_first>

<action>
1. Replace the 3-mode description with a detailed section covering:
   - The 5-level priority chain: explicit heavy > explicit light > --verification-depth string > keyword match > smart default
   - Smart default rules: discovery=light, production=at least standard, final phase=heavy, security keywords=heavy
   - What each depth means for continue (light: watcher only, standard: watcher+probe, heavy: watcher+gatekeeper+auditor+probe)
   - What each depth means for build (light: no gatekeeper/auditor, standard: auditor+probe, heavy: gatekeeper+auditor+probe)
   - What each depth means for seal (light: no review agents, standard: auditor+probe, heavy: gatekeeper+auditor+probe)
   - The spawn budget cap per depth (continue: light=3, standard=4, heavy=6 workers)
2. Keep the same plain-English style with a "for dummies" layer
3. Add a note: "If docs and runtime disagree, runtime wins"
</action>

<acceptance_criteria>
- A user reading the new CLAUDE.md section can correctly predict:
  - What depth a phase with name "Security hardening" will get (heavy)
  - What depth a phase with name "Documentation cleanup" in discovery mode will get (light)
  - Which agents spawn for continue at standard depth (watcher + probe + scored castes)
- The section is under 100 lines
</acceptance_criteria>

### Task 3: Add verification test

<read_first>
- cmd/review_depth_test.go (existing tests)
</read_first>

<action>
1. Create a test that reads CLAUDE.md and verifies key claims against runtime behavior:
   - Test that "discovery mode = light" is documented
   - Test that "final phase = heavy" is documented
   - Test that "security keywords = heavy" is documented
   - Test that each depth level lists the correct always-required castes
2. The test should fail if CLAUDE.md diverges from runtime behavior
</action>

<acceptance_criteria>
- `go test ./cmd/... -run TestCLAUDEMDVerificationDepth -count=1 -v` passes
- The test fails if CLAUDE.md claims something the runtime doesn't do
</acceptance_criteria>

## verification

```bash
# Run verification test
go test ./cmd/... -run TestCLAUDEMD -count=1 -v

# Run full suite
go test ./cmd/... -count=1
```

## risks

- CLAUDE.md is the user-facing contract; changes must not confuse non-technical users
- The "for dummies" layer is especially important here
