---
phase: 195-coherent-jobs
plan: 03
type: execute
wave: 2
depends_on: ["195-01"]
files_modified:
  - cmd/codex_build.go
  - cmd/codex_workflow_cmds.go
  - cmd/dispatch_coalesce_test.go
  - cmd/codex_build_test.go
autonomous: true
requirements: [JOBS-01, JOBS-02, JOBS-03, JOBS-04]

must_haves:
  truths:
    - "Every build path plans coherent jobs before job waves and before worktree ownership is constructed."
    - "A Queen proposal reaches the final dispatch manifest only after runtime validation, with accepted/refused/repaired decisions visible."
    - "Each grouped dispatch retains every covered task's requirements, constraints, criteria, evidence instructions, and declared paths."
    - "The real CalVault fixture produces one in-repo Builder dispatch and the same canonical job plan is used when parallel_mode is worktree."
  artifacts:
    - path: "cmd/codex_build.go"
      provides: "Canonical planner wiring, grouped dispatch metadata, job waves, briefs, and stable names"
    - path: "cmd/dispatch_coalesce_test.go"
      provides: "Real CalVault in-repo fail-then-pass integration fixture"
  key_links:
    - from: "cmd/codex_build.go"
      to: "cmd/coherent_jobs.go"
      via: "plannedBuildDispatchesWithJudgement calls planCoherentJobs before task-wave/worktree code"
      pattern: "planCoherentJobs"
    - from: "cmd/codex_workflow_cmds.go"
      to: "cmd/codex_build.go"
      via: "repeatable --job-proposal decoded into codexBuildOptions"
      pattern: "job-proposal"
---

<objective>
Make coherent jobs the one authoritative grouping pass for direct, plan-only,
selected-task, in-repo, and worktree builds.

Purpose: This is the reachability step. The pure planner must replace the
post-wave-only `coalesceSequentialDispatches` authority so grouping affects the
manifest, brief, deterministic worker identity, and later worktree ownership.
Output: CLI proposal transport, canonical planner wiring, grouped dispatch
metadata, and an end-to-end CalVault in-repo regression.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-coherent-jobs/195-CONTEXT.md
@.planning/phases/195-coherent-jobs/195-RESEARCH.md
@.planning/phases/195-coherent-jobs/195-PATTERNS.md
@.planning/phases/195-coherent-jobs/195-01-SUMMARY.md
@cmd/coherent_jobs.go
@cmd/codex_build.go
@cmd/codex_workflow_cmds.go
</context>

<interfaces>
Preserve `codexBuildDispatch.TaskID` as the primary compatibility identity and
`CoveredTaskIDs` as assignment scope. Add dispatch fields `JobName`,
`JobReason`, and `JobSource`; add manifest `JobDecisions`; add
`codexBuildOptions.JobProposals []coherentJobProposal`. Do not make wrappers
authoritative for any of these values.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire Queen proposals and the pre-wave planner into every build lane</name>
  <files>cmd/codex_build.go, cmd/codex_workflow_cmds.go, cmd/codex_build_test.go</files>
  <read_first>
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-02, D-04 through D-07, Go-authority rule, and integration points
    - .planning/phases/195-coherent-jobs/195-RESEARCH.md — Summary, architecture diagram, Pattern 3, Prescriptive Ordering waves 0/1, selected-task warning, and Pitfall 1
    - .planning/phases/195-coherent-jobs/195-PATTERNS.md — existing merge contract, additive proposal pattern, and worktree exclusion location
    - .planning/phases/195-coherent-jobs/195-01-SUMMARY.md — exact planner types and any executor-approved signature deviations
    - cmd/codex_build.go — codexBuildDispatch, codexBuildManifest, codexBuildOptions, runCodexBuildPlanOnlyWithOptions, plannedBuildDispatchesWithJudgement, taskWaves/coalescer, manifest/brief writes, deterministic name allocation
    - cmd/codex_workflow_cmds.go — build flag parsing and the Phase 194 proposal/re-fetch path
    - cmd/codex_build_test.go — plan-only/direct/selected-task and zero-side-effect command fixtures
  </read_first>
  <behavior>
    - A repeatable `--job-proposal` JSON object reaches runtime options, and accepted/refused/repaired job decisions appear in the returned manifest/result.
    - Invalid JSON, duplicate proposal names, unknown/unselected/completed task IDs, missing relationship/benefit, missing cross-caste owner reason, missing dependency, or a cycle creates no attempt or manifest.
    - Direct, plan-only, selected-task, and worktree-mode planning all call the same pre-wave planner.
    - A refused proposal is not silently reordered; its runtime replacement jobs are visible and safe proposals remain unchanged.
  </behavior>
  <action>
Write command/integration tests first and record RED. Add repeatable
`--job-proposal` parsing into `codexBuildOptions.JobProposals`; decode one JSON
object per flag and return all named parse/contract errors before
`runCodexBuildPlanOnlyWithOptions` can checkpoint or begin an attempt. Extend
`codexBuildManifest` with job decisions and each `codexBuildDispatch` with job
name/reason/source while preserving TaskID and CoveredTaskIDs. Refactor
`plannedBuildDispatchesWithJudgement` to resolve one owner seed per task, call
`planCoherentJobs`, build one dispatch per returned job, and assign dispatch
waves from the job DAG. This call must happen before `taskWaves`,
`validateDeclaredWorktreeOwnership`, or any worktree conversion; remove the
parallel-mode exclusion and any live call that treats
`coalesceSequentialDispatches` as authoritative. Keep a compatibility helper
only if an existing caller demonstrably needs it. Persist the runtime decision,
never wrapper-authored groups.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestBuildJobProposalRoundTrip|TestBuildRejectsInvalidJobProposalBeforeAttempt|TestGroupingOccursBeforeWavesAndWorktreeOwnership|TestSelectedTaskJobProposalCannotPullOtherTasks|TestUnsafeJobProposalIsVisiblyRepaired' -count=1` passes
    - A side-effect assertion proves invalid proposal/cycle input creates no checkpoint, manifest, attempt, worktree, or executing-state transition
    - A wiring assertion proves direct, plan-only, selected-task, and worktree options all reach `planCoherentJobs`
    - No production build path calls `coalesceSequentialDispatches` after dispatch waves exist
    - Manifest JSON preserves existing task_id/covered_task_ids and adds accepted/refused/repaired decisions with job name, reason, source, offending task, and dependency when applicable
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(BuildJobProposal|BuildRejectsInvalidJobProposal|GroupingOccursBefore|SelectedTaskJobProposal|UnsafeJobProposal)' -count=1</automated>
  </verify>
  <done>All build modes consume one Go-validated coherent-job plan before waves or worktree ownership, and Queen decisions are visible without becoming authoritative wrapper state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-prove CalVault, grouped briefs, task credit scope, and stable identity</name>
  <files>cmd/codex_build.go, cmd/dispatch_coalesce_test.go, cmd/codex_build_test.go</files>
  <read_first>
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-01 through D-04 plus JOBS-02/JOBS-03 observable proof
    - .planning/research/v1.27-milestone-brief.md — feature 3 and the exact CalVault proof promise
    - .planning/HARDENING-PLAN.md — H5 field shape and the old worktree exclusion
    - cmd/dispatch_coalesce_test.go — six dependent file-copy batches and independent/cross-caste controls
    - cmd/codex_build.go — mergeDispatchInto, dispatchCoveredTaskIDs, findDispatchTasks, renderCoveredTaskSummary, dispatchCoveredTasksNote, worker-name allocation, and brief rendering
    - cmd/codex_build_test.go — grouped manifest, brief, selected-task, and deterministic-name integration assertions modified by this task
    - cmd/merged_dispatch_task_credit_test.go — current whole-success covered-task chain; do not weaken its semantics
    - cmd/build_print_brief.go — task-content allowance and task-section renderer
  </read_first>
  <behavior>
    - `TestCalVaultSixBatchesBecomeOneInRepoJob` observes six dispatches before production wiring, then one Builder with all six task IDs after it.
    - The grouped brief contains each covered task's own goal, constraints, criteria, evidence requirements, and declared paths once.
    - Independent work still fans out, cross-caste work stays separate automatically, and selected-task builds never reintroduce completed/unselected work.
    - Grouped worker names seed from phase, owner caste, and ordered covered IDs; single-task names keep their pre-phase-195 seed/output.
    - User-facing text says `covered tasks`, not the now-false `dependent tasks`.
  </behavior>
  <action>
Start with the literal CalVault integration regression and capture the six-worker
RED result. Use the planner output to compose one numbered dispatch instruction,
the union of declared paths, and ordered CoveredTaskIDs. Keep `findDispatchTasks`
as the single brief source so every task's constraints, criteria, and evidence
travel with the job. Generalize merged-task wording to `covered tasks`. Derive
grouped deterministic names from phase ID, normalized owner caste, and ordered
covered IDs, but branch to the unchanged legacy seed for a one-task job. Extend
the real manifest tests to assert the final dispatch list and brief content,
not only a job decision record. Ensure the same plan is returned when state says
parallel_mode `worktree`; plan 195-08 will prove execution/merge-back.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestCalVaultSixBatchesBecomeOneInRepoJob|TestGroupedJobBriefCarriesEveryTaskContract|TestIndependentTasksStillFanOut|TestGroupedWorkerNameUsesOrderedCoveredIDs|TestSingleTaskWorkerNameIsStable|TestSelectedTaskGroupingStaysInScope' -count=1` passes
    - The CalVault test asserts exactly one Builder and six ordered CoveredTaskIDs
    - The grouped brief assertion names all six task criteria/evidence rows and fails if any one is removed
    - An independent three-task fixture still returns three workers
    - A source/output assertion finds no owner-facing phrase `dependent tasks` in grouped-job summaries
    - Existing `TestMergedDispatchCreditsEveryCoveredTask` still passes before receipt changes land
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(CalVaultSixBatchesBecomeOneInRepoJob|GroupedJobBrief|IndependentTasksStillFanOut|GroupedWorkerName|SingleTaskWorkerName|SelectedTaskGrouping|MergedDispatchCreditsEveryCoveredTask)' -count=1</automated>
  </verify>
  <done>The canonical planner changes the final dispatches and briefs: CalVault is one in-repo worker, all task contracts travel, controls stay parallel, and compatibility identities remain stable.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(BuildJob|GroupingOccursBefore|CalVault|GroupedJob|IndependentTasks|MergedDispatchCredits)' -count=1`
- Inspect one generated in-repo and one worktree-mode manifest: both have the same jobs/job waves before execution adapters run.
</verification>

<success_criteria>
- Grouping is reachable on every build path and happens before waves/worktrees.
- Queen proposals are validated and recorded by Go.
- The real CalVault in-repo regression fails at six workers before the change and passes at one after it.
</success_criteria>

<output>
Create `.planning/phases/195-coherent-jobs/195-03-SUMMARY.md` when done.
</output>
