---
phase: 195-coherent-jobs
plan: 09
type: execute
wave: 7
depends_on: ["195-05", "195-08"]
files_modified:
  - .aether/commands/build.yaml
  - cmd/command_guide.go
  - cmd/command_guide_test.go
  - cmd/parity_test.go
  - .claude/commands/ant/build.md
  - .claude/commands/ant-build.md
  - .opencode/commands/ant/build.md
  - .aether/skills/colony/aether-colony-build-cycle/SKILL.md
  - cmd/lifecycle_wrapper_contract_test.go
  - cmd/cli_flag_audit_test.go
autonomous: true
requirements: [JOBS-01, JOBS-02, JOBS-03, JOBS-04]

must_haves:
  truths:
    - "Wrappers may propose jobs and submit task receipts, but every accepted group, completion credit, retry, worktree reconciliation, and check-in decision remains Go-owned."
    - "The YAML command source, Codex command guide, Codex build-cycle skill, all three wrappers, and CLI parity surfaces change atomically and are verified against the generated completion schema delivered by Plan 195-02."
    - "All three build-wrapper copies are byte-identical and render runtime decisions rather than constructing manifest jobs themselves."
    - "No intermediate commit can expose an updated canonical contract while leaving any shipped wrapper or Codex surface stale."
  artifacts:
    - path: ".aether/commands/build.yaml"
      provides: "Canonical lifecycle contract for job proposals, receipts, recovery, and check-in fields"
    - path: ".claude/commands/ant/build.md"
      provides: "Primary wrapper behavior for proposing/rendering/finalizing coherent jobs"
    - path: "cmd/command_guide.go"
      provides: "Codex orchestration guidance aligned with the Go runtime"
  key_links:
    - from: ".aether/commands/build.yaml"
      to: "cmd/command_guide.go"
      via: "parity tests over the canonical build contract"
      pattern: "job_proposal"
    - from: ".claude/commands/ant/build.md"
      to: "aether build/build-finalize"
      via: "runtime manifest decisions and task_receipts completion packet"
      pattern: "task_receipts"
    - from: ".aether/schemas/completion-packet.schema.json"
      to: ".aether/commands/build.yaml"
      via: "documented packet fields match generated contract"
      pattern: "task_receipts"
---

<objective>
Synchronize every parity-critical orchestration surface in one atomic change.

Purpose: AGENTS.md requires intelligent build behavior to keep the YAML source,
Codex guide, Codex build-cycle skill, and Claude/OpenCode wrappers aligned in
the same change. This prevents a valid runtime contract from shipping with any
platform still proposing, finalizing, or explaining coherent jobs incorrectly.
Output: synchronized canonical YAML, guide, skill, wrapper triplet, and parity
tests. Owner documentation, todo closure, and phase-wide gates are isolated in
plan 195-10.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-coherent-jobs/195-CONTEXT.md
@.planning/phases/195-coherent-jobs/195-RESEARCH.md
@.planning/phases/195-coherent-jobs/195-PATTERNS.md
@.planning/phases/195-coherent-jobs/195-05-SUMMARY.md
@.planning/phases/195-coherent-jobs/195-06-SUMMARY.md
@.planning/phases/195-coherent-jobs/195-07-SUMMARY.md
@.planning/phases/195-coherent-jobs/195-08-SUMMARY.md
@.aether/schemas/completion-packet.schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synchronize YAML, guide, skill, and wrapper triplet atomically</name>
  <files>.aether/commands/build.yaml, cmd/command_guide.go, cmd/command_guide_test.go, cmd/parity_test.go, .aether/skills/colony/aether-colony-build-cycle/SKILL.md, .claude/commands/ant/build.md, .claude/commands/ant-build.md, .opencode/commands/ant/build.md, cmd/lifecycle_wrapper_contract_test.go, cmd/cli_flag_audit_test.go</files>
  <read_first>
    - AGENTS.md — Codex orchestration layer and the same-change parity rule; this task is one commit because these surfaces define one intelligent build behavior
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — Go-authority boundary and D-04 through D-14 user-facing facts
    - .planning/phases/195-coherent-jobs/195-RESEARCH.md — Project Constraints, resolved receipt contract, deprecated claims, and orchestration parity guidance
    - .planning/phases/195-coherent-jobs/195-PATTERNS.md — wrapper/skill/guide parity assignment and authority pattern
    - .planning/phases/195-coherent-jobs/195-05-SUMMARY.md — actual check-in flags/reasons/summary and pending-decision behavior
    - .planning/phases/195-coherent-jobs/195-06-SUMMARY.md and .planning/phases/195-coherent-jobs/195-07-SUMMARY.md — actual task receipt and recovery response fields
    - .planning/phases/195-coherent-jobs/195-08-SUMMARY.md — actual worktree admission, sync, finalization, and recovery behavior
    - .aether/commands/build.yaml — canonical manifest/finalizer/check-in contract and drift-guard declaration
    - cmd/command_guide.go and cmd/command_guide_test.go — build PreSteps and contract assertions
    - .aether/skills/colony/aether-colony-build-cycle/SKILL.md — Codex plan/build/finalize orchestration; read completely before editing
    - .claude/commands/ant/build.md — canonical wrapper manifest re-fetch, team check-in, spawn, completion-packet, and finalize stages
    - .claude/commands/ant-build.md — secondary Claude wrapper copy modified by this task; compare it to the canonical wrapper before synchronizing
    - .opencode/commands/ant/build.md — OpenCode wrapper copy modified by this task; compare it to the canonical wrapper before synchronizing
    - cmd/lifecycle_wrapper_contract_test.go, cmd/cli_flag_audit_test.go, and cmd/parity_test.go — byte parity, referenced flags, and cross-surface contract tests
    - .aether/schemas/completion-packet.schema.json — generated task_receipts shape from plan 195-02; document it, never edit it here
  </read_first>
  <action>
Extend the parity/contract tests first so any stale YAML, guide, skill, or
wrapper fails by the missing contract item. In this same task and commit, update
the build YAML, Codex command guide, Codex build-cycle skill, and canonical
`.claude/commands/ant/build.md`; then copy that canonical wrapper body so
`.claude/commands/ant-build.md` and `.opencode/commands/ant/build.md` are
byte-identical.

This task deliberately accepts the ten-file scope warning because AGENTS.md
defines these YAML, guide, skill, wrapper, flag-audit, and parity-test surfaces
as one atomic intelligent-build contract. Splitting any of them into a separate
plan or commit would permit a shipped platform surface to disagree with the
canonical runtime contract, so every listed file is essential to the parity
boundary rather than separable test or documentation work.

Every surface must name repeatable `--job-proposal`, runtime `job_decisions`
(accepted/refused/repaired), dispatch job name/reason/source, `task_receipts`,
runtime-owned `completed_task_ids`, parent-linked recovery job, `--checkin`,
`checkin_requested`, `checkin_reason`, and compact summary. State plainly that
wrappers propose/render/spawn/submit while Go validates grouping, dependency
order, completion credit, retry, worktree admission/sync/finalization, and
check-in policy. The wrapper may re-fetch through the Phase 194 proposal loop,
but it renders runtime decisions and never mutates dispatches. When automatic
one-worker policy makes `checkin_requested` false, render the runtime compact
summary and continue; when true, preserve the Phase 194 waiver flow. Completion
instructions keep `covered_task_ids` as assignment scope, submit per-task
receipts for proven partial work, and use runtime recovery fields. Per D-08/D-09,
no surface may imply provisional receipt admission is completion credit. Do not
teach any platform to edit a manifest or colony state, and do not change
CLAUDE.md or todo files in this parity commit.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestCommandGuideBuildCoherentJobsContract|TestBuildCommandYAMLCoherentJobsParity|TestLifecycleWrapper.*CoherentJob|TestCLIFlagAudit|TestPlatformParity' -count=1` passes
    - YAML, guide, skill, and each wrapper state that Go owns accepted groups, credit, retry, worktree reconciliation, and check-in policy
    - Every surface names --job-proposal, job_decisions, task_receipts, completed_task_ids, --checkin, checkin_reason, and the admission-before-credit rule
    - No surface instructs an agent to write CoveredTaskIDs/CompletedTaskIDs directly into a manifest or colony state
    - No wrapper says checkin_requested=false only means --no-checkin; each consumes runtime checkin_reason/compact summary
    - `cmp -s .claude/commands/ant/build.md .claude/commands/ant-build.md` and `cmp -s .claude/commands/ant/build.md .opencode/commands/ant/build.md` both succeed
    - `go run ./cmd/aether contract-schema --check` remains green and this task does not modify the generated schema
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(CommandGuideBuildCoherentJobsContract|BuildCommandYAMLCoherentJobsParity|LifecycleWrapper.*CoherentJob|CLIFlagAudit|PlatformParity)' -count=1 &amp;&amp; cmp -s .claude/commands/ant/build.md .claude/commands/ant-build.md &amp;&amp; cmp -s .claude/commands/ant/build.md .opencode/commands/ant/build.md &amp;&amp; go run ./cmd/aether contract-schema --check</automated>
  </verify>
  <done>All parity-critical build surfaces change together and expose one runtime-owned coherent-job, receipt, recovery, worktree, and check-in contract.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(CommandGuideBuildCoherentJobsContract|BuildCommandYAMLCoherentJobsParity|LifecycleWrapper.*CoherentJob|CLIFlagAudit|PlatformParity)' -count=1`
- All three build wrapper files compare byte-identical.
- `go run ./cmd/aether contract-schema --check`
</verification>

<success_criteria>
- Canonical YAML, guide, skill, and all wrappers are aligned in one task/commit.
- Wrappers render Go decisions and submit evidence without becoming manifest or credit authorities.
- No deferred Phase 196–198 or spec-builder work entered the parity surfaces.
</success_criteria>

<output>
Create `.planning/phases/195-coherent-jobs/195-09-SUMMARY.md` when done.
</output>
