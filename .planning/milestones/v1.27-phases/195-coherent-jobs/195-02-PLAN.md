---
phase: 195-coherent-jobs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/codex/task_receipt.go
  - pkg/codex/worker.go
  - pkg/codex/task_receipt_contract_test.go
  - cmd/codex_build_finalize.go
  - cmd/contract_schema_test.go
  - .aether/schemas/completion-packet.schema.json
autonomous: true
requirements: [JOBS-02]

must_haves:
  truths:
    - "A worker can report optional per-task completion receipts without changing the meaning of covered_task_ids or breaking legacy whole-job success."
    - "Native and external completion JSON use the same task-receipt vocabulary."
    - "The checked-in completion schema is generated from Go types and rejects malformed task receipts."
  artifacts:
    - path: "pkg/codex/task_receipt.go"
      provides: "Shared additive TaskReceipt wire contract"
    - path: ".aether/schemas/completion-packet.schema.json"
      provides: "Generated external completion-packet schema with task_receipts"
  key_links:
    - from: "pkg/codex/worker.go"
      to: "pkg/codex/task_receipt.go"
      via: "WorkerResult and trailing workerClaims task_receipts"
      pattern: "TaskReceipts"
    - from: "cmd/codex_build_finalize.go"
      to: ".aether/schemas/completion-packet.schema.json"
      via: "reflected codexExternalBuildWorkerResult contract"
      pattern: "task_receipts"
---

<objective>
Add the backward-compatible receipt wire contract before either completion lane
uses it for credit.

Purpose: D-08/D-09 require trustworthy task-level proof, but
`covered_task_ids` must remain assignment scope. A distinct additive contract
lets later plans validate partial outcomes through one vocabulary.
Output: shared Go receipt types, native/external JSON fields, parser/schema
tests, and regenerated completion schema.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/195-coherent-jobs/195-CONTEXT.md
@.planning/phases/195-coherent-jobs/195-RESEARCH.md
@.planning/phases/195-coherent-jobs/195-PATTERNS.md
@pkg/codex/worker.go
@pkg/codex/handoff.go
@cmd/codex_build_finalize.go
@cmd/contract_schema.go
</context>

<interfaces>
Create `codex.TaskReceipt` with JSON fields `task_id`, `status`, `summary`,
`files_created`, `files_modified`, `tests_written`, and `handoff`. Successful
receipt statuses are `completed` and `completed_no_change`; semantic admission
against a manifest belongs to plan 195-04, not this wire-contract plan.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add one task-receipt type to native worker output</name>
  <files>pkg/codex/task_receipt.go, pkg/codex/worker.go, pkg/codex/task_receipt_contract_test.go</files>
  <read_first>
    - pkg/codex/task_receipt.go and pkg/codex/task_receipt_contract_test.go — new files; keep the contract type separate and follow existing pkg/codex test style
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-08 and D-09; note that receipt proof is task-specific and covered_task_ids remains assignment scope
    - .planning/phases/195-coherent-jobs/195-RESEARCH.md — Pattern 4 and the completion-propagation warning under "What Might Have Been Missed"
    - .planning/phases/195-coherent-jobs/195-PATTERNS.md — additive WorkerResult model and handoff validation analog
    - pkg/codex/worker.go — WorkerResult, workerClaims, ParseWorkerOutput, workerClaimsSchema, normalization, and FakeInvoker paths
    - pkg/codex/handoff.go — WorkerHandoff and ValidateWorkerHandoff; reuse this evidence vocabulary
  </read_first>
  <behavior>
    - Worker output with two task_receipts parses into two `TaskReceipt` values without losing task-specific files or handoff evidence.
    - Worker output without task_receipts parses exactly as before.
    - The generated worker output schema permits only the named receipt fields and retains existing top-level required fields.
    - A task receipt does not contain or redefine manifest requirements/criteria and does not carry authoritative artifact hashes.
  </behavior>
  <action>
Write parsing/schema regressions first and record RED. Add `TaskReceipt` in its
own `pkg/codex` file and optional `TaskReceipts []TaskReceipt
\`json:"task_receipts,omitempty"\`` fields to `WorkerResult` and
`workerClaims`. Thread the field through ParseWorkerOutput normalization,
FakeInvoker, RealInvoker result construction, and `workerClaimsSchema` without
changing callers that omit it. Keep task-specific evidence in the existing
`WorkerHandoff`; do not add requirements, criteria, worker-supplied hashes, or
partial-credit decisions to this transport type.
  </action>
  <acceptance_criteria>
    - `go test ./pkg/codex -run 'TestTaskReceiptContract|TestWorkerClaimsTaskReceipts|TestWorkerClaimsWithoutTaskReceiptsIsBackwardCompatible' -count=1` passes
    - Round-trip JSON preserves task ID, status, summary, all three path lists, verification status, commands, and evidence
    - Legacy worker JSON with no `task_receipts` remains accepted
    - The worker JSON schema has `additionalProperties: false` for each receipt and no receipt field named `requirements`, `criteria`, or `hash`
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/codex -run 'Test(TaskReceipt|WorkerClaims.*TaskReceipt)' -count=1</automated>
  </verify>
  <done>Native worker results carry an optional, task-specific receipt contract while legacy results remain byte-contract compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project task receipts into the external packet and generated schema</name>
  <files>cmd/codex_build_finalize.go, cmd/contract_schema_test.go, .aether/schemas/completion-packet.schema.json</files>
  <read_first>
    - .planning/phases/195-coherent-jobs/195-CONTEXT.md — D-09 contract boundary and the rule that Go remains authoritative
    - .planning/phases/195-coherent-jobs/195-RESEARCH.md — completion schema standard stack and Pattern 4's direct/external parity requirement
    - .planning/phases/195-coherent-jobs/195-PATTERNS.md — completion-schema generator and drift workflow
    - cmd/codex_build_finalize.go — codexExternalBuildWorkerResult and codexExternalBuildCompletion only; do not implement semantic receipt credit in this task
    - cmd/contract_schema.go — generated schema and in-memory fail-closed validator
    - cmd/contract_schema_test.go — existing drift and strictness tests
    - .aether/schemas/completion-packet.schema.json — generated artifact; never hand-edit it
  </read_first>
  <behavior>
    - External completion results accept the same `[]codex.TaskReceipt` shape as native results.
    - The generated checked-in schema includes optional task_receipts and rejects unknown receipt fields or wrong field types.
    - A completion packet with no receipts continues to validate.
  </behavior>
  <action>
Add the failing schema/struct tests first. Add `TaskReceipts
[]codex.TaskReceipt \`json:"task_receipts,omitempty"\`` to
`codexExternalBuildWorkerResult`; keep `CoveredTaskIDs` unchanged. Regenerate
the checked-in schema exclusively with `go run ./cmd/aether contract-schema
--write`, then prove the on-disk artifact equals reflection output and that the
runtime validator rejects a synthetic receipt with an unknown field. Do not
hand-edit the JSON and do not credit tasks yet.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestCompletionPacketSchema.*TaskReceipt|TestContractSchema' -count=1` passes
    - `go run ./cmd/aether contract-schema --check` exits 0
    - A legacy completion packet without `task_receipts` validates
    - A packet with an unknown receipt property is rejected by the runtime-compiled schema
    - `covered_task_ids` and `task_receipts` remain two distinct fields in the generated schema
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(CompletionPacketSchema.*TaskReceipt|ContractSchema)' -count=1 &amp;&amp; go run ./cmd/aether contract-schema --check</automated>
  </verify>
  <done>Native and external lanes share one additive receipt vocabulary, and the generated schema locks it without weakening legacy packets.</done>
</task>

</tasks>

<verification>
- `go test ./pkg/codex -run 'Test(TaskReceipt|WorkerClaims.*TaskReceipt)' -count=1`
- `go test ./cmd -run 'Test(CompletionPacketSchema.*TaskReceipt|ContractSchema)' -count=1`
- `go run ./cmd/aether contract-schema --check`
</verification>

<success_criteria>
- One Go receipt type is used by both completion lanes.
- Receipt omission remains backward-compatible.
- Generated schema drift and unknown fields fail executable tests.
</success_criteria>

<output>
Create `.planning/phases/195-coherent-jobs/195-02-SUMMARY.md` when done.
</output>
