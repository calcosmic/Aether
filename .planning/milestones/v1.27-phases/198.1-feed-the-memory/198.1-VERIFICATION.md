---
phase: 198.1-feed-the-memory
verified: 2026-08-30T13:00:00Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 198.1: Feed the Memory Verification Report

**Phase Goal:** Reconnect the learning loop's starved sources — worker results become observations, failures become midden entries, verified learnings become instincts and Queen-file wisdom, phase completions and decisions leave feedback notes, and strong instincts reach the hive without waiting for a seal — automatically, on the path the owner actually uses.
**Verified:** 2026-08-30T13:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | After a build and a check on a fixture project whose helpers report real findings, the observation log contains sentences those helpers wrote, with no hand-typed memory command | ✓ VERIFIED | `TestBuildWorkerLessonsBecomeObservations`, `TestCheckWorkerLessonsBecomeObservationsOnBothLanes` (wrapper + fast lane), and the phase's own end-to-end proof `TestOneRunFeedsEveryStore` (one real build + one real check, no hand-seeding) — all run and passed live (see Behavioral Spot-Checks) |
| 2 | A failed worker, failed build, or failed verification on build, check, quick, and swarm writes a failure record, and the next build brief carries that failure's own wording | ✓ VERIFIED | `TestFailedBuildWorkerReachesTheNextBriefOnTheDelegateLane` (build), `TestFailedCheckWritesOneFailureRecordOnBothLanes` (check, both lanes), `TestQuickFailureReachesTheFailureLog` (quick), `TestSwarmWorkerFailureReachesTheFailureLogOnBothLanes` + `TestNativeSwarmLaneRecordsSelfReportedWorkerFailure` (swarm, both lanes, including the CR-01 self-reported-status gap) — all passed live |
| 3 | A check that verifies real learnings creates instincts and promotes them into the Queen file through existing consolidation; the promoted list is non-empty on the fixture, not hand-seeded | ✓ VERIFIED | `TestWorkerLessonBecomesQueenFileWisdom` (real capture → instinct → delivery → application → promotion, no hand-seeding) and its negative twin `TestQueenPromotionNeverHappensWithoutRecordedUse` — both passed live. FEED-03 code and tests are satisfied; REQUIREMENTS.md checkbox is unticked — see Requirements Coverage note below |
| 4 | A completed phase and an answered decision each leave a feedback note; failures past threshold leave an automatic "don't do this" note; the CLAUDE.md sentences claiming these are true again | ✓ VERIFIED | `TestFinishedPhaseLeavesANoteNamingWhatItProduced`, `TestAnsweredQuestionLeavesANoteCarryingTheAnswer`, `TestThreeFailuresOfOneKindProduceOneRedirect` / `TestTwoFailuresProduceNoRedirect` / `TestFourthFailureReinforcesTheSameRedirect`, `TestExpiringAValuableNoteKeepsItInLongTermMemory` — all passed live; CLAUDE.md's Midden System / Wisdom Pipeline / Hive Brain / Core Insight sections cite these test names and `TestEveryLearningClaimInCLAUDEMDNamesALiveTest` (AST-based, not grep) confirms every cited name is a real, compiling test |
| 5 | An instinct at ≥0.8 confidence reaches the hive at end of check, not only at seal, under the existing `AETHER_HIVE_POLICY` switch | ✓ VERIFIED | `TestStrongInstinctReachesTheSharedStoreAtCheck`, `TestHivePromotionAtCheckHonoursThePolicySwitch` (unset/empty/promote/read/off/nonsense table), `TestHiveFailureNeverBlocksThePhase` — all passed live |
| 6 | The memory drill-down shows wisdom entries and pending promotions again; status shows the top instincts; the bare-number JSON is gone as the default | ✓ VERIFIED | `TestMemoryDetailsCarriesTheActualSentences`, `TestMemoryDetailsRenderShowsTheSentencesNotTheCounts`, `TestMemoryDetailsJSONFlagStillReturnsTheEnvelope`, `TestMemoryDetailsIsNoLongerAnAliasOfMemoryMetrics`, `TestQueenMarkdownUpdatedAtFallsBackToModTime`, `TestStatusListsTheStrongestInstinctFirst`, `TestStatusInstinctHeadingMatchesTheRanking` — all passed live; also confirmed by direct behavioral run against a built binary (see Behavioral Spot-Checks) |

**Score:** 6/6 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `cmd/memory_feed.go` | Shared build-side feed boundary (observations, midden) | ✓ VERIFIED | Exists, substantive, wired into both `codex_build.go` and `codex_build_finalize.go` result loops; `TestEveryBuildLaneFeedsMemoryThroughOneBoundary` asserts single-boundary property |
| `cmd/memory_feed_continue.go` | Check/quick/swarm-side feed | ✓ VERIFIED | Exists; wired into `codex_continue.go`, `codex_continue_finalize.go`, `deterministic_floor.go`, `swarm_cmd.go`; `TestOnlyOneContinueMemoryEntryPoint` proves single entry point |
| `cmd/instinct_application.go` | Delivery + application recording | ✓ VERIFIED | Exists; wired into `recordDispatchWorkerOutcome` (both build lanes) and `runPhaseEndConsolidation` |
| `cmd/phase_end_signals.go` | Phase-completion / decision / threshold pheromones, eternal promotion | ✓ VERIFIED | Exists; wired into `runPhaseEndConsolidation`, `recordDecisionAnswer`, `expireSignalsByType` |
| `cmd/phase_end_hive.go` | Check-time hive promotion | ✓ VERIFIED | Exists; wired into both continue lanes; same gate/writer as seal |
| `cmd/memory_details_render.go` | Rendered drill-down | ✓ VERIFIED | Exists; wired into `memory_details.go`'s `outputWorkflow` call; confirmed rendering real content via live binary run |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `codex_build.go` / `codex_build_finalize.go` result loops | `midden.json` / `learning-observations.json` | `recordDispatchWorkerOutcome` | ✓ WIRED | `TestEveryBuildLaneFeedsMemoryThroughOneBoundary`, `TestNativeBuildLaneFeedsTheSameMemory` |
| `runDeterministicFloor` | `midden.json` | `recordFailedChecksToMidden` (sanitized, CR-02 fix) | ✓ WIRED | `TestFailedCheckWritesOneFailureRecordOnBothLanes`, `TestCheckAndQuickFailureTextIsSanitisedBeforeMemory` |
| `executeSwarmWave` (native) | `midden.json` | `recordSwarmWorkerFailureToMidden` (CR-01 fix: final-status check) | ✓ WIRED | `TestNativeSwarmLaneRecordsSelfReportedWorkerFailure` |
| `runPhaseEndConsolidation` | `instincts.json` ApplicationHistory → `QUEEN.md` | `recordInstinctApplicationsForPhase` → `pipeline.RunConsolidation` | ✓ WIRED | `TestWorkerLessonBecomesQueenFileWisdom`, `TestPhaseEndConsolidationReportsWhatReachedTheQueenFile` |
| `runPhaseEndConsolidation` summary | `~/.aether/hive/wisdom.json` | `promotePhaseEndInstinctsToHive` → `promoteToHiveWithReference` | ✓ WIRED | `TestStrongInstinctReachesTheSharedStoreAtCheck` |
| `pheromones.json` expiry | `~/.aether/eternal/memory.json` | `appendEternalMemoryEntry` | ✓ WIRED | `TestExpiringAValuableNoteKeepsItInLongTermMemory`, `TestEternalWriterShapeMatchesTheEternalStoreCommand` |
| `memory-details` command | rendered output | `buildMemoryDetails` → `outputWorkflow` → `renderMemoryDetailsVisual` | ✓ WIRED | Live binary run (Behavioral Spot-Checks) plus `TestMemoryDetailsRenderShowsTheSentencesNotTheCounts` |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| CLAUDE.md test-name guard (every cited test genuinely compiles, AST-parsed not grepped) | `go test ./cmd/ -run TestEveryLearningClaimInCLAUDEMDNamesALiveTest -count=1 -v` | PASS | ✓ PASS |
| Plan 01 build-feed tests (8 named tests) | `go test ./cmd/ -run '<8 names>' -count=1 -v` | all PASS | ✓ PASS |
| Plan 02 check/quick/swarm-feed tests (9 named tests incl. CR-01/CR-02 fixes) | `go test ./cmd/ -run '<9 names>' -count=1 -v` | all PASS | ✓ PASS |
| Plan 03 instinct-application tests (7 named tests, end-to-end and negative) | `go test ./cmd/ -run '<7 names>' -count=1 -v` | all PASS | ✓ PASS |
| Plan 04 pheromone/redirect/eternal tests (13 named tests) | `go test ./cmd/ -run '<13 names>' -count=1 -v` | all PASS | ✓ PASS |
| Plan 05 hive-at-check tests (2 core names, in addition to guard above) | `go test ./cmd/ -run 'TestStrongInstinctReachesTheSharedStoreAtCheck|TestHivePromotionAtCheckHonoursThePolicySwitch' -count=1 -v` | all PASS | ✓ PASS |
| Plan 06 memory-details/status tests (8 named tests) | `go test ./cmd/ -run '<8 names>' -count=1 -v` | all PASS | ✓ PASS |
| Phase's own end-to-end proof (one build, one check, four stores) | `go test ./cmd/ -run TestOneRunFeedsEveryStore -count=1 -v` | PASS | ✓ PASS |
| `go vet ./cmd/...` | full vet | clean, no output | ✓ PASS |
| `go build ./cmd/aether` | full build | succeeds | ✓ PASS |
| Live binary: `memory-details` rendered view against a real fixture QUEEN.md | `AETHER_FORCE_VISUAL=1 aether memory-details` in a scratch fixture with a two-category QUEEN.md | Rendered "What the colony has learned" with the real Patterns/Philosophies sentences, "Lessons waiting to be promoted", "Recent failures", and "Wisdom file last changed: 2026-08-30T12:38:09Z" (no longer empty) | ✓ PASS |
| Live binary: `memory-details --json` / non-TTY default | same fixture | JSON envelope returned (non-TTY defaults to JSON per pre-existing `shouldRenderVisualOutput`, unrelated to this phase) — `--json` and `AETHER_OUTPUT_MODE=json` both force it explicitly | ✓ PASS |
| `status.go` "Strongest Instincts" heading backed by `loadStrongestRuntimeInstincts` | code read | Heading text matches ranking function name/semantics, not "Recent Instincts" | ✓ PASS |

### Code Review Findings (198.1-REVIEW.md) — Fix Verification

| Finding | Fix Commit | Verified |
|---------|-----------|----------|
| CR-01: native swarm lane drops self-reported worker failures | `cefca7ac` | ✓ Fix present in `cmd/swarm_cmd.go` (final-status check mirrors wrapper lane exactly); `TestNativeSwarmLaneRecordsSelfReportedWorkerFailure` passes live, covering failed/timeout/completed cases |
| CR-02: `recordFailedChecksToMidden` / `recordQuickFailureToMidden` skip sanitization | `cc48873a` | ✓ Both functions now call `colony.SanitizeSignalContent` before storing, with a safe fallback message on rejection; `TestCheckAndQuickFailureTextIsSanitisedBeforeMemory` drives a real injection phrase through the real check/quick paths and confirms it never reaches midden.json unsanitised |
| WR-01: `--no-learn` help text overclaimed scope | `26c04d24`, `3e0a2741` | ✓ Flag help text, `.aether/docs/host-command-reference.md`, and `cmd/contracts/continue.md` all corrected to state observations/failures are still written; `TestCLIFlagAudit` passes with the reworded text |
| WR-02: `appendMiddenEntryOnce` dedup contract undocumented | `26c04d24` | ✓ Doc comment added explaining the (category, message) dedup key and caller responsibility |
| IN-01: golden_continue.txt duration line unrelated to phase | not addressed | ℹ️ Info-level only; not a functional gap, left as-is |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| FEED-01 | 01, 02 | Worker results become observations automatically | ✓ SATISFIED | See Truth 1 |
| FEED-02 | 01, 02 | Failures write midden entries automatically, on all four surfaces | ✓ SATISFIED | See Truth 2 |
| FEED-03 | 03 | Instincts created and promoted through existing consolidation | ✓ SATISFIED (code/tests) — REQUIREMENTS.md checkbox unticked | The SUMMARY for plan 03 claims complete and cites 9 named tests; I independently re-ran all 9 plus the two decisive end-to-end tests (`TestWorkerLessonBecomesQueenFileWisdom`, `TestQueenPromotionNeverHappensWithoutRecordedUse`) live and they pass, driving a real worker sentence through capture → instinct → delivery → application → promotion with no hand-seeding, and a negative test proving promotion never happens without a recorded use. This is a documentation/checkbox gap in REQUIREMENTS.md, not a functional gap — the checkbox should be ticked as a housekeeping follow-up but does not block phase completion |
| FEED-04 | 04, 05 | Phase/decision/threshold pheromones, eternal promotion, CLAUDE.md claims test-backed | ✓ SATISFIED | See Truth 4; `TestEveryLearningClaimInCLAUDEMDNamesALiveTest` passed live |
| FEED-05 | 05 | Hive promotion at check, honouring policy switch | ✓ SATISFIED | See Truth 5 |
| FEED-06 | 06 | Memory drill-down and status rendering restored | ✓ SATISFIED | See Truth 6; confirmed with live binary run |

No orphaned requirements — all six FEED-01..06 IDs are claimed by exactly one plan each and independently verified against the codebase.

### Anti-Patterns Found

None. Scanned all files this phase created or modified (`cmd/memory_feed.go`, `cmd/memory_feed_continue.go`, `cmd/instinct_application.go`, `cmd/phase_end_signals.go`, `cmd/phase_end_hive.go`, `cmd/memory_details_render.go`, `cmd/memory_details.go`, `cmd/status.go`, `cmd/consolidation_lifecycle.go`, `cmd/swarm_cmd.go`, `cmd/pheromone_write.go`, `cmd/handoff_decisions_cmd.go`, and the build/continue integration points) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/"not yet implemented" — zero matches.

### Human Verification Required

None. Every must-have truth resolved to a behavior-dependent test that was independently re-run and passed live against real code paths (real dispatch/continue flows through the runtime's own parsers, not hand-typed fixture structs, per this repo's Definition of Done), plus one direct behavioral run of the built binary confirming the rendered drill-down view.

### Gaps Summary

No functional gaps. One paperwork item: REQUIREMENTS.md's FEED-03 checkbox is unticked even though plan 03's SUMMARY records it complete and the underlying code/tests fully satisfy it (verified independently above, not merely trusted from the SUMMARY). This should be ticked in REQUIREMENTS.md as routine housekeeping; it does not block phase completion or the next phase.

Both critical code-review findings (CR-01, CR-02) and both warnings (WR-01, WR-02) from 198.1-REVIEW.md were fixed in the four post-review commits, and each fix carries its own new passing test that fails if the fix regresses.

---

_Verified: 2026-08-30T13:00:00Z_
_Verifier: Claude (gsd-verifier)_
