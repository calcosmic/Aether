---
phase: 198.1-feed-the-memory
plan: 05
subsystem: memory
tags: [hive, claude-md-guard, learning-authority-doc, go]

# Dependency graph
requires:
  - phase: 198.1-02
    provides: "cmd/memory_feed_continue.go: captureContinueMemory, the one entry point both continue lanes call -- confirms both call sites' shape for this plan's own wiring"
  - phase: 198.1-03
    provides: "cmd/instinct_application.go and cmd/consolidation_lifecycle.go: runPhaseEndConsolidation, the exact point after which this plan's hive-promotion call sits"
  - phase: 198.1-04
    provides: "cmd/phase_end_signals.go: emitPhaseCompletionFeedback / emitMiddenThresholdRedirect, the signal-store link this plan's TestOneRunFeedsEveryStore asserts on"
provides:
  - "cmd/phase_end_hive.go: promotePhaseEndInstinctsToHive, the phase-end hive-promotion call both continue lanes now make"
  - "A strong instinct (confidence >= 0.8) reaches the shared cross-project store at the end of every check, not only at project close, under the same AETHER_HIVE_POLICY switch seal already honours"
  - "Every learning-loop claim CLAUDE.md keeps names a test that exists, enforced by a parser-backed guard (TestEveryLearningClaimInCLAUDEMDNamesALiveTest)"
  - ".aether/docs/learning-system-authority.md Decision 5: the observation, failure, instinct, signal, and shared-store links each have a named live caller and a named test"
affects: [198.2, 198.3, 198.4]

# Actuals (#2632)
actuals:
  tokens: 11555
  tasks: 3
  commits: 4

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "phaseEndHiveRepoName duplicates seal's own origin-remote/cwd-basename resolution rather than extracting a shared helper -- a deliberate, narrow duplication the plan itself called for, since the seal call site sits inline inside a much larger function"
    - "promotePhaseEndInstinctsToHive calls the exact same two functions seal's promotion loop calls (automaticHivePromotionEnabled, promoteToHiveWithReference) rather than re-deriving eligibility or writing a second hive writer"
    - "TestEveryLearningClaimInCLAUDEMDNamesALiveTest scopes its scan to the four top-level CLAUDE.md sections this plan edited (not the whole file), reusing the existing extractMarkdownSection helper from cmd/codex_continue.go rather than a second implementation"
    - "collectLiveGoTestFuncNames walks cmd/ and pkg/ with go/parser, never grep, so a test name inside a comment or string literal cannot satisfy the guard"

key-files:
  created:
    - cmd/phase_end_hive.go
    - cmd/phase_end_hive_test.go
  modified:
    - cmd/codex_continue.go
    - cmd/codex_continue_finalize.go
    - CLAUDE.md
    - .aether/docs/learning-system-authority.md

key-decisions:
  - "Seeded eligible instincts directly into instincts.json in Task 1's tests rather than deriving them through the full observe -> consolidate pipeline Plan 03 already proved. The plan's own action text specifies this ('Seed an instinct at confidence 0.85...'), and this task's concern is what happens to an already-eligible instinct, not how one comes to exist."
  - "Reused the pre-existing extractMarkdownSection(content, heading string) helper in cmd/codex_continue.go instead of writing a second one -- go vet caught the redeclaration immediately, and the existing helper is heading-level aware (handles nested ### subsections and code fences correctly), which a from-scratch version would have had to reinvent."
  - "TestOneRunFeedsEveryStore asserts exactly the four stores the plan's own <behavior> block names (observation, failure, instinct, signal) -- not a fifth hive-promotion assertion -- matching the plan's literal four-bullet list rather than expanding scope to also prove hive promotion in the same test (that property already has five dedicated tests in Task 1)."
  - "Verified both new guard tests (TestEveryLearningClaimInCLAUDEMDNamesALiveTest and TestOneRunFeedsEveryStore) can actually fail before treating them as done: renamed a cited test in cmd/trust_test.go and confirmed the CLAUDE.md guard failed naming it (then restored); disabled emitPhaseCompletionFeedback's body and confirmed TestOneRunFeedsEveryStore failed with the pheromones.json message specifically (then restored, diff-clean both times)."

requirements-completed: [FEED-05, FEED-04]

coverage:
  - id: D1
    description: "Strong lessons (confidence >= 0.8) reach the shared cross-project store at the end of every check, on both continue lanes, gated by AETHER_HIVE_POLICY exactly as seal is, and a hub write failure never blocks the phase"
    requirement: "FEED-05"
    verification:
      - kind: unit
        ref: "cmd/phase_end_hive_test.go#TestStrongInstinctReachesTheSharedStoreAtCheck"
        status: pass
      - kind: unit
        ref: "cmd/phase_end_hive_test.go#TestHivePromotionAtCheckHonoursThePolicySwitch"
        status: pass
      - kind: unit
        ref: "cmd/phase_end_hive_test.go#TestWeakInstinctIsNotPromotedAtCheck"
        status: pass
      - kind: unit
        ref: "cmd/phase_end_hive_test.go#TestRepeatedPhaseEndPromotionDoesNotDuplicate"
        status: pass
      - kind: unit
        ref: "cmd/phase_end_hive_test.go#TestHiveFailureNeverBlocksThePhase"
        status: pass
    human_judgment: false
  - id: D2
    description: "Every learning-loop claim left in CLAUDE.md is true and names a test that exists; a parser-backed guard fails naming any cited test that stops existing"
    requirement: "FEED-04"
    verification:
      - kind: unit
        ref: "cmd/phase_end_hive_test.go#TestEveryLearningClaimInCLAUDEMDNamesALiveTest"
        status: pass
    human_judgment: false
  - id: D3
    description: "One fixture run (one build worker succeeding, one failing, then one check to a durable advance) feeds the observation log, failure log, instinct store and signal store, each with a distinct failure message per store"
    requirement: "FEED-05"
    verification:
      - kind: unit
        ref: "cmd/phase_end_hive_test.go#TestOneRunFeedsEveryStore"
        status: pass
    human_judgment: false

duration: ~95min
completed: 2026-08-30
status: complete
---

# Phase 198.1 Plan 05: Feed the Memory (hive promotion at check, CLAUDE.md truth) Summary

**Strong lessons now reach the shared cross-project store at the end of every check, not only at project close, and every learning-loop sentence CLAUDE.md still makes now names a test that a parser-backed guard proves actually exists.**

## Performance

- **Duration:** ~95 min
- **Completed:** 2026-08-30T11:51:50Z
- **Tasks:** 3
- **Files modified:** 6 (2 created, 4 modified)

## Accomplishments

- `promotePhaseEndInstinctsToHive` (`cmd/phase_end_hive.go`) runs on both continue lanes, immediately after phase-end consolidation, calling the exact same gate (`automaticHivePromotionEnabled()`) and the exact same writer (`promoteToHiveWithReference`) seal's own promotion loop calls -- one switch, one writer, two call sites now instead of one. A hub write failure is logged and never blocks the check.
- CLAUDE.md's learning-loop sentences that described stages nothing invoked -- "Decisions become pheromones (auto-emit during builds)" (never true at the build layer), "Midden affects behavior (threshold auto-REDIRECT)" (no threshold existed), a Midden System writer list citing `build.md` (never a writer), and a Wisdom Pipeline table with a stale "Output" column -- were rewritten to state what the runtime now actually does, each naming the test that fails if it stops being true.
- `TestEveryLearningClaimInCLAUDEMDNamesALiveTest` parses every `Test`-shaped token out of the four CLAUDE.md sections this plan edited (The Core Insight, Midden System, Wisdom Pipeline, Hive Brain) with `go/parser` -- never grep, so a name inside a comment cannot satisfy it -- and fails naming any cited test that does not exist as a real `func TestXxx(t *testing.T)` under `cmd/` or `pkg/`.
- `TestOneRunFeedsEveryStore` drives one real build (one worker succeeding with a do-not-repeat sentence, one worker failing with a blocker sentence) through `runCodexBuildFinalize`, then a real check to a durable advance through `runCodexContinue`, and asserts all four memory stores this phase and Plans 01-04 wired are non-empty -- each assertion naming its own store's failure message.
- `.aether/docs/learning-system-authority.md` gained a new Decision 5 recording the live caller and named test behind each of the five feed links this phase (198.1) wired, and states plainly that a future change removing a caller must delete the corresponding claim.

## Task Commits

Each task was committed atomically, following the same git-hunk-inseparability pattern already recorded for Plans 01-04 in this phase (production code and CLAUDE.md/doc changes commit separately from the tests that prove them, since all three tasks' tests share fixtures in one new file):

1. **Task 1: `promotePhaseEndInstinctsToHive` + both continue-lane call sites** - `6740d9e2` (feat)
2. **Task 2: CLAUDE.md corrections, naming each retained claim's test** - `b449fce1` (docs)
3. **Tasks 1-3: full test coverage (phase-end hive promotion, CLAUDE.md guard, one-run four-store proof)** - `80bfa4ab` (test)
4. **Task 3: `.aether/docs/learning-system-authority.md` Decision 5** - `231fcc06` (docs)

## Files Created/Modified

- `cmd/phase_end_hive.go` - `promotePhaseEndInstinctsToHive`, `attachHivePromotionSummary`, `phaseEndHiveRepoName`
- `cmd/phase_end_hive_test.go` - 8 tests: 5 for Task 1 (phase-end hive promotion), 1 for Task 2 (the CLAUDE.md guard), 1 for Task 3 (the one-run four-store proof), plus shared fixture helpers
- `cmd/codex_continue.go` - default lane calls `promotePhaseEndInstinctsToHive` right after `runPhaseEndConsolidation`, attaches the summary beside `attachConsolidationSummary`
- `cmd/codex_continue_finalize.go` - wrapper lane, same placement
- `CLAUDE.md` - The Core Insight, Midden System (Failure Tracking), Wisdom Pipeline's Pipeline Stages table, and Hive Brain's promotion-hook section all rewritten to name live tests
- `.aether/docs/learning-system-authority.md` - new Decision 5 (every feed link's caller and test), the phase-end promotion recorded beside the existing seal-time entry, a new Consequences row

## Decisions Made

See `key-decisions` in the frontmatter above for the full rationale on: seeding instincts directly rather than deriving them through the full pipeline, reusing the pre-existing `extractMarkdownSection` helper instead of duplicating it, scoping `TestOneRunFeedsEveryStore` to exactly the plan's four named stores, and the two ablation checks that proved both new guard tests can actually fail before being treated as done.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `extractMarkdownSection` redeclared**
- **Found during:** Task 2 (`go vet ./cmd/...` after writing the CLAUDE.md guard test)
- **Issue:** `cmd/codex_continue.go` already declares a general-purpose, heading-level-aware `extractMarkdownSection(content, heading string) string`, used by verification-command scanning. My first draft of `TestEveryLearningClaimInCLAUDEMDNamesALiveTest` wrote a second, simpler one with the identical name and signature.
- **Fix:** Deleted the duplicate and reused the existing helper, which handles nested `###` subsections and fenced code blocks the from-scratch version did not account for.
- **Files modified:** cmd/phase_end_hive_test.go
- **Verification:** `go vet ./cmd/...` clean; `TestEveryLearningClaimInCLAUDEMDNamesALiveTest` passes.
- **Committed in:** `80bfa4ab` (test commit)

**2. [Rule 1 - Bug] Acceptance-criterion grep counted a comment mention of `AETHER_HIVE_POLICY`**
- **Found during:** Task 1, running the plan's own acceptance-criterion command (`grep -c 'AETHER_HIVE_POLICY' cmd/phase_end_hive.go` was required to return 0, and returned 1)
- **Issue:** A doc-comment on `promotePhaseEndInstinctsToHive` named the environment variable literally, to explain which switch it means -- correct in spirit (the code never reads it directly) but it failed the plan's own literal grep check.
- **Fix:** Reworded the comment to point at `cmd/hive_policy.go` instead of naming the env var string.
- **Files modified:** cmd/phase_end_hive.go
- **Verification:** `grep -c 'AETHER_HIVE_POLICY' cmd/phase_end_hive.go` returns 0.
- **Committed in:** `6740d9e2` (feat commit)

**3. [Rule 1 - Bug] The false-positive "black-box changed the source checkout" failures mid-session were self-inflicted, not a real regression**
- **Found during:** the first full `go test ./cmd/ -count=1` run after committing
- **Issue:** `TestCLIExternalAdapterBuildContract` and four sibling black-box CLI tests fail if `git status --short` differs between two snapshots the test itself takes. Running that full-suite test concurrently with my own `git add`/`git commit` operations in the same worktree tripped this guard -- correctly, from the guard's own perspective, since the checkout genuinely changed mid-run. Not a code defect.
- **Fix:** No code fix needed. Re-ran the full suite twice more after all commits landed and the working tree was stable; both reruns passed clean (274s and 267s), confirming the failures were purely a self-inflicted timing collision, not a real regression.
- **Files modified:** none
- **Verification:** `go test ./cmd/ -count=1` (274.136s, exit 0) and `go test ./cmd/ ./pkg/memory/ ./pkg/learn/ -count=1` (267.360s + 0.620s + 1.639s, all `ok`), both run against the fully-committed, stable working tree.
- **Committed in:** n/a (no code change; documented here per the deviation-rules requirement to record what was investigated and how)

---

**Total deviations:** 3 (1 Rule 3 blocking, 1 Rule 1 acceptance-criterion wording fix, 1 Rule 1 investigation of a false-positive test failure with no code change needed).
**Impact on plan:** None on scope. All three were necessary corrections or investigations to make the plan's own stated acceptance criteria and verification commands actually hold; no scope creep.

## Issues Encountered

None beyond the three auto-fixed/investigated items above, all documented as deviations.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `cmd/phase_end_hive.go`'s `promotePhaseEndInstinctsToHive` and `attachHivePromotionSummary` are available to any later plan (198.2-198.4) that wants to reason about or report phase-end hive promotion.
- `attachHivePromotionSummary`'s `result["hive_promotion"]` map exists on both continue lanes' JSON output but is not yet surfaced in any rendered visual/card -- the same "available for a later plan to surface" pattern 198.1-03 and 198.1-04 left for their own new summary fields.
- This is the last plan of Phase 198.1 that touches the learning-loop wiring itself; 198.1-06 (memory-details rendering) is the remaining wave-2 plan, already summarized. The phase's full end-to-end proof (`TestOneRunFeedsEveryStore`) now exists as a durable regression guard for all four plans' wiring together.
- No blockers.

---
*Phase: 198.1-feed-the-memory*
*Completed: 2026-08-30*

## Self-Check: PASSED

- FOUND: cmd/phase_end_hive.go
- FOUND: cmd/phase_end_hive_test.go
- FOUND: .aether/docs/learning-system-authority.md (modified)
- FOUND: CLAUDE.md (modified)
- FOUND commit: 6740d9e2 (feat)
- FOUND commit: b449fce1 (docs)
- FOUND commit: 80bfa4ab (test)
- FOUND commit: 231fcc06 (docs)
- Re-ran all 8 named tests (TestStrongInstinctReachesTheSharedStoreAtCheck,
  TestHivePromotionAtCheckHonoursThePolicySwitch, TestWeakInstinctIsNotPromotedAtCheck,
  TestRepeatedPhaseEndPromotionDoesNotDuplicate, TestHiveFailureNeverBlocksThePhase,
  TestEveryLearningClaimInCLAUDEMDNamesALiveTest, TestOneRunFeedsEveryStore): all pass
- Re-ran plan-level `<verification>`: go build ./cmd/aether (0), go vet ./... (0),
  go test ./cmd/ ./pkg/memory/ ./pkg/learn/ -count=1 (all ok, cmd 267.360s),
  grep -n 'auto-emit during builds' CLAUDE.md (0 matches)
- Full `cmd` package suite (`go test ./cmd/ -count=1`) run twice after all commits
  landed on a stable working tree: 274.136s and 267.360s, both `ok`, exit 0
