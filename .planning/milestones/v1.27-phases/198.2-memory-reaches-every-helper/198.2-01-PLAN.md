---
phase: 198.2-memory-reaches-every-helper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/codex_plan.go
  - cmd/codex_colonize.go
  - cmd/wire_capsule_198_2_test.go
  - cmd/lifecycle_wrapper_contract_test.go
  - .claude/commands/ant/plan.md
  - .claude/commands/ant-plan.md
  - .opencode/commands/ant/plan.md
  - .claude/commands/ant/colonize.md
  - .claude/commands/ant-colonize.md
  - .opencode/commands/ant/colonize.md
  - .aether/commands/plan.yaml
  - .aether/commands/colonize.yaml
autonomous: true
requirements: [WIRE-01]

estimate:
  tokens: 98000
  raw_tokens: 98000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-03: `aether plan` on the lane a Claude/OpenCode session actually runs (the delegate/plan-only lane) returns a manifest whose `context_capsule` carries a sentinel sentence from every one of the seven memory sources named in WIRE-01: the Queen file, the shared cross-project store, a learned habit, the failure log, an owner steering note, an owner answer, and the previous helper's relay note."
    - "D-03: `aether colonize` on the same delegate lane returns a manifest whose `context_capsule` carries the same seven sentinel sentences."
    - "D-03: the capsule the delegate lane carries and the capsule the in-process lane sets on its worker dispatch are byte-identical for the same colony state, because both come from one call to the same assembly function with the same arguments."
    - "WIRE-01 (adjacency): the steering-note section and the previous-helper relay section each appear exactly once in a delegate-lane worker prompt — the manifest capsule is their only home on this lane, and no per-dispatch field repeats either heading."
    - "WIRE-01 (empty): on a colony with no memory of any kind, `context_capsule` is absent from the manifest JSON entirely (omitempty) rather than present as an empty string or a run of empty headings."
    - "WIRE-01 (ordering): two `--plan-only` runs against unchanged colony state produce byte-identical `context_capsule` values, so equal-ranked sections never reorder between runs."
    - "All three hand-maintained copies of each wrapper — the nested Claude one, the flat installed-consumer Claude mirror, and the OpenCode one — instruct the spawner to read the manifest-level capsule once and prepend it verbatim to every worker prompt, in the same words `build.md` already uses."
    - "197 S-04 (triplet parity): the three copies of the plan wrapper are byte-identical to each other after the edit, and so are the three copies of the colonize wrapper — asserted the same three-way way `buildWrapperTripletPaths` already asserts it for `build.md`, so the flat mirror the owner's Claude Code session actually runs cannot drift from the nested source."
  artifacts:
    - cmd/codex_plan.go
    - cmd/codex_colonize.go
    - cmd/wire_capsule_198_2_test.go
    - cmd/lifecycle_wrapper_contract_test.go
    - .claude/commands/ant/plan.md
    - .claude/commands/ant-plan.md
    - .opencode/commands/ant/plan.md
    - .claude/commands/ant/colonize.md
    - .claude/commands/ant-colonize.md
    - .opencode/commands/ant/colonize.md
    - .aether/commands/plan.yaml
    - .aether/commands/colonize.yaml
  key_links:
    - "codexPlanManifest.ContextCapsule <- resolveCodexWorkerContext() (single call site in runCodexPlanPlanOnly)"
    - "codexColonizeManifest.ContextCapsule <- resolveCodexWorkerContext() (single call site in runCodexColonizePlanOnly)"
    - "manifest.context_capsule -> platform wrapper -> spawned worker prompt"
  prohibitions:
    - statement: "The capsule must never be assembled by a wrapper; the wrapper may only pass through what the runtime already assembled."
      status: required
      verification: none
    - statement: "No section may be delivered twice on the new lanes — a per-dispatch steering or relay field must not repeat a heading the manifest capsule already renders."
      status: required
      verification: none
    - statement: "This plan must not add a single new worker dispatch, ceremony step, or owner check-in."
      status: required
      verification: none
    - statement: "A wrapper instruction must never land on only two of a command's three hand-maintained copies — the flat Claude mirror is the file an installed session runs, so a two-copy edit ships the change to nobody."
      status: required
      verification: none
---

<objective>
Put the colony's memory pack on the two command lanes the owner actually runs: `/ant-plan` and `/ant-colonize` in Claude Code and OpenCode.

Today `codexPlanManifest` and `codexColonizeManifest` have no capsule field at all. The `ContextCapsule` set at `codex_plan.go:1478` and `codex_colonize.go:690` lives on the in-process `codex.WorkerDispatch` that `ShouldUseAgentDelegatePath()` routes every Claude/OpenCode session away from — so the planner and all four surveyors receive nothing. `codexBuildManifest` already does this correctly (`codex_build.go:160-169`, `:2392-2395`, `:2420`); this plan copies that exact shape twice.

Purpose: the planner and the surveyors are the two helpers that most need to know what the project already learned, and they are the two that currently get least.
Output: a capsule field on both manifests, all three hand-maintained copies of each wrapper told to prepend it (the flat `.claude/commands/ant-<verb>.md` mirror included — that is the file an installed Claude Code session runs), and one delegate-lane table test that fails when any memory source stops arriving.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198.2-memory-reaches-every-helper/198.2-CONTEXT.md
@.planning/phases/198.2-memory-reaches-every-helper/198.2-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end — one memory sentence reaches a planner spawned from the delegate manifest</name>
  <files>cmd/codex_plan.go, cmd/wire_capsule_198_2_test.go</files>
  <read_first>
    - cmd/codex_build.go lines 150-176 (the `ContextCapsule` field and its doc comment — the exact shape to copy)
    - cmd/codex_build.go lines 2386-2425 (the plan-only-only single call site that populates it)
    - cmd/codex_plan.go lines 216-265 (`codexPlanManifest` struct)
    - cmd/codex_plan.go lines 1086-1165 (`runCodexPlanPlanOnly`, where the manifest literal is built)
    - cmd/codex_plan.go lines 1470-1500 (the in-process lane's `capsule := resolveCodexWorkerContext()` and the D-190-05-A comment forbidding a second steering render)
    - cmd/codex_plan.go lines 400-425 (`ShouldUseAgentDelegatePath()` routing to `runCodexPlanAgentDelegate`)
    - cmd/codex_build_manifest_context_test.go (the existing manifest-capsule assertion, including the hosted-path-stays-empty half)
    - cmd/build_pheromone_190_05_test.go lines 60-120 and 200-270 (`saveGlobalsCmd(t)`, the sentinel table shape, and the "fixture broken" guard)
  </read_first>
  <behavior>
    - A `--plan-only` / delegate-lane plan run on a colony carrying one active owner steering note returns a manifest whose `context_capsule` contains that note's exact text.
    - The same run on a colony with no memory at all returns a manifest with no `context_capsule` key.
    - The hosted (non-plan-only, non-delegate) path leaves `context_capsule` empty, because that path already shares its own capsule with each worker dispatch.
  </behavior>
  <action>
    Add a `ContextCapsule string` field with JSON tag `context_capsule,omitempty` to `codexPlanManifest` in cmd/codex_plan.go. Carry over the doc-comment shape from `codexBuildManifest.ContextCapsule`, restated for planning: it is the colony-prime grounding payload, computed once per plan-only manifest, carried at the top level rather than copied into every dispatch brief, read once by the wrapper and prepended verbatim to each spawned worker prompt, and populated only on the plan-only/delegate manifest because the in-process path at line 1478 already delivers the same value on each `codex.WorkerDispatch`.

    Populate it at exactly one site inside `runCodexPlanPlanOnly`, immediately before the `manifest := codexPlanManifest{...}` literal, mirroring cmd/codex_build.go's guarded assignment: compute `contextCapsule := resolveCodexWorkerContext()` and assign it into the literal. Never compute it inside a per-dispatch loop. Do not add a `PheromoneSection` or a `renderWorkerHandoffSection` call anywhere on this path — the capsule already renders both, and the existing comment at line 1479 records why a second render is forbidden.

    `runCodexPlanAgentDelegate` (cmd/codex_plan.go:910) copies the manifest out of `runCodexPlanPlanOnly`'s result, mutates `DispatchMode`, and writes it back into both `plan_manifest` and `planning_manifest`; verify the capsule survives that copy and is present under both keys.

    Create cmd/wire_capsule_198_2_test.go with `saveGlobalsCmd(t)` as its first statement. Write `TestPlanDelegateManifestCarriesOneSteeringNote` first and watch it fail: seed one active steering note the way the runtime writes one (through the same pheromone write path the runtime uses, not a hand-typed JSON literal), force the delegate lane, run the plan-only path, and assert the seeded sentence appears in `plan_manifest.context_capsule`. Add the two boundary cases from `<behavior>` in the same test file: a memory-free colony leaves the field absent, and a hosted-path manifest leaves it empty.
  </action>
  <verify>
    <automated>go build ./cmd/aether &amp;&amp; go test ./cmd/ -run 'TestPlanDelegateManifestCarriesOneSteeringNote' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'context_capsule,omitempty' cmd/codex_plan.go` returns exactly one line.
    - `grep -c 'resolveCodexWorkerContext()' cmd/codex_plan.go` returns 2 (the pre-existing in-process site at ~:1478 plus the new plan-only site) and neither sits inside a `for` loop.
    - `go test ./cmd/ -run 'TestPlanDelegateManifestCarriesOneSteeringNote' -count=1` exits 0.
    - Commenting out the new `contextCapsule` assignment in `runCodexPlanPlanOnly` makes `TestPlanDelegateManifestCarriesOneSteeringNote` fail (prove the fix by breaking it; restore afterwards).
    - `go vet ./...` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">D-03 — a second, tailored planning capsule would need its own per-source content tests on both lanes and re-opens the two-lane drift this phase exists to close.</reversibility>
  <done>A planner spawned from the Claude/OpenCode plan manifest receives the colony's steering note, proven by a test that fails when the assignment is removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The same capsule on the colonize manifest, and all three copies of each wrapper told to prepend it</name>
  <files>cmd/codex_colonize.go, cmd/wire_capsule_198_2_test.go, .claude/commands/ant/plan.md, .claude/commands/ant-plan.md, .opencode/commands/ant/plan.md, .claude/commands/ant/colonize.md, .claude/commands/ant-colonize.md, .opencode/commands/ant/colonize.md, .aether/commands/plan.yaml, .aether/commands/colonize.yaml</files>
  <read_first>
    - cmd/codex_colonize.go lines 60-90 (`codexColonizeManifest` struct)
    - cmd/codex_colonize.go lines 330-375 (`runCodexColonizePlanOnly`, the delegate/plan-only branch and its result map)
    - cmd/codex_colonize.go lines 376-421 (`buildCodexColonizeManifest`)
    - cmd/codex_colonize.go lines 683-735 (the in-process lane's `capsule := resolveCodexWorkerContext()` and its D-190-05-A comment)
    - .claude/commands/ant/build.md lines 30-40 and 338-368 (the exact sentences that tell the spawner to read `dispatch_manifest.context_capsule` once and prepend it verbatim, and that name it the sole home of steering notes and relay notes)
    - .claude/commands/ant/plan.md lines 100-125 (the dispatch stage where each worker's prompt is assembled)
    - .claude/commands/ant/colonize.md lines 45-65 (the surveyor dispatch stage)
    - .aether/commands/plan.yaml and .aether/commands/colonize.yaml (the source definitions that must move with the wrappers)
    - cmd/lifecycle_wrapper_contract_test.go lines 28-40 (`flatMirrorPath(repoRoot, verb)` — the helper that names the flat installed-consumer copy `.claude/commands/ant-<verb>.md`) and lines 741-800 (`buildWrapperTripletPaths` plus the `triplet_is_byte_identical` subtest — the exact three-way shape to mirror, canonical-nested first, flat mirror second, OpenCode third)
    - cmd/plan_wrapper_ceremony_test.go lines 10-40 (`TestPlanWrapperCeremonyContract`'s `wrapperPaths`, which already reads all three plan-wrapper copies — the required-substring list this task's new instruction must not break)
  </read_first>
  <behavior>
    - A delegate-lane colonize run on a colony carrying one active steering note returns `colonize_manifest.context_capsule` containing that note.
    - All three copies of the plan wrapper stay byte-identical to each other after the edit — the nested Claude one, the flat Claude mirror `.claude/commands/ant-plan.md`, and the OpenCode one; likewise all three colonize copies.
    - The capsule instruction is present in all six wrapper files, not just the nested pair — a run that edits only two copies of a command fails.
  </behavior>
  <action>
    Add the same `ContextCapsule string` field with tag `context_capsule,omitempty` to `codexColonizeManifest`, with a doc comment restated for the survey workflow. Populate it inside `buildCodexColonizeManifest` from a value computed once by its caller `runCodexColonizePlanOnly` — pass it in as a parameter rather than calling `resolveCodexWorkerContext()` inside the builder, so the builder stays a pure assembler and there is still exactly one call site on this lane. Confirm the manifest reaches the result map key `colonize_manifest` with the field intact.

    Edit SIX wrapper markdown files — three per command, not two. Each of `/ant-plan` and `/ant-colonize` has three hand-maintained copies with no generator between them: the canonical nested Claude wrapper (`.claude/commands/ant/<verb>.md`), the flat installed-consumer Claude mirror (`.claude/commands/ant-<verb>.md`, the file a Claude Code session actually runs), and the OpenCode copy (`.opencode/commands/ant/<verb>.md`). All three are currently byte-identical per command; verify that with `diff` before editing, and if any pair already differs, stop and report rather than editing on top of an existing drift.

    In each of the three plan wrapper copies, in the dispatch stage that currently says to pass each dispatch's brief verbatim under a `Runtime Worker Brief` heading, add the instruction to read `plan_manifest.context_capsule` ONCE from the manifest (it is not per-dispatch data — reuse the same value for every worker this run spawns), prepend it VERBATIM ahead of the brief, and treat it as the sole source of the owner's steering notes and the previous helper's relay note so neither is written a second time. Mirror the wording `build.md` already uses at its line 348 rather than inventing new phrasing. Apply the identical instruction to all three colonize copies against `colonize_manifest.context_capsule`, in the numbered dispatch step that currently lists brief, output_paths, active signals and skill_section.

    Make each command's three copies byte-identical: after editing, `diff` the canonical nested copy against the flat mirror and against the OpenCode copy, and reconcile until both diffs are empty for each command. Then update `.aether/commands/plan.yaml` and `.aether/commands/colonize.yaml` — the source definitions the wrappers correspond to — with the same one-sentence instruction, so the source and the three copies do not drift.

    Extend cmd/wire_capsule_198_2_test.go with `TestColonizeDelegateManifestCarriesOneSteeringNote`, the colonize twin of Task 1's test, plus `TestPlanAndColonizeWrappersAreByteIdentical`. That parity test must be three-way per command, not pairwise: add `planWrapperTripletPaths(repoRoot)` and `colonizeWrapperTripletPaths(repoRoot)` next to the existing `buildWrapperTripletPaths` in cmd/lifecycle_wrapper_contract_test.go, each returning the canonical nested path, `flatMirrorPath(repoRoot, verb)`, and the OpenCode path in that order, and reuse the `triplet_is_byte_identical` subtest shape that compares every copy against the canonical one. Assert in the same test that the capsule instruction substring appears in all three copies of each command and in both YAML sources, so a two-copy edit fails by name and reports which copy is missing it.
  </action>
  <verify>
    <automated>go build ./cmd/aether &amp;&amp; go test ./cmd/ -run 'TestColonizeDelegateManifestCarriesOneSteeringNote|TestPlanAndColonizeWrappersAreByteIdentical' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'context_capsule,omitempty' cmd/codex_colonize.go` returns exactly one line.
    - `grep -c 'resolveCodexWorkerContext()' cmd/codex_colonize.go` returns 2, and neither is inside `buildCodexColonizeManifest`.
    - `diff .claude/commands/ant/plan.md .claude/commands/ant-plan.md` and `diff .claude/commands/ant/plan.md .opencode/commands/ant/plan.md` both produce no output; same two diffs for colonize against `.claude/commands/ant-colonize.md` and `.opencode/commands/ant/colonize.md`.
    - `grep -l 'context_capsule' .claude/commands/ant/plan.md .claude/commands/ant-plan.md .opencode/commands/ant/plan.md .claude/commands/ant/colonize.md .claude/commands/ant-colonize.md .opencode/commands/ant/colonize.md .aether/commands/plan.yaml .aether/commands/colonize.yaml` lists all eight files.
    - `grep -c 'flatMirrorPath' cmd/lifecycle_wrapper_contract_test.go` returns at least 4 — the helper's own definition plus one use in each of the build, plan and colonize triplet functions; the flat mirror is named by that helper on every command, never hardcoded in one place and forgotten in another.
    - Reverting the capsule instruction in `.claude/commands/ant-plan.md` alone makes `TestPlanAndColonizeWrappersAreByteIdentical` fail and name that file (prove by breaking it; restore afterwards) — the flat mirror is the copy an installed Claude Code session runs, so a green test with that file unedited would be a false certificate.
    - `go test ./cmd/ -run 'TestColonizeDelegateManifestCarriesOneSteeringNote|TestPlanAndColonizeWrappersAreByteIdentical' -count=1` exits 0.
    - `go test ./cmd/ -run 'TestPlanWrapperCeremonyContract|TestPlanWrapperStageSkeleton|TestLifecycleWrappersCarryCoherentJobContract' -count=1` exits 0 — the existing wrapper contracts still hold after the edit.
  </acceptance_criteria>
  <done>Both the planner and all four surveyors, spawned from Claude or OpenCode, receive the runtime-assembled memory pack, and none of the three copies of either wrapper can drift from the others without a test failing by name.</done>
</task>

<task type="auto">
  <name>Task 3: Every memory source, on the delegate lane, proven per command</name>
  <files>cmd/wire_capsule_198_2_test.go</files>
  <read_first>
    - cmd/wire_capsule_198_2_test.go (the file as it stands after Tasks 1-2)
    - cmd/build_pheromone_190_05_test.go lines 200-270 (`TestEightCommandsDeliverPheromoneExactlyOnce` — the table shape, and the reason CONTEXT.md names it as the test that stayed green while the shipped lane carried nothing)
    - cmd/colony_prime_context.go lines 540-950 (every section builder and the store each reads from, so each sentinel is seeded through the runtime's own writer rather than a hand-typed literal)
    - cmd/build_review_190_findings_test.go lines 440-500 (a worked example of seeding a populated colony and reading the resulting capsule)
    - cmd/codex_dispatch_contract.go lines 750-800 (`renderWorkerHandoffSection` and the one-home-per-heading convention)
  </read_first>
  <action>
    Add `TestPlanAndColonizeDelegateLanesCarryEveryMemorySource` — a table test with one row per command (`plan`, `colonize`) crossed with one row per memory source. Seed each source through the runtime's own writer, never by writing a JSON literal a real run could not produce: a Queen-file line via the same preferences/wisdom write path `aether preferences` uses; a shared cross-project entry via the hive store writer; a learned habit via the promotion service; a failure-log entry via the runtime's midden append; an owner steering note via the pheromone writer; an owner answer via `decision-answer`; a relay note via the worker-handoff writer. Each sentinel string must be unique per source and per command so a mix-up is visible in the failure message.

    Assert each sentinel appears in the manifest-level `context_capsule` for that command. On failure, name the missing source in plain words in the message ("the failure log did not reach the planner's prompt"), not just a diff.

    Add `TestPlanAndColonizeCapsulesMatchTheInProcessLane`: for the same seeded colony, capture the capsule the delegate manifest carries and the capsule the in-process path sets on its worker dispatch, and assert they are byte-identical (D-03). Add `TestDelegateCapsuleRendersSteeringAndRelayExactlyOnce`: count occurrences of the steering-notes heading and the previous-helper-relay heading across the full assembled worker prompt (capsule plus every per-dispatch field the wrapper is told to append) and fail if either exceeds one. Add `TestDelegateCapsuleIsStableAcrossRuns`: run the plan-only path twice against unchanged state and assert the two capsules are byte-identical.

    Every subtest must run against the delegate/plan-only path. A subtest that only exercises the in-process dispatch does not satisfy WIRE-01; add a comment at the top of the file saying so and naming why.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestPlanAndColonizeDelegateLanesCarryEveryMemorySource|TestPlanAndColonizeCapsulesMatchTheInProcessLane|TestDelegateCapsuleRendersSteeringAndRelayExactlyOnce|TestDelegateCapsuleIsStableAcrossRuns' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/ -run 'TestPlanAndColonizeDelegateLanesCarryEveryMemorySource' -v -count=1` reports at least 14 subtests (7 sources x 2 commands) and exits 0.
    - Removing the new `contextCapsule` assignment from `runCodexColonizePlanOnly` makes at least 7 of those subtests fail (prove by breaking it; restore afterwards).
    - `go test ./cmd/ -run 'TestPlanAndColonizeCapsulesMatchTheInProcessLane|TestDelegateCapsuleRendersSteeringAndRelayExactlyOnce|TestDelegateCapsuleIsStableAcrossRuns' -count=1` exits 0.
    - `grep -c 'saveGlobalsCmd(t)' cmd/wire_capsule_198_2_test.go` is at least 1.
  </acceptance_criteria>
  <done>Each of the seven memory sources is proven to arrive at both the planner and the surveyors on the lane the owner runs, and the two lanes are proven to deliver the same bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored memory -> worker prompt | Text written by earlier workers (relay notes, failure-log entries) and by the owner (steering notes, answers) is replayed verbatim into a new worker's instructions. |
| runtime -> wrapper -> spawned worker | The manifest crosses a process boundary into a platform wrapper that composes the actual prompt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-198.2-01 | Tampering | `resolveCodexWorkerContext()` payload on two new lanes | high | mitigate | Reuse the single existing assembler, which already applies `colony.SanitizeSignalContent` on the midden and observation paths (198.1 review CR-02). Add no new store reader and no second sanitisation path on these lanes. |
| T-198.2-02 | Elevation of Privilege | wrapper prompt composition | medium | mitigate | Wrappers are instructed to pass the capsule through verbatim and are explicitly forbidden from assembling or editing memory; the runtime stays the only assembler. |
| T-198.2-03 | Information Disclosure | capsule on a memory-free colony | low | accept | `omitempty` keeps the field absent rather than emitting an empty structure; no secret material is newly exposed because the same payload already ships on the build manifest. |
</threat_model>

<verification>
- `go build ./cmd/aether` and `go vet ./...` exit 0.
- `go test ./cmd/ -run '198_2|DelegateManifestCarries|DelegateLanesCarry|WrappersAreByteIdentical' -count=1` exits 0.
- `go test ./cmd/ -run 'TestEightCommandsDeliverPheromoneExactlyOnce|TestBuildManifestCarriesContextCapsuleOnce' -count=1` still exits 0 (no regression in the existing one-home guards).
- `go test ./cmd/ -run 'Wrapper' -count=1` exits 0 — every existing wrapper contract, including the three-copy build triplet and the plan wrapper's ceremony contract, survives the edit.
</verification>

<success_criteria>
The plan and colonize manifests on the Claude/OpenCode lane each carry a `context_capsule` containing all seven memory sources; the two lanes' capsules are byte-identical; all three hand-maintained copies of each wrapper and both YAML sources tell the spawner to prepend it verbatim, with the three copies proven byte-identical; and no section is delivered twice.
</success_criteria>

## Artifacts this phase produces (Plan 01)

| Symbol / path | Kind |
|---|---|
| `codexPlanManifest.ContextCapsule` (json `context_capsule`) | new struct field |
| `codexColonizeManifest.ContextCapsule` (json `context_capsule`) | new struct field |
| `buildCodexColonizeManifest(..., contextCapsule string)` | changed signature |
| `cmd/wire_capsule_198_2_test.go` | new file |
| `TestPlanDelegateManifestCarriesOneSteeringNote` | new test |
| `TestColonizeDelegateManifestCarriesOneSteeringNote` | new test |
| `TestPlanAndColonizeDelegateLanesCarryEveryMemorySource` | new test |
| `TestPlanAndColonizeCapsulesMatchTheInProcessLane` | new test |
| `TestDelegateCapsuleRendersSteeringAndRelayExactlyOnce` | new test |
| `TestDelegateCapsuleIsStableAcrossRuns` | new test |
| `TestPlanAndColonizeWrappersAreByteIdentical` | new test (three-way per command) |
| `planWrapperTripletPaths(repoRoot)` / `colonizeWrapperTripletPaths(repoRoot)` | new test helpers (cmd/lifecycle_wrapper_contract_test.go) |
| capsule pass-through instruction in 6 wrapper `.md` files (3 copies x 2 commands) + 2 `.yaml` sources | changed behaviour |

<output>
Create `.planning/phases/198.2-memory-reaches-every-helper/198.2-01-SUMMARY.md` when done
</output>
