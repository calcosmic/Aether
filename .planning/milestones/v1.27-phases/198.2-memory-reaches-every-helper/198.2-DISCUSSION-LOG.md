# Phase 198.2: Memory Reaches Every Helper - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-30
**Phase:** 198.2-memory-reaches-every-helper
**Areas discussed:** Page limit for the planner's memory, What happens when deep research finishes, What the next phase hears about the last one, The greeting card

---

## Backlog todos (cross-reference step)

| Option | Description | Selected |
|--------|-------------|----------|
| Worker turnaround is too slow | Fold as a constraint: more memory must not slow brief assembly or add a helper | ✓ (folded) |
| Spec builder command | New user-facing spec command; parked for v1.28+ | ✓ (ticked — recorded as deferred, not a constraint) |
| Preflight timeout hardcoded | 20-second pre-start check that can't be tuned; unrelated | |

**Notes:** The spec-builder tick was treated as "don't lose it" rather than scope for this phase, matching how Phase 198 handled the same tick.

---

## Page limit for the planner's memory

| Option | Description | Selected |
|--------|-------------|----------|
| Its own space | Separate capped slot for the map digest; memory allowance untouched | ✓ |
| Share the existing allowance | Briefs stay today's size; ranking decides what's trimmed | |
| Own space for planning only | Separate slot for planner/surveyors/scout; builder shares | |

| Option | Description | Selected |
|--------|-------------|----------|
| Send it with an age warning | Digest arrives headed by a plain age line | ✓ |
| Drop it past a cut-off | Older than a limit → omitted with a refresh hint | |
| Always send, no warning | No staleness signal | |

| Option | Description | Selected |
|--------|-------------|----------|
| Exactly the same pack | One assembly function, byte-identical on both lanes, parity test | ✓ |
| A lighter planning version | Tailored subset; two versions to keep in sync | |

| Option | Description | Selected |
|--------|-------------|----------|
| The same top 5 everyone gets | One hive selection rule for the research scout | ✓ |
| A wider set for the scout | Top ~15 for the scout; second rule to maintain | |

**User's choice:** All four recommended options.
**Notes:** None.

---

## What happens when deep research finishes

| Option | Description | Selected |
|--------|-------------|----------|
| No — file only, keep offering promotion | Write-up reaches helpers as reading; habits stay a yes/no step (recommended) | |
| Yes — auto-promote strong findings | Findings above the bar become habits with no prompt | ✓ |
| Auto-record as unconfirmed hunches | Stored as hypotheses; steer nobody until confirmed | |

| Option | Description | Selected |
|--------|-------------|----------|
| File it, clearly marked partial | Interrupted run's findings filed with a partial label | ✓ |
| File nothing | Only completed runs are filed | |

| Option | Description | Selected |
|--------|-------------|----------|
| Keep both, newest first | Same-topic write-ups accumulate, newest shown first | ✓ |
| Replace the older one | New run supersedes; title-match risk | |

**User's choice:** Auto-promote (against recommendation); partial filing; keep both.
**Notes:** Claude added a source label on promoted habits ("from research: <topic>, <date>") as a guard, without asking, so a wrong finding can be traced and removed.

---

## What the next phase hears about the last one

| Option | Description | Selected |
|--------|-------------|----------|
| Only what failed or was flagged | Failures/flags one sentence each; passes collapse to one line | ✓ |
| The full reports, condensed | Everything within a cap | |

| Option | Description | Selected |
|--------|-------------|----------|
| Previous phase only | Older failures already arrive via midden and ledgers | ✓ |
| Running summary of every phase | Growing block, duplicates the failure log | |

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, the same closing summary | Closing-card text reused word-for-word as opening context | ✓ |
| No — failures and flags only | Leaner; writer doesn't learn what was delivered | |

**User's choice:** All three recommended options.
**Notes:** "outcome.md" in WIRE-07 has never existed; the closing summary is what "outcome" means (CONTEXT D-11).

---

## The greeting card

| Option | Description | Selected |
|--------|-------------|----------|
| A few lines each | One preferences line, top 3 habits, one-line relay note | ✓ |
| Fuller | All preferences, top 5 habits, full next-worker instructions | |

| Option | Description | Selected |
|--------|-------------|----------|
| The strongest | Ranked by confidence; also fixes the dashboard's most-recent regression | ✓ |
| The most recent | Last three learned regardless of confidence | |

| Option | Description | Selected |
|--------|-------------|----------|
| Leave those parts out | Card shows only what exists | ✓ |
| Say 'nothing learned yet' | One filler line per missing part | |

**User's choice:** All three recommended options.
**Notes:** None.

---

## Claude's Discretion

- Sizes of the map-digest and carry-forward slots; brief section ordering.
- Deterministic condensation method for the seven survey reports.
- Research registration mechanism (state pointer vs directory scan).
- Persisting the closing summary as `outcome.md` vs reading `continue.json`.
- WIRE-06: give `Phase Learnings` / `Key Decisions` a real writer or remove them; never duplicate another section; writer-invariant test.
- Test design (sentinel sentences per source on the delegate lane; brief-size invariant).

## Deferred Ideas

- Spec builder command — new command, v1.28+ backlog.
- ts-host preflight timeout — reviewed, not folded, unrelated.
