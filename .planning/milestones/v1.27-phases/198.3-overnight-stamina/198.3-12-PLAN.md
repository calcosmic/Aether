---
phase: 198.3-overnight-stamina
plan: 12
type: execute
wave: 2
depends_on:
  - 198.3-11
  - 198.3-13
  - 198.3-14
  - 198.3-15
  - 198.3-16
files_modified:
  - cmd/autopilot_checkpoints.go
  - cmd/codex_workflow_cmds.go
  - cmd/criterion_owner_confirmation.go
  - cmd/recovery_engine.go
  - cmd/autopilot_report.go
  - cmd/autopilot_checkpoints_test.go
  - cmd/seal_ceremony_test.go
  - cmd/autopilot_report_test.go
  - README.md
autonomous: true
requirements:
  - STAM-01
  - STAM-05
gap_closure: true
must_haves:
  truths:
    - "Seal refuses when pending-decision storage is corrupt, unreadable, or cannot durably issue an owner recovery capability."
    - "Only a genuinely absent pending-decision file is treated as having no queued checkpoints."
    - "Every unresolved checkpoint shown at seal has exactly one fresh, capability-bearing recovery command, even when the same criterion also has a legacy owner-confirmation projection."
    - "Malformed storage cannot be hidden by the legacy flags.json fallback or by an empty checkpoint slice."
    - "Visual seal refusal is written to stderr and exits 1, while raw checkpoint capabilities occur only in that immediate owner-facing output and never in pending decisions, reports, lifecycle events, or documentation."
  artifacts:
    - path: "cmd/autopilot_checkpoints.go"
      provides: "Error-aware seal checkpoint loading and fresh transient recovery capability issuance"
    - path: "cmd/codex_workflow_cmds.go"
      provides: "Fail-closed seal integration and stable checkpoint-identity deduplication"
    - path: "cmd/criterion_owner_confirmation.go"
      provides: "Legacy owner-confirmation projection keyed by the same stable checkpoint identity"
    - path: "cmd/recovery_engine.go"
      provides: "Visual stderr/error-code ownership and capability-safe lifecycle telemetry"
    - path: "cmd/autopilot_report.go"
      provides: "Durable report projection that never copies transient checkpoint capabilities"
    - path: "cmd/seal_ceremony_test.go"
      provides: "Black-box JSON/visual seal refusal, exact-command, deduplication, and durable-secret proof"
    - path: "README.md"
      provides: "Owner instructions to copy the emitted capability-bearing command rather than construct one from redacted rows"
  key_links:
    - from: "cmd/codex_workflow_cmds.go"
      to: "cmd/autopilot_checkpoints.go"
      via: "checkSealBlockers consumes both checkpoint blockers and the loader error"
      pattern: "autopilotCheckpointSealBlockers"
    - from: "cmd/autopilot_checkpoints.go"
      to: "pending-decisions.json"
      via: "atomic hash issuance before rendering each seal recovery command"
      pattern: "UpdateJSONAtomically"
    - from: "cmd/codex_workflow_cmds.go"
      to: "cmd/criterion_owner_confirmation.go"
      via: "checkpoint and legacy compatibility blockers deduplicate by stable checkpoint identity, never command text"
      pattern: "CheckpointKey"
    - from: "cmd/codex_workflow_cmds.go"
      to: "cmd/recovery_engine.go"
      via: "seal refusal writes the visual menu to stderr, marks exit code 1, and records only capability-safe telemetry"
      pattern: "markRenderedCommandError"
    - from: "cmd/autopilot_report.go"
      to: "cmd/autopilot_checkpoints.go"
      via: "durable next-action projection retains checkpoint identity but strips the transient capability-bearing command"
      pattern: "checkpoint-capability"
---

<objective>
Make the seal boundary fail closed when durable owner-checkpoint state cannot be trusted.

Purpose: An overnight run may finish code while owner work remains queued; D-02 requires seal to wait, and D-03 classifies corrupt required state as a genuine stop rather than permission to continue.
Output: Error-aware seal blocker loading, one capability-bearing recovery command per checkpoint, non-zero JSON/visual refusal, secret-safe durable sinks, and owner instructions that use the emitted command.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
@.planning/phases/198.3-overnight-stamina/198.3-11-SUMMARY.md
@cmd/seal_confirmation.go
@pkg/storage/storage.go

<interfaces>
- Plan 198.3-11 makes `checkpointDecisionAnswerCommand` require a transient checkpoint capability and provides the atomic hash-issuance helper.
- Plan 198.3-11 keeps checkpoint ID/`CheckpointKey` stable across capability rotations; that identity, not command text, is the compatibility-dedup key.
- Plan 198.3-14 makes terminal report-write failures error-first; this plan preserves that result while ensuring a successful durable report never copies a transient checkpoint capability into `LastReport.Next`.
- `checkSealBlockers(store, state)` is the shared pre-seal gate used by `validateSealReady` and the seal command.
- `storage.Store.LoadJSON` wraps filesystem errors, so `errors.Is(err, os.ErrNotExist)` is the sole allowed absent-file branch.
- `flags.json` remains a legacy ordinary-flag fallback only when `pending-decisions.json` is absent, never when it exists but is invalid.
- `renderRecoveryMenu` currently returns visual text while seal call sites discard the return, and it sends the full `errMsg` to `emitLoopBreakEvent`; both behaviors must change at this boundary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Return trustworthy checkpoint blockers or an explicit storage error</name>
  <files>cmd/autopilot_checkpoints.go, cmd/autopilot_checkpoints_test.go</files>
  <read_first>
    - cmd/autopilot_checkpoints.go
    - cmd/autopilot_checkpoints_test.go
    - cmd/pending_decision.go
    - cmd/forced_reviewer_waiver.go
    - pkg/storage/storage.go
  </read_first>
  <behavior>
    - Missing `pending-decisions.json` returns no checkpoint blockers and no error.
    - Malformed JSON, a directory at the file path, permission/read errors, or an atomic capability-write error returns a non-nil error rather than an empty blocker list.
    - A valid file returns only current-scope unresolved visual/runtime checkpoints, each with a freshly issued capability in its exact recovery command and the same stable ID/`CheckpointKey` it had before capability rotation.
  </behavior>
  <action>
Change `autopilotCheckpointSealBlockers` to return blockers plus an error. Distinguish only `errors.Is(err, os.ErrNotExist)` as the valid no-file/no-checkpoint case; propagate every other read or decode error. For a valid file, atomically issue a fresh checkpoint capability hash for every current-scope unresolved visual/runtime row using Plan 198.3-11's helper, then render blocker entries from the transient raw capabilities. Capability rotation must not change the row's stable ID/`CheckpointKey`, because Task 2 uses that identity to suppress the legacy compatibility blocker. If capability issuance or persistence fails, return the error and no authorization-bearing blocker set. Preserve phase, type-specific description, and scope filtering. Do not cache raw tokens, place them in `LastReport`, or silently fall back to a capability-free command.
  </action>
  <verify>
    <automated>rtk go test ./cmd -run 'TestAutopilotCheckpointSeal(Blockers|Storage|Capability)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Error-semantics assertion: absent file is the only storage error that maps to `(no blockers, nil)`.
    - Behavior assertion: corrupt, directory-backed, and unwritable fixtures return explicit errors.
    - Recovery assertion: every returned checkpoint blocker command includes a newly valid capability whose raw value is absent from durable JSON.
    - Test assertion: focused seal-loader tests pass with `-count=1`.
  </acceptance_criteria>
  <done>The checkpoint seal loader distinguishes absence from every unsafe storage failure and emits capability-bearing blockers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deduplicate and render a capability-safe, non-zero seal refusal</name>
  <files>cmd/codex_workflow_cmds.go, cmd/criterion_owner_confirmation.go, cmd/recovery_engine.go, cmd/autopilot_checkpoints_test.go, cmd/seal_ceremony_test.go</files>
  <read_first>
    - cmd/codex_workflow_cmds.go
    - cmd/criterion_owner_confirmation.go
    - cmd/recovery_engine.go
    - cmd/autopilot_checkpoints_test.go
    - cmd/seal_ceremony_test.go
    - cmd/autopilot_checkpoints.go
    - cmd/seal_confirmation.go
    - cmd/root.go
    - cmd/ceremony_emitter.go
    - pkg/storage/storage.go
  </read_first>
  <behavior>
    - `checkSealBlockers` appends a deterministic hard blocker when checkpoint storage exists but cannot be read, decoded, or updated.
    - Corrupt `pending-decisions.json` never falls through to valid-looking `flags.json` and never authorizes `aether seal`.
    - An absent pending-decision file may still use legacy `flags.json` for ordinary flags and otherwise permits seal when all other gates pass.
    - A durable runtime checkpoint and its legacy `needs_owner_confirmation` projection collapse by stable checkpoint identity to one blocker and one usable command containing `--checkpoint-capability`; command text is never the dedup key.
    - JSON and visual seal invocations identify the durability failure and return non-zero; visual mode writes the refusal to stderr and causes public `Execute` to return rendered exit code 1.
    - Lifecycle telemetry stores a safe error class/checkpoint identity or digest, never the raw blocker summary or capability; pending decisions and event JSONL contain hashes/identity only.
  </behavior>
  <action>
Consume the new error-aware checkpoint loader in `checkSealBlockers`. On any non-absent storage failure, append one stable blocker-shaped entry whose description names `pending-decisions.json`, the read/decode/write failure, and a safe diagnostic/recovery command. Prevent the ordinary pending-decision loader from treating an existing invalid file as permission to consult `flags.json`; legacy fallback is allowed only for `errors.Is(err, os.ErrNotExist)`. Deduplicate the synthetic durability blocker if both ordinary-flag and checkpoint parsing observe the same failure.

Migrate the legacy owner-confirmation consumer instead of comparing recovery strings: make `ownerConfirmationSealBlockers` derive the same stable runtime-checkpoint identity from phase, task, criterion, current session, and current goal that `upsertAutopilotCheckpoint` uses, then have `checkSealBlockers` suppress the compatibility projection by that ID/identity when the durable checkpoint blocker exists. The capability rotates, the identity does not. Never deduplicate on `RecoveryCommand`, question prose alone, or a capability hash. The resulting seal refusal must contain exactly one command for the criterion, and that command must include the fresh `--checkpoint-capability`; no capability-free duplicate may remain.

Make `renderRecoveryMenu` own both visual delivery and failure state because every current seal call site discards its returned string: in visual mode write `buildVisualRecoveryMenu(...)` to `stderr`, call `markRenderedCommandError(1)`, and retain a return value only for compatibility/pure tests. Keep JSON's `ok:false`/code 1 behavior. Replace the `emitLoopBreakEvent` detection signal that currently embeds full `errMsg` with capability-safe telemetry containing only the normalized command, error class, stable blocker/checkpoint identity when available, and/or a SHA-256 digest; the raw blocker summary and `--checkpoint-capability` value must never enter `event-bus.jsonl` or another lifecycle event.

Add a public-root black-box fixture that seeds one durable runtime checkpoint plus the matching legacy verification report, runs seal in JSON and `AETHER_OUTPUT_MODE=visual`, and proves stderr contains exactly one usable capability command and `Execute` returns rendered exit code 1. Extract the raw token from immediate output, inspect `pending-decisions.json` and `event-bus.jsonl`, and assert the token is absent while its SHA-256 binding and stable checkpoint identity are present. Cover corrupt, directory-backed, and unwritable stores, legacy fallback only on true absence, and successful seal compatibility. Keep unresolved owner checkpoints blocking after final phase completion. Do not implement the disconnected pause/retry helper cleanup; that remains Phase 198.4.
  </action>
  <verify>
    <automated>rtk go test ./cmd -run 'Test(Seal.*PendingDecision|Seal.*Checkpoint.*Capability|Seal.*Visual.*Exit|FinalPhaseAdvancesWithOwnerCheckpointAndSealBlocks)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: corrupt and unreadable pending-decision fixtures make both JSON and visual seal invocations non-zero.
    - Visual ownership assertion: the real seal command writes refusal text to stderr and public `Execute` returns rendered exit code 1, rather than merely returning a string or an error-shaped payload.
    - Dedup assertion: one durable checkpoint plus its legacy owner-confirmation projection renders exactly one recovery command, identified by stable checkpoint identity and containing a valid `--checkpoint-capability`.
    - Secret assertion: the extracted raw capability occurs only in immediate JSON/visual owner output; pending-state and lifecycle-event scans find no raw token, while pending state contains its SHA-256 hash.
    - Fallback assertion: a valid legacy `flags.json` does not mask an invalid present `pending-decisions.json`.
    - Compatibility assertion: absent pending-decision storage alone does not manufacture a blocker.
  </acceptance_criteria>
  <done>Seal fails closed in JSON and visual modes, emits one identity-deduplicated capability command, and keeps the raw capability out of pending state and lifecycle telemetry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keep transient capabilities out of durable reports and owner documentation</name>
  <files>cmd/autopilot_report.go, cmd/autopilot_report_test.go, README.md</files>
  <read_first>
    - cmd/autopilot_report.go
    - cmd/autopilot_report_test.go
    - cmd/autopilot_checkpoints.go
    - cmd/compatibility_cmds.go
    - cmd/status.go
    - README.md
  </read_first>
  <behavior>
    - Immediate run/seal output may contain the fresh capability-bearing command, but `autopilotInvocationReport.Next`, `autopilot/state.json`, and report JSON never contain its raw token.
    - Durable reports retain queued checkpoint IDs/questions plus a stable capability-free re-entry action; seal-ready state uses `aether seal` so the next owner invocation reissues a valid command.
    - README tells the owner to run seal and copy the exact emitted capability-bearing command, not construct a capability-free answer from the redacted decision list.
    - A recursive scan of the completed fixture's durable data tree finds the raw token nowhere while its SHA-256 binding remains in the pending row.
  </behavior>
  <action>
Separate immediate and durable next-action projections. The seal/run result may show the fresh capability-bearing command to the owner, but `autopilotInvocationReport.Next` and `autopilot/state.json` must persist a stable capability-free re-entry action plus queued checkpoint IDs/questions, never the raw token. Use `aether seal` when the state is seal-ready so seal reissues the command; otherwise use the existing safe lifecycle re-entry action rather than persisting a secret. Preserve Plan 198.3-14's error-first unsaved-report behavior and the normal status/report field contract.

Update the README morning handoff to inspect decision identities, invoke seal, copy each exact emitted capability-bearing command verbatim, answer, and rerun seal. `pending-decision-list --unresolved` remains an identity/evidence view with redacted hashes and must not be documented as a recipe for reconstructing authorization. Add report tests that extract the immediate raw token, persist/render status, recursively scan the test data root (including `autopilot/state.json` and report JSON), and prove the token is absent while checkpoint identity/evidence and the pending SHA-256 binding survive.
  </action>
  <verify>
    <automated>rtk go test ./cmd -run 'Test(AutopilotReport.*Capability|MorningHandoff.*Checkpoint)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Report assertion: `LastReport.Next` is a capability-free stable re-entry action and queued checkpoint identity/evidence remain durable.
    - Secret assertion: recursive durable-file scans find no extracted raw capability, while pending state contains its SHA-256 hash.
    - Status assertion: reopening status cannot reveal an old raw capability and still explains the stable next action.
    - Documentation assertion: README directs owners to copy the command emitted by seal and contains no capability-free checkpoint answer template.
  </acceptance_criteria>
  <done>Reports, status, and documentation retain useful checkpoint identity and recovery guidance without durably retaining or reconstructing a raw capability.</done>
</task>

</tasks>

<verification>
This is the final Wave 2 plan and depends on every Wave 1 gap plan. After the focused capability/seal suite, rerun the complete Phase 198.3 release proof in this order:

1. `rtk go test ./cmd -run 'Test(AutopilotCheckpoint|Seal|PendingDecision|DecisionAnswer|RunReportPersistenceFailure)' -count=1`
2. `rtk go test ./cmd -run 'Test(AuditCatalogGolden|CatalogCompleteness|PlatformParityGolden|NoPhantomCommands|NoOrphanRuntimeCommands|NoRegisteredSubcommandIsUnreferenced|OrphanAllowlistOnlyShrinks)' -count=1`
3. `rtk go test ./cmd -run 'Test(OvernightRunCompletesSixPhases|OvernightRunBlockerBaselineExceptionIsNarrow|OvernightRunQueueableMutationStopsEarly)' -count=1`
4. `rtk go build -o /dev/null ./cmd/aether`
5. `rtk go vet ./...`
6. `rtk go test ./...`
7. `rtk go test ./... -race`

The JSON and `AETHER_OUTPUT_MODE=visual` seal cases must invoke the public command boundary, inspect stderr, and assert command result 1. The durable-secret fixture must scan all files below the test data root after refusal and fail on the extracted raw capability.
</verification>

<success_criteria>
- Seal cannot pass when the owner-work store is unreadable, corrupt, or cannot save capability state.
- Missing storage alone remains a legitimate empty state.
- Valid unresolved checkpoints render exactly one usable single-use owner recovery command per stable checkpoint identity.
- Visual refusal is present on stderr and the process exits 1; JSON refusal remains `ok:false`/code 1.
- Raw capabilities exist only in immediate owner-facing output; durable decisions, reports, lifecycle events, and docs contain hashes/identity or safe re-entry commands only.
- Build, vet, full tests, race tests, command catalog/parity/orphan ratchets, and the six-phase mutation proof all pass after Plans 11-16.
- Phase 198.4 dead pause/retry helper cleanup remains explicitly deferred.
</success_criteria>

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-12-SUMMARY.md` when done.
</output>
