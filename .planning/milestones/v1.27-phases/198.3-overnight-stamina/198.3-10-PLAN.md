---
phase: 198.3-overnight-stamina
plan: 10
type: execute
wave: 7
depends_on: ["198.3-06", "198.3-07", "198.3-09"]
files_modified:
  - cmd/run_overnight_198_3_test.go
  - cmd/TEST_MATRIX.md
  - colony/playbooks/continue.md
  - .planning/decisions/autopilot-pause-conditions.md
  - .aether/docs/PARITY_CLASSIC_VS_GO.md
  - README.md
autonomous: true
requirements: [STAM-01, STAM-02, STAM-03, STAM-04, STAM-05, STAM-06]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "STAM-06: one invocation of the real run loop completes a simulated colony of at least six phases while visual/runtime/replan work is queued, not treated as a stop."
    - "The integrated fixture also exercises an ordinary blocker baseline, a High finding, phase-scoped readiness, elapsed/spend reporting, exact next action, and the rule that run never seals."
    - "A mutation/proof check fails before phase six if any queueable trigger is changed to terminal stop."
    - "The superseded pause decision and owner docs describe the implemented canonical trigger/disposition contract, measured-only reporting, and seal boundary."
    - "Repository-wide build, vet, test, race, command catalog, parity, and orphan ratchets are clean."
  artifacts:
    - cmd/run_overnight_198_3_test.go
    - updated autopilot decision record
    - updated README/parity/test matrix
    - clean full verification evidence in 198.3-08-SUMMARY.md
  key_links:
    - "six-phase fixture -> real runCompatibilityAutopilot -> real build/continue state transitions -> persisted report/status"
    - "queueable trigger mutation -> fixture stops early -> STAM-06 guard"
    - "implemented typed catalogue -> decision/README dry-run table"
  prohibitions:
    - statement: "A test that calls only the pure trigger evaluator cannot satisfy STAM-06."
      status: required
      verification: fixture invokes the public run command/live loop and asserts six durable phase advances
    - statement: "Do not weaken, skip, or replace repository-wide final gates with targeted tests."
      status: required
      verification: summary records all four required Go commands and command-surface ratchets
    - statement: "Autopilot must not seal in the fixture."
      status: required
      verification: command/worker log and final exact next action assertion
---

<objective>
Prove the entire overnight story under one real six-phase run, align the durable decision/docs with the shipped behavior, and close every repository quality gate.

Purpose: focused tests can all pass while the orchestration loop still stops after phase two. This plan is the end-to-end proof that the parts survive one long invocation.

Output: a mutation-sensitive six-phase fixture, updated owner-facing contract, and full clean verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-RESEARCH.md
@.planning/phases/198.3-overnight-stamina/198.3-PATTERNS.md
@.planning/phases/198.3-overnight-stamina/198.3-05-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-06-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-07-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-08-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete six simulated phases under one live run with every queueable condition in the way</name>
  <files>cmd/run_overnight_198_3_test.go</files>
  <read_first>
    - cmd/run_autopilot_test.go `completingRunInvoker`, `withCompletingInvoker`, and `seedRunFixture`
    - the final plan 01 policy/blocker tests, plan 02 reviewer/checkpoint tests, plan 03 preflight call-count tests, plan 04 replan/run tests, and plan 05 report tests
    - cmd/codex_build.go/continue finalization fixture requirements so simulated workers still produce genuine created files and verified claims
    - existing spend-ledger test writer/helper; do not handcraft a reduced invented ledger schema
    - `saveGlobalsCmd(t)`/environment/workspace cleanup conventions
  </read_first>
  <behavior>
    - A colony with 6 or more real phase records enters one `aether run --headless` invocation and finishes every phase through build+continue state transitions.
    - At least one UI created/modified claim queues a visual checkpoint; one owner-only criterion queues runtime verification; an interval checkpoint with a confirmed learning queues one replan decision; none stops headless execution.
    - One ordinary blocker exists before the run and remains count-stable; one completed Auditor emits a High finding and passing score; neither stops the run.
    - Readiness instrumentation observes one successful preflight per phase shared by build/continue and a fresh call at the next phase.
    - Final report says complete, carries queued IDs/types, unchanged blocker counts, High finding, measured/unreported spend, elapsed wall clock, six compact phase outcomes, and exact next `aether seal`. Status returns the same report.
    - No command/worker log contains seal, provider switch, phase skip, or a seventh unintended build.
  </behavior>
  <action>
    Create `cmd/run_overnight_198_3_test.go` and call `saveGlobalsCmd(t)` first in every top-level test that mutates command globals. Extend the existing fixture/invoker rather than creating a second runner. The invoker must perform tiny genuine workspace writes so claim verification is real; make one phase write a UI path, make one task criterion owner-only, and have one Auditor that was already legitimately dispatched by an existing named-risk/explicit-review policy return an artifact with score >=60 and one High finding. Never add or dispatch an Auditor solely to manufacture test score evidence. Instrument provider preflight, seed an ordinary blocker through the flag writer, and write spend rows through the existing ledger writer. Use an injected clock to avoid sleeps.

    Invoke the public root command/live `runCompatibilityAutopilot` path once for six phases in headless mode with a replan interval that is actually reached after successful learning capture. Assert every phase's durable state/task completion, not merely `phases_completed` from a fake result. Load pending decisions and assert visual/runtime/replan types; load autopilot report and assert all D-17..D-20 fields; call status and compare the stored report object.

    Add failure variants for each queueable type, or one table that injects a catalogue/evaluator override only within the test seam: when visual, runtime, or replan disposition is changed to terminal stop, the same fixture must finish fewer than six phases. If production design does not expose a safe injection seam, perform and record the planner-discipline mutation proof during execution: temporarily flip each disposition, run the test and observe failure, then restore before commit. Do not weaken this to a pure evaluator assertion.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestOvernightRunCompletesSixPhases|TestOvernightRunQueueableMutationStopsEarly' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The main test invokes one real `run` command/live loop and asserts at least six durable phase completions.
    - Pending decisions include visual, runtime-verification, and one replan item; all remain unresolved for seal and none appears as stopped reason.
    - Ordinary blocker before/after count is equal, escalated count zero, High count >=1, Auditor score >=60.
    - Preflight count equals phase count (not worker count and not one global call).
    - Report/status equality includes elapsed, spend honesty, phase summaries, queue IDs, blockers, and exact `aether seal` next action.
    - Mutation proof fails before phase six for each queueable condition and passes after restoration.
    - Invocation log has no seal/switch/skip call.
  </acceptance_criteria>
  <reversibility rating="reversible">This task adds tests only; it exposes integration defects without adding another production path.</reversibility>
  <done>A real six-phase simulated colony can survive every owner-only overnight checkpoint in one run and hand the complete morning state back honestly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update the canonical overnight contract and pass every final gate</name>
  <files>cmd/TEST_MATRIX.md, colony/playbooks/continue.md, .planning/decisions/autopilot-pause-conditions.md, .aether/docs/PARITY_CLASSIC_VS_GO.md, README.md</files>
  <read_first>
    - .planning/decisions/autopilot-pause-conditions.md in full (mark superseded clauses explicitly; do not leave conflicting headless behavior)
    - README.md autopilot sections around the pause list, headless example, command table, and Codex workflow
    - .aether/docs/PARITY_CLASSIC_VS_GO.md autopilot parity anchor
    - cmd/TEST_MATRIX.md conventions and nearest lifecycle row
    - colony/playbooks/continue.md stale `medic-auto-spawn-check` instruction (replace with the supported runtime-owned genuine-stop recovery/Medic advice behavior; do not invent another command)
    - all plan summaries 01–07 and the final canonical catalogue/result structs (document implemented names, not planned names)
    - cmd/audit_catalog_test.go, cmd/parity_test.go, cmd/subcommand_reachability_ratchet_test.go final gate/update rules
  </read_first>
  <behavior>
    - Decision record clearly says Phase 198.3 supersedes the old “headless queues then stops” rule and records the implemented named trigger/disposition table, Auditor threshold, Critical-only rule, blocker baseline, lesson-aware replan, preflight bounds, report truth, and seal boundary.
    - README explains headless queue-and-continue versus genuine/normal stops in plain English and points owners to dry-run/status/decision-answer/seal.
    - Parity/test matrix names the new six-phase proof and no longer cites the obsolete broad pause golden test as authority.
    - Generated command/catalog/orphan fixtures are clean from plans 04/05 and no baseline is broadened.
  </behavior>
  <action>
    Update the decision document rather than silently deleting its history: add a Phase 198.3 supersession header and replace the active contract with the exact implemented trigger codes and mode dispositions. State that owner checkpoints queue headlessly but block seal, score 60 passes, High reports but does not stop, existing ordinary blocker count is a baseline, and run never seals.

    Rewrite README's stale broad pause paragraph/table and headless example to match the runtime. Keep the explanation beginner-friendly: “things only you can judge are put on a morning list; broken/unsafe work still stops.” Document `aether run --dry-run`, `aether status`, exact decision-answer flow, and final `aether seal`. Update the parity doc/test matrix to point at `TestOvernightRunCompletesSixPhases` plus focused catalogue tests. Replace the deleted `medic-auto-spawn-check` line in `colony/playbooks/continue.md` with the implemented runtime-owned genuine-stop recovery/Medic advice path; no shipped document may tell a wrapper to call the removed adapter.

    Run targeted phase tests and generated command-surface ratchets first. If a generated fixture differs, regenerate only with its owning test flag and inspect the diff; never widen the orphan baseline. Then run the project gates in the required order: build, vet, full tests, race tests. Fix only Phase 198.3 regressions; preserve unrelated dirty work. Record exact commands/results and any intentionally unreported external telemetry in the plan summary.
  </action>
  <verify>
    <automated>go build ./cmd/aether &amp;&amp; go vet ./... &amp;&amp; go test ./... &amp;&amp; go test ./... -race</automated>
  </verify>
  <acceptance_criteria>
    - Search finds no active doc statement that headless runtime/visual/replan decisions queue and then stop, that High findings halt, or that interval alone forces replan.
    - Shipped/current docs and TEST_MATRIX contain no instruction to call any of the eleven removed adapters; historical decision/baseline records may retain names only as explicitly marked history.
    - README includes a plain-English overnight explanation and exact morning commands.
    - Catalog/parity/orphan ratchets pass; current orphan allowlist shrank as intended and baseline is unchanged.
    - `go build ./cmd/aether`, `go vet ./...`, `go test ./...`, and `go test ./... -race` each exit 0.
    - `198.3-10-SUMMARY.md` records the six-phase test, mutation proof, and all final command results.
  </acceptance_criteria>
  <reversibility rating="reversible">Docs describe test-proven behavior; final gates do not mutate product state beyond ordinary generated fixtures already owned by this phase.</reversibility>
  <done>The implemented overnight behavior, decision history, owner docs, parity map, and complete repository test suite agree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|---|---|---|
| simulated fixture -> confidence claim | A fake may bypass the real loop and falsely “prove” stamina | Invoke public run/live build+continue transitions; inspect durable state; mutation must stop early |
| docs -> operator behavior | Stale pause instructions could cause unsafe assumptions | Generate table from implemented names during execution review; search for superseded statements |
| broad test update flags -> golden files | Regeneration could hide unrelated command drift | Use owning test flags, inspect exact diff, never touch orphan baseline, final ratchets |
| full test/race execution -> dirty workspace | Fixes could overwrite unrelated user changes | Check status first, scope fixes to phase files, preserve unrelated edits per repository policy |
</threat_model>

<verification>
- `go test ./cmd/ -run 'OvernightRun|AutopilotPolicy|BlockerSnapshot|ReviewerArtifact|OwnerCheckpoint|PhaseScopedPreflight|LessonAwareReplan|AutopilotReport|GuidedInsert|SwarmThreeStrike' -count=1`
- `go test ./cmd/ -run 'TestAuditCatalogGolden|TestPlatformParityGolden|TestNoOrphanRuntimeCommands|TestOrphanAllowlistOnlyShrinks' -count=1`
- `go build ./cmd/aether`
- `go vet ./...`
- `go test ./...`
- `go test ./... -race`
</verification>

<success_criteria>
One public `aether run --headless` invocation completes six or more simulated phases through real state transitions while queueable work accumulates safely; its persisted report/status are honest and complete; mutation proves queue conditions cannot regress to stops unnoticed; docs match; all repository gates pass.
</success_criteria>

## Artifacts this phase produces (Plan 10)

| Symbol/path | Kind |
|---|---|
| `TestOvernightRunCompletesSixPhases` | integrated STAM-06 proof |
| queueable mutation guard | regression sensitivity proof |
| updated autopilot decision record | canonical behavioral history |
| updated README/parity/test matrix | owner/developer documentation |
| `198.3-10-SUMMARY.md` | final verification evidence |

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-10-SUMMARY.md` when done.
</output>
