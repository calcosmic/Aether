---
phase: 198.3-overnight-stamina
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/codex_continue_finalize.go
  - cmd/codex_continue.go
  - cmd/reviewer_artifact_198_3_test.go
autonomous: true
requirements:
  - STAM-01
  - STAM-02
gap_closure: true
must_haves:
  truths:
    - "Every completed Auditor supplies a structured review object with an integer overall_score from 0 through 100."
    - "Missing artifacts, empty objects, malformed JSON, invalid scores, and invalid severities fail the review instead of behaving like no Auditor signal."
    - "The direct continue lane and external-finalize lane apply the same fail-closed evidence predicate."
    - "A valid score of 60 passes the score floor; High findings remain report-only and Critical findings remain blocking."
  artifacts:
    - path: "cmd/codex_continue_finalize.go"
      provides: "Shared structured reviewer evidence validation and external-lane blocking"
    - path: "cmd/codex_continue.go"
      provides: "Direct-lane propagation of evidence errors into the review gate"
    - path: "cmd/reviewer_artifact_198_3_test.go"
      provides: "Direct/external negative matrix and boundary regressions"
  key_links:
    - from: "cmd/codex_continue.go"
      to: "cmd/codex_continue_finalize.go"
      via: "direct WorkerResult.Artifacts passes through normalizeContinueReviewEvidence"
      pattern: "normalizeContinueReviewEvidence"
    - from: "cmd/codex_continue_finalize.go"
      to: "externalContinueReviewReport"
      via: "EvidenceErrors become blocking issues before phase advancement"
      pattern: "EvidenceErrors"
---

<objective>
Fail completed review work closed when its structured evidence is absent or malformed, on both continue execution lanes.

Purpose: D-04 and D-05 are meaningful only when a completed Auditor cannot evade the score floor or Critical-finding gate by returning invalid evidence.
Output: One shared validation contract, direct/external blocking propagation, and a complete negative-path matrix.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
@.planning/phases/198.3-overnight-stamina/198.3-REVIEW.md
@.planning/phases/198.3-overnight-stamina/198.3-03-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-10-SUMMARY.md

<interfaces>
- `normalizeContinueReviewEvidence(step, artifacts)` is called by both direct `runCodexContinueReviews` and external `mergeExternalContinueResults`.
- `codexContinueWorkerFlowStep.EvidenceErrors` is the shared typed channel for invalid review evidence.
- `externalContinueReviewReport` decides whether the external lane advances.
- The direct review report accumulates `BlockingIssues` before setting `Passed`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define a fail-closed completed-review evidence contract</name>
  <files>cmd/codex_continue_finalize.go, cmd/reviewer_artifact_198_3_test.go</files>
  <read_first>
    - cmd/codex_continue_finalize.go
    - cmd/reviewer_artifact_198_3_test.go
    - cmd/codex_continue.go
    - cmd/autopilot_policy.go
    - .aether/workers.md
  </read_first>
  <behavior>
    - Completed Auditor plus no `artifacts.review`, `{}`, `null`, a non-object, malformed JSON, a non-integer/out-of-range/missing `overall_score`, or an unsupported finding severity produces `EvidenceErrors`.
    - A completed Auditor with `overall_score: 60` and valid findings has no evidence errors; score 59 remains the existing below-floor stop.
    - Explicit malformed review evidence from any completed reviewer is an evidence error, while an Auditor-specific score requirement is not imposed when no Auditor was selected or the Auditor did not complete.
  </behavior>
  <action>
Strengthen `normalizeContinueReviewEvidence` as the single schema boundary. For a successfully completed Auditor, require a present JSON-object `artifacts.review` with a present integer `overall_score` in `[0,100]`; absence is an error, not legacy fallback. Continue validating every structured finding severity against CRITICAL/HIGH/MEDIUM/LOW/INFO and reject malformed/non-object evidence. Preserve valid fields for diagnostics but ensure any schema error is explicit and deduplicated. Do not manufacture an Auditor score when no Auditor ran, and do not turn timeout/failed launch into a missing-score violation because those statuses already have their own semantics. Preserve D-04's strict `<60` floor and D-05's Critical-only finding stop.
  </action>
  <verify>
    <automated>rtk go test ./cmd -run 'TestReviewerArtifact.*(Missing|Empty|Malformed|Score|Severity|Boundary)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Test matrix assertion: missing artifact, `{}`, malformed JSON, score -1, score 101, fractional/string score, and invalid severity each yield evidence errors.
    - Boundary assertion: valid score 60 passes and valid score 59 is represented for the existing below-floor evaluation.
    - Absence assertion: a plan with no Auditor dispatch does not gain a score or missing-score error.
    - Source assertion: both lanes still call the one normalizer rather than duplicating validation.
  </acceptance_criteria>
  <done>The shared normalizer rejects every missing or malformed completed-Auditor evidence case at the locked score/severity boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Block direct and external advancement on invalid reviewer evidence</name>
  <files>cmd/codex_continue_finalize.go, cmd/codex_continue.go, cmd/reviewer_artifact_198_3_test.go</files>
  <read_first>
    - cmd/codex_continue_finalize.go
    - cmd/codex_continue.go
    - cmd/reviewer_artifact_198_3_test.go
    - cmd/codex_continue_finalize_test.go
    - cmd/codex_continue_test.go
    - cmd/autopilot_report.go
  </read_first>
  <behavior>
    - A successful-status direct review step with evidence errors makes the direct review report fail and prevents advancement.
    - The equivalent external-finalize step makes `externalContinueReviewReport.Passed` false and persists named blocking issues.
    - Both lanes retain the primary reviewer status, raw sanitized diagnostic, valid findings, and exact evidence errors for the terminal report.
    - High-only valid evidence passes; Critical valid evidence and score 59 continue to stop for their existing named reasons.
  </behavior>
  <action>
In the direct review aggregation, append every normalized `EvidenceErrors` entry to `BlockingIssues` before `report.Passed` is computed, even when the worker status is `completed`. In `externalContinueReviewReport`, perform the same check before the successful-status `continue` branch so malformed evidence cannot bypass the gate; include reviewer name/caste and the validation reason in persisted blocking issues. Add table-driven tests that feed every negative artifact through both the direct WorkerResult path and `mergeExternalContinueResults`/`runCodexContinueFinalize`, asserting blocked/no-advance behavior rather than merely checking the helper's error slice. Retain primary outcome evidence and all valid High/Critical findings. Do not add a new stop trigger that would drift from the locked named catalogue; invalid completed review evidence fails the deterministic review/verification wall and therefore reaches the existing genuine-stop path under D-03.
  </action>
  <verify>
    <automated>rtk go test ./cmd -run 'TestReviewerArtifact.*(Direct|External|FailClosed|High|Critical)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Direct-flow assertion: every invalid completed-Auditor fixture produces `Passed:false`, no phase advancement, and a named blocking issue.
    - External-flow assertion: the same fixtures produce a blocked finalize result and leave durable phase state unadvanced.
    - Evidence assertion: output retains the original status/findings plus the validation reason.
    - Regression assertion: score 60, High-only findings, no-Auditor selection, and timeout semantics remain unchanged.
  </acceptance_criteria>
  <done>Direct and external continue flows both block advancement and retain diagnostics for invalid review evidence.</done>
</task>

</tasks>

<verification>
Run `rtk go test ./cmd -run 'TestReviewerArtifact' -count=1`, then `rtk go test ./cmd -run 'Test(ContinueReview|ExternalContinueReview|AutopilotAuditor)' -count=1`, followed by `rtk go test ./cmd -count=1`.
</verification>

<success_criteria>
- Completed Auditor evidence is valid, scored structured data or a blocking failure.
- Direct and external lanes produce equivalent results for every malformed case.
- Score 60 and High-only behavior remain exactly at their locked boundaries.
- Invalid evidence remains visible in the terminal report instead of being normalized into absence.
</success_criteria>

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-13-SUMMARY.md` when done.
</output>
