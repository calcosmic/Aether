---
phase: 198.3-overnight-stamina
plan: 19
subsystem: runtime-validation
tags: [go, continue, reviewer-evidence, fail-closed, tdd]

# Dependency graph
requires:
  - phase: 198.3-13
    provides: shared reviewer artifact normalization and direct/external evidence blockers
  - phase: 198.3-17
    provides: generation-bound continuation authorization and merged gap-closure baseline
provides:
  - mandatory human-readable bodies for structured and legacy review findings
  - fail-closed suggestion-only Critical handling on both continuation lanes
  - phase-boundary regressions preserving D-05 Critical/High severity policy
affects: [continue, continue-finalize, autopilot, reviewer-contract, overnight-stamina]

# Tech tracking
tech-stack:
  added: []
  patterns: [shared finding-body validation, suggestion-as-recovery-only, pre-advancement evidence blocking]

key-files:
  created: []
  modified:
    - cmd/codex_continue_finalize.go
    - cmd/reviewer_artifact_198_3_test.go

key-decisions:
  - "A review finding is admissible only when its title or description contains non-whitespace text; a suggestion is recovery advice, never evidence."
  - "Structured and legacy rows use the same fail-closed body rule before deduplication or phase advancement."
  - "Valid Critical findings continue to block, while valid High findings remain report-only even when the legacy blocking bit is true."

patterns-established:
  - "Validate legacy findings before merging so malformed rows cannot disappear without an evidence error."
  - "Boundary tests compare the serialized current-phase and phase-status projection before and after a blocked continuation."

requirements-completed: [STAM-01, STAM-02]

# Metrics
duration: 18min
completed: 2026-09-01
---

# Phase 198.3 Plan 19: Reviewer Finding Body Validation Summary

**Suggestion-only Critical findings now fail closed as invalid evidence before either direct continue or external continue-finalize can credit a phase.**

## Performance

- **Duration:** 18 min
- **Started:** 2026-09-01T18:56:43Z
- **Completed:** 2026-09-01T19:15:12Z
- **Tasks:** 2
- **Files modified:** 2 implementation/test files

## Accomplishments

- Required every structured `artifacts.review.findings`/`issues` row to carry a non-blank title or description, with worker- and index-specific evidence errors.
- Applied the same validation to legacy top-level findings before merging, preventing omission of `artifacts.review` from becoming a bypass.
- Removed `Suggestion` as a finding-admission condition while retaining it on otherwise valid rows as recovery advice.
- Proved both actual continuation boundaries return `advanced=false` and `blocked=true` without changing the serialized current-phase/phase-status projection.
- Kept D-05 intact: valid Critical rows stop through the Critical-finding gate and valid High rows advance when no other blocker exists.

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: Define the finding-body validation matrix** - `27a67143` (test)
2. **Task 1 GREEN: Validate structured and legacy finding bodies** - `bc280b02` (feat)
3. **Task 2: Prove both continuation boundaries stop before phase credit** - `ef94d2eb` (test)

**Plan metadata:** committed separately in the summary commit.

## Files Created/Modified

- `cmd/codex_continue_finalize.go` - Validates finding bodies before admission, preserves raw legacy rows until validation, and excludes suggestion-only rows from merging.
- `cmd/reviewer_artifact_198_3_test.go` - Covers the six-case body matrix plus direct/external phase-boundary, valid Critical, valid High, and durable-state regressions.

## Decisions Made

- Kept `normalizeContinueReviewEvidence` as the single schema boundary for direct and external review evidence.
- Reused `continueReviewEvidenceBlockingIssues` rather than adding a new stop path; malformed evidence therefore blocks at the existing pre-advancement wall.
- Tested typed severity and state outcomes rather than requiring identical direct/external renderer wording (`critical finding` versus `blocking finding`).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The first Task 2 run exposed a test-only wording assumption: the direct lane says `critical finding` while the external lane says `blocking finding`. The assertion was corrected to inspect the typed Critical finding and invalid-evidence state rather than renderer prose; all 33 Task 2 cases then passed.
- An optional full `go test ./cmd -count=1` run completed with 7,034 passes, 10 skips, and an unrelated pre-existing failure in `TestSwarmWorkerFailureReachesTheFailureLogOnBothLanes/wrapper_lane`: `swarm_manifest worker_count 0 does not match 1 dispatches`. The failure reproduces alone and is outside Plan 19's two-file ownership; the orchestrator was notified. Plan 19's required suites and static analysis are green.

## Verification

- RED gate: `TestReviewerArtifactFindingBodyValidationMatrix` produced 3 passes and 4 expected failures before implementation.
- `go test ./cmd -run 'Test(ReviewerArtifactFindingBodyValidationMatrix|ReviewerArtifactScoreAndSeverityValidationMatrix|HighReviewerFindingNeverBlocksSolelyOnLegacyBlockingBit)' -count=1` - 14 passed.
- `go test ./cmd -run 'Test(SuggestionOnlyCritical|ReviewerArtifactDirectFlowFailsClosed|ReviewerArtifactExternalFinalizeFailsClosed|HighReviewerFindingNeverBlocks)' -count=1` - 33 passed.
- `go test ./cmd -run 'Test(ReviewerArtifact|SuggestionOnlyCritical|HighReviewerFinding)' -count=1` - 59 passed.
- `go vet ./cmd/...` - passed.

## TDD Gate Compliance

- Task 1 has an explicit failing RED commit (`27a67143`) followed by the GREEN implementation (`bc280b02`).
- Task 2's boundary tests were added after Task 1 had already routed the new evidence error through the shared blocker seam, so no duplicate production implementation was needed; `ef94d2eb` locks the direct/external boundary behavior and D-05 controls.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The suggestion-only Critical reviewer bypass identified by phase verification is closed on both continuation lanes.
- Plan 19 adds no new threat surface or authentication gate.
- The unrelated swarm fixture regression remains for its owning plan/orchestrator to reconcile before a repository-wide `cmd` suite can be called fully green.

## Self-Check: PASSED

- Both modified task files and this summary exist in the isolated Plan 19 worktree.
- All three task commits are present in git history.
- No unexpected deletions, generated files, or untracked task artifacts remain.

---
*Phase: 198.3-overnight-stamina*
*Completed: 2026-09-01*
