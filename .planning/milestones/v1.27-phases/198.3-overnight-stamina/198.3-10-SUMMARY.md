---
phase: 198.3-overnight-stamina
plan: 10
subsystem: orchestration-testing
tags: [go, autopilot, integration, preflight, blockers, documentation]

# Dependency graph
requires:
  - phase: 198.3-01-through-09
    provides: typed run policy, blocker snapshots, owner checkpoints, phase-scoped readiness, lesson-aware replanning, durable reports, and the reduced CLI surface
provides:
  - one public headless run proof completing six genuine build/continue phases
  - a capability-scoped stable-blocker exception that leaves direct continue strict
  - build/continue readiness sharing with one successful probe per phase
  - a mutation-sensitive replan path that obeys the canonical typed disposition
  - canonical owner, parity, test-matrix, decision, and continue-playbook documentation
affects: [autopilot, continue, provider-readiness, seal, owner-handoff, release-verification]

# Tech tracking
tech-stack:
  added: []
  patterns: [capability-scoped gate exception, phase-scoped dispatch preflight, mutation-sensitive end-to-end fixture, frozen report equality]

key-files:
  created:
    - cmd/run_overnight_198_3_test.go
  modified:
    - cmd/codex_build.go
    - cmd/codex_continue.go
    - cmd/compatibility_cmds.go
    - cmd/TEST_MATRIX.md
    - colony/playbooks/continue.md
    - .planning/decisions/autopilot-pause-conditions.md
    - .aether/docs/PARITY_CLASSIC_VS_GO.md
    - README.md

key-decisions:
  - "Only the in-process run loop may carry a live blocker baseline into continue; direct aether continue keeps the classic strict Iron Law."
  - "Build performs one phase-scoped provider preflight before lifecycle mutation or worker invocation, and continue reuses that same-phase success."
  - "Headless replanning consults the typed disposition just like visual and runtime owner checkpoints; canonical queue-and-continue remains the active behavior."
  - "Run completion returns the owner boundary aether seal but never invokes seal or resolves queued owner work automatically."

patterns-established:
  - "Overnight proof: one public root command drives real build and continue state transitions, then verifies durable state and the frozen status report."
  - "Narrow capability: stable blockers can pass only with an explicit run-owned snapshot and canonical movement comparison; live flags are never rewritten."
  - "Mutation discipline: each queueable disposition must make the same six-phase fixture stop early when temporarily changed to stop."

requirements-completed: [STAM-01, STAM-02, STAM-03, STAM-04, STAM-05, STAM-06]

# Metrics
duration: 44min
completed: 2026-09-01
---

# Phase 198.3 Plan 10: Overnight Stamina Integration Summary

**One public headless invocation now completes six real build/continue phases while owner-only work queues for morning review, current danger stops, reports remain honest, and seal stays owner-controlled.**

## Performance

- **Duration:** 44 min
- **Started:** 2026-08-31T23:02:26Z
- **Completed:** 2026-08-31T23:46:46Z
- **Tasks:** 2
- **Files modified:** 10 implementation/test/documentation files

## Accomplishments

- Added a genuine six-phase fixture that invokes the public `run --headless` command once, performs tiny real workspace writes, advances every durable task/phase through build and continue, and then compares status with the persisted final report.
- Proved visual, runtime-verification, and confirmed-lesson replan work all remain unresolved morning decisions without becoming the run's stop reason; score 60 plus a High Auditor finding and one unchanged ordinary blocker also do not stop the run.
- Made provider readiness observable exactly once per phase, shared by build and continue, including a phase that intentionally skips its Watcher.
- Preserved the direct-continue Iron Law while letting only the explicit run lane prove that existing baseline blockers did not grow or escalate; focused tests also pin new-count, new-escalation, and no-baseline failure.
- Replaced stale pause documentation with the exact 14-code typed catalogue, plain-English morning workflow, measured-only reporting contract, and owner-only seal boundary.
- Passed build, vet, 8,353 non-race tests, 8,353 race-enabled tests, and all command-catalog/parity/orphan ratchets without changing either immutable orphan baseline.

## Task Commits

Task 1 followed the required RED/GREEN sequence; Task 2 was committed after every final gate passed:

1. **Task 1 RED: define the failing six-phase stamina proof** - `017ce95c` (test)
2. **Task 1 GREEN: complete the integrated overnight runtime contract** - `87845ea6` (feat)
3. **Task 2: publish the canonical overnight contract** - `f22ef360` (docs)

**Plan metadata:** committed separately with this summary after self-check.

## Files Created/Modified

- `cmd/run_overnight_198_3_test.go` - Six-phase public-run fixture, real claims/UI write, owner-only checkpoint gap, measured/unreported spend assertions, report/status equality, blocker-boundary tests, and queueable mutation catalogue guard.
- `cmd/compatibility_cmds.go` - Passes only the live run-stage blocker baseline into continue and makes headless replan handling obey its typed disposition.
- `cmd/codex_continue.go` - Carries the internal baseline into gate evaluation, keeps direct calls strict, records truthful unchanged-blocker detail, and supplies phase/workflow identity on Watcher dispatches.
- `cmd/codex_build.go` - Performs one provider preflight over the completed phase-scoped build dispatch slice before lifecycle mutation or invocation.
- `.planning/decisions/autopilot-pause-conditions.md` - Marks the 2026-08-16 scanner rules as history and records the active 14-code disposition/evidence contract.
- `README.md` - Explains morning queues, genuine stops, normal endings, dry-run, status, exact decision-answer flags, replan review, and final seal in beginner-friendly language.
- `.aether/docs/PARITY_CLASSIC_VS_GO.md` - Replaces the obsolete broad pause golden anchor with the six-phase proof and focused catalogue tests.
- `cmd/TEST_MATRIX.md` - Adds joined-up run coverage and the narrow blocker/canonical-policy anchors.
- `colony/playbooks/continue.md` - Aligns Auditor, owner checkpoint, blocker-baseline, and final seal guidance while preserving advice-only Medic handling.

## Decisions Made

- Kept the blocker exception as an unpersisted field on the internal `codexContinueOptions`. No JSON, public flag, wrapper option, or generic direct-continue capability was added.
- Used `compareBlockerSnapshots` as the sole movement authority. Same-count ordinary churn remains diagnostic, while count growth or any new escalation still fails.
- Put build readiness at `executeCodexBuildDispatches`, after phase-scoped dispatch construction and before Queen/worktree lifecycle work, so it runs once rather than per worker.
- Kept the fixture on the real lifecycle path. Reconciliation is used only to retain genuine task evidence for the deliberately reviewer-free owner criterion; the test does not fabricate a reviewer verdict.
- Preserved the Plan 09 Medic wording: recovery remains runtime-owned and advice-only, with no automatic scan, repair, or worker dispatch.

## Deviations from Plan

### Authorized and Auto-fixed Issues

**1. [Rule 1 - Bug] Allowed a stable blocker baseline only inside the live run lane**

- **Found during:** Task 1 RED
- **Issue:** The public six-phase run stopped in phase 1 because its in-process continue call inherited direct continue's strict unresolved-blocker gate even though the live blocker snapshot stayed 1 ordinary blocker to 1 with no escalation.
- **Authorization:** The orchestrator expanded production ownership to `cmd/compatibility_cmds.go` and `cmd/codex_continue.go` with a narrow no-persistence/no-rewrite contract.
- **Fix:** Added an explicit internal baseline capability, applied the canonical snapshot comparison, emitted truthful unresolved follow-up detail, and left direct continue plus worse blocker movement strict.
- **Files modified:** `cmd/compatibility_cmds.go`, `cmd/codex_continue.go`, `cmd/run_overnight_198_3_test.go`
- **Verification:** Stable autopilot baseline passes; new count, new escalation, and direct no-baseline subtests fail the blocker gate; the live flag rows remain byte-for-field unchanged.
- **Committed in:** `87845ea6`

**2. [Rule 3 - Blocking] Wired phase-scoped readiness through the real build and continue dispatches**

- **Found during:** Task 1 GREEN integration run
- **Issue:** Build dispatch bypassed `preflightWorkerProvider`, while the planned continue Watcher omitted `Workflow` and `Phase`, so readiness appeared only through incidental phase-0 or reviewer paths and phase 4 recorded no probe when Watcher was intentionally skipped.
- **Authorization:** The orchestrator expanded ownership to `cmd/codex_build.go` for the exact post-construction/pre-lifecycle chokepoint.
- **Fix:** Preflighted the complete phase-scoped build slice once and added the current phase/workflow to the continue Watcher dispatch. Continue then reuses the build success while the next phase probes fresh.
- **Files modified:** `cmd/codex_build.go`, `cmd/codex_continue.go`, `cmd/run_overnight_198_3_test.go`
- **Verification:** The six-phase recorder observes exactly `{1:1, 2:1, 3:1, 4:1, 5:1, 6:1}`; 37 focused preflight tests and 9 focused build tests pass.
- **Committed in:** `87845ea6`

**3. [Rule 1 - Bug] Made headless replan enforcement consult the typed catalogue**

- **Found during:** Task 1 required mutation proof
- **Issue:** Temporarily changing `replan_due` from `queue_and_continue` to `stop` still let all six phases finish because the replan block branched directly on `opts.Headless` and bypassed `autopilotRunDecisionForCode`.
- **Fix:** Constructed the typed replan decision first and switched on its disposition. The active catalogue still queues headlessly and pauses interactively; terminal mutations now stop before phase six.
- **Files modified:** `cmd/compatibility_cmds.go`
- **Verification:** Replan mutation now stops after phase 2 with `replan_due`; restored policy completes six phases and retains one replan note.
- **Committed in:** `87845ea6`

---

**Total deviations:** 3 resolved (2 Rule 1 bugs, 1 Rule 3 blocker), all within explicitly authorized production ownership.

**Impact on plan:** The fixes are the minimum integrations required for the planned test to prove the existing Phase 198.3 contract. They add no public command, schema, provider retry, automatic recovery, or seal path.

## Mutation Proof

No safe production policy-override seam exists, so the plan's manual mutation discipline was used. Each `HeadlessDisposition` was changed to `stop` one at a time in `cmd/autopilot_policy.go`, the public six-phase test was run, and the file was restored before the next mutation and before commit:

- `visual_checkpoint_needed` - expected test failure; run stopped at the phase-2 visual checkpoint before six completions.
- `replan_due` - expected test failure; run stopped with `replan_due` after phase 2.
- `runtime_verification_needed` - expected test failure; run stopped at the phase-4 owner-only criterion.
- Restored catalogue - `TestOvernightRunCompletesSixPhases` passed with all six durable phases complete.

`git diff --exit-code -- cmd/autopilot_policy.go` passed after restoration; no mutation was committed.

## Verification

- RED: `rtk go test ./cmd/ -run 'TestOvernightRunCompletesSixPhases|TestOvernightRunQueueableMutationStopsEarly' -count=1` - failed for the intended stable-blocker integration gap before production changes.
- Required Task 1 command after GREEN: same command - **2 passed in 1 package**.
- Phase 198.3 focused gate: `rtk go test ./cmd/ -run 'OvernightRun|AutopilotPolicy|BlockerSnapshot|ReviewerArtifact|OwnerCheckpoint|PhaseScopedPreflight|LessonAwareReplan|AutopilotReport|GuidedInsert|SwarmThreeStrike' -count=1` - **50 passed in 1 package**.
- Catalog/parity/orphan gate: `rtk go test ./cmd/ -run 'TestAuditCatalogGolden|TestPlatformParityGolden|TestNoOrphanRuntimeCommands|TestOrphanAllowlistOnlyShrinks' -count=1` - **4 passed in 1 package**.
- Documentation/CLI truth gate - **55 passed in 1 package** (`CLIFlagAudit`, documented command resolution/severity, CLI alignment, lifecycle docs, preflight knobs, and version truth).
- Focused blocker/continue gate - **37 passed in 1 package**.
- Focused phase-scoped preflight gate - **37 passed in 1 package**.
- Focused build dispatch gate - **9 passed in 1 package**.
- `rtk go build ./cmd/aether` (underlying `go build ./cmd/aether`) - **passed**; the untracked build artifact was removed after verification.
- `rtk go vet ./...` (underlying `go vet ./...`) - **passed**.
- `rtk go test ./...` (underlying `go test ./...`) - **8,353 passed across 20 packages**.
- `rtk go test ./... -race` (underlying `go test ./... -race`) - **8,353 passed across 20 packages**, no race reports.
- `git diff --check dcd6bed7eab37f7c7d646592ac968953a6930db3..HEAD` - **passed**.
- Both immutable orphan baseline files - **byte-unchanged from the plan base**.

## Telemetry Honesty

- The deterministic fixture emitted one provider-format measured usage event with 1,200 input and 300 output tokens and asserted that the final report retained measured rows alongside unreported rows.
- No external provider billing, currency, or production-duration telemetry was measured. The summary makes no cost estimate; absent provider figures remain unreported by design.

## TDD Gate Compliance

- RED commit `017ce95c` precedes GREEN commit `87845ea6`.
- The RED test failed on the intended integrated behavior, not compilation or fixture setup.
- The GREEN commit includes the smallest authorized runtime integrations plus the assertions that lock their scope.

## Known Stubs

None. Added-line scans found no `TODO`, `FIXME`, placeholder, coming-soon, UI-flowing hardcoded-empty value, or unwired data source.

## Threat Surface Scan

No new network endpoint, authentication path, schema trust boundary, or externally writable file-access surface was introduced. The new baseline pointer is in-process only and is neither serialized nor persisted.

## Issues Encountered

- Raw JSON map string comparison made status/report equality order-sensitive; the fixture now decodes the status value into the typed `autopilotInvocationReport` and compares the frozen objects.
- The owner-only criterion initially retained machine claims that could accidentally satisfy it. The fixture removes only the current claims packet at the injected phase-4 boundary and uses the supported reconciliation path for task completion, leaving the criterion honestly unverified.
- No authentication gates, dependency installs, external services, or unrelated dirty-work conflicts occurred.

## User Setup Required

None - no external service configuration or migration is required.

## Next Phase Readiness

- Phase 198.3 now has a joined-up, mutation-sensitive stamina proof and canonical owner contract over the final 388-command runtime surface.
- The branch is ready for orchestrator integration and phase verification. No implementation, documentation, test, race, or command-surface blocker remains.

## Self-Check: PASSED

- All 10 implementation/test/documentation files and this summary exist.
- RED `017ce95c`, GREEN `87845ea6`, and documentation `f22ef360` resolve in the required order.
- Requirement IDs, exact test/build/vet/race evidence, immutable-baseline claims, and diff hygiene were rechecked before the summary commit.

---
*Phase: 198.3-overnight-stamina*
*Completed: 2026-09-01*
