---
phase: 198.3-overnight-stamina
verified: 2026-09-01T22:42:46Z
status: passed
score: "94/94 plan must-have truths verified"
roadmap_score: "6/6 success criteria verified"
requirements_score: "6/6 STAM requirements satisfied"
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "80/94"
  gaps_closed:
    - "Legacy same-text answers are now scoped to the active session and goal."
    - "Suggestion-only Critical rows now fail both direct and external continue lanes."
    - "Completed-phase replan cadence is reconstructed from durable plan state instead of a process-local counter."
    - "Run, continue advancement, status, and persisted reports now fail closed when canonical blocker truth is unavailable."
    - "External swarm result cardinality and identity checks are exact, and partial third-strike escalation writes repair idempotently."
    - "External swarm identifiers are contained and convention-validated before every finalizer mutation."
    - "External completions are bound to runtime-issued manifest digests; exact replay is read-only and altered replay is rejected."
    - "Seal now uses the canonical fail-closed reader for malformed, unreadable, directory-backed, and symlink-backed legacy flags."
    - "Legacy reviewer severities use the structured-artifact allowlist; invalid evidence blocks advancement."
    - "Visual owner work is derived only after persisted claims match the terminal claims embedded in the exact built attempt."
    - "Replan cadence filters durable decisions to the active session/goal before the existing active-revision projection."
    - "The complete normal and race Go suites now finish successfully instead of timing out inconclusively."
  gaps_remaining: []
  regressions: []
previous_gaps:
  - truth: "External swarm finalization validates identifier containment before any durable mutation."
    status: failed
    reason: "runSwarmFinalize passes caller-controlled swarm_id to initializeSwarmRun before the later saveSwarmResultRecord validation, allowing writes outside the intended swarms/<id> directory before rejection."
    artifacts:
      - path: "cmd/swarm_cmd.go"
        issue: "initializeSwarmRun joins the unvalidated ID into MkdirAll and SaveJSON paths."
      - path: "cmd/swarm_strikes.go"
        issue: "safeIdentifierSegment and durable ID validation occur only after earlier swarm/spawn/midden writes."
    missing:
      - "Validate swarm_id with both containment and durable-ID rules before beginRuntimeSpawnRun, initializeSwarmRun, midden writes, or spawn-tree writes."
      - "Add a zero-mutation traversal regression through the public swarm-finalize path."
  - truth: "External swarm evidence is bound to one runtime-issued manifest and cannot be altered or replayed to rewrite strike truth."
    status: failed
    reason: "swarm --plan-only persists no issuance record or digest, while swarm-finalize trusts the manifest embedded in the completion file and SaveJSON overwrites the same result path. Exact per-manifest result cardinality is preserved, but the manifest itself is self-asserted and an altered replay can replace durable outcome history."
    artifacts:
      - path: "cmd/swarm_cmd.go"
        issue: "runSwarmPlanOnly returns an unregistered manifest; runSwarmFinalize validates only its shape, root, and age."
      - path: "cmd/swarm_strikes.go"
        issue: "saveSwarmResultRecord overwrites swarms/<id>/result.json without an immutable completion digest or replay comparison."
    missing:
      - "Bind finalization to a runtime-issued manifest digest or one-time capability."
      - "Make exact replay idempotent and reject any different completion for an already finalized swarm ID before mutation."
  - truth: "Seal fails closed on malformed or unreadable current and legacy blocker storage."
    status: failed
    reason: "When pending-decisions.json is absent, checkSealBlockers loads flags.json but ignores every legacy load error. A malformed or non-regular legacy store therefore becomes an empty seal blocker set even though the canonical blocker reader correctly reports unavailable truth."
    artifacts:
      - path: "cmd/codex_workflow_cmds.go"
        issue: "The flags.json fallback handles only legacyErr == nil and drops all other errors."
      - path: "cmd/blocker_snapshot.go"
        issue: "The correct readCanonicalBlockerFlags fail-closed semantics are not used by checkSealBlockers."
    missing:
      - "Route seal through the canonical blocker reader or convert any non-ENOENT legacy error into the durability blocker."
      - "Test absent current plus malformed, unreadable, directory, and symlink legacy stores through the public seal boundary."
  - truth: "Every legacy reviewer finding has a valid normalized severity or blocks advancement as invalid evidence."
    status: failed
    reason: "normalizeContinueReviewEvidence validates title/description for legacy findings but never calls validReviewArtifactSeverity. A misspelled Critical severity is retained as an ordinary finding, creates no Critical trigger, and does not fail either advancement lane."
    artifacts:
      - path: "cmd/codex_continue_finalize.go"
        issue: "The no-artifact compatibility branch checks body text only; severity validation exists only in the structured artifact branch."
      - path: "cmd/codex_continue.go"
        issue: "The direct lane consumes the same normalized legacy result and therefore shares the bypass."
    missing:
      - "Normalize and validate legacy severities with the same allowlist used for artifacts.review."
      - "Add direct and external phase-advancement regressions for blank, typo, and mixed-case legacy severities."
  - truth: "A visual owner approval is bound to immutable terminal claims from the exact built attempt."
    status: failed
    reason: "The attempt record already embeds terminal Claims, but materializeVisualCheckpointFromBuildResult hashes whatever bytes currently occupy claims_path and never compares them with attempt.Claims. Editing or clearing that file after build changes or removes owner work without failing provenance validation."
    artifacts:
      - path: "cmd/autopilot_checkpoints.go"
        issue: "Current claims bytes are hashed and trusted after path/attempt checks, with no terminal-claims digest comparison."
      - path: "cmd/build_attempt.go"
        issue: "The immutable terminal Claims field exists but is unused by visual checkpoint validation."
      - path: "cmd/autopilot_checkpoints_test.go"
        issue: "TestVisualCheckpointWorkGenerationRequiresFreshApproval rewrites the claims file after the attempt and expects acceptance rather than tamper rejection."
    missing:
      - "Compare persisted claims with attempt.Claims or a digest committed in the built attempt before deriving checkpoint paths or generation."
      - "Reject post-build claims tampering before pending-decision mutation and add removal/invention adversarial cases."
  - truth: "Replan cadence considers only current session and goal decisions for the active plan revision."
    status: failed
    reason: "The run passes every pending decision into projectAutopilotReplanCadence, which filters only by type and deterministic plan revision ID. A stale cross-scope decision with the same revision and checkpoint phase can advance LastHandledBoundary and suppress the current colony's due replan."
    artifacts:
      - path: "cmd/compatibility_cmds.go"
        issue: "handleReplan passes loadPendingDecisionFile().Decisions without the standard scope filter."
      - path: "cmd/autopilot_lessons.go"
        issue: "Cadence projection matches plan revision only and does not call pendingDecisionMatchesScope."
    missing:
      - "Filter pending decisions with pendingDecisionScopeFromState before cadence projection."
      - "Add same-plan cross-session and cross-goal cadence regressions."
---

# Phase 198.3: Overnight Stamina — Final Re-verification

**Phase Goal:** Autopilot can be left alone for a night again. Restore the named stop contract, queue owner-eye work instead of halting, explain replan reasons, report honest elapsed/cost information, keep blocker gates live, and prove a simulated six-phase unattended run completes.

**Verified:** 2026-09-01T22:42:46Z  
**Status:** passed  
**Re-verification:** Yes — the six trust-boundary gaps from the 2026-09-01 report were repaired and the previously inconclusive full normal/race gates completed.

## Plain-English Verdict

Yes. The six-lap overnight test already worked; the locks around it now work too. A forged swarm path cannot write outside its directory, a made-up or altered swarm packet cannot replace history, broken legacy blocker/reviewer data stops instead of looking empty, changed UI evidence is rejected, and an old decision from another colony cannot suppress today's replan.

This verifies the simulated unattended runtime. It does not replace the later owner-watched real-project proof.

## Final Gap Closure

| Prior blocker | Final status | Production evidence | Adversarial proof |
|---|---|---|---|
| External swarm ID reached writers before validation | ✓ CLOSED | `runSwarmFinalize`, `initializeSwarmRun`, and result persistence share `validateDurableSwarmID` before spawn-tree, midden, path, or result writes. | Public traversal completion is rejected with a byte-identical workspace snapshot. |
| Swarm manifest was self-asserted/replayable | ✓ CLOSED | Plan-only persists `issuance.json` with the canonical manifest digest; finalization reserves and freezes one completion digest/outcome. | Unissued and altered manifests fail without mutation; exact replay is read-only; changed replay fails without mutation. |
| Seal ignored malformed legacy blocker storage | ✓ CLOSED | `checkSealBlockers` now uses `readCanonicalBlockerFlags`; `loadBlockerFlagsPath` rejects symlinks via `Lstat`. | Malformed, unreadable, directory-backed, and symlink-backed `flags.json` cases all return public exit 1 without completing the colony. |
| Legacy reviewer severity was unchecked | ✓ CLOSED | The compatibility branch uppercases and validates the same `CRITICAL/HIGH/MEDIUM/LOW/INFO` allowlist as `artifacts.review`. | Blank/typo rows become blocking evidence errors; mixed-case Critical blocks and mixed-case High remains report-only. |
| Visual checkpoint trusted mutable claims bytes | ✓ CLOSED | Persisted claims must canonically match `buildAttemptRecord.Claims`; checkpoint paths and generation digest then derive from those terminal claims. | Post-build removal, invention, and replacement are rejected before pending-decision mutation. |
| Replan cadence consumed stale scope | ✓ CLOSED | Live and dry-run paths filter with `pendingDecisionScopeFromState` before active-revision cadence projection. | Same-revision decisions from another session or goal cannot suppress the due boundary and no build dispatch occurs first. |

Locked D-06 behavior is unchanged: same-count ordinary blocker replacement remains diagnostic. The rejected CR-03 review suggestion was deliberately not implemented.

## Goal and Requirement Scores

| Measure | Result | Why |
|---|---:|---|
| Roadmap success criteria | **6/6** | Queueing, named stops, scoped lesson replans, elapsed/cost truth, live blocker gates, and the six-phase unattended fixture all pass. |
| STAM requirements | **6/6** | STAM-01 through STAM-06 have live source paths and passing public/integration evidence. |
| Plan must-have truths | **94/94** | The prior 80 verified truths were preserved; the 13 failed truths closed with the six repairs; the one uncertain aggregate-gate truth is now proven. |

## Verification Evidence

| Gate | Result |
|---|---|
| New swarm issuance/containment/replay regressions | 29 passed |
| New seal and legacy-reviewer regressions | 27 passed |
| New visual-provenance and replan-scope regressions | 8 passed |
| Broader affected subsystem suite | 282 passed |
| Phase integration, catalogue, parity, orphan, and storage selection | 123 passed across 2 packages |
| `go vet ./...` | exit 0 |
| `go build ./...` | exit 0 |
| TypeScript host typecheck | exit 0 |
| TypeScript host suite | 540 passed |
| `go test ./... -count=1` | **8,566 passed across 20 packages** |
| `go test -race ./... -count=1` | **8,566 passed across 20 packages; no race report** |
| `git diff --check` | exit 0 |

The complete Go runs were allowed to finish beyond the old 300-second cap. They are actual green exits, not silence interpreted as success.

## Remaining Non-blocking Debt

- Phase 198.2 WIRE-06 real-world UAT remains acknowledged debt and can be revisited with `$gsd-verify-work 198.2`.
- WR-02 checkpoint capability-hash growth and WR-03 missing Critical detail in the morning report remain documented warnings; neither violates a Phase 198.3 must-have.
- Real owner-watched overnight confidence belongs after Classic Capability Reconciliation and the Classic Colony Restoration milestone, before the deferred proof/showdown.

## Final Decision

Phase 198.3 is verified complete. Phase 198.4 may begin as **Classic Capability Reconciliation**; irreversible pruning remains forbidden until every historical capability has an explicit owner disposition.

<details>
<summary>Previous 2026-09-01 gaps-found verification snapshot</summary>

# Phase 198.3: Overnight Stamina Verification Report

**Phase Goal:** Autopilot can be left alone for a night again. Restore the named stop contract, queue owner-eye work instead of halting, explain replan reasons, report honest elapsed/cost information, keep blocker gates live, and prove a simulated six-phase unattended run completes.

**Verified:** 2026-09-01T21:11:24Z  
**Status:** gaps_found  
**Re-verification:** Yes — all seven prior regressions and plans 17–22 were rechecked against the merged checkout.

## Plain-English Verdict

No: the demonstrated overnight path works, but the safety envelope around it does not yet justify leaving Aether alone.

For a non-specialist: the car completed the six-lap test drive, its dashboard reports time and fuel honestly, and ordinary warning lights behave correctly. Several locks around the test, however, still accept tampered paperwork: an external swarm can choose an unsafe storage path, its manifest can be replayed or rewritten, seal can mistake broken legacy blocker data for no blockers, and visual/reviewer/replan evidence can be stale or malformed without stopping. Those are release blockers, not cosmetic warnings.

## Re-verification of the Previous Seven Gaps

| Previous regression | Status | Live evidence |
|---|---|---|
| Checkpoint generation was not bound to work | ⚠ PARTIAL | Plan 17 adds attempt/execution/generation identities and runtime parity, but visual evidence is still read from mutable `claims_path` bytes without comparison to `buildAttemptRecord.Claims`. |
| Legacy answered questions were unscoped | ✓ CLOSED | `answeredDecisionTexts` and owner-confirmation compatibility now use current session/goal scope; focused cross-scope tests pass. |
| Suggestion-only Critical findings were skipped | ✓ CLOSED | Both lanes reject empty title/description before advancement; Plan 19 matrices pass. |
| Replan cadence was process-local | ⚠ PARTIAL | Cadence is reconstructed durably across bounded runs, but stale cross-scope decisions can falsely mark a boundary handled. |
| Unreadable blocker storage became zero | ✓ CLOSED for run/status/continue | The canonical reader returns availability errors, and run/status/report/continue fail closed. Seal still bypasses that reader for malformed legacy `flags.json`. |
| External swarm results were non-terminal or identity-unbound | ⚠ PARTIAL | Plan 21 preserves exact result count, terminal status, and manifest-relative identity with zero mutation on listed rejections. The manifest itself remains self-asserted/replayable, and its ID is used before containment validation. |
| Partial third-strike persistence wedged recovery | ✓ CLOSED | The same-target retry repairs one stable escalation idempotently without a fourth dispatch; focused public and storage tests pass. |

## Goal Achievement

### Roadmap Success Criteria

| # | Roadmap truth | Status | Evidence |
|---|---|---|---|
| 1 | Headless visual/human work queues and only genuine blockers halt. | ✗ FAILED (BLOCKER) | Valid visual/runtime work queues and the six-phase fixture advances, but mutable visual claims can remove or invent owner work and invalid legacy reviewer evidence can evade a genuine stop. |
| 2 | Named stop contract and dry-run enumeration are complete. | ✗ FAILED (BLOCKER) | The canonical 14-trigger catalogue, score floor, valid Critical handling, runtime checkpoint, escalation handling, and exact dry-run projection exist. The legacy finding compatibility branch does not validate severity, so malformed current reviewer evidence can bypass the named Critical/invalid-evidence stop boundary. |
| 3 | Replan pause reports lessons since the last plan. | ✗ FAILED (BLOCKER) | Revision-scoped lesson selection and durable cadence work normally, but cadence consumes stale decisions from other goal/session scopes and can suppress the current due note. |
| 4 | Run summary reports elapsed wall clock and honest cost. | ✓ VERIFIED | `autopilotInvocationReport` uses invocation start/finish, measured ledger rows only, null/unreported telemetry, and status renders the frozen report. |
| 5 | Pre/post blocker gate is live in run and status. | ✓ VERIFIED | `readBlockerSnapshot` is error-aware and shared by diagnostic, run, report, and status; pre/post failure paths stop as `colony_not_runnable`. Same-count ordinary replacement remains diagnostic by locked D-06. |
| 6 | At least six simulated phases complete under one run and fail on a queueable stop mutation. | ✓ VERIFIED | The independently run `TestOvernightRun...` suite passed all 7 tests/subtests through the real run loop, including the queueable-disposition mutation guard. |

**Roadmap score:** 3/6

### Plan Must-Have Truths

All 94 truths from the 22 PLAN frontmatters are resolved here. A failed truth is a blocker; the one uncertain gate claim is not counted as verified.

| ID | Must-have truth (abridged) | Status | Evidence |
|---|---|---|---|
| P01.1 | One typed catalogue owns genuine, queueable, and normal stops; dry-run projects it. | ✓ VERIFIED | One `autopilotTriggerSpecs` catalogue drives lookup and rendering. |
| P01.2 | Ordinary blockers are shown; only count growth or escalation is unsafe. | ✓ VERIFIED | Error-aware snapshots compare count/escalation; same-count churn is intentionally diagnostic under D-06. |
| P01.3 | Flag check, run, and status share one blocker reader. | ✓ VERIFIED | All public consumers use `readBlockerSnapshot` or its evidence projection. |
| P02.1 | Completed Auditor scores are validated; 59 stops, 60 passes, absence is no signal. | ✓ VERIFIED | Structured artifact boundary tests pass on both lanes. |
| P02.2 | Valid Critical findings stop; valid High findings remain report-only. | ✓ VERIFIED | Typed signals and boundary tests pass for valid evidence. |
| P02.3 | Owner checkpoints deduplicate; headless queues, interactive pauses, seal waits. | ✓ VERIFIED | The durable row/capability/seal lifecycle is wired for valid evidence generations. |
| P02.4 | Final phase advances without sealing; unresolved owner work remains enforced at seal. | ✗ FAILED | Final advancement works, but mutable visual claims and malformed legacy seal storage can suppress required owner/blocker work. |
| P03.1 | Readiness tries at most twice and retries timeout only. | ✓ VERIFIED | Provider probe policy is bounded and error-class aware. |
| P03.2 | Readiness success is shared within a phase and rechecked next phase. | ✓ VERIFIED | Phase-scoped cache and recorder tests pass. |
| P03.3 | Go and TypeScript honor AETHER_PREFLIGHT_TIMEOUT with a 45s default. | ✓ VERIFIED | Go/TS resolvers, typecheck, and 540 host tests pass. |
| P03.4 | Readiness failure never switches provider or skips phase. | ✓ VERIFIED | Failure returns a named stop; no fallback/skip branch exists. |
| P04.1 | Current policy supersedes queue-then-stop and restores optional-Auditor floor. | ✓ VERIFIED | Valid queue signals advance and the score floor is live. |
| P04.2 | Replan requires cadence plus unique confirmed post-revision lessons. | ✗ FAILED | Lesson selection is correct, but stale cross-scope cadence decisions can suppress the boundary. |
| P04.3 | Headless replan upserts one note and continues; interactive pauses with metadata. | ✓ VERIFIED | Current-scope upsert persists revision, count, references, and first/latest phase. |
| P04.4 | Live run enforces typed owner/genuine/normal dispositions. | ✗ FAILED | A malformed legacy reviewer severity is neither rejected nor promoted to a stop. |
| P04.5 | Run does not retry whole builds; readiness owns bounded retry. | ✓ VERIFIED | One build occurs per phase transition; retry lives in preflight. |
| P05.1 | Every run ending persists one report and status renders that record. | ✓ VERIFIED | Terminal paths share `finishAutopilotInvocation`; persistence failure is non-zero. |
| P05.2 | Elapsed is this invocation's wall clock. | ✓ VERIFIED | Frozen start/finish excludes offline time and does not sum worker durations. |
| P05.3 | Cost uses measured ledgers only and exposes unreported telemetry. | ✓ VERIFIED | No estimate or currency field enters the report. |
| P05.4 | High findings, queued decisions, blockers, outcomes, and exact next survive. | ✓ VERIFIED | All declared fields persist and render; Critical detail omission is WR-03, outside this wording. |
| P05.5 | Recovery is internal and genuine-stop-only; Medic is not routinely spawned. | ✓ VERIFIED | Report records bounded advice rather than dispatching routine workers. |
| P06.1 | Guided insert-phase accepts one issue sentence. | ✓ VERIFIED | The Go resolver synthesizes the request; public tests pass. |
| P06.2 | Explicit after/name/description automation remains compatible. | ✓ VERIFIED | Explicit compatibility tests pass. |
| P06.3 | Empty non-TTY input is non-mutating; TTY gathers required detail. | ✓ VERIFIED | State snapshots and interactive resolver tests pass. |
| P06.4 | Go owns input; Claude/OpenCode wrappers are aligned guidance. | ✓ VERIFIED | Wrapper and command-guide parity gates pass. |
| P07.1 | Three same-target failures create one escalated blocker. | ✗ FAILED | Normal/partial-write paths work, but replayable self-asserted external results can replace strike truth. |
| P07.2 | Fourth same target refuses with guided insert; success resets streak. | ✓ VERIFIED | Supported guarded/refusal/reset flow passes. |
| P07.3 | Direct and external swarms share trustworthy strike evaluation. | ✗ FAILED | Both call the helper, but external manifest provenance and ID containment are unsafe before the helper. |
| P07.4 | Third strike reaches run/status through the blocker snapshot. | ✓ VERIFIED | Valid escalation and repaired partial write flow into the shared reader. |
| P08.1 | Seven superseded autopilot commands/scanner are removed after replacement. | ✓ VERIFIED | Catalogue and reachability ratchets prove removal/no live caller. |
| P08.2 | Useful autopilot state/report types remain. | ✓ VERIFIED | Run/status state compatibility remains live. |
| P08.3 | Supported lifecycle is run plus status. | ✓ VERIFIED | Runtime surface and docs agree. |
| P08.4 | First fixture shrink is exact. | ✓ VERIFIED | Command fixture/catalog tests pass. |
| P09.1 | Four recovery/Medic adapters are removed after internal wiring. | ✓ VERIFIED | Caller audit and runtime catalogue pass. |
| P09.2 | Classifier/log/budget/Medic functions remain live internally. | ✓ VERIFIED | Internal callers and focused tests remain. |
| P09.3 | Second shrink leaves inventory eleven lower. | ✓ VERIFIED | Generated count ratchets pass. |
| P09.4 | Generated diffs remove only audited names. | ✓ VERIFIED | Catalog/parity/regression tests pass. |
| P09.5 | Historical orphan baseline is unchanged; live allowlists shrink. | ✓ VERIFIED | Orphan ratchets pass. |
| P10.1 | One real run completes at least six simulated phases with queued owner work. | ✓ VERIFIED | Real run-loop suite passes. |
| P10.2 | Fixture includes blocker baseline, High finding, readiness, report, next, and no seal. | ✓ VERIFIED | Integrated assertions pass. |
| P10.3 | Queueable-to-terminal mutation fails before phase six. | ✓ VERIFIED | Mutation guard passes. |
| P10.4 | Owner docs accurately describe stop/report/seal behavior. | ✗ FAILED | Immutable-owner and fail-closed-seal claims overstate the mutable-claims and legacy-store paths. |
| P10.5 | Build/vet/test/race/catalog/parity/orphan gates are clean. | ? UNCERTAIN (WARNING) | Build, vet, focused, catalog, and TS gates are green. A fresh `go test ./...` emitted no failure but exceeded 300s and was terminated; no complete post-fix race sweep exists. |
| P11.1 | Checkpoint answer needs a capability for the exact row. | ✓ VERIFIED | Resolver binds generation row, question, phase, scope, and hash. |
| P11.2 | Capability is single-use; missing/wrong/cross/replay do not mutate. | ✓ VERIFIED | Adversarial capability tests pass. |
| P11.3 | Generic resolvers cannot resolve protected checkpoints. | ✓ VERIFIED | Protected questions route only to the capability-aware resolver. |
| P11.4 | Identity survives rotation; hashes persist and raw tokens are transient. | ✓ VERIFIED | Serialization/redaction and rotation tests pass. |
| P12.1 | Seal refuses corrupt/unreadable owner-state storage or failed capability issue. | ✗ FAILED | Current pending-decision errors fail closed, but absent current plus malformed legacy `flags.json` is ignored. |
| P12.2 | Only a genuinely absent pending-decision file means no queued checkpoints. | ✓ VERIFIED | Checkpoint loader distinguishes absence from current-file errors. |
| P12.3 | Each unresolved seal checkpoint has one fresh capability command and deduped legacy projection. | ✓ VERIFIED | Generation-compatible seal projection tests pass for valid stores. |
| P12.4 | Malformed storage cannot hide behind legacy flags fallback or empty slice. | ✗ FAILED | `checkSealBlockers` silently drops a malformed legacy fallback load. |
| P12.5 | Visual seal refusal is stderr/exit 1 and raw capability is immediate-only. | ✓ VERIFIED | Output/durability scans pass for surfaced checkpoint failures. |
| P13.1 | Completed Auditor supplies structured integer score 0–100. | ✓ VERIFIED | Required artifact/score checks pass. |
| P13.2 | Missing/empty/malformed artifact, invalid score/severity fails review. | ✓ VERIFIED | The `artifacts.review` negative matrix passes; CR-05 concerns the separate legacy branch. |
| P13.3 | Direct and external artifact lanes share fail-closed predicate. | ✓ VERIFIED | Both call the same normalizer and consume evidence errors. |
| P13.4 | Score 60 passes; High reports; valid Critical blocks. | ✓ VERIFIED | Exact boundary tests pass. |
| P14.1 | Unsavable terminal report returns non-zero in JSON and visual modes. | ✓ VERIFIED | Fault-injection tests pass. |
| P14.2 | Visual durability failure is error-first and labels unsaved evidence. | ✓ VERIFIED | Ordered rendering assertion passes. |
| P14.3 | Dry-run replan shares live lesson predicate/disposition. | ✓ VERIFIED | Both use `evaluateAutopilotReplan`. |
| P14.4 | No lessons means no due; headless preview queues without writes. | ✓ VERIFIED | Dry-run parity/non-mutation tests pass. |
| P15.1 | Direct build probes read-only state before lifecycle/artifact mutation. | ✓ VERIFIED | Source order and failure snapshots prove it. |
| P15.2 | Failed preflight is byte-identical across repair/force fixtures. | ✓ VERIFIED | Five-case matrix passes. |
| P15.3 | Late dispatch defense remains and shares phase cache. | ✓ VERIFIED | Source/call-count tests pass. |
| P15.4 | One build performs one real probe per phase trust window. | ✓ VERIFIED | Cache-count regression passes. |
| P16.1 | Three failures refuse a fourth exact target. | ✓ VERIFIED | Public guard test passes. |
| P16.2 | Emitted corrective command creates verified target recovery. | ✓ VERIFIED | Plan-bound recovery epoch and partial-write repair pass. |
| P16.3 | Verified recovery permits exact target and resolves stale escalation before dispatch. | ✓ VERIFIED | End-to-end reconciliation passes. |
| P16.4 | Malformed/spoofed/cross-target/uncommitted recovery cannot reset strikes. | ✓ VERIFIED | Recovery-record matrix passes. |
| P17.1 | Approval applies only to the exact build/verification generation. | ✗ FAILED | Runtime generation is bound; visual generation trusts mutable current claims rather than the attempt's terminal claims. |
| P17.2 | Identical immutable evidence deduplicates; changed work opens fresh owner work. | ✗ FAILED | Row behavior works, but the accepted visual evidence is not immutable or authenticated to the built attempt. |
| P17.3 | Generation rows retain stable compatibility identity for seal dedupe. | ✓ VERIFIED | Distinct generation IDs and compatibility projection are wired. |
| P17.4 | Direct and external continue bind runtime checkpoints before advancement. | ✓ VERIFIED | Both lanes call validated runtime generation/materialization. |
| P18.1 | Legacy same-text answer works only in active session/goal. | ✓ VERIFIED | Scoped lookup and regression pass. |
| P18.2 | Session/goal change makes owner work unresolved again. | ✓ VERIFIED | Cross-scope owner confirmation tests pass. |
| P18.3 | Current-scope compatibility does not bypass protected capability. | ✓ VERIFIED | Resolver reservation remains intact. |
| P19.1 | Every Critical row has title or description before use. | ✓ VERIFIED | Body validation covers artifact and legacy rows. |
| P19.2 | Suggestion-only Critical blocks both lanes as invalid evidence. | ✓ VERIFIED | Direct/external advancement tests pass. |
| P19.3 | Valid Critical blocks and valid High remains report-only. | ✓ VERIFIED | Boundary tests pass. |
| P20.1 | Completed-phase cadence survives bounded/interrupted run invocations. | ✗ FAILED | Reconstruction survives restart, but unfiltered stale-scope decisions can falsify its handled boundary. |
| P20.2 | A missed boundary is reconstructed before another build. | ✓ VERIFIED | Pre-dispatch replan check and two-invocation test pass. |
| P20.3 | Cadence resets only on accepted plan revision. | ✓ VERIFIED | Restart, resolution, max-phases, and revision reset cases pass within current scope. |
| P20.4 | Replan requires boundary plus unique confirmed post-revision lesson. | ✓ VERIFIED | Selector/predicate matrices pass. |
| P21.1 | External finalizer accepts exactly one terminal result per manifest dispatch. | ✓ VERIFIED | Count, duplicate, missing, surplus, and terminal allowlist checks remain live after the fixture fix. |
| P21.2 | Result payload cannot replace manifest name/caste/role/task. | ✓ VERIFIED | Nonblank mismatches reject; merged output takes manifest identity. |
| P21.3 | Listed rejected result evidence causes zero durable mutation. | ✓ VERIFIED | Validation completes before the mutation boundary for duplicate/surplus/forged/missing/non-terminal result cases. |
| P21.4 | Partial third-strike write repairs one stable escalation. | ✓ VERIFIED | Storage and public-flow tests pass. |
| P21.5 | Escalation repair is idempotent without a fourth strike/dispatch. | ✓ VERIFIED | Replay tests pass. |
| P22.1 | Absence of current and legacy blocker stores is available empty. | ✓ VERIFIED | Canonical reader returns an explicit available zero snapshot. |
| P22.2 | Malformed/unreadable current or legacy blocker storage is unavailable, never zero. | ✗ FAILED | True for run/status/continue reader; false for seal's separate legacy fallback. |
| P22.3 | Run fails closed at baseline and post-stage blocker reads. | ✓ VERIFIED | Focused failure-path tests pass. |
| P22.4 | Continue advancement reads blocker truth unconditionally. | ✓ VERIFIED | Advancement gate blocks unavailable truth. |
| P22.5 | Status labels blocker truth unavailable. | ✓ VERIFIED | JSON/visual cases pass. |
| P22.6 | Morning report explicitly represents unavailable blocker truth. | ✓ VERIFIED | Report schema/render tests pass. |
| P22.7 | Six repairs preserve the six-phase run and all repository gates pass. | ✗ FAILED | Six-phase/focused gates pass, but visual provenance and seal legacy handling remain broken; aggregate normal/race proof is also incomplete. |

**Plan truth score:** 80/94

| Plan | Verified | Failed | Uncertain | Total |
|---|---:|---:|---:|---:|
| 01 | 3 | 0 | 0 | 3 |
| 02 | 3 | 1 | 0 | 4 |
| 03 | 4 | 0 | 0 | 4 |
| 04 | 3 | 2 | 0 | 5 |
| 05 | 5 | 0 | 0 | 5 |
| 06 | 4 | 0 | 0 | 4 |
| 07 | 2 | 2 | 0 | 4 |
| 08 | 4 | 0 | 0 | 4 |
| 09 | 5 | 0 | 0 | 5 |
| 10 | 3 | 1 | 1 | 5 |
| 11 | 4 | 0 | 0 | 4 |
| 12 | 3 | 2 | 0 | 5 |
| 13 | 4 | 0 | 0 | 4 |
| 14 | 4 | 0 | 0 | 4 |
| 15 | 4 | 0 | 0 | 4 |
| 16 | 4 | 0 | 0 | 4 |
| 17 | 2 | 2 | 0 | 4 |
| 18 | 3 | 0 | 0 | 3 |
| 19 | 3 | 0 | 0 | 3 |
| 20 | 3 | 1 | 0 | 4 |
| 21 | 5 | 0 | 0 | 5 |
| 22 | 5 | 2 | 0 | 7 |

## Required Artifacts

All 52 concrete path artifacts declared across the PLAN frontmatters exist. The 36 older descriptive artifact groups were mapped manually to their live files; existence was not treated as behavior.

| Plan/group | Exists and substantive | Wired/data flowing | Final status |
|---|---|---|---|
| 01 trigger policy + blocker snapshot | Yes | Run/status/diagnostic flow correctly | ✓ VERIFIED |
| 02 review + checkpoint lifecycle | Yes | Valid artifacts flow; legacy severity and visual provenance have gaps | ⚠ PARTIAL |
| 03 provider preflight | Yes | Provider → bounded probe/cache → build/continue | ✓ VERIFIED |
| 04 run policy + replan | Yes | Live run wired; cadence reads unscoped decisions | ⚠ PARTIAL |
| 05 frozen invocation report | Yes | Clock/ledgers/signals → report → status | ✓ VERIFIED |
| 06 guided insert | Yes | Public input → Go resolver → plan mutation | ✓ VERIFIED |
| 07 swarm escalation | Yes | Direct flow works; external trust boundary does not | ⚠ PARTIAL |
| 08–09 surface reductions | Yes | Needed internal state/recovery callers remain | ✓ VERIFIED |
| 10 overnight fixture/docs | Yes | Real run loop passes; docs overstate unsafe edges | ⚠ PARTIAL |
| 11 checkpoint capabilities | Yes | Exact-row capability path works | ✓ VERIFIED |
| 12 seal checkpoint safety | Yes | Current store errors flow; malformed legacy fallback is dropped | ⚠ PARTIAL |
| 13 reviewer artifact predicate | Yes | Artifact lanes fail closed; legacy branch has CR-05 | ⚠ PARTIAL |
| 14 report durability/dry-run | Yes | Shared finalizer/evaluator paths work | ✓ VERIFIED |
| 15 earliest preflight | Yes | Read-only prepare → probe → mutation | ✓ VERIFIED |
| 16 recovery epoch | Yes | Verified target recovery and repair flow | ✓ VERIFIED |
| 17 generation identity | Yes | Runtime flowing; visual source is mutable | ⚠ HOLLOW VISUAL SOURCE |
| 18 legacy answer scoping | Yes | Session/goal filters reach both compatibility consumers | ✓ VERIFIED |
| 19 finding body validation | Yes | Both advancement lanes wired | ✓ VERIFIED |
| 20 durable cadence | Yes | Revision reconstruction works; scope filter missing | ⚠ PARTIAL |
| 21 exact swarm results/repair | Yes | Cardinality/identity/terminal and repair tests pass | ✓ VERIFIED |
| 22 blocker availability | Yes | Run/status/report/continue work; seal diverges | ⚠ PARTIAL |

Structured SDK checks: plans 11–21 passed all 45/45 path artifact assertions; Plan 22 passed 6/7 because the SDK searched `cmd/compatibility_cmds.go` for the literal `colony_not_runnable`, while production references the typed `autopilotTriggerColonyNotRunnable` constant there and defines the literal in `cmd/autopilot_policy.go`. That tooling mismatch was manually verified, not counted as a code gap. SDK link checks passed 31/39; the eight failures in plans 14–16 use function symbols where the SDK expects file paths and were inspected manually.

## Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| Trigger catalogue | Dry-run/live run/report | Typed code lookup | ✓ WIRED | One source of disposition truth. |
| Canonical blocker reader | Run/status/report/continue | Error-aware snapshot/evidence | ✓ WIRED | Unavailable truth stops or renders unknown. |
| Seal | Legacy blocker fallback | `checkSealBlockers` direct LoadJSON | ✗ NOT WIRED | Bypasses canonical error handling. |
| `artifacts.review` | Both advancement lanes | Shared normalizer/evidence errors | ✓ WIRED | Structured path is fail closed. |
| Legacy findings | Both advancement lanes | Same normalizer compatibility branch | ⚠ PARTIAL | Body checked; severity not checked. |
| Built attempt | Visual checkpoint | Attempt path/binding plus current claims file | ⚠ HOLLOW | Current claims are not authenticated to terminal `attempt.Claims`. |
| Plan revision/lessons | Replan decision | Durable cadence + confirmed selector | ⚠ PARTIAL | Decision input is not current-scope filtered. |
| Invocation clock/ledgers | Last report/status | One terminal finalizer | ✓ WIRED | Elapsed/spend are honest and frozen. |
| Swarm manifest | Finalizer/result history | Completion-embedded manifest | ✗ NOT TRUST-BOUND | Shape is checked, issuance and replay are not. |
| Manifest dispatches | External result records | Exact cardinality/identity/terminal validator | ✓ WIRED | Plan 21 contract remains intact. |
| Six-phase fixture | Real run loop | Simulated build/continue callbacks | ✓ WIRED | Not a pure-policy shortcut and never seals. |

## Data-Flow Trace (Level 4)

| Artifact | Dynamic data | Source | Produces trustworthy data | Status |
|---|---|---|---|---|
| Visual checkpoint | UI paths + generation digest | Mutable persisted claims file | No; not compared to terminal attempt claims | ✗ HOLLOW PROVENANCE |
| Runtime checkpoint | Criterion state/evidence + execution binding | Validated continue manifest/attempt | Yes | ✓ FLOWING |
| Reviewer policy | Score/findings | Structured artifact or legacy fields | Structured yes; legacy severity no | ⚠ PARTIAL |
| Replan note | Revision, cadence, lesson references | Plan state, learning entries, pending decisions | Lessons yes; cadence can ingest stale scope | ⚠ PARTIAL |
| Blocker run/status | Counts, IDs, escalations, availability | Canonical current/legacy store | Yes | ✓ FLOWING |
| Seal blockers | Flags + owner checkpoints | Separate current/legacy load plus checkpoint store | Malformed legacy error is lost | ✗ HOLLOW ERROR PATH |
| Morning report | Elapsed, spend, phases, decisions, blockers, next | Invocation clock, ledgers, live signals | Yes; Critical detail warning noted below | ✓ FLOWING |
| Swarm strikes | Terminal outcomes and target history | Caller-provided manifest/results | Exact relative result shape, untrusted manifest/replay | ✗ HOLLOW TRUST |
| Overnight proof | Six state transitions + final report | Simulated invoker through live run | Yes for the simulated scenario | ✓ FLOWING |

## Code Review Finding Adjudication

Every finding in `198.3-REVIEW.md` was independently traced through its live caller chain.

| Finding | Verdict | Classification | Evidence |
|---|---|---|---|
| CR-01 external `swarm_id` traversal | CONFIRMED | 🛑 BLOCKER | `runSwarmFinalize` calls `initializeSwarmRun` at `cmd/swarm_cmd.go:688-700`; containment validation occurs later in `cmd/swarm_strikes.go:126-130`. |
| CR-02 self-asserted/replayable swarm manifest | CONFIRMED | 🛑 BLOCKER | Plan-only returns without an issuance record (`cmd/swarm_cmd.go:393-416`); finalizer trusts the embedded manifest (`:652-668`); result SaveJSON overwrites the same ID path. Plan 21 cardinality/identity checks remain intact but are relative to that untrusted manifest. |
| CR-03 same-count blocker replacement | REJECTED | ℹ INTENTIONAL CONTRACT | Locked D-06 says stop on count rise or escalation; Research line 184 explicitly says same-count ID replacement must not become a different policy; `compareBlockerSnapshots` implements exactly that. |
| CR-04 malformed legacy seal store | CONFIRMED | 🛑 BLOCKER | `checkSealBlockers` handles `legacyErr == nil` only and ignores every other legacy error at `cmd/codex_workflow_cmds.go:1331-1342`. |
| CR-05 legacy severity validation | CONFIRMED | 🛑 BLOCKER | Legacy branch at `cmd/codex_continue_finalize.go:819-833` checks body only; the structured branch validates severity at `:885-900`. Both production lanes call the normalizer. |
| CR-06 visual evidence not bound to attempt | CONFIRMED | 🛑 BLOCKER | Production hashes current file bytes at `cmd/autopilot_checkpoints.go:403-432`; attempt embeds terminal claims at `cmd/build_attempt.go:161`; the test rewrites the file and expects acceptance at `cmd/autopilot_checkpoints_test.go:317-329`. |
| WR-01 replan consumes stale scope | CONFIRMED | 🛑 GOAL-BLOCKING | Run passes all decisions at `cmd/compatibility_cmds.go:744`; projection filters only revision/type at `cmd/autopilot_lessons.go:229-245`. This can suppress STAM-03, so verification upgrades it from code-review warning to phase blocker. |
| WR-02 checkpoint capability hash growth | CONFIRMED, INTENTIONAL | ⚠ WARNING | Hashes append and all remain valid (`cmd/autopilot_checkpoints.go:696-735`), but Plan 11 line 94 explicitly requires previously displayed commands to remain valid. Lack of cap/TTL is real debt, not a current must-have failure. |
| WR-03 morning report omits Critical detail | CONFIRMED | ⚠ WARNING | Phase reports retain only `HighFindings` (`cmd/autopilot_report.go:31-36`, `:190-199`). The stop code/recovery evidence remains, but file/line/fix detail is absent. STAM-04 requires elapsed/cost honesty, so this does not independently fail that requirement. |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Final gap-closure integration | `rtk go test ./cmd -run 'Test(ContinueAdvancementFailsClosedWhenBlockerTruthUnavailable|RunFailsClosedWhenBlockerTruth|AutopilotReportUnavailableBlockerTruthIsNeverZero|OvernightRun|VisualCheckpointWorkGeneration|RuntimeCheckpointWorkGeneration|LegacyAnsweredDecisionText|OwnerConfirmationAnsweredRejectsCrossScope|SuggestionOnlyCritical|AutopilotReplanCadence|RunReplanCadence|SwarmFinalizeRejectsUnbound|SwarmThirdStrikeRepairs|SwarmPartialEscalation)' -count=1` | 56 passed | ✓ PASS |
| Plans 17–21 focused contracts | Combined declared Plan 17–21 regex | 132 passed | ✓ PASS |
| Canonical blocker/status/diagnostic | `rtk go test ./cmd -run 'Test(BlockerSnapshot|Status.*Blocker|FlagCheckBlockers)' -count=1` | 27 passed | ✓ PASS |
| Continue/report unavailable blocker truth | Focused Plan 22 three-test regex | 3 passed | ✓ PASS |
| Six-phase run and mutation guard | `rtk go test ./cmd -run 'TestOvernightRun(CompletesSixPhases|BlockerBaselineExceptionIsNarrow|QueueableMutationStopsEarly)$' -count=1` | 7 passed | ✓ PASS |
| Repaired external-swarm wrapper fixture | `rtk go test ./cmd -run '^TestSwarmWorkerFailureReachesTheFailureLogOnBothLanes$' -count=1` | 3 passed | ✓ PASS |
| macOS storage-path regression | `rtk go test ./pkg/storage -run '^TestResolveAetherRoot_GitFallback$' -count=1` | 1 passed | ✓ PASS |
| Build/static analysis | `rtk go build -o /dev/null ./cmd/aether`; `rtk go vet ./...` | exit 0 | ✓ PASS |
| TypeScript host | typecheck + host test suite | typecheck exit 0; 540 passed | ✓ PASS |
| Full Go suite | `rtk go test ./... -count=1` | No failure output; still running at 300s, interrupted with exit 130 | ? INCONCLUSIVE, NOT PASS |
| Full race suite | Not rerun after normal suite hit the cap | Prior isolated run reported 8,537 pass, 11 skip, three now-targeted regressions, no race warning | ? INCONCLUSIVE, NOT PASS |

The focused green suites prove the intended happy paths. They do not exercise CR-01, CR-02, CR-04, CR-05, CR-06, or WR-01; each of those failures is directly observable in the production source/caller chain.

## Probe Execution

No PLAN or SUMMARY declares a shell probe, `find scripts -path '*/tests/probe-*.sh' -type f` returned no files, and no deferred `<human-check>` block exists. Step 7c is **SKIPPED (no declared or conventional probes)**.

## Requirements Coverage

All six STAM IDs are declared by one or more plans and mapped to Phase 198.3 in `REQUIREMENTS.md`. No Phase 198.3 requirement is orphaned.

| Requirement | Source plans | Status | Evidence |
|---|---|---|---|
| STAM-01 | 02, 03, 04, 05, 10–13, 15–19, 21, 22 | ✗ BLOCKED | Valid owner work queues, but mutable visual claims can suppress current owner work and malformed reviewer/seal evidence can fail open. |
| STAM-02 | 01, 02, 04, 10, 11, 13–15, 17, 19, 22 | ✗ BLOCKED | Catalogue/dry-run and valid score/Critical/runtime/escalation behavior exist; legacy severity validation is missing. |
| STAM-03 | 04, 10, 14, 20 | ✗ BLOCKED | Lesson count/references and durable cadence exist, but stale goal/session decisions can suppress a due replan. |
| STAM-04 | 05, 10, 14, 22 | ✓ SATISFIED | Wall clock and measured/unreported cost survive into the frozen report and status. |
| STAM-05 | 01, 04, 05, 07, 10, 12, 16, 21, 22 | ✓ SATISFIED | Pre/post blocker count/escalation/availability is live in run and status; same-count replacement is intentionally non-terminal under D-06. |
| STAM-06 | 10, 14, 15, 22 | ✓ SATISFIED | Real run-loop six-phase proof and queueable mutation guard pass independently. |

**Requirements score:** 3/6

## Anti-Patterns Found

| File | Line(s) | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `cmd/swarm_cmd.go` | 688–700 | User-controlled path used before validation | 🛑 BLOCKER | Writes can escape the intended swarm directory before rejection. |
| `cmd/swarm_cmd.go` / `cmd/swarm_strikes.go` | 393–416, 652–668 / 145–147 | Unissued manifest + overwriteable replay | 🛑 BLOCKER | Durable swarm/strike truth is caller-rewriteable. |
| `cmd/codex_workflow_cmds.go` | 1331–1342 | Legacy storage error discarded | 🛑 BLOCKER | Seal can report zero blockers for broken legacy state. |
| `cmd/codex_continue_finalize.go` | 819–833 | Compatibility validation weaker than canonical artifact path | 🛑 BLOCKER | Invalid reviewer severity can be credited. |
| `cmd/autopilot_checkpoints.go` | 403–432 | Mutable file treated as immutable authorization evidence | 🛑 BLOCKER | Current visual owner work can be removed or invented. |
| `cmd/compatibility_cmds.go` / `cmd/autopilot_lessons.go` | 744 / 229–245 | Scope filter omitted from durable cadence | 🛑 BLOCKER | Stale decisions can suppress replanning. |
| `cmd/autopilot_checkpoints.go` | 696–735 | Unbounded valid capability hashes | ⚠ WARNING | Authorization window/file growth continues until resolution; intentional compatibility behavior. |
| `cmd/autopilot_report.go` | 31–36, 190–199 | Critical detail omitted from frozen phase report | ⚠ WARNING | Morning report names the stop but loses detailed file/line/fix context. |

Debt-marker scan across the 84 reviewed phase files plus the post-review wrapper fixture found no actionable `TBD`, `FIXME`, or `XXX`. The only match is documentation naming TODO/FIXME as a scan category. Placeholder/empty-return matches were generated command descriptions, documentation concepts, tests, typed empty collections, or initialized state—not user-visible stubs.

## Deferred-Phase Filter

None of the six gaps is deferred. Phase 198.4 is specifically a dead-code/disconnected-artifact cleanup, and Phase 199 is owner-observed proof. Neither later goal or success contract promises swarm trust hardening, seal error propagation, reviewer severity validation, checkpoint provenance, or replan scoping.

## Human Verification Required

None before repair. Phase 199 owns the real owner-watched overnight experience, but human UAT cannot override deterministic trust-boundary failures in this phase. After the six gaps close, rerun complete normal and race suites and then proceed to real-owner proof.

## Gaps Summary

The merged implementation proves the central happy path: six phases complete under one run, queueable owner work does not halt it, replan notes contain confirmed lessons, elapsed/cost reporting is honest, and run/status share a live fail-closed blocker gate.

The phase still fails its actual overnight-safety goal for six concrete reasons:

1. External swarm IDs can escape their storage directory before validation.
2. External swarm manifests/results are self-asserted and replayable.
3. Seal ignores malformed legacy blocker storage.
4. Legacy reviewer severity is not validated.
5. Visual checkpoint evidence is mutable after the build attempt.
6. Replan cadence accepts stale cross-session/cross-goal decisions.

These failures are observable in live production call chains. The Escalation Gate remains closed; Phase 198.4 should not begin on the assumption that Overnight Stamina is achieved.

---

_Verified: 2026-09-01T21:11:24Z_  
_Verifier: the agent (gsd-verifier)_

</details>
