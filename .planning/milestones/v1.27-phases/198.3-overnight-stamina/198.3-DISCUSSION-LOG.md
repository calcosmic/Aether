# Phase 198.3: Overnight Stamina - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-31
**Phase:** 198.3-overnight-stamina
**Areas discussed:** Backlog triage, What stops the night, Replanning overnight, Provider timeouts, Morning report

---

## Backlog Triage

| Existing item | Meaning for this phase | Selected |
|---------------|------------------------|----------|
| Worker turnaround is too slow | Add no needless workers/check-ins; remove repeated provider checks; measure the real run time | ✓ |
| TS-host preflight timeout | Make every live provider check share the bounded configurable contract | ✓ |
| Spec-builder command | Separate user-facing specification feature | |

**User's choice:** Fold the turnaround and timeout items; leave the spec builder in the backlog.

---

## What Stops the Night

| Decision point | Option | Consequence | Selected |
|----------------|--------|-------------|----------|
| Basic safety rule | Stop only when continuing is unsafe or impossible | Machine-confirmed danger stops; owner-only checks wait | ✓ |
| Basic safety rule | Stop only for security/data-loss danger | Other machine failures could be built upon | |
| Basic safety rule | Stop for every failed check or warning | Preserves today's over-stopping behaviour | |
| Auditor floor | Below 60 | Restores the original threshold without spawning an Auditor for a score | ✓ |
| Auditor floor | Below 70 | Stricter; more ordinary quality pauses | |
| Auditor floor | Below 50 | More permissive; weaker work may flow forward | |
| Existing blockers | Show/baseline them; stop on new or escalated blockers | Preserves Phase 198's headless ruling and detects worsening state | ✓ |
| Existing blockers | Stop before any worker starts | Old/stale owner notes could waste the night | |
| High findings | Critical stops; High waits | Matches the written STAM contract | ✓ |
| High findings | High security/data-loss also stops | Adds a domain-specific severity exception | |
| High findings | Critical and High always stop | Closest to the oldest Chaos rule; more false halts | |

**User's choice:** Stop only for unsafe/impossible machine-confirmed states; score floor 60; baseline existing blockers; only Critical findings halt.

**Notes:** Human-only runtime and visual verification remains unresolved for the owner but is queueable in headless mode. The user selected the recommended option at each decision point.

---

## Replanning Overnight

| Decision point | Option | Consequence | Selected |
|----------------|--------|-------------|----------|
| Headless replan | Queue suggestion and continue | Interactive pauses; overnight run remains unattended | ✓ |
| Headless replan | Rewrite the plan automatically | AI changes remaining roadmap and adds spend without approval | |
| Headless replan | Ignore replanning until the end | Useful plan-drift warning disappears | |
| Trigger timing | Phase interval plus at least one confirmed lesson | A phase count alone is not treated as a reason | ✓ |
| Trigger timing | Every two phases regardless of lessons | Preserves a fixed but sometimes meaningless rhythm | |
| Trigger timing | Immediately on any lesson | Responsive but noisy | |
| Lesson count | Unique confirmed lessons since last successful plan | Durable, deduplicated and actionable | ✓ |
| Lesson count | Every raw observation | Inflated by repeats and minor notes | |
| Lesson count | Only long-term promoted habits | Too delayed to explain current plan drift | |
| Repeated triggers | Update one open note | Preserves first/latest trigger and avoids queue spam | ✓ |
| Repeated triggers | Add one note per interval | Full history but repeated questions | |
| Repeated triggers | Add only a final note | Hides when drift first became material | |

**User's choice:** Queue and continue; require interval plus a confirmed lesson; count unique confirmed lessons since the last plan; update one open note.

**Notes:** The user selected the recommended option at each decision point.

---

## Provider Timeouts

| Decision point | Option | Consequence | Selected |
|----------------|--------|-------------|----------|
| Attempts | Two total | One timeout-only retry, matching the proven Go path | ✓ |
| Attempts | Three total | More tolerance but potentially several minutes of delay | |
| Attempts | Retry until overall deadline | A provider stall could consume the whole night | |
| Check frequency | Once per phase, shared by its workers | Cuts repeated overhead but rechecks during a long night | ✓ |
| Check frequency | Once per whole run | Fastest, but readiness can go stale hours later | |
| Check frequency | Before every worker | Freshest but repeats the original cost/delay | |
| Exhausted timeout | Save and stop at unfinished phase | Keeps completed work and resumes exactly | ✓ |
| Exhausted timeout | Switch provider automatically | Introduces unapproved routing/cost changes | |
| Exhausted timeout | Skip the phase | Later phases may depend on missing work | |
| Configuration | One shared setting, 45-second default | Go and TypeScript obey one visible contract | ✓ |
| Configuration | Separate auth/model settings | Flexible but easy to misunderstand and drift | |
| Configuration | Fixed 45 seconds | Recreates the unconfigurable-machine problem | |

**User's choice:** Two attempts; check once per phase; preserve state and stop on exhaustion; one shared 45-second-default setting.

**Notes:** A real missing login or rejected authentication is not a timeout and does not retry. The user selected the recommended option at each decision point.

---

## Morning Report

| Decision point | Option | Consequence | Selected |
|----------------|--------|-------------|----------|
| First view | Outcome-first summary | Completion/stop, queued work, time, cost and next command lead | ✓ |
| First view | Full timeline | Complete history but buries the answer | |
| First view | Totals only | Too little explanation for a stop | |
| Elapsed meaning | Wall clock for this invocation | Includes checks/retries/waits, excludes offline pause time | ✓ |
| Elapsed meaning | Calendar time across resumes | Offline hours distort actual run time | |
| Elapsed meaning | Sum of worker times | Not the owner's waiting time | |
| Cost detail | Measured total plus compact phase breakdown | Useful overview with honest missing-data marks | ✓ |
| Cost detail | Total only | Requires `aether spend` for every detail | |
| Cost detail | Every worker row | Too long after a large run | |
| Persistence | Save and show the same summary in status | Terminal loss does not lose the result | ✓ |
| Persistence | Separate report file only | Durable but difficult to discover | |
| Persistence | Terminal only | Disconnection loses the morning answer | |

**User's choice:** Outcome-first; invocation wall clock; measured total plus phase breakdown; persist and surface through `aether status`.

**Notes:** Missing provider usage remains visibly `— not reported` and is never estimated. The user selected the recommended option at each decision point.

---

## Planner's Discretion

- Exact persisted summary schema and dedup keys.
- Exact reason-code names and compact house-style wording.
- Technical method for unique confirmed-lesson counting and phase-scoped provider caching.
- Bounded integration of the roadmap's recovery/audit subjects, without a second loop or routine extra workers.
- Construction of the six-plus-phase simulated end-to-end fixture.

## Deferred Ideas

- Spec-builder command — separate future intent-capture feature.
- Automatic provider switching and automatic plan rewriting — considered and not selected.
- Broad worker-speed programme beyond the overnight loop — future work.
- Real owner-observed overnight proof — Phase 199.
