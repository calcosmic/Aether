---
phase: 198.3-overnight-stamina
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/compatibility_cmds.go
  - cmd/autopilot_report_test.go
  - cmd/autopilot_policy_test.go
autonomous: true
requirements:
  - STAM-02
  - STAM-03
  - STAM-04
  - STAM-06
gap_closure: true
must_haves:
  truths:
    - "A terminal run whose outcome report cannot be persisted returns non-zero in JSON and visual modes."
    - "A visual durability failure leads with an error headline before any outcome-like content, then labels the retained outcome, stop reason, phase evidence, elapsed time, spend evidence, blockers, and next action as unsaved in-memory evidence."
    - "Dry-run replan preview uses the same lesson-aware predicate and mode disposition as the live loop."
    - "A cadence boundary with no confirmed new lessons does not fabricate replan_due; a due headless replan previews queue-and-continue without writing state."
  artifacts:
    - path: "cmd/compatibility_cmds.go"
      provides: "Non-zero terminal durability rendering and one shared live/dry replan evaluator"
    - path: "cmd/autopilot_report_test.go"
      provides: "JSON/visual report persistence failure and evidence-retention proof"
    - path: "cmd/autopilot_policy_test.go"
      provides: "Lesson-aware headless/interactive dry-run parity and non-mutation proof"
  key_links:
    - from: "finishAutopilotInvocation"
      to: "runCompatibilityCmd.RunE"
      via: "report_persist_error suppresses terminal success rendering, selects an error-first envelope/visual on stderr, and marks a non-zero command result"
      pattern: "report_persist_error"
    - from: "buildRunDryRunResult"
      to: "runCompatibilityAutopilot live continue branch"
      via: "both call the same replan eligibility and disposition helper"
      pattern: "lessonAwareReplanDue"
---

<objective>
Make terminal report durability honest and remove the dry-run replan preview's drift from live lesson-aware behavior.

Purpose: D-17 through D-20 require a trustworthy morning report, while D-07/D-10 require preview to explain the behavior the unattended run will actually take.
Output: Non-zero report-persistence failures with complete evidence, plus a read-only live-parity replan preview.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
@.planning/phases/198.3-overnight-stamina/198.3-REVIEW.md
@.planning/phases/198.3-overnight-stamina/198.3-05-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-09-SUMMARY.md
@cmd/autopilot_lessons.go
@cmd/autopilot_report.go

<interfaces>
- `finishAutopilotInvocation` is the single post-start finalizer and currently records `report_persist_error` in the result.
- `outputError` marks a rendered command failure so `cmd.Execute` becomes non-zero and emits JSON details in JSON mode.
- `runAutopilotLoadLessons(plan)` is the injectable live loader for confirmed typed lessons.
- `lessonAwareReplanDue` plus `legacyInteractiveReplanDue` are the existing live predicates; `autopilotRunDecisionForCode(replan_due, headless, evidence)` supplies the locked mode disposition.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail terminal runs non-zero when the final report is not durable</name>
  <files>cmd/compatibility_cmds.go, cmd/autopilot_report_test.go</files>
  <read_first>
    - cmd/compatibility_cmds.go
    - cmd/autopilot_report_test.go
    - cmd/autopilot_report.go
    - cmd/helpers.go
    - cmd/root.go
    - cmd/codex_visuals.go
    - cmd/run_overnight_198_3_test.go
  </read_first>
  <behavior>
    - Terminal report persistence success keeps the existing success envelope and visual report.
    - Final-report persistence failure produces a non-zero command result and JSON `ok:false` with the complete run result in details.
    - Visual failure writes a durability-error headline to stderr before any outcome or completion-looking content, then renders the retained record under an explicit `UNSAVED IN-MEMORY EVIDENCE` label.
    - The in-memory report is retained even if the store write fails; the persistence error does not replace outcome/stop/next/elapsed/spend/blocker/phase evidence, but none of that evidence is described as saved, completed, or ready for seal.
  </behavior>
  <action>
Keep `finishAutopilotInvocation` as the sole post-start finalizer and preserve its fully built `last_report` when `syncRunAutopilotStateWithReport` fails. Add a narrow injectable persistence seam if needed so tests can fail only the terminal write after earlier running-state writes succeed. Suppress `renderAutopilotComplete`, `renderProjectComplete`, the ordinary typed terminal decision, and every normal success/report renderer when `report_persist_error` is present; otherwise those progress emissions can precede the error before `RunE` sees the result. In `runCompatibilityCmd.RunE`, detect `report_persist_error` after `runCompatibilityAutopilot`: JSON mode must call `outputError` with code 1, a durability-specific message, and the entire run result as details; visual mode must first write a durability-failure headline to stderr and call `markRenderedCommandError(1)`, then render the retained outcome under an unmistakable `UNSAVED IN-MEMORY EVIDENCE` heading. The failure renderer may reuse report field formatting, but it must not reuse a wrapper that labels the outcome completed/successful or return early before the error. D-17 outcome-first ordering remains the durable-success contract; on a failed D-20 write, the authoritative verifier's fail-closed ordering takes precedence. Preserve D-18 measured/unreported cost semantics, D-19 wall-clock elapsed time, and the single in-memory record. Do not return a success envelope after the failed write.
  </action>
  <verify>
    <automated>rtk go test ./cmd -run 'TestRunReportPersistenceFailure' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Exit assertion: black-box JSON and visual command invocations both return exit code 1 on terminal report write failure.
    - JSON assertion: the error envelope details include `last_report`, `report_persist_error`, trigger/disposition, and primary phase evidence.
    - Visual ordering assertion: stderr begins with the report-durability failure; `UNSAVED IN-MEMORY EVIDENCE` and Outcome/Stop reason/evidence/Elapsed/Cost/Next follow it, and no success/completion presentation appears before the error headline.
    - Regression assertion: successful persistence retains the existing output and exit code 0.
  </acceptance_criteria>
  <done>Terminal persistence failure renders an error first, retains the complete outcome only as explicitly unsaved evidence, and exits non-zero in JSON and visual modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project live lesson-aware replan behavior in dry-run mode</name>
  <files>cmd/compatibility_cmds.go, cmd/autopilot_policy_test.go</files>
  <read_first>
    - cmd/compatibility_cmds.go
    - cmd/autopilot_policy_test.go
    - cmd/autopilot_lessons.go
    - cmd/autopilot_lessons_test.go
    - cmd/autopilot_policy.go
    - cmd/autopilot_report.go
  </read_first>
  <behavior>
    - Cadence reached with zero confirmed post-revision lessons produces no replan event or stop.
    - Cadence plus confirmed lessons produces `replan_due`; interactive preview pauses, while headless preview records a would-queue step and continues to the next projected phase.
    - `--continue` bypasses the same due event in live and dry paths.
    - Dry-run reads live lesson evidence but changes no colony, lesson, pending-decision, blocker, autopilot-state, or plan bytes.
  </behavior>
  <action>
Extract one replan eligibility/disposition helper used by both the live post-continue branch and `buildRunDryRunResult`. It must combine `lessonAwareReplanDue`, the narrowly scoped legacy interactive predicate, and `autopilotRunDecisionForCode` without creating a pending decision. Load confirmed lessons through `runAutopilotLoadLessons` for dry-run; propagate lesson-load corruption as a read-only preview failure rather than inventing eligibility. At a due headless boundary append a clearly preview-only queue-and-continue step containing trigger code, disposition, lesson count, and active plan revision, then continue projecting. At a due interactive boundary stop with the canonical replan decision. Remove the cadence-only unconditional `replan_due` branch. Add before/after filesystem snapshots proving no writes, including no `pending-decisions.json` creation. This directly closes verification warning WR-01 under D-07, D-09, D-10, and D-11.
  </action>
  <verify>
    <automated>rtk go test ./cmd -run 'TestRunDryRun.*(Lesson|Replan|Headless|NonMutating|Catalogue)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Negative assertion: interval cadence without confirmed lessons never reports `replan_due`.
    - Mode assertion: the same due evidence previews pause interactively and queue-and-continue headlessly.
    - Evidence assertion: preview includes lesson count and active revision identity from the live typed selector.
    - Mutation assertion: recursive durable-file snapshots are identical before and after every dry-run case.
  </acceptance_criteria>
  <done>Dry-run replan output is live-lesson aware, mode accurate, and byte-for-byte non-mutating.</done>
</task>

</tasks>

<verification>
Run `rtk go test ./cmd -run 'Test(RunReport|AutopilotReport|RunDryRun|AutopilotLesson|AutopilotTrigger)' -count=1`, then `rtk go test ./cmd -count=1`. Confirm JSON and visual black-box failure tests assert the command result, not merely a payload field.
</verification>

<success_criteria>
- A non-durable terminal report can never look successful to a shell or hosted caller.
- Visual failure shows the durability error before any explicitly unsaved primary-report evidence, with no preceding terminal success/completion presentation.
- Dry-run and live execution share replan eligibility and mode disposition.
- Headless preview is read-only and never fabricates a cadence-only replan.
</success_criteria>

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-14-SUMMARY.md` when done.
</output>
