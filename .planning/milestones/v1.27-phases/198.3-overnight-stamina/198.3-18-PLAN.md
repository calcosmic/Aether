---
phase: 198.3-overnight-stamina
plan: 18
type: execute
wave: 2
depends_on:
  - 198.3-17
files_modified:
  - cmd/handoff_decisions_cmd.go
  - cmd/handoff_decisions_cmd_test.go
  - cmd/criterion_owner_confirmation.go
  - cmd/criterion_owner_confirmation_test.go
autonomous: true
requirements:
  - STAM-01
gap_closure: true
must_haves:
  truths:
    - "A legacy same-text answer suppresses owner work only inside the active colony session and goal."
    - "Changing session or goal makes the current handoff question and owner confirmation unresolved again."
    - "Current-scope compatibility answers still work without bypassing protected checkpoint capabilities."
  artifacts:
    - path: "cmd/handoff_decisions_cmd.go"
      provides: "Scope-filtered legacy answered-question compatibility lookup"
      contains: "pendingDecisionMatchesScope"
    - path: "cmd/criterion_owner_confirmation.go"
      provides: "Scope-aware owner-confirmation answer check"
      contains: "ownerConfirmationAnswered"
    - path: "cmd/handoff_decisions_cmd_test.go"
      provides: "Cross-session and cross-goal handoff regressions"
    - path: "cmd/criterion_owner_confirmation_test.go"
      provides: "Cross-scope criterion confirmation regression"
  key_links:
    - from: "cmd/handoff_decisions_cmd.go"
      to: "cmd/pending_decision.go"
      via: "resolved legacy answers are filtered through the active pending-decision scope"
      pattern: "pendingDecisionMatchesScope"
    - from: "cmd/criterion_owner_confirmation.go"
      to: "cmd/handoff_decisions_cmd.go"
      via: "owner confirmation consumes only the active-scope answered-text set"
      pattern: "answeredDecisionTexts"
---

<objective>
Scope legacy answered-question compatibility to the active session and goal.

Purpose: Prevent an old same-text answer from silently resolving current owner work while retaining compatibility for answers recorded in the current colony scope.
Output: One shared scope-aware lookup and adversarial handoff/criterion tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
@.planning/phases/198.3-overnight-stamina/198.3-11-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filter legacy handoff answers by the active pending-decision scope</name>
  <files>cmd/handoff_decisions_cmd.go, cmd/handoff_decisions_cmd_test.go</files>
  <read_first>
    cmd/handoff_decisions_cmd.go
    cmd/handoff_decisions_cmd_test.go
    cmd/pending_decision.go
    cmd/pending_decision_test.go
    cmd/autopilot_checkpoints.go
    .planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
    .planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
  </read_first>
  <behavior>
    - A resolved generic decision with matching normalized text and matching session/goal suppresses the current handoff question.
    - The same text from a different session, a different goal hash, or an unscoped stale row does not suppress current work.
    - Protected checkpoint rows remain resolvable only by their row-bound capability route.
  </behavior>
  <action>Replace the global `answeredDecisionTexts()` aggregation with a scope-requiring form that obtains the active `pendingDecisionScope` once and includes a resolved answer only when `pendingDecisionMatchesScope` succeeds. Make `pendingHandoffDecisions` pass that active scope instead of comparing against every historical resolution. Preserve normalized exact-text compatibility inside the current scope and preserve the protected-checkpoint exclusion introduced by Plan 11; do not make question text an authorization mechanism. This implements D-02's requirement that current owner work remain unresolved until the current owner answers it.</action>
  <acceptance_criteria>
    - No production helper gathers resolved answer text without an explicit active session/goal scope.
    - `TestLegacyAnsweredDecisionTextIsScopedToActiveSessionAndGoal` covers matching scope, foreign session, foreign goal hash, and stale unscoped rows.
    - `TestHandoffDecisionsExcludesAnsweredQuestions` continues to pass for a current-scope answer.
    - `TestDecisionAnswerRejectsCheckpointAuthorizationFailures` continues to prove generic answers cannot resolve protected checkpoint rows.
    - `go test ./cmd -run 'Test(LegacyAnsweredDecisionTextIsScopedToActiveSessionAndGoal|HandoffDecisions|DecisionAnswerRejectsCheckpointAuthorizationFailures)' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(LegacyAnsweredDecisionTextIsScopedToActiveSessionAndGoal|HandoffDecisions|DecisionAnswerRejectsCheckpointAuthorizationFailures)' -count=1</automated>
  </verify>
  <done>Historical same-text answers cannot hide a current handoff question across session or goal boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the same scope contract to criterion owner confirmations</name>
  <files>cmd/criterion_owner_confirmation.go, cmd/criterion_owner_confirmation_test.go, cmd/handoff_decisions_cmd.go</files>
  <read_first>
    cmd/criterion_owner_confirmation.go
    cmd/criterion_owner_confirmation_test.go
    cmd/handoff_decisions_cmd.go
    cmd/handoff_decisions_cmd_test.go
    cmd/pending_decision.go
    cmd/autopilot_checkpoints.go
    .planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
    .planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
  </read_first>
  <behavior>
    - `ownerConfirmationAnswered` returns true for an exact current-scope legacy answer.
    - It returns false for the identical question resolved under another session or goal.
    - Owner-confirmation seal blockers remain present until a current-scope answer or protected checkpoint resolution exists.
  </behavior>
  <action>Make `ownerConfirmationAnswered` consume the same scope-filtered answer set as handoff decisions, using the current state scope rather than a global normalized-text set. Keep phase/task/criterion question construction and shell-safe answer commands unchanged. Add table-driven session and goal mutations around a single criterion, and assert both the boolean helper and `ownerConfirmationSealBlockers` so the test proves the stale answer cannot bypass seal enforcement.</action>
  <acceptance_criteria>
    - Handoff and criterion compatibility matching call one shared scope-aware answer helper; neither performs an independent unscoped scan.
    - `TestOwnerConfirmationAnsweredRejectsCrossScopeResolution` proves foreign-session and foreign-goal answers return false and leave the seal blocker live.
    - The same test proves a current-scope answer returns true and removes only its matching blocker.
    - `TestOwnerConfirmationCommandIsShellSafe` and `TestShellQuoteEscapesEmbeddedSingleQuotes` remain green.
    - `go test ./cmd -run 'Test(OwnerConfirmationAnsweredRejectsCrossScopeResolution|OwnerConfirmationCommandIsShellSafe|ShellQuoteEscapesEmbeddedSingleQuotes)' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(OwnerConfirmationAnsweredRejectsCrossScopeResolution|OwnerConfirmationCommandIsShellSafe|ShellQuoteEscapesEmbeddedSingleQuotes)' -count=1</automated>
  </verify>
  <done>Legacy owner confirmations are compatible only within the active session/goal and cannot cross-authorize seal completion.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(HandoffDecisions|DecisionAnswer|OwnerConfirmation|ShellQuote)' -count=1`
- `go vet ./cmd/...`
</verification>

<success_criteria>
- Current-scope legacy answers remain functional.
- Cross-session, cross-goal, and stale unscoped answers cannot suppress current owner work.
- Protected checkpoint capability resolution is unchanged.
</success_criteria>

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-18-SUMMARY.md` when done.
</output>
