---
phase: 198.3-overnight-stamina
plan: 03
subsystem: worker-readiness
tags: [go, typescript, preflight, cache, retry]

# Dependency graph
requires: []
provides:
  - Phase-scoped provider-readiness success shared across build and continue workers
  - One timeout/retry contract using AETHER_PREFLIGHT_TIMEOUT with a 45-second default
  - Phase propagation through the active TypeScript host and internal Go adapter
affects: [worker-dispatch, build, continue, overnight-stamina]

# Tech tracking
tech-stack:
  added: []
  patterns: [phase-scoped readiness cache, keyed in-process singleflight, zero-phase compatibility lane]

key-files:
  created:
    - cmd/preflight_phase_198_3_test.go
    - pkg/codex/preflight_phase_198_3_test.go
    - .aether/ts-host/test/preflight-phase-198-3.test.ts
  modified:
    - cmd/preflight_cache.go
    - cmd/dispatch_runtime.go
    - cmd/internal_worker_adapter.go
    - pkg/codex/platform_dispatch.go
    - pkg/codex/availability_probe_retry_test.go
    - .aether/ts-host/src/worker-dispatch.ts
    - .aether/ts-host/src/host.ts

key-decisions:
  - "Phase 0 remains the explicit unscoped compatibility lane; positive phases cache readiness by platform and phase."
  - "The active TypeScript host always sends one --phase argument, using 0 for plan and the manifest phase for build/continue."
  - "The deprecated platform-dispatcher.ts route remains untouched because caller search found no production caller."

patterns-established:
  - "Readiness identity: platform#phase=N for positive phases, with the platform-only key reserved for phase 0."
  - "Compatibility wrappers preserve phase-0 behavior while live dispatch paths call phase-aware APIs."

requirements-completed: [STAM-01]

# Metrics
duration: 33min
completed: 2026-08-31
---

# Phase 198.3 Plan 03: Phase-Scoped Provider Readiness Summary

**Provider readiness now runs at most once per platform and phase, retries only timeouts, and uses the same configurable 45-second timeout across direct Go and hosted TypeScript dispatch.**

## Performance

- **Duration:** 33 min
- **Started:** 2026-08-31T14:59:24Z
- **Completed:** 2026-08-31T15:32:06Z
- **Tasks:** 2
- **Files modified:** 10

## Accomplishments

- Bumped the readiness cache to schema 2 and scoped successful entries to `platform + phase`, while safely discarding legacy successes and clearing every phase during platform invalidation.
- Preserved TTL reuse and keyed singleflight within a phase, but guaranteed the next positive phase performs a fresh readiness check.
- Unified hosted-model and availability/auth probes on `AETHER_PREFLIGHT_TIMEOUT`, with exactly two attempts for timeouts and one for auth, binary, login, or other failures.
- Threaded build and continue manifest phases through the active TypeScript host into `internal-worker-adapter --phase`; plan explicitly uses the compatible unscoped phase 0.

## Task Commits

Each TDD task was committed as an atomic RED/GREEN pair:

1. **Task 1 RED: failing phase-cache and timeout-contract regressions** - `ab7183db` (test)
2. **Task 1 GREEN: phase-scoped readiness and unified timeout behavior** - `150b55d3` (feat)
3. **Task 2 RED: failing adapter and hosted phase-bridge regressions** - `e61c9b83` (test)
4. **Task 2 GREEN: active host-to-adapter phase propagation** - `392ce665` (feat)

## Files Created/Modified

- `cmd/preflight_cache.go` - Adds schema-2 phase keys, phase-aware load/record/invalidation, and keyed singleflight.
- `cmd/dispatch_runtime.go` - Derives and validates one phase for direct readiness before dispatch.
- `cmd/internal_worker_adapter.go` - Adds `--phase`, validates it before provider selection, and calls the phase-aware gate.
- `cmd/preflight_phase_198_3_test.go` - Covers phase reuse/turnover, invalidation, old schema, and adapter CLI validation.
- `pkg/codex/platform_dispatch.go` - Shares the preflight timeout resolver with availability/auth probing and preserves timeout-only retry.
- `pkg/codex/preflight_phase_198_3_test.go` - Pins the unified environment variable, default, and attempt categories.
- `pkg/codex/availability_probe_retry_test.go` - Updates the existing live-probe compatibility assertions to the unified timeout contract.
- `.aether/ts-host/src/worker-dispatch.ts` - Sends phase through the Go preflight bridge.
- `.aether/ts-host/src/host.ts` - Supplies build/continue manifest phases and phase 0 for plan.
- `.aether/ts-host/test/preflight-phase-198-3.test.ts` - Captures exact bridge arguments and pins active timeout/caller behavior.

## Decisions Made

- Kept the existing four-argument `runInternalWorkerAdapter` and unscoped `gatedProviderPreflight` wrappers so unrelated callers retain phase-0 behavior without repository-wide signature churn.
- Rejected negative phase values inside the adapter before resolving a provider; Cobra's integer flag parser rejects non-numeric input before the command runs.
- Sent `--phase 0` explicitly from the active TypeScript plan lane, making the compatibility scope visible and testable.
- Left `.aether/ts-host/src/platform-dispatcher.ts` unchanged. The only source import found is a type-only dependency in `prompt-assembler.ts`; production worker dispatch does not call the retired launcher.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated the pre-existing availability retry compatibility test for the intentional timeout-variable replacement**

- **Found during:** Task 1 (Scope readiness success to a phase and unify the Go retry/timeout contract)
- **Issue:** `pkg/codex/availability_probe_retry_test.go` still set and expected the retired `AETHER_PROBE_TIMEOUT`, so the required package test could not validate the newly unified live contract.
- **Fix:** Replaced only those environment/default expectations with `AETHER_PREFLIGHT_TIMEOUT` and the shared 45-second default.
- **Files modified:** `pkg/codex/availability_probe_retry_test.go`
- **Verification:** The full `Preflight|Availability` selection passes 95 tests across `cmd` and `pkg/codex`.
- **Committed in:** `150b55d3`

---

**Total deviations:** 1 auto-fixed (1 Rule 3)
**Impact on plan:** Compatibility-only test correction required to verify the intentional live timeout replacement; no runtime scope expansion.

## Issues Encountered

- The isolated worktree had only the TypeScript host's test-binary cache, not its locked dependencies. Typecheck was run against temporary symlinks to the already installed, lockfile-backed dependencies in the main checkout; the links were removed afterward and no packages or manifests changed.

## TDD Gate Compliance

- Task 1: RED `ab7183db` precedes GREEN `150b55d3`.
- Task 2: RED `e61c9b83` precedes GREEN `392ce665`.
- Both RED runs failed for the intended missing behavior before implementation; all focused and plan-wide gates pass after GREEN.

## Verification

- `go test ./cmd/ ./pkg/codex/ -run 'Preflight|Availability' -count=1` - 95 passed.
- `npm --prefix .aether/ts-host run typecheck` - passed.
- Hosted Phase 198.3 test - 3 passed.
- `go build ./cmd/aether` - passed; generated binary removed after verification.
- `go vet ./...` - passed.

## Known Stubs

None. Empty-value scan found only established initialization/validation patterns, not placeholder data introduced by this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Direct Go and hosted TypeScript readiness now share the same phase identity and timeout/retry contract.
- No blockers remain. The deprecated five-second TypeScript dispatcher remains isolated forensic code, protected by an existing no-production-import regression.

## Self-Check: PASSED

All 10 implementation/test files and all four RED/GREEN task commits were found; commit ordering and `git diff --check` also passed.

---
*Phase: 198.3-overnight-stamina*
*Completed: 2026-08-31*
