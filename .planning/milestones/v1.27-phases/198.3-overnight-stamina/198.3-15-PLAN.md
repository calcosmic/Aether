---
phase: 198.3-overnight-stamina
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/codex_build.go
  - cmd/state_load.go
  - cmd/state_repair.go
  - cmd/preflight_phase_198_3_test.go
  - cmd/state_load_test.go
  - cmd/codex_build_test.go
autonomous: true
requirements:
  - STAM-01
  - STAM-02
  - STAM-06
gap_closure: true
must_haves:
  truths:
    - "Direct build validates a read-only, in-memory repaired state and proves provider/model readiness before persisting compatibility repair, missing-plan recovery, trusted-manifest reconciliation, repair events, or any build/lifecycle artifact."
    - "Failed preflight leaves colony state and events byte-identical for ordinary, Force:true, legacy-numeric, missing-plan, and trusted-manifest-reconciliation fixtures; attempts, checkpoints, manifests, briefs, claims, worker activity, and cleanup sentinels are also unchanged."
    - "The later dispatch-boundary preflight remains as defense in depth and hits the same phase-scoped cache."
    - "One direct build performs one real provider probe per phase trust window, not one early probe plus another late probe."
  artifacts:
    - path: "cmd/codex_build.go"
      provides: "Read-only direct-build preparation, earliest safe provider preflight, post-success repair commit, and one reused invoker"
    - path: "cmd/state_load.go"
      provides: "Explicit non-persisting active-state load seam alongside the existing repair-persisting loader"
    - path: "cmd/state_repair.go"
      provides: "Pure missing-plan reconstruction separated from its durable repair commit"
    - path: "cmd/preflight_phase_198_3_test.go"
      provides: "Five-case failed-preflight byte/event snapshot matrix plus cache-count regression proof"
    - path: "cmd/state_load_test.go"
      provides: "Read-only legacy-numeric and missing-plan staging proof with existing repair persistence retained"
    - path: "cmd/codex_build_test.go"
      provides: "Successful post-preflight missing-plan and trusted-manifest repair regression proof"
  key_links:
    - from: "runCodexBuildWithOptions"
      to: "loadActiveColonyStateReadOnly"
      via: "pre-probe state, phase, task, policy, and dispatch validation over in-memory repairs only"
      pattern: "loadActiveColonyStateReadOnly"
    - from: "runCodexBuildWithOptions"
      to: "preflightWorkerProvider"
      via: "phase-scoped read-only dispatch projection before loadActiveColonyState, reconcilePriorCompletedPhaseTasksFromTrustedManifests, or any lifecycle writer"
      pattern: "preflightWorkerProvider"
    - from: "loadActiveColonyStateReadOnly"
      to: "stageMissingPlanFromArtifacts"
      via: "legacy compatibility decode and missing-plan reconstruction without SaveJSON or repair-event persistence"
      pattern: "stageMissingPlanFromArtifacts"
    - from: "runCodexBuildWithOptions"
      to: "executeCodexBuildDispatches"
      via: "the same invoker reaches the later cached defense-in-depth check"
      pattern: "buildInvoker"
---

<objective>
Move direct-build provider preflight ahead of every build mutation while retaining the cached dispatch-boundary check.

Purpose: D-13 through D-15 require unavailable providers to stop cleanly without making an overnight phase look started, interrupted, or partially prepared.
Output: A read-only/staged state-load seam, early direct-build preflight, deferred repair commits, and byte-level no-mutation/one-probe tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
@.planning/phases/198.3-overnight-stamina/198.3-REVIEW.md
@.planning/phases/198.3-overnight-stamina/198.3-10-SUMMARY.md
@cmd/state_load.go
@cmd/state_repair.go
@cmd/dispatch_runtime.go
@cmd/preflight_cache.go

<interfaces>
- Current `loadActiveColonyState() (colony.ColonyState, error)` calls `loadColonyStateWithCompatibilityRepair`, which appends `state_repaired|load` and saves `COLONY_STATE.json`, then calls `repairMissingPlanFromArtifacts`, which appends `plan_recovered|state` and saves the same file.
- Target `loadActiveColonyStateReadOnly() (colony.ColonyState, error)` must return the same semantically usable `colony.ColonyState` for normal, legacy-numeric, and recoverable missing-plan inputs without any `SaveJSON`, atomic update, or persisted repair event; keep `loadActiveColonyState` as the mutation-authorized loader used after readiness succeeds.
- Target `stageMissingPlanFromArtifacts(state colony.ColonyState) (colony.ColonyState, bool, error)` owns pure in-memory reconstruction; existing `repairMissingPlanFromArtifacts` delegates to it and retains its current durable event/write behavior for mutation-authorized callers.
- Current `reconcilePriorCompletedPhaseTasksFromTrustedManifests` rehearses through `applyPriorCompletedPhaseTaskRepairs`, then `UpdateJSONAtomically` rewrites task rows and appends `phase_tasks_repaired|build`; direct build must use only the in-memory `applyPriorCompletedPhaseTaskRepairs` rehearsal before readiness and defer the reconciling atomic update until readiness succeeds.
- `runCodexBuildWithOptions` completes read-only repaired-state validation and dispatch planning before `preflightWorkerProvider`; after success it reloads through `loadActiveColonyState`, performs trusted-manifest reconciliation, and revalidates the current state before `interruptLatestBuildAttempt`, `beginRuntimeSpawnRun`, cleanup, checkpoint, attempt, state, manifest, brief, claim, activity, or heartbeat writes.
- `preflightWorkerProvider(ctx, invoker, []codex.WorkerDispatch)` needs only a consistent phase and root to select the phase-scoped provider cache.
- `executeCodexBuildDispatches` performs the later full-dispatch preflight and must retain it.
- `newCodexWorkerInvoker` is injectable and must be called once so early and late checks use the same provider identity.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage active-state compatibility and missing-plan repair without writing</name>
  <files>cmd/state_load.go, cmd/state_repair.go, cmd/state_load_test.go</files>
  <read_first>
    - cmd/state_load.go
    - cmd/state_repair.go
    - cmd/state_load_test.go
    - cmd/next_action_input.go
    - cmd/next_action_test.go
  </read_first>
  <behavior>
    - `loadActiveColonyStateReadOnly` returns the same usable normal state as `loadActiveColonyState` without changing any saved byte.
    - A legacy string `current_phase` is decoded in memory, but `COLONY_STATE.json` stays byte-identical and gains no `state_repaired|load` event until a mutation-authorized load occurs.
    - A missing plan is reconstructed in memory from `planning/phase-plan.json`, but state stays byte-identical and gains no `plan_recovered|state` event until a mutation-authorized load occurs.
    - Existing `loadActiveColonyState` callers retain their current compatibility repair, missing-plan persistence, exact repair-event vocabulary, and error behavior.
  </behavior>
  <action>
Write failing unit cases first, then introduce `loadActiveColonyStateReadOnly() (colony.ColonyState, error)` as the explicit build preflight seam. Factor the legacy numeric decode out of `loadColonyStateWithCompatibilityRepair` so the read-only path can apply `repairLegacyNumericStringFields` and `normalizeLegacyColonyState` in memory without `store.SaveJSON`; keep the existing mutating loader responsible for appending the exact `state_repaired|load` event and persisting after readiness. Split `repairMissingPlanFromArtifacts` so pure `stageMissingPlanFromArtifacts(state colony.ColonyState) (colony.ColonyState, bool, error)` performs artifact validation, plan reconstruction, progress inference, and in-memory event projection, while the existing function alone persists the staged result. The read-only loader must use the pure stage and must not swallow errors differently from the current active loader. Reuse the byte/hash snapshot style in `cmd/next_action_test.go`; do not route direct build through `readColonyStateWithoutWriting`, whose `(state, bool)` query contract intentionally collapses load errors and does not stage missing-plan recovery.
  </action>
  <verify>
    <automated>rtk go test ./cmd -run 'TestLoadActiveColonyState(ReadOnly|RepairsMissingPlanFromPlanningArtifact|RepairsStringCurrentPhase)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - API assertion: `cmd/state_load.go` contains `loadActiveColonyStateReadOnly() (colony.ColonyState, error)` and `cmd/state_repair.go` contains `stageMissingPlanFromArtifacts`.
    - Read-only assertion: normal, legacy-numeric, and missing-plan read-only loads leave a recursive SHA-256 snapshot of `.aether/data` unchanged.
    - Event assertion: read-only legacy/missing-plan tests prove the persisted event slice does not gain `state_repaired|load` or `plan_recovered|state`.
    - Compatibility assertion: the existing mutating loader tests still prove numeric normalization and recovered plan/events are durably saved.
  </acceptance_criteria>
  <done>Direct build can validate repaired state semantics without authorizing either compatibility repair or missing-plan persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate every direct-build repair and lifecycle mutation, retaining the cached late defense</name>
  <files>cmd/codex_build.go, cmd/preflight_phase_198_3_test.go, cmd/codex_build_test.go</files>
  <read_first>
    - cmd/codex_build.go
    - cmd/preflight_phase_198_3_test.go
    - cmd/codex_build_test.go
    - cmd/state_load.go
    - cmd/state_repair.go
    - cmd/state_load_test.go
    - cmd/dispatch_runtime.go
    - cmd/preflight_cache.go
    - cmd/build_attempt.go
    - cmd/build_attempt_test.go
    - cmd/spawn_runs.go
    - cmd/preflight_gate_test.go
    - cmd/spawn_reap_test.go
  </read_first>
  <behavior>
    - An unavailable preflighter returns `workerProviderPreflightError` before compatibility/missing-plan persistence, trusted-manifest task reconciliation, repair-event persistence, runtime-run creation, forced-attempt interruption, stale cleanup, checkpoint/attempt creation, state projection, manifest/brief/claim creation, activity/heartbeat writes, or worker dispatch.
    - Read-only state/phase/task/policy/dispatch/orphan validation, including an in-memory `applyPriorCompletedPhaseTaskRepairs` rehearsal, runs before the provider probe so invalid input does not probe unnecessarily.
    - On readiness success, compatibility/missing-plan repair and `reconcilePriorCompletedPhaseTasksFromTrustedManifests` commit before build mutation, and the command revalidates the freshly loaded state before proceeding.
    - Successful direct build reaches both the early call site and the existing full-dispatch call site.
    - The provider's real preflight method is called once for that phase because the late check consumes the successful phase-scoped cache entry.
    - A new phase or explicit cache invalidation still causes a fresh bounded probe.
    - Synthetic/FakeInvoker builds retain their existing behavior when no provider preflighter exists.
  </behavior>
  <action>
Start `runCodexBuildWithOptions` with `loadActiveColonyStateReadOnly`, then rehearse prior completed-phase repair by calling `applyPriorCompletedPhaseTaskRepairs` only on the in-memory state before the existing phase/task/evidence/gate/state/policy/dispatch/orphan checks. Create the parent context and instantiate `buildInvoker` exactly once (or `codex.FakeInvoker` for synthetic execution), build a read-only `[]codex.WorkerDispatch` projection carrying `Workflow: "build"`, `Root: root`, and `Phase: phaseNum`, and call `preflightWorkerProvider`. On typed readiness failure, return immediately. Only on success call the existing mutation-authorized `loadActiveColonyState`, then `reconcilePriorCompletedPhaseTasksFromTrustedManifests`; re-run the phase/task/state/gate and dispatch preparation against that freshly loaded state (extract one pure preparation helper if needed so the two validation passes cannot drift). The first post-readiness durable operations may then be compatibility/missing-plan persistence and trusted-manifest reconciliation; `interruptLatestBuildAttempt`, `beginRuntimeSpawnRun`, `cleanupStaleBuildAttemptArtifacts`, checkpoint/attempt/state/manifest/brief/claim/activity/heartbeat writes, and worker invocation must remain later. Pass the same `buildInvoker` to `executeCodexBuildDispatches` and retain its existing `preflightWorkerProvider` call unchanged per D-13, D-14, and D-15.

Add a failed-readiness integration matrix with named subtests `ordinary`, `force_active_attempt`, `legacy_numeric_current_phase`, `missing_plan_recovery`, and `trusted_manifest_prior_task_reconciliation`. Seed the Force case with an active attempt/latest pointer; seed cleanup evidence under `build/phase-N/{verification.json,gates.json,continue.json,review.json,worker-reports/,worker-briefs/}`; seed the repair cases with the same raw state, planning artifact, and trusted final manifest shapes used by `cmd/state_load_test.go` and `cmd/codex_build_test.go`. Compare recursive before/after file-byte hashes for every lifecycle path (isolate only `preflight-cache.json`/diagnostic output from that protected snapshot), compare `COLONY_STATE.json` bytes and decoded `Events` explicitly, and assert no attempt interruption, repair event, spawn run, checkpoint, manifest, brief, claim, activity, heartbeat, cleanup, or `Invoke` call. Add a successful counting invoker case proving the early and late call sites produce exactly one real `Preflight` call and at least one `Invoke`; retain/extend the source-order defense asserting the late call remains inside `executeCodexBuildDispatches`, and keep the existing cross-phase/invalidation tests green.
  </action>
  <verify>
    <automated>rtk go test ./cmd -run 'Test(DirectBuildPreflight|PreflightSuccessIsScopedToWorkerDispatchPhase|PreflightInvalidationClearsEveryPhaseScope|BuildRepairsCompletedPriorPhaseTasksFromTrustedManifest|BuildRecoversMissingPlanFromPersistedPlanningArtifact)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Ordering assertion: the direct path is `loadActiveColonyStateReadOnly` plus in-memory prior-task rehearsal, validation, early `preflightWorkerProvider`, then `loadActiveColonyState` and `reconcilePriorCompletedPhaseTasksFromTrustedManifests`, then force interruption/spawn-run/cleanup/writes.
    - Snapshot assertion: all five failed-readiness subtests preserve `COLONY_STATE.json`, decoded events, existing active-attempt bytes, latest pointer, lifecycle artifacts, and cleanup sentinels exactly; no protected path is added or removed.
    - Repair assertion: failed legacy, missing-plan, and trusted-manifest cases persist none of `state_repaired|load`, `plan_recovered|state`, or `phase_tasks_repaired|build`, while successful existing repair tests still persist them after readiness.
    - Invocation assertion: failed readiness makes one `Preflight` call and zero `Invoke` calls, including `Force:true`.
    - Defense assertion: two preflight call sites remain in the direct execution path, with the later call still inside `executeCodexBuildDispatches`.
    - Probe-count assertion: one successful phase build invokes the provider preflight method exactly once.
    - Scope assertion: entering a different phase or invalidating the provider cache triggers exactly one new real probe.
    - Full-test assertion: existing state repair, missing-plan build, trusted-manifest reconciliation, preflight phase/cache, and force-attempt tests remain green.
  </acceptance_criteria>
  <done>Readiness failure precedes every direct-build repair, event, lifecycle write, destructive cleanup, and invocation; readiness success retains all repairs and one cached late defense.</done>
</task>

</tasks>

<verification>
Run `rtk go test ./cmd -run 'Test(LoadActiveColonyStateReadOnly|LoadActiveColonyStateRepairs|DirectBuildPreflight|PreflightSuccessIsScoped|PreflightInvalidation|InternalWorkerAdapterPreflight|BuildRepairsCompletedPriorPhaseTasksFromTrustedManifest|BuildRecoversMissingPlanFromPersistedPlanningArtifact|ForceRedispatchMarksActiveAttemptInterrupted)' -count=1`, then `rtk go test ./cmd -count=1`. Inspect the five failed-readiness subtests and confirm their recursive protected-path hashes, raw `COLONY_STATE.json` bytes, decoded event slices, active-attempt/latest-pointer bytes, and cleanup sentinels are identical before/after; only the explicitly isolated preflight cache/diagnostic surface may differ. Inspect source order and confirm early preflight precedes `loadActiveColonyState`, `reconcilePriorCompletedPhaseTasksFromTrustedManifests`, `interruptLatestBuildAttempt`, `beginRuntimeSpawnRun`, `cleanupStaleBuildAttemptArtifacts`, and every writer, while the late call remains inside `executeCodexBuildDispatches`.
</verification>

<success_criteria>
- Provider unavailability stops direct build before compatibility repair, missing-plan recovery, trusted-manifest reconciliation, repair events, or any protected lifecycle mutation/cleanup.
- Ordinary, Force:true, legacy-numeric, missing-plan, and trusted-manifest fixtures retain byte-identical protected state and event evidence on failed readiness.
- Forced redispatch cannot interrupt an attempt before readiness succeeds, and successful readiness still permits the existing repair paths to commit.
- The late full-dispatch check remains present and cache-backed.
- Successful overnight phases still perform exactly one real readiness probe per phase.
</success_criteria>

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-15-SUMMARY.md` when done.
</output>
