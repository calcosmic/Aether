---
phase: 198.3-overnight-stamina
plan: 09
subsystem: cli-command-surface
tags: [recovery, medic, cobra, command-audit, golden-fixtures, tdd]

# Dependency graph
requires:
  - phase: 198.3-05
    provides: genuine-stop recovery integration through direct classifier, log, budget, and Medic eligibility helpers
  - phase: 198.3-08
    provides: first seven-command shrink and metadata-safe canonical orphan generation
provides:
  - internal-only recovery classification and phase-log capability for supported run/build/continue/Queen flows
  - internal-only Medic eligibility with advice-only recovery guidance and no automatic worker dispatch
  - exact four-command Plan 09 and eleven-command cumulative catalog/parity/regression/live-orphan shrink
  - shipped continue playbooks with no caller of the retired Medic adapter
affects: [198.3-10, command-audit, orphan-ratchet, recovery, medic, continue]

# Tech tracking
tech-stack:
  added: []
  patterns: [AST command-surface ratchet, direct-helper caller proof, generator-owned fixtures, advice-only recovery, TDD]

key-files:
  created: []
  modified:
    - cmd/recovery_classify.go
    - cmd/recovery_classify_test.go
    - cmd/medic_auto_spawn.go
    - cmd/medic_auto_spawn_test.go
    - cmd/command_call_audit_test.go
    - cmd/testdata/command_catalog.json
    - cmd/testdata/parity_snapshot.json
    - cmd/testdata/regression_snapshot.json
    - cmd/testdata/orphan_allowlist.json
    - .aether/docs/command-playbooks/continue-gates.md
    - colony/playbooks/continue.md

key-decisions:
  - "Remove only the four dead Cobra adapters and command-only rendering while preserving classifier, recovery-log, budget, Medic eligibility, and scan persistence helpers used by supported workflows."
  - "Regenerate each live command-surface fixture once through its owning test writer; preserve every surviving record and keep the historical orphan baseline byte-identical."
  - "Continue surfaces runtime-owned genuine-stop Medic advice only; deep scans, repairs, and worker dispatch remain explicit operator actions."
  - "Pull the two stale Medic playbook caller corrections forward under explicit authorization so the repository remains green between Plans 09 and 10."

patterns-established:
  - "Internal-only proof combines runtime-catalog absence, production AST symbol absence, and direct caller assertions for retained helpers."
  - "A deleted diagnostic adapter must be removed from shipped playbooks in the same green repository state; advisory recovery must not imply that a scan ran."

requirements-completed: []

# Metrics
duration: 34m
completed: 2026-09-01
---

# Phase 198.3 Plan 09: Recovery and Medic Command Surface Removal Summary

**Four orphan recovery/Medic adapters are gone while their internal engine remains live, completing the audited Phase 198.3 shrink from 399 to 388 commands with exact fixtures and advice-only Medic guidance.**

## Performance

- **Duration:** 34m
- **Started:** 2026-08-31T22:16:42Z
- **Completed:** 2026-08-31T22:50:15Z
- **Tasks:** 2
- **Implementation/audit files modified:** 11

## Accomplishments

- Removed `failure-classify`, `recovery-log-read`, and `recovery-log-write` declarations, flags, formatting, and registration without changing classification rules, recovery-log schema/idempotency, or recovery budgets.
- Removed `medic-auto-spawn-check` and command-only rendering while retaining Medic eligibility and persisted scan helpers. Genuine-stop reporting still calls eligibility directly and recommends `aether medic --deep` only when appropriate.
- Replaced command-envelope coverage with runtime-catalog, AST-symbol, and direct-production-caller proof. Build, continue, recovery orchestration, Queen reporting, and overnight reporting retain direct helper calls.
- Regenerated all four live command-surface fixtures through their canonical test flags. Plan 09 moves catalog/orphans from `392 -> 388` and `261 -> 257`; combined with Plan 08, the phase moves from `399 -> 388` and `268 -> 257` exactly.
- Removed stale automatic Medic dispatch instructions from the detailed and canonical continue playbooks. They now record a truthful non-blocking advisory result, make no health claim, and leave scans/repairs/dispatch to explicit operator action.

## Task Commits

Each behavior change was developed through explicit RED/GREEN gates and committed atomically:

1. **Task 1 RED: specify internal-only recovery helpers** - `f456ec35` (test)
2. **Task 1 GREEN: retire recovery command adapters** - `2671c5ad` (feat)
3. **Task 2 RED: specify internal-only Medic eligibility** - `822b948d` (test)
4. **Task 2 GREEN: retire the Medic command adapter** - `a999937b` (feat)
5. **Task 2 artifacts: refresh final command-surface ratchets** - `70a2dab5` (chore)
6. **Verification RED: guard advice-only Medic playbooks** - `91ab6350` (test)
7. **Verification GREEN: make detailed Medic continue handling advice-only** - `712715d3` (docs)
8. **Verification GREEN: remove the canonical stale Medic caller** - `d022d028` (docs)

**Plan metadata:** committed separately with this summary after self-check.

## Files Modified

- `cmd/recovery_classify.go` - Retains failure types, classifier rules, recovery-log schema, atomic writes, reads, and deterministic upsert; removes the three Cobra adapters and command-only rendering.
- `cmd/recovery_classify_test.go` - Retains direct category/log/idempotency cases and adds internal-only runtime, AST, and production-caller proof.
- `cmd/medic_auto_spawn.go` - Retains eligibility, blocker/health evaluation, and Medic scan persistence; removes the Cobra adapter and visual renderer.
- `cmd/medic_auto_spawn_test.go` - Retains direct eligibility/scan tests and adds adapter, caller, and advice-only playbook ratchets.
- `cmd/command_call_audit_test.go` - Removes the deleted Medic command from the reviewed enrichment exception set.
- `cmd/testdata/command_catalog.json` - Canonically regenerated from 392 to 388 entries, with no surviving-entry metadata changes.
- `cmd/testdata/parity_snapshot.json` - Removes only the four Plan 09 names from `runtime_catalog_names`.
- `cmd/testdata/regression_snapshot.json` - Changes only the audited command count from 392 to 388.
- `cmd/testdata/orphan_allowlist.json` - Canonically regenerated from 261 to 257 entries, exactly removing the four deleted command paths while preserving every survivor.
- `.aether/docs/command-playbooks/continue-gates.md` - Replaces the obsolete automatic Medic check/spawn gate with an honest advice-only, non-blocking handoff.
- `colony/playbooks/continue.md` - Replaces the one stale adapter call with runtime-owned genuine-stop advice and explicit no-dispatch wording.

## Decisions Made

- Kept lifecycle capability independent of public CLI surface. `classifyWorkerFailure`, recovery-log read/write/upsert, recovery budget behavior, `shouldAutoSpawnMedic`, and Medic scan persistence remain unchanged and directly callable.
- Proved deletion through runtime and source structure rather than string absence alone, so comments and historical fixtures do not look like production registrations.
- Ran the catalog, parity, regression, and live-orphan generators only after all four adapters were removed, then compared the result semantically against the pre-refresh commit and Phase 198.3 start.
- Kept the immutable orphan baseline at Git blob `44ff02ba73bb9a2ae160308719d2257f40ba306d`; deleted commands remain valid historical evidence there.
- Treated Medic output as advice unless an operator explicitly runs the supported diagnostic. Neither continue playbook claims health, performs a scan, repairs state, nor spawns a worker automatically.

## Deviations from Plan

### Authorized Sequencing Adjustment

**1. [Rule 3 - Authorized] Pulled stale Medic playbook caller cleanup forward from Plan 10**

- **Found during:** Post-Task 2 repository-wide verification
- **Issue:** The four planned command removals and required gates passed, but `TestCLIFlagAudit` and `TestDocumentedCommandNamesResolve` found two shipped continue instructions still calling `medic-auto-spawn-check`; the detailed version also generated and auto-spawned a Medic worker. Leaving them staged would make the removed adapter observable as broken documentation and fail the full suite.
- **Authorization:** The orchestrator explicitly expanded Plan 09 ownership first to `.aether/docs/command-playbooks/continue-gates.md`, then to the single stale Step 1.14 sentence in `colony/playbooks/continue.md`. The latter is the exact one-line correction otherwise assigned to Plan 10.
- **Fix:** Added a focused advice-only playbook ratchet, rewrote only detailed Step 1.14 as a non-blocking advisory handoff, and replaced only the canonical stale sentence. Both playbooks now point to existing genuine-stop advice and the supported operator command `aether medic --deep`, with no scan/health claim/automatic dispatch.
- **Files modified:** `cmd/medic_auto_spawn_test.go`, `.aether/docs/command-playbooks/continue-gates.md`, `colony/playbooks/continue.md`
- **Commits:** `91ab6350`, `712715d3`, `d022d028`

---

**Total deviations:** 1 resolved authorized blocking/sequencing adjustment
**Impact on plan:** The adjustment completes the plan's dead-surface and advice-only guarantees while keeping the repository green; Plan 10 still owns its broader integrated contract and documentation work.

## Issues Encountered

- The first extra `go test ./... -count=1` run reported 8,344 passes and two failures, both caused by the stale playbook callers described above. After the authorized focused corrections, the full rerun passed all 8,346 tests.
- No authentication gates, external service dependencies, or deferred implementation issues remain.

## Verification

- Pre-deletion recovery caller baseline - 39 focused tests passed.
- Task 1 RED gate - the new internal-only recovery test failed because all three commands/symbols were still registered; after implementation, the recovery/helper suite passed 37 tests.
- Pre-deletion Medic/recovery baseline - 62 focused tests passed.
- Task 2 RED gate - the new internal-only Medic test failed because its command and renderer remained; after implementation, the combined recovery/Medic suite passed 62 tests.
- Canonical generators - catalog 1 test, parity 1 test, regression 1 test, and live orphan writer 2 tests passed with update flags; the no-update ratchet rerun passed 7 tests.
- Exact semantic fixture audit - Plan 09 catalog `392 -> 388` and orphan list `261 -> 257` remove only `failure-classify`, `medic-auto-spawn-check`, `recovery-log-read`, and `recovery-log-write`; parity changes only `runtime_catalog_names`; regression changes only command count; all surviving catalog/orphan records are identical.
- Cumulative Phase 198.3 audit - catalog `399 -> 388` and live orphan list `268 -> 257`, exactly the seven Plan 08 autopilot names plus the four Plan 09 names.
- `orphan_allowlist_baseline.json` - byte-identical to the plan base (Git blob `44ff02ba73bb9a2ae160308719d2257f40ba306d`).
- Final caller/playbook audit - 65 focused tests passed, including direct recovery/Medic callers, zero retired registrations, CLI flag hygiene, and documented-command resolution.
- Final generated-fixture/orphan gate - 7 tests passed.
- `go build ./cmd/aether` - passed.
- `go vet ./...` - passed.
- `go test ./... -count=1` - 8,346 tests passed across 20 packages.
- `git diff --check` - passed.

## User Setup Required

None - no external configuration or migration is required. Existing recovery logs and Medic scan data remain compatible, and operators continue to use `aether medic --deep` explicitly when advised.

## Next Phase Readiness

- Plan 10 receives the final honest 388-command runtime surface, live internal recovery behavior, and green shipped command documentation.
- The canonical `colony/playbooks/continue.md` one-line Medic correction is already complete; Plan 10 retains ownership of the broader integrated contract, six-phase fixture, README/parity documentation, and final quality gates.
- No blockers remain.

## Self-Check: PASSED

- All eleven implementation/audit files and this summary exist in the isolated Plan 09 worktree.
- All eight TDD, implementation, fixture, and authorized playbook commit hashes resolve on the plan branch.
- Catalog, regression, and live-orphan counts are 388, 388, and 257 respectively; the immutable orphan baseline matches the plan-base Git blob exactly.
- Required focused tests, caller/document audits, full repository tests, canonical generator reruns, build, vet, exact fixture comparison, whitespace check, stub scan, and threat-surface scan completed successfully.
- No new network endpoint, authentication path, schema trust boundary, production file-access surface, placeholder, or blocking stub was introduced.

---
*Phase: 198.3-overnight-stamina*
*Completed: 2026-09-01*
