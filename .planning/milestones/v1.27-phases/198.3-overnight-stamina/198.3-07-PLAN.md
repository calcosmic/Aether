---
phase: 198.3-overnight-stamina
plan: 07
type: execute
wave: 2
depends_on: ["198.3-01"]
files_modified:
  - cmd/swarm_strikes.go
  - cmd/swarm_strikes_test.go
  - cmd/swarm_cmd.go
  - cmd/swarm_cmd_test.go
  - cmd/swarm_cleanup_truth_test.go
  - cmd/blocker_snapshot_test.go
autonomous: true
requirements: [STAM-05]
audit_scope: [swarm-three-strike-escalation]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Three consecutive failed/blocked swarms for the same normalized problem create one real escalated blocker; attempts one and two do not."
    - "A fourth same-problem attempt refuses dispatch and recommends the guided insert-phase command; a successful swarm resets the streak."
    - "Direct and externally finalized swarms record and evaluate strikes through the same helper."
    - "STAM-05: the third strike reaches run and status automatically through plan 01's shared blocker snapshot, with no separate strike database."
  artifacts:
    - cmd/swarm_strikes.go
    - cmd/swarm_strikes_test.go
    - cmd/swarm_cmd.go
  key_links:
    - "existing swarm result.json history -> normalized target fingerprint -> consecutive-failure evaluator"
    - "third failure -> normal flags store Source=escalation -> blockerSnapshot -> run/status"
    - "fourth refusal -> exact aether insert-phase issue command from plan 06-compatible surface"
  prohibitions:
    - statement: "Failures for different normalized targets must never share strikes."
      status: required
      verification: interleaved-target table test
    - statement: "Do not add a mutable swarm-strike counter file."
      status: required
      verification: tests assert only existing swarm result and flags artifacts are written
---

<objective>
Restore the audited swarm three-strike safety valve using the result history and blocker system Aether already owns.

Purpose: repeatedly throwing workers at the same unsolved problem is not overnight progress. On the third failed attempt Aether should surface an architectural blocker, and a fourth should guide the owner toward a stabilisation phase.

Output: deterministic same-problem strike evaluation, deduplicated escalation, and pre-dispatch refusal after three strikes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-RESEARCH.md
@.planning/phases/198.3-overnight-stamina/198.3-PATTERNS.md
@.planning/phases/198.3-overnight-stamina/198.3-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive consecutive same-problem strikes from existing swarm result history</name>
  <files>cmd/swarm_strikes.go, cmd/swarm_strikes_test.go, cmd/swarm_cleanup_truth_test.go</files>
  <read_first>
    - cmd/swarm_cmd.go lines 1-110, 156-250, and 628 onward (manifest/result types, direct entry, and external finalize persistence)
    - cmd/swarm_cmd_test.go (fake invoker, run directories, direct/external fixtures)
    - cmd/swarm_cleanup_truth_test.go (which swarm artifacts are durable versus disposable)
    - cmd/blocker_snapshot.go from plan 01 (exact escalation source/count semantics)
    - repository tag `v5.3.3` swarm wrapper/command source for the historical three-strike intent
  </read_first>
  <behavior>
    - Target fingerprint is stable across case, repeated whitespace, and harmless surrounding punctuation while materially different problem text stays separate.
    - Only consecutive `failed`/`blocked` results for that fingerprint since its last `completed` result count.
    - Malformed/unrelated records never count for the target; filesystem order cannot change the result.
    - The evaluator returns prior swarm IDs/evidence and the next-attempt number without mutating any store.
  </behavior>
  <action>
    Create failing table tests for fingerprint equivalence/distinction and history sequences: zero, one, two, three failures; failed/completed/failed reset; interleaved different targets; blocked as strike; cancelled/non-terminal behavior according to existing status vocabulary; malformed files; and shuffled directory order. Seed result files through the existing result writer/helper wherever possible.

    Implement `cmd/swarm_strikes.go` as a pure history reader/evaluator over `.aether/data/swarms/*/result.json`. Add a version-compatible optional target fingerprint to newly written result records if the current shape lacks it; for older files derive from stored target. Sort records by their durable timestamp/run ID convention before evaluating. Stop the same-target streak at its last completed result and return evidence IDs in deterministic order.

    Do not write a counter or flag in this task. Keep cleanup behavior from deleting evidence that the current result contract already declares durable.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestSwarmTargetFingerprint|TestSwarmStrikeHistory|TestSwarm.*CleanupTruth' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Table tests prove case/spacing variants share one fingerprint and a different issue does not.
    - A successful result resets only its own target's consecutive streak.
    - Shuffling seeded filenames does not change strike count/evidence order.
    - Test-created artifacts contain no new strike-counter file.
    - `go test ./cmd/ -run 'SwarmTargetFingerprint|SwarmStrikeHistory|Swarm.*CleanupTruth' -count=1` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">History is derived from existing event records; the only schema addition is optional and backward-compatible.</reversibility>
  <done>Aether can deterministically tell how many consecutive times this exact swarm problem has failed without maintaining another counter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Escalate the third failure and refuse a fourth dispatch on both swarm lanes</name>
  <files>cmd/swarm_strikes.go, cmd/swarm_strikes_test.go, cmd/swarm_cmd.go, cmd/swarm_cmd_test.go, cmd/blocker_snapshot_test.go</files>
  <read_first>
    - cmd/swarm_cmd.go `runSwarmCompatibility`, plan-only/direct execution completion, and `runSwarmFinalize`
    - cmd/flag_cmds.go and another stable internal flag upsert writer (reuse store format, sanitization, timestamps, and dedup convention)
    - cmd/blocker_snapshot.go from plan 01
    - cmd/state_extra.go from plan 06 or its planned public shorthand contract (`aether insert-phase "issue"`)
    - cmd/swarm_cmd_test.go fake invoker call-count pattern
  </read_first>
  <behavior>
    - First and second same-target failed outcomes persist normally and create no escalation.
    - Persisting the third failed/blocked outcome upserts exactly one unresolved blocker with `Source: escalation`, a stable identity, and all three swarm IDs in evidence.
    - A subsequent same-target command refuses before invoking/planning workers and returns the exact guided `aether insert-phase "..."` next command.
    - Re-running evaluation does not duplicate escalation flags; a completed swarm resets the streak so a later attempt may dispatch.
    - Direct and external finalize paths produce the same result/flag shape.
  </behavior>
  <action>
    First add end-to-end command tests with a counting fake invoker: attempts 1–3 dispatch, the third creates one escalation, attempt 4 leaves the call count unchanged, another target still dispatches, and a seeded success resets the original target. Duplicate/replayed finalize must keep one flag. Add a parity test that feeds equivalent direct and external completion results and compares normalized strike/escalation outputs.

    Add a pre-dispatch guard to the shared earliest point of `runSwarmCompatibility`/plan-only routing that reads Task 1's history. At three prior strikes return a structured refusal (`architectural_concern` or existing compatible status), include evidence IDs, and render an exact shell-safe `aether insert-phase <quoted issue>` command. Do not create a fourth result record that would pretend workers ran.

    After direct and external completion persist a failed/blocked result, call one shared escalation helper. Upsert a normal unresolved blocker flag with `Source: "escalation"`, a stable target-derived ID/dedup key, plain-English description, and swarm IDs. Reuse flag sanitization/storage; never handwrite an incompatible flags file. Confirm plan 01's snapshot and status observe it.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestSwarmThreeStrike|TestSwarmFourthAttempt|TestSwarmStrike.*Parity|TestBlockerSnapshot.*Escalat' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Counting invoker observes calls for attempts 1, 2, 3 and no call for attempt 4.
    - Exactly one unresolved `Source=escalation` blocker exists after third failure and replayed finalize.
    - `readBlockerSnapshot` reports that escalation without swarm-specific code.
    - Refusal output contains the exact guided insert command and the three evidence IDs.
    - Direct/external finalization parity test exits 0.
  </acceptance_criteria>
  <reversibility rating="moderate">The policy adds a real blocker flag on third failure; it remains owner-resolvable through the normal flag lifecycle.</reversibility>
  <done>Repeated swarm failure becomes a visible architectural blocker, and Aether stops wasting a fourth dispatch on the same problem.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|---|---|---|
| free-form target -> fingerprint | Collisions could block unrelated work; trivial formatting could evade strikes | Conservative normalization with distinction tests; stable hash over normalized full text |
| persisted result files -> escalation | Malformed/spoofed history could fabricate three strikes | Validate status, target, timestamps/run IDs; ignore/report malformed rows; deterministic same-target evidence |
| target text -> shell recovery command | Quotes/metacharacters could produce unsafe pasted command | Use existing shell-quoting helper; assert adversarial target renders as one argument |
| replayed finalize -> flags | Duplicate callbacks could multiply blockers | Stable target-derived flag identity and idempotent upsert |
</threat_model>

<verification>
- `go test ./cmd/ -run 'SwarmTargetFingerprint|SwarmStrike|SwarmThreeStrike|SwarmFourthAttempt|BlockerSnapshot.*Escalat' -count=1`
- `go test ./cmd/ -run 'TestSwarm' -count=1`
- `go build ./cmd/aether`
- `go vet ./...`
</verification>

<success_criteria>
Swarm failures are counted from durable same-target history, the third creates one normal escalation flag visible to run/status, a fourth refuses before dispatch with guided recovery, and success safely resets the streak.
</success_criteria>

## Artifacts this phase produces (Plan 07)

| Symbol/path | Kind |
|---|---|
| target fingerprint/history evaluator | new pure policy helper |
| optional fingerprint in swarm result | compatible schema extension |
| third-strike escalation upsert | new runtime behavior |
| fourth-attempt pre-dispatch refusal | new runtime behavior |
| direct/external/reset/idempotency tests | regression proof |

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-07-SUMMARY.md` when done.
</output>
