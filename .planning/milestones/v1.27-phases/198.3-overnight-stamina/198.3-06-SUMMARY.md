---
phase: 198.3-overnight-stamina
plan: 06
subsystem: cli
tags: [go, cobra, tdd, command-guide, wrapper-parity]

# Dependency graph
requires: []
provides:
  - "Go-owned insert-phase request resolver for explicit flags, one-sentence shorthand, and a three-question TTY interview"
  - "Non-TTY input_required response with an exact executable example before any colony-state write"
  - "Byte-aligned Claude/OpenCode wrappers and Codex command-guide examples that delegate directly to the Go runtime"
affects: [198.3-07-swarm-three-strike-escalation, guided-insert-phase, command-wrapper-parity]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Resolve all command input through one pure merge boundary before mutation"
    - "Inject prompt sessions in tests while production reads Cobra's inherited input and reuses shared TTY detection"
    - "Keep managed wrappers as direct runtime passthroughs and enforce their parity in tests"

key-files:
  created: []
  modified:
    - cmd/state_extra.go
    - cmd/state_extra_test.go
    - cmd/command_guide.go
    - cmd/command_guide_test.go
    - cmd/visual_wrapper_contract_test.go
    - .aether/commands/insert-phase.yaml
    - .claude/commands/ant/insert-phase.md
    - .opencode/commands/ant/insert-phase.md

key-decisions:
  - "Explicit flags override shorthand-derived fields; omitted position resolves from CurrentPhase only after valid state is loaded."
  - "Constraints remain in the existing phase description rather than adding a new state schema field."
  - "Claude and OpenCode only pass $ARGUMENTS to the visual Go command; they do not reproduce the interview or naming algorithm."

patterns-established:
  - "Guided command resolver: explicit flags > positional issue defaults > TTY answers > structured input_required."
  - "Non-interactive missing input exits before state loading or writing and is proven with byte-for-byte state assertions."

requirements-completed: []

# Metrics
duration: 24min
completed: 2026-08-31
---

# Phase 198.3 Plan 06: Guided Insert Phase Summary

**One issue sentence now creates a bounded `Stabilize …` corrective phase, while scripts keep their explicit flag contract and every platform delegates to the same Go resolver.**

## Performance

- **Duration:** 24 min 18 sec
- **Started:** 2026-08-31T15:01:04Z
- **Completed:** 2026-08-31T15:25:22Z
- **Tasks:** 2 completed
- **Files modified:** 8

## Accomplishments

- Added a pure resolver that combines explicit flags, positional shorthand, and interactive answers before the existing phase insertion mutation.
- Added a deterministic `Stabilize` label capped at six useful issue words, plus optional constraints retained in the supported description field.
- Made zero-input automation return `input_required` and the exact `aether insert-phase "problem to stabilise"` example without changing state bytes.
- Restored the three-question TTY flow for issue, desired outcome, and constraints through an injectable prompt seam.
- Aligned canonical YAML, Claude, OpenCode, and Codex guide text around direct Go ownership and both shorthand and fully flagged examples.

## Task Commits

Each TDD task was committed with a RED test gate followed by its GREEN implementation:

1. **Task 1 RED: Guided input and no-mutation contracts** - `4e0f4eac` (test)
2. **Task 1 GREEN: Go-owned guided request resolution** - `595066ba` (feat)
3. **Task 2 RED: Guide and wrapper parity contracts** - `82520896` (test)
4. **Task 2 GREEN: Aligned platform guidance** - `1afb78b9` (feat)

## Files Created/Modified

- `cmd/state_extra.go` - Resolves shorthand, prompted, and explicit input; derives bounded names; returns non-mutating input guidance.
- `cmd/state_extra_test.go` - Covers explicit compatibility, override precedence, TTY prompting, cancellation, name bounds, constraints, and byte-identical no-input state.
- `cmd/command_guide.go` - Gives Codex exact shorthand and non-interactive forms while retaining literal runtime delegation.
- `cmd/command_guide_test.go` - Locks the insert-phase guide's examples and direct visual command.
- `cmd/visual_wrapper_contract_test.go` - Locks canonical/managed guidance and Claude/OpenCode body parity.
- `.aether/commands/insert-phase.yaml` - Documents guided and explicit usage as the canonical wrapper source.
- `.claude/commands/ant/insert-phase.md` - Direct visual passthrough with usable examples.
- `.opencode/commands/ant/insert-phase.md` - Byte-aligned direct visual passthrough with usable examples.

## Decisions Made

- Production TTY detection reuses `isTerminalWriter`, but command input comes from Cobra's inherited reader so tests and embedded callers do not depend on process-global stdin.
- A prompted desired outcome and constraints are appended to the phase description. This preserves every owner answer without introducing a second phase schema.
- Literal wrapper status remains unchanged: guide prose explains the richer Go behavior, while the run command stays `AETHER_OUTPUT_MODE=visual aether insert-phase $ARGUMENTS`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Narrowed an over-broad wrapper ownership assertion**
- **Found during:** Task 2 GREEN
- **Issue:** The RED test initially classified the existing safety warning about not hand-editing `COLONY_STATE.json` as a forbidden implementation detail, even though that warning is not a naming algorithm and should remain visible.
- **Fix:** Kept the safety warning and limited the forbidden markers to the actual Go resolver/helper names.
- **Files modified:** `cmd/visual_wrapper_contract_test.go`
- **Verification:** `go test ./cmd/ -run 'InsertPhase|VisualWrapperContract|Parity' -count=1` passed 103 tests after the correction.
- **Committed in:** `1afb78b9`

---

**Total deviations:** 1 auto-fixed bug
**Impact on plan:** The correction preserves an existing safety guard and does not expand scope.

## Issues Encountered

None beyond the expected TDD RED failures and the corrected assertion documented above.

## User Setup Required

None - no external service configuration required.

## Verification

- `go test ./cmd/ -run 'PhaseInsert|GuidedInsert|InsertPhase|VisualWrapperContract|Parity' -count=1` - 112 passed
- `go build ./cmd/aether` (isolated output path) - passed
- `go vet ./...` - passed

## TDD Gate Compliance

- Task 1: RED `4e0f4eac` precedes GREEN `595066ba`.
- Task 2: RED `82520896` precedes GREEN `1afb78b9`.

## Known Stubs

None. The modified runtime and guidance surfaces contain no placeholder or unwired behavior.

## Next Phase Readiness

- Plan 07 can return the exact guided `aether insert-phase "<issue>"` command after a fourth same-problem swarm attempt.
- No blockers remain for the guided corrective-phase recovery flow.

## Self-Check: PASSED

- All eight declared implementation/test/guidance files and this summary exist.
- All four task commits exist in RED/GREEN order.
- Final focused tests, build, vet, worktree status, and whitespace checks passed.

---
*Phase: 198.3-overnight-stamina*
*Completed: 2026-08-31*
