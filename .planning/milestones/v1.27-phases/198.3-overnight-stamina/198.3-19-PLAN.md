---
phase: 198.3-overnight-stamina
plan: 19
type: execute
wave: 2
depends_on:
  - 198.3-17
files_modified:
  - cmd/codex_continue_finalize.go
  - cmd/reviewer_artifact_198_3_test.go
autonomous: true
requirements:
  - STAM-01
  - STAM-02
gap_closure: true
must_haves:
  truths:
    - "Every Critical reviewer row has a non-empty title or description before it can be treated as evidence."
    - "A suggestion-only Critical row is invalid evidence and blocks both direct continue and external continue-finalize before advancement."
    - "Valid Critical findings still block and valid High findings remain report-only."
  artifacts:
    - path: "cmd/codex_continue_finalize.go"
      provides: "Shared finding-body validation and fail-closed review blocker"
      contains: "normalizeContinueReviewEvidence"
    - path: "cmd/reviewer_artifact_198_3_test.go"
      provides: "Suggestion-only Critical unit and advancement-boundary regressions"
  key_links:
    - from: "cmd/codex_continue_finalize.go"
      to: "externalContinueReviewReport"
      via: "body validation becomes an evidence blocker before external finalization"
      pattern: "continueReviewEvidenceBlockingIssues"
    - from: "cmd/codex_continue_finalize.go"
      to: "runCodexContinue"
      via: "the direct review flow consumes the same normalized EvidenceErrors"
      pattern: "normalizeContinueReviewEvidence"
---

<objective>
Reject malformed Critical reviewer findings before either continuation lane can advance a phase.

Purpose: Close the suggestion-only bypass while preserving the locked D-05 severity policy: valid Critical stops; valid High remains recorded and non-blocking.
Output: One shared body validator and direct/external phase-boundary tests.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
@.planning/phases/198.3-overnight-stamina/198.3-13-SUMMARY.md
@.planning/phases/198.3-overnight-stamina/198.3-17-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate the human-readable body of every structured review finding</name>
  <files>cmd/codex_continue_finalize.go, cmd/reviewer_artifact_198_3_test.go</files>
  <read_first>
    cmd/codex_continue_finalize.go
    cmd/reviewer_artifact_198_3_test.go
    cmd/codex_continue.go
    cmd/review_ledger.go
    .planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
    .planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
  </read_first>
  <behavior>
    - A finding with valid severity and non-empty title or description remains valid.
    - A finding whose title and description are both blank produces an index-specific evidence error even when suggestion is non-empty.
    - Suggestion is recovery advice, never the finding body and never sufficient to admit a row.
  </behavior>
  <action>In `normalizeContinueReviewEvidence`, validate each authoritative `artifacts.review.findings`/`issues` row after severity normalization and before adding it to `validFindings`: require at least one non-whitespace `Title` or `Description`, append a stable `findings[index] must include a non-empty title or description` evidence error otherwise, and exclude that row. Apply the same body rule to legacy top-level findings when no explicit artifact is present so a reviewer lane cannot bypass validation by omitting `artifacts.review`. Change `mergeCodexReviewFindings` so `Suggestion` is not an admission condition. Retain suggestions on otherwise valid rows. Per D-03/D-05, malformed evidence fails closed while valid High findings remain non-blocking.</action>
  <acceptance_criteria>
    - No finding with both blank `Title` and `Description` survives in `step.Findings`, regardless of `Suggestion`.
    - `step.EvidenceErrors` identifies the worker and exact finding index for a suggestion-only row.
    - `TestReviewerArtifactFindingBodyValidationMatrix` covers title-only, description-only, both, whitespace-only, suggestion-only Critical, and legacy top-level suggestion-only Critical.
    - Existing severity/score matrices and `TestHighReviewerFindingNeverBlocksSolelyOnLegacyBlockingBit` remain green.
    - `go test ./cmd -run 'Test(ReviewerArtifactFindingBodyValidationMatrix|ReviewerArtifactScoreAndSeverityValidationMatrix|HighReviewerFindingNeverBlocksSolelyOnLegacyBlockingBit)' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(ReviewerArtifactFindingBodyValidationMatrix|ReviewerArtifactScoreAndSeverityValidationMatrix|HighReviewerFindingNeverBlocksSolelyOnLegacyBlockingBit)' -count=1</automated>
  </verify>
  <done>Suggestion-only or blank-body review rows are invalid evidence, while valid severity behavior is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove malformed Critical evidence stops before phase credit on both lanes</name>
  <files>cmd/codex_continue_finalize.go, cmd/reviewer_artifact_198_3_test.go</files>
  <read_first>
    cmd/codex_continue_finalize.go
    cmd/reviewer_artifact_198_3_test.go
    cmd/codex_continue.go
    cmd/codex_continue_finalize_test.go
    cmd/phase_outcome.go
    .planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
    .planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
  </read_first>
  <behavior>
    - Direct continue returns a blocked result and leaves the phase unadvanced for a suggestion-only Critical artifact.
    - External continue-finalize does the same before final state mutation.
    - The blocking diagnostics name invalid review evidence rather than silently dropping the Critical row.
  </behavior>
  <action>Route the new body-validation error through the existing `continueReviewEvidenceBlockingIssues` seam used by `runCodexContinueReview` and `externalContinueReviewReport`. Keep the evidence error in persisted review/continue diagnostics and ensure advancement is decided only after that blocker is present. Add full boundary fixtures for direct continue and `runCodexContinueFinalize`, capture the pre-call colony state, and assert the phase status/current phase are byte-for-byte unchanged on rejection. Also retain one valid Critical and one valid High control to prove D-05 rather than replacing severity policy with a blanket finding stop.</action>
  <acceptance_criteria>
    - `TestSuggestionOnlyCriticalDirectFlowFailsBeforeAdvancement` returns `advanced=false`, `blocked=true`, preserves current phase/status, and includes the body-validation diagnostic.
    - `TestSuggestionOnlyCriticalExternalFinalizeFailsBeforeAdvancement` proves the same state and diagnostic assertions through the finalizer lane.
    - Valid Critical controls still block through the Critical-finding gate; valid High controls pass when no other blocker exists.
    - No test relies only on `continueReviewAutopilotSignals` after advancement; both invoke the actual continuation boundary.
    - `go test ./cmd -run 'Test(SuggestionOnlyCritical|ReviewerArtifactDirectFlowFailsClosed|ReviewerArtifactExternalFinalizeFailsClosed|HighReviewerFindingNeverBlocks)' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(SuggestionOnlyCritical|ReviewerArtifactDirectFlowFailsClosed|ReviewerArtifactExternalFinalizeFailsClosed|HighReviewerFindingNeverBlocks)' -count=1</automated>
  </verify>
  <done>Malformed Critical evidence stops direct and external continuation before any phase is credited.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(ReviewerArtifact|SuggestionOnlyCritical|HighReviewerFinding)' -count=1`
- `go vet ./cmd/...`
</verification>

<success_criteria>
- Suggestion-only Critical rows cannot be skipped by either review gate.
- The phase never advances before malformed review evidence is handled.
- Valid Critical/High behavior remains exactly D-05's contract.
</success_criteria>

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-19-SUMMARY.md` when done.
</output>
