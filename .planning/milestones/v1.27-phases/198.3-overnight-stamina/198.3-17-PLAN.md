---
phase: 198.3-overnight-stamina
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/pending_decision.go
  - cmd/autopilot_checkpoints.go
  - cmd/autopilot_checkpoints_test.go
  - cmd/criterion_owner_confirmation.go
  - cmd/codex_workflow_cmds.go
  - cmd/codex_continue.go
  - cmd/codex_continue_finalize.go
  - cmd/seal_ceremony_test.go
  - cmd/autopilot_report_test.go
  - cmd/handoff_decisions_cmd_test.go
  - cmd/pending_decision_test.go
  - cmd/run_autopilot_198_3_test.go
autonomous: true
requirements:
  - STAM-01
  - STAM-02
gap_closure: true
must_haves:
  truths:
    - "An owner approval applies only to the exact build or verification generation that produced the checkpoint."
    - "Replaying identical immutable work evidence deduplicates, while a new attempt or changed evidence creates a new unresolved owner decision."
    - "Generation-specific authorization rows retain one stable compatibility identity so the durable and legacy seal projections deduplicate without conflating different work generations."
    - "Both direct continue and external continue-finalize bind runtime checkpoints before phase advancement."
  artifacts:
    - path: "cmd/pending_decision.go"
      provides: "Persisted work-generation provenance on protected owner checkpoints"
      contains: "WorkGeneration"
    - path: "cmd/autopilot_checkpoints.go"
      provides: "Canonical generation digest and generation-aware checkpoint identity"
      contains: "autopilotCheckpointGeneration"
    - path: "cmd/autopilot_checkpoints_test.go"
      provides: "Changed-build and changed-verification adversarial regressions"
    - path: "cmd/codex_workflow_cmds.go"
      provides: "Compatibility-identity seal deduplication that preserves generation-specific durable rows"
      contains: "checkSealBlockers"
    - path: "cmd/seal_ceremony_test.go"
      provides: "Plan 12 seal capability and legacy/durable deduplication regression"
      contains: "TestSealCheckpointCapabilityDeduplicatesAndStaysTransient"
  key_links:
    - from: "cmd/autopilot_checkpoints.go"
      to: "cmd/build_attempt.go"
      via: "visual checkpoint generation loads the durable attempt/execution record and claims bytes"
      pattern: "buildAttemptRecord|claims_path"
    - from: "cmd/codex_continue.go"
      to: "cmd/autopilot_checkpoints.go"
      via: "direct continue passes manifest attempt/execution binding into runtime checkpoint materialization"
      pattern: "materializeRuntimeVerificationCheckpoints"
    - from: "cmd/codex_continue_finalize.go"
      to: "cmd/autopilot_checkpoints.go"
      via: "external finalizer passes the same immutable generation contract"
      pattern: "materializeRuntimeVerificationCheckpoints"
    - from: "cmd/criterion_owner_confirmation.go"
      to: "cmd/autopilot_checkpoints.go"
      via: "legacy owner-confirmation blockers derive the stable compatibility identity, never the generation-specific authorization row ID"
      pattern: "stableAutopilotCheckpointKey"
    - from: "cmd/codex_workflow_cmds.go"
      to: "cmd/autopilot_checkpoints.go"
      via: "seal suppresses the legacy projection by compatibility identity while retaining every distinct durable generation row and its capability command"
      pattern: "checkpointCompatibility"
---

<objective>
Bind every visual and runtime owner checkpoint to immutable work-generation evidence.

Purpose: Close the authorization freshness hole where changed work could inherit a resolved checkpoint from an older build, while preserving same-generation idempotency and D-02's queue-and-continue behavior.
Output: Persisted work-generation provenance, generation-aware keys, and direct/external regression coverage.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
@.planning/phases/198.3-overnight-stamina/198.3-11-SUMMARY.md

<interfaces>
`codexBuildManifest` already carries `AttemptID`, `AttemptPath`, and `ExecutionBinding`; `buildAttemptRecord` carries the durable attempt ID, run ID, manifest/workspace digests, and plan manifest. `PendingDecision` already carries protected-checkpoint scope and capability hashes. `ownerConfirmationSealBlockers` derives the pre-generation type/phase/subject/scope identity, and `checkSealBlockers` currently deduplicates that legacy projection against durable checkpoints by blocker ID. Extend those contracts rather than introducing a second checkpoint store: keep the pre-generation identity as an explicit compatibility identity, and add a separate generation-specific row identity for authorization.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make visual checkpoint identity fresh to its build generation</name>
  <files>cmd/pending_decision.go, cmd/autopilot_checkpoints.go, cmd/autopilot_checkpoints_test.go, cmd/criterion_owner_confirmation.go, cmd/codex_workflow_cmds.go, cmd/seal_ceremony_test.go</files>
  <read_first>
    cmd/pending_decision.go
    cmd/autopilot_checkpoints.go
    cmd/autopilot_checkpoints_test.go
    cmd/criterion_owner_confirmation.go
    cmd/codex_workflow_cmds.go
    cmd/seal_ceremony_test.go
    cmd/build_attempt.go
    cmd/codex_build.go
    cmd/build_execution_binding_test.go
    .planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
    .planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
  </read_first>
  <behavior>
    - Same phase, subject, session, goal, attempt/execution binding, and claims digest reuses one checkpoint row and rotates only its transient capability.
    - A different attempt ID, execution-binding digest, or claims digest creates a distinct unresolved checkpoint even when an older row for the same subject is resolved.
    - A visual checkpoint cannot be materialized when its durable attempt/execution provenance or claims bytes are missing or inconsistent.
    - The compatibility identity is unchanged across generations, but the persisted row ID and capability authorization identity change with `WorkGeneration`.
    - At seal, one legacy owner-confirmation projection is suppressed when a matching durable runtime row exists; distinct unresolved durable generations remain distinct blockers with their own fresh capability commands.
  </behavior>
  <action>Add persisted `CheckpointCompatibilityKey`, `CheckpointAttemptID`, `CheckpointExecutionBindingSHA256`, `CheckpointEvidenceSHA256`, and `WorkGeneration` fields to `PendingDecision`, plus a validated `autopilotCheckpointGeneration` contract in `cmd/autopilot_checkpoints.go`. Implement an explicit two-level identity: `stableAutopilotCheckpointKey(type, phase, subject, scope)` remains the compatibility key used only to match the legacy seal projection; a new generation-row key hashes that compatibility key with `WorkGeneration`, and the existing `CheckpointKey`/`ID` and capability resolver use only this generation-row key. Canonicalize `WorkGeneration` from the attempt ID, all immutable execution-binding fields (schema, run ID, attempt ID, manifest/workspace digests, execution owner), and a SHA-256 digest of the exact persisted evidence bytes. Reject empty or internally inconsistent generation fields before mutation. In `materializeVisualCheckpointFromBuildResult`, resolve both `buildResult["attempt"]` and `buildResult["claims_path"]` beneath `.aether/data`, load and validate the `buildAttemptRecord`, require its phase, attempt path, execution binding, and claims path to match the request, and hash the exact claims file bytes before upsert. In `autopilotCheckpointSealBlockersFromStore`, retain generation-specific durable blocker IDs and commands. Expose the durable rows' compatibility IDs to `checkSealBlockers` through a dedicated projection/helper; suppress `ownerConfirmationSealBlockers` only by that compatibility-ID set, never by the generation row ID and never by deduplicating two durable generations together. Update `TestSealCheckpointCapabilityDeduplicatesAndStaysTransient` to assert this contract: the legacy ID equals the compatibility ID, the durable blocker keeps its distinct row ID, one durable generation plus its legacy projection renders exactly one blocker/command, capabilities remain transient, and an exact replay stays deduplicated. Preserve D-01/D-02 and Plan 12: owner-only work queues, seal waits, and approval is valid only for its generation.</action>
  <acceptance_criteria>
    - `PendingDecision` serializes non-empty attempt, execution-binding, evidence-digest, and combined work-generation identifiers for protected visual/runtime rows.
    - Every protected row serializes both a stable compatibility key and a generation-specific `CheckpointKey`; capability resolution validates the latter, while seal legacy/durable matching uses only the former.
    - `upsertAutopilotCheckpoint` returns the existing row only when its generation identifier is identical; it never appends changed evidence to a resolved row.
    - `TestVisualCheckpointWorkGenerationRequiresFreshApproval` resolves generation A, proves an exact replay deduplicates, then proves changed attempt and changed claims each create a new unresolved row.
    - `TestVisualCheckpointRejectsMissingOrMismatchedGenerationEvidence` covers missing attempt path, missing claims, phase mismatch, and attempt/claims-path mismatch.
    - `TestSealCheckpointCapabilityDeduplicatesAndStaysTransient` proves stable compatibility deduplication and generation-specific authorization coexist without duplicate legacy commands or hidden durable generations.
    - `go test ./cmd -run 'Test(VisualCheckpointWorkGenerationRequiresFreshApproval|VisualCheckpointRejectsMissingOrMismatchedGenerationEvidence|VisualCheckpointMaterializesFromPersistedClaims|CheckpointCapability|SealCheckpointCapabilityDeduplicatesAndStaysTransient)' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(VisualCheckpointWorkGenerationRequiresFreshApproval|VisualCheckpointRejectsMissingOrMismatchedGenerationEvidence|VisualCheckpointMaterializesFromPersistedClaims|CheckpointCapability|SealCheckpointCapabilityDeduplicatesAndStaysTransient)' -count=1</automated>
  </verify>
  <done>Visual owner approval is cryptographically fresh to one validated build attempt and claims generation, with exact replay idempotency retained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind both runtime-checkpoint lanes to attempt and verification evidence</name>
  <files>cmd/autopilot_checkpoints.go, cmd/autopilot_checkpoints_test.go, cmd/codex_continue.go, cmd/codex_continue_finalize.go, cmd/seal_ceremony_test.go, cmd/autopilot_report_test.go, cmd/handoff_decisions_cmd_test.go, cmd/pending_decision_test.go, cmd/run_autopilot_198_3_test.go</files>
  <read_first>
    cmd/autopilot_checkpoints.go
    cmd/autopilot_checkpoints_test.go
    cmd/codex_continue.go
    cmd/codex_continue_finalize.go
    cmd/seal_ceremony_test.go
    cmd/autopilot_report_test.go
    cmd/handoff_decisions_cmd_test.go
    cmd/pending_decision_test.go
    cmd/run_autopilot_198_3_test.go
    cmd/codex_build.go
    cmd/build_attempt.go
    cmd/codex_continue_plan_test.go
    .planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
    .planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
  </read_first>
  <behavior>
    - Runtime criteria from the same attempt, execution binding, and canonical criterion evidence deduplicate across retry/finalize re-entry.
    - A new attempt or changed criterion result/evidence creates a new unresolved runtime checkpoint.
    - Direct continue and external continue-finalize produce the same generation identity for the same manifest and verification evidence.
    - Every production and test caller supplies a non-empty immutable generation; absence is rejected rather than mapped to a default generation.
  </behavior>
  <action>Change `materializeRuntimeVerificationCheckpoints` to require an explicit `autopilotCheckpointGeneration` derived by one helper from `manifest.Data.AttemptID`, `manifest.Data.ExecutionBinding`, and a deterministic SHA-256 projection of the criterion fields that authorize owner review (`TaskID`, criterion text, state/status/result, summary/evidence, and source paths; sort criteria and set-like evidence/source paths, and exclude timestamps, transient capabilities, and renderer text). Wire that generation through the production calls in `runCodexContinue` and `runCodexContinueFinalize`; both must validate the loaded build manifest's attempt and execution binding before any checkpoint mutation. Create one valid immutable test-generation fixture helper in `cmd/autopilot_checkpoints_test.go` and migrate every existing Go caller found by the repository audit: all calls in `cmd/autopilot_checkpoints_test.go`, both calls in `cmd/seal_ceremony_test.go`, the calls in `cmd/autopilot_report_test.go` and `cmd/handoff_decisions_cmd_test.go`, and both production calls. Also replace direct protected-checkpoint seeds in `cmd/pending_decision_test.go`, `cmd/handoff_decisions_cmd_test.go`, and `cmd/run_autopilot_198_3_test.go` with the same generation-requiring helper so no test creates an authorization row with zero/default provenance. Use distinct fixture attempt IDs/bindings when a test needs distinct rows, and identical immutable fixtures when it asserts replay deduplication. Add direct/external parity and changed-verification cases. Do not weaken capability checks, let the owner answer supply generation data, or use the compatibility key as the row authorization key.</action>
  <acceptance_criteria>
    - Both production calls to `materializeRuntimeVerificationCheckpoints` pass the manifest attempt/execution binding and canonical verification digest; no production call uses an empty/default generation.
    - Repository-wide caller audit covers `cmd/autopilot_checkpoints_test.go`, `cmd/seal_ceremony_test.go`, `cmd/autopilot_report_test.go`, `cmd/handoff_decisions_cmd_test.go`, `cmd/pending_decision_test.go`, `cmd/run_autopilot_198_3_test.go`, `cmd/codex_continue.go`, and `cmd/codex_continue_finalize.go`; every protected checkpoint creation carries a valid immutable fixture or production generation.
    - Canonical criterion ordering yields the same digest for semantically identical evidence and a different digest when status, evidence, or source paths change.
    - `TestRuntimeCheckpointWorkGenerationRequiresFreshApproval` proves exact replay deduplication and fresh unresolved rows for changed attempt and changed verification evidence.
    - `TestRuntimeCheckpointGenerationDirectExternalParity` proves direct continue and external finalization compute the same identity from identical durable inputs.
    - `TestSealCheckpointCapabilityDeduplicatesAndStaysTransient`, `TestAutopilotReportCheckpointCapabilityIsImmediateOnly`, `TestDecisionAnswerRejectsCheckpointAuthorizationFailures`, and the protected pending-decision/run fixtures all retain their existing behavior with valid generations.
    - `rg -n 'materializeRuntimeVerificationCheckpoints\(|upsertAutopilotCheckpoint\(' cmd --glob '*.go'` has no caller that omits generation provenance, and `go test ./cmd -run 'Test(RuntimeCheckpointWorkGenerationRequiresFreshApproval|RuntimeCheckpointGenerationDirectExternalParity|RuntimeVerificationDecisionIsIdempotent|CheckpointAnswerResolvesOriginalRow|FinalPhaseAdvancesWithOwnerCheckpointAndSealBlocks|SealCheckpointCapabilityDeduplicatesAndStaysTransient|AutopilotReportCheckpointCapabilityIsImmediateOnly|DecisionAnswerRejectsCheckpointAuthorizationFailures|PendingDecisionResolveRejectsProtectedCheckpoint|RunHeadlessQueuesVisualRuntimeAndReplanThenCompletes)' -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'Test(RuntimeCheckpointWorkGenerationRequiresFreshApproval|RuntimeCheckpointGenerationDirectExternalParity|RuntimeVerificationDecisionIsIdempotent|CheckpointAnswerResolvesOriginalRow|FinalPhaseAdvancesWithOwnerCheckpointAndSealBlocks|SealCheckpointCapabilityDeduplicatesAndStaysTransient|AutopilotReportCheckpointCapabilityIsImmediateOnly|DecisionAnswerRejectsCheckpointAuthorizationFailures|PendingDecisionResolveRejectsProtectedCheckpoint|RunHeadlessQueuesVisualRuntimeAndReplanThenCompletes)' -count=1</automated>
  </verify>
  <done>Runtime checkpoints on both continuation lanes are bound to the same immutable attempt plus verification generation and cannot inherit stale approval.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'Test(VisualCheckpoint|RuntimeCheckpoint|RuntimeVerificationDecision|CheckpointCapability|CheckpointAnswer|FinalPhaseAdvancesWithOwnerCheckpoint)' -count=1`
- `go test ./cmd -run 'Test(SealCheckpointCapabilityDeduplicatesAndStaysTransient|AutopilotReportCheckpointCapabilityIsImmediateOnly|DecisionAnswerRejectsCheckpointAuthorizationFailures|PendingDecisionResolveRejectsProtectedCheckpoint|RunHeadlessQueuesVisualRuntimeAndReplanThenCompletes)' -count=1`
- `go vet ./cmd/...`
</verification>

<success_criteria>
- A resolved checkpoint never authorizes changed work.
- Replaying the exact same durable generation remains idempotent.
- Missing/tampered generation provenance fails before pending-decision mutation.
- Direct and external continue lanes enforce the identical freshness contract.
- Seal keeps one stable legacy/durable compatibility match without collapsing distinct generation-specific authorization rows.
- Every existing production and test checkpoint caller supplies immutable generation data; the package compiles with no default-generation escape hatch.
</success_criteria>

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-17-SUMMARY.md` when done.
</output>
