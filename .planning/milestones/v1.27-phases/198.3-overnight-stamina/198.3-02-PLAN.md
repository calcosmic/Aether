---
phase: 198.3-overnight-stamina
plan: 02
type: execute
wave: 2
depends_on: ["198.3-01"]
files_modified:
  - cmd/codex_continue_plan.go
  - cmd/codex_continue.go
  - cmd/codex_continue_finalize.go
  - cmd/reviewer_artifact_198_3_test.go
  - cmd/autopilot_checkpoints.go
  - cmd/autopilot_checkpoints_test.go
  - cmd/handoff_decisions_cmd.go
  - cmd/pending_decision.go
  - cmd/codex_workflow_cmds.go
autonomous: true
requirements: [STAM-01, STAM-02]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "D-04: an Auditor that actually completed emits a validated 0-100 score; 59 is a stop signal and 60 passes; no Auditor means no score signal and no new dispatch."
    - "D-05: only Critical reviewer findings become stop signals; High findings persist for reporting even if a legacy blocking bit is true."
    - "D-02: runtime and visual owner checkpoints become deduplicated pending decisions; headless run may later queue-and-continue, interactive mode may pause, and seal always waits."
    - "Final-phase owner confirmation does not prevent phase advancement because run never seals; unresolved owner work remains enforced at seal."
  artifacts:
    - structured `artifacts.review` normalization across direct and external continue lanes
    - current-result autopilot review/checkpoint signals
    - cmd/autopilot_checkpoints.go
    - checkpoint resolution/seal tests
  key_links:
    - "WorkerResult.Artifacts/external completion Artifacts -> normalized Auditor score/findings -> continue result autopilot signals"
    - "build claims paths + needs_owner_confirmation criteria -> pending decisions -> decision-answer resolution -> seal blockers"
    - "plan 01 trigger codes -> typed current-result evidence, without enforcing mode yet"
  prohibitions:
    - statement: "Do not parse terminal prose as the authoritative Auditor score."
      status: required
      verification: structured artifact wins over conflicting summary test
    - statement: "Do not trust a worker-supplied ui_touched boolean."
      status: required
      verification: visual checkpoint derives from persisted created/modified paths
    - statement: "Do not make a queueable checkpoint a phase-advancement failure."
      status: required
      verification: final-phase advancement and seal-refusal pair test
---

<objective>
Preserve the exact current review and owner-checkpoint evidence that overnight policy needs, across both direct and hosted continue lanes.

Purpose: the run loop cannot safely distinguish Critical/High/score/owner-work while the hosted seam drops artifacts and owner work is represented as a generic blocker. This plan creates trustworthy typed signals but leaves headless versus interactive enforcement to plan 04.

Output: structured reviewer evidence, Critical-only review stop semantics, durable runtime/visual decisions, resolution, and seal enforcement.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-RESEARCH.md
@.planning/phases/198.3-overnight-stamina/198.3-PATTERNS.md
@.planning/phases/198.3-overnight-stamina/198.3-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry structured Auditor score and severity findings through both continue lanes</name>
  <files>cmd/codex_continue_plan.go, cmd/codex_continue.go, cmd/codex_continue_finalize.go, cmd/reviewer_artifact_198_3_test.go</files>
  <read_first>
    - pkg/codex/worker.go `WorkerResult`/claims artifact fields and provider conversion
    - cmd/internal_worker_adapter.go result serialization (prove artifacts already cross the bridge up to the Go completion payload)
    - cmd/codex_continue_plan.go `codexContinueExternalDispatch`, external dispatch planning, and reviewer brief construction
    - cmd/codex_continue.go `codexContinueWorkerFlowStep`, `codexReviewFinding`, `externalContinueReviewReport`, `appendReviewFindingsGateResult`, and direct reviewer result normalization
    - cmd/codex_continue_finalize.go external completion validation/normalization and result map construction
    - cmd/critics_bring_solutions_test.go (current `Blocking` versus severity expectations)
    - .codex/agents/aether-auditor.toml output fields as compatibility evidence; do not assume its top-level score currently reaches continue
  </read_first>
  <behavior>
    - Direct `WorkerResult.Artifacts["review"]` and hosted/external `artifacts.review` normalize to the same typed score/findings structure.
    - Only a completed Auditor supplies `OverallScore`; valid range is 0..100. Score 59 activates `auditor_score_below_floor`; 60 does not. No Auditor or failed/timed-out Auditor supplies no score trigger.
    - Critical severity activates `critical_review_finding`. High/Medium/Low/Info remain stored and reportable but never fail the review gate solely because `Blocking` is true.
    - Structured artifact is authoritative over conflicting free-form summary/output. Malformed artifact is surfaced as evidence error, never silently score 0.
  </behavior>
  <action>
    Create `cmd/reviewer_artifact_198_3_test.go` with direct and external table fixtures. Start with failing tests for Auditor 59/60/no-Auditor/failed-Auditor, Critical versus High with `blocking:true`, malformed/out-of-range score, and a conflict where summary says 95 but artifact says 59. Assert the two lanes produce equal normalized review evidence and trigger codes.

    Add `Artifacts map[string]json.RawMessage` to `codexContinueExternalDispatch` and carry it through external completion validation into normalized worker flow. Define a narrow `review` artifact schema containing `overall_score` and normalized findings/issues; add `OverallScore *int` (or an equivalent optional field) to current review/worker-flow evidence. Extend the Auditor task-specific continue brief/response contract to request `artifacts.review`; do not redesign every caste's base agent contract.

    Decode the named artifact for completed Auditor dispatches, validate score range/caste/status, normalize severity case-insensitively to the existing canonical vocabulary, and expose a JSON-safe current-result `autopilot_signals`/review projection. Preserve all High findings for plan 05's report.

    Change `externalContinueReviewReport` and `appendReviewFindingsGateResult` so a review finding blocks by severity Critical only. Do not alter deterministic watcher failure, failed worker status, or other established gates. Keep a compatibility fallback only if an existing real payload requires it; prove structured artifact takes precedence.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestReviewerArtifact|TestAuditorScore|TestCritical.*Finding|TestHigh.*Finding|TestCriticsBringSolutions' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Direct/external normalization equality test covers score and each severity.
    - Score 59 activates exactly the below-floor trigger; score 60 and absent Auditor do not.
    - A High `blocking:true` finding does not fail the review gate; changing Critical-only logic back to `Blocking || Critical` makes the test fail.
    - Critical activates the named trigger and remains in persisted review evidence.
    - Conflicting summary cannot override structured score; malformed/out-of-range data is explicit and never treated as zero.
    - Existing continue/reviewer tests still pass.
  </acceptance_criteria>
  <reversibility rating="moderate">The hosted completion schema gains an optional field and review blocking semantics narrow to the locked Critical-only contract.</reversibility>
  <done>The current continue result carries trustworthy optional Auditor score and severity evidence on both dispatch lanes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materialize visual/runtime owner checkpoints as durable decisions and enforce them only at the owner boundary</name>
  <files>cmd/autopilot_checkpoints.go, cmd/autopilot_checkpoints_test.go, cmd/handoff_decisions_cmd.go, cmd/pending_decision.go, cmd/codex_continue_finalize.go, cmd/codex_workflow_cmds.go</files>
  <read_first>
    - cmd/pending_decision.go in full and phase-research decision upsert helpers for idempotent scoped records
    - cmd/handoff_decisions_cmd.go `recordDecisionAnswer` and its matching semantics
    - cmd/criterion_owner_confirmation.go in full, including synthesized seal blockers
    - cmd/codex_workflow_cmds.go `checkSealBlockers` and final-phase advance/seal paths
    - cmd/floor_reviewer_free_gate_test.go owner-confirmation advancement tests
    - cmd/codex_build.go/codex build claims type and the current claims-path result field
    - cmd/codex_continue_finalize.go criterion evidence and final result construction
  </read_first>
  <behavior>
    - Created/modified UI paths in the persisted build claims produce one phase-scoped `visual-checkpoint` pending decision; a worker boolean alone cannot.
    - Current continue criteria in `needs_owner_confirmation` produce deduplicated `runtime-verification` decisions with criterion/evidence in metadata.
    - Replaying build/continue does not duplicate decisions; answering with the exact decision flow resolves the queued row and it disappears from unresolved output.
    - A phase, including the final phase, may advance with these owner checkpoints unresolved. `aether seal` refuses and names every unresolved checkpoint plus exact answer command.
    - The continue result exposes the new decision IDs/types so plan 04 can choose queue versus pause by mode.
  </behavior>
  <action>
    Write failing tests first: table-test the path classifier with UI/non-UI/docs/test paths; seed actual claims created/modified paths and assert a visual decision; seed `needs_owner_confirmation` criteria and assert runtime decisions; replay and assert one each; answer one and assert its unresolved row is gone; advance a final phase with unresolved owner work; then call seal and assert refusal includes the same decision IDs and exact recovery commands.

    Add `cmd/autopilot_checkpoints.go` with a pure UI path classifier, stable checkpoint key, and idempotent pending-decision upsert. Extend `PendingDecision` with optional typed metadata only if required (phase, source path/criterion, plan revision fields are allowed under `omitempty`). Store checkpoint type explicitly; do not use `Type: blocker` for queueable work.

    Expose visual materialization as a helper that accepts the post-build claims path/result; plan 04 owns calling it immediately after a successful build because this plan deliberately does not edit the main run loop. Hook runtime materialization to continue finalization after criterion evidence is known and add its IDs/types to the current result's `autopilot_signals`. Update `recordDecisionAnswer` to resolve a matching open checkpoint atomically/logically instead of appending a separate resolved record while leaving the original open.

    Remove the final-phase-only advancement exception for `needs_owner_confirmation` while retaining every deterministic failure gate. Extend `checkSealBlockers`/seal card to synthesize named blockers from unresolved `runtime-verification` and `visual-checkpoint` decisions and show the shared exact decision-answer command. This must preserve the rule that `aether run` never calls seal.
  </action>
  <verify>
    <automated>go test ./cmd/ -run 'TestVisualCheckpoint|TestRuntimeVerificationDecision|TestCheckpoint.*Resolve|TestFinalPhase.*Owner|TestSeal.*Checkpoint|TestReviewerFree' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - UI detection/materialization accepts a real claims path, reads created/modified paths, and has no production `ui_touched` trust flag; plan 04 is named as its live run-loop caller.
    - Replaying the same phase leaves exactly one visual and one per-criterion runtime decision.
    - `decision-answer` marks the matching original row resolved; unresolved list no longer shows it.
    - Final phase reaches completed/advanced state with unresolved owner checkpoints, but seal refuses until all are resolved.
    - Deterministic failed checks still block advancement in existing floor tests.
  </acceptance_criteria>
  <reversibility rating="moderate">New pending-decision types are backward-compatible, but advancement/seal responsibility is deliberately redistributed.</reversibility>
  <done>Owner-only checks survive as durable work without pretending the phase failed, while sealing still requires the owner to close them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|---|---|---|
| reviewer JSON -> unattended stop | Fabricated/malformed score or severity could stop/pass a run | Require completed Auditor caste, named structured artifact, score bounds, canonical severity, explicit malformed evidence |
| build claims paths -> visual checkpoint | Path traversal/noisy paths could flood decisions | Normalize relative paths, reject outside-root paths, stable dedup key, bounded evidence |
| pending decision answer -> owner boundary | Approximate matching could resolve wrong checkpoint | Stable ID/exact scoped match; preserve audit fields; tests with similar questions |
| phase advancement -> seal | Moving confirmation out of advancement could accidentally bypass owner entirely | Seal synthesizes unresolved checkpoint blockers and exact commands; paired advance/seal test |
</threat_model>

<verification>
- `go test ./cmd/ -run 'ReviewerArtifact|AuditorScore|Critical.*Finding|High.*Finding|VisualCheckpoint|RuntimeVerificationDecision|Checkpoint.*Resolve|FinalPhase.*Owner|Seal.*Checkpoint|ReviewerFree' -count=1`
- `go test ./cmd/ -run 'TestCodexContinue|TestSeal|TestPendingDecision|TestCriterionOwner' -count=1`
- `go build ./cmd/aether`
- `go vet ./...`
</verification>

<success_criteria>
Both continue lanes expose the same optional Auditor score and severity evidence, only score below 60 or Critical findings create stop signals, owner-only runtime/visual work is durably deduplicated and resolvable, phases advance, and seal remains the hard owner boundary.
</success_criteria>

## Artifacts this phase produces (Plan 02)

| Symbol/path | Kind |
|---|---|
| `artifacts.review` schema and normalization | new structured evidence contract |
| optional Auditor score/current review signals | new continue result fields |
| Critical-only finding gate | changed policy |
| visual/runtime checkpoint classifier/upserter | new durable owner-work path |
| checkpoint resolution + seal synthesis | changed owner boundary behavior |
| direct/external/final-phase/seal tests | regression proof |

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-02-SUMMARY.md` when done.
</output>
