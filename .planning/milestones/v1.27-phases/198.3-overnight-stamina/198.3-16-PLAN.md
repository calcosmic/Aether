---
phase: 198.3-overnight-stamina
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/swarm_strikes.go
  - cmd/swarm_cmd.go
  - cmd/state_extra.go
  - cmd/swarm_strikes_test.go
  - cmd/swarm_cmd_test.go
autonomous: true
requirements:
  - STAM-01
  - STAM-05
gap_closure: true
must_haves:
  truths:
    - "Three consecutive failed or blocked attempts still refuse a fourth dispatch for the exact normalized target."
    - "Running the emitted corrective phase command creates a verified, target-bound recovery epoch rather than fabricating a successful swarm result."
    - "After that verified recovery, the exact normalized target can dispatch again and the stale escalation blocker is resolved atomically before dispatch."
    - "Malformed, spoofed, cross-target, or uncommitted recovery records cannot reset strikes."
  artifacts:
    - path: "cmd/swarm_strikes.go"
      provides: "Typed corrective-phase recovery records and epoch-aware strike derivation"
    - path: "cmd/state_extra.go"
      provides: "Corrective phase insertion wiring that records a target-bound recovery epoch"
    - path: "cmd/swarm_cmd.go"
      provides: "Public pre-dispatch reconciliation of recovered escalation state"
    - path: "cmd/swarm_cmd_test.go"
      provides: "Three strikes, emitted recovery command, and exact-target retry end-to-end proof"
  key_links:
    - from: "swarmArchitecturalConcernResult"
      to: "phaseInsertCmd"
      via: "the exact emitted insert-phase target identifies the active escalation and records its recovery phase"
      pattern: "swarmInsertPhaseCommand"
    - from: "evaluateSwarmStrikeHistory"
      to: "runSwarmCompatibility"
      via: "verified recovery evidence clears only older same-target strikes and authorizes blocker reconciliation"
      pattern: "recovery"
---

<objective>
Give the three-strike swarm stop a real recovery epoch so the prescribed corrective action unlocks an exact-target retry.

Purpose: The current guard is a one-way wall; D-01/D-03 require a genuine architectural stop, but once the owner performs the emitted corrective replan, the system must recognize that new recovery context.
Output: Verified recovery records tied to inserted phase definitions, stale-blocker reconciliation, and a public end-to-end retry proof.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/198.3-overnight-stamina/198.3-CONTEXT.md
@.planning/phases/198.3-overnight-stamina/198.3-VERIFICATION.md
@.planning/phases/198.3-overnight-stamina/198.3-REVIEW.md
@.planning/phases/198.3-overnight-stamina/198.3-08-SUMMARY.md
@cmd/plan_revision.go
@pkg/colony/flags.go

<interfaces>
- `swarmResultRecord` under `swarms/<id>/result.json` is the sole durable history; no mutable strike counter exists.
- `evaluateSwarmStrikeHistory(store, target)` sorts valid same-target records and derives consecutive strikes.
- `swarmInsertPhaseCommand(target)` is the fourth-attempt recovery command shown to the owner.
- `phaseInsertCmd` atomically mutates `COLONY_STATE.json` and has both the before/after plan plus inserted phase available inside its callback.
- `planDefinitionHash` ignores phase/task execution status and is the source-of-truth definition fingerprint analog.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a validated recovery event to derived swarm history</name>
  <files>cmd/swarm_strikes.go, cmd/swarm_strikes_test.go</files>
  <read_first>
    - cmd/swarm_strikes.go
    - cmd/swarm_strikes_test.go
    - cmd/plan_revision.go
    - cmd/state_load.go
    - pkg/colony/colony.go
    - pkg/colony/flags.go
  </read_first>
  <behavior>
    - A typed `recovered` history row clears older failures only for its exact target and only when it proves a changed plan definition plus an inserted corrective phase still present in live state.
    - The recovery row is not a completed swarm and never contributes success evidence, worker counts, files, or tests.
    - Later failed/blocked results in the new epoch accumulate from zero; a later real completion keeps its existing reset behavior.
    - Malformed JSON, mismatched target/fingerprint, identical before/after hashes, missing phase fingerprint, absent inserted phase, or an uncommitted state change does not reset history.
  </behavior>
  <action>
Extend `swarmResultRecord` with optional typed recovery metadata: the exact target fingerprint, before/after plan-definition hashes, inserted phase identity/definition fingerprint, and authorization source `corrective-phase-insert`. Permit status `recovered` only with complete metadata. Add a helper that builds this event from the active three-strike escalation plus the pre-insert and post-insert plans; use a distinct valid swarm history ID and do not create a second strike database. In `evaluateSwarmStrikeHistory`, validate recovery records against normalized target and a matching corrective phase definition in the live plan before clearing older evidence. An event staged before a failed `COLONY_STATE.json` commit must remain inert because its phase is not present. Return latest verified recovery evidence in `swarmStrikeHistory` so the public guard can reconcile the old blocker. Preserve deterministic timestamp/ID ordering, legacy result compatibility, different-target isolation, and the read-only nature of evaluation.
  </action>
  <verify>
    <automated>rtk go test ./cmd -run 'TestSwarm(RecoveryEpoch|StrikeHistory|TargetFingerprint)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Derivation assertion: a valid same-target corrective recovery clears only preceding strikes; subsequent failures count normally.
    - Authenticity assertion: every malformed/spoofed/uncommitted recovery fixture leaves the original three strikes active.
    - Storage assertion: no strike counter or mutable retry database is introduced; the typed event lives in existing result history.
    - Compatibility assertion: legacy failures, real completion reset, timestamp ordering, and direct/external parity tests remain green.
  </acceptance_criteria>
  <done>Only a valid same-target corrective-phase recovery event resets derived strike history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the emitted corrective phase through exact-target public retry</name>
  <files>cmd/swarm_cmd.go, cmd/state_extra.go, cmd/swarm_cmd_test.go</files>
  <read_first>
    - cmd/swarm_cmd.go
    - cmd/state_extra.go
    - cmd/swarm_cmd_test.go
    - cmd/state_extra_test.go
    - cmd/swarm_strikes.go
    - cmd/codex_workflow_cmds.go
    - pkg/storage/storage.go
  </read_first>
  <behavior>
    - After the third same-target failure, both plan-only and direct fourth attempts refuse dispatch and emit the exact quoted `aether insert-phase` command.
    - Executing that public command for the escalated target persists a corrective phase and a verified recovery event; if staging the event fails, state insertion does not commit.
    - The next exact normalized target retry resolves the matching stale escalation blocker and dispatches workers; different targets and unrecovered epochs remain isolated.
    - Three new failures after recovery escalate again, proving a reset rather than permanent disablement.
  </behavior>
  <action>
Inside `phaseInsertCmd`'s atomic state callback, detect whether the positional issue matches an active same-scope swarm escalation with at least three derived strikes. After constructing the post-insert plan but before allowing the state commit, stage the typed recovery event from Task 1; return an error if the event cannot be durably staged so an unrecorded recovery phase is not reported as successful. Do not create recovery events for arbitrary inserts, explicit descriptions that do not match an escalation, or other targets. In `runSwarmCompatibility`, after read-only history evaluation and before any run/spawn creation, atomically resolve only the stable same-target escalation flag when `LatestRecovery` is verified; if reconciliation fails, return an error and dispatch nobody. Then allow the exact target through with strike count zero. Extend the existing three-strike public test to execute the emitted root command, retry the exact normalized target, assert real invoker activity, assert stale blocker resolution, and then prove a fresh three-strike cycle. Do not seed a fake `completed` result in this proof.
  </action>
  <verify>
    <automated>rtk go test ./cmd -run 'TestSwarmThreeStrike.*(Recovery|ExactTarget|Retry)|TestSwarmRecovery.*Public' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Public-flow assertion: the test performs three real failed/blocked attempts, observes refusal, executes the emitted insert-phase command, and then observes exact-target worker dispatch.
    - Blocker assertion: the pre-recovery escalation is resolved before retry dispatch and remains auditable with the recovery event/phase reference.
    - Failure assertion: recovery-event persistence or blocker reconciliation failure is non-zero and dispatches zero workers.
    - Epoch assertion: a later three failures in the recovered target's new epoch refuse again exactly once.
  </acceptance_criteria>
  <done>The emitted public corrective command enables exact-target redispatch, resolves stale escalation, and permits later re-escalation.</done>
</task>

</tasks>

<source_coverage_audit>
- GOAL: Plans 198.3-11 through 16 close all five verifier gap groups plus dry-run warning WR-01; completed Plans 01-10 retain the six-phase overnight proof and already-passing stamina behavior.
- REQ: STAM-01 is repaired by 11/12/13/15/16; STAM-02 by 11/13/14/15; STAM-03 and STAM-04 by 14; STAM-05 by 12/16; STAM-06 remains covered by completed Plan 10 and is protected by 14/15.
- RESEARCH: Owner checkpoint durability/capabilities map to 11/12; reviewer evidence to 13; reports and lesson-aware replanning to 14; phase-scoped provider readiness to 15; result-derived strike recovery to 16. No dependency, schema push, UI, or new service is introduced.
- CONTEXT: D-01-D-03 map to 11-16; D-04-D-05 to 13; D-07 and D-09-D-11 to 14; D-13-D-15 to 15; D-17-D-20 to 14. D-06, D-08, D-12, and D-16 remain satisfied by byte-preserved completed Plans 01-10 and are regression-checked by the focused/full suites.
- EXCLUDED: Disconnected `pauseAutopilotRun` and retry-record helper cleanup is intentionally deferred to Phase 198.4 exactly as the verifier directed; no deferred CONTEXT idea is pulled into this gap set.
</source_coverage_audit>

<verification>
Run `rtk go test ./cmd -run 'TestSwarm' -count=1`, then `rtk go test ./cmd -run 'TestGuidedInsertPhase' -count=1`, followed by `rtk go test ./cmd -count=1`. Inspect the public-flow fixture and confirm no synthetic completed result is written between refusal and successful retry.
</verification>

<success_criteria>
- Three strikes remain a hard, evidence-rich stop.
- The prescribed corrective phase establishes a verified, exact-target recovery epoch.
- A recovered retry updates live blocker state before dispatch and can later re-escalate.
- Spoofed or partially committed recovery evidence never unlocks dispatch.
</success_criteria>

<output>
Create `.planning/phases/198.3-overnight-stamina/198.3-16-SUMMARY.md` when done.
</output>
