---
phase: 198-put-the-thrown-away-data-back-on-screen
plan: 04
type: execute
wave: 2
depends_on: ["198-01", "198-03"]
files_modified:
  - cmd/closeout_direct_render.go
  - cmd/wrapper_path_parity_test.go
  - .claude/commands/ant/plan.md
  - .opencode/commands/ant/plan.md
  - .claude/commands/ant-plan.md
  - cmd/plan_wrapper_ceremony_test.go
autonomous: true
requirements: [SHOW-01, SHOW-02]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The chat-path closing screen for planning matches the direct command-line screen, including how confident the plan is and how many planning rounds it took (SHOW-01, SHOW-02, D-12)."
    - "The chat-path closing screen for finishing a project matches the direct command-line screen, including any override the owner recorded (SHOW-01, D-12)."
    - "All three workflows named in the roadmap — plan, continue and finish — are covered by one named parity test (SHOW-01)."
  artifacts:
    - "cmd/closeout_direct_render.go — plan and seal branches, closeoutPlanRenderInputs, closeoutSealRenderInputs"
    - "cmd/wrapper_path_parity_test.go — plan and seal rows in TestWrapperPathRendersSameCeremonyAsDirectPath"
  key_links:
    - "closeoutDirectVisual -> renderPlanVisual (plan) and renderSealVisual (seal)"
    - "plan finalize result keys confidence / planning_loop / gaps / unresolved_clarifications -> the chat screen"
  prohibitions:
    - statement: "This plan does not edit cmd/codex_visuals.go; any shape normalisation the round-tripped map needs lives in cmd/closeout_direct_render.go, because a same-wave plan owns that file."
      status: enforced
      verification: "git diff --name-only HEAD -- cmd/codex_visuals.go produces no output for this plan's commits"
    - statement: "No key the plan or seal finalizer already writes is dropped on the way to the screen."
      status: enforced
      verification: "go test ./cmd -run TestWrapperPathRendersSameCeremonyAsDirectPath -count=1"
---

<objective>
Expand the tracer to the other two workflows the roadmap names: planning and finishing. After
this plan, `TestWrapperPathRendersSameCeremonyAsDirectPath` covers plan, continue and seal —
which is criterion 1 of the phase, closed.

Purpose: plan 198-01 proved one renderer works for one workflow. Plan confidence, the planning
round count, gaps, clarification warnings and seal's override reporting are all computed today
and all die at the same seam. Routing plan and seal through the direct renderers restores them
without writing a second copy of either screen.

Output: the plan and seal branches of `closeoutDirectVisual`, the two extra parity rows, and
the three plan-wrapper copies updated together.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-put-the-thrown-away-data-back-on-screen/198-CONTEXT.md
@.planning/phases/198-put-the-thrown-away-data-back-on-screen/198-RESEARCH.md
@.planning/phases/198-put-the-thrown-away-data-back-on-screen/198-PATTERNS.md
@.planning/phases/198-put-the-thrown-away-data-back-on-screen/198-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route planning and finishing through the same one renderer</name>
  <files>cmd/closeout_direct_render.go, cmd/wrapper_path_parity_test.go</files>
  <read_first>
    - cmd/closeout_direct_render.go (created by plan 198-01 — the `closeoutDirectVisual` switch and the continue branch to copy)
    - cmd/codex_visuals.go lines 1399-1525 (`renderPlanVisual`, including the confidence block at ~1488-1500 and the planning-loop dual-type branches) and 2595-2700 (`renderSealVisual`)
    - cmd/codex_plan_finalize.go lines 444-495 (the completed-plan result map) and 863-882 (the mid-loop `requires_next_iteration` variant)
    - cmd/codex_workflow_cmds.go around line 775-786 (the seal result map) and the `awaiting_owner_confirmation` key added by plan 198-03
    - .planning/phases/198-put-the-thrown-away-data-back-on-screen/198-RESEARCH.md Q1 (the per-key carried/rendered tables for plan and seal)
  </read_first>
  <behavior>
    - A plan-finalize result round-tripped through JSON renders byte-equal to renderPlanVisual's output for the same typed result, including the confidence percentage and the planning-round line.
    - A mid-loop plan result carrying requires_next_iteration renders the same on both paths.
    - A seal result round-tripped through JSON renders byte-equal to renderSealVisual's output, including the override fields when the owner force-finished.
    - A seal result that is still awaiting the owner's answer renders the same on both paths.
    - A completion file whose payload is missing the keys a branch needs reports not-handled, so the generic renderer still produces a coherent screen rather than a half one.
  </behavior>
  <action>
1. Add the `plan` branch to `closeoutDirectVisual`: pass the raw completion map straight to
   `renderPlanVisual`. Add `closeoutPlanRenderInputs` only if a value needs reshaping; keep any
   such normalisation inside cmd/closeout_direct_render.go — do NOT edit cmd/codex_visuals.go,
   which a same-wave plan owns.

2. Add the `seal` branch: resolve `renderSealVisual`'s `state` from the already-loaded live
   colony state and its `summaryPath` from the completion map's own summary key, falling back
   to the standard summary document path. Add `closeoutSealRenderInputs` for that resolution.

3. Where the round-tripped map shape differs from the typed shape (nested structs arriving as
   `map[string]interface{}`, slices of structs as `[]interface{}`), normalise in
   `cmd/closeout_direct_render.go` before calling the renderer, following the type-switch
   discipline `renderContinueWorkerFlowValue` established. Normalise only what the parity test
   proves is needed — no speculative conversions.

4. Extend `TestWrapperPathRendersSameCeremonyAsDirectPath` with rows for: a completed plan, a
   mid-loop plan, a completed seal, a force-finished seal, and a seal awaiting the owner's
   answer. Build each fixture in runtime shape and round-trip it through `json.Marshal` /
   `json.Unmarshal` for the closeout side, exactly as the continue rows do.

5. Add `TestCloseoutRefusesToHalfRenderAnIncompletePayload` — a completion map missing the keys
   a branch needs reports not-handled rather than rendering a partial screen.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./cmd &amp;&amp; go test ./cmd -run 'TestWrapperPathRendersSameCeremonyAsDirectPath|TestCloseoutRefusesToHalfRenderAnIncompletePayload|TestCloseoutUnhandledWorkflowsKeepTheGenericRenderer' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestWrapperPathRendersSameCeremonyAsDirectPath|TestCloseoutRefusesToHalfRenderAnIncompletePayload' -count=1` exits 0.
    - `TestWrapperPathRendersSameCeremonyAsDirectPath` contains rows for all three workflows: `plan`, `continue`, `seal`.
    - `git diff --name-only` for this plan's commits does not list `cmd/codex_visuals.go`.
    - `go build ./...` and `go vet ./cmd` exit 0.
  </acceptance_criteria>
  <done>Planning and finishing show the same screen in the chat as on the command line, proven by the same named test that already covers continuing.</done>
</task>

<task type="auto">
  <name>Task 2: Update all three planning-wrapper copies and lock them</name>
  <files>.claude/commands/ant/plan.md, .opencode/commands/ant/plan.md, .claude/commands/ant-plan.md, cmd/plan_wrapper_ceremony_test.go</files>
  <read_first>
    - .claude/commands/ant/plan.md lines 130-155 (the finalize + closeout call pair)
    - cmd/plan_wrapper_ceremony_test.go (the required-substring loop)
    - cmd/lifecycle_wrapper_contract_test.go around line 135 (`TestLifecycleFlatMirrorsMatchCanonical`)
    - .planning/phases/197-one-answer-to-what-next/197-CONTEXT.md S-04 (byte-identical triplets) and S-05 (plain English)
  </read_first>
  <action>
Update the planning wrapper prose so it states, in plain English, that the closing screen the
owner sees in the chat is now the same one the direct command prints — including how confident
the plan is and how many rounds of planning it took. Edit all three committed copies
byte-identically in this one task: `.claude/commands/ant/plan.md`,
`.opencode/commands/ant/plan.md`, and the flat mirror `.claude/commands/ant-plan.md`.

Do not change any command string the wrapper executes; the existing finalize and closeout call
pair stays exactly as it is. Extend `cmd/plan_wrapper_ceremony_test.go`'s `required` slice with
the new substring so a copy that drifts fails by name.
  </action>
  <verify>
    <automated>go test ./cmd -run 'TestPlanWrapperCeremonyContract|TestLifecycleFlatMirrorsMatchCanonical' -count=1 && diff .claude/commands/ant/plan.md .opencode/commands/ant/plan.md && diff .claude/commands/ant/plan.md .claude/commands/ant-plan.md</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestPlanWrapperCeremonyContract|TestLifecycleFlatMirrorsMatchCanonical' -count=1` exits 0.
    - `diff .claude/commands/ant/plan.md .opencode/commands/ant/plan.md` exits 0.
    - `diff .claude/commands/ant/plan.md .claude/commands/ant-plan.md` exits 0.
    - The two command strings the wrapper executes for finalize and closeout are unchanged from before this plan.
  </acceptance_criteria>
  <done>All three copies of the planning instructions say the same thing about the closing screen, and a test fails if one drifts.</done>
</task>

</tasks>

## Artifacts this phase produces

| Kind | Name | Location |
|---|---|---|
| function | `closeoutPlanRenderInputs(raw map[string]interface{}) (map[string]interface{}, bool)` | cmd/closeout_direct_render.go |
| function | `closeoutSealRenderInputs(raw map[string]interface{}, state colony.ColonyState) (string, bool)` | cmd/closeout_direct_render.go |
| test row | `plan` and `seal` rows in `TestWrapperPathRendersSameCeremonyAsDirectPath` | cmd/wrapper_path_parity_test.go |
| test | `TestCloseoutRefusesToHalfRenderAnIncompletePayload` | cmd/wrapper_path_parity_test.go |
| wrapper text | new required substring in the three plan-wrapper copies | .claude/commands/ant/plan.md, .opencode/commands/ant/plan.md, .claude/commands/ant-plan.md |

<verification>
- `go build ./...` and `go vet ./cmd` exit 0.
- `go test ./cmd -run 'TestWrapperPathRendersSameCeremonyAsDirectPath|TestCloseoutRefusesToHalfRenderAnIncompletePayload|TestCloseoutUnhandledWorkflowsKeepTheGenericRenderer|TestPlanWrapperCeremonyContract|TestSealWrapperCeremonyContract|TestContinueWrapperCeremonyContract|TestLifecycleFlatMirrorsMatchCanonical' -count=1` exits 0.
- `cmd/codex_visuals.go` is untouched by this plan.
</verification>

<success_criteria>
Roadmap criterion 1 is closed: what the owner sees in the chat for planning, continuing and
finishing matches the direct command-line view, proven by one named test covering all three.
</success_criteria>

<output>
Create `.planning/phases/198-put-the-thrown-away-data-back-on-screen/198-04-SUMMARY.md` when done
</output>
