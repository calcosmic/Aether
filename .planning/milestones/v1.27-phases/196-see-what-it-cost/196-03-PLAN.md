---
phase: 196-see-what-it-cost
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/wrapper_usage_claude.go
  - cmd/wrapper_usage_claude_test.go
  - cmd/testdata/spend/claude/README.md
  - cmd/testdata/spend/claude/transcript-with-duplicates.jsonl
autonomous: true
requirements: [COST-02]

must_haves:
  truths:
    - "D-04 as corrected: the parser deduplicates usage by the message identifier, across the two line types that actually carry usage — assistant and user."
    - "D-04 as corrected: the fixture is a real captured transcript that still CONTAINS the repeated message identifiers, so a regression to naive line-summing fails."
    - "The naive and deduplicated totals are re-derived from the actual capture; the figures from the superseded reading are not carried forward."
    - "A usage block sitting inside a tool description is not counted — the parser reads structure, not substrings."
    - "A transcript path outside the platform's own projects directory is refused, and the real directory is never read during tests."
    - "Rows read from a transcript are never tagged provider-grade."
    - "This parser is wired into a real dispatch path by plan 196-05, in this same phase — it does not sit unwired."
  artifacts:
    - path: "cmd/wrapper_usage_claude.go"
      provides: "The Claude Code transcript reader: deduplicated, bounded, path-contained"
    - path: "cmd/testdata/spend/claude/transcript-with-duplicates.jsonl"
      provides: "A real captured transcript whose duplicate usage blocks are preserved on purpose"
  key_links:
    - from: "cmd/wrapper_usage_claude.go"
      to: "cmd/spend_session_capture.go"
      via: "the same projects-directory containment rule the session record already enforces"
      pattern: "validateSpendTranscriptPath"
    - from: "cmd/wrapper_usage_claude.go"
      to: "pkg/codex/usage.go"
      via: "returns WorkerUsage tagged session-transcript, never provider"
      pattern: "UsageSourceSessionTranscript"
    - from: "cmd/wrapper_usage_claude.go"
      to: "cmd/testdata/spend/claude/transcript-with-duplicates.jsonl"
      via: "deduplication keyed on the message identifier, corroborated by the request identifier"
      pattern: "message.id"
---

<objective>
Write the Claude Code transcript reader, and prove with a real fixture that it
does not count the same tokens two or three times.

Purpose: this is the half of criterion 2 that does not exist anywhere. The
duplication is real — measured on a real transcript, fifty usage rows carried
only twenty-five distinct message identifiers, one identifier appearing four
times with an identical output figure — and the deleted branch's fixture listed
each block once, so its tests could never have caught it. A fixture that cannot
fail is the same structural failure as the 186x undercount, in this same
subsystem, one phase later.
Output: the reader, a real captured fixture that keeps its repeats, and the six
requirements harvested from the deleted branch plus the deduplication one.
</objective>

<execution_context>
@/Users/callumcowie/.codex-5x/get-shit-done/workflows/execute-plan.md
@/Users/callumcowie/.codex-5x/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/196-see-what-it-cost/196-CONTEXT.md
@.planning/phases/196-see-what-it-cost/196-SALVAGE-ASSESSMENT.md
@pkg/codex/usage.go
@cmd/spend_session_capture.go
</context>

<interfaces>
The deleted branch `worktree-agent-a59fd3ee68644ea21` referenced four symbols
that have never existed in any ref. Do NOT restore that branch's files — it is
deleted in plan 196-08 with its written reason. Its six requirements are copied
into this plan's behaviour list, which is the part that had value.

Write a fresh reader in `cmd/wrapper_usage_claude.go` exposing: a parse entry
point taking a transcript path, and a bounded variant taking an explicit byte
bound. Both return worker-keyed usage rows and an error. Rows are
`codex.WorkerUsage` tagged as session-transcript.

Read D-04 as CORRECTED in `196-CONTEXT.md` before writing anything. The original
reading of this defect was wrong about its mechanism and the correction changes
what the parser keys on: usage-bearing lines are only the assistant and user
types, the identifier the earlier reading proposed is essentially absent from
them, and the repetition happens within the assistant type keyed on the message
identifier. A parser built to the superseded reading would deduplicate nothing
and reproduce the whole overcount, while its fixture-integrity test could never
be satisfied from a real capture.

Reuse the containment rule already implemented for the session record rather
than writing a second one; plan 196-05 extracts the shared helper, so here the
requirement is only that the same rule is enforced and the same kind of error is
returned.

Nothing calls this reader yet. Plan 196-05 wires it into the finalize path in
this same phase and plan 196-08 fails if it is still unreached.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Capture a real transcript fixture that keeps its repeated message identifiers</name>
  <files>cmd/testdata/spend/claude/transcript-with-duplicates.jsonl, cmd/testdata/spend/claude/README.md, cmd/wrapper_usage_claude_test.go</files>
  <read_first>
    - .planning/phases/196-see-what-it-cost/196-CONTEXT.md — D-04 as CORRECTED in full: which line types carry usage, what the key is, why the earlier reading was wrong, and the instruction to re-derive both totals rather than reuse the superseded ones
    - .planning/phases/196-see-what-it-cost/196-SALVAGE-ASSESSMENT.md — Branch 3, "The defect the fixture actively conceals"; note that its line-type and key claims are SUPERSEDED by D-04 as corrected, and only its account of the trap's existence and size survives
    - cmd/testdata/spend/opencode — not yet present in the tree; keep the new fixture in its own claude subdirectory so plan 196-05's fixtures cannot collide with it
  </read_first>
  <behavior>
    - The fixture is captured from a real transcript under the platform's own projects directory on this machine, not hand-written.
    - At least one message identifier occurs more than once among the fixture's usage-bearing lines, and usage-bearing lines of both surviving types — assistant and user — are present.
    - Every absolute path, prompt body, file content and personally identifying string is redacted, while the fields that carry the duplication — the message identifier, the request identifier and the line type — survive untouched.
    - A fixture-integrity test fails if a future edit removes the repetition, so the regression fixture cannot quietly become a fixture that cannot fail.
    - The README records where the fixture came from, what was redacted, and the two totals as measured on this capture: the sum of every usage-bearing occurrence, and the deduplicated truth.
  </behavior>
  <action>
Capture the fixture from a real transcript. First re-measure the capture on this
machine rather than trusting any figure written down before: count usage-bearing
lines by their top-level line type, count occurrences per message identifier, and
record both the sum of every usage-bearing occurrence and the deduplicated total.
Those two numbers, measured here, are the literals the tests use. The figures in
the first version of D-04 came from a mistaken reading and must not be reused.

Select a contiguous slice of one session containing at least one message
identifier that repeats across usage-bearing lines, at least one that appears
once, and usage-bearing lines of both surviving types. Keep the slice small
enough to read but large enough that the repetition is genuine rather than
constructed.

Redact in place: replace absolute paths with a placeholder root, replace prompt
and result text with short filler, and remove anything identifying. Do not
renumber, reorder or normalise the usage blocks, the message identifiers, the
request identifiers or the line types — those are the data under test. A fixture
that has been tidied into one block per message is worthless here; that is
precisely the shape being guarded against.

Write the README beside it stating the source, the redaction rules applied, and
both totals as literals, each labelled with how it was measured.

Write the fixture-integrity test now, before the reader exists: it reads the
fixture directly, counts occurrences per message identifier among usage-bearing
lines, and fails unless at least one identifier occurs more than once and lines
of both surviving types are present. This test protects the other tests, and it
is the one that would have caught the deleted branch.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestClaudeTranscriptFixtureContainsRepeatedMessageIDs' -count=1` passes
    - The test fails if the fixture is edited so every message identifier appears once
    - The fixture contains usage-bearing lines of both surviving types
    - The README states both totals as literals, measured on this capture, and names the redaction rules
    - Neither literal is one of the two figures recorded before D-04 was corrected
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestClaudeTranscriptFixtureContainsRepeatedMessageIDs' -count=1</automated>
  </verify>
  <done>A real, redacted transcript is committed with its repetition intact, its two totals measured here, and a test that fails the moment the repetition is lost.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the reader, deduplicated and bounded</name>
  <files>cmd/wrapper_usage_claude.go, cmd/wrapper_usage_claude_test.go</files>
  <read_first>
    - .planning/phases/196-see-what-it-cost/196-CONTEXT.md — D-04 as corrected, and the standing constraint against a test that re-implements the code's own arithmetic
    - .planning/phases/196-see-what-it-cost/196-03-PLAN.md — this plan's Task 1 output: the fixture's measured naive and deduplicated totals, which are this task's expected literals
    - .planning/phases/196-see-what-it-cost/196-SALVAGE-ASSESSMENT.md — Branch 3, "What is worth keeping": the six harvested requirements this task must satisfy, noting its line-type and key claims are superseded
    - pkg/codex/usage.go — WorkerUsage, UsageSourceSessionTranscript, and why a transcript row is never provider-grade
    - cmd/spend_session_capture.go — validateSpendTranscriptPath: the containment rule, its symlink evaluation, and its relative-path rejection
    - cmd/internal_worker_usage_test.go — literal-expectation discipline
  </read_first>
  <behavior>
    - Usage counted once per message identifier, last occurrence winning, across both usage-bearing line types.
    - A usage-bearing line carrying no message identifier is still counted once, rather than dropped or merged with another.
    - The deduplicated total for the fixture equals the literal recorded in the fixture README, and the naive sum of every occurrence is strictly larger — a reader that stops deduplicating produces the larger figure and fails.
    - A usage block appearing inside a tool description is not counted: matching is structural, not a substring search.
    - A transcript path outside the platform's projects directory is refused with an error, exercised through a temporary home directory so the real directory is never read.
    - An oversized line is skipped and the rest of the file is still read.
    - An absent file returns no rows and no error.
    - An explicit byte bound stops the read early rather than reading the whole file.
    - Every returned row is tagged session-transcript and never provider-grade.
    - The legacy colon-shaped usage block is tolerated if encountered but is not the shape the reader targets; it appears in older transcripts and not once in a current one.
  </behavior>
  <action>
Create the reader file with its real function signatures returning empty results
first, so the package still compiles, then write the failing tests and record
RED. Do not commit a test file that references a symbol which does not exist —
that is exactly what made the deleted branch unmergeable: it turned a green
repository red rather than failing a check.

Implement the read as a line-by-line scan with a byte bound and a per-line size
bound. Decode each line as structured data and take the usage block from its
declared position, so a block quoted inside a tool description is never matched.

Read the line type from the TOP-LEVEL field, never from a nested type field on a
content block. Confusing the two is what produced the superseded reading of this
defect, in the decision that exists to require structure over substrings — and it
would silently widen the accepted set to line types that carry no usage at all.

Key each usage block by its message identifier and let the last occurrence win:
the same assistant message is republished as the turn progresses rather than
representing new spend. Corroborate with the request identifier where present.
Sum across distinct identifiers only, and count an identifier-less usage-bearing
line once on its own rather than folding it into another.

Enforce the projects-directory containment rule before opening anything, using
the same relative-path containment with symlink evaluation the session record
already uses — never a plain prefix comparison on the raw strings.

Tag every row as read from a session transcript. Do not reach for the
provider-grade tag: only the provider parser may set it, and a transcript's
accounting semantics are undocumented by the vendor.

State every expected number in the tests as a literal. Do not build an expected
total by calling the reader's own summation or the billed-total helper.
  </action>
  <acceptance_criteria>
    - `go test ./cmd -run 'TestClaudeTranscriptDeduplicatesUsageByMessageID|TestClaudeTranscriptReadsBothUsageBearingLineTypes|TestClaudeTranscriptIgnoresUsageInsideToolDescription|TestClaudeTranscriptRefusesPathOutsideProjectsRoot|TestClaudeTranscriptSkipsOversizedLineAndContinues|TestClaudeTranscriptAbsentFileIsEmptyAndNoError|TestClaudeTranscriptByteBoundStopsEarly|TestClaudeTranscriptRowsAreNeverProviderGrade|TestClaudeTranscriptLegacyColonFormIsTolerated' -count=1` passes
    - The deduplication test asserts both the deduplicated literal and that the naive occurrence sum is strictly larger, so removing deduplication fails it
    - Both literals match the README's measured figures and neither is one of the two superseded ones
    - A line-type assertion reads the top-level field, and a fixture line whose nested content block carries a differently-named type does not change the result.
      The decoy line MUST be usage-bearing, and its TOP-LEVEL type must be OUTSIDE the accepted set while its NESTED type is INSIDE it — otherwise a naive
      substring reader skips the line for its own reasons and the assertion proves nothing. Constructed the other way round this criterion is vacuous, which
      is the exact failure this whole plan exists to prevent, one layer up.
    - The path-refusal test sets a temporary home directory and never reads the real projects directory
    - No expected value in the test file is produced by calling the reader or the billed-total helper
    - `go vet ./cmd` is clean
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd -run 'TestClaudeTranscript' -count=1</automated>
  </verify>
  <done>The Claude transcript reader returns one honest figure per worker, and a regression to naive summing fails against a real fixture.</done>
</task>

</tasks>

<verification>
- `go test ./cmd -run 'TestClaudeTranscript' -count=1`
- `go build ./cmd/aether && go vet ./cmd`
- Read the fixture and confirm by eye that one message identifier genuinely appears more than once among usage-bearing lines, and that both surviving line types are represented.
</verification>

<success_criteria>
- The reader deduplicates by message identifier across both usage-bearing line types.
- The fixture would fail a naive summing implementation, against literals measured from this capture.
- All six requirements harvested from the deleted branch are satisfied by named tests.
</success_criteria>

<output>
Create `.planning/phases/196-see-what-it-cost/196-03-SUMMARY.md` when done.
</output>
