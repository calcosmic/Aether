---
phase: 163.2-ts-host-preflight-configurability-make-the-worker-platform-p
plan: 01
subsystem: infra
tags: [go, preflight, caching, cost-policy, ttl]

# Dependency graph
requires: []
provides:
  - "cmd/preflight_cache.go: TTL-bounded, per-platform preflight success cache with skip switch and notices"
  - "8 public helpers: resolvedPreflightCacheTTL, preflightSkipRequested, preflightCacheEntryIsFresh, loadFreshPreflightCache, recordPreflightSuccess, clearPreflightCache, emitPreflightSkipNotice, emitPreflightCacheHitNotice"
affects: [163.2-04 (wires this cache into the two dispatch chokepoints), 163.2-05 (D-03 auto-invalidation on provider failure uses clearPreflightCache)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Env-var-with-safe-fallback: mirrors resolvedPreflightTimeout (empty -> default, parse error or non-positive -> default, never errors)"
    - "Clock-skew-guarded freshness: copied from sealFinalReviewIsFresh (future timestamp > 1min rejected)"
    - "Load-merge-save via store.UpdateJSONAtomically for per-platform map entries, so concurrent platforms never clobber each other"
    - "Nil-store safety: every exported helper checks `store == nil` first and degrades to a miss/no-op rather than panicking"

key-files:
  created: [cmd/preflight_cache.go, cmd/preflight_cache_test.go]
  modified: []

key-decisions:
  - "Cache keyed by a map[string]preflightCacheEntry rather than a single entry, so switching AETHER_WORKER_PLATFORM never destroys another platform's trust window (D-03)"
  - "preflightCacheDefaultTTL declared as a var, not const, so tests can shrink it with the same t.Cleanup idiom already used for hostedPreflightTimeout"
  - "Only available:true entries are ever honoured on read, closing the spoofing threat (T-163.2-01) without extra validation logic"

patterns-established:
  - "Preflight cost-policy layer: TTL cache + skip switch + loud notices, mechanism only, no call sites (plan 04 wires it)"

requirements-completed: [D-01, D-02, D-06]

# Metrics
duration: 35min
completed: 2026-08-01
---

# Phase 163.2 Plan 01: Preflight Cost-Policy Cache Summary

**TTL-bounded, per-platform preflight success cache in `cmd/preflight_cache.go` (`AETHER_PREFLIGHT_CACHE_TTL`, 1h default) plus a loud `AETHER_SKIP_PREFLIGHT` opt-out, backed by 8 invariant tests with 3 confirmed mutation kills — no dispatch call site changed yet.**

## Performance

- **Duration:** 35 min
- **Started:** 2026-08-01T15:38:00Z (approx, first branch-check command)
- **Completed:** 2026-08-01T16:13:25Z
- **Tasks:** 2/2 completed
- **Files modified:** 2 (both new)

## Accomplishments
- Built the Go-side preflight cost-policy layer (`cmd/preflight_cache.go`): a per-platform, TTL-bounded success cache, an explicit skip switch, and two loud notice functions — all nil-store safe.
- Copied the exact `resolvedPreflightTimeout` env-fallback idiom for `AETHER_PREFLIGHT_CACHE_TTL`, and the exact `sealFinalReviewIsFresh` clock-skew guard for entry freshness.
- Wrote 8 invariant tests covering TTL resolution, freshness, per-platform keying (merge not overwrite), skip-string parsing, round-trip + clear, unknown schema version rejection, nil-store safety, and notice wording/single-line shape.
- Manually confirmed 3 of the plan's specified mutation checks actually kill the test suite (default TTL change, clock-skew guard removal, platform-key isolation removal), then restored the clean file and re-verified green.
- Ran the full `cmd` package suite (`go test ./cmd/ -count=1`) — 335s, zero failures, and confirmed the real `.aether/data/` directory was untouched.

## Task Commits

Each task was committed atomically:

1. **Task 1: Create the preflight cost-policy layer** - `6667edf6` (feat)
2. **Task 2: Invariant tests for TTL, freshness, platform keying and skip parsing** - `9773f4e3` (test)

**Plan metadata:** committed together with this SUMMARY.md (see below)

## Files Created/Modified
- `cmd/preflight_cache.go` - Preflight cost-policy layer: `preflightCachePathRel`/`preflightCacheSchemaVersion`/`preflightCacheDefaultTTL`, `preflightCacheEntry`/`preflightCacheFile` types, and 8 helpers (`resolvedPreflightCacheTTL`, `preflightSkipRequested`, `preflightCacheEntryIsFresh`, `loadFreshPreflightCache`, `recordPreflightSuccess`, `clearPreflightCache`, `emitPreflightSkipNotice`, `emitPreflightCacheHitNotice`)
- `cmd/preflight_cache_test.go` - 8 top-level invariant tests (`TestResolvedPreflightCacheTTLEnvOverride`, `TestPreflightSkipRequestedParsing`, `TestPreflightCacheEntryFreshness`, `TestPreflightCacheIsKeyedPerPlatform`, `TestPreflightCacheRoundTripAndClear`, `TestPreflightCacheRejectsUnknownSchemaVersion`, `TestPreflightCacheHelpersSafeWithoutStore`, `TestPreflightNoticesAreLoudAndSingleLine`), using `newTestStore(t)` from `cmd/write_cmds_test.go`

## Decisions Made
- Used `store.UpdateJSONAtomically` (existing lock-safe load-merge-save primitive) for `recordPreflightSuccess` and `clearPreflightCache` rather than hand-rolling a Load-then-Save sequence, since it already provides the exact per-platform merge semantics D-03 requires under the store's file lock.
- `preflightCacheEntry.Category`/`Binary` use `omitempty` per the plan's field spec; `Platform`/`Available`/`CheckedAt` are always written.

## Deviations from Plan

None - plan executed exactly as written. Both `<read_first>` reference files were read before implementation, all 8 helper signatures match the plan's spec, and both tasks' acceptance criteria were verified via grep/build/test commands before committing.

## Issues Encountered

The worktree's HEAD was on a stale base (`6577f51c`, a later commit unrelated to phase 163.2) rather than the expected parent `5dfcc0fb`, and the `.planning/phases/163.2-...` directory was missing from the working tree entirely at agent start. Corrected per the `<worktree_branch_check>` protocol with `git reset --hard 5dfcc0fb...` (working tree was clean, no risk of data loss); the phase directory and plan files were present after the reset.

The initial `go test ./cmd/ -count=1` run exceeded the 3-minute foreground Bash timeout and was moved to background; its output file came back empty on inspection, so the full suite was re-run directly (with output redirected to a scratch log) to get a verifiable exit code. Confirmed exit code 0, 335s, zero `--- FAIL` lines.

## User Setup Required

None - no external service configuration required. This plan adds no call sites, so no runtime behavior changed and there is nothing for a user to verify yet.

## Next Phase Readiness
- `cmd/preflight_cache.go` is fully self-contained, compiles, and is not referenced by any dispatch path yet — ready for plan 04 to wire it into `preflightWorkerProvider` (cmd/dispatch_runtime.go) and the `internal-worker-adapter --preflight` boundary.
- `clearPreflightCache` is ready for plan 05's D-03 auto-invalidation-on-failure work without further changes.
- No blockers.

---
*Phase: 163.2-ts-host-preflight-configurability-make-the-worker-platform-p*
*Completed: 2026-08-01*

## Self-Check: PASSED

- FOUND: cmd/preflight_cache.go
- FOUND: cmd/preflight_cache_test.go
- FOUND: .planning/phases/163.2-ts-host-preflight-configurability-make-the-worker-platform-p/163.2-01-SUMMARY.md
- FOUND: commit 6667edf6 (Task 1)
- FOUND: commit 9773f4e3 (Task 2)
- FOUND: commit 64965579 (SUMMARY.md)
