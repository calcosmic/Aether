---
phase: 08-orchestrator-upgrade
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - tests/unit/oracle-convergence.test.js
  - tests/bash/test-oracle-convergence.sh
autonomous: true
requirements: [LOOP-04, INTL-05, OUTP-02]

must_haves:
  truths:
    - "compute_convergence correctly calculates gap resolution, coverage, and novelty from plan.json data"
    - "detect_diminishing_returns triggers strategy_change or synthesize_now after 3 consecutive low-change iterations"
    - "detect_diminishing_returns uses phase-adjusted novelty thresholds (0 for investigate, 1 for others)"
    - "check_convergence requires composite >= 85 AND 2+ low-novelty iterations"
    - "validate_and_recover restores from pre-iteration backup when JSON is malformed"
    - "update_convergence_metrics writes convergence object with history to state.json"
    - "build_synthesis_prompt includes synthesis directive and oracle.md content"
  artifacts:
    - path: "tests/unit/oracle-convergence.test.js"
      provides: "Ava unit tests for convergence functions"
      min_lines: 200
    - path: "tests/bash/test-oracle-convergence.sh"
      provides: "Bash integration tests for convergence metrics, diminishing returns, and JSON recovery"
      min_lines: 150
  key_links:
    - from: "tests/unit/oracle-convergence.test.js"
      to: ".aether/oracle/oracle.sh"
      via: "sed function extraction + bash -c execution"
      pattern: "sed -n"
    - from: "tests/bash/test-oracle-convergence.sh"
      to: ".aether/oracle/oracle.sh"
      via: "sed function extraction + eval for isolated testing"
      pattern: "sed -n"
---

<objective>
Write comprehensive tests for all Phase 8 oracle.sh functions: convergence computation, diminishing returns detection, convergence checking, JSON validation/recovery, convergence metrics update, and synthesis prompt construction.

Purpose: Regression safety net for convergence logic. These tests protect against threshold tuning regressions and ensure recovery paths work correctly. Following the exact test patterns established in Phase 7 (07-02).

Output: Two test files covering all new functions from both Ava (unit) and bash (integration) perspectives.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-orchestrator-upgrade/08-RESEARCH.md
@.planning/phases/08-orchestrator-upgrade/08-01-SUMMARY.md
@.aether/oracle/oracle.sh
@tests/unit/oracle-phase-transitions.test.js
@tests/bash/test-oracle-phase.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Ava unit tests for convergence functions</name>
  <files>tests/unit/oracle-convergence.test.js</files>
  <action>
Create `tests/unit/oracle-convergence.test.js` following the exact patterns from `tests/unit/oracle-phase-transitions.test.js`:
- Same imports (ava, child_process, path, fs, os)
- Same ORACLE_SH and PROJECT_ROOT constants
- Same `createTmpDir()` helper
- Same `writePlan()` and `writeState()` fixture helpers (extended with convergence fields)
- Function extraction via `sed -n '/^funcname()/,/^}/p'` pattern

**Helper additions:**
- `writeStateWithConvergence(dir, phase, convergence)` -- extends writeState to include a convergence object in state.json
- `runComputeConvergence(planFile, stateFile)` -- extracts compute_convergence via sed, runs it, returns parsed JSON
- `runDetectDiminishingReturns(stateFile)` -- extracts detect_diminishing_returns, returns string output
- `runCheckConvergence(stateFile)` -- extracts check_convergence, returns exit code (0 = converged, 1 = not)
- `runUpdateConvergenceMetrics(stateFile, planFile)` -- extracts update_convergence_metrics + compute_convergence (both needed), runs, reads back state.json
- `runBuildSynthesisPrompt(reason)` -- extracts build_synthesis_prompt, returns string output

**Tests to write (minimum 15):**

compute_convergence tests:
1. "compute_convergence: all questions answered returns 100% gap resolution" -- 3 answered questions, expect gap_resolution_pct=100
2. "compute_convergence: no questions answered returns 0% gap resolution" -- 3 open questions at 0% confidence, expect gap_resolution_pct=0
3. "compute_convergence: partial with 70%+ confidence counts as resolved" -- 2 partial at 75% + 1 open at 20%, expect gap_resolution_pct=66 (2/3)
4. "compute_convergence: coverage counts questions with any iterations_touched" -- 2 questions touched, 1 not, expect coverage_pct=66
5. "compute_convergence: zero questions returns 100 for all metrics" -- empty questions array, edge case
6. "compute_convergence: novelty delta computed from prev_findings_count" -- state.json has prev_findings_count=3, plan has 5 total findings, expect novelty_delta=2

detect_diminishing_returns tests:
7. "detect_diminishing_returns: returns continue when history has fewer than 3 entries" -- only 2 history entries
8. "detect_diminishing_returns: returns strategy_change when 3 low-change iterations in survey phase" -- 3 entries with novelty_delta=0, phase=survey
9. "detect_diminishing_returns: returns synthesize_now when 3 low-change iterations in synthesize phase" -- 3 entries with novelty_delta=0, phase=synthesize
10. "detect_diminishing_returns: returns continue when iterations have high novelty" -- 3 entries with novelty_delta=5
11. "detect_diminishing_returns: investigate phase uses threshold 0 (novelty_delta=1 is progress)" -- 3 entries with novelty_delta=1, phase=investigate, expect "continue"

check_convergence tests:
12. "check_convergence: returns true when composite >= 85 and 2+ low novelty iterations" -- composite_score=90, last 2 history entries have novelty_delta=0
13. "check_convergence: returns false when composite < 85" -- composite_score=70
14. "check_convergence: returns false when history has fewer than 2 entries" -- composite=90 but only 1 history entry

build_synthesis_prompt tests:
15. "build_synthesis_prompt: includes synthesis directive and reason" -- reason="converged", check output contains "SYNTHESIS PASS" and "converged"
16. "build_synthesis_prompt: includes oracle.md content" -- check output contains "Oracle Ant"

validate_and_recover tests (bash-heavy, but test the logic):
17. "validate_and_recover: returns 0 for valid JSON" -- write valid JSON, expect success
18. "validate_and_recover: recovers from pre-iteration backup" -- write invalid JSON to file, valid JSON to .pre-iteration, expect recovery

Each test uses `t.teardown(() => fs.rmSync(dir, { recursive: true, force: true }))` for cleanup.
  </action>
  <verify>
`npx ava tests/unit/oracle-convergence.test.js` -- all tests pass. `npx ava tests/unit/oracle-phase-transitions.test.js` -- still passes (no regressions). `wc -l tests/unit/oracle-convergence.test.js` reports 200+ lines.
  </verify>
  <done>
15+ Ava unit tests covering compute_convergence (6 tests), detect_diminishing_returns (5 tests), check_convergence (3 tests), build_synthesis_prompt (2 tests), and validate_and_recover (2 tests). All pass. No regressions in existing oracle test suites.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write bash integration tests for convergence and recovery</name>
  <files>tests/bash/test-oracle-convergence.sh</files>
  <action>
Create `tests/bash/test-oracle-convergence.sh` following the exact patterns from `tests/bash/test-oracle-phase.sh`:
- Same test framework (TESTS_RUN/PASSED/FAILED counters, run_test helper, setup_tmpdir/cleanup_tmpdir)
- Same function extraction via `eval "$(sed -n '/^funcname()/,/^}/p' '$ORACLE_SH')"`
- Same write_state and write_plan helpers (extended for convergence)

**Additional helpers:**
- `write_state_with_convergence()` -- takes dir, phase, history_json, prev_findings, composite_score args. Writes state.json with convergence object.

**Test functions to write (minimum 5 functions, 10+ assertions):**

1. `test_compute_convergence_metrics()` -- 3 assertions:
   - Given 3 questions (1 answered, 1 partial at 75%, 1 open at 20%), verify gap_resolution_pct=66 (2/3 resolved)
   - Given 2/3 questions touched, verify coverage_pct=66
   - Given prev_findings_count=2 and current total=5, verify novelty_delta=3

2. `test_update_convergence_metrics_writes_state()` -- 2 assertions:
   - Run update_convergence_metrics, verify state.json now has `.convergence.history` array with length >= 1
   - Verify `.convergence.prev_findings_count` was updated to current findings count

3. `test_diminishing_returns_detection()` -- 3 assertions:
   - 3 entries with novelty_delta=0 in survey phase -> "strategy_change"
   - 3 entries with novelty_delta=0 in verify phase -> "synthesize_now"
   - 3 entries with novelty_delta=5 -> "continue"

4. `test_validate_and_recover()` -- 2 assertions:
   - Valid JSON file -> returns 0
   - Invalid JSON with valid .pre-iteration backup -> returns 0 and file is restored

5. `test_convergence_check()` -- 2 assertions:
   - composite_score=90 with 2+ low-novelty history entries -> returns 0
   - composite_score=70 -> returns 1

Each function uses setup_tmpdir/cleanup_tmpdir. The script runs all test functions and reports results in the same format as test-oracle-phase.sh.

Note: For functions that depend on other functions (e.g., update_convergence_metrics depends on compute_convergence), extract BOTH functions via sed before testing.
  </action>
  <verify>
`bash tests/bash/test-oracle-convergence.sh` -- all tests pass. `bash tests/bash/test-oracle-phase.sh` -- still passes (no regressions). `wc -l tests/bash/test-oracle-convergence.sh` reports 150+ lines.
  </verify>
  <done>
5+ bash integration test functions with 10+ sub-assertions covering convergence computation, convergence metrics state updates, diminishing returns detection, JSON validation/recovery, and convergence checking. All pass. No regressions in existing bash test suites.
  </done>
</task>

</tasks>

<verification>
1. `npx ava tests/unit/oracle-convergence.test.js` -- all 15+ tests pass
2. `bash tests/bash/test-oracle-convergence.sh` -- all 10+ assertions pass
3. `npx ava tests/unit/oracle-phase-transitions.test.js` -- all 14 existing tests still pass (no regressions)
4. `bash tests/bash/test-oracle-phase.sh` -- all 11 existing assertions still pass (no regressions)
5. `npx ava tests/unit/oracle-state.test.js` -- existing state tests still pass
6. `wc -l tests/unit/oracle-convergence.test.js` -- 200+ lines
7. `wc -l tests/bash/test-oracle-convergence.sh` -- 150+ lines
</verification>

<success_criteria>
- Every new oracle.sh function (compute_convergence, detect_diminishing_returns, check_convergence, update_convergence_metrics, validate_and_recover, build_synthesis_prompt) has at least 2 tests
- Edge cases covered: zero questions, boundary thresholds, phase-adjusted novelty, insufficient history
- Recovery path tested: invalid JSON with valid pre-iteration backup
- No regressions in any existing oracle test suite
- Tests follow exact patterns from Phase 7 (07-02) for consistency
</success_criteria>

<output>
After completion, create `.planning/phases/08-orchestrator-upgrade/08-02-SUMMARY.md`
</output>
