---
phase: 10-steering-integration
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - tests/unit/oracle-steering.test.js
  - tests/bash/test-oracle-steering.sh
autonomous: true
requirements: [STRC-01, STRC-02, STRC-03]

must_haves:
  truths:
    - "read_steering_signals returns formatted directive when pheromone signals exist"
    - "read_steering_signals returns empty string when no signals exist"
    - "read_steering_signals gracefully handles missing pheromone system"
    - "build_oracle_prompt includes strategy modifier for breadth-first and depth-first"
    - "build_oracle_prompt omits strategy modifier for adaptive"
    - "build_oracle_prompt includes steering directive when provided"
    - "validate-oracle-state passes with strategy and focus_areas fields"
    - "validate-oracle-state passes without strategy and focus_areas fields (backward compat)"
    - "validate-oracle-state rejects invalid strategy values"
    - "Signal limits are respected: max 3 FOCUS, 2 REDIRECT, 2 FEEDBACK"
  artifacts:
    - path: "tests/unit/oracle-steering.test.js"
      provides: "Ava unit tests for steering signal reading, strategy handling, and validation"
      min_lines: 150
    - path: "tests/bash/test-oracle-steering.sh"
      provides: "Bash integration tests for steering functions"
      min_lines: 100
  key_links:
    - from: "tests/unit/oracle-steering.test.js"
      to: ".aether/oracle/oracle.sh"
      via: "sed extraction of read_steering_signals and build_oracle_prompt"
      pattern: "sed -n"
    - from: "tests/bash/test-oracle-steering.sh"
      to: ".aether/oracle/oracle.sh"
      via: "sed extraction and eval sourcing"
      pattern: "sed -n.*oracle.sh"
---

<objective>
Write comprehensive tests for all Phase 10 steering integration functionality.

Purpose: Verify read_steering_signals, build_oracle_prompt strategy modifier, and validate-oracle-state extensions work correctly and handle edge cases -- consistent with the established oracle test patterns from Phase 7/8/9.

Output: Ava unit test file (oracle-steering.test.js) and bash integration test file (test-oracle-steering.sh) with full coverage of steering functionality.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-steering-integration/10-RESEARCH.md
@.planning/phases/10-steering-integration/10-01-SUMMARY.md
@.aether/oracle/oracle.sh
@tests/unit/oracle-trust.test.js
@tests/bash/test-oracle-trust.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ava unit tests for steering signal reading and strategy handling</name>
  <files>tests/unit/oracle-steering.test.js</files>
  <action>
Create `tests/unit/oracle-steering.test.js` following the exact patterns from `tests/unit/oracle-trust.test.js` (same imports, same helper structure, same sed extraction pattern).

**Test helpers:**

`createTmpDir()` -- same as oracle-trust.test.js.

`writeState(dir, overrides)` -- write state.json with defaults (version "1.1", topic "Test", scope "both", phase "survey", iteration 0, max_iterations 15, target_confidence 95, overall_confidence 0, started_at/last_updated ISO timestamps, status "active", strategy "adaptive", focus_areas []) merged with overrides object. Use `JSON.stringify(Object.assign(defaults, overrides))`.

`writePlan(dir, questions)` -- same helper as oracle-trust.test.js writePlanWithSources, accepting array of {confidence, touched, status, findings} with sources registry.

`writePheromones(dir, signals)` -- write a pheromones.json file at `$dir/data/pheromones.json` with the provided signals array. Each signal: `{id, type, content:{text}, effective_strength, created_at, expires_at}`. Create parent dir with `mkdirSync(..., {recursive: true})`.

`runReadSteeringSignals(aetherRoot)` -- extract `read_steering_signals` from oracle.sh using sed (`sed -n '/^read_steering_signals()/,/^}/p'`), source it, and call with aetherRoot arg. Since read_steering_signals calls `pheromone-read` via aether-utils.sh, and we cannot easily mock that in bash, use a different approach: create a mock aether-utils.sh in the tmpdir that returns known JSON when called with `pheromone-read`. The mock script:
```bash
#!/bin/bash
case "$1" in
  pheromone-read) echo '{"ok":true,"result":{"signals":[...test data...]}}' ;;
esac
```
Place it at `$tmpdir/.aether/aether-utils.sh` and pass `$tmpdir` as aether_root.

`runBuildOraclePrompt(stateFile, oracleMd, steeringDirective)` -- extract `build_oracle_prompt` from oracle.sh using sed, execute with args. Create a minimal oracle.md in tmpdir containing "BASE_PROMPT_MARKER" so tests can verify it is appended.

**Test cases (10+ tests):**

1. **read_steering_signals returns empty when no signals** -- Mock pheromone-read returns empty signals array. Assert output is empty string.

2. **read_steering_signals formats FOCUS signals** -- Mock returns 2 FOCUS signals with different strengths. Assert output contains "FOCUS (Prioritize these areas)" and signal texts sorted by strength descending.

3. **read_steering_signals formats REDIRECT signals** -- Mock returns 1 REDIRECT signal. Assert output contains "REDIRECT (Hard constraints -- MUST follow)" and signal text.

4. **read_steering_signals formats FEEDBACK signals** -- Mock returns 1 FEEDBACK signal. Assert output contains "FEEDBACK (Adjust approach)" and signal text.

5. **read_steering_signals respects signal limits** -- Mock returns 5 FOCUS signals. Assert output contains only 3 FOCUS entries (highest strength).

6. **read_steering_signals handles mixed signal types** -- Mock returns 1 of each type. Assert output contains all three section headers in correct order (REDIRECT, FOCUS, FEEDBACK).

7. **read_steering_signals graceful on missing utils** -- Pass a nonexistent aether_root. Assert output is empty string (no crash).

8. **build_oracle_prompt includes strategy modifier for breadth-first** -- Write state with strategy "breadth-first". Assert output contains "Breadth-first" and "covering ALL questions".

9. **build_oracle_prompt includes strategy modifier for depth-first** -- Write state with strategy "depth-first". Assert output contains "Depth-first" and "exhaustively".

10. **build_oracle_prompt omits strategy modifier for adaptive** -- Write state with strategy "adaptive". Assert output does NOT contain "STRATEGY NOTE".

11. **build_oracle_prompt includes steering directive** -- Pass a steering directive string. Assert output contains the directive text between the phase directive and the oracle.md base prompt.

12. **validate-oracle-state passes with strategy and focus_areas** -- Write state.json with strategy "depth-first" and focus_areas ["security"]. Run validate-oracle-state. Assert pass is true.

13. **validate-oracle-state passes without strategy and focus_areas (backward compat)** -- Write state.json without strategy/focus_areas fields. Run validate-oracle-state. Assert pass is true.

14. **validate-oracle-state rejects invalid strategy** -- Write state.json with strategy "random". Run validate-oracle-state. Assert pass is false and checks contain "fail" for strategy.

Follow ava `test()` format with `t.true()`, `t.false()`, `t.is()`, `t.regex()`, `t.notRegex()` assertions. Use `test.afterEach.always()` for tmpdir cleanup.
  </action>
  <verify>
`cd /Users/callumcowie/repos/Aether && npx ava tests/unit/oracle-steering.test.js --timeout=30s` -- all tests pass.
  </verify>
  <done>
14+ Ava unit tests pass covering: read_steering_signals (empty signals, FOCUS/REDIRECT/FEEDBACK formatting, signal limits, mixed types, graceful degradation), build_oracle_prompt (breadth-first/depth-first/adaptive strategy modifiers, steering directive injection), and validate-oracle-state (new fields accepted, backward compatibility, invalid strategy rejection).
  </done>
</task>

<task type="auto">
  <name>Task 2: Bash integration tests for steering functions</name>
  <files>tests/bash/test-oracle-steering.sh</files>
  <action>
Create `tests/bash/test-oracle-steering.sh` following the exact patterns from `tests/bash/test-oracle-trust.sh` (same test counter variables, same run_test helper, same setup_tmpdir/cleanup_tmpdir, same color output).

**Test helpers:**

`write_state()` -- same as trust tests but with strategy and focus_areas fields. Parameters: dir, phase, iteration, strategy (default "adaptive"), focus_areas_json (default "[]").

`write_plan()` -- same as trust tests. Parameters: dir, questions JSON array.

`write_mock_utils()` -- create a mock `$dir/.aether/aether-utils.sh` that responds to `pheromone-read` with a configurable JSON response. Write the mock script to the tmpdir.

**Test functions (each contains multiple assertions via run_test):**

`test_read_steering_signals()`:
1. **No signals returns empty** -- Mock returns empty signals array. Extract and run read_steering_signals. Assert output is empty.
2. **FOCUS formatting** -- Mock returns 2 FOCUS signals. Assert output contains "FOCUS" header and both signal texts.
3. **REDIRECT formatting** -- Mock returns 1 REDIRECT signal. Assert output contains "REDIRECT" header.
4. **Signal limit enforcement** -- Mock returns 5 FOCUS signals. Count FOCUS signal lines in output. Assert count is 3 (max).
5. **Graceful degradation** -- Pass nonexistent path. Assert exit code 0 and empty output.

`test_build_oracle_prompt_strategy()`:
1. **Breadth-first modifier** -- Write state with strategy "breadth-first". Extract and run build_oracle_prompt. Assert output contains "Breadth-first".
2. **Depth-first modifier** -- Write state with strategy "depth-first". Assert output contains "Depth-first".
3. **Adaptive no modifier** -- Write state with strategy "adaptive". Assert output does NOT contain "STRATEGY NOTE".
4. **Steering directive injection** -- Pass steering text as third arg. Assert output contains the steering text.

`test_validate_oracle_state_steering()`:
1. **Accepts strategy field** -- Write state with strategy "breadth-first". Run `bash .aether/aether-utils.sh validate-oracle-state state` with ORACLE_DIR override. Assert "pass":true.
2. **Accepts focus_areas field** -- Write state with focus_areas ["sec","perf"]. Run validation. Assert "pass":true.
3. **Backward compat** -- Write state without strategy/focus_areas. Run validation. Assert "pass":true.
4. **Rejects invalid strategy** -- Write state with strategy "invalid". Run validation. Assert "pass":false.

Use the established sed extraction pattern for oracle.sh functions:
```bash
eval "$(sed -n '/^read_steering_signals()/,/^}/p' "$ORACLE_SH")"
```

For build_oracle_prompt which depends on cat-ing oracle.md, set `SCRIPT_DIR` explicitly before extraction (same pattern as Phase 8 tests).

For multi-function extraction (if read_steering_signals uses no other oracle.sh functions, it should be self-contained since it calls pheromone-read externally).

Use `jq -n` for JSON construction in bash (avoid heredoc escaping issues, per Phase 9 decision).

Print final summary:
```
===========================
$TESTS_PASSED / $TESTS_RUN passed ($TESTS_FAILED failed)
===========================
```

Exit with code 1 if any tests failed.
  </action>
  <verify>
`cd /Users/callumcowie/repos/Aether && bash tests/bash/test-oracle-steering.sh` -- all tests pass with 0 failures.
  </verify>
  <done>
13+ bash integration assertions pass covering: read_steering_signals (empty signals, FOCUS/REDIRECT formatting, signal limits, graceful degradation), build_oracle_prompt strategy modifiers (breadth-first, depth-first, adaptive, steering directive), and validate-oracle-state extensions (strategy accepted, focus_areas accepted, backward compatibility, invalid strategy rejected).
  </done>
</task>

</tasks>

<verification>
1. `npx ava tests/unit/oracle-steering.test.js --timeout=30s` -- all tests pass
2. `bash tests/bash/test-oracle-steering.sh` -- all tests pass with 0 failures
3. `npm test` -- full test suite still passes (no regressions)
</verification>

<success_criteria>
- Ava unit tests cover read_steering_signals, build_oracle_prompt strategy, validate-oracle-state extensions
- Bash integration tests cover same functions via sed extraction pattern
- Tests verify signal formatting, limits, graceful degradation, strategy modifiers, backward compatibility
- No test regressions in existing oracle test suites
</success_criteria>

<output>
After completion, create `.planning/phases/10-steering-integration/10-02-SUMMARY.md`
</output>
