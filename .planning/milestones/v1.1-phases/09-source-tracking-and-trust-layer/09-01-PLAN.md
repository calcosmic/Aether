---
phase: 09-source-tracking-and-trust-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/oracle/oracle.sh
  - .aether/oracle/oracle.md
  - .aether/aether-utils.sh
  - .claude/commands/ant/oracle.md
  - .opencode/commands/ant/oracle.md
autonomous: true
requirements: [TRST-01, TRST-02, TRST-03]

must_haves:
  truths:
    - "Every finding in plan.json carries source_ids linking to a sources registry"
    - "Single-source findings are flagged via trust_summary in plan.json"
    - "The synthesis pass produces inline citations [S1] and a Sources section"
    - "The research-plan.md shows trust ratio after each iteration"
    - "Old plan.json files with string key_findings still pass validation and convergence"
  artifacts:
    - path: ".aether/oracle/oracle.sh"
      provides: "compute_trust_scores function, updated build_synthesis_prompt, updated generate_research_plan"
      contains: "compute_trust_scores"
    - path: ".aether/oracle/oracle.md"
      provides: "Source tracking instructions, updated confidence rubric, synthesis citation rules"
      contains: "source_ids"
    - path: ".aether/aether-utils.sh"
      provides: "Backward-compatible plan.json validation for sources and structured findings"
      contains: "sources"
    - path: ".claude/commands/ant/oracle.md"
      provides: "Updated wizard creating plan.json v1.1 with empty sources registry"
      contains: "sources"
    - path: ".opencode/commands/ant/oracle.md"
      provides: "Mirror of Claude wizard changes for parity"
      contains: "sources"
  key_links:
    - from: ".aether/oracle/oracle.md"
      to: ".aether/oracle/oracle.sh"
      via: "AI writes structured findings with source_ids; oracle.sh counts them in compute_trust_scores"
      pattern: "source_ids"
    - from: ".aether/oracle/oracle.sh"
      to: ".aether/oracle/oracle.sh"
      via: "compute_trust_scores called after update_convergence_metrics in main loop"
      pattern: "compute_trust_scores"
    - from: ".aether/oracle/oracle.sh"
      to: ".aether/oracle/oracle.md"
      via: "build_synthesis_prompt requires inline citations and Sources section"
      pattern: "inline citations"
    - from: ".aether/aether-utils.sh"
      to: ".aether/oracle/oracle.sh"
      via: "validate-oracle-state accepts both string and object key_findings"
      pattern: "source_ids"
---

<objective>
Add source tracking, trust scoring, and citation formatting to the oracle system.

Purpose: Every factual claim in oracle research output must be traceable to its source. Single-source claims must be flagged. The final report must include inline citations and a sources section. This makes oracle research verifiable and trustworthy.

Output: Updated oracle.sh (compute_trust_scores, updated synthesis/research-plan), updated oracle.md (source tracking prompt), updated validation (backward-compatible), updated wizards (plan.json v1.1 template).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-source-tracking-and-trust-layer/09-RESEARCH.md
@.planning/phases/08-orchestrator-upgrade/08-01-SUMMARY.md
@.planning/phases/08-orchestrator-upgrade/08-02-SUMMARY.md
@.aether/oracle/oracle.sh
@.aether/oracle/oracle.md
@.aether/aether-utils.sh
@.claude/commands/ant/oracle.md
@.opencode/commands/ant/oracle.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compute_trust_scores to oracle.sh and update build_synthesis_prompt and generate_research_plan</name>
  <files>
    .aether/oracle/oracle.sh
  </files>
  <action>
Add the `compute_trust_scores` function to oracle.sh (place it after `update_convergence_metrics`, before `check_convergence`). This function:

1. Takes plan_file as its single argument
2. Checks if plan.json uses structured findings format by checking `[.questions[].key_findings[] | type] | if length == 0 then false else any(. == "object") end` -- if false (pre-Phase-9 string findings), return 0 early (backward compatible)
3. Counts total_findings, single_source (source_ids length == 1), multi_source (source_ids length >= 2), no_source (source_ids missing or length == 0)
4. Computes trust_ratio as `multi_source * 100 / total_findings` (integer arithmetic, 0 if total is 0)
5. Writes trust_summary object to plan.json via jq: `{total_findings, single_source, multi_source, no_source, trust_ratio}`

Use the exact jq queries from the research (Pattern 3, Example 3) -- they handle edge cases correctly.

**In the main loop**, add a call to `compute_trust_scores "$PLAN_FILE"` immediately after the `update_convergence_metrics` call (around line 605). This mirrors how convergence metrics are computed after each iteration.

**Update `build_synthesis_prompt`** (around line 422-454):
- Change the "Read ALL of these files" list to mention "questions, findings, confidence, AND sources registry" for plan.json
- Add a 5th Required Section: "5. **Sources** -- List ALL sources from plan.json sources registry: Format: [S1] Title -- URL (accessed: date). Group by type (documentation, blog, codebase, etc.). Note total source count and multi-source coverage percentage."
- In the Findings by Question section description, add: "Use inline citations [S1], [S2] linking findings to their sources. Flag single-source findings with (single source) marker."

**Update `generate_research_plan`** (around line 28-65):
- After the existing questions table and Next Steps section, add a trust summary block. Read trust_ratio from plan.json via `jq '.trust_summary.trust_ratio // -1'`. If >= 0, also read total_findings, single_source, multi_source from trust_summary and output a "## Source Trust" section with a markdown table: `| Total Findings | Multi-Source | Single-Source | Trust Ratio |`.

Do NOT change `compute_convergence` or any existing convergence logic -- the `[.questions[].key_findings | length] | add // 0` query naturally works with both string and object array elements.
  </action>
  <verify>
Run `bash -c 'set +e; eval "$(sed -n "/^compute_trust_scores()/,/^}/p" .aether/oracle/oracle.sh)"; echo "function exists"'` -- should print "function exists".

Verify build_synthesis_prompt contains "Sources" and "inline citations" strings:
`grep -c "Sources" .aether/oracle/oracle.sh` should be >= 2 (the section header + the instruction).
`grep -c "inline citations" .aether/oracle/oracle.sh` should be >= 1.

Verify generate_research_plan contains "Source Trust" and "trust_ratio":
`grep -c "Source Trust" .aether/oracle/oracle.sh` should be >= 1.
  </verify>
  <done>
compute_trust_scores function exists in oracle.sh and is called in the main loop after update_convergence_metrics.
build_synthesis_prompt includes Sources section requirement and inline citation instructions.
generate_research_plan includes Source Trust table when trust_summary data exists.
No existing functions (compute_convergence, update_convergence_metrics, check_convergence, detect_diminishing_returns) are modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update oracle.md prompt with source tracking requirements and update phase directives</name>
  <files>
    .aether/oracle/oracle.md
  </files>
  <action>
Update oracle.md to require source tracking in every iteration. Make these specific changes:

**Step 3 (Research) -- add Source Tracking section after the "Use available tools" list:**

Add a "**Source Tracking (MANDATORY):**" subsection that instructs:
- For every new finding, record the source URL (or file path for codebase research), source title/description, and date accessed
- Register sources in plan.json under the top-level `sources` object using sequential IDs (S1, S2, S3...)
- Reuse existing source IDs if citing the same URL again
- Source types: "documentation", "blog", "github", "academic", "codebase", "forum", "official"
- For codebase research, use file paths as URLs with type "codebase"

**Step 4 (Update State Files) -- change key_findings format in plan.json instructions:**

Replace the existing `key_findings` instruction ("Add ONLY genuinely new findings to `key_findings` array (not restatements)") with:
- Add findings as OBJECTS not strings: `{"text": "finding text", "source_ids": ["S1", "S2"], "iteration": <current>}`
- Every finding MUST have at least one source_id
- Add new sources to the top-level `sources` registry
- Reuse existing source IDs for the same URL

**Confidence Scoring Rubric -- add source-backed rules:**

After the existing rubric table and anti-inflation rules, add:

"**Source-backed confidence rules:**
- 0 sources: Finding is UNSUPPORTED -- do not record it
- 1 source: Single-source claim, capped at 50% contribution to question confidence
- 2+ sources: Multi-source claim, full confidence contribution
- The overall question confidence should reflect the source backing of its findings"

**Important Rules section -- add synthesis citation rule:**

Add at the end: "- In synthesis passes, use inline citations [S1], [S2] linking findings to sources. Include a ## Sources section listing all sources with IDs, URLs, titles, and access dates."

**Phase directives in oracle.sh -- add source tracking reminder to each phase directive:**

Back in oracle.sh, in the `build_oracle_prompt` function, add a one-line reminder to each of the 4 phase directive heredocs (survey, investigate, synthesize, verify). Add before the closing `---` line in each:

For survey: `Source tracking is MANDATORY -- register sources and link every finding to source_ids.`
For investigate: `Source tracking is MANDATORY this iteration. Every new finding must have at least one source_id.`
For synthesize: `Verify source attribution is complete. Flag any findings missing source_ids.`
For verify: `Cross-reference source coverage. Ensure all key findings have 2+ independent sources.`
  </action>
  <verify>
`grep -c "source_ids" .aether/oracle/oracle.md` should be >= 2.
`grep -c "Source Tracking" .aether/oracle/oracle.md` should be >= 1.
`grep -c "MANDATORY" .aether/oracle/oracle.sh` should be >= 2 (survey + investigate directives).
`grep -c "source" .aether/oracle/oracle.md` should be >= 5 (multiple references throughout).
  </verify>
  <done>
oracle.md requires source tracking for every finding with source_ids, source registry, and inline citations.
Confidence rubric includes source-backed rules (0 sources = unsupported, 1 source = capped 50%, 2+ = full).
Each phase directive in oracle.sh includes a source tracking reminder line.
Synthesis rules require inline [S1] citations and a Sources section.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update validate-oracle-state, wizard commands, and version bump</name>
  <files>
    .aether/aether-utils.sh
    .claude/commands/ant/oracle.md
    .opencode/commands/ant/oracle.md
  </files>
  <action>
**Update validate-oracle-state plan validation in aether-utils.sh** (around line 1233-1259):

Add two new backward-compatible validation checks AFTER the existing checks array in the plan validation jq expression (before the closing `]}` that produces the pass result):

1. Sources registry validation (optional field):
```
if (.sources // null) != null then
  if (.sources | type) == "object" then
    if (.sources | to_entries | length == 0) or (.sources | to_entries | all(.value | has("url","title","date_accessed"))) then "pass"
    else "fail: sources entries missing required fields (url, title, date_accessed)"
    end
  else "fail: sources must be an object"
  end
else "pass"
end
```

2. Structured findings validation (optional, backward-compatible):
```
if ([.questions[].key_findings[] | type] | any(. == "object")) then
  if ([.questions[].key_findings[] | select(type == "object") | has("text","source_ids")] | all) then "pass"
  else "fail: structured findings missing required fields (text, source_ids)"
  end
else "pass"
end
```

These checks pass for pre-Phase-9 plan.json files (no sources, string key_findings) and validate the new format when present.

**Update Claude wizard (.claude/commands/ant/oracle.md):**

In the "Write plan.json" section (around line 257-277), change the template:
- Change `"version": "1.0"` to `"version": "1.1"`
- Add `"sources": {},` as the second field (after version, before questions)

The questions array template stays the same -- key_findings starts empty, so the new format applies when the AI adds findings during iterations.

**Update OpenCode wizard (.opencode/commands/ant/oracle.md):**

Apply the exact same changes as the Claude wizard for parity:
- `"version": "1.0"` -> `"version": "1.1"`
- Add `"sources": {},` field

After making changes, run `npm run lint:sync` to verify command parity is maintained.
  </action>
  <verify>
Test validation with a v1.1 plan.json containing sources:
```bash
echo '{"version":"1.1","sources":{"S1":{"url":"https://example.com","title":"Test","date_accessed":"2026-03-13","type":"documentation"}},"questions":[{"id":"q1","text":"Q?","status":"open","confidence":0,"key_findings":[{"text":"finding","source_ids":["S1"],"iteration":1}],"iterations_touched":[1]}],"created_at":"2026-03-13T00:00:00Z","last_updated":"2026-03-13T00:00:00Z"}' > /tmp/test-plan.json
ORACLE_DIR=/tmp bash .aether/aether-utils.sh validate-oracle-state plan
```
Should return `{"ok":true,...,"pass":true}`.

Test backward compatibility with a v1.0 plan.json (string findings, no sources):
```bash
echo '{"version":"1.0","questions":[{"id":"q1","text":"Q?","status":"open","confidence":0,"key_findings":["finding"],"iterations_touched":[1]}],"created_at":"2026-03-13T00:00:00Z","last_updated":"2026-03-13T00:00:00Z"}' > /tmp/test-plan-old.json
ORACLE_DIR=/tmp bash .aether/aether-utils.sh validate-oracle-state plan
```
Should also return pass:true.

Run `npm run lint:sync` and verify no parity errors.
  </verify>
  <done>
validate-oracle-state accepts both v1.0 (string findings, no sources) and v1.1 (structured findings, sources registry) plan.json formats.
Both Claude and OpenCode wizard commands create plan.json v1.1 with empty sources registry.
lint:sync passes confirming command parity.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "compute_trust_scores" .aether/oracle/oracle.sh` returns >= 3 (function definition, function body references, main loop call)
2. `grep -c "source_ids" .aether/oracle/oracle.md` returns >= 2
3. `grep -c "sources" .aether/aether-utils.sh` returns >= 2 (in validation section)
4. `grep "version.*1.1" .claude/commands/ant/oracle.md` finds the version bump
5. `grep "version.*1.1" .opencode/commands/ant/oracle.md` finds the matching version bump
6. Existing tests still pass: `npx ava tests/unit/oracle-convergence.test.js` and `bash tests/bash/test-oracle-convergence.sh` (backward compatibility)
</verification>

<success_criteria>
- compute_trust_scores function exists in oracle.sh, called in main loop after convergence metrics
- oracle.md requires source tracking for every finding with source_ids
- build_synthesis_prompt requires inline citations and Sources section
- generate_research_plan shows Source Trust table when trust data exists
- validate-oracle-state accepts both old (string findings) and new (structured findings + sources) formats
- Wizard commands create plan.json v1.1 with empty sources registry
- All existing oracle tests pass unchanged (backward compatibility confirmed)
</success_criteria>

<output>
After completion, create `.planning/phases/09-source-tracking-and-trust-layer/09-01-SUMMARY.md`
</output>
