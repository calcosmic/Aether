---
phase: 159-end-to-end-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - control-ts/package.json
  - control-ts/tests/control/integration.test.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "User can run `go test ./...` and all tests pass"
    - "User can run `npm run test:schemas` and all Zod schemas validate against sample YAML files"
    - "User can run `npm run test:control` and the phase runner and orchestrator tests pass"
  artifacts:
    - path: "control-ts/package.json"
      provides: "npm scripts for test:control and aether:control"
      contains: '"test:control"'
    - path: "control-ts/tests/control/integration.test.ts"
      provides: "Integration test suite for control plane"
      min_lines: 30
  key_links:
    - from: "control-ts/package.json"
      to: "vitest"
      via: "npm script test:control runs vitest over orchestrator tests"
      pattern: '"test:control"'
---

<objective>
Wire up the missing npm scripts and integration test entry point so that all three automated test gates (Go regression, schema validation, control plane integration) are executable and pass.

Purpose: TEST-01, TEST-02, and TEST-03 from REQUIREMENTS.md are currently blocked by missing npm scripts. This plan unblocks them.
Output: `control-ts/package.json` with new scripts; `control-ts/tests/control/integration.test.ts` covering runner + orchestrator integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase-159-end-to-end-acceptance/159-RESEARCH.md
@.planning/phases/phase-159-end-to-end-acceptance/159-PATTERNS.md
@.planning/phases/phase-159-end-to-end-acceptance/159-VALIDATION.md

<interfaces>
From control-ts/package.json (existing scripts):
```json
{
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:schemas": "vitest run tests/schemas"
  }
}
```

From control-ts/vitest.config.ts:
```typescript
export default defineConfig({
  test: {
    environment: "node",
    include: ["tests/**/*.test.ts"],
  },
});
```

From control-ts/tests/orchestrator/executePlan.test.ts:
- Uses `executePlan`, `readColonyState`, `writeColonyState`, `updateColonyState`, `parseEventLine`
- Uses temp directory isolation via `AETHER_EVENTS_FILE` env override
- Tests: full sequence, plan events, failure policy block/skip/retry, state transitions, empty sequence, per-phase inputs

From control-ts/tests/orchestrator/runPhase.test.ts:
- Uses `runPhase`, `parseEventLine`, `projectRoot`
- Tests: init phase loads queen agent, emits phase:start/phase:complete, throws for missing phase, NDJSON append, inputs in payload
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing npm scripts to control-ts/package.json</name>
  <files>control-ts/package.json</files>
  <read_first>
    - control-ts/package.json (current scripts block)
    - control-ts/vitest.config.ts (test include pattern)
  </read_first>
  <action>
    Add two scripts to the `scripts` object in control-ts/package.json:
    1. `"test:control": "vitest run tests/orchestrator tests/agents tests/phases tests/prompts tests/skills tests/memory"` — runs all control-plane-related test directories.
    2. `"aether:control": "tsx src/cli.ts"` — CLI entry point for the demo flow (cli.ts will be created in Plan 02).
    Preserve existing scripts (build, typecheck, test, test:schemas). Do not change dependency versions.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/control-ts && npm run test:control -- --run 2>&1 | grep -c "passed"</automated>
  </verify>
  <acceptance_criteria>
    - control-ts/package.json contains `"test:control"` with value `"vitest run tests/orchestrator tests/agents tests/phases tests/prompts tests/skills tests/memory"`
    - control-ts/package.json contains `"aether:control"` with value `"tsx src/cli.ts"`
    - Existing scripts (build, typecheck, test, test:schemas) are unchanged
    - `npm run test:control` exits 0 and reports passing tests
  </acceptance_criteria>
  <done>npm scripts added and `npm run test:control` passes</done>
</task>

<task type="auto">
  <name>Task 2: Create integration test suite for control plane</name>
  <files>control-ts/tests/control/integration.test.ts</files>
  <read_first>
    - control-ts/tests/orchestrator/executePlan.test.ts (isolation patterns, assertions)
    - control-ts/tests/orchestrator/runPhase.test.ts (event verification patterns)
    - control-ts/src/orchestrator/executePlan.ts (PlanResult interface)
    - control-ts/src/orchestrator/runPhase.ts (PhaseResult interface)
    - control-ts/src/memory/store.ts (readColonyState, writeColonyState, updateColonyState signatures)
  </read_first>
  <action>
    Create control-ts/tests/control/integration.test.ts that exercises the full control plane integration:
    1. Import `executePlan`, `runPhase`, `parseEventLine`, colony state helpers, and `projectRoot`.
    2. Use the same temp-directory + `AETHER_EVENTS_FILE` env override isolation pattern as executePlan.test.ts.
    3. Seed a fresh colony state in beforeEach (goal = "Integration test colony").
    4. Write at least these test cases:
       a. "runs init -> plan -> build and emits events for each step" — call `executePlan(["init", "plan", "build"])`, assert status === "completed", assert 3 results, assert events include phase:start, phase:complete, plan:start, plan:complete.
       b. "runPhase loads agent and phase from colony assets" — call `runPhase("init")`, assert agentId === "queen", assert phaseId === "init", assert status === "completed".
       c. "colony state is updated after plan execution" — after `executePlan(["init", "plan"])`, call `readColonyState()`, assert `state.state === "COMPLETED"` and `state.current_phase === 2`.
       d. "NDJSON events are parseable and contain required fields" — read events file, parse each line with `parseEventLine`, assert every event has `type`, `timestamp`, and `payload`.
    5. Clean up temp dir and colony state in afterEach.
    6. Do NOT mock `runPhase` or `executePlan` — this is an integration test using real implementations.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/control-ts && npx vitest run tests/control/integration.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - control-ts/tests/control/integration.test.ts exists and contains at least 4 test cases
    - Each test case uses real `executePlan` or `runPhase` (no mocks of these functions)
    - Tests use temp-directory isolation with `AETHER_EVENTS_FILE` override
    - Tests seed and clean up colony state via `readColonyState` / `writeColonyState` / `updateColonyState`
    - `npx vitest run tests/control/integration.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Integration test file exists and passes when run via vitest</done>
</task>

<task type="auto">
  <name>Task 3: Run Go regression tests and full TS test suite</name>
  <files></files>
  <read_first>
    - control-ts/package.json (to confirm scripts)
  </read_first>
  <action>
    Run the three test gates in sequence and capture output:
    1. `go test ./...` from repo root — verify all Go tests pass (TEST-01).
    2. `cd control-ts && npm run test:schemas` — verify schema tests pass (TEST-02).
    3. `cd control-ts && npm run test:control` — verify control plane tests pass, including the new integration test (TEST-03).
    If any gate fails, capture the failure output, diagnose whether it is a pre-existing issue or caused by the new integration test, and fix if caused by this plan.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && go test ./... 2>&1 | tail -5; cd /Users/callumcowie/repos/Aether/control-ts && npm run test:schemas 2>&1 | tail -5; cd /Users/callumcowie/repos/Aether/control-ts && npm run test:control 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./...` exits 0 with no FAIL lines
    - `npm run test:schemas` exits 0 and reports all schema tests passing
    - `npm run test:control` exits 0 and reports all control plane tests passing
    - No test failures are introduced by the new integration test file
  </acceptance_criteria>
  <done>All three automated test gates pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner -> filesystem | Tests write to temp directories and colony state files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-01 | Denial of Service | integration.test.ts | mitigate | Tests use temp directories and `cleanupState()` to avoid polluting real colony state |
| T-159-02 | Information Disclosure | integration.test.ts | mitigate | Tests do not log secrets; event payloads contain only test fixture data |
| T-159-03 | Tampering | package.json scripts | accept | Scripts are developer-facing build commands; no untrusted input |
</threat_model>

<verification>
- `go test ./...` passes (TEST-01)
- `npm run test:schemas` passes (TEST-02)
- `npm run test:control` passes including new integration tests (TEST-03)
</verification>

<success_criteria>
- control-ts/package.json has `test:control` and `aether:control` scripts
- control-ts/tests/control/integration.test.ts exists with >=4 tests, all passing
- All three test gates (Go, schemas, control) run successfully
</success_criteria>

<output>
After completion, create `.planning/phases/159-end-to-end-acceptance/159-01-SUMMARY.md`
</output>
