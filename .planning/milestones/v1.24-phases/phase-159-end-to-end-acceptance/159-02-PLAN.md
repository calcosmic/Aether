---
phase: 159-end-to-end-acceptance
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - control-ts/src/cli.ts
  - scripts/audit-hardcoded.sh
  - scripts/audit-extraction.sh
autonomous: true
requirements:
  - TEST-04
  - TEST-05
  - TEST-06
must_haves:
  truths:
    - "User can run `npm run aether:control -- --task \"...\"` and see the task complete with events for each lifecycle step"
    - "A grep/audit confirms no prompt text is hardcoded in Go except fallback/error text"
    - "The extraction audit confirms no agent behaviour exists only in Go"
  artifacts:
    - path: "control-ts/src/cli.ts"
      provides: "CLI entry point for demo flow"
      min_lines: 30
      exports: ["main"]
    - path: "scripts/audit-hardcoded.sh"
      provides: "Automated grep audit for hardcoded prompt text in Go"
      min_lines: 20
    - path: "scripts/audit-extraction.sh"
      provides: "Automated extraction audit verifying no agent behaviour exists only in Go"
      min_lines: 20
  key_links:
    - from: "control-ts/src/cli.ts"
      to: "control-ts/src/orchestrator/executePlan.ts"
      via: "import { executePlan } from './orchestrator/executePlan.js'"
      pattern: "executePlan"
    - from: "scripts/audit-hardcoded.sh"
      to: "cmd/"
      via: "grep -rn"
      pattern: "grep.*cmd/.*\\.go"
    - from: "scripts/audit-extraction.sh"
      to: ".planning/phases/152-boundary-parity/152-BEHAVIOUR_EXTRACTION_AUDIT.md"
      via: "cross-reference against audit doc"
      pattern: "152-BEHAVIOUR_EXTRACTION_AUDIT"
---

<objective>
Build the demo CLI entry point and automated audit scripts so that TEST-04 (end-to-end demo flow), TEST-05 (hardcoded prompt audit), and TEST-06 (extraction audit) are verifiable.

Purpose: The acceptance phase needs a runnable demo and automated evidence that no behaviour leaked back into Go.
Output: `control-ts/src/cli.ts` (demo CLI), `scripts/audit-hardcoded.sh`, `scripts/audit-extraction.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase-159-end-to-end-acceptance/159-RESEARCH.md
@.planning/phases/phase-159-end-to-end-acceptance/159-PATTERNS.md
@.planning/phases/phase-159-end-to-end-acceptance/159-VALIDATION.md

<interfaces>
From control-ts/src/orchestrator/executePlan.ts:
```typescript
export interface PlanResult {
  status: "completed" | "failed" | "partial";
  results: PhaseResult[];
  events: ColonyEvent[];
  startedAt: string;
  completedAt: string;
}
export async function executePlan(
  sequence: string[],
  options?: { inputs?: Record<string, Record<string, unknown>>; maxRetries?: number; }
): Promise<PlanResult>;
```

From control-ts/src/orchestrator/runPhase.ts:
```typescript
export interface PhaseResult {
  phaseId: string;
  agentId: string;
  status: "completed" | "failed";
  events: ColonyEvent[];
  error?: string;
}
```

From control-ts/src/schemas/event.schema.ts:
```typescript
export function parseEventLine(line: string): ColonyEvent;
```

From control-ts/src/memory/store.ts:
```typescript
export function readColonyState(): ColonyState;
export function writeColonyState(state: ColonyState): void;
export function updateColonyState(updater: (s: ColonyState) => ColonyState): void;
```

From scripts/smoke-test-classic.sh (pattern for audit scripts):
- `set -euo pipefail`
- `PASS_COUNT=0`
- `pass()` and `fail()` helpers
- Exit summary: `echo "=== All audit checks passed (${PASS_COUNT}/N) ==="`

From 159-PATTERNS.md — known hardcoded directive pattern in cmd/oracle_loop.go (lines 1544-1557):
```go
func buildOraclePhaseDirective(phase string) string {
    switch strings.ToLower(strings.TrimSpace(phase)) {
    case "survey":
        return "Your task is to survey the landscape..."
    // ... 4 more cases
    default:
        return "Investigate pass: deepen the lowest-confidence unresolved question..."
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demo CLI entry point (control-ts/src/cli.ts)</name>
  <files>control-ts/src/cli.ts</files>
  <read_first>
    - control-ts/src/orchestrator/executePlan.ts (PlanResult interface, executePlan signature)
    - control-ts/src/schemas/event.schema.ts (parseEventLine signature)
    - control-ts/src/memory/store.ts (readColonyState, updateColonyState signatures)
    - control-ts/src/utils/projectRoot.ts (projectRoot export)
    - control-ts/package.json (confirm "aether:control": "tsx src/cli.ts" exists from Plan 01)
  </read_first>
  <action>
    Create control-ts/src/cli.ts that:
    1. Imports `executePlan` from `./orchestrator/executePlan.js`, `parseEventLine` from `./schemas/event.schema.js`, `readColonyState` and `updateColonyState` from `./memory/store.js`, and `projectRoot` from `./utils/projectRoot.js`.
    2. Parses `--task` from `process.argv` using simple index lookup (no external deps). Default task = "create a small test file and verify it".
    3. Defines a `main()` async function that:
       a. Calls `executePlan(["init", "plan", "build"])` with per-phase inputs: `{ init: { goal: task } }`.
       b. After `executePlan` resolves, reads the NDJSON events file at `resolve(projectRoot, "..", ".aether", "events", "current.ndjson")`.
       c. Parses each line with `parseEventLine`.
       d. Prints a summary to stdout: plan status, number of phases executed, number of events, and a list of event types in order.
       e. If status is "completed", exits with code 0. If "failed" or "partial", exits with code 1.
    4. Guards against unhandled promise rejection with `.catch((err) => { console.error(err); process.exit(1); })`.
    5. Does NOT perform real LLM API calls — `executePlan` uses stub adapters already built in Phase 157.
    6. Does NOT override `AETHER_EVENTS_FILE` — uses the default path so events are visible in `.aether/events/current.ndjson`.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/control-ts && npm run aether:control -- --task "create a small test file and verify it" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - control-ts/src/cli.ts exists and exports a `main` function
    - `npm run aether:control -- --task "create a small test file and verify it"` exits 0 and prints plan status, phase count, event count, and event types
    - The command writes events to `.aether/events/current.ndjson` (file exists and contains parseable NDJSON lines after run)
    - No real LLM API calls are made (stub adapters only)
  </acceptance_criteria>
  <done>Demo CLI runs end-to-end and emits events for each lifecycle step</done>
</task>

<task type="auto">
  <name>Task 2: Create hardcoded behaviour audit script (scripts/audit-hardcoded.sh)</name>
  <files>scripts/audit-hardcoded.sh</files>
  <read_first>
    - scripts/smoke-test-classic.sh (script structure pattern: set -euo pipefail, pass/fail helpers, PASS_COUNT)
    - .planning/phases/152-boundary-parity/152-BEHAVIOUR_EXTRACTION_AUDIT.md (classification table for cross-reference)
    - 159-PATTERNS.md (known hardcoded directive pattern in cmd/oracle_loop.go)
  </read_first>
  <action>
    Create scripts/audit-hardcoded.sh that audits Go source for hardcoded prompt/agent/phase text:
    1. Use `set -euo pipefail`.
    2. Define `PASS_COUNT=0`, `pass()`, and `fail()` helpers matching smoke-test-classic.sh pattern.
    3. Run these checks and increment PASS_COUNT for each pass:
       a. "No 'You are' agent directive strings in non-test Go files" — `grep -rn '"You are' cmd/ --include="*.go" | grep -v '_test.go' | grep -v 'fallback\|error\|fmt.Errorf'`. Count must be 0. If >0, list the matches and fail.
       b. "No hardcoded ceremony/ritual strings in non-test Go files" — `grep -rn 'ceremony\|ritual\|playbook' cmd/ --include="*.go" -l | grep -v '_test.go'`. Count must be 0. If >0, list files and fail.
       c. "No phase directive strings (e.g., 'Your task is to') in non-test Go files" — `grep -rn '"Your task is to' cmd/ --include="*.go" | grep -v '_test.go' | grep -v 'fallback\|error\|fmt.Errorf'`. Count must be 0. If >0, list matches and fail.
       d. "Fallback and error text is allowed" — document this as an informational pass (no grep needed, just echo that fallback/error text is permitted per boundary contract).
    4. Print summary: `=== Hardcoded behaviour audit passed (${PASS_COUNT}/4) ===`.
    5. Make the script executable (`chmod +x scripts/audit-hardcoded.sh`).
    6. Note: The 5 known strings in `cmd/oracle_loop.go` (buildOraclePhaseDirective) are the primary target. If they still exist, the audit must fail and report them.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && bash scripts/audit-hardcoded.sh 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - scripts/audit-hardcoded.sh exists and is executable
    - Running `bash scripts/audit-hardcoded.sh` from repo root exits 0
    - The script reports 4 checks with PASS/FAIL for each
    - Any remaining hardcoded "You are" or "Your task is to" strings in non-test Go files are flagged as FAIL with file:line context
    - The script explicitly allows fallback/error text
  </acceptance_criteria>
  <done>Audit script runs and confirms no hardcoded prompt text remains in Go (except fallback/error)</done>
</task>

<task type="auto">
  <name>Task 3: Create extraction audit script (scripts/audit-extraction.sh)</name>
  <files>scripts/audit-extraction.sh</files>
  <read_first>
    - scripts/smoke-test-classic.sh (script structure pattern)
    - .planning/phases/152-boundary-parity/152-BEHAVIOUR_EXTRACTION_AUDIT.md (classification table)
    - colony/agents/ (verify agent YAML files exist)
    - colony/prompts/ (verify prompt Markdown files exist)
    - colony/phases/ (verify phase YAML files exist)
  </read_first>
  <action>
    Create scripts/audit-extraction.sh that verifies no agent behaviour exists only in Go:
    1. Use `set -euo pipefail` and `pass()`/`fail()` helpers.
    2. Run these checks:
       a. "Agent YAML files exist" — verify `colony/agents/` contains at least 8 YAML files (queen, builder, watcher, scout, oracle, gatekeeper, auditor, probe). Count must be >= 8.
       b. "Prompt Markdown files exist" — verify `colony/prompts/` contains at least 8 Markdown files. Count must be >= 8.
       c. "Phase YAML files exist" — verify `colony/phases/` contains at least 6 YAML files (init, plan, build, continue, seal, oracle). Count must be >= 6.
       d. "No agent personality strings only in Go" — grep for `builder\|watcher\|scout\|queen\|oracle\|gatekeeper\|auditor\|probe` in `cmd/*.go` excluding `_test.go`, then cross-reference: if any file contains these strings AND the corresponding agent YAML does not exist, flag it. Simpler approach: grep for `"You are a.*builder"` or `"You are a.*watcher"` patterns in non-test Go files. Count must be 0.
       e. "Extraction audit doc is current" — verify `.planning/phases/152-boundary-parity/152-BEHAVIOUR_EXTRACTION_AUDIT.md` exists and contains at least 90 classified symbols (the doc already has 94). Count must be >= 90.
    3. Print summary with PASS_COUNT.
    4. Make executable.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && bash scripts/audit-extraction.sh 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - scripts/audit-extraction.sh exists and is executable
    - Running `bash scripts/audit-extraction.sh` from repo root exits 0
    - The script reports at least 5 checks with PASS/FAIL for each
    - Colony asset directories (agents, prompts, phases) are verified to contain the expected number of files
    - The extraction audit doc is verified to exist and contain >= 90 classified symbols
  </acceptance_criteria>
  <done>Extraction audit script runs and confirms no agent behaviour exists only in Go</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cli.ts -> filesystem | Writes events and colony state to .aether/ |
| audit scripts -> source code | Reads Go source and colony assets for analysis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-159-04 | Tampering | cli.ts | mitigate | Uses executePlan stub adapters; no untrusted input reaches file system beyond task string |
| T-159-05 | Information Disclosure | audit-hardcoded.sh | accept | Script only reads source code; no secrets involved |
| T-159-06 | Denial of Service | cli.ts | mitigate | Demo uses stub adapters with zero-delay simulation; no external network calls |
</threat_model>

<verification>
- `npm run aether:control -- --task "create a small test file and verify it"` completes with events (TEST-04)
- `bash scripts/audit-hardcoded.sh` passes, confirming no prompt text hardcoded in Go (TEST-05)
- `bash scripts/audit-extraction.sh` passes, confirming no agent behaviour exists only in Go (TEST-06)
</verification>

<success_criteria>
- control-ts/src/cli.ts exists and `npm run aether:control` runs successfully
- scripts/audit-hardcoded.sh exists, is executable, and passes
- scripts/audit-extraction.sh exists, is executable, and passes
- All three TEST-04/05/06 criteria are verifiable via automated commands
</success_criteria>

<output>
After completion, create `.planning/phases/159-end-to-end-acceptance/159-02-SUMMARY.md`
</output>
