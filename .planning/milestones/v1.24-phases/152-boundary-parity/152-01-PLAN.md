---
phase: 152-boundary-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/docs/ARCHITECTURE_BOUNDARY.md
  - .aether/docs/PARITY_CLASSIC_VS_GO.md
autonomous: true
requirements:
  - BOUNDARY-01
  - BOUNDARY-02
  - BOUNDARY-03
must_haves:
  truths:
    - "User can read ARCHITECTURE_BOUNDARY.md and understand what stays in Go, what moves to TS, and what moves to Markdown/YAML/JSON"
    - "User can read PARITY_CLASSIC_VS_GO.md and see 15+ verifiable items covering init ceremony, queen loads, worker spawn, planner, oracle loop, scout, build wave, watcher, gatekeeper, probe, memory, lessons, skills, events, recovery, and cleanup"
    - "The hard rule 'Compiled code may execute behaviour, but editable assets must define behaviour' is documented and referenced in both docs"
  artifacts:
    - path: ".aether/docs/ARCHITECTURE_BOUNDARY.md"
      provides: "Architecture boundary definition with three tiers and asset-type matrix"
      contains: "## What Stays in Go"
    - path: ".aether/docs/PARITY_CLASSIC_VS_GO.md"
      provides: "Classic parity checklist with 15+ verifiable items"
      contains: "| Classic Behaviour | Go Equivalent | Status |"
  key_links:
    - from: "ARCHITECTURE_BOUNDARY.md"
      to: "PARITY_CLASSIC_VS_GO.md"
      via: "cross-reference in boundary doc"
      pattern: "PARITY_CLASSIC_VS_GO.md"
---

<objective>
Create the two primary reference documents that define the hybrid architecture salvage: the architecture boundary doc and the Classic parity checklist.

Purpose: These docs are the north star for Phases 153-159. They tell every downstream planner and implementer what belongs where and what "correct" looks like.
Output: `.aether/docs/ARCHITECTURE_BOUNDARY.md` and `.aether/docs/PARITY_CLASSIC_VS_GO.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md §Phase 152
@.planning/REQUIREMENTS.md §Boundary & Parity
@.planning/phases/152-boundary-parity/152-CONTEXT.md
@.planning/milestones/v1.18-ROADMAP.md
@.planning/milestones/v1.18-REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@CLAUDE.md §UX Architecture

<interfaces>
From 152-CONTEXT.md decisions:
- D-01: Move hardcoded visuals from compiled Go to editable config files
- D-02: One shared visual config file across all platforms
- D-03: Format: Markdown with YAML frontmatter
- D-04: Location: `.aether/visuals.md` (or equivalent under `.aether/config/`)
- D-05: Full inventory of all Classic v5.4.0 behaviours
- D-06: When Classic and Go disagree, user decides per item
- D-07: Verification: automated golden/snapshot tests for 9 flagship workflows + manual checklist for edge cases
- D-11: ARCHITECTURE_BOUNDARY.md and PARITY_CLASSIC_VS_GO.md live in `.aether/docs/`
- D-13: Format: plain reference doc markdown with clear sections

From REQUIREMENTS.md:
- BOUNDARY-01: docs/ARCHITECTURE_BOUNDARY.md defining what stays in Go, what moves to TS, and what moves to Markdown/YAML/JSON
- BOUNDARY-02: Hard rule documented: "Compiled code may execute behaviour, but editable assets must define behaviour."
- BOUNDARY-03: Classic parity checklist at docs/PARITY_CLASSIC_VS_GO.md with 15+ verifiable items

From v1.18-ROADMAP.md:
- Classic baseline: v5.4.0
- Golden tests match v5.4 baseline for build, continue, Oracle, dashboard, install/update, state
- 9 flagship workflows: build, continue, plan, colonize, autopilot, seal, entomb, swarm, oracle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ARCHITECTURE_BOUNDARY.md</name>
  <files>.aether/docs/ARCHITECTURE_BOUNDARY.md</files>
  <read_first>
    - .planning/phases/152-boundary-parity/152-CONTEXT.md (decisions D-01 through D-13)
    - .planning/REQUIREMENTS.md (BOUNDARY-01, BOUNDARY-02)
    - .planning/codebase/ARCHITECTURE.md (current layer separation)
    - CLAUDE.md §UX Architecture (wrapper-runtime contract, caste identity, stage markers)
    - .planning/milestones/v1.18-ROADMAP.md (hybrid runtime boundary from v1.16)
  </read_first>
  <action>
    Create `.aether/docs/ARCHITECTURE_BOUNDARY.md` as a plain reference doc (not ADR template) with the following exact sections:

    1. **Opening statement** with the hard rule: "Compiled code may execute behaviour, but editable assets must define behaviour." This is BOUNDARY-02.

    2. **Three-tier boundary table** with columns: Layer, Owner, Examples, Rationale. Rows:
       - Go Runtime Spine (state mutation, verification, CLI truth, file locking, install/update/publish)
       - TypeScript Control Plane (orchestration, agent loader, phase runner, prompt assembler, event stream consumer)
       - Editable Assets (agent definitions, prompts, phases, playbooks, policies, skills, visual config, ceremony templates)
       - Bash Glue (small wrapper scripts, platform-specific command dispatch)

    3. **Asset-type matrix** with columns: Asset Type, Current Location, Target Location, Format, Status. Include at least these rows:
       - Agent definitions (currently in `.claude/agents/ant/*.md`, `.opencode/agents/*.md`, `.codex/agents/*.toml` → target `colony/agents/*.yaml`)
       - Prompt text (currently hardcoded in Go render functions → target `colony/prompts/*.md`)
       - Phase definitions (currently hardcoded in Go workflow commands → target `colony/phases/*.yaml`)
       - Playbooks (currently in `.aether/docs/command-playbooks/*.md` → target `colony/playbooks/*.md`)
       - Model-routing policy (currently in `.aether/workers.md` and agent frontmatter → target `colony/policies/model-routing.yaml`)
       - Memory/skill-creation policy (currently hardcoded in Go → target `colony/policies/*.yaml`)
       - Visual config (currently hardcoded in `cmd/codex_visuals.go` → target `.aether/visuals.md` or `.aether/config/visuals.md`)
       - Ceremony templates (currently hardcoded in Go render functions → target `colony/ceremony/*.md`)
       - Command definitions (currently in `.aether/commands/*.yaml` → stays in `.aether/commands/*.yaml`)
       - Worker specs (currently in `.aether/workers.md` → stays in `.aether/workers.md`)

    4. **Integration points** section describing how the three tiers communicate:
       - Go emits NDJSON events → TS control plane reads them
       - TS control plane calls Go CLI for state mutation (never writes `.aether/data` directly)
       - Go loads editable assets at runtime from `colony/` and `.aether/`
       - `aether update` distributes new assets alongside existing skills/commands

    5. **Migration sequence** section referencing the v1.24 phase order (152-159) and which asset types move in which phase.

    6. **Decision log** referencing D-01 through D-13 from 152-CONTEXT.md with brief rationale for each.

    7. **Cross-references** to PARITY_CLASSIC_VS_GO.md, REQUIREMENTS.md, and v1.18 milestone docs.

    Do NOT include fenced code blocks with full implementations. Use tables and bullet lists. Keep it under 300 lines.
  </action>
  <verify>
    <automated>grep -c "Compiled code may execute behaviour" .aether/docs/ARCHITECTURE_BOUNDARY.md | grep -q "1"</automated>
  </verify>
  <done>
    - File exists at `.aether/docs/ARCHITECTURE_BOUNDARY.md`
    - Contains the hard rule verbatim
    - Contains three-tier boundary table with Go/TS/Assets/Bash rows
    - Contains asset-type matrix with at least 10 rows
    - Contains integration points section
    - Contains migration sequence referencing phases 152-159
    - Contains decision log referencing D-01 through D-13
    - File is under 300 lines
  </done>
  <acceptance_criteria>
    - `grep -c "Compiled code may execute behaviour" .aether/docs/ARCHITECTURE_BOUNDARY.md` returns 1
    - `grep -c "## " .aether/docs/ARCHITECTURE_BOUNDARY.md` returns at least 7 (opening, tiers, matrix, integration, migration, decisions, cross-refs)
    - `grep -c "|" .aether/docs/ARCHITECTURE_BOUNDARY.md` returns at least 20 (tables present)
    - `grep -c "PARITY_CLASSIC_VS_GO.md" .aether/docs/ARCHITECTURE_BOUNDARY.md` returns at least 1
    - `wc -l .aether/docs/ARCHITECTURE_BOUNDARY.md` returns less than 300
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write PARITY_CLASSIC_VS_GO.md</name>
  <files>.aether/docs/PARITY_CLASSIC_VS_GO.md</files>
  <read_first>
    - .planning/phases/152-boundary-parity/152-CONTEXT.md (decisions D-05, D-06, D-07)
    - .planning/REQUIREMENTS.md (BOUNDARY-03)
    - .planning/milestones/v1.18-ROADMAP.md (Classic parity coverage, PAR-01 through PAR-07)
    - .planning/milestones/v1.18-REQUIREMENTS.md (PAR requirements)
    - .aether/docs/classic-command-parity-matrix.md (if exists)
    - .aether/docs/classic-command-parity-release-summary.md (if exists)
  </read_first>
  <action>
    Create `.aether/docs/PARITY_CLASSIC_VS_GO.md` as a plain reference doc with the following exact sections:

    1. **Purpose and scope** explaining this is the golden reference for Classic v5.4.0 vs current Go runtime, per D-05.

    2. **How to use this doc** explaining the table columns and the decision rule: when Classic and Go disagree, user decides per item (per D-06) — no blanket "Classic wins" or "Go wins".

    3. **Parity checklist table** with columns: Classic Behaviour, Go Equivalent, Status (MATCH/GAP/DEGRADED/INTENTIONALLY_CHANGED), Verification Method, Decision (Classic/Go/Hybrid/PENDING). Include at least 15 rows covering:
       - Init ceremony (charter, research, launch brief)
       - Queen loads (colony-prime context assembly, pheromone injection, skill matching)
       - Worker spawn (platform dispatch, spawn-log/spawn-complete, caste identity)
       - Planner (scout planning, route-setter phase breakdown, oracle loop)
       - Oracle loop (RALF, confidence iteration, research planning)
       - Scout (codebase survey, territory mapping)
       - Build wave (manifest generation, worker brief assembly, parallel execution)
       - Watcher (verification depth, quality gates, test coverage)
       - Gatekeeper (security scan, antipattern check)
       - Probe (coverage analysis, test gap identification)
       - Memory (learning pipeline, instinct promotion, hive store)
       - Lessons (phase learnings, hypothesis tracking, validated/disproven)
       - Skills (skill index, skill match, skill inject)
       - Events (event bus, NDJSON stream, event TTL)
       - Recovery (stuck-state detection, auto-repair, resume)
       - Cleanup (data-clean, midden review, session sync)

    4. **Verification methods** section per D-07:
       - Automated: golden/snapshot tests for the 9 flagship workflows (build, continue, plan, colonize, autopilot, seal, entomb, swarm, oracle)
       - Manual: checklist steps for edge cases and ceremony
       - Reference: `v1.18-ROADMAP.md` PAR-01 through PAR-07 as already-verified baseline

    5. **Known gaps** section listing any Classic behaviours intentionally not restored (reference PAR-07 from v1.18).

    6. **Decision log** for any items where Classic vs Go was already decided in prior milestones.

    7. **Cross-references** to ARCHITECTURE_BOUNDARY.md, v1.18 milestone docs, and REQUIREMENTS.md.

    Do NOT include full test code or implementation details. Use the table format from D-05's specific idea: "Classic behaviour | Go equivalent | Status | Verification method | Decision".
  </action>
  <verify>
    <automated>grep -c "MATCH\|GAP\|DEGRADED\|INTENTIONALLY_CHANGED" .aether/docs/PARITY_CLASSIC_VS_GO.md | grep -q "[1-9]"</automated>
  </verify>
  <done>
    - File exists at `.aether/docs/PARITY_CLASSIC_VS_GO.md`
    - Contains parity checklist table with at least 15 rows
    - Covers all required areas: init ceremony, queen loads, worker spawn, planner, oracle loop, scout, build wave, watcher, gatekeeper, probe, memory, lessons, skills, events, recovery, cleanup
    - Contains verification methods section with automated + manual
    - Contains known gaps section
    - References v1.18 PAR-01 through PAR-07
    - References ARCHITECTURE_BOUNDARY.md
  </done>
  <acceptance_criteria>
    - `grep -c "MATCH\|GAP\|DEGRADED\|INTENTIONALLY_CHANGED" .aether/docs/PARITY_CLASSIC_VS_GO.md` returns at least 1
    - `grep -c "|" .aether/docs/PARITY_CLASSIC_VS_GO.md` returns at least 30 (table rows present)
    - `grep -c "init ceremony\|queen loads\|worker spawn\|planner\|oracle loop\|scout\|build wave\|watcher\|gatekeeper\|probe\|memory\|lessons\|skills\|events\|recovery\|cleanup" .aether/docs/PARITY_CLASSIC_VS_GO.md` returns at least 15
    - `grep -c "ARCHITECTURE_BOUNDARY.md" .aether/docs/PARITY_CLASSIC_VS_GO.md` returns at least 1
    - `grep -c "v1.18" .aether/docs/PARITY_CLASSIC_VS_GO.md` returns at least 1
    - `grep -c "golden\|snapshot\|flagship" .aether/docs/PARITY_CLASSIC_VS_GO.md` returns at least 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → runtime | Docs describe intent; runtime implements it. Mismatch is a documentation bug, not a security issue. |
| Classic → Go | Classic is a behavioural baseline, not a live system. No runtime trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-01 | Information Disclosure | ARCHITECTURE_BOUNDARY.md | accept | Public reference doc; no secrets. |
| T-152-02 | Tampering | PARITY_CLASSIC_VS_GO.md | mitigate | Version-controlled; changes require PR review. Doc includes "last updated" timestamp. |
</threat_model>

<verification>
- Both files exist in `.aether/docs/`
- ARCHITECTURE_BOUNDARY.md contains the hard rule and three-tier boundary
- PARITY_CLASSIC_VS_GO.md contains 15+ row parity table
- Both files cross-reference each other
- `grep` counts match acceptance criteria
</verification>

<success_criteria>
- BOUNDARY-01 satisfied: ARCHITECTURE_BOUNDARY.md exists and defines what stays in Go, what moves to TS, and what moves to Markdown/YAML/JSON
- BOUNDARY-02 satisfied: Hard rule is documented verbatim in ARCHITECTURE_BOUNDARY.md
- BOUNDARY-03 satisfied: PARITY_CLASSIC_VS_GO.md exists with 15+ verifiable items covering all required areas
</success_criteria>

<output>
After completion, create `.planning/phases/152-boundary-parity/152-01-SUMMARY.md`
</output>
