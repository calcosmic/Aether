---
phase: 156-ts-control-plane-core
plan: 02
type: execute
wave: 2
depends_on:
  - 156-01
files_modified:
  - control-ts/src/skills/loadSkills.ts
  - control-ts/src/memory/store.ts
  - control-ts/src/orchestrator/runPhase.ts
  - control-ts/tests/skills/loadSkills.test.ts
  - control-ts/tests/memory/store.test.ts
  - control-ts/tests/orchestrator/runPhase.test.ts
  - control-ts/src/types/index.ts
  - control-ts/src/index.ts
autonomous: true
requirements:
  - CONTROL-05
  - CONTROL-09
  - CONTROL-10
must_haves:
  truths:
    - "User can run runPhase.ts with a phase ID and see it load the phase definition, select the entry agent, and emit events"
    - "User can run a script that loads skills from control-ts/src/skills/ and reads/writes memory via control-ts/src/memory/"
  artifacts:
    - path: "control-ts/src/skills/loadSkills.ts"
      provides: "Skill loader — reads .aether/skills/, parses frontmatter, matches by role + task + pheromones"
      exports: ["loadSkills", "matchSkillsForWorker"]
    - path: "control-ts/src/memory/store.ts"
      provides: "Memory store — read/write COLONY_STATE.json with typed accessors and atomic writes"
      exports: ["readColonyState", "writeColonyState", "updateColonyState"]
    - path: "control-ts/src/orchestrator/runPhase.ts"
      provides: "Phase runner — accepts phase ID, loads phase, selects entry agent, emits NDJSON events"
      exports: ["runPhase"]
    - path: "control-ts/tests/skills/loadSkills.test.ts"
      provides: "Tests for skill loader"
    - path: "control-ts/tests/memory/store.test.ts"
      provides: "Tests for memory store"
    - path: "control-ts/tests/orchestrator/runPhase.test.ts"
      provides: "Tests for phase runner"
  key_links:
    - from: "control-ts/src/orchestrator/runPhase.ts"
      to: "control-ts/src/phases/loadPhases.ts"
      via: "import { loadPhaseById }"
      pattern: "loadPhaseById"
    - from: "control-ts/src/orchestrator/runPhase.ts"
      to: "control-ts/src/agents/loadAgents.ts"
      via: "import { loadAgentById }"
      pattern: "loadAgentById"
    - from: "control-ts/src/orchestrator/runPhase.ts"
      to: ".aether/events/current.ndjson"
      via: "appendFileSync with NDJSON line"
      pattern: "appendFileSync.*ndjson"
    - from: "control-ts/src/memory/store.ts"
      to: ".aether/data/COLONY_STATE.json"
      via: "readFileSync / writeFileSync"
      pattern: "COLONY_STATE\\.json"
    - from: "control-ts/src/skills/loadSkills.ts"
      to: ".aether/skills/colony/ and domain/"
      via: "readdirSync recursive"
      pattern: "skills/.*SKILL\\.md"
---

<objective>
Build three runtime modules: skill loader, memory store, and phase runner. The phase runner depends on the loaders from Plan 01 and is the first executable orchestration component. Skills and memory are independent subsystems that the runner (and later the plan executor) will consume.

Purpose: These modules transform the control plane from a read-only asset catalog into a runtime that can execute colony behaviour, persist state, and inject skills into workers.
Output: Skill loader, memory store, phase runner, all with tests, wired into the public API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-ts-control-plane-core/156-01-PLAN.md
@control-ts/src/schemas/event.schema.ts
@control-ts/src/utils/projectRoot.ts

<interfaces>
From control-ts/src/schemas/event.schema.ts:
```typescript
export const EventSchema = z.object({
  type: z.string().min(1),
  timestamp: z.string().datetime({ offset: true }),
  payload: z.record(z.string(), z.unknown()),
});
export type ColonyEvent = z.infer<typeof EventSchema>;
export function parseEventLine(line: string): ColonyEvent;
```

From control-ts/src/utils/projectRoot.ts:
```typescript
export const projectRoot = resolve(dirname(fileURLToPath(import.meta.url)), "../..");
```

Skill file format (from .aether/skills/colony/acceptance-test-generation/SKILL.md):
```yaml
---
source: shipped
name: acceptance-test-generation
description: "..."
type: colony
domains: [testing, quality-assurance, verification]
agent_roles: [watcher, probe, builder]
workflow_triggers: [build, continue]
task_keywords: [test, acceptance, criteria, coverage, e2e, unit]
priority: normal
version: "1.0"
---
```

Colony state file format (from .aether/data/COLONY_STATE.json):
```json
{
  "version": "3.0",
  "goal": "...",
  "scope": "project",
  "colony_mode": "colony",
  "state": "COMPLETED",
  "current_phase": 7,
  "session_id": "...",
  "initialized_at": "...",
  "plan": { "generated_at": "...", "confidence": 88, "phases": [...] }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build skill loader and memory store</name>
  <files>
    control-ts/src/skills/loadSkills.ts
    control-ts/src/memory/store.ts
    control-ts/tests/skills/loadSkills.test.ts
    control-ts/tests/memory/store.test.ts
  </files>
  <behavior>
    - Test 1 (skills): loadSkills() returns an array of skill objects parsed from .aether/skills/colony/ and domain/ SKILL.md files
    - Test 2 (skills): Each skill has name, type, agent_roles, task_keywords, and description extracted from frontmatter
    - Test 3 (skills): matchSkillsForWorker("builder", "build", []) returns skills whose agent_roles include "builder"
    - Test 4 (skills): matchSkillsForWorker with pheromone FOCUS signals boosts matching skills
    - Test 5 (memory): readColonyState() reads and parses .aether/data/COLONY_STATE.json, returns typed object
    - Test 6 (memory): writeColonyState(data) serializes and writes atomically to .aether/data/COLONY_STATE.json
    - Test 7 (memory): updateColonyState(fn) reads current state, applies fn, writes back atomically
    - Test 8 (memory): writeColonyState creates parent directories if they do not exist
  </behavior>
  <action>
    Create control-ts/src/skills/loadSkills.ts with two exported functions:
    - loadSkills(): recursively reads .aether/skills/colony/ and .aether/skills/domain/ for SKILL.md files. For each file, extract YAML frontmatter (text between first "---" and second "---"), parse with yaml.parse, and return an array of Skill objects. The Skill type must include: name (string), type ("colony" | "domain"), description (string), agent_roles (string[]), task_keywords (string[]), domains (string[]), workflow_triggers (string[]), priority (string), source (string), version (string), content (string — the markdown body after frontmatter). Use readFileSync and simple string splitting to separate frontmatter from body. If a SKILL.md lacks frontmatter, skip it with a console.warn. If frontmatter parse fails, skip with console.warn including file path.
    - matchSkillsForWorker(role: string, task: string, pheromones: { type: string; content: string }[]): returns Skill[] sorted by relevance. Relevance scoring: +2 for role match in agent_roles, +1 for task keyword match in task_keywords, +1 for each pheromone whose content includes a task_keyword. Return top 6 skills (3 colony + 3 domain) sorted by score descending. If fewer than 6, return all.

    Create control-ts/src/memory/store.ts with three exported functions:
    - readColonyState(): reads .aether/data/COLONY_STATE.json, parses JSON, returns ColonyState type. If file does not exist, return a default empty state object: { version: "3.0", goal: "", scope: "project", colony_mode: "colony", state: "INIT", current_phase: 0, session_id: "", initialized_at: new Date().toISOString(), plan: { generated_at: new Date().toISOString(), confidence: 0, phases: [] } }. Type definition: export interface ColonyState { version: string; goal: string; scope: string; colony_mode: string; state: string; current_phase: number; session_id: string; initialized_at: string; plan: { generated_at: string; confidence: number; phases: unknown[] }; }.
    - writeColonyState(state: ColonyState): writes JSON to .aether/data/COLONY_STATE.json atomically. Use writeFileSync to a temp path (.aether/data/COLONY_STATE.json.tmp), then renameSync to final path. Create parent directories with mkdirSync(..., { recursive: true }) if needed. Use JSON.stringify(state, null, 2).
    - updateColonyState(updater: (state: ColonyState) => ColonyState): reads current state, applies updater, writes back atomically. Wrap in try/catch; on error, throw with "Memory update failed: {message}".

    Write tests in control-ts/tests/skills/loadSkills.test.ts and control-ts/tests/memory/store.test.ts. For skills, verify against real .aether/skills/ directory. For memory, use a temporary file path or mock the filesystem; if mocking is complex, use a temp directory under tests/fixtures/ and clean up after each test. At minimum, verify read/write roundtrip and updateColonyState mutation.
  </action>
  <verify>
    <automated>cd control-ts && npm test -- tests/skills/loadSkills.test.ts tests/memory/store.test.ts</automated>
  </verify>
  <done>
    - loadSkills.ts loads and parses all SKILL.md files with frontmatter extraction
    - matchSkillsForWorker filters and ranks skills by role, task, and pheromones
    - store.ts provides readColonyState, writeColonyState, updateColonyState with atomic writes
    - All tests pass
    - npm run typecheck passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build phase runner with event emission</name>
  <files>
    control-ts/src/orchestrator/runPhase.ts
    control-ts/tests/orchestrator/runPhase.test.ts
  </files>
  <behavior>
    - Test 1: runPhase("init") loads the init phase, finds the queen agent, and returns a result with phaseId, agentId, and status "completed"
    - Test 2: runPhase emits at least two NDJSON events: "phase:start" and "phase:complete" with valid ColonyEvent shape
    - Test 3: runPhase with nonexistent phase ID throws "Phase not found: {id}"
    - Test 4: runPhase with phase whose entry_agent does not exist throws "Entry agent not found: {agentId}"
    - Test 5: Emitted events are appended to .aether/events/current.ndjson and are parseable with parseEventLine
    - Test 6: runPhase accepts optional inputs object and includes it in the phase:start event payload
  </behavior>
  <action>
    Create control-ts/src/orchestrator/runPhase.ts with one exported function:
    - runPhase(phaseId: string, inputs?: Record<string, unknown>): Promise<PhaseResult>

    PhaseResult type:
    ```typescript
    export interface PhaseResult {
      phaseId: string;
      agentId: string;
      status: "completed" | "failed" | "skipped";
      events: ColonyEvent[];
      error?: string;
    }
    ```

    Implementation steps:
    1. Call loadPhaseById(phaseId) from ../phases/loadPhases.js. If null, throw Error("Phase not found: {phaseId}").
    2. Call loadAgentById(phase.entry_agent) from ../agents/loadAgents.js. If null, throw Error("Entry agent not found: {phase.entry_agent}").
    3. Ensure .aether/events/ directory exists (mkdirSync recursive).
    4. Emit "phase:start" event: { type: "phase:start", timestamp: new Date().toISOString(), payload: { phaseId, agentId: agent.id, inputs } }.
    5. Append the JSON-stringified event line to .aether/events/current.ndjson (with newline).
    6. Simulate execution: since Phase 157 builds adapters, this runner simulates the phase by creating a mock result. The "execution" is: wait 0ms (no actual adapter call), set status to "completed", emit "phase:complete" event with payload { phaseId, agentId: agent.id, status: "completed" }.
    7. Return PhaseResult with all emitted events.

    On any error during steps 4-6, catch, emit "phase:failed" event, and return PhaseResult with status "failed" and error message.

    Use appendFileSync for NDJSON writes. Use EventSchema.parse to validate each event before appending (defensive). Use projectRoot to resolve the events file path.

    Write tests in control-ts/tests/orchestrator/runPhase.test.ts. Use real colony/ assets. Before each test, clear .aether/events/current.ndjson or write to a temporary events file. Verify events are valid NDJSON lines by reading the file and parsing each line with parseEventLine. Verify result shape.
  </action>
  <verify>
    <automated>cd control-ts && npm test -- tests/orchestrator/runPhase.test.ts</automated>
  </verify>
  <done>
    - runPhase.ts loads phase and agent, emits NDJSON events, returns PhaseResult
    - Tests verify event emission, error handling, and result shape
    - npm run typecheck passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire public API exports for runtime modules</name>
  <files>
    control-ts/src/types/index.ts
    control-ts/src/index.ts
  </files>
  <action>
    Update control-ts/src/types/index.ts to export new types:
    - export type { Skill } from "../skills/loadSkills.js";
    - export type { ColonyState, PhaseResult } from "../memory/store.js" and "../orchestrator/runPhase.js" (or create a shared types file if circular imports arise).

    If circular imports between memory/store.ts and orchestrator/runPhase.ts are problematic, create control-ts/src/types/runtime.ts with ColonyState and PhaseResult, and have both modules import from there.

    Update control-ts/src/index.ts to re-export:
    - loadSkills, matchSkillsForWorker
    - readColonyState, writeColonyState, updateColonyState
    - runPhase
    - Skill, ColonyState, PhaseResult types

    Keep the barrel focused — no internal helpers.
  </action>
  <verify>
    <automated>cd control-ts && npm run typecheck</automated>
  </verify>
  <done>
    - All new runtime symbols exported from index.ts
    - typecheck passes with no errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system -> Skill loader | SKILL.md content from disk crosses into parsed objects |
| File system -> Memory store | COLONY_STATE.json read/write is the colony truth boundary |
| Orchestrator -> Event stream | runPhase appends to shared NDJSON file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-06 | Tampering | memory/store.ts | mitigate | Atomic write via temp-file + renameSync prevents partial writes on crash |
| T-156-07 | Denial of Service | skills/loadSkills.ts | mitigate | Recursive read bounded to .aether/skills/ subtree; no symlink following |
| T-156-08 | Information Disclosure | memory/store.ts | accept | COLONY_STATE.json contains colony metadata, not secrets; secrets live in env |
| T-156-09 | Repudiation | orchestrator/runPhase.ts | mitigate | Every phase run appends timestamped events to NDJSON; immutable append-only log |
| T-156-10 | Elevation of Privilege | orchestrator/runPhase.ts | accept | No privilege escalation path; runner only reads colony assets and writes events |
</threat_model>

<verification>
- All new test files pass: cd control-ts && npm test
- TypeScript typecheck passes: cd control-ts && npm run typecheck
- Manual spot-check: npx tsx -e "import { runPhase } from './src/orchestrator/runPhase.ts'; runPhase('init').then(r => console.log(r.status))" should print "completed"
- Manual spot-check: npx tsx -e "import { loadSkills } from './src/skills/loadSkills.ts'; console.log((await loadSkills()).length)" should print a positive number
- Manual spot-check: npx tsx -e "import { readColonyState } from './src/memory/store.ts'; console.log(readColonyState().version)" should print "3.0"
</verification>

<success_criteria>
- runPhase.ts executes a phase by ID, loads its definition and entry agent, emits NDJSON events, and returns a PhaseResult
- loadSkills.ts parses all SKILL.md files with frontmatter and supports worker matching
- store.ts provides atomic read/write for COLONY_STATE.json with typed accessors
- All tests pass (vitest run)
- TypeScript compiles without errors (npm run typecheck)
- All new modules are exported from control-ts/src/index.ts
</success_criteria>

<output>
After completion, create `.planning/phases/156-ts-control-plane-core/156-02-SUMMARY.md`
</output>
