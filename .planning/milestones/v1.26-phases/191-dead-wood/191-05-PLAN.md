---
phase: 191-dead-wood
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/trace/cost.go
  - pkg/agent/pool.go
  - cmd/helpers.go
  - cmd/skills.go
  - cmd/skills_test.go
  - cmd/testdata/orphan_allowlist.json
  - cmd/subcommand_reachability_ratchet_test.go
autonomous: true
requirements: [SKILL-01]

must_haves:
  truths:
    - "pkg/trace/cost.go no longer exists, and pkg/agent/pool.go no longer contains any reference to it -- proven by go build succeeding"
    - "newLearningValidator no longer exists anywhere in the codebase"
    - "aether skill-index, skill-detect, skill-match, skill-inject, skill-list, skill-diff, skill-parse-frontmatter, and skill-cache-rebuild no longer exist as standalone CLI subcommands"
    - "matchSkillsForWorkflow, resolveSkillMatchInput, and renderSkillInjectResult still exist, completely unmodified in behavior, and cmd/codex_build.go:3268's live call to matchSkillsForWorkflow for worker-brief skill injection still functions identically -- proven by the full existing build-brief test suite passing unmodified"
    - "cmd/testdata/orphan_allowlist.json and cmd/subcommand_reachability_ratchet_test.go's D-08 block are internally consistent with the 8 commands no longer existing -- go test ./cmd/... passes"
  artifacts:
    - path: "cmd/skills.go"
      provides: "matchSkillsForWorkflow, resolveSkillMatchInput, renderSkillInjectResult and any other still-live helper, with the 8 dead CLI wrappers removed"
      exports: ["matchSkillsForWorkflow"]
    - path: "cmd/subcommand_reachability_ratchet_test.go"
      provides: "A D-08 block that records the SKILL-01 ruling-by-deletion instead of measuring a set that no longer exists"
  key_links:
    - from: "cmd/codex_build.go:3268 (composeBuildManifestBrief)"
      to: "cmd/skills.go:767 (matchSkillsForWorkflow)"
      via: "direct, in-process Go function call -- the live worker-brief skill-injection path"
      pattern: "matchSkillsForWorkflow\\("
---

<objective>
Remove four confirmed-dead pieces of Go code named in ROADMAP Phase 191 criterion 3, each verified by a
distinct evidentiary standard rather than assumed from the ROADMAP's summary text:

1. `pkg/trace/cost.go` + the "unconstructed pool path" — `trace.CalculateCost`'s only caller is
   `pkg/agent.Pool`'s `poolStreamHandler.OnComplete`, and `agent.NewPool` has zero production (`cmd/`)
   callers (only its own package's tests). Delete `cost.go`; remove the now-dead cost-tracking block
   from `pool.go`'s `OnComplete` (required for `pool.go` to keep compiling).
2. `newLearningValidator` (`cmd/helpers.go:198-202`) — confirmed zero callers anywhere in the repo.
   Delete outright.
3. **The true SKILL-01 8-command set** — `skill-index`, `skill-detect`, `skill-match`, `skill-inject`,
   `skill-list`, `skill-diff`, `skill-parse-frontmatter`, `skill-cache-rebuild` (`cmd/skills.go`).
   Delete the standalone CLI wrappers ONLY. This is the highest-stakes edit in this phase: the
   underlying function `matchSkillsForWorkflow` (called by both `skill-match`'s and `skill-inject`'s
   `RunE` closures) is ALSO called directly, in-process, by `cmd/codex_build.go:3268` — the live
   worker-brief skill-injection mechanism. Get the CLI-surface-vs-logic distinction wrong here and the
   build's skill-injection feature breaks silently. See 191-PATTERNS.md
   §"CLI-Surface Deletion Is Not Logic Deletion" before touching this file.
4. Update `cmd/testdata/orphan_allowlist.json` and `cmd/subcommand_reachability_ratchet_test.go`'s D-08
   block (`skillLifecycleOrphanCandidates`, `resolveSkillLifecyclePaths`, the `len(phase178) != 6`
   assertion) so they stay internally consistent once the 8 commands no longer exist to be measured —
   `resolveSkillLifecyclePaths` currently `t.Fatalf`s if any of the 8 leaf names fails to resolve via
   `rootCmd.Find`, so this MUST be updated in the same change, not after.

**Explicitly NOT in scope this task** (191-CONTEXT.md D-14): the 8 CRUD-authoring skill-lifecycle
commands (`skill-patch`, `skill-archive`, `skill-pin`, `skill-list-lifecycle`, `skill-promote`,
`skill-view`, `skill-curator-run`, `skill-recover`) are a different, unreviewed subsystem per the
existing reachability test's own comment (lines 1204-1207) — not SKILL-01's scope, not touched.
`session-verify-fresh` is explicitly NOT deleted (191-CONTEXT.md D-07 — it has a live caller in the
medic wrapper; the ROADMAP's characterization of it as dead code is stale) — do not remove anything
named `session-verify-fresh`/`sessionVerifyFresh` in `cmd/session_cmds.go` or `cmd/session_flow_cmds.go`.

Purpose: ROADMAP criterion 3. Delivers SKILL-01 in full, as a ruling-by-deletion.

Output: 2 files deleted (cost.go, none other), ~9 dead functions/commands removed, `matchSkillsForWorkflow`
and its live callers unconditionally preserved, orphan-allowlist and reachability-ratchet kept internally
consistent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/191-dead-wood/191-CONTEXT.md
@.planning/phases/191-dead-wood/191-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing contracts the executor must build against -- extracted from the codebase. -->

From pkg/agent/pool.go (existing -- the block to remove, not the whole file):
```go
// poolStreamHandler.OnComplete (~line 178-190). Remove ONLY the cost-tracking
// conditional block that calls trace.CalculateCost/h.tracer.LogTokenUsage.
// Keep everything else in this file and in this function unmodified -- Pool,
// NewPool, and the rest of poolStreamHandler are OUT OF SCOPE (191-CONTEXT.md
// D-06). Read pool_test.go and pool_streaming_test.go first to confirm
// neither test asserts specifically on the block being removed.
func (h *poolStreamHandler) OnComplete(result *llm.StreamResult) {
	// ... unrelated payload/publishEvent code stays ...
	if h.onTokenUsage != nil {
		h.onTokenUsage(result.Model, result.Usage.InputTokens, result.Usage.OutputTokens)
	}
	if h.tracer != nil && h.runID != "" {
		cost := trace.CalculateCost(result.Model, result.Usage.InputTokens, result.Usage.OutputTokens)
		_ = h.tracer.LogTokenUsage(h.runID, result.Model, result.Usage.InputTokens, result.Usage.OutputTokens, cost, "agent-pool")
	} // <-- this whole if-block is the one to remove
}
```

From cmd/skills.go (existing -- the exact functions to preserve, verbatim, regardless of what else in
this file changes):
```go
func matchSkillsForWorkflow(hub, workflow, role, task string) skillMatchResult // line ~767 -- KEEP,
// called live by cmd/codex_build.go:3268
func resolveSkillMatchInput(cmd *cobra.Command, args []string) (role, task string) // KEEP if still
// referenced after the 8 wrappers are gone -- check via grep after editing
func renderSkillInjectResult(...) ... // KEEP if still referenced -- check via grep after editing
```

From cmd/subcommand_reachability_ratchet_test.go (existing -- the block that must change together):
```go
// lines 95-111
var skillLifecycleOrphanCandidates = map[string]bool{
	"skill-index": true, "skill-detect": true, "skill-match": true, "skill-inject": true,
	"skill-list": true, "skill-diff": true, "skill-parse-frontmatter": true, "skill-cache-rebuild": true,
}
// lines 113-131
func resolveSkillLifecyclePaths(t *testing.T) map[string]bool // t.Fatalf's if any leaf name above
// fails to resolve via rootCmd.Find -- WILL fire the moment the 8 commands are deleted unless this
// whole block is rewritten in the same change
// lines ~1200-1236: the D-08 comment block and the `len(phase178) != 6` assertion
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete cost.go's unconstructed pool path and newLearningValidator</name>

  <files>pkg/trace/cost.go, pkg/agent/pool.go, cmd/helpers.go</files>

  <read_first>
    - `pkg/trace/cost.go` (full file, 39 lines) -- confirm no other caller has appeared since planning:
      `grep -rn "trace.CalculateCost\|CalculateCost(" cmd/ pkg/` should show only `pkg/agent/pool.go`
    - `pkg/agent/pool.go` (full file) -- `poolStreamHandler.OnComplete` in full context, to remove only
      the specific cost-tracking block, not the surrounding event-publishing code
    - `pkg/agent/pool_test.go`, `pkg/agent/pool_streaming_test.go` (full files) -- confirm neither test
      makes an assertion specifically about the cost-tracking block's behavior (if one does, that
      assertion is testing dead-path behavior and must be removed, not preserved by faking a caller)
    - `pkg/trace/trace.go` -- confirm `LogTokenUsage`'s definition and its doc comment; do NOT delete
      `LogTokenUsage` itself (a different file, not named for deletion — 191-CONTEXT.md D-06 scopes this
      narrowly to cost.go and pool.go's one call site)
    - `cmd/helpers.go:190-210` -- `newLearningValidator`'s full context; confirm zero callers remain via
      `grep -rn "newLearningValidator" cmd/ pkg/` (should show only this file's own comment + signature)
  </read_first>

  <behavior>
    - `go build ./...` succeeds after `pkg/trace/cost.go` is deleted and `pool.go`'s `OnComplete` no
      longer references `trace.CalculateCost` or the cost-derived `LogTokenUsage` call.
    - `pkg/agent/pool_test.go` and `pkg/agent/pool_streaming_test.go` pass unmodified (or, if either
      asserted specifically on the removed block, that specific assertion is removed with a note in the
      SUMMARY explaining it tested dead-path behavior).
    - `newLearningValidator` no longer exists anywhere; `cmd/helpers_test.go` (if it references the
      function) is updated accordingly.
  </behavior>

  <action>
Delete `pkg/trace/cost.go` (`git rm`). In `pkg/agent/pool.go`'s `poolStreamHandler.OnComplete`, remove
the `if h.tracer != nil && h.runID != "" { cost := trace.CalculateCost(...); _ =
h.tracer.LogTokenUsage(...) }` block shown in this plan's `<interfaces>` section, and remove the now-unused
`"github.com/calcosmic/Aether/pkg/trace"` import if nothing else in the file uses it (check via grep
within the file first). Leave every other line of `pool.go` untouched — do not remove `Pool`, `NewPool`,
`Registry` usage, or any other method.

In `cmd/helpers.go`, delete `newLearningValidator` (its doc comment and function body, lines ~198-202
and however far the function body extends). If `cmd/helpers_test.go` references it, remove that test
too (it would be testing dead code with zero production callers).

Run `pkg/agent`'s and `cmd`'s test suites; confirm both pass.
  </action>

  <verify>
    <automated>go build ./... && go vet ./... && go test ./pkg/agent/... ./pkg/trace/... -count=1 -v</automated>
  </verify>

  <acceptance_criteria>
    - `pkg/trace/cost.go` no longer exists.
    - `grep -rn "CalculateCost\|newLearningValidator" cmd/ pkg/` finds zero matches anywhere in the
      repository.
    - `go build ./... && go vet ./...` exit 0.
    - `go test ./pkg/agent/... ./pkg/trace/... ./cmd/... -count=1` all exit 0.
  </acceptance_criteria>

  <done>pkg/trace/cost.go and newLearningValidator are gone; pool.go compiles cleanly with no reference to either; every existing pkg/agent and pkg/trace test still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the 8 skill-lifecycle CLI wrappers; preserve the live skill-injection logic; reconcile the reachability ratchet</name>

  <files>cmd/skills.go, cmd/skills_test.go, cmd/testdata/orphan_allowlist.json, cmd/subcommand_reachability_ratchet_test.go</files>

  <read_first>
    - 191-PATTERNS.md §"CLI-Surface Deletion Is Not Logic Deletion" -- the full four-step discipline
      before deleting any command found by a reachability scanner
    - `cmd/skills.go` lines 151-410 (full) -- every one of the 8 target `cobra.Command` definitions and
      their `RunE` closures, plus `matchSkillsForWorkflow` (line ~767) and everything it calls internally
    - `cmd/codex_build.go:3268` and its enclosing function `composeBuildManifestBrief` -- the live,
      in-process caller that MUST keep working identically after this task
    - `cmd/skills_test.go` (full file) -- which existing tests exercise the 8 doomed commands
      specifically (via their CLI path, e.g. `rootCmd.SetArgs([]string{"skill-index", ...})`) vs. which
      exercise `matchSkillsForWorkflow`/related helpers directly as Go functions (the latter must survive
      unmodified; the former test dead CLI surface and should be removed with the commands)
    - `cmd/subcommand_reachability_ratchet_test.go` lines 93-131 (`skillLifecycleOrphanCandidates`,
      `resolveSkillLifecyclePaths`) and lines ~1152-1237 (T-172-07 boundary proof, orphan-allowlist
      compare, D-08 count assertions) -- full read required; `resolveSkillLifecyclePaths` `t.Fatalf`s on
      any of the 8 leaf names failing to resolve, so this block must change in the SAME commit as the
      command deletion
    - `cmd/testdata/orphan_allowlist.json` -- locate the exact entries (by `name` field) for all 8 target
      commands (`aether skill-index`, `aether skill-detect`, `aether skill-match`, `aether skill-inject`,
      `aether skill-list`, `aether skill-diff`, `aether skill-parse-frontmatter`,
      `aether skill-cache-rebuild`)
  </read_first>

  <behavior>
    - `aether skill-index`, `skill-detect`, `skill-match`, `skill-inject`, `skill-list`, `skill-diff`,
      `skill-parse-frontmatter`, `skill-cache-rebuild` no longer exist as invocable CLI subcommands
      (`rootCmd.Find([]string{"skill-index"})` etc. returns "command not found" or resolves to `rootCmd`
      itself).
    - `matchSkillsForWorkflow`, `resolveSkillMatchInput`, and any other helper still reachable after the
      8 wrappers are removed remain byte-identical in behavior. `cmd/codex_build.go:3268`'s call
      continues to compile and function exactly as before -- proven by running the existing build-brief
      test suite (whatever test currently covers `composeBuildManifestBrief`'s skill-section assembly)
      unmodified and green.
    - `buildFullIndex`, `loadSkillIndexOrBuild`, `skillWorkspaceMatchReasons` (called only by the 8
      doomed commands, per 191-CONTEXT.md's read) are deleted ONLY if, after removing the 8 wrappers, a
      fresh grep confirms zero remaining callers anywhere (including from within `matchSkillsForWorkflow`
      itself, which must be checked, not assumed, since it might call one of these internally as part of
      its own implementation).
    - `cmd/testdata/orphan_allowlist.json` no longer lists any of the 8 deleted commands (a deleted
      command is not an "allowed orphan" -- it does not exist to be measured).
    - `cmd/subcommand_reachability_ratchet_test.go`'s reachability scan runs cleanly with the 8 commands
      gone -- `skillLifecycleOrphanCandidates`/`resolveSkillLifecyclePaths`/the `len(phase178) != 6`
      assertion no longer reference leaf names that cannot resolve.
  </behavior>

  <action>
First, run the EXISTING `cmd/subcommand_reachability_ratchet_test.go` suite as-is (before any edit) to
see today's real, current orphan/allowlist state with certainty — do not rely on the planning-time
snapshot in 191-CONTEXT.md, which may have drifted. Record this baseline run's output in the SUMMARY.

For each of the 8 target commands' `RunE` closures, list every non-trivial function it calls
(`buildFullIndex`, `loadSkillIndexOrBuild`, `skillWorkspaceMatchReasons`, `matchSkillsForWorkflow`,
`resolveSkillMatchInput`, `renderSkillInjectResult`, and whatever `skill-parse-frontmatter`'s closure
calls). For each, grep for callers OUTSIDE `cmd/skills.go` and outside `cmd/skills_test.go`. Record which
functions have a confirmed external caller (keep unconditionally) and which do not (candidates for
deletion, pending the second grep pass below).

Delete the 8 `cobra.Command` variable declarations (`skillIndexCmd`, `skillDetectCmd`, `skillMatchCmd`,
`skillInjectCmd`, `skillListCmd`, `skillCacheRebuildCmd`, `skillDiffCmd`, and the
`skill-parse-frontmatter` command) and their `rootCmd.AddCommand(...)` registrations. Do NOT delete
`matchSkillsForWorkflow`, `resolveSkillMatchInput`, or `renderSkillInjectResult` regardless of what the
first grep pass showed — these are confirmed live via `cmd/codex_build.go:3268` and must survive this
task unconditionally.

Re-run `go build ./...` — the compiler will immediately flag any now-orphaned helper that was ONLY
called by one of the 8 deleted closures (an "declared and not used" or similar error will not occur for
package-level functions, so instead re-run the grep from the first pass on the NOW-EDITED file: any
helper with zero remaining callers anywhere is genuinely, transitively dead — delete it. Any helper still
called (directly or transitively) by `matchSkillsForWorkflow` or another surviving function stays,
completely unmodified.

Remove any test in `cmd/skills_test.go` that exercised one of the 8 commands via its CLI path
specifically (e.g. `rootCmd.SetArgs([]string{"skill-index", ...})` or similar). Keep every test that
exercises `matchSkillsForWorkflow` or its helpers as direct Go function calls.

Update `cmd/testdata/orphan_allowlist.json`: remove the entries whose `name` matches
`aether skill-index`, `aether skill-detect`, `aether skill-match`, `aether skill-inject`,
`aether skill-list`, `aether skill-diff`, `aether skill-parse-frontmatter`, `aether skill-cache-rebuild`.
Leave every other entry (including the 8 CRUD-authoring commands' entries, per 191-CONTEXT.md D-14)
untouched.

Rewrite `cmd/subcommand_reachability_ratchet_test.go`'s D-08 block: `skillLifecycleOrphanCandidates` and
`resolveSkillLifecyclePaths` must not reference leaf names that no longer resolve. Replace the
`len(phase178) != 6` assertion and its surrounding comment with a record of the SKILL-01 ruling: Phase
191 delivered SKILL-01 by deleting the reviewed 8-command set entirely (not by wiring them), so there is
no longer a "reaches zero" count to measure — the set reached zero by not existing. Preserve the
T-172-07 substring-boundary proof test (lines ~1152-1170) unmodified; it protects a general scanner
property (`skill-list` vs `skill-list-lifecycle`) that remains relevant regardless of this deletion.

Re-run the full `cmd/subcommand_reachability_ratchet_test.go` suite; it must pass cleanly with the 8
commands gone and the allowlist/ratchet internally consistent.
  </action>

  <verify>
    <automated>go build ./... && go vet ./... && go test ./cmd/ -run 'TestSubcommandReachability|TestOrphan|TestSkill' -v -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `rootCmd.Find([]string{"skill-index"})` (and the same for the other 7) no longer resolves to a
      real, non-root command -- proven in a test, not just asserted in prose.
    - `matchSkillsForWorkflow`, `resolveSkillMatchInput`, and `renderSkillInjectResult` are unchanged
      (byte-identical function bodies, confirmed by diffing against the pre-task version) OR, if
      genuinely refactored for compile correctness, behavior-identical, proven by the existing
      build-brief test suite passing unmodified.
    - Whatever test currently covers `composeBuildManifestBrief`'s skill-section assembly
      (`cmd/codex_build_test.go`, per 190-CONTEXT.md's citation of this function) passes unmodified.
    - `cmd/testdata/orphan_allowlist.json` contains zero entries for the 8 deleted commands and is
      otherwise unchanged (the 8 CRUD-authoring entries and everything else stay).
    - `cmd/subcommand_reachability_ratchet_test.go`'s full suite passes, including the T-172-07 boundary
      proof (unmodified) and the rewritten D-08 block.
    - `go build ./... && go vet ./...` exit 0.
    - `go test ./cmd/... -count=1` exits 0 (full package, zero regressions).
  </acceptance_criteria>

  <done>The 8 unreferenced skill-lifecycle CLI commands are gone; the live in-process skill-injection logic they shared with the build path is completely unaffected; the orphan allowlist and reachability ratchet are internally consistent with the new state. SKILL-01 is delivered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `matchSkillsForWorkflow` -> every worker's assembled brief | The single highest-risk edit in this entire phase: this function computes the skill section injected into every builder/watcher prompt. An accidental deletion or behavior change here silently degrades every future build's context quality with no obvious symptom. |
| `cmd/subcommand_reachability_ratchet_test.go` -> CI release gate | This ratchet is itself a security-relevant control (it is what caught the original 278-orphan finding in Phase 172). Editing it carelessly could weaken it for commands far beyond this task's scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-191-05-01 | Tampering (silent feature regression) | `matchSkillsForWorkflow` and its live caller at `cmd/codex_build.go:3268` | mitigate | 191-PATTERNS.md's 4-step CLI-surface-vs-logic discipline is mandatory before any deletion in this file; the existing build-brief test suite is required to pass UNMODIFIED, not adapted, as direct proof the live path is untouched |
| T-191-05-02 | Tampering (weakened reachability ratchet) | `cmd/subcommand_reachability_ratchet_test.go`'s D-08 rewrite | mitigate | The T-172-07 substring-boundary proof and the general orphan-allowlist mechanism are explicitly required to survive unmodified; only the SKILL-01-specific count assertion is rewritten, and only to record a ruling, not to loosen the scan |
| T-191-05-03 | Denial of Service (broken compile) | `pool.go`'s import list after `cost.go`'s deletion | mitigate | `go build ./...` is required after every edit in this plan; an unused import is a compile error, not a silent runtime issue |
| T-191-05-04 | Repudiation (undocumented dead-vs-live determination) | The grep-pass results for `buildFullIndex`/`loadSkillIndexOrBuild`/`skillWorkspaceMatchReasons` | mitigate | The task requires recording, in the SUMMARY, exactly which helper functions were confirmed dead (and by what evidence) vs. which were kept live |

</threat_model>

<verification>
```bash
go build ./...
go vet ./...
go test ./pkg/agent/... ./pkg/trace/... -count=1 -v
go test ./cmd/ -run 'TestSubcommandReachability|TestOrphan|TestSkill' -v -count=1
go test ./cmd/... -count=1
```
</verification>

<success_criteria>
- `pkg/trace/cost.go` is deleted; `pool.go` compiles with no reference to it.
- `newLearningValidator` no longer exists anywhere.
- The 8 SKILL-01 CLI commands no longer exist as invocable subcommands.
- `matchSkillsForWorkflow` and its live in-process caller in `cmd/codex_build.go` are completely
  unaffected, proven by the existing, unmodified build-brief test suite passing.
- `cmd/testdata/orphan_allowlist.json` and `cmd/subcommand_reachability_ratchet_test.go` remain
  internally consistent with the new state, and the T-172-07 boundary proof is unmodified.
- `session-verify-fresh` is untouched (191-CONTEXT.md D-07).
- Full `go test ./cmd/... ./pkg/agent/... ./pkg/trace/... -count=1` suites pass with zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/191-dead-wood/191-05-SUMMARY.md`, including: the baseline
reachability-scan output, the per-helper-function dead-vs-live determination table, and confirmation the
build-brief skill-injection test suite passed unmodified.
Note: `.planning/` is gitignored but tracked -- new files there need `git add -f` or they silently
vanish from the commit.
</output>
