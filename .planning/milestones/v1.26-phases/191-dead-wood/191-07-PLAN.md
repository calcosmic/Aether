---
phase: 191-dead-wood
plan: 07
type: execute
wave: 2
depends_on: ["191-01", "191-02", "191-03", "191-04", "191-05", "191-06"]
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "colony/policies/oracle-phase-directives.yaml exists, byte-identical to its state before this phase started"
    - "No auxiliary command's wrapper markdown or agent definition (/ant-oracle, /ant-dream, chaos, archaeology, swarm, council -- any platform) was modified by any plan in this phase"
    - "/ant-oracle smoke-passes via a real, executed command -- not asserted from reading code"
    - "/ant-dream smoke-passes via a real, executed command -- not asserted from reading code"
    - "The full go test ./... -race suite passes with zero regressions after all six Wave-1 plans have landed together"
    - "Every item this phase found zero-reader but deliberately did not delete (191-CONTEXT.md D-02, D-14) is confirmed still present and unmodified"
  artifacts: []
  key_links:
    - from: "cmd/oracle_loop.go (the three-path oracle-phase-directives.yaml reader)"
      to: "colony/policies/oracle-phase-directives.yaml"
      via: "unchanged CWD/root/hub three-path search"
      pattern: "oracle-phase-directives.yaml"
---

<objective>
Verify, by execution rather than by re-reading diffs, that Phase 191's five wiring criteria all hold
SIMULTANEOUSLY once every Wave-1 plan (191-01 through 191-06) has landed together — something no
individual plan can prove alone, since each was scoped to its own deletion target. This is the phase's
own closing instance of "verify by execution, not grep" (CLAUDE.md, and this phase's own prime rule).

Confirms, in order: (1) criterion 5's explicit protection held — `oracle-phase-directives.yaml` is
byte-unchanged and no auxiliary command was touched; (2) `/ant-oracle` and `/ant-dream` still work,
proven by running them, not by inspecting their source; (3) the full test suite is green with all six
Wave-1 plans' changes present together (each plan proved its own tests green in isolation; this plan
proves there is no cross-plan interaction none of them could see alone); (4) the items this phase found
zero-reader but deliberately did NOT delete (191-CONTEXT.md D-02: 4 more zero-reader policy files, 27
more zero-reader prompt files, `colony/playbooks/*.md`'s ambiguous status; D-14: the 8 CRUD-authoring
skill-lifecycle commands) are still present, confirming this phase's conservative scope boundary held in
practice, not just on paper.

Purpose: ROADMAP criterion 5, plus the phase-level closing verification CLAUDE.md's Definition of Done
requires before any phase can be considered complete.

Output: A verification record (this plan's SUMMARY) proving all five criteria hold together, with every
check backed by an executed command, not a read.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/191-dead-wood/191-CONTEXT.md
@.planning/phases/191-dead-wood/191-PATTERNS.md
@CLAUDE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the protected surface is untouched and the full suite is green together</name>

  <files>(verification only -- no files modified)</files>

  <read_first>
    - `.planning/phases/191-dead-wood/191-01-SUMMARY.md` through `191-06-SUMMARY.md` -- what each plan
      actually did, to know the expected footprint of this phase's total diff
    - 191-CONTEXT.md §"Criterion 5" -- exactly what must remain untouched
  </read_first>

  <action>
Run `git diff --stat main...HEAD -- colony/policies/oracle-phase-directives.yaml` (or the equivalent
comparison against this phase's starting commit) and confirm it shows NO changes to that file. If any
change is shown, this is a blocking finding — stop and report it rather than proceeding.

Run `git diff --stat` (against this phase's starting commit) restricted to auxiliary command surfaces:
`.claude/commands/ant/oracle.md`, `.claude/commands/ant/dream.md`, `.claude/commands/ant/chaos.md`,
`.claude/commands/ant/archaeology.md`, `.claude/commands/ant/swarm.md`, `.claude/commands/ant/council.md`
(and their OpenCode/flat-mirror equivalents, and `.codex/agents/` equivalents where applicable). Confirm
zero changes to any of them. `cmd/oracle_loop.go` must also show zero changes (191-05 and 191-06 do not
touch it; confirm this held in practice).

Run the full verification command sequence CLAUDE.md's own "Verification Commands" section specifies:
`go build ./cmd/aether`, `go vet ./...`, `go test ./... -race`, `aether version` (against the freshly
built binary). All must succeed with the combined state of all six Wave-1 plans present together — this
is the first point in the phase where all six diffs coexist and get tested as a whole, since each
Wave-1 plan only proved its own tests green with only its own changes present.

Confirm the D-02/D-14 conservative-scope items are still present and unmodified:
`colony/policies/pheromone-lifecycle.yaml`, `colony/policies/safety-gates.yaml`,
`colony/policies/signal-rules.yaml`, `colony/policies/skill-creation.yaml`; all 27
`colony/prompts/*.md` files other than `colony-prime.md` (deleted by 191-02); all 7
`colony/playbooks/*.md` files; and the 8 CRUD-authoring skill-lifecycle commands
(`skill-create` — expected to still resolve as a real command — plus `skill-patch`, `skill-archive`,
`skill-pin`, `skill-list-lifecycle`, `skill-promote`, `skill-view`, `skill-curator-run`,
`skill-recover`, which should still resolve via `rootCmd.Find` exactly as before this phase, since
191-05 explicitly scoped their deletion out).
  </action>

  <verify>
    <automated>go build ./cmd/aether && go vet ./... && go test ./... -race -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `oracle-phase-directives.yaml` and every auxiliary command file show zero diff against this phase's
      starting commit.
    - `go build ./cmd/aether && go vet ./... && go test ./... -race -count=1` all exit 0 with all six
      Wave-1 plans' changes present together.
    - `aether version` (built binary) reports successfully.
    - Every D-02/D-14 conservative-scope item is confirmed present and unmodified, itemized in the
      SUMMARY.
  </acceptance_criteria>

  <done>The protected surface is proven untouched, the full test suite is green with all six Wave-1 plans combined, and every deliberately-out-of-scope item is confirmed still present.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-pass /ant-oracle and /ant-dream by execution</name>

  <files>(verification only -- no files modified, unless a genuinely missing dream smoke check must be added per the action below)</files>

  <read_first>
    - STATE.md's WORKFLOW-03 evidence citation -- `aether oracle selftest` "runs one real research round
      and exits non-zero if the dispatcher, agent definition, artifacts or progress log are broken" — the
      existing, established smoke mechanism for oracle
    - `cmd/` -- grep for a `dream`-equivalent selftest command (`aether dream selftest` or similar); if
      none exists, identify the most direct existing test that exercises the real `/ant-dream` dispatch
      path end to end (not just a unit test of an isolated helper function)
  </read_first>

  <action>
Run `aether oracle selftest` (or the equivalent current invocation, if the exact flag has changed since
STATE.md's citation) and confirm it exits 0, proving `/ant-oracle` still works after this phase's
changes.

For `/ant-dream`: if an equivalent selftest command already exists, run it and confirm success. If none
exists, run the most direct existing end-to-end test covering the real dream dispatch path (search
`cmd/*dream*_test.go` for one); if genuinely no such test exists at all, this is itself a finding worth
recording (a pre-existing gap, not something this phase caused) — in that case, perform a minimal manual
smoke run instead (invoke the dream command's underlying entry point directly with trivial input,
confirm no error and a sane structural response) and record that this was the closest available proof,
distinct from an automated regression test.

Record in the SUMMARY: the exact command(s) run for each, their exit codes, and — for dream specifically
— whether an existing automated selftest was found or a manual smoke run was substituted.
  </action>

  <verify>
    <automated>aether oracle selftest</automated>
  </verify>

  <acceptance_criteria>
    - `aether oracle selftest` (or its current equivalent) exits 0.
    - A dream smoke-pass is recorded with its exact command and exit code, or — if no automated selftest
      exists — a manual smoke run's outcome, with the gap noted honestly rather than silently skipped.
  </acceptance_criteria>

  <done>/ant-oracle and /ant-dream are both proven working after this phase's changes, by execution, with the exact evidence recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| This plan's own verification claims -> the phase's closing record | This is the phase's last line of defense against "passed for the wrong reason" — CLAUDE.md's own history names this as the repeated failure mode across 18 of 25 prior milestones. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-191-07-01 | Repudiation (unproven smoke claim) | `/ant-oracle`, `/ant-dream` verification | mitigate | Both required to be proven by an EXECUTED command with a recorded exit code, not by reading source and asserting it should still work |
| T-191-07-02 | Tampering (undetected cross-plan interaction) | The combined state of all six Wave-1 plans | mitigate | Full `go test ./... -race` run only after all six plans' changes coexist -- each Wave-1 plan's own green run only proved its own changes in isolation |

</threat_model>

<verification>
```bash
go build ./cmd/aether
go vet ./...
go test ./... -race -count=1
aether version
aether oracle selftest
```
</verification>

<success_criteria>
- `colony/policies/oracle-phase-directives.yaml` and every auxiliary command surface are byte-unchanged.
- The full `go test ./... -race` suite passes with all six Wave-1 plans' changes present together.
- `/ant-oracle` and `/ant-dream` both smoke-pass by real execution.
- Every item this phase deliberately left out of scope (D-02, D-14) is confirmed still present.
</success_criteria>

<output>
After completion, create `.planning/phases/191-dead-wood/191-07-SUMMARY.md`, consolidating: the full
verification command transcript, the oracle/dream smoke-pass evidence, and the itemized confirmation of
every D-02/D-14 out-of-scope item's continued presence. This SUMMARY is the phase's closing record —
write it as the definitive account of what Phase 191 did and did not touch.
Note: `.planning/` is gitignored but tracked -- new files there need `git add -f` or they silently
vanish from the commit.
</output>
