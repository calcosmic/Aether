---
phase: 191-dead-wood
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/workers.md
  - CLAUDE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "workers.md:825 states the true, currently-verified mechanism by which build work produces captured observations -- traced to a real call site, not restated unverified"
    - "CLAUDE.md's Trim order list matches cmd/context.go's real trimOrder slice exactly, including the two separately-prioritized QUEEN wisdom tiers"
    - "CLAUDE.md's Command Playbooks section distinguishes the interactive wrapper's real flow (aether build --plan-only + wrapper-driven spawn + build-finalize) from the separate autopilot/host-driven flow (aether host build -> dispatch_manifest -> build-finalize), and no longer implies build.md/continue.md describe the host-manifest flow directly"
  artifacts:
    - path: ".aether/workers.md"
      provides: "Corrected Wisdom Pipeline description at line ~825"
    - path: "CLAUDE.md"
      provides: "Corrected Trim order list and corrected Command Playbooks host-build description"
  key_links:
    - from: ".aether/workers.md (Wisdom Pipeline section)"
      to: "pkg/learn (the real automatic capture mechanism) OR cmd/learning.go's memory-capture command (if genuinely worker-invoked)"
      via: "whichever the trace confirms -- documented explicitly in the corrected sentence"
      pattern: "N/A -- prose correction, verified by trace not by pattern match"
---

<objective>
Correct three false or misleading documentation claims named in ROADMAP Phase 191 criterion 4, each
verified against live source before correction — not restated from a prior phase's summary or from
assumption.

1. `.aether/workers.md:825` claims build work's observations are captured "via `memory-capture` in
   continue step." Planning confirmed `memory-capture` is a real, separately-registered command
   (`cmd/learning.go:213`) but is mentioned in NEITHER `.claude/commands/ant/build.md` NOR
   `.claude/commands/ant/continue.md` — no wrapper prose currently instructs calling it. Phase 148's
   WORKFLOW-01 evidence confirms learnings genuinely ARE extracted during continue, but via `pkg/learn`'s
   own automatic pipeline (STATE.md: "a second, different learning system... already runs live on every
   continue"), which planning did not fully trace to its exact trigger point. This task must complete
   that trace and state the real mechanism.

2. CLAUDE.md's "Trim order" list (`CLAUDE.md:628-636`) shows 9 items with a single "QUEEN.md wisdom"
   entry. The real implementation (`cmd/context.go:1027-1038`) has two SEPARATELY-prioritized QUEEN
   wisdom tiers (global, then local) — CLAUDE.md's list understates the real granularity by one rank.

3. CLAUDE.md's "Command Playbooks (Reference Material)" section (`CLAUDE.md:445-455`) claims execution
   behavior "lives in the host-manifest flow (`aether host build` -> spawn from `dispatch_manifest` ->
   `build-finalize`)" and that `build.md`/`continue.md` "describe the host-manifest flow directly."
   `.claude/commands/ant/build.md:348` EXPLICITLY FORBIDS `aether host build` from the interactive
   wrapper ("the TS host hop is off the interactive build path"), documenting instead
   `aether build --plan-only` (manifest) -> wrapper-driven spawn -> `aether build-finalize`. CLAUDE.md's
   text conflates the autopilot-only mechanism with what the primary, interactive wrapper actually does.

Fully self-contained: touches only `.aether/workers.md` and `CLAUDE.md`, shared with no other plan in
this phase.

Purpose: ROADMAP criterion 4 in full.

Output: 3 corrected documentation claims, each traced to and matching live source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/191-dead-wood/191-CONTEXT.md
@.planning/phases/191-dead-wood/191-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace and correct workers.md:825's memory-capture claim</name>

  <files>.aether/workers.md</files>

  <read_first>
    - `.aether/workers.md:795-833` -- the full "Wisdom Pipeline" section, including line 825's current
      claim
    - `pkg/learn` (full package) -- find the automatic learning-capture trigger point; confirm whether it
      is invoked from continue's Go implementation directly (in-process function call), shells out to a
      command, or is triggered some other way
    - `cmd/codex_continue.go`, `cmd/codex_continue_plan.go` -- grep for any `pkg/learn` import/call site
      to find WHERE in the continue flow the automatic capture actually happens
    - `cmd/learning.go:213` (`memory-capture`'s own `RunE`) -- confirm what this command does when
      invoked directly (worker-callable, or genuinely only ever meant to be called by continue's
      internals); grep the WHOLE repo (not just build.md/continue.md) for any caller of `memory-capture`
      that planning may have missed, including `.claude/agents/ant/*.md` (individual agent prompt files,
      distinct from the lifecycle wrapper commands already checked)
    - CLAUDE.md's own "Wisdom Pipeline" section (the pipeline-stage table: "1. Observe |
      `memory-capture "learning"` | Records observation...") -- if this table is ALSO inaccurate given
      what this task's trace finds, note it in the SUMMARY as a related finding, but do not edit
      CLAUDE.md's Wisdom Pipeline table in this task unless it shares the exact false claim being
      corrected here (stay scoped to what ROADMAP criterion 4 names: workers.md:825 specifically)
  </read_first>

  <action>
Trace the real mechanism precisely: does `pkg/learn`'s automatic pipeline run as an in-process Go call
during continue (no `memory-capture` CLI invocation involved at all), or does `memory-capture` genuinely
get invoked by some path this task's grep finds (e.g. an individual agent prompt file instructing a
BUILDER to call it directly during its own task, making "produces observations... in continue step" the
inaccurate part rather than "via memory-capture")?

Once the real mechanism is confirmed with a specific file:line citation, rewrite
`.aether/workers.md:825`'s bullet to state it accurately. The corrected sentence must be traceable to the
citation found — do not soften into vague prose ("observations are captured automatically") if a precise
mechanism was found; state the precise mechanism.

If the trace finds the CLAUDE.md Wisdom Pipeline table shares the identical false claim, note this in the
SUMMARY as a discovered-but-out-of-scope-for-this-task finding (matching this phase's own established
conservative-logging discipline) rather than silently expanding this task's edit to a second file's
unrelated section.
  </action>

  <verify>
    <automated>grep -n "memory-capture" .claude/commands/ant/build.md .claude/commands/ant/continue.md .aether/workers.md</automated>
  </verify>

  <acceptance_criteria>
    - The corrected sentence at `.aether/workers.md:825` is traceable to a specific, cited file:line in
      the SUMMARY -- not restated from the prior, unverified claim.
    - If the trace confirms `pkg/learn` is the real automatic mechanism (not a `memory-capture` CLI
      call), the corrected sentence says so and no longer implies a wrapper-visible `memory-capture`
      invocation happens "in continue step" unless the trace found one.
    - `grep -n "memory-capture" .aether/workers.md` still finds the term used accurately (either
      correctly describing a real call site, or removed from this specific claim if the real mechanism
      does not involve it at all).
  </acceptance_criteria>

  <done>workers.md:825 states the real, traced mechanism by which build-work observations get captured, matching a cited live call site.</done>
</task>

<task type="auto">
  <name>Task 2: Correct CLAUDE.md's Trim order list and Command Playbooks host-build claim</name>

  <files>CLAUDE.md</files>

  <read_first>
    - `CLAUDE.md:620-640` -- the full "Token Budget" section including the current 9-item Trim order list
    - `cmd/context.go:1015-1055` -- the real `trimOrder` implementation and surrounding budget-enforcement
      logic, confirming the exact 9 header strings in order and the "BLOCKERS is NEVER trimmed" comment
    - `CLAUDE.md:440-460` -- the full "Command Playbooks (Reference Material)" section including the
      "Authority note" bullets
    - `.claude/commands/ant/build.md` lines 21, 41, 105-108, 253, 259, 272, 295, 304, 348-355 -- the real
      interactive-wrapper flow (plan-only manifest, wrapper-driven spawn, build-finalize) and the explicit
      prohibition on `aether host build`
    - `.claude/commands/ant/continue.md` (relevant sections) and `.opencode/commands/ant/continue.md:263`
      -- confirm continue's actual relationship to `aether host continue --dry-run` (per 190-CONTEXT.md's
      own citation), so the corrected CLAUDE.md text describes continue accurately too, not just build
  </read_first>

  <behavior>
    - CLAUDE.md's Trim order list has 9 distinct trim-priority entries (not 8 items collapsed to a false
      9th) plus "Blockers (NEVER trimmed)" as a 10th line, with the two QUEEN wisdom tiers listed
      separately, in the correct relative order, matching `cmd/context.go:1027-1038` exactly (including
      the exact header text where practical, e.g. "QUEEN wisdom (global)" / "QUEEN wisdom (local)").
    - CLAUDE.md's Command Playbooks section states two distinct flows: the interactive wrapper's real
      path and the separate autopilot/host-driven path, and its "Authority note" bullet about
      `build.md`/`continue.md` "describing the host-manifest flow directly" is corrected to what those
      files actually describe.
    - Neither correction changes any OTHER claim in these two CLAUDE.md sections that this task's
      read_first did not confirm is false.
  </behavior>

  <action>
Rewrite `CLAUDE.md`'s "Trim order" list (in the "Token Budget" section) to match
`cmd/context.go:1027-1038` exactly: Rolling summary, Phase learnings, Key decisions, Hive wisdom, Context
capsule, User preferences, QUEEN wisdom (global), QUEEN wisdom (local), Pheromone signals / active
signals, then Blockers (NEVER trimmed) as the closing, unnumbered note — 9 trim-priority tiers plus the
never-trimmed exception, not 8. Keep the existing "(first trimmed = lowest retention priority)" /
"(trimmed last -- highest retention priority)" framing, adjusted only to the corrected item count and
order.

Rewrite `CLAUDE.md`'s "Command Playboods (Reference Material)" section's claim that execution behavior
"lives in the host-manifest flow (`aether host build` -> spawn from `dispatch_manifest` ->
`build-finalize`)" to name BOTH flows distinctly: the interactive wrapper's real path (`aether build
$ARGUMENTS --plan-only` for the manifest, wrapper-driven worker spawn, `aether build-finalize` for
completion) as what the primary, documented wrapper actually does; and the separate autopilot/host-driven
path (`aether host build` -> `dispatch_manifest` -> `build-finalize`, reached only via `aether run` or
another host-driven invocation, never the interactive wrapper) as the mechanism the OLD sentence was
actually describing. Correct the "Authority note" bullet claiming `build.md`/`continue.md` "describe the
host-manifest flow directly" to state what `read_first` confirms they actually describe — if continue's
relationship to `aether host continue --dry-run` is materially different from build's relationship to
`aether host build` (per 190-CONTEXT.md's citation), state that distinction accurately rather than
treating both files identically.
  </action>

  <verify>
    <automated>grep -n "QUEEN wisdom (global)\|QUEEN wisdom (local)" CLAUDE.md && grep -c "host build\|host-build" CLAUDE.md</automated>
  </verify>

  <acceptance_criteria>
    - CLAUDE.md's Trim order list has exactly 9 trim-priority lines plus the blockers-never-trimmed note,
      with both QUEEN wisdom tiers present and separately listed, matching `cmd/context.go`'s order
      exactly.
    - CLAUDE.md's Command Playbooks section no longer implies the interactive wrapper (`build.md`) uses
      `aether host build` -- it names the plan-only/wrapper-driven flow as what `build.md` actually
      documents, and names the host-driven flow as the separate autopilot mechanism.
    - No other claim in either CLAUDE.md section was altered beyond what `read_first` confirmed false.
  </acceptance_criteria>

  <done>CLAUDE.md's Trim order list matches the real code exactly, and its Command Playbooks section accurately distinguishes the interactive wrapper's real flow from the separate autopilot/host-driven flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CLAUDE.md / workers.md text -> a future agent's or maintainer's mental model of the system | These are the two most-read orientation documents in the repo (CLAUDE.md is loaded into every session per this very planning session's own system prompt). A false claim here propagates into every future decision made against it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-191-06-01 | Repudiation (unverified doc claim) | `.aether/workers.md:825`, `CLAUDE.md`'s Trim order and Command Playbooks sections | mitigate | Every correction in this plan is required to cite a specific, read file:line as its source -- no correction is written from inference or from restating the ROADMAP's own summary text |
| T-191-06-02 | Tampering (scope creep into unrelated doc claims) | CLAUDE.md, workers.md | mitigate | Task actions explicitly scope edits to the three named claims; a related-but-different false claim discovered during the trace (e.g. CLAUDE.md's Wisdom Pipeline table) is logged in the SUMMARY, not silently also edited |

</threat_model>

<verification>
```bash
grep -n "memory-capture" .claude/commands/ant/build.md .claude/commands/ant/continue.md .aether/workers.md
grep -n "QUEEN wisdom (global)\|QUEEN wisdom (local)" CLAUDE.md
grep -c "host build\|host-build" CLAUDE.md
```
</verification>

<success_criteria>
- `.aether/workers.md:825` states the real, traced observation-capture mechanism, cited to a specific
  file:line.
- CLAUDE.md's Trim order list matches `cmd/context.go`'s real trim order exactly, including both QUEEN
  wisdom tiers.
- CLAUDE.md's Command Playbooks section accurately distinguishes the interactive wrapper's real flow from
  the separate autopilot/host-driven flow.
</success_criteria>

<output>
After completion, create `.planning/phases/191-dead-wood/191-06-SUMMARY.md`, citing the exact source
file:line each correction was traced to.
Note: `.planning/` is gitignored but tracked -- new files there need `git add -f` or they silently
vanish from the commit.
</output>
