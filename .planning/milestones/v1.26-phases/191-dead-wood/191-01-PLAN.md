---
phase: 191-dead-wood
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - colony/agents/*.yaml
  - colony/phases/*.yaml
  - colony/policies/model-routing.yaml
  - colony/policies/autopilot.yaml
  - colony/policies/memory-rules.yaml
  - cmd/colony_zero_reader_config_ratchet_test.go
autonomous: true
requirements: [ROSTER-01, ROSTER-02]

must_haves:
  truths:
    - "colony/agents/*.yaml (27 files), colony/phases/*.yaml (9 files), colony/policies/model-routing.yaml, colony/policies/autopilot.yaml, and colony/policies/memory-rules.yaml no longer exist anywhere in the repository"
    - "Reintroducing any one of the 39 deleted files causes `go test ./cmd/...` to fail with a message naming that specific file path and the ROSTER-01/ROSTER-02 ruling it violates -- proven by a fail-then-pass demonstration, not assumed"
    - "Zero-readership for all 39 files was reconfirmed across Go source (including string-built paths), the TS host, wrapper markdown on all platforms, skills, and the publish manifest immediately before deletion -- not carried forward unverified from planning"
  artifacts:
    - path: "cmd/colony_zero_reader_config_ratchet_test.go"
      provides: "Existence-check ratchet proving all 39 deleted paths stay deleted"
      exports: ["TestZeroReaderColonyConfigsDoNotReappear"]
  key_links:
    - from: "cmd/colony_zero_reader_config_ratchet_test.go"
      to: "go test ./cmd/... (CI / release gate)"
      via: "standard Go test discovery -- no special invocation needed"
      pattern: "func TestZeroReaderColonyConfigsDoNotReappear"
---

<objective>
Delete the five confirmed-zero-reader `colony/` config groups named in ROADMAP Phase 191 criterion 1
(`colony/agents/*.yaml`, `colony/phases/*.yaml`, `colony/policies/model-routing.yaml`,
`colony/policies/autopilot.yaml`, `colony/policies/memory-rules.yaml` — 39 files total) and lock their
absence with a reappearance ratchet.

These 39 files were built in Phase 154 ("Colony Assets") to serve `control-ts/`, the TypeScript control
plane Phase 160 confirmed dead and deleted. `colony/` itself was never cleaned up alongside it. Planning
reconnaissance (`191-CONTEXT.md` §"Criterion 1") confirmed all 39 have zero readers across Go, TS,
wrapper markdown, skills, and the publish manifest — `colony/` is git-tracked but not shipped by
`aether publish`/`aether install` (`cmd/publish_cmd.go`, `cmd/install_cmd.go` contain zero `colony`
references), so this is a normal, self-contained git deletion with no distribution-side cleanup.

Purpose: ROADMAP criterion 1 in full. Delivers ROSTER-01 ("the `colony/agents/*.yaml` zero-reader
contradiction is resolved by an explicit recorded ruling — wired with executable proof, or deleted with
the compiled registry documented as authoritative") and ROSTER-02 ("no file remains that claims to
define agents and defines nothing — including `colony/policies/model-routing.yaml`") in full, as
rulings-by-deletion.

Output: 39 files removed, one new ratchet test proving they stay removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/191-dead-wood/191-CONTEXT.md
@.planning/phases/191-dead-wood/191-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- No new interfaces created by this plan -- this is pure deletion plus a self-contained ratchet
     test. The ratchet test needs no contract from elsewhere in the codebase beyond os/filepath from
     the standard library and this repo's own convention for locating repo root inside cmd/ tests
     (grep an existing cmd/*_ratchet_test.go for how it does this, e.g.
     cmd/colony_state_atomicity_ratchet_test.go or cmd/subcommand_reachability_ratchet_test.go, and
     reuse the same helper rather than inventing a new one). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconfirm zero-readership and delete the 39 files</name>

  <files>colony/agents/*.yaml, colony/phases/*.yaml, colony/policies/model-routing.yaml, colony/policies/autopilot.yaml, colony/policies/memory-rules.yaml</files>

  <read_first>
    - 191-CONTEXT.md §"Criterion 1" and §"Criterion 1 — zero-reader configs" under `<canonical_refs>` --
      the planning-time evidence for zero readership
    - 191-PATTERNS.md §"Multi-Surface Zero-Readership Proof" -- the six-surface checklist to re-run
      before deleting, since time may have passed between planning and execution
    - `cmd/publish_cmd.go`, `cmd/install_cmd.go` -- confirm still zero `colony` references (D-13); if
      either now references `colony`, STOP and flag this as a blocker rather than deleting
  </read_first>

  <action>
Re-run the six-surface zero-readership check from 191-PATTERNS.md against all 39 target files
immediately before deleting anything, to catch any reader introduced since planning:
(1) `grep -rn "colony/agents\|colony/phases\|model-routing\|colony/policies/autopilot\|memory-rules"
--include="*.go" cmd/ pkg/` (exclude test files touching only test fixtures), checking specifically for
any NEW `policyPath("model-routing")`/`policyPath("autopilot")`/`policyPath("memory-rules")` call site
that did not exist during planning; (2) the same sweep against `.aether/ts-host/src/`; (3) against
`.claude/commands/`, `.opencode/commands/`, `.codex/agents/`; (4) against `.aether/skills/`; (5) confirm
`cmd/publish_cmd.go`/`cmd/install_cmd.go` still contain zero `colony` references; (6) confirm no
wrapper/skill/doc instructs executing a command that reads these paths. If ANY of the 39 files now shows
a reader that did not exist during planning, stop, do not delete that specific file, and record the
finding in the SUMMARY instead of proceeding past it.

Once reconfirmed, delete: all 27 files under `colony/agents/*.yaml`, all 9 files under
`colony/phases/*.yaml`, and the three named policy files
(`colony/policies/model-routing.yaml`, `colony/policies/autopilot.yaml`,
`colony/policies/memory-rules.yaml`). Use `git rm` for each so the deletion is staged cleanly. Do NOT
touch any other file under `colony/agents/`, `colony/phases/`, or `colony/policies/` — in particular,
`colony/policies/dispatch-contract.yaml`, `colony/policies/review-depth.yaml`, and
`colony/policies/oracle-phase-directives.yaml` are explicitly out of scope for this task (handled by
other plans or explicitly protected — see 191-CONTEXT.md D-05). `colony/policies/pheromone-lifecycle.yaml`,
`safety-gates.yaml`, `signal-rules.yaml`, and `skill-creation.yaml` are ALSO out of scope this phase
(191-CONTEXT.md D-02) — confirmed zero-reader but not named by the ROADMAP's literal criterion text;
leave them in place.
  </action>

  <verify>
    <automated>git status --porcelain colony/ | grep -c '^D ' </automated>
  </verify>

  <acceptance_criteria>
    - `git status --porcelain colony/ | grep -c '^D '` reports exactly 39.
    - `find colony/agents colony/phases -type f | wc -l` reports 0.
    - `test -f colony/policies/model-routing.yaml`, `test -f colony/policies/autopilot.yaml`,
      `test -f colony/policies/memory-rules.yaml` all fail (files absent).
    - `test -f colony/policies/dispatch-contract.yaml`, `test -f colony/policies/review-depth.yaml`,
      `test -f colony/policies/oracle-phase-directives.yaml`, `test -f colony/policies/pheromone-lifecycle.yaml`,
      `test -f colony/policies/safety-gates.yaml`, `test -f colony/policies/signal-rules.yaml`,
      `test -f colony/policies/skill-creation.yaml` all succeed (files untouched).
    - `go build ./... && go vet ./...` exit 0 (deletion of these files cannot affect Go compilation --
      confirms nothing was silently depending on them via a path this task's grep sweep missed).
  </acceptance_criteria>

  <done>All 39 zero-reader files are deleted; every adjacent, in-scope-for-a-different-decision policy file is confirmed untouched; the build still succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the reappearance ratchet with fail-then-pass proof</name>

  <files>cmd/colony_zero_reader_config_ratchet_test.go</files>

  <read_first>
    - `cmd/colony_state_atomicity_ratchet_test.go` (header doc comment, full) -- the house style for a
      ratchet's doc comment: name the ROADMAP criterion, name the specific failure modes considered and
      rejected, name what "fails loudly, never vacuously" means for this specific check
    - `cmd/subcommand_reachability_ratchet_test.go` lines 1-50 -- how an existing ratchet locates repo
      root from inside a `cmd/` package test (Go test CWD is the package directory, not repo root --
      reuse whatever helper this file already uses, do not invent a second one)
    - 191-PATTERNS.md §"The Ratchet House Style" -- Tier 1 (simple existence check), including the exact
      failure-message shape to follow
  </read_first>

  <behavior>
    - `TestZeroReaderColonyConfigsDoNotReappear` fails, individually and by name, for each of the 39
      paths if that specific path exists on disk -- not a single aggregate pass/fail that hides which
      file came back.
    - The test's failure message names the specific file, the ROSTER-01/ROSTER-02 ruling, and the two
      valid responses (either the file has a genuine new reader and the ratchet plus 191-CONTEXT.md need
      updating with that evidence, or the file should be deleted again) -- matching 191-PATTERNS.md's
      required failure-message shape.
    - The test PASSES cleanly against the current (post-Task-1) repository state.
    - Fail-then-pass is proven, not assumed: temporarily recreate one file from each of the three
      target groups (one `colony/agents/*.yaml`, one `colony/phases/*.yaml`, one
      `colony/policies/*.yaml`), confirm the test fails naming each recreated file, delete them again,
      confirm the test passes. Record exactly which files were used for this proof and the fail/pass
      transcript in the plan SUMMARY.
  </behavior>

  <action>
Create `cmd/colony_zero_reader_config_ratchet_test.go` with a header doc comment following the house
style (see read_first): name ROADMAP Phase 191 criterion 1, name ROSTER-01/ROSTER-02, and state this is
deliberately a Tier-1 existence check per 191-PATTERNS.md, not an AST scanner, because the property being
guarded is pure file presence/absence.

Implement `TestZeroReaderColonyConfigsDoNotReappear` iterating a literal list of all 39 relative paths
(hardcode the 27 agent names, 9 phase names, and 3 policy filenames explicitly in the test -- do not
compute them from a `colony/agents/` directory listing, since an empty/deleted directory makes that
approach vacuous). For each path, `os.Stat` against the repo root (using this file's own repo-root
resolution helper, matching the pattern read_first identified) and `t.Errorf` (not `t.Fatalf` --
continue checking the rest so a single run reports every reappeared file, not just the first) with the
message shape from 191-PATTERNS.md if the file exists.

Add a second, small test or a subtest, `TestZeroReaderColonyConfigsListIsComplete`, asserting the
hardcoded list has exactly 39 entries (27 + 9 + 3) -- so an incomplete list (e.g. someone typos or drops
an entry during a future edit) fails loudly rather than silently checking fewer than 39 paths.

Run the fail-then-pass proof described in `<behavior>` and record the transcript in the SUMMARY.
  </action>

  <verify>
    <automated>go test ./cmd/ -run 'TestZeroReaderColonyConfigsDoNotReappear|TestZeroReaderColonyConfigsListIsComplete' -v -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `TestZeroReaderColonyConfigsDoNotReappear` and `TestZeroReaderColonyConfigsListIsComplete` both
      pass against the current repository state.
    - Fail-then-pass demonstrated and recorded in the SUMMARY for at least one file per target group (3
      files minimum).
    - `go build ./... && go vet ./...` exit 0.
    - `go test ./cmd/... -count=1` exits 0 (full package, zero regressions from adding this test file).
  </acceptance_criteria>

  <done>A permanent, proven ratchet exists that fails loudly and by name if any of the 39 deleted zero-reader colony/ files reappear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Repository state -> future commits | A future edit (human or agent) could reintroduce a deleted file, believing it is needed, without rediscovering that it was zero-reader. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-191-01-01 | Tampering (silent reintroduction) | `colony/agents/*.yaml`, `colony/phases/*.yaml`, 3 named policy files | mitigate | `TestZeroReaderColonyConfigsDoNotReappear` fails loudly, by name, per file, proven fail-then-pass |
| T-191-01-02 | Repudiation (undocumented deletion rationale) | This plan's own change | mitigate | 191-CONTEXT.md records the full zero-readership evidence per file group; the ratchet's own doc comment cites the ROADMAP criterion and requirement IDs |
| T-191-01-03 | Denial of Service (accidental deletion of a still-read file) | Task 1's `git rm` sweep | mitigate | Task 1 requires a fresh six-surface re-check immediately before deletion (not reuse of stale planning-time evidence), plus a full `go build`/`go vet` pass after, which would fail immediately if Go code still referenced a deleted path |

</threat_model>

<verification>
```bash
git status --porcelain colony/ | grep -c '^D '
find colony/agents colony/phases -type f | wc -l
go build ./...
go vet ./...
go test ./cmd/ -run 'TestZeroReaderColonyConfigsDoNotReappear|TestZeroReaderColonyConfigsListIsComplete' -v -count=1
go test ./cmd/... -count=1
```
</verification>

<success_criteria>
- All 39 zero-reader files (colony/agents/*.yaml, colony/phases/*.yaml, and the 3 named policy files)
  are deleted from the repository.
- Zero-readership was reconfirmed fresh, immediately before deletion, across all six surfaces named in
  191-PATTERNS.md.
- A permanent ratchet test proves, by name and per file, that none of the 39 can reappear silently --
  demonstrated fail-then-pass.
- Every adjacent, out-of-scope-this-phase file (dispatch-contract.yaml, review-depth.yaml,
  oracle-phase-directives.yaml, pheromone-lifecycle.yaml, safety-gates.yaml, signal-rules.yaml,
  skill-creation.yaml) is confirmed untouched.
- Full `go test ./cmd/... -count=1` suite passes with zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/191-dead-wood/191-01-SUMMARY.md`.
Note: `.planning/` is gitignored but tracked -- new files there need `git add -f` or they silently
vanish from the commit.
</output>
