---
phase: 191-dead-wood
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - colony/policies/review-depth.yaml
  - cmd/review_depth.go
  - cmd/review_depth_test.go
  - cmd/review_depth_ratchet_test.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "colony/policies/review-depth.yaml no longer exists, yet heavy-keyword, security-risk-keyword, and blast-radius-keyword driven review-depth selection is byte-identical to what it produced before deletion, both in a Go-level regression test and in a real dev-checkout CLI capture"
    - "Every field review-depth.yaml defined that diverged from its Go compiled default has been folded into that default before deletion"
    - "loadReviewDepthPolicy() remains in the codebase as a permanently-fallback function -- not deleted, per 191-CONTEXT.md D-04's explicit discretion"
    - "Reintroducing colony/policies/review-depth.yaml causes a named test to fail, proven fail-then-pass"
  artifacts:
    - path: "cmd/review_depth.go"
      provides: "The folded, now-authoritative heavyKeywordsFallback/securityRiskKeywordsFallback/blastRadiusKeywordsFallback slices and any smart-default-reason fallback strings"
    - path: "cmd/review_depth_ratchet_test.go"
      provides: "Reappearance ratchet for colony/policies/review-depth.yaml"
      exports: ["TestReviewDepthPolicyDoesNotReappear"]
  key_links:
    - from: "cmd/review_depth.go (getHeavyKeywords / getSecurityRiskKeywords / getBlastRadiusKeywords)"
      to: "cmd/review_depth.go (loadReviewDepthPolicy)"
      via: "direct Go function call, nil-check-and-fallback pattern"
      pattern: "loadReviewDepthPolicy\\(\\)"
---

<objective>
Resolve the fourth of the four CWD-relative silent-fallback loaders identified in 191-CONTEXT.md
§"Criterion 2": `loadReviewDepthPolicy()` (`cmd/review_depth.go`), which reads
`colony/policies/review-depth.yaml` and feeds the keyword lists that drive smart review-depth selection
(heavy-keyword, security-risk-keyword, and blast-radius-keyword matching — the mechanism CLAUDE.md's
"Queen-Owned Orchestration" section documents as step 3 of the depth-selection priority order: "Keyword
match in phase name"). Like the other three loaders, this only ever successfully reads the file in a dev
checkout (CWD = repo root) — every installed/published Aether run already hits the compiled fallback
(`colony/` is not published, 191-CONTEXT.md D-13). Diff `review-depth.yaml`'s current values against
`review_depth.go`'s compiled fallback slices, fold any divergence in, delete the file, and prove
byte-identical output before and after.

Fully self-contained: `cmd/review_depth.go` shares no files with any other plan in this phase (verified
in 191-CONTEXT.md's `<domain>`/`<canonical_refs>`), so this plan runs independently in Wave 1.

Purpose: ROADMAP criterion 2 (the last of four loaders).

Output: 1 file deleted, its Go compiled defaults folded to match, byte-identical output proven, one
reappearance ratchet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/191-dead-wood/191-CONTEXT.md
@.planning/phases/191-dead-wood/191-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing contracts the executor must build against -- extracted from the codebase. -->

From cmd/review_depth.go (existing, do not reinvent):
```go
var reviewDepthPathOverride string
func loadReviewDepthPolicy() *reviewDepthPolicy // sync.Once-cached; no reset function confirmed to
// exist today -- read_first must confirm and add one mirroring resetVisualsCache()'s shape if absent

type reviewDepthPolicy struct {
	ReviewDepthVersion   string
	HeavyKeywords        []string
	SecurityRiskKeywords []string
	BlastRadiusKeywords  []string
	SmartDefaultReasons  map[string]string
}

var heavyKeywordsFallback = []string{ /* ... */ }
var securityRiskKeywordsFallback = []string{ /* ... */ }
var blastRadiusKeywordsFallback = []string{ /* ... */ }

func getHeavyKeywords() []string
func getSecurityRiskKeywords() []string
// a getBlastRadiusKeywords()-equivalent and any SmartDefaultReasons accessor likely exist alongside
// these two -- read the full file to find every accessor, do not assume only these two exist
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Baseline-capture, diff-and-fold, delete review-depth.yaml</name>

  <files>colony/policies/review-depth.yaml, cmd/review_depth.go, cmd/review_depth_test.go</files>

  <read_first>
    - `colony/policies/review-depth.yaml` (full file) -- every field (`review_depth_version`,
      `heavy_keywords`, `security_risk_keywords`, `blast_radius_keywords`, `smart_default_reasons`)
    - `cmd/review_depth.go` (full file, not just lines 1-90) -- every fallback slice/map and every
      accessor function that reads `loadReviewDepthPolicy()`'s result, including any beyond
      `getHeavyKeywords`/`getSecurityRiskKeywords` (a `getBlastRadiusKeywords`-equivalent and a
      `SmartDefaultReasons` accessor almost certainly exist — confirm by reading the whole file)
    - `cmd/review_depth_test.go` (full file) -- existing test patterns and fixtures to extend
    - 191-PATTERNS.md §"Baseline Capture Before Any Change" -- the two-layer requirement
    - A concrete CLI surface where review-depth keyword matching is observable (e.g. a build or plan
      command against a phase whose name/description contains a heavy keyword, showing the smart-default
      depth selection and its stated reason -- CLAUDE.md's "Queen-Owned Orchestration" section documents
      this behavior; locate the actual render/log site via grep for `getHeavyKeywords(` call sites)
  </read_first>

  <behavior>
    - Every keyword list (heavy, security-risk, blast-radius) and every smart-default-reason string
      resolves identically whether `colony/policies/review-depth.yaml` is present with its current
      content or absent (using the folded compiled default).
    - A before/after regression test in `cmd/review_depth_test.go`, using `reviewDepthPathOverride`
      (and a reset function added if one does not already exist), proves this for every field in
      `reviewDepthPolicy`, not just the two keyword lists already read during planning.
    - A real dev-checkout CLI capture (review-depth.yaml present vs. deleted) is byte-identical.
  </behavior>

  <action>
From this repo's root, build the binary and run the CLI surface `read_first` identifies as exercising
review-depth keyword matching; save the result verbatim (e.g. `/tmp/aether-191-baseline-review-depth.txt`).

Read `colony/policies/review-depth.yaml` field by field against `cmd/review_depth.go`'s compiled
fallback slices/maps, INCLUDING `smart_default_reasons` (not confirmed read during planning — read the
full file to find its accessor). Produce a complete field-by-field diff table — record it in the
SUMMARY.

Fold every divergent value into `cmd/review_depth.go`'s compiled fallbacks. If no reset function exists
for `loadedReviewDepthPolicyOnce`, add one (`resetReviewDepthPolicyCache()` or equivalent) mirroring
`resetVisualsCache()`'s shape. Write (or extend) a before/after regression test in
`cmd/review_depth_test.go`: render via `reviewDepthPathOverride` pointed at a temp copy of
review-depth.yaml's ORIGINAL content, then via the override pointed at a nonexistent path (reset between
renders), assert every accessor (heavy keywords, security-risk keywords, blast-radius keywords, and
smart-default reasons) matches across both renders, for every entry. Run it before deleting the file
(proves the fold is complete).

Delete `colony/policies/review-depth.yaml` (`git rm`). Re-run the regression test (must still pass).
Re-run the real dev-checkout CLI command and diff byte-for-byte against the baseline; record the diff
(or its clean absence) in the SUMMARY.
  </action>

  <verify>
    <automated>go test ./cmd/ -run 'TestReviewDepth|TestGetHeavyKeywords|TestGetSecurityRiskKeywords' -v -count=1</automated>
  </verify>

  <acceptance_criteria>
    - The before/after regression test covers every field in `reviewDepthPolicy` (including
      `smart_default_reasons`, not just the two keyword lists) and passes both before and after deletion.
    - The real dev-checkout CLI capture, re-run after deletion, is byte-for-byte identical to the
      baseline -- diff recorded in the SUMMARY.
    - `colony/policies/review-depth.yaml` no longer exists.
    - `TestBuildWorkerCapHonoursVerificationDepth` and any other pre-existing depth-selection test named
      in CLAUDE.md's "Queen-Owned Orchestration" section still pass unmodified -- confirms this fold did
      not disturb the locked depth-selection numbers.
    - `go build ./... && go vet ./...` exit 0.
    - `go test ./cmd/... -count=1` exits 0.
  </acceptance_criteria>

  <done>colony/policies/review-depth.yaml is deleted; cmd/review_depth.go's compiled defaults now match what it last said for every field; review-depth keyword matching is proven byte-identical before and after.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the reappearance ratchet with fail-then-pass proof</name>

  <files>cmd/review_depth_ratchet_test.go</files>

  <read_first>
    - 191-PATTERNS.md §"The Ratchet House Style" -- Tier 1 shape and the mandatory fail-then-pass proof
  </read_first>

  <behavior>
    - `TestReviewDepthPolicyDoesNotReappear` fails, by name, if `colony/policies/review-depth.yaml`
      exists on disk.
    - Passes cleanly against the current (post-Task-1) repository state.
    - Fail-then-pass proven: temporarily recreate the file, confirm failure naming it, remove it,
      confirm the test passes again.
  </behavior>

  <action>
Create `cmd/review_depth_ratchet_test.go` with `TestReviewDepthPolicyDoesNotReappear`, following
191-PATTERNS.md's Tier-1 existence-check shape exactly: `os.Stat` against repo root (reuse this repo's
existing repo-root-resolution helper), `t.Errorf` with a message naming the file and citing this plan's
ruling.

Run the fail-then-pass proof described in `<behavior>` and record the transcript in the SUMMARY.
  </action>

  <verify>
    <automated>go test ./cmd/ -run 'TestReviewDepthPolicyDoesNotReappear' -v -count=1</automated>
  </verify>

  <acceptance_criteria>
    - `TestReviewDepthPolicyDoesNotReappear` passes against the current repository state.
    - Fail-then-pass demonstrated and recorded in the SUMMARY.
    - `go build ./... && go vet ./...` exit 0.
    - `go test ./cmd/... -count=1` exits 0.
  </acceptance_criteria>

  <done>A permanent, proven ratchet fails loudly and by name if colony/policies/review-depth.yaml reappears.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `colony/policies/review-depth.yaml` content -> which reviewers spawn on a given phase | Review-depth keyword matching directly gates whether Gatekeeper/Auditor spawn on a phase (per CLAUDE.md's "Queen-Owned Orchestration" section) -- a fold mistake here could silently narrow or widen the security review surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-191-04-01 | Tampering (silent security-relevant regression) | `cmd/review_depth.go`'s compiled keyword slices | mitigate | Field-by-field diff table covers all three keyword lists plus smart-default-reasons, not a sample; before/after regression test asserts every entry |
| T-191-04-02 | Tampering (silent reintroduction) | `colony/policies/review-depth.yaml` | mitigate | `TestReviewDepthPolicyDoesNotReappear`, proven fail-then-pass |
| T-191-04-03 | Elevation of Privilege (weakened security-keyword matching) | `securityRiskKeywordsFallback` | mitigate | This task's fold step is diff-and-preserve, never diff-and-narrow -- any divergent value found is folded in, not silently dropped; the before/after test would fail if a keyword were lost |

</threat_model>

<verification>
```bash
go build ./...
go vet ./...
go test ./cmd/ -run 'TestReviewDepth|TestGetHeavyKeywords|TestGetSecurityRiskKeywords|TestReviewDepthPolicyDoesNotReappear' -v -count=1
go test ./cmd/... -count=1
```
</verification>

<success_criteria>
- `colony/policies/review-depth.yaml` is deleted.
- Every field it defined that diverged from `cmd/review_depth.go`'s compiled defaults was folded in
  before deletion.
- Review-depth keyword matching is proven byte-identical before and after, by Go-level regression test
  AND real dev-checkout CLI capture.
- A proven, fail-then-pass reappearance ratchet exists.
- Pre-existing depth-selection tests (`TestBuildWorkerCapHonoursVerificationDepth` and siblings) still
  pass unmodified.
- Full `go test ./cmd/... -count=1` suite passes with zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/191-dead-wood/191-04-SUMMARY.md`, including the field-by-field
diff table and the before/after CLI capture diff.
Note: `.planning/` is gitignored but tracked -- new files there need `git add -f` or they silently
vanish from the commit.
</output>
