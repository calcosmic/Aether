# Phase 174 Discussion Log

**Date:** 2026-08-13
**Mode:** default discussion, redirected by the owner into a four-perspective alignment review

## How this discussion actually went

The standard gray-area menu (dollar figures / when you see it / keeping
history / report style) was presented once. The owner's answer changed the
premise instead of picking areas: they are on subscription billing (dollars
irrelevant), their real goal is the Queen selecting the right agents/skills
and delegation where necessary so cheaper models work well, and they feared
the roadmap was drifting into framework-for-framework's-sake. They asked for
a multi-agent review "to make sure we're not wasting time."

## The review (4 independent perspectives, workflow run wf_f4ee6f19-53d)

1. **Phase 174 fit** — verdict: keep, slimmed. ~90% of the phase is a token
   instrument; only the dollars residue is API-billing machinery. Flagged one
   real design gap: the wrapper-path token figure must be honestly sourced
   (an LLM-relayed number is an assertion, not a measurement).
2. **Roadmap vs goal** — keep 175 (rank #1) and 179 (rank #2); trim 174 and
   178; shrink 176 to wire-or-delete; cut original 177 and 172.1's blocklist
   half. Found the "measurement has no consumer" gap → folded into 179.
3. **Bloat audit** — same phase verdicts independently; plus standing
   dispositions (63-command menu, XML signal exchange, 287 caller-less
   subcommands default-delete when RECLAIM is ever touched).
4. **Capability check** — the Queen's multi-reviewer selection already exists
   test-locked; depth-2 suffices for reviewer fan-out (horizontal). Found the
   milestone's one real hole: guards exist, capability was never granted —
   ordinary workers don't carry the dispatch tool → new Phase 177 (Worker
   Delegation Grant, SPAWN-09..11).

## Owner decision

Asked: reshape the roadmap along these lines? Options: reshape (recommended) /
reshape but keep 177 / leave as-is / just slim 174.

**Owner selected: "Yes, reshape it (Recommended)".**

Applied: ROADMAP.md reshape note + phase edits (174 slimmed with SPEND-08
added; 175 brevity guard; 176 → Roster Ruling; 177 → Worker Delegation Grant
with SPAWN-09..11; ROSTER-03..08 shelved to Future Requirements; 172.1
decided to structural option, housekeeping-tier; 179 gains retune criterion;
v1.25 leftovers marked superseded). REQUIREMENTS.md updated to match.

## Decisions captured in CONTEXT.md

D-01 dollars never headline; D-02 no price table ever; D-03 end-of-build
plain line is the primary surface; D-04 `aether spend` is the detail view,
mutation-free; D-05 colony worker identities in the report; D-06 wrapper-path
measurement honesty (no provider tag on relayed figures); D-07 parent
attribution + no-double-count invariant, dual-column view deferred.

## Deferred

Self/subtree dual-column view (until Phase 177 lands); spend thresholds/
alerts (new capability, no requirement).
