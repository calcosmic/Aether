---
phase: 03-context-expansion
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - tests/integration/context-expansion.test.js
autonomous: true
requirements: [CTX-01, CTX-02]

must_haves:
  truths:
    - "Integration tests prove CONTEXT.md decisions flow from file through colony-prime to prompt_section"
    - "Integration tests prove blocker flags flow from flags.json through colony-prime to prompt_section"
    - "Integration tests prove BLOCKER WARNINGS and REDIRECT pheromones are in separate sections"
    - "Integration tests prove missing files produce no errors or empty sections"
    - "Integration tests prove compact mode caps are enforced"
  artifacts:
    - path: "tests/integration/context-expansion.test.js"
      provides: "End-to-end context expansion tests"
      min_lines: 200
  key_links:
    - from: "tests/integration/context-expansion.test.js"
      to: ".aether/aether-utils.sh (colony-prime)"
      via: "runAetherUtil helper calling colony-prime"
      pattern: "runAetherUtil.*colony-prime"
---

<objective>
Create integration tests proving CONTEXT.md decisions and blocker flags reach builder prompts through colony-prime

Purpose: Regression protection for the context expansion pipeline (CTX-01 and CTX-02). Ensures decisions and blockers are correctly extracted, formatted, capped, and distinguishable from other prompt sections.

Output: tests/integration/context-expansion.test.js with end-to-end tests
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-context-expansion/03-RESEARCH.md
@.planning/phases/03-context-expansion/03-01-SUMMARY.md
@tests/integration/learnings-injection.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create context expansion integration tests</name>
  <files>tests/integration/context-expansion.test.js</files>
  <action>
Create `tests/integration/context-expansion.test.js` following the exact same helper pattern as `tests/integration/learnings-injection.test.js`:
- Copy the same `createTempDir`, `cleanupTempDir`, `runAetherUtil`, and `setupTestColony` helper functions
- Extend `setupTestColony` inline with two new options: `contextDecisions` (array of {date, decision, rationale, madeBy}) and `blockerFlags` (array of {id, type, severity, title, description, source, phase, resolved_at})
- When `contextDecisions` is provided, write a `.aether/CONTEXT.md` file with the "Recent Decisions" markdown table populated from the array
- When `blockerFlags` is provided, write `.aether/data/flags.json` with the flags array

Write these test cases (all using `test.serial`):

1. **colony-prime includes CONTEXT.md decisions in prompt** -- Setup colony with 2 decisions in CONTEXT.md. Run colony-prime. Assert prompt_section includes "KEY DECISIONS", includes both decision texts.

2. **colony-prime includes blocker warnings from flags.json** -- Setup colony with 1 unresolved blocker for the current phase. Run colony-prime. Assert prompt_section includes "BLOCKER WARNINGS", includes blocker title, includes `[source: verification]`.

3. **blocker warnings are distinguishable from REDIRECT pheromones** -- Setup colony with BOTH a REDIRECT pheromone in pheromones.json AND a blocker flag. Run colony-prime. Assert prompt_section includes both "BLOCKER WARNINGS" and "REDIRECT (HARD CONSTRAINTS". Assert they are in separate sections (different indexOf positions). Assert BLOCKER WARNINGS appears BEFORE ACTIVE SIGNALS section.

4. **missing CONTEXT.md produces no error and no section** -- Setup colony WITHOUT CONTEXT.md file. Run colony-prime. Assert it succeeds (json.ok === true). Assert prompt_section does NOT include "KEY DECISIONS".

5. **missing flags.json produces no error and no section** -- Setup colony WITHOUT flags.json. Run colony-prime. Assert it succeeds. Assert prompt_section does NOT include "BLOCKER WARNINGS".

6. **empty decisions table produces no section** -- Setup colony with CONTEXT.md that has the "Recent Decisions" table header but NO data rows. Run colony-prime. Assert prompt_section does NOT include "KEY DECISIONS".

7. **resolved blockers are excluded** -- Setup colony with 1 resolved blocker (`resolved_at` is a date string) and 1 unresolved blocker. Run colony-prime. Assert "BLOCKER WARNINGS" section includes only the unresolved blocker title, not the resolved one.

8. **blockers from wrong phase are excluded** -- Setup colony on phase 2 with a blocker for phase 5 and a blocker for phase 2. Run colony-prime. Assert only the phase-2 blocker appears. Also add a phase-null blocker and assert it IS included (global blockers shown for all phases).

9. **compact mode caps decisions** -- Setup colony with 6 decisions. Run colony-prime in compact mode. Extract the KEY DECISIONS section between markers. Count bullet lines. Assert count <= 3.

10. **log_line includes decision and blocker counts** -- Setup colony with decisions and blockers. Run colony-prime. Assert json.result.log_line includes "decisions" and "blockers".

Reference the test patterns in 03-RESEARCH.md for CONTEXT.md and flags.json test data format. Follow the exact test style and assertion patterns from learnings-injection.test.js.
  </action>
  <verify>Run `npx ava tests/integration/context-expansion.test.js --timeout=30s` -- all 10 tests must pass.</verify>
  <done>All 10 integration tests pass, covering: decision extraction, blocker injection, distinguishability from REDIRECT pheromones, missing file handling, empty table handling, resolved blocker exclusion, wrong-phase exclusion, compact mode caps, and log_line counts</done>
</task>

</tasks>

<verification>
1. `npx ava tests/integration/context-expansion.test.js --timeout=30s` -- all 10 tests pass
2. `npx ava tests/integration/ --timeout=30s` -- all integration tests pass (no regressions in existing suites)
</verification>

<success_criteria>
- 10 integration tests all passing
- No regressions in existing integration test suites
- Test coverage for both CTX-01 (decisions) and CTX-02 (blockers)
- Edge cases covered: missing files, empty data, resolved blockers, wrong phase, compact mode
</success_criteria>

<output>
After completion, create `.planning/phases/03-context-expansion/03-02-SUMMARY.md`
</output>
