---
phase: 78-platform-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/chamber.go
  - cmd/chamber_test.go
  - cmd/status.go
  - cmd/status_ux_test.go
  - cmd/smoke_test.go
  - cmd/state_mutate_flag_test.go
autonomous: true
requirements:
  - PLAT-03
  - PLAT-04
  - UX-04

must_haves:
  truths:
    - "chamber-compare compares a chamber manifest against current colony state and returns real matches and diffs"
    - "Dashboard warnings surface platform health issues from persisted audit data"
    - "state-mutate --verify-only returns guard check result without mutating state"
    - "state-mutate --revert removes a guard precondition from colony state"
    - "All existing tests continue to pass"
  artifacts:
    - path: "cmd/chamber.go"
      provides: "chamber-compare with real comparison logic"
      contains: "func.*compareChamberManifest|loadActiveColonyState"
    - path: "cmd/chamber_test.go"
      provides: "Tests for chamber-compare real data output"
      exports: ["TestChamberCompareWithRealData", "TestChamberCompareEmpty"]
    - path: "cmd/status.go"
      provides: "computeWarnings reads platform-health.json"
      contains: "platform-health"
    - path: "cmd/status_ux_test.go"
      provides: "Test for platform health warnings"
      contains: "TestComputeWarningsPlatformHealth"
    - path: "cmd/state_mutate_flag_test.go"
      provides: "Tests for --verify-only and --revert flags"
      exports: ["TestStateMutateVerifyOnly", "TestStateMutateRevert"]
  key_links:
    - from: "cmd/chamber.go"
      to: "cmd/state_cmds.go loadActiveColonyState"
      via: "function call to load colony state for comparison"
      pattern: "loadActiveColonyState"
    - from: "cmd/status.go"
      to: "platform-health.json"
      via: "store.LoadJSON"
      pattern: "LoadJSON.*platform-health"
    - from: "cmd/smoke_test.go"
      to: "platform-health.json"
      via: "store.SaveJSON"
      pattern: "SaveJSON.*platform-health"
---

<objective>
Wire chamber-compare to produce real comparison data, add platform health warnings to the dashboard, and add test coverage for state-mutate --verify-only/--revert flags.

Purpose: Close the remaining v1.11 milestone audit gaps -- PLAT-03 (dispatch test already exists but chamber-compare was a stub), PLAT-04 (dead flag tests), and UX-04 (dashboard warnings consuming runtime data). Note: PLAT-05's full scope (platform-specific output rendering verification across 3 AI platforms) is deferred -- it requires running commands on Claude/OpenCode/Codex which is outside Go test scope.

Output: Chamber-compare produces real data, dashboard shows platform health warnings, state-mutate flag behavior has test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.11-MILESTONE-AUDIT.md
@.planning/phases/71-platform-hardening/71-VERIFICATION.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From cmd/chamber.go (current chamberCompareCmd):
```go
var chamberCompareCmd = &cobra.Command{
    Use:   "chamber-compare [name]",
    Short: "Compare chamber archive with current colony state",
    RunE: func(cmd *cobra.Command, args []string) error { ... }
}
// Currently returns hardcoded empty matches/diffs at lines 185-189
```

From cmd/chamber.go (chamberListCmd -- pattern for reading chamber manifests):
```go
// Chamber manifest is stored at: .aether/chambers/{name}/manifest.json
// Manifest fields: name, goal, milestone, phases_completed, total_phases, scope
```

From cmd/state_cmds.go (verify-only and revert flag handling, lines 42-52):
```go
verifyOnly, _ := cmd.Flags().GetBool("verify-only")
if verifyOnly && guard != "" {
    outputOK(map[string]interface{}{"guard": guard, "allowed": true, "mode": "verify-only"})
    return nil
}
revert, _ := cmd.Flags().GetString("revert")
if revert != "" {
    return executeRevertGuard(revert)
}
```

From cmd/status.go (computeWarnings -- current signature):
```go
func computeWarnings(state colony.ColonyState, s *storage.Store) []string {
    // Currently checks: stale state, failed phases, unacknowledged midden, pheromone expiry
}
```

From pkg/colony (colony state structure):
```go
type ColonyState struct {
    Goal          *string
    State         string
    CurrentPhase  int
    Plan          Plan
    // ... other fields
}
```

From cmd/chamber_suggest_maintenance_test.go (test pattern for chamber tests):
```go
func setupChamberTest(t *testing.T) (*storage.Store, func()) {
    // Creates temp dir, store, resets rootCmd
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire chamber-compare to real data and add tests</name>
  <files>cmd/chamber.go, cmd/chamber_test.go</files>
  <read_first>
    cmd/chamber.go (full file -- understand chamberCreateCmd, chamberListCmd, chamberCompareCmd, and how manifest.json is structured)
    cmd/chamber_suggest_maintenance_test.go (full file -- understand test setup pattern for chamber tests)
    cmd/state_cmds.go (lines 26-65 -- understand how loadActiveColonyState is called)
  </read_first>
  <action>
    1. Create `cmd/chamber_test.go` with tests FIRST (TDD):
       - `TestChamberCompareWithRealData`: Create a chamber with manifest.json containing `{name: "test-chamber", goal: "test goal", milestone: "v1.0", phases_completed: 2, total_phases: 3}`. Create a COLONY_STATE.json with matching goal but different phases_completed. Run `chamber-compare --name test-chamber`. Assert output JSON has `matches` array containing at least `"goal"` (both have "test goal") and `diffs` array containing at least `"phases_completed"` (2 vs actual). The matches/diffs arrays MUST NOT be empty.
       - `TestChamberCompareNoChamber`: Run `chamber-compare --name nonexistent`. Assert output JSON has `chamber: "nonexistent"`, `error` field containing "not found", and empty matches/diffs.
       - `TestChamberCompareMatchingState`: Create chamber manifest and colony state with identical goal, milestone, phases_completed. Assert `matches` contains all matching fields and `diffs` is empty.

    2. Rewrite the `chamberCompareCmd` RunE in `cmd/chamber.go` to produce real data:
       - Read the chamber manifest from `.aether/chambers/{name}/manifest.json` using `storage.ResolveAetherRoot(context.Background())` and `os.ReadFile` (follow the pattern from `chamberVerifyCmd` at line 77).
       - If the chamber manifest file does not exist: return `outputOK` with `chamber: name, error: "chamber '{name}' not found", matches: [], diffs: []`.
       - Load the current colony state using `loadActiveColonyState()`. If it fails: return matches from manifest only, with `error: "colony state not available"`.
       - Compare these fields between chamber manifest and colony state:
         - `goal`: manifest["goal"] vs *state.Goal
         - `milestone`: manifest["milestone"] vs "" (colony state has no milestone field -- compare against empty string, always a diff if manifest has one)
         - `phases_completed`: manifest["phases_completed"] vs len(completed phases in state.Plan.Phases where Status == colony.PhaseCompleted)
         - `total_phases`: manifest["total_phases"] vs len(state.Plan.Phases)
       - For each field: if values match (string equality or int equality), add to `matches` as `map[string]interface{}{"field": fieldName, "chamber": chamberValue, "current": currentValue}`. If different, add to `diffs` with same structure plus `"chamber_value"` and `"current_value"` keys.
       - Return `outputOK` with `chamber: name, matches: matchesSlice, diffs: diffsSlice, total_compared: 4`.

    3. Run the tests to verify they pass.
  </action>
  <acceptance_criteria>
    - cmd/chamber.go chamberCompareCmd RunE contains `os.ReadFile` (reads actual manifest file)
    - cmd/chamber.go chamberCompareCmd RunE contains `loadActiveColonyState` (reads current state)
    - cmd/chamber.go NO LONGER contains the hardcoded `outputOK(map[string]interface{}{"chamber": name, "matches": []interface{}{}, "diffs": []interface{}{}})` pattern
    - cmd/chamber_test.go exists and contains `TestChamberCompareWithRealData`
    - cmd/chamber_test.go contains `TestChamberCompareNoChamber`
    - cmd/chamber_test.go contains `TestChamberCompareMatchingState`
    - `go test ./cmd/ -run TestChamberCompare -count=1` exits 0
    - `go test ./cmd/ -run TestChamberCreate -count=1` exits 0 (existing tests still pass)
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/ -run "TestChamberCompare|TestChamberCreate|TestChamberVerify|TestChamberList" -count=1 -v</automated>
  </verify>
  <done>
    chamber-compare reads actual chamber manifest and colony state, produces real matches and diffs arrays. Tests cover real data, missing chamber, and matching state scenarios.
  </done>
</task>

<task type="auto">
  <name>Task 2A: Add platform health warnings to dashboard with producer</name>
  <files>cmd/status.go, cmd/status_ux_test.go, cmd/smoke_test.go</files>
  <read_first>
    cmd/status.go (full file -- understand computeWarnings function and all existing warning categories)
    cmd/status_ux_test.go (full file -- understand test patterns for computeWarnings)
    cmd/smoke_test.go (full file -- understand how smoke test works, where to add platform-health.json writing)
  </read_first>
  <action>
    Part A: Dashboard consumer.

    1. Add a new warning category to `computeWarnings` in `cmd/status.go` that reads platform health data from the store:
       - After the existing pheromone expiry warning section (after line ~113), add a new section:
       ```
       // 5. Platform health warnings
       if s != nil {
           var ph map[string]interface{}
           if s.LoadJSON("platform-health.json", &ph) == nil {
               if failed, ok := ph["failed_commands"].([]interface{}); ok && len(failed) > 0 {
                   warnings = append(warnings, fmt.Sprintf("Platform health: %d command(s) failed smoke test. Run `aether smoke-test` to diagnose.", len(failed)))
               }
               if mismatches, ok := ph["flag_mismatches"].([]interface{}); ok && len(mismatches) > 0 {
                   warnings = append(warnings, fmt.Sprintf("Platform health: %d CLI flag mismatch(es) detected. Run `aether cli-audit` to review.", len(mismatches)))
               }
           }
       }
       ```
       This reads a `platform-health.json` file from the data store.

    2. Add tests in `cmd/status_ux_test.go`:
       - `TestComputeWarningsPlatformHealth_FailedCommands`: Create a temp store, write `platform-health.json` with `{"failed_commands": ["cmd1", "cmd2"], "flag_mismatches": []}`. Call computeWarnings with minimal ColonyState. Assert warning string contains "2 command(s) failed smoke test" and "smoke-test".
       - `TestComputeWarningsPlatformHealth_FlagMismatches`: Create a temp store, write `platform-health.json` with `{"failed_commands": [], "flag_mismatches": ["flag1"]}`. Call computeWarnings. Assert warning contains "1 CLI flag mismatch" and "cli-audit".
       - `TestComputeWarningsPlatformHealth_Clean`: Create a temp store, write `platform-health.json` with `{"failed_commands": [], "flag_mismatches": []}`. Call computeWarnings. Assert no platform health warning appears.
       - `TestComputeWarningsPlatformHealth_NoFile`: Call computeWarnings with no platform-health.json. Assert no platform health warning appears (graceful degradation).

    Part B: Producer in smoke test.

    3. In `cmd/smoke_test.go`, after the smoke test completes its checks, add code that writes results to `platform-health.json` in the store:
       - Collect `failed_commands` (list of commands that returned errors or non-zero exit)
       - Collect `flag_mismatches` (list of flags found in markdown but missing from Go runtime, or vice versa -- if the smoke test already checks this, aggregate the results)
       - Write `{"failed_commands": [...], "flag_mismatches": [...]}` to `platform-health.json` via `s.SaveJSON("platform-health.json", data)`
       - This ensures the dashboard consumer (computeWarnings) has real data to read after `aether smoke-test` runs.

    4. Run all tests to verify they pass.
  </action>
  <acceptance_criteria>
    - cmd/status.go computeWarnings function contains the string "platform-health.json"
    - cmd/status.go computeWarnings function contains the string "failed_commands"
    - cmd/status.go computeWarnings function contains the string "flag_mismatches"
    - cmd/status.go computeWarnings function contains "smoke-test" (in warning message)
    - cmd/smoke_test.go writes "platform-health.json" (grep confirms SaveJSON call)
    - cmd/status_ux_test.go contains `TestComputeWarningsPlatformHealth_FailedCommands`
    - cmd/status_ux_test.go contains `TestComputeWarningsPlatformHealth_FlagMismatches`
    - cmd/status_ux_test.go contains `TestComputeWarningsPlatformHealth_Clean`
    - `go test ./cmd/ -run TestComputeWarnings -count=1` exits 0
    - `go test ./cmd/ -count=1` exits 0 (all existing tests still pass)
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/ -run "TestComputeWarnings" -count=1 -v</automated>
  </verify>
  <done>
    Dashboard warnings consume platform-health.json as runtime data (failed commands, flag mismatches). Smoke test produces platform-health.json so the dashboard consumer has real data. All existing tests continue to pass.
  </done>
</task>

<task type="auto">
  <name>Task 2B: Add state-mutate flag tests</name>
  <files>cmd/state_mutate_flag_test.go</files>
  <read_first>
    cmd/state_cmds.go (lines 26-65 -- verify the --verify-only and --revert flag handling code)
    cmd/state_cmds_test.go (full file -- understand test setup pattern)
  </read_first>
  <action>
    1. Create `cmd/state_mutate_flag_test.go` with tests for --verify-only and --revert:
       - `TestStateMutateVerifyOnly_GuardPasses`: Create a colony state with a task marked complete. Run `state-mutate --guard "task-complete:1.1" --verify-only`. Parse JSON output. Assert `mode` == "verify-only", `guard` == "task-complete:1.1", `allowed` == true. Verify COLONY_STATE.json was NOT modified (read before and after, compare).
       - `TestStateMutateVerifyOnly_GuardFails`: Create colony state WITHOUT the task complete. Run `state-mutate --guard "task-complete:1.1" --verify-only`. Assert output is an error (not ok:true). Verify state was NOT modified.
       - `TestStateMutateRevert`: Create colony state with a completed guard entry (e.g. task-complete:1.1 is true). Run `state-mutate --revert "task-complete:1.1"`. Assert output confirms revert. Read colony state and verify the guard entry is removed.
       - `TestStateMutateVerifyOnly_NoGuard`: Run `state-mutate --verify-only` (no --guard). Assert it proceeds to normal mutation behavior (verify-only without guard should fall through, since the condition `verifyOnly && guard != ""` is false).

    2. Follow the test setup pattern from existing state_cmds_test.go: use `saveGlobals(t)`, `resetRootCmd(t)`, create temp dir with `.aether/data`, create store, set up colony state with `store.SaveJSON`.

    3. Run all tests to verify they pass.
  </action>
  <acceptance_criteria>
    - cmd/state_mutate_flag_test.go exists and contains `TestStateMutateVerifyOnly_GuardPasses`
    - cmd/state_mutate_flag_test.go contains `TestStateMutateVerifyOnly_GuardFails`
    - cmd/state_mutate_flag_test.go contains `TestStateMutateRevert`
    - `go test ./cmd/ -run TestStateMutateVerifyOnly -count=1` exits 0
    - `go test ./cmd/ -run TestStateMutateRevert -count=1` exits 0
    - `go test ./cmd/ -count=1` exits 0 (all existing tests still pass)
  </acceptance_criteria>
  <verify>
    <automated>go test ./cmd/ -run "TestStateMutateVerifyOnly|TestStateMutateRevert" -count=1 -v</automated>
  </verify>
  <done>
    State-mutate --verify-only and --revert flags have dedicated test coverage proving they work correctly. All existing tests continue to pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| platform-health.json -> dashboard | External data file read by status command -- file could be malformed |
| chamber manifest -> comparison | Archive data compared against current state -- stale data possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Tampering | platform-health.json | accept | File is local-only, not user-input; contents controlled by smoke-test CLI |
| T-78-02 | Denial of Service | computeWarnings | mitigate | Use `s.LoadJSON` with standard error handling -- malformed JSON returns error, warning skipped |
| T-78-03 | Information Disclosure | chamber-compare | accept | Chamber data is local archive, no cross-boundary flow |
</threat_model>

<verification>
1. All new tests pass: `go test ./cmd/ -run "TestChamberCompare|TestComputeWarningsPlatform|TestStateMutateVerifyOnly|TestStateMutateRevert" -count=1 -v`
2. All existing tests pass: `go test ./... -count=1`
3. Dispatch manifest test still passes: `go test ./cmd/ -run TestDispatchManifestAllCastes -count=1 -v`
4. Smoke test writes platform-health.json: grep confirms SaveJSON call in smoke_test.go
5. Full suite with race detector: `go test ./... -race -count=1`
</verification>

<success_criteria>
1. chamber-compare returns real matches and diffs (not hardcoded empty arrays) -- verified by test
2. Dashboard warnings surface platform health data from platform-health.json -- verified by test
3. state-mutate --verify-only prevents mutation and reports guard check -- verified by test
4. state-mutate --revert removes guard entries -- verified by test
5. All existing tests continue to pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/78-platform-test-coverage/78-01-SUMMARY.md`
</output>
