---
phase: 72-smart-init-charter
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - pkg/colony/colony.go
  - pkg/colony/colony_test.go
  - pkg/colony/testdata/COLONY_STATE.golden.json
  - cmd/init_research.go
  - cmd/init_research_test.go
  - cmd/init_cmd.go
autonomous: true
requirements:
  - INIT-01

must_haves:
  truths:
    - "init-research generates a 7-section charter with all fields populated"
    - "COLONY_STATE.json includes a charter sub-object when created with --charter-json"
    - "Old COLONY_STATE.json files without charter still load correctly (backward compatible)"
  artifacts:
    - path: "pkg/colony/colony.go"
      provides: "Charter struct definition"
      contains: "type Charter struct"
    - path: "cmd/init_research.go"
      provides: "Expanded charterData with 7 fields, 3 new section generators"
      contains: "tech_stack"
    - path: "cmd/init_cmd.go"
      provides: "--charter-json flag for passing approved charter data"
      contains: "charter-json"
  key_links:
    - from: "cmd/init_research.go"
      to: "pkg/colony/colony.go"
      via: "import colony package, use colony.Charter type"
      pattern: "colony\\.Charter"
    - from: "cmd/init_cmd.go"
      to: "pkg/colony/colony.go"
      via: "ColonyState.Charter field assignment"
      pattern: "Charter.*charter"
---

<objective>
Expand the charter data model from 4 to 7 sections, add the Charter struct to the colony package, and wire the --charter-json flag so both wrappers and the Go-native ceremony can persist approved charters.

Purpose: This is the data layer that both the Go-native ceremony (Plan 02) and the wrapper ceremony depend on. Without it, charter data is ephemeral and never persisted in COLONY_STATE.json.
Output: Expanded init-research output with 7 charter fields, Charter struct in pkg/colony/, --charter-json flag on aether init, backward-compatible COLONY_STATE.json schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-smart-init-charter/72-CONTEXT.md
@.planning/phases/72-smart-init-charter/72-RESEARCH.md
@.planning/phases/72-smart-init-charter/72-PATTERNS.md
@.planning/phases/72-smart-init-charter/72-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From pkg/colony/colony.go (lines 159-187):
```go
type ColonyState struct {
    Version            string          `json:"version"`
    Goal               *string         `json:"goal"`
    Scope              ColonyScope     `json:"scope,omitempty"`
    ColonyName         *string         `json:"colony_name"`
    ColonyVersion      int             `json:"colony_version"`
    State              State           `json:"state"`
    CurrentPhase       int             `json:"current_phase"`
    SessionID          *string         `json:"session_id"`
    InitializedAt      *time.Time      `json:"initialized_at"`
    BuildStartedAt     *time.Time      `json:"build_started_at"`
    Plan               Plan            `json:"plan"`
    Memory             Memory          `json:"memory"`
    Errors             Errors          `json:"errors"`
    Signals            []Signal        `json:"signals"`
    Graveyards         []Graveyard     `json:"graveyards"`
    Events             []string        `json:"events"`
    ColonyDepth        string          `json:"colony_depth,omitempty"`
    PlanGranularity    PlanGranularity `json:"plan_granularity,omitempty"`
    ParallelMode       ParallelMode    `json:"parallel_mode,omitempty"`
    TerritorySurveyed  *string         `json:"territory_surveyed,omitempty"`
    Milestone          string          `json:"milestone"`
    MilestoneUpdatedAt *string         `json:"milestone_updated_at,omitempty"`
    Paused             bool            `json:"paused,omitempty"`
    PausedAt           *string         `json:"paused_at,omitempty"`
    Worktrees          []WorktreeEntry `json:"worktrees,omitempty"`
    RunID              *string            `json:"run_id,omitempty"`
    GateResults        []GateResultEntry  `json:"gate_results,omitempty"`
}
```

From cmd/init_research.go (lines 17-53):
```go
type governanceInfo struct {
    Linters        []string `json:"linters"`
    Formatters     []string `json:"formatters"`
    TestFrameworks []string `json:"test_frameworks"`
    CIConfigs      []string `json:"ci_configs"`
    BuildTools     []string `json:"build_tools"`
}

type pheromoneSuggestion struct {
    Type    string `json:"type"`
    Content string `json:"content"`
    Reason  string `json:"reason"`
}

type charterData struct {
    Intent     string `json:"intent"`
    Vision     string `json:"vision"`
    Governance string `json:"governance"`
    Goals      string `json:"goals"`
}
```

From cmd/init_research.go (line 360):
```go
func generateCharter(goal, detected string, governance governanceInfo, readmeSummary string, gitHistory gitHistoryInfo) charterData
```

From cmd/init_cmd.go (lines 218-221):
```go
func init() {
    initCmd.Flags().String("scope", string(colony.ScopeProject), "Colony scope: project or meta")
    rootCmd.AddCommand(initCmd)
}
```

From cmd/init_cmd.go (lines 121-145) -- ColonyState creation:
```go
state := colony.ColonyState{
    Version:       "3.0",
    Goal:          &goal,
    Scope:         scope,
    // ... all fields ...
    ParallelMode:  colony.ModeInRepo,
}
if err := store.SaveJSON("COLONY_STATE.json", state); err != nil { ... }
```
</interfaces>
</context>

<feature>
  <name>Charter Data Expansion and Persistence</name>
  <files>pkg/colony/colony.go, pkg/colony/colony_test.go, pkg/colony/testdata/COLONY_STATE.golden.json, cmd/init_research.go, cmd/init_research_test.go, cmd/init_cmd.go</files>
  <behavior>
    - Test 1: ColonyState with Charter field round-trips through JSON marshal/unmarshal (TestCharterRoundTrip)
    - Test 2: ColonyState without Charter field (old state) unmarshals to nil Charter (backward compat)
    - Test 3: init-research output includes 7 charter fields (tech_stack, key_risks, constraints non-empty when go.mod present)
    - Test 4: init-research key_risks includes "No CI/CD pipeline" when no CI config exists
    - Test 5: init-research constraints includes linter/formatter rules when governance tools detected
    - Test 6: --charter-json flag on aether init stores the parsed charter in COLONY_STATE.json
    - Test 7: Golden file round-trip with Charter field present
  </behavior>
  <implementation>
    RED: Write failing tests for each behavior above.
    GREEN: Implement minimum code to pass all tests.
    REFACTOR: Clean up any duplication.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Charter struct to pkg/colony and expand charterData in init_research</name>
  <files>pkg/colony/colony.go, pkg/colony/colony_test.go, pkg/colony/testdata/COLONY_STATE.golden.json, cmd/init_research.go, cmd/init_research_test.go</files>
  <read_first>
    - pkg/colony/colony.go (full file -- need to see ColonyState struct and all existing types)
    - pkg/colony/colony_test.go (full file -- need to see test patterns, especially TestRoundTripColonyState and TestNullableFields_Nil)
    - pkg/colony/testdata/COLONY_STATE.golden.json (current golden file content)
    - cmd/init_research.go (full file -- need charterData struct, generateCharter function, governanceInfo struct, initResearchCmd RunE function)
    - cmd/init_research_test.go (full file -- need TestInitResearchCharter and test patterns)
  </read_first>
  <action>
    Per D-01: Add Charter struct to pkg/colony/colony.go with 7 fields: Intent, Vision, Governance, Goals, TechStack, KeyRisks, Constraints. Use json tags matching D-01 field names: "intent", "vision", "governance", "goals", "tech_stack", "key_risks", "constraints".

    Per D-01: Add Charter *Charter field with json tag "charter,omitempty" to ColonyState struct. Place it after GateResults (the current last field) and before the closing brace. Use pointer type with omitempty for backward compatibility (per RESEARCH Pitfall 5).

    Per D-03: Expand charterData struct in cmd/init_research.go from 4 to 7 fields. Add TechStack string with json:"tech_stack", KeyRisks string with json:"key_risks", Constraints string with json:"constraints".

    Per D-03/D-04: Expand generateCharter() function to generate 3 new sections from data already computed in the RunE function:

    1. generateTechStack: Use the already-computed `languages []string` and `frameworks []string` from the RunE function. Format as "Languages: go, node. Frameworks/Tools: maven, gradle" (deduplicate frameworks that overlap with languages). If no languages/frameworks detected, return "No specific tech stack detected".

    2. generateKeyRisks: Use the already-computed `governance governanceInfo`, `isGitRepo bool`, and `pheromoneSuggestions []pheromoneSuggestion`. Heuristics:
       - No CI config -> "No CI/CD pipeline detected -- manual deployment risk"
       - No test frameworks -> "No test framework detected -- regression risk"
       - No linters -> "No linter configured -- code quality may drift"
       - REDIRECT pheromone suggestion containing "secrets" -> "Potential secret exposure -- .env files without .gitignore protection"
       - Not a git repo -> "Not a git repository -- no version control"
       If no risks, return "No significant risks detected from initial scan".

    3. generateConstraints: Use the already-computed `governance governanceInfo`. Heuristics:
       - Has linters -> "Follow {linter1}/{linter2} rules"
       - Has formatters -> "Use {formatter1}/{formatter2} for code formatting"
       - Has test frameworks -> "Write tests using {framework1}/{framework2}"
       - Has build tools -> "Build with {tool1}/{tool2}"
       If no constraints, return "No formal constraints detected -- colony should establish conventions".

    Change generateCharter signature to accept the additional data it needs. Add these parameters: `languages []string`, `frameworks []string`, `isGitRepo bool`, `pheromoneSuggestions []pheromoneSuggestion`. The existing parameters (goal, detected, governance, readmeSummary, gitHistory) remain unchanged.

    Update the call site in initResearchCmd.RunE (around line 545) to pass the new arguments: `languages`, `frameworks`, `isGitRepo`, `pheromoneSuggestions`.

    These can be private helper functions within init_research.go (generateTechStack, generateKeyRisks, generateConstraints) called from within generateCharter, or inline logic -- executor's choice.

    Write tests FIRST (RED):
    - TestCharterRoundTrip in pkg/colony/colony_test.go: Create ColonyState with a non-nil Charter (all 7 fields set), marshal, unmarshal, verify all fields preserved.
    - TestCharterOmitEmpty in pkg/colony/colony_test.go: Create ColonyState without Charter, marshal, verify JSON does not contain "charter" key. Unmarshal, verify Charter is nil.
    - TestCharterBackwardCompat in pkg/colony/colony_test.go: Unmarshal the existing golden JSON (which has no charter field), verify Charter is nil and all other fields load correctly.
    - TestInitResearchCharterExpanded in cmd/init_research_test.go: Extend the existing TestInitResearchCharter to also verify that `tech_stack`, `key_risks`, and `constraints` are present and non-empty in the charter output when a go.mod file exists.
    - TestInitResearchCharterKeyRisksNoCI in cmd/init_research_test.go: Create a target dir with no CI config, run init-research, verify charter.key_risks contains "No CI/CD pipeline".
    - TestInitResearchCharterConstraintsWithLinter in cmd/init_research_test.go: Create a target dir with .eslintrc, run init-research, verify charter.constraints contains "ESLint".
    - Update golden file testdata/COLONY_STATE.golden.json: Add a "charter" sub-object with all 7 fields after the "gate_results" field, with representative values. This must match the serialized output of the Charter struct.

    Then implement (GREEN):
    - Add Charter struct to pkg/colony/colony.go
    - Add Charter field to ColonyState
    - Expand charterData in init_research.go
    - Expand generateCharter with 3 new section generators
    - Update call site in RunE
    - Update golden file
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && go test ./pkg/colony/... -run "TestCharter|TestRoundTripColonyState|TestGoldenColonyState" -count=1 -v && go test ./cmd/... -run "TestInitResearchCharter" -count=1 -v</automated>
  </verify>
  <done>
    - pkg/colony/colony.go contains "type Charter struct" with 7 fields (Intent, Vision, Governance, Goals, TechStack, KeyRisks, Constraints)
    - ColonyState struct contains "Charter *Charter" field with "charter,omitempty" json tag
    - cmd/init_research.go charterData struct has 7 fields including tech_stack, key_risks, constraints
    - generateCharter() signature includes languages, frameworks, isGitRepo, pheromoneSuggestions parameters
    - init-research JSON output includes charter with all 7 fields populated
    - TestCharterRoundTrip passes: marshal/unmarshal preserves all 7 fields
    - TestCharterOmitEmpty passes: state without charter serializes without "charter" key
    - TestCharterBackwardCompat passes: old JSON without charter unmarshals to nil Charter
    - TestInitResearchCharterExpanded passes: tech_stack, key_risks, constraints are non-empty
    - Golden file testdata/COLONY_STATE.golden.json round-trips correctly
  </done>
  <acceptance_criteria>
    - "type Charter struct" present in pkg/colony/colony.go
    - "Charter *Charter" present in ColonyState struct in pkg/colony/colony.go
    - `grep -c "tech_stack" cmd/init_research.go` returns >= 1
    - `grep -c "key_risks" cmd/init_research.go` returns >= 1
    - `grep -c "constraints" cmd/init_research.go` returns >= 1
    - `go test ./pkg/colony/... -run TestCharter -count=1` exits 0
    - `go test ./cmd/... -run TestInitResearchCharter -count=1` exits 0
    - `go test ./pkg/colony/... -run TestGoldenColonyState -count=1` exits 0
    - `go test ./... -race -count=1 -timeout 120s` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add --charter-json flag to init command and wire charter persistence</name>
  <files>cmd/init_cmd.go, cmd/init_research.go</files>
  <read_first>
    - cmd/init_cmd.go (full file -- need initCmd RunE, flag registration, state creation)
    - cmd/init_research.go (need updated charterData struct from Task 1)
  </read_first>
  <action>
    Per D-01: When --charter-json flag is provided, parse the JSON string into a colony.Charter struct and assign it to the ColonyState.Charter field before saving.

    Per D-02: No separate charter.md file -- charter lives as structured data in COLONY_STATE.json only.

    Per D-04: This flag is used by wrappers to pass approved charter data. The Go-native ceremony (Plan 02) will also use this internally.

    Implementation:

    1. Add `--charter-json` flag to initCmd in the init() function:
       ```go
       initCmd.Flags().String("charter-json", "", "Approved charter data as JSON string")
       ```

    2. Add `"encoding/json"` to imports in init_cmd.go.

    3. In the initCmd RunE function, after creating the ColonyState struct and before `store.SaveJSON`:
       - Read the --charter-json flag value
       - If non-empty, parse it into a colony.Charter struct using json.Unmarshal
       - If parsing fails, call outputError and return nil (do not proceed with broken charter data)
       - If parsing succeeds, assign the parsed Charter pointer to state.Charter

    4. Also update charterData in init_research.go to use the colony.Charter type directly (replace the standalone charterData struct). This ensures the JSON field names are always in sync between research output and state persistence. Change:
       ```go
       type charterData struct { ... }  // REMOVE this
       ```
       Use `colony.Charter` directly in the output map and generateCharter return type.

    Write tests FIRST (RED):
    - TestInitWithCharterJSONFlag: Create a temp store, run `aether init --charter-json '{"intent":"Build X","vision":"A go project","governance":"No governance","goals":"Goal: Build X","tech_stack":"Languages: go","key_risks":"No risks","constraints":"No constraints"}' "Build X"` and verify the resulting COLONY_STATE.json contains a "charter" object with all 7 fields.
    - TestInitWithoutCharterJSONFlag: Run `aether init "Build Y"` and verify COLONY_STATE.json has no "charter" key (or charter is null/omitted).
    - TestInitInvalidCharterJSON: Run `aether init --charter-json 'not-json' "Build Z"` and verify it returns an error (ok:false in output) and does NOT create COLONY_STATE.json.

    Then implement (GREEN).

    SECURITY (T-72-01): Validate JSON structure before unmarshaling into Charter struct. The --charter-json flag accepts user-controlled input. Use json.Unmarshal into the typed struct (which inherently rejects extra fields and validates types). Sanitize string fields by trimming whitespace. Reject any field value over 2000 characters to prevent unreasonably large state files. Return outputError if validation fails.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && go test ./cmd/... -run "TestInit.*Charter" -count=1 -v && go test ./... -race -count=1 -timeout 120s</automated>
  </verify>
  <done>
    - init_cmd.go registers --charter-json flag
    - aether init with --charter-json stores Charter in COLONY_STATE.json
    - aether init without --charter-json works identically to before (backward compatible)
    - Invalid JSON in --charter-json produces an error and does not create COLONY_STATE.json
    - charterData removed from init_research.go; colony.Charter used directly
    - All existing tests still pass
  </done>
  <acceptance_criteria>
    - `grep -c "charter-json" cmd/init_cmd.go` returns >= 1
    - `grep -c "charterData struct" cmd/init_research.go` returns 0 (replaced with colony.Charter)
    - `grep -c "colony.Charter" cmd/init_research.go` returns >= 1
    - `go test ./cmd/... -run TestInitWithCharterJSONFlag -count=1` exits 0
    - `go test ./cmd/... -run TestInitWithoutCharterJSONFlag -count=1` exits 0
    - `go test ./cmd/... -run TestInitInvalidCharterJSON -count=1` exits 0
    - `go test ./... -race -count=1 -timeout 120s` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| --charter-json flag input | User-controlled JSON string passed to aether init |
| COLONY_STATE.json on disk | Persistent state file that downstream commands read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Tampering | --charter-json flag in init_cmd.go | mitigate | json.Unmarshal into typed struct rejects invalid structure; trim whitespace on all string fields; reject field values over 2000 chars |
| T-72-02 | Tampering | init-research target path | accept | Already mitigated by filepath.Clean + directory validation in existing init-research code |
| T-72-03 | Denial of Service | Very large charter JSON string | accept | Low risk -- CLI runs locally, user would only harm their own session; 2000-char per-field limit in T-72-01 provides reasonable bound |
</threat_model>

<verification>
1. `go test ./pkg/colony/... -count=1` -- Charter struct and round-trip tests pass
2. `go test ./cmd/... -run "TestInit" -count=1 -v` -- All init-related tests pass including new charter tests
3. `go test ./... -race -count=1 -timeout 120s` -- Full test suite green
4. `go build ./cmd/aether` -- Binary compiles successfully
</verification>

<success_criteria>
- init-research JSON output contains charter object with 7 fields (intent, vision, governance, goals, tech_stack, key_risks, constraints)
- `aether init --charter-json '...'` stores charter in COLONY_STATE.json
- Old COLONY_STATE.json files without charter load correctly (Charter field is nil)
- All 2900+ existing tests pass with race detector enabled
- Golden file round-trip passes
</success_criteria>

<output>
After completion, create `.planning/phases/72-smart-init-charter/72-01-SUMMARY.md`
</output>
