---
milestone: v1.14
milestone_name: Queen Authority
audited: "2026-05-04T23:59:00Z"
status: PARTIAL
scores:
  requirements: 19/19
  phases: 7/7
  integration: 14/16
  flows: 3/4
gaps:
  summary: "1 BLOCKER: 4 build finalize recovery tests fail -- runCodexBuildFinalize never calls orchestrateRecovery. 1 WARNING: resolveReferenceSection is a stub returning empty string."
  integration:
    - type: BLOCKER
      description: "runCodexBuildFinalize() in cmd/codex_build_finalize.go does not call orchestrateRecovery() -- 4 tests fail expecting recovery_instructions in build finalize result map"
      severity: code_gap
      failing_tests:
        - "TestBuildFinalize_RecoveryForFailedDispatch"
        - "TestBuildFinalize_RecoveryForBlockingDispatch"
        - "TestBuildFinalize_BudgetPersisted"
        - "TestBuildFinalize_MultipleFailedDispatches"
      actual_wiring: "Build recovery only works through queen_wave_lifecycle.go (Phase 98), NOT through the external build finalize path"
    - type: WARNING
      description: "resolveReferenceSection() in cmd/references.go is a stub returning empty string -- never replaced by full implementation"
      severity: stub_implementation
      consumers:
        - "cmd/codex_build.go:1908 calls resolveReferenceSection() which always returns empty string"
---

# v1.14 Queen Authority -- Integration Audit

**Auditor:** Cross-phase integration check
**Date:** 2026-05-04
**Phases in scope:** 93-99

## Wiring Summary

**Connected:** 14 exports properly used across phases
**Orphaned:** 0 exports created but unused
**Missing:** 2 expected connections (1 code gap, 1 stub)
**Flows complete:** 3/4 end-to-end flows verified (1 partial)

## Phase Export/Import Map

```
Phase 93 (Gate Classification):
  provides:  GateClassificationTier, gateClassify(), isHardBlockGate(),
             QueenAnnotation, gate-classify CLI
  consumed by: Phase 95 (auto-resolve tier checks), Phase 96 (continue finalize
                gate tier evaluation), Phase 97 (queenDecide tier lookup)

Phase 94 (Recovery Data Model):
  provides:  FailureClassification, FailureRecord, RecoveryLogEntry,
             classifyWorkerFailure(), recoveryLogWritePhase/ReadPhase,
             failure-classify CLI
  consumed by: Phase 95 (auto-resolve recovery log entries), Phase 96
                (orchestrateRecovery classification), Phase 99 (audit consolidation)

Phase 95 (Smart Gate Pipeline):
  provides:  autoResolveSoftBlockGates(), autoResolveDepthMultiplier(),
             shouldAutoResolve(), annotateGateResult(), dispatchFixer() wiring
  consumed by: Phase 96 (continue finalize preserves dispatchFixer call),
                Phase 97 (depth threshold awareness in queen decisions)

Phase 96 (Auto-Recovery Orchestrator):
  provides:  orchestrateRecovery(), RecoveryBudget, RecoveryContext/Outcome,
             budgetFromRecoveryLog, newRecoveryBudget, persistBudgetToRecoveryLog
  consumed by: Phase 97 (budget in queenDecide), Phase 98 (between-wave recovery
                in queenWaveLifecycle), Phase 99 (audit reads recovery-log)

Phase 97 (Queen-Led Continue):
  provides:  QueenDecision, QueenStateFile, queenDecide(), queenStateWrite(),
             queenStateRead(), queenLogEscalation()
  consumed by: Phase 98 (state persistence pattern), Phase 99 (audit reads
                queen-state)

Phase 98 (Queen Wave Lifecycle):
  provides:  queenWaveLifecycle(), WaveDispatchFunc, WaveLifecycleSummary,
             writeWaveSummary(), readWaveSummary(), renderWaveSummaryTable()
  consumed by: Phase 99 (audit reads wave-summary, build command calls lifecycle)

Phase 99 (Output Filtering & Phase Summary):
  provides:  filteredPrintln/Fprintf/Fprintln, setBuildVerbose,
             consolidateQueenAudit(), writeAuditFile(), readAuditFile(),
             renderActionsNeeded(), renderPhaseEndSummary()
  consumed by: build command (all wired inline)
```

## Detailed Cross-Phase Wiring Verification

### Phase 93 -> Phase 95: Gate Classification into Auto-Resolve

| Export | From | Consumer | Status |
|--------|------|----------|--------|
| `gateClassify()` | cmd/gate.go:610 | cmd/codex_continue_finalize.go:321,349 | WIRED |
| `softBlock` constant | cmd/gate.go | cmd/codex_continue_finalize.go:322 | WIRED |
| `hardBlock` constant | cmd/gate.go | cmd/codex_continue_finalize.go:351 | WIRED |
| `QueenAnnotation` | cmd/gate.go | cmd/codex_continue_finalize.go:260 | WIRED |

### Phase 93 -> Phase 96: Gate Classification into Recovery

| Export | From | Consumer | Status |
|--------|------|----------|--------|
| `gateClassify()` | cmd/gate.go | cmd/codex_continue_finalize.go:321,349 | WIRED |
| `hardBlock`/`softBlock` | cmd/gate.go | cmd/codex_continue_finalize.go:322,351 | WIRED |

### Phase 93 -> Phase 97: Gate Classification into Queen Decision

| Export | From | Consumer | Status |
|--------|------|----------|--------|
| `gateClassify()` | cmd/gate.go | cmd/queen_decision.go:76 | WIRED |
| `GateClassificationTier` | cmd/gate.go | cmd/queen_decision.go | WIRED |

### Phase 94 -> Phase 95: Recovery Data into Auto-Resolve

| Export | From | Consumer | Status |
|--------|------|----------|--------|
| `RecoveryLogEntry` | cmd/recovery_classify.go | cmd/codex_continue_finalize.go:288-306 | WIRED |
| `FailureRecord` | cmd/recovery_classify.go | cmd/codex_continue_finalize.go:290 | WIRED |
| `Recoverable`/`Transient` | cmd/recovery_classify.go | cmd/codex_continue_finalize.go:296-297 | WIRED |
| `recoveryLogReadPhase()` | cmd/recovery_classify.go:135 | cmd/codex_continue_finalize.go:309 | WIRED |
| `recoveryLogWritePhase()` | cmd/recovery_classify.go:125 | cmd/codex_continue_finalize.go:312 | WIRED |

### Phase 94 -> Phase 96: Recovery Data into Orchestrator

| Export | From | Consumer | Status |
|--------|------|----------|--------|
| `classifyWorkerFailure()` | cmd/recovery_classify.go:63 | cmd/recovery_orchestrator.go:138 | WIRED |
| `recoveryLogReadPhase()` | cmd/recovery_classify.go:135 | cmd/recovery_orchestrator.go:75,88 | WIRED |
| `CircuitBreaker` | cmd/circuit_breaker.go | cmd/recovery_orchestrator.go:116,216,273 | WIRED |
| `findSameCastePeer()` | cmd/circuit_breaker.go:102 | cmd/recovery_orchestrator.go:339 | WIRED |

### Phase 95 -> Phase 96: Auto-Resolve into Recovery Wiring

| Export | From | Consumer | Status |
|--------|------|----------|--------|
| `autoResolveSoftBlockGates()` | cmd/gate.go:731 | cmd/codex_continue_finalize.go:240 | WIRED |
| `dispatchFixer()` | cmd/fixer_dispatch.go:131 | cmd/codex_continue_finalize.go:330 | WIRED |

### Phase 96 -> Phase 97: Recovery into Queen Decision

| Export | From | Consumer | Status |
|--------|------|----------|--------|
| `RecoveryBudget` | cmd/recovery_orchestrator.go | cmd/queen_decision.go (param type) | WIRED |
| `CircuitBreaker` | cmd/circuit_breaker.go | cmd/queen_decision.go (param type) | WIRED |
| `budgetFromRecoveryLog()` | cmd/recovery_orchestrator.go:74 | cmd/codex_continue_plan.go:104 | WIRED |
| `newRecoveryBudget()` | cmd/recovery_orchestrator.go:65 | cmd/codex_continue_plan.go:106, queen_wave_lifecycle.go:81 | WIRED |

### Phase 96 -> Phase 98: Recovery into Wave Lifecycle

| Export | From | Consumer | Status |
|--------|------|----------|--------|
| `orchestrateRecovery()` | cmd/recovery_orchestrator.go:137 | cmd/queen_wave_lifecycle.go:151 | WIRED |
| `RecoveryContext` | cmd/recovery_orchestrator.go | cmd/queen_wave_lifecycle.go:138-149 | WIRED |
| `newRecoveryBudget()` | cmd/recovery_orchestrator.go:65 | cmd/queen_wave_lifecycle.go:81 | WIRED |
| `recoveryLogReadPhase()` | cmd/recovery_classify.go:135 | cmd/queen_wave_lifecycle.go:155 | WIRED |
| `recoveryLogWritePhase()` | cmd/recovery_classify.go:125 | cmd/queen_wave_lifecycle.go:158 | WIRED |

### Phase 97 -> Phase 98: Queen State into Wave Lifecycle

| Export | From | Consumer | Status |
|--------|------|----------|--------|
| Queen state persistence pattern | cmd/queen_decision.go | cmd/queen_wave_lifecycle.go (same pattern) | WIRED |

### Phase 97 -> Phase 99: Queen State into Audit

| Export | From | Consumer | Status |
|--------|------|----------|--------|
| `queenStateRead()` | cmd/queen_decision.go:195 | cmd/queen_audit.go:39 | WIRED |
| `QueenStateFile` | cmd/queen_decision.go | cmd/queen_audit.go (schema read) | WIRED |
| `QueenDecision` | cmd/queen_decision.go | cmd/queen_audit.go:40-47 | WIRED |
| `EscalationEntry` | cmd/queen_decision.go | cmd/queen_audit.go:49-57 | WIRED |

### Phase 96 -> Phase 99: Recovery Data into Audit

| Export | From | Consumer | Status |
|--------|------|----------|--------|
| `recoveryLogReadPhase()` | cmd/recovery_classify.go:135 | cmd/queen_audit.go:61 | WIRED |
| `RecoveryLogEntry` | cmd/recovery_classify.go | cmd/queen_audit.go:62-78 | WIRED |

### Phase 98 -> Phase 99: Wave Summary into Audit

| Export | From | Consumer | Status |
|--------|------|----------|--------|
| `readWaveSummary()` | cmd/queen_wave_lifecycle.go:235 | cmd/queen_audit.go:82 | WIRED |
| `WaveLifecycleSummary` | cmd/queen_wave_lifecycle.go | cmd/queen_audit.go:83-102 | WIRED |

### Phase 98 -> Phase 99: Wave Summary into Phase-End Summary

| Export | From | Consumer | Status |
|--------|------|----------|--------|
| `WaveLifecycleSummary` | cmd/queen_wave_lifecycle.go | cmd/queen_phase_summary.go:12 (param type) | WIRED |
| `RecoveryLogFile` | cmd/recovery_classify.go | cmd/queen_phase_summary.go:12 (param type) | WIRED |
| `recoveryLogReadPhase()` | cmd/recovery_classify.go:135 | cmd/queen_phase_summary.go:51 | WIRED |

### Phase 99 -> Build Command: All Output Components Wired

| Export | From | Consumer | Status |
|--------|------|----------|--------|
| `setBuildVerbose()` | cmd/output_filter.go:13 | cmd/codex_build.go:1039 | WIRED |
| `filteredFprintln()` | cmd/output_filter.go:35 | cmd/codex_build.go:1086 | WIRED |
| `queenWaveLifecycle()` | cmd/queen_wave_lifecycle.go:59 | cmd/codex_build.go:1045 | WIRED |
| `writeWaveSummary()` | cmd/queen_wave_lifecycle.go:229 | cmd/codex_build.go:1047 | WIRED |
| `consolidateQueenAudit()` | cmd/queen_audit.go:32 | cmd/codex_build.go:1050 | WIRED |
| `writeAuditFile()` | cmd/queen_audit.go:109 | cmd/codex_build.go:1051 | WIRED |
| `renderPhaseEndSummary()` | cmd/queen_phase_summary.go:49 | cmd/codex_build.go:1054 | WIRED |

## Build Command Post-Wave Hook Chain

Verified in cmd/codex_build.go lines 1039-1054:

```
setBuildVerbose(verbose)                              // Phase 99 (OUT-03)
  -> queenWaveLifecycle(ctx, dispatches, fn, ...)     // Phase 98 (COORD-01)
     -> [internal] orchestrateRecovery() per failed    // Phase 96 (RECV-02)
  -> writeWaveSummary(phase, summary)                  // Phase 98 -> Phase 99
  -> consolidateQueenAudit(phase)                      // Phase 99 (OUT-02)
     -> queenStateRead()                               // Phase 97 (COORD-02)
     -> recoveryLogReadPhase()                         // Phase 94 (RECV-01)
     -> readWaveSummary()                              // Phase 98 (COORD-01)
  -> writeAuditFile(phase, audit)                      // Phase 99 (OUT-02)
  -> renderPhaseEndSummary(summary, phase)             // Phase 99 (OUT-01)
     -> recoveryLogReadPhase()                         // Phase 94 (RECV-01)
```

## Continue Finalize Integration Chain

Verified in cmd/codex_continue_finalize.go lines 183-410:

```
queenStateRead(phase)                                  // Phase 97 (COORD-03)
  -> advisory only (_ = queenAdvisory)
runCodexContinueGates(phase, ...)                      // gate evaluation
autoResolveSoftBlockGates(phase, gates, depth)         // Phase 95 (GATE-03)
  -> [internal] gateClassify() per gate                // Phase 93 (GATE-01)
  -> [internal] autoResolveDepthMultiplier()           // Phase 95 (GATE-04)
  -> gateResultsWritePhase() with QueenAnnotation      // Phase 93 (GATE-05)
  -> recoveryLogWritePhase() for auto-resolved         // Phase 94 (RECV-06)
gateClassify() for remaining soft_block gates          // Phase 93 (GATE-01)
dispatchFixer(phase, "propose")                        // Phase 95 (preserved)
orchestrateRecovery(ctx) for gate failures             // Phase 96 (RECV-02)
  -> [internal] classifyWorkerFailure()                // Phase 94 (RECV-01)
  -> [internal] gateClassify() for tier                // Phase 93 (GATE-01)
  -> [internal] CircuitBreaker check                   // Phase 96 (RECV-03)
  -> recoveryLogWritePhase() for outcomes              // Phase 94 (RECV-06)
  -> persistBudgetToRecoveryLog()                      // Phase 96 (RECV-02)
queenLogEscalation(phase, tripped, rationale)          // Phase 97 (COORD-04)
```

## E2E Flows

### Flow 1: Build with Worker Failures via Queen Wave Lifecycle (COMPLETE)

1. User runs `aether build N`
2. `codex_build.go` calls `setBuildVerbose()` then `queenWaveLifecycle()`
3. `queenWaveLifecycle` dispatches waves via `WaveDispatchFunc` closure
4. Failed workers trigger `orchestrateRecovery()` between waves
5. Recovery outcomes logged to `recovery-log-{N}.json`
6. Wave summary persisted to `wave-summary-{N}.json`
7. Audit consolidated from queen-state, recovery-log, wave-summary
8. Phase-end summary rendered with actions-needed section

**Status: COMPLETE** -- all 8 steps verified through code trace.

### Flow 2: Continue with Soft-Block Gate Failures (COMPLETE)

1. User runs `aether continue` (finalize step)
2. `codex_continue_finalize.go` reads queen-state as advisory (Phase 97)
3. Gate evaluation runs, producing gate report
4. `autoResolveSoftBlockGates()` attempts auto-resolve (Phase 95)
5. Auto-resolved gates get `QueenAnnotation` and re-persisted (Phase 93)
6. Recovery log entries written for auto-resolved gates (Phase 94)
7. Remaining soft_block gates trigger `dispatchFixer()` (Phase 95)
8. `orchestrateRecovery()` evaluates remaining gate failures (Phase 96)
9. `queenLogEscalation()` logs circuit breaker events (Phase 97)

**Status: COMPLETE** -- all 9 steps verified through code trace.

### Flow 3: Plan-Only Queen Decision (COMPLETE)

1. User runs `aether continue` (plan-only step)
2. `codex_continue_plan.go` runs gate evaluation
3. `budgetFromRecoveryLog()` loads recovery budget (Phase 96)
4. `queenDecide()` produces per-gate recommendations (Phase 97)
5. `queenStateWrite()` persists to `queen-state-{N}.json` (Phase 97)
6. Result map includes `queen_decisions` and `queen_state_file` keys

**Status: COMPLETE** -- all 6 steps verified through code trace.

### Flow 4: External Build Finalize with Recovery (BROKEN)

1. External workers complete and submit results via `aether build --finalize`
2. `runCodexBuildFinalize()` merges external results
3. **BREAK**: `runCodexBuildFinalize()` does NOT call `orchestrateRecovery()` for failed dispatches
4. **BREAK**: Result map does NOT include `recovery_instructions`
5. Tests expect recovery_instructions but the function never produces them

**Status: BROKEN at step 3** -- `runCodexBuildFinalize()` in `cmd/codex_build_finalize.go` contains zero references to `orchestrateRecovery`, `RecoveryContext`, `classifyWorkerFailure`, or `budgetFromRecoveryLog`. The 4 build finalize recovery tests all fail:
- `TestBuildFinalize_RecoveryForFailedDispatch` -- FAIL
- `TestBuildFinalize_RecoveryForBlockingDispatch` -- FAIL
- `TestBuildFinalize_BudgetPersisted` -- FAIL
- `TestBuildFinalize_MultipleFailedDispatches` -- FAIL

**Note**: Build recovery DOES work for the direct build path (`aether build N`) via `queenWaveLifecycle` -> `orchestrateRecovery`. The gap is specifically in the external finalize path where workers submit results back asynchronously.

### Flow 5: Phase-End Audit Consolidation (COMPLETE)

1. Build completes, post-wave hook chain fires
2. `consolidateQueenAudit()` reads three source files:
   - `queen-state-{N}.json` via `queenStateRead()` (Phase 97)
   - `recovery-log-{N}.json` via `recoveryLogReadPhase()` (Phase 94)
   - `wave-summary-{N}.json` via `readWaveSummary()` (Phase 98)
3. All entries mapped to `QueenAuditEntry` with decision types
4. Written to `queen-audit-{N}.json` via `writeAuditFile()` (Phase 99)
5. `renderPhaseEndSummary()` reads recovery log and renders actions-needed (Phase 99)

**Status: COMPLETE** -- all 5 steps verified through code trace.

## Detailed Findings

### Blockers

**B-01: runCodexBuildFinalize() missing recovery orchestrator wiring (4 failing tests)**

- File: `/Users/callumcowie/repos/Aether/cmd/codex_build_finalize.go`
- Phase: 96-02 claims "Build finalize flow calls orchestrateRecovery for every failed worker dispatch and includes recovery_instructions in the output"
- Reality: `runCodexBuildFinalize()` (line 146-263) produces a result map (line 247-262) that does NOT include `recovery_instructions`. The function never calls `orchestrateRecovery()`, `classifyWorkerFailure()`, `budgetFromRecoveryLog()`, or any recovery function.
- The 4 tests that validate this behavior all FAIL:
  ```
  TestBuildFinalize_RecoveryForFailedDispatch     -- expects recovery_instructions in result
  TestBuildFinalize_RecoveryForBlockingDispatch    -- expects recovery_instructions in result
  TestBuildFinalize_BudgetPersisted               -- expects budget retries_used=1 after failed dispatch
  TestBuildFinalize_MultipleFailedDispatches       -- expects recovery_instructions in result
  ```
- Recovery IS wired for the direct build path via `queenWaveLifecycle` (Phase 98), so this gap only affects the external finalize path.
- Fix required: Add recovery orchestrator call inside `runCodexBuildFinalize()` after merging external results, before building the result map. Mirror the pattern from `codex_continue_finalize.go` lines 338-393.

### Warnings

**W-01: resolveReferenceSection() is a stub that always returns empty string**

- File: `/Users/callumcowie/repos/Aether/cmd/references.go`
- Created in Phase 99-03 as a stub to fix a pre-existing build break
- Comment says "another agent (99-01 wave 2) provides the full version" but that wave never executed
- Called at `cmd/codex_build.go:1908` -- always returns empty, so skill sections in worker briefs are silently empty
- Not a functional break (code compiles and runs), but skill reference enrichment is non-operational

### Orphaned Exports

None found. All exported functions from all 7 phases are consumed by another phase or by the build/continue commands.

## Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|-------------|-----------------|--------|-------|
| GATE-01 | Phase 93 gateClassify() -> Phase 95 autoResolveSoftBlockGates() -> Phase 96 codex_continue_finalize.go:321 -> Phase 97 queenDecide() | WIRED | -- |
| GATE-02 | Phase 93 gateClassify() + isHardBlockGate() -> Phase 95 shouldAutoResolve() -> Phase 96 gate tier evaluation in finalize | WIRED | -- |
| GATE-05 | Phase 93 QueenAnnotation on GateCheckResult -> Phase 95 annotateGateResult() -> Phase 96 codex_continue_finalize.go:260 | WIRED | -- |
| RECV-01 | Phase 94 classifyWorkerFailure() -> Phase 96 orchestrateRecovery():138 | WIRED | -- |
| RECV-05 | Phase 94 FailureType (transient/systemic) -> Phase 96 RecoveryContext -> Phase 99 audit consolidation decision type mapping | WIRED | -- |
| RECV-06 | Phase 94 recoveryLogWritePhase/ReadPhase -> Phase 95 codex_continue_finalize.go:309,312 -> Phase 96 recovery_orchestrator.go:75,88 -> Phase 99 queen_audit.go:61 | WIRED | -- |
| GATE-03 | Phase 95 autoResolveSoftBlockGates() -> codex_continue_finalize.go:240 -> annotation persistence + recovery log | WIRED | -- |
| GATE-04 | Phase 95 autoResolveDepthMultiplier() -> autoResolveSoftBlockGates():733 -> depth-based threshold adjustment | WIRED | -- |
| RECV-02 | Phase 96 orchestrateRecovery() -> queen_wave_lifecycle.go:151 -> codex_continue_finalize.go:372 | PARTIAL | Build finalize path (runCodexBuildFinalize) does not call orchestrateRecovery -- 4 tests fail |
| RECV-03 | Phase 96 RecoveryContext.Dispatches -> findSameCastePeer():339 -> peer redistribution in orchestrator | WIRED | -- |
| RECV-04 | Phase 96 dispatchFixer preserved in codex_continue_finalize.go:330 -> orchestrateRecovery provides recovery context | WIRED | -- |
| COORD-02 | Phase 97 queenDecide() -> codex_continue_plan.go:108 -> queenStateWrite():115 -> result map keys queen_decisions/queen_state_file | WIRED | -- |
| COORD-03 | Phase 97 queenStateRead() -> codex_continue_finalize.go:183 -> advisory context (informational only, D-09) | WIRED | -- |
| COORD-04 | Phase 97 queenLogEscalation() -> codex_continue_finalize.go:399 -> circuit breaker escalation events logged | WIRED | -- |
| COORD-01 | Phase 98 queenWaveLifecycle() -> codex_build.go:1045 -> WaveDispatchFunc closure wraps dispatchCodexBuildWorkers | WIRED | -- |
| OUT-01 | Phase 99 renderPhaseEndSummary() -> codex_build.go:1054 -> reads recoveryLogReadPhase, renders actions-needed | WIRED | -- |
| OUT-02 | Phase 99 consolidateQueenAudit() -> codex_build.go:1050 -> reads queen-state + recovery-log + wave-summary -> writeAuditFile | WIRED | -- |
| OUT-03 | Phase 99 setBuildVerbose() -> codex_build.go:1039 -> filteredFprintln for RawOutput at codex_build.go:1086 | WIRED | -- |

**Requirements with no cross-phase wiring:** None. All 19 requirements have cross-phase integration paths.

## Self-Contained Requirements

No requirements exist entirely within a single phase without integration touchpoints. Every requirement in this milestone contributes to or consumes from at least one other phase.

## Test Results

```
PASS: TestOrchestrateRecovery_* (14 tests)
PASS: TestContinueFinalize_GateRecovery* (4 tests)
PASS: TestPlanOnly* (7 tests)
PASS: TestFinalize* (7 tests -- advisory context, escalation)
PASS: TestQueenWaveLifecycle_* (15 tests)
PASS: TestQueenAudit_* (9 tests)
PASS: TestPhaseEndSummary / renderActionsNeeded (8 tests)
PASS: TestOutputFilter / setBuildVerbose (8 tests)
PASS: TestAutoResolve* (11 tests)
PASS: TestGateClassifications* (10 tests)
PASS: TestFailureClassifications* (20 tests)
PASS: TestRecoveryBudget* (4 tests)
PASS: TestCircuitBreaker* (14 tests)

FAIL: TestBuildFinalize_RecoveryForFailedDispatch
FAIL: TestBuildFinalize_RecoveryForBlockingDispatch
FAIL: TestBuildFinalize_BudgetPersisted
FAIL: TestBuildFinalize_MultipleFailedDispatches
```

## Summary

The v1.14 Queen Authority milestone is **partially wired**. The main build path (via queen wave lifecycle) and continue finalize path are fully integrated across all 7 phases. However, the **external build finalize path** (`runCodexBuildFinalize`) is missing recovery orchestrator wiring, causing 4 test failures. The 96-02 SUMMARY claimed this wiring existed, but the implementation was never added to `codex_build_finalize.go`.

The build command post-wave hook chain and continue finalize chain are complete end-to-end. The audit consolidation reads all three source files correctly. The only functional gap is recovery in the external finalize path.

---
*Milestone: v1.14 Queen Authority*
*Phases: 93-99*
*Status: PARTIAL*
*Blockers: 1 (build finalize recovery wiring -- 4 failing tests)*
*Warnings: 1 (resolveReferenceSection stub)*
