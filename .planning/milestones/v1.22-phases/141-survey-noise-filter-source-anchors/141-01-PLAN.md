---
phase: 141-survey-noise-filter-source-anchors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/codegraph/scan_filter.go
  - pkg/codegraph/scan_filter_test.go
  - pkg/codegraph/codegraph.go
  - pkg/codegraph/codegraph_test.go
  - cmd/codex_colonize.go
  - cmd/codex_colonize_test.go
  - cmd/init_research.go
  - cmd/skills.go
  - cmd/discuss_analyze.go
autonomous: true
requirements:
  - GROUND-01
  - GROUND-02
  - GROUND-05

must_haves:
  truths:
    - "All five call sites (codegraph Scan, surveyWorkspace, init_research, skills, discuss_analyze) use ShouldSkipDir from scan_filter.go"
    - "ShouldSkipDir returns true for .venv, __pycache__, site-packages, .pytest_cache, .tox, .mypy_cache, node_modules, .gradle, .cache, target"
    - "No local skip-list maps (dirsToSkip, shouldSkipSurveyDir, extendedSkipDirs, skillScanSkipDirs) remain in the codebase"
    - "A fixture with .venv/site-packages noise produces zero .venv references in survey output"
  artifacts:
    - path: "pkg/codegraph/scan_filter.go"
      provides: "Canonical ScanFilter with ShouldSkipDir, ShouldSkipFile, NoiseDirCount"
      exports: ["ShouldSkipDir", "ShouldSkipFile", "NoiseDirCount"]
    - path: "pkg/codegraph/scan_filter_test.go"
      provides: "Filter unit tests, extended coverage tests, divergence detection test"
      contains: "TestShouldSkipDir"
    - path: "pkg/codegraph/codegraph.go"
      provides: "Scan function using shared filter"
      contains: "ShouldSkipDir"
    - path: "cmd/codex_colonize.go"
      provides: "surveyWorkspace using shared filter, shouldSkipSurveyDir deleted"
      contains: "codegraph.ShouldSkipDir"
  key_links:
    - from: "pkg/codegraph/codegraph.go"
      to: "pkg/codegraph/scan_filter.go"
      via: "import and call ShouldSkipDir"
      pattern: "codegraph\\.ShouldSkipDir"
    - from: "cmd/codex_colonize.go"
      to: "pkg/codegraph/scan_filter.go"
      via: "import and call codegraph.ShouldSkipDir"
      pattern: "codegraph\\.ShouldSkipDir"
    - from: "cmd/init_research.go"
      to: "pkg/codegraph/scan_filter.go"
      via: "import and call codegraph.ShouldSkipDir"
      pattern: "codegraph\\.ShouldSkipDir"
    - from: "cmd/skills.go"
      to: "pkg/codegraph/scan_filter.go"
      via: "import and call codegraph.ShouldSkipDir"
      pattern: "codegraph\\.ShouldSkipDir"
---

<objective>
Create a single canonical ScanFilter in pkg/codegraph/scan_filter.go and migrate all five divergent skip-list call sites to use it. This is the root-cause fix for generic plans -- the survey currently trusts dependency/cache paths instead of repo-owned source files because each subsystem has a different, incomplete exclusion list.

Purpose: One source of truth for directory exclusion prevents the .venv bug class from recurring. If a noise directory is missing, it gets added once in scan_filter.go and all callers benefit immediately.

Output: scan_filter.go (new), scan_filter_test.go (new), codegraph.go (modified), codex_colonize.go (modified), init_research.go (modified), skills.go (modified), discuss_analyze.go (modified), test files updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-survey-noise-filter-source-anchors/141-RESEARCH.md
@pkg/codegraph/codegraph.go
@pkg/codegraph/codegraph_test.go
@cmd/codex_colonize.go
@cmd/codex_colonize_test.go
@cmd/init_research.go
@cmd/skills.go
@cmd/discuss_analyze.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create canonical ScanFilter and tests, migrate codegraph.go</name>
  <files>pkg/codegraph/scan_filter.go, pkg/codegraph/scan_filter_test.go, pkg/codegraph/codegraph.go, pkg/codegraph/codegraph_test.go</files>
  <read_first>
    - pkg/codegraph/codegraph.go (lines 70-77 for dirsToSkip map, lines 497-524 for Scan function that uses dirsToSkip)
    - pkg/codegraph/codegraph_test.go (lines 334-357 for TestDirsToSkip pattern to replicate)
    - cmd/skills.go (lines 150-172 for the most complete existing skip list -- 23 entries with struct{} values)
    - .planning/phases/141-survey-noise-filter-source-anchors/141-RESEARCH.md (Code Examples section with the canonical filter design)
  </read_first>
  <behavior>
    - TestShouldSkipDir: for each directory name in the canonical noiseDirs map, ShouldSkipDir(name) returns true
    - TestShouldSkipDir_Negative: for names like "src", "cmd", "pkg", "internal", "env", ShouldSkipDir returns false
    - TestShouldSkipDir_ExtendedCoverage: specifically assert .venv, __pycache__, site-packages, .pytest_cache, .tox, .mypy_cache, node_modules, .gradle, .cache, .cargo, target are all true
    - TestShouldSkipFile: .pyc, .pyo, .so, .dylib, .dll, .map extensions return true; .go, .ts, .py, .js return false
    - TestShouldSkipFile_CompoundExtensions: files ending in .min.js, .min.css, .bundle.js, .d.ts return true
    - TestShouldSkipDir_NoBareEnv: ShouldSkipDir("env") returns false (prevents over-exclusion per Pitfall 8)
    - TestNoiseDirCount: NoiseDirCount() returns a value >= 25 (the canonical map should have at least 25 entries)
  </behavior>
  <action>
    Create pkg/codegraph/scan_filter.go as the single source of truth for directory and file exclusion. The canonical noiseDirs map must contain every entry from the RESEARCH.md Code Examples section (version control, Python, Node.js, Go, Rust, build artifacts, caches, Aether-managed, IDE/editor, temp -- at minimum 27 entries). Also add .cargo to the map (Rust package cache). Do NOT include bare "env" (without leading dot) -- Pitfall 8 warns this breaks repos with env/ source directories.

    Export three functions: ShouldSkipDir(name string) bool (map lookup), ShouldSkipFile(name string) bool (compound suffixes first, then extension lookup in noiseFileExts), and NoiseDirCount() int (returns len(noiseDirs), used by divergence tests).

    The noiseFileExts map should contain: .pyc, .pyo, .so, .dylib, .dll, .map. The compoundNoiseSuffixes slice should contain: .min.js, .min.css, .bundle.js, .d.ts.

    Then create pkg/codegraph/scan_filter_test.go with the seven test functions listed in the behavior block. Use table-driven subtests for ShouldSkipDir coverage.

    Then modify codegraph.go: delete the dirsToSkip variable declaration (lines 72-77), and in the Scan function (line 516) replace `if dirsToSkip[d.Name()]` with `if ShouldSkipDir(d.Name())`. No other changes to codegraph.go.

    Then modify codegraph_test.go: rename TestDirsToSkip to TestScanSkipsNoiseDirs (or update its internals) to verify that Scan still skips noise directories using the shared filter. The test should still create a repo with node_modules, .git, vendor subdirectories and assert those are skipped.
  </action>
  <verify>
    <automated>go test ./pkg/codegraph/ -run "TestShouldSkip|TestNoiseDirCount|TestScanSkips" -v -count=1</automated>
  </verify>
  <done>
    - scan_filter.go exports ShouldSkipDir, ShouldSkipFile, NoiseDirCount
    - noiseDirs map has 27+ entries including .venv, __pycache__, site-packages, .pytest_cache, .tox, .mypy_cache, .gradle, .cargo
    - ShouldSkipDir("env") returns false
    - codegraph.go has zero references to dirsToSkip
    - All existing codegraph tests pass
    - All 7 new test functions in scan_filter_test.go pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate four remaining call sites and add regression test</name>
  <files>cmd/codex_colonize.go, cmd/codex_colonize_test.go, cmd/init_research.go, cmd/skills.go, cmd/discuss_analyze.go</files>
  <read_first>
    - cmd/codex_colonize.go (lines 452-453 and 522-523 for shouldSkipSurveyDir usage, lines 976-982 for the function definition to delete)
    - cmd/init_research.go (lines 147-162 for extendedSkipDirs map, line 1870 for its single usage in WalkDir)
    - cmd/skills.go (lines 150-172 for skillScanSkipDirs map, lines 1293 and 1339 for its two usage sites)
    - cmd/discuss_analyze.go (line 131 for extendedSkipDirs usage from init_research.go)
    - cmd/codex_colonize_test.go (lines 18-57 for createDockerSurveyFixture pattern, lines 797-837 for TestSurveyWorkspaceDockerRepoIgnoresPlatformGuidance pattern)
    - pkg/codegraph/scan_filter.go (created in Task 1 -- to import codegraph package)
  </read_first>
  <behavior>
    - TestVenvNoiseExclusion: create a fixture with .venv/lib/site-packages/requests/api.py and cmd/main.go; surveyWorkspace returns zero paths containing ".venv" and zero paths containing "site-packages"
    - TestVenvNoiseExclusion_SourceFilesPreserved: same fixture includes src/app.py; surveyWorkspace returns a path containing "src/app.py" (proves source files are NOT excluded)
    - TestSkipListDivergence: grep cmd/*.go for any remaining local skip-list pattern (map[string]bool or map[string]struct{} assigned to a variable whose name contains "skip" or "Skip"); assert zero matches excluding the test file itself
  </behavior>
  <action>
    Migrate cmd/codex_colonize.go: delete the shouldSkipSurveyDir function (lines 976-982). At the two call sites (line 453 and line 522), replace `shouldSkipSurveyDir(name)` with `codegraph.ShouldSkipDir(name)` and `shouldSkipSurveyDir(d.Name())` with `codegraph.ShouldSkipDir(d.Name())`. Add import of "github.com/aethervoices/aether/pkg/codegraph" if not already present.

    Migrate cmd/init_research.go: delete the extendedSkipDirs variable declaration (lines 147-162). At the call site (line 1870), replace `extendedSkipDirs[d.Name()]` with `codegraph.ShouldSkipDir(d.Name())`. Add import of the codegraph package. Note the existing guard `&& path != target` must be preserved.

    Migrate cmd/skills.go: delete the skillScanSkipDirs variable declaration (lines 150-172 plus any closing brace). At the two call sites (lines 1293 and 1339), replace the map lookup `_, skip := skillScanSkipDirs[d.Name()]` with `codegraph.ShouldSkipDir(d.Name())` and `_, skip := skillScanSkipDirs[part]` with `codegraph.ShouldSkipDir(part)`. Add import of the codegraph package.

    cmd/discuss_analyze.go requires no changes because it references extendedSkipDirs from init_research.go -- once that variable is deleted, the compiler will catch it. Verify the import path resolves and the single usage at line 131 now uses codegraph.ShouldSkipDir.

    Wait -- discuss_analyze.go imports and uses extendedSkipDirs directly from the same package (cmd). So the variable deletion in init_research.go will break discuss_analyze.go. Fix discuss_analyze.go line 131: replace `extendedSkipDirs[d.Name()]` with `codegraph.ShouldSkipDir(d.Name())`. Add the codegraph import to discuss_analyze.go as well.

    Then add regression tests to cmd/codex_colonize_test.go. Create a helper function createVenvNoiseFixture that builds a temp directory tree with: .venv/lib/site-packages/requests/api.py (noise), cmd/main.go (source), src/app.py (source), __pycache__/cache.pyc (noise). Write TestVenvNoiseExclusion and TestVenvNoiseExclusion_SourceFilesPreserved as described in the behavior block.

    Add TestSkipListDivergence to cmd/codex_colonize_test.go that uses filepath.Glob on cmd/*.go, reads each file, and asserts no line matches the pattern `var\s+\w*[Ss]kip\w*\s*=\s*map\[string\]` unless the file is the test file itself. This prevents anyone from re-introducing a local skip list.
  </action>
  <verify>
    <automated>go test ./pkg/codegraph/ ./cmd/ -run "TestVenvNoise|TestSkipListDivergence" -v -count=1 && go vet ./cmd/ ./pkg/codegraph/</automated>
  </verify>
  <done>
    - shouldSkipSurveyDir function deleted from codex_colonize.go
    - extendedSkipDirs variable deleted from init_research.go
    - skillScanSkipDirs variable deleted from skills.go
    - All four files (codex_colonize.go, init_research.go, skills.go, discuss_analyze.go) import codegraph and call codegraph.ShouldSkipDir
    - discuss_analyze.go compiles (no stale reference to deleted extendedSkipDirs)
    - TestVenvNoiseExclusion passes: zero .venv or site-packages paths in survey output
    - TestVenvNoiseExclusion_SourceFilesPreserved passes: src/app.py is present
    - TestSkipListDivergence passes: no local skip-list maps remain in cmd/
    - go vet passes on all modified packages
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scan filter inputs | Directory names passed to ShouldSkipDir come from filesystem WalkDir -- untrusted but bounded to directory names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-01 | Tampering | scan_filter.go noiseDirs map | accept | Map is hardcoded in compiled Go binary; not user-configurable. No runtime modification possible. |
| T-141-02 | Information Disclosure | Survey output | accept | Survey already scans full repo; filter only reduces scope, never adds data. |
</threat_model>

<verification>
go test ./pkg/codegraph/ ./cmd/ -count=1 -v
go vet ./pkg/codegraph/ ./cmd/
grep -rn "dirsToSkip\|shouldSkipSurveyDir\|extendedSkipDirs\|skillScanSkipDirs" cmd/ pkg/codegraph/ --include="*.go" | grep -v "_test.go" | grep -v "// " | grep -v "scan_filter.go"
</verification>

<success_criteria>
- ShouldSkipDir and ShouldSkipFile exported from pkg/codegraph/scan_filter.go
- All five call sites import codegraph and use ShouldSkipDir
- Zero local skip-list variables remain in cmd/ (verified by grep and TestSkipListDivergence)
- ShouldSkipDir(".venv") == true, ShouldSkipDir("env") == false
- TestVenvNoiseExclusion passes: fixture with .venv noise produces zero .venv paths
- All existing Go tests pass: go test ./... -count=1
</success_criteria>

<output>
After completion, create `.planning/phases/141-survey-noise-filter-source-anchors/141-01-SUMMARY.md`
</output>
