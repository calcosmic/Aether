---
phase: 142-grounding-gate-decision-binding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/plan_grounding.go
  - cmd/plan_grounding_test.go
  - cmd/codex_plan_finalize.go
  - cmd/codex_plan.go
autonomous: true
requirements: [GROUND-06, GROUND-07]

must_haves:
  truths:
    - "Plan-finalize emits a grounding warning when source anchors exist but zero non-research tasks contain file/path references"
    - "Research/architecture phases are exempt from grounding checks"
    - "Grounding warning does not block plan-finalize -- it is soft, not an error"
    - "Planner worker brief mentions source anchors when available"
  artifacts:
    - path: "cmd/plan_grounding.go"
      provides: "checkPlanGrounding, isGroundedTask, isResearchPhase, planGroundingWarning"
      exports: ["checkPlanGrounding", "isGroundedTask", "isResearchPhase", "planGroundingWarning"]
    - path: "cmd/plan_grounding_test.go"
      provides: "Unit tests for grounding gate"
      contains: "TestCheckPlanGrounding"
    - path: "cmd/codex_plan_finalize.go"
      provides: "Integration call to grounding gate in runCodexPlanFinalize"
      contains: "checkPlanGrounding"
    - path: "cmd/codex_plan.go"
      provides: "Source anchor mention in planner worker brief"
      contains: "Source anchors available"
  key_links:
    - from: "cmd/codex_plan_finalize.go"
      to: "cmd/plan_grounding.go"
      via: "checkPlanGrounding(phases, manifest.Survey.SourceAnchors)"
      pattern: "checkPlanGrounding"
    - from: "cmd/codex_plan.go"
      to: "codexSurveyContext.SourceAnchors"
      via: "renderPlanningWorkerBrief anchor section"
      pattern: "Source anchors available"
---

<objective>
Create the plan-grounding validation gate and wire it into plan-finalize.

Purpose: Detect generic plans that ignore concrete repo files. When source anchors exist (from Phase 141's survey pipeline) but plan tasks contain zero file/path references, emit a soft warning. This catches the root cause of vague plans before they reach builders.

Output: New plan_grounding.go with checkPlanGrounding + tests, grounding gate integration in codex_plan_finalize.go, and planner brief anchor hint in codex_plan.go.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-survey-noise-filter-source-anchors/141-02-SUMMARY.md
@.planning/phases/142-grounding-gate-decision-binding/142-RESEARCH.md

<interfaces>
<!-- Colony types the executor must use -->

From pkg/colony/colony.go:
```go
type Phase struct {
    ID              int       `json:"id"`
    Name            string    `json:"name"`
    Description     string    `json:"description"`
    Status          string    `json:"status"`
    Mode            PhaseMode `json:"mode,omitempty"`
    Tasks           []Task    `json:"tasks"`
    SuccessCriteria []string  `json:"success_criteria"`
    WatcherFailureCount int   `json:"watcher_failure_count,omitempty"`
}

type Task struct {
    ID              *string  `json:"id"`
    Goal            string   `json:"goal"`
    Status          string   `json:"status"`
    Constraints     []string `json:"constraints,omitempty"`
    Hints           []string `json:"hints,omitempty"`
    SuccessCriteria []string `json:"success_criteria,omitempty"`
    DependsOn       []string `json:"depends_on,omitempty"`
}
```

From cmd/codex_plan.go (codexSurveyContext):
```go
type codexSurveyContext struct {
    SurveyDir        string
    SurveyDocs       []string
    Languages        []string
    Frameworks       []string
    Directories      []string
    EntryPoints      []string
    Dependencies     []string
    TestFiles        []string
    Issues           []string
    SecurityPatterns []string
    SourceAnchors    []string
}
```

From cmd/codex_plan.go (codexPlanManifest):
```go
Survey codexSurveyContext `json:"survey"`
```

From cmd/codex_plan_finalize.go runCodexPlanFinalize (integration point):
```
Line 172: phases := buildWorkerPlanPhases(*phasePlan)
Line 173: if len(phases) == 0 || buildablePlanTaskCount(phases) == 0 { ... }
Line 274: result := map[string]interface{}{ ... }
Line 300: "planning_warning": provenance.PlanningWarning,
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create plan_grounding.go with checkPlanGrounding and unit tests</name>
  <files>cmd/plan_grounding.go, cmd/plan_grounding_test.go, cmd/codex_plan_finalize.go</files>
  <read_first>
    cmd/codex_plan_finalize.go (full file -- need integration point at line 172 and result map at line 274)
    pkg/colony/colony.go (Phase and Task struct definitions at lines 522-542)
  </read_first>
  <behavior>
    - TestCheckPlanGrounding_NoAnchors: when sourceAnchors is empty/nil, returns nil (no warnings)
    - TestCheckPlanGrounding_AnchorsWithFileRefs: tasks containing paths like "cmd/main.go" or "pkg/storage/store.go" in Goal/Constraints/Hints produce no warnings
    - TestCheckPlanGrounding_AnchorsNoFileRefs: when anchors exist but no tasks have file-path-like strings in Goal/Constraints/Hints, returns warning for that phase
    - TestCheckPlanGrounding_ResearchPhaseExempt: phase names containing "research", "survey", "architecture", or "design" produce no warnings regardless of task content
    - TestCheckPlanGrounding_MixedPhases: one grounded phase + one ungrounded phase = warning only for the ungrounded phase
    - TestIsGroundedTask_FileInGoal: task with "Edit cmd/main.go" in Goal returns true
    - TestIsGroundedTask_FileInConstraints: task with "Must not modify pkg/storage/store.go" in Constraints returns true
    - TestIsGroundedTask_FileInHints: task with "See cmd/skills.go for pattern" in Hints returns true
    - TestIsGroundedTask_NoFileRef: task with "Set up the authentication system" returns false
    - TestIsResearchPhase: names "research", "Architecture Review", "Survey Cleanup" return true; "User Auth", "Build API" return false
    - TestPlanFinalize_GroundingSoftGate: integration test verifying runCodexPlanFinalize returns success with grounding_warnings in result when phases are ungrounded
  </behavior>
  <action>
    Create cmd/plan_grounding.go in package cmd with:

    1. planGroundingWarning struct with PhaseID int, PhaseName string, AnchorCount int, UngroundedTasks int fields (all json-tagged).

    2. checkPlanGrounding(phases []colony.Phase, sourceAnchors []string) []planGroundingWarning -- pure function. Returns nil when sourceAnchors is empty (no anchors to ground against). Iterates phases, skips those matching isResearchPhase. For each non-research phase, counts tasks where isGroundedTask returns true. If groundedCount == 0 and len(phase.Tasks) > 0, appends a warning. Returns warnings slice.

    3. isGroundedTask(task colony.Task) bool -- checks Goal, joined Constraints, joined Hints for file-path patterns using regex. Also checks individual Hints via looksLikeFile for extension-only matches like "main.go".

    4. filePathPattern regex: match strings with at least one "/" followed by filename with optional extension (e.g., "cmd/main.go", "pkg/storage/store.go"). Pattern: require at least one path separator and a filename component. Must be anchored to word boundaries (space, quote, backtick, paren, or start/end of string) to avoid false positives on English text.

    5. isResearchPhase(name string) bool -- case-insensitive check against keywords: "research", "survey", "architecture", "design", "planning", "discovery".

    6. looksLikeFile(s string) bool -- checks for common source extensions: .go, .ts, .js, .py, .rs, .java, .yaml, .json, .toml, .md.

    Then modify cmd/codex_plan_finalize.go in runCodexPlanFinalize:

    After line 174 (after the buildablePlanTaskCount check), add grounding check:
    groundingWarnings := checkPlanGrounding(phases, manifest.Survey.SourceAnchors)

    In the result map construction (around line 274), after "planning_warning" entry, add:
    - If groundingWarnings is non-nil, set result["grounding_warnings"] to groundingWarnings and append a formatted warning string to the existing "planning_warning" value (join with "; " if provenance.PlanningWarning is non-empty).

    The warning format per phase: "Phase {id} ({name}): {count} tasks with no file references ({anchors} anchors available)"

    CRITICAL: The grounding gate is SOFT. It must never return an error. It only adds warnings to the result map. The plan-finalize continues regardless.

    Create cmd/plan_grounding_test.go with all tests listed in the behavior block. Use colony.Phase and colony.Task structs directly. No mocks needed -- checkPlanGrounding is a pure function.
  </action>
  <verify>
    <automated>go test ./cmd/ -run "TestCheckPlanGrounding|TestIsGroundedTask|TestIsResearchPhase|TestPlanFinalize_GroundingSoftGate" -v -count=1</automated>
  </verify>
  <done>
    - cmd/plan_grounding.go exists with checkPlanGrounding, isGroundedTask, isResearchPhase, looksLikeFile, planGroundingWarning
    - cmd/plan_grounding_test.go exists with 11+ test functions covering all behaviors
    - cmd/codex_plan_finalize.go calls checkPlanGrounding after building phases and adds grounding_warnings to result map
    - All new tests pass
    - Grounding warnings never block plan-finalize (soft gate verified by TestPlanFinalize_GroundingSoftGate)
    - go vet ./cmd/ passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Add source anchor hint to planner worker brief</name>
  <files>cmd/codex_plan.go</files>
  <read_first>
    cmd/codex_plan.go (lines 1533-1636, renderPlanningWorkerBrief function)
  </read_first>
  <action>
    In renderPlanningWorkerBrief (cmd/codex_plan.go line 1533), add a source anchors hint to the brief.

    After the "Repo inspection rule" line (around line 1568), add conditional section:
    If survey.SourceAnchors is non-empty, append a line: "- Source anchors available: {len(survey.SourceAnchors)} repo-owned files from survey. Prefer referencing these files in task goals."
    If survey.SourceAnchors is empty, do nothing (no clutter).

    This tells the Route-Setter planner about available anchors so it can reference concrete files in its phase plans. The anchors come from the survey pipeline built in Phase 141 (extractSourceAnchors -> anchors.json -> codexSurveyContext.SourceAnchors).

    Also add a test in cmd/codex_plan_test.go: TestRenderPlanningWorkerBrief_SourceAnchors that verifies the anchor line appears when SourceAnchors has entries, and does not appear when SourceAnchors is empty. Use the existing test patterns in that file for renderPlanningWorkerBrief tests.
  </action>
  <verify>
    <automated>go test ./cmd/ -run "TestRenderPlanningWorkerBrief_SourceAnchors" -v -count=1</automated>
  </verify>
  <done>
    - renderPlanningWorkerBrief emits "Source anchors available: N repo-owned files" when survey.SourceAnchors is non-empty
    - No line emitted when survey.SourceAnchors is empty
    - Test passes verifying both conditions
    - go vet ./cmd/ passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan-finalize input -> grounding gate | Completion file and manifest come from external wrapper; task text is LLM-generated and may contain crafted patterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Tampering | checkPlanGrounding | accept | Grounding gate is soft-only; no security impact from crafted input |
| T-142-02 | Information Disclosure | grounding_warnings in result | accept | Warning exposes phase names and anchor counts; no sensitive data |
</threat_model>

<verification>
go test ./cmd/ -run "TestCheckPlanGrounding|TestIsGroundedTask|TestIsResearchPhase|TestPlanFinalize_GroundingSoftGate|TestRenderPlanningWorkerBrief_SourceAnchors" -v -count=1
go vet ./cmd/
</verification>

<success_criteria>
- checkPlanGrounding returns warnings when anchors exist but no tasks have file references
- Research/architecture phases are exempt
- Grounding gate is soft (never returns error, only adds warnings)
- Planner brief mentions source anchors when available
- All tests pass, go vet clean
</success_criteria>

<output>
After completion, create `.planning/phases/142-grounding-gate-decision-binding/142-01-SUMMARY.md`
</output>
