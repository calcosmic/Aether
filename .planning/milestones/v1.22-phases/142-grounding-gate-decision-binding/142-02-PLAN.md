---
phase: 142-grounding-gate-decision-binding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/discuss.go
  - cmd/discuss_test.go
autonomous: true
requirements: [GROUND-10]

must_haves:
  truths:
    - "Contradictory resolved decisions emit FEEDBACK pheromones during discuss resolution"
    - "Non-contradictory decisions emit no conflicts"
    - "Conflict detection does not block the resolution -- it only emits warnings"
  artifacts:
    - path: "cmd/discuss.go"
      provides: "detectDecisionConflicts function with contradiction keyword pairs"
      contains: "detectDecisionConflicts"
    - path: "cmd/discuss_test.go"
      provides: "Unit tests for conflict detection"
      contains: "TestDetectDecisionConflicts"
  key_links:
    - from: "cmd/discuss.go resolveDiscussQuestion"
      to: "cmd/discuss.go detectDecisionConflicts"
      via: "called after decision resolution, before return"
      pattern: "detectDecisionConflicts"
    - from: "cmd/discuss.go detectDecisionConflicts"
      to: "cmd/discuss.go createPheromoneSignal"
      via: "FEEDBACK emission for each conflict"
      pattern: "createPheromoneSignal.*FEEDBACK"
---

<objective>
Add decision conflict detection to the discuss resolution pipeline.

Purpose: When resolved decisions contradict each other (e.g., "use PostgreSQL" vs "keep serverless"), the system emits a FEEDBACK pheromone so planners and builders are aware of the tension. This prevents silently building toward conflicting goals.

Output: detectDecisionConflicts function in discuss.go, called from resolveDiscussQuestion, with FEEDBACK pheromone emission and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-grounding-gate-decision-binding/142-RESEARCH.md

<interfaces>
<!-- Discuss structures the executor must use -->

From cmd/discuss.go (resolveDiscussQuestion, lines 198-269):
```go
func resolveDiscussQuestion(id, answer string) (map[string]interface{}, error)
```
Integration point: after line 243 (after REDIRECT emission), before the tracer block at line 245.

From cmd/pending_decision.go:
```go
type PendingDecision struct {
    ID          string `json:"id"`
    Type        string `json:"type,omitempty"`
    Description string `json:"description"`
    Phase       *int   `json:"phase,omitempty"`
    Source      string `json:"source,omitempty"`
    SessionID   string `json:"session_id,omitempty"`
    GoalHash    string `json:"goal_hash,omitempty"`
    Resolution  string `json:"resolution,omitempty"`
    Resolved    bool   `json:"resolved"`
    CreatedAt   string `json:"created_at"`
    ResolvedAt  string `json:"resolved_at,omitempty"`
}

type PendingDecisionFile struct {
    Decisions []PendingDecision `json:"decisions"`
}
```

From cmd/discuss.go (existing pheromone emission pattern):
```go
createPheromoneSignal("REDIRECT", redirectText, "discuss", "resolved clarification", "", 1.0, "high")
```
Conflict emission should use: createPheromoneSignal("FEEDBACK", conflictText, "discuss", "decision conflict detection", "", 0.5, "low")

From cmd/pending_decision.go:
```go
const pendingDecisionsFile = "pending-decisions.json"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add detectDecisionConflicts to discuss.go with tests</name>
  <files>cmd/discuss.go, cmd/discuss_test.go</files>
  <read_first>
    cmd/discuss.go (full file, focus on resolveDiscussQuestion at lines 198-269 and the pheromone emission pattern at lines 235-243)
    cmd/discuss_test.go (existing test patterns to match style)
    cmd/pending_decision.go (PendingDecision and PendingDecisionFile structs at lines 11-28)
  </read_first>
  <behavior>
    - TestDetectDecisionConflicts_NoDecisions: empty or single resolved decision returns nil
    - TestDetectDecisionConflicts_NoConflict: two resolved decisions with compatible resolutions (e.g., "use Go" + "add tests") return nil
    - TestDetectDecisionConflicts_DatabaseConflict: "use PostgreSQL" + "keep serverless" returns conflict string containing "database"
    - TestDetectDecisionConflicts_ArchitectureConflict: "build monolith" + "use microservices" returns conflict string containing "architecture"
    - TestDetectDecisionConflicts_FrontendConflict: "use React" + "use Vue" returns conflict string containing "frontend"
    - TestDetectDecisionConflicts_MultipleConflicts: three decisions with two conflicts returns both conflict strings
    - TestDetectDecisionConflicts_UnresolvedIgnored: only resolved decisions are checked; unresolved ones are skipped
    - TestDetectDecisionConflicts_EmptyResolutionIgnored: resolved decisions with blank Resolution are skipped
  </behavior>
  <action>
    Add to cmd/discuss.go:

    1. contradictionPairs variable -- slice of structs with positive string, negative string, category string. Conservative pairs covering the most common contradictions:
       - ("postgresql", "serverless", "database")
       - ("mysql", "serverless", "database")
       - ("sqlite", "serverless", "database")
       - ("monolith", "microservice", "architecture")
       - ("react", "vue", "frontend")
       - ("react", "svelte", "frontend")
       - ("rest", "graphql", "api")
       - ("docker", "no docker", "deployment")
       - ("kubernetes", "no kubernetes", "deployment")

    2. detectDecisionConflicts(decisions []PendingDecision) []string -- function that:
       - Extracts resolved decision Resolution text (lowercased), skipping unresolved or empty-resolution entries
       - Returns nil if fewer than 2 resolutions
       - For each contradictionPair, checks if any resolution contains positive AND any resolution contains negative
       - Appends formatted conflict string: "Possible {category} conflict: decisions reference both '{positive}' and '{negative}'"
       - Returns conflict slice (nil if empty)

    3. In resolveDiscussQuestion, after the REDIRECT emission block (line 243), add conflict detection:
       Load the updated decision file (file variable already in scope from line 231), call detectDecisionConflicts(file.Decisions). For each conflict, call createPheromoneSignal("FEEDBACK", conflict, "discuss", "decision conflict detection", "", 0.5, "low"). Ignore errors from pheromone creation (non-blocking).

    Add tests to cmd/discuss_test.go matching the behavior block. Use PendingDecision structs directly -- no mocks. detectDecisionConflicts is a pure function that only reads decision Resolution text.

    CRITICAL: Conflict detection is non-blocking. It must not prevent the resolution from completing. It only emits FEEDBACK pheromones as warnings.

    IMPORTANT: Use "nosql" as a single compound word if adding a sql/nosql pair (not "no" + "sql" which would false-match on negation). Keep the pairs list conservative -- only genuinely contradictory terms.
  </action>
  <verify>
    <automated>go test ./cmd/ -run "TestDetectDecisionConflicts" -v -count=1</automated>
  </verify>
  <done>
    - detectDecisionConflicts function exists in cmd/discuss.go with contradictionPairs
    - resolveDiscussQuestion calls detectDecisionConflicts after resolution and emits FEEDBACK pheromones for conflicts
    - 8+ test functions pass covering all behaviors
    - Conflict detection never blocks resolution
    - go vet ./cmd/ passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-generated decisions -> conflict detection | Decision resolutions come from LLM output; keyword matching is inherently imprecise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-03 | Tampering | detectDecisionConflicts | accept | Keyword matching is conservative; false negatives are acceptable (missed conflicts), false positives are low-impact (extra FEEDBACK signal) |
</threat_model>

<verification>
go test ./cmd/ -run "TestDetectDecisionConflicts" -v -count=1
go vet ./cmd/
</verification>

<success_criteria>
- detectDecisionConflicts identifies contradictory keyword pairs in resolved decisions
- FEEDBACK pheromones emitted for each conflict during resolution
- Non-contradictory decisions produce no conflicts
- Conflict detection is non-blocking
- All tests pass, go vet clean
</success_criteria>

<output>
After completion, create `.planning/phases/142-grounding-gate-decision-binding/142-02-SUMMARY.md`
</output>
