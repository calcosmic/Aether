---
phase: 144-regression-execution-path-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/codex_colonize_test.go
  - cmd/command_guide_test.go
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-05, CLEAN-06]

must_haves:
  truths:
    - "M4L .venv fixture produces grounded plan with zero .venv references"
    - "One declared execution path per workflow per platform is documented and verified"
    - "Codex command-guide produces correct output without playbook loading"
    - "Build ceremony output matches between Claude Code and OpenCode"
  artifacts:
    - path: "cmd/codex_colonize_test.go"
      provides: "TestM4LRegression_VenvProducesGroundedPlan integration test"
      contains: "TestM4LRegression"
  key_links:
    - from: "cmd/codex_colonize_test.go::TestM4LRegression_VenvProducesGroundedPlan"
      to: "cmd/plan_grounding.go::checkPlanGrounding"
      via: "direct function call"
      pattern: "checkPlanGrounding"
    - from: "cmd/command_guide_test.go"
      to: "cmd/command_guide.go::buildCommandGuide"
      via: "Codex smoke test"
      pattern: "buildCommandGuide"
---

<objective>
Create the M4L regression test proving the full survey-to-grounding pipeline works, add Codex command-guide smoke tests, extend cross-platform parity checks, and document the one-execution-path-per-workflow audit.

Purpose: This is the capstone verification for the entire v1.22 milestone. The M4L test proves that survey noise filtering (Phase 141), source anchors, and the grounding gate (Phase 142) compose correctly end-to-end.

Output: New M4L regression test, Codex smoke tests, cross-platform parity extension, execution path audit documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/144-regression-execution-path-cleanup/144-RESEARCH.md
@.planning/phases/143-build-plan-ceremony-restore/143-01-SUMMARY.md
@.planning/phases/143-build-plan-ceremony-restore/143-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create M4L regression test and Codex command-guide smoke tests</name>
  <files>cmd/codex_colonize_test.go, cmd/command_guide_test.go</files>
  <read_first>
    - cmd/codex_colonize_test.go (existing createVenvNoiseFixture helper at line 1573 and TestVenvNoiseExclusion at line 1602)
    - cmd/plan_grounding.go (checkPlanGrounding function signature and colony.Phase/Task structs)
    - cmd/command_guide_test.go (existing TestCommandGuideRegistered, TestCommandGuideCoversAllYamlCommands patterns)
    - cmd/command_guide.go (buildCommandGuide function and commandGuideCatalog)
    - pkg/colony/colony.go (Phase struct at line 522, Task struct at line 534)
  </read_first>
  <behavior>
    - TestM4LRegression_VenvProducesGroundedPlan: createVenvNoiseFixture produces survey with zero .venv references, source anchors contain no .venv paths, checkPlanGrounding with grounded tasks returns zero warnings, checkPlanGrounding with ungrounded tasks returns warnings
    - TestCommandGuideBuildSmoke: buildCommandGuide("build", "codex") returns a result with non-empty RunCommand and at least one PreSteps or PostSteps entry
    - TestCommandGuidePlanSmoke: buildCommandGuide("plan", "codex") returns a result with non-empty RunCommand and at least one PreSteps or PostSteps entry
  </behavior>
  <action>
    Add TestM4LRegression_VenvProducesGroundedPlan to cmd/codex_colonize_test.go. This test uses the existing createVenvNoiseFixture helper to create a Python repo with .venv noise, runs surveyWorkspace on it, then asserts:

    1. Survey output has zero .venv references (already tested by TestVenvNoiseExclusion, but include for completeness in this pipeline test)
    2. Source anchors (facts.SourceAnchors or extracted via survey facts) contain no .venv paths
    3. Create a colony.Phase slice with a grounded task (one that references "cmd/main.go" in its Goal) -- checkPlanGrounding should return zero warnings
    4. Create a colony.Phase slice with an ungrounded task (Goal: "Build the feature" with no file paths) -- checkPlanGrounding should return a warning when source anchors exist

    The test must import colony package for Phase and Task structs. Use the imports already present in the test file as a guide.

    For CLEAN-05, add two Codex smoke tests to cmd/command_guide_test.go:
    - TestCommandGuideBuildSmoke: call buildCommandGuide("build", "codex") and assert the result has non-empty RunCommand, non-empty PreSteps (at least 1 entry), and non-empty PostSteps
    - TestCommandGuidePlanSmoke: same pattern for "plan" platform "codex"

    These smoke tests verify command-guide produces output without playbook loading (Codex does not use the TS host PlaybookLoader).
  </action>
  <verify>
    <automated>go test ./cmd -run "TestM4LRegression_VenvProducesGroundedPlan|TestCommandGuideBuildSmoke|TestCommandGuidePlanSmoke" -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - TestM4LRegression_VenvProducesGroundedPlan passes with zero errors
    - TestCommandGuideBuildSmoke passes with non-empty RunCommand
    - TestCommandGuidePlanSmoke passes with non-empty RunCommand
    - TestM4LRegression asserts zero .venv in source anchors
    - TestM4LRegression asserts checkPlanGrounding returns warnings for ungrounded tasks when anchors exist
    - TestM4LRegression asserts checkPlanGrounding returns zero warnings for grounded tasks
  </acceptance_criteria>
  <done>M4L regression test proves .venv fixture -> survey -> anchors -> grounding pipeline works; Codex command-guide smoke tests prove output without playbooks</done>
</task>

<task type="auto">
  <name>Task 2: Document execution path audit (CLEAN-02) and verify cross-platform parity (CLEAN-06)</name>
  <files>cmd/command_guide_test.go</files>
  <read_first>
    - .aether/commands/build.yaml (execution path: runtime.command = "aether host build")
    - .aether/commands/plan.yaml (execution path: runtime.command = "aether host plan")
    - .aether/commands/continue.yaml (two paths: default via aether continue, heavy via aether host continue)
    - .aether/commands/colonize.yaml (execution path: wrapper_additions.orchestration with host command)
    - .aether/commands/seal.yaml (execution path)
    - .aether/ts-host/test/cross-platform-parity.test.ts (existing parity checks)
    - .claude/commands/ant/build.md (Claude Code wrapper for build)
    - .opencode/commands/ant/build.md (OpenCode wrapper for build)
  </read_first>
  <action>
    CLEAN-02 requires documenting that there is one declared execution path per workflow per platform. Add a test to cmd/command_guide_test.go named TestExecutionPathAudit_OneConductorPerWorkflow that programmatically verifies:

    For each of the 5 workflows (build, plan, continue, colonize, seal):
    1. Load the corresponding YAML file from .aether/commands/
    2. Assert it has a `runtime.command` field (the Go CLI invocation path)
    3. For build, plan, colonize: assert the runtime.command contains "aether host" (TS host conductor for Claude/OpenCode)
    4. For continue: assert the runtime.command contains "aether continue" (default path) AND the orchestration block references "aether host continue" (heavy path) -- two paths is correct for continue, one default and one conditional
    5. For each workflow, call buildCommandGuide(workflow, "codex") and assert it returns a non-empty RunCommand (Codex has its own conductor via command-guide, not TS host)

    This test IS the audit -- it programmatically verifies the one-conductor-per-platform pattern and will fail if someone accidentally introduces a dual conductor.

    For CLEAN-06, verify the existing cross-platform-parity.test.ts already checks build ceremony alignment. Read the test and confirm it covers:
    - "Claude and OpenCode wrappers have matching ceremony invocation patterns" for build command
    - "dispatched command wrappers contain all 4 ceremony invocations" for build command

    If the existing tests already cover CLEAN-06, add a brief comment to the TestExecutionPathAudit test noting CLEAN-06 coverage is provided by the TS cross-platform-parity tests. If there is a gap (e.g., the TS tests do not verify build command YAML content parity), add a minimal assertion.

    Note: The execution path audit documents colonize.yaml as having a wrapper_additions.orchestration block. This is intentional -- colonize was not stripped in Phase 143. The test should NOT assert that colonize lacks orchestration; it should assert that colonize has a valid execution path via "aether host colonize" in its orchestration block.
  </action>
  <verify>
    <automated>go test ./cmd -run "TestExecutionPathAudit_OneConductorPerWorkflow" -count=1 -v && cd /Users/callumcowie/repos/Aether/.aether/ts-host && node --test test/cross-platform-parity.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TestExecutionPathAudit_OneConductorPerWorkflow passes
    - Test asserts build.yaml runtime.command contains "aether host build"
    - Test asserts plan.yaml runtime.command contains "aether host plan"
    - Test asserts colonize.yaml orchestration references "aether host colonize"
    - Test asserts continue.yaml has both "aether continue" (default) and "aether host continue" (heavy)
    - Test asserts each workflow's Codex command-guide produces non-empty output
    - TS cross-platform-parity tests pass (all ceremony alignment checks green)
  </acceptance_criteria>
  <done>Execution path audit is codified as a passing test proving one conductor per workflow per platform; cross-platform build parity verified</done>
</task>

</tasks>

<verification>
go test ./cmd -run "TestM4LRegression|TestCommandGuideBuildSmoke|TestCommandGuidePlanSmoke|TestExecutionPathAudit" -count=1 -v && cd /Users/callumcowie/repos/Aether/.aether/ts-host && node --test test/cross-platform-parity.test.ts
</verification>

<success_criteria>
- M4L regression test passes: .venv fixture -> survey -> anchors -> grounding all clean
- Codex command-guide smoke tests pass: build and plan produce correct output
- Execution path audit test passes: one declared conductor per workflow per platform
- Cross-platform parity tests pass: build ceremony matches between Claude and OpenCode
</success_criteria>

<output>
After completion, create `.planning/phases/144-regression-execution-path-cleanup/144-02-SUMMARY.md`
</output>
