---
phase: 143-build-plan-ceremony-restore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/ts-host/src/playbook-loader.ts
  - .aether/ts-host/test/playbook-loader.test.ts
  - .aether/docs/command-playbooks/plan-prep.md
  - .aether/docs/command-playbooks/plan-dispatch.md
  - .aether/commands/build.yaml
  - .aether/commands/plan.yaml
autonomous: true
requirements:
  - CEREMONY-01
  - CEREMONY-02
  - CEREMONY-03
  - CEREMONY-06

must_haves:
  truths:
    - "PlaybookLoader resolves playbook files from repo-local, hub, and absolute paths"
    - "loadPlaybooksForWorkflow returns the correct playbook list for build, plan, and continue workflows"
    - "renderPlaybookContext truncates playbook content to budget and injects as document section"
    - "Plan playbooks exist for plan-prep and plan-dispatch flows"
    - "YAML build.yaml and plan.yaml have no orchestration procedure in wrapper_additions"
  artifacts:
    - path: ".aether/ts-host/src/playbook-loader.ts"
      provides: "resolvePlaybookCandidates, loadPlaybook, loadPlaybooksForWorkflow, renderPlaybookContext"
      exports: ["resolvePlaybookCandidates", "loadPlaybook", "loadPlaybooksForWorkflow", "renderPlaybookContext", "Playbook"]
    - path: ".aether/ts-host/test/playbook-loader.test.ts"
      provides: "Unit tests for playbook resolution, loading, workflow maps, and document injection"
    - path: ".aether/docs/command-playbooks/plan-prep.md"
      provides: "Plan preparation steps: status check, depth selection, unresolved clarifications gate"
      contains: "Step"
    - path: ".aether/docs/command-playbooks/plan-dispatch.md"
      provides: "Plan dispatch: Scout wave 1, Route-Setter wave 2, grounding gate, plan-finalize"
      contains: "Step"
    - path: ".aether/commands/build.yaml"
      provides: "Build command YAML with orchestration section removed"
      contains: "wrapper_additions"
    - path: ".aether/commands/plan.yaml"
      provides: "Plan command YAML with orchestration section removed"
      contains: "wrapper_additions"
  key_links:
    - from: ".aether/ts-host/src/playbook-loader.ts"
      to: ".aether/docs/command-playbooks/"
      via: "resolvePlaybookCandidates reads playbook markdown files from repo and hub paths"
      pattern: "readFileSync.*command-playbooks"
    - from: ".aether/ts-host/src/playbook-loader.ts"
      to: "cmd/build_playbook_context.go"
      via: "Same candidate resolution order: repo-local, hub system path, hub root path, bare path"
      pattern: "resolvePlaybookCandidates"
---

<objective>
Create the PlaybookLoader module, plan playbooks, and slim YAML orchestration sections.

Purpose: This plan establishes the foundation for playbook-driven ceremony. The PlaybookLoader reads playbook markdown files from the same paths the Go runtime uses (repo, hub, absolute), making playbook content available to the TS host for document injection into worker briefs. Plan playbooks (plan-prep.md, plan-dispatch.md) are created because none exist yet (only build and continue playbooks exist). YAML orchestration sections are removed so there is one conductor per platform (CEREMONY-03, CEREMONY-04).

Output: playbook-loader.ts with tests, two new plan playbooks, slimmed YAML files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-build-plan-ceremony-restore/143-RESEARCH.md
@.planning/phases/142-grounding-gate-decision-binding/142-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From .aether/ts-host/src/types.ts:
```typescript
export interface BuildManifest {
  playbooks: string[];
  worker_briefs: string[];
  dispatches: BuildDispatch[];
  // ... other fields
}
```

From cmd/build_playbook_context.go (Go constants to replicate):
```go
const buildWorkerBriefPlaybookBudgetChars = 7000
const buildWorkerBriefPlaybookPerFileChars = 2800
```

From cmd/build_playbook_context.go:57-77 (candidate resolution order):
```go
func buildPlaybookCandidates(root, playbook string) []string {
  // 1. Absolute path
  // 2. root + playbook path
  // 3. hub + "system" + trimmed path
  // 4. hub + trimmed path
  // 5. bare path (fallback)
}
```

From .aether/ts-host/src/template-loader.ts:133-148 (pattern to follow):
```typescript
export function loadTemplate(cwd: string, name: string): ParsedTemplate {
  const filePath = join(cwd, ".aether", "templates", "ceremony", `${name}.md`);
  // readFileSync, fallback to DEFAULT_TEMPLATES
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create playbook-loader.ts with PlaybookLoader and unit tests</name>
  <files>.aether/ts-host/src/playbook-loader.ts, .aether/ts-host/test/playbook-loader.test.ts</files>
  <read_first>
    .aether/ts-host/src/template-loader.ts
    .aether/ts-host/src/types.ts
    cmd/build_playbook_context.go
  </read_first>
  <behavior>
    - Test 1: resolvePlaybookCandidates returns repo-local path, hub system path, hub root path, bare path for relative playbook names
    - Test 2: resolvePlaybookCandidates returns absolute path first when input is absolute
    - Test 3: resolvePlaybookCandidates returns empty array for empty string input
    - Test 4: loadPlaybook returns null when no candidates exist on disk
    - Test 5: loadPlaybook returns Playbook with name, path, content when first candidate exists
    - Test 6: loadPlaybook skips non-existent candidates and loads from later candidate
    - Test 7: loadPlaybooksForWorkflow("build") returns 5 playbooks matching Go's codexBuildPlaybooks list (build-prep, build-context, build-wave, build-verify, build-complete) -- only the ones that actually exist on disk
    - Test 8: loadPlaybooksForWorkflow("plan") returns plan-prep and plan-dispatch playbooks (will be 0 until Task 2 creates the files)
    - Test 9: loadPlaybooksForWorkflow("continue") returns 4 playbooks (continue-verify, continue-gates, continue-advance, continue-finalize) -- only ones that exist
    - Test 10: loadPlaybooksForWorkflow with unknown workflow returns empty array
    - Test 11: renderPlaybookContext returns empty string for empty playbooks array
    - Test 12: renderPlaybookContext returns "## Relevant Playbooks" header with playbook content
    - Test 13: renderPlaybookContext truncates individual files to maxPerFile chars with "[playbook truncated]" marker
    - Test 14: renderPlaybookContext stops adding playbooks when maxBudget chars are exhausted
  </behavior>
  <action>
    Create .aether/ts-host/src/playbook-loader.ts with these exported functions:

    1. `resolvePlaybookCandidates(root: string, playbook: string): string[]` -- mirrors Go's buildPlaybookCandidates: absolute path first (if input is absolute), then repo-local path at `root + ".aether/docs/command-playbooks/" + playbook`, then `root + playbook`, then hub path at `homedir() + ".aether/system/docs/command-playbooks/" + trimmed` (stripping ".aether/" prefix), then `homedir() + trimmed`, then bare playbook path. Deduplicate. Return empty array for empty/whitespace input.

    2. `loadPlaybook(root: string, name: string): Playbook | null` -- iterate candidates, return first where existsSync is true with { name, path, content } from readFileSync. Return null if none exist.

    3. `loadPlaybooksForWorkflow(root: string, workflow: "build" | "plan" | "continue"): Playbook[]` -- map workflow to playbook file lists: build maps to ["build-prep.md", "build-context.md", "build-wave.md", "build-verify.md", "build-complete.md"], plan maps to ["plan-prep.md", "plan-dispatch.md"], continue maps to ["continue-verify.md", "continue-gates.md", "continue-advance.md", "continue-finalize.md"]. Call loadPlaybook for each name, filter nulls, return Playbook array.

    4. `renderPlaybookContext(playbooks: Playbook[], maxBudget?: number, maxPerFile?: number): string` -- same pattern as Go's renderBuildPlaybookContext: build "## Relevant Playbooks\n\n" header, iterate playbooks truncating each to min(maxPerFile, remaining) chars with "\n\n[playbook truncated]" marker when over budget. Default maxBudget=7000, maxPerFile=2800 (same as Go constants). Return trimmed string.

    Export the `Playbook` interface: `{ name: string; path: string; content: string }`.

    Use `node:fs` (readFileSync, existsSync), `node:path` (join, isAbsolute), `node:os` (homedir). No external dependencies.

    Create .aether/ts-host/test/playbook-loader.test.ts using Node.js built-in test runner (import { describe, it } from "node:test", import assert from "node:assert/strict"). Use temp directories or mock existsSync/readFileSync via re-exported mutable references (follow the host.ts pattern with __setCallGoJSON/__restoreCallGoJSON) for isolated testing. For resolvePlaybookCandidates, test the path generation logic directly without filesystem access. For loadPlaybook, use a dedicated test fixture directory with a known playbook file.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether/.aether/ts-host && npx tsx --test test/playbook-loader.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>
    - playbook-loader.ts exports resolvePlaybookCandidates, loadPlaybook, loadPlaybooksForWorkflow, renderPlaybookContext, and Playbook type
    - All 14+ tests pass
    - Candidate resolution order matches Go's buildPlaybookCandidates (absolute, repo, hub system, hub root, bare)
    - Budget constants match Go (7000 total, 2800 per file)
    - No external dependencies added
  </done>
</task>

<task type="auto">
  <name>Task 2: Create plan playbooks and slim YAML orchestration sections</name>
  <files>.aether/docs/command-playbooks/plan-prep.md, .aether/docs/command-playbooks/plan-dispatch.md, .aether/commands/build.yaml, .aether/commands/plan.yaml</files>
  <read_first>
    .aether/commands/build.yaml
    .aether/commands/plan.yaml
    .aether/docs/command-playbooks/build-prep.md
    .aether/docs/command-playbooks/build-wave.md
  </read_first>
  <acceptance_criteria>
    - .aether/docs/command-playbooks/plan-prep.md exists and contains "Step" headings for plan preparation
    - .aether/docs/command-playbooks/plan-dispatch.md exists and contains Scout and Route-Setter dispatch flow
    - `grep -c "orchestration:" .aether/commands/build.yaml` returns 0 (orchestration key removed from wrapper_additions)
    - `grep -c "orchestration:" .aether/commands/plan.yaml` returns 0 (orchestration key removed from wrapper_additions)
    - `grep "pre_build:" .aether/commands/build.yaml` succeeds (pre_build retained)
    - `grep "post_build:" .aether/commands/build.yaml` succeeds (post_build retained)
    - `grep "guardrails:" .aether/commands/build.yaml` succeeds (guardrails retained)
    - `grep "codex_orchestration:" .aether/commands/build.yaml` succeeds (codex_orchestration retained -- Codex is separate platform per CEREMONY-04)
    - `grep "depth_ceremony:" .aether/commands/plan.yaml` succeeds (depth_ceremony retained)
    - `grep "post_plan:" .aether/commands/plan.yaml` succeeds (post_plan retained)
    - Existing tests still pass: `cd /Users/callumcowie/repos/Aether/.aether/ts-host && npm test 2>&1 | grep -E "tests|pass|fail" | tail -3`
  </acceptance_criteria>
  <action>
    **Create plan playbooks:**

    Create .aether/docs/command-playbooks/plan-prep.md following the same markdown structure as build-prep.md. Content covers: Step 1 -- status check and colony state review, Step 2 -- planning depth selection (fast/balanced/deep/exhaustive), Step 3 -- planning depth selection (light/standard/deep for task decomposition), Step 4 -- unresolved clarifications gate (route to /ant-discuss if clarifications exist), Step 5 -- request plan manifest from runtime.

    Create .aether/docs/command-playbooks/plan-dispatch.md following the same structure. Content covers: Step 1 -- render spawn-plan ceremony for plan workflow, Step 2 -- dispatch Scout workers in wave 1 with skill injection, Step 3 -- render wave-start for wave 2, Step 4 -- dispatch Route-Setter workers in wave 2 with Scout results injected into briefs, Step 5 -- grounding gate check (soft warning from plan-finalize), Step 6 -- write completion file and call plan-finalize, Step 7 -- render closeout ceremony.

    Both playbooks should be concise (under 150 lines each) since the plan flow is simpler than build. Use the same "Step N:" heading format as existing build playbooks.

    **Slim YAML orchestration sections:**

    Edit .aether/commands/build.yaml: remove the entire `orchestration: |` key and its 15-step content block from `wrapper_additions`. Keep `pre_build`, `post_build`, `guardrails`, `follow_up`, `codex_orchestration`, and all other keys unchanged. The orchestration procedure is now owned by the TS host (playbook-driven).

    Edit .aether/commands/plan.yaml: remove the entire `orchestration: |` key and its 20-step content block from `wrapper_additions`. Keep `depth_ceremony`, `planning_depth_ceremony`, `iterative_planning_ceremony`, `post_plan`, `guardrails`, `codex_orchestration`, and all other keys unchanged.

    Do NOT modify codex_orchestration sections -- Codex uses command-guide + skills (CEREMONY-04).
    Do NOT modify continue.yaml orchestration -- continue is out of scope for CEREMONY-03.
  </action>
  <verify>
    <automated>cd /Users/callumcowie/repos/Aether && grep -c "orchestration:" .aether/commands/build.yaml && grep -c "orchestration:" .aether/commands/plan.yaml && test -f .aether/docs/command-playbooks/plan-prep.md && test -f .aether/docs/command-playbooks/plan-dispatch.md && cd .aether/ts-host && npm test 2>&1 | grep -E "tests|pass|fail" | tail -3</automated>
  </verify>
  <done>
    - plan-prep.md exists with 5 plan preparation steps
    - plan-dispatch.md exists with Scout -> Route-Setter -> grounding gate -> finalize flow
    - build.yaml has zero "orchestration:" lines in wrapper_additions (packaging-only)
    - plan.yaml has zero "orchestration:" lines in wrapper_additions (packaging-only)
    - All other YAML keys (pre_build, post_build, guardrails, codex_orchestration, depth_ceremony, post_plan) preserved
    - TS test suite passes with no more than the 2 pre-existing failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem reads | Playbook loader reads from repo and hub paths -- must not follow symlinks outside expected directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-01 | Tampering | playbook-loader.ts | accept | Playbook files are developer-authored markdown in repo/hub; no user input flows into file paths |
| T-143-02 | Information Disclosure | playbook-loader.ts | accept | Playbook content is non-sensitive project documentation; no secrets in playbook files |
</threat_model>

<verification>
1. All playbook-loader tests pass: `cd .aether/ts-host && npx tsx --test test/playbook-loader.test.ts`
2. Plan playbooks exist: `ls .aether/docs/command-playbooks/plan-*.md`
3. YAML orchestration removed: `grep -c "orchestration:" .aether/commands/build.yaml` returns 0, same for plan.yaml
4. Full TS test suite: `cd .aether/ts-host && npm test`
5. Go tests unaffected: `go test ./... -count=1`
</verification>

<success_criteria>
- PlaybookLoader resolves playbooks from repo, hub, and absolute paths in the same order as Go's buildPlaybookCandidates
- loadPlaybooksForWorkflow maps build/plan/continue to correct playbook file lists
- renderPlaybookContext truncates to budget (7000/2800) matching Go constants
- Plan playbooks (plan-prep.md, plan-dispatch.md) exist with correct step structure
- YAML build.yaml and plan.yaml have no orchestration procedure in wrapper_additions
- All other YAML keys preserved (pre_build, post_build, guardrails, codex_orchestration, etc.)
- No TS test regressions beyond the 2 pre-existing failures
</success_criteria>

<output>
After completion, create `.planning/phases/143-build-plan-ceremony-restore/143-01-SUMMARY.md`
</output>
