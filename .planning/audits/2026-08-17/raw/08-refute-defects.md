# Raw report 08 — Hostile re-derivation of the defect list

*Verbatim output of the "Refute the defect list" investigation, 2026-08-17. Verdicts: SURVIVES / WEAKENED / REFUTED per claim.*

### 1. Worktree GC destroys unmerged work — SURVIVES
- GC targets exactly the vulnerable statuses: codex_build_worktree.go:1063 removes Allocated, InProgress, AND Orphaned. No age filter, no dirty check, no unmerged-commit check anywhere in the function.
- Removal unconditionally destructive: removeGitWorktree (:824-830): `git worktree remove --force` + `git branch -D`.
- All three call sites confirmed and ungated: session_flow_cmds.go:261 (resume, unconditional), init_cmd.go:201 (+ os.RemoveAll at :205), codex_continue.go:540 at the very top of runCodexContinue — BEFORE manifest reconciliation, and continue never merges (merge lives only in codex_build_finalize.go:568). No parallel_mode gate at any call site.
- The GC destroys worktrees the same file tried to preserve: reconcileWorktreeWave sets preserveWorktree=true on sync failure/conflict/worker failure and marks them WorktreeOrphaned (:412-446) — precisely the status GC then deletes.
- Two guards exist but do not refute: (a) runCodexBuildWithOptions blocks a new build when orphans exist (codex_build.go:481-487) — but the canonical next commands after a crash (/ant-resume, /ant-continue) GC them first; resume/continue wrappers contain zero worktree mentions. (b) Init's destruction arguably intended; backs up COLONY_STATE.json but not worktree contents.

### 2. Merge gate hard-codes `go test ./...` — SURVIVES
codex_build_worktree.go:1124: `exec.CommandContext(testCtx, "go", "test", "./...")`; on failure finalize is fatal (codex_build_finalize.go:573-575). The manual recovery command hard-codes it too (worktree.go:751; help text :694). resolveTestCommand() (gate.go:244) does CLAUDE.md → codebase.md → go.mod/package.json/Cargo.toml/pom.xml detection — only callers gate.go:176 and verification_command_section.go:19; never the worktree paths. No override. Non-Go repo: every branch fails, finalize errors. Gated to worktree mode only — exactly the claimed scope.

### 3. Midden path split — SURVIVES
Writers: every mutating subcommand in midden_cmds.go (SaveJSON("midden.json", ...) at :124, :211, :250, :290, :385, :463) and spawn_budget.go:381 → flat `.aether/data/midden.json`. Readers on the dir path: context.go:236 (LoadJSON("midden/midden.json") feeding colony-prime failureCount), autopilot.go:89, plus immune.go:256, memory_health.go:77, context.go:859, medic_scanner.go:511. NO writer to midden/midden.json exists anywhere (grep zero); no normalizer; no migration. status.go:148/:576 read the flat path — /ant-status shows counts while colony-prime/autopilot/immune/memory-health forever see zero.

### 4. Handoff schema never reaches Claude-path workers — SURVIVES (wording corrected)
The schema IS communicated — to the Queen: build.md:261-262 spells the full object and "an empty handoff will be rejected by the finalizer." But build.md:272 simultaneously FORBIDS relaying it: worker prompt = capsule + brief VERBATIM + skill_section, "Nothing else, nothing invented." Verified: zero files in .claude/agents/ant/ contain "handoff"; the runtime brief ends at `## Expected Output` — one caste line + verification commands (codex_build.go:2799-2805), no return contract; HandoffSection is INCOMING prior handoffs (:3094-3096). Rejection real (codex_build_finalize.go:1145-1146, IsEmptyWorkerHandoff). Telling: the runtime-invoker path synthesizes a handoff from claims when the worker omits one (pkg/codex/worker.go:1037-1038, synthesizeWorkerHandoff) — on the Claude path that synthesis duty falls, uninstructed, on the Queen. Schema communicated to orchestrator, structurally unreachable by the worker, handoffs Queen-authored.

### 5. Heavy continue reviewers get only the bare brief — SURVIVES (mechanism refined)
continue.md:168/:176: "Pass each dispatch's runtime-provided `brief` verbatim" — the entire spawn instruction; contrast build.md:272 which explicitly prepends capsule and appends skill_section, proving the authors know the pattern. Refutation candidate "continue's brief is self-contained" fails: renderCodexContinueReviewBrief (codex_continue.go:1442-1508) has role text, evidence pointers, survey, verification commands — NO skill section, capsule, or pheromones. Refinement: skill_section IS shipped in the external manifest (codexContinueExternalDispatch.SkillSection, codex_continue_plan.go:35, populated :307/:332) and never mentioned by the wrapper; capsule and pheromones are worse — the external dispatch struct has NO FIELDS for them at all (:15-41); they exist only on the runtime-spawned path (codex_continue.go:1428/1433). TS host additionally attaches hive_section to dry-run dispatches (host.ts:631-634), also never delivered.

### 6. brief_path advertised but never produced on plan-only — WEAKENED
Factually correct: BriefPath assigned only in writeCodexBuildArtifacts (codex_build.go:2071), callers :606/:684 — direct-dispatch only; plan-only builds its manifest at :293 with workerBriefs=nil; codexBuildDispatchMaps emits brief_path only when non-empty (:2024). BUT build.md hedges both times (":259 when present"; ":272 falling back to inline when absent") — graceful degradation by design; nothing breaks. What remains is dead-letter prose: the stated reason for preferring brief_path (6-22KB inline briefs subject to Read-tool long-line truncation) is unmitigated on precisely the governed path. A wart, not a broken contract.

### 7. Coalescing drops later tasks' criteria — SURVIVES
mergeDispatchInto (codex_build.go:1077-1091) merges only Task text (numbered concatenation), CoveredTaskIDs, DeclaredPaths — no constraints/hints/criteria. Brief renderer resolves per-task sections via findDispatchTask (:2700), which matches dispatch.TaskID only (:2821-2831) — never updated by the merge, stays the FIRST task. The renderer does NOT iterate CoveredTaskIDs; its only consumer is completedBuildTaskIDs (:2142-2157) — which makes it worse: every covered task is marked COMPLETE by a worker never shown the later tasks' constraints or criteria. Honest narrowings: later tasks' goal text survives (concatenated :1084-1085); merging occurs only for strictly serial single-worker chains (dispatchesFormOneJob :1046-1074 — perWave==1, len(DependsOn)==1).

### 8. Empty status defaults to "completed" — REFUTED (as a practical fail-open)
The normalizer exists as quoted (codex_continue.go:3566-3572) but every path ingesting worker-supplied status is fail-closed BEFORE it: external path — codex_continue_finalize.go:624-626 rejects non-terminal status; normalizeExternalBuildStatus("") returns "" (codex_build_finalize.go:1209-1222), not in the terminal set (:1225-1231) → hard error; build finalizer identical (:1043-1049). Runtime-spawned path — claims parser defaults unknown/empty to FAILED (or blocked with blockers), never completed (pkg/codex/worker.go:1019-1031). What the normalizer actually touches: Go-authored flow steps and Go-written manifest entries. Residual exposure requires a hand-tampered on-disk manifest reaching evaluateContinueWatcherVerification (:1920-1932). Defense-in-depth wart, not a fail-open.

### 9. Legacy attempt-binding escape hatch — SURVIVES
build_attempt.go:461-463: attemptID=="" && attemptPath=="" → {Legacy: true} — and NO caller ever inspects .Legacy. On finalize (codex_build_finalize.go:418), an unbound manifest skips: manifest-SHA-vs-journal match, execution binding, colony-state-hash, supersession, completion-seal — then finalize CREATES A FRESH ATTEMPT on the spot (:493-498) and proceeds. Reachability confirmed (finalizer accepts any completion file outside .aether/data; staging's binding requirement not enforced by finalize). Context the claim should carry: the legacy branch still faces PlanOnly, phase/root/colony-mode/plan-revision match, freshness window, task-set validation, pre-build gates, packet semantics, provenance, terminal statuses, handoff validity. What a hand-crafted manifest bypasses is the tamper ratchet — content integrity against the durable journal and the state-unchanged guarantee. Ungated by age, flag, or warning.

**Score:** 6 SURVIVE (1,2,3,4,5,7 — wording corrections on 4 and 5), 2 WEAKENED-to-REFUTED (6 prose-rot; 8 refuted), 1 SURVIVES with required context (9).
