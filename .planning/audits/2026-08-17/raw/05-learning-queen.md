# Raw report 05 — Liveness Audit: learning/memory stack + Queen adaptive intelligence

*Verbatim output of the "Audit learning and Queen intelligence" investigation, 2026-08-17, branch `oracle-reinstate`. Verdicts: LIVE = automatic lifecycle caller found; MANUAL-ONLY = only user/LLM-invoked subcommand; CONFIG-WITHOUT-READER = file exists, nothing loads it.*

## Task 1 — Learning/memory stack

1. **Observation intake (memory-capture / learning-observe) — MANUAL-ONLY (the dead link in an otherwise live pipeline).** cmd/learning.go:212 and :71 are the only callers of `ObservationService.CaptureWithTrust`. No build/continue/seal Go path calls it; no live wrapper invokes it — only docs (.aether/workers.md:825) and a stale worktree copy. The pkg/memory observation→instinct auto-promotion machinery consolidates a stream nothing feeds automatically.
2. **trust-score-compute — MANUAL-ONLY as subcommand; trust math LIVE inside consolidation** (decay/archival in `pipeline.RunConsolidation`), operating on data only manual capture creates.
3. **event-bus-publish — subcommand MANUAL-ONLY; the bus itself LIVE** (cmd/consolidation_lifecycle.go:118,308,400; cmd/learning.go; cmd/internal_cmds.go:481).
4. **instinct-create / queen-promote — subcommands MANUAL-ONLY; promotion LIVE via consolidation** (pkg/memory/promote.go), plus the seal's subordinate loop `promoteInstinctLocal` (cmd/codex_workflow_cmds.go:503-513) — needed because QueenEligible's ≥3-applications bar is rarely cleared, and `instinct-apply` (cmd/internal_cmds.go:408), the only recorder of applications, has no lifecycle caller, so the bar is structurally unreachable without the subordinate loop.
5. **consolidation-phase-end — LIVE, wiring-tested. Doc claim VERIFIED.** `runPhaseEndConsolidation` (consolidation_lifecycle.go:111) called from both continue paths: codex_continue.go:994 and codex_continue_finalize.go:520, after durable advance. Tests: TestContinueAdvanceInvokesPhaseEndConsolidation (:263), TestRunPhaseEndConsolidationMutatesOnRealPath (:76), timeout-bound tests, dry-run no-mutate tests.
6. **consolidation-seal — LIVE, wiring-tested. VERIFIED.** `runSealConsolidation` (:294) from `completeSealRuntime` (codex_workflow_cmds.go:459), reached from seal (:423) and seal_final_review.go:491. Tests: TestSealRendersEightNamedAnts (seal_ceremony_test.go:1103), TestSealDoesNotDoublePromoteInstincts (:855), + curation-failure variant (:926).
7. **Eight curation ants — LIVE at seal only; phase-end does NOT run them.** `curation.NewOrchestrator(...).Run` live only at consolidation_lifecycle.go:316-320 (seal). Phase-end is pipeline.RunConsolidation only; pkg/memory has zero references to the curation package. Janitor TTL cleanup, herald hive broadcast, sentinel guard run once per colony lifetime plus manual invocation.
8. **Hive brain — LIVE both directions, policy-gated.** Read: `readHiveWisdomEntriesForDomains` (colony_prime_context.go:714) with eternal fallback, into the capsule. Promote at seal: codex_workflow_cmds.go:521, gated by `automaticHivePromotionEnabled()` (AETHER_HIVE_POLICY, default promote). Tests: TestSealHivePromote/NonBlocking/PromotedCount (seal_ceremony_test.go:669,751,802).
9. **Eternal memory fallback — LIVE** (context_weighting.go:48-73, inside capsule assembly).
10. **Knowledge graph (pkg/graph) — MANUAL-ONLY / near-orphan.** Imported by exactly one file: cmd/graph_consolidation_cmds.go. The `instinct-graph.json` written live during promotion (promote.go:216-230) is written directly, not via pkg/graph, and nothing on a lifecycle path reads it back.
11. **behavior-observations / /ant-profile — MANUAL-ONLY.** behavior-observe (cmd/profile.go:33) invoked only by profile.md:12. queen-promote of [profiled] directives fires only inside profile refresh (:424).
12. **Pheromone decay/expiry — LIVE.** Housekeeping (`runSignalHousekeepingWithState`) on every continue (codex_continue.go:519-520 and finalize path); FOCUS expiry at seal (codex_workflow_cmds.go:571); decay-weighted ordering at injection (cmd/context.go:1388-1417, cmd/pheromone_loader.go:96-119). *(Hostile review correction: runs on every ADVANCING continue — blocked outcomes return before it.)*
13. **Does injected wisdom reach a consumer? — YES prompt-level; the consumer is the LLM worker; one Go-side chain exists but its intake is manual.** The capsule is computed live for every flow: build plan-only (codex_build.go:1899-1901, :1927), subprocess (:1685 → AssemblePrompt), continue (:1413, :1816), plan (:1478), colonize (:678). Wrapper contract-bound to prepend it (build.md:259,272); pheromones + handoffs additionally in per-worker briefs (:3110-3142; pinned by TestPlanOnlyManifestBriefCarriesPheromones). No Go execution decision (caste choice, depth, budget) reads instincts or wisdom — behavioral effect entirely via prompt text.
14. **Durable learning capture — LIVE (separate store from pkg/memory).** `captureContinueLearning` (codex_continue_finalize.go:1391) fires on DEFAULT continue (codex_continue.go:987) and finalize (:508), storing to .aether/data/learn/ (pkg/learn ColonyStore), injected by colony-prime (:745), upgradeable via `newLearningValidator` (cmd/helpers.go:202). Note the fork: continue feeds pkg/learn; the pkg/memory pipeline has no automatic feeder. Two learning stores, one live intake each way. *(Hostile review correction: newLearningValidator has NO CALLER — the bridge is dead code.)*
15. **Worker handoffs — LIVE both paths** (persist codex_build.go:1758 / codex_build_finalize.go:1172 with validity enforced :1056; inject via renderWorkerHandoffSection, codex_dispatch_contract.go:704).
16. **ADVERSARIAL FINDING — midden path split.** Writers (midden_cmds.go:437-area, spawn_budget.go:359-381) write `.aether/data/midden.json`. Colony-prime failure context (cmd/context.go:236) and autopilot's chaos-pause (cmd/autopilot.go:89) read `.aether/data/midden/midden.json` — a different file. Live readers read a path the writers never populate. The documented midden→behavior loop is broken at the filesystem level; the only functioning midden→REDIRECT route is prose in chaos.md:322-335.

## Task 2 — Queen adaptive intelligence

17. **Verification depth selection — LIVE on build and continue.** `resolveVerificationDepth` (cmd/review_depth.go:185) feeds build policy (codex_dispatch_contract.go:505-509 → consumed codex_build.go:272,517) and continue (codex_continue.go:332,384). Test: TestBuildWorkerCapHonoursVerificationDepth.
18. **Required-caste gating — LIVE.** queenBuildSafetyRequiredCastes (queen_spawn_budget.go:139), queenPhaseHasSecuritySignal (:187), queenPhaseProducesTestableCode (:342) inside applyQueenSpawnBudget (:42), reached from queenOrchestrate (caste_relevance.go:146-147) on the real planning path (plannedBuildDispatchesWithJudgement, codex_build.go:1100, called :273). Tests exist as documented (queen_probe_gating_test.go:19,80,156; caste_relevance_test.go:509).
19. **Caste/team selection — HYBRID, honestly wired.** LLM proposes via --castes (flag codex_workflow_cmds.go:1343; wrapper build.md:186); Go normalizes/aliases (queen_judgement.go:191-230), merges with the keyword-scored registry (queenApplyJudgement, codex_build.go:1114), re-adds safety castes regardless. Without --castes, Go's casteRelevanceRegistry decides alone. The roster the Queen chooses from is Go's registry (queenCasteRoster, queen_judgement.go:335), NOT any YAML.
20. **Worker budget — LIVE** (applyQueenSpawnBudget; surfaced via enrichQueenExecutionPolicyWithSpawnBudget, codex_dispatch_contract.go:516). Budget-ceiling violations write midden records (spawn_budget.go:342) — to the wrong path per finding 16.
21. **Model routing — model-routing.yaml is CONFIG-WITHOUT-READER.** Zero readers in cmd/, pkg/, ts-host. workers.md:83 admits spawn-time routing "was previously built and archived". What routes models: agent frontmatter, mirrored by the Go display-only map casteModelSlot (codex_visuals.go:4415) whose resolveCasteModel (:4447) tags spawn announcements and fills dispatch.Model (codex_build.go:3086) — presentation, not routing.
22. **"Is research needed" — LIVE but typed-mode-driven, not adaptive.** Oracle gated purely by phase mode == discovery (isCasteSuppressed — "gated by the phase's TYPED mode, never prose").
23. **Iteration/escalation — LIVE on the subprocess path only.** Circuit breaker parameter of executeCodexBuildDispatches (:1673); autopilot pause conditions in Go (autopilot.go:49, invoked :197) — live under /ant-run, though the chaos check reads the wrong midden path.
24. **27-caste roster colony/agents/*.yaml — CONFIG-WITHOUT-READER; v1.26 claim STILL TRUE.** No file references colony/agents. Sibling status: review-depth.yaml LIVE (review_depth.go:45), dispatch-contract.yaml LIVE (codex_dispatch_contract.go:58), oracle-phase-directives.yaml LIVE (oracle_loop.go:1682), prompts/colony-prime.md LIVE (prompt_template_loader.go:66), ceremony/visuals.md LIVE (visuals_config.go:39); autopilot.yaml, memory-rules.yaml, colony/phases/*.yaml — NO READER. *(Hostile review correction: the four "LIVE" loaders are CWD-relative with silent fallback and never distributed — live only in the dev checkout; downstream runs hardcoded defaults.)*
25. **spawn-orchestrator.ts / TS host — LIVE but narrower than it looks.** Exec'd by `aether host *` (host_cmd.go:205); live wrapper callers: host colonize, host plan, host continue --dry-run. host.ts imports createSpawnOrchestrator (:38). But `aether host build` has NO wrapper caller and is forbidden on the interactive path (build.md:348). Depth-2 child spawning exercised only where the host dispatches workers (plan's Scout/Route-Setter, colonize), not builds.

## registry.json deletion

Resolved — commit c6bac0cb (v1.0.58) deleted .aether/registry.json (this repo's local colony-registry artifact leaking into hub publishes) and gitignored it; publish/sync exclusion lists name it (install_cmd.go:729, platform_sync.go:330). Operative registry: ~/.aether/registry/registry.json (cmd/registry.go:65,160).

## Bottom line

The v1.25/v1.26-era "built but never switched on" pattern is substantially fixed for consolidation, hive, pheromone housekeeping, handoffs, and Queen depth/caste gating — real callers, named wiring tests. Remaining dark spots: (a) pkg/memory observation intake has no automatic feeder — trust scoring and auto-promotion churn an empty stream; (b) curation ants run once per colony (seal), not per phase; (c) pkg/graph, instinct-apply, behavior profiling manual-only; (d) colony/agents/*.yaml, model-routing.yaml, autopilot.yaml, memory-rules.yaml, colony/phases/*.yaml config without readers; (e) new bug: midden writers and its two live readers disagree on the file path, silently severing the failure-learning loop.
