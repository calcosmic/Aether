# Raw report 09 — Hostile re-derivation of liveness and orphan claims

*Verbatim output of the "Refute liveness and orphan claims" investigation, 2026-08-17. User-path definition: /ant-build = `aether build N --plan-only` → wrapper spawns → build-completion-stage → build-finalize; /ant-continue default = one call to `aether continue`; heavy = `host continue --dry-run` → continue-finalize; /ant-seal default = `host seal` → seal-finalize.*

| # | Claim | Verdict | Decisive evidence |
|---|---|---|---|
| 1 | phase-end consolidation | **LIVE-ON-USER-PATH — with a hollow core** | Call real and unconditional on advance: codex_continue.go:994 after the atomic commit (:915-958) inside runCodexContinue = what `aether continue` runs (codex_workflow_cmds.go:263). Second site codex_continue_finalize.go:520 heavy-path only. Mutates instincts.json/QUEEN.md via learn.NewPipeline(...).RunConsolidation (consolidation_lifecycle.go:118-141); instincts.json IS read into capsules (colony_prime_context.go:611). BUT (see #10) nothing on the lifecycle writes the observations it consolidates — on a colony never running manual memory-capture or Oracle promote (oracle_promote.go:155-176), the live caller runs in permanent zero-state ("colony observed nothing new this phase", :79-82). Plumbing live; the water main feeding it manual. |
| 2 | seal consolidation + 8 ants | **LIVE-ON-USER-PATH** | seal-finalize → runSealFinalize → completeSealRuntime (seal_final_review.go:491) → runSealConsolidation (codex_workflow_cmds.go:459) → curation.NewOrchestrator(...).Run (consolidation_lifecycle.go:316-320). Raw `aether seal` also reaches it (:423). Only seal readiness gates it. Same zero-state caveat. |
| 3 | hive read + promote | **LIVE-ON-USER-PATH** | Read: colony_prime_context.go:714 inside buildColonyPrimeOutputOpts, called by resolveCodexWorkerContext (:1126-1127), populating the plan-only manifest's ContextCapsule (codex_build.go:1899-1901,1927). Gated by automaticHiveReadEnabled; unset AETHER_HIVE_POLICY resolves to promote (hive_policy.go:37-38) — both default-on. Promote at seal: codex_workflow_cmds.go:513-526, both routes. |
| 4 | housekeeping "on every continue" | **LIVE — claim overstated** | Cited :519 is only the var; the call is codex_continue.go:963 — AFTER the atomic advance. Every blocked outcome (:568, :599, :747, :826) returns BEFORE it. Finalize path likewise advance-only (:1130). Genuinely mutates pheromones.json (signal_housekeeping.go:66-75). Correct claim: every ADVANCING continue, never a blocked one. |
| 5 | evidence-bound criteria | **LIVE (both routes)** | Default: codex_continue.go:1660 inside runCodexContinueVerification, invoked :663. External: codex_continue_plan.go:267 serves plan-only + finalize; TS host generates the heavy manifest by shelling to `aether continue --plan-only` (command-registry.ts:123). Legacy fraction: ZERO for any plan the current flow accepts — validateNewPlanEvidenceContract unconditional at codex_plan.go:693 and codex_plan_finalize.go:333; synthetic plans get synthetic bindings (criterion_evidence.go:130-141). Prose-only survives only in colonies whose plans predate the contract. |
| 6 | Queen gating on plan-only | **LIVE-ON-USER-PATH** | Inside runCodexBuildPlanOnlyWithOptions: recommendQueenExecutionPolicy (codex_build.go:265) → resolveVerificationDepth (codex_dispatch_contract.go:505-509); plannedBuildDispatchesWithJudgement (:273) → queenApplyJudgement (:1113) → queenOrchestrate → applyQueenSpawnBudget (queen_judgement.go:95-97, caste_relevance.go:146-147); budget enrichment :282. |
| 7 | handoff injection on plan-only | **LIVE, loop closed** | Written by the wrapper's own finalizers (codex_build_finalize.go:1172, codex_continue_finalize.go:717); injected per-dispatch and embedded in the Brief (codex_build.go:3094-3096, embed :3135), plus capsule (:695). |
| 8a | "zero-reader" configs | **DEAD — confirmed** | colony/agents/*.yaml (27), colony/phases/*.yaml (9), model-routing.yaml, autopilot.yaml, memory-rules.yaml: exhaustive grep over cmd/, pkg/, ts-host, all three wrapper trees, Makefile, hooks, CI — no reader; only doc prose (a prior report at docs/audits/AETHER_FINAL_COMPARATIVE_READINESS_REPORT_2026-08-05.md:365 already recorded model-routing reader-less). |
| 8b | "LIVE" configs | **CONTRADICTS AUDIT: LIVE-ONLY-ELSEWHERE for three of four** | review-depth.yaml, dispatch-contract.yaml, colony/prompts/colony-prime.md, colony/ceremony/visuals.md all load from a CWD-relative path (policy_loader.go:18-20; prompt_template_loader.go:66; visuals_config.go:39) with silent fallback to hardcoded defaults. Hub publish syncs only oracle-phase-directives out of colony/policies (install_cmd.go:822-827); `aether update` never places colony/ downstream. In every repo except the Aether source checkout these files never load — hardcoded fallbacks are the real policy. Only oracle-phase-directives.yaml is genuinely distributed and multi-path resolved (oracle_loop.go:1681-1690) — and fires only on /ant-oracle. |
| 9 | manual-only orphans | **CONFIRMED, sharpened** | memory-capture/learning-observe: registered (learning.go:212-277), no lifecycle caller; hooks invoke only hook-pre-tool-use/stop/pre-compact. workers.md:825 ("via memory-capture in continue step") is FALSE documentation. instinct-apply (internal_cmds.go:408): zero references; worse — application counts feed Queen-eligibility ("3 applications", codex_workflow_cmds.go:531-541) and only manual paths increment them (internal_cmds.go:430, instinct.go:64) — a promotion criterion with no automatic writer. behavior-observe: one instruction in profile.md:12. pkg/graph: imported solely by graph_consolidation_cmds.go; the librarian ant imports pkg/memory, not pkg/graph. session-verify-fresh: only caller medic.md:28 omits the timestamp; without it every file is fresh (session_cmds.go:420-423) — vacuous as claimed. |
| 10 | two learning stores | **CONFIRMED — and the cited bridge is DEAD** | pkg/learn live: captureContinueLearning at codex_continue.go:987 and codex_continue_finalize.go:508, writing .aether/data/learn/ (:1453-1462), read back as LEARNED MEMORY (colony_prime_context.go:745-749). pkg/memory: no automatic feeder. The bridge the audit cited, newLearningValidator (helpers.go:202), HAS NO CALLER — pipelineConfigForStore (graph_consolidation_cmds.go:16-30) never sets LearningValidator, so the hook at pkg/memory/pipeline.go:135-136 always sees nil. Unbridged in production. |
| 11 | pkg/trace/cost.go | **CONFIRMED doubly dead** | Sole production caller pkg/agent/pool.go:192; agent.NewPool has ZERO production call sites — poolStreamHandler.OnComplete never executes. Bonus: pkg/codex/usage.go:285's comment calling cost.go "a live second instance" is itself wrong. |

## Three findings most contradicting the prior audit
1. **The "live" policy/prompt YAMLs are dev-checkout-only** (#8b) — downstream, the shipped YAML is decorative and hardcoded Go fallbacks are the actual behavior; the exact "documentation claim about runtime behaviour" failure CLAUDE.md warns about, hidden by silent fallback.
2. **The cited memory↔learn bridge does not exist at runtime** (#10) — written, documented, never wired.
3. **Consolidation is live but starved** (#1/#2/#9) — callers fire on default paths, but the observation store and application counts have no automatic writers; the front half (pkg/learn) runs daily while the back half (observations → instincts) advances only on manual subcommands nobody's wrapper invokes.
