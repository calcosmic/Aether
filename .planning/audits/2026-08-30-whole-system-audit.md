# Whole-system audit — 2026-08-30

Raw: `.planning/audits/2026-08-30-whole-system-audit.json` — 284 findings (42 checked by a skeptic, the rest unchecked), 4 refuted, 184 behaviours confirmed kept.

## memory_context (119)

| sev | status | chk | subject | claim | evidence |
|---|---|---|---|---|---|
| high | degraded | ✓ | memory-details | The drill-down view is gone: the command is now an alias of memory-metrics that returns bare counts as JSON with no visual renderer, so no wisdom entries, pending-promotion text, deferred proposals, or midden failure lines are ever shown. | cmd/memory_details.go:12-55 (Aliases memory-details, outputOK counts only, no outputWorkflow/visual); cmd/helpers.go:20-27 outputOK prints raw JSON regardless o |
| high | degraded | – | memory-capture (learning.sh observe -> trust-calculate -> learning-promote-auto -> instinct-create -> queen-promote chain) | The whole observation->instinct->QUEEN auto-promotion chain now fires only when someone types `aether memory-capture` by hand; the continue path writes a separate learn.Entry hypothesis and never captures an observation, so learning-observations.json is never written on any live path. | cmd/codex_continue_finalize.go:1545-1633 captureContinueLearning writes learn.Entry via learn.NewColonyStore(store).Add (StatusHypothesis) — no ObservationServi |
| high | degraded | – | consolidation-phase-end promotion stages (observations->instincts, instincts->QUEEN.md) | Phase-end consolidation runs every continue but its promotion stages are starved: it promotes observations from learning-observations.json (nothing live writes it) and promotes instincts to QUEEN only when Confidence>=0.75 AND >=3 recorded applications (nothing live records an application), so on th | pkg/memory/pipeline.go:161-196 RunConsolidation; pkg/memory/consolidate.go:107 loads learning-observations.json, :151-156 QueenEligible requires SummarizeInstin |
| high | degraded | – | midden-write (automatic failure logging) | Old build, continue, seal, quick and chaos flows all wrote failures into the midden automatically; at HEAD the only runtime writer is the autopilot retry-exhausted record — worker failures, abandoned approaches, seal findings and chaos findings never reach the midden unless someone types `aether mid | cmd/midden_cmds.go:402 (cobra only, 0 wrapper callers); appendMiddenEntry's sole non-test caller is cmd/autopilot_retry_record.go:46; no midden write in cmd/cod |
| high | dropped | ✓ | continue: observation -> instinct -> QUEEN.md promotion | The observation->instinct->QUEEN.md promotion stages are live on the continue path (codex_continue.go:1103 / codex_continue_finalize.go:612 -> runPhaseEndConsolidation -> pipeline.RunConsolidation, which writes instincts.json and QUEEN.md and populates QueenPromoted). The gap is at the source: nothi | cmd/codex_continue_finalize.go:1607 (learnStore.Add only); learning-observations.json writers are CLI-only cmd/learning.go:71,240 and cmd/learning_cmds.go:58-30 |
| high | dropped | ✓ | continue: auto-emitted phase FEEDBACK / decision FEEDBACK / error REDIRECT pheromones | Continue no longer writes any pheromone: the phase-completion FEEDBACK, per-decision FEEDBACK, and midden-threshold auto-REDIRECT signals are gone, although CLAUDE.md still claims 'Decisions become pheromones' and 'Midden affects behavior (threshold auto-REDIRECT)'. | only pheromone reference in cmd/codex_continue.go:4297 is expiry housekeeping; no pheromone write in cmd/codex_continue_finalize.go or cmd/advance_phase.go; gre |
| high | dropped | ✓ | build: midden-write on worker/build failure | Build and continue never record worker/build/verification failures to the midden any more. The only runtime writers are autopilot retry exhaustion (cmd/autopilot_retry_record.go:46) and the spawn-tree helper-budget ceiling (cmd/spawn_budget.go:349, reached via spawn-log/spawn-can-spawn), neither of  | appendMiddenEntry callers: cmd/autopilot_retry_record.go:46 only; no wrapper invokes `aether midden-write` (.claude/commands/ant/*.md); cmd/codex_build_finalize |
| high | dropped | ✓ | insert-phase | Inserting a corrective phase no longer emits the 30-day FOCUS pheromone, the REDIRECT for constraints (the --constraints input itself no longer exists), the memory-capture learning, or the phase_inserted event; additionally, the old default two-task scaffold (N.1 "Diagnose and correct", N.2 "Validat | cmd/state_extra.go:119-217 — only COLONY_STATE.json is touched; no createPheromoneSignal / memory capture / event append |
| high | dropped | – | instinct-create from verified phase learnings on /ant-continue | No instinct is created on the continue path any more — instincts now only come from the `instinct-create` CLI, from `aether oracle promote`, or from consolidation of observations that nothing live captures. | cmd/codex_continue.go and cmd/codex_continue_finalize.go contain zero references to 'nstinct' (grep count 0); pkg/memory/promote.go PromoteService.Promote calle |
| high | orphaned | – | event-bus-publish / event-bus-query / event-bus-replay / event-bus-subscribe / event-bus-cleanup | The whole event-bus CLI is uncalled; replay/subscribe are wired only into the TS event bridge under host.ts `case "lifecycle"`, which nothing invokes. Wisdom Pipeline stage 1b (event-bus-publish after trust score) never runs. | cmd/eventbus.go:40,79,122,165,194; .aether/ts-host/src/event-bridge.ts:87,114 called from host.ts:1566 (lifecycle case only); no wrapper/yaml/hook reference |
| high | orphaned | – | learning-* proposal/promotion CLI (15 subcommands) | None of learning-approve-proposals, learning-defer-proposals, learning-display-proposals, learning-extract-fallback, learning-inject, learning-promote, learning-select-proposals, learning-undo-promotions, learning-propose, learning-validate, learning-disprove, learning-list, learning-observe, learni | cmd/learning_cmds.go:16,73,130,167,199,260,324,367,479,527,570,613 and cmd/learning.go:39,95,164 define them; grep of all live surfaces returns zero calls; cmd/ |
| high | orphaned | – | instinct-create / instinct-read / instinct-read-trusted / instinct-decay-all / instinct-archive / instinct-apply | The six instinct subcommands have no live caller; instinct-create is only mentioned in .aether/workers.md:836 as a heading-level list ('Key subcommands:'), not executed. | cmd/instinct.go:28,156,206,260 and cmd/internal_cmds.go:362,408; zero calls in wrappers/yaml/hooks/ts-host/Go non-test. Would: create or reinforce an instinct,  |
| high | orphaned | – | midden-write / midden-acknowledge / midden-search / midden-tag / midden-collect / midden-handle-revert / midden-cross-pr-analysis / midden-prune | Eight of the ten midden subcommands have no caller; only midden-recent-failures (chaos.md:324) is live, so nothing on a live path ever writes a failure record via the CLI. | cmd/midden_cmds.go:403 (write), :86 (acknowledge), :137 (search), :174 (tag), :224 (collect), :263 (handle-revert), :303 (cross-pr), :351 (prune); zero calls in |
| high | unwired | ✓ | dream | The Dreamer's journal write to .aether/dreams/{date}.md via the Write or Edit tool is blocked unconditionally by the PreToolUse hook (settings.json:3-11 → cmd/hook_cmds.go:79-93 → :483-484), main session and subagents alike; the only way the wrapper's Step 2/3 could succeed is an uninstructed Bash-s | .claude/commands/ant/dream.md:~60 still instructs Write to .aether/dreams/; .claude/settings.json:5-9 runs `aether hook-pre-tool-use` on Write/Edit; cmd/hook_cm |
| medium | degraded | ✓ | continue: hive promotion at phase end | Cross-colony hive promotion of high-confidence instincts no longer runs at /ant-continue (v5.3.3 ran it every phase via continue-advance.md step 3d). At HEAD it runs only during seal (cmd/codex_workflow_cmds.go:703-717, subject to AETHER_HIVE_POLICY) or via a manual `aether hive-promote`; phase-end  | hive promote callers only in cmd/codex_workflow_cmds.go:623-817 (seal); none in cmd/codex_continue*.go |
| medium | degraded | ✓ | build: learning capture from worker results | Build no longer captures learnings from worker results: the only build-path memory-capture (.claude/commands/ant/build.md:324) records the owner's check-in trim, not worker findings; build-finalize has no learning call; learning-check-promotion and learning-approve-proposals have no wrapper caller.  | cmd/codex_build_finalize.go has no learn.* call (grep 'earning' empty); cmd/learning.go:95 learning-check-promotion and learning-approve-proposals are CLI-only  |
| medium | degraded | ✓ | interpret (act-on-dreams) | The approved-action step that turned dream insights into FOCUS/REDIRECT signals or TO-DOS.md items from inside the command is gone; the wrapper is read-only and only names commands for the user to run afterwards. | .claude/commands/ant/interpret.md:13-14 'Do not auto-inject pheromones or create action items... recommend explicit follow-up commands' |
| medium | degraded | – | hive-promote on every /ant-continue | Cross-colony hive promotion now happens only at seal; the old per-continue idempotent promotion of >=0.8 instincts is gone, so a colony that is never sealed contributes nothing to the hive. | promoteToHive/promoteToHiveWithReference called only from cmd/codex_workflow_cmds.go:712 (seal); grep 'hive/Hive' in cmd/codex_continue.go and cmd/codex_continu |
| medium | degraded | – | hive-read access tracking (access_count / last_accessed feeding LRU eviction) | The live worker-prompt path reads wisdom.json directly and never bumps access counters, so the 200-cap LRU eviction is driven only by manual `hive-read` CLI calls and can evict exactly the entries workers actually use. | cmd/context_weighting.go:18-73 readHiveWisdomEntriesForDomains is read-only (no updateWisdomLocked/SaveJSON); access bump exists only in the CLI at cmd/hive.go: |
| medium | degraded | – | .aether/data/build/phase-N/verification.json | Verification report content (claim mismatches, criteria results, task assessments) is persisted but only the needs_owner_confirmation criteria are ever read back, at seal time; nothing from a failed verification reaches the next build brief, the midden, or the learning store. | writers cmd/codex_continue.go:857-861, cmd/codex_continue_finalize.go:315-318; sole reader cmd/criterion_owner_confirmation.go:121-128 (only report.Criteria wit |
| medium | degraded | – | .aether/data/build/phase-N/review.json | The review report file is read only when sealing, and only for the final phase; per-worker review summaries and blocking issues of every other phase never reach the next phase's workers — only structured findings of a PASSING review flow onward (via in-process ledger/learn writes, not this file), so | writers cmd/codex_continue.go:944-948, cmd/codex_continue_finalize.go:557-560; only file reader cmd/seal_final_review.go:885-887 (finalCompletedPhase only). On  |
| medium | dropped | ✓ | council: clarification mode (category menu -> pheromones + instincts) | Council's interactive clarification flow (category menu -> FOCUS/REDIRECT/FEEDBACK signals + instincts) is dropped; its runtime subcommands write only council/history.json. A narrower successor lives in /ant-discuss (cmd/discuss.go), which turns a resolved hard-constraint clarification into a REDIRE | .claude/commands/ant/council.md:19-21 (no interactive flow, 'say so plainly'); cmd/council.go:176 addCouncilPosition only appends text to council/history.json |
| medium | dropped | ✓ | entomb | Eternal-memory colony record (goal/milestone/sealed_at/chamber appended to ~/.aether/eternal/memory.json .colonies) is no longer written. | cmd/entomb_cmd.go has no eternal write; grep for eternal 'colonies' in cmd/ yields nothing (cmd/hive.go:92 is a comment only); registry stats update at cmd/ento |
| medium | dropped | ✓ | data-clean | Cleaning now touches only pheromones.json plus a new worker-debug/ artifact prune (not in v5.3.3); the five other v5.3.3 targets (QUEEN.md test entries, learning-observations.json, midden.json, spawn-tree.txt, constraints.json) are no longer scanned or cleaned, and the per-file report is replaced by | cmd/maintenance.go:43-113 loads only pheromones.json + worker-debug dir; cmd/maintenance.go:305-325 isTestArtifact checks id prefix and 'test signal'/'demo patt |
| medium | dropped | ✓ | feedback | Feedback no longer creates an instinct (source user_feedback, confidence 0.7) in COLONY_STATE memory.instincts, nor updates the context document; the wrapper explicitly forbids the instinct write and the runtime does not do it. | .claude/commands/ant/feedback.md:11 'Do not append instinct records'; cmd/codex_workflow_cmds.go:885 feedbackCmd -> newSignalShortcutCommand -> createPheromoneS |
| medium | dropped | ✓ | focus | `--ttl` on focus is gone: runtime focus is ExactArgs(1) with no ttl flag and passes ttl "" to pheromone-write, so `/ant-focus "x" --ttl 2h` errors on unknown flag instead of setting a lifetime. | cmd/codex_workflow_cmds.go:959-969 (Args: cobra.ExactArgs(1); createPheromoneSignal(..., ttlFlag "", ...)) |
| medium | dropped | ✓ | history | `--type` (comma-separated event types), `--since` and `--until` (ISO or relative 1d/2h/30m) filters are gone; only a substring --filter remains and the wrapper passes raw $ARGUMENTS so `--type phase_started` or `--since 1d` fail as unknown flags. | cmd/history.go:79-84 (only --limit, --filter, --json) |
| medium | dropped | ✓ | oracle (final synthesis pass) | Minor precision only: HEAD's mechanical report is still template-aware (tech-eval / architecture-review / bug-investigation / generic section layouts at cmd/oracle_loop.go:2863-2874), so the structure survives; what is dropped is the AI-authored prose (executive summary, cross-question synthesis, co | cmd/oracle_loop.go:591-593 stop path calls writeOracleDerivedReports only; finalizeOracleLoop (cmd/oracle_loop.go) calls writeOracleDerivedReports and saveOracl |
| medium | dropped | – | seal | Seal no longer writes a CHANGELOG.md entry; the changelog-append Go command exists but has no runtime or wrapper caller. | cmd/codex_workflow_cmds.go:772-880 completeSealRuntime (no changelog call); cmd/changelog.go:13 command defined, grep shows no caller in cmd/, .claude/commands, |
| medium | dropped | – | swarm: FOCUS/REDIRECT learning injection after a successful fix | A successful swarm no longer records the pattern that worked or the anti-pattern that caused the bug as pheromone signals. | cmd/swarm_cmd.go has no pheromone write on either the in-process (:251-350) or finalize (:628-720) path (grep pheromone/midden/instinct: none) |
| medium | dropped | – | status: survey freshness line | Status no longer reports survey doc count / age / fresh-stale-missing. | cmd/status.go:786-1080 renderDashboard has no survey reference (grep survey: none) |
| medium | dropped | – | hive-read for plan research priming (Scout gets hive wisdom before researching a phase) | The phase-research Scout no longer receives hive wisdom; the brief only carries a blank '## Hive Wisdom' heading for the scout to fill, and the fallback research artifact hardcodes 'No relevant hive wisdom found'. | cmd/phase_research.go:123-156 renderPhaseResearchBrief has no hive read, line 156 is an output-template placeholder only; cmd/codex_plan.go:3385-3386 writes 'No |
| medium | dropped | – | pheromone-expire → eternal-store promotion on expiry | Old expiry promoted high-value expired signals into eternal memory automatically; HEAD expiry (manual command, seal FOCUS purge, continue housekeeping) never stores to eternal, and eternal-store has no caller at all. | cmd/pheromone_write.go:286-325 expireSignalsByType has no eternal path; cmd/internal_cmds.go:137 eternal-store cobra only, 0 callers; colony-prime has no eterna |
| medium | dropped | – | changelog-append / changelog-collect-plan-data | Seal no longer records itself in CHANGELOG.md; both changelog subcommands have no wrapper, YAML, hook, host, or Go caller. | cmd/changelog.go:13 and cmd/changelog.go:64 define them; .claude/commands/ant/seal.md has no changelog step; grep of cmd/seal*.go finds no changelog write |
| medium | dropped | – | charter-write | The colony charter is never written into QUEEN.md any more; charter-write and its helper buildCharterLines have no caller outside the command itself. | cmd/queen.go:451 defines it; buildCharterLines called only at cmd/queen.go:472; .claude/commands/ant/init.md:37 says 'no charter call' |
| medium | dropped | – | context-update | CONTEXT.md is never created or refreshed: no wrapper or Go path calls context-update; only a skill doc tells workers to call it. | cmd/context.go:1718 definition; only reference is .aether/skills/colony/context-management/SKILL.md:29 (advice, not a call) |
| medium | dropped | – | .aether/data/research/ (architect-{phase}.md / oracle-{phase}.md) | v5.3.3 build waves had Architect and Oracle workers write per-phase design/research docs to .aether/data/research/ and passed research_file to builders; HEAD has no writer, no reader and no injection for that directory. | grep for "data", "research" / data/research in cmd/ and pkg/ returns nothing; build brief only injects phase-research/ (cmd/codex_build.go:3337) and COLONY_STAT |
| medium | orphaned | ✓ | import-signals | The colony-prefix argument (second positional, prefixes imported IDs `<colony>:` to avoid collisions and tag provenance) is gone from the user path: the alias command accepts MaximumNArgs(1) and hard-codes sourcePrefix ""; the prefixing code is reachable only from tunnels --import-signals. | cmd/alias_cmds.go:32-38 (MaximumNArgs(1)); cmd/exchange.go:377 (importPheromonesData(inputPath, xmlData, "")); only other caller cmd/entomb_cmd.go:420 |
| medium | orphaned | – | learning-check-promotion / learning-approve-proposals (tick-to-approve wisdom review) | The proposal review flow (check, display, select, defer, approve, undo) exists as CLIs but nothing on the continue, seal, or entomb path invokes it any more. | cmd/learning.go:95, cmd/learning_cmds.go:16-430 registered; grep across .claude/commands, .opencode/commands, .codex, .aether/commands, .claude/agents, .claude/ |
| medium | orphaned | – | pr-context | The 10-source CI/review context assembler that fed build verification and continue-advance is a registered command that nothing calls. | cmd/context.go:709 `Use: "pr-context"`; 0 callers in wrappers, Go non-test code, hooks, ts-host |
| medium | orphaned | – | midden-collect / midden-cross-pr-analysis / midden-handle-revert / midden-prune | PR-merge failure collection and cross-PR pattern analysis ran automatically after build verify and phase advance; now they are CLI-only with no runtime caller. | cmd/midden_cmds.go:224,263,303,351 cobra commands; no wrapper/Go/ts-host/hook caller |
| medium | orphaned | – | queen-write-learnings | Phase learnings were written straight into QUEEN.md at every continue-finalize; HEAD keeps the command but nothing invokes it (phase-end consolidation promotes instincts instead, which needs ≥2 observations to fire). | cmd/queen.go:188 `Use: "queen-write-learnings"`; 0 wrapper/Go/ts-host callers; substitute is runPhaseEndConsolidation (cmd/codex_continue_finalize.go:612) |
| medium | orphaned | – | graph.sh (graph-link / graph-neighbors / graph-reach / graph-cluster) | The instinct relationship graph exists as four CLI commands but nothing on any lifecycle path (consolidation, learning capture, seal, curation) ever links or reads an edge, so the graph layer documented in structural-learning-stack.md never populates. | cmd/graph_consolidation_cmds.go:68,115,153,191 define the commands; pkg/graph is imported only by that file; sed 224-380 of the same file (consolidation-phase-e |
| medium | orphaned | – | graph-link / graph-neighbors / graph-reach / graph-cluster | Instinct graph CLI has no caller; the 'graph layer for instinct relationships' promised in CLAUDE.md is never populated or queried. | cmd/graph_consolidation_cmds.go:68,115,153,191 — zero references outside testdata/allowlist |
| medium | orphaned | – | hive-init / hive-abstract / hive-revoke / hive-search | Four hive management commands have no caller; hive-init is never run so a fresh machine relies on lazy creation elsewhere or hits 'hive is not initialized' (hive.go:130). | cmd/hive.go:275,531,743; cmd/hive_search.go:12 — zero references outside testdata |
| medium | orphaned | – | eternal-init / eternal-store | Eternal-memory fallback is never initialised or written; the documented 'expired pheromone -> eternal memory' promotion has no runtime path. | cmd/hive.go:803, cmd/internal_cmds.go:138 — zero references outside testdata/allowlist |
| medium | orphaned | – | midden-review | midden-review's only caller is the TS host build orchestrator's midden threshold check, and `aether host build` itself has no live caller (build.md:452 forbids it; cmd/autopilot.go never execs host), so the auto-REDIRECT-on-repeated-failure path is transitively dead. | cmd/midden_cmds.go:53 definition; only caller .aether/ts-host/src/queen/midden-check.ts:41, reached via orchestrator.ts:90 -> lifecycle.ts:415 (build lane only) |
| medium | orphaned | – | immune-auto-scar | Auto-detection of failure patterns from the midden into scars has no caller. | cmd/immune.go:243; no live surface references it. Would: scan midden for repeated failure patterns and record scars. |
| medium | orphaned | – | pheromone-prime / pheromone-snapshot-inject / pheromone-merge-back / pheromone-count / pheromone-validate-xml | Five pheromone subcommands have no caller; in particular pheromone-snapshot-inject and pheromone-merge-back (worktree pheromone sync) are never run from the worktree build/continue path. | cmd/pheromone_mgmt.go:15 (prime), :259 (snapshot-inject), :290 (merge-back); cmd/pheromones_read.go:45 (count); cmd/pheromone_write.go:272 (validate-xml, self-d |
| medium | orphaned | – | queen-init / queen-read / queen-promote / queen-thresholds / queen-write-learnings / queen-promote-instinct / queen-migrate | Seven of nine queen-* CLIs have no caller; old flow initialised QUEEN.md at init (5x), promoted wisdom on continue, and wrote learnings at finalize. Only queen-seed-from-hive (init_cmd.go:378) and queen-compose survive. | cmd/queen.go:47,94,123,169,189,240,413 definitions; only references are codex_visuals.go:252-260 emoji map and own-file render calls; all seven on orphan_allowl |
| medium | orphaned | – | trust-score-compute / trust-score-decay / trust-tier | Wisdom Pipeline stage 1a ('trust-score-compute assigns weighted trust score') is documented in CLAUDE.md but the CLI is never invoked on any path. | cmd/trust.go:18,44,65 definitions; zero callers outside testdata; CLAUDE.md Wisdom Pipeline table row '1a. Trust score / trust-score-compute' is an untestable r |
| medium | orphaned | – | memory.phase_learnings -> '## Phase Learnings' brief section | colony-prime still renders a Phase Learnings section from COLONY_STATE.memory.phase_learnings, but no runtime path writes that field any more — the section is a reader with no writer, so previous-phase outcomes never reach the next build brief through it. | reader cmd/colony_prime_context.go:674-691; only writers are resets: cmd/init_cmd.go:284 (empty slice) and cmd/entomb_cmd.go:680 (clears). grep 'PhaseLearning{' |
| medium | orphaned | – | colony-prime 'Phase Learnings' section (state.Memory.PhaseLearnings) | The prime-context section that injects memory.phase_learnings into worker briefs reads a field no HEAD runtime path ever appends to; v5.3.3's continue-advance playbook appended to it, and HEAD's continue captures learnings into the separate learn store instead, so this section is permanently empty f | reader cmd/colony_prime_context.go:674-691; writers: only init empties it (cmd/init_cmd.go:284) and entomb clears it (cmd/entomb_cmd.go:680); continue's learnin |
| medium | pointer_only | – | .aether/data/planning/SCOUT.md | The planning Scout report file is written but the Route-Setter brief only receives its path, never its Findings/Gaps/Study Files content; the scout's substance reaches the planner only if the wrapper pastes the terminal result. | writer cmd/codex_plan.go:2577-2600 (writePlanningScoutArtifact); reader cmd/codex_plan.go:2165-2169 writes only 'Read scout output ... if it exists: <path>' int |
| medium | unwired | ✓ | oracle (wrapper persistence suggestions) | The oracle wrapper (.claude/commands/ant/oracle.md:211,218) tells the assistant to persist findings with `pheromone-write --content '{"text":"..."}'`, but the runtime treats --content as raw text: it hashes it (cmd/pheromone_write.go:87), sanitizes it (:91; braces/quotes are not rejected), then wrap | .claude/commands/ant/oracle.md:211,218 vs cmd/pheromone_write.go:56-63 (sanitizes raw content then json.Marshal(map{"text": sanitized})) |
| low | degraded | ✓ | memory-details (queen_md_updated) | last_activity.queen_md_updated is hard-coded to an empty string; the old command derived it from QUEEN.md metadata or file mtime. | cmd/memory_details.go:46 "queen_md_updated": "" |
| low | degraded | ✓ | oracle (promote observation record) | Oracle promote (live via .claude/commands/ant/oracle.md:147-148 → cmd/oracle_promote.go) no longer records a learning observation: old Step 0d called `memory-capture learning ... "oracle:promote"` (→ learning-observe, the observation store feeding auto-promotion) per finding; HEAD writes a learn.Ent | cmd/oracle_promote.go:125-149 learn.Entry Status: learn.StatusHypothesis; cmd/oracle_promote.go:155-175 PromoteService.Promote only; no memory-capture/observati |
| low | degraded | ✓ | patrol: colony-health metrics + stale pheromone expiry | Patrol used to actually expire stale pheromones and report instinct/learning/event-error counts; now it only lists stale signals as a warning and mutates nothing, and memory metrics are not shown. | cmd/patrol_check.go:159-218 detects stale signals read-only (by design); memory-metrics (cmd/memory_details.go:13) only wired to memory-details.md, not patrol |
| low | degraded | ✓ | pause-colony: handoff 'What Was Happening' / phase-progress list / per-task status icons | HANDOFF.md written by `aether pause` (cmd/session_flow_cmds.go:104 → cmd/recovery_snapshot.go:522-621) has no free-text "What Was Happening" (Session Summary is the literal "Paused at phase N"), no prose per-phase status list, and its Tasks section lists only incomplete task goals with no status ico | cmd/session_flow_cmds.go:104 Summary = 'Paused at phase %d'; cmd/recovery_snapshot.go:522-621 renders Goal/Phase/Signals/Blockers/Next Step/Tasks/Session Summar |
| low | degraded | ✓ | pause-colony: pheromone strengths in handoff | Handoff used to list each active signal with its current decayed strength; now only signal text (max 8) is listed. | cmd/recovery_snapshot.go:544 `extractSignalTexts(8)` text-only |
| low | degraded | – | resume-colony | Survey staleness (age in days, fresh/stale/missing verdict with the --force-resurvey recommendation) is reduced to a surveyed-at timestamp and a file list with no verdict. | cmd/codex_visuals.go renderResumeVisual lines ~153-172 (Surveyed:/files/path only); cmd/context.go:210 buildResumeSurveySummary |
| low | degraded | – | seal | The Chronicler documentation-coverage audit is no longer unconditional; it is only relevance-selected by queenOrchestrate and its gaps go to review ledgers instead of midden. | cmd/seal_final_review.go:1085-1098 queenSealReviewSpecs via queenOrchestrate; cmd/caste_relevance.go:583-595 chronicler never required; findings persisted at cm |
| low | degraded | – | status: HANDOFF.md 'Resuming from paused session' notice | Status no longer detects/displays a pending handoff; only resume-colony reads it. | cmd/status.go has no HANDOFF reference; handoff read lives in cmd/session_flow_cmds.go:177-… |
| low | degraded | – | status: top-3 instincts by confidence | Instinct list is now the 3 most recent, not the 3 strongest. | cmd/status.go:1042-1047 loadRecentRuntimeInstincts(s,&state,3) |
| low | degraded | – | tunnels: pheromone-trail diff (new decisions/learnings listed) | Compare shows only count deltas; the lists of new decisions and new learnings in chamber B are gone. | cmd/entomb_cmd.go:329-370 compareChambers (ints only); cmd/codex_visuals.go:4700-4712 |
| low | degraded | – | oracle end-of-loop promotion (instinct-create + learning-promote + memory-capture) | Oracle findings used to be written into instincts, QUEEN.md and observations automatically when a loop finished; now the wrapper only 'offers' `aether oracle promote` and it runs solely if the user says yes. | .claude/commands/ant/oracle.md:142-148 'offer promotion' with `aether oracle promote --dry-run` / `aether oracle promote`; cmd/oracle_promote.go:32 runOraclePro |
| low | degraded | – | hive-abstract (generalise repo-specific text before hive store) | Abstraction shrank from four transforms (absolute paths, repo name, version numbers, branch names) to repo-name/prefix replacement only, so paths, versions and branch names now leak into cross-colony wisdom. | cmd/hive.go:541-551 (hive-abstract RunE) and cmd/hive.go:585-600 (promoteToHiveWithReference) do only strings.ReplaceAll(sourceRepo -> RepoPlaceholder) and pref |
| low | degraded | – | seal pheromone-export-xml | Seal used to export pheromones.xml automatically; HEAD seal exports nothing, only entomb writes colony-archive.xml. | no export in cmd/seal_final_review.go / cmd/shelf_seal.go / cmd/codex_workflow_cmds.go completeSealRuntime; only cmd/entomb_cmd.go:121 exportArchiveXMLToFile |
| low | degraded | – | queen-init on init/seal/entomb | Old init, seal and entomb ensured the hub QUEEN.md existed before promotion; HEAD only lay-eggs creates a repo-local QUEEN.md and queen-init is manual. | cmd/queen.go:47 cobra only, 0 wrapper callers; cmd/setup_cmd.go:97 creates QUEEN.md on setup only; cmd/init_cmd.go has no QUEEN.md creation |
| low | degraded | – | event-bus cleanup (janitor / _event_cleanup) | Expired-TTL event removal runs only if someone types `aether event-bus-cleanup`; no lifecycle path, hook, or curation ant calls Bus.Cleanup, so the JSONL event log grows without bound. | pkg/events/bus.go:256 Cleanup() has exactly one caller, cmd/eventbus.go:178 (the CLI); grep of cmd/*.go, pkg/agent/*.go, .claude/settings.json finds no other ca |
| low | degraded | – | oracle compute_trust_scores (trust_summary / multi-source ratio) | The per-run trust summary (fraction of findings backed by more than one source) is no longer computed; HEAD records source_count per finding but never rolls it up or acts on it. | cmd/oracle_loop.go:1183,1209,1318 store source_count only; grep trust/multi/corrobor across cmd/oracle_*.go finds no summary or threshold use |
| low | degraded | – | swarm-cleanup --archive | Swarm findings were archived automatically at the end of every swarm; now they are either left forever or destroyed by a manual command with no archive option. | cmd/swarm.go:75-132 swarm-cleanup only os.RemoveAll's swarms/<id>; no --archive flag; .claude/commands/ant/swarm.md:145 lists it as an optional manual command;  |
| low | degraded | – | spawn-complete failed-spawn record in colony state / activity.log | A failed helper no longer leaves a `spawn_failed` event in COLONY_STATE or a line in activity.log; only an event-bus ceremony message is published. | cmd/spawn.go:177-290 spawn-complete updates spawn-tree.txt and calls emitSpawnTreeCeremony (:236) only; cmd/spawn.go has no activity.log write (grep) and no sta |
| low | degraded | – | .aether/data/CROWNED-ANTHILL.md | The seal record is written to .aether/CROWNED-ANTHILL.md (not .aether/data/); no wrapper displays its content any more (v5.3.3 tunnels printed the full document), and its content is never fed into the next colony's briefs — only entomb reads it, to gate and to return seal_summary in the chamber deta | writer cmd/codex_workflow_cmds.go:828 (filepath.Join(aetherDir, "CROWNED-ANTHILL.md")); seal visual prints only the path cmd/codex_visuals.go:3304; readers cmd/ |
| low | dropped | ✓ | focus | CONTEXT.md constraint tracking no longer runs after a focus. The Go port exists — `aether context-update --section constraint --key focus --content <text>` (cmd/context.go:1733 -> runContextSectionUpdate, cmd/context_update.go:522) appends to the Active Constraints table — but nothing on the focus p | .claude/commands/ant/focus.md (no context step); cmd/context.go:1717 contextUpdateCmd — grep finds no non-test caller |
| low | dropped | ✓ | lay-eggs (registry-add) | Repo is no longer registered in the hub registry at setup time (lay-eggs → runSetup has no registry call). Registration now happens only at init (cmd/init_cmd.go:373) and seal (cmd/codex_workflow_cmds.go:851); entomb (cmd/entomb_cmd.go:145) only updates an existing entry and no-ops otherwise. `aethe | cmd/setup_cmd.go:44-197 has no registry call; upsertColonyRegistryEntry callers are only cmd/init_cmd.go:373 and cmd/codex_workflow_cmds.go:851 |
| low | dropped | ✓ | interpret (activity-log + next-up) | Interpret no longer records an INTERPRET entry in activity.log nor prints a state-based Next Up block; there is no aether interpret command and no hook path for it. | .claude/commands/ant/interpret.md:7-15 (pure prompt, no CLI call); logActivity exists at cmd/codex_colonize.go:90-101 but has no interpret caller |
| low | dropped | ✓ | phase: `list` / `all` summary view | The grouped all-phases view (per-phase id/name listed under Completed / In Progress / Pending) no longer exists anywhere; `aether phase` resolves exactly one phase (cmd/phase.go:18,33-43). Only the aggregate "N/M phases complete" figure survives, relocated to `aether status` (cmd/status.go:840-853). | cmd/phase.go:35-52 resolves exactly one phase and errors otherwise; no list mode |
| low | dropped | ✓ | phase: task dependencies and success criteria in the view | Single-phase view (`/ant-phase` → `aether phase`) no longer shows per-task 'depends on' or phase/task success criteria; that information is still rendered only in the full plan output (`aether plan`, renderPlanVisual), not on the phase view. | cmd/phase.go:66-97 buildPhaseResult emits only id/goal/status per task and no criteria; cmd/codex_visuals.go:4110-4160 renders nothing else |
| low | dropped | – | quick | Quick no longer records the scout in the spawn log / spawn tree nor touches session.json, so /ant-watch, history and the session card never see a quick mission. | cmd/command_truth.go:224-282 runQuickScout — no spawn-log/updateSessionSummary call; wrapper .claude/commands/ant/quick.md:11 explicitly forbids them and nothin |
| low | dropped | ✓ | redirect | Redirect no longer writes the CONTEXT.md constraint entry: v5.3.3 redirect.md:44 called `context-update constraint redirect`, while HEAD's wrapper (.claude/commands/ant/redirect.md:9) only runs `aether redirect`, which creates a pheromone signal (cmd/codex_workflow_cmds.go:959-985) and never invokes | cmd/codex_workflow_cmds.go:959-985 (no context-update call); cmd/context_update.go defines the command with no redirect caller |
| low | dropped | – | resume | Codebase drift note ("N commit(s), M file(s) modified since last session") is gone; only a boolean git_match is computed and it is never rendered. | cmd/session_flow_cmds.go:65 result.GitMatch computed; :270 emitted as freshness.git_match JSON only; grep git_match/GitMatch in cmd/codex_visuals.go returns not |
| low | dropped | – | resume-colony | The colony_resumed event is no longer appended to COLONY_STATE.json events, so history/archaeology cannot see session boundaries. | cmd/session_flow_cmds.go:200-231 (state saved without an event append; grep colony_resumed in cmd/ returns nothing) |
| low | dropped | – | seal | Seal-time XML exports (pheromones.xml, queen-wisdom.xml, colony-registry.xml for cross-colony sharing) are gone from seal; only colony-archive.xml survives and only at entomb. | cmd/codex_workflow_cmds.go:772-880 (no export); cmd/entomb_cmd.go:121 exports colony-archive.xml only; wisdom-export-xml/registry-export-xml live in cmd/alias_c |
| low | dropped | – | seal | The Sage analytics review (velocity / bug-density / review-turnaround trends, top recommendations written to midden) at seal is gone; sage is not a seal-selectable caste. | cmd/caste_relevance.go:583-595 isAlwaysRequired seal (gatekeeper/auditor/probe only); cmd/seal_final_review.go:1100-1111 sealFinalReviewSpecForCaste has porter/ |
| low | dropped | – | seal | Seal no longer logs to activity.log (activity-log MODIFIED Queen "Colony sealed"); the command exists but seal does not call it. | cmd/codex_workflow_cmds.go:772-880 (no activity-log); cmd/session.go:12 defines activity-log |
| low | dropped | – | swarm: SWARM_SUCCESS / SWARM_FAILED activity-log entries | Swarm outcomes are no longer written to activity.log; only the spawn-runs record exists. | cmd/swarm_cmd.go:267 beginRuntimeSpawnRun only; no activity-log write in swarm_cmd.go |
| low | dropped | – | swarm-cleanup --archive | Findings can no longer be archived on cleanup; HEAD swarm-cleanup only deletes the directory. | cmd/swarm.go:74-129 os.RemoveAll only, no --archive flag |
| low | dropped | – | status: dreams count and latest | Status no longer shows the dream journal count / latest timestamp. | cmd/status.go: no dreams reference in renderDashboard (786-1080) |
| low | dropped | – | scan.sh _scan_survey_status | init-research no longer reports whether a territory survey exists or is stale, so the old init-time hint 'No territory survey found, run colonize before planning' is not data-driven any more. | cmd/init_research.go has no survey/has_survey/is_stale field (grep survey -> none); init.md:26,335 lists /ant-colonize as a generic routing option without check |
| low | dropped | – | chamber-compare `diff` (content-level new decisions/learnings) | Comparing two chambers no longer lists which decisions and learnings are new in the later colony; only counts survive. | cmd/entomb_cmd.go:329-370 compareChambers returns decisions_diff/learnings_diff as integers from chamberStateMemorySummary (:372); no per-item diff; `diff`/`sta |
| low | dropped | – | .aether/data/build/phase-N/outcome.md | No per-phase human-readable outcome file exists at HEAD; the closest v5.3.3 equivalents (HANDOFF.md written at build-complete and read at next build-prep, CONTEXT.md activity log) are gone from the runtime path — HANDOFF.md is now only produced by /ant-pause and consumed by /ant-resume, never by bui | grep 'outcome.md' in cmd/, pkg/, ts-host/src, .claude/commands/ant = 0 hits at HEAD (and none at v5.3.3 either). HANDOFF.md at HEAD: cmd/codex_visuals.go:639-64 |
| low | orphaned | ✓ | build: graveyard (grave-add / grave-check) | Failure graves for files a failed worker touched are never recorded or checked; the subcommands exist but nothing calls them. | cmd/grave.go:37,81 CLI only; no wrapper or Go call site |
| low | orphaned | ✓ | continue/build: midden-collect and midden-cross-pr-analysis | Cross-PR failure analysis and midden collection into worker/verification context no longer run anywhere. | cmd/midden_cmds.go:224,303 CLI only; no wrapper or Go caller |
| low | orphaned | – | seal | The interactive wisdom review (learning-check-promotion + learning-approve-proposals tick-to-approve) is no longer run at seal; both Go commands exist with no runtime or wrapper caller, replaced by the automatic consolidation loop. | cmd/seal_confirmation.go:302 runSealWisdomReview (automatic); cmd/learning.go:95 and cmd/learning_cmds.go:16 defined; grep learning-approve-proposals across cmd |
| low | orphaned | – | seal | crowned-anthill.template.md ships in .aether/templates but the runtime builds CROWNED-ANTHILL.md from Go strings and never reads the template; the old per-phase recap and colony-age fields are gone and "Completed phases" reports len(all phases) even on a force-seal with unverified phases. | cmd/codex_workflow_cmds.go:1379-1392 buildSealSummary (Completed phases: len(state.Plan.Phases), no recap/age); cmd/codex_visuals.go:3303 same count; grep crown |
| low | orphaned | – | learning-extract-fallback (recover learnings when workers report none) | The fallback that scraped learnings from worker output when none were reported is a CLI with no caller. | cmd/learning_cmds.go:167 registered; no reference in cmd/codex_continue*.go or any wrapper/yaml/agent file |
| low | orphaned | – | trust-score-compute / trust-score-decay / trust-tier and instinct-read-trusted | Trust scoring is only exercised through the observation service (which no live path calls) and the trust CLIs; the worker prompt injects every non-archived instinct with no trust filter, so the trust axis never affects what a worker sees. | cmd/trust.go:18-84 and cmd/instinct.go:156-200 registered, no non-test caller; cmd/colony_prime_context.go:609-617 loads instincts.json and skips only inst.Arch |
| low | orphaned | – | chamber-create / chamber-verify / chamber-list / chamber-compare | All four chamber subcommands are uncalled; entomb writes chambers in-process (cmd/entomb_cmd.go:59) so archiving works, but chamber-compare (diffing an archived colony against the live one) is unreachable. | cmd/chamber.go:17,79,126,178; no wrapper/YAML/host/Go caller |
| low | orphaned | – | activity-log-init / activity-log-read | The activity log is appended to by four wrappers but nothing ever initialises or reads it back via these subcommands. | cmd/session.go:56 and cmd/session.go:77; only `aether activity-log` (write) appears in .claude/commands/ant/{chaos,dream,archaeology,organize}.md |
| low | orphaned | – | codebase-scan / codebase-query | The code-graph scan/query commands (runCodebaseScan/runCodebaseQuery) have no caller anywhere, so the dependency graph is never built or consulted. | cmd/codegraph.go:22 and cmd/codegraph.go:32; runCodebaseScan/runCodebaseQuery have zero non-test callers |
| low | orphaned | – | flag-auto-resolve | Age-based flag housekeeping is never triggered by any runtime path. | cmd/flag_cmds.go:340 — only allowlisted in cmd/testdata/orphan_allowlist.json |
| low | orphaned | – | hive-read | CLI hive-read is uncalled (hive-injector.js exists in ts-host/src but no .ts imports it); cross-colony wisdom reaches workers only via the in-process readHiveWisdom call in colony-prime. | cmd/hive.go:471; in-process reuse cmd/context.go:816; .aether/ts-host/src/hive-injector.js:52 has no importer |
| low | orphaned | – | hive-promote | CLI hive-promote has no caller; seal promotes instincts in-process via promoteToHiveWithReference instead, and entomb prints 'run aether hive-promote' as advice nobody executes. | cmd/hive.go:695; in-process cmd/codex_workflow_cmds.go:712; suggestion-only cmd/entomb_cmd.go:182 |
| low | orphaned | – | error-add / error-flag-pattern / error-summary / error-pattern-check | Error record pipeline in COLONY_STATE.json is never fed or summarised; error-pattern-check is additionally marked Deprecated. | cmd/error_cmds.go:19,131,234,273 (Deprecated at :276); only scripts/classify_commands.py:43 names error-pattern-check in a list |
| low | orphaned | – | export (pheromones/wisdom/registry/archive) | The XML `export` command tree is uncalled; /ant-export-signals runs the separate `export-signals` command, so wisdom/registry/archive export has no path. | cmd/exchange.go:23 (+subcommands added :36-39); wrappers reference only `aether export-signals` (.claude/commands/ant/export-signals.md:9) |
| low | orphaned | – | pending-decision-add / pending-decision-list / pending-decision-resolve | The three pending-decision subcommands have no caller; the decision flow now goes through handoff-decisions/decision-answer/discuss instead, leaving this CLI dead. | cmd/pending_decision.go:52,106,171; zero calls in wrappers/yaml/ts-host/Go non-test. Would: append/list/resolve entries in .aether/data/pending-decisions.json. |
| low | orphaned | – | learn-export / learn-import | Portable learning-pack export/import has no caller on any surface. | cmd/learn_export.go:12,44; no references outside testdata. Would: write/read a portable pack of learning entries. |
| low | orphaned | – | pr-context | CI agent context assembly has no caller. | cmd/context.go:710; zero calls. Would: assemble PR/CI agent context from multiple data sources. |
| low | orphaned | – | session-init / session-read / session-update / session-mark-resumed | Session file lifecycle CLIs are uncalled; old init/resume/build wrappers used them (session-update 9x). HEAD writes session summaries internally via updateSessionSummary, so behaviour is degraded to whatever that covers, not lost. | cmd/session_cmds.go:79,138,188,449 definitions; only session-clear and session-verify-fresh are called (.claude/commands/ant/medic.md:28-30); cmd/codex_workflow |
| low | orphaned | – | review-ledger-read / review-ledger-summary / review-ledger-resolve | Ledger is written by workers (review-ledger-write is injected into watcher/chaos/measurer/archaeologist briefs) and read internally at seal/colony-prime, but the read/summary/resolve CLIs have no caller, so findings can never be marked resolved. | cmd/review_ledger.go:279,346,386 definitions; write side live via cmd/codex_build.go:1706 findingsInjectionForCaste; internal readers cmd/seal_final_review.go:7 |
| low | orphaned | – | skill-patch / skill-archive / skill-pin / skill-list-lifecycle / skill-promote / skill-view / skill-curator-run / skill-recover / skill-index-read / skill-manifest-read / skill-is-user-created | The whole skill lifecycle (Keeper Curator, archive/pin/promote/recover) is CLI-only with no wrapper, agent or runtime caller; only skill-create is wired. | cmd/skill_lifecycle.go:51,77,101,125,150,180; cmd/skill_curator.go:24,53; cmd/skills.go:151,176,205 definitions; only skill-create called (.aether/commands/skil |
| low | orphaned | – | shelf-promote-batch / shelf-dismiss-batch / shelf-detect | Batch shelf promotion/dismissal and seal-time shelf-candidate detection have no caller; init.yaml:51 says `aether init` does promotion itself, and seal never runs shelf-detect. | cmd/shelf_init.go:20,75; cmd/shelf_seal.go:13 definitions; only .aether/commands/init.yaml:51 guardrail prose; on orphan_allowlist.json |
| low | pointer_only | ✓ | dream | Dream's activity logging (dream.md:232 -> cmd/session.go:41) writes to activity-log.jsonl, while dream.md:52 Step 1 reads .aether/data/activity.log (written only by init_cmd.go:356 and codex_colonize.go:101), so dream sessions never appear in the Dreamer's own "what has been happening" context on la | .claude/commands/ant/dream.md:246 `aether activity-log` -> cmd/session.go:41 AppendJSONL("activity-log.jsonl"); dream.md Step 1 reads `.aether/data/activity.log |
| low | pointer_only | – | workers.md learning pipeline description | The shipped worker doc still tells workers that observations auto-promote to instincts and names memory-capture/instinct-create/queen-promote as the pipeline, but those steps only run by hand now; the doc's claim that hypothesis entries are 'handed to phase-end consolidation' is false — consolidatio | .aether/workers.md:828-836; cmd/consolidation_lifecycle.go:111-186 runPhaseEndConsolidation never touches learn.NewColonyStore; pkg/memory/consolidate.go:107 re |
| low | pointer_only | – | /ant-organize reads `## Keeper` from workers.md | The organize wrapper instructs the assistant to extract a `## Keeper` section from workers.md that does not exist, so the Keeper role definition never reaches the worker (pre-existing in v5.3.3, unchanged). | .claude/commands/ant/organize.md:60 reads `## Keeper` from ~/.aether/system/workers.md; .aether/workers.md has no `## Keeper` heading (only a mention at :670). |
| low | pointer_only | – | .aether/data/build/phase-N/continue.json (advanced case) | For a phase that advanced, the continue report's content (Summary, WorkerFlow, OperationalIssues, PartialSuccess, ClosedWorkers) is never read; the file's existence/mtime is the only thing consumed. | writer cmd/codex_continue.go:1123-1135, cmd/codex_continue_finalize.go:1304-1315. Readers: loadActiveRecoveryGuidance cmd/recovery_snapshot.go:220 returns nil w |
| low | pointer_only | – | previous phase verification.json / review.json → next build brief | The prior phase's verification.json and review.json contents never reach the next phase's build brief; the only carry-forward is the domain review ledgers (open findings) and worker handoffs — the verification and review reports themselves are read only by continue-side owner confirmation and the se | writers cmd/codex_continue_finalize.go:315,557; readers cmd/criterion_owner_confirmation.go:121 (continue path), cmd/seal_final_review.go:885 (seal); build brie |
| low | unwired | – | .aether/data/build/phase-N/gates.json | Per-phase gate report is written on both continue lanes and never read by anything; its only 'reader' is the cleanup that deletes it. | writers cmd/codex_continue.go:858-864, cmd/codex_continue_finalize.go:316-322; no LoadJSON of gates.json anywhere (grep); only os.Remove at cmd/codex_build.go:3 |

## autopilot_stamina (17)

| sev | status | chk | subject | claim | evidence |
|---|---|---|---|---|---|
| high | dropped | ✓ | insert-phase | The zero-flag guided flow (ask what is broken, what to accomplish, constraints; derive 'Stabilize ...' name; insert after current_phase) is gone — runtime demands --after/--name/--description and the wrapper passes raw $ARGUMENTS with no instruction to collect or translate them, so `/ant-insert-phas | cmd/state_extra.go:119-138 (Args: cobra.NoArgs, mustGetString name/description, --after 0-based index); .claude/commands/ant/insert-phase.md:8-9 (just `aether i |
| high | orphaned | – | flag-check-blockers | No wrapper, hook, host or Go path runs it; the pre/post-build blocker-count gate that v5.3.3 autopilot and status used is gone. | cmd/flag_cmds.go:220 — no caller outside _test.go and cmd/testdata/orphan_allowlist.json ('unreviewed-pre-existing') |
| medium | dropped | ✓ | redirect | The --ttl option on /ant-redirect is gone from the user-facing path: the wrapper passes "$ARGUMENTS" as one quoted arg to `aether redirect`, which takes ExactArgs(1), defines no flags, and hard-codes ttl="" (cmd/codex_workflow_cmds.go:969), so a typed ttl is stored as part of the signal text. TTL pa | cmd/codex_workflow_cmds.go:884 redirectCmd = newSignalShortcutCommand; :959-985 ExactArgs(1), no flags, ttlFlag passed as "" at :969; wrapper .claude/commands/a |
| medium | orphaned | – | autopilot-init / autopilot-update / autopilot-status / autopilot-stop / autopilot-set-headless / autopilot-headless-check / autopilot-resume | The seven autopilot state-file subcommands are called by nothing; `aether run` (cmd/compatibility_cmds.go:232) runs in-process and never touches them, so stop/resume/headless state is dead surface. | cmd/autopilot.go:109,148,232,269,304,336,359; zero hits in wrappers, YAML, settings.json, ts-host, Makefile, scripts, non-test Go |
| medium | orphaned | – | failure-classify | Failure classification is reachable only via TS host escalation.js, which is imported solely by lifecycle.ts, and `aether host lifecycle` has no caller. | cmd/recovery_classify.go:191; .aether/ts-host/src/queen/escalation.js:32 <- queen/orchestrator.ts:134 <- lifecycle.ts:43; `host lifecycle` (cmd/host_cmd.go:43)  |
| medium | orphaned | – | medic-auto-spawn-check | The check that decides whether Medic should auto-spawn is never executed by any live path. | cmd/medic_auto_spawn.go:161; no wrapper, hook, ts-host or Go non-test caller. Would: evaluate colony health signals and report whether a Medic dispatch is warra |
| medium | orphaned | – | spawn-track | Its only caller is .aether/utils/hooks/timeout-guard.js, which is not registered in .claude/settings.json and not exec'd by any Go path, so the ratchet credits it while nothing runs it. | cmd/spawn_track.go:25 definition; .aether/utils/hooks/timeout-guard.js:60 `aether spawn-track --action clear`; .claude/settings.json:9-84 registers only hook-pr |
| medium | unwired | – | .aether/data/worker-debug/*.json | Hosted-worker failure dumps (stdout/stderr excerpts, exit code, failure_mode) are written and pruned but never read by any command, prompt, recovery flow or the session card; only the file path is echoed in the error. | writer pkg/codex/platform_dispatch.go:1379-1416 (writeHostedWorkerOutputDebug), called at :1134,1146,1160,1168 returning only relPath for the error message; the |
| low | degraded | ✓ | colonize: interactive path depends on the TS host | Unlike build and continue, the interactive colonize wrappers (Claude and OpenCode) obtain their manifest only via `aether host colonize`, which requires node plus a built ts-host dist (repo-local .aether/ts-host/dist/host.js or the hub's ~/.aether/system/ts-host/dist/host.js) and its node_modules; i | .claude/commands/ant/colonize.md:12 and Guardrails 'Do NOT bypass aether host colonize'; cmd/host_cmd.go:107-129 (discoverNode, prepareTsHostForExec) |
| low | degraded | ✓ | flag | Manual blocker flags no longer auto-resolve on build_pass; only machine sources (verification/watcher/escalation/build/continue/medic) clear, so a user-raised blocker must be resolved by hand (intentional, but the wrapper's old 'Auto-resolved: blockers auto-resolve on build_pass' promise is silently | cmd/flag_cmds.go:399-415 autoResolveMachineSources excludes user-raised sources |
| low | degraded | – | seal | Incomplete phases used to warn-and-allow after a yes/no; now they hard-block unless the owner reruns with --force --reason, so a colony finished outside the tool needs two extra steps. | cmd/seal_final_review.go:376-378 returns error without --force; .claude/commands/ant/seal.md:31-41 |
| low | dropped | – | resume | The workflow-step BLOCK guards (no plan yet / plan has no phases / build interrupted) that stopped the dashboard and redirected the user are gone; resume always renders and only suggests. | cmd/context.go:115-395 buildResumeDashboardResult never early-returns on plan state; cmd/session_flow_cmds.go:177-330 |
| low | dropped | – | swarm: 3-strike 'architectural concern' escalation | Repeated swarm failures on the same issue no longer trigger the stop-and-rethink notice; each run is independent. | cmd/swarm_cmd.go: no attempt counter or prior-swarm lookup (grep attempt/architectural: none); result.json written per swarmID only (:324-336) |
| low | dropped | – | status: escalated-flags count | The 'Escalated: N task(s) awaiting your decision' line is gone even though flags with source=escalation still exist. | cmd/status.go:1367-1387 countFlags counts by severity only; cmd/flag_cmds.go:409 still accepts source "escalation" |
| low | orphaned | – | immune.sh (trophallaxis-diagnose/retry, scar-add/list/check, immune-auto-scar) | The immune-system retry/scar machinery is ported to Go but has no runtime caller (it had none in v5.3.3 either — pre-existing orphan, not a regression). | cmd/immune.go:30,92,129,177,200,243 cobra commands; diagnoseError (cmd/immune.go:46) has no non-test caller |
| low | orphaned | – | scar-add / scar-list / scar-check / trophallaxis-diagnose / trophallaxis-retry | Immune-system commands (record failure pattern, check command against scars, diagnose+retry) have no caller, so recorded scars can never steer a retry. | cmd/immune.go:30,92,129,177,200 definitions; zero callers; on orphan_allowlist.json |
| low | orphaned | – | recovery-log-read / recovery-log-write | Recovery log entries are never written or read by any live path. | cmd/recovery_classify.go:235,264 definitions; zero callers; on orphan_allowlist.json |

## speed (4)

| sev | status | chk | subject | claim | evidence |
|---|---|---|---|---|---|
| medium | unwired | ✓ | phase: numeric argument `/ant-phase 3` | The wrapper passes `$ARGUMENTS` positionally, but the Go command accepts no positional args and only reads --number, so `/ant-phase 3` errors instead of showing phase 3. | .claude/commands/ant/phase.md:9 `aether phase $ARGUMENTS`; cmd/phase.go:18 `Args: cobra.NoArgs`, cmd/phase.go:60 flag `--number`; the runtime's own recovery tex |
| low | degraded | ✓ | quick | Quick scout runs as a headless CLI subprocess via codex.NewWorkerInvoker with a 10-minute default timeout instead of a visible Task agent; the pre-spawn "Spawning {scout_name}" announcement and spawn-log are gone, though a deterministic scout name and a "⚡ Quick Scout" banner are still printed post- | cmd/command_truth.go:20 newQuickWorkerInvoker = codex.NewWorkerInvoker; :122 default worker timeout; .claude/commands/ant/quick.md:13 "no spawning ceremony is s |
| low | orphaned | – | oracle-iterate / oracle-iterate-finalize | Both TS-host Oracle iteration commands are referenced only from the TS host oracle lane (command-registry.ts:216-217, host.ts:1502), and `aether host oracle` has no caller — /ant-oracle runs the Go RALF loop via `aether oracle`. | cmd/oracle_iterate_cmd.go:90,190; no `host oracle` invocation in wrappers/yaml/scripts. Would: emit an Oracle iteration manifest and commit its results. |
| low | orphaned | – | should-skip-gate | Gate-skip decision based on prior results is never consulted, so every gate re-runs from scratch. | cmd/gate.go:1408 definition; zero callers; on orphan_allowlist.json |

## safety (57)

| sev | status | chk | subject | claim | evidence |
|---|---|---|---|---|---|
| high | degraded | – | hooks/clash-pre-tool-use.js (edit-time clash block) | The per-edit PreToolUse hook that blocked an Edit/Write on a file another worktree had already modified no longer runs on any path; clash detection now happens only at merge-back, after the overwrite has already been written. | .claude/settings.json registers only `aether hook-pre-tool-use`; cmd/hook_cmds.go has zero clash references; .aether/utils/hooks/clash-pre-tool-use.js still shi |
| high | dropped | ✓ | entomb | User confirmation gate ('Entomb this colony? yes/no' AskUserQuestion) removed; `aether entomb` archives, resets state and deletes pheromones/flags/constraints with no prompt and no --yes flag. | .claude/commands/ant/entomb.md:10 executes `aether entomb $ARGUMENTS` directly; cmd/entomb_cmd.go:25 Args NoArgs, no confirm flag; cmd/entomb_cmd.go:132-141 res |
| high | dropped | ✓ | patrol: plan-vs-reality task evidence verification | The pre-seal audit that checked every completed task's claimed files/commands actually exist in the codebase (VERIFIED/UNVERIFIABLE/MISSING per task) no longer runs anywhere on the patrol path. | cmd/patrol_check.go:54-88 runs only runJSONValidityCheck, runStalePheromonesCheck, runInterruptedBuildsCheck; .claude/commands/ant/patrol.md:9-10 lists only tho |
| high | dropped | ✓ | patrol: unresolved flags + recurring midden review | Patrol (`aether patrol-check`) no longer reads open blocker flags or midden failures, so a colony with unresolved blockers reports overall_status healthy. Note: unacknowledged midden entries still surface as a warning in `aether status` (cmd/status.go:146-157), so midden review is moved/degraded rat | cmd/patrol_check.go:54-88 never reads flags.json or midden; flag-list (cmd/flags.go:19) and midden-recent-failures (cmd/midden_cmds.go:18) exist but are not on  |
| high | dropped | ✓ | patrol: test-suite run and test-file census | Patrol no longer runs the project's tests or counts test files (cmd/patrol_check.go runs only JSON-validity, stale-pheromone and interrupted-build checks, with no exec); the wrapper's instruction to relay runtime-reported test failures is dead because the runtime never produces them. A failing suite | cmd/patrol_check.go contains no exec of any test command; wrapper patrol.md:14 says to 'relay' test failures the runtime reports, but the runtime never produces |
| high | dropped | – | swarm: git checkpoint before fix + rollback on failed verification | Swarm no longer snapshots the tree before a builder edits it and never rolls back a failed fix; autofix-checkpoint is now only called by medic. | cmd/swarm_cmd.go:251-350 runSwarmDestroy and :628-720 runSwarmFinalize contain no checkpoint/rollback; only caller of createAutofixCheckpoint is cmd/medic_repai |
| high | orphaned | – | verify-claims | Classified as a safety gate ('unexecuted, nothing checks that the reported work happened') yet no wrapper, hook, host or Go path ever runs it. | cmd/verify_claims.go:10 (definition); cmd/command_call_severity.go:39 names it a gate; grep of .claude/commands/ant, .opencode/commands/ant, .aether/commands, . |
| medium | degraded | – | insert-phase | Inserted phase has an empty task list (old seeded two tasks 'Diagnose and correct' + 'Validate end-to-end' with success criteria and phase-level success criteria), and downstream task IDs / dependencies ('N.M') are not remapped when phase IDs shift — only phase.ID is renumbered. | cmd/state_extra.go:154-159 (Tasks: []colony.Task{}), :172-181 (renumbers phase IDs only) |
| medium | degraded | – | import-signals | ID-collision policy dropped: old merge grouped by id with the current colony winning; HEAD appends every sanitized signal, so re-importing the same XML duplicates signals (only content-hash dedup in pheromone-write exists, and import bypasses it). | cmd/exchange.go:464-472 (file.Signals = append(file.Signals, sanitized...)) |
| medium | degraded | – | resume | /ant-resume is now a plain alias of resume-colony: the old read-only, drift-aware, block-guarded quick restore is gone and running it mutates state (clears paused, rotates spawn tree, deletes HANDOFF.md). | cmd/session_flow_cmds.go:177-180 Aliases: ["resume"]; :210-214 state.Paused=false + SaveJSON; :222 rotateSpawnTree; :311 removeHandoffDocument; .claude/commands |
| medium | degraded | – | swarm: build/test verification of the applied fix | The runtime never runs the project's build/test commands after the swarm fix; completion is taken from the watcher worker's self-reported status, so a worker claim becomes the swarm verdict. | cmd/swarm_cmd.go:1234-1277 summarizeSwarmOutcome derives status/solution from worker responses; no exec.Command / verification call anywhere in cmd/swarm_cmd.go |
| medium | degraded | – | update: dirty managed files stop-and-show | Locally modified managed files are silently counted as 'unchanged' unless --force; the user is never told which files were left stale. | cmd/install_cmd.go:373-376 preserveLocalChanges → result.skipped++ with no listing; cmd/update_cmd.go:217 message 'N copied, M unchanged' |
| medium | degraded | – | flag-check-blockers (autopilot pause on blocker flags) | Old /ant-run and /ant-status consulted flags.json blockers before proceeding; HEAD autopilot only checks pending-decisions.json blockers, so a blocker flag raised via `aether flag` no longer pauses autopilot. | cmd/flag_cmds.go:220 cobra only, 0 callers; cmd/autopilot.go:54-64 reads pending-decisions.json, no flags.json load in autopilot.go |
| medium | degraded | – | clash-detect PreToolUse hook (edit-time cross-worktree conflict block) | Conflicting edits across worktrees are no longer blocked before the edit; they only surface as a failed merge-back after the work is done. | .claude/settings.json PreToolUse Write/Edit runs `aether hook-pre-tool-use`; its Write/Edit branch cmd/hook_cmds.go:79-107 checks only protectedHookWriteReason/ |
| medium | degraded | – | .aether/schemas/pheromone.xsd + pheromone-validate-xml | XML schema validation of pheromone exchange files is now a stub that always reports valid, and none of the shipped XSDs are read by any runtime path. | cmd/pheromone_write.go:272-278 `pheromone-validate-xml` Short: "(placeholder)" and returns {"valid": true}; pkg/exchange/import.go:13-77 does xml.Unmarshal only |
| medium | dropped | – | build: persist worker blockers as flags | Worker-reported blockers are folded into a summary string instead of being persisted as blocker flags, so the blocker advisory on the next build never sees them. | cmd/codex_build_finalize.go:1361-1362 concatenates dispatch.Blockers into message; no flag creation in build finalize |
| medium | dropped | – | insert-phase | Sealed-colony guard removed: old wrapper refused insertion when milestone == Crowned Anthill; runtime now inserts into a sealed colony (shouldReopenInsertedPhase only declines to reopen it). | cmd/state_extra.go:140-150 (only after-index validated), :219-227 |
| medium | dropped | – | patrol: documentation-accuracy Watcher audit | The Watcher spawn that verified command/agent counts, version consistency, colony-rules command table and source-of-truth map is gone; patrol spawns no agent at all. | cmd/patrol_check.go has no spawn path; .claude/commands/ant/patrol.md contains no Task/spawn instruction; grep 'accuracy_score' cmd/ .claude/commands returns no |
| medium | dropped | – | patrol: completion-report.md + seal recommendation | The written completion report and the READY TO SEAL / ISSUES TO RESOLVE recommendation are gone; nothing at HEAD writes or reads completion-report.md. | .claude/commands/ant/patrol.md:13 forbids synthesizing completion-report.md; grep 'completion-report' cmd/*.go returns no hits |
| medium | dropped | – | resume | Both resume and resume-colony lost the sealed-colony guard: a Crowned Anthill colony is resumed and un-paused instead of stopping with "This colony has been sealed. Start a new colony". | cmd/session_flow_cmds.go:200-222 and cmd/context.go:115-260 contain no milestone/StateCOMPLETED check (grep Crowned/StateCOMPLETED only hits codex_visuals.go:21 |
| medium | dropped | – | seal | Pre-seal rolling state checkpoint is gone; state-checkpoint exists as a Go command but seal never calls it before overwriting COLONY_STATE.json. | cmd/codex_workflow_cmds.go:772-795 saves state with no checkpoint; cmd/state_extra.go:14 defines state-checkpoint, no seal caller |
| medium | dropped | – | seal | The already-sealed and still-executing guards are gone from the seal path: nothing checks milestone==Crowned Anthill or state==EXECUTING before re-running the ceremony and rewriting state/events. | cmd/seal_final_review.go:362-385 validateSealReady checks only plan/phases/blockers; grep StateEXECUTING in seal_final_review.go, seal_confirmation.go, codex_wo |
| medium | dropped | – | tunnels: chamber integrity (SHA256) verification | Chamber detail no longer shows 'Chamber integrity: verified/hash mismatch'; HEAD chamber-verify only checks manifest.json is valid JSON, entomb writes no hash, and tunnels never calls it. | cmd/chamber.go:78-124 (json.Valid + file list only, orphaned: no caller); cmd/entomb_cmd.go:493-514 manifest has no hash; cmd/entomb_cmd.go:235-273 detail view  |
| medium | dropped | – | update: checkpoint + automatic rollback on failure | Update performs no checkpoint before sync and no rollback on sync error; a partial sync is left in place and only an error count is reported. | cmd/update_cmd.go:133-160 outputError on sync errors, no restore; runUpdateSync :355-420 has no checkpoint ('checkpoints' appears only as a protected dir name : |
| medium | dropped | – | autofix-checkpoint / autofix-rollback around /ant-swarm | Swarm no longer snapshots before attempting a fix or rolls back when the fix fails verification. | .claude/commands/ant/swarm.md has no checkpoint/rollback step; cmd/swarm_cmd.go runSwarmDestroy (:251) and runSwarmFinalize (:628) never call createAutofixCheck |
| medium | dropped | – | chamber_create/chamber_verify SHA256 integrity | Chamber manifests no longer record a hash of the archived COLONY_STATE.json and verification is existence-only, so a corrupted or edited archive passes as intact. | cmd/entomb_cmd.go:493-514 writeEntombManifest has no `files`/hash field; cmd/entomb_cmd.go:652-664 verifyEntombedChamber only os.Stat()s four files; no sha256/S |
| medium | orphaned | – | continue: Gatekeeper antipattern scan (check-antipattern) and error pattern flagging | The secrets/debug-artifact scan and error-pattern flagging that ran on every continue are now CLI-only subcommands with no runtime or wrapper caller; a Gatekeeper is dispatched only on the five named risk signals, and even then nothing runs check-antipattern. | cmd/security_cmds.go:181 and cmd/error_cmds.go:131 registered at :485-492 / :341-346 only; no caller in cmd/codex_continue*.go; no wrapper invokes them |
| medium | orphaned | – | pause-colony: commit suggestion before pausing | The optional 'commit your work before pausing?' flow using a generated commit message is gone from pause; the Go generate-commit-message command still exists but nothing invokes it. | cmd/session_flow_cmds.go:76-134 pause path has no git/commit step; cmd/generate_cmds.go:81 defines generate-commit-message; grep across .claude/commands/ant/*.m |
| medium | orphaned | – | clash-setup / clash-check (cmd/clash.go) | The Go replacement for the clash installer (git merge driver `aether clash-check`) is never invoked by any wrapper, hook, or Go path, so the merge driver is never installed and clash-check never runs. | cmd/clash.go:18 (clash-check) and cmd/clash.go:86 (clash-setup) have no callers in .claude/commands, .aether/commands/*.yaml, ts-host/src, or other cmd/*.go; .c |
| medium | orphaned | – | clash-check / clash-setup | Worktree clash detection (which worker paths collide before merge) is never run; no hook, wrapper, or Go path invokes either subcommand. | cmd/clash.go:18 and cmd/clash.go:86; .claude/settings.json has no clash hook; parseWorktreePaths is reused by worktree.go but the clash check itself is not |
| medium | orphaned | – | privacy-scan | Secret/private-path scanning has no caller on any live path (Gatekeeper uses check-antipattern instead). | cmd/security_cmds.go:472; zero calls. Would: scan content for secrets and private paths. |
| medium | orphaned | – | validate-worker-response | Worker-response shape validation CLI has no caller; the old build path validated every worker return with it. | cmd/spawn.go:632 definition; no wrapper/hook/host caller; validateWorkerResponse( has 0 non-test Go callers |
| medium | orphaned | – | validate-state / unload-state / state-mutate / state-write / state-checkpoint / state-read-field | The entire state-mutation and validation CLI family is uncalled; old wrappers validated state 6x, checkpointed before waves and mutated via state-mutate. | cmd/state_cmds.go:22,950,960,1009; cmd/state_extra.go:14,49 definitions; only references are self (state_cmds.go:285 tracer label) and testdata; all six on orph |
| low | degraded | – | data-clean | Wrapper no longer runs dry-run first, shows counts, and asks 'Remove these artifacts? (yes/no)'; it forwards $ARGUMENTS blind and refers to 'the runtime-supported confirmation flag' without naming --confirm. | .claude/commands/ant/data-clean.md:10-11; flag defined at cmd/maintenance.go:328 only |
| low | degraded | – | redirect | Redirect no longer refuses when no colony is initialized; pheromone-write only reads COLONY_STATE.json for source_phase and writes the signal regardless. | cmd/pheromone_write.go:110-113 (state load is best-effort, no goal check); no errNoColonyInitialized in the file |
| low | degraded | – | tunnels: import preview + confirmation | --import-signals imports immediately with no signal-count preview or yes/no gate. | cmd/entomb_cmd.go:216-224, 407-428 importSignalsFromChamber runs unconditionally |
| low | degraded | – | update: dry-run preview before apply | The wrapper no longer previews with --dry-run before applying; the flag exists but nothing sets it on the /ant-update path. | .claude/commands/ant/update.md:11-12 (runs `aether update $ARGUMENTS` directly, forbids dry-run preview); cmd/update_cmd.go:48 flag only |
| low | degraded | – | session-verify-fresh on colonize / swarm / watch | The automatic stale-artifact check that colonize, swarm and watch wrappers ran before spawning is gone from those command paths; only medic.md still invokes it, and swarm/colonize Go paths have no freshness check of their own. | grep session-verify-fresh over .claude/commands/ant/*.md hits only medic.md:28-30; cmd/swarm_cmd.go has no stale/fresh handling; cmd/session_cmds.go:358 keeps t |
| low | dropped | – | migrate-state | The v1 six-file consolidation (PROJECT_PLAN.json, pheromones.json, memory.json, errors.json, events.json -> COLONY_STATE.json with backup-v1/) no longer exists; the runtime reads only COLONY_STATE.json and errors out on anything it cannot unmarshal, while the wrapper description still advertises 'v1 | cmd/command_truth.go:459-475 runMigrateState reads COLONY_STATE.json only, fails 'legacy state is not compatible with automatic migration'; no PROJECT_PLAN/back |
| low | dropped | – | skill-create | Post-write validation is gone: the frontmatter parse check and cache rebuild were deleted in Phase 191, so a malformed SKILL.md is only caught by the assistant re-reading it; the adjust path still tells the assistant to re-run those deleted commands. | .claude/commands/ant/skill-create.md:226-230 (Read-tool check, "no separate cache-rebuild step"); :261 still says "re-run skill-parse-frontmatter and skill-cach |
| low | dropped | – | tunnels chamber integrity line | Browsing a chamber no longer shows its integrity/hash status. | cmd/entomb_cmd.go:191-275 tunnels detail builds result from manifest + archive_xml presence only; no verify call. |
| low | orphaned | – | data-safety-stats | Go subcommand is registered but nothing calls it, status dropped the Data Safety panel, and its semantics changed from safety-event counts to file counts. | cmd/build_flow_cmds.go:255-300 (file/lock counts); registered :349; no caller in cmd/ or wrappers |
| low | orphaned | – | clash-setup | clash-setup now installs a git merge driver that nothing references, and nothing calls clash-setup anyway. | cmd/clash.go:86-113 sets merge.aether-clash.driver = `aether clash-check --file %A`; .gitattributes contains only `package-lock.json merge=lockfile`, no path us |
| low | orphaned | – | build-reconcile | build-reconcile (repair build attempt state, with --dry-run) has no caller; runBuildReconcile is invoked only from its own RunE. | cmd/build_reconcile.go:16; runBuildReconcile called only at cmd/build_reconcile.go:28 |
| low | orphaned | – | gate-check | The CLI subcommand has no caller; its logic survives only because state-mutate calls runGateCheck in-process. | cmd/gate.go:62 — no exec caller; in-process reuse at cmd/state_cmds.go:128,135 |
| low | orphaned | – | gate-results-read / gate-results-write | Gate result CLI read/write have no callers; writes happen in-process via gateResultsWrite from continue, so the read side is unreachable. | cmd/gate.go:1362, cmd/gate.go:1378 — no wrapper/host references; gateResultsWrite used in-process at cmd/codex_continue.go:810 |
| low | orphaned | – | grave-add / grave-check | Agent 'grave' registry (mark/check a dead agent) is never written or consulted by any dispatch path. | cmd/grave.go:38, cmd/grave.go:82 — zero references outside testdata |
| low | orphaned | – | data-safety-stats | Data integrity stats no longer shown; /ant-status dropped the call. | cmd/build_flow_cmds.go:256 — allowlist reason 'candidate to document in the data-clean wrapper or retire' |
| low | orphaned | – | force-unlock | Emergency lock release has no caller and no wrapper/hint points at it any more. | cmd/internal_cmds.go:21 — zero references outside testdata |
| low | orphaned | – | incident-rule-add | Adding a rule to decree/constraint/gate files has no caller. | cmd/internal_cmds.go:224; zero calls. Would: append a rule to a decree, constraint, or gate file. |
| low | orphaned | – | worktree-create / worktree-cleanup / worktree-allocate / worktree-list / worktree-orphan-scan / worktree-merge-back | Six worktree CLIs are uncalled; build allocates and syncs worktrees in-process (codex_build_worktree.go), so only the manual/inspection surface is dead, but worktree-list and worktree-orphan-scan are the only way to see orphaned branches and nothing offers them. | cmd/clash.go:119,184; cmd/worktree.go:126,256,333,691 definitions; only self-references (worktree.go:232 action label, :759 blocker source) and an error message |
| low | orphaned | – | signature-scan / signature-match | Regex/signature file scanners have no caller (check-antipattern is the live scanner). | cmd/security_cmds.go:241,345 definitions; zero callers; on orphan_allowlist.json |
| low | orphaned | – | survey-verify / validate-oracle-state | Survey integrity check no longer runs before continue's verification; validate-oracle-state targets the dead .aether/data/oracle/ store. | cmd/survey.go:51; cmd/state_extra.go:215 definitions; zero callers; allowlist marks validate-oracle-state 'superseded-2026-08-16' |
| low | pointer_only | – | .aether/data/porter/readiness.json | porter check writes a full integrityResult JSON but nothing ever loads it back; the seal/closeout 'porter_readiness' summary recomputes versions and git status live and only the file path is shown as 'Evidence'. | writer cmd/porter_cmd.go:242,260 (recordPorterReadinessEvidence); only other reference is displayDataPath at :242; buildPorterReadinessSummary cmd/porter_cmd.go |
| low | unwired | – | lay-eggs (platform settings/rules) | Wrapper lists 'User-owned platform settings' as read-only and says nothing is copied into .claude/, but the runtime setup merges hub settings into .claude/settings.json and copies rules into .claude/rules on every lay-eggs. | .claude/commands/ant/lay-eggs.md:58,61-75 vs cmd/platform_sync.go:94-95 (Settings (claude) merge, Rules (claude)) invoked from cmd/setup_cmd.go:104-131 |
| low | unwired | – | .aether/data/archive/manifest.json | session-verify-fresh's seal/entomb freshness entries point at .aether/data/archive/manifest.json, a file no Go code writes; entomb archives to .aether/chambers/<name>/ instead, so the freshness check for seal/entomb can never find anything. | cmd/session_cmds.go:56-62 (seal/entomb → dir .aether/data/archive, files manifest.json); no writer: grep '"archive", "manifest' / data/archive in cmd/ finds onl |
| low | unwired | – | .aether/data/swarm/{findings,display,timing}.json | The swarm freshness entry watches .aether/data/swarm/*.json but the runtime writes swarm artifacts under .aether/data/swarms/<id>/, so session-verify-fresh for swarm checks paths nothing produces. | cmd/session_cmds.go:49-50 (dir .aether/data/swarm); actual writer cmd/swarm_cmd.go:533,1190 and reader cmd/swarm.go:56 use "swarms/<id>/findings.json" |

## other (87)

| sev | status | chk | subject | claim | evidence |
|---|---|---|---|---|---|
| medium | dropped | – | seal | Seal never increments colony_version any more; nothing in the runtime bumps it (init sets 0, entomb resets 0) so the ceremony always prints "Crowned Anthill v0". | cmd/codex_visuals.go:3293 prints state.ColonyVersion; only writers are cmd/init_cmd.go:276 (0) and cmd/entomb_cmd.go:671 (0); no increment anywhere in cmd/ |
| medium | orphaned | – | flag-list | CLI flag listing has no caller; /ant-flags no longer invokes it. | cmd/flags.go:19 — zero references in .claude/.opencode/.codex/.aether/commands/settings.json/ts-host |
| medium | pointer_only | – | .aether/workers.md sub-spawn protocol (spawn-can-spawn / spawn-log / swarm-display-update from inside a worker) | The worker-side spawn protocol is not delivered to any worker prompt, points at an unloaded playbook, and its swarm-display-update invocation cannot succeed. | .aether/workers.md:319-404 still instructs workers to run these; workers.md is referenced only by cmd/install_cmd.go:240, cmd/platform_sync.go:333, cmd/source_c |
| medium | unwired | – | lay-eggs (wrapper leak check) | The wrapper's 'global asset leak' scan counts .aether/WHAT-IS-THIS.md and .aether/checkpoints/ — both created by the runtime itself — as leaks, so every fresh setup reports 2 false 'old global-asset paths detected'. | .claude/commands/ant/lay-eggs.md:123-135 allowlist is data/locks/dreams/oracle/QUEEN.md/skills; runtime writes checkpoints/ at cmd/platform_sync.go:889 and WHAT |
| medium | unwired | – | skill-create | The runtime spec says /ant-skill-create runs `aether skill-create $ARGUMENTS`, but that Go command creates a repo-local SQLite "pheromone skill" (requires --content) — a different feature — and the wrapper never invokes it; the wrapper is entirely LLM-driven with no runtime path. | .aether/commands/skill-create.yaml:5 command: aether skill-create $ARGUMENTS; cmd/skill_lifecycle.go:12-48 (learn.NewSkillService, --content/--pin); .claude/com |
| low | degraded | – | council: deliberation with Advocate/Challenger/Sage scouts | Deliberation no longer spawns three independent scouts; the wrapper drives council-advocate/challenger/sage, which merely store whatever text the Queen itself types, so all three 'positions' come from one model turn. | .claude/commands/ant/council.md:12-17; cmd/council.go:89-176 store --position/--challenge/--wisdom strings only |
| low | degraded | – | colonize: existing-survey overwrite/keep/merge choice | The three-way prompt (overwrite / keep / merge only outdated sections) is replaced by a hard error requiring --force-resurvey; partial re-survey is gone. | cmd/codex_colonize.go:125,336 and cmd/codex_colonize_finalize.go:141 error 'existing territory survey found; rerun with --force-resurvey' |
| low | degraded | – | entomb | Closing message no longer names /ant-lay-eggs or /ant-tunnels as next steps nor offers the three-way AskUserQuestion; runtime prints raw `aether init`/`aether tunnels` CLI names only. | cmd/entomb_cmd.go:923-925 renderNextUp with `aether init "next goal"` and `aether tunnels`; result next at cmd/entomb_cmd.go:179 |
| low | degraded | – | feedback | FEEDBACK is now written at strength 1.0 (same as FOCUS/REDIRECT) instead of 0.7, and the --ttl option is gone — the wrapper quotes "$ARGUMENTS" into a single ExactArgs(1) text so `--ttl 2h` becomes signal content. | cmd/codex_workflow_cmds.go:~1005 createPheromoneSignal(signalType, args[0], "user", "", "", 1.0, ""); Args cobra.ExactArgs(1); .claude/commands/ant/feedback.md: |
| low | degraded | – | export-signals | Default output file (.aether/exchange/pheromones.xml) and positional path are gone; with no --output the XML is dumped to stdout, and a positional path is silently ignored (no Args validation). Schema validation ('Validated: yes/no') line dropped. | cmd/alias_cmds.go:23-29 pheromoneExportXMLCmd has no Args spec; cmd/exchange.go:71-81 writes file only when --output set else prints to stdout; no validation in |
| low | degraded | – | flag | Type-derived severity (blocker=critical, issue=high, note=low) and the `-t`/`-p` short flags are gone; severity is a separate flag defaulting to 'high' for every type and is not even persisted on FlagEntry. | cmd/flag_cmds.go:31-34 severity default 'high' regardless of type, validated at :72-77 but absent from pkg/colony/flags.go:4-14 FlagEntry; cmd/flag_cmds.go:457- |
| low | degraded | – | focus | Focus signal strength changed from 0.8 to 1.0 and the stored reason 'User directed colony attention' is no longer written (reason "" → Reason nil); the old length/usage check moved to sanitizer but the 3-line confirmation no longer reports active signal counts (pheromone-count is a CLI nobody on the | cmd/codex_workflow_cmds.go:969 (strength 1.0, reason ""); cmd/codex_visuals.go:3317-3348 (no counts); cmd/pheromones_read.go:45 pheromone-count has no caller in |
| low | degraded | – | history | 'Showing X of Y events (filtered from Z total)' and 'Total events recorded' counts are gone — the visual prints only 'Recent events: N' for the truncated slice, so the user cannot see total history size. | cmd/history.go:102-118 (count = len(sliced)); cmd/codex_visuals.go:4204 (Recent events only) |
| low | degraded | – | flags | Default listing now includes resolved flags and the old --all/-a, -t, -p short flags are gone: with no --status the Go filter keeps Resolved entries, and `/ant-flags --all` or `-t blocker` (passed raw via $ARGUMENTS) errors as an unknown flag. | cmd/flags.go:65-68 (--type/--status/--json/--phase only), :72-90 (no status filter → resolved shown) |
| low | degraded | – | flags | Old `--resolve <id> "msg"` / `--ack <id>` shortcuts from the flags command are gone; HEAD wrapper only tells the assistant to translate to flag-resolve/flag-acknowledge, and a raw `/ant-flags --resolve ...` fails; also --phase N no longer includes phase-less (global) flags. | .claude/commands/ant/flags.md:9-10; cmd/flags.go:84 (entry.Phase == nil → excluded) |
| low | degraded | – | help | Help no longer produces a state-based Next Up (old ran print-next-up against COLONY_STATE); the HEAD overview is a static wrapper text that never calls the runtime, ending with 'ask what they're trying to do' — the runtime's next_action decision is not consulted. | .claude/commands/ant/help.md (no aether call in overview path; only per-command `aether command-guide`) |
| low | degraded | – | maturity (milestone semantics/version) | Milestone derivation changed from absolute completed-phase thresholds (1/3/5, Failed Mound on critical errors, stored Crowned Anthill honoured) to 25/50/75/100% ratios with no failure state, and the 'version' field now reports the Aether binary version instead of the colony's computed vMajor.Minor.P | cmd/command_truth.go:164-190 deriveMilestoneProgress ratio switch; cmd/command_truth.go:153 "version": resolveVersion(resolveAetherRoot()); same ratio logic dup |
| low | degraded | – | preferences: placeholder cleanup and section placement | Adding a preference no longer removes the '*No user preferences recorded yet.*' placeholder the shipped QUEEN.md template contains, inserts the bullet directly under the heading (before the '> ...' description line and any existing bullets, reversing order), and creates a missing section at end-of-f | cmd/codex_workflow_cmds.go:934-944 (insert right after header, append section at end, no placeholder removal, no stats); .aether/templates/QUEEN.md.template:12  |
| low | degraded | – | pheromones: `clear` reports before/after counts | The wrapper delegates clear to signal-housekeeping, which returns counts only as JSON the assistant must summarise; also housekeeping applies extra rules (worker:continue expiry after 3 phase completions) the old clear did not — acceptable, but the wrapper's one-sentence summary is model-authored, n | .claude/commands/ant/pheromones.md:11; cmd/signal_housekeeping.go:39 outputOK JSON only (no visual renderer) |
| low | degraded | – | redirect | Redirect confirmation lost the active-signal counts (N FOCUS / N REDIRECT / N FEEDBACK), the recorded reason, and the expiry line; strength changed from 0.9 to 1.0 with no reason stored. | cmd/codex_workflow_cmds.go:969 createPheromoneSignal(..., "", "", 1.0, "") reason empty; cmd/codex_visuals.go:3317-3348 renderSignalVisual prints only status/pr |
| low | degraded | – | seal | Wrapper promises the seal-finalize/host flow spawns Gatekeeper, Auditor and Probe; at default depth only Auditor (and Probe if testable code) are required, and at light depth none are — the wrapper's "Expected manifest" overstates the team. | .claude/commands/ant/seal.md:49 "dispatches: Gatekeeper, Auditor, and Probe"; cmd/caste_relevance.go:583-595 isAlwaysRequired seal |
| low | degraded | – | swarm: confidence-weighted solution ranking | Cross-scout ranking (#1/#2/#3 with confidence and supporting evidence) is gone; the first builder proposed_fix (or first summary) wins. | cmd/swarm_cmd.go:1234-1277 summarizeSwarmOutcome takes the first ProposedFix / first Summary |
| low | degraded | – | swarm --watch live display | Live view lost per-ant tool-usage counts, token consumption, chamber activity map and animated status; HEAD shows name/caste/task/status lists only. | cmd/swarm_cmd.go:1384-1440 renderSwarmCompatibilityVisual; cmd/swarm_cmd.go:232-249 spawnEntriesToWatchMaps exposes only name/caste/task/status/summary/timestam |
| low | degraded | – | tunnels: malformed / incomplete chamber reporting | Chambers with a bad manifest are silently skipped from the timeline instead of shown as 'Invalid manifest' / 'Incomplete chamber'. | cmd/entomb_cmd.go:291-299 continue on read/parse error |
| low | degraded | – | verify-castes: caste → model-slot table | Per-caste model assignments are computed but only emitted in JSON; the visual output the wrapper runs prints just per-surface file counts. | cmd/command_truth.go:679-697 loadCasteAssignments into result["castes"]; renderVerifyCastesVisual :700-720 never renders castes; wrapper .claude/commands/ant/ve |
| low | degraded | – | watch: 4-pane tmux session (status / progress / spawn tree / colorized activity log) | Watch is a single-pane TTY refresh of the swarm snapshot; the spawn-tree and colorized activity-log streams are no longer shown, and colorize-log.sh / watch-spawn-tree.sh no longer ship. | cmd/compatibility_cmds.go:31-53, 354-384; .aether/utils/ now contains only hooks/ and queen-to-md.xsl |
| low | degraded | – | watch-spawn-tree.sh (live hierarchical spawn tree pane) | The live view lost the parent/child tree: /ant-watch now shows a flat worker list with no depth, parent, or expand/collapse. | .claude/commands/ant/watch.md runs `aether watch` -> buildSwarmWatchResult -> spawnEntriesToWatchMaps cmd/swarm_cmd.go:232-249 emits name/caste/task/status/summ |
| low | dropped | – | help | The 'HOW IT WORKS' system explanation (lifecycle, spawn depth, watcher independence, colony memory kinds, state files) and the caste legend are gone from help. | .claude/commands/ant/help.md — no equivalent section; `aether command-guide`/`recipes` do not cover system overview |
| low | dropped | – | maturity | The six-milestone journey progress bar, per-milestone description text, 'Next: X -> Y, complete N more phases' line, and Colony Statistics block are gone; the runtime prints five plain lines. | cmd/command_truth.go:203-224 renderMaturityVisual prints Goal/Milestone/Progress/Age/Version + next-up only |
| low | dropped | – | verify-castes: LiteLLM proxy health + model-config switch guidance | Proxy health check (localhost:4000/health) and the Claude/GLM settings-switch instructions are gone. | cmd/command_truth.go:652-720 has no proxy/model-config output |
| low | dropped | – | pheromone-export-branch / pheromone-merge-log / midden-ingest-errors / suggest-quick-dismiss / queen-write-charter | These subcommands no longer exist at HEAD; only pheromone-export-branch had a live old caller (worktree.sh), and its role is covered by syncPheromoneStores. | no `Use:` for any of these names in cmd/*.go; pheromone sync lives in cmd/pheromone_sync.go:33 called from cmd/codex_build_worktree.go |
| low | dropped | – | session-is-stale / session-clear-context / session-summary | Three session helpers have no Go equivalent; nothing regressed on a live path because nothing called them in v5.3.3 either. | grep 'session-is-stale/session-clear-context/session-summary' over cmd/*.go and .claude/commands/ant/*.md -> no hits; cmd/session_cmds.go registers only init/re |
| low | dropped | – | semantic-cli.sh (semantic-init/index/search/find-duplicate/rebuild/get-context/status) | Embedding-based semantic search over colony memory has no Go equivalent; it was already marked DEPRECATED and uncalled at v5.3.3, so no live capability was lost. | grep 'semantic-search/semanticSearch/semantic-index' over cmd/ and pkg/ -> no hits (only prose uses of the word 'semantic') |
| low | dropped | – | skills.sh _skill_diff / skill-cache-rebuild | The manifest-diff and cache-rebuild helpers are gone; update safety for user skills is now handled differently (prune shipped-from-user-dir) rather than by diffing a manifest. | cmd/skills.go registers skill-list, skill-manifest-read, skill-is-user-created only (Phase 191 deletion noted at cmd/skills.go:904); cmd/install_cmd.go:848 prun |
| low | dropped | – | .aether/data/verification.json / build-claims.json / claims.json (root-level names) | None of these three root-level files exists at HEAD or is referenced by any wrapper, Go path, or doc; the only live claims artifact is last-build-claims.json (kept, see kept list). | grep '"verification.json"' outside build/phase-N joins, 'build-claims.json', '"claims.json"' in cmd/, pkg/, ts-host/src, .claude/commands/ant, .aether/docs = 0  |
| low | orphaned | – | continue: changelog-append / changelog-collect-plan-data | Automatic CHANGELOG entries at phase advance are gone; the subcommands exist with no caller. | cmd/changelog.go:13,108-115 CLI only; no wrapper or Go call site |
| low | orphaned | – | continue: temp-clean / backup-prune-global housekeeping | Temp-file cleanup and backup pruning no longer run at phase end. | cmd/maintenance.go:183,259 CLI only; no callers |
| low | orphaned | – | seal | generate-commit-message (seal/pause commit suggestions) has no caller; the seal commit + push offer was replaced by a wrapper-only Porter delivery question. | cmd/generate_cmds.go:81 defined; grep generate-commit-message in cmd/ (non-test), .claude/commands/ant/, .aether/commands/ returns no caller; .claude/commands/a |
| low | orphaned | – | milestone-detect / version-check-cached | Both Go subcommands remain registered with no wrapper or Go caller; status shows Milestone without the computed colony version string. | cmd/build_flow_cmds.go:25 and :82 registered; grep across .claude/commands/ant, .aether/commands, cmd (non-test): no callers; cmd/status.go:922-925 prints miles |
| low | orphaned | – | chamber-compare (Go) | Registered subcommand compares a chamber to the live colony, is not the tunnels comparison, and has no caller. | cmd/chamber.go:177-…; no reference in cmd/ (non-test), .claude/commands/ant, ts-host |
| low | orphaned | – | update-progress | Go subcommand that fed the old watch progress pane is registered but has no caller; watch-progress.txt now just holds the last rendered snapshot. | cmd/build_flow_cmds.go:137 registered; only self-reference :206; no caller in wrappers/cmd/ts-host; cmd/compatibility_cmds.go:351 writes visual to watch-progres |
| low | orphaned | – | pheromone-snapshot-inject / pheromone-merge-back commands | Both cobra commands are registered but nothing calls them; the behaviour survives only through the different in-process syncPheromoneStores path. | cmd/pheromone_mgmt.go:259,290 cobra only, 0 callers; live path is cmd/pheromone_sync.go:33 ← cmd/codex_build_worktree.go |
| low | orphaned | – | worktree-create CLI | The standalone worktree-create advertises pheromone injection but does a bare `git worktree add` with no base branch, no colony-data copy and no pheromone snapshot, and nothing calls it. | cmd/clash.go:119-150 Short says "Create worktree with pheromone injection" but body is only exec git worktree add <branch> -b <branch>; no callers in wrappers/G |
| low | orphaned | – | swarm-display-get / -render / -inline / -text | The four swarm display commands no longer show swarm activity at all — they print colony phase progress — and nothing calls them. | cmd/internal_cmds.go:646 swarm-display-get returns goal/milestone/phases; cmd/swarm_display.go:41 swarm-display-render walks state.Plan.Phases with --max-depth  |
| low | orphaned | – | chamber-create / chamber-verify / chamber-list / chamber-compare CLI (cmd/chamber.go) | A second, standalone chamber CLI exists in Go but nothing calls it; entomb and tunnels use their own private implementations in entomb_cmd.go, so the two can drift (chamber.go manifest shape differs from entomb's). | cmd/chamber.go:16,78,125,177 registered at :317-320; grep of .claude/commands, .aether/commands, .aether/docs, ts-host/src, cmd/*.go (non-test) finds no caller. |
| low | orphaned | – | .aether/schemas/worker-priming.xsd, prompt.xsd, example-prompt-builder.xml | Worker-priming/prompt schemas are shipped but no runtime path composes or validates against them (they were already only reachable via xml-compose.sh in v5.3.3, which no command invoked). | grep for worker-priming/prompt.xsd/example-prompt-builder across cmd/, pkg/, ts-host/src returns nothing; files remain under .aether/schemas/. |
| low | orphaned | – | colony-name / colony-depth | Neither subcommand is called; status no longer reads colony-depth as it did. | cmd/colony_cmds.go:14 and cmd/colony_cmds.go:66; no wrapper/YAML/Go caller |
| low | orphaned | – | curation-sentinel / curation-nurse / curation-critic / curation-herald / curation-janitor / curation-archivist / curation-librarian / curation-scribe / curation-run | All nine curation-ant CLI subcommands are uncalled; the ants themselves still run in-process via the orchestrator at seal, so this is dead CLI surface only. | cmd/curation_cmds.go:15,47,79,111,143,176,209,242,272; in-process path is cmd/consolidation_lifecycle.go:316 → pkg/agent/curation/orchestrator.go:79-86 |
| low | orphaned | – | consolidation-phase-end / consolidation-seal | The CLI forms have no caller; the same pipeline runs in-process from continue/seal (cmd/consolidation_lifecycle.go:141,378), so the capability is present but the 'manual inspection path' is unreferenced by any wrapper. | cmd/graph_consolidation_cmds.go:224 and :297; zero wrapper/YAML/host hits |
| low | orphaned | – | colony-vital-signs / context-capsule / check-antipattern / contract-schema / cache-clean / cache-clean-stale | Six subcommands are uncalled CLI wrappers around helpers that do run in-process (status.go:1053, colony_prime_context.go:1212, gate.go:520, contract_schema.go:94, context.go:478); dead surface, no lost capability. | cmd/memory_details.go:58, cmd/context.go:430, cmd/security_cmds.go:181, cmd/contract_schema.go:260, cmd/cache_cmds.go:12, cmd/cache_cmds.go:36 |
| low | orphaned | – | closeout (top-level) | Top-level `aether closeout` duplicates `aether ceremony closeout`; every wrapper calls the ceremony form, nothing calls this one. | cmd/closeout_cmd.go:17 (registered at :67); wrappers use `aether ceremony closeout` (.claude/commands/ant/build.md:393, seal.md:154, continue.md:202) |
| low | orphaned | – | audit-catalog / backup-prune-global / binary-download / bootstrap-system / colony-archive-xml | Five maintenance/alias subcommands have no caller: audit-catalog (command reliability table), backup-prune-global (prune ~/.aether backups), binary-download (standalone runtime download; install --download-binary does it in-process), bootstrap-system (hub bootstrap), colony-archive-xml (alias of arc | cmd/audit_catalog.go:124 (root.go:206 only exempts it), cmd/maintenance.go:183, cmd/binary_download.go:16, cmd/internal_cmds.go:295, cmd/alias_cmds.go:71 |
| low | orphaned | – | gate-classify / gate-recovery-template / gate-auto-resolve | Three gate rationale/template display commands have no caller anywhere. | cmd/gate.go:1450, cmd/gate.go:1433, cmd/gate.go:1494 — zero references outside testdata |
| low | orphaned | – | generate-ant-name | Uncalled; workers.md:11,349 still tells workers to shell out to it while aether-queen.md:170 forbids it — contradictory guidance around a dead command. | cmd/generate_cmds.go:47; .aether/workers.md:11; .claude/agents/ant/aether-queen.md:170 ('Do not use') |
| low | orphaned | – | generate-commit-message / generate-progress-bar / generate-threshold-bar | Rendering helpers have no caller; colony-visuals SKILL.md:37 instructs 'always use generate-progress-bar' for a command nothing runs. | cmd/generate_cmds.go:81,128,172; .aether/skills/colony/colony-visuals/SKILL.md:37 |
| low | orphaned | – | entropy-score | Colony health/entropy score computed by nothing. | cmd/internal_cmds.go:63 — zero references outside testdata |
| low | orphaned | – | emoji-audit | Command-file emoji audit is not run by Makefile, scripts, CI, or any wrapper. | cmd/emoji_audit.go:45 — zero references outside testdata |
| low | orphaned | – | colony-depth get / plan-granularity get / parallel-mode get / phase-commits get | All four nested `get` readers have no caller; CLAUDE.md claims status/resume 'display the active parallel mode' but no path runs `parallel-mode get`. | cmd/colony_cmds.go:72,154,247; cmd/phase_commit.go:237 — no wrapper/yaml/ts reference; three of four already in cmd/testdata/orphan_allowlist.json |
| low | orphaned | – | domain-detect | Only mentioned to the user in init.md as something that 'shows the tags'; nothing executes it. | cmd/colony_cmds.go:322; sole reference .claude/commands/ant/init.md:293 (prose, not a step) |
| low | orphaned | – | milestone-detect | Milestone detection (First Mound ... Crowned Anthill) has no caller. | cmd/build_flow_cmds.go:82; zero references outside testdata. Would: derive the current milestone name from colony state. |
| low | orphaned | – | parallel-mode (get/set) | parallel-mode get/set is documented in CLAUDE.md as the way to read or change the mode, but no wrapper (init.md included) or runtime path invokes it. | cmd/colony_cmds.go:241; grep of .claude/commands/ant, .aether/commands, ts-host, Go non-test finds no call. Would: read or write parallel_mode in COLONY_STATE.j |
| low | orphaned | – | plan-granularity (get/set) | Planning-granularity get/set has no caller; only scripts/classify_commands.py names it as data. | cmd/colony_cmds.go:148; no execution site. Would: read/write the planning granularity setting in colony state. |
| low | orphaned | – | load-state | load-state has no caller. | cmd/state_cmds.go:931; zero calls. Would: print COLONY_STATE.json. |
| low | orphaned | – | normalize-args | normalize-args has no caller. | cmd/normalize_args.go:12; zero calls. Would: normalise args from env or positional params. |
| low | orphaned | – | proof | The runtime-context/skill proof inspector is never invoked; build.md's word 'proof' (line 399) is prose, not a call. | cmd/proof_cmd.go:84; no `aether proof` anywhere. Would: report context-injection and skill-match proof for the active colony. |
| low | orphaned | – | host oracle / host lifecycle | Two `aether host` subcommands (oracle, lifecycle) have no caller; only host plan, host colonize, and host continue --dry-run are invoked from wrappers. | cmd/host_cmd.go:40 (lifecycle), :80 (oracle); grep for 'host lifecycle'/'host oracle' across wrappers, yaml, CODEX.md, scripts, Makefile returns nothing. Would: |
| low | orphaned | – | import (parent) / import pheromones / export pheromones (nested paths) | The nested `aether import`, `aether import pheromones` and `aether export pheromones` cobra paths have no caller; wrappers use the flat aliases import-signals/export-signals which resolve to pheromone-import-xml/pheromone-export-xml (cmd/alias_cmds.go:24,32), so the nested tree is a dead duplicate. | cmd/exchange.go:29 (import parent), :344 (import pheromones), :46 (export pheromones); .claude/commands/ant/import-signals.md:9 and export-signals.md:9 call the |
| low | orphaned | – | spawn-can-spawn / spawn-can-spawn-swarm / spawn-get-depth / spawn-tree-load / spawn-tree-active / spawn-tree-depth / spawn-efficiency | Spawn-budget and spawn-tree inspection CLIs are uncalled; budget is now enforced internally (spawnBudgetPreflight) so only the inspection surface is dead. | cmd/spawn.go:341,406,434,568,596; cmd/internal_cmds.go:495,542 definitions; zero callers; internal replacement cmd/codex_build.go:730 spawnBudgetPreflight |
| low | orphaned | – | swarm-display-render / -inline / -text / -init / -update / -get, swarm-timing-start / -get / -eta, swarm-activity-log | Ten swarm display/timing CLIs have no caller; the old workers.md had child workers report progress via swarm-display-update, HEAD workers report nothing to the swarm display. | cmd/swarm_display.go:41,110,176; cmd/swarm.go:134,160,212,239,278; cmd/internal_cmds.go:646,683 definitions; .claude/commands/ant/swarm.md calls only swarm --pl |
| low | orphaned | – | host watch / host swarm | TS-host lanes for watch and swarm exist in both Go and the host registry but no wrapper routes through them; watch.md and swarm.md call the Go commands directly. | cmd/host_cmd.go:88,96 definitions; .aether/ts-host/src/command-registry.ts:233,242; .claude/commands/ant/watch.md:9 `aether watch`, swarm.md:24 `aether swarm -- |
| low | orphaned | – | trace-replay / trace-export / trace-summary / trace-inspect / trace-rotate | Trace inspection and rotation CLIs have no caller; trace.jsonl is written by tracer.Log* but never rotated, so it grows unbounded. | cmd/trace_cmds.go:17,100,192,238,439 definitions; zero callers outside testdata; on orphan_allowlist.json |
| low | orphaned | – | view-state-init / -get / -set / -toggle / -expand / -collapse | Six view-state CLIs for the watch dashboard have no caller. | cmd/view_state.go:28,52,88,124,159,190 definitions; zero callers; on orphan_allowlist.json |
| low | orphaned | – | worktree-reap / verify-out-of-band / skip-phase | Operator-only commands that are never executed by a wrapper or hook; they surface solely as text hints the user is told to type (worktree-reap in a preservation note, skip-phase as continue's SkipCommand), and verify-out-of-band is not surfaced anywhere. | cmd/worktree_reap.go:35, cmd/verify_out_of_band.go:36, cmd/phase_skip.go:27 definitions; hints at cmd/codex_build_worktree.go:1313 and cmd/codex_continue.go:405 |
| low | orphaned | – | version-check-cached / versions / update-progress | Old resume-colony checked colony version on every resume (3x) and watch.md updated progress; HEAD nothing calls any of the three. | cmd/build_flow_cmds.go:25,137; cmd/compatibility_cmds.go:286 definitions; only cmd/codex_visuals.go:308,404 output-mode case list and build_flow_cmds.go:206 tra |
| low | orphaned | – | registry-add | Explicit registry-add is uncalled; superseded by upsertColonyRegistryEntry on init/seal (2 Go callers), so no capability loss. | cmd/registry.go:42 definition; cmd/registry.go:208 upsertColonyRegistryEntry replaces it; allowlist 'dispositioned-2026-08-16: superseded' |
| low | orphaned | – | wisdom-export-xml / wisdom-import-xml / registry-export-xml / registry-import-xml / export wisdom / export registry / import wisdom / import registry | Eight XML exchange commands (four aliases + four nested) have no caller; old init/seal wrappers imported/exported the registry. | cmd/alias_cmds.go:41,48,56,63; cmd/exchange.go:86,142,524,553 definitions; zero callers; allowlist 'dispositioned-2026-08-16: retirement ruled in .planning/deci |
| low | orphaned | – | colony-depth set / plan-granularity set / parallel-mode set / phase-commits set | All four colony-setting mutators are uncalled; parallel-mode set is only hinted in visuals text, phase-commits set only in a Go comment, so /ant-init's documented 'Step 6.5 strategy selection' has no runtime path. | cmd/colony_cmds.go:106,197,281; cmd/phase_commit.go:262 definitions; hint strings cmd/codex_visuals.go:5169 and cmd/codex_build_progress.go:40; comment cmd/phas |
| low | orphaned | – | temp-clean | Old continue-finalize cleaned temp files older than 7 days every phase; nothing calls it now. | cmd/maintenance.go:259 definition; zero callers; on orphan_allowlist.json |
| low | orphaned | – | setup / serve / reconcile / source-check / visuals-dump | Five top-level operator tools have no wrapper, Makefile, script, CI or hook caller; source-check ('verify source-of-truth and generated wrapper parity') is exactly the parity check this repo's wrapper-triplet rule needs and nothing runs it. | cmd/setup_cmd.go:15; cmd/serve.go:20; cmd/reconcile.go:119; cmd/source_check.go:70; cmd/visuals_dump.go:18 definitions; grep of Makefile, scripts/, .github/work |
| low | orphaned | – | .aether/data/gates.json (rule file via incident-rule-add) | A second, unrelated 'gates.json' at the data root is written by `aether incident-rule-add --type gate` (also decree.json), which nothing calls and no reader loads. | writer cmd/internal_cmds.go:222-275 (filename = "gates.json"/"decree.json"); no ReadFile/LoadJSON of either name outside that command; command sits in cmd/testd |
| low | pointer_only | – | import-signals | HEAD runtime's own Next Up hint after export tells users to run `aether import-signals --file <path>`, while the wrapper usage says positional path only; both work, but the wrapper's promised 'colony-name' usage line and collision note have been silently dropped rather than documented. | .claude/commands/ant/import-signals.md:9; cmd/codex_visuals.go:4571-4590 (no prefix/collision line) |
| low | pointer_only | – | skill-create | The wrapper's closing line points the user at /ant-skill-list, a command that does not exist on HEAD. | .claude/commands/ant/skill-create.md:271; ls .claude/commands/ant/ contains only skill-create.md for skill*; no Use: "skill-list" in cmd/ |
| low | pointer_only | – | .aether/data/spawn-tree.txt | Spawn tree content is consumed only for name-collision avoidance, status updates, medic validation and entomb copying; worker/reviewer prompts receive it solely as a CoordinationPath filename or hint string, never any entry content. | prompt-facing refs are filenames only: cmd/codex_build.go:1991 and cmd/codex_dispatch_contract.go:195,257 CoordinationPath; cmd/codex_plan.go:2706,2788 Hints; c |
| low | unwired | – | patrol wrapper promise: remediation suggestions and visual report | The wrapper promises a structured health report 'with remediation suggestions' and passes $ARGUMENTS, but patrol-check emits a raw JSON envelope with no remediation text even in visual mode and rejects any argument. | .claude/commands/ant/patrol.md:9-10 vs cmd/patrol_check.go:57 (cobra.NoArgs) and :81 outputOK -> cmd/helpers.go:20 (plain JSON, no visual renderer); no 'remedia |
| low | unwired | – | .aether/data/planning/notes.md | No writer and no reader exist anywhere in cmd/, pkg/, wrappers or ts-host; the path appears only in the audit prompt. | grep across cmd/ pkg/ .claude/ .aether/commands ts-host/src returns nothing for planning/notes.md |
| low | unwired | – | .aether/data/planning/run-N/iteration-state.json | No run-N subdirectory layout exists; iteration state lives flat at planning/iteration-state.json (which is wired). | cmd/codex_plan.go:1904 planningIterationStateRel = "planning/iteration-state.json"; no 'planning/run-' or 'run-%d' path anywhere in cmd/ |
| low | unwired | – | outcome.md | No file named outcome.md is written or read anywhere at HEAD or in v5.3.3; the per-phase outcome exists only as review.json/verification.json/handoffs. | grep 'outcome\.md' across cmd/ pkg/ .claude/ .aether/ returns nothing |

## Refuted

- **build: pheromone suggestion approval** — The claim that the build closeout "only shows a pending count" is wrong. The build wrapper's Finalize step runs `aether ceremony closeout --workflow build` (.claude/commands/ant/build.md:393). That co
- **continue: survey-verify** — The claimed gap ("survey staleness verification at continue has no caller; continue only injects the survey section") is wrong at runtime. resolveSurveySection (cmd/helpers.go:200) is not a bare point
- **entomb** — The cited entomb facts are accurate (cmd/entomb_cmd.go:26-186 has no promotion call; :680-683 clears PhaseLearnings/Decisions/Instincts; :79-84 saves only [0.5,0.8) near-miss instincts), but the concl
- **oracle (focus areas / strategy)** — The core factual claims are wrong. `state.FocusAreas` IS assigned on the live start path: cmd/oracle_loop.go:692 (`FocusAreas: currentOracleFocusAreas()`) inside `startOracleCompatibility` (oracle_loo