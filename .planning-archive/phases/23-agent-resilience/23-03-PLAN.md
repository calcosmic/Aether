---
phase: 23-agent-resilience
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/ant/init.md
  - .claude/commands/ant/build.md
  - .claude/commands/ant/lay-eggs.md
  - .claude/commands/ant/seal.md
  - .claude/commands/ant/entomb.md
  - .claude/commands/ant/colonize.md
autonomous: true
requirements: [RESIL-01, RESIL-02, RESIL-03]

must_haves:
  truths:
    - "All 6 high-risk slash commands have <failure_modes>, <success_criteria>, and <read_only> sections addressing their specific risks"
    - "Sections are brief (focused on 1-2 critical failure points per command, not exhaustive)"
    - "Commands' existing workflow steps and error handling are NOT modified"
    - "init.md addresses colony state overwrite risk"
    - "build.md addresses wave failure and partial file write risk"
    - "seal.md and entomb.md address archive corruption risk"
  artifacts:
    - path: ".claude/commands/ant/init.md"
      provides: "Init resilience sections"
      contains: "<failure_modes>"
    - path: ".claude/commands/ant/build.md"
      provides: "Build resilience sections"
      contains: "<failure_modes>"
    - path: ".claude/commands/ant/lay-eggs.md"
      provides: "Lay-eggs resilience sections"
      contains: "<failure_modes>"
    - path: ".claude/commands/ant/seal.md"
      provides: "Seal resilience sections"
      contains: "<failure_modes>"
    - path: ".claude/commands/ant/entomb.md"
      provides: "Entomb resilience sections"
      contains: "<failure_modes>"
    - path: ".claude/commands/ant/colonize.md"
      provides: "Colonize resilience sections"
      contains: "<failure_modes>"
  key_links:
    - from: ".claude/commands/ant/init.md"
      to: "COLONY_STATE.json"
      via: "failure_modes warns about overwrite risk"
      pattern: "COLONY_STATE"
    - from: ".claude/commands/ant/build.md"
      to: "wave execution"
      via: "failure_modes addresses partial wave failure"
      pattern: "wave.*fail"
---

<objective>
Add brief resilience sections (`<failure_modes>`, `<success_criteria>`, `<read_only>`) to the 6 high-risk Claude Code slash commands: init, build, lay-eggs, seal, entomb, colonize.

Purpose: These commands write persistent state (colony state, archives, plans) and a failure mid-operation can leave the system in a broken state. Brief resilience sections near the top of each command document the 1-2 catastrophic failure points, what successful completion looks like, and paths the command must not touch. This is NOT exhaustive error handling -- it targets the single worst-case scenario per command.

Output: 6 slash command files updated with `<failure_modes>`, `<success_criteria>`, and `<read_only>` XML sections placed near the top (after the Instructions heading, before the first Step).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-agent-resilience/23-CONTEXT.md
@.planning/phases/23-agent-resilience/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resilience sections to init, build, and lay-eggs commands</name>
  <files>
    .claude/commands/ant/init.md
    .claude/commands/ant/build.md
    .claude/commands/ant/lay-eggs.md
  </files>
  <action>
Read each command file completely before writing. Insert three XML sections near the TOP of each file -- after the YAML frontmatter and the initial heading/instructions paragraph, but BEFORE the first Step (e.g., before "### Step 0:" in init.md). Do NOT modify any existing content. This is an INSERT, not an append.

**Why near the top:** Slash commands are long (init: 323 lines, build: 1121 lines). Placing resilience guidance at the top ensures the LLM reads it before executing any steps. Placing it at the end risks it being lost in the context window.

**Format:** Use three separate XML tags per user decision: `<failure_modes>`, `<success_criteria>`, `<read_only>`. Keep each section brief (5-10 lines each, ~20-25 lines total per command). Focus on the ONE catastrophic failure scenario, not every possible error.

### init.md -- Colony State Overwrite Risk

```xml
<failure_modes>
### Colony State Overwrite
If COLONY_STATE.json already exists with an active colony:
- STOP before overwriting
- Warn: "Active colony detected with goal: [goal]. Overwriting will lose this data."
- Options: (1) Archive first with /ant:seal, (2) Continue and overwrite, (3) Cancel

### Write Failure Mid-Init
If writing COLONY_STATE.json fails partway:
- Remove the incomplete file (partial state is worse than no state)
- Report the error
- Recovery: user can run /ant:init again safely
</failure_modes>

<success_criteria>
Command is complete when:
- COLONY_STATE.json exists and is valid JSON
- Colony goal, milestone, and timestamp are set
- Session file is written
- User sees confirmation of colony creation
</success_criteria>

<read_only>
Do not touch during init:
- .aether/dreams/ (user notes)
- .aether/chambers/ (archived colonies)
- .env* files
- .claude/settings.json
- .github/workflows/
</read_only>
```

### build.md -- Wave Failure and Partial Writes

```xml
<failure_modes>
### Wave Failure Mid-Build
If a worker fails during a build wave:
- Do NOT continue to next wave (failed dependencies will cascade)
- Report which worker failed, what task it was on, and what was attempted
- Options: (1) Retry the failed task, (2) Skip and continue with remaining tasks, (3) Abort build

### Partial File Writes
If a builder crashes mid-write:
- Check git status for uncommitted partial changes
- If partial changes exist, offer: (1) Review and keep, (2) Revert with git checkout, (3) Stash for later

### State Corruption
If COLONY_STATE.json becomes invalid during build:
- STOP all workers immediately
- Do not attempt to fix state automatically
- Report the issue and offer to restore from last known good state
</failure_modes>

<success_criteria>
Command is complete when:
- All waves executed in order with no skipped dependencies
- Each worker's task output is verified (files exist, tests pass)
- COLONY_STATE.json reflects completed phase progress
- Build summary reports all workers' outcomes
</success_criteria>

<read_only>
Do not touch during build:
- .aether/dreams/ (user notes)
- .aether/chambers/ (archived colonies)
- .env* files
- .claude/settings.json
- .github/workflows/
- Other agents' config files (only modify files assigned to the current build task)
</read_only>
```

### lay-eggs.md -- Plan Writing Failure

```xml
<failure_modes>
### Plan File Write Failure
If writing phase plans to COLONY_STATE.json fails:
- Do not leave partial plan data in state
- Report what was successfully written vs. what failed
- Recovery: user can re-run /ant:lay-eggs to regenerate plans

### Goal Parsing Failure
If the user's goal cannot be parsed into actionable phases:
- Do not generate placeholder phases
- Ask user to clarify or simplify the goal
- Offer examples of well-formed goals
</failure_modes>

<success_criteria>
Command is complete when:
- Phase plan is written to COLONY_STATE.json with tasks and success criteria
- Plan structure is valid (phases have tasks, tasks have descriptions)
- User sees the plan and can approve or request changes
</success_criteria>

<read_only>
Do not touch during lay-eggs:
- .aether/dreams/ (user notes)
- .aether/chambers/ (archived colonies)
- Source code files (planning only, no implementation)
- .env* files
- .claude/settings.json
</read_only>
```

IMPORTANT: Insert after the initial description/instructions paragraph but BEFORE the first "### Step" heading in each file. For init.md, that means after the "Parse $ARGUMENTS" section but before "### Step 0: Initialize Visual Mode". For build.md and lay-eggs.md, find the equivalent first step marker.
  </action>
  <verify>
1. `grep -c '<failure_modes>' .claude/commands/ant/init.md` returns 1
2. `grep -c '<success_criteria>' .claude/commands/ant/init.md` returns 1
3. `grep -c '<read_only>' .claude/commands/ant/init.md` returns 1
4. Repeat for build.md and lay-eggs.md (all three tags present in each)
5. All three sections appear BEFORE the first "### Step" in each file (check line numbers)
6. No existing content was removed or modified (line counts increased by ~20-25 lines each)
7. `grep 'COLONY_STATE' .claude/commands/ant/init.md` appears in both existing content and failure_modes
  </verify>
  <done>Init, build, and lay-eggs commands each have three resilience sections (failure_modes, success_criteria, read_only) inserted near the top. Init addresses overwrite risk. Build addresses wave failure and partial writes. Lay-eggs addresses plan writing failure. Existing command workflows are untouched. Format matches the XML tags decision from CONTEXT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Add resilience sections to seal, entomb, and colonize commands</name>
  <files>
    .claude/commands/ant/seal.md
    .claude/commands/ant/entomb.md
    .claude/commands/ant/colonize.md
  </files>
  <action>
Read each command file completely before writing. Insert three XML sections near the TOP of each file -- after the YAML frontmatter and initial description, BEFORE the first Step heading. Do NOT modify existing content.

**Format:** Use three separate XML tags: `<failure_modes>`, `<success_criteria>`, `<read_only>`. Keep each brief (5-10 lines each).

### seal.md -- Archive Corruption Risk

```xml
<failure_modes>
### Crowned Anthill Write Failure
If writing the Crowned Anthill milestone document fails:
- Do not mark the colony as sealed in state
- Report the error -- sealing is incomplete
- Recovery: user can re-run /ant:seal after fixing the issue

### State Update Failure After Seal
If COLONY_STATE.json update fails after seal document is written:
- The seal document exists but state doesn't reflect it
- Report the inconsistency
- Options: (1) Retry state update only, (2) Manual state fix, (3) Re-run /ant:seal
</failure_modes>

<success_criteria>
Command is complete when:
- Crowned Anthill milestone document is written
- COLONY_STATE.json reflects sealed status
- All phase evidence is summarized in the seal document
- User sees confirmation of successful seal
</success_criteria>

<read_only>
Do not touch during seal:
- .aether/dreams/ (user notes)
- .aether/chambers/ (archived colonies -- seal does NOT archive)
- Source code files
- .env* files
- .claude/settings.json
</read_only>
```

### entomb.md -- Archival with No Rollback

```xml
<failure_modes>
### Archive Write Failure
If writing to chambers/ archive fails partway:
- Do NOT delete source data (the active colony) until archive is verified
- Report what was archived vs. what failed
- Recovery: the original colony data is intact -- user can retry

### Seal Verification Failure
If the colony was not properly sealed before entomb:
- STOP -- do not archive an incomplete colony
- Direct user to run /ant:seal first
- This is a hard gate, not a suggestion

### Chamber Naming Conflict
If the target chamber directory already exists:
- STOP -- do not overwrite an existing archive
- Report the conflict
- Options: (1) Use a different name, (2) Merge with existing, (3) Cancel
</failure_modes>

<success_criteria>
Command is complete when:
- Colony data is fully copied to chambers/ archive directory
- Archive integrity is verified (all expected files present)
- Active colony state is cleared only after archive verification
- User sees confirmation with archive location
</success_criteria>

<read_only>
Do not touch during entomb:
- .aether/dreams/ (user notes -- not archived)
- Source code files
- .env* files
- .claude/settings.json
- .github/workflows/
- Other chamber directories (only write to the new chamber)
</read_only>
```

### colonize.md -- Survey Overwrite Risk

```xml
<failure_modes>
### Existing Survey Overwrite
If .aether/data/survey/ already contains survey documents:
- Warn before overwriting: "Existing survey found from [date]. Re-surveying will replace it."
- Options: (1) Continue and overwrite, (2) Keep existing survey, (3) Merge (re-survey only outdated sections)

### Surveyor Spawn Failure
If a surveyor agent fails during codebase exploration:
- Report which survey document was not produced
- Partial surveys are acceptable -- note which documents are complete vs. missing
- Recovery: user can re-run /ant:colonize to regenerate missing surveys
</failure_modes>

<success_criteria>
Command is complete when:
- All surveyor agents have completed their exploration
- Survey documents exist in .aether/data/survey/
- COLONY_STATE.json reflects colonized status
- User sees summary of survey findings
</success_criteria>

<read_only>
Do not touch during colonize:
- .aether/dreams/ (user notes)
- .aether/chambers/ (archived colonies)
- Source code files (survey is read-only exploration)
- .env* files
- .claude/settings.json
- COLONY_STATE.json structure beyond colonize-specific fields
</read_only>
```

IMPORTANT: Insert after the initial description/instructions but BEFORE the first "### Step" heading in each file.
  </action>
  <verify>
1. `grep -c '<failure_modes>' .claude/commands/ant/seal.md` returns 1
2. `grep -c '<success_criteria>' .claude/commands/ant/seal.md` returns 1
3. `grep -c '<read_only>' .claude/commands/ant/seal.md` returns 1
4. Repeat for entomb.md and colonize.md (all three tags present in each)
5. All three sections appear BEFORE the first "### Step" in each file
6. No existing content was removed or modified (line counts increased)
7. entomb.md failure_modes mentions seal verification (hard gate)
  </verify>
  <done>Seal, entomb, and colonize commands each have three resilience sections (failure_modes, success_criteria, read_only) inserted near the top. Seal addresses archive corruption. Entomb addresses the no-rollback risk and enforces seal-first gate. Colonize addresses survey overwrite. Format uses the three-part XML tags per user decision. Existing command workflows are untouched.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All 6 high-risk slash commands contain exactly one `<failure_modes>`, one `<success_criteria>`, and one `<read_only>` section each
2. Sections are positioned near the top (before first Step)
3. Each failure_modes is brief (10-15 lines) and addresses the specific catastrophic scenario
4. Each success_criteria defines what "done" looks like for the command
5. Each read_only declares paths the command must not touch
6. No existing command workflow steps were modified
7. Recovery instructions are included in failure_modes
8. Format uses three separate XML tags per locked decision (not a combined resilience block)
</verification>

<success_criteria>
- 6 slash command files updated with 3 XML sections each (18 sections total: 6 failure_modes + 6 success_criteria + 6 read_only)
- Sections are concise and focused (1-2 failure scenarios each, not exhaustive)
- Positioned near top of file so LLM reads them before executing steps
- Existing command workflows completely untouched
- Each failure_modes includes recovery path and user options per user decision
- Format matches locked decision: `<failure_modes>`, `<success_criteria>`, `<read_only>` XML tags
</success_criteria>

<output>
After completion, create `.planning/phases/23-agent-resilience/23-03-SUMMARY.md`
</output>
