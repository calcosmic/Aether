---
phase: 23-agent-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/agents/aether-queen.md
  - .opencode/agents/aether-builder.md
  - .opencode/agents/aether-watcher.md
  - .opencode/agents/aether-weaver.md
  - .opencode/agents/aether-route-setter.md
  - .opencode/agents/aether-ambassador.md
  - .opencode/agents/aether-tracker.md
autonomous: true
requirements: [RESIL-01, RESIL-02, RESIL-03]

must_haves:
  truths:
    - "All 7 high-risk agents have <failure_modes> sections with minor/major tiers and escalation format"
    - "All 7 high-risk agents have <success_criteria> sections with self-check steps and report format"
    - "All 7 high-risk agents have <read_only> sections with global + agent-specific boundaries"
    - "Existing agent rules (Builder 3-Fix Rule, Builder Iron Law, Watcher Iron Law, Queen Verification Discipline) are referenced, not contradicted"
    - "Builder, Queen, and Watcher include peer review triggers in success criteria"
  artifacts:
    - path: ".opencode/agents/aether-queen.md"
      provides: "Queen resilience sections"
      contains: "<failure_modes>"
    - path: ".opencode/agents/aether-builder.md"
      provides: "Builder resilience sections"
      contains: "<failure_modes>"
    - path: ".opencode/agents/aether-watcher.md"
      provides: "Watcher resilience sections"
      contains: "<failure_modes>"
    - path: ".opencode/agents/aether-weaver.md"
      provides: "Weaver resilience sections"
      contains: "<failure_modes>"
    - path: ".opencode/agents/aether-route-setter.md"
      provides: "Route-setter resilience sections"
      contains: "<failure_modes>"
    - path: ".opencode/agents/aether-ambassador.md"
      provides: "Ambassador resilience sections"
      contains: "<failure_modes>"
    - path: ".opencode/agents/aether-tracker.md"
      provides: "Tracker resilience sections"
      contains: "<failure_modes>"
  key_links:
    - from: ".opencode/agents/aether-builder.md"
      to: "Builder 3-Fix Rule"
      via: "failure_modes references existing rule"
      pattern: "3-Fix Rule"
    - from: ".opencode/agents/aether-watcher.md"
      to: "Watcher Iron Law"
      via: "success_criteria references existing verification discipline"
      pattern: "Iron Law"
    - from: ".opencode/agents/aether-queen.md"
      to: "Watcher peer review"
      via: "success_criteria declares Watcher verifies Queen's phase completion"
      pattern: "Watcher"
---

<objective>
Add resilience sections to all 7 HIGH-risk OpenCode agents: queen, builder, watcher, weaver, route-setter, ambassador, tracker.

Purpose: These agents modify files, colony state, or coordinate spawns. They need the most detailed failure handling, success verification, and boundary declarations because failures in these agents can corrupt state or break builds.

Output: 7 agent files updated with `<failure_modes>`, `<success_criteria>`, and `<read_only>` XML sections appended after the existing Output Format section.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-agent-resilience/23-CONTEXT.md
@.planning/phases/23-agent-resilience/23-RESEARCH.md
@.claude/rules/security.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resilience sections to queen, builder, and watcher agents</name>
  <files>
    .opencode/agents/aether-queen.md
    .opencode/agents/aether-builder.md
    .opencode/agents/aether-watcher.md
  </files>
  <action>
Read each agent file completely before writing. Append three XML-tagged sections AFTER the existing `## Output Format` JSON block (at the very end of each file). Do NOT modify any existing content.

**For each agent, write customized sections based on their specific role:**

### aether-queen.md

`<failure_modes>`: Minor failures = file not found (retry with alternate path), command exit non-zero (read error, retry). Major failures = COLONY_STATE.json corruption detected (STOP, do not write), spawn failure leaves orphaned worker (STOP, clean up spawn-tree), destructive git operation attempted (STOP). Reference existing "Verification Discipline" Iron Law. Escalation format: what failed, 2-3 options with trade-offs, recommendation.

`<success_criteria>`: Self-check = verify COLONY_STATE.json is valid JSON after any update, verify spawn-tree entries are logged, verify phase advancement evidence is fresh (per existing Iron Law). Report format = phases completed, workers spawned, state changes made, verification evidence. Peer review trigger = "Queen's phase completion evidence and critical state changes (colony goal updates, phase advancement, milestone transitions) are verified by Watcher before marking phase done. If Watcher finds issues with the evidence, address within 2-attempt limit before escalating."

`<read_only>`: Global protected paths (dreams/, .env*, .claude/settings.json, .github/workflows/). Queen-specific: do not write to .aether/dreams/, do not run destructive git (reset --hard, push --force, clean -f), do not directly edit source files (spawn a Builder). Queen IS permitted to write COLONY_STATE.json, constraints.json, flags.json via aether-utils.sh commands.

### aether-builder.md

`<failure_modes>`: Minor failures = file not found (re-read directory, try alternate path), command exits non-zero (read error output, diagnose, retry), test fails unexpectedly (check dependency setup, retry). Major = protected path in write target (STOP), state corruption risk (STOP), 2 retries exhausted (STOP). Reference existing "3-Fix Rule" — if 3 fixes fail on a bug, escalate with architectural concern. The 2-attempt retry limit applies to individual task failures; the 3-Fix Rule applies to debugging iterations.

`<success_criteria>`: Self-check = verify every file created/modified exists and is readable, run project test/build command, confirm deliverable matches task. Peer review trigger = "Your work is reviewed by Watcher. Output is not final until Watcher approves. If Watcher finds issues, address within 2-attempt limit." Report format = files created, files modified, verification command and result.

`<read_only>`: Global protected paths. Builder-specific: do not modify .aether/aether-utils.sh unless task explicitly targets it, do not delete files (create and modify only), do not modify other agents' output files (watcher reports, chaos findings), do not write to .aether/data/ (colony state area).

### aether-watcher.md

`<failure_modes>`: Minor = verification command not found (try alternate resolution via Command Resolution chain already in agent), test suite exits with unexpected error (check environment, retry). Major = false negative risk (verification passes but evidence is incomplete — re-run fresh), COLONY_STATE.json appears corrupted during read (STOP, do not create flags, escalate). Reference existing "Iron Law: Evidence before approval, always."

`<success_criteria>`: Self-check = re-run every verification command fresh (not cached results), confirm quality_score reflects execution_verification outcomes, verify flags were created for genuine failures only. Watcher self-verifies (it IS the verifier). If quality_score < 7, include explicit explanation of what brought it down. Report format = files verified, execution results, quality score, flags created.

`<read_only>`: Global protected paths. Watcher-specific: do not edit source files (that is Builder's job), do not write to COLONY_STATE.json directly (only create flags via flag-add), do not delete any files, do not modify test files (only run them). Watcher MAY create flags via `bash .aether/aether-utils.sh flag-add`.

Use 2-attempt retry limit and tiered severity (minor=retry silently, major=stop immediately) per user decision. Include escalation format (what failed, 2-3 options, recommendation) in all three agents. Never fail silently.
  </action>
  <verify>
For each of the 3 files, verify:
1. `grep -c '<failure_modes>' .opencode/agents/aether-queen.md` returns 1
2. `grep -c '<success_criteria>' .opencode/agents/aether-queen.md` returns 1
3. `grep -c '<read_only>' .opencode/agents/aether-queen.md` returns 1
4. Repeat for builder and watcher
5. `grep '3-Fix Rule' .opencode/agents/aether-builder.md` appears in BOTH the existing section AND the failure_modes section
6. `grep 'Iron Law' .opencode/agents/aether-watcher.md` appears in BOTH existing section AND new sections
7. `grep 'Watcher' .opencode/agents/aether-queen.md` appears in the success_criteria section (peer review trigger)
8. No existing content was removed (file line count increased, not decreased)
  </verify>
  <done>Queen, builder, and watcher each have all 3 resilience XML sections. Existing rules (3-Fix Rule, Iron Laws, Verification Discipline) are referenced in new sections, not contradicted. All three HIGH-stakes agents have peer review language: Builder and Queen are reviewed by Watcher; Watcher self-verifies as it IS the verifier. All sections use tiered severity and escalation format per user decision.</done>
</task>

<task type="auto">
  <name>Task 2: Add resilience sections to weaver, route-setter, ambassador, and tracker agents</name>
  <files>
    .opencode/agents/aether-weaver.md
    .opencode/agents/aether-route-setter.md
    .opencode/agents/aether-ambassador.md
    .opencode/agents/aether-tracker.md
  </files>
  <action>
Read each agent file completely before writing. Append three XML-tagged sections AFTER the existing `## Output Format` JSON block. Do NOT modify existing content.

### aether-weaver.md

`<failure_modes>`: Minor = file not found (re-read directory, try alternate path), test fails after refactor (revert change, try smaller increment). Major = behavior change detected (tests that passed before now fail — STOP, revert to pre-refactor state), protected path in target (STOP). The 2-attempt limit applies per refactoring step, not per file.

`<success_criteria>`: Self-check = run full test suite before AND after refactoring (all must pass both times), verify no behavioral changes (same test results), confirm complexity metrics improved. Self-verify only (no peer review). Report format = files refactored, complexity before/after, tests passing before/after.

`<read_only>`: Global protected paths. Weaver-specific: do not change test expectations without changing implementation (that changes behavior), do not modify .aether/ system files, do not create new features (refactor only — behavior-preserving changes).

### aether-route-setter.md

`<failure_modes>`: Minor = codebase file not found during analysis (broaden search), aether-utils.sh command fails (check syntax, retry). Major = COLONY_STATE.json is malformed when read (STOP, do not write plan), plan would overwrite existing phases (STOP, confirm with user). Escalation format per standard.

`<success_criteria>`: Self-check = verify plan structure is valid (phases have tasks, tasks have success criteria), verify file paths referenced in plan actually exist, verify phase count is reasonable (3-6). Self-verify only. Report format = phases planned, tasks created, file paths verified.

`<read_only>`: Global protected paths. Route-setter-specific: do not directly edit COLONY_STATE.json (use aether-utils.sh commands only), do not modify source code (planning only), do not create or edit test files.

### aether-ambassador.md

`<failure_modes>`: Minor = API endpoint returns unexpected format (parse what was received, retry with adjusted request), SDK method not found (check version, try alternate method name). Major = API key or secret would be written to tracked file (STOP immediately), authentication failure after 2 retries (STOP, escalate — likely invalid credentials). Escalation format per standard.

`<success_criteria>`: Self-check = verify integration connects successfully (test API call), verify error handling covers timeout/auth/rate-limit scenarios, verify no secrets in committed code. Self-verify only. Report format = endpoints integrated, authentication verified, error scenarios covered.

`<read_only>`: Global protected paths. Ambassador-specific: do not write API keys or secrets to tracked files (document needed env vars only), do not modify .env files (instruct user to set them), do not modify unrelated source files (integration code only).

### aether-tracker.md

`<failure_modes>`: Minor = reproduction fails (try alternate reproduction steps, check environment), log file not found (search for alternate log locations). Major = fix introduces new test failure (revert immediately), 2 fix attempts fail on same bug (STOP). Reference existing "3-Fix Rule" — after 3 failed fixes, question understanding and escalate with architectural concern. The 2-attempt retry (per user decision) applies to individual operations; the 3-Fix Rule applies to the debugging cycle.

`<success_criteria>`: Self-check = verify the original bug no longer reproduces, run full test suite (no new failures), confirm root cause matches evidence chain. Self-verify only. Report format = symptom, root cause, evidence chain, fix applied, tests passing.

`<read_only>`: Global protected paths. Tracker-specific: same boundaries as Builder (do not modify .aether/aether-utils.sh unless task targets it, do not delete files, do not modify other agents' output). Do not modify colony state files.

Use 2-attempt retry limit and tiered severity consistently. Include escalation format in all four agents. Never fail silently.
  </action>
  <verify>
For each of the 4 files, verify:
1. `grep -c '<failure_modes>' .opencode/agents/aether-{weaver,route-setter,ambassador,tracker}.md` each returns 1
2. `grep -c '<success_criteria>' .opencode/agents/aether-{weaver,route-setter,ambassador,tracker}.md` each returns 1
3. `grep -c '<read_only>' .opencode/agents/aether-{weaver,route-setter,ambassador,tracker}.md` each returns 1
4. `grep '3-Fix Rule' .opencode/agents/aether-tracker.md` appears in both existing section and failure_modes
5. No existing content was removed (line counts increased)
  </verify>
  <done>Weaver, route-setter, ambassador, and tracker each have all 3 resilience XML sections. Existing rules (Tracker 3-Fix Rule) referenced in new sections. All use tiered severity and escalation format per user decision. No peer review triggers (these agents self-verify per research classification).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All 7 HIGH-risk agent files contain exactly one `<failure_modes>`, one `<success_criteria>`, one `<read_only>` section each
2. No existing agent content was modified or removed
3. New sections are placed after the Output Format JSON block
4. Builder references "3-Fix Rule" in failure_modes
5. Watcher references "Iron Law" in success/failure sections
6. Queen references "Verification Discipline" in success_criteria
7. Queen includes Watcher peer review trigger in success_criteria
8. All agents include 2-attempt retry limit, tiered severity, escalation format, and "never fail silently"
</verification>

<success_criteria>
- 7 agent files updated with 3 XML resilience sections each (21 sections total)
- Zero contradictions with existing agent rules
- HIGH-risk template pattern used: detailed minor/major failure tiers, self-check + peer review (where applicable), global + agent-specific boundaries
- All 3 HIGH-stakes agents (builder, queen, watcher) have peer review: Builder and Queen reviewed by Watcher, Watcher self-verifies
- Consistent escalation format across all 7 agents
</success_criteria>

<output>
After completion, create `.planning/phases/23-agent-resilience/23-01-SUMMARY.md`
</output>
