---
phase: 23-agent-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/agents/aether-chronicler.md
  - .opencode/agents/aether-probe.md
  - .opencode/agents/aether-architect.md
  - .opencode/agents/aether-keeper.md
  - .opencode/agents/aether-archaeologist.md
  - .opencode/agents/aether-chaos.md
  - .opencode/agents/aether-scout.md
  - .opencode/agents/aether-sage.md
  - .opencode/agents/aether-auditor.md
  - .opencode/agents/aether-guardian.md
  - .opencode/agents/aether-measurer.md
  - .opencode/agents/aether-includer.md
  - .opencode/agents/aether-gatekeeper.md
  - .opencode/agents/aether-surveyor-nest.md
  - .opencode/agents/aether-surveyor-disciplines.md
  - .opencode/agents/aether-surveyor-pathogens.md
  - .opencode/agents/aether-surveyor-provisions.md
autonomous: true
requirements: [RESIL-01, RESIL-02, RESIL-03]

must_haves:
  truths:
    - "All 4 MEDIUM-risk agents have <failure_modes>, <success_criteria>, and <read_only> sections"
    - "All 9 LOW-risk read-only agents have <failure_modes>, <success_criteria>, and <read_only> sections"
    - "All 4 surveyor agents have their existing <success_criteria> updated in-place, plus new <failure_modes> and <read_only> sections added"
    - "Read-only agents explicitly state they must not write or modify files"
    - "Surveyor agents declare .aether/data/survey/ as their only permitted write location"
    - "Existing read-only statements in archaeologist and chaos are strengthened, not overwritten"
  artifacts:
    - path: ".opencode/agents/aether-chronicler.md"
      provides: "Chronicler resilience sections"
      contains: "<failure_modes>"
    - path: ".opencode/agents/aether-archaeologist.md"
      provides: "Archaeologist resilience sections"
      contains: "<read_only>"
    - path: ".opencode/agents/aether-surveyor-nest.md"
      provides: "Surveyor-nest updated success_criteria + new sections"
      contains: "<failure_modes>"
  key_links:
    - from: ".opencode/agents/aether-archaeologist.md"
      to: "Archaeologist's Law"
      via: "read_only reinforces existing read-only statement"
      pattern: "NEVER modify"
    - from: ".opencode/agents/aether-chaos.md"
      to: "Tester's Law"
      via: "read_only reinforces existing read-only statement"
      pattern: "NEVER modify"
    - from: ".opencode/agents/aether-surveyor-nest.md"
      to: "existing <success_criteria>"
      via: "updated in-place, not duplicated"
      pattern: "<success_criteria>"
---

<objective>
Add resilience sections to all 17 MEDIUM-risk and LOW-risk OpenCode agents, including the 4 surveyor agents that need special handling.

Purpose: MEDIUM-risk agents write docs/tests/analysis and need moderate boundary declarations. LOW-risk agents are read-only investigators and need tight "no writes" declarations. Surveyor agents already have `<success_criteria>` that must be updated in-place rather than appended.

Output: 17 agent files updated — 4 MEDIUM-risk with moderate resilience sections, 9 LOW-risk with strict read-only sections, 4 surveyors with updated success_criteria + new failure_modes + read_only.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-agent-resilience/23-CONTEXT.md
@.planning/phases/23-agent-resilience/23-RESEARCH.md
@.claude/rules/security.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resilience sections to 4 MEDIUM-risk agents</name>
  <files>
    .opencode/agents/aether-chronicler.md
    .opencode/agents/aether-probe.md
    .opencode/agents/aether-architect.md
    .opencode/agents/aether-keeper.md
  </files>
  <action>
Read each agent file completely before writing. Append three XML-tagged sections AFTER the existing `## Output Format` JSON block. Do NOT modify existing content.

MEDIUM-risk agents write documentation, tests, or analysis output. They get moderate-depth failure modes and boundary declarations.

### aether-chronicler.md

`<failure_modes>`: Minor = source file not found for documentation (search with glob, try alternate path), documentation target directory missing (create it). Major = would overwrite existing documentation with less content (STOP, confirm with user), source code contradicts current docs (STOP, flag inconsistency). Escalation per standard format.

`<success_criteria>`: Self-check = verify all documented APIs/features exist in current code, verify code examples run or compile, verify no broken links. Self-verify only. Report format = docs created, docs updated, code examples verified.

`<read_only>`: Global protected paths. Chronicler-specific: do not modify source code (documentation only), do not touch .aether/data/ (colony state), do not modify test files. May write to docs/, README.md, CHANGELOG.md, and inline code comments.

### aether-probe.md

`<failure_modes>`: Minor = test framework not installed (check package.json, suggest install), test file syntax error (read error, fix syntax, retry). Major = would delete or modify existing passing tests (STOP, confirm). Escalation per standard format.

`<success_criteria>`: Self-check = run all new tests (must pass), run existing tests (must still pass), verify coverage metrics improved or maintained. Self-verify only. Report format = tests added, coverage before/after, edge cases discovered.

`<read_only>`: Global protected paths. Probe-specific: do not modify source code (test files only), do not delete existing tests, do not modify .aether/ system files. May create and modify test files only.

### aether-architect.md

`<failure_modes>`: Minor = source material insufficient for synthesis (note gaps, proceed with available data), pattern not clearly identifiable (document uncertainty). Major = synthesis would contradict established architectural decisions (STOP, flag conflict). Escalation per standard format.

`<success_criteria>`: Self-check = verify all findings cite specific files or evidence, verify recommendations are actionable (not vague), verify output matches JSON schema. Self-verify only. Report format = patterns extracted, key findings, recommendations with evidence.

`<read_only>`: Global protected paths. Architect-specific: do not modify source code, do not modify colony state, do not modify agent definitions. May write synthesis documents and architectural analysis only.

### aether-keeper.md

`<failure_modes>`: Minor = pattern source file not found (search for related patterns, note gap), knowledge base directory structure missing (create it). Major = would overwrite existing curated patterns with less refined versions (STOP, confirm). Escalation per standard format.

`<success_criteria>`: Self-check = verify archived patterns follow the template structure, verify no duplicate patterns, verify categorization is correct. Self-verify only. Report format = patterns archived, updated, pruned, categories organized.

`<read_only>`: Global protected paths. Keeper-specific: do not modify source code, do not modify colony state files (COLONY_STATE.json, constraints.json, flags.json), do not modify agent definitions. May write to pattern/knowledge directories and .aether/data/ pattern area.

Use 2-attempt retry limit and tiered severity (minor=retry silently, major=stop immediately). Include escalation format. Never fail silently.
  </action>
  <verify>
For each of the 4 files, verify:
1. `grep -c '<failure_modes>' .opencode/agents/aether-{chronicler,probe,architect,keeper}.md` each returns 1
2. `grep -c '<success_criteria>' .opencode/agents/aether-{chronicler,probe,architect,keeper}.md` each returns 1
3. `grep -c '<read_only>' .opencode/agents/aether-{chronicler,probe,architect,keeper}.md` each returns 1
4. No existing content was removed (line counts increased)
  </verify>
  <done>Chronicler, probe, architect, and keeper each have all 3 resilience XML sections. MEDIUM-risk template used: moderate failure modes, self-verify only, role-specific write boundaries. All use tiered severity and escalation format.</done>
</task>

<task type="auto">
  <name>Task 2: Add resilience sections to 9 LOW-risk read-only agents</name>
  <files>
    .opencode/agents/aether-archaeologist.md
    .opencode/agents/aether-chaos.md
    .opencode/agents/aether-scout.md
    .opencode/agents/aether-sage.md
    .opencode/agents/aether-auditor.md
    .opencode/agents/aether-guardian.md
    .opencode/agents/aether-measurer.md
    .opencode/agents/aether-includer.md
    .opencode/agents/aether-gatekeeper.md
  </files>
  <action>
Read each agent file completely before writing. Append three XML-tagged sections AFTER the existing `## Output Format` JSON block. Do NOT modify existing content.

LOW-risk agents are read-only investigators. They get short, focused resilience sections. The `<read_only>` section is the most important one for these agents — it reinforces that they must not write anything.

**CRITICAL for archaeologist and chaos:** These agents already have explicit read-only statements in their body ("You NEVER modify code. You NEVER modify colony state." for archaeologist; "You NEVER modify code. You NEVER fix what you find." for chaos). The new `<read_only>` section REINFORCES these existing statements. Do NOT remove or contradict the existing statements.

### Template for all 9 LOW-risk agents:

`<failure_modes>` (keep SHORT — 3-4 sentences max per agent):
- Minor failures (retry once): file/command not found → try alternate path or broader search. Search yields no results → note what was tried, broaden scope.
- Escalation: After 2 attempts, report honestly what was searched, what was found/not found, and recommended next steps.
- Never fabricate findings. "Nothing found" or "insufficient evidence" is a valid result.
- Customize the 1-2 lines of minor failure examples to match each agent's investigation domain (e.g., archaeologist: git commands; chaos: test scenarios; guardian: vulnerability databases; measurer: profiling tools; etc.)

`<success_criteria>` (keep SHORT):
- Self-check: confirm findings cite specific files/lines/evidence, output matches JSON schema, all areas in scope were examined.
- Report format: 1 line summarizing what to include (findings count, evidence citations, confidence level).
- Customize the evidence type per agent (archaeologist: commits/blame; chaos: reproduction steps; auditor: code review evidence; etc.)

`<read_only>` (the key section for these agents):
- "You are a strictly read-only agent. You investigate and report only."
- "No Writes Permitted: Do not create, modify, or delete any files. Do not update colony state."
- "If Asked to Modify Something: Refuse. Explain your role is investigation only. Suggest the appropriate agent (Builder for code, Chronicler for docs, Queen for colony state)."
- For agents with existing read-only statements (archaeologist, chaos): add "This reinforces your existing [Law name]." to connect old and new.

The 9 agents and their investigation domains for customization:
1. **archaeologist** — git history, blame, commits (existing: "Archaeologist's Law")
2. **chaos** — edge cases, boundary conditions, error handling (existing: "Tester's Law")
3. **scout** — research, documentation, external sources
4. **sage** — analytics, metrics, trend data
5. **auditor** — code review, style, best practices
6. **guardian** — security vulnerabilities, secrets, injection
7. **measurer** — performance profiling, benchmarks, memory
8. **includer** — accessibility, WCAG compliance, screen readers
9. **gatekeeper** — dependencies, supply chain, licenses
  </action>
  <verify>
For each of the 9 files:
1. `grep -c '<failure_modes>' .opencode/agents/aether-{archaeologist,chaos,scout,sage,auditor,guardian,measurer,includer,gatekeeper}.md` each returns 1
2. `grep -c '<success_criteria>' .opencode/agents/aether-{archaeologist,chaos,scout,sage,auditor,guardian,measurer,includer,gatekeeper}.md` each returns 1
3. `grep -c '<read_only>' .opencode/agents/aether-{archaeologist,chaos,scout,sage,auditor,guardian,measurer,includer,gatekeeper}.md` each returns 1
4. `grep 'NEVER modify' .opencode/agents/aether-archaeologist.md` still returns existing statements (not removed)
5. `grep 'NEVER modify' .opencode/agents/aether-chaos.md` still returns existing statements (not removed)
6. New sections are SHORT (failure_modes under 15 lines each for low-risk agents)
  </verify>
  <done>All 9 LOW-risk agents have 3 resilience XML sections. Sections are concise (low-risk template). Read-only declarations are strict: no writes permitted. Existing read-only statements (archaeologist, chaos) are reinforced, not contradicted. Each agent's investigation domain is reflected in customized examples.</done>
</task>

<task type="auto">
  <name>Task 3: Update surveyor agents — in-place success_criteria + new resilience sections</name>
  <files>
    .opencode/agents/aether-surveyor-nest.md
    .opencode/agents/aether-surveyor-disciplines.md
    .opencode/agents/aether-surveyor-pathogens.md
    .opencode/agents/aether-surveyor-provisions.md
  </files>
  <action>
Read each surveyor agent file completely before writing. Surveyors need SPECIAL HANDLING because they already have `<success_criteria>` sections within their `<process>` area.

**Step 1: Update existing `<success_criteria>` in-place.**

Each surveyor already has a `<success_criteria>` section at the end of the file (after `</process>` and `<critical_rules>`). Update this existing section to add:
- A "Self-Check" heading with: verify survey documents exist and are readable, verify all template sections are filled (not placeholder), verify file paths reference real codebase files
- A "Completion Report Must Include" heading with: documents written with line counts, key findings summarized, confidence note
- Keep existing checklist items — ADD to them, do not remove any existing items

**Step 2: Add `<failure_modes>` section BEFORE the existing `<success_criteria>`** (after `<critical_rules>` closing tag, before `<success_criteria>`):

`<failure_modes>`:
- Minor = codebase directory not found (broaden search, try alternate paths), no files match expected pattern (note and document what was found instead)
- Major = survey would overwrite existing survey with less content (STOP, confirm), write target is outside .aether/data/survey/ (STOP, that is outside permitted scope)
- Escalation per standard format

**Step 3: Add `<read_only>` section AFTER the updated `<success_criteria>`** (at the very end):

`<read_only>`:
- "You may ONLY write to `.aether/data/survey/`. All other paths are read-only."
- Permitted write locations: list the specific survey documents this agent produces (nest: BLUEPRINT.md, CHAMBERS.md; disciplines: DISCIPLINES.md; pathogens: PATHOGENS.md; provisions: PROVISIONS.md)
- Globally protected: COLONY_STATE.json, constraints.json, dreams/, .env*
- "If a task would require writing outside the survey directory, stop and escalate."

Customize per surveyor:
- **nest** — writes BLUEPRINT.md and CHAMBERS.md
- **disciplines** — writes DISCIPLINES.md
- **pathogens** — writes PATHOGENS.md
- **provisions** — writes PROVISIONS.md
  </action>
  <verify>
For each of the 4 surveyor files:
1. `grep -c '<failure_modes>' .opencode/agents/aether-surveyor-{nest,disciplines,pathogens,provisions}.md` each returns 1
2. `grep -c '<success_criteria>' .opencode/agents/aether-surveyor-{nest,disciplines,pathogens,provisions}.md` each returns exactly 1 (updated in-place, NOT duplicated)
3. `grep -c '<read_only>' .opencode/agents/aether-surveyor-{nest,disciplines,pathogens,provisions}.md` each returns 1
4. Existing success_criteria checklist items are preserved (grep for existing items like "Nest focus parsed correctly" in surveyor-nest)
5. `grep 'survey/' .opencode/agents/aether-surveyor-nest.md` returns results in the read_only section
  </verify>
  <done>All 4 surveyor agents have updated success_criteria (in-place, not duplicated), new failure_modes, and new read_only sections. Surveyors declare .aether/data/survey/ as their only write location. Existing checklist items are preserved. Each surveyor lists its specific output documents.</done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. All 17 MEDIUM + LOW + Surveyor agent files contain `<failure_modes>`, `<success_criteria>`, and `<read_only>` sections
2. Surveyor agents have exactly 1 `<success_criteria>` each (updated in-place, not duplicated)
3. No existing agent content was removed or contradicted
4. LOW-risk agents have short, focused sections
5. MEDIUM-risk agents have moderate-depth sections
6. Surveyor agents declare limited write scope (.aether/data/survey/ only)
7. Archaeologist and chaos existing read-only statements are preserved
</verification>

<success_criteria>
- 17 agent files updated with resilience sections (51 total sections: 17 failure_modes + 17 success_criteria + 17 read_only)
- LOW-risk sections are concise (failure_modes under 15 lines each)
- MEDIUM-risk sections are moderate depth
- Surveyor success_criteria updated in-place (not duplicated)
- Existing agent rules and statements preserved
- Consistent escalation format and 2-attempt retry limit across all agents
</success_criteria>

<output>
After completion, create `.planning/phases/23-agent-resilience/23-02-SUMMARY.md`
</output>
