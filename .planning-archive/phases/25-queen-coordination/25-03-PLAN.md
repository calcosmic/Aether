---
phase: 25-queen-coordination
plan: 03
type: execute
wave: 2
depends_on:
  - 25-01
  - 25-02
files_modified:
  - .opencode/agents/aether-queen.md
  - .aether/docs/caste-system.md
  - .aether/workers.md
  - README.md
  - .opencode/OPENCODE.md
autonomous: true
requirements:
  - COORD-03
  - COORD-04

must_haves:
  truths:
    - "Queen Worker Castes section lists 23 agents (Architect and Guardian removed, Keeper and Auditor note merged capabilities)"
    - "caste-system.md architect and guardian rows annotated as 'merged into Keeper/Auditor — no dedicated agent file, caste emoji still active'"
    - "workers.md Architect section removed or annotated as merged into Keeper"
    - "README.md says '23 Specialized Agents' (not 25)"
    - "OPENCODE.md no longer lists aether-architect.md and aether-guardian.md"
    - "All agent count references consistently say 23"
  artifacts:
    - path: ".opencode/agents/aether-queen.md"
      provides: "Updated Worker Castes (23 agents)"
      contains: "Keeper (includes Architect capabilities)"
    - path: ".aether/docs/caste-system.md"
      provides: "Annotated caste table"
      contains: "merged into Keeper"
    - path: "README.md"
      provides: "Updated agent count"
      contains: "23 Specialized Agents"
  key_links:
    - from: ".aether/docs/caste-system.md"
      to: ".opencode/agents/aether-queen.md"
      via: "Consistent agent count and caste definitions"
      pattern: "23"
    - from: "README.md"
      to: ".aether/docs/caste-system.md"
      via: "Agent count matches caste system"
      pattern: "23 Specialized Agents"
---

<objective>
Update all agent count references from 25 to 23 and annotate merged agents in reference docs. This is the cleanup pass that ensures the colony presents a consistent 23-agent identity after the merges in Plan 02.

Purpose: Clean transition to 23-agent team — no stale "25 agents" references anywhere. Caste system preserves emoji mappings for workers named after Architect/Guardian patterns while noting the agent files are merged.

Output: Updated Queen Worker Castes, annotated caste-system.md, updated workers.md, README.md, and OPENCODE.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-queen-coordination/25-RESEARCH.md
@.planning/phases/25-queen-coordination/25-01-SUMMARY.md
@.planning/phases/25-queen-coordination/25-02-SUMMARY.md
@.opencode/agents/aether-queen.md
@.aether/docs/caste-system.md
@.aether/workers.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Queen Worker Castes and caste-system.md</name>
  <files>
    .opencode/agents/aether-queen.md
    .aether/docs/caste-system.md
  </files>
  <action>
**A. Update Queen Worker Castes section**

In `.opencode/agents/aether-queen.md`, find the `## Worker Castes` section. Make these changes:

1. In the `### Core Castes` list:
   - Remove the line `- Architect - Knowledge synthesis and design`
   - Update Keeper line or add note: In the `### Knowledge Cluster (Leafcutter Ants)` section, change the Keeper entry from:
     `- Keeper (`aether-keeper`) - Knowledge curation and pattern archiving`
     to:
     `- Keeper (`aether-keeper`) - Knowledge curation, pattern archiving, and architectural synthesis (includes Architect capabilities)`

2. In the `### Quality Cluster (Soldier Ants)` section:
   - Remove the line `- Guardian (`aether-guardian`) - Security audits and vulnerability scanning`
   - Update the Auditor entry from:
     `- Auditor (`aether-auditor`) - Code review with specialized lenses`
     to:
     `- Auditor (`aether-auditor`) - Code review with specialized lenses, including security audits (includes Guardian capabilities)`

**B. Annotate caste-system.md**

In `.aether/docs/caste-system.md`, do NOT remove the `architect` and `guardian` rows from the Caste Table — the emojis must stay because `get_caste_emoji()` in aether-utils.sh matches by name pattern.

Instead, add a note column or inline annotation:

For the `architect` row, add a note after the Name Patterns:
`| architect | 🏛️🐜 | Planning, design (merged into Keeper — no dedicated agent file) | Architect, Blueprint, Draft, Design, Plan, Schema, Frame, Sketch, Model |`

For the `guardian` row:
`| guardian | 🛡️🐜 | Security audits (merged into Auditor — no dedicated agent file) | Guardian, Defend, Patrol, Secure, Vigil, Watch, Safety, Security |`

Add a note in the `## Notes` section:
```markdown
- The `architect` and `guardian` castes are **merged**: their capabilities were absorbed by Keeper and Auditor respectively (Phase 25). The caste emoji rows remain because workers named after those patterns (e.g., "Blueprint-3", "Patrol-7") still resolve to the correct emojis via `get_caste_emoji()`. There are no longer dedicated agent files for these castes.
```

**Important:** Do NOT modify `get_caste_emoji()` in aether-utils.sh — it still needs to map architect/guardian name patterns to their emojis for any worker that gets named with those patterns.
  </action>
  <verify>
Read `.opencode/agents/aether-queen.md` Worker Castes section:
1. Architect is no longer listed as a separate entry in Core Castes
2. Guardian is no longer listed as a separate entry in Quality Cluster
3. Keeper entry mentions "includes Architect capabilities"
4. Auditor entry mentions "includes Guardian capabilities"

Read `.aether/docs/caste-system.md`:
1. architect row still exists with 🏛️🐜 emoji but has "merged into Keeper" note
2. guardian row still exists with 🛡️🐜 emoji but has "merged into Auditor" note
3. Notes section explains the merge and why rows are preserved
  </verify>
  <done>Queen Worker Castes reflects 23 agents, caste-system.md preserves emoji rows with merge annotations</done>
</task>

<task type="auto">
  <name>Task 2: Update workers.md, README.md, and OPENCODE.md agent counts</name>
  <files>
    .aether/workers.md
    README.md
    .opencode/OPENCODE.md
  </files>
  <action>
**A. Update workers.md**

In `.aether/workers.md`, find the `## Architect` section (at approximately line 624 per grep). This section defines the Architect worker discipline.

Add a merge note at the top of the section:
```markdown
## Architect (Merged into Keeper)

> **Note:** As of Phase 25, Architect capabilities are absorbed by the Keeper agent as "Architecture Mode". Workers named with Architect patterns still resolve to the 🏛️🐜 caste emoji. See `.opencode/agents/aether-keeper.md` for the merged definition.
```

If there is a `## Guardian` section in workers.md, apply the same pattern:
```markdown
## Guardian (Merged into Auditor)

> **Note:** As of Phase 25, Guardian capabilities are absorbed by the Auditor agent as "Security Lens Mode". Workers named with Guardian patterns still resolve to the 🛡️🐜 caste emoji. See `.opencode/agents/aether-auditor.md` for the merged definition.
```

Do NOT delete the sections — other content in workers.md may reference the Architect/Guardian discipline text. The merge note makes it clear these are historical.

**B. Update README.md**

In `README.md`, find and replace:
- `**25 Specialized Agents**` → `**23 Specialized Agents**`
- `(25 specialized agents)` → `(23 specialized agents)`

Search for any other "25 agents" references in README.md and update to "23 agents".

**C. Update OPENCODE.md**

In `.opencode/OPENCODE.md`, find the agent file listing that includes `aether-architect.md` and `aether-guardian.md`. Remove those two lines from the listing since the files no longer exist.

Update any agent count references from 25 to 23.

**D. Final sweep**

Run a broad grep for "25 agents" or "25 Specialized" across the repo to catch any remaining references:

```bash
grep -rn "25 \(agents\|Agents\|Specialized\|specialized\)" --include="*.md" --include="*.js" --include="*.sh" . | grep -v node_modules | grep -v .planning/
```

Update any remaining references found. Exclude `.aether/oracle/` files (auto-generated reports that are not user-facing).
  </action>
  <verify>
1. Read `.aether/workers.md` — confirm Architect section has merge note
2. Read `README.md` — confirm "23 Specialized Agents" appears (not 25)
3. Read `.opencode/OPENCODE.md` — confirm aether-architect.md and aether-guardian.md removed from listing
4. Run: `grep -rn "25 \(agents\|Agents\|Specialized\|specialized\)" --include="*.md" . | grep -v node_modules | grep -v .planning/ | grep -v oracle` — should return zero results
  </verify>
  <done>All agent count references updated from 25 to 23 across README.md, workers.md, and OPENCODE.md — clean 23-agent identity</done>
</task>

</tasks>

<verification>
1. `grep -rn "25 \(agents\|Agents\|Specialized\)" --include="*.md" . | grep -v node_modules | grep -v .planning/ | grep -v oracle` returns zero results
2. Queen Worker Castes section has no Architect or Guardian as separate entries
3. caste-system.md still has architect and guardian EMOJI rows (annotated as merged)
4. workers.md Architect section has merge note
5. README.md says "23 Specialized Agents"
6. OPENCODE.md does not list aether-architect.md or aether-guardian.md
7. `npm run lint:sync` — verify no new sync drift
</verification>

<success_criteria>
- All "25 agents" references updated to "23 agents" in user-facing docs
- Queen Worker Castes reflects merged state (Keeper includes Architect, Auditor includes Guardian)
- caste-system.md preserves emoji rows with clear merge annotations
- workers.md sections annotated (not deleted) for historical reference
- No orphaned references to deleted agents in any documentation
</success_criteria>

<output>
After completion, create `.planning/phases/25-queen-coordination/25-03-SUMMARY.md`
</output>
