---
phase: 25-queen-coordination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/agents/aether-queen.md
  - .claude/commands/ant/build.md
  - .opencode/commands/ant/build.md
  - .claude/commands/ant/status.md
  - .opencode/commands/ant/status.md
autonomous: true
requirements:
  - COORD-01
  - COORD-02

must_haves:
  truths:
    - "Queen agent definition contains a 4-tier escalation chain (Worker retry → Parent reassign → Queen reassign → User escalation)"
    - "Queen agent definition contains 6 named workflow patterns (SPBV, Investigate-Fix, Deep Research, Refactor, Compliance, Documentation Sprint)"
    - "Each workflow pattern has defined phases, a rollback step, and an announce line"
    - "build.md (both platforms) includes pattern selection logic and announcement display before spawning workers"
    - "build.md (both platforms) includes escalation banner with options format when Tier 4 fires"
    - "status.md (both platforms) shows conditional escalation state when escalated count > 0"
  artifacts:
    - path: ".opencode/agents/aether-queen.md"
      provides: "Escalation chain + workflow patterns"
      contains: "### Escalation Chain"
    - path: ".opencode/agents/aether-queen.md"
      provides: "6 named workflow patterns"
      contains: "## Workflow Patterns"
    - path: ".claude/commands/ant/build.md"
      provides: "Pattern selection and escalation banner"
      contains: "ESCALATION"
    - path: ".claude/commands/ant/status.md"
      provides: "Conditional escalation state display"
      contains: "Escalated"
  key_links:
    - from: ".opencode/agents/aether-queen.md"
      to: ".claude/commands/ant/build.md"
      via: "Escalation chain and workflow patterns referenced by build command"
      pattern: "Tier 4.*User escalation"
    - from: ".claude/commands/ant/build.md"
      to: ".claude/commands/ant/status.md"
      via: "Escalated flags visible in status display"
      pattern: "escalat"
---

<objective>
Rewrite the Queen agent definition to add a 4-tier escalation chain and 6 named workflow patterns. Wire escalation banners into build.md and conditional escalation state into status.md on both platforms.

Purpose: Formalize colony failure handling so workers retry silently through 3 tiers before surfacing to the user with clear options. Codify 6 named workflow patterns so the colony announces its approach at build start.

Output: Updated aether-queen.md with new sections, updated build.md (both platforms) with pattern selection + escalation banner, updated status.md (both platforms) with conditional escalation display.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-queen-coordination/25-RESEARCH.md
@.opencode/agents/aether-queen.md
@.claude/commands/ant/build.md
@.claude/commands/ant/status.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add escalation chain and workflow patterns to Queen agent definition</name>
  <files>.opencode/agents/aether-queen.md</files>
  <action>
Add two new sections to the Queen agent definition in `.opencode/agents/aether-queen.md`.

**1. Escalation Chain section** — Insert inside the existing `<failure_modes>` block, replacing the current "Minor Failures" and "Major Failures" structure with the 4-tier escalation chain. Keep the existing "Escalation Format" and "Reference" subsections. The new structure:

```markdown
### Escalation Chain

Failures escalate through four tiers. Tiers 1-3 are fully silent — the user never sees them. Only Tier 4 surfaces to the user.

**Tier 1: Worker retry** (silent, max 2 attempts)
The failing worker retries the operation with a corrected approach. Covers: file not found (alternate path), command error (fixed invocation), spawn status unexpected (re-read spawn tree).

**Tier 2: Parent reassignment** (silent)
If Tier 1 exhausted, the parent worker tries a different approach. Covers: different file path strategy, alternate command, different search pattern.

**Tier 3: Queen reassigns** (silent)
If Tier 2 exhausted, the Queen retires the failed worker and spawns a different caste for the same task. Example: Builder fails → Queen spawns Tracker to investigate root cause → Queen spawns fresh Builder with Tracker's findings.

**Tier 4: User escalation** (visible — only fires when Tier 3 fails)
Display the ESCALATION banner. Never skip the failed task silently — acknowledge it and present options.
```

Then add the escalation banner template:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ⚠ ESCALATION — QUEEN NEEDS YOU
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Task: {task description}
Phase: {phase number} — {phase name}

Tried:
  • Worker retry (2 attempts) — {what failed}
  • Parent tried alternate approach — {what failed}
  • Queen reassigned to {other caste} — {what failed}

Options:
  A) {option} — RECOMMENDED
  B) {option}
  C) Skip and continue — this task will be marked blocked

Awaiting your choice.
```

And the flag-logging instruction:
```bash
bash .aether/aether-utils.sh flag-add "blocker" "{task title}" "{failure summary}" "escalation" {phase_number}
```

Keep the existing Major Failures items (COLONY_STATE corruption, orphaned worker, destructive git) as a "### Critical Failures (STOP immediately)" subsection after the escalation chain — these are above the escalation chain (immediate stop, no retry).

**2. Workflow Patterns section** — Add a new `## Workflow Patterns` section after the existing `## Spawn Limits` section and before `## Output Format`. Include all 6 named patterns with the structure from the research:

Each pattern has: **Use when**, **Phases** (as a numbered arrow sequence), **Rollback** step, and **Announce** line.

The 6 patterns are:
1. SPBV (Scout-Plan-Build-Verify) — default for new features
2. Investigate-Fix — for known bugs (Symptom → Isolate → Prove → Fix → Guard)
3. Deep Research — oracle-led, no code changes
4. Refactor — snapshot before, verify equivalence after
5. Compliance — Auditor-led read-only audit
6. Documentation Sprint — Chronicler-led doc-only changes

After the 6 patterns, add a `### Pattern Selection` subsection with the keyword-based heuristic table from the research, and the display format:
```
━━ Pattern: {pattern_name} ━━
{announce_line}
```

Add a note that "Add Tests" is a variant of SPBV, not a separate pattern.
  </action>
  <verify>
Read `.opencode/agents/aether-queen.md` and confirm:
1. `### Escalation Chain` section exists inside `<failure_modes>`
2. All 4 tiers are documented (Tier 1 through Tier 4)
3. Escalation banner template is present with ━━━ formatting
4. flag-add command with source "escalation" is present
5. `## Workflow Patterns` section exists with 6 named patterns
6. Each pattern has Use when, Phases, Rollback, and Announce fields
7. `### Pattern Selection` subsection with keyword table exists
8. "Add Tests" mentioned as SPBV variant, not a 7th pattern
  </verify>
  <done>Queen agent definition has a 4-tier escalation chain in failure_modes and 6 named workflow patterns with selection heuristics</done>
</task>

<task type="auto">
  <name>Task 2: Wire pattern selection and escalation banner into build.md (both platforms)</name>
  <files>
    .claude/commands/ant/build.md
    .opencode/commands/ant/build.md
  </files>
  <action>
Make two additions to build.md on both platforms (`.claude/commands/ant/build.md` and `.opencode/commands/ant/build.md`):

**A. Pattern selection and announcement (Step 5, before spawning workers)**

After the existing "Display spawn plan with caste emojis" block in Step 5 and before Step 5.1, add a new sub-step:

```markdown
### Step 5.0.5: Select and Announce Workflow Pattern

Examine the phase name and task descriptions. Select the first matching pattern:

| Phase contains | Pattern |
|----------------|---------|
| "bug", "fix", "error", "broken", "failing" | Investigate-Fix |
| "research", "oracle", "explore", "investigate" | Deep Research |
| "refactor", "restructure", "clean", "reorganize" | Refactor |
| "security", "audit", "compliance", "accessibility", "license" | Compliance |
| "docs", "documentation", "readme", "guide" | Documentation Sprint |
| (default) | SPBV |

Display the selected pattern:
```
━━ Pattern: {pattern_name} ━━
{announce_line from Queen's Workflow Patterns definition}
```

Store `selected_pattern` for inclusion in the BUILD SUMMARY (Step 7).
```

**B. Escalation in wave failure path (Step 5.2)**

In the existing "Check for total wave failure" section in Step 5.2, after the WAVE FAILURE banner, add an additional escalation path. After the existing wave failure block, add:

```markdown
**Partial wave failure — escalation path:**

If SOME (but not all) workers in the wave failed:
1. For each failed worker, attempt Tier 3 escalation: Queen spawns a different caste for the same task
2. If Tier 3 succeeds: continue to next wave
3. If Tier 3 fails: display the Tier 4 ESCALATION banner (from Queen agent definition):

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ⚠ ESCALATION — QUEEN NEEDS YOU
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Task: {failed task description}
Phase: {phase number} — {phase name}

Tried:
  • Worker retry (2 attempts) — {what failed}
  • Parent tried alternate approach — {what failed}
  • Queen reassigned to {other caste} — {what failed}

Options:
  A) {recommended option} — RECOMMENDED
  B) {alternate option}
  C) Skip and continue — this task will be marked blocked

Awaiting your choice.
```

Log escalation as flag:
Run using the Bash tool with description "Logging escalation...": `bash .aether/aether-utils.sh flag-add "blocker" "{task title}" "{failure summary}" "escalation" {phase_number}`
```

**C. Pattern in BUILD SUMMARY (Step 7)**

In the BUILD SUMMARY display block, add a `Pattern:` line after the `Phase {id}: {name}` line:
```
Pattern:  {selected_pattern}
```

Apply all three changes (A, B, C) to BOTH `.claude/commands/ant/build.md` and `.opencode/commands/ant/build.md`.
  </action>
  <verify>
Read both build.md files and confirm:
1. Step 5.0.5 (or equivalent) exists with pattern selection table and announcement display
2. Escalation banner is present in the wave failure section
3. flag-add with source "escalation" is present
4. BUILD SUMMARY includes Pattern line
5. Both platforms have identical additions
  </verify>
  <done>build.md on both platforms has pattern selection/announcement at build start, escalation banner in wave failure path, and pattern in BUILD SUMMARY</done>
</task>

<task type="auto">
  <name>Task 3: Add conditional escalation state to status.md (both platforms)</name>
  <files>
    .claude/commands/ant/status.md
    .opencode/commands/ant/status.md
  </files>
  <action>
Add conditional escalation state display to status.md on both platforms.

In Step 2 (Compute Summary), after the existing **Flags** section that runs `flag-check-blockers`, add:

```markdown
**Escalation state:**
Count escalated flags by checking for blocker flags with source "escalation":

Run using the Bash tool with description "Checking escalation state...":
```bash
escalated_count=$(bash .aether/aether-utils.sh flag-list --type blocker 2>/dev/null | jq '[.result.flags[] | select(.source == "escalation")] | length' 2>/dev/null || echo "0")
echo "escalated_count=$escalated_count"
```
```

In Step 3 (Display), after the existing Flags line (`🚩 Flags: ...`), add a conditional line:

```markdown
{if escalated_count > 0:}
⚠️  Escalated: {escalated_count} task(s) awaiting your decision
{end if}
```

This line ONLY renders when escalated_count is greater than 0. When zero, nothing is displayed — no noise.

Apply to BOTH `.claude/commands/ant/status.md` and `.opencode/commands/ant/status.md`.
  </action>
  <verify>
Read both status.md files and confirm:
1. Escalation state computation (flag-list with jq filter for source "escalation") is present
2. Conditional display line with "Escalated:" and task count is present
3. Display is conditional on escalated_count > 0
4. Both platforms have identical additions
  </verify>
  <done>status.md on both platforms shows conditional escalation state only when escalated tasks exist</done>
</task>

</tasks>

<verification>
1. Read `.opencode/agents/aether-queen.md` — confirm Escalation Chain (4 tiers) and Workflow Patterns (6 patterns) sections exist
2. Read `.claude/commands/ant/build.md` — confirm pattern selection, escalation banner, and pattern in BUILD SUMMARY
3. Read `.opencode/commands/ant/build.md` — confirm same additions as Claude Code build.md
4. Read `.claude/commands/ant/status.md` — confirm conditional escalation display
5. Read `.opencode/commands/ant/status.md` — confirm same additions as Claude Code status.md
6. Run `npm run lint:sync` — verify no new sync drift introduced (pre-existing drift acceptable)
</verification>

<success_criteria>
- Queen agent has 4-tier escalation chain with banner template
- Queen agent has 6 named workflow patterns with selection heuristics
- build.md (both platforms) announces pattern at build start
- build.md (both platforms) displays escalation banner on partial wave failure
- status.md (both platforms) shows escalation count conditionally
- No new files created — all edits to existing files
</success_criteria>

<output>
After completion, create `.planning/phases/25-queen-coordination/25-01-SUMMARY.md`
</output>
