---
phase: 25-queen-coordination
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/agents/aether-keeper.md
  - .opencode/agents/aether-auditor.md
  - .opencode/agents/aether-architect.md
  - .opencode/agents/aether-guardian.md
  - .claude/commands/ant/organize.md
  - .opencode/commands/ant/organize.md
autonomous: true
requirements:
  - COORD-03
  - COORD-04

must_haves:
  truths:
    - "Keeper agent definition includes Architecture Mode ('Keeper (Architect)') with absorbed Synthesis Workflow"
    - "Auditor agent definition includes Security Lens Mode ('Auditor (Guardian)') with absorbed security domains"
    - "Architect agent file is deleted (no stub/redirect)"
    - "Guardian agent file is deleted (no stub/redirect)"
    - "organize.md (both platforms) spawns aether-keeper instead of aether-architect"
    - "No command file references aether-architect or aether-guardian as spawn targets"
  artifacts:
    - path: ".opencode/agents/aether-keeper.md"
      provides: "Keeper with Architecture Mode"
      contains: "Architecture Mode"
    - path: ".opencode/agents/aether-auditor.md"
      provides: "Auditor with Security Lens Mode"
      contains: "Security Lens Mode"
  key_links:
    - from: ".claude/commands/ant/organize.md"
      to: ".opencode/agents/aether-keeper.md"
      via: "spawn target updated from aether-architect to aether-keeper"
      pattern: "aether-keeper"
---

<objective>
Merge Architect capabilities into Keeper and Guardian capabilities into Auditor. Delete old agent files. Update spawn references in organize.md.

Purpose: Consolidate overlapping agents from 25 to 23, making the colony's agent set more coherent. Keeper absorbs Architect's synthesis workflow as "Architecture Mode", Auditor absorbs Guardian's security domains as "Security Lens Mode".

Output: Updated aether-keeper.md and aether-auditor.md with merged capabilities, deleted aether-architect.md and aether-guardian.md, updated organize.md spawn targets.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-queen-coordination/25-RESEARCH.md
@.opencode/agents/aether-keeper.md
@.opencode/agents/aether-auditor.md
@.opencode/agents/aether-architect.md
@.opencode/agents/aether-guardian.md
@.claude/commands/ant/organize.md
@.opencode/commands/ant/organize.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge Architect into Keeper and Guardian into Auditor</name>
  <files>
    .opencode/agents/aether-keeper.md
    .opencode/agents/aether-auditor.md
  </files>
  <action>
**A. Merge Architect capabilities into Keeper**

Edit `.opencode/agents/aether-keeper.md` to add an Architecture Mode section. Insert after the existing `## Your Role` section and before `## Knowledge Organization`:

```markdown
### Architecture Mode ("Keeper (Architect)")

When tasked with knowledge synthesis, architectural analysis, or documentation coordination — roles previously handled by the Architect agent:

**Activate when:** Task description mentions "synthesize", "analyze architecture", "extract patterns", "design", or "coordinate documentation"

**In this mode:**
- Log as: `activity-log "ACTION" "{your_name} (Keeper — Architect Mode)" "description"`
- Apply the Synthesis Workflow: Gather → Analyze → Structure → Document
- Output JSON: add `"mode": "architect"` alongside standard Keeper fields

**Synthesis Workflow (from Architect):**
1. Gather — collect all relevant information
2. Analyze — identify patterns and themes
3. Structure — organize into logical hierarchy
4. Document — create clear, actionable output

**Escalation format (same as standard Keeper):**
```
BLOCKED: [what was attempted, twice]
Options:
  A) [First option with trade-off]
  B) [Second option with trade-off]
  C) Skip this item and note it as a gap
Awaiting your choice.
```
```

Also update the Keeper's `<failure_modes>` section to add an Architect-mode failure case:
- Under Minor: add "Synthesis source material insufficient → note gaps explicitly, proceed with available data, document what could not be analyzed"
- Under Major: add "Synthesis would contradict an established architectural decision in colony state → STOP, flag the conflict and present options"

**B. Merge Guardian capabilities into Auditor**

Edit `.opencode/agents/aether-auditor.md` to add a Security Lens Mode section. Insert after the existing `### Maintainability Lens` section and before `## Severity Ratings`:

```markdown
### Security Lens Mode ("Auditor (Guardian)")

When tasked with security audits, vulnerability scanning, or threat assessment — roles previously handled by the Guardian agent:

**Activate when:** Task description mentions "security", "vulnerability", "CVE", "OWASP", "threat assessment", or "security audit"

**In this mode:**
- Log as: `activity-log "ACTION" "{your_name} (Auditor — Guardian Mode)" "description"`
- Apply the Security Audit domains below
- Output JSON: add `"mode": "guardian"` alongside standard Auditor fields

**Security Domains (from Guardian):**

#### Authentication & Authorization
- Session management, Token handling (JWT, OAuth), Permission checks, RBAC, MFA

#### Input Validation
- SQL injection, XSS, CSRF, Command injection, Path traversal, File upload validation

#### Data Protection
- Encryption at rest/transit, Secret management, PII handling, Data retention

#### Infrastructure
- Dependency vulnerabilities (CVEs), Container security, Network security, Logging security, Configuration security
```

Also update the Auditor's `<failure_modes>` to add a Guardian-mode failure case:
- Add: "CVE database or vulnerability scanner unavailable → perform manual code review against OWASP Top 10 patterns and note the tool limitation"

Update the Auditor's `<read_only>` section to clarify that in Security Lens Mode, the agent remains strictly read-only (same as Guardian was).

**Important:** Preserve the existing ant emoji caste identities. Keeper stays 📚, Auditor stays 👥. The caste emoji in caste-system.md for `architect` (🏛️🐜) and `guardian` (🛡️🐜) still applies to workers with those NAME patterns — they are not changed.
  </action>
  <verify>
Read `.opencode/agents/aether-keeper.md` and confirm:
1. `### Architecture Mode ("Keeper (Architect)")` section exists
2. Synthesis Workflow (Gather → Analyze → Structure → Document) is present
3. Activity log format includes "(Keeper — Architect Mode)"
4. Architect-mode failure cases added to failure_modes

Read `.opencode/agents/aether-auditor.md` and confirm:
1. `### Security Lens Mode ("Auditor (Guardian)")` section exists
2. All 4 Guardian security domains are present (Auth, Input Validation, Data Protection, Infrastructure)
3. Activity log format includes "(Auditor — Guardian Mode)"
4. Guardian-mode failure case added to failure_modes
5. Read-only boundary still applies in Security Lens Mode
  </verify>
  <done>Keeper has Architecture Mode with Synthesis Workflow, Auditor has Security Lens Mode with all Guardian security domains</done>
</task>

<task type="auto">
  <name>Task 2: Delete old agent files and update spawn references</name>
  <files>
    .opencode/agents/aether-architect.md
    .opencode/agents/aether-guardian.md
    .claude/commands/ant/organize.md
    .opencode/commands/ant/organize.md
  </files>
  <action>
**A. Verify no orphaned spawn references exist**

Before deleting, run a grep to confirm no command file (besides organize.md, which we're fixing) spawns aether-architect or aether-guardian:

```bash
grep -rn "aether-architect\|aether-guardian" .claude/commands/ .opencode/commands/ | grep -v "organize.md"
```

Expected: Zero results (or only the Queen's Worker Castes list, which is updated in Plan 03).

If unexpected results found, update those references to use aether-keeper or aether-auditor respectively BEFORE deleting the files.

**B. Delete old agent files**

Delete both files (Claude's discretion from user context — deleting is preferred over redirects per research recommendation):

```bash
git rm .opencode/agents/aether-architect.md
git rm .opencode/agents/aether-guardian.md
```

Use `git rm` so the deletion is tracked. Do NOT just `rm` them.

**C. Update organize.md spawn references**

In `.claude/commands/ant/organize.md`:
- Change `subagent_type="aether-architect"` to `subagent_type="aether-keeper"`
- Update the FALLBACK comment from "Architect Ant - synthesizes knowledge" to "Keeper Ant - curates knowledge and synthesizes patterns"
- Update any "Architect" references in the worker spec section to "Keeper"

In `.opencode/commands/ant/organize.md`:
- Update the NOTE comment from referencing "aether-architect" to referencing "aether-keeper"
- Update any "Architect" references to "Keeper" in the role injection text

**Important:** The Queen's Worker Castes section (which lists Architect and Guardian) is updated in Plan 03 to avoid conflicting edits with Plan 01 which also modifies the Queen file.
  </action>
  <verify>
1. Confirm aether-architect.md and aether-guardian.md no longer exist in `.opencode/agents/`
2. Run: `ls .opencode/agents/ | wc -l` — should be 23 (was 25: 24 agents + workers.md, minus 2 deleted = 23)
3. Run: `grep -rn "aether-architect\|aether-guardian" .claude/commands/ .opencode/commands/` — should return zero results (or only the Queen agent file's Worker Castes, fixed in Plan 03)
4. Read `.claude/commands/ant/organize.md` — confirm `subagent_type="aether-keeper"`
5. Read `.opencode/commands/ant/organize.md` — confirm updated reference
  </verify>
  <done>Architect and Guardian agent files deleted, organize.md (both platforms) spawns aether-keeper instead of aether-architect, no orphaned spawn references remain</done>
</task>

</tasks>

<verification>
1. Read `.opencode/agents/aether-keeper.md` — Architecture Mode section present
2. Read `.opencode/agents/aether-auditor.md` — Security Lens Mode section present
3. Confirm `.opencode/agents/aether-architect.md` does not exist
4. Confirm `.opencode/agents/aether-guardian.md` does not exist
5. `ls .opencode/agents/ | wc -l` returns 23
6. `grep -rn "aether-architect\|aether-guardian" .claude/commands/ .opencode/commands/` returns zero results (Queen fixed in Plan 03)
7. Run `npm run lint:sync` — verify no new sync drift
</verification>

<success_criteria>
- Keeper has "Keeper (Architect)" Architecture Mode with Synthesis Workflow
- Auditor has "Auditor (Guardian)" Security Lens Mode with 4 security domains
- aether-architect.md deleted from .opencode/agents/
- aether-guardian.md deleted from .opencode/agents/
- organize.md (both platforms) uses aether-keeper as spawn target
- Emoji identities preserved (📚 Keeper, 👥 Auditor, 🏛️ architect caste, 🛡️ guardian caste all unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/25-queen-coordination/25-02-SUMMARY.md`
</output>
