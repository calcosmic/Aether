---
phase: 31-integration-verification-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/docs/README.md
  - .aether/docs/archive/QUEEN_ANT_ARCHITECTURE.md
  - .aether/docs/archive/implementation-learnings.md
  - .aether/docs/archive/constraints.md
  - .aether/docs/archive/pathogen-schema.md
  - .aether/docs/archive/pathogen-schema-example.json
  - .aether/docs/archive/progressive-disclosure.md
  - README.md
  - repo-structure.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-04

must_haves:
  truths:
    - ".aether/docs/ contains 8 root-level files plus the disciplines/ subdirectory"
    - "Archived docs exist in .aether/docs/archive/ (not deleted)"
    - "repo-structure.md exists at repo root with top-level directory descriptions"
    - "README.md features v2.0 agents as a key capability with action-oriented tone"
    - "CLEAN-02 verified: no root-level docs/ directory exists; .planning/ is organized as project history and untouched"
  artifacts:
    - path: ".aether/docs/archive/"
      provides: "Archive directory for removed docs"
    - path: "repo-structure.md"
      provides: "High-level repo overview for re-orientation"
    - path: "README.md"
      provides: "v2.0 README with agent capabilities"
    - path: ".aether/docs/README.md"
      provides: "Updated docs index reflecting archived files"
  key_links:
    - from: "README.md"
      to: "repo-structure.md"
      via: "cross-reference link"
      pattern: "repo-structure"
    - from: ".aether/docs/README.md"
      to: ".aether/docs/archive/"
      via: "note about archived docs"
      pattern: "archive"
---

<objective>
Curate .aether/docs/ from 14 files down to 8-10, create repo-structure.md, and update README.md for v2.0.

Purpose: Clean the docs directory for quick re-orientation after a break, and update outward-facing documentation to reflect the v2.0 agent system.
Output: 8 keeper docs + archive/, repo-structure.md at root, v2.0 README
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-integration-verification-cleanup/31-RESEARCH.md
@.planning/phases/31-integration-verification-cleanup/31-CONTEXT.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Curate docs — archive 6 files, update docs/README.md (CLEAN-01, CLEAN-02)</name>
  <files>
    .aether/docs/README.md
    .aether/docs/archive/QUEEN_ANT_ARCHITECTURE.md
    .aether/docs/archive/implementation-learnings.md
    .aether/docs/archive/constraints.md
    .aether/docs/archive/pathogen-schema.md
    .aether/docs/archive/pathogen-schema-example.json
    .aether/docs/archive/progressive-disclosure.md
  </files>
  <action>
**Create archive directory:** `mkdir -p .aether/docs/archive/`

**Move 6 files to archive (per research recommendation):**
1. `QUEEN_ANT_ARCHITECTURE.md` — superseded by agent files themselves
2. `implementation-learnings.md` — historical, not actively referenced
3. `constraints.md` — content now lives in agent definitions
4. `pathogen-schema.md` — only relevant if actively using pathogens
5. `pathogen-schema-example.json` — archive with its schema doc
6. `progressive-disclosure.md` — design philosophy, not referenced by code

Use `git mv` for each file to preserve history.

**Remaining 8 keeper docs (root level):**
1. `README.md` — navigation index
2. `caste-system.md` — referenced by system; core reference
3. `error-codes.md` — E_* constants developer reference
4. `known-issues.md` — active bugs; essential for re-orientation
5. `pheromones.md` — user guide for signals
6. `QUEEN-SYSTEM.md` — queen wisdom system docs
7. `queen-commands.md` — queen-init/read/promote
8. `QUEEN.md` — generated wisdom file

**Keep `disciplines/` subdirectory as-is** — it's referenced by continue.md for worker training. This counts as a unit, not 7 separate files.

**Update `.aether/docs/README.md`** to:
- Remove entries for the 6 archived files
- Add a note: "Archived docs available in `archive/` subdirectory"
- Keep entries for the 8 remaining docs
- Keep entry for `disciplines/` subdirectory

**CLEAN-02 verification:** Confirm `docs/` does not exist at repo root (it doesn't). Confirm `.planning/` directory is untouched (per user decision: keep as-is). Record both as verified-by-inspection. No file changes needed for CLEAN-02.
  </action>
  <verify>
1. `ls .aether/docs/*.md .aether/docs/*.json 2>/dev/null | wc -l` shows 8 root-level files (README.md, caste-system.md, error-codes.md, known-issues.md, pheromones.md, QUEEN-SYSTEM.md, queen-commands.md, QUEEN.md)
2. `ls .aether/docs/archive/ | wc -l` shows 6 files
3. `ls .aether/docs/disciplines/ | wc -l` shows 7 files (unchanged)
4. `ls docs/ 2>/dev/null` returns nothing (no root-level docs/ directory)
  </verify>
  <done>
.aether/docs/ has 8 root-level files + disciplines/ subdirectory. 6 files archived to .aether/docs/archive/. CLEAN-02 confirmed by inspection (.planning/ untouched, no root docs/).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create repo-structure.md and update README.md for v2.0 (CLEAN-04)</name>
  <files>
    repo-structure.md
    README.md
  </files>
  <action>
**Create `repo-structure.md` at repo root.** High-level overview only. Audience is the project owner coming back after a break. Structure:

```markdown
# Repo Structure

One-line descriptions of top-level directories and key files.

## Directories

| Directory | Purpose |
|-----------|---------|
| `.aether/` | Colony system — workers.md, aether-utils.sh, docs/, utils/, templates/ |
| `.aether/data/` | Colony state (COLONY_STATE.json, pheromones, spawn tree) — never distributed |
| `.aether/docs/` | Developer reference — error codes, known issues, caste system |
| `.claude/commands/ant/` | 34 Claude Code slash commands (/ant:build, /ant:plan, etc.) |
| `.claude/agents/ant/` | 22 Claude Code subagents (aether-builder, aether-queen, etc.) |
| `.opencode/` | OpenCode equivalent commands and agents |
| `.planning/` | Development history — roadmap, phases, requirements |
| `bin/` | CLI tools — cli.js, validate-package.sh, lib/ |
| `tests/` | AVA unit tests + bash integration tests |
| `src/` | Source modules (thin — main logic in bin/) |

## Key Files

| File | Purpose |
|------|---------|
| `package.json` | npm package config — version, scripts, dependencies |
| `CLAUDE.md` | Claude Code instructions — architecture, rules, workflows |
| `CHANGELOG.md` | Version history |
| `TO-DOS.md` | Development backlog |
```

Adjust the exact content based on what actually exists at repo root. Keep it concise — no deep nesting, no multi-paragraph descriptions.

**Update `README.md` for v2.0.** Make these specific changes:

1. **Version line:** Change `**v4.0.0** — Distribution Simplification (runtime/ eliminated)` to `**v2.0.0** — Worker Emergence (22 real Claude Code agents)`

2. **Description:** Update the center description to mention agents:
   `A multi-agent orchestration system for Claude Code — 22 specialized agents that spawn, coordinate, and self-organize.`

3. **Key Features section:** Update:
   - Change "23 Specialized Agents" to "22 Claude Code Agents" and note they're real subagents, not just definitions
   - Add a bullet: "**Real Agent Spawning** — `/ant:build` resolves `aether-builder` as a genuine Claude Code subagent"

4. **Installation section:** Add a line after the install command noting agents are installed:
   `This installs 22 Claude Code agents to ~/.claude/agents/ant/ plus 34 slash commands.`

5. **The Castes section:** Update the table to include all current castes (add Keeper, Tracker, Probe, Weaver, Auditor, Gatekeeper, Includer, Measurer, Ambassador, Chronicler, Sage, Archaeologist). Group by tier: Core (Queen, Builder, Watcher, Scout), Orchestration (Route-Setter, Surveyor variants), Specialists (Keeper, Tracker, Probe, Weaver, Auditor), Niche (Gatekeeper, Includer, Measurer, Chaos, Archaeologist, Ambassador, Chronicler, Sage).

6. **Tone:** Make it action-oriented. Examples:
   - "Run `/ant:build 1` and a real `aether-builder` agent spawns to write your code"
   - "Run `/ant:colonize` and 4 surveyor agents map your entire codebase"

7. **Add a link to repo-structure.md:** In the File Structure section, add: "See [repo-structure.md](repo-structure.md) for a quick overview of all directories."

Do NOT change: Quick Start, CLI Commands, Safety Features, License, Acknowledgments sections (leave as-is unless something is factually wrong).
  </action>
  <verify>
1. `cat repo-structure.md` exists and has directory table
2. `grep "v2.0.0" README.md` finds the version reference
3. `grep "22" README.md` finds the agent count reference
4. `grep "repo-structure" README.md` finds the cross-link
5. README tone feels action-oriented — commands lead to concrete agent actions
  </verify>
  <done>
repo-structure.md exists at root with high-level directory descriptions. README.md updated: v2.0.0 version, 22 agents featured, action-oriented tone, full caste table, cross-link to repo-structure.md.
  </done>
</task>

</tasks>

<verification>
- `ls .aether/docs/*.md .aether/docs/*.json 2>/dev/null | wc -l` returns 8
- `ls .aether/docs/archive/ | wc -l` returns 6
- `cat repo-structure.md` has directory overview
- `grep "v2.0.0" README.md` matches
- `.planning/` directory has NOT been modified
</verification>

<success_criteria>
- .aether/docs/ has exactly 8 root-level files + disciplines/ + archive/ (CLEAN-01)
- .planning/ untouched, no root docs/ directory (CLEAN-02 verified by inspection)
- repo-structure.md exists at repo root with one-line directory descriptions (CLEAN-04)
- README.md features v2.0 agents with action-oriented tone (CLEAN-04)
</success_criteria>

<output>
After completion, create `.planning/phases/31-integration-verification-cleanup/31-02-SUMMARY.md`
</output>
