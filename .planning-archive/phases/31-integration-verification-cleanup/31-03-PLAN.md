---
phase: 31-integration-verification-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 31-01
  - 31-02
files_modified:
  - package.json
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - CHANGELOG.md
autonomous: false

requirements:
  - CLEAN-02

must_haves:
  truths:
    - "package.json version is 2.0.0"
    - "ROADMAP.md shows v2.0 as shipped with completion date"
    - "STATE.md reflects Phase 31 complete and v2.0 shipped"
    - "CHANGELOG.md has a v2.0.0 entry documenting Worker Emergence"
    - "User has approved the final state before npm publish + git tag"
  artifacts:
    - path: "package.json"
      provides: "Version 2.0.0"
      contains: "2.0.0"
    - path: ".planning/ROADMAP.md"
      provides: "v2.0 milestone marked shipped"
      contains: "shipped"
    - path: ".planning/STATE.md"
      provides: "Phase 31 complete status"
    - path: "CHANGELOG.md"
      provides: "v2.0.0 release entry"
      contains: "2.0.0"
  key_links:
    - from: "package.json"
      to: "CHANGELOG.md"
      via: "version number consistency"
      pattern: "2.0.0"
---

<objective>
Mark v2.0 as shipped: version bump to 2.0.0, update planning docs, add changelog entry, then get user approval before npm publish and git tag.

Purpose: Formally close the v2.0 Worker Emergence milestone with proper version tracking, planning document updates, and a verified release.
Output: package.json at 2.0.0, ROADMAP/STATE updated, CHANGELOG entry, user-approved release
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-integration-verification-cleanup/31-CONTEXT.md
@.planning/phases/31-integration-verification-cleanup/31-01-SUMMARY.md
@.planning/phases/31-integration-verification-cleanup/31-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version bump, ROADMAP/STATE update, CHANGELOG entry</name>
  <files>
    package.json
    .planning/ROADMAP.md
    .planning/STATE.md
    CHANGELOG.md
  </files>
  <action>
**package.json:** Change `"version": "4.0.0"` to `"version": "2.0.0"`. This aligns the npm version with the "v2.0 Worker Emergence" milestone name. Per user decision — this is intentionally setting 2.0.0 even though the current version is 4.0.0.

**ROADMAP.md and STATE.md updates** (these edits are completion record-keeping only — marking v2.0 as shipped and Phase 31 as done. This is NOT reorganization of project history, which is kept as-is per user decision):

**ROADMAP.md:**
1. Change `🚧 **v2.0 Worker Emergence** — Phases 27-31 (in progress)` to `✅ **v2.0 Worker Emergence** — Phases 27-31 (shipped {today's date})`
2. Update Phase 31 entry in the progress table: set Plans Complete to `3/3`, Status to `Complete`, Completed to today's date
3. Update Phase 31 Plans line from `**Plans**: TBD` to `**Plans**: 3 plans` with the plan list:
   ```
   Plans:
   - [x] 31-01-PLAN.md — Integration verification + bash bug fix
   - [x] 31-02-PLAN.md — Docs curation + repo documentation
   - [x] 31-03-PLAN.md — Ship v2.0 (version bump, release)
   ```
4. Also mark Phase 27 plans as complete (they were executed as part of the v2.0 work but the table still shows `0/4`)

**STATE.md:**
1. Update Phase line to: `Phase: 31 of 31 (Integration Verification + Cleanup) — COMPLETE`
2. Update Status to: `Status: v2.0 Worker Emergence shipped`
3. Update Progress to: `Progress: [██████████] 100% (31/31 phases complete)`
4. Update Performance Metrics cumulative totals to include Phase 31 plans
5. Update Last activity to today's date with Phase 31 completion note

**CHANGELOG.md:** Add a v2.0.0 entry at the top documenting the Worker Emergence milestone:
- **v2.0.0 — Worker Emergence** ({today's date})
  - 22 Claude Code subagents: every ant caste is now a real subagent resolvable via the Task tool
  - Agent distribution: `npm install -g .` syncs agents to hub, `aether update` delivers to target repos
  - Agent quality: 6 AVA tests enforce frontmatter, tool restrictions, naming, and content standards
  - Bash line wrapping bug fixed across 7 command files (58 instances)
  - .aether/docs/ curated from 14 to 8 files + archive
  - repo-structure.md added for quick re-orientation
  - README.md updated with v2.0 agent capabilities
  - Phases 27-31: 10 plans across 5 phases
  </action>
  <verify>
1. `node -e "console.log(require('./package.json').version)"` outputs `2.0.0`
2. `grep "shipped" .planning/ROADMAP.md | grep "v2.0"` finds the shipped milestone
3. `grep "COMPLETE" .planning/STATE.md | head -1` shows Phase 31 complete
4. `grep "2.0.0" CHANGELOG.md` finds the changelog entry
  </verify>
  <done>
package.json is 2.0.0. ROADMAP shows v2.0 shipped. STATE shows all 31 phases complete at 100%. CHANGELOG has v2.0.0 entry.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies v2.0 release and approves publish</name>
  <files>(none — verification only)</files>
  <action>
Present the v2.0 release summary to the user for review. After user approval, execute:
1. `npm publish` to push v2.0.0 to npm registry
2. `git tag v2.0.0` to tag the release
3. `git push --tags` to push the tag

**Important: npm version downgrade warning.**
The npm registry already has published versions 3.x and 4.x (e.g., 3.1.17+). Publishing v2.0.0 may be rejected by npm because it sees this as a version downgrade. Present these options to the user:
- **(a)** Use `npm publish --tag v2` to publish under a non-latest dist-tag (avoids overwriting the "latest" pointer, still publishes successfully)
- **(b)** Accept that only the git tag succeeds and skip `npm publish` (the git tag `v2.0.0` marks the milestone without changing the npm registry)
- **(c)** Bump to a higher version instead (e.g., 5.0.0) — this changes the user's locked decision so only offer if they want it

**What was built across all Phase 31 plans:**
- All 22 agents verified and synced to hub
- Bash line wrapping bug fixed in 7 command files with regression test
- .aether/docs/ curated (8 keepers, 6 archived)
- repo-structure.md created at root
- README.md updated for v2.0 (action-oriented, 22 agents featured)
- package.json bumped to 2.0.0
- ROADMAP.md and STATE.md mark v2.0 as shipped
- CHANGELOG.md has the v2.0.0 entry

**User verification steps:**
1. Open `README.md` — does it feel right? Action-oriented, v2.0 agents featured, accurate?
2. Open `repo-structure.md` — useful for quick re-orientation?
3. Check `.aether/docs/` — right files kept? Archive makes sense?
4. Review `CHANGELOG.md` v2.0.0 entry — captures the milestone well?

Resume signal: Type "approved" to publish (and choose option a, b, or c for the npm version constraint), or describe any changes needed.
  </action>
  <verify>User has typed "approved" with their chosen publish option, or provided feedback for iteration.</verify>
  <done>User has reviewed all v2.0 deliverables and approved the release. npm publish (per chosen option) and git tag completed.</done>
</task>

</tasks>

<verification>
- package.json version is 2.0.0
- ROADMAP.md v2.0 milestone is marked shipped
- STATE.md shows 100% progress
- CHANGELOG.md has v2.0.0 entry
- User has reviewed and approved the release
</verification>

<success_criteria>
- v2.0 is formally shipped: version bumped, docs updated, changelog written
- User has verified README, repo-structure, docs curation, and changelog
- npm publish and git tag complete (after user approval, using user's chosen publish strategy)
</success_criteria>

<output>
After completion, create `.planning/phases/31-integration-verification-cleanup/31-03-SUMMARY.md`
</output>
