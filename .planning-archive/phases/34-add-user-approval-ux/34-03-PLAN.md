---
phase: 34-add-user-approval-ux
plan: 03
type: execute
wave: 3
depends_on:
  - 34-02
files_modified:
  - .aether/aether-utils.sh
  - .claude/commands/ant/continue.md
autonomous: true
requirements:
  - PHER-EVOL-03
must_haves:
  truths:
    - User can approve selected proposals in batch
    - Approved proposals are promoted via queen-promote
    - Unselected proposals are stored in learning-deferred.json
    - Undo prompt appears after successful promotions
    - /ant:continue --deferred shows deferred proposals
  artifacts:
    - path: .aether/aether-utils.sh
      provides: learning-approve-proposals function
      exports: ["learning-approve-proposals"]
    - path: .aether/aether-utils.sh
      provides: learning-defer-proposals function
      exports: ["learning-defer-proposals"]
    - path: .aether/data/learning-deferred.json
      provides: Storage for deferred proposals
    - path: .claude/commands/ant/continue.md
      provides: --deferred flag support
  key_links:
    - from: learning-approve-proposals
      to: queen-promote
      via: "calls for each selected proposal"
    - from: learning-approve-proposals
      to: learning-defer-proposals
      via: "calls to store unselected"
    - from: continue.md
      to: learning-approve-proposals
      via: "invokes at end of phase"
---

<objective>
Create the approval execution, deferred storage, and undo functionality to complete the tick-to-approve UX.

Purpose: Wire everything together — execute batch promotions, store deferred proposals, offer undo, and integrate with continue.md.
Output: Complete approval workflow with deferred storage and undo support
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-add-user-approval-ux/34-CONTEXT.md
@.planning/phases/34-add-user-approval-ux/34-RESEARCH.md
@.aether/aether-utils.sh (queen-promote at line 3616, learning-select-proposals from 34-02)
@.claude/commands/ant/continue.md
</context>

<tasks>

<task type="auto">
  <name>Create learning-defer-proposals function</name>
  <files>.aether/aether-utils.sh</files>
  <action>
    Add a new case handler `learning-defer-proposals` to aether-utils.sh that:
    1. Takes JSON array of proposals to defer as argument
    2. Acquires lock on learning-deferred.json
    3. Reads existing deferred file (creates if doesn't exist)
    4. Adds `deferred_at` timestamp to each proposal
    5. Merges new deferred with existing (avoiding duplicates by content_hash)
    6. Writes atomically to learning-deferred.json
    7. Releases lock
    8. Returns count of newly deferred items

    Data format for learning-deferred.json (from 34-RESEARCH.md):
    {
      "deferred": [
        {
          "content_hash": "sha256:...",
          "content": "...",
          "wisdom_type": "pattern",
          "observation_count": 2,
          "threshold": 3,
          "first_seen": "2026-02-20T19:50:43Z",
          "deferred_at": "2026-02-20T20:15:00Z",
          "colonies": ["colony-a"]
        }
      ]
    }

    Add TTL handling: auto-expire entries older than 30 days during write.
    Log activity: "DEFERRED" action with count.

    Place after learning-select-proposals.
  </action>
  <verify>
    echo '[{"content_hash":"sha256:test","content":"Test defer","wisdom_type":"pattern","observation_count":2,"threshold":3,"first_seen":"2026-02-20T19:50:43Z","colonies":["test"]}]' | \
    bash .aether/aether-utils.sh learning-defer-proposals
    # Should create learning-deferred.json with the proposal plus deferred_at timestamp
    cat .aether/data/learning-deferred.json
  </verify>
  <done>
    Function creates/updates learning-deferred.json
    Adds deferred_at timestamp to each proposal
    Merges without duplicates
    Auto-expires entries older than 30 days
    Logs activity
  </done>
</task>

<task type="auto">
  <name>Create learning-approve-proposals function</name>
  <files>.aether/aether-utils.sh</files>
  <action>
    Add a new case handler `learning-approve-proposals` to aether-utils.sh that:
    1. Gets colony name from COLONY_STATE.json (session_id split)
    2. Calls learning-select-proposals to get user selection
    3. If defer_all action, calls learning-defer-proposals for all and exits
    4. For each selected proposal:
       - Call queen-promote with type, content, colony_name
       - Track successes and failures
       - Stop on first error (per CONTEXT.md decisions)
       - Show "✓ Promoted {type}: {content}" for each success
    5. If any promotions succeeded, offer undo prompt
    6. Move unselected proposals to deferred via learning-defer-proposals
    7. Log activity with counts: "PROMOTED N, DEFERRED M"
    8. Return JSON summary: {promoted: N, deferred: M, failed: item|null}

    Error handling (per 34-CONTEXT.md):
    - Stop on first error
    - Show which succeeded before failure
    - Leave successful promotions in place
    - Log failure for manual retry

    Undo implementation:
    - Store promoted items in $DATA_DIR/.promotion-undo.json with timestamp
    - Prompt: "Undo these promotions? (y/n)"
    - If yes, call learning-undo-promotions (create this function too)
    - Undo file has 24h TTL

    Add flags:
    - --dry-run: Show what would happen without executing
    - --yes: Skip confirmation prompts
    - --verbose: Show full content in displays
  </action>
  <verify>
    # Test dry-run mode
    bash .aether/aether-utils.sh learning-approve-proposals --dry-run
    # Should show display and selection but not execute
  </verify>
  <done>
    Function orchestrates full approval workflow
    Promotes selected via queen-promote
    Defers unselected to learning-deferred.json
    Shows success feedback for each promotion
    Stops on first error with partial success reported
    Offers undo prompt after successful promotions
    Logs activity
  </done>
</task>

<task type="auto">
  <name>Create learning-undo-promotions function</name>
  <files>.aether/aether-utils.sh</files>
  <action>
    Add a new case handler `learning-undo-promotions` to aether-utils.sh that:
    1. Reads $DATA_DIR/.promotion-undo.json
    2. Checks if undo is within 24h TTL
    3. For each promoted item in undo file:
       - Remove from QUEEN.md (find and delete matching entry)
       - Use sed or awk to remove the line
       - Handle section cleanup if entry was the only one
    4. Remove the undo file after successful revert
    5. Log activity: "UNDONE" action with count
    6. Return success/failure status

    Edge cases:
    - Undo file missing or expired -> error
    - Entry already removed from QUEEN.md -> warn but continue
    - Partial undo failure -> report which items couldn't be reverted

    Also add --undo flag to learning-approve-proposals to trigger undo without new promotions.
  </action>
  <verify>
    # Create mock undo file
    echo '{"promoted":[{"type":"pattern","content":"Test undo"}],"timestamp":'$(date +%s)'}' > .aether/data/.promotion-undo.json
    bash .aether/aether-utils.sh learning-undo-promotions
    # Should attempt to remove the entry from QUEEN.md
  </verify>
  <done>
    Function successfully reverts promotions from QUEEN.md
    Handles expired undo files gracefully
    Logs activity
    --undo flag works on learning-approve-proposals
  </done>
</task>

<task type="auto">
  <name>Update continue.md with approval UX integration</name>
  <files>.claude/commands/ant/continue.md</files>
  <action>
    Update continue.md Step 2.1.5 (Check for Promotion Proposals) to:
    1. Call learning-approve-proposals instead of manual flow
    2. Remove the old AskUserQuestion-based approval (replaced by CLI UX)
    3. Keep the check for proposals existence
    4. Add support for --deferred flag:
       - If --deferred passed, show "Reviewing deferred proposals..."
       - Call learning-display-proposals with deferred items
       - Allow approval of deferred items same as regular
    5. Add support for --verbose flag passthrough
    6. Update skip conditions:
       - Skip if no proposals AND no --deferred flag
       - Skip if QUEEN.md doesn't exist

    The new flow should be:
    ```
    # Check for promotion proposals
    if bash .aether/aether-utils.sh learning-check-promotion | jq -e '.proposals | length > 0' >/dev/null 2>&1; then
      bash .aether/aether-utils.sh learning-approve-proposals
    fi
    ```

    For --deferred:
    ```
    if [[ "$*" == *"--deferred"* ]] && [[ -f .aether/data/learning-deferred.json ]]; then
      echo "📦 Reviewing deferred proposals..."
      bash .aether/aether-utils.sh learning-approve-proposals --deferred
    fi
    ```
  </action>
  <verify>
    grep -A 5 "learning-approve-proposals" .claude/commands/ant/continue.md
    # Should show the new function call replacing old AskUserQuestion flow
    grep "\-\-deferred" .claude/commands/ant/continue.md
    # Should show --deferred flag handling
  </verify>
  <done>
    continue.md uses learning-approve-proposals for approval flow
    --deferred flag supported for reviewing deferred proposals
    --verbose flag passed through
    Old AskUserQuestion flow removed
  </done>
</task>

<task type="auto">
  <name>Add learning-deferred.json to gitignore</name>
  <files>.aether/.gitignore or .gitignore</files>
  <action>
    Ensure learning-deferred.json is not tracked by git:
    1. Check if .aether/.gitignore exists
    2. If not, check root .gitignore
    3. Add entry: learning-deferred.json (or .aether/data/learning-deferred.json)
    4. This file is colony-local state, should not be committed

    Also verify learning-observations.json is already ignored (should be from Phase 33).
  </action>
  <verify>
    grep "learning-deferred" .aether/.gitignore 2>/dev/null || grep "learning-deferred" .gitignore 2>/dev/null
    # Should show the ignore entry
  </verify>
  <done>
    learning-deferred.json added to appropriate gitignore
    learning-observations.json confirmed ignored
  </done>
</task>

</tasks>

<verification>
- [ ] learning-defer-proposals stores unselected items with timestamps
- [ ] learning-approve-proposals orchestrates full workflow
- [ ] Batch promotions execute with success feedback
- [ ] Undo prompt appears after promotions
- [ ] Undo function reverts promotions from QUEEN.md
- [ ] continue.md integrated with new approval flow
- [ ] --deferred flag works in continue.md
- [ ] learning-deferred.json is gitignored
</verification>

<success_criteria>
User can:
1. Run `/ant:continue` and see tick-to-approve UX for proposals
2. Select proposals by number and batch approve
3. See deferred proposals stored for later
4. Run `/ant:continue --deferred` to review deferred items
5. Undo promotions immediately after approval
6. See success feedback for each promoted item
</success_criteria>

<output>
After completion, create `.planning/phases/34-add-user-approval-ux/34-03-SUMMARY.md`
</output>
