---
phase: 34-add-user-approval-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/aether-utils.sh
autonomous: true
requirements:
  - PHER-EVOL-03
must_haves:
  truths:
    - User can view proposals grouped by wisdom type
    - Each proposal shows checkbox, number, content, and threshold progress
    - Below-threshold proposals show warning but are selectable
  artifacts:
    - path: .aether/aether-utils.sh
      provides: learning-display-proposals function
      exports: ["learning-display-proposals"]
    - path: .aether/aether-utils.sh
      provides: generate_threshold_bar helper
      pattern: "generate_threshold_bar\\(\\)"
  key_links:
    - from: learning-display-proposals
      to: learning-check-promotion
      via: "calls to get proposals"
    - from: generate_threshold_bar
      to: learning-display-proposals
      via: "used for visual threshold display"
---

<objective>
Create the proposal display function with checkbox-style UI, grouped by wisdom type, with threshold progress bars.

Purpose: Provide the visual foundation for the tick-to-approve UX — users need to see what they're approving before they can select it.
Output: `learning-display-proposals` function in aether-utils.sh
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-add-user-approval-ux/34-CONTEXT.md
@.planning/phases/34-add-user-approval-ux/34-RESEARCH.md
@.aether/aether-utils.sh (learning-check-promotion at line 4066)
</context>

<tasks>

<task type="auto">
  <name>Create generate_threshold_bar helper function</name>
  <files>.aether/aether-utils.sh</files>
  <action>
    Add a new helper function `generate_threshold_bar()` to aether-utils.sh that:
    1. Takes two arguments: observation_count and threshold
    2. Generates a visual progress bar using Unicode circles (● for filled, ○ for empty)
    3. Returns format like "●●●○○ (3/5)"
    4. Includes UTF-8 locale check with ASCII fallback ([=--] style) if UTF-8 not available
    5. Handle edge cases: count > threshold (show all filled), threshold = 0 (show "immediate")

    Place this function near other helper functions (around line 3600, before queen-promote).
    Use the pattern from 34-RESEARCH.md Pattern 3.
  </action>
  <verify>
    bash -c 'source .aether/aether-utils.sh && generate_threshold_bar 3 5'
    # Should output: "●●●○○ (3/5)"
  </verify>
  <done>
    Function exists, outputs correct format for various inputs (3/5, 5/5, 0/3, 1/1)
    ASCII fallback works when LANG is not UTF-8
  </done>
</task>

<task type="auto">
  <name>Create learning-display-proposals function</name>
  <files>.aether/aether-utils.sh</files>
  <action>
    Add a new case handler `learning-display-proposals` to aether-utils.sh that:
    1. Accepts optional --verbose flag
    2. Calls learning-check-promotion to get proposals JSON
    3. Groups proposals by wisdom type (philosophy, pattern, redirect, stack, decree)
    4. Displays each group with emoji header (📜 Philosophies, 🧭 Patterns, etc.)
    5. Shows each proposal as: "  [ ] {number}. \"{content}\" {threshold_bar} ({count}/{threshold}){warning}"
    6. Truncates content to 40 chars unless --verbose (then show full)
    7. Shows ⚠️ warning for below-threshold items
    8. Numbers proposals sequentially across all groups (1, 2, 3...)
    9. Returns empty display with message if no proposals

    Use the format from 34-CONTEXT.md decisions:
    - Format: `[ ] 1. Philosophy: "Keep functions small" ●●●●● (5/5)`
    - Group by type with headers
    - Use generate_threshold_bar for progress visualization

    Add this case after learning-check-promotion (around line 4119).
  </action>
  <verify>
    # Create test observations file
    echo '{"observations":[{"content_hash":"sha256:test1","content":"Always validate inputs","wisdom_type":"pattern","observation_count":3,"first_seen":"2026-02-20T19:50:43Z","colonies":["test"]}]}' > .aether/data/learning-observations.json
    bash .aether/aether-utils.sh learning-display-proposals
    # Should show grouped display with checkbox, number, content, threshold bar
  </verify>
  <done>
    Function displays proposals in correct format with checkboxes, numbers, and threshold bars
    Groups by wisdom type with emoji headers
    Shows warning for below-threshold items
    Handles empty state gracefully
  </done>
</task>

<task type="auto">
  <name>Add Unicode/ASCII detection and color support</name>
  <files>.aether/aether-utils.sh</files>
  <action>
    Enhance the display function with:
    1. UTF-8 locale detection (check if ${LANG:-} contains "UTF-8")
    2. Color support detection (check if terminal supports colors)
    3. For threshold bars: Use ●/○ in UTF-8, [=--] in ASCII
    4. For warnings: Use yellow/red colors if supported, plain text otherwise
    5. Add a --no-color flag to force plain output

    Use existing color patterns from aether-utils.sh if available, or add minimal color helpers.
    Keep the implementation simple — this is for visual polish, not core functionality.
  </action>
  <verify>
    bash .aether/aether-utils.sh learning-display-proposals | cat
    # Should show plain output (no colors when piped)
    LANG=C bash .aether/aether-utils.sh learning-display-proposals
    # Should show ASCII bars instead of Unicode circles
  </verify>
  <done>
    Unicode/ASCII detection works correctly
    Colors disabled when output is not a TTY
    --no-color flag forces plain output
  </done>
</task>

</tasks>

<verification>
- [ ] generate_threshold_bar outputs correct visual format
- [ ] learning-display-proposals shows grouped proposals with checkboxes
- [ ] Threshold bars display correctly for all wisdom types
- [ ] Below-threshold warnings are visible
- [ ] Empty state handled gracefully
- [ ] UTF-8/ASCII fallback works
</verification>

<success_criteria>
User can run `bash .aether/aether-utils.sh learning-display-proposals` and see:
1. Proposals grouped by wisdom type with emoji headers
2. Each proposal numbered with checkbox `[ ]`
3. Visual threshold progress (●●●○○ style)
4. Warnings for below-threshold items
</success_criteria>

<output>
After completion, create `.planning/phases/34-add-user-approval-ux/34-01-SUMMARY.md`
</output>
