---
phase: 24-template-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/templates/crowned-anthill.template.md
  - .aether/templates/handoff.template.md
  - .claude/commands/ant/seal.md
  - .opencode/commands/ant/seal.md
  - .claude/commands/ant/entomb.md
  - .opencode/commands/ant/entomb.md
autonomous: true
requirements: [WIRE-02, WIRE-03, WIRE-04]

must_haves:
  truths:
    - "seal.md reads crowned-anthill.template.md instead of inline heredoc"
    - "entomb.md uses jq -f colony-state-reset.jq.template instead of inline jq filter"
    - "entomb.md reads handoff.template.md instead of inline HANDOFF heredoc"
    - "OpenCode seal.md HANDOFF heredoc also wired to template"
    - "Crowned-anthill template has triumphant, warm, narrative mood"
    - "Handoff template has reflective, warm, narrative mood"
    - "No inline heredocs or jq filters remain in any of the 4 command files"
    - "Template-not-found produces clear error and stops execution"
  artifacts:
    - path: ".aether/templates/crowned-anthill.template.md"
      provides: "Refreshed seal ceremony template with triumphant mood"
      contains: "{{GOAL}}"
    - path: ".aether/templates/handoff.template.md"
      provides: "Refreshed entomb handoff template with reflective mood"
      contains: "{{CHAMBER_NAME}}"
    - path: ".claude/commands/ant/seal.md"
      provides: "Template-wired seal command (Claude Code)"
    - path: ".opencode/commands/ant/seal.md"
      provides: "Template-wired seal command (OpenCode)"
    - path: ".claude/commands/ant/entomb.md"
      provides: "Template-wired entomb command (Claude Code)"
    - path: ".opencode/commands/ant/entomb.md"
      provides: "Template-wired entomb command (OpenCode)"
  key_links:
    - from: ".claude/commands/ant/seal.md"
      to: ".aether/templates/crowned-anthill.template.md"
      via: "LLM reads template, fills {{PLACEHOLDER}} values, writes CROWNED-ANTHILL.md"
      pattern: "crowned-anthill.template.md"
    - from: ".claude/commands/ant/entomb.md"
      to: ".aether/templates/colony-state-reset.jq.template"
      via: "jq -f template_path runs filter against backup state"
      pattern: "jq -f"
    - from: ".claude/commands/ant/entomb.md"
      to: ".aether/templates/handoff.template.md"
      via: "LLM reads template, fills {{PLACEHOLDER}} values, writes HANDOFF.md"
      pattern: "handoff.template.md"
---

<objective>
Wire seal.md and entomb.md (both platforms) to read from templates instead of inline heredocs/filters. Refresh crowned-anthill.template.md with triumphant mood and handoff.template.md with reflective mood per locked decisions.

Purpose: Replace 5 inline structures across 4 command files (seal heredoc, entomb jq filter, entomb HANDOFF heredoc, OpenCode seal HANDOFF heredoc). Make templates the single source of truth with intentional ceremony voices.

Output: Updated seal.md and entomb.md (both platforms). Refreshed crowned-anthill and handoff templates with distinct emotional registers.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-template-integration/24-RESEARCH.md

@.aether/templates/crowned-anthill.template.md
@.aether/templates/handoff.template.md
@.aether/templates/colony-state-reset.jq.template
@.claude/commands/ant/seal.md
@.opencode/commands/ant/seal.md
@.claude/commands/ant/entomb.md
@.opencode/commands/ant/entomb.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh ceremony templates and wire seal.md (both platforms)</name>
  <files>
    .aether/templates/crowned-anthill.template.md
    .aether/templates/handoff.template.md
    .claude/commands/ant/seal.md
    .opencode/commands/ant/seal.md
  </files>
  <action>
**Part A: Refresh crowned-anthill.template.md — TRIUMPHANT mood**

Read `.aether/templates/crowned-anthill.template.md`. Rewrite the content to be warm and narrative with a triumphant mood — the colony has reached its peak, crowned anthill achieved. This is like closing out a great season. Keep the same placeholder names ({{GOAL}}, {{SEAL_DATE}}, {{VERSION}}, {{TOTAL_PHASES}}, {{PHASES_COMPLETED}}, {{COLONY_AGE_DAYS}}, {{PROMOTIONS_MADE}}, {{PHASE_RECAP}}).

Guidelines for the triumphant voice:
- Opening should celebrate the achievement — this colony accomplished something real
- Stats section should frame numbers as accomplishments, not bare data
- Phase Recap section keeps {{PHASE_RECAP}} placeholder but frames it warmly
- Pheromone Legacy should acknowledge the wisdom that will carry forward
- Closing should feel like a proud moment — the anthill stands crowned
- Keep the HTML comment header (template name, version, fill instructions)
- Update version to 2.0 in the comment header

**Part B: Refresh handoff.template.md — REFLECTIVE mood**

Read `.aether/templates/handoff.template.md`. Rewrite with warm, reflective mood — the colony's story is complete, a chapter closing. This is like the final page of a chapter. Keep the same placeholder names ({{CHAMBER_NAME}}, {{GOAL}}, {{PHASES_COMPLETED}}, {{TOTAL_PHASES}}, {{MILESTONE}}, {{ENTOMB_TIMESTAMP}}).

Guidelines for the reflective voice:
- Opening should honor the colony's journey, not just state "ENTOMBED"
- Chamber location feels like a place of rest, not a filing cabinet
- Colony Summary frames the work done, not just metrics
- Chamber Contents stays factual (file listing) but with a warmer framing
- Session Note should feel like a quiet closing — dignity, not just instructions
- Recovery guidance (lay-eggs, tunnels) still present but framed gently
- Keep the HTML comment header; update version to 2.0

**CRITICAL:** These are two distinct emotional moments — triumphant (seal) vs reflective (entomb). They must NOT sound the same.

**Part C: Wire Claude Code seal.md**

Read `.claude/commands/ant/seal.md`. Replace the CROWNED-ANTHILL.md heredoc (Step 6, lines ~240-263, the `cat > .aether/CROWNED-ANTHILL.md << SEAL_EOF ... SEAL_EOF` block) with template read instructions:

```
Resolve the crowned-anthill template path:
  Check ~/.aether/system/templates/crowned-anthill.template.md first,
  then .aether/templates/crowned-anthill.template.md.

If no template found: output "Template missing: crowned-anthill.template.md. Run aether update to fix." and stop.

Read the template file. Fill all {{PLACEHOLDER}} values:
  - {{GOAL}} → goal (from colony state)
  - {{SEAL_DATE}} → seal_date (ISO-8601 UTC timestamp)
  - {{VERSION}} → version (from colony state)
  - {{TOTAL_PHASES}} → total_phases
  - {{PHASES_COMPLETED}} → phases_completed
  - {{COLONY_AGE_DAYS}} → colony_age_days
  - {{PROMOTIONS_MADE}} → promotions_made
  - {{PHASE_RECAP}} → phase recap list (one entry per line, formatted from the bash loop above)

Remove the HTML comment lines at the top of the template (lines starting with <!--).
Write the result to .aether/CROWNED-ANTHILL.md using the Write tool.
```

Keep the bash blocks above that compute goal, total_phases, phases_completed, colony_age_days, promotions_made, phase_recap, version, and seal_date. Only replace the `cat > ... << SEAL_EOF` heredoc block itself.

Completely remove the heredoc. No commented-out backups.

**Part D: Wire OpenCode seal.md**

Read `.opencode/commands/ant/seal.md`. This is architecturally different from Claude Code:
- It has a CROWNED-ANTHILL.md heredoc (similar to Claude Code's)
- It ALSO has a HANDOFF.md heredoc at Step 5.5 (lines ~154-180) that Claude Code does NOT have

Wire BOTH heredocs:

1. **CROWNED-ANTHILL.md heredoc:** Replace with identical template read instructions as Claude Code (Part C above). The crowned-anthill template path resolution and fill instructions are the same.

2. **HANDOFF.md heredoc (Step 5.5, lines ~154-180):** Replace with template read instructions using handoff.template.md:

```
Resolve the handoff template path:
  Check ~/.aether/system/templates/handoff.template.md first,
  then .aether/templates/handoff.template.md.

If no template found: output "Template missing: handoff.template.md. Run aether update to fix." and stop.

Read the template file. Fill all {{PLACEHOLDER}} values:
  - {{CHAMBER_NAME}} → archive directory name
  - {{GOAL}} → goal
  - {{PHASES_COMPLETED}} → total_phases (OpenCode seal archives completed colonies)
  - {{TOTAL_PHASES}} → total_phases
  - {{MILESTONE}} → "Crowned Anthill"
  - {{ENTOMB_TIMESTAMP}} → seal timestamp

Remove the HTML comment lines at the top of the template.
Write the result to .aether/HANDOFF.md using the Write tool.
```

Completely remove both heredocs. Preserve OpenCode-specific differences (normalize-args, display functions, step numbering).
  </action>
  <verify>
1. `grep -c 'SEAL_EOF' .claude/commands/ant/seal.md` returns 0 (heredoc removed)
2. `grep -c 'crowned-anthill.template.md' .claude/commands/ant/seal.md` returns at least 2
3. `grep -c 'Template missing' .claude/commands/ant/seal.md` returns at least 1
4. `grep -c 'HANDOFF_EOF' .opencode/commands/ant/seal.md` returns 0 (heredoc removed)
5. `grep -c 'crowned-anthill.template.md' .opencode/commands/ant/seal.md` returns at least 2
6. `grep -c 'handoff.template.md' .opencode/commands/ant/seal.md` returns at least 2
7. crowned-anthill.template.md contains triumphant language (not bare "Colony Stats")
8. handoff.template.md contains reflective language (not bare "Colony Archived")
  </verify>
  <done>
Both seal.md files wired to crowned-anthill.template.md. OpenCode seal.md also wired to handoff.template.md for its HANDOFF heredoc. Templates refreshed with distinct emotional voices: triumphant (seal) vs reflective (entomb). No inline heredocs remain in either seal.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire entomb.md jq filter and HANDOFF heredoc (both platforms)</name>
  <files>
    .claude/commands/ant/entomb.md
    .opencode/commands/ant/entomb.md
  </files>
  <action>
**Part A: Wire Claude Code entomb.md jq filter (Step 10)**

Read `.claude/commands/ant/entomb.md`. Replace the inline jq filter (Step 10, lines ~396-415) with shell script that resolves the template path and uses `jq -f`:

Replace the existing jq inline filter block with:

```bash
# Resolve jq template path (hub-first)
jq_template=""
for path in \
  "$HOME/.aether/system/templates/colony-state-reset.jq.template" \
  ".aether/templates/colony-state-reset.jq.template"; do
  if [[ -f "$path" ]]; then
    jq_template="$path"
    break
  fi
done

if [[ -z "$jq_template" ]]; then
  echo "Template missing: colony-state-reset.jq.template. Run aether update to fix."
  exit 1
fi

jq -f "$jq_template" .aether/data/COLONY_STATE.json.bak > .aether/data/COLONY_STATE.json
```

Keep the backup step before (`cp .aether/data/COLONY_STATE.json .aether/data/COLONY_STATE.json.bak`) and the verification step after (check `new_goal` is null). Only replace the inline `jq '...'` block itself.

**Part B: Wire Claude Code entomb.md HANDOFF heredoc (Step 11)**

Replace the HANDOFF.md heredoc (Step 11, lines ~449-479, `cat > .aether/HANDOFF.md << 'HANDOFF_EOF' ... HANDOFF_EOF`) with template read instructions:

```
Resolve the handoff template path:
  Check ~/.aether/system/templates/handoff.template.md first,
  then .aether/templates/handoff.template.md.

If no template found: output "Template missing: handoff.template.md. Run aether update to fix." and stop.

Read the template file. Fill all {{PLACEHOLDER}} values:
  - {{CHAMBER_NAME}} → chamber_name
  - {{GOAL}} → goal
  - {{PHASES_COMPLETED}} → phases completed count
  - {{TOTAL_PHASES}} → total phases count
  - {{MILESTONE}} → milestone
  - {{ENTOMB_TIMESTAMP}} → current ISO-8601 UTC timestamp

Remove the HTML comment lines at the top of the template.
Write the result to .aether/HANDOFF.md using the Write tool.
```

Completely remove the heredoc. No fallback.

**Part C: Wire OpenCode entomb.md jq filter (Step 8)**

Read `.opencode/commands/ant/entomb.md`. The jq filter is at Step 8 (lines ~269-284). Replace with the same hub-first `jq -f` pattern as Part A.

**IMPORTANT NOTE:** OpenCode's entomb jq filter does NOT reset `.memory.instincts`, `.memory.phase_learnings`, and `.memory.decisions` (unlike Claude Code's). The `colony-state-reset.jq.template` DOES reset these fields (it matches Claude Code's version). This means wiring OpenCode to the template will normalize the behavior — which is correct, since entomb should reset all memory (it's already promoted to QUEEN.md during seal). This is intentional alignment, not a bug.

**Part D: Wire OpenCode entomb.md HANDOFF heredoc (Step 8.5)**

Read `.opencode/commands/ant/entomb.md`. The HANDOFF heredoc is at Step 8.5 (lines ~308-337). Replace with the same template read instructions as Part B.

Completely remove the heredoc. Preserve OpenCode-specific differences.
  </action>
  <verify>
1. `grep -c "jq '" .claude/commands/ant/entomb.md` returns 0 (no inline jq filters — the backtick-delimited filter string is gone)
2. `grep -c 'jq -f' .claude/commands/ant/entomb.md` returns at least 1 (template-based jq)
3. `grep -c 'HANDOFF_EOF' .claude/commands/ant/entomb.md` returns 0 (heredoc removed)
4. `grep -c 'handoff.template.md' .claude/commands/ant/entomb.md` returns at least 2
5. `grep -c 'colony-state-reset.jq.template' .claude/commands/ant/entomb.md` returns at least 1
6. `grep -c "jq '" .opencode/commands/ant/entomb.md` returns 0 (inline jq filter removed)
7. `grep -c 'HANDOFF_EOF' .opencode/commands/ant/entomb.md` returns 0 (heredoc removed)
8. `grep -c 'Template missing' .claude/commands/ant/entomb.md` returns at least 2 (jq + handoff)
9. `grep -c 'Template missing' .opencode/commands/ant/entomb.md` returns at least 2
  </verify>
  <done>
Both entomb.md files use jq -f with hub-first template lookup for state reset. Both entomb.md files read handoff.template.md instead of inline HANDOFF heredoc. OpenCode entomb behavior normalized to match Claude Code (memory fields now reset during entomb). No inline jq filters or heredocs remain.
  </done>
</task>

</tasks>

<verification>
1. Zero inline heredocs (`SEAL_EOF`, `HANDOFF_EOF`) in any of the 4 command files
2. Zero inline jq filter strings in either entomb.md
3. All 4 command files reference template files by name
4. All template-not-found paths produce "Template missing: {name}. Run aether update to fix."
5. crowned-anthill.template.md reads as triumphant and warm (not bare "Colony Stats")
6. handoff.template.md reads as reflective and warm (not bare "Colony Archived")
7. `npm test` passes (no regressions)
</verification>

<success_criteria>
- seal.md (both platforms) reads crowned-anthill.template.md instead of inline heredoc
- entomb.md (both platforms) uses `jq -f` for state reset instead of inline filter
- entomb.md (both platforms) reads handoff.template.md instead of inline HANDOFF heredoc
- OpenCode seal.md HANDOFF heredoc also wired to handoff.template.md
- Ceremony templates have distinct voices: triumphant vs reflective
- No inline structures remain in any of the 4 files
- Template-not-found errors match locked decision format
</success_criteria>

<output>
After completion, create `.planning/phases/24-template-integration/24-02-SUMMARY.md`
</output>
