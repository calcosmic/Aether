---
phase: 24-template-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/templates/colony-state.template.json
  - .aether/templates/constraints.template.json
  - .claude/commands/ant/init.md
  - .opencode/commands/ant/init.md
autonomous: true
requirements: [WIRE-01]

must_haves:
  truths:
    - "init.md reads colony-state.template.json from template file instead of inline JSON"
    - "init.md reads constraints.template.json from template file instead of inline JSON"
    - "No inline JSON blocks for COLONY_STATE or constraints remain in either init.md"
    - "Template-not-found produces clear error message and stops execution"
    - "Both Claude Code and OpenCode init.md are wired simultaneously"
  artifacts:
    - path: ".aether/templates/colony-state.template.json"
      provides: "Refreshed annotated colony state template"
      contains: "__GOAL__"
    - path: ".aether/templates/constraints.template.json"
      provides: "Refreshed annotated constraints template"
      contains: "_instructions"
    - path: ".claude/commands/ant/init.md"
      provides: "Template-wired init command (Claude Code)"
    - path: ".opencode/commands/ant/init.md"
      provides: "Template-wired init command (OpenCode)"
  key_links:
    - from: ".claude/commands/ant/init.md"
      to: ".aether/templates/colony-state.template.json"
      via: "LLM reads template, fills __PLACEHOLDER__ values, writes COLONY_STATE.json"
      pattern: "colony-state.template.json"
    - from: ".claude/commands/ant/init.md"
      to: ".aether/templates/constraints.template.json"
      via: "LLM reads template, strips underscore keys, writes constraints.json"
      pattern: "constraints.template.json"
---

<objective>
Wire init.md (both Claude Code and OpenCode) to read colony-state.template.json and constraints.template.json instead of inline JSON blocks. Refresh both JSON templates for cohesion per locked decision.

Purpose: Eliminate the first 2 inline structures, making templates the single source of truth for init's JSON outputs. Both platforms updated simultaneously to prevent drift.

Output: Updated init.md (both platforms) with template read instructions replacing inline JSON. Refreshed JSON templates with improved annotations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-template-integration/24-RESEARCH.md

@.aether/templates/colony-state.template.json
@.aether/templates/constraints.template.json
@.claude/commands/ant/init.md
@.opencode/commands/ant/init.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh JSON templates and wire Claude Code init.md</name>
  <files>
    .aether/templates/colony-state.template.json
    .aether/templates/constraints.template.json
    .claude/commands/ant/init.md
  </files>
  <action>
**Part A: Refresh colony-state.template.json**

Read `.aether/templates/colony-state.template.json`. Refresh the template content for cohesion (per locked decision: "internal templates also refreshed for cohesion"). Improvements to make:
- Improve `_instructions` to be clearer and more specific about the placeholder filling process
- Add a `_comment_events` field explaining the events array format
- Ensure `_comment_memory` clearly states that `__PHASE_LEARNINGS__` and `__INSTINCTS__` must be JSON arrays (not strings)
- Keep the same JSON structure and placeholder names — this is a documentation/clarity refresh, not a schema change

**Part B: Refresh constraints.template.json**

Read `.aether/templates/constraints.template.json`. Refresh for cohesion:
- Improve `_instructions` to be more descriptive about what constraints.json controls
- Add `_comment_purpose` explaining that this file stores user pheromone signals (FOCUS/REDIRECT)
- Keep the same data keys (version, focus, constraints) — schema unchanged

**Part C: Wire Claude Code init.md**

Read `.claude/commands/ant/init.md`. Make these changes:

1. **Step 3 (COLONY_STATE.json):** Replace the inline JSON block (lines ~215-244) with template read instructions:

```
Resolve the colony-state template path:
  Check ~/.aether/system/templates/colony-state.template.json first,
  then .aether/templates/colony-state.template.json.

If no template found: output "Template missing: colony-state.template.json. Run aether update to fix." and stop.

Read the template file. Follow its _instructions field.
Replace all __PLACEHOLDER__ values:
  - __GOAL__ → the user's goal from $ARGUMENTS
  - __SESSION_ID__ → generated session_id (format: "session_{timestamp}")
  - __ISO8601_TIMESTAMP__ → current ISO-8601 UTC timestamp
  - __PHASE_LEARNINGS__ → JSON array from Step 2.6, or [] if none
  - __INSTINCTS__ → JSON array from Step 2.6, or [] if none

IMPORTANT: __PHASE_LEARNINGS__ and __INSTINCTS__ must be JSON array values (e.g., [] not "[]").

Remove ALL keys starting with underscore (_template, _version, _instructions, _comment_*).
Write the resulting JSON to .aether/data/COLONY_STATE.json using the Write tool.
```

2. **Step 4 (constraints.json):** Replace the inline JSON block (lines ~250-255) with template read instructions:

```
Resolve the constraints template path:
  Check ~/.aether/system/templates/constraints.template.json first,
  then .aether/templates/constraints.template.json.

If no template found: output "Template missing: constraints.template.json. Run aether update to fix." and stop.

Read the template file. Follow its _instructions field.
No placeholder substitution needed — write the data keys as-is.
Remove ALL keys starting with underscore (_template, _version, _instructions, _comment_*).
Write the resulting JSON to .aether/data/constraints.json using the Write tool.
```

3. **Fix cross-references:** Check the 10 lines before and after each removed block. Update any text that says "the JSON structure below" or "as before" to reference the template instead. Specifically, Step 2.6's "use empty arrays as before" should become "use empty arrays for `__PHASE_LEARNINGS__` and `__INSTINCTS__` placeholders."

4. **Completely remove** the old inline JSON blocks. No commented-out backups, no fallback to inline (per locked decision).

Use Claude's discretion to tighten surrounding command code if it benefits readability — judge per section, clean what's messy, leave what's fine.
  </action>
  <verify>
1. `grep -c '\"version\": \"3.0\"' .claude/commands/ant/init.md` returns 0 (inline JSON removed)
2. `grep -c 'colony-state.template.json' .claude/commands/ant/init.md` returns at least 2 (template referenced)
3. `grep -c 'constraints.template.json' .claude/commands/ant/init.md` returns at least 2 (template referenced)
4. `grep -c 'Template missing' .claude/commands/ant/init.md` returns at least 2 (error handling present)
5. `grep -c '_instructions' .aether/templates/colony-state.template.json` returns 1 (template has instructions)
6. JSON templates are valid JSON: `jq . .aether/templates/colony-state.template.json` and `jq . .aether/templates/constraints.template.json` both succeed
  </verify>
  <done>
Claude Code init.md Step 3 reads colony-state.template.json instead of inline JSON. Step 4 reads constraints.template.json instead of inline JSON. Both JSON templates refreshed with improved annotations. No inline JSON blocks remain. Template-not-found errors produce clear recovery message.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire OpenCode init.md to templates</name>
  <files>
    .opencode/commands/ant/init.md
  </files>
  <action>
Read `.opencode/commands/ant/init.md`. Apply the same template wiring as Task 1 Part C, but adapted for OpenCode's structure:

1. **Step 3 (COLONY_STATE.json):** Replace the inline JSON block with identical template read instructions as Claude Code. OpenCode's Step 3 has the same inline JSON structure — the replacement is identical.

2. **Step 4 (constraints.json):** Replace the inline JSON block with identical template read instructions as Claude Code.

3. **Fix cross-references:** Same as Claude Code — update any "as before" references to mention template placeholders.

4. **Completely remove** old inline JSON blocks. No commented-out backups.

5. **Preserve OpenCode-specific differences:** OpenCode's init.md has a `normalize-args` step at the top and slightly different step numbering. Do NOT change these — only replace the JSON inline blocks with template read instructions.

Cross-check with the Claude Code version created in Task 1 to ensure the template read instruction text is identical (same placeholder names, same error messages, same recovery guidance).
  </action>
  <verify>
1. `grep -c '\"version\": \"3.0\"' .opencode/commands/ant/init.md` returns 0 (inline JSON removed)
2. `grep -c 'colony-state.template.json' .opencode/commands/ant/init.md` returns at least 2
3. `grep -c 'constraints.template.json' .opencode/commands/ant/init.md` returns at least 2
4. `grep -c 'Template missing' .opencode/commands/ant/init.md` returns at least 2
5. `diff <(grep 'colony-state.template.json' .claude/commands/ant/init.md) <(grep 'colony-state.template.json' .opencode/commands/ant/init.md)` — template references match between platforms
  </verify>
  <done>
OpenCode init.md wired identically to Claude Code. No inline JSON blocks remain. Template read instructions match between platforms. OpenCode-specific normalize-args and step numbering preserved.
  </done>
</task>

</tasks>

<verification>
1. Neither init.md file contains inline JSON blocks for COLONY_STATE or constraints
2. Both init.md files reference the same template filenames
3. Both init.md files have "Template missing" error handling
4. JSON template files are valid JSON with refreshed annotations
5. `npm test` passes (no regressions from template content changes)
</verification>

<success_criteria>
- Zero inline JSON blocks in either init.md
- colony-state.template.json and constraints.template.json refreshed with improved annotations
- Hub-first template lookup pattern in both init.md files
- Template-not-found error message matches locked decision: "Template missing. Run aether update to fix."
- Both platforms wired simultaneously with identical template read instructions
</success_criteria>

<output>
After completion, create `.planning/phases/24-template-integration/24-01-SUMMARY.md`
</output>
