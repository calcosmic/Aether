---
phase: 43-make-learning-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - .claude/commands/ant/build.md
  - .claude/commands/ant/continue.md
  - .aether/aether-utils.sh
autonomous: true
requirements:
  - FLOW-02
must_haves:
  truths:
    - Observations recorded during builds accumulate in learning-observations.json
    - Thresholds trigger promotion proposals automatically
    - User sees proposals one at a time with Approve/Reject/Skip actions
    - After user acts on a proposal, next proposal auto-shows
    - Approved proposals appear in QUEEN.md with correct formatting
    - colony-prime includes promoted wisdom in worker context
  artifacts:
    - path: ".claude/commands/ant/build.md"
      provides: "Build command with observation recording and end-of-build promotion check"
      contains: "learning-check-promotion"
    - path: ".claude/commands/ant/continue.md"
      provides: "Continue command with promotion checking after phase advancement"
      contains: "learning-check-promotion"
    - path: ".aether/aether-utils.sh"
      provides: "One-at-a-time proposal UI and aligned threshold values"
      contains: "[A]pprove"
  key_links:
    - from: ".claude/commands/ant/build.md"
      to: ".aether/data/learning-observations.json"
      via: "learning-observe call on builder failure"
      pattern: "learning-observe.*failure"
    - from: ".claude/commands/ant/build.md"
      to: "learning-approve-proposals"
      via: "end-of-build promotion check"
      pattern: "learning-check-promotion"
    - from: ".claude/commands/ant/continue.md"
      to: "learning-approve-proposals"
      via: "post-phase-advancement check"
      pattern: "learning-check-promotion"
    - from: ".aether/aether-utils.sh"
      to: ".aether/QUEEN.md"
      via: "queen-promote writes approved wisdom"
      pattern: "QUEEN.md"
---

<objective>
Wire the learning pipeline so observations flow to QUEEN.md promotions automatically.

Purpose: The components exist (learning-observe, learning-check-promotion, learning-approve-proposals, queen-promote) but need to be connected. Build.md records observations but doesn't check for promotions at the end. The approve UI needs to show proposals one at a time per user decision. Thresholds need alignment between all functions.
Output: Updated build.md with end-of-build promotion checking, one-at-a-time proposal UI in aether-utils.sh, threshold alignment across all learning functions, and verified integration with continue.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/43-make-learning-flow/43-CONTEXT.md
@.planning/phases/43-make-learning-flow/43-RESEARCH.md
@.claude/commands/ant/build.md
@.claude/commands/ant/continue.md
@.aether/aether-utils.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add end-of-build promotion check to build.md</name>
  <files>.claude/commands/ant/build.md</files>
  <action>
    Add a new step at the end of build.md (after Step 5.9 synthesis) to check for promotion proposals.

    Insert new Step 5.10: Check for Promotion Proposals

    After the build synthesis step (around line 1100-1200 depending on file), add:

    ```
    ### Step 5.10: Check for Promotion Proposals

    After build completion (success or failure), check if any observations have met promotion thresholds.

    Run using the Bash tool with description "Checking for wisdom promotions...":
    ```bash
    proposals=$(bash .aether/aether-utils.sh learning-check-promotion 2>/dev/null || echo '{"proposals":[]}')
    proposal_count=$(echo "$proposals" | jq '.proposals | length')
    echo "{\"proposal_count\": $proposal_count}"
    ```

    Parse the result. If proposal_count > 0:
    - Display: "📚 $proposal_count wisdom proposal(s) ready for review"
    - Run: `bash .aether/aether-utils.sh learning-approve-proposals`
    - This presents the one-at-a-time UI for user review

    If proposal_count == 0:
    - Silently continue (no output needed per user decision)

    Note: This runs regardless of build success/failure. Failed builds may have recorded failure observations that are ready for promotion.
    ```

    The step should be placed after all builder/watcher/chaos processing is complete but before the final build summary display.
  </action>
  <verify>grep -n "Step 5.10" .claude/commands/ant/build.md</verify>
  <done>Step 5.10 added to build.md with promotion checking logic</done>
</task>

<task type="auto">
  <name>Task 2: Modify learning-approve-proposals for one-at-a-time UI</name>
  <files>.aether/aether-utils.sh</files>
  <action>
    Read the learning-approve-proposals function (around lines 4700-4770) and modify it to present proposals one at a time instead of batch display with numeric selection.

    Current behavior: Shows all proposals in a list with [ ] checkboxes, asks for numeric input like "1 3 5"
    New behavior per user decision: Show one proposal at a time with [A]pprove/[R]eject/[S]kip actions, auto-advance to next

    Replace the batch display and selection logic with:

    1. Get proposals array from learning-check-promotion
    2. Loop through proposals one at a time:
       - Clear screen (optional) or add separator
       - Show: "Proposal X of Y"
       - Show wisdom type emoji + name
       - Show observation content (full text, not truncated)
       - Show: "[A]pprove  [R]eject  [S]kip"
       - Prompt: "Your choice: "
    3. Handle user input:
       - 'a' or 'A': Call queen-promote for this proposal, then show next
       - 'r' or 'R': Mark as rejected (remove from pending or mark status), then show next
       - 's' or 'S' or Enter: Skip (leave pending for later), then show next
    4. After all proposals processed, show summary: "X approved, Y rejected, Z skipped"

    The function should still:
    - Exit gracefully if no proposals
    - Handle the --dry-run flag (show what would happen without prompting)
    - Return JSON summary on completion

    Keep the existing threshold checking logic - only change the presentation and interaction model.
  </action>
  <verify>grep -n "\[A\]pprove" .aether/aether-utils.sh</verify>
  <done>learning-approve-proposals shows one proposal at a time with Approve/Reject/Skip actions</done>
</task>

<task type="auto">
  <name>Task 3: Align thresholds across ALL learning functions</name>
  <files>.aether/aether-utils.sh</files>
  <action>
    Update threshold values in THREE locations to match learning-observe (threshold=1 for most types):

    1. learning-display-proposals (around lines 4664-4673):
       - Change: philosophy=5, pattern=3, redirect=2, stack=1, decree=0
       - To: philosophy=1, pattern=1, redirect=1, stack=1, decree=0, failure=1

    2. learning-display-proposals display section (around line 4703):
       - Change: type_thresholds=(5 3 2 1 0)
       - To: type_thresholds=(1 1 1 1 0)
       - Also update types array to include "failure" if not present

    3. learning-select-proposals (around lines 4802-4809):
       - Change: philosophy=5, pattern=3, redirect=2, stack=1, decree=0
       - To: philosophy=1, pattern=1, redirect=1, stack=1, decree=0, failure=1

    4. Also verify learning-check-promotion has the failure type (around line 4578-4586):
       - Should have: elif type == "failure" then 1

    The aligned thresholds should be:
    - philosophy: 1
    - pattern: 1
    - redirect: 1
    - stack: 1
    - decree: 0
    - failure: 1
  </action>
  <verify>grep -n "get_threshold" .aether/aether-utils.sh | head -10</verify>
  <done>All three functions (learning-display-proposals, learning-select-proposals, learning-check-promotion) use aligned thresholds with failure type included</done>
</task>

<task type="auto">
  <name>Task 4: Verify continue.md promotion check alignment</name>
  <files>.claude/commands/ant/continue.md</files>
  <action>
    Read continue.md around lines 1219-1260 to verify the existing promotion check in Step 2.1.5.

    Confirm:
    1. It calls learning-check-promotion
    2. It calls learning-approve-proposals when proposals exist
    3. It silently skips when no proposals (per user decision)

    The pattern in continue.md should match what we add to build.md.

    If the pattern is already correct, no changes needed. If there are discrepancies (e.g., different threshold handling), note them for alignment.
  </action>
  <verify>grep -A5 "learning-check-promotion" .claude/commands/ant/continue.md | head -20</verify>
  <done>Verified continue.md has correct promotion check pattern</done>
</task>

<task type="auto">
  <name>Task 5: Verify queen-promote writes to QUEEN.md correctly</name>
  <files>.aether/aether-utils.sh</files>
  <action>
    Read the queen-promote function (around lines 4102-4300) to verify:
    1. It writes to .aether/QUEEN.md
    2. Entry format is correct: "- **${colony_name}** (${timestamp}): ${content}"
    3. It handles different wisdom types (philosophy, pattern, redirect, stack, decree, failure)
    4. It returns proper JSON on success/failure

    The format should match what colony-prime expects when reading wisdom back.

    Verify the entry format in the code around line 4180-4220 where the actual write happens.
  </action>
  <verify>grep -A10 "QUEEN.md" .aether/aether-utils.sh | grep -A5 "colony_name"</verify>
  <done>queen-promote correctly writes formatted entries to QUEEN.md</done>
</task>

</tasks>

<verification>
1. build.md has Step 5.10 with promotion check: grep "Step 5.10" .claude/commands/ant/build.md
2. build.md calls learning-check-promotion: grep "learning-check-promotion" .claude/commands/ant/build.md
3. One-at-a-time UI implemented: grep "\[A\]pprove" .aether/aether-utils.sh
4. Thresholds aligned (all use 1 for most types): grep -A10 "get_threshold" .aether/aether-utils.sh | grep -E "(philosophy|pattern|redirect|stack|failure)"
5. failure type in all threshold checks: grep -B2 -A2 'type == "failure"' .aether/aether-utils.sh
6. continue.md pattern verified: grep "learning-check-promotion" .claude/commands/ant/continue.md
7. queen-promote format correct: grep "colony_name.*timestamp" .aether/aether-utils.sh | head -3
</verification>

<success_criteria>
- build.md has end-of-build promotion checking (Step 5.10)
- learning-approve-proposals shows one proposal at a time with [A]pprove/[R]eject/[S]kip actions
- After user acts on a proposal, next proposal auto-shows
- Thresholds are aligned across learning-observe, learning-check-promotion, learning-display-proposals, and learning-select-proposals
- failure type is handled in all threshold checks
- continue.md pattern verified and consistent
- queen-promote writes correct format to QUEEN.md
</success_criteria>

<output>
After completion, create `.planning/phases/43-make-learning-flow/43-02-SUMMARY.md`
</output>
