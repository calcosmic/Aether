---
phase: 43-make-learning-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/ant/init.md
  - .aether/data/learning-observations.json
autonomous: true
requirements:
  - FLOW-01
must_haves:
  truths:
    - learning-observations.json is auto-created during /ant:init if missing
    - Template pattern matches existing pheromones.json and midden.json creation
    - File has valid JSON structure with observations array
  artifacts:
    - path: ".aether/data/learning-observations.json"
      provides: "Observation storage for learning pipeline"
      min_lines: 5
    - path: ".claude/commands/ant/init.md"
      provides: "Updated init command with learning-observations.json creation"
      contains: "learning-observations.template.json"
  key_links:
    - from: ".claude/commands/ant/init.md"
      to: ".aether/data/learning-observations.json"
      via: "bash loop creating from template"
      pattern: "learning-observations"
---

<objective>
Auto-create learning-observations.json during /ant:init if the file is missing.

Purpose: The learning pipeline needs this file to exist before observations can be recorded. Currently it's only created when the first observation is recorded, which can cause silent failures in learning-check-promotion.
Output: Updated init.md that creates learning-observations.json from template, matching the existing pattern for pheromones.json and midden.json.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/43-make-learning-flow/43-CONTEXT.md
@.planning/phases/43-make-learning-flow/43-RESEARCH.md
@.claude/commands/ant/init.md
@.aether/templates/learning-observations.template.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify init.md template creation pattern</name>
  <files>.claude/commands/ant/init.md</files>
  <action>
    Read init.md lines 283-309 to confirm the existing template creation pattern for pheromones and midden.
    The pattern is:
    1. Loop through templates: for template in pheromones midden learning-observations
    2. Determine target path based on template name
    3. Check if target file exists
    4. Find template file in hub or local paths
    5. Use jq to copy template removing underscore-prefixed keys

    Verify that learning-observations is already in the list at line 290.
  </action>
  <verify>grep -n "for template in pheromones midden" .claude/commands/ant/init.md</verify>
  <done>Confirmed the template loop pattern exists and includes learning-observations</done>
</task>

<task type="auto">
  <name>Task 2: Verify learning-observations.template.json exists</name>
  <files>.aether/templates/learning-observations.template.json</files>
  <action>
    Verify the template file exists and has valid JSON structure with:
    - _template, _version, _instructions metadata fields
    - observations array (empty)

    The template is already in place per RESEARCH.md.
  </action>
  <verify>cat .aether/templates/learning-observations.template.json | jq .</verify>
  <done>Template file exists with valid JSON structure</done>
</task>

<task type="auto">
  <name>Task 3: Test the init flow creates learning-observations.json</name>
  <files>.aether/data/learning-observations.json</files>
  <action>
    Simulate what happens during init by manually running the template creation logic:

    1. First, remove any existing learning-observations.json to test fresh creation:
       rm -f .aether/data/learning-observations.json

    2. Run the template creation loop from init.md:
       for template in pheromones midden learning-observations; do
         if [[ "$template" == "midden" ]]; then
           target=".aether/data/midden/midden.json"
         else
           target=".aether/data/${template}.json"
         fi
         if [[ ! -f "$target" ]]; then
           template_file=""
           for path in ~/.aether/system/templates/${template}.template.json .aether/templates/${template}.template.json; do
             if [[ -f "$path" ]]; then
               template_file="$path"
               break
             fi
           done
           if [[ -n "$template_file" ]]; then
             jq 'with_entries(select(.key | startswith("_") | not))' "$template_file" > "$target" 2>/dev/null || true
           fi
         fi
       done

    3. Verify the file was created with correct structure:
       cat .aether/data/learning-observations.json | jq .

    4. Verify the file contains only the observations array (no underscore keys)
  </action>
  <verify>test -f .aether/data/learning-observations.json && jq -e '.observations' .aether/data/learning-observations.json</verify>
  <done>learning-observations.json is created with valid structure containing observations array</done>
</task>

</tasks>

<verification>
1. File exists: test -f .aether/data/learning-observations.json
2. Valid JSON: jq . .aether/data/learning-observations.json >/dev/null
3. Has observations array: jq -e '.observations' .aether/data/learning-observations.json
4. No underscore keys: jq 'keys[]' .aether/data/learning-observations.json | grep -q "^_" && echo "FAIL: has underscore keys" || echo "PASS"
5. Init.md already has the pattern (no changes needed): grep "learning-observations" .claude/commands/ant/init.md
</verification>

<success_criteria>
- learning-observations.json is created during init if missing
- File has valid JSON with observations array
- No underscore-prefixed keys in output file
- Pattern matches existing pheromones.json and midden.json creation
</success_criteria>

<output>
After completion, create `.planning/phases/43-make-learning-flow/43-01-SUMMARY.md`
</output>
