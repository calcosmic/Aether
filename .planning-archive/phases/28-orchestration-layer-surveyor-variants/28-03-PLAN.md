---
phase: 28-orchestration-layer-surveyor-variants
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/ant/aether-surveyor-nest.md
  - .claude/agents/ant/aether-surveyor-disciplines.md
  - .claude/agents/ant/aether-surveyor-pathogens.md
  - .claude/agents/ant/aether-surveyor-provisions.md
autonomous: true
requirements:
  - CORE-06
  - CORE-07
  - CORE-08
  - CORE-09

must_haves:
  truths:
    - "All 4 surveyor agents load in Claude Code without YAML parse errors"
    - "All 4 surveyors have Write in their tools field (can create survey documents)"
    - "All 4 surveyors have boundaries restricting writes to .aether/data/survey/ only"
    - "No surveyor has Edit in its tools field"
    - "Each surveyor description names the specific documents it writes and the commands that spawn it"
  artifacts:
    - path: ".claude/agents/ant/aether-surveyor-nest.md"
      provides: "Architecture and directory survey agent writing BLUEPRINT.md and CHAMBERS.md"
      min_lines: 100
    - path: ".claude/agents/ant/aether-surveyor-disciplines.md"
      provides: "Conventions and testing survey agent writing DISCIPLINES.md and SENTINEL-PROTOCOLS.md"
      min_lines: 100
    - path: ".claude/agents/ant/aether-surveyor-pathogens.md"
      provides: "Technical debt survey agent writing PATHOGENS.md"
      min_lines: 100
    - path: ".claude/agents/ant/aether-surveyor-provisions.md"
      provides: "Dependencies and integrations survey agent writing PROVISIONS.md and TRAILS.md"
      min_lines: 100
  key_links:
    - from: ".claude/agents/ant/aether-surveyor-nest.md"
      to: ".aether/data/survey/"
      via: "boundaries section write scope"
      pattern: "\\.aether/data/survey/"
    - from: ".claude/agents/ant/aether-surveyor-disciplines.md"
      to: ".aether/data/survey/"
      via: "boundaries section write scope"
      pattern: "\\.aether/data/survey/"
    - from: ".claude/agents/ant/aether-surveyor-pathogens.md"
      to: ".aether/data/survey/"
      via: "boundaries section write scope"
      pattern: "\\.aether/data/survey/"
    - from: ".claude/agents/ant/aether-surveyor-provisions.md"
      to: ".aether/data/survey/"
      via: "boundaries section write scope"
      pattern: "\\.aether/data/survey/"
---

<objective>
Create all 4 surveyor agents as Claude Code subagents. Surveyors explore a codebase and write survey documents to `.aether/data/survey/`. Each variant focuses on a different domain: nest (architecture), disciplines (conventions), pathogens (tech debt), provisions (dependencies).

Purpose: Surveyors are the most straightforward conversions — their OpenCode XML bodies are already well-structured. The key addition is the Write tool with tight boundary restrictions to `.aether/data/survey/` only (per locked decision overriding the roadmap's read-only criteria).

Output: Four PWR-compliant Claude Code subagent files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-orchestration-layer-surveyor-variants/28-CONTEXT.md
@.planning/phases/28-orchestration-layer-surveyor-variants/28-RESEARCH.md
@.planning/phases/27-distribution-infrastructure-first-core-agents/27-02-SUMMARY.md

# Source content (read for content to port):
@.opencode/agents/aether-surveyor-nest.md
@.opencode/agents/aether-surveyor-disciplines.md
@.opencode/agents/aether-surveyor-pathogens.md
@.opencode/agents/aether-surveyor-provisions.md

# Template reference (read for format):
@.claude/agents/ant/aether-builder.md
@.claude/agents/ant/aether-watcher.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Surveyor-Nest and Surveyor-Disciplines agents</name>
  <files>.claude/agents/ant/aether-surveyor-nest.md, .claude/agents/ant/aether-surveyor-disciplines.md</files>
  <action>
Create both surveyor agents following the 8-section XML template. The OpenCode source files already have well-structured XML bodies — port the content faithfully, reshaping into the standard 8 sections.

**Common pattern for all surveyors:**
- Tools: `Read, Grep, Glob, Bash, Write` (Write needed to create survey docs; Bash for codebase exploration commands)
- No Edit tool (surveyors create new documents, they do not edit existing source)
- No Task tool (surveyors do not spawn other agents)
- Boundaries section restricts writes to `.aether/data/survey/` ONLY

---

**Surveyor-Nest (`aether-surveyor-nest.md`):**

YAML Frontmatter:
```yaml
---
name: aether-surveyor-nest
description: "Use this agent to map the codebase's architecture, directory structure, and project topology. Writes BLUEPRINT.md and CHAMBERS.md to .aether/data/survey/. Spawned by /ant:colonize to survey the nest before colony work begins. Use when colony context is missing or stale for this project."
tools: Read, Grep, Glob, Bash, Write
model: inherit
---
```

XML Body — reshape OpenCode `<role>`, `<process>`, `<critical_rules>`, `<failure_modes>`, `<success_criteria>`, `<read_only>` into the 8-section template:

1. **`<role>`**: Port from OpenCode. Explore codebase to map nest structure. Write BLUEPRINT.md (architecture, layers, data flow) and CHAMBERS.md (directory structure, naming conventions). "Return confirmation only — do not include document contents in your response." "Progress is tracked through structured returns, not activity logs."

2. **`<execution_flow>`**: Port the 4-step process from OpenCode:
   - Step 1: explore_architecture — directory structure, entry points, imports, architectural markers
   - Step 2: write_blueprint — `.aether/data/survey/BLUEPRINT.md` with the template structure (Pattern Overview, Layers, Data Flow, Key Abstractions, Entry Points, Error Handling, Cross-Cutting)
   - Step 3: explore_structure — full tree, file counts, key file types, special directories
   - Step 4: write_chambers — `.aether/data/survey/CHAMBERS.md` with the template structure (Directory Layout, Directory Purposes, Key File Locations, Naming Conventions, Where to Add New Code)
   Include the `<consumption>` table from OpenCode (which commands load these docs and how builders use them).

3. **`<critical_rules>`**: Port from OpenCode:
   - Write documents directly — do not return contents to orchestrator
   - Always include file paths with backticks
   - Use the templates — fill in the structure
   - Be thorough — read actual files, do not guess
   - Return only confirmation — ~10 lines max
   - Do not commit — orchestrator handles git

4. **`<return_format>`**: Brief confirmation format:
   ```
   ## Survey Complete
   **Focus:** nest
   **Documents written:**
   - `.aether/data/survey/BLUEPRINT.md` ({N} lines)
   - `.aether/data/survey/CHAMBERS.md` ({N} lines)
   Ready for colony use.
   ```

5. **`<success_criteria>`**: Port self-check from OpenCode: BLUEPRINT.md and CHAMBERS.md exist and are readable, all template sections filled (no [placeholder] text), every component references actual file paths. Include line counts and key pattern identified.

6. **`<failure_modes>`**: Port from OpenCode: minor (retry once) for directory not found or no pattern match. Major (STOP) for overwrite with less content or write target outside survey/. Escalation format with BLOCKED, options, awaiting choice.

7. **`<escalation>`**: If survey scope exceeds codebase accessibility (e.g., cannot explore key directories), return with status "blocked" and explain what was inaccessible. "Do NOT attempt to spawn sub-workers — Claude Code subagents cannot spawn other subagents."

8. **`<boundaries>`**: CRITICAL — this is the locked decision override:
   - Write scope RESTRICTED to `.aether/data/survey/` ONLY
   - Permitted write targets: `.aether/data/survey/BLUEPRINT.md`, `.aether/data/survey/CHAMBERS.md`
   - Global protected paths: COLONY_STATE.json, constraints.json, dreams/, .env*
   - Escalation on boundary violation: if task requires writing outside survey/, STOP and escalate

Include the `<philosophy>` content from OpenCode (document quality, always include file paths, be prescriptive) inside the `<role>` or `<critical_rules>` section.

---

**Surveyor-Disciplines (`aether-surveyor-disciplines.md`):**

YAML Frontmatter:
```yaml
---
name: aether-surveyor-disciplines
description: "Use this agent to map coding conventions, testing patterns, and development practices. Writes DISCIPLINES.md and SENTINEL-PROTOCOLS.md to .aether/data/survey/. Spawned by /ant:colonize to document how the team builds software."
tools: Read, Grep, Glob, Bash, Write
model: inherit
---
```

Follow the same 8-section pattern as Surveyor-Nest, porting from `.opencode/agents/aether-surveyor-disciplines.md`:

- **execution_flow**: 4 steps — explore_conventions, write_disciplines, explore_testing, write_sentinel_protocols. Include template structures for DISCIPLINES.md (Naming, Style, Imports, Error Handling, Logging, Comments, Functions, Modules) and SENTINEL-PROTOCOLS.md (Framework, Organization, Structure, Mocking, Fixtures, Coverage, Types, Patterns).
- **boundaries**: Permitted writes ONLY to `.aether/data/survey/DISCIPLINES.md` and `.aether/data/survey/SENTINEL-PROTOCOLS.md`
- **critical_rules**: Include "Include real code examples from the codebase" from OpenCode version
- All other sections follow the same pattern as Surveyor-Nest
  </action>
  <verify>
1. Both files exist: `ls -la .claude/agents/ant/aether-surveyor-nest.md .claude/agents/ant/aether-surveyor-disciplines.md`
2. Both have correct tools: `grep 'tools:' .claude/agents/ant/aether-surveyor-nest.md .claude/agents/ant/aether-surveyor-disciplines.md` — should show Read, Grep, Glob, Bash, Write (no Edit, no Task)
3. Both have all 8 XML sections: `grep -c '<role>\|<execution_flow>\|<critical_rules>\|<return_format>\|<success_criteria>\|<failure_modes>\|<escalation>\|<boundaries>' .claude/agents/ant/aether-surveyor-nest.md` should return 8 (same for disciplines)
4. Both have write scope restriction: `grep '.aether/data/survey/' .claude/agents/ant/aether-surveyor-nest.md .claude/agents/ant/aether-surveyor-disciplines.md` should match in boundaries sections
5. Zero OpenCode-specific patterns in both: `grep -c 'activity-log\|spawn-can-spawn\|generate-ant-name\|spawn-log\|spawn-complete\|flag-add' .claude/agents/ant/aether-surveyor-nest.md .claude/agents/ant/aether-surveyor-disciplines.md` should return 0:0
  </verify>
  <done>
Both surveyor-nest and surveyor-disciplines agent files exist with: YAML frontmatter (Write in tools, no Edit/Task), 8 XML sections, survey document templates, write scope restricted to `.aether/data/survey/`, zero OpenCode bash patterns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Surveyor-Pathogens and Surveyor-Provisions agents</name>
  <files>.claude/agents/ant/aether-surveyor-pathogens.md, .claude/agents/ant/aether-surveyor-provisions.md</files>
  <action>
Create both surveyor agents following the identical 8-section XML template pattern.

---

**Surveyor-Pathogens (`aether-surveyor-pathogens.md`):**

YAML Frontmatter:
```yaml
---
name: aether-surveyor-pathogens
description: "Use this agent to identify technical debt, bugs, security concerns, and fragile areas in the codebase. Writes PATHOGENS.md to .aether/data/survey/. Spawned by /ant:colonize to detect what needs fixing before colony work begins."
tools: Read, Grep, Glob, Bash, Write
model: inherit
---
```

Port from `.opencode/agents/aether-surveyor-pathogens.md`:

- **role**: Explore codebase to identify pathogens. Write ONE document: PATHOGENS.md. Include "This is critical work — issues you identify may become future phases."
- **execution_flow**: 2 steps — explore_concerns (TODO/FIXME/HACK, large files, empty returns, any/unknown types, disabled lint, complex conditionals, security patterns), write_pathogens (PATHOGENS.md with template: Tech Debt, Known Bugs, Security, Performance, Fragile Areas, Type Safety, Test Coverage, Dependencies at Risk)
- **critical_rules**: Always include file paths, be specific about impact and fix approaches, prioritize honestly (not everything is High), include remediation paths, return only confirmation ~10 lines
- **return_format**: Confirmation with issue count by priority (High/Medium/Low)
- **boundaries**: Permitted write ONLY to `.aether/data/survey/PATHOGENS.md`
- All other sections follow the standard surveyor pattern

---

**Surveyor-Provisions (`aether-surveyor-provisions.md`):**

YAML Frontmatter:
```yaml
---
name: aether-surveyor-provisions
description: "Use this agent to map technology stack, dependencies, and external integrations. Writes PROVISIONS.md and TRAILS.md to .aether/data/survey/. Spawned by /ant:colonize to inventory what the project relies on."
tools: Read, Grep, Glob, Bash, Write
model: inherit
---
```

Port from `.opencode/agents/aether-surveyor-provisions.md`:

- **role**: Explore codebase to map provisions (dependencies) and trails (external integrations). Write TWO documents: PROVISIONS.md and TRAILS.md.
- **execution_flow**: 4 steps — explore_provisions (package manifests, configs, runtime), write_provisions (PROVISIONS.md with template: Languages, Runtime, Frameworks, Key Dependencies, Configuration, Platform), explore_trails (SDK/API imports, env vars, config files), write_trails (TRAILS.md with template: APIs, Data Storage, Auth, Monitoring, CI/CD, Env Config, Webhooks)
- **critical_rules**: Document quality over brevity, always include file paths, be prescriptive not descriptive, return only confirmation
- **return_format**: Confirmation with primary language/framework identified and key integrations found
- **boundaries**: Permitted writes ONLY to `.aether/data/survey/PROVISIONS.md` and `.aether/data/survey/TRAILS.md`
- All other sections follow the standard surveyor pattern
  </action>
  <verify>
1. Both files exist: `ls -la .claude/agents/ant/aether-surveyor-pathogens.md .claude/agents/ant/aether-surveyor-provisions.md`
2. Both have correct tools: `grep 'tools:' .claude/agents/ant/aether-surveyor-pathogens.md .claude/agents/ant/aether-surveyor-provisions.md` — should show Read, Grep, Glob, Bash, Write (no Edit, no Task)
3. Both have all 8 XML sections: `grep -c '<role>\|<execution_flow>\|<critical_rules>\|<return_format>\|<success_criteria>\|<failure_modes>\|<escalation>\|<boundaries>' .claude/agents/ant/aether-surveyor-pathogens.md` should return 8 (same for provisions)
4. Both have write scope restriction: `grep '.aether/data/survey/' .claude/agents/ant/aether-surveyor-pathogens.md .claude/agents/ant/aether-surveyor-provisions.md` should match in boundaries sections
5. Zero OpenCode-specific patterns in both: `grep -c 'activity-log\|spawn-can-spawn\|generate-ant-name\|spawn-log\|spawn-complete\|flag-add' .claude/agents/ant/aether-surveyor-pathogens.md .claude/agents/ant/aether-surveyor-provisions.md` should return 0:0
6. All 4 surveyor files now exist: `ls .claude/agents/ant/aether-surveyor-*.md | wc -l` should return 4
  </verify>
  <done>
Both surveyor-pathogens and surveyor-provisions agent files exist with: YAML frontmatter (Write in tools, no Edit/Task), 8 XML sections, survey document templates, write scope restricted to `.aether/data/survey/`, zero OpenCode bash patterns. All 4 surveyors are complete.
  </done>
</task>

</tasks>

<verification>
1. All 4 surveyor files exist at `.claude/agents/ant/aether-surveyor-{nest,disciplines,pathogens,provisions}.md`
2. All 4 have Write in tools (per locked decision override)
3. No surveyor has Edit or Task in tools
4. All 4 have boundaries restricting writes to `.aether/data/survey/`
5. All 4 have all 8 XML sections
6. All 4 have zero OpenCode-specific bash calls
7. Each surveyor's description names the documents it writes and /ant:colonize as its spawn source
</verification>

<success_criteria>
All 4 surveyor agents are complete, PWR-compliant Claude Code subagents. Each writes survey documents to `.aether/data/survey/` (Write tool with boundary restrictions). They load without YAML errors and contain the full survey methodology from OpenCode with zero OpenCode-specific patterns remaining.
</success_criteria>

<output>
After completion, create `.planning/phases/28-orchestration-layer-surveyor-variants/28-03-SUMMARY.md`
</output>
