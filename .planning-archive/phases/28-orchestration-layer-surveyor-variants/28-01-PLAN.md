---
phase: 28-orchestration-layer-surveyor-variants
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/ant/aether-queen.md
autonomous: true
requirements:
  - CORE-01

must_haves:
  truths:
    - "aether-queen agent loads in Claude Code without YAML parse errors"
    - "Queen's tools field includes Task for spawning named subagents"
    - "Queen's description does NOT fire for simple build tasks or simple research"
    - "All 6 workflow patterns (SPBV, Investigate-Fix, Deep Research, Refactor, Compliance, Documentation Sprint) are present in the XML body"
    - "4-tier escalation chain is present (worker retry, parent reassign, queen reassign, user escalation)"
    - "Caste emoji spawn protocol is documented in execution_flow"
  artifacts:
    - path: ".claude/agents/ant/aether-queen.md"
      provides: "Queen orchestrator agent with Task tool and 6 workflow patterns"
      min_lines: 200
  key_links:
    - from: ".claude/agents/ant/aether-queen.md"
      to: "Task tool"
      via: "tools field in YAML frontmatter"
      pattern: "tools:.*Task"
    - from: ".claude/agents/ant/aether-queen.md"
      to: "Worker castes"
      via: "execution_flow spawn protocol"
      pattern: "aether-builder|aether-scout|aether-watcher"
---

<objective>
Create the Queen agent as a Claude Code subagent with full orchestration capability via the Task tool. The Queen is the colony's central coordinator — it spawns workers, manages workflow patterns, and handles escalation.

Purpose: Queen is the most complex agent conversion. It gets the Task tool (making it a true orchestrator), preserves all 6 workflow patterns from OpenCode, and has the 4-tier escalation chain. Getting Queen right is critical because it coordinates every other agent.

Output: `.claude/agents/ant/aether-queen.md` — a PWR-compliant Claude Code subagent.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-orchestration-layer-surveyor-variants/28-CONTEXT.md
@.planning/phases/28-orchestration-layer-surveyor-variants/28-RESEARCH.md
@.planning/phases/27-distribution-infrastructure-first-core-agents/27-02-SUMMARY.md

# Source content (read for content to port):
@.opencode/agents/aether-queen.md

# Template reference (read for format):
@.claude/agents/ant/aether-builder.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Queen agent with Task tool, 6 workflow patterns, and 4-tier escalation</name>
  <files>.claude/agents/ant/aether-queen.md</files>
  <action>
Create `.claude/agents/ant/aether-queen.md` following the 8-section XML template established by Builder (aether-builder.md).

**YAML Frontmatter:**
```yaml
---
name: aether-queen
description: "Use this agent when coordinating multi-phase projects, managing multiple workers across a build session, or executing colony workflows like SPBV, Investigate-Fix, Refactor, Compliance, or Documentation Sprint. Spawned by /ant:build and /ant:colonize when a goal requires planning, delegation, and synthesis across multiple steps. Do NOT use for single-task implementation (use aether-builder) or quick research (use aether-scout)."
tools: Read, Write, Edit, Bash, Grep, Glob, Task
model: inherit
---
```

Key points:
- Description is double-quoted (contains colons)
- Task tool is in the tools field with NO parenthesized restrictions (unrestricted spawning per locked decision)
- Explicit "Do NOT use for..." guidance in description for routing precision

**XML Body — 8 sections:**

1. **`<role>`**: Port Queen role from OpenCode. State she orchestrates multi-phase projects by spawning workers via Task tool. Include: "Progress is tracked through structured returns, not activity logs." Remove all references to activity-log bash commands.

2. **`<execution_flow>`**: Port the following from OpenCode Queen:
   - 6 workflow patterns verbatim (SPBV, Investigate-Fix, Deep Research, Refactor, Compliance, Documentation Sprint) with their use-when triggers, phases, rollback strategies, and announce lines
   - Pattern selection table (phase-contains → pattern mapping)
   - State management section: colony state lives in `.aether/data/` — managed via `bash .aether/aether-utils.sh` commands (state-get, state-set, phase-advance)
   - Worker castes reference table (Core, Development, Knowledge, Quality clusters)
   - **NEW spawn protocol section** with caste emojis:
     ```
     When spawning workers via Task tool, include the caste emoji in the description parameter:
     Builder: "🔨🐜 {task name} — {full task specification}"
     Scout: "🔭🐜 {research topic} — {what to find and report}"
     Watcher: "👁🐜 Verify {artifact} — {what to check}"
     Route-Setter: "🗺🐜 Plan {goal} — {context and constraints}"
     Surveyor: "🗺🐜 Survey {domain} — {what to write and where}"
     ```
   - Spawn limits (depth 0: max 4, depth 1: max 4, depth 2: max 2, depth 3: no spawning, global: 10 per phase)

3. **`<critical_rules>`**: Port verification discipline Iron Law ("No completion claims without fresh verification evidence"), emergence within phases principle, pheromone guidance. Do NOT include any OpenCode-specific bash calls.

4. **`<return_format>`**: Structured JSON with: ant_name, caste ("queen"), status, summary, phases_completed, phases_remaining, learnings, blockers. Remove spawn_tree field (requires aether-utils.sh to populate). Keep it clean for Claude Code consumption.

5. **`<success_criteria>`**: Self-check before reporting phase complete: verify COLONY_STATE.json is valid JSON, verify spawn dispatches completed, verify phase advancement evidence is fresh. Report format with phases_completed, workers_spawned, state_changes, verification_evidence.

6. **`<failure_modes>`**: Port the 4-tier escalation chain from OpenCode Queen VERBATIM:
   - Tier 1: Worker retry (silent, max 2 attempts)
   - Tier 2: Parent reassignment (silent)
   - Tier 3: Queen reassigns (silent — retire failed worker, spawn different caste)
   - Tier 4: User escalation (visible — ESCALATION banner with tried/options/recommendation)
   Remove the `flag-add` bash call from Tier 4. Replace with: "If the calling command supports flag persistence, note the blocker for /ant:status." Keep the escalation banner format.
   Add critical failures: COLONY_STATE.json corruption (STOP), destructive git operation (STOP).

7. **`<escalation>`**: Queen is the top of the chain. Escalation at Tier 4 goes to the user. Note: "Queen does not escalate to another agent — Tier 4 surfaces directly to the user." Include: "Do NOT attempt to spawn sub-workers from a sub-worker — Claude Code subagents cannot spawn other subagents."

8. **`<boundaries>`**: Port from OpenCode Queen:
   - Global protected paths (dreams, env, settings, workflows)
   - Queen-specific: no direct source editing (spawn Builder), no destructive git ops, no reading/exposing API keys
   - Queen IS permitted to: write colony state via aether-utils.sh, spawn workers via Task tool, read any file

**PWR Compliance Checklist (all must pass):**
- PWR-01: YAML frontmatter with name, quoted description, tools, model
- PWR-02: 8 XML sections present
- PWR-03: No spawn-can-spawn, generate-ant-name, spawn-log bash calls
- PWR-04: No activity-log bash calls
- PWR-05: Explicit tools field (Read, Write, Edit, Bash, Grep, Glob, Task)
- PWR-06: Escalation section present (replaces spawn sub-workers)
- PWR-07: Boundaries section present with protected paths
- PWR-08: No flag-add bash calls (replaced with structured return)
  </action>
  <verify>
1. File exists: `ls -la .claude/agents/ant/aether-queen.md`
2. YAML parses correctly: check that frontmatter has name, description (quoted), tools (includes Task), model
3. Contains all 8 XML sections: `grep -c '<role>\|<execution_flow>\|<critical_rules>\|<return_format>\|<success_criteria>\|<failure_modes>\|<escalation>\|<boundaries>' .claude/agents/ant/aether-queen.md` should return 8
4. Contains all 6 workflow patterns: `grep -c 'SPBV\|Investigate-Fix\|Deep Research\|Refactor\|Compliance\|Documentation Sprint' .claude/agents/ant/aether-queen.md` should return at least 6
5. Zero OpenCode-specific patterns: `grep -c 'activity-log\|spawn-can-spawn\|generate-ant-name\|spawn-log\|spawn-complete\|flag-add' .claude/agents/ant/aether-queen.md` should return 0
6. Task tool present: `grep 'tools:.*Task' .claude/agents/ant/aether-queen.md` should match
7. Caste emojis in spawn protocol: `grep -c '🔨🐜\|🔭🐜\|👁🐜\|🗺🐜' .claude/agents/ant/aether-queen.md` should return at least 4
  </verify>
  <done>
`.claude/agents/ant/aether-queen.md` exists with: YAML frontmatter (quoted description, Task in tools), 8 XML sections, all 6 workflow patterns, 4-tier escalation chain, caste emoji spawn protocol, zero OpenCode bash patterns. File is PWR-01 through PWR-08 compliant.
  </done>
</task>

</tasks>

<verification>
1. Queen agent file exists at `.claude/agents/ant/aether-queen.md`
2. All 8 XML sections present (role, execution_flow, critical_rules, return_format, success_criteria, failure_modes, escalation, boundaries)
3. Task tool in frontmatter tools field
4. All 6 workflow patterns ported verbatim
5. 4-tier escalation chain complete
6. Zero OpenCode-specific bash calls (activity-log, spawn-can-spawn, generate-ant-name, spawn-log, spawn-complete, flag-add)
7. Description includes explicit "Do NOT use for..." routing guidance
</verification>

<success_criteria>
Queen agent is a complete, PWR-compliant Claude Code subagent with Task tool orchestration capability. The agent loads without YAML errors and contains the full colony coordination logic from OpenCode with zero OpenCode-specific patterns remaining.
</success_criteria>

<output>
After completion, create `.planning/phases/28-orchestration-layer-surveyor-variants/28-01-SUMMARY.md`
</output>
