---
phase: 30-niche-agents
plan: "03"
type: execute
wave: 2
depends_on: ["30-01", "30-02"]
files_modified:
  - tests/unit/agent-quality.test.js
autonomous: true
requirements: [NICHE-01, NICHE-02, NICHE-03, NICHE-04, NICHE-05, NICHE-06, NICHE-07, NICHE-08]

must_haves:
  truths:
    - "TEST-05 passes: 22 agents found, 22 expected"
    - "TEST-03 enforces read-only constraints on all 8 read-only agents (not just Tracker and Auditor)"
    - "All 6 test functions pass for all 22 agents (npm test is clean)"
    - "READ_ONLY_CONSTRAINTS registry includes 6 new niche read-only agents with correct forbidden tool lists"
  artifacts:
    - path: "tests/unit/agent-quality.test.js"
      provides: "Expanded READ_ONLY_CONSTRAINTS covering 8 read-only agents"
      contains: "aether-gatekeeper"
  key_links:
    - from: "tests/unit/agent-quality.test.js READ_ONLY_CONSTRAINTS"
      to: ".claude/agents/ant/aether-gatekeeper.md"
      via: "TEST-03 forbidden tool validation"
      pattern: "aether-gatekeeper.*forbidden.*Write.*Edit.*Bash"
    - from: "tests/unit/agent-quality.test.js READ_ONLY_CONSTRAINTS"
      to: ".claude/agents/ant/aether-includer.md"
      via: "TEST-03 forbidden tool validation"
      pattern: "aether-includer.*forbidden.*Write.*Edit.*Bash"
    - from: "tests/unit/agent-quality.test.js EXPECTED_AGENT_COUNT"
      to: ".claude/agents/ant/ (22 files)"
      via: "TEST-05 count validation"
      pattern: "EXPECTED_AGENT_COUNT = 22"
---

<objective>
Expand the READ_ONLY_CONSTRAINTS registry in the agent quality test suite to cover all 8 read-only agents, then run the full test suite to confirm all 22 agents pass all quality gates.

Purpose: Without this update, TEST-03 only validates read-only constraints for Tracker and Auditor (Phase 29 agents). The 6 new read-only niche agents would pass tests even with incorrect tool assignments. This plan closes that gap and confirms the complete 22-agent roster is quality-compliant.

Output: Updated test file with expanded registry. All tests green including the previously-failing TEST-05.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-niche-agents/30-RESEARCH.md

Test file to modify:
@tests/unit/agent-quality.test.js

Prior plan SUMMARYs (needed for understanding what was created):
@.planning/phases/30-niche-agents/30-01-SUMMARY.md
@.planning/phases/30-niche-agents/30-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand READ_ONLY_CONSTRAINTS and verify full test suite</name>
  <files>tests/unit/agent-quality.test.js</files>
  <action>
Update the `READ_ONLY_CONSTRAINTS` object in `tests/unit/agent-quality.test.js` to include all 6 new read-only niche agents alongside the existing 2 Phase 29 agents.

**Replace the existing READ_ONLY_CONSTRAINTS (around line 60-63) with:**

```javascript
const READ_ONLY_CONSTRAINTS = {
  // Phase 29 — specialists
  'aether-tracker':       { forbidden: ['Write', 'Edit'] },
  'aether-auditor':       { forbidden: ['Write', 'Edit', 'Bash'] },
  // Phase 30 — niche agents (read-only set)
  'aether-chaos':         { forbidden: ['Write', 'Edit'] },
  'aether-archaeologist': { forbidden: ['Write', 'Edit'] },
  'aether-gatekeeper':    { forbidden: ['Write', 'Edit', 'Bash'] },
  'aether-includer':      { forbidden: ['Write', 'Edit', 'Bash'] },
  'aether-measurer':      { forbidden: ['Write', 'Edit'] },
  'aether-sage':          { forbidden: ['Write', 'Edit'] },
};
```

**Constraint details:**
- Chaos: `Read, Bash, Grep, Glob` — forbidden: Write, Edit
- Archaeologist: `Read, Bash, Grep, Glob` — forbidden: Write, Edit
- Gatekeeper: `Read, Grep, Glob` — forbidden: Write, Edit, Bash (most restrictive)
- Includer: `Read, Grep, Glob` — forbidden: Write, Edit, Bash (most restrictive)
- Measurer: `Read, Bash, Grep, Glob` — forbidden: Write, Edit
- Sage: `Read, Grep, Glob, Bash` — forbidden: Write, Edit

**Do NOT change EXPECTED_AGENT_COUNT** — it is already 22. The test passes when all 22 agents exist.

**Also update the file header comment** to reflect Phase 30 completion. Change the note:
```
 * TEST-05 intentionally fails until Phase 30 completes — this is a
 * tracking mechanism, not a defect. Expected: 22. Current after Phase 29: 14.
```
to:
```
 * TEST-05 now passes — Phase 30 completed the full 22-agent roster.
```

After updating the file, run the full test suite:
```bash
npm test
```

All tests must pass including TEST-05. If any test fails, investigate and fix the root cause (likely a typo in an agent frontmatter or a missing XML section).
  </action>
  <verify>
1. `npm test` passes with 0 failures
2. TEST-05 passes: 22 agents found, 22 expected
3. TEST-03 covers 8 agents in READ_ONLY_CONSTRAINTS (confirm with `grep -c "aether-" tests/unit/agent-quality.test.js` in the constraints section)
4. Verify expanded constraints: `grep "aether-gatekeeper" tests/unit/agent-quality.test.js` returns a match
5. Verify EXPECTED_AGENT_COUNT unchanged: `grep "EXPECTED_AGENT_COUNT = 22" tests/unit/agent-quality.test.js` returns a match
  </verify>
  <done>
READ_ONLY_CONSTRAINTS expanded to cover all 8 read-only agents with correct forbidden tool lists. EXPECTED_AGENT_COUNT remains 22 (unchanged). `npm test` passes with 0 failures — all 22 agents pass TEST-01 through TEST-05 and the body quality check. Phase 30 is complete.
  </done>
</task>

</tasks>

<verification>
After task completes:
1. `npm test` exits with code 0
2. All 6 test functions (TEST-01 through TEST-05 + body quality) pass for all 22 agents
3. TEST-05 no longer intentionally fails — 22 agents found, 22 expected
4. TEST-03 validates read-only constraints on Tracker, Auditor, Chaos, Archaeologist, Gatekeeper, Includer, Measurer, Sage
5. No test regressions — all existing tests still pass
</verification>

<success_criteria>
- `npm test` passes with 0 failures and 0 skips
- TEST-05 reports 22 agents found (matching EXPECTED_AGENT_COUNT of 22)
- TEST-03 covers 8 read-only agents (2 from Phase 29 + 6 from Phase 30)
- Gatekeeper and Includer have Bash in their forbidden list (most restrictive)
- EXPECTED_AGENT_COUNT was NOT changed (still 22)
- The full 22-agent roster passes all quality gates
</success_criteria>

<output>
After completion, create `.planning/phases/30-niche-agents/30-03-SUMMARY.md`
</output>
