---
phase: 44-suggest-pheromones
plan: 03
type: execute
wave: 2
depends_on:
  - 44-01
  - 44-02
files_modified:
  - .claude/commands/ant/build.md
autonomous: true
requirements:
  - SUGG-01
  - SUGG-02
must_haves:
  truths:
    - Build command shows pheromone suggestions at build start
    - Suggestions appear after Load Colony Context (Step 4)
    - Suggestions appear before Initialize Swarm Display (Step 5)
    - User can dismiss all suggestions and proceed immediately
    - Build proceeds normally after suggestion handling
    - No build delay if user dismisses all
  artifacts:
    - path: .claude/commands/ant/build.md
      provides: "Integration point for pheromone suggestions in build flow"
      contains: "Step 4.2: Suggest Pheromones"
  key_links:
    - from: "build.md Step 4"
      to: "suggest-approve command"
      via: "bash .aether/aether-utils.sh suggest-approve"
---

<objective>
Integrate the pheromone suggestion system into the build command flow.

Purpose: Wire the suggestion UI into the build process so users see suggested pheromones at the right moment — after context is loaded but before workers are spawned.

Output: Updated build.md with Step 4.2 that calls suggest-approve.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-suggest-pheromones/44-RESEARCH.md
@.planning/phases/44-suggest-pheromones/44-CONTEXT.md
@.claude/commands/ant/build.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 4.2 to build.md for pheromone suggestions</name>
  <files>.claude/commands/ant/build.md</files>
  <action>
    Insert a new Step 4.2 "Suggest Pheromones" into build.md between Step 4 (Load Colony Context) and Step 5 (Initialize Swarm Display).

    The new step should:

    1. **Timing**: Run after Step 4 (colony-prime) and before Step 5 (swarm display init)
       - This ensures pheromones are already displayed to the user
       - User sees current signals before getting suggestions for new ones

    2. **Integration logic**:
       ```markdown
       ### Step 4.2: Suggest Pheromones

       Analyze codebase and suggest pheromone signals based on detected patterns.

       Run using the Bash tool with description "Analyzing codebase for suggestions...":
       ```bash
       bash .aether/aether-utils.sh suggest-approve --dry-run 2>/dev/null
       ```

       Parse the JSON result to get `suggestion_count`.

       If `suggestion_count` > 0:
       - Display: "💡 {count} pheromone suggestion(s) detected from code analysis"
       - Run: `bash .aether/aether-utils.sh suggest-approve`
       - Parse result for approved/rejected/skipped counts
       - If approved > 0: Display "✓ {approved} FOCUS signal(s) added"

       If `suggestion_count` == 0:
       - Skip silently (no output)

       **Non-blocking**: This step never stops the build. Even if suggest-approve fails,
       log a warning and continue to Step 5.
       ```

    3. **Error handling**:
       - If suggest-analyze returns error: Log warning, continue
       - If suggest-approve returns error: Log warning, continue
       - Never let suggestion failures block the build

    4. **Flags propagation**:
       - If user passed `--yes` to build: Pass `--yes` to suggest-approve
       - If user passed `--no-suggest` to build: Skip Step 4.2 entirely

    Update the flag parsing in Step 1 to recognize:
    - `--no-suggest`: Skip pheromone suggestions

    Update the usage/help text to include:
    ```
    --no-suggest        Skip pheromone suggestion analysis
    ```
  </action>
  <verify>
    grep -q "Step 4.2: Suggest Pheromones" .claude/commands/ant/build.md
  </verify>
  <done>
    Step 4.2 added to build.md, integrated between Step 4 and Step 5, handles flags correctly
  </done>
</task>

<task type="auto">
  <name>Task 2: Update build command documentation</name>
  <files>.claude/commands/ant/build.md</files>
  <action>
    Update the build command documentation to reflect the new suggestion feature:

    1. In the usage/help section (Step 1), add `--no-suggest` flag:
       ```
       Options:
         --verbose, -v       Show full completion details
         --no-visual         Disable real-time visual display
         --model, -m <name>  Override model for this build
         --no-suggest        Skip pheromone suggestion analysis
       ```

    2. Add a brief comment in the header/description explaining:
       ```markdown
       ## Pheromone Suggestions

       At build start, the colony analyzes the codebase for patterns that might benefit
       from pheromone signals (complex files, TODOs, debug artifacts, etc.). Suggested
       signals are presented for approval and written as FOCUS pheromones if approved.

       Use `--no-suggest` to skip this analysis.
       ```

    3. Ensure the step numbering is correct after insertion:
       - Step 4: Load Colony Context
       - Step 4.0: Load Territory Survey
       - Step 4.1: Archaeologist Pre-Build Scan
       - Step 4.2: Suggest Pheromones (NEW)
       - Step 5: Initialize Swarm Display
  </action>
  <verify>
    grep -q "--no-suggest" .claude/commands/ant/build.md && grep -q "pheromone suggestion" .claude/commands/ant/build.md
  </verify>
  <done>
    Documentation updated with --no-suggest flag and pheromone suggestion explanation
  </done>
</task>

</tasks>

<verification>
1. Read build.md and verify Step 4.2 exists in correct location
2. Verify --no-suggest flag is documented
3. Verify step numbering is consistent
4. Check that error handling is non-blocking
</verification>

<success_criteria>
- Step 4.2 "Suggest Pheromones" exists in build.md
- Positioned after Step 4 (Load Colony Context) and before Step 5 (Initialize Swarm)
- --no-suggest flag documented and handled
- Non-blocking error handling (warnings only)
- Integration calls suggest-approve correctly
</success_criteria>

<output>
After completion, create `.planning/phases/44-suggest-pheromones/44-03-SUMMARY.md`
</output>
