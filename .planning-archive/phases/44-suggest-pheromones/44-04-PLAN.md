---
phase: 44-suggest-pheromones
plan: 04
type: tdd
wave: 3
depends_on:
  - 44-01
  - 44-02
  - 44-03
files_modified:
  - tests/integration/suggest-pheromones.test.js
  - .aether/data/session.json
autonomous: true
requirements:
  - SUGG-01
  - SUGG-02
must_haves:
  truths:
    - Tests verify suggest-analyze detects all 6 pattern types
    - Tests verify suggest-approve UI workflow
    - Tests verify integration with build command
    - Tests verify deduplication logic
    - Tests verify exclusion patterns
  artifacts:
    - path: tests/integration/suggest-pheromones.test.js
      provides: "Integration tests for pheromone suggestion system"
      min_lines: 100
  key_links:
    - from: "Test suite"
      to: "suggest-analyze, suggest-approve commands"
      via: "bash .aether/aether-utils.sh"
---

<objective>
Create comprehensive tests for the pheromone suggestion system.

Purpose: Ensure the suggestion system works correctly across all scenarios — pattern detection, UI workflow, deduplication, and build integration.

Output: Integration test file with passing tests for all suggestion functionality.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-suggest-pheromones/44-RESEARCH.md
@.planning/phases/44-suggest-pheromones/44-CONTEXT.md
@.aether/aether-utils.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration tests for suggest-analyze</name>
  <files>tests/integration/suggest-pheromones.test.js</files>
  <action>
    Create integration tests for the suggest-analyze command:

    1. **Pattern detection tests:**
       - Large file detection: Create temp file >300 lines, verify FOCUS suggestion
       - TODO/FIXME detection: Create file with TODO comments, verify FEEDBACK suggestion
       - Debug artifact detection: Create file with console.log, verify REDIRECT suggestion
       - Type gap detection: Create file with `: any`, verify FEEDBACK suggestion
       - Complexity detection: Create file with >20 functions, verify FOCUS suggestion
       - Test gap detection: Create source file without test, verify FOCUS suggestion

    2. **Exclusion tests:**
       - Verify node_modules/ files are excluded
       - Verify .aether/ files are excluded
       - Verify dist/ and build/ are excluded

    3. **Deduplication tests:**
       - Verify existing pheromones are not re-suggested
       - Verify session-recorded suggestions are not re-suggested
       - Verify hash generation is consistent

    4. **JSON output tests:**
       - Verify valid JSON returned
       - Verify suggestions array structure
       - Verify analyzed_files count
       - Verify patterns_found count

    Use temp directories for test files. Clean up after each test.
  </action>
  <verify>npm test -- tests/integration/suggest-pheromones.test.js passes</verify>
  <done>All suggest-analyze tests pass, covering pattern detection, exclusions, and deduplication</done>
</task>

<task type="auto">
  <name>Task 2: Create integration tests for suggest-approve</name>
  <files>tests/integration/suggest-pheromones.test.js</files>
  <action>
    Add tests for the suggest-approve command:

    1. **UI workflow tests (using --dry-run):**
       - Verify suggestions displayed one at a time
       - Verify correct emoji per type (🎯 🚫 💬)
       - Verify Approve/Reject/Skip options exist
       - Verify Dismiss All option exists

    2. **Flag tests:**
       - --yes flag: Verify auto-approval works
       - --dry-run flag: Verify no pheromones written
       - --no-suggest flag: Verify immediate exit

    3. **Integration tests:**
       - Verify approved suggestions written to pheromones.json
       - Verify rejected suggestions recorded in session.json
       - Verify JSON summary returned with correct counts

    4. **Error handling tests:**
       - Verify graceful handling when suggest-analyze fails
       - Verify non-blocking behavior on errors

    Mock user input for interactive tests using stdin redirection.
  </action>
  <verify>npm test -- tests/integration/suggest-pheromones.test.js passes</verify>
  <done>All suggest-approve tests pass, covering UI workflow, flags, and integration</done>
</task>

</tasks>

<verification>
1. Run `npm test -- tests/integration/suggest-pheromones.test.js` - all tests pass
2. Verify test coverage includes all 6 heuristics
3. Verify test coverage includes all UI options
4. Check that temp files are cleaned up
</verification>

<success_criteria>
- Integration test file exists with comprehensive test coverage
- All tests pass
- Tests cover pattern detection (6 heuristics)
- Tests cover UI workflow (Approve/Reject/Skip/Dismiss All)
- Tests cover flag handling (--yes, --dry-run, --no-suggest)
- Tests cover deduplication logic
- Tests cover exclusion patterns
</success_criteria>

<output>
After completion, create `.planning/phases/44-suggest-pheromones/44-04-SUMMARY.md`
</output>
