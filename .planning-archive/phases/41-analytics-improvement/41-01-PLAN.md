---
phase: 41-analytics-improvement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/ant/seal.md
autonomous: true
requirements:
  - ANA-01
  - ANA-02
  - ANA-03

must_haves:
  truths:
    - Sage spawns in /ant:seal Step 3.5 when colony has 3+ completed phases
    - Sage analyzes velocity trends, bug density, and review turnaround
    - Sage provides data-driven insights to wisdom promotion process
  artifacts:
    - path: .claude/commands/ant/seal.md
      provides: Sage integration at Step 3.5
      contains: "Step 3.5: Analytics Review"
  key_links:
    - from: seal.md Step 3.5
      to: aether-sage agent
      via: Task tool with subagent_type="aether-sage"
    - from: Sage output
      to: wisdom promotion process
      via: JSON findings with recommendations
---

<objective>
Integrate Sage agent into /ant:seal command for colony analytics review.

Purpose: Provide data-driven insights during seal ceremony to inform wisdom promotion decisions. Sage analyzes historical colony data to identify patterns and trends.

Output: Updated seal.md with Sage spawn at Step 3.5 (between wisdom approval and activity logging).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.opencode/agents/aether-sage.md
@.claude/commands/ant/seal.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sage trigger logic to seal.md Step 3.5</name>
  <files>.claude/commands/ant/seal.md</files>
  <action>
Insert new Step 3.5 between existing Step 3.5 (Wisdom Approval) and Step 4 (Log Seal Activity).

The new Step 3.5 should:
1. Check if colony has 3+ completed phases by reading COLONY_STATE.json
2. If phases_completed >= 3, spawn Sage agent
3. If phases_completed < 3, skip silently (no output)

Sage spawn pattern (following Chronicler pattern from Step 5.5):
```bash
# Check phase threshold
phases_completed=$(jq '[.plan.phases[] | select(.status == "completed")] | length' .aether/data/COLONY_STATE.json 2>/dev/null || echo "0")

if [[ "$phases_completed" -ge 3 ]]; then
  # Generate Sage name and dispatch
  sage_name=$(bash .aether/aether-utils.sh generate-ant-name "sage")
  bash .aether/aether-utils.sh spawn-log "Queen" "sage" "$sage_name" "Colony analytics review"
  bash .aether/aether-utils.sh swarm-display-update "$sage_name" "sage" "analyzing" "Colony analytics review" "Queen" '{"read":0,"grep":0,"edit":0,"bash":0}' 0 "fungus_garden" 0

  # Display spawn line
  echo "📜🐜 Sage $sage_name spawning — Analyzing colony trends and patterns..."
fi
```

Use Task tool with subagent_type="aether-sage" to spawn the agent when threshold is met.
  </action>
  <verify>grep -q "Step 3.5: Analytics Review" .claude/commands/ant/seal.md && echo "Step 3.5 header added"</verify>
  <done>Step 3.5 header exists and contains phase threshold check (>= 3)</done>
</task>

<task type="auto">
  <name>Task 2: Add Sage agent spawn with data sources</name>
  <files>.claude/commands/ant/seal.md</files>
  <action>
Add the Sage agent spawn using Task tool (following the pattern from Chronicler in Step 5.5).

The Sage agent should receive:
- phases_completed count
- Data sources: COLONY_STATE.json, activity.log, midden.json
- Analysis focus: velocity trends, bug density, review turnaround

Spawn XML structure:
```xml
<task>
  <description>📜 Sage {sage_name}: Colony analytics review</description>
  <prompt>
You are {sage_name}, a 📜 Sage Ant.

Mission: Analyze colony history and provide data-driven insights for wisdom promotion

Data Sources:
- COLONY_STATE.json: Colony configuration, phases, learnings, instincts
- activity.log: Activity history with timestamps
- midden.json: Failure patterns and flagged issues

Analysis Areas:
1. Velocity Trends: Phase completion rate over time
2. Bug Density: Error frequency and patterns from midden
3. Review Turnaround: Time from build to continue

Work:
1. Read COLONY_STATE.json for phase history and learnings
2. Read activity.log for velocity patterns
3. Read midden.json for bug density analysis
4. Calculate metrics and identify trends
5. Provide recommendations for wisdom promotion

**IMPORTANT:** You are strictly read-only. Do not modify any files.

Log activity: bash .aether/aether-utils.sh activity-log "ANALYZING" "{sage_name}" "description"

Return ONLY this JSON (no other text):
{{
  "ant_name": "{sage_name}",
  "caste": "sage",
  "status": "completed" | "failed" | "blocked",
  "summary": "What you analyzed and found",
  "key_findings": [],
  "trends": {{
    "velocity": "increasing|stable|decreasing",
    "bug_density": "high|medium|low",
    "review_turnaround": "fast|moderate|slow"
  }},
  "metrics_analyzed": [],
  "predictions": [],
  "recommendations": [
    {{"priority": 1, "action": "", "expected_impact": ""}}
  ],
  "next_steps": [],
  "blockers": []
}}
  </prompt>
</task>
```

After spawn, parse JSON output and:
1. Log completion via spawn-complete
2. Update swarm display to completed
3. Display findings summary (non-blocking)
4. Log high-priority recommendations to midden
  </action>
  <verify>grep -q "subagent_type=\"aether-sage\"" .claude/commands/ant/seal.md && echo "Sage spawn added"</verify>
  <done>Sage agent spawn exists with Task tool and proper JSON output parsing</done>
</task>

<task type="auto">
  <name>Task 3: Add Sage completion handling and integration</name>
  <files>.claude/commands/ant/seal.md</files>
  <action>
Add Sage completion handling after the Task spawn (following Chronicler pattern):

1. Parse Sage JSON output for key_findings and recommendations
2. Log completion:
```bash
bash .aether/aether-utils.sh spawn-complete "$sage_name" "completed" "Analytics review complete"
```

3. Update swarm display:
```bash
bash .aether/aether-utils.sh swarm-display-update "$sage_name" "sage" "completed" "Analytics review complete" "Queen" '{"read":5,"grep":3,"edit":0,"bash":2}' 100 "fungus_garden" 100
```

4. Display completion line:
```
📜 {sage_name}: Colony analytics review complete ✓
   Velocity: {trend} | Bug density: {level} | Review turnaround: {speed}
```

5. Log high-priority recommendations to midden (non-blocking):
```bash
# For each recommendation with priority <= 2
bash .aether/aether-utils.sh midden-write "analytics" "Sage recommendation (P{priority}): {action}" "sage"
```

6. Display insights summary (always show, even if empty):
```
📜 Sage Insights:
   Key Findings: {count}
   Top Recommendation: {first recommendation action}
```

The Sage step is NON-BLOCKING — seal always proceeds to Step 4 regardless of Sage findings.
  </action>
  <verify>grep -q "spawn-complete.*sage_name" .claude/commands/ant/seal.md && grep -q "midden-write.*analytics" .claude/commands/ant/seal.md && echo "Sage completion handling added"</verify>
  <done>Sage completion handling includes spawn-complete, swarm update, and midden logging</done>
</task>

</tasks>

<verification>
1. seal.md contains Step 3.5: Analytics Review
2. Step 3.5 checks for phases_completed >= 3 before spawning
3. Sage spawns via Task tool with subagent_type="aether-sage"
4. Sage receives data sources (COLONY_STATE.json, activity.log, midden.json)
5. Sage JSON output is parsed for key_findings and recommendations
6. High-priority recommendations are logged to midden
7. Sage is non-blocking — seal proceeds regardless of findings
8. Renumber subsequent steps (old Step 3.5 becomes Step 3.6, etc.)
</verification>

<success_criteria>
- Sage spawns conditionally when colony has 3+ completed phases
- Sage analyzes velocity trends, bug density, and review turnaround
- Sage provides data-driven insights via JSON output
- Findings are logged to midden for reference
- Sage integration is non-blocking
</success_criteria>

<output>
After completion, create `.planning/phases/41-analytics-improvement/41-01-SUMMARY.md`
</output>
