---
phase: 22-agent-boilerplate-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/agents/aether-queen.md
  - .opencode/agents/aether-builder.md
  - .opencode/agents/aether-watcher.md
  - .opencode/agents/aether-scout.md
  - .opencode/agents/aether-route-setter.md
  - .opencode/agents/aether-weaver.md
  - .opencode/agents/aether-probe.md
  - .opencode/agents/aether-ambassador.md
  - .opencode/agents/aether-tracker.md
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-03, AGENT-04]

must_haves:
  truths:
    - "Core 5 agents (queen, builder, watcher, scout, route-setter) have no Aether Integration, Depth-Based Behavior, or Reference sections"
    - "Development 4 agents (weaver, probe, ambassador, tracker) have no Aether Integration, Depth-Based Behavior, or Reference sections"
    - "All 9 agents retain their unique content sections (Activity Logging, Spawn Protocol, Flag-Creation, etc.)"
    - "All 9 agents have descriptions in 'Use this agent for...' format"
    - "npm run lint:sync passes after all edits"
    - "npm test passes after all edits"
  artifacts:
    - path: ".opencode/agents/aether-queen.md"
      provides: "Cleaned queen agent without boilerplate"
      contains: "Use this agent for"
    - path: ".opencode/agents/aether-builder.md"
      provides: "Cleaned builder agent without boilerplate"
      contains: "Use this agent for"
    - path: ".opencode/agents/aether-watcher.md"
      provides: "Cleaned watcher agent without boilerplate"
      contains: "Use this agent for"
    - path: ".opencode/agents/aether-scout.md"
      provides: "Cleaned scout agent without boilerplate"
      contains: "Use this agent for"
    - path: ".opencode/agents/aether-route-setter.md"
      provides: "Cleaned route-setter agent without boilerplate"
      contains: "Use this agent for"
  key_links:
    - from: ".opencode/agents/aether-queen.md"
      to: "Spawn Protocol section"
      via: "unique content preserved"
      pattern: "Spawn Protocol"
    - from: ".opencode/agents/aether-builder.md"
      to: "Spawning Sub-Workers section"
      via: "unique content preserved"
      pattern: "Spawning Sub-Workers"
    - from: ".opencode/agents/aether-watcher.md"
      to: "Creating Flags section"
      via: "unique content preserved"
      pattern: "flag-add"
---

<objective>
Clean boilerplate from the Core 5 and Development 4 agent clusters (9 agents total).

Purpose: Remove the three universally redundant sections (Aether Integration, Depth-Based Behavior, Reference) from each flat-markdown agent so they read like focused job descriptions. Update old-format descriptions to the "Use this agent for..." pattern. This is Batches 1 and 2 of 6.

Output: 9 cleaned agent files with boilerplate stripped and descriptions standardized.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-agent-boilerplate-cleanup/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip boilerplate from Core 5 agents (Batch 1)</name>
  <files>
    .opencode/agents/aether-queen.md
    .opencode/agents/aether-builder.md
    .opencode/agents/aether-watcher.md
    .opencode/agents/aether-scout.md
    .opencode/agents/aether-route-setter.md
  </files>
  <action>
For each of the 5 Core agents, read the file fully, then remove exactly these three sections:

**Section 1 — "## Aether Integration"** (and its bullet list):
- Standard form (19 agents): "This agent operates as a **specialist worker**..." with 4 bullets
- Queen variant: "This agent operates as the **orchestrator**..." with 5 bullets
- Remove the `## Aether Integration` header and ALL lines until the next `##` header

**Section 2 — "## Depth-Based Behavior"** (and its table):
- Standard form: 3-row table (Depth 1/2/3)
- Queen variant: 4-row table (Depth 0/1/2/3)
- Remove the `## Depth-Based Behavior` header and ALL lines until the next `##` header
- CRITICAL: Do NOT remove Queen's "## Spawn Protocol" or "## Spawn Limits" — those are unique content
- CRITICAL: Do NOT remove Builder's "## Spawning Sub-Workers" — that is unique content

**Section 3 — "## Reference"** (and its content):
- Standard form: "Full worker specifications: `.aether/workers.md`"
- Remove the `## Reference` header and ALL remaining lines (it is typically the last section)

**Description updates** — update the YAML frontmatter `description:` field for these agents that use the old format:
- queen: `"Use this agent for colony orchestration, phase coordination, and spawning specialized workers. The queen sets colony intention and manages state across the session."`
- builder: `"Use this agent for code implementation, file creation, command execution, and build tasks. The builder turns plans into working code."`
- watcher: `"Use this agent for validation, testing, quality assurance, and monitoring. The watcher ensures quality and guards the colony against regressions."`
- scout: `"Use this agent for research, information gathering, documentation exploration, and codebase analysis. The scout explores and reports back findings."`
- route-setter: `"Use this agent for creating structured phase plans, analyzing dependencies, and optimizing task ordering. The route-setter charts the colony's path forward."`

**Preserve ALL other sections.** Specifically watch for:
- Queen: Keep Spawn Protocol, Spawn Limits, Phase Flow, Colony State Management, Worker Communication
- Builder: Keep Spawning Sub-Workers, Build Discipline, TDD Discipline
- Watcher: Keep Creating Flags for Failures, Verification Workflow, Testing Discipline
- Scout: Keep spawning section (parallel scout), Research Protocol
- Route-setter: Keep Planning Discipline, Dependency Analysis

After editing all 5 files, run verification.
  </action>
  <verify>
Run `npm run lint:sync` — must pass with exit code 0.
Run `npm test` — must pass with 0 failures.
For each of the 5 files, confirm:
- No "## Aether Integration" header exists
- No "## Depth-Based Behavior" header exists
- No "## Reference" header exists
- The `description:` field starts with "Use this agent for"
- Activity Logging section is still present
- Agent-specific unique sections are still present (spot-check queen for "Spawn Protocol", builder for "Spawning Sub-Workers", watcher for "flag-add")
  </verify>
  <done>5 Core agents cleaned: no boilerplate sections remain, descriptions updated, all unique content preserved, lint:sync and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Strip boilerplate from Development 4 agents (Batch 2)</name>
  <files>
    .opencode/agents/aether-weaver.md
    .opencode/agents/aether-probe.md
    .opencode/agents/aether-ambassador.md
    .opencode/agents/aether-tracker.md
  </files>
  <action>
For each of the 4 Development cluster agents, read the file fully, then remove exactly the same three sections as Task 1:

**Section 1 — "## Aether Integration"** — remove header and all content through the bullet list, up to the next `##`
**Section 2 — "## Depth-Based Behavior"** — remove header and table, up to the next `##`
**Section 3 — "## Reference"** — remove header and remaining content (typically the last section)

**Description updates** — these agents already use the "Use this agent for..." format in their descriptions. Verify each one and leave as-is if already correct.

**Preserve ALL other sections.** These agents have fewer unique sections than the Core 5 but still have agent-specific content:
- Weaver: Refactoring Discipline, Code Patterns
- Probe: Testing Methodology, Mutation Testing
- Ambassador: Integration Patterns, SDK Setup
- Tracker: Bug Investigation, Root Cause Analysis

After editing all 4 files, run verification.
  </action>
  <verify>
Run `npm run lint:sync` — must pass with exit code 0.
Run `npm test` — must pass with 0 failures.
For each of the 4 files, confirm:
- No "## Aether Integration" header exists
- No "## Depth-Based Behavior" header exists
- No "## Reference" header exists
- Activity Logging section is still present
- Agent-specific content sections are still present
  </verify>
  <done>4 Development agents cleaned: no boilerplate sections remain, descriptions verified, all unique content preserved, lint:sync and tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run lint:sync` passes — commands still in sync between Claude Code and OpenCode
2. `npm test` passes — all 446+ tests still green, 0 failures
3. All 9 agent files have no Aether Integration, Depth-Based Behavior, or Reference sections
4. All 9 agent files retain their Activity Logging and agent-specific unique sections
5. All 5 Core agents have updated "Use this agent for..." descriptions
</verification>

<success_criteria>
- 9 flat-markdown agents stripped of 3 boilerplate sections each (~14 lines removed per agent, ~126 lines total)
- 5 agent descriptions updated to "Use this agent for..." format
- Zero test regressions
- All unique agent content preserved intact
</success_criteria>

<output>
After completion, create `.planning/phases/22-agent-boilerplate-cleanup/22-01-SUMMARY.md`
</output>
