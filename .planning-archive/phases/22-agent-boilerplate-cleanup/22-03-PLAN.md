---
phase: 22-agent-boilerplate-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/agents/aether-archaeologist.md
  - .opencode/agents/aether-chaos.md
  - .opencode/agents/aether-architect.md
  - .opencode/agents/aether-surveyor-nest.md
  - .opencode/agents/aether-surveyor-disciplines.md
  - .opencode/agents/aether-surveyor-pathogens.md
  - .opencode/agents/aether-surveyor-provisions.md
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-03, AGENT-04]

must_haves:
  truths:
    - "Special 3 agents (archaeologist, chaos, architect) have no Aether Integration, Depth-Based Behavior, or Reference sections"
    - "All 3 special agents have descriptions in 'Use this agent for...' format"
    - "4 surveyor agents have descriptions updated to 'Use this agent for...' format"
    - "4 surveyor agents have no other changes (no boilerplate exists to strip)"
    - "npm run lint:sync passes after all edits"
    - "npm test passes after all edits"
  artifacts:
    - path: ".opencode/agents/aether-archaeologist.md"
      provides: "Cleaned archaeologist agent without boilerplate"
      contains: "Use this agent for"
    - path: ".opencode/agents/aether-chaos.md"
      provides: "Cleaned chaos agent without boilerplate"
      contains: "Use this agent for"
    - path: ".opencode/agents/aether-architect.md"
      provides: "Cleaned architect agent without boilerplate"
      contains: "Use this agent for"
    - path: ".opencode/agents/aether-surveyor-nest.md"
      provides: "Surveyor with updated description"
      contains: "Use this agent for"
    - path: ".opencode/agents/aether-surveyor-disciplines.md"
      provides: "Surveyor with updated description"
      contains: "Use this agent for"
    - path: ".opencode/agents/aether-surveyor-pathogens.md"
      provides: "Surveyor with updated description"
      contains: "Use this agent for"
    - path: ".opencode/agents/aether-surveyor-provisions.md"
      provides: "Surveyor with updated description"
      contains: "Use this agent for"
  key_links:
    - from: ".opencode/agents/aether-architect.md"
      to: "Model Context section"
      via: "NOT stripped — deferred per user decision (outdated content is separate task)"
      pattern: "Model Context"
---

<objective>
Clean boilerplate from the Special 3 agents and update descriptions on the Surveyor 4 agents (7 agents total).

Purpose: Remove the three universally redundant sections from the 3 special agents and update all 7 agents' descriptions to the "Use this agent for..." format. The surveyor agents use XML structure and have zero boilerplate to strip — they only need description updates. This is Batches 5 and 6 of 6.

Output: 3 cleaned special agent files + 4 surveyor files with updated descriptions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-agent-boilerplate-cleanup/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip boilerplate from Special 3 agents (Batch 5)</name>
  <files>
    .opencode/agents/aether-archaeologist.md
    .opencode/agents/aether-chaos.md
    .opencode/agents/aether-architect.md
  </files>
  <action>
For each of the 3 Special agents, read the file fully, then remove exactly these three sections:

**Section 1 — "## Aether Integration"** — remove the header and all content (bullet list) up to the next `##` header.

**Section 2 — "## Depth-Based Behavior"** — remove the header and table up to the next `##` header.

**Section 3 — "## Reference"** — remove the header and all remaining content. Special notes:
- Archaeologist has an extra line: "Archaeology command documentation: `.claude/commands/ant/archaeology.md`" — strip this too (the agent's workflow section already describes everything it needs)
- Chaos has an extra line: "Chaos command documentation: `.claude/commands/ant/chaos.md`" — strip this too (same reasoning)
- Both agents have fully detailed workflows making these reference lines unnecessary

**Description updates** — update the YAML frontmatter `description:` field:
- archaeologist: `"Use this agent for git history excavation, understanding why code exists, and tracing the evolution of decisions through commit archaeology."`
- chaos: `"Use this agent for resilience testing, edge case probing, and boundary condition analysis. The chaos agent stress-tests your system to find where it breaks."`
- architect: `"Use this agent for knowledge synthesis, documentation coordination, and architectural analysis. The architect connects dots across the codebase."`

**CRITICAL — Do NOT strip architect's Model Context section.** Per user decision: "Outdated references: DO NOT fix — only strip boilerplate." The Model Context section is not part of the three-section boilerplate taxonomy. Leave it as-is.

**Preserve ALL other sections.** Each agent has unique content:
- Archaeologist: Excavation Methodology, Git Archaeology Tools, Output Format
- Chaos: Chaos Testing Methodology, Boundary Probing, Resilience Patterns
- Architect: Model Context (keep!), Synthesis Workflow, Documentation Coordination

After editing all 3 files, run verification.
  </action>
  <verify>
Run `npm run lint:sync` — must pass with exit code 0.
Run `npm test` — must pass with 0 failures.
For each of the 3 files, confirm:
- No "## Aether Integration" header exists
- No "## Depth-Based Behavior" header exists
- No "## Reference" header exists
- The `description:` field starts with "Use this agent for"
- Activity Logging section is still present
- Architect still has its Model Context section
  </verify>
  <done>3 Special agents cleaned: no boilerplate sections remain, descriptions updated, all unique content preserved, lint:sync and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update descriptions on Surveyor 4 agents (Batch 6)</name>
  <files>
    .opencode/agents/aether-surveyor-nest.md
    .opencode/agents/aether-surveyor-disciplines.md
    .opencode/agents/aether-surveyor-pathogens.md
    .opencode/agents/aether-surveyor-provisions.md
  </files>
  <action>
The 4 surveyor agents use XML structure and have ZERO of the three boilerplate sections. Do NOT attempt to strip sections — there is nothing to strip.

The ONLY change needed is updating the YAML frontmatter `description:` field from the old format to the "Use this agent for..." pattern:

- surveyor-nest: `"Use this agent for mapping architecture, directory structure, and codebase topology. The nest surveyor creates a structural map of the entire project."`
- surveyor-disciplines: `"Use this agent for mapping coding conventions, testing patterns, and development practices. The disciplines surveyor documents how the team builds software."`
- surveyor-pathogens: `"Use this agent for identifying technical debt, bugs, and codebase health concerns. The pathogens surveyor detects what needs fixing."`
- surveyor-provisions: `"Use this agent for mapping technology stack, dependencies, and external integrations. The provisions surveyor inventories what the project relies on."`

Read each file, update ONLY the `description:` line in the YAML frontmatter, and leave everything else untouched.

After editing all 4 files, run verification.
  </action>
  <verify>
Run `npm run lint:sync` — must pass with exit code 0.
Run `npm test` — must pass with 0 failures.
For each of the 4 files, confirm:
- The `description:` field starts with "Use this agent for"
- No other content has changed (diff should show only the description line)
  </verify>
  <done>4 Surveyor agents updated: descriptions standardized to "Use this agent for..." format, zero other changes, lint:sync and tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run lint:sync` passes — commands still in sync
2. `npm test` passes — all 446+ tests still green, 0 failures
3. All 3 special agent files have no Aether Integration, Depth-Based Behavior, or Reference sections
4. All 7 agent files have "Use this agent for..." descriptions
5. Architect's Model Context section is still present (not stripped)
6. All 4 surveyor files have ONLY description changes (no other edits)
</verification>

<success_criteria>
- 3 special agents stripped of 3 boilerplate sections each (~14 lines removed per agent, ~42 lines total)
- 7 agent descriptions updated to "Use this agent for..." format
- Zero test regressions
- All unique agent content preserved intact
- Surveyor file bodies completely untouched
</success_criteria>

<output>
After completion, create `.planning/phases/22-agent-boilerplate-cleanup/22-03-SUMMARY.md`
</output>
