---
phase: 22-agent-boilerplate-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/agents/aether-chronicler.md
  - .opencode/agents/aether-keeper.md
  - .opencode/agents/aether-auditor.md
  - .opencode/agents/aether-sage.md
  - .opencode/agents/aether-guardian.md
  - .opencode/agents/aether-measurer.md
  - .opencode/agents/aether-includer.md
  - .opencode/agents/aether-gatekeeper.md
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-04]

must_haves:
  truths:
    - "Knowledge 4 agents (chronicler, keeper, auditor, sage) have no Aether Integration, Depth-Based Behavior, or Reference sections"
    - "Quality 4 agents (guardian, measurer, includer, gatekeeper) have no Aether Integration, Depth-Based Behavior, or Reference sections"
    - "All 8 agents retain their unique content sections (Activity Logging plus domain-specific sections)"
    - "npm run lint:sync passes after all edits"
    - "npm test passes after all edits"
  artifacts:
    - path: ".opencode/agents/aether-chronicler.md"
      provides: "Cleaned chronicler agent without boilerplate"
    - path: ".opencode/agents/aether-keeper.md"
      provides: "Cleaned keeper agent without boilerplate"
    - path: ".opencode/agents/aether-auditor.md"
      provides: "Cleaned auditor agent without boilerplate"
    - path: ".opencode/agents/aether-sage.md"
      provides: "Cleaned sage agent without boilerplate"
    - path: ".opencode/agents/aether-guardian.md"
      provides: "Cleaned guardian agent without boilerplate"
    - path: ".opencode/agents/aether-measurer.md"
      provides: "Cleaned measurer agent without boilerplate"
    - path: ".opencode/agents/aether-includer.md"
      provides: "Cleaned includer agent without boilerplate"
    - path: ".opencode/agents/aether-gatekeeper.md"
      provides: "Cleaned gatekeeper agent without boilerplate"
  key_links:
    - from: "All 8 agents"
      to: "Activity Logging section"
      via: "unique caste-specific Actions list preserved"
      pattern: "Activity Logging"
---

<objective>
Clean boilerplate from the Knowledge 4 and Quality 4 agent clusters (8 agents total).

Purpose: Remove the three universally redundant sections (Aether Integration, Depth-Based Behavior, Reference) from each flat-markdown agent so they read like focused job descriptions. These 8 agents already have the "Use this agent for..." description format, so no description updates are needed. This is Batches 3 and 4 of 6.

Output: 8 cleaned agent files with boilerplate stripped.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-agent-boilerplate-cleanup/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip boilerplate from Knowledge 4 agents (Batch 3)</name>
  <files>
    .opencode/agents/aether-chronicler.md
    .opencode/agents/aether-keeper.md
    .opencode/agents/aether-auditor.md
    .opencode/agents/aether-sage.md
  </files>
  <action>
For each of the 4 Knowledge cluster agents, read the file fully, then remove exactly these three sections:

**Section 1 — "## Aether Integration"** — remove the header and all content (bullet list) up to the next `##` header.

**Section 2 — "## Depth-Based Behavior"** — remove the header and table up to the next `##` header.

**Section 3 — "## Reference"** — remove the header and all remaining content (typically the last section).

**Description verification** — these 4 agents already use the "Use this agent for..." format. Confirm each is correct; do not change.

**Preserve ALL other sections.** Each agent has unique domain content:
- Chronicler: Documentation Generation, README patterns, API docs
- Keeper: Knowledge Curation, Pattern Extraction, Institutional Memory
- Auditor: Code Review Lenses (security, performance, maintainability), Audit Output Format
- Sage: Analytics, Trend Analysis, Pattern Recognition, Data Insights

After editing all 4 files, run verification.
  </action>
  <verify>
Run `npm run lint:sync` — must pass with exit code 0.
Run `npm test` — must pass with 0 failures.
For each of the 4 files, confirm:
- No "## Aether Integration" header exists
- No "## Depth-Based Behavior" header exists
- No "## Reference" header exists
- Activity Logging section is still present
- Agent-specific domain sections are still present
  </verify>
  <done>4 Knowledge agents cleaned: no boilerplate sections remain, descriptions verified correct, all unique content preserved, lint:sync and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Strip boilerplate from Quality 4 agents (Batch 4)</name>
  <files>
    .opencode/agents/aether-guardian.md
    .opencode/agents/aether-measurer.md
    .opencode/agents/aether-includer.md
    .opencode/agents/aether-gatekeeper.md
  </files>
  <action>
For each of the 4 Quality cluster agents, read the file fully, then remove exactly the same three sections:

**Section 1 — "## Aether Integration"** — remove header and all content up to the next `##` header.

**Section 2 — "## Depth-Based Behavior"** — remove header and table up to the next `##` header.

**Section 3 — "## Reference"** — remove header and all remaining content (typically the last section).

**Description verification** — these 4 agents already use the "Use this agent for..." format. Confirm each is correct; do not change.

**Preserve ALL other sections.** Each agent has unique quality-domain content:
- Guardian: Security Domains, Vulnerability Patterns, Threat Assessment
- Measurer: Performance Profiling, Benchmarking, Bottleneck Detection
- Includer: Accessibility Auditing, WCAG Compliance, Inclusive Design
- Gatekeeper: Dependency Management, Supply Chain Security, License Compliance

After editing all 4 files, run verification.
  </action>
  <verify>
Run `npm run lint:sync` — must pass with exit code 0.
Run `npm test` — must pass with 0 failures.
For each of the 4 files, confirm:
- No "## Aether Integration" header exists
- No "## Depth-Based Behavior" header exists
- No "## Reference" header exists
- Activity Logging section is still present
- Agent-specific quality sections are still present
  </verify>
  <done>4 Quality agents cleaned: no boilerplate sections remain, descriptions verified correct, all unique content preserved, lint:sync and tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run lint:sync` passes — commands still in sync
2. `npm test` passes — all 446+ tests still green, 0 failures
3. All 8 agent files have no Aether Integration, Depth-Based Behavior, or Reference sections
4. All 8 agent files retain their Activity Logging and domain-specific unique sections
5. All 8 descriptions already in "Use this agent for..." format (no changes needed)
</verification>

<success_criteria>
- 8 flat-markdown agents stripped of 3 boilerplate sections each (~14 lines removed per agent, ~112 lines total)
- Zero test regressions
- All unique agent content preserved intact
</success_criteria>

<output>
After completion, create `.planning/phases/22-agent-boilerplate-cleanup/22-02-SUMMARY.md`
</output>
