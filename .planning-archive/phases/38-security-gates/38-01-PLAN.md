---
phase: 38-security-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/ant/continue.md
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - Gatekeeper spawns when package.json exists in project root
    - Gatekeeper performs CVE scanning, license compliance, and supply chain audit
    - Critical CVEs block phase advancement with hard stop
    - High CVEs warn and continue, logged to midden for later review
    - No package.json results in graceful skip with informative note
  artifacts:
    - path: .claude/commands/ant/continue.md
      provides: "Gatekeeper security gate integration in verification workflow"
      contains: "Step 1.8.1: Gatekeeper Security Gate"
  key_links:
    - from: continue.md Step 1.8.1
      to: Gatekeeper agent spawn
      via: Task tool with subagent_type=aether-gatekeeper
      pattern: "spawn-log.*gatekeeper"
    - from: Gatekeeper JSON output
      to: Gate decision logic
      via: "security.critical > 0" check
      pattern: "security.*critical"
---

<objective>
Integrate Gatekeeper agent into `/ant:continue` verification workflow to provide professional supply chain security scanning.

Purpose: Replace the basic grep security scan with comprehensive CVE scanning, license compliance checking, and dependency health assessment.
Output: Updated continue.md with Gatekeeper security gate (Step 1.8.1) that runs between Watcher verification and Auditor gate.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-security-gates/38-CONTEXT.md
@.planning/phases/38-security-gates/38-RESEARCH.md
@.opencode/agents/aether-gatekeeper.md
@.claude/commands/ant/continue.md

## User Decisions (LOCKED)

### Spawn Triggers
- Gatekeeper spawns when `package.json` exists in project root
- If no package.json, Gatekeeper skips gracefully (no error, just a note)

### Blocking Severity
- **Critical CVEs:** Hard block — no override, must fix to continue
- **High CVEs:** Warn and continue, log to midden for later review

### Integration Point
- Security gates run **after Watcher verification** (Step 1.8 area)
- Sequential order: Gatekeeper first (supply chain), then Auditor (code quality)
- Replaces existing basic grep security scan (Step 1.5, Phase 5)

### Agent Constraints
- Gatekeeper is strictly read-only
- Will not modify code, create files, or update colony state
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Gatekeeper Security Gate to continue.md</name>
  <files>.claude/commands/ant/continue.md</files>
  <action>
Insert new Step 1.8.1 "Gatekeeper Security Gate (Conditional)" between existing Step 1.7 (Anti-Pattern Gate) and Step 1.8 (TDD Evidence Gate - renumber to 1.9).

The new step must:

1. Check for package.json existence:
   - Run: `test -f package.json && echo "exists" || echo "missing"`
   - If missing: display "📦🐜 Gatekeeper: No package.json found — skipping supply chain audit" and continue to Step 1.8.2

2. If package.json exists:
   - Generate Gatekeeper name: `bash .aether/aether-utils.sh generate-ant-name "gatekeeper"`
   - Log spawn: `bash .aether/aether-utils.sh spawn-log "Queen" "gatekeeper" "{name}" "Supply chain security audit"`
   - Update swarm display for visual mode
   - Display: "📦🐜 Gatekeeper {name} spawning - Scanning dependencies for CVEs and license compliance..."

3. Spawn Gatekeeper using Task tool with `subagent_type="aether-gatekeeper"`:
   - Include fallback: If "Agent type not found", use general-purpose and inject role
   - Pass mission context and work instructions per agent definition
   - Request JSON output matching Gatekeeper schema

4. Parse Gatekeeper JSON output:
   - Extract: `security.critical`, `security.high`, `status`
   - Log completion via `spawn-complete`

5. Gate Decision Logic:
   - If `security.critical > 0`: Display hard block message, STOP progression, do NOT proceed
   - If `security.high > 0`: Display warning, log to midden via `midden-write`, continue
   - If clean: Display success, continue to Step 1.8.2

6. Update step numbering:
   - Current Step 1.8 (TDD Evidence Gate) → Step 1.9
   - Current Step 1.9 (Runtime Verification) → Step 1.10
   - Current Step 1.10 (Flags Gate) → Step 1.11

IMPORTANT: Keep existing Phase 5 security scan (secrets, debug artifacts) — Gatekeeper adds CVE scanning, doesn't replace it.
  </action>
  <verify>
    - grep -n "Step 1.8.1: Gatekeeper Security Gate" .claude/commands/ant/continue.md
    - grep -n "security.critical" .claude/commands/ant/continue.md
    - grep -n "subagent_type.*gatekeeper" .claude/commands/ant/continue.md
  </verify>
  <done>
    Step 1.8.1 exists with Gatekeeper spawn logic, critical CVE blocking, high CVE warning with midden logging, and proper step renumbering for subsequent gates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add midden-write utility function</name>
  <files>.aether/aether-utils.sh</files>
  <action>
Add a `midden-write` subcommand to aether-utils.sh for logging security warnings that don't block progression.

The function should:
1. Accept category (e.g., "security"), message, and source parameters
2. Append to `.aether/data/midden/midden.json` with proper JSON structure
3. Create midden directory if it doesn't exist
4. Include timestamp in ISO-8601 format
5. Return success even if midden doesn't exist (graceful degradation)

Structure:
```bash
midden-write)
  # Usage: midden-write <category> <message> <source>
  # Writes warning to midden for later review
  ;;
```

The JSON entry format:
```json
{
  "timestamp": "2026-02-22T10:30:00Z",
  "category": "security",
  "source": "gatekeeper",
  "message": "High CVEs found: 3",
  "reviewed": false
}
```

Place this near other midden-related functions (around line 6674-6787 where pheromone expiry archives to midden).
  </action>
  <verify>
    - grep -n "midden-write)" .aether/aether-utils.sh
    - bash .aether/aether-utils.sh midden-write "test" "test message" "test-source"
  </verify>
  <done>
    midden-write function exists and can write entries to midden.json successfully.
  </done>
</task>

</tasks>

<verification>
1. Gatekeeper step exists in continue.md at Step 1.8.1
2. Gatekeeper spawns only when package.json exists
3. Critical CVEs trigger hard block with clear error message
4. High CVEs trigger warning and midden logging
5. Step renumbering is correct (1.8→1.9, 1.9→1.10, 1.10→1.11)
6. midden-write utility exists and functions correctly
</verification>

<success_criteria>
- Step 1.8.1: Gatekeeper Security Gate exists in continue.md
- Gatekeeper spawns via Task tool with subagent_type="aether-gatekeeper"
- Critical CVE blocking logic implemented with hard stop
- High CVE warning logic implemented with midden logging
- All subsequent steps properly renumbered
- midden-write utility added to aether-utils.sh
</success_criteria>

<output>
After completion, create `.planning/phases/38-security-gates/38-01-SUMMARY.md`
</output>
