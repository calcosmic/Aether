---
phase: 33-add-promotion-proposals
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/aether-utils.sh
  - .aether/data/learning-observations.json
autonomous: true
requirements:
  - OBS-01
  - OBS-03
  - OBS-04

must_haves:
  truths:
    - "Observations accumulate across colonies, not just per-colony"
    - "Content hashing prevents duplicate entries"
    - "Observation count increases with each valid observation"
  artifacts:
    - path: ".aether/aether-utils.sh"
      provides: "learning-observe function"
      contains: "learning-observe)"
    - path: ".aether/data/learning-observations.json"
      provides: "Observation storage"
      contains: "observations"
  key_links:
    - from: ".aether/aether-utils.sh"
      to: ".aether/data/learning-observations.json"
      via: "file read/write"
      pattern: "learning-observations\\.json"
---

<objective>
Create learning-observe function to record observations of learnings across colonies.

Purpose: Track how many times each learning is observed/used to determine when it meets promotion thresholds.
Output: learning-observe function in aether-utils.sh and learning-observations.json storage file.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md

# Phase 32 context (what was built)
@.planning/phases/32-wire-queen-md-into-commands/32-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create learning-observe function in aether-utils.sh</name>
  <files>.aether/aether-utils.sh</files>
  <action>
    Add learning-observe function to aether-utils.sh after queen-promote (around line 3700).

    The function should:
    1. Accept arguments: <content> <wisdom_type> [colony_name]
    2. Generate SHA256 hash of content for deduplication
    3. Read existing .aether/data/learning-observations.json (create if doesn't exist)
    4. Check if observation with same hash already exists:
       - If exists: increment observation_count, update last_seen, add colony to colonies list if not present
       - If new: create new observation entry with observation_count=1
    5. Write updated observations back to JSON file
    6. Return JSON with observation_count, threshold status, and colonies list

    Use jq for JSON manipulation. Handle file locking for concurrent access.
    Store observations in .aether/data/learning-observations.json format:
    {
      "observations": [
        {
          "content_hash": "sha256:abc123...",
          "content": "learning text",
          "wisdom_type": "pattern",
          "observation_count": 3,
          "first_seen": "2026-02-15T10:00:00Z",
          "last_seen": "2026-02-20T15:30:00Z",
          "colonies": ["colony-a", "colony-b"]
        }
      ]
    }
  </action>
  <verify>bash .aether/aether-utils.sh learning-observe "Always validate inputs" "pattern" "test-colony"</verify>
  <done>learning-observe function exists, records observations with content hashing, accumulates across colonies</done>
</task>

<task type="auto">
  <name>Task 2: Test learning-observe with multiple colonies</name>
  <files>.aether/data/learning-observations.json</files>
  <action>
    Test the learning-observe function:
    1. Run observation with "test-colony" - should create new entry with count 1
    2. Run same content with "different-colony" - should increment count to 2, add colony to list
    3. Run same content again with "test-colony" - should increment count to 3, not duplicate colony
    4. Verify JSON structure is correct with jq
  </action>
  <verify>cat .aether/data/learning-observations.json | jq '.observations[0].observation_count'</verify>
  <done>Multiple colonies can contribute to same observation, count accumulates correctly</done>
</task>

</tasks>

<verification>
- learning-observe function is callable from CLI
- JSON file created in correct location
- Hash deduplication works (same content = same hash)
- Colony accumulation works (different colonies add to list)
- Count increments correctly
</verification>

<success_criteria>
- learning-observe function added to aether-utils.sh
- learning-observations.json file created and updated correctly
- Content hashing prevents duplicates (OBS-04)
- Cross-colony accumulation works (OBS-03)
</success_criteria>

<output>
After completion, create `.planning/phases/33-add-promotion-proposals/33-01-SUMMARY.md`
</output>
