---
phase: 33-add-promotion-proposals
plan: "02"
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - .aether/aether-utils.sh
autonomous: true
requirements:
  - OBS-02
  - META-01

must_haves:
  truths:
    - "System proposes learnings meeting promotion thresholds"
    - "Thresholds vary by wisdom type (philosophy:5, pattern:3, redirect:2, stack:1, decree:0)"
    - "Proposals include observation count and contributing colonies"
  artifacts:
    - path: ".aether/aether-utils.sh"
      provides: "learning-check-promotion function"
      contains: "learning-check-promotion)"
  key_links:
    - from: ".aether/aether-utils.sh"
      to: ".aether/data/learning-observations.json"
      via: "file read"
      pattern: "learning-observations\\.json"
---

<objective>
Create learning-check-promotion function to check which learnings meet promotion thresholds.

Purpose: Determine which observations have accumulated enough evidence to be promoted to QUEEN.md wisdom.
Output: learning-check-promotion function in aether-utils.sh.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# learning-observe builds on Plan 33-01
@.planning/phases/33-add-promotion-proposals/33-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create learning-check-promotion function</name>
  <files>.aether/aether-utils.sh</files>
  <action>
    Add learning-check-promotion function to aether-utils.sh after learning-observe.

    The function should:
    1. Accept optional argument: path to learning-observations.json (default: .aether/data/learning-observations.json)
    2. Define promotion thresholds (META-01):
       - philosophy: 5 observations
       - pattern: 3 observations
       - redirect: 2 observations
       - stack: 1 observation
       - decree: 0 observations (always eligible)
    3. Read learning-observations.json
    4. For each observation, check if observation_count >= threshold for its wisdom_type
    5. Return JSON array of proposals meeting thresholds:
       {
         "proposals": [
           {
             "content": "learning text",
             "wisdom_type": "pattern",
             "observation_count": 3,
             "threshold": 3,
             "colonies": ["colony-a", "colony-b"],
             "ready": true
           }
         ]
       }
    6. If no observations meet thresholds, return empty proposals array
  </action>
  <verify>bash .aether/aether-utils.sh learning-check-promotion</verify>
  <done>learning-check-promotion returns proposals meeting thresholds</done>
</task>

<task type="auto">
  <name>Task 2: Test threshold checking</name>
  <files>.aether/data/learning-observations.json</files>
  <action>
    Test the threshold checking:
    1. Add observations with different counts (1, 2, 3, 5)
    2. Run learning-check-promotion
    3. Verify only observations meeting thresholds are returned
    4. Test with wisdom_type requiring 5 (philosophy), verify count 3 doesn't qualify
    5. Test with wisdom_type requiring 1 (stack), verify count 1 qualifies
  </action>
  <verify>bash .aether/aether-utils.sh learning-check-promotion | jq '.proposals | length'</verify>
  <done>Threshold checking works correctly for all wisdom types</done>
</task>

</tasks>

<verification>
- learning-check-promotion is callable from CLI
- Correct thresholds applied per wisdom type
- Returns proper JSON structure with proposals
- Handles edge cases (no observations, no thresholds met)
</verification>

<success_criteria>
- learning-check-promotion function added to aether-utils.sh
- Correctly identifies observations meeting thresholds (META-01)
- Returns proposals in correct format (OBS-02)
</success_criteria>

<output>
After completion, create `.planning/phases/33-add-promotion-proposals/33-02-SUMMARY.md`
</output>
