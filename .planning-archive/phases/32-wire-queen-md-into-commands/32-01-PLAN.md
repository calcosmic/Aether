---
phase: 32-wire-queen-md-into-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/aether-utils.sh
autonomous: true
requirements:
  - PRIME-01
  - PRIME-02
  - PRIME-03
  - QUEEN-01
  - QUEEN-03
  - QUEEN-05

must_haves:
  truths:
    - "colony-prime() function exists in aether-utils.sh"
    - "colony-prime() combines wisdom + signals + instincts into single output"
    - "Two-level QUEEN.md loading works (global first, then local)"
    - "queen-read excludes metadata/evolution log from worker context"
  artifacts:
    - path: ".aether/aether-utils.sh"
      provides: "colony-prime() function"
      contains: "colony-prime)"
      min_lines: 50
  key_links:
    - from: ".aether/aether-utils.sh"
      to: "colony-prime"
      via: "function definition"
      pattern: "colony-prime\\)"
    - from: "colony-prime"
      to: "queen-read + pheromone-prime"
      via: "internal function calls"
      pattern: "queen-read.*pheromone-prime"
---

<objective>
Create unified colony-prime() function in aether-utils.sh that combines queen-read and pheromone-prime into a single call. Also implement two-level QUEEN.md loading (global ~/.aether/QUEEN.md first, then local .aether/QUEEN.md).
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-wire-queen-md-into-commands/32-CONTEXT.md
@.planning/phases/32-wire-queen-md-into-commands/32-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add colony-prime() function to aether-utils.sh</name>
  <files>.aether/aether-utils.sh</files>
  <action>
Create new function colony-prime() in aether-utils.sh that:
1. Calls queen-read to get wisdom (filtering out metadata and evolution log sections per user decision)
2. Calls pheromone-prime to get signals + instincts
3. Returns unified JSON with:
   - wisdom object (combined wisdom from two-level loading)
   - signals object (pheromone-prime output)
   - prompt_section (formatted markdown for worker injection)
4. Implements error handling per locked decisions:
   - If QUEEN.md missing: FAIL HARD with clear error message requiring /ant:init
   - If pheromones.json missing: silently continue with warning (don't block build)
5. Two-level loading: Load global ~/.aether/QUEEN.md first, then local .aether/QUEEN.md, combine wisdom sections

Reference existing functions: queen-read at line 3419, pheromone-prime at line 4460.
  </action>
  <verify>
grep -n "^colony-prime)" .aether/aether-utils.sh && echo "Function exists"
bash .aether/aether-utils.sh colony-prime 2>&1 | head -20
  </verify>
  <done>
colony-prime() function exists in aether-utils.sh, returns unified JSON with wisdom + signals + prompt_section, fails hard if QUEEN.md missing, warns but continues if pheromones.json missing
  </done>
</task>

<task type="auto">
  <name>Task 2: Update queen-read for two-level loading</name>
  <files>.aether/aether-utils.sh</files>
  <action>
Update queen-read function to implement two-level loading:
1. Global QUEEN.md loads first from ~/.aether/QUEEN.md
2. Local QUEEN.md loads second from .aether/docs/QUEEN.md (or .aether/QUEEN.md)
3. Combine wisdom sections: local entries append to global entries per category
4. Per locked decision: Exclude metadata and evolution_log sections from worker context (only categories: Philosophies, Patterns, Redirects, Stack Wisdom, Decrees)
5. Return metadata in JSON response but NOT in prompt_section
  </verify>
  <verify>
# Verify two-level loading works
AETHER_ROOT="$PWD" bash .aether/aether-utils.sh queen-read 2>&1 | jq -r '.wisdom.philosophies' | head -5
  </verify>
  <done>
queen-read loads global + local QUEEN.md, combines wisdom sections, excludes metadata/evolution from prompt_section
  </done>
</task>

</tasks>

<verification>
- colony-prime function exists and returns valid JSON
- Two-level loading returns combined wisdom from both global and local QUEEN.md
- Missing QUEEN.md produces hard failure with actionable error
- Missing pheromones.json produces warning but continues
</verification>

<success_criteria>
colony-prime() function implemented in aether-utils.sh, combines queen-read + pheromone-prime, implements two-level loading, error handling per locked decisions
</success_criteria>

<output>
After completion, create `.planning/phases/32-wire-queen-md-into-commands/32-01-SUMMARY.md`
</output>
