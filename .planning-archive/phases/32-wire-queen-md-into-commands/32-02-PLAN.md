---
phase: 32-wire-queen-md-into-commands
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - .claude/commands/ant/build.md
autonomous: true
requirements:
  - INT-02
  - PHER-EVOL-01
  - PHER-EVOL-04

must_haves:
  truths:
    - "build.md uses single colony-prime() call instead of 3 separate calls"
    - "Workers receive unified context (wisdom + pheromones + instincts)"
    - "No duplicate context injections"
  artifacts:
    - path: ".claude/commands/ant/build.md"
      provides: "Unified worker priming via colony-prime()"
      contains: "colony-prime"
  key_links:
    - from: ".claude/commands/ant/build.md"
      to: "colony-prime"
      via: "bash call"
      pattern: "colony-prime"
---

<objective>
Update build.md to use colony-prime() instead of three separate calls (pheromone-prime, queen-read, pheromone-read). Replace overlapping calls with single unified worker context injection.
</objective>

<context>
@.planning/phases/32-wire-queen-md-into-commands/32-CONTEXT.md
@.planning/phases/32-wire-queen-md-into-commands/32-RESEARCH.md
@.planning/phases/32-wire-queen-md-into-commands/32-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace three calls with colony-prime() in build.md</name>
  <files>.claude/commands/ant/build.md</files>
  <action>
Update build.md to use single colony-prime() call:
1. Find the three current calls:
   - Step 4: pheromone-prime (line ~244)
   - Step 4.1: queen-read (line ~309-313)
   - Step 4.1.6: pheromone-read (mentioned in research)
2. Replace with single call to colony-prime()
3. Update variable references:
   - Extract wisdom from colony-prime output for worker prompt
   - Extract pheromone_section from colony-prime output
   - Extract instincts from colony-prime output
4. Keep the unified output format from colony-prime (wisdom + signals + prompt_section)
5. Per locked decisions:
   - FAIL HARD if colony-prime fails due to missing QUEEN.md
   - WARN but continue if pheromones fail (pheromones.json missing)
6. Per user's discretion: Choose exact placement of colony-prime() call (after Load Constraints, before Territory Survey)
  </action>
  <verify>
grep -n "colony-prime" .claude/commands/ant/build.md
grep -c "pheromone-prime\|queen-read\|pheromone-read" .claude/commands/ant/build.md
  </verify>
  <done>
build.md uses single colony-prime() call, no duplicate calls remain, workers receive unified context
  </done>
</task>

</tasks>

<verification>
- build.md contains single colony-prime() call
- No remaining pheromone-prime, queen-read, or pheromone-read calls in build.md
- Worker prompt receives combined wisdom + pheromones + instincts
</verification>

<success_criteria>
build.md refactored to use colony-prime() for unified worker context, three separate calls consolidated into one
</success_criteria>

<output>
After completion, create `.planning/phases/32-wire-queen-md-into-commands/32-02-SUMMARY.md`
</output>
