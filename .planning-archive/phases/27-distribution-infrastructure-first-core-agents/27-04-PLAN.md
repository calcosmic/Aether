---
phase: 27-distribution-infrastructure-first-core-agents
plan: 04
type: execute
wave: 2
depends_on: ["27-01", "27-02", "27-03"]
files_modified: []
autonomous: false
requirements:
  - DIST-04
  - DIST-05
  - DIST-06

must_haves:
  truths:
    - "npm pack --dry-run shows .claude/agents/ant/aether-builder.md and .claude/agents/ant/aether-watcher.md in the package"
    - "npm pack --dry-run does NOT show any gsd-*.md files from .claude/agents/"
    - "npm install -g . succeeds and populates ~/.aether/system/agents-claude/ with both agent files"
    - "aether update in a target repo delivers both agents to .claude/agents/ant/"
  artifacts: []
  key_links:
    - from: "npm pack"
      to: ".claude/agents/ant/"
      via: "package.json files array scoping"
      pattern: "agents/ant/aether-"
    - from: "npm install -g ."
      to: "~/.aether/system/agents-claude/"
      via: "setupHub() syncDirWithCleanup"
      pattern: "aether-builder.md.*aether-watcher.md"
---

<objective>
Verify the complete distribution chain end-to-end — from npm packaging through hub sync to target repo delivery.

Purpose: This is the proof that the pipeline works. Phase 27's success criteria require observable evidence that agents flow from source to hub to target. This plan runs the actual commands and verifies the results.

Output: Verified distribution chain. No files created — this is a verification-only plan.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-distribution-infrastructure-first-core-agents/27-01-SUMMARY.md
@.planning/phases/27-distribution-infrastructure-first-core-agents/27-02-SUMMARY.md
@.planning/phases/27-distribution-infrastructure-first-core-agents/27-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify npm packaging includes ant agents and excludes GSD agents</name>
  <files></files>
  <action>
Run `npm pack --dry-run` and examine the output.

**Verify INCLUDED (DIST-04):**
- `.claude/agents/ant/aether-builder.md` appears in the package listing
- `.claude/agents/ant/aether-watcher.md` appears in the package listing

**Verify EXCLUDED (DIST-01 — GSD isolation):**
- No `gsd-planner.md`, `gsd-executor.md`, or any other `gsd-*.md` file appears in the listing
- No files from `.claude/agents/` (parent directory) appear — only `.claude/agents/ant/` subdirectory

**If verification fails:**
- Missing ant agents: Check `package.json` files array for `.claude/agents/ant/` entry
- GSD agents included: The `files` array may have `.claude/agents/` instead of `.claude/agents/ant/` — fix scoping
  </action>
  <verify>
`npm pack --dry-run 2>&1 | grep "agents/ant"` — should show both aether-builder.md and aether-watcher.md.
`npm pack --dry-run 2>&1 | grep "gsd-"` — should show zero results.
  </verify>
  <done>
npm pack --dry-run confirms both ant agents are included and zero GSD agents are included. Package scoping is correct.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify hub population and run all tests</name>
  <files></files>
  <action>
**Step 1 — Hub population (DIST-05):**
Run `npm install -g .` to trigger setupHub(). This validates the package and pushes to hub.

After install completes, verify:
- `ls ~/.aether/system/agents-claude/` — should list `aether-builder.md` and `aether-watcher.md`
- Both files should have content (non-empty): `wc -l ~/.aether/system/agents-claude/aether-builder.md`

**Step 2 — Idempotency (DIST-08):**
Run `npm install -g .` a second time. The output should show "0 files" or "unchanged" for the agents-claude sync line — no files re-copied when nothing changed.

**Step 3 — Full test suite:**
Run `npm test` to verify all existing tests pass with the new code.

**If verification fails:**
- Hub dir missing: Check cli.js setupHub() mkdir and sync blocks
- Files not synced: Check the `claudeAgentsSrc` path matches actual file location
- Tests fail: Fix regressions introduced by the pipeline changes
  </action>
  <verify>
`ls ~/.aether/system/agents-claude/` — lists aether-builder.md and aether-watcher.md.
`npm test` — all tests pass.
  </verify>
  <done>
Hub at `~/.aether/system/agents-claude/` contains both agent files. Second install shows idempotent behavior (no unnecessary re-copies). All tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify agents load in Claude Code and deliver to target repo</name>
  <action>
This is a human verification checkpoint. The automated pipeline verification is complete. The user must confirm agents load correctly in Claude Code (which cannot be verified via bash).

**What was verified automatically:**
1. `npm pack --dry-run` — ant agents included, GSD agents excluded
2. `npm install -g .` — hub populated with both agents at `~/.aether/system/agents-claude/`
3. Idempotency — second install skips unchanged files
4. All tests pass

**User verification steps:**

1. Open a new Claude Code session (or restart current one — agents load at session start)
2. Run `/agents` command
3. Confirm both agents appear in the list:
   - `aether-builder` — with description mentioning implementation, TDD, 3-Fix Rule
   - `aether-watcher` — with description mentioning validation, testing, quality gates
4. Both should show their explicit tools (Builder: Read/Write/Edit/Bash/Grep/Glob; Watcher: Read/Bash/Grep/Glob)

**If agents do NOT appear:**
- Check for YAML malformation: `head -6 .claude/agents/ant/aether-builder.md` — frontmatter must be valid YAML
- Common issue: unescaped colons in description field — descriptions must be quoted

**Required — verify delivery to a target repo (DIST-06):**
1. Navigate to any repo with `.aether/` directory (not the Aether repo itself)
2. Run `aether update`
3. Check `ls .claude/agents/ant/` — should show both `aether-builder.md` and `aether-watcher.md`
4. Verify the `aether update` output includes claude agent file counts in the summary line
5. If no other repo is available, create a temporary test directory: `mkdir /tmp/aether-test && cd /tmp/aether-test && aether init && ls .claude/agents/ant/`
  </action>
  <verify>User confirms agents appear in `/agents` output AND both agents exist at `.claude/agents/ant/` in a target repo after `aether update`.</verify>
  <done>Both aether-builder and aether-watcher appear in `/agents` output with correct descriptions and tool sets. Both agents are delivered to `.claude/agents/ant/` in a target repo via `aether update` (DIST-06 proven). Distribution chain is proven end-to-end.</done>
</task>

</tasks>

<verification>
1. `npm pack --dry-run` confirms correct packaging (ant agents in, GSD agents out)
2. `~/.aether/system/agents-claude/` contains aether-builder.md and aether-watcher.md
3. Second `npm install -g .` is idempotent (no unnecessary copies)
4. `/agents` command in Claude Code shows both aether-builder and aether-watcher
5. `npm test` — all tests pass
</verification>

<success_criteria>
The complete distribution chain is proven end-to-end:
1. npm packaging includes ant agents and excludes GSD agents (DIST-04)
2. npm install populates hub (DIST-05)
3. aether update delivers both agents to .claude/agents/ant/ in a target repo (DIST-06)
4. Agents load in Claude Code (observable via /agents)
5. All tests pass
User has confirmed agents appear in /agents output AND both agents exist in a target repo after aether update.
</success_criteria>

<output>
After completion, create `.planning/phases/27-distribution-infrastructure-first-core-agents/27-04-SUMMARY.md`
</output>
