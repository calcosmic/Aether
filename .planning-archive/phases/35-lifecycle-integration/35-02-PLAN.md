---
phase: 35-lifecycle-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/ant/entomb.md
autonomous: true
requirements:
  - INT-05
must_haves:
  truths:
    - entomb.md prompts user to review wisdom proposals before archiving
    - entomb.md blocks ceremony progression until wisdom is approved/deferred
    - entomb.md shows "No wisdom proposals to review" when empty
    - entomb.md uses same approval UI as continue.md (checkboxes, threshold bars)
  artifacts:
    - path: .claude/commands/ant/entomb.md
      provides: "Entomb command with integrated wisdom approval workflow"
      contains: "Step 3.5: Wisdom Approval"
  key_links:
    - from: entomb.md Step 3.5
      to: aether-utils.sh learning-approve-proposals
      via: bash command invocation
      pattern: "learning-approve-proposals"
---

<objective>
Integrate the wisdom approval workflow into entomb.md so users must review and approve wisdom proposals before the colony is archived.

Purpose: Final wisdom review at the archival boundary ensures valuable learnings are preserved in QUEEN.md.
Output: Updated entomb.md with Step 3.5 wisdom approval blocking step.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-lifecycle-integration/35-CONTEXT.md
@.planning/phases/35-lifecycle-integration/35-RESEARCH.md
@.claude/commands/ant/entomb.md
@.claude/commands/ant/seal.md (reference for parallel implementation)

## Current entomb.md Structure
- Step 3: User Confirmation (user confirms entombment)
- Step 3.5: Check XML Tools
- Step 3.75: (not used)
- Step 4: Promote Wisdom to QUEEN.md (auto-promotion without approval)
- Step 5: Generate Chamber Name
- Step 6: Create Chamber

## Integration Point
Add Step 3.25 (or renumber) between Step 3 (confirmation) and Step 3.5 (XML tools).
The existing Step 4 auto-promotion becomes redundant and should be removed/simplified.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 3.25 Wisdom Approval to entomb.md</name>
  <files>.claude/commands/ant/entomb.md</files>
  <action>
Add Step 3.25: Wisdom Approval between Step 3 (User Confirmation) and Step 3.5 (Check XML Tools).

The new step should:
1. Check for pending proposals using `learning-check-promotion`
2. If proposals exist (>0), display the wisdom review header and call `learning-approve-proposals`
3. If no proposals exist, show "No wisdom proposals to review." and continue
4. Block progression until approval workflow completes

Insert this after Step 3's confirmation handling (after the "Stop here" if user doesn't confirm):

```
### Step 3.25: Wisdom Approval

Before archiving, review wisdom proposals accumulated during this colony's lifecycle.

```bash
# Check for pending proposals
proposals=$(bash .aether/aether-utils.sh learning-check-promotion 2>/dev/null || echo '{"proposals":[]}')
proposal_count=$(echo "$proposals" | jq '.proposals | length')

if [[ "$proposal_count" -gt 0 ]]; then
  echo ""
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo "   🧠 FINAL WISDOM REVIEW"
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo ""
  echo "Review wisdom proposals before archiving this colony."
  echo "Approved proposals will be promoted to QUEEN.md."
  echo ""

  # Run approval workflow (blocking)
  bash .aether/aether-utils.sh learning-approve-proposals

  echo ""
  echo "Wisdom review complete. Proceeding with entombment..."
  echo ""
else
  echo "No wisdom proposals to review."
fi
```

Use decimal step number (3.25) to fit between Step 3 and Step 3.5 without renumbering.
  </action>
  <verify>grep -A 30 "Step 3.25: Wisdom Approval" .claude/commands/ant/entomb.md</verify>
  <done>Step 3.25 exists with learning-check-promotion call and conditional display logic</done>
</task>

<task type="auto">
  <name>Task 2: Simplify existing Step 4 auto-promotion</name>
  <files>.claude/commands/ant/entomb.md</files>
  <action>
The existing Step 4 auto-promotes validated learnings without user approval. Since Step 3.25 now handles approval via learning-approve-proposals, the existing Step 4 logic is redundant.

Replace the existing Step 4 with a simplified version that:
1. Removes the manual extraction and promotion loops (the while read loops)
2. Keeps only the QUEEN.md initialization check (Step 4.1)
3. Updates the header to reflect that wisdom was already handled

The simplified Step 4 should be:
```
### Step 4: Ensure QUEEN.md Exists

Verify QUEEN.md is initialized for wisdom storage:

```bash
queen_file=".aether/docs/QUEEN.md"
if [[ ! -f "$queen_file" ]]; then
  init_result=$(bash .aether/aether-utils.sh queen-init 2>/dev/null || echo '{"ok":false}')
  init_ok=$(echo "$init_result" | jq -r '.ok // false')
  if [[ "$init_ok" == "true" ]]; then
    created=$(echo "$init_result" | jq -r '.result.created // false')
    if [[ "$created" == "true" ]]; then
      bash .aether/aether-utils.sh activity-log "CREATED" "Queen" "Initialized QUEEN.md for wisdom storage"
    fi
  fi
fi
```
```

Remove the promotion logic (Step 4.2 and 4.3) since learning-approve-proposals handles this.
  </action>
  <verify>grep -A 10 "Step 4:" .claude/commands/ant/entomb.md | head -15</verify>
  <done>Step 4 is simplified to just QUEEN.md initialization, promotion loops removed</done>
</task>

<task type="auto">
  <name>Task 3: Update step references in entomb.md</name>
  <files>.claude/commands/ant/entomb.md</files>
  <action>
Update any internal step references in entomb.md to account for the new Step 3.25:

1. Find any references to "Step 4" that now refer to QUEEN.md init only
2. Find any references to "Step 5" that now refer to chamber name generation
3. Update the references to use the new step numbers

Specifically:
- "Step 5" (generate chamber name) becomes "Step 5" (unchanged)
- "Step 6" (create chamber) becomes "Step 6" (unchanged)
- "Step 7" (archive files) becomes "Step 7" (unchanged)
- "Step 7.5" (XML export) becomes "Step 7.5" (unchanged)
- "Step 8" (verify) becomes "Step 8" (unchanged)
- "Step 9" (eternal memory) becomes "Step 9" (unchanged)
- "Step 10" (reset state) becomes "Step 10" (unchanged)
- "Step 11" (handoff) becomes "Step 11" (unchanged)
- "Step 12" (display) becomes "Step 12" (unchanged)

The main change is that Step 4 no longer has substeps (4.1, 4.2, 4.3) since promotion is handled in Step 3.25.

Use grep to find all "Step [0-9]" references and ensure consistency.
  </action>
  <verify>grep -n "Step [0-9]" .claude/commands/ant/entomb.md | grep -v "Step 3\.[0-9]" | head -20</verify>
  <done>All step references are consistent, no orphaned references to removed substeps</done>
</task>

</tasks>

<verification>
After completing this plan:
1. Read entomb.md to verify Step 3.25 exists with proper wisdom approval flow
2. Verify learning-approve-proposals is called correctly
3. Verify empty state handling shows "No wisdom proposals to review"
4. Verify existing auto-promotion was simplified
5. Verify step numbering is consistent throughout
</verification>

<success_criteria>
- entomb.md contains Step 3.25: Wisdom Approval with learning-approve-proposals call
- Step 3.25 is positioned between Step 3 (confirmation) and Step 3.5 (XML tools)
- Empty state shows "No wisdom proposals to review" and continues
- Existing auto-promotion simplified to avoid duplication
- All step references updated consistently
</success_criteria>

<output>
After completion, create `.planning/phases/35-lifecycle-integration/35-02-SUMMARY.md`
</output>
