---
phase: 35-lifecycle-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/ant/seal.md
autonomous: true
requirements:
  - INT-04
must_haves:
  truths:
    - seal.md prompts user to review wisdom proposals before sealing
    - seal.md blocks ceremony progression until wisdom is approved/deferred
    - seal.md shows "No wisdom proposals to review" when empty
    - seal.md uses same approval UI as continue.md (checkboxes, threshold bars)
  artifacts:
    - path: .claude/commands/ant/seal.md
      provides: "Seal command with integrated wisdom approval workflow"
      contains: "Step 3.5: Wisdom Approval"
  key_links:
    - from: seal.md Step 3.5
      to: aether-utils.sh learning-approve-proposals
      via: bash command invocation
      pattern: "learning-approve-proposals"
---

<objective>
Integrate the wisdom approval workflow into seal.md so users must review and approve wisdom proposals before the colony is sealed.

Purpose: Wisdom is permanent — users should validate what gets promoted to QUEEN.md at this final lifecycle boundary.
Output: Updated seal.md with Step 3.5 wisdom approval blocking step.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-lifecycle-integration/35-CONTEXT.md
@.planning/phases/35-lifecycle-integration/35-RESEARCH.md
@.claude/commands/ant/seal.md
@.claude/commands/ant/continue.md (reference for approval UX pattern)

## Current seal.md Structure
- Step 3: Confirmation (user confirms seal)
- Step 4: Promote Colony Wisdom (auto-promotion without approval)
- Step 5: Update Milestone to Crowned Anthill
- Step 6: Write CROWNED-ANTHILL.md

## Integration Point
Add Step 3.5 between Step 3 (confirmation) and Step 4 (existing auto-promotion).
The existing Step 4 auto-promotion becomes redundant and should be removed/simplified.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 3.5 Wisdom Approval to seal.md</name>
  <files>.claude/commands/ant/seal.md</files>
  <action>
Add Step 3.5: Wisdom Approval between Step 3 (Confirmation) and Step 4 (Promote Colony Wisdom).

The new step should:
1. Check for pending proposals using `learning-check-promotion`
2. If proposals exist (>0), display the wisdom review header and call `learning-approve-proposals`
3. If no proposals exist, show "No wisdom proposals to review." and continue
4. Block progression until approval workflow completes

Insert this after Step 3's confirmation handling (after the "Stop here" if user doesn't confirm):

```
### Step 3.5: Wisdom Approval

Before sealing, review wisdom proposals accumulated during this colony's lifecycle.

```bash
# Check for pending proposals
proposals=$(bash .aether/aether-utils.sh learning-check-promotion 2>/dev/null || echo '{"proposals":[]}')
proposal_count=$(echo "$proposals" | jq '.proposals | length')

if [[ "$proposal_count" -gt 0 ]]; then
  echo ""
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo "   🧠 WISDOM REVIEW"
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo ""
  echo "Review wisdom proposals before sealing this colony."
  echo "Approved proposals will be promoted to QUEEN.md."
  echo ""

  # Run approval workflow (blocking)
  bash .aether/aether-utils.sh learning-approve-proposals

  echo ""
  echo "Wisdom review complete. Proceeding with sealing ceremony..."
  echo ""
else
  echo "No wisdom proposals to review."
fi
```

Use decimal step number (3.5) to avoid renumbering existing steps.
  </action>
  <verify>grep -A 30 "Step 3.5: Wisdom Approval" .claude/commands/ant/seal.md</verify>
  <done>Step 3.5 exists with learning-check-promotion call and conditional display logic</done>
</task>

<task type="auto">
  <name>Task 2: Simplify existing Step 4 auto-promotion</name>
  <files>.claude/commands/ant/seal.md</files>
  <action>
The existing Step 4 auto-promotes validated learnings without user approval. Since Step 3.5 now handles approval via learning-approve-proposals, the existing Step 4 logic is redundant.

Replace the existing Step 4 with a simplified version that:
1. Removes the manual extraction and promotion loops
2. Keeps only the activity log entry noting that seal completed
3. References that wisdom was already handled in Step 3.5

The simplified Step 4 should be:
```
### Step 4: Log Seal Activity

Log the seal ceremony to activity log:
```bash
bash .aether/aether-utils.sh activity-log "MODIFIED" "Queen" "Colony sealed - wisdom review completed"
```
```

This maintains the step numbering while removing redundant promotion logic.
  </action>
  <verify>grep -A 5 "Step 4:" .claude/commands/ant/seal.md | head -10</verify>
  <done>Step 4 is simplified to just activity logging, manual promotion loops removed</done>
</task>

<task type="auto">
  <name>Task 3: Update step references in seal.md</name>
  <files>.claude/commands/ant/seal.md</files>
  <action>
Update any internal step references in seal.md to account for the new Step 3.5:

1. Find any references to "Step 4" that now refer to milestone update (was Step 5)
2. Find any references to "Step 5" that now refer to writing CROWNED-ANTHILL.md (was Step 6)
3. Update the references to use the new step numbers

Specifically:
- "Step 4" (milestone update) becomes "Step 5"
- "Step 5" (write document) becomes "Step 6"
- "Step 6" (XML export) becomes "Step 6.5"
- "Step 6.5" (display) becomes "Step 7"

Use grep to find all "Step [0-9]" references and update them appropriately.
  </action>
  <verify>grep -n "Step [0-9]" .claude/commands/ant/seal.md | grep -v "Step 3.5" | grep -v "Step [0-9]\.[0-9]"</verify>
  <done>All step references updated to match new numbering</done>
</task>

</tasks>

<verification>
After completing this plan:
1. Read seal.md to verify Step 3.5 exists with proper wisdom approval flow
2. Verify learning-approve-proposals is called correctly
3. Verify empty state handling shows "No wisdom proposals to review"
4. Verify existing auto-promotion was simplified
5. Verify step numbering is consistent throughout
</verification>

<success_criteria>
- seal.md contains Step 3.5: Wisdom Approval with learning-approve-proposals call
- Step 3.5 is positioned between Step 3 (confirmation) and Step 5 (milestone update)
- Empty state shows "No wisdom proposals to review" and continues
- Existing auto-promotion simplified to avoid duplication
- All step references updated consistently
</success_criteria>

<output>
After completion, create `.planning/phases/35-lifecycle-integration/35-01-SUMMARY.md`
</output>
