---
phase: 26-file-audit
plan: 04
type: execute
wave: 2
depends_on:
  - 26-01
  - 26-02
  - 26-03
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - CLEAN-08
  - CLEAN-09
  - CLEAN-10

must_haves:
  truths:
    - "npm pack --dry-run succeeds and shows reduced file count"
    - "npm install -g . succeeds (validates .aether/ via validate-package.sh)"
    - "npm test passes with no new failures beyond pre-existing baseline"
    - "npm run lint:sync passes"
    - "3-5 key slash commands spot-checked and still contain valid content"
    - "README.md updated to reflect post-cleanup repo structure"
  artifacts:
    - path: "README.md"
      provides: "Updated README reflecting simplified repo structure"
    - path: "CLAUDE.md"
      provides: "Updated references (if any) to deleted files/directories"
  key_links:
    - from: "bin/validate-package.sh"
      to: ".aether/"
      via: "REQUIRED_FILES validation"
      pattern: "REQUIRED_FILES"
    - from: "npm run lint:sync"
      to: ".claude/commands/ant/"
      via: "command sync verification"
      pattern: "lint:sync"
---

<objective>
Verify all cleanup is safe and update documentation to reflect the new simplified structure.

Purpose: After three plans of file deletions, this plan verifies nothing is broken and updates user-facing documentation. This is the safety net — full test suite, package validation, command spot-checks, and documentation updates.

Output: Verified clean repo with updated README.md. All CLEAN-08, CLEAN-09, CLEAN-10 requirements satisfied.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-file-audit/26-RESEARCH.md
@.planning/phases/26-file-audit/26-01-SUMMARY.md
@.planning/phases/26-file-audit/26-02-SUMMARY.md
@.planning/phases/26-file-audit/26-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full verification suite — tests, packaging, and command spot-checks</name>
  <files></files>
  <action>
    Run comprehensive verification to confirm nothing is broken:

    **Step 1: Package validation**
    ```
    npm pack --dry-run
    ```
    Verify the output shows a reduced file count compared to the ~206 files that were there before cleanup. Specifically confirm:
    - No deleted files appear in the list
    - `.aether/docs/` shows only ~13 files (down from 35+)
    - All REQUIRED_FILES are present

    **Step 2: Install validation**
    ```
    npm install -g .
    ```
    This runs `bin/validate-package.sh` as a preinstall hook and verifies all REQUIRED_FILES exist. If this succeeds, packaging is correct.

    **Step 3: Full test suite**
    ```
    npm test
    ```
    Verify no NEW failures. Pre-existing baseline: 2 validate-state test failures are known.

    **Step 4: Command sync verification**
    ```
    npm run lint:sync
    ```
    Verify sync passes. Pre-existing content drift (10+ files) is known — only check for structural sync (same commands exist in both Claude Code and OpenCode directories).

    **Step 5: Spot-check 3-5 key slash commands**
    Read the first 5 lines of each to verify they're intact:
    - `.claude/commands/ant/build.md` — core build command
    - `.claude/commands/ant/init.md` — core init command
    - `.claude/commands/ant/seal.md` — core lifecycle command
    - `.claude/commands/ant/oracle.md` — advanced command
    - `.claude/commands/ant/resume.md` — context persistence command

    If any command file is empty or missing, that's a NEW failure to investigate.

    **Step 6: Verify CLEAN-02 and CLEAN-03 documentation**
    Confirm `.aether/agents/` and `.aether/commands/` do not exist. These requirements were pre-satisfied but should be formally verified.

    If ANY verification step reveals a NEW failure caused by the cleanup:
    1. Identify which deleted file caused the failure
    2. Decide per-case: fix the reference (if it's a stale reference in code) OR revert the deletion (if the file was actually needed)
    3. Document the decision in the summary
  </action>
  <verify>
    - `npm pack --dry-run` exits 0
    - `npm install -g .` exits 0
    - `npm test` has no new failures
    - `npm run lint:sync` exits 0
    - All 5 spot-checked commands exist and have content
  </verify>
  <done>
    Full verification suite passes. No cleanup-induced failures. CLEAN-08 and CLEAN-09 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md and CLAUDE.md to reflect post-cleanup structure</name>
  <files>
    README.md
    CLAUDE.md
  </files>
  <action>
    **README.md updates:**

    Read `README.md` and update any references to directories or files that no longer exist after cleanup:
    - Remove or update references to `docs/` directory (no longer exists)
    - Remove or update references to `docs/plans/` (deleted)
    - Remove or update references to `docs/worktree-salvage/` (deleted)
    - If there's a directory structure diagram, update it to show current state
    - If there are references to `.aether/agents/` or `.aether/commands/`, remove them (these are dead)
    - Keep the README focused on what currently exists and how to use Aether

    **CLAUDE.md updates:**

    Read `CLAUDE.md` and check for references to deleted files or directories:
    - Check the Key Directories table — remove any rows for deleted directories
    - Check file references — update any paths that point to deleted files
    - Do NOT change the fundamental structure or instructions — only update stale references

    Be conservative: only change references to things that were actually deleted. Do not rewrite sections unnecessarily.
  </action>
  <verify>
    - `grep -n "docs/plans" README.md CLAUDE.md` — no references to deleted docs/plans/
    - `grep -n "worktree-salvage" README.md CLAUDE.md` — no references to deleted worktree-salvage
    - `grep -n "\.aether/agents/" README.md CLAUDE.md` — no references to non-existent .aether/agents/
    - `grep -n "\.aether/commands/" README.md CLAUDE.md` — no references to non-existent .aether/commands/
    - README.md accurately describes current repo structure
    - CLAUDE.md references only existing files and directories
  </verify>
  <done>
    README.md and CLAUDE.md updated to reflect post-cleanup repo structure. No references to deleted files remain. CLEAN-10 satisfied.
  </done>
</task>

</tasks>

<verification>
- All CLEAN-08 checks pass (tests, lint:sync, command spot-checks)
- CLEAN-09 verified (npm pack + npm install -g . both succeed)
- CLEAN-10 verified (README.md and CLAUDE.md updated, no stale references)
- No cleanup-induced failures in any verification step
</verification>

<success_criteria>
Full verification suite passes with no new failures. README.md and CLAUDE.md updated to reflect the simplified repo. All 10 CLEAN requirements across the phase are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/26-file-audit/26-04-SUMMARY.md`
</output>
