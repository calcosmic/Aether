---
phase: 26-file-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "Aether Notes"
  - aether-logo.png
  - logo_block.txt
  - logo_block_color.txt
  - planning/
  - .cursor/
  - .worktrees/
  - .aether/workers-new-castes.md
  - .aether/recover.sh
  - .aether/HANDOFF_AETHER_DEV_2026-02-15.md
  - .aether/PHASE-0-ANALYSIS.md
  - .aether/RESEARCH-SHARED-DATA.md
  - .aether/diagnose-self-reference.md
  - .aether/DIAGNOSIS_PROMPT.md
  - .aether/pheromone_system.py
  - .aether/semantic_layer.py
  - .aether/__pycache__/
  - .aether/examples/
  - .claude/commands/gsd/new-project.md.bak
  - .opencode/agents/workers.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-07

must_haves:
  truths:
    - "No dead files remain in repo root (logo files, Aether Notes, empty planning/, .cursor/)"
    - "No dead files remain in .aether/ root (workers-new-castes.md, recover.sh, debugging artifacts, Python prototypes, examples/)"
    - "No dead duplicates in .claude/ or .opencode/ (.bak file removed, stale workers.md removed)"
    - "CLEAN-02 and CLEAN-03 confirmed already done (directories do not exist)"
    - "npm pack --dry-run succeeds without errors"
    - "npm test passes with no new failures beyond pre-existing baseline"
  artifacts:
    - path: ".aether/"
      provides: "Clean .aether/ root with only active system files"
    - path: ".claude/commands/gsd/"
      provides: "No .bak files"
    - path: ".opencode/agents/"
      provides: "No stale workers.md duplicate"
  key_links:
    - from: "bin/validate-package.sh"
      to: ".aether/"
      via: "REQUIRED_FILES check"
      pattern: "REQUIRED_FILES"
---

<objective>
Delete dead files from repo root, .aether/ root, .claude/, and .opencode/ directories.

Purpose: Remove dead weight that clutters the repo — old debugging artifacts, Python prototypes, logo files, empty directories, stale duplicates. All LOW risk since these files are either excluded from npm via .npmignore or are clearly superseded.

Output: Clean repo root and .aether/ root with only actively-used files remaining.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-file-audit/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete repo root and .aether/ root dead files</name>
  <files>
    "Aether Notes"
    aether-logo.png
    logo_block.txt
    logo_block_color.txt
    planning/
    .cursor/
    .aether/workers-new-castes.md
    .aether/recover.sh
    .aether/HANDOFF_AETHER_DEV_2026-02-15.md
    .aether/PHASE-0-ANALYSIS.md
    .aether/RESEARCH-SHARED-DATA.md
    .aether/diagnose-self-reference.md
    .aether/DIAGNOSIS_PROMPT.md
    .aether/pheromone_system.py
    .aether/semantic_layer.py
    .aether/__pycache__/
    .aether/examples/
  </files>
  <action>
    Delete all dead files in two groups:

    **Repo root dead files (all excluded from npm package):**
    - `"Aether Notes"` — macOS alias/bookmark binary file, no code purpose
    - `aether-logo.png` — 1.2MB image, excluded by .npmignore
    - `logo_block.txt` — ASCII art, excluded by .npmignore
    - `logo_block_color.txt` — ASCII art, excluded by .npmignore
    - `planning/` — empty root-level directory (not `.planning/`), contains only `_meta/` with a README
    - `.cursor/` — Cursor IDE config with only `worktrees.json`, not for git
    - `.worktrees/` — worktree tracking data, excluded by .gitignore, stale

    **`.aether/` root dead files (all in .npmignore or non-required):**
    - `workers-new-castes.md` — old draft superseded by current workers.md. Also check if `bin/cli.js` has a reference to this file in a migration path; if so, remove that reference since it's a one-time migration that's long since completed.
    - `recover.sh` — old recovery script, not in REQUIRED_FILES or update allowlist
    - `HANDOFF_AETHER_DEV_2026-02-15.md` — dated handoff doc, in .npmignore
    - `PHASE-0-ANALYSIS.md` — old analysis, in .npmignore
    - `RESEARCH-SHARED-DATA.md` — old research artifact, in .npmignore
    - `diagnose-self-reference.md` — debugging artifact, in .npmignore
    - `DIAGNOSIS_PROMPT.md` — debugging artifact, in .npmignore
    - `pheromone_system.py` — Python prototype, superseded by shell implementation
    - `semantic_layer.py` — Python prototype, superseded by shell implementation
    - `__pycache__/` — Python bytecode cache
    - `examples/` — contains only stale `worker-priming.xml`, excluded by .npmignore

    Use `git rm` for tracked files, `rm -rf` for untracked/gitignored files.

    **Confirm CLEAN-02 and CLEAN-03:** Verify `.aether/agents/` and `.aether/commands/` directories do not exist. Document this in the summary — these requirements are already satisfied.

    After deletion, run:
    1. `npm pack --dry-run 2>&1 | tail -20` — verify package still builds
    2. `npm test` — verify no new test failures (2 pre-existing validate-state failures are baseline)
  </action>
  <verify>
    - `ls "Aether Notes" aether-logo.png logo_block.txt logo_block_color.txt 2>&1` shows "No such file"
    - `ls -d planning/ .cursor/ .worktrees/ 2>&1` shows "No such file"
    - `ls .aether/workers-new-castes.md .aether/recover.sh .aether/pheromone_system.py .aether/semantic_layer.py 2>&1` shows "No such file"
    - `ls -d .aether/examples/ .aether/__pycache__/ 2>&1` shows "No such file"
    - `ls -d .aether/agents/ .aether/commands/ 2>&1` shows "No such file" (confirms CLEAN-02/03)
    - `npm pack --dry-run` succeeds
    - `npm test` has no new failures
  </verify>
  <done>
    All dead files removed from repo root and .aether/ root. CLEAN-02 and CLEAN-03 confirmed already done. npm package still builds. Test suite has no new failures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete .claude/ and .opencode/ dead duplicates</name>
  <files>
    .claude/commands/gsd/new-project.md.bak
    .opencode/agents/workers.md
  </files>
  <action>
    Delete two dead files from tooling directories:

    1. `.claude/commands/gsd/new-project.md.bak` — backup file; the `.md` version is the live one
    2. `.opencode/agents/workers.md` — stale near-duplicate of `.aether/workers.md` (1,034 lines vs current 765 lines; missing Phase 25 updates)

    Before deleting `.opencode/agents/workers.md`, grep for references to confirm nothing depends on it:
    ```
    grep -r "workers.md" .opencode/ --include="*.md" --include="*.json"
    ```
    If any OpenCode agent file explicitly references `workers.md`, note it but proceed — the authoritative copy is `.aether/workers.md` which is distributed via hub.

    Use `git rm` for tracked files.

    After deletion, run `npm run lint:sync` to verify command sync still passes.
  </action>
  <verify>
    - `ls .claude/commands/gsd/new-project.md.bak 2>&1` shows "No such file"
    - `ls .opencode/agents/workers.md 2>&1` shows "No such file"
    - `npm run lint:sync` passes (or documents pre-existing drift — do not block on content-level drift which is known pre-existing debt)
  </verify>
  <done>
    Dead .bak file and stale workers.md duplicate removed. Command sync verified. CLEAN-07 satisfied.
  </done>
</task>

</tasks>

<verification>
- No dead files remain in repo root: logo files, Aether Notes, empty planning/, .cursor/ all gone
- No dead files remain in .aether/ root: workers-new-castes.md, recover.sh, debugging artifacts, Python prototypes, examples/ all gone
- .aether/agents/ and .aether/commands/ confirmed non-existent (CLEAN-02, CLEAN-03 already done)
- No dead duplicates in .claude/ or .opencode/ (CLEAN-07)
- `npm pack --dry-run` succeeds
- `npm test` passes with no new failures
- `npm run lint:sync` passes
</verification>

<success_criteria>
All files listed in this plan are deleted. Package builds correctly. Tests pass with no new failures. CLEAN-01 (partial — repo root and .aether/ root audit), CLEAN-02, CLEAN-03, and CLEAN-07 are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/26-file-audit/26-01-SUMMARY.md`
</output>
