---
phase: 39-quality-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/ant/continue.md
autonomous: true
requirements:
  - COV-01
  - COV-02
  - COV-03
  - COV-04
must_haves:
  truths:
    - Probe spawns in /ant:continue when coverage < 80% after tests pass
    - Probe generates tests for uncovered code paths
    - Probe discovers edge cases through mutation testing
    - Probe is strictly non-blocking — phase continues even if coverage cannot be improved
    - Probe only modifies test files, never source code
  artifacts:
    - path: .claude/commands/ant/continue.md
      provides: "Probe coverage agent integration in verification workflow"
      contains: "Step 1.5.1: Probe Coverage Agent"
  key_links:
    - from: continue.md Step 1.5.1
      to: Probe agent spawn
      via: Task tool with subagent_type=aether-probe
      pattern: "spawn-log.*probe"
    - from: Probe JSON output
      to: Non-blocking continuation logic
      via: "Probe findings logged to midden, phase continues regardless"
      pattern: "midden-write.*probe"
---

<objective>
Integrate the existing Probe agent into `/ant:continue` verification workflow to automatically improve test coverage when it falls below 80%.

Purpose: Add intelligent test generation for uncovered code paths and edge case discovery through mutation testing, improving overall code quality without blocking phase advancement.
Output: Updated continue.md with Probe coverage agent integration (Step 1.5.1) that runs after the verification loop coverage check and before the secrets scan.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.claude/commands/ant/continue.md
@.opencode/agents/aether-probe.md
@.planning/phases/38-security-gates/38-01-SUMMARY.md
@.planning/phases/38-security-gates/38-02-SUMMARY.md

## User Decisions (LOCKED)

### Spawn Triggers
- Probe spawns when coverage < 80% AND tests pass
- If coverage >= 80% or tests fail, Probe skips silently

### Blocking Behavior
- **Probe is NON-BLOCKING** — this is critical difference from Gatekeeper/Auditor
- Phase continues regardless of Probe results
- Findings are logged to midden for reference

### Integration Point
- Probe runs after Step 1.5 (Verification Loop) coverage check
- Uses existing coverage data from verification loop — no duplicate checks
- Runs before Step 1.5 Phase 5 (Secrets Scan)
- Insert as Step 1.5.1 between coverage check and secrets scan

### Agent Already Exists
- Probe agent definition is at `.opencode/agents/aether-probe.md`
- DO NOT create a new agent — integrate the existing one
- Spawn via Task tool with `subagent_type="aether-probe"`

### Agent Constraints (from existing definition)
- Probe can ONLY modify test files — never source code
- Probe is read-only for source code analysis
- Generated tests must follow existing test conventions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Probe Coverage Agent to continue.md</name>
  <files>.claude/commands/ant/continue.md</files>
  <action>
Insert new Step 1.5.1 "Probe Coverage Agent (Conditional)" after the coverage check in Step 1.5 (Verification Loop) and before the Phase 5 secrets scan.

The new step must:

1. Check coverage threshold condition:
   - Coverage data is already available from Step 1.5 verification loop
   - Extract coverage_percent from the coverage check (already recorded)
   - If coverage_percent >= 80%: Skip Probe silently, continue to secrets scan
   - If tests failed: Skip Probe silently (coverage data unreliable), continue to secrets scan
   - If coverage_percent < 80% AND tests passed: Proceed to spawn Probe

2. Generate Probe name and dispatch:
   - Run: `bash .aether/aether-utils.sh generate-ant-name "probe"`
   - Store as `{probe_name}`
   - Log spawn: `bash .aether/aether-utils.sh spawn-log "Queen" "probe" "{probe_name}" "Coverage improvement: {coverage_percent}%"`
   - Update swarm display for visual mode
   - Display: "🧪🐜 Probe {probe_name} spawning — Coverage at {coverage_percent}%, generating tests for uncovered paths..."

3. Determine uncovered files:
   - Run: `git diff --name-only HEAD~1 2>/dev/null || git diff --name-only`
   - Filter for source files (not test files)
   - These are the files Probe should analyze for coverage gaps

4. Spawn Probe using Task tool with `subagent_type="aether-probe"`:
   - **CRITICAL:** The agent already exists at `.opencode/agents/aether-probe.md`
   - Include fallback: If "Agent type not found", use general-purpose and inject Probe role
   - Pass mission context with:
     - Current coverage percentage
     - List of modified source files to analyze
     - Target: 80% coverage
     - Constraint: Test files only, never modify source
   - Request JSON output matching Probe schema (see agent definition)

5. Parse Probe JSON output:
   - Extract: `tests_added`, `coverage`, `edge_cases_discovered`, `mutation_score`
   - Log completion: `bash .aether/aether-utils.sh spawn-complete "{probe_name}" "completed" "Generated tests, edge cases discovered"`

6. Log findings to midden:
   - Run: `bash .aether/aether-utils.sh midden-write "coverage" "Probe generated tests, coverage: {lines}%/{branches}%/{functions}%" "probe"`
   - If edge cases found: `bash .aether/aether-utils.sh midden-write "edge_cases" "Found {count} edge cases" "probe"`

7. NON-BLOCKING continuation:
   - Display Probe findings summary
   - ALWAYS continue to secrets scan regardless of Probe results
   - Do NOT gate phase advancement on Probe success/failure
   - Display: "🧪🐜 Probe complete — Findings logged to midden, continuing verification..."

8. Update verification report to include Probe results:
   - Add line after Coverage in verification report:
   - `🧪 Probe        [ACTIVE/SKIP] (tests added: X, edge cases: Y)`
  </action>
  <verify>
    - grep -n "Step 1.5.1" .claude/commands/ant/continue.md
    - grep -n "subagent_type.*aether-probe" .claude/commands/ant/continue.md
    - grep -n "midden-write.*probe" .claude/commands/ant/continue.md
    - grep -n "NON-BLOCKING\|non-blocking" .claude/commands/ant/continue.md
  </verify>
  <done>
    Step 1.5.1 exists with Probe spawn logic, coverage threshold check (< 80%), test generation, edge case discovery, midden logging, and non-blocking continuation to secrets scan.
  </done>
</task>

</tasks>

<verification>
1. Probe step exists in continue.md at Step 1.5.1
2. Probe spawns only when coverage < 80% AND tests pass
3. Probe uses existing coverage data from verification loop (no duplicate checks)
4. Probe spawns via Task tool with subagent_type="aether-probe"
5. Probe findings logged to midden via midden-write
6. Probe is non-blocking — phase continues regardless of results
7. Existing Probe agent at .opencode/agents/aether-probe.md is used (not recreated)
8. Verification report updated to show Probe status
</verification>

<success_criteria>
- Step 1.5.1: Probe Coverage Agent exists in continue.md
- Probe spawns conditionally based on coverage < 80% threshold
- Probe generates tests for uncovered code paths
- Probe discovers edge cases through mutation testing concepts
- Probe is strictly non-blocking (COV-04)
- Probe only modifies test files, never source code (COV-02, COV-03)
- Existing Probe agent definition used (not recreated)
- Findings logged to midden for future reference
</success_criteria>

<output>
After completion, create `.planning/phases/39-quality-coverage/39-01-SUMMARY.md`
</output>
