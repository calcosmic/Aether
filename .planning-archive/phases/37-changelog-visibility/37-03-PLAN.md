---
phase: 37-changelog-visibility
plan: 03
type: execute
wave: 2
depends_on:
  - 37-01
  - 37-02
files_modified:
  - .claude/commands/ant/resume.md
  - .claude/commands/ant/status.md
  - .claude/commands/ant/memory-details.md
autonomous: true
requirements:
  - VIS-01
  - VIS-02

must_haves:
  truths:
    - "/ant:resume shows memory health counts (wisdom, pending, failures) as secondary section"
    - "/ant:status shows memory health in table format with four metrics"
    - "/ant:memory-details command exists for drill-down into full learnings/failures"
    - "Resume PRIMARY section remains 'Where am I now' with phase progress"
  artifacts:
    - path: ".claude/commands/ant/resume.md"
      provides: "Updated resume command with memory health display"
      contains: "Memory Health"
    - path: ".claude/commands/ant/status.md"
      provides: "Updated status command with memory health table"
      contains: "┌─" # table border
    - path: ".claude/commands/ant/memory-details.md"
      provides: "Drill-down command for full memory details"
      contains: "memory-details"
  key_links:
    - from: "resume.md"
      to: "aether-utils.sh resume-dashboard"
      via: "bash .aether/aether-utils.sh resume-dashboard"
      pattern: "resume-dashboard"
    - from: "status.md"
      to: "aether-utils.sh memory-metrics"
      via: "bash .aether/aether-utils.sh memory-metrics"
      pattern: "memory-metrics"
---

<objective>
Integrate memory health visibility into /ant:resume and /ant:status commands. Update resume.md to show memory health counts as a secondary section. Update status.md to display the four memory health metrics in table format. Create a new /ant:memory-details command for drill-down into full learnings and failures.

Purpose: Make colony memory visible to users (VIS-01, VIS-02). Users can see wisdom accumulation, pending promotions, and recent failures at a glance.

Output: Updated resume.md, updated status.md, new memory-details.md command file.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-changelog-visibility/37-CONTEXT.md
@.planning/phases/37-changelog-visibility/37-RESEARCH.md
@.claude/commands/ant/resume.md
@.claude/commands/ant/status.md

## User Decisions (LOCKED)

### Resume Dashboard Layout
- PRIMARY: "Where am I now" — current phase progress, last task, what's next
- SECONDARY: Learnings, failures, wisdom shown as counts only (not full items)
- Drill-down available if user wants details
- Recency from research: last 5 decisions, last 10 events

### Status Memory Health Display
- **Metrics to show (all four):**
  - Wisdom count (total in QUEEN.md)
  - Pending promotions (observations pending, deferred proposals)
  - Recent failures (from midden/)
  - Last activity (when QUEEN.md updated, last learning captured)
- **Format:** Table format (rows and columns, easy to scan in terminal)

### Claude's Discretion
- Exact table column widths and spacing
- Threshold for "recent" failures: Using last 5 (matches decision recency)
- Drill-down command: /ant:memory-details

## Dependencies

This plan depends on 37-01 (utility functions) and 37-02 (changelog system):
- memory-metrics function (from 37-01)
- resume-dashboard function (from 37-01)
- midden-recent-failures function (from 37-01)

## Existing Command Patterns

### resume.md
- Currently shows: Next recommendation, drift detection, goal, state, phase progress, recent decisions, active signals
- Memory health will be added as a new section after "Active Signals"

### status.md
- Currently shows: Progress bars, focus/constraints, instincts, flags, milestone, dreams
- Memory health table will be added as a new section
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update resume.md with memory health section</name>
  <files>.claude/commands/ant/resume.md</files>
  <action>
    Update the resume.md command file to include a memory health section as SECONDARY content (after the PRIMARY "Where am I now" section).

    Changes to make:

    1. After Step 8 (Render Dashboard), add a new Step 8.5: Display Memory Health

    Insert this section:
    ```markdown
    ### Step 8.5: Display Memory Health (Secondary)

    Run using the Bash tool with description "Loading memory health...":
    ```bash
    bash .aether/aether-utils.sh resume-dashboard
    ```

    Extract memory_health from the JSON result:
    - wisdom_count
    - pending_promotions
    - recent_failures

    Display after the main dashboard:
    ```
    📊 Memory Health
       Wisdom: {wisdom_count} entries | Pending: {pending_promotions} promotions | Failures: {recent_failures} recent

       Run /ant:memory-details for full breakdown
    ```

    If all counts are 0, show:
    ```
    📊 Memory Health
       No accumulated wisdom yet. Complete phases to build colony memory.
    ```
    ```

    2. Update the "Last Command" line to move after memory health section

    The memory health section should appear after "Active Signals" (if any) and before "Last Command".

    Ensure the PRIMARY focus remains on "Where am I now" — memory health is secondary context.
  </action>
  <verify>
    grep -q "Memory Health" .claude/commands/ant/resume.md
    grep -q "memory-details" .claude/commands/ant/resume.md
    grep -q "resume-dashboard" .claude/commands/ant/resume.md
    # All patterns should be found
  </verify>
  <done>resume.md updated with memory health section showing counts and drill-down command</done>
</task>

<task type="auto">
  <name>Task 2: Update status.md with memory health table</name>
  <files>.claude/commands/ant/status.md</files>
  <action>
    Update the status.md command file to include a memory health table display.

    Changes to make:

    1. After Step 2.6 (Detect Milestone), add a new Step 2.8: Load Memory Health Metrics

    Insert this section:
    ```markdown
    ### Step 2.8: Load Memory Health Metrics

    Run using the Bash tool with description "Loading memory health metrics...":
    ```bash
    bash .aether/aether-utils.sh memory-metrics
    ```

    Extract from JSON result:
    - wisdom.total
    - pending.total
    - recent_failures.count
    - last_activity.queen_md_updated
    - last_activity.learning_captured

    Format timestamps for display (YYYY-MM-DD HH:MM).
    ```

    2. Update Step 3 (Display) to include the memory health table

    Add after the Milestone line and before Dreams:
    ```markdown
    📚 Memory Health
    ┌─────────────────┬────────┬─────────────────────────────┐
    │ Metric          │ Count  │ Last Updated                │
    ├─────────────────┼────────┼─────────────────────────────┤
    │ Wisdom Entries  │ {wisdom_total:>6} │ {queen_updated}             │
    │ Pending Promos  │ {pending_total:>6} │ {learning_updated}          │
    │ Recent Failures │ {failures_count:>6} │ {last_failure}              │
    └─────────────────┴────────┴─────────────────────────────┘
    ```

    Use the memory-metrics JSON to populate the table values.

    3. If memory-metrics returns empty/null values, show:
    ```
    📚 Memory Health
       No memory data available. Colony wisdom will accumulate as you complete phases.
    ```

    The table format should use box-drawing characters (┌─┬─┐) for clarity.
  </action>
  <verify>
    grep -q "Memory Health" .claude/commands/ant/status.md
    grep -q "memory-metrics" .claude/commands/ant/status.md
    grep -q "┌─" .claude/commands/ant/status.md
    # All patterns should be found
  </verify>
  <done>status.md updated with memory health table showing four metrics</done>
</task>

<task type="auto">
  <name>Task 3: Create memory-details.md command</name>
  <files>.claude/commands/ant/memory-details.md</files>
  <action>
    Create a new command file `.claude/commands/ant/memory-details.md` that provides drill-down details for colony memory.

    The command should:

    1. Show full wisdom entries from QUEEN.md (categorized by type)
    2. Show pending promotion proposals from learning-observations.json
    3. Show deferred proposals from learning-deferred.json
    4. Show recent failures from midden.json with full details

    Command structure:
    ```markdown
    ---
    name: memory-details
    description: "Show detailed colony memory — wisdom, pending promotions, and recent failures"
    symbol: brain
    ---

    # /ant:memory-details — Colony Memory Details

    Drill-down view of accumulated colony wisdom, pending promotions, and recent failures.

    ## Usage

    ```bash
    /ant:memory-details
    ```

    ## Implementation

    ### Step 1: Load Memory Data

    Run using the Bash tool with description "Loading colony memory...":
    ```bash
    bash .aether/aether-utils.sh memory-metrics
    ```

    ### Step 2: Display Wisdom (from QUEEN.md)

    Read .aether/docs/QUEEN.md and display entries by category:
    - Philosophies
    - Patterns
    - Redirects
    - Stack
    - Decrees

    Format:
    ```
    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    COLONY WISDOM (X entries)
    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    📜 Philosophies (N)
       - Entry 1...
       - Entry 2...

    🔧 Patterns (N)
       - Entry 1...
    ```

    ### Step 3: Display Pending Promotions

    Show observations meeting threshold but not yet promoted:
    ```
    ⏳ Pending Promotions (N)
       - [type] Content... (X observations)
    ```

    Show deferred proposals:
    ```
    💤 Deferred Proposals (N)
       - [type] Content... (deferred YYYY-MM-DD)
    ```

    ### Step 4: Display Recent Failures

    Show last 5 failures from midden:
    ```
    ⚠️ Recent Failures (N)
       [YYYY-MM-DD HH:MM] Source: context
       Content...
    ```

    ### Step 5: Summary

    Show counts summary and reminder command:
    ```
    Run /ant:status for quick overview
    ```
    ```

    Follow the existing command file patterns (see resume.md, status.md for reference).
  </action>
  <verify>
    test -f .claude/commands/ant/memory-details.md
    grep -q "memory-metrics" .claude/commands/ant/memory-details.md
    grep -q "QUEEN.md" .claude/commands/ant/memory-details.md
    grep -q "Pending Promotions" .claude/commands/ant/memory-details.md
    grep -q "Recent Failures" .claude/commands/ant/memory-details.md
    # All files and patterns should exist
  </verify>
  <done>memory-details.md command created with full drill-down display</done>
</task>

<task type="auto">
  <name>Task 4: Sync to OpenCode commands</name>
  <files>.opencode/commands/ant/resume.md, .opencode/commands/ant/status.md, .opencode/commands/ant/memory-details.md</files>
  <action>
    Sync the updated commands to the OpenCode commands directory.

    Copy the updated files:
    1. .claude/commands/ant/resume.md → .opencode/commands/ant/resume.md
    2. .claude/commands/ant/status.md → .opencode/commands/ant/status.md
    3. .claude/commands/ant/memory-details.md → .opencode/commands/ant/memory-details.md

    Run using Bash tool with description "Syncing commands to OpenCode...":
    ```bash
    cp .claude/commands/ant/resume.md .opencode/commands/ant/resume.md
    cp .claude/commands/ant/status.md .opencode/commands/ant/status.md
    cp .claude/commands/ant/memory-details.md .opencode/commands/ant/memory-details.md
    ls -la .opencode/commands/ant/resume.md .opencode/commands/ant/status.md .opencode/commands/ant/memory-details.md
    ```

    Verify all three files exist in the OpenCode directory.
  </action>
  <verify>
    test -f .opencode/commands/ant/memory-details.md
    diff .claude/commands/ant/memory-details.md .opencode/commands/ant/memory-details.md
    # Files should be identical
  </verify>
  <done>All three commands synced to OpenCode commands directory</done>
</task>

<task type="auto">
  <name>Task 5: Verify command sync with lint</name>
  <files>.claude/commands/ant/*.md, .opencode/commands/ant/*.md</files>
  <action>
    Run the command sync linter to verify Claude and OpenCode commands are in sync.

    Run using Bash tool with description "Verifying command sync...":
    ```bash
    npm run lint:sync 2>&1 || true
    ```

    If there are sync issues, identify and fix them.

    Also verify the new memory-details command appears in both directories.
  </action>
  <verify>
    # Check that files exist and have similar content
    test -f .claude/commands/ant/memory-details.md
    test -f .opencode/commands/ant/memory-details.md
    echo "Command sync verified"
  </verify>
  <done>Command sync verified between Claude and OpenCode</done>
</task>

</tasks>

<verification>
- [ ] resume.md updated with memory health section (counts only)
- [ ] resume.md shows drill-down command (/ant:memory-details)
- [ ] status.md updated with memory health table
- [ ] Table includes all four metrics: wisdom, pending, failures, activity
- [ ] memory-details.md command created
- [ ] memory-details shows full wisdom, pending, and failures
- [ ] All commands synced to OpenCode
- [ ] PRIMARY focus on "Where am I now" preserved in resume
</verification>

<success_criteria>
Visibility features integrated (VIS-01, VIS-02):

1. **/ant:resume** shows:
   - PRIMARY: "Where am I now" (phase progress, next steps) - unchanged
   - SECONDARY: Memory health counts (wisdom, pending, failures)
   - Drill-down command reference: /ant:memory-details

2. **/ant:status** shows:
   - Memory health table with four metrics:
     - Wisdom Entries (count + last updated)
     - Pending Promos (count + last updated)
     - Recent Failures (count + last failure)
     - Activity timestamps
   - Table format using box-drawing characters for clarity

3. **/ant:memory-details** provides:
   - Full wisdom entries from QUEEN.md (by category)
   - Pending promotion proposals
   - Deferred proposals
   - Recent failures with full details

4. **Command sync**: All commands synced to OpenCode directory

Users can now see colony memory health at a glance and drill down for details.
</success_criteria>

<output>
After completion, create `.planning/phases/37-changelog-visibility/37-03-SUMMARY.md`
</output>
