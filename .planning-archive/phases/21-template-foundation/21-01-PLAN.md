---
phase: 21-template-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .aether/templates/colony-state.template.json
  - .aether/templates/constraints.template.json
  - .aether/templates/colony-state-reset.jq.template
autonomous: true
requirements: [TMPL-01, TMPL-02, TMPL-05]

must_haves:
  truths:
    - "colony-state.template.json contains annotated JSON matching COLONY_STATE.json v3.0 schema"
    - "constraints.template.json contains annotated JSON matching constraints.json v1.0 schema"
    - "colony-state-reset.jq.template is a valid jq filter that resets all colony state fields"
  artifacts:
    - path: ".aether/templates/colony-state.template.json"
      provides: "Annotated colony state template with __PLACEHOLDER__ values"
      contains: "__GOAL__"
    - path: ".aether/templates/constraints.template.json"
      provides: "Annotated constraints template"
      contains: "_template"
    - path: ".aether/templates/colony-state-reset.jq.template"
      provides: "jq filter for resetting colony state"
      contains: ".goal = null"
  key_links:
    - from: ".aether/templates/colony-state.template.json"
      to: ".claude/commands/ant/init.md"
      via: "Structure matches init.md lines 184-213"
      pattern: '"version": "3.0"'
    - from: ".aether/templates/colony-state-reset.jq.template"
      to: ".claude/commands/ant/entomb.md"
      via: "jq filter matches entomb.md lines 358-377"
      pattern: '.state = "IDLE"'
---

<objective>
Create data-structure templates: colony-state JSON, constraints JSON, and colony-state-reset jq filter as standalone template files in `.aether/templates/`.

Purpose: Extract embedded JSON blocks and jq filters from command files into standalone templates. LLM agents currently reconstruct these structures from memory inside 300-500 line instruction files — they add fields, drop required ones, or mangle the format. Standalone templates use the "read and fill" pattern: agent reads exact structure at point of use.

Output: Three template files in `.aether/templates/` using flat directory layout (matching existing QUEEN.md.template pattern).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-template-foundation/21-RESEARCH.md

Source files containing inline structures to extract:
@.claude/commands/ant/init.md (lines 184-225 — colony state JSON and constraints JSON)
@.claude/commands/ant/entomb.md (lines 358-377 — jq reset filter)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create colony-state.template.json</name>
  <files>.aether/templates/colony-state.template.json</files>
  <action>
Create `.aether/templates/colony-state.template.json` using the annotated JSON format from the research document. The template must match the v3.0 schema used in init.md lines 184-213.

**Template content — use exactly this structure:**

```json
{
  "_template": "colony-state",
  "_version": "3.0",
  "_instructions": "Write this file to .aether/data/COLONY_STATE.json. Replace all __PLACEHOLDER__ values with real data. Remove all keys starting with underscore (_template, _version, _instructions, and any _comment_*) before writing the file.",
  "_comment_goal": "The user's goal as provided to /ant:init or /ant:lay-eggs",
  "_comment_memory": "Seed from completion-report.md if prior colony found. Otherwise use empty arrays [].",
  "version": "3.0",
  "goal": "__GOAL__",
  "state": "READY",
  "current_phase": 0,
  "session_id": "__SESSION_ID__",
  "initialized_at": "__ISO8601_TIMESTAMP__",
  "build_started_at": null,
  "plan": {
    "generated_at": null,
    "confidence": null,
    "phases": []
  },
  "memory": {
    "phase_learnings": "__PHASE_LEARNINGS__",
    "decisions": [],
    "instincts": "__INSTINCTS__"
  },
  "errors": {
    "records": [],
    "flagged_patterns": []
  },
  "signals": [],
  "graveyards": [],
  "events": [
    "__ISO8601_TIMESTAMP__|colony_initialized|init|Colony initialized with goal: __GOAL__"
  ]
}
```

**Important notes:**
- `__PHASE_LEARNINGS__` and `__INSTINCTS__` must resolve to JSON arrays (e.g., `[]` or `[{...}]`), not strings. The `_comment_memory` annotation explains this to the LLM.
- Flat directory: file goes directly in `.aether/templates/`, not in a subdirectory.
- Do NOT modify init.md or any command file — this is CREATE ONLY.
  </action>
  <verify>
Verify the file exists and is valid JSON:
```bash
jq . .aether/templates/colony-state.template.json
```
Verify it contains the required placeholders:
```bash
grep -c "__GOAL__" .aether/templates/colony-state.template.json  # Should be >= 2
grep -c "__SESSION_ID__" .aether/templates/colony-state.template.json  # Should be 1
grep -c "_template" .aether/templates/colony-state.template.json  # Should be 1
```
  </verify>
  <done>colony-state.template.json exists in .aether/templates/, is valid JSON, contains _template/_version/_instructions metadata keys, uses __PLACEHOLDER__ convention for all variable values, and matches the v3.0 schema structure from init.md</done>
</task>

<task type="auto">
  <name>Task 2: Create constraints.template.json and colony-state-reset.jq.template</name>
  <files>.aether/templates/constraints.template.json, .aether/templates/colony-state-reset.jq.template</files>
  <action>
**Part A: Create `.aether/templates/constraints.template.json`**

This is a small template (init.md lines 219-225). Use annotated JSON format:

```json
{
  "_template": "constraints",
  "_version": "1.0",
  "_instructions": "Write this file to .aether/data/constraints.json. No placeholder substitution needed — write the data keys as-is. Remove all underscore-prefixed keys before writing.",
  "version": "1.0",
  "focus": [],
  "constraints": []
}
```

**Part B: Create `.aether/templates/colony-state-reset.jq.template`**

This is a raw jq filter (entomb.md lines 358-377). Store verbatim — no annotation keys needed since jq files don't support metadata. Add a comment header instead:

```jq
# colony-state-reset.jq.template — Reset colony state fields for entomb
# Usage: jq -f .aether/templates/colony-state-reset.jq.template STATE.json.bak > STATE.json
# Version: 1.0

.goal = null |
.state = "IDLE" |
.current_phase = 0 |
.plan.phases = [] |
.plan.generated_at = null |
.plan.confidence = null |
.build_started_at = null |
.session_id = null |
.initialized_at = null |
.milestone = null |
.events = [] |
.errors.records = [] |
.errors.flagged_patterns = [] |
.signals = [] |
.graveyards = [] |
.memory.instincts = [] |
.memory.phase_learnings = [] |
.memory.decisions = []
```

**Important:** Both files go in `.aether/templates/` (flat directory). Do NOT modify entomb.md or init.md.
  </action>
  <verify>
Verify constraints template is valid JSON:
```bash
jq . .aether/templates/constraints.template.json
```

Verify jq filter works on test input:
```bash
echo '{"version":"3.0","goal":"test","state":"ACTIVE","current_phase":2,"plan":{"phases":["a"],"generated_at":"2026-01-01","confidence":"high"},"build_started_at":"2026-01-01","session_id":"abc","initialized_at":"2026-01-01","milestone":"Open Chambers","events":["test"],"errors":{"records":["err"],"flagged_patterns":["pat"]},"signals":["sig"],"graveyards":["g"],"memory":{"instincts":["i"],"phase_learnings":["l"],"decisions":["d"]}}' | jq -f .aether/templates/colony-state-reset.jq.template
```
Expected: All reset fields should be null/empty, version preserved.
  </verify>
  <done>constraints.template.json exists, is valid JSON with _template metadata. colony-state-reset.jq.template exists, runs successfully via `jq -f`, and resets all expected colony state fields to null/empty while preserving `version`.</done>
</task>

</tasks>

<verification>
1. All three template files exist in `.aether/templates/`:
   - `colony-state.template.json` — valid JSON, has `__GOAL__`, `__SESSION_ID__`, `_template` metadata
   - `constraints.template.json` — valid JSON, has `_template` metadata
   - `colony-state-reset.jq.template` — valid jq filter, runs without error
2. No command files (init.md, entomb.md, seal.md) were modified
3. `jq -f .aether/templates/colony-state-reset.jq.template` produces valid JSON output with all fields reset
4. Template naming follows `{name}.template.{ext}` convention matching existing `QUEEN.md.template`
</verification>

<success_criteria>
- 3 template files created in `.aether/templates/`
- colony-state.template.json matches init.md v3.0 schema structure exactly
- constraints.template.json matches init.md constraints structure exactly
- colony-state-reset.jq.template matches entomb.md jq filter exactly
- All files use correct placeholder conventions (__DOUBLE_UNDERSCORE__ for JSON, comments for jq)
- No existing files modified
</success_criteria>

<output>
After completion, create `.planning/phases/21-template-foundation/21-01-SUMMARY.md`
</output>
