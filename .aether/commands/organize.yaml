name: ant-organize
description: "🧹 Run codebase hygiene report"
source_of_truth: "Use the Go `aether` CLI as the source of truth."
guardrails:
  - "Do not write colony state files, session files, or pheromone files by hand from this command spec."
  - "If docs and runtime disagree, runtime wins."
  - "Use only current lifecycle vocabulary: `/ant-pause` and `/ant-resume`; never teach retired pause-colony, resume-colony, recover, or abandon routes."
  - "Exception: the wrapper MAY write the advisory report to .aether/data/hygiene-report.md; that report file is the only sanctioned wrapper-written artifact under .aether/data/."
