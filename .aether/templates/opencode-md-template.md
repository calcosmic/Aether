# {COLONY_NAME}

> Aether Colony -- OpenCode Instructions

This file provides project-level instructions for OpenCode (equivalent to
CLAUDE.md for Claude Code). Aether manages this colony.

Colony goal: {COLONY_GOAL}

---

## How Aether Works in OpenCode

OpenCode uses slash commands as the primary interface for colony operations:

```
/ant-init "Build feature X"     # Start a colony
/ant-plan                       # Generate project phases
/ant-build <phase>              # Execute a phase
/ant-continue                   # Verify and advance
/ant-status                     # Colony dashboard
/ant-run                        # Autopilot mode
```

Agent definitions live in `.opencode/agents/*.md` (Markdown with YAML frontmatter).

---

## Key Commands

### Setup and Getting Started

| Command | Purpose |
|---------|---------|
| `/ant-init "<goal>"` | Start a colony with a goal |
| `/ant-colonize` | Analyze existing codebase |
| `/ant-plan` | Generate project phases |
| `/ant-build <N>` | Execute a phase |
| `/ant-continue` | Verify, learn, advance |
| `/ant-status` | Colony dashboard |
| `/ant-run` | Autopilot all phases |

### Pheromone Signals

| Command | Priority | Purpose |
|---------|----------|---------|
| `/ant-focus "<area>"` | normal | Guide colony attention |
| `/ant-redirect "<pattern>"` | high | Hard constraint -- avoid this |
| `/ant-feedback "<note>"` | low | Gentle adjustment |
| `/ant-pheromones` | -- | View active signals |

### Session Management

| Command | Purpose |
|---------|---------|
| `/ant-pause` | Stop at a safe boundary and save one resumable handoff |
| `/ant-resume` | Validate and restore the safest honest recovery point |

### Lifecycle

| Command | Purpose |
|---------|---------|
| `/ant-seal` | Seal colony and retain active state for review |
| `/ant-entomb` | Optional explicit owner-invoked archive-and-clear alternative |
| `/ant-update` | Update system files from hub |

---

## Key Directories

| Directory | Purpose |
|-----------|---------|
| `.opencode/agents/` | Agent definitions (Markdown + YAML frontmatter) |
| `.opencode/commands/ant/` | Slash commands |
| `.aether/` | Colony companion files |
| `.aether/data/` | Local state (never versioned) |
| `.aether/skills/` | Colony and domain skills |
| `.aether/templates/` | State and config templates |

---

## Typical Workflow

```
/ant-init "Build feature X"       # Start colony
/ant-colonize                      # Analyze existing code (optional)
/ant-plan                          # Generate phases
/ant-focus "security"              # Guide colony (optional)
/ant-build 1                       # Execute phase 1
/ant-continue                      # Verify and advance
/ant-run                           # Or autopilot all phases

# After completing:
/ant-seal                          # Seal and retain active state for review
/ant-status                        # Review the retained sealed state first
/ant-entomb                        # Optional explicit owner-invoked archive-and-clear alternative
/ant-init "next project goal"      # Only after a successful archive-and-clear receipt
```

### Sealed colony: review before archive

After `/ant-seal`, whether it is verified or a forced-incomplete closure, the
active colony remains retained for review; a forced-incomplete seal is never
verified completion.

1. First run `/ant-status` to review the retained sealed state.
2. `/ant-entomb` is an optional, explicit owner-invoked archive-and-clear
   alternative; it is never automatic or required after sealing.
3. A forced-incomplete marker remains visible in `/ant-status` and optional
   `/ant-entomb`.
4. Only after a successful archive-and-clear receipt has verified the archive
   and cleared active state may you run `/ant-init` for a new goal.

---

## Verification Commands

*Generated by Aether setup. Customize this file for your project.*
