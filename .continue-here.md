# Continue Here
> Session paused: 2026-05-04T00:00:00+02:00

## Current State

- Task: Implement "Global Agents/Commands, Local-Only Repo State".
- Status: Paused mid-implementation, not ready to commit.
- Critical warning: the worktree is very dirty and contains unrelated pre-existing changes. Do not clean, reset, or revert broad paths. Inspect file contents before assuming a change exists.
- Plain-English summary: the target architecture is "shared Aether machinery lives once in the global hub; target repos keep only their own colony state."

## Red Flag: Queen Compose Regression

Another terminal reported that `aether queen-compose` was overwritten/removed.
Current verification in this terminal agrees:

- `go run ./cmd/aether queen-compose --help` returns `unknown command "queen-compose"`.
- Current `cmd/queen.go` contains `queen-init`, `queen-read`, `queen-promote`, `queen-thresholds`, and `queen-write-learnings`, but no `queen-compose` command registration/implementation.
- These files are present but untracked and should be deliberately reviewed/added:
  - `.aether/commands/queen-compose.yaml`
  - `.claude/commands/ant/queen-compose.md`
  - `.claude/commands/ant-queen-compose.md`
  - `.opencode/commands/ant/queen-compose.md`
  - `.aether/QUEEN.md`
- Missing `.aether/commands/claude/queen-compose.md` and `.aether/commands/opencode/queen-compose.md` is **not** itself a regression in this refactor, because `.aether/commands/claude/` and `.aether/commands/opencode/` are deleted source mirrors.
- The actual regression is the missing Go runtime command and its tests.

For dummies: the wrappers/signs for queen-compose exist, but the CLI engine does
not. Fix the engine, and do not recreate the deleted mirror directories.

## Salvage Inventory For Same-Day Work

The user is also worried that other same-day features were overwritten. A
dedicated inventory now exists:

- `.handoff-salvage-inventory.md`

Read it before changing or cleaning anything. It records the status of:

- init synthesis and charter flow
- Oracle/RALF/PRD/PDR/reference work
- codebase graph / colonize graph work
- queen-compose
- untracked shelf/source-check/recipes/to-prd/reference assets

Current high-level finding: not all is lost. Init synthesis and core Oracle are
still working; global/local Queen memory and Oracle confidence/iteration logic
pass focused tests; planning confidence/depth focused tests pass; codegraph and
reference content survive as untracked files; the main broken pieces are missing
runtime registrations/engines for queen-compose and reference commands, plus
colonize graph wiring.

## User Concern To Preserve

The user noticed that some command wrappers were heavily reduced. Treat this as a requirement:

- Do not remove ceremony, visual quality, detailed workflow instructions, or useful wrapper behavior unless the runtime fully replaces it.
- If a wrapper currently has a large deletion, prefer restoring the rich wrapper from `HEAD` with `git show HEAD:path` and then patch only the stale repo-local paths.
- The correct approach is to update assumptions such as "read `.aether/workers.md`" to hub/global paths, not to replace the whole command with a tiny CLI stub.
- Status update: the live Claude/OpenCode `lay-eggs` wrappers have been restored/adapted into rich ~197-line wrappers. Keep this as the standard for any further wrapper edits.

## What Was Done

- Used `session-handoff` skill for this pause.
- Confirmed the five source mirror directories have been deleted from the working tree:
  - `.aether/agents-claude/`
  - `.aether/agents-codex/`
  - `.aether/commands/claude/`
  - `.aether/commands/opencode/`
  - `.aether/skills-codex/`
- Updated `embedded_assets.go` to stop embedding the deleted mirror directories.
- Partially updated `cmd/platform_sync.go`:
  - `installSyncPairs()` now points Codex skill install at `.aether/skills` instead of `.aether/skills-codex`.
  - `repoSyncPairs()` now removes platform/agent/skill repo sync pairs and keeps only repo cleanup, Claude settings, and Claude rules.
  - Added cleanup helpers for stale repo-local platform assets and shipped repo-local skills.
  - Added `ensureRepoLocalScaffold()` for `.aether/data`, `.aether/checkpoints`, `.aether/locks`, `.aether/.gitignore`, and `.aether/QUEEN.md`.
- Partially updated tests:
  - `cmd/update_cmd_test.go` was replaced with a smaller suite around local-only repo state and cleanup.
  - `cmd/update_roundtrip_test.go` was replaced with a focused "global assets stay out of repo" test.
  - `cmd/setup_cmd_test.go`, `cmd/e2e_install_setup_update_test.go`, `cmd/codex_e2e_test.go`, `cmd/codex_instruction_hygiene_test.go`, and `cmd/medic_wrapper_test.go` were partially updated.
  - `cmd/agent_mirror_test.go` was deleted.
- Updated docs in progress:
  - `CLAUDE.md`
  - `AGENTS.md`
  - `.codex/CODEX.md`
  - `.opencode/OPENCODE.md`
  - `.aether/docs/source-of-truth-map.md`
  - `.aether/docs/publish-update-runbook.md`
  - `.aether/docs/skill-catalog-curation-plan.md`
  - `README.md`
- Deleted stale doc `.aether/docs/verification-phase5.txt`.
- Modified wrappers/playbooks:
  - `.aether/commands/build.yaml`
  - `.claude/commands/ant/build.md`
  - `.opencode/commands/ant/build.md`
  - `.aether/docs/command-playbooks/build-wave.md`
  - `.aether/docs/command-playbooks/build-full.md`
  - `.claude/commands/ant/organize.md`
  - `.opencode/commands/ant/organize.md`
  - `.claude/commands/ant/export-signals.md`
  - `.opencode/commands/ant/export-signals.md`
  - `.aether/commands/lay-eggs.yaml`
  - `.claude/commands/ant/lay-eggs.md`
  - `.opencode/commands/ant/lay-eggs.md`
- After the user flagged over-simplification, the live `lay-eggs` wrappers were repaired:
  - `.claude/commands/ant/lay-eggs.md` is now a rich procedural wrapper again.
  - `.opencode/commands/ant/lay-eggs.md` is now a rich procedural wrapper again.
  - The rewrite preserves Queen framing, failure modes, success criteria, read-only/protected local state, runtime execution, local-only verification, and visual completion output.

## Important Reality Check

Not all intended code changes are currently present in tracked files.

Current `rg` still shows old mirror assumptions in non-test Go files:

- `cmd/install_cmd.go`
  - Still says `.aether/skills-codex/ -> ~/.codex/skills/aether/`.
  - `setupInstallHub()` still needs to be changed to publish canonical sources:
    - `.claude/agents/ant` -> hub `agents-claude`
    - `.claude/commands/ant` -> hub command-wrapper directory
    - `.opencode/commands/ant` -> hub OpenCode command-wrapper directory
    - `.opencode/agents` -> hub `agents`
    - `.codex/agents` -> hub `codex`
    - `.aether/skills` -> hub Codex-shipped skills directory
  - `hubExcludeDirs` still has stale comments/behavior and needs slash-path excludes verified.
- `cmd/update_cmd.go`
  - Still has old dry-run text and old `runUpdateSync()` behavior unless changed after this handoff.
  - Must call cleanup/scaffold helpers added in `cmd/platform_sync.go`.
- `cmd/setup_cmd.go`
  - Still likely uses old `repoSyncPairs()` loop but does not yet clearly call the new cleanup/scaffold helpers.
- `cmd/skills.go`
  - Still scans `.aether/skills-codex`; remove repo-local scan and keep hub global shipped skill scan if still needed.
- `cmd/medic_wrapper.go`
  - Still checks deleted source mirrors; remove mirror source checks, keep hub integrity checks.
- `cmd/queen.go`
  - `queen-compose` is currently missing from the runtime command set.
  - Reapply/restore the Go command and tests from the other terminal/session if available, or rebuild it from the untracked wrapper intent.
- `cmd/integrity_cmd.go` and `cmd/update_cmd.go`
  - References to hub command/skill directories are okay if they are hub-global, but avoid docs grep expectations by not using literal stale source-mirror wording in docs.

## Known Broken/Incomplete Areas

- `cmd/platform_sync.go` now references helpers and concepts that are not fully wired into `setup_cmd.go` and `update_cmd.go`.
- The source mirror directories are deleted before `install/publish` has been fully updated to publish from canonical sources. This is expected in the final plan, but the code must be completed before tests can pass.
- The `lay-eggs` over-simplification has been repaired. Still audit other wrappers for accidental flattening before finalizing.
- `export-signals` default path changed to `.aether/data/pheromones-export.xml`; verify this matches runtime behavior before keeping it.
- There are many unrelated dirty/untracked files in the repo. Do not assume all dirty files were created by this task.
- No final full verification has been run after the latest runtime/docs edits.

## Last Verification

- `go test ./cmd -run TestDoesNotExist` passed earlier after the first round of code/test edits.
- Focused wrapper/source tests now pass after restoring rich `lay-eggs` wrappers:
  - `go test ./cmd -run 'TestCommandWrappersReferenceRealYamlSources|TestSourceOfTruthMapDocumentsWrapperYamlOwnership|TestCommandParity|TestCommandCount'`
- Full tests were not run.
- `go test ./... -race` was not run.

## Next Actions

1. Run `git status --short` and `rg` to confirm actual current state.
2. Complete the missing runtime wiring:
   - `cmd/install_cmd.go`
   - `cmd/update_cmd.go`
   - `cmd/setup_cmd.go`
   - `cmd/skills.go`
   - `cmd/medic_wrapper.go`
   - `cmd/queen.go` for missing `queen-compose`
   - possibly `cmd/integrity_cmd.go`
3. Check all wrapper path changes:
   - `build` should load playbooks from the hub or runtime-provided path.
   - `organize` should not read repo-local shipped `workers.md`.
   - `export/import-signals` should not default to shipped `.aether/exchange/` if exchange is global-only.
4. Remove obsolete source mirror tests and update tests based on actual behavior.
5. Run:
   - `gofmt -w` on modified Go files.
   - `go test ./cmd -run TestDoesNotExist`
   - focused tests around install/setup/update/source-check/medic/codex.
   - only then consider full `go test ./... -race`.
6. Do not commit until the user confirms, because the worktree had major pre-existing unrelated changes.
7. For parallel continuation, use the two split handoffs:
   - `.handoff-runtime-agent.md`
   - `.handoff-surface-docs-agent.md`

## Modified Files From This Session

High-confidence session changes:

- `cmd/platform_sync.go` - partially changed sync pair model and cleanup helpers.
- `embedded_assets.go` - removed mirror embed patterns.
- `cmd/update_cmd_test.go` - replaced with smaller local-only tests.
- `cmd/update_roundtrip_test.go` - replaced with focused global-assets test.
- `cmd/setup_cmd_test.go` - partially updated setup expectations.
- `cmd/e2e_install_setup_update_test.go` - partially updated end-to-end expectations.
- `cmd/codex_e2e_test.go` - partially updated Codex install/setup expectations.
- `cmd/codex_instruction_hygiene_test.go` - pointed skill hygiene checks to `.aether/skills`.
- `cmd/medic_wrapper_test.go` - removed source mirror setup expectations.
- `cmd/agent_mirror_test.go` - deleted.
- `.aether/docs/source-of-truth-map.md` - rewritten for global/local split.
- `CLAUDE.md`, `AGENTS.md`, `.codex/CODEX.md`, `.opencode/OPENCODE.md`, `README.md` - partially updated.
- `.aether/docs/publish-update-runbook.md`, `.aether/docs/skill-catalog-curation-plan.md` - partially updated.
- `.aether/docs/verification-phase5.txt` - deleted.
- `.aether/commands/build.yaml`, `.aether/commands/lay-eggs.yaml`, build/organize/export wrappers and build playbooks - partially updated.
- Deleted mirror directories listed above.

## Exact Prompt To Continue

Use this prompt for a single next context if not splitting work. If splitting
work, use the two prompts in `.handoff-runtime-agent.md` and
`.handoff-surface-docs-agent.md` instead.

```text
We are in /Users/callumcowie/repos/Aether. Continue the "Global Agents/Commands, Local-Only Repo State" refactor from .continue-here.md.

First read .continue-here.md, then run git status --short and inspect actual file contents before assuming prior edits exist. The worktree is dirty with unrelated pre-existing changes; do not reset, checkout, or revert broad paths. Do not commit.

Mission:
- Shared Aether assets live once in the global hub/platform homes:
  - agents, commands, shipped skills, templates, docs, utils, workers.md, exchange, references
- Target repos keep only repo-local generated state:
  - .aether/data, dreams, oracle, locks, QUEEN.md, and custom repo skills
- Deleted source mirrors are intentional:
  - .aether/agents-claude/
  - .aether/agents-codex/
  - .aether/commands/claude/
  - .aether/commands/opencode/
  - .aether/skills-codex/

Critical product requirement:
Preserve Aether's rich command wrapper behavior, visual ceremony, and detailed workflow instructions. The live lay-eggs wrappers were restored after an over-simplification; do not flatten them again. Patch only stale assumptions/paths. Do not replace detailed wrappers with tiny CLI stubs unless the runtime truly owns the full behavior.

Concrete next steps:
1. Verify current actual code with rg:
   rg -n "skills-codex|agents-claude|agents-codex|commands/claude|commands/opencode" cmd embedded_assets.go --glob '!cmd/*_test.go'
2. Complete runtime wiring:
   - cmd/install_cmd.go: install global Codex skills from .aether/skills; setupInstallHub must publish canonical platform sources directly into hub, while excluding deleted source mirrors from .aether hub sync.
   - cmd/update_cmd.go: update should create local scaffolding, prune stale generated repo-local platform assets and shipped skills, preserve custom repo skills/state, and not copy agents/commands/shipped skills/templates/docs/utils/workers/exchange/references into target repos.
   - cmd/setup_cmd.go: same local-only setup behavior.
   - cmd/skills.go: remove repo-local .aether/skills-codex scan; keep .aether/skills source and hub/global shipped skill scan as appropriate.
   - cmd/medic_wrapper.go: remove source mirror parity checks; keep canonical platform source checks and hub publish integrity.
   - cmd/queen.go: restore/reimplement `aether queen-compose` and tests. The current CLI returns unknown command. Do not treat deleted `.aether/commands/claude|opencode` mirror wrappers as required.
   - embedded_assets.go already has mirror embeds removed, verify it still embeds canonical sources.
3. Fix tests to match the final behavior without deleting meaningful coverage:
   - install publishes canonical sources to hub/global homes
   - setup/update do not copy global assets into target repos
   - stale generated repo-local assets are pruned safely
   - custom repo skills and .aether/data/dreams/oracle/locks/QUEEN.md are preserved
   - source-check no longer expects mirrors
4. Audit wrapper files:
   - lay-eggs is already restored to rich wrapper form; preserve it.
   - Audit build, organize, export-signals, import-signals wrappers for stale local asset assumptions.
   - Keep visual ceremony and detailed workflow instructions.
5. Update docs only after code behavior is stable. The docs should say:
   - edit canonical source files in the Aether repo
   - publish/install writes to global hub/platform homes
   - target repos are local-only state
   - no source mirrors remain
6. Verification:
   - gofmt modified Go files
   - go test ./cmd -run TestDoesNotExist
   - focused tests for install/setup/update/source-check/medic/codex
   - if focused tests pass, consider go test ./... -race

Be explicit in the final report about any tests blocked by unrelated pre-existing dirty work.
```
