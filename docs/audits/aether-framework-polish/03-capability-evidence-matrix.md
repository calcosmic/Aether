# Capability Evidence Matrix

Status vocabulary: **proven**, **limited**, **partial**, **disconnected**, **stubbed**, **docs-only**, **duplicated**, **obsolete**, **regressed**, **unclear**.

Severity is the consequence if the advertised behavior is relied upon: P0 framework credibility, P1 reliable daily use, P2 differentiation, or Later.

## Unreleased Remediation Delta

This table records changes implemented after the forensic baseline below. Baseline rows remain unchanged so later sessions can distinguish original evidence from remediation.

| Capability | Current unreleased behaviour | Evidence | Tests | Status | Remaining severity | Recommendation |
| --- | --- | --- | --- | --- | --- | --- |
| New plan acceptance | Requires every phase/task criterion to bind artifacts and/or supported checks; saved policy is `bound_v1` | `pkg/colony/colony.go`; `cmd/criterion_evidence.go`; plan/finalizer paths | Unit plus compiled lifecycle | proven | P1 | Add richer semantic/UI evidence types; retain explicit legacy migration |
| Legacy plan migration | Infers and persists `legacy_unbound`; preserves byte-exact pre-migration state and emits rollback command | `cmd/state_load.go`; `cmd/command_truth.go` | Unit plus compiled upgrade | proven | P1 | Add versioned schema migration registry before next schema change |
| Direct build truth | Nonterminal workers and provider-backed implementation no-ops cannot commit `BUILT`; read-only discovery requires durable task reports | `cmd/codex_build.go`; attempt journal | Adapter matrix plus compiled lifecycle | proven | P1 | Extend fault injection around state-commit boundaries |
| Wrapper build recovery | Accepted completion is staged in Go-owned attempt storage before finalization; resume points at the exact packet | `cmd/build_attempt.go`; `cmd/codex_build_finalize.go`; TS host | Go/TS integration and replay tests | proven | P1 | Apply the same attempt protocol to planning, colonization, and seal wrappers |
| Criteria verification | Fresh file fingerprints and required executed checks gate advancement; skipped Watcher/check does not satisfy a binding | `cmd/criterion_evidence.go`; `cmd/codex_continue.go` | Unit and compiled build/continue | proven | P1 | Add behavior-level evidence beyond files and command exit status |
| Interrupted direct build | Dead attempt is exposed and force-redispatched without false success; live PID routes to watch | `cmd/build_attempt.go`; `cmd/context.go` | Hard-kill compiled test | proven | P1 | Selectively resume terminal workers only after ownership semantics are transactional |
| Worktree overlap | Second claimant of the same path is blocked and preserved as orphan evidence | `cmd/codex_build_worktree.go` | Controlled overlap test | limited | P1 | Preallocate ownership and reconcile the entire wave atomically |
| Hive influence | Automatic read/promotion is off unless `AETHER_HIVE_POLICY` explicitly enables it | `cmd/hive_policy.go`; Hive/context/seal readers | Unit plus TS injector | proven safe default | P1 | Redesign evidence, contradiction, stable repo identity, revocation, decay, and locks before default enablement |
| Platform parity claims | Machine-readable contract discloses direct/indirect routing, command-surface, progress, and permission limitations | `pkg/codex/platform_contract.go`; `cmd/command_guide.go` | Contract tests | proven disclosure | P1 | Add live release-candidate contract tests; do not label permissions equivalent |
| Install-to-seal core | Compiled binary and isolated provider process complete the full deterministic core journey | `cmd/blackbox_harness_test.go`; adapter fixture | Black-box pass | proven in source candidate | P0 release | Run the same suite against packed npm and release archives |
| Update/migration | Force update preserves Queen, custom skill, and state; migration backup is byte exact; binary/hub versions agree | compiled upgrade contract | Black-box pass | proven in source candidate | P0 release | Add N-1 released artifact, interrupted activation, and binary rollback matrix |
| Completed resume route | Unsealed `COMPLETED` routes to `aether seal`; only `Crowned Anthill` routes to entomb | `cmd/lifecycle_helpers.go`; `cmd/recovery_snapshot.go` | Unit plus compiled lifecycle | proven | P1 | Keep lifecycle next-action assertions in every journey |
| Production subprocess owner | Direct Go and TS-host subprocess runs select, preflight, launch, and parse providers through the Go adapter; TS retains wave coordination only | `cmd/internal_worker_adapter.go`; `.aether/ts-host/src/worker-dispatch.ts` | Go unit, compiled black-box, TS delegation/static-import tests | proven | P1 | Keep wrapper-native launch as an explicit separate run mode; add run-owner journal binding |
| Explicit provider pin | A valid `AETHER_WORKER_PLATFORM` pin is absolute and fails when unavailable instead of falling back | `pkg/codex/platform_dispatch.go` | Unit plus compiled adapter | proven | P1 | Extend to release-candidate live-provider matrix |
| Retired `control-ts` | Private package reports all adapters unavailable, phase execution fails without completion, and tests write only to temporary/quarantine state | `control-ts/src/retired.ts`; adapters; memory store | 133 tests plus live-state hash guard | proven unavailable | P0 resolved locally | Remove package after salvaging any still-useful schemas |

## Lifecycle And Public Commands

| Capability | Intended behaviour | Actual behaviour | Evidence | Tests | Status | Severity | Recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `lay-eggs` | Initialize coherent repo scaffold | Creates minimal local scaffold and global references; generated docs still mention retired repo-local agents/skills; scaffold itself appears as unreconciled implementation | `cmd/setup_cmd.go`; greenfield journey | Go tests; live temp repo | limited | P1 | Make setup idempotent, versioned, and excluded from build evidence; generate docs from installed topology |
| `init` | Turn an imperfect goal into durable colony state | Reliably creates goal/session/charter state when called directly; richer intent depends on wrapper interview | `cmd/codex_init.go`; temp journeys | Go tests; live | limited | P1 | Commit typed goal, decisions, unknowns, baseline, and charter in one transition |
| `discuss` | Clarify goal and record decisions | Direct run asked generic implementation questions for an ambiguous notes tool and persisted answers as month-long REDIRECTs | `cmd/discuss_cmd.go`; Journey A | Unit plus live | partial | P1 | Use goal-domain questions; store typed decisions/unknowns, emit signals only for actual steering policy |
| `colonize` | Understand brownfield structure, history, conventions, debt | Scanned 1,133 files and created maps, but provider pin was ignored, fallback workers remained `spawned`, reports were generic, and Git history had no visible influence | `cmd/codex_colonize*.go`; brownfield artifacts | Unit plus Journey B | partial | P0 | Make fallback status honest; require history/convention evidence and confidence/citations |
| `oracle` | Iterative evidence-based research that changes work | Real loop produced durable sources, gaps, confidence and a sound recommendation, but only answered 1/6 questions in one iteration; planning link is opportunistic | `cmd/oracle*.go`; `.aether/ts-host/src/oracle-lifecycle.ts`; Journey A | Extensive unit plus live | limited | P2 | Add typed research decision outputs and explicit plan dependency/invalidation links |
| `plan` | Iterative, research-aware phase plan | Real Route-Setter wrote a strong plan then timed out; canonical state stayed empty and retry erased good artifacts. Synthetic fallback was generic | `cmd/codex_plan*.go`; Journey A | Extensive Go tests; live fail | regressed | P0 | Journal planning attempts; retain accepted partial artifacts; resume result collection |
| `plan --refresh` | Replan after evidence changes | Accepts an evidence-bound revision that preserves the exact completed prefix, replaces and renumbers only unfinished phases, records reason/evidence/history, and rejects stale plan or evidence packets | `cmd/plan_revision.go`; `cmd/codex_plan*.go`; compiled Journey E rerun | Unit, external-finalizer, compiled restart journey | working with limitations | P1 | Run the same journey through real Oracle, Scout, and Route-Setter providers; add typed assumption/decision impact links |
| `assumptions-analyze` | Identify and drive resolution of plan assumptions | Emits three generic template assumptions and a FOCUS; does not invalidate or revise plan | `cmd/assumptions.go`; Journey E | Unit plus live | disconnected | P1 | Link assumptions to phase/task IDs and block or revise affected nodes |
| `phase-insert` | Add corrective work | Can insert one phase; no public split, merge, reorder, dependency rewrite, or plan-change rationale | `cmd/phase_insert.go` | Unit | limited | P1 | Fold into `aether plan revise` transaction |
| `build` | Dispatch real workers and produce implementation evidence | Real subprocesses run. Interrupted run with six failed/cancelled workers returned `ok:true`, state `BUILT`, and `build_completed` | `cmd/codex_build*.go`; Journey D | Many unit tests; live fail | regressed | P0 | Rename intermediate state and derive terminal result strictly from accepted worker/evidence records |
| `build --plan-only` | Expose an exact, resumable worker plan without lifecycle mutation | Produces an inspectable manifest plus durable `awaiting_external` attempt ID/hash; blocks duplicate dispatch, supports explicit supersession, and does not advance `COLONY_STATE.json` | `cmd/codex_build.go`; `cmd/build_attempt.go`; compiled CLI bound-completion test | Unit, compiled CLI, live | proven | P1 | Preserve as the wrapper dispatch boundary; trim enormous embedded prompt payload by default |
| `build --synthetic` | Explicitly simulate lifecycle for tests/offline | Clearly tagged in manifests, but nearby real paths can still degrade or trust insufficient evidence | build/continue code | Unit | limited | P0 | Test-only adapter namespace; never permit production state completion from simulation |
| `build-reconcile` / `reconcile` | Reconcile manual or interrupted changes | Current worktree contains new unreleased command; existing continue supports manual reconciliation but does not bypass claims | `cmd/reconcile.go`, `cmd/codex_continue.go` | Pre-existing dirty tests | unclear | P1 | Finish as journaled baseline adoption with explicit human evidence, not implicit change credit |
| `continue` | Deterministically verify, review, learn, advance | Strong freshness and claim checks exist. Live run completed no application because all test commands were skipped and watcher was skipped | `cmd/codex_continue.go`; Journey G | Extensive tests; live fail | regressed | P0 | No required check may resolve to skipped; bind criteria to executable/evidentiary methods |
| `continue --skip-watchers` | Permit deterministic-only verification | Treats skipped watcher as passed; with no resolved commands it can complete | `cmd/codex_continue.go:1418-1461,2603`; Journey G | Tests codify skip | regressed | P0 | Allow only when deterministic required evidence is non-empty and policy authorizes it |
| Quality gates | Security, quality, coverage, performance decisions | Mixture of deterministic report code and model castes; can be skipped by depth/availability and are not permission-isolated | `cmd/queen_decision.go`, continue code | Unit | partial | P0 | Make required gates explicit per risk policy; separate code results from model judgment |
| `run` | Autopilot remaining build/continue loop | Loops existing lifecycle, inheriting false completion and adapter ambiguity | `cmd/run_cmd.go` | Unit/smoke | limited | P0 | Hide until single-phase journey gates pass; stop on missing evidence or ambiguous status |
| `swarm` | Route work or observe active workers | Compatibility/router behavior plus watch surface; not proof of self-organization | `cmd/swarm_cmd.go`, TS host | Unit/snapshot | limited | P2 | Advanced alias only; use run records as truth |
| `watch` | Show live real workers | Spawn tree/process records exist; stale and narrated statuses have historically required reconciliation | `pkg/agent/spawn_tree.go`, TS dashboard | Unit/snapshot | limited | P1 | Render solely from journaled process/result events with heartbeat and owner PID |
| `status` | Truthful dashboard and next action | Useful canonical summary and unreconciled Git detection; corrupt state emits JSON error with exit 0 | `cmd/status.go`; corruption journey | Unit plus live | limited | P0 | Correct exit status; include run/evidence status and projection freshness |
| `resume` | Restore sufficient current context after interruption | Recovers ordinary build interruption enough for continue; corrupted state recovery from handoff lost all phases and still said `resumed:true` | `cmd/session_cmds.go`; Journey D/corruption | Unit plus live fail | partial | P0 | Replay canonical journal/checkpoint; report partial recovery and lost fields explicitly |
| `pause` | Pause without false advancement | State flag exists; lifecycle/session surfaces vary | state/session commands | Unit | limited | P1 | One paused transition with resumable operation ID |
| `skip-phase` | Emergency abandonment | Explicit force/reason escape hatch | command and tests | Unit | limited | P1 | Preserve, but journal abandoned evidence and downstream impact |
| `seal` | Final verified project summary and knowledge promotion | Rich review/promotion surface, but inherits false COMPLETED state and unsafe Hive | seal commands | Unit/snapshot | partial | P0 | Require release acceptance policy and evidence manifest; Hive promotion off by default |
| `patrol` / `medic` diagnostics | Diagnose corrupted/stale colony | Multiple health checks and recovery hints exist | medic/patrol commands | Unit | limited | P1 | Consolidate as `aether doctor`; never repair without journaled backup/preview |
| `focus` | Prioritize a bounded concern | Stored, deduplicated, reinforced, injected into prompts | pheromone commands; live signal test | Unit plus live | limited | P2 | Add scope, conflict resolution, acknowledgment, and decision trace |
| `redirect` | Enforce hard constraint | Prompt says non-negotiable; not runtime-enforced; can coexist with identical conflicting FOCUS | signal files/skills; live | partial | P0 | Enforce deterministic policy where possible and block unresolved conflict |
| `feedback` | Calibrate future choices | Prompt context with provenance/TTL; influence not measured | signal commands/context | Unit plus live | limited | P2 | Track which decision consumed it and later outcome |
| `pheromones` / display | Show active signal truth | Stored `active` can remain true after expiry until housekeeping; prompt loader and display can disagree | signal commands; live | Unit plus live | regressed | P1 | Compute effective status at read time and show source/scope/conflicts |
| `preferences` | Persist global user preferences | Stores prose in global Queen and injects it | Queen commands/context | Unit | limited | P1 | Separate typed user preferences from wisdom; add edit/delete/scope |
| `history` / `phase` / `flags` | Inspect lifecycle | Useful projections over several files | commands | Unit | limited | P1 | Rebuild from canonical journal and evidence ledger |
| `audit-catalog` | Inventory public command truth | Correctly exposes scale: 396 entries, but seven remain unclassified and mutation/retry metadata are absent from output | catalog JSON | CI/unit | proven | P1 | Add owner, mutation, idempotency, audience; use it to reduce surface |

## Installation, Update, And Release

| Capability | Intended behaviour | Actual behaviour | Evidence | Tests | Status | Severity | Recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `npx aether-colony@latest` | One-command current install | npm latest is `1.0.22`; clean install fails because archive member is uppercase `Aether` and bootstrap searches lowercase | npm registry/archive; live clean install | Seven unit tests miss real artifact | regressed | P0 | Make release matrix publish and install exact package artifact before promotion |
| Go release download | Download correct binary and verify checksum | `v1.0.34` direct download works and verifies checksum | `pkg/downloader`; live install | Unit plus live | proven | P1 | Preserve; add signed provenance/attestation and rollback |
| Embedded `aether install` | Install matching binary and companion assets | Installs embedded `1.0.42` assets before current binary download fails 404; forcing `1.0.34` pairs different versions | `cmd/install_cmd.go`; live | Unit | regressed | P0 | Stage complete versioned bundle, verify agreement, atomically switch symlink/directory |
| `aether update` | Safely refresh initialized repos | Protects local data and has dry-run/force, but companion/hub/binary paths are separate and can drift | `cmd/update_cmd.go`; runbook | Unit | limited | P0 | Resolve one release manifest; migrate in temp; atomically activate or rollback |
| Stable/dev channels | Isolate released and source development | Separate hubs/binaries are implemented; manual workflow is complex | install/publish code/docs; live source install | Unit/live | limited | P1 | Keep dev channel, hide internals behind `aether dev install` |
| Version agreement | One public version | Source/npm dirty `1.0.42`, GitHub `1.0.34`, npm `1.0.22`; workflow may skip npm when token absent | version files, release workflow, registries | CI checks source files only | regressed | P0 | Release must fail unless GitHub, npm, manifest, binary, and install smoke agree |
| Rollback | Restore last working release | Bootstrap replacement path and companion update lack a user-facing atomic rollback contract | npm bootstrap/install code | Unit only | partial | P0 | Keep N-1 bundle until post-install smoke passes; add `aether rollback` |
| Companion generation | Refresh platform agents/commands/docs | Large copies and generated shims work; topology docs can be stale; three platform definitions duplicate | install output/files | Unit/live | limited | P1 | Generate adapters/assets from one canonical role/command schema |

## State, Context, Research, And Memory

| Capability | Intended behaviour | Actual behaviour | Evidence | Tests | Status | Severity | Recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Atomic JSON store | Cross-process safe persistence | Atomic writes and file locks exist; scoped containment is not enforced and several subsystems write directly | `pkg/storage` | Strong unit/race coverage | limited | P0 | Create explicitly scoped stores; prohibit bypass writes for canonical data |
| Canonical colony state | One lifecycle truth | Broad schema is useful but competes with session, handoff, reports, DB, Queen and Git; task schema lacks artifacts/verification evidence | `pkg/colony/colony.go` | Strong unit | partial | P0 | Journal plus materialized state; extend plan graph/evidence references |
| Checkpoints | Restore interrupted transitions | Command-specific checkpoints exist; planning and handoff recovery can lose richer artifacts | `.aether/data/checkpoints`, commands | Unit/live | partial | P0 | Uniform transition journal and resumable result collection |
| Context capsule | Compact correct next-action context | Budget/ledger/sanitization are strong; new goal capsule was only 24 words and projections can be stale | `cmd/colony_prime_context.go`; Journey A | Strong unit | limited | P1 | Define sufficiency contract and trace each statement to state/evidence revision |
| Session freshness | Detect changed session/Git baseline | Baseline and age checks exist; reports can show contradictory handoff existence/removal | session commands; live | Unit/live | partial | P1 | One workspace fingerprint and projection revision |
| Oracle artifacts | Durable reusable research | State/plan/questions/synthesis/gaps/sources persist and are inspectable | Oracle code/live | Unit/live | proven | P2 | Promote typed conclusions only through decision records |
| Research-to-plan | Research changes plan | Completed Oracle output now exposes a non-automatic revision command; revision manifests require repository-local evidence paths and content hashes and inject those inputs into planners. Oracle still does not emit typed affected assumption/decision IDs | `cmd/oracle_loop.go`; `cmd/plan_revision.go`; compiled Journey E rerun | Unit plus compiled synthetic replan | limited | P1 | Add typed Oracle conclusion and affected-node records, then prove the full live provider loop |
| Decisions | Preserve why choices were made | State supports generic decisions, but discussion, plan revision, and research conclusions use separate prose/artifacts | state and commands | Unit | partial | P1 | Typed decision log with alternatives, evidence, scope, supersession |
| Assumptions | Track uncertainty | Persisted separately and emits signals; no phase invalidation | assumptions command | Unit/live | disconnected | P1 | Typed lifecycle entity with validation and impact graph |
| Project instincts | Learn repeated project patterns | Richer trust/decay code exists, but uncached consolidation test fails and duplicate state schemas exist | `pkg/memory`, instinct commands | Failing test | regressed | P0 | Stop automatic promotion; unify pipeline and require evidence/provenance |
| Project Queen | Coordinate and preserve project wisdom | Mixes readable wisdom, learning output, preferences and identity; not canonical | `cmd/queen.go`, local `QUEEN.md` | Unit | limited | P1 | Make Queen a generated project coordination view |
| Global Queen | User preferences and wisdom | Global prose can contaminate project prompts and mixes types | global Queen commands/context | Unit | partial | P0 | Restrict to typed user preferences; no project facts |
| Hive store | Trusted cross-project wisdom | Arbitrary source strings raise confidence to 0.95; no evidence, contradiction, decay, revocation or lock | `cmd/hive.go`; Journey F | Unit misses adversarial flow | regressed | P0 | Disable auto-injection/promotion; redesign as evidence-backed opt-in store |
| SQLite learning store | Durable queryable learning | Separate learning projection with its own migrations/tests | `pkg/learn` | Strong unit | duplicated | P1 | Keep only as rebuildable index, not authority |
| Event bus/trace | Reconstruct activity | Several JSONL/event systems exist; `control-ts` event bridge is in-memory stub | `pkg/events`, trace, `control-ts/src/events` | Unit | duplicated | P1 | One append-only journal with typed event families |
| Midden | Track failures | Persistent records and review commands exist; effect on deterministic routing is limited | midden commands | Unit | limited | P2 | Store failure evidence and explicit policy outcomes, not free-text folklore |

## Platform And Orchestration

| Capability | Intended behaviour | Actual behaviour | Evidence | Tests | Status | Severity | Recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Direct Go orchestrator | Reliable lifecycle on Codex and fallback platforms | Real and extensively implemented; duplicates host behavior and embeds prompts/routing/ceremony | `cmd/codex_*` | Extensive | limited | P0 | Retain transition core; choose whether loop remains here or moves behind adapter, not both |
| `.aether/ts-host` | Platform-aware orchestration over Go finalizers | Substantial lifecycle/Oracle/dispatch implementation; 512 tests pass, many via fakes; checked-in TS/JS/dist duplicates | host source/tests | Pass/typecheck | limited | P0 | Candidate single host during migration; remove generated source duplicates and require live adapter suite |
| `control-ts` forensic baseline | New control plane and adapter layer | At audit time all four adapters returned completed empty results and available true; the integration test touched live repo state. The unreleased remediation now retires it fail-closed and isolates state. | `control-ts/src`; unreleased delta above | Baseline failure; remediation 133/133 pass | obsolete/quarantined | P0 resolved locally | Delete after useful schemas are migrated; never restore it as a second orchestrator |
| Claude adapter | Native Claude workers with enforceable coarse permissions | Go uses named agents; read-only uses `plan`, workspace-write uses `acceptEdits` plus fail-closed native sandboxing; no `bypassPermissions` | `pkg/codex/platform_dispatch.go`; permission tests | Deterministic fixtures; live smoke still manual | working with limitations | P1 | Bind durable run identity; retain live release smoke |
| OpenCode adapter | Native OpenCode workers with enforceable coarse permissions | Go launches restricted `aether-worker-router`, attests target definitions, denies external directories, and asks Task to delegate once | dispatcher; `.opencode/agents/aether-worker-router.md` | Static/fixture attestation; live smoke still manual | working with limitations | P1 | Attest installed provider state in live smoke; retain honest indirect-routing limitation |
| Codex adapter | Native Codex workers with caste profile sandbox | Real `codex exec`, structured schema, read-only/workspace-write selected per caste; no writable `CODEX_HOME` addition | `pkg/codex/worker.go`; permission tests | Strong subprocess fixtures plus live CLI history | working with limitations | P1 | Add durable run identity; document secondary wrapper surface |
| Platform selection | Honor explicit pin, otherwise deterministic order | Explicit valid pins are now hard and unavailable pins fail without fallback | `pkg/codex/platform_dispatch.go` | Unit plus compiled black-box | proven | P1 | Preserve as release invariant |
| Claude/OpenCode wrappers | Same lifecycle semantics with native UX | Files are near-identical 881-line sets for seven core commands, duplicating orchestration guidance around Go | wrapper files | Snapshot/parity | duplicated | P1 | Generate thin wrappers from platform contract; no state or verification logic |
| Typed caste permissions | Every worker requests a host-enforced profile or is rejected | Scout/Includer use repository-read-only; other current castes use workspace-write; narrow test/ledger/survey/doc rules are visibly behavioral; scoped/test profiles reject | `pkg/codex/permission_profile.go`; manifests; hidden adapter | Matrix, sandbox args, OpenCode attestation, TS request tests | working with limitations | P1 | Prove narrow profiles before promoting behavioral scopes to guarantees |
| Codex TOML agents | Discoverable specialized agents | 27 definitions exist; the Go adapter now maps canonical coarse profiles, while narrower role behavior remains prompt-defined | `.codex/agents`, worker code | Definition and permission tests | limited | P1 | Generate from canonical roles; prove unique role value before expansion |
| Ceremony | Visible truthful progress | Rich visual renderer and snapshots; `build_completed` can mean packet prepared after failed workers | visuals/events/live | Snapshot-heavy | regressed | P0 | Render only committed run events; reserve completed for accepted terminal transition |
| Worktree parallelism | Concurrent independent work with safe merge | Real concurrency exists; overlapping untracked file edits silently last-win while both workers are accepted | worktree code; Journey G | Unit misses content conflict | regressed | P0 | Predeclare ownership; three-way conflict/content hash detection; no merge acceptance on overlap |
| In-repo parallelism | Multiple workers in shared tree | Builder task waves intentionally serialize in-repo | execution plan code/live | Unit/live | docs mismatch | P1 | Say serial shared-tree mode; call parallel only worktree/independent read work |

## Caste Audit

All 27 castes have definitions on Claude, OpenCode, and Codex; OpenCode also has one infrastructure-only router. Coarse repository-read-only/workspace-write boundaries are now typed and host-enforced. Any narrower prompt distinction remains behavioral and is not a security boundary.

| Capability | Intended behaviour | Actual behaviour | Evidence | Tests | Status | Severity | Recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Queen | Orchestrate phases and workers | Runtime functions, hard-coded scoring/budget, prompts, state reports, and memory branding share the role | Queen/build code | Extensive unit | duplicated | P0 | Keep identity; reduce to coordination projection plus policy decision record |
| Route-Setter | Produce structured plans | Real planner produced excellent plan before timeout; output was not committed | plan dispatch/live | Unit/live | limited | P0 | Keep core Planner role with resumable result acceptance |
| Scout | Repository/research exploration | Real planning scout and generic research role; overlaps Oracle/Colonizer/Archaeologist | plan/agent files | Unit/live | limited | P1 | Keep core Investigator; use skills for lenses |
| Builder | Implement bounded task | Real file/tool work and claims; also assigned investigation/test tasks by task-caste fallback | build code/Journey C | Strong | limited | P0 | Keep core; enforce ownership and implementation evidence |
| Watcher | Independent read-only verification | Independent prompt exists, but permissions allow writes and watcher may be skipped/auto-skipped | continue/agent files | Strong plus live fail | regressed | P0 | Keep core Verifier with enforced read-only sandbox and veto evidence |
| Oracle | Deep iterative research | Real standalone loop; build can also spawn it by keyword/mode | Oracle/build code | Strong/live | limited | P2 | Keep advanced research mode, not default build caste |
| Architect | Structural design | Keyword/risk-selected pre-wave prompt; no unique tools/permissions | caste relevance/build | Unit/live | partial | P1 | Optional skill or review policy |
| Tracker | Root-cause investigation | Selected for bug text, but build also assigns Builder to investigation task; no write isolation | Journey C | Unit/live plan | partial | P0 | Core Investigator mode with read-only evidence phase |
| Probe | Tests/coverage | Always required in observed build plus watcher; overlaps test-writing and verification | build policy/agent | Unit/live | duplicated | P1 | Merge test verification into Watcher; optional write-enabled test-author mode |
| Auditor | Quality/compliance | Often hard-coded required post-wave; overlaps Watcher/Gatekeeper | build policy | Unit | duplicated | P1 | Reviewer skill/policy, not default caste |
| Gatekeeper | Supply chain/security | Hard-coded required in bug build despite low risk; prompt/tool boundary not unique | Journey C | Unit | partial | P0 | Risk-triggered security policy with least-privilege tools |
| Includer | Accessibility | Keyword-selected reviewer, no unique runtime capability | agent/relevance | Definition/unit | partial | P2 | Optional accessibility review skill |
| Measurer | Performance | Heavy-depth conditional, no unique profiler guarantee | build policy | Unit | partial | P2 | Optional adapter capability/skill when benchmarks exist |
| Chaos | Resilience | Policy can add even to light maintenance build; no isolated environment | Journey C/build policy | Unit | partial | P2 | Extension requiring sandbox and explicit destructive-test policy |
| Archaeologist | Git history | Pre-wave history role; colonization did not actually use history in observed fallback | build policy/agent | Unit | disconnected | P1 | Git-history skill for Investigator/Colonizer |
| Weaver | Refactoring | Prompt-specialized Builder | agent/relevance | Definition/unit | partial | P2 | Builder skill |
| Keeper | Curate project knowledge | Overlaps Sage/Queen/memory pipeline; pre-wave on simple implementation | Journey A | Unit | duplicated | P1 | Merge with Sage into optional Knowledge Curator after trust redesign |
| Sage | Trend/wisdom synthesis | Overlaps Keeper and learning pipeline | agent/build policy | Definition/unit | duplicated | P1 | Merge with Keeper |
| Medic | Diagnose colony health | Overlaps deterministic doctor and Fixer | agent/relevance | Unit | duplicated | P1 | Deterministic `doctor`; model-assisted repair optional |
| Fixer | Recover gate failures | Overlaps Builder/Medic; prompt defines autonomy modes not uniformly enforced | agent/relevance | Unit | partial | P0 | Builder repair mode gated by explicit approval policy |
| Chronicler | Documentation | Prompt-specialized Builder | agent/relevance | Definition/unit | partial | P2 | Documentation skill |
| Porter | Publish/push/deploy | Distinct high-risk delivery job, but prompt permissions are not an enforcement boundary | agent/seal | Unit | partial | P0 | Optional delivery plugin with explicit user approval/capabilities |
| Ambassador | External integration | Keyword-selected architecture role; no unique secret/network boundary | agent/relevance | Unit | partial | P2 | Integration skill with scoped credentials when implemented |
| Surveyor Nest | Map structure | Four survey workers plus fallback synthesis | colonize/agent | Unit/live | duplicated | P1 | Merge into one Colonizer with structure lens |
| Surveyor Disciplines | Map conventions | Same runtime/tools, separate prompt | colonize/agent | Unit/live | duplicated | P1 | Colonizer conventions lens |
| Surveyor Pathogens | Find debt | Observed fallback scanned planning docs and produced noise | Journey B | Unit/live | regressed | P1 | Colonizer health lens with source filters |
| Surveyor Provisions | Map dependencies | Same runtime/tools, separate prompt | colonize/agent | Unit/live | duplicated | P1 | Colonizer dependency lens |

## Skills And Extensibility

| Capability | Intended behaviour | Actual behaviour | Evidence | Tests | Status | Severity | Recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 86 shipped skills | Match up to role/task/workflow/codebase | Matching/injection is real and budgeted; observed bug plan injected unrelated API/Supabase skills and enormous prompt text | skill commands; Journey C | Unit/live | limited | P1 | Evaluate precision; cap rendered details; keep only behaviorally tested skills |
| Custom/project/global skills | User extension | Locations and update protections exist; trust and permission model is prompt-only | install/skill code | Unit | limited | P0 | Signed/source-attributed packages, explicit capabilities, project opt-in |
| Custom castes | Extend role roster | Three platform formats and hard-coded Go routing make this not a stable plugin contract | agent/routing files | Definition tests | docs-only | P2 | Do not document as stable until canonical schema and adapter mapping exist |
| Lifecycle hooks/providers/policies | Extensible runtime | Internal seams exist but no versioned external compatibility contract | host/control/core code | Internal tests | unclear | Later | Keep private until core acceptance passes |
