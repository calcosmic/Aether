id: archaeologist
role: archaeologist
prompt_file: ../../colony/prompts/archaeologist.md
allowed_tools:
- read
- bash
- grep
- glob
- write
tier: niche
model: opus
color: orange
description: Use this agent before modifying code in an area with complex or uncertain
  history — its primary job is regression prevention. Excavates git history to surface
  past bugs that were fixed, deliberate architectural choices that look like oddities,
  and areas that have been unstable. Returns a stability map and tribal knowledge
  report so you do not undo previous work. Do NOT use for implementation (use aether-builder)
  or refactoring (use aether-weaver).
