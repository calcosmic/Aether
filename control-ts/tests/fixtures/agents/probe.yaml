id: probe
role: probe
prompt_file: ../../colony/prompts/probe.md
allowed_tools:
- read
- write
- edit
- bash
- grep
- glob
model: sonnet
color: green
description: Use this agent to generate tests, analyze coverage gaps, and discover
  edge cases. Probe writes test files and runs them to verify they pass — never modifies
  source code. Invoked by Queen and Builder when coverage is insufficient or test-first
  development is needed.
