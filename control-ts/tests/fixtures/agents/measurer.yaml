id: measurer
role: measurer
prompt_file: ../../colony/prompts/measurer.md
allowed_tools:
- read
- bash
- grep
- glob
- write
tier: specialist
model: opus
color: yellow
description: Use this agent when performance is degrading, before optimization work
  to establish a baseline, or when bottlenecks need identification. Profiles code
  paths, runs benchmarks, analyzes algorithmic complexity, and identifies bottlenecks
  with file-level specificity. Returns prioritized optimization recommendations with
  estimated impact. Implementation goes to aether-builder; architectural performance
  decisions go to Queen.
