id: auditor
role: auditor
prompt_file: ../../colony/prompts/auditor.md
allowed_tools:
- read
- grep
- glob
- write
model: opus
color: green
description: Use this agent for code review, security audits, and compliance checks.
  Strictly read-only — returns structured findings (file, line, severity, category,
  description, suggestion). For security escalations, routes to Queen. Do NOT use
  for fixes (use aether-builder) or test additions (use aether-probe).
