id: weaver
role: weaver
prompt_file: ../../colony/prompts/weaver.md
allowed_tools:
- read
- write
- edit
- bash
- grep
- glob
tier: specialist
model: sonnet
color: purple
description: Use this agent to refactor code without changing behavior. Weaver runs
  tests before and after every refactoring step — if tests break, it reverts immediately.
  Do NOT use for new features (use aether-builder) or bug fixes (use aether-tracker
  + aether-builder).
